*** UID:0001VJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// PatchPane2 field declarations are emitted by [UID:0000AA][PatchPane2](by-class/PatchPane2.md); this layout evidence page has no duplicate standalone type declaration.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

# PatchPane2 Layout

## Status

- Confidence: source-ready for the complete `0x294` object size, inheritance/EBO boundary, vtable subobject offsets, tail member types/order, and consumer-backed source-facing names.
- Size: `0x294` / 660 bytes (Verified with `int_convert.py`) from scalar deleting destructor evidence and current generated metadata.
- Source owner: [UID:0000MH][PatchPane](by-file/PatchPane.md)
- Class: [UID:0000AA][PatchPane2](by-class/PatchPane2.md)
- Rebuild handling: `source-declared/generated-binary`; the source class declaration and field ordering are required, while exact vtable bytes and compiler cleanup tables should be regenerated by the compiler/linker.
- Reconstructable: yes, as source-declared class layout semantics. The standalone managed comment prevents duplicate type emission because [UID:0000AA][PatchPane2](by-class/PatchPane2.md) now owns the complete human class declaration.

## Assignment Gate

`AUTOGEN_PARENT_UID` remains [UID:0000AA][PatchPane2](by-class/PatchPane2.md). This layout page, its direct class parent, and the [UID:0000MH][PatchPane](by-file/PatchPane.md) source route are synchronized at `92/94` by the UID0002ZP callback.

The class parent is the narrowest correct owner because the offsets describe `PatchPane2` object storage rather than a file-level helper record. The `PatchPane` file page remains the source module route for the class and the related patch/update family.

## Layout

| Offset | Type / role | Evidence |
| --- | --- | --- |
| `+0x000` | `DialogPane` primary/base region | Constructor calls the dialog constructor and writes primary vtable `0x00621f40`. |
| `+0x0a0` / 160 (Verified with `int_convert.py`) | secondary callback/listener subobject vtable | Constructor writes vtable `0x00621fa0`; adjustor thunk `0x0054934b` subtracts `0xa0`. Slots include the work-event predicate and main loop. |
| `+0x0a4` / 164 (Verified with `int_convert.py`) | tertiary callback/action subobject vtable | Constructor writes vtable `0x00621fd0`; adjustor thunk `0x00549356` subtracts `0xa4`. |
| `+0x269-0x26b` | alignment/padding after generated `DialogPane` body | Generated metadata exposes three undefined bytes before aligned tail fields. |
| `+0x26c` | `m_finalPatchPath`, final destination/request leaf path string, currently initialized from `Patcher.exe` | Constructor initializes it with `sub_582B20`, assigns `Patcher.exe`, `SendDownloadRequest` uses it in the formatted request path, and main-loop/close-rename code deletes/renames this final path. |
| `+0x270` | `m_tempPatchPath`, temporary download path string, initialized from `Patcher.exe.new` | Constructor assigns `Patcher.exe.new`; main loop opens this path for writing and rename helpers use it as the source temp path. |
| `+0x274` | `m_patchFileNames._Myfirst`, file-name vector begin | Constructor zeroes vector triplet and pushes UTF-16 file names parsed from the packed input buffer. |
| `+0x278` | `m_patchFileNames._Mylast`, file-name vector end | Used with `+0x274` to compute vector length in the main loop. |
| `+0x27c` | `m_patchFileNames._Myend`, file-name vector capacity | Destroyed with the vector cleanup in scalar deleting destructor `0x00549370`. |
| `+0x280` | `m_currentDownloadIndex`, current file-name index | `SendDownloadRequest` selects `vector[index]`; main loop increments or resets it. |
| `+0x284` | `m_activePatchFile`, active output `FILE*` | Main loop stores the opened temp file here; `ClosePatchFile`, close/rename helper, and destructor close and clear it. |
| `+0x288` | `m_downloadPassCount`, retry/pass counter | Main loop increments it after cycling through the vector and fails after the observed `0x0a` threshold. |
| `+0x28c` | `m_patchVersionMajor`, patch/download version component 0 | Constructor copies `a2[0]`; `SendDownloadRequest` formats it with `%d/%d/%s`. |
| `+0x290` | `m_patchVersionMinor`, patch/download version component 1 | Constructor copies `a2[1]`; `SendDownloadRequest` formats it with `%d/%d/%s`. |

## Input Buffer

The constructor takes a packed patch/download descriptor:

- `uint16_t[0]` and `uint16_t[1]` become the two version/request path components at `+0x28c` and `+0x290`.
- byte `+0x04` is the number of file names.
- file names begin at byte `+0x05` and advance in `0x100` / 256-byte records (Verified with `int_convert.py`).

## IDA Evidence

- Constructor `0x00548690` writes `dword_69BA2C`, installs the three vtables, initializes strings at `+0x26c` and `+0x270`, initializes vector triplet `+0x274/+0x278/+0x27c`, zeroes `+0x280/+0x284/+0x288`, and copies version fields at `+0x28c/+0x290`.
- Main loop `0x00548a80` is entered through the secondary `+0xa0` subobject, so decompiled offsets such as `this + 460` and `this + 464` correspond to full-object offsets `+0x26c` and `+0x270`.
- Raw advance helper `0x00549020` uses full-object offsets `+0x274`, `+0x278`, `+0x280`, and `+0x288`.
- Close/rename helper `0x00549080` uses full-object offsets `+0x26c`, `+0x270`, and `+0x284`.
- Destructor `0x00549370` closes `+0x284`, destroys the vector at `+0x274`, destroys strings at `+0x270` and `+0x26c`, and clears [UID:0000RZ][g_pPatchPane2](by-global/g_pPatchPane2.md).

2026-05-31 IDA MCP recheck:

- `lookup_funcs` confirms modeled starts and sizes for constructor `0x00548690-0x00548a06`, main loop `0x00548a80-0x00548f1a`, close/rename helper `0x00549080-0x00549214`, send request `0x00549220-0x005492cf`, scalar deleting destructor `0x00549370-0x0054940f`, and the two adjustor thunks `0x0054934b-0x00549356` / `0x00549356-0x00549361`.
- Constructor disassembly writes the primary vtable at `+0x000`, secondary vtable at `+0x0a0`, and tertiary vtable at `+0x0a4`; it initializes `+0x26c`/`+0x270` as string objects, zeroes vector/counter/file fields through `+0x288`, and copies input words to `+0x28c/+0x290`.
- Constructor disassembly parses `*((byte*)input + 4)` names starting at `input + 5`, advancing by `0x100` bytes per name and appending converted strings into the `+0x274/+0x278/+0x27c` vector.
- `xrefs_to` confirms vtable constants `0x00621f40`, `0x00621fa0`, and `0x00621fd0` are written by the constructor and destructor and referenced by setup code; `0x0054934b` and `0x00549356` are referenced from the secondary/tertiary vtables.
- `xrefs_to 0x0069ba2c` confirms singleton writes/clears from the constructor, singleton clear helper, and destructor.
- `callers` confirms the constructor is reached from application startup/patch decision function `0x004f6d80`; `SendDownloadRequest` is called from the constructor and main-loop retry/progress cases.
- Destructor disassembly explicitly pushes `0x294` in the vector-delete path, upgrading the class-size evidence from generated metadata to IDA-verified code evidence.
- String search confirms the runtime uses `patcher.exe` at `0x00612f2c`; final string object contents are still documented semantically because one temp-path literal is represented through local string helper storage and not yet named as a standalone string.

2026-06-13 A001 live IDA MCP refresh:

- Active session `a001_goal2_0002UM` over `NexusTK.exe` reconfirmed the current modeled boundaries: constructor `sub_548690` at `0x00548690` size `0x376`, main loop `sub_548A80` at `0x00548a80` size `0x49a`, predicate `sub_548F30` at `0x00548f30` size `0x11`, close/rename helper `sub_549080` at `0x00549080` size `0x194`, send-request helper `sub_549220` at `0x00549220` size `0xaf`, clear helper `sub_549340` at `0x00549340` size `0x0b`, adjustor thunks `0x0054934b` and `0x00549356` size `0x0b` each, and scalar deleting destructor `sub_549370` at `0x00549370` size `0x9f`. `lookup_funcs 0x00549020` still reports `Not a function`, matching the raw-helper caveat.
- Constructor disassembly at `0x00548709`, `0x0054870f`, and `0x00548719` writes primary, secondary, and tertiary `PatchPane2` vtables at offsets `+0x000`, `+0x0a0`, and `+0x0a4`; destructor disassembly repeats the same vptr restore at `0x0054937c`, `0x00549382`, and `0x0054938c`.
- Current `xrefs_to` confirms the vtable-base store sets: `0x00621f40` from `0x00548709`, `0x00548a19`, and `0x0054937c`; `0x00621fa0` from `0x0054870f`, `0x00548a1f`, and `0x00549382`; and `0x00621fd0` from `0x00548719`, `0x00548a29`, and `0x0054938c`.
- Current slot-target refs confirm the layout-backed callback slots: `0x00548f30 <- 0x00621fb4`, `0x00548a80 <- 0x00621fb8`, `0x0054934b <- 0x00621fa0`, `0x00549356 <- 0x00621fd0`, and `0x00549370 <- 0x00621f40` plus calls from the two adjustor thunks.
- Constructor disassembly initializes the tail state exactly as modeled: `0x00548728` addresses `+0x270`, `0x0054873c` addresses the vector triplet at `+0x274`, `0x00548756`/`0x00548760`/`0x0054876a` zero `+0x280`/`+0x284`/`+0x288`, `0x00548777` and `0x00548781` copy input words into `+0x28c` and `+0x290`, and `0x00548989` advances parsed names by `0x100` / 256 bytes (Verified with `int_convert.py`).
- Main-loop and helper disassembly agree with the field model: `0x00548c4c`/`0x00548c52`/`0x00548c58` use vector end/begin/current index, `0x005490bf` clears `+0x284`, `0x005490d6`/`0x0054910b` address final path `+0x26c`, `0x00549123` addresses temp path `+0x270`, `0x00549254` and `0x0054925d` push version fields `+0x290` and `+0x28c`, and `0x0054926e`/`0x00549274` select vector index/begin for the current file name.
- Raw advance-body disassembly still uses `+0x274`, `+0x278`, `+0x280`, and `+0x288`, compares the retry/pass counter with `0x0a` / 10 (Verified with `int_convert.py`), and has no xrefs to `0x00549020`.
- Destructor disassembly closes and clears `+0x284`, destroys the vector at `+0x274`, destroys strings at `+0x270` and `+0x26c`, clears `g_pPatchPane2` at `0x005493d0`, and pushes `0x294` / 660 (Verified with `int_convert.py`) in the vector-delete path.

## 2026-06-27 B009 Main-Loop Field Name Sync

B009's accepted source-quality implementation for [UID:0001EV][0x00548a80-0x00548f1a.PatchPane2MainLoop](by-memory/0x00548a80-0x00548f1a.PatchPane2MainLoop.md) promotes the layout names above from generic roles to source-facing inferred field names for the formal `PatchPane2::HandleDownloadWorkEvent` C++ block. MCP session `398b87c1` reconfirmed the secondary-subobject offset translation used by the emitted method: decompiler offsets `+0x1cc/+0x1d0/+0x1d4/+0x1d8/+0x1dc/+0x1e0/+0x1e4/+0x1e8` map back to owner offsets `+0x26c/+0x270/+0x274/+0x278/+0x27c/+0x280/+0x284/+0x288` because the work-event handler is called through the `+0xa0` secondary callback subobject.

These names remain documented as inferred source-facing names, not recovered original private spellings. They are accepted for current generated source because they match the object roles: `m_finalPatchPath` is displayed and used as delete/rename target, `m_tempPatchPath` is opened/written and renamed as source, `m_patchFileNames` supplies the download vector, `m_currentDownloadIndex` selects the next entry, `m_activePatchFile` tracks the live `FILE *`, and `m_downloadPassCount` caps retry passes at `10`.

## 2026-07-01 B011 Send-Request Field Name Sync

B011's accepted [UID:0001EY][0x00549220-0x005492cf.PatchPane2SendDownloadRequest](by-memory/0x00549220-0x005492cf.PatchPane2SendDownloadRequest.md) implementation promotes the version-word roles at owner offsets `+0x28c` and `+0x290` to source-facing inferred field names `m_patchVersionMajor` and `m_patchVersionMinor`. Current MCP decompilation shows the send-request helper passing those two words into the `%d/%d/%s` request suffix along with `m_finalPatchPath`, and constructor evidence shows they are copied from packed input words `a2[0]` and `a2[1]`.

The [UID:0001VJ] layout page remains support-only. Its managed block is the accepted covered-by comment because the UID0002ZP callback completed the `PatchPane2` declaration, callback-interface surface, and member order on [UID:0000AA][PatchPane2](by-class/PatchPane2.md).

## Score Rationale

Completion is `92` because the page records the exact class parent, object size, vtable subobject offsets, tail field map, input descriptor format, live function boundaries, vptr-store xrefs, slot-target refs, and cross-checks from constructor, main loop, raw advance bytes, close/rename bytes, send-request helper, ordinary destructor, and scalar wrapper. The complete class declaration and unified source route are now applied.

Confidence is `94` because current IDA evidence, supporting class/file/vtable/global docs, and instruction-level offset uses all agree. The remaining lexical uncertainty is limited to unrecoverable private spellings; consumer roles and types are exact, while `0x00549020` is conclusively retained as non-entry duplicate evidence rather than a source method.

## Generated-Data Caveat

Historical generated field names were generic `mbr_0x26c` labels and modeled `DialogPane` as a `617`-byte base followed by three bytes of compiler alignment. Those labels are superseded by the current class declaration; the three bytes remain natural base-tail alignment and are not handwritten fields.

## UID0002ZP Layout Closure

- `PatchPane2` derives directly from `DialogPane` and `Singleton<PatchPane2>`. RTTI PMD `+0x26c` identifies an empty singleton base at the start of the class tail; it is not a stored pointer or hidden source member.
- The source-owned tail is exactly two `StringBase<wchar_t>` objects, one three-pointer `std::vector<StringBase<wchar_t> >`, one download index, one `FILE *`, one pass count, and two version words. This produces `sizeof(PatchPane2) == 0x294` with no reserved/raw filler field.
- The inherited child manager reached at full-object `+0x1fc` belongs to `DialogPane`; callback source calls `GetChild<StaticTextControlPane>(0/1)`. The older `m_statusControls` spelling is rejected as a decompiler-era synthetic label rather than preserved as a PatchPane2 member.
- Constructor [UID:0004VS][0x00548690-0x00548a06.PatchPane2Constructor](by-memory/0x00548690-0x00548a06.PatchPane2Constructor.md), ordinary destructor [UID:0004VT][0x00548a10-0x00548a7d.PatchPane2Destructor](by-memory/0x00548a10-0x00548a7d.PatchPane2Destructor.md), callback UID0001EV, request helper UID0001EY, and compiler wrapper [UID:0004VX][0x00549370-0x0054940f.PatchPane2ScalarDeletingDestructor](by-memory/0x00549370-0x0054940f.PatchPane2ScalarDeletingDestructor.md) independently cover initialization, use, destruction, and final size.

## UID0002ZP Implementation Callback

- 2026-07-21: raised this layout support page from `86/89` to `92/94`, preserved owner/emitter UID0000AA and the established source route, and applied the accepted no-duplicate type marker. All prior offset and consumer evidence remains authoritative.

## Cross-References

- [UID:0000AA][PatchPane2](by-class/PatchPane2.md)
- [UID:0000MH][PatchPane](by-file/PatchPane.md)
- [UID:0001YF][PatchPaneVtableFamily](by-type/by-vtable/PatchPaneVtableFamily.md)
- [UID:0001EV][0x00548a80-0x00548f1a.PatchPane2MainLoop](by-memory/0x00548a80-0x00548f1a.PatchPane2MainLoop.md)
- [UID:0001EX][0x00549080-0x00549214.PatchPane2CloseAndRenameDownloadedFile](by-memory/0x00549080-0x00549214.PatchPane2CloseAndRenameDownloadedFile.md)
- [UID:0001EY][0x00549220-0x005492cf.PatchPane2SendDownloadRequest](by-memory/0x00549220-0x005492cf.PatchPane2SendDownloadRequest.md)
- [UID:0000RZ][g_pPatchPane2](by-global/g_pPatchPane2.md)

## Changes

- 2026-05-31 IDA recheck and grading:
  - What existed before: validator metadata was `0/0`, `RECONSTRUCTABLE` was blank, and the page relied partly on generated metadata for the `0x294` size.
  - What it was changed to: marked reconstructable, attached the layout page to [UID:0000AA][PatchPane2](by-class/PatchPane2.md), set scores to `76/84`, and added IDA evidence for function boundaries, vtable writes, tail offsets, input-buffer parsing, singleton xrefs, caller evidence, and destructor size.
  - Summary/evidence: IDA MCP `lookup_funcs`, `decompile`, focused `py_eval` disassembly, `xrefs_to`, `callers`, and string search confirm the field-offset model. Scores stay below 95 because final field names, source declaration shape, and some string/helper identities remain provisional.
- 2026-06-13 A001 type-quality refresh:
  - Before: `COMPLETION:76`, `CONFIDENCE:84`; the page was accurate but below the corrected assignment gate and lacked current parent-gate, rebuild-handling, verified conversions, and live xref/disassembly refresh evidence.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:89`; `RECONSTRUCTABLE:TRUE` and `AUTOGEN_PARENT_UID:0000AA` retained.
  - Summary/evidence: live IDA MCP reconfirmed the PatchPane2 function boundaries, the non-function status of `0x00549020`, primary/secondary/tertiary vptr stores, slot-target refs, singleton refs, tail field initialization/use across constructor/main-loop/raw-advance/close-rename/send/destructor paths, and destructor size `0x294` / 660 (Verified with `int_convert.py`). The direct class parent [UID:0000AA][PatchPane2](by-class/PatchPane2.md) is already `86/85`, so the strict child-and-parent gate is satisfied.
- 2026-06-27 B009 main-loop source-ready sync:
  - Score unchanged at `86/89`.
  - Summary/evidence: updated the tail field table with source-facing inferred names accepted by [UID:0001EV][0x00548a80-0x00548f1a.PatchPane2MainLoop](by-memory/0x00548a80-0x00548f1a.PatchPane2MainLoop.md)'s formal C++ block: `m_finalPatchPath`, `m_tempPatchPath`, `m_patchFileNames`, `m_currentDownloadIndex`, `m_activePatchFile`, and `m_downloadPassCount`. B009 MCP session `398b87c1` reconfirmed the secondary-subobject-to-owner offset map and the state-machine reads/writes that justify those names.
- 2026-07-01 B011 send-request source-ready sync:
  - Score unchanged at `86/89`.
  - Summary/evidence: named `+0x28c` and `+0x290` as inferred source-facing `m_patchVersionMajor` and `m_patchVersionMinor` for [UID:0001EY][0x00549220-0x005492cf.PatchPane2SendDownloadRequest](by-memory/0x00549220-0x005492cf.PatchPane2SendDownloadRequest.md)'s formal first-draft C++ body. The layout formal body remains blank because this page should be absorbed into a later class declaration pass rather than emitted standalone.
