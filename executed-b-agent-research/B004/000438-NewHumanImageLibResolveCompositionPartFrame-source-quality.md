** TARGET-REPORT-UID:000438 **
** AUTHOR-AGENT-ID:B004 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# 000438 NewHumanImageLibResolveCompositionPartFrame Ownership / Split Research


## Finalized Report / Current Recommendation

- Current recommendation: implemented. [UID:000438] is a reconstructable, class-owned `void NewHumanImageLib::ResolveCompositionPartFrame(...)` method with the accepted source-facing body.
- Final disposition: `CANONICAL_OWNER:000092`, `EMITTER_UIDS:000092`, `RECONSTRUCTABLE:TRUE`, exact half-open range `0x004e4210-0x004e427c`, and target score `92/94` are applied and scoped-validator verified.
- Required action state: the core accepted callback, the later four-item ordinary-document reconciliation callback, and supervisor application/serial validation of all twelve exact manual coverage handoffs are complete. `by-file/NewHumanImageLib.md` and `by-class/NewHumanImageLib.md` preserve the 2026-06-30 ImageFrameTable-only/`dword_67A744` rejection and UID000417 `88/90` promotion as dated historical evidence, explicitly supersede the old callee/receiver conclusion with the accepted UID0002P6 `ResourceLayoutTable::LoadFrameDrawRecord` ImageLib-facet route through `g_pEPFLib`, and state the current UID000417 `91/93` result. Scoped ordinary commands `16541`/`16542` and supervisor manual-root commands `16577`/`16579`-`16583` all passed. Current command-16594 generated sources retain the exact NewHuman child-emission and ImageLib/ImageFrameTable source-placement proof.
- Confidence: very strong for behavior, ABI, range, owner, source return, table strides, part-row consumers, output type, and source route; strong for the inferred private method and field spellings.

## Supporting Research

- Lifecycle/status: Gate 1 passed for exact pre-implementation report SHA256 `DE92D398053174624B3E269DF2A964256721FE59E2694BEA995D483A3C3F1C3`; the supervisor then issued the core implementation callback. Gate 1 later passed for exact reconciled report SHA256 `71588BFB1B8A865A1A2A8243A94513C6A95A882BE188B418ADC4EA298825BA11`, and the supervisor authorized only the four ordinary NewHuman class/file reconciliations. Those four edits, their serial scoped validators, and supervisor application/validation of all twelve manual coverage handoffs are complete. The terminal marker is a durable historical B004 handoff token, not a mutable lifecycle assertion. Current lifecycle truth must be read from the report's latest validator-controlled path/status/footer; at this B004 handoff the active artifact had no validator lifecycle footer, and none is fabricated here. B004 did not perform any manual-coverage, validator, or lifecycle action during this reconciliation.
- Live IDA MCP session `9b0396a3` remained healthy throughout the pass. `server_health` returned `status:ok`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true` for `NexusTK.exe.i64` at imagebase `0x00400000`.
- Historical Gate 1 validator/generated context was read only: `NewHumanImageLib.cpp` command `16487`, tracker command `16492`, target hash `C8409C3293F6720513888F1733FC094BC06DA511F8DEB0A305A81AD891658D69`, and generated hash `0C20DCAB5D9D953728659367CD111D0C6B9D8D68D5ABDAAE74A70F6A839E2B8D` with no three target-family UIDs. This is preserved as the pre-repair baseline.
- Current target source hash after callback and validator command `16501` is `89C5154F6703EA8A6EC69815DD912A231E1792CC7CC44E3905DEE12968C38078`, 11,120 bytes, 113 lines.
- Historical bounded generated checkpoints are commands `16542` and `16569`; command `16569`, refreshed `2026-07-22T20:20:37-04:00`, carried `NewHumanImageLib.cpp` SHA256 `564D3D65F15A13186E6C5E6691CDBD1E1BA9AE8506062B6420C47EE0ED360125`, `ImageLib.cpp` SHA256 `2E0518BBEC17543849D91E96D671D42C57E0DBC06ACCCB67FB51EB5980743FEC`, and `ImageFrameTable.cpp` SHA256 `AF4E1766315C210B5CEE433B1150AAB40E0F5A52627D0F8FB39CB023CFC44DF8` with the same accepted semantic placement. Current generated readback is command `000000016594`, refreshed `2026-07-22T20:38:53-04:00`: `NewHumanImageLib.cpp` SHA256 `9011EDA6FC031801D84DB486F2302A592C60DB53C30EEAAD0F37DD01E66F2829`, 43,719 bytes, 1,108 lines, 2,992 words; `ImageLib.cpp` SHA256 `871106A22EF30FD490D788F9E6FF013DDE4E64D3A2B33F47B7832D2DEC480AC5`, 12,739 bytes, 323 lines, 1,131 words; and `ImageFrameTable.cpp` SHA256 `953A3D46A367D71347EF2EE35CC87BC2601B05FD5E70C8B25D737C7F621C9558`, 9,019 bytes, 288 lines, 745 words. NewHuman UIDs `000417`, `000437`, and `000438` each occur exactly once, UID000438 has zero empty-emitter markers, and `ResolveCompositionPartFrame` occurs twice. UID0002P6 and `LoadFrameDrawRecord` occur once and twice respectively in `ImageLib.cpp`, while both are absent from `ImageFrameTable.cpp`; all three current files have zero `sub_`/`dword_67A744` residue and zero empty-emitter markers.
- Historical B014/B004/B002/B007/B008/B010 NewHuman-family reports were used only as leads. Every material target claim below was rechecked against current documentation and live MCP evidence.

## Target

- Target UID: `000438`.
- Additional target UIDs: none. Related UIDs below are support destinations, not declared additional report targets.
- Declared-target inventory: [UID:000438] `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`, exact source-authored helper body and sole primary target.
- Target path: `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`.
- Historical source queue/report row: command-16492 `auto-generated/-ag-research-tracker.md`, `87/89`, combined `88.0`, reconstructable true, zero direct/additional reports. It records the assignment baseline, not current tracker state.
- Settled tracker identity: historical command `000000016626`, refreshed `2026-07-22T22:16:12-04:00`, was superseded after B003 supervisor execution. Current `auto-generated/-ag-research-tracker.md` is command `000000016627`, refreshed `2026-07-22T22:30:22-04:00` by `deferred-generated-refresh`, SHA256 `5345FEAB06910E4942FC9A378176D2DF6BC615B1DBFC3B3A31013AF338D252FC`, 1,634,696 bytes, 6,507 lines. Its by-memory section begins at line 1665, `Not-Covered Files - Reconstructable` at line 1667, and the exact UID000438 row at line 3198 records `92/94`, combined `93.0`, reconstructable `true`, and zero direct/additional/total report counts. Commands 16492 and 16626 are historical only.
- Current supervisor classification: completed same-report artifact after the exact-SHA Gate 1 acceptance, narrow four-item callback, supervisor application/validation of all twelve manual handoffs, and settled tracker readback. All B004 ordinary and supervisor manual-root actions represented here are complete. The terminal handoff token remains historical; current validator path/status/footer controls lifecycle truth.
- Current scores and parent state after callback: target `92/94`; class parent [UID:000092] `88/90`; file parent [UID:0000LR] `91/91`.

## Current Target State

- Current metadata: `COMPLETION:92`, `CONFIDENCE:94`, `CANONICAL_OWNER:000092`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000092`, blank position, `Nested:0`.
- Current owner/emitter/reconstructable state: the canonical class/file metadata, formal route, generated source, and both NewHuman class/file historical-prose reconciliations are applied and verified.
- Current C++/emitter state: formal C++ is the accepted `void` non-static member block, uses an inline 0x10-byte sequence slot, calls `g_pEPFLib->LoadFrameDrawRecord`, and calls `FrameDrawRecord::sourceRect.Offset`. [UID:000092] contains `[[CHILDREN]]`; current command-16594 generated output contains UID000417/UID000437/UID000438 exactly once each and no UID000438 empty marker, places UID0002P6 once in `ImageLib.cpp`, and leaves UID0002P6 absent from `ImageFrameTable.cpp`.
- Gate 1 open questions/blockers now resolved by the callback and preserved for audit:
  - `partId` is misleading; the 16-bit value selects a descriptor within one `NewHumanPartEntry`.
  - `descriptor->sequenceFrames[sequence]` is an inline 0x10-byte slot, not a pointer.
  - the nullable value is the slot's `frames` member at `+0x0c`.
  - the target has no source-significant return; current `return 0` and returned helper values are decompiler artifacts.
  - all 18 callers pass a `NewHumanImageLib` receiver in `ecx`, proving a non-static private member source shape despite the body not reading `this`.
  - every observed caller of `sub_4D1600` deliberately establishes the `g_pEPFLib` receiver or preserves an equivalent local copy. The two NewHuman class/file destinations now retain the former free/file-local/ImageFrameTable-only interpretation only as superseded history and state the accepted ResourceLayoutTable/ImageLib-facet member route.
- Related target/support docs checked: [UID:000092], [UID:0000LR], [UID:0001VF], [UID:000437], [UID:000417], [UID:0002P6], [UID:0000UX], [UID:0000BY], [UID:0000K2], [UID:0000K1], [UID:0000QU], [UID:0000BU], and all twelve current manual rows independently read back exactly once after supervisor validation.
- Current artifact/lifecycle status: the core callback, four-item reconciliation callback, supervisor manual coverage application/validation, command-16594 generated readback, and command-16627 tracker readback are complete. This report does not encode a mutable pending stage: its terminal token records B004's completed handoff, while current lifecycle truth comes only from the latest validator-controlled path/status/footer. At handoff no validator lifecycle footer existed on the active artifact. B004 has zero leases.

## Executive Recommendation

The following recommendation is fully implemented across the core callback, the accepted four-item reconciliation callback, and the twelve supervisor-owned manual coverage updates. Current command-16594 generated readback verifies the child-emission and source-placement results, all twelve manual rows occur exactly once, and both NewHuman class/file prose clusters state current route/score truth while preserving the older interpretation as dated history.

- Keep direct owner [UID:000092] `NewHumanImageLib` and source file [UID:0000LR] `NexusTK/render/NewHumanImageLib.cpp`.
- Keep the exact target range intact. It is one 108-byte function fenced by 11 preceding and 4 following `0xcc` bytes.
- Correct the method to source `void` and remove explicit `__stdcall`; caller setup proves the compiler ABI is a non-static `thiscall` member with seven explicit stack arguments and an unused receiver.
- Use the current descriptive name `ResolveCompositionPartFrame`; it accurately distinguishes this draw-record resolver from adjacent [UID:000437] `ResolveCompositionPartRect`.
- Rename the formal parameters to `descriptorIndex`, `motionIndex`, and `frameIndex`; these are the highest-probability source-facing roles proven by table strides and callers.
- Route the projection call through `g_pEPFLib->LoadFrameDrawRecord(...)`. Reassign canonical body [UID:0002P6] from file-level ImageFrameTable ownership to class/facet [UID:0000BY], which already emits non-virtual `g_pEPFLib` methods through [UID:0000K2] `ImageLib`.
- Add `[[CHILDREN]]` to [UID:000092]'s managed block. This was applied; the pre-repair command-16487 artifact lacked the target, historical bounded commands 16516/16542/16569 established and retained the repair, and current command-16594 contains it exactly once.
- The prior callback synchronized the same proven slot/return/call shape into support siblings [UID:000417] and [UID:000437]. These are implemented support changes, not duplicate report coverage.

## Supervisor Active Recheck

- Historical initial trigger: the B004 goal assigned UID000438 for a complete new report-first source-quality pass from the not-covered reconstructable queue. The current phase is same-report post-implementation reconciliation after the core callback, exact-SHA Gate 1 acceptance, and accepted four-item ordinary callback; it is not a new research-only artifact.
- Split-first result: no target split is required. Exact function and alignment boundaries are already correct.
- Source-bearing children: the assigned target already exists as one exact source-bearing child. No missing subrange needs creation.
- The active recheck did not accept the historical return, pointer-slot, helper-owner, or generated-route assumptions merely because they appeared in existing docs.

## Inference Research Guidance Check

- Current `by-structure.md`/skill discipline requires binary facts, documentation evidence, and inference to remain distinct. This report labels each accordingly.
- Existing documentation assumptions treated as uncertain and rechecked: `int __stdcall`, source return values, `partId`, pointer-valued sequence slots, free `LoadFrameDrawRecord`, synthetic `RectBoundsAdjust`, ImageFrameTable ownership, and the claim that child bodies already emit.
- IDA facts: exact bytes/range, padding, instructions, xrefs, caller setup, offsets, strides, callee bodies, and ignored EAX values.
- Documentation evidence: accepted names and declarations for `FrameDrawRecord`, `RectBounds::Offset`, `NewHumanPartEntry`, descriptor/sequence/frame layouts, `g_pEPFLib`, and the ResourceLayoutTable facet route.
- Inference: private method spelling, parameter spellings, and `ResourceLayoutTable::LoadFrameDrawRecord` as the most consistent human source declaration.
- Stale Wave2/Wave3 mentions were ignored. No current conclusion depends on either workflow.

## Heuristic / Inference Reanalysis And Validation

- Issue: IDA infers `int __stdcall` because the helper does not read incoming `ecx` and leaves incidental values in `eax`.
  - Resolution: all 18 callsites set `ecx` to the enclosing NewHuman object and ignore EAX; the success path ends in documented `void RectBounds::Offset`, while the null path leaves a table pointer in EAX. Best source shape is a `void` non-static member.
- Historical pre-callback issue: `partId` naming implied a global part category.
  - Resolution: the argument is compared with `part->descriptorCount` and scales into `part->frameDescriptors` by 0x20. `descriptorIndex` is the best descriptive name.
- Gate 1 issue: the prior formal treated `descriptor->sequenceFrames[motionIndex]` as a pointer.
  - Resolution: live instructions read `descriptor + 0x1c`, then index by 0x10 and load `[slot + 0x0c]`. The array element is an inline `NewHumanSequenceFrameTable`; only `.frames` is nullable.
- Gate 1 issue: the prior source used a free `LoadFrameDrawRecord`.
  - Resolution: target instruction `0x004e424f` loads `dword_67A744` into ECX immediately before call `0x004e4261`. UID000417 does the same at `0x004e0db1` before `0x004e0dc3`. Representative modeled and raw callers also set/preserve the singleton receiver. A non-virtual `ResourceLayoutTable`/ImageLib-facet member is the best source explanation.
- Gate 1 issue: the prior source used synthetic `RectBoundsAdjust` and returned it.
  - Resolution: exact callee `0x004b78f0` is the documented `void RectBounds::Offset(int dx, int dy)` method. Target pushes `yOffset`, sets ECX to `outRecord + 0x10`, then pushes `xOffset`, matching `sourceRect.Offset(xOffset, yOffset)`.
- Gate 1 issue: the prior target claimed a null-sequence return.
  - Resolution: the branch only tests `sequence.frames`; source behavior is an early `return;` with no output write. No source value is returned.
- Issue: target was marked coded in generated coverage but absent from historical generated source.
  - Resolution: class [UID:000092] had a managed include/comment block without `[[CHILDREN]]`; the core callback applied the marker, historical bounded commands 16516/16542/16569 verified and retained the repair, and current command-16594 reconfirms it after the prose-reconciliation callback.
- Issue: original private names are stripped.
  - Resolution: use consistent project vocabulary `ResolveCompositionPartFrame`, `descriptorIndex`, `motionIndex`, `frameIndex`, `drawIndexBase`, `sequenceFrames`, and `frames`; do not retain `sub_`, raw-offset, or decompiler labels in source.
- Rejected alternative: static/global `__stdcall` helper. Rejected because all 18 callers deliberately establish the class receiver.
- Rejected alternative: return `int`, output pointer, y offset, or status. Rejected because EAX differs by path and no caller reads it.
- Rejected alternative: clamp invalid descriptor selector to the last descriptor. Rejected because the binary selects descriptor zero.
- Rejected alternative: guard `descriptorCount == 0`. Rejected because UID000438 does not; adding one changes behavior.
- Rejected alternative: treat null slot as null `NewHumanSequenceFrameTable *`. Rejected by exact 0x10 slot indexing and `[slot+0x0c]` load.
- Rejected alternative: keep projection in `ImageFrameTable.cpp` because ECX is overwritten inside the callee. Rejected because an unused member receiver may be overwritten, while widespread caller-side `g_pEPFLib` setup has no reason under a free-function ABI.

## Evidence Standards Used

- Live MCP: `server_health`, `lookup_funcs`, `analyze_function`, `xrefs_to`, exact byte reads, and disassembly/caller-window checks.
- Binary evidence: exact 108-byte body hash, `cc` fences, stack cleanup `retn 1Ch`, fixed offsets and strides, direct calls, and post-call EAX use.
- Documentation evidence: current by-memory, by-class, by-file, by-type, by-item, by-global, manual coverage, generated coverage, generated source, and research tracker state.
- Negative evidence: no target split, no caller result consumption, no source-significant EAX merge, no free-helper caller convention, historical pre-callback command-16487 generated no target body, and no need for a new source file. Historical bounded commands 16516/16542/16569 superseded and retained that repair; current command-16594 confirms it remains intact.
- Evidence ladder: direct current IDA facts control behavior/ABI; current support docs control accepted vocabulary; inference is used only for stripped source spellings and class/facet presentation.
- Tool limitation: original private symbols and source headers are unavailable. That limits spelling certainty, not behavior or the required human-readable source decision.

## Evidence Checked

- IDA MCP/manual-disassembly/raw-byte checks performed:
  - active database/session `9b0396a3` health;
  - target `0x004e4210`, adjacent `0x004e4140`, caller `0x004e4280`, projection `0x004d1600`, wrapper [UID:000417] `0x004e0d70`, and rect offset `0x004b78f0`;
  - exact target bytes and SHA256;
  - all 18 target xrefs and all 31 UID000437 xrefs;
  - post-call instruction windows proving ignored EAX;
  - target and sibling stack argument order;
  - representative `sub_4D1600` receiver setup across modeled and raw callers.
- by-* docs checked: target, UID000437, UID000417, UID0002P6, UID0000UX, NewHuman class/file/layout, ResourceLayoutTable class, ImageLib file, `g_pEPFLib`, RectBounds, and relevant manual coverage rows.
- old reports checked as leads: executed NewHuman reports for UID0002V8, UID000417, UID000436, UID00041B, UID00041C, UID00017S, and UID0004DO.
- generated checks: historical pre-repair command-16487 `NewHumanImageLib.cpp`, historical source-queue command-16492 tracker, historical bounded post-core/post-reconciliation commands 16516/16542/16569, current command-16594 `NewHumanImageLib.cpp`/`ImageLib.cpp`/`ImageFrameTable.cpp`, and the current `-ag-memory-coverage.md` row.
- tracker checks: historical command16492 assignment baseline and historical command16626 settled checkpoint; current command16627 header/source/hash/metrics, by-memory and reconstructable-section anchors, and exact UID000438 line-3198 row with `92/94`, combined `93.0`, reconstructable true, and `0/0/0` report counts.
- Negative checks performed: no EAX consumer after any target call; no EAX consumer after either UID000417 call; historical command-16487 had no target/sibling occurrence in generated NewHuman source, while current command-16594 supersedes that result with UID000417/UID000437/UID000438 exactly once each; UID0002P6 is absent from current `ImageFrameTable.cpp` and present once in current `ImageLib.cpp`; no target-internal padding; no alternate owner-specific state use.
- Failed/unavailable checks: none. MCP remained available and healthy.

## Claim And Incorporation Ledger

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C438-001 | 000438 | Exact range is `0x004e4210-0x004e427c`, 108 bytes. | direct/very strong | MCP lookup/analyze/bytes | target Status/Evidence | incorporate | applied |
| C438-002 | 000438 | Body SHA256 is `AF886C944C70E0043EEF40D5514F8B483BE774140C2D3A1E0ECA5963B0599AE3`. | direct/very strong | exact MCP bytes | target Evidence | incorporate | applied |
| C438-003 | 000438 | Preceded by 11 `cc` bytes and followed by 4 `cc` bytes. | direct/very strong | MCP byte reads | target Boundary Evidence | incorporate | applied |
| C438-004 | 000438 | Target is source-authored class helper, not compiler glue. | direct/strong | prologue/body/18 semantic calls | target Status | already-present | already-present |
| C438-005 | 000438 | Source return is `void`. | direct/very strong | all callers ignore EAX; path-dependent residue | target formal/Behavior | incorporate | applied |
| C438-006 | 000438 | Source ABI is non-static member, not explicit `__stdcall`. | direct/very strong | all callers set NewHuman receiver in ECX | target formal/Status | incorporate | applied |
| C438-007 | 000438 | Argument 2 is a descriptor selector. | direct/strong | count compare and 0x20 descriptor stride | target formal/Behavior | incorporate | applied |
| C438-008 | 000438 | Invalid selector falls back to descriptor zero. | direct/very strong | `cmovg` from zero | target formal/Behavior | incorporate | applied |
| C438-009 | 000438 | No zero-count guard exists. | direct/very strong | full CFG | target formal/negative evidence | incorporate | applied |
| C438-010 | 000438 | Descriptor stride is 0x20 and array pointer is part `+0x3c`. | direct/very strong | assembly | target layout evidence | incorporate | applied |
| C438-011 | 000438 | `sequenceFrames` points to inline 0x10-byte motion slots. | direct/very strong | slot index `16*motionIndex` | target formal/layout | incorporate | applied |
| C438-012 | 000438 | Nullable branch tests slot `.frames` at `+0x0c`. | direct/very strong | exact load/test | target formal/Behavior | incorporate | applied |
| C438-013 | 000438 | Frame row stride is 9 bytes. | direct/very strong | `index*8 + index` | target layout evidence | incorporate | applied |
| C438-014 | 000438 | Frame `recordIndex` is added to descriptor `drawIndexBase` at `+0x08`. | direct/very strong | assembly | target formal/Behavior | incorporate | applied |
| C438-015 | 000438 | `part->rawRecords` is at `+0x40`. | direct/very strong | call argument load | target formal/layout | incorporate | applied |
| C438-016 | 000438 | Projection receiver is `g_pEPFLib`. | direct/very strong | `mov ecx,dword_67A744` before call | target formal/Behavior | incorporate | applied |
| C438-017 | 000438 | Output rectangle is `FrameDrawRecord::sourceRect` at `+0x10`. | direct + docs/very strong | LEA out+0x10; accepted type | target formal/Behavior | incorporate | applied |
| C438-018 | 000438 | Offset order is `(xOffset, yOffset)`. | direct/very strong | stack push order into 0x4b78f0 | target formal | incorporate | applied |
| C438-019 | 000438 | Null frames branch does not write output. | direct/very strong | branch bypasses both calls | target Behavior | incorporate | applied |
| C438-020 | 000438 | There are exactly 18 direct calls, all in RenderComposition. | direct/very strong | xrefs_to | target Caller Inventory | incorporate | applied |
| C438-021 | 000438 | Part rows used are 0-16 and 18; row 17 All is not passed. | direct/strong | 18 caller argument bases | target Caller Inventory | incorporate | applied |
| C438-022 | 000438 | Gate 1 generated source omitted the target. | direct/very strong | command-16487 text count zero | target/generated notes | historicalize | applied |
| C438-023 | 000092 | Historical pre-callback aggregate formal lacked `[[CHILDREN]]`. | direct/very strong | pre-callback managed block and command-16487 omission | class formal | incorporate | applied |
| C438-024 | 000417 | Its sequence slot has the same inline-object layout. | direct/very strong | live assembly 0x4e0d95-0x4e0d9e | support formal | incorporate | applied |
| C438-025 | 000417 | Its projection call is also `g_pEPFLib` member-shaped. | direct/very strong | `mov ecx,dword_67A744` at 0x4e0db1 | support formal | incorporate | applied |
| C438-026 | 000417 | Its source return remains `void`; both callers ignore EAX. | direct/very strong | post-call windows 0x50141a/0x5018fa | support behavior | incorporate | applied |
| C438-027 | 000437 | Source return is `void`, not `int`. | direct/very strong | all 31 calls ignore EAX; void Offset tail | support formal | incorporate | applied |
| C438-028 | 000437 | Its sequence slot is inline and `.frames` is nullable. | direct/very strong | live assembly | support formal | incorporate | applied |
| C438-029 | 000437 | Out-of-range raw record clears RectBounds then offsets it. | direct/very strong | assembly/decompile | support formal | incorporate | applied |
| C438-030 | 0002P6 | Projection body is a non-virtual g_pEPFLib member/facet method. | inference/very strong | systematic caller ECX setup | support owner/formal | incorporate | applied |
| C438-031 | 0002P6 | Projection source return is `void`. | direct + caller evidence/strong | result is output assignment residue | support formal | incorporate | applied |
| C438-032 | 0000UX | Alias/index must follow UID0002P6 owner and remain non-emitting. | documentation/strong | canonical memory relation | item Status/metadata | incorporate | applied |
| C438-033 | 0001VF | Existing accepted type names are valid, but prose must state slot-object semantics. | direct + docs/very strong | loader + consumers | layout Notes | incorporate | applied |
| C438-034 | 0000BY | Existing ResourceLayout facet is the best direct class route. | inference/strong | same singleton and non-virtual method convention | class Method Notes | incorporate | applied |
| C438-035 | 0000K2 | ImageLib file remains final source route for UID0000BY children. | docs/strong | current owner/emitter chain | file Contents/Changes | incorporate | applied |
| C438-036 | 0000LR | NewHuman file remains the target source route. | direct + docs/very strong | owner chain/call family | file Contents/Changes | incorporate | applied |
| C438-037 | 000438 | No IDA DB mutation is required for this research/callback. | policy/strong | current goal restriction | IDA Recommendations | not-applicable | excluded-with-reason |
| C438-038 | 000438 | Historical pre-supervisor manual target coverage row was stale; the current row is corrected. | direct/very strong | former row said null return/87; current root readback says void/no-write/92 | manual coverage handoff | incorporate | applied |
| C438-039 | 000438 | Historical support manual rows were stale or absent; all current support rows are synchronized. | direct/strong | six current manual roots and twelve exact row readbacks | manual coverage handoff | incorporate | applied |
| C438-040 | 000438 | Score `92/94` reflects resolved behavior/source blockers with spelling-only caps. | reasoned/strong | complete evidence set | target metadata | incorporate | applied |

## Positive Evidence Summary

- The target has a normal frame, exact `retn 1Ch`, one semantic null branch, two established project callees, and 18 repeated semantic consumers. It is source-authored code.
- Every target call is made from one NewHuman render function and every call establishes the same class receiver, strongly proving private class membership.
- Table geometry is exact: 68-byte part row, 0x20 descriptor, 0x10 motion slot, 9-byte frame row, and 40-byte output record.
- The target and its adjacent rectangle sibling use the same selector/descriptor/slot/frame pattern, which independently corroborates the layout.
- Current accepted `FrameDrawRecord` and `RectBounds` docs align exactly with raw output offsets and the two callee bodies.
- The `g_pEPFLib` load immediately before `sub_4D1600` is repeated in UID000417 and broad image-library consumers, providing a coherent member-method source route.
- The historical generated omission had a direct mechanical cause: the class formal lacked `[[CHILDREN]]`; the applied marker now yields UID000417/UID000437/UID000438 exactly once each in current command-16594 NewHuman source and UID0002P6 only in current ImageLib source.

## IDA MCP Facts

- Function/range facts:
  - `sub_4E4210`: `0x004e4210-0x004e427c`, size `0x6c` / 108, 3 basic blocks, complexity 2.
  - predecessor sibling `sub_4E4140`: `0x004e4140-0x004e4205`, size `0xc5` / 197.
  - successor `sub_4E4280`: `0x004e4280`, size `0xaf9` / 2809.
  - projection `sub_4D1600`: `0x004d1600-0x004d165d`, size `0x5d` / 93, 81 direct xrefs.
  - rectangle offset `sub_4B78F0`: `0x004b78f0-0x004b7908`, size 24, documented source `void RectBounds::Offset(int,int)`.
- Data/table/padding facts:
  - target bytes SHA256 `AF886C944C70E0043EEF40D5514F8B483BE774140C2D3A1E0ECA5963B0599AE3`;
  - `0x004e4205-0x004e4210`: eleven `cc` bytes;
  - `0x004e427c-0x004e4280`: four `cc` bytes;
  - descriptor count `+0x34`, descriptor pointer `+0x3c`, raw record table `+0x40`;
  - descriptor stride 0x20, `drawIndexBase +0x08`, sequence-slot array `+0x1c`;
  - motion slot stride 0x10, frames member `+0x0c`;
  - frame row stride 9, record index at row `+0x00`;
  - output `sourceRect +0x10`.
- Xref facts: exactly 18 target calls at `0x4e465d`, `0x4e4670`, `0x4e46b6`, `0x4e46c6`, `0x4e4726`, `0x4e4786`, `0x4e47fc`, `0x4e483c`, `0x4e488e`, `0x4e48cf`, `0x4e4910`, `0x4e4951`, `0x4e499b`, `0x4e49ee`, `0x4e4a4b`, `0x4e4a90`, `0x4e4ad5`, and `0x4e4b1a`.
- Vtable/global/type facts: target is not virtual; receiver is the current NewHuman object. Projection receiver is global [UID:0000QU] `g_pEPFLib` at `0x0067a744`.
- Negative IDA facts: no target caller consumes EAX; no internal split/padding; no source guard for descriptorCount zero; no evidence for a free projection call; no IDA DB edit was attempted.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004e4140-0x004e4205` | 000437 / ResolveCompositionPartRect | adjacent rectangle resolver | true | 000092 | 88/90 -> 92/94 applied | support implementation verified |
| `0x004e4205-0x004e4210` | ignored padding | alignment | false | ignored | 100 | unchanged |
| `0x004e4210-0x004e427c` | 000438 / assigned target | frame draw-record resolver | true | 000092 | 87/89 -> 92/94 applied | primary implementation verified |
| `0x004e427c-0x004e4280` | ignored padding | alignment | false | ignored | 100 | unchanged |
| `0x004e4280-...` | RenderComposition | sole caller function | source authored | 000092 | aggregate docs | corroborating consumer |
| `0x004e0d70-0x004e0dcf` | 000417 / LoadPartFrameDrawRecord | same slot/projection support | true | 000092 | 88/90 -> 91/93 applied | support implementation verified |
| `0x004d1600-0x004d165d` | 0002P6 / LoadFrameDrawRecord | shared projection member | true | 0000BY applied | 88/92 -> 91/94 applied | owner-route implementation verified |
| by-item alias | 0000UX | non-emitting index | false | 0000BY applied | 86/91 -> 89/93 applied | metadata/prose synchronization implemented |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x4e465d` | target call with part row 1 Coat | first category producer |
| `0x4e4670` | part row 0 Body | body producer |
| `0x4e46b6` | part row 3 Emotion | emotion producer |
| `0x4e46c6` | part row 2 Face | face producer |
| `0x4e4726` | part row 4 FaceDec | decoration producer |
| `0x4e4786` | part row 5 Hair | hair producer |
| `0x4e47fc` | part row 6 Helmet | helmet producer |
| `0x4e483c` | part row 7 HairDec | hair-decoration producer |
| `0x4e488e` | part row 8 Sword | sword producer |
| `0x4e48cf` | part row 9 Spear | spear producer |
| `0x4e4910` | part row 10 Bow | bow producer |
| `0x4e4951` | part row 11 Fan | fan producer |
| `0x4e499b` | part row 12 Shield | shield producer |
| `0x4e49ee` | part row 13 Arrow | arrow producer |
| `0x4e4a4b` | part row 18 Back | back producer |
| `0x4e4a90` | part row 14 Mantle | mantle producer |
| `0x4e4ad5` | part row 15 Shoes | shoes producer |
| `0x4e4b1a` | part row 16 Neck | neck producer |
| all 18 | post-call path | EAX ignored; caller continues/jumps using unrelated state |
| `0x4e4261` | target -> `sub_4D1600` | ECX is `g_pEPFLib`; projects FrameDrawRecord |
| `0x4e426f` | target -> `sub_4B78F0` | ECX is `outRecord->sourceRect`; offsets by x/y |
| `0x50141a`, `0x5018fa` | callers -> UID000417 | both overwrite EAX immediately after call |
| 31 UID000437 callsites | CalculateCompositionBounds family | every post-call path ignores EAX |

## Documentation Evidence And IDA Status

- Existing docs that support the conclusion:
  - [UID:0001VF] defines `NewHumanPartEntry`, 0x20 descriptors, 0x10 sequence slots, 9-byte frame rows, and accepted `FrameDrawRecord` use.
  - [UID:0000BU] declares `void RectBounds::Offset(int dx, int dy)` at `0x004b78f0`.
  - [UID:0000BY] already models non-virtual methods called on the ImageLib singleton/facet.
  - [UID:0000QU] identifies `0x0067a744` as `g_pEPFLib`.
- Historical pre-callback docs that were stale/incomplete/contradicted and were repaired in the accepted core callback:
  - target formal return/calling convention/null-return/pointer-slot/free-helper shape;
  - sibling UID000437's same return and pointer-slot shape;
  - UID000417's pointer-slot and free-helper shape;
  - UID0002P6/UID0000UX ImageFrameTable owner route;
  - NewHuman class statement that child methods emit despite missing `[[CHILDREN]]`.
- Accepted four-item ordinary-document reconciliation now applied:
  - `by-file/NewHumanImageLib.md` preserves the 2026-06-30 ImageFrameTable-only/`dword_67A744` receiver rejection as a superseded interpretation, states the current UID0002P6 `ResourceLayoutTable::LoadFrameDrawRecord` ImageLib-facet route through `g_pEPFLib`, and distinguishes the dated UID000417 `88/90` promotion from current `91/93`;
  - `by-class/NewHumanImageLib.md` preserves the corresponding callee-owner/receiver rejection as superseded history, states that UID000417 remains NewHuman-owned while canonical UID0002P6 routes through the `g_pEPFLib` ResourceLayoutTable/ImageLib facet, and distinguishes the dated UID000417 `88/90` promotion from current `91/93`.
- Generated/coverage state:
  - historical pre-repair command-16487 NewHuman source lacked all three exact method children; that bounded omission was first superseded by commands 16516/16542 and retained by historical command16569, while current command-16594 contains UID000417, UID000437, and UID000438 exactly once each and UID000438 has no empty-emitter marker;
  - the historical memory autogen labeled UID000438 `coded` while command-16487 omitted it; current command-16594 retains the repaired generated route;
  - the supervisor applied and serially validated all twelve exact manual handoffs under commands 16577/16579-16583. Read-only reconciliation confirms every intended row occurs exactly once, including the validator-normalized UID0000UX link to canonical UID0002P6.

## Ranked Ownership Analysis

### 1. NewHumanImageLib class [UID:000092]

- Evidence for: all 18 calls occur in one NewHuman class method, all establish the NewHuman receiver, accepted part/layout types are class state, and target is physically inside the NewHuman local method cluster.
- Evidence against: target body itself does not read `this`.
- Decision: direct target owner retained. Unused `this` does not outweigh caller ABI evidence.

### 2. NewHumanImageLib file [UID:0000LR]

- Evidence for: class parent already routes here; target is a private composition helper used only by NewHuman render code.
- Evidence against: no original source filename/debug symbol survives.
- Decision: retain as final source file. No new file is justified.

### 3. ImageFrameTable file [UID:0000K1]

- Evidence for: target consumes frame table declarations and calls the shared projection body currently routed there.
- Evidence against: target itself is NewHuman-specific; the projection caller ABI repeatedly establishes `g_pEPFLib`, and ImageFrameTable ownership mistakes dependency declarations for method ownership.
- Decision: reject for UID000438. Keep `ImageFrameTable.h` as a declaration dependency, but move projection method ownership to [UID:0000BY].

### Proposed new file/grouping, if applicable

- Proposed owner/name/path: none.
- Likely full contents: existing `NexusTK/render/NewHumanImageLib.cpp` and `NexusTK/render/ImageLib.cpp` routes are sufficient.
- Candidate related items that belong: UID000438/417/437 under NewHuman; UID0002P6 under ResourceLayoutTable facet.
- Candidate related items rejected: compiler vector helpers remain marker-only; raw ResourceLayout helpers with no receiver remain file-level UID0000N5.
- Standalone/narrow/broad inference: a new one-helper file would be less plausible than the existing class-local source grouping.

## Source Placement

- UID000438: private non-virtual `NewHumanImageLib` method in `NexusTK/render/NewHumanImageLib.cpp`.
- UID0002P6: non-virtual `ResourceLayoutTable`/ImageLib-facet method emitted through `NexusTK/render/ImageLib.cpp`.
- Header dependencies: `ImageFrameTable.h` continues to supply archive/output declarations; NewHuman layout declarations supply part/descriptor/sequence types.
- Rejected placements: free helper in NewHuman source, static ImageFrameTable projection, standalone `NewHumanCompositionHelpers.cpp`, or raw-address helper file.
- Remaining placement uncertainty: the exact historical header declaration boundary between `ImageLib` and its recovered `ResourceLayoutTable` facet is inferred. Existing project convention already uses `ResourceLayoutTable::` for the non-virtual facet children, so that is the consistent source-facing route.

## Range / Split / Padding / Reclassification Analysis

- Exact target range is one function: `[0x004e4210,0x004e427c)`.
- Exact preceding padding: `[0x004e4205,0x004e4210)`, 11 `cc` bytes.
- Exact following padding: `[0x004e427c,0x004e4280)`, 4 `cc` bytes.
- No child creation, merge, shrink, expansion, or target reclassification is required.
- Parent aggregate remains [UID:00017R] NewHuman local method cluster and non-emitting container.
- The target should not absorb adjacent UID000437 or successor RenderComposition.

## Negative Evidence Summary

- No target callsite tests, stores, forwards, compares, or branches on EAX.
- No stable source return can be inferred from target EAX: success inherits y offset from `RectBounds::Offset`; null inherits sequence array state.
- No target instruction reads incoming ECX, but all callers establish it; this is negative evidence against field access, not against member ownership.
- No descriptorCount-zero guard exists in target. Adding one would alter exact behavior.
- No nullable sequence-slot pointer exists; only its `frames` field is nullable.
- No caller-side evidence supports a static `LoadFrameDrawRecord`; the singleton receiver setup is repeated across unrelated consumers.
- Historical pre-repair command-16487 contained no target method despite `coded` status; bounded commands 16516/16542/16569 superseded and retained that repair, and current command-16594 contains UID000438 exactly once with no target empty marker.
- Nearby functions prove family adjacency but do not justify merging ranges.

## IDA Rename / Type / Comment Recommendations

- Proposed source-facing target name: `NewHumanImageLib::ResolveCompositionPartFrame`.
- Proposed source signature: `void` non-static member with the seven explicit parameters in the formal block below.
- Proposed parameter names: `part`, `descriptorIndex`, `motionIndex`, `frameIndex`, `yOffset`, `xOffset`, `outRecord`.
- Proposed support method name: `ResourceLayoutTable::LoadFrameDrawRecord` for `0x004d1600`.
- Proposed slot types: `NewHumanSequenceFrameTable &sequence`; nullable `sequence.frames`.
- Evidence: exact calling conventions, offsets, strides, accepted support type docs, and repeated singleton/class receiver setup.
- Intentionally unchanged: IDA database names and types. The current same-report Gate 1 reconciliation forbids IDA DB writes, and the completed prior callback also made no IDA mutation.
- Safety: source recommendations are high-confidence; an IDA rename/type callback would be reasonable only under a separate explicit supervisor instruction.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes. The target is reconstructable, above gate, exact behavior is fully modeled, and source-shape blockers are resolved.
- Recommended target code, exact managed block:

```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewHumanImageLib::ResolveCompositionPartFrame(
    NewHumanPartEntry *part,
    unsigned short descriptorIndex,
    int motionIndex,
    int frameIndex,
    int yOffset,
    int xOffset,
    FrameDrawRecord *outRecord)
{
    int selectedDescriptor = 0;
    if (part->descriptorCount > descriptorIndex)
        selectedDescriptor = descriptorIndex;

    NewHumanPartFrameDescriptor &descriptor =
        part->frameDescriptors[selectedDescriptor];
    NewHumanSequenceFrameTable &sequence =
        descriptor.sequenceFrames[motionIndex];

    if (sequence.frames == NULL)
        return;

    g_pEPFLib->LoadFrameDrawRecord(
        part->rawRecords,
        sequence.frames[frameIndex].recordIndex + descriptor.drawIndexBase,
        outRecord);

    outRecord->sourceRect.Offset(xOffset, yOffset);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Recommended UID000417 support code, exact managed block:

```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewHumanImageLib::LoadPartFrameDrawRecord(
    int partIndex,
    unsigned short descriptorIndex,
    int motionIndex,
    int frameIndex,
    FrameDrawRecord *outRecord)
{
    NewHumanPartEntry &part = m_partEntries[partIndex];
    NewHumanPartFrameDescriptor &descriptor =
        part.frameDescriptors[descriptorIndex];
    NewHumanSequenceFrameTable &sequence =
        descriptor.sequenceFrames[motionIndex];

    if (sequence.frames == NULL)
        return;

    g_pEPFLib->LoadFrameDrawRecord(
        part.rawRecords,
        sequence.frames[frameIndex].recordIndex + descriptor.drawIndexBase,
        outRecord);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Recommended UID000437 support code, exact managed block:

```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewHumanImageLib::ResolveCompositionPartRect(
    NewHumanPartEntry *part,
    unsigned short descriptorIndex,
    int motionIndex,
    int frameIndex,
    int yOffset,
    int xOffset,
    RectBounds *outBounds)
{
    int descriptorCount = part->descriptorCount;
    if (descriptorCount == 0)
        return;

    int selectedDescriptor = 0;
    if (descriptorCount > descriptorIndex)
        selectedDescriptor = descriptorIndex;

    NewHumanPartFrameDescriptor &descriptor =
        part->frameDescriptors[selectedDescriptor];
    NewHumanSequenceFrameTable &sequence =
        descriptor.sequenceFrames[motionIndex];

    if (sequence.frames == NULL)
        return;

    int rectIndex = sequence.frames[frameIndex].recordIndex +
        descriptor.drawIndexBase;
    if (rectIndex >= part->rawRecords->recordCount)
        outBounds->SetLTRB(0, 0, 0, 0);
    else
        *outBounds = part->rawRecords->records[rectIndex].bounds;

    outBounds->Offset(xOffset, yOffset);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Recommended UID0002P6 support code, exact managed block:

```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ResourceLayoutTable::LoadFrameDrawRecord(
    const ArchiveMetadataTable *table,
    int frameIndex,
    FrameDrawRecord *outRecord)
{
    if (frameIndex < 0 || frameIndex >= table->frameCount) {
        outRecord->payloadStart = NULL;
        outRecord->payloadEnd = NULL;
        return;
    }

    const PackedArchiveRecord *record = &table->records[frameIndex];
    outRecord->payloadStart = record->payloadStart;
    outRecord->width = record->bounds.right - record->bounds.left;
    outRecord->sourceRect = record->bounds;
    outRecord->payloadEnd = record->payloadEnd;
    outRecord->payloadSpan =
        (int)(record[1].payloadStart - record->payloadEnd);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Recommended [UID:000092] aggregate managed block:

```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "ImageFrameTable.h"

// NewHumanImageLib method bodies and data are emitted by exact child pages;
// the aggregate class declaration remains deferred until the remaining
// header/layout/helper declarations are source-ready.
[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Third-party import directive: not applicable; all involved code is NexusTK project source.
- Exact behavior preservation: code retains descriptor-zero fallback, target's deliberate lack of zero-count guard, no-write null-frames exit, exact record index arithmetic, projection output, and x/y rectangle offset.
- Plausible original source shape: ordinary private methods, references to inline table rows, accepted project types, and member calls replace explicit decompiler conventions and synthetic helper names.
- Inferred names are marked in prose but source uses the best human-readable project-consistent forms; no `sub_`, `dword_`, offset arithmetic, or raw ABI names remain in the code.
- Naming style follows current NewHuman/ImageLib/RectBounds documentation. Behavioral fidelity takes priority over stylistic consistency.

## Final Recommendation

- UID000438 retains class/file ownership and exact range, uses the corrected void member formal, and is `92/94`.
- UID000092 contains `[[CHILDREN]]`, is `88/90`, and current command-16594 proves exact child bodies appear.
- UID0000LR retains its source path and the applied helper/source route at `91/91`; `by-file/NewHumanImageLib.md` now preserves the former ImageFrameTable-only/`dword_67A744` rejection as superseded history, states the current `g_pEPFLib` ResourceLayoutTable/ImageLib-facet receiver route, and distinguishes the dated UID000417 `88/90` promotion from current `91/93`.
- UID000417 uses the inline sequence slot and g_pEPFLib member call at `91/93`.
- UID000437 uses void return, inline sequence slot, and RectBounds member calls at `92/94`.
- UID0002P6 is routed from UID0000K1 to UID0000BY, remains reconstructable/emitting, uses the member formal, and is `91/94`.
- UID0000UX follows UID0002P6 to owner UID0000BY, remains non-reconstructable/non-emitting, and is `89/93`.
- UID0000BY/UID0000K2 include the shared projection method in their inventories at `90/91` and `91/90`.
- UID0001VF states inline 0x10-byte slot semantics with only slot `.frames` nullable and is `87/90`.
- No new by-memory page, split, file, class, global, or IDA mutation is needed.
- Current implemented source/ownership/formal/no-code disposition remains accepted as the report recommendation: UID000438/UID000417/UID000437 are exact class-owned source methods, UID0002P6 is a ResourceLayoutTable/ImageLib-facet member routed through UID0000BY, UID0000UX is its non-emitting index, all five managed formal blocks remain the exact source recommendation, and no standalone raw/decompiler-shaped helper body is recommended.
- The accepted later callback and all twelve supervisor-owned manual coverage handoffs are complete. Both `by-file/NewHumanImageLib.md` and `by-class/NewHumanImageLib.md` preserve the old callee/receiver interpretation and `88/90` promotion as dated history while stating the current UID0002P6 ResourceLayoutTable/ImageLib-facet route and UID000417 `91/93` state. Current manual roots carry the same disposition exactly once per intended row, command-16594 generated output confirms source placement, and command-16627 tracker state records the settled score/report-count row. The terminal token is durable handoff history; validator-controlled path/status/footer state governs lifecycle without changing the accepted binary/source disposition.

## Recommended Target Doc Changes

- Historical accepted callback target changes, already applied and verified:
- Target path: `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`.
- Exact incorporated facts: body hash, padding fences, 18 callsites and part-row map, ignored EAX, non-static member ABI, void return, descriptor/slot/frame layout, no zero-count guard, g_pEPFLib projection receiver, FrameDrawRecord sourceRect offset, and the historical generated-route defect.
- Metadata applied: `87/89 -> 92/94`; owner/emitter/reconstructable unchanged.
- Formal C++ applied: the exact managed block above.
- Item Summary applied: null-return wording was replaced with the no-write null-frames early return and void member result.
- Historical assumptions preserved as superseded: B014's `int __stdcall`, `partId`, pointer-slot, free projection, synthetic rect helper, and zero-return model, with the rejection reason for each.

## Recommended Support Doc Changes

### Historical Accepted Callback Destination Changes - Applied

- `by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md`:
  - applied the exact formal above;
  - documented the inline slot and member projection evidence;
  - applied `88/90 -> 91/93`; metadata route otherwise unchanged.
- `by-memory/0x004e4140-0x004e4205.NewHumanImageLibResolveCompositionPartRect.md`:
  - applied the exact formal above;
  - documented all 31 ignored returns and the void source contract;
  - applied `88/90 -> 92/94`; owner/emitter unchanged.
- `by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md`:
  - applied the exact member formal above;
  - changed owner/emitter `0000K1 -> 0000BY`;
  - preserved all projection, 81-xref, sentinel-boundary, and adjacent-helper evidence;
  - applied `88/92 -> 91/94`.
- `by-item/LoadFrameDrawRecord_004D1600.md`:
  - changed owner `0000K1 -> 0000BY`, retained `RECONSTRUCTABLE:FALSE` and blank emitters/formal;
  - preserved the index role and link to canonical UID0002P6;
  - applied `86/91 -> 89/93`.
- `by-class/NewHumanImageLib.md`:
  - added the exact `[[CHILDREN]]` block above;
  - applied the core UID000417/437/438 method inventory wording;
  - preserved the deferred aggregate declaration and all unrelated class evidence;
  - applied `86/88 -> 88/90`;
  - the core callback left two older contradictory prose clusters for a later exact callback; the accepted later callback now historicalizes the old callee/receiver rejection, states the current UID0002P6 ResourceLayoutTable/ImageLib-facet route through `g_pEPFLib`, and reconciles the dated UID000417 `88/90` promotion against current `91/93` without deleting history.
- `by-file/NewHumanImageLib.md`:
  - documented the corrected three helper contracts and child-emission repair;
  - retained path/owner and applied `90/89 -> 91/91` after historical bounded command-16516 generated proof;
  - the core callback left two older contradictory prose clusters for a later exact callback; the accepted later callback now historicalizes the old ImageFrameTable-only/`dword_67A744` receiver rejection, states the current `g_pEPFLib` ResourceLayoutTable/ImageLib-facet route, and reconciles the dated UID000417 `88/90` promotion against current `91/93` without deleting history.
- `by-type/by-struct/NewHumanImageLibLayout.md`:
  - clarified inline sequence-slot semantics and consumer names;
  - preserved all offsets and historical aliases; applied `85/88 -> 87/90`.
- `by-class/ResourceLayoutTable.md`:
  - added `LoadFrameDrawRecord` to the non-virtual facet method inventory;
  - preserved raw no-this helper exclusions and the current class/facet caveat;
  - applied `89/88 -> 90/91`.
- `by-file/ImageLib.md`:
  - added UID0002P6 to the UID0000BY child/source inventory;
  - preserved ImageLib/ResourceLayoutTable declaration uncertainty;
  - applied `90/88 -> 91/90`.
- `by-file/ImageFrameTable.md`:
  - removed canonical ownership of UID0002P6 while retaining declarations and adjacent raw helper ownership; score did not need to change.
- `by-global/g_pEPFLib.md`:
  - added UID0002P6 as a non-virtual receiver consumer; metadata did not need to change.

### Accepted Four-Item Ordinary-Document Reconciliation - Applied

- `by-file/NewHumanImageLib.md`: historicalized the contradictory 2026-06-30 prose that assigned the shared `LoadFrameDrawRecord` callee only to ImageFrameTable and rejected `dword_67A744`/`g_pEPFLib` as a receiver route because callee ECX is overwritten; preserved the caller-only and exact callee-instruction evidence, labeled the old ownership conclusion superseded, and stated the accepted UID0002P6 `ResourceLayoutTable::LoadFrameDrawRecord` ImageLib-facet route through `g_pEPFLib`.
- `by-file/NewHumanImageLib.md`: retained the dated UID000417 `88/90` promotion and explicitly distinguished it from the current `91/93` method state in the overview, source-quality sync, and historical change record.
- `by-class/NewHumanImageLib.md`: historicalized the contradictory callee-owner/receiver rejection while preserving UID000417's NewHuman ownership and the exact ECX evidence; stated that canonical UID0002P6 is the `ResourceLayoutTable::LoadFrameDrawRecord` ImageLib-facet method reached through `g_pEPFLib`/`dword_67A744`.
- `by-class/NewHumanImageLib.md`: retained the dated UID000417 `88/90` promotion and explicitly distinguished it from the current `91/93` state in the method inventory, source-quality sync, and historical change record.
- Scoped validator command `16541` validated the file destination and command `16542` validated the class destination, both exit `0` / `ok: 1`. Historical command-16542 generated readback first preserved all three exact child UIDs once with no UID000438 empty marker, command16569 retained that result, and current command-16594 retains both the child proof and accepted UID0002P6 ImageLib placement.

## Score And Metadata Recommendation

- Gate 1 target baseline: `87/89`, owner/emitter `000092`, reconstructable true.
- Applied current target: `92/94`, owner/emitter `000092`, reconstructable true, blank position, `Nested:0`.
- Reason not higher: exact original private method, parameter, and some nested field spellings are stripped; the final ImageLib/ResourceLayoutTable declaration boundary remains inferred; aggregate NewHuman class declaration remains deferred.
- Reason not lower: exact bytes, range, padding, CFG, all callers, callees, caller ABI, return contract, part categories, table offsets/strides, no-write branch, output type, and source route are established with current live evidence.
- Score-improvement attempts:
  - return blocker: audited all target and sibling callers; resolved to void.
  - calling-convention blocker: audited receiver setup; resolved to non-static member.
  - nested-type blocker: reconciled loader docs with exact slot indexing; resolved to inline slot plus nullable frames.
  - helper-owner blocker: audited singleton ECX setup across caller families; resolved to ResourceLayout facet method.
  - generated omission blocker: compared class formal with generated output; resolved to missing `[[CHILDREN]]` marker.
  - naming blocker: selected project-consistent descriptive names; no raw labels remain necessary.
- Metadata fields to leave unchanged on target: `CANONICAL_OWNER`, `RECONSTRUCTABLE`, `EMITTER_UIDS`, position, and Nested.

## Open Questions With Attempted Resolution

- Open question: was the exact original target name `ResolveCompositionPartFrame`?
  - Evidence checked: role, adjacent rect sibling, all callers, current naming conventions, and old reports.
  - Resolution: retain `ResolveCompositionPartFrame` as the highest-probability descriptive name. No original private symbol survives; this spelling uncertainty does not justify an IDA-style label or blank code.
- Open question: should the selector be named `partId` or `descriptorIndex`?
  - Evidence checked: compare target, descriptor count, 0x20 scaling, caller values, and Part.tbl row selection performed before the call.
  - Resolution: `descriptorIndex`; `partId` conflates the already-selected part row with its descriptor selector.
- Open question: free helper, ImageFrameTable helper, ImageLib method, or ResourceLayoutTable method for `0x004d1600`?
  - Evidence checked: 81 xrefs, modeled/raw caller windows, global receiver loads, current class/facet docs, and callee body.
  - Resolution: non-virtual `ResourceLayoutTable::LoadFrameDrawRecord` emitted through ImageLib. This matches the project's established facet convention and caller ABI.
- Open question: source return from UID000438/437/417?
  - Evidence checked: every direct caller and terminal callee behavior.
  - Resolution: void for all three. Residual EAX has no stable semantic contract.
- Open question: is `sequenceFrames[motionIndex]` nullable?
  - Evidence checked: loader layout and exact consumer instructions.
  - Resolution: the slot object is not nullable; its `.frames` pointer is nullable.
- Questions remaining unresolved: only exact historical spellings and final header/facet presentation. The report supplies the best defensible human source forms now; these do not block score improvement or first-draft code.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- The supervisor applied all twelve exact handoffs and validated the six roots serially. B004 independently re-read the later current roots and every intended row; each intended UID still occurs exactly once and every exact row body below remains unchanged. B004 did not edit or validate any root:
  - `by-memory/-coverage-report.md`: SHA256 `A7D812B60CB35399C541461DC0E214A8380B61D7793E2BB499B9247449A1A70B`, 1,982,596 bytes, 4,564 lines. UID0002P6 is at line 1804, UID000417 at line 1858, UID000437 at line 1862, and UID000438 at line 1863; each occurs once.
  - `by-item/-coverage-report.md`: SHA256 `F634DDF3DCDD0C2BA8EC1CE07E1E17B61BA1EC3CEBE4B72A2206064FCCDBB0A1`, 29,408 bytes, 87 lines. UID0000UX is at line 56 exactly once; the validator normalized its canonical UID0002P6 reference to the current linked form reproduced below.
  - `by-class/-coverage-report.md`: SHA256 `E4F6985825BD360CD322C6099EC097CF87E7597BCD9F328E500829A02937875E`, 257,808 bytes, 624 lines. UID000092 is at line 349 and UID0000BY at line 453, each exactly once.
  - `by-file/-coverage-report.md`: SHA256 `78B5F2C81A5BDD86AC67655EA7152F0FC8541F1AF5A7DACF8EB7AB7ACB2B0D90`, 154,448 bytes, 317 lines. UID0000K1 is at line 118, UID0000K2 at line 119, and UID0000LR at line 180, each exactly once.
  - `by-type/by-struct/-coverage-report.md`: SHA256 `04ADEC897541A72ACC40FAB47489583F5D927FB483A4DA10B25DFD11CA51819E`, 58,207 bytes, 137 lines. UID0001VF is at line 86 exactly once between UID0001VE and UID0001VG.
  - `by-global/-coverage-report.md`: SHA256 `D25BDD5BB2F097258C9B76628317CDC773F8E4F1014B808BAB421E1CC0040226`, 99,098 bytes, 217 lines. UID0000QU is at line 84 exactly once between UID0000QT and UID0000QV.
- Settled tracker readback: historical command `000000016626`, refreshed `2026-07-22T22:16:12-04:00`, was superseded after B003 supervisor execution. Current `auto-generated/-ag-research-tracker.md` is command `000000016627`, refreshed `2026-07-22T22:30:22-04:00` by `deferred-generated-refresh`, SHA256 `5345FEAB06910E4942FC9A378176D2DF6BC615B1DBFC3B3A31013AF338D252FC`, 1,634,696 bytes, 6,507 lines. The by-memory section is at line 1665, `Not-Covered Files - Reconstructable` at line 1667, and UID000438 remains at line 3198 with `92/94`, combined `93.0`, reconstructable `true`, and zero report counts. Commands 16492 and 16626 are historical only.
- Applied/readback rule: every current row below preserves the still-valid predecessor facts, historicalizes only superseded facts, and carries the accepted result. Supervisor commands 16577/16579-16583 applied/validated the handoffs; the later identities above and exact rows below are current readback, not pending proposals.
- Current readback: `by-memory/-coverage-report.md` line 1863, UID000438 exactly once:

```text
        - [UID:000438][0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame](by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md) 0x004e4210-0x004e427c | method | NewHumanImageLib::ResolveCompositionPartFrame : reconstructable : 92% : very-strong : Exact 108-byte void private NewHuman composition frame resolver with 18 RenderComposition callsites and the typed FrameDrawRecord producer used by RenderComposition's 40-byte local vector; it preserves the observed selector clamp by falling back to descriptor zero when out of range, indexes the inline 0x10-byte motion slot and packed 9-byte frame row, projects the accepted FrameDrawRecord through g_pEPFLib's ResourceLayoutTable/ImageLib facet, offsets its sourceRect through RectBounds::Offset, and returns without writing when the selected slot.frames array is null, superseding the historical decompiler-shaped null-sequence return.
```

- Current readback: `by-memory/-coverage-report.md` line 1858, UID000417 exactly once:

```text
        - [UID:000417][0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord](by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md) 0x004e0d70-0x004e0dcf | method | NewHumanImageLib::LoadPartFrameDrawRecord : reconstructable : 91% : very-strong : Exact class/file-owned void part/descriptor/motion frame-record resolver with one-byte successor padding, two callers, accepted first-draft body, and canonical LoadFrameDrawRecord dependency; it indexes the inline 0x10-byte sequence slot rather than a nullable slot pointer, returns without writing when slot.frames is null, and projects the selected packed 9-byte frame row through g_pEPFLib->LoadFrameDrawRecord into the accepted FrameDrawRecord output.
```

- Current readback: `by-memory/-coverage-report.md` line 1862, UID000437 exactly once after UID0002VN and before UID000438:

```text
        - [UID:000437][0x004e4140-0x004e4205.NewHumanImageLibResolveCompositionPartRect](by-memory/0x004e4140-0x004e4205.NewHumanImageLibResolveCompositionPartRect.md) 0x004e4140-0x004e4205 | method | NewHumanImageLib::ResolveCompositionPartRect : reconstructable : 92% : very-strong : Exact 197-byte void NewHuman composition rectangle resolver with 31 ignored-result callsites; it guards zero descriptor count, falls back to descriptor zero for an out-of-range selector, indexes the inline motion slot and 9-byte frame row, copies or clears the raw archive RectBounds, and applies RectBounds::Offset without a source return value.
```

- Current readback: `by-memory/-coverage-report.md` line 1804, UID0002P6 exactly once:

```text
        - [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md) 0x004d1600-0x004d165d | method | ResourceLayoutTable::LoadFrameDrawRecord : reconstructable : 91% : very-strong : Exact g_pEPFLib non-virtual frame-table projection method with 81 direct xrefs across broad image-library consumers, bounds/count checks, payload pointer/span/end reconstruction, next-record boundary semantics, accepted 40-byte FrameDrawRecord output, adjacent raw-helper splits, and RenderComposition evidence that projected records are shallow-copied into its local vector through UID00017U/UID0004DO; repeated caller-side singleton receiver setup routes the body through ResourceLayoutTable/ImageLib and supersedes the historical free ImageFrameTable projection-helper ownership without discarding ImageFrameTable declaration dependency evidence.
```

- Current readback: `by-item/-coverage-report.md` line 56, UID0000UX exactly once; command16579 normalized the canonical UID0002P6 link and the normalized text is accepted current truth:

```text
- [UID:0000UX][LoadFrameDrawRecord_004D1600](by-item/LoadFrameDrawRecord_004D1600.md) : not_reconstructable : 89% : very-strong : Non-emitting name/index page for canonical [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md) ResourceLayoutTable::LoadFrameDrawRecord; live IDA-refresh evidence preserves the exact 0x004d1600-0x004d165d range, no-callee body, 81 direct xrefs across ten modeled functions and broader raw consumers, output-field writes, resolved payload pointer/span/end and next-record boundary relation, exact adjacent helper-page splits, final type-name caveats, and g_pEPFLib receiver route without duplicating the canonical by-memory body. The historical assigned/reconstructable ImageFrameTable wording is superseded by the current non-reconstructable index and ResourceLayoutTable/ImageLib-facet owner route.
```

- Current readback: `by-class/-coverage-report.md` line 349, UID000092 exactly once:

```text
- [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) : reconstructable : 88% : very-strong : Singleton new-human composition/render class with constructor/destructor/resource/vector method families, source-ready lifecycle and loaders, table/layout vocabulary, and the typed 80-byte NewHumanCompositionDrawCommand per-draw vector at +0x28 built by BuildCompositionDrawCommands, consumed by RenderPreparedComposition, reordered/removed/reset by Draw, and regenerated through marker-only UID0004DQ/UID0004PA compiler support; the restored [[CHILDREN]] hook now emits exact UID000417/UID000437/UID000438 methods with void member contracts, inline sequence slots, accepted FrameDrawRecord/RectBounds members, and g_pEPFLib projection routing without decompiler-shaped returns or pointer aliases.
```

- Current readback: `by-class/-coverage-report.md` line 453, UID0000BY exactly once:

```text
- [UID:0000BY][ResourceLayoutTable](by-class/ResourceLayoutTable.md) : reconstructable : 90% : very-strong : Non-virtual ImageLib singleton/facet method family over g_pEPFLib with live method boundaries, exact lazy EPF/EPD registry behavior, row-layout evidence, ImageLib singleton ownership, and shared ResourceLayoutTable::LoadFrameDrawRecord projection; caller-side singleton receiver proof routes UID0002P6 here, while B001-023's raw-buffer UID0002KQ and raw-record UID0002KT remain no-this helpers owned by file-level UID0000N5 ResourceLayoutTable, preserving the established class/facet split without introducing an unsupported independent object declaration.
```

- Current readback: `by-file/-coverage-report.md` line 180, UID0000LR exactly once:

```text
- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) : reconstructable : 91% : very-strong : NexusTK/render/NewHumanImageLib.cpp source root with singleton/vtable/layout/resource/method families, typed command/frame vectors, and working exact-child emission; BuildCompositionDrawCommands owns the per-draw 80-byte NewHumanCompositionDrawCommand vector at +0x28, RenderPreparedComposition consumes its exact frame/options/palette layout, Draw preserves ordering/removal/reset behavior, and marker-only UID0004DQ/UID0004PA regenerate typed push_back/grow support without handwritten STL bodies. Composition helpers UID000417/UID000437/UID000438 now preserve void member ABI, inline motion-slot/frame semantics, accepted RectBounds/FrameDrawRecord calls, and g_pEPFLib projection routing while all compiler vector support remains marker-only.
```

- Current readback: `by-file/-coverage-report.md` line 118, UID0000K1 exactly once:

```text
- [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md) : reconstructable : 90% : strong : Shared `NexusTK/render/ImageFrameTable.cpp` EPF/EPD frame-table module with exact ranges, caller/callee evidence, DAT/resource dependencies, source-ready `LoadImageFrameTable`, accepted archive/output declarations, and the ImageFrameTable-owned cleanup/raw-helper family. `DestroyOwnedImageBlock` remains the nested-payload cleanup companion proven here by constructor-store/destructor-destroy pairing, resolving the older separate cleanup-owner question; raw helpers UID00031T and UID00031U retain their exact 24-byte-record behavior, no-modeled-function/xref/pointer-hit caveats, current MemoryMan allocation/memmove route, and `CopyFrameRecordBounds` source shape. Historical B001-023 grouping of UID0002P6 `LoadFrameDrawRecord` under ImageFrameTable is superseded only for canonical body ownership: this file still supplies `ArchiveMetadataTable`, `PackedArchiveRecord`, `FrameDrawRecord`, and adjacent raw-helper declaration/context, while UID0002P6 and non-emitting alias UID0000UX now route through the `g_pEPFLib` ResourceLayoutTable/ImageLib facet.
```

- Current readback: `by-file/-coverage-report.md` line 119, UID0000K2 exactly once:

```text
- [UID:0000K2][ImageLib](by-file/ImageLib.md) : reconstructable : 91% : very-strong : NexusTK/render/ImageLib.cpp EPF image-library manager and source route for the non-virtual ResourceLayoutTable facet, with corrected IDA method bounds, constructor/destructor, g_pEPFLib, vtable/layout anchors, cache-list lifecycle, signature correction, registry/list methods, and UID0002P6 ResourceLayoutTable::LoadFrameDrawRecord sharing the singleton receiver route. B001-023 raw-buffer UID0002KQ and raw-record UID0002KT remain no-this helpers routed to file-level UID0000N5 ResourceLayoutTable rather than ImageLib class-method ownership, and the exact ImageLib/ResourceLayoutTable declaration boundary remains explicitly inferred.
```

- Current readback: `by-type/by-struct/-coverage-report.md` line 86, UID0001VF exactly once; this remains a replacement outcome, not an optional insertion:

```text
- [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md) : reconstructable : 87% : very-strong : Exact NewHumanImageLib object/vector/state and nested Part.tbl row-layout support with constructor/destructor agreement, singleton/vtable/resource/vector evidence, 0x20-byte descriptors, motion-indexed inline 0x10-byte NewHumanSequenceFrameTable slots, nullable slot.frames pointers, packed 9-byte frame rows, and accepted FrameDrawRecord consumers; the exact +0x28/+0x2c/+0x30 NewHumanCompositionDrawCommand vector uses 0x50-byte rows containing partId, a 40-byte FrameDrawRecord, 32-byte SurfaceSpriteBlitOptions, and a DLPalette pointer. Historical auxiliary/NewHumanDrawRecord and nullable-slot shorthand are superseded while original identifier spellings remain confidence caps rather than raw IDA labels.
```

- Current readback: `by-global/-coverage-report.md` line 84, UID0000QU exactly once:

```text
- [UID:0000QU][g_pEPFLib](by-global/g_pEPFLib.md) : reconstructable : 89% : strong : Process-wide EPF/EPD layout and image-resource singleton storage at `0x0067a744`, owned by ImageLib and exposed through the ResourceLayoutTable non-virtual facet. The 2026-06-16 live IDA recheck preserves exact constructor/destructor writes and clears, ImageLib vtable/lifecycle refs, broad UI/render consumers, aliases and layout references, saved `g_pEPFLib` / `ImageLib_vftable` labels, the final `ImageLib*` versus `ResourceLayoutTable*` base/header question, and the contrasting UID0002KQ raw-buffer caller-preload caveat where loading this global does not prove helper ownership. The formal no-standalone-definition marker remains correct. Accepted UID0002P6 `ResourceLayoutTable::LoadFrameDrawRecord` is now an explicit direct receiver consumer: its 81-xref cross-caller family systematically loads or preserves `g_pEPFLib` in `ecx` before the projection call, routing that canonical body through ResourceLayoutTable/ImageLib rather than a free ImageFrameTable helper even though the callee later reuses the incoming register.
```

- Attribution: B004 supplied but did not apply these rows. The supervisor applied and validated them; B004's current action was read-only row/root reconciliation. The rows above are exact current readback text, including the command16579 UID0000UX normalization.

## Follow-Up Actions

- Supervisor actor-boundary invariant:
  - determine current report lifecycle only from the latest validator-controlled path/status/footer, not from the durable terminal handoff token;
  - perform any required audit, Gate 2 verification, execution, or archive transition only through the supervisor workflow and validator lifecycle;
  - preserve the report body as completed B004 research/implementation history. At B004 handoff the active artifact had no validator lifecycle footer, and the report does not fabricate one.
- A-agent actions: none.
- B004 current authorized actions: none after the completed four-item callback and same-report reconciliation. Remain retained; do not edit ordinary/manual/generated/lifecycle files, validate, execute, move, or archive unless the supervisor issues a new exact instruction.
- B004 completed callback actions: historicalized the conflicting callee-owner/receiver/stale-score prose in `by-file/NewHumanImageLib.md` and `by-class/NewHumanImageLib.md` without deleting valid historical evidence, ran only the two authorized scoped validators serially, released each lease immediately, and reconciled this report.

## Confidence

- Recommendation confidence: very strong (`94`) for the primary target behavior and route.
- Score confidence: strong; `92/94` is conservative relative to the exact live proof and remaining spelling/facet caps.
- Remaining uncertainty: original private spellings and final ImageLib/ResourceLayoutTable header presentation only. No unresolved behavior, ownership, split, return, or first-draft C++ blocker remains.

## Validator Results

- Historical prior-callback validation record: all callback validators were ordinary scoped file validations, run serially from `source-3/project-documentation`. Every command exited `0` with `ok: 1`; no coverage-report or lifecycle validator was run.
- Accepted four-item reconciliation callback: B004 leased and edited one ordinary destination at a time, ran exactly one scoped file validator for that destination, and released the lease immediately before proceeding. Commands `16541` and `16542` both exited `0` with `ok: 1`. No coverage-report or lifecycle validator was run or probed.
- Supervisor manual coverage validation: after applying all twelve handoffs, the supervisor validated the six manual roots serially from `E:\NTK\GhidraBridge\source-3\project-documentation` under commands `16577`, `16579`, `16580`, `16581`, `16582`, and `16583`. Each exited `0` with `ok: 1`; all six generated refreshes were deferred. These are supervisor actions, not B004 commands.
- Historical command timestamps below are the exact `origin_command_timestamp` values recovered read-only from the corresponding validator-owned `tools/validator_queue/generated_refresh_results/zz-generated-refresh-<command-id>-*.result.json` artifacts; none is inferred from filesystem time.

| Command ID | Exact command target | Result and validator-owned effects |
| --- | --- | --- |
| `000000016501` | `python .\tools\validator.py --mode file --file by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:10:07-04:00`; completion/confidence 92/94, autogen registry hash, three reference-index adds, projected stats; generated deferred. |
| `000000016502` | `python .\tools\validator.py --mode file --file by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:11:27-04:00`; completion/confidence 91/93, autogen registry hash, UID links/reference index, projected stats; generated deferred. |
| `000000016503` | `python .\tools\validator.py --mode file --file by-memory/0x004e4140-0x004e4205.NewHumanImageLibResolveCompositionPartRect.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:12:18-04:00`; completion/confidence 92/94, autogen registry hash, UID link, projected stats; generated deferred. |
| `000000016504` | `python .\tools\validator.py --mode file --file by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:13:23-04:00`; completion/confidence 91/94, canonical owner/autogen owner `0000K1 -> 0000BY`, autogen hash, UID links/reference index, projected stats; generated deferred. |
| `000000016505` | `python .\tools\validator.py --mode file --file by-item/LoadFrameDrawRecord_004D1600.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:14:20-04:00`; completion/confidence 89/93, canonical owner `0000K1 -> 0000BY`, UID links/reference index, projected-stats row removal/rescore recommendation; generated deferred. |
| `000000016508` | `python .\tools\validator.py --mode file --file by-type/by-struct/NewHumanImageLibLayout.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:14:58-04:00`; completion/confidence 87/90 and projected-stats row updates/removals; generated deferred. |
| `000000016510` | `python .\tools\validator.py --mode file --file by-class/ResourceLayoutTable.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:15:37-04:00`; completion/confidence 90/91, UID links/reference index, projected stats; generated deferred. |
| `000000016512` | `python .\tools\validator.py --mode file --file by-file/ImageLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:16:16-04:00`; completion/confidence 91/90, UID links/reference index, projected stats; generated deferred. |
| `000000016513` | `python .\tools\validator.py --mode file --file by-file/ImageFrameTable.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:17:29-04:00`; metadata unchanged, UID links and three reference-index adds, projected stats; generated deferred. |
| `000000016514` | `python .\tools\validator.py --mode file --file by-global/g_pEPFLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:17:58-04:00`; metadata unchanged, UID0002P6 link/reference index and projected-stats row update; generated deferred. |
| `000000016515` | `python .\tools\validator.py --mode file --file by-file/NewHumanImageLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:19:08-04:00`; completion/confidence 91/91, UID links and projected stats; generated deferred. |
| `000000016516` | `python .\tools\validator.py --mode file --file by-class/NewHumanImageLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T18:20:16-04:00`; completion/confidence 88/90, autogen registry hash, UID link/projected stats, and historical bounded post-core generated `NewHumanImageLib.cpp` refresh under this command. |
| `000000016541` | `python .\tools\validator.py --mode file --file by-file/NewHumanImageLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T19:23:11-04:00`; projected-stats update, three reference-index adds for UIDs 0000BY/0000K2/0000QU, four UID-link insertions for UIDs 0000QU/0002P6/0000BY/0000K2, UID0000LR stats incremental no-op, and deferred generated refresh. |
| `000000016542` | `python .\tools\validator.py --mode file --file by-class/NewHumanImageLib.md --apply --queue-timeout 240` | exit 0, ok 1 at `2026-07-22T19:24:19-04:00`; projected-stats update, one reference-index add for UID0000QU, two UID-link insertions for UIDs 0002P6/0000QU, UID000092 stats incremental no-op, and deferred generated refresh completed under command 16542. |
| `000000016577` | `python .\tools\validator.py --mode file --file by-memory/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:30:56-04:00`; pre-existing missing refs UID0003MT/UID0003VS/UID00039L/UID0003T6, reference-index add UID000437, generated refresh deferred. |
| `000000016579` | `python .\tools\validator.py --mode file --file by-item/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:31:06-04:00`; pre-existing missing refs UID0003VI/UID0003VJ/UID0003VL, UID0002P6 link insertion/reference-index add and accepted normalization of the UID0000UX row, generated refresh deferred. |
| `000000016580` | `python .\tools\validator.py --mode file --file by-class/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:31:10-04:00`; no target-specific warning/effect beyond validated applied rows, generated refresh deferred. |
| `000000016581` | `python .\tools\validator.py --mode file --file by-file/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:31:13-04:00`; no target-specific warning/effect beyond validated applied rows, generated refresh deferred. |
| `000000016582` | `python .\tools\validator.py --mode file --file by-type/by-struct/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:31:15-04:00`; no target-specific warning/effect beyond validated applied row, generated refresh deferred. |
| `000000016583` | `python .\tools\validator.py --mode file --file by-global/-coverage-report.md --apply --queue-timeout 240` | supervisor command; exit 0, ok 1 at `2026-07-22T20:31:17-04:00`; pre-existing missing ref UID0003MS, generated refresh deferred. |

- Historical generated checkpoints:
  - command `000000016542`, refreshed `2026-07-22T19:24:19-04:00`, first established the reconciled NewHuman child readback;
  - command `000000016569`, refreshed `2026-07-22T20:20:37-04:00`, retained the same accepted placement with `NewHumanImageLib.cpp` SHA256 `564D3D65F15A13186E6C5E6691CDBD1E1BA9AE8506062B6420C47EE0ED360125` (43,719 bytes / 1,108 lines / 2,992 words), `ImageLib.cpp` SHA256 `2E0518BBEC17543849D91E96D671D42C57E0DBC06ACCCB67FB51EB5980743FEC` (12,739 / 323 / 1,131), and `ImageFrameTable.cpp` SHA256 `AF4E1766315C210B5CEE433B1150AAB40E0F5A52627D0F8FB39CB023CFC44DF8` (9,019 / 288 / 745). It is a bounded prior checkpoint, not current.
- Current generated readback: all three files carry validator command `000000016594`, refreshed `2026-07-22T20:38:53-04:00`:
  - `auto-generated/NexusTK/render/NewHumanImageLib.cpp`: SHA256 `9011EDA6FC031801D84DB486F2302A592C60DB53C30EEAAD0F37DD01E66F2829`, 43,719 bytes, 1,108 lines, 2,992 words. UID000417/UID000437/UID000438 counts are `1`/`1`/`1`; `ResolveCompositionPartFrame` count `2`; empty-marker/`sub_`/`dword_67A744` counts `0`/`0`/`0`.
  - `auto-generated/NexusTK/render/ImageLib.cpp`: SHA256 `871106A22EF30FD490D788F9E6FF013DDE4E64D3A2B33F47B7832D2DEC480AC5`, 12,739 bytes, 323 lines, 1,131 words. UID0002P6 count `1`; `LoadFrameDrawRecord` count `2`; empty-marker/`sub_`/`dword_67A744` counts `0`/`0`/`0`.
  - `auto-generated/NexusTK/render/ImageFrameTable.cpp`: SHA256 `953A3D46A367D71347EF2EE35CC87BC2601B05FD5E70C8B25D737C7F621C9558`, 9,019 bytes, 288 lines, 745 words. UID0002P6 and `LoadFrameDrawRecord` counts are both `0`; empty-marker/`sub_`/`dword_67A744` counts `0`/`0`/`0`.
- Current command-16594 generated semantic verification: definitions for `LoadPartFrameDrawRecord`, `ResolveCompositionPartRect`, and `ResolveCompositionPartFrame` remain present in NewHuman source; canonical UID0002P6 remains emitted only through ImageLib, not ImageFrameTable.
- Validator-owned generated/registry/reference/projected-stats changes are attributed to the scoped validator commands above, not direct B004 edits.
- Unresolved validator warnings/errors: none.

## Changed Files

- Modified directly in the accepted four-item reconciliation callback:
  - `by-file/NewHumanImageLib.md` - before `FDE3A7A52DB4BF09AFBCF7BF226FB6FD1BFCD62B9E5F847BE15259F681AD7F16`, 71,416 bytes / 322 lines; current after command `16541` `B2C84C06C049A5A54A4FC4E34252CD4C1E055066B31154AAAFB5063383CD42E9`, 73,025 bytes / 322 lines.
  - `by-class/NewHumanImageLib.md` - before `488EF660662725DB23C40AB79962B4BE5844C348FFAA25C25720AB423161A0BF`, 81,073 bytes / 384 lines; current after command `16542` `D0AAB502041509FA2014D78AB090C5BD4E544A783E09DA5DFD0C3F034CDF3B03`, 82,318 bytes / 384 lines.
  - this same report - reconciled in place after both ordinary validators; no report lifecycle move or execution occurred.
- Modified directly under the historical accepted core implementation callback; preserved post-core/pre-reconciliation hashes/metrics for audit:
  - `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md` - 11,120 bytes / 113 lines / SHA256 `89C5154F6703EA8A6EC69815DD912A231E1792CC7CC44E3905DEE12968C38078`.
  - `by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md` - 19,196 / 214 / `3305509174951E5EB080F724790B61F9BAA79B93A54DB875A3CF11AD51E7E669`.
  - `by-memory/0x004e4140-0x004e4205.NewHumanImageLibResolveCompositionPartRect.md` - 8,131 / 112 / `1F0CB6420227760EAD0E06F4AFEB0DD2FE84F635A76661EC3700627CE4483F20`.
  - `by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md` - 20,737 / 227 / `B29C2AC6B969C43AA90CF346B0B9E7530C1A9B900B621501CAACBE364D389602`.
  - `by-item/LoadFrameDrawRecord_004D1600.md` - 17,813 / 151 / `E22F924D65FEB52F5AD84E65E7E8F98F13E806E92A333220FB14C2E1BDAC4242`.
  - `by-type/by-struct/NewHumanImageLibLayout.md` - 45,949 / 315 / `A1E31B8E9E0C292020EBD947AB5532D18659D04EA97AC4CA4422F476F8A69471`.
  - `by-class/ResourceLayoutTable.md` - 17,834 / 146 / `C1B3DA39F6E53BB73270E9DA65F0EB944FD937CB32D2ECDEDBD7EEEBE26EE016`.
  - `by-file/ImageLib.md` - 26,978 / 179 / `DA57B255A4BB5E380C535FBEE0DA79C287C16BD7D44F4C7E773EC3AD4EF70918`.
  - `by-file/ImageFrameTable.md` - 27,240 / 153 / `64378909D96AB5C62068DF1B5D3D38938DA477428AB1D1753613065C73D46849`.
  - `by-global/g_pEPFLib.md` - 13,482 / 123 / `F45C0FFE72AD6B69388817E05E677F8BAA00327A2C34090E2BFA0CB140A542F7`.
  - `by-file/NewHumanImageLib.md` - 71,416 / 322 / `FDE3A7A52DB4BF09AFBCF7BF226FB6FD1BFCD62B9E5F847BE15259F681AD7F16`.
  - `by-class/NewHumanImageLib.md` - 81,073 / 384 / `488EF660662725DB23C40AB79962B4BE5844C348FFAA25C25720AB423161A0BF`.
- This report preserves all Gate 1 research, C438-001..040 ledger rows, five exact formal blocks, prior ordinary hashes, validator/generated evidence, negative evidence, historical assumptions, the original ten exact manual rows plus the two no-loss UID0000K1/UID0000QU rows, and implementation checklist detail while reconciling the completed callback state.
- Modified externally by the supervisor after B004 handoff, then independently read back by B004 without writes:
  - `by-memory/-coverage-report.md` - handoff applied/validated under command `16577`; current SHA256 `A7D812B60CB35399C541461DC0E214A8380B61D7793E2BB499B9247449A1A70B`, 1,982,596 bytes / 4,564 lines; four intended rows occur once each at lines 1804/1858/1862/1863.
  - `by-item/-coverage-report.md` - command `16579`, SHA256 `F634DDF3DCDD0C2BA8EC1CE07E1E17B61BA1EC3CEBE4B72A2206064FCCDBB0A1`, 29,408 / 87; UID0000UX occurs once with the accepted validator-normalized UID0002P6 link.
  - `by-class/-coverage-report.md` - handoffs applied/validated under command `16580`; current SHA256 `E4F6985825BD360CD322C6099EC097CF87E7597BCD9F328E500829A02937875E`, 257,808 / 624; two intended rows occur once each at lines 349/453.
  - `by-file/-coverage-report.md` - handoffs applied/validated under command `16581`; current SHA256 `78B5F2C81A5BDD86AC67655EA7152F0FC8541F1AF5A7DACF8EB7AB7ACB2B0D90`, 154,448 / 317; three intended rows occur once each at lines 118/119/180.
  - `by-type/by-struct/-coverage-report.md` - handoff applied/validated under command `16582`; current SHA256 `04ADEC897541A72ACC40FAB47489583F5D927FB483A4DA10B25DFD11CA51819E`, 58,207 / 137; UID0001VF occurs once at line 86.
  - `by-global/-coverage-report.md` - handoff applied/validated under command `16583`; current SHA256 `D25BDD5BB2F097258C9B76628317CDC773F8E4F1014B808BAB421E1CC0040226`, 99,098 / 217; UID0000QU occurs once at line 84.
- Validator-owned tracker readback, not edited by B004: historical commands 16492/16626 are superseded; current command `000000016627`, refreshed `2026-07-22T22:30:22-04:00` by `deferred-generated-refresh`, SHA256 `5345FEAB06910E4942FC9A378176D2DF6BC615B1DBFC3B3A31013AF338D252FC`, 1,634,696 bytes / 6,507 lines, retains UID000438 at line 3198 with `92/94`, combined `93.0`, reconstructable true, and zero report counts.
- Validator-owned effects: historical core autogen registry/reference/UID-link/projected-stats changes, commands `16541`/`16542` reference/UID-link/projected-stats changes, historical command-16569 and current command-16594 generated artifacts, and supervisor commands `16577`/`16579`-`16583` manual-root validation effects are itemized under Validator Results. B004 did not directly edit generated, manual, or validator-owned state.
- Lease sequence: B004 leased only `by-file/NewHumanImageLib.md` immediately before its edit, validated it with command `16541`, and released it immediately; B004 then leased only `by-class/NewHumanImageLib.md`, validated it with command `16542`, and released it immediately. Current lease table is empty.
- Renamed: none.
- Directly edited prohibited files by B004: none. The six manual roots above were supervisor-applied and supervisor-validated; B004 only read them. No generated/tracker/audit/supervisor/validator-state file, IDA database, or agent lifecycle state was directly edited by B004.
- Report execution/move/archive: not run or probed.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor Gate 1 validation passed before implementation.
- [x] Target/support destination set recorded and implemented.
- [x] No additional report target UID declared; all related UIDs are destination-specific support pages.
- [x] Current target state and actual evidence checked recorded.
- [x] Claim And Incorporation Ledger normalized claim-by-claim to the allowed action/state vocabulary with C438-039 assigned to target UID000438 and no blocked row.
- [x] Metadata/score changes applied as accepted.
- [x] Score-limiting blockers researched and resolved.
- [x] UID0002P6/UID0000UX owner route `0000K1 -> 0000BY` applied; UID0000UX remains non-reconstructable/non-emitting.
- [x] No split/new child required; source-facing corrections applied.
- [x] Source placement, range, padding, reclassification, and no-IDA-mutation decisions applied/confirmed.
- [x] Five exact managed blocks supplied and applied.
- [x] Third-party import not applicable.
- [x] Exact target/support facts incorporated at report-level detail.
- [x] Historical assumptions, rejected alternatives, and negative evidence preserved.
- [x] Stale Wave2/Wave3 artifacts ignored/rejected.
- [x] Open questions closed to highest-probability source forms with spelling-only caps.
- [x] Supervisor-approved ordinary scoped validators run serially and recorded.
- [x] Generated NewHuman output refreshed and verified; all twelve exact manual supervisor-owned rows were applied, serially validated, and independently read back exactly once.

Implementation callback pass:
- [x] Exact Gate 1 report accepted by supervisor for implementation.
- [x] All core details accepted in the historical implementation callback were incorporated at report-level detail; the later full-artifact audit identified four additional ordinary prose reconciliations, all now completed under the exact accepted callback below.
- [x] Primary target and core support destinations were verified independently against the ledger and current docs during the historical callback; that verification did not close the four later-discovered NewHuman class/file prose defects.
- [x] C438-001..C438-040 ledger actions/states reconciled to `incorporate`/`already-present`/`historicalize`/`not-applicable` and `applied`/`already-present`/`excluded-with-reason`; no claim is blocked, and C438-038/C438-039 record completed supervisor-owned manual application/validation and independent readback.
- [x] Metadata/score/owner/emitter/C++ changes applied; no split/rename/new child required.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence preserved.
- [x] Open behavior/ownership/return/layout questions resolved; only original spelling/header-presentation caps remain.
- [x] Fourteen ordinary scoped validators across the core and narrow reconciliation callbacks ran serially with exit 0/ok 1 and are recorded above.
- [x] Current command-16594 generated source hashes and semantic counts verified across NewHumanImageLib.cpp, ImageLib.cpp, and ImageFrameTable.cpp; command16569 remains recorded as the historical prior checkpoint, and all current supervisor-owned manual rows were independently read back.
- [x] At historical callback closure, the then-known unapplied items were manual `-coverage-report.md` rows, independent Gate 2, and lifecycle execution/move; the later full-artifact audit added four ordinary NewHuman prose repairs, now completed and checked below.
- [x] All B004 leases released; current lease table is empty.
- [x] No `execute_report` or equivalent lifecycle command was run or probed.

Same-report full-artifact Gate 1 repair and completed callback obligations:
- [x] Added the literal required `## Final Recommendation` heading with the implemented source/ownership/formal/no-code disposition.
- [x] Rebased lifecycle language to an archive-stable actor boundary: the terminal token is durable B004 handoff history, while current lifecycle truth comes from validator-controlled path/status/footer state; no footer is fabricated.
- [x] Marked Function / Child Inventory outcomes as applied/implemented rather than recommended repairs or reroutes.
- [x] Historicalized command-16487 omissions, preserved commands 16516/16542/16569 as bounded generated checkpoints, and explicitly superseded them with current command-16594 generated readback; historical tracker commands 16492/16626 are explicitly superseded by current command16627 readback.
- [x] Re-read all six supervisor-owned coverage roots after later project updates and recorded current hashes, metrics, row lines, and exactly-once occurrence proof for all twelve unchanged row bodies.
- [x] Preserved the original ten manual handoff rows and added no-loss UID0000K1/UID0000QU replacements, yielding twelve current replacements/insertions without dropping valid historical facts.
- [x] UID0000K1 manual replacement preserves the current ImageFrameTable loader/declaration/cleanup/raw-helper facts, resolves the historical cleanup-owner question, and historicalizes only UID0002P6 canonical body ownership.
- [x] UID0000QU manual replacement preserves current storage/lifecycle/vtable/consumer/alias/type-boundary/raw-helper-caveat facts and adds the exact UID0002P6 81-xref receiver-consumer route.
- [x] Corrected UID0001VF from a proposed optional insertion to an exact replacement of the existing by-type/by-struct row at line 86.
- [x] Preserved all 40 claims, all five exact formal blocks, ordinary hashes, validator/generated evidence, and prior checklist detail.
- [x] Narrowed all support-completion statements at the prior repair stage and preserved the four later ordinary-document obligations explicitly until authorization.
- [x] Reorganized `Recommended Support Doc Changes` without dropping any destination detail; the former pending subsection now records the accepted four-item callback as applied.
- [x] Supervisor accepted exact repaired artifact SHA256 `71588BFB1B8A865A1A2A8243A94513C6A95A882BE188B418ADC4EA298825BA11` at Gate 1 and authorized only the four listed ordinary reconciliations.
- [x] `by-file/NewHumanImageLib.md` historicalizes the ImageFrameTable-only/`dword_67A744` receiver rejection as superseded and states the accepted UID0002P6 ResourceLayoutTable/ImageLib-facet route through `g_pEPFLib`.
- [x] `by-file/NewHumanImageLib.md` preserves the dated UID000417 `88/90` promotion while stating current `91/93` in every repaired cluster.
- [x] `by-class/NewHumanImageLib.md` historicalizes the contradictory callee/receiver rejection, preserves UID000417 NewHuman ownership, and states the accepted UID0002P6 ResourceLayoutTable/ImageLib-facet route through `g_pEPFLib`/`dword_67A744`.
- [x] `by-class/NewHumanImageLib.md` preserves the dated UID000417 `88/90` promotion while stating current `91/93` in every repaired cluster.
- [x] Ran only the two authorized ordinary scoped validators serially as commands `16541` and `16542`, recorded exact results/hashes, verified their historical generated checkpoint, released both leases, and updated this same report.
- [x] Supervisor applied and serially validated all twelve manual handoffs under commands `16577`, `16579`, `16580`, `16581`, `16582`, and `16583`; B004 independently read back the six later current roots, accepted command16579's normalized UID0000UX link, and confirmed each intended row exactly once.
- [x] Actor-boundary invariant: B004 completed research, accepted callbacks, report reconciliation, and handoff without running/probing report lifecycle; any present or future Gate 2/execution/archive state is supervisor-owned and determined only from the latest validator-controlled path/status/footer, not from mutable report prose.

READY_FOR_SUPERVISOR_EXECUTE

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B004","command_id":"000000016628","destination_path":"executed-b-agent-research/B004/000438-NewHumanImageLibResolveCompositionPartFrame-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B004/research/000438-NewHumanImageLibResolveCompositionPartFrame-source-quality.md","timestamp":"2026-07-22T22:36:36-04:00","uid":"000438"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
