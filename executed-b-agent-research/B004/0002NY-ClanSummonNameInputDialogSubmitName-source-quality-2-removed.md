<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B004\0002NY-ClanSummonNameInputDialogSubmitName-source-quality-2.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Legacy Material Archive

Source report: [0002NY-ClanSummonNameInputDialogSubmitName-source-quality-2.md](0002NY-ClanSummonNameInputDialogSubmitName-source-quality-2.md)

This archive is non-authoritative and must never be executed. It is not a B report and receives no Gate, coverage, score, IDA, validator, or lifecycle credit.

## Removed Block CLEANUP-20260813-0002NY-SECTION21

The following report-local transaction and request material is preserved verbatim from source artifact SHA256 `7A4E14B3B97C0B3EE771E78806A5C508C55683261C5F45495B1DE83233E335A4`.

<!-- BEGIN CLEANUP-20260813-0002NY-SECTION21 -->
## IDA Rename / Type / Comment Recommendations

B004 performed read-only MCP research only. No current mutation is recommended. Gate 2B must fresh-read each protected entity separately and classify it as already present only if every literal prestate matches. Any mismatch is a hard stop requiring same-report repair; it is not permission to normalize, rename, retype, comment, define, reshape, or save.

Supervisor runtime controls are external procedure, not report automation:

| Control | Exact current-schema contract and stop condition |
| --- | --- |
| Open and bind | Call public `idb_open` with `input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"`, `preferred_session_id:"supervisor-b004-uid0002ny-gate2b"`, `mode:"force_headless"`, `run_auto_analysis:false`, `build_caches:true`, and `init_hexrays:true`. Treat `session.session_id` actually returned by that call as the only valid database argument; the preferred ID is not authority. Require the returned input/canonical path to resolve to `E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64`. |
| Runtime attestation | Before any stateful boundary, call only public `runtime_attestation` with `expected_database` equal to the exact returned session and `expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"`. Require top-level `schema_version:1`, `ok:true`, no errors, a fresh listener nonce/generation owned by the PID listening on `127.0.0.1:13337`, exactly one active worker route for the returned session/path, matching worker/parent process identity, and listener/worker manifests whose loaded `ida_pro_mcp`/`zeromcp` module `file`, `spec_origin`, and `resolved_path` values agree and remain contained under the attested source root. Any partial/mismatched result is a hard stop. |
| Durable backup | Before any mutation is even considered, the supervisor applies its current collision-safe durable-backup procedure to the exact canonical IDB, chooses a UID0002NY-specific backup path proven absent, forbids overwrite, records closed-disk identity/hash, and verifies the backup independently. The report does not implement this procedure. Backup failure or path collision is a hard stop. |
| Readback and save | Execute only the read-only P01-P08 calls below against the literal `session.session_id` returned by `idb_open`. Expected delta count is zero. The normal branch performs no `idb_save`. If a later independently accepted report adds a mutation, all direct in-session readbacks and protected no-change checks must pass before the sole `idb_save` call whose `database` argument is that literal returned session ID; no earlier or second save is permitted. |
| Persistence and rollback | After any future sole save, retire that exact worker under supervisor procedure, reopen the canonical path with `run_auto_analysis:false`, re-attest the newly returned session/path, and repeat every direct persistence readback. On any pre-save mismatch, stop with no save. On any post-save mismatch, use the guarded supervisor rollback procedure from the verified durable backup, prove closed-disk identity and fresh-session restored readback, and do not save the failed state. For this no-change report, an observed delta is itself failure and requires no-save retirement plus report repair. |

| ID | Exact entity/address | Literal current prestate | MCP-schema-safe action/no-change row | Evidence | Safety constraints | Expected direct readback |
| --- | --- | --- | --- | --- | --- | --- |
| P01 | Modeled function `[0x004892b0,0x0048939e)` | `lookup_funcs` name `ClanSummonNameInputDialog__OnSubmitText`, size `0xee`; `inspect_items(0x004892b0)` head `0x004892b0`, end `0x004892b1`, width 1, code true/data false, exact declaration `void __thiscall(ClanSummonNameInputDialog *this, const wchar_t *text)`; body SHA256 `1D266F1D3FD77EFD7D6AD7525CFE09DDDCCD0ABF4CDF52A8C4960B602AEB16A4`; address regular `absent`; address repeatable `absent`; function regular `ClanSummonNameInputDialog::OnSubmitText converts the submitted name with CP_ACP, builds {0x4b,8,1,length,name...}, and queues convertedLength+4 bytes; the observed 0x100 and post-copy 0x80 failures are compiler range instrumentation around indexed terminator writes.`; function repeatable `absent`; one inbound data xref `0x00615bd4`; zero ordinary callers. | No change. Read with `lookup_funcs(queries:"0x004892b0")`, `inspect_items(addrs:"0x004892b0")`, `get_comments(addrs:"0x004892b0")`, `stack_frame(addrs:"0x004892b0")`, `xrefs_to(addrs:"0x004892b0",limit:100)`, and bounded `get_bytes`; every call uses the exact returned database session. | Dated 2026-08-12 live readback and exact body receipt. | Do not rename, set type, define/undefine, alter bytes/range/xrefs, or write any comment channel. | Every listed field identical; no delta and no save. |
| P02 | Stack frame for `0x004892b0` | Exact ten rows in order: `convertedName@+0x8 size0x100 CHAR[256]`; `packetOpcode@+0x108 size0x1 _BYTE`; `packetSubtype@+0x109 size0x1 unsigned __int8`; `packetAction@+0x10a size0x1 unsigned __int8`; `packetNameLength@+0x10b size0x1 unsigned __int8`; `packetNameBytes@+0x10c size0x7c _BYTE[124]`; `var_4@+0x188 size0x4 _DWORD`; `__saved_registers@+0x18c size0x4 _DWORD`; `__return_address@+0x190 size0x4 _UNKNOWN *`; `text@+0x194 size0x4 LPCWCH`. | No change. Read with `stack_frame(addrs:"0x004892b0")` against the exact returned database session. | Dated 2026-08-12 live frame readback and exact access pattern. | Do not declare/delete/rename/coalesce rows, manufacture a physical 128-byte packet row, rename compiler rows, or change order/offset/size/type. | The exact ten rows return in the exact order and shape; no delta and no save. |
| P03 | Local type-catalog entity `ClanSummonNameInputDialog`, ordinal 1006 | `type_query` returns exactly one row: name `ClanSummonNameInputDialog`; declaration `ClanSummonNameInputDialog`; kind `other`; unknown/sentinel size; member count 0; members `[]`; related count 0. Address-regular, address-repeatable, function-regular, and function-repeatable comments are not applicable because this is a type-catalog entity, not an address/function head. | No change. Read with `type_query(filter:"ClanSummonNameInputDialog",count:20,include_decl:true,include_members:true,include_relationships:true,max_members:100,sort_by:"ordinal")` against the exact returned database session. | Dated 2026-08-12 live type-catalog readback plus function/vtable identity. | Do not declare/replace the type or add size, base, members, fields, or inheritance without a separate accepted layout report. | Exactly one incomplete zero-member ordinal-1006 row with the same declaration/kind/unknown size; no delta and no save. |
| P04 | Vtable data cell `[0x00615bd4,0x00615bd8)` | Head `0x00615bd4`, end `0x00615bd8`, width 4, data true/code false; name `absent`; type `absent`; bytes `B0 92 48 00`, SHA256 `E19379E4421B5118481ED669970E90E7735646E814A34C07430983E87BCB5542`, value `0x004892b0`; address regular `ClanSummonNameInputDialog::OnSubmitText virtual slot -> 0x004892b0.`; address repeatable `absent`; function regular `absent`; function repeatable `absent`; inbound xrefs to the cell 0. | No change. Read with `inspect_items`, `get_bytes`, `get_comments`, and `xrefs_to(limit:100)` for `0x00615bd4` against the exact returned database session. | Dated 2026-08-12 live item/comment/xref readback and sole target-xref direction. | Do not name, type, resize, redefine, patch, or alter any comment channel. | Exact head/end/width/kind/name/type/bytes/value/four comments/xref count; no delta and no save. |
| P05 | Pre-target padding `[0x004892a5,0x004892b0)` | Head `0x004892a5`, end `0x004892b0`, width 11, data true/code false; name `absent`; type `absent`; eleven bytes all `CC`, SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`; address regular `absent`; address repeatable `absent`; function regular `absent`; function repeatable `absent`; inbound xrefs 0. | No change. Read exact address with `inspect_items`, `get_bytes`, `get_comments`, and `xrefs_to(limit:100)` against the exact returned database session. | Dated 2026-08-12 live boundary readback. | Do not absorb into target, define code/function, rename/type, patch, resize, or comment. | Every listed field identical; no delta and no save. |
| P06 | Post-target padding `[0x0048939e,0x004893a0)` | Head `0x0048939e`, end `0x004893a0`, width 2, data true/code false; name `absent`; type `absent`; bytes `CC CC`, SHA256 `E3966E3275BE536A16092EC0CADF1638F718218E616FBBE8FF1C5E67FFF4DEF2`; address regular `absent`; address repeatable `absent`; function regular `absent`; function repeatable `absent`; inbound xrefs 0. | No change. Read exact address with `inspect_items`, `get_bytes`, `get_comments`, and `xrefs_to(limit:100)` against the exact returned database session. | Dated 2026-08-12 live boundary readback. | Do not absorb into target/successor, define code/function, rename/type, patch, resize, or comment. | Every listed field identical; no delta and no save. |
| P07 | Raw successor `[0x004893a0,0x004893d5)` | `lookup_funcs(0x004893a0)` returns `Not a function`; start item head `0x004893a0`, end `0x004893a1`, width 1, code true/data false; name `absent`; type `absent`; complete 53-byte range SHA256 `4D1B55FDF24D5FD5BEA4B883262C030DA150F9B29CEF2B7F82C65640800A6F4C`; address regular `absent`; address repeatable `absent`; function regular `absent`; function repeatable `absent`; inbound start xrefs 0. | No change. Read with `lookup_funcs`, `inspect_items`, bounded `get_bytes(size:53)`, `get_comments`, and `xrefs_to(limit:100)` for `0x004893a0` against the exact returned database session. | Dated 2026-08-12 live raw-successor readback and separate UID0002NZ documentation. | Do not define a function, absorb into target, rename/type, patch, alter instruction/data heads, or comment. | Not-a-function result plus exact start item, full-range hash, four absent comments, and zero start xrefs; no delta and no save. |
| P08 | Post-successor alignment `[0x004893d5,0x004893e0)` | Head `0x004893d5`, end `0x004893e0`, width 11, data true/code false; name `absent`; type `absent`; eleven bytes all `CC`, SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`; address regular `absent`; address repeatable `absent`; function regular `absent`; function repeatable `absent`; inbound xrefs 0. | No change. Read exact address with `inspect_items`, `get_bytes`, `get_comments`, and `xrefs_to(limit:100)` against the exact returned database session. | Dated 2026-08-12 live boundary readback. | Do not absorb into raw successor or `0x004893e0`, define code/function, rename/type, patch, resize, or comment. | Every listed field identical; no delta and no save. |

Dependencies already carrying current source-quality names/types are evidence, not additional Gate 2B actions: `PacketBufferWriteUInt8`, `MemoryMan_MemmoveWrapper`, `Socket_QueueAndSendPacket`, and `g_packetSender` remain unchanged.
<!-- END CLEANUP-20260813-0002NY-SECTION21 -->

## Removed Block CLEANUP-20260813-0002NY-OUTSIDE21-ATTESTATION

Source: [0002NY-ClanSummonNameInputDialogSubmitName-source-quality-2.md](0002NY-ClanSummonNameInputDialogSubmitName-source-quality-2.md)

This archive is non-authoritative and must never be executed. It preserves the exact report line removed during supervisor cleanup.

<!-- BEGIN CLEANUP-20260813-0002NY-OUTSIDE21-ATTESTATION -->
174: - Dated `2026-08-12` runtime evidence: public `runtime_attestation(expected_database:"supervisor-b007-uid0002YJ-rollback-fresh-20260811", expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64")` returned top-level `schema_version:1`, `ok:true`, and no errors. It bound listener PID `9800` at `127.0.0.1:13337` to one active worker route `127.0.0.1:55476`, worker PID `24676`, parent/redirector PID `2388`, exact returned session `supervisor-b007-uid0002YJ-rollback-fresh-20260811`, and canonical path `e:\\ntk\\resources\\nexustk\\nexustk.exe.i64`; listener and worker manifests both enumerated the loaded `ida_pro_mcp`/`zeromcp` modules. This is dated read-only research evidence, not authorization for B004 mutation or a permanent-current lifecycle assertion.
<!-- END CLEANUP-20260813-0002NY-OUTSIDE21-ATTESTATION -->
