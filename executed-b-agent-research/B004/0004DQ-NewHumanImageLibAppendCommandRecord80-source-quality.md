** TARGET-REPORT-UID:0004DQ **
** AUTHOR-AGENT-ID:B004 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID0004DQ NewHumanImageLib Append Command Record 80 Source Quality Research


## Finalized Report / Current Recommendation

- Implemented recommendation: UID0004DQ is UID-preservingly renamed to `NewHumanCompositionDrawCommandPushBackCompilerSupport`, remains reconstructable file-routed compiler support, and is now `92/93`.
- Final disposition: `[0x004e70a0,0x004e70e9)` is the out-of-line fast/slow `std::vector<NewHumanCompositionDrawCommand>::push_back` lowering used by `NewHumanImageLib::BuildCompositionDrawCommands`. It is not a source-authored helper method and received no handwritten allocator/copy body.
- Completed action: serial validator registration assigned UID0004PA to the exact `[0x004e5600,0x004e57bb)` full-capacity grow/insert companion; all accepted NewHuman file/class/layout/constructor/destructor/local-cluster/split-parent terminology, bounded VectorHelpers rejection, and ignored-padding updates are applied.
- Exact human source operation: `m_compositionDrawCommands.push_back(drawCommand);`. The target and grow helper each receive a formal source-disposition marker, not an independent function body.
- Confidence: high. The function boundaries, bytes, vector header, 80-byte element width, caller, slow path, source consumer, record field layout, and per-draw lifetime are directly observed. Exact original identifier spellings remain inferred and cap the recommendation below final-symbol confidence.

## Supporting Research

- The mandatory live IDA MCP pass used database `e98ecbb2`, obtained from a fresh `idb_list` after the supervisor recovery. `server_health` reported `status=ok` with auto-analysis, Hex-Rays, and string caches ready. A valid bounded `lookup_funcs(0x004e70a0)` and valid bounded byte reads succeeded before the evidence pass. One earlier malformed local `get_bytes` request used the wrong parameter key; the subsequent schema-correct bounded calls succeeded, so that client-side parameter error was not an MCP outage and no fallback evidence was substituted.
- Exact old-report searches used all of: `UID0004DQ`, `UID:0004DQ`, `0004DQ`, `0x004e70a0`, `0x004E70A0`, `0x004e70e9`, `0x004E70E9`, `NewHumanImageLibAppendCommandRecord80`, `AppendCommandRecord80`, `sub_4E70A0`, `BuildCompositionDrawCommands`, `RenderPreparedComposition`, `NewHumanImageLib`, `0x004e5600`, `sub_4E5600`, `80-byte command`, and `command-record append`.
- At search time, active B001-B004 research roots contained no Markdown reports matching those terms. B005's active root contained one Markdown artifact and no match. Therefore the evidence-time search found no countable direct active UID0004DQ report.
- `executed-b-agent-research/B011/00022R-ImageLibVectorSupportHelpers-source-quality.md` was opened and classified as adjacent split history: it created the exact target range and established one caller, an 80-byte append, and the `0x004e5600` grow route, but explicitly left the command type and helper disposition open.
- `executed-b-agent-research/B004/0004DN-NewHumanImageLibGrowCommandSideVector-source-quality.md` was opened and classified as a sibling control: it proves the separate pointer-ring subobject at NewHuman offset `+0x34` and the project precedent for a file-routed marker-only compiler helper. It does not establish UID0004DQ's 80-byte record.
- `executed-b-agent-research/B002/0004DO-CopyRecordVector40Range-source-quality.md` was opened and classified as an independent compiler-template control. Its exact current artifact SHA256 when inspected was `309A29349304AE630A1E9B9D9D7BB3025433BCF7498300D9FE074D5A3F572BF3`; it establishes `0x004554e0`-family 80-byte range-copy behavior and marker-only source disposition, while leaving UID0004DQ at its earlier state.
- The current B004 UID0004DN executed artifact SHA256 when inspected was `19F7B1F2AB168FB01460BA0FD7D83978C8F1A5C6606C91073ACBDD5513B82FA2`; that hash is evidence-time report identity, not a durable lifecycle assertion.
- `executed-b-agent-research/B011/0000LR-NewHumanImageLib-empty-emitter-family-source-quality.md`, `executed-b-agent-research/B011/0002JR-NewHumanImageLibConstructor-source-quality.md`, and `executed-b-agent-research/B010/00017S-NewHumanImageLibDestructor-source-quality.md` were opened as owner/lifecycle leads. They explain the historical generic `auxiliary` label but contain no direct child-specific resolution for UID0004DQ.
- `tools/leaser/Agents/Older-Research` exists and its three Markdown files produced no match for the exact UID/address/helper/source-family terms. `tools/leaser/Agents/SpecialReports` exists and its five Markdown files produced no match. The actual project archive root `project-documentation/archived` exists, contained zero Markdown reports when checked, and therefore produced no direct or related report.
- These searches preserve the prior reports as historical/adjacent evidence only. Every adopted binary claim was independently rechecked against the current IDB.

## Target

- Target UID: `0004DQ`.
- Historical pre-callback target path: `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md`; validator command `000000011478` confirmed it no longer exists after the bounded move.
- Current UID-preserved path: `by-memory/0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport.md`.
- Historical pre-callback source queue row: validator command `000000011445`, `auto-generated/-ag-research-tracker.md`, `## by-memory` -> `### Not-Covered Files - Reconstructable`; this report does not freeze that generated row as current after later validators.
- Evidence-time state: `86/90`, `CANONICAL_OWNER:0000LR`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000LR`, blank emitter position, `Nested:0`, comment-only formal marker, and zero countable direct-target reports.
- Exact binary range: `[0x004e70a0,0x004e70e9)`, 73 bytes.

## Current Target State

- Current metadata routes UID0004DQ and UID0004PA through [UID:0000LR][NewHumanImageLib](../../../../by-file/NewHumanImageLib.md) at `92/93`, reconstructable true, no class emitter position, and `Nested:0`. Both exact pages carry their accepted marker-only compiler-support blocks and complete binary/source-disposition evidence.
- UID0004DQ's title, evidence, typed vector route, negative evidence, history, and rejection set are resolved. UID0004PA is registered at the exact companion range with the same source owner/emitter and final score. Direct VectorHelpers fallback is removed only for this resolved pair.
- The accepted file/class/layout/constructor/destructor/local-cluster/split-parent and padding synchronizations are applied without unrelated score or formal-C++ inflation. All four managed formal destinations match the accepted report blocks.
- Manual `-coverage-report.md` files remain unedited and supervisor-owned. The exact replacement/addition text below now contains real UID0004PA and no registration placeholder.
- The immediate B004 waited readback at validator command `000000011498` / `2026-07-14T11:51:22-04:00` contained one UID0004DQ marker and one UID0004PA marker, zero Empty Emitter Markers, and zero helper definitions for either compiler-only range. A later external validator-owned refresh was visible at final read-only check as header `000000011513` / `2026-07-14T11:59:27-04:00` and preserved the same one/one/zero/zero structure.
- Implementation and scoped validation are complete. At callback completion no B004 lease remained. B004 performed no report execute/probe/move/archive/lifecycle command; external report validation, execution, count, path, move, and archive state remain supervisor/validator-owned and are not asserted by this artifact.

## Executive Recommendation

- Direct owner/emitter UID0000LR is retained because both ranges are compiler-generated support for source in `NexusTK/render/NewHumanImageLib.cpp`. UID000092 remains class context and emits no duplicate helper body.
- UID0004DQ was renamed descriptively, raised to `92/93`, retained reconstructable true with blank position and `Nested:0`, and received the exact formal marker below.
- `[0x004e5600,0x004e57bb)` was created first as separate compiler support and serially registered as UID0004PA before cross-references were added. Every pre-registration token has been replaced.
- The human behavior is represented exactly once as the typed source operation in the BuildComposition source route. Neither binary helper emits handwritten ABI/STL internals.
- The object `+0x28/+0x2c/+0x30` member triplet is now named `m_compositionDrawCommands`, with the inferred 80-byte `NewHumanCompositionDrawCommand` layout documented across the accepted destinations.

## Supervisor Active Recheck

- Historical supervisor Gate 1 accepted exact pre-callback report SHA256 `533225AA991F029464352D557C14942D67A6F2A1804104848A953227DA2EE735`; the accepted callback is now implemented and each changed ordinary destination is scoped-validator checked.
- The target does not require an internal split: its 73-byte function is exact and ends before seven alignment bytes. The directly triggered companion at `0x004e5600` is registered separately as UID0004PA, not as a child split of UID0004DQ.
- All source-bearing behavior in scope is resolved: the authored source operation is typed vector `push_back`; UID0004DQ and `0x004e5600` are compiler/template lowerings; the following IME range, separate `+0x34` pointer-ring support, and generic 40/80-byte range-copy controls are excluded.
- Report validation/execution/count/path/move/archive state is external supervisor/validator-owned. No implementation item remains in this artifact, and B004 neither performed nor directs lifecycle actions.

## Inference Research Guidance Check

- `by-structure.md` and the workflow evidence ladder require source-facing C++ to prefer the typed operation that explains the binary rather than decompiler-shaped ABI internals. That rule directly selects `push_back` plus marker-only compiler support.
- Existing names `AppendCommandRecord80`, `auxiliaryEntries`, and generic `VectorHelpers` fallback were treated as descriptive hypotheses, not original-symbol facts.
- Direct IDA facts are labeled as exact ranges, bytes, calls, offsets, strides, and branch behavior. Documentation facts are labeled as current/historical rows. `NewHumanCompositionDrawCommand`, `m_compositionDrawCommands`, and field names are explicitly inferred source-facing names.
- No current Wave2/Wave3 artifact was used. Any stale wave-era language encountered in older documentation was ignored under the current workflow.

## Heuristic / Inference Reanalysis And Validation

| Issue | Reanalysis and evidence | Resolution |
| --- | --- | --- |
| Generated helper name | No symbols/strings name `sub_4E70A0`; sole caller and typed vector receiver establish role. | Use descriptive `NewHumanCompositionDrawCommandPushBackCompilerSupport`, not a claimed original symbol. |
| Human helper versus compiler support | Fast path is five unaligned 16-byte copies plus end increment; slow path delegates allocation/growth to `0x004e5600`; caller ignores the physical return. | No human helper. This is an out-of-line STL `push_back` instantiation. |
| Record size | Five exact OWORD lanes copy offsets `+0x00,+0x10,+0x20,+0x30,+0x40`; end advances `0x50`; builder, renderer, reorder, remove, and grow helper all use division/stride 80. | Exact 80-byte element size. |
| Record layout | `BuildPartDrawCommand` initializes/fills `+0x00`, `+0x04`, `+0x2c`, `+0x34`, and `+0x4c`; render uses frame at `+0x04`, options at `+0x2c`, palette at `+0x4c`. Existing `FrameDrawRecord` is 40 bytes and `SurfaceSpriteBlitOptions` is 32 bytes. | Inferred source layout: `int partId` at `+0x00`; `FrameDrawRecord frame` at `+0x04`; `SurfaceSpriteBlitOptions options` at `+0x2c`; `DLPalette *palette` at `+0x4c`. |
| `+0x34` confusion | UID0004DN receives `this+13` and manages 4-byte pointer slots. BuildPart uses that side storage separately. | Reject merging UID0004DN with the `+0x28` 80-byte command vector. |
| `0x004e5600` role | It accepts vector header, insertion iterator, and source pointer; computes index/count by `/80`; allocates 1.5x storage; copies prefix/insert/suffix; frees old storage; updates all three pointers. | Separate marker-only full-capacity grow/insert support page. |
| Return type | Fast path returns source pointer while slow path returns inserted address; sole caller ignores EAX. | Physical return is optimizer/template artifact. Do not model a human return-bearing helper API. |
| Duplicate append | Part/tag case `0x3d` invokes UID0004DQ before the common append path, yielding two copies for that valid branch. | Preserve exact duplicate behavior; do not normalize as accidental decompiler duplication. |
| Source ownership | All three grow-helper callers are inside `BuildCompositionDrawCommands`; vector header is object `+0x28`; Draw consumes and clears it. | NewHuman file route, class-method source context, no generic direct owner. |
| Lifetime | Draw builds, optionally reorders/removes by 80-byte moves, renders, then resets end to begin while retaining capacity. Destructor frees storage through `0x4557f0`. | Per-draw scratch `std::vector<NewHumanCompositionDrawCommand>`. |
| Triviality | `0x4557f0` frees storage without element destructor calls; `0x4554e0` shallow-copies 80-byte rows. | Shallow/trivially destructible record; no hidden owning destructor. |
| Source declaration | No symbols prove exact spelling. `NewHumanDrawRecord` historically denoted the 40-byte `FrameDrawRecord` and would collide semantically. | `NewHumanCompositionDrawCommand` is the highest-probability descriptive human type name. |
| Reconstructable state | Source `push_back` can regenerate the lowering under the compatible compiler/STL; raw address-level body must not be handwritten. | Retain `RECONSTRUCTABLE:TRUE` with a non-empty disposition marker. |
| Score blockers | Range, bytes, callers, slow path, type width, layout, owner, liveness, source operation, padding, and generated route are resolved. Only original identifier proof and complete opaque option semantics remain unavailable. | Improve to `92/93`, not 100/100. |

Rejected alternatives:

- Standalone `AppendCommandRecord80` method: rejected because ECX is the vector header, not a `NewHumanImageLib *`, and the body is a canonical template lowering.
- Handwritten raw allocator/memmove/free compatibility body: rejected because it duplicates compiler/STL internals and risks a second emitted implementation.
- Direct UID0000P3 `VectorHelpers` ownership: rejected because the typed NewHuman member, three NewHuman-only helper callers, and exact method lifecycle resolve the source route.
- UID000092 class-method owner/position: rejected because there is no source declaration or ordinary method call ABI.
- UID00022R whole-island ownership: rejected because it is a non-emitting mixed helper index.
- UID000189 IME ownership: rejected by exact postpadding and the next function at `0x004e70f0`.
- `RECONSTRUCTABLE:FALSE`: rejected because the human typed operation is known and the project models analogous compiler lowerings as reconstructable markers.
- `NewHumanDrawRecord`: rejected as historical ambiguity with the distinct 40-byte `FrameDrawRecord`.

## Evidence Standards Used

- Primary evidence: current IDA MCP function lookup, decompile, disassembly, bounded bytes, SHA256, signature uniqueness, CFG/instruction metrics, xrefs, caller/callee inventories, and adjacent-function checks.
- Secondary evidence: current by-memory/by-class/by-file/by-struct docs, exact managed C++ blocks, manual coverage rows, generated readback, tracker row, and report-history searches.
- Controls: `0x4554e0` 80-byte forward range copy, `0x4557f0` 80-byte vector cleanup, `0x457a60` 40-byte frame initialization, UID0004DN's distinct pointer-ring storage, and UID0004DO's compiler-copy disposition.
- Negative evidence: no target-address data refs, no xref to either exclusive function end, no source-name strings, no direct report, no IME overlap, no element destructors, and no non-NewHuman caller to `0x004e5600`.
- The evidence is strong enough for source disposition and exact layout offsets. Lack of original PDB/source names and fully declared `SurfaceSpriteBlitOptions` prevents final-symbol confidence.

## Evidence Checked

- IDA MCP on `e98ecbb2`: fresh `idb_list`; healthy `server_health`; `lookup_funcs`, bounded `get_bytes`, decompile/disassembly/CFG, xrefs/callees/callers, byte signatures, raw address searches, and exact SHA256 for `0x004e70a0`, `0x004e5600`, `0x4554e0`, `0x4557f0`, `0x457a60`, `0x4e21d0`, `0x4e2eb0`, `0x4e2fe0`, and `0x4e06a0` as applicable.
- Exact target bytes: `55 8b ec 8b 51 04 39 51 08 74 31 8b 45 08 0f 10 00 0f 11 02 0f 10 40 10 0f 11 42 10 0f 10 40 20 0f 11 42 20 0f 10 40 30 0f 11 42 30 0f 10 40 40 0f 11 42 40 83 41 04 50 5d c2 04 00 ff 75 08 52 e8 1b e5 ff ff 5d c2 04 00`.
- Exact target SHA256: `A1E71734677EF2AB5F7830B6DCE9EBFB2E5B912532F9E5D4D5B1972EB81026F1`.
- Exact grow-helper SHA256 over 443 bytes: `5D17C5A25D456B051CD3829AB153A3273587A15D9EDE2FA6FB31EFB2F453B3AC`.
- Unique target wildcard signature: `55 8B EC 8B 51 ? 39 51 ? 74 ? 8B 45 ? 0F 10 00 0F 11 02 0F 10 40 ? 0F 11 42 ? 0F 10 40 ? 0F 11 42 ? 0F 10 40 ? 0F 11 42 ? 0F 10 40 ? 0F 11 42 ? 83 41 ? ? 5D C2 04 00 FF 75 ? 52 E8 ? ? ? ? 5D C2 04 00`; signature search returned unique at `0x004e70a0`.
- The grow-helper wildcard signature beginning `55 8B EC 83 EC 0C 8B 55 ? B8 67 66 66 66 53 56 57 8B F9` was also unique for its full generated signature.
- Docs opened: UID0004DQ target; UID0000LR file; UID000092 class; UID0001VF layout; UID0002JR constructor; UID00017S destructor; UID00017R local cluster; UID00022R split parent; UID0004DN/UID0004DO siblings; UID00017U compiler vector grow control; UID0000P3 VectorHelpers; UID0000VN ignored ranges; `SurfaceRenderCallbackTable`; and the existing `FrameDrawRecord` declaration in `by-item/EPFArchiveMetadataTable.md`.
- Manual coverage opened read-only: `by-memory/-coverage-report.md`, `by-file/-coverage-report.md`, `by-class/-coverage-report.md`, and `by-type/by-struct/-coverage-report.md` at every affected destination row.
- Report roots searched/classified: active B001-B005 research, `executed-b-agent-research`, `tools/leaser/Agents/Older-Research`, `tools/leaser/Agents/SpecialReports`, and actual project `archived`.
- Negative checks: no xref to `0x004e70e9` or `0x004e57bb`; zero absolute-VA and RVA pointer-byte matches for `0x004e70a0`; no exact current page/manual row for `0x004e5600`; no target overlap with IME; no source symbols; and no non-NewHuman call to the grow helper.
- Historical Gate 1 evidence-pass boundary: no IDA mutation, ordinary-doc edit, lease, implementation validator, generated refresh, or report lifecycle command was performed during that report-only phase.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | UID0004DQ is exactly `[0x004e70a0,0x004e70e9)`, 73 bytes. | very high | lookup/bytes | Renamed UID0004DQ evidence; validator `000000011478`, exit 0, ok 1. | incorporate | applied |
| C02 | Target SHA256 is `A1E71734677EF2AB5F7830B6DCE9EBFB2E5B912532F9E5D4D5B1972EB81026F1`. | very high | bounded bytes/hash | Renamed UID0004DQ exact-byte/hash evidence; `000000011478`. | incorporate | applied |
| C03 | Target has 24 instructions, three CFG blocks, and exact fast/full branches. | very high | disassembly/CFG | Renamed UID0004DQ CFG/behavior; `000000011478`. | incorporate | applied |
| C04 | ABI is `__thiscall`; ECX is a vector header; one explicit record-reference argument; physical `retn 4`. | very high | instructions/decompile | Renamed UID0004DQ ABI; `000000011478`. | incorporate | applied |
| C05 | Fast path copies five OWORD lanes and advances end by exactly 80 bytes. | very high | instructions | Renamed UID0004DQ behavior; `000000011478`. | incorporate | applied |
| C06 | Full path delegates to `0x004e5600`; caller ignores EAX, so no human return-bearing helper is justified. | very high | caller/callee | UID0004DQ and UID0004PA source disposition; `000000011478` / `000000011477` / `000000011489`. | incorporate | applied |
| C07 | Sole target caller is `0x004e2c83` in `BuildCompositionDrawCommands`. | very high | xrefs/function containment | UID0004DQ plus UID0000LR/UID000092/UID00017R sync; `000000011478` / `000000011498` / `000000011479` / `000000011483`. | incorporate | applied |
| C08 | Target prepadding is 12 `0xcc`; postpadding is 7 `0xcc`; no IME overlap. | very high | bytes/adjacency | UID0004DQ and preserved UID00022R detail; `000000011478` / `000000011484`. | preserve | applied |
| C09 | No data pointer refs or exclusive-end xrefs support a table, thunk, or alternate route. | high | negative searches | UID0004DQ negative-evidence section; `000000011478`. | incorporate | applied |
| C10 | Object offsets `+0x28/+0x2c/+0x30` are begin/end/capacity for the 80-byte command vector. | very high | caller receiver/constructor/destructor/Draw | UID000092, UID0001VF, UID0002JR, UID00017S, UID0000LR; validators `000000011479`-`000000011482` and `000000011498`. | incorporate | applied |
| C11 | Best source member name is inferred `m_compositionDrawCommands`. | high | construction/consumer semantics | UID000092/UID0001VF/UID0002JR/UID00017S/UID0000LR; `000000011479`-`000000011482`, `000000011498`. | rename-inference | applied |
| C12 | Best source element name is inferred `NewHumanCompositionDrawCommand`. | high | builder/render role and historical-name rejection | UID0004DQ/UID0004PA/UID0001VF/UID0000LR; `000000011478` / `000000011477` / `000000011489` / `000000011480` / `000000011498`. | rename-inference | applied |
| C13 | Record `+0x00` is inferred `int partId`. | high | BuildPart/comparison/reorder | UID0004DQ, UID0004PA, UID0001VF; `000000011478` / `000000011477` / `000000011480`. | incorporate | applied |
| C14 | Record `+0x04` is exact 40-byte `FrameDrawRecord`. | very high | initializer/builder/renderer/existing declaration | UID0004DQ, UID0004PA, UID0001VF; same scoped validators. | incorporate | applied |
| C15 | Record `+0x2c` is exact 32-byte `SurfaceSpriteBlitOptions`. | high | renderer/callback table/field offsets | UID0004DQ, UID0004PA, UID0001VF; same scoped validators. | incorporate | applied |
| C16 | Record `+0x4c` is `DLPalette *palette`. | high | `0x543e40` and render callback | UID0004DQ, UID0004PA, UID0001VF; same scoped validators. | incorporate | applied |
| C17 | BuildComposition initializes the record in exact order and appends it. | very high | decompile/disassembly | UID0000LR/UID000092/UID00017R plus compiler pages; `000000011498` / `000000011479` / `000000011483` / `000000011478` / `000000011477`. | incorporate | applied |
| C18 | Part/tag `0x3d` performs the target append and then the common append; duplicate behavior must remain. | very high | CFG/callsites | UID0004DQ, UID0004PA, UID0000LR, UID00017R; corresponding validators. | preserve | applied |
| C19 | Draw reorders/removes 80-byte records, renders, and resets end to begin while preserving capacity. | very high | Draw decompile | UID0000LR/UID000092/UID0001VF/UID00017R; `000000011498` / `000000011479` / `000000011480` / `000000011483`. | incorporate | applied |
| C20 | Render computes count by `/80`, consumes frame/options/palette, and invokes Surface callbacks. | very high | render decompile | UID0000LR/UID000092/UID0001VF/UID00017R; same scoped validators. | incorporate | applied |
| C21 | UID0004DN's `+0x34` pointer-ring storage is separate and unchanged. | very high | sibling caller/receiver | Preserved in target/class/file evidence; UID0004DN page remained read-only. | preserve | already-present |
| C22 | `0x4554e0` is generic 80-byte forward range-copy compiler support. | high | five callers/decompile | UID0004DQ/UID0004PA control evidence; `000000011478` / `000000011477`. | incorporate | applied |
| C23 | `0x4557f0` frees the 80-byte vector without element destructors, proving shallow destruction. | high | decompile/xrefs | UID0004DQ/UID0004PA/UID0001VF/UID00017S; `000000011478` / `000000011477` / `000000011480` / `000000011482`. | incorporate | applied |
| C24 | `0x457a60` initializes the embedded 40-byte FrameDrawRecord-shaped region. | high | decompile/offsets | UID0004DQ/UID0004PA/UID0001VF; `000000011478` / `000000011477` / `000000011480`. | incorporate | applied |
| C25 | `0x004e5600` is exactly `[0x004e5600,0x004e57bb)`, 443 bytes, SHA256 `5D17C5A25D456B051CD3829AB153A3273587A15D9EDE2FA6FB31EFB2F453B3AC`. | very high | lookup/bytes/hash | Registered UID0004PA exact range/hash; `000000011477`, exit 0; bounded text repair `000000011489`, exit 0, ok 1. | create | applied |
| C26 | Grow helper has 162 instructions, 23 blocks, complexity 9, `__thiscall`, two explicit args, and `retn 8`. | very high | analysis/disassembly | UID0004PA analysis/ABI; `000000011477` / `000000011489`. | incorporate | applied |
| C27 | Grow helper computes insertion index/count by `/80`, uses 1.5x growth, max `0x3333333`, and inserts one exact record. | very high | decompile/disassembly | UID0004PA behavior; `000000011477` / `000000011489`. | incorporate | applied |
| C28 | Grow helper preserves small/large allocation, 32-byte alignment, checked free, and prefix/suffix copy behavior. | very high | decompile/callees | UID0004PA behavior; `000000011477` / `000000011489`. | incorporate | applied |
| C29 | Grow helper has exactly three NewHuman BuildComposition callsites at `0x4e23a9`, `0x4e2cc5`, and `0x4e70e0`. | very high | xrefs | UID0004PA/UID0000LR/UID000092/UID00017R; helper and support validators. | incorporate | applied |
| C30 | Grow helper prepadding is 10 `0xcc`; postpadding is 5 `0xcc`; both require ignored-ledger entries. | very high | bytes | UID0004PA plus UID0000VN exact spans; `000000011477` / `000000011487`, exit 0, ok 1. | add | applied |
| C31 | UID0004DQ is compiler support regenerated by typed `push_back`, not a standalone source function. | very high | combined evidence | Exact UID0004DQ marker; `000000011478`; generated marker readback `000000011498`. | incorporate | applied |
| C32 | `0x004e5600` is the matching full-capacity compiler support and also gets no standalone body. | very high | combined evidence | Exact UID0004PA marker; `000000011477` / `000000011489`; generated marker readback `000000011498`. | incorporate | applied |
| C33 | Direct owner/emitter remains UID0000LR; class UID000092 is context only; position remains blank. | high | source route/ABI | UID0004DQ and UID0004PA headers plus file/class sync; `000000011478` / `000000011477` / `000000011498` / `000000011479`. | preserve | applied |
| C34 | UID0004DQ is UID-preservingly renamed and scored `92/93`, true, owner/emitter 0000LR, Nested 0. | high | blocker closure | Final path/header; validator `000000011478` confirmed UID/path uniqueness and old-path removal. | rename | applied |
| C35 | The new helper is serially registered as UID0004PA at `92/93`, true, owner/emitter 0000LR, blank position, Nested 0. | high | exact helper/source route | UID0004PA assigned by `000000011477`; final page revalidated by `000000011489`. | create-register | applied |
| C36 | Class/file/layout/local-cluster/split-parent terminology must be synchronized without unrelated score inflation. | high | current-doc comparison | UID000092 `000000011479`; UID0001VF `000000011480`; UID00017R `000000011483`; UID00022R `000000011484`; UID0000LR `000000011498`. | synchronize | applied |
| C37 | Constructor formal/member prose should use `m_compositionDrawCommands`. | high | exact member initialization | UID0002JR exact managed block/prose; `000000011481`, exit 0, ok 1. | bounded-formal-update | applied |
| C38 | Destructor formal/member prose should use the descriptive lowered cleanup alias and typed member. | high | cleanup helper/lifetime | UID00017S exact managed block/prose; `000000011482`, exit 0, ok 1. | bounded-formal-update | applied |
| C39 | Direct UID0004DQ/`0x004e5600` `VectorHelpers` fallback is rejected; unrelated VectorHelpers content remains. | high | typed route/caller exclusivity | UID0000P3 `000000011486` and UID0000LR `000000011498`; both exit 0, ok 1. | bounded-rejection | applied |
| C40 | Target and grow helper each require exact marker-only managed blocks; constructor/destructor require complete rebased managed blocks. | high | workflow/formal destination review | Four exact managed blocks applied in UID0004DQ, UID0004PA, UID0002JR, UID00017S and mechanically rechecked. | apply-formal | applied |
| C41 | Manual by-memory coverage needs target/new-helper/padding rows and exact affected aggregate-row replacements. | high | read-only manual-row comparison | Exact supervisor-owned text below now uses UID0004PA; manual coverage files were not edited. | supervisor-only | excluded-with-reason |
| C42 | Manual class/file/layout coverage rows are stale and need exact replacements; VectorHelpers row requires no change. | high | read-only manual-row comparison | Exact supervisor-owned replacements remain in this report; manual coverage files were not edited. | supervisor-only | excluded-with-reason |
| C43 | Generated readback contains one target marker, one grow-helper marker, no target empty marker, and no handwritten compiler bodies. | high | emitter plan/generated readback | B004 waited refresh `000000011498`, then later external header `000000011513`: UID0004DQ 1, UID0004PA 1, Empty Emitter Marker 0, helper definitions 0/0 in both checks. | verify-after-callback | applied |
| C44 | UID0004PA is the validator-assigned grow-helper UID and every pre-registration token must be replaced after serial registration. | very high | validator UID ownership rule | `000000011477` assigned UID0004PA; full report/destination scan found zero placeholder tokens. | replace-after-registration | applied |

## Positive Evidence Summary

- The target performs the exact canonical vector operation: compare end/capacity, shallow-copy one fixed-size element into end, increment end, otherwise call a grow/insert routine.
- The sole caller passes object `+0x28`, proving the vector belongs to NewHumanImageLib composition state rather than a generic free-standing container.
- Builder, grow helper, renderer, Draw reorder/remove, and cleanup all independently agree on an 80-byte stride.
- The record's four source-facing fields exactly fill 80 bytes and each has direct producer/consumer evidence.
- The full-capacity helper's three callers are all within the same BuildComposition function, strongly supporting a private template instantiation emitted in the NewHuman translation unit.
- Current project precedents UID0004DN, UID0004DO, UID00017U, and other NewHuman vector growth pages use reconstructable file-routed marker disposition rather than handwritten STL bodies.

## IDA MCP Facts

- Target: `sub_4E70A0`, range `[0x004e70a0,0x004e70e9)`, size `0x49`, 24 instructions, 3 blocks.
- Target ABI: ECX vector header, one source-record pointer/reference argument, `retn 4`.
- Target xrefs: exactly one incoming code xref at `0x004e2c83`; no end xref; no VA/RVA pointer-byte match.
- Target callee: only `sub_4E5600` at `0x004e70e0`.
- Target boundaries: twelve `0xcc` bytes before start and seven after end.
- Grow helper: `sub_4E5600`, range `[0x004e5600,0x004e57bb)`, size `0x1bb`, 162 instructions, 23 blocks, cyclomatic complexity 9.
- Grow ABI: ECX vector header; position and source arguments; `retn 8`; inserted-address physical return.
- Grow xrefs: three calls at `0x004e23a9`, `0x004e2cc5`, and `0x004e70e0`; no exclusive-end xref.
- Grow boundaries: ten `0xcc` bytes before start and five after end.
- `BuildCompositionDrawCommands`: `sub_4E21D0`, size `0xb23`, 729 instructions, 92 blocks, cyclomatic complexity 62, sole caller in Draw at `0x004e076f`.
- `BuildPartDrawCommand`: `sub_4E2EB0`, size `0x125`, fills the exact command fields and separately manages UID0004DN's side storage.
- `RenderPreparedComposition`: `sub_4E2FE0`, size `0x20f`, count and traversal stride 80, sole caller in Draw at `0x004e08d9`.
- Draw: `sub_4E06A0`, size `0x257`, builds, mutates, renders, and clears the scratch vector each draw.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `[0x004e5600,0x004e57bb)` | UID0004PA / `NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md` | full-capacity compiler grow/insert | true | UID0000LR | `92/93` | serially registered and fully documented |
| `[0x004e55f6,0x004e5600)` | UID0000VN | alignment padding | false/ignored | ignored ledger | ignored/100 | exact bytes added |
| `[0x004e57bb,0x004e57c0)` | UID0000VN | alignment padding | false/ignored | ignored ledger | ignored/100 | exact bytes added |
| `[0x004e70a0,0x004e70e9)` | UID0004DQ | fast/slow push_back compiler support | true | UID0000LR | `92/93` | UID-preserved rename/detail complete |
| `[0x004e7094,0x004e70a0)` | UID0000VN | existing prepadding | false/ignored | ignored ledger | ignored/100 | preserve |
| `[0x004e70e9,0x004e70f0)` | UID0000VN | existing postpadding | false/ignored | ignored ledger | ignored/100 | preserve |
| `[0x004e6ae0,0x004e70e9)` | UID00022R | non-emitting mixed helper index | false | NONE | `88/91` | preserve; update target row/link only |
| `[0x004e70f0,0x004e8433)` | UID000189 | IME family | unchanged | unchanged | unchanged | excluded |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x004e2c83` | BuildComposition -> UID0004DQ | sole out-of-line fast/slow push_back call for the case-`0x3d` command |
| `0x004e70e0` | UID0004DQ -> `0x004e5600` | capacity-full delegation |
| `0x004e23a9` | BuildComposition -> `0x004e5600` | special/ghost branch full-capacity append |
| `0x004e2cc5` | BuildComposition -> `0x004e5600` | common append full-capacity path |
| `0x004e076f` | Draw -> BuildComposition | command production |
| `0x004e08d9` | Draw -> RenderPreparedComposition | command consumption |
| `0x004e2f7a` | BuildPart -> UID0004DN | separate `+0x34` pointer-ring support, not command-vector storage |

## Documentation Evidence And IDA Status

- UID0004DQ now has the correct exact range, owner route, one caller, 80-byte append, UID0004PA grow callee, IME boundary, inferred command type, and final marker-only source disposition.
- UID00022R continues to treat the island as non-emitting and contains UID0004DQ as an exact child; its renamed target row/link/detail is synchronized without altering siblings.
- UID0000LR, UID000092, UID0001VF, UID0002JR, UID00017S, and UID00017R now use the typed `m_compositionDrawCommands` terminology at the exact `+0x28` triplet and preserve direct producer/consumer evidence.
- UID0004DN remains accurate for the separate `+0x34` pointer-ring state. UID0004DO and UID00017U provide compiler-support controls and remain unchanged.
- Waited validator command `000000011498` generated one UID0004DQ marker and one UID0004PA marker, with no Empty Emitter Marker and no handwritten helper body; generated output was inspected read-only.
- IDA MCP was healthy throughout the evidence pass. The final pre-hash recheck used a fresh transport session: `idb_list` returned only active database `e98ecbb2`, `server_health` remained `status=ok`, `lookup_funcs(0x004e70a0)` returned `sub_4E70A0` size `0x49`, and a bounded 16-byte `get_bytes` returned the expected target prefix. This is evidence-time availability, not an indefinite future-session assertion.

## Ranked Ownership Analysis

### 1. UID0000LR NewHumanImageLib file route

- Evidence for: all target/grow callsites reside in BuildComposition; the member header is object `+0x28`; Draw and destructor own its lifetime; analogous private compiler helpers route through the same file.
- Evidence against: no original source filename/debug symbol names the instantiation, and exact compiler emission can vary by toolchain.
- Decision: direct owner and emitter for target and new grow-helper marker.

### 2. UID000092 NewHumanImageLib class context

- Evidence for: the vector is a class data member used only by class methods.
- Evidence against: ECX is a vector header rather than class `this`; no method declaration or vtable slot exists; a class emitter position would imply a duplicate ordinary member function.
- Decision: contextual cross-reference and layout owner only, not target canonical owner/emitter.

### 3. UID0000P3 VectorHelpers or UID00022R aggregate

- Evidence for: binary bodies are template/vector mechanics and live in a helper-heavy address neighborhood.
- Evidence against: typed NewHuman-only source route is complete; UID00022R intentionally has no source owner; direct generic ownership would misplace private instantiation markers.
- Decision: reject direct ownership and reject fallback wording for this resolved family.

### Proposed new file/grouping, if applicable

- No new source file was introduced. The only new documentation page is registered UID0004PA compiler support at `by-memory/0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md`.
- It remains under UID0000LR `NexusTK/render/NewHumanImageLib.cpp` and contains a formal marker only.
- UID0004DN, UID0004DO, UID00017U, the following IME family, and generic shared helper pages remain separate.

## Source Placement

- Applied source placement: the human expression belongs in `NewHumanImageLib::BuildCompositionDrawCommands` in `NexusTK/render/NewHumanImageLib.cpp`; the target and grow-helper docs emit disposition comments through UID0000LR.
- This placement fits the three exclusive BuildComposition callsites, the object `+0x28` member, and the Draw/build/render lifecycle.
- Rejected placements: generic `VectorHelpers.cpp`, IME source, UID00022R aggregate source, class header method declaration, or a new standalone append/grow source file.
- Remaining uncertainty is limited to exact original type/member spellings and the source/header split for the inferred record declaration. It does not block the file route or no-standalone-body decision.

## Range / Split / Padding / Reclassification Analysis

- UID0004DQ's exact body is complete and requires no split. Its pre/post padding remains outside the body and already appears in UID0000VN.
- `0x004e5600` is an independently exact function now registered as sibling UID0004PA, not merged into target or UID00017U.
- `[0x004e55f6,0x004e5600)` is exactly ten `0xcc` bytes after UID00041G; `[0x004e57bb,0x004e57c0)` is exactly five `0xcc` bytes before UID00017U. Both are now recorded in UID0000VN.
- Reclassify the target's semantics from unresolved generic append helper to compiler-generated typed vector push_back support. Preserve true reconstruction because the source operation regenerates it.
- Parent UID00022R remains non-emitting with existing score/metadata; only the renamed child row and resolved detail change.

## Negative Evidence Summary

- No source symbol, RTTI name, string, or existing declaration proves an original helper/type/member spelling.
- No call treats UID0004DQ as a class method; ECX is always the vector header.
- No target callsite consumes the return value, so physical EAX differences do not justify a source API.
- No absolute VA/RVA pointer bytes reference the target, and no xref lands at its exclusive end.
- No non-NewHuman caller reaches `0x004e5600`; direct generic source ownership is unsupported.
- No per-element destructor is invoked by vector cleanup; the record does not hide an owning lifetime.
- No boundary crosses the following IME function, and no target byte is padding/data.
- Existing historical `NewHumanDrawRecord` shorthand describes a different 40-byte concept and is rejected.
- The target's specific source route is resolved, so a compatibility `VectorHelpers` fallback is no longer needed here.

## IDA Rename / Type / Comment Recommendations

- IDA mutation was neither requested nor performed during research or callback implementation.
- Documentation/source-facing target name: `NewHumanCompositionDrawCommandPushBackCompilerSupport` (descriptive inference).
- Documentation/source-facing grow name: `NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport` (descriptive inference).
- Inferred type: `NewHumanCompositionDrawCommand`, exact size `0x50`.
- Inferred member: `m_compositionDrawCommands`, exact vector header at object `+0x28/+0x2c/+0x30`.
- Inferred field names: `partId`, `frame`, `options`, `palette`; offsets/types are direct or strongly corroborated, spellings are inferred.
- Leave IDA's raw names unchanged unless a separate supervisor-authorized IDA task exists; documentation can carry the source-quality names without mutating shared IDA state.

## First-Draft C++ Recommendation

- Applied formal destinations: UID0004DQ marker, UID0004PA `0x004e5600` marker, UID0002JR constructor lexical member correction, and UID00017S destructor lexical member/cleanup correction. These are four complete managed destination blocks and mechanically match their destinations.
- UID0004DQ exact applied formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated std::vector<NewHumanCompositionDrawCommand>::push_back
// support for NewHumanImageLib::BuildCompositionDrawCommands.
// Regenerated by the human source operation:
//     m_compositionDrawCommands.push_back(drawCommand);
// Do not emit an independent source function for [0x004e70a0,0x004e70e9).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- UID0004PA exact applied formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated full-capacity grow/insert support for the
// std::vector<NewHumanCompositionDrawCommand> member used by
// NewHumanImageLib::BuildCompositionDrawCommands.
// Regenerated by the human source operation:
//     m_compositionDrawCommands.push_back(drawCommand);
// Do not emit an independent source function for [0x004e5600,0x004e57bb).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- UID0002JR exact complete managed block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewHumanImageLib::NewHumanImageLib()
    : LObject(),
      m_partEntries(),
      m_layerEntries(),
      m_motionEntries(),
      m_compositionDrawCommands(),
      m_loadedPartList(),
      m_acc2drwEntries(),
      m_hairColorEntries()
{
    g_pNewHumanImageLib = this;

    LoadMotionTable(L"Motion.tbl");

    {
        DATFile layerFile;
        unsigned char header[23];
        int layerCount = 0;

        layerFile.Open(L"Layer.tbl");
        layerFile.Read(header, sizeof(header));
        layerFile.Read(&layerCount, sizeof(layerCount));

        for (int index = 0; index < layerCount; ++index)
        {
            NewHumanLayerEntry layer = {};

            layerFile.Read(&layer.layerId, sizeof(layer.layerId));
            layerFile.Read(layer.name, sizeof(layer.name));
            layerFile.Read(&layer.childCount, sizeof(layer.childCount));

            if (layer.childCount > 0)
            {
                layer.childIndexes = new int[layer.childCount];
                layerFile.Read(layer.childIndexes,
                               sizeof(layer.childIndexes[0]) * layer.childCount);
            }

            m_layerEntries.push_back(layer);
        }

        layerFile.Close();
    }

    {
        DATFile partFile;
        unsigned char header[23];
        int partCount = 0;

        partFile.Open(L"Part.tbl");
        partFile.Read(header, sizeof(header));
        partFile.Read(&partCount, sizeof(partCount));

        for (int index = 0; index < partCount; ++index)
        {
            NewHumanPartEntry part = {};
            char partName[256];

            partFile.Read(&part.partId, sizeof(part.partId));
            partFile.Read(partName, 20);
            partFile.Read(&part.drawIndexBase, sizeof(part.drawIndexBase));
            partFile.Read(&part.descriptorBase, sizeof(part.descriptorBase));

            const int partNameLength = strlen(partName);
            MultiByteToWideChar(CP_ACP, 0, partName, partNameLength, part.name, 255);
            part.name[partNameLength] = L'\0';

            part.spriteArchive = ResolveSpritePartPath(part.name);
            m_partEntries.push_back(part);
        }

        partFile.Close();
    }

    LoadPartSpriteDescriptors();
    LoadAcc2DrwTable(L"acc2drw.tbl");

    {
        DATFile hairColorFile;
        char colorText[6];
        const int hairColorCount = m_partEntries[kNewHumanHairPartIndex].descriptorCount;

        hairColorFile.Open(L"HAIRCOL.TBL");

        for (int index = 0; index < hairColorCount; ++index)
        {
            memset(colorText, 0, sizeof(colorText));
            hairColorFile.ReadLine(colorText, sizeof(colorText));
            m_hairColorEntries.push_back(ParseUnsignedLongBase10(colorText));
        }

        hairColorFile.Close();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- UID00017S exact complete managed block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewHumanImageLib::~NewHumanImageLib()
{
    for (NewHumanPartEntry *part = m_partEntries.begin();
         part != m_partEntries.end();
         ++part)
    {
        ReleaseSpritePartArchive(part->spriteArchive);

        for (int descriptorIndex = 0;
             descriptorIndex < part->descriptorCount;
             ++descriptorIndex)
        {
            NewHumanPartFrameDescriptor &descriptor =
                part->frameDescriptors[descriptorIndex];

            delete [] descriptor.primaryFrameData;
            descriptor.primaryFrameData = NULL;
            descriptor.hasPrimaryFrameData = false;

            delete [] descriptor.secondaryFrameData;
            descriptor.secondaryFrameData = NULL;
            descriptor.hasSecondaryFrameData = false;

            for (int sequenceIndex = 0;
                 sequenceIndex < descriptor.sequenceCount;
                 ++sequenceIndex)
            {
                delete [] descriptor.sequenceFrames[sequenceIndex].frames;
                descriptor.sequenceFrames[sequenceIndex].frames = NULL;
                descriptor.sequenceFrames[sequenceIndex].frameCount = 0;
            }

            delete [] descriptor.sequenceFrames;
            descriptor.sequenceFrames = NULL;
            descriptor.sequenceCount = 0;
        }

        delete [] part->frameDescriptors;
        part->frameDescriptors = NULL;
        part->descriptorCount = 0;
    }

    for (NewHumanLayerEntry *layer = m_layerEntries.begin();
         layer != m_layerEntries.end();
         ++layer)
    {
        delete [] layer->childIndexes;
        layer->childIndexes = NULL;
        layer->childCount = 0;
    }

    for (NewHumanMotionEntry *motion = m_motionEntries.begin();
         motion != m_motionEntries.end();
         ++motion)
    {
        delete [] motion->frameOffsets;
        motion->frameOffsets = NULL;

        delete [] motion->frameData;
        motion->frameData = NULL;
        motion->frameCount = 0;
    }

    m_hairColorEntries.clear();
    DestroyAcc2DrwVector(&m_acc2drwEntries);
    DestroyLoadedPartListState(&m_loadedPartList);
    DestroyCompositionDrawCommands(&m_compositionDrawCommands);
    // First-draft shorthand for the lowered vector-storage cleanup at [UID:000185][0x004e5dd0-0x004e5e39.DestroyMotionVector](by-memory/0x004e5dd0-0x004e5e39.DestroyMotionVector.md);
    // not proof of a standalone source-authored helper body.
    DestroyMotionVector(&m_motionEntries);
    DestroyLayerVector(&m_layerEntries);
    DestroyPartVector(&m_partEntries);

    g_pNewHumanImageLib = NULL;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Behavior preservation: the two compiler pages do not duplicate ABI internals; the constructor/destructor blocks differ from the prior accepted blocks only in the resolved member/cleanup terminology. `DestroyCompositionDrawCommands` is explicitly descriptive first-draft shorthand for compiler-lowered vector destruction, not proof of a source-authored helper.
- The most plausible mid-2000s source shape is a private typed vector member manipulated with ordinary `push_back`, iteration/memmove-style vector operations, and automatic member destruction. The compiler generated the observed out-of-line capacity support.
- Third-party inline import is not applicable: this is project-owned NewHuman source using compiler STL support, not a static third-party source embed.

## Final Recommendation

- UID0004DQ was renamed in place with UID preserved and now has `92/93`, owner/emitter 0000LR, true, blank position, `Nested:0`, exact evidence, and exact marker.
- Exact UID0004PA support was serially registered before cross-referencing; all registration placeholders were replaced with the real UID.
- Only the named NewHuman target/support docs and ignored padding ledger were synchronized. Unrelated scores/formal content and all UID0004DN/UID0004DO/UID00017U behavior were preserved.
- UID00022R remains non-emitting and the following IME family remains unchanged.
- No unresolved in-scope binary blocker remains. Exact original identifier spellings remain bounded inference and are already reflected in the score cap.

## Recommended Target Doc Changes

- Applied validator-managed UID-preserving rename from `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md` to `by-memory/0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport.md`; old path absent and UID unique.
- Applied `COMPLETION:92`, `CONFIDENCE:93`; preserved `CANONICAL_OWNER:0000LR`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000LR`, blank position, and `Nested:0`.
- Replaced the deferred marker with the exact first formal block above.
- Incorporated exact range/bytes/hash/signature/CFG/ABI, one caller, one callee, fast path, full path, vector header, record layout, duplicate-append branch, source placement, liveness, padding, score rationale, historical assumptions, negative evidence, and rejected alternatives at report-level detail.
- Preserved the exact range and pre/post padding facts while replacing stale child-specific-pass and generic fallback wording.

## Recommended Support Doc Changes

- UID0004PA: serially registered at `92/93`, owner/emitter 0000LR, true, blank position, `Nested:0`; exact helper marker and all C25-C32 evidence applied.
- `by-file/NewHumanImageLib.md`: preserved `90/89` and source route; added the typed `+0x28` command vector, target/helper compiler disposition, exact build/render/Draw lifecycle, record layout, duplicate append, and direct fallback rejection.
- `by-class/NewHumanImageLib.md`: preserved `86/88` and formal block; replaced current `+0x28 auxiliary` and deferred helper wording with `m_compositionDrawCommands`, exact record layout, lifecycle, and final links.
- `by-type/by-struct/NewHumanImageLibLayout.md`: preserved `85/88` and formal disposition; replaced `auxiliaryEntriesBegin/End/Capacity` with inferred `NewHumanCompositionDrawCommand *compositionDrawCommandsBegin/End/Capacity` and documented exact `0x50` row offsets/confidence.
- UID0002JR: preserved `91/92` and all behavior; applied the complete rebased formal block and changed only the accepted generic auxiliary member terminology.
- UID00017S: preserved `90/91` and all behavior; applied the complete rebased formal block and typed cleanup terminology with the compiler-lowered/no-standalone-helper caveat.
- UID00017R: preserved NONE/FALSE/blank and score; added exact BuildComposition vector/record/duplicate append, Render consumption, and Draw lifetime facts.
- UID00022R: preserved `88/91`, NONE/FALSE/blank, `Nested:7`, and all siblings; updated only UID0004DQ's renamed row/link/detail and recorded UID0004PA as external companion support.
- `by-file/VectorHelpers.md`: preserved score/formal/unrelated helper content; removed only UID0004DQ/UID0004PA as unresolved compatibility fallback and recorded the direct typed NewHuman route.
- `by-memory/-ignored.md`: added exact `0x004e55f6-0x004e5600` ten-byte and `0x004e57bb-0x004e57c0` five-byte `0xcc` spans with adjacent UID00041G/UID0004PA/UID00017U evidence.
- `by-item/EPFArchiveMetadataTable.md`, Surface callback support, UID0004DN, UID0004DO, UID00017U, and IME docs remained verify-only with no edit or score change.

## Score And Metadata Recommendation

- Historical pre-callback target: `86/90`, owner/emitter 0000LR, true, blank position, `Nested:0`.
- Current target: `92/93`, same owner/emitter/reconstructable/position/nesting.
- Completion increased because the record type/fields, vector member, source operation, caller, full-capacity helper, source placement, liveness, compiler disposition, formal marker, and coverage omissions are resolved.
- Confidence increased because five independent flows agree: BuildComposition construction, UID0004DQ copy, UID0004PA growth, render consumption, and Draw/destructor lifetime.
- It remains below 100 because PDB/source names are absent, `SurfaceSpriteBlitOptions` remains an accepted opaque 32-byte support type, and exact historical compiler/STL version matching is a rebuild-equivalence concern.
- Registered UID0004PA is `92/93`, owner/emitter 0000LR, true, blank position, `Nested:0`.
- No support score changed. Terminology/detail synchronization did not inflate class/file/layout/aggregate/lifecycle scores.

## Open Questions With Attempted Resolution

- Is UID0004DQ a human helper? Resolved no through receiver ABI, body shape, slow-path delegation, ignored return, and typed caller.
- What is the exact record width? Resolved 80 through five copies, vector end arithmetic, grow division, Draw moves, and renderer count.
- What are the source-facing fields? Resolved to the four-entry layout above. Types/offsets are exact or strongly corroborated; names are explicitly inferred.
- Is `0x004e5600` part of UID0004DQ? Resolved no: it is a separate exact function with separate callers and padding, requiring its own page.
- Is the vector generic shared support? Resolved no for source ownership: all relevant callsites and lifetime are NewHuman-private. Shared helper callees remain compiler controls only.
- Why does case `0x3d` append twice? Exact branch behavior is proven, but semantic design intent is not recoverable without original source. The implementation requirement is resolved: preserve both appends. This does not lower source disposition confidence.
- Is `partId` the original field spelling? Not provable. It is the highest-probability descriptive name because comparisons/reordering use part categories 50-63. This bounded naming uncertainty is included in `93` confidence.
- Is `DestroyCompositionDrawCommands` an original helper? No claim is made. It is a first-draft shorthand preserving the current destructor block's lowered-cleanup style; prose must retain that caveat.
- No in-scope question remains as a future-work deferral. Only unavailable original symbol spellings and exact compiler-version equivalence remain confidence caps.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- Read-only comparison was performed against current `by-memory/-coverage-report.md`, `by-file/-coverage-report.md`, `by-class/-coverage-report.md`, and `by-type/by-struct/-coverage-report.md`. No manual file was edited.
- `by-memory/-coverage-report.md`: UID0004DQ, UID0004PA, and both new padding rows were absent in the inspected manual file; UID00017R and UID00022R rows were stale for the resolved vector/type/child identity; constructor/destructor rows did not contain the generic member token and need no row replacement; existing target-adjacent padding rows remain correct. This is a read-only evidence-time comparison, and manual coverage remains supervisor-owned.
- Exact target addition placement: as an indented child of UID00022R after the existing `0x004e7094-0x004e70a0` padding row and before `0x004e70e9-0x004e70f0` padding:

```text
        - [UID:0004DQ][0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport](by-memory/0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport.md) 0x004e70a0-0x004e70e9 | compiler-generated vector support | NewHumanCompositionDrawCommand push_back compiler support : reconstructable : 92% : very-strong : Exact one-caller std::vector<NewHumanCompositionDrawCommand>::push_back lowering for BuildCompositionDrawCommands; five 16-byte fast-path copies, 80-byte end advance, full-capacity UID0004PA route, typed +0x28 member ownership, exact record layout and per-draw consumer lifetime, marker-only UID0000LR emission, and no standalone helper body.
```

- Exact new helper/padding additions placement: immediately before current UID00017U row, in address order. The text now uses real validator-assigned UID0004PA:

```text
    - [UID:0000VN][-ignored](by-memory/-ignored.md) 0x004e55f6-0x004e5600 | padding | VectorGrowPart-to-composition-command-grow alignment : ignored : 100% : strong : Confirmed ten `0xcc` alignment bytes after UID00041G and before UID0004PA.
    - [UID:0004PA][0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport](by-memory/0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md) 0x004e5600-0x004e57bb | compiler-generated vector support | NewHumanCompositionDrawCommand full-capacity grow/insert support : reconstructable : 92% : very-strong : Exact three-caller full-capacity std::vector<NewHumanCompositionDrawCommand> grow/insert lowering with 80-byte rows, 1.5x capacity, aligned allocation, prefix/insert/suffix copy, checked old-storage free, vector triplet update, marker-only UID0000LR emission, and no standalone helper body.
    - [UID:0000VN][-ignored](by-memory/-ignored.md) 0x004e57bb-0x004e57c0 | padding | Composition-command-grow-to-FrameDrawRecord-grow alignment : ignored : 100% : strong : Confirmed five `0xcc` alignment bytes after UID0004PA and before UID00017U.
```

- Exact UID00017R row replacement:

```text
    - [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md) 0x004dfd10-0x004e4d79 | class/source aggregate | NewHumanImageLib : not_reconstructable : 85% : strong : Non-emitting NewHuman local-method index with exact source-bearing children; BuildCompositionDrawCommands owns the typed 80-byte NewHumanCompositionDrawCommand scratch vector at +0x28, including the preserved case-0x3d duplicate append and marker-only UID0004DQ/UID0004PA compiler support, while RenderPreparedComposition consumes frame/options/palette fields and Draw reorders, removes, renders, then retains capacity by resetting end to begin.
```

- Exact UID00022R row replacement:

```text
    - [UID:00022R][0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers](by-memory/0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers.md) 0x004e6ae0-0x004e70e9 | mixed helper split index | ImageLib vector/support helper island : not_reconstructable : 88% : very-strong : Non-emitting exact-child index spanning old-human table loaders, compiler/vector support, JPEG initialization, and NewHuman private helpers; UID0004DQ is resolved as marker-only typed NewHumanCompositionDrawCommand push_back support through UID0000LR, with all sibling boundaries and internal `0xcc` padding preserved and no whole-range source owner.
```

- Exact `by-file/-coverage-report.md` UID0000LR row replacement:

```text
- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) : reconstructable : 90% : strong : NexusTK/render/NewHumanImageLib.cpp source root with singleton/vtable/layout/resource/method families and typed compiler-vector support; BuildCompositionDrawCommands owns the per-draw 80-byte NewHumanCompositionDrawCommand vector at +0x28 and regenerates marker-only UID0004DQ/UID0004PA push_back/grow support, while RenderPreparedComposition consumes the exact frame/options/palette layout and Draw preserves ordering/removal/reset behavior without handwritten STL bodies.
```

- Exact `by-class/-coverage-report.md` UID000092 row replacement:

```text
- [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) : reconstructable : 86% : strong : Singleton new-human composition/render class with constructor/destructor/resource/vector method families; the +0x28 member is the typed 80-byte NewHumanCompositionDrawCommand per-draw vector built by BuildCompositionDrawCommands, consumed by RenderPreparedComposition, reordered/removed/reset by Draw, and regenerated through marker-only UID0004DQ/UID0004PA compiler support without misclassifying those lowerings as ordinary class methods.
```

- Exact `by-type/by-struct/-coverage-report.md` UID0001VF row replacement:

```text
- [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md) : reconstructable : 85% : strong : NewHumanImageLib object and nested-row layout support with constructor/destructor agreement, singleton/vtable/resource/vector evidence, and an exact +0x28/+0x2c/+0x30 NewHumanCompositionDrawCommand vector whose 0x50-byte rows contain partId, 40-byte FrameDrawRecord, 32-byte SurfaceSpriteBlitOptions, and DLPalette pointer; historical auxiliary/NewHumanDrawRecord shorthand is superseded while original identifier spellings remain confidence caps.
```

- Current `by-file/-coverage-report.md` UID0000P3 VectorHelpers row was inspected. It does not name UID0004DQ or claim direct ownership, so no row replacement is needed after the bounded support-doc fallback wording correction.
- The validator-owned tracker/generated coverage is read-only and receives no report-authored row text. Manual coverage remains supervisor-owned; B004 must not apply these rows directly.

## Follow-Up Actions

- Implementation, scoped validation, lease release, UID registration, placeholder replacement, and waited generated readback are complete; no accepted C01-C44 implementation item remains.
- Report validation, manual coverage application, execution, count, current path, move, and archive status are external supervisor/validator-owned state and are neither asserted nor directed by this artifact. B004 performed none of those lifecycle actions.
- A-agent work is not required for the source-disposition decision. Exact original symbol spellings could only be upgraded if new source/PDB evidence appears; current implementation does not depend on it.
- No second B-agent target is started by this report.

## Confidence

- Recommendation confidence: high (`93`).
- Score confidence: high; `92/93` reflects complete binary/source-disposition closure without pretending original-name proof.
- Remaining uncertainty: exact historical type/member/helper spellings and compiler-version rebuild identity. Neither affects range, behavior, owner, marker-only disposition, or the required source operation.

## Validator Results

- `000000011477`, `2026-07-14T11:40:46-04:00`: scoped `--mode file --file by-memory/0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md --apply`; exit 0; registration output assigned UID0004PA (the registration run did not print a separate `ok:` field); registry/reference updates succeeded; generated refresh deferred.
- `000000011478`, `2026-07-14T11:42:03-04:00`: scoped final-path validation of renamed UID0004DQ; exit 0, `ok: 1`; UID/path and known reverse references updated; generated refresh deferred. Warnings were pre-existing missing UID0003SC in VectorHelpers and missing UID0003UD references twice in UID00017R, not target failures.
- `000000011479`, `2026-07-14T11:42:56-04:00`: scoped `by-class/NewHumanImageLib.md`; exit 0, `ok: 1`; generated refresh deferred; no target warning.
- `000000011480`, `2026-07-14T11:43:43-04:00`: scoped `by-type/by-struct/NewHumanImageLibLayout.md`; exit 0, `ok: 1`; generated refresh deferred; no target warning.
- `000000011481`, `2026-07-14T11:44:18-04:00`: scoped UID0002JR constructor; exit 0, `ok: 1`; generated refresh deferred; no target warning.
- `000000011482`, `2026-07-14T11:45:02-04:00`: scoped UID00017S destructor; exit 0, `ok: 1`; generated refresh deferred; no target warning.
- `000000011483`, `2026-07-14T11:45:56-04:00`: scoped UID00017R local cluster; exit 0, `ok: 1`; generated refresh deferred. Two missing UID0003UD reference warnings were pre-existing and unrelated.
- `000000011484`, `2026-07-14T11:46:30-04:00`: scoped UID00022R split parent; exit 0, `ok: 1`; generated refresh deferred. The `autogen_value_invalid emitter_inactive_has_data` warning reflects the preserved accepted FALSE/non-emitting split marker, not this callback's target state.
- `000000011486`, `2026-07-14T11:47:14-04:00`: scoped `by-file/VectorHelpers.md`; exit 0, `ok: 1`; generated refresh deferred. Missing UID0003SC was pre-existing and unrelated.
- `000000011487`, `2026-07-14T11:47:46-04:00`: scoped `by-memory/-ignored.md`; exit 0, `ok: 1`; generated refresh deferred. Its broad 313 missing-reference diagnostics are pre-existing ignored-ledger diagnostics; both accepted spans validated.
- `000000011489`, `2026-07-14T11:48:20-04:00`: scoped UID0004PA final text after rename propagation; exit 0, `ok: 1`; generated refresh deferred; no target warning.
- `000000011498`, `2026-07-14T11:51:22-04:00`: scoped `by-file/NewHumanImageLib.md --apply --wait-generated`; exit 0, `ok: 1`; generated refresh completed under the same command/timestamp. Validator-owned side effects were registry/reference/statistics/generated-metadata refreshes, including `generated_metadata_refresh: 280`; broad project diagnostics reported 14 fallback child inserts, 84 missing explicit child markers, and 191 emitter-has-no-code rows, with no UID0004DQ/UID0004PA failure.
- Immediate generated readback of `auto-generated/NexusTK/render/NewHumanImageLib.cpp` was read-only at B004 waited command `000000011498`: UID0004DQ sections 1; UID0004PA sections 1; target/grow names 1 each; exact independent-function disposition lines 1 each; Empty Emitter Marker total 0; target/grow helper definitions 0/0. A later external validator-owned header `000000011513` at `2026-07-14T11:59:27-04:00` was observed in the final read-only check and retained those exact counts. Therefore there is one marker per compiler page, no handwritten compiler body, and no duplicate helper source body.
- MCP was not needed during the callback because implementation introduced no new binary uncertainty; the accepted live-MCP evidence remains unchanged.

## Changed Files

- Renamed once with UID preserved: `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md` -> `by-memory/0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport.md`; old path absent, UID0004DQ unique.
- Created and serially registered: `by-memory/0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md` as UID0004PA.
- Modified accepted support: `by-file/NewHumanImageLib.md`; `by-class/NewHumanImageLib.md`; `by-type/by-struct/NewHumanImageLibLayout.md`; UID0002JR constructor; UID00017S destructor; UID00017R local cluster; UID00022R split parent; `by-file/VectorHelpers.md`; and `by-memory/-ignored.md`.
- Modified report: `tools/leaser/Agents/Agent-B004/research/0004DQ-NewHumanImageLibAppendCommandRecord80-source-quality.md`.
- Validator-managed rename propagation touched reverse links only within accepted changed destinations: UID000092, UID0000LR, UID0000P3, UID00017R, and UID0004PA. No additional ordinary destination was introduced.
- Leases: each of the 11 ordinary pages had one B004 one-file edit window and was reread after lease acquisition; UID0004PA had a second short lease for its post-rename text repair. Its initial absent-path creation used the documented unregistered-new-file exception, followed immediately by the B004 lease before completion/registration. The UID0004DQ old path was leased before its single move. Every `unlease` returned success; the immediate callback-completion `tools/leaser/Agents/current_leases.md` readback contained no B004 or NewHumanImageLib lease.
- B004 manually edited no coverage, generated, tracker, audit, supervisor, validator-state, lifecycle, IDA, queue, lock, or archive file. Generated/registry/statistics side effects above were produced only by authorized validator commands.
- Report execution: not run. B004 performed no execute/probe/count/revalidation/move/archive/report-lifecycle command.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Historical supervisor Gate 1 accepted exact pre-callback SHA256 `533225AA991F029464352D557C14942D67A6F2A1804104848A953227DA2EE735` before implementation.
- [x] UID-preserving rename of UID0004DQ to `NewHumanCompositionDrawCommandPushBackCompilerSupport` completed; old path absent and UID unique.
- [x] Serially created/registered `[0x004e5600,0x004e57bb)` as real UID0004PA before cross-referencing it.
- [x] Replaced every pre-registration token in report, docs, and supervisor coverage text; zero placeholder remains.
- [x] Applied UID0004DQ `92/93`, owner/emitter 0000LR, true, blank position, `Nested:0`, exact formal marker, and all C01-C24/C31-C34 facts.
- [x] Applied UID0004PA `92/93`, owner/emitter 0000LR, true, blank position, `Nested:0`, exact marker, and all C25-C32 facts.
- [x] Updated UID0000LR NewHuman file detail without score/formal inflation.
- [x] Updated UID000092 class `+0x28` member/method/helper detail without score/formal inflation.
- [x] Updated UID0001VF layout fields and exact 80-byte record layout without score/formal inflation.
- [x] Applied the complete UID0002JR constructor managed block and preserved every unrelated line/score/fact.
- [x] Applied the complete UID00017S destructor managed block and preserved every unrelated line/score/fact.
- [x] Updated UID00017R local-cluster build/render/Draw facts while preserving NONE/FALSE/blank and score.
- [x] Updated UID00022R renamed child row/link/detail while preserving all siblings, score, metadata, `Nested:7`, and blank C++.
- [x] Removed only the stale UID0004DQ/UID0004PA direct VectorHelpers fallback; preserved unrelated helper content and score.
- [x] Added the two exact padding spans to UID0000VN.
- [x] Preserved UID0004DN, UID0004DO, UID00017U, Surface callback support, FrameDrawRecord declaration, and IME family unchanged except accepted validator-managed reverse links.
- [x] Preserved exact binary facts separately from inferred source-facing names and retained every rejected/historical/negative alternative.
- [x] Applied exactly four managed formal destination blocks; no body-only/sample C++ was added elsewhere.
- [x] Confirmed third-party import is not applicable.
- [x] Ran scoped validators for every changed ordinary by-* page and recorded command IDs, timestamps, exits, ok fields, warnings, and side effects.
- [x] Released each one-file lease immediately after its edit/validation window; no lease remains.
- [x] Ran final authorized B004 `--wait-generated` command `000000011498` and verified one UID0004DQ marker, one UID0004PA marker, zero target Empty Emitter Markers, zero handwritten compiler-helper bodies, and no duplicate helper source body.
- [x] Re-read generated `NexusTK/render/NewHumanImageLib.cpp` without editing it; recorded exact counts/content for B004 command `000000011498` and the later external validator-owned freshness header `000000011513`.
- [x] Kept all manual coverage rows supervisor-owned and updated this report's exact text with UID0004PA.
- [x] Updated C01-C44 to legal `applied`, `already-present`, or `excluded-with-reason` states with claim-by-claim destination proof.
- [x] Updated Validator Results, Changed Files, current-state wording, and callback checklist archive-neutrally.

Implementation callback pass:
- [x] Report accepted by supervisor for implementation at the exact historical pre-callback SHA above.
- [x] All accepted target/support facts incorporated at report-level detail.
- [x] Real validator UID0004PA captured and every pre-registration token removed.
- [x] Metadata/score/owner/emitter/rename/C++ changes applied exactly.
- [x] Historical assumptions, rejected alternatives, and negative evidence preserved.
- [x] All in-scope open questions remain closed without future-work deferral.
- [x] Scoped validators run and exact results recorded.
- [x] Final waited generated readback completed and assertions recorded.
- [x] All one-file leases released.
- [x] No forbidden file was manually edited and no forbidden report/IDA command was run; authorized validator-owned generated/registry/statistics side effects are recorded above.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B004","command_id":"000000011516","destination_path":"executed-b-agent-research/B004/0004DQ-NewHumanImageLibAppendCommandRecord80-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B004/research/0004DQ-NewHumanImageLibAppendCommandRecord80-source-quality.md","timestamp":"2026-07-14T12:06:58-04:00","uid":"0004DQ"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
