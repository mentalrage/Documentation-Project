** TARGET-REPORT-UID:0004D1 **
** AUTHOR-AGENT-ID:B004 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | Medium **
# 0004D1 StartupNoticeLoadAssets Source-Quality Research


## Finalized Report / Current Recommendation
- Current callback disposition: implementation callback complete for [UID:0004D1] `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md`; ready for supervisor Gate 2/execute verification. Agent-B004 did not run `execute_report`, any lifecycle/archive command, registry command, manual report move, generated-file edit, or coverage edit.
- Final implemented target disposition: target metadata is now `COMPLETION:88`, `CONFIDENCE:91`; `CANONICAL_OWNER:0000O5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000O5`, blank `EMITTER_POSITION_OPTIONAL`, `Nested:0`, and the existing formal `static void StartupNoticeLoadAssets(StartupWindow *startupWindow, HDC dc)` block were preserved.
- Callback application summary: target/support docs now incorporate current MCP session `13191102` facts, preserve the B008 exact-child split model, keep the helper out of ImageLoaders/Browser/Win32 and the non-emitting aggregate/tracker pages, and record scoped validator results for every changed by-* file.
- Confidence: high for exact physical range, source owner/emitter route, PCX resource behavior, rect/state initialization, generated output presence, and no-direct-route negative evidence. Confidence remains capped below final because IDA still has no function object or decompilation at `0x005818d0`, no direct start refs, no VA/RVA pointer-pattern hits, current IDA names the PCX loader `sub_4A17B0`, and exact original helper/member/type names remain source-facing inference.

## Supporting Research
- Lifecycle/status notes: historical report-only work produced this report, then supervisor Gate 1 passed and sent an implementation callback. During callback, Agent-B004 edited only in-scope by-* docs plus this report, ran scoped file validators, released leases, and stopped before supervisor execution. No `execute_report`, dry-run/probe, registry lifecycle, archive, manual report-move command, manual generated edit, coverage edit, validator-state edit, or supervisor-ledger edit was run by B004.
- MCP pause/resume history: initial work paused because the MCP listener was reachable but `idb_list` returned `{"sessions":[],"count":0}`. Supervisor restored MCP; the current evidence pass was redone with active session `13191102`.
- Current MCP evidence uses session `13191102` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. `server_health` returned `status:ok`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- Current docs and reports were treated as leads and rechecked against live MCP before the recommendation. Existing formal C++ from the B008 split remained source-shaped and was preserved; the target page now carries current UID-specific evidence and the accepted score refresh.

## Target
- Target UID: `0004D1`.
- Target path: `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md`.
- Historical pre-callback source queue/report row: `auto-generated/-ag-research-tracker.md` -> `## by-memory` -> `### Not-Covered Files - Reconstructable`, row `86/88`, reconstructable true, reports column blank/zero for this UID-specific report.
- Current supervisor classification: reconstructable by-memory exact child under `StartupWindow.cpp`, created by the B008 StartupWindow family split.
- Current implemented scores and parent state: target `88/91`; canonical owner/emitter [UID:0000O5] `by-file/StartupWindow.md`; parent [UID:0001IO] `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md` and [UID:0000VI] `StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md` remain non-emitting split/tracker pages and now record the direct UID0004D1 `88/91` child coverage.

## Current Target State
- Current implemented metadata: `COMPLETION:88`, `CONFIDENCE:91`, `CANONICAL_OWNER:0000O5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000O5`, blank `EMITTER_POSITION_OPTIONAL`, `Nested:0`.
- Current owner/emitter/reconstructable state: correct and unchanged. The helper belongs to [UID:0000O5] `StartupWindow` / `NexusTK/app/StartupWindow.cpp`, not to ImageLoaders or a broad aggregate emitter.
- Current C++/emitter state: formal C++ is preserved exactly as `static void StartupNoticeLoadAssets(StartupWindow *startupWindow, HDC dc)`. Generated `auto-generated/NexusTK/app/StartupWindow.cpp` refreshed after scoped validators and shows UID0004D1 at `Completion:88 | Confidence:91` with the same helper body.
- Current open questions, blockers, placeholder names, stale assumptions, or contradictory claims: target evidence now includes MCP session `13191102`, exact no-function/decompile-failure proof, string/xref inventory, raw disassembly facts, unique signature, pointer-pattern negative evidence, generated freshness, and explicit confidence caps for inferred `StartupWindow` field/button layout names. Dependency docs still use the accepted source-facing `LoadPcxImage` name while current IDA session `13191102` reports the loader as `sub_4A17B0`; this is documented as an IDA-name drift caveat, not a source-placement blocker.
- Related target/support docs checked: target page, `by-file/StartupWindow.md`, `by-class/StartupWindow.md`, `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md`, `by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md`, `by-resource/startup-update-notice-resources.md`, `by-file/ImageLoaders.md`, `by-memory/0x004a17b0-0x004a18a8.LoadPcxImage.md`, generated `auto-generated/NexusTK/app/StartupWindow.cpp`, generated research tracker, executed B008/B010 reports, and current sibling B-agent reports for StartupWindow helpers.
- Current artifact/lifecycle status: this report is the post-callback artifact in `tools/leaser/Agents/Agent-B004/research/0004D1-StartupNoticeLoadAssets-source-quality.md`, ready for supervisor Gate 2/execute verification.

## Executive Recommendation
- Best disposition: keep UID0004D1 as a source-bearing, file-local `StartupWindow.cpp` helper named `StartupNoticeLoadAssets`.
- Metadata recommendation: raise the target to `COMPLETION:88`, `CONFIDENCE:91`; preserve owner, reconstructable, emitter, optional position, `Nested:0`, and formal C++.
- Source-placement recommendation: keep direct source route through [UID:0000O5] `StartupWindow`. `ImageLoaders` owns `LoadPcxImage`; StartupWindow owns the notice-specific orchestration, asset-state guard, button image slots, rect bounds, and state initialization.
- Exact condition for higher confidence later: a future accepted IDA DB function-boundary/name/type pass, original symbols/source, or a class-layout report would need to prove the exact helper declaration, `StartupNoticeButton` layout, image-slot ordering, and member names. Current evidence is already sufficient for first-draft source C++ and the score increase.

## Supervisor Active Recheck
- Historical supervisor report-only instruction: resume Agent-B004 UID0004D1 research after MCP restore, use active current IDA MCP evidence, and stop at `READY_FOR_SUPERVISOR_GATE1_REVIEW`. Superseding callback state: Gate 1 passed and implementation is now complete; current stop point is `READY_FOR_SUPERVISOR_EXECUTE`.
- Split status: no new split is required. B008 already created UID0004D1 as the exact child for `0x005818d0-0x005819cc`; parent [UID:0001IO] and tracker [UID:0000VI] are already non-emitting pages.
- Source-bearing child status: UID0004D1 already exists, emits formal C++, and generated `StartupWindow.cpp` contains the helper. This report supplies the missing UID-specific evidence and implementation checklist so the target can leave the direct-report gap.

## Inference Research Guidance Check
- IDA facts are separated from documentation evidence and source-facing inference. Current MCP controls exact range, bytes, disassembly, no-function status, decompile failure, xrefs, string addresses, unique signature, and pointer-pattern negatives.
- Documentation evidence used as lead material: B008/B010 split reports, current by-* pages, generated `StartupWindow.cpp`, and resource/ImageLoaders docs. Those were not copied blindly; live MCP rechecked the target behavior and negative route evidence.
- Existing documentation assumptions treated as uncertain: exact field names such as `m_startNormalImage`, `m_startHoverImage`, `m_startPressedImage`, `m_buttons`, and `StartupNoticeButton`; exact helper spelling `StartupNoticeLoadAssets`; and current IDA name persistence for `LoadPcxImage`.
- Wave2/Wave3 mentions encountered through old `wave3_data_issues.md` search hits were not used as authority. Current by-* docs, executed B reports, generated output, and live MCP supersede stale Wave2/Wave3 material.

## Heuristic / Inference Reanalysis And Validation
- Range and raw liveness: MCP `lookup_funcs` reports `0x005818d0` as `Not a function`, but `disasm` and `insn_query` show valid source-shaped code starting with `push ebp; mov ebp, esp; push ebx; mov ebx, ecx`, ending at `0x005819c9` with `retn 4`, followed by four `0xcc` bytes at `0x005819cc-0x005819d0`. This is live raw helper code, not padding.
- Function/source shape: the body uses `ecx` as the `StartupWindow *` receiver, `[ebp+8]` as the `HDC`, checks `this+0x04`, sets the byte to `1`, loads six PCX assets through `sub_4A17B0`, initializes rects through `sub_4B7C50`, queries PCX width/height through `sub_4A1780` and `sub_4A1790`, writes state slots at `+0x30` and `+0x50`, then returns with `ret 4`.
- Resource behavior: current MCP `get_string` confirms strings at `0x0062d530` `brm_main.pcx`, `0x0062d540` `brm_st_a.pcx`, `0x0062d550` `brm_st_b.pcx`, `0x0062d560` `brm_st_c.pcx`, `0x0062d570` `brm_ex_a.pcx`, and `0x0062d580` `brm_ex_b.pcx`.
- Xref behavior: each PCX string has exactly two xrefs, one modeled WndProc ref in `sub_581100` and one raw UID0004D1 ref. `xrefs_to 0x004a17b0` reports twelve loader callsites: six in `sub_581100` and six raw UID0004D1 callsites with `fn:null`.
- Source placement: the PCX decoder/path loader stays in ImageLoaders; UID0004D1 stays in StartupWindow because this body is the startup notice asset gate and button layout initializer over StartupWindow object fields.
- Field/type inference: source-facing field names from generated/docs are plausible and useful, but current binary proof only gives offsets. The target implementation should not claim exact original layout spelling. The WndProc drawing code also contains decompiler-sensitive image/state selection logic, so image-slot order and `StartupNoticeButton` member names remain confidence caps.
- Current IDA name/type status: `lookup_funcs 0x004a17b0` in session `13191102` returns `sub_4A17B0`, not `LoadPcxImage`. Current by-* docs and generated output use `LoadPcxImage` as an accepted source-facing function name. This report does not request IDA DB renames, but target docs should be clear that `LoadPcxImage` is the source-facing dependency name and `sub_4A17B0` is the current IDA-session name.
- Rejected alternatives: no-code/padding treatment, broad [UID:0001IO] aggregate emission, by-item [UID:0000VI] tracker emission, ImageLoaders ownership, Browser ownership, Win32/API ownership, direct public `StartupWindow` method route, and moving this behavior into `StartupWindowUpdateCheckWindowProc` are all rejected by current range, source route, and split evidence.

## Evidence Standards Used
- MCP evidence: `idb_list`, `server_health`, `lookup_funcs`, `decompile`, `disasm`, `insn_query`, `xrefs_to`, `get_bytes`, `get_string`, `search_text`, `find` for code/data/immediate refs, `find_bytes` for VA/RVA pointer patterns, `make_signature_for_range`, `callees`, `entity_query`, `type_query`, and `int_convert`.
- Documentation evidence: current target/support by-* pages, generated `auto-generated/NexusTK/app/StartupWindow.cpp`, generated research tracker, executed B008/B010 reports, sibling B-agent StartupWindow helper reports, resource docs, and ImageLoaders/LoadPcxImage docs.
- Evidence ladder: current MCP facts override older report text when there is conflict. Existing generated output is used as current emitter/freshness evidence and source-shape lead, not as sole behavioral proof.
- Evidence strength: sufficient for exact range, source placement, generated route, formal helper retention, and score improvement. Confidence is capped by no IDA function object, decompile failure at raw start, no direct raw-start route, no current IDA source-facing helper name, and inferred field/layout spellings.

## Evidence Checked
- IDA MCP checks performed:
  - `idb_list`: active session `13191102`, input `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `11632`, `is_active:true`, `is_analyzing:false`.
  - `server_health`: `status:ok`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready.
  - `lookup_funcs`: `0x005818d0`, `0x005819cc`, and `0x005819d0` are not functions; WndProc `0x00581100` is `sub_581100`, size `0x4aa`; loader dependency `0x004a17b0` is `sub_4A17B0`, size `0xf8`; draw helper `0x00581dc0` is `sub_581DC0`, size `0x75`.
  - `decompile 0x005818d0`: failed with `Decompilation failed at 0x5818d0`.
  - `disasm 0x005818d0`: disassembled under `<no function>` in `.text`, with prologue, guard check, six `sub_4A17B0` calls, and PCX string refs.
  - `insn_query 0x005818d0-0x005819cc`: 80 raw instructions, no function metadata, final `retn 4` at `0x005819c9`.
  - `get_bytes`: `0x005818c3-0x005818d0` is thirteen `0xcc` bytes; start bytes are `55 8b ec 53 8b d9 ...`; tail bytes at `0x005819bc` include state write, pops, and `c2 04 00`; `0x005819cc-0x005819d0` is four `0xcc` bytes.
  - `xrefs_to`: zero xrefs to `0x005818d0` and zero xrefs to `0x005819cc`; `xrefs_to 0x004a17b0` returns twelve callsites, six in `sub_581100` and six raw UID0004D1 calls.
  - PCX string xrefs: each `brm_*.pcx` string has two refs, one WndProc and one raw UID0004D1 ref.
  - `search_text` in target range for `sub_4A17B0`: six hits at `0x005818f1`, `0x00581901`, `0x00581911`, `0x00581921`, `0x0058196d`, and `0x00581984`.
  - `search_text` in WndProc range for `sub_4A17B0`: six hits at `0x00581227`, `0x00581237`, `0x00581246`, `0x00581256`, `0x005812a3`, and `0x005812ba`.
  - `find code_ref`, `find data_ref`, and `find immediate` for `0x005818d0` and `0x005819cc`: zero matches.
  - `find_bytes` for VA/RVA patterns `D0 18 58 00`, `D0 18 18 00`, `CC 19 58 00`, and `CC 19 18 00`: zero matches.
  - `make_signature_for_range 0x005818d0-0x005819cc`: unique signature reported `unique:true`.
  - `callees`: WndProc calls `sub_4A17B0`, `sub_581DC0`, button helpers, paint/cursor/window APIs; loader `0x004a17b0` calls DAT/string/PCX factory dependencies; draw helper calls GDI/DC and PCX bitmap accessors.
  - `entity_query` for StartupWindow/StartupNotice/LoadPcxImage names: current session contains StartupWindow constructor/run/vtable/RTTI names but no `StartupNoticeLoadAssets` raw-function name; current `lookup_funcs` reports the loader as `sub_4A17B0`.
- by-* docs, support docs, old reports, generated reports, and trackers checked: target, StartupWindow file/class, StartupWindow aggregate, unmodeled helper tracker, startup resource page, ImageLoaders file, LoadPcxImage exact child, generated `StartupWindow.cpp`, generated research tracker, executed B008/B010 reports, and current sibling StartupWindow helper reports.
- Negative checks performed: no IDA function at raw target start/end, no decompile at raw start, zero xrefs to start/end, zero code/data/immediate ref finds to start/end, zero VA/RVA pointer-byte hits for start/end, no IDA source-facing name for `StartupNoticeLoadAssets`, no support for ImageLoaders/Browser/Win32 ownership.
- Failed, unavailable, or intentionally skipped checks and why: during the historical report-only pass no validators were run because no by-* docs were edited. During the accepted callback, scoped validators were run for every changed by-* doc and are recorded under `Validator Results`. No IDA DB rename/type/comment operation was run or requested. No broad survey/callgraph/full-list calls were used because the assignment required narrow/paged MCP discipline.

## Claim And Incorporation Ledger
| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| D1-001 | UID0004D1 remains source-bearing StartupWindow file-local helper `StartupNoticeLoadAssets`, not padding/no-code. | High | MCP raw disasm/insn range proves executable prologue, body, and `ret 4`; generated output shows helper body. | Target `Status`/`Evidence`; generated `StartupWindow.cpp` UID0004D1 block | incorporate | applied: target evidence expanded and generated header/body refreshed by validators `000000006403`/`000000006408` |
| D1-002 | Historical pre-callback target metadata moved from `86/88` to implemented `88/91`. | Medium-high | Current UID-specific MCP pass closes direct-report evidence gap; no-function/no-ref/layout caps remain. | Target metadata and generated output | incorporate | applied: target header set to `88/91`; validator `000000006403` reported `completion_update` and `confidence_update` |
| D1-003 | Preserve `CANONICAL_OWNER:0000O5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000O5`, blank optional position, and `Nested:0`. | High | StartupWindow support docs, B008 split, generated output, and target object-field behavior. | Target metadata | already-present | already-present: metadata preserved unchanged in target header |
| D1-004 | Preserve existing formal C++ block as exact source-facing first draft. | Medium-high | MCP proves guard, six PCX loads, rect/state writes, and generated route; field/layout names remain inferred. | Target formal `RECONSTRUCTION_CPP CODE`; generated `StartupWindow.cpp` | already-present | already-present: formal block left unchanged; generated output still emits `StartupNoticeLoadAssets` |
| D1-005 | Active MCP proof source is session `13191102`, with healthy IDB, Hex-Rays, and strings cache. | High | `idb_list` and `server_health` current output. | Target `Status`/`Evidence` and support notes | incorporate | applied: target records session `13191102`; file/class/aggregate/tracker support notes reference the same session where relevant |
| D1-006 | IDA still has no function object and decompile fails at `0x005818d0`. | High | `lookup_funcs 0x005818d0` `Not a function`; `decompile` error. | Target evidence and confidence cap | incorporate | applied: target evidence records no function/decompile failure |
| D1-007 | Exact range is `0x005818d0-0x005819cc`, size `0xfc` / 252 bytes (Verified with int_convert.py), ending with `ret 4` at `0x005819c9`. | High | `insn_query`, `get_bytes`, `int_convert`; `0x005819cc-0x005819d0` padding. | Target evidence; support rows | incorporate | applied: target and support rows record exact range, size, and `ret 4` |
| D1-008 | Preceding `0x005818c3-0x005818d0` and successor `0x005819cc-0x005819d0` are padding, not part of the helper. | High | `get_bytes` shows thirteen prebytes and four postbytes of `0xcc`. | Target evidence; parent/tracker/resource support if stale | incorporate | applied: target records both padding spans; aggregate/tracker support preserves exact split |
| D1-009 | Body uses `ecx`/`ebx` as StartupWindow receiver, `[ebp+8]`/`edi` as `HDC`, and `this+0x04` as the asset-loaded guard. | High | Raw disasm `mov ebx, ecx`, `mov edi, [ebp+8]`, `cmp byte ptr [ebx+4],0`, `mov byte ptr [ebx+4],1`. | Target evidence and formal C++ rationale | incorporate | applied: target evidence records receiver/HDC/guard roles |
| D1-010 | Six PCX strings and six loader calls are exact: `brm_main`, `brm_st_a`, `brm_st_b`, `brm_st_c`, `brm_ex_a`, `brm_ex_b`. | High | `get_string`, `xrefs_to`, and `search_text` in raw range. | Target evidence and resource support | incorporate | applied: target evidence records addresses/calls; resource support remains synchronized and now references direct UID0004D1 coverage |
| D1-011 | `LoadPcxImage` remains source-facing dependency name, but current IDA session calls it `sub_4A17B0`. | Medium-high | `lookup_funcs 0x004a17b0` returns `sub_4A17B0`; current docs/generated use `LoadPcxImage`. | Target dependency wording; StartupWindow support notes; ImageLoaders support docs out of broadened scope | incorporate | applied: target/file/class/aggregate wording records the caveat; ImageLoaders/LoadPcxImage docs intentionally not edited because callback did not broaden dependency repair |
| D1-012 | The helper initializes start button rect at `183,348` and exit button rect at `474,0`, using loaded image width/height and states at `+0x30/+0x50`. | High | `insn_query` pushes `0xb7`, `0x15c`, `0x1da`, calls rect/width/height helpers, writes states; conversions verified with `int_convert`. | Target behavior evidence | incorporate | applied: target evidence records offsets/constants and confidence caps |
| D1-013 | No direct xrefs or pointer patterns prove a binary caller to the raw start/end. | High | `xrefs_to`, `find code_ref/data_ref/immediate`, and `find_bytes` VA/RVA checks all zero. | Target negative evidence/confidence cap | incorporate | applied: target negative evidence records xref/ref/pointer-pattern negatives |
| D1-014 | Parent [UID:0001IO] and tracker [UID:0000VI] remain non-emitting split/tracker pages and should not absorb or duplicate this helper. | High | Current support docs and B008/B010 reports already document exact child route. | Aggregate/tracker support docs and Final Recommendation | already-present | applied: aggregate/tracker rows now explicitly preserve UID0004D1 as direct child while parent/tracker metadata remains non-emitting |
| D1-015 | Reject ImageLoaders, Browser, Win32/API, public method, and WndProc-inline ownership alternatives. | High | Dependency direction: StartupWindow object fields and notice UI state are local; ImageLoaders owns PCX loader only. | Target negative evidence and Ranked Ownership Analysis | incorporate | applied: target rejected-alternatives paragraph records all accepted ownership/no-code rejections |
| D1-016 | Support docs should be inspected during callback but edited only if stale about UID0004D1 score/evidence/direct-report coverage. | Medium-high | Support docs listed exact child but lacked this UID-specific direct report and one resource open question was stale. | `by-file`, `by-class`, aggregate by-memory, by-item tracker, by-resource support pages | incorporate | applied: all five stale support docs synced and validated; dependency docs inspected by report and excluded from edit absent explicit broadened repair |
| D1-017 | No manual generated/coverage/tracker edits were recommended or performed; generated state is refreshed by validator tools. | High | Workflow forbids manual generated edits; validator handles generated refresh. | Exact Manual Supervisor-Owned Coverage Or Tracker Text; Validator Results | not-applicable | excluded-with-reason: B004 made no manual generated/coverage/tracker/lifecycle edits; validators refreshed generated `StartupWindow.cpp` and project stats as tool side effects |

## Positive Evidence Summary
- Direct facts supporting the chosen recommendation: live MCP proves a complete raw helper at `0x005818d0-0x005819cc`, exact PCX strings/calls, asset-loaded guard, StartupWindow receiver state, button rect/state initialization, unique range signature, padding boundaries, and generated source emission through `StartupWindow.cpp`.
- Corroborating documentation/generated-report evidence: target page already has formal C++; `by-file/StartupWindow.md`, `by-class/StartupWindow.md`, [UID:0001IO], [UID:0000VI], and B008/B010 all route this exact child through StartupWindow; generated `auto-generated/NexusTK/app/StartupWindow.cpp` currently emits UID0004D1.
- Strongest inference chain: raw body mutates `StartupWindow` notice fields and uses PCX loader as a dependency, while the parent WndProc performs the same first-paint PCX load behavior. This supports a private StartupWindow file-local helper even without a direct binary ref to the raw start.

## IDA MCP Facts
- Function/range facts: active session `13191102`; `0x005818d0` and `0x005819cc` are not IDA functions; raw disasm starts at `0x005818d0` in `.text`; `decompile 0x005818d0` fails; `insn_query` finds an 80-instruction raw body through `retn 4` at `0x005819c9`; `0x005819cc-0x005819d0` is padding.
- Data/table/padding facts: `get_bytes` confirms thirteen `0xcc` bytes before the body and four `0xcc` bytes after; six PCX strings are at `0x0062d530`, `0x0062d540`, `0x0062d550`, `0x0062d560`, `0x0062d570`, and `0x0062d580`.
- Xref facts: zero xrefs to `0x005818d0` and `0x005819cc`; twelve xrefs to `0x004a17b0`, with six modeled WndProc calls and six raw target calls; each PCX string has one WndProc and one raw target ref.
- Vtable/global/type facts: no vtable or global owner evidence is needed for this file-local helper; current names include StartupWindow constructor/run/vtable/RTTI but no `StartupNoticeLoadAssets` IDA name. Current type query did not provide a reliable `StartupNoticeButton` UDT proof for this target.
- Negative IDA facts: no function object, no raw-start decompile, no direct raw-start/end refs, no code/data/immediate ref hits to start/end, no VA/RVA pointer-byte hits to start/end, and no safe IDA DB rename/type/comment operation requested.

## Function / Child Inventory
| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x005818c3-0x005818d0` | ignored padding | Invalidate-one-to-assets alignment | false | [UID:0001IO] context | not scored | Thirteen `0xcc` bytes, not part of target |
| `0x005818d0-0x005819cc` | [UID:0004D1] `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md` | Raw StartupWindow notice PCX asset loader and button bounds/state initializer | true | [UID:0000O5] `StartupWindow` | current implemented `88/91` | Source-bearing exact child; formal C++ present and callback-applied |
| `0x005819cc-0x005819d0` | ignored padding | Assets-to-create-window alignment | false | [UID:0001IO] context | not scored | Four `0xcc` bytes |
| `0x005819d0-0x00581b75` | [UID:0004D2] `StartupNoticeCreateWindowRaw` | Adjacent raw create-window/setup helper | true | [UID:0000O5] `StartupWindow` | current sibling context `88/90` | Separate sibling; not merged with UID0004D1 |
| `0x004a17b0-0x004a18a8` | [UID:000314] `LoadPcxImage` | PCX path-loader dependency | true | [UID:0000K3] `ImageLoaders` | `88/90` | Dependency only; current IDA session name `sub_4A17B0` |
| `0x005807d0-0x0058206e` | [UID:0001IO] StartupWindow aggregate | Non-emitting split/index page | false | [UID:0000O5] | `88/90` | Already-present parent context |
| `0x005815b0-0x00581b7f` | [UID:0000VI] raw helper tracker | Non-emitting tracker for raw notice helper family | false | [UID:0001IO] | `88/90` | Already-present tracker context |

## Direct Xref / Caller Inventory
| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x005818d0` | `xrefs_to`: zero | No direct binary caller to raw helper start; confidence cap |
| `0x005819cc` | `xrefs_to`: zero | No direct ref to target end/padding boundary |
| `0x004a17b0` | calls from raw UID0004D1 at `0x005818f1`, `0x00581901`, `0x00581911`, `0x00581921`, `0x0058196d`, `0x00581984` | Target invokes PCX loader six times |
| `0x004a17b0` | calls from WndProc at `0x00581227`, `0x00581237`, `0x00581246`, `0x00581256`, `0x005812a3`, `0x005812ba` | Modeled WndProc contains same first-paint asset-load behavior |
| `0x0062d530` | refs from `0x0058121d` and raw `0x005818e7` | `brm_main.pcx` in WndProc and target |
| `0x0062d540` | refs from `0x0058122e` and raw `0x005818f8` | `brm_st_a.pcx` in WndProc and target |
| `0x0062d550` | refs from `0x0058123e` and raw `0x00581908` | `brm_st_b.pcx` in WndProc and target |
| `0x0062d560` | refs from `0x0058124d` and raw `0x00581918` | `brm_st_c.pcx` in WndProc and target |
| `0x0062d570` | refs from `0x00581293` and raw `0x0058195d` | `brm_ex_a.pcx` in WndProc and target |
| `0x0062d580` | refs from `0x005812aa` and raw `0x00581974` | `brm_ex_b.pcx` in WndProc and target |

## Documentation Evidence And IDA Status
- Existing docs that support the conclusion after callback: target names the helper and emits formal C++; `by-file/StartupWindow.md` lists UID0004D1 as the direct `88/91` child; `by-class/StartupWindow.md` lists the asset helper in Method Notes; [UID:0001IO] includes the exact range, padding, and direct child score; [UID:0000VI] maps this raw helper to the exact child; resource docs list the six PCX assets and historicalize stale promotion wording; ImageLoaders docs keep PCX decoding in [UID:0000K3].
- Existing docs that remain as caveats: dependency docs use source-facing `LoadPcxImage`, while current IDA session `13191102` names the loader `sub_4A17B0`; this is documented in target/StartupWindow support and does not require dependency-doc repair without supervisor-broadened scope.
- Generated/coverage report state after callback: generated `auto-generated/NexusTK/app/StartupWindow.cpp` was observed refreshed at validator command `000000006422`, timestamp `2026-07-04T18:25:08-04:00`, and emits UID0004D1 with `Completion:88 | Confidence:91` plus the preserved helper body. No manual generated or coverage edit was made.

## Ranked Ownership Analysis

### 1. [UID:0000O5] StartupWindow / `NexusTK/app/StartupWindow.cpp`
- Evidence for: body uses `this`/`ecx` as a StartupWindow receiver, checks and sets `this+0x04`, writes notice image/rect/state fields, matches StartupWindow WndProc first-paint PCX behavior, appears in generated `StartupWindow.cpp`, and existing B008 split routes the exact child through [UID:0000O5].
- Evidence against: no direct xrefs to raw start; no IDA function object or decompile at `0x005818d0`; exact helper/member spellings are inferred.
- Decision: accepted direct owner/emitter. The negative evidence caps confidence but does not block source-bearing C++.

### 2. [UID:0000K3] ImageLoaders / `NexusTK/render/ImageLoaders.cpp`
- Evidence for: target calls the PCX loader dependency six times; the six calls currently resolve to IDA name `sub_4A17B0`, documented source-facing `LoadPcxImage`.
- Evidence against: target owns startup notice asset gating and button layout writes over StartupWindow fields. ImageLoaders owns PCX decoding/path loading only and has no ownership of StartupWindow UI state.
- Decision: rejected as target owner. Keep as dependency/source-facing callee only.

### 3. [UID:0001IO] aggregate and [UID:0000VI] tracker
- Evidence for: both pages physically cover or track the raw helper family.
- Evidence against: both are already non-emitting split/tracker pages after B008; exact child pages carry source-bearing bodies to avoid duplicate broad aggregate emission.
- Decision: rejected as emitters. They should remain support context only.

### 4. Browser / Win32 / WndProc-inline alternatives
- Evidence for: WndProc hosts paint behavior and Browser is part of StartupWindow notice flow.
- Evidence against: UID0004D1 does not own Browser behavior, Win32 API implementations, or a public WndProc body. The helper is a private StartupWindow notice asset/layout body.
- Decision: rejected.

### Proposed new file/grouping, if applicable
- Proposed owner/name/path: not applicable. Keep existing [UID:0000O5] `NexusTK/app/StartupWindow.cpp`.
- Likely full contents: existing StartupWindow class, update-check, WndProc, private notice helpers, globals/pointer declarations, and exact children already listed in StartupWindow docs.
- Candidate related items that belong: exact StartupWindow notice helper children [UID:0004CU]-[UID:0004D2], draw/string helpers already routed through StartupWindow as documented.
- Candidate related items rejected: ImageLoaders PCX internals, Browser hosting internals, MD5/minimap persistence, Win32 API code, raw non-emitting aggregate/tracker duplication.
- Standalone, narrow, or broad source-file inference: narrow file-local helper inside existing `StartupWindow.cpp`.

## Source Placement
- Recommended source file/class/global/module placement: `NexusTK/app/StartupWindow.cpp`, file-local static helper `StartupNoticeLoadAssets(StartupWindow *startupWindow, HDC dc)`.
- Why this placement fits source-tree and subsystem context: the helper is startup notice UI orchestration, not image decoding. It mutates StartupWindow fields, uses notice button resources, and is part of the pre-game startup/update notice path.
- Rejected placements and why: ImageLoaders owns only `LoadPcxImage`; Browser owns embedded browser behavior; Win32 owns APIs only; [UID:0001IO] and [UID:0000VI] are non-emitting containers; a public class method route lacks declaration/direct-call proof.
- Remaining placement uncertainty: exact original helper name and whether original source factored this raw duplicate as a helper or inline clone are not symbol-proven. Existing generated/source docs already chose helper extraction, and current evidence supports preserving it.

## Range / Split / Padding / Reclassification Analysis
- Exact range/boundary facts: target is `0x005818d0-0x005819cc`, length `0xfc` / 252 bytes (Verified with int_convert.py), ending with `retn 4` at `0x005819c9`.
- Children/subranges to create, repair, merge, leave ignored, or leave no-owner: no new child creation. Leave `0x005818c3-0x005818d0` and `0x005819cc-0x005819d0` as ignored/alignment padding. Keep sibling `0x005819d0-0x00581b75` separate.
- Padding/table/data/code distinctions: target is executable raw `.text` code; adjacent pre/post ranges are `0xcc` alignment. No table/data reclassification is supported.
- Parent/container impact: [UID:0001IO] remains a non-emitting split/index and [UID:0000VI] remains a non-emitting tracker. Do not merge UID0004D1 back into either.

## Negative Evidence Summary
- No IDA function object at `0x005818d0`; `lookup_funcs` reports `Not a function`.
- No Hex-Rays decompile at `0x005818d0`; decompile returned `Decompilation failed at 0x5818d0`.
- No direct xrefs to `0x005818d0` or `0x005819cc`.
- No code-ref, data-ref, or immediate hits to `0x005818d0` or `0x005819cc`.
- No VA/RVA pointer-byte hits for start/end patterns `D0 18 58 00`, `D0 18 18 00`, `CC 19 58 00`, or `CC 19 18 00`.
- No IDA name `StartupNoticeLoadAssets` exists in session `13191102`.
- No evidence supports moving PCX loader ownership to StartupWindow; only the caller/orchestration belongs here.
- No evidence supports public API placement; no symbol, declaration, vtable slot, external caller, or direct call route proves a public method.
- No evidence supports treating the helper as padding, switch table, import glue, compiler thunk, scalar deleting wrapper, or handwritten Win32/API code.

## IDA Rename / Type / Comment Recommendations
- Proposed source-facing names/types/comments: keep documentation/source-facing names `StartupNoticeLoadAssets`, `StartupWindow`, `StartupNoticeButton`, `m_assetsLoaded`, `m_backgroundImage`, `m_startNormalImage`, `m_startHoverImage`, `m_startPressedImage`, `m_exitNormalImage`, `m_exitPressedImage`, `m_buttons`, `rect`, `state`, and `LoadPcxImage` as descriptive source reconstruction names.
- Evidence for each proposed name/type/comment: PCX strings and button rect/state writes prove "LoadAssets"; receiver and support docs prove StartupWindow placement; sibling helper names establish `StartupNotice*` family; WndProc and generated code use `m_buttons`/state source shape; ImageLoaders docs and generated source use `LoadPcxImage` for the dependency.
- Items intentionally left unchanged and why: no IDA DB rename for `0x005818d0` because the target has no IDA function object; no UDT/type application for `StartupWindow` or `StartupNoticeButton` because current MCP did not prove exact UDT layout; no rename of `sub_4A17B0` in this report because ImageLoaders ownership/IDA DB rename is outside this target callback and current docs already provide the source-facing name.
- Whether IDA DB edits are safe, unsafe, or not requested: not requested and not performed. A future supervisor-authorized IDA pass could define/name the raw function and align current IDA loader naming, but UID0004D1 documentation was improved without DB mutation.

## First-Draft C++ Recommendation
- Eligible for draft C++: yes. The target is source-bearing and already has formal C++.
- Recommended code: preserve this exact formal `RECONSTRUCTION_CPP CODE` header/block insertion text after callback:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void StartupNoticeLoadAssets(StartupWindow *startupWindow, HDC dc)
{
    if (startupWindow->m_assetsLoaded) {
        return;
    }

    startupWindow->m_assetsLoaded = true;
    startupWindow->m_backgroundImage = LoadPcxImage(dc, "brm_main.pcx", -1);
    startupWindow->m_startNormalImage = LoadPcxImage(dc, "brm_st_a.pcx", -1);
    startupWindow->m_startHoverImage = LoadPcxImage(dc, "brm_st_b.pcx", -1);
    startupWindow->m_startPressedImage = LoadPcxImage(dc, "brm_st_c.pcx", -1);
    startupWindow->m_exitNormalImage = LoadPcxImage(dc, "brm_ex_a.pcx", -1);
    startupWindow->m_exitPressedImage = LoadPcxImage(dc, "brm_ex_b.pcx", -1);

    startupWindow->m_buttons[0].images[0] = startupWindow->m_startNormalImage;
    startupWindow->m_buttons[0].images[1] = startupWindow->m_startPressedImage;
    startupWindow->m_buttons[0].images[2] = startupWindow->m_startHoverImage;
    SetRect(&startupWindow->m_buttons[0].rect, 183, 348,
            183 + startupWindow->m_startNormalImage->GetWidth(),
            348 + startupWindow->m_startNormalImage->GetHeight());
    startupWindow->m_buttons[0].state = 0;

    startupWindow->m_buttons[1].images[0] = startupWindow->m_exitNormalImage;
    startupWindow->m_buttons[1].images[1] = startupWindow->m_exitPressedImage;
    startupWindow->m_buttons[1].images[2] = startupWindow->m_exitNormalImage;
    SetRect(&startupWindow->m_buttons[1].rect, 474, 0,
            474 + startupWindow->m_exitNormalImage->GetWidth(),
            startupWindow->m_exitNormalImage->GetHeight());
    startupWindow->m_buttons[1].state = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Third-party import directive, when applicable: not applicable.
- Reason it preserves exact original behavior: current MCP raw disassembly proves the guard, asset-loaded set, six PCX loads, `SetRect`-style rect initialization, width/height-derived right/bottom, and state resets. The formal C++ expresses those effects in the accepted StartupWindow source shape.
- Reason it matches the most plausible original precompiled mid-2000s developer source shape: a private static helper for a startup notice window is more plausible than exposing a public method or leaving decompiler offsets/raw names in source.
- Inferred source-facing names/types/fields used instead of IDA/decompiler labels: `StartupNoticeLoadAssets`, `StartupWindow`, `StartupNoticeButton`, `m_assetsLoaded`, image fields, `m_buttons`, `LoadPcxImage`, `GetWidth`, and `GetHeight`.
- Naming/coding style convention used and evidence for consistency: current generated StartupWindow helper family uses `StartupNotice*` static helpers and `m_` member names; sibling helper reports preserve that convention.
- Reason code should remain blank, if applicable: not applicable.
- Exact no-code proof, if not eligible: not applicable.

## Final Recommendation
- Exact changes applied: target UID0004D1 is set to `COMPLETION:88`, `CONFIDENCE:91`; owner/reconstructable/emitter/optional/Nested metadata are preserved; formal C++ is preserved; target evidence and negative evidence were expanded at report-level detail.
- Exact parent assignments applied/reaffirmed: none. [UID:0000O5] remains canonical owner/emitter. [UID:0001IO] and [UID:0000VI] remain non-emitting support pages and now explicitly record the direct UID0004D1 `88/91` child coverage.
- Exact items left no-owner/non-emitting and why: no UID0004D1 item is left no-owner/non-emitting. Parent [UID:0001IO] and tracker [UID:0000VI] stay non-emitting to avoid duplicate broad source bodies.
- Exact future work outside this assignment scope: a future supervisor-authorized IDA DB pass may define/name raw helper `0x005818d0` and reconcile current `sub_4A17B0` IDA naming with accepted `LoadPcxImage`; a future layout/type report may prove exact `StartupNoticeButton` field order.

## Recommended Target Doc Changes
- Target path: `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md`.
- Callback application state: applied to target and validated by command `000000006403`.
- Exact report facts incorporated:
  - MCP session `13191102`, active IDB path, server health ok, Hex-Rays and strings cache ready.
  - `lookup_funcs` no function at `0x005818d0`, `0x005819cc`, or `0x005819d0`.
  - `decompile 0x005818d0` failure.
  - Exact range `0x005818d0-0x005819cc`, size `0xfc` / 252 bytes (Verified with int_convert.py), `ret 4` at `0x005819c9`.
  - Thirteen `0xcc` prebytes and four `0xcc` postbytes.
  - Unique range signature.
  - Six PCX strings/addresses and six raw loader calls.
  - WndProc has the same six modeled PCX loader calls.
  - No start/end xrefs, code/data/immediate refs, or VA/RVA pointer-byte hits.
  - `LoadPcxImage` source-facing dependency name versus current IDA `sub_4A17B0` caveat.
  - Field/layout/name confidence caps.
- Metadata/score/owner/emitter/reconstructable/C++ changes applied: set `COMPLETION:88`, `CONFIDENCE:91`; kept `CANONICAL_OWNER:0000O5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000O5`, blank optional position, `Nested:0`; kept formal C++ unchanged.
- Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: no IDA function/decompile/direct route; no pointer pattern; not padding/no-code; not ImageLoaders owner; not Browser/Win32 owner; no broad aggregate/tracker emission; no IDA DB rename/type change requested.

## Recommended Support Doc Changes
- `by-file/StartupWindow.md`: applied. The notice asset loader helper row and changes log now state UID0004D1 direct B004 `88/91` coverage, MCP session `13191102`, preserved formal helper C++, no function/decompile, exact range/padding/calls, WndProc duplicate context, no route, and confidence caps. Validated by command `000000006407`.
- `by-class/StartupWindow.md`: applied. The method note and changes log now state UID0004D1 direct B004 `88/91` coverage and the same range/session/no-route/source-emitter facts. Validated by command `000000006408`.
- `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md`: applied. The UID0004D1 covered row and changes log now record direct child coverage at `88/91` while preserving aggregate non-emitter metadata. Validated by command `000000006409`.
- `by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md`: applied. The UID0004D1 tracker row and score rationale now reflect the accepted direct child coverage and remove stale "not promoted" wording while preserving non-emitting tracker state. Validated by command `000000006410`.
- `by-resource/startup-update-notice-resources.md`: applied. The stale raw-helper-promotion open questions were historicalized; the page now records that UID0004D1 carries direct `88/91` source-quality evidence while resource-level original field/art-state naming and adjacent unproven PCX assets remain caveats. Validated by commands `000000006411` and `000000006423`.
- `by-file/ImageLoaders.md` and `by-memory/0x004a17b0-0x004a18a8.LoadPcxImage.md`: excluded-with-reason. Dependency docs preserve ImageLoaders ownership; supervisor did not broaden this callback into dependency IDA-name drift repair, and the target/support StartupWindow docs now carry the `LoadPcxImage` versus `sub_4A17B0` caveat.

## Score And Metadata Recommendation
- Historical pre-callback score/metadata: `COMPLETION:86`, `CONFIDENCE:88`, owner `0000O5`, reconstructable true, emitter `0000O5`, blank optional position, `Nested:0`, formal C++ present.
- Current implemented score/metadata: `COMPLETION:88`, `CONFIDENCE:91`, same owner/reconstructable/emitter/optional/Nested fields, same formal C++. Validator command `000000006403` reported the target completion/confidence updates.
- Score rationale and reason not higher/lower:
  - Completion rises to 88 because the report closes the direct UID-specific evidence gap, documents current MCP session facts, exact bytes/range/padding, raw disassembly, strings/xrefs, generated output, support-doc state, and rejected alternatives.
  - Completion stays below 90 because exact source field/type spelling remains inferred even after target/support synchronization.
  - Confidence rises to 91 because current MCP strongly proves body behavior, range, PCX resources, source route, generated output, and negative reachability evidence.
  - Confidence stays below 92/95 because there is no IDA function, no decompile, no direct start ref, no pointer-pattern route, no current IDA source-facing function name at `0x005818d0`, and no original-symbol/type proof for field layout.
- Score-improvement attempt:
  - Raw-function blocker checked with `lookup_funcs`, `decompile`, `disasm`, and `insn_query`: still no function/decompile, but raw body is proven executable and source-shaped.
  - Reachability blocker checked with `xrefs_to`, `find` code/data/immediate refs, and `find_bytes` VA/RVA patterns: no direct route found, so confidence cap remains.
  - C++ blocker checked against current disassembly, generated output, WndProc decompile, and support docs: existing formal code remains source-shaped and should be preserved, with layout/name caps.
  - Owner blocker checked against StartupWindow, ImageLoaders, Browser, aggregate/tracker, and resource docs: StartupWindow remains best owner/emitter.
- Metadata fields changed or left unchanged: changed completion/confidence only; left all other target metadata and formal code unchanged.

## Open Questions With Attempted Resolution
- Is `StartupNoticeLoadAssets` the original helper name? Not proven. Existing docs/generated output and sibling naming make it the best source-facing name; current IDA has no function/name at the raw start.
- Is the raw helper directly called anywhere? Checked and unresolved against direct proof: `xrefs_to`, code/data/immediate search, and VA/RVA pointer-byte search are all zero. This is a confidence cap, not a no-code proof, because bytes and behavior are live source-shaped code.
- Does `LoadPcxImage` still exist as an IDA name? Current session says no: `lookup_funcs 0x004a17b0` returns `sub_4A17B0`. Source-facing `LoadPcxImage` remains accepted in current docs/generated source, so this target can preserve it without requesting an IDA DB change.
- Are exact field names and `StartupNoticeButton` layout proven? Not fully. Current raw offsets and generated code support the names, but no UDT/original-source proof exists. Formal C++ should preserve the accepted source shape while target evidence records the offset-level facts and confidence cap.
- Should the score exceed `88/91`? No. No function/decompile/direct route and inferred layout/name evidence prevent a high-final score, even though the helper is source-bearing and implementation-ready.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text
- File/placement: not applicable.
- Exact replacement/insert/delete text: none recommended.
- Reason B agent must not apply it directly: tracker and generated coverage reports are validator/supervisor-owned. The callback used scoped validators instead; generated `StartupWindow.cpp` was observed refreshed automatically to command `000000006422` and no manual generated/coverage text is needed from B004.

## Follow-Up Actions
- Supervisor actions: perform Gate 2 verification and, if accepted, run supervisor-owned report execution/archive lifecycle. B004 stopped before `execute_report`.
- A-agent actions: none.
- B004 future callback actions: none pending for accepted UID0004D1 scope. Remaining possible future work is outside this callback: IDA DB function/name/type pass for the raw helper or dependency loader naming, and a class-layout report for exact original field names.

## Confidence
- Recommendation confidence: high.
- Score confidence: high for `88/91`.
- Remaining uncertainty: exact original helper/function name, original class/member/type layout, and whether a future IDA DB function-definition pass will expose a direct source route or preserve the raw no-ref status.

## Validator Results
- Historical report-only state: no validators were run during the report-only pass because no by-* docs were edited.
- Scoped callback validators from `E:\NTK\GhidraBridge\source-3\project-documentation`:
  - `python .\tools\validator.py --mode file --file by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md --apply --queue-timeout 240` -> command_id `000000006403`, command_timestamp `2026-07-04T18:14:17-04:00`, exit `0`, `ok: 1`, `completion_update:1`, `confidence_update:1`, `uid_link_update:1`, `uid_link_insert:2`, `reference_index_add:2`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-file/StartupWindow.md --apply --queue-timeout 240` -> command_id `000000006407`, command_timestamp `2026-07-04T18:17:44-04:00`, exit `0`, `ok: 1`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-class/StartupWindow.md --apply --queue-timeout 240` -> command_id `000000006408`, command_timestamp `2026-07-04T18:17:46-04:00`, exit `0`, `ok: 1`, `reference_index_add:1`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md --apply --queue-timeout 240` -> command_id `000000006409`, command_timestamp `2026-07-04T18:17:48-04:00`, exit `0`, `ok: 1`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md --apply --queue-timeout 240` -> command_id `000000006410`, command_timestamp `2026-07-04T18:17:49-04:00`, exit `0`, `ok: 1`, `stats_row_update:1`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-resource/startup-update-notice-resources.md --apply --queue-timeout 240` -> command_id `000000006411`, command_timestamp `2026-07-04T18:17:58-04:00`, exit `0`, `ok: 1`, `reference_index_add:1`, `stats_row_update:1`, `projected_stats_update:1`, generated_refresh `deferred`.
  - `python .\tools\validator.py --mode file --file by-resource/startup-update-notice-resources.md --apply --queue-timeout 240` -> command_id `000000006423`, command_timestamp `2026-07-04T18:25:12-04:00`, exit `0`, `ok: 1`, `stats_row_update:1`, `projected_stats_update:1`, generated_refresh `deferred`.
- Warnings/errors: no target-specific validator warnings or errors were reported.
- Generated freshness: `auto-generated/NexusTK/app/StartupWindow.cpp` was observed at validator-command-id `000000006422` / `2026-07-04T18:25:08-04:00`, which is newer than target validator `000000006403` and shows `// UID:0004D1 ... Completion:88 | Confidence:91` plus the preserved `StartupNoticeLoadAssets` helper body.

## Changed Files
- Modified by B004 callback:
  - `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md`
  - `by-file/StartupWindow.md`
  - `by-class/StartupWindow.md`
  - `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md`
  - `by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md`
  - `by-resource/startup-update-notice-resources.md`
  - `tools/leaser/Agents/Agent-B004/research/0004D1-StartupNoticeLoadAssets-source-quality.md`
- Validator/tool side effects observed: `auto-generated/NexusTK/app/StartupWindow.cpp` refreshed automatically; `project-level/-auto-completion-stats.md` projected stats updated by scoped validators. B004 did not hand-edit generated or project-level files.
- Renamed: none.
- Report execution: not run by B004.
- Leases: target lease used/released for `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md`; support leases used/released for `by-file/StartupWindow.md`, `by-class/StartupWindow.md`, `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md`, `by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md`, and `by-resource/startup-update-notice-resources.md`. Follow-up lease check showed no active B004 leases; later active leases belong to other agents.

## Implementation Tracking Checklist

Callback implementation status:
- [x] Supervisor validation requirement satisfied: Gate 1 passed for SHA256 `448ED2D082E22DCD390E212B8463274A1C0BCA4FC761F65A3F5AB849D420FE74`; this callback applied the accepted details and stops at `READY_FOR_SUPERVISOR_EXECUTE`.
- [x] Target metadata applied: `by-memory/0x005818d0-0x005819cc.StartupNoticeLoadAssets.md` now has `COMPLETION:88`, `CONFIDENCE:91`; validator `000000006403` confirmed completion/confidence updates.
- [x] Owner/emitter/reconstructable fields preserved: target still has `CANONICAL_OWNER:0000O5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000O5`, blank optional position, and `Nested:0`.
- [x] Formal C++ preserved: target `RECONSTRUCTION_CPP CODE` block was not changed; generated `StartupWindow.cpp` still emits `StartupNoticeLoadAssets(StartupWindow *startupWindow, HDC dc)`.
- [x] Target evidence incorporated at report-level detail: MCP session `13191102`, no-function/decompile failure, exact `0xfc` range, `ret 4`, `0xcc` padding, six PCX strings/calls, WndProc duplicate call context, no xrefs/ref/pointer route, source-facing `LoadPcxImage` versus IDA `sub_4A17B0`, field/layout caps, and rejected alternatives are in the target.
- [x] Support docs inspected and synchronized where stale: edited `by-file/StartupWindow.md`, `by-class/StartupWindow.md`, `by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md`, `by-item/StartupWindowUnmodeledNoticeHelpers_5815b0_581b7f.md`, and `by-resource/startup-update-notice-resources.md`; support metadata stayed unchanged.
- [x] Dependency docs excluded with reason: `by-file/ImageLoaders.md` and `by-memory/0x004a17b0-0x004a18a8.LoadPcxImage.md` were not edited because dependency ownership is already correct and supervisor did not broaden scope to IDA-name drift repair.
- [x] Source placement, range/split/padding/reclassification, and owner alternatives preserved: `StartupWindow.cpp` placement retained, parent/tracker remain non-emitting, no split/rename/new child was needed, and rejected ImageLoaders/Browser/Win32/public/WndProc-inline/no-code alternatives are documented.
- [x] Historical/stale assumptions handled: target stale thin-evidence state replaced; tracker stale "not promoted" wording corrected; resource stale raw-helper-promotion open question historicalized; Wave2/Wave3 material remains ignored as stale lead material.
- [x] Open questions documented as evidence-backed unresolved: exact original helper name, exact class/button field layout, current IDA DB naming drift for `sub_4A17B0`, and future function-definition/name pass remain confidence caps only.
- [x] Scoped validators run for every changed by-* file: target `000000006403`; support validators `000000006407`, `000000006408`, `000000006409`, `000000006410`, `000000006411`, and resource revalidation `000000006423`, all exit `0` with `ok:1`.
- [x] Generated freshness checked: `auto-generated/NexusTK/app/StartupWindow.cpp` was observed at validator-command-id `000000006422`, timestamp `2026-07-04T18:25:08-04:00`, and shows UID0004D1 as `Completion:88 | Confidence:91` with the preserved helper body.
- [x] Claim And Incorporation Ledger updated: rows D1-001 through D1-017 now have callback verification states `applied`, `already-present`, or `excluded-with-reason`; no row remains `proposed` or `blocked`.
- [x] Leases released: follow-up lease report shows no active B004 leases; later active leases are owned by other agents.
- [x] Manual generated/coverage/lifecycle exclusions preserved: no generated/manual coverage files were hand-edited; no validator state, supervisor ledger, registry, archive, manual report move, or `execute_report` command was run by B004.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B004","command_id":"000000006444","destination_path":"executed-b-agent-research/B004/0004D1-StartupNoticeLoadAssets-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B004/research/0004D1-StartupNoticeLoadAssets-source-quality.md","timestamp":"2026-07-04T18:53:02-04:00","uid":"0004D1"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
