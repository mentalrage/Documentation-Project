** TARGET-REPORT-UID:0003TH **
** AUTHOR-AGENT-ID:B004 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID0003TH MapPane Create HitBarObjectPane Ownership / Source-Quality Research


## Finalized Report / Current Recommendation

- Current implementation truth: UID0003TH is one live, source-authored private `MapPane` method and has been UID-preservingly renamed from `MapPaneCreateHitBarObjectPaneEventOverlay` to `MapPaneCreateHitBarObjectPane`.
- Final disposition applied: exactly one `void MapPane::CreateHitBarObjectPane(LivingObjectPane *objectPane, unsigned short currentValue, unsigned short maximumValue)` definition emits through owner/emitter UID00007Q in `NexusTK/map/MapPane.cpp`.
- Completed action: the final target path is present uniquely, score is `92/94`, Item Summary and managed C++ are populated, the complete current MapPane declaration was preserved with only the accepted private declaration correction, and all bounded parent/caller/class/file/support destinations are synchronized.
- Exact behavior: null target returns; otherwise read its `MapPoint`, choose HitBar style `1` only for `ObjectStatusBlob::HumanObject` and `0` for every other status kind, preserve two separate ordinary `new` expressions, attach and register the pane, schedule timer event `0` for `500` ms, update position, obtain bounds, and invalidate those bounds.
- Compiler boundary: pool allocation, EH states, security-cookie mechanics, complete-object-to-`TimerHandler` `+0xa4` adjustment, vtable-slot syntax, and raw field offsets are compiler/decompiler evidence only and must not enter emitted human C++.
- Confidence: very strong for range, ABI, callers, owner/emitter, types, branch semantics, calls, timer lifetime, and source body; high but inferential for the exact original lexical method name.
- Callback state: all accepted ordinary edits and scoped validators are complete, the authorized waited generated refresh passed, and every B004 lease is released. B004 manually edited no manual coverage, generated, tracker, audit, supervisor, validator-state, lifecycle, archive, or IDA file and ran no report lifecycle command; generated/tracker/auto-coverage changes were validator-owned side effects of command `13515`.
- Post-coverage state: supervisor Gate 1/Gate 2 passed for pre-coverage artifact SHA256 `49B24774E93084BB12DF30039F616EC3A433BFC54BD87206758EBCC224FB483F`, then supervisor commands `13517`, `13518`, and `13519` applied the exact accepted by-memory, by-class, and by-file manual handoff with exit `0` / `ok:1`. Current rows match the preserved handoff text exactly; report execution/count/move/archive state remains external and is not asserted here.

## Supporting Research

- Authoritative workspace: `E:\NTK\GhidraBridge\source-3\project-documentation`.
- The initial live evidence pass used streamable MCP transport session header `a5c7c188-6cbb-4cbc-8e95-7bf66513bd1f`. A final transport reinitialization then issued corrected bounded calls `306`, `307`, and `314`; they found active/adopted NexusTK database `64c11373`, worker PID `21508`, `is_analyzing:false`, health `ok`, imagebase `0x400000`, and ready auto-analysis/Hex-Rays/2067-entry string cache. Call `314` returned the target's expected first 16 bytes. Earlier malformed client calls `302`-`304`, `308`, and `310` returned parameter-schema errors without reporting a worker/session outage; `tools/list` call `312` exposed the exact `get_bytes(regions:[{addr,size}])` schema used by successful call `314`.
- Exact old-report search terms: `UID0003TH`, `0003TH`, `0x0050edd0`, `0x0050eef5`, `MapPaneCreateHitBarObjectPaneEventOverlay`, `CreateHitBarObjectPaneEventOverlay`, `CreateHitBarObjectPane`, `sub_50EDD0`, `sub_512960`, `HitBarObjectPane`, `LivingObjectPane`, `ObjectStatusBlob`, `ObjectList`, `TimerHandler`, `MapPane`, `action`, `combat`, and `overlay`.
- Search roots checked read-only: `executed-b-agent-research/**`; all current `tools/leaser/Agents/Agent-B*/research/**`; actual project `archived/**`; `tools/leaser/Agents/Older-Research/**`; and `tools/leaser/Agents/SpecialReports/**`.
- Relevant executed reports opened and classified:
  - `executed-b-agent-research/B001/0001AW-mappane-packet-source-split-continuation-batch4.md`, SHA256 `FBD3E00AF7FB29AA12B242CD7C7AD5C1CC13F65969366B74C43F7FD2DA172544`: direct historical split provenance for UID0003TH, its two calls, broad callee set, owner, and eleven-byte padding. Its blank-C++ blocker, `+0x178` generic overlay-state label, generic bounds setter, and long working name are superseded by this target-specific pass.
  - B001 continuation batch5, SHA256 `61E2AC70BB73C3F3DE97504E03A8774115DFC95422474AC587A047B136E9A065`: later parent inventory and adjacency lead; no target-specific source closure.
  - B001 continuation batch6, SHA256 `6FAE8659F592D0BE16CF3F9478E28A44E3FA1F25B3B4A4DD3327E3C81D8B4D06`, and batch7, SHA256 `F3A8BFECC02DB7B3BB2939015044E75C10F476AC4D4AFA97DA81513D2F97BF61`: direct action/combat caller-family leads; final target types and body remained deferred.
  - B001 continuation batch8, SHA256 `7F9C4CDB3572EC2C51451D450A168AE58EA658F10E8189469D705DC58C13DB55`: exact UID0003TQ split and both target callsites; its broader packet-handler blockers do not prevent reconstructing this callee.
  - `executed-b-agent-research/B001/0003Z4-LivingObjectPaneLinkedObjectSetupHelpers-source-quality.md`, SHA256 `522FD383FE3DD472AEA32CD18348A7943AA6D6650361549F8B857889A4F8D3A4`: direct typed-slot support proving `LivingObjectPane::m_hitBarObjectPane` at `+0x1dc` and the one-store setter route.
  - `executed-b-agent-research/B002/0003TC-MapPaneApplyHitBarObjectPane-source-quality.md`, SHA256 `E6A1A6FD86AB823A140BC4FB6DDE404002F38F03E251CD585EDE494D23F6F260`: direct sibling policy support for typed LivingObjectPane/HitBar fields, ordinary `new`, MapPoint, ObjectList registration, `UpdatePosition`, inherited `GetBounds`, and `InvalidateRect` order.
  - `executed-b-agent-research/B001/0003TF-MapPaneCreateEffectObjectPane-source-quality.md`, SHA256 `EE9B4615A285E38B8BD47A043755C65CA455F94E848C399708653993FADADB46`: adjacent source-ready creation helper and strongest local style/order evidence for direct TimerManager scheduling and bounds invalidation.
  - `executed-b-agent-research/B005/0002C9-ObjectListTypeDispatchInsertCategorize-source-quality.md`, SHA256 `73E903FA2560252EC17FE40AA2A58921D74A568D9E6F939F9E5A183ADDED25A7`: exact `ObjectList::AddObjectPane(ObjectPane *)` source identity.
  - `executed-b-agent-research/B009/0000HJ-AttachedObjectPane-empty-emitter-family-source-quality.md`, SHA256 `2460A17316FF5C9BE8285EAD5EA265D4A3CF48930B8CE64C7B1E8F518961FE4C`: HitBar pool/vtable/AttachedObjectPane family context; constructed-object ownership does not displace MapPane method ownership.
- The matching B005 report that was active when searched, `tools/leaser/Agents/Agent-B005/research/0003TE-MapPaneCoordinateEffectDescriptorDispatch-source-quality.md`, current read-only SHA256 `CE568958F77E685033600A7531CECBDC4F36E30B9A22F716B969E34A0BDC25C5`, records a completed source callback and a report-only shared-union rebase. It confirms the current MapPane class/generated union and does not claim UID0003TH implementation ownership.
- `archived/**`, `Older-Research/**`, and `SpecialReports/**` contained no direct UID/address/current-name report. Broader source-family hits were either the executed reports above or unrelated MapPane/HitBar uses; therefore there is no hidden target-specific report to substitute for this pass.
- Wave2/Wave3 references were treated as stale process history and were not used as evidence.

## Target

- Target UID: `0003TH`.
- Historical pre-callback target path: `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPaneEventOverlay.md`; it is now absent.
- Current UID-preserved target path: `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md`; UID0003TH is unique there.
- Source queue row: `auto-generated/-ag-research-tracker.md`, `## by-memory` / `### Not-Covered Files - Reconstructable`, current assignment-time `86/90`, zero direct reports.
- Exact range: `[0x0050edd0,0x0050eef5)`, size `0x125` / 293 bytes.
- Historical pre-callback metadata was completion `86`, confidence `90`, owner/emitter `00007Q`, reconstructable true, blank position, `Nested:0`, blank Item Summary, and blank managed C++.
- Current metadata is completion `92`, confidence `94`, owner/emitter `00007Q`, reconstructable true, blank position, `Nested:0`, the exact accepted Item Summary, and the complete accepted managed source body.
- Current target SHA256 is `8AB3FC668FDB9F48763A6CF0761053EDA9D58CCA8A4650B1CA96907284045028`, 11,659 bytes / 158 lines. Historical pre-callback SHA256 `3912FB2FCDF0CAEAE27547D05CCA6404156B84E176A5E60F98380B5FA4BA98D7` remains evidence of the accepted starting state.

## Current Target State

- UID0003TH is now exact reconstructable MapPane source at the UID-preserved final path with owner/emitter UID00007Q, exact range, blank position, and `Nested:0`.
- The current page contains the accepted source-clean body and preserves the exact two UID0003TQ calls, separate HitBar allocation/construction branches, typed `m_hitBarObjectPane` installation, ObjectList insertion, event-0/500-ms timer schedule, update/bounds/invalidation sequence, and eleven-byte successor padding evidence.
- Every former source blocker is resolved in current ordinary documentation: unsigned-short parameters; `m_objectStatus.m_statusKind`; `ObjectList::AddObjectPane`; inherited TimerHandler; `HitBarObjectPane::UpdatePosition`; inherited `GrafPort::GetBounds`; `ObjectPane::InvalidateRect`; and HumanObject style 1 versus all other statuses style 0.
- Historical generated checkpoint command `13487`, SHA256 `E11FC9F0AE5F74220823A2BBC6E28C6EC353A820A963EF17785B2C0763998554`, 102,689 bytes / 2,996 lines, contained the provisional declaration and one target Empty Emitter Marker. Authorized waited command `13515` supersedes it: current `auto-generated/NexusTK/map/MapPane.cpp` SHA256 `0E2FC02FE45981F395CE3967FC06F20CCCDB050832AF55B2A412E16D0D5D4E5A`, 103,866 bytes / 3,043 lines, contains exactly one accepted declaration, one accepted definition, and one UID0003TH marker, with zero old-name tokens and zero target Empty Emitter Markers.
- Current ordinary destination hashes are recorded under Changed Files. Historical pre-callback hashes remain valid accepted-input evidence but are not presented as current bytes.
- Supervisor B002 manual-coverage commands `13498`-`13500` externally advanced the three applicable manual snapshots after the first evidence checkpoint. Direct final reread classifies their 15 rows as unrelated ScreenCursor/ScreenPane/CursorManager/Application/LogoPane changes: by-memory UID0002PN/UID0003LL/UID0002PO and UID0001G6/UID0001G7/UID0004RR-UID0004S2; by-class UID00000D/UID00003E/UID0000CB; by-file UID0000IL/UID0000NB, with UID0000HG inspected unchanged. They do not alter the MapPane neighborhood or UID0003TH handoff substance.
- Current manual coverage hashes are recorded below. Supervisor command `13517` installed the exact UID0003TH and successor-padding rows; commands `13518` and `13519` installed the exact MapPane class/file replacements. The preserved no-loss handoff text now has direct current parity, and every manual coverage item is applied.
- Report validation, execution, count, move, archive, and manual-coverage lifecycle are external supervisor/validator-owned state. This artifact asserts completed implementation/validation facts and B004's no-lifecycle boundary without claiming external lifecycle status.

## Executive Recommendation

- Rename the source-facing helper to `MapPane::CreateHitBarObjectPane`. This is the highest-probability human name because the method creates exactly one HitBarObjectPane and its nearest source-ready sibling is `CreateEffectObjectPane`; "EventOverlay" describes call context and lifetime, not the construction operation.
- Use `LivingObjectPane *objectPane`, `unsigned short currentValue`, and `unsigned short maximumValue`. The target loads both stack values with `movzx word ptr`; the HitBar constructor receives two 16-bit fields; and the action caller's packet byte and literal `100` are promoted into these helper arguments.
- Use the accepted source predicate `objectPane->m_objectStatus.m_statusKind == ObjectStatusBlob::HumanObject`. Reject generic `+0x178`, "overlay state," bool, link-state, and nonzero-style interpretations.
- Preserve the binary's two separate `new HitBarObjectPane(...)` expressions. The two allocation sites, two constructor sites, and branch-specific EH states are stronger source-shape evidence than a single allocation with a ternary style local.
- Preserve unsafe behavior: check only `objectPane`; do not check pool/new, `m_objectList`, `g_pTimerMgr`, or the constructed pane; do not remove or delete a pre-existing HitBar pointer; and do not clamp values.
- Keep owner/emitter UID00007Q and `NexusTK/map/MapPane.cpp`; HitBarObjectPane, AttachedObjectPane, ObjectList, TimerHandler/TimerManager, GrafPort, LivingObjectPane, and ObjectStatusBlob remain dependencies/support owners.

## Supervisor Active Recheck

- Historical Gate 1 acceptance authorized the callback after the direct-target report resolved `+0x178`, source role, timer, virtual callbacks, signature, score, and formal-C++ blockers. The accepted callback is now fully applied and locally verified.
- Terminal supervisor Gate 1/Gate 2 passed for artifact `49B24774E93084BB12DF30039F616EC3A433BFC54BD87206758EBCC224FB483F`; the exact supervisor-owned coverage handoff was then applied by commands `13517`-`13519` and verified read-only in this rebase. Those are completed historical/current coordination facts, not lifecycle actions performed by B004.
- The original broad split was already performed by B001. This pass rechecked exact boundaries, source-bearing status, and adjacent padding; no further child split, merge, or range correction is required.
- The target itself is the only source-bearing function in its exact range. Successor bytes are compiler alignment, predecessor and successor functions remain separate, and every in-scope source blocker has an implementation-ready decision.

## Inference Research Guidance Check

- Direct IDA fact, current documentation, historical-report evidence, and source inference are separated throughout this report.
- Existing generated names and blank-C++ rationale were treated as provisional rather than authoritative. Current typed support docs were also rechecked against the live target before reuse.
- The recommendation favors plausible mid-2000s private-member source, ordinary `new`, enum/member names, and typed calls. It rejects raw offsets, vtable expressions, pool APIs, EH labels, and decompiler temporaries from emitted C++.
- Exact original symbols are absent. `CreateHitBarObjectPane` is therefore a high-confidence source-facing inference, not claimed symbol recovery.
- Wave2/Wave3 artifacts were ignored as stale.

## Heuristic / Inference Reanalysis And Validation

| Issue | Evidence and reanalysis | Best source decision | Rejected alternatives |
| --- | --- | --- | --- |
| Helper name | Exact behavior is create/attach/register/schedule; adjacent accepted method is `CreateEffectObjectPane`; no symbols/strings preserve spelling. | `CreateHitBarObjectPane` | Current `CreateHitBarObjectPaneEventOverlay` is useful historical description but too call-context-heavy; `ShowHitBar`, `CreateTemporaryHitBar`, and `CreateCombatOverlay` overstate presentation/context. |
| Receiver/first argument | Both callers set ECX to MapPane and push a resolved target; target calls LivingObjectPane methods/fields. | private `MapPane` member, `LivingObjectPane *objectPane` | HitBarObjectPane member, free helper, and generic `ObjectPane *` reject typed field/status evidence. |
| Value parameter types | Target entry performs two `movzx word ptr [ebp+...]`; ctor stores two unsigned shorts; `retn 0x0c`; callers push a zero-extended UInt8 value and literal 100. | `unsigned short currentValue`, `unsigned short maximumValue` | Current `unsigned int` loses target ABI/source intent; signed short lacks signed operations; unsigned char describes current callers but not target stack loads/full domain. |
| `+0x178` | LivingObjectPane layout places `ObjectStatusBlob m_objectStatus` there; blob member zero is `StatusKind m_statusKind`; accepted enum value `HumanObject=0`. | compare `m_statusKind` to `ObjectStatusBlob::HumanObject` | bool overlay state, link state, active flag, and generic dword are contradicted by current layout. |
| Style flag | HumanObject branch passes final ctor arg 1; nonzero status branch passes 0; ctor stores one byte at HitBar `+0x130`. | Human style `1`, all others `0` | Attachment flag/name is unsupported; bool hides the accepted byte-style field. |
| Allocation source shape | Two pool allocation calls, two ctor calls, and branch-specific EH state transitions. | two ordinary `new` expressions inside `if/else` | one allocation plus ternary style could be behaviorally similar but does not best explain duplicated lowering/EH sites. |
| Position | `0x005374d0` copies target row/column into `MapPoint`; ctor contract is mapY/mapX. | `MapPoint mapPosition`; pass `.row`, then `.column` | screen coordinates and column-before-row reject accepted contracts. |
| Owner slot | `0x0053ae70` is exact one-store `SetHitBarObjectPane`; field is `+0x1dc`. | `objectPane->SetHitBarObjectPane(hitBarObjectPane)` | direct raw slot store or generic linked-overlay setter. |
| Map insertion | `0x005314a0` is accepted `ObjectList::AddObjectPane(ObjectPane *)`; MapPane field is `m_objectList`. | `m_objectList->AddObjectPane(hitBarObjectPane)` | MapPane-owned ad hoc insert helper and raw list calls. |
| Timer route | Direct call to `g_pTimerMgr->ScheduleTimer`; adjusted `+0xa4` view is inherited TimerHandler; args are event 0, delay 500, 0, 0. Timer callback 0 self-deletes the complete HitBar pane. | direct manager call with `static_cast<TimerHandler *>` | wrapper `hitBarObjectPane->ScheduleTimer` is source-plausible but less exact for this direct callsite; explicit pointer adjustment is compiler lowering. |
| Virtual `+0x70` | HitBar primary vtable entry at `0x006207a8` points to `0x00538ec0`; support docs identify `UpdatePosition`. | `hitBarObjectPane->UpdatePosition()` | generic refresh/update slot. |
| Bounds helper | `0x004b8e00` copies inherited GrafPort bounds; 103 callers; accepted owner is GrafPort. | `RectBounds bounds; hitBarObjectPane->GetBounds(&bounds)` | historical BackPane owner and raw sub call. |
| Virtual `+0x20` | HitBar primary vtable entry at `0x00620758` points to `0x00537720`; accepted ObjectPane method is `InvalidateRect`. | `hitBarObjectPane->InvalidateRect(&bounds)` | historical "bounds setter" is contradicted. |
| Existing HitBar lifecycle | Binary never reads `m_hitBarObjectPane` before overwriting it and never removes/deletes an old pane here. | preserve overwrite/no-check behavior | adding reuse/removal to match UID0003TC would change behavior. |
| Return | `retn 0x0c`, no source result use, all paths are side effects/void. | `void` | pointer/bool return is unsupported residue. |
| Compiler boundary | Security cookie, pool calls, EH cleanup, vptr and subobject adjustments are lowering artifacts. | exclude all from formal C++ | handwritten allocator/EH/vptr/adjustor source. |
| Range/padding | Exact function ends `0x0050eef5`; next eleven bytes are `CC`; successor starts `0x0050ef00`. | unchanged target range plus ignored padding | extending target through padding or merging successor. |

No investigable blocker remains. The absent original lexical symbol caps confidence at 94 but does not justify a blank body or the current 86 completion.

## Evidence Standards Used

- Highest tier: live IDA MCP function/range/bytes/decompile/disassembly/xrefs/caller/callee/vtable/data reads and negative pointer searches against database `64c11373`.
- Strong corroboration: current accepted by-class/by-memory layouts and method contracts, generated source readback, manual coverage rows, exact prior executed reports, and local adjacent source patterns.
- Inference tier: original lexical method name and local variable spelling only. These are explicitly ranked and do not override machine behavior.
- Binary facts control behavior, ABI, order, unsafe checks, and compiler boundaries. Current documentation controls accepted project-wide names where live binary evidence is compatible.
- Tool limitations: no original symbols or source strings name the helper. That limits lexical confidence, not function/body confidence.

## Evidence Checked

- Live MCP checks: fresh `idb_list`; `server_health`; bounded target bytes; `lookup_funcs` for target/predecessor/successor/caller; full target bytes/hash; target and caller disassembly/decompilation; `analyze_function`; exact xrefs; call-instruction neighborhoods; direct callee analyses; HitBar primary and TimerHandler-view vtable bytes; timer callback analysis; trailing padding bytes; and target VA/RVA/raw-offset pointer-pattern negatives.
- Target machine facts: function `sub_50EDD0`, `[0x0050edd0,0x0050eef5)`, 293 bytes, 14 basic blocks, complexity 2, full-byte SHA256 `170AE5F6E8296C07A1F831432A64B47FFBA36A9BD6E903F81A73FBAB00316D6F`; first 16 bytes `55 8B EC 6A FF 68 15 2A 60 00 64 A1 00 00 00 00`; last 16 bytes `8B 4D F0 33 CD E8 40 88 0B 00 8B E5 5D C2 0C 00`.
- Caller checks: sole caller function `sub_512960` / UID0003TQ, `[0x00512960,0x00512ca4)`, 836 bytes, 43 blocks, complexity 22, dispatcher call at `0x0050837e` for opcode `0x13`, exact target calls at `0x00512b96` and `0x00512c7f`; both compare the parsed value to `0x64`, then push literal `0x64`, parsed value, and resolved LivingObjectPane before calling target.
- Callee checks: `0x005374d0` GetMapPosition; HitBar pool allocation; `0x00538d50` HitBar constructor; `0x0053ae70` typed owner-slot setter; `0x005314a0` ObjectList insertion; `0x00597910` timer manager scheduling; `0x00538ec0` UpdatePosition; `0x004b8e00` inherited GetBounds; `0x00537720` InvalidateRect.
- Timer-liveness check: HitBar TimerHandler-view callback at `0x005386a0` tests timer id 0, adjusts to the complete object, and invokes deleting-destructor behavior with flag 1. The 500 ms schedule is therefore temporary overlay self-destruction, not an arbitrary refresh delay.
- Negative IDA checks: zero absolute-VA, RVA, and raw-offset pointer hits for target start/end; no other target callers; no strings/constants naming it; no pre-existing-pane lookup/removal; no allocation failure branch; no value clamp inside target; no target-owned global/table/vtable.
- Current docs checked: target, UID0001AW parent, UID0003TQ caller, MapPane class/file, HitBarObjectPane, LivingObjectPane, ObjectStatusBlob, ObjectList, TimerHandler/TimerManager support, MapPoint, ObjectPane/GetMapPosition, GrafPort/GetBounds, setter, pool/vtable support, and `by-memory/-ignored.md`.
- Generated/manual checked: current `auto-generated/NexusTK/map/MapPane.cpp`; current by-memory, by-class, and by-file manual coverage neighborhoods; current tracker assignment row as queue context only.
- Prior-report roots and exact matching reports are itemized under Supporting Research. `archived/**`, `Older-Research/**`, and `SpecialReports/**` had no direct target report.
- Intentionally skipped: IDA renaming/retyping/comment mutation, implementation validators, generated refresh, manual coverage edits, lifecycle probes, report execution/move/archive. All are forbidden or callback-only.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C3TH-001 | UID0003TH is exact source-authored code at `[0x0050edd0,0x0050eef5)`, 293 bytes. | Certain | live lookup/analyze/full bytes | Destination 1 | incorporate | applied |
| C3TH-002 | Full target-byte SHA256 is `170AE5F6E8296C07A1F831432A64B47FFBA36A9BD6E903F81A73FBAB00316D6F`. | Certain | MCP full-byte read | Destination 1 | incorporate | applied |
| C3TH-003 | Target has 14 blocks, complexity 2, and no strings. | Very high | live analyze/decompile | Destination 1 | incorporate | applied |
| C3TH-004 | Exact owner/emitter remains UID00007Q MapPane in UID0000L3 `NexusTK/map/MapPane.cpp`. | Very high | ECX/callers/field/callee/source cluster | Destinations 1-4 | incorporate | applied |
| C3TH-005 | UID0003TH stays reconstructable true, blank position, `Nested:0`; no split/merge/range change. | Certain | current metadata/boundaries | Destination 1 | already-present | already-present |
| C3TH-006 | UID-preserving source-facing rename is `MapPaneCreateHitBarObjectPane` / `CreateHitBarObjectPane`. | High | behavior/adjacent naming/negative symbol search | Destinations 1-9 | incorporate | applied |
| C3TH-007 | Source signature returns `void`. | Very high | side-effect paths/retn/caller use | Destinations 1-2 | incorporate | applied |
| C3TH-008 | First parameter is `LivingObjectPane *objectPane`. | Very high | caller resolution/typed fields/methods | Destinations 1-2 | incorporate | applied |
| C3TH-009 | Value parameters are `unsigned short`, not current `unsigned int` or caller-width `unsigned char`. | Very high | target `movzx word`, ctor fields, stack ABI | Destinations 1-5 | incorporate | applied |
| C3TH-010 | Null `objectPane` returns without side effects. | Certain | target CFG | Destination 1 | incorporate | applied |
| C3TH-011 | Position source is `GetMapPosition(MapPoint *)`, preserving row then column. | Very high | callee body/current contracts | Destination 1 | incorporate | applied |
| C3TH-012 | `+0x178` is `objectPane->m_objectStatus.m_statusKind`. | Very high | LivingObjectPane/ObjectStatusBlob layouts | Destinations 1, 7, 8 | incorporate | applied |
| C3TH-013 | Status zero is `ObjectStatusBlob::HumanObject`. | Very high | accepted enum/current machine compare | Destinations 1, 8 | incorporate | applied |
| C3TH-014 | HumanObject branch passes HitBar style byte 1; all other statuses pass 0. | Certain | branch/call arguments/ctor field | Destinations 1, 6 | incorporate | applied |
| C3TH-015 | Source should preserve two separate ordinary `new` expressions. | High | duplicate allocation/ctor/EH states | Destination 1 | incorporate | applied |
| C3TH-016 | Pool allocation and free-unwind calls are compiler lowering, not source allocator APIs. | Very high | pool/static/EH pattern | Destination 1 | reject-invalid | excluded-with-reason |
| C3TH-017 | Constructor arguments are owner, map row, map column, current, maximum, style. | Very high | live ctor analysis/current formal | Destinations 1, 6 | incorporate | applied |
| C3TH-018 | Target stores the pane through `SetHitBarObjectPane`; field is `m_hitBarObjectPane`. | Very high | setter/store/layout | Destinations 1, 7 | incorporate | applied |
| C3TH-019 | Target does not inspect/remove/delete a pre-existing owner HitBar pointer. | Certain | negative target read/CFG | Destinations 1, 7 | incorporate | applied |
| C3TH-020 | MapPane registration is `m_objectList->AddObjectPane`. | Very high | exact callee/current ObjectList contract | Destinations 1, 3, 5 | incorporate | applied |
| C3TH-021 | No allocation/ObjectList/TimerManager null check may be added. | Certain | exact CFG/unsafe behavior | Destination 1 | incorporate | applied |
| C3TH-022 | Timer scheduling is direct `g_pTimerMgr->ScheduleTimer`. | Very high | direct call/adjacent source convention | Destination 1 | incorporate | applied |
| C3TH-023 | Timer receiver is inherited `TimerHandler *`; `+0xa4` is compiler adjustment. | Very high | Pane layout/vtable/call lowering | Destinations 1, 6 | incorporate | applied |
| C3TH-024 | Exact timer arguments are event 0, 500 ms, 0, 0. | Certain | disassembly/decompile | Destinations 1, 6 | incorporate | applied |
| C3TH-025 | HitBar timer event 0 self-deletes the complete pane after the delay. | Very high | timer vtable/callback `0x005386a0` | Destinations 1, 6 | incorporate | applied |
| C3TH-026 | Primary vslot `+0x70` is `HitBarObjectPane::UpdatePosition`. | Very high | vtable bytes/callee/current docs | Destinations 1, 6 | incorporate | applied |
| C3TH-027 | `0x004b8e00` is inherited `GrafPort::GetBounds`. | Very high | exact body/103 callers/current docs | Destination 1 | incorporate | applied |
| C3TH-028 | Primary vslot `+0x20` is `ObjectPane::InvalidateRect`. | Very high | vtable bytes/current docs | Destination 1 | incorporate | applied |
| C3TH-029 | Exact post-registration order is timer, UpdatePosition, GetBounds, InvalidateRect. | Certain | target instruction order | Destination 1 | incorporate | applied |
| C3TH-030 | Value inputs are not clamped or converted to percentages in this helper. | Certain | target CFG/arithmetic negative | Destinations 1, 5 | incorporate | applied |
| C3TH-031 | Both calls are live inside opcode-0x13 UID0003TQ at `0x00512b96`/`0x00512c7f`. | Certain | xrefs/caller disassembly | Destinations 1, 4, 5 | incorporate | applied |
| C3TH-032 | Both current callers permit values only through 100 and pass maximum 100, but helper ABI remains unsigned short. | Very high | exact call neighborhoods | Destinations 1, 5 | incorporate | applied |
| C3TH-033 | No absolute/RVA/raw pointer table reaches target; direct calls establish liveness. | Very high | negative binary scans/xrefs | Destinations 1, 4 | incorporate | applied |
| C3TH-034 | Target predecessor ends exactly at start; successor starts at `0x0050ef00`; no merge. | Certain | lookup/neighbor disassembly | Destinations 1, 4 | incorporate | applied |
| C3TH-035 | `[0x0050eef5,0x0050ef00)` is exactly eleven `CC` alignment bytes. | Certain | live bytes/current ignored row | Destination 9 | already-present | applied |
| C3TH-036 | Parent UID0001AW remains `86/90`, non-reconstructable, blank emitter/formal, `Nested:0`. | Very high | current parent split state | Destination 4 | incorporate | applied |
| C3TH-037 | Target score is `92/94` after the accepted `86/90 -> 92/94` change; missing original lexical symbol caps 94. | High | resolved blocker matrix | Destination 1 | incorporate | applied |
| C3TH-038 | MapPane class remains `93/94`, file remains `92/92`; only target declaration/prose/link changes. | Very high | current complete union | Destinations 2-3 | incorporate | applied |
| C3TH-039 | HitBarObjectPane remains `90/93`; add target-specific style/timer caller facts without formal change. | Very high | current support/body | Destination 6 | incorporate | applied |
| C3TH-040 | LivingObjectPane remains `92/93`; add status/slot consumer facts without formal change. | Very high | current layout/setter | Destination 7 | incorporate | applied |
| C3TH-041 | ObjectStatusBlob remains `92/93`; add exact HumanObject style consumer without formal change. | Very high | current enum/layout | Destination 8 | incorporate | applied |
| C3TH-042 | Target exact managed C++ is Destination 1; no draft/sample code exists outside managed blocks. | Very high | source reconstruction | Destination 1 | incorporate | applied |
| C3TH-043 | Complete MapPane class block must preserve all current declarations/layout and change only target name/types. | Certain | current managed block diff | Destination 2 | incorporate | applied |
| C3TH-044 | Supervisor-owned manual by-memory coverage contains the exact target/padding insertion and class/file rows contain the exact no-loss replacements. | Very high | commands 13517-13519 and current row parity | supervisor-owned manual coverage | incorporate | applied |
| C3TH-045 | Waited generated output yields one target declaration/definition and zero old-name/target Empty Emitter Marker. | Very high | command 13515 generated readback | final waited verification | incorporate | applied |
| C3TH-046 | Historical long-name, blank-C++, generic `+0x178`, BackPane bounds, generic vslots, and attachment-flag assumptions must remain explicitly superseded/rejected. | Very high | old/current versus live evidence | Destinations 1-9/report | historicalize | applied |

Terminal proof mapping: Destination 1 is the unique final UID0003TH page validated by rename command `13502` and final waited command `13515`; Destination 2 is the exact MapPane block validated by `13505`; Destinations 3-9 were validated respectively by `13508`, `13509`, `13510`, `13511`, `13512`, `13513`, and `13514`. C3TH-016 is excluded because compiler allocation/EH mechanics are intentionally absent from source. C3TH-044 was not edited by B004 but is now externally applied: supervisor commands `13517`, `13518`, and `13519` installed the exact preserved handoff rows, all exit `0` / `ok:1`, and current readback matches them byte-for-byte. Every other row is applied or already present at its named destination, and command `13515` provides the terminal generated proof for C3TH-042, C3TH-043, and C3TH-045.

## Positive Evidence Summary

- Direct liveness is exact: the target has two rel32 call xrefs inside the live opcode-0x13 MapPane action handler. It is not a retained no-route helper.
- The complete function range, bytes, CFG, ABI cleanup, first/last bytes, predecessor, successor, and padding are all machine-bounded.
- The caller supplies a resolved LivingObjectPane, a zero-extended packet value checked against 100, and literal 100. The target independently reads the last two arguments as words, proving the helper-level 16-bit contract.
- Current accepted layouts resolve every former raw field: LivingObjectPane `m_objectStatus`, ObjectStatusBlob `m_statusKind`, HumanObject enum zero, and LivingObjectPane `m_hitBarObjectPane`.
- Direct callees and vtable bytes resolve every former generic helper: GetMapPosition, HitBar constructor, SetHitBarObjectPane, AddObjectPane, ScheduleTimer, UpdatePosition, GetBounds, and InvalidateRect.
- The HitBar timer callback independently proves event 0 is the temporary overlay self-delete route. This gives the 500 ms call semantic meaning without inventing a source-level timer wrapper.
- Adjacent source-ready MapPane/EffectObjectPane and HitBar sibling reports corroborate the selected source style and exact register/timer/position/bounds/invalidation order.
- The strongest inference chain is therefore complete: live MapPane packet caller -> typed target/status -> branch-specific HitBar construction -> typed owner/list registration -> inherited timer lifetime -> position/bounds invalidation. This is sufficient for a human source body.

## IDA MCP Facts

- Function: `sub_50EDD0`, `[0x0050edd0,0x0050eef5)`, size `0x125`, 14 blocks, complexity 2, one null-return branch plus one style branch.
- Full-byte SHA256: `170AE5F6E8296C07A1F831432A64B47FFBA36A9BD6E903F81A73FBAB00316D6F`.
- Prototype lead: `void __thiscall(_DWORD **this, _DWORD *, __int16, __int16)`; typed reconstruction resolves the pointer and unsigned 16-bit semantics.
- Callers/xrefs: containing caller `sub_512960`; exact xrefs `0x00512b96` and `0x00512c7f`; no other caller.
- Callees: security cookie; GetMapPosition `0x005374d0`; HitBar pool allocation; HitBar ctor `0x00538d50`; SetHitBarObjectPane `0x0053ae70`; ObjectList AddObjectPane `0x005314a0`; TimerManager ScheduleTimer `0x00597910`; GrafPort GetBounds `0x004b8e00`; two primary virtual calls.
- Vtable facts: HitBar primary slot `+0x20` at data `0x00620758` contains `0x00537720` (`InvalidateRect`); primary slot `+0x70` at `0x006207a8` contains `0x00538ec0` (`UpdatePosition`). TimerHandler-view table at `0x006207e0` contains adjusted destructor and timer callback `0x005386a0`.
- Timer callback: exact 29-byte body; timer id 0 adjusts `this-0xa4` to complete object and invokes primary deleting-destructor behavior with flag 1, then returns true.
- Data/padding: target successor `[0x0050eef5,0x0050ef00)` is eleven `CC` bytes; no target-owned table/global is present.
- Negative machine facts: no target start/end VA, RVA, or raw-offset pointer patterns; no string; no allocation failure branch; no value clamp; no old-pane read/removal; no extra timer event.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `[0x0050ec30,0x0050edd0)` | retained `sub_50EC30` under UID0001AW | separate predecessor helper | parent-managed | UID0001AW | unchanged | Ends exactly at target start; not merged or owned by this report. |
| `[0x0050edd0,0x0050eef5)` | UID0003TH current old path; recommended `...MapPaneCreateHitBarObjectPane.md` | live private MapPane source method | true | UID0001AW / UID00007Q | `86/90 -> 92/94` | Rename and emit Destination 1. |
| `[0x0050eef5,0x0050ef00)` | UID0000VN `by-memory/-ignored.md` | compiler/linker alignment | no source | adjacency | ignored/100 | Existing exact row; rename its link/wording only. |
| `[0x0050ef00,0x0050f029)` | retained `sub_50EF00` under UID0001AW | separate successor helper | parent-managed | UID0001AW | unchanged | Starts after padding; not merged. |
| `[0x00512960,0x00512ca4)` | UID0003TQ `MapPaneHandleActionPacket` | sole live caller/packet handler | true | UID0001AW / UID00007Q | `86/90` unchanged | Add exact two-call argument facts; broader body remains blank. |

No child registration is required. UID0003TH already exists and is the exact source child; the callback operation is a validator-aware UID-preserving rename.

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0050837e` | dispatcher -> `sub_512960` | Live MapPane opcode `0x13` action/combat route. |
| `0x00512b96` | UID0003TQ -> UID0003TH | First target call after value `<=100`; pushes 100, parsed value, resolved LivingObjectPane. |
| `0x00512c7f` | UID0003TQ -> UID0003TH | Second target call with the same exact argument pattern. |
| target entry | `objectPane == NULL` | Sole explicit safety check. |
| target -> `0x005374d0` | `ObjectPane::GetMapPosition` | Copies exact row/column. |
| target -> `0x00538d50` twice | HitBar constructor | Two source allocations, style 1/0. |
| target -> `0x0053ae70` | `SetHitBarObjectPane` | Installs borrowed owner pointer at LivingObjectPane `+0x1dc`. |
| target -> `0x005314a0` | `ObjectList::AddObjectPane` | Registers new type-7 pane in MapPane object state. |
| target -> `0x00597910` | `TimerManager::ScheduleTimer` | Schedules TimerHandler view, event 0, delay 500, args 0/0. |
| primary vslot `+0x70` | `0x00538ec0` | `HitBarObjectPane::UpdatePosition`. |
| target -> `0x004b8e00` | inherited GrafPort | `GetBounds`. |
| primary vslot `+0x20` | `0x00537720` | `ObjectPane::InvalidateRect`. |
| TimerHandler slot | `0x005386a0` | Event-0 complete-object self-delete callback. |

## Documentation Evidence And IDA Status

- The historical target state was useful but incomplete. Its exact range/callers/ownership/pool/timer/padding facts were preserved; its blank Item Summary/body and unresolved generic field/helper vocabulary are now explicitly superseded.
- UID0001AW remains a non-emitting split container. Its target child row is renamed and expanded without converting the aggregate back into emission.
- UID0003TQ still records both calls and now includes exact <=100/literal-100/unsigned-short promotion facts without changing its score or blank broader formal body.
- Current MapPane class/file pages preserve the complete B001/B002/B004/B005 union. Destination 2 is applied byte-for-byte except for the accepted target declaration name and two value types; class/file scores remain unchanged.
- Current HitBarObjectPane, LivingObjectPane, and ObjectStatusBlob formals retain their ctor/field/enum contracts; bounded consumer/liveness prose was added without score or formal changes.
- Historical generated command `13487` had one stale declaration, one UID0003TH Empty Emitter Marker, and zero target definitions. Current waited command `13515` has one accepted declaration, one accepted definition, one UID marker, and zero target Empty Emitter Markers or old-name tokens.
- Current manual by-memory coverage contains UID0003TH and its exact padding row at lines 2289-2290 between UID0003TG padding and UID0001AX. Current class/file rows at lines 302 and 156 contain the exact source-ready HitBar creation replacements. The preserved no-loss text supplied later matches all four current rows exactly.
- IDA status: database `64c11373` was healthy when this evidence was collected and revalidated; this statement does not assert indefinite future session availability.

## Ranked Ownership Analysis

### 1. MapPane / NexusTK/map/MapPane.cpp

- Evidence for: ECX is MapPane at both live calls; source reads `m_objectList`; method is called only by a MapPane packet handler; address/source cluster is MapPane; it decides target status, construction style, attachment, registration, timer, and invalidation policy.
- Evidence against: exact original method symbol is absent; this affects spelling only.
- Decision: direct owner UID00007Q, file owner UID0000L3, sole emitter UID00007Q.

### 2. HitBarObjectPane / AttachedObjectPane.cpp

- Evidence for: target allocates and constructs HitBarObjectPane and calls its timer/position/render surfaces.
- Evidence against: those are dependency/constructed-object facts. The receiver is MapPane, the sole callers are MapPane, and policy depends on LivingObjectPane status plus MapPane ObjectList state.
- Decision: support/constructed-class owner only, not target source owner.

### 3. ObjectList, LivingObjectPane, or TimerManager support modules

- Evidence for: each owns a called API or field contract.
- Evidence against: no candidate owns the whole control flow or receiver; consumer/callee relationships are not ownership.
- Decision: reject as target owners; retain bounded support synchronization only.

### Proposed new file/grouping, if applicable

- Proposed owner/name/path was and remains not applicable. The implementation uses existing `NexusTK/map/MapPane.cpp`.
- Likely full contents: the current complete MapPane class and address-ordered child definitions already establish the source unit.
- Candidate related items that belong: this one private method after the effect-helper family and before later static-object helpers according to existing emitter ordering.
- Candidate related items rejected: HitBar constructor/vtables/pool/timer callback remain AttachedObjectPane-family source; action packet parser remains UID0003TQ; padding remains ignored.
- Standalone, narrow, or broad source-file inference: existing broad MapPane source root, narrow private member definition.

## Source Placement

- Recommended source file/class/module: private `MapPane` declaration and one definition in `NexusTK/map/MapPane.cpp`, emitted by UID00007Q/UID0000L3.
- This placement fits the exact receiver, sole callers, `m_objectList` dependency, neighboring MapPane helper cluster, and current generated source route.
- Rejected placements: `AttachedObjectPane.cpp` owns the constructed HitBar class; `LivingObjectPane.cpp` owns target storage/lifecycle; `ObjectList.cpp` owns generic insertion; timer modules own scheduling infrastructure; none owns this MapPane decision policy.
- Remaining uncertainty: exact lexical method spelling only. No placement uncertainty remains.

## Range / Split / Padding / Reclassification Analysis

- Exact target boundary is unchanged: `[0x0050edd0,0x0050eef5)`, 293 bytes, full-byte SHA above.
- The predecessor function ends exactly at `0x0050edd0`. The successor function starts at `0x0050ef00`. No instruction or xref crosses either target boundary.
- `[0x0050eef5,0x0050ef00)` is exactly eleven `0xcc` bytes and remains UID0000VN ignored alignment. Only the target-name/link wording changed with the completed UID-preserving rename.
- UID0003TH remains `Nested:0`. The manual hierarchy places it as a child of UID0001AW at the same indentation as adjacent exact MapPane children; no raw nesting delta changed.
- Parent UID0001AW remains non-emitting/reconstructable false. This exact child owns the source body; the parent retains broad raw/child inventory and blank formal C++.
- Reclassification is source-quality only: current descriptive `...EventOverlay` path/name becomes the likely human source method `...CreateHitBarObjectPane`. No new UID or child is created.

## Negative Evidence Summary

- No surviving symbol/string proves the original name. The recommendation is ranked inference and confidence remains below 95.
- No pointer-table/absolute/RVA/raw-offset reference reaches the target; this rejects callback/vtable registration but does not weaken two direct call xrefs.
- No other target caller exists. The two live action-handler calls fully establish liveness but do not prove a public API.
- No target-owned global, table, vtable, RTTI, or standalone file route exists.
- No old HitBar pointer is read, cleared, removed, or deleted in this helper. Importing UID0003TC replacement policy would be a behavioral regression.
- No allocation, ObjectList, TimerManager, or constructed-pane null guard exists.
- No percentage computation, saturation, clamp, or signed comparison occurs in the target.
- No source-level reason supports explicit pool allocation, EH cleanup, security cookie, vptr assignment, TimerHandler byte adjustment, or vtable calls.
- Historical BackPane ownership of `0x004b8e00`, generic "bounds setter" for vslot `+0x20`, generic refresh for `+0x70`, and generic `+0x178` overlay/link state are contradicted by current typed evidence.
- `CreateHitBarObjectPaneEventOverlay`, `ShowHitBar`, `CreateTemporaryHitBar`, and `CreateCombatOverlay` remain weaker names because they encode call context/lifetime rather than the direct creation operation.

## IDA Rename / Type / Comment Recommendations

- Source-facing rename: `sub_50EDD0` / `MapPaneCreateHitBarObjectPaneEventOverlay` -> `MapPane::CreateHitBarObjectPane` in documentation and emitted source.
- Source-facing prototype: `void __thiscall MapPane::CreateHitBarObjectPane(LivingObjectPane *objectPane, unsigned short currentValue, unsigned short maximumValue)`.
- Source-facing fields/types: `ObjectStatusBlob::m_statusKind`, `ObjectStatusBlob::HumanObject`, `LivingObjectPane::m_hitBarObjectPane`, `MapPane::m_objectList`, `MapPoint`, `RectBounds`, `TimerHandler`.
- Comments/prose should state that HumanObject selects style 1, other kinds style 0, and event 0 self-deletes the temporary pane after 500 ms. Do not insert reverse-engineering offsets into formal C++.
- IDA DB edits are not requested and were not performed. Documentation/source-quality rename/type recommendations are sufficient for this assignment.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes. The target is live, source-authored, reconstructable, fully bounded, and all behavior/type blockers are resolved.
- Managed formal count: `2`. Destination 1 is the exact target definition. Destination 2 is the complete no-loss current MapPane class block with only the target private declaration changed.
- No C++ draft, sample, pseudocode, or proof comment exists outside these exact managed blocks.

### Destination 1 - renamed UID0003TH target

Destination path: `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::CreateHitBarObjectPane(
    LivingObjectPane *objectPane,
    unsigned short currentValue,
    unsigned short maximumValue)
{
    if (objectPane == NULL)
        return;

    MapPoint mapPosition;
    objectPane->GetMapPosition(&mapPosition);

    HitBarObjectPane *hitBarObjectPane;
    if (objectPane->m_objectStatus.m_statusKind ==
        ObjectStatusBlob::HumanObject)
    {
        hitBarObjectPane = new HitBarObjectPane(
            objectPane,
            mapPosition.row,
            mapPosition.column,
            currentValue,
            maximumValue,
            1);
    }
    else
    {
        hitBarObjectPane = new HitBarObjectPane(
            objectPane,
            mapPosition.row,
            mapPosition.column,
            currentValue,
            maximumValue,
            0);
    }

    objectPane->SetHitBarObjectPane(hitBarObjectPane);
    m_objectList->AddObjectPane(hitBarObjectPane);
    g_pTimerMgr->ScheduleTimer(
        static_cast<TimerHandler *>(hitBarObjectPane),
        0,
        500,
        0,
        0);
    hitBarObjectPane->UpdatePosition();

    RectBounds bounds;
    hitBarObjectPane->GetBounds(&bounds);
    hitBarObjectPane->InvalidateRect(&bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 2 - complete current MapPane class block

Destination path: `by-class/MapPane.md`. The block below preserves the complete evidence-time class union and changes only `CreateHitBarObjectPaneEventOverlay(... unsigned int ...)` to `CreateHitBarObjectPane(... unsigned short ...)`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "Pane.h"
#include "GrafPort.h"
#include "Motion.h"

#include <vector>

class AlphaMaskSurface;
class Event;
class EffectObjectPane;
class Layer;
class LightingObjectPane;
class LivingObjectPane;
class LObject;
class Message;
class ObjectList;
class ObjectPane;
class ScreenEffecter;
struct EffectInfo;
struct ItemObjectImageEntry;
struct MapTileLayerIds
{
    unsigned int groundTileId;
    unsigned int objectNation;
    unsigned int flags;
};
struct MapPaneTileRecord;
struct MapPoint;
struct RectBounds;
typedef RectBounds MapRect;
struct ObjectStatusBlob;
struct Rect;

class MapPane : public Pane
{
public:
    MapPane(int visibleTileColumns, int visibleTileRows);
    virtual ~MapPane();

    virtual void AddToLayer(
        const RectBounds *bounds,
        int order,
        Pane *parentPane,
        Layer *layer);
    virtual void QueueMotionMessage(const Rect *rect);
    virtual void OnChangeMessage(LObject *sender, Message *message);
    virtual bool HandlePointerOrMouseEvent(Event *event);
    virtual bool HandleKeyOrTextEvent(Event *event);
    virtual bool HandlePacketEvent(Event *event);
    virtual bool OnNotifyMessage(const unsigned char *message);
    virtual bool OnTimerEvent(int timerId, int param1, int param2);
    virtual void OnPaint();

    bool HasScreenEffecters() const;
    ScreenEffecter *FindActivePixelEffecter();
    void ResetScreenEffecterListFields();
    void ConfigureScreenEffecterList(signed char effectMode);
    void ClearScreenEffecterList();
    void ExitToMenu(bool notifyServer);
    void LoadCompressedMapFile(unsigned short mapId, int unusedLoadOption);
    void SaveCompressedMapFile();
    void MapToScreenCoords(MapPoint *point) const;
    void ScreenToMapCoords(MapPoint *point) const;
    void RedrawVisibleTiles();
    bool ValidateMapAndViewportCoords(
        int mapColumn,
        int mapRow,
        int viewportColumn,
        int viewportRow) const;
    void GetClampedVisibleTileBounds(MapRect *outBounds) const;
    void GetVisibleTileBounds(MapRect *outBounds) const;
    MapPoint *GetVisibleTileSpan(MapPoint *outSpan) const;
    void GetExtendedVisibleTileBounds(MapRect *outBounds) const;
    int CheckMovementCollision(
        int mapColumn,
        int mapRow,
        unsigned char direction);
    bool AllowsMovementStatusTag() const;
    bool ApplyMovementSubstepScroll(unsigned char direction);
    unsigned short GetAdjacentVisibleTileBounds(
        unsigned char direction,
        MapRect *outBounds) const;
    void AfterLocalPlayerWarp();
    void ObjectCallbackNoOp(ObjectPane *objectPane);
    void ScrollViewportByDirection(unsigned char direction);
    void ReindexObjectPanePosition(
        ObjectPane *objectPane,
        int removeRow,
        int removeColumn,
        int addRow,
        int addColumn);
    void RecenterAndSendPosition(
        int targetX,
        int targetY,
        int viewportX,
        int viewportY,
        bool useActiveObjectViewport);
    void ClearQueuedMapStateRefresh();
    void ClearPendingActionPoll();
    ObjectPane *CreateOrUpdateObjectPane(
        int tileX,
        int tileY,
        int objectId,
        unsigned char action,
        const ObjectStatusBlob &status);
    void RemoveObjectPaneById(int objectId);
    void DetachObjectPane(ObjectPane *objectPane);
    bool HasObjectIntersectingRect(const Rect *rect) const;
    ObjectPane *FindObjectPaneById(int objectId);
    ObjectPane *FindPreviousSelectableObject(ObjectPane *current);
    ObjectPane *FindNextSelectableObject(ObjectPane *current);
    ObjectPane *FindPreviousItemTargetObject(ObjectPane *current);
    ObjectPane *FindNextItemTargetObject(ObjectPane *current);
    ObjectPane *FindPreviousLivingTarget(ObjectPane *current, int targetFilter);
    ObjectPane *FindNextLivingTarget(ObjectPane *current, int targetFilter);
    void RemoveObjectPaneIfPresent(ObjectPane *objectPane);
    void UpdateObjectPaneScreenBounds(ObjectPane *objectPane);
    void UpdateObjectPosition(ObjectPane *objectPane);
    LivingObjectPane *HitTestObjectAtPoint(int y, int x);
    unsigned short GetCurrentMapId() const;
    void RenderMapView();
    void ComputeDayNightBrightnessAndTint(
        unsigned char hour,
        float brightnessScale,
        float brightnessBias,
        float *outBrightness,
        float *outTintAmount) const;
    void SetDayNightFadeTarget(float targetBrightness, float tintAmount);
    void AttachObjectLighting(
        ObjectPane *anchorSource,
        int localOffsetX,
        int localOffsetY,
        int lightImageIndex);
    void RenderDayNightOverlay(const RectBounds *dirtyBounds);
    bool ShouldUseLivingObjectLightOverlay() const;
    bool ShouldOffsetElevatedLivingObjects() const;
    MapPoint *GetObjectScreenPoint(
        ObjectPane *objectPane,
        MapPoint *outPoint) const;
    void FlashMapColor(int drawColor);
    void FinishVisibleObjectRefresh();
    void InteractWithObject(LivingObjectPane *target);
    void CollectMovementTargetObjectIds(
        std::vector<unsigned int> &objectIds) const;
    void SortMovementTargetObjectIdsByDistance(
        std::vector<unsigned int> &objectIds) const;
    void ChangeMap(
        unsigned short mapId,
        short mapWidth,
        short mapHeight,
        unsigned char weatherMode,
        unsigned char transitionState,
        int loadOption);
    void AllocateTileBuffer();
    void RequestObjectEffect(
        unsigned int objectId,
        int effectDescriptorId,
        int timerAction,
        bool renderAfterLivingObjects);
    void RefreshGroupMemberHitBar(unsigned int memberId);
    void ApplyHitBarObjectPane(
        LivingObjectPane *objectPane,
        unsigned int primaryValue,
        unsigned int primaryMaximum,
        unsigned int secondaryValue,
        unsigned int secondaryMaximum);
    void RemoveGroupMemberHitBar(unsigned int memberId);
    bool HandleMapInfoTileRegionPacket(const unsigned char *packet);

private:
    void NotifyDeferredMotionChanged();
    void RefreshQueuedMapState();
    LivingObjectPane *FindObjectAtMapPoint(int y, int x);
    void DrawVisibleTiles();
    void DrawTileAt(int tileX, int tileY, bool refreshStaticObjects);
    unsigned short ComputeTileRectChecksum(const MapRect *bounds) const;
    unsigned int ComputeVisibleTileChecksum(const MapRect &bounds) const;
    MapTileLayerIds *GetLegacyAwareMapTileLayerIds(
        MapTileLayerIds *outTile,
        int mapColumn,
        int mapRow) const;
    unsigned char RemapMovementDirectionForMapScroll(unsigned char direction) const;
    void RefreshScrolledTileData(
        int objectColumn,
        int objectRow,
        int viewportColumn,
        int viewportRow,
        unsigned char direction);
    int SelectMovementPathStep();
    bool ShouldClearMovementTimerState() const;
    int BuildPathToPendingActionTarget(
        ObjectPane *target,
        const MapPoint &currentPosition);
    void ResolvePendingActionTarget(
        ObjectPane *target,
        const MapPoint &currentPosition);
    void SetMapState(int weatherType, unsigned char weatherMode);
    void RequestCoordinateEffect(
        int mapColumn,
        int mapRow,
        int effectDescriptorId,
        int timerAction,
        bool renderAfterLivingObjects);
    void ApplyEffectObjectPaneTimerAction(
        EffectObjectPane *effectObjectPane,
        int timerAction);
    void CreateEffectObjectPane(
        LivingObjectPane *target,
        int mapRow,
        int mapColumn,
        EffectInfo effectInfo,
        int timerAction,
        int sequenceIndex,
        bool renderAfterLivingObjects);
    void AttachObjectLighting(
        ObjectPane *anchorSource,
        const ItemObjectImageEntry *imageEntry,
        int localOffsetX,
        int localOffsetY);
    void CreateHitBarObjectPane(
        LivingObjectPane *objectPane,
        unsigned short currentValue,
        unsigned short maximumValue);
    void RefreshStaticObjectTileRegion(const MapRect &tileBounds);
    void ResetMapResourceStateForModeChange();
    void RefreshDeferredMapSurfacesForTileRegion();
    bool InvalidateShiftedMapArea(int deltaX, int deltaY);
    bool HandleObjectInfoPacket(const unsigned char *packet);
    bool HandleEffectMapStatePacket(const unsigned char *packet);
    bool HandleDrawObjectsPacket(const unsigned char *packet);
    bool HandleMovementSpeechBalloonPacket(const unsigned char *packet);
    bool HandleObjectMoveEffectPacket(const unsigned char *packet);
    bool HandleObjectWorldMapCreationPacket(const unsigned char *packet);
    bool HandleActionPacket(const unsigned char *packet);
    bool HandleFlyingObjectUpdatePacket(const unsigned char *packet);
    bool HandleAnimationSoundPacket(const unsigned char *packet);
    bool HandleObjectStatusPacket(const unsigned char *packet);

public:
    int m_screenEffecterCount;
    int m_screenEffecterCapacity;
    ScreenEffecter **m_screenEffecters;
    LivingObjectPane *m_highlightedObjectPane;
    bool m_selectedObjectActive;
    unsigned char m_selectedObjectPadding[3];
    LivingObjectPane *m_selectedObjectPane;
    int m_mapResourceMode;
    bool m_mapEffectEnabled;
    unsigned char m_mapEffectPadding[3];
    Motion m_deferredMotionRecords[32];
    int m_deferredMotionRecordCount;
    bool m_deferMotionMessages;
    bool m_queuedMapStateRefresh;
    bool m_pendingActionPoll;
    unsigned char m_deferredStatePadding;
    int m_pendingMovementState[6];
    float m_currentDayNightBrightness;
    float m_dayNightCurrentScale;
    float m_dayNightCurrentBias;
    float m_dayNightCurrentTint;
    float m_dayNightTintCurveScale;
    float m_dayNightTintCurveBase;
    float m_dayNightTintAmount;
    float m_newHumanProjectionDivisor;
    float m_newHumanProjectionShear;
    float m_dayNightTargetBrightness;
    AlphaMaskSurface *m_selectionOverlay;
    float m_dayNightBrightnessScale;
    float m_dayNightBrightnessBias;
    unsigned int m_pendingMapStateToken;
    bool m_waitingForLocalPlayerStatus;
    bool m_objectInteractionTimerActive;
    unsigned short m_mapId;
    short m_mapWidth;
    short m_mapHeight;
    MapPaneTileRecord *m_tileBuffer;
    int m_tileOriginX;
    int m_tileOriginY;
    int m_visibleTileColumns;
    int m_visibleTileRows;
    unsigned char m_mapTransitionState;
    unsigned char m_weatherMode;
    unsigned char m_movementGate;
    unsigned char m_dayNightHour;
    int m_weatherType;
    Pane *m_weatherLayerPane;
    LivingObjectPane *m_localPlayerObjectPane;
    Pane *m_auxiliaryPane;
    Layer *m_activeScreenLayer;
    ObjectList *m_objectList;
    GrafPort m_viewportGrafPort;
    int m_mapFlashColor;
    unsigned char m_mapRuntimeMode;
    bool m_visibleObjectRefreshPending;
    bool m_refreshWholeVisibleMap;
    unsigned char m_mapRefreshPadding;
    int m_mapDisplayOriginX;
    int m_mapDisplayOriginY;
    int m_mapDisplayScalePercent;
    wchar_t m_mapName[256];
    unsigned short m_mapNameLength;
    wchar_t m_mapResourceName[256];
    unsigned short m_mapResourceNameLength;
    unsigned char m_actionStatePrefix[8];
    bool m_pendingActionModeActive;
    unsigned char m_pendingActionModePadding[3];
    int m_pendingActionTileX;
    int m_pendingActionTileY;
    unsigned char m_pendingActionState[20];
    unsigned int m_pendingActionTargetObjectId;
    bool m_deferredPrimarySurfaceDirty;
    unsigned char m_deferredSurfacePadding;
    bool m_deferredSecondarySurfaceDirty;
    unsigned char m_finalPadding;
};

typedef char MapPaneSizeMustBe0x910[
    sizeof(MapPane) == 0x910 ? 1 : -1];

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Behavior preservation: Destination 1 follows exact branch/call order and intentionally preserves unsafe behavior and duplicate allocation lowering.
- Original-source shape: ordinary private method, enum/member access, ordinary `new`, one local pane pointer, and one local bounds value match the surrounding VC6/VC7-era project style.
- Inferred names/types: only the helper/local lexical names are inferred; class, enum, field, and API names are current accepted project contracts corroborated by live evidence.
- Third-party import directive: not applicable. This is first-party NexusTK class source, not an embedded third-party source file.
- No-code proof: not applicable. The target is eligible and receives a complete body.

## Final Recommendation

- Exact target result: UID0003TH was UID-preservingly renamed to `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md`; it is `92/94` with owner/emitter `00007Q`, reconstructable true, blank position, exact range, `Nested:0`, the exact Item Summary, and Destination 1.
- Exact class result: all UID00007Q MapPane content, score `93/94`, position 0, size guard, and class-before-children layout were preserved; Destination 2 is exact.
- Exact file result: UID0000L3 remains `92/92` on the same route with bounded target source/behavior/history detail and the renamed cross-reference.
- Exact parent/caller result: UID0001AW remains non-emitting at `86/90` and UID0003TQ remains source-deferred at `86/90`; only accepted links and child/call argument facts were added.
- Exact support result: bounded consumer/liveness prose was added to HitBarObjectPane, LivingObjectPane, and ObjectStatusBlob without score/formal changes; the existing ignored-padding owner link/label was renamed without changing bytes/disposition.
- Exact parent assignment: unchanged UID0001AW split-container relationship and UID00007Q class ownership.
- Exact no-owner/non-emitting items: none created. UID0001AW and padding remain non-emitting for their existing independent reasons.
- Exact future work outside scope: UID0003TQ's complete packet body and retained predecessor/successor helper dispositions remain separate targets; they do not block this exact method.

## Recommended Target Doc Changes

Destination 1 historical pre-callback path: `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPaneEventOverlay.md`.

Destination 1 final path: `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md`.

- Historical accepted callback prescription, now complete: use the documented validator-aware UID-preserving one-move rename workflow, validate the final path in default file mode, confirm the old path absent and UID unique, then release.
- Metadata: completion `92`, confidence `94`, owner `00007Q`, reconstructable `TRUE`, emitter `00007Q`, blank position, `Nested:0`.
- Exact Item Summary:
  `Live private MapPane::CreateHitBarObjectPane source method with two opcode-0x13 callers; accepts LivingObjectPane plus unsigned-short current/maximum values, selects style 1 only for ObjectStatusBlob::HumanObject and 0 otherwise, preserves separate ordinary-new branches, installs the typed owner slot, registers with ObjectList, schedules timer event 0 for 500 ms self-delete, updates position, obtains inherited GrafPort bounds, and invalidates them; compiler pool/EH/vptr/TimerHandler-adjustor lowering is excluded.`
- Install Destination 1 exactly in the managed formal block. No other C++ text may remain outside that block.
- Replace Status/Behavior/IDA/Ownership/Score/Cross-References/Changes prose with report-level facts from C3TH-001 through C3TH-046, preserving exact historical B001 provenance and explicitly superseding its unresolved vocabulary.
- Preserve exact bytes/hash/CFG/callers/callees/unsafe behavior/timer lifetime/negative scans/padding/source-placement/ranked naming evidence.
- Preserve rejected alternatives: long EventOverlay spelling, generic overlay/link state at `+0x178`, attachment-flag interpretation, unsigned-int/unsigned-char helper signatures, single ternary allocation, pre-existing-pane cleanup, percentage clamp, source-level pool/EH/vptr/adjustor/vslot syntax, HitBar/AttachedObjectPane owner, BackPane GetBounds, and bounds-setter vslot.

## Recommended Support Doc Changes

The destination prescriptions below are retained at full detail as the accepted callback specification. They are now applied and validated; imperative wording records that accepted specification rather than outstanding work.

### Destination 2 - `by-class/MapPane.md`

- Apply the complete Destination 2 managed block exactly, preserving the full current class union, includes/forwards, inheritance, size guard, fields, unrelated methods, `[[CHILDREN]]`, score `93/94`, owner/file route, and position 0.
- Add a UID0003TH detail section with exact range/hash/caller/value/status/style/allocation/registration/timer/update/bounds/invalidation/negative/history/score evidence.
- Rename the cross-reference and historical current-facing detail to the new target path/name. Preserve historical B001 wording only when labeled historical.
- Do not change any other score, signature, field, method, or formal content.

### Destination 3 - `by-file/MapPane.md`

- Preserve UID0000L3 score `92/92`, `NexusTK/map/` route, complete source-family union, and prose-only formal policy.
- Add UID0003TH as source-ready private `CreateHitBarObjectPane`; include unsigned-short ABI, HumanObject style decision, separate ordinary-new branches, typed owner/ObjectList registration, event-0 500 ms self-delete timer, UpdatePosition/GetBounds/InvalidateRect order, compiler exclusions, and renamed cross-reference.
- Do not inflate file score or add C++ to this by-file page.

### Destination 4 - `by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md` (UID0001AW)

- Preserve `86/90`, owner UID00007Q, reconstructable false, blank emitter/position/formal, `Nested:0`, all current exact children/raw-history facts, and every concurrent B001/B004/B005 addition.
- Replace only UID0003TH current-facing child/path/name/detail text with exact source-ready facts and link. Preserve the aggregate as non-emitting.
- Do not claim that UID0003TQ or retained helper bodies became source-ready through this work.

### Destination 5 - `by-memory/0x00512960-0x00512ca4.MapPaneHandleActionPacket.md` (UID0003TQ)

- Preserve `86/90`, owner/emitter UID00007Q, reconstructable true, blank position/formal, `Nested:0`, and broader packet uncertainty.
- Rename the two target links. Add exact calls `0x00512b96`/`0x00512c7f`, each after unsigned value `<=100`, each passing maximum 100 and a resolved LivingObjectPane, with source promotion into unsigned-short helper parameters.
- Keep target source-body ownership separate from UID0003TQ's still-blank broad packet method.

### Destination 6 - `by-class/HitBarObjectPane.md`

- Preserve `90/93`, owner/emitter UID0000HJ, position 20, complete ctor/class formals, and all sibling behavior.
- Add UID0003TH as the only caller of the single-value constructor at `0x00538d50`; exact fields are current/maximum unsigned shorts and style byte; HumanObject selects 1, other status kinds 0.
- Add target timer-liveness fact: direct event-0/500 schedule through inherited TimerHandler; exact TimerHandler-view callback `0x005386a0` self-deletes the complete pane. Preserve existing destructor/vtable/pool ownership.

### Destination 7 - `by-class/LivingObjectPane.md`

- Preserve `92/93`, owner/emitter UID0000KU, complete layout/formal, and unrelated fields/methods.
- Add UID0003TH as a consumer of `m_objectStatus.m_statusKind`, GetMapPosition, and setter-installed `m_hitBarObjectPane`; record that this helper overwrites the slot without first reading/removing an old pane.
- Do not change field offsets/types/formal C++.

### Destination 8 - `by-class/ObjectStatusBlob.md`

- Preserve `92/93`, owner/emitter UID0000M6, enum/layout/formal, and unrelated status consumers.
- Add UID0003TH as an exact consumer: `HumanObject` value 0 selects HitBar style 1; all nonzero kinds select style 0.
- Do not reinterpret `m_statusKind` as bool or change enum values.

### Destination 9 - `by-memory/-ignored.md`

- Preserve every unrelated row and the exact existing `[0x0050eef5,0x0050ef00)` eleven-CC disposition/evidence.
- Change only the target-facing label/link from `MapPaneCreateHitBarObjectPaneEventOverlay` and old path to `MapPaneCreateHitBarObjectPane` and final path.
- No score, formal, range, or source replacement applies.

### Verify-only support

- Verify unchanged unless validator-managed reverse-reference propagation occurs: UID00009Q ObjectList; exact AddObjectPane child; TimerHandler/TimerManager/global support; MapPoint; ObjectPane GetMapPosition; GrafPort GetBounds; UID0004QM setter; HitBar constructor/pool/vtable/storage pages; UID0003TC/UID0003TB/UID0003TD; predecessor/successor helpers.
- If a concurrent shared-page change appears before callback, reread the latest destination under the short lease and rebase this bounded delta onto the complete union. Do not overwrite unrelated content.

## Score And Metadata Recommendation

- Historical pre-callback target: `86/90`, owner/emitter UID00007Q, reconstructable true, blank position, blank Item Summary/C++, `Nested:0`.
- Current applied target: `92/94` with all non-score metadata unchanged except populated Item Summary/C++ and the completed UID-preserving name/path correction.
- Completion rationale: exact body, signature, types, fields, branches, API names, unsafe behavior, source route, compiler boundary, caller/liveness, padding, and generated/manual plan remove every listed source-quality blocker.
- Confidence rationale: live binary and current typed support form a mutually consistent whole. Confidence remains 94 because no original symbol proves exact helper/local spelling and because only two current callers constrain the practical value domain.
- Reason not lower: direct calls, exact target bytes, typed layouts, constructor/vtable/callback analyses, and adjacent source patterns are substantially stronger than the old 86/90 description.
- Reason not higher: inferred lexical name and no original source/PDB.
- Score blocker audit:
  - blank Item Summary: resolved with exact text above;
  - packet values: resolved to helper-level unsigned shorts, with caller UInt8 promotion documented;
  - `+0x178`: resolved to ObjectStatusBlob `m_statusKind` and HumanObject zero;
  - object/list insertion: resolved to `m_objectList->AddObjectPane`;
  - timer subobject/API: resolved to inherited TimerHandler and direct manager scheduling;
  - virtual refresh/bounds calls: resolved to UpdatePosition/GetBounds/InvalidateRect;
  - style flag: resolved to style byte 1/0 by status kind;
  - original name: exact symbol unavailable after exhaustive search, best source-facing name ranked;
  - source placement/owner/emitter: resolved to MapPane/UID00007Q/UID0000L3;
  - range/split/padding: exact and unchanged;
  - formal C++: complete in two managed blocks.
- Support scores remain unchanged: MapPane class `93/94`, MapPane file `92/92`, parent/caller `86/90`, HitBar `90/93`, LivingObjectPane `92/93`, ObjectStatusBlob `92/93`.

## Open Questions With Attempted Resolution

- Original helper spelling: searched IDA names/strings, exact/broad prior-report roots, current docs, generated output, sibling names, and source-tree context. No exact symbol survives. `CreateHitBarObjectPane` is the strongest source-facing decision; this is a confidence cap, not a body blocker.
- Parameter width: compared caller byte extraction, exact pushes, target word loads, `retn 0x0c`, ctor prototype, and stored fields. Resolution is unsigned short. Unsigned int is a stale broad declaration; unsigned char proves only caller values, not target ABI.
- `+0x178`: re-read current complete LivingObjectPane/ObjectStatusBlob layouts and live target compare. Resolution is `m_objectStatus.m_statusKind == HumanObject`.
- Style semantic name: ctor field is currently accepted as byte `style`; no stronger lexical symbol exists. Keep numeric style values in callsites rather than inventing a new enum.
- Timer source shape: compared inherited wrapper availability with direct manager call and adjacent accepted MapPane source. Resolution is the direct manager call with source-level `static_cast<TimerHandler *>`; no raw `+0xa4`.
- One allocation with ternary versus two branch allocations: compared exact duplicate callsites and EH-state structure. Two source `new` expressions are higher probability and selected.
- Existing HitBar cleanup: exhaustive target read finds none. Resolution is to preserve overwrite/no-check behavior even if it looks unsafe.
- Remaining unresolved investigable questions: none. Only original lexical spelling remains inherently unavailable and is bounded by ranked evidence.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- Inspected manual files read-only after supervisor coverage application:
  - `by-memory/-coverage-report.md`, current read-only SHA256 `6BFCF7632E4A1477673E6933A5F5747D431D8619779039066A1C212ED33725E4`, 1,717,855 bytes / 4,140 lines. Supervisor command `13517` applied the exact handoff: UID0003TH is at line 2289 and its `[0x0050eef5,0x0050ef00)` padding row is at line 2290.
  - `by-class/-coverage-report.md`, current read-only SHA256 `FB3EAAB086260B1963DD61D7CAF72658A41047B37273069AF393316FA4B7858C`, 223,012 bytes / 622 lines. Supervisor command `13518` applied the exact UID00007Q replacement at line 302.
  - `by-file/-coverage-report.md`, current read-only SHA256 `31E734F88D76EAF3939F77651578AC59B2D00C8273576D99A8716AA70419B37D`, 126,347 bytes / 316 lines. Supervisor command `13519` applied the exact UID0000L3 replacement at line 156.
  - `by-type/by-struct/-coverage-report.md`, current read-only SHA256 `269D77A8801F26AAA36F410C842B137973103FE81B55D0BA37C062AE2D16A1E5`, 57,098 bytes / 137 lines. The accepted MapPoint row remains at line 72 with row/Y then column/X and synchronized consumers; no by-struct replacement/addition/deletion is required.
- External application classification: the hashes advanced from the pre-coverage snapshots because the supervisor applied this report's exact handoff. Commands `13517`, `13518`, and `13519` each exited 0 with `ok:1`; no blocking warning was reported. Direct reread proves exact target/padding/class/file parity with the preserved text below.
- Direct string-parity readback: preserved UID0003TH row equals current by-memory line 2289; preserved padding row equals line 2290; preserved UID00007Q replacement equals current by-class line 302; preserved UID0000L3 replacement equals current by-file line 156. All four comparisons are exact `True`.
- Validator-generated auto coverage did not substitute for these manual rows. B004 did not edit them; the supervisor-owned application is complete.

### by-memory exact insertion

Historical exact placement prescription, now applied by supervisor command `13517`: insert after the existing UID0003TG successor-padding row for `[0x0050ec2e,0x0050ec30)` at line 2288. Current applied rows are UID0003TH at line 2289 and its successor padding at line 2290, before the shifted UID0001AX row.

```text
        - [UID:0003TH][0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane](by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md) 0x0050edd0-0x0050eef5 | private class method | MapPaneCreateHitBarObjectPane : reconstructable : 92% : very-strong : Exact live private MapPane::CreateHitBarObjectPane source helper with two opcode-0x13 callsites; accepts LivingObjectPane and unsigned-short current/maximum values, selects style 1 only for ObjectStatusBlob::HumanObject and 0 otherwise, preserves separate ordinary-new branches, installs the typed HitBar owner slot, registers with ObjectList, schedules timer event 0 for 500 ms self-delete, updates position, obtains inherited GrafPort bounds, and invalidates them; compiler pool/EH/vptr/TimerHandler-adjustor lowering is excluded.
        - [UID:0000VN][-ignored](by-memory/-ignored.md) 0x0050eef5-0x0050ef00 | padding | MapPaneCreateHitBarObjectPane to retained DamageNumberObjectPane helper alignment : ignored : 100% : strong : Eleven exact 0xcc compiler/linker alignment bytes after UID0003TH end-exclusive 0x0050eef5 and before the next modeled retained helper begins at 0x0050ef00; no source replacement.
```

### by-class exact replacement

Historical exact UID00007Q replacement, applied by supervisor command `13518` and currently present at line 302:

```text
- [UID:00007Q][MapPane](by-class/MapPane.md) : reconstructable : 93% : very strong : Complete source-era MapPane declaration emitter at position 0 with exact 0x910 layout and current method union, including source-ready RequestObjectEffect, RequestCoordinateEffect, CreateEffectObjectPane, and ApplyEffectObjectPaneTimerAction integration with typed EffectInfo/LivingObjectPane/EffectObjectPane and exact event-2 action semantics, plus source-ready private CreateHitBarObjectPane with unsigned-short values, HumanObject style selection, separate ordinary-new branches, typed owner/ObjectList registration, event-0 500 ms self-delete scheduling, UpdatePosition, inherited GetBounds, and InvalidateRect, followed by address-ordered exact children.
```

### by-file exact replacement

Historical exact UID0000L3 replacement, applied by supervisor command `13519` and currently present at line 156:

```text
- [UID:0000L3][MapPane](by-file/MapPane.md) : reconstructable : 92% : very strong : NexusTK/map/MapPane.cpp source root with complete UID00007Q class-before-children route and exact source-ready object/coordinate effect descriptor, EffectObjectPane creation, and existing-pane delete/cancel/guarded timer replacement family; preserves typed LivingObjectPane/EffectInfo/TimerHandler contracts, target registration/timer/bounds behavior, and prose-only by-file policy, plus source-ready private HitBarObjectPane creation with ObjectStatusBlob HumanObject style selection, separate ordinary-new branches, typed owner/ObjectList registration, event-0 500 ms self-delete timing, and exact UpdatePosition/GetBounds/InvalidateRect order.
```

- No tracker text is authored: auto-generated tracker state is validator/supervisor-owned and should follow ordinary validation/report lifecycle externally.
- B004 boundary: manual `-coverage-report.md` files are supervisor-owned restricted files. B004 preserved the handoff text and performed only read-only parity checks; commands `13517`-`13519` were supervisor actions.

## Follow-Up Actions

- Terminal Gate 1/Gate 2 for pre-coverage artifact `49B24774E93084BB12DF30039F616EC3A433BFC54BD87206758EBCC224FB483F` and manual coverage commands `13517`-`13519` are completed supervisor-owned facts. Report execution/count/path/move/archive state and later lifecycle history remain external and are neither asserted nor directed by this artifact.
- A-agent actions: none required for this target-specific source closure.
- B004 implementation actions are complete: Destinations 1-9 were applied under short one-file leases, the latest shared union was preserved, all scoped validators and the waited target refresh passed, generated assertions were verified, and this report was reconciled claim by claim.
- Outside this assignment: UID0003TQ full packet reconstruction and retained no-route helper bodies remain independent tracker targets. This report does not defer any UID0003TH source item to them.

## Confidence

- Recommendation confidence: `94/100` very strong.
- Score confidence: very strong for `92/94`.
- Range/ABI/behavior confidence: effectively exact from live binary and accepted type contracts.
- Ownership/source placement confidence: very strong.
- Naming confidence: high for `CreateHitBarObjectPane`, not original-symbol proof.
- Remaining uncertainty: only exact original lexical spellings for the helper/local variables and the semantic label of ctor style byte. Neither changes generated behavior or prevents source-ready C++.

## Validator Results

| Command | Timestamp EDT | Scoped path | Exit / ok | Warnings and side effects |
| --- | --- | --- | --- | --- |
| `13502` | `2026-07-15T21:09:33-04:00` | renamed UID0003TH final path | `0 / 1` | UID path changed once; 13 reverse links and 9 reference-source paths updated; score `92/94`; registry blank-to-block; deferred generation. Existing repository missing-reference diagnostics were reported without failure. |
| `13505` | `2026-07-15T21:11:38-04:00` | `by-class/MapPane.md` | `0 / 1` | Formal registry hash updated; projected stats refreshed; existing missing-reference diagnostics only. |
| `13508` | `2026-07-15T21:12:28-04:00` | `by-file/MapPane.md` | `0 / 1` | Prose-only file route validated; projected stats refreshed; existing missing-reference diagnostics only. |
| `13509` | `2026-07-15T21:13:41-04:00` | UID0001AW parent | `0 / 1` | Non-emitting parent validated; projected stats refreshed; existing missing-reference diagnostics only. |
| `13510` | `2026-07-15T21:14:17-04:00` | UID0003TQ caller | `0 / 1` | Scoped page and projected stats validated; no target warning. |
| `13511` | `2026-07-15T21:14:54-04:00` | `by-class/HitBarObjectPane.md` | `0 / 1` | UID0003TH reference-index edge added; projected stats refreshed. |
| `13512` | `2026-07-15T21:15:26-04:00` | `by-class/LivingObjectPane.md` | `0 / 1` | UID0003TH reference-index edge added; existing unrelated missing-reference diagnostics only. |
| `13513` | `2026-07-15T21:16:00-04:00` | `by-class/ObjectStatusBlob.md` | `0 / 1` | UID0003TH reference-index edge added; projected stats refreshed. |
| `13514` | `2026-07-15T21:16:40-04:00` | `by-memory/-ignored.md` | `0 / 1` | Existing large-ledger missing-reference diagnostics only; projected stats refreshed. |
| `13515` | `2026-07-15T21:17:18-04:00` | final UID0003TH path with `--wait-generated` | `0 / 1` | Correct LivingObjectPane/ObjectStatusBlob edges added; registry rebuild and authorized generated metadata/tracker/auto-coverage refresh completed. Existing project-wide children/emitter advisories did not fail the target. |
| `13517` | supervisor post-Gate coverage action | `by-memory/-coverage-report.md` | `0 / 1` | Applied the exact two-row UID0003TH/padding handoff; current rows 2289-2290 match the preserved text. No blocking warning was reported; the manual file advanced to SHA256 `6BFCF7632E4A1477673E6933A5F5747D431D8619779039066A1C212ED33725E4`. |
| `13518` | supervisor post-Gate coverage action | `by-class/-coverage-report.md` | `0 / 1` | Applied the exact UID00007Q no-loss replacement at line 302. No blocking warning was reported; the manual file advanced to SHA256 `FB3EAAB086260B1963DD61D7CAF72658A41047B37273069AF393316FA4B7858C`. |
| `13519` | supervisor post-Gate coverage action | `by-file/-coverage-report.md` | `0 / 1` | Applied the exact UID0000L3 no-loss replacement at line 156. No blocking warning was reported; the manual file advanced to SHA256 `31E734F88D76EAF3939F77651578AC59B2D00C8273576D99A8716AA70419B37D`. |

- Historical generated baseline command `13487` had one stale declaration, one target Empty Emitter Marker, and zero target definitions.
- Current generated readback after command `13515`: `auto-generated/NexusTK/map/MapPane.cpp`, SHA256 `0E2FC02FE45981F395CE3967FC06F20CCCDB050832AF55B2A412E16D0D5D4E5A`, 103,866 bytes / 3,043 lines.
- Generated assertions: declaration count `1`; definition count `1`; UID0003TH marker count `1`; old-name count `0`; target Empty Emitter Marker count `0`; target generated interval is lines 2624-2676 and contains exactly two ordinary `new HitBarObjectPane` expressions, one HumanObject test, and one event-0/500 `ScheduleTimer`; `UpdatePosition`, `GetBounds`, and `InvalidateRect` occur in increasing source order; handwritten target pool/EH/cookie/vptr/raw TimerHandler-offset/vtable-slot count `0`; no duplicate target or sibling body was introduced.
- Formal equality: report Destination 1 equals the final UID0003TH managed block byte-for-byte at SHA256 `B0BFB62D47E07FB6AD515E2A944B2809ABDDD51BCEC5C94B51286A755353BD3D`; report Destination 2 equals the complete final MapPane managed block byte-for-byte at SHA256 `7DC24413189495C7F1F70552370985CFCA6766545696755220AD5B3A9F9E94B0`. No C++ was added to by-file, parent, caller, or the three support-class formals.
- All B004 implementation validators and supervisor coverage commands exited 0 with `ok:1`; no implementation or manual-handoff blocker remains.

## Changed Files

- Renamed once with UID preservation: old `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPaneEventOverlay.md` is absent; final `by-memory/0x0050edd0-0x0050eef5.MapPaneCreateHitBarObjectPane.md` is the unique UID0003TH page, SHA256 `8AB3FC668FDB9F48763A6CF0761053EDA9D58CCA8A4650B1CA96907284045028`, 11,659 bytes / 158 lines.
- Modified `by-class/MapPane.md`: SHA256 `81E3DA7781AC2D7B8ED89FDFCED4EEC5F24322524EF8C0DC238F385C6A9B2652`, 156,814 bytes / 896 lines.
- Modified `by-file/MapPane.md`: SHA256 `58EC08AF0CDF821DCDC4B9B10C975A849EFB2ACEE48E91858E61A81DE2CC9E47`, 157,764 bytes / 656 lines.
- Modified UID0001AW `by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md`: SHA256 `EF0A51650C70C6C7C75D6CD8ACA18282B0DEA7326AE8709EDBFB371311F14720`, 76,302 bytes / 313 lines.
- Modified UID0003TQ `by-memory/0x00512960-0x00512ca4.MapPaneHandleActionPacket.md`: SHA256 `D17912847FE3F399DA565C7C101EDFE065E42D1E287549BD6A56355E9D0F3898`, 7,154 bytes / 87 lines.
- Modified `by-class/HitBarObjectPane.md`: SHA256 `9266E03ACC4E41DE0C874728AFAA54C3D8AD329E8F2DF00404AC7DD80E3CF7D6`, 16,182 bytes / 147 lines.
- Modified `by-class/LivingObjectPane.md`: SHA256 `013022614F924AE69BFD7419BBA5CBB46088A1AC5C55A44081A6FE7D68B304DC`, 125,725 bytes / 644 lines.
- Modified `by-class/ObjectStatusBlob.md`: SHA256 `B9A89DB15F633BB2F43C25F83680884135E9705905D24DCF18014EBCB23D3EC4`, 13,323 bytes / 191 lines.
- Modified only the target label/link text in `by-memory/-ignored.md`: SHA256 `A8111F2842AE6EA604D7E7452DFC3108E0FA9A384B6F166BAECF4BF85E502947`, 1,042,474 bytes / 5,103 lines.
- Modified this same report in place; final report metrics and SHA256 are computed only after reconciliation stabilizes.
- Supervisor-owned post-callback changes, read-only to B004: `by-memory/-coverage-report.md` now has SHA256 `6BFCF7632E4A1477673E6933A5F5747D431D8619779039066A1C212ED33725E4`; `by-class/-coverage-report.md` now has `FB3EAAB086260B1963DD61D7CAF72658A41047B37273069AF393316FA4B7858C`; `by-file/-coverage-report.md` now has `31E734F88D76EAF3939F77651578AC59B2D00C8273576D99A8716AA70419B37D`. B004 changed none of them.
- Verify-only ordinary pages remained unedited except validator-managed reverse-reference propagation from command `13502`; no scope contradiction was found.
- B004 manually edited no manual coverage, generated, tracker, audit, supervisor, validator-state, lifecycle, archive, queue/lock, or IDA file. Command `13515` performed only authorized validator-owned generated side effects.
- All ordinary leases were acquired one at a time and released immediately after validation. Current lease registry states `No active leases.`
- B004 did not run or probe `execute_report`, status/count, revalidation, move, archive, or any report lifecycle command.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor validation required before implementation.
- [x] Re-read target and every destination immediately before callback editing.
- [x] Preserve current complete shared MapPane/B001/B002/B004/B005 union.
- [x] Apply C3TH-001 through C3TH-046 claim by claim without compression.
- [x] UID-preservingly rename the target old path to `...MapPaneCreateHitBarObjectPane.md` through validator-aware one-move workflow.
- [x] Confirm old target path is absent and UID0003TH remains unique at the final path.
- [x] Apply target metadata `92/94`, owner/emitter `00007Q`, reconstructable true, blank position, `Nested:0`.
- [x] Apply exact target Item Summary.
- [x] Apply Destination 1 exact managed C++.
- [x] Preserve separate ordinary-new branches and exact unsafe/no-check behavior.
- [x] Apply Destination 2 complete MapPane managed block with no unrelated byte loss.
- [x] Preserve MapPane class `93/94`, file `92/92`, class position 0, 0x910 guard, and class-before-children order.
- [x] Synchronize by-file/MapPane target source/behavior/link detail without adding formal C++.
- [x] Synchronize UID0001AW target child row/link while preserving `86/90`, false, blank emitter/formal, and full child/raw union.
- [x] Synchronize UID0003TQ two exact calls/values/types/link while preserving `86/90` and blank formal body.
- [x] Synchronize HitBarObjectPane constructor/style/timer-liveness consumer detail without score/formal change.
- [x] Synchronize LivingObjectPane status/slot consumer detail without score/formal change.
- [x] Synchronize ObjectStatusBlob HumanObject/style consumer detail without score/formal change.
- [x] Synchronize only the UID0003TH label/link in the existing ignored-padding entry.
- [x] Preserve verify-only ObjectList/Timer/MapPoint/ObjectPane/GrafPort/setter/pool/vtable/sibling pages unless validator reverse references change them.
- [x] Preserve exact range/full-byte hash/CFG/callers/callees/vtable/timer/padding evidence.
- [x] Preserve positive, negative, historical, rejected-alternative, and source-placement evidence at report-level detail.
- [x] Preserve historical long-name/blank-C++/generic-offset/vslot assumptions as explicitly superseded, not current truth.
- [x] Confirm third-party import directive is not applicable.
- [x] Keep all draft C++ inside the two exact managed blocks.
- [x] Keep manual coverage text supervisor-owned, preserve it exactly, and record the completed supervisor application without B004 editing.
- [x] Keep report validation/execution/archive/count state external and archive-neutral.
- [x] Confirm zero ordinary/prohibited edits and zero leases during report-only phase.

Implementation callback pass:
- [x] Report accepted by supervisor for implementation.
- [x] Acquire only one short lease immediately before each ordinary edit.
- [x] Reread each shared destination after acquiring its lease and rebase onto current bytes.
- [x] Wait/work elsewhere rather than editing through any conflicting lease.
- [x] Edit, scoped-validate, and release each ordinary file before acquiring the next lease.
- [x] Validate final renamed UID0003TH path in default file mode so UID path/reverse references update.
- [x] Record each scoped validator command id, timestamp, exit, ok, warnings, and side effects.
- [x] Record each ordinary destination's final SHA256/bytes/lines.
- [x] Confirm exact formal equality for Destination 1.
- [x] Confirm exact complete formal equality for Destination 2.
- [x] Confirm no C++ was inserted into by-file, parent, caller, or prose-only support pages.
- [x] Confirm target score/metadata/path/name/Item Summary are exact.
- [x] Confirm parent/caller/support scores and unrelated content remain unchanged.
- [x] Confirm all current reverse references use final target path/name or are explicitly historical.
- [x] Run one authorized final target validator with `--wait-generated` after all ordinary edits.
- [x] Verify exactly one `MapPane::CreateHitBarObjectPane` declaration.
- [x] Verify exactly one `MapPane::CreateHitBarObjectPane` definition.
- [x] Verify zero `CreateHitBarObjectPaneEventOverlay` tokens.
- [x] Verify zero UID0003TH Empty Emitter Marker.
- [x] Verify exactly two ordinary `new HitBarObjectPane` expressions in the target body.
- [x] Verify exact HumanObject/style 1 versus other/style 0 branches.
- [x] Verify exact event-0/500 TimerManager scheduling, UpdatePosition, GetBounds, and InvalidateRect order.
- [x] Verify zero handwritten pool/EH/security-cookie/vptr/TimerHandler-offset/vtable-slot source.
- [x] Verify no duplicate target or sibling C++ and no unrelated generated regression.
- [x] Re-read manual by-memory/class/file coverage before handoff and again after supervisor application; refresh exact row/hash placement in this report.
- [x] Do not edit manual coverage; retain the exact supervisor-owned handoff text and verify current applied parity.
- [x] Update every C3TH ledger row to a legal terminal state with exact destination/validator proof.
- [x] Check every implementation item only when independently true; list any exact blocker rather than claiming completion.
- [x] Update Current Target State, Validator Results, Changed Files, generated proof, and lifecycle wording to archive-neutral callback truth.
- [x] Confirm every lease is released and B004 holds zero leases.
- [x] Confirm no generated/tracker/coverage/audit/supervisor/validator-state/lifecycle/IDA file was manually edited.
- [x] Confirm B004 did not run/probe execute_report, count/status, revalidation, move, archive, or any lifecycle command.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B004","command_id":"000000013521","destination_path":"executed-b-agent-research/B004/0003TH-MapPaneCreateHitBarObjectPaneEventOverlay-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B004/research/0003TH-MapPaneCreateHitBarObjectPaneEventOverlay-source-quality.md","timestamp":"2026-07-15T21:59:52-04:00","uid":"0003TH"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
