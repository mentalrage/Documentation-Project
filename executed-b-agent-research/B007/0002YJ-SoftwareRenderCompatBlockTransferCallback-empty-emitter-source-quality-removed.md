<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B007\0002YJ-SoftwareRenderCompatBlockTransferCallback-empty-emitter-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable/Transactional Material

Source report: [0002YJ-SoftwareRenderCompatBlockTransferCallback-empty-emitter-source-quality.md](0002YJ-SoftwareRenderCompatBlockTransferCallback-empty-emitter-source-quality.md)

Status: non-authoritative preservation archive.

Safety: this archive is non-authoritative and must never be executed, replayed, adapted, or treated as current MCP/IDA action authority. It is retained only to document what was removed from the source report during report-format cleanup.

## Removed Material

~~~text

Every request below is bound to the literal public MCP schema rechecked read-only on 2026-08-11, the fresh I01 endpoint behavior observed during the middle unsaved 2026-08-12 Gate 2B attempt, and the fresh I05/I06 comment-channel behavior observed during the latest unsaved retry. Sessions `supervisor-b009-uid00028d-rollback-verify-20260810`, `supervisor-b007-uid0002YJ-rollback-fresh-20260811`, `supervisor-uid0002YJ-rollback-20260812T2256Z`, and `d6ebc8b8` are retained only as dated prestate/rollback evidence and are not action authority. In I01-I06, symbolic value `<Gate2BDatabase>` means the exact current database identifier returned by the primary supervisor's fresh canonical-path open; it must be replaced with that literal identifier at action time and must never be sent verbatim or resolved from any dated session. The supervisor must repeat each complete prestate against that fresh database, and any mismatch blocks the action. Before each mutation and the sole save, require public `runtime_attestation` with that exact current database and `expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"`. Gate 2B owns backup, mutation, save, readback, and guarded rollback; this report supplies only exact item-local actions. All failed attempts were unsaved and receive no implementation credit.

The following current safety contract is normative for any supervisor Gate 2B use of I01-I07. It is structured handoff data, not executable automation. The dated snapshots below are evidence baselines only; the literal action-entry authority is the fresh readback obtained under this contract.

| Gate 2B boundary | Exact public request/action contract | Required proof and fail-closed result |
| --- | --- | --- |
| Closed canonical authority | Before backup or open, prove no active session resolves to canonical path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; retire only an exactly identified prior worker and prove its PID/start/path is absent without PID reuse; record canonical SHA256, bytes, timestamps, and a successful exclusive `FileShare.None` open. An inactive registry row is not authority. | Any active/ambiguous canonical worker, PID ambiguity, path mismatch, failed exclusive-open classification, or indeterminate disk identity stops before backup, open, read, mutation, or save. |
| Collision-safe durable backup | Under complete canonical quiescence, create a new non-overwriting UID0002YJ/timestamp-qualified backup under `E:\NTK\Resources\NexusTK\ida-backups\`; require the destination not to exist and physically reread source and backup. | Backup SHA256 and bytes must equal the closed canonical prestate exactly. Preserve it through final fresh readback. Collision, copy failure, or identity mismatch stops. The historical SHA `049D...F92B` is not reusable current backup authority. |
| Fresh canonical open | Call public `idb_open` exactly with `{"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":600,"preferred_session_id":""}`. | Require `success:true`; record the exact returned native `session.session_id` and normalized canonical path. Replace `<Gate2BDatabase>` everywhere with only that returned session and use it byte-for-byte as top-level `database` for every later call. |
| Public runtime binding | Call public `runtime_attestation` exactly with `{"expected_database":"<returned-native-session>","expected_canonical_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"}` immediately after open and again before each stateful I01-I06 operation, the sole save, verifier open, and rollback boundary. | Require schema version `1`, `ok:true`, no errors, exact database/path, listener nonce/generation/PID ownership, exactly one active canonical worker/session, exact worker PID/parent route, and complete loaded-module manifests with exact file, `spec_origin`, resolved path, source root, size, timestamps, and SHA256. Internal runtime/attest routes are not substitutes. |
| Fresh literal preflight | In the attested returned session, reread I01-I07 item/range/name/type/declaration/full-frame/comment/xref/byte/boundary prestates with the exact public endpoint shapes in those rows. Use bounded `entity_query(functions/names)` and `list_globals` as specified where `lookup_funcs` or `inspect_items` has a known projection/alias defect. | Every dated baseline field must match the fresh literal readback before its dependent action. Any mismatch, extra name/type/comment/frame row/xref, changed extent/byte, collision, or projection not already bounded by the row stops and requires report repair; historical baselines never override current state. |
| Serial actions and immediate readback | Apply only accepted I01-I06 in order. After each request, reread its complete changed entity plus every protected name/type/frame/comment/xref/byte/range field before continuing. I05 must end with regular absent and repeatable absent. I06 must write the exact repeatable text, immediately call `get_comments`, perform at most the exact-text regular-only cleanup authorized in I06, call `get_comments` again after cleanup, and prove final regular absent plus exact repeatable before any broader pre-save verification. I07 remains read-only. | Any no-op, unexpected normalization, extra delta, omitted receipt, repeatable mismatch, unrecognized regular text, cleanup failure, or protection failure stops before the next action and forbids save. The exact stale regular text is cleanup-eligible only after I06 and only while the requested repeatable text is exact. Partial success is not completion. |
| Complete pre-save verification and sole save | After I06 has independently proved final regular absent and exact repeatable text, rerun every I01-I07 expected readback and protected no-change field together in the same attested returned session. Recheck I06 regular/repeatable channels, type/name/extent/value/five refs/bytes/adjacent slots as part of this full pass. Only after all pass, call public `idb_save` exactly once with `{"database":"<returned-native-session>","path":""}`. | Any stale regular reappearance, repeatable mismatch, or protected-state delta fails closed before save. Never retry an absent, failed, or indeterminate save response. Record the raw result, one save-attempt count, and physical canonical SHA256/bytes/timestamps. A response without coherent session and disk evidence is failure. |
| Fresh persistence readback | Retire only the exact transaction worker; prove PID/start/path absence, no active canonical-path session, and exclusive-open state. Reopen with the exact `run_auto_analysis:false` request, bind the newly returned native session through public attestation, and repeat all I01-I07/protection readbacks without another save. | All final fields and persisted disk identity must agree. A stale inactive session row is non-authoritative; active/ambiguous routing, missing readback, or any persisted mismatch fails Gate 2B. |
| Guarded rollback/no-save | Before-save failure: retire the exact unsaved worker and fresh-open canonical to prove the original prestate; do not save. Definite/indeterminate post-save failure: classify exact canonical and durable-backup identities under complete quiescence before any restore. | Restore only for one unambiguous transaction-attributable bad canonical identity and one exact verified backup. Partial/mixed/both-pass/neither-pass/unknown/third-identity/active-worker/PID-ambiguous states stop without restore. After authorized closed-file restore, fresh-open/re-attest and prove the complete original I01-I07 prestate and disk identity; perform no save that could overwrite the restored bytes. |

| ID | Entity / exact extent | Literal action-entry prestate | Exact public endpoint/request | Evidence and protected state | Exact expected readback |
| --- | --- | --- | --- | --- | --- |
| I01 | Function `0x004bee40`, `[0x004bee40,0x004c069e)`, 6,238 bytes | Persisted name `sub_4BEE40`; persisted type `__m128i *__cdecl(__m128i *, unsigned int, int)`; frame rows exactly `__saved_registers` `_DWORD` `+0x4/4`, `__return_address` `_UNKNOWN *` `+0x8/4`, `arg_0` `_DWORD` `+0xc/4`, `arg_4` `_DWORD` `+0x10/4`, `arg_8` `_DWORD` `+0x14/4`; zero locals; address regular absent; address repeatable absent; function regular absent; function repeatable absent. Whole-IDB exact proposed-name `entity_query(functions)` returns zero rows. The bounded request `entity_query` with `database:"<Gate2BDatabase>"`, `queries:[{kind:"functions",regex:"^(SoftwareRenderCompatBlockTransferCallback|sub_4BEE40)$",min_addr:"0x004bee40",max_addr:"0x004bee41",offset:0,count:4,sort_by:"addr",descending:false,fields:["addr","name","size"]},{kind:"names",regex:"^(SoftwareRenderCompatBlockTransferCallback|sub_4BEE40)$",min_addr:"0x004bee40",max_addr:"0x004bee41",offset:0,count:4,sort_by:"addr",descending:false,fields:["addr","name"]}]` returns exactly one persisted old row from `functions` and zero rows from `names`. | First dry-run `rename` with `database:"<Gate2BDatabase>"`, `batch:{func:{addr:"0x004bee40",name:"SoftwareRenderCompatBlockTransferCallback"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true}`. `<Gate2BDatabase>` must be replaced by the literal identifier from the fresh canonical Gate 2B open. Only if that succeeds, apply the identical request with `dry_run:false`; all other fields remain identical. Then rerun the exact bounded `entity_query` request from prestate. | Two provider refs, two void consumers, exact RGB555 writes, and no name collision. Protect type, every frame row, zero-local count, extent, bytes, xrefs, CFG, comments, internal labels, switch table, and function-directory placement; `pure:true` is mandatory. `lookup_funcs("sub_4BEE40")` is explicitly non-authoritative because it can resolve the renamed address through an old address-shaped alias. Current endpoint behavior makes both bounded projections operative for immediate readback: `functions` proves function-directory replacement and `names` independently reflects the renamed function after apply. The prior expectation that post-rename `names` remains empty is dated failed-unsaved history only. | The exact bounded `entity_query` request returns exactly one `functions` row for `SoftwareRenderCompatBlockTransferCallback` at `0x004bee40`, exactly one `names` row for `SoftwareRenderCompatBlockTransferCallback` at `0x004bee40`, zero `sub_4BEE40` function rows, and zero old-name or extra proposed-name collision rows. A separate whole-IDB exact proposed-name `functions` query returns exactly the renamed row and no extra collision row. Old type and all five frame rows remain unchanged; zero locals; same extent/bytes/xrefs/CFG; all four comment channels remain absent. A `lookup_funcs("sub_4BEE40")` alias hit is recorded but neither accepted as an old-name row nor used to fail the rename. |
| I02 | Function `0x004bee40`, same exact extent and bytes | Required action-entry name `SoftwareRenderCompatBlockTransferCallback` from I01; type still `__m128i *__cdecl(__m128i *, unsigned int, int)`; current frame exactly `__saved_registers` `_DWORD` `+0x4/4`, `__return_address` `_UNKNOWN *` `+0x8/4`, `arg_0` `_DWORD` `+0xc/4`, `arg_4` `_DWORD` `+0x10/4`, `arg_8` `_DWORD` `+0x14/4`; zero locals; all four comments absent. | `set_type` with `database:"<Gate2BDatabase>"` (literal fresh canonical Gate 2B identifier) and `edits:{addr:"0x004bee40",kind:"function",signature:"void __cdecl SoftwareRenderCompatBlockTransferCallback(unsigned short *pixels, unsigned int pixelCount, int fadeStep)"}`. This request deliberately preserves the human source spelling `unsigned short *`. | Exact ABI follows both consumers/providers and 16-bit mutation. Permitted frame deltas are only three argument name/type changes: `arg_0` `_DWORD` -> `pixels` endpoint-normalized `unsigned __int16 *` at `+0xc/4`; `arg_4` `_DWORD` -> `pixelCount` `unsigned int` at `+0x10/4`; `arg_8` `_DWORD` -> `fadeStep` `int` at `+0x14/4`. Added rows 0; removed rows 0; argument count stays 3. Protect saved-register and return-address rows, zero locals, extent, bytes, xrefs, CFG, labels, and comments. | IDA endpoint-normalized signature exactly `void __cdecl SoftwareRenderCompatBlockTransferCallback(unsigned __int16 *pixels, unsigned int pixelCount, int fadeStep)`; frame exactly `__saved_registers` `_DWORD` `+0x4/4`, `__return_address` `_UNKNOWN *` `+0x8/4`, `pixels` `unsigned __int16 *` `+0xc/4`, `pixelCount` `unsigned int` `+0x10/4`, `fadeStep` `int` `+0x14/4`; zero locals; no other delta. The formal human C++ remains `unsigned short *`. |
| I03 | Function address `0x004bee40`, same exact function | Required action-entry name/type/frame are the exact I02 post-state; address regular absent; address repeatable absent; function regular absent; function repeatable absent. | `set_address_repeatable_comments` with `database:"<Gate2BDatabase>"` (literal fresh canonical Gate 2B identifier) and `items:{addr:"0x004bee40",comment:"RGB555 in-place ScreenPane fade transfer: clamps fadeStep to 0..24, preserves step 0, applies exact packed attenuation to pixelCount pixels, and clears at step 24."}`. | The text records exact semantics without inventing source names beyond the accepted ABI. Protect the I02 name/type/frame, address regular channel, both function comment channels, extent, bytes, xrefs, CFG, internal labels, and switch table. | Address repeatable comment exactly equals the proposed text; address regular remains absent; function regular/repeatable remain absent; exact I02 name/type/frame and all protected binary state remain unchanged. |
| I04 | Type catalog name `SurfaceTransferPixelsProc`; 32-bit pointer ABI | `type_query` for exact name and wildcard `*Transfer*Pixels*` each returns total 0; no existing declaration, ordinal, or collision. | Exact fail-closed request: `declare_type` with `database:"<Gate2BDatabase>"` and `decls:["typedef void (__cdecl *SurfaceTransferPixelsProc)(unsigned short *pixels, unsigned int pixelCount, int fadeStep);"]`. `decls` must be this one-element array; the scalar form is forbidden because it comma-split the declaration during the failed unsaved attempt. | Matches both providers and both consumers. Do not replace or rename any existing type, create a UDT, or model the callback table as one source object. Reject the action if the endpoint does not report one complete declaration or if any comma-separated fragment appears as a separate declaration. | Exactly one typedef named `SurfaceTransferPixelsProc`; human declaration exactly `typedef void (__cdecl *SurfaceTransferPixelsProc)(unsigned short *pixels, unsigned int pixelCount, int fadeStep);`; size exactly 4; no fragment declarations and no unrelated catalog delta. Endpoint-normalized `unsigned __int16 *` rendering is acceptable only as readback of the same 16-bit pointer type. |
| I05 | Slot 11 bytes `0x0069b40c-0x0069b40f`; expected source object width 4 | `list_globals` and the exact `entity_query` request `database:"<Gate2BDatabase>"`, `queries:[{kind:"names",regex:"^g_pfnTransferPixels$",min_addr:"0x0069b40c",max_addr:"0x0069b410",offset:0,count:2,sort_by:"addr",descending:false,fields:["addr","name"]},{kind:"globals",regex:"^g_pfnTransferPixels$",min_addr:"0x0069b40c",max_addr:"0x0069b410",offset:0,count:2,sort_by:"addr",descending:false,fields:["addr","name","size"]}]` each return unique name `g_pfnTransferPixels` at `0x0069b40c`; the global projection reports size 1. `inspect_items` has a known projection defect: it reports blank names and four separate undefined one-byte heads `[0x69b40c,0x69b40d)`, `[0x69b40d,0x69b40e)`, `[0x69b40e,0x69b40f)`, `[0x69b40f,0x69b410)`; only the first head carries type `int (__cdecl *)(_DWORD, _DWORD, _DWORD)`. All four bytes are zero; head has five xrefs; tail bytes have zero xrefs; regular and repeatable address comments at the head are absent; no tail name/type/comment evidence exists. I04 typedef must already exist. | Call `set_type` with `database:"<Gate2BDatabase>"` and `edits:{addr:"0x0069b40c",kind:"global",ty:"SurfaceTransferPixelsProc"}`; no rename is permitted. Immediately call `get_comments` with `database:"<Gate2BDatabase>"`, `addrs:["0x0069b40c"]`. Both regular and repeatable must be absent. Any non-absent comment or protected-state delta rejects Gate 2B and requires no-save rollback; I05 authorizes no comment cleanup. | The exact `entity_query` request and independent `list_globals` are authoritative; `inspect_items` remains non-authoritative for the name before and after typing. The operation may replace exactly the four undefined byte heads with one four-byte data item and no other range. The latest unsaved retry proves I05 itself leaves both comment channels absent; the stale regular side effect is modeled and conditionally cleaned only in I06. Protect authoritative name `g_pfnTransferPixels`, five head xrefs, zero tail xrefs, four zero bytes, adjacent slots, comments, and every address outside `[0x0069b40c,0x0069b410)`. Stop if any tail acquires an independent name, type, comment, xref, or nonzero byte before apply. | `inspect_items` shows one item headed at `0x0069b40c`, exact extent `[0x0069b40c,0x0069b410)`, width 4, blank projected item name, type `SurfaceTransferPixelsProc`, value zero, five xrefs all to the head, and no independent tail heads. Independent `list_globals` plus the exact bounded `entity_query` request remain authoritative and each resolve `g_pfnTransferPixels` at the head. Immediate `get_comments` reports regular absent and repeatable absent; adjacent slots, bytes, and every other protected field remain unchanged. |
| I06 | Typed global `0x0069b40c`, exact post-I05 extent `[0x0069b40c,0x0069b410)` | Required action-entry state is the exact I05 post-state: `inspect_items` has one four-byte typed item with blank projected name; independent `list_globals`/`entity_query` resolve authoritative name `g_pfnTransferPixels`; type `SurfaceTransferPixelsProc`; value zero; five head xrefs; regular absent; repeatable absent. | First call `set_address_repeatable_comments` with `database:"<Gate2BDatabase>"` and `items:{addr:"0x0069b40c",comment:"Surface fade-transfer callback; receives a mutable 16-bit pixel buffer, pixel count, and fade step; initialized with RGB555 or RGB565 providers and called by ScreenPane presentation paths."}`. Immediately call `get_comments` with `database:"<Gate2BDatabase>"`, `addrs:["0x0069b40c"]`. The repeatable channel must exactly equal the requested I06 text. If regular is absent, continue to complete protected-state readback. If and only if regular exactly equals `Slot 11 g_pfnTransferPixels: pixel buffer/count/mode block transform; callers ignore the physical return. Compat 0x4BEE40, RGB565 0x4C4790.` while repeatable exactly equals the requested I06 text, call `set_address_comments` with `database:"<Gate2BDatabase>"`, `items:{addr:"0x0069b40c",comment:""}`, then repeat `get_comments` before any further action. Any other regular text, repeatable mismatch, cleanup failure, or protected-state delta rejects Gate 2B and requires no-save rollback. | Protect authoritative name, type, extent, value, five head xrefs, zero tail xrefs, adjacent slots, all bytes, and the known blank-name `inspect_items` projection. The regular-only cleanup is narrowly authorized at I06 solely for the exact historical text and solely when the repeatable channel already equals the requested text; it must not alter the repeatable channel or any protected field. The latest failed-unsaved retry proves this exact side effect occurs after I06, not I05. | Final `get_comments` reports regular absent and repeatable exactly `Surface fade-transfer callback; receives a mutable 16-bit pixel buffer, pixel count, and fade step; initialized with RGB555 or RGB565 providers and called by ScreenPane presentation paths.` `inspect_items` may still omit the item name but must retain exact type `SurfaceTransferPixelsProc`, extent `[0x0069b40c,0x0069b410)`, width 4, value zero, five xrefs all to the head, zero independent tail heads, and unchanged four zero bytes. Independent `list_globals` and bounded `entity_query` still return exact authoritative name `g_pfnTransferPixels`; adjacent slots and every other protected field remain unchanged. |
| I07 | Jump-table item `0x004c06a0`, `[0x004c06a0,0x004c0704)`, 100 bytes | Name `jpt_4BEE6D`; data item; no type; regular comment exactly `jump table for switch statement`; repeatable absent; one xref from `0x004bee6d`. | No mutation. Read with `inspect_items`, `get_comments`, and `xrefs_to` using the exact Gate 2B database and compare to this prestate. | Compiler-generated local switch table is already modeled correctly. Do not declare a source table, type it, rename it, or rename case labels. | Exact name, item kind, extent, bytes, regular comment, absent repeatable comment, and sole xref remain unchanged. |

- The `g_pfnTransferPixels` name discrepancy is resolved, not deferred: independent `list_globals`/`entity_query` endpoints agree on the name, while `inspect_items` omits it both before and after typing. The physical four-byte modeling defect remains an explicit I05 operation with an exact replaceable range, fail-closed tail protections, and a narrowly bounded regular-comment cleanup that preserves the separate accepted repeatable channel.
- The failed unsaved attempt is retained as negative operational evidence: scalar I04 input, `lookup_funcs` old-label lookup, source-spelled I02 readback, and the original I05 comment promise were all disproved. Fresh Gate 2B must use the array declaration, bounded name-authority queries, endpoint-normalized type readback, and the exact I05 cleanup/readback contract above.
- Constant entities `kRgb555FadeDelta1Words` through `kRgb555FadeDelta30Words` are already normalized by UID0002V6 and are dependencies, not actions in this report.
- Negative constraints: do not change the RGB565 peer, internal case labels, jump-table item, alignment items, constant-pool items, ScreenPane fields, adjacent callback slots, or any function boundary.
~~~
