** TARGET-REPORT-UID:0001N5 **
** AUTHOR-AGENT-ID:B014 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 0001N5 UserPaneDoubleParamCallback Source-Quality Research

## Finalized Report / Current Recommendation

- Current recommendation: keep [UID:0001N5] `UserPaneDoubleParamCallback` attached to [UID:000040] `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>` and emitted through the existing `FunctionObjects` route, raise the target from `84/90` to `87/91`, replace stale 95/95 no-code wording, and document that the formal target C++ must stay generated-template proof, not a handwritten standalone invoke body.
- Final disposition: source-declared/generated-binary callback-template invoke wrapper. The rebuilt project needs the shared `FunctionObjects` template declaration and the feature construction sites; the exact `0x005b77b0` body should be regenerated by the compiler from that template.
- Required action: supervisor should send an implementation callback for target/support doc updates. Do not edit `by-memory/-coverage-report.md` directly; exact replacement row text is included below.
- Confidence: high for exact bytes, vtable slot, construction helper relation, field layout, bound `UserPane` member targets, and owner/emitter route. Not final-audit because the original template header basename and exact template spelling remain inferred.

Recommended target metadata after implementation:

```text
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000040 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000040 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
```

## Target

- Target UID: `0001N5`
- Target path: `source-3/project-documentation/by-memory/0x005b77b0-0x005b77c8.UserPaneDoubleParamCallback.md`
- Required report path: `source-3/project-documentation/tools/leaser/Agents/Agent-B014/research/0001N5-UserPaneDoubleParamCallback-source-quality.md`
- Current scores and route: `84/90`, `CANONICAL_OWNER:000040`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000040`.
- Current generated route: `auto-generated/-ag-memory-coverage.md` lists UID `0001N5` as emitting through `000040` to `auto-generated/NexusTK/util/FunctionObjects.cpp`, with `Coded` currently `no`.
- Current generated output: `auto-generated/NexusTK/util/FunctionObjects.cpp` has an empty marker for UID `0001N5`.

## Evidence Checked

Documentation evidence:

- Target page `by-memory/0x005b77b0-0x005b77c8.UserPaneDoubleParamCallback.md`
- Construction helper [UID:0002VM] `by-memory/0x005b7450-0x005b74dd.CreateUserPaneDoubleParamCallback.md`
- Concrete wrapper class [UID:000040] `by-class/DoubleParamMemberFunctionObject0_void____thiscall_UserPane_____signed_char__h43ff6c8e0528.md`
- Vtable data [UID:0002YW] `by-memory/0x00630a78-0x00630a90.UserPaneDoubleParamCallbackVtableData.md`
- Source family [UID:0000JO] `by-file/FunctionObjects.md`
- Template family [UID:0001WQ] `by-type/by-template/FunctionObjectTemplates.md`
- Callback dispatch topic [UID:0001Q9] `by-meta/client_callback_dispatch.md`
- `UserPane` support [UID:0000P1] and [UID:0000FQ]
- `ItemActionInputPanes` support [UID:0000KC] and aggregate [UID:0001MG]
- `ConfirmInputPane` support [UID:000035] and constructor [UID:0001N4]
- `g_pCollectionData` [UID:0000QK] and `g_packetSender` [UID:0000Q5] support
- Sibling callback-template docs for PopupMenu, MixItemDialog, MusicControlDialog, and Application callback invoke wrappers
- `by-memory/-coverage-report.md`, `project-level/-auto-completion-stats.md`, and `auto-generated/-ag-memory-coverage.md`

Direct local PE evidence:

- Executable: `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`
- MD5: `4247e04e20b65d6414c7238aa8ff5515`
- SHA256: `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`
- Image base: `0x00400000`
- Local Capstone disassembly and pointer scans were run over the target, construction helper, four construction caller windows, vtable data, and the two bound member callback targets.

IDA/MCP availability:

- Live MCP check to `http://127.0.0.1:13337/mcp` failed with `Unable to connect to the remote server`.
- This report therefore uses the current IDA-backed docs plus direct PE byte/disassembly/pointer scans. IDA MCP validation needs are listed below.

## Executive Recommendation

Accepted source model:

- Source family: `NexusTK/util/FunctionObjects.h` with optional `FunctionObjects.cpp` support.
- Concrete wrapper doc/name: `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>`.
- Full source-facing type direction:

```cpp
DoubleParamMemberFunctionObject0<
    void (UserPane::*)(signed char, bool),
    UserPane,
    signed char,
    bool>
```

The exact compiler-decorated vtable spelling at `0x00630a78` confirms this shape:

```text
??_7?$DoubleParamMemberFunctionObject0@P8UserPane@@AEXC_N@ZV1@C_N@@6B@
```

The target range itself should not emit a handwritten C++ invoke body such as a standalone `sub_5B77B0` translation. It is compiler-emitted template glue. If the implementation pass needs nonblank emitted text so validator/autogen does not treat the special case as missing code, use a comment-only proof marker in `RECONSTRUCTION_CPP`, not a function body:

```cpp
// Compiler-emitted invoke wrapper for DoubleParamMemberFunctionObject0<UserPane, signed char, bool>;
// generated from the shared FunctionObjects template declaration.
```

The human source that should ultimately generate this range belongs in the shared template declaration, conceptually:

```cpp
template <class TObject, class TArg0, class TArg1>
class DoubleParamMemberFunctionObject0 : public FunctionObject0 {
public:
    typedef void (TObject::*MemberCallback)(TArg0, TArg1);

    DoubleParamMemberFunctionObject0(MemberCallback callback,
                                     TObject* object,
                                     TArg0 arg0,
                                     TArg1 arg1)
        : m_callback(callback),
          m_object(object),
          m_arg0(arg0),
          m_arg1(arg1)
    {
    }

    virtual void Invoke()
    {
        (m_object->*m_callback)(m_arg0, m_arg1);
    }

private:
    MemberCallback m_callback;
    TObject* m_object;
    TArg0 m_arg0;
    TArg1 m_arg1;
};
```

That template draft is for [UID:0001WQ] / [UID:0000JO] support, not a target-level function body for [UID:0001N5].

## Heuristic / Inference Reanalysis And Validation

### 1. Generated names and source-facing names

Raw names investigated:

- `sub_5B77B0`: exact invoke wrapper at target range.
- Vtable `0x00630a84`: data slot pointing to `0x005b77b0`.
- Long class doc name `DoubleParamMemberFunctionObject0_void____thiscall_UserPane_____signed_char__h43ff6c8e0528`.
- Member callback pointer immediates `0x005ad7a0` and `0x005ad720`.

Best source-quality names:

| Raw item | Recommended source-facing name | Confidence |
| --- | --- | --- |
| `sub_5B77B0` | `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>::Invoke` or `UserPaneDoubleParamCallback_Invoke` as an IDA label | high descriptive |
| `0x00630a78` vtable | `DoubleParamMemberFunctionObject0_UserPane_signed_char_bool_vftable` | high descriptive |
| `0x005b7450` | `CreateUserPaneDoubleParamCallback` / `CreateUserPaneDropGiveCallback` | high descriptive |
| `0x005ad7a0` | `UserPane::SendDropItemPacket(signed char slot, bool allItems)` or conservative `UserPane::SendConfirmedDropItemPacket` | high behavior, medium original name |
| `0x005ad720` | `UserPane::SendGiveItemPacket(signed char slot, bool allItems)` or conservative `UserPane::SendConfirmedGiveItemPacket` | high behavior, medium original name |

Rejected names:

- Do not use `sub_5B77B0` or address names in final source docs except as historical IDA aliases.
- Do not promote the long generated class name as an original source file or handwritten class name.
- Do not name the target after `DropInputPane`/`GiveInputPane`; those classes construct the object, but the reusable callback template owns the wrapper.
- Do not call `0x005ad720` / `0x005ad7a0` raw free functions. They are passed as `UserPane` member callbacks even though their current bodies do not use `this`.

### 2. Object layout and captured fields

The paired construction helper [UID:0002VM] writes a 24-byte object. The invoke wrapper reads the same fields.

| Offset | Binary field | Source-facing role | Evidence |
| --- | --- | --- | --- |
| `+0x00` | vptr `0x00630a78` | concrete callback vtable | construction store at `0x005b7495` |
| `+0x04` | base/reserved field | inherited `FunctionObject0`/`LObject` support | initialized by base helper `0x004f4a80`, not touched by invoke |
| `+0x08` | function pointer | member callback code pointer | invoke loads into `eax` and calls it |
| `+0x0c` | this adjustment | pointer-to-member adjustment dword | added to target pointer before call |
| `+0x10` | target object pointer | bound `UserPane*`, observed as `g_pCollectionData` | invoke moves into `ecx`, callers pass `0x0067a748` contents |
| `+0x14` | captured byte | `signed char slot` | member targets sign-extend `[ebp+8]` |
| `+0x15` | captured byte | `bool allItems` / all-vs-single flag | member targets zero-extend `[ebp+0x0c]` |
| `+0x16-+0x17` | tail padding | alignment/unused | 24-byte object size slot returns `0x18` |

The source-level pointer-to-member is represented in the binary as a function pointer plus this-adjust dword. That is expected MSVC callback-template layout and should not be exposed as separate public fields in final human C++ unless the project standardizes this low-level structure.

### 3. Invoke wrapper behavior

Direct Capstone disassembly of `0x005b77b0-0x005b77c8`:

```asm
005b77b0  mov     edx, ecx
005b77b2  movzx   eax, byte ptr [edx + 0x15]
005b77b6  mov     ecx, dword ptr [edx + 0x10]
005b77b9  add     ecx, dword ptr [edx + 0x0c]
005b77bc  push    eax
005b77bd  movzx   eax, byte ptr [edx + 0x14]
005b77c1  push    eax
005b77c2  mov     eax, dword ptr [edx + 0x08]
005b77c5  call    eax
005b77c7  ret
```

Source behavior:

- `ecx` is the callback object.
- Adjusted target `this` becomes `*(this+0x10) + *(this+0x0c)`.
- The wrapper pushes `bool allItems` then `slot`, so the target member receives `(signed char slot, bool allItems)` under right-to-left x86 argument order.
- The wrapper has no runtime parameters and returns with plain `ret`, matching the `FunctionObject0` family.
- The indirect member call means `callees` is empty and direct code callers are absent; reachability is through vtable dispatch.

### 4. Bound `UserPane` target methods

Direct local disassembly resolves the two member callback targets that the construction helper stores.

`0x005ad7a0-0x005ad811`: drop callback target

- Writes opcode `0x08`.
- Writes signed slot byte from `[ebp+8]`.
- Writes bool/all flag from `[ebp+0x0c]`.
- Zero-terminates the local packet scratch byte at the fourth byte.
- Sends exactly `3` bytes through `g_packetSender` / `0x00574bb0`.
- Returns with `ret 8`.

`0x005ad720-0x005ad791`: give callback target

- Writes opcode `0x29`.
- Writes signed slot byte from `[ebp+8]`.
- Writes bool/all flag from `[ebp+0x0c]`.
- Zero-terminates the local packet scratch byte at the fourth byte.
- Sends exactly `3` bytes through `g_packetSender` / `0x00574bb0`.
- Returns with `ret 8`.

Best source-facing signatures:

```cpp
void UserPane::SendDropItemPacket(signed char slot, bool allItems);
void UserPane::SendGiveItemPacket(signed char slot, bool allItems);
```

Because these are reached through `ConfirmInputPane` yes/confirm callbacks, the more explicit descriptive names `SendConfirmedDropItemPacket` and `SendConfirmedGiveItemPacket` are also defensible if the project wants to distinguish them from direct raw item-action helpers. The opcode/body behavior is stronger than the exact original method name.

Rejected alternatives:

- `DropAllInputPane` or `GiveInputPane` method ownership is rejected because the callback helper passes `g_pCollectionData` as the target object and stores a `UserPane` member pointer.
- Static free-function ownership is rejected because the source type is a `UserPane` member-function pointer even though the emitted bodies do not dereference `ecx`.
- `CollectionPane` ownership is rejected because `g_pCollectionData` is a broad `UserPane`/local-player state pointer, not collection-specific state.

### 5. Construction helper and caller context

The construction helper [UID:0002VM] is called exactly from four item-action prompt paths:

| Caller | Member target | Captured flag | Meaning |
| --- | --- | --- | --- |
| `0x005b46d6` in `DropAllInputPane` | `0x005ad7a0` drop packet | `1` | drop all from slot |
| `0x005b4a05` in `DropInputPane` | `0x005ad7a0` drop packet | `0` | drop one from slot |
| `0x005b4ec0` in `GiveAllInputPane` | `0x005ad720` give packet | `1` | give all from slot |
| `0x005b5198` in `GiveInputPane` | `0x005ad720` give packet | `0` | give one from slot |

Each caller:

1. Allocates a `ConfirmInputPane` object.
2. Pushes the all/single flag.
3. Pushes the captured slot byte.
4. Pushes `g_pCollectionData` / `dword_67A748` as the `UserPane` target.
5. Pushes this-adjust `0`.
6. Pushes the selected member target pointer.
7. Calls `CreateUserPaneDoubleParamCallback`.
8. Passes the returned callback object into `ConfirmInputPane::ConfirmInputPane`.

Drop callers use prompt literal [UID:0003J4] at `0x00630a90`. Give callers ask `LanguageMan` for prompt id `0xdf` before constructing `ConfirmInputPane`.

### 6. Vtable slot context

Direct PE dword read around `0x00630a78`:

| Address | Dword | Meaning |
| --- | --- | --- |
| `0x00630a74` | `0x00653a44` | RTTI complete-object locator immediately before vtable |
| `0x00630a78` | `0x004671f0` | `FunctionObject0` scalar deleting destructor variant for 24-byte objects |
| `0x00630a7c` | `0x004f4b10` | inherited `LObject` runtime/type virtual |
| `0x00630a80` | `0x0041b6c0` | inherited/default no-op virtual |
| `0x00630a84` | `0x005b77b0` | this target invoke wrapper |
| `0x00630a88` | `0x004673f0` | object-size virtual returning `0x18` |
| `0x00630a8c` | `0x00000000` | null terminator/alignment before next wide string |
| `0x00630a90` | wide string | `Are you sure you want to drop this item? (Y/N)` starts here |

This vtable context strengthens the `FunctionObject0`-family classification. It also proves that `0x005b77b0` is not a normal feature helper reached by direct calls.

### 7. Reachability and negative evidence

Direct PE scan results:

- No rel32 call/jump targets to `0x005b77b0`.
- One absolute pointer to `0x005b77b0`: `.rdata` at `0x00630a84`.
- No rel32 call/jump targets to `0x005ad720` or `0x005ad7a0`.
- Absolute pointer/immediate hits for `0x005ad720` occur only in give construction callers at `0x005b4ebc` and `0x005b5194`.
- Absolute pointer/immediate hits for `0x005ad7a0` occur only in drop construction callers at `0x005b46d2` and `0x005b4a01`.
- `0x005b7450` construction helper has four rel32 call hits at `0x005b46d6`, `0x005b4a05`, `0x005b4ec0`, and `0x005b5198`.

Interpretation:

- Lack of direct callers to the invoke wrapper is expected and positive evidence for vtable-dispatched callback-template support.
- The member target pointer immediates prove the drop/give callback target relation, not ordinary direct reachability.
- No split or merge is needed for target [UID:0001N5].

### 8. Range and split/merge analysis

Target range is exact:

- `0x005b77b0-0x005b77c8`: 24-byte invoke body.
- `0x005b77a0-0x005b77a5`: preceding small jump thunk, outside target.
- `0x005b77a5-0x005b77b0`: `0xcc` padding, outside target.
- `0x005b77c8`: next function start, shared input-pane adjustor thunks [UID:0001N6].

No split is needed inside [UID:0001N5]. Do not merge it into [UID:0001N6]; the latter is compiler-generated destructor adjustor glue for input panes and has different ownership/rebuild handling.

Recommended future split work outside this assignment:

- Create exact by-memory child for `0x005ad720-0x005ad791` as `UserPane::SendGiveItemPacket` / confirmed give packet callback target.
- Create exact by-memory child for `0x005ad7a0-0x005ad811` as `UserPane::SendDropItemPacket` / confirmed drop packet callback target.

These future children should likely route through [UID:0000FQ] `UserPane` / [UID:0000P1] `UserPane.cpp`, not through `FunctionObjects`.

### 9. Generated-output policy and C++ readiness

Current target numerically clears the active combined-score/emitter gate:

- Current average: `(84 + 90) / 2 = 87`.
- Recommended average: `(87 + 91) / 2 = 89`.
- Emitter route reaches `auto-generated/NexusTK/util/FunctionObjects.cpp`.

However, the source-placement and generated-template policy hard-blocks a handwritten target-level function body:

- The target is a compiler-emitted virtual `Invoke` body generated from a reusable template.
- Writing a concrete standalone method body for this long instantiation would preserve compiler output rather than original mid-2000s human C++.
- The actual source belongs in [UID:0001WQ] / [UID:0000JO] template declarations and in feature construction sites.

Recommendation:

- Keep `RECONSTRUCTABLE:TRUE` and `EMITTER_UIDS:000040`.
- Do not emit a handwritten invoke body for [UID:0001N5].
- Replace stale 95/95 language with generated-template policy language.
- If the supervisor wants this emitting special case to stop appearing as missing code, use the comment-only proof marker shown in the Executive Recommendation.

## IDA MCP Facts To Revalidate

When MCP is available, validate:

- `lookup_funcs 0x005b77b0` returns size `0x18`, end-exclusive `0x005b77c8`.
- `xrefs_to 0x005b77b0` returns only vtable/data reference `0x00630a84`.
- `callers 0x005b77b0` and `callees 0x005b77b0` are empty or no direct code callers/callees.
- `xrefs_to 0x00630a78` includes constructor helper store `0x005b7495`.
- `xrefs_to 0x005b7450` / `callers 0x005b7450` returns exactly the four drop/give construction calls.
- `lookup_funcs 0x005ad720` and `0x005ad7a0` confirm exact starts/ends or, if IDA still lacks names, disassembly confirms the packet helper bodies and padding ends.
- `xrefs_to 0x005ad720` and `0x005ad7a0` show only the construction-site immediate/pointer uses listed above.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x005b77b0-0x005b77c8` | `0001N5` target | compiler-emitted `Invoke` wrapper | `TRUE` | `000040` | recommend `87/91` | exact target, no split |
| `0x005b7450-0x005b74dd` | `0002VM` | callback object construction helper | `TRUE` | `000040` | recommend support refresh to `86/91` | paired helper |
| `0x00630a78-0x00630a90` | `0002YW` | vtable data | `TRUE` | `000040` | keep `88/91` or raise only after full slot update | paired vtable |
| `0x005ad720-0x005ad791` | no exact child yet | `UserPane` give packet callback target | likely `TRUE` | likely `0000FQ` | n/a | future child split |
| `0x005ad7a0-0x005ad811` | no exact child yet | `UserPane` drop packet callback target | likely `TRUE` | likely `0000FQ` | n/a | future child split |
| `0x005b77c8-0x005b77de` | `0001N6` | shared input-pane destructor adjustor thunks | `FALSE` | `NONE` | `88/92` | ignored compiler glue |

## Ranked Ownership Analysis

### 1. [UID:000040] concrete `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>` wrapper

Evidence for:

- Target reads the exact 24-byte callback object layout built by [UID:0002VM].
- Vtable slot `0x00630a84` points to target `0x005b77b0`.
- Vtable data [UID:0002YW] is the concrete decorated `DoubleParamMemberFunctionObject0<UserPane,...>` vtable.
- The wrapper class already clears `85/89` and routes upward to [UID:0000JO].

Evidence against:

- Long class page name is generated and should not be treated as original source spelling.

Decision: accepted direct semantic owner.

### 2. [UID:0000JO] FunctionObjects source family

Evidence for:

- Proposed source tree places `FunctionObjects.cpp` / header-heavy callback declarations under `NexusTK/util/`.
- Sibling callback wrappers use this file as the shared utility/template source family.
- The template declaration, not feature source, should generate the wrapper.

Evidence against:

- By-structure direct owner for a concrete template instantiation child is the class/type page, not the file root.

Decision: accepted source route through [UID:000040], not direct target owner.

### 3. [UID:0000P1] / [UID:0000FQ] UserPane

Evidence for:

- Bound object pointer is `g_pCollectionData`, the `UserPane`/local-player singleton view.
- Bound member targets are `UserPane` packet helpers.

Evidence against:

- Target range is not a `UserPane` method; it operates on a callback wrapper object and is reached through callback vtable dispatch.

Decision: rejected as direct owner for [UID:0001N5], accepted owner direction for future exact child pages at `0x005ad720` and `0x005ad7a0`.

### 4. [UID:0000KC] ItemActionInputPanes

Evidence for:

- Drop/Give panes construct the callback object.
- Caller windows identify all/single flag meanings.

Evidence against:

- The constructed callback type and invoke body are reusable FunctionObjects support.
- Feature panes own construction call sites and prompt logic, not the invoke wrapper.

Decision: rejected as direct owner; accepted as caller/construction context.

## Recommended Exact Target Doc Changes

For `by-memory/0x005b77b0-0x005b77c8.UserPaneDoubleParamCallback.md`:

1. Change metadata to `COMPLETION:87`, `CONFIDENCE:91`; keep owner/emitter/reconstructable fields unchanged.

2. Replace stale status language:

```text
Final C++ stays blank because the shared template declaration and source names remain below the 95/95 final-source gate.
```

with:

```text
Formal handwritten C++ for this exact memory page should not be emitted: the range is compiler-emitted `Invoke` glue generated from the shared `FunctionObjects` template declaration. The active combined-score/emitter gate is satisfied, but source-placement policy requires the human source to live in the reusable template declaration and feature construction sites. If a nonblank generated marker is needed, use a comment-only generated-binary proof marker rather than a decompiler-shaped function body.
```

3. Replace or expand the behavior paragraph with:

```text
The invoke wrapper reads the 24-byte callback object created by [UID:0002VM]. It computes the adjusted `UserPane` target as `*(this + 0x10) + *(this + 0x0c)`, loads the member callback pointer stored at `+0x08`, and calls it with captured `(signed char slot, bool allItems)` from `+0x14/+0x15`. The wrapper itself zero-extends both captured bytes before pushing them; the bound `UserPane` packet targets sign-extend the slot and zero-extend the bool flag, confirming the source-level `(signed char, bool)` signature.
```

4. Add a source-quality names section:

```text
Recommended source-facing type name: `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>` / `DoubleParamMemberFunctionObject0<void (UserPane::*)(signed char, bool), UserPane, signed char, bool>`. Treat `DoubleParamMemberFunctionObject0_void____thiscall_UserPane_____signed_char__h43ff6c8e0528` as a generated documentation slug, not an original source name.
```

5. Add the bound target table:

```text
| Target | Best source-facing name | Packet behavior |
| --- | --- | --- |
| `0x005ad7a0-0x005ad811` | `UserPane::SendDropItemPacket(signed char slot, bool allItems)` / conservative `SendConfirmedDropItemPacket` | opcode `0x08`, slot, all/single flag, sends length `3` through `g_packetSender` |
| `0x005ad720-0x005ad791` | `UserPane::SendGiveItemPacket(signed char slot, bool allItems)` / conservative `SendConfirmedGiveItemPacket` | opcode `0x29`, slot, all/single flag, sends length `3` through `g_packetSender` |
```

6. Add full vtable context:

```text
`0x00630a78` slots are `0x004671f0` 24-byte FunctionObject0 scalar deleting destructor, inherited `0x004f4b10`, inherited no-op `0x0041b6c0`, invoke slot `0x005b77b0`, object-size slot `0x004673f0` returning `0x18`, then null/alignment at `0x00630a8c` before the drop-confirmation string at `0x00630a90`.
```

7. Optional formal reconstruction block if the supervisor chooses comment-marker policy:

```cpp
// Compiler-emitted invoke wrapper for DoubleParamMemberFunctionObject0<UserPane, signed char, bool>;
// generated from the shared FunctionObjects template declaration.
```

Do not add a standalone C++ function body for `0x005b77b0`.

## Recommended Support Doc Changes

`by-class/DoubleParamMemberFunctionObject0_void____thiscall_UserPane_____signed_char__h43ff6c8e0528.md`:

- Raise from `85/89` to approximately `87/90`.
- Replace the stale 95/95 no-code reason with generated-template policy.
- Add friendly display alias `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>`.
- Add field layout table from this report.
- Add member target names and exact packet behavior for `0x005ad7a0` / `0x005ad720`.

`by-memory/0x005b7450-0x005b74dd.CreateUserPaneDoubleParamCallback.md`:

- Raise from `84/90` to approximately `86/91` after incorporating source-quality names.
- Replace `sub_5AD7A0` and `sub_5AD720` with the recommended `UserPane::SendDropItemPacket` / `UserPane::SendGiveItemPacket` descriptive names, preserving raw aliases in parentheses.
- Replace `dword_67A748` primary wording with `g_pCollectionData` / `UserPane*`, preserving the historical IDA alias.
- Clarify captured `+0x14` as signed slot and `+0x15` as bool all/single flag.
- Keep formal C++ as generated-template proof, not a handwritten allocator body, unless a later template implementation pass chooses a coordinated source declaration strategy.

`by-memory/0x00630a78-0x00630a90.UserPaneDoubleParamCallbackVtableData.md`:

- Keep current score or raise only after updating the body with the full slot table.
- Add slot `0x00630a7c -> 0x004f4b10`, slot `0x00630a80 -> 0x0041b6c0`, object-size slot `0x00630a88 -> 0x004673f0`, and null/alignment dword `0x00630a8c`.
- State that `0x00630a90` is the first byte of the following wide drop-confirmation prompt string, not part of the vtable.

`by-type/by-template/FunctionObjectTemplates.md`:

- Add a short source-facing template draft for `DoubleParamMemberFunctionObject0` with `Invoke()` forwarding captured arguments to a bound member callback.
- Keep the exact original template/header name as inferred, not proven.
- Clarify that the concrete [UID:0001N5] body stays comment/no-body at target level because final human source should be template declaration code.

`by-file/FunctionObjects.md`:

- Add this B014 reanalysis to the UserPane callback row: resolved bound targets, signed-slot/bool field meanings, full vtable slot context, and generated-template no-body policy.
- Score change is optional; current `87/85` remains defensible because file-level exact original header/source naming is still the broad cap.

`by-file/UserPane.md` / `by-class/UserPane.md`:

- Add future child candidates:
  - `0x005ad7a0-0x005ad811` `UserPane::SendDropItemPacket(signed char slot, bool allItems)`
  - `0x005ad720-0x005ad791` `UserPane::SendGiveItemPacket(signed char slot, bool allItems)`
- Note that both are callback targets for drop/give confirmations, use `g_packetSender`, and do not dereference `this` even though source binds them as `UserPane` members.

`by-file/ItemActionInputPanes.md`:

- Add or refine the callback construction note: DropAll/Drop/GiveAll/Give panes pass the selected slot and all/single flag into `CreateUserPaneDoubleParamCallback`, then pass the returned action object to `ConfirmInputPane`.
- Keep packet helper ownership for direct raw send helpers in `ItemActionInputPanes`; do not reassign the target invoke wrapper there.

## Supervisor-Owned Coverage Row

Replace the current `by-memory/-coverage-report.md` row for UID `0001N5` with:

```text
    - [UID:0001N5][0x005b77b0-0x005b77c8.UserPaneDoubleParamCallback](by-memory/0x005b77b0-0x005b77c8.UserPaneDoubleParamCallback.md) 0x005b77b0-0x005b77c8 | invoke wrapper | UserPaneDoubleParamCallback : reconstructable : 87% : very strong : B014 source-quality reanalysis resolves the concrete generated-template role and stale no-code rationale: the exact 24-byte wrapper is `DoubleParamMemberFunctionObject0<UserPane, signed char, bool>::Invoke`, reached only through vtable slot `0x00630a84`, reading member callback pointer `+0x08`, this-adjust `+0x0c`, bound `UserPane*` `+0x10`, signed slot `+0x14`, and bool all/single flag `+0x15`. Paired construction helper [UID:0002VM][0x005b7450-0x005b74dd.CreateUserPaneDoubleParamCallback](by-memory/0x005b7450-0x005b74dd.CreateUserPaneDoubleParamCallback.md) is called from DropAll/Drop/GiveAll/Give confirmation paths and binds target methods `0x005ad7a0` drop opcode `0x08` and `0x005ad720` give opcode `0x29`; owner/emitter remain [UID:000040][DoubleParamMemberFunctionObject0<UserPane, signed char, bool>](by-class/DoubleParamMemberFunctionObject0_void____thiscall_UserPane_____signed_char__h43ff6c8e0528.md) through [UID:0000JO][FunctionObjects](by-file/FunctionObjects.md). No handwritten standalone invoke body should be emitted; final source should come from the shared FunctionObjects template declaration, with only a comment/proof marker if nonblank generated output is required for this source-declared/generated-binary special case.
```

Do not edit `by-memory/-coverage-report.md` directly while the shared-file ban is active.

## Validation Commands Needed

Report-only verification already performed:

> Executable block R001 was removed from this report and preserved verbatim in [0001N5-UserPaneDoubleParamCallback-source-quality-removed.md](0001N5-UserPaneDoubleParamCallback-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Recommended implementation validation after accepted doc edits:

> Executable block R002 was removed from this report and preserved verbatim in [0001N5-UserPaneDoubleParamCallback-source-quality-removed.md](0001N5-UserPaneDoubleParamCallback-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Recommended non-mutating search checks:

> Executable block R003 was removed from this report and preserved verbatim in [0001N5-UserPaneDoubleParamCallback-source-quality-removed.md](0001N5-UserPaneDoubleParamCallback-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

## IDA Rename / Type / Comment Recommendations

Safe IDA labels:

| Address | Recommendation | Confidence |
| --- | --- | --- |
| `0x005b77b0` | `UserPaneDoubleParamCallback_Invoke` | high |
| `0x005b7450` | `CreateUserPaneDoubleParamCallback` | high |
| `0x00630a78` | `DoubleParamMemberFunctionObject0_UserPane_signed_char_bool_vftable` | high |
| `0x005ad7a0` | `UserPane_SendDropItemPacket_CallbackTarget` | high behavior, medium original name |
| `0x005ad720` | `UserPane_SendGiveItemPacket_CallbackTarget` | high behavior, medium original name |

Recommended IDA type/comment direction:

```cpp
typedef void (__thiscall UserPane::*UserPaneItemTransferCallback)(signed char slot, bool allItems);
```

Add comments rather than force full struct/type mutation if the IDA type model is not ready:

- At `0x005b77b0`: "Invoke for DoubleParamMemberFunctionObject0<UserPane, signed char, bool>; calls captured UserPane member with signed slot and all/single flag."
- At `0x005b7495`: "Stores UserPane double-param callback vtable 0x00630a78."
- At `0x005b74af`: "Captured signed slot byte at object +0x14."
- At `0x005b74b5`: "Captured bool all/single flag at object +0x15."
- At `0x005b46d1` / `0x005b4a00`: "Bind drop packet callback target."
- At `0x005b4ebb` / `0x005b5193`: "Bind give packet callback target."
- At `0x00630a84`: "Invoke slot for UserPane double-param callback."
- At `0x00630a88`: "Object-size virtual, returns 0x18."

Avoid:

- Do not force exact original template spelling as proven source.
- Do not rename the member target methods without a descriptive suffix unless the project accepts the inferred `SendDropItemPacket` / `SendGiveItemPacket` names.
- Do not create a separate source file for the long generated instantiation.

## Remaining Open Questions

- Exact original header basename and template class spelling remain inferred. Current best is `FunctionObjects.h` / `DoubleParamMemberFunctionObject0`, but no debug/source metadata proves it.
- Whether final generated output for source-declared/generated-binary callback wrappers should use blank blocks or comment-only proof markers is a supervisor/project policy choice. This report recommends comment-only marker if nonblank output is required, and explicitly rejects a handwritten invoke body.
- Exact original names for `0x005ad7a0` and `0x005ad720` are not proven. Their behavior and signatures are resolved strongly enough for descriptive names and future child docs.

These do not block the recommended score/metadata update or owner/emitter route.

## Changed Files

- Created: `source-3/project-documentation/tools/leaser/Agents/Agent-B014/research/0001N5-UserPaneDoubleParamCallback-source-quality.md`
- Modified: none outside this report.
- Renamed: none.
- Moved to executed: none.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B014","command_id":"000000004165","destination_path":"executed-b-agent-research/B014/0001N5-UserPaneDoubleParamCallback-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:36","uid":"0001N5"} -->
<!-- {"agent":"B014","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 0001N5-UserPaneDoubleParamCallback-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B014/0001N5-UserPaneDoubleParamCallback-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"0001N5"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
