<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B010\0002FL-ChattingVarietySelectPaneOnPaint-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable Code Archive

- Source report: [0002FL-ChattingVarietySelectPaneOnPaint-source-quality.md](0002FL-ChattingVarietySelectPaneOnPaint-source-quality.md)
- Source report SHA256 before cleanup: `33482F2C481B447B72A8C421186D8237695FCC6CF31534447121737723EFC372`
- Cleanup generated: `2026-08-13T06:28:23Z`
- Removed executable blocks: `1`
- Scope: exact executable or command-shaped material removed from the source report by supervisor housekeeping.
- Authority: this file is not a B-agent research report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: do not execute, import, evaluate, or use any block in this archive as an operational instruction.
- Status: non-authoritative preservation archive.

## Removed Block R001

- Original source line: `318`
- Original representation: inline PowerShell/.NET invocation
- Block SHA256: `F848EC66259F282284D0C8D6E9732DE9407EE0FF3653BD68A062A53134A5DC87`

```powershell
[System.IO.File]::Open($BASE_PATH,[System.IO.FileMode]::Open,[System.IO.FileAccess]::Read,[System.IO.FileShare]::None)
```
+
## Removed Block R002

- Status: non-authoritative legacy transaction/request archive.
- Safety: never execute or replay this block.
- Scope: diagnostic/transaction worker orchestration, request objects, ordered mutation/comment calls, persistence/rollback sequence, and procedural xref-probe contracts removed during the current supervisor cleanup.

~~~text
### Pre-Baseline Analysis And Frozen Inspection Contract

1. Open a dedicated **diagnostic analysis worker** with literal `idb_open {input_path:"<canonical-path>",mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"b010-uid0002fl-diagnostic-<attempt>"}`. Before a baseline is frozen, the primary supervisor may run only the bounded analysis-producing calls needed to reconstruct this package: `analyze_function`, `decompile`, `func_profile`, `disasm`, `callees`, and `insn_query` for I01-I03/P01-P07. Current `tools/list` has no `callers` method, so that endpoint is neither required nor allowed; direct callers are derived in the transaction preflight through the paginated read-only `xref_query` rule below. Any call that can queue analysis belongs in this disposable worker and stage. `define_func`, byte edits, item creation, type/name/comment writes, and saves are forbidden.
2. Wait for every bounded analysis call to finish, then terminate/discard the diagnostic worker **without save**. Prove the exact recorded worker process identity ended with the closed-worker primitive below. No state from the diagnostic worker may be saved, reused as the mutation worker, or treated as the frozen transaction prestate.
3. After the closed-worker primitive passes, reread the canonical disk and record exact `BASE_PATH`, `BASE_SIZE`, `BASE_MTIME`, and `BASE_SHA256`. Create a collision-safe no-overwrite backup named `E:\NTK\Resources\NexusTK\NexusTK.exe.i64.bak-UID0002FL-prestate-<yyyyMMdd-HHmmss>-<first16(BASE_SHA256)>`. Require that the path does not exist; on collision choose a new timestamp rather than overwrite or reuse it. Immediately before hashing the source, copying the backup, hashing the backup, and performing the final baseline rehash, repeat the no-active-session and exclusive-handle checks below. Verify backup path, SHA256, size, and source identity equal `BASE_PATH`/`BASE_SHA256`/`BASE_SIZE`, then reread path/size/mtime/SHA of the canonical disk and require exact baseline parity.
4. After one final successful exclusive-handle probe has been disposed, fresh-open a distinct **transaction worker** from the exact `BASE_PATH` identity with literal `idb_open {input_path:"<BASE_PATH>",mode:"force_headless",run_auto_analysis:false,build_caches:false,init_hexrays:false,idle_ttl_sec:600,preferred_session_id:"b010-uid0002fl-transaction-<attempt>"}`. Record its exact process identity and MCP session row as attempt-local evidence and require the on-disk path/size/mtime/SHA still equal the frozen baseline. No analysis-producing call is permitted in this worker. If later evidence requires one, terminate the transaction worker without save, pass the closed-worker primitive, and begin again from step 1.
5. The transaction worker's complete stateful preflight allowlist is `idb_list`, `server_health`, `lookup_funcs`, `inspect_items`, `stack_frame`, `get_comments`, `get_bytes`, read-only `entity_query`, read-only `xref_query`, read-only `type_inspect`, and integer/value reads such as `get_int`. No decompile, analysis, disassembly, callgraph, nonexistent `callers`, item-definition, or other unlisted call is allowed. Each direct-caller or protection query uses one entity per incoming `xref_query`, fixed `count` no greater than 200, starts at `offset:0`, follows every returned `next_offset` until it is `null`, and requires the accumulated row count to equal the endpoint-reported `total`. The ordered edge tuples and their SHA256 digest are then frozen. Oversized `count:5000` is forbidden because primary testing showed it can return only ten P06 rows with `next_offset:null`, silently truncating the set.
6. Repeat all function-name and stack-name collision checks in the fresh transaction worker. Exact prestate, collision, bytes, item, frame, comment, type, callee/callsite, xref, and protection rows below must match. Any mismatch is semantic drift: terminate without save, pass the closed-worker primitive, historicalize the attempt, and require same-report reconciliation plus fresh Gate 1.

### Exact Closed-Worker And Closed-Disk Probe Contract

- At worker creation, record the attempt-local MCP session identifier and the exact OS process identity tuple: numeric PID, process creation/start time, executable image path, and command line. A worker is closed only when that exact process identity no longer exists. Query the PID after termination: the same identity still present is active and stops; a different creation-time/image/command tuple is explicit PID reuse, not proof that the old worker survives, and must be recorded before continuing; an unavailable or ambiguous identity stops.
- Dynamically query `idb_list` after process termination and require zero `is_active:true` rows for the canonical IDB path. The MCP server has no `idb_close`; a stale row with `is_active:false` is non-authoritative and need not disappear or change its `owned:true`/`adopted:true` flags after the exact worker process ends. A canonical row that is active, a row/process association that is ambiguous, or a live same-identity worker stops the transaction.
- Before every canonical-disk hash, backup copy, reopen, restore copy, or persistence/failure classification, use a supervisor-owned read-only exclusive-open probe for `BASE_PATH`, then dispose that handle before the operation. Failure to acquire or dispose the exclusive probe stops; it is never treated as evidence for save/no-save, never bypassed, and never followed by copy, restore, reopen, or classification.

> The executable inline probe expression was removed from this report and preserved verbatim in [0002FL-ChattingVarietySelectPaneOnPaint-source-quality-removed.md](0002FL-ChattingVarietySelectPaneOnPaint-source-quality-removed.md). The archived expression is non-authoritative and must not be executed.
- Only after exact process-identity absence, PID-reuse resolution, zero active canonical-IDB rows, and a successful disposed exclusive probe may the supervisor read or compare the exact disk tuple `PATH/SIZE/MTIME/SHA256`, hash/copy/reopen/restore, or classify an attempt. These checks are repeated immediately before each such operation. Inactive stale MCP rows remain dated metadata only and cannot authorize or block disk handling by themselves.
- Every diagnostic, transaction, verifier, post-save verifier, and rollback verifier `idb_open` call must explicitly set `run_auto_analysis:false`; omission or `true` stops. Transaction and verifier opens additionally use `build_caches:false` and `init_hexrays:false`. A verifier uses `mode:"force_headless"`, exact canonical input path, a unique attempt-local preferred session ID, and the same process/session/disk checks; it never mutates or saves.

### Exact Mutation Endpoint Contract

- Gate 2B must dynamically reread live `tools/list` and require current `rename.batch.pure` plus `set_address_comments`. It must hash the active `api_modify.py` and require SHA256 `161B314A14DAAA34C718176444292597BBB7498FA80158E25C358542BC16D1B8`; any schema or implementation drift stops before mutation for same-report reconciliation.
- Every I01-I03 **function** dry-run/live rename call includes `pure:true`. Under the inspected implementation this calls only the checked IDA name setter and skips `/vibe/` directory creation/link/save plus `refresh_decompiler_ctext`. Each result must contain the expected address/old/new name, contain no `error`, `dir`, or `dir_error`, and be followed immediately by exact name/range/type/frame/comment/bytes/xref protection readback. A function/data rename without literal `pure:true` is forbidden.
- I01 **stack** rows cannot use pure mode: the current endpoint returns `pure rename does not support stack members`. Each stack dry-run/live call therefore includes literal `pure:false`; the inspected implementation resolves the existing frame UDM and the live path calls only `ida_frame.define_stkvar` with the preserved type. No decompiler-local rename is proposed. After each call the full nine-row frame, function name/type, all four comment channels, body bytes/hash, and xrefs are reread; any extra/deleted/shifted/renamed/retyped row or any response outside the expected stack result stops before the next mutation.
- Regular address comments use only `set_address_comments`, whose inspected implementation writes only `idaapi.set_cmt(ea,text,False)` and never invokes Hex-Rays. Address repeatable uses only `set_address_repeatable_comments`, implemented as `idaapi.set_cmt(ea,text,True)` only. Function regular uses only `set_function_comments`, implemented as `idc.set_func_cmt(function_start,text,False)` only. Function repeatable uses `set_repeatable_comments` only after verifying the address is the function entry, where its implementation selects only `idc.set_func_cmt(ea,text,True)`. All four prestates are blank, so no clear is requested. `set_comments` is forbidden because it also invokes decompilation and can write function-repeatable state at a function entry.
- The mutation allowlist is exactly 34 ordered action calls before the single later save: six function `rename` calls with `pure:true` (three dry plus three live), three function `set_type` calls, twelve stack `rename` calls with `pure:false` (six dry plus six live), one stack `set_type` call only for `pane`, and twelve narrow comment calls (`set_address_comments`, `set_address_repeatable_comments`, `set_function_comments`, and entry-only `set_repeatable_comments`, four per function). The former five redundant stack type calls are forbidden. Everything else is inspection-only or forbidden. After every comment call, `get_comments` must prove only the intended channel changed and all future channels remain blank.

- Function operation order: (1) dry-run `rename {batch:{func:{addr:"0x00481490",name:"ChattingVarietySelectPane__OnPaint"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<active-session>"}` and require collision-free success with no `error`/`dir`/`dir_error`; (2) repeat with `dry_run:false,pure:true`; (3) read back exact name/range and all protected state; (4) call `set_type {edits:{addr:"0x00481490",kind:"function",signature:"void __thiscall ChattingVarietySelectPane__OnPaint(ChattingVarietySelectPane *this)"},database:"<active-session>"}`; (5) read back the literal canonical type `void __thiscall(ChattingVarietySelectPane *this)`. Raw-pointer, byte-pointer, integer-this, non-`__thiscall`, non-`void`, `/vibe/` result fields, or decompiler-refresh evidence are failures.
- Stack operation order is one row at a time. For each row, run the literal rename dry-run with `allow_overwrite:false`, repeat with `dry_run:false`, and immediately read the whole canonical `stack_frame`. The four integer locals already have canonical `int` and `outRect` already has canonical `RectBounds`; preserve those physical types and omit their redundant `set_type` calls. Only `var_18 -> pane` has an intended type delta, so only that row receives one `set_type` under its new name followed by a second full-frame readback:

| Order | Frozen old row | Exact rename calls | Exact type action/disposition | Required immediate canonical row |
| --- | --- | --- | --- | --- |
| 1 | `var_28 +0x0c size0x4 int` | `rename ... stack:{func_addr:"0x00481490",old:"var_28",new:"frameRight"}` dry then live | Omit `set_type`; preserve existing canonical `int`. | `frameRight +0x0c size0x4 int` |
| 2 | `var_24 +0x10 size0x4 int` | `rename ... stack:{func_addr:"0x00481490",old:"var_24",new:"frameTop"}` dry then live | Omit `set_type`; preserve existing canonical `int`. | `frameTop +0x10 size0x4 int` |
| 3 | `x +0x14 size0x4 int` | `rename ... stack:{func_addr:"0x00481490",old:"x",new:"frameLeft"}` dry then live | Omit `set_type`; preserve existing canonical `int`. | `frameLeft +0x14 size0x4 int` |
| 4 | `var_1C +0x18 size0x4 int` | `rename ... stack:{func_addr:"0x00481490",old:"var_1C",new:"frameBottom"}` dry then live | Omit `set_type`; preserve existing canonical `int`. | `frameBottom +0x18 size0x4 int` |
| 5 | `var_18 +0x1c size0x4 GrafPort *` | `rename ... stack:{func_addr:"0x00481490",old:"var_18",new:"pane"}` dry then live | `set_type ... {addr:"0x00481490",kind:"stack",name:"pane",ty:"ChattingVarietySelectPane *"}` | `pane +0x1c size0x4 ChattingVarietySelectPane *` |
| 6 | `outRect +0x20 size0x10 RectBounds` | `rename ... stack:{func_addr:"0x00481490",old:"outRect",new:"itemBounds"}` dry then live | Omit `set_type`; preserve existing canonical `RectBounds`. | `itemBounds +0x20 size0x10 RectBounds` |

- Every abbreviated `rename ...` row above expands to `rename {batch:{stack:{...},dry_run:<true-or-false>,stop_on_error:true,allow_overwrite:false,pure:false},database:"<active-session>"}`. Pure mode is explicitly forbidden for stack rows because the endpoint rejects it. Only the `pane` type application follows its successful frame-member rename and addresses the new name. The protected rows must remain `var_4 +0x30 size0x4 _DWORD`, `__saved_registers +0x34 size0x4 _DWORD`, and `__return_address +0x38 size0x4 _UNKNOWN *`; no row may be added, deleted, merged, shifted, or canonicalized from literal `int` to `signed __int32`, and no decompiler-local path may be used.
- Comment operation order after all name/type/frame work passes: (1) `set_address_comments {items:{addr:"0x00481490",comment:"ChattingVarietySelectPane::OnPaint entry."},database:"<active-session>"}`; (2) `set_address_repeatable_comments {items:{addr:"0x00481490",comment:"Virtual selector paint override at primary vtable slot 0x00614F70."},database:"<active-session>"}`; (3) `set_function_comments {items:{addr:"0x00481490",comment:"Paints Talk, Shout, Whisper, Group, Clan, and -Color-, then draws the active-row bevel; m_pressCaptured reverses palette indices 128 and 143."},database:"<active-session>"}`; (4) entry-only `set_repeatable_comments {items:{addr:"0x00481490",comment:"Source: void ChattingVarietySelectPane::OnPaint()."},database:"<active-session>"}`. After each call, `get_comments` must independently reread entry regular, entry repeatable, function regular, and function repeatable channels; only the just-addressed channel may change, completed channels must match literally, future channels must remain absent, and name/type/frame/bytes/xrefs must remain unchanged.

- Exact operation order: dry-run `rename {batch:{func:{addr:"0x004816d0",name:"ChattingVarietySelectPane__SetupBackground"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<active-session>"}`, then the same call with `dry_run:false,pure:true`; require no `error`/`dir`/`dir_error`, then immediate exact name/range/protection readback; `set_type` signature `void __thiscall ChattingVarietySelectPane__SetupBackground(ChattingVarietySelectPane *this)`; immediate literal canonical readback `void __thiscall(ChattingVarietySelectPane *this)`; preserve the five frame rows literally.
- Comment order and independent readback: `set_address_comments {items:{addr:"0x004816d0",comment:"ChattingVarietySelectPane::SetupBackground entry."},database:"<active-session>"}`; `set_address_repeatable_comments {items:{addr:"0x004816d0",comment:"Virtual selector-background override at primary vtable slot 0x00614F74."},database:"<active-session>"}`; `set_function_comments {items:{addr:"0x004816d0",comment:"Loads SUBWIN.EPF frame 8, insets m_visibleBounds by one pixel, and tiles the selector background."},database:"<active-session>"}`; entry-only `set_repeatable_comments {items:{addr:"0x004816d0",comment:"Source: void ChattingVarietySelectPane::SetupBackground()."},database:"<active-session>"}`. Run `get_comments` after every call and require only the intended channel to change, cumulative literal channel state, and all future channels absent.

- Exact operation order: dry-run `rename {batch:{func:{addr:"0x00481750",name:"ChattingVarietySelectPane__DrawBorder"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<active-session>"}`, then the same call with `dry_run:false,pure:true`; require no `error`/`dir`/`dir_error`, then immediate exact name/range/protection readback; `set_type` signature `void __thiscall ChattingVarietySelectPane__DrawBorder(ChattingVarietySelectPane *this)`; immediate literal canonical readback `void __thiscall(ChattingVarietySelectPane *this)`; preserve all seven frame rows literally.
- Comment order and independent readback: `set_address_comments {items:{addr:"0x00481750",comment:"ChattingVarietySelectPane::DrawBorder entry."},database:"<active-session>"}`; `set_address_repeatable_comments {items:{addr:"0x00481750",comment:"Virtual selector-border override at primary vtable slot 0x00614F78."},database:"<active-session>"}`; `set_function_comments {items:{addr:"0x00481750",comment:"Draws the selector's SUBWIN.EPF 7x7 edge tiles and four corner tiles around m_visibleBounds."},database:"<active-session>"}`; entry-only `set_repeatable_comments {items:{addr:"0x00481750",comment:"Source: void ChattingVarietySelectPane::DrawBorder()."},database:"<active-session>"}`. Run `get_comments` after every call and require only the intended channel to change, cumulative literal channel state, and all future channels absent.

- P01-P10 xref comparator contract: never compare raw `xref_query` JSON byte-for-byte across intended function renames. For each protected set, query one entity at a time with `count <= 200`, paginate from offset zero through every `next_offset` until `null`, and require accumulated rows equal the endpoint-reported `total`. Canonicalize every row to its ordered structural tuple `(from_ea,to_ea,xref_type,direction)` plus any stable non-name flags, record the exact tuple list/count/SHA256 digest at P0, and require those three invariants unchanged after every action and after reopen. Separately record caller-function start/name metadata. The only allowed metadata deltas are `0x00481490 sub_481490 -> ChattingVarietySelectPane__OnPaint`, `0x004816d0 sub_4816D0 -> ChattingVarietySelectPane__SetupBackground`, and `0x00481750 sub_481750 -> ChattingVarietySelectPane__DrawBorder` on rows whose containing caller is exactly that start. These expected decorations do not mutate a protected helper. Any edge/type/order/count/digest change, helper entity name/type/comment/frame change, metadata change for another caller, or unexpected name is real drift and stops before save. Attempt 1's raw-JSON mismatch is historicalized as a comparator defect, not accepted structural drift.

- For P03-P07, derive direct callers with one incoming `xref_query` entity at a time using `include_fn:true`, fixed page size no greater than 200, and pagination from offset zero through every `next_offset` until `null`; require accumulated rows equal reported `total`. The nonexistent `callers` endpoint and oversized `count:5000` are forbidden; the latter was observed to truncate P06 to ten rows while returning `next_offset:null`. Dated diagnostic-worker results are research evidence only. After the diagnostic worker is discarded, the closed-worker/exclusive-probe contract passes, and the P0 backup is verified, the fresh exact-P0 transaction worker must recapture and bind every complete ordered structural tuple list/count/digest plus the separately compared caller-name metadata during its inspection-only preflight; P06/P07 are not frozen before backup. Only the exact three caller-name transitions permitted by the P01-P10 comparator may differ after mutation. Any structural mismatch or other metadata mismatch is drift, not permission to use a partial result.

### Transaction, Persistence, And Rollback

- Historical fail-closed results, no credit: attempt 1 began from exact P0/verified backup, applied only the I01 `pure:true` function rename, and stopped before save when a raw JSON comparator misclassified the intended caller-name decoration in protected helper xref rows; complete closure/probe/verifier readback proved exact P0. Attempt 2 restarted from fresh exact P0, applied the I01 pure rename and function type plus the first stack rename, then stopped before save when redundant `set_type ty:"int"` canonicalized the row to `signed __int32`; a second complete closure/probe/verifier readback again proved exact P0. Neither attempt may be resumed or credited. The corrected structural-xref comparator and 34-call action package apply only to a new attempt that starts at step 1.
1. Complete bounded analysis in the dedicated diagnostic worker; treat every resulting callee/xref/frame/comment/type/layout record as dated research only, terminate the worker without save, and pass the exact closed-worker/PID-reuse/no-active-session/exclusive-handle primitive. Freeze `BASE_PATH`/`BASE_SIZE`/`BASE_MTIME`/`BASE_SHA256`, create and verify the collision-safe backup under the repeated exclusive-probe rule, open the exact baseline in the distinct transaction worker with `run_auto_analysis:false`, and recapture every transaction-authoritative I01-I03/P01-P10 prestate, including complete P03-P07 xref digests, through the inspection-only allowlist. Attempt session/PID/process identity is evidence, never permanent authority.
2. Re-read live endpoint schema and require the exact implementation hash in the mutation-endpoint contract. Repeat the three function-name and six stack-name collision checks. Derive every direct caller exclusively through paginated incoming `xref_query` with `count <= 200`, follow `next_offset` to `null`, require accumulated count equals `total`, and freeze the ordered structural tuple list/count/digest plus caller-name metadata separately. A missing `pure`/`set_address_comments` capability, implementation drift, dry-run collision, canonical type alias outside the literal accepted forms, partial caller/xref page set, oversized `count:5000`, comment mismatch, item/range mismatch, or protection mismatch stops before mutation.
3. Apply I01 serially: function dry-run `rename` with `pure:true`, function live `rename` with `pure:true`, exact result/name/range/protection readback, function type, exact readback; then each of six stack rows as dry-run `rename` with `pure:false`, live `rename` with `pure:false`, and full-frame/protection readback. Omit `set_type` for the four already-canonical `int` rows and the already-canonical `RectBounds` row; call stack `set_type` only once for `pane` and reread the full frame. Apply the four ordered one-channel comment calls beginning with `set_address_comments`, with independent four-channel readback after each. At this point exactly 20 I01 action calls have run. Reread all I01/P01/P02/P03-P08 protections, comparing xref structural edges exactly while allowing only the expected I01 caller-name decoration, before continuing.
4. Apply I02 serially: dry-run and live function `rename` with `pure:true`, exact result/name/range/protection readback, exact function type/readback, then the four ordered one-channel comment calls beginning with `set_address_comments`. Reread I02/P02, both adjacent fences, complete I02 callees/callsites/inbound xrefs, frames/comments, and P03-P08.
5. Apply I03 serially in the same pure-function-rename and one-channel-comment order, then reread I03/P02, both adjacent fences, complete I03 callees/callsites/inbound xrefs, frames/comments, and P03-P08. I02 and I03 each contribute seven calls, so the complete pre-save action package is exactly 34 calls: six pure function rename calls, three function type calls, twelve specialized stack rename calls, one `pane` stack type call, and twelve narrow comment calls. No action is grouped and no later step may proceed after a mismatched immediate readback, unexpected `dir`/`dir_error`, unintended comment channel, decompiler-refresh evidence, structural xref drift, caller-name metadata outside the three allowed transitions, or other side effect.
6. **Distinct pre-save stop/no-save branch:** after the first mutation but before `idb_save`, any tool error, action/readback mismatch, protection mismatch, collision, endpoint-side-effect mismatch, or incomplete pre-save poststate is a pre-save failure. That attempt ends permanently and can never call `idb_save`; never reuse a post-save branch to infer its result. Stop mutation; terminate the exact transaction process without save; pass the closed-worker primitive, including no active canonical session and the exclusive handle probe; then reread the canonical `BASE_PATH` tuple twice and classify only against exact P0:
   - Exact `BASE_PATH`/`BASE_SIZE`/`BASE_MTIME`/`BASE_SHA256`: classify `PRE_SAVE_NO_SAVE`; do not copy or restore the backup. After a disposed exclusive probe, open an independent read-only verifier with `run_auto_analysis:false`, prove complete I01-I03/P01-P10 P0 prestate/protection parity, terminate it, pass the closed-worker primitive again, and rehash exact P0 before recording the stopped attempt.
   - Any non-P0, unavailable, unknown, changed-between-reads, active/ambiguous worker, failed exclusive probe, or third identity before save is not transaction-attributable. Halt without backup copy, restore, overwrite, retry, reopen, or later-work inference; preserve the backup and report the incident.
7. Perform a complete pre-save poststate readback using only the frozen inspection allowlist: exactly 34 action-call receipts; exact I01-I03 names/ranges/literal types/four comments/complete frames; the four preserved `int` locals and preserved `RectBounds`; the sole `pane` type delta; P01-P10 bytes/items/names/types/comments/values/callees/callsites/xrefs; exact structural xref tuples/counts/digests with only the three allowed caller-name metadata transitions; uniqueness; no extra item/function/frame/comment mutation; no `dir`/`dir_error`; and no unlisted/decompiler side effect. If any row fails, take step 6. Only a separate fresh attempt restarted at step 1, with a newly probed/verified exact P0 and fully passing poststate, may invoke `idb_save` exactly once.
8. On acknowledged save success, never save again. Terminate the transaction process, pass the closed-worker primitive, and only then read the canonical tuple twice and freeze `S1_PATH`/`S1_SIZE`/`S1_MTIME`/`S1_SHA256`. After another successful disposed exclusive probe, open an independent verifier with `run_auto_analysis:false`, repeat complete persisted I01-I03/P01-P10 readback without mutation/save, terminate it, pass the closed-worker primitive, then rehash/restate the canonical tuple and require exact S1. Persistence credit exists only after verifier proof and final exact S1 rehash.
9. An explicit `idb_save` error/failure is a **determinate save failure**. Never retry. Terminate the process without another save and pass the closed-worker primitive before any classification/copy:
   - Exact P0 tuple after two exclusive-probe-guarded reads: classify `DETERMINATE_NO_SAVE`, open a `run_auto_analysis:false` verifier, prove P0, terminate/probe/rehash exact P0, and do not copy the backup.
   - A changed tuple equal to the exact transaction-attributable failed identity captured after closure/probe: classify `DETERMINATE_FAILED_OR_PARTIAL_SAVE` and enter guarded rollback only while that tuple still matches at the final pre-copy probe/check.
   - Any active/ambiguous worker, failed probe, third identity, post-classification change, unavailable tuple, or later-work evidence halts without overwrite, copy, retry, reopen, or rollback.
10. An `idb_save` timeout, disconnect, missing response, or worker loss is **indeterminate**, never permission to retry. Terminate the exact process, pass the closed-worker primitive, and only then classify the exclusive-probe-guarded disk tuple:
   - Exact P0: classify `NO_SAVE`, open a `run_auto_analysis:false` verifier, prove P0, terminate/probe/rehash exact P0, and do not copy.
   - Changed tuple: capture provisional S1, open a `run_auto_analysis:false` verifier, prove complete expected poststate, terminate/probe, and require final tuple exact provisional S1 before promoting it to S1.
   - Partial/mismatched transaction-owned tuple: classify `FAILED_OR_PARTIAL_SAVE`, never retry, and enter guarded rollback.
   - Active/ambiguous worker, failed exclusive probe, unavailable tuple, third identity, or failed reopen/readback halts indeterminate without blind restore.
11. Guarded rollback is allowed only for proven determinate/indeterminate failed or partial save, or post-save persisted-readback failure; never for step 6. Before classification and again immediately before any restore copy, pass the closed-worker primitive and exclusive probe, require the backup exact P0, and require canonical disk still equal the exact last transaction-owned failed tuple. Exact P0 means no copy. Third/later-work identity, active/ambiguous worker, or probe failure stops without overwrite. Otherwise restore, verify P0, open an independent `run_auto_analysis:false` verifier, prove complete I01-I03/P01-P10 prestate, terminate/probe, and rehash exact P0. No blind inverse mutation or restore over later work is permitted.
12. Never create/split functions, alter bytes/ranges/vtable values/literal items, materialize the selector UDT, overwrite protected nonblank comments, mutate protected helpers, use function/data `rename` without `pure:true`, use pure mode for stack/local renames, use `set_comments`, save/reuse diagnostic analysis state, call analysis in transaction/verifier workers, omit `run_auto_analysis:false`, save early, retry a save, reuse a post-save branch for pre-save failure, or claim action/persistence credit from preflight alone.
~~~
