** TARGET-REPORT-UID:000125 **
** AUTHOR-AGENT-ID:B010 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 000125 RectangleControlPane Scalar Deleting Destructor Source-Quality Research

## Finalized Report / Current Recommendation
- Current recommendation: reclassify [UID:000125] `0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor` as no-code MSVC scalar deleting destructor glue, not a source-authored `RectangleControlPane` method body.
- Final disposition: `RECONSTRUCTABLE:FALSE`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank formal `RECONSTRUCTION_CPP`.
- Required action: keep the target page as exact binary evidence for the `RectangleControlPane` destructor/vtable family, but remove generated-source routing and replace the stale below-95 no-code blocker with a target-specific compiler-glue proof.
- Recommended score: `COMPLETION:88`, `CONFIDENCE:92`.
- Confidence: very high for exact boundary, padding, vtable/adjustor reachability, callee roles, and no-code policy from local PE/Capstone verification plus existing live-IDA-backed support docs; not final-audit because the IDA MCP endpoint was unavailable during this B010 pass.

## Supporting Research

## Target
- Target UID: `000125`.
- Target path: `source-3/project-documentation/by-memory/0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor.md`.
- Required report path: `source-3/project-documentation/tools/leaser/Agents/Agent-B010/research/000125-RectangleControlPaneScalarDeletingDestructor-source-quality.md`.
- Current state: `COMPLETION:84`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000BT`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000BT`, blank formal C++.
- Current generated state: `auto-generated/NexusTK/ui/controls/RectangleControlPane.cpp` contains an empty emitter marker for UID `000125`; `auto-generated/-ag-memory-coverage.md` marks it as `emits` through `0000BT`.
- Current coverage state: `by-memory/-coverage-report.md` line found by `rg` marks UID `000125` as `reconstructable : 84% : strong`.

## Executive Recommendation
[UID:000125] should not emit C++ and should not stay reconstructable. The exact range is the MSVC deleting-destructor wrapper generated for `RectangleControlPane`, reached by the primary vtable and by two multiple-inheritance adjustor thunks. It restores the three `RectangleControlPane` vtable views, calls the ordinary inherited pane teardown at `0x00544580`, applies the usual deleting-destructor flag byte, optionally calls the MemoryMan-backed global `operator delete` wrapper at `0x004f4ac0`, and has the compiler/runtime guarded flag-4 branch through `0x0041b6a0` with object size `0x108` / 264.

The source-facing destructor placement is the ordinary/non-deleting helper at `0x00499ce0-0x00499cff` currently covered inside [UID:00011Q] `RectangleControlPaneCore`, or the class declaration/destructor in [UID:0000BT] `RectangleControlPane`. Even that ordinary helper has no Rectangle-specific cleanup beyond vtable restoration and tail-jumping to `Pane::~Pane()`, so the source-level body is at most an empty/default destructor:

```cpp
RectangleControlPane::~RectangleControlPane()
{
}
```

That draft belongs only to an ordinary destructor/class page if the supervisor later creates or accepts that source-bearing page. It must not be inserted into [UID:000125].

## Supervisor Active Recheck
- This is a report-only B-source-quality pass. No target/support by-* docs, generated files, IDA DB files, project-level reports, or `by-memory/-coverage-report.md` were edited.
- Split repair was not performed because the assignment explicitly requested a report. No child page was created.
- The exact scalar wrapper range does not need splitting: local disassembly confirms a single function body at `0x0049b490-0x0049b4e5`, followed by eleven bytes of `0xcc` padding at `0x0049b4e5-0x0049b4f0`.
- The adjacent source-facing ordinary destructor range should be corrected in support docs from stale `0x00499ce0-0x00499cfe` wording to half-open `0x00499ce0-0x00499cff`; the one byte at `0x00499cff-0x00499d00` is padding.

## Inference Research Guidance Check
`by-structure.md` separates source semantics from compiler/linker-generated artifacts. It lists adjustor thunks, EH/RTTI glue, padding, and compiler/linker output as documentation targets that should be recognized and excluded from hand-porting unless a source declaration is required to regenerate them. It also states that final C++ requires a reconstructable item, a confirmed emitter route, and a combined-score gate, but that this gate is minimum eligibility rather than permission to emit weak or compiler-shaped C++.

For this target, the stale "blank until 95/95" explanation is not the real blocker. The real blocker is source shape: the exact range is a deleting-destructor wrapper and must be regenerated by the compiler from class/destructor declarations. The `RectangleControlPane` class remains the semantic context, but this exact by-memory page should be a non-emitting compiler-glue proof page.

`inference_research.md` cautions that vtables, compiler artifacts, and decompiler/compiler names are strong behavioral evidence but weak original-source-name evidence. I treated current by-* docs and the prior B007 report as leads, then rechecked the byte-level and reference evidence directly against the read-only executable.

## Heuristic / Inference Reanalysis And Validation

### Boundary and padding
- Best inference: exact function body is `0x0049b490-0x0049b4e5`, size `0x55` / 85, with post-body padding `0x0049b4e5-0x0049b4f0`, eleven `0xcc` bytes.
- Evidence checked: local PE section mapping and Capstone disassembly of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`; existing target page; [UID:00011Y] destructor-band support page.
- Local disassembly begins with `push ebp; mov ebp, esp; push esi; mov esi, ecx` at `0x0049b490` and returns at `0x0049b4e2` with `ret 4`; `0x0049b4e5` is the first `int3` padding byte.
- Preceding local padding also checks out: the previous destructor returns before `0x0049b487`, and `0x0049b487-0x0049b490` is nine `0xcc` bytes.
- Decision: keep target boundary unchanged.

### Primary vtable and adjustor-thunk reachability
- Best inference: liveness is destructor-dispatch/vtable-only, not ordinary source-call reachability.
- Evidence checked:
  - Local dword scan: `0x00618434 -> 0x0049b490`, `0x0061849c -> 0x0049afc1`, `0x006184cc -> 0x0049afcc`.
  - Local VA-pointer scan: `0x0049b490` appears as a data pointer at `0x00618434`; `0x0049afc1` at `0x0061849c`; `0x0049afcc` at `0x006184cc`.
  - Local Capstone over [UID:000122]: `0x0049afc1` subtracts `0xa0` and jumps to `0x0049b490`; `0x0049afcc` subtracts `0xa4` and jumps to `0x0049b490`.
  - Existing [UID:000122] records IDA xrefs to `0x0049b490` from the thunk jumps plus the primary vtable ref.
- Rejected alternative: treat secondary/tertiary adjustor thunk refs as source callers. They are compiler-generated `this` adjustment wrappers for multiple-inheritance vtable entries.
- Decision: support docs should say [UID:000125] has vtable/adjustor-only reachability and no ordinary direct code callers.

### Vtable restore sequence and class context
- Best inference: the wrapper is `RectangleControlPane` class-context compiler glue.
- Evidence checked:
  - Local disassembly stores `0x00618434` at `[esi]`, `0x0061849c` at `[esi+0xa0]`, and `0x006184cc` at `[esi+0xa4]`.
  - Local VA-pointer scan finds those vtable base constants in constructor/helper/wrapper stores at `0x00499ca1/0x00499ce2/0x0049b498`, `0x00499cab/0x00499cec/0x0049b4a2`, and `0x00499cb5/0x00499cf6/0x0049b4ac`.
  - [UID:0002OE] and [UID:0003IT] document the exact primary/secondary/tertiary `RectangleControlPane` vtable/RTTI data and the `NumericStringControlPane`/`CheckBoxControlPane` boundaries.
- Rejected alternative: assign wrapper ownership to the file page [UID:0000N1] as a free helper. The vtable stores and slots are class ABI output, not a file-local handwritten helper.
- Decision: body prose should keep semantic class context [UID:0000BT], but metadata should use `CANONICAL_OWNER:NONE` because this exact function is compiler-generated output and should not have an emitter.

### Shared pane teardown `0x00544580`
- Best inference: `0x00544580` is ordinary `Pane::~Pane()` base teardown.
- Evidence checked: local disassembly shows `call 0x00544580` at `0x0049b4b0`; [UID:0001EA] `PaneCore` documents `0x00544580-0x00544687` as ordinary `Pane::~Pane()`.
- Rejected alternative: describe `0x00544580` as a Rectangle-specific teardown or TextButtonEx cleanup. Current PaneCore support and broad destructor fanout make it inherited base cleanup.
- C++ impact: do not hand-write `Pane::~Pane()` in a derived destructor body; C++ base destructor chaining is implicit.

### Delete helper `0x004f4ac0`
- Best inference: `0x004f4ac0` is the MemoryMan-backed global `operator delete(void *)` wrapper.
- Evidence checked: local disassembly calls `0x004f4ac0` at `0x0049b4c1` only after `test al, 1` passes and `test al, 4` fails; [UID:000197] identifies `0x004f4ac0-0x004f4b00` as the global delete wrapper with broad destructor fanout.
- Rejected alternative: model this as a `RectangleControlPane` or `ControlPane` helper. It is a process-wide storage-free routine used by many scalar deleting destructors.
- C++ impact: source should use ordinary `delete`/destructor declarations and let the compiler call the delete routine in the generated wrapper.

### Alternate deleting-flag / guard branch
- Best inference: `flags & 4` selects a compiler/runtime guarded size-delete path at `0x0041b6a0`, not handwritten `RectangleControlPane` logic.
- Evidence checked: local disassembly:
  - `mov eax, [ebp+8]`;
  - `test al, 1`; if bit 1 is clear, return `this`;
  - `test al, 4`; if bit 4 is set, push `0x108`, push `esi`, call `0x0041b6a0`, clean 8 bytes, and return `this`;
  - otherwise push `esi`, call `0x004f4ac0`, clean 4 bytes, and return `this`.
- Existing cross-target evidence checked: other scalar deleting destructor research/docs describe `0x0041b6a0` as an unstable `@_guard_check_icall_nop@4` / guard/no-op label when used as a vtable slot, but as a compiler/runtime guard-or-vector-delete helper path at deleting-destructor call sites that push `this` plus a size operand.
- Size conversion: `0x108` = 264 verified with `tools/int_convert.py`.
- Rejected alternative: name `0x0041b6a0` as a source-level Rectangle helper. The same address is reused widely as a guard/no-op slot and runtime helper target; its local call signature here is compiler-output evidence.

### Ordinary destructor placement and stale endpoint
- Best inference: source-facing ordinary destructor helper is `0x00499ce0-0x00499cff`; existing support rows that say `0x00499ce0-0x00499cfe` are stale half-open endpoint wording.
- Evidence checked:
  - Local Capstone: `0x00499ce0` stores the same three Rectangle vtable bases and `jmp 0x00544580` at `0x00499cfa`; `0x00499cff` is `int3`.
  - [UID:00011Q] already records live IDA size `0x1f`, which gives half-open `0x00499ce0-0x00499cff`.
  - [UID:00011Q] records `0x00499cff-0x00499d00` as one padding byte.
- Rejected alternative: source-facing destructor body lives at [UID:000125]. Rejected because the ordinary helper is the non-deleting destructor placement; [UID:000125] adds scalar-delete flags and storage free.
- C++ impact: if a source body is needed, use an empty/default `RectangleControlPane::~RectangleControlPane()` in the ordinary destructor/class context, not the scalar wrapper.

### Source/compiler-glue policy and generated output pollution
- Best inference: [UID:000125] should become `RECONSTRUCTABLE:FALSE`, owner `NONE`, no emitters, blank code.
- Evidence checked:
  - Local binary body is entirely ABI mechanics after class-context vtable restoration.
  - Existing [UID:000122] adjustor thunks are already `RECONSTRUCTABLE:FALSE`, `CANONICAL_OWNER:NONE`, blank emitters.
  - [UID:00011Y] broad destructor/thunk aggregate is non-reconstructable and says source-level reconstruction belongs to owning classes/declarations that cause compiler thunks/wrappers.
  - `auto-generated/NexusTK/ui/controls/RectangleControlPane.cpp` currently contains only an empty emitter marker for UID `000125`, demonstrating generated-output pollution rather than useful source.
- Rejected alternatives:
  - Keep owner/emitter `0000BT` with a blank code block. Rejected because this still routes a non-source wrapper into generated output.
  - Insert a comment-only `//` block. Rejected for this target because the better metadata fix is non-reconstructable/no-emitter; comment-only markers are only useful for special emitting policy cases.
  - Emit a method named `RectangleControlPaneScalarDeletingDestructor`. Rejected as decompiler/binary documentation terminology, not original source C++.
  - Lower the page to ignored/undocumented. Rejected because exact boundary, route, and class ABI evidence is valuable and should remain documented.

### Remaining open questions
- Live IDA MCP was unavailable (`Unable to connect to the remote server` from JSON-RPC `tools/list` to `http://127.0.0.1:13337/mcp`). Local PE/Capstone evidence was used instead and matched the existing live-IDA-backed docs.
- It remains unproven whether original source explicitly declared an empty `virtual ~RectangleControlPane()` or relied on an implicit/default destructor. This does not affect [UID:000125], because both source shapes regenerate scalar deleting wrapper output.
- Exact original spelling of the paint virtual (`OnPaint` versus `Paint`) and type-id method remains outside this target. Do not use those sibling questions to keep [UID:000125] as source-emitting.

## Evidence Standards Used
- Direct local binary evidence: read-only PE bytes from `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, section-based VA mapping, Capstone x86 disassembly, pointer scans, rel32 displacement scans, and dword table reads.
- Numeric conversion evidence: `tools/int_convert.py` verified `0x55` = 85, `0x0b` = 11, `0xa0` = 160, `0xa4` = 164, `0x108` = 264, nine-byte and fourteen-byte padding counts.
- Documentation evidence: current target page, [UID:000122] adjustor thunks, [UID:0002OE] vtable data, [UID:0003IT] vtable type, [UID:00011Q] core page, [UID:0000BT] class page, [UID:0000N1] file page, [UID:0001EA] PaneCore, [UID:000197] OperatorDeleteWrapper, and prior B007 research used as a lead only.
- Generated/report evidence: `auto-generated/NexusTK/ui/controls/RectangleControlPane.cpp`, `auto-generated/-ag-memory-coverage.md`, `project-level/-auto-completion-stats.md`, and `by-memory/-coverage-report.md`.
- Negative evidence: no ordinary direct caller route was found in current docs or local reference scans; the only direct reachability is vtable/adjustor dispatch. No Rectangle-specific field/resource release exists in the wrapper body.

## IDA / Local Binary Facts
- IDA MCP facts could not be freshly collected because the endpoint was unavailable. Existing docs contain prior live IDA facts from 2026-06-05 through 2026-06-11, and the local PE checks below independently verify the critical bytes/routes.
- Function/range facts:
  - `0x0049b490-0x0049b4e5`: scalar deleting destructor wrapper, size `0x55` / 85.
  - `0x0049afc1-0x0049afcc`: secondary adjustor thunk, `sub ecx, 0xa0; jmp 0x0049b490`.
  - `0x0049afcc-0x0049afd7`: tertiary adjustor thunk, `sub ecx, 0xa4; jmp 0x0049b490`.
  - `0x00499ce0-0x00499cff`: ordinary/non-deleting destructor helper placement; `0x00499cff-0x00499d00` is padding.
- Padding facts:
  - `0x00499cd2-0x00499ce0`: fourteen `0xcc` bytes.
  - `0x00499cff-0x00499d00`: one `0xcc` byte.
  - `0x0049b487-0x0049b490`: nine `0xcc` bytes.
  - `0x0049b4e5-0x0049b4f0`: eleven `0xcc` bytes.
- Vtable facts:
  - `0x00618430`: primary RTTI/COL pointer `0x00645ff0`.
  - `0x00618434`: primary vtable slot 0 `0x0049b490`.
  - `0x00618478`: paint slot `0x00499d00`.
  - `0x00618494`: type-id slot `0x0049b8f0`.
  - `0x00618498`: secondary RTTI/COL pointer `0x00646050`.
  - `0x0061849c`: secondary vtable slot 0 `0x0049afc1`.
  - `0x006184c8`: tertiary RTTI/COL pointer `0x00646064`.
  - `0x006184cc`: tertiary vtable slot 0 `0x0049afcc`.
  - `0x006184d4`: next `CheckBoxControlPane` RTTI/COL pointer `0x00646078`.
- Callee facts:
  - `0x0049b4b0`: calls `0x00544580`, documented by [UID:0001EA] as ordinary `Pane::~Pane()`.
  - `0x0049b4c1`: calls `0x004f4ac0`, documented by [UID:000197] as MemoryMan-backed global `operator delete(void *)`.
  - `0x0049b4d6`: calls `0x0041b6a0` after pushing `0x108` and `this`; this is the compiler/runtime guarded size-delete path, not source Rectangle logic.

## Function / Child Inventory
| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x00499c60-0x00499cd2` | [UID:00011P] `RectangleControlPaneConstructor` | Source constructor | TRUE | [UID:0000BT] | current accepted first-draft state | Source-bearing, not part of no-code wrapper. |
| `0x00499ce0-0x00499cff` | currently inside [UID:00011Q] `RectangleControlPaneCore` | Ordinary/non-deleting destructor helper | source-facing if split | [UID:0000BT] if split | not separate | Best placement for `RectangleControlPane::~RectangleControlPane()` semantics; endpoint wording needs support fix. |
| `0x00499d00-0x00499d37` | [UID:00011R] `RectangleControlPanePaint` | Paint virtual | TRUE | [UID:0000BT] | current accepted first-draft state | Source-bearing sibling. |
| `0x0049afc1-0x0049afd7` | [UID:000122] `RectangleControlPaneAdjustorThunks` | Secondary/tertiary destructor adjustor thunks | FALSE | NONE | `88/92` | Already correct compiler-glue/no-emitter model; update only if cross-reference wording says [UID:000125] remains reconstructable. |
| `0x0049b490-0x0049b4e5` | [UID:000125] target | Scalar deleting destructor wrapper | recommend FALSE | recommend NONE | recommend `88/92` | No-code compiler glue. |
| `0x0049b8f0-0x0049b8f5` | [UID:000128] `RectangleControlPaneTypeId` | Fixed type-id virtual returning `21` | TRUE | [UID:0000BT] | `88/92` | Source-bearing sibling; outside this target. |
| `0x00618430-0x006184d4` | [UID:0002OE] `RectangleControlPaneVtableData` | Source-declared/generated-binary vtable/RTTI data | TRUE | [UID:0000BT] | `87/92` | Rebuilt through class declarations and compiler output, not handwritten dwords. |

## Direct Xref / Caller Inventory
| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x00618434` | dword pointer to `0x0049b490` | Primary vtable deleting-destructor slot. |
| `0x0061849c` | dword pointer to `0x0049afc1` | Secondary vtable destructor adjustor slot. |
| `0x006184cc` | dword pointer to `0x0049afcc` | Tertiary vtable destructor adjustor slot. |
| `0x0049afc7` | `jmp 0x0049b490` after `this -= 0xa0` | Compiler adjustor thunk route. |
| `0x0049afd2` | `jmp 0x0049b490` after `this -= 0xa4` | Compiler adjustor thunk route. |
| `0x0049b4b0` | call `0x00544580` | Ordinary `Pane::~Pane()` base cleanup. |
| `0x0049b4c1` | call `0x004f4ac0` | Global MemoryMan-backed `operator delete(void *)`. |
| `0x0049b4d6` | call `0x0041b6a0` after `push 0x108; push esi` | Compiler/runtime guarded flag-4 size-delete path. |

## Documentation Evidence And Status
- Existing docs that support the conclusion:
  - [UID:000122] already models the two adjustor thunks as no-code compiler glue and records xrefs into the scalar deleting destructor.
  - [UID:0002OE] and [UID:0003IT] document the exact `RectangleControlPane` vtable layout and state that vtable bytes should be recreated through class declarations.
  - [UID:00011Q] records the ordinary helper at `0x00499ce0`, size `0x1f`, with vtable restores and tail jump to shared pane teardown.
  - [UID:0001EA] documents `0x00544580` as `Pane::~Pane()`.
  - [UID:000197] documents `0x004f4ac0` as global operator delete.
  - [UID:00011Y] already treats the broader destructor/thunk band as a non-reconstructable index and says wrappers/thunks should be generated from source declarations.
- Docs that need accepted callback updates:
  - [UID:000125] target still says reconstructable with owner/emitter `0000BT` and uses stale below-95 no-code reasoning.
  - [UID:0000BT] class method table and [UID:0000N1] file method table still list stale ordinary-helper endpoint `0x00499ce0-0x00499cfe`.
  - [UID:000122] contains one sentence saying the scalar deleting destructor remains reconstructable; that should be superseded if support docs are updated.
  - [UID:00011Q] should clarify why the group page remains blank and correct the ordinary helper endpoint wording.

## Ranked Ownership Analysis

### 1. Compiler-generated no-code wrapper, semantic RectangleControlPane context
- Evidence for: vtable/adjustor-only inbound route, three class vtable restores, base destructor call, delete-flag logic, optional global delete, guarded flag-4 helper path, and no Rectangle-specific field/resource cleanup.
- Evidence against: the wrapper is class-specific ABI output and therefore should remain linked in prose to `RectangleControlPane`.
- Decision: accepted. Metadata should be `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitters; prose should state semantic class context [UID:0000BT].

### 2. Keep `CANONICAL_OWNER:0000BT`, `RECONSTRUCTABLE:TRUE`, blank/comment-only source
- Evidence for: some older scalar destructor pages preserve class owner/emitter because a class destructor declaration is needed to regenerate the binary wrapper.
- Evidence against: current generated output has an empty marker for UID `000125`; this exact range cannot be written as natural C++ source; the ordinary destructor/class declaration, not [UID:000125], is the source item that causes regeneration.
- Decision: rejected for this exact target.

### 3. Emit a source method named `RectangleControlPaneScalarDeletingDestructor`
- Evidence for: descriptive title matches the binary role.
- Evidence against: scalar deleting destructor names are compiler/decompiler artifacts; original mid-2000s source would not define a manually named wrapper with flag-byte ABI mechanics.
- Decision: rejected.

### 4. Assign to global delete, Pane, or vtable-data owner
- Evidence for: the body calls `Pane::~Pane()`, calls global delete, and is referenced by vtable data.
- Evidence against: callees and data consumers are not direct semantic owners. The body is class ABI glue, not global-delete source, not `Pane` source, and not a source-authored vtable data object.
- Decision: rejected.

## Proposed New Page / Split Direction
- Optional new page: `source-3/project-documentation/by-memory/0x00499ce0-0x00499cff.RectangleControlPaneDestructor.md`.
- Proposed UID: supervisor/validator should allocate if the split is accepted.
- Proposed owner/emitter if created: [UID:0000BT] `RectangleControlPane`, emitted through the class/file route only if the supervisor accepts explicit empty destructor source.
- Proposed contents: ordinary/non-deleting destructor helper; vtable restores to `0x00618434/0x0061849c/0x006184cc`; tail jump to `Pane::~Pane()` at `0x00544580`; no direct xrefs; one-byte padding at `0x00499cff-0x00499d00`; scalar wrapper parity note.
- Source-file inference: narrow exact method child under existing `NexusTK/ui/controls/RectangleControlPane.cpp`; no new source file or broader grouping needed.

## Negative Evidence Summary
- No ordinary call route proves source reachability for [UID:000125]; vtable and adjustor thunk routes prove ABI liveness.
- The delete helper call is generic storage management, not Rectangle-specific cleanup.
- The `Pane::~Pane()` call is inherited base cleanup, not `RectangleControlPane` source logic.
- The flag-byte and guarded size path are compiler deleting-destructor mechanics, not a source API.
- Current generated output is an empty UID marker, which is evidence against keeping this target as an emitting page.
- Sibling source-bearing pages remain valid class context but do not make the scalar wrapper source-authored.

## Final Recommendation

### Exact target metadata recommendation
Use this header state in [UID:000125]:

```text
*** UID:000125 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Exact target prose changes recommended
- Change entity kind to "MSVC scalar deleting destructor wrapper / compiler-generated destructor glue".
- Keep "Related class" and "Related file" links as semantic context, but explain that metadata owner/emitter are intentionally blank because this exact range is not source-authored.
- Add local-disassembly proof:
  - exact body `0x0049b490-0x0049b4e5`, size `0x55` / 85;
  - prepad `0x0049b487-0x0049b490` nine `0xcc`;
  - postpad `0x0049b4e5-0x0049b4f0` eleven `0xcc`;
  - vtable restores to `0x00618434`, `0x0061849c`, `0x006184cc`;
  - primary vtable `0x00618434` and adjustor thunks `0x0049afc1` / `0x0049afcc`;
  - `Pane::~Pane()` call at `0x00544580`;
  - optional `operator delete` call at `0x004f4ac0`;
  - flag-4 compiler/runtime helper path at `0x0041b6a0` with size `0x108` / 264.
- Replace below-95 no-code wording with:
  - "Formal C++ is intentionally blank because this exact range is compiler-generated deleting-destructor glue. Source reconstruction should express `RectangleControlPane::~RectangleControlPane()` or rely on the class's default destructor and let MSVC regenerate the deleting wrapper and adjustor thunks."
- Add rejected alternatives:
  - `RectangleControlPaneScalarDeletingDestructor()` as a handwritten method;
  - ownership by `Pane`, global delete, vtable data, or file-local helper;
  - comment-only emitter marker;
  - moving/collapsing the wrapper into [UID:000122] adjustor thunk page;
  - ignoring the page rather than documenting exact no-code proof.

### Support doc changes recommended
- [UID:0000BT] `by-class/RectangleControlPane.md`:
  - Update method table row `non-deleting destructor helper` from `0x00499ce0-0x00499cfe` to `0x00499ce0-0x00499cff`.
  - Change scalar deleting destructor row to "compiler-generated MSVC deleting-destructor wrapper; vtable/adjustor-only reachability; no handwritten source body".
  - Add a destructor-family note: ordinary destructor semantics live at `0x00499ce0-0x00499cff` or class declaration level; [UID:000125] should not emit; adjustor thunks [UID:000122] also stay no-code.
  - Keep class source route through [UID:0000N1].
- [UID:0000N1] `by-file/RectangleControlPane.md`:
  - Update proposed contents row for the ordinary helper to `0x00499ce0-0x00499cff`.
  - Change scalar deleting destructor row to no-code compiler wrapper and remove any implication that `RectangleControlPane.cpp` should hand-author a scalar-delete function.
  - Say the source file should express constructor, paint, type-id, and any ordinary destructor/class declaration, while vtables, adjustor thunks, and scalar deleting wrapper are generated.
- [UID:00011Q] `by-memory/0x00499c60-0x00499d37.RectangleControlPaneCore.md`:
  - Correct the ordinary helper row endpoint to `0x00499ce0-0x00499cff`.
  - Keep the group/index formal C++ blank because exact children own source emission and the scalar wrapper is no-code compiler glue.
  - Add the one-byte `0x00499cff-0x00499d00` padding as the endpoint proof if not already clear.
- [UID:000122] `by-memory/0x0049afc1-0x0049afd7.RectangleControlPaneAdjustorThunks.md`:
  - Replace the note "The scalar deleting destructor remains reconstructable" with "The scalar deleting destructor is now resolved as no-code compiler deleting-destructor glue; the ordinary destructor/class declaration is the source-facing placement."
- [UID:00011Y] `by-memory/0x0049af11-0x0049b8d5.ButtonChoiceControlDestructors.md`:
  - Update the [UID:000125] row/notes if they still imply source emission; keep the aggregate non-reconstructable split-index disposition.
- Optional support docs:
  - [UID:0002OE] and [UID:0003IT] already contain the correct vtable facts; edit only if adding back-reference text that [UID:000125] is no-code.
  - [UID:0001EA] and [UID:000197] do not need changes unless the implementer adds backlink notes.

## First-Draft C++ / Exact No-Code Proof
Do not populate [UID:000125] formal C++. Do not add a comment-only source marker. The recommended metadata removes this target from emission entirely.

Target-specific no-code proof:

```text
0x0049b490-0x0049b4e5 is reached from RectangleControlPane primary vtable slot 0x00618434 and secondary/tertiary destructor adjustor thunks 0x0049afc1 and 0x0049afcc.
The body restores RectangleControlPane vtable bases 0x00618434, 0x0061849c, and 0x006184cc, calls ordinary Pane::~Pane() at 0x00544580, then implements MSVC deleting-destructor flag handling.
If flags bit 1 is clear, it returns without freeing. If bit 1 is set and bit 4 is clear, it calls MemoryMan-backed operator delete at 0x004f4ac0. If bit 4 is set, it calls the compiler/runtime guarded size-delete path at 0x0041b6a0 after pushing object size 0x108 and this.
No RectangleControlPane field/resource cleanup, singleton ownership, custom delete logic, or source-level method behavior exists in this range.
Therefore source should define or imply RectangleControlPane::~RectangleControlPane() at the class/ordinary destructor level and let the compiler regenerate this scalar deleting destructor wrapper and its adjustor thunks.
```

If the optional ordinary destructor child is created or class declaration code is updated later, the only source-style C++ draft supported by current evidence is:

```cpp
RectangleControlPane::~RectangleControlPane()
{
}
```

Do not attach that draft to [UID:000125].

## Score / Metadata Rationale
| Field | Current | Recommended | Rationale |
| --- | --- | --- | --- |
| `COMPLETION` | `84` | `88` | Boundary, padding, vtable/adjustor route, callee roles, flag semantics, source/no-code policy, support-doc implications, and rejected alternatives are now fully documented. |
| `CONFIDENCE` | `90` | `92` | Local PE/Capstone verification matches prior live-IDA-backed docs. Confidence is capped below final-audit because live IDA MCP was unavailable and original explicit-versus-implicit destructor declaration remains unknowable. |
| `CANONICAL_OWNER` | `0000BT` | `NONE` | Semantic class context remains in prose, but exact range is compiler-generated wrapper output and should not be a direct source owner child. |
| `RECONSTRUCTABLE` | `TRUE` | `FALSE` | This exact wrapper is compiler/linker-generated ABI glue. |
| `EMITTER_UIDS` | `0000BT` | blank | Removes empty generated marker pollution from `RectangleControlPane.cpp`. |
| `RECONSTRUCTION_CPP` | blank | blank | Blank for target-specific no-code proof, not stale score threshold. |

## IDA Rename / Type / Comment Recommendations
- `0x0049b490`: keep a descriptive binary name such as `RectangleControlPane_scalar_deleting_destructor` or IDA's decorated MSVC scalar-deleting-destructor name if available. Add a repeatable comment: "compiler-generated MSVC scalar deleting destructor; no source C++ body; generated from `RectangleControlPane::~RectangleControlPane()`/class destructor declaration."
- `0x00499ce0`: name/comment as `RectangleControlPane::~RectangleControlPane` ordinary/non-deleting destructor helper, not as the scalar deleting destructor. If IDA requires a unique technical name, use `RectangleControlPane_destructor_non_deleting`.
- `0x0049afc1`: comment "secondary destructor adjustor thunk; adjusts `this -= 0xa0`, jumps to `0x0049b490`; no source body."
- `0x0049afcc`: comment "tertiary destructor adjustor thunk; adjusts `this -= 0xa4`, jumps to `0x0049b490`; no source body."
- `0x00618434`: comment "RectangleControlPane primary vtable deleting-destructor slot."
- `0x0061849c`: comment "RectangleControlPane secondary vtable destructor adjustor slot."
- `0x006184cc`: comment "RectangleControlPane tertiary vtable destructor adjustor slot."
- `0x0041b6a0`: avoid a Rectangle-specific name. Existing docs show this address is reused as `@_guard_check_icall_nop@4` / guard-no-op slot in some contexts; at scalar deleting destructor call sites, describe it as the compiler/runtime guarded size-delete helper path with an unstable label.

## Exact Implementation Checklist For Callback
1. Lease before editing:
   - `by-memory/0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor.md`
   - `by-class/RectangleControlPane.md`
   - `by-file/RectangleControlPane.md`
   - `by-memory/0x00499c60-0x00499d37.RectangleControlPaneCore.md`
   - `by-memory/0x0049afc1-0x0049afd7.RectangleControlPaneAdjustorThunks.md` if changing the stale "scalar remains reconstructable" sentence.
   - `by-memory/0x0049af11-0x0049b8d5.ButtonChoiceControlDestructors.md` if updating aggregate row wording.
2. Update [UID:000125] metadata exactly as recommended above.
3. Keep [UID:000125] formal C++ blank; do not insert the empty destructor draft there.
4. Replace stale below-95 no-code text with the target-specific no-code proof and route/callee evidence.
5. Add rejected alternatives and generated-output pollution note.
6. Correct support-doc ordinary helper endpoints to `0x00499ce0-0x00499cff`.
7. Update support wording so [UID:000125] is a compiler-generated deleting-destructor wrapper and [UID:000122] adjustors remain no-code.
8. Do not edit `by-memory/-coverage-report.md`; use the exact supervisor-owned row below.
9. Run scoped validators from `source-3/project-documentation` for every edited file.

## Expected Validation Commands
No validators were run in this report-only pass because no by-* docs were edited. Expected implementation validation commands:

> Executable block R001 was removed from this report and preserved verbatim in [000125-RectangleControlPaneScalarDeletingDestructor-source-quality-removed.md](000125-RectangleControlPaneScalarDeletingDestructor-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Run the last two only if those support docs are edited. If an optional ordinary destructor child is created, validate the new child and rerun validators for class/file/core support docs.

## Pending Coverage Text
Do not edit `source-3/project-documentation/by-memory/-coverage-report.md` directly. Supervisor-owned replacement:

Replace current row:

```text
    - [UID:000125][0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor](by-memory/0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor.md) : reconstructable : 84% : strong : RectangleControlPane scalar deleting destructor; live IDA confirms `0x55` size, vtable/adjustor-thunk reachability, vtable restoration, shared teardown/free path, alternate deleting-flag branch, parent attachment, and trailing alignment.
```

With:

```text
    - [UID:000125][0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor](by-memory/0x0049b490-0x0049b4e5.RectangleControlPaneScalarDeletingDestructor.md) 0x0049b490-0x0049b4e5 | compiler-generated scalar deleting destructor wrapper | RectangleControlPaneScalarDeletingDestructor : not_reconstructable : 88% : very strong : B010 source-quality pass resolves this as no-code MSVC deleting-destructor glue in [UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md) semantic context. Local PE/Capstone verification plus existing live-IDA-backed docs confirm exact `0x55` body, nine-byte prepad at `0x0049b487-0x0049b490`, eleven-byte postpad at `0x0049b4e5-0x0049b4f0`, primary vtable slot `0x00618434 -> 0x0049b490`, secondary/tertiary adjustor thunk routes `0x0049afc1`/`0x0049afcc -> 0x0049b490`, no ordinary direct callers, vtable restores to `0x00618434`/`0x0061849c`/`0x006184cc`, ordinary [UID:0001EA][Pane](by-memory/0x00544460-0x00545086.PaneCore.md) teardown call `0x00544580`, optional MemoryMan-backed [UID:000197][operator delete](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) call `0x004f4ac0`, compiler/runtime flag-4 guarded size-delete path through `0x0041b6a0` with size `0x108`, and source-facing ordinary destructor placement at `0x00499ce0-0x00499cff`. Formal C++ stays blank and owner/emitter should be cleared because final source should express `RectangleControlPane::~RectangleControlPane()` or default class destructor semantics and let MSVC regenerate this ABI wrapper.
```

Generated `auto-generated/-ag-memory-coverage.md`, `project-level/-auto-completion-stats.md`, and generated `RectangleControlPane.cpp` should change only through validators/autogen refresh after by-* implementation, not by manual edit in this report pass.

## Follow-Up Actions
- Supervisor: review and, if accepted, send B010 an implementation callback with the exact target/support files above; apply the supervisor-owned coverage row after implementation verification.
- B010 on callback: lease, apply the target/support doc changes, run the scoped validators, report changed files, validation exit codes, generated/project-level side effects, and the still-pending coverage row.
- Optional future work: decide whether to split/create the exact ordinary destructor child `0x00499ce0-0x00499cff.RectangleControlPaneDestructor.md`. This is useful for exact memory/source placement but not required to resolve [UID:000125].

## Changed Files
- Created: `source-3/project-documentation/tools/leaser/Agents/Agent-B010/research/000125-RectangleControlPaneScalarDeletingDestructor-source-quality.md`.
- Modified: none outside Agent-B010 research.
- Renamed: none.
- Moved to executed: none.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B010","command_id":"000000004165","destination_path":"executed-b-agent-research/B010/000125-RectangleControlPaneScalarDeletingDestructor-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:34","uid":"000125"} -->
<!-- {"agent":"B010","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 000125-RectangleControlPaneScalarDeletingDestructor-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B010/000125-RectangleControlPaneScalarDeletingDestructor-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"000125"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
