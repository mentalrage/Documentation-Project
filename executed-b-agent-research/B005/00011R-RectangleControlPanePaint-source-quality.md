** TARGET-REPORT-UID:00011R **
** AUTHOR-AGENT-ID:B005 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 00011R RectangleControlPanePaint Source-Quality Report

Agent: B005
Target: `source-3/project-documentation/by-memory/0x00499d00-0x00499d37.RectangleControlPanePaint.md`
Required focus: replace stale below-95 no-code reasoning with current 85/85 gate analysis; resolve paint method source shape, draw/fill helper names, rectangle bounds fields, control-pane state, owner/emitter routing, compiler-generated/raw names, open questions, score/metadata recommendation, and first-draft C++ readiness.

## Result

Recommended status: implementation-ready after a small target-page documentation update.

The current target page should no longer justify a blank C++ block with the old below-95/95 rule. Under the current gate, this function is eligible for source code once the page records the resolved source shape:

- `RECONSTRUCTABLE:TRUE`
- nonblank `EMITTER_UIDS:0000BT`
- confirmed owner/emitter route through `RectangleControlPane` / `RectangleControlPane.cpp`
- recommended direct target score `86/90`, average `88.0`, above the active 85/85 code-entry threshold

The page already has enough semantic evidence for a first-draft C++ method. The confidence should remain below 95 because the exact original spelling of the paint virtual and the source-level wrapper name for SurfaceRenderCallbackTable slot 7 are still inferred rather than proven from source symbols.

## Evidence Checked

- Target memory page: `by-memory/0x00499d00-0x00499d37.RectangleControlPanePaint.md`
- Grouping page: `by-memory/0x00499c60-0x00499d37.RectangleControlPaneCore.md`
- Constructor page: `by-memory/0x00499c60-0x00499cd2.RectangleControlPaneConstructor.md`
- RectangleControlPane class/file pages:
  - `by-class/RectangleControlPane.md`
  - `by-file/RectangleControlPane.md`
- RectangleControlPane vtable evidence:
  - `by-memory/0x00618430-0x006184d4.RectangleControlPaneVtableData.md`
  - `by-type/by-vtable/RectangleControlPaneVtables.md`
- Bounds/layout evidence:
  - `by-memory/0x004b8e00-0x004b8e11.BackPaneGetBounds.md`
  - `by-type/by-struct/PaneLayout.md`
  - `by-type/by-struct/RectBoundsLayout.md`
- Draw/fill callback evidence:
  - `by-global/SurfaceRenderCallbackTable.md`
  - `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`
  - `by-memory/0x004be680-0x004bed67.SoftwareRenderCompatFillCallback.md`
  - `by-memory/0x004c4380-0x004c46c0.SoftwareRenderRgb565FillCallback.md`
  - `by-global/g_pfnLockSurface.md`
- Draw-state helper evidence:
  - `by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md`
- Comparable paint/fill path:
  - `by-memory/0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar.md`
- Consumer context:
  - `by-class/MiniMapDialog.md`
  - `by-file/MiniMap.md`
- Generated/cached evidence:
  - `source-3/simroot_v2/class_RectangleControlPane.cpp`
  - `source-3/core/data/cache/prewave/functions/0x00499d00.json`
  - readonly Wave2 RectangleControlPane records and rename registry

IDA MCP was unavailable in this session (`http://127.0.0.1:13337/mcp` connection failed), so this report relies on current by-* docs, local autogenerated reports, simroot output, and cached IDA/Ghidra decompilation records.

Scoped validator dry-run was checked:

_Executable command block removed from the research report; preserved in [00011R-RectangleControlPanePaint-source-quality-removed.md](00011R-RectangleControlPanePaint-source-quality-removed.md)._

Result: `ok: 1`, dry-run only, no target errors and no file changes.

## Function Shape

Cached IDA decompilation for `0x00499d00` gives the essential body:

```c
int __thiscall sub_499D00(void *this)
{
  __int128 bounds;
  sub_4B8E00((int)this, &bounds);
  return dword_69B3FC(this, &bounds);
}
```

That matches the current target-page notes:

- live function span is `0x00499d00-0x00499d37`, size `0x37`
- `0x00499d37-0x00499d40` is post-function `0xcc` padding
- reachability is through the RectangleControlPane primary vtable slot at `0x00618478`
- no ordinary code caller is needed for this virtual method
- callee `0x004b8e00` copies the inherited Pane bounds rectangle from `this + 0x44`
- callee/global slot `0x0069b3fc` dispatches the active Surface render fill/presentation callback

The Ghidra cache summary that frames this as cleanup/destruction is contradicted by the calls, the vtable slot, the RectangleControlPane docs, and the SurfaceRenderCallbackTable documentation. It should not drive the source name or behavior.

## Source Method Name

Recommended source-facing method name: `RectangleControlPane::OnPaint`.

Acceptable fallback if the surrounding C++ naming pass prefers shorter virtual names: `RectangleControlPane::Paint`.

Rejected names:

- `InvalidateControl`: generated Wave2/simroot rename. The body does not merely invalidate; it copies current bounds and dispatches the active rectangle fill callback. This name is callsite-biased and should not be preserved as final source.
- raw `sub_499D00`: compiler/recovery name only.
- cleanup/destructor wording from cached Ghidra summary: contradicted by body and vtable evidence.

The current documentation title `RectangleControlPanePaint` is fine as a page label. The final C++ block should use `OnPaint` unless a later naming sweep proves a different virtual spelling.

## Bounds And State

The bounds helper is `0x004b8e00`, currently documented as `BackPane::GetBounds(RectBounds *outBounds) const`. Its machine behavior is generic Pane-layout access: copy the 16-byte rectangle at inherited `Pane` offset `+0x44` into the caller-provided output buffer and return it.

Relevant field shape:

- `PaneLayout.md`: `+0x44` is the inherited local/bounds rectangle, also described as `m_bounds` or `m_localBounds`
- `RectBoundsLayout.md`: four 32-bit fields, `left`, `top`, `right`, `bottom`, size `0x10`

For this target, the source-facing C++ should use an unqualified inherited accessor:

```cpp
GetBounds(&bounds);
```

Do not imply RectangleControlPane is owned by BackPane. `BackPane::GetBounds` is the current helper-owner documentation, not the semantic owner of this paint method.

The constructor page and `GrafPortDrawStateAccessors.md` show the draw/fill color state is set before painting through `0x004b9660`, a GrafPort draw-state setter storing the active color at `this + 0x74`. Generated names such as `FittingRoomListPane::SetTextColor` are polluted by broad callers and should not be used here. For RectangleControlPane, this is active draw/fill color state used by the fill callback.

## Draw/Fill Helper Name

The call at `0x0069b3fc` is SurfaceRenderCallbackTable slot 7, not a source-level `g_pfnLockSurface` call. Supporting docs show it is initialized by surface setup to either compat or RGB565 software render fill callbacks, and the target callbacks clip/intersect the rectangle, resolve draw-state color/mode, and fill/blend the destination.

Recommended source-facing helper in first-draft C++:

```cpp
FillRect(&bounds);
```

Meaning: a Pane/GrafPort-level wrapper for SurfaceRenderCallbackTable slot 7 using the active draw/fill state. If the implementation callback wants to be more explicit while callback-table naming is still unsettled, document that `FillRect` is the wrapper for [UID:0000TN] slot `0x0069b3fc`; do not emit raw `dword_69B3FC` or generated `g_pfnLockSurface` in final source unless the broader Surface API pass requires raw globals.

## Ownership And Emitter Routing

Keep the existing metadata:

```text
CANONICAL_OWNER:0000BT
EMITTER_UIDS:0000BT
```

Rationale:

- `RectangleControlPane` class/file pages already route this method to `NexusTK/ui/controls/RectangleControlPane.cpp`.
- The primary vtable at `0x00618434` has slot `+0x44` / `0x00618478 -> 0x00499d00`.
- The only known construction route is `MiniMapDialog::RenderSymbols`, but MiniMap is a consumer, not the owner. The control is generic and reusable.
- `SurfaceRenderCallbackTable` owns the fill callback slot and implementations, not this virtual method.
- `Pane`/`BackPane` owns or documents the bounds helper, not this RectangleControlPane override.
- `ControlPane` is the base class and constructor dependency, not the derived method owner.

No owner migration is recommended.

## Recommended Target Metadata

Exact recommended metadata change for the target page:

```text
COMPLETION:86
CONFIDENCE:90
RECONSTRUCTABLE:TRUE
CANONICAL_OWNER:0000BT
EMITTER_UIDS:0000BT
```

This is intentionally conservative:

- raises completion from 84 to 86 to reflect resolved gate, helper names, source shape, and first-draft code readiness
- keeps confidence at 90 because exact original virtual/helper spelling remains inferred
- yields `(86 + 90) / 2 = 88.0`, clearing the current code-entry gate

The existing `84/90` average is already `87.0`, but raising the direct completion to at least 85 removes ambiguity for child/direct-parent 85/85 checks and replaces the stale below-95 rationale with the active gate model.

## Recommended First-Draft C++

Recommended replacement for the blank target C++ block:

```cpp
void RectangleControlPane::OnPaint()
{
    RectBounds bounds;
    GetBounds(&bounds);
    FillRect(&bounds);
}
```

Notes for implementation:

- `RectBounds` is preferred over Win32 `RECT` because the by-type struct docs already define the local four-int game/UI rectangle.
- `GetBounds` is the source-facing inherited accessor for `0x004b8e00`.
- `FillRect` is the source-facing wrapper for the SurfaceRenderCallbackTable slot `0x0069b3fc`.
- Do not preserve stack-cookie noise, `__int128`, raw callback globals, generated rename names, or Ghidra cleanup/destructor wording.

## Recommended Exact Target Doc Changes

Replace the stale no-code explanation with text equivalent to:

```text
Current gate note: this page is eligible for first-draft C++ under the active 85/85 code-entry gate. The previous below-95 no-code rationale is stale; 95+ remains a final-audit quality bar, not the threshold for emitting well-supported reconstruction code.
```

Add or update a source-shape note:

```text
Source shape: RectangleControlPane's primary paint virtual copies the inherited Pane bounds rectangle (`this + 0x44`, `RectBounds { left, top, right, bottom }`) through the shared bounds accessor at `0x004b8e00`, then fills those bounds through SurfaceRenderCallbackTable slot `0x0069b3fc` using the active GrafPort draw/fill color set by the constructor path.
```

Add or update generated/raw-name guidance:

```text
Name guidance: use `RectangleControlPane::OnPaint` (or `Paint` if the later virtual naming sweep standardizes that spelling). Reject generated `InvalidateControl`, raw `dword_69B3FC` / `g_pfnLockSurface`, and cleanup/destructor interpretations for this method.
```

Add or update ownership guidance:

```text
Ownership: keep `CANONICAL_OWNER:0000BT` and `EMITTER_UIDS:0000BT`. MiniMapDialog is the known consumer, SurfaceRenderCallbackTable owns the fill callback dependency, and Pane/BackPane owns the bounds helper dependency; none of those should own this RectangleControlPane override.
```

Optional support-doc sync for later callback:

- `by-memory/0x00499c60-0x00499d37.RectangleControlPaneCore.md`: replace its stale below-95 grouping-page wording with a note that the grouping page should stay an index/cluster while the child paint page is code-ready.
- `by-class/RectangleControlPane.md` and `by-file/RectangleControlPane.md`: update method-table wording from "paint/invalidating virtual" to "`OnPaint`/paint virtual" and reject generated `InvalidateControl`.

## Validator Needs

After the implementation callback edits the target page, run:

_Executable command block removed from the research report; preserved in [00011R-RectangleControlPanePaint-source-quality-removed.md](00011R-RectangleControlPanePaint-source-quality-removed.md)._

If class/file support docs are also touched in that callback, run scoped validator applies for those files too.

Expected generated stats after target metadata update:

```text
| `00011R` | 86 | 90 | 88.0 | `by-memory/0x00499d00-0x00499d37.RectangleControlPanePaint.md` |
```

## Exact Pending Coverage Text

Do not edit `by-memory/-coverage-report.md` during this B-agent pass. If the implementation callback updates the target page to the recommended metadata, replace the current coverage row with:

```text
    - [UID:00011R][0x00499d00-0x00499d37.RectangleControlPanePaint](by-memory/0x00499d00-0x00499d37.RectangleControlPanePaint.md) : reconstructable : 86% : strong : Exact `RectangleControlPane::OnPaint`/paint virtual; current source-quality pass resolves the stale 95/95 no-code note under the active gate, rejects generated `InvalidateControl` and cleanup interpretations, confirms vtable-only reachability at `0x00618478`, `RectBounds` copy from inherited Pane `+0x44` via `0x004b8e00`, fill through SurfaceRenderCallbackTable slot `0x0069b3fc`, RectangleControlPane owner/emitter route [UID:0000BT], first-draft C++ readiness, and `0x00499d37-0x00499d40` padding.
```

## Open Questions

- Exact original virtual spelling remains unproven. `OnPaint` is the best source-facing recommendation; `Paint` is the only likely alternate.
- Exact original wrapper for SurfaceRenderCallbackTable slot 7 remains unsettled. `FillRect` is the recommended source-facing wrapper name for this target, but the Surface API naming pass may later standardize a different spelling such as `FillRectangle`.
- Exact field/member spelling for bounds remains provisional (`m_bounds` vs `m_localBounds`), but `RectBounds` layout and inherited offset `+0x44` are strong enough for this method.
- IDA MCP should be rechecked if available during implementation, but current local/cached evidence is sufficient for the recommended target-page upgrade.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B005","command_id":"000000004165","destination_path":"executed-b-agent-research/B005/00011R-RectangleControlPanePaint-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:30","uid":"00011R"} -->
<!-- {"agent":"B005","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 00011R-RectangleControlPanePaint-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B005/00011R-RectangleControlPanePaint-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"00011R"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
