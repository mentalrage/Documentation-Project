** TARGET-REPORT-UID:0002QJ **
** AUTHOR-AGENT-ID:B005 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID0002QJ MapPaneLoadCompressedMapFile Empty-Emitter Source-Quality Research


## Finalized Report / Current Recommendation

- Implemented disposition: UID0002QJ remains one reconstructable `MapPane` member at `92/94`, owner/emitter UID00007Q, blank source position, and `Nested:0`; its former empty formal block now carries accepted Destination 1 exactly.
- Implemented split: validator command `000000011751` serially registered exact source child [UID:0004PZ][0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer](../../../../../by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md). The child is `92/94`, owner/emitter UID00007Q, true, blank position, stored `Nested:4` relative delta, and carries Destination 2. No placeholder or guessed UID remains.
- Implemented support synchronization: canonical `m_mapWidth`, `m_mapHeight`, and `m_tileBuffer` names now span UID0003TL Destination 3, MapPane class/file/layout/type, paired save, and both caller aggregates. UID00037Y and UID000232 nesting, helper, range, liveness, and history are synchronized without changing their accepted scores/routes.
- Callback state: all twelve ordinary destinations were leased, reread, edited, scoped-validated, and released serially. A readback-only class-prose correction was revalidated as `000000011802`; terminal waited refresh command `000000011803` completed afterward, and generated MapPane.cpp was reinspected read-only. Manual coverage changes at `15:58:36-04:00` were validator-owned side effects, not B005 edits. No report lifecycle or restricted command was run.
- Confidence: very strong for exact bytes, range, CFG, callers, branch ordering, file format, field offsets/types, helper split, APIs, and source route; high but capped for original lexical spelling and the semantic name of the ABI-preserved unused second argument.

## Supporting Research

- Queue basis checked read-only: evidence-time `auto-generated/-ag-research-tracker.md` listed UID0002QJ in `Not-Covered Files - Reconstructable` at `86/90`, reports `0`. Evidence-time `auto-generated/NexusTK/map/MapPane.cpp` contained one UID0002QJ Empty Emitter Marker.
- Supervisor recovery was followed by a fresh `idb_list` at `2026-07-14T14:43:45.4764215-04:00`. It returned exactly one active/adopted database session, `6b2e78f3`, for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `15716`, `is_analyzing:false`.
- `server_health` returned `status=ok`, module `NexusTK.exe`, image base `0x00400000`, and ready auto-analysis, Hex-Rays, and string cache (`2067` strings). These are evidence-time facts, not an assertion that the transient session remains alive after review or archival.
- Valid bounded `get_bytes`, function, analysis, disassembly, caller, xref, callee, type, entity, and support-function calls succeeded against session `6b2e78f3`. No valid bounded MCP request failed after recovery and no IDA mutation was requested.
- Exact old-report search terms actually used were `0002QJ`, `0x00504b90`, `MapPaneLoadCompressedMapFile`, `LoadCompressedMapFile`, `LoadMapFromFile`, `sub_504B90`, `sub_504b90`, `0002QK`, `0x00504e00`, `MapPaneSaveCompressedMapFile`, `SaveCompressedMapFile`, `compressed map`, `MapPane cache`, `0x0050e2a0`, `MapPaneAllocateTileBuffer`, `AllocateTileBuffer`, `ResetTileBufferStorage`, `MapPaneTileRecord`, `g_mapPaneCmapSignature`, and `PathExistsViaStat`.
- Search roots actually checked were central `executed-b-agent-research`; active `tools/leaser/Agents/Agent-B001/research` through `Agent-B005/research`; `tools/leaser/Agents/Older-Research`; `tools/leaser/Agents/SpecialReports`; and legacy `archived/b-agent-reports-20260623`.
- Every matching report was opened and classified:
  - `executed-b-agent-research/B015/0002PY-LogoPlayerPaneScalarDeletingDestructor-source-quality.md`: incidental, executed/current support for AUTOBUF destruction only; no UID0002QJ body evidence.
  - `executed-b-agent-research/B009/00039S-ScreenPanePresentationModeFlagSetters-source-quality.md`: incidental, executed/current teardown mention of `SaveCompressedMapFile`; no load-body evidence.
  - `executed-b-agent-research/B007/0002H7-ApplicationStartup-source-quality.md`: incidental, executed/current `PathExistsViaStat` consumer precedent only.
  - `executed-b-agent-research/B005/00037Y-MapPaneTileObjectGridRenderCore-source-quality.md`: direct support, executed/current; strongest prior evidence for exact helper `[0x0050e2a0,0x0050e2f4)`, `AllocateTileBuffer`, three load callers, and MapPane field offsets. It is not a direct UID0002QJ report.
  - `executed-b-agent-research/B009/0001AP-MapPaneWeatherCoordinateObjectCore-source-quality.md`: direct broad-range support, executed/current; exact target/save ranges and MapPane ownership, but no complete load source body.
  - `executed-b-agent-research/B005/0002XR-g_objectNationMap-source-quality.md`: incidental broad MapPane term match; no adopted target-specific fact.
  - `executed-b-agent-research/B005/0002UA-WideFormatWrapper-source-quality.md`: direct support, executed/current; proves `0x004236e0` is non-emitting fixed-260 CRT secure-format support and the source expression belongs in the caller.
  - `executed-b-agent-research/B008/0000O4-StartupLogoPanes-empty-emitter-family-source-quality.md`: incidental `PathExistsViaStat`/StdioFile consumer precedent only.
  - `executed-b-agent-research/B010/0000O9-StdioFile-empty-emitter-family-source-quality.md`: direct support, executed/current; exact StdioFile ownership, method inventory, and target caller route.
  - `executed-b-agent-research/B006/0001IQ-StdioFileConstructor-empty-emitter-source-quality.md`: direct support, executed/current; confirms the local StdioFile construction call at `0x00504c9b` and layout.
  - `executed-b-agent-research/B007/0000W2-ZlibPublicCompressChecksum-source-quality.md`: direct support, executed/current; exact lowercase zlib `compress`/`uncompress` identities and both cache call sites.
  - `executed-b-agent-research/B003/0000ZW-ChangeManRegister-source-quality.md`: incidental generated `SaveCompressedMapFile` call only.
  - `executed-b-agent-research/B007/0000FE-UniAPIInit-class-source-quality.md`: direct platform-table support, executed/current; dispatch-slot initialization and `PathExistsViaStat` context.
  - `executed-b-agent-research/B011/0003TL-MapPaneHandleMapInfoTileRegionPacket-source-quality.md`: direct support, executed/current; establishes `MapPaneTileRecord` and the three conflicting newer field aliases. Its behavior/body remain accepted; only aliases require synchronization.
  - `executed-b-agent-research/B002/0002QH-0002QI-0002QO-0002QP-0002QQ-000231-MapPaneTeardownObjectPacket-source-quality.md`: direct caller support, executed/current; confirms teardown-side save use, not the load body.
  - `executed-b-agent-research/B005/0001AV-SaveUserSettings-empty-emitter-source-quality.md`: direct source-shape support, executed/current; confirms caller-local `swprintf_s`, `SHGetFolderPathW`, and `g_pfnCreateDirectoryW` policy.
  - `executed-b-agent-research/B005/0001AR-DrawVisibleTilesHelper-source-quality.md`: incidental MapPane tile-buffer support; no exact target-body addition.
  - `executed-b-agent-research/B004/00019E-AUTOBUFUnsignedCharConstructor-source-quality.md`: direct support, executed/current; confirms local `_AUTOBUF<unsigned char>` constructor, `m_data`, and `m_count` at the target.
  - `executed-b-agent-research/B004/0000L3-MapPane-empty-emitter-family-source-quality.md`: direct broad source-family support, executed/current. Its exact path/save/load outline and signature-global evidence remain useful; its statement that the target name/body blockers remained unresolved is superseded by this bounded pass.
  - `executed-b-agent-research/B002/0002I7-0002QF-mappane-lifecycle-source-quality.md`: direct lifecycle support, executed/current; confirms save calls, map fields, and teardown behavior.
  - `executed-b-agent-research/B001/0001AW-mappane-packet-source-split-continuation-batch6.md`: direct caller support, executed/current; confirms the packet-side save/load transition route.
  - `executed-b-agent-research/B001/00022K-FileExists.md`: direct utility support, executed/current; accepted `PathExistsViaStat` source ownership/name.
  - `executed-b-agent-research/B002/00015U-BuildFieldMapPath-source-quality.md`: incidental path-helper ownership comparison only; it does not own this cache-path construction.
- No matches existed in any active Agent-B*/research root, `Older-Research`, `SpecialReports`, or the checked legacy archived root. Evidence-based conclusion: no prior direct standalone UID0002QJ report exists. UID00037Y and UID0000L3 are direct support reports, and all other matches are support/incidental history as classified above.

## Target

- Target UID: `0002QJ`.
- Target path: `by-memory/0x00504b90-0x00504df1.MapPaneLoadCompressedMapFile.md`.
- Exact half-open machine-code range: `[0x00504b90,0x00504df1)`, `0x261` / 609 bytes.
- Current metadata: `92/94`, canonical owner UID00007Q, reconstructable true, emitter UID00007Q, blank position, Destination 1 formal C++, `Nested:0`.
- Current source route: [UID:00007Q][MapPane](../../../../../by-class/MapPane.md) through [UID:0000L3][MapPane](../../../../../by-file/MapPane.md), `NexusTK/map/MapPane.cpp`.
- Current generated state: terminal refresh command `000000011803` emits exactly one UID0002QJ source row at generated line 319 and one definition at line 320, with no UID0002QJ Empty Emitter Marker.

## Current Target State

- The page preserves its exact range, owner, route, path family, two callers, CMAP header, StdioFile/AUTOBUF/zlib dependencies, and three machine-code calls to the helper start. Completion/confidence is now `92/94`, and Destination 1 replaces the prior blank formal.
- Source-quality blockers are closed: the method is `void MapPane::LoadCompressedMapFile(unsigned short mapId, int /*unusedLoadOption*/)`; the registered helper is UID0004PZ `void MapPane::AllocateTileBuffer()`; fields are `m_mapWidth`, `m_mapHeight`, and `m_tileBuffer`; the type is `MapPaneTileRecord`; and platform/source helpers are `g_pfnCreateDirectoryW`, `PathExistsViaStat`, caller-local `swprintf_s`, `g_mapPaneCmapSignature`, and lowercase zlib `uncompress`.
- Exact behavior remains explicit: only missing-cache, bad-signature/dimension, and valid-before-inflate categories allocate zeroed tile storage. Documents lookup or either directory-create failure returns before allocation; StdioFile `Open` can throw; short reads and zlib failures are not tested.
- UID0004PZ is independently source-bearing under broad aggregate UID00037Y. It is not folded into UID0002QJ, left as a raw label, duplicated inline as an independent helper definition, or renamed as Reset/Reallocate behavior.
- Generated output confirms one target definition, one helper definition, no target/helper marker, and the accepted source route. The older blank-formal, unresolved-name, report-only, and future-registration statements are historical pre-callback facts.

## Executive Recommendation

- Destination 1 is applied with the exact two-argument ABI; normal body instructions still do not read the second argument.
- UID0004PZ is serially registered for exact `[0x0050e2a0,0x0050e2f4)` and emits Destination 2.
- UID00037Y remains the `87/89` comment-only method-cluster aggregate with complete UID0004PZ liveness/no-free/range evidence. UID000232 changed only stored `Nested:4 -> 0`, preserving cumulative address-sort depth.
- Destination 3 is applied to UID0003TL, replacing only `m_mapWidthTiles`, `m_mapHeightTiles`, and `m_mapTileRecords` with canonical `m_mapWidth`, `m_mapHeight`, and `m_tileBuffer` while preserving the rest of its body and metadata.
- MapPane class/file scores and the class's blank formal block remain unchanged; exact declaration/dependency contracts are prose because a partial 124-method class shell would be invalid.
- Paired save UID0002QK remains `86/90` with blank formal C++; its exact names, direct-CRT behavior, failure retention, successful free/clear, monster-cache behavior, and load contrast are synchronized without adding save C++.

## Supervisor Active Recheck

- Supervisor Gate 1 accepted the exact report-only artifact SHA `1316CB6D89DC9083B535E7D6E98238141F8E363656F72D969431964412C3CF4E`; this section records the completed, separate implementation callback against that scope.
- Mandatory MCP evidence was collected during healthy session `6b2e78f3`; callback implementation did not require IDA mutation or additional MCP evidence.
- Destinations 1-3 are the only applied C++ changes. Every other accepted change is metadata/prose/nesting/support synchronization or verify-only disposition.
- Real helper UID0004PZ was issued before cross-linking. All six report placeholders were replaced, all ordinary validators passed, final generation completed, and no B005 lease remains.

## Inference Research Guidance Check

- `by-structure.md` was applied as a graph/range contract: half-open ranges, independent child identity, validator-issued UIDs, owner/emitter separation, address-sort order, `Nested` as a cumulative relative delta, and exact destination-specific formal C++.
- Existing narratives and decompiler labels were treated as hypotheses. Exact ranges, bytes, reads, offsets, calls, and branch order are direct evidence; method/helper/member names and source organization are ranked inference constrained by current accepted project interfaces.
- No source name was left as an unresolved blocker. Each selected name is ranked below against alternatives, and lexical uncertainty is represented by confidence caps rather than blank code.
- No Wave2/Wave3 statement was used as authoritative evidence. Current MCP, current ordinary docs, current generated output, and executed reports control this recommendation.
- The target is one modeled source method, so blank/covered-by disposition was rejected. The local helper is independently callable source code, so inline-only or parent-only disposition was also rejected.

## Heuristic / Inference Reanalysis And Validation

- **Method identity:** paired `SaveCompressedMapFile`, current title, direct caller roles, and path/file semantics make `LoadCompressedMapFile` the highest-probability source name. Historical `LoadMapFromFile` in AUTOBUF support is less symmetric and is superseded; `LoadCompressedMapCache` is descriptive but unsupported by project naming.
- **Signature:** IDA models `void __thiscall(int this, unsigned __int16, int)` and the body ends `ret 8`. Both callers pass two explicit arguments. The first is the map id. The second is caller-provided state (`a7` in the map-change path, zero in the packet path) but no normal target instruction reads it. `int /*unusedLoadOption*/` preserves ABI/source surface without inventing a runtime use.
- **Return:** source return is `void`. Callers ignore EAX, IDA's current target prototype is void, and all exits are control-only. Any decompiler residue from final callees is not a semantic return value.
- **Early gates:** `SHGetFolderPathW` must return `S_OK`. Each `g_pfnCreateDirectoryW` failure is accepted only when `GetLastError()==ERROR_ALREADY_EXISTS`. Those three early failures return without allocating or clearing `m_tileBuffer`.
- **Path construction:** `0x004236e0` is fixed-260 secure CRT support generated from caller-local formatting. Source must use three local `wchar_t[MAX_PATH]` buffers and caller-local `swprintf_s`, not call a NexusTK `WideFormatWrapper` method.
- **File error behavior:** missing file calls `AllocateTileBuffer` and returns. Existing files use `StdioFile::Open`; `Open` may throw `FileError`. The target has compiler EH cleanup but no source catch, so no catch/fallback is added.
- **Read behavior:** every `StdioFile::Read` result is ignored. A truncated file can leave part of the four-byte signature or two dimension locals unspecified. Source must not add read-count checks, initialization, file-size checks, or rollback.
- **Header/dimensions:** exactly four bytes are compared with `g_mapPaneCmapSignature` through `strncmp`; two little-endian host-order `unsigned short` values follow and must equal `m_mapWidth`/`m_mapHeight`.
- **Allocation timing:** invalid signature/dimensions calls `AllocateTileBuffer` once before cleanup. Valid data also calls it once before constructing/reading the compressed AUTOBUF. The helper does not free the prior pointer; adding delete/free would alter behavior.
- **Inflate behavior:** output capacity is exactly `sizeof(MapPaneTileRecord) * cachedWidth * cachedHeight`, i.e. six bytes per tile. The zlib `uncompress` return code and output-length mutation are ignored. Failure can leave the newly zeroed output partially modified; no status branch is added.
- **Helper identity:** exact callers at `0x00504c8b`, `0x00504d3e`, and `0x00504dc5` all occur in UID0002QJ. Current UID00037Y already ranks `AllocateTileBuffer` highest. `ResetTileBufferStorage` is rejected because the helper never releases prior storage; `AllocateMapTileBuffer` is plausible but less consistent with accepted project wording.
- **Field names:** constructor and many emitted MapPane methods already use `m_mapWidth`, `m_mapHeight`, and `m_tileBuffer`. Only UID0003TL introduced `m_mapWidthTiles`, `m_mapHeightTiles`, and `m_mapTileRecords`. Canonicalizing the outlier avoids two names for the same offsets and makes target/helper/packet code internally consistent.
- **Tile type:** UID0004HP proves `MapPaneTileRecord` is exactly six bytes with three `unsigned short` members. `MapTileRecord` is a different four-byte MapTileImageLib resource row and is rejected here.
- **Source order/declarations:** `MapPaneTileRecord` and `g_mapPaneCmapSignature` emit before the final marker tail in MapPane.cpp; UID0002QJ and UID0004PZ now emit exact bodies earlier in address order. The external/manual MapPane header contract includes the exact load method, private allocation helper, complete tile record, and canonical fields. The MapPane class formal stays blank because a two-method fragment would conflict with the accepted 124-method inventory.
- **Paired save:** save independently corroborates the same path, `CMAP` signature, dimensions, six-byte raw tile size, fields, and zlib family. Its direct CRT `_wfopen`/`fwrite`/`fclose`, output-capacity, failure retention, tile free, and monster-image clear contract are not silently converted to StdioFile or absorbed into the load body.
- **Compiler/EH exclusion:** SEH registration, security-cookie setup/check, cleanup funclets, AUTOBUF/StdioFile destructor calls, allocator overflow lowering, and final `ret 8` are compiler consequences. Source uses automatic objects, array allocation, and ordinary returns; it does not hand-author these mechanics.
- **No third-party import:** the target is NexusTK integration code around bundled zlib, not third-party zlib implementation. Existing zlib headers/library supply `uncompress`; no import directive or third-party source copy applies.

## Evidence Standards Used

- Direct evidence: fresh MCP database identity/health; exact bytes and SHA-256; function bounds; disassembly/instruction count; basic blocks and complexity; callers/callees/xrefs; decompilation; bounded predecessor/successor/helper/save reads; type/entity negatives; current generated output; and exact support-document formals.
- Corroborating evidence: paired save behavior; constructor/packet/teardown callers; accepted StdioFile/AUTOBUF/zlib/platform/global/type pages; current MapPane field usage; and executed reports classified under Supporting Research.
- Inference is restricted to source-facing lexical names, access level, and header organization. It does not change observed branches, argument widths/counts, file layout, field offsets, ignored results, allocation timing, or ownership.
- Scores below 95 reflect missing original symbols/source and the unresolved semantic meaning, not ABI shape, of the unused second parameter. These limitations do not justify blank C++.

## Evidence Checked

- MCP: fresh `idb_list`; `server_health`; target/save/helper/padding `get_bytes`; `lookup_funcs`; `analyze_function`; `basic_blocks`; disassembly/decompilation; caller/callee/xref queries; bounded string/data/global checks; and negative type/entity queries.
- Current ordinary documentation: UID0002QJ, UID0002QK, UID00037Y, UID000232, UID0003TL, UID00007Q, UID0000L3, UID00042K, UID0004HP, UID0001AT, UID0001AW, UID0002UA, UID0001IZ, StdioFile class/file/children, AUTOBUF file/template/constructor, zlib, CMAP global/storage, WideApi dispatch global/storage, resource strings, caller/teardown pages, and adjacent ranges.
- Generated/tracker evidence read-only: current `auto-generated/NexusTK/map/MapPane.cpp`, `auto-generated/-ag-research-tracker.md`, and manual memory/class/file/struct coverage rows.
- Old reports: every matching path listed and classified under Supporting Research; no direct standalone UID0002QJ report exists.
- Negative checks: no recovered MapPane/StdioFile/AUTOBUF UDT in IDA; no named entity in the target/helper neighborhoods; no helper caller outside the three target sites; no additional signature-pointer consumer beyond load/save; no normal read of the second target argument; no helper free; no source catch; no read/zlib status test; no target-owned predecessor/successor padding; and no reason to emit compiler cleanup.
- Failed/unavailable mandatory checks: none after recovery. Two malformed client-side `get_bytes` argument-shape attempts returned immediate schema/parameter diagnostics before the valid `regions[{addr,size}]` call; they were not valid bounded evidence requests and caused no fallback conclusion.
- Historical report-only state: no validator or lease existed before Gate 1. The completed callback validator/lease/generated evidence is recorded under Validator Results without altering the earlier research evidence.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | Preserve exact target range `[0x00504b90,0x00504df1)`, 609 bytes, SHA-256 `969B47474AB5797643D77CD735C17039E8C68E447F07C9309EF27214468CEF4B`. | 99 | MCP bytes/hash | UID0002QJ exact evidence | incorporate | applied |
| C02 | Preserve one `0xcc` predecessor byte and fifteen-byte successor padding with SHA-256 `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C`. | 99 | bounded bytes/functions | UID0002QJ range/padding | incorporate | applied |
| C03 | Record 165 target instructions, 21 analyzed blocks including EH/external cleanup chunks, main-flow 14-block partition, and cyclomatic complexity 7. | 98 | analyze/disasm/CFG | UID0002QJ MCP evidence | incorporate | applied |
| C04 | Record exactly two callers/xrefs at `0x0050bcad` and `0x00510746`, including the first caller's nonzero-capable second argument and the packet caller's zero. | 99 | callers/xrefs/caller disasm | UID0002QJ, UID0001AT, UID0001AW | incorporate | applied |
| C05 | Use `void MapPane::LoadCompressedMapFile(unsigned short mapId, int /*unusedLoadOption*/)` and historicalize unresolved-name/return claims. | 96 | ABI, callers, paired name | UID0002QJ Destination 1/class/file prose | incorporate | applied |
| C06 | Preserve Documents/base/maps path construction and exact early-return state: no tile allocation on Documents or directory-create failure. | 99 | CFG/decompile/calls | UID0002QJ behavior | incorporate | applied |
| C07 | Use caller-local `swprintf_s`, `g_pfnCreateDirectoryW`, `GetLastError`, and `PathExistsViaStat`; do not emit UID0002UA as product code. | 98 | calls/support reports | UID0002QJ/support prose | incorporate | applied |
| C08 | Preserve four-byte CMAP compare, two uint16 reads, ignored read counts, uninitialized-short-read edge, and exact dimension checks. | 98 | decompile/disasm/Stdio API | UID0002QJ Destination 1/evidence | incorporate | applied |
| C09 | Preserve exactly three machine-code `AllocateTileBuffer` call sites: miss, shared invalid header/dimensions, and valid-before-inflate; no early-gate allocation. | 99 | helper xrefs/CFG | UID0002QJ/helper | incorporate | applied |
| C10 | Preserve `ReadRemaining`, six-byte output size, lowercase zlib `uncompress`, and ignored return/output-length behavior. | 99 | calls/paired format/type | UID0002QJ Destination 1 | incorporate | applied |
| C11 | Preserve StdioFile `Open` throw propagation and automatic EH cleanup; add no source catch, rollback, or manual destructor. | 97 | Stdio docs/EH chunks | UID0002QJ evidence/negative | incorporate | applied |
| C12 | Split exact helper `[0x0050e2a0,0x0050e2f4)`, 84 bytes, SHA-256 `747DA193988F95133903DFDA6AF7FFA6E22F427BF68A213DFD5E5E75B8B95929`, one block, only three target calls. | 99 | bytes/hash/CFG/xrefs | UID0004PZ | incorporate | applied |
| C13 | Name helper `MapPane::AllocateTileBuffer`; preserve `new MapPaneTileRecord[count]`, zeroing, no prior free, void semantics, and lexical cap. | 96 | helper body/UID00037Y/type | UID0004PZ Destination 2 | incorporate | applied |
| C14 | Register the helper serially with validator-issued UID0004PZ, `92/94`, owner/emitter UID00007Q, true, blank position, stored `Nested:4` (+4 relative delta). | 98 | split/graph rules | UID0004PZ metadata | incorporate | applied |
| C15 | Keep UID00037Y `87/89`, owner/emitter/formal/`Nested:-4`; add exact helper child/liveness/source evidence and update its blank Item Summary. | 98 | current aggregate/helper facts | UID00037Y | incorporate | applied |
| C16 | Change only UID000232 stored `Nested:4 -> 0` so cumulative address-sort depth remains unchanged after the new child. | 99 | validator nesting contract | UID000232 | incorporate | applied |
| C17 | Canonicalize `m_mapWidth` `+0x3f4`, `m_mapHeight` `+0x3f6`, and `MapPaneTileRecord *m_tileBuffer` `+0x3f8`; reject duplicate newer aliases. | 98 | constructor/generated/many consumers | target/helper/class/file/layout/type | incorporate | applied |
| C18 | Apply Destination 3 to UID0003TL, changing only three field aliases in its complete accepted body; preserve `90/90` and all other metadata/evidence. | 99 | current formal/project consistency | UID0003TL | incorporate | applied |
| C19 | Synchronize UID00007Q with exact load/helper declarations, fields, branches, and dependencies; preserve `89/89` and blank complete-class formal disposition. | 96 | 124-method surface/current policy | by-class/MapPane.md | incorporate | applied |
| C20 | Synchronize UID0000L3 with target/helper/save order, includes/dependencies, generated contract, and historical aliases; preserve `89/85`, path, and file metadata. | 97 | source graph/generated | by-file/MapPane.md | incorporate | applied |
| C21 | Synchronize UID00042K and UID0004HP to one canonical field/type vocabulary; preserve scores, owner/emitter, and formal blocks. | 98 | exact offsets/record layout | MapPaneLayout/MapPaneTileRecord | incorporate | applied |
| C22 | Synchronize paired UID0002QK with exact same fields/path/signature and its distinct direct-CRT/compress/free/monster-clear/failure contract; leave `86/90` and blank formal unchanged. | 98 | save bytes/hash/decompile/callers | UID0002QK | incorporate | applied |
| C23 | Synchronize UID0001AT and UID0001AW caller facts with exact load signature, argument values, save-before-load ordering, and no return use; scores/formals unchanged. | 98 | caller disassembly | caller aggregates | incorporate | applied |
| C24 | Preserve `g_mapPaneCmapSignature` and storage pages unchanged; they already emit exact `static const char *...="CMAP"` and have exactly load/save consumers. | 99 | global formal/xrefs | UID0002ZJ/UID0002ZN verify-only | already-present | already-present |
| C25 | Preserve accepted StdioFile and AUTOBUF declarations unchanged: void Open, size_t Read, int ReadRemaining by reference, m_data/m_count. | 99 | current formal blocks/calls | StdioFile/AUTOBUF verify-only | already-present | already-present |
| C26 | Preserve current UID0002UA, WideApi, PathExistsViaStat, zlib, resource-string, and dispatch-storage ownership/formals; add consumer prose only where exact target context is missing. | 98 | current support docs/MCP | dependency pages | already-present | already-present |
| C27 | Preserve helper-neighborhood partition: 12 `0xcc` bytes, route-negative wrapper `[0x0050e300,0x0050e316)`, 10 `0xcc` bytes, then UID000232 at `0x0050e320`; do not absorb or duplicate them. | 99 | exact bytes/entity/xref checks | UID00037Y range analysis | incorporate | applied |
| C28 | Preserve target owner/emitter UID00007Q, reconstructable true, blank position, `Nested:0`, and `NexusTK/map/MapPane.cpp` route. | 99 | receivers/callers/source graph | UID0002QJ metadata | incorporate | applied |
| C29 | Raise target only `86/90 -> 92/94`, apply exact Item Summary and Destination 1, replacing the Empty Emitter Marker. | 98 | blocker closure/body | UID0002QJ | incorporate | applied |
| C30 | Create helper at `92/94` with exact Item Summary and Destination 2; never guess/pre-reference its UID before registration. | 98 | exact helper proof/workflow | UID0004PZ | incorporate | applied |
| C31 | Preserve all positive/negative evidence and historicalize stale unresolved-name, 95/95-gate, all-failures-reset, `LoadMapFromFile`, alias, helper-free/reset, uppercase `Uncompress`, and wrapper-method assumptions. | 98 | current reanalysis | all changed docs/history | historicalize | applied |
| C32 | Require generated MapPane.cpp to contain exactly one target and helper definition, corrected UID0003TL aliases, no target/helper marker, unchanged class/save markers, and no handwritten ABI/EH mechanics. | 98 | emitter graph/current generated | final generated verification | incorporate | applied |
| C33 | Supply exact supervisor-owned manual coverage replacements/additions for every changed scored/split page; B005 never edits coverage. | 99 | manual reports inspected | exact coverage text section | incorporate | applied |
| C34 | Declare third-party import not applicable; existing bundled zlib/header route supplies `uncompress`, while target/helper remain NexusTK source. | 99 | ownership/source boundary | source placement/follow-up | not-applicable | excluded-with-reason |
| C35 | Lease one ordinary page, edit/validate/release serially; register helper before references; run scoped validators and one final waited refresh; update this same report. | 99 | workflow requirement | callback checklist/results | incorporate | applied |

### Callback Verification Notes

| Claim | Destination-specific proof |
| --- | --- |
| C01 | UID0002QJ exact-range/hash evidence is retained in its callback section; validator `000000011753` passed and final generation identifies the same path/range. |
| C02 | UID0002QJ predecessor/successor padding evidence and successor hash remain unchanged; no adjacent or ignored page was edited. |
| C03 | UID0002QJ retains the 165-instruction, 21-block/14-main-block, complexity-7 evidence and compiler-chunk separation. |
| C04 | UID0002QJ plus validated UID0001AT (`000000011781`) and UID0001AW (`000000011783`) record only `0x0050bcad`/`0x00510746`, with `a7` versus zero. |
| C05 | Destination 1 emits one `void MapPane::LoadCompressedMapFile(unsigned short, int)` definition; class/file prose carries the exact contract and stale name/return claims are historical. |
| C06 | UID0002QJ exact behavior and Destination 1 preserve Documents/base/maps path gates and pre-allocation early returns. |
| C07 | UID0002QJ uses caller-local formatting and accepted platform/path helpers; UID0002UA was reread and remains verify-only/non-emitting product support. |
| C08 | UID0002QJ preserves four-byte signature compare, two uninitialized 16-bit locals, ignored `Read` results, and exact dimension checks. |
| C09 | UID0002QJ/UID0004PZ retain the three machine xrefs `0x00504c8b`, `0x00504d3e`, `0x00504dc5`; no Documents/directory gate calls the helper. Destination 1 preserves the accepted miss, invalid-signature/dimension, and valid allocation categories. |
| C10 | Destination 1 retains `ReadRemaining`, six-byte `MapPaneTileRecord` sizing, lowercase `uncompress`, and ignored status/output-length mutation. |
| C11 | UID0002QJ documents propagated StdioFile `Open` failure and compiler-only cleanup; generated negative scans found no explicit EH/cookie/destructor mechanics. |
| C12 | UID0004PZ is the registered exact 84-byte child with accepted SHA, one block, three target machine callers, and no external route; validators `000000011751`/`11752` passed. |
| C13 | UID0004PZ carries Destination 2 exactly, including six-byte array allocation, zeroing, no prior free, void behavior, and lexical cap. |
| C14 | Registration command `000000011751` issued UID0004PZ before cross-links; correction command `000000011752` stored validator syntax `Nested:4`; metadata is `92/94`, UID00007Q/UID00007Q, true, blank position. |
| C15 | UID00037Y validator `000000011758` passed with `87/89`, original owner/emitter/comment formal/`Nested:-4`, exact helper child and nonblank aggregate summary. |
| C16 | UID000232 validator `000000011760` passed after only `Nested:4 -> 0` plus closure note; score, body, owner/emitter, route, and evidence remain intact. |
| C17 | Validated target/helper/class/file/layout/type pages use canonical offsets and names; final generated counts for all three superseded aliases are zero. |
| C18 | UID0003TL validator `000000011755` passed; its complete formal differs only by the three accepted aliases and generated output contains one canonical body. |
| C19 | MapPane class validators `000000011771` and corrective `000000011802` passed at unchanged `89/89`; blank formal remains, and the exact unsigned-short signature/three machine call sites/load-helper-field-dependency evidence is current. |
| C20 | MapPane file validator `000000011773` passed at unchanged `89/85`/path; exact source order, dependency, generated, alias, paired-save, and no-import evidence is present. |
| C21 | MapPaneLayout `000000011775` and MapPaneTileRecord `000000011777` passed with unchanged scores/formals/metadata and canonical six-byte consumer evidence. |
| C22 | UID0002QK validator `000000011779` passed at unchanged `86/90`, blank formal, and `Nested:0`; direct CRT/lowercase `compress`/failure-retention/free-clear/monster-cache/load contrast is synchronized. |
| C23 | UID0001AT/UID0001AW validators `000000011781`/`11783` passed with unchanged scores/formals/routes and exact ordered caller facts. |
| C24 | UID0002ZJ/UID0002ZN were reread: exact `"CMAP"` declaration/covered-by marker and only load/save xrefs remain current; no edit was needed. |
| C25 | StdioFile class/file/Open/Read/ReadRemaining and AUTOBUF class/file/template/constructor were reread; accepted void/size/reference and `m_data`/`m_count` contracts remain current and unchanged. |
| C26 | UID0002UA, UID0001IZ, UID0000W2, UID0000TQ/UID0002AS, UID0002ST, teardown, and adjacent docs were reread; ownership/formals already match and remain verify-only. |
| C27 | UID00037Y now states 12-byte padding, exact route-negative wrapper, ten-byte padding from `0x0050e316`, then UID000232; class/file off-by-one prose was corrected and validated. |
| C28 | UID0002QJ validator `000000011753` preserved owner/emitter UID00007Q, true, blank position, `Nested:0`, exact path, and `NexusTK/map/MapPane.cpp` route. |
| C29 | UID0002QJ is exactly `92/94` with accepted Item Summary/Destination 1; final MapPane.cpp has one definition and no UID0002QJ marker. |
| C30 | UID0004PZ is exactly `92/94` with accepted Item Summary/Destination 2; all six report placeholders were replaced only after `000000011751`. |
| C31 | Target, aggregate, class/file/layout/type, save, and report sections retain negative/history detail while labeling stale blank gate, names, aliases, all-failures, Reset/free, uppercase, and wrapper assumptions superseded. |
| C32 | Terminal command `000000011803` generated MapPane.cpp SHA-256 `A478CF6D113BE16F165A5ED15551C59636DE28F376C29A0C92D7EA8C15203913`; exact assertions are listed under Validator Results. |
| C33 | Exact supervisor-owned text below now uses real UID0004PZ. B005 never manually edited a coverage/tracker page; validator-owned coverage refresh is separately recorded. |
| C34 | Excluded with reason: bundled zlib supplies `uncompress`; target/helper are NexusTK integration code and no import/copy directive applies. |
| C35 | All ordinary pages followed lease/reread/scoped-validator/release; no lease was held for terminal waited command `000000011803`, the report is reconciled, and no lifecycle command ran. |

## Positive Evidence Summary

- Exact target and paired-save bytes, boundaries, hashes, callers, and branch families were freshly reconfirmed from the active NexusTK IDB.
- Load and save share path strings, CMAP pointer, width/height fields, six-byte tile payload, and zlib family while retaining deliberately different file-wrapper and post-success behavior.
- Target callers are both MapPane state transitions and pass the map id plus one ABI-preserved int; no external owner or utility route exists.
- The allocation helper is exact, modeled, source-shaped, and live through three target calls. Its three field reads/writes align with constructor, render, collision, packet, and save consumers.
- Current generated output and ordinary docs overwhelmingly use the canonical old field names; UID0003TL is the isolated alias conflict.
- Accepted StdioFile, AUTOBUF, zlib, WideApi, CMAP, and MapPaneTileRecord pages provide complete source-facing interfaces for a human load body.

## IDA MCP Facts

- Evidence session: database `6b2e78f3`, discovered fresh; `server_health status=ok`, analysis/Hex-Rays/strings ready.
- Target lookup: `sub_504B90`, modeled size `0x261`, `[0x00504b90,0x00504df1)`, IDA prototype `void __thiscall(int this, unsigned __int16, int)`.
- Target disassembly: 165 instructions. Analyzer reports 21 blocks and cyclomatic complexity 7; fourteen blocks are normal in-range source flow and seven are EH/external cleanup/leaf chunks.
- Main in-range block starts/ends: `504b90-504bdb`, `504bdb-504c09`, `504c09-504c1a`, `504c1a-504c48`, `504c48-504c59`, `504c59-504c89`, `504c89-504c95`, `504c95-504cec`, `504cec-504d28`, `504d28-504d3c`, `504d3c-504dc3`, `504dc3-504dca`, `504dca-504dd5`, and `504dd5-504df1`.
- Analyzer-associated cleanup/external chunks include `0x004e5c10-0x004e5c2c`, `0x00602261-0x0060226c`, `0x0060226c-0x00602277`, `0x00602277-0x0060229f`, and external leaves at `0x004f4a90`, `0x005820a0`, and `0x005c956c`. They are not additional target source bodies.
- Target callers/xrefs: only `0x0050bcad` in `sub_50BBB0` and `0x00510746` in `sub_5104D0`.
- Signature pointer `0x0066db48`: exactly target compare `0x00504ccf` and save write `0x00504f41`.
- Helper lookup: one modeled function `[0x0050e2a0,0x0050e2f4)`, one block, complexity 1, exactly target calls `0x00504c8b`, `0x00504d3e`, `0x00504dc5`, and no other xref.
- Negative IDB type queries for `*MapPane*`, `*StdioFile*`, and `*_AUTOBUF*` returned no UDTs. Bounded entity queries returned no recovered names in target/helper neighborhoods.

## Exact Byte Inventory

| Range | Size | SHA-256 | Exact head/tail or classification |
| --- | ---: | --- | --- |
| target `[0x00504b90,0x00504df1)` | 609 | `969B47474AB5797643D77CD735C17039E8C68E447F07C9309EF27214468CEF4B` | head `55 8B EC 6A FF 68 77 22 60 00 64 A1 00 00 00 00 50 81 EC 44 06 00 00 A1 24 2F 67 00 33 C5 89 45`; tail `CB D2 07 00 8B 4D F4 64 89 0D 00 00 00 00 59 5E 8B 4D F0 33 CD E8 44 29 0C 00 8B E5 5D C2 08 00` |
| target successor `[0x00504df1,0x00504e00)` | 15 | `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C` | fifteen `CC` bytes |
| paired save `[0x00504e00,0x00505075)` | 629 | `595B2258168B19996F00835595DA04821ED0E043FB58082639C605D8FAEAF1BD` | head `55 8B EC 6A FF 68 AA 22 60 00 64 A1 00 00 00 00 50 81 EC 34 06 00 00 A1 24 2F 67 00 33 C5 89 45`; tail ends `... 8B E5 5D C3` |
| save successor `[0x00505075,0x00505080)` | 11 | `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943` | eleven `CC` bytes |
| helper `[0x0050e2a0,0x0050e2f4)` | 84 | `747DA193988F95133903DFDA6AF7FFA6E22F427BF68A213DFD5E5E75B8B95929` | head `56 8B F1 33 C9 0F B7 96 F4 03 00 00 0F B7 86 F6 03 00 00 0F AF C2 BA 06 00 00 00 F7 E2 0F 90 C1`; tail `F6 03 00 00 0F AF D1 89 86 F8 03 00 00 8D 0C 52 03 C9 51 6A 00 50 E8 C1 BD 0B 00 83 C4 10 5E C3` |
| helper successor neighborhood `[0x0050e2f4,0x0050e320)` | 44 | `B12979E62ADC3E8C9E3E6B711F2D5EE41C8887684A4AAD514A8ABCDDD8B43054` | 12 `CC`; route-negative wrapper `[0x0050e300,0x0050e316)`; 10 `CC` |

- The prior modeled function ends at `0x00504b8f`; byte `0x00504b8f` is one `CC` alignment byte before the target.
- The route-negative `0x0050e300` wrapper reads the object-list field at `this+0x424`, returns when null, otherwise tail-jumps to UID00023D. It has no modeled-function entry or proven route and remains aggregate evidence, not part of the new allocation child.

## Function / Child Inventory

| Range | Current/source identity | Disposition |
| --- | --- | --- |
| `[0x00504b70,0x00504b8f)` | UID0002QI MapPaneTagDetachMessage | predecessor, unchanged |
| `[0x00504b8f,0x00504b90)` | one-byte alignment | ignored/preserved |
| `[0x00504b90,0x00504df1)` | UID0002QJ `MapPane::LoadCompressedMapFile` | one complete source method, Destination 1 |
| `[0x00504df1,0x00504e00)` | fifteen-byte alignment | ignored/preserved |
| `[0x00504e00,0x00505075)` | UID0002QK `MapPane::SaveCompressedMapFile` | paired support; blank formal unchanged in this scope |
| `[0x00505075,0x00505080)` | eleven-byte alignment | ignored/preserved |
| `[0x0050bce0,0x0050e320)` | UID00037Y MapPane tile/object/grid cluster | comment-only aggregate, unchanged metadata/formal |
| `[0x0050e2a0,0x0050e2f4)` | `0004PZ` `MapPane::AllocateTileBuffer` | new exact source child, Destination 2 |
| `[0x0050e2f4,0x0050e300)` | twelve-byte alignment | retained under aggregate, no child duplication |
| `[0x0050e300,0x0050e316)` | route-negative object-list lighting wrapper | retained aggregate evidence, no source body in this assignment |
| `[0x0050e316,0x0050e320)` | ten-byte alignment | retained under aggregate |
| `[0x0050e320,0x0050e4b6)` | UID000232 `MapPane::RequestObjectEffect` | source body unchanged; nesting closure only |

## Direct Xref / Caller Inventory

| Callee | Call site | Exact caller-side facts |
| --- | --- | --- |
| UID0002QJ | `0x0050bcad` in `sub_50BBB0` | after save and map-id/dimension state update, pushes caller argument `a7`, then 16-bit map id; ignores return |
| UID0002QJ | `0x00510746` in `sub_5104D0` | packet path pushes `0`, pushes map id in EBX, sets ECX to MapPane, calls; ignores return |
| `AllocateTileBuffer` | `0x00504c8b` | final path absent branch; allocates zeroed current-dimension tile storage and exits |
| `AllocateTileBuffer` | `0x00504d3e` | valid signature/dimensions branch before AUTOBUF/read/inflate |
| `AllocateTileBuffer` | `0x00504dc5` | shared invalid signature/dimension branch before StdioFile cleanup |

- Paired save has four exact callers: `0x0050458b`, `0x0050483e`, `0x0050bbe7`, and `0x00510671`.
- Consumer spread reinforces MapPane ownership but does not move target/helper source to caller aggregates, StdioFile, AUTOBUF, zlib, PathUtil, or platform-dispatch modules.

## Documentation Evidence And IDA Status

- UID0002QJ's current behavior outline is substantially correct but its name/type/helper/header blockers and blank formal are obsolete after this pass.
- UID00037Y identified `AllocateTileBuffer`, exact offsets, byte count, and three call sites before the callback; validator-issued UID0004PZ is the completed deterministic split child.
- UID0003TL provides complete accepted packet C++ and exact `MapPaneTileRecord` use; its former three-field alias conflict is corrected without rewriting the rest of the body.
- UID0004HP's formal six-byte record is already complete and should not change.
- UID00007Q and UID0000L3 deliberately keep class/file formal policy separate: method bodies emit from by-memory children. Their scores are not raised by this bounded source-family synchronization.
- Current generated MapPane.cpp was refreshed by validator command `000000011692` at `2026-07-14T14:39:19-04:00`, SHA-256 `9E2F441AA26FD3566023D456257E93BFDD46835DC32BDE37432E8D15BEC743E8`. It had one UID0002QJ marker, one UID0002QK marker, one UID00007Q marker, one UID0003TL body using newer aliases, and an exact MapPaneTileRecord definition/global signature before the marker tail.

## Ranked Ownership Analysis

1. [UID:00007Q][MapPane](../../../../../by-class/MapPane.md), [UID:0000L3][MapPane](../../../../../by-file/MapPane.md): overwhelmingly strongest. Both target callers are MapPane methods; receiver fields are MapPane map id/dimensions/tile state; paired save and lifecycle are MapPane-owned; current route already matches.
2. Private MapPane helper for `[0x0050e2a0,0x0050e2f4)`: strongest helper placement. It reads/writes only MapPane fields, has only target calls, and current UID00037Y/MapPane class/file already inventory it.
3. File-local free helper in MapPane.cpp: rejected. ECX is the MapPane receiver, current accepted vocabulary is member-oriented, and a free helper would require an explicit receiver parameter absent from source-facing thiscall shape.
4. StdioFile/AUTOBUF/zlib/PathUtil/WideApi owner: rejected. These are called dependencies with independent classes/files/formals; none owns receiver state or target callers.
5. Caller aggregates UID0001AT/UID0001AW: rejected as owners. They are broad range/split indexes containing consumer methods, not source modules for the callee body.

## Source Placement

- Target/helper source: `NexusTK/map/MapPane.cpp`, emitted through UID00007Q and UID0000L3.
- Header contract: the existing/manual MapPane declaration must expose `LoadCompressedMapFile(unsigned short, int)` and private `AllocateTileBuffer()`, with complete `MapPaneTileRecord` availability and fields `unsigned short m_mapWidth`, `unsigned short m_mapHeight`, `MapPaneTileRecord *m_tileBuffer` at the documented layout offsets.
- Include/dependency visibility: Windows/Shell constants/APIs (`MAX_PATH`, `CSIDL_PERSONAL`, `S_OK`, `ERROR_ALREADY_EXISTS`, `SHGetFolderPathW`, `GetLastError`), wide CRT formatting, `StdioFile`, `_AUTOBUF<unsigned char>`, C string compare, `MapPaneTileRecord`, and zlib `Bytef/uLongf/uncompress` must be visible through the MapPane source/header include graph.
- Deterministic source order: preserve blank emitter positions and current address-sort behavior. The MapPaneTileRecord/global signature support is available before the current marker tail; target precedes paired save by address; helper may be defined later because its private declaration is in the header.
- The MapPane class page remains blank formally because a partial class block is invalid against its 124-method surface. This is an exact no-change disposition, not a deferral of the target/helper signatures.

## Range / Split / Padding / Reclassification Analysis

- UID0002QJ is exactly one modeled function and needs no target split.
- The allocation helper is outside UID0002QJ and inside UID00037Y. It is independently registered as UID0004PZ because it has a modeled function, coherent source semantics, three live machine calls, and a complete body.
- The helper child begins at `0x0050e2a0` and ends exclusively at `0x0050e2f4`. Extending it to `0x0050e300`, `0x0050e316`, or `0x0050e320` would absorb padding or an unrelated route-negative wrapper and is invalid.
- Pre-callback cumulative nesting was UID00037Y level 271 (`Nested:-4`), UID000232 level 275 (stored `Nested:4`), UID0001AW level 275 (`Nested:0`). Completed split state is UID00037Y level 271; UID0004PZ applies stored `Nested:4` to level 275; UID000232 stores `0` and remains level 275; UID0001AW remains `0` and level 275.
- `Nested` is a relative address-sort depth delta, not a child count. Parent `Nested` does not become `1`, and child pages do not use `0` merely because they have no descendants.
- No existing ignored range is duplicated. Target/save boundary padding remains unchanged; helper-neighborhood padding and wrapper remain documented inside the broad aggregate.

## Negative Evidence Summary

- No PDB/source symbol proves original target/helper/member names; lexical scores remain capped.
- No normal target instruction reads the second argument, but both callers pass it and `ret 8` proves it cannot be deleted from the source ABI.
- No caller consumes a target return; no evidence supports `bool`, `int`, pointer, or FILE return.
- No target branch resets storage on Documents lookup or directory-create failures.
- No helper instruction frees, deletes, checks, or reuses the prior tile pointer.
- No read-count, file-size, zlib-status, output-length, null-buffer, overflow-result, rollback, or corruption-recovery branch exists in source flow.
- No target source catch exists; only compiler EH cleanup exists.
- No alternate helper caller, vtable slot, pointer table, or external module route exists.
- No IDA UDT or entity name resolves lexical spelling; accepted current docs/generated use determines best names.
- No evidence supports absorbing MapPaneTileRecord into the four-byte MapTileImageLib `MapTileRecord`.
- No source need exists for SEH records, cookies, explicit destructor calls, allocator overflow helpers, raw dispatch addresses, IDA labels, or uppercase `Uncompress`.
- No evidence supports implementing paired save C++ as part of this target; its remaining direct-CRT writer edge audit is independent.

## IDA Rename / Type / Comment Recommendations

- IDA mutation is not part of this workflow and was not performed.
- Safe future rename candidates, if separately approved: `sub_504B90 -> MapPane__LoadCompressedMapFile` and `sub_50E2A0 -> MapPane__AllocateTileBuffer`.
- Safe future target prototype: thiscall `void(MapPane *, unsigned short, int)`; preserve the second int even though unused by normal body instructions.
- Resolved evidence-time helper prototype: thiscall `void(MapPane *)`; no IDA mutation was performed.
- Safe future receiver types: `+0x3f4 unsigned short`, `+0x3f6 unsigned short`, `+0x3f8 MapPaneTileRecord *`; avoid creating duplicate alias fields.
- Comments should identify ignored read/zlib results, early gate state preservation, no-free helper behavior, and compiler-only EH cleanup without asserting original names as symbol-proven.

## First-Draft C++ Recommendation

- The following three accepted destination blocks are the only applied C++/declaration/body changes. All other callback changes are prose, metadata, score, nesting, or verify-only updates.
- Destination 1: `by-memory/0x00504b90-0x00504df1.MapPaneLoadCompressedMapFile.md`.

*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::LoadCompressedMapFile(
    unsigned short mapId,
    int /*unusedLoadOption*/)
{
    wchar_t documentsPath[MAX_PATH];
    if (SHGetFolderPathW(NULL, CSIDL_PERSONAL, NULL, 0, documentsPath) != S_OK)
        return;

    wchar_t mapDirectory[MAX_PATH];
    swprintf_s(mapDirectory, L"%s\\NexusTK", documentsPath);
    if (!g_pfnCreateDirectoryW(mapDirectory, NULL) &&
        GetLastError() != ERROR_ALREADY_EXISTS)
        return;

    swprintf_s(mapDirectory, L"%s\\NexusTK\\Maps", documentsPath);
    if (!g_pfnCreateDirectoryW(mapDirectory, NULL) &&
        GetLastError() != ERROR_ALREADY_EXISTS)
        return;

    wchar_t cachePath[MAX_PATH];
    swprintf_s(cachePath, L"%s\\TK%06d.cmp", mapDirectory, mapId);

    if (!PathExistsViaStat(cachePath))
    {
        AllocateTileBuffer();
        return;
    }

    StdioFile cacheFile;
    cacheFile.Open(cachePath);

    char signature[4];
    cacheFile.Read(signature, sizeof(signature));
    if (strncmp(signature, g_mapPaneCmapSignature, sizeof(signature)) != 0)
    {
        AllocateTileBuffer();
        return;
    }

    unsigned short cachedWidth;
    unsigned short cachedHeight;
    cacheFile.Read(&cachedWidth, sizeof(cachedWidth));
    cacheFile.Read(&cachedHeight, sizeof(cachedHeight));

    if (cachedWidth != m_mapWidth || cachedHeight != m_mapHeight)
    {
        AllocateTileBuffer();
        return;
    }

    AllocateTileBuffer();

    _AUTOBUF<unsigned char> compressedMap;
    cacheFile.ReadRemaining(compressedMap);

    uLongf uncompressedSize =
        sizeof(MapPaneTileRecord) * cachedWidth * cachedHeight;
    uncompress(
        reinterpret_cast<Bytef *>(m_tileBuffer),
        &uncompressedSize,
        compressedMap.m_data,
        compressedMap.m_count);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

- Destination 2: validator-issued UID0004PZ `by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md`.

*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::AllocateTileBuffer()
{
    unsigned int tileCount = m_mapWidth * m_mapHeight;
    m_tileBuffer = new MapPaneTileRecord[tileCount];
    memset(
        m_tileBuffer,
        0,
        tileCount * sizeof(MapPaneTileRecord));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

- Destination 3: `by-memory/0x00510960-0x00511347.MapPaneHandleMapInfoTileRegionPacket.md`. This preserves the complete accepted formal block and changes only the three field aliases.

*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace
{
    enum MapInfoTileRegionPacketFlags
    {
        kMapInfoTileRegionHasMapName = 0x01,
        kMapInfoTileRegionHasResourceName = 0x02,
        kMapInfoTileRegionHasResourceMode = 0x04
    };

    struct MapInfoTileRegionPacketView
    {
        explicit MapInfoTileRegionPacketView(const unsigned char *packetBytes)
            : bytes(packetBytes), cursor(1)
        {
        }

        unsigned char ReadByte()
        {
            return bytes[cursor++];
        }

        unsigned short ReadUInt16()
        {
            unsigned short value = PacketBufferReadUInt16BE(bytes + cursor);
            cursor += 2;
            return value;
        }

        unsigned short ReadTileWord()
        {
            return PacketBufferReadUInt16BE(bytes, &cursor);
        }

        int ReadAnsiString(char *buffer, int bufferCount)
        {
            int byteCount = ReadByte();
            int copyCount = byteCount;
            if (copyCount >= bufferCount)
                copyCount = bufferCount - 1;

            GetMemoryMan()->MemmoveWrapper(buffer, bytes + cursor, copyCount);
            buffer[copyCount] = '\0';
            cursor += byteCount;
            return copyCount;
        }

        const unsigned char *bytes;
        int cursor;
    };
}

bool MapPane::HandleMapInfoTileRegionPacket(const unsigned char *packet)
{
    char mapNameBytes[256];
    char resourceNameBytes[256];
    wchar_t epfResourceName[260];
    wchar_t palResourceName[260];

    MapInfoTileRegionPacketView packetView(packet);
    unsigned char oldResourceMode = m_mapResourceMode;
    unsigned char flags = packetView.ReadByte();

    m_mapNameLength = 0;
    m_mapResourceNameLength = 0;
    m_mapResourceMode = 0;

    if ((flags & kMapInfoTileRegionHasMapName) != 0)
    {
        int mapNameLength = packetView.ReadAnsiString(mapNameBytes, sizeof(mapNameBytes));
        m_mapNameLength = static_cast<unsigned char>(
            MultiByteToWideChar(CP_ACP, 0, mapNameBytes, mapNameLength, m_mapName, 256));
        m_mapName[m_mapNameLength] = L'\0';
    }

    if ((flags & kMapInfoTileRegionHasResourceName) != 0)
    {
        int resourceNameLength = packetView.ReadAnsiString(resourceNameBytes, sizeof(resourceNameBytes));
        m_mapResourceNameLength = static_cast<unsigned char>(
            MultiByteToWideChar(CP_ACP, 0, resourceNameBytes, resourceNameLength, m_mapResourceName, 256));
        m_mapResourceName[m_mapResourceNameLength] = L'\0';

        wcscpy_s(epfResourceName, _countof(epfResourceName), m_mapResourceName);
        wcscat_s(epfResourceName, _countof(epfResourceName), L".EPF");
        wcscpy_s(palResourceName, _countof(palResourceName), m_mapResourceName);
        wcscat_s(palResourceName, _countof(palResourceName), L".PAL");
    }

    if ((flags & kMapInfoTileRegionHasResourceMode) != 0)
        m_mapResourceMode = packetView.ReadByte();

    if (m_mapResourceMode != oldResourceMode)
        ResetMapResourceStateForModeChange();

    int originTileX = packetView.ReadUInt16();
    int originTileY = packetView.ReadUInt16();
    int tileWidth = packetView.ReadByte();
    int tileHeight = packetView.ReadByte();

    MapRect packetBounds;
    SetRectBounds(&packetBounds, originTileX, originTileY,
        originTileX + tileWidth, originTileY + tileHeight);

    MapRect mapBounds;
    SetRectBounds(&mapBounds, 0, 0, m_mapWidth, m_mapHeight);

    MapRect clippedBounds = packetBounds;
    IntersectRectBounds(&clippedBounds, &mapBounds);

    for (int tileY = clippedBounds.top; tileY < clippedBounds.bottom; ++tileY)
    {
        for (int tileX = clippedBounds.left; tileX < clippedBounds.right; ++tileX)
        {
            MapPaneTileRecord &tile = m_tileBuffer[tileX + tileY * m_mapWidth];
            unsigned short oldGroundTileId = tile.groundTileId;

            tile.groundTileId = packetView.ReadTileWord();
            unsigned short tileFlags = packetView.ReadTileWord();
            tile.flags = static_cast<unsigned short>((tile.flags & ~1) | (tileFlags & 1));
            tile.staticObjectId = packetView.ReadTileWord();

            if (tile.groundTileId != oldGroundTileId)
                DrawTileAt(tileX, tileY, true);
        }
    }

    MapRect refreshBounds;
    if (m_refreshWholeVisibleMap)
    {
        m_refreshWholeVisibleMap = false;
        GetExtendedVisibleTileBounds(&refreshBounds);
    }
    else if (m_hasRenderableMap)
    {
        SetRectBounds(&refreshBounds, 0, 0, 0, 0);
    }
    else
    {
        GetExtendedVisibleTileBounds(&refreshBounds);
        IntersectRectBounds(&refreshBounds, &clippedBounds);
    }

    RefreshStaticObjectTileRegion(refreshBounds);

    if (g_useEpfAssets)
        RefreshDeferredMapSurfacesForTileRegion();

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

## Final Recommendation

- UID0002QJ is source-ready at `92/94` with Destination 1, exact owner/emitter/range/title/path/position/nesting, and a historicalized empty-marker rationale.
- Exactly one allocation child, UID0004PZ, is registered at `92/94` with Destination 2. UID00037Y remains an aggregate and UID000232's source body is unchanged apart from relative nesting closure.
- Destination 3 is applied exactly, removing the isolated UID0003TL field-alias conflict while preserving all packet logic and metadata.
- Every accepted support page is synchronized at report-level detail with unrelated and same-or-greater content, positive/negative evidence, and superseded historical assumptions preserved.
- No save body, complete MapPane class shell, third-party import, IDA mutation, manual coverage edit, or generated/manual lifecycle edit belongs in this assignment.

## Recommended Target Doc Changes

- Applied and verified on UID0002QJ: raised only `86/90 -> 92/94`; preserved UID, path, title, exact range, owner UID00007Q, true, emitter UID00007Q, blank position, and `Nested:0`.
- Exact Item Summary: `Source-ready MapPane compressed-map loader: builds Documents\\NexusTK\\Maps\\TK%06d.cmp, preserves receiver state on early Documents/directory failures, allocates zeroed six-byte MapPaneTileRecord storage on cache miss/header/dimension failure and before inflate, and intentionally ignores file-read and zlib status results.`
- Destination 1 is applied exactly. Blank-C++/final-name blocker prose is replaced by complete signature, header dependency, path, branch, read, edge-case, helper, field, caller, paired-save, EH/compiler exclusion, and score evidence.
- All valid evidence is preserved; the old `95/95` blank gate, unresolved names, uppercase `Uncompress`, and all-failures-reset wording are explicit superseded history.

## Recommended Support Doc Changes

- Applied on new helper [UID:0004PZ][0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer](../../../../../by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md): serial registration; `92/94`; owner/emitter UID00007Q; true; blank position; stored `Nested:4` (+4 relative); exact bytes/hash/one-block/three-call/no-other-route/field/type/allocation/zero/no-free/compiler-lowering/negative/name-cap/source-route evidence; Destination 2.
- New helper exact Item Summary: `Source-ready private MapPane tile-buffer allocator; exact [0x0050e2a0,0x0050e2f4) body reads m_mapWidth/m_mapHeight, allocates a six-byte MapPaneTileRecord array into m_tileBuffer, zeroes the full allocation, never frees prior storage, and is called only three times by UID0002QJ.`
- UID00037Y preserves `87/89`, owner/emitter, true, blank position, comment-only formal, and `Nested:-4`; it now includes UID0004PZ, evidence-time MCP/hash/liveness/no-free facts, canonical fields/type, exact successor wrapper/padding, target relationship, and a nonblank aggregate Item Summary.
- UID000232 changed only stored `Nested:4 -> 0` plus exact closure explanation; `89/91`, owner/emitter, formal body, route, behavior, and unrelated evidence remain unchanged.
- UID0003TL preserves `90/90`, metadata, route, and all research; Destination 3 and historical alias disposition are applied.
- UID00007Q preserves `89/89` and blank formal while carrying the exact target/helper contract, canonical fields/type, path/error behavior, paired-save distinction, dependencies/order/negatives, and partial-class rejection.
- UID0000L3 preserves `89/85`, path, and metadata while carrying target/helper/save inventory/order, dependencies/generated expectations, aliases, edge behavior, and no-import disposition.
- UID00042K preserves `84/87`, owner/emitter/formal and now uses canonical `+0x3f4/+0x3f6/+0x3f8` rows with historical aliases and complete consumers.
- UID0004HP preserves `90/90`, exact struct formal, owner/emitter, and `Nested:0`; current role is `m_tileBuffer`, with exact load/helper/save stride evidence and four-byte-type rejection.
- UID0002QK preserves `86/90`, metadata, blank formal, and `Nested:0`; canonical names, lowercase `compress`, path/signature/size, failure retention, direct CRT, successful free/clear/monster-cache behavior, and load contrast are synchronized without save C++.
- UID0001AT and UID0001AW preserve scores/formals/routes and now record exact argument/order/no-return facts: `a7` at `0x0050bca7` and zero at `0x00510741`.
- UID0002ZJ/UID0002ZN, StdioFile class/file/children, AUTOBUF file/template/constructor, UID0002UA, UID0001IZ, UID0000W2, UID0000TQ/UID0002AS, UID0002ST, and adjacent/teardown pages were reread and remain verify-only with accepted formals/scores/routes unchanged.
- Manual coverage is supervisor-owned. Exact pending replacement/addition text is supplied below; B005 must never edit a `-coverage-report.md` page.

## Score And Metadata Recommendation

| Destination | Current callback state | Result | Metadata disposition | Rationale/cap |
| --- | --- | --- | --- | --- |
| UID0002QJ target | `92/94` | applied | owner/emitter UID00007Q, true, blank position, `Nested:0` preserved | complete body and blocker closure; cap for no original symbols and unused-argument semantics |
| UID0004PZ helper | `92/94` | applied | owner/emitter UID00007Q, true, blank position, stored `Nested:4` | exact bytes/body/callers/type/source; cap for original lexical spelling |
| UID00037Y | `87/89` | unchanged | preserve all metadata/formal/`Nested:-4` | aggregate gains one split but remains broadly unresolved |
| UID000232 | `89/91` | unchanged | only stored `Nested:4 -> 0` | closure does not add source evidence |
| UID0003TL | `90/90` | unchanged | formal alias correction only | consistency repair does not add packet evidence |
| UID00007Q | `89/89` | unchanged | blank class formal preserved | complete class surface remains intentionally outside bounded scope |
| UID0000L3 | `89/85` | unchanged | path/file metadata preserved | bounded source inventory sync only |
| UID00042K | `84/87` | unchanged | formal/owner/emitter preserved | alias consistency only |
| UID0004HP | `90/90` | unchanged | exact struct formal preserved | added consumers do not alter layout proof |
| UID0002QK | `86/90` | unchanged | blank formal preserved | paired evidence, not independent complete save audit |

- Target completion rose because exact source behavior, signature, helper, fields, types, names, dependencies, and C++ are resolved.
- Target confidence rose because evidence-time MCP, paired save, callers, accepted support formals, field usage, and exact negatives agree. It remains below 95 because original symbols/source are absent.

## Open Questions With Attempted Resolution

- **Original target name?** Resolved to highest-probability `LoadCompressedMapFile` by title, paired `SaveCompressedMapFile`, behavior, and source-family precedent. Lexical confidence remains capped; no blank gate remains.
- **Second argument?** Exact ABI is resolved, semantic role is not observed. Preserve it as `int /*unusedLoadOption*/`; removing it is invalid and inventing a used name/branch is unjustified.
- **Return type?** Resolved to void from IDA prototype, caller non-use, and control-only exits.
- **Helper split/name?** Resolved and registered as UID0004PZ exact child `[0x0050e2a0,0x0050e2f4)` / `AllocateTileBuffer`. No-free evidence rejects Reset naming.
- **Fields/type?** Resolved to established `m_mapWidth`, `m_mapHeight`, `m_tileBuffer`, and six-byte `MapPaneTileRecord`. Former UID0003TL aliases are superseded history.
- **Early failures?** Resolved branch by branch: Documents/directory failures preserve existing pointer; cache miss/header/dimension branches allocate zero storage; Open can throw; read/inflate statuses are ignored.
- **Class declaration?** Exact method/helper/field/dependency contract is resolved. Emitting a partial class block is rejected because it would omit the accepted 124-method class surface; class prose and external header route carry the contract until a complete class-header project is undertaken.
- **Save body in scope?** No. Paired save is evidence and support synchronization only; complete source-quality audit of its direct CRT writer and all failure exits is independently required before formal C++.
- **Third-party source?** No. Existing bundled zlib is a dependency; target/helper are NexusTK integration code.
- **Internal wrapper at `0x0050e300`?** Exact range/behavior/no-route is resolved and retained under UID00037Y. It is not part of the helper and receives no code in this report.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- B005 did not manually edit coverage or tracker state. The following exact supervisor-owned reconciliation text already uses validator-issued UID0004PZ; no placeholder or guessed UID remains. Initial waited command `000000011784` refreshed memory/class/file coverage as an automated side effect, which does not convert these rows into B005-authored manual changes.
- Replace the current UID0002QJ row in `by-memory/-coverage-report.md` with:

`        - [UID:0002QJ][0x00504b90-0x00504df1.MapPaneLoadCompressedMapFile](by-memory/0x00504b90-0x00504df1.MapPaneLoadCompressedMapFile.md) 0x00504b90-0x00504df1 | method | MapPaneLoadCompressedMapFile : reconstructable : 92% : very strong : Source-ready MapPane compressed-map loader with exact 609-byte hash/CFG, two call routes and preserved two-argument void ABI; builds Documents\NexusTK\Maps\TK%06d.cmp, preserves state on early path failures, validates CMAP and dimensions through StdioFile, allocates zeroed six-byte MapPaneTileRecord storage through an exact child, and intentionally preserves ignored read/uncompress results and exception behavior.`

- After the UID0001AV row and before UID000232, add/update these address-sorted rows:

`    - [UID:00037Y][0x0050bce0-0x0050e320.MapPaneTileObjectGridRenderCore](by-memory/0x0050bce0-0x0050e320.MapPaneTileObjectGridRenderCore.md) 0x0050bce0-0x0050e320 | class aggregate | MapPaneTileObjectGridRenderCore : reconstructable : 87% : very strong : Comment-only MapPane method-cluster aggregate with exact helper inventory, field/type evidence, internal route-negative wrapper/padding disposition, and independently registered source children including the exact tile-buffer allocator.`

`        - [UID:0004PZ][0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer](by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md) 0x0050e2a0-0x0050e2f4 | private method | MapPaneAllocateTileBuffer : reconstructable : 92% : very strong : Exact source-ready one-block MapPane allocator called only three times by UID0002QJ; allocates m_mapWidth*m_mapHeight six-byte MapPaneTileRecord rows into m_tileBuffer and zeroes them without freeing prior storage.`

- Replace the stale UID000232 row with:

`    - [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) 0x0050e320-0x0050e4b6 | class method | MapPaneObjectEffectDescriptorDispatch : reconstructable : 89% : very strong : Source-ready MapPane::RequestObjectEffect with exact descriptor lookup, ObjectList target lookup, secondary-effect reuse/timer/create flow, primary-effect replacement/apply flow, four callers, exact padding, and nesting closure after the preceding allocation-helper child.`

- Under UID0001AW in address order, add:

`        - [UID:0003TL][0x00510960-0x00511347.MapPaneHandleMapInfoTileRegionPacket](by-memory/0x00510960-0x00511347.MapPaneHandleMapInfoTileRegionPacket.md) 0x00510960-0x00511347 | packet method | MapPaneHandleMapInfoTileRegionPacket : reconstructable : 90% : very strong : Complete source-ready opcode-0x06 map-info/tile-region parser with exact flags, strings, resource mode, rectangle clipping, canonical m_mapWidth/m_mapHeight/m_tileBuffer fields, six-byte MapPaneTileRecord updates, redraw/static-object/deferred-surface refresh behavior, and preserved malformed-packet trust semantics.`

- Replace the MapPane class row in `by-class/-coverage-report.md` with:

`- [UID:00007Q][MapPane](by-class/MapPane.md) : reconstructable : 89% : very-strong : Live game-world pane class at unchanged 89/89 score/route and blank complete-class formal disposition, with source-ready compressed-map load plus exact private tile-buffer allocation contract, canonical width/height/tile fields and six-byte tile type, paired-save distinction, packet/render/weather/object families, and exact TimerPane dependency synchronization.`

- Replace the MapPane file row in `by-file/-coverage-report.md` with:

`- [UID:0000L3][MapPane](by-file/MapPane.md) : reconstructable : 89% : very-strong : Major NexusTK/map/MapPane.cpp source root at unchanged score/route with source-ready compressed-map loader and exact allocation child, canonical map dimensions/tile buffer, paired direct-CRT save contract, deterministic dependency/order/generated policy, exact opcode 0x67 TimerPane call flow, and retained duplicate 0x005140a0 no-route evidence.`

- In `by-type/by-struct/-coverage-report.md`, insert these rows before the current MapTileImageLibLayout row:

`- [UID:00042K][MapPaneLayout](by-type/by-struct/MapPaneLayout.md) : reconstructable : 84% : strong : Focused MapPane layout support with canonical m_mapWidth/m_mapHeight/m_tileBuffer offsets, exact six-byte MapPaneTileRecord consumers across compressed load/save/allocation and packet/render/collision paths, preserved broader field ledger, and no standalone class emission.`

`- [UID:0004HP][MapPaneTileRecord](by-type/by-struct/MapPaneTileRecord.md) : reconstructable : 90% : very strong : Exact six-byte live MapPane tile-buffer record with groundTileId, flags, and staticObjectId words; packet writes, static-object reads, compressed-map load/save stride, and allocation/zeroing consumers distinguish it from the four-byte MapTileImageLib MapTileRecord.`

- UID0002QK's existing manual row remains verify-only because its score and compact description remain accurate. No exact row change is required solely for added paired contrast.
- Tracker registration/count/move state is validator/lifecycle-owned and must derive from the report lifecycle and registered helper page; this report supplies no manual tracker edit or permanent pre-execution count assertion.

## Follow-Up Actions

- B005's accepted implementation responsibilities are complete: C01-C35, Destinations 1-3, all bounded support synchronization, validators, generated verification, report reconciliation, and lease release are recorded in this artifact.
- Real UID0004PZ registration, scoped command IDs/timestamps/results/side effects, changed and verify-only paths, lease closure, and final waited generated assertions are durable callback evidence below.
- Report execution, archival, tracker lifecycle, and any manual coverage reconciliation remain external supervisor-owned state; this report issues no directive or permanent count assertion about that state.
- UID0002QK remains an independently scoped source-quality target; this callback synchronized its accepted support facts without adding save C++ or leaving hidden UID0002QJ work.

## Confidence

- Target behavior/range/ownership/ABI: `99/100`.
- Helper behavior/range/liveness/ownership: `99/100`.
- Field offsets/types and canonical current names: `98/100`.
- Exact original target/helper lexical spellings: `90/100`; source-facing selections are highest probability and sufficient for C++.
- Second-argument semantic label: `75/100`; ABI type/count and non-use are `99/100`, so the comment-preserved parameter is the least-inventive exact source shape.
- Overall recommendation: very strong, with target/helper recommended at `92/94` rather than 95+ due missing original symbols/source and no rebuilt-binary equivalence check.

## Validator Results

- Historical report-only phase: no validator command or lease existed before exact-artifact Gate 1, as required.
- Evidence-time generated read-only baseline: command `000000011692`, refreshed `2026-07-14T14:39:19-04:00`, MapPane.cpp SHA-256 `9E2F441AA26FD3566023D456257E93BFDD46835DC32BDE37432E8D15BEC743E8`.
- Callback validator records:

| Command | Timestamp | Scoped path/result | Warnings / validator-owned side effects |
| --- | --- | --- | --- |
| `000000011751` | `2026-07-14T15:42:27-04:00` | new UID0004PZ helper registration; exit 0, UID issued | registration/reference/metadata updates; generation deferred; registration output did not emit a separate `ok` count |
| `000000011752` | `2026-07-14T15:43:28-04:00` | UID0004PZ stored nesting correction; exit 0, `ok:1` | blank header normalization; generation deferred |
| `000000011753` | `2026-07-14T15:45:41-04:00` | UID0002QJ; exit 0, `ok:1` | completion/confidence/formal/autogen/ref updates; generation deferred |
| `000000011755` | `2026-07-14T15:46:46-04:00` | UID0003TL; exit 0, `ok:1` | pre-existing missing refs `0003TK` twice and `0003YN` once; generation deferred |
| `000000011758` | `2026-07-14T15:48:00-04:00` | UID00037Y; exit 0, `ok:1` | UID0004PZ label/ref normalization; generation deferred |
| `000000011760` | `2026-07-14T15:48:30-04:00` | UID000232; exit 0, `ok:1` | pre-existing missing refs `0003TG`, `0003XP`, `0003Z6`; generation deferred |
| `000000011771` | `2026-07-14T15:52:42-04:00` | by-class/MapPane.md; exit 0, `ok:1` | 75 pre-existing missing-ref warnings, UID0004PZ link normalization, five reference adds; generation deferred |
| `000000011773` | `2026-07-14T15:54:04-04:00` | by-file/MapPane.md; exit 0, `ok:1` | 86 pre-existing missing-ref warnings, three reference adds; generation deferred |
| `000000011775` | `2026-07-14T15:54:51-04:00` | MapPaneLayout; exit 0, `ok:1` | three reference adds; generation deferred |
| `000000011777` | `2026-07-14T15:55:32-04:00` | MapPaneTileRecord; exit 0, `ok:1` | three reference adds; generation deferred |
| `000000011779` | `2026-07-14T15:56:38-04:00` | UID0002QK; exit 0, `ok:1` | two reference adds; generation deferred |
| `000000011781` | `2026-07-14T15:57:34-04:00` | UID0001AT; exit 0, `ok:1` | 15 pre-existing missing-ref warnings, three reference adds; generation deferred |
| `000000011783` | `2026-07-14T15:58:07-04:00` | UID0001AW; exit 0, `ok:1` | 87 pre-existing missing-ref warnings, four reference adds; generation deferred |
| `000000011784` | `2026-07-14T15:58:24-04:00` | initial UID0002QJ `--wait-generated`; exit 0, `ok:1`, completed in 19 seconds | global autogen warnings: 14 fallback insertions, 85 missing children markers, 181 emitters without code; registry rebuild and 281 generated metadata refreshes; superseded as terminal refresh only after class-prose readback correction |
| `000000011802` | `2026-07-14T16:09:43-04:00` | by-class/MapPane.md exact signature/callsite prose correction; exit 0, `ok:1` | 75 pre-existing missing-ref warnings; generation deferred; class score/formal unchanged |
| `000000011803` | `2026-07-14T16:09:55-04:00` | terminal UID0002QJ `--wait-generated`; exit 0, `ok:1`, completed in 18.9 seconds | same unrelated global warning counts 14/85/181, registry rebuild, 281 generated metadata refreshes; no target-specific failure |

- Scoped validators refreshed validator-owned registry/reference/projected-stat state. The final waited refresh also refreshed `by-memory/-coverage-report.md`, `by-class/-coverage-report.md`, and `by-file/-coverage-report.md` at `2026-07-14T15:58:36-04:00` plus generated coverage/tracker/C++ outputs. B005 did not manually edit any of those files.
- Final generated `auto-generated/NexusTK/map/MapPane.cpp`: header command `000000011803`, refresh timestamp `2026-07-14T16:09:55-04:00`, SHA-256 `A478CF6D113BE16F165A5ED15551C59636DE28F376C29A0C92D7EA8C15203913`.
- Generated assertions passed: one `void MapPane::LoadCompressedMapFile` at line 320; one `void MapPane::AllocateTileBuffer` at line 1102; one UID0002QJ and one UID0004PZ source row; no target/helper marker; one UID0003TL body; zero `m_mapWidthTiles`, `m_mapHeightTiles`, and `m_mapTileRecords`; unchanged UID00007Q and UID0002QK markers at lines 1523/1525; unchanged UID00037Y aggregate comment at lines 1096-1099; one UID000232 definition at line 1113; correct address order UID0002QJ -> UID0004PZ -> UID000232, with UID0002QK retained in marker tail by emitter policy.
- Normalized byte-for-byte managed-body comparison passed for all three report destinations against their current docs: Destination 1 SHA-256 `8CA43D936D48A90B8AB7278C3742EDDEC90B2FE241A490B673FC2E8DE82BECF5` / 1762 chars; Destination 2 `5F36CCAFFABE84160E386C221E0405A5D86A210D53ECF16D55C994C508FFBB57` / 239; Destination 3 `905C3EB47673EF4BACFB9D6D5796CF8C0B2286E9E8ADE566407913EA28DDFF54` / 4769.
- Negative generated scans returned zero `__security_cookie`, `__ehhandler`, `__CxxFrameHandler`, raw `ret 8`/`ret 0x8`, `operator delete`, `HeapFree`, and explicit `delete[] m_tileBuffer` occurrences. No compiler-shaped ABI/EH/overflow/destructor mechanics were handwritten.
- Every B005 ordinary lease was released before command `000000011803`; the final current-leases read contained no active lease.

## Changed Files

- Callback artifact updated: `tools/leaser/Agents/Agent-B005/research/0002QJ-MapPaneLoadCompressedMapFile-empty-emitter-source-quality.md`.
- Ordinary changed paths:
  - `by-memory/0x00504b90-0x00504df1.MapPaneLoadCompressedMapFile.md`
  - `by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md` (created/registered UID0004PZ)
  - `by-memory/0x0050bce0-0x0050e320.MapPaneTileObjectGridRenderCore.md`
  - `by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md`
  - `by-memory/0x00510960-0x00511347.MapPaneHandleMapInfoTileRegionPacket.md`
  - `by-class/MapPane.md`
  - `by-file/MapPane.md`
  - `by-type/by-struct/MapPaneLayout.md`
  - `by-type/by-struct/MapPaneTileRecord.md`
  - `by-memory/0x00504e00-0x00505075.MapPaneSaveCompressedMapFile.md`
  - `by-memory/0x00506970-0x0050e320.MapPaneInputPacketRenderCore.md`
  - `by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md`
- Verify-only/read-only dependencies: UID0002ZJ/UID0002ZN CMAP global/storage; StdioFile class/file/Open/Read/ReadRemaining; AUTOBUF class/file/template/constructor; UID0002UA; UID0001IZ; UID0000W2; UID0000TQ/UID0002AS; UID0002ST; UID0002QF/UID0002QH; adjacent/ignored pages; manual coverage; generated output.
- Validator-owned side effects include validator registry/reference/projected-stat state and final generated/coverage/tracker refreshes. These were not manually edited. No audit, supervisor, goal, report lifecycle, queue/lock, executed-report/archive, or IDA state was changed by B005.

## Implementation Tracking Checklist

- [x] Supervisor Gate 1 accepted exact report-only SHA `1316CB6D89DC9083B535E7D6E98238141F8E363656F72D969431964412C3CF4E` before implementation.
- [x] C01-C35 are incorporated claim by claim without compression and ledger states/proof are legal and complete.
- [x] UID0002QJ is raised only to `92/94`, exact metadata/nesting are preserved, Item Summary is replaced, and Destination 1 is applied exactly.
- [x] Exact helper `[0x0050e2a0,0x0050e2f4)` was registered serially as UID0004PZ before cross-reference; all six placeholders were replaced.
- [x] UID0004PZ is `92/94`, owner/emitter UID00007Q, true, blank position, stored `Nested:4`, exact Item Summary/evidence, and Destination 2.
- [x] UID00037Y retains `87/89`, owner/emitter/formal/`Nested:-4` and has complete helper/successor/canonical-field evidence without detail loss.
- [x] UID000232 changed only stored `Nested:4 -> 0` plus closure note; body, score, owner/emitter, route, and unrelated evidence remain unchanged.
- [x] UID0003TL applies Destination 3 byte-for-byte and historicalizes only three superseded aliases; score/metadata and all other body/evidence remain unchanged.
- [x] UID00007Q preserves `89/89` and blank formal with exact declaration/dependency/field/load/helper/save evidence.
- [x] UID0000L3 preserves `89/85`, path, and metadata with exact source inventory/order/dependency/generated evidence.
- [x] UID00042K and UID0004HP preserve scores/formals/metadata and use canonical fields/type consistently.
- [x] UID0002QK preserves `86/90` and blank formal with paired behavior/names/failure state synchronized and no save C++.
- [x] UID0001AT and UID0001AW preserve scores/formals and contain exact caller argument/order/return facts.
- [x] UID0002ZJ/UID0002ZN, StdioFile, AUTOBUF, UID0002UA, PathExistsViaStat, zlib, WideApi, resource strings, teardown, adjacent, and ignored dependencies were reread and remain verify-only.
- [x] All valid positive/negative evidence, edge cases, compiler/EH exclusions, rejected alternatives, and historical assumptions remain at same-or-greater detail.
- [x] C++ appears only in Destinations 1-3; no extra destination or third-party import was added.
- [x] One ordinary file at a time was leased immediately before edit, reread, scoped-validated, and released; no lease was held while waiting.
- [x] Every changed ordinary page has its required scoped record; registration and nesting-correction validations are explicitly separate.
- [x] Terminal waited refresh `000000011803` ran after ordinary validation, corrective class revalidation, and lease release.
- [x] Generated MapPane.cpp satisfies target/helper/UID0003TL/class/save/aggregate/count/order/negative assertions and was inspected read-only.
- [x] Exact supervisor-owned coverage text uses real UID0004PZ; B005 made no manual coverage/tracker edit.
- [x] Validator-owned coverage/generated/registry side effects are recorded and were never manually edited.
- [x] Current Target State, Final Recommendation, Validator Results, Changed Files, Follow-Up Actions, ledger, and checklist state durable callback truth.
- [x] No `execute_report`, report probe/count/move/archive, lifecycle command, manual restricted-state edit, or IDA mutation occurred.
- [x] All B005 leases are released and no accepted claim is blocked.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B005","command_id":"000000011828","destination_path":"executed-b-agent-research/B005/0002QJ-MapPaneLoadCompressedMapFile-empty-emitter-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B005/research/0002QJ-MapPaneLoadCompressedMapFile-empty-emitter-source-quality.md","timestamp":"2026-07-14T16:26:12-04:00","uid":"0002QJ"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
