** TARGET-REPORT-UID:00013N **
** AUTHOR-AGENT-ID:B005 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 00013N DecoderConstructor Empty-Emitter Source-Quality Research

## Finalized Report / Current Recommendation

- Current recommendation: convert [UID:00013N] `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md` from an empty emitter marker to formal first-draft constructor C++.
- Final disposition: formal C++ on the exact by-memory method page. This is not a `[[CHILDREN]]` parent, covered-by marker, non-emitting container, split repair, or compiler-only no-code target.
- Required action: after supervisor validation, update only the accepted target/support docs under normal lease discipline, populate the target formal `RECONSTRUCTION_CPP CODE` block, refresh the target evidence and stale no-code rationale, then run a scoped validator with generated output current.
- Confidence: high for exact range, field writes, direct owner, emitter route, and source-level constructor shape; medium-high for original member spellings and final header split because those remain inferred.

Recommended target metadata after implementation:

```text
COMPLETION: 85 -> 87
CONFIDENCE: keep 90
CANONICAL_OWNER: keep 00003M
RECONSTRUCTABLE: keep TRUE
EMITTER_UIDS: keep 00003M
EMITTER_POSITION_OPTIONAL: keep blank
```

## Target

- Target UID: `00013N`
- Target path: `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md`
- Assignment id: `B005-report-00013N-DecoderConstructor-empty-emitter-source-quality-20260628`
- Source queue/report row: temporary Files With Empty Emitters queue; `auto-generated/-ag-research-tracker.md` row currently shows `80/88`, combined `84.0`, reconstructable `true`, reports `0`.
- Current supervisor classification: by-memory reconstructable empty emitter marker in `auto-generated/NexusTK/util/Decoder.cpp`.
- Current source-page state: target header is already `85/90`, owner [UID:00003M] `Decoder`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00003M`, blank formal C++.
- Generated state mismatch: generated `Decoder.cpp` and generated by-memory coverage still show stale `80/88` and `emits_code:false` for [UID:00013N]. These generated files must refresh through validator output, not manual edits.

## Current Target State

- Existing metadata: `COMPLETION:85`, `CONFIDENCE:90`, `CANONICAL_OWNER:00003M`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00003M`, blank `EMITTER_POSITION_OPTIONAL`, blank formal C++.
- Existing owner/emitter state: direct semantic owner is [UID:00003M] `Decoder`; emitter chain reaches [UID:00003M] -> [UID:0000IQ] `Decoder` -> `NexusTK/util/Decoder.cpp`.
- Existing open blocker: the page says final C++ remains blank because source-facing field/header names and raw-reader method set were unresolved. That rationale is now stale for this exact constructor because accepted exact Decoder child C++ already standardized the needed field names and the sibling Encoder constructor precedent resolves the same source-shape question.
- Related target/support docs checked: [UID:00003M] `Decoder`, [UID:0000IQ] `Decoder`, [UID:0000HQ] `BinaryCodec`, [UID:0001TS] `BinaryCodecCursorLayout`, [UID:0001X1] `BinaryCodecVtables`, [UID:00013M] `DecoderAndCodecVtableGlue`, [UID:00013O] `DecoderDestructor`, [UID:00013S] `DecoderFinalize`, [UID:00013T] `DecoderNoopVirtual`, [UID:00013U] `DecoderScalarDeletingDestructor`, [UID:0003IB] `DecoderVtableData`, and [UID:00013C] `EncoderConstructor`.

## Executive Recommendation

[UID:00013N] should emit source-level constructor code:

```cpp
Decoder::Decoder()
    : m_inputBuffer(NULL),
      m_inputSize(0),
      m_readPos(0),
      m_useBigEndian(true),
      m_isValid(true)
{
}
```

This preserves the exact binary semantics while omitting compiler-generated constructor lowering:

- the vptr store to `0x006192d8` is compiler-generated object construction evidence, not handwritten source;
- the three dword clears map to `m_inputBuffer`, `m_inputSize`, and `m_readPos`;
- the word store `0x0101` at `+0x10` is best represented as two adjacent true byte/bool fields, `m_useBigEndian` and `m_isValid`;
- no allocation, buffer attach, free, call, or explicit return belongs in source.

No split, merge, new child, or owner/emitter change is recommended.

## Supervisor Active Recheck

The user explicitly assigned B005 to report-only research for [UID:00013N] from the Files With Empty Emitters queue. I did not edit target/support by-* docs, generated files, project-level files, coverage reports, validator/tool state, or IDA DB state. I did not lease files.

The assigned item does not need split repair. Live MCP confirms one exact function block at `0x004a5640-0x004a5664`, followed by `0xcc` padding until [UID:00013O] `DecoderDestructor` at `0x004a5670`.

## Inference Research Guidance Check

I treated existing docs as leads and rechecked the constructor with current MCP. Direct IDA facts are separated below from documentation evidence and inference. Stale Wave2/Wave3 material was not used as authority; `simroot_v2/class_Decoder.cpp` is relevant only as a generated-output caveat because it omits this constructor.

`by-structure.md` controls the code gate. The current gate is `RECONSTRUCTABLE:TRUE`, a confirmed nonblank emitter route, and `(COMPLETION + CONFIDENCE) / 2 > 85`. [UID:00013N] already clears that gate from source metadata before any score increase. The old below-95 rationale is not valid under current rules.

## Heuristic / Inference Reanalysis And Validation

### Empty Emitter Versus Formal C++

Best decision: formal C++.

Evidence:

- Source page is reconstructable and routed to [UID:00003M].
- Live MCP session `b005_00013N_20260628` confirms exact source-visible member initialization after the compiler vptr write.
- [UID:00013C] `EncoderConstructor` is the direct symmetry precedent. It has the same `0x24` / 36-byte shape (Verified with `int_convert.py`), three cursor-field zero stores, and `0x0101` flag-word store, and now emits constructor initializer-list C++.
- Accepted Decoder raw child methods in generated `Decoder.cpp` already use `m_inputBuffer`, `m_inputSize`, `m_readPos`, `m_useBigEndian`, and `m_isValid`.

Rejected alternatives:

- Keep empty emitter marker: rejected because the only prior blocker was stale below-95/header uncertainty, and current exact child field names are sufficient for this constructor.
- `[[CHILDREN]]`: rejected because this page is a single full function body with no nested source-bearing children.
- Covered-by/no-code marker: rejected because no broader exact function page emits this constructor.
- Non-emitting metadata: rejected because this is not a container or compiler-only wrapper; most of the body initializes source-visible state.

### `0x0101` Flag Meaning

Best inference: two adjacent true fields.

Evidence:

- Live MCP decompile writes `*((_WORD *)this + 8) = 257` at `0x004a565d`; `0x0101` is decimal `257` and bytes `01 01` (Verified with `int_convert.py`).
- [UID:0001TS] records `+0x10` as `m_useBigEndian` and `+0x11` as `m_isValid`.
- `Decoder::SetByteOrder` writes the caller byte exactly into `+0x10`.
- Decoder readers check/clear `+0x11` independently.

Rejected alternatives:

- Single opaque `m_flags` field: weaker source shape because methods access the bytes independently.
- Capacity or cursor initialization: rejected because `+0x08` and `+0x0c` are separate dword stores.
- Boolean normalization in constructor: not represented as a setter call; the source initializer-list form is the cleanest human C++ for the same state.

### Source Placement

Best route: [UID:00013N] -> [UID:00003M] `Decoder` -> [UID:0000IQ] `Decoder` / `NexusTK/util/Decoder.cpp`, with [UID:0000HQ] `BinaryCodec` as family coordinator.

Evidence:

- [UID:00003M] owns the Decoder vtable, layout, and exact method inventory.
- [UID:0000IQ] is a validated `NexusTK/util/` source root for Decoder output.
- `by-project-structure/proposed-source-tree.md` lists `BinaryCodec.cpp` as a coordination root while concrete codec code is staged in `Encoder.cpp` and `Decoder.cpp`.
- [UID:0000HQ] explicitly says exact Decoder child pages remain source-owned emitters where applicable.

Rejected placements:

- [UID:0000HQ] as direct owner: too broad for an exact class constructor.
- [UID:0000IQ] as direct canonical owner: file root is the emitter destination, but `by-structure.md` requires the narrowest semantic owner.
- TextEdit, PacketBuffer, DATFile, or no-owner: no positive evidence for this exact constructor.

### No-Caller Caveat

Live MCP reports zero xrefs to `0x004a5640` and no VA/RVA pointer pattern to the constructor start. This caps final-audit confidence and live reachability claims, but it does not block formal C++:

- constructors can be retained source methods even when current IDA lacks a direct caller;
- the function is modeled, named, and vtable-identified;
- the body is source-visible member initialization, not a pure compiler artifact;
- current generated Decoder child methods already emit despite raw no-function/no-xref caveats when their bodies and ownership are understood.

## Evidence Standards Used

Evidence used:

- Current IDA MCP `server_health`, `lookup_funcs`, `entity_query`, `analyze_function`, `xrefs_to`, `get_bytes`, `get_int`, `find_bytes`, `callees`, and `basic_blocks`.
- Current source docs and generated reports.
- Executed B-report context: B009 `00013M-DecoderAndCodecVtableGlue-source-quality.md` and B011 `00013C-EncoderConstructor-source-quality.md`.
- `int_convert.py` for numeric conversions.

The evidence is strong enough for first-draft constructor C++ because function range, bytes, field writes, vtable ownership, layout names, emitter route, and sibling constructor source precedent agree. It is not final-audit confidence because no ordinary constructor caller or original debug/member names are proven.

## Evidence Checked

### IDA MCP

Session provenance:

- Endpoint: `http://127.0.0.1:13337/mcp`
- Initial `idb_list`: no open sessions.
- Opened/adopted via MCP `idb_open` using documented read-only executable `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`.
- Active session: `b005_00013N_20260628`
- MCP health: OK, `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready `true`, Hex-Rays ready `true`, strings cache ready `true`, strings cache size `2067`.

Current MCP checks:

- `lookup_funcs` for `0x004a5630`, `0x004a5640`, `0x004a5664`, `0x004a5670`, `0x004a5680`, `0x004a5dd0`, `0x004a5df0`, `0x004a5e00`, `0x004a5e30`, and `0x004a4e70`.
- `entity_query functions` over `0x004a5630-0x004a5680`.
- `analyze_function 0x004a5640 include_asm=true`.
- `xrefs_to` for `0x004a5640`, `0x006192d8`, `0x006192dc`, `0x006192d4`, and `0x006192e0`.
- `get_bytes` for `0x004a5630` size `0x50` and `0x006192d4` size `0x10`.
- `get_int u32le` for `0x006192d4`, `0x006192d8`, `0x006192dc`, and `0x006192e0`.
- `find_bytes` patterns `40 56 4a 00`, `40 56 0a 00`, and `d8 92 61 00`.
- `callees 0x004a5640`.
- `basic_blocks 0x004a5640`.

### Docs And Reports

Checked:

- `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md`
- `by-class/Decoder.md`
- `by-file/Decoder.md`
- `by-file/BinaryCodec.md`
- `by-type/by-struct/BinaryCodecCursorLayout.md`
- `by-type/by-vtable/BinaryCodecVtables.md`
- `by-memory/0x004a5630-0x004a5e54.DecoderAndCodecVtableGlue.md`
- `by-memory/0x004a5670-0x004a5677.DecoderDestructor.md`
- `by-memory/0x004a5dd0-0x004a5ded.DecoderFinalize.md`
- `by-memory/0x004a5df0-0x004a5df1.DecoderNoopVirtual.md`
- `by-memory/0x004a5e00-0x004a5e24.DecoderScalarDeletingDestructor.md`
- `by-memory/0x006192d4-0x006192e0.DecoderVtableData.md`
- `by-memory/0x004a4e70-0x004a4e94.EncoderConstructor.md`
- `by-project-structure/proposed-source-tree.md`
- `auto-generated/NexusTK/util/Decoder.cpp`
- `auto-generated/-ag-research-tracker.md`
- `auto-generated/-ag-coverage-report-by-memory.md`
- `executed-b-agent-research/B009/00013M-DecoderAndCodecVtableGlue-source-quality.md`
- `executed-b-agent-research/B011/00013C-EncoderConstructor-source-quality.md`

### Negative Checks

- `xrefs_to 0x004a5640` returns zero xrefs.
- `find_bytes` finds no VA pointer pattern `40 56 4a 00` and no RVA pointer pattern `40 56 0a 00`.
- `callees 0x004a5640` returns empty.
- `lookup_funcs 0x004a5664` reports not a function, confirming the half-open end boundary.
- `lookup_funcs 0x004a5680` reports not a function, preserving the separate raw reader start caveat.

## Inference Claim Ledger

| Claim | Confidence | Evidence | Counter-evidence checked | Next verification / remaining uncertainty |
| --- | --- | --- | --- | --- |
| [UID:00013N] is an exact `Decoder::Decoder()` constructor body. | Confirmed binary role; strong source role. | MCP function name `Decoder_Constructor`, size `0x24`, vtable store to `??_7Decoder@@6B@`, Decoder class/file docs. | Checked no ordinary function-start xrefs and no VA/RVA pointer route; absence does not change body identity. | Original source caller route remains unavailable. |
| Formal constructor C++ is safe. | Strong. | Reconstructable, emitter route, combined score clears gate, exact field initialization, accepted Encoder constructor precedent. | Rejected empty marker, no-code compiler glue, and container alternatives. | Exact original field spellings remain inferred. |
| `0x0101` means default big-endian and valid/readable flags are true. | Strong. | `int_convert.py` verifies `0x0101` decimal `257`, bytes `01 01`; layout and child method docs use independent `+0x10` and `+0x11` fields. | Rejected single opaque final `m_flags` source model. | Final header layout and tail padding remain open. |
| Direct owner stays [UID:00003M]. | Strong. | Class vtable data, constructor/destructor/scalar destructor refs, class and file route above gate. | Rejected BinaryCodec direct owner, Decoder file as canonical owner, no-owner, TextEdit/Packet/DAT routes. | None blocking. |
| Score should become `87/90`. | Medium-high. | Completion rises with formal C++ and fresh MCP; confidence already strong but no direct caller remains. | `95+` rejected by no-caller/original-name/header caveats; `85/90` would undercount resolved empty-emitter blocker. | Supervisor may choose `87/91` only if it treats no-caller as non-material. |

## Positive Evidence Summary

- MCP `lookup_funcs` reports `0x004a5640` as `Decoder_Constructor`, size `0x24`.
- MCP `analyze_function` shows one basic block, no callees, no strings, no callers, and decompiled writes matching the exact constructor body.
- MCP disassembly:
  - `mov dword ptr [ecx], offset ??_7Decoder@@6B@`
  - `mov eax, ecx`
  - zero dwords at `+0x04`, `+0x08`, `+0x0c`
  - `mov word ptr [ecx+10h], 101h`
  - `retn`
- MCP `xrefs_to 0x006192d8` returns exactly the expected Decoder vtable-base refs from constructor `0x004a5640`, destructor `0x004a5670`, and scalar deleting destructor `0x004a5e0a`.
- MCP `get_bytes` confirms the constructor is bracketed by padding after the prior no-op virtual and before the destructor.
- Support docs already establish the field names used by emitted Decoder children.

## IDA MCP Facts

- Function/range facts: `0x004a5640-0x004a5664`, size `0x24` / 36 bytes (Verified with `int_convert.py`), `.text`, one basic block.
- Data/table facts: `0x006192d4 -> 0x00646b74`, `0x006192d8 -> 0x004a5e00`, `0x006192dc -> 0x004a5df0`, `0x006192e0 -> 0x1a`.
- Xref facts: zero xrefs to `0x004a5640`; three data refs to `0x006192d8` from `0x004a5640`, `0x004a5670`, `0x004a5e0a`; one data ref to `0x006192e0` from adjacent DAT helper `0x004a5e9e`.
- Negative IDA facts: no callees, no function-start xrefs, no VA/RVA pointer patterns to `0x004a5640`.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004a5630-0x004a5631` | [UID:00013L] | Encoder no-op virtual | TRUE | [UID:00004F] | `85/90` | sibling compiler/vtable support |
| `0x004a5640-0x004a5664` | [UID:00013N] target | `Decoder::Decoder()` | TRUE | [UID:00003M] | recommend `87/90` | exact constructor, should emit formal C++ |
| `0x004a5664-0x004a5670` | local boundary | padding | FALSE | n/a | n/a | `0xcc` bytes before destructor |
| `0x004a5670-0x004a5677` | [UID:00013O] | `Decoder::~Decoder()` | TRUE | [UID:00003M] | `85/90` | separate no-code/source-declaration decision |
| `0x004a5680-0x004a5dce` | [UID:00013Q] | raw reader family index | FALSE | [UID:00003M] | `90/92` | non-emitting split index over exact raw children |
| `0x006192d4-0x006192e0` | [UID:0003IB] | Decoder vtable data | TRUE | [UID:00003M] | `90/93` | source-declared/generated-binary vtable child |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x004a5640` | zero incoming xrefs | No current ordinary caller/pointer route for constructor start. |
| `0x006192d8` | refs from `0x004a5640`, `0x004a5670`, `0x004a5e0a` | Decoder vtable base used by constructor, destructor, scalar deleting destructor. |
| `0x006192dc` | zero ordinary refs | No-op virtual slot has no direct ordinary refs. |
| `0x006192e0` | data ref from `0x004a5e9e` in `0x004a5e60` | Adjacent DAT constant boundary, not a Decoder vtable extension. |

## Documentation Evidence And IDA Status

Existing docs that support the conclusion:

- [UID:00003M] `Decoder` records the class purpose, declaration direction, field layout through `+0x11`, and accepted exact raw child method C++ using the same field names.
- [UID:0000IQ] `Decoder` records `NexusTK/util/Decoder.cpp` as the source root and concrete owner for exact Decoder child output.
- [UID:0001TS] records the shared layout and explicitly says `Decoder::Decoder` writes vtable, clears `+0x04/+0x08/+0x0c`, then writes `0x0101`.
- [UID:00013C] `EncoderConstructor` supplies the accepted symmetric constructor source shape.
- [UID:00013M] and B009 report confirm the broad glue page is a non-emitting index, so exact children such as [UID:00013N] should carry source-bearing output where eligible.

Existing docs that are stale/incomplete:

- [UID:00013N] still says final C++ remains blank due field/header/raw-reader blockers. Those blockers no longer block this exact constructor.
- Generated tracker and generated `Decoder.cpp` still show [UID:00013N] as an empty marker at stale `80/88`.
- The target Item Summary still mentions legacy position wording while `EMITTER_POSITION_OPTIONAL` is blank. No position metadata change is required because sibling exact children also use blank position metadata; the summary can be simplified during implementation.

## Ranked Ownership Analysis

### 1. [UID:00003M] Decoder Class

- Evidence for: constructor stores Decoder vtable base; exact vtable data is Decoder-owned; Decoder class clears the gate; exact raw child methods use the same fields and emit through this class.
- Evidence against: original constructor caller is not currently recovered.
- Decision: accepted direct owner and emitter.

### 2. [UID:0000IQ] Decoder Source File

- Evidence for: valid `NexusTK/util/` source root and generated output destination.
- Evidence against: file root is not the narrowest semantic owner.
- Decision: accepted as source root through [UID:00003M], not direct canonical owner.

### 3. [UID:0000HQ] BinaryCodec

- Evidence for: shared Encoder/Decoder layout and coordinator docs.
- Evidence against: this exact constructor writes only Decoder instance state and has a narrower class owner.
- Decision: keep as support/coordinator context only.

### 4. No-Owner / Non-Emitting / Covered-By

- Evidence for: no ordinary constructor caller.
- Evidence against: modeled function, vtable identity, source-visible field writes, direct class/file route, and active code gate.
- Decision: rejected.

## Source Placement

Recommended placement is `NexusTK/util/Decoder.cpp` through [UID:0000IQ]. The formal C++ body should appear as a normal `Decoder::Decoder()` method body alongside the already emitted exact Decoder reader methods. [UID:0000HQ] `BinaryCodec.cpp` remains the coordination root only; it should not emit this method or duplicate Decoder child output.

## Range / Split / Padding / Reclassification Analysis

No split or reclassification is needed.

- Exact target start: `0x004a5640`
- Exact target end: `0x004a5664`
- MCP `basic_blocks` reports a single block `0x4a5640-0x4a5664`, size `36`.
- `lookup_funcs 0x004a5664` reports not a function.
- `get_bytes 0x004a5630 size 0x50` shows prior [UID:00013L] one-byte `ret`, padding, the constructor body, padding, and the separate destructor at `0x004a5670`.
- `0x004a5680` remains a separate raw reader start and not part of this constructor.

## Negative Evidence Summary

- No ordinary caller/xref to `0x004a5640`.
- No VA pointer bytes `40 56 4a 00` or RVA pointer bytes `40 56 0a 00` in the image.
- No callee or helper call in the constructor body.
- No allocation, buffer attach, free, or ownership transfer.
- No evidence supports PacketBuffer, DATFile, TextEdit, BinaryCodec-direct, no-owner, or non-emitting treatment for this exact page.

## IDA Rename / Type / Comment Recommendations

No IDA DB edits are requested in this report-only pass.

- `0x004a5640`: source-facing name is `Decoder::Decoder()` / IDA-safe `Decoder_Constructor`; current MCP already reports `Decoder_Constructor`.
- `+0x04`: `m_inputBuffer`
- `+0x08`: `m_inputSize`
- `+0x0c`: `m_readPos`
- `+0x10`: `m_useBigEndian`
- `+0x11`: `m_isValid`

Do not create raw Decoder reader functions or install provisional struct types from this report. Those are outside the accepted target edit and remain separately documented.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes.
- Reason it preserves exact original behavior: compiler-generated vptr setup is omitted, while every source-visible member write is represented by a constructor initializer. `NULL`, zero integer initialization, and `true` flags compile to the same state shape under normal C++ object construction.
- Reason it matches plausible original source: it mirrors the accepted [UID:00013C] `Encoder::Encoder()` initializer-list style and uses the established Decoder member names from accepted raw child C++.
- Naming style: member names follow the existing emitted Decoder methods: `m_inputBuffer`, `m_inputSize`, `m_readPos`, `m_useBigEndian`, `m_isValid`.
- Third-party import directive: not applicable.
- No-code proof: not applicable because formal C++ is recommended.

Exact formal insertion text:

```markdown
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
Decoder::Decoder()
    : m_inputBuffer(NULL),
      m_inputSize(0),
      m_readPos(0),
      m_useBigEndian(true),
      m_isValid(true)
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

## Final Recommendation

- Update [UID:00013N] target page to `87/90`.
- Keep owner/emitter/reconstructable metadata unchanged.
- Populate formal constructor C++ exactly as above.
- Replace stale empty-emitter/no-code wording with active gate reasoning.
- Add the 2026-06-28 B005 MCP evidence and source-quality rationale.
- Do not edit generated reports or manual coverage reports.
- No support metadata change is required. Support docs already contain enough field/source-route evidence, but [UID:00003M] and [UID:0000IQ] may receive concise change notes if the supervisor wants the marker-clearance decision reflected at class/file level.

## Recommended Target Doc Changes

Target path: `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md`

Exact facts to incorporate:

- Current MCP session `b005_00013N_20260628` health and target checks.
- Exact range `0x004a5640-0x004a5664`, size `0x24` / 36 bytes (Verified with `int_convert.py`).
- One basic block, no callees, no strings, zero function-start xrefs, no VA/RVA pointer route.
- Vtable-base refs to `0x006192d8` from constructor, destructor, and scalar deleting destructor.
- Field writes: vptr store is compiler-generated; dword clears at `+0x04/+0x08/+0x0c`; word `0x0101` at `+0x10` is two true flags.
- Formal C++ body above.
- Rejected alternatives: empty marker, no-code compiler glue, `[[CHILDREN]]`, non-emitting metadata, BinaryCodec direct owner, no-owner.

Metadata/score changes:

- `COMPLETION:85 -> 87`
- `CONFIDENCE:90` unchanged
- `CANONICAL_OWNER:00003M` unchanged
- `RECONSTRUCTABLE:TRUE` unchanged
- `EMITTER_UIDS:00003M` unchanged
- `EMITTER_POSITION_OPTIONAL` unchanged blank

## Recommended Support Doc Changes

No support metadata changes are required.

If the supervisor wants support prose synchronized during implementation, use concise notes only:

- `by-class/Decoder.md`: note that [UID:00013N] now carries first-draft constructor C++ using `m_inputBuffer`, `m_inputSize`, `m_readPos`, `m_useBigEndian`, and `m_isValid`, while class-level declaration C++ remains blank.
- `by-file/Decoder.md`: note that [UID:00013N] should no longer be an empty emitter marker after target validation and should emit through [UID:00003M] to `NexusTK/util/Decoder.cpp`.

`by-type/by-struct/BinaryCodecCursorLayout.md`, `by-file/BinaryCodec.md`, and `by-type/by-vtable/BinaryCodecVtables.md` already contain the needed field/vtable/source-route evidence at same-or-greater detail for this target. No edit is required there.

## Score And Metadata Recommendation

- Current source-page score/metadata: `85/90`, [UID:00003M], `TRUE`, emitter [UID:00003M], blank C++.
- Current generated row: stale `80/88`, `emits_code:false`, empty marker.
- Recommended source-page score/metadata: `87/90`, [UID:00003M], `TRUE`, emitter [UID:00003M], formal C++ populated.
- Reason not higher: no ordinary constructor caller, no VA/RVA pointer route, original member names are inferred, final `sizeof(Decoder)`/tail padding and exact original header split remain open. These prevent final-audit scoring but do not block constructor C++.
- Reason not lower: exact body, range, bytes, vtable ownership, field writes, source route, active gate, and Encoder constructor symmetry are all confirmed.

Score-limiting blocker review:

| Blocker | Research performed | Result |
| --- | --- | --- |
| Empty emitter marker | Checked target metadata, generated `Decoder.cpp`, generated tracker/coverage, active code gate, and Encoder constructor precedent. | Formal C++ is implementation-ready. |
| Field/header naming | Checked [UID:00003M], [UID:0000IQ], [UID:0001TS], accepted emitted Decoder child methods, and [UID:00013C]. | Names are strong inferred source-facing names, sufficient for first-draft constructor C++. |
| `0x0101` flag meaning | Checked MCP decompile/disasm and `int_convert.py`; cross-checked layout and raw child behavior. | Two adjacent true flags: `m_useBigEndian` and `m_isValid`. |
| Caller route | Checked `xrefs_to`, `find_bytes` VA/RVA patterns, `callees`, and B009 context. | No caller/pointer route. Confidence cap only, not no-code proof. |
| Split/container risk | Checked function inventory, basic block, bytes, neighboring destructor/raw-reader start, and broad glue page. | Exact single function. No split or container treatment. |

## Open Questions With Attempted Resolution

- Original member spellings: unresolved because no debug symbols/source names exist. Best inferred names are accepted from current Decoder child C++. Score impact: caps final audit, no C++ blocker.
- Original header split: unresolved between separate `Decoder.h` and a shared `BinaryCodec.h` style. `Decoder.cpp` source route is still current. Score impact: caps final audit, no constructor-body blocker.
- Live constructor reachability: no direct caller or pointer route found. Score impact: keeps confidence at `90` instead of raising; no owner/emitter/C++ blocker because modeled source-bearing body and vtable identity are strong.
- Exact `sizeof(Decoder)` / tail padding: unresolved. Observed fields span `0x12` / 18 bytes (Verified with `int_convert.py`). Score impact: layout final-audit caveat only.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

Not applicable. Do not write row text for validator-owned `auto-generated/-ag-*` reports. No manual `-coverage-report.md` edit is requested by this assignment.

Expected generated refresh after target validation:

- `auto-generated/NexusTK/util/Decoder.cpp` should replace the [UID:00013N] empty marker with `Decoder::Decoder()` formal C++.
- `auto-generated/-ag-coverage-report-by-memory.md` should change [UID:00013N] from `emits_code:false` to coded/generated output.
- `auto-generated/-ag-research-tracker.md` should remove or update the stale Files With Empty Emitters row after the report is executed/validator refreshes generated state.

Implementation result, 2026-06-28:

- `auto-generated/NexusTK/util/Decoder.cpp` header is current for final validator command `000000005025` at `2026-06-28T04:30:37-04:00`; [UID:00013N] now appears as `Decoder::Decoder()` and no [UID:00013N] empty-emitter marker remains.
- `auto-generated/-ag-coverage-report-by-memory.md` now reports [UID:00013N] with `emits_code:true`, `87%`, `very-strong`, updated `2026-06-28 04:27:05`.
- `auto-generated/-ag-research-tracker.md` now reports [UID:00013N] as `87/90`, combined `88.5`, reconstructable `true`.

## Follow-Up Actions

- Supervisor: verify the implementation claim-by-claim and run `execute_report` only after verification succeeds.
- B005 callback: completed on 2026-06-28. Leases were taken only for the immediate target/support edit and validator batch, then released.
- Future research: [UID:00013O] `DecoderDestructor`, [UID:00013S] `DecoderFinalize`, [UID:00013T] `DecoderNoopVirtual`, and [UID:00013U] `DecoderScalarDeletingDestructor` are separate empty markers. This report does not decide those pages, though it preserves their current no-code/compiler-support rationale as separate scope.

## Confidence

- Recommendation confidence: high.
- Score confidence: medium-high.
- Remaining uncertainty: original names/header split/caller route only. None blocks the target constructor C++.

## Validator Results

- Report-only commands: none.
- Implementation validators run from `E:\NTK\GhidraBridge\source-3\project-documentation`:

> Executable block R001 was removed from this report and preserved verbatim in [00013N-DecoderConstructor-empty-emitter-source-quality-removed.md](00013N-DecoderConstructor-empty-emitter-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Result: exit code `0`, `command_id: 000000005022`, `command_timestamp: 2026-06-28T04:27:04-04:00`, `ok: 1`, `generated_refresh: completed`, `generated_refresh_command_id: 000000005022`, `generated_refresh_timestamp: 2026-06-28T04:27:04-04:00`.

> Executable block R002 was removed from this report and preserved verbatim in [00013N-DecoderConstructor-empty-emitter-source-quality-removed.md](00013N-DecoderConstructor-empty-emitter-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Result: exit code `0`, `command_id: 000000005023`, `command_timestamp: 2026-06-28T04:27:23-04:00`, `ok: 1`, `generated_refresh: completed`, `generated_refresh_command_id: 000000005023`, `generated_refresh_timestamp: 2026-06-28T04:27:23-04:00`. Target-local warning: existing [UID:0003IB] references in `by-class/Decoder.md` remain missing from `validator.ini`.

> Executable block R003 was removed from this report and preserved verbatim in [00013N-DecoderConstructor-empty-emitter-source-quality-removed.md](00013N-DecoderConstructor-empty-emitter-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Result: exit code `0`, `command_id: 000000005024`, `command_timestamp: 2026-06-28T04:27:38-04:00`, `ok: 1`, `generated_refresh: completed`, `generated_refresh_command_id: 000000005024`, `generated_refresh_timestamp: 2026-06-28T04:27:38-04:00`.

Target-only stale-history cleanup revalidation:

> Executable block R004 was removed from this report and preserved verbatim in [00013N-DecoderConstructor-empty-emitter-source-quality-removed.md](00013N-DecoderConstructor-empty-emitter-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Result: exit code `0`, `command_id: 000000005025`, `command_timestamp: 2026-06-28T04:30:37-04:00`, `ok: 1`, `generated_refresh: completed`, `generated_refresh_command_id: 000000005025`, `generated_refresh_timestamp: 2026-06-28T04:30:37-04:00`.

Generated-output check:

> Executable block R005 was removed from this report and preserved verbatim in [00013N-DecoderConstructor-empty-emitter-source-quality-removed.md](00013N-DecoderConstructor-empty-emitter-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Proof: `auto-generated/NexusTK/util/Decoder.cpp` has `validator-command-id: 000000005025`, `validator-refreshed-at: 2026-06-28T04:30:37-04:00`, [UID:00013N] at generated line 7, and `Decoder::Decoder()` at generated line 8. No [UID:00013N] `Empty Emitter Marker` remains; sibling lifecycle markers [UID:00013O], [UID:00013S], [UID:00013T], and [UID:00013U] remain separate scope.

Validator side effects: validator-owned generated files, projected stats, `tools/validator.ini`, and autogen backup folders were updated by the validator. No generated, project-level, coverage-report, tool-state, or IDA DB files were manually edited.

## Changed Files

- Created: `tools/leaser/Agents/Agent-B005/research/00013N-DecoderConstructor-empty-emitter-source-quality.md`
- Modified manually by B005 implementation:
  - `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md`
  - `by-class/Decoder.md`
  - `by-file/Decoder.md`
  - `tools/leaser/Agents/Agent-B005/research/00013N-DecoderConstructor-empty-emitter-source-quality.md`
- Validator-owned generated/state side effects observed:
  - `auto-generated/NexusTK/util/Decoder.cpp`
  - `auto-generated/-ag-coverage-report-by-memory.md`
  - `auto-generated/-ag-research-tracker.md`
  - `project-level/-auto-completion-stats.md`
  - `tools/validator.ini`
  - `tools/validator_autogen_backup/...`
- Renamed: none.
- Report execution: not run; supervisor owns `execute_report`.

## Implementation Tracking Checklist

Initial report-only pass:

- [x] Supervisor validation required before implementation. Proof: supervisor callback accepted this report for implementation on assignment `B005-implement-00013N-DecoderConstructor-empty-emitter-source-quality-20260628`.
- [x] Target doc to update: `by-memory/0x004a5640-0x004a5664.DecoderConstructor.md`. Proof: target doc updated and validator command `000000005022` returned `ok: 1`.
- [x] Optional support notes only if supervisor includes them: `by-class/Decoder.md` and `by-file/Decoder.md`; no support metadata changes required. Proof: concise support sync added to both files; class validator `000000005023` and file validator `000000005024` returned `ok: 1`.
- [x] Preserve current target owner/emitter/reconstructable metadata: [UID:00003M], `TRUE`, `EMITTER_UIDS:00003M`, blank position. Proof: target header still has `CANONICAL_OWNER:00003M`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00003M`, and blank `EMITTER_POSITION_OPTIONAL`.
- [x] Apply score change `85/90 -> 87/90`. Proof: target header and generated coverage now show `87/90`.
- [x] Populate formal `RECONSTRUCTION_CPP CODE` block with `Decoder::Decoder()` initializer-list body from this report. Proof: target formal block and generated `Decoder.cpp` contain the accepted constructor.
- [x] Replace stale blank-C++ rationale with active combined-score/emitter gate reasoning. Proof: target `Source-Quality Notes` now records the active gate, rejected blank/no-code rationale, and formal source shape.
- [x] Record current MCP session `b005_00013N_20260628`, health OK, exact function range, one-block/no-callee/no-caller facts, vtable-base xrefs, bytes, and pointer-route negatives. Proof: target `Evidence` and `Source-Quality Notes` carry those facts.
- [x] Record `0x0101` as two adjacent true flags, verified with `int_convert.py`. Proof: target `Source-Quality Notes` and support docs record `m_useBigEndian(true)` / `m_isValid(true)` and `0x0101` decimal/bytes verification.
- [x] Preserve rejected alternatives and negative evidence: no explicit vptr assignment in source, no `return this`, no allocation/free/attach, no `[[CHILDREN]]`, no no-code marker, no non-emitting reclassification. Proof: target `Source-Quality Notes` records all listed rejections.
- [x] Close current empty-emitter disposition as formal C++, not a blocker deferral. Proof: generated `Decoder.cpp` now emits [UID:00013N] as `Decoder::Decoder()`, not an empty marker.
- [x] Do not edit generated reports, project-level files, manual coverage reports, validator/tool state, or IDA DB. Proof: manual edits were limited to the three by-* docs and this B005 report; validator-owned generated/state refresh happened only through scoped validator commands; no manual coverage or IDA DB edits were made.
- [x] Run scoped validator with `--wait-generated` after implementation and report command id, timestamp, exit code, `ok` count, and generated refresh state. Proof: commands `000000005022`, `000000005023`, `000000005024`, and final target revalidation `000000005025` are recorded in `Validator Results`.
- [x] Verify generated `auto-generated/NexusTK/util/Decoder.cpp` no longer contains an empty marker for [UID:00013N]. Proof: generated header is command `000000005025`; [UID:00013N] appears at line 7 followed by `Decoder::Decoder()` at line 8, with no [UID:00013N] empty marker.

Implementation callback pass:

- [x] Report accepted by supervisor for implementation. Proof: callback assignment `B005-implement-00013N-DecoderConstructor-empty-emitter-source-quality-20260628`.
- [x] Leases taken only for immediate target/support edit batch and released immediately after validation. Proof: `python .\tools\leaser\leaser.py B005 lease ...` returned `Success` for target/class/file and later for the target-only stale-history cleanup; matching `unlease` commands returned `Success`; current lease report shows no active B005 leases.
- [x] Accepted target/support doc details incorporated at report-level detail or confirmed already present. Proof: target carries MCP evidence, negative evidence, gate reasoning, field mapping, rejected alternatives, score/metadata update, and formal C++; support docs carry the requested concise constructor/source-root sync.
- [x] Formal C++ applied exactly or intentionally not applied with supervisor-approved reason. Proof: exact accepted `Decoder::Decoder()` initializer list is in the target formal block and generated `Decoder.cpp`.
- [x] Validator output recorded and generated refresh checked. Proof: `Validator Results` records command IDs/timestamps/exit codes/`ok` counts and generated header freshness for final command `000000005025`.
- [x] Remaining unapplied accepted items listed with exact blocker. Proof: none remain unapplied; no blockers.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B005","command_id":"000000004165","destination_path":"executed-b-agent-research/B005/00013N-DecoderConstructor-empty-emitter-source-quality.md","details":"legacy registry reconstruction","event":"executed","source_path":"tools/leaser/Agents/Agent-B005/research/00013N-DecoderConstructor-empty-emitter-source-quality.md","timestamp":"2026-06-28T13:58:45","uid":"00013N"} -->
<!-- {"agent":"B005","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 00013N-DecoderConstructor-empty-emitter-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B005/00013N-DecoderConstructor-empty-emitter-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"00013N"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
