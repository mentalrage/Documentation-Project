# UID0000HF Removed Operational Persistence Material

Source report: [0000HF-AlphaMaskSurface-file-source-quality.md](./0000HF-AlphaMaskSurface-file-source-quality.md)

This sibling is inert historical material. It is non-authoritative, must never be executed, and must never be submitted to `execute_report`. It receives no Gate, coverage, score, IDA, validator, implementation, or lifecycle credit and is not a B report.

## Verbatim Removed Operational Material

The persistence handoff now names one supported Windows durability primitive instead of assuming that a directory handle can be flushed: synchronous local Storage-module `Write-VolumeCache` against the exact pre-bound `MSFT_Volume`. File `Flush(true)`, handle closure, independent file/path/identity/hash receipts, exclusive probes, collision/no-overwrite, same-parent/same-volume, anti-alias/link/reparse, one-attempt atomic replacement, source preservation, and zero-residue gates all remain mandatory. The volume-cache result is an additional stage barrier, never a substitute for those checks.

A supervisor-supplied closed physical repair receipt dated `2026-08-22` observed current canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` at 143,210,685 bytes and SHA256 `4C159ADDE049BFDFAB43C5DEB6222073A6636B46A9709E76D42A99E81945D7EF`. This receipt is dated drift evidence only. Neither it nor historical `7F4B827D...` may populate a future Gate 2B baseline; each attempt must derive its operational identity from a new closed physical startup receipt.

The next supervisor-owned Gate 2B startup attempt failed closed before `idb_open`, IDA mutation, or save. The freshly created one-link `BACKUP` and `TX` copies matched the new canonical receipt; their file-handle `Flush(true)` calls, closure, independent hashes, and exclusive probes passed. The requested directory-handle `FlushFileBuffers` call returned `Access is denied`, and a custom write-through move harness was blocked by execution security policy before it ran. That dated attempt established neither parent-directory durability nor Gate 2B credit and is historical failure evidence only.

Read-only environment research on `2026-08-22` found the built-in Storage-module `Write-VolumeCache` cmdlet and a unique healthy local NTFS `MSFT_Volume` for the canonical path. B005 did not invoke the flush. Microsoft documents that `Write-VolumeCache` writes the file-system cache to disk and forcibly flushes the write cache; Section 21 turns that supported primitive into the only accepted indirect containing-directory durability receipt for this transaction. Command/module availability and the dated volume observation are not future authority: each attempt must freshly bind its exact local volume and produce each named success receipt.

The supervisor's active persistence recheck must also keep all five session identities disjoint. `DB` is the isolated mutation/save role on `TX`; `DB_VERIFY` is the independent saved-TX read-only verifier; `DB_FINAL` is the successful promoted-canonical read-only proof role; `DB_ROLLBACK` is a newly returned read-only session used only to classify a failed or ambiguous post-promotion `CANON`; and `DB_RESTORE` is a different newly returned read-only session used only after sealed-backup replacement to prove restored original `CANON`. No identifier, worker route, role-specific module baseline, package, or authority may be reused or substituted across those roles. A post-promotion failure is not closed by a generic rollback/restore session: Section 21 requires `DB_ROLLBACK` post-open/pre-read/post-read classification, physical restore only from sealed `BACKUP` after that route retires, and then separate `DB_RESTORE` post-open/pre-read/post-read original-state proof with independent filesystem receipts. Gate 2B must derive `START_CANON_*` only from its own fresh closed physical startup receipt; historical `7F4B827D...` and dated repair receipts remain non-authoritative provenance. The dated parent-directory attempt that received `Access is denied` from directory-handle `FlushFileBuffers` and a security-policy block on a custom harness is historical failed-closed evidence only; it opened no IDB and grants no durability credit. Section 21 now uses one supported durability primitive: one synchronous local `Write-VolumeCache` call against the exact freshly bound `MSFT_Volume` at each named initial-copy, saved-TX, candidate-create/cleanup, atomic-commit, and final-canonical-closure boundary. Its documented volume file-system-cache flush supplies indirect containing-directory metadata durability only when the stage receipt proves exact input/returned/post-call volume identity and error-free success; file `Flush(true)`, hashes, and probes cannot substitute. Both physical transitions retain the complete named-candidate protocol: exact resolved parent and same volume; path/file-identity distinctness from canonical, backup, TX, every session path, and every prior candidate; reparse/symlink/junction/mount/ADS/hardlink/alias/case-fold rejection; collision-failing create-new/no-overwrite yielding one regular one-link identity; authorized-source-only copy; file flush then all-handle closure; named volume-cache creation receipt; independent identity/stat/SHA256 reopen and closed `FileShare.None` probe; one expected-CANON-bound atomic attempt with no fallback, cleanup-and-retry, or second attempt; named post-commit volume-cache receipt; unchanged source/BACKUP proof; and exact-parent zero-residue scan. `PROMOTION_CANDIDATE` comes only from verified saved `TX` after every DB/DB_VERIFY and CANON/TX-capable route retires; `RESTORE_CANDIDATE` comes only from freshly reverified sealed `BACKUP` after `DB_ROLLBACK` classifies failed CANON and every canonical route retires. Any unavailable, unsupported, access-denied, ambiguous, timed-out, policy-blocked, missing, or mismatched durability result stops at its exact phase with no alternate utility, hash-only success, retry, candidate recreation, or second atomic attempt. No failed-canonical diagnostic copy exists; DB_ROLLBACK plus physical CANON/BACKUP/TX receipts are the complete failure evidence. At every package boundary, top-level and worker-local `schema_version:1`, `ok:true`, and `errors:[]` remain mandatory; the current listener object has no listener-local `ok` or `errors`, so listener closure uses only its actual role, freshness, endpoint/port ownership, process generation, source-root, implementation manifest, and loaded-module identity fields.

- Latest dated pre-open durability failure: supervisor-created `BACKUP` and `TX` were one-link, non-reparse, source-identical closed files with successful file `Flush(true)`, independent stat/SHA256 receipts, and exclusive probes. Directory-handle `FlushFileBuffers` returned `Access is denied`; a custom write-through move harness was security-policy blocked before execution. The supervisor therefore opened no IDB and performed no mutation or save. Hash equality did not substitute for the missing durability barrier.
- Supported durability evidence: dated local command discovery resolved `Write-VolumeCache` as a synchronous function from Windows Storage module version `2.0.0.0`, with `InputObject`, `PassThru`, and common `ErrorAction` parameters; a read-only canonical-path query resolved one healthy local NTFS `MSFT_Volume`. Microsoft's [`Write-VolumeCache` documentation](https://learn.microsoft.com/en-us/powershell/module/storage/write-volumecache) says the cmdlet writes the file-system cache to disk and forcibly flushes the write cache by writing it to disk. Microsoft's [`MSFT_Volume.Flush` documentation](https://learn.microsoft.com/en-us/windows-hardware/drivers/storage/msft-volume-flush) describes the underlying operation as flushing cached data in the volume's file system to disk and defines `Success`, `Not Supported`, `Unspecified Error`, `Timeout`, `Failed`, and `Invalid Parameter` results. Section 21 uses only an exact successful local volume result as indirect parent-directory metadata durability evidence; B005 did not invoke the primitive.

- Authority boundary: every named session and transaction in this report is dated evidence only. The fresh operational-type snapshot supersedes `supervisor_uid0002ox_persistverify_20260817`'s earlier broad snapshot only for the three persisted/analysis type fields named above; the failed I05.08 receipt supersedes only the former expected I05.08 post-type frame display, and the latest failed I05.13 receipt supersedes only the former automatic I05.13 name/type-adoption contract and dependent allocation/protection counts. No snapshot or discarded worker is durable current/final authority or proof that a UID0000HF transaction persisted. Every Gate 2B transaction must obtain a new path-bound attestation and repeat all row-local preflight reads in that same transaction-bound session immediately before mutation.

| C0HF-053 | IDA | Derive `START_CANON_*` only from a fresh closed physical startup receipt after route retirement, all-handle closure, and an exclusive `FileShare.None` probe; historical hashes and dated repair receipts are never operational authority. Create distinct `BACKUP` and `TX` only from those bytes through collision-failing create-new/no-overwrite regular one-link non-reparse paths; for each, complete file `Flush(true)`, close every handle, pass its one exact local `INITIAL_BACKUP_CREATE` or `INITIAL_TX_CREATE` `Write-VolumeCache` receipt, then independently reopen/stat/SHA256/close and pass an exclusive probe. Require every fresh public `tools/list` to advertise exactly one `runtime_attestation`, exclude public/internal `runtime/attest`, and prove current `idb_save` request `{database,path?}` / result `{ok,error?,path}` schema; open exact `TX`, bind only its returned session as mutation/save `DB`, and preserve every I01-I09 and P01-P08/P01.01 action/frame/diagnostic/protection contract, including zero-map automatic `firstBounds@0x2c/4:const RectBounds *` at I05.08 and the exact I05.13 `arg_0@0xc/1:char` intermediate, `arg_0 -> deleteFlags` name-only mapping, and occupied-stack `unsigned __int8` type correction. After the sole exact TX save and post-save package/observation, retire `DB`, close and file-flush saved TX, pass its one `SAVED_TX_CLOSE` volume-cache receipt, and only then finalize `TX_SAVED_*`; independently reopen saved `TX` read-only as distinct `DB_VERIFY`, complete its packages/entity proof/receipt, bind fresh closed `PROMOTION_EXPECTED_CANON`, then retire `DB_VERIFY` and every CANON/TX-capable route. For each `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE`, prove exact resolved `START_CANON_PARENT`/parent identity/same volume; require normalized/resolved/case-fold path and volume-plus-file ID distinct from `CANON`, `BACKUP`, `TX`, every session path, and every prior candidate; reject reparse point, symlink, junction, mount, ADS, hardlink, alias, and collision; and use one collision-failing create-new/no-overwrite operation yielding one regular one-link new identity. A collision stops untouched with no cleanup or reuse. Copy/write only from the authorized verified source, complete candidate file `Flush(true)`, close every handle, and pass the exact `PROMOTION_CANDIDATE_CREATE` or `RESTORE_CANDIDATE_CREATE` volume-cache receipt before independent normalized identity/stat/SHA256 reopen, source-equality proof, closure, and exclusive probe. Bind one spent same-directory atomic replacement attempt to the fresh expected-CANON receipt; prohibit copy/delete fallback, another source, cross-directory/volume movement, drift, cleanup-and-retry, candidate recreation, or a second attempt. After consumption, close every handle and pass exact `PROMOTION_COMMIT` or `RESTORE_COMMIT` volume-cache receipt before independently reopening/hashing/closing `CANON`, proving sealed `BACKUP` and source unchanged, and scanning the exact parent by path/file identity for candidate/temp/alias/residue/second artifacts. A permitted pre-attempt candidate removal must use recorded identity, its one named cleanup volume-cache receipt, zero-residue scan, and permanent stop without retry. Promotion is saved-TX-only after all DB/DB_VERIFY and CANON/TX-capable routes retire; only complete commit closure permits new read-only `DB_FINAL`, whose retirement is followed by `FINAL_CANON_CLOSE` and final receipts. Any committed-promotion or final-close failure opens new read-only `DB_ROLLBACK` on failed/ambiguous `CANON` for its own baseline/packages/complete I01-I09/P01-P08/P01.01 classification and closed receipts, then retires every canonical route. No failed-canonical diagnostic copy is created; DB_ROLLBACK plus failed-CANON/BACKUP/TX receipts are the complete failure evidence. Restoration is freshly reverified sealed-BACKUP-only, uses one distinct `RESTORE_CANDIDATE` against `RESTORE_EXPECTED_CANON`, and only complete commit closure permits new read-only `DB_RESTORE`, followed after retirement by `RESTORED_CANON_CLOSE` and final original-state receipts. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are pairwise non-reusable and bind only their stated paths/authority. Any unavailable, unsupported, access-denied, ambiguous, timed-out, failed, security-policy-blocked, missing, multiple, or mismatched volume-cache result hard-stops at its exact phase; file flush, hash, probe, prior receipt, alternate utility, custom harness, or directory-handle retry is not success. | High | `idb_open`, not attestation or a preferred name, returns each authoritative session binding. Runtime proof supplies public tool surface, role-specific loaded implementation, process/route/path identity, and freshness; separate closed filesystem receipts alone supply physical identities. Microsoft documents `Write-VolumeCache` as writing the exact volume's file-system cache to disk and forcibly flushing its write cache, so a stage-specific exact-volume success receipt supplies the required indirect containing-directory durability evidence after file flush and all-handle closure. Pure rename/readback and failed I05.02/I05.05/I05.06/I05.08/I05.13 evidence preserve six exact row mappings, 31 generic plus one already-named automatic adoption (32 automatic/adopted total), one exact I05.13 occupied-stack type edit, two protected named no-op rows, and exact primitive/const/tag displays. Fresh startup receipts prevent stale-hash authority. Exact-parent/same-volume, anti-alias/link/reparse, create-new collision, one-link identity, file flush, exact-volume barrier, all-handle closure, independent hash/exclusive probe, one-attempt atomic replacement, source preservation, and no-residue gates prevent an alternate candidate or fallback path. `DB_ROLLBACK` proves the failed canonical directly; sealed `BACKUP` is the sole restore source; `DB_RESTORE` independently proves original state after replacement. Any startup, package, role, action, frame, primitive, candidate, replacement, receipt, classification, restore, or persistence mismatch follows the exact pre-open stop, unsaved discard, no-promotion, no-restore, no-retry, or named post-promotion hard-stop branch. | Supervisor Gate 2B | incorporate | proposed |

Current supervisor transaction startup is an operation contract, not historical evidence. The supervisor first binds `TX` to the exact byte-identical transaction IDB path selected after backup, then calls `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"supervisor_uid0000hf_gate2b_<UTC>"})`. The call must return `success:true`, exact returned `session.input_path == TX`, and a nonempty `session.session_id`; only that returned `session.session_id` is bound as `DB`. Only after that binding may the supervisor call `runtime_attestation({expected_database:DB,expected_canonical_path:TX})` and require `schema_version:1`, `ok:true`, and exact worker/session/path consistency. No named historical session, preferred-session request value, or attestation response may substitute for the returned `DB`. Inside that opened transaction, I04 retains pre-action `lookup_funcs` collision checks but takes immediate persisted-name authority only from exact-address `inspect_items`; function-directory and decompiler freshness are deferred to the corresponding I05 exact-address `force_recompile` and literal `decompile` stage.

Current persistence authority is also an operation contract, not historical evidence: every boundary requires a fresh public `tools/list` with exactly one advertised `runtime_attestation`, no public/internal `runtime/attest`, and the current public `idb_save` request/result schema, followed by the complete public runtime package in Section 21. Listener and worker loaded-module identities are each sealed only to their own role-specific baseline and are never required to equal one another. Runtime attestation supplies no physical IDB byte count, SHA256, or durability proof; all canonical, backup, TX, verifier, final, failed-canonical, and restored-canonical path-size-hash claims require separate filesystem receipts. The package is mandatory after open and before/after every bounded mutation/readback stage, immediately before and after the sole exact `idb_save({database:DB,path:TX}) -> {ok,error?,path}`, throughout independent `DB_VERIFY`, successful canonical `DB_FINAL`, failed-canonical classification `DB_ROLLBACK`, and restored-canonical proof `DB_RESTORE`. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are mutually exclusive newly bound roles with exact Section 21 paths and authority limits; none may reuse or substitute another role's identifier, route, baseline, package, or receipt. Initial BACKUP/TX creation, saved-TX closure, promotion and restore candidate creation/cleanup, both atomic commits, and final promoted/restored canonical closure each require the exact stage-labeled synchronous local `Write-VolumeCache` receipt on the freshly bound `MSFT_Volume`; the documented volume file-system-cache write is the only accepted indirect parent-directory metadata durability proof. Promotion and restore still require exact resolved parent/same-volume proof; normalized/resolved/case-fold and volume-plus-file-ID distinctness from every protected/session/prior-candidate identity; reparse/symlink/junction/mount/ADS/hardlink/alias rejection; collision-failing create-new/no-overwrite regular one-link creation; authorized-source-only copy; successful file `Flush(true)` and all-handle closure before the candidate-create barrier; independent identity/stat/SHA256 reopen and closed exclusive probe; one expected-CANON-bound same-directory atomic attempt with no fallback, drift, cleanup-and-retry, recreation, or second attempt; a successful post-commit volume-cache barrier before reopen; independent final CANON/source/BACKUP receipts; and exact-parent path/file-identity scan proving no candidate, temporary, alias, residue, or second artifact. Post-promotion restore occurs only from sealed `BACKUP` between retired `DB_ROLLBACK` and newly opened `DB_RESTORE`. Any unavailable, unsupported, access-denied, ambiguous, timed-out, failed, security-policy-blocked, missing, multiple, or mismatched durability result stops at its exact phase; an ordinary hash, file flush, directory-handle attempt, custom harness, alternate utility, or receipt from another stage is not success. No save receipt, inactive route, copied path, prior attestation, historical session, generic combined rollback/restore role, undefined failed-canonical copy, runtime-only response, stale historical baseline, aliased candidate, unflushed state, open handle, fallback/second attempt, or residual candidate path substitutes for those current boundaries.

This section is a declarative, non-executable supervisor handoff. B005 performed no mutation or save. Bind `CANON := E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; no report-time size or hash is operational authority. Before MCP open or any copy, retire every CANON/TX-capable stateful route, close every process/file/directory handle to canonical, reject any active or ambiguous canonical-path worker, and pass a read-only exclusive `FileShare.None` probe. Freshly resolve and normalize `CANON` and its exact parent, reject reparse/symlink/junction/mount/ADS/alias/case-fold ambiguity, require canonical to be one regular one-link file, and take a closed physical startup receipt recording normalized and resolved paths, parent identity, volume identity, file identity, byte count, uppercase SHA256, observation time, and closure proof. Bind only that receipt as `START_CANON_PATH`, `START_CANON_PARENT`, `START_CANON_PARENT_ID`, `START_CANON_VOLUME_ID`, `START_CANON_FILE_ID`, `START_CANON_SIZE`, and `START_CANON_SHA256`. Historical `7F4B827D...`, the dated `2026-08-22` repair receipt, and every prior receipt are provenance only. `BACKUP` and `TX` are supervisor-selected absolute paths for one durable backup and one isolated transaction copy made only from the freshly receipted canonical bytes. For each destination, resolve its exact parent/volume, reject reparse/symlink/junction/mount/ADS/hardlink/alias/case-fold ambiguity, require one collision-failing create-new/no-overwrite regular one-link identity, copy only canonical bytes, complete file `Flush(true)`, close every source/destination/file/directory handle, and then pass the Section 21 `INITIAL_BACKUP_CREATE` or `INITIAL_TX_CREATE` volume-cache durability barrier on that exact bound volume. Independently reopen/stat/SHA256/close each copy, pass a closed exclusive `FileShare.None` probe, and require both equal `START_CANON_SIZE`/`START_CANON_SHA256`; independently re-receipt closed `CANON` after both copies and require its complete `START_CANON_*` identity unchanged. Require `CANON`, `BACKUP`, and `TX` to be distinct regular one-link identities by normalized/resolved/case-fold path and volume-plus-file ID, with no reparse/link/ADS/alias relationship. A preexisting/colliding destination remains untouched and is never cleaned or reused. Any route, primitive, volume, probe, identity, copy, source-preservation, closure, path, size, hash, or durability-receipt mismatch hard-stops before preflight or mutation. Gate 2B never opens `CANON` or `BACKUP` for mutation and never saves either path directly.

### Mandatory Isolated Startup And Public Tool Surface

1. In the initialized public MCP session, issue a fresh public `tools/list` and require exactly one advertised tool whose literal name is `runtime_attestation`. Require zero advertised tools or methods named `runtime/attest`; the internal/unadvertised worker RPC `runtime/attest` must be absent from the public list and must never be called, constructed, proxied, or treated as evidence. The same list must expose the current public `idb_save` schema: request object `{database,path?}` with required string `database` and optional string `path`, and result object `{ok,error?,path}` with required boolean `ok`, required string-or-null `path`, and optional string `error`. Any list error, partial result, duplicate, alias, exposure, save-schema drift, or attempted internal invocation hard-stops. A tool-list digest, command line, venv path, native-module list, on-disk candidate module, or separately launched interpreter is not loaded-runtime proof.
2. Bind `TX` to the verified transaction IDB and call `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"supervisor_uid0000hf_gate2b_<UTC>"})`. Require top-level `success:true`, no error-bearing result, a non-null `session`, exact normalized `session.input_path == TX`, and a nonempty returned `session.session_id`; bind that returned value, and only that value, as `DB`. A requested/preferred identifier, historical session, `idb_list` row, canonical alias, or attestation response cannot substitute for the returned `DB`.
3. Immediately after open, and again immediately before the first preflight or mutation, pair a fresh public `tools/list` result with `runtime_attestation({expected_database:DB,expected_canonical_path:TX})` and require the complete package below. Attestation validates only the already-returned DB/TX pair; it does not open, select, rename, or return the authoritative session. Every mutation, preflight, cache refresh, decompile, readback, save, and transaction attestation request uses that exact `DB`, and every transaction-path comparison uses exact normalized `TX`.

### Complete Public Runtime-Attestation Package

The package below is indivisible and must be freshly reproduced at every scheduled boundary through `runtime_attestation({expected_database:ACTIVE_DB,expected_canonical_path:ACTIVE_PATH})`. During mutation/save, `ACTIVE_DB := DB` and `ACTIVE_PATH := TX`; during verifier readback, `ACTIVE_DB := DB_VERIFY` and `ACTIVE_PATH := TX`; during successful final canonical proof, `ACTIVE_DB := DB_FINAL` and `ACTIVE_PATH := CANON`; during post-promotion failure classification, `ACTIVE_DB := DB_ROLLBACK` and `ACTIVE_PATH := CANON`; after sealed-backup physical restoration, `ACTIVE_DB := DB_RESTORE` and `ACTIVE_PATH := CANON`. Every role is bound only from its own fresh successful `idb_open` response after the prior role's route retires. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` must be pairwise distinct and may not reuse or substitute identifiers, workers, redirectors, process generations, role-specific module baselines, runtime packages, filesystem receipts, or authority. Every named field must be present, non-null where the schema permits a value, well formed, and mutually consistent. Require top-level `schema_version:1`, `ok:true`, and `errors:[]`; require worker-local `schema_version:1`, `ok:true`, and `errors:[]`. The current listener object has no listener-local `ok` or `errors`; do not require, infer, or synthesize either field. Instead, validate listener closure exclusively through the actual listener role, fresh `attested_at_utc`/nonce, endpoint/port ownership, process identity/generation, source-root, implementation-manifest, and complete loaded-module fields in the rows below. Require fresh worker `attested_at_utc`/nonce values for the boundary and no missing, stale, ambiguous, duplicate, extra, or unbound active stateful worker/session. Cached, partially repeated, mixed-boundary, or wrong-role output grants no authority. Runtime attestation supplies runtime/session/path/process/route/module identity and freshness only; it supplies no physical IDB size or SHA256.

| Closure | Mandatory literal equality and ownership checks |
| --- | --- |
| Public tool surface | The boundary's fresh public `tools/list` contains exactly one advertised `runtime_attestation`, excludes public/internal `runtime/attest` by both tool name and method, and proves the current public `idb_save` request `{database,path?}` / result `{ok,error?,path}` schema stated above. More/fewer endpoints, an alias, exposure, attempted invocation, list error, partial result, or save-schema drift hard-stops. |
| Listener identity, generation, and port ownership | Require `listener.role == "supervisor_listener"`; fresh `listener.attested_at_utc` and `listener.nonce`; `listener.endpoint.host == "127.0.0.1"`; `listener.endpoint.port == 13337`; and independent current operating-system proof that `listener.process.pid` owns that TCP endpoint. Record and require exact `parent_pid`, `creation_time_100ns`, `generation`, `sys_executable`, `process_image`, and `argv`; reject PID/generation reuse or any unexplained listener transition. |
| Listener loaded implementation | Require exact `listener.source_root` and 64-hex `listener.implementation_manifest_sha256`. Every actually loaded `ida_pro_mcp`/`zeromcp` module row records exact `sys_modules_name`, `module_id`, `file`, `spec_origin`, `resolved_path`, `size`, `mtime_ns`, `ctime_ns`, and 64-hex `sha256`; normalized file/origin/resolved paths identify the same file, and containment under `source_root` is proved before deriving a relative path. Seal the complete ordered listener tuple set and listener manifest as the accepted listener-role startup baseline; require that listener-role identity unchanged at every later boundary. |
| One owned active-role redirector route | Require exactly one active `workers[]` route to claim both `ACTIVE_DB` and normalized `ACTIVE_PATH`, with route host/port equal to the attested worker endpoint. Its `supervisor_sessions[]` contains exactly one active row with `session_id == ACTIVE_DB`, canonical/input path exactly `ACTIVE_PATH`, expected backend, `owned == true`, and the route's exact `redirector_pid` and `registered_pid`. Reject inactive, historical, wrong-path, duplicate, competing, or unbound claims. |
| Worker process and parent route | Require worker attestation schema 1, exact worker role, fresh worker attestation time/nonce, endpoint host/port equal to the owned route, and independent proof that the worker PID owns that route port. Record exact worker PID, `parent_pid`, `creation_time_100ns`, `generation`, `sys_executable`, `process_image`, and `argv`; require worker parent, route `redirector_pid`, and `registered_pid` to reproduce the exact supervisor-spawned redirector-to-worker chain with live PID/creation-time checks. Any orphan, substituted parent, PID reuse, or route/process disagreement hard-stops. |
| Worker session and loaded implementation | Require exactly one active worker session identified by `ACTIVE_DB`, with canonical and input paths exactly `ACTIVE_PATH`; mutation/save `DB` and verifier `DB_VERIFY` permit no second writable or canonical session, while `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` permit no second writable session. Require exact worker `source_root`, 64-hex `implementation_manifest_sha256`, and the same complete loaded-module fields/containment rules as the listener. Seal the complete ordered worker tuple set and worker manifest as that worker role's accepted baseline and require the same role unchanged across its protected checkpoints. Listener and stateful-worker module sets/manifests are expected to differ by design: never require cross-role manifest, tuple, common-module, file, origin, path, size, timestamp, or SHA256 equality. Each newly created `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, or `DB_RESTORE` worker establishes its own role-specific baseline at that role's first successful post-open complete package and must remain sealed to only that baseline for the life of that role. |

### Separate Physical Filesystem Receipts

Physical IDB identity is never an attestation field. Every canonical, backup, transaction, save, verifier, promotion, final, failed-canonical classification, backup restore, and restored-canonical claim below must come from a separate closed filesystem receipt taken at its named boundary and kept distinct from public runtime proof. Each receipt records normalized and resolved absolute path, exact parent and volume identities, file identity, regular-file/link/reparse state, byte count, uppercase 64-hex SHA256, observation time, physical source/role, and handle-closure/exclusive-probe result where required. Before save, separate receipts require distinct `CANON`, `BACKUP`, and `TX` all at fresh `START_CANON_SIZE`/`START_CANON_SHA256`; no historical literal supplies those values. After the sole TX save, a new separate receipt binds physical `TX_SAVED_SIZE`/`TX_SAVED_SHA256`; runtime `path` equality and attested session/path identity establish neither value. `DB_VERIFY` and `DB_FINAL` receipts independently remeasure their opened physical paths. `DB_ROLLBACK` requires pre-open and post-read failed/ambiguous `CANON` receipts, while `DB_RESTORE` requires post-replacement pre-open and post-read restored `CANON` receipts equal to the fresh startup identity. Sealed `BACKUP` is independently re-receipted immediately before restoration and is the only authorized restore source. No runtime manifest hash, module hash, session path, save result, historical hash, or prior attempt is reused as a file-content receipt.

### Supported Windows Volume-Cache Durability Receipt

The sole accepted indirect containing-directory durability primitive is the built-in Windows Storage-module `Write-VolumeCache` operation on one exact local `MSFT_Volume`. Microsoft documents that this operation writes the volume's file-system cache to disk and forcibly flushes the write cache; the underlying `MSFT_Volume.Flush` contract says it flushes cached data in the volume's file system to disk. This volume-wide barrier is required because a supported directory-handle `FlushFileBuffers` attempt returned `Access is denied`. It supplements, and never replaces, each file's successful `Flush(true)`, all-handle closure, independent normalized-path/file-ID/size/SHA256 reread, exclusive probe, source-preservation receipt, or exact-parent residue scan.

Before each named barrier, freshly resolve the changed path to exactly one local `MSFT_Volume` and bind its `ObjectId`, `UniqueId`, volume path, drive letter when present, file-system type, health, and operational status as `DURABILITY_VOLUME`; require its volume identity to equal the file receipt's already-bound volume and reject a remote `CimSession`, cluster-wide target, array/multiple result, alias, stale object, unhealthy/non-operational volume, or path/volume mismatch. Invoke one synchronous local `Write-VolumeCache` with `InputObject := DURABILITY_VOLUME`, `PassThru := true`, `ErrorAction := Stop`, `AsJob := false`, and no remote session. The receipt records stage label, exact primitive/module identity and version, UTC start/end, input volume fields, completion without terminating or nonterminating error, underlying `Success (0)` when exposed, exactly one returned `MSFT_Volume`, exact returned/input identity equality, and a fresh post-call path-to-volume resolution equal to the same identity. A successful call is the documented indirect proof that the exact volume's file-system cache, including the preceding creation/replacement/removal metadata transition, was written to disk.

Exactly one successful receipt is required at each applicable stage: `INITIAL_BACKUP_CREATE`, `INITIAL_TX_CREATE`, `SAVED_TX_CLOSE`, `PROMOTION_CANDIDATE_CREATE`, `PROMOTION_CANDIDATE_CLEANUP` when pre-attempt cleanup is allowed, `PROMOTION_COMMIT`, `FINAL_CANON_CLOSE`, `RESTORE_CANDIDATE_CREATE`, `RESTORE_CANDIDATE_CLEANUP` when pre-attempt cleanup is allowed, `RESTORE_COMMIT`, and `RESTORED_CANON_CLOSE`. Mark each call spent before invocation. No receipt, output object, volume binding, or success from another stage or volume may be reused.

If the Storage primitive/module/provider is absent, unavailable, unsupported, access-denied, unhealthy, nonlocal, ambiguous, policy-blocked, cancelled, timed out, returns any error/non-success/missing/multiple/mismatched object, or cannot prove the exact post-call volume binding, durability is unproved. Close all obtainable handles, take failure-time file/volume/parent/residue receipts without calling them durable, and stop that phase; file `Flush(true)`, SHA256 equality, exclusive access, or a successful earlier/later barrier cannot substitute. Before `idb_open`, this leaves Gate 2B unopened and unmutated. After TX save but before atomic promotion, promote nothing and leave canonical/backup untouched. After an atomic promotion invocation, treat canonical as changed or ambiguous and enter only the named `DB_ROLLBACK` classification branch; if the primitive remains unavailable, do not attempt physical restore. Before restore commit, leave the classified failed canonical untouched. After restore invocation or during restored-final closure, grant no restored-state credit and never retry restoration in place. A permitted pre-attempt cleanup must remove only the operation-created candidate by recorded file identity and itself pass its named cleanup barrier plus zero-residue scan; otherwise cleanup is unproved and requires supervisor intervention. No alternate flush utility, directory-handle retry, custom harness, hash-only assertion, candidate recreation, cleanup-and-retry, fallback copy/delete, or second atomic attempt is permitted.

### Mandatory Boundary, Save, Verifier, Promotion, And Persistence Schedule

A bounded stage is each complete I01, I02, I03, I04, I05.01 through I05.13, I06, I07, I08, or I09 mutation/readback unit, plus each independently performed P01-P08 or P01.01 no-action protection reread. The fresh public `tools/list` plus complete attestation package is a protection gate and does not alter the action/protection topology or permit any unlisted mutation.

| Boundary | Mandatory fresh package and permitted next step |
| --- | --- |
| Post-open/startup | Pass immediately after `idb_open(TX)` and again immediately before the first preflight or mutation, using returned `DB` and exact `TX`. Only then may bounded preflight begin. |
| Every bounded mutation/readback stage | Pass immediately before the stage's first prestate read or mutation and again after that stage's complete action/protection readback, before the next stage. This brackets every declaration, definition, pure function-rename batch, each individual I05 type/optional stack substage, comment batch, vtable rename/comment action, and each independent P-row reread; no package from an earlier stage may be reused. |
| Immediately pre-save | After every I01-I09 action and every P01-P08/P01.01 protection passes, reproduce a fresh complete `DB`/`TX` package immediately before save. Re-read every entity through public endpoints and require the full final transaction state exact. No mutation, cache action, close, path operation, or unrelated call may intervene before the sole save. |
| Sole isolated save | Invoke exactly `idb_save({database:DB,path:TX}) -> {ok,error?,path}`, once, and save only the transaction path. Require `ok:true`, absent or empty `error`, and non-null `path` whose normalized absolute value equals exact `TX`; any `ok:false`, nonempty error, null/different path, missing required field, schema drift, timeout, or ambiguous response is a save failure. Do not retry in place or save `CANON`/`BACKUP`. This public result contains no accepted physical size/SHA256 proof. |
| Immediately post-save | Before verifier creation, mutation-worker retirement, close, replacement, promotion, or any other path operation, reproduce a new complete `DB`/`TX` runtime package. Then obtain a separate filesystem observation for saved `TX`, bind candidate physical `TX_SAVED_SIZE`/`TX_SAVED_SHA256`, and obtain separate receipts proving `CANON` and sealed `BACKUP` remain at fresh `START_CANON_SIZE`/`START_CANON_SHA256` with their expected unchanged file identities. This first post-save observation is content evidence, not durable saved-TX authority until the next row's closed `SAVED_TX_CLOSE` barrier passes. Runtime attestation and the `{ok,error?,path}` save result do not supply or substitute for any physical or durability receipt. |
| Independently owned verifier reopen | Only after the post-save package passes, retire the mutation redirector/worker without touching the public listener, `CANON`, `BACKUP`, or saved `TX`; close every TX-capable route and file/directory handle. Pass a closed exclusive probe, open exact saved `TX` only through an exclusive non-mutating file handle sufficient to complete file `Flush(true)`, close it, and invoke the one synchronous `SAVED_TX_CLOSE` volume-cache barrier on TX's exact bound volume. Independently reopen/stat/SHA256/close saved `TX`, require equality with the immediate post-save observation, and only then finalize `TX_SAVED_SIZE`/`TX_SAVED_SHA256` as durable saved-TX authority. Any file flush, closure, volume barrier, or rereceipt failure takes the pre-promotion no-promotion branch. Freshly reopen exact saved `TX` in a new independently owned worker/redirector route, require a different live process chain/generation and a newly returned session, bind only that `session.session_id` as `DB_VERIFY`, and require normalized `session.input_path == TX`. `DB_VERIFY` is read-only by contract: no mutation, cache-changing repair, save, promotion, or inverse action is permitted. Pass the complete `DB_VERIFY`/`TX` runtime package immediately after open, immediately before full persistence readback, and again after complete I01-I09 and P01-P08/P01.01 entity readback. Then obtain a separate verifier filesystem receipt for normalized `VERIFY_PATH == TX`, physical `TX_SAVED_SIZE`, and physical `TX_SAVED_SHA256`; attested path equality alone is insufficient. |
| Pre-promotion physical identity gate | Immediately before any separately authorized promotion, pass a fresh complete `DB_VERIFY`/`TX` runtime package and full verifier readback. Independently obtain closed filesystem receipts for all four role labels: `CANON` remains the full fresh `START_CANON_*` identity; distinct sealed `BACKUP` remains the same startup content identity and its own unchanged file identity; physical `TX` remains `TX_SAVED_SIZE`/`TX_SAVED_SHA256`; and verifier `session.input_path` (`VERIFY_PATH`) normalizes and resolves exactly to `TX` while the separately measured verifier-path identity/size/hash equal saved TX. Bind the current closed canonical receipt, including file identity/size/hash and observation time, as `PROMOTION_EXPECTED_CANON`; the atomic replacement may target no other state. `CANON`, `BACKUP`, and `TX` remain distinct by normalized/resolved/case-fold path and volume-plus-file ID; `VERIFY_PATH == TX` is the required verifier binding, not a fourth copy. Any route, alias, link, reparse, path, identity, size, hash, or closure mismatch means promote nothing. This report grants no promotion authority. |
| Fail-closed promotion boundary | Promotion may begin only under separate supervisor authorization after the pre-promotion gate and after `DB`, `DB_VERIFY`, and every CANON/TX-capable stateful route retire. Close every CANON/TX/source/destination/file/directory handle; prove no active or ambiguous route; pass closed exclusive `FileShare.None` probes; and freshly re-receipt `CANON == PROMOTION_EXPECTED_CANON`, sealed `BACKUP == START_CANON_*`, and physical `TX == TX_SAVED_*`. Resolve the prospective `PROMOTION_CANDIDATE` parent and require exact normalized/resolved equality with `START_CANON_PARENT`, exact `START_CANON_PARENT_ID`, and exact `START_CANON_VOLUME_ID`, proving the candidate and `CANON` are in the same resolved parent and volume. Before creation, reject any reparse point, symlink, junction, mount, ADS, hardlink, alias, alternate normalized/resolved spelling, case-fold collision, or path/file-identity equality with `CANON`, `BACKUP`, `TX`, every recorded session path, and every current or prior candidate. Use exactly one collision-failing create-new/no-overwrite operation; if any path or case-fold/alias collision exists, stop without opening, deleting, renaming, cleaning, or reusing that colliding path. The successful create must yield one new regular one-link non-reparse file whose normalized/resolved path and volume-plus-file ID are distinct from every forbidden identity; record that identity as the only `PROMOTION_CANDIDATE` for this phase and forbid candidate recreation. Open verified saved `TX` read-only as the sole authorized source and the new candidate as the sole destination, copy/write only saved-TX bytes, complete candidate file `Flush(true)`, and close every source/destination/file/directory handle. Invoke the one synchronous `PROMOTION_CANDIDATE_CREATE` volume-cache barrier on exact `START_CANON_VOLUME_ID`; this is the required indirect exact-parent creation-metadata durability proof. Independently reopen the closed candidate read-only only to re-resolve/normalize it, stat regular/link/reparse/volume/file identity and byte count, and compute SHA256; require exact saved-TX content identity plus all path/anti-alias invariants, then close it and pass a separate exclusive read-only `FileShare.None` probe that is also closed. Immediately re-receipt closed `CANON` against `PROMOTION_EXPECTED_CANON`, saved `TX` against `TX_SAVED_*`, and sealed `BACKUP` against `START_CANON_*`; bind those exact current receipts and the verified closed candidate to one same-directory atomic replacement attempt, mark the attempt spent before invocation, and permit exactly one guarded atomic consume. Prohibit copy/delete fallback, another source, cross-directory/cross-volume movement, canonical/candidate drift, cleanup-and-retry, candidate recreation, or a second replacement attempt. After successful consumption, close every replacement/file/directory handle and invoke the one synchronous `PROMOTION_COMMIT` volume-cache barrier on exact `START_CANON_VOLUME_ID`; the candidate's completed file flush plus this documented volume-file-system-cache flush are the required durable promoted-file and exact-parent replacement/removal metadata closure. Independently reopen/stat/SHA256/close `CANON` and require saved-TX content, independently prove saved `TX` and sealed `BACKUP` unchanged, and scan the exact resolved parent by normalized/resolved/case-fold path and file identity for the candidate name/identity, temporary names, hardlink aliases, residue, or any second promotable artifact. Require `PROMOTION_CANDIDATE` absent and zero operation-created residue before `DB_FINAL` may open. Any collision leaves the colliding object untouched. Any pre-attempt failure may remove only this operation-created candidate after proving its recorded identity, then must close all handles, pass the one `PROMOTION_CANDIDATE_CLEANUP` volume-cache barrier, scan the exact parent for zero residue, and stop permanently; that cleanup grants no retry. Any candidate-create/cleanup barrier failure is an unproved pre-promotion state requiring supervisor intervention and no promotion. Any atomic failure or ambiguity exhausts the attempt, permits no fallback or retry, requires closed physical receipts and residue accounting, and enters `DB_ROLLBACK` if canonical changed or cannot be proven unchanged. Any `PROMOTION_COMMIT` barrier failure always classifies canonical as changed/ambiguous and enters `DB_ROLLBACK`; `DB_FINAL` has no rollback or restore authority. |
| Post-promotion failed-state classification: `DB_ROLLBACK` | After any committed-promotion path/hash/size, open, runtime-package, entity, route, or final-persistence failure, stop all Gate 2B/lifecycle credit and retire every prior `DB`, `DB_VERIFY`, or `DB_FINAL` route. Before opening, obtain a separate filesystem receipt for exact active `CANON` and bind its observed failed/ambiguous physical identity as `ROLLBACK_CANON_SIZE`/`ROLLBACK_CANON_SHA256`; preserve saved `TX` and sealed `BACKUP`. Freshly call `idb_open({input_path:CANON,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"supervisor_uid0000hf_rollback_<UTC>"})`; require success, exact normalized returned `session.input_path == CANON`, and a new session identifier unequal to every prior role, then bind only it as `DB_ROLLBACK`. Establish and seal the rollback worker's own role-specific module baseline in a fresh complete `DB_ROLLBACK`/`CANON` package immediately post-open; repeat fresh indivisible packages immediately before and immediately after the complete failed-state readback. That readback must classify every I01-I09 entity/result and every P01-P08/P01.01 protection, record every exact mismatch/ambiguity, and permit no repair. Obtain a second separate `CANON` receipt after readback and require it unchanged from the pre-open rollback receipt. `DB_ROLLBACK` is read-only classification authority only: no mutation, save, cache-changing repair, inverse action, promotion, physical replacement, backup write, or restore authority. Retire it before any restore operation. |
| Sealed-backup physical restoration | Restoration is a supervisor-owned closed-route physical operation, never an IDB session action. Complete `DB_ROLLBACK` classification first, obtain its unchanged post-read failed-CANON receipt, retire `DB_ROLLBACK` and every canonical-capable route, close every CANON/BACKUP/TX/source/destination/file/directory handle, prove no active or ambiguous canonical route, and pass closed exclusive probes. Bind a fresh closed receipt equal to the classified failed canonical as `RESTORE_EXPECTED_CANON`. Independently re-receipt sealed `BACKUP` and require its original path/file identity and `START_CANON_SIZE`/`START_CANON_SHA256`; preserve saved `TX`. No failed-canonical diagnostic copy is created or retained: failure evidence is only the complete `DB_ROLLBACK` readback plus closed physical failed-CANON, BACKUP, and TX receipts. Resolve the prospective `RESTORE_CANDIDATE` parent and require exact normalized/resolved equality with `START_CANON_PARENT`, exact `START_CANON_PARENT_ID`, and exact `START_CANON_VOLUME_ID`, proving the candidate and `CANON` are in the same resolved parent and volume. Before creation, reject any reparse point, symlink, junction, mount, ADS, hardlink, alias, alternate normalized/resolved spelling, case-fold collision, or path/file-identity equality with `CANON`, `BACKUP`, `TX`, every recorded session path, and every current or prior candidate. Use exactly one collision-failing create-new/no-overwrite operation; if any path or case-fold/alias collision exists, stop without opening, deleting, renaming, cleaning, or reusing that colliding path. The successful create must yield one new regular one-link non-reparse file whose normalized/resolved path and volume-plus-file ID are distinct from every forbidden identity; record that identity as the only `RESTORE_CANDIDATE` for this phase and forbid candidate recreation. Open freshly reverified sealed `BACKUP` read-only as the sole authorized source and the new candidate as the sole destination, copy/write only original-backup bytes, complete candidate file `Flush(true)`, and close every source/destination/file/directory handle. Invoke the one synchronous `RESTORE_CANDIDATE_CREATE` volume-cache barrier on exact `START_CANON_VOLUME_ID`; this is the required indirect exact-parent creation-metadata durability proof. Independently reopen the closed candidate read-only only to re-resolve/normalize it, stat regular/link/reparse/volume/file identity and byte count, and compute SHA256; require exact original-BACKUP content identity plus all path/anti-alias invariants, then close it and pass a separate exclusive read-only `FileShare.None` probe that is also closed. Immediately re-receipt closed `CANON` against `RESTORE_EXPECTED_CANON`, sealed `BACKUP` against its original receipt, and saved `TX` against `TX_SAVED_*`; bind those exact current receipts and the verified closed candidate to one same-directory atomic replacement attempt, mark the attempt spent before invocation, and permit exactly one guarded atomic consume. Prohibit copy/delete fallback, another source, cross-directory/cross-volume movement, canonical/backup/candidate drift, cleanup-and-retry, candidate recreation, or a second replacement attempt. After successful consumption, close every replacement/file/directory handle and invoke the one synchronous `RESTORE_COMMIT` volume-cache barrier on exact `START_CANON_VOLUME_ID`; the candidate's completed file flush plus this documented volume-file-system-cache flush are the required durable restored-file and exact-parent replacement/removal metadata closure. Independently reopen/stat/SHA256/close `CANON` and require `START_CANON_SIZE`/`START_CANON_SHA256`, independently prove sealed `BACKUP` and saved `TX` unchanged, and scan the exact resolved parent by normalized/resolved/case-fold path and file identity for the candidate name/identity, temporary names, hardlink aliases, residue, or any second restore artifact. Require `RESTORE_CANDIDATE` absent and zero operation-created residue before `DB_RESTORE` may open. Any collision leaves the colliding object untouched. Any pre-attempt failure may remove only this operation-created candidate after proving its recorded identity, then must close all handles, pass the one `RESTORE_CANDIDATE_CLEANUP` volume-cache barrier, scan the exact parent for zero residue, and stop permanently; that cleanup grants no retry. Any candidate-create/cleanup barrier failure leaves an unproved failed-canonical environment requiring supervisor intervention and no restore. Any atomic failure or ambiguity exhausts the attempt, permits no fallback or retry, and requires supervisor intervention with closed physical receipts and residue accounting; never restore again in place. Any `RESTORE_COMMIT` barrier failure classifies the restored canonical as durability-ambiguous, grants no restored-state credit, and forbids another restore in place. A replacement receipt alone is not restored-state proof and grants no Gate 2B/lifecycle credit. |
| Restored-original-state proof: `DB_RESTORE` | Only after the complete BACKUP-only atomic restoration closure and its fresh restored-canonical receipt, freshly call `idb_open({input_path:CANON,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"supervisor_uid0000hf_restore_<UTC>"})`; require success, exact normalized/resolved returned `session.input_path == CANON`, and a new identifier unequal to `DB`, `DB_VERIFY`, `DB_FINAL`, and `DB_ROLLBACK`, then bind only it as `DB_RESTORE`. Establish and seal this restore worker's own role-specific module baseline in a fresh complete `DB_RESTORE`/`CANON` package immediately post-open; repeat fresh indivisible packages immediately before and immediately after complete original-state readback. The readback must reproduce the entire literal pre-I01 transaction state and every P01-P08/P01.01 protection: proposed enum/UDT absent, raw constructor not a function, all original names/types/profiles/decompiler/frame/comment states exact, original vtable identity/comment state exact, all protected RTTI/EH/stub/successor/dependency records exact, and every range/byte/body-hash/xref invariant exact. Obtain separate restored-`CANON` receipts before open and after post-read; both must equal fresh `START_CANON_SIZE`/`START_CANON_SHA256`, while separately receipted `BACKUP` remains sealed and saved `TX` remains at its saved identity. `DB_RESTORE` is read-only original-state proof only and has no mutation, save, cache-changing repair, promotion, rollback, replacement, or further restore authority. Retire it after proof, close every canonical/file/directory handle, pass the one synchronous `RESTORED_CANON_CLOSE` volume-cache barrier on exact `START_CANON_VOLUME_ID`, then independently re-receipt restored `CANON`, sealed `BACKUP`, saved `TX`, and exact-parent zero residue. Only that final closure completes restored-original-state proof. Any mismatch or barrier failure hard-stops without restored-state credit; do not reuse either phase role or restore again in place. |
| Final canonical persistence | Only after the complete saved-TX-only atomic promotion closure, obtain separate filesystem receipts requiring `CANON` content equal `TX_SAVED_SIZE`/`TX_SAVED_SHA256`, sealed `BACKUP` still equal fresh `START_CANON_SIZE`/`START_CANON_SHA256` with unchanged backup file identity, and saved `TX` still equal its saved content and file identities. Reopen exact `CANON` in a new read-only final worker, require exact normalized/resolved returned `session.input_path == CANON`, bind only its newly returned session as `DB_FINAL`, and pass the complete `DB_FINAL`/`CANON` runtime package immediately after open, immediately before full entity readback, and again after it. Full readback must reproduce every I01-I09 result and P01-P08/P01.01 protection: enum/UDT identities, function definition/ranges/names/types/profiles/decompiler dispositions, complete frames, all comment channels, bytes/body hashes/xrefs, vtable data, protected RTTI/EH/stub/successor state, and dependency types. Retire the final read-only route, close every canonical/file/directory handle, pass the one synchronous `FINAL_CANON_CLOSE` volume-cache barrier on exact `START_CANON_VOLUME_ID`, obtain new independent filesystem receipts for `CANON`, `BACKUP`, and `TX`, require their final identities unchanged, repeat the exact-parent zero-residue/alias scan, and prove no active writable/mutation route remains. Only that canonical reopen, complete public attestation, complete entity readback, final volume-cache barrier, and separate final physical closure is persistence proof. Any `DB_FINAL` mismatch or `FINAL_CANON_CLOSE` failure is a post-promotion failure and may proceed only to a newly returned `DB_ROLLBACK`; `DB_FINAL` cannot classify or restore. |

Any package or stage failure before save means save nothing, retire only the isolated redirector/worker, discard `TX`, and leave freshly receipted `CANON`/sealed `BACKUP` unchanged. Any initial-copy `Write-VolumeCache` failure occurs before `idb_open`: close and receipt what exists, call no fallback, perform no mutation, and stop with durability/residue unresolved until supervisor intervention. A failure during or after TX save but before the guarded atomic promotion attempt, including `SAVED_TX_CLOSE`, candidate-create, or candidate-cleanup barrier failure, means promote nothing and keep canonical/backup untouched; only a freshly bound independent `DB_VERIFY` may inspect saved TX after `SAVED_TX_CLOSE` itself passed. Candidate collision leaves the colliding path untouched and permanently stops that phase; an operation-created pre-attempt candidate may be removed only by its recorded identity followed by its one named cleanup barrier and zero-residue scan, and is never recreated. One atomic attempt is spent before invocation; failure or ambiguity permits no copy/delete fallback, cleanup-and-retry, alternate source, or second attempt. A failed/ambiguous `PROMOTION_COMMIT` or `FINAL_CANON_CLOSE` receipt classifies canonical as changed/ambiguous even when hashes look expected. Any changed or ambiguous post-attempt canonical grants no Gate 2B, coverage, validation, execution, or lifecycle credit and must follow the exact ordered branch: retire prior roles; newly bind read-only `DB_ROLLBACK` to failed/ambiguous `CANON`; pass fresh post-open/pre-read/post-read packages, complete failed-state readback, and closed physical CANON/BACKUP/TX receipts; retire `DB_ROLLBACK` and every canonical route. No failed-canonical diagnostic copy is created or retained. Restore physically only from freshly reverified sealed `BACKUP` through the one-attempt `RESTORE_CANDIDATE` protocol and only while the volume-cache primitive is freshly supported; primitive failure before commit means no restore, while failure after restore invocation or at `RESTORED_CANON_CLOSE` grants no restored-state credit and permits no repeat restore. Newly bind distinct read-only `DB_RESTORE` only after complete `RESTORE_COMMIT` durability and zero-residue closure. No generic combined role or reuse among `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` is permitted. Dated snapshots remain expected-prestate evidence, never current startup/final authority. A later stage is forbidden until startup, transaction-local prestate, complete immediate poststate, and every protection/persistence boundary match; file flush/hash/probe evidence without its named volume-cache receipt never advances the schedule.

`AR`, `AP`, `FR`, and `FP` mean address regular, address repeatable, function regular, and function repeatable comments. Each channel is independently stated. Older named sessions remain dated historical evidence for their bounded scopes. The earlier broad `supervisor_uid0002ox_persistverify_20260817` snapshot supplies the twelve modeled action-target records and P01-P08 protection rows below. A fresh path/session-bound read-only snapshot in the same canonical session, listener-attested at `2026-08-18T02:34:14.374111+00:00` and worker-attested at `2026-08-18T02:34:14.511200+00:00`, supersedes the earlier snapshot only for the three operational persisted/analysis type fields at `0x4622d0`, `0x462320`, and `0x462520`. No IDA mutation or save occurred. The session may disappear or drift; every row requires a new path-bound attestation and immediate transaction-local preflight before its action.

Final save/persistence contract: the complete schedule above is mandatory, not optional summary guidance. Initial `BACKUP`/`TX` creation must already have successful file `Flush(true)`, all-handle closure, separate `INITIAL_BACKUP_CREATE`/`INITIAL_TX_CREATE` exact-volume `Write-VolumeCache` receipts, independent physical rereads, and exclusive probes before any `idb_open`. After I01-I09, P01-P08, and P01.01 pass, the supervisor must complete the fresh pre-save runtime package, exact one-time `idb_save({database:DB,path:TX}) -> {ok,error?,path}` contract, fresh post-save runtime package, mutation-route retirement, closed saved-TX file flush and `SAVED_TX_CLOSE` barrier, separate physical saved-TX receipt, independent read-only `DB_VERIFY` reopen and full entity proof, separately receipted four-role pre-promotion path/hash/size gate, separately authorized fail-closed promotion, and successful read-only `DB_FINAL` canonical reopen/complete-attestation/full-entity/`FINAL_CANON_CLOSE`/separate-physical-persistence proof. `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` each require the exact Section 21 sequence: resolved `START_CANON_PARENT` identity and same-volume proof; normalized/resolved/case-fold path and volume-plus-file-ID distinctness from `CANON`, `BACKUP`, `TX`, every session path, and every prior candidate; rejection of reparse point, symlink, junction, mount, ADS, hardlink, alias, and collision; one create-new/no-overwrite regular one-link identity whose collision stops untouched; authorized-source-only copy/write; candidate file `Flush(true)`; closure of every handle; one exact `PROMOTION_CANDIDATE_CREATE` or `RESTORE_CANDIDATE_CREATE` volume-cache receipt as indirect exact-parent creation-metadata durability proof; independent identity/stat/SHA256 reopen equal to saved TX or original BACKUP; closed exclusive `FileShare.None` probe; one expected-CANON-bound same-directory atomic attempt with no fallback, drift, cleanup-and-retry, recreation, or second attempt; one exact `PROMOTION_COMMIT` or `RESTORE_COMMIT` volume-cache receipt before reopened-CANON proof; independent CANON/source/BACKUP receipts; and exact-parent path/file-identity scan proving no candidate, temporary, alias, residue, or second artifact. A permitted pre-attempt candidate removal requires its one named cleanup barrier and zero-residue scan but grants no retry. Promotion uses only verified saved `TX` after `DB`/`DB_VERIFY` and every CANON/TX-capable route retire; restoration uses only freshly reverified sealed `BACKUP` after `DB_ROLLBACK` classification and every canonical route retire. No failed-canonical diagnostic copy exists; the rollback readback and closed CANON/BACKUP/TX receipts are the complete failure record. A post-promotion failure, including commit/final-close durability ambiguity, has one ordered no-credit branch: newly returned read-only `DB_ROLLBACK` on failed/ambiguous `CANON` with its own baseline, fresh post-open/pre-read/post-read packages, complete failed-state readback, and separate receipts; route retirement; closed-route physical replacement only from sealed/reverified `BACKUP` while the volume primitive is supported; then distinct newly returned read-only `DB_RESTORE` on restored `CANON` with its own baseline, fresh post-open/pre-read/post-read packages, complete original-state/protection readback, `RESTORED_CANON_CLOSE`, and separate baseline receipts. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are pairwise non-reusable and each has only its exact path and authority. Each listener/worker role stays sealed only to its own role-specific module baseline; no cross-role equality is required. Runtime attestation supplies no physical IDB size/SHA256 or durability proof. Any unavailable, unsupported, access-denied, ambiguous, timed-out, failed, security-policy-blocked, missing, multiple, or mismatched volume-cache result hard-stops at its exact phase; no file flush/hash/probe, prior receipt, alternate utility, directory retry, or custom harness substitutes. The public stack batch is not claimed rollback-atomic: any dry-run/apply mismatch, partial success, error, I05.13 occupied-stack type receipt/readback mismatch, or later protection drift before save requires immediate unsaved-worker discard and a clean retry from I01; never attempt inverse renames or save a contaminated worker. Failures after save but before promotion use no-promotion; failures after promotion use only the named two-session classification/restoration branch. B005 never applies these actions.

The later pre-open durability stop likewise changes no source, score, ordinary implementation, or IDA recommendation. It proves only that successful file `Flush(true)`, closure, hashes, and exclusive probes cannot satisfy an exact parent-directory durability gate when directory-handle `FlushFileBuffers` is access-denied. The renewed persistence handoff therefore requires the built-in synchronous local `Write-VolumeCache` stage receipts defined in Section 21 for initial copies, saved TX, candidate creation/cleanup, both atomic commits, and final promoted/restored canonical closure. The primitive's documented exact-volume file-system-cache write supplies the indirect directory-metadata barrier; any unavailable, denied, unsupported, ambiguous, timed-out, failed, blocked, or identity-mismatched result stops without fallback, retry, candidate recreation, hash-only acceptance, Gate 2B credit, or lifecycle credit.

| Gate 2B prestate durability | all named sessions compared with fresh `idb_list`; the earlier broad I01-I09/P01-P08 snapshot was retained, and fresh path-bound reads corrected only the three operational persisted/analysis type fields at `0x4622d0`, `0x462320`, and `0x462520`; public startup and pure-rename semantics were checked separately | every named session and snapshot is dated evidence only; supervisor must bind exact `TX`, call full-schema `idb_open`, bind returned `session.session_id` as `DB`, then attest that DB/TX pair before immediate preflight. I04 uses exact-address persisted-name authority; directory/decompiler freshness is deferred to address-specific I05 recompile/decompile. Any literal drift requires report repair. |

3. Supervisor performs fail-closed Gate 2B from Section 21 by deriving `START_CANON_*` only from a fresh closed physical CANON startup receipt after route retirement, all-handle closure, and an exclusive probe; creating distinct `BACKUP`/`TX` only from that identity through collision-failing create-new/no-overwrite regular one-link paths; completing each file's `Flush(true)` and all-handle closure; and passing separate `INITIAL_BACKUP_CREATE`/`INITIAL_TX_CREATE` synchronous local `Write-VolumeCache` receipts on each exact bound volume before independent reopen/hash/probe. Historical hashes, repair receipts, and the dated access-denied directory-flush/custom-harness-block attempt remain non-authoritative provenance. Require a public `tools/list` with exactly one advertised `runtime_attestation`, no public/internal `runtime/attest`, and current `idb_save` request `{database,path?}` / result `{ok,error?,path}` schema, open exact `TX`, bind only the returned session as `DB`, and pass the complete public runtime package after open and before/after every bounded stage. Require top-level and worker-local `schema_version:1`/`ok:true`/`errors:[]`; never require listener-local `ok` or `errors`, and validate the listener through its actual role/freshness/endpoint/process/generation/source-root/manifest/module fields. Seal listener and each worker role only against its own role-specific module baseline; never require cross-role module/manifest equality. Reproduce every retained literal plus the automatic-adoption/preserved-row prestates and restart at I01. I04 retains exact-address persisted-name authority; I03/I05.01 retain the exact diagnostic disposition. I05.02-I05.13 retain the one-edit function-type/cache/decompile/profile/frame sequence. Thirty-one generic rows plus already-named I05.08 `firstBounds` adopt automatically, for 32 automatic/adopted rows total; I05.08 requires exact `firstBounds@0x2c/4:const RectBounds *` and zero stack mappings. I05.02, I05.06, I05.11, I05.12, and I05.13 perform the six collision-checked name-only stack mappings. I05.13 must first reproduce `arg_0@0xc/1:char`, dry-run/apply only `arg_0 -> deleteFlags`, prove the immediate name-only row remains `deleteFlags@0xc/1:char`, apply exactly one supported `set_type({database:DB,edits:[{addr:"0x463270",kind:"stack",name:"deleteFlags",variable:"deleteFlags",ty:"unsigned __int8"}]})` result, then pass its second address-specific recompile/decompile/profile/full-frame protection readback with final `deleteFlags@0xc/1:unsigned __int8`. `point` and `outView` remain the two protected named no-op rows. Any fresh-startup, route, primitive, volume, closure, package, action, receipt, frame, or protection mismatch stops before the next stage and discards the worker unsaved; file flush/hash/probe evidence without the named exact-volume receipt is not durability.

4. After every I01-I09 and P01-P08/P01.01 result passes, supervisor performs the exact Section 21 persistence schedule: fresh complete pre-save runtime package; sole `idb_save({database:DB,path:TX}) -> {ok,error?,path}` with `ok:true`, absent/empty error, and normalized `path == TX`; fresh complete post-save runtime package; DB retirement, saved-TX file flush/closure, one `SAVED_TX_CLOSE` volume-cache receipt, and separate physical saved-TX/unchanged canonical/backup receipts; independently owned read-only `DB_VERIFY` reopen with complete pre/post entity readback and separate verifier-path receipt; separately receipted four-role `CANON`/`BACKUP`/`TX`/verifier path-hash-size gate; separately authorized fail-closed promotion; and final read-only `DB_FINAL` canonical reopen with complete public attestation, every entity/protection reread, one `FINAL_CANON_CLOSE` barrier after route retirement, separate final disk receipts, zero-residue scan, and no active mutation route. Both named candidates require exact resolved-parent identity and same-volume proof; path/file-identity distinctness from canonical, backup, TX, every session path, and every prior candidate; complete reparse/symlink/junction/mount/ADS/hardlink/alias/case-fold rejection; collision-failing create-new/no-overwrite one-regular-one-link identity with collision stop/no cleanup or reuse; authorized-source-only copy; file `Flush(true)` and all-handle closure; one exact candidate-create volume-cache receipt as indirect parent-metadata durability proof; independent identity/stat/SHA256 reopen and exclusive `FileShare.None` probe; one expected-CANON-bound same-directory atomic attempt with no copy/delete fallback, alternate source, cross-directory/volume move, drift, cleanup-and-retry, recreation, or second attempt; one exact post-commit volume-cache receipt; independent CANON/source/BACKUP proof; and exact-parent path/file-identity scan proving zero candidate/temp/alias/residue/second artifact. A permitted pre-attempt candidate removal requires its one named cleanup barrier and zero-residue scan without retry. `PROMOTION_CANDIDATE` is saved-TX-only after every DB/DB_VERIFY and CANON/TX-capable route retires; `RESTORE_CANDIDATE` is sealed-BACKUP-only after complete failed-state `DB_ROLLBACK` classification and every canonical route retires. No failed-canonical diagnostic copy is created; rollback readback plus closed CANON/BACKUP/TX receipts are complete failure evidence. Runtime proof never substitutes for physical IDB size/SHA256 or durability. Any unavailable, unsupported, access-denied, ambiguous, timed-out, failed, policy-blocked, missing, multiple, or mismatched volume-cache result stops at its exact phase without alternate utility, directory retry, custom harness, hash-only acceptance, cleanup-and-retry, or second attempt. A failure after save but before promotion promotes nothing. Any failure after committed promotion earns no credit and requires the exact separate phases: retire prior roles; newly open failed/ambiguous `CANON` read-only as `DB_ROLLBACK` for its own baseline, post-open/pre-read/post-read packages, complete failed-state readback, and receipts; retire it; restore only from freshly reverified sealed `BACKUP` through the one-attempt closed-route `RESTORE_CANDIDATE` sequence while the primitive is supported; then newly open restored `CANON` read-only as distinct `DB_RESTORE` for its own baseline, post-open/pre-read/post-read packages, complete original-state/protection readback, `RESTORED_CANON_CLOSE`, and baseline receipts. Never reuse or substitute `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, or `DB_RESTORE`.

The earlier isolated-TX persistence-contract repair likewise ran no validator, touched no IDA or lifecycle state, and refreshed no generated artifact. It established the public save/verifier/promotion/final proof but still used a generic combined rollback/restore role, so it was not sufficient for fresh Gate 1.

This distinct `DB_ROLLBACK`/`DB_RESTORE` same-report repair ran no validator, touched no IDA or lifecycle state, and refreshed no generated artifact. It changes only the report contract for a fresh supervisor Gate 1 and claims no Gate 1, Gate 2, validation, or lifecycle completion itself.

This listener-schema correction likewise ran no validator, touched no IDA or lifecycle state, and refreshed no generated artifact. It removes the invalid listener-local `ok`/`errors` requirement while retaining top-level and worker-local status/error closure and the complete actual listener identity/process/module proof.

The earlier candidate-durability correction likewise ran no validator, touched no IDA/runtime/lifecycle state, and refreshed no generated artifact. It established the promotion/restore candidate closure but predated the supported exact-volume barrier now required for parent-directory metadata durability; it is historical repair context and grants no current Gate 1, Gate 2, validation, or lifecycle completion by itself.

This fresh-baseline/candidate-identity correction likewise ran no validator, touched no IDA/runtime/lifecycle state, and refreshed no generated artifact. It removes stale operational hash authority, completes both one-attempt candidate closures, and removes the undefined failed-canonical diagnostic copy only in this report.

This 2026-08-22 durability-handoff correction likewise ran no validator, invoked no `Write-VolumeCache` flush, touched no IDA/runtime/lifecycle state, and refreshed no generated artifact. It preserves prior Gate 2A ordinary implementation credit while requiring a fresh exact-artifact Gate 1 audit before Gate 2B resumes. The supervisor's access-denied directory-flush and blocked custom-harness attempt remains historical failed-closed evidence; the new report contract uses only the supported declarative exact-volume receipt protocol.

The earlier isolated-TX persistence repair also changed only this same report. It additively expanded the current-state contract, Section 21, final persistence summary, Follow-Up Actions, and exact C0HF-053 ledger/checklist twins with the public tool surface, save result, repeated attestation, independent verifier, pre-promotion, promotion, and final canonical persistence requirements, but its combined rollback/restore role was incomplete. It changed no by-* document, IDA database, coverage/generated/tracker/audit/catalog/lifecycle state, formal CPP/H payload, score, owner/emitter route, claim ID/order, or actor allocation.

This current distinct-role repair changed only this same report. It reconciles Supervisor Active Recheck, current persistence authority, Section 21, the final save summary, Follow-Up Actions, Validator Results, Changed Files, and exact C0HF-053 twins to define read-only `DB_ROLLBACK` failed-state classification, sealed-`BACKUP` physical restoration, and separate read-only `DB_RESTORE` original-state proof with pairwise role non-reuse. It changed no ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA database, formal CPP/H payload, score, owner/emitter route, claim ID/order, actor allocation, or terminal marker.

This listener-schema correction also changed only this same report. It reconciles Supervisor Active Recheck, the dated runtime-summary wording, Section 21's complete public package, Follow-Up Actions, Validator Results, and Changed Files to require status/error fields only where the current public schema supplies them and to preserve complete listener identity/process/module validation. It changed no ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA database, formal CPP/H payload, score, owner/emitter route, claim ID/order, actor allocation, distinct persistence-role contract, or terminal marker.

The earlier candidate-durability correction also changed only this same report. It reconciled the then-current persistence summaries with named exact-parent candidates, authorized sources, requested file/directory durability, all-handle closure, independent closed-file verification, guarded atomic consumption, and zero-residue proof. Its unspecified directory-flush mechanism is superseded by the present stage-labeled exact-volume protocol; all other candidate guarantees remain preserved. It changed no ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA/runtime state, formal CPP/H payload, score, owner/emitter route, claim identity/order, actor allocation, five-role state machine, or terminal marker.

This fresh-baseline/candidate-identity repair also changed only this same report. It reconciles Supervisor Active Recheck, dated current-state provenance, current persistence authority, Section 21, exact C0HF-053 twins, final persistence summary, Follow-Up Actions, Validator Results, and Changed Files with fresh closed startup authority, full exact-parent/same-volume and anti-alias candidate identity, collision-stop create-new semantics, exclusive probes, one expected-CANON-bound atomic attempt, post-replacement durability/source/residue receipts, and no failed-canonical diagnostic copy. It changed no ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA/runtime state, formal CPP/H payload, score, owner/emitter route, claim identity/order, actor allocation, five-role state machine, or terminal marker.

This 2026-08-22 durability-handoff repair changed only this same report. It reconciles Finalized Report / Current Recommendation, Current Target State, Supervisor Active Recheck, Evidence Checked, C0HF-053 and its exact twin, current persistence authority, the Section 21 startup/physical-receipt/save/verifier/promotion/restoration/final-closure schedule, Final Recommendation, final persistence summary, Follow-Up Actions, Validator Results, and Changed Files with the supported synchronous local `Write-VolumeCache` exact-volume barrier and its phase-specific fail-closed receipt protocol. It preserves every ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA/runtime state, formal CPP/H payload, score, owner/emitter route, claim identity/order, 64/36 allocation, five-role state machine, and terminal marker.

This I05.13 additive repair changed only this same report. It adds the latest unsaved I05.13 transaction/rollback evidence and current public action-schema proof; reconciles Finalized Report / Current Recommendation, Current Target State, Supervisor Active Recheck, Evidence Checked, IDA Recommendations, I05.13 snapshot/action/readback, Sequential Function Type Actions, stack allocation, parser/tag audit, P01/P01.01, C0HF-053/C0HF-057 and their exact checklist twins, Final Recommendation, Open Questions, Follow-Up Actions, Validator Results, and Changed Files; and changes the exact allocation from 33 automatic/adopted plus five renamed rows to 32 automatic/adopted plus six renamed rows and one I05.13 occupied-stack type edit, while preserving two protected named no-op rows. It changes no ordinary/generated/coverage/tracker/audit/catalog/lifecycle file, IDA/runtime state, formal CPP/H payload, score, owner/emitter route, claim identity/order, 64/36 allocation, five-role persistence state machine, supported durability protocol, or terminal marker.

| [ ] | C0HF-053 | IDA | Derive `START_CANON_*` only from a fresh closed physical startup receipt after route retirement, all-handle closure, and an exclusive `FileShare.None` probe; historical hashes and dated repair receipts are never operational authority. Create distinct `BACKUP` and `TX` only from those bytes through collision-failing create-new/no-overwrite regular one-link non-reparse paths; for each, complete file `Flush(true)`, close every handle, pass its one exact local `INITIAL_BACKUP_CREATE` or `INITIAL_TX_CREATE` `Write-VolumeCache` receipt, then independently reopen/stat/SHA256/close and pass an exclusive probe. Require every fresh public `tools/list` to advertise exactly one `runtime_attestation`, exclude public/internal `runtime/attest`, and prove current `idb_save` request `{database,path?}` / result `{ok,error?,path}` schema; open exact `TX`, bind only its returned session as mutation/save `DB`, and preserve every I01-I09 and P01-P08/P01.01 action/frame/diagnostic/protection contract, including zero-map automatic `firstBounds@0x2c/4:const RectBounds *` at I05.08 and the exact I05.13 `arg_0@0xc/1:char` intermediate, `arg_0 -> deleteFlags` name-only mapping, and occupied-stack `unsigned __int8` type correction. After the sole exact TX save and post-save package/observation, retire `DB`, close and file-flush saved TX, pass its one `SAVED_TX_CLOSE` volume-cache receipt, and only then finalize `TX_SAVED_*`; independently reopen saved `TX` read-only as distinct `DB_VERIFY`, complete its packages/entity proof/receipt, bind fresh closed `PROMOTION_EXPECTED_CANON`, then retire `DB_VERIFY` and every CANON/TX-capable route. For each `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE`, prove exact resolved `START_CANON_PARENT`/parent identity/same volume; require normalized/resolved/case-fold path and volume-plus-file ID distinct from `CANON`, `BACKUP`, `TX`, every session path, and every prior candidate; reject reparse point, symlink, junction, mount, ADS, hardlink, alias, and collision; and use one collision-failing create-new/no-overwrite operation yielding one regular one-link new identity. A collision stops untouched with no cleanup or reuse. Copy/write only from the authorized verified source, complete candidate file `Flush(true)`, close every handle, and pass the exact `PROMOTION_CANDIDATE_CREATE` or `RESTORE_CANDIDATE_CREATE` volume-cache receipt before independent normalized identity/stat/SHA256 reopen, source-equality proof, closure, and exclusive probe. Bind one spent same-directory atomic replacement attempt to the fresh expected-CANON receipt; prohibit copy/delete fallback, another source, cross-directory/volume movement, drift, cleanup-and-retry, candidate recreation, or a second attempt. After consumption, close every handle and pass exact `PROMOTION_COMMIT` or `RESTORE_COMMIT` volume-cache receipt before independently reopening/hashing/closing `CANON`, proving sealed `BACKUP` and source unchanged, and scanning the exact parent by path/file identity for candidate/temp/alias/residue/second artifacts. A permitted pre-attempt candidate removal must use recorded identity, its one named cleanup volume-cache receipt, zero-residue scan, and permanent stop without retry. Promotion is saved-TX-only after all DB/DB_VERIFY and CANON/TX-capable routes retire; only complete commit closure permits new read-only `DB_FINAL`, whose retirement is followed by `FINAL_CANON_CLOSE` and final receipts. Any committed-promotion or final-close failure opens new read-only `DB_ROLLBACK` on failed/ambiguous `CANON` for its own baseline/packages/complete I01-I09/P01-P08/P01.01 classification and closed receipts, then retires every canonical route. No failed-canonical diagnostic copy is created; DB_ROLLBACK plus failed-CANON/BACKUP/TX receipts are the complete failure evidence. Restoration is freshly reverified sealed-BACKUP-only, uses one distinct `RESTORE_CANDIDATE` against `RESTORE_EXPECTED_CANON`, and only complete commit closure permits new read-only `DB_RESTORE`, followed after retirement by `RESTORED_CANON_CLOSE` and final original-state receipts. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are pairwise non-reusable and bind only their stated paths/authority. Any unavailable, unsupported, access-denied, ambiguous, timed-out, failed, security-policy-blocked, missing, multiple, or mismatched volume-cache result hard-stops at its exact phase; file flush, hash, probe, prior receipt, alternate utility, custom harness, or directory-handle retry is not success. | High | `idb_open`, not attestation or a preferred name, returns each authoritative session binding. Runtime proof supplies public tool surface, role-specific loaded implementation, process/route/path identity, and freshness; separate closed filesystem receipts alone supply physical identities. Microsoft documents `Write-VolumeCache` as writing the exact volume's file-system cache to disk and forcibly flushing its write cache, so a stage-specific exact-volume success receipt supplies the required indirect containing-directory durability evidence after file flush and all-handle closure. Pure rename/readback and failed I05.02/I05.05/I05.06/I05.08/I05.13 evidence preserve six exact row mappings, 31 generic plus one already-named automatic adoption (32 automatic/adopted total), one exact I05.13 occupied-stack type edit, two protected named no-op rows, and exact primitive/const/tag displays. Fresh startup receipts prevent stale-hash authority. Exact-parent/same-volume, anti-alias/link/reparse, create-new collision, one-link identity, file flush, exact-volume barrier, all-handle closure, independent hash/exclusive probe, one-attempt atomic replacement, source preservation, and no-residue gates prevent an alternate candidate or fallback path. `DB_ROLLBACK` proves the failed canonical directly; sealed `BACKUP` is the sole restore source; `DB_RESTORE` independently proves original state after replacement. Any startup, package, role, action, frame, primitive, candidate, replacement, receipt, classification, restore, or persistence mismatch follows the exact pre-open stop, unsaved discard, no-promotion, no-restore, no-retry, or named post-promotion hard-stop branch. | Supervisor Gate 2B | incorporate | proposed |
