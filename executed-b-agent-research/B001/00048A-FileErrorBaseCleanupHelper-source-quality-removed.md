<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B001\00048A-FileErrorBaseCleanupHelper-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed operational material for 00048A-FileErrorBaseCleanupHelper-source-quality.md

Source: [00048A-FileErrorBaseCleanupHelper-source-quality.md](./00048A-FileErrorBaseCleanupHelper-source-quality.md)

This companion is non-authoritative archival evidence. It must never be executed, adapted, or treated as a current request, procedure, mutation authority, or lifecycle instruction.

## Removed operational snapshot from 7276AA04D45D9D1D2F6E3B8A2A76B9C33199AC0A8CF567F1E384DB4F11790295

> Source: [00048A-FileErrorBaseCleanupHelper-source-quality.md](./00048A-FileErrorBaseCleanupHelper-source-quality.md)
> NON-AUTHORITATIVE ARCHIVE. Retained only for evidentiary no-loss review.
> NEVER EXECUTE, ADAPT, OR TREAT THE CONTENT BELOW AS A CURRENT REQUEST, PROCEDURE, MUTATION AUTHORITY, OR LIFECYCLE INSTRUCTION.

`````text
## IDA Rename / Type / Comment Recommendations

The following is an inert structured supervisor handoff. B001 did not mutate or save the IDB.

Gate 1 repair history: the earlier compact handoff grouped neighbors and compressed comment-channel state. The A001-A009 table supersedes that schema while preserving every accepted technical conclusion and providing one literal row per entity. All named sessions and IDA reads are dated evidence only. A001-A007 prestates came from the dated target pass; A008-A009 were reread through ephemeral session `supervisor-b005-uid0002fn-persist-verify-20260810`; and A007's later dated proposal came from ephemeral session `b002-uid000312-frame-readback-20260811`. Whenever the supervisor applies A001, it must bind the exact canonical IDB to the live MCP runtime, reproduce every A001-A009 prestate, and stop on any mismatch. A001's literal expected prestate is name `sub_4A6470`; declaration `void __thiscall(_DWORD *this)`; address-regular, address-repeatable, function-regular, and function-repeatable comments each `absent`; bounds `[0x004a6470,0x004a647b)`; bytes `c7 01 44 93 61 00 e9 15 e6 04 00`; instructions `mov dword ptr [ecx], offset ??_7Error@@6B@` then `jmp LObject_destructor`; zero explicit arguments, zero locals, zero saved-register rows, and the sole physical-frame row `__return_address` at offset `0x0`, size `0x4`, type `_UNKNOWN *`. A007 remains protected as `sub_4A6400`, `void __thiscall(_DWORD *this)`, exact bytes/bounds, four absent comment channels, and its return-address-only frame. UID00048A authorizes no A007 mutation.

| Action ID | Address/range | Entity kind | Current name | Current type/declaration | Current regular comment | Current repeatable comment | Current function regular comment | Current function repeatable comment | Proposed name | Proposed type/declaration | Proposed regular comment | Proposed repeatable comment | Proposed function regular comment | Proposed function repeatable comment | Evidence / safety constraints | Expected readback | Classification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A001 | `0x004a6470` | function | `sub_4A6470` | `void __thiscall(_DWORD *this)` | absent | absent | absent | absent | `FileErrorConstructionErrorCleanupThunk` | `void __thiscall FileErrorConstructionErrorCleanupThunk(Error *this)` | remain absent | remain absent | `Compiler-generated cleanup thunk adjacent to FileError construction; restores Error::vftable and tail-transfers to LObject::~LObject. No standalone source function; covered by Error destructor/unwind source.` | remain absent | Apply only if exact 11 bytes/bounds, old name/declaration, all four blank comment channels, exact frame manifest below, existing 4-byte `Error` UDT, vtable immediate, and tail target still match. Use only the literal operations below; do not create a FileError UDT or use a broad multi-channel comment endpoint. | Name/declaration and function-regular comment match exactly; entry regular/repeatable and function-repeatable comments remain absent; only implicit-ECX receiver type changes; complete physical frame and bytes/bounds remain unchanged. | supervisor apply |
| A002 | `0x004a6476-0x004a647b` | terminal code item inside A001 | absent | absent | absent | absent | not applicable: not a function entry | not applicable: not a function entry | remain absent | remain absent | remain absent | remain absent | not applicable | not applicable | Preserve item head `0x004a6476`, end `0x004a647b`; do not create a second function, label, type, or comment at the tail. | Name/type/regular/repeatable comments remain absent; disassembly remains `jmp LObject_destructor`; item bounds remain exact. | protected no change |
| A003 | `0x00619344-0x00619348` | four-byte Error vtable data item | `??_7Error@@6B@` | absent | absent | absent | not applicable: data item | not applicable: data item | remain `??_7Error@@6B@` | remain absent | remain absent | remain absent | not applicable | not applicable | Correct decorated compiler symbol already gives exact identity; do not overwrite shared vtable evidence or data bounds. | Name remains `??_7Error@@6B@`; type/regular/repeatable comments remain absent; item remains four-byte data. | protected no change |
| A004 | `0x004f4a90-0x004f4a97` | inherited destructor function | `LObject_destructor` | `void __thiscall(LObject *this)` | absent | absent | `LObject::~LObject ordinary destructor body. The sole vptr store is compiler lowering; human source body is empty.` | absent | remain `LObject_destructor` | remain `void __thiscall(LObject *this)` | remain absent | remain absent | remain `LObject::~LObject ordinary destructor body. The sole vptr store is compiler lowering; human source body is empty.` | remain absent | Shared 214-caller dependency lies outside target; the dated source-quality name/type/comment are the expected protected prestate and must be reproduced by fresh bounded readback whenever A001 is applied. | All four name/type/comment states and seven-byte bounds remain exactly the reproduced prestate. | protected no change |
| A005 | `0x004a646f-0x004a6470` | one-byte predecessor padding data item | absent | absent | absent | absent | not applicable: data item | not applicable: data item | remain absent | remain absent | remain absent | remain absent | not applicable | not applicable | Byte is exactly `cc`; preserve data/alignment classification and do not append to constructor or target. | Name/type/regular/repeatable comments remain absent; one-byte data item and `cc` byte remain. | protected no change |
| A006 | `0x004a647b-0x004a6480` | five-byte successor padding data item | absent | absent | absent | absent | not applicable: data item | not applicable: data item | remain absent | remain absent | remain absent | remain absent | not applicable | not applicable | Bytes are exactly `cc cc cc cc cc`; preserve data/alignment classification; exclusive end has no xrefs. | Name/type/regular/repeatable comments remain absent; five-byte data item and exact bytes remain. | protected no change |
| A007 | `0x004a6400-0x004a640b` | sibling cleanup function | `sub_4A6400` | `void __thiscall(_DWORD *this)` | absent | absent | absent | absent | remain `sub_4A6400` | remain `void __thiscall(_DWORD *this)` | remain absent | remain absent | remain absent | remain absent | Same semantic pattern but outside the target. UID00048A neither caused nor authorizes sibling mutation: protect the dated 2026-08-11 proposed `_DWORD *this` declaration, exact bytes `c7 01 44 93 61 00 e9 85 e6 04 00`, and sole physical-frame row `__return_address` at `0x0`, size `0x4`, type `_UNKNOWN *`; require fresh bounded readback under the Section 21 contract and a separately accepted UID00013Y contract for any future A007 change. The prior `LObject *this` display is superseded historical evidence only. | Name remains `sub_4A6400`; declaration remains `void __thiscall(_DWORD *this)`; all four comment channels remain absent; exact bytes and `[0x004a6400,0x004a640b)` bounds remain unchanged; physical frame remains exactly the sole `__return_address` row at `0x0`, size `0x4`, type `_UNKNOWN *`. | protected no change |
| A008 | `0x004a6430-0x004a646f` | FileError constructor function | `sub_4A6430` | `wchar_t *__thiscall(wchar_t *this, char ArgList)` | absent | absent | absent | absent | remain `sub_4A6430` | remain `wchar_t *__thiscall(wchar_t *this, char ArgList)` | remain absent | remain absent | remain absent | remain absent | Source-authored neighbor has independent UID0002HL contract and known type/name work; UID00048A must not preempt it. Preserve exact function size `0x3f`; the listed values are dated expected prestate requiring fresh confirmation. | Name/declaration, all four comment channels, and `[0x004a6430,0x004a646f)` bounds remain exactly the freshly confirmed prestate. | protected no change |
| A009 | `0x004a6480-0x004a6499` | FileError message helper function | `sub_4A6480` | `errno_t __thiscall(const wchar_t *this, wchar_t *Destination, rsize_t SizeInWords)` | absent | absent | absent | absent | remain `sub_4A6480` | remain `errno_t __thiscall(const wchar_t *this, wchar_t *Destination, rsize_t SizeInWords)` | remain absent | remain absent | remain absent | remain absent | Source-authored neighbor has independent UID000140 contract and known type/name work; UID00048A must not preempt it. Preserve exact function size `0x19`; the listed values are dated expected prestate requiring fresh confirmation. | Name/declaration, all four comment channels, and `[0x004a6480,0x004a6499)` bounds remain exactly the freshly confirmed prestate. | protected no change |

### C024 Current Supervisor Runtime / Persistence Contract

This is a structured non-executable handoff. B001 did not and must not start/stop MCP, mutate or save IDA, create a backup, restore a database, or run report lifecycle commands. The supervisor must satisfy every phase below whenever applying C024; the row remains `proposed` until independent supervisor application and readback.

| Phase | Required supervisor evidence/action | Required readback or stop condition |
| --- | --- | --- |
| Runtime discovery | Run fresh `server_health` and `idb_list`; identify the exact canonical IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. If the supervisor opens it, `idb_open` must use the literal path and `run_auto_analysis:false`; the exact returned database/session identifier becomes the sole authority for every later request and the sole save. | Stop for a missing listener, ambiguous active canonical sessions, canonical-path mismatch, failed bounded calls, or unexpected auto-analysis. Never substitute a requested/preferred or dated session identifier for the returned identifier. |
| Public runtime attestation | Use only public read-only `runtime_attestation` with the literal current request shape `runtime_attestation {expected_database:"<EXACT_RETURNED_CURRENT_DATABASE_SESSION_ID>",expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"}`. The expected database must be the exact fresh `idb_list`/`idb_open` return, and the expected path must be the normalized canonical input identity. | Require top-level `schema_version:1`, `ok:true`, no errors, a fresh listener nonce/generation, listener PID owning `127.0.0.1:13337`, and exactly one matching active worker route whose database and canonical input path match the request. The internal worker RPC `runtime/attest` is forbidden. |
| Loaded-module binding | Verify that both listener and worker manifests identify the actually loaded `ida_pro_mcp` and `zeromcp` modules. | Require exact `file`, `spec_origin`, and `resolved_path` equality plus source-root containment, size, timestamps, and SHA256. A command line, venv path, separately launched interpreter, or on-disk source tree is not loaded-runtime proof. |
| Durable pre-mutation backup | Before the first mutation, create a new, non-overwriting backup at `tools/leaser/Agents/ida-backups/NexusTK.exe.uid00048A-prechange-<UTC>-<CANONICAL_SHA256_PREFIX>.i64`; fail rather than reuse a path. Record canonical and backup absolute paths, SHA256, byte counts, timestamps, create-new/no-overwrite result, and successful read-only reopen proof. | Require distinct canonical/backup paths, equal pre-mutation SHA256 and bytes, and successful read-only reopen. No mutation without this verified collision-safe backup; never overwrite the sole verified backup. |
| Exact prestate | Re-read A001 name, declaration, all four comment channels, bytes, bounds, instructions, xrefs, receiver, and complete frame. Re-read every protected A002-A009 entity. | Stop on any difference from the literal A001-A009 proposed prestates. Do not broaden, reinterpret, or partially apply the recommendation. |
| Rename | Use the exact returned database identifier. Perform the A001 function rename dry-run and apply with `pure:true`, `allow_overwrite:false`, and `stop_on_error:true`. | Read back only `FileErrorConstructionErrorCleanupThunk`; require no collision, directory mutation, boundary change, or protected-entity change. |
| Function type | Apply only `void __thiscall FileErrorConstructionErrorCleanupThunk(Error *this)`. | Return/convention remain unchanged; implicit ECX receiver changes only from `_DWORD *` to `Error *`; zero explicit arguments, locals, and saved-register rows remain zero; `__return_address` remains offset `0x0`, size `0x4`, type `_UNKNOWN *`. |
| Function comment | Apply only the exact A001 function-regular comment. | Function-regular comment matches exactly; address regular/repeatable and function-repeatable channels remain absent. |
| Immediate mutation readback | After each rename, type, and comment operation, reread A001's name, declaration, four comment channels, bytes, bounds, instructions, xrefs, receiver, and complete frame before proceeding. Re-read A002-A009 after each relevant mutation boundary. | Stop without save on any unpermitted A001 delta or any protected A002-A009 delta. Every protected name/type/comment/frame/bytes/bounds/xref state remains exact. |
| Pre-save runtime binding | Repeat public `runtime_attestation` immediately before save against the exact returned session/path. | Stop if runtime generation, PID ownership, active route, session/path, or loaded-module identity changed unexpectedly. |
| Sole save | Only after every immediate A001 and protected A002-A009 readback passes, perform exactly one supervisor-owned `idb_save` against the exact returned database/session identifier. | Record the save result and canonical disk SHA256, bytes, and timestamps. Any pre-save drift means stop with no save. This report authorizes no second save. |
| Fresh persisted-state verification | Close or isolate the mutating route, open the canonical IDB through a fresh verifier route with `run_auto_analysis:false`, bind the newly returned session through public `runtime_attestation`, and reread A001-A009 from disk. | Require exact A001 poststate, exact protected A002-A009 state, exact target bytes/bounds/instructions/xrefs, unchanged non-target relationships, and canonical disk identity matching the sole-save receipt. The mutating session's in-memory readback alone is insufficient. |
| Failure and guarded rollback | On any pre-save mismatch, stop without saving and preserve the verified backup. On any post-save/persistence mismatch, close the worker under supervisor control, prove no active canonical worker remains, quarantine the changed canonical file identity, and restore only from the verified backup through the guarded rollback procedure. | Require PID/process-identity absence with PID-reuse checks, no `is_active:true` canonical session, exact closed-disk identity, successful exclusive-open proof, restored SHA/bytes equal to the recorded pre-mutation canonical identity, a fresh `run_auto_analysis:false` open and public attestation, and exact restored A001-A009 prestate. Never overwrite or discard the sole verified backup. An inactive stale registry row alone is non-authoritative and is not failure. |
| Actor boundary | B001 supplies evidence and this handoff only. | Only the supervisor mutates, saves, restores, records Gate 2B completion, or performs report lifecycle work. |

### A001 Literal MCP Operation Schema

After the runtime, backup, and prestate phases pass, substitute only the exact database/session identifier returned by the current runtime open/discovery. These are literal one-operation MCP request shapes, not an executable report package:

- Rename dry-run: `rename {database:"<EXACT_RETURNED_CURRENT_DATABASE_SESSION_ID>",batch:{func:{addr:"0x004a6470",name:"FileErrorConstructionErrorCleanupThunk"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true}}`.
- Rename apply, only after the dry-run reports the intended single function rename without error: `rename {database:"<EXACT_RETURNED_CURRENT_DATABASE_SESSION_ID>",batch:{func:{addr:"0x004a6470",name:"FileErrorConstructionErrorCleanupThunk"},stop_on_error:true,dry_run:false,allow_overwrite:false,pure:true}}`.
- Function type apply, only after rename readback: `set_type {database:"<EXACT_RETURNED_CURRENT_DATABASE_SESSION_ID>",edits:{addr:"0x004a6470",kind:"function",signature:"void __thiscall FileErrorConstructionErrorCleanupThunk(Error *this)"}}`.
- Function-regular comment apply, only after type/frame readback: `set_function_comments {database:"<EXACT_RETURNED_CURRENT_DATABASE_SESSION_ID>",items:{addr:"0x004a6470",comment:"Compiler-generated cleanup thunk adjacent to FileError construction; restores Error::vftable and tail-transfers to LObject::~LObject. No standalone source function; covered by Error destructor/unwind source."}}`.
- Forbidden comment substitutions: do not call an address-regular, address-repeatable, function-repeatable, or broad multi-channel comment endpoint for A001. Entry address regular, entry address repeatable, and function repeatable must remain absent; only function regular changes from absent to the exact text above.

### A001 Complete Stack-Frame And Receiver Contract

The dated 2026-08-11 `stack_frame`, item, disassembly, and comment reads define the proposed receiver retype. Any supervisor application must reproduce this complete frame through fresh runtime-bound readback before mutation. The only permitted post-`set_type` delta is:

| Protected element | Literal current prestate | Permitted poststate | Required readback |
| --- | --- | --- | --- |
| Function return and convention | return `void`; convention `__thiscall` | unchanged | exact `void __thiscall` declaration prefix |
| Implicit receiver | name `this`; implicit register `ECX`; type `_DWORD *` | name/location unchanged; type becomes `Error *` | `this` remains implicit `ECX`; only receiver type changes |
| Explicit stack arguments | zero rows | zero rows | no argument row created, deleted, renamed, resized, or retyped |
| Stack locals | zero rows | zero rows | no local row created, deleted, renamed, resized, or retyped |
| Saved-register rows | zero rows | zero rows | no saved-register row created, deleted, renamed, resized, or retyped |
| Return-address row | `__return_address`, offset `0x0`, size `0x4`, type `_UNKNOWN *` | unchanged | exact same name, offset, size, and type |

The manifest is complete: there are no additional physical frame rows. Any extra/missing row, any changed return-address property, any explicit argument creation, or any receiver name/location change is a hard mismatch; the supervisor must stop before save rather than broadening A001.

Negative constraints: do not rename A001 as a C++ destructor; do not create a new function at `0x004a6476`; do not include either padding span; do not rename the Error vtable; do not replace the shared LObject destructor comment; do not create a `FileError` UDT solely to force the target type; do not save if any pre-state differs without returning the report for revalidation.

`````

## Additional removed operational lines: B001 residual orchestration cleanup

> NON-AUTHORITATIVE ARCHIVE. The exact lines below were removed from the source and must never be executed.

`````text
| C024 | 00048A | The complete Section 21 supervisor handoff requires runtime binding, collision-safe durable backup, exact prestate, mutation, immediate readback, one save only after all readbacks, fresh persisted-state readback, and guarded rollback. It permits A001 at `0x004a6470` only through the exact returned database identifier: pure function-rename dry-run/apply with overwrite disabled and stop-on-error enabled, exact function `set_type` to `void __thiscall FileErrorConstructionErrorCleanupThunk(Error *this)`, and function-regular `set_function_comments` only; preserve all three non-function-regular comment channels and every physical-frame row except the permitted implicit-ECX receiver type change from `_DWORD *` to `Error *`. | strong | A001 dated proposed prestate, invariant runtime/safety binding, and expected readback contract | IDA A001 / `0x004a6470` | incorporate | proposed |
- The A001 supervisor handoff permits application only when canonical runtime binding, collision-safe durable backup, literal prestate, and protected A002-A009 entities satisfy Section 21. It permits one save only after complete immediate readback and requires fresh persistence verification plus guarded rollback on post-save drift.
- No remaining research question blocks score, owner/emitter, source placement, range, formal C++/H, or the structured IDA handoff. A001 evidence analysis is complete. Section 21 defines the invariant public-runtime binding, complete prestate reproduction, durable backup, exact mutation, protected readback, sole-save, fresh persistence, and guarded rollback contract; using that contract is supervisor execution, not deferred schema/frame research.
- IDA application authority: C024-C025 and C035-C041 are governed by fresh `server_health`/`idb_list`/public `runtime_attestation`, loaded-module binding, collision-safe durable backup, exact A001-A009 prestate, immediate mutation/protected readbacks, one save only after all readbacks, fresh persisted-state verification, and guarded rollback under Section 21. B001 neither performs nor claims that work.
| [ ] | C024 | 00048A | The complete Section 21 supervisor handoff requires runtime binding, collision-safe durable backup, exact prestate, mutation, immediate readback, one save only after all readbacks, fresh persisted-state readback, and guarded rollback. It permits A001 at `0x004a6470` only through the exact returned database identifier: pure function-rename dry-run/apply with overwrite disabled and stop-on-error enabled, exact function `set_type` to `void __thiscall FileErrorConstructionErrorCleanupThunk(Error *this)`, and function-regular `set_function_comments` only; preserve all three non-function-regular comment channels and every physical-frame row except the permitted implicit-ECX receiver type change from `_DWORD *` to `Error *`. | strong | A001 dated proposed prestate, invariant runtime/safety binding, and expected readback contract | IDA A001 / `0x004a6470` | incorporate | proposed |
`````

