** TARGET-REPORT-UID:0002R2 **
** AUTHOR-AGENT-ID:B001 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **

# 0002R2 LivingObjectPane Render Frame Methods Report

## Assignment And Mode

- Assignment id: `B001-implementation-rework-0002R2-living-object-pane-render-frame-methods-20260626`
- Mode: implementation rework after supervisor verification rejection; the existing report is updated in place with the corrected implementation proof.
- Target: [UID:0002R2] `by-memory/0x0053bb20-0x0053beb6.LivingObjectPaneRenderFrameMethods.md`
- Edit guard: target/support by-* docs were edited only through scoped leases and validators. Generated files, project-level generated files, validator state/cache, IDA DB, and coverage reports were not manually edited.
- IDA MCP use: mandatory evidence was collected from live session `80de0a67`.

## Current Target State

The source target page is already repaired beyond the stale generated tracker row:

| Source | Observed state |
| --- | --- |
| Target by-memory page | `COMPLETION:88`, `CONFIDENCE:90`, `CANONICAL_OWNER:00007B`, `RECONSTRUCTABLE:FALSE`, blank emitters and blank C++ |
| Target role | Non-emitting render-method split/index for three exact vtable methods plus `0xcc` padding |
| Exact children | [UID:0003NU] `0x0053bb20-0x0053bd1c`, [UID:0003NV] `0x0053bd20-0x0053bdb1`, [UID:0003NW] `0x0053bdc0-0x0053beb6` |
| Generated tracker row | stale at original intake: `auto-generated/-ag-research-tracker.md` listed [UID:0002R2] as `76/84`, reconstructable `true`, not covered; refresh is validator/generator-owned and was not manually edited |
| Generated by-memory row | stale at original intake: `auto-generated/-ag-coverage-report-by-memory.md` listed [UID:0002R2] as `76%`, reconstructable; refresh is validator/generator-owned and was not manually edited |

Interpretation: the assigned tracker row was stale relative to current source docs. The implementation callback preserves the source by-memory split state, applies targeted child/support improvements, and records scoped validator refresh. It does not manually edit generated reports.

## Evidence Checked

Documentation checked:

- Target [UID:0002R2] `by-memory/0x0053bb20-0x0053beb6.LivingObjectPaneRenderFrameMethods.md`.
- Exact children [UID:0003NU] `by-memory/0x0053bb20-0x0053bd1c.LivingObjectPaneRenderFrame.md`, [UID:0003NV] `by-memory/0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md`, and [UID:0003NW] `by-memory/0x0053bdc0-0x0053beb6.LivingObjectPaneComputeRenderBounds.md`.
- Owner/source roots [UID:00007B] `by-class/LivingObjectPane.md` and [UID:0000KU] `by-file/LivingObjectPane.md`.
- Vtable data [UID:000358] `by-memory/0x00620978-0x00620a38.LivingObjectPaneVtableData.md`.
- Sprite/config support [UID:0003Z3] `by-memory/0x0053a6b0-0x0053aae9.LivingObjectPaneSpriteConfigHelpers.md`.
- Overlay/string support [UID:0002SO] `by-memory/0x00620b90-0x00620c70.ObjectPaneResourceStringsAndOverlayConstants.md` and [UID:0003CC] `by-memory/0x00620c10-0x00620c64.ObjectOverlayFrameIndexTables.md`.
- Helper/source support docs for `NewHumanImageLib`, `MonsterImageLib`, `GrafPort`, `RectBounds`, and `MapPane`, including [UID:00015R] `RectBounds::Inset`.
- Prior executed reports `0001DE-LivingObjectPaneCore-source-quality.md`, `00007B-LivingObjectPane-class-source-quality.md`, B008 `00015R-RectBoundsInset-source-quality.md`, and B008 `0003CP-ObjectImageControlPaneRenderObjectPreview-source-quality.md` for image-helper naming comparison.
- Generated tracker and generated by-memory coverage rows for current freshness comparison only. No generated file was edited.

Live IDA MCP status:

- `idb_list`: one active session, session id `80de0a67`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, `is_analyzing:false`, `is_active:true`, worker PID `26892`.
- `server_health`: status `ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, strings cache size `2067`.
- MCP schema correction checked in this rework: `decompile` and `disasm` require singular `addr`; `xrefs_to` uses `addrs`; `get_bytes` uses `regions`; `server_health` requires `database`.

Live IDA MCP target evidence:

| Tool/evidence | Result |
| --- | --- |
| `lookup_funcs` | `0x0053bb20` is `sub_53BB20` size `0x1fc`; `0x0053bd20` is `sub_53BD20` size `0x91`; `0x0053bdc0` is `sub_53BDC0` size `0xf6`; boundary addresses `0x0053bd1c`, `0x0053bdb1`, and `0x0053beb6` are not functions; `0x0053bec0` starts successor `sub_53BEC0` size `0x78`. |
| `entity_query functions 0x0053bb00-0x0053bec0` | Finds exactly the three render children plus successor `0x0053bec0`; no hidden function starts inside the padding gaps. |
| `xrefs_to` child starts | `0x0053bb20` has data refs `0x006209d8` and `0x0062ef68`; `0x0053bdc0` has data refs `0x006209dc` and `0x0062ef6c`; `0x0053bd20` has data refs `0x006209e0` and `0x0062ef70`. No code callers were reported for the vtable methods. |
| `entity_query names 0x00620970-0x00620c80` | Names include `??_7BoxRotator@LivingObjectPane@@6B@`, `??_7LivingObjectPane@@6B@`, adjusted `??_7LivingObjectPane@@6B@_0` and `??_7LivingObjectPane@@6B@_1`, then resource-string names such as `aBgaugeEpf`. No source method names or overlay-table names are present for the child slots. |
| `get_bytes` code boundaries | `0x0053bd1c-0x0053bd20` is four `0xcc` bytes; `0x0053bdb1-0x0053bdc0` is fifteen `0xcc` bytes; `0x0053beb6-0x0053bec0` is ten `0xcc` bytes; `0x0053bec0` begins a normal function prologue. |
| `get_bytes` overlay/string area | `0x00620b90` decodes to UTF-16 `BGAUGE.EPF` and `BGAUGE.pal`, not a marker table. The overlay frame-index table bytes begin at `0x00620c10` and contain three 7-dword rows: `0xf1,0xf3,0xf5,0xf7,0xf1,0xf3,0xf5`; `0x2f,0x2e,0x2d,0x2c,0x2f,0x2e,0x2d`; `0xe0,0xe1,0xe2,0xe3,0xe0,0xe1,0xe2`. |
| `xrefs_to` overlay table | `0x00620c10`, `0x00620c2c`, and `0x00620c48` report no IDA data xrefs. LivingObjectPane reaches them through immediate base constants in decompiled code, so [UID:0003CC] remains owner and [UID:0003NU] is a consumer. |

Numeric conversions performed with `tools/int_convert.py`:

- Decimal `6425616` -> `0x620c10` (Verified with int_convert.py).
- Decimal `6425644` -> `0x620c2c` (Verified with int_convert.py).
- Decimal `6425672` -> `0x620c48` (Verified with int_convert.py).
- Decimal `2678366` -> `0x28de5e` (Verified with int_convert.py).
- Offsets `0x11c`, `0x178`, `0x1c0`, `0x1c4`, `0x1c5`, `0x1c6`, `0x1c8`, `0x1cc`, `0x1d4`, `0x1e8`, `0x1ec`, and `0x1f8` convert to decimal `284`, `376`, `448`, `452`, `453`, `454`, `456`, `460`, `468`, `488`, `492`, and `504` respectively (Verified with int_convert.py).

Per-child MCP behavior:

| Child | Live behavior evidence |
| --- | --- |
| [UID:0003NU] `0x0053bb20-0x0053bd1c` | `analyze_function` reports prototype `void __thiscall(int this, int, int *)`, no direct callers, vtable/data xrefs, callees `0x0050ab70`, `0x0050ab40`, `0x004e06a0`, `0x004db3b0`, `0x004b9660`, `0x004ba450`, `0x004b7910`, and stack-cookie support. Full decompile shows a `GrafPort`-style render port at argument 2 and `RectBounds`-style input rectangle at argument 3; standing/elevation handling at `this+0x11c`/decimal `284` (Verified with int_convert.py); sprite descriptor at `this+0x178`/decimal `376` (Verified with int_convert.py); human-vs-monster image draw split; optional overlay gate at `this+0x1e8`/decimal `488` (Verified with int_convert.py); marker index at `this+0x1ec`/decimal `492` (Verified with int_convert.py); overlay row bases `0x00620c10`, `0x00620c2c`, and `0x00620c48`; `GrafPort::SetDrawColor`; four `GrafPort::DrawRectFrame` passes; and three `RectBounds::Inset(1,1)` calls. |
| [UID:0003NV] `0x0053bd20-0x0053bdb1` | `analyze_function` reports prototype `void __thiscall(int this, int, _DWORD *)`, no direct callers, vtable/data xrefs, and callees `0x004db100` and `0x004e0900`. Decompile branches on descriptor dword `this+0x178`/decimal `376` (Verified with int_convert.py), uses the same movement/facing/frame fields seen in [UID:0003Z3], then dispatches through monster or human image-library draw helpers with a `GrafPort`-style port and `RectBounds`-style rectangle. |
| [UID:0003NW] `0x0053bdc0-0x0053beb6` | `analyze_function` reports prototype `_DWORD *__thiscall(int this, _DWORD *)`, no direct callers, vtable/data xrefs, and callees `0x0050b080`, `0x004e09e0`, `0x004db5c0`, `0x004b78f0`, and stack-cookie support. Decompile calls the MapPane coordinate helper, uses human or monster image bounds helpers, applies `RectBounds::Offset`, writes four dwords to the caller output rectangle, and returns that output pointer. |

Resolved helper names and broad-support prototype limits:

- `0x004e06a0` and `0x004e0900`: `NewHumanImageLib::Draw` / `RenderHuman` direction; MCP prototypes remain raw `_DWORD *`, `int *`, `__m128i *`, mixed char/int parameter lists. This keeps broad helper class C++ blank but does not block exact child first-draft calls.
- `0x004e09e0`: `NewHumanImageLib::CalculateBounds` / `ComputeOverlayBounds` direction; exact helper declaration remains broad-support work, while the [UID:0003NW] call roles are strong enough for first-draft child C++.
- `0x004db100`: `MonsterImageLib::RenderMonster` direction; caller-specific arguments are visible, but final source signature is not declared in support docs.
- `0x004db3b0`: `MonsterImageLib::RenderMonsterImage` direction; MCP prototype is still raw and shared by several callers.
- `0x004db5c0`: `MonsterImageLib::ComputeRenderBounds` direction; bounds behavior is understood enough for [UID:0003NW], while final render-helper declarations remain broad-support work.
- `0x004b9660`: `GrafPort::SetDrawColor` / active draw-color setter; decompile writes argument to `this+0x74`.
- `0x004ba450`: `GrafPort::DrawRectFrame(const RectBounds *)`; formal C++ already exists in support docs.
- `0x004b7910`: [UID:00015R] `RectBounds::Inset(int dx, int dy)`; formal C++ already exists in support docs.
- `0x004b78f0`: `RectBounds::Offset(int dx, int dy)`; behavior and class method are documented.
- `0x0050ab40` and `0x0050ab70`: MapPane shadow/day-night overlay predicates; they test the MapPane scalar at index `247` and config byte at offset `0x28de5e` (Verified with int_convert.py).
- `0x0050b080`: MapPane coordinate/object placement helper; source-facing role is object-to-screen coordinate conversion, but exact output-point field names remain tied to broader MapPane coordinate conventions.

## Heuristic And Inference Reanalysis

### Aggregate Disposition

Best recommendation: keep [UID:0002R2] as a reviewed non-emitting split/index, not as source-bearing code.

Evidence:

- The range is not one function. MCP identifies three separate function starts and three padding gaps.
- Each real method already has an exact child page with owner/emitter metadata.
- Vtable/data references route the starts as method slots; no ordinary code callers were reported.
- The source-bearing work belongs to the exact children, while this parent preserves coverage, padding, and grouping evidence.

Rejected alternatives:

- `RECONSTRUCTABLE:TRUE` on the aggregate: rejected because it would make the convenience range look like a source-authored method or data object.
- Parent `EMITTER_UIDS:00007B`: rejected because it would duplicate child method output and violate the by-memory rule that aggregate pages must not emit child-range code.
- Parent formal C++ marker or `[[CHILDREN]]`: rejected here because the target is `RECONSTRUCTABLE:FALSE` and does not represent a source declaration that needs an insertion point.

### Ownership And Source Placement

Ranked ownership:

1. [UID:00007B] `LivingObjectPane` class: confirmed direct semantic owner for the three vtable methods. Constructor/vtable docs, class/file support pages, and live vtable refs support this.
2. [UID:0000KU] `LivingObjectPane.cpp`: correct file root reached through the class emitter chain.
3. [UID:0000L3] `MapPane`, [UID:0000LR] `NewHumanImageLib`, `MonsterImageLib`, `GrafPort`, and `RectBounds`: dependencies only, not owners.

Rejected owners:

- `MapPane`: it supplies map state and coordinate helpers, but the receiver and vtable slots are LivingObjectPane.
- `NewHumanImageLib` or `MonsterImageLib`: they own rendering library helpers, but the dispatching vtable methods read LivingObjectPane state and choose the backend.
- `GrafPort` or `RectBounds`: they own draw-state and rectangle helpers, not the actor render method.

### Field And Type Readiness

The current best source-facing field direction is strong enough for first-draft child formal C++ when kept on the exact child pages. Broader class/file declarations and complete image-helper prototypes remain support-doc work, so those aggregate formal C++ blocks stay blank.

| Offset/data | Best current role | Readiness |
| --- | --- | --- |
| `this+0x11c` / decimal `284` (Verified with int_convert.py) | `m_elevationOffset` standing/elevation offset that contributes to render elevation and bounds | inferred source-facing spelling accepted for child code |
| `this+0x178..` / decimal `376` (Verified with int_convert.py) | `m_spriteConfig` / sprite descriptor block; zero selects human path, nonzero selects monster/object path | role strong enough for child code; full `LivingSpriteConfig` declaration remains support-doc work |
| `this+0x1c0` / decimal `448` (Verified with int_convert.py) | `m_spriteConfig.bodyFrame` human body/frame/style argument in human render helpers | role strong enough for child code; broad descriptor declaration still provisional |
| `this+0x1c4` / decimal `452` (Verified with int_convert.py) | `m_isMoving` movement/animation-active byte, matching [UID:0003Z3] | source-facing spelling accepted for child code |
| `this+0x1c5` / decimal `453` (Verified with int_convert.py) | `m_facing` facing/direction byte, matching [UID:0003Z3] | source-facing spelling accepted for child code |
| `this+0x1c6` / decimal `454` (Verified with int_convert.py) | `m_moveFrame` movement frame/subframe signed byte, matching [UID:0003Z3] | source-facing spelling accepted for child code |
| `this+0x1c8` / decimal `456` (Verified with int_convert.py) | `m_animationFrame` monster/object animation frame argument | inferred source-facing spelling accepted for child code |
| `this+0x1cc` / decimal `460` (Verified with int_convert.py) | `m_spriteConfig.frameGroup` monster/object frame group argument | inferred source-facing spelling accepted for child code |
| `this+0x1d4` / decimal `468` (Verified with int_convert.py) | `m_spriteConfig.palette` human palette/effect/style argument | inferred source-facing spelling accepted for child code |
| `this+0x1e8` / decimal `488` (Verified with int_convert.py) | `m_markerOverlayEnabled` marker/selection overlay enable byte | inferred source-facing spelling accepted for [UID:0003NU] |
| `this+0x1ec` / decimal `492` (Verified with int_convert.py) | `m_markerOverlayIndex` marker-row index for overlay table in [UID:0003NU] | inferred source-facing spelling accepted for [UID:0003NU] |
| `this+0x1f8` / decimal `504` (Verified with int_convert.py) | `m_renderEffectState` payload forwarded to main image helpers | inferred source-facing spelling accepted for child code; replaces stale `m_cachedRenderRect` wording in render context |
| `0x00620c10`, `0x00620c2c`, `0x00620c48` | `kLivingObjectMarkerFrameColors`, `kLivingObjectMarkerColorSet0`, and `kLivingObjectMarkerColorSet1` rows selected by [UID:0003NU] immediate constants and MapPane overlay mode | table bytes and consumer behavior proven; table owner remains [UID:0003CC], no direct IDA xrefs |
| `0x00620b90` | UTF-16 `BGAUGE.EPF` string, not the overlay marker table | stale prior table wording rejected |
| render argument `a2` in [UID:0003NU]/[UID:0003NV] | `GrafPort *target`; `+0x70` draw/blit flag and `+0x74` active draw color are touched by helpers | accepted source-facing child parameter |
| render argument `a3` in [UID:0003NU]/[UID:0003NV] | `const RectBounds *bounds` rectangle pointer | accepted source-facing child parameter |
| output argument `a2` in [UID:0003NW] | caller-provided `RectBounds *outBounds` output pointer returned by the method | accepted source-facing child parameter and return type |

### Named Blocker Audit

The rejected report named five blockers. This rework investigated each one against live MCP and support docs:

| Blocker | Current-pass result |
| --- | --- |
| Render context/prototype | Resolved for child first-draft formal C++: [UID:0003NU] and [UID:0003NV] use `GrafPort *target` plus `const RectBounds *bounds`; [UID:0003NW] uses and returns `RectBounds *outBounds`. Broad vtable declaration audit remains outside this child-page implementation. |
| Sprite descriptor layout | Resolved to the level required by the exact child bodies: `m_spriteConfig`, `m_isMoving`, `m_facing`, `m_moveFrame`, `m_spriteConfig.bodyFrame`, `m_animationFrame`, `m_spriteConfig.frameGroup`, `m_spriteConfig.palette`, and `m_renderEffectState` are accepted source-facing names in the child formal code. The complete class-level descriptor declaration remains support work. |
| Marker table/index meaning | Resolved for [UID:0003NU]: `0x00620b90` is `BGAUGE.EPF`; actual rows are [UID:0003CC] at `0x00620c10`, `0x00620c2c`, and `0x00620c48` (Verified with int_convert.py). The child code uses `kLivingObjectMarkerFrameColors`, `kLivingObjectMarkerColorSet0`, and `kLivingObjectMarkerColorSet1` as source-facing consumer names while preserving [UID:0003CC] ownership. |
| Image-library helper prototypes | Resolved enough for child calls without raw IDA labels: [UID:0003NU] uses `NewHumanImageLib::Draw` and `MonsterImageLib::RenderMonsterImage`; [UID:0003NV] uses `NewHumanImageLib::RenderHuman` and `MonsterImageLib::RenderMonster`; [UID:0003NW] uses `NewHumanImageLib::CalculateBounds` and `MonsterImageLib::ComputeRenderBounds`. Broad helper class declarations remain blank/provisional. |
| Vtable slot/source-facing method names | Resolved as first-draft names in [UID:000358] and the children: `LivingObjectPane::RenderFrame`, `LivingObjectPane::DrawSpriteFrame`, and `LivingObjectPane::ComputeRenderBounds`. No decorated original names exist, but the inferred names are supported by behavior, vtable position, and child page names. |

### Formal C++ Readiness

No formal C++ should be inserted for [UID:0002R2]. Target-specific no-code proof:

- The page is intentionally `RECONSTRUCTABLE:FALSE`.
- The page contains child functions and padding, not one source object.
- Any aggregate code would duplicate [UID:0003NU], [UID:0003NV], and [UID:0003NW].
- The correct implementation path is exact child method pages plus class/file emitters, not parent aggregate code.

Formal C++ is now inserted for all three exact children. The child bodies are first-draft source-shaped reconstructions that use inferred source-facing names and avoid raw IDA labels. They remain scoped to the exact child `RECONSTRUCTION_CPP CODE` blocks, not the parent aggregate or broad class/file support pages.

[UID:0003NU] `LivingObjectPaneRenderFrame` formal code:

```cpp
void LivingObjectPane::RenderFrame(GrafPort *target, const RectBounds *bounds)
{
    FrameDrawRecord *translatedClipRecord = 0;
    float translatedClipOffset = 0.0f;
    const bool useMapLightOverlay = g_pActiveMapPane->ShouldUseLivingObjectLightOverlay();

    if (m_elevationOffset != 0.0f)
    {
        if (g_pActiveMapPane->ShouldOffsetElevatedLivingObjects())
        {
            translatedClipRecord = target->m_translatedClipRecord;
            translatedClipOffset = target->m_translatedClipOffsetY + m_elevationOffset;
        }
    }

    if (m_spriteConfig.kind != 0)
    {
        if (m_spriteConfig.frameGroup < 9 && m_isMoving)
        {
            g_pMonsterImageLib->RenderMonsterImage(
                target,
                bounds,
                &m_spriteConfig,
                m_facing + 5,
                m_renderEffectState,
                m_moveFrame,
                useMapLightOverlay,
                translatedClipRecord,
                translatedClipOffset);
        }
        else
        {
            g_pMonsterImageLib->RenderMonsterImage(
                target,
                bounds,
                &m_spriteConfig,
                m_spriteConfig.frameGroup,
                m_renderEffectState,
                m_animationFrame,
                useMapLightOverlay,
                translatedClipRecord,
                translatedClipOffset);
        }
    }
    else
    {
        g_pNewHumanImageLib->Draw(
            target,
            bounds,
            &m_spriteConfig,
            m_facing,
            m_spriteConfig.bodyFrame,
            m_renderEffectState,
            m_moveFrame,
            useMapLightOverlay,
            translatedClipRecord,
            translatedClipOffset,
            0,
            m_isMoving,
            m_spriteConfig.palette);
    }

    if (m_markerOverlayEnabled)
    {
        const unsigned int *colors = kLivingObjectMarkerFrameColors + m_markerOverlayIndex;

        if (g_pActiveMapPane->UsesAlternateLivingObjectMarkerColors())
        {
            const int colorSet = g_pActiveMapPane->GetLivingObjectMarkerColorSet();
            if (colorSet == 0)
                colors = kLivingObjectMarkerColorSet0 + m_markerOverlayIndex;
            else if (colorSet == 1)
                colors = kLivingObjectMarkerColorSet1 + m_markerOverlayIndex;
        }

        target->m_drawMode = 0;

        RectBounds markerBounds = *bounds;
        target->SetDrawColor(colors[0]);
        target->DrawRectFrame(&markerBounds);

        for (int i = 1; i < 4; ++i)
        {
            markerBounds.Inset(1, 1);
            target->SetDrawColor(colors[i]);
            target->DrawRectFrame(&markerBounds);
        }
    }
}
```

[UID:0003NV] `LivingObjectPaneDrawSpriteFrame` formal code:

```cpp
void LivingObjectPane::DrawSpriteFrame(GrafPort *target, const RectBounds *bounds)
{
    if (m_spriteConfig.kind != 0)
    {
        if (m_spriteConfig.frameGroup < 9 && m_isMoving)
        {
            g_pMonsterImageLib->RenderMonster(
                target,
                bounds,
                &m_spriteConfig,
                m_facing + 5,
                m_moveFrame);
        }
        else
        {
            g_pMonsterImageLib->RenderMonster(
                target,
                bounds,
                &m_spriteConfig,
                m_spriteConfig.frameGroup,
                m_animationFrame);
        }
        return;
    }

    g_pNewHumanImageLib->RenderHuman(
        target,
        bounds,
        &m_spriteConfig,
        m_facing,
        m_spriteConfig.bodyFrame,
        m_moveFrame,
        m_isMoving,
        m_spriteConfig.palette);
}
```

[UID:0003NW] `LivingObjectPaneComputeRenderBounds` formal code:

```cpp
RectBounds *LivingObjectPane::ComputeRenderBounds(RectBounds *outBounds)
{
    MapCoordinatePair screenPoint;
    RectBounds imageBounds;
    RectBounds renderBounds;

    g_pActiveMapPane->GetObjectScreenPoint(this, &screenPoint);

    if (m_spriteConfig.kind != 0)
    {
        if (m_spriteConfig.frameGroup < 9 && m_isMoving)
        {
            g_pMonsterImageLib->ComputeRenderBounds(
                &m_spriteConfig,
                m_facing + 5,
                m_moveFrame,
                &imageBounds,
                &renderBounds);
        }
        else
        {
            g_pMonsterImageLib->ComputeRenderBounds(
                &m_spriteConfig,
                m_spriteConfig.frameGroup,
                m_animationFrame,
                &imageBounds,
                &renderBounds);
        }
    }
    else
    {
        g_pNewHumanImageLib->CalculateBounds(
            &m_spriteConfig,
            m_facing,
            m_spriteConfig.bodyFrame,
            m_moveFrame,
            &imageBounds,
            &renderBounds,
            0,
            m_isMoving,
            m_spriteConfig.palette);
    }

    renderBounds.Offset(screenPoint.x, screenPoint.y);
    *outBounds = renderBounds;
    return outBounds;
}
```

Score outcome after rejection repair: parent [UID:0002R2] remains `COMPLETION:88`, `CONFIDENCE:90`, `RECONSTRUCTABLE:FALSE`, non-emitting, and blank formal C++. Children are now emitting first-draft C++ targets with [UID:0003NU] at `89/89`, [UID:0003NV] at `88/89`, and [UID:0003NW] at `88/89`.

## Recommended Implementation Changes If Accepted

### Target Page [UID:0002R2]

Keep header metadata unchanged:

- `COMPLETION:88`
- `CONFIDENCE:90`
- `CANONICAL_OWNER:00007B`
- `RECONSTRUCTABLE:FALSE`
- blank `EMITTER_UIDS`
- blank `RECONSTRUCTION_CPP CODE`

Add a `2026-06-26 B001 report-only MCP rework` change entry or equivalent prose preserving:

- Live MCP session `80de0a67` was responsive and IDB-backed.
- MCP schema correction: `decompile`/`disasm` use singular `addr`; `xrefs_to` uses `addrs`; `get_bytes` uses `regions`; `server_health` requires `database`.
- `lookup_funcs` confirms exact child starts `0x0053bb20`, `0x0053bd20`, and `0x0053bdc0`; boundary addresses are padding/not functions.
- `xrefs_to` confirms vtable/data refs at `0x006209d8`, `0x006209dc`, `0x006209e0`, `0x0062ef68`, `0x0062ef6c`, and `0x0062ef70`.
- `get_bytes` confirms the three `0xcc` padding spans.
- The child pages are the exact source-bearing ranges. The parent remains an index/split page, not an emitter.
- Generated tracker/coverage rows were stale at report time and must be refreshed by validator/autogen, not by manual generated-file edits.

No score/owner/emitter/C++ change is recommended for the target.

### Exact Child Support Pages

Apply targeted support edits to the three child pages:

1. [UID:0003NU] `by-memory/0x0053bb20-0x0053bd1c.LivingObjectPaneRenderFrame.md`
   - Update metadata to `COMPLETION:89`, `CONFIDENCE:89`; keep owner/emitter [UID:00007B] and `RECONSTRUCTABLE:TRUE`.
   - Set `Item Summary` to: `Main living-object render vtable method with map overlay predicates, standing/elevation adjustment, human/monster image draw split, optional marker overlay frames, and RectBounds/GrafPort helper evidence.`
   - Insert the formal `LivingObjectPane::RenderFrame(GrafPort *target, const RectBounds *bounds)` first-draft C++ from this report into the target `RECONSTRUCTION_CPP CODE` block.
   - Add exact MCP facts: prototype `void __thiscall(int this, int, int *)`, size `0x1fc`, no direct callers, data refs `0x006209d8` and `0x0062ef68`, callees `0x0050ab70`, `0x0050ab40`, `0x004e06a0`, `0x004db3b0`, `0x004b9660`, `0x004ba450`, `0x004b7910`, and `0x0053bd1c-0x0053bd20` four-byte `0xcc` padding.
   - Correct stale overlay wording: `0x00620b90` is `BGAUGE.EPF`; actual overlay rows are [UID:0003CC] at `0x00620c10`, `0x00620c2c`, and `0x00620c48` (Verified with int_convert.py), consumed by immediate base constants with no IDA data xrefs.
   - Mention resolved helper aliases: MapPane overlay predicates, `NewHumanImageLib::Draw`, `MonsterImageLib::RenderMonsterImage`, `GrafPort::SetDrawColor`, `GrafPort::DrawRectFrame`, and `RectBounds::Inset`.

2. [UID:0003NV] `by-memory/0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md`
   - Update metadata to `COMPLETION:88`, `CONFIDENCE:89`; keep owner/emitter [UID:00007B] and `RECONSTRUCTABLE:TRUE`.
   - Set `Item Summary` to: `Compact living-object sprite-frame draw vtable method with exact vtable refs and human/monster image-library dispatch over sprite descriptor and frame/effect fields.`
   - Insert the formal `LivingObjectPane::DrawSpriteFrame(GrafPort *target, const RectBounds *bounds)` first-draft C++ from this report into the target `RECONSTRUCTION_CPP CODE` block.
   - Add exact MCP facts: prototype `void __thiscall(int this, int, _DWORD *)`, size `0x91`, no direct callers, data refs `0x006209e0` and `0x0062ef70`, callees `0x004db100` and `0x004e0900`, and `0x0053bdb1-0x0053bdc0` fifteen-byte `0xcc` padding.
   - Mention resolved helper aliases: `MonsterImageLib::RenderMonster` and `NewHumanImageLib::RenderHuman` direction.

3. [UID:0003NW] `by-memory/0x0053bdc0-0x0053beb6.LivingObjectPaneComputeRenderBounds.md`
   - Update metadata to `COMPLETION:88`, `CONFIDENCE:89`; keep owner/emitter [UID:00007B] and `RECONSTRUCTABLE:TRUE`.
   - Set `Item Summary` to: `Living-object render-bounds vtable helper that combines MapPane coordinate conversion, human/monster image dimensions, standing/elevation state, and writes a caller rectangle output.`
   - Insert the formal `RectBounds *LivingObjectPane::ComputeRenderBounds(RectBounds *outBounds)` first-draft C++ from this report into the target `RECONSTRUCTION_CPP CODE` block.
   - Add exact MCP facts: prototype `_DWORD *__thiscall(int this, _DWORD *)`, size `0xf6`, no direct callers, data refs `0x006209dc` and `0x0062ef6c`, callees `0x0050b080`, `0x004e09e0`, `0x004db5c0`, `0x004b78f0`, and `0x0053beb6-0x0053bec0` ten-byte `0xcc` padding.
   - Mention resolved helper aliases: MapPane coordinate helper `0x0050b080`, `NewHumanImageLib::CalculateBounds`, `MonsterImageLib::ComputeRenderBounds`, and `RectBounds::Offset`.

### Other Support Docs

- [UID:00007B] `by-class/LivingObjectPane.md`: update the render-method section so the stale blocker wording no longer says child formal C++ is rejected. Preserve broad class-level formal C++ blank, but record that exact child pages now own first-draft bodies for `RenderFrame`, `DrawSpriteFrame`, and `ComputeRenderBounds`.
- [UID:0000KU] `by-file/LivingObjectPane.md`: preserve route `NexusTK/map/LivingObjectPane.cpp`; add that the exact child pages, not the parent aggregate, are the source-bearing homes and now contain first-draft formal C++.
- [UID:0003Z3] `by-memory/0x0053a6b0-0x0053aae9.LivingObjectPaneSpriteConfigHelpers.md`: add a support note that its `m_spriteConfig`, `m_isMoving`, `m_facing`, and `m_moveFrame` names plus accepted sibling fields feed the render child formal C++; the broad helper aggregate still does not freeze a complete class descriptor declaration.
- [UID:0003CC] `by-memory/0x00620c10-0x00620c64.ObjectOverlayFrameIndexTables.md`: add a consumer note that [UID:0003NU] reaches the three rows through immediate constants `6425616`, `6425644`, and `6425672` -> `0x620c10`, `0x620c2c`, and `0x620c48` (Verified with int_convert.py), even though `xrefs_to` reports no IDA data xrefs. Preserve [UID:0003CC] ownership and do not move table ownership to LivingObjectPane. Add the provisional consumer names `kLivingObjectMarkerFrameColors`, `kLivingObjectMarkerColorSet0`, and `kLivingObjectMarkerColorSet1`.
- [UID:000358] `by-memory/0x00620978-0x00620a38.LivingObjectPaneVtableData.md`: add the source-facing child slot names and signatures for `RenderFrame`, `ComputeRenderBounds`, and `DrawSpriteFrame` while preserving the primary and adjusted vtable refs.
- `NewHumanImageLib`, `MonsterImageLib`, `GrafPort`, and `RectBounds` class/file pages: already provide the helper aliases and exact RectBounds/GrafPort helper C++ needed by this report. Do not edit them unless an accepted detail is missing during implementation review.

No generated report or manual coverage-report edit is recommended. Validator refresh is the correct repair for stale generated rows.

## Validator And Generated Freshness Plan

Implementation rework validators were run from `E:\NTK\GhidraBridge\source-3\project-documentation` for every edited by-* file:

> Executable block R001 was removed from this report and preserved verbatim in [0002R2-LivingObjectPaneRenderFrameMethods-report-removed.md](0002R2-LivingObjectPaneRenderFrameMethods-report-removed.md). The archived block is non-authoritative and must not be executed.

Final validator batch results:

| File | Command id | Timestamp | Exit/ok | Generated refresh |
| --- | --- | --- | --- | --- |
| [UID:0002R2] `by-memory\0x0053bb20-0x0053beb6.LivingObjectPaneRenderFrameMethods.md` | `000000002158` | `2026-06-26T02:24:44-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0003NU] `by-memory\0x0053bb20-0x0053bd1c.LivingObjectPaneRenderFrame.md` | `000000002159` | `2026-06-26T02:24:46-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0003NV] `by-memory\0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md` | `000000002160` | `2026-06-26T02:24:47-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0003NW] `by-memory\0x0053bdc0-0x0053beb6.LivingObjectPaneComputeRenderBounds.md` | `000000002161` | `2026-06-26T02:24:57-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:00007B] `by-class\LivingObjectPane.md` | `000000002162` | `2026-06-26T02:25:06-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0000KU] `by-file\LivingObjectPane.md` | `000000002164` | `2026-06-26T02:25:16-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0003Z3] `by-memory\0x0053a6b0-0x0053aae9.LivingObjectPaneSpriteConfigHelpers.md` | `000000002165` | `2026-06-26T02:25:17-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:0003CC] `by-memory\0x00620c10-0x00620c64.ObjectOverlayFrameIndexTables.md` | `000000002166` | `2026-06-26T02:25:27-04:00` | exit `0`, `ok:1` | `deferred` |
| [UID:000358] `by-memory\0x00620978-0x00620a38.LivingObjectPaneVtableData.md` | `000000002167` | `2026-06-26T02:25:33-04:00` | exit `0`, `ok:1` | `deferred` |

Generated freshness policy: stale generated rows are validator/generator-owned. The implementation did not manually edit generated reports, generated C++, project-level generated files, validator cache/state, coverage reports, or IDA DB files. The final filtered validator batch reported `generated_refresh: deferred`; any stale generated rows should refresh through the normal validator/generator pipeline only.

## Implementation Tracking Checklist

- [x] Lease only the immediate edit batch for [UID:0002R2], [UID:0003NU], [UID:0003NV], [UID:0003NW], [UID:00007B], [UID:0000KU], [UID:0003Z3], [UID:0003CC], and [UID:000358]. Proof: B001 leased exactly those nine by-* files for the edit/validator work. The first lease window expired during the long validator batch; B001 then re-leased the same nine files for validation and released all nine successfully. `current_leases.md` was empty after release.
- [x] Update [UID:0002R2] with the 2026-06-26 B001 MCP rework/change note while preserving `88/90`, owner [UID:00007B], `RECONSTRUCTABLE:FALSE`, blank emitters, and blank C++. Proof: `by-memory/0x0053bb20-0x0053beb6.LivingObjectPaneRenderFrameMethods.md` preserves `COMPLETION:88`, `CONFIDENCE:90`, `CANONICAL_OWNER:00007B`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, and blank `RECONSTRUCTION_CPP CODE`, with the MCP rework note and child source-bearing route.
- [x] Update [UID:0002R2] with MCP session `80de0a67`, `server_health:ok`, schema correction, exact child starts/sizes, vtable refs, padding bytes, and generated-row stale status. Proof: target page records session `80de0a67`, IDB/process facts, `server_health ok`, schema parameter correction, child sizes `0x1fc`/`0x91`/`0xf6`, vtable refs, `0xcc` padding spans, and generated-row handling through validator/generator refresh.
- [x] Update [UID:0003NU] to `89/89`, set the `Item Summary`, insert formal `LivingObjectPane::RenderFrame(GrafPort *target, const RectBounds *bounds)` C++, and carry exact behavior evidence for `GrafPort`, `RectBounds`, `NewHumanImageLib::Draw`, `MonsterImageLib::RenderMonsterImage`, overlay predicates, `SetDrawColor`, `DrawRectFrame`, and `RectBounds::Inset`. Proof: `by-memory/0x0053bb20-0x0053bd1c.LivingObjectPaneRenderFrame.md` now has `89/89`, accepted summary, formal C++ beginning `void LivingObjectPane::RenderFrame(GrafPort *target, const RectBounds *bounds)`, MCP evidence, helper/callee behavior details, overlay draw loop, and helper aliases.
- [x] Update [UID:0003NU] overlay-table wording: reject `0x00620b90` as table, state it is `BGAUGE.EPF`, and document [UID:0003CC] rows `0x00620c10`, `0x00620c2c`, and `0x00620c48` with int-convert proof and no-xref consumer status. Proof: [UID:0003NU] now says `0x00620b90` is UTF-16 `BGAUGE.EPF` / `BGAUGE.pal`, gives row bases `0x00620c10`/`0x00620c2c`/`0x00620c48`, records decimal immediate conversions, and preserves [UID:0003CC] owner/no-xref status.
- [x] Update [UID:0003NV] to `88/89`, set the `Item Summary`, insert formal `LivingObjectPane::DrawSpriteFrame(GrafPort *target, const RectBounds *bounds)` C++, and carry exact behavior evidence for `MonsterImageLib::RenderMonster`, `NewHumanImageLib::RenderHuman`, descriptor branch, vtable refs, and padding. Proof: `by-memory/0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md` now has `88/89`, accepted summary, formal C++ beginning `void LivingObjectPane::DrawSpriteFrame(GrafPort *target, const RectBounds *bounds)`, prototype `void __thiscall(int this, int, _DWORD *)`, vtable refs `0x006209e0`/`0x0062ef70`, callees `0x004db100`/`0x004e0900`, descriptor/field details, and fifteen-byte post-body padding.
- [x] Update [UID:0003NW] to `88/89`, set the `Item Summary`, insert formal `RectBounds *LivingObjectPane::ComputeRenderBounds(RectBounds *outBounds)` C++, and carry exact behavior evidence for output `RectBounds`, MapPane coordinate helper, `NewHumanImageLib::CalculateBounds`, `MonsterImageLib::ComputeRenderBounds`, `RectBounds::Offset`, vtable refs, and padding. Proof: `by-memory/0x0053bdc0-0x0053beb6.LivingObjectPaneComputeRenderBounds.md` now has `88/89`, accepted summary, formal C++ beginning `RectBounds *LivingObjectPane::ComputeRenderBounds(RectBounds *outBounds)`, prototype `_DWORD *__thiscall(int this, _DWORD *)`, output-rectangle return contract, vtable refs `0x006209dc`/`0x0062ef6c`, callees `0x0050b080`/`0x004e09e0`/`0x004db5c0`/`0x004b78f0`, and ten-byte post-body padding.
- [x] Update [UID:00007B] `by-class/LivingObjectPane.md` with the corrected render-readiness state. Proof: class page now has render child rows at [UID:0003NU] `89/89`, [UID:0003NV] `88/89`, and [UID:0003NW] `88/89`; records `RenderFrame`, `DrawSpriteFrame`, and `ComputeRenderBounds` as exact child formal C++ homes; preserves broad class formal C++ blank because whole-class declaration audit is broader than this callback.
- [x] Update [UID:0000KU] `by-file/LivingObjectPane.md` with the same route/source-bearing child-page note. Proof: file page preserves `NexusTK/map/LivingObjectPane.cpp`, adds the child rows to proposed contents/method families, records first-draft formal C++ on exact child pages, and states generated rows refresh through validator/generator only.
- [x] Update [UID:0003Z3] with a support note tying its sprite-config field names to the render child formal C++. Proof: `by-memory/0x0053a6b0-0x0053aae9.LivingObjectPaneSpriteConfigHelpers.md` now has `Render Child Descriptor Cross-Reference` tying `m_spriteConfig`, `m_isMoving`, `m_facing`, `m_moveFrame`, `m_spriteConfig.bodyFrame`, `m_animationFrame`, `m_spriteConfig.frameGroup`, `m_spriteConfig.palette`, and `m_renderEffectState` to [UID:0003NU]/[UID:0003NV]/[UID:0003NW].
- [x] Update [UID:0003CC] with the LivingObjectPane immediate-constant consumer note while preserving AttachedObjectPane/table ownership and no direct IDA xrefs. Proof: `by-memory/0x00620c10-0x00620c64.ObjectOverlayFrameIndexTables.md` records [UID:0003NU] as immediate-constant consumer of the three rows, names the rows `kLivingObjectMarkerFrameColors`, `kLivingObjectMarkerColorSet0`, and `kLivingObjectMarkerColorSet1`, and keeps [UID:0000HJ] ownership/no-direct-xref evidence.
- [x] Update [UID:000358] with source-facing child vtable slot names. Proof: `by-memory/0x00620978-0x00620a38.LivingObjectPaneVtableData.md` now records `0x006209d8 -> LivingObjectPane::RenderFrame(GrafPort *target, const RectBounds *bounds)`, `0x006209dc -> LivingObjectPane::ComputeRenderBounds(RectBounds *outBounds)`, and `0x006209e0 -> LivingObjectPane::DrawSpriteFrame(GrafPort *target, const RectBounds *bounds)`, plus adjusted/secondary refs `0x0062ef68`, `0x0062ef6c`, and `0x0062ef70`.
- [x] Confirm `NewHumanImageLib`, `MonsterImageLib`, `GrafPort`, and `RectBounds` support docs are covered without extra edits. Proof: implementation used existing helper support only; no accepted fact required edits outside the nine scoped files, and child code uses source-facing helper names rather than raw `sub_` labels.
- [x] Preserve formal C++ only where appropriate. Proof: [UID:0002R2] parent formal `RECONSTRUCTION_CPP CODE` remains blank because it is a non-emitting aggregate. [UID:0003NU], [UID:0003NV], and [UID:0003NW] each now contain formal first-draft C++ in their own formal blocks.
- [x] Run scoped validators for every edited file and record exit code, `ok` count, `command_id`, `command_timestamp`, and generated-refresh state. Proof: final validation batch command IDs `000000002158` through `000000002167` all exited `0`, all returned `ok:1`, and all reported `generated_refresh: deferred`; per-file timestamps are listed in the table above.
- [x] Do not manually edit generated outputs. Proof: generated reports, generated C++, project-level generated files, validator state/cache, IDA DB, and coverage reports were not manually edited. Generated refresh remains validator/generator-owned.
- [x] Release all leases immediately after the edit/validator batch and record release state. Proof: B001 unleased all nine scoped paths successfully after validators; follow-up `current_leases.md` showed `No active leases.`

## Implementation Rework Validation Status

- By-* docs were edited only in implementation rework mode and only under scoped leases.
- Changed by-* files: [UID:0002R2], [UID:0003NU], [UID:0003NV], [UID:0003NW], [UID:00007B], [UID:0000KU], [UID:0003Z3], [UID:0003CC], and [UID:000358].
- Changed agent report file: `tools/leaser/Agents/Agent-B001/research/0002R2-LivingObjectPaneRenderFrameMethods-report.md`.
- Validators: command IDs `000000002158` through `000000002167`, all exit `0`, all `ok:1`, all `generated_refresh: deferred`.
- Lease state: validation lease was released successfully for all nine by-* files, and `current_leases.md` showed `No active leases.`

## Implementation Callback Status

- Supervisor verification rework completed on 2026-06-26.
- Parent [UID:0002R2] remains a non-emitting split/index page at `88/90` with blank formal C++.
- Child formal C++ state: [UID:0003NU], [UID:0003NV], and [UID:0003NW] now contain formal first-draft C++ in their own `RECONSTRUCTION_CPP CODE` blocks.
- Validators: command IDs `000000002158` through `000000002167`, all exit `0`, all `ok:1`, all `generated_refresh: deferred`.
- Generated refresh observation: generated rows refresh through validator/generator only; generated files were not manually edited.
- Lease state: validation lease was released successfully for all nine by-* files, and `current_leases.md` showed `No active leases.`

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B001","command_id":"000000004165","destination_path":"executed-b-agent-research/B001/0002R2-LivingObjectPaneRenderFrameMethods-report.md","details":"legacy registry reconstruction","event":"executed","source_path":"tools/leaser/Agents/Agent-B001/research/0002R2-LivingObjectPaneRenderFrameMethods-report.md","timestamp":"2026-06-26T02:35:53","uid":"0002R2"} -->
<!-- {"agent":"B001","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 0002R2-LivingObjectPaneRenderFrameMethods-report-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B001/0002R2-LivingObjectPaneRenderFrameMethods-report.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"0002R2"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
