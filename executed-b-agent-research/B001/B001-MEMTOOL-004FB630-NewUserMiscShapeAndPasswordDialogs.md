** TARGET-REPORT-UID:00019U **
** AUTHOR-AGENT-ID:B001 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# B001-MEMTOOL-004FB630 NewUserMiscShapeAndPasswordDialogs Warning Audit

## Finalized Report / Current Recommendation

- Current recommendation: treat the `auto-generated/by-memory-tool-report.md` warning as a real but low-severity document-text heuristic issue, not as an ownership, range, split, merge, or IDA repair problem.
- Final disposition: [UID:00019U] `by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md` should remain a reviewed non-reconstructable mixed-owner aggregate/index at `85/88`, with `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, and blank `EMITTER_UIDS`.
- Required action: optional target-page body text fix only. Add the exact compact filename label `NewUserMiscShapeAndPasswordDialogs` to the document body so the Advanced-Error-Scan can match the filename label.
- Confidence: high for the warning cause and for no split/range/owner repair.

No by-memory, generated, coverage-report, owner/emitter, score, or C++ reconstruction edit was made by B001.

## Supporting Research

## Target

- Target UID: `00019U`.
- Target path: `by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md`.
- Source warning: `auto-generated/by-memory-tool-report.md` Advanced-Error-Scan:

```text
0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md: missing document text for NewUserMiscShapeAndPasswordDialogs
```

- Current scores and parent state:

```text
COMPLETION:85
CONFIDENCE:88
CANONICAL_OWNER:NONE
RECONSTRUCTABLE:FALSE
EMITTER_UIDS:
```

## Executive Recommendation

The warning is caused by the exact filename label token not appearing in the target body. The page title uses the expanded human-readable form:

```text
# 0x004fb630-0x004fe782 - NewUser Misc, Shape Selector, And Password Dialogs
```

The body repeatedly contains both addresses and the individual child names, but it does not contain the exact contiguous CamelCase label `NewUserMiscShapeAndPasswordDialogs`. The Advanced-Error-Scan is text-only and checks whether filename label text appears after configured ignored sections are skipped. This explains the warning without implying stale range, bad filename, bad split, or missing documentation.

Recommended exact target-page edit, if the supervisor wants the warning cleared:

Insert under the `## Status` heading in `by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md`:

```markdown
- Filename label: `NewUserMiscShapeAndPasswordDialogs`; this is the compact filename/search label for the expanded title "NewUser Misc, Shape Selector, And Password Dialogs".
```

No score change is recommended. No `by-memory/-coverage-report.md` change is recommended. No reconstruction C++ should be added; this aggregate is `RECONSTRUCTABLE:FALSE`, while the exact source-bearing child pages carry reconstruction state.

## Supervisor Active Recheck

- Trigger: `B001-MEMTOOL-004FB630`, a memory-tool Advanced-Error-Scan warning.
- Split repair required: no. The exact children already cover the mixed physical range.
- Owner/emitter repair required: no. The aggregate is correctly non-reconstructable and no-owner; child pages route individually.
- Range repair required: no. Live IDA and current docs agree on the start, child boundaries, padding, and successor packet-helper cluster.
- Report-only handling: yes. B001 did not edit the target page because the user preferred final report only unless an edit was obviously required and safe. The edit is safe and recommended, but not urgent for correctness.

## Inference Research Guidance Check

`by-structure.md` says mixed-range pages should be marked `RECONSTRUCTABLE:FALSE` when the container is only an audit/index page and exact children carry source ownership. That is the current state of UID `00019U`.

`inference_research.md` warns against using address adjacency as source-file proof. This aggregate spans create-user miscellaneous dialog logic, generic select-box controls, old/new shape selectors, and password-change dialog logic because of physical locality, not because one source file owns the full range.

The warning itself is generated by a text heuristic, so the key inference question was whether the filename label mismatch reflects stale documentation. It does not. The body evidence, coverage row, and IDA boundary checks still match the filename and range.

## Evidence Standards Used

Evidence reviewed:

- Target page body and headers.
- `auto-generated/by-memory-tool-report.md` Advanced-Error-Scan warning.
- `auto-generated/-ag-memory-coverage.md` generated state for the parent and exact children.
- `by-memory/-coverage-report.md` row for UID `00019U`.
- Exact child pages for UIDs `0002Q7`, `00019V`, `0002Q8`, `0002Q9`, and `0002QA`.
- Source-root pages for `NewUserMiscDialogPane`, `ChangePasswordDialogPane`, and `UserCreateAppearanceControls`.
- Live IDA MCP `lookup_funcs`, `get_bytes`, and `xrefs_to` checks against session `b001_0003gy`.

## IDA MCP Facts

IDA MCP state:

```text
database: b001_0003gy
input: E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe
imagebase: 0x00400000
auto-analysis: ready
Hex-Rays: ready
```

### Function / Range Facts

Live `lookup_funcs` confirms these modeled functions inside or adjacent to the aggregate:

| Address | IDA result | Meaning |
| --- | --- | --- |
| `0x004fb630` | `sub_4FB630`, size `0x99f` | `NewUserMiscDialogPane` constructor body |
| `0x004fbfd0` | not a function | documented raw destructor-shaped island |
| `0x004fc010` | `sub_4FC010`, size `0x56e` | NewUserMisc command/action handler |
| `0x004fc5c0` | `sub_4FC5C0`, size `0x26` | local reply wrapper |
| `0x004fc5f0` | `sub_4FC5F0`, size `0x18c` | key/help path |
| `0x004fc780` | `sub_4FC780`, size `0x267` | help/description handler |
| `0x004fca20` | not a function | documented raw packet helper |
| `0x004fcac0` | not a function | documented raw packet helper |
| `0x004fcb10` | `sub_4FCB10`, size `0x1de` | NewUserMisc reply handler |
| `0x004fccf0` | `sub_4FCCF0`, size `0x78` | SelectBoxControlPane constructor |
| `0x004fcd70` | `sub_4FCD70`, size `0x7` | select-state accessor |
| `0x004fcd80` | `sub_4FCD80`, size `0x24` | select-state setter |
| `0x004fcdb0` | `sub_4FCDB0`, size `0x48` | select-box mouse handler |
| `0x004fce00` | `sub_4FCE00`, size `0x7f` | select-box paint |
| `0x004fce80` | `sub_4FCE80`, size `0x135` | UserShapeSelectControlPane constructor |
| `0x004fd520` | `sub_4FD520`, size `0xeb` | NewUserShapeSelectControlPane constructor |
| `0x004fdd40` | `sub_4FDD40`, size `0x720` | ChangePasswordDialogPane constructor |
| `0x004fe460` | `sub_4FE460`, size `0x2e` | change-password action helper |
| `0x004fe490` | `sub_4FE490`, size `0x96` | validation helper |
| `0x004fe530` | `sub_4FE530`, size `0x26` | reply dispatcher |
| `0x004fe560` | `sub_4FE560`, size `0x222` | submit helper |
| `0x004fe790` | not a function | raw start of adjacent packet-helper cluster |

### Padding / Boundary Facts

Live `get_bytes` confirms:

- `0x004fb62a-0x004fb630` is `0xcc` padding before the target starts.
- `0x004fccee-0x004fccf0` is padding between `NewUserMiscDialogPaneCoreAndPacketHelpers` and `SelectBoxControlPane`.
- `0x004fce7e-0x004fce80` is padding before `UserShapeSelectControlPaneCore`.
- `0x004fdd33-0x004fdd40` is `0xcc` padding before `ChangePasswordDialogPaneCore`.
- `0x004fe782-0x004fe790` is `0xcc` padding before the adjacent password-change packet-helper cluster.

These facts support the documented target range and child split. They do not suggest a stale filename or missing split.

### Xref / Caller Facts

Live `xrefs_to` confirms the caller split used by the target page:

| Target | Xrefs | Ownership signal |
| --- | --- | --- |
| `0x004fb630` | `0x004fa74e` inside `sub_4FA5B0` | NewUserMisc dialog path |
| `0x004fce80` | four refs in `sub_4FB630` and four refs in `sub_52A540` | reusable UserShape selector across old/new create-user paths |
| `0x004fd520` | `0x0052c7aa` inside `sub_52C360` | NewUserShape selector / newer create-user path |
| `0x004fdd40` | `0x004f7b49` in `sub_4F7A10`, `0x004f90a0` in `0x004f9060` launcher | ChangePassword dialog path |
| `0x004fe9e0` | `0x004fe778` inside `sub_4FE560` | adjacent password-change packet sender is called from submit helper |

This split confirms why UID `00019U` should remain a non-emitting mixed-owner index.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004fb630-0x004fe782` | [UID:00019U] `NewUserMiscShapeAndPasswordDialogs` | Mixed physical aggregate/index | `FALSE` | `NONE` | `85/88` | Keep unchanged; optional alias text fix |
| `0x004fb630-0x004fccee` | [UID:0002Q7] `NewUserMiscDialogPaneCoreAndPacketHelpers` | NewUserMisc constructor/handlers/raw helpers | `TRUE` | `0000LX` | `82/88` | Existing child route |
| `0x004fccf0-0x004fce7e` | [UID:00019V] `SelectBoxControlPane` | Generic select-box class methods | `TRUE` | `0000CQ` | `82/90` | Existing child route |
| `0x004fce80-0x004fd520` | [UID:0002Q8] `UserShapeSelectControlPaneCore` | older/reusable body-shape selector | `TRUE` | `0000FR` | `86/90` | Existing child route |
| `0x004fd520-0x004fdd33` | [UID:0002Q9] `NewUserShapeSelectControlPaneCore` | newer body-shape selector | `TRUE` | `00009G` | `86/88` | Existing child route |
| `0x004fdd40-0x004fe782` | [UID:0002QA] `ChangePasswordDialogPaneCore` | password-change dialog methods | `TRUE` | `00001L` | `86/89` | Existing child route |
| `0x004fe790-0x004ff03f` | [UID:00022W] `ChangePasswordDialogPacketHelpers` | adjacent packet helper cluster | `TRUE` | `0000I3` route | `85`+ docs | Separate successor, not part of target |

## Documentation Evidence And IDA Status

Existing docs that support the conclusion:

- Target UID `00019U` already says it is a reviewed mixed-owner index/container and `RECONSTRUCTABLE:FALSE`.
- `by-memory/-coverage-report.md` already contains a correct row naming `NewUserMiscShapeAndPasswordDialogs` and summarizing it as an ignored/non-emitting mixed-owner aggregate.
- `auto-generated/-ag-memory-coverage.md` lists UID `00019U` as `not_reconstructable` with owner `NONE`.
- Exact child rows in generated coverage emit through distinct parents/source roots.
- Child pages contain current owner/emitter routes and boundary evidence.

Existing docs that are stale or incomplete:

- The target body does not include the exact contiguous filename label `NewUserMiscShapeAndPasswordDialogs`. It only uses the expanded prose form `NewUser Misc, Shape Selector, And Password Dialogs`.
- This is enough to trigger the Advanced-Error-Scan warning.

Generated/coverage state:

- `auto-generated/by-memory-tool-report.md` warning is current.
- `auto-generated/-ag-memory-coverage.md` and `by-memory/-coverage-report.md` are otherwise consistent with current target state.
- No `by-memory/-coverage-report.md` edit is needed.

## Ranked Issue Analysis

### 1. Missing exact filename label in body - accepted

Evidence for:

- The warning names only `NewUserMiscShapeAndPasswordDialogs`, not the addresses.
- `Select-String` finds the address bounds and expanded title in the target page, but no exact `NewUserMiscShapeAndPasswordDialogs` body token.
- The tool report describes Advanced-Error-Scan as a text-only filename/body heuristic.

Evidence against:

- None. This exactly matches the observed document state.

Decision:

- Accept. Add the exact alias line if clearing the warning is desired.

### 2. Stale filename/range/name - rejected

Evidence for:

- Filename label and title differ in formatting.

Evidence against:

- Coverage report and generated coverage both know UID `00019U` as `NewUserMiscShapeAndPasswordDialogs`.
- The expanded page title accurately describes the same contents.
- Live IDA confirms the range and child split.

Decision:

- Reject. Do not rename the file or retitle the page as a correctness fix.

### 3. Split/merge repair needed - rejected

Evidence for:

- The target is broad and mixed.

Evidence against:

- It is intentionally broad and marked `RECONSTRUCTABLE:FALSE`.
- Five exact children already carry source ownership.
- Padding and successor boundaries are documented and live-IDA confirmed.

Decision:

- Reject. No split/merge work is needed for this warning.

### 4. Owner/emitter repair needed - rejected

Evidence for:

- The target has `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS`.

Evidence against:

- This is correct for a non-reconstructable mixed index.
- The exact reconstructable children have their own owner/emitter routes.

Decision:

- Reject. Keep parent metadata unchanged.

### 5. Heuristic false positive requiring no documentation edit - partly rejected

Evidence for:

- The page is substantively documented and correct.

Evidence against:

- The warning is not random; the exact token really is absent.
- Adding a one-line filename-label alias is low risk and would make the page friendlier to the tool.

Decision:

- Treat as a low-severity true positive for exact text, not a substantive documentation defect.

## Exact Recommended Edits

Recommended optional edit to the target page only:

File:

```text
by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md
```

Placement: immediately below `## Status`.

Insert:

```markdown
- Filename label: `NewUserMiscShapeAndPasswordDialogs`; this is the compact filename/search label for the expanded title "NewUser Misc, Shape Selector, And Password Dialogs".
```

Do not change:

```text
COMPLETION:85
CONFIDENCE:88
CANONICAL_OWNER:NONE
RECONSTRUCTABLE:FALSE
EMITTER_UIDS:
EMITTER_POSITION_OPTIONAL:
RECONSTRUCTION_CPP CODE
```

No exact `by-memory/-coverage-report.md` row replacement is needed.

## Validation / Regeneration Recommendation

Validation performed by B001:

- No validator run. No by-* files were edited.
- No dry-run validator mode was used.
- No memory-range mode was used.

Recommended after applying the optional target-page alias edit:

- Rerun the normal project flow that regenerates `auto-generated/by-memory-tool-report.md` / Advanced-Error-Scan so the warning can disappear.
- A file-level validator check may be run if the applying agent's workflow requires it for by-memory edits, but this edit does not alter metadata, ranges, links, ownership, or reconstruction code.
- No `by-memory/-coverage-report.md` rerun/edit is needed for this warning unless the supervisor's normal regeneration bundle includes it.

## Score / Code-Entry Recommendation

Score before:

```text
COMPLETION:85
CONFIDENCE:88
```

Score after recommendation:

```text
COMPLETION:85
CONFIDENCE:88
```

No code-entry-quality content is needed. UID `00019U` is a non-reconstructable aggregate/index, so it must not receive reconstruction C++ even though several exact children are reconstructable. The current `90/90+` source-code gate applies to source-bearing child pages, not to this non-emitting container.

## Final Recommendation

Exact changes applied by B001:

- Created this research report only.

Exact edits recommended:

- Add one filename-label alias line to the target body if the supervisor wants the Advanced-Error-Scan warning cleared.

Exact ownership/range decisions recommended:

- Keep UID `00019U` as `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`.
- Keep the filename and range unchanged.
- Keep all child splits unchanged.
- Keep child owner/emitter routing unchanged.
- No IDA repair is required.

## Follow-Up Actions

- Supervisor or authorized documentation agent: optionally apply the exact one-line alias edit to the target page.
- Supervisor or authorized documentation agent: after the edit, rerun the normal by-memory tool-report generation / Advanced-Error-Scan flow.
- B001: no further research needed for this warning unless the tool still reports the file after the alias is present.

## Confidence

- Recommendation confidence: high.
- Score confidence: high for unchanged `85/88`.
- Remaining uncertainty: only the exact Advanced-Error-Scan ignore-section behavior. The observed missing token is enough to explain the warning, and the recommended alias line should satisfy a text-token check unless the tool's ignored-section patterns also exclude `## Status`.

## Changed Files

- Created: `tools/leaser/Agents/Agent-B001/research/B001-MEMTOOL-004FB630-NewUserMiscShapeAndPasswordDialogs.md`.
- Modified: none outside Agent-B001 research.
- Renamed: none.
- Moved to executed: none.

## Blockers

None.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B001","command_id":"000000004165","destination_path":"executed-b-agent-research/B001/B001-MEMTOOL-004FB630-NewUserMiscShapeAndPasswordDialogs.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:23","uid":"00019U"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
