** TARGET-REPORT-UID:0003FY **
** AUTHOR-AGENT-ID:B001 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
** REPORT-VALIDATION-STATUS: needs-revalidation **
** REPORT-REVALIDATION-REASON: target_uid_unknown: 0003FY **
# 0003FY SharedDownloaderUserAgentWideString Post-Migration Owner/Emitter Research

Revision: B001-0003FY-post-migration, 2026-06-13

## Finalized Report / Current Recommendation

- Target: [UID:0003FY] `by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md`
- Current recommendation: keep `CANONICAL_OWNER:NONE`, keep `RECONSTRUCTABLE:TRUE`, and add two emitter routes: `EMITTER_UIDS:0000JC,0000LE`.
- Final disposition: no canonical owner, but not non-emitting. This is a compiler/linker-pooled UTF-16 `HTTPTEST` user-agent literal with proven source-use contexts in [UID:0000JC] `FileDownloader` and [UID:0000LE] `MiniMap`.
- Required metadata action:

```text
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JC,0000LE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
```

- Keep `COMPLETION:88`, `CONFIDENCE:93`, and blank `RECONSTRUCTION_CPP CODE`.
- No split, merge, new shared owner, canonical-owner assignment, score change, or reclassification is recommended.
- Confidence: high for `CANONICAL_OWNER:NONE` plus the two emitter routes. The only remaining uncertainty is whether original source used repeated local literals pooled by the linker or a named shared declaration; either way, the two source-use file roots are proven and should be recorded as emitters.

## Supporting Research

## Target

- Target UID: `0003FY`
- Target path: `by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md`
- Assignment: `B001-0003FY-post-migration`
- Source report row: `auto-generated/-ag-memory-coverage.md` currently reports the target as `no-owner`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`.
- Previous report: `tools/leaser/Agents/Agent-B001/research/executed/0003FY-SharedDownloaderUserAgentWideString-B001-0003FY.md`
- Current target state: `88/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank C++.

Current generated row:

```text
| [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md) | no-owner | `NONE` |  |  | no |  | `by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md` |  |
```

This row should be regenerated by normal tooling after the target metadata is updated. Do not hand-edit `auto-generated/-ag-memory-coverage.md`.

## Executive Recommendation

Under the current owner/emitter split, the old "parent blank" conclusion is incomplete. The single physical literal still has no defensible canonical declaration owner, but the evidence does justify emitter routes for the two proven source-use file roots:

| Emitter UID | File | Evidence |
| --- | --- | --- |
| `0000JC` | `by-file/FileDownloader.md` | Three direct `push offset szAgent` refs in FileDownloader-dispatched WinINet helpers: `DownloadMinimapFile` (`0x0041a750`), `DownloadCashShopVersion` (`0x0041aa00`), and `DownloadCashShopCatalog` (`0x0041ae20`). |
| `0000LE` | `by-file/MiniMap.md` | One direct modeled MiniMapDownloader ref in `DownloadMinimapFile_453AA0` (`0x00453aa0`) plus one raw MiniMap-side WinINet helper body at `0x00454e30-0x00455040`. |

The final source text should appear as local string literal use in the consuming source functions once those functions are reconstructed. This page should not invent a standalone global or emit a C++ declaration.

## Supervisor Active Recheck

The supervisor requested a post-migration recheck specifically because `0003FY` is a pooled/shared literal case. The key question is not whether FileDownloader or MiniMap is the single owner; the previous report already rejected that. The current-system question is whether proven source-use emitters should be recorded even while `CANONICAL_OWNER:NONE` remains correct.

Answer: yes. This target matches the post-migration pattern already used by [UID:0003IO] `SharedGmWideString`: keep no canonical owner, but route emitters to every proven source-use file root.

## Inference Research Guidance Check

Facts from `by-structure.md` applied here:

- `CANONICAL_OWNER` is direct semantic ownership. It must not be assigned to the consumer with the most xrefs if other source families materially use the same pooled item.
- `EMITTER_UIDS` is output routing, not ownership.
- Pooled string literals and shared constants can have `CANONICAL_OWNER:NONE` with multiple emitters when no single declaration/source owner is proven but source-use contexts are proven.
- Do not invent a shared file/global solely to avoid `NONE`.
- Reconstructable items can have blank C++ when the final source body/declaration is not ready.

`inference_research.md` also applies: `.rdata` adjacency and read xrefs alone do not prove source-file ownership because compiler/linker string pooling can merge repeated literals. Here, the emitter recommendation does not rely on adjacency. It relies on direct immediate operands in two scored source roots.

## Evidence Standards Used

- IDA MCP live facts: health, xrefs, function lookup, rendered listing searches, bytes, signatures, component analysis.
- Local PE facts: unique UTF-16 byte pattern and absolute-address immediate hits.
- Documentation evidence: current target page, prior B001 report, `FileDownloader`, `MiniMap`, `MiniMapDownloader`, FileDownloader helper pages, sibling literal pages, and the local shared-literal precedent [UID:0003IO].
- Negative evidence: no interior/tail xrefs, no RVA-form refs, no hidden pointer refs, no single consumer file covering every use, no separate global/static declaration object.

## IDA MCP Facts

Live IDA MCP used existing session `b001_0003gy` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`; `server_health` reported imagebase `0x400000`, Hex-Rays ready, auto-analysis ready, and strings cache ready.

### Literal And Boundary Facts

| Check | Result |
| --- | --- |
| `get_bytes 0x0060d7e0` | Bytes decode as UTF-16LE `HTTPTEST` followed by four zero bytes. |
| `get_bytes 0x0060d7d0` | Confirms previous FileDownloader vtable tail through `0x0060d7df`, then `HTTPTEST`, then successor minimap literals at `0x0060d7f4`. |
| `xrefs_to 0x0060d7e4` | Zero xrefs; IDA's interior `aTptest` label is not a separate source literal. |
| `xrefs_to 0x0060d7f2` | Zero xrefs; tail null/alignment is not a separate source literal. |
| `xrefs_to 0x0060d7f4` | Two refs from the successor minimap literal child, proving the next child starts separately. |

### Direct Xrefs To `0x0060d7e0`

| Xref | Containing function | Source-use interpretation |
| --- | --- | --- |
| `0x0041a773` | `sub_41A750` / [UID:0002TR] `DownloadMinimapFile` | FileDownloader-dispatched minimap download helper; `push offset szAgent ; lpszAgent`, then `InternetOpenW`. |
| `0x0041aa3b` | `sub_41AA00` / [UID:0002TS] `DownloadCashShopVersion` | FileDownloader-dispatched item-shop version helper; `push offset szAgent ; lpszAgent`, then `InternetOpenW`. |
| `0x0041ae5b` | `sub_41AE20` / [UID:0002TT] `DownloadCashShopCatalog` | FileDownloader-dispatched item-shop catalog helper; `push offset szAgent ; lpszAgent`, then `InternetOpenW`. |
| `0x00453ac3` | `sub_453AA0` inside [UID:0000XN] `MiniMapDownloader` | MiniMapDownloader direct minimap worker helper; `push offset szAgent ; lpszAgent`, then `InternetOpenW`. |
| `0x00454e52` | raw body, no modeled function | Raw MiniMap-side WinINet helper at `0x00454e30-0x00455040`; listing shows `push offset szAgent` and later `InternetOpenW`. |

`search_text` over `0x0041a750-0x0041b110` found the three FileDownloader `push offset szAgent` sites and three `InternetOpenW` calls. `search_text` over `0x00453aa0-0x00455040` found the modeled MiniMapDownloader site plus the raw MiniMap-side `push offset szAgent` site and both corresponding `InternetOpenW` calls.

### Component Facts

`analyze_component` over `0x0041a750`, `0x0041aa00`, `0x0041ae20`, and `0x00453aa0` reported `szAgent` at `0x0060d7e0` as the only shared global/string across all four modeled downloader helpers. It also reported:

- minimap successor literals `0x0060d7f4`, `0x0060d7f8`, `0x0060d7fc`, `0x0060d808`, `0x0060d818`, `0x0060d880`, and `0x0060d894` shared only by `sub_41A750` and `sub_453AA0`;
- item-shop progress literal `0x0060d91c` shared by `sub_41AA00` and `sub_41AE20`;
- fitting-room state `dword_67A73C` used only by the item-shop version/catalog helpers.

This supports the current exact split and proves `HTTPTEST` is broader than the minimap successor child and broader than the FileDownloader-only item-shop children.

### Raw MiniMap Helper Facts

| Check | Result |
| --- | --- |
| `lookup_funcs 0x00454e30` | Not an IDA function. |
| `xrefs_to 0x00454e30` | Zero xrefs to the raw start. |
| `search_text 0x00454e30-0x00455040` | Finds `push offset szAgent`, `.mnm`, S3 minimap URL, and `Downloading : %d\n` use, with WinINet/file operations. |
| `make_signature_for_range 0x00454e30-0x00455040` | Unique function-shaped signature. |
| `lookup_funcs 0x00455050/0x00455060/0x004550d0` | Modeled functions start after the raw body; the raw body is a real code island but not currently callable through a known xref. |

Inference: the raw helper is MiniMap-side source-use evidence, but because the modeled MiniMapDownloader helper already justifies the `0000LE` emitter, the raw helper is supporting evidence rather than a separate emitter route.

## Local PE Facts

An indexed read-only PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` found:

| Pattern | Result |
| --- | --- |
| UTF-16LE `HTTPTEST` plus terminator/alignment | Exactly one hit: file offset `0x20c1e0`, VA `0x0060d7e0`. |
| Absolute VA dword `0x0060d7e0` | Exactly five hits, matching IDA xrefs at VAs `0x0041a774`, `0x0041aa3c`, `0x0041ae5c`, `0x00453ac4`, and `0x00454e53`. |
| RVA dword `0x0020d7e0` | Zero hits. |

An initial unoptimized local PE scan timed out; it was repeated successfully with indexed byte search. No files were changed by either scan.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Current metadata | Post-migration recommendation |
| --- | --- | --- | --- | --- |
| `0x0060d7e0-0x0060d7f4` | [UID:0003FY] `SharedDownloaderUserAgentWideString` | Pooled UTF-16 `HTTPTEST` user-agent literal | `88/93`, `CANONICAL_OWNER:NONE`, blank emitters | Keep `NONE`; set `EMITTER_UIDS:0000JC,0000LE`. |
| `0x0060d7e0-0x0060d9c0` | [UID:0003AL] `CashShopDownloaderLiterals` | Mixed non-emitting literal-pool inventory | `89/92`, `RECONSTRUCTABLE:FALSE` | Remains a physical container, not an emitter/owner. |
| `0x0060d7f4-0x0060d8b8` | [UID:0003FZ] `MinimapDownloadWideLiterals` | Shared minimap literal group | `89/93`, blank emitters | Outside this assignment, but analogous post-migration recheck likely should consider `0000JC,0000LE`. |
| `0x0060d8b8-0x0060d94c` | [UID:0003G0] `CashShopVersionDownloadWideLiterals` | FileDownloader-only item-shop version literals | assigned/emits through `0000JC` | No change. |
| `0x0060d94c-0x0060d958` | [UID:0003G1] `SharedVersionJsonKey` | Shared narrow `version` key | no-owner, blank emitters | Outside this assignment. Broader consumers require separate post-migration review. |
| `0x0060d958-0x0060d9c0` | [UID:0003G2] `CashShopCatalogDownloadWideUrl` | FileDownloader-only item-shop catalog URL | assigned/emits through `0000JC` | No change. |

## Documentation Evidence And IDA Status

Confirmed documentation evidence:

- [UID:0003FY] target page correctly records the bytes, five xrefs, no interior/tail xrefs, raw MiniMap-side helper, and no single canonical owner.
- Previous B001 report correctly rejected FileDownloader-only and MiniMap-only canonical ownership.
- [UID:0000JC] `FileDownloader` clears the file-root gate at `85/88`, owns the downloader dispatcher and FileDownloader-dispatched WinINet helper surface, and already owns FileDownloader-only adjacent literal children.
- [UID:0000LE] `MiniMap` clears the file-root gate at `85/86`, owns the MiniMapDownloader source family, and already routes [UID:0000XN] `MiniMapDownloader` through this root.
- [UID:0003IO] `SharedGmWideString` is the direct current-model precedent: `CANONICAL_OWNER:NONE`, multiple by-file `EMITTER_UIDS`, blank C++ for a pooled UTF-16 literal.

Stale or superseded documentation evidence:

- The old "parent blank and non-emitting" conclusion is superseded only in its emitter part. It remains correct for canonical ownership.
- The prior report used old `AUTOGEN_PARENT_UID` language and recorded a dry-run validator command. This pass did not use dry-run validator modes and applies the current `CANONICAL_OWNER` / `EMITTER_UIDS` split.

## Ranked Ownership And Emitter Analysis

### 1. `CANONICAL_OWNER:NONE` with emitters `0000JC,0000LE`

Evidence for:

- One physical string instance has FileDownloader and MiniMap consumers.
- `FileDownloader` and `MiniMap` both clear `85/85` as file roots.
- The FileDownloader source-use context is direct and repeated: three modeled helper functions push the literal to `InternetOpenW`.
- The MiniMap source-use context is direct: modeled `MiniMapDownloader` helper pushes the literal to `InternetOpenW`; raw MiniMap-side helper provides a second MiniMap-side use.
- Current by-structure specifically says pooled string literals can keep `CANONICAL_OWNER:NONE` while listing multiple `EMITTER_UIDS` for proven source-use contexts.
- This matches the accepted [UID:0003IO] `SharedGmWideString` routing pattern.

Evidence against:

- It does not prove whether the original source used a shared constant declaration or repeated local literals.
- It leaves final C++ blank; the literal will only appear when consuming functions are reconstructed.

Decision: accepted. This is the best current-model representation.

### 2. Canonical owner [UID:0000JC] `FileDownloader`

Evidence for:

- Three of the five direct refs are FileDownloader-dispatched helper functions.
- FileDownloader owns the message dispatcher and several adjacent FileDownloader-only literal children.
- The physical literal immediately follows FileDownloader vtable data.

Evidence against:

- MiniMapDownloader has a modeled direct use, and the raw `0x00454e30-0x00455040` helper is MiniMap-side.
- Address adjacency to FileDownloader vtable data is weak because the surrounding `0x0060d7e0-0x0060d9c0` pool is mixed.
- Assigning canonical ownership to FileDownloader would overclaim declaration ownership for MiniMap source use.

Decision: reject as canonical owner. Accept as emitter.

### 3. Canonical owner [UID:0000LE] `MiniMap`

Evidence for:

- MiniMap owns the direct `MiniMapDownloader` helper and raw MiniMap-side helper context.
- The successor minimap literal group is shared by FileDownloader and MiniMap, reinforcing a real minimap source context.

Evidence against:

- Three FileDownloader-dispatched helpers use `HTTPTEST`, including two item-shop helpers that are not minimap behavior.
- MiniMap ownership would overclaim the FileDownloader item-shop source-use sites.

Decision: reject as canonical owner. Accept as emitter.

### 4. Physical container [UID:0003AL] `CashShopDownloaderLiterals`

Evidence for:

- Physically contains the target and sibling literal children.

Evidence against:

- It is `RECONSTRUCTABLE:FALSE` and intentionally non-emitting.
- It is a split-audit container over mixed FileDownloader, MiniMap, FittingRoom, StartupWindow, and shared literal data.

Decision: reject as owner or emitter.

### 5. New shared source owner such as `DownloaderHttpConstants`

Evidence for:

- A shared user-agent constant is plausible original source design.

Evidence against:

- No pointer global, static object, relocation table, source symbol, initialization site, or source path proves such a declaration.
- Every observed reference is a direct immediate operand to the pooled literal.
- Creating a new owner would convert a linker-pooling artifact into an unsupported source module.

Decision: reject. Do not invent a new owner; use multiple emitters instead.

### 6. FittingRoom, CashShopVersionRequest, StartupWindow, Socket, or ProtocolSend

Evidence for:

- Some downstream item-shop behavior touches FittingRoom state; related sibling `version` key has broader FittingRoom/StartupWindow consumers.

Evidence against:

- `HTTPTEST` direct uses are only FileDownloader and MiniMap-side WinINet opens.
- No `HTTPTEST` xref lands in FittingRoom, StartupWindow, Socket, or ProtocolSend.
- Socket/ProtocolSend are network transport, not WinINet downloader sources.

Decision: reject as owners and emitters.

## Negative Evidence Summary

- No xrefs to the interior `0x0060d7e4` label.
- No xrefs to the terminator/tail at `0x0060d7f2`.
- No RVA-form refs to `0x0060d7e0`.
- No extra absolute-address refs beyond the five direct use sites.
- No by-global page or storage object for a user-agent constant.
- No single source root covers all direct uses without overclaiming.
- No evidence that a new shared constants file existed.
- The raw MiniMap helper has no xrefs to its start, so it should not create a new canonical owner; it only strengthens the MiniMap emitter context.

## Final Recommendation

Recommended target metadata edit:

```text
*** EMITTER_UIDS:0000JC,0000LE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
```

All other header fields remain as-is:

```text
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
```

Recommended target-page body clarification, if the supervisor/A-agent updates the page:

```text
Post-migration routing: keep `CANONICAL_OWNER:NONE` because FileDownloader, MiniMap, and a synthetic shared constants owner each overclaim declaration ownership for this pooled literal. Set `EMITTER_UIDS:0000JC,0000LE` because IDA proves source-use contexts in FileDownloader WinINet helpers and MiniMap downloader/helper code. This is not a standalone global declaration; final source should spell `L"HTTPTEST"` at the consuming source sites unless later source/debug evidence proves a named shared declaration.
```

## Coverage / Supervisor Rows

Do not edit `by-memory/-coverage-report.md` directly. If applying this recommendation, replace the current [UID:0003FY] row under [UID:0003AL] `CashShopDownloaderLiterals`, immediately before [UID:0003FZ], with:

```text
            - [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md) 0x0060d7e0-0x0060d7f4 | string-data | SharedDownloaderUserAgentWideString : reconstructable : 88% : very-strong : UTF-16LE `HTTPTEST` pooled user-agent literal; B001-0003FY post-migration live IDA reconfirmed five direct `push offset szAgent` refs into `InternetOpenW` from FileDownloader minimap/version/catalog helpers, MiniMapDownloader, and raw MiniMap-side helper `0x00454e30-0x00455040`; keep `CANONICAL_OWNER:NONE` because FileDownloader, MiniMap, and the physical mixed container each overclaim declaration ownership, while `EMITTER_UIDS:0000JC,0000LE` records the two proven source-use file roots.
```

No manual `auto-generated/-ag-memory-coverage.md` edit is recommended. After metadata update and normal project tooling, the generated row should show `CANONICAL_OWNER:NONE` and emitter UIDs `0000JC`,`0000LE`.

## Follow-Up Actions

- Supervisor/A-agent: update the target `EMITTER_UIDS` header to `0000JC,0000LE` and refresh generated reports through the normal non-dry-run project flow.
- Supervisor: apply the coverage-row wording above if human coverage-report text is being synchronized.
- Future B-agent target: [UID:0003FZ] `MinimapDownloadWideLiterals` is analogous and likely deserves its own post-migration emitter recheck for `0000JC,0000LE`; it is outside this assignment.

## Confidence

- Recommendation confidence: `91/100`.
- Score confidence: keep `88/93`; the existing score remains accurate because bytes, xrefs, boundaries, and source-use contexts are strong, but original declaration style is unresolved.
- Emitter confidence: `92/100` for `0000JC`; `90/100` for `0000LE`.
- Canonical owner rejection confidence: `90/100`; every candidate owner covers only part of the xref set or is a physical/synthetic container.

## Validator / Command Results

No validator command was run in this pass because no validator-managed by-* files were edited. No dry-run validator mode was used.

Commands/evidence collected:

- IDA MCP `server_health` on `b001_0003gy`: ok.
- IDA MCP `xrefs_to` for `0x0060d7e0`, `0x0060d7e4`, `0x0060d7f2`, `0x0060d7f4`, `0x0060d8b8`, `0x0060d94c`, `0x0060d958`.
- IDA MCP `lookup_funcs` for the five `HTTPTEST` refs and containing helper starts.
- IDA MCP `get_bytes` for `0x0060d7d0`, `0x0060d7e0`, and raw helper `0x00454e30`.
- IDA MCP `find_bytes` for the UTF-16 literal, absolute target VA, target RVA, and interior/tail labels.
- IDA MCP `search_text` for `szAgent` and `InternetOpenW` in FileDownloader and MiniMap helper ranges.
- IDA MCP `analyze_component` for `0x0041a750`, `0x0041aa00`, `0x0041ae20`, and `0x00453aa0`.
- IDA MCP `make_signature_for_range 0x00454e30-0x00455040`: unique raw helper signature.
- Local PE indexed byte scan: unique literal at `0x0060d7e0`, five absolute VA immediate hits, zero RVA hits.

## Changed Files

- Created: `tools/leaser/Agents/Agent-B001/research/0003FY-SharedDownloaderUserAgentWideString-post-migration.md`
- Modified: none outside this report.

## Blockers

No blocker prevents the recommendation. The only unresolved point is original declaration style, which would require debug/source/linker-map evidence. That uncertainty blocks a canonical owner or standalone shared-constant declaration, but it does not block the two emitter routes.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"command_id":"000000004167","details":"report marked for revalidation","event":"needs-revalidation","issue":"target_uid_unknown: 0003FY","source_path":"executed-b-agent-research/B001/0003FY-SharedDownloaderUserAgentWideString-post-migration.md","timestamp":"2026-07-01T19:52:03-04:00"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
