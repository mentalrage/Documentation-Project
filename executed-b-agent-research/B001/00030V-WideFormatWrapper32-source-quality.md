** TARGET-REPORT-UID:00030V **
** AUTHOR-AGENT-ID:B001 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# B001 Report - [UID:00030V] WideFormatWrapper32 Source Quality

Assignment ID: `B001-report-00030V-wide-format-wrapper32-source-quality-20260624`

Target: `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md`

Required report path: `tools/leaser/Agents/Agent-B001/research/00030V-WideFormatWrapper32-source-quality.md`

Status: `FINISHED_IMPLEMENTATION_READY_FOR_SUPERVISOR_VERIFICATION`

## Recommendation

Recommended target disposition:

| Field | Current | Recommended |
| --- | --- | --- |
| `COMPLETION` | `85` | `90` |
| `CONFIDENCE` | `88` | `92` |
| `CANONICAL_OWNER` | `0000OB` (`StringUtil`) | `NONE` |
| `RECONSTRUCTABLE` | `TRUE` | `FALSE` |
| `EMITTER_UIDS` | `0000OB` | blank |
| `RECONSTRUCTION_CPP` | blank | blank |
| `Nested` | `0` | `0` |

The target should be corrected from a source-emitting `StringUtil` helper to reviewed non-emitting MSVC/UCRT secure wide-format support. The source-facing operation is the standard secure wide formatting path with a fixed destination capacity of 32 wide characters, regenerated by the CRT/header/toolchain route at build time or expressed directly at each human-authored caller as normal secure wide formatting. It should not be hand-authored as a standalone NexusTK helper, and it should not emit through `StringUtil.cpp`.

This is a score improvement and metadata correction, not an unchanged-score exception. The previous blocker was the source helper API and owner route. Current MCP evidence plus local UCRT header evidence resolves the source shape enough to reject the old direct `StringUtil` ownership/emission route for [UID:00030V].

## Current Target State Checked

Current page `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` records:

- UID: `00030V`
- `COMPLETION:85`
- `CONFIDENCE:88`
- `CANONICAL_OWNER:0000OB`
- `RECONSTRUCTABLE:TRUE`
- `EMITTER_UIDS:0000OB`
- blank formal `RECONSTRUCTION_CPP CODE`
- summary: fixed `0x20` wide-character wrapper around `___stdio_common_vswprintf_s`, with fitting-room and non-fitting-room UI callers, assigned to `StringUtil`.

The old page correctly captures many binary facts, but its current source-quality conclusion is too source-emitting. The stronger source interpretation is compiler/CRT secure formatting support, not a project-authored StringUtil wrapper.

## MCP Evidence

Mandatory IDA MCP evidence was refreshed using session `80de0a67`; this report is not fallback-only.

MCP session status:

- `server_health database=80de0a67`: OK.
- Active IDB: `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
- Input binary recorded by MCP: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`.
- Module: `NexusTK.exe`.
- Imagebase: `0x400000`.
- Auto-analysis ready: true.
- Hex-Rays ready: true.
- Strings cache ready: true.
- `idb_list`: one active worker session, `80de0a67`.

Boundary and function checks:

| Address | MCP result |
| --- | --- |
| `0x0042122b` | not a function |
| `0x00421230` | function `sub_421230`, size `0x2d` |
| `0x0042125c` | inside `sub_421230` |
| `0x0042125d` | not a function |
| `0x00421260` | function `sub_421260`, size `0x30` |

Target bytes for `0x00421230-0x0042125d`:

`55 8b ec 8d 45 10 50 6a 00 ff 75 0c 6a 20 ff 75 08 e8 5a a7 ff ff ff 70 04 ff 30 e8 c5 10 1b 00 83 c9 ff 83 c4 1c 85 c0 0f 48 c1 5d c3`

Adjacent padding:

- `0x0042122b-0x00421230`: five bytes, all `0xcc`.
- `0x0042125d-0x00421260`: three bytes, all `0xcc`.

The target range is an exact function island with no merged leading or trailing code/data. The successor starts cleanly at `0x00421260`.

Disassembly semantics:

- Standard frame setup.
- Builds a `va_list` from the first variadic argument at `[ebp+0x10]`.
- Pushes null locale.
- Pushes caller-provided format argument.
- Pushes fixed destination count `0x20`.
- Pushes caller-provided destination buffer.
- Calls `sub_41B9A0`.
- Pushes the two dwords returned through the local stdio options pointer.
- Calls `___stdio_common_vswprintf_s`.
- Normalizes any negative result to `-1` with `or ecx, 0xffffffff`, `test eax,eax`, and `cmovs eax,ecx`.
- Returns the nonnegative formatter result unchanged.

Hex-Rays cross-check agrees with the instruction semantics: destination buffer pointer, format pointer, variadic arguments, call to `sub_41B9A0`, call to `__stdio_common_vswprintf_s` with fixed count `0x20`, and negative-result normalization to `-1`.

Target callees:

- `0x0041b9a0` (`sub_41B9A0`), documented separately as local stdio option storage / CRT glue.
- `___stdio_common_vswprintf_s`.

Target xrefs:

| Call site | Caller function | Existing source context |
| --- | --- | --- |
| `0x0041f9e7` | `0x0041f2b0` | `FittingRoomListPane::OnPaint` wrapping item-name tokens |
| `0x0041f9fc` | `0x0041f2b0` | `FittingRoomListPane::OnPaint` single-token fallback |
| `0x0057fb94` | `0x0057faa0` | `NewMacroEditControlPane::OnPaint` macro row label |
| `0x005800de` | `0x0057ffb0` | `IntegrateMacroEditControlPane::OnPaint` macro row accelerator label |

Negative route checks:

- `find type=code_ref target 0x00421230`: exactly the four call sites above.
- `find type=data_ref target 0x00421230`: zero matches.
- Immediate searches for `0x00421230` and RVA-style `0x00121230`: zero matches.
- Byte search for target body: unique match at `0x00421230`.
- Byte search for padding plus target prefix: unique match at `0x0042122b`.
- Xref query for `0x0042125d`: no external references to the post-target padding boundary.

No vtable, callback table, pointer table, resource, data reference, or non-call route was found. All current reachability is direct code call reachability from four UI paint callers.

## Caller Detail

`FittingRoomListPane::OnPaint` (`0x0041f2b0`) uses this function twice while laying out wrapped fitting-room item-name text. The nearby `0x00421260` sibling handles the larger fixed-capacity copy of the initial item-name string; [UID:00030V] handles the smaller 32-wide-character stack buffer used while recombining one or two token fragments for draw calls. The call shapes are format-string driven and do not include fitting-room state inside the helper.

`NewMacroEditControlPane::OnPaint` (`0x0057faa0`) uses this target once to format a small numeric macro slot/control label before drawing the label and then drawing the macro type text such as item/spell category text. This proves the helper is not fitting-room-local.

`IntegrateMacroEditControlPane::OnPaint` (`0x0057ffb0`) uses this target for small accelerator labels including control-key, alt-key, and numeric patterns. This second macro caller confirms the helper is a shared formatting artifact rather than a fitting-room helper with one accidental outside use.

The source-level replacement therefore belongs at the caller expression level: each caller should format its own local fixed-size wide buffer with the normal secure CRT call. The helper itself has no source-owned project state, no UI-class fields, and no text/layout policy of its own.

## UCRT/Header Evidence

Local Windows SDK/UCRT headers were checked to resolve the previously open source-facing API blocker:

- `C:\Program Files (x86)\Windows Kits\10\Include\10.0.26100.0\ucrt\corecrt_stdio_config.h`
- `C:\Program Files (x86)\Windows Kits\10\Include\10.0.26100.0\ucrt\corecrt_wstdio.h`
- `C:\Program Files (x86)\Windows Kits\10\Include\10.0.26100.0\ucrt\corecrt.h`

Relevant header facts:

- The local stdio options helper is declared as a noinline inline function that returns a pointer to process-local/static printf options storage. That matches the call to `sub_41B9A0` and the subsequent two-dword option load.
- The secure wide formatting path calls `__stdio_common_vswprintf_s` with local printf options, destination buffer, destination count, format, null locale for the non-locale overload, and a `va_list`.
- The secure wrapper returns `-1` when the common backend returns a negative value; this matches the target's `cmovs` negative normalization.
- The C++ secure overload machinery supplies the destination array size as a compile-time count. The target's fixed `0x20` capacity is exactly the sort of constant produced for a local 32-wide-character destination buffer.

This header evidence explains the previously unresolved fixed-count helper name/signature without inventing a project API. The correct source-facing interpretation is not a custom `WideFormatWrapper32` or `StringUtil` function; it is ordinary secure wide formatting on a 32-wide-character destination buffer.

No exact original compiler version is asserted. The local UCRT headers are used as current comparative/runtime evidence for the source shape, and the binary body supplies the exact behavior to document. Confidence remains below final-audit levels because the exact original header text and build settings are not present, but the source-quality blocker is resolved enough to change metadata.

## Wrapper Family Check

Related fixed wide-format wrappers were rechecked as part of the target source-placement question:

| UID / Address | Fixed count | Current route | Current evidence from this pass |
| --- | ---: | --- | --- |
| [UID:00030V] `0x00421230-0x0042125d` | `0x20` | `StringUtil` | Exact CRT/UCRT secure wide-format array-overload body; four direct UI paint callers; no pointer/data route |
| [UID:00030X] `0x00421260-0x00421290` | `0x80` | `StringUtil` | Same callee pair and same negative-normalization body shape with fixed `0x80`; one fitting-room paint caller |
| [UID:0002UA] `0x004236e0-0x00423710` | `0x104` | `StringUtil` | Same callee pair and fixed-count body shape; broad caller fan-in |
| [UID:0003X8] `0x0045ef20-0x0045ef50` | `0x100` | `StringUtil` | Same callee pair and fixed-count body shape; ranking/screenshot/user-list caller spread |

The sibling functions are useful support evidence and should receive a caveat in their pages, but this report's implementation recommendation is target-specific for [UID:00030V]. The strong conclusion for [UID:00030V] is not dependent on postponing sibling work: [UID:00030V] has exact bytes, boundaries, caller inventory, no pointer routes, CRT option helper route, and header-level source-shape evidence sufficient for its own metadata repair now.

For implementation, sibling support pages should record that [UID:00030V] is now proven to be the fixed-32 secure wide-format header/CRT specialization and that the family-wide `StringUtil` owner assumption is a historical source-placement hypothesis, not proof of project-authored helper code. Sibling metadata can remain unchanged unless the supervisor chooses to widen the accepted callback; their pages still benefit from a support note preserving this evidence.

## Ranked Ownership and Source Placement

1. Recommended: non-emitting CRT/UCRT secure wide-format support, `CANONICAL_OWNER:NONE`, no emitters.

   This matches the exact instruction body, local stdio options helper, `___stdio_common_vswprintf_s` backend, fixed count `0x20`, null locale, variadic argument forwarding, and negative-result normalization. It also matches by-structure guidance that runtime/CRT/compiler support should come from dependencies/toolchain instead of NexusTK source. Source should be expressed in the owning caller methods as ordinary secure wide formatting of local fixed-size buffers.

2. Rejected as direct owner/emitter: [UID:0000OB] `StringUtil`.

   `StringUtil` was a plausible historical bucket because the helper is shared across multiple UI families and looks like formatting utility code. Current evidence shows the body has no project logic beyond a CRT secure formatting wrapper shape. A `StringUtil` formal C++ block would create an artificial project helper and hide the likely original source shape at the caller sites. `StringUtil` should retain a support/cross-reference note so older docs and sibling pages remain navigable, but [UID:00030V] should no longer emit through it.

3. Rejected: `FittingRoomListPane` or `FittingRoom`.

   Two direct call sites are in `FittingRoomListPane::OnPaint`, but two other direct call sites are in macro edit-control paint methods. The helper has no fitting-room-only state or layout policy. Fitting-room code is a consumer, not the source owner.

4. Rejected: `MacroDialogs`.

   Macro callers prove non-fitting-room reuse, but they do not supply ownership. The same body is used by fitting-room painting, and the helper itself is generic CRT formatting support rather than macro-specific source.

5. Rejected: [UID:0002DR] `FittingRoomSelectionVectorHelpers` aggregate as owner.

   The aggregate is a mixed non-emitting range/container with vector helpers, fitting-room methods, StringUtil-labeled wrappers, and padding. It should not own this helper. If [UID:00030V] is corrected as CRT/header support, the aggregate should list it as a non-emitting child/support function, not as a fitting-room or aggregate-emitted function.

6. Rejected: new project helper name such as `WideFormatWrapper32`, `FormatWide32`, or `StringUtil::FormatWide32`.

   These names describe the decompiled machine helper, not plausible original human source. The binary body is exactly explained by secure CRT formatting machinery and a fixed-size destination array. Inventing a named project helper would reduce source fidelity.

## Positive Evidence

- Exact IDA MCP function boundary `0x00421230-0x0042125d` confirmed.
- Leading and trailing padding confirmed with raw bytes, not inferred only from old docs.
- Unique byte signature for the target body confirmed.
- Direct caller set confirmed as exactly four code xrefs.
- No pointer, data, immediate, vtable, or callback route found.
- Callee pair is exclusively local stdio options glue plus `___stdio_common_vswprintf_s`.
- Fixed destination count is the literal `0x20`.
- Locale argument is null.
- Negative formatter result is normalized to `-1`.
- Caller roles explain why the fixed 32-wide-character destination size appears in both fitting-room text wrapping and macro row label painting.
- Local UCRT headers explain the local stdio options helper and secure wide-format return contract.
- `by-meta/client_libraries.md` already documents static MSVC CRT/STL/Dinkumware support as toolchain/library code, supporting a non-emitting classification for this target.

## Negative Evidence and Rejected Stale Assumptions

- No evidence supports an original NexusTK source function named `WideFormatWrapper32`; that is a documentation/decompiler convenience name.
- No evidence supports a `StringUtil` member/static helper being called from source. The body contains only CRT secure formatting mechanics.
- No evidence supports fitting-room-local ownership; macro paint callers use the same helper.
- No evidence supports macro-dialog ownership; fitting-room paint callers use the same helper.
- No evidence supports aggregate ownership by [UID:0002DR]; that page is a mixed neighborhood/audit container.
- No data xrefs or pointer table entries support a runtime-dispatched helper.
- No successor overlap or no-function island was found at `0x0042125d`; the next function begins at `0x00421260`.
- Old executed reports and Wave2-era artifacts were treated as leads only. Current MCP boundary/xref/callee/byte checks supersede the older source-placement assumption that this target must emit through `StringUtil`.

## Heuristic and Inference Reanalysis

The previous score cap came from unresolved source-facing name/signature and route. That is now resolved enough for metadata improvement:

- Source-facing API: ordinary secure wide formatting into a fixed 32-wide-character destination buffer, not a named project helper.
- Binary helper role: compiler/CRT support artifact for the secure wide-format overload path.
- Fixed count: `0x20` is a destination element count, not a magic UI field or StringUtil policy.
- `sub_41B9A0` role: local stdio printf-options storage helper, not NexusTK string utility logic.
- `___stdio_common_vswprintf_s`: CRT backend call, not project library call.
- Return behavior: negative backend result becomes `-1`; nonnegative result propagates.
- Field naming: no class fields or project globals are read/written by this function.
- Function naming: final docs should avoid promoting `sub_421230` or `WideFormatWrapper32` as source names. They can remain historical/search aliases in evidence only.
- Source placement: caller methods should own their local formatting expressions; the target page should document the binary artifact and be non-emitting.

This satisfies the tightened Rule 26 requirement to investigate the named blockers now. The report does not leave "source route unresolved"; it recommends the current repair.

## C++ Disposition

Formal `RECONSTRUCTION_CPP CODE` disposition for [UID:00030V]: leave the block blank.

Target-specific no-code proof:

- The function body is CRT/header secure formatting support with no NexusTK-owned policy, fields, state, packet flow, UI behavior, or source-level abstraction.
- The source-level operation should appear inside the four caller methods as their own local fixed-buffer secure formatting calls.
- Adding a formal helper body under `StringUtil` would introduce a project API not evidenced by the binary, would duplicate CRT/header behavior, and would make the reconstructed source less like the likely original precompiled source.
- by-structure guidance classifies CRT/runtime/compiler support as non-source-emitting project code. [UID:00030V] is therefore better represented as `RECONSTRUCTABLE:FALSE` with blank emitters and blank C++.
- The combined score/emitter gate is no longer relevant after the recommended metadata correction because the target should not be an emitting NexusTK item. If the supervisor declines the metadata correction and keeps `RECONSTRUCTABLE:TRUE`, the current evidence still does not justify a formal project helper block; it would require intentionally emitting a toolchain artifact as game-client source.

No draft/example/sample C++ is provided in this report. There is no side-section body snippet to copy. The exact formal reconstruction block recommendation is blank.

## Score Rationale

Recommended `90/92`:

- `COMPLETION` rises to `90` because the exact range, padding, caller set, callee set, fixed capacity, return behavior, source API class, and owner/emitter repair are all resolved for this target.
- `CONFIDENCE` rises to `92` because the binary facts are current MCP facts and the CRT/header comparison independently explains the full body.
- Scores are intentionally below final-audit levels because the exact original compiler header version, original source text, and family-wide sibling metadata cleanup are not all proven in this one target callback. Those are confidence caps, not blockers to the target repair.

The recommended metadata change should also remove [UID:00030V] from the current "Not-Covered Files - Reconstructable" queue after accepted implementation and generated refresh.

## Exact Target and Support Doc Changes Recommended

Target page `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md`:

- Change metadata to `COMPLETION:90`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, `Nested:0`, blank emitter position, and blank C++.
- Rewrite the summary/status to identify the target as a fixed-32 secure wide-format CRT/header support helper, not `StringUtil` source.
- Add MCP session `80de0a67` evidence: function boundary, raw bytes, leading/trailing padding, exact caller table, callee pair, no pointer/data/immediate routes, unique byte signature, and successor boundary at `0x00421260`.
- Add source-quality explanation that `0x20` is a destination wide-character count and that negative results normalize to `-1`.
- Add no-code proof and rejected owner/source-placement alternatives.

Support page `by-file/StringUtil.md`:

- Update the fixed wide-format wrapper ownership note so [UID:00030V] is no longer listed as direct emitted `StringUtil` content.
- Preserve a historical/cross-reference note that [UID:00030V] was previously assigned to `StringUtil` but current evidence proves a CRT/header secure-format route.
- Add a family caution for [UID:00030X], [UID:0002UA], and [UID:0003X8] that their same-body fixed-count wrappers should not be assumed project-authored solely because callers are broad.
- Keep actual human-authored string utility functions in `StringUtil`; do not remove unrelated accepted `StringUtil` content.

Support page `by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md`:

- Update the child inventory row for [UID:00030V] to mark it as non-emitting CRT/header secure wide-format support.
- Keep the aggregate non-emitting and mixed-owner.
- Preserve `FittingRoomListPane` and vector-helper child ownership as-is.
- Adjust the aggregate narrative so the fixed-32 helper is a consumer-side formatting artifact used by fitting-room paint and macro paint callers, not a fitting-room/source aggregate output.

Sibling support pages:

- `by-memory/0x00421260-0x00421290.WideFormatWrapper128.md`: add a support note that current [UID:00030V] evidence proves the shared fixed-count wide-format pattern is CRT/header secure-format support; do not add C++ solely because of the old `StringUtil` bucket.
- `by-memory/0x004236e0-0x00423710.WideFormatWrapper.md`: add the same family caveat for the fixed `0x104` wrapper and preserve existing caller facts.
- `by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md`: add the same family caveat for the fixed `0x100` wrapper and preserve its ranking/screenshot/user-list caller facts.

Fitting-room support pages:

- `by-class/FittingRoomListPane.md`: update the helper/source-quality note for `OnPaint` so [UID:00030V] is described as fixed-32 CRT/header secure-format support consumed by the paint method, not as fitting-room-owned source.
- `by-file/FittingRoom.md`: update the helper/source-quality note so fitting-room remains a caller/consumer and not the owner of [UID:00030V].

Ignore/support page `by-memory/-ignored.md`:

- Add or update an entry for `0x00421230-0x0042125d` stating that [UID:00030V] is reviewed non-emitting MSVC/UCRT secure wide-format support for a 32-wide-character destination, with replacement source expression at the caller methods and dependency/toolchain ownership through the CRT/header route.
- Preserve existing padding entries at `0x0042122b-0x00421230` and `0x0042125d-0x00421260`.

No `-coverage-report.md` file should be edited. Generated tracker/coverage changes should be produced only by the validator/generator pipeline after supervisor-accepted implementation.

## Coverage Text

Supervisor-owned/generated coverage expectation after accepted implementation:

`[UID:00030V] 0x00421230-0x0042125d.WideFormatWrapper32.md: COMPLETION 90, CONFIDENCE 92, RECONSTRUCTABLE FALSE, CANONICAL_OWNER NONE, EMITTER_UIDS blank, emits_code false; reviewed non-emitting MSVC/UCRT secure wide-format fixed-32 support. Remove from Not-Covered Files - Reconstructable after generated refresh.`

Manual `-coverage-report.md` edit recommendation: none.

## Validator Plan for Implementation Callback

Run each command from `E:\NTK\GhidraBridge\source-3\project-documentation` after the accepted by-* edits:

- `python .\tools\validator.py --mode file --file by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-file/StringUtil.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-memory/0x00421260-0x00421290.WideFormatWrapper128.md --apply --queue-timeout 240` - not run in the accepted implementation callback because `goal.md` explicitly excluded [UID:00030X] while B002 is actively assigned to that target.
- `python .\tools\validator.py --mode file --file by-memory/0x004236e0-0x00423710.WideFormatWrapper.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-class/FittingRoomListPane.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-file/FittingRoom.md --apply --queue-timeout 240`
- `python .\tools\validator.py --mode file --file by-memory/-ignored.md --apply --queue-timeout 240`

Implementation callback must record command id, command timestamp, exit code, ok count, warnings, and side effects for each scoped validator run.

## Implementation Callback Results

Accepted by-* edits applied on 2026-06-24:

- `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md`
- `by-file/StringUtil.md`
- `by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md`
- `by-memory/0x004236e0-0x00423710.WideFormatWrapper.md`
- `by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md`
- `by-class/FittingRoomListPane.md`
- `by-file/FittingRoom.md`
- `by-memory/-ignored.md`

Explicit callback exclusion:

- `by-memory/0x00421260-0x00421290.WideFormatWrapper128.md` was not edited and not validated by B001 because the accepted implementation callback states: "Do not edit `by-memory/0x00421260-0x00421290.WideFormatWrapper128.md` in this callback because B002 is actively assigned to [UID:00030X]."

Lease proof:

- B001 lease command succeeded for the eight editable by-* files: target, `StringUtil`, aggregate, `0002UA`, `0003X8`, `FittingRoomListPane`, `FittingRoom`, and `by-memory/-ignored.md`.
- B001 release command after the edit/validator batch reported B001 leases had already expired before cleanup: `No active lease` for the target, `0002UA`, and `0003X8`, and `Lease owned by B002` for `StringUtil`, aggregate, `FittingRoomListPane`, `FittingRoom`, and `by-memory/-ignored.md`.
- Current lease-report recheck after the B002 window confirms no active B001 leases remain. A later independent B005 lease exists on `by-file/StringUtil.md`; B001 did not edit through that later B005 lease.
- Final target-header correction audit: B001 leased only `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` and corrected the stale `Item Summary` line locally, but the standard file validator rewrote that line back from manual `by-memory/-coverage-report.md` line 223. The validator code path is `desired_memory_coverage_metadata_defaults()` -> `manual_memory_coverage_absolute_defaults()` -> `ensure_memory_coverage_metadata_lines(... update_existing=True)`, so the line is coverage-owned until the supervisor applies the coverage text below. B001 released the one-file target lease successfully and did not edit the manual coverage report.

Validator results:

| File | Command | command_id | command_timestamp | Exit | ok | Warnings | Side effects |
| --- | --- | --- | --- | ---: | ---: | --- | --- |
| `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` | `python .\tools\validator.py --mode file --file by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md --apply --queue-timeout 240` | `000000000220` | `2026-06-24T07:38:57-04:00` | `0` | `1` | none reported | `completion_update`, `confidence_update`, `canonical_owner_update 0000OB -> NONE`, `autogen_registry_update true -> false`, emitter registry cleared, UID link updates/inserts, `stats_row_remove` from `by-memory_Reconstructable`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` target-header correction audit | `python .\tools\validator.py --mode file --file by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md --apply --queue-timeout 240` | `000000000232` | `2026-06-24T07:45:51-04:00` | `0` | `1` | none reported | `insert_header_blank`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`; validator restored the stale manual-coverage `Item Summary`. |
| `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` target-header correction audit retry | `python .\tools\validator.py --mode file --file by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md --apply --queue-timeout 240` | `000000000237` | `2026-06-24T07:47:50-04:00` | `0` | `1` | none reported | `insert_header_blank`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`; validator again restored the stale manual-coverage `Item Summary`, proving the remaining header-summary update is supervisor/manual-coverage-owned. |
| `by-file/StringUtil.md` | `python .\tools\validator.py --mode file --file by-file/StringUtil.md --apply --queue-timeout 240` | `000000000221` | `2026-06-24T07:39:06-04:00` | `0` | `1` | none reported | UID link inserts for `00030V`/`00030X`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md` | `python .\tools\validator.py --mode file --file by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md --apply --queue-timeout 240` | `000000000222` | `2026-06-24T07:39:17-04:00` | `0` | `1` | none reported | UID link update for renamed `00030R`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-memory/0x004236e0-0x00423710.WideFormatWrapper.md` | `python .\tools\validator.py --mode file --file by-memory/0x004236e0-0x00423710.WideFormatWrapper.md --apply --queue-timeout 240` | `000000000223` | `2026-06-24T07:39:25-04:00` | `0` | `1` | none reported | UID link inserts for `00030V`, reference index add, stats row update, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md` | `python .\tools\validator.py --mode file --file by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md --apply --queue-timeout 240` | `000000000224` | `2026-06-24T07:39:40-04:00` | `0` | `1` | none reported | UID link inserts for `00030V`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-class/FittingRoomListPane.md` | `python .\tools\validator.py --mode file --file by-class/FittingRoomListPane.md --apply --queue-timeout 240` | `000000000225` | `2026-06-24T07:39:50-04:00` | `0` | `1` | none reported | `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-file/FittingRoom.md` | `python .\tools\validator.py --mode file --file by-file/FittingRoom.md --apply --queue-timeout 240` | `000000000227` | `2026-06-24T07:40:05-04:00` | `0` | `1` | none reported | Reference index add for `00030V`, `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |
| `by-memory/-ignored.md` | `python .\tools\validator.py --mode file --file by-memory/-ignored.md --apply --queue-timeout 240` | `000000000228` | `2026-06-24T07:40:17-04:00` | `0` | `1` | none reported | `stats_incremental_noop`, `projected_stats_update`, `generated_refresh: deferred`. |

Manual coverage report action:

- No manual `-coverage-report.md` file was edited. The coverage expectation below remains supervisor/generated-owner text only.
- The top `Item Summary` line in the target page still reflects stale manual coverage text because the validator rewrites it from `by-memory/-coverage-report.md` line 223. This was intentionally left unapplied under the callback's `-coverage-report.md` edit ban; the target body summary/status, metadata, ownership, C++ disposition, and evidence sections are updated at report-level detail.

## Implementation Tracking Checklist

- [x] Lease only the target/support files immediately before editing, and release the lease immediately after the edit/validator batch. Proof: B001 lease command succeeded for the eight callback files. Release cleanup found B001 leases already expired and no current active B001 lease remains; later B002/B005 leases were not edited through after detection. Final target-header correction audits used one-file B001 leases and the final one-file release succeeded after validator `000000000237`; current lease report shows no active leases.
- [x] Update `by-memory/0x00421230-0x0042125d.WideFormatWrapper32.md` metadata to `90/92`, owner `NONE`, `RECONSTRUCTABLE:FALSE`, blank emitters, blank formal C++. Proof: target metadata header is updated; validator `000000000220` exit `0`, `ok:1`, recorded completion/confidence/canonical-owner/autogen registry updates, and retry validator `000000000237` exit `0`, `ok:1`.
- [x] Mark the target `Item Summary` header update as supervisor/manual-coverage-owned. Proof: B001 twice patched the stale line, but validators `000000000232` and `000000000237` restored it from manual `by-memory/-coverage-report.md` line 223 through the documented validator metadata-injection path; editing that coverage report is forbidden in this callback. Exact supervisor-owned coverage text remains in the `Coverage Text` section.
- [x] Insert current MCP evidence into the target page: session `80de0a67`, exact bytes, disassembly semantics, raw padding, unique signature, caller table, callee pair, and no pointer/data/immediate route. Proof: target page now has a detailed `Evidence` section with all listed facts.
- [x] Insert target-specific no-code proof and rejected ownership/source-placement alternatives into the target page. Proof: target page now has `Source Placement And Rejected Alternatives` and `C++ Disposition` sections preserving the no-code proof.
- [x] Update `by-file/StringUtil.md` so [UID:00030V] is no longer direct emitted `StringUtil` content and the old route is preserved as superseded historical context. Proof: `StringUtil.md` likely-contents/ownership/evidence/scope-exclusion/change sections updated; validator `000000000221` exit `0`, `ok:1`.
- [x] Update `by-memory/0x00420de0-0x00421301.FittingRoomSelectionVectorHelpers.md` child inventory and aggregate narrative for [UID:00030V] as non-emitting CRT/header support. Proof: aggregate summary, status, child table, ownership notes, split audit, parent-source follow-up, ignored-ledger cross-reference, and change log updated; validator `000000000222` exit `0`, `ok:1`.
- [x] Add fixed-wide-format family caveats to `by-memory/0x00421260-0x00421290.WideFormatWrapper128.md`, `by-memory/0x004236e0-0x00423710.WideFormatWrapper.md`, and `by-memory/0x0045ef20-0x0045ef50.WideFormatWrapper256.md` without inventing C++ helper code. Proof: `0002UA` and `0003X8` pages updated with family caveats and blank-C++ policy; validators `000000000223` and `000000000224` exit `0`, `ok:1`. [UID:00030X] page intentionally excluded by accepted callback because B002 is actively assigned to that target; no B001 edit or validator was run for `by-memory/0x00421260-0x00421290.WideFormatWrapper128.md`.
- [x] Update `by-class/FittingRoomListPane.md` and `by-file/FittingRoom.md` to describe [UID:00030V] as consumed CRT/header secure-format support, not fitting-room-owned source. Proof: both support docs updated with the consumed-support wording and `00030X` active-B002 note; validators `000000000225` and `000000000227` exit `0`, `ok:1`.
- [x] Update `by-memory/-ignored.md` with reviewed non-emitting support status for `0x00421230-0x0042125d`, preserving adjacent padding entries. Proof: ignored ledger now has a dedicated [UID:00030V] reviewed non-emitting entry and the existing adjacent padding entry is preserved; validator `000000000228` exit `0`, `ok:1`.
- [x] Run scoped validators for every changed by-* file from `source-3/project-documentation`. Proof: eight scoped validators ran with exit `0` and `ok:1`; [UID:00030X] was not changed and not validated due explicit callback exclusion.
- [x] Confirm no `-coverage-report.md`, generated/project-level file, validator state, IDA DB, or unrelated by-* file was edited. Proof: no manual edits were made to coverage reports, generated files, queue/lock files, IDA DB, or unrelated by-* docs. Validator-owned side effects did update `tools/validator.ini` and `project-level/-auto-completion-stats.md`/deferred generated state as reported by validator output.
- [x] Report lease release/expiry state after implementation. Proof: lease section above records successful lease acquisition, expiration before release cleanup, and current no-active-B001 lease state.
- [x] Leave manual coverage text unapplied; supervisor/generator owns coverage refresh. Proof: no `-coverage-report.md` file was edited; coverage expectation remains in the `Coverage Text` section for supervisor/generated handling.

## Report-Only Compliance

- No target/support by-* docs were edited during this report-only pass.
- No generated files were edited.
- No project-level files were edited.
- No manual `-coverage-report.md` file was edited.
- No validator state was edited.
- IDA DB/tool state was not modified.
- No subagents were spawned.
- No C++ reconstruction snippet was written outside a formal block; the formal recommendation is blank C++ with a target-specific no-code proof.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B001","command_id":"000000004165","destination_path":"executed-b-agent-research/B001/00030V-WideFormatWrapper32-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-24T08:16:36","uid":"00030V"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
