** TARGET-REPORT-UID:0004HT **
** AUTHOR-AGENT-ID:B001 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 0004HT DLPaletteExpandMappedColorRaw Source-Quality Research

## Finalized Report / Current Recommendation
- Current recommendation: [UID:0004HT] `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` should be upgraded from blank empty-emitter state to a source-ready DLPalette method body using the formal `RECONSTRUCTION_CPP CODE` block in this report.
- Final disposition: direct owner and emitter remain [UID:00003Z] `DLPalette`, with [UID:0000MA] `Palette` as the source-file umbrella. The helper is a retained raw, non-IDA-modeled DLPalette receiver method that expands the mapped native color stored at `this + 0x2c + index*2` through Surface callback-table slot `0x0069b400`.
- Implementation callback status: target/support by-* docs were updated at report-level detail, the blank target formal C++ block was replaced with the exact formal block below, and DLPalette/Palette/Surface callback slot-8 support docs now use the descriptive callback alias `g_pfnExpandPixel`.
- Confidence: high for exact range, behavior, argument order, receiver ownership, callback slot, no-direct-xref state, and no split/padding issue; medium-high for the descriptive source-facing method and callback variable names because no original symbol or live caller was recovered.
- Implementation boundary: only the accepted target/support by-* docs, this B001 report, and validator-generated `auto-generated/NexusTK/render/Palette.cpp` output changed. No coverage reports, supervisor ledgers, queue files, lifecycle/archive files, manual generated edits, MCP/IDA state edits, or report execution/archive paths were touched.

## Supporting Research
- Direct current target: [UID:0004HT] `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`.
- Parent split report: `executed-b-agent-research/B008/0001E4-DLPaletteMethodCluster-source-quality.md` created the exact child and recorded it as a raw DLPalette receiver helper with no direct xrefs, no VA/RVA pointer hits, and no direct rel32 callers.
- Surface callback evidence: `executed-b-agent-research/B005/0000OC-Surface-empty-emitter-family-source-quality.md` and the slot-8 child pages establish that `0x0069b400` dispatches to the RGB555/RGB565 expand callbacks at `0x004bed80` and `0x004c46d0`.
- Support docs checked: `by-class/DLPalette.md`, `by-file/Palette.md`, `by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md`, sibling pages UID0004HQ/UID0004HR/UID0004HS, `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`, the slot-8 callback child pages, generated `auto-generated/NexusTK/render/Palette.cpp`, generated memory coverage, generated by-memory coverage, and the current research tracker row.
- Required search terms used across current docs and executed B-agent reports: `TARGET-REPORT-UID:0004HT`, `0004HT`, `0x00542b70`, `0x00542baf`, `DLPaletteExpandMappedColorRaw`, `dword_69B400`, `unk_69B400`, `ExpandRgb555Pixel`, `ExpandRgb565Pixel`, `DLPalette::MapColor`, `DLPalette::SetColor`, `DLPaletteSetPackedColor`, `DLPalette`, `Palette`, `SurfaceRenderCallbackTable`, `00003Z`, and `0000MA`.
- Result of old-report search: no direct executed B-agent report for `TARGET-REPORT-UID:0004HT` was found; B008 and B005 are the relevant parent/support reports.

## Target
- Target UID: `0004HT`.
- Target path: `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`.
- Metadata after callback: `COMPLETION:88`, `CONFIDENCE:91`, `CANONICAL_OWNER:00003Z`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00003Z`, blank optional emitter position, formal C++ block present.
- Validator state after callback: target validator command `000000007742` updated UID0004HT completion/confidence and changed the autogen registry entry from blank to block.
- Current generated state: `auto-generated/NexusTK/render/Palette.cpp` was regenerated by the scoped validator path and now contains the UID0004HT `DLPalette::ExpandMappedColor` body, not a UID0004HT empty emitter marker.

## Current Target State
- The target now has the correct exact half-open range, DLPalette ownership, `88/91` metadata, and formal first-draft C++ despite no direct static entry route.
- The target text records the raw function-shaped body, IDA's lack of a function object at `0x00542b70`, the `ecx` receiver, index guard, mapped-color table access at `+0x2c`, dispatch through `0x0069b400`, and zero fallback writes.
- The stale blocker was resolved as a score cap: no direct caller/original helper name was recovered, but the body is fully bounded, source-owned, self-contained, and has a confirmed emitter route.
- The support docs now place the helper in the DLPalette/Palette family as source-ready with retained raw/no-direct-caller caveat, and Surface table docs now carry the slot-8 `g_pfnExpandPixel` / `SurfaceExpandPixelProc` provider dependency.

## Heuristic / Inference Reanalysis And Validation
- IDA modeling issue: resolved. `lookup_funcs 0x00542b70` returns "Not a function", but `get_bytes` and bounded disassembly show a complete prologue, two return sites, and an alignment byte at `0x00542baf`. The lack of an IDA function object is a database modeling limitation, not a proof against source.
- Liveness issue: resolved as a score cap. `xrefs_to`, `find_bytes`, and a PE rel32 scan still find no static entry route to `0x00542b70`. This prevents proving the original method name or callsite role, but it does not change the helper body or ownership.
- Source ownership issue: resolved. The helper uses the DLPalette receiver in `ecx` and reads only the mapped color table at `this + 0x2c`; Surface only owns the callback implementation behind `0x0069b400`.
- Callback naming issue: implementation-ready with support repair. Existing slot-8 child pages use `ExpandRgb555Pixel` and `ExpandRgb565Pixel`; support docs should name the shared slot as descriptive `g_pfnExpandPixel` / `SurfaceExpandPixelProc`.
- Return-type issue: resolved as void. The target ignores the callback return and has no evidence of a meaningful method return; B005 already noted the slot-8 consumer at `0x00542b70` calls `0x0069b400`, adjusts `esp`, and returns without consuming `eax`.
- Range/split issue: resolved. The body is exactly `0x00542b70-0x00542baf`, preceded by padding after UID0004HS and followed by one alignment byte before `sub_542BB0`; no sibling code belongs inside this page.
- Rejected alternatives:
  - Keep blank C++ with nonblank emitter: rejected because it leaves an empty generated marker after the behavior and emitter route are known.
  - Clear `EMITTER_UIDS` instead of emitting: rejected because the direct source owner and source-file route are sufficiently established for this exact method.
  - Route to Surface: rejected because Surface provides the callback slot and installed callback targets only; this body is a DLPalette receiver helper.
  - Move into PaletteLib or a standalone helper file: rejected because the receiver is DLPalette and the established source umbrella is `Palette.cpp`.

## Score-Blocker Audit
| Blocker | Current status | Resolution |
| --- | --- | --- |
| No IDA function object at `0x00542b70` | Still true | Not a code blocker; raw bytes/disassembly prove a complete method body. |
| No direct xrefs or pointer hits | Still true | Score cap and caveat only; no evidence contradicts retained source method ownership. |
| Original helper name unknown | Still true | Use descriptive source-facing `DLPalette::ExpandMappedColor`; do not claim original symbol recovery. |
| Callback slot source name unsettled | Partially unresolved | Add support alias `g_pfnExpandPixel` / `SurfaceExpandPixelProc` in callback-table docs. |
| Empty generated marker | Resolved in callback | Target formal C++ block is filled, validator command `000000007742` changed the registry entry from blank to block, and generated `Palette.cpp` now emits UID0004HT code. |
| Split/range uncertainty | Resolved | Exact `0x3f`-byte body and padding boundaries confirmed by MCP bytes/disassembly. |

## Evidence Standards Used
- Direct IDA MCP evidence: `server_health`, `lookup_funcs`, `get_bytes`, `disasm`, `decompile`, `xrefs_to`, `xref_query`, `find_bytes`, `int_convert`, and sibling/callback decompilation.
- Read-only local binary evidence: PE executable-section scan for direct `E8`/`E9` rel32 calls or jumps to `0x00542b70`.
- Documentation evidence: current target page, parent split inventory, DLPalette class page, Palette file page, Surface callback-table memory page, slot-8 callback child pages, generated output, generated coverage, tracker row, and executed B-agent reports listed above.
- Inference used: final descriptive source names `DLPalette::ExpandMappedColor`, `g_pfnExpandPixel`, and `SurfaceExpandPixelProc`. These are role-derived names, not claimed original symbols.

## Evidence Checked
- MCP session: `b001-0004HT`, opened against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
- MCP health: `status:ok`, module `NexusTK.exe`, input `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, image base `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, strings cache size `2067`.
- `lookup_funcs`: `0x00542b70`, `0x00542b63`, and `0x00542baf` are not functions; `0x00542bb0` is `sub_542BB0` size `0x2a`; siblings `0x00542ac0`, `0x00542ae0`, and `0x00542b50` are modeled as `sub_542AC0`, `sub_542AE0`, and `sub_542B50`.
- `disasm 0x00542b70`: shows `push ebp`, `mov ebp, esp`, load index from `[ebp+8]`, unsigned compare against `0xff`, success branch loads `word ptr [ecx+eax*2+2Ch]`, pushes three output pointers and the mapped color, calls `dword ptr unk_69B400`, adjusts `esp` by `0x10`, and returns with `retn 10h`; fallback writes zero through the three output pointers and returns with `retn 10h`.
- `decompile 0x00542b70`: fails with "Decompilation failed at 0x542b70", matching the no-function state.
- `get_bytes`: exact target bytes from `0x00542b70` for 63 bytes are `55 8b ec 8b 45 08 3d ff 00 00 00 77 1c ff 75 14 0f b7 44 41 2c ff 75 10 ff 75 0c 50 ff 15 00 b4 69 00 83 c4 10 5d c2 10 00 8b 45 14 c6 00 00 8b 45 10 c6 00 00 8b 45 0c c6 00 00 5d c2 10 00`.
- `xrefs_to`: `0x00542b70`, `0x00542b63`, and `0x00542baf` each have zero cross-references; `0x0069b400` has four xrefs including the target call site at `0x00542b8c` and Surface initializer/setup references.
- `find_bytes`: VA bytes `70 2B 54 00` and RVA bytes `70 2B 14 00` have zero matches.
- `int_convert`: `0x00542b70` converts to bytes `70 2b 54 00`; `0x00142b70` converts to bytes `70 2b 14 00`; target range length `0x3f` converts to decimal `63`.
- Local PE scan: executable `.text` section image base `0x400000`, `.text` VA `0x1000`, raw pointer `0x400`, raw size `0x20b600`; zero `E8`/`E9` rel32 calls or jumps target `0x00542b70`.
- Sibling/callback decompilation: `0x00542ac0` returns mapped table entry or zero; `0x00542ae0` packs/stores through `0x0069b404`; `0x00542b50` stores a packed value; `0x004bed80` and `0x004c46d0` expand packed pixels into three output byte lanes.
- Failed/unavailable/skipped checks: no IDA rename/type/comment edits were attempted. Implementation validators were later run in the callback as recorded under `Validator Results`.

## Claim And Incorporation Ledger
| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| HT-001 | UID0004HT is a complete raw DLPalette receiver method despite no IDA function object. | High | MCP `lookup_funcs`, `get_bytes`, `disasm`, `decompile` failure; implementation proof: target Status/Address Range/Raw Evidence updated and validator `000000007742` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` Status, Range, Reconstruction Notes. | incorporate | applied |
| HT-002 | The body reads `m_mappedColors[index]` at `this+0x2c` when `index <= 0xff`. | High | `movzx eax, word ptr [ecx+eax*2+2Ch]`, DLPalette layout; implementation proof: target Behavior/C++ and `by-class/DLPalette.md` method row updated, validators `000000007742` and `000000007743` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` Behavior; `by-class/DLPalette.md` layout/method table. | incorporate | applied |
| HT-003 | Out-of-range indexes zero all three output byte pointers. | High | fallback instructions at `0x542b99-0x542bac`; implementation proof: target Behavior and formal C++ preserve fallback order, validator `000000007742` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` Behavior and formal `RECONSTRUCTION_CPP CODE` block. | incorporate | applied |
| HT-004 | Slot `0x0069b400` is an expand-pixel callback provider dependency, not the target owner. | High | target call at `0x542b8c`, slot-8 callback child pages, Surface callback table; implementation proof: target dependency/rejected alternatives and both Surface table docs updated, validators `000000007742`, `000000007746`, and `000000007747` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` dependencies/rejected alternatives; `by-global/SurfaceRenderCallbackTable.md`; `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`. | incorporate | applied |
| HT-005 | No direct xrefs, VA/RVA pointer hits, or direct rel32 branches target `0x00542b70`. | High | MCP `xrefs_to`, `find_bytes`, local PE scan; implementation proof: target Raw Evidence/Changes preserves the negative evidence and validator `000000007742` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` Negative Evidence, Open Questions, and Changes entry. | incorporate | applied |
| HT-006 | Formal C++ should be emitted through DLPalette/Palette rather than leaving an empty emitter marker. | Medium-high | confirmed owner/emitter route, code gate score, exact behavior, generated empty marker; implementation proof: target formal block inserted, `by-file/Palette.md` row updated, generated `Palette.cpp` now emits UID0004HT body, validators `000000007742` and `000000007744` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` metadata/formal C++ block; `by-file/Palette.md` DLPalette contents. | incorporate | applied |
| HT-007 | Recommended source-facing names are descriptive, not recovered originals. | Medium-high | no symbols/xrefs, existing project naming conventions, slot leaf names; implementation proof: target/class/file/global/storage caveats updated and validators `000000007742` through `000000007747` exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` caveats; `by-class/DLPalette.md`; `by-file/Palette.md`; `by-global/SurfaceRenderCallbackTable.md`; `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`. | incorporate | applied |
| HT-008 | Recommended score after callback is `88/91`. | Medium-high | behavior/range strong; original name/caller unresolved; implementation proof: target header now `COMPLETION:88` / `CONFIDENCE:91`, validator `000000007742` recorded completion/confidence updates and exited 0/ok 1. | `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md` metadata header and Changes entry. | incorporate | applied |

## Positive Evidence Summary
- The raw body is exact and compact: 63 bytes from `0x00542b70` through `0x00542baf`, with a normal prologue, two return sites, and no internal split.
- The DLPalette receiver is direct: `ecx` indexes the `+0x2c` mapped-color table already documented in `DLPalette.md`.
- The behavior aligns with neighboring DLPalette methods: UID0004HQ reads mapped colors, UID0004HR packs RGB through slot `0x0069b404`, UID0004HS stores packed values, and UID0004HT expands a stored packed value through slot `0x0069b400`.
- The callback dependency is well supported: slot 8 has RGB555 and RGB565 installed expand callbacks, and the target call pushes the mapped color followed by the three output pointers.
- The code gate is met: reconstructable true, direct emitter route `00003Z`, and current combined score above 85. The new evidence supports filling the formal block rather than leaving an empty generated marker.

## Negative Evidence Summary
- IDA still does not model `0x00542b70` as a function and Hex-Rays cannot decompile it directly.
- No static direct xref, pointer byte match, or direct rel32 branch to `0x00542b70` was found.
- No original source name, declaration location, or caller-side parameter names were recovered.
- The callback slot has leaf names but no finalized shared source declaration in the current support docs.
- These negatives cap the score below final-audit certainty; they do not contradict the body, owner, range, or first-draft C++.

## IDA MCP Facts
- Session `b001-0004HT` was live and healthy; no fallback-only report was produced.
- `lookup_funcs 0x00542b70`: not a function.
- `disasm 0x00542b70`: raw code in `.text`, label `<no function>`, call to `unk_69B400` at `0x542b8c`, successor alignment at `0x542baf`, next modeled function `sub_542BB0` at `0x542bb0`.
- `xrefs_to 0x0069b400`: four xrefs, including target dispatch `0x542b8c` and Surface initializer/setup refs at `0x5587a3`, `0x558bda`, and `0x558ef6`.
- `xrefs_to 0x0069b404`: nine xrefs, including sibling `DLPalette::SetColor` at `0x542b35`, confirming the adjacent pack/expand callback pair.
- `decompile 0x004bed80`: RGB555-family expand target writes lane bytes as shifted/masked values.
- `decompile 0x004c46d0`: RGB565-family expand target writes lane bytes as shifted/masked values with the middle lane using the wider green mask.

## Function / Child Inventory
| Range / Item | UID / Path | Role | Current state | Recommendation |
| --- | --- | --- | --- | --- |
| `0x00542ac0-0x00542adc` | UID0004HQ `DLPaletteMapColor` | Reads mapped native color or zero | source-owned sibling | no required change for UID0004HT |
| `0x00542ae0-0x00542b4a` | UID0004HR `DLPaletteSetColor` | Packs bytes through slot `0x0069b404` and stores native color | source-owned sibling | no required change for UID0004HT |
| `0x00542b50-0x00542b63` | UID0004HS `DLPaletteSetPackedColor` | Stores already-packed native color | source-owned sibling | no required change for UID0004HT |
| `0x00542b63-0x00542b70` | ignored/padding | alignment before raw expand helper | padding | keep outside UID0004HT |
| `0x00542b70-0x00542baf` | UID0004HT target | Expands mapped native color through slot `0x0069b400`, or zeroes outputs when out of range | source-ready formal C++ applied | keep no-direct-caller/original-symbol caveat |
| `0x00542baf-0x00542bb0` | ignored/padding | one-byte alignment before constructor | padding | keep outside UID0004HT |
| `0x00542bb0-0x00542bda` | UID0004HU `DLPaletteDefaultConstructor` | next modeled DLPalette constructor | successor sibling | no required change for UID0004HT |

## Direct Xref / Caller Inventory
| Address / Item | Evidence | Meaning |
| --- | --- | --- |
| `0x00542b70` | zero `xrefs_to`; zero VA/RVA pointer byte matches; zero local PE rel32 hits | retained raw helper has no static entry route recovered |
| `0x00542b8c` | call through `dword ptr unk_69B400` | target consumes Surface callback-table slot 8 |
| `0x0069b400` | four xrefs including target and Surface setup | shared expand callback slot |
| `0x004bed80` | slot-8 RGB555 target | installed expand implementation |
| `0x004c46d0` | slot-8 RGB565 target | installed expand implementation |
| `0x00542bb0` | four xrefs | next modeled DLPalette constructor, proving the post-target boundary is live and separate |

## Documentation Evidence And IDA Status
- Target page: exact range, behavior, `88/91` metadata, no-direct-route caveats, and formal `DLPalette::ExpandMappedColor` C++ are now applied.
- `DLPalette.md`: method table now records UID0004HT as source-ready retained raw DLPalette method with no-direct-caller/original-symbol caveat.
- `Palette.md`: DLPalette contents now list UID0004HT as an emitted DLPalette child under `Palette.cpp`, not a standalone helper file or Surface-owned method.
- UID0001E4 parent: UID0004HT row/status now records source-ready child status while retaining the no-direct-caller caveat.
- Surface callback-table docs: slot 8 now carries descriptive `g_pfnExpandPixel` / `SurfaceExpandPixelProc`, UID0004HT consumer at `0x00542b8c`, and RGB555/RGB565 target ties.
- Generated state: `Palette.cpp` contains UID0004HT code with completion/confidence `88/91`; no UID0004HT empty emitter marker remains.

## Range / Split / Padding / Reclassification Analysis
- Exact body: `0x00542b70-0x00542baf`, 63 bytes.
- Preceding span: `0x00542b63-0x00542b70` is padding after UID0004HS.
- Following span: `0x00542baf-0x00542bb0` is a one-byte alignment before `sub_542BB0`.
- No child split is needed inside UID0004HT: both return sites belong to the same method, and the callback call is a dependency rather than an inline child.
- No reclassification to ignored/padding is supported; bytes are executable method-shaped code with stateful DLPalette behavior.

## Ranked Ownership Analysis

### 1. DLPalette class [UID:00003Z]
- Evidence for: receiver in `ecx`; reads DLPalette mapped-color table at `+0x2c`; neighbors are DLPalette methods; target already belongs to DLPalette inventory.
- Evidence against: no direct caller or original symbol was recovered.
- Decision: direct owner and emitter.

### 2. Palette file umbrella [UID:0000MA]
- Evidence for: established source umbrella for DLPalette implementation and generated `NexusTK/render/Palette.cpp`.
- Evidence against: too broad to be the direct owner of the exact method.
- Decision: source-file umbrella through DLPalette, not direct target owner.

### 3. Surface / SurfaceRenderCallbackTable [UID:0000TN]
- Evidence for: owns slot `0x0069b400` and installed RGB555/RGB565 expand callbacks.
- Evidence against: UID0004HT is a caller/consumer using a DLPalette receiver and table; callback provider ownership does not transfer to the caller.
- Decision: dependency and support-doc update only.

### 4. PaletteLib [UID:0000A1]
- Evidence for: adjacent later Palette-family ranges exist.
- Evidence against: no PaletteLib receiver/state appears in UID0004HT.
- Decision: rejected.

## Source Placement
- Place the emitted method under [UID:00003Z] `DLPalette`, routed into [UID:0000MA] `Palette` / `NexusTK/render/Palette.cpp`.
- Do not create a new standalone helper file.
- Use support alias `g_pfnExpandPixel` for Surface callback slot 8 after updating callback-table docs.
- Preserve caveat text that `DLPalette::ExpandMappedColor` is a descriptive reconstruction name, not a recovered original symbol.

## First-Draft C++ Recommendation
Use this exact formal header/block insertion text for UID0004HT:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void DLPalette::ExpandMappedColor(unsigned int index,
                                  unsigned char *low,
                                  unsigned char *middle,
                                  unsigned char *high)
{
    if (index > 0xff) {
        *high = 0;
        *middle = 0;
        *low = 0;
        return;
    }

    g_pfnExpandPixel(m_mappedColors[index], low, middle, high);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Notes for implementation:
- The fallback write order intentionally mirrors the observed assembly order: third output pointer, second output pointer, first output pointer.
- `g_pfnExpandPixel` must be added or confirmed in Surface callback-table support docs as the descriptive source-facing alias for slot `0x0069b400`.
- The method name is descriptive and should be documented as such.

## Final Recommendation
- Update UID0004HT to source-ready first-draft C++ with score `88/91`.
- Keep `CANONICAL_OWNER:00003Z`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:00003Z`.
- Replace the blank formal C++ block with the exact block above.
- Update target prose to state that no direct xref/caller/original name was recovered, but this is a score cap rather than a code blocker.
- Update support docs so DLPalette and Palette list UID0004HT as an emitted child and Surface callback-table docs name slot 8 as the expand-pixel callback consumed by UID0004HT.

## Recommended Target Doc Changes
- Metadata:
  - Change `COMPLETION` from `85` to `88`.
  - Change `CONFIDENCE` from `88` to `91`.
  - Keep `CANONICAL_OWNER:00003Z`.
  - Keep `RECONSTRUCTABLE:TRUE`.
  - Keep `EMITTER_UIDS:00003Z`.
- Formal C++:
  - Replace the empty formal C++ block with the exact block from `First-Draft C++ Recommendation`.
- Status and reconstruction notes:
  - State that the helper is source-ready despite no direct static entry route.
  - Record that `DLPalette::ExpandMappedColor` is a descriptive reconstructed name.
  - Record that the callback slot should be referenced as `g_pfnExpandPixel` / `SurfaceExpandPixelProc` after support repair.
  - Preserve the negative evidence: no IDA function object, no direct xrefs, no pointer byte hits, no rel32 caller hits, no recovered original symbol.
- Behavior:
  - Document receiver in `ecx`.
  - Document unsigned `index > 0xff` fallback.
  - Document mapped color load from `this + 0x2c + index*2`.
  - Document output-zero write order and callback argument order.
- Changes:
  - Add a B001 2026-07-06 source-quality entry with MCP session `b001-0004HT`, score change, formal C++ addition, and no-direct-caller caveat.

## Recommended Support Doc Changes
- `by-class/DLPalette.md`:
  - Update the method table entry for UID0004HT from blank/no-live-route wording to source-ready retained raw method wording.
  - Record that it reads `m_mappedColors` and consumes Surface slot 8 through `g_pfnExpandPixel`.
  - Keep the caveat that the method name is descriptive, not an original symbol.
  - Required validator after edit: `python .\tools\validator.py --mode file --file by-class\DLPalette.md --apply --queue-timeout 240`.
- `by-file/Palette.md`:
  - Update the DLPalette contents list so UID0004HT is an emitted DLPalette child in `Palette.cpp`.
  - Do not create a new source file or move the helper to Surface.
  - Required validator after edit: `python .\tools\validator.py --mode file --file by-file\Palette.md --apply --queue-timeout 240`.
- `by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md`:
  - Update the UID0004HT row/status from blank raw child to source-ready raw DLPalette child with no-direct-caller caveat.
  - Required validator after edit: `python .\tools\validator.py --mode file --file by-memory\0x00542ac0-0x00543149.DLPaletteMethodCluster.md --apply --queue-timeout 240`.
- `by-global/SurfaceRenderCallbackTable.md`:
  - Add descriptive slot-8 alias `g_pfnExpandPixel` / `SurfaceExpandPixelProc`.
  - Record UID0004HT as the DLPalette consumer at `0x00542b8c`.
  - Tie slot 8 to the existing RGB555/RGB565 expand child implementations at `0x004bed80` and `0x004c46d0`.
  - Do not change score or formal C++ for this global support page as part of UID0004HT implementation.
  - Required validator after edit: `python .\tools\validator.py --mode file --file by-global\SurfaceRenderCallbackTable.md --apply --queue-timeout 240`.
- `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`:
  - Add descriptive slot-8 alias `g_pfnExpandPixel` / `SurfaceExpandPixelProc`.
  - Record UID0004HT as the DLPalette consumer at `0x00542b8c`.
  - Tie slot 8 to the existing RGB555/RGB565 expand child implementations at `0x004bed80` and `0x004c46d0`.
  - No score or formal C++ change is recommended for this table-storage page; it remains storage/support documentation.
  - Required validator after edit: `python .\tools\validator.py --mode file --file by-memory\0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md --apply --queue-timeout 240`.
- Explicit exclusions:
  - `by-memory/0x004bed80-0x004bedaa.SoftwareRenderCompatRgb555ExpandCallback.md` is excluded from required edits because its current formal body already documents the RGB555 slot-8 expand implementation; no UID0004HT-specific reverse-consumer note is needed.
  - `by-memory/0x004c46d0-0x004c46fa.SoftwareRenderRgb565ExpandCallback.md` is excluded from required edits because its current formal body already documents the RGB565 slot-8 expand implementation; no UID0004HT-specific reverse-consumer note is needed.
  - Generated `auto-generated/NexusTK/render/Palette.cpp` is excluded from manual edits; it should be checked for freshness only after scoped validators run during the implementation callback.

## Score And Metadata Recommendation
- Recommended target score: `COMPLETION:88`, `CONFIDENCE:91`.
- Rationale for completion `88`: exact behavior, C++ block, ownership, range, callback dependency, and support update plan are known; original name/caller and finalized shared callback declaration remain unresolved.
- Rationale for confidence `91`: direct byte/disassembly evidence is strong and corroborated by sibling/callback docs; remaining uncertainty is naming/source-declaration polish, not behavior.
- Recommended ownership/emission:
  - `CANONICAL_OWNER:00003Z`
  - `RECONSTRUCTABLE:TRUE`
  - `EMITTER_UIDS:00003Z`
  - blank optional emitter position

## Open Questions With Attempted Resolution
- Original method name: searched target UID, address, DLPalette helper names, callback slot names, generated output, and executed reports. No original symbol found. Use descriptive name and preserve caveat.
- Direct caller/liveness: checked MCP xrefs, VA/RVA pointer bytes, and direct rel32 branches. No static route found. Treat as retained raw method with no-direct-route caveat.
- Callback global declaration style: support docs now carry descriptive `g_pfnExpandPixel` / `SurfaceExpandPixelProc`; the exact final table declaration style remains open.
- Channel names: output byte lanes are kept as `low`, `middle`, and `high` to match existing slot-8 child docs and avoid overclaiming RGB order.
- Constness: not used in the first draft. The body is read-only against DLPalette state, but no source declaration/caller evidence proves a const-qualified method.

## Validator Results

All scoped validators were run from `source-3/project-documentation` only:

| Command ID | Timestamp | Command | Exit | OK | Warnings / notes |
| --- | --- | --- | ---: | ---: | --- |
| `000000007742` | `2026-07-06T20:13:41-04:00` | `python .\tools\validator.py --mode file --file by-memory\0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md --apply --queue-timeout 240` | 0 | 1 | No warnings; completion/confidence updated to `88/91`; autogen registry updated from blank to block; generated refresh reported `deferred`. |
| `000000007743` | `2026-07-06T20:13:47-04:00` | `python .\tools\validator.py --mode file --file by-class\DLPalette.md --apply --queue-timeout 240` | 0 | 1 | `missing_ref_uid: 6` for pre-existing `0003MU`/`0003MT` references; generated refresh reported `deferred`. |
| `000000007744` | `2026-07-06T20:14:00-04:00` | `python .\tools\validator.py --mode file --file by-file\Palette.md --apply --queue-timeout 240` | 0 | 1 | `missing_ref_uid: 19` for pre-existing Palette child UID references including `0003MU`, `0003MT`, `0003MV`, `0003LS`; generated refresh reported `deferred`. |
| `000000007745` | `2026-07-06T20:14:08-04:00` | `python .\tools\validator.py --mode file --file by-memory\0x00542ac0-0x00543149.DLPaletteMethodCluster.md --apply --queue-timeout 240` | 0 | 1 | `missing_ref_uid: 6` for pre-existing `0003MT`/`0003MU` references; generated refresh reported `deferred`. |
| `000000007746` | `2026-07-06T20:14:16-04:00` | `python .\tools\validator.py --mode file --file by-global\SurfaceRenderCallbackTable.md --apply --queue-timeout 240` | 0 | 1 | `missing_ref_uid: 7` for pre-existing `0003ZP`/`0003XE`; reference index added UID0004HT; generated refresh reported `deferred`. |
| `000000007747` | `2026-07-06T20:14:28-04:00` | `python .\tools\validator.py --mode file --file by-memory\0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md --apply --queue-timeout 240` | 0 | 1 | `missing_ref_uid: 8` for pre-existing `0003ZP`/`0003XE`/`0003PN`; reference index added UID0004HT; generated refresh reported `deferred`. |

Generated freshness: `auto-generated/NexusTK/render/Palette.cpp` was not manually edited. Read-only check after the validators showed LastWriteTime `2026-07-06 20:14:08` and UID0004HT emitted as `DLPalette::ExpandMappedColor` with completion/confidence `88/91`; UID0004HT no longer appears only as an empty emitter marker.

Forbidden command confirmation: no `execute_report`, dry-run/probing execute variants, registry lifecycle commands, manual report moves, archive moves, or equivalent lifecycle/archive commands were run.

## Changed Files
- `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`
- `by-class/DLPalette.md`
- `by-file/Palette.md`
- `by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md`
- `by-global/SurfaceRenderCallbackTable.md`
- `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`
- `tools/leaser/Agents/Agent-B001/research/0004HT-DLPaletteExpandMappedColorRaw-source-quality.md`
- `auto-generated/NexusTK/render/Palette.cpp` (validator-generated refresh only; no manual generated edit)
- `project-level/-auto-completion-stats.md` (validator `--apply` projected/stat updates only)
- `tools/validator.ini` (validator `--apply` registry/reference updates only)
- `tools/validator_command_state.json` (validator command-state update only)

No coverage reports, supervisor ledgers, lifecycle/archive files, queue files, manual generated files, manual validator-state files, or MCP/IDA state were edited.

## Implementation Tracking Checklist
- [x] Gate 1 repair only: normalized the Claim And Incorporation Ledger verification states to `proposed`, named exact support paths/exclusions, and replaced broad callback checklist items with file-specific implementation items.
- [x] Gate 1 repair boundary: no by-* docs, generated files, coverage reports, validator state, supervisor ledgers, lifecycle/archive files, queue files, or validators were edited/run during this repair.
- [x] Lease `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`, update `COMPLETION:88`, update `CONFIDENCE:91`, preserve `CANONICAL_OWNER:00003Z`, preserve `RECONSTRUCTABLE:TRUE`, preserve `EMITTER_UIDS:00003Z`, preserve blank optional emitter position, and release the lease after the target edit/validator batch. Proof: lease command succeeded for this file; validator `000000007742` exited 0/ok 1; post-validator unlease attempt returned `Rejected[No active lease]` and `current_leases.md` showed no active leases.
- [x] In `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`, insert exactly the formal `RECONSTRUCTION_CPP CODE` block from this report and preserve the fallback output-zero order, `m_mappedColors[index]`, and `g_pfnExpandPixel(m_mappedColors[index], low, middle, high)` call shape. Proof: target validator `000000007742` exited 0/ok 1.
- [x] In `by-memory/0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md`, preserve stale/negative evidence: no IDA function object, no direct xrefs, no VA/RVA pointer hits, no direct rel32 caller hits, no recovered original symbol, and descriptive-only `DLPalette::ExpandMappedColor` naming. Proof: target Raw Evidence/Ownership/C++ Disposition/Changes sections updated; validator `000000007742` exited 0/ok 1.
- [x] Lease `by-class/DLPalette.md`, update the UID0004HT method table/support prose with source-ready retained raw DLPalette method status, `m_mappedColors` read, Surface slot-8 `g_pfnExpandPixel` dependency, and descriptive-name caveat, then release the lease after validation. Proof: lease command succeeded; validator `000000007743` exited 0/ok 1; no active B001 leases remained afterward.
- [x] Lease `by-file/Palette.md`, update the DLPalette contents list so UID0004HT is an emitted DLPalette child in `Palette.cpp`, explicitly preserving no standalone helper file and no Surface ownership, then release the lease after validation. Proof: lease command succeeded; validator `000000007744` exited 0/ok 1; no active B001 leases remained afterward.
- [x] Lease `by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md`, update the UID0004HT inventory row/status from blank raw child to source-ready raw DLPalette child with no-direct-caller caveat, then release the lease after validation. Proof: lease command succeeded; validator `000000007745` exited 0/ok 1; no active B001 leases remained afterward.
- [x] Lease `by-global/SurfaceRenderCallbackTable.md`, add slot-8 alias `g_pfnExpandPixel` / `SurfaceExpandPixelProc`, UID0004HT consumer at `0x00542b8c`, and RGB555/RGB565 expand target ties to `0x004bed80` / `0x004c46d0`, then release the lease after validation. Proof: lease command succeeded; validator `000000007746` exited 0/ok 1; no active B001 leases remained afterward.
- [x] Lease `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md`, add the same slot-8 alias, UID0004HT consumer, and RGB555/RGB565 expand target ties while preserving storage-page no-C++/no-score-change status, then release the lease after validation. Proof: lease command succeeded; validator `000000007747` exited 0/ok 1; no active B001 leases remained afterward.
- [x] Explicitly exclude `by-memory/0x004bed80-0x004bedaa.SoftwareRenderCompatRgb555ExpandCallback.md` from edits because the current formal body already supports UID0004HT.
- [x] Explicitly exclude `by-memory/0x004c46d0-0x004c46fa.SoftwareRenderRgb565ExpandCallback.md` from edits because the current formal body already supports UID0004HT.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-memory\0x00542b70-0x00542baf.DLPaletteExpandMappedColorRaw.md --apply --queue-timeout 240`; recorded command `000000007742`, timestamp `2026-07-06T20:13:41-04:00`, exit 0, ok 1.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-class\DLPalette.md --apply --queue-timeout 240`; recorded command `000000007743`, timestamp `2026-07-06T20:13:47-04:00`, exit 0, ok 1.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-file\Palette.md --apply --queue-timeout 240`; recorded command `000000007744`, timestamp `2026-07-06T20:14:00-04:00`, exit 0, ok 1.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-memory\0x00542ac0-0x00543149.DLPaletteMethodCluster.md --apply --queue-timeout 240`; recorded command `000000007745`, timestamp `2026-07-06T20:14:08-04:00`, exit 0, ok 1.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-global\SurfaceRenderCallbackTable.md --apply --queue-timeout 240`; recorded command `000000007746`, timestamp `2026-07-06T20:14:16-04:00`, exit 0, ok 1.
- [x] Run from `source-3/project-documentation`: `python .\tools\validator.py --mode file --file by-memory\0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md --apply --queue-timeout 240`; recorded command `000000007747`, timestamp `2026-07-06T20:14:28-04:00`, exit 0, ok 1.
- [x] After the final scoped validator, check generated `auto-generated/NexusTK/render/Palette.cpp` freshness by confirming UID0004HT no longer appears only as an empty emitter marker; do not edit generated files manually. Proof: read-only check found UID0004HT body at file lines 7-21, completion/confidence `88/91`, `g_pfnExpandPixel` call, and LastWriteTime `2026-07-06 20:14:08`.
- [x] After implementation, update this report's Claim And Incorporation Ledger verification states from `proposed` to `applied` with concrete path/section/validator proof for each claim.
- [x] After implementation, update this checklist with completed file-specific proof, lease/release status, validator metadata, generated freshness result, and explicit exclusions.
- [x] Confirm forbidden actions remained unused: no `execute_report`, no dry-run/probing execute variant, no registry lifecycle command, no manual report move, no archive move, no manual generated edit, no coverage edit, no supervisor-ledger edit, and no queue/lifecycle header/footer edit.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B001","command_id":"000000007755","destination_path":"executed-b-agent-research/B001/0004HT-DLPaletteExpandMappedColorRaw-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B001/research/0004HT-DLPaletteExpandMappedColorRaw-source-quality.md","timestamp":"2026-07-06T20:22:31-04:00","uid":"0004HT"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
