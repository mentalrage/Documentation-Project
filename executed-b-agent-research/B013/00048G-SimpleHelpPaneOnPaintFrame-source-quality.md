** TARGET-REPORT-UID:00048G **
** AUTHOR-AGENT-ID:B013 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | Medium **

# [UID:00048G] SimpleHelpPaneOnPaintFrame Source-Quality Report

Report path: `tools/leaser/Agents/Agent-B013/research/00048G-SimpleHelpPaneOnPaintFrame-source-quality.md`  
Target: `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md`  
Agent: `Agent-B013`  
Mode: implementation callback applied; awaiting supervisor Gate 2 review.

## Finalized Report / Current Recommendation

UID00048G remains an exact reconstructable method child for `SimpleHelpPane::OnPaintFrame()` under [UID:0000D6] `SimpleHelpPane`, routed through [UID:0000JU] `HelpPanes`. The implementation callback has replaced the target formal C++ with the corrected source-shaped body from this report.

Current target metadata is now `COMPLETION:89`, `CONFIDENCE:91`, `CANONICAL_OWNER:0000D6`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000D6`, with `EMITTER_POSITION_OPTIONAL:` blank.

The reason remains narrow: live MCP session `73c77998` confirms the exact method body, vtable-only reachability, padding, and helper sequence. The binary computes `wcslen(m_textBuffer)` before the loop, treats only CR/LF/tab as in-loop delimiters, does not special-case CRLF as one newline, and draws the final trailing segment after the loop. The pre-callback formal/generated C++ treated `NUL` as an in-loop delimiter and coalesced CRLF; that stale first-draft shape has been historicalized in the target/support docs.

## Supporting Research

This is the current post-callback artifact. The supervisor issued an implementation callback after Gate 1, and Agent-B013 applied the accepted details to the target/support by-* docs, ran scoped validators, released leases, and updated this ledger/checklist. Historical report-only facts remain below only where explicitly describing the original research pass.

Required local context was checked before finalizing: the assigned `goal.md`, `notes.md`, project B-agent workflow, `by-structure.md` MCP/code-entry rules, target page, `by-class/SimpleHelpPane.md`, `by-file/HelpPanes.md`, parent split index `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md`, constructor child `by-memory/0x004c6f90-0x004c7287.SimpleHelpPaneConstructor.md`, singleton storage child `by-memory/0x0069ae00-0x0069ae04.g_pSimpleHelpPane.md`, generated `auto-generated/NexusTK/ui/controls/HelpPanes.cpp`, generated by-memory coverage row, and matching historical executed split report `executed-b-agent-research/B007/00016S-SimpleHelpPanes-source-quality.md`.

MCP was rechecked after supervisor resume. Fresh `idb_list` returned active NexusTK IDB session `73c77998`, and `server_health` reported `status: ok`, `auto_analysis_ready: true`, `hexrays_ready: true`, and `strings_cache_ready: true`.

## Target

- UID: `00048G`.
- Target path: `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md`.
- Current target classification: reconstructable exact `SimpleHelpPane::OnPaintFrame` child, owner/emitter [UID:0000D6] `SimpleHelpPane`.
- Current score: `89/91`.
- Recommended score: implemented at `89/91`.
- Current source family: [UID:0000JU] `HelpPanes`, `NexusTK/ui/controls/HelpPanes.cpp`.
- Current formal C++ state: emits the corrected `SimpleHelpPane::OnPaintFrame()` body from this report inside the target's formal block.
- Current generated state: `auto-generated/NexusTK/ui/controls/HelpPanes.cpp` refreshed through scoped validators and now contains [UID:00048G] at `89/91` with the corrected CR/LF/tab loop and final trailing draw. Its header reports `validator-command-id: 000000006160`, `validator-refreshed-at: 2026-07-04T05:57:51-04:00`.

## Current Target State

The target page now has the accepted post-callback disposition: exact range `0x004c7310-0x004c743e`, owner/emitter [UID:0000D6], parent split index [UID:00016S], score `89/91`, and corrected formal `SimpleHelpPane::OnPaintFrame()` C++. It says the method draws a popup frame/background/text and walks copied UTF-16 lines from `m_textBuffer` at `+0xf8`.

The resolved pre-callback blockers were target-specific source-quality blockers:

- Pre-callback formal C++ used `GetBounds`, `DrawHelpPaneFrame`, and `DrawTextRun(x, y, ...)` abstractions that hid the emitted helper sequence; the target prose now documents the observed fill/frame/text/cursor helper roles.
- Pre-callback formal C++ checked `*scan == L'\0'` inside the line-break test. Live MCP shows `wcslen` is computed first and the scan loop uses `index < textLength`, so the terminator is not an in-loop delimiter.
- Pre-callback formal C++ collapsed CRLF by incrementing past `\n` after `\r`. Live disassembly shows no such coalescing; every CR, LF, or tab encountered in the `0 <= index < length` loop is an independent delimiter.
- Pre-callback formal C++ drew at `bounds.left + margin` and started `drawY = bounds.top + margin`. The implemented body uses local text cursor coordinates: `sub_4B7C30(&point, lineHeight + 5, 5)` then `sub_4B9600(this, x=5, y=drawY)` before each draw. The bounds copy from `this+0x44` is used for frame/background helpers, not as the text origin.

## Heuristic / Inference Reanalysis And Validation

Ownership and emitter route are strong and should not change. The target is a single modeled function with one method-sized range, one data/vtable xref, and an exact owner path already established by the `SimpleHelpPane` split. There is no evidence that this is a free helper, compiler artifact, or mixed aggregate.

The main reanalysis is source-shape, not ownership. The emitted method has a conventional hand-authored UI paint shape:

- copy current pane bounds from `this + 0x44`;
- set a draw/fill state to `1`;
- call global surface/presentation callback pointer `0x0069b3fc` with the copied bounds;
- set draw state/color to `0x80` and call the frame helper `0x004ba450`;
- set text color/state to `0x8f`;
- compute a local text cursor `(x=5, y=GetTextLineHeight()+5)`;
- scan copied text at `this + 0xf8` using a precomputed wide length;
- split only on CR, LF, and tab;
- draw the segment preceding each delimiter, advance one line by `lineHeight + 1`, and set the next segment start to `index + 1`;
- after the loop, draw the final segment from the last start index through `textLength`.

This supports a direct method body with inferred source-facing helper names such as `SetDrawColor`, `FillPaneRect`, `DrawRectFrame`, `SetTextColor`, `MoveTo`, `DrawTextRun`, and `GetTextLineHeight`. Those names are inferred wrappers over shared pane/GrafPort helpers and should be documented as such. It does not support creating new target-local helper functions or retaining the stale NUL/CRLF loop.

## Evidence Standards Used

- Current by-* docs and generated output were used as leads, not as authority for final behavior.
- IDA MCP session `73c77998` was used as the authoritative evidence source for function boundaries, padding, xrefs, decompile/disassembly shape, helper roles, and relevant bytes.
- MCP calls were narrow and paged: `idb_list`, `server_health`, exact-address `lookup_funcs`, target `analyze_function`, paged `disasm`, bounded `get_bytes`, exact-address `xrefs_to`, targeted helper `decompile`, and targeted `int_convert`.
- Numeric conversions recorded below are from MCP `int_convert`, not manual arithmetic.
- Historical executed report `B007/00016S-SimpleHelpPanes-source-quality.md` was used only as a prior split/creation lead and checked against current target evidence.

## Evidence Checked

- `goal.md`: confirmed UID00048G, target path, report path, Medium provenance, and original report-only boundary before supervisor callback.
- `by-structure.md`: confirms IDA MCP Output Discipline and formal C++ gate/range ownership rules.
- Target page `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md`: pre-callback `86/88` with stale formal C++ loop; post-callback `89/91` with corrected formal C++ and owner/emitter [UID:0000D6].
- `by-class/SimpleHelpPane.md`: class route lists UID00048G as exact `OnPaintFrame` child and records copied text buffer field `+0xf8`.
- `by-file/HelpPanes.md`: source-family route lists UID00048G as exact child under the HelpPanes family.
- Parent `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md`: non-emitting split index lists UID00048G range and padding around it.
- Constructor child `by-memory/0x004c6f90-0x004c7287.SimpleHelpPaneConstructor.md`: constructor allocates/copies wide text into `m_textBuffer` at `+0xf8`.
- Singleton storage child `by-memory/0x0069ae00-0x0069ae04.g_pSimpleHelpPane.md`: confirms lifecycle and broader class ownership context.
- Generated `auto-generated/NexusTK/ui/controls/HelpPanes.cpp`: pre-callback generated [UID:00048G] body matched the stale target C++; post-callback validator refresh now shows UID00048G at `89/91` with the corrected body and header `validator-command-id: 000000006160`.
- Generated `auto-generated/-ag-coverage-report-by-memory.md`: pre-callback UID00048G row showed `emits_code:true`, `reconstructable`, `86%`, strong; validator-owned refresh updated generated coverage after target validation.
- `rg` search terms included `00048G`, `SimpleHelpPaneOnPaintFrame`, `0x004c7310`, `004c743e`, `SimpleHelpPane`, and `OnPaintFrame`.
- MCP `idb_list`: active `73c77998` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
- MCP `server_health`: `status: ok`, module `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready.
- MCP `lookup_funcs`: `0x004c7310 -> sub_4C7310`, size `0x12e`; `0x12e == 302` (Verified with MCP `int_convert`); `0x004c743e` is not a function; successor `0x004c7440 -> sub_4C7440`, size `0x10`.
- MCP `get_bytes`: nine `0xcc` bytes at `0x004c7307-0x004c7310`, two `0xcc` bytes at `0x004c743e-0x004c7440`, and target prologue bytes beginning `55 8b ec 83 ec 20`.
- MCP `xrefs_to 0x004c7310`: exactly one data xref at `0x0061ac28`; `0x0061ac28 == 6401064` (Verified with MCP `int_convert`).
- MCP `get_bytes 0x0061ac20 size 16`: contains little-endian `0x004c7310` at `0x0061ac28`, in the vtable neighborhood.
- MCP `analyze_function 0x004c7310`: no code callers, one data xref, callees `0x004bab70`, `0x0069b3fc`, security cookie, `0x004ba450`, `0x004b9680`, `0x004b7c30`, `0x004b9660`, `0x004b9600`, and `0x004baaa0`.
- MCP target disassembly: `movups xmm0, [ebx+44h]`, calls `0x004b9660` with `1`, callback through `0x0069b3fc`, calls `0x004b9660` with `0x80`, frame helper `0x004ba450`, text state `0x8f`, line-height helper `0x004baaa0`, point setup `0x004b7c30`, copied text load `[ebx+0xf8]`, delimiter compares `0x0d`, `0x0a`, and `0x09`, final trailing draw after loop, and stack-cookie epilogue.
- MCP helper decompiles: `0x004b7c30` initializes a two-int point, `0x004b9600` sets cursor fields, `0x004b9660` sets draw state/color field, `0x004b9680` sets text state/color field, `0x004baaa0` returns a font/text line-height-like metric, and `0x004bab70` draws a counted UTF-16 run through shared text/GrafPort helpers.
- MCP `int_convert`: `0xf8 == 248`, `0x44 == 68`, `0x8f == 143`, `0x80 == 128`, `0x0d == 13`, `0x0a == 10`, and `0x09 == 9` (Verified with MCP `int_convert`).

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C-00048G-01 | UID00048G is exactly `sub_4C7310`, range `0x004c7310-0x004c743e`, size `0x12e` / 302 bytes. | 0.99 | MCP `lookup_funcs`, `analyze_function`, `disasm`; `int_convert 0x12e`. | Target Scope/Evidence | incorporate | applied |
| C-00048G-02 | Neighboring bytes are padding/function boundaries: `0x004c7307-0x004c7310` nine `0xcc`, `0x004c743e-0x004c7440` two `0xcc`, successor `0x004c7440` starts `sub_4C7440`. | 0.99 | MCP `lookup_funcs` and `get_bytes`. | Target Scope/Evidence and parent child row if stale | incorporate | applied |
| C-00048G-03 | The only xref to the method start is data/vtable xref `0x0061ac28`; there are no direct code callers. | 0.98 | MCP `xrefs_to 0x004c7310`; `analyze_function` callers empty; vtable bytes contain `10 73 4c 00`. | Target Evidence and class method note | incorporate | applied |
| C-00048G-04 | Direct owner/emitter remains [UID:0000D6] `SimpleHelpPane`; source family remains [UID:0000JU] `HelpPanes`; parent [UID:00016S] remains a non-emitting split index. | 0.97 | Target/class/file/parent docs plus vtable/singleton/constructor context; no competing owner evidence. | Target metadata, SimpleHelpPane, HelpPanes, parent split index | already-present | applied |
| C-00048G-05 | The copied text field consumed by paint is `this + 0xf8` / 248, source-facing `m_textBuffer` or `m_copiedTextBuffer`. | 0.97 | MCP target decompile/disasm load `[ebx+0xf8]`; constructor child documents allocation/copy into same field; `int_convert 0xf8`. | Target Evidence/Source-Quality Notes; class method inventory if stale | incorporate | applied |
| C-00048G-06 | Paint copies bounds from `this + 0x44` / 68 for fill/frame operations, but text cursor coordinates are local `(5, lineHeight + 5)` rather than `bounds.left/top + 5`. | 0.94 | MCP disasm `movups [ebx+44h]`, `0x004b7c30(&point, lineHeight+5, 5)`, `0x004b9600(this, x, y)` helper decompile. | Target Formal C++ and Evidence | incorporate | applied |
| C-00048G-07 | The method sets fill/draw state `1`, calls global callback `0x0069b3fc`, sets state `0x80`, draws frame via `0x004ba450`, then sets text state `0x8f`. | 0.93 | MCP disasm/callee list/helper decompiles; `int_convert` for `0x80` and `0x8f`. | Target Evidence and C++ helper-role prose | incorporate | applied |
| C-00048G-08 | The binary computes wide text length before scanning and scans `index < textLength`; `NUL` is not an in-loop delimiter. | 0.98 | MCP decompile `v4 = wcslen(v3)` then `for (v5 < v4)`; disasm wide-length loop before delimiter loop. | Target Formal C++ and stale-generated historical note | reject-stale | applied |
| C-00048G-09 | In-loop delimiters are CR, LF, and tab only; no CRLF coalescing is present in the target body. | 0.98 | MCP disasm compares `0x0d`, `0x0a`, `0x09`; branch sets `lineStart = index + 1`; no lookahead. | Target Formal C++ and Evidence | incorporate | applied |
| C-00048G-10 | After each delimiter draw, the cursor advances by `GetTextLineHeight() + 1`; after the loop the final trailing segment is always drawn. | 0.96 | MCP decompile/disasm: call `0x004baaa0`, `inc`, add to previous Y; tail at `0x004c740b-0x004c7428` draws final segment. | Target Formal C++ and Evidence | incorporate | applied |
| C-00048G-11 | Shared helper names are inferred source-facing wrappers, not symbol-proven names; do not introduce new target-local emitted helper functions. | 0.90 | Helper callee xrefs are broad shared pane/GrafPort helpers, not unique local functions; target body has only one observed method body. | Target Source-Quality Notes and C++ caveat | incorporate | applied |
| C-00048G-12 | Recommended score is `89/91`, not 95+, because behavior/range/owner are strongly proven but final exact helper/member names remain inferred. | 0.91 | Current MCP proof plus unresolved original helper spelling. | Target metadata and Score section | incorporate | applied |

## Positive Evidence Summary

- Exact range and padding are proven by live MCP boundaries and bytes.
- The method is a real source-authored virtual paint override, reached through the `SimpleHelpPane` vtable slot at `0x0061ac28`.
- The owner route is coherent with the constructor/destructor/singleton docs: constructor writes `m_textBuffer` at `+0xf8`, destructor frees it, and paint consumes it.
- The body has a small stable UI paint shape that is suitable for formal C++: frame/background setup, local text cursor setup, delimiter scan, counted text draw, and final segment draw.
- Current support docs already place this child in the correct `SimpleHelpPane` / `HelpPanes` family, so no split or owner migration is needed.

## Negative Evidence Summary

- There are no direct code callers; reachability is vtable/data only. This is expected for a virtual paint method, but it should stay documented as a reachability caveat.
- No current MCP evidence proves exact original helper spellings for `SetDrawColor`, `FillPaneRect`, `DrawRectFrame`, `SetTextColor`, `MoveTo`, `DrawTextRun`, or `GetTextLineHeight`; names should remain source-facing inferred wrappers.
- The current formal/generated C++ includes two unsupported source-shape details: NUL as an in-loop delimiter and CRLF coalescing.
- `0x0069b3fc` is a callback pointer/global, not a function body, and must not be claimed as a target-owned helper.
- Shared pane/GrafPort helpers have many callers; UID00048G must not introduce new namespace-local helpers or claim ownership of those helpers.

## Ranked Ownership Analysis

| Rank | Candidate | Evidence For | Evidence Against | Decision |
| --- | --- | --- | --- | --- |
| 1 | [UID:0000D6] `SimpleHelpPane` | Existing target owner/emitter, vtable-only xref at `0x0061ac28`, constructor/destructor/singleton context, method name in class docs, copied text field lifecycle. | Exact original helper/member names remain inferred. | Keep as direct owner/emitter. |
| 2 | [UID:0000JU] `HelpPanes` source family | File page owns the contiguous help/tooltip source family and routes `SimpleHelpPane` children. | It is a source-family route, not the direct class owner of this method body. | Keep as source file/support owner. |
| 3 | [UID:00016S] `SimpleHelpPanes` parent | Parent split index covers the contiguous cluster and records UID00048G. | Parent is non-emitting and spans multiple methods/classes/padding. | Keep as parent index only. |
| 4 | [UID:000063] `HelpPane` / GrafPort helpers | Paint body uses inherited/shared pane drawing helpers. | This override consumes inherited helpers but is reached through `SimpleHelpPane` vtable and field `+0xf8`. | Reject as direct owner. |
| 5 | Global callback `0x0069b3fc` / surface render owner | Paint calls through this callback. | Dependency only; not a function body and not owner of line parsing/text buffer. | Reject as owner. |
| 6 | No-owner/non-emitting | No code callers and helper names are inferred. | Exact method body is modeled, vtable reachable, reconstructable, and already has valid emitter route. | Reject. |

## Source Placement

Keep source placement under `NexusTK/ui/controls/HelpPanes.cpp` through [UID:0000JU] `HelpPanes`. This matches the current generated route, class/file docs, parent split index, singleton lifecycle, and adjacent `SimpleHelpPane` method children.

Do not create a new `SimpleHelpPaneOnPaintFrame.cpp`, do not move the body to a GrafPort/surface file, and do not fold this child back into the non-emitting parent [UID:00016S]. If a future broader source-layout audit splits `SimpleHelpPane` into its own file, the whole `SimpleHelpPane` family should move together, not only this paint method.

## First-Draft C++ Recommendation

The exact formal `RECONSTRUCTION_CPP CODE` insertion text for UID00048G is:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SimpleHelpPane::OnPaintFrame()
{
    RectBounds bounds = m_bounds;

    SetDrawColor(1);
    FillPaneRect(bounds);

    SetDrawColor(0x80);
    DrawRectFrame(bounds);

    SetTextColor(0x8f);

    const int margin = 5;
    const wchar_t *text = m_textBuffer;
    const int textLength = static_cast<int>(wcslen(text));
    const int drawX = margin;
    int drawY = GetTextLineHeight() + margin;
    int lineStart = 0;

    for (int index = 0; index < textLength; ++index)
    {
        const wchar_t ch = text[index];
        if (ch == L'\r' || ch == L'\n' || ch == L'\t')
        {
            MoveTo(drawX, drawY);
            DrawTextRun(text + lineStart, index - lineStart);

            drawY += GetTextLineHeight() + 1;
            lineStart = index + 1;
        }
    }

    MoveTo(drawX, drawY);
    DrawTextRun(text + lineStart, textLength - lineStart);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

C++ caveats to preserve in prose:

- `RectBounds`, `m_bounds`, `SetDrawColor`, `FillPaneRect`, `DrawRectFrame`, `SetTextColor`, `MoveTo`, `DrawTextRun`, and `GetTextLineHeight` are source-facing inferred names for observed fields/helpers. They are not proven original spellings.
- `0x80` and `0x8f` are observed numeric drawing/text-state values. Do not silently rename them to final enum names unless a support page proves those names.
- The formal block intentionally does not check `L'\0'` inside the loop and intentionally does not coalesce CRLF.
- The formal block intentionally emits no new helper function definitions; all helper names are calls to existing shared pane/GrafPort methods or globals.

## Final Recommendation

Implementation callback is complete for UID00048G. The target and directly stale support rows/prose were updated, the formal body was replaced with the corrected one above, the target was raised to `89/91`, and the exact MCP-backed proof was preserved.

No split, new child, owner migration, global ownership change, or no-code reclassification is recommended.

## Recommended Target Doc Changes

Target file: `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md`.

- Applied: `COMPLETION:89`.
- Applied: `CONFIDENCE:91`.
- Preserved: `CANONICAL_OWNER:0000D6`.
- Preserved: `RECONSTRUCTABLE:TRUE`.
- Preserved: `EMITTER_UIDS:0000D6`.
- Preserved: `EMITTER_POSITION_OPTIONAL:` blank.
- Applied: the report now supplies the full formal `RECONSTRUCTION_CPP CODE` header/block insertion text, and the target page contains the accepted body inside its existing formal block.
- Applied: no duplicate sentinel/header lines were pasted into the target block during implementation.
- Applied: `Item Summary` and prose preserve session `73c77998`, exact range/size, no-function end `0x004c743e`, padding before/after, vtable-only xref at `0x0061ac28`, copied text field `+0xf8`, bounds field `+0x44`, helper sequence, local cursor origin, CR/LF/tab-only delimiter loop, final trailing draw, stale old C++ correction, and inferred helper-name cap.
- Applied: the old `NUL` delimiter and CRLF coalescing are historicalized as stale first-draft/generated shape, not current binary truth.

## Recommended Support Doc Changes

Support docs were touched only where current wording was stale or too shallow after target correction.

- Applied in `by-class/SimpleHelpPane.md`: UID00048G method row/evidence notes now record `89/91`, local text cursor `(5, lineHeight+5)`, CR/LF/tab delimiters, and final trailing segment draw from `m_textBuffer`.
- Applied in `by-file/HelpPanes.md`: proposed contents/data caveat now says the exact child is formal-C++ ready at `89/91`, with corrected byte-backed text-loop behavior and no helper ownership migration.
- Applied in `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md`: only the UID00048G row/evidence/source-quality notes were updated, preserving the parent as a non-emitting split index.
- Excluded with reason: no manual changes were made to `by-memory/-coverage-report.md` or generated reports. Generated `HelpPanes.cpp` and generated reports refreshed through scoped validator execution.

## Score And Metadata Recommendation

| Field | Current | Recommended | Rationale |
| --- | --- | --- | --- |
| `COMPLETION` | `86` | `89` | The exact method range, padding, vtable xref, helper sequence, text field, delimiter behavior, final draw, and corrected formal C++ are now documented. Completion stays below 95 because final helper/member spellings remain inferred. |
| `CONFIDENCE` | `88` | `91` | Live MCP session `73c77998` independently reconfirmed boundaries, bytes, decompile/disassembly, xrefs, and helper roles. Confidence remains below final audit because the source-facing helper names are not symbol-proven. |
| `CANONICAL_OWNER` | `0000D6` | `0000D6` | Direct class owner remains `SimpleHelpPane`. |
| `RECONSTRUCTABLE` | `TRUE` | `TRUE` | This is source-authored UI paint code. |
| `EMITTER_UIDS` | `0000D6` | `0000D6` | Existing emitter route is valid and generated output already emits this child. |
| `EMITTER_POSITION_OPTIONAL` | blank | blank | No custom placement needed. |

## Open Questions With Attempted Resolution

- Exact helper names: unresolved original spellings for draw-state, fill, frame, text-state, cursor, text-run, and line-height helpers. Resolution: use source-facing inferred names in C++ and document the cap; do not leave raw `sub_` names in formal code.
- Exact enum/color names for `1`, `0x80`, and `0x8f`: unresolved. Resolution: keep numeric constants in first-draft C++ and document observed roles.
- Text coordinate origin: resolved. MCP helper decompile for `0x004b7c30` and `0x004b9600` shows a local cursor point, not `bounds.left/top + margin`.
- Delimiter semantics: resolved. MCP disassembly/decompile show CR/LF/tab only inside a length-bounded loop, with final trailing draw after loop and no CRLF coalescing.
- Owner/source placement: resolved. Keep `SimpleHelpPane` / `HelpPanes`; no competing owner evidence remains.

## Validator Results

Scoped validators were run from `source-3/project-documentation` for every changed by-* doc:

| File | Command | command_id | command_timestamp | Exit | ok | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md` | `python .\tools\validator.py --mode file --file by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md --apply --queue-timeout 240 --wait-generated` | `000000006160` | `2026-07-04T05:57:51-04:00` | 0 | 1 | Final target validation after the post-validator prose wording fix; generated refresh completed and `HelpPanes.cpp` header advanced to this command. Warnings were unrelated existing `autogen_children_*` / `autogen_emitter_has_no_code` rows plus validator-owned refreshes influenced by concurrent agents. Earlier target validation `000000006143` also exited 0/ok 1 before the final prose fix. |
| `by-class/SimpleHelpPane.md` | `python .\tools\validator.py --mode file --file by-class/SimpleHelpPane.md --apply --queue-timeout 240` | `000000006145` | `2026-07-04T05:54:06-04:00` | 0 | 1 | Reported existing `missing_ref_uid 0003YZ` warnings; generated refresh deferred and refreshed `HelpPanes.cpp` header to this command. |
| `by-file/HelpPanes.md` | `python .\tools\validator.py --mode file --file by-file/HelpPanes.md --apply --queue-timeout 240` | `000000006146` | `2026-07-04T05:54:09-04:00` | 0 | 1 | Reported existing missing reference warnings for historical helper UIDs; generated refresh deferred. |
| `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md` | `python .\tools\validator.py --mode file --file by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md --apply --queue-timeout 240` | `000000006148` | `2026-07-04T05:54:17-04:00` | 0 | 1 | No target-specific warnings; generated refresh deferred. |

Generated freshness check: `auto-generated/NexusTK/ui/controls/HelpPanes.cpp` currently reports `validator-command-id: 000000006160`, `validator-refreshed-at: 2026-07-04T05:57:51-04:00`, `validator-refresh-source: foreground-generated-refresh`, and contains UID00048G at `Completion:89 | Confidence:91` with the corrected CR/LF/tab loop and final trailing `DrawTextRun`.

## Changed Files

Files changed by Agent-B013 during the callback:

- `tools/leaser/Agents/Agent-B013/research/00048G-SimpleHelpPaneOnPaintFrame-source-quality.md`
- `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md`
- `by-class/SimpleHelpPane.md`
- `by-file/HelpPanes.md`
- `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md`

Validator-owned generated side effects from scoped validation were observed and not manually edited: `auto-generated/NexusTK/ui/controls/HelpPanes.cpp`, generated coverage/tracker reports, `project-level/-auto-completion-stats.md`, and `tools/validator.ini`. No `execute_report`, lifecycle/archive command, manual report move, manual coverage edit, or supervisor-ledger edit was run.

## Implementation Tracking Checklist

- [x] Target metadata: `by-memory/0x004c7310-0x004c743e.SimpleHelpPaneOnPaintFrame.md` is now `COMPLETION:89`, `CONFIDENCE:91`, with `CANONICAL_OWNER:0000D6`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000D6`, and blank `EMITTER_POSITION_OPTIONAL:`.
- [x] Target C++: the report now provides the exact formal `RECONSTRUCTION_CPP CODE` header/block insertion text, and the target page contains the exact `SimpleHelpPane::OnPaintFrame` body inside its existing formal block; no duplicate sentinel/header lines or extra helper functions were added to the target page.
- [x] Target evidence/prose: incorporated session `73c77998` proof for range `0x004c7310-0x004c743e`, size `0x12e` / 302 bytes, no function at `0x004c743e`, successor `0x004c7440`, padding bytes, vtable xref `0x0061ac28`, no code callers, helper sequence, copied text field `+0xf8`, bounds field `+0x44`, delimiter loop, final trailing draw, and helper-name inference cap.
- [x] Target stale-code historicalization: prior NUL-delimiter and CRLF-coalescing C++ is marked stale first-draft/generated shape, not observed target behavior.
- [x] Support `by-class/SimpleHelpPane.md`: updated method row/evidence notes, preserved owner route, and noted corrected UID00048G text-loop behavior.
- [x] Support `by-file/HelpPanes.md`: updated stale row/caveat, preserved `NexusTK/ui/controls/HelpPanes.cpp` route and no helper ownership migration.
- [x] Support parent `by-memory/0x004c6f90-0x004c7680.SimpleHelpPanes.md`: updated only UID00048G child row/evidence/source-quality notes; parent remains non-emitting.
- [x] Manual coverage/generated boundary: no manual coverage reports or generated files were edited. Generated `HelpPanes.cpp` refreshed through scoped validator execution.
- [x] Scoped validators: ran validators for all four changed by-* docs; target validator used `--wait-generated`. Command ids/timestamps/exit/ok are recorded in `Validator Results`.
- [x] Report ledger/checklist: Claim And Incorporation Ledger verification states were updated row by row to `applied`; validator command id/timestamp/exit/ok and generated freshness are recorded.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B013","command_id":"000000006210","destination_path":"executed-b-agent-research/B013/00048G-SimpleHelpPaneOnPaintFrame-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B013/research/00048G-SimpleHelpPaneOnPaintFrame-source-quality.md","timestamp":"2026-07-04T11:22:21-04:00","uid":"00048G"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
