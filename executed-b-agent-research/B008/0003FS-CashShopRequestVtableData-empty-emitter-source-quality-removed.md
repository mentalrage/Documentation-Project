<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B008\0003FS-CashShopRequestVtableData-empty-emitter-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable Code Archive

- Source report: [0003FS-CashShopRequestVtableData-empty-emitter-source-quality.md](0003FS-CashShopRequestVtableData-empty-emitter-source-quality.md)
- Source report SHA256 before cleanup: `7DAA9A3818C46246F98F63142B07E1F5225FDC36B7DAFF85C98086F20747FC13`
- Cleanup generated: `2026-08-11T12:47:13Z`
- Removed executable blocks: `5`
- Scope: exact executable or command-shaped fenced blocks removed from the source report by supervisor housekeeping.
- Authority: this file is not a B-agent research report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: do not execute, import, evaluate, or use any block in this archive as an operational instruction.

## Removed Block R001

- Original source lines: `632-634`
- Original language: `powershell`
- Block SHA256: `3F97F4C4D8B8A6350DFC1E86E94E6E6B3CF9CBC89CED693C096DB0CCDCF3ECF4`

```powershell
python .\tools\validator.py --mode file --file by-memory\0x0041a5d0-0x0041a607.CashShopRequestConstructorRaw.md --apply --queue-timeout 240 --no-generated-refresh
```

## Removed Block R002

- Original source lines: `656-658`
- Original language: `powershell`
- Block SHA256: `1ED8EC27A2D79C99043473F3485ED31A5C4FB94F6C4DC9AF9DC2CF03CECB7D5C`

```powershell
python .\tools\validator.py --mode file --file by-memory\-coverage-report.md --apply --queue-timeout 240 --no-generated-refresh
```

## Removed Block R003

- Original source lines: `698-719`
- Original language: `powershell`
- Block SHA256: `E461A561F0ED2D7866AA6345DAD305272B30080A2A3AE0DC71BF7AEB0E60E15B`

```powershell
python .\tools\validator.py --mode file --file by-class\BaseRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0060d79c-0x0060d7a4.CashShopRequestVtableData.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\CashShopRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\CashShopVersionRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\FileDownloader.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-file\CashShopRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-file\CashShopVersionRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-file\FileDownloader.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0060d794-0x0060d7e0.CashShopDownloaderVtables.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0060d794-0x0060e174.CashShopFittingRoomReadOnlyData.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0060d794-0x0060d79c.CashShopVersionRequestVtableData.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0041a5d0-0x0041a607.CashShopRequestConstructorRaw.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0041a610-0x0041a662.CashShopRequestNonDeletingDestructorRaw.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0041b570-0x0041b5db.CashShopRequestScalarDeletingDestructor.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0041b5e0-0x0041b604.CashShopVersionRequestScalarDeletingDestructor.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\CashShopRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\CashShopVersionRequest.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-file\FileDownloader.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0060d7a4-0x0060d7e0.FileDownloaderVtableData.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-memory\0x0041b5e0-0x0041b604.CashShopVersionRequestScalarDeletingDestructor.md --apply --queue-timeout 240 --no-generated-refresh
```

## Removed Block R004

- Original source lines: `758-762`
- Original language: `powershell`
- Block SHA256: `D6018A0B11B460CFD5CFDE81FABB45FBBEDF49656030AAE128088E76A21FE9DA`

```powershell
python .\tools\validator.py --mode file --file by-memory\-coverage-report.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-class\-coverage-report.md --apply --queue-timeout 240 --no-generated-refresh
python .\tools\validator.py --mode file --file by-file\-coverage-report.md --apply --queue-timeout 240 --no-generated-refresh
```

## Removed Block R005

- Original source lines: `768-770`
- Original language: `powershell`
- Block SHA256: `CF21B477184E4BD7415E7C94883624401CDF218039342AF48787606E87B1D7F2`

```powershell
python .\tools\validator.py --mode file --file by-memory\0x0060d79c-0x0060d7a4.CashShopRequestVtableData.md --apply --queue-timeout 240 --wait-generated
```

## Removed operational snapshot from 1DF44597395CE0F9069CF6935C48FCB40500599B1388D61EE9D1944A908B6F59

> Source: [0003FS-CashShopRequestVtableData-empty-emitter-source-quality.md](./0003FS-CashShopRequestVtableData-empty-emitter-source-quality.md)
> NON-AUTHORITATIVE ARCHIVE. Retained only for evidentiary no-loss review.
> NEVER EXECUTE, ADAPT, OR TREAT THE CONTENT BELOW AS A CURRENT REQUEST, PROCEDURE, MUTATION AUTHORITY, OR LIFECYCLE INSTRUCTION.

`````text
## IDA Rename / Type / Comment Recommendations

All actions are supervisor-only Gate 2B transactions. B008 performed no IDA query, mutation, or save in this reconciliation. The literal A01-A08/P01-P11 prestate below was restored exactly by the earlier unsaved rollback cycles: first by `b008-uid0003fs-rollback` against disk `74720635...FC46`, then by `supervisor-uid0003fs-rollback-reopen` against disk/backup `B8B1CFB3...A299`, size `143196536`; the later PID7004 out-of-allowlist cycle also proved stable exact P0 `F11A047D...E048` and grants no credit. Historical identities including `b002-persist-reopen`/`400BFA5A...`, `b005-persist-reopen`/`92CD98A2...`, the b006/b008 rollback pair, disk `85582A48...AF5D`, PID16032, and PID7004 remain provenance. The successful primary-supervisor package then used fresh pinned PID12692, applied A01-A07, passed A08/P01-P11, saved exactly once, and used fresh no-analysis verifier PID13268 to reproduce every poststate/no-change protection at exact S1 `2F559453...8D70`.

The first supervisor Gate 2B attempt is retained as exact failed/rolled-back history. Session `b006-uid0004e0-rollback`, worker PID `15144`, began from canonical and backup SHA256 `74720635...FC46`. A01-A03 succeeded in memory. A04's rename, exact unsigned-int signature, function regular comment, and address repeatable comment succeeded; `analyze_function` read the intended `CashShopRequest *__thiscall(CashShopRequest *self, unsigned int flags)` prototype. `stack_frame(0x0041b570)` nevertheless retained `arg_0` at frame offset `0x0c`, size `0x1`, type `char`. That physical frame representation is verified IDA behavior and is not a reason to weaken the source-facing signature. Because the predecessor report incorrectly required size 4, the supervisor stopped before A05 and before save, discarded only PID15144, and reopened `b008-uid0003fs-rollback`; all A01-A04 names/types/comments and BaseRequest were absent/restored to prestate, proving full rollback. No in-memory success from this cycle satisfies any IDA claim; its no-credit history is superseded for current disposition by the later persisted transaction.

The second supervisor Gate 2B attempt is retained as a separate failed/unsaved/full-rollback history. Canonical prestate and backup `E:\NTK\Resources\NexusTK\NexusTK.exe.i64.bak-UID0003FS-prestate-20260804_181800` matched SHA256 `B8B1CFB3E7B660AC394A904F4FBFAF05B6E99A8B074DACC1431E9CD1C865A299`, size `143196536`. All A/P prestates matched; unsaved A01-A03 passed; A04 rename, exact signature, and `set_function_comments` regular function text passed. The next `set_repeatable_comments` call at function entry populated `function_repeatable`, not address repeatable, violating the exact four-channel poststate. The supervisor stopped before A05/save, killed only worker PID16032, preserved the listener, and reopened canonical disk in `supervisor-uid0003fs-rollback-reopen`. Readback proved exact disk SHA, type, names, A02/A03 comments, and all four A04/A05 comment channels restored. No in-memory success from this cycle satisfies any IDA claim. The later persisted transaction correctly used `set_address_repeatable_comments` for A04/A05 function-entry address-repeatable text and never substituted `set_repeatable_comments` there.

**Superseding atomic closed-disk transaction protocol and completed receipt.** The predecessor pinned-session protocol above is retained as historical design context. The successful PID12692/S1 transaction and PID13268 verifier followed the following stricter contract; the terms remain the exact safety/readback definition for this persisted result.

1. **Disposable diagnostic isolation and feasible worker release.** Every call capable of producing or changing analysis, including `analyze_function`, `decompile`, `func_profile`, `force_recompile`, `disasm`, or any equivalent operation, runs only in a disposable diagnostic worker before `P0`. Its literal open is `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64", mode:"force_headless", run_auto_analysis:false, build_caches:true, init_hexrays:true, idle_ttl_sec:3600, preferred_session_id:<collision-free-diagnostic-id>}`; an existing/adopted session or any response not proving the exact new worker identity stops. That worker may not perform an A-row mutation or call `idb_save`. Current MCP exposes no `idb_close`; therefore the supervisor may terminate only the exact recorded worker identity when release is required. It must then prove the exact PID/process identity absent, reject any PID-reuse ambiguity, require fresh `idb_list` to contain no `is_active:true` row for the canonical path, and treat any surviving `is_active:false`/`owned:true`/`adopted:true` row as stale non-authoritative registry history rather than requiring row disappearance. A successful read-only exclusive Windows handle probe that opens the canonical IDB with `FileMode.Open`, `FileAccess.Read`, `FileShare.None` and immediately releases the handle is mandatory before proceeding. Any active/ambiguous worker, exact-process absence failure, PID-reuse ambiguity, active canonical row, or handle-probe failure stops. Diagnostic results remain dated evidence only and may not substitute for transaction-worker readback.
2. **Exclusive-handle closed-disk `P0` and backup.** After the phase-1 proof, repeat the no-active-canonical-session check and successful read-only `FileShare.None` probe immediately before hashing the canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; record path, SHA256, size, and last-write as `P0`. Select a new collision-free backup path that does not already exist. Repeat the active-session check and exclusive probe immediately before copying exact `P0`, then hash/read back the backup under another successful probe. Canonical and backup SHA256/size must be byte-identical before any worker opens; a pre-existing backup-path collision, copy ambiguity, active/ambiguous worker, PID ambiguity, probe failure, or mismatch stops without mutation.
3. **Fresh pinned `P0` transaction worker and frozen allowlist.** After another successful active-session check and read-only exclusive probe, open one fresh worker from exact `P0` using literal `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64", mode:"force_headless", run_auto_analysis:false, build_caches:false, init_hexrays:false, idle_ttl_sec:3600, preferred_session_id:<collision-free-transaction-id>}`. Require the returned database/session/PID/process-start identity to be newly created and exact; an existing/adopted session, identity ambiguity, or auto-analysis request stops. Pin that identity through the one save attempt. Its only authorized mutating calls are the literal A01-A07 operations: `declare_type`, function-only `rename` batches with `stop_on_error:true`, `dry_run:false`, `allow_overwrite:false`, `pure:true`, `set_type`, `set_function_comments`, and `set_address_repeatable_comments` at the exact named targets. Generic `set_repeatable_comments`, `set_address_comments`, local/stack rename, and every `/vibe/` or decompiler-refresh path are forbidden because no such channel/side effect is intended. Its only authorized read/health calls are `idb_list`, `server_health`, `type_inspect`, `lookup_funcs`, `stack_frame`, `get_comments`, `inspect_items`, `get_bytes`, and `xrefs_to`; `idb_save` is separately authorized exactly once after all immediate A01-A08 and final P01-P11 checks pass. No other call is permitted. In particular, the transaction worker must never call `analyze_function`, `decompile`, `func_profile`, `force_recompile`, `disasm`, or another analysis-producing operation. Before mutation, every literal A/P name, range, byte, type, comment channel, frame member, xref set, dependency, collision, `P0` identity, current endpoint schema/implementation, and worker identity must match using only that allowlist. Apply one exact A-row at a time and read it back immediately with the same allowlist.
4. **Single save attempt and feasible worker-release proof.** Any failed action, warning, partial/mixed result, immediate readback mismatch, protected-row drift, session/transport loss, PID/database change, or unexpected side effect stops remaining A-rows. If the complete package passes, call `idb_save` exactly once. Classify its single result as explicit success, explicit failure, or indeterminate/transport loss; never retry in any branch. Whether the attempt succeeds, fails, or is indeterminate, the supervisor terminates only the exact pinned transaction-worker identity when needed, proves its exact PID/process identity absent without PID-reuse ambiguity, confirms fresh `idb_list` has no `is_active:true` canonical-path session, and treats surviving inactive registry rows as non-authoritative. It must then complete and release a read-only `FileShare.None` handle probe before hashing the canonical disk, copying, restoring, classifying, or opening a verifier. Any active/ambiguous worker, PID reuse ambiguity, or probe failure stops.
5. **Exclusive-handle closed-disk result classification and restore gate.** After the phase-4 proof, repeat the active-session check and exclusive probe immediately before hashing/classifying the canonical disk. Exact `P0` means no disk change and no backup copy is permitted. A non-`P0` identity may be recorded as transaction-attributable `S1` on explicit save success, or as the exact transaction-attributable failing/indeterminate identity on those branches, only because exclusive-handle `P0`, the single pinned worker, the one save attempt, exact-process absence, no active canonical session, and the handle probe were all proven. Any identity other than exact `P0` or that exact recorded transaction-attributable identity is a third identity: stop and escalate without copying over it. Restore from the verified `P0` backup is permitted only while a fresh exclusive-handle probe succeeds and the current disk is exactly the recorded transaction-attributable `S1`/failing identity; if it is exact `P0`, perform no copy. After a permitted restore, use the same feasible proof and probe before rehashing exact `P0` and before opening a fresh readback worker with literal `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64", mode:"force_headless", run_auto_analysis:false, build_caches:false, init_hexrays:false, idle_ttl_sec:3600, preferred_session_id:<collision-free-restore-verifier-id>}`; require a new exact worker, prove the complete A01-A08/P01-P11 literal prestate, release the exact verifier through PID/process/active-session/probe proof, and rehash exact `P0` again. A failed or indeterminate save grants no retry and no action credit.
6. **Saved `S1` verification and persistence credit.** Explicit save success plus a transaction-attributable exclusive-handle closed-disk `S1` is provisional only. After a fresh no-active-session check and successful exclusive probe, open one fresh readback worker from exact `S1` with literal `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64", mode:"force_headless", run_auto_analysis:false, build_caches:false, init_hexrays:false, idle_ttl_sec:3600, preferred_session_id:<collision-free-S1-verifier-id>}` and require a new exact worker identity. Use the same inspection-only allowlist, with no mutation, save, or analysis-producing call, to prove every A01-A08 poststate and every P01-P11 no-change protection. Release only that exact verifier identity when needed, prove exact PID/process absence without reuse ambiguity, require no `is_active:true` canonical-path session, treat stale inactive registry rows as non-authoritative, and complete/release the exclusive probe before the final hash. Persistence credit exists only if this final hash/size is exact recorded `S1` and every poststate passed. A verifier mismatch follows the same identity-gated restore path: release the exact verifier, pass the PID/process/active-session/probe package, require the disk still be exact `S1`, restore verified `P0`, then fresh-reopen with the literal restore-verifier `idb_open`/prove/release/probe/rehash full `P0`. Any active/ambiguous worker, PID reuse ambiguity, probe failure, or third identity stops without overwrite.

**Completed primary-supervisor Gate 2B receipt.** Resumed worker `primary-uid0003fs-transaction-202608060519`/PID7004 was discarded unsaved after the single out-of-allowlist `export_funcs` read; exact release, no-active-session, `FileShare.None`, full prestate, and stable P0 `F11A047D...E048` proved rollback and assign no credit. The retry backup `NexusTK.exe.i64.bak-UID0003FS-prestate-20260806-052400` matched P0. Fresh transaction worker `primary-uid0003fs-transaction2-202608060524`/PID12692, started `2026-08-06T05:24:32.6491648-04:00`, used only the frozen allowlist, applied A01-A07, passed A08/P01-P11, called `idb_save` exactly once with explicit success, retired exactly, and produced closed-disk S1 SHA256 `2F5594535B9730C2195466DC6673A6325B19E572D0DCCB9D054B0F6DFAC48D70`, size `143197685`, mtime UTC `2026-08-06T09:28:16.2969329Z`. Fresh verifier `primary-uid0003fs-s1-verifier-202608060529`/PID13268, started `2026-08-06T05:28:41.2703145-04:00`, initialized no analysis/caches/Hex-Rays, used only `type_inspect`, `lookup_funcs`, `stack_frame`, `get_comments`, `inspect_items`, `get_bytes`, and `xrefs_to`, reproduced all A01-A08/P01-P11 states, made zero mutation/save calls, retired exactly, passed `FileShare.None`, and left final disk exact S1. C078-C084 and C086 are applied; C085 and C097-C107 are already-present/no-change.

**A01 - declare physical analysis UDT `BaseRequest`.**

- Literal prestate: `type_inspect("BaseRequest")` returns `exists:false`; `CashShopVersionRequest` is also absent. Existing `CashShopRequest` is a `0x28` UDT and is protected under A08. No type-library collision exists for the exact `BaseRequest` name.
- Exact authorized delta: call `declare_type` once with `struct BaseRequest { bool downloadComplete; unsigned char alignment_01[3]; unsigned int contentLength; unsigned int bytesDownloaded; };`. No other type declaration or member edit is authorized within A01; A05 separately authorizes the exact physical `CashShopVersionRequest` UDT after A01 has passed.
- Deterministic poststate: `type_inspect("BaseRequest", include_members=true)` returns UDT size `0x0c` with `downloadComplete` offset `0x0`, size `1`, type `bool`; `alignment_01` offset `0x1`, size `3`, type `unsigned char[3]` or IDA-equivalent `unsigned __int8[3]`; `contentLength` offset `0x4`, size `4`, type `unsigned int`; `bytesDownloaded` offset `0x8`, size `4`, type `unsigned int`.
- Stop/rollback: stop on a pre-existing type, parse warning, member-offset/size/type mismatch, or any change to `CashShopRequest`. If declaration occurred but poststate fails, invoke the mandatory full-package abort: no inverse and no save; release only the exact pinned worker through the PID/process-absence, no-active-canonical-session, stale-inactive-row, and read-only `FileShare.None` proof package; classify the exclusive-handle closed disk under the exact `P0`/transaction-attributable-result gate; and use a fresh post-restore verifier to prove `BaseRequest` absent and `CashShopRequest` unchanged. The explicit alignment member is analysis-only physical layout and must not appear in formal C++.

**A02 - repeatable address comment on hidden COL-pointer dword `0x0060d79c`.**

- Literal prestate: data item `[0x0060d79c,0x0060d7a0)`, size `4`, name absent, type absent, regular comment absent, repeatable comment absent, bytes `18 04 64 00`, value `0x00640418`, zero incoming xrefs. Predecessor item is `??_7CashShopVersionRequest@@6B@` at `0x0060d798`; successor item is `??_7CashShopRequest@@6B@` at `0x0060d7a0`.
- Exact authorized delta: dedicated `set_address_repeatable_comments` with schema `{database:<pinned>, items:{addr:"0x0060d79c", comment:"CashShopRequest vtable preamble: pointer to complete object locator 0x00640418; the public vftable begins at 0x0060d7a0."}}`. The current implementation writes only `idaapi.set_cmt(ea, comment, True)`. Generic `set_repeatable_comments` and regular-address `set_address_comments` are not called. No name, type, width, regular-comment, byte, or xref change is authorized.
- Deterministic poststate: `get_comments` returns regular `""` and the exact repeatable text; `inspect_items` and `get_bytes` return the same head/end/size/name/type/bytes/value; `xrefs_to` remains empty.
- Stop/rollback: any prestate or poststate mismatch invokes the mandatory full-package abort. Do not clear only the comment as a partial inverse; do not save; release only the exact pinned worker through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to establish both comment channels and all physical properties at literal `P0`.

**A03 - repeatable address comment on CashShopRequest vftable slot `0x0060d7a0`.**

- Literal prestate: data item `[0x0060d7a0,0x0060d7a4)`, size `4`, name `??_7CashShopRequest@@6B@`, type absent, regular/repeatable comments absent, bytes `70 b5 41 00`, value `0x0041b570`. Incoming xrefs are exactly `0x0041a5e8`, `0x0041a613`, `0x0041b22d`, and `0x0041b576`.
- Exact authorized delta: dedicated `set_address_repeatable_comments` with schema `{database:<pinned>, items:{addr:"0x0060d7a0", comment:"CashShopRequest vftable slot 0: scalar deleting destructor; RTTI confirms nonvirtual BaseRequest subobject at +0x04."}}`. Generic `set_repeatable_comments` and regular-address `set_address_comments` are not called. No other channel or item property may change.
- Deterministic poststate: exact repeatable text; blank regular comment; unchanged decorated name, absent type, bytes, width, value, four-xref set, and neighboring item heads.
- Stop/rollback: collision, xref drift, item-boundary drift, byte drift, or comment mismatch invokes the mandatory full-package abort. No comment-only inverse is authorized; release only the exact pinned worker without save through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to reread the exact slot, four-xref set, bytes, and blank comments at `P0`.

**A04 - function rename/type/comments at `0x0041b570`.**

- Literal function prestate: `[0x0041b570,0x0041b5db)`, size `0x6b`, name `sub_41B570`, return `_DWORD *`, decompiler declaration `_DWORD *__thiscall sub_41B570(_DWORD *Block, char a2)`. Address regular/repeatable and function regular/repeatable comments are all absent. Incoming xref is exactly data xref `0x0060d7a0`; callees are `sub_5C7526` and `__invalid_parameter_noinfo_noreturn`; proposed-name collision query returns zero rows.
- Literal frame/local prestate: implicit `this` is ECX and decompiler name/type is `Block : _DWORD *`; explicit argument is `arg1`/`a2 : char`. IDA frame member `__saved_registers` is frame offset `0x4`, size `4`, type `_DWORD`, physically saved ESI at `[ebp-0x04]`; `__return_address` is frame offset `0x8`, size `4`, type `_UNKNOWN *`, physically `[ebp+0x04]`; `arg_0` is frame offset `0x0c`, size `1`, type `char`, physically `[ebp+0x08]`. There are no ordinary stack locals and no other decompiler locals requiring rename.
- Exact authorized deltas, in order: (1) literal `rename {database:<pinned>, batch:{func:{addr:"0x0041b570", name:"CashShopRequest_scalar_deleting_destructor"}, stop_on_error:true, dry_run:false, allow_overwrite:false, pure:true}}`; current implementation therefore performs only `idaapi.set_name` and skips both `/vibe/` linking and decompiler refresh; (2) `set_type` at `0x0041b570` with exact signature `CashShopRequest *__thiscall CashShopRequest_scalar_deleting_destructor(CashShopRequest *self, unsigned int flags)`; (3) `set_function_comments {database:<pinned>, items:{addr:"0x0041b570", comment:"Compiler-generated scalar deleting destructor for CashShopRequest; resets vptr, destroys responseText, and conditionally frees self from flags."}}`, which writes only regular function text; (4) dedicated `set_address_repeatable_comments {database:<pinned>, items:{addr:"0x0041b570", comment:"MSVC scalar deleting destructor; source cause is CashShopRequest's virtual destructor, not an explicit source function."}}`, which writes only repeatable address text. No local/stack rename, `/vibe/` link, decompiler refresh, address-regular, or function-repeatable mutation is authorized. Generic `set_repeatable_comments` is explicitly forbidden for this function-entry address channel; `set_address_comments` is not called because address regular must remain blank.
- Deterministic poststate: the `rename` result has the exact address/old/new name, no error, and no `dir`/`dir_error` field; current `api_modify.py` remains SHA256 `161B314A...D1B8`, the transaction worker was opened with `init_hexrays:false`, and no local/stack batch exists, jointly proving that neither `/vibe/` placement nor decompiler refresh was requested. Inspection-only `lookup_funcs`/function-type readback returns the exact function name/signature, including implicit ECX `self : CashShopRequest *` and explicit source/function prototype argument `flags : unsigned int`; `get_bytes`, `inspect_items`, and bounded `xrefs_to` checks prove range/body/callees/xref unchanged without invoking analysis. Independently, `stack_frame(0x0041b570)` must retain the verified physical row `arg_0` at frame offset `0x0c`, size `0x1`, type `char`, physically `[ebp+0x08]`; setting the function prototype does not require or authorize expansion of that physical frame row to size 4. Saved ESI and return-address frame members remain exact and no new local/frame member appears. `get_comments` returns address regular `""`, exact address repeatable text, exact function regular text, function repeatable `""`. The historical `analyze_function` result belongs only to the already-released diagnostic attempt and is forbidden in this stateful transaction worker.
- Stop/rollback: stop before mutation if the existing CashShopRequest UDT is absent or not `0x28`, collision appears, frame differs, or non-analysis rename/type preconditions fail. Any later body/range/xref/callee/frame drift, partial rename/type/comment result, address-repeatable mismatch, or nonblank function-repeatable channel invokes the mandatory full-package abort: no inverse and no save; release only the exact pinned worker through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to establish the original name, declaration, four blank comment channels, and exact physical frame at `P0`.

**A05 - exact physical UDT plus typed function rename/type/comments at `0x0041b5e0`; physical frame protected.**

- Literal type/function prestate: `type_inspect("CashShopVersionRequest")` returns `exists:false`; A01's `BaseRequest` poststate is exact size `0x0c`. Function `[0x0041b5e0,0x0041b604)`, size `0x24`, is named `sub_41B5E0`, return `_DWORD *`, declaration `_DWORD *__thiscall sub_41B5E0(_DWORD *Block, char a2)`. Address regular/repeatable and function regular/repeatable comments are all absent. Incoming xref is exactly data xref `0x0060d798`; only callee is `sub_5C7526`; proposed type/function-name collision queries return zero rows.
- Literal frame/local prestate: implicit `this` is ECX and decompiler name/type is `Block : _DWORD *`; explicit argument is `arg1`/`a2 : char`. `__saved_registers` frame offset `0x4`, size `4`, `_DWORD`, physically saved ESI at `[ebp-0x04]`; `__return_address` frame offset `0x8`, size `4`, `_UNKNOWN *`, physically `[ebp+0x04]`; `arg_0` frame offset `0x0c`, size `1`, `char`, physically `[ebp+0x08]`. No ordinary stack locals exist.
- Exact authorized deltas, in order: (1) `declare_type` exactly `struct CashShopVersionRequest { void *vftable; BaseRequest baseRequest; };`; (2) literal `rename {database:<pinned>, batch:{func:{addr:"0x0041b5e0", name:"CashShopVersionRequest_scalar_deleting_destructor"}, stop_on_error:true, dry_run:false, allow_overwrite:false, pure:true}}`, which performs only the IDA name change and skips `/vibe/` and decompiler refresh; (3) `set_type` at `0x0041b5e0` with exact signature `CashShopVersionRequest *__thiscall CashShopVersionRequest_scalar_deleting_destructor(CashShopVersionRequest *self, unsigned int flags)`; (4) `set_function_comments {database:<pinned>, items:{addr:"0x0041b5e0", comment:"Compiler-generated scalar deleting destructor for CashShopVersionRequest; conditionally frees self from flags."}}`; (5) dedicated `set_address_repeatable_comments {database:<pinned>, items:{addr:"0x0041b5e0", comment:"MSVC scalar deleting destructor; source cause is CashShopVersionRequest's virtual destructor."}}`. No local/stack/frame rename, `/vibe/` link, decompiler refresh, address-regular, function-repeatable, item, byte, xref, body, or callee mutation is authorized. Generic `set_repeatable_comments` and regular-address `set_address_comments` are not called.
- Deterministic poststate: `type_inspect("CashShopVersionRequest", include_members=true)` returns UDT size `0x10`, `vftable` offset `0x0`, size `4`, type `void *`, and `baseRequest` offset `0x4`, size `0x0c`, type `BaseRequest`, with no additional member. The `rename` result has exact address/old/new name, no error, and no `dir`/`dir_error`; current `api_modify.py` remains SHA256 `161B314A...D1B8`, transaction `idb_open` used `init_hexrays:false`, and no local/stack batch exists. Inspection-only `lookup_funcs`/function-type readback returns the exact function name/signature, including implicit ECX `self : CashShopVersionRequest *`, return `CashShopVersionRequest *`, and explicit source/function prototype argument `flags : unsigned int`. Independently, `stack_frame(0x0041b5e0)` retains `__saved_registers` offset `0x4`, size `4`, `_DWORD`; `__return_address` offset `0x8`, size `4`, `_UNKNOWN *`; and physical `arg_0` offset `0x0c`, size `0x1`, type `char`, physically `[ebp+0x08]`, with no new local/frame member. `get_bytes`, `inspect_items`, and bounded `xrefs_to` checks prove range/body bytes, one data xref, sole callee, and item boundaries remain exact without invoking analysis. `get_comments` reads address regular `""`, exact address repeatable, exact function regular, and function repeatable `""`; no regular-address setter or function-repeatable setter was called. No stateful pre-save or verifier readback may call `analyze_function`.
- Stop/rollback: collision, UDT parse/member drift, signature/frame drift, changed xref/callee/body/range/bytes, partial comment result, address-repeatable mismatch, or nonblank function-repeatable channel invokes the mandatory full-package abort. Do not remove only the UDT/name/type/comments as a partial inverse; do not save; release only the exact pinned worker through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to establish `CashShopVersionRequest` absent plus the original function name/declaration/frame/four blank channels and all protected physical state at `P0`.

**A06 - repeatable address comment on shared BaseRequest BCD `0x006403c8`.**

- Literal prestate: head item `[0x006403c8,0x006403cc)`, size `4`, name `??_R13?0A@EA@BaseRequest@@8`, type absent, regular comment exactly `reference to type description`, repeatable comment absent. The protected 28-byte BCD record at `0x006403c8-0x006403e4` is `00 39 67 00 00 00 00 00 04 00 00 00 ff ff ff ff 00 00 00 00 40 00 00 00 e4 03 64 00`, encoding type descriptor `0x00673900`, contained bases `0`, PMD `4/-1/0`, attributes `0x40`, hierarchy pointer `0x006403e4`. Incoming xrefs are exactly `0x006403a4` and `0x00640440`.
- Exact authorized delta: dedicated `set_address_repeatable_comments {database:<pinned>, items:{addr:"0x006403c8", comment:"Shared BaseRequest base-class descriptor: nonvirtual subobject at derived offset +0x04 in CashShopVersionRequest and CashShopRequest."}}`. The current implementation writes only the repeatable address channel. Generic `set_repeatable_comments` and regular-address `set_address_comments` are not called. Preserve the existing regular comment and every other property.
- Deterministic poststate: exact decorated name, absent type, exact regular and repeatable comments, identical 28 bytes/decoded fields/two-xref set/item boundary.
- Stop/rollback: byte/field/xref/comment/name/type drift invokes the mandatory full-package abort. Do not clear only the repeatable channel; release only the exact pinned worker without save through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to establish the exact 28-byte record, two-xref set, regular comment, and blank repeatable channel at `P0`.

**A07 - repeatable address comment on CashShopRequest COL `0x00640418`.**

- Literal prestate: head item `[0x00640418,0x0064041c)`, size `4`, name `??_R4CashShopRequest@@6B@`, type absent, regular comment exactly `signature`, repeatable comment absent. The protected 20-byte COL record at `0x00640418-0x0064042c` is `00 00 00 00 00 00 00 00 00 00 00 00 1c 39 67 00 2c 04 64 00`, encoding signature/offset/cdOffset `0/0/0`, type descriptor `0x0067391c`, CHD `0x0064042c`. Incoming xref is exactly `0x0060d79c`.
- Exact authorized delta: dedicated `set_address_repeatable_comments {database:<pinned>, items:{addr:"0x00640418", comment:"CashShopRequest complete object locator; hierarchy contains CashShopRequest and nonvirtual BaseRequest at +0x04."}}`. The current implementation writes only the repeatable address channel. Generic `set_repeatable_comments` and regular-address `set_address_comments` are not called. Preserve the regular comment and all physical properties.
- Deterministic poststate: exact decorated name, absent type, exact regular/repeatable comments, identical 20 bytes/decoded fields/one-xref set/item boundary.
- Stop/rollback: any physical or semantic drift invokes the mandatory full-package abort. Do not clear only the repeatable channel; release only the exact pinned worker without save through the feasible PID/process/no-active-session/exclusive-handle proof, classify/restore only under the exact disk-identity gate, and use a fresh verifier followed by the same release/probe proof to establish the exact 20-byte COL, one-xref set, regular comment, and blank repeatable channel at `P0`.

**A08 - protected no-change transaction for current flattened `CashShopRequest` UDT.**

- Literal prestate/poststate required: UDT size `0x28`; `vftable` offset `0x0`, size `4`, `void *`; `downloadComplete` offset `0x4`, size `1`, `bool`; `alignment_05` offset `0x5`, size `3`, `unsigned __int8[3]`; `contentLength` offset `0x8`, size `4`, `unsigned int`; `bytesDownloaded` offset `0x0c`, size `4`, `unsigned int`; `responseText` offset `0x10`, size `24`, `unsigned __int8[24]`.
- Exact authorized delta: none. Do not replace the flattened UDT, insert an inheritance base, rename members, or change member comments/types. Available tools cannot atomically convert this UDT to inheritance while proving preservation of every hidden channel.
- Deterministic readback: `type_inspect("CashShopRequest", include_members=true)` returns the identical six-member layout after A01-A07. Any difference invokes the full-package abort; backup restoration is allowed only after exact transaction-worker PID/process absence, no active canonical session, stale inactive rows treated as non-authoritative, successful read-only `FileShare.None` probe, and exact closed-disk identity classification under the `P0`/transaction-attributable-result gate.

**Protected dependency/item transactions.** Each row is an independent `no change recommended` readback inside the fresh `P0` transaction worker. For every row the authorized delta is exactly none. Before any mutation, compare the literal prestate below with fresh `inspect_items`, `get_bytes`, `get_comments`, `xrefs_to`, and, for raw starts, `lookup_funcs` from the frozen inspection-only allowlist. After A01-A07, repeat the same reads and require byte-for-byte and field-for-field identity. Any drift invokes the mandatory full-package abort: stop remaining work, do not save or partially invert, release only the exact pinned unsaved worker through the PID/process-absence, no-active-canonical-session, stale-inactive-row, and read-only `FileShare.None` proof package, classify the exclusive-handle closed disk, and use the strict `P0`/transaction-attributable-result restore gate before a fresh full prestate verifier. Any row-level phrase below saying to restore the backup is shorthand for this gated full-package procedure, never permission for an immediate or partial copy. Never rename decorated compiler RTTI/vftable symbols, apply a type to these physical heads, overwrite either comment channel, change item boundaries, or create functions at raw starts.

| Protection ID | Exact current physical/item prestate | Exact xref/comment prestate | Required poststate / rollback contract |
| --- | --- | --- | --- |
| P01 | CashShopRequest CHD head `[0x0064042c,0x00640430)`, size `4`, data, name `??_R3CashShopRequest@@8`, type absent. Protected CHD bytes `[0x0064042c,0x0064043c)` are `00 00 00 00 00 00 00 00 02 00 00 00 3c 04 64 00`: signature `0`, attributes `0`, base count `2`, base-array pointer `0x0064043c`. | Incoming data xrefs exactly `0x00640428`, `0x00640460`; regular comment exactly `signature`; repeatable comment absent. | Exact same head/end/size/kind/name/type, 16 bytes/decoded fields, two-xref set, and both comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not reinterpret/recreate CHD data. |
| P02 | CashShopRequest base-array head `[0x0064043c,0x00640440)`, size `4`, data, name `??_R2CashShopRequest@@8`, type absent. Protected two-dword array `[0x0064043c,0x00640444)` is `48 04 64 00 c8 03 64 00`, pointing to `0x00640448` then `0x006403c8`. | Incoming data xref exactly `0x00640438`; regular comment exactly `reference to base class decription 1`; repeatable comment absent. | Exact same physical/item, two pointers/order, one-xref set, typo-preserving regular comment, and absent repeatable comment. On drift invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not normalize the historical comment spelling. |
| P03 | CashShopRequest self BCD head `[0x00640448,0x0064044c)`, size `4`, data, name `??_R1A@?0A@EA@CashShopRequest@@8`, type absent. Protected 28 bytes are `1c 39 67 00 01 00 00 00 00 00 00 00 ff ff ff ff 00 00 00 00 40 00 00 00 2c 04 64 00`: type descriptor `0x0067391c`, contained bases `1`, PMD `0/-1/0`, attributes `0x40`, hierarchy `0x0064042c`. | Incoming data xref exactly `0x0064043c`; regular comment exactly `reference to type description`; repeatable comment absent. | Exact same item, 28 bytes/decoded fields, one-xref set, and comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not rename/retype the decorated BCD. |
| P04 | BaseRequest type-descriptor head `[0x00673900,0x00673904)`, size `4`, data, name `??_R0?AVBaseRequest@@@8`, type absent. Protected 28 bytes are `78 50 63 00 00 00 00 00 2e 3f 41 56 42 61 73 65 52 65 71 75 65 73 74 40 40 00 00 00`: RTTI vftable `0x00635078`, spare `0`, decorated name `.?AVBaseRequest@@`. | Incoming data xrefs exactly `0x006403c8`, `0x006403fc`; regular comment exactly `reference to RTTI's vftable`; repeatable comment absent. | Exact same item/bytes/two-xref set/comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; A01 may create a named UDT but must not alter this physical descriptor. |
| P05 | CashShopRequest type-descriptor head `[0x0067391c,0x00673920)`, size `4`, data, name `??_R0?AVCashShopRequest@@@8`, type absent. Protected 32-byte descriptor is `78 50 63 00 00 00 00 00 2e 3f 41 56 43 61 73 68 53 68 6f 70 52 65 71 75 65 73 74 40 40 00 00 00`: RTTI vftable `0x00635078`, spare `0`, decorated name `.?AVCashShopRequest@@`. | Incoming data xrefs exactly `0x00640424`, `0x00640448`; regular comment exactly `reference to RTTI's vftable`; repeatable comment absent. | Exact same item/bytes/two-xref set/comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not conflate the physical descriptor with the protected UDT in A08. |
| P06 | Predecessor hidden COL-pointer head `[0x0060d794,0x0060d798)`, size `4`, data, name absent, type absent, bytes `7c 03 64 00`, value `0x0064037c`. | No incoming xrefs; regular and repeatable comments both absent. | Exact same item/value/bytes/no-xref/comment state. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not create a source-facing label or type. |
| P07 | Predecessor public vftable-slot head `[0x0060d798,0x0060d79c)`, size `4`, data, name `??_7CashShopVersionRequest@@6B@`, type absent, bytes `e0 b5 41 00`, value `0x0041b5e0`. | Incoming data xrefs exactly `0x0041b2a4`, `0x0041b5ea`; regular and repeatable comments both absent. | Exact same item/value/bytes/two-xref set/comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; only A05 may rename/comment the pointed-to function, never this decorated vftable item. |
| P08 | Successor hidden COL-pointer head `[0x0060d7a4,0x0060d7a8)`, size `4`, data, name absent, type absent, bytes `64 04 64 00`, value `0x00640464`. | No incoming xrefs; regular and repeatable comments both absent. | Exact same item/value/bytes/no-xref/comment state. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not create a source-facing label or type. |
| P09 | Successor public vftable-slot head `[0x0060d7a8,0x0060d7ac)`, size `4`, data, name `??_7FileDownloader@@6B@`, type absent, bytes `10 b6 41 00`, value `0x0041b610`. | Incoming data xrefs exactly `0x0041a6c8`, `0x0041a715`, `0x0041b63c`; regular and repeatable comments both absent. | Exact same item/value/bytes/three-xref set/comments. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not rename/retype the decorated sibling slot. |
| P10 | Raw constructor start head `[0x0041a5d0,0x0041a5d1)`, size `1`, code, name/type absent; `lookup_funcs` returns no modeled function. First eight bytes are `55 8b ec 51 c6 41 04 00`; retained body range is `[0x0041a5d0,0x0041a607]`. | No incoming xrefs. Regular comment exactly `Unreferenced retained CashShopRequest constructor body; exact range 0x0041A5D0-0x0041A607. Live inline equivalent at 0x0041B200 initializes the active 0x28-byte request payload.` Repeatable comment exactly `CashShopRequest::CashShopRequest(): clears downloadComplete/contentLength/bytesDownloaded and default-constructs responseText (24-byte SSO-15 layout). Do not define a function unless a real entry route is recovered.` | Exact same non-function/item/bytes/no-xref/two-comment state. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not define a function, frame, type, or new label. |
| P11 | Raw destructor start head `[0x0041a610,0x0041a611)`, size `1`, code, name/type absent; `lookup_funcs` returns no modeled function. First eight bytes are `56 8b f1 c7 06 a0 d7 60`; retained body range is `[0x0041a610,0x0041a662]`. | No incoming xrefs; regular and repeatable comments both absent. | Exact same non-function/item/bytes/no-xref/comment state. On mismatch invoke the feasible exclusive-handle full-package abort and exact identity-gated restore; do not define a function, frame, type, comment, or new label. |

**Final Gate 2B readback/save contract.** The fresh transaction worker must remain the exact database/session/PID/process-start identity opened from exclusive-handle closed-disk `P0`; switching workers or accepting PID reuse is forbidden. Before save, use only `type_inspect`, `lookup_funcs`, `stack_frame`, `get_comments`, `inspect_items`, `get_bytes`, and `xrefs_to` from the frozen inspection allowlist to verify every A01-A08 poststate and every P01-P11 `verified no change` row; do not call `disasm`, `analyze_function`, `decompile`, `func_profile`, `force_recompile`, or any other analysis-producing operation. Any mismatch or identity/transport change forbids save and enters the feasible exact-worker-release failure branch. After a complete pass, call `idb_save` once and never retry. Record success/failure/indeterminate, terminate only the exact transaction worker when needed, prove its exact PID/process identity absent without reuse ambiguity, require no `is_active:true` canonical-path session, ignore stale inactive registry rows as non-authoritative, and complete/release the read-only `FileShare.None` probe before hashing. Explicit success must produce a transaction-attributable exclusive-handle closed-disk `S1`; after another active-session/probe check, open a fresh inspection-only verifier on exact `S1`, prove all A/P poststates, release that exact verifier through the same PID/process/no-active-session/probe package, and rehash exact `S1` before persistence credit. Failure or indeterminate result receives no credit: exact `P0` requires no copy, exact transaction-attributable failing identity permits restore from the byte-identical `P0` backup only after a fresh exclusive probe, and any active/ambiguous worker, PID reuse ambiguity, probe failure, or third identity stops without overwrite. A verifier failure follows the same exact-`S1` restore gate. Every restore must be followed by the feasible release/probe proof, fresh reopen, complete prestate proof, another exact-verifier release/probe proof, and exact closed-disk `P0` rehash. No individual A-row is applied without this coherent feasible closed-disk proof.

**Generated-authority actor boundary.** Command21052 proves only the dated UID0003FS routing state observed at that timestamp; command21289 memory versus command21224 CPP preserves the later pre-refresh mismatch as explicit history. Primary-supervisor command21445 closes that mismatch: CPP `0C729875...C1F5F` and H `F791C436...230F` contain the coherent UID0002CN/UID0003FS/UID0003FT covered-by routes, no UID0002CN/UID0003FT empty-emitter marker, zero literal no-children token, one global declaration, four-class header order, and no obsolete standalone request outputs. Command21450 tracker `9DD24D75...3131` records UID0003FS `94/94`, true, `0/0/0`; command21453 current memory `8C3194FA...3C75` preserves the relevant coded rows, its tracker `783B17E2...C2B9` is dated history, and later command21458 current tracker `DA40D597...C67F` preserves UID0003FS `94/94`, true, `0/0/0`. Generated closure is credited only to the primary supervisor. Gate2B IDA closure is separately complete under the PID12692/PID13268 persisted S1 receipt and does not alter generated actor credit.

`````

## Additional removed operational lines: mirrored prose cleanup

The following source-report lines were removed during the same cleanup because they retained exact request-schema or transaction orchestration outside Section 21. They are non-authoritative historical material and must never be executed.

`````text
- The later supervisor transaction used canonical/backup SHA256 `B8B1CFB3E7B660AC394A904F4FBFAF05B6E99A8B074DACC1431E9CD1C865A299`, size `143196536`, and backup `E:\NTK\Resources\NexusTK\NexusTK.exe.i64.bak-UID0003FS-prestate-20260804_181800`. All A/P prestates matched; unsaved A01-A03 and A04 rename/type/function-regular comment succeeded. At A04, `set_repeatable_comments` on function entry `0x0041b570` populated `function_repeatable` and left address repeatable blank. The supervisor stopped before A05/save, killed only PID16032, preserved the listener, and reopened `supervisor-uid0003fs-rollback-reopen`. Readback proved unchanged disk SHA, absent BaseRequest, unchanged CashShopRequest, blank A02/A03 comments, raw `sub_41B570`/`sub_41B5E0` names/types, and four blank entry/function channels. Current `tools/list` evidence provides dedicated `set_address_repeatable_comments` schema `{database, items:{addr,comment}}`; that tool is the accepted A04/A05 address-repeatable channel, while `set_function_comments` remains the function-regular channel.

Supervisor-only scope is actor-separated by stable policy. Exact-artifact report acceptance is established only by a matching external audit. Generated, tracker, manual, and IDB currentness is established only by dynamic readback, not by treating command21052, command21055, or any hash in this report as permanent. Every diagnostic/transaction/verifier `idb_open` literal sets `run_auto_analysis:false`; stateful opens additionally set `build_caches:false` and `init_hexrays:false`. The accepted IDA transaction releases each exact worker through PID/process absence, no `is_active:true` canonical-path session, stale inactive rows treated as non-authoritative, and a successful read-only `FileShare.None` probe; establishes exclusive-handle closed-disk `P0`; and creates/verifies a collision-free byte-identical backup. It then opens and pins one fresh `P0` transaction worker, uses `rename pure:true` and channel-specific comment setters, verifies every A01-A08/P01-P11 pre/poststate using only the frozen non-analysis allowlist plus exact authorized mutations, and attempts `idb_save` once without retry. Success, failure, and indeterminate branches all require the same feasible release/probe proof before hashing or restore. Saved credit requires transaction-attributable closed-disk `S1`, fresh inspection-only reopen, complete poststate proof, exact-verifier release/probe proof, and exact `S1` rehash. On failure/indeterminate/verifier failure, exact closed-disk `P0` is terminal with no backup copy; only an exact transaction-attributable `S1`/failing saved identity may be overwritten from the verified `P0` backup, followed by fresh full prestate proof and exact `P0` rehash. Active/ambiguous workers, PID reuse ambiguity, probe failure, or a third identity immediately stop. Both unsaved rollback cycles, prior sessions, command21036/21048 evidence, and prior disks are dated history. B008's bounded callback changed only the two named class pages and this report, revalidated BaseRequest unchanged, and ran three no-generated-refresh scoped validators; B008 did not edit manual/generated/IDA/audit/lifecycle state. Report execution/archive status is authoritative only from the current physical path and validator-owned status/history metadata.

- **May a failed A-row be repaired with a comment/type inverse and the remaining transaction continue?** No. Any partial/mixed result invalidates the entire transaction package. Release only the exact pinned worker without save through PID/process-absence, no-active-canonical-session, stale-inactive-row, and read-only `FileShare.None` proof before disk inspection. Exact closed-disk `P0` requires no copy; only the exact transaction-attributable failing identity may be replaced from the collision-free byte-identical `P0` backup; active/ambiguous workers, PID reuse ambiguity, probe failure, or any third identity stop without overwrite. A permitted restore is incomplete until a fresh `run_auto_analysis:false` readback worker proves the entire literal prestate, is released through the same feasible proof, and the canonical disk rehashes to exact `P0`.
`````
