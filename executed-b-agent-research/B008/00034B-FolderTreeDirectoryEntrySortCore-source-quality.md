** TARGET-REPORT-UID:00034B **
** AUTHOR-AGENT-ID:B008 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 00034B FolderTreeDirectoryEntrySortCore Source-Quality Research

## Finalized Report / Current Recommendation

- Current recommendation: keep [UID:00034B] under [UID:0000JG] `FolderTreePane.cpp`, but replace the source-facing interpretation from generic `FolderTreeDirectoryEntrySortCore` to `FolderTreeDirectoryEntryUnguardedPartition` / `std::sort` partition helper for the local folder-entry record.
- Final disposition: source-declared/generated-binary template support emitted from the `FolderTreePane` child-directory sort source expression, not a standalone handwritten NexusTK function body.
- Required action: rename/rewrite the target and support sort-helper docs to document the MSVC/Dinkumware-style introsort chain: `_Med3`/median ordering, unguarded partition, insertion sort cutoff, heap-adjust fallback, and local `DirectoryEntry` comparison. Keep owner/emitter [UID:0000JG].
- C++ disposition: exact no-code proof for this target's formal block. The source-level reconstruction belongs in the caller/sort wrapper as a high-level sort over local directory entries, not as a copied body for `0x004b4520`.
- Confidence: high for boundary, reachability, owner/emitter, record layout, and unguarded-partition role; medium-high for exact original spelling because no symbols/PDB/source names are available and IDA MCP was unavailable in this session.

## Supporting Research

## Target

- Target UID: `00034B`
- Target path: `E:\NTK\GhidraBridge\source-3\project-documentation\by-memory\0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md`
- Source queue/report row: project-level low-both row `85/87`, current by-memory coverage row says "directory-entry sort core with inferred original helper names still below final-source quality."
- Current supervisor classification: source-quality reanalysis, reconstructable `85/87`.
- Current metadata state: `CANONICAL_OWNER:0000JG`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JG`, formal C++ blank.
- Parent state: [UID:0000JG] `FolderTreePane` is `89/85` and routes to `auto-generated/NexusTK/ui/controls/FolderTreePane.cpp`; [UID:00005A] `FolderTreePane` is `85/86`.

## Executive Recommendation

The current owner/emitter is correct, but the source-facing name and "source-authored helper" wording are too imprecise. `0x004b4520-0x004b4fc5` is best modeled as the unguarded partition body from the compiler-emitted `std::sort`/introsort specialization for a local `FolderTreePane` directory-entry record. It is called only from [UID:00034D] at `0x004b52e0`, returns a pair of partition iterators through a hidden output pointer, performs median-of-three or Tukey-ninther pivot ordering, scans equal/less/greater partitions around a 12-byte record pivot, and swaps records by copy-constructing/destroying their string field.

Recommended target name:

- Best source-facing descriptive name: `FolderTreeDirectoryEntryUnguardedPartition`.
- Acceptable alternate search alias: `FolderTreeDirectoryEntrySortCore_4B4520` only as an old-address alias.
- Rejected final name: `FolderTreeDirectoryEntrySortCore`, because the bytes are not the full sort; they are the partition step called by the sort loop.

Recommended source placement:

- Keep canonical owner and emitter [UID:0000JG] `FolderTreePane.cpp`.
- Keep `RECONSTRUCTABLE:TRUE`, but change rebuild handling prose to `source-declared/generated-binary template support`.
- Do not create a new file, global, or class owner. This is local template/helper code generated by the `FolderTreePane` child-directory sort path.

Recommended score after implementation:

- `COMPLETION:90`
- `CONFIDENCE:91`

The improvement is justified by closing the main open source-quality issue: the helper is now tied to an introsort partition role and a high-level sort source shape rather than an unknown long optimized sort body. Scores should stay below `95` because IDA MCP was unavailable for this recheck, original symbol names are not recovered, and the caller/full `FolderTreePane` C++ remains incomplete.

## Supervisor Active Recheck

- Trigger: user assignment `B008-goal2-folder-tree-directory-entry-sort-core-source-quality-00034B-20260619`.
- Report-only scope: no edits were made to by-memory docs, support by-* docs, generated reports, project-level reports, or `by-memory/-coverage-report.md`.
- Split requirement: no split is required. The target is one exact function range with a confirmed exclusive end and no contained child functions.
- Source-bearing child state: not applicable; `00034B` is already the exact child for `0x004b4520-0x004b4fc5`.
- Implementation readiness: ready for a supervisor callback; the checklist below gives exact target/support changes.

## Inference Research Guidance Check

`by-structure.md` requires source-quality questions to be investigated even above `85/85`, requires owner and emitter to be separated, and requires exact no-code proof when an eligible emitter should not receive standalone final C++. `inference_research.md` warns that source-file and source-name decisions must be probability-weighted from xrefs, caller/callee patterns, data flow, and negative evidence rather than copied from generated labels.

Applied here:

- IDA fact from existing docs and local PE/Capstone evidence: function boundary, direct caller, direct callees, 12-byte stride, output-pair stores, and wrapper call arguments.
- Documentation evidence: current target/support pages record IDA MCP checks from 2026-06-10; [UID:000155], [UID:000157], [UID:00022E], and sibling sort pages establish FolderTreePane ownership and child-directory enumeration context.
- Inference: original source probably used a standard/library-style sort expression over a local directory-entry vector. The exact original spelling could have been `std::sort(entries.begin(), entries.end(), DirectoryEntryNameLess)` or a project wrapper around that call; no evidence supports a handwritten exported `FolderTreeDirectoryEntrySortCore` function.

## Heuristic / Inference Reanalysis And Validation

### Generated placeholder `sub_4B4520`

- Evidence checked: target doc, unresolved report entries, previous B001 split report, local PE/Capstone disassembly of `0x004b4520-0x004b4fc5`, raw rel32 xref scan over `.text`, sibling wrapper disassembly.
- Best inference: `FolderTreeDirectoryEntryUnguardedPartition`.
- Evidence: the wrapper at `0x004b5280` calls `0x004b4520` with a hidden output pair, first iterator, last iterator, and comparator argument. The target returns two pointers at `[out+0]` and `[out+4]` at `0x004b4fae-0x004b4fb0`. The wrapper then compares the two returned partition sizes and recurses/continues on the smaller/larger side, matching introsort partition flow.
- Rejected alternatives:
  - `SortCore`: too broad; the wrapper owns the sort loop and heap/insertion fallback.
  - `SortPartition`: acceptable generic role, but weaker than `UnguardedPartition` because this exact function returns a partition pair and performs unguarded scans around a pivot.
  - `FolderTreeDirectoryEntryQuickSort`: wrong scope; no recursive calls occur inside `0x004b4520`.
- Final direction: rename target/page title to `FolderTreeDirectoryEntryUnguardedPartition`, retain old name only as a search alias.

### Sibling helper names

- `0x004b5280` [UID:00034D]: best source-facing role is `FolderTreeDirectoryEntrySortLoop` / `FolderTreeDirectoryEntryIntroSort`, not merely `SortWrapper`. It is the Dinkumware/MSVC-style sort driver called by `FindFirstVisibleChild`.
- `0x004b4fd0` [UID:00034C]: current `SortPartition` name is stale. The wrapper calls it in the depth-limit/heap fallback path: first while walking parents from `count/2`, then while moving the heap end toward the beginning. Best source-facing role is `FolderTreeDirectoryEntryAdjustHeap` or `FolderTreeDirectoryEntryHeapSiftDown`.
- `0x004b44c0` [UID:00034A]: best source-facing role is `FolderTreeDirectoryEntryMedian3` / `FolderTreeDirectoryEntryOrderThree`, because it orders three records via compare/swap and is used five times by the partition target for median-of-three and ninther pivot selection.
- `0x004b4240` [UID:000349]: `FolderTreeDirectoryEntryInsertionSort` remains correct for the small-range cutoff path.
- `0x004b5510` [UID:00034E]: `FolderTreeDirectoryEntrySwap` remains correct; implementation should note it is compiler/template support equivalent to swapping a local `DirectoryEntry`.
- `0x004b3350` [UID:000343]: `FolderTreeDirectoryEntryNameLess` remains correct; implementation should document it as the comparator used by the sort expression.

### Local type / record fields

- Best local type name: `FolderTreePane::DirectoryEntry` or file-local `DirectoryEntry`. Use `FolderTreeDirectoryEntry` only in docs/search aliases if avoiding a new nested class declaration.
- Record size: `0x0c` / 12 bytes (Verified with int_convert.py).
- Field model:
  - `+0x00`: `name`, a SimpleUString/StringBase-style handle copied by `0x00582a90`, assigned by `0x00582c60`, destroyed by `0x00582b70`, and normalized for comparison by `0x004b5c40`.
  - `+0x04`: `row` or pending `treeRow`. `FindFirstVisibleChild` copies it into the new `TreeElem` word at offset `+0x18`.
  - `+0x06`: `depthOrIndent`. `FindFirstVisibleChild` copies it into the new `TreeElem` word at offset `+0x1a`.
  - `+0x08`: `childrenEnumerated` default byte. Construction zeros the two flag bytes together before insertion; consumption copies this byte into `TreeElem+0x1c`.
  - `+0x09`: `isExpanded` default byte. Consumption copies this byte into `TreeElem+0x1d`.
- Rejected field alternatives:
  - Treating `+0x04/+0x06/+0x08/+0x09` as sort-only metadata: rejected because `FindFirstVisibleChild` copies them directly into `TreeElem` fields after sorting.
  - Treating the record as a plain string vector: rejected because record movement preserves four non-string fields and the insertion/consumption code copies them into tree-node layout.

### Caller / reachability

- Raw rel32 scan over executable `.text` found exactly one direct rel32 call to target `0x004b4520`, at `0x004b52e0`.
- The only caller, [UID:00034D] at `0x004b5280`, has three rel32 refs total: external call from `FindFirstVisibleChild` at `0x004b214e`, and self-recursive calls at `0x004b5329` and `0x004b533d`.
- No direct rel32 calls or jumps from unrelated modules were found for target or its sibling sort helpers.
- Final reachability direction: target is live only through `FolderTreePane::FindFirstVisibleChild` sorting temporary child-directory entries.

### Source placement and owner

- Best owner: [UID:0000JG] `FolderTreePane.cpp`.
- Evidence for owner: the top-level sort driver is called from `FindFirstVisibleChild`, which constructs a scratch vector from `FindFirstFileA`/`FindNextFileA` visible subdirectory enumeration and then inserts sorted records into the embedded `Tree<FolderTreePane::TreeElem>`.
- Evidence against narrower owners:
  - [UID:00005A] `FolderTreePane` class is the UI class, but `00034B` is a file-local/template sort helper, not a method using `this`.
  - [UID:0001UJ] `FolderTreePane::TreeElem` owns the final 36-byte tree record, but this target sorts the 12-byte temporary directory-entry record before tree insertion.
  - [UID:0001WP] template page covers tree templates, not this directory-entry sort algorithm.
  - No separate by-file/template owner is justified because the only source-use context is `FolderTreePane.cpp`.

### First-draft C++ / no-code status

- Current metadata technically meets the code-entry gate: `RECONSTRUCTABLE:TRUE`, emitter [UID:0000JG], and average score `86`.
- Exact no-code proof for this page: the target corresponds to a compiler-emitted partition helper from a standard/library-style sort specialization, not a standalone original source function that should be written into `FolderTreePane.cpp`.
- Recommended formal code block content for [UID:00034B]:

```cpp
// Compiler-emitted std::sort/introsort unguarded partition specialization for
// FolderTreePane child-directory entries. Reconstructed by the high-level
// FolderTreePane::FindFirstVisibleChild directory-entry sort expression; no
// standalone handwritten source function should be emitted for this range.
```

This comment follows the current special-case emitter rule better than leaving the emitted marker blank, but it is intentionally not a function body.

## Evidence Standards Used

- Existing IDA MCP-backed docs: target/support pages from 2026-06-10 and later FolderTree support passes.
- Local PE/Capstone facts: image base, `.text` extraction from `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, disassembly of `0x004b4520`, `0x004b5280`, and `FindFirstVisibleChild` call windows.
- Raw rel32 xref scan: direct calls/jumps to target and sibling helper starts.
- Integer conversion: `0xaa5` / 2725 bytes, `0x0c` / 12-byte stride, `0x20` / 32-entry wrapper cutoff, `0x28` / 40-entry median/ninther target threshold, `0x8c` / 140-byte local stack reservation, and `0x2aaaaaab` / 715827883 division-by-12 reciprocal were verified with `tools/int_convert.py`.
- Negative evidence: no IDA MCP live session, no PDB/source names, no direct caller outside `FolderTreePane`, no reason to make a new source file.

## IDA MCP / Local Binary Facts

- IDA MCP status for this session: unavailable. `Invoke-WebRequest` to `http://127.0.0.1:13337/mcp` failed with "Unable to connect to the remote server." Existing docs' IDA MCP evidence was treated as prior documentation evidence and rechecked where possible through local binary disassembly.
- Binary used for local verification: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`.
- PE facts from local parser: image base `0x00400000`; `.text` starts at VA `0x00401000`.
- Target range: `0x004b4520-0x004b4fc5`, length `0xaa5` / 2725 bytes (Verified with int_convert.py).
- Prologue: SEH/security-cookie style prologue at `0x004b4520`, local stack `0x8c` / 140 bytes (Verified with int_convert.py).
- Record stride: repeated pointer arithmetic uses reciprocal `0x2aaaaaab` and 12-byte increments/decrements; record stride `0x0c` / 12 bytes (Verified with int_convert.py).
- Pivot ordering: calls [UID:00034A] five times at `0x004b45b2`, `0x004b45c3`, `0x004b45d6`, `0x004b45e7`, and `0x004b45f7`.
- Large-range pivot threshold: target uses `cmp eax, 0x28` before the ninther-style path; `0x28` / 40 entries (Verified with int_convert.py).
- String comparison/copy helpers in target:
  - `0x004b5c40`: 16 calls.
  - `0x00583450`: 16 calls.
  - `0x005840f0`: 8 calls.
  - `0x00582a90`: 7 calls.
  - `0x00582c60`: 14 calls.
  - `0x00582b70`: 23 calls.
- Return: stores two iterator pointers through hidden output pointer at `0x004b4fa8-0x004b4fb0`.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004b3350-0x004b33fc` | [UID:000343] | comparator / name-less predicate | TRUE | [UID:0000JG] | `86/88` | support name remains mostly correct |
| `0x004b4240-0x004b44b3` | [UID:000349] | insertion sort cutoff helper | TRUE | [UID:0000JG] | `86/88` | support name correct; should note template/introsort role |
| `0x004b44c0-0x004b451b` | [UID:00034A] | median-of-three/order-three helper | TRUE | [UID:0000JG] | `87/90` | support rename recommended from `CompareSwap3` to `Median3`/`OrderThree` |
| `0x004b4520-0x004b4fc5` | [UID:00034B] | unguarded partition returning iterator pair | TRUE | [UID:0000JG] | proposed `90/91` | target rename/rewrite recommended |
| `0x004b4fd0-0x004b5271` | [UID:00034C] | heap adjust/sift-down fallback helper | TRUE | [UID:0000JG] | current `85/87` | support rename recommended; current `SortPartition` is stale |
| `0x004b5280-0x004b5510` | [UID:00034D] | introsort/sort-loop driver | TRUE | [UID:0000JG] | current `86/88` | support rewrite recommended |
| `0x004b5510-0x004b55d3` | [UID:00034E] | 12-byte record swap | TRUE | [UID:0000JG] | `87/90` | support name correct |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x004b52e0` | direct rel32 call to `0x004b4520` | only direct target caller; called from the sort loop |
| `0x004b214e` | direct rel32 call to `0x004b5280` | `FindFirstVisibleChild` invokes the sort driver after directory enumeration |
| `0x004b5329`, `0x004b533d` | direct rel32 self-calls to `0x004b5280` | recursive introsort calls on partition subranges |
| `0x004b45b2`, `0x004b45c3`, `0x004b45d6`, `0x004b45e7`, `0x004b45f7` | calls to `0x004b44c0` | median/ninther pivot ordering inside target |
| `0x004b5400`, `0x004b54c0` | calls to `0x004b4fd0` from wrapper | heap adjust/sift-down fallback, not ordinary partitioning |
| `0x004b5373` | call to `0x004b4240` from wrapper | insertion sort path for ranges at or below `0x20` / 32 entries (Verified with int_convert.py) |

## Documentation Evidence And IDA Status

- Existing docs that support the conclusion:
  - [UID:000155] documents `FindFirstVisibleChild`, Win32 enumeration, and the call into the directory-entry sort wrapper.
  - [UID:000157] records the full helper cluster and exact child boundaries.
  - [UID:00034D] documents the wrapper's sole non-recursive caller and recursive call pattern.
  - [UID:00022E] documents `0x004b5c40` as local string normalization for sort comparisons.
  - [UID:0001UJ] documents the final `TreeElem` fields that receive the 12-byte directory-entry metadata.
- Existing docs that are stale or incomplete:
  - [UID:00034B] says `SortCore`, but does not identify the hidden pair return or unguarded partition role.
  - [UID:00034C] says `SortPartition`, but wrapper disassembly shows heap-adjust fallback behavior.
  - [UID:00034A] says `CompareSwap3`; correct but less source-facing than median/order-three.
  - Many sort helper pages still say "Do not emit final C++ yet" because names are inferred; this report closes the target's source-shape blocker with no-code proof rather than requiring standalone helper C++.
- Generated/coverage report state:
  - `auto-generated/-ag-memory-coverage.md` says [UID:00034B] emits to `auto-generated/NexusTK/ui/controls/FolderTreePane.cpp`, but generated C++ marks it as `Empty Emitter Marker`.
  - `by-memory/-coverage-report.md` row is current-score/stale-name and should be replaced by the exact row below during supervisor-owned coverage work.

## Ranked Ownership Analysis

### 1. [UID:0000JG] `FolderTreePane.cpp`

- Evidence for: only top-level reachability is `FolderTreePane::FindFirstVisibleChild`; target/sibling helpers operate on the scratch directory-entry vector built from filesystem child enumeration; the parent file page already owns the local tree/sort helper cluster and clears the gate.
- Evidence against: the algorithm shape is standard-library/template generated, so it is not a handwritten file-local function in the same way as UI methods.
- Decision: accepted. [UID:0000JG] remains the best canonical owner/emitter because this is the translation-unit context that instantiates the sort helper.

### 2. [UID:00005A] `FolderTreePane` class

- Evidence for: child-directory sorting occurs inside a class method and eventually inserts records into the class-owned tree.
- Evidence against: target has no `this` parameter, is called through the sort wrapper, and belongs to file-local/template support rather than class member behavior.
- Decision: rejected as direct owner; keep class as context/support, not canonical owner for this target.

### 3. [UID:0001WP] `FolderTreePaneTreeTemplates`

- Evidence for: nearby helpers are template-like support and the source-shape issue is template-generated code.
- Evidence against: `0001WP` documents the 36-byte tree/template helpers, not the 12-byte child-directory scratch vector sort. It is not a source root and should not own this target.
- Decision: rejected as direct owner; can receive a support note only if the broader FolderTreePane template/container docs are refreshed.

### 4. New source file or common sort/template owner

- Evidence for: the algorithm is standard introsort/heap/insertion support, and the 12-byte range helpers have some shared-template flavor.
- Evidence against: no direct caller outside `FolderTreePane`; the relevant source-use expression is local child-directory sorting; creating a new `DirectoryEntrySort.cpp` or generic sort owner would invent source structure not supported by caller evidence.
- Decision: rejected.

## Negative Evidence Summary

- No live IDA MCP connection was available; local PE/Capstone was used for byte and xref checks instead.
- No PDB/source/debug metadata or original helper spelling was found in the reviewed docs.
- No direct rel32 caller outside `0x004b5280` was found for target.
- No target evidence supports a standalone handwritten source function. Emitting a copied partition function would make the reconstructed source look decompiler-shaped and less like mid-2000s C++.
- Address adjacency to tree/template helpers does not prove the target belongs to the `Tree` or `TreeItor` template owners; xrefs tie it to the child-directory sort path.

## Open-Question Closure

- "What is `sub_4B4520`?" Closed as best-supported `FolderTreeDirectoryEntryUnguardedPartition`.
- "Is the target a broad sort core?" Closed: no; wrapper [UID:00034D] owns the sort loop.
- "Should formal C++ be emitted for the target?" Closed: no standalone function body. Add a comment-only code block or keep blank with explicit no-code proof; comment-only is preferred for tracker clarity.
- "Does owner/emitter need repair?" Closed: no; [UID:0000JG] remains correct.
- "Does the target need splitting?" Closed: no; exact single function range.
- "Are record field names final?" Partially closed. `name`, `row`, `depthOrIndent`, `childrenEnumerated`, and `isExpanded` are the best-supported meanings because the caller copies them into `TreeElem`; exact original local type/field spellings remain inferred and should cap score below final audit.
- "Are support helper names final?" Partially closed. `00034C` should be corrected to heap-adjust/sift-down; `00034A` should be median/order-three; wrapper should be sort-loop/introsort. Exact Dinkumware internal names remain inferred.

## First-Draft C++ / Exact No-Code Proof

Do not populate [UID:00034B] with a function body. The target is eligible by metadata, but the body is the compiler-emitted partition helper for a high-level sort expression.

Recommended target code block:

```cpp
// Compiler-emitted std::sort/introsort unguarded partition specialization for
// FolderTreePane child-directory entries. Reconstructed by the high-level
// FolderTreePane::FindFirstVisibleChild directory-entry sort expression; no
// standalone handwritten source function should be emitted for this range.
```

Recommended source-level sketch for the owning `FindFirstVisibleChild`/sort support documentation, not for this target's formal block:

```cpp
struct FolderTreePane::DirectoryEntry {
    SimpleUString name;
    short row;
    short depthOrIndent;
    unsigned char childrenEnumerated;
    unsigned char isExpanded;
};

static bool DirectoryEntryNameLess(const FolderTreePane::DirectoryEntry& lhs,
                                   const FolderTreePane::DirectoryEntry& rhs)
{
    return CompareNormalizedDirectoryEntryName(lhs.name, rhs.name) < 0;
}

// Source-level expression that causes 0x004b4520 and sibling sort helpers:
std::sort(entries.begin(), entries.end(), DirectoryEntryNameLess);
```

This sketch should be placed only where the owning `FindFirstVisibleChild` or sort wrapper reconstruction is being documented; it should not be copied into [UID:00034B] as formal emitted C++.

## Final Recommendation

- Exact target changes recommended:
  - Rename file/title from `0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md` to `0x004b4520-0x004b4fc5.FolderTreeDirectoryEntryUnguardedPartition.md`.
  - Update heading to `0x004b4520-0x004b4fc5 FolderTreeDirectoryEntryUnguardedPartition`.
  - Set `COMPLETION:90`, `CONFIDENCE:91`.
  - Keep `CANONICAL_OWNER:0000JG`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JG`.
  - Replace "source-authored local helper" prose with "source-declared/generated-binary template support emitted from FolderTreePane's directory-entry sort expression."
  - Add local PE/Capstone evidence and raw rel32 xref scan facts.
  - Add exact no-code proof and recommended comment-only code block.
- Exact support changes recommended:
  - [UID:00034D]: rename/prose update to `FolderTreeDirectoryEntrySortLoop` / introsort driver; document `0x20` / 32-entry insertion cutoff, `FindFirstVisibleChild` caller, recursive partition handling, and heap fallback.
  - [UID:00034C]: rename/prose update to `FolderTreeDirectoryEntryAdjustHeap` / heap sift-down helper; remove stale "partition/merge" language.
  - [UID:00034A]: update prose/name to median/order-three helper while preserving compare/swap behavior.
  - [UID:000343], [UID:000349], [UID:00034E], [UID:000348], [UID:00022E]: add short cross-notes that these are pieces of the same compiler-emitted sort/vector support for `FolderTreePane::DirectoryEntry`.
  - [UID:000155], [UID:0000JG], [UID:00005A], [UID:0001UJ]: add source-shape note that `FindFirstVisibleChild` constructs local `DirectoryEntry` records, sorts them by normalized name, then inserts sorted child nodes; preserve field-name caveats.
- Exact items left no-owner/non-emitting: none for target. The target remains owned and emitted to [UID:0000JG], but emits no standalone body by proof.
- Future work outside this assignment: full final C++ for `FindFirstVisibleChild` and the whole `FolderTreePane` class still needs a broader class/file implementation pass.

## Supervisor-Owned Coverage Row

Placement context: replace the current [UID:00034B] row under the nested [UID:000157] `FolderTreePaneTreeAndSortHelpers` children in `by-memory/-coverage-report.md`, between [UID:00034A] and [UID:00034C]. If the implementation renames the file as recommended, use this exact replacement row:

```markdown
        - [UID:00034B][0x004b4520-0x004b4fc5.FolderTreeDirectoryEntryUnguardedPartition](by-memory/0x004b4520-0x004b4fc5.FolderTreeDirectoryEntryUnguardedPartition.md) 0x004b4520-0x004b4fc5 | template/introsort partition helper | FolderTreeDirectoryEntryUnguardedPartition : reconstructable : 90% : very strong : Assigned to [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md); B008 source-quality reanalysis identifies this range as the compiler-emitted std::sort/introsort unguarded partition specialization for the local 12-byte FolderTreePane directory-entry record, not a standalone handwritten sort core. Local PE/Capstone and existing IDA-backed docs confirm exact `0xaa5` / 2725-byte range (Verified with int_convert.py), `0x0c` / 12-byte record stride (Verified with int_convert.py), sole direct caller at `0x004b52e0` inside the sort loop, hidden two-pointer partition-pair return at `0x004b4fae-0x004b4fb0`, five median/order-three calls to [UID:00034A], 16 normalized-string preparation calls to [UID:00022E], 8 wide-string comparisons, repeated copy/swap/destruct handling for string-plus-row/depth/flag records, and exact no-code proof because source reconstruction should emit the high-level FolderTreePane directory-entry sort expression rather than this partition helper body.
```

If the supervisor rejects the rename but accepts the source-quality content, use the same row text with the existing link label/path `0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore`.

## Target / Support Implementation Checklist

1. Lease the target and all support docs before editing.
2. Rename target file and title to `FolderTreeDirectoryEntryUnguardedPartition`, or document the old name as a stale/search alias if rename is deferred.
3. Update target metadata to `90/91`; keep owner/emitter [UID:0000JG].
4. Insert the full source-quality narrative from this report: unguarded partition role, hidden output pair, median/ninther pivot ordering, 12-byte record layout, caller/reachability, no split, no new owner, no standalone C++.
5. Insert the comment-only formal C++ block or an equivalent explicit no-code marker; do not emit a function body.
6. Update [UID:00034D] to explain the sort loop/introsort driver and source-level sort expression.
7. Update [UID:00034C] to correct `SortPartition` to heap adjust/sift-down fallback.
8. Update [UID:00034A] to identify median/order-three behavior.
9. Update [UID:000343], [UID:000349], [UID:00034E], [UID:000348], and [UID:00022E] with cross-notes only as needed to keep naming/source-shape consistent.
10. Update [UID:000155], [UID:0000JG], [UID:00005A], and [UID:0001UJ] only with facts directly necessary to preserve the local `DirectoryEntry` record/source-shape decision and field caveats.
11. Do not edit `by-memory/-coverage-report.md`; leave the exact row above for supervisor-owned application.
12. Run scoped validators for every edited target/support file and report command, exit code, and `ok` count.

## Follow-Up Actions

- Supervisor actions: review this report, accept/reject rename and no-code recommendation, then send B008 an implementation checklist if accepted.
- A-agent actions: none required before supervisor review.
- B008 future research actions: if assigned implementation, apply the target/support docs claim-by-claim, validate, and leave exact coverage row text.

## Confidence

- Recommendation confidence: high.
- Score confidence: medium-high; `90/91` is justified after implementation but not higher because IDA MCP was unavailable and original symbol names remain inferred.
- Remaining uncertainty: exact original spelling of the local record type and whether the source call was direct `std::sort` or a small project wrapper around `std::sort`.

## Validator Results

- Commands run: none. Report-only assignment; no by-* docs were edited.
- Results: not applicable.
- Any unresolved validator warnings/errors: not checked.

## Changed Files

- Created: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\Agent-B008\research\00034B-FolderTreeDirectoryEntrySortCore-source-quality.md`
- Modified: none outside this report.
- Renamed: none.
- Moved to executed: none.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B008","command_id":"000000004165","destination_path":"executed-b-agent-research/B008/00034B-FolderTreeDirectoryEntrySortCore-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:33","uid":"00034B"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
