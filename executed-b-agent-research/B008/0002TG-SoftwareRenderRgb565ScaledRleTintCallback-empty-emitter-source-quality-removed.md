<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\Agent-B008\research\0002TG-SoftwareRenderRgb565ScaledRleTintCallback-empty-emitter-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable / Transaction Material Archive

- Source report: [0002TG-SoftwareRenderRgb565ScaledRleTintCallback-empty-emitter-source-quality.md](0002TG-SoftwareRenderRgb565ScaledRleTintCallback-empty-emitter-source-quality.md)
- Status: non-authoritative preservation archive; this is not a B-agent report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: this material must never be executed, imported, evaluated, replayed, or used as operational instruction.
- Scope: exact canonical-session/attestation/backup/save/reopen runbook and request-shaped IDA actions removed from the source report.

## Removed Block R001

~~~text
All actions are supervisor-owned and conditional on a freshly authenticated exact-canonical session binding, serialized exclusive mutation ownership, and a fresh item-by-item prestate recheck that exactly reproduces the literals below. The safe active subset is deliberately limited to one pure function rename and two comments. The prior eight stack renames, eight stack types, and function `set_type` proposal are rejected as stale handoff mechanics: read-only evidence proves the source ABI, but it does not prove the exact deterministic physical frame that those mutations would install. Source-facing CPP retains the inferred names and types; IDA keeps the current literal stored signature `void __thiscall(const struct GrafPort *this, int, _DWORD *, int *, char, float, int, float, float)` and frame until a separate mutation-specific pass can prove a complete poststate.

Each active mutation receives an immediate readback; cumulative state is reread before save and from a fresh reopened persisted copy after save. Any unexpected range, bytes, xrefs, frame row, name collision, comment-channel delta, data-boundary change, or protected-neighbor change requires stopping without save. A01 uses isolated `rename` dry-run and apply calls with `pure:true`; this is strictly name-only and skips function-directory plus Hex-Rays/decompiler refresh/cache side effects. Any such side effect is a contract failure. A02 uses `set_function_comments`. A03 uses `set_address_comments`. These are literal data handoffs, not executable packages.

### Canonical Session, Runtime Attestation, And Database Binding

| Phase | Literal public request or binding | Required result and fail-closed disposition |
| --- | --- | --- |
| S01 canonical selection, adoption, or open | Begin with fresh `idb_list {}` and normalize every returned `input_path`. If exactly one active, non-analyzing session is bound to `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, record its exact `session_id` as the S02 candidate; an already-adopted session is permitted and its label is immaterial. If no exact-path session exists, choose a fresh requested label and issue `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64",preferred_session_id:"<FRESH_REQUESTED_CANONICAL_SESSION_ID>",mode:"prefer_headless",run_auto_analysis:false}`, then record the nonempty returned `structuredContent.session.session_id` as the candidate. If `idb_open` adopts/reuses an existing exact-path session, that reuse is permitted and is not by itself a collision. | Require one unambiguous exact-path candidate, no top-level discovery/open errors, and either the fresh `idb_list` identity or a successful `idb_open` response whose returned path normalizes exactly to the canonical path. Multiple exact-path candidates, inactive/analyzing state, wrong/ambiguous path, missing identifier, or open/discovery error stops before backup or mutation. Record the candidate as `<BOUND_CANONICAL_SESSION_ID>` only after S02 succeeds. Session-label equality/reuse is neither authority nor a collision; a mutation collision means supervisor coordination identifies another active mutation owner. |
| S02 public runtime attestation, health, and exclusive ownership | For the exact S01 candidate issue fresh `server_health {database:"<S01_CANDIDATE_SESSION_ID>"}` and `runtime_attestation {expected_database:"<S01_CANDIDATE_SESSION_ID>",expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"}`. Independently record that the primary supervisor has serialized exclusive mutation ownership for the full backup/mutation/save boundary. | Require health `status:ok`, exact IDB path, a fresh top-level attestation with `schema_version:1`, `ok:true`, empty errors, fresh listener nonce/generation, and one exact active worker route for the candidate session/path with a stable worker generation/parent route and matching loaded-module provenance. Public `tools/list` must contain `runtime_attestation` and omit internal `runtime/attest`. Exclusive ownership must identify this primary-supervisor boundary and confirm no other active IDA mutation; inability to establish it is a hard stop. After these checks, bind the exact candidate as `<BOUND_CANONICAL_SESSION_ID>`. A reused historical label does not weaken or strengthen authority. |
| S03 prestate and per-operation database contract | Before backup or mutation, bind every target/protected read to top-level `database:"<BOUND_CANONICAL_SESSION_ID>"` and freshly reproduce all literal A01-A03, full 27-row frame, P01-P05, boundary, byte, type, comment, collision, and xref prestates. Every later A01 dry run/apply, A02, A03, immediate/cumulative/final readback, protected-dependency readback, and save carries that same explicit database binding. No request may omit `database`, use an implicit/default session, use a label without S01-S02 authentication, or use an alternate coherent IDB copy. | Any prestate mismatch stops before backup or mutation. Re-run fresh `idb_list`, health, and public attestation immediately before each stateful mutation boundary and before the sole save; require the same exact session/path/worker generation and empty errors, and reconfirm serialized exclusive mutation ownership. If another mutation owner becomes active, treat that as a collision and stop without save. Session reuse alone is not a collision. Any route, generation, ownership, path, tool-provenance, or literal-state drift fails closed. |
| S04 durable backup, readback, sole save, and guarded rollback | Immediately after S03 succeeds and before any mutation, record canonical `<PRECHANGE_SHA256>` and `<PRECHANGE_SIZE>`. Choose `<PRECHANGE_BACKUP_PATH>` only under `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\ida-backups\` with stem `NexusTK.exe.uid0002TG-prechange-<fresh-timestamp>-<fresh-nonce>.i64`; require that the normalized path did not exist, is inside that fixed directory, is not a reparse/alias/hard-link route to canonical, and after one physical copy is an ordinary distinct file identity whose SHA256/size equal `<PRECHANGE_SHA256>`/`<PRECHANGE_SIZE>`. Keep it immutable. A01 readback covers name/collision/range/type/frame/bytes/body hash/exact xrefs and all four comment channels; A02 repeats them with only the exact function-regular comment delta; A03 repeats cumulative target state plus the exact padding item/bytes/name/type/comment channels and exact predecessor/successor boundary state. Re-run S02-S03 before each mutation and before the sole `idb_save {database:"<BOUND_CANONICAL_SESSION_ID>"}`. | Any pre-save mismatch discards the unsaved canonical worker without `idb_save`; canonical disk must still hash to `<PRECHANGE_SHA256>`/`<PRECHANGE_SIZE>`. Save exactly once only after every immediate, cumulative, A01-A03, P01-P05, full-frame, range, byte, hash, xref, type, and untargeted-channel readback matches. Record the bound session ID and canonical `<POSTSAVE_SHA256>`/`<POSTSAVE_SIZE>`, which must differ from prechange only through the authorized persisted metadata. A post-save failure must not trigger a blind restore: rollback is allowed only after re-authenticating the immutable backup and proving canonical still equals the just-recorded post-save identity; then restore from that exact backup and require canonical to equal prechange hash/size. Any identity, path, reparse, alias, hard-link, process-route, or hash ambiguity stops for manual supervisor recovery without further save or overwrite. |
| S05 fresh persisted verifier | After S04 save, choose `<POSTSAVE_VERIFIER_PATH>` only under the same fixed `ida-backups` directory with stem `NexusTK.exe.uid0002TG-postsave-verify-<fresh-timestamp>-<fresh-nonce>.i64`. Require a nonexisting normalized path, ordinary non-reparse file, distinct physical identity with no alias/hard link to canonical or the prechange backup, and one physical copy whose SHA256/size exactly equal `<POSTSAVE_SHA256>`/`<POSTSAVE_SIZE>`. Choose a fresh verifier label and issue `idb_open {input_path:"<POSTSAVE_VERIFIER_PATH>",preferred_session_id:"<FRESH_REQUESTED_VERIFIER_SESSION_ID>",mode:"prefer_headless",run_auto_analysis:false}`. Require `success:true`, capture the exact returned `structuredContent.session.session_id` as `<RETURNED_VERIFIER_SESSION_ID>`, require returned `session.input_path` to normalize exactly to `<POSTSAVE_VERIFIER_PATH>`, bind every verifier read to `database:"<RETURNED_VERIFIER_SESSION_ID>"`, and attest with `runtime_attestation {expected_database:"<RETURNED_VERIFIER_SESSION_ID>",expected_canonical_path:"<POSTSAVE_VERIFIER_PATH>"}`. Symbolic tokens are filled only from fresh values and are never literal IDs. | Collision, path/identity, copy-hash, returned-session, or attestation mismatch stops. Require exact persisted A01-A03 poststate plus unchanged P01-P05, complete 27-row frame, target/predecessor/padding/successor items, bytes, ranges, hashes, exact xrefs, types, and untargeted comment channels. The verifier is read-only for this gate: no second `idb_save`, no mutation, and no authority transfer from its fresh-copy path back to canonical. |

The dated witnesses `supervisor-b007-uid00034w-postsave-verify-20260810`, `supervisor-uid00048A-persist-20260812`, and `supervisor-canonical-20260812T-current` remain read-only research provenance only. The prior UID0002TG operational hold is also dated history: `idb_open` reused already-adopted exact-path session `supervisor-uid0002OW-gate2b-20260813T0115Z`, while the former contract required a newly labeled UID0002TG session and rejected that binding before backup or mutation. No IDA effect or claim credit resulted. Fresh 2026-08-13 `idb_list`, health, public attestation at `2026-08-13T04:34:29.647811+00:00`, and bounded item-by-item reads now show that same session healthy on the exact canonical path and reproduce the target/action/protection literals, including the stored `const struct GrafPort *this` receiver, complete frame, empty comment channels, boundaries, types, and 2/21/12/11/60/37 xref counts; physical canonical identity remains SHA256 `B8C1ABC6A9FBCBE1B2021BC72AC15AD7EAAD0EF3DD82443ADEFCFC0ADE0151F9`, 143,206,895 bytes. This does not retroactively make the session authoritative. It is an eligible already-adopted S01 candidate only when the primary supervisor freshly repeats S01-S03, establishes serialized exclusive mutation ownership, and finds every literal prestate exact. A reused label alone is neither authority nor collision; another active mutation owner is a collision. S01-S03 fill `<BOUND_CANONICAL_SESSION_ID>` from the authenticated candidate, while S05 independently fills `<RETURNED_VERIFIER_SESSION_ID>` from its fresh response. No hard-coded session ID is authoritative.

| ID | Entity/address | Literal current prestate | Exact MCP request schema / disposition | Evidence | Expected readback | Safety and protected state |
| --- | --- | --- | --- | --- | --- | --- |
| A01 | function `0x004c3a50` | current read-only witness name `sub_4C3A50`; range `[0x004c3a50,0x004c3ce1)`; size `0x291`; stored signature `void __thiscall(const struct GrafPort *this, int, _DWORD *, int *, char, float, int, float, float)`; body SHA256 `CF941A937FF9CF24FA19A2421020BBE42B03FFBA9EDC02C00FAB427F6C68A6F7`; exactly two data/setup refs at `0x00558829` and `0x00558ec4`, no direct code caller; address-regular absent; address-repeatable absent; function-regular absent; function-repeatable absent; proposed-name collision absent | dry run `rename {batch:{func:{addr:"0x004c3a50",name:"SoftwareRenderRgb565ScaledRleTintCallback"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<BOUND_CANONICAL_SESSION_ID>"}`, then the identical apply request with `dry_run:false`; fill the database token only from the authenticated S01-S03 binding | initializer, provider-family naming, Surface ownership | database-bound lookup/name/comment/frame/byte/xref readback: exact name `SoftwareRenderRgb565ScaledRleTintCallback`; address-regular absent; address-repeatable absent; function-regular absent; function-repeatable absent; exact same range, size, stored signature, complete 27-row frame, bytes/body SHA256, refs `0x00558829`/`0x00558ec4`, and no code caller | `pure:true` permits only the intended IDA name change and explicitly skips function-directory and Hex-Rays/decompiler refresh/cache effects; any such effect or any type/frame/bytes/xref/item/comment-channel delta fails closed; preserve exact predecessor table and successor padding/function boundary |
| A02 | function `0x004c3a50` | current physical entry name `sub_4C3A50`; required post-A01 entry name `SoftwareRenderRgb565ScaledRleTintCallback`; range `[0x004c3a50,0x004c3ce1)`; stored signature `void __thiscall(const struct GrafPort *this, int, _DWORD *, int *, char, float, int, float, float)`; body SHA256 `CF941A937FF9CF24FA19A2421020BBE42B03FFBA9EDC02C00FAB427F6C68A6F7`; complete frame is exactly rows 1-27 above and no others; exact refs `0x00558829`/`0x00558ec4`, no code caller; address-regular absent; address-repeatable absent; function-regular absent; function-repeatable absent before and immediately after A01 | `set_function_comments {items:{addr:"0x004c3a50",comment:"RGB565 slot-4 provider; clips and scales signed zero-delimited EPF mask rows, then tints existing destination pixels through 5/6-bit lookup tables."},database:"<BOUND_CANONICAL_SESSION_ID>"}`; fill the database token only from the authenticated S01-S03 binding | exact source role and body behavior | database-bound comment/function/frame/byte/xref readback: function-regular exactly `RGB565 slot-4 provider; clips and scales signed zero-delimited EPF mask rows, then tints existing destination pixels through 5/6-bit lookup tables.`; address-regular absent; address-repeatable absent; function-repeatable absent; A01 name retained; exact same range/signature/27-row frame/bytes/body SHA256/refs and no code caller | No address-comment, function-repeatable, type, frame, name, item, function-directory, decompiler-refresh/cache, or neighbor delta; any nonliteral comment-channel result fails closed |
| A03 | padding `0x004c3ce1` | dated data item `[0x004c3ce1,0x004c3cf0)`, width 15; bytes are fifteen `CC`; name absent; type absent; regular absent; repeatable absent; exact predecessor `[0x004c3a40,0x004c3a50)` bytes `11 33 4c 00 26 34 4c 00 f0 34 4c 00 9a 37 4c 00`; next function `SoftwareRenderRgb565AlphaLookupBlitCallback` is `[0x004c3cf0,0x004c3e93)`, size `0x1a3` | `set_address_comments {items:{addr:"0x004c3ce1",comment:"15-byte alignment between RGB565 scaled-RLE-tint and alpha-lookup callbacks."},database:"<BOUND_CANONICAL_SESSION_ID>"}`; fill the database token only from the authenticated S01-S03 binding | exact alignment boundary | database-bound item/comment/byte/neighbor readback: regular exactly `15-byte alignment between RGB565 scaled-RLE-tint and alpha-lookup callbacks.`; repeatable absent; same padding item/range/bytes/name/type; exact predecessor bytes unchanged; next function still named `SoftwareRenderRgb565AlphaLookupBlitCallback` at `[0x004c3cf0,0x004c3e93)`, size `0x1a3` | Do not create code, name, or type; do not touch any byte outside `[0x004c3ce1,0x004c3cf0)`, the next function, or predecessor table; any boundary/name/range/size/byte or untargeted-comment delta fails closed |

These rows are read-only protection contracts, not mutation actions. Their literals were reproduced by the 2026-08-10, 2026-08-12, and fresh 2026-08-13 read-only passes, and Gate 2B must reproduce them again through the freshly authenticated `<BOUND_CANONICAL_SESSION_ID>` before backup or the first mutation, in the cumulative pre-save readback, and through `<RETURNED_VERIFIER_SESSION_ID>` after reopening the persisted copy. No P row authorizes a rename, type, item-boundary, comment, or other IDA effect; any fresh mismatch cancels all actions rather than being normalized to an older witness.
~~~

## Removed Block R002

The following additional prose was removed from the source because it prescribed runtime/session, ownership, backup, persistence, or lifecycle ordering rather than recording research facts or declarative intended states. It is preserved verbatim only as historical material and must never be followed.

~~~text
Gate 2B must dynamically discover and bind the exact canonical-IDB session through fresh `idb_list`, `server_health`, and public `runtime_attestation`, verify the exact path/session/healthy worker generation with empty errors, acquire serialized exclusive supervisor mutation ownership, and recheck every literal prestate before backup or mutation. The bound session may be an already-adopted exact-path session or one returned by a new `idb_open`; a reused label is not authority and is not by itself a collision. A collision exists when another active mutation owner overlaps the action boundary.

Under the repaired contract, this session is only an eligible Gate 2B candidate; it becomes mutation authority solely after the primary supervisor freshly repeats S01-S03, records serialized exclusive mutation ownership, and confirms every literal prestate.

Primary supervisor order: first complete fresh exact-artifact Gate 1; second perform Gate 2B for C2TG-051 through C2TG-059 with only A01-A03 as mutations, using S01-S03 to discover/adopt-or-open and freshly attest the exact canonical session, establish serialized exclusive mutation ownership, and reread every literal prestate before backup or mutation; third perform lifecycle handling.

Gate 2B must repeat that collision check immediately before A01 dry-run and apply.

Each relevant gate and the post-move audit must dynamically reread the tracker, and Gate 2B must freshly discover/adopt-or-open, health-check, and publicly attest its exact canonical session, bind the returned verifier session independently, record serialized exclusive mutation ownership, and reread every literal prestate.
~~~
