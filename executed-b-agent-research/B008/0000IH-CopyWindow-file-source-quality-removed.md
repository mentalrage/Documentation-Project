# Inert Removed Operational Persistence Material

Source report: [0000IH-CopyWindow-file-source-quality.md](0000IH-CopyWindow-file-source-quality.md)

This sibling contains only historical operational persistence material removed verbatim from the source report. It is non-authoritative and must never be executed. It is not a research report, must never be submitted to `execute_report`, and receives no Gate 1, Gate 2, coverage, score, IDA, validator, or lifecycle credit.

## Removed From Finalized Report / Current Recommendation

- The pending Gate 2B handoff is phase-separated and fail closed. Every applicable attestation boundary requires literal top-level `schema_version:1`, `ok:true`, `errors:[]` and literal worker-local `schema_version:1`, `ok:true`, `errors:[]`; listener validation uses only its actual role/nonce/generation/ownership/route/source/module fields and never listener-local `ok` or `errors`. Mutation uses only returned `DB`; saved-TX verification uses fresh `DB_VERIFY`; normal canonical persistence uses fresh `DB_FINAL`; post-promotion failure classification uses fresh `DB_ROLLBACK`; restored-baseline verification uses fresh `DB_RESTORE`. Promotion consumes only a closed/reopened/rehashed `PROMOTION_CANDIDATE` exclusively created from saved TX in CANON's exact parent, and restoration consumes only an equivalently sealed distinct `RESTORE_CANDIDATE` created from BACKUP there; each path is atomically consumed with durable file/parent-directory flushes and no residual second artifact. The five DB roles, sessions, phase-local baselines, and two physical candidate identities cannot be reused, aliased, or substituted, and the report grants no save, promotion, rollback, restore, or lifecycle authority to B008.

## Removed From Supporting Research

- Historical Gate 1 role/schema repair started from report SHA256 `81A4D13E0531D72DFAD154A4797B6FF37611054F0C4552A9E2E86113CE8705DC` and produced the now-superseded rejected artifact `60925B1369DE17EADE6672FD3E943401565F93E89EF656A6CCDEB77CB654D932`. It added literal top-level/worker-local status, listener's actual schema, saved-TX `DB_VERIFY`, distinct `DB_FINAL`/`DB_ROLLBACK`/`DB_RESTORE`, and no phase-session reuse; those accepted corrections remain current.

- Historical Gate 1 sealed-candidate repair started from rejected report SHA256 `60925B1369DE17EADE6672FD3E943401565F93E89EF656A6CCDEB77CB654D932`. It changed only this report's supervisor-owned physical persistence handoff by adding exact-parent `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE`, exclusive collision/path-distinct creation, durable candidate-file and parent-directory metadata flushes, complete handle closure, independent reopen/receipt/rehash, one guarded same-directory atomic consumption, and no-residue/no-second-artifact proof. It repeated no ordinary implementation and changed no IDA or generated/documentation state.

## Removed From Supervisor Active Recheck

- At Gate 2B, use the complete Section 21 phase-separated contract: independent CANON/BACKUP/TX receipts; public `tools/list`; literal top-level `schema_version:1`, `ok:true`, `errors:[]` and literal worker-local `schema_version:1`, `ok:true`, `errors:[]`, with no listener-local `ok`/`errors`; mutation only through returned `DB` and the sole exact save; proven mutation-route retirement; fresh saved-TX `DB_VERIFY`; and retirement of every CANON/TX-capable route before candidate creation. Promotion must exclusively create, file-and-parent durably flush, close, independently reopen/receipt/rehash, and atomically consume exact-parent `PROMOTION_CANDIDATE` with path-distinctness and no residue before fresh `DB_FINAL`. After distinct read-only `DB_ROLLBACK` classification and retirement, restoration must perform the symmetric BACKUP-only `RESTORE_CANDIDATE` contract and prove no residue before distinct read-only `DB_RESTORE`. No phase/session, baseline, or candidate identity is reusable, and any mismatch is a no-save/no-promotion/no-restore-success hard stop.

## Removed From IDA Rename / Type / Comment Recommendations

Gate 2B startup is supervisor-only and must precede every target preflight or mutation. Bind `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` only as physical canonical role `CANON`; before any open, independently receipt CANON, create a collision-safe durable physical `BACKUP`, create a separate isolated physical transaction copy `TX`, close every copy/receipt handle, and prove BACKUP and TX byte-identical to CANON by exact normalized path, size, and SHA256. `runtime_attestation` supplies none of those physical values. A creation, collision, closure, path, size, or hash mismatch stops before IDA opens.

Public-tool-surface contract: on every fresh listener connection for mutation `DB`, saved-TX verification `DB_VERIFY`, final canonical verification `DB_FINAL`, failed-canonical classification `DB_ROLLBACK`, or restored-baseline verification `DB_RESTORE`, and after any listener generation change, call public `tools/list`. Every result must expose current public `idb_open`, exactly one current `make_data({database,items:[{addr,type,name?,delete_existing?}]})`, the current `idb_save({database,path}) -> {ok,error?,path}` schema, exactly one public `runtime_attestation`, and no advertised internal `runtime/attest`. Never call `runtime/attest`. A missing/duplicate tool, advertised internal RPC, schema drift, or surface change retires that phase and stops mutation, save, candidate creation, replacement, or success classification.

Only after public discovery passes may the supervisor call public `idb_open` with `{"input_path":"<TX: exact verified isolated transaction-copy path>","mode":"force_headless","run_auto_analysis":false,"build_caches":true,"init_hexrays":true,"idle_ttl_sec":600,"preferred_session_id":""}`. Require `success:true`, no `error`, a present session, exact normalized returned `session.input_path` equal to sealed `TX`, a nonempty returned `session.session_id`, and successful cache/Hex-Rays warmup; bind only that exact returned session string as `DB`. The preferred request value, canonical session/path, remembered or dated session, `idb_list`, attestation response, or report-chosen alias cannot create or substitute for `TX` or `DB`.

Attestation schema contract: every applicable boundary is a fresh public `runtime_attestation` call with the exact current phase's returned database ID and exact normalized TX or CANON path. The response must contain literal top-level `schema_version:1`, `ok:true`, `errors:[]`; the exact phase worker object must independently contain literal worker-local `schema_version:1`, `ok:true`, `errors:[]`. Those are the only schema/ok/errors status surfaces required here. The listener object has no listener-local `ok` or `errors`; do not require, synthesize, infer, or fail on their absence. Instead fail closed on the listener's actual role, fresh nonce/generation, endpoint/port ownership, PID/process/parent/generation route, source-root containment, role manifest, and loaded modules, plus the worker's exact session/path/route ownership. The response proves runtime identity/path/time/state, never physical IDB size/SHA256.

Phase-baseline contract: the first post-open package for each of `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` independently seals that phase's complete listener-role loaded-module baseline and complete worker-role loaded-module baseline, including exact `file`, `spec_origin`, `resolved_path`, proven `source_root` containment, size, timestamps, and SHA256. Later packages in that same phase must match each role only to its own complete phase-local baseline, with no missing, extra, or divergent row. Never compare listener and worker manifests to each other, and never carry a baseline, nonce, generation, PID, route, session, or result into another phase. A generation change is not continuity: retire the affected phase and restart only where the boundary permits a fresh returned role.

Independent physical-receipt contract: every CANON/BACKUP/TX/`PROMOTION_CANDIDATE`/`RESTORE_CANDIDATE` path, parent, size, file identity, link/alias state, or SHA256 assertion comes from a separate supervisor filesystem receipt, never from `runtime_attestation`. Preserve sealed pre-open CANON/BACKUP/TX receipts; pair mutation packages with fresh receipts wherever unchanged disk identity is asserted; independently receipt saved TX after the sole save and during DB_VERIFY; and separately receipt each closed candidate, final CANON, failed CANON, restored CANON, BACKUP, candidate-path absence, and exact-parent no-residue state at the defined boundaries. A candidate receipt occurs only after durable file-data/file-metadata and containing-directory-metadata flushes plus closure of every creating/flush handle; then independently reopen the candidate operationally read-only to record normalized path/parent/file identity/link state/size/SHA256 and rehash against its verified source, close every receipt/hash handle, and prove closed state with a fresh exclusive read-only `FileShare.None` probe before replacement. Any unavailable, ambiguous, aliased, open, unflushed, residual, or mismatched receipt is a no-save/no-promotion/no-restore-success hard stop.

Mutation schedule: after isolated-TX `idb_open` returns a nonempty exact TX session, bind only that return as `DB`. Take complete fresh post-open A0 before target read and pre-mutation A1 after collision/prestate/protection reads; then S1 after IDA-IH-01, S2 after IDA-IH-02..05, S3 after IDA-IH-06..08, S4 after IDA-IH-09..11, S5 after IDA-IH-12..14, and S6 after IDA-IH-15A..15F. Each package includes current tool-surface proof where applicable, the complete Attestation schema contract, mutation-phase role baselines/routes, and separate physical receipts. Replace every descriptive `database` placeholder in IDA-IH requests/readbacks byte-for-byte with `DB`; no request may use CANON, a path, another phase ID, another worker, or any unreturned identifier.

Save and mutation-retirement boundary: after S6 and complete protected-state readback, take fresh pre-save package and CANON/BACKUP/TX receipts. The sole save is literally `idb_save({database:DB,path:TX}) -> {ok,error?,path}` with exact DB/TX substituted; require `ok:true`, `error` absent or empty, and returned normalized `path` exactly TX. Then independently compute the saved-TX size/SHA256, take a fresh post-save mutation package, close the mutation connection/worker route, and prove `DB` retired by exact worker PID/process absence with PID-reuse checks, no active TX session, unchanged closed saved-TX receipt, and a successful read-only exclusive `FileShare.None` probe. A stale inactive registry row is non-authoritative; an active/ambiguous worker, PID ambiguity, failed exclusive probe, or receipt mismatch forbids verification and promotion.

Saved-TX verification boundary: only after mutation-route retirement, create a fresh listener connection, repeat public `tools/list`, and reopen the exact same physically verified saved TX under an operationally read-only policy. Bind only that fresh `idb_open` return as `DB_VERIFY`; it must differ from `DB`. Do not copy TX to a separate verifier file or assign any alternate verifier identifier. Under independent DB_VERIFY listener/worker baselines, require fresh post-open, pre-read, and post-read packages, separate closed-TX receipts, and complete persisted action plus protected-state readback. DB_VERIFY has no mutation or save authority. After successful post-read, close its connection/worker route and prove retirement by the same process/session/receipt/exclusive-probe contract.

`PROMOTION_CANDIDATE` creation boundary: only after DB and DB_VERIFY plus every CANON/TX-capable route are proven retired, define `CANON_PARENT` as CANON's exact normalized parent directory and freshly reverify closed saved TX, original CANON, and original BACKUP. Exclusively create one previously nonexistent `PROMOTION_CANDIDATE` whose normalized/resolved parent is exactly CANON_PARENT and whose normalized path and file identity are distinct from CANON, BACKUP, TX, RESTORE_CANDIDATE, every prior candidate, and every session input path. Reject case-fold/path aliasing, hard-link or reparse identity, a preexisting object, a second candidate name/object, or any collision. Populate it only from the already verified saved-TX bytes; no runtime role may open or mutate it.

`PROMOTION_CANDIDATE` seal boundary: durably flush the complete candidate file data and file metadata, durably flush CANON_PARENT directory metadata recording its creation, and close every source, destination, file, and directory handle. Then independently reopen PROMOTION_CANDIDATE operationally read-only only long enough to prove exact normalized path/parent, one distinct file identity with no alternate link/alias, size, and SHA256 and to rehash it exactly equal saved TX; close every receipt/hash handle and prove closed state with a fresh exclusive read-only `FileShare.None` probe. Immediately before replacement, reprove DB/DB_VERIFY and all CANON/TX routes retired, candidate still sealed/equal saved TX, and CANON/BACKUP still equal their original receipts. Any mismatch invalidates the candidate and forbids replacement.

`PROMOTION_CANDIDATE` atomic-consumption boundary: only that exact closed/reopened/rehashed candidate may be the source of one supervisor-owned guarded same-directory atomic replacement of CANON; reject copy/delete fallback, cross-directory movement, a different source, overwrite drift, or a second attempt. On success, durably flush promoted CANON file data/metadata and CANON_PARENT replacement/removal metadata, close every replacement/flush handle, and independently reopen/receipt/rehash CANON equal saved TX while BACKUP remains original. Prove the PROMOTION_CANDIDATE path is absent, its pre-replacement file identity is reachable only as CANON, no hard-link/alias or prior/second candidate path survives, and an exact-parent scan/receipt exposes no second promotable artifact. Only this atomic consumption plus no-residue proof permits DB_FINAL; this report grants no promotion authority.

Normal final-persistence boundary: after guarded replacement and complete handle closure, independently receipt CANON exactly equal to saved TX, establish a fresh listener connection, repeat public `tools/list`, and reopen exact CANON operationally read-only. Bind only that return as fresh `DB_FINAL`, distinct from every earlier ID. Under new independent DB_FINAL listener/worker baselines, require fresh post-open, pre-read, and post-read packages, final physical CANON identity equal saved TX, and complete action/protection readback. Persistence is proven only after all checks and DB_FINAL route retirement; DB_FINAL has no mutation/save/replacement/promotion authority.

Failed-canonical classification boundary: on any post-promotion DB_FINAL failure or ambiguity, retire every failed/partial DB_FINAL route first. Then freshly reopen exact CANON operationally read-only and bind only that new return as distinct `DB_ROLLBACK`. Under its own independent listener/worker baselines, require fresh post-open, pre-read, and post-read packages, complete failed-canonical semantic/protection readback, and separate CANON/BACKUP/TX physical receipts. DB_ROLLBACK exists only to classify the complete failed canonical state; it has no mutation, save, candidate, replacement, promotion, restore, or lifecycle authority, and it plus every other canonical route must be retired before any RESTORE_CANDIDATE creation.

`RESTORE_CANDIDATE` creation boundary: only after failed CANON is completely classified and DB_ROLLBACK plus every canonical route is proven retired may the supervisor freshly reverify sealed BACKUP and original baseline receipts. Exclusively create one previously nonexistent `RESTORE_CANDIDATE` in exact CANON_PARENT, populate it only from verified BACKUP bytes, and require its normalized path and file identity distinct from CANON, BACKUP, TX, PROMOTION_CANDIDATE, every prior candidate, and every session input path. Reject case-fold/path aliasing, hard-link or reparse identity, a preexisting object, a second restore candidate, or any collision; no runtime role may open or mutate it.

`RESTORE_CANDIDATE` seal boundary: durably flush complete candidate file data/metadata and CANON_PARENT creation metadata, close every source/destination/file/directory handle, then independently reopen RESTORE_CANDIDATE operationally read-only only to prove exact normalized path/parent/file-identity/link-state/size/SHA256 and rehash it exactly equal BACKUP. Close every receipt/hash handle, prove closed state with a fresh exclusive read-only `FileShare.None` probe, and immediately reprove every canonical route retired, candidate still sealed/equal BACKUP, failed CANON still equal its DB_ROLLBACK classification receipt, and BACKUP still equal original. Any mismatch invalidates the candidate and forbids restoration.

`RESTORE_CANDIDATE` atomic-consumption boundary: only that exact closed/reopened/rehashed candidate may be the source of one supervisor-owned guarded same-directory atomic replacement of CANON; reject direct BACKUP-to-CANON replacement, copy/delete fallback, cross-directory movement, another source, drift, or a second attempt. On success, durably flush restored CANON file data/metadata and CANON_PARENT replacement/removal metadata, close every handle, and independently reopen/receipt/rehash restored CANON and BACKUP equal the original sealed baseline. Prove RESTORE_CANDIDATE path absent, its pre-replacement file identity reachable only as CANON, no hard-link/alias or prior/second restore path survives, and exact CANON_PARENT contains no restoration residue or second restorable artifact. Only then may fresh DB_RESTORE open and perform its existing post-open/pre-read/post-read original-baseline proof; this report grants no restoration authority.

Role-separation hard stop: `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are mutually distinct values returned by five phase-specific opens; no role, session, connection, nonce/generation, PID/route, baseline, package, result, or authority may be reused, aliased, substituted, or carried across phases even when two phases open the same path. PROMOTION_CANDIDATE and RESTORE_CANDIDATE are distinct one-use physical paths/file identities, never database roles or reusable artifacts. Only DB may mutate or perform the sole save. Any pre-save mismatch means no save; any post-save/pre-promotion mismatch means no promotion; any post-promotion mismatch requires DB_ROLLBACK classification and, when needed, sealed RESTORE_CANDIDATE atomic restoration plus DB_RESTORE verification before any persistence claim.

Operations below are supervisor-only, dependency ordered, and fail closed. `AR` means regular address comment, `AP` repeatable address comment, `FR` regular function comment, and `FP` repeatable function comment. Every field not explicitly changed by a row is protected. RTTI objects, shared thunks, subsystem globals, and `ConnectionClosedDialog__InitializeConnectionState` remain research evidence but are not mutation dependencies; they are deliberately excluded from the Gate 2B transaction rather than hidden in a grouped protection row.

Cache/readback rule: after each successful IDA-IH-04, IDA-IH-07, or IDA-IH-10 `set_type`, call public `force_recompile` for that exact function address and require a non-error invalidation result, then call public `decompile` for that exact address with `include_addresses:false` and require the literal named signature and body contract in the row. `force_recompile` only invalidates the Hex-Rays cache and the following decompilation only regenerates pseudocode: both are nonpersistent, are not IDB mutations or save receipts, authorize no additional IDB change, and never replace complete persisted public readback or post-save verification. Any error, stale signature/body, cache result for another address, or protected-state delta stops and rolls back.

## Removed From Claim And Incorporation Ledger

| C-IH-045 | 000039 | After independent CANON/BACKUP/TX receipts and the public tool-surface check pass, bind only the isolated-TX open return as mutation `DB`; declare the incomplete CopyWindow UDT only if fresh post-open and pre-mutation packages retain literal top-level `schema_version:1`, `ok:true`, `errors:[]`, literal worker-local `schema_version:1`, `ok:true`, `errors:[]`, and the exact prestate remains absent. | High | Public discovery exposes current `idb_save`, exactly one `runtime_attestation`, and no internal `runtime/attest`; listener validation uses its actual nonce/generation/ownership/route/source/module fields without listener-local `ok` or `errors`; mutation-phase listener and worker modules each match only their own sealed phase-local role baseline. | Section 21 startup and IDA-IH-01 | incorporate | proposed |

| C-IH-059 | 000039 | Protect every collateral item while the UID0003G6 replacement and all other mutations occur only in mutation `DB`; perform the sole exact save; retire DB; verify the same saved TX only through fresh operationally read-only `DB_VERIFY`; retire every CANON/TX route; then use only sealed exact-parent `PROMOTION_CANDIDATE` for one guarded atomic promotion and only sealed exact-parent `RESTORE_CANDIDATE` after retired `DB_ROLLBACK` for one guarded atomic restoration before `DB_RESTORE`. | High | Each candidate is exclusively collision-checked and path/file-identity-distinct, populated only from its verified source, durably file/parent-directory flushed, fully closed, independently reopened/receipted/rehashed, closed again, atomically consumed, and proven absent without a second candidate artifact; CANON/BACKUP/source receipts, five phase roles, complete item-transition/protection reads, and route retirements remain exact. | Section 21 startup and IDA-IH-15A through IDA-IH-15F | already-present | proposed |

## Removed From Final Recommendation

- C-IH-045 through C-IH-059 remain unchecked supervisor-owned Gate 2B work; B008 did not mutate IDA. IDA-IH-12 now deterministically replaces the current UID0003G6 split item with one `aYes[4]` item before comment/save, under the unchanged mutation-only DB, saved-TX DB_VERIFY, DB_FINAL/DB_ROLLBACK/DB_RESTORE, no-reuse, and sealed-candidate contracts.

- No open research or ordinary-document question is deferred. The remaining UID0003G6 manual row and Gate 2B transaction are supervisor-owned actions with exact dispositions.

## Removed From Open Questions With Attempted Resolution

| Gate 2B phase/session/candidate persistence separation | current public schema, physical-receipt authority, route-retirement requirements, and promotion/restoration durability boundaries | the five DB roles retain literal top-level/worker status, listener actual-field validation, independent phase baselines/packages, and no reuse; exact-parent PROMOTION_CANDIDATE and RESTORE_CANDIDATE each require exclusive source-bound creation, collision/path/file-identity distinction, durable file/parent flush, full closure, independent reopen/receipt/rehash, guarded atomic consumption, and no-residue/no-second-artifact proof | closed at report handoff; supervisor must execute fail closed |

## Removed From Follow-Up Actions

- Supervisor: perform fresh exact-artifact Gate 1 on this completed callback report, including 44/16 allocation, exact twins, six validator receipts, dynamic generated readback, nine already-present manual rows, one pending UID0003G6 insertion, and unchanged five-role persistence contract.

- Supervisor Gate 2B must follow complete Section 21 exactly, including the UID0003G6 two-call aggregate-then-type sequence, mandatory empty-type intermediate readback, final typed readback, and only then its comment. Retain independent CANON/BACKUP/TX receipts, current public schema, literal top-level/worker status, listener actual-field validation, mutation/stages/sole save only through DB, saved-TX DB_VERIFY, the five distinct DB roles, phase-local baselines/packages, semantic/protection checks, and no session reuse. The accepted exact-parent PROMOTION_CANDIDATE/RESTORE_CANDIDATE contract remains unchanged.

## Removed From Changed Files

- Historical Gate 1 report-only repair advanced rejected SHA256 `81A4D13E0531D72DFAD154A4797B6FF37611054F0C4552A9E2E86113CE8705DC` to subsequently rejected `60925B1369DE17EADE6672FD3E943401565F93E89EF656A6CCDEB77CB654D932`, then additively repaired only sealed promotion/restoration candidate handoff paragraphs, normative summaries, C-IH-059/C-IH-060 exact twins, and mechanics receipts. During that historical repair B008 edited no by-* documentation and ran no validator. Earlier ordinary implementation, five-role/schema corrections, and validation/generated-readback evidence remain intact.

## Removed From Implementation Tracking Checklist

| [ ] | C-IH-045 | 000039 | After independent CANON/BACKUP/TX receipts and the public tool-surface check pass, bind only the isolated-TX open return as mutation `DB`; declare the incomplete CopyWindow UDT only if fresh post-open and pre-mutation packages retain literal top-level `schema_version:1`, `ok:true`, `errors:[]`, literal worker-local `schema_version:1`, `ok:true`, `errors:[]`, and the exact prestate remains absent. | High | Public discovery exposes current `idb_save`, exactly one `runtime_attestation`, and no internal `runtime/attest`; listener validation uses its actual nonce/generation/ownership/route/source/module fields without listener-local `ok` or `errors`; mutation-phase listener and worker modules each match only their own sealed phase-local role baseline. | Section 21 startup and IDA-IH-01 | incorporate | proposed |

| [ ] | C-IH-059 | 000039 | Protect every collateral item while the UID0003G6 replacement and all other mutations occur only in mutation `DB`; perform the sole exact save; retire DB; verify the same saved TX only through fresh operationally read-only `DB_VERIFY`; retire every CANON/TX route; then use only sealed exact-parent `PROMOTION_CANDIDATE` for one guarded atomic promotion and only sealed exact-parent `RESTORE_CANDIDATE` after retired `DB_ROLLBACK` for one guarded atomic restoration before `DB_RESTORE`. | High | Each candidate is exclusively collision-checked and path/file-identity-distinct, populated only from its verified source, durably file/parent-directory flushed, fully closed, independently reopened/receipted/rehashed, closed again, atomically consumed, and proven absent without a second candidate artifact; CANON/BACKUP/source receipts, five phase roles, complete item-transition/protection reads, and route retirements remain exact. | Section 21 startup and IDA-IH-15A through IDA-IH-15F | already-present | proposed |
