# Removed Operational Material Archive

**NON-AUTHORITATIVE HISTORICAL MATERIAL. NEVER EXECUTE OR USE AS AN IDA PROCEDURE.**

Source report: [0000LO-MyItemListPane-file-source-quality.md](0000LO-MyItemListPane-file-source-quality.md)

The following material was removed from the source report during the 2026-08-14 Gate 1 repair. It is preserved verbatim only to retain audit history. It is not a B-agent report, recommendation, transaction plan, permission, or executable handoff.

```text
**Global preflight and stop contract**

1. Run a fresh `idb_list` immediately before Gate 2B and dynamically select the unique active/adopted session whose canonical path exactly equals `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. Stop if no exact match exists, more than one exact canonical match is active, or the selected session reports another path. Session IDs are ephemeral discovery results, not authority.
2. Call public `runtime_attestation` with `expected_database` equal to that freshly selected session and `expected_canonical_path` equal to the exact path above. Require `schema_version:1`, top-level `ok:true`, the selected worker's `attestation_ok:true`, and empty top-level/selected-worker errors; then require `server_health.status:"ok"` and the exact IDB path. Stop on any schema, attestation, worker, health, or path mismatch. A contemporaneous disk SHA256/size/mtime may be recorded as a dated receipt, but must never be compared with a historical fixed hash as future authority.
3. Before any mutation, reread every IDA-A, IDA-T, IDA-F, and IDA-P row from the dynamically selected session. Stop on any head, range, width, bytes, name, type, AR, AP, FR, FP, frame, or xref mismatch.
4. Search the complete name catalog for each proposed function name. Stop on any collision outside its exact target head; `rename` must use `allow_overwrite:false`, so an occupied destination or any attempted overwrite is a hard stop.
5. No type parser is invoked. Stop if an operator attempts a prototype, stack, UDT, vtable, RTTI, EH, padding, literal, thunk, destructor, or successor edit.
6. Apply IDA-A01 through IDA-A07 in order through only the public endpoints bound below. After each action, use `lookup_funcs`, bounded `func_profile` with prototype, `get_comments`, and the required xref query to reread the exact name, range, width, type, AR, AP, FR, FP, and xrefs. Stop on endpoint error, collision, parser error, or any readback mismatch.
7. On any failure after a mutation, restore every changed name/comment to the exact prestate below, verify restoration, do not save, and report the failed action. A successful batch also requires supervisor review before any save.

**Public endpoint and channel binding**

- Channel names are exact `get_comments` fields: AR = `regular`, AP = `repeatable`, FR = `function_regular`, and FP = `function_repeatable`.
- IDA-A01/A03/A06 use public `rename` with one `batch.func` row, `pure:true`, `allow_overwrite:false`, `dry_run:false`, and `stop_on_error:true`. Pure mode changes only the IDA function name and skips function-directory/decompiler-refresh side effects.
- The public schema has no function-repeatable-only setter. `set_function_comments` changes only regular function comments; `set_repeatable_comments` is not function-scoped; `set_address_repeatable_comments` explicitly sets AP, including at function entry addresses. Therefore IDA-A02/A05/A07 use `set_address_repeatable_comments` and leave FR/FP unchanged.
- IDA-A04 uses public `set_address_comments`, whose supported schema sets only the regular disassembly/address comment and never invokes Hex-Rays. `set_comments` is not authorized because it also changes decompiler-side comment state.

| Order | Action ID | Depends on | Exact current item and four channels | Exact proposal | Expected readback |
| --- | --- | --- | --- | --- | --- |
| 1 | IDA-A01 | PRE-ALL | 0x004aeb30-0x004aec77; width 0x147; name sub_4AEB30; type `_DWORD *__thiscall(_DWORD *this)`; AR/AP/FR/FP blank | Call public `rename` with `batch.func:{addr:"0x004aeb30",name:"MyItemListPane_Constructor"}`, `pure:true`, `allow_overwrite:false`, `dry_run:false`, and `stop_on_error:true`. | `rename.func` reports exact address/name with no error; `lookup_funcs` by address and name resolves only 0x004aeb30; `func_profile` returns 0x004aeb30-0x004aec77, width 0x147, and exact unchanged prototype; `get_comments` returns all four channels blank; xrefs unchanged. |
| 2 | IDA-A02 | IDA-A01 | 0x004aeb30-0x004aec77 after A01; width 0x147; name MyItemListPane_Constructor; same type; AR/AP/FR/FP blank | Call public `set_address_repeatable_comments` with `items:{addr:"0x004aeb30",comment:"Constructs the 0x14c ListPane-derived player inventory picker and appends one 0x20c row for each active local inventory slot."}`; this changes AP, not FP. | Name/type/range/xrefs unchanged; `get_comments.regular`, `function_regular`, and `function_repeatable` remain blank; `get_comments.repeatable` is the exact proposed text. |
| 3 | IDA-A03 | IDA-A02 | 0x004aec80-0x004aec8e; width 0x0e; name sub_4AEC80; type `char __thiscall(_DWORD **this)`; AR `index`; AP/FR/FP blank | Call public `rename` with `batch.func:{addr:"0x004aec80",name:"MyItemListPane_GetSelectedSlotIndex"}`, `pure:true`, `allow_overwrite:false`, `dry_run:false`, and `stop_on_error:true`. | `rename.func` reports exact address/name with no error; `lookup_funcs` by address and name resolves only 0x004aec80; `func_profile` returns 0x004aec80-0x004aec8e, width 0x0e, and exact unchanged prototype; `get_comments` returns AR `index`, AP/FR/FP blank; xrefs unchanged. |
| 4 | IDA-A04 | IDA-A03 | 0x004aec80-0x004aec8e after A03; width 0x0e; name MyItemListPane_GetSelectedSlotIndex; same type; AR `index`; AP/FR/FP blank | Call public `set_address_comments` with `items:{addr:"0x004aec80",comment:"Returns the inventory-slot byte from the row at inherited selected index +0x134."}`; this replaces only AR. | Name/type/range/xrefs unchanged; `get_comments.regular` is the exact proposed text; `repeatable`, `function_regular`, and `function_repeatable` remain blank. |
| 5 | IDA-A05 | IDA-A04 | 0x004aec80-0x004aec8e after A04; width 0x0e; same name/type; AR is A04 exact text; AP/FR/FP blank | Call public `set_address_repeatable_comments` with `items:{addr:"0x004aec80",comment:"Returns MyItemListEntry::slotIndex for the selected ListPane row; callers perform range validation."}`; this changes AP, not FP. | Name/type/range/xrefs unchanged; `get_comments.regular` remains A04 exact text; `repeatable` is A05 exact text; `function_regular` and `function_repeatable` remain blank. |
| 6 | IDA-A06 | IDA-A05 | 0x004aec90-0x004af031; width 0x3a1; name sub_4AEC90; type `void __thiscall(int this, int, int, int *)`; AR/AP/FR/FP blank; sole xref 0x00619fa8 | Call public `rename` with `batch.func:{addr:"0x004aec90",name:"MyItemListPane_DrawListEntry"}`, `pure:true`, `allow_overwrite:false`, `dry_run:false`, and `stop_on_error:true`. | `rename.func` reports exact address/name with no error; `lookup_funcs` by address and name resolves only 0x004aec90; `func_profile` returns 0x004aec90-0x004af031, width 0x3a1, and exact unchanged prototype; `get_comments` returns all four channels blank; sole xref remains 0x00619fa8. |
| 7 | IDA-A07 | IDA-A06 | 0x004aec90-0x004af031 after A06; width 0x3a1; name MyItemListPane_DrawListEntry; same type; AR/AP/FR/FP blank; sole xref 0x00619fa8 | Call public `set_address_repeatable_comments` with `items:{addr:"0x004aec90",comment:"ListPane row renderer: fills selected rows, draws EPF-scaled or legacy item icons, and fits the UTF-16 label with an ellipsis."}`; this changes AP, not FP. | Name/type/range unchanged; `get_comments.regular`, `function_regular`, and `function_repeatable` remain blank; `get_comments.repeatable` is the exact proposed text; sole xref remains 0x00619fa8. |
```
