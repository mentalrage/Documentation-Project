<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\Agent-B008\research\0002PR-LogoPaneScalarDeletingDestructor-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable / Transaction Material Archive

- Source report: [0002PR-LogoPaneScalarDeletingDestructor-source-quality.md](0002PR-LogoPaneScalarDeletingDestructor-source-quality.md)
- Status: non-authoritative preservation archive; this is not a B-agent research report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: never execute, import, evaluate, replay, or use the material below as an operational instruction.
- Scope: exact legacy transaction/session/open/attestation/backup/save/reopen orchestration and request-shaped IDA actions removed from the source report during supervisor Gate 1 cleanup.

## Removed Block R001

~~~text
| Stage | Literal request / artifact | Required authority and readback |
| --- | --- | --- |
| S01 canonical open | Choose a fresh non-colliding request label `<FRESH_REQUESTED_CANONICAL_SESSION_ID>` at Gate 2B and issue the current public schema `idb_open {input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64",preferred_session_id:"<FRESH_REQUESTED_CANONICAL_SESSION_ID>",mode:"prefer_headless",run_auto_analysis:false}`. Capture `structuredContent.session.session_id` as `<RETURNED_CANONICAL_SESSION_ID>`; do not assume it equals the preferred label. | Accept only a returned session whose normalized physical `input_path` is exactly `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, whose returned identifier is nonempty, and whose active worker route is confirmed by S02. Record the physical canonical prehash/size before effects. The actually returned session, never a current/default/report-time witness or caller-supplied alias, is the sole mutation authority. Any alternate path, alias/reparse route, inactive state, ambiguous identity, or substitution stops before effects. |
| S02 public runtime attestation | Issue `runtime_attestation {expected_database:"<RETURNED_CANONICAL_SESSION_ID>",expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"}` only after substituting the literal S01-returned identifier. | Require `schema_version:1`, `ok:true`, empty errors, a fresh listener generation/nonce whose PID owns `127.0.0.1:13337`, exactly one active worker route for the returned session and canonical path, matching worker/parent route, and matching `ida_pro_mcp`/`zeromcp` loaded-module identity. Public `tools/list` must retain `runtime_attestation` and omit internal `runtime/attest`. Missing, partial, ambiguous, stale, or mismatched attestation stops before effects. |
| S03 per-operation database binding | Every A01-A19 request and every immediate/cumulative/final readback carries top-level `database:"<RETURNED_CANONICAL_SESSION_ID>"`, replaced at runtime only with the literal identifier returned by S01. Every cumulative/final A01 readback must preserve human declaration intent `unsigned char m_reserved34f` while requiring the sole canonical IDA stored spelling `m_reserved34f@0x34f/1/unsigned __int8`. | Re-run S02 immediately before each stateful mutation and before the sole save. Require the same listener generation, process route, worker/session/path, module identity, and public-tool digest. Do not substitute another coherent copy, a requested-but-not-returned label, or an implicit/current session; drift stops without save. Any other reserved-member spelling, width, offset, layout, dependency, or member delta stops without save. |
| S04 collision-safe durable backup, guarded pre-save failure, and sole save | Before mutation, choose `<PRECHANGE_BACKUP_PATH>` under `tools/leaser/Agents/ida-backups/` using literal UID0002PR plus a fresh timestamp and nonce. Require the resolved path to be new, non-reparse, non-aliased, and outside the canonical path; copy the canonical IDB once, require backup SHA256/size to equal the recorded canonical prehash/size, physically reread it, and retain it durably through Gate 2B and lifecycle closure. After all immediate/cumulative readbacks pass, issue exactly one `idb_save {database:"<RETURNED_CANONICAL_SESSION_ID>"}`. | Any path collision, alias/reparse/hard-link ambiguity, copy/hash mismatch, pre-save readback mismatch, attestation drift, or unexpected canonical identity change stops and discards the unsaved worker without `idb_save`; the canonical disk must remain at the recorded prehash/size. Record canonical posthash/size only after the sole successful save. The report authorizes no second canonical save. If a failure occurs after that save, guarded recovery may copy only the authenticated immutable `<PRECHANGE_BACKUP_PATH>` back to the exact canonical path after independently proving the current canonical identity is the just-saved poststate; otherwise stop for manual supervisor review rather than overwriting an unknown file. |
| S05 fresh persisted verifier | After the sole save, choose a distinct new non-reparse/non-aliased `<POSTSAVE_VERIFIER_PATH>` under `tools/leaser/Agents/ida-backups/`, prove its SHA256/size byte-identical to the saved canonical poststate, choose `<FRESH_REQUESTED_VERIFIER_SESSION_ID>`, and issue the current public schema `idb_open {input_path:"<POSTSAVE_VERIFIER_PATH>",preferred_session_id:"<FRESH_REQUESTED_VERIFIER_SESSION_ID>",mode:"prefer_headless",run_auto_analysis:false}`. Capture `structuredContent.session.session_id` as `<RETURNED_VERIFIER_SESSION_ID>`, then issue `runtime_attestation {expected_database:"<RETURNED_VERIFIER_SESSION_ID>",expected_canonical_path:"<POSTSAVE_VERIFIER_PATH>"}`. | Accept only the exact returned verifier session, normalized verifier path, and active state. Bind every persisted readback to `<RETURNED_VERIFIER_SESSION_ID>` and repeat the complete mutation/protection matrix, including exact `LogoPane` size `0x354` and `m_reserved34f@0x34f/1/unsigned __int8` as the sole accepted persisted IDA spelling for the human `unsigned char` member. Do not save the verifier. Any other alias/layout/member/dependency result, copy collision, identity mismatch, attestation drift, or persisted mismatch fails Gate 2B and permits only S04's guarded authenticated recovery; no report claim receives persisted credit from the in-memory canonical worker alone. |

For each mutation, treat the self-contained literal prestate below as an expectation independently corroborated by bounded read-only MCP at 2026-08-12T23:36-04:00, then reproduce it again in the Gate 2B freshly returned and attested UID0002PR canonical session before applying only the literal public MCP schema. Read back immediately and repeat cumulative protected-state checks. The discarded session `supervisor-uid0002PR-gate2b-20260812T1428Z` is dated non-credit history only: it proved the A01 renderer alias but cannot supply current prestate, mutation, save, or persisted authority. The UID0002OW session likewise supplies no mutation or persisted authority. A recorded value is never accepted merely because it appears here. Stop without save on any missing entity, mismatch, or unexpected delta.

| ID | Exact entity and fresh live read-only prestate requiring Gate 2B reproduction | Proposed action and exact schema | Evidence / safety constraints | Expected readback |
| --- | --- | --- | --- | --- |
| A01 | Type `LogoPane` is absent. `Pane` is exactly size `0xf8`; `EPFTileContext` is exactly size `0x28`; `DLPalette` is exactly size `0x22c`; `FunctionObject0` exists as an incomplete declaration. The discarded unsaved trial does not change this canonical prestate. | `declare_type {decls:"struct LogoPane { Pane _paneBase; EPFTileContext m_logoTileContext; DLPalette m_logoPalette; bool m_advanceRequestedBeforeReady; bool m_logoReady; bool m_advanceQueued; unsigned char m_reserved34f; FunctionObject0 *m_completionCallback; };",database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Keep human/source declaration spelling exactly `unsigned char m_reserved34f`. Current schema accepts only top-level `decls` and exact `database`. Require no collision/redefinition and no dependency-type delta. The only accepted canonical IDA renderer alias is `unsigned __int8`; reject `_BYTE`, `uchar`, `char`, `unsigned char`, or any other stored/readback spelling, and reject every offset/width/layout/member/dependency delta. | Exact `LogoPane` UDT size `0x354`; members exactly `_paneBase@0x000/0xf8/Pane`, `m_logoTileContext@0x0f8/0x28/EPFTileContext`, `m_logoPalette@0x120/0x22c/DLPalette`, `m_advanceRequestedBeforeReady@0x34c/1/bool`, `m_logoReady@0x34d/1/bool`, `m_advanceQueued@0x34e/1/bool`, `m_reserved34f@0x34f/1/unsigned __int8`, and `m_completionCallback@0x350/4/FunctionObject0 *`; total size exactly `0x354`; dependency types remain byte-for-byte unchanged. The human reconstructed member spelling remains `unsigned char m_reserved34f`; only IDA readback uses `unsigned __int8`. |
| A02 | `0x004f5300` is `sub_4F5300`, range `[0x004f5300,0x004f53a8)`, type `Pane *__thiscall(Pane *Block, char)`, body size `0xa8`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `arg_0@0x20/4/int`, no other rows; address-regular, address-repeatable, function-regular, and function-repeatable are absent; inbound refs are code `0x004f52e4`, code `0x004f52ef`, and data `0x0061cf78`, with no ordinary caller. | Dry run `rename {batch:{func:{addr:"0x004f5300",name:"LogoPane__ScalarDeletingDestructor"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the intended name delta and skips function-directory/decompiler-refresh side effects. Collision, overwrite request, dry-run/apply disagreement, or any type/frame/range/body/comment/xref delta stops without save. | Name exactly `LogoPane__ScalarDeletingDestructor`; the literal range/type/frame/body/comments and three inbound refs above remain exact. |
| A03 | After A02, `0x004f5300` is `LogoPane__ScalarDeletingDestructor`, range `[0x004f5300,0x004f53a8)`, type `Pane *__thiscall(Pane *Block, char)`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `arg_0@0x20/4/int`, no other rows; all four comment channels are absent; refs remain `0x004f52e4`, `0x004f52ef`, `0x0061cf78`. | `set_type {edits:{addr:"0x004f5300",kind:"function",signature:"LogoPane *__thiscall LogoPane__ScalarDeletingDestructor(LogoPane *this, unsigned int arg_0)"},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Sole target function-type mutation. Stop if the renderer changes range/body/comments/xrefs; renames `arg_0`; creates/removes a row; or changes any local, saved-register, return-address offset/width/type. | Stored prototype exactly `LogoPane *__thiscall(LogoPane *this, unsigned int arg_0)`; post-frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `arg_0@0x20/4/unsigned __int32`, no other rows; all four comments, range/body, and three refs unchanged. |
| A04 | After A03, `0x004f5300` is `LogoPane__ScalarDeletingDestructor`, range `[0x004f5300,0x004f53a8)`, prototype exactly `LogoPane *__thiscall(LogoPane *this, unsigned int arg_0)`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `arg_0@0x20/4/unsigned __int32`, no other rows; all four comments absent; refs `0x004f52e4`, `0x004f52ef`, `0x0061cf78`. | Dry run `rename {batch:{stack:{func_addr:"0x004f5300",old:"arg_0",new:"flags"},stop_on_error:true,dry_run:true,allow_overwrite:false},database:"<RETURNED_CANONICAL_SESSION_ID>"}` with no pure mode; after exact acceptance only, apply the identical request with `dry_run:false`. | Rename only that existing stack row. Collision, missing row, dry-run/apply disagreement, or type/offset/width/protected-row/range/body/comment/xref drift stops without save. | Prototype exactly `LogoPane *__thiscall(LogoPane *this, unsigned int flags)`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `flags@0x20/4/unsigned __int32`, no other rows; all other literal state unchanged. |
| A05 | After A04, `0x004f5300` is `LogoPane__ScalarDeletingDestructor`, range `[0x004f5300,0x004f53a8)`, prototype `LogoPane *__thiscall(LogoPane *this, unsigned int flags)`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `flags@0x20/4/unsigned __int32`, no other rows; all four comments absent; refs `0x004f52e4`, `0x004f52ef`, `0x0061cf78`. | No mutation. Issue database-bound function/type and `stack_frame` readbacks only; do not issue a second stack or function `set_type`. | Verification-only barrier prevents a contradictory write. Any difference from the literal prestate is stop-without-save. | The complete A05 literal state persists with zero delta. |
| A06 | After A05, `0x004f5300` is `LogoPane__ScalarDeletingDestructor`, range `[0x004f5300,0x004f53a8)`, prototype `LogoPane *__thiscall(LogoPane *this, unsigned int flags)`; frame exactly `var_C@0x0c/4/_DWORD`, `var_4@0x14/4/_DWORD`, saved registers `@0x18/4/_DWORD`, return address `@0x1c/4/_UNKNOWN *`, `flags@0x20/4/unsigned __int32`, no other rows; address-regular, address-repeatable, function-regular, and function-repeatable are absent; refs `0x004f52e4`, `0x004f52ef`, `0x0061cf78`. | `set_function_comments {items:{addr:"0x004f5300",comment:"MSVC scalar deleting destructor for LogoPane. Source is represented by LogoPane::~LogoPane(); this wrapper resets three vptr views, destroys members/base, and applies scalar-delete flags. Do not hand-port."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any address/repeatable comment, type/frame/range/body/xref, or extra-function delta stops without save. | Function-regular exactly requested; address-regular, address-repeatable, and function-repeatable remain absent; literal name/range/prototype/frame/body/refs remain exact. |
| A07 | `0x00458500` is `sub_458500`, range `[0x00458500,0x00458557)`, type `void *__thiscall(int this)`, body size `0x57`; frame exactly `__return_address@0x8/4/_UNKNOWN *`, no locals/saved-register/argument rows; all four comment channels absent; exactly 71 inbound xrefs. | Dry run `rename {batch:{func:{addr:"0x00458500",name:"EPFTileContext__ReleaseBuffers"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the name delta. Collision, overwrite, dry-run/apply disagreement, or range/type/frame/body/comment/xref delta stops without save. | Name exactly `EPFTileContext__ReleaseBuffers`; range/type/frame/body/comments and 71-xref set unchanged. |
| A08 | After A07, `0x00458500` is `EPFTileContext__ReleaseBuffers`, range `[0x00458500,0x00458557)`, type `void *__thiscall(int this)`; frame exactly `__return_address@0x8/4/_UNKNOWN *`, no other rows; all four comments absent; exactly 71 inbound xrefs. | `set_type {edits:{addr:"0x00458500",kind:"function",signature:"void *__thiscall EPFTileContext__ReleaseBuffers(EPFTileContext *this)"},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Body/layout/xrefs prove the receiver, but exact EAX/caller-use proof is insufficient to change the stored `void *` return, so preserve it. Any calling-convention/return/frame/range/body/comment/xref or dependency-type delta stops. | Prototype exactly `void *__thiscall EPFTileContext__ReleaseBuffers(EPFTileContext *this)`; frame remains exactly `__return_address@0x8/4/_UNKNOWN *`, no other rows; range/body/comments and exact 71-xref set unchanged. |
| A09 | After A08, `0x00458500` is `EPFTileContext__ReleaseBuffers`, range `[0x00458500,0x00458557)`, prototype `void *__thiscall EPFTileContext__ReleaseBuffers(EPFTileContext *this)`; frame exactly `__return_address@0x8/4/_UNKNOWN *`, no other rows; address-regular, address-repeatable, function-regular, and function-repeatable absent; exactly 71 inbound xrefs. | `set_function_comments {items:{addr:"0x00458500",comment:"Releases EPFTileContext pixel, alpha, and encoded-mask buffers and clears the associated pointers/count. Shared render-support method; not LogoPane-owned."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any other comment, frame/type/range/body/xref, or dependency-type delta stops. | Function-regular exactly requested; other three comment channels absent; literal name/range/prototype/frame/body and 71-xref set unchanged. |
| A10 | `0x00542cd0` is `sub_542CD0`, range `[0x00542cd0,0x00542cf3)`, type `void __thiscall(_DWORD *this)`, body size `0x23`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no locals/saved-register/argument rows; all four comments absent; exactly 3 inbound xrefs. | Dry run `rename {batch:{func:{addr:"0x00542cd0",name:"DLPalette__FreePaletteData"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the name delta. Collision, overwrite, dry-run/apply disagreement, or range/type/frame/body/comment/xref delta stops. | Name exactly `DLPalette__FreePaletteData`; range/type/frame/body/comments and exact 3-xref set unchanged. |
| A11 | After A10, `0x00542cd0` is `DLPalette__FreePaletteData`, range `[0x00542cd0,0x00542cf3)`, type `void __thiscall(_DWORD *this)`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; all four comments absent; exactly 3 inbound xrefs. | `set_type {edits:{addr:"0x00542cd0",kind:"function",signature:"void __thiscall DLPalette__FreePaletteData(DLPalette *this)"},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Do not alter DLPalette UDT, body, callers, range, or comments. Any frame creation/removal/rename/retype/offset/width delta stops. | Prototype exactly `void __thiscall DLPalette__FreePaletteData(DLPalette *this)`; frame remains exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; range/body/comments and exact 3-xref set unchanged. |
| A12 | After A11, `0x00542cd0` is `DLPalette__FreePaletteData`, range `[0x00542cd0,0x00542cf3)`, prototype `void __thiscall DLPalette__FreePaletteData(DLPalette *this)`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; all four comments absent; exactly 3 inbound xrefs. | `set_function_comments {items:{addr:"0x00542cd0",comment:"Clears and releases DLPalette range/remap storage without destroying the object. Shared DLPalette method; not LogoPane-owned."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any other comment, type/frame/range/body/xref, or dependency-type delta stops. | Function-regular exactly requested; address-regular, address-repeatable, and function-repeatable absent; literal name/range/prototype/frame/body and exact 3-xref set unchanged. |
| A13 | `0x00542ca0` is `sub_542CA0`, range `[0x00542ca0,0x00542ccf)`, type `void __thiscall(LObject *this)`, body size `0x2f`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no locals/saved-register/argument rows; all four comments absent; exactly 15 inbound xrefs. | Dry run `rename {batch:{func:{addr:"0x00542ca0",name:"DLPalette__Destructor"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the name delta. Collision, overwrite, dry-run/apply disagreement, or range/type/frame/body/comment/xref delta stops. | Name exactly `DLPalette__Destructor`; range/type/frame/body/comments and exact 15-xref set unchanged. |
| A14 | After A13, `0x00542ca0` is `DLPalette__Destructor`, range `[0x00542ca0,0x00542ccf)`, type `void __thiscall(LObject *this)`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; all four comments absent; exactly 15 inbound xrefs. | `set_type {edits:{addr:"0x00542ca0",kind:"function",signature:"void __thiscall DLPalette__Destructor(DLPalette *this)"},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Preserve thiscall, return, body, base chain, DLPalette/LObject types, range, comments, and xrefs. Any frame or protected-state delta stops. | Prototype exactly `void __thiscall DLPalette__Destructor(DLPalette *this)`; frame remains exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; range/body/comments and exact 15-xref set unchanged. |
| A15 | After A14, `0x00542ca0` is `DLPalette__Destructor`, range `[0x00542ca0,0x00542ccf)`, prototype `void __thiscall DLPalette__Destructor(DLPalette *this)`; frame exactly `__return_address@0x4/4/_UNKNOWN *`, no other rows; all four comments absent; exactly 15 inbound xrefs. | `set_function_comments {items:{addr:"0x00542ca0",comment:"DLPalette ordinary destructor: releases palette range/remap storage, then performs compiler-emitted LObject base teardown. Shared DLPalette method; not LogoPane-owned."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any other comment, type/frame/range/body/xref, or dependency-type delta stops. | Function-regular exactly requested; address-regular, address-repeatable, and function-repeatable absent; literal name/range/prototype/frame/body and exact 15-xref set unchanged. |
| A16 | `0x004f52de` is `sub_4F52DE`, range `[0x004f52de,0x004f52e9)`, size `0xb`, type `_DWORD *__thiscall(_DWORD *this, char)`; frame exactly `__return_address@0x0/4/_UNKNOWN *`, with no local/saved-register/argument rows; body exactly subtracts `0xa0` from `ecx` then tail-jumps at `0x004f52e4` to `0x004f5300`; address-regular exactly `Block`, address-repeatable/function-regular/function-repeatable absent; incoming data ref exactly `0x0061cfc4`, no ordinary caller. | Dry run `rename {batch:{func:{addr:"0x004f52de",name:"LogoPane__ScalarDeletingDestructor_AdjustA0"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the name delta. Preserve raw thunk type, exact frame, bytes, range, refs, and `Block` address comment. Collision, overwrite, dry-run/apply disagreement, or any non-name delta stops. | Name exactly `LogoPane__ScalarDeletingDestructor_AdjustA0`; literal range/type/frame/body/ref/comment state unchanged. |
| A17 | After A16, `0x004f52de` is `LogoPane__ScalarDeletingDestructor_AdjustA0`, range `[0x004f52de,0x004f52e9)`, type `_DWORD *__thiscall(_DWORD *this, char)`; frame exactly `__return_address@0x0/4/_UNKNOWN *`, with no other rows; body subtracts `0xa0` then tail-jumps at `0x004f52e4`; address-regular exactly `Block`, address-repeatable/function-regular/function-repeatable absent; data ref `0x0061cfc4`, no ordinary caller. | `set_function_comments {items:{addr:"0x004f52de",comment:"Compiler-generated LogoPane deleting-destructor adjustor: subtracts 0xa0 from the secondary-base receiver and tail-jumps to the primary scalar deleting destructor. No source method."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any name/type/frame/range/body/ref, `Block`, or repeatable-channel delta stops. | Function-regular exactly requested; address-regular remains `Block`; repeatable channels absent; literal name/type/frame/range/body/ref unchanged. |
| A18 | `0x004f52e9` is `sub_4F52E9`, range `[0x004f52e9,0x004f52f4)`, size `0xb`, type `_DWORD *__thiscall(_DWORD *this, char)`; frame exactly `__return_address@0x0/4/_UNKNOWN *`, with no local/saved-register/argument rows; body exactly subtracts `0xa4` from `ecx` then tail-jumps at `0x004f52ef` to `0x004f5300`; address-regular exactly `Block`, address-repeatable/function-regular/function-repeatable absent; incoming data ref exactly `0x0061cff4`, no ordinary caller. | Dry run `rename {batch:{func:{addr:"0x004f52e9",name:"LogoPane__ScalarDeletingDestructor_AdjustA4"},stop_on_error:true,dry_run:true,allow_overwrite:false,pure:true},database:"<RETURNED_CANONICAL_SESSION_ID>"}`; after exact acceptance only, apply the identical request with `dry_run:false`. | `pure:true` permits only the name delta. Preserve raw thunk type, exact frame, bytes, range, refs, and `Block` address comment. Collision, overwrite, dry-run/apply disagreement, or any non-name delta stops. | Name exactly `LogoPane__ScalarDeletingDestructor_AdjustA4`; literal range/type/frame/body/ref/comment state unchanged. |
| A19 | After A18, `0x004f52e9` is `LogoPane__ScalarDeletingDestructor_AdjustA4`, range `[0x004f52e9,0x004f52f4)`, type `_DWORD *__thiscall(_DWORD *this, char)`; frame exactly `__return_address@0x0/4/_UNKNOWN *`, with no other rows; body subtracts `0xa4` then tail-jumps at `0x004f52ef`; address-regular exactly `Block`, address-repeatable/function-regular/function-repeatable absent; data ref `0x0061cff4`, no ordinary caller. | `set_function_comments {items:{addr:"0x004f52e9",comment:"Compiler-generated LogoPane deleting-destructor adjustor: subtracts 0xa4 from the secondary-base receiver and tail-jumps to the primary scalar deleting destructor. No source method."},database:"<RETURNED_CANONICAL_SESSION_ID>"}` | Single-item object schema sets function-regular only. Any name/type/frame/range/body/ref, `Block`, or repeatable-channel delta stops. | Function-regular exactly requested; address-regular remains `Block`; repeatable channels absent; literal name/type/frame/range/body/ref unchanged. |

Protected no-change entities for every cumulative and save/reopen readback:

| Entity | Literal protected prestate independently matched at 2026-08-12T23:36-04:00 and still requiring fresh UID0002PR Gate 2B reproduction |
| --- | --- |
| `0x0061cf78` | exact item `[0x0061cf78,0x0061cf7c)`, width 4, bytes `00 53 4F 00`, name `??_7LogoPane@@6B@`, type absent, value/outgoing ref exactly `0x004f5300`; address-regular/repeatable absent and function channels not applicable; incoming stores exactly `0x004f4c5a`, `0x004f4edc`, `0x004f5333` |
| `0x0061cfc4` | exact item `[0x0061cfc4,0x0061cfc8)`, width 4, bytes `DE 52 4F 00`, name `??_7LogoPane@@6B@_0`, type absent, value/outgoing ref exactly `0x004f52de`; address-regular/repeatable absent and function channels not applicable; incoming stores exactly `0x004f4c60`, `0x004f4ee2`, `0x004f5339` |
| `0x0061cff4` | exact item `[0x0061cff4,0x0061cff8)`, width 4, bytes `E9 52 4F 00`, name `??_7LogoPane@@6B@_1`, type absent, value/outgoing ref exactly `0x004f52e9`; address-regular/repeatable absent and function channels not applicable; incoming stores exactly `0x004f4c6a`, `0x004f4eec`, `0x004f5343` |
| `0x004f52f4` | exact data/alignment item `[0x004f52f4,0x004f5300)`, width 12, bytes `CC CC CC CC CC CC CC CC CC CC CC CC`, name absent, type absent, address-regular/repeatable absent, function channels not applicable, and no function start |
| `0x004f53a8` | exact data/alignment item `[0x004f53a8,0x004f53b0)`, width 8, bytes `CC CC CC CC CC CC CC CC`, name absent, type absent, address-regular/repeatable absent, function channels not applicable, and no function start |
| `0x00544580` | function `Pane__Destructor`, exact range `[0x00544580,0x00544687)`, size `0x107`, prototype `void __thiscall(Pane *this)`; frame exactly `var_C@0x10/4/_DWORD`, saved registers `@0x1c/4/_DWORD`, return address `@0x20/4/_UNKNOWN *`, `arg_4@0x28/4/_DWORD`, no other rows; address-regular/repeatable absent; function-regular exactly `Source Pane ordinary destructor. Calls from derived destructors are compiler-generated base teardown and must not be handwritten explicitly; ownership remains with Pane.`; function-repeatable absent; exactly 303 inbound xrefs in the fresh 2026-08-12 witness; body/range/frame/comment channels and the complete freshly reread Gate 2B xref vector are protected no-change state |
| `0x004f4ac0` | function `NexusTK_operator_delete_wrapper`, exact range `[0x004f4ac0,0x004f4b00)`, size `0x40`, prototype `void __cdecl(void *block)`; frame exactly `var_C@0x4/4/_DWORD`, saved registers `@0x10/4/_DWORD`, return address `@0x14/4/_UNKNOWN *`, `Block@0x18/4/void *`, `arg_4@0x1c/4/int`, no other rows; address-regular/repeatable absent; function-regular exactly `Project global operator-delete wrapper used by scalar deleting destructors; target ownership does not transfer here.`; function-repeatable absent; exactly 2,129 inbound xrefs in the fresh 2026-08-12 witness; body/range/frame/comment channels and the complete freshly reread Gate 2B xref vector are protected no-change state |
| `0x0041b6a0` | function `@_guard_check_icall_nop@4`, exact range `[0x0041b6a0,0x0041b6a1)`, size 1, byte `C3`, prototype `void()`; frame exactly `__return_address@0x0/4/_UNKNOWN *`, no other rows; address-regular/repeatable absent; function-regular exactly two lines: `One-byte compiler/runtime guarded-size path no-op; not a project source method.` then `One-byte compiler/runtime no-op used by deleting-wrapper flag paths; not sized delete, vector delete, guarded delete, or a deallocator.`; function-repeatable absent; exactly 682 inbound refs; all function/frame/comment/xref state is protected |

Supervisor-owned staged/final discipline: before every action, rerun S02 and reread that row's literal prestate with every request bound to `database:"<RETURNED_CANONICAL_SESSION_ID>"`. Every rename requires its `dry_run:true` result to name only the intended collision-free change before the matching `dry_run:false` request; `allow_overwrite:false`, `stop_on_error:true`, and the documented pure/no-pure mode remain binding. Read back each action immediately, then cumulatively reread every prior action and all protected rows. Every post-A01 cumulative readback must retain exact size `0x354`, every literal member offset/size/type above, and specifically `m_reserved34f@0x34f/1/unsigned __int8` as the sole accepted canonical IDA alias for human-source `unsigned char`. A mismatch, collision, missing entity, schema rejection, any other renderer spelling, attestation drift, or unlisted delta stops without save.

Before S04's single supervisor-owned save, verify the exact `LogoPane` UDT layout, including all literal offsets/sizes/types and the sole canonical stored renderer `m_reserved34f@0x34f/1/unsigned __int8` for the human-source `unsigned char` member; all six names; all four function prototypes; the complete target and three helper frames; all six function-regular comments and every untargeted address/repeatable channel; every function range/body byte/xref; all three vtable items/values/names/types/bytes/incoming stores/outgoing refs; both the twelve-byte leading and eight-byte trailing alignment items; the three protected support functions and their literal comments/ranges/types/bytes/xref baselines; and every dependency type. Then rerun S02 and issue exactly the one database-bound `idb_save`. S05 must open the exact byte-identical post-save verifier copy in its distinct returned session, attest it, and repeat this same full matrix with the same sole accepted `unsigned __int8` IDA alias. No persisted credit is allowed from the in-memory canonical worker alone, no reopening of an implicit/current session is accepted, and the verifier receives no save.
~~~
## Additional Removed Operational Prose

Source report: [0002PR-LogoPaneScalarDeletingDestructor-source-quality.md](0002PR-LogoPaneScalarDeletingDestructor-source-quality.md)

This text is non-authoritative audit provenance and must never be executed. It is not research evidence and receives no Gate, coverage, score, IDA, validator, or lifecycle credit.

> Before any IDA action, the supervisor must freshly open the canonical IDB with `run_auto_analysis:false`, record its then-current physical prehash/size, bind authority only to the session identifier actually returned by that open, obtain fresh public runtime attestation for that returned session/path, compare every literal prestate in Section 21, and stop without save on any mismatch. The currently open UID0002OW session is explicitly ineligible.
>
> Gate 2B must independently open, bind, and attest its own freshly returned UID0002PR session against the then-current disk identity.
>
> Gate 2B must independently reproduce each applicable item in its own freshly opened, publicly attested UID0002PR canonical session before using it as mutation or no-change authority.
>
> Gate 2B must rerun collision checks in its own freshly returned session immediately before each rename.
>
> Protected no-change entities for every cumulative and save/reopen readback.
>
> Supervisor Gate 2B after fresh full Gate 1: restart A01-A19 from canonical prestate with backup, exact public prestate attestation, sole accepted A01 `unsigned __int8` IDA readback for human `unsigned char`, immediate/cumulative/fresh-save readback, and no unexpected deltas. The discarded prior trial receives no action credit.

