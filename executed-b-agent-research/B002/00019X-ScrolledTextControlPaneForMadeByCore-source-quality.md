** TARGET-REPORT-UID:00019X **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# 00019X ScrolledTextControlPaneForMadeByCore Ownership / Split Research

## Finalized Report / Current Recommendation

- Completed callback state: UID00019X is a validated `92/94` semantic split/index owned by UID0000CJ `ScrolledTextControlPaneForMadeBy`. It retains `CANONICAL_OWNER:0000CJ`, exact range, and `Nested:0`; has `RECONSTRUCTABLE:FALSE`; and has blank `EMITTER_UIDS`, `EMITTER_POSITION_OPTIONAL`, and formal C++.
- Four exact source-bearing children were serially validator-registered and validated: constructor UID0004MA `[0x004ff400,0x004ff644)` at position `110`, ordinary destructor UID0004MB `[0x004ff650,0x004ff6bc)` at `120`, timer callback UID0004MC `[0x004ff6c0,0x004ff725)` at `130`, and paint callback UID0004MD `[0x004ff730,0x004ff7cd)` at `140`. Each is owned/emitted by UID0000CJ and carries one complete method body; the aggregate duplicates none of them.
- UID0000CJ is now the validated `92/94`, position-`100` declaration container for the exact `0x1dc` layout: `ControlPane` base, `EPFTileContext m_renderedText` at `+0x108`, embedded `GrafPort m_renderPort` at `+0x130`, `int m_scrollOffset` at `+0x1d0`, `int m_scrollLimit` at `+0x1d4`, and `signed char m_scrollInterval` at `+0x1d8`, followed by implicit alignment through `+0x1dc`. No generic reserve member was added.
- Keep source placement in UID0000NI `NexusTK/ui/controls/ScrolledTextControlPane.cpp`. The generic UID00019W/UID0000CI control is a parallel sibling, not a base class. It is comparison evidence and is not duplicated by this report.
- The exact formal class and four source bodies are installed and generated in deterministic order. Original private/helper spellings are stripped, so descriptive names remain a confidence cap rather than a source blocker.
- B002 callback work, scoped validators, final waited refresh, generated verification, and lease releases are complete. Current/future report validation, execution, location, move, archive, and lifecycle state are external validator/supervisor-owned and are not asserted.

## Supporting Research

At assignment/evidence time, this target was the first then-vetted `by-memory` tracker row with report count `0` whose exact range had no executed report. The 34 preceding vetted rows were not reopened because each then had executed range-matching research and no specific defect was assigned.

Exact historical search terms:

`UID00019X`, `UID:00019X`, `TARGET-REPORT-UID:00019X`, `00019X`, `0x004ff400`, `0x004ff7cd`, `0x004ff040`, `ScrolledTextControlPaneForMadeByCore`, `ScrolledTextControlPaneForMadeBy`, `ScrolledTextControlPaneCore`, `NewHistoryDialogPane`, `g_pNewHistoryDialog`, `MADEBY`, `HISTORYN`, `ScrolledTextControlPaneDestructorThunks`, and `ScrolledTextControlPaneForMadeByScalarDeletingDestructor`.

Exact searched roots and outcomes:

| Root | Outcome |
| --- | --- |
| `executed-b-agent-research/**` | Evidence-time search found no dedicated UID00019X or exact-range report and no `TARGET-REPORT-UID:00019X`. Exact-start matches were B003 NewHistory support and B004 ScrolledPicture boundary support. Broad class/resource matches were opened and classified below. |
| `archived/**` | No direct UID, exact-range, target-name, or exact start/end report match. |
| `tools/leaser/Agents/Older-Research/**` | No direct target report. |
| `tools/leaser/Agents/SpecialReports/**` | No direct target report. |
| `tools/leaser/Agents/Agent-B001/research/executed/**` through `Agent-B015/research/executed/**` | Evidence-time legacy executed roots contained no direct UID00019X/exact-range report; executed research was centralized above. |
| `tools/leaser/Agents/Agent-B001/research/**` | Evidence-time search found no active UID00019X/exact-range report. |
| `tools/leaser/Agents/Agent-B002/research/**` | No pre-existing report existed before this assigned artifact. |
| `tools/leaser/Agents/Agent-B003/research/**` | Evidence-time search found no active UID00019X/exact-range report. |
| `tools/leaser/Agents/Agent-B004/research/**` | Evidence-time search found no active UID00019X/exact-range report. |
| `tools/leaser/Agents/Agent-B005/research/**` | Evidence-time search found no active UID00019X/exact-range report. |

Relevant reports opened and classified:

| Report | Finding / classification |
| --- | --- |
| `executed-b-agent-research/B003/0001A3-new-history-dialog-pane-core-source-quality.md` | Direct caller/resource support. It proves NewHistory constructs the made-by scroller with `L"MADEBY"`, bounds `(614,156)-(950,556)`, and interval `30`; owns `HISTORYN.EPF/PAL`; and publishes `g_pNewHistoryDialog`. It explicitly keeps scroller ownership out of NewHistory source. Not a target report. |
| `executed-b-agent-research/B004/00019Y-ScrolledPictureControlPaneCore-source-quality.md` | Immediate successor/boundary and sibling timer/paint support. It proves ScrolledPicture starts at `0x004ff7d0`, after three `0xcc` bytes following target end `0x004ff7cd`. It is a separate control and does not own the target. |
| `executed-b-agent-research/B001/00014W-HistoryViewingDialogPaneCore-source-quality.md` | History-dialog family context only; no target body or owner claim. |
| `executed-b-agent-research/B014/0001A2-BackStoryDialogPaneCore-source-quality.md` | Adjacent main-menu dialog/source context only. |
| `executed-b-agent-research/B006/0001A4-HistoryViewingDialogAdvance-source-quality.md` | Timer/dialog navigation context only. |
| `executed-b-agent-research/B005/0001AH-NewHistoryDialogPaneDestructor-source-quality.md` | NewHistory lifetime/singleton support only. It does not own scroller teardown. |

Evidence-time documentation inputs opened directly:

- UID00019X target, UID0000CJ class, UID0000NI file, generic UID00019W/UID0000CI sibling, compiler UID0003LX/UID0003LY support, UID0001A3 NewHistory core, UID0001PV/UID0000RS singleton, UID00003T DialogPane, UID000038 ControlPane, UID00005V GrafPort, UID0000EO TextEditPane, UID0000EP TextEditScrap, UID00003G DATFile, EPFTileContext layout, Surface callback-table support, and the then-current generated `NexusTK/ui/controls/ScrolledTextControlPane.cpp`.
- Generated output was read only. At evidence time it contained Empty Emitter Markers for UID0000CI, UID00019W, UID0000CJ, and UID00019X; it supplied no source-body proof and was not edited or refreshed.

## Target

- Target UID: `00019X`.
- Target path: `by-memory/0x004ff400-0x004ff7cd.ScrolledTextControlPaneForMadeByCore.md`.
- Assigned report path: `tools/leaser/Agents/Agent-B002/research/00019X-ScrolledTextControlPaneForMadeByCore-source-quality.md`.
- Evidence-time assigned queue state: `86/90`, report count `0`, reconstructable broad aggregate.
- Exact binary span: `[0x004ff400,0x004ff7cd)`, `0x3cd` / 973 bytes.
- Exact SHA256 over the target bytes: `ABA0099F9CD9F1B30EB1E77C52AE69B70BD32D21BF3F0A42A5AFF396C012F716`.

## Current Target State

- Evidence-time state was `86/90`, `CANONICAL_OWNER:0000CJ`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000CJ`, blank position/C++, exact range, and `Nested:0`; that source-emitting aggregate treatment and below-`95/95` rationale are preserved only as historical input.
- Callback state is `92/94`, same semantic owner/range/`Nested:0`, `RECONSTRUCTABLE:FALSE`, and blank emitter/position/formal C++. The page now carries the exact aggregate/function/padding hashes, `0x1dc` layout, ABI, complete behavior, caller/vtable/resource/global evidence, negative evidence, source order, and compiler exclusions.
- Exact source definitions are UID0004MA/UID0004MB/UID0004MC/UID0004MD. UID0003LX is now `90/93`, `NONE/FALSE`, blank emitter/position/C++; UID0003LY was verified unchanged `NONE/FALSE` and blank.
- UID0000CJ is `92/94`, owner/emitter UID0000NI, position `100`, with the exact class declaration and `[[CHILDREN]]`. B002's waited generated verification under command `000000010056` contained one class and one each constructor/destructor/timer/paint in positions `100/110/120/130/140`, no UID00019X marker, and no handwritten scalar/thunk/vtable ABI; the final read-only audit of subsequent external refresh `000000010069` confirmed the same content.

## Executive Recommendation

UID00019X is not one source declaration or method. It is a physical aggregate containing four ordinary source methods separated by compiler alignment. Emitting all methods from the aggregate would make exact child coverage impossible and invites duplicate destructor/timer/paint source. The durable route is a non-emitting index plus exact children.

UID0000CJ is the semantic class owner and UID0000NI remains the source file. The generic and made-by controls are sibling `ControlPane` derivatives with parallel layouts and distinct vtables. The made-by variant differs in two source-visible ways: it constructs its temporary TextEditPane with the read-only flag clear, and its timer advances by two then closes NewHistory when the offset exceeds the limit.

The constructor is source-ready despite stripped names. It loads a named DAT resource, copies the exact byte count into a fixed 1001-byte allocation, appends NUL without a size guard, imports a TextEditScrap, renders it through a temporary 372-byte TextEditPane into an EPFTileContext, builds an encoded mask, deletes the temporary editor, and schedules timer `0` with initial delay `1`. The destructor explicitly releases the stored EPFTileContext; embedded GrafPort and base cleanup are compiler-generated member/base teardown. Timer and paint bodies are fully resolved below.

## Supervisor Active Recheck

- Gate 1 accepted the exact pre-callback SHA `DFE34825EF3B24B4B3AC56EF84ECED56426F33B8288613F40B3F62341B1B2E7C`, after which the supervisor authorized C01-C30 and Destinations 1-10.
- Mandatory database identity and health were verified during evidence collection. All IDB-backed facts use evidence-time database `supervisor_nexustk_20260713`; no present availability is asserted and no IDA write or process management occurred.
- Split-first completed under the UID00019X parent lease. Validators assigned UID0004MA-UID0004MD serially, then the aggregate, class, and bounded support pages were applied without a collision or duplicate target body.
- Ten ordinary by-* pages passed scoped validation; one final authorized `--wait-generated` validation completed. Every B002 lease was released. B002 did not run `execute_report`, a lifecycle/probe/dry-run command, move, or archive operation.

## Inference Research Guidance Check

- Existing scores, names, field guesses, and blank C++ were treated as hypotheses. Live callers, vtables, bytes, helpers, and generic sibling behavior were rechecked independently.
- Direct facts are ranges, bytes/hashes, xrefs, vtable cells, argument widths, object offsets, helper targets, branch comparisons, constants, resource/global references, and caller allocation size.
- Strong inference supplies human source spellings such as `m_renderedText`, `m_renderPort`, `m_scrollOffset`, `m_scrollLimit`, `m_scrollInterval`, `PasteFromClipboard`, `GetLineCount`, `SetViewportBounds`, `SetTextAreaBounds`, `OnTimerEvent`, and `OnPaintFrame`. These names describe exact observed roles and avoid raw offsets/decompiler labels.
- Period-plausible source uses ordinary local DATFile/TextEditScrap objects, one heap TextEditPane, manual MemoryMan buffer allocation, direct virtual callbacks, raw pointers, and `NULL`. It does not introduce modern RAII containers, lambdas, smart pointers, spans, or exceptions not present in the binary.
- Compiler-generated vptr stores, EH unwind labels, scalar deleting wrappers, adjustor thunks, alignment bytes, and automatic base/member destructor calls are excluded from handwritten bodies.
- The combined source gate is cleared by the applied child scores. Historical `95/95` blank-C++ wording is retained only as a superseded policy assumption.

## Heuristic / Inference Reanalysis And Validation

1. Constructor identity is not inferred from adjacency alone: it has one direct code call from NewHistory construction, initializes ControlPane and all derived members, installs all three made-by vptr views, and returns with three explicit arguments.
2. The ordinary destructor is not the scalar wrapper: `[0x004ff650,0x004ff6bc)` performs member/base teardown and is referenced by the non-contiguous wrapper. UID0003LX is compiler delete glue and should not emit source.
3. Timer identity is fixed by the tertiary vtable cell `0x0061d888 -> 0x004ff6c0`, adjusted `this` view, three stack parameters, `retn 0x0c`, scheduling call, and constant-true AL return.
4. Paint identity is fixed by primary vtable cell `0x0061d830 -> 0x004ff730`, inherited bounds/color use, fill callback, source/destination rectangle construction, and sprite blit.
5. The class is not derived from generic ScrolledTextControlPane. Both constructors call ControlPane directly and install unrelated three-view vtables. Their near-identical code is source-family duplication between sibling classes.
6. Object size is not a generic reserve: NewHistory allocates `0x1dc`; exact construction/destruction offsets identify a `0x28` EPFTileContext, `0xa0` GrafPort, two dwords, one signed byte, and implicit tail alignment.
7. `HISTORYN.EPF/PAL` are NewHistory background resources. The target receives only `MADEBY` and reads it through DATFile. The target must not acquire HISTORYN fields or ownership.
8. The close call is not null-safe: timer reads `g_pNewHistoryDialog` and calls the close wrapper without a null test. Adding a guard would change observed behavior.
9. The paint callback performs no target-local clipping/null check. Installed callbacks consume the rectangles. Adding clipping or skipping empty rectangles would change source shape.
10. Helper lexical names remain inferred, but every helper ABI and side effect needed by the first draft is resolved. This caps confidence below final-symbol quality without blocking source.

## Evidence Standards Used

- Exact: IDA function boundaries, raw bytes, hashes, xrefs, data cells, caller instructions, stack cleanup, constants, offsets, branch predicates, and padding.
- Very strong: class/vtable route, object layout, source ABI, ownership, generic-sibling relationship, timer/paint role, resource/global distinction, and member lifetimes.
- Strong inference: original method/private/helper spellings and final header include arrangement.
- Historical/generated text is corroboration only. It is not used to override current binary evidence.
- Negative evidence is based on complete target CFG/helper/xref review: absent null/failure/size checks, absent alternate caller, absent second target body, absent generic-base construction, and absent source reason for handwritten ABI wrappers.

## Evidence Checked

Evidence-time MCP observations on 2026-07-13:

- Fresh streamable JSON-RPC `initialize`, `tools/list`, then `idb_list` returned one active/adopted IDB session `supervisor_nexustk_20260713`, `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `2236`.
- `server_health(database='supervisor_nexustk_20260713')` returned `status:ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and cache size `2067`.
- Bounded target lookups included `lookup_funcs` at `0x004ff400`, `0x004ff650`, `0x004ff6c0`, `0x004ff730`, `0x004ff7cc`, and `0x004ff7cd`; `analyze_function` for all four target methods, the four generic siblings, NewHistory constructor, TextEdit helpers, EPFTileContext helpers, and render/close dependencies; `xref_query` for all target starts, vtable cells, singleton storage, constructor, wrappers, and caller; and bounded byte reads for functions and padding.
- Evidence-time `xref_query` returned exactly one constructor code xref at `0x0050052e` in `sub_500410`; exactly one timer data xref at `0x0061d888`; exactly one paint data xref at `0x0061d830`; and five `g_pNewHistoryDialog` storage refs, including the target timer read at `0x004ff6eb`.
- Evidence-time `get_global_value` returned `0x0061d830 = 0x004ff730`, `0x0061d888 = 0x004ff6c0`, and the runtime singleton storage value `0` at that observation. The runtime value is not a source null-check and is not asserted as persistent state.
- MCP remained available for all required bounded calls. No fallback-only conclusion was accepted.

Repository checks:

- Opened target/class/file/generic sibling, compiler support, NewHistory/singleton, base classes, layout types, TextEdit/DAT/MemoryMan, render callbacks, generated output, and all reports classified under Supporting Research.
- Searched the exact terms and roots listed under Supporting Research. No direct report exists; all matches were classified rather than silently inherited.
- During the report-only evidence phase, current source-tree documentation and generated output were read only; no by-* or generated file was modified before Gate 1.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | Evidence was collected from mandatory database `supervisor_nexustk_20260713`; health and bounded target lookup succeeded during the evidence window. | exact observation | idb_list/server_health/lookup | UID00019X evidence-time provenance retained; validator `000000010049` | incorporate | applied |
| C02 | Aggregate is exactly `[0x004ff400,0x004ff7cd)`, 973 bytes, SHA256 `ABA0099F9CD9F1B30EB1E77C52AE69B70BD32D21BF3F0A42A5AFF396C012F716`. | exact | lookup/bytes/hash | UID00019X `Exact Aggregate Inventory`; validator `000000010049` | incorporate | applied |
| C03 | Aggregate contains four modeled source functions and three internal alignment spans; this required the applied conversion to a non-emitting split index. | exact/very strong | function inventory/padding | UID00019X `FALSE`, blank emitter/position/formal, real child/padding inventory; `000000010049` | incorporate | applied |
| C04 | Constructor is `[0x004ff400,0x004ff644)`, 580 bytes, one direct NewHistory call. | exact | lookup/xref/hash | UID0004MA plus UID00019X inventory; validator `000000010045` | incorporate | applied |
| C05 | Constructor ABI is three explicit args: wide resource name, RectBounds pointer, signed one-byte interval. | very strong | caller pushes/retn 0x0c/field store | UID0000CJ declaration, UID0004MA body, UID0001A3 `&rect`; `000000010045/50/54` | incorporate | applied |
| C06 | Class is a direct ControlPane sibling of generic ScrolledTextControlPane, not derived from it. | very strong | both base ctor calls/vtables | UID0000CJ relationship/layout and UID00019X behavior; `000000010049/50` | reject-stale | applied |
| C07 | Object size `0x1dc` and exact typed layout are resolved; no opaque reserve member remains. | exact/very strong | caller allocation/member offsets/destructors | UID0000CJ exact declaration/layout with implicit tail; `000000010050` | incorporate | applied |
| C08 | Constructor loads only caller resource `MADEBY` through DATFile; HISTORYN is dialog-owned. | exact | caller strings/callee flow | UID0004MA resources and UID0001A3 caller distinction; `000000010045/54` | incorporate | applied |
| C09 | DAT pipeline is Open, GetSize, allocate 1001, Read exact size, append NUL, Close, import scrap, free. | exact/very strong | ordered calls/constants | UID0004MA formal/body evidence; `000000010045` | incorporate | applied |
| C10 | No open/read/size/null/`dataSize <= 1000` guard exists. | exact | complete constructor CFG | UID0004MA `Lifetime, Failure, And Negative Evidence`; `000000010045` | incorporate | applied |
| C11 | Temporary TextEditPane allocation is exactly `0x174`; constructor args include background 143, style 1, legacy mode, read-only false, input mode 0. | exact/very strong | caller instructions/helper ABI | UID0004MA behavior and UID0000EO support; `000000010045/51` | incorporate | applied |
| C12 | Temporary editor imports TextEditScrap, computes `12 * lineCount`, and sets scroll limit to pane height plus text height. | exact/strong naming | helper analyses/data flow | UID0004MA body and UID0000EO declarations; `000000010045/51` | incorporate | applied |
| C13 | Editor render pipeline updates render region/two rect fields, disables edit/selection state, sets mode 1, paints, copies surface context, builds mask, ends paint, then deletes editor. | exact/strong naming | ordered calls/helper analyses | UID0004MA body and UID0000EO public helper/OnDraw support; `000000010045/51` | incorporate | applied |
| C14 | Initial timer scheduling is `(0,1,0,0)` after editor deletion. | exact | constructor tail | UID0004MA formal/body; `000000010045` | incorporate | applied |
| C15 | Ordinary destructor is `[0x004ff650,0x004ff6bc)`; explicit source body releases `m_renderedText`, while GrafPort/base teardown is automatic compiler lowering. | exact/very strong | destructor calls/C++ lifetime | UID0004MB formal/body and UID0003LX source distinction; `000000010046/53` | incorporate | applied |
| C16 | UID0003LX is a scalar deleting ABI wrapper, not a second source destructor. | exact/very strong | vtable/wrapper/destructor route | UID0003LX `90/93`, `NONE/FALSE`, blank formal; `000000010053` | reject-stale | applied |
| C17 | UID0003LY adjustor thunks remain `NONE/FALSE` with blank emitter/position/C++; no handwritten thunk. | exact | thunk bytes/vtable views | UID0003LY re-read unchanged; generated ABI search count 0 under `000000010056` | already-present | already-present |
| C18 | Timer is `[0x004ff6c0,0x004ff725)`, tertiary slot callback, source `bool OnTimerEvent(int,int,int)`, always true. | exact/very strong | vtable/ret/CFG | UID0004MC body and UID0000CJ declaration; `000000010047/50` | incorporate | applied |
| C19 | Timer ignores nonzero event ids; event 0 increments by 2, closes only when new offset is greater than limit, then reschedules and invalidates. | exact | branch/data flow | UID0004MC `Exact Behavior And Order`; `000000010047` | incorporate | applied |
| C20 | Timer resets offset before calling `g_pNewHistoryDialog->CloseDialog()` and performs no singleton null check. | exact/strong source name | singleton xref/call order | UID0004MC state/negative evidence and UID00003T access; `000000010047/52` | incorporate | applied |
| C21 | `DialogPane::CloseDialog` must be public for the observed cross-class source call; protected placement is compile-incompatible. | very strong | inheritance/source access/current formal | UID00003T public declaration exactly once; `000000010052` | incorporate | applied |
| C22 | Paint is `[0x004ff730,0x004ff7cd)`, primary paint slot, source-void despite incidental EAX return. | exact/very strong | vtable/ret/callback | UID0004MD body and UID0000CJ declaration; `000000010048/50` | incorporate | applied |
| C23 | Paint order is SetDrawColor(0), FillRect(bounds), build source/destination rects, branch on offset versus pane height, then one mode-0 null-palette/null-options blit. | exact | complete paint CFG | UID0004MD `Exact Paint Order`; `000000010048` | incorporate | applied |
| C24 | Paint first branch moves destination top upward by offset and extends bottom by source height; second branch crops source top and shortens destination bottom using limit-offset. | exact | arithmetic instructions | UID0004MD rectangle semantics/formal body; `000000010048` | incorporate | applied |
| C25 | Paint has no target-local null, clip, or empty-rect guard; callbacks receive exact rectangles. | exact | complete paint CFG | UID0004MD `Negative Evidence And Exclusions`; `000000010048` | incorporate | applied |
| C26 | UID0000CJ remains class owner; UID0000NI remains file owner; generic UID00019W/UID0000CI are read-only parallel-sibling support. | very strong | vtables/base/source family/docs | UID00019X/UID0000CJ ownership retained; UID0000NI/UID00019W/UID0000CI re-read unchanged | already-present | already-present |
| C27 | Class position `100` and child positions `110/120/130/140` provide deterministic made-by source order without colliding with the generic class family. | strong | physical order/emitter hierarchy | UID0000CJ and UID0004MA-UID0004MD metadata; generated lines 7/27/83/89/110; `000000010056` | incorporate | applied |
| C28 | Target and class are `92/94`; children clear the >85 C++ gate with individually justified applied scores. | strong | C01-C27 blocker closure | UID00019X/UID0000CJ `92/94`; UID0004MA `92/94`; UID0004MB-MD `93/95`; scoped validators | incorporate | applied |
| C29 | Evidence-time TextEditPane needed compile-visible declarations for four omitted consumed helpers and public OnDraw; callback validator `000000010051` applied them while retaining accepted `SetEditActiveState` and all other existing declaration detail. | strong | exact helper ABIs/current class block | UID0000EO exact bounded sync; validator `000000010051` | incorporate | applied |
| C30 | Exact formal route is blank aggregate, class `[[CHILDREN]]` declaration, four child bodies, bounded TextEdit/Dialog/NewHistory compile support, compiler wrappers blank, by-file prose only, and no duplicate target source. | very strong | by-structure/by-memory rules/full inventory | Destinations 1-10 applied; generated exact counts `1/1/1/1/1`, aggregate/ABI counts `0/0`; `000000010056` | incorporate | applied |

## Positive Evidence Summary

- Four exact IDA functions, three alignment spans, one immediate successor, and function-specific hashes provide a complete physical inventory.
- One direct NewHistory caller proves constructor liveness and supplies exact resource, rectangle, interval, and object-size evidence.
- Primary/secondary/tertiary vtable data prove class identity, timer and paint dispatch, destructor wrappers, and multiple-inheritance adjusted views.
- Generic sibling functions are near-isomorphic positive controls. The only constructor semantic difference is temporary-editor read-only mode; the only timer semantic difference is +2/close versus +1/wrap.
- Exact member construction/destruction offsets resolve the entire derived layout and lifetime.
- TextEdit, EPFTileContext, GrafPort, Surface callback, DialogPane, MemoryMan, and DATFile support docs converge with the live helper sequence.
- Singleton/resource xrefs separate target `MADEBY` input from NewHistory `HISTORYN` assets.

## IDA MCP Facts

### Function metrics and complete target-local branch inventory

| Start | IDA prototype | Bytes / instructions / blocks | Target-local normal branches |
| --- | --- | --- | --- |
| `0x004ff400` | `_DWORD *__thiscall(_DWORD *, wchar_t *, int *, char)` | 580 / 211 / 15 | one compiler new-result test at `0x004ff50e -> 0x004ff53e`; remaining non-linear entries are SEH/unwind lowering, not source conditionals |
| `0x004ff650` | `void __thiscall(_DWORD *)` | 108 / 36 / 3 | no source branch; extra blocks are frame-handler lowering |
| `0x004ff6c0` | `char __thiscall(int adjustedThis, int, int, int)` | 101 / 29 / 5 | `eventId != 0` skips all work; `newOffset <= limit` skips reset/close |
| `0x004ff730` | `int __thiscall(int this)` | 157 / 56 / 4 | one signed `m_scrollOffset < paneHeight` split; security-cookie epilogue is compiler lowering |

Complete modeled constructor callee set: `sub_4949E0` ControlPane constructor, `sub_457A60` EPFTileContext initialize, `sub_4B8BF0` GrafPort constructor, `sub_516030` GetMemoryMan, `sub_49C130/180/2F0/310/240/160` DATFile lifecycle/read operations, `sub_594F30/595250/594F70` TextEditScrap lifecycle/import, `sub_516050/516170` MemoryMan allocate/free, `sub_4F4AA0` operator new, `sub_4B60B0` legacy-asset mode, `sub_58DCE0` TextEditPane constructor, `sub_5904A0` scrap paste, `sub_58F1A0` line count, `sub_4B7C50` rectangle initialization, `sub_58F4B0/58F490` rectangle setters, `sub_58EA80` edit-state setter, `sub_5446B0` pane mode, `sub_4B9820` surface update, virtual slot `+0x44` draw, `sub_4B8DF0` surface-context getter, `sub_457E00` context copy, `sub_457AB0` mask build, `sub_4B98C0` end paint, virtual deleting destructor, `sub_5975E0` timer schedule, and compiler security/EH support. No unidentified target call remains outside a source or compiler disposition.

### Aggregate bytes and boundaries

| Span | Classification | Size | SHA256 / bytes |
| --- | --- | --- | --- |
| `0x004ff400-0x004ff644` | constructor | `0x244` / 580 | `05F7862E8D138FE169B32AA6DA91F6801C3F96CE454D4A490075FB8DE0A8A99E` |
| `0x004ff644-0x004ff650` | alignment | 12 | all `cc`; `A0D08A155AA4E9AADD5DA959565DE99ACC8DC9DBDA96EC3852DDE76C97330902` |
| `0x004ff650-0x004ff6bc` | ordinary destructor | `0x6c` / 108 | `02DF6CF84D9E1ECE0BE8465F35BCC53BC6F4363406AD1825E67FE350F445D3F1` |
| `0x004ff6bc-0x004ff6c0` | alignment | 4 | all `cc`; `8843B54D2DF63CA265CF4A05D27DD2B29A74FB476D296DD44A0E171D74B441CA` |
| `0x004ff6c0-0x004ff725` | timer callback | `0x65` / 101 | `5486AF915F5AB423B1102D19E2972ACE9A1BAB2D0B7B956789CDB29AF66A11B6` |
| `0x004ff725-0x004ff730` | alignment | 11 | all `cc`; `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943` |
| `0x004ff730-0x004ff7cd` | paint callback | `0x9d` / 157 | `08651A82B2CDB7115843F5BD9064A7F56F552BFA30DD451F2F1649609F8082FF` |
| `0x004ff7cd-0x004ff7d0` | successor alignment outside target | 3 | `cc cc cc`; `E668F0A1B72560CED14C54488A25335D11336448950BB6485267EBC5EB3FECEE` |

`lookup_funcs(0x004ff7cc)` resolves to `sub_4FF730`; `lookup_funcs(0x004ff7cd)` reports no function. The successor ScrolledPicture constructor begins at `0x004ff7d0`.

### Constructor exact operation map

1. Calls `ControlPane::ControlPane(8,bounds)`; compiler then stores made-by primary/secondary/tertiary vptrs.
2. Calls EPFTileContext initializer on `this+0x108`; constructs embedded GrafPort on `this+0x130`.
3. Obtains MemoryMan, constructs local DATFile, constructs local TextEditScrap.
4. Opens caller `resourceName`, gets `dataSize`, allocates literal `1001`, reads exactly `dataSize`, writes `buffer[dataSize]=0`, closes the DATFile, imports the block into the scrap, and frees the temporary buffer.
5. Computes `width=bounds->right-bounds->left` and `height=bounds->bottom-bounds->top`.
6. Allocates exactly `0x174` bytes and constructs TextEditPane with `(width,height,width,height,143,0,0,0,1,IsLegacyAssetMode(),0,0)`.
7. Calls the scrap-consuming paste helper `0x005904a0`; queries line count at `0x0058f1a0`; creates `(0,0,width,12*lineCount)` text bounds.
8. Stores `m_scrollLimit=height+textBounds.Height()`, `m_scrollOffset=0`, and the signed interval byte.
9. Calls inherited UpdateRenderRegion; copies text bounds to TextEditPane fields `+0x110` and `+0x120`; calls accepted `SetEditActiveState(false,false)`; sets mode `1`; updates surface info; invokes virtual OnDraw.
10. Gets the editor surface context; copies it into `m_renderedText`; builds the encoded mask; ends paint; deletes the temporary editor through its scalar wrapper.
11. Schedules `(eventId=0, interval=1, param1=0, param2=0)`; then automatic local TextEditScrap and DATFile destruction completes the normal path.
12. Constructor EH cleanup addresses are compiler unwind lowering for those locals/member/base lifetimes. They are not extra source methods or handwritten catch blocks.

### Destructor exact operation map

- Compiler restores made-by vptrs around teardown.
- Calls `m_renderedText.ReleaseBuffers()` at `this+0x108`.
- Calls embedded `m_renderPort.~GrafPort()` at `this+0x130`.
- Calls `Pane::~Pane()` through ControlPane's implicit base teardown.
- Source-level destructor body needs only the explicit EPFTileContext release. Member GrafPort and base cleanup must not be manually duplicated.

### Timer exact operation map

- Incoming callback receiver is the tertiary TimerHandler view at complete object `+0xa4`; source projection hides the compiler adjustment.
- If `eventId != 0`, it performs no field write/call and returns true.
- If `eventId == 0`, it adds literal `2` to `m_scrollOffset`.
- If the new offset is `<= m_scrollLimit`, it skips close/reset.
- If the new offset is `> m_scrollLimit`, it writes zero to `m_scrollOffset`, loads `g_pNewHistoryDialog` from `0x0069b49c`, and calls close wrapper `0x0049dad0` with no null check.
- It sign-extends `m_scrollInterval`, schedules timer `0`, invalidates inherited `m_bounds`, and returns AL `1`.
- `retn 0x0c` proves three explicit 32-bit callback arguments.

### Paint exact operation map

- Calls accepted `SetDrawColor(0)`.
- Invokes slot 7 at `0x0069b3fc` with receiver plus `&m_bounds`, accepted at this consumer as `FillRect(&m_bounds)`.
- Copies `m_renderedText.bounds` to local `sourceRect` and `m_bounds` to local `destinationRect`.
- Computes pane height as `m_bounds.bottom-m_bounds.top`.
- For `m_scrollOffset < paneHeight`, writes `destinationRect.top=destinationRect.bottom-m_scrollOffset`, then `destinationRect.bottom=destinationRect.top+sourceRect.Height()`.
- Otherwise writes `sourceRect.top=sourceRect.bottom+m_scrollOffset-m_scrollLimit` and `destinationRect.bottom=m_scrollLimit-m_scrollOffset`; destination top remains the inherited bounds top.
- Invokes slot 2 `g_pfnBlitSprite(this,&m_renderedText,&sourceRect,&destinationRect,0,NULL,NULL)` exactly once. The callback return is not a source return contract.

### Vtables and class views

- Primary made-by vtable begins at `0x0061d7ec`; primary paint cell `0x0061d830 -> 0x004ff730`.
- Secondary vtable begins at `0x0061d854`; destructor adjustor is `0x0050257c`.
- Tertiary vtable begins at `0x0061d884`; destructor adjustor is `0x00502587`; timer cell `0x0061d888 -> 0x004ff6c0`.
- Scalar deleting destructor target is `0x00502c10` through the primary destructor slot.
- Next class locator/vtable family begins at `0x0061d88c/0x0061d890`, matching ScrolledPicture.

## Function / Child Inventory

| Source order | Exact range | Recommended child/path | Owner/emitter | Position | Score | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| container | `0x004ff400-0x004ff7cd` | UID00019X existing aggregate | semantic owner UID0000CJ; emitter blank | blank | `92/94` | `FALSE`, blank formal, exact child/padding index |
| declaration | class page | UID0000CJ existing class | owner/emitter UID0000NI | `100` | `92/94` | exact class declaration with `[[CHILDREN]]` |
| 1 | `0x004ff400-0x004ff644` | UID0004MA `0x004ff400-0x004ff644.ScrolledTextControlPaneForMadeByConstructor.md` | UID0000CJ | `110` | `92/94` | source constructor body |
| 2 | `0x004ff650-0x004ff6bc` | UID0004MB `0x004ff650-0x004ff6bc.ScrolledTextControlPaneForMadeByDestructor.md` | UID0000CJ | `120` | `93/95` | ordinary source destructor body |
| 3 | `0x004ff6c0-0x004ff725` | UID0004MC `0x004ff6c0-0x004ff725.ScrolledTextControlPaneForMadeByOnTimerEvent.md` | UID0000CJ | `130` | `93/95` | timer source body |
| 4 | `0x004ff730-0x004ff7cd` | UID0004MD `0x004ff730-0x004ff7cd.ScrolledTextControlPaneForMadeByOnPaintFrame.md` | UID0000CJ | `140` | `93/95` | paint source body |
| compiler support | `0x00502c10-0x00502cbd` | UID0003LX existing scalar wrapper | NONE; emitter blank | blank | `90/93` | compiler-only, `FALSE`, blank formal |
| compiler support | `0x00502566-0x00502592` | UID0003LY existing thunks | NONE; emitter blank | blank | retain | compiler-only, already `FALSE`, blank formal |

The authorized callback allocated these UIDs serially through validators `000000010045`-`000000010048`; the report did not fabricate identifiers.

## Direct Xref / Caller Inventory

| Target | Inbound route | Classification |
| --- | --- | --- |
| `0x004ff400` constructor | code call `0x0050052e` in `sub_500410` NewHistory constructor | sole direct constructor caller; allocates `0x1dc`, passes `L"MADEBY"`, local rect, and `30` |
| `0x004ff650` ordinary destructor | reached by class destructor/scalar-wrapper teardown paths | class-owned source destructor, not a public ordinary caller surface |
| `0x004ff6c0` timer | data xref `0x0061d888` | tertiary TimerHandler virtual callback |
| `0x004ff730` paint | data xref `0x0061d830` | primary Pane/GrafPort paint virtual callback |
| `0x0069b49c` singleton | target read `0x004ff6eb`; NewHistory publish/cleanup and other destructor support refs | borrowed global; no target ownership transfer |

NewHistory caller construction details:

- Allocates `0x1dc` bytes for the target object.
- Builds local RectBounds `(614,156,950,556)`.
- Passes signed interval `30` and UTF-16 `L"MADEBY"` to the target constructor at `0x0050052e`.
- Adds the resulting ControlPane to the dialog.
- Separately creates/loads `HISTORYN.EPF` and `HISTORYN.PAL`; those are not target inputs.

No other direct constructor call, duplicate full function body, inline copy, callback-table duplicate, or alternate owner route was found.

## Documentation Evidence And IDA Status

- **Evidence-time / pre-callback UID00019X and UID0000CJ state:** both pages correctly identified the made-by role and four methods, but UID00019X still conflated physical inventory with source emission and UID0000CJ still had blank formal C++; the exact layout, leaf bodies, and source order were unresolved in those pages.
- **Current applied UID00019X state:** validator `000000010049` applied `92/94`, retained semantic `CANONICAL_OWNER:0000CJ`, set `RECONSTRUCTABLE:FALSE`, blanked emitter/position/formal C++, and installed the exact UID0004MA-UID0004MD child/padding inventory. UID00019X is now a non-emitting semantic index and carries no duplicate body.
- **Current applied UID0000CJ state:** validator `000000010050` applied `92/94`, retained owner/emitter UID0000NI and `RECONSTRUCTABLE:TRUE`, set position `100`, and installed the exact `ControlPane`-derived `0x1dc` class declaration with typed members, implicit tail alignment, and `[[CHILDREN]]`.
- **Current verify-only UID0000NI state:** it correctly groups generic and made-by text scrollers in one source module and remains the file owner/emitter. Its by-file root has no reconstruction metadata and required no callback edit.
- **Current verify-only UID00019W/UID0000CI state:** they remain independent parallel-sibling comparison support, unchanged and without a duplicate report or target body from this assignment.
- **Current applied UID0001A3 state:** NewHistory continues to assign `MADEBY` construction to ScrolledTextControlPane source and `HISTORYN.EPF/PAL` to NewHistory source; validator `000000010054` changed only the made-by constructor call to the compile-correct `&rect` argument.
- **Current verify-only UID0003LY state:** it retains the correct `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal C++ compiler-thunk disposition and remained unchanged.
- **Evidence-time / pre-callback UID0003LX state:** it incorrectly retained class owner/emitter/reconstructable metadata even though it is a scalar deleting wrapper. That state is superseded; source destructor semantics belong only to UID0004MB.
- **Current applied UID0003LX state:** validator `000000010053` applied `90/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal C++, and explicit compiler-only scalar-deleting-wrapper history.
- **Evidence-time / pre-callback UID0000EO state:** TextEditPane had a strong declaration but omitted the four constructor-consumed helper declarations and placed `OnDraw` under protected access.
- **Current applied UID0000EO state:** validator `000000010051` preserved its score, owner, existing methods, fields, and `SetEditActiveState`; added `class TextEditScrap;`; added public `PasteFromClipboard`, `GetLineCount`, `SetViewportBounds`, and `SetTextAreaBounds`; and moved the existing `virtual void OnDraw();` declaration to public exactly once.
- **Evidence-time / pre-callback UID00003T state:** DialogPane had the accepted `CloseDialog` spelling/body route but declared it protected, which was compile-incompatible with the unrelated made-by `ControlPane` caller.
- **Current applied UID00003T state:** validator `000000010052` preserved all other declaration detail and moved `void CloseDialog();` to public exactly once.
- **Current verify-only UID0000TN/Surface state:** callback support still accepts `FillRect(&m_bounds)` at pane consumers and `g_pfnBlitSprite` / `SurfaceSpriteBlitProc` for slot 2. No callback type invention or Surface edit was required.

## Ranked Ownership Analysis

### 1. UID0000CJ ScrolledTextControlPaneForMadeBy class ownership - accepted

The receiver is a complete made-by object, constructor installs made-by vtables, timer/paint occupy made-by slots, destructor tears down made-by members, and NewHistory allocates the exact class size. UID0000CJ remains semantic owner and owns all four exact children.

### 2. UID0000NI ScrolledTextControlPane source-file ownership - accepted as file route, rejected as direct method owner

Generic and made-by sibling classes share one DAT-text scroller implementation family and are physically adjacent. UID0000NI is the strongest existing source module and now emits the accepted made-by declaration/children; class methods remain canonically owned by their classes.

### 3. Generic UID0000CI/UID00019W, NewHistory, GrafPort, TextEditPane, or no-owner - rejected

- Generic scroller is a parallel class with distinct vtables and direct ControlPane construction.
- NewHistory is the sole caller/context and owns the singleton/background, not the control implementation.
- GrafPort/TextEditPane/DATFile/Surface are dependencies whose broad callers reject feature ownership.
- No-owner is contradicted by exact class vtables, one live constructor call, stateful receiver fields, and complete source methods.

### Proposed new file/grouping, if applicable

No new file was needed. The callback used existing UID0000NI route `NexusTK/ui/controls/ScrolledTextControlPane.cpp` and created only exact method children UID0004MA-UID0004MD under UID0000CJ.

## Source Placement

Applied generated/source order in UID0000NI:

1. Existing generic `ScrolledTextControlPane` declaration/children retain their independent order.
2. UID0000CJ made-by class declaration at position `100`.
3. UID0004MA made-by constructor child at `110`.
4. UID0004MB ordinary destructor child at `120`.
5. UID0004MC timer child at `130`.
6. UID0004MD paint child at `140`.

The class block contains `[[CHILDREN]]`; the four exact children carry bodies. UID00019X carries no C++. UID0003LX/UID0003LY carry no C++. UID0000NI receives prose/include/source-placement guidance only and must never receive `RECONSTRUCTION_CPP` metadata because by-file roots do not emit child C++ directly.

Highest-probability header dependencies are existing control/render/text/archive headers exposing `ControlPane`, `RectBounds`, `EPFTileContext`, `GrafPort`, `DATFile`, `TextEditScrap`, `TextEditPane`, MemoryMan, `g_pNewHistoryDialog`, and Surface callbacks. UID0000NI remains a verify-only prose source route with no reconstruction metadata; generated compile visibility comes through the validated class/method emitters and accepted support declarations, not a by-file formal block.

## Range / Split / Padding / Reclassification Analysis

- Parent range is exact and should not shrink. It remains valuable as a semantic inventory and physical coverage index.
- Four source methods are disjoint and modeled. UID0004MA-UID0004MD now provide one exact leaf page each, making declaration/body ownership, score, source position, and generated counting unambiguous.
- Internal `cc` spans are compiler alignment: 12 bytes after constructor, 4 after destructor, 11 after timer. They are not ignored source children and must not enter method bodies.
- Three `cc` bytes after parent end precede ScrolledPicture and remain outside UID00019X.
- UID0003LX is non-contiguous scalar-delete ABI support. It does not extend target range and does not justify a fifth source method.
- UID0003LY contains non-contiguous destructor adjustors shared with generic/made-by class views. It remains compiler-only.
- Constructor EH cleanup addresses are compiler lowering for local DATFile/TextEditScrap/editor/member lifetimes. They are not source-authored catch helpers or new children.
- No jump table or literal pool lies inside the target. Timer and paint branches remain entirely within their exact method ranges.

## Negative Evidence Summary

- No second constructor caller or duplicate full body.
- No inheritance call to generic ScrolledTextControlPane; both siblings call ControlPane directly.
- No target use of HISTORYN resources, palette, NewHistory background fields, or dialog control manager.
- No file-open result check, allocation null check, data-size cap, read result check, TextEditPane null check, or cleanup branch written by target source.
- No persistent ownership of DATFile, TextEditScrap, temporary text buffer, or temporary TextEditPane after constructor return.
- No explicit GrafPort cleanup should be handwritten in the destructor.
- No timer work for nonzero event id; no close when offset equals limit; no singleton null check; no param1/param2 use.
- No paint-local clipping, null check, palette lookup, draw mode change, alternate blit mode, options record, or return-value use.
- No source reason to emit vptr stores, scalar deleting destructor, adjustor thunks, padding, EH labels, or callback-table storage.
- No evidence supports generic reserve bytes, a separate image object pointer, a source-level generic-scroller base, a NewHistory-owned implementation, or a new source file.

## IDA Rename / Type / Comment Recommendations

No IDA mutation was performed. Future safe directions:

| Address/entity | Recommendation | Basis/caveat |
| --- | --- | --- |
| `0x004ff400` | `ScrolledTextControlPaneForMadeBy::ScrolledTextControlPaneForMadeBy` | exact constructor role; lexical class name accepted from RTTI/docs |
| `0x004ff650` | `ScrolledTextControlPaneForMadeBy::~ScrolledTextControlPaneForMadeBy` | ordinary destructor, not scalar wrapper |
| `0x004ff6c0` | `ScrolledTextControlPaneForMadeBy::OnTimerEvent` | tertiary vtable/timer ABI; exact spelling inferred but project-consistent |
| `0x004ff730` | `ScrolledTextControlPaneForMadeBy::OnPaintFrame` | primary paint slot; accepted family spelling |
| `this+0x108` | `m_renderedText : EPFTileContext` | initializer/copy/mask/release and exact 0x28 extent |
| `this+0x130` | `m_renderPort : GrafPort` | constructor/destructor and exact 0xa0 extent |
| `this+0x1d0` | `m_scrollOffset : int` | timer increment/reset and paint branch/arithmetic |
| `this+0x1d4` | `m_scrollLimit : int` | constructor write, timer compare, paint crop math |
| `this+0x1d8` | `m_scrollInterval : signed char` | byte store plus signed extension before scheduling |
| `0x0061d830` | comment `made-by primary paint slot -> OnPaintFrame` | exact data value `0x004ff730` |
| `0x0061d888` | comment `made-by tertiary timer slot -> OnTimerEvent` | exact data value `0x004ff6c0` |
| `0x00502c10` | `ScrolledTextControlPaneForMadeBy::scalar deleting destructor` | compiler-only wrapper; do not rename to ordinary destructor |
| `0x005904a0` | `TextEditPane::PasteFromClipboard(TextEditScrap *)` | consumes scrap into editor; original spelling inferred |
| `0x0058f1a0` | `TextEditPane::GetLineCount() const` | returns stored line count minus sentinel |
| `0x0058f4b0` | `TextEditPane::SetViewportBounds(const RectBounds *)` | exact 16-byte copy to `+0x110`; spelling inferred |
| `0x0058f490` | `TextEditPane::SetTextAreaBounds(const RectBounds *)` | exact 16-byte copy to `+0x120`; spelling inferred |

## First-Draft C++ Recommendation

- Eligible and applied: yes, on the exact children only. All behavioral, ownership, layout, ABI, liveness, source-placement, and compile-support blockers are resolved to the highest-probability period source form.
- Third-party import: not applicable.
- Each C++ snippet below remains destination-specific exact formal text inside literal managed lines. Validator-assigned child UIDs are recorded in the destination headings.

### Destination 1 - UID00019X aggregate blank formal

Exact destination: `by-memory/0x004ff400-0x004ff7cd.ScrolledTextControlPaneForMadeByCore.md`. Applied state is `92/94`, retained `CANONICAL_OWNER:0000CJ`, `RECONSTRUCTABLE:FALSE`, blank emitter/position, retained range and `Nested:0`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 2 - UID0000CJ exact class declaration

Exact destination: `by-class/ScrolledTextControlPaneForMadeBy.md`. Applied state is `92/94`, retained owner/emitter UID0000NI and `RECONSTRUCTABLE:TRUE`, with position `100`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
class ScrolledTextControlPaneForMadeBy : public ControlPane
{
public:
    ScrolledTextControlPaneForMadeBy(const wchar_t *resourceName,
                                     const RectBounds *bounds,
                                     signed char scrollInterval);
    virtual ~ScrolledTextControlPaneForMadeBy();

    virtual bool OnTimerEvent(int eventId, int param1, int param2);
    virtual void OnPaintFrame();

private:
    EPFTileContext m_renderedText;
    GrafPort m_renderPort;
    int m_scrollOffset;
    int m_scrollLimit;
    signed char m_scrollInterval;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

The three bytes after `m_scrollInterval` are implicit class alignment. Do not add an explicit reserve/padding field.

### Destination 3 - UID0004MA constructor child

Exact applied destination: `by-memory/0x004ff400-0x004ff644.ScrolledTextControlPaneForMadeByConstructor.md`, validator-assigned UID0004MA, owner/emitter UID0000CJ, position `110`, score `92/94`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ScrolledTextControlPaneForMadeBy::ScrolledTextControlPaneForMadeBy(
    const wchar_t *resourceName,
    const RectBounds *bounds,
    signed char scrollInterval)
    : ControlPane(8, bounds)
{
    m_renderedText.Initialize();

    MemoryMan *memoryMan = GetMemoryMan();
    DATFile dataFile;
    TextEditScrap scrap;

    dataFile.Open(resourceName);
    int dataSize = dataFile.GetSize();
    char *data = static_cast<char *>(
        memoryMan->AllocateBufferMemory(1001));
    dataFile.Read(data, dataSize);
    data[dataSize] = '\0';
    dataFile.Close();
    scrap.SetFromClipboardBlock(data, dataSize);
    memoryMan->FreeBufferMemory(data);

    int width = bounds->right - bounds->left;
    int height = bounds->bottom - bounds->top;
    TextEditPane *editor = new TextEditPane(
        width, height, width, height,
        143, 0, 0, 0, 1, IsLegacyAssetMode(), 0, 0);

    editor->PasteFromClipboard(&scrap);

    RectBounds textBounds;
    InitRectBounds(&textBounds,
                   0,
                   0,
                   width,
                   12 * editor->GetLineCount());
    m_scrollLimit = height + textBounds.bottom - textBounds.top;
    m_scrollOffset = 0;
    m_scrollInterval = scrollInterval;

    editor->UpdateRenderRegion(&textBounds);
    editor->SetViewportBounds(&textBounds);
    editor->SetTextAreaBounds(&textBounds);
    editor->SetEditActiveState(false, false);
    editor->SetMode(1);
    editor->UpdateSurfaceInfo();
    editor->OnDraw();
    editor->GetSurfaceContext()->CopyTo(&m_renderedText);
    m_renderedText.BuildEncodedMask();
    editor->EndPaint();
    delete editor;

    m_timerHandler.ScheduleTimer(0, 1, 0, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 4 - UID0004MB ordinary destructor child

Exact applied destination: `by-memory/0x004ff650-0x004ff6bc.ScrolledTextControlPaneForMadeByDestructor.md`, validator-assigned UID0004MB, owner/emitter UID0000CJ, position `120`, score `93/95`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ScrolledTextControlPaneForMadeBy::~ScrolledTextControlPaneForMadeBy()
{
    m_renderedText.ReleaseBuffers();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 5 - UID0004MC timer child

Exact applied destination: `by-memory/0x004ff6c0-0x004ff725.ScrolledTextControlPaneForMadeByOnTimerEvent.md`, validator-assigned UID0004MC, owner/emitter UID0000CJ, position `130`, score `93/95`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ScrolledTextControlPaneForMadeBy::OnTimerEvent(
    int eventId,
    int,
    int)
{
    if (eventId == 0) {
        m_scrollOffset += 2;
        if (m_scrollOffset > m_scrollLimit) {
            m_scrollOffset = 0;
            g_pNewHistoryDialog->CloseDialog();
        }

        m_timerHandler.ScheduleTimer(
            0, m_scrollInterval, 0, 0);
        InvalidateRect(&m_bounds);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 6 - UID0004MD paint child

Exact applied destination: `by-memory/0x004ff730-0x004ff7cd.ScrolledTextControlPaneForMadeByOnPaintFrame.md`, validator-assigned UID0004MD, owner/emitter UID0000CJ, position `140`, score `93/95`.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrolledTextControlPaneForMadeBy::OnPaintFrame()
{
    SetDrawColor(0);
    FillRect(&m_bounds);

    RectBounds sourceRect = m_renderedText.bounds;
    RectBounds destinationRect = m_bounds;
    int paneHeight = m_bounds.bottom - m_bounds.top;

    if (m_scrollOffset < paneHeight) {
        destinationRect.top =
            destinationRect.bottom - m_scrollOffset;
        destinationRect.bottom =
            destinationRect.top + sourceRect.bottom - sourceRect.top;
    } else {
        sourceRect.top =
            sourceRect.bottom + m_scrollOffset - m_scrollLimit;
        destinationRect.bottom =
            m_scrollLimit - m_scrollOffset;
    }

    g_pfnBlitSprite(this,
                    &m_renderedText,
                    &sourceRect,
                    &destinationRect,
                    0,
                    NULL,
                    NULL);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 7 - UID0000EO bounded TextEditPane declaration sync

Exact destination: `by-class/TextEditPane.md`. The applied block preserved the existing declaration and all accepted methods/fields; added `class TextEditScrap;` and the four formerly omitted public helper declarations below; retained existing `SetEditActiveState(bool,bool)`; and moved existing `virtual void OnDraw();` from protected to public exactly once. The exact resulting formal block is:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
class Event;
class List;
class TextEditScrap;
namespace mystr {
template <class CharT> struct mychar_traits;
template <class CharT, class Traits> class StringBase;
}

class TextEditPane : public ScrollablePane
{
public:
    TextEditPane(int maxWidth,
                 int maxHeight,
                 int contentWidth,
                 int contentHeight,
                 int backgroundColor,
                 int scrollColor,
                 char hasHorizontalScrollbar,
                 char hasVerticalScrollbar,
                 unsigned int styleFlags,
                 unsigned short legacyAssetMode,
                 char isReadOnly,
                 char inputMode);
    virtual ~TextEditPane();

    short CopyWideText(wchar_t *outText, short capacity) const;
    mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > &
        GetText(mystr::StringBase<wchar_t,
                mystr::mychar_traits<wchar_t> > &outText) const;
    short GetTextLength() const;
    unsigned char *EncodeState(unsigned short *outSize) const;
    void DecodeState(const unsigned char *data, unsigned short size);

    void InsertText(const wchar_t *text, int flags);
    void SetText(const wchar_t *text, short length, short selectStart, short selectEnd);
    void SetSelectionRange(short start, short end);
    void SelectAll();
    void SetMaxLength(short maxLength);
    void SetMaxLines(short maxLines);
    void EnableMaskedText();
    void SetEditActiveState(bool editActive, bool selectAll);
    bool IsEmpty() const;
    void ScrollToTop();
    unsigned char GetMode() const { return m_mode; }

    void PasteFromClipboard(TextEditScrap *scrap);
    short GetLineCount() const;
    void SetViewportBounds(const RectBounds *bounds);
    void SetTextAreaBounds(const RectBounds *bounds);
    virtual void OnDraw();

protected:
    virtual int OnKeyEvent(Event *event);
    virtual int OnMouseEvent(Event *event);

    void InvalidateTextRegion(short start, short end);
    void DeleteSelection();
    void FindWordBoundary(short index, short *wordStart, short *wordEnd) const;
    void AutoScrollSelectionToPoint(int x, int y);

private:
    List *m_textStorage;
    List *m_lineTable;
    List *m_styleRuns;
    List *m_formatRecords;
    short m_selectionStart;
    short m_selectionEnd;
    unsigned int m_editorFlags;
    unsigned char m_mouseMode;
    unsigned char m_dirty;
    unsigned char m_maskedText;
    short m_maxLength;
    short m_maxLines;
    unsigned char m_inputState;
    unsigned char m_lineLimitState;
    unsigned char m_formatMode;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 8 - UID00003T DialogPane access sync

Exact destination: `by-class/DialogPane.md`. The applied block preserved the entire accepted class declaration and moved only `void CloseDialog();` from protected to public exactly once. The exact resulting formal block is:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
class DialogPane : public Pane
{
public:
    DialogPane(const wchar_t *title, unsigned short dialogConfigId, unsigned char paneMode);
    virtual ~DialogPane();

    virtual void OnCreate(const RectBounds *bounds, int zOrder, Pane *parentPane, Layer *layer);
    virtual void OnShow(Pane *afterPane, Pane *beforePane);
    virtual void OnDestroy();
    virtual void OnHide();
    virtual bool OnInputEvent(InputEvent *event);
    virtual bool OnKeyInput(InputEvent *event);
    virtual void OnControlCommand(int controlIndex, int notifyCode);
    int FindControlIndex(const ControlPane *control) const;
    void CloseDialog();

protected:
    void StoreClampRect(const RectBounds *bounds);
    void ClearClampRect();
    void SetTitleText(const wchar_t *title);
    void SetBackgroundResource(const wchar_t *resourceName, const wchar_t *paletteName);
    void AddControl(unsigned char category, ControlPane *control);
    void AddControl(ControlPane *control);
    void RemoveControl(ControlPane *control);
    void RemoveControl(int controlIndex);
    int GetControlCount() const;
    ControlPane *GetChildControl(int controlIndex) const;

    template <class T>
    T *GetChild(int controlIndex) const
    {
        return static_cast<T *>(GetChildControl(controlIndex));
    }

    void SetFocusedControl(int controlId);
    void SetPendingControl(int controlId);
    virtual void SetHoverControl(int controlId);
    void SetSelectionVisualState(int controlId, unsigned char state);
    void ActivateFocusedControl();
    void ActivatePendingControl();
    void RedrawSelectedControl();

    void SlideOpenVertical();
    void SlideCloseVertical();
    void SlideOpenHorizontal(unsigned char direction);
    void SlideCloseHorizontal(unsigned char direction);
    void SlideShrink(unsigned char direction);
    void SlideExpand(unsigned char direction);

    wchar_t m_title[128];
    unsigned short m_dialogConfigId;
    List *m_controlManager;
    int m_focusedControlId;
    int m_pendingControlId;
    unsigned char m_isTitleDragActive;
    unsigned char m_savedPaneMode;
    int m_dragOriginX;
    int m_dragOriginY;
    int m_activeControlId;
    unsigned char m_pressedControlActive;
    int m_pressedControlId;
    unsigned char m_pressedHitType;
    int m_selectedControlId;
    unsigned char m_selectionVisualState;
    unsigned char m_clampDragToBounds;
    int m_dragClampMinY;
    int m_dragClampMinX;
    int m_dragClampMaxY;
    int m_dragClampMaxX;
    EPFTileContext m_tileContext;
    DialogBackgroundState m_backgroundState;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 9 - UID0003LX scalar deleting wrapper blank formal

Exact destination: `by-memory/0x00502c10-0x00502cbd.ScrolledTextControlPaneForMadeByScalarDeletingDestructor.md`. Applied state is `90/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/position, with the exact range and `Nested:0` retained.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 10 - UID0001A3 NewHistory caller signature sync

Exact destination: `by-memory/0x00500410-0x00500632.NewHistoryDialogPaneCore.md`. The applied block preserved all accepted NewHistory behavior and changed only the made-by constructor argument from `rect` to `&rect` so it matches Destination 2. The exact resulting formal block is:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace {
constexpr unsigned char kEventLeftButtonUp = 3;
constexpr unsigned char kEventRightButtonUp = 6;
constexpr unsigned char kEventKeyInput = 8;
}

NewHistoryDialogPane::NewHistoryDialogPane()
    : DialogPane(L"", -1, true)
{
    g_pNewHistoryDialog = this;

    RectBounds rect;
    InitRectBounds(&rect, 0, 0, 1024, 768);
    AddControl(new EPFImageControlPane(L"HISTORYN.EPF", 0, true, rect, L"HISTORYN.PAL"));

    InitRectBounds(&rect, 614, 156, 950, 556);
    AddControl(new ScrolledTextControlPaneForMadeBy(L"MADEBY", &rect, 30));

    SetBackgroundResource(L"HISTORYN.EPF", 0);
    SetMode(1);

    InitRectBounds(&rect, 0, 0, 1024, 768);
    Create(rect, 0, 0, dword_69B36C);
    Show(false, g_pMainUiGraph);
}

bool NewHistoryDialogPane::OnKeyInput(const Event& event)
{
    if (event.type == kEventKeyInput) {
        const char key = NarrowKeyCode(event.keyCode, event.defaultChar);
        if (key == '\r' || key == '\x1b' || key == ' ') {
            Close();
        }
    }

    return true;
}

bool NewHistoryDialogPane::OnMouseInput(const Event& event)
{
    if (event.type == kEventLeftButtonUp || event.type == kEventRightButtonUp) {
        Close();
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

No formal block belongs on UID0000NI by-file. UID0003LY remains blank and unchanged. UID00019W/UID0000CI remain read-only comparison. Destination 10 is caller-signature support only and must not become a duplicate target body or a rewrite of unrelated NewHistory behavior.

## Final Recommendation

- C01-C30 and Destinations 1-10 are applied exactly in the supervisor-authorized implementation callback.
- UID00019X is `92/94`, semantic owner UID0000CJ, `FALSE`, blank emitter/position/formal, with the complete child/padding inventory.
- UID0000CJ is `92/94`, position `100`, with the exact typed class declaration and `[[CHILDREN]]`.
- Exact children UID0004MA/UID0004MB/UID0004MC/UID0004MD were registered serially and emit Destinations 3-6 at positions `110/120/130/140`.
- Bounded compile support is applied to UID0000EO, UID00003T, and the UID0001A3 target call expression after re-reading their current blocks and preserving same-or-greater independent detail.
- UID0003LX is compiler-only `90/93`, `NONE/FALSE`, blank formal. UID0003LY remains unchanged as already-correct compiler-only support.
- Keep UID0000NI as prose-only file route; do not add reconstruction metadata to a by-file root.
- Do not duplicate or rewrite generic UID00019W/UID0000CI, NewHistory resources, callback implementations, or compiler ABI wrappers.

## Recommended Target Doc Changes

For UID00019X:

- Applied `86/90 -> 92/94`.
- Retained `CANONICAL_OWNER:0000CJ`, exact filename/range, and `Nested:0`.
- Applied `RECONSTRUCTABLE:TRUE -> FALSE`.
- Blanked `EMITTER_UIDS` and retained blank position.
- Kept the literal formal block empty as Destination 1.
- Replaced stale below-`95/95` wording with current split-index truth while retaining it as labeled historical policy.
- Added exact aggregate hash, function/padding inventory, object layout, constructor/destructor/timer/paint behavior, caller/vtables/resources/globals, compiler exclusions, negative evidence, source order, and child destinations without compressing existing valid details.

## Recommended Support Doc Changes

| Page | Recommended change | Callback status now |
| --- | --- | --- |
| UID0000CJ class | `92/94`, position 100, Destination 2, exact layout/method/source notes | applied; `000000010050` and final `000000010056` |
| UID0004MA-UID0004MD children | registered serially; positions 110-140; Destinations 3-6 and complete evidence | applied; `000000010045`-`000000010048` |
| UID0000EO TextEditPane | Destination 7 declarations/access only; preserve all existing detail | applied; `000000010051` |
| UID00003T DialogPane | Destination 8: move CloseDialog to public exactly once | applied; `000000010052` |
| UID0003LX scalar wrapper | `90/93`, NONE/FALSE/blank, compiler-only history, Destination 9 | applied; `000000010053` |
| UID0003LY thunks | verify same-or-greater; no edit unless concrete drift | already-present; re-read unchanged |
| UID0000NI file | source-placement/dependency/sibling route; no formal metadata | already-present; no edit required |
| UID0001A3 NewHistory core | Destination 10; retained all caller/resource facts and changed only the made-by caller expression to `&rect` | applied; `000000010054` |
| UID0001PV/UID0000RS singleton | preserve exact timer read/no-null/close relation; no ownership change | already-present; read-only |
| UID00019W/UID0000CI generic sibling | read-only comparison; no duplicate report/body | already-present; read-only |
| EPFTileContext/GrafPort/Surface/DATFile/TextEditScrap/MemoryMan | read-only same-or-greater support unless concrete callback drift is documented | already-present; no drift found |

## Score And Metadata Recommendation

| Entity | Evidence-time state | Applied callback state | Reason / cap |
| --- | --- | --- | --- |
| UID00019X aggregate | `86/90`, TRUE, emitter UID0000CJ | `92/94`, owner UID0000CJ, FALSE, blank emitter/position/C++ | exact split, hashes, methods, layout, owner/source route; lexical names cap below final |
| UID0000CJ class | `86/89`, TRUE, emitter UID0000NI | `92/94`, TRUE, emitter UID0000NI, position 100 | exact base/layout/size/declaration/method inventory/source route |
| UID0004MA constructor | absent | `92/94`, TRUE, owner/emitter UID0000CJ, position 110 | complete behavior/lifetime/ABI; private helper spellings inferred |
| UID0004MB destructor | absent | `93/95`, TRUE, owner/emitter UID0000CJ, position 120 | exact body and compiler-lifetime distinction |
| UID0004MC timer | absent | `93/95`, TRUE, owner/emitter UID0000CJ, position 130 | exact slot/branches/state/global/calls/return |
| UID0004MD paint | absent | `93/95`, TRUE, owner/emitter UID0000CJ, position 140 | exact slot/rectangle arithmetic/callback ABI/order |
| UID0003LX scalar wrapper | `86/90`, owner/emitter UID0000CJ, TRUE | `90/93`, NONE, FALSE, blank emitter/position/C++ | exact compiler ABI wrapper; source destructor moved to exact child |
| UID0003LY thunks | `NONE/FALSE`, blank | retained unchanged | already correct compiler disposition |

All source-bearing children clear the active >85 combined gate. Remaining uncertainty is lexical/source-header spelling, not behavior, liveness, ownership, source placement, ABI, or compile-shape blocking.

## Open Questions With Attempted Resolution

1. **Is the aggregate itself a source body?** Resolved no. Four exact modeled methods and internal alignment require leaf children.
2. **Does the made-by class derive from generic ScrolledTextControlPane?** Resolved no. Both independently call ControlPane and install distinct vtables.
3. **What occupies `+0x108..+0x1dc`?** Resolved fully as EPFTileContext, GrafPort, two ints, one signed byte, implicit tail alignment.
4. **What is the constructor resource?** Resolved as caller-provided `MADEBY`; HISTORYN assets remain NewHistory-owned.
5. **What is `0x005904a0`?** Highest-probability `TextEditPane::PasteFromClipboard(TextEditScrap *)`; exact behavior/ABI and caller set are resolved, original spelling remains a score cap.
6. **Are the two rectangle setters distinguishable?** Resolved behaviorally: exact copies to TextEditPane `+0x110` and `+0x120`; current draft names them viewport and text-area bounds. Original spellings remain a score cap.
7. **Should CloseDialog remain protected?** Resolved no for current formal declarations. The unrelated made-by ControlPane calls it directly, so public access is the only compile-coherent ordinary source form absent unsupported friendship.
8. **Should the timer null-check the singleton?** Resolved no. Binary has no check; source draft preserves that.
9. **Should paint clip or test empty rectangles?** Resolved no. No such target branch exists; callback implementations own downstream clipping behavior.
10. **Should compiler wrappers be emitted?** Resolved no. Ordinary destructor child is source; scalar/adjustor wrappers regenerate from virtual destruction.
11. **Should source move to NewHistoryDialogPane.cpp?** Resolved no. Generic/made-by family and existing UID0000NI route outweigh one caller context.
12. **Can final C++ remain blank because symbols are stripped?** Resolved no. All semantic blockers are closed; inferred human spellings are normal confidence caps under the workflow.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

No manual coverage/tracker edit was performed by B002 during report-only or callback work. Authorized scoped validators derived target/class/new-child state, and the final waited refresh updated validator-owned generated coverage. If a supervisor-owned narrative row is independently required, use this exact durable text:

`UID00019X ScrolledTextControlPaneForMadeByCore: 92/94 non-emitting semantic split index for exact constructor, ordinary destructor, timer, and paint children; class owner UID0000CJ, source route UID0000NI; complete 0x1dc layout, MADEBY DAT/TextEdit render pipeline, +2 close-on-end timer, clipped scrolling blit math, and compiler wrapper/padding exclusions resolved.`

## Follow-Up Actions

- B002's bounded callback, validators, waited generated refresh, exact-count audit, report reconciliation, and lease releases are complete; no further B002 implementation action is pending in this artifact.
- UID0003LY, UID00019W, UID0000CI, UID0000NI, singleton, layout, and generic dependency pages were re-read and remained same-or-greater, so no speculative support edit was made.
- Current or future report validation, execution, path, report count, move, archive, coverage, tracker, and lifecycle state are external validator/supervisor-owned and are not asserted here.

## Confidence

- Range, split, padding, bytes/hash, vtables, caller, resource/global references, object size/layout, constructor sequence, destructor lifetime, timer branches, paint arithmetic, and ownership: `94/100`.
- Human source names/types and source placement: `92/100`.
- Formal first draft: `92/100`; behavior and ABI are complete, while stripped helper/private spellings cap lexical certainty.
- Overall recommendation: `93/100`.

## Validator Results

- Report-only phase: no implementation validator or lease was used before Gate 1. MCP operations against evidence-time database `supervisor_nexustk_20260713` were read-only; no IDA mutation or process management occurred.
- Every implementation validator below exited `0`, reported `ok:1`, and used `--mode file --apply --queue-timeout 240` from `source-3/project-documentation`. The final row additionally used `--wait-generated`.

| Command ID | Timestamp (`-04:00`) | Scoped page / purpose | Exit / ok | Warnings | Validator-owned side effects |
| --- | --- | --- | --- | --- | --- |
| `000000010045` | `2026-07-13T14:13:25` | new constructor child | `0 / 1` | none | inserted UID0004MA; registered owner/emitter/position/block/hash; references/stats; generated deferred |
| `000000010046` | `2026-07-13T14:13:59` | new ordinary-destructor child | `0 / 1` | four `missing_ref_uid` rows for then-unregistered UID0003LX/UID0003LY | inserted UID0004MB; registered metadata/block/hash; references/stats; generated deferred |
| `000000010047` | `2026-07-13T14:14:41` | new timer child | `0 / 1` | none | inserted UID0004MC; registered metadata/block/hash; references/stats; generated deferred |
| `000000010048` | `2026-07-13T14:15:18` | new paint child | `0 / 1` | none | inserted UID0004MD; registered metadata/block/hash; references/stats; generated deferred |
| `000000010049` | `2026-07-13T14:16:47` | UID00019X semantic-index conversion | `0 / 1` | four `missing_ref_uid` rows for UID0003LX/UID0003LY | applied `92/94`, `FALSE`, blank emitter; normalized real-child links; references/stats; generated deferred |
| `000000010050` | `2026-07-13T14:17:59` | UID0000CJ class/layout/declaration | `0 / 1` | six `missing_ref_uid` rows for UID0003LX/UID0003LY | applied `92/94`, position 100, block/hash, child links; references/stats; generated deferred |
| `000000010051` | `2026-07-13T14:18:43` | UID0000EO TextEditPane support | `0 / 1` | eight pre-existing missing-reference rows for UID00040S/00040T/0003M8/0003FD/0003FC | updated declaration block hash and UID0004MA reference; stats; generated deferred |
| `000000010052` | `2026-07-13T14:19:11` | UID00003T DialogPane access | `0 / 1` | ten pre-existing missing-reference rows for UID0003JB/0003AW/0003KD/0003KP | updated declaration block hash and UID0004MC reference; stats; generated deferred |
| `000000010053` | `2026-07-13T14:19:52` | UID0003LX compiler-wrapper disposition | `0 / 1` | two `missing_ref_uid` rows for UID0003LY | applied `90/93`, `NONE/FALSE`, blank emitter/position/block; registry/path/references/stats; generated deferred |
| `000000010054` | `2026-07-13T14:20:25` | UID0001A3 NewHistory pointer call | `0 / 1` | none | updated block hash and UID0004MA reference; stats; generated deferred |
| `000000010056` | `2026-07-13T14:20:49` | UID0000CJ final waited generated verification | `0 / 1` | three UID0003LY missing-reference rows plus project-wide advisory totals (`15` fallback inserts, `83` missing markers, `202` empty emitters) | rebuilt validator registry; refreshed generated metadata/coverage/stats; generated refresh completed; global refresh also updated unrelated concurrent RankingDialog output |

- The missing-reference and project-wide advisory rows are non-fatal validator inventory findings; they did not alter the accepted target behavior or legal source route. B002 did not manually edit any validator-owned output.
- Lease proof: UID00019X was leased once for split-first child registration plus target conversion; UID0000CJ was leased once for editing and once for final waited validation; UID0000EO, UID00003T, UID0003LX, and UID0001A3 were each leased only around their edit/validator. Every lease/unlease command returned `Success`. New children were created and registered serially under the parent split lease, not separately leased before existence.
- Final lease audit after command `000000010056` showed no B002 lease; only unrelated B001/B004 leases remained.
- Generated proof: B002's authorized waited refresh completed as command `000000010056` at `2026-07-13T14:20:49-04:00`. A final read-only audit after unrelated validator activity found current generated metadata command `000000010069` at `2026-07-13T14:26:03-04:00` with unchanged target content. Exact anchored counts remain class `1`, constructor `1`, destructor `1`, timer `1`, paint `1`, UID00019X marker `0`, and scalar/thunk/vtable source `0`. Emission order remains UID0000CJ line 7, UID0004MA line 27, UID0004MB line 83, UID0004MC line 89, and UID0004MD line 110.
- No `execute_report`, dry-run/probe/lifecycle, move, or archive command was run.

## Changed Files

- Mutated existing ordinary pages:
  - `by-memory/0x004ff400-0x004ff7cd.ScrolledTextControlPaneForMadeByCore.md`
  - `by-class/ScrolledTextControlPaneForMadeBy.md`
  - `by-class/TextEditPane.md`
  - `by-class/DialogPane.md`
  - `by-memory/0x00502c10-0x00502cbd.ScrolledTextControlPaneForMadeByScalarDeletingDestructor.md`
  - `by-memory/0x00500410-0x00500632.NewHistoryDialogPaneCore.md`
- Created and validator-registered ordinary pages:
  - UID0004MA `by-memory/0x004ff400-0x004ff644.ScrolledTextControlPaneForMadeByConstructor.md`
  - UID0004MB `by-memory/0x004ff650-0x004ff6bc.ScrolledTextControlPaneForMadeByDestructor.md`
  - UID0004MC `by-memory/0x004ff6c0-0x004ff725.ScrolledTextControlPaneForMadeByOnTimerEvent.md`
  - UID0004MD `by-memory/0x004ff730-0x004ff7cd.ScrolledTextControlPaneForMadeByOnPaintFrame.md`
- Updated this same report: `tools/leaser/Agents/Agent-B002/research/00019X-ScrolledTextControlPaneForMadeByCore-source-quality.md`.
- Authorized validators changed only their owned registry/reference/stats/generated/coverage outputs. B002 made no manual generated, coverage, tracker, audit, supervisor, validator-state, lifecycle, IDA, or source-tree file edit.

## Implementation Tracking Checklist

### Report-only research audit

- [x] Read current goal, project skill, literal report template, score-blocker standard, by-structure rules, and by-memory guidance.
- [x] Verified fresh `idb_list`, `server_health`, and bounded target lookup on `supervisor_nexustk_20260713`.
- [x] Exhausted all four target methods, boundaries, padding, hashes, CFG/branches, callers/xrefs, vtables, fields/types, resources/globals, helpers, generic sibling, NewHistory, compiler wrappers, ownership, and source placement.
- [x] Searched every named central, legacy, archived, special, and active root with the exact term list and classified every relevant report.
- [x] Resolved all score and C++ blockers; supplied exact managed destination blocks and deterministic child/source order.
- [x] At Gate-1 submission, confirmed every C01-C30 Action cell used only `incorporate`, `already-present`, `historicalize`, `reject-stale`, `reject-invalid`, or `not-applicable`, and every report-only Verification state was exactly `proposed`.
- [x] Confirmed no duplicate report for the 34 preceding already-researched tracker rows.
- [x] Confirmed no prohibited edit, lease, validator, lifecycle, execute, move, archive, generated refresh, or IDA mutation occurred.

### Implementation callback - completed

- [x] Re-read this exact accepted report and callback scope; did not start another target.
- [x] Rechecked active reports/leases/support pages for collision and same-or-greater detail.
- [x] Leased UID00019X, created the split serially, applied C01-C30 target detail and Destination 1, validated under `000000010049`, and released.
- [x] Leased UID0000CJ, applied Destination 2 and full layout/source detail, validated under `000000010050`, and released.
- [x] Created/registered constructor child UID0004MA, installed Destination 3 at position 110, and validated under `000000010045`.
- [x] Created/registered ordinary-destructor child UID0004MB, installed Destination 4 at position 120, and validated under `000000010046`.
- [x] Created/registered timer child UID0004MC, installed Destination 5 at position 130, and validated under `000000010047`.
- [x] Created/registered paint child UID0004MD, installed Destination 6 at position 140, and validated under `000000010048`.
- [x] Leased UID0000EO, applied Destination 7 without losing stronger detail, validated under `000000010051`, and released.
- [x] Leased UID00003T, moved `CloseDialog` public exactly once per Destination 8, validated under `000000010052`, and released.
- [x] Leased UID0003LX, applied Destination 9 compiler-only metadata/prose, validated under `000000010053`, and released.
- [x] Leased UID0001A3, applied only Destination 10's `&rect` call sync, validated under `000000010054`, and released.
- [x] Verified UID0003LY and generic UID00019W/UID0000CI read-only with no concrete drift; no edit made.
- [x] Verified UID0000NI/source route and singleton/layout dependencies were already same-or-greater; no by-file reconstruction metadata or speculative support edit added.
- [x] Ran final authorized `--wait-generated` validation `000000010056` after all ordinary pages passed.
- [x] Verified exact generated count/order: one made-by class, constructor, destructor, timer, and paint; no duplicate destructor, scalar/thunk/vtable ABI, aggregate marker, or aggregate body.
- [x] Updated this same report with legal callback states, separate destination proof, validator commands/timestamps/results/warnings/side effects, changed files, lease proof, generated proof, and archive-neutral current state.
- [x] Confirmed every B002 lease is released and stopped without `execute_report`, dry-run/probe/lifecycle, move, or archive.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000010130","destination_path":"executed-b-agent-research/B002/00019X-ScrolledTextControlPaneForMadeByCore-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/00019X-ScrolledTextControlPaneForMadeByCore-source-quality.md","timestamp":"2026-07-13T14:47:37-04:00","uid":"00019X"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
