** TARGET-REPORT-UID:0004DO **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# 0004DO CopyRecordVector40Range Ownership / Split Research


## Finalized Report / Current Recommendation

- Completed callback state: B002 applied C01-C30 and Destinations 1-16 within the accepted scope. UID0004DO now uses the selected UID-preserving renamed path, UID00017U and UID0000BS have the accepted compiler-support/index dispositions, all accepted ordinary support changes are validated, Destinations 14-15 were verified same-or-greater without edits, every lease was released, and final generated readback is recorded below. Report execution, move, archive, manual coverage application, and any current/future lifecycle or report-path state are external validator/supervisor-owned state and are not asserted.
- Implemented recommendation: [UID:0004DO] is the exact out-of-line uninitialized-range-copy compiler support emitted by the compiler-era MSVC `std::vector<FrameDrawRecord>` instantiation used only by `NewHumanImageLib::RenderComposition`. The documentation artifact was renamed exactly from `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md` to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; `FrameDrawRecordUninitializedCopyCompilerSupport` is a descriptive compiler-disposition name, not a claimed original symbol.
- Final disposition: `RECONSTRUCTABLE:TRUE`, `COMPLETION:92`, `CONFIDENCE:94`, both `CANONICAL_OWNER` and `EMITTER_UIDS` routed directly to file owner [UID:0000LR] `NewHumanImageLib`, deliberately blank `EMITTER_POSITION_OPTIONAL` because the range has no independent authored source order, `Nested:0`, and Destination 1's exact covered-by marker. No independent helper body is emitted.
- Implemented support action: UID00017U uses the same typed source operation and NewHuman file route; synthetic pseudo-class UID0000BS is a non-emitting documentation index; direct UID0004DO/UID00017U ownership claims were removed from `VectorHelpers`; UID000417/UID000438 use the accepted `FrameDrawRecord`; `ImageFrameTable.h` is compile-visible on the NewHuman class route; and the remaining NewHuman, split-index, and 40-byte record support prose was synchronized. UID0004DN and UID0004DQ were re-read and left unchanged because no concrete drift exists.
- Confidence: very strong for exact bytes, half-open range, ABI, return, CFG, caller count, both argument triplets, fixed stride, forward-copy behavior, trivial/shallow element copy, and compiler-template source disposition. Confidence is capped below final because original local-variable spelling and the private MSVC STL helper spelling are stripped.
- Source operation that regenerates the helper: the `RenderComposition` local `std::vector<FrameDrawRecord>` append represented exactly in Destinations 1 and 2. UID00017U is the full-capacity grow/insert lowering; UID0004DO is its private uninitialized prefix/suffix range-copy lowering. Neither is a separately authored NexusTK function.

## Supporting Research

- Assignment source: `tools/leaser/Agents/Agent-B002/goal.md`, report-only UID0004DO source-quality research.
- Evidence-time target: `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md`, then `86/90`, owner/emitter UID0000BS, reconstructable, blank position, `Nested:0`, and comment-only generic RecordVector/VectorHelpers marker. The applied renamed target state is recorded under Current Target State.
- Direct current support: `by-memory/0x004e57c0-0x004e5989.RecordVectorInsert.md`, `by-class/RecordVector_4E57C0.md`, `by-memory/0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers.md`, `by-file/VectorHelpers.md`, `by-file/NewHumanImageLib.md`, `by-class/NewHumanImageLib.md`, and `by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md`.
- Record/type support: `by-class/EPFTileContext.md`, `by-item/EPFArchiveMetadataTable.md`, `by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md`, `by-item/LoadFrameDrawRecord_004D1600.md`, `by-file/ImageFrameTable.md`, `by-type/by-struct/NewHumanImageLibLayout.md`, `by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md`, and `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`.
- Sibling/template controls: `by-memory/0x004e6e50-0x004e6fe2.NewHumanImageLibGrowCommandSideVector.md`, `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md`, `by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md`, and the 80-byte copy helper documented inside `by-memory/0x00453df0-0x004563b5.MiniMapRendererAndControls.md`.
- Evidence-time generated output was read only as a routing check: `auto-generated/NexusTK/util/VectorHelpers.cpp`, `auto-generated/NexusTK/render/NewHumanImageLib.cpp`, `auto-generated/NexusTK/render/EPFTileContext.cpp`, and `auto-generated/NexusTK/render/ImageFrameTable.cpp`. During callback, generated output changed only through authorized waited validator refreshes `000000010459` and final `000000010472` and was read back without manual edits.
- Manual coverage was read only from `by-memory/-coverage-report.md`, `by-class/-coverage-report.md`, `by-file/-coverage-report.md`, and `by-item/-coverage-report.md`. Exact supervisor-owned text is supplied below.
- Evidence-time / pre-callback lifecycle note: this artifact began as report-only research; at that stage no by-* page, lease, implementation validator, generated file, coverage file, tracker, audit, lifecycle state, or IDA database was written. The later accepted callback changed only the ordinary by-* destinations and this report listed under Changed Files, used validators to refresh generated output, and left manual coverage/tracker/audit/lifecycle/IDA state untouched. Current/future validation, execution, move, archive, report path, and report count remain external and are not asserted.

## Target

- Target UID: `0004DO`.
- Evidence-time target path: `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md`.
- Applied path operation/title: UID0004DO was renamed to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md` with title `0x004e6ff0-0x004e7026 FrameDrawRecord Uninitialized Copy Compiler Support`. The old target path is absent; the old filename remains historical provenance only.
- Evidence-time source queue/report row: generated tracker `by-memory` / `Not-Covered Files - Reconstructable`, `86/90`, report count zero at assignment time. No current/future tracker or report-count state is asserted.
- Current supervisor classification: first vetted exact-range report; B004 UID0004DN, B008 UID00017U, and B011 UID00022R are executed contextual inputs, not exact-target substitutes.
- Evidence-time scores and parent state: `86/90`, `CANONICAL_OWNER:0000BS`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000BS`, blank position, `Nested:0`. Applied state is `92/94`, owner/emitter UID0000LR, reconstructable true, blank position, and `Nested:0`.
- Exact executable range: `[0x004e6ff0,0x004e7026)`, 54 bytes.
- Exact body SHA256: `6B1A32D9E94AC33E3CE75377AC260A953B5642BF970A3D0B44D18B7175AB2293`.

## Current Target State

- Evidence-time / pre-callback metadata: `COMPLETION:86`, `CONFIDENCE:90`, owner/emitter UID0000BS, reconstructable true, blank position, `Nested:0`.
- Evidence-time / pre-callback C++/emitter state: a comment-only marker said `CopyRecordVector40Range` was covered by `RecordVector_4E57C0::Insert` or a `VectorHelpers` compatibility template. Generated `VectorHelpers.cpp` consequently carried UID0004DO under the synthetic RecordVector route.
- Preserved useful facts: exact range, two UID00017U callsites, no callees, 40-byte stride, two 16-byte plus one 8-byte copy shape, and no standalone-body caution.
- Superseded assumptions: `CopyRecordVector40Range` was treated as if it could be a human helper name; UID0000BS was treated as a real source class and direct owner; `VectorHelpers.cpp` was treated as the direct source route; the 40-byte element type was unnamed; the two callsite argument triplets, return value, overlap contract, unused `ECX`, and exact `RenderComposition` source operation were absent.
- Applied target state: UID0004DO is `92/94`, owner/emitter UID0000LR, reconstructable true, blank position, `Nested:0`, and uses Destination 1's exact marker at the renamed path. The exact typed source operation is documented, UID0000BS is non-emitting, and generated `NewHumanImageLib.cpp` contains the target once while generated `VectorHelpers.cpp` contains neither UID0004DO nor a UID0000BS Empty Emitter Marker.
- Related target/support docs checked: all files listed under Supporting Research and Destinations 1-16.
- Artifact/lifecycle status: B002 research, callback edits, scoped validators, waited generated verification, and lease releases are complete. External validation, execution, move, archive, report path, count, and manual coverage state are not asserted.

## Executive Recommendation

- Direct source owner: [UID:0000LR] `NewHumanImageLib`, because all product semantics reduce to the one `RenderComposition` local vector and no cross-module caller reaches either UID00017U or UID0004DO.
- Target disposition: reconstructable compiler support with a precise covered-by marker, not a handwritten helper body and not a no-owner raw range.
- Applied target path operation: UID0004DO was renamed exactly from `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md` to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; the old path is absent, and normal scoped file validation updated the UID mapping and known references. No alternate stable-path outcome remains.
- Source type: `FrameDrawRecord`, the accepted 40-byte ImageFrameTable projection record used by `LoadFrameDrawRecord`. It is ABI-compatible with the accepted 40-byte `EPFTileContext` storage and is initialized through the same raw initializer, but current field semantics at `+0x20/+0x24` do not justify collapsing the two source-facing declarations. The undeclared NewHuman-only name `NewHumanDrawRecord` is superseded: UID000417/UID000438 already forward the pointer to `LoadFrameDrawRecord`, and UID000438's raw `+0x10` bounds access maps directly to `FrameDrawRecord::sourceRect`.
- Source operation: `RenderComposition` appends one local frame draw record to a local vector. The no-capacity path copies the 40 bytes inline; the full-capacity path calls UID00017U, which allocates new storage, copies the inserted record, and calls UID0004DO for prefix and suffix ranges.
- UID00017U disposition: its documentation title/working name is described as `FrameDrawRecordVectorGrowInsertCompilerSupport`; stable path `by-memory/0x004e57c0-0x004e5989.RecordVectorInsert.md` is retained; prose records exact half-open body `[0x004e57c0,0x004e598a)` while preserving filename endpoint `0x004e5989` as historical inclusive-last-byte convention; score is `92/94`; owner/emitter is UID0000LR; and Destination 2's marker is installed. No UID00017U path rename was performed.
- UID0000BS disposition: `92/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal C++; retain only as a historical synthetic compiler-support index linking the two exact source-bearing ranges.
- Remaining condition: none blocks this recommendation. Original private STL helper spelling remains intentionally unclaimed and caps confidence only.

## Supervisor Active Recheck

- Supervisor instruction: independently research UID0004DO under the current literal template, resolve its compiler/source disposition, caller semantics, 40-byte type, ownership/emission, score blockers, formal destinations, and manual coverage text.
- Split repair: no new executable child is needed. UID0004DO is already one exact modeled function, and UID00022R is already a seven-child non-emitting split/index.
- Source-bearing scope: UID0004DO and UID00017U are the exact compiler-support ranges. `RenderComposition` is the sole caller-owned source operation. UID0000BS is not source-bearing after reclassification. UID0004DN and UID0004DQ remain separate accepted NewHuman compiler-support siblings.

## Inference Research Guidance Check

- `by-structure.md` and the project workflow require source ownership to follow human source semantics rather than address adjacency, generated labels, or current generated output. That rule moves this one-caller template lowering from synthetic `RecordVector_4E57C0`/generic `VectorHelpers.cpp` to the NewHuman file that contains `RenderComposition`.
- Evidence-time hypotheses treated as uncertain: then-current target name, then-current UID0000BS class identity, direct VectorHelpers ownership, standalone-helper eligibility, exact 40-byte type name, blank-position meaning, and B008's older unresolved-type conclusion. The accepted callback resolutions are now applied.
- Direct IDA facts are kept separate from documentation facts and inference. Direct facts prove bytes, boundaries, calls, arguments, return, stride, and caller source shape. Current docs supply accepted names/layouts. The MSVC vector-template/private uninitialized-copy classification is the strongest inference joining those facts.
- No relevant Wave2/Wave3 artifact was found in the exact active/executed/archive/special report search. Any generic historical generated-name language was treated only as a lead and independently revalidated.

## Heuristic / Inference Reanalysis And Validation

| Issue | Reanalysis and evidence | Resolution / classification |
| --- | --- | --- |
| Generated target name | No symbol, string, data xref, or separate product caller supports `CopyRecordVector40Range`. The body is reached only from the vector grow/insert routine. Current `by-structure.md` and validator rename handling support a real UID-preserving path rename. | Rename the target exactly to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; reject the old spelling as an original-source claim and reject retaining the old path. |
| Function vs compiler support | Both calls occur in the allocation/rebalance branch of UID00017U. Each call is surrounded by vector begin/end/capacity arithmetic. `ECX` is loaded with the vector header before both calls although the body never reads it. | Compiler-era MSVC vector private member/template lowering, not a handwritten free function. |
| ABI | Body has three stack parameters and `ret 0x0c`; IDA infers `__stdcall` only because `ECX` is unused. Callers explicitly load `ECX=EBX`, the vector object. | Lowered ABI is an unused-receiver `__thiscall` template/member helper with three explicit pointer arguments. Do not publish a source-level standalone signature. |
| Return | `EAX` begins as destination, advances by `0x28`, and returns destination end; empty range returns the original destination. Both callers ignore it. | Conventional output iterator result from uninitialized range copy; preserve in evidence, not independent C++. |
| Copy semantics | Forward loop compares source pointer only to exact end, copies at current addresses, then increments source/destination. | `[first,last)` must be a multiple of 40. Safe for disjoint storage and forward overlap where destination precedes source; not general `memmove`. Actual calls are old-to-new disjoint storage. |
| Lane shape | `movups` copies `+0x00..+0x0f` and `+0x10..+0x1f`; `movq` copies `+0x20..+0x27`. | Exact shallow 40-byte object copy. No field constructor/destructor/copy hook. |
| Element type | `RenderComposition` calls the accepted initializer at `0x00457a60` on the 40-byte local; 18 calls to `0x004e4210` populate it through `LoadFrameDrawRecord`; accepted `FrameDrawRecord` and `EPFTileContext` layouts are both exactly `0x28`. UID000417/UID000438 use an undeclared `NewHumanDrawRecord` name but immediately pass it to the formal `FrameDrawRecord *` loader; UID000438's raw `+0x10` access is the accepted `sourceRect`. | Use source-facing `FrameDrawRecord` for this vector. Replace the redundant NewHuman alias in both exact children, use `sourceRect`, make `ImageFrameTable.h` compile-visible, and record EPFTileContext ABI compatibility without collapsing declarations. |
| Lifetime | Target has no calls, unwind state, allocation, free, cleanup, or element hook. UID00017U allocates/frees only backing storage. `RenderComposition` does not release per-element fields. | Trivially copied shallow record containing borrowed frame pointers and bounds/state. |
| Caller source expression | At capacity, `RenderComposition` calls UID00017U with position equal to end and address of the local record; otherwise it copies 40 bytes inline and advances end. | Highest-probability human source is one local vector append. The formal markers name that operation exactly. |
| `std::copy` | Could explain a forward loop but not the explicit vector receiver loaded into `ECX`, the two calls surrounding an inserted record, or the allocation branch. | Rejected as the authored operation. It may be an internal implementation concept only. |
| `memcpy` / `memmove` | No runtime call exists; body is element-stepped; actual prefix/suffix ranges are disjoint after new allocation. | Rejected as the authored source operation and as a general overlap contract. |
| Named local copy helper | Machine body could be hand-written, but there are no product callers, symbols, strings, or source-specific state, and exact vector-private callsite shape is stronger. | Rejected. Do not emit a standalone loop. |
| Direct `VectorHelpers` source | Evidence-time generated output contained staged target markers and a synthetic empty class. One feature-local caller and accepted NewHuman vector-helper precedents contradict direct generic ownership; final generated readback removes both stale projections. | Reject direct target/UID17U ownership. Keep VectorHelpers only as historical project compatibility context for other genuinely shared helpers. |
| Position | No authored helper declaration/body exists to order in NewHuman source; output is compiler-generated from an in-method local vector operation. | Blank `EMITTER_POSITION_OPTIONAL` is the exact resolved value, not an unresolved omission. |
| Nested | Exact function is not a parent/container and has no subchildren. | Retain `Nested:0`. |

## Evidence Standards Used

- Evidence ladder: current live IDA MCP modeled function and raw bytes first; exact callsite disassembly, xrefs, caller/callee/CFG, and adjacent boundaries second; accepted by-* type/source docs third; executed B reports and generated output as historical/contextual leads only.
- Binary evidence includes `idb_list`, `server_health`, bounded `lookup_funcs`, `analyze_function`, `disasm`, `xrefs_to`, `get_bytes`, and `find_bytes` calls on database `supervisor_nexustk_20260713`.
- Storage/section context: target and callers are `.text`; pre/post spans are `0xcc` alignment; no data/vtable/global ownership applies.
- Tool limitations: stripped symbols prevent original local/private-template spellings. This does not prevent source-disposition, type-role, ABI, range, or behavior conclusions.
- Negative evidence was used affirmatively: no target callees, strings, constants, EH, data xrefs, independent callers, object-field access, or product-domain state.

## Evidence Checked

- MCP transport: streamable endpoint `http://127.0.0.1:13337/mcp`; each client call negotiated a fresh HTTP MCP transport session. All IDB tool arguments used `database:"supervisor_nexustk_20260713"`; no stale database ID was reused.
- Evidence-time health observation on 2026-07-13 around `16:39 -04:00`: `idb_list` showed the loaded NexusTK database session `supervisor_nexustk_20260713`, worker/PID `2236`, active and not analyzing. `server_health` returned `status:ok`, NexusTK.exe image base `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready with 2067 entries. This is an evidence-collection-time observation, not an assertion of future availability.
- Final bounded MCP recheck at `2026-07-13T16:56:34-04:00`: fresh `idb_list` again returned the one active/adopted database `supervisor_nexustk_20260713`, worker/PID `2236`, not analyzing; `server_health` remained `ok` with analysis/Hex-Rays/strings ready; `lookup_funcs 0x004e6ff0` again returned `sub_4E6FF0`, size `0x36`. No MCP outage or fallback substitution occurred.
- `lookup_funcs`: target `0x004e6ff0` -> `sub_4E6FF0`, size `0x36`; `0x004e7026` not a function; successor `0x004e7030` -> `sub_4E7030`, size `0x64`; UID00017U `0x004e57c0` -> `sub_4E57C0`, size `0x1ca`; `0x004e5989` lies in it, `0x004e598a` is not a function, and successor `0x004e5990` is distinct.
- `analyze_function 0x004e6ff0`: 54 bytes, 19 instructions, three blocks, cyclomatic complexity three, inferred three-stack-argument prototype, no callees/strings/constants, only caller function `sub_4E57C0`, two xrefs at `0x004e58fa` and `0x004e590e`.
- `get_bytes`: exact target bytes, fourteen-byte `0xcc` prepad from `0x004e6fe2`, ten-byte `0xcc` postpad from `0x004e7026`, and successor prologue at `0x004e7030`.
- `disasm/analyze_function 0x004e57c0`: fixed 40-byte record insert/grow routine with 1.5x capacity policy, max-size guard, aligned large allocation, two target calls in the middle-insert branch, old-storage checked free, and vector triplet update.
- `disasm 0x004e4280`: exact local initializer call at `0x004e4357`; frame record producer sequence; non-null gate at local offset `+4`; inline 40-byte append at `0x004e4bd9-0x004e4bfb`; full-capacity call to UID00017U at `0x004e4c14` with vector receiver, position=end, and local record address.
- `analyze_function/xrefs_to 0x004e4210`: 108-byte producer helper, exactly 18 callsites in `RenderComposition`, calls `LoadFrameDrawRecord` at `0x004e4261`, then offsets embedded bounds at `+0x10` through `RectBounds::Offset` at `0x004e426f`.
- `analyze_function 0x00457a60`: accepted 40-byte context initializer clears pointer/state fields and bounds; broad call fan-in confirms it is shared context/record initialization rather than ownership by this target.
- `find_bytes`: target prologue/control shape occurs at `0x004e6ff0` and one 80-byte analog at `0x004554e0`; the exact 40-byte two-`movups` plus `movq` lane signature occurs only at `0x004e7000`.
- `analyze_function 0x004554e0`: 66-byte forward range-copy helper for 80-byte records, no callees, output-iterator return, five callers including the NewHuman 80-byte vector grow helper at `0x004e5600`. It is a compiler-template control, not evidence of a shared 40-byte handwritten helper.
- Exact old-report search terms: `UID0004DO`, `UID:0004DO`, `TARGET-REPORT-UID:0004DO`, `0004DO`, `0x004e6ff0`, `0x004e7026`, `CopyRecordVector40Range`, `sub_4E6FF0`, `UID00017U`, `0x004e57c0`, `0x004e5989`, `0x004e598a`, `RecordVectorInsert`, `RecordVector_4E57C0`, `UID0004DN`, `UID00022R`, `ImageLibVectorSupportHelpers`, `VectorHelpers`, `FrameDrawRecord`, `EPFTileContext`, `RenderComposition`, `0x004e4280`, and `0x004e4c14`.
- Central executed root searched: `source-3/project-documentation/executed-b-agent-research/**/*.md`. Exact target/range matches opened:
  - `executed-b-agent-research/B004/0004DN-NewHumanImageLibGrowCommandSideVector-source-quality.md`: sibling UID0004DN is executed NewHuman file-routed compiler support; it supplies the direct-NewHuman/VectorHelpers-fallback precedent and exact predecessor/postpad boundary, but no UID0004DO type/callsite conclusion.
  - `executed-b-agent-research/B008/00017U-RecordVectorInsert-source-quality.md`: historical accepted report correctly proves the sole RenderComposition caller and compiler/vector slow path, but leaves the 40-byte type unresolved and accepts the now-stale UID0000BS -> VectorHelpers direct route.
  - `executed-b-agent-research/B011/00022R-ImageLibVectorSupportHelpers-source-quality.md`: executed split report creates UID0004DO, proves the two calls/no-callee/40-byte shape, and recommends a marker, but explicitly retains the RecordVector/VectorHelpers route pending child-specific research. This report supersedes only that child-specific route/type/name conclusion, not B011's split.
- Legacy executed roots searched: `source-3/project-documentation/tools/leaser/Agents/Agent-B*/research/executed/**/*.md`; no such current legacy directories or matching report artifacts exist.
- Archived roots searched: `source-3/project-documentation/archived/b-agent-reports-20260623/**/*.md` and `source-3/project-documentation/archived/duplicate-loose-b-agent-reports/**/*.md`; no exact UID/range/name match.
- Active report roots searched: `source-3/project-documentation/tools/leaser/Agents/Agent-B*/research/*.md`; no exact UID/range/name match and no competing active UID0004DO report.
- Special report root searched: `source-3/project-documentation/tools/leaser/Agents/SpecialReports/**/*.md`; no exact UID/range/name match.
- Current by-* docs and all five affected manual coverage files listed under Supporting Research were opened, including `by-type/by-struct/-coverage-report.md`. Generated source was read only. No fallback-only evidence was accepted.
- Path-operation recheck: `by-structure.md` lines 239-243 explicitly direct a scoped validator after a file rename; `tools/validator_README.txt` lines 696-700 state that a real rename updates the UID mapping once the old mapped path no longer exists; lines 798-804 state that default file-mode scanning updates known UID references after a target rename. This supports one exact implementation route: rename UID0004DO to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`, scan that renamed file with normal scoped file mode, and do not retain a stable-path fallback.
- Evidence-time / pre-callback intentionally skipped: IDA renames/types/comments writes, implementation validators, leases, generated refresh, coverage/tracker edits, and all report lifecycle commands, because report-only restrictions prohibited them. The later accepted callback used only the leases/validators recorded below and still made no IDA, manual coverage/tracker, or lifecycle write.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | UID0004DO is exactly `[0x004e6ff0,0x004e7026)`, 54 bytes, SHA256 `6B1A...2293`. | Certain | MCP lookup/analyze/get_bytes and local SHA256. | Destination 1 target metadata/evidence. | incorporate | applied |
| C02 | Fourteen `0xcc` bytes at `[0x004e6fe2,0x004e6ff0)` are predecessor alignment, not target code. | Certain | Raw bytes; UID0004DN exact end. | Destination 1 range/padding; Destination 8 index. | incorporate | applied |
| C03 | Ten `0xcc` bytes at `[0x004e7026,0x004e7030)` separate target from `sub_4E7030`. | Certain | Raw bytes and lookup. | Destination 1 range/padding; Destination 8 index. | incorporate | applied |
| C04 | Target CFG is empty check, one forward loop, return: three blocks/complexity three. | Certain | Analyze/disassembly. | Destination 1 behavior. | incorporate | applied |
| C05 | Observed lowered ABI is unused-receiver member/template support with three explicit stack pointers and callee cleanup. | High | Both callers set `ECX=EBX`; body ignores ECX; `ret 0x0c`. | Destinations 1, 2, 4-8. | incorporate | applied |
| C06 | Return is destination end; empty input returns original destination; both callers ignore EAX. | Certain | Instruction flow and caller disassembly. | Destination 1 behavior. | incorporate | applied |
| C07 | Exactly two code xrefs, both inside UID00017U; no callees/strings/constants/data owner. | Certain | Analyze/xrefs. | Destinations 1 and 8. | incorporate | applied |
| C08 | First call copies `[oldBegin,position)` to `newBegin`. | Certain | `0x004e58f5-0x004e58fa` pushes and vector-state registers. | Destinations 1 and 2. | incorporate | applied |
| C09 | Second call copies `[position,oldEnd)` to `newBegin + insertionOffset + 40`. | Certain | `0x004e58ff-0x004e590e` pushes/arithmetic. | Destinations 1 and 2. | incorporate | applied |
| C10 | Copy is forward-only and not general overlap-safe; actual old/new storage ranges are disjoint. | Certain/high | Loop direction plus UID00017U allocation path. | Destination 1 behavior/negative evidence. | incorporate | applied |
| C11 | Each element copy is two unaligned 16-byte lanes plus one 8-byte tail, exact stride 40. | Certain | Target disassembly. | Destination 1 behavior/type. | incorporate | applied |
| C12 | Target performs no allocation, free, EH, validation, cleanup, constructor, destructor, or element hook. | Certain | No callees; instruction inventory. | Destination 1 compiler/lifetime disposition. | incorporate | applied |
| C13 | Element copies are shallow/trivial; pointer fields are copied as values. | High | Lane copy, no hooks, caller lifecycle. | Destinations 1, 9-11. | incorporate | applied |
| C14 | UID00017U modeled function is `[0x004e57c0,0x004e598a)`; current filename endpoint `5989` is the inclusive final byte. | Certain | Size `0x1ca`, boundary bytes, successor `0x4e5990`. | Destination 2 range prose/manual coverage. | historicalize | applied |
| C15 | UID00017U is the full-capacity grow/insert lowering with 1.5x growth, aligned allocation, prefix/suffix relocation, old-storage free, and triplet update. | Certain | Full analyze/disassembly. | Destination 2. | incorporate | applied |
| C16 | UID00017U has one product caller, `RenderComposition` at `0x004e4c14`, and is called only when end equals capacity. | Certain | Xrefs and caller disassembly. | Destinations 2, 5-7. | incorporate | applied |
| C17 | Caller source operation is one local `FrameDrawRecord` vector append; no-capacity path is inline, full-capacity path uses UID00017U. | High | Exact branch/call arguments and producer/type docs. | Formal Destinations 1-2; prose Destinations 5-7. | incorporate | applied |
| C18 | `0x004e4210` populates the record through `LoadFrameDrawRecord` and offsets bounds; it has 18 RenderComposition callsites. | Certain | Analyze/xrefs. | Destinations 5-7, 9-10, 12-13. | incorporate | applied |
| C19 | The local record is explicitly initialized by raw `0x00457a60` before population. | Certain | Caller disassembly. | Destinations 5-7, 9-11. | incorporate | applied |
| C20 | `FrameDrawRecord` is the strongest source-facing role; its 0x28 layout is ABI-compatible with EPFTileContext but has distinct frame-payload field semantics. The undeclared `NewHumanDrawRecord` alias and UID000438 `bounds` spelling are superseded by `FrameDrawRecord` and `sourceRect`. | High | Accepted declarations and raw producer/consumer use. | Destinations 6, 9-13. | reject-stale | applied |
| C21 | Collapsing FrameDrawRecord into EPFTileContext is not justified by current semantic evidence. | High | `+0x20/+0x24` payload-span/end versus encoded-mask meanings. | Destinations 9-11/open questions. | reject-invalid | applied |
| C22 | The 80-byte copy helper at `0x4554e0` is a compiler-template control; UID0004DO's exact 40-byte lane signature is unique. | High | find_bytes/analyze/xrefs. | Destination 1 heuristic evidence; siblings verify-only. | incorporate | applied |
| C23 | `CopyRecordVector40Range` and `RecordVectorInsert` are descriptive historical labels, not original source functions. UID0004DO was renamed exactly to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; UID00017U retains its stable path with only title/working-name prose changed. | High | No symbols, compiler-template source proof, `by-structure.md` rename instruction, and validator UID/reference rename support. | Destination 1 exact path rename; Destinations 2-4 history/name synchronization. | historicalize | applied |
| C24 | Direct UID0000BS/VectorHelpers ownership is stale; direct source owner/emitter is UID0000LR. | High | One NewHuman source operation and sibling file-route precedent. | Destinations 1, 2, 4-8. | reject-stale | applied |
| C25 | UID0000BS is a generated pseudo-class with no independent declaration/body and is non-emitting. | High | Current blank class block, child behavior, source placement. | Destination 3. | reject-invalid | applied |
| C26 | UID0004DO needs no independent helper body; exact compiler-lowering proof and exact source operation are complete. | High | ABI/calls/type/analog/negative checks. | Formal Destination 1 and no-code proof. | incorporate | applied |
| C27 | Blank emitter position is intentional because compiler support has no authored source order. | High | Source disposition. | Destinations 1-3 metadata. | incorporate | applied |
| C28 | Target score is `92/94`; UID00017U is `92/94`; UID0000BS is `92/93`. | High | Closed blocker matrix. | Destinations 1-3/score section. | incorporate | applied |
| C29 | UID00022R remains a non-emitting seven-child split/index; UID0004DN/UID0004DQ remain accepted read-only siblings. | Certain | Current docs and executed B004/B011 reports. | Destinations 8, 14, 15. | already-present | already-present |
| C30 | Manual coverage rows are absent/stale and require the exact supervisor-owned text supplied below, including the final renamed UID0004DO path; generated files remain validator-owned and manual coverage application remains supervisor-only. | Certain | Read-only manual coverage comparison and exact selected rename route. | Exact Manual Supervisor-Owned Coverage section; Destination 16 generated verification; supervisor-only follow-up. | incorporate | excluded-with-reason |

### Callback Claim Proof

| Claims | Applied destination proof |
| --- | --- |
| C01-C13, C26-C28 | UID0004DO was UID-preserving-renamed, validated by `000000010429`, and now carries exact `92/94`, UID0000LR owner/emitter, blank position, `Nested:0`, complete range/bytes/hash/CFG/ABI/caller/copy/lifetime evidence, and Destination 1's exact no-independent-body marker. |
| C14-C17 | UID00017U was validated by `000000010430` and final historical-label audit `000000010461` at `92/94` with exact `[0x004e57c0,0x004e598a)` body history, stable inclusive-last-byte filename, full grow/allocation/EH/free behavior, one `RenderComposition` caller, and Destination 2's typed append marker. |
| C18-C21 | NewHuman file/class/layout/aggregate, shared FrameDrawRecord, loader/index, EPFTileContext, UID000417, and UID000438 destinations were synchronized by commands `000000010433`-`000000010435`, `000000010442`, `000000010448`/`000000010449`, `000000010451`, `000000010455`, `000000010457`-`000000010459`, and final alias/durability audit commands `000000010464`, `000000010465`, `000000010468`, `000000010469`, and `000000010472`; exact shared type, initializer, shallow-copy, 18-caller, and `sourceRect` facts are present without collapsing EPFTileContext. |
| C22-C25 | UID0000BS is `92/93`, NONE/FALSE/blank after `000000010431`; VectorHelpers direct target routing was removed by `000000010432`; UID00022R route text was synchronized by `000000010447`; historical names and rejected direct ownership are preserved. |
| C29 | Read-only recheck confirmed UID0004DN remains direct UID0000LR `88/91` marker support with exact `[0x004e6fe2,0x004e6ff0)` postpad, and UID0004DQ remains distinct direct UID0000LR `86/90` 80-byte command-record support. No edit was warranted. |
| C30 | B002 did not edit manual coverage. The exact supervisor-owned replacement/addition text remains below. Final validator `000000010472` refreshed generated state, and the readback proof below verifies Destination 16 without substituting generated coverage for manual coverage. |

## Positive Evidence Summary

- Direct facts: exact modeled function and boundaries; unique 40-byte lane signature; two and only two xrefs; both xrefs inside one vector grow routine; no target callees or state; exact source/destination argument triplets; unused receiver loaded before both calls; output-iterator return; no per-element lifetime calls.
- Caller facts: UID00017U has one product caller; caller position equals end; caller has an inline no-growth 40-byte append and calls UID00017U only at capacity; local record is initialized and populated through `LoadFrameDrawRecord` before the append.
- Type facts: accepted `FrameDrawRecord` is exactly 40 bytes at offsets matching the copied lanes; accepted EPFTileContext is also exactly 40 bytes and supplies ABI-compatible initializer evidence; record/vector cleanup is backing-storage-only.
- Corroboration: UID0004DN and other NewHuman vector lowerings already route one-feature compiler support through NewHumanImageLib, not direct VectorHelpers. The 80-byte range-copy control has the same iterator-loop architecture.
- Strongest inference chain: one typed local vector append -> compiler full-capacity insertion -> private uninitialized prefix/suffix copy. This explains every observed instruction, argument, return, receiver setup, caller count, and negative fact without inventing a product helper.

## IDA MCP Facts

- Target assembly inventory:

| Address | Instruction group | Meaning |
| --- | --- | --- |
| `0x004e6ff0-0x004e6ffc` | frame setup; load stack `first`, `last`, `destination`; compare | Three explicit pointer arguments and empty-range gate. |
| `0x004e6ffe` | jump to `0x004e7022` when equal | Empty range returns initial destination. |
| `0x004e7000-0x004e700d` | two `movups` load/store pairs | Copy bytes `0x00..0x1f`. |
| `0x004e700e-0x004e701a` | `movq` load/store with source/destination increments by `0x28` | Copy bytes `0x20..0x27`; advance one record. |
| `0x004e701e-0x004e7020` | compare new source to exact end; backward branch | Forward range loop. |
| `0x004e7022-0x004e7026` | restore frame; `ret 0x0c` | Return destination end in EAX and pop three arguments. |

- Exact bytes: `55 8B EC 8B 4D 08 8B 55 0C 8B 45 10 3B CA 74 22 0F 10 01 0F 11 00 0F 10 41 10 0F 11 40 10 F3 0F 7E 41 20 83 C1 28 66 0F D6 40 20 83 C0 28 3B CA 75 DE 5D C2 0C 00`.
- CFG: three blocks, one loop backedge, complexity three.
- Xrefs: `0x004e58fa` and `0x004e590e`, both in `sub_4E57C0`; no other code/data xref.
- Calls: none from target.
- Padding: `[0x004e6fe2,0x004e6ff0)` fourteen `0xcc`; `[0x004e7026,0x004e7030)` ten `0xcc`.
- Vtable/global/type facts: no vtable/global access in target. Type comes from caller storage and accepted producer/layout docs, not target field accesses.
- Negative IDA facts: no strings, constants beyond stride/control displacements, EH state, allocator, free, validation, null check, overlap branch, reverse loop, constructor, destructor, or copy hook.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `[0x004e6ff0,0x004e7026)` | UID0004DO target | 40-byte uninitialized range-copy compiler support | Yes, marker disposition | UID0000LR | `92/94` | Renamed/rerouted and validated. |
| `[0x004e57c0,0x004e598a)` | UID00017U stable path ends `5989` | Full-capacity FrameDrawRecord vector grow/insert compiler support | Yes, marker disposition | UID0000LR | `92/94` | Route/type/range clarification applied and validated. |
| `0x004e4280-0x004e4d79` | RenderComposition inside UID00017R | Sole caller-owned human source operation | Aggregate remains non-emitting | UID000092/UID0000LR semantic/file context | `85/89` aggregate unchanged | Exact vector source-operation prose applied. |
| `0x004e4210-0x004e427c` | UID000438 | Populates one FrameDrawRecord and offsets `sourceRect` | Existing source child | UID000092 | `87/89` unchanged | Accepted type/field spelling applied; waited validation passed. |
| `0x004e0d70-0x004e0dcf` | UID000417 | Loads one part FrameDrawRecord | Existing source child | UID000092 | `88/90` unchanged | Accepted alias replacement applied; body otherwise unchanged. |
| `0x00457a60` | EPFTileContext initializer in UID0000XY/UID00004I | Initializes the 40-byte ABI-compatible local | Existing source support | UID00004I/UID0000J4 | Existing score unchanged | Formal declaration preserved; prose sync applied. |
| `0x004d1600-0x004d165d` | UID0002P6 | Projects archive metadata into FrameDrawRecord | Yes | UID0000K1 | `88/92` unchanged | Vector consumer/trivial-copy evidence applied. |
| `0x004e6ae0-0x004e70e9` | UID00022R | Seven-child non-emitting split/index | No independent source | NONE | `88/91` unchanged | UID0004DO route/name updated only. |
| UID0000BS | `by-class/RecordVector_4E57C0.md` | Synthetic historical compiler-support index | No | NONE | `92/93` | Emitter/formal class identity cleared. |
| `0x004e6e50-0x004e6fe2` | UID0004DN | NewHuman command-side vector compiler support | Existing marker | UID0000LR | `88/91` | Read-only comparison; no target edit. |
| `0x004e70a0-0x004e70e9` | UID0004DQ | NewHuman 80-byte command-record append support | Existing marker | UID0000LR | Existing unchanged | Read-only comparison; no target edit. |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x004e58fa` -> UID0004DO | UID00017U pushes `newBegin`, `position`, `oldBegin`; loads vector receiver in `ECX`. | Copies old prefix to replacement storage. |
| `0x004e590e` -> UID0004DO | UID00017U pushes destination after inserted record, `oldEnd`, `position`; reloads vector receiver. | Copies old suffix after inserted record. |
| `0x004e4c14` -> UID00017U | `RenderComposition` passes local vector receiver, `position=end`, address of 40-byte local. | Full-capacity implementation of caller-owned append. |
| `0x004e4bd9-0x004e4bfb` | Inline `movups`/`movq` append in same caller when capacity remains. | Proves source operation is append, not direct authored call to a helper. |
| `0x004e4357` -> `0x00457a60` | Initialize 40-byte local before frame projection. | ABI/type/lifetime anchor. |
| 18 calls to `0x004e4210` | All inside `RenderComposition`, `0x004e465d` through `0x004e4b1a`. | Populate the same local record for composition cases. |
| `0x004e4261` -> `0x004d1600` | Producer calls accepted `LoadFrameDrawRecord`. | Strongest source-facing `FrameDrawRecord` role. |
| `0x004e426f` -> `0x004b78f0` | Producer offsets record bounds at `+0x10`. | Confirms embedded RectBounds. |
| `0x004554e0` control | Five callers, including `0x004e5600`; copies 80-byte records. | Independent compiler-template range-copy precedent. |

## Documentation Evidence And IDA Status

- Applied UID0004DO state preserves the exact target range, two calls, no callees, 40-byte copy, and no-standalone-body proof while adding `92/94`, UID0000LR owner/emitter, the accepted renamed path, and exact typed source marker. Validator `000000010429` applied the UID-preserving rename and updated known references.
- Applied UID00017U state is `92/94`, owner/emitter UID0000LR, and identifies the exact compiler/vector grow path and sole `RenderComposition` caller. Executed B008's unresolved type and direct UID0000BS/VectorHelpers route remain preserved as superseded history; validator `000000010430` applied current producer/type/callsite evidence.
- `EPFArchiveMetadataTable.md` still supplies the exact `FrameDrawRecord` 40-byte declaration: pointer at `+0x04`, width/state at `+0x0c`, `sourceRect` at `+0x10`, span at `+0x20`, end pointer at `+0x24`. Validators `000000010448` and corrective final `000000010449` added the exact `RenderComposition` shallow-vector consumer/type-boundary evidence without changing the declaration.
- Evidence-time / pre-callback UID000417 and UID000438 used undeclared `NewHumanDrawRecord *`, and UID000438 used `outRecord->bounds`; those were concrete compile-facing support defects, not evidence for a distinct type. Applied Destinations 6, 12, and 13 now provide the compile-visible include, exact shared `FrameDrawRecord *` signatures, and UID000438 `sourceRect`; validators `000000010434`, `000000010458`, `000000010459`, `000000010464`, `000000010465`, `000000010468`, `000000010469`, and final waited `000000010472` passed.
- `EPFTileContext.md` retains its independent accepted 40-byte layout and initializer semantics. Validator `000000010457` added the exact `RenderComposition` initializer reuse evidence while preserving the distinct `+0x20/+0x24` source roles that prevent an unproved type collapse.
- UID0004DN and other NewHuman vector supports continue to establish direct file-local NewHuman routing when a compiler helper has one NewHuman source family. UID0004DN and UID0004DQ were verified same-or-greater and left unchanged.
- Evidence-time / pre-callback generated `VectorHelpers.cpp` was stale as a source-placement projection: it contained UID0004DO markers and a UID0000BS Empty Emitter Marker because metadata routed them there. After final waited validator `000000010472`, generated `VectorHelpers.cpp` contains neither, while generated `NewHumanImageLib.cpp` contains UID00017U then UID0004DO exactly once. Generated output remains validator-projected verification, not proof of original source.
- Current manual coverage is stale: UID0004DO is absent; UID00017U, UID00017R, UID00022R, UID0000BS, UID0000LR, UID0000P3, UID00004I, UID0000UN, and UID0002P6 rows predate current page scores/dispositions. Exact replacement/addition text is below.

## Ranked Ownership Analysis

### 1. UID0000LR NewHumanImageLib / RenderComposition local vector

- Evidence for: one product caller chain; exact source operation inside `RenderComposition`; type producer belongs to composition rendering; direct NewHuman vector-support precedents; no cross-module target use.
- Evidence against: target body itself contains no class field access and is compiler/template code rather than a normal NewHuman method.
- Decision: accepted direct file owner/emitter for marker disposition. File ownership represents where the typed source operation is authored, not a claim that the helper is a class method.

### 2. Compiler-era MSVC STL vector instantiation

- Evidence for: unused receiver loaded in ECX, callee cleanup for three explicit iterator pointers, output-iterator return, two prefix/suffix calls from grow/insert, fixed trivial-element copy, and analogous range-copy instantiation.
- Evidence against: compiler runtime/STL is not a project by-file owner and private helper spelling is stripped.
- Decision: accepted source disposition, not a direct documentation owner. Human source remains the typed NewHuman vector append; the compiler regenerates UID00017U/UID0004DO.

### 3. UID0000BS RecordVector_4E57C0 / UID0000P3 VectorHelpers

- Evidence for, historical only: pre-callback metadata/generated routing and older reports grouped generic vector-like support there.
- Evidence against: pseudo-class has no declaration, object, vtable, storage, or authored body; target/UID17U have one NewHuman source operation; generated files are metadata projections; accepted sibling policy rejects generic direct ownership for one-feature NewHuman vector lowerings.
- Applied decision: direct ownership is rejected. UID0000BS is a `92/93` NONE/FALSE/blank historical index, and these two children are absent from VectorHelpers direct coverage.

### Proposed new file/grouping, if applicable

- Applied owner/name/path: no new file was created. Existing `NexusTK/render/NewHumanImageLib.cpp` / UID0000LR is used.
- Likely full contents: existing NewHuman class/method source, including `RenderComposition` and its local typed record vector.
- Candidate related items that belong: UID00017U and UID0004DO marker dispositions; existing UID0004DN/UID0004DQ remain same-file siblings.
- Candidate related items rejected: generic shared pointer/uint vector helpers, ImageFrameTable loader implementation, EPFTileContext implementation, MiniMap 80-byte helper, compiler runtime source.
- Standalone, narrow, or broad source-file inference: existing broad feature file is correct; no standalone `RecordVector.cpp`, `VectorHelpers.cpp` target body, or `CopyRecordVector40Range.cpp` is defensible.

## Source Placement

- Applied placement: UID0004DO and UID00017U formal markers emit through UID0000LR `NexusTK/render/NewHumanImageLib.cpp`; `RenderComposition` is the exact caller/class operation that regenerates them.
- Why it fits: all record production, bounds adjustment, append, iteration, and render callback consumption occur in `RenderComposition`; no other source family calls the helpers.
- Header/type placement: Destination 6 emits one `#include "ImageFrameTable.h"` before NewHuman marker output. The shared header supplies `FrameDrawRecord` and `LoadFrameDrawRecord`; no duplicate NewHuman-only record declaration was added.
- `ImageFrameTable` remains the owner of record projection/type declarations, not the vector compiler support.
- `EPFTileContext` remains shared initialization/layout support, not the vector compiler support owner.
- Rejected placements: direct VectorHelpers utility source, synthetic RecordVector class source, compiler runtime source, ImageFrameTable source, EPFTileContext source, and standalone helper file.
- Remaining uncertainty: only the original local variable and private STL helper spellings. Placement is not blocked.

## Range / Split / Padding / Reclassification Analysis

- Target body is exact half-open `[0x004e6ff0,0x004e7026)`; no internal padding and no split required.
- Predecessor UID0004DN ends at `0x004e6fe2`; fourteen `0xcc` bytes fill `[0x004e6fe2,0x004e6ff0)`.
- Ten `0xcc` bytes fill `[0x004e7026,0x004e7030)` before successor `sub_4E7030`.
- UID00017U modeled body is half-open `[0x004e57c0,0x004e598a)`. Its existing filename endpoint `0x004e5989` is the inclusive last byte; that historical path key was retained and only title/working-name prose changed. No UID00017U path rename was performed.
- UID00022R remains a seven-child non-emitting index; only UID0004DO's child name/route/description was updated.
- UID0000BS changed from a reconstructable emitting pseudo-class to a `NONE/FALSE/blank` non-emitting index. Exact children retain source-bearing marker disposition through UID0000LR.
- Target `Nested:0` remains exact. Blank position remains deliberate compiler-support policy.

## Negative Evidence Summary

- No independent target caller outside UID00017U; no UID00017U product caller outside `RenderComposition`.
- No target callee, allocation, free, exception, validation, element constructor/destructor, copy method, null check, or reverse/overlap branch.
- No symbol/string/global/vtable/member field ties the target to a product helper name or pseudo-class.
- No evidence makes `CopyRecordVector40Range`, `RecordVectorInsert`, or `RecordVector_4E57C0` an original source spelling.
- No evidence supports a general `memmove`; forward overlapping destination-after-source would overwrite unread bytes.
- No evidence supports direct generic VectorHelpers ownership; generated output is a validator projection of metadata and now verifies the accepted NewHuman route.
- No evidence supports merging target with predecessor, postpadding, successor, UID0004DN, UID0004DQ, or UID00022R aggregate.
- No evidence safely collapses FrameDrawRecord and EPFTileContext source declarations despite exact ABI compatibility; accepted field roles differ.
- No evidence supports handwritten ABI wrappers/tables or a direct private-STL helper body in reconstructed source.

## IDA Rename / Type / Comment Recommendations

- Proposed documentation/source-facing target name: `FrameDrawRecordUninitializedCopyCompilerSupport`.
- Proposed UID00017U documentation name: `FrameDrawRecordVectorGrowInsertCompilerSupport`.
- Proposed source-facing element/vector role: `FrameDrawRecord` and local `std::vector<FrameDrawRecord>` in `NewHumanImageLib::RenderComposition`; reject the undeclared `NewHumanDrawRecord` compatibility name as superseded documentation shorthand.
- Proposed IDA comment at `0x004e6ff0`: compiler-generated uninitialized copy of `[first,last)` 40-byte FrameDrawRecord elements into replacement vector storage; called for prefix and suffix by the RenderComposition vector grow path; output iterator returned in EAX.
- Proposed IDA type if the supervisor later authorizes database edits: unused-receiver member/template helper with explicit `const FrameDrawRecord *first`, `const FrameDrawRecord *last`, and `FrameDrawRecord *destination`, returning `FrameDrawRecord *`. Do not force an IDA free-function `__stdcall` source identity merely because ECX is unused.
- Intentionally unchanged: accepted `FrameDrawRecord` and `EPFTileContext` declarations; their ABI compatibility is documented without conversion to an unsupported typedef/union.
- IDA DB edits are not requested or authorized in this assignment. No IDA state was changed.

## First-Draft C++ Recommendation

- Eligible for independent body C++: no. The body is compiler-generated private vector support, and a standalone function would duplicate compiler output and misstate source architecture.
- Eligible formal content: exact covered-by markers on UID0004DO and UID00017U; exact blank block on non-emitting UID0000BS.

Destination 1 - UID0004DO after the exact path rename to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`, byte-for-byte formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated uninitialized range-copy support for the local
// std::vector<FrameDrawRecord> in NewHumanImageLib::RenderComposition.
// Regenerated by the human source operation:
//     frameDrawRecords.push_back(frameDrawRecord);
// Do not emit an independent source function for [0x004e6ff0,0x004e7026).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Destination 2 - UID00017U, byte-for-byte formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated grow/insert support for the local
// std::vector<FrameDrawRecord> in NewHumanImageLib::RenderComposition.
// Regenerated by the human source operation:
//     frameDrawRecords.push_back(frameDrawRecord);
// The compiler emits the full-capacity path and its private range-copy support;
// do not emit an independent RecordVector class method.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Destination 3 - UID0000BS, byte-for-byte empty formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Destination 6 - UID000092 NewHumanImageLib class route, byte-for-byte formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "ImageFrameTable.h"

// NewHumanImageLib method bodies and data are emitted by exact child pages;
// the aggregate class declaration remains deferred until the remaining
// header/layout/helper declarations are source-ready.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Destination 12 - UID000417, byte-for-byte formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewHumanImageLib::LoadPartFrameDrawRecord(int partIndex,
                                               unsigned short descriptorIndex,
                                               int motionIndex,
                                               int frameIndex,
                                               FrameDrawRecord *outRecord)
{
    NewHumanPartEntry *part = &m_partEntries[partIndex];
    NewHumanPartFrameDescriptor *descriptor =
        &part->frameDescriptors[descriptorIndex];
    NewHumanSequenceFrameTable *sequenceFrames =
        descriptor->sequenceFrames[motionIndex];
    if (sequenceFrames == NULL)
        return;

    LoadFrameDrawRecord(part->rawRecords,
                        sequenceFrames->frames[frameIndex].recordIndex +
                            descriptor->drawIndexBase,
                        outRecord);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

Destination 13 - UID000438, byte-for-byte formal block:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int __stdcall NewHumanImageLib::ResolveCompositionPartFrame(
    NewHumanPartEntry *part,
    unsigned short partId,
    int sequence,
    int frame,
    int yOffset,
    int xOffset,
    FrameDrawRecord *outRecord)
{
    int selectedPart = 0;
    if (part->descriptorCount > partId)
        selectedPart = partId;

    NewHumanPartFrameDescriptor *descriptor = &part->frameDescriptors[selectedPart];
    NewHumanSequenceFrameTable *sequenceFrames = descriptor->sequenceFrames[sequence];
    if (sequenceFrames != NULL) {
        LoadFrameDrawRecord(
            part->rawRecords,
            sequenceFrames->frames[frame].recordIndex + descriptor->drawIndexBase,
            outRecord);
        return RectBoundsAdjust(&outRecord->sourceRect, xOffset, yOffset);
    }

    return 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Reason behavior is exact: the marker names the one human append operation whose no-growth and full-capacity branches are both directly visible. Compiling that typed vector operation produces allocation/grow support and the private uninitialized prefix/suffix copy represented by the two exact ranges. Destinations 6, 12, and 13 make the already accepted record producer path compile-visible with the one shared `FrameDrawRecord` declaration and do not change any observed branch or field offset.
- Period plausibility: compiler-era MSVC `std::vector` emits out-of-line private helpers for full-capacity insertion and trivial uninitialized range copying. A developer would write one vector append, not raw SSE lanes, allocator cookie validation, or a product `RecordVector_4E57C0` class.
- Inferred names: `frameDrawRecords` and `frameDrawRecord` are descriptive local names in comments only; original spellings are not claimed. `FrameDrawRecord`, `NewHumanImageLib`, and `RenderComposition` are current accepted source-facing types/names.
- Coding style: period C++ member method and STL vector operation, with no modern-only constructs.
- Exact no-code proof: target has no independent callers/state/callees; both calls are private prefix/suffix relocation inside full-capacity vector insertion; callers supply an unused vector receiver; return is output iterator; element copy is trivial; actual source operation is independently visible in the sole product caller. A handwritten helper cannot improve fidelity and would create duplicate output.

## Final Recommendation

- Destinations 1-13 were applied exactly after supervisor Gate 1 acceptance; Destinations 14-15 were verified same-or-greater without edits; Destination 16 generated verification passed. Existing unrelated detail and historical provenance were preserved.
- The target documentation artifact was renamed from `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md` to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`, with the descriptive title, `92/94`, owner/emitter UID0000LR, reconstructable true, blank position, `Nested:0`, and Destination 1. The old target path is absent.
- UID00017U is `92/94`, owner/emitter UID0000LR, reconstructable true, blank position, with exact half-open range prose, Destination 2, typed caller arguments, allocation/free/EH, and source-operation evidence.
- UID0000BS is `92/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/position/C++, with Destination 3 and historical-index prose.
- Destination 6 supplies the compile-visible `ImageFrameTable.h` include. UID000417/UID000438 use the accepted `FrameDrawRecord *` signature, and UID000438 uses `sourceRect` at raw offset `+0x10`; their accepted behavior, scores, and ownership remain intact.
- UID00022R remains a non-emitting seven-child index, and UID0004DN/UID0004DQ remain accepted direct-NewHuman siblings.
- FrameDrawRecord and EPFTileContext remain separate accepted source-facing declarations with documented 0x28 ABI compatibility and distinct semantics.
- No source-bearing target is blocked. Original private STL/local names remain confidence caps only.
- No third-party import directive applies.

## Recommended Target Doc Changes

- Applied target path: renamed `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md` exactly to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; set the accepted descriptive title/working name; removed the old path. Scoped validator `000000010429` updated the existing UID mapping and known references through the documented validator rename mechanism. No stable-path fallback was used.
- Applied metadata: `COMPLETION:92`, `CONFIDENCE:94`, `CANONICAL_OWNER:0000LR`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000LR`, blank `EMITTER_POSITION_OPTIONAL`, `Nested:0`.
- Applied formal C++: Destination 1 exactly.
- Applied Item Summary: `Compiler-generated uninitialized range-copy support for the RenderComposition local 40-byte FrameDrawRecord vector; exact body, boundaries, ABI, two prefix/suffix calls, output-iterator return, shallow-copy contract, NewHuman source route, and no-independent-body disposition documented.`
- Incorporated exact bytes/hash, instruction/CFG, padding, two callers and arguments, output return, empty range, forward/overlap limits, 40-byte lane shape, type/lifetime, compiler disposition, source operation, owner/position rationale, analog, negative evidence, rejected alternatives, and score rationale.
- Historicalized old `CopyRecordVector40Range`, synthetic RecordVector direct ownership, and VectorHelpers direct route without deleting their provenance.

## Recommended Support Doc Changes

The destination instructions below are retained as the full accepted callback specification. Destinations 2-13 are applied as written, Destinations 14-15 are verified same-or-greater and unchanged, and Destination 16 is verified by final validator command `000000010472` plus the exact readback recorded below.

Destination 2 - `by-memory/0x004e57c0-0x004e5989.RecordVectorInsert.md`:
- Set `92/94`, owner/emitter UID0000LR, reconstructable true, blank position, and Destination 2 formal marker.
- State exact modeled half-open body `[0x004e57c0,0x004e598a)` and that stable filename endpoint `5989` is inclusive-last-byte history.
- Replace generic RecordVector prose with exact FrameDrawRecord vector full-capacity grow/insert behavior, all allocator/max/growth/alignment/free/EH facts, both UID0004DO call arguments, one caller at `0x004e4c14`, and source-operation/no-independent-body conclusion.

Destination 3 - `by-class/RecordVector_4E57C0.md`:
- Set `92/93`, owner NONE, reconstructable false, blank emitter/position/formal block.
- Preserve as a historical generated-name index linking UID00017U and UID0004DO; state no source class/object/declaration exists and exact children emit marker dispositions through UID0000LR.

Destination 4 - `by-file/VectorHelpers.md`:
- Remove UID0000BS from direct covered generated classes and reject direct UID00017U/UID0004DO ownership.
- Preserve the old route as labeled historical and keep unrelated shared pointer/uint/vector helpers unchanged.
- Record that one-feature NewHuman compiler lowerings belong to the typed NewHuman source operation; no by-file reconstruction metadata/block may be added.

Destination 5 - `by-file/NewHumanImageLib.md`:
- Add report-level detail for the `RenderComposition` local FrameDrawRecord vector, 18 producer calls, initializer, inline no-growth append, full-capacity UID00017U call, UID0004DO prefix/suffix support, trivial-copy/borrowed-record lifetime, and direct file route.
- Record `ImageFrameTable.h` as the highest-probability original header dependency supplying `FrameDrawRecord` and `LoadFrameDrawRecord`; this is source-placement prose only because by-file roots must not receive reconstruction metadata/blocks.
- Preserve all unrelated NewHuman detail and existing UID0004DN/UID0004DQ dispositions.

Destination 6 - `by-class/NewHumanImageLib.md` plus prose-only `by-type/by-struct/NewHumanImageLibLayout.md` synchronization:
- Install Destination 6's exact formal block so `ImageFrameTable.h` is compile-visible before NewHuman exact children and preserve the existing class-container comment.
- Add the same semantic method inventory and explicitly state UID00017U/UID0004DO are file-routed compiler support, not ordinary class methods.
- In the layout page, historicalize `NewHumanDrawRecord` as superseded shorthand, name accepted `FrameDrawRecord` for UID000417/UID000438/RenderComposition, preserve all offsets and other nested names, and leave the layout formal block/metadata unchanged.
- No class metadata change is recommended.

Destination 7 - `by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md`:
- In `RenderComposition`, record the exact 40-byte local initialization/producer/append/iteration route, one full-capacity call, inline fast path, and compiler-support children.
- Retain aggregate NONE/FALSE/blank metadata and current unrelated method evidence.

Destination 8 - `by-memory/0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers.md`:
- Keep `88/91`, NONE/FALSE, seven children, blank emitter/position, and current split marker.
- Update only UID0004DO's child row/link to the accepted descriptive name and exact renamed path `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`, UID0000LR route, exact typed compiler disposition, and target score; preserve all boundaries/padding/siblings.

Destination 9 - `by-item/EPFArchiveMetadataTable.md`:
- Preserve the current formal `FrameDrawRecord` declaration byte-for-byte.
- Add that `RenderComposition` uses it as a 40-byte trivially copied vector element populated by LoadFrameDrawRecord; record ABI compatibility with EPFTileContext and reject an unsupported type collapse.

Destination 10 - `by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md` and `by-item/LoadFrameDrawRecord_004D1600.md`:
- Preserve current formal function body and source names.
- Add the `RenderComposition`/UID0004DO consumer: projected records are shallow copied in a local vector, with payload pointers, width/state, bounds, span, and end pointer retained exactly.

Destination 11 - `by-class/EPFTileContext.md`:
- Preserve metadata/layout/formal declaration.
- Add exact evidence that `RenderComposition` invokes the shared 0x28 initializer on its local before LoadFrameDrawRecord, while distinct `+0x20/+0x24` semantic names prevent collapsing EPFTileContext and FrameDrawRecord.

Destination 12 - `by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md`:
- Preserve `88/90`, owner/emitter UID000092, range, branch/body, and all accepted descriptor/sequence evidence.
- Install Destination 12 exactly: replace only undeclared `NewHumanDrawRecord *` with accepted `FrameDrawRecord *`; no behavior changes.

Destination 13 - `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`:
- Preserve `87/89`, owner/emitter UID000092, range, all arguments/branches, record-index calculation, bounds offset order, and accepted descriptor/sequence evidence.
- Install Destination 13 exactly: replace undeclared `NewHumanDrawRecord *` with `FrameDrawRecord *` and map raw offset `+0x10` to accepted field `sourceRect`.

Destination 14 - `by-memory/0x004e6e50-0x004e6fe2.NewHumanImageLibGrowCommandSideVector.md`:
- Read-only comparison. Current direct UID0000LR marker route and exact postpad are same-or-greater; no edit unless callback finds concrete drift.

Destination 15 - `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md` and its accepted support:
- Read-only comparison. Preserve distinct 80-byte command-record append/grow semantics; do not conflate with the 40-byte FrameDrawRecord vector.

Destination 16 - generated/read-only verification after an accepted callback:
- Final waited validator refresh must show UID00017U then UID0004DO exactly once in `auto-generated/NexusTK/render/NewHumanImageLib.cpp` with the accepted markers.
- `auto-generated/NexusTK/util/VectorHelpers.cpp` must contain neither target marker nor UID0000BS Empty Emitter Marker.
- Generated NewHuman output must show one compile-visible `#include "ImageFrameTable.h"`; if UID000417/UID000438 are emitted by the accepted class-child route, their signatures must use `FrameDrawRecord *`, UID000438 must use `sourceRect`, and no `NewHumanDrawRecord` token may remain.
- No standalone copy/grow body, duplicate target, handwritten private STL ABI, or manual generated edit is allowed.

## Score And Metadata Recommendation

| Artifact | Evidence-time / pre-callback | Applied | Rationale / cap |
| --- | --- | --- | --- |
| UID0004DO | `86/90`, owner/emitter UID0000BS, true, blank position | `92/94`, owner/emitter UID0000LR, true, blank position, `Nested:0` | Exact range/body/hash/CFG/ABI/return/calls/arguments/type/lifetime/source operation/no-code proof resolved. Cap: stripped private STL and local names. |
| UID00017U | `86/90`, owner/emitter UID0000BS, true, blank position | `92/94`, owner/emitter UID0000LR, true, blank position | Exact half-open range, single caller, all growth/EH/allocation/copy/free behavior, FrameDrawRecord type, and source append resolved. |
| UID0000BS | `85/86`, owner/emitter UID0000P3, true | `92/93`, owner NONE, false, blank emitter/position/C++ | Pseudo-class identity rejected; complete historical index remains. Cap: generated label has no original symbol. |
| UID00022R | `88/91`, NONE/FALSE, seven children | unchanged | Current split/index is correct; child route text only. |
| UID0000LR / UID000092 / UID00017R | existing metadata | unchanged | Source-operation evidence added without claiming full RenderComposition reconstruction. |
| UID0000P3 | `86/86` file | unchanged | Stale direct route removed; unrelated utility inventory prevents target-driven score change. |
| FrameDrawRecord/EPFTileContext support | existing metadata | unchanged | Consumer/type-boundary evidence added; no formal type collapse. |
| UID000417 / UID000438 | `88/90` and `87/89` | scores/owners unchanged | Exact formal type sync applied, removing undeclared `NewHumanDrawRecord`; UID000438 uses accepted `sourceRect`. No score increase was needed because broader original nested names remain existing caps. |

Score-improvement attempt:

- Range/boundary blocker: removed with modeled size, raw pre/post bytes, successor lookup, and hash.
- ABI/signature blocker: removed with both callsites' explicit ECX receiver plus `ret 0x0c`; free-stdcall decompiler inference is historical only.
- Caller/xref blocker: removed with exact two target xrefs and one UID00017U product caller.
- Type/layout blocker: removed to highest defensible `FrameDrawRecord` role using producer and accepted 0x28 declaration; UID000417/UID000438 exact signatures and field spelling are synchronized; EPFTileContext ABI compatibility is documented without unsafe collapse.
- Copy/lifetime blocker: removed with exact lanes, no hooks, backing-only cleanup, borrowed pointer behavior, and forward overlap contract.
- Source-operation blocker: removed with inline/no-capacity versus call/full-capacity branch in `RenderComposition`.
- Ownership/block-position blocker: removed through direct NewHuman source operation and compiler-support no-authored-position rule.
- C++ blocker: removed by exact target-specific no-independent-body proof and exact formal markers. No body is omitted for lack of research.
- Name blocker: old generated name rejected; descriptive compiler-support name selected. Original private STL spelling remains a confidence cap, not a reconstruction blocker.

## Open Questions With Attempted Resolution

1. Is this a handwritten copy helper?
   - Checked symbols, callers, xrefs, body state, callsite receiver, vector branch, analog helper, and executed reports.
   - Resolution: no. Compiler-template private uninitialized range copy is decisively stronger.
2. Is the authored operation `std::copy`, `memcpy`, `insert`, or append?
   - Checked no-growth inline path and full-capacity call arguments.
   - Resolution: human source is a local vector append; UID00017U/UID0004DO are lowerings. `std::copy`/memcpy are rejected as authored calls.
3. Is the 40-byte type EPFTileContext or FrameDrawRecord?
   - Checked both accepted layouts, initializer, producer, downstream field roles, `+0x20/+0x24` semantics, UID000417/UID000438 formal signatures, and the absent `NewHumanDrawRecord` declaration.
   - Resolution: use FrameDrawRecord for this vector and both exact NewHuman producer children; use `sourceRect` at raw `+0x10`; make `ImageFrameTable.h` compile-visible. Document exact EPFTileContext ABI compatibility but do not collapse those declarations.
4. Is overlap supported?
   - Checked loop direction and both actual allocations.
   - Resolution: forward copy only; actual ranges are disjoint old/new storage. Not memmove-safe generally.
5. Should UID0004DO emit a body?
   - Checked all reasonable source routes and compiler controls.
   - Resolution: no independent body. Destination 1 is complete no-code marker proof, not a deferred blocker.
6. Who owns source?
   - Checked one caller family, sibling precedents, generated output, and synthetic class state.
   - Resolution: UID0000LR direct owner/emitter; UID0000BS non-emitting; VectorHelpers historical context only.
7. What position is correct?
   - Checked source architecture and emitter ordering semantics.
   - Resolution: blank is exact because the helper has no authored source position.
8. What remains unresolved?
   - Only original local variable/private STL helper spellings. All healthy current routes were exhausted. This caps confidence at 94 and does not affect code disposition, score threshold, owner, route, range, or formal markers.
9. Does UID0004DO retain its current filename or receive the descriptive filename?
   - Checked `by-structure.md` rename guidance and the validator's real-rename, UID-map, and reverse-reference behavior.
   - Resolution and applied result: renamed exactly to `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`, removed the old target path, and scanned the renamed file in normal scoped file mode under command `000000010429`. Retaining `CopyRecordVector40Range.md` was rejected rather than deferred to callback choice.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

Inspected manual rows:

- `by-memory/-coverage-report.md`: UID0004DO absent; UID00017U line 1725 stale `78%`; UID00017R line 1716 stale `76%`/reconstructable; UID00022R line 1747 stale reconstructable/direct-VectorHelpers description; padding rows at `0x004e6fe2-0x004e6ff0` and `0x004e7026-0x004e7030` remain accurate.
- `by-class/-coverage-report.md`: UID0000BS line 446 stale `80%` reconstructable/direct-VectorHelpers; UID000092 line 349 stale `85%`; UID00004I line 187 stale `85%` and lacks ABI-compatibility consumer evidence.
- `by-file/-coverage-report.md`: UID0000LR line 180 stale `87%`; UID0000P3 line 299 retains RecordVector direct ownership; UID0000K1 line 118 was inspected and needs no target-specific change because formal projection ownership remains unchanged.
- `by-item/-coverage-report.md`: UID0000UN line 46 stale `85%` and lacks vector/trivial-copy consumer evidence.
- `by-memory/-coverage-report.md`: UID0002P6 line 1667 stale `80%` and lacks RenderComposition vector-consumer evidence.
- `by-memory/-coverage-report.md`: UID000417 and UID000438 rows are absent despite current exact source-bearing child pages; both require supervisor-owned additions reflecting the applied callback type synchronization.
- `by-type/by-struct/-coverage-report.md`: UID0001VF line 83 retains the undeclared `NewHumanDrawRecord` support-name direction indirectly and lacks the accepted FrameDrawRecord synchronization.

Exact supervisor replacement/addition text:

File `by-memory/-coverage-report.md`, replace UID00017R row:

```markdown
    - [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md) 0x004dfd10-0x004e4d79 | class/source aggregate | NewHumanImageLib : not_reconstructable : 85% : strong : Non-emitting NewHuman local-method index with exact source-bearing children; RenderComposition now records the 40-byte FrameDrawRecord initialization/projection, inline vector append, full-capacity UID00017U grow path, UID0004DO private range-copy support, and direct NewHuman file route while the aggregate remains NONE/FALSE/blank.
```

File `by-memory/-coverage-report.md`, replace UID00017U row:

```markdown
    - [UID:00017U][0x004e57c0-0x004e5989.RecordVectorInsert](by-memory/0x004e57c0-0x004e5989.RecordVectorInsert.md) 0x004e57c0-0x004e598a | compiler-generated vector support | FrameDrawRecordVectorGrowInsertCompilerSupport : reconstructable : 92% : very-strong : Exact full-capacity std::vector<FrameDrawRecord> grow/insert lowering for NewHumanImageLib::RenderComposition, with one caller, 1.5x growth, aligned allocation, inserted-record copy, UID0004DO prefix/suffix relocation, checked old-storage free, vector triplet update, typed source append marker, direct UID0000LR route, and stable filename endpoint 0x004e5989 preserved as the inclusive last-byte key.
```

File `by-memory/-coverage-report.md`, replace UID00022R row:

```markdown
    - [UID:00022R][0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers](by-memory/0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers.md) 0x004e6ae0-0x004e70e9 | split/index | ImageLibVectorSupportHelpers : not_reconstructable : 88% : very-strong : Non-emitting seven-child index over HumanImageLib loaders, Monster/NewHuman compiler-vector support, FrameDrawRecord range-copy support, and ImageLoaders JPEG setup; exact children carry their own source routes, including UID0004DO through UID0000LR, and no whole-range VectorHelpers owner/emitter remains.
```

File `by-memory/-coverage-report.md`, add immediately after the existing `0x004e6fe2-0x004e6ff0` padding row and before `0x004e7026-0x004e7030` padding:

```markdown
        - [UID:0004DO][0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport](by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md) 0x004e6ff0-0x004e7026 | compiler-generated vector support | FrameDrawRecordUninitializedCopyCompilerSupport : reconstructable : 92% : very-strong : Exact 54-byte private uninitialized range-copy lowering for RenderComposition's local 40-byte FrameDrawRecord vector; two UID00017U prefix/suffix calls, unused receiver ABI, output-iterator return, two 16-byte plus one 8-byte shallow copy, forward/disjoint-storage contract, no callees/EH/lifetime hooks, direct UID0000LR route, and no-independent-body marker are resolved.
```

File `by-memory/-coverage-report.md`, replace UID0002P6 row:

```markdown
        - [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md) 0x004d1600-0x004d165d | function | LoadFrameDrawRecord : reconstructable : 88% : very-strong : Exact ImageFrameTable projection helper with bounds/count checks, payload pointer/span/end reconstruction, next-record boundary semantics, broad consumers, adjacent raw-helper splits, and RenderComposition evidence that projected 40-byte FrameDrawRecord values are shallow-copied in a local vector through UID00017U/UID0004DO.
```

File `by-memory/-coverage-report.md`, add UID000417 under UID00017R with the other exact NewHuman method children:

```markdown
        - [UID:000417][0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord](by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md) 0x004e0d70-0x004e0dcf | method | NewHumanImageLib::LoadPartFrameDrawRecord : reconstructable : 88% : very-strong : Exact part/descriptor/sequence frame-record resolver with one-byte successor padding, two callers, LoadFrameDrawRecord dependency, accepted FrameDrawRecord output type, null-sequence no-write branch, class/file ownership, and first-draft body.
```

File `by-memory/-coverage-report.md`, add UID000438 under UID00017R immediately before the RenderComposition/compiler-support entries:

```markdown
        - [UID:000438][0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame](by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md) 0x004e4210-0x004e427c | method | NewHumanImageLib::ResolveCompositionPartFrame : reconstructable : 87% : strong : Exact 18-caller composition frame resolver that clamps the part selector, projects one accepted FrameDrawRecord, offsets its sourceRect, preserves the null-sequence return, and supplies the typed producer for RenderComposition's 40-byte local vector.
```

File `by-class/-coverage-report.md`, replace UID0000BS row:

```markdown
- [UID:0000BS][RecordVector_4E57C0](by-class/RecordVector_4E57C0.md) : not_reconstructable : 92% : very-strong : Historical generated pseudo-class index for UID00017U grow/insert and UID0004DO range-copy compiler support; no class object, declaration, vtable, storage, or independent source body exists, so owner/emitter/C++ are blank and exact children route through NewHumanImageLib's RenderComposition source operation.
```

File `by-class/-coverage-report.md`, replace UID000092 row:

```markdown
- [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) : reconstructable : 86% : strong : Singleton new-human composition/render class with constructor/destructor/resource/vector method families; RenderComposition now records its local 40-byte FrameDrawRecord vector, inline append, full-capacity UID00017U grow path, UID0004DO private range-copy support, and direct NewHuman file routing without misclassifying compiler support as ordinary class methods.
```

File `by-class/-coverage-report.md`, replace UID00004I row:

```markdown
- [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) : reconstructable : 88% : very-strong : Exact 0x28 decoded tile-context layout with lifecycle/mask/copy/allocation methods and broad producers/consumers; RenderComposition additionally proves the shared initializer is applied to a 0x28 frame-record local before LoadFrameDrawRecord, while distinct +0x20/+0x24 semantics keep EPFTileContext and FrameDrawRecord source declarations separate despite ABI compatibility.
```

File `by-file/-coverage-report.md`, replace UID0000LR row:

```markdown
- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) : reconstructable : 90% : strong : NexusTK/render/NewHumanImageLib.cpp source root with singleton/vtable/layout/resource/method families and typed compiler-vector support; RenderComposition owns the local FrameDrawRecord append whose full-capacity UID00017U and private UID0004DO range-copy lowerings emit marker dispositions through this file alongside the accepted NewHuman vector-support siblings.
```

File `by-file/-coverage-report.md`, replace UID0000P3 row:

```markdown
- [UID:0000P3][VectorHelpers](by-file/VectorHelpers.md) : reconstructable : 86% : strong : Utility/template source-structure index for genuinely shared pointer, unsigned-integer, storage, and compatibility helpers; historical RecordVector_4E57C0 direct ownership is superseded because UID00017U/UID0004DO are one-caller NewHuman compiler lowerings routed through RenderComposition, while unrelated VectorHelpers children and header/template caveats remain unchanged.
```

File `by-item/-coverage-report.md`, replace UID0000UN row:

```markdown
- [UID:0000UN][EPFArchiveMetadataTable](by-item/EPFArchiveMetadataTable.md) : reconstructable : 88% : very-strong : Shared EPF/EPD table, 24-byte packed record, and 40-byte FrameDrawRecord declarations tied to loader/projection/sentinel evidence; RenderComposition additionally proves FrameDrawRecord is a trivially shallow-copied local vector element, ABI-compatible with EPFTileContext storage but retaining distinct frame-payload field semantics.
```

File `by-type/by-struct/-coverage-report.md`, replace UID0001VF row:

```markdown
- [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md) : reconstructable : 85% : strong : NewHumanImageLib object and nested-row layout support with constructor/destructor agreement, singleton/vtable/resource/vector evidence, and exact FrameDrawRecord synchronization for UID000417/UID000438/RenderComposition; historical undeclared NewHumanDrawRecord shorthand is superseded while broader nested original names remain confidence caps.
```

- Reason B agent must not apply directly: every listed `-coverage-report.md` file is manual supervisor-owned coverage state. The report-only assignment forbids coverage writes, and validator-owned generated coverage does not update these manual rows.
- No auto-generated tracker/coverage row text is supplied; target `Item Summary` and `Nested` metadata are the validator source for generated coverage.

## Follow-Up Actions

- External ownership: B002 callback work, validators, generated readback, and lease releases are complete. Exact-artifact review, execution, move, archive, report path/count state, and manual coverage application are supervisor/validator-owned and are not asserted as current or pending actions by this report. If manual coverage is later applied, only the supervisor uses the exact replacements/additions above, including the UID0004DO row linked to the renamed target path.
- A-agent actions: none. Manual coverage ownership for this report is exclusively supervisor-owned.
- B002 remaining assignment work: none. No accepted callback item is blocked or omitted; C30 is intentionally `excluded-with-reason` because manual coverage is outside B002 write authority.

## Confidence

- Recommendation confidence: very strong for compiler-template no-independent-body disposition and direct NewHuman source route.
- Score confidence: high for `92/94`; every behavior, type-role, placement, and formal blocker is closed. Confidence stays below final due stripped original helper/local names.
- Remaining uncertainty: original MSVC private helper and local variable spellings only; no effect on range, semantics, source operation, owner/emitter, formal marker, or generated placement.

## Validator Results

- Command form for ordinary pages: `python .\tools\validator.py --mode file --file <relative-by-path> --apply --queue-timeout 240`; UID000438 waited commands additionally used `--wait-generated`. Command `000000010459` was the accepted initial completion refresh; `000000010472` is the final current refresh after the additive alias/durability audit.

| Command ID | Timestamp | Scoped page/result | Exit / ok | Warnings and side effects |
| --- | --- | --- | --- | --- |
| `000000010429` | `2026-07-13T17:25:40-04:00` | UID0004DO renamed target | `0 / 1` | Updated old-to-new UID path, metadata/owner registry, reverse references, and known links; generated refresh deferred. Three unrelated missing-reference diagnostics (`0003TP` twice, `0003SC`) surfaced from rename propagation through existing VectorHelpers content. |
| `000000010430` | `2026-07-13T17:27:17-04:00` | UID00017U | `0 / 1` | Updated metadata/owner/autogen registry; generated refresh deferred; no target warning. |
| `000000010431` | `2026-07-13T17:28:16-04:00` | UID0000BS | `0 / 1` | Applied non-emitting metadata, removed stale stats rows, updated registry; generated refresh deferred; no target warning. |
| `000000010432` | `2026-07-13T17:29:30-04:00` | `by-file/VectorHelpers.md` | `0 / 1` | Applied target route/reference removals; generated refresh deferred. Existing missing-reference diagnostics remained for `0003TP` twice and `0003SC`; unrelated content was not changed. |
| `000000010433` | `2026-07-13T17:30:21-04:00` | `by-file/NewHumanImageLib.md` | `0 / 1` | Added references/source-route prose; generated refresh deferred; no target warning. |
| `000000010434` | `2026-07-13T17:31:54-04:00` | UID000092 class | `0 / 1` | Updated formal hash and references; generated refresh deferred; no target warning. |
| `000000010435` | `2026-07-13T17:32:25-04:00` | UID0001VF layout | `0 / 1` | Updated statistics/references; generated refresh deferred; no target warning. |
| `000000010442` | `2026-07-13T17:33:19-04:00` | UID00017R aggregate | `0 / 1` | Updated target/source references; generated refresh deferred. Existing unrelated missing-reference diagnostic `0003UD` appeared twice. |
| `000000010447` | `2026-07-13T17:34:05-04:00` | UID00022R aggregate | `0 / 1` | Updated UID0004DO child path/route and removed stale UID00017U reference; generated refresh deferred. Existing accepted aggregate marker produced `emitter_inactive_has_data`; NONE/FALSE split/index disposition was intentionally preserved. |
| `000000010448` | `2026-07-13T17:36:19-04:00` | UID0000UN first pass | `0 / 1` | Added consumer references/stats; generated refresh deferred. This pass exposed three B002-introduced invalid provisional UID links; they were corrected immediately before release. |
| `000000010449` | `2026-07-13T17:36:52-04:00` | UID0000UN corrected final pass | `0 / 1` | Clean final scoped result; added valid UID00017R/UID000417/UID000438 references and updated stats; generated refresh deferred. |
| `000000010451` | `2026-07-13T17:37:25-04:00` | UID0002P6 | `0 / 1` | Added five accepted target/consumer references; generated refresh deferred; no target warning. |
| `000000010455` | `2026-07-13T17:37:57-04:00` | UID0000UX | `0 / 1` | Added five accepted target/consumer references and updated stats; generated refresh deferred; no target warning. |
| `000000010457` | `2026-07-13T17:38:31-04:00` | UID00004I | `0 / 1` | Added UID00017R/UID0000UX references; generated refresh deferred. Three pre-existing missing-reference diagnostics for UID0003ZP remained unrelated. |
| `000000010458` | `2026-07-13T17:39:15-04:00` | UID000417 | `0 / 1` | Updated exact formal-block autogen hash; generated refresh deferred; no target warning. |
| `000000010459` | `2026-07-13T17:40:08-04:00` | UID000438 plus `--wait-generated` | `0 / 1` | Updated exact formal-block hash/references and completed generated refresh. Broad project diagnostics were informational (`children` marker/fallback and emitting-children-only inventory); target validation succeeded. Rebuilt registry with `4733` nodes/`3841` edges, refreshed `280` generated metadata outputs, and refreshed validator-owned generated coverage/statistics. |
| `000000010461` | `2026-07-13T17:51:42-04:00` | UID00017U historical-label audit | `0 / 1` | Historicalized the last pseudo-class method sentence; generated refresh deferred; no target warning. |
| `000000010464` | `2026-07-13T17:52:23-04:00` | UID000092 alias/header-history audit | `0 / 1` | Reconciled current `FrameDrawRecord` wording and time-scoped superseded header/alias evidence; generated refresh deferred; no target warning. |
| `000000010465` | `2026-07-13T17:52:50-04:00` | UID00017R alias audit | `0 / 1` | Reconciled current accepted `FrameDrawRecord` wording; generated refresh deferred. Existing unrelated UID0003UD missing-reference warning appeared twice. |
| `000000010468` | `2026-07-13T17:53:12-04:00` | `by-file/NewHumanImageLib.md` compile-visibility audit | `0 / 1` | Reconciled prose with the applied UID000092 include route while preserving the by-file no-metadata rule; generated refresh deferred; no target warning. |
| `000000010469` | `2026-07-13T17:53:41-04:00` | UID0001VF historical-alias audit | `0 / 1` | Labeled the remaining dated alias occurrence as superseded and updated three projected statistics rows; generated refresh deferred; no target warning. |
| `000000010472` | `2026-07-13T17:54:00-04:00` | final UID000438 `--wait-generated` | `0 / 1` | Final generated refresh completed after alias/durability audit. Rebuilt registry with `4734` nodes/`3840` edges and refreshed `278` metadata outputs. Broad marker/emitter diagnostics were informational. Validator-owned side effects included generated metadata/coverage/statistics, one unrelated `ItemDialogs.cpp` autogen update, and research-tracker refresh; B002 made no manual generated/tracker edit. |

### Generated Readback Proof

- `auto-generated/NexusTK/render/NewHumanImageLib.cpp` header records final validator command `000000010472` at `2026-07-13T17:54:00-04:00`; SHA256 is `2D48EE18EB19994932F4C9DF08976662337F3729C97F18F90CBB29CCC59F14C5`.
- UID00017U occurs exactly once at line 222 and UID0004DO occurs exactly once later at line 246. Each marker segment contains zero non-comment/nonblank lines and zero opening braces; UID0004DO's segment contains no `memmove` or `operator new`, proving no standalone copy/grow body or handwritten private STL ABI was emitted.
- `#include "ImageFrameTable.h"` occurs exactly once. `NewHumanDrawRecord`, `CopyRecordVector40Range`, and `RecordVector_4E57C0` each occur zero times.
- UID000417/UID000438 are not currently selected into generated output by the accepted class-child route, so Destination 16's conditional generated-signature clause does not trigger. Their ordinary pages nevertheless contain the exact accepted `FrameDrawRecord *` formal blocks, UID000438 uses `sourceRect`, both scoped validators passed, and generated NewHuman output contains zero `NewHumanDrawRecord` tokens.
- `auto-generated/NexusTK/util/VectorHelpers.cpp` SHA256 is `F3303DB34F4C2B9A582DE72F02A502BE8CA823E76703DA77B965956BFC81DFA7`; it contains zero UID0004DO markers, zero `FrameDrawRecordUninitializedCopyCompilerSupport` markers, and zero UID0000BS Empty Emitter Markers.
- Generated/manual ownership: B002 made no manual generated, coverage, tracker, audit, supervisor, validator-state, or lifecycle edit. All generated changes above are validator side effects from authorized waited commands `000000010459` and final `000000010472`.

## Changed Files

- Report: `tools/leaser/Agents/Agent-B002/research/0004DO-CopyRecordVector40Range-source-quality.md`, preserving report-only research and Gate 1 repair history while adding callback proof.
- UID-preserving rename: `by-memory/0x004e6ff0-0x004e7026.CopyRecordVector40Range.md` -> `by-memory/0x004e6ff0-0x004e7026.FrameDrawRecordUninitializedCopyCompilerSupport.md`; old path absent.
- Modified ordinary pages: `by-memory/0x004e57c0-0x004e5989.RecordVectorInsert.md`; `by-class/RecordVector_4E57C0.md`; `by-file/VectorHelpers.md`; `by-file/NewHumanImageLib.md`; `by-class/NewHumanImageLib.md`; `by-type/by-struct/NewHumanImageLibLayout.md`; `by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md`; `by-memory/0x004e6ae0-0x004e70e9.ImageLibVectorSupportHelpers.md`; `by-item/EPFArchiveMetadataTable.md`; `by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md`; `by-item/LoadFrameDrawRecord_004D1600.md`; `by-class/EPFTileContext.md`; `by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md`; and `by-memory/0x004e4210-0x004e427c.NewHumanImageLibResolveCompositionPartFrame.md`.
- Verify-only unchanged pages: `by-memory/0x004e6e50-0x004e6fe2.NewHumanImageLibGrowCommandSideVector.md` and `by-memory/0x004e70a0-0x004e70e9.NewHumanImageLibAppendCommandRecord80.md`.
- Leases: every ordinary page was leased only immediately before its edit and released immediately after its final scoped validator. Final read-only `tools/leaser/Agents/current_leases.md` search returned no B002 entry. UID0000UN required a corrective second validator before release; no second file was leased concurrently.
- IDA/manual coverage/tracker/audit/supervisor/validator-state/lifecycle state: untouched. Validator-owned generated/statistics outputs changed only as recorded side effects.
- Report execution: not run. B002 did not run execute_report, a probe, lifecycle command, move, or archive action.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor Gate 1 validation accepted exact SHA `96E758113F9D73BD52351A2F66EA80CE6B4A37FBDC77E8F97D9863F6C6858FE8` before implementation.
- [x] Target/support scope completed: Destinations 1-13 applied; Destinations 14-15 verified without drift; Destination 16 generated verification completed.
- [x] Current target state and actual evidence checked recorded: exact bytes/hash/range/CFG/ABI/xrefs/calls/type/source operation/report roots/manual coverage are specified above.
- [x] Claim And Incorporation Ledger uses only accepted Action values; final callback states use only `applied`, `already-present`, and `excluded-with-reason`, with separate destination proof.
- [x] Metadata/score changes applied: UID0004DO `92/94`; UID00017U `92/94`; UID0000BS `92/93`; other listed metadata unchanged.
- [x] Score-limiting blockers resolved; original private spellings remain confidence caps only.
- [x] Owner/emitter/reconstructable changes applied: UID0004DO/UID00017U -> UID0000LR/TRUE; UID0000BS -> NONE/FALSE/blank.
- [x] No new child/split created; UID0004DO was UID-preserving-renamed exactly, old path removed, and normal scoped validation updated mappings; UID00017U retained its stable path with exact range history.
- [x] Source placement, range/split/padding/reclassification, and no-IDA-write dispositions applied or confirmed not applicable.
- [x] Destinations 1-3, 6, 12, and 13 managed blocks are exact; target/compiler-support no-independent-body proof is preserved.
- [x] Third-party import is not applicable; target is compiler-generated project source support, not a vetted third-party static embed.
- [x] Destinations 1-16 and C01-C30 incorporated at report-level detail.
- [x] Historical/stale assumptions, rejected alternatives, B008/B011 provenance, and negative evidence preserved.
- [x] No relevant Wave2/Wave3 exact artifact exists; generic historical generated-name assumptions remain independently revalidated.
- [x] Behavior/source blockers closed; stripped private/local spellings documented as confidence-only.
- [x] One scoped validator ran for each final edited ordinary page; UID0000UN's initial link defect was corrected and revalidated; final UID000438 validator used `--wait-generated`.
- [x] Destination 16 exact-count/route/type proof and exact supervisor-owned manual coverage text are recorded; B002 did not edit manual coverage.

Implementation callback pass:
- [x] Report accepted by supervisor for implementation at exact Gate 1 SHA recorded above.
- [x] All accepted target/support doc details incorporated at report-level detail.
- [x] Claim And Incorporation Ledger updated with destination and legal final verification state for every accepted claim.
- [x] Metadata/score/owner/emitter/split/rename/C++ changes applied or explicitly excluded with reason.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence preserved.
- [x] Open questions resolved or documented with evidence-backed confidence caps.
- [x] Validators run and exact commands/results/side effects recorded.
- [x] Waited generated refresh completed and exact NewHuman/VectorHelpers readback recorded.
- [x] Manual `by-*/-coverage-report.md` files remain unedited by B002; exact coverage replacements/additions remain exclusively supervisor-owned.
- [x] Remaining unapplied accepted items: none. Manual coverage is not a B002 callback item and is explicitly excluded with reason under C30.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000010512","destination_path":"executed-b-agent-research/B002/0004DO-CopyRecordVector40Range-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/0004DO-CopyRecordVector40Range-source-quality.md","timestamp":"2026-07-13T18:12:55-04:00","uid":"0004DO"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
