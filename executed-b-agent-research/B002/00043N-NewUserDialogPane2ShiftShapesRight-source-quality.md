** TARGET-REPORT-UID:00043N **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID00043N NewUserDialogPane2ShiftShapesRight Ownership / Source-Quality Research


## Finalized Report / Current Recommendation
- Implemented recommendation: [UID:00043N] is the source-authored `void NewUserDialogPane2::ShiftShapesRight()` method at exact range `0x0052b1d0-0x0052b30d`, owned and emitted by [UID:00009E][NewUserDialogPane2](by-class/NewUserDialogPane2.md) through [UID:0000LW][NewUserDialogPane2](by-file/NewUserDialogPane2.md).
- Applied target disposition: Destination 1 is installed byte-for-byte; target metadata is `92/94`, `RECONSTRUCTABLE:TRUE`, owner/emitter `00009E`, blank optional position, and `Nested:0`.
- Applied support union: symmetric [UID:00043M] carries Destination 2; [UID:00009E] omits the duplicate derived `m_childControls` and closes before `[[CHILDREN]]` with Destination 3; [UID:000038] exposes the direct enabled-state read with Destination 4; [UID:0000FR] emits Destination 5 before [UID:0002Q8] Destination 6; aggregate/class/file prose preserves and extends all unrelated history.
- Callback state: all ten authorized ordinary destinations passed scoped validation, all six managed bodies are byte-equal to the accepted report, and ordinary Gate 2 independently passed report SHA `48E1345FCC65ABCBE59BF8E71BD53F747696F78997F7B0323D3E45950C1883A8`. The supervisor then applied and validated all ten exact manual coverage actions under commands `000000016420`-`000000016423`; B002 holds zero leases and report lifecycle remains supervisor-owned.
- Confidence: `94` for target behavior and source ownership. Three target xrefs, constructor callback binding, two live callers, exact control ids/offsets, symmetric left helper, callee bodies, and virtual-slot evidence remove the current body/type/source blockers. Remaining uncertainty is lexical spelling of locals and the inferred accessor name, so confidence remains below original-symbol certainty.

## Supporting Research
- B002 used the canonical `source-3/project-documentation` tree and live read-only IDA MCP session `9b0396a3`. A fresh `idb_list` at the final evidence pass returned one active adopted NexusTK IDB worker, PID `15732`; `server_health` returned `ok`, image base `0x00400000`, and analysis, Hex-Rays, and string cache ready.
- Fresh bounded `get_bytes` with the current schema read all `317` bytes at `0x0052b1d0`; SHA256 is `CEE065413412B5AA55F2074F4A8256704BA7A176417C370533D75C5EB17E6F41`. The first 32 bytes are `55 8B EC 83 EC 24 A1 24 2F 67 00 33 C5 89 45 FC 53 56 8B F1 57 6A 08 8B 8E FC 01 00 00 8B 01 FF`; the last 16 are `4D FC 5F 5E 33 CD 5B E8 26 C4 09 00 8B E5 5D C3`.
- Existing report search terms were exactly: `UID:00043N`, `UID00043N`, `0x0052b1d0`, `0x0052b30d`, `NewUserDialogPane2ShiftShapesRight`, and `ShiftShapesRight`.
- Central executed root `executed-b-agent-research/` returned only B004 `0002QR-NewUserDialogPane2CreateUserVariant-source-quality.md` and B003 `0004GQ-NewUserMiscDialogPaneOnKeyDown-source-quality.md`. The B004 artifact SHA `493008AE706362D9939432E255AEE7CE62FA2A3DDD2BE3A51A15E143DBAC3D42` is a broad split lead whose simplistic target body is disproved here. The B003 artifact SHA `21718D44E3CA3E28949F8908A9A47F83F8ECF7320DACA9A297397F400858591E` independently supports the old shape-control step family and left/right edge semantics.
- Legacy executed roots under agent-local `research/executed/` did not exist; `tools/leaser/Agents/Older-Research/` returned no direct match; no central `tools/leaser/Agents/archived/` root exists; current active B001-B005 research directories returned no direct target report. Agent-A002 notes and B002's assignment mention the target but are not reports or source evidence.
- B014 `0004E5-PrettyButtonControlPaneStateByteVirtualForwarder-source-quality.md` SHA `18FCF56A911BE1BBDFAC30511666627C1F01F0470CAC460DE8B3CE3124974944` and B015 `0004E6-PrettyButtonControlPaneSetCallback-source-quality.md` SHA `DD0D1047A6B2A97FCE4A0662460B4E5401E376ED3887B4D1C267108CA9ECC05A` were checked as support leads. B015 corroborates constructor-time binding of the left/right member callbacks; B014 supports PrettyButton/ControlPane state forwarding but does not replace target analysis.
- At B002's report-only coordination read before Gate 1, B001 was on UID0002ZO Parcel reconciliation, B003 on UID00023U ScreenPane destructor research, B004 on UID0002SR MapRefreshDimmer vtable research, and B005 on UID0002ZD palette-buffer research. None overlapped NewUserDialogPane2, UserShapeSelectControlPane, or ControlPane. The callback reread every destination before its edit and found no conflicting lease; the final lease read is empty.
- At the callback-final coordination read, B001 had moved to report-only UID0003GT while B003/B004/B005 remained on UID00023U/UID0002SR/UID0002ZD; all were disjoint, and B001-B005 had zero leases. This is a time-bounded final-read fact, not an indefinite queue assertion.
- Supervisor Gate 1 accepted exact pre-callback SHA `9AD4A18A830A4A3C4CD9CDB859A39B2FBC8B7DFC3D740AA1F015059248245081`; B002 then applied only the authorized destinations and support prose under serial leases and validators.
- After ordinary Gate 2 passed exact post-callback SHA `48E1345FCC65ABCBE59BF8E71BD53F747696F78997F7B0323D3E45950C1883A8`, the supervisor applied the exact ten-row handoff: by-memory command `000000016420`, by-class `000000016421`, by-file `000000016422`, and final waited target command `000000016423`. This report-only reconciliation attributes those external edits to the supervisor and does not rewrite them as B002 work.
- All session availability, generated, tracker, manual coverage, report, and ordinary-file hashes below are evidence-collection-time snapshots, not claims of indefinite external state.

## Target
- Target UID: [UID:00043N].
- Target path: `by-memory/0x0052b1d0-0x0052b30d.NewUserDialogPane2ShiftShapesRight.md`.
- Evidence-time queue row before implementation was tracker line `1597`, tuple `87/89`, combined `88.0`, reconstructable true, zero direct reports.
- Callback classification: implementation and supervisor-owned manual coverage are complete; this reconciled exact artifact is ready for fresh supervisor Gate 2 review. Report execution/movement remains external supervisor lifecycle work.
- Applied scores and parent state: target `92/94`, owner/emitter `00009E`, reconstructable true, blank optional position, `Nested:0`; class [UID:00009E] `92/94`; file [UID:0000LW] `92/94`; aggregate [UID:0002QR] remains `89/90` non-emitting split/index.

## Current Target State
- Evidence-time/pre-callback state is preserved historically: target body used nonexistent helper APIs; UID00009E duplicated inherited `+0x1fc` manager storage; UID0000FR was a blank class emitter; UID0002Q8 used `int` steps plus fabricated fallback returns; command `000000016371` generated those stale forms. Those facts are superseded, not deleted.
- Current target SHA is `D699512D5FEA3A0989BD87689E775004699FEFDB6AEFD4B7665B11E069126EA9`, `5,779` bytes/`79` lines. It carries Destination 1, `92/94`, complete binary/prose/history evidence, and unchanged owner/emitter/range/position/nesting.
- Current support hashes are UID00043M `87D6FFBC6D60EBB22A8DFFD1044D447104EF852F5EB17EE783AA9195D8564032`; UID00009E `06B55DF937B07EFB2B4944FADE2AE3A947D78A584CD752ABA8ABC6C3DEA6D465`; UID000038 `BAB29EA5E3DD9054110A96AB535A9D8CA1563F5681474EF47EA06AA8110B3F75`; UID0000FR `7666C4B8C688B2387F75BCD60DFAD38EF2ABE9C7DBB1C110F956DEEA7C883D03`; UID0002Q8 `B5B6AB6F075E080FFD87A4EEE8C43B4696093307D111AAC8CE190760E5621EE2`; UID0002QR `135C09A1CE4BF44B182C16DD6F978FDC27F632069E707A80EBEEAFD611D314FF`; UID0000LW `039D4B3848875B3E318309513228FEF4ED631D4D8B800AFFC2C7C7A9757A74A5`; UID0000OX `1BF4E3924BA0138B2CB3E07A781947025E97EAE6B24D2E7F32C0735F93063E8B`; UID0000IG `7746E55AB73563FE9ED9DBADB47476F016FFA69DCF8ABB78E6B91008DAA0C5D9`; verify-only UID00003T remains byte-identical at `7101A93A4EF53CFA1F11529AEF111AA94B13EBC76222ADE546D7B59D6D521DBE`.
- B002's callback-final command `000000016407`, timestamp `2026-07-22T15:53:26-04:00`, exit `0`, `ok:1`, and its three generated hashes are retained as the historical ordinary-implementation checkpoint. It was superseded only in volatile headers/hashes by later supervisor validator epochs; source semantics did not change.
- Supervisor handoff-final command `000000016423`, timestamp `2026-07-22T16:12:07-04:00`, exit `0`, `ok:1`, completed the accepted coverage epoch generated refresh. At that checkpoint `NewUserDialogPane2.cpp` was SHA `CB4531FB3232651A7358C5613E7098BB518046EAB93ABC12425244D0F6DFBCDD`, `14,158` bytes/`508` lines; `UserCreateAppearanceControls.cpp` was SHA `C44A0A8E50E044DF0C562B5AB0B2098EB87B8EBE8D2F2422ACDEBE0370199ACB`, `33,370`/`988`; `ControlPane.cpp` was SHA `F3AC3DE90DE48961436D577A961087B0AD7F0F0CA1BB90FEEACD02162525039C`, `2,947`/`101`.
- A bounded final read after unrelated supervisor commands `000000016424`-`000000016426` observed command/header `000000016426`, refreshed `2026-07-22T16:16:56-04:00`: `NewUserDialogPane2.cpp` SHA `53847E210AC9C4C97F876961B4A9BA45910AEBED38C18552DD4D874E9B10D1A4`, `14,158/508`; `UserCreateAppearanceControls.cpp` SHA `F2EFC288B232385229D6BE02CBE81A5371663F516E50F2E0EE9734A3DC750D7E`, `33,370/988`; `ControlPane.cpp` SHA `3BA254A3FCAEAB4BE1EBF754A6630467029D84D1087A758C83E8DDB159BE6A09`, `2,947/101`.
- Generated semantic readback: exactly one UID00043N/right definition and one UID00043M/left definition; one NewUserDialogPane2 class closes at line 41 before its first definition at line 44; zero derived `m_childControls`; zero UID00043N Empty Emitter Marker. UserCreateAppearanceControls has one UID0000FR class closing at line 44 before UID0002Q8 at line 46, one forward and one backward short declaration plus one definition each, zero `int` step definitions, and zero UID0000FR Empty Emitter Marker. ControlPane has one inline `IsEnabled` and zero out-of-line definitions. All three outputs have zero cookie, handwritten vtable/thunk/scalar-ABI, or decompiler-label matches.
- Tracker command `000000016407`/SHA `2524...99CD50` and handoff command `000000016423`/SHA `99A044C5857BB7D4018628474B6FB2EA3DB5A6590E9EFC3F957EEADBF83ACBAA` remain historical checkpoints. At the bounded final read, command-`000000016426` tracker SHA is `C20B49075EE4012D90BDA5344BD8E426E170E0455B739379D99ED16941591D48`, `1,627,458` bytes/`6,485` lines; UID00043N still occurs at line `3256`, `92/94`, combined `93.0`, reconstructable true.
- Commands `000000016420`-`000000016423` applied and validated the ten accepted rows at handoff-final hashes by-memory `755F5188551AC818573B76EF0710F77AA66893361B03F3B395B89E955E5609F8`, by-class `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, and by-file `C21CDF487AFE771EF013A09BE6C24AA299A47DF48DF26B9C81B36A7A93C0DB5F`. Later unrelated supervisor commands `000000016424`-`000000016426` advanced the bounded final-read shared roots to by-memory SHA `94434F13116AE98DAFA5389C85EB21E03217184CB821AE09226129290CBC1AB1`, `1,958,001` bytes/`4,530` lines; unchanged by-class SHA `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, `253,106/623`; and by-file SHA `2BF7C218FCF6760E10CC73E9992618491D04A680AF4962E54CFDF33BD2CF85C2`, `150,805/317`. UID0002Q8/UID0002QR/UID00043M/UID00043N, UID000038/UID00009E/UID0000FR, and UID0000IG/UID0000LW/UID0000OX each still occur exactly once at their recorded anchors. B002 implementation and lease release remain complete; lifecycle state remains external supervisor/validator authority.

## Executive Recommendation
- Keep UID00043N as a direct class method, not a helper in UserCreateAppearanceControls and not part of compiler scaffolding.
- Use inherited `DialogPane::GetChild<T>` for controls `8-11`, previous button `6`, and next button `7`. Use `ControlPane::IsEnabled()` as an inline source-facing rendering of the observed direct `+0x101` read.
- For non-edge selected slots, deselect the current shape control, increment `m_selectedShapeSlot`, select the new control, and return. Do not call `SelectShapeSlot`; that helper performs a broader selector operation not present in this body.
- At slot `3`, return without side effects if next is disabled. Otherwise step control `11` first, then controls `8`, `9`, `10`; disable next when the first result is `-1`, else enable previous.
- Correct the paired left helper and the shared 16-bit step declarations in the same callback so generated source has one coherent left/right contract.

## Supervisor Active Recheck
- The supervisor assigned UID00043N because the tracker had no direct report and required independent revalidation rather than reuse of the broad B004 aggregate report.
- No range split or new child is required: `lookup_funcs` fixes the target at `0x0052b1d0`, size `0x13d`, with successor `0x0052b310`; padding separates it from both neighbors.
- Every directly consumed source-bearing dependency has an implementation-ready disposition: typed inherited child lookup already exists; ControlPane receives an inline enabled getter; UserShapeSelectControlPane receives a declaration; its exact core receives corrected 16-bit step signatures/bodies; the symmetric left helper receives exact C++.

## Inference Research Guidance Check
- Direct IDA facts are kept separate from documentation and inference. Addresses, bytes, xrefs, offsets, call order, branch predicates, and virtual slots are direct facts. Current UID links/scores are documentation facts. Local names, accessor spelling, access sections, and header/source organization are source-shape inferences.
- The B004 aggregate's generated body and its `m_childControls` label were treated as uncertain leads. Current DialogPane documentation and live target instructions supersede them.
- Source shape follows period-appropriate class methods, a stack pointer array, inherited typed child lookup, an inline const getter, and simple loops/branches. It rejects decompiler temporaries, raw vtable calls, pointer arithmetic, and modern abstractions.
- Historical Wave3 text in the NewUserDialogPane2 class is explicitly stale and ignored. No Wave2/Wave3 artifact was used as current evidence.

## Heuristic / Inference Reanalysis And Validation
- **Liveness:** `xrefs_to` returns exactly three target xrefs: calls at `0x0052b7e7` and `0x0052b8f5`, plus constructor data use at `0x0052a994`. The constructor instantiates a no-argument member-function callback and binds this target to a PrettyButton. The method is live source, not retained dead code.
- **Signature:** IDA's `char __thiscall` is return-register residue. Constructor callback type, callers ignoring EAX, and no semantic return establish `void NewUserDialogPane2::ShiftShapesRight()`.
- **Child lookup:** repeated virtual `+0x10` calls on the object at `this+0x1fc` fetch controls. Current DialogPane exposes this as inherited typed lookup. Adding another derived member is both unnecessary and layout-wrong.
- **Control identities:** ids `8-11` are four UserShapeSelectControlPane previews; id `6` is previous/left and id `7` is next/right. The paired left helper reverses these button roles, independently validating the interpretation.
- **Selected state:** `this+0x274` is a 32-bit slot index `0..3`. The non-edge branch invokes SetSelected(false), increments the field, then invokes SetSelected(true). It neither rotates shape data nor performs notification.
- **Edge gate:** the target reads byte `nextButton+0x101`; ControlPane docs map it to `m_enabled`. Because `m_enabled` is protected and the target is not ControlPane-derived, direct member access would not compile. An inline `IsEnabled() const` is the highest-probability source representation and generates a direct read.
- **Step type:** both `0x004fd070` and `0x004fd0d0` are IDA `__int16 __thiscall`; assembly returns through AX/EAX and the edge sentinel is `-1`. Source type is `short`, not current `int`.
- **Invalid-state fallthrough:** forward index greater than `8` and backward index less than `1` reach a path that returns an uninitialized AX word. The most faithful source is a non-void 16-bit method with no final return. Adding the current fallback return changes binary behavior; clamping or wrapping is also rejected. Valid callers guard these states with navigation-button enable state.
- **Call order:** on the right edge, control `11` is stepped first and its result retained; controls `8`, `9`, `10` are then stepped in that exact order in both result branches. A loop over `0..3` would call control `8` first and is not behavior-identical if invalidation or callbacks have visible timing.
- **Button update:** retained result `-1` disables next through vslot `+0x50`; other results enable previous through `+0x4c`. Reversing weights/roles or toggling both buttons is rejected.
- **Notification:** command case `7` calls this method then performs a separate selected-shape notification through the dialog virtual surface. The key path calls the method directly. Notification therefore stays outside the target.
- **Compiler output:** stack cookie load/check and epilogue are compiler lowering caused by the local pointer array. No handwritten cookie, vptr, EH, allocation, free, or cleanup belongs in source.
- **Source ownership:** the method touches NewUserDialogPane2 own slot state and inherited controls, is directly bound to that object, and is adjacent to sibling class methods. UserShapeSelectControlPane owns only the step/set-selected callees; DialogPane owns only lookup.
- **Rejected alternatives:** current `SelectShapeSlot` body, invented `ShiftShapeRight`, `GetShapeSelectControl`, file-local free helper, UserShapeSelectControlPane ownership, compiler-wrapper classification, direct protected-field access, notifying inside the target, broad loop reordering, int return type, and explicit invalid-index fallback are all contradicted by current evidence.

## Evidence Standards Used
- Primary evidence: live IDA MCP `idb_list`, `server_health`, `lookup_funcs`, `get_bytes`, `decompile`, `analyze_function`, and `xrefs_to` against database `9b0396a3`.
- Secondary evidence: exact target/sibling/owner/support by-* pages, current generated C++, current tracker, manual coverage roots, current leases/goals, and matching executed reports.
- Corroboration ladder: exact bytes/range; target decompilation/CFG; direct xrefs and constructor callback; sibling symmetry; callee assembly/signatures; current class/layout docs; generated and old reports as leads.
- Confidence is not raised to `95+` because original debug names are stripped and `IsEnabled`, local button names, and exact access sections remain strongly inferred rather than symbol-proven.

## Evidence Checked
- IDA MCP checks: fresh `idb_list`; `server_health`; bounded target bytes; `lookup_funcs` at `0x0052b1d0`, `0x0052b090`, `0x0052b30d`, and `0x0052b310`; target decompile/analyze; target xrefs; sibling decompile/analyze; `0x004fd070` and `0x004fd0d0` decompile/assembly; `0x004fd140`, `0x00494bb0`, and `0x00494bd0` behavior; constructor/caller routes.
- Ordinary docs checked: UID00043N, UID00043M, UID00009E, UID0000LW, UID0002QR, UID0002Q8, UID0000FR, UID0000OX, UID000038, UID0000IG, and UID00003T.
- Generated/tracker checked: `NewUserDialogPane2.cpp`, `UserCreateAppearanceControls.cpp`, `ControlPane.cpp`, and `-ag-research-tracker.md` command/header/target row.
- Reports checked after exact-term search: executed B004 UID0002QR, executed B003 UID0004GQ, B014 UID0004E5, and B015 UID0004E6. Active, Older-Research, legacy-executed, and archive-root outcomes are recorded under Supporting Research.
- Manual roots checked read-only: by-memory, by-class, and by-file coverage rows for every recommended destination; exact hashes and handoff text appear below.
- Negative checks: no extra target xrefs, no target strings, no allocation/free/EH, no null/bounds check, no target notification, no target Empty Emitter Marker, no need for a new child, no active B001-B005 collision, and no live lease.
- At the report-only evidence checkpoint, no IDA mutation, validator, lease, ordinary edit, generated edit, coverage edit, or lifecycle action had been performed. The later authorized callback used only the ordinary leases/validators recorded below; B002 still made no direct generated/coverage/tracker/audit/supervisor/validator-state/IDA/lifecycle edit.

## Claim And Incorporation Ledger
| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C01 | UID00043N is exact source code `0x0052b1d0-0x0052b30d`, 317 bytes, SHA `CEE065...F41`. | 99 | target SHA `D699...EA9`; full evidence retained | D1 target | incorporate | applied |
| C02 | Five predecessor and three successor `0xcc` bytes are padding; successor starts `0x0052b310`. | 98 | target range/boundary prose; UID2QR SHA `135C...4FF` | D1, D7 | incorporate | applied |
| C03 | Target has exactly two code callers and one constructor callback-data xref. | 99 | target/class/file caller inventories | D1, D3, D8 | incorporate | applied |
| C04 | Source signature is `void` no-argument member despite IDA return residue. | 97 | D1/D3 exact formals; generated right definition count 1 | D1, D3 | incorporate | applied |
| C05 | UID00009E is semantic owner/emitter; UID0000LW is source file. | 98 | retained target/class/file metadata and generated route | D1, D3, D8 | already-present | already-present |
| C06 | `+0x1fc` is inherited DialogPane lookup state; derived duplicate field is invalid. | 98 | D3/D7-D8 prose; generated derived-manager count 0 | D3, D7-D8 | reject-stale | applied |
| C07 | Child ids 8-11 are shape controls; 6 previous; 7 next. | 98 | D1/D2 bodies and target/support prose | D1-D3 | incorporate | applied |
| C08 | `+0x274` is 32-bit `m_selectedShapeSlot`. | 98 | retained D1/D3/D7-D8 field evidence | D1, D3, D7-D8 | already-present | already-present |
| C09 | Non-edge path deselects, increments, selects, returns. | 99 | D1 byte-equal; generated right body count 1 | D1 | incorporate | applied |
| C10 | Edge path is gated solely by next button `m_enabled` at `+0x101`. | 99 | D1 plus D4 inline getter; generated accessor count 1 | D1, D4 | incorporate | applied |
| C11 | Right-edge step order is control 11 then 8, 9, 10. | 99 | D1 and generated lines 114+ readback | D1 | incorporate | applied |
| C12 | First result `-1` disables next; otherwise previous is enabled. | 99 | D1/D4 exact formals and generated readback | D1, D4 | incorporate | applied |
| C13 | Target has no null/bounds check, notification, allocation, cleanup, or direct packet work. | 99 | complete target prose and formal exclusions | D1, D8 | incorporate | applied |
| C14 | Command case 7 calls target then separately notifies selected shape id 8. | 97 | target/class/file caller evidence retained | D1, D3, D8 | incorporate | applied |
| C15 | Key handler is a second live direct caller and performs no target-internal notification. | 97 | target/class/file caller evidence retained | D1, D3, D8 | incorporate | applied |
| C16 | `ShiftShapesRight` is the highest-probability source name. | 96 | retained metadata/formal/generated name | D1, D3 | already-present | already-present |
| C17 | Stack cookie/epilogue are compiler output, not handwritten source. | 99 | D1/D7-D8 exclusions; generated cookie count 0 | D1, D7-D8 | incorporate | applied |
| C18 | StepShapeForward/Backward return `short`, not `int`. | 99 | D5/D6 byte-equal; generated short declaration/definition counts 2 each, int count 0 | D5-D6, D9 | reject-stale | applied |
| C19 | Invalid step states fall through without a defined source return; current fallback changes behavior. | 96 | D6 exact bodies and generated readback | D5-D6, D9 | reject-invalid | applied |
| C20 | SetSelected writes `+0x116` only on change and invalidates. | 99 | retained D1/D5-D6 body/evidence | D1, D5-D6 | already-present | already-present |
| C21 | Inline `ControlPane::IsEnabled() const` is the source-safe direct-read representation. | 94 | D4 byte-equal; generated inline 1/out-of-line 0 | D4, D10 | incorporate | applied |
| C22 | Destination 1 is behavior-identical target formal C++. | 97 | accepted/report/target block byte equality; command 16407 readback | D1 | incorporate | applied |
| C23 | Symmetric UID00043M had the same stale API/body defect; Destination 2 is the accepted correction. | 97 | D2 byte-equal; validator 16387; generated left count 1 | D2, D3 | incorporate | applied |
| C24 | NewUserDialogPane2 class must close before `[[CHILDREN]]` and omit duplicate manager storage. | 98 | D3 byte-equal; generated close line 41/first definition 44/manager count 0 | D3 | incorporate | applied |
| C25 | UserShapeSelectControlPane needs a complete `0x140` declaration before UID0002Q8 bodies. | 96 | D5 byte-equal; generated close line 44/core marker 46/no empty marker | D5 | incorporate | applied |
| C26 | UID0002Q8 formal needs period-plausible constants, legal base-member assignment, and short step bodies. | 96 | D6 byte-equal; validators 16394/16406; generated readback | D6 | incorporate | applied |
| C27 | Aggregate and file prose must historicalize the simplistic shift model and duplicate manager alias. | 98 | UID2QR/UID0LW/UID0OX/UID0IG final hashes | D7-D10 | historicalize | applied |
| C28 | Generated output is verification evidence, not source authority; waited refresh proves the repaired union. | 99 | historical command `000000016407`; supervisor handoff command `000000016423`; bounded final-read command `000000016426`; three current volatile hashes and unchanged semantic counts | D1-D6 validator plan | incorporate | applied |
| C29 | Target and left become `92/94`; class becomes `92/94`; UserShape class/core become `92/93` and `91/94`; other scores stay. | 94 | current metadata plus scoped validators | metadata destinations | incorporate | applied |
| C30 | Ten exact no-loss manual row actions are supervisor-owned; current union and coordination must be preserved. | 99 | supervisor commands `000000016420`-`000000016423`; all ten exact rows occur once; current manual hashes recorded; B002 made no coverage edit | coverage handoff | incorporate | applied |

## Positive Evidence Summary
- Exact target bytes, stable function boundary, complete decompilation, and two code callers prove a real source-authored method.
- Constructor address use at `0x0052a994` is stronger than ordinary call reachability: it binds the target into a no-argument member callback used by the dialog's right button.
- Target and sibling agree on child ids, selected-slot state, edge gating, step order, sentinel interpretation, and reciprocal button updates.
- Direct callee bodies resolve `StepShapeForward`, `StepShapeBackward`, `SetSelected`, `Enable`, and `Disable`; current accepted DialogPane declaration resolves typed lookup.
- The inference chain is sufficient because no remaining unknown changes control flow, side effects, ABI-visible parameter shape, source ownership, or formal body.

## IDA MCP Facts
- Function/range: `sub_52B1D0`, start `0x0052b1d0`, size `0x13d`/317, seven basic blocks, cyclomatic complexity `2`, no strings, direct callees `sub_4FD070`, `sub_4FD140`, and compiler security-cookie check.
- Bytes/hash: full range SHA `CEE065413412B5AA55F2074F4A8256704BA7A176417C370533D75C5EB17E6F41`.
- Xrefs: `0x0052b7e7` from `sub_52B760`, `0x0052b8f5` from `sub_52B890`, and constructor data use `0x0052a994` from `sub_52A540`; exactly three.
- State: inherited manager pointer at `this+0x1fc`; selected slot at `this+0x274`; next enabled byte at control `+0x101`; selected flag at shape control `+0x116`; selected shape index word at `+0x10c`.
- Calls: manager vslot `+0x10`; ControlPane Enable vslot `+0x4c`; Disable vslot `+0x50`; StepShapeForward at `0x004fd070`; SetSelected at `0x004fd140`.
- Step ABI: `0x004fd070` is `__int16 __thiscall`, 89 bytes, five blocks; `0x004fd0d0` is `__int16 __thiscall`, 86 bytes, five blocks. Both return `-1` only on the terminal transition and omit a defined invalid-state return.
- Padding: five `0xcc` before target and three `0xcc` after target; next modeled function is `0x0052b310`.

## Function / Child Inventory
| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x0052b1d0-0x0052b30d` | UID00043N target | right shift callback | true | UID00009E | `92/94` | applied; Destination 1 byte-equal |
| `0x0052b090-0x0052b1cb` | UID00043M | symmetric left shift callback | true | UID00009E | `92/94` | applied; Destination 2 byte-equal |
| class | UID00009E | dialog declaration/layout | true | UID0000LW | `92/94` | applied; no duplicate field; class closes before children |
| aggregate | UID0002QR | exact executable index | false | UID0000LW | `89/90` | synchronized prose/index only; no duplicate source |
| class | UID0000FR | old shape-control declaration | true | UID0000OX | `92/93` | applied declaration; generated empty marker removed |
| `0x004fce80-0x004fd520` | UID0002Q8 | shape-control source core | true | UID0000FR | `91/94` | applied short ABI/source fallthrough at position 10 |
| class | UID000038 | base control declaration | true | UID0000IG | `91/93` | applied inline enabled getter only |
| class | UID00003T | DialogPane typed lookup | true | UID0000IT | `92/94` | verify-only, already sufficient |
| file | UID0000LW | NewUserDialogPane2 source | file root | FILE | `92/94` | applied include/order/prose sync |
| file | UID0000OX | appearance controls source | file root | FILE | `92/93` | applied declaration/core prose sync |
| file | UID0000IG | ControlPane source | file root | FILE | `90/92` | applied getter prose sync |

## Direct Xref / Caller Inventory
| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0052a994` | constructor data xref | member callback target stored for right PrettyButton; proves void/no-arg liveness |
| `0x0052b7e7` | OnCommand case 7 call | invokes target, then caller performs selected-shape notification |
| `0x0052b8f5` | key-event right call | invokes target directly from keyboard path |
| `0x004fd070` | StepShapeForward | 16-bit selected-index step and terminal `-1` sentinel |
| `0x004fd140` | SetSelected | changed-only selected-byte store and invalidation |
| ControlPane vslot `+0x4c` | Enable | enables previous button only after a nonterminal right shift |
| ControlPane vslot `+0x50` | Disable | disables next button at terminal right edge |

## Documentation Evidence And IDA Status
- Evidence-time/pre-callback UID00043N and UID00043M preserved correct broad role/name/ownership but had superseded formal bodies. Current pages are `92/94`, carry Destinations 1-2 byte-for-byte, and passed validators `000000016386`/`000000016387`.
- Evidence-time UID00009E treated inherited `+0x1fc` as a derived field and placed `[[CHILDREN]]` inside the class. Current `92/94` page carries Destination 3, has no duplicate manager field, closes before children, and passed validator `000000016388`.
- UID00003T already supplies `GetChildControl(int) const` and typed `GetChild<T>(int) const`; no DialogPane edit is needed.
- UID000038 retains exact `m_enabled` at `+0x101` and Enable/Disable virtuals and now includes the public inline getter for legal dialog use; validators `000000016389`/`000000016404` record class/file support.
- Evidence-time UID0000FR had a blank declaration and UID0002Q8 used wrong int/fallback step semantics. Current class/core are `92/93` and `91/94`, positions `0/10`, carry Destinations 5-6 byte-for-byte, and passed validators `000000016392`, `000000016394`, and exact-block correction validator `000000016406`.
- Historical B002 callback command `000000016407` established generated UID00043N and UID00043M one-definition source, complete UID00009E/UID0000FR declarations, short steps, inline enabled getter, zero target/class empty markers, and zero handwritten ABI/decompiler artifacts. Supervisor handoff command `000000016423` preserved those assertions, and bounded final-read command `000000016426` is the later volatile external checkpoint with the same semantic result.

## Ranked Ownership Analysis

### 1. NewUserDialogPane2 class / NewUserDialogPane2 source
- Evidence for: target receiver, own `+0x274` field, constructor callback, two class callers, address cluster, sibling, class declarations, and current owner/emitter all agree.
- Evidence against: no debug symbol preserves exact spelling; this affects lexical confidence only.
- Decision: retain UID00009E owner/emitter and UID0000LW file route.

### 2. UserShapeSelectControlPane
- Evidence for: owns StepShapeForward and SetSelected callees and their mutable shape state.
- Evidence against: target itself owns dialog slot selection, child lookup, navigation buttons, and callback/caller routes.
- Decision: dependency/support owner only, not target owner.

### 3. DialogPane or file-local free helper
- Evidence for: DialogPane owns child lookup; a free helper could technically receive the dialog.
- Evidence against: constructor binds a member-function pointer, target uses derived own state, and source family declares paired methods.
- Decision: reject as direct owner/source shape.

### Proposed new file/grouping, if applicable
- Not applicable. Existing NewUserDialogPane2.cpp and UserCreateAppearanceControls.cpp routes are sufficient; no new file, child, or report is proposed.

## Source Placement
- Place UID00043N and UID00043M as protected helper definitions in `NexusTK/login/NewUserDialogPane2.cpp`, declared by NewUserDialogPane2.
- Keep UserShapeSelectControlPane declaration and core in the existing UserCreateAppearanceControls source/header family. NewUserDialogPane2's source/header should consume that declaration rather than redeclare the shape control.
- Keep `IsEnabled` inline on ControlPane's declaration, matching the direct byte read and avoiding a new out-of-line function absent from the binary.
- Reject placement in DialogPane, PrettyButton, the aggregate index, UserShapeSelectControlPane source as target owner, or a raw address-named helper file.
- No source-placement uncertainty affects code emission.

## Range / Split / Padding / Reclassification Analysis
- Target remains exact `0x0052b1d0-0x0052b30d`; no internal table/data/padding exists.
- UID00043M remains exact `0x0052b090-0x0052b1cb`; five-byte alignment separates it from UID00043N. Three-byte alignment separates UID00043N from UID00043O at `0x0052b310`.
- UID0002QR remains non-emitting semantic index; exact children carry source. No new child or range rename is needed.
- Stack-cookie instructions are compiler scaffolding within the target range, regenerated from the local pointer array rather than split into source.
- NewUserDialogPane2 own fields begin at `+0x26c`; inherited DialogPane storage through `+0x26b` includes the `+0x1fc` manager. Natural implicit padding after bool `+0x278` precedes SimpleUString at `+0x27c`.
- UserShapeSelectControlPane is `0x140`: ControlPane base through `+0x107`, seven 16-bit fields through `+0x115`, bool `+0x116`, implicit one-byte alignment, and two 10-word tables `+0x118..+0x13f`.

## Negative Evidence Summary
- No target strings, packets, resources, allocations, frees, EH cleanup, null checks, bounds checks, or direct notifications exist.
- No target-owned vtable slot is required; liveness comes from a member callback and direct callers.
- Consumer calls into UserShapeSelectControlPane do not transfer target ownership.
- The B004 body is not accepted merely because it generated once; binary call order and APIs disprove it.
- The direct `+0x101` read does not justify exposing `m_enabled` publicly or adding friendship; an inline getter is narrower.
- Partial class/source declarations are rejected because they leave the current qualified bodies source-incomplete.
- No active lease or B001-B005 scope collision was found.

## IDA Rename / Type / Comment Recommendations
- Source-facing target: `void NewUserDialogPane2::ShiftShapesRight()`; retain current name.
- Source-facing sibling: `void NewUserDialogPane2::ShiftShapesLeft()`; retain current name.
- Source-facing dependencies: `short UserShapeSelectControlPane::StepShapeForward()`, `short StepShapeBackward()`, `void SetSelected(bool)`, `bool ControlPane::IsEnabled() const`, `ControlPane *previousButton`, `ControlPane *nextButton`, and `UserShapeSelectControlPane *shapeControls[4]`.
- Preserve `m_selectedShapeSlot` at `+0x274`; historicalize derived `m_childControls` at `+0x1fc` as the inherited DialogPane manager/typed lookup route.
- IDA's `char` target return is rejected as residue; `__int16` step return is accepted. Decompiler `_WORD`, `_BYTE`, raw vtable calls, `v*` locals, and `sub_*` names must not reach source.
- No IDA DB edit is requested or authorized. These are documentation/source recommendations only.

## First-Draft C++ Recommendation
- Six managed destinations are required. The text below is the exact destination-specific formal insertion text; no C++ draft exists outside these blocks.

### Destination 1 - UID00043N target
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewUserDialogPane2::ShiftShapesRight()
{
    UserShapeSelectControlPane *shapeControls[4];

    for (int i = 0; i < 4; ++i) {
        shapeControls[i] = GetChild<UserShapeSelectControlPane>(8 + i);
    }

    ControlPane *previousButton = GetChild<ControlPane>(6);
    ControlPane *nextButton = GetChild<ControlPane>(7);

    if (m_selectedShapeSlot != 3) {
        shapeControls[m_selectedShapeSlot]->SetSelected(false);
        ++m_selectedShapeSlot;
        shapeControls[m_selectedShapeSlot]->SetSelected(true);
        return;
    }

    if (!nextButton->IsEnabled()) {
        return;
    }

    short edgeResult = shapeControls[3]->StepShapeForward();
    shapeControls[0]->StepShapeForward();
    shapeControls[1]->StepShapeForward();
    shapeControls[2]->StepShapeForward();

    if (edgeResult == -1) {
        nextButton->Disable();
    } else {
        previousButton->Enable();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 2 - UID00043M symmetric support
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewUserDialogPane2::ShiftShapesLeft()
{
    UserShapeSelectControlPane *shapeControls[4];

    for (int i = 0; i < 4; ++i) {
        shapeControls[i] = GetChild<UserShapeSelectControlPane>(8 + i);
    }

    ControlPane *previousButton = GetChild<ControlPane>(6);
    ControlPane *nextButton = GetChild<ControlPane>(7);

    if (m_selectedShapeSlot != 0) {
        shapeControls[m_selectedShapeSlot]->SetSelected(false);
        --m_selectedShapeSlot;
        shapeControls[m_selectedShapeSlot]->SetSelected(true);
        return;
    }

    if (!previousButton->IsEnabled()) {
        return;
    }

    short edgeResult = shapeControls[0]->StepShapeBackward();
    shapeControls[1]->StepShapeBackward();
    shapeControls[2]->StepShapeBackward();
    shapeControls[3]->StepShapeBackward();

    if (edgeResult == -1) {
        previousButton->Disable();
    } else {
        nextButton->Enable();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 3 - UID00009E class declaration
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
class NewUserDialogPane2 : public DialogPane
{
public:
    NewUserDialogPane2();
    virtual ~NewUserDialogPane2();

protected:
    virtual bool OnCommand(int commandId);
    virtual bool HandleShapeKeyEvent(const KeyEvent &event);
    virtual bool HandleCreateUserPacketEvent(const unsigned char *packet);
    virtual bool ForwardDialogNotify(DialogEvent &event);

    void ShiftShapesLeft();
    void ShiftShapesRight();
    void NotifySelectedShapeChanged();
    void SelectNation(int nation);
    void SelectTotem(int totem);
    void SelectShapeSlot(int slot);
    void SelectMaleMode();
    void SelectFemaleMode();
    void PromptCreateCharacterName();
    void ApplyCreateCharacterName(const SimpleUString &name);

    void SubmitCreateUser();
    void SendCreateCharacterRequest();
    bool HandleCreateUserReply(const unsigned char *packet);

private:
    int m_selectedNation;
    int m_selectedTotem;
    int m_selectedShapeSlot;
    bool m_waitingForCharacterReply;
    SimpleUString m_createdUserName;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 4 - UID000038 ControlPane declaration
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "Pane.h"

struct RectBounds;

class ControlPane : public Pane
{
public:
    ControlPane(unsigned char controlType, const RectBounds *bounds);
    virtual ~ControlPane() {}

protected:
    virtual void SetVisualState(unsigned char visualState);

public:
    virtual void Enable();
    virtual void Disable();
    bool IsEnabled() const { return m_enabled; }
    bool IgnoresHitTesting() const;
    virtual void SetIgnoreHitTesting(bool ignoreHitTesting);

protected:
    virtual void Deactivate();
    virtual void Activate();
    virtual unsigned char HitTestPart(int, int)
    {
        return 10;
    }

    short m_rangeStart;
    short m_rangeCurrent;
    short m_rangeEnd;
    unsigned char m_controlType;
    unsigned char m_controlFlags;
    unsigned char m_reservedControlState;
    bool m_enabled;
    bool m_controlActive;
    unsigned char m_visualState;
    bool m_ignoreHitTesting;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 5 - UID0000FR UserShapeSelectControlPane declaration
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct MouseEvent;
struct RectBounds;
class HumanImageRenderParams;

class UserShapeSelectControlPane : public ControlPane
{
public:
    UserShapeSelectControlPane(short initialShapeIndex,
                               bool useMaleShapeBank,
                               const RectBounds *bounds,
                               unsigned char controlFlags);
    virtual ~UserShapeSelectControlPane() {}

    virtual bool OnTimerEvent(int timerId, int, int);
    virtual bool OnMouseEvent(const MouseEvent *event);
    virtual void OnPaint();

    HumanImageRenderParams *BuildPreviewParams(
        HumanImageRenderParams *outParams) const;
    void SetMaleShapeBank();
    void SetFemaleShapeBank();
    short StepShapeForward();
    short StepShapeBackward();
    bool IsSelected() const;
    void SetSelected(bool selected);

private:
    unsigned short m_reservedShapeState;
    unsigned short m_shapeIndexLimit;
    short m_selectedShapeIndex;
    unsigned short m_shapeBank;
    unsigned short m_animationFrame;
    unsigned short m_direction;
    unsigned short m_animationTick;
    bool m_selected;
    unsigned short m_shapeSpriteIds[2][10];
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### Destination 6 - UID0002Q8 exact core
```text
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace
{
const unsigned char kUserShapeControlType = 8;
const unsigned char kInitialShapeDirection = 2;
const unsigned short kOldUserShapeLastIndex = 9;
const unsigned short kOldUserShapeForwardEdge = 8;
const unsigned short kOldUserShapeTimerMs = 200;
const unsigned short kOldUserShapeDirectionTickLimit = 16;
const unsigned char kControlPaneVisualStateNormal = 10;
const unsigned char kShapePreviewDescriptorKind = 0x50;

const unsigned short kUserShapeSpriteIds[2][10] = {
    {2, 60, 1, 8, 34, 48, 80, 36, 3, 83},
    {33, 74, 78, 50, 91, 10, 31, 4, 7, 45},
};

int CenterLow(int outerLow, int outerHigh, int innerLow, int innerHigh)
{
    return (outerLow + outerHigh) / 2 - (innerHigh - innerLow) / 2;
}

void CenterRectInBounds(RectBounds *outRect,
                        const RectBounds &outerRect,
                        const RectBounds &sourceRect)
{
    const int sourceLeft = sourceRect.left;
    const int sourceTop = sourceRect.top;
    const int sourceRight = sourceRect.right;
    const int sourceBottom = sourceRect.bottom;
    const int width = sourceRight - sourceLeft;
    const int height = sourceBottom - sourceTop;

    outRect->left = CenterLow(outerRect.left, outerRect.right,
                              sourceLeft, sourceRight);
    outRect->top = CenterLow(outerRect.top, outerRect.bottom,
                             sourceTop, sourceBottom);
    outRect->right = outRect->left + width;
    outRect->bottom = outRect->top + height;
}
}

UserShapeSelectControlPane::UserShapeSelectControlPane(short initialShapeIndex,
                                                       bool useMaleShapeBank,
                                                       const RectBounds *bounds,
                                                       unsigned char controlFlags)
    : ControlPane(kUserShapeControlType, bounds),
      m_reservedShapeState(0),
      m_shapeIndexLimit(kOldUserShapeLastIndex),
      m_selectedShapeIndex(initialShapeIndex),
      m_shapeBank(useMaleShapeBank ? 0 : 1),
      m_animationFrame(0),
      m_direction(kInitialShapeDirection),
      m_animationTick(0),
      m_selected(false)
{
    m_controlFlags = controlFlags;

    for (int bank = 0; bank < 2; ++bank) {
        for (int index = 0; index < 10; ++index) {
            m_shapeSpriteIds[bank][index] = kUserShapeSpriteIds[bank][index];
        }
    }

    ScheduleTimer(0, kOldUserShapeTimerMs, 0, 0);
}

HumanImageRenderParams *UserShapeSelectControlPane::BuildPreviewParams(
    HumanImageRenderParams *outParams) const
{
    outParams->Reset();
    outParams->isFemale = (m_shapeBank != 0);
    outParams->shapeIndex = static_cast<unsigned char>(
        m_shapeSpriteIds[m_shapeBank][m_selectedShapeIndex]);
    outParams->bodyMode = static_cast<unsigned char>(m_shapeBank);
    outParams->paletteOrFrame = -1;
    return outParams;
}

void UserShapeSelectControlPane::SetMaleShapeBank()
{
    m_shapeBank = 0;
    InvalidateBounds();
}

void UserShapeSelectControlPane::SetFemaleShapeBank()
{
    m_shapeBank = 1;
    InvalidateBounds();
}

short UserShapeSelectControlPane::StepShapeForward()
{
    if (m_selectedShapeIndex < kOldUserShapeForwardEdge) {
        ++m_selectedShapeIndex;
        InvalidateBounds();
        return m_selectedShapeIndex;
    }

    if (m_selectedShapeIndex == kOldUserShapeForwardEdge) {
        m_selectedShapeIndex = kOldUserShapeLastIndex;
        InvalidateBounds();
        return -1;
    }
}

short UserShapeSelectControlPane::StepShapeBackward()
{
    if (m_selectedShapeIndex > 1) {
        --m_selectedShapeIndex;
        InvalidateBounds();
        return m_selectedShapeIndex;
    }

    if (m_selectedShapeIndex == 1) {
        m_selectedShapeIndex = 0;
        InvalidateBounds();
        return -1;
    }
}

bool UserShapeSelectControlPane::IsSelected() const
{
    return m_selected;
}

void UserShapeSelectControlPane::SetSelected(bool selected)
{
    if (m_selected == selected) {
        return;
    }

    m_selected = selected;
    InvalidateBounds();
}

bool UserShapeSelectControlPane::OnTimerEvent(int timerId, int, int)
{
    if (timerId == 0) {
        m_animationFrame = static_cast<unsigned short>((m_animationFrame + 1) % 4);
        ++m_animationTick;

        if (m_animationTick >= kOldUserShapeDirectionTickLimit) {
            m_animationTick = 0;
            m_direction = static_cast<unsigned short>((m_direction + 1) % 4);
        }

        InvalidateBounds();
        ScheduleTimer(0, kOldUserShapeTimerMs, 0, 0);
    }

    return true;
}

bool UserShapeSelectControlPane::OnMouseEvent(const MouseEvent *event)
{
    if (event != NULL &&
        event->type == MouseEvent::ButtonDown &&
        PointInRect(event->mouseY, event->mouseX, &m_bounds) &&
        !m_selected)
    {
        m_selected = true;
        InvalidateBounds();
    }

    return true;
}

void UserShapeSelectControlPane::OnPaint()
{
    EPFTileContext frameContext;
    RectBounds frameRect;
    RectBounds drawRect;
    HumanImageRenderParams params;

    frameContext.Initialize();
    SetDrawColor(0);
    FillRect(&m_bounds);

    if (m_selected || m_visualState != kControlPaneVisualStateNormal) {
        const wchar_t *frameResource = g_useEpfAssets == 1
            ? L"USERPART.EPF"
            : L"CAPSULE.EPD";
        const wchar_t *paletteResource = g_useEpfAssets == 1
            ? L"NEWBUTT.PAL"
            : L"NPAL3.PAL";

        g_pEPFLib->LookupLayoutEntry(frameResource, 0, &frameContext);

        const RectBounds &sourceRect = frameContext.bounds;
        CenterRectInBounds(&frameRect, m_bounds, sourceRect);

        RenderTileFrame(&frameContext, &sourceRect, &frameRect, 0, paletteResource, 0);
    }

    BuildPreviewParams(&params);
    params.kind = kShapePreviewDescriptorKind;
    g_pNewHumanImageLib->CalculateBounds(&params, m_direction, &drawRect);

    CenterRectInBounds(&drawRect, m_bounds, drawRect);

    BuildPreviewParams(&params);
    params.kind = kShapePreviewDescriptorKind;
    g_pNewHumanImageLib->Draw(this, &drawRect, &params, m_direction);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- These blocks preserve the exact observed target behavior and the plausible Visual C++ source shape. `const` namespace data, simple class methods, inline accessor, stack array, implicit padding, and intentionally absent invalid-state return are period-plausible.
- No target-specific no-code proof applies: UID00043N and all five formal support destinations are source-bearing and defensibly reconstructable.

## Final Recommendation
- Destinations 1-6 are applied byte-for-byte after the supervisor Gate 1 pass.
- UID00043N retains owner/emitter `00009E`, reconstructable true, blank position, `Nested:0`, and is now `92/94`.
- The paired left helper is synchronized as bounded support at `92/94`; no duplicate report or source body was created.
- Keep UID0002QR non-emitting and UID00003T verify-only. Do not emit aggregate C++, raw vtable calls, stack cookies, or new wrappers.
- Exact range/hash/CFG/xref/caller/callee/field/control/order/negative/history facts are incorporated into target, class, aggregate, and file support pages.
- All scoped validators and generated semantic checks passed; B002 holds zero leases. After ordinary Gate 2, the supervisor applied and validated the exact ten-row manual handoff under commands `000000016420`-`000000016423`. Report lifecycle actions remain supervisor-owned.

## Recommended Target Doc Changes
- Target path: `by-memory/0x0052b1d0-0x0052b30d.NewUserDialogPane2ShiftShapesRight.md`.
- Applied completion/confidence `92/94`; retained owner/emitter/reconstructable/blank position/Nested.
- Installed formal C++ byte-for-byte with Destination 1.
- Incorporated exact bytes/hash, boundary/padding, seven-block CFG, all three xrefs, callback signature, control ids, offset meanings, non-edge/edge branches, step order, sentinel/button behavior, caller notification separation, no-check/no-allocation facts, compiler-cookie exclusion, source ownership, rejected alternatives, and historical B004 body disposition. Final SHA is `D699512D5FEA3A0989BD87689E775004699FEFDB6AEFD4B7665B11E069126EA9`.

## Recommended Support Doc Changes
- Applied UID00043M `92/94`, Destination 2, exact symmetric behavior and history.
- Applied UID00009E `92/94`, Destination 3, inherited manager correction, exact own layout, caller/callback evidence, class closure before children, and preserved unrelated methods/history.
- Preserved UID000038 `91/93`, owner/emitter/position/layout; Destination 4 adds only inline `IsEnabled` and target consumer evidence.
- Applied UID0000FR `92/93`, Destination 5, exact `0x140` layout, method/access/type inventory, implicit padding, and source route.
- Applied UID0002Q8 `91/94`, position `10`, Destination 6, short ABI, invalid-state source fallthrough, legal inherited member assignment, period constants, and preserved complete core evidence.
- Preserved UID0002QR `89/90`, NONE/FALSE/blank formal; synchronized index prose for corrected target/sibling and inherited manager disposition.
- Applied UID0000LW `92/94` with complete shift behavior, source/header dependencies, method ordering, compiler exclusions, and superseded B004 model.
- Preserved UID0000OX `92/93` while adding complete UserShape declaration/core ordering and short-step contract.
- Preserved UID0000IG `90/92` while adding inline enabled getter source role and direct target consumer evidence.
- Verified UID00003T read-only and byte-identical at SHA `7101A93A4EF53CFA1F11529AEF111AA94B13EBC76222ADE546D7B59D6D521DBE`; its typed `GetChild` API remains same-or-greater.

## Score And Metadata Recommendation
- UID00043N: applied `92/94`; owner/emitter `00009E`, reconstructable true, blank position, Nested 0 unchanged.
- UID00043M: applied `92/94`; same metadata unchanged.
- UID00009E: applied `92/94`; owner/emitter `0000LW`, reconstructable true, blank position unchanged.
- UID0000FR: applied `92/93`; owner/emitter `0000OX`, reconstructable true, position `0`.
- UID0002Q8: applied `91/94`; owner/emitter `0000FR`, reconstructable true, position `10`.
- UID000038, UID0000IG, UID0000OX, UID0002QR, and UID00003T keep current scores.
- Target blockers removed: complete liveness, callback signature, all branches, exact control identities, types, source APIs, ownership, range, compiler split, formal C++, class/access dependencies, and generated verification plan.
- Scores are not higher because original lexical names/access spellings are stripped and broader unrelated NewUserDialogPane2 methods remain at their accepted quality rather than being re-researched here.

## Open Questions With Attempted Resolution
- **Is target return char?** No. Constructor member-callback shape and caller behavior resolve void; IDA's char is residue.
- **Is the right edge a wrap/rotation?** It advances all four displayed shape indexes only when selected slot is 3 and next is enabled; it does not wrap selected slot.
- **Which result controls navigation?** The first call on control 11; exact compare and sibling symmetry resolve this.
- **Why read `+0x101` directly?** It is inherited ControlPane enabled state. Inline `IsEnabled` is the source-safe representation.
- **Should a fallback return be added to step methods?** No. Exact assembly preserves undefined AX on invalid state, while callers prevent the path. A fallback would change behavior.
- **Does `+0x1fc` belong to NewUserDialogPane2?** No. DialogPane owns it; the derived duplicate is historical documentation pollution.
- **Must target notify?** No. Command caller performs notification after the call; key path does not add one.
- **Is a new source file/child needed?** No. Existing exact pages and file roots cover every source-bearing unit.
- No investigable question remains that blocks score, metadata, ownership, source placement, or C++.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text
- Historical pre-coverage snapshots were by-memory SHA `85AD82FC4D1E579255B06B4EA26813C8651A0AA24E6BB8D0D26D92AEC3E3CBF7`, by-class SHA `B9A93C85C1CE2DDE5C3F76C2FC6985F2C0DBE6D37384A1F9601CCB9BC83B7570`, and by-file SHA `35EA6391EF8C89256572B6CFCDD81256A6DAA7D5C14545B8AFFC1EB94336DDC3`; at that checkpoint the handoff was unapplied. That state is superseded but retained as history.
- Supervisor handoff-final command `000000016423` recorded by-memory SHA `755F5188551AC818573B76EF0710F77AA66893361B03F3B395B89E955E5609F8`, `1,957,659` bytes/`4,530` lines; by-class SHA `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, `253,106/623`; and by-file SHA `C21CDF487AFE771EF013A09BE6C24AA299A47DF48DF26B9C81B36A7A93C0DB5F`, `150,592/317`. A bounded final read after unrelated supervisor commands `000000016424`-`000000016426` observed by-memory SHA `94434F13116AE98DAFA5389C85EB21E03217184CB821AE09226129290CBC1AB1`, `1,958,001/4,530`; unchanged by-class SHA `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, `253,106/623`; and by-file SHA `2BF7C218FCF6760E10CC73E9992618491D04A680AF4962E54CFDF33BD2CF85C2`, `150,805/317`.
- Exact current anchors are by-memory UID0002Q8 line `2130`, UID0002QR line `2574`, UID00043M line `2575`, UID00043N line `2576`; by-class UID000038 line `141`, UID00009E line `361`, UID0000FR line `588`; by-file UID0000IG line `60`, UID0000LW line `185`, UID0000OX line `294`. Every accepted row occurs exactly once.
- The following ten entries preserve the exact no-loss handoff text applied externally by the supervisor. The numbered replace/insert labels are historical instructions documenting placement; commands `000000016420`-`000000016423` completed and validated them, and B002 did not edit coverage.

1. Replace by-memory UID0002QR row with:
```text
        - [UID:0002QR][0x0052a540-0x0052c325.NewUserDialogPane2CreateUserVariant](by-memory/0x0052a540-0x0052c325.NewUserDialogPane2CreateUserVariant.md) 0x0052a540-0x0052c325 | executable child/index | NewUserDialogPane2CreateUserVariant : reconstructable : 89% : very-strong : Exact alternate 640x480 create-user dialog split/index with constructor, complete-destructor evidence, left/right shape shifts, nation/totem/shape/gender selectors, name prompt/apply path, command/key/packet/notify bridges, account/character submit and packet/reply paths, singleton/vtable evidence, exact child boundaries/padding, inherited DialogPane control-manager route, corrected selected-state/edge-button behavior, source-emitting exact children, and compiler/raw no-code exclusions; historical broad aggregate and derived m_childControls assumptions are superseded.
```
2. Insert by-memory UID00043M row after UID0002QR:
```text
            - [UID:00043M][0x0052b090-0x0052b1cb.NewUserDialogPane2ShiftShapesLeft](by-memory/0x0052b090-0x0052b1cb.NewUserDialogPane2ShiftShapesLeft.md) 0x0052b090-0x0052b1cb | class method | NewUserDialogPane2::ShiftShapesLeft : reconstructable : 92% : very-strong : Exact void no-argument left-button callback and key/command helper: inherited typed lookup of shape controls 8-11 and previous/next controls 6/7, non-edge deselect/decrement/select, previous-enabled edge gate, StepShapeBackward order 8 then 9/10/11, terminal -1 disables previous, nonterminal result enables next, exact range/padding/callers/compiler-cookie exclusions, and superseded SelectShapeSlot/ShiftShapeLeft draft preserved historically.
```
3. Insert by-memory UID00043N row after UID00043M:
```text
            - [UID:00043N][0x0052b1d0-0x0052b30d.NewUserDialogPane2ShiftShapesRight](by-memory/0x0052b1d0-0x0052b30d.NewUserDialogPane2ShiftShapesRight.md) 0x0052b1d0-0x0052b30d | class method | NewUserDialogPane2::ShiftShapesRight : reconstructable : 92% : very-strong : Exact 317-byte void no-argument right-button callback and key/command helper: SHA CEE065413412B5AA55F2074F4A8256704BA7A176417C370533D75C5EB17E6F41, two code callers plus constructor callback xref, inherited typed lookup of shape controls 8-11 and previous/next controls 6/7, non-edge deselect/increment/select, next-enabled edge gate, StepShapeForward order 11 then 8/9/10, terminal -1 disables next, nonterminal result enables previous, caller-owned notification, exact padding/CFG/no-check facts, compiler-cookie exclusion, and superseded SelectShapeSlot/ShiftShapeRight draft preserved historically.
```
4. Replace by-memory UID0002Q8 row with:
```text
        - [UID:0002Q8][0x004fce80-0x004fd520.UserShapeSelectControlPaneCore](by-memory/0x004fce80-0x004fd520.UserShapeSelectControlPaneCore.md) 0x004fce80-0x004fd520 | class method cluster | UserShapeSelectControlPaneCore : reconstructable : 91% : very-strong : Source-ready old UserShapeSelectControlPane constructor, descriptor builder, male/female bank helpers, exact short StepShapeForward/StepShapeBackward terminal-sentinel behavior and invalid-state fallthrough, selected getter/setter, timer/mouse/paint handlers, 0x140 layout, two ten-word sprite-id banks, current/legacy resources, globals/render dependencies, direct class owner route, formal C++, caller fanout including NewUserDialogPane2 shifts, compiler destructor/thunk exclusions, and exact padding boundary.
```
5. Replace by-class UID000038 row with:
```text
- [UID:000038][ControlPane](by-class/ControlPane.md) : reconstructable : 91% : very-strong : Complete 0x108 Pane-derived control base declaration with exact ControlPane(unsigned char,const RectBounds *) constructor, inline empty virtual destructor, exact primary slot order through byte-return HitTestPart, public enable/disable/hit-testing API plus inline IsEnabled direct-read accessor for inherited +0x101 state, protected state virtuals, exact protected +0xf8..+0x104 range/control/state layout, natural tail alignment, complete child routing, NewUserDialogPane2 edge-button consumer, and compiler vtable/thunk/wrapper exclusions at source position 10.
```
6. Replace by-class UID00009E row with:
```text
- [UID:00009E][NewUserDialogPane2](by-class/NewUserDialogPane2.md) : reconstructable : 92% : very-strong : Complete alternate 640x480 create-user dialog declaration and exact-child source route under NewUserDialogPane2.cpp: constructor callers, singleton/vtable evidence, inherited DialogPane typed child lookup, own +0x26c nation/+0x270 totem/+0x274 shape-slot/+0x278 reply-phase/+0x27c string layout with implicit padding, exact left/right selection and edge-step behavior, selectors, command/key/packet/notify bridges, submit/account/character packet/reply paths, class closure before [[CHILDREN]], compiler/raw exclusions, and superseded duplicate m_childControls and simplistic shift drafts preserved historically.
```
7. Replace by-class UID0000FR row with:
```text
- [UID:0000FR][UserShapeSelectControlPane](by-class/UserShapeSelectControlPane.md) : reconstructable : 92% : very-strong : Complete source-ready 0x140 ControlPane-derived old body-shape selector declaration under UserCreateAppearanceControls: constructor, descriptor builder, male/female bank methods, exact short forward/backward step contract, selected getter/setter, timer/mouse/paint virtuals, +0x108..+0x13f state/tables with implicit alignment, eight constructor sites, NewUserMisc/NewUserDialogPane2 caller fanout, exact core child source order, vtable/destructor/thunk evidence, resource/global dependencies, hair-selector alias rejection, and compiler-only scalar/adjustor exclusions.
```
8. Replace by-file UID0000IG row with:
```text
- [UID:0000IG][ControlPane](by-file/ControlPane.md) : reconstructable : 90% : very-strong : Base UI control module under NexusTK/ui/core/ with the complete 0x108 ControlPane declaration before qualified definitions, exact RectBounds constructor, protected +0xf8..+0x104 state band, exact primary virtual order through byte-return HitTestPart, enable/disable/hit-testing methods, inline IsEnabled accessor for direct inherited +0x101 reads including NewUserDialogPane2 shape-edge gating, broad derived-control caller fanout, and compiler vtable/adjustor/deleting-wrapper exclusions.
```
9. Replace by-file UID0000LW row with:
```text
- [UID:0000LW][NewUserDialogPane2](by-file/NewUserDialogPane2.md) : reconstructable : 92% : very-strong : Alternate 640x480 create-character source under NexusTK/login/ with complete NewUserDialogPane2 declaration and exact child definitions: singleton storage, constructor/resources/callbacks, inherited DialogPane child lookup, exact left/right shape-slot and edge-step/button behavior, nation/totem/shape/gender selectors, command/key/packet/notify bridges, name prompt/apply, account/character submit/packet/reply paths, complete own layout, exact vtable/data/lifecycle boundaries, main-menu launchers, neighboring variant separation, compiler/raw exclusions, and historical old-account, duplicate-manager, and simplistic shift assumptions explicitly disposed.
```
10. Replace by-file UID0000OX row with:
```text
- [UID:0000OX][UserCreateAppearanceControls](by-file/UserCreateAppearanceControls.md) : reconstructable : 92% : very-strong : Dedicated NexusTK/login appearance-controls source with five closed classes, complete UserShapeSelectControlPane declaration before its exact core definitions, 26 exact methods, corrected short forward/backward step ABI and invalid-state fallthrough, shared swatch type/table, two shape sprite-id banks, current/legacy resources, NewUserMisc/NewUserDialogPane2 consumers, generated vtable/destructor support, resolved dependencies, natural layout padding, and no aggregate duplicate emission.
```
- The validator-owned tracker was not manually edited. Handoff-final command `000000016423` refreshed it to SHA `99A044C5857BB7D4018628474B6FB2EA3DB5A6590E9EFC3F957EEADBF83ACBAA`; bounded final-read command `000000016426` advanced only the volatile external snapshot to SHA `C20B49075EE4012D90BDA5344BD8E426E170E0455B739379D99ED16941591D48`. B002 did not apply manual coverage or tracker text directly.

## Follow-Up Actions
- B002 callback work is complete: each ordinary page was reread, leased, edited, scoped-validated, and released serially; final waited command `000000016407` passed lease-free.
- Ordinary Gate 2 passed exact report SHA `48E1345FCC65ABCBE59BF8E71BD53F747696F78997F7B0323D3E45950C1883A8`; the supervisor then applied all ten manual actions and validated them through final waited command `000000016423`.
- Bounded final-read command `000000016426` preserves the generated assertions: one UID00043N definition with Destination 1; one UID00043M definition with Destination 2; one closed UID00009E class before definitions; no target marker; one UID0000FR class before UID0002Q8; no UID0000FR Empty Emitter Marker; short step declarations/definitions; one inline IsEnabled; no handwritten cookie/vtable/thunk/scalar ABI.
- This reconciled report returns for fresh exact-artifact Gate 2 review. Any later `execute_report`, movement, archive, or lifecycle result is supervisor/validator-owned and is not asserted here.
- B002 future research: none required for this target. Broader unrelated NewUserDialogPane2 methods remain independent tracker items.

## Confidence
- Recommendation confidence: `94/100`.
- Score confidence: `94/100` for target `92/94`; support scores reflect directly resolved source blockers and retained broader uncertainty.
- Remaining uncertainty: exact original lexical spelling for `previousButton`, `nextButton`, `shapeControls`, and `IsEnabled`, plus unrelated class methods outside this report. None changes behavior or source placement.

## Validator Results
- All commands ran from canonical `source-3/project-documentation`; every command exited `0` with `ok:1`, and every deferred generated refresh was followed by the single authorized waited refresh.
- `000000016386`, `2026-07-22T15:41:39-04:00`: UID00043N scoped apply; completion/confidence/registry/stats updates, references added for UID00003T/UID0000FR, generated refresh deferred; no warning was reported.
- `000000016387`, `2026-07-22T15:42:30-04:00`: UID00043M scoped apply; completion/confidence/registry/stats/reference updates and canonical link normalization, generated refresh deferred; no warning was reported.
- `000000016388`, `2026-07-22T15:43:30-04:00`: UID00009E scoped apply; completion/confidence/registry/stats/reference updates for UID00003T/UID00043M/UID00043N, generated refresh deferred; no warning was reported.
- `000000016389`, `2026-07-22T15:45:37-04:00`: UID000038 scoped apply; registry/stats updates and UID00043M/UID00043N reference additions; `16` pre-existing missing-reference warnings on UID0003J8/UID0003J9/UID0003N6/UID0003N7 and related existing links; generated refresh deferred.
- `000000016392`, `2026-07-22T15:47:03-04:00`: UID0000FR scoped apply; completion/confidence, source position, formal-block registry, and stats updates; generated refresh deferred; no warning was reported.
- `000000016394`, `2026-07-22T15:48:21-04:00`: UID0002Q8 scoped apply; completion/confidence, position, formal hash, and stats updates; generated refresh deferred; no warning was reported.
- `000000016397`, `2026-07-22T15:49:20-04:00`: UID0002QR scoped apply; UID00003T reference addition and stats update; generated refresh deferred; no warning was reported.
- `000000016399`, `2026-07-22T15:50:21-04:00`: UID0000LW scoped apply; completion/confidence and stats updates; generated refresh deferred; no warning was reported.
- `000000016402`, `2026-07-22T15:51:11-04:00`: UID0000OX scoped apply; stats update only; generated refresh deferred; no warning was reported.
- `000000016404`, `2026-07-22T15:52:03-04:00`: UID0000IG scoped apply; stats updates and UID00043M/UID00043N reference additions; `14` pre-existing missing-reference warnings on UID0003J8/UID0003J9/UID0003N6/UID0003N7 and related existing links; generated refresh deferred.
- `000000016406`, `2026-07-22T15:53:10-04:00`: UID0002Q8 exact-block whitespace correction scoped apply; formal hash and stats update; generated refresh deferred; no warning was reported.
- `000000016407`, `2026-07-22T15:53:26-04:00`: final UID00043N `--wait-generated`; exit `0`, `ok:1`, generated refresh completed. Project-wide side effects were registry rebuild (`5264` nodes/`4151` edges), `281` generated metadata refreshes, and existing global diagnostics (`13` children fallback inserts, `72` missing children-marker notices, `115` emitting-children-only notices); none is a target failure.
- Supervisor external coverage epoch after ordinary Gate 2:
  - `000000016420`: by-memory coverage apply/validate, exit `0`, `ok:1`; exact UID0002Q8/UID0002QR/UID00043M/UID00043N rows each occur once. It reported four pre-existing missing-reference warnings for UID0003MT/UID0003VS/UID00039L/UID0003T6 and added UID00043M/UID00043N references.
  - `000000016421`: by-class coverage apply/validate, exit `0`, `ok:1`; exact UID000038/UID00009E/UID0000FR rows each occur once.
  - `000000016422`: by-file coverage apply/validate, exit `0`, `ok:1`; exact UID0000IG/UID0000LW/UID0000OX rows each occur once.
  - `000000016423`, `2026-07-22T16:12:07-04:00`: final waited target validation, exit `0`, `ok:1`, generated refresh completed. Its handoff-final manual, generated, and tracker hashes are recorded under Current Target State and the coverage section.
- Later unrelated supervisor B005 coverage activity is read-only coordination evidence for this report: command `000000016424`, `2026-07-22T16:16:38-04:00`, advanced by-memory; command `000000016425`, `2026-07-22T16:16:47-04:00`, advanced by-file; command `000000016426`, `2026-07-22T16:16:56-04:00`, completed the waited generated refresh. The ten B002 rows, ordinary hashes, and accepted generated semantics remained unchanged; final-read external hashes are recorded above.
- MCP remained healthy for the bounded evidence pass. No fallback-only conclusion was used, and B002 did not mutate IDA.

## Changed Files
- Updated report: `tools/leaser/Agents/Agent-B002/research/00043N-NewUserDialogPane2ShiftShapesRight-source-quality.md`; research, six accepted managed blocks, historical evidence, and exact ten-row manual handoff remain intact.
- Changed ordinary destinations, exact callback-final manifests:

| Path | SHA256 | Bytes / lines |
| --- | --- | ---: |
| `by-memory/0x0052b1d0-0x0052b30d.NewUserDialogPane2ShiftShapesRight.md` | `D699512D5FEA3A0989BD87689E775004699FEFDB6AEFD4B7665B11E069126EA9` | `5,779 / 79` |
| `by-memory/0x0052b090-0x0052b1cb.NewUserDialogPane2ShiftShapesLeft.md` | `87D6FFBC6D60EBB22A8DFFD1044D447104EF852F5EB17EE783AA9195D8564032` | `4,789 / 73` |
| `by-class/NewUserDialogPane2.md` | `06B55DF937B07EFB2B4944FADE2AE3A947D78A584CD752ABA8ABC6C3DEA6D465` | `22,144 / 159` |
| `by-class/ControlPane.md` | `BAB29EA5E3DD9054110A96AB535A9D8CA1563F5681474EF47EA06AA8110B3F75` | `33,683 / 230` |
| `by-class/UserShapeSelectControlPane.md` | `7666C4B8C688B2387F75BCD60DFAD38EF2ABE9C7DBB1C110F956DEEA7C883D03` | `17,621 / 156` |
| `by-memory/0x004fce80-0x004fd520.UserShapeSelectControlPaneCore.md` | `B5B6AB6F075E080FFD87A4EEE8C43B4696093307D111AAC8CE190760E5621EE2` | `30,773 / 378` |
| `by-memory/0x0052a540-0x0052c325.NewUserDialogPane2CreateUserVariant.md` | `135C09A1CE4BF44B182C16DD6F978FDC27F632069E707A80EBEEAFD611D314FF` | `18,571 / 120` |
| `by-file/NewUserDialogPane2.md` | `039D4B3848875B3E318309513228FEF4ED631D4D8B800AFFC2C7C7A9757A74A5` | `22,267 / 140` |
| `by-file/UserCreateAppearanceControls.md` | `1BF4E3924BA0138B2CB3E07A781947025E97EAE6B24D2E7F32C0735F93063E8B` | `38,840 / 209` |
| `by-file/ControlPane.md` | `7746E55AB73563FE9ED9DBADB47476F016FFA69DCF8ABB78E6B91008DAA0C5D9` | `25,222 / 139` |

- Verify-only `by-class/DialogPane.md` remained unchanged at SHA `7101A93A4EF53CFA1F11529AEF111AA94B13EBC76222ADE546D7B59D6D521DBE` (`38,969` bytes/`333` lines).
- Generated files were not edited manually. B002's historical command `000000016407` established the ordinary semantic proof; supervisor handoff command `000000016423` refreshed the accepted coverage-epoch headers/hashes, and later unrelated command `000000016426` advanced only volatile external headers/hashes. Bounded final-read generated manifests are NewUserDialogPane2.cpp `53847E210AC9C4C97F876961B4A9BA45910AEBED38C18552DD4D874E9B10D1A4` (`14,158/508`), UserCreateAppearanceControls.cpp `F2EFC288B232385229D6BE02CBE81A5371663F516E50F2E0EE9734A3DC750D7E` (`33,370/988`), and ControlPane.cpp `3BA254A3FCAEAB4BE1EBF754A6630467029D84D1087A758C83E8DDB159BE6A09` (`2,947/101`); semantic assertions are unchanged.
- Manual coverage commands `000000016420`-`000000016423` applied B002's exact ten-row handoff at by-memory `755F5188551AC818573B76EF0710F77AA66893361B03F3B395B89E955E5609F8`, by-class `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, and by-file `C21CDF487AFE771EF013A09BE6C24AA299A47DF48DF26B9C81B36A7A93C0DB5F`. Later unrelated supervisor commands `000000016424`-`000000016426` advanced bounded final-read shared hashes to by-memory `94434F13116AE98DAFA5389C85EB21E03217184CB821AE09226129290CBC1AB1`, unchanged by-class `2F346B5D08E3D4EFAC3785449B1FF1F0A0DE15123DF2002AA0CDF8E7302D2BE2`, by-file `2BF7C218FCF6760E10CC73E9992618491D04A680AF4962E54CFDF33BD2CF85C2`, and tracker `C20B49075EE4012D90BDA5344BD8E426E170E0455B739379D99ED16941591D48`. All ten rows remain exact and singular; B002 edited none of those files.
- Audit, goal, supervisor, validator-state, IDA, lifecycle, archive, and adjacent project files were not edited by B002 during reconciliation; only this report changed.
- Each ordinary lease was acquired only immediately around its edit/validator and released immediately afterward. Final `current_leases.md` read has zero B002 rows. No file was renamed, moved, archived, or executed by B002.

## Implementation Tracking Checklist
- [x] Supervisor exact-artifact Gate 1 validated all 33 literal H2 sections, C01-C30, six managed blocks, ten manual actions, and the pre-callback checklist at SHA `9AD4A18A830A4A3C4CD9CDB859A39B2FBC8B7DFC3D740AA1F015059248245081`.
- [x] Confirm ledger Action cells retain permitted enums and every Verification state is now a legal callback terminal state: `applied` or `already-present`.
- [x] Reread target, all destinations, current B001-B005 goals/reports, generated files, tracker/manual rows, and lease ledger around callback edits and final reconciliation; no target overlap or live B002 lease remains.
- [x] UID00043N: set `92/94`; retain owner/emitter `00009E`, reconstructable true, blank optional position, Nested 0.
- [x] UID00043N: install Destination 1 byte-for-byte.
- [x] UID00043N: incorporate exact 317-byte range/hash, seven-block CFG, boundaries, padding, and compiler-cookie disposition.
- [x] UID00043N: incorporate all three xrefs, constructor member-callback signature, callers, and notification separation.
- [x] UID00043N: incorporate ids 8-11/6/7, `+0x274`, `+0x101`, non-edge branch, edge gate, exact call order, sentinel/button branch, and no-check facts.
- [x] UID00043N: preserve historical B004 body as superseded, with exact rejection reasons.
- [x] UID00043M: set `92/94`, retain metadata, install Destination 2, and incorporate symmetric exact evidence.
- [x] UID00009E: set `92/94`, retain owner/emitter/reconstructable/position, and install Destination 3.
- [x] UID00009E: remove only the duplicate derived `m_childControls`; preserve inherited DialogPane route and exact own offsets.
- [x] UID00009E: place class closing brace before `[[CHILDREN]]` and preserve every unrelated method/field/history fact.
- [x] UID000038: preserve `91/93`, owner/emitter/position/layout, install Destination 4, and add exact target consumer evidence.
- [x] UID0000FR: set `92/93`, position 0, retain owner/emitter/reconstructable, install Destination 5.
- [x] UID0000FR: record exact `0x140` layout, implicit alignment, methods, caller fanout, and compiler exclusions.
- [x] UID0002Q8: set `91/94`, position 10, retain owner/emitter/reconstructable, install Destination 6.
- [x] UID0002Q8: preserve every accepted constructor/descriptor/bank/selected/timer/mouse/paint/resource/global/table fact.
- [x] UID0002Q8: correct both return types to short and preserve exact invalid-state fallthrough without a fabricated return.
- [x] UID0002Q8: move inherited `m_controlFlags` assignment into the constructor body and use period-plausible namespace constants.
- [x] UID0002QR: preserve `89/90` non-emitting index and synchronize exact shift/inherited-manager/history prose.
- [x] UID0000LW: set `92/94` and incorporate complete target/class/source-order/dependency/compiler/history evidence.
- [x] UID0000OX: preserve `92/93` and incorporate complete declaration/core/short-step/source-order evidence.
- [x] UID0000IG: preserve `90/92` and incorporate inline IsEnabled/target consumer evidence.
- [x] UID00003T: verify existing typed GetChild API same-or-greater; no edit was required and its hash remained unchanged.
- [x] Preserve all unrelated concurrent content and all prior valid provenance on every destination.
- [x] Do not create a duplicate target report, new child page, new source file, or aggregate source body.
- [x] Do not emit stack cookies, vtable arrays, raw virtual calls, adjustor thunks, scalar deleting wrappers, or explicit padding members.
- [x] Run one scoped validator for UID00043N and record command id/timestamp/exit/ok/warnings/side effects.
- [x] Run one scoped validator for UID00043M and record command metadata.
- [x] Run one scoped validator for UID00009E and record command metadata.
- [x] Run one scoped validator for UID000038 and record command metadata.
- [x] Run one scoped validator for UID0000FR and record command metadata.
- [x] Run scoped validators for UID0002Q8, including the final exact-block whitespace correction, and record command metadata.
- [x] Run scoped validators for every prose destination actually changed and record command metadata.
- [x] Release each ordinary lease immediately after its scoped validator; hold zero leases before generation.
- [x] Run one final authorized waited validation targeting UID00043N.
- [x] Verify generated NewUserDialogPane2.cpp contains exactly one target and one sibling body with exact branch/call order.
- [x] Verify generated NewUserDialogPane2.cpp has one complete class closed before children, no target Empty Emitter Marker, and no duplicate derived child-manager field.
- [x] Verify generated UserCreateAppearanceControls.cpp contains one complete UID0000FR class before UID0002Q8, short step signatures, and no UID0000FR Empty Emitter Marker.
- [x] Verify generated ControlPane.cpp contains exactly one inline IsEnabled declaration and no duplicate out-of-line definition.
- [x] Verify no handwritten cookie, vtable, thunk, scalar wrapper, raw ABI table, or decompiler label was introduced.
- [x] Rebase all ten manual actions against then-current rows and preserve the current unrelated union; B002 kept coverage read-only.
- [x] Update C01-C30 to legal callback terminal states with destination proof outside state cells.
- [x] Record final ordinary/generated hashes, validator metadata, changed files, lease proof, and exact generated counts in this report.
- [x] Check every accepted implementation row truthfully; no blocked callback row remains.
- [x] Confirm third-party import directive is not applicable; this is NexusTK-authored class source, not vetted embedded third-party code.
- [x] Confirm no Wave2/Wave3 artifact was incorporated as current evidence.
- [x] Confirm B002 did not edit manual coverage/generated/tracker/audit/supervisor/validator-state/IDA/lifecycle files.
- [x] Record that ordinary Gate 2 independently passed exact report SHA `48E1345FCC65ABCBE59BF8E71BD53F747696F78997F7B0323D3E45950C1883A8` before the supervisor coverage epoch.
- [x] Reread and verify all ten supervisor-applied manual rows occur exactly once at the recorded anchors at handoff command `000000016423` and bounded final-read command `000000016426`.
- [x] Record supervisor commands `000000016420`-`000000016423`, command-16420 warnings/reference additions, and the later unrelated command-`16426` volatile generated/tracker manifests without attributing them to B002.
- [x] Reconfirm generated semantic counts are unchanged from accepted ordinary Gate 2 proof through bounded final-read command `000000016426`.
- [x] Preserve the boundary that supervisor applied exact manual coverage after ordinary Gate 2 and retains ownership of later report lifecycle actions.

READY_FOR_SUPERVISOR_GATE2_REVIEW

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000016456","destination_path":"executed-b-agent-research/B002/00043N-NewUserDialogPane2ShiftShapesRight-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/00043N-NewUserDialogPane2ShiftShapesRight-source-quality.md","timestamp":"2026-07-22T16:51:32-04:00","uid":"00043N"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
