** TARGET-REPORT-UID:0001ZG **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# 0001ZG RankingRewardInfoDialogCore Ownership / Split Research


## Finalized Report / Current Recommendation

- Current recommendation: retain implemented [UID:0001ZG] as a non-emitting exact-child split index. The range does not represent one source-level function, declaration, or data object; it maps a `RankingRewardInfoDialog` method island plus retained raw bodies, vector/template support, exception funclets, padding, adjustor thunks, and a scalar-deleting wrapper.
- Final disposition: `CANONICAL_OWNER:0000BP`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, and blank formal C++ on the parent. Existing and new exact children carry the real source/ABI dispositions.
- Implemented result: exact children UID0004I7-UID0004IE cover all eight previously uncovered spans; all thirteen padding spans are recorded; class/file/vtable/rdata/global/existing-child support is synchronized; the five new exact children plus existing UID0001ZH emit six source-authored core behaviors; only compiler/EH/ABI children remain non-emitting within the core inventory.
- Confidence: high for the split, exact boundaries, vtable routes, constructor/control layout, accepted EventHandler packet interface, `std::vector` reward model, packet/reward interactions, private retained-helper disposition, and owner/file route; medium-high for exact original private method spellings and paint-local helper typedef spelling.

## Supporting Research

- Historical context: the supervisor returned the earlier artifact at SHA256 `2AB4BEEBCA91BF3763028CB2667D1523B4C02D80077433405F416910F6A74661` for additive Gate 1 repair, then accepted repaired SHA256 `F3D6E5BFF0E8D29279E653DF659953B4DC0F628896B7D686A0043BD648C681D7` for this implementation callback.
- The immediately preceding UID0000Y5 assignment was stopped before any UID0000Y5 report was created. It has no bearing on this artifact.
- Original IDA MCP evidence was collected read-only from NexusTK IDB session `359c7886` through MCP transport session `5f2d3e9d-d8a3-4df9-8507-e4607c99b30e`. The additive repair used fresh transport session `d599c186-75ca-425b-963d-a6080e9cf12b`; `idb_list` id `3` reconfirmed active session `359c7886`, and health id `4` returned `status:"ok"`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- Gate 1 additive packet repair used restored NexusTK IDB session `cfa0ae1a` through MCP transport `90e84628-ef1f-4ff9-9cac-05c85ae98989`. Refreshed `tools/list`, `idb_list` id `3`, `server_health` id `4`, and real `lookup_funcs` id `5` proved one active owned worker (`pid 12260`), `status:"ok"`, auto-analysis/Hex-Rays/string-cache readiness, and real lookup of the handler/parser/accessor/reader functions. Current handler/sibling evidence is recorded below.
- Callback implementation is complete in the target/support by-* pages listed under `Changed Files`. Generated and tracker files were updated only by validator commands; the original callback waited refresh was `000000008295`, and the additive packet repair completed waited refresh `000000008342`. The read-only `RankingDialog.cpp` header is `000000008338` because that parser-support command produced the last content change; final command `000000008342` completed without further generated-content delta. The generated class declaration and UID0001ZH body were verified directly. B002 did not manually edit generated files. No IDA database, coverage report, supervisor ledger, lifecycle state, or other agent artifact was edited.

## Target

- Target UID: `0001ZG`.
- Target path: `by-memory/0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore.md`.
- Historical queue row before callback: `auto-generated/-ag-research-tracker.md` -> `## by-memory` -> `### Not-Covered Files - Reconstructable`, `86/90`, reconstructable true.
- Current classification: implemented callback artifact; report execution/lifecycle state remains external validator-owned history.
- Current scores and parent state: `COMPLETION:91`, `CONFIDENCE:93`, `CANONICAL_OWNER:0000BP`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, blank formal C++.

## Current Target State

- Corrected metadata routes the aggregate to class UID0000BP as a non-emitting index; UID0000MZ remains the terminal source file through the exact source children.
- B002 completed additive waited command `000000008342`; final read-only audit shows `RankingDialog.cpp` content header `000000008338`, no UID0001ZG marker, formal source under UID0004I7/I9/IA/IB/IC and UID0001ZH, and no emission from UID0004I8/ID/IE.
- Every source/compiler span now has an exact direct page. Existing packet/vector/singleton/destroy/thunk children and StringUtil formatter ownership remain intact.
- The broad range is not itself a class declaration. [UID:0000BP] is the class declaration/support surface; [UID:0001ZG] is an address-ordered audit map.
- No accepted implementation item remains. UID0001ZH now emits `RankingRewardInfoDialog::HandlePacketEvent(Event *event)` at `91/93`; the former packet-context/listener, accessor, vector, and invalidation blockers are resolved by accepted project APIs.
- Related target/support docs checked: [UID:0000BP] `by-class/RankingRewardInfoDialog.md`, [UID:0000MZ] `by-file/RankingDialog.md`, [UID:0000S3] `by-global/g_pRankingRewardInfoDialog.md`, [UID:0000BK] `by-class/RankingCategoryCollection.md`, [UID:0000BM] `by-class/RankingDialog.md`, [UID:0001YJ] `by-type/by-vtable/RankingDialogVtableFamily.md`, [UID:0002ON] `by-memory/0x00610980-0x00610e38.RankingDialogReadOnlyData.md`, [UID:0001ZH], [UID:0001ZI], [UID:0000YA], [UID:0001ZK], [UID:0000YB], the split vector children [UID:0003X6]-[UID:0003X9], DialogPane/RectBounds/ImageButton/BlackHole/packet support, generated coverage/source, and matching executed reports.
- Current artifact/lifecycle status: callback-complete report at the expected B002 research path. B002 did not execute, move, archive, or alter lifecycle history; supervisor validation/execution/archive remain external actions rather than pending instructions inside this report.

## Executive Recommendation

- UID0001ZG is implemented as a reviewed `RECONSTRUCTABLE:FALSE` split index with direct semantic owner [UID:0000BP] and no emitter/code.
- Exact children UID0004I7-UID0004IE are registered low-to-high for constructor, constructor-unwind cleanup, `OnDialogAction`, `OnPaint`, raw close-button-bounds, raw reward-info request, reward-entry range cleanup, and scalar-deleting destructor wrapper.
- Keep existing packet/vector/singleton/thunk child pages in place. UID0001ZH is source-emitting; do not merge the shared [UID:0003X8] `WideFormatWrapper256` back into reward ownership.
- Exact first-draft C++ now emits for construction, `OnDialogAction`, `HandlePacketEvent`, `OnPaint`, private `BuildCloseButtonBounds`, and private `SendRewardInfoRequest`; compiler cleanup/thunk/scalar-wrapper children remain non-emitting.
- Source route remains `NexusTK/ui/dialogs/RankingDialog.cpp` through [UID:0000BP] -> [UID:0000MZ]. No new file is warranted.

## Supervisor Active Recheck

- Triggering callback: implement accepted ZG-01 through ZG-16, create/register children low-to-high, apply formal bodies/support/padding, validate every changed by-* file, perform waited generated verification, and keep the report archive-neutral. Runtime/provenance remain `gpt-5.6-sol` xHigh / `CHATGPT | 5.6 | xHigh`.
- Triggering additive Gate 1 repair: report SHA256 `90B2715EBCC99A696E6212A6C0EADF3BF246E25C41F8381F796611F7EE0F0C8B` failed because First-Draft snippets lacked literal managed formal headers and because UID0001ZH was simultaneously treated as source-authored and deferred for unresolved APIs. This revision preserves all accepted ZG-01..ZG-16 work, adds destination-specific managed blocks, resolves and implements UID0001ZH, and reconciles every current-state/score/inventory/checklist claim.
- Split repair is complete. Every source-bearing or compiler-only child in `0x0045dba0-0x0045f12c` has an exact page and current disposition.
- UID00033K was stopped before any report was created or retained and was not touched during this callback.

## Inference Research Guidance Check

- `by-structure.md` requires exact method/function children to own their own C++ and forbids a parent aggregate from pasting child bodies into its formal block. It also classifies a reviewed audit/index range as `RECONSTRUCTABLE:FALSE` even when contained children are reconstructable.
- Existing assumptions treated as uncertain were direct [UID:0000MZ] semantic ownership, the idea that one broad core range could emit source, the wording that all raw helpers merely needed final names, and the implicit treatment of a scalar-deleting wrapper as a handwritten destructor method.
- Direct IDA facts are separated below from documentation evidence and source-shape inference. Inferred names are explicitly marked descriptive where no symbol proves original spelling.
- A stale Wave3 link exists in `RankingDialogVtableFamily.md`. It was ignored. No Wave2/Wave3 artifact was used as evidence.

## Heuristic / Inference Reanalysis And Validation

1. **Aggregate versus source object.** IDA enumerates thirteen modeled functions in the range and three retained unmodeled bodies, with compiler EH and padding interleaved. The aggregate is therefore an address map, not a source function or class declaration. The class source surface already exists at [UID:0000BP].
2. **Direct owner.** Constructor stores, vtable slots, singleton lifecycle, object size, and the sole dialog vector at `+0x26c` all bind the source methods to `RankingRewardInfoDialog`. Direct file ownership is too broad; [UID:0000MZ] remains the terminal emitter only.
3. **Constructor and vector model.** `sub_45DBA0` is a 438-byte constructor (Verified with MCP `int_convert`, original call id `30`), called from `RankingDialog::OnButtonClick` case `5`. That caller allocates 632 bytes (`0x278`, Verified with MCP `int_convert`) and calls the constructor only when `g_pRankingRewardInfoDialog` is null. Fresh MCP decompile id `5` reconfirms the three zeroed dwords at `+0x26c/+0x270/+0x274`; packet append/deep-copy, paint stride arithmetic, and vector destruction show the high-probability source member is `std::vector<RankingRewardEntry> m_rewardEntries`, whose VC6/VC7 three-pointer object exactly occupies the 12-byte derived tail. The same model applies to `RankingRewardEntry::items` at `+0x224` as `std::vector<RankingRewardItem>`.
4. **Control construction and source declaration.** Fresh MCP decompile id `17` proves `sub_495BF0` receives explicit arguments `(int imageId, const RectBounds *bounds)`, forwards the bounds to its base constructor, stores image id at `+0x10c`, and initializes the generic image-button fields. Its 306 documented constructor xrefs and sibling dialog drafts confirm the reusable declaration `ImageButtonControlPane(int imageId, const RectBounds *bounds)`. The target constructor initializes `DialogPane(L"", -1, 0)`, publishes the singleton, calls the retained close-bounds method with constant selector `0` (inlined to `InitRectBoundsFromSize(279,468,63,24)`), adds `new ImageButtonControlPane(14, &closeBounds)`, creates with `InitRectBoundsFromSize(202,104,620,500)` and `dword_69B36C`, shows relative to `g_pBackPane`, sets focused/pending control `0`, calls the retained request method (also inlined), and calls `SlideOpenVertical()`.
5. **Action method.** Primary vtable cell `0x00610c0c` points to `0x0045dda0`. The method has two stack parameters, `retn 8`, tests only the first, and on action `0` calls `DialogPane::SlideCloseVertical()` followed by `g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(this)`. Accepted dialog-action precedent supports `OnDialogAction(int actionId, int actionParam)`.
6. **Packet interaction.** Existing [UID:0001ZH] is the secondary `EventHandler` packet-family override for opcode `0x7d`, subcommand `5`. Current MCP session `cfa0ae1a` reconfirmed `event + 0x0c`, sole vtable xref `0x00610c34`, big-endian category/count readers, selected category accessors, `RankingRewardEntry` parser, vector append/deep-copy/cleanup, and the adjusted Pane slot `+0x20` invalidation call. Accepted sibling packet-handler and EventHandler docs resolve the source shape as `bool HandlePacketEvent(Event *event)`, `event->packet`, `m_rewardEntries.push_back(entry)`, and `InvalidateRect(&m_bounds)`. UID0001ZH now emits the behavior-identical human-shaped body below.
7. **Paint behavior and dependencies.** Primary vtable cell `0x00610c08` points to the 2691-byte `OnPaint` method (Verified with MCP `int_convert`). Fresh full decompile id `7` and bytes id `16` fix the complete source sequence: initialize a 40-byte `EPFTileContext`/layout-compatible `FrameDrawRecord`; load `WBOARDBK.EPF`/`.PAL`; tile the interior; load `WEBBOARD.EPF` frames `1,6,3,4,0,2,5,7` for horizontal edges, vertical edges, and corners; set text colors `143/128`; render selected category title and the exact headers `Rank`, `Reward Legend`, and `Reward Items`; traverse `std::vector<RankingRewardEntry>` and nested `std::vector<RankingRewardItem>`; draw `~` between unequal rank strings; draw `SYMBOLS.EPF`; choose fixed-slot versus half-size item paths from glyph dimensions; and format `%s(%d)`. Current accepted dependencies provide source-facing declarations for `EPFTileContext::Initialize`, `ResourceLayoutTable::LookupLayoutEntry`, `GrafPort::DrawTiledBackground`, `GrafPort::RenderTileFrame`, `SetTextColor`, `SetTextBackColor`, `DrawOutlinedText`, `GetTextWidth`, `GetItemGlyphBounds`, `ItemObjImageLib::DrawItemImageUnscaled`, `ItemObjImageLib::DrawItemImageIn43x43Slot`, and `g_pfnBlitSprite`. `FrameDrawRecord` should be a source alias of the same 40-byte `EPFTileContext` record for these GrafPort APIs; no missing graphics API remains a target blocker.
8. **Retained bounds helper disposition.** Fresh bounded listing id `13` reconfirms the complete `0x0045ea20-0x0045ea5e` body and `retn 8`. Two stack arguments plus callee cleanup, no use of `ecx`, immediate adjacency to other class methods, and the constructor's constant-zero inline duplicate make a private non-static `__thiscall` method whose receiver optimized unused substantially more likely than a default-cdecl file-static helper. Recommend source name `RankingRewardInfoDialog::BuildCloseButtonBounds(short layoutSelector, RectBounds *outBounds) const`; nonzero writes `(-1,-1,-1,-1)`, zero writes `(279,468,63,24)` through `InitRectBoundsFromSize`. Zero xrefs describe retained/inlined liveness, not a no-code condition.
9. **Retained request sender disposition.** Fresh bounded listing id `14` reconfirms the complete `0x0045ea60-0x0045ead9` security-cookie body. It has no explicit stack arguments, sits immediately beside the paired private bounds helper, and has a byte-for-byte semantic inline equivalent in the constructor. The highest-probability source is private `RankingRewardInfoDialog::SendRewardInfoRequest()`, with all calls inlined while an out-of-line copy remains. It obtains `GetSelectedRecord()`, writes `0x7d`, `5`, and `GetCategoryId()` through `PacketBufferWriteUInt8/UInt32BE`, writes local-only `packet[6]=0`, and sends exactly six bytes through `g_packetSender->QueueAndSendPacket`. Unresolved original spelling is only a confidence cap.
10. **Cleanup spans.** `0x0045dd60-0x0045dd9a` restores the class vtables, destroys the reward-entry vector, clears the singleton, and tail-jumps to `DialogPane` destruction. `0x0045eff0-0x0045f018` walks `0x230` entries and destroys each nested `+0x224` item vector; its only routes are EH metadata/tail-chunk references. Both are compiler cleanup and should not emit handwritten methods.
11. **Destructor wrapper.** `0x0045f0c0-0x0045f12c` is reached from the primary destructor slot and two adjustor thunks. It performs vector destruction, singleton clear, base teardown, flag checks, and optional delete. The scalar-delete flag logic and explicit vtable stores are ABI output. A standalone source destructor body is unsafe until the final class inheritance/member destruction model explains the observed vector-before-singleton-clear order; keep this child non-emitting.
12. **Rejected alternatives.** A single parent C++ block, direct file ownership, `RankingDialog` class ownership, `RankingCategoryCollection` ownership, `StringUtil` ownership, raw-pointer-only reward containers, file-static/default-cdecl bounds-helper placement, and a new `RankingRewardInfoDialog.cpp` file are rejected. The class/vtable/object lifecycle, `retn 8` member ABI clue, inlined constructor duplicates, vector helper family, and existing compact feature file route are stronger.
13. **Source readiness.** The old constructor/paint/raw-helper and UID0001ZH no-code comments are rejected by the additive Gate 1 repairs. Current accepted support declarations plus explicit inferred aliases are sufficient for source-shaped bodies without raw `sub_` labels. Exact original private/parser spellings and `FrameDrawRecord` alias spelling remain below final-audit proof but do not justify withholding first drafts under the active combined-score policy.

## Evidence Standards Used

- Evidence types: MCP IDB/session health, function enumeration, exact function lookup, compact decompilation, bounded raw disassembly, raw bytes, direct xrefs, absolute little-endian pointer-byte searches, vtable cells, constructor caller analysis, type-query negatives, adjacent-range/padding checks, current by-* docs, generated coverage/source, and search-gated executed reports.
- Binary facts control range and behavior claims. Current documentation controls accepted source-facing support names only where it agrees with the binary. Source placement and private helper spelling are inference.
- The evidence is strong enough for the parent split and child dispositions because every byte span has a modeled function, complete raw body, existing exact child, or verified padding/compiler classification. Confidence remains below `95` because exact private helper spellings and the historical `FrameDrawRecord` alias token are not symbol-proven; accepted dependencies and inheritance are sufficient for the implemented source.
- IDA type recovery returned no `RankingReward*` or `DialogPane*` types (MCP id `18`), so no inferred structure is presented as recovered PDB/type authority.

## Evidence Checked

- Original IDA MCP/manual-disassembly/raw-byte checks: `idb_list` id `3`; health id `8`; function range enumeration id `9`; start/boundary lookups ids `10`/`25`; function analysis ids `11`-`15` and caller analysis id `29`; xrefs id `16`; pointer-byte search id `17`; type query id `18`; bounded raw disassembly ids `19`-`23`; bytes id `24`; paint call/field/branch searches ids `26`-`28` and detailed paint windows ids `31`-`32`; numeric conversions id `30`.
- Additive Gate 1 MCP recheck on transport `d599c186-75ca-425b-963d-a6080e9cf12b`: `idb_list` id `3`; health id `4`; constructor analysis id `5`; paint compact/full decompilation ids `6`/`7`; ranking accessor query/decompilation ids `8`/`10`/`11`; tool-schema checks ids `9`/`12`/`15`; raw bounds/request bounded listings ids `13`/`14`; ranking string/resource bytes id `16`; `ImageButtonControlPane` constructor decompile id `17`; packet-handler analysis id `18`; `GrafPort::RenderTileFrame` and `DrawTiledBackground` decompiles ids `19`/`20`.
- UID0001ZH Gate 1 repair on current database `cfa0ae1a`: refreshed schema with `tools/list` id `2`; `idb_list` id `3`; health id `4`; handler/parser/category/reader real lookups id `5`; handler decompile/disassembly/callees/xrefs/stack/analyze ids `6`-`11`; sibling RankingDialog and ExchangeDialog packet-handler decompiles ids `12`-`13`; sibling/vtable xrefs id `14`; RankingDialog packet-handler disassembly id `15`. All calls explicitly used `database=cfa0ae1a` where required.
- Docs/reports checked: target, direct class/file/global owners, category collection/dialog support, vtable/read-only-data support, all current direct children and vector split children, `DialogPane`, `RectBounds`, `ImageButtonControlPane`, `g_pApplicationCleanupQueue`, `g_packetSender`, generated tracker/coverage/source, manual coverage as read-only stale context, executed B001 reports `0000XZ` and `0001ZE-0001ZJ`, executed B006 `0000MZ`, executed B004 `000202`, and executed B005 `000204`.
- Old-report search terms: `0001ZG`, `0x0045dba0`, `0x0045f12c`, `RankingRewardInfoDialogCore`, `RankingRewardInfoDialog`, `RewardInfo`, `0x0045ea20`, and `0x0045ea60`. No prior dedicated UID0001ZG report was found.
- Negative checks: zero raw-start xrefs for `0x0045dd60`, `0x0045ea20`, and `0x0045ea60`; zero absolute VA byte hits for those starts and constructor start; no recovered target types; no new source-file evidence; no ordinary caller for vtable-only action/packet/paint methods.
- Callback tools: scoped validators were run for every changed/new by-* file and final waited generated refresh was completed. Lifecycle commands, manual generated/coverage edits, IDA DB edits, process management, and broad unbounded MCP calls remained prohibited and were not performed.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| ZG-01 | UID0001ZG is a non-emitting split index, not one source object. | High | MCP ids `9`-`25`; by-structure aggregate rule. | UID0001ZG metadata/Status/Split Decision | incorporate | applied |
| ZG-02 | Direct semantic owner is UID0000BP; UID0000MZ is terminal source file. | High | Constructor/vtables/layout; class/file gates. | UID0001ZG, UID0000BP, UID0000MZ | incorporate | applied |
| ZG-03 | Constructor is exactly `0x0045dba0-0x0045dd56`, size `0x1b6`, called from `0x00458abd`, object size `0x278`, and is first-draft ready. | High | Original ids `10`, `11`, `16`, `24`, `25`, `29`, `30`; additive id `5`; ImageButton id `17`. | UID0004I7; class method/layout notes | incorporate | applied |
| ZG-04 | Derived tail is source `std::vector<RankingRewardEntry> m_rewardEntries` at `+0x26c`, represented by begin/end/capacity at `+0x26c/+0x270/+0x274`; nested items use the same vector model at entry `+0x224`. | High | Constructor/packet/paint/destructor data flow; vector helper family; additive ids `5`, `7`, `18`. | UID0000BP, UID000202/000204, vector children | incorporate | applied |
| ZG-05 | `0x0045dda0-0x0045ddc2` is `OnDialogAction(int,int)` and is formal-C++ ready. | High | Vtable xref, `retn 8`, action-zero close route, accepted DialogPane/BlackHole names. | UID0004I9; class/file/vtable/rdata | incorporate | applied |
| ZG-06 | `0x0045df90-0x0045ea13` is vtable-only `OnPaint`; accepted render/text/item dependencies and vector aliases now support the exact first draft below. | High | Original ids `14`, `16`, `17`, `26`-`28`, `31`-`32`; additive ids `6`, `7`, `16`, `19`, `20`. | UID0004IA; class/file/vtable/rdata | incorporate | applied |
| ZG-07 | Raw bounds helper is exact `0x0045ea20-0x0045ea5e` and is most probably retained/inlined private `BuildCloseButtonBounds`; first-draft body ready. | High behavior / medium-high name | Original ids `10`, `16`, `17`, `20`, `24`, `25`; additive ids `13`, `17`; constructor inline duplicate. | UID0004IB; class private declaration | incorporate | applied |
| ZG-08 | Raw request helper is exact `0x0045ea60-0x0045ead9` and is most probably retained/inlined private `SendRewardInfoRequest`; first-draft body ready. | High behavior / medium-high name | Original ids `10`, `16`, `17`, `21`, `24`, `25`; additive ids `5`, `14`; constructor inline copy and accepted packet APIs. | UID0004IC; class/file/packet context | incorporate | applied |
| ZG-09 | Constructor unwind and reward-entry range cleanup are compiler funclets, not methods. | High | MCP ids `19`, `22`-`25`; EH-only xrefs. | UID0004I8 and UID0004ID | incorporate | applied |
| ZG-10 | Scalar deleting destructor wrapper is compiler/ABI output and should not emit source. | High | MCP ids `15`-`17`, `24`, `25`; vtable/thunk routes. | UID0004IE; class/vtable/rdata/global | incorporate | applied |
| ZG-11 | Internal padding ranges must be recorded exactly. | High | MCP ids `20`-`25`; `0xcc` bytes and align directives. | `by-memory/-ignored.md`; UID0001ZG inventory | incorporate | applied |
| ZG-12 | Existing packet/vector/singleton/thunk child ownership remains intact; shared formatter remains StringUtil-owned. | High | Current metadata and executed B001/B006 reports. | Existing children and parent/file cross-references | already-present | already-present |
| ZG-13 | Parent score is `91/93`; class support and UID0001ZH are `91/93`; other source child scores reflect their resolved bodies. | Medium-high | Completed split/boundary/owner audit plus both additive source-readiness rechecks. | UID0001ZG, UID0000BP, UID0001ZH, UID0004I7-UID0004IE | incorporate | applied |
| ZG-14 | No new source file or IDA rename is required in this callback. | High | Existing route and assignment restrictions. | Source Placement / IDA recommendations | not-applicable | excluded-with-reason |
| ZG-15 | `ImageButtonControlPane` constructor source declaration is `ImageButtonControlPane(int imageId, const RectBounds *bounds)`. | High | Additive MCP id `17`; 306-call reuse context; sibling control construction. | UID00006D and UID0004I7 | incorporate | applied |
| ZG-16 | `FrameDrawRecord` should be treated as an alias/layout view of the same 40-byte `EPFTileContext` consumed by ResourceLayout and GrafPort helpers. | Medium-high | Additive ids `6`, `7`, `19`, `20`; current EPFTileContext/ResourceLayout/GrafPort declarations. | UID0000BP alias; existing GrafPort support sufficient | incorporate | applied |
| ZG-17 | Every C++ recommendation is destination-specific exact insertion text enclosed by the literal managed `RECONSTRUCTION_CPP CODE:[[[]]]`, `BEGIN`, and `END` lines. | High | Gate 1 failure requirement; current destination formal blocks. | First-Draft C++ Recommendation | incorporate | applied |
| ZG-18 | UID0001ZH is source-emitting `bool RankingRewardInfoDialog::HandlePacketEvent(Event *event)`; accepted Event, ranking, vector, parser-declaration, and Pane APIs resolve every former blocker. | High behavior / medium-high original spelling | Current MCP `cfa0ae1a` ids `2`-`15`; EventHandler/ExchangeDialog/CollectionPane/ranking support docs. | UID0001ZH, UID0000BP, UID0001ZG, UID0001ZF, UID0000MZ, UID0001YJ | incorporate | applied |

## Positive Evidence Summary

- The constructor has one real call at `0x00458abd`, and its caller allocates the exact `0x278` object only for ranking action `5` when the singleton is null.
- Primary vtable cells independently route scalar destructor, paint, and action methods; the secondary vtable routes the packet handler; adjustor thunks route the scalar wrapper.
- Constructor, packet, paint, request helper, vector destroy, and scalar wrapper all agree on the `+0x26c` reward-entry vector.
- Packet parsing, paint traversal, vector helpers, and class support agree on `0x230`-byte entries and nested `0x208`-byte items.
- The `ImageButtonControlPane` constructor ABI, current sibling dialog source, and target allocation agree on `(imageId, bounds)`; accepted graphics/text/item helper pages now cover every target-critical paint call.
- Both raw bodies have exact constructor inline equivalents. The bounds helper's two-argument `retn 8` supplies positive private-member ABI evidence; zero incoming xrefs are expected when all current call sites were inlined.
- The strongest inference chain is constructor caller -> object size -> vtable stores -> singleton lifecycle -> vector field use -> class support -> file route. It is sufficient to select UID0000BP as direct owner and UID0000MZ as emitter root.

## IDA MCP Facts

- Function/range facts: thirteen modeled functions occur in the target; source methods are constructor `0x45dba0` size `0x1b6`, action `0x45dda0` size `0x22`, packet `0x45ddd0` size `0x1bc`, paint `0x45df90` size `0xa83`; vector/formatter helpers occupy the existing split; singleton clear is size `0x0b`; vector destroy size `0x87`; thunks size `0x0b` each; scalar wrapper size `0x6c`.
- Data/table/padding facts: vtable bases are `0x00610bc4`, `0x00610c24`, and `0x00610c54`; derived object tail is `+0x26c..+0x277`; all listed internal gaps below are `0xcc` padding.
- Xref facts: constructor one code caller; action/packet/paint one vtable data xref each; raw starts zero; singleton clear one EH reference; cleanup funclet two EH references; scalar wrapper two thunk calls plus primary vtable data xref.
- Vtable/global/type facts: singleton is `0x0067a7e8`; selected-category owner is `g_pRankingDialog` at `0x0067a7e4`; packet sender is `0x0067a7ec`; cleanup queue is `0x0067a74c`; target type query returned no recovered types.
- Negative IDA facts: no function objects at the three retained raw starts; no pointer-byte table hits for them; no evidence for a separate source file; no direct ordinary callers for the virtual methods. These are liveness/retention facts, not source-body contradictions.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x0045dba0-0x0045dd56` | [UID:0004I7] `RankingRewardInfoDialogConstructor.md` | source constructor | TRUE | `0000BP` | `91/93` | registered; formal body emits |
| `0x0045dd60-0x0045dd9a` | [UID:0004I8] `RankingRewardInfoDialogConstructorUnwindCleanup.md` | compiler constructor-unwind cleanup | FALSE | `0000BP` semantic context | `89/93` | registered; no emitter/code |
| `0x0045dda0-0x0045ddc2` | [UID:0004I9] `RankingRewardInfoDialogOnDialogAction.md` | primary virtual action handler | TRUE | `0000BP` | `90/92` | registered; formal body emits |
| `0x0045ddd0-0x0045df8c` | [UID:0001ZH] existing | secondary `EventHandler` packet-family override | TRUE | `0000BP` | current `91/93` | formal `HandlePacketEvent(Event *event)` body emits |
| `0x0045df90-0x0045ea13` | [UID:0004IA] `RankingRewardInfoDialogOnPaint.md` | primary virtual paint method | TRUE | `0000BP` | `90/92` | registered; formal body emits |
| `0x0045ea20-0x0045ea5e` | [UID:0004IB] `RankingRewardInfoDialogBuildCloseButtonBoundsRaw.md` | retained/inlined private bounds method | TRUE | `0000BP` | `89/91` | registered; formal body emits |
| `0x0045ea60-0x0045ead9` | [UID:0004IC] `RankingRewardInfoDialogSendRewardInfoRequestRaw.md` | retained/inlined private request method | TRUE | `0000BP` | `90/92` | registered; formal body emits |
| `0x0045eae0-0x0045efd0` | [UID:0001ZI] existing | non-emitting vector-helper split index | FALSE | NONE | current `86/90` | keep with exact children |
| `0x0045efe0-0x0045efeb` | [UID:0000YA] existing | singleton-clear unwind helper | TRUE | `0000BP` | current `88/95` | keep |
| `0x0045eff0-0x0045f018` | [UID:0004ID] `RankingRewardEntryRangeDestroyCleanupFunclet.md` | compiler EH cleanup funclet | FALSE | `0000BP` semantic context | `88/93` | registered; no emitter/code |
| `0x0045f020-0x0045f0a7` | [UID:0001ZK] existing | source-declared/generated-binary vector destroy | TRUE | `0000BP` | current `86/90` | keep current disposition |
| `0x0045f0a8-0x0045f0be` | [UID:0000YB] existing filename ends `...f0bd` | compiler adjustor thunks | FALSE | NONE | current `88/92` | keep; document exclusive end `0x45f0be` |
| `0x0045f0c0-0x0045f12c` | [UID:0004IE] `RankingRewardInfoDialogScalarDeletingDestructor.md` | scalar-deleting destructor wrapper | FALSE | `0000BP` semantic context | `89/93` | registered; no emitter/code |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0045dba0` | code caller `0x00458abd` in `RankingDialog::OnButtonClick` | live construction route for action `5` |
| `0x0045dda0` | data xref `0x00610c0c` | primary vtable action slot |
| `0x0045ddd0` | data xref `0x00610c34` | secondary packet-listener slot |
| `0x0045df90` | data xref `0x00610c08` | primary paint slot |
| `0x0045dd60` | no xrefs/pointer hits | compiler cleanup body only |
| `0x0045ea20` | no xrefs/pointer hits | retained/inlined raw helper, no live route |
| `0x0045ea60` | no xrefs/pointer hits | retained/inlined raw helper, no live route |
| `0x0045efe0` | EH reference `0x005fa371` | constructor-unwind singleton clear |
| `0x0045eff0` | EH references `0x005fa3f3`, `0x005fa423` | shared reward-entry cleanup funclet |
| `0x0045f0c0` | calls from `0x45f0ae`/`0x45f0b9`, data xref `0x00610bc4` | adjustor and primary destructor routes |

## Documentation Evidence And IDA Status

- Current docs correctly identify the class purpose, packet behavior, reward entry/item layouts, vtables, singleton, and file route.
- Historical pre-callback stale/incomplete items were UID0001ZG direct-file metadata, missing exact child pages, missing padding rows, `SetHover`-style broad helper wording, and the claim that final source-level naming alone blocked the aggregate C++.
- Current generated state correctly omits parent UID0001ZG, includes the exact class/child sources including UID0001ZH, and omits compiler-only UID0004I8/ID/IE. The earlier five-child/missing-eight-page state is historical evidence only.
- Manual `by-memory/-coverage-report.md` is stale relative to current scores/reconstructability and is read-only in this assignment; it is not evidence authority.

## Ranked Ownership Analysis

### 1. RankingRewardInfoDialog class [UID:0000BP]

- Evidence for: exact constructor object, three class vtables, singleton publication/clear, virtual slots, class-only `+0x26c` vector, packet parsing, paint traversal, and existing class support declarations.
- Evidence against: raw bounds/request bodies do not consume `this`, and complete multiple-inheritance declarations are not finalized.
- Decision: selected direct semantic owner for the parent index and all feature-specific source children. The no-`this` raw bodies remain private/static spelling caveats, not ownership blockers.

### 2. RankingDialog file [UID:0000MZ]

- Evidence for: the compact ranking feature source route, address neighborhood, generated path, and all class/global children eventually emit there.
- Evidence against: it is not the narrowest semantic owner for class methods or class-owned vector state.
- Decision: terminal emitter/source file only, not direct owner of UID0001ZG.

### 3. RankingDialog class / RankingCategoryCollection / StringUtil

- Evidence for: `RankingDialog` opens the dialog and provides selected-category state; `RankingCategoryCollection` supplies the selected record; `StringUtil` owns the fixed wide formatter.
- Evidence against: none constructs or owns the reward-info object, vtables, singleton, vector, action, packet, or paint methods. The formatter is already correctly split to [UID:0003X8].
- Decision: dependencies or sibling owner only; rejected as parent owner.

### Proposed new file/grouping, if applicable

- Not applicable. `NexusTK/ui/dialogs/RankingDialog.cpp` remains the best compact source file.
- The full source family already includes `RankingDialog`, `RankingCategoryCollection`, `RankingEventListPane`, `RankingEventScrollPane`, `RankingRewardInfoDialog`, `RankingUserListPane`, related globals, and exact child methods.
- A standalone `RankingRewardInfoDialog.cpp` is rejected because no binary/source-path evidence outweighs the accepted compact ranking feature file.

## Source Placement

- Recommended placement: class methods/private helpers under `RankingRewardInfoDialog`, emitted through [UID:0000BP] to [UID:0000MZ] at `NexusTK/ui/dialogs/RankingDialog.cpp`.
- This fits the contiguous ranking dialog feature island, shared opcode `0x7d` protocol family, shared category state, singleton neighborhood, vtable family, and current generated source organization.
- Rejected placements: direct [UID:0000MZ] ownership, `RankingDialog` class, `RankingCategoryCollection`, `StringUtil`, generic `DialogPane`, `ProtocolSend`, and a new file.
- Disposition: model both raw helpers as private non-static `RankingRewardInfoDialog` methods. The bounds helper's `retn 8`, two explicit stack arguments, absent `ecx` use, adjacency, and constructor inline duplicate are consistent with an optimized-unused receiver; the request helper's adjacency and exact constructor inline duplicate support the same class-private route. Exact original spellings remain inferential and affect confidence only.

## Range / Split / Padding / Reclassification Analysis

- Exact parent range remains `0x0045dba0-0x0045f12c` half-open. Rename is not required; body/title should say `Split Index`.
- New children are the eight exact ranges listed in `Function / Child Inventory`; existing children remain nested.
- Record these verified padding spans in the parent inventory and `by-memory/-ignored.md`: `0x0045dd56-0x0045dd60` (10 bytes), `0x0045dd9a-0x0045dda0` (6), `0x0045ddc2-0x0045ddd0` (14), `0x0045df8c-0x0045df90` (4), `0x0045ea13-0x0045ea20` (13), `0x0045ea5e-0x0045ea60` (2), `0x0045ead9-0x0045eae0` (7), `0x0045efd0-0x0045efe0` (16), `0x0045efeb-0x0045eff0` (5), `0x0045f018-0x0045f020` (8), `0x0045f0a7-0x0045f0a8` (1), `0x0045f0be-0x0045f0c0` (2), and `0x0045f12c-0x0045f130` (4). Counts are direct byte-span facts; all bytes were confirmed `0xcc` in MCP id `24`/bounded disassembly.
- [UID:0000YB] filename uses an inclusive-looking `...f0bd`; its body should explicitly state the two 11-byte thunks occupy half-open `0x0045f0a8-0x0045f0be`. Do not rename it in this callback unless the validator/supervisor requests range normalization.
- Parent impact: set `RECONSTRUCTABLE:FALSE`, clear emitters/code, direct owner `0000BP`, retain `Nested:0`, and enumerate children/padding low-to-high.

## Negative Evidence Summary

- No direct xrefs or absolute address bytes exist for the three retained raw starts. They cannot be described as live ordinary callees, but the two source helpers have constructor inline duplicates and complete independent bodies; route-negative evidence therefore caps liveness confidence without suppressing source output.
- Address adjacency alone does not make vector helpers, formatter, EH funclets, or thunks handwritten class methods.
- Consumer access to `g_pRankingDialog`, `g_packetSender`, `g_pItemObjImageLib`, graphics helpers, or `StringUtil` does not transfer ownership.
- No current type record proves original private helper spellings or the exact typedef token used for the 40-byte paint record. Binary layout and generated vector mechanics nevertheless support `std::vector` and the private method dispositions strongly enough for first drafts.
- No source-path or cross-file call evidence supports a new `RankingRewardInfoDialog.cpp`.
- Broad scope and unresolved exact original spelling are not used as no-code proofs. After the additive repair, only compiler/EH/ABI children remain blank for target-specific reasons.

## IDA Rename / Type / Comment Recommendations

- Accepted source-facing names: constructor `RankingRewardInfoDialog::RankingRewardInfoDialog`, action `OnDialogAction`, packet override `HandlePacketEvent(Event *event)`, paint `OnPaint`, derived `std::vector<RankingRewardEntry> m_rewardEntries`, and private retained helpers `BuildCloseButtonBounds` / `SendRewardInfoRequest`. Keep `Raw` only in child filenames to preserve address-liveness provenance.
- Rejected/superseded historical packet spelling: `HandleRewardInfoPacket` was an earlier descriptive feature label, not the accepted source method name. The function occupies the inherited `EventHandler` packet-family slot `+0x10`, and accepted project interface/sibling evidence fixes the override signature as `HandlePacketEvent(Event *event)`. Do not use `HandleRewardInfoPacket` in source, declarations, IDA rename recommendations, or current-state claims.
- Proposed source member: `m_rewardEntries` at `+0x26c`, whose VC6/VC7 vector object stores begin/end/capacity at `+0x26c/+0x270/+0x274`; nested `RankingRewardEntry::items` uses the same `std::vector` model at `+0x224`.
- Evidence: constructor/packet/paint/destructor all use the same triplet; vtable slots establish virtual roles; DialogPane/packet/RectBounds support provides current project names.
- Intentionally unchanged: no IDA DB labels, prototypes, structs, comments, functions, or tail chunks. The accepted callback did not request IDA edits; exact original private spelling remains inferential.
- Safe future IDA changes after supervisor approval would be modeled-function labels for constructor/action/packet/paint only, with the packet label based on accepted `HandlePacketEvent`, not the superseded feature label. Creating raw functions or retyping EH tail chunks is unsafe in this callback.

## First-Draft C++ Recommendation

- Parent UID0001ZG: not eligible. After reclassification to `RECONSTRUCTABLE:FALSE`, its formal header value and multiline block must be blank.
- [UID:0000BP] destination `by-class/RankingRewardInfoDialog.md`: source-level vector model, parser dependency, and dialog declaration needed by the exact children. `FrameDrawRecord` is an alias of the same 40-byte `EPFTileContext` record used by ResourceLayout/GrafPort code. Exact destination formal block insertion text, including the required managed header:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
typedef EPFTileContext FrameDrawRecord;

class Event;

struct RankingRewardItem
{
    wchar_t name[256];
    int quantity;
    unsigned short iconId;
    unsigned char paletteIndex;
    unsigned char reserved_207;
};

typedef std::vector<RankingRewardItem> RankingRewardItemVector;

struct RankingRewardEntry
{
    RankingRewardEntry();
    int ParseFromPacket(const unsigned char *packet);

    wchar_t rankStartText[8];
    wchar_t rankEndText[8];
    wchar_t symbolText[256];
    char symbolFrameIndex;
    unsigned char symbolAttribute;
    unsigned char reserved_222[2];
    RankingRewardItemVector items;

    RankingRewardItem *ItemAt(int index);
};

typedef std::vector<RankingRewardEntry> RankingRewardEntryVector;

class RankingRewardInfoDialog : public DialogPane
{
public:
    RankingRewardInfoDialog();

    virtual bool HandlePacketEvent(Event *event);
    virtual void OnPaint();
    virtual void OnDialogAction(int actionId, int actionParam);

private:
    void BuildCloseButtonBounds(short layoutSelector, RectBounds *outBounds) const;
    void SendRewardInfoRequest();

    RankingRewardEntryVector m_rewardEntries;
};

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- [UID:0004I7] destination `by-memory/0x0045dba0-0x0045dd56.RankingRewardInfoDialogConstructor.md`: eligible constructor. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
RankingRewardInfoDialog::RankingRewardInfoDialog()
    : DialogPane(L"", static_cast<unsigned short>(-1), 0)
{
    g_pRankingRewardInfoDialog = this;

    RectBounds closeBounds;
    BuildCloseButtonBounds(0, &closeBounds);
    AddControl(new ImageButtonControlPane(14, &closeBounds));

    RectBounds dialogBounds;
    InitRectBoundsFromSize(&dialogBounds, 202, 104, 620, 500);
    OnCreate(&dialogBounds, 0, NULL, dword_69B36C);
    OnShow(NULL, g_pBackPane);
    SetFocusedControl(0);
    SetPendingControl(0);

    SendRewardInfoRequest();
    SlideOpenVertical();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- This constructor shape explains the binary without compiler scaffolding: base/member constructors generate the three vptr/vector writes, the two private calls are inlined at their only active call site, and `std::vector` default construction generates the `+0x26c/+0x270/+0x274` zeroes. Fresh MCP id `17` fixes the reusable control constructor order as `(imageId, bounds)`.
- [UID:0004I9] destination `by-memory/0x0045dda0-0x0045ddc2.RankingRewardInfoDialogOnDialogAction.md`: eligible action override. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingRewardInfoDialog::OnDialogAction(int actionId, int actionParam)
{
    (void)actionParam;

    if (actionId == 0)
    {
        SlideCloseVertical();
        g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(this);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- The body preserves the exact action-zero gate, close-animation call, deferred-delete call, and unused second parameter. It uses accepted source-facing DialogPane/BlackHole names and ordinary VC6/VC7-era style.
- [UID:0001ZH] destination `by-memory/0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler.md`: eligible packet-event override. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool RankingRewardInfoDialog::HandlePacketEvent(Event *event)
{
    const unsigned char *packet = event->packet;
    if (packet[0] != 0x7d || packet[1] != 5)
        return false;

    const unsigned int categoryId = PacketBufferReadUInt32BE(packet + 2);
    RankingCategoryRecord *category =
        g_pRankingDialog->m_categories.GetSelectedRecord();

    if (category != NULL &&
        static_cast<unsigned int>(category->GetCategoryId()) == categoryId)
    {
        const short entryCount =
            static_cast<short>(PacketBufferReadUInt16BE(packet + 6));
        int packetOffset = 8;

        for (int index = 0; index < entryCount; ++index)
        {
            RankingRewardEntry entry;
            packetOffset += entry.ParseFromPacket(packet + packetOffset);
            m_rewardEntries.push_back(entry);
        }

        InvalidateRect(&m_bounds);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- This body uses the accepted `EventHandler` packet-family signature and `Event::packet` member, preserves the binary's signed-positive count gate and append-without-clear behavior, lets `std::vector`/RAII reproduce deep copy and temporary cleanup, invalidates on every selected matching-category response including zero entries, and returns true for any matching opcode/subcommand even when the selected record is absent or differs.
- [UID:0004IA] destination `by-memory/0x0045df90-0x0045ea13.RankingRewardInfoDialogOnPaint.md`: eligible paint override. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingRewardInfoDialog::OnPaint()
{
    FrameDrawRecord frame;
    frame.Initialize();
    g_pEPFLib->LookupLayoutEntry(L"WBOARDBK.EPF", 0, &frame);

    m_drawMode = 1;

    RectBounds backgroundBounds = m_visibleBounds;
    backgroundBounds.left += 11;
    backgroundBounds.top += 42;
    backgroundBounds.right -= 11;
    backgroundBounds.bottom -= 40;
    DrawTiledBackground(&backgroundBounds, &frame, L"WBOARDBK.PAL");

    const int width = m_visibleBounds.right - m_visibleBounds.left;
    const int height = m_visibleBounds.bottom - m_visibleBounds.top;
    RectBounds sourceBounds;
    RectBounds destinationBounds;

    frame.Initialize();

    InitRectBounds(&sourceBounds, 0, 0, 37, 42);
    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, 37, 0);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 1, &frame);
    do
    {
        RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                        1, L"WEBBOARD.PAL", NULL);
        OffsetRect(&destinationBounds, 37, 0);
    }
    while (destinationBounds.right < width + sourceBounds.right);

    InitRectBounds(&sourceBounds, 0, 0, 37, 40);
    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, 37, height - 40);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 6, &frame);
    do
    {
        RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                        1, L"WEBBOARD.PAL", NULL);
        OffsetRect(&destinationBounds, 37, 0);
    }
    while (destinationBounds.right < width + sourceBounds.right);

    InitRectBounds(&sourceBounds, 0, 0, 11, 40);
    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, 0, 40);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 3, &frame);
    do
    {
        RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                        1, L"WEBBOARD.PAL", NULL);
        OffsetRect(&destinationBounds, 0, 40);
    }
    while (destinationBounds.bottom < height + sourceBounds.bottom);

    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, width - 11, 40);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 4, &frame);
    do
    {
        RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                        1, L"WEBBOARD.PAL", NULL);
        OffsetRect(&destinationBounds, 0, 40);
    }
    while (destinationBounds.bottom < height + sourceBounds.bottom);

    InitRectBounds(&sourceBounds, 0, 0, 37, 42);
    destinationBounds = sourceBounds;
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 0, &frame);
    RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                    1, L"WEBBOARD.PAL", NULL);

    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, width - 37, 0);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 2, &frame);
    RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                    1, L"WEBBOARD.PAL", NULL);

    InitRectBounds(&sourceBounds, 0, 0, 37, 40);
    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, 0, height - 40);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 5, &frame);
    RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                    1, L"WEBBOARD.PAL", NULL);

    destinationBounds = sourceBounds;
    OffsetRect(&destinationBounds, width - 37, height - 40);
    g_pEPFLib->LookupLayoutEntry(L"WEBBOARD.EPF", 7, &frame);
    RenderTileFrame(&frame, &sourceBounds, &destinationBounds,
                    1, L"WEBBOARD.PAL", NULL);

    SetTextColor(143);
    SetTextBackColor(128);

    RankingCategoryRecord *category =
        g_pRankingDialog->m_categories.GetSelectedRecord();
    if (category != NULL)
    {
        const wchar_t *title = category->GetTitleText();
        DrawOutlinedText(30, 64, title, static_cast<int>(wcslen(title)));
    }

    DrawOutlinedText(30, 86, L"Rank", 4);
    DrawOutlinedText(110, 86, L"Reward Legend", 13);
    DrawOutlinedText(290, 86, L"Reward Items", 12);

    Point cursor;
    cursor.y = 110;
    cursor.x = 30;
    frame.Initialize();

    const signed char entryCount =
        static_cast<signed char>(m_rewardEntries.size());
    for (int entryIndex = 0; entryIndex < entryCount; ++entryIndex)
    {
        RankingRewardEntry &entry = m_rewardEntries[entryIndex];

        if (wcscmp(entry.rankStartText, entry.rankEndText) != 0)
        {
            DrawOutlinedText(cursor.x, cursor.y, entry.rankStartText,
                             static_cast<int>(wcslen(entry.rankStartText)));
            cursor.x += 25;
            DrawOutlinedText(cursor.x, cursor.y, L"~", 1);
            cursor.x += 15;
            DrawOutlinedText(cursor.x, cursor.y, entry.rankEndText,
                             static_cast<int>(wcslen(entry.rankEndText)));
            cursor.x += 40;
        }
        else
        {
            DrawOutlinedText(cursor.x, cursor.y, entry.rankStartText,
                             static_cast<int>(wcslen(entry.rankStartText)));
            cursor.x += 80;
        }

        if (entry.symbolFrameIndex >= 0)
        {
            RectBounds symbolBounds;
            InitRectBoundsFromSize(&symbolBounds, cursor.x, cursor.y - 10, 10, 10);
            g_pEPFLib->LookupLayoutEntry(L"SYMBOLS.EPF",
                                         entry.symbolFrameIndex,
                                         &frame);
            g_pfnBlitSprite(this, &frame, &frame.bounds, &symbolBounds,
                            1, NULL, NULL);
            DrawOutlinedText(cursor.x + 20, cursor.y, entry.symbolText,
                             static_cast<int>(wcslen(entry.symbolText)));
        }

        cursor.x += 190;

        const signed char itemCount =
            static_cast<signed char>(entry.items.size());
        for (int itemIndex = 0; itemIndex < itemCount; ++itemIndex)
        {
            RankingRewardItem &item = entry.items[itemIndex];
            short itemId = static_cast<short>(item.iconId - 0x4000);
            if (item.iconId >= 0xc000)
                itemId = static_cast<short>(item.iconId);

            RectBounds itemBounds;
            GetItemGlyphBounds(itemId, &itemBounds);
            const int itemWidth = itemBounds.right - itemBounds.left;
            const int itemHeight = itemBounds.bottom - itemBounds.top;

            RectBounds drawBounds;
            if (itemHeight > 40 || itemWidth > 40)
            {
                const int drawWidth = itemWidth / 2;
                const int drawHeight = itemHeight / 2;
                const int left = cursor.x - drawWidth / 2 - 5;
                const int top = cursor.y - drawHeight / 2 - 5;
                InitRectBounds(&drawBounds, left, top,
                               left + drawWidth, top + drawHeight);
                g_pItemObjImageLib->DrawItemImageUnscaled(
                    this, &drawBounds, itemId, item.paletteIndex);
            }
            else
            {
                InitRectBounds(&drawBounds,
                               cursor.x - 25, cursor.y - 25,
                               cursor.x + 15, cursor.y + 15);
                g_pItemObjImageLib->DrawItemImageIn43x43Slot(
                    this, &drawBounds, itemId, item.paletteIndex, NULL, 0.0f);
            }

            cursor.x += 10;

            wchar_t label[256];
            swprintf_s(label, _countof(label), L"%s(%d)",
                       item.name, item.quantity);
            const int labelLength = static_cast<int>(wcslen(label));
            DrawOutlinedText(cursor.x, cursor.y, label, labelLength);
            cursor.x += GetTextWidth(label, labelLength) + 30;
        }

        cursor.y += 26;
        cursor.x = 30;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- The body deliberately uses accepted source-facing helpers rather than decompiler labels. The signed-byte loop counts preserve the binary's `char` cast/positive test for reward and item vector sizes; the frame order, loop bounds, cursor increments, item-id remap, size threshold, destination arithmetic, colors, text coordinates, and local formatting are direct from MCP id `7`.
- [UID:0004IB] destination `by-memory/0x0045ea20-0x0045ea5e.RankingRewardInfoDialogBuildCloseButtonBoundsRaw.md`: eligible retained private member. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingRewardInfoDialog::BuildCloseButtonBounds(
    short layoutSelector,
    RectBounds *outBounds) const
{
    if (layoutSelector != 0)
        InitRectBounds(outBounds, -1, -1, -1, -1);
    else
        InitRectBoundsFromSize(outBounds, 279, 468, 63, 24);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- [UID:0004IC] destination `by-memory/0x0045ea60-0x0045ead9.RankingRewardInfoDialogSendRewardInfoRequestRaw.md`: eligible retained private member. Exact destination formal block insertion text:

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingRewardInfoDialog::SendRewardInfoRequest()
{
    RankingCategoryRecord *category =
        g_pRankingDialog->m_categories.GetSelectedRecord();
    if (category == NULL)
        return;

    unsigned char packet[7];
    PacketBufferWriteUInt8(0x7d, &packet[0]);
    PacketBufferWriteUInt8(5, &packet[1]);
    PacketBufferWriteUInt32BE(category->GetCategoryId(), &packet[2]);
    packet[6] = 0;

    g_packetSender->QueueAndSendPacket(packet, 6);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- For both retained helpers, the child filename may retain `Raw` to document the no-function/no-xref IDB state; the source method name should omit `Raw`. The constructor should call both methods, allowing ordinary optimization to reproduce the observed inline bodies while retaining the exact out-of-line copies.
- Constructor-unwind cleanup, reward-entry cleanup funclet, adjustor thunks, and scalar-deleting wrapper: `RECONSTRUCTABLE:FALSE`, blank emitters, blank formal C++. They are regenerated from declarations/member destruction/ABI, not handwritten bodies.
- Existing generated vector-helper dispositions remain unchanged because the `std::vector` declarations regenerate their ABI mechanics. UID0001ZH now emits its source-authored packet-event body; adjacent parser UID0001ZF separately declares `ParseFromPacket` and retains its own parser/accessor-page decision.
- Third-party import directive: not applicable; all spans are NexusTK/compiled support, not a vetted third-party source embed.

## Final Recommendation

- Applied UID0001ZG non-emitting split-index classification and moved canonical ownership from direct file UID0000MZ to class UID0000BP.
- Registered exact children UID0004I7-UID0004IE low-to-high and recorded all thirteen padding spans.
- Emitted exact constructor, `OnDialogAction`, `HandlePacketEvent`, `OnPaint`, private close-bounds, and private request first drafts; only compiler/EH/ABI blocks remain blank in the core source-method inventory.
- Preserved existing vector/singleton/thunk children and the StringUtil formatter exclusion; upgraded existing packet child UID0001ZH additively without rewriting accepted history.
- Treat exact original private spellings and the `FrameDrawRecord` alias token as documented confidence caps, not future-work gates. No source-readiness blocker remains.

## Recommended Target Doc Changes

- Target path: `by-memory/0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore.md`.
- Applied metadata: `COMPLETION:91`, `CONFIDENCE:93`, `CANONICAL_OWNER:0000BP`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, blank formal C++.
- Applied title `RankingRewardInfoDialogCore Split Index`; filename and range remain unchanged.
- Applied exact child/padding inventory with validator-assigned UID0004I7-UID0004IE.
- Incorporated MCP health/function/xref/raw/caller/vtable/layout/source-placement/rejected-alternative evidence while preserving B001/B006 history as historical.

## Recommended Support Doc Changes

- `by-class/RankingRewardInfoDialog.md`: original callback applied `87/91 -> 90/92`; additive packet repair moved it to `91/93`, added accepted `Event`/`ParseFromPacket`/`HandlePacketEvent` declarations, retained `std::vector` aliases and complete `DialogPane`-derived declaration, and routes all six source-authored core behaviors.
- `by-class/ImageButtonControlPane.md`: score unchanged; applied `ImageButtonControlPane(int imageId, const RectBounds *bounds)` with MCP argument/store evidence.
- `by-memory/0x004ba6b0-0x004ba81d.GrafPortDrawTiledBackground.md` / `by-class/GrafPort.md`: already contained accepted `FrameDrawRecord` API support; no rewrite or score change was needed. UID0000BP now supplies `typedef EPFTileContext FrameDrawRecord`.
- `by-file/RankingDialog.md`: score unchanged `88/91`; applied exact UID0000BP child route and non-emitting-index wording.
- `by-type/by-vtable/RankingDialogVtableFamily.md`, `by-memory/0x00610980-0x00610e38.RankingDialogReadOnlyData.md`, and `by-global/g_pRankingRewardInfoDialog.md`: added exact source/compiler lifecycle links with metadata unchanged.
- `by-memory/-ignored.md`: added thirteen padding spans and preserved/corrected the UID0000YB thunk half-open endpoint without renaming its file.
- UID0001ZH: moved `87/91 -> 91/93`, replaced the stale formal no-code comment with `HandlePacketEvent(Event *event)`, and updated class/core/file/vtable/parser support. UID0001ZI, UID0000YA, UID0001ZK, UID0000YB, UID0003X6-UID0003X9, UID000202, and UID000204 retain their accepted ownership/source decisions; UID0003X8 remains StringUtil-owned.

## Score And Metadata Recommendation

- Historical target before callback: `86/90`, direct file owner, reconstructable/emitter true, blank C++.
- Current implemented target: `91/93`, class owner `0000BP`, `RECONSTRUCTABLE:FALSE`, no emitter/code.
- Completion rises because every contained method, retained raw body, existing child, compiler span, boundary, padding gap, owner route, and C++ disposition is implementation-ready, and all six source-authored core behaviors now have formal first drafts. Confidence rises because fresh MCP independently confirms constructor dependencies, EventHandler packet interface/payload/invalidation, full paint flow, inline/raw parity, exact strings/resources, private-member ABI evidence, and the vector source model.
- Score remains below `95` because exact original private method spellings and the historical `FrameDrawRecord` typedef token are inferred rather than symbol-proven; these are confidence caps only, not implementation blockers.
- Score-improvement attempts:
  - Missing function inventory: resolved through MCP function enumeration and exact raw disassembly.
  - Constructor/control layout: resolved through caller allocation, fresh constructor decompile, fresh ImageButton constructor decompile, RectBounds support, sibling control constructors, and DialogPane helper support; exact first draft supplied.
  - Caller/liveness: resolved for modeled functions; raw helper no-route status exhausted through xrefs/pointer bytes and reinterpreted with constructor inline parity plus `retn 8` private-member evidence; both source bodies supplied.
  - Packet/reward interactions: resolved through current MCP plus accepted `HandlePacketEvent(Event *event)`, `Event::packet`, ranking accessors, parser declaration, `std::vector`, and `InvalidateRect(&m_bounds)` APIs; exact formal UID0001ZH body supplied and implemented.
  - Paint behavior: resolved through full fresh decompile, exact resource/string bytes, accepted GrafPort/EPFTileContext/ResourceLayout/item-image APIs, and `std::vector` layout; exact first draft supplied.
  - Owner/source placement: resolved to UID0000BP -> UID0000MZ.
  - Split/container: resolved to false index with exact children.
  - Formal C++: class/declarations, constructor, OnDialogAction, HandlePacketEvent, OnPaint, retained bounds, and retained request methods resolved; only compiler/EH/ABI spans retain exact no-code dispositions within the core inventory.
- Metadata fields not listed above remain unchanged/blank.

## Open Questions With Attempted Resolution

- **Was each raw helper a private member or file-static helper?** Checked function objects, xrefs, pointer bytes, constructor inline duplicates, address neighborhood, class/file docs, and analogous retained helpers. Resolve both as private non-static methods: the bounds helper's `retn 8` consumes its two explicit stack arguments while the receiver is optimized unused, and both out-of-line bodies exactly match constructor-inline behavior. The exact original names remain inferential, but both formal bodies are source-ready.
- **Can constructor C++ be emitted now?** Yes. The fresh `ImageButtonControlPane(int imageId, const RectBounds *bounds)` decompile fixes dependency order and ABI interpretation; DialogPane, RectBounds, control registration, dialog lifecycle, vector, and packet APIs cover the remaining operations. The exact first draft is supplied above.
- **Can paint C++ be emitted now?** Yes. The full paint decompile and exact resource/string bytes, together with accepted GrafPort, EPFTileContext/FrameDrawRecord, ResourceLayout, item-image, text, and reward-vector APIs, cover every operation. The exact first draft is supplied above without forwarding invented APIs.
- **Can UID0001ZH packet C++ be emitted now?** Yes. Refreshed MCP on `cfa0ae1a` proves the secondary EventHandler slot, `event + 0x0c` packet payload, readers/accessors/parser/vector flow, signed-positive count gate, and `InvalidateRect(&m_bounds)` call. Existing EventHandler, ExchangeDialog, CollectionPane, ranking, PacketBuffer, and Pane docs provide the source-facing names. The exact first draft is supplied above and implemented.
- **Should scalar wrapper emit a destructor?** Checked wrapper order, thunks, vtable slot, constructor unwind, class support, and scalar-destructor precedent. No: observed vector-before-singleton-clear ordering cannot safely be represented as a simple destructor body without the final inheritance/member destruction model. Class declarations should regenerate ABI glue.
- **Should vector children be reclassified false?** Checked accepted B001/B006 decisions and current metadata. No change in this callback: those pages intentionally document source-declared/generated-binary deep-copy/destruction behavior and have accepted direct class routes. Parent reclassification does not require reopening them.
- No question remains as an open-ended source-readiness blocker. Exact private/parser spellings and the `FrameDrawRecord` alias token remain confidence caps only; they do not withhold any of the six source-authored core bodies.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- No manual coverage/tracker text is required. UID0004I7-UID0004IE and UID0001ZH metadata/formal blocks drove validator-owned generated coverage/source, and additive waited command `000000008342` completed the current generated refresh.
- `by-memory/-coverage-report.md` and all `auto-generated/-ag-*` files were not manually edited. The exact low-to-high inventory and thirteen padding spans remain in the source parent/ignored docs.

## Follow-Up Actions

- Callback work is complete; no B002 or A-agent implementation item remains.
- Supervisor validation, report execution, archive movement, and lifecycle history are external validator-owned actions and are intentionally not embedded here as pending report instructions.

## Confidence

- Recommendation confidence: high (`93`) for parent split/owner/source route, child boundaries, and repaired source readiness.
- Score confidence: high; `91/93` reflects the exact split inventory, owner route, complete first drafts, and explicit compiler-artifact dispositions.
- Remaining uncertainty: exact original private helper names and the original `FrameDrawRecord` alias token. Both are documented inference choices and do not block implementation.

## Validator Results

- New-child scoped validators, all exit `0` / `ok:1`: `000000008254` (`16:52:03`, UID0004I7), `000000008260` (`16:52:28`, UID0004I8), `000000008264` (`16:52:54`, UID0004I9), `000000008268` (`16:53:56`, UID0004IA), `000000008271` (`16:54:23`, UID0004IB), `000000008273` (`16:54:57`, UID0004IC), `000000008274` (`16:55:36`, UID0004ID), and `000000008275` (`16:56:01`, UID0004IE). Command shape: `python .\tools\validator.py --mode file --file <child> --apply --queue-timeout 240`.
- Core support scoped validators, all exit `0` / `ok:1`: `000000008276` target, `000000008277` UID0000BP class, `000000008278` UID0000MZ file, and `000000008279` UID00006D ImageButton support.
- Cross-reference/vector/padding scoped validators, all exit `0` / `ok:1`: `000000008280` vtable, `000000008281` rdata, `000000008282` global, `000000008283` ignored ledger, `000000008284` UID000202, `000000008285` UID000204, `000000008286` UID0001ZH, `000000008287` UID0001ZI, `000000008288` UID0000YA, `000000008289` UID0001ZK, `000000008290` UID0000YB, and `000000008291`-`000000008294` UID0003X6-UID0003X9.
- Historical callback waited command: `python .\tools\validator.py --mode file --file by-class\RankingRewardInfoDialog.md --apply --wait-generated --queue-timeout 240`; command `000000008295`, timestamp `2026-07-12T17:03:13-04:00`, exit `0`, `ok:1`, `generated_refresh:completed`.
- Additive UID0001ZH scoped validators, all exit `0` / `ok:1`: `000000008336` UID0001ZH (`91/93`), `000000008337` UID0000BP (`91/93`), `000000008338` UID0001ZF parser dependency clarification, `000000008339` UID0001ZG core reconciliation, `000000008340` UID0000MZ current route, and `000000008341` UID0001YJ vtable slot. Command shape: `python .\tools\validator.py --mode file --file <path> --apply --queue-timeout 240`.
- Current final waited command: `python .\tools\validator.py --mode file --file by-memory\0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler.md --apply --queue-timeout 240 --wait-generated`; command `000000008342`, timestamp `2026-07-12T17:30:01-04:00`, exit `0`, `ok:1`, `generated_refresh:completed`.
- Diagnostics: early commands reported missing validator mappings for existing UID0003X6-UID0003X9; scoped commands `000000008291`-`000000008294` restored those mappings. `by-memory/-ignored.md` still reports broad pre-existing missing-reference diagnostics (`341`) and final autogen reports project-wide `children_marker_missing`/`emitter_has_no_code` warnings; none is specific to UID0001ZG or its new children.
- Read-only generated verification: current `RankingDialog.cpp` content header is command `000000008338` at `2026-07-12T17:29:45-04:00`, older than final waited command `000000008342` only because no later content delta required rewriting the file. Direct inspection after `000000008342` confirms no UID0001ZG marker or raw vector pointer names; UID0000BP is `91/93` with `Event`, `ParseFromPacket`, and `HandlePacketEvent` declarations; UID0004I7/I9/IA/IB/IC and UID0001ZH bodies are present; UID0004I8/ID/IE are absent as required.

## Changed Files

- Created: eight exact child pages `by-memory/0x0045dba0-0x0045dd56.RankingRewardInfoDialogConstructor.md`, `0x0045dd60-0x0045dd9a.RankingRewardInfoDialogConstructorUnwindCleanup.md`, `0x0045dda0-0x0045ddc2.RankingRewardInfoDialogOnDialogAction.md`, `0x0045df90-0x0045ea13.RankingRewardInfoDialogOnPaint.md`, `0x0045ea20-0x0045ea5e.RankingRewardInfoDialogBuildCloseButtonBoundsRaw.md`, `0x0045ea60-0x0045ead9.RankingRewardInfoDialogSendRewardInfoRequestRaw.md`, `0x0045eff0-0x0045f018.RankingRewardEntryRangeDestroyCleanupFunclet.md`, and `0x0045f0c0-0x0045f12c.RankingRewardInfoDialogScalarDeletingDestructor.md`.
- Modified target/core support: UID0001ZG target, `by-class/RankingRewardInfoDialog.md`, `by-file/RankingDialog.md`, `by-class/ImageButtonControlPane.md`, and UID0001ZF parser dependency documentation.
- Modified lifecycle/data support: `by-type/by-vtable/RankingDialogVtableFamily.md`, `by-memory/0x00610980-0x00610e38.RankingDialogReadOnlyData.md`, `by-global/g_pRankingRewardInfoDialog.md`, and `by-memory/-ignored.md`.
- Modified existing exact children: UID000202, UID000204, UID0001ZH, UID0001ZI, UID0000YA, UID0001ZK, UID0000YB, and UID0003X6-UID0003X9. Additive UID0001ZH repair changed the handler, class, parser support, core, file route, and vtable family only.
- Modified report: this report only inside B002's owned research folder.
- Renamed: none.
- Leases used: the two original short B002 batches plus one additive short batch for UID0001ZH, UID0000BP, UID0001ZF, UID0001ZG, UID0000MZ, and UID0001YJ; every lease was released immediately after its edit/scoped-validator batch. New child pages and this report required no lease.
- Validator-owned side effects: generated metadata/reports, tracker state, registry mappings, and generated C++ refreshed through commands above; no generated or coverage file was manually edited.
- Report execution: not run. No lifecycle, move, or archive command was run.

## Implementation Tracking Checklist

- [x] Supervisor Gate 1 accepted SHA256 `F3D6E5BFF0E8D29279E653DF659953B4DC0F628896B7D686A0043BD648C681D7` for implementation.
- [x] Updated UID0001ZG to `91/93`, owner `0000BP`, non-reconstructable/non-emitting split index with exact inventory.
- [x] Created and registered UID0004I7-UID0004IE low-to-high with accepted scores, owners, `Nested:4`, formal bodies, or exact compiler no-code proof.
- [x] Preserved/updated UID0001ZI, UID0000YA, UID0001ZK, UID0000YB, UID0003X6-UID0003X9, UID000202, and UID000204; UID0003X8 remains StringUtil-owned.
- [x] Reopened UID0001ZH with current MCP, replaced its stale no-code block with behavior-identical `HandlePacketEvent(Event *event)`, raised it to `91/93`, and synchronized UID0001ZF dependency wording.
- [x] Updated UID0000BP to `91/93` with `std::vector` aliases, complete class declaration, `Event`/`ParseFromPacket`/`HandlePacketEvent` declarations, object/tail layout, method/compiler inventory, and child routing.
- [x] Updated UID0000MZ without score change; UID0001ZG is a non-emitting index and six source-authored core behaviors route through UID0000BP.
- [x] Updated vtable, rdata, global, and ImageButton support at report-level detail without unrelated metadata changes.
- [x] Added all thirteen verified `0xcc` spans to parent and ignored ledger; preserved UID0000YB and documented exact half-open thunk end.
- [x] Kept no new source file, no IDA DB edit, no third-party import, and no manual generated/coverage/supervisor edit.
- [x] Preserved ZG-01 through ZG-16 and added applied ZG-17/ZG-18 managed-block and UID0001ZH closure claims with destination proof.
- [x] Preserved B001/B006 history, raw no-route/type-query negatives, rejected source placements, and compiler-artifact evidence.
- [x] Ran one scoped validator per changed/new by-* file; additive commands `000000008336`-`000000008341` and final waited `000000008342` all exited `0` with `ok:1`.
- [x] Verified generated `RankingDialog.cpp` content header `000000008338`, no UID0001ZG marker/raw vector pointers, six source-authored core bodies including UID0001ZH, and non-emission of compiler children.
- [x] Released all leases and did not run `execute_report`, lifecycle, move/archive, or subagent commands.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000008397","destination_path":"executed-b-agent-research/B002/0001ZG-RankingRewardInfoDialogCore-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/0001ZG-RankingRewardInfoDialogCore-source-quality.md","timestamp":"2026-07-12T18:11:01-04:00","uid":"0001ZG"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
