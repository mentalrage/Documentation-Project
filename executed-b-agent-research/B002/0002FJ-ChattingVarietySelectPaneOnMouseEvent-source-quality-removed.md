<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B002\0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable Code Archive

- Source report: [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md)
- Source report SHA256 before cleanup: `B4009622ED67B8D1AC188C023F5F63225022B01D5788CE7F00640F76ED3347B4`
- Cleanup generated: `2026-08-11T12:47:02Z`
- Removed executable blocks: `2`
- Scope: exact executable or command-shaped fenced blocks removed from the source report by supervisor housekeeping.
- Authority: this file is not a B-agent research report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: do not execute, import, evaluate, or use any block in this archive as an operational instruction.

## Removed Block R001

- Original source lines: `359-363`
- Original language: `powershell`
- Block SHA256: `61F39C536CEC693E83EAC5E2C71FB60B8D4699BB959EA0FFCC7CC4E18DB80BAF`

```powershell
$idb = 'E:\NTK\Resources\NexusTK\NexusTK.exe.i64'
$probe = [System.IO.File]::Open($idb, [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::None)
try { if (-not $probe.CanRead) { throw 'Exclusive canonical-IDB probe is not readable.' } } finally { $probe.Dispose() }
```

## Removed Block CLEANUP-20260813-0002FJ-OUTSIDE21-RUNBOOK

Source: [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md)

This archive is non-authoritative and must never be executed. It preserves the exact report lines removed during supervisor cleanup.

<!-- BEGIN CLEANUP-20260813-0002FJ-OUTSIDE21-RUNBOOK -->
55: - The primary supervisor completed the thirteen atomic IDA mutations and their full protection/persistence proof. Section 21 now records both that dated accepted transaction and the literal reproducible package corrected for current MCP capabilities: function renames use `rename` with `pure:true`, comments use the channel-specific endpoint, every `idb_open` disables auto-analysis, and worker closure is proved by exact process identity plus an exclusive read-only file probe rather than unavailable `idb_close` or stale registry-row disappearance.
64: - Historical primary-supervisor Gate 2B retry evidence from fresh session `supervisor-b002-uid0002fj-gate2b-retry1`: after all six accepted renames were applied in memory and exact I002-T `set_type` ran at `0x481470`, immediate complete `stack_frame` readback returned exactly two rows: `__return_address` at offset `0x4`, size `0x4`, type `_UNKNOWN *`; and `event` at offset `0x8`, size `0x4`, type `Event *`. This disproves the former return-row-only post-type expectation while confirming the one-explicit-argument `retn 4` ABI. Worker PID `25088` was identity-checked and terminated without save. The canonical IDB and timestamped prestate backup both remained 143,197,232 bytes with SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`; none of the in-memory renames or type change persisted. This is dated failed-transaction evidence, not a claim that Gate 2B applied or saved any recommendation.
65: - Historical primary `GATE2B_FAILED_PRE_SAVE` audit for exact report SHA256 `2A690241A92B530F5656DD71E1E733F2DDBC874E1EEB1478A140403F19469ECD`: two fresh exact-P0 workers independently returned literal MCP `stack_frame` row `size 0x2`, type `wchar_t`, for I003 `var_210`/`Buffer`/`Destination` and I005/I006 `Buffer`. The frozen report had conflated those API row sizes with separately inferred array-like storage spans. The primary failed closed before every rename, type edit, comment write, or save. Canonical P0 remained SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes, mtime `2026-08-05T22:14:13.0368626Z`; mutation count `0`, save count `0`, transaction worker and verifier absent, and no disk delta. This is historical pre-save failure evidence only and grants no Gate 2B or action credit.
66: - Accepted primary Gate 2B outcome: a later one-save transaction started from byte-identical backup/P0 SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, applied the six accepted function names, exact OnDismiss type, and six function-regular comments, passed all item/frame/comment/byte/xref/UDT/sibling protections, and fresh-reopen persistence. Closed-disk S1 is SHA256 `2ACC15A3372DB71C34296F263700AC30C18ADDD39D4E1F3BB5D6035B278B1024`, 143,197,232 bytes. Exact PID/process absence, PID-reuse checks, no active canonical session, exact disk tuple, and a successful read-only Windows `FileShare.None` probe are the authoritative closure proof; stale inactive `idb_list` rows are non-authoritative and need not disappear.
68: - The active session identity may rotate. Gate 2B must begin with fresh `idb_list`, `server_health`, and canonical disk hash/size/mtime; no action may rely on this report's session/PID as permanently current.
113: - Endpoint audit: current live tools/schema and installed implementation were checked for `rename pure:true`, `set_function_comments`, `set_address_comments`, and `idb_open run_auto_analysis:false`; the transaction package below matches the exact supported channel/side-effect boundaries.
157: | C0002FJ-032 | 0002FJ | Complete the ordered Gate 2B transaction with corrected API-row/storage-span handling, exact channel-specific operations, one save, process/file-lock closure proof, closed-disk S1, and fresh-reopen persistence | Exact | Accepted primary transaction advanced byte-identical P0 `29AA9F5B...1028D` to persisted S1 `2ACC15A3...1024`; reproducible schema now binds function renames to `pure:true` and never requires unavailable `idb_close` | IDA / Gate 2B transaction | Primary supervisor | incorporate | applied |
635: | C0002FJ-032 | Primary supervisor | incorporate | [x] Accepted one-save P0-to-S1 transaction completed all thirteen actions and persistence proof; reproducible package now uses `rename pure:true`, channel-specific comments, `run_auto_analysis:false`, and PID/file-lock closure without `idb_close`. | applied |
<!-- END CLEANUP-20260813-0002FJ-OUTSIDE21-RUNBOOK -->

## Removed Block CLEANUP-20260813-0002FJ-SECTION21

Source: [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md)

This archive is non-authoritative and must never be executed. It preserves the exact report text removed during supervisor cleanup.

<!-- BEGIN CLEANUP-20260813-0002FJ-SECTION21 -->
## IDA Rename / Type / Comment Recommendations

All prestates below are the exact P0 state used to define the accepted actions. The primary supervisor completed and persisted those actions at S1. The literal package remains as reproducibility, rollback, and future-audit evidence; any re-performance must dynamically rebind and compare current state and must not assume P0 remains current.

The thirteen mutations are atomic. A rename, type change, and comment write are separate rows and separate tool calls even when they affect the same function. Every function rename uses literal `rename` batch option `pure:true`; current implementation then performs only the IDA name change and skips `/vibe/` function-directory linkage and decompiler refresh. Pure mode does not support local/stack renames, and none is requested. `set_function_comments` changes only function-regular text through `idc.set_func_cmt(..., False)`; address regular, address repeatable, and function-repeatable channels are protected independently. `set_address_comments` is the supported regular-address-comment endpoint, but no address-comment set or clear is intended here, so it is not permitted in this transaction.

Historical failed transaction refinement: session `supervisor-b002-uid0002fj-gate2b-retry1` applied the six planned renames in memory and then exact I002-T. The immediate post-type `stack_frame` result was the exact two-row state specified below, not the former one-row expectation. The supervisor failed closed, identity-checked and terminated PID `25088` without save, and proved both canonical disk and backup remained SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes. No recommendation from that attempt is applied or persisted; a fresh transaction must begin from dynamically rebound disk state.

Historical primary `GATE2B_FAILED_PRE_SAVE` refinement for exact report SHA256 `2A690241A92B530F5656DD71E1E733F2DDBC874E1EEB1478A140403F19469ECD`: two fresh exact-P0 workers independently returned API row `size 0x2`, type `wchar_t`, for I003 `var_210`/`Buffer`/`Destination` and I005/I006 `Buffer`. The former larger values describe offset-derived storage spans, not literal MCP row sizes. The audit stopped before any dry-run mutation, rename, type edit, comment write, or save; P0 stayed SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes, mtime `2026-08-05T22:14:13.0368626Z`, with transaction worker and verifier absent and no disk delta. This audit grants no action or Gate 2B credit.

Accepted primary Gate 2B superseding outcome: the one-save transaction used the exact action/protection intent below, persisted all thirteen actions from P0 `29AA9F5B...1028D` to S1 `2ACC15A3...1024`, and passed complete fresh-reopen readback. Current endpoint feasibility is narrower than the historical default `rename` behavior: the reproducible calls below require `pure:true`, and acceptance requires no `/vibe/`, decompiler-refresh, local, or stack side effect. The report does not retroactively infer an unrecorded call field; action credit rests on the supervisor-supplied saved poststate/protection proof, while this literal package is the mandatory safe form for any future replay.

| ID | Address | Literal current prestate / prerequisite | Exact one-item action | Required immediate readback |
| --- | --- | --- | --- | --- |
| I001 | `0x4811c0` | Function `ChattingVarietySelectPane__HandlePointerOrMouseEvent`; type `bool __thiscall(ChattingVarietySelectPane *this, Event *event)`; function regular comment exact target dispatcher sentence; function repeatable/address regular/address repeatable blank; code item `[0x4811c0,0x481461)`; exact seven-row frame is listed below | No mutation; protect | Same name/type/all four comment channels, every literal seven-row frame name/offset/width/type, body hash, vtable xref, bounded inbound/outbound xrefs, and internal comments |
| I002-R | `0x481470` | Gate-start name `sub_481470`; type `char __thiscall(char *this, int)`; four entry comment channels blank; internal address-regular `0x48147e = this`; code item `[0x481470,0x48148f)`; bytes end `c2 04 00`; complete pre-type `stack_frame` is exactly `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`; no destination-name collision | Rename function to `ChattingVarietySelectPane__OnDismiss` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; old type; exact one-row pre-type frame; four entry channels, internal `this`, item, bytes, inbound xref `0x614f88`, and bounded outbound xrefs unchanged |
| I002-T | `0x481470` | I002-R name present; old type still `char __thiscall(char *this, int)`; physical tail bytes `c2 04 00`; complete pre-type `stack_frame` is exactly one row, `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`; logical current `arg1` is one 4-byte stack argument | Set type to `bool __thiscall ChattingVarietySelectPane__OnDismiss(ChattingVarietySelectPane *this, Event *event)` | Inspection-only `lookup_funcs` exact-address/name readback returns the exact new function type with one `Event *event`; `stack_frame` returns exactly two rows: unchanged `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`, plus permitted `event` offset `0x8`, size `0x4`, type `Event *`; `get_bytes` retains exact body hash/tail `c2 04 00`; item, xrefs, and all comment channels remain exact; no decompile or other analysis-producing readback is permitted |
| I002-C | `0x481470` | I002-R/I002-T name/type present; complete post-type frame is exactly the I002-T return-plus-event two-row state; function-regular blank; function-repeatable/address-regular/address-repeatable blank; internal address-regular `0x48147e = this` | Set function-regular comment exactly `ChattingVarietySelectPane dismissal handler. Ignores the Event argument, refreshes the compact variety pane, marks this popup for deferred deletion, and returns false.` | Exact function-regular text; exact two-row post-type frame unchanged; other three entry channels blank; internal `0x48147e = this`; name/type/body/xrefs unchanged |
| I003-R | `0x5a5110` | Function `sub_5A5110`; type `LineInputPane *__thiscall(_DWORD *this)`; four entry channels blank; body `[0x5a5110,0x5a5337)`; no destination collision | Rename function to `OpenInputPaneForCurrentSayMode` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; current type, all comments, complete literal API frame rows and separately inferred storage spans, body, and bounded inbound/outbound xrefs unchanged |
| I003-C | `0x5a5110` | I003-R name present; type `LineInputPane *__thiscall(_DWORD *this)`; function-regular blank; other three entry channels blank | Set function-regular comment exactly `Dispatches UserPane::m_currentSayMode to default, shout, whisper, group, or clan input-pane creation.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage spans/body/bounded inbound-outbound xrefs/internal comments unchanged |
| I004-R | `0x5a5450` | Function `___std_parallel_algorithms_hw_threads@0_10`; type `void *__cdecl()`; four entry channels blank; body `[0x5a5450,0x5a54a4)`; no destination collision | Rename function to `OpenSayToUserNameInputPane` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal frame/body/sole inbound xref unchanged |
| I004-C | `0x5a5450` | I004-R name present; type `void *__cdecl()`; function-regular blank; other three entry channels blank | Set function-regular comment exactly `Opens the recipient-name input pane used by the Whisper selector row.` | Exact new function-regular; name/type/other channels/complete literal frame/body/sole inbound xref/internal comments unchanged |
| I005-R | `0x5a5570` | Function `sub_5A5570`; type `LineInputPane *()`; four entry channels blank; body `[0x5a5570,0x5a563a)`; no destination collision | Rename function to `OpenSayToGroupMessageInputPane` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals unchanged |
| I005-C | `0x5a5570` | I005-R name present; type `LineInputPane *()`; function-regular blank; other three entry channels blank | Set function-regular comment exactly `Opens the group-message input pane with the "!!" command prefix and "Group => " prompt.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals/internal comments unchanged |
| I006-R | `0x5a5640` | Function `sub_5A5640`; type `LineInputPane *()`; four entry channels blank; body `[0x5a5640,0x5a570a)`; no destination collision | Rename function to `OpenSayToClanMessageInputPane` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals unchanged |
| I006-C | `0x5a5640` | I006-R name present; type `LineInputPane *()`; function-regular blank; other three entry channels blank | Set function-regular comment exactly `Opens the clan-message input pane with the "!" command prefix and "Clan => " prompt.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals/internal comments unchanged |
| I007-R | `0x5a5710` | Function `sub_5A5710`; type `LineInputPane *__thiscall(_DWORD *this)`; four entry channels blank; body `[0x5a5710,0x5a5791)`; no destination collision | Rename function to `OpenShoutInputPane` with one-item `rename` batch and `pure:true` | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal frame/body/three inbound xrefs/literal unchanged |
| I007-C | `0x5a5710` | I007-R name present; type `LineInputPane *__thiscall(_DWORD *this)`; function-regular blank; other three entry channels blank | Set function-regular comment exactly `Sets shout mode and opens a ShoutInputPane initialized with the "! " prompt.` | Exact new function-regular; name/type/other channels/complete literal frame/body/three inbound xrefs/literal/internal comments unchanged |

Complete literal physical-frame protections and row roles:

For `stack_frame`, `size` is the literal API-reported size of the row's declared scalar type, not the allocation extent inferred for an array-like local. Every affected `wchar_t` row must read back with exact API `size 0x2` and type `wchar_t`. Its storage span is protected separately from that row's offset through the next frame-row offset. An inferred storage span must never be compared to or substituted for the API row size.

- I001 target frame, protected with no permitted row delta: local `var_10`, offset `0x10`, width `0x4`, type `_DWORD`; local `var_C`, offset `0x14`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x1c`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x20`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x24`, width `0x4`, type `_UNKNOWN *`; unaffected first stack-argument row `Block`, offset `0x28`, width `0x4`, type `void *`, backing the already-typed logical `Event *event` without authorizing a frame edit; protected analyzer/EH artifact `arg_4`, offset `0x2c`, width `0x4`, type `_DWORD`, not a second source argument.
- I002 pre-type frame has no local or saved-register row and exactly one protected return-address row, `__return_address`, offset `0x4`, width `0x4`, type `_UNKNOWN *`; the old prototype's logical four-byte `int arg1` is not materialized as a physical row. The sole permitted frame delta from I002-T is addition of argument row `event`, offset `0x8`, width `0x4`, type `Event *`; the return row is protected unchanged and no local, saved-register, second argument, or third frame row may appear.
- I003 frame, protected with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `var_210`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local buffer `Buffer`, offset `0x10c`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0x10c,0x18c)` / `0x80` bytes; local buffer `Destination`, offset `0x18c`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0x18c,0x20c)` / `0x80` bytes; local `var_10`, offset `0x20c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x210`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x218`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x21c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x220`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x228`, API size `0x4`, type `_DWORD`, not a source argument. The implicit `this` remains register-carried in ECX and has no frame row.
- I004 frame, protected with no permitted row delta: local `Block`, offset `0x4`, width `0x4`, type `void *`; local `var_C`, offset `0x8`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x10`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x14`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x18`, width `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x20`, width `0x4`, type `_DWORD`, not a source argument. The preserved cdecl prototype has no source argument.
- I005 frame, protected with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `Buffer`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local `var_10`, offset `0x10c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x110`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x118`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x11c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x120`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x128`, API size `0x4`, type `_DWORD`, not a source argument. The preserved prototype has no source argument.
- I006 frame, protected independently with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `Buffer`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local `var_10`, offset `0x10c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x110`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x118`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x11c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x120`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x128`, API size `0x4`, type `_DWORD`, not a source argument. The preserved prototype has no source argument.
- I007 frame, protected with no permitted row delta: local `Block`, offset `0x8`, width `0x4`, type `void *`; local `var_C`, offset `0xc`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x14`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x18`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x1c`, width `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x24`, width `0x4`, type `_DWORD`, not a source argument. The implicit `this` remains register-carried in ECX and has no frame row.

No I001/I003-I007 API frame row may be renamed, retyped, added, removed, resized, or moved, and none of the separately inferred I003/I005/I006 storage spans may change. I002's `event +0x8/0x4 Event *` materialization is the only permitted physical-frame delta in the complete transaction.

Complete internal regular-comment protections from the latest bounded readback; every corresponding repeatable channel is also protected unchanged:

- I002: `0x48147e this`.
- I003: `0x5a519e Source`, `0x5a51a5 SizeInWords`, `0x5a51a7 Destination`, `0x5a51e6 Source`, `0x5a5220 promptText`, `0x5a5222 this`, `0x5a5238 SizeInWords`, `0x5a523d ArgList`, `0x5a5268 BufferCount`, `0x5a526d Buffer`, `0x5a52b2 promptText`, `0x5a52b4 this`, `0x5a52ca SizeInWords`, `0x5a52cf ArgList`, `0x5a52fa BufferCount`, `0x5a52ff Buffer`, `0x5a532c StackCookie`, `0x60a576 block`, `0x60a584 block`, `0x60a58c this`, `0x60a59d block`, `0x60a5a5 this`, `0x60a5bd StackCookie`, `0x60a5c7 StackCookie`.
- I004: `0x60a601 block`, `0x60a613 StackCookie`.
- I005: `0x5a55bc promptText`, `0x5a55be this`, `0x5a55d4 SizeInWords`, `0x5a55d9 ArgList`, `0x5a5604 BufferCount`, `0x5a5609 Buffer`, `0x5a562f StackCookie`, `0x60a660 block`, `0x60a668 this`, `0x60a680 StackCookie`, `0x60a68a StackCookie`.
- I006: `0x5a568c promptText`, `0x5a568e this`, `0x5a56a4 SizeInWords`, `0x5a56a9 ArgList`, `0x5a56d4 BufferCount`, `0x5a56d9 Buffer`, `0x5a56ff StackCookie`, `0x60a6a1 block`, `0x60a6a9 this`, `0x60a6c1 StackCookie`, `0x60a6cb StackCookie`.
- I007: `0x5a5760 this`, `0x60a6df block`, `0x60a6f1 StackCookie`.

Exact reproducible operation package, one entity/action at a time:

### Disposable diagnostics, worker absence, and closed-disk P0

1. Run `analyze_function`, `decompile`, `force_recompile`, `disasm`, `callers`, `callees`, `func_profile`, or any other analysis-producing/callgraph request only in an explicitly disposable unsaved diagnostic worker before P0 exists. Every `idb_open` is literal and disables auto-analysis:

```text
idb_open {"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":3600,"preferred_session_id":"b002-uid0002fj-diagnostic-<timestamp>"}
```

After diagnostics, issue no save. Because current MCP has no `idb_close`, the primary supervisor records the exact worker PID, creation/start time, executable path, command line, session ID, and canonical input path; identity-checks that same process immediately before termination; terminates only that exact PID; waits for exit; and checks for PID reuse before treating it absent. `idb_list` must show no `is_active:true` session for the canonical path. Stale inactive rows, including `is_active:false/owned:true/adopted:true`, are registry history and are non-authoritative. Finally, a read-only exclusive Windows probe must succeed:

> Executable block R001 was removed from this report and preserved verbatim in [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Any active canonical session, ambiguous process identity/PID reuse, worker-exit uncertainty, or exclusive-probe failure stops before P0. Registry-row disappearance is never required.

2. Only after the absence/lock proof, record closed-disk P0 and create one collision-safe timestamped backup. The exact backup destination must not exist; never overwrite or reuse a prior backup. Record P0 path/hash/size/UTC mtime and backup path/hash/size/UTC mtime.

> Executable block R002 was removed from this report and preserved verbatim in [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Repeat the no-active-session/process and exclusive `FileShare.None` proof before opening the transaction worker. A collision, mismatch, third identity, active/ambiguous worker, or probe failure fails closed.

### Separate fresh exact-P0 transaction worker

3. Fresh-open canonical disk in a unique transaction worker only after step 2. It must be a new PID/session and every literal `idb_open` sets `run_auto_analysis:false`:

```text
idb_open {"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":3600,"preferred_session_id":"b002-uid0002fj-transaction-<timestamp>"}
idb_list {}
server_health {"database":"<fresh-transaction-session>"}
```

Bind the returned session to its exact PID/process identity and canonical path. Immediately reread disk path/hash/size/UTC mtime and require P0 before any prestate query, dry-run, or mutation. Failure uses the process-identity termination and exclusive-probe procedure above without save.

### Frozen inspection-only transaction preflight and staged actions

4. The transaction inspection allowlist is exactly `idb_list`, `server_health`, `lookup_funcs`, `inspect_items`, `get_comments`, `stack_frame`, read-only `xref_query`, `get_bytes`, read-only `entity_query`, and read-only `type_inspect`. Analysis/callgraph/item-defining/unlisted requests are forbidden. Mutation allowlist is exactly six one-item `rename` dry-runs with `pure:true`, six one-item function renames with `pure:true`, one `set_type`, six `set_function_comments`, and one `idb_save`. `set_address_comments`, local/stack renames, non-pure rename, `/vibe/` linkage, and decompiler refresh are prohibited. Every request contains at most ten entries; malformed, truncated, missing, or uncertain output stops before save.

5. Run the complete S0 bundle for I001 and I002-R through I007-C. Reproduce every literal frame row/role above; exact I003/I005/I006 `wchar_t` API size `0x2` plus separate offset-derived spans; pre-type I002's sole return row; fences; vtable; same-slot siblings; HitTest; geometry/deletion/color dependencies; UDTs; all four comment channels; items; bytes; and bounded xrefs. `xref_query` proves edges and `get_bytes` proves bodies/tails. No analysis-producing substitute is accepted.

6. Run each collision dry-run separately:

```text
rename {"database":"<fresh-transaction-session>","batch":{"func":[{"addr":"<address>","name":"<exact-name>"}],"stop_on_error":true,"dry_run":true,"allow_overwrite":false,"pure":true}}
```

Require a successful result without `error`, `dir`, or `dir_error`, then repeat the affected complete S0 readback and require no delta. `pure:true` is mandatory even for dry-run schema parity.

7. Apply each I002-R/I003-R/I004-R/I005-R/I006-R/I007-R separately:

```text
rename {"database":"<fresh-transaction-session>","batch":{"func":[{"addr":"<address>","name":"<exact-name>"}],"stop_on_error":true,"dry_run":false,"allow_overwrite":false,"pure":true}}
```

Require exact new name, no `error`/`dir`/`dir_error`, and the affected row's complete inspection-only poststate before the next mutation. The implementation branch for `pure:true` changes the function name through `idaapi.set_name` and skips function dirtree and decompiler refresh; no `/vibe/` or analysis side effect is authorized.

8. Apply only I002-T with `set_type {"database":"<fresh-transaction-session>","edits":{"addr":"0x481470","signature":"bool __thiscall ChattingVarietySelectPane__OnDismiss(ChattingVarietySelectPane *this, Event *event)","kind":"function"}}`. Inspection-only `lookup_funcs` must return the exact new type; `stack_frame` must return only unchanged return plus `event +0x8/0x4 Event *`; `get_bytes` must preserve body/tail `c2 04 00`; and items/comments/xrefs must remain exact. No decompile or analysis call is allowed.

9. Apply each I002-C/I003-C/I004-C/I005-C/I006-C/I007-C separately:

```text
set_function_comments {"database":"<fresh-transaction-session>","items":{"addr":"<address>","comment":"<exact row text>"}}
```

This endpoint writes only function-regular text. Immediately reread all four channels and require only the function-regular delta; function-repeatable, address-regular, address-repeatable, and every internal address comment remain exact. `set_address_comments` is not called because no regular address-comment edit or clear is intended.

10. Repeat the complete inspection-only bundle. Require I002 return-plus-event frame, exact tail, all I001/I003-I007 frames/spans, bytes, items, xrefs, comments, fences, vtable, UDTs, siblings, and neighbors. Immediately before save, require exact P0 disk tuple. The only permitted database deltas are six names, six function-regular comments, I002 type, and I002's deterministic event frame row. No `/vibe/`, decompiler-refresh, address-comment, local/stack-name, byte/item/xref/UDT/boundary/neighbor, or other delta is accepted.

### One-shot save, closed-worker classification, and fresh persistence

11. Invoke `idb_save` at most once:

```text
idb_save {"path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","database":"<fresh-transaction-session>"}
```

Classify exact success, explicit failure, or indeterminate response. Never retry blindly. Issue no later save call. Identity-check and terminate only the exact transaction PID, prove its process identity absent with PID-reuse checks, require no `is_active:true` canonical session, tolerate stale inactive registry rows as non-authoritative, and require the exclusive read-only `FileShare.None` probe. Only then capture closed-disk S1 path/size/UTC mtime/SHA.

12. For persistence proof, fresh-open a unique verifier with automatic analysis disabled:

```text
idb_open {"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":3600,"preferred_session_id":"b002-uid0002fj-persist-verify-<timestamp>"}
```

Run the complete inspection-only poststate bundle. Then issue no save, identity-check/terminate only that verifier PID, prove process absence/PID-reuse safety, require no active canonical session, and require the exclusive read-only file probe. Rehash/stat disk and require exact S1 before credit.

13. Classify: `persisted` only when S1 differs from P0, complete fresh readback passes, verifier closure/lock proof passes, and final disk still equals S1; `not persisted` only when disk equals P0 and a fresh verifier reproduces S0; otherwise `unknown/failed`. An indeterminate save may earn credit only through independent persisted proof. Explicit failure never earns credit. No analysis-producing call is permitted during classification.

Permitted deterministic delta: I002's logical explicit parameter changes from unnamed `int arg1` to named `Event *event` without changing count, bytes, or `retn 4`; its physical frame adds only the typed event row. Six names and six function-regular comments are the remaining deltas. Helper `wchar_t` API rows stay size `0x2`; inferred spans stay unchanged. No third frame row, address-comment channel, `/vibe/` link, decompiler refresh, local/stack rename, byte, item, xref, UDT, boundary, or neighbor may change.

Rollback contracts:

- Before save, terminate the exact unsaved worker using the identity/PID-reuse protocol, require no active canonical session plus exclusive file probe, and hash disk. If disk equals P0, fresh-open a rollback verifier with literal `idb_open {"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":3600,"preferred_session_id":"b002-uid0002fj-rollback-verify-<timestamp>"}` and prove full S0; then terminate/probe again without save. If disk differs from P0, do not overwrite possible later work.
- After save/indeterminate save, terminate transaction/verifier processes and require the same absence/lock proof before restore consideration. Restore only if canonical equals this transaction's exact failed S1 and the collision-safe backup still equals P0. If canonical equals P0, do not copy; if it equals neither P0 nor S1, stop. Recheck immediately before `Copy-Item`.
- After authorized restore, require exact P0 disk tuple and fresh-open a verifier with `run_auto_analysis:false`; prove complete inspection-only S0, then terminate/probe without save. Never require stale inactive registry-row disappearance, never restore while an active/ambiguous worker or failed exclusive probe exists, never layer compensating mutations over a bad transaction, and never award partial credit.
<!-- END CLEANUP-20260813-0002FJ-SECTION21 -->

## Removed Block R002

- Original source lines: `369-385`
- Original language: `powershell`
- Block SHA256: `4B9E0DFBB0FC3555F6974B6ACD533D5E348FD86BAA843F5B6C5D201A886E17B5`

```powershell
$preItem = Get-Item -LiteralPath $idb
$prePath = $preItem.FullName
$preSize = $preItem.Length
$preSha = (Get-FileHash -LiteralPath $idb -Algorithm SHA256).Hash
$preMtime = $preItem.LastWriteTimeUtc
$stamp = Get-Date -Format 'yyyyMMdd-HHmmss-fffffff'
$backup = "$idb.bak-UID0002FJ-prestate-$stamp-$($preSha.Substring(0,16))"
if (Test-Path -LiteralPath $backup) { throw 'UID0002FJ backup destination already exists.' }
Copy-Item -LiteralPath $idb -Destination $backup -ErrorAction Stop
$backupItem = Get-Item -LiteralPath $backup
$backupSha = (Get-FileHash -LiteralPath $backup -Algorithm SHA256).Hash
if ($backupItem.Length -ne $preSize -or $backupSha -ne $preSha) { throw 'Backup is not byte-identical to P0.' }
$closedAgain = Get-Item -LiteralPath $idb
$closedAgainSha = (Get-FileHash -LiteralPath $idb -Algorithm SHA256).Hash
if ($closedAgain.FullName -ne $prePath -or $closedAgain.Length -ne $preSize -or $closedAgain.LastWriteTimeUtc -ne $preMtime -or $closedAgainSha -ne $preSha) { throw 'Canonical disk changed during backup creation.' }
```
