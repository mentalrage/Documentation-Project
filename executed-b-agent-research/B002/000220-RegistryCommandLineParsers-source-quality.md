** TARGET-REPORT-UID:000220 **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 000220 RegistryCommandLineParsers Source-Quality Research


## Finalized Report / Current Recommendation

- B002 callback-time recommendation/status: an earlier supervisor callback cited Gate 1 pass at pre-repair SHA `CF70E92150450638EF2309AFB981B9C64BAD82AEF33343E91AF4FFAD6278D7F3`; the implementation callback applied the accepted target update for [UID:000220] `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md`, raising it from `86/89` to `88/90`. This repaired report text is intended for a fresh supervisor Gate 1 audit at its current SHA; after supervisor execution, validator-owned lifecycle history on the archived artifact is authoritative for archive state.
- Final disposition: keep `CANONICAL_OWNER:0000N4`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N4`, blank `EMITTER_POSITION_OPTIONAL`, and the existing formal comment-only aggregate C++ marker.
- Implementation result: target MCP evidence, stale generated-output statement, score rationale, score headers, rejected alternatives, and open-question conclusions are incorporated in the target page. The manual `by-memory/-coverage-report.md` row remains excluded from B002 edits because the callback explicitly kept manual coverage text supervisor-owned.
- Confidence: high for exact range, child inventory, padding, file-level `RegistryConfig.cpp` ownership, child C++ emission, and no direct xref/pointer-route evidence. Confidence remains capped below final audit because the actual launcher dispatcher/caller edge and original source symbol spelling for the parser family remain unrecovered.

## Supporting Research

- Lifecycle/status notes: during the B002 report/callback pass, the working report path was `tools/leaser/Agents/Agent-B002/research/000220-RegistryCommandLineParsers-source-quality.md`; that working artifact passed Gate 1 and received a target-only implementation callback. B002 edited the target by-memory page under lease, ran the scoped file validator, and observed generated output refresh. B002 did not run registry lifecycle, archive move, manual report move, dry-run/probing execute variant, or `execute_report` commands during the report/callback pass.
- Local clock recorded during B002 evidence pass: `2026-07-05T21:56:57-04:00`.
- The MCP session used during the B002 evidence pass was `supervisor_recovery_20260705`, with `server_health` status `ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- Search-gated prior report pass used target UID, exact range, current target name, child helper names, and source-family terms. No executed B report for UID000220 itself was found. Matching executed child/support reports were opened as leads and accepted evidence context: B007 `0002BG`, B008 `0002BH` and `0002BI`, B006 `0002BJ`, B009 `0002BK` and `0002BQ`, B010 `0002BL`, B003 `0002BM` and `0002BP`, B013 `0002BN`, B001 `0002BO`, B011 `0002BR`, and B007 `0000N4` RegistryConfig empty-emitter family.

## Target

- Target UID: `000220`
- Target path: `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md`
- B002 callback-time generated tracker row after scoped validation: `auto-generated/-ag-research-tracker.md` line 2329 reported `88/90`, average `89.0`, reconstructable `true`, reports `0`. After supervisor execution, validator-owned lifecycle history and refreshed generated tracker state are authoritative.
- B002 callback-time handoff classification: Gate 1 accepted; target-only implementation callback completed; B002 returned `READY_FOR_SUPERVISOR_EXECUTE`.
- B002 callback-time scores and parent state: target `88/90`; owner/emitter [UID:0000N4] `RegistryConfig` by-file `90/90`; class support [UID:0000BW] `RegistryConfig` `88/90`.

## Current Target State

- Callback-applied metadata: `COMPLETION:88`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000N4`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N4`, blank `EMITTER_POSITION_OPTIONAL`.
- Existing owner/emitter/reconstructable state: the aggregate is source-authored `RegistryConfig.cpp` parser-island evidence. It is reconstructable and emitted through [UID:0000N4], but it intentionally emits a comment-only aggregate marker because the twelve exact children carry the actual helper bodies.
- B002 callback-time C++/emitter state: `auto-generated/NexusTK/config/RegistryConfig.cpp` was populated at validator command id `000000007419`, refreshed `2026-07-05T22:11:26-04:00`. It included the UID000220 comment marker at line 684 and parser child helper bodies including `ParseUnitelRelayStartupHostPort` and `ParseStructuredHostPortIdLauncher`. This superseded the target page's older statement that generated `RegistryConfig.cpp` and `Config.cpp` were zero-byte.
- Existing open questions/blockers: direct launcher dispatcher/caller route remains unrecovered; original source symbol names remain inferred; class-method versus file-static helper placement remains unresolved at final-source level; some field names remain target-local or overlay-aware rather than final class-layout declarations.
- Related target/support docs checked: target page, all twelve child pages, [UID:0000N4] `by-file/RegistryConfig.md`, [UID:0000IE] `by-file/Config.md`, [UID:0000BW] `by-class/RegistryConfig.md`, adjacent parents [UID:000110] and [UID:000111], runtime/helper support [UID:000221], string-data support [UID:0003FV] and [UID:0003FX], generated tracker/coverage, generated `RegistryConfig.cpp`, manual `by-memory/-coverage-report.md`, project resolved/unresolved alias reports, and matching executed B reports.
- B002 callback-time artifact/lifecycle status: implementation callback complete; before supervisor-owned execution, the working report had not been executed or archived. After supervisor execution, validator-owned lifecycle headers/history on the archived artifact are authoritative for current lifecycle state.

## Heuristic / Inference Reanalysis And Validation

- Split/range: live MCP `lookup_funcs` and `get_bytes` validate the current split. The twelve child starts still occupy the exact range `0x0048f400-0x00491b28`, and every predecessor/internal/successor gap checked in this pass is `0xcc` padding. No new child split, merge, or ignored subrange is currently justified.
- Ownership/source placement: file-level [UID:0000N4] remains the best owner/emitter. Every child mutates `RegistryConfig` startup/config state and the support docs already route all parser helpers through `RegistryConfig.cpp`. [UID:0000BW] remains plausible as a future class-method route, but no direct call, vtable slot, stored pointer, or selector route currently proves class-member status for the whole island.
- Parser/helper naming: current child helper names are source-facing inferred names, not original-symbol proof. They are still the best documented source shape: `ScanStartupCommandLineTokenLimit`, `ParseUnitelRelayStartupHostPort`, `ParseNetsgoStartupAuthentication`, `ParseThrunetStartupAuthenticationFile`, `ParseLgInternetSessionLauncher`, `ParseLauncherHostPortPayload`, `ParseMihosoftLauncher`, `ParseExciteGameLauncherAuthentication`, `ParseBixelLauncher`, `ParseKornetWorldLauncher`, `ParseGameBillLauncher`, and `ParseStructuredHostPortIdLauncher`.
- Command-line/config field names: the current `m_startupAddress`, `m_startupHostName`, `m_startupPort`, `m_startupReady`, `m_startupConnectionMode`, `m_startupLauncherId`, `m_netsgoLaunchCommand`, and mode-dependent payload/session/token overlay names are valid for child-level source reconstruction. They should not be promoted into a full final class field declaration without a broader layout audit.
- B002 callback-time generated-output status: the old generated-output blocker was resolved for this aggregate. Generated `RegistryConfig.cpp` included the aggregate marker and child bodies. That improved completion because generated body absence was no longer a blocker in the callback evidence.
- Rejected alternative, monolithic body: rejected. `0x0048f400` is not a modeled monolithic function, the range is an aggregate over twelve exact children, and the formal block already says no monolithic parser-island function is emitted.
- Rejected alternative, reclassify as non-reconstructable container: rejected. The aggregate is a source-authored parser island and the children emit source. Keeping the aggregate reconstructable with a comment marker is the current project pattern for source-bearing aggregate/index items.
- Rejected alternative, raise to `95+`: rejected. The dispatcher/caller route and original source symbol names remain unresolved, so the target is not final-audit quality under by-memory scoring rules.

## Evidence Standards Used

- Direct IDA/MCP fact was treated as authority for function modeling, xrefs, byte gaps, pointer-byte searches, bounded listing hits, and string/data refs.
- Current by-* docs and executed B reports were treated as documentation evidence and accepted implementation history, not as substitutes for current MCP checks.
- Generated reports and generated C++ were treated as validator-owned current-state evidence, not as source authority for ownership or final names.
- Inference was used only for source-facing helper names, file-static versus member-method shape, and score movement. Each inference was checked against child docs, owner docs, current generated output, and MCP negative evidence.

## Evidence Checked

- IDA MCP/manual-disassembly/raw-byte checks performed:
  - `initialize`, `tools/list`, `idb_list`, and `server_health` against `supervisor_recovery_20260705`.
  - `lookup_funcs` for all twelve child starts plus successor `0x00491b30`.
  - `xrefs_to` for all twelve child starts plus successor `0x00491b30`.
  - `get_bytes` for predecessor, all internal, and successor gaps.
  - `find_bytes` for all twelve starts using little-endian VA and RVA patterns, 24 patterns total.
  - bounded `search_text` in `0x0048f400-0x00491b28` for `GetCommandLineW`, shared field offsets, and launcher/API anchors.
  - `xrefs_to` for representative string/data anchors at `0x00616e60`, `0x00616f38`, `0x00617048`, `0x006173c0`, `0x00617994`, `0x006179a4`, `0x006179b8`, and `0x006179c4`.
- by-* docs, support docs, old reports, generated reports, and trackers checked:
  - target and all twelve child pages;
  - [UID:0000N4] `RegistryConfig`, [UID:0000IE] `Config`, and [UID:0000BW] `RegistryConfig`;
  - [UID:000110], [UID:000111], [UID:000221], [UID:0003FV], and [UID:0003FX];
  - `auto-generated/-ag-research-tracker.md`, `auto-generated/-ag-coverage-report-by-memory.md`, generated `RegistryConfig.cpp`, manual `by-memory/-coverage-report.md`, `project-level/-resolved*.md`, and `project-level/-unresolved.md`;
  - matching executed B reports for the twelve children and RegistryConfig family support.
- Negative checks performed:
  - zero direct xrefs to all twelve child starts;
  - zero VA/RVA pointer-byte matches for all twelve child starts;
  - no new non-padding gap between children;
  - no evidence that the aggregate should emit a monolithic body;
  - no current proof that the whole family should be promoted to `RegistryConfig::` methods.
- Failed, unavailable, or intentionally skipped checks and why:
  - `source-3/simroot_v2` and `source-3/core/data/cache` paths from older by-structure guidance were not present in this workspace path; current generated C++ and by-* docs were used as generated-output context instead.
  - B002 did not run broad validators or execute/lifecycle commands during the report/callback pass. B002 ran only the required scoped file validator after the target edit.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | UID000220 should move to `COMPLETION:88`, `CONFIDENCE:90`. | High | Current MCP recheck, child C++ state, generated `RegistryConfig.cpp` current, previous generated-output blocker resolved. | Target metadata and score rationale | incorporate | applied |
| C2 | `CANONICAL_OWNER:0000N4`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000N4` should stay unchanged. | High | by-file/class support, child owner/emitter metadata, source-authored RegistryConfig parser behavior. | Target metadata/status | already-present | already-present |
| C3 | The aggregate should remain comment-only in `RECONSTRUCTION_CPP CODE`; no monolithic parser body should be emitted. | High | `0x0048f400` not a modeled function, children have exact bodies, current generated output includes UID000220 comment marker. | Target C++ block and source reconstruction notes | already-present | already-present |
| C4 | The old generated-output omission text is stale because generated `RegistryConfig.cpp` now contains UID000220 and all child helpers. | High | Generated header command id `000000007419`, refreshed `2026-07-05T22:11:26-04:00`, UID hits in generated source. | Target evidence/source notes | incorporate | applied |
| C5 | No split repair is needed; all gaps are `0xcc` padding. | High | `get_bytes` for thirteen gap regions, all `0xcc`. | Target evidence and range/split notes | incorporate | applied |
| C6 | The missing dispatcher/caller route remains unresolved after current direct xref and pointer-byte checks. | High | `xrefs_to` zero for twelve starts; 24 pointer-byte patterns zero. | Target score rationale/open questions | incorporate | applied |
| C7 | Successor `0x00491b30` remains separate and has the expected vtable data xref from `0x00612620`; it should not be merged into UID000220. | High | `lookup_funcs` finds `sub_491B30`; `xrefs_to` has one data ref at `0x00612620`; successor gap is eight `0xcc`. | Target split/boundary notes | incorporate | applied |
| C8 | File-static helper placement is still the least-claiming source shape; class-method promotion remains unsupported. | High | by-file/class docs, prior child reports, no direct caller/vtable/stored pointer route. | Target/source placement notes | already-present | applied |
| C9 | Manual `by-memory/-coverage-report.md` row for UID000220 is stale at `82%` and should be supervisor-updated if manual coverage maintenance is authorized. | High | Manual row line 1036 versus target metadata and generated coverage row. | Manual coverage report | incorporate | excluded-with-reason: callback explicitly kept manual coverage text supervisor-owned and limited B002 edit scope to the target page |

## Positive Evidence Summary

- Direct facts supporting the recommendation:
  - `lookup_funcs`: nine raw starts remain non-functions; three modeled children remain `sub_48FCA0` size `0x550`, `sub_490B10` size `0x5b0`, and `sub_4918E0` size `0x248`; successor `sub_491B30` starts at `0x00491b30`.
  - `xrefs_to`: zero direct xrefs to all twelve parser starts; successor `0x00491b30` has one data xref at `0x00612620`.
  - `get_bytes`: every checked gap from `0x0048f3f1-0x0048f400`, internal child gaps, and `0x00491b28-0x00491b30` is `0xcc`.
  - `find_bytes`: all 24 VA/RVA little-endian pointer patterns for the twelve starts returned zero matches.
  - bounded listing search: all twelve parser children call `GetCommandLineW`; shared startup offsets and parser string/data refs still appear in the assigned range.
- Corroborating documentation/generated-report evidence:
  - Every child page is now reconstructable, owned/emitted by [UID:0000N4], and has nonblank formal C++.
  - Generated `RegistryConfig.cpp` now contains the UID000220 marker plus child helpers.
  - [UID:0000N4] and [UID:0000BW] already preserve the file-level helper route and reject class-method promotion without a dispatcher.
- Strongest inference chain and why it is sufficient:
  - The aggregate is not a callable monolithic routine, but it is a coherent source-authored parser family. Current evidence supports a modest score increase because generated body absence is resolved and live MCP reconfirms the aggregate boundaries; it does not support a larger increase because the dispatcher and original symbol route remain missing.

## IDA MCP Facts

- Function/range facts: `0x0048f400`, `0x0048f4d0`, `0x0048f6d0`, `0x00490200`, `0x00490510`, `0x00490810`, `0x004910d0`, `0x00491450`, and `0x00491790` are not IDA function objects. `0x0048fca0`, `0x00490b10`, and `0x004918e0` remain modeled as the three SEH-bearing child functions.
- Data/table/padding facts: all checked boundaries and gaps are `0xcc`; no hidden code island remains between children.
- Xref facts: all twelve parser starts have `xref_count:0`; successor `0x00491b30` has one data xref from `0x00612620`.
- String/global/type facts: `thrunet.clsURLCHK` at `0x00616e60` xrefs from `0x0048ffcb`; `WaitForSessionParameter` at `0x00616f38` xrefs from `0x004902bb`; `Mihosoft` at `0x00617048` xrefs from `0x004908e4`; GameBill text at `0x006173c0` xrefs from `0x00491889`; structured markers `/host:`, `/portno:`, `/id:`, and host-failure text xref inside `sub_4918E0`.
- Negative IDA facts: no direct caller, vtable, selector, code ref, data ref, or stored VA/RVA pointer route to the twelve child starts was recovered in this pass.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x0048f400-0x0048f4cf` | [UID:0002BG] `CommandLineArgumentCounter` | token-limit/startup-ready scanner | TRUE | [UID:0000N4] | `88/89` | emits `ScanStartupCommandLineTokenLimit` |
| `0x0048f4d0-0x0048f6cb` | [UID:0002BH] `StartupHostPortCommandLineParser` | Unitel Relay host/port parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseUnitelRelayStartupHostPort` |
| `0x0048f6d0-0x0048fc96` | [UID:0002BI] `NetsgoStartupAuthenticationParser` | Netsgo auth parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseNetsgoStartupAuthentication` |
| `0x0048fca0-0x004901f0` | [UID:0002BJ] `ThrunetStartupAuthFileParser` | file-backed Thrunet auth parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseThrunetStartupAuthenticationFile` |
| `0x00490200-0x0049050a` | [UID:0002BK] `LgInternetSessionLauncherParser` | LG Internet session parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseLgInternetSessionLauncher` |
| `0x00490510-0x00490809` | [UID:0002BL] `LauncherHostPortPayloadParser` | host/port/two-payload parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseLauncherHostPortPayload` |
| `0x00490810-0x00490b01` | [UID:0002BM] `MihosoftLauncherParser` | Mihosoft host/port parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseMihosoftLauncher` |
| `0x00490b10-0x004910c0` | [UID:0002BN] `ComBackedLauncherAuthenticationParser` | Excite Game COM auth parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseExciteGameLauncherAuthentication` |
| `0x004910d0-0x00491445` | [UID:0002BO] `BixelLauncherParser` | Bixel host/port/payload parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseBixelLauncher` |
| `0x00491450-0x0049178d` | [UID:0002BP] `KwgLauncherParser` | KornetWorld `/KWG` parser | TRUE | [UID:0000N4] | `88/90` | emits `ParseKornetWorldLauncher` |
| `0x00491790-0x004918dc` | [UID:0002BQ] `GameBillLauncherGate` | GameBill ready/mode gate | TRUE | [UID:0000N4] | `88/90` | emits `ParseGameBillLauncher` |
| `0x004918e0-0x00491b28` | [UID:0002BR] `StructuredHostPortIdLauncherParser` | `/host:` `/portno:` `/id:` parser | TRUE | [UID:0000N4] | `90/92` | emits `ParseStructuredHostPortIdLauncher` |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| All twelve parser starts | `xrefs_to` returned zero xrefs | Direct dispatch/caller route remains unrecovered. |
| All twelve parser starts | 24 VA/RVA `find_bytes` patterns returned zero matches | No simple stored function pointer table was found. |
| `0x00491b30` | one data xref from `0x00612620` | Successor is a separate vtable-routed `RegistryConfig` method, not part of UID000220. |
| `GetCommandLineW` calls | hits at `0x0048f424`, `0x0048f4f7`, `0x0048f6fb`, `0x0048fce1`, `0x00490217`, `0x00490531`, `0x00490831`, `0x00490b5b`, `0x004910f1`, `0x00491471`, `0x004917ab`, and `0x0049190a` | Confirms all twelve children are command-line parser helpers. |

## Documentation Evidence And IDA Status

- Existing docs that support the conclusion: target page child table/source notes; child pages; [UID:0000N4] command-line parser source-placement notes; [UID:0000BW] ownership notes and startup field inventory; generated `RegistryConfig.cpp`.
- Existing docs that are stale, incomplete, or contradicted:
  - target page historical text previously said generated `RegistryConfig.cpp` and `Config.cpp` were zero-byte. The implementation callback replaced that with historicalized wording; current generated `RegistryConfig.cpp` is nonzero and contains UID000220 plus child helper bodies.
  - manual `by-memory/-coverage-report.md` line 1036 still reports UID000220 as `82%`; current target metadata and generated coverage report have moved past that.
  - project-level unresolved aliases still contain stale generated labels such as `dword_69BE30`, `dword_69BE80`, `param_1`, and `off_*` for child pages. Current child/support docs resolve many of these as accepted source-facing names or preserve them as stale unresolved-index artifacts.
- B002 callback-time generated/coverage report state: `auto-generated/-ag-research-tracker.md` line 2329 listed reports `0` for UID000220 before supervisor execution and reported `88/90` average `89.0`. `auto-generated/-ag-coverage-report-by-memory.md` line 730 had `88%` very-strong generated coverage. Generated `RegistryConfig.cpp` was current at command id `000000007419`. After supervisor execution, validator-owned lifecycle history and refreshed generated reports are authoritative.

## Ranked Ownership Analysis

### 1. [UID:0000N4] `RegistryConfig` by-file

- Evidence for: all exact children mutate RegistryConfig startup/config fields; by-file page is the source root `NexusTK/config/`; generated `RegistryConfig.cpp` includes all child bodies; child reports and current docs consistently route the family through [UID:0000N4].
- Evidence against: no recovered dispatcher/caller proves the exact original local helper ordering or source symbol spelling.
- Decision: keep as canonical owner and emitter.

### 2. [UID:0000BW] `RegistryConfig` by-class

- Evidence for: children receive the config object in `ecx`/register state and write class-owned startup fields; some modeled children decompile as `thiscall`.
- Evidence against: no vtable route, direct caller, stored pointer, or whole-island dispatcher proves these are declared member methods. The class doc explicitly rejects promoting the entire parser island to class methods until that route is recovered.
- Decision: keep as support context only; do not set as direct owner for UID000220.

### 3. Separate launcher/networking module

- Evidence for: helper names and literals reference external launchers/providers such as Unitel Relay, Netsgo, Thrunet, LG Internet, Mihosoft, Bixel, KornetWorld, GameBill, and HITEL2000.
- Evidence against: the children sit between RegistryConfig defaults and persistence, use the RegistryConfig startup-state layout, and emit under `RegistryConfig.cpp`; no independent source root or owner page is evidenced.
- Decision: reject for this target.

### Proposed new file/grouping, if applicable

- Proposed owner/name/path: none.
- Likely full contents: not applicable because [UID:0000N4] already owns the family.
- Candidate related items that belong: the twelve exact child pages and support string-data pages already belong to [UID:0000N4].
- Candidate related items rejected: [UID:000221] COM/CRT helper island remains runtime/helper support and should not be folded into this target.
- Standalone, narrow, or broad source-file inference: broad file-level helper family under `config/RegistryConfig.cpp`; not a new source file.

## Source Placement

- Recommended source file/class/global/module placement: keep UID000220 in `NexusTK/config/RegistryConfig.cpp` via [UID:0000N4].
- Why this placement fits source-tree and subsystem context: the range is physically adjacent to RegistryConfig defaults and persistence, every child uses RegistryConfig startup fields, and generated output already routes the marker and children through `RegistryConfig.cpp`.
- Rejected placements and why: `RegistryConfig` class methods are plausible but unproven; separate launcher/provider files are contradicted by shared RegistryConfig state and lack of independent dispatch evidence; non-emitting container is contradicted by source-authored child helpers.
- Remaining placement uncertainty, if any: the original source may have expressed the helpers as private member methods or file-static functions. Current documentation should keep file-static helper placement because it makes fewer claims.

## Range / Split / Padding / Reclassification Analysis

- Exact range/boundary facts: UID000220 covers `0x0048f400-0x00491b28`. Predecessor `0x0048f3f1-0x0048f400`, all internal child gaps, and successor `0x00491b28-0x00491b30` were current-MCP checked as `0xcc`.
- Children/subranges to create, repair, merge, leave ignored, or leave no-owner: no new children needed. All twelve exact child pages already exist, clear the current C++ gate, and emit through [UID:0000N4].
- Padding/table/data/code distinctions: internal gaps are padding, not unmodeled parser code. String/data support is already covered by [UID:0003FV] and [UID:0003FX]; runtime COM/CRT support is covered by [UID:000221].
- Parent/container impact: UID000220 should remain a reconstructable source-authored aggregate/index with comment-only C++.

## Negative Evidence Summary

- Direct xrefs do not prove a dispatcher: all twelve starts still have zero direct xrefs.
- Pointer-byte searches do not prove a dispatch table: all little-endian VA/RVA patterns for the twelve starts returned zero matches.
- Raw starts do not prove dead code: all children are coherent command-line/parser bodies, now emit individually, and share RegistryConfig state.
- Incoming `ecx` does not prove class-method status by itself: no vtable/direct caller route ties the whole island to `RegistryConfig::` methods.
- Generated output does not justify a monolithic body: the generated source includes a marker for UID000220 plus child bodies, which is exactly the intended aggregate behavior.
- Manual coverage row staleness is not target evidence: it should be repaired only through authorized manual coverage maintenance, not used to lower current target metadata.

## IDA Rename / Type / Comment Recommendations

- Proposed source-facing names/types/comments: no IDA DB edits requested. Documentation should continue using file-static helper names for child source and keep UID000220 as `RegistryCommandLineParsers`.
- Evidence for each proposed name/type/comment: accepted child reports, current child C++ blocks, by-file/class support docs, and current MCP command-line/string/field evidence.
- Items intentionally left unchanged and why:
  - do not rename the aggregate into a single function name because it is not one function;
  - do not promote children to `RegistryConfig::` methods because the dispatcher/caller route is absent;
  - do not replace overlay-aware field names with final class fields until a broad layout audit proves them.
- Whether IDA DB edits are safe, unsafe, or not requested: not requested in this assignment or implementation callback.

## First-Draft C++ Recommendation

- Eligible for draft C++: the aggregate is eligible only for a formal comment-only marker. The exact child pages are already eligible and already carry formal helper C++.
- Recommended code: keep the target's existing formal block content:

```cpp
// RegistryCommandLineParsers is an aggregate/index over exact parser children.
// The twelve child helpers emit individually under RegistryConfig.cpp; no monolithic parser-island function is emitted here.
```

- Reason it preserves exact original behavior: it avoids creating a non-existent monolithic dispatcher/body while allowing exact child helper bodies to emit.
- Reason it matches plausible original source shape: an original source file would likely have a family of local startup parser helpers in `RegistryConfig.cpp`; the aggregate documentation is an index over those helpers, not a source routine.
- Inferred source-facing names/types/fields used instead of IDA/decompiler labels: child pages use `RegistryConfig *config`, `m_startupAddress`, `m_startupHostName`, `m_startupPort`, `m_startupReady`, `m_startupConnectionMode`, `m_startupLauncherId`, and target-local payload/session names instead of raw `param_1`, `dword_*`, and offset-only labels.
- Naming/coding style convention used and evidence for consistency: file-static `Parse*` helpers under `RegistryConfig.cpp`, consistent with accepted child reports and generated source.
- Reason code should remain blank/comment-only, if applicable: no monolithic body exists; emitting a dispatcher would invent a caller/selection route that MCP did not find.
- Exact no-code proof, if not eligible: not a no-code source item. The aggregate emits a comment marker; executable code lives on exact child pages.

## Final Recommendation

- Exact changes implemented: UID000220 metadata is now `COMPLETION:88`, `CONFIDENCE:90`; owner/emitter/reconstructable and C++ marker remain unchanged; evidence and score rationale now reflect current MCP and generated-output state.
- Exact parent assignments recommended: keep [UID:0000N4] as canonical owner and emitter.
- Exact items left no-owner/non-emitting and why: none for UID000220. Runtime/helper support [UID:000221] remains non-reconstructable outside this target.
- Exact future work, if any, outside this assignment scope: recover the actual launcher dispatcher/caller route if future evidence appears; perform a broad final layout/name audit before promoting helpers to `RegistryConfig::` methods or declaring final class fields.

## Recommended Target Doc Changes

- Target path: `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md`
- Exact report facts incorporated:
  - header scores are now `COMPLETION:88` and `CONFIDENCE:90`;
  - added a 2026-07-05 B002 evidence note with MCP session `supervisor_recovery_20260705`, healthy IDB state, modeled/raw start result, zero direct xrefs to all twelve starts, zero VA/RVA pointer-byte matches, all `0xcc` gap bytes, bounded `GetCommandLineW` call hits, representative string/data xrefs, successor separation, and generated `RegistryConfig.cpp` freshness;
  - replaced stale wording that `auto-generated/NexusTK/config/RegistryConfig.cpp` and `Config.cpp` are zero-byte with current wording: generated `RegistryConfig.cpp` is populated and contains the UID000220 marker plus child helper bodies;
  - updated score rationale so the remaining cap is dispatcher/caller route, dispatch table absence, original symbol spelling, class-method-vs-file-static route, and final broad class-layout field certainty, not generated body absence;
  - kept the formal comment-only C++ marker unchanged.
- Metadata/score/owner/emitter/reconstructable/C++ changes:

```text
COMPLETION:88
CONFIDENCE:90
CANONICAL_OWNER:0000N4
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000N4
EMITTER_POSITION_OPTIONAL:
RECONSTRUCTION_CPP CODE: keep existing comment-only marker
```

- Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: preserve no-direct-xref/no-pointer-route facts, class-method rejection, monolithic-body rejection, and 95+ score rejection.

## Recommended Support Doc Changes

- Support path: `by-memory/-coverage-report.md`
- Exact report facts disposition: the UID000220 manual row is stale at `82%`, but the implementation callback explicitly kept manual coverage text supervisor-owned and limited B002's edit scope to the target page. This support change is excluded-with-reason for B002.
- Metadata/link/score/coverage/source-placement changes: no manual support doc was edited. Validator-owned generated tracker/coverage refreshed through the scoped validator. No [UID:0000N4], [UID:0000BW], or child page change is required because those docs already carry the current file-static helper route and child-level C++ evidence.

## Score And Metadata Recommendation

- Pre-callback score/metadata: `86/89`, owner/emitter [UID:0000N4], reconstructable true, comment-only aggregate C++.
- B002 callback-applied score/metadata: `88/90`, owner/emitter/reconstructable/C++ marker unchanged.
- Score rationale and reason not higher/lower:
  - Completion should rise because generated `RegistryConfig.cpp` now contains UID000220 and all child helper bodies, the old generated-output omission is stale, every child has nonblank formal C++ and current source-placement support, and B002 current MCP reconfirmed the exact range/gap/xref/pointer state.
  - Confidence should rise one point because current MCP independently reconfirmed the prior negative route and boundary evidence against the current IDB, and support docs now consistently reflect file-static helper placement.
  - Do not raise above `90` because no dispatcher/caller route, original helper names, final class-method route, or full class-layout field declaration is proven.
- Score-improvement attempt:
  - Dispatcher/caller blocker: checked direct xrefs and 24 VA/RVA pointer patterns; unresolved, now documented as a confidence cap.
  - Split/range blocker: checked all gaps; resolved, no split repair needed.
  - Source placement blocker: checked by-file/class support and generated source; resolved to [UID:0000N4] file-static route.
  - Generated-output blocker: checked generated `RegistryConfig.cpp`; resolved.
  - Parser/helper/field naming blocker: child reports and support docs provide best current names; original spellings remain unproven and cap score.
- Metadata fields changed or left unchanged: changed only completion/confidence; owner/emitter/reconstructable and blank emitter position stayed unchanged.

## Open Questions With Attempted Resolution

- Open question: where is the runtime/static dispatcher that selects these parser helpers?
  - Evidence checked: `xrefs_to` all starts, VA/RVA pointer-byte search, prior child reports, by-file/class docs.
  - Best supported resolution: not recovered in current evidence. This blocks class-method promotion and final dispatcher C++ but not child helper emission or aggregate score improvement.
- Open question: are helpers originally file-static or private `RegistryConfig` methods?
  - Evidence checked: incoming config object use, class doc, modeled `thiscall` children, vtable route, direct xrefs.
  - Best supported resolution: file-static helper placement is safest until the whole island route is recovered.
- Open question: should UID000220 emit more than a comment?
  - Evidence checked: function modeling, child inventory, generated output, split/range docs.
  - Best supported resolution: no. Exact children emit code; aggregate emits marker only.
- Open question: should the manual coverage report be updated?
  - Evidence checked: manual row, generated coverage, target metadata.
  - Best supported resolution: yes, but only through supervisor-authorized manual coverage maintenance. It was explicitly excluded from B002's target-only implementation callback.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- File/placement: `by-memory/-coverage-report.md`, replace the existing UID000220 row under [UID:000110] after the `0x0048f3f1-0x0048f400` padding row and before child [UID:0002BG].
- Exact replacement text:

```text
    - [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) 0x0048f400-0x00491b28 | function cluster | RegistryCommandLineParsers : reconstructable : 88% : strong : RegistryConfig command-line, login/session, DNS, and launcher parser island; 2026-07-05 B002 live MCP reconfirmed all child starts, modeled chunks, zero direct start xrefs, zero VA/RVA pointer encodings, internal `0xcc` gaps, string/API/field anchors, and RegistryConfig file-level ownership; exact child helpers emit individually under RegistryConfig.cpp, while the aggregate remains a comment-only evidence/index marker rather than a monolithic parser body.
```

- Reason B agent did not apply it directly: the implementation callback explicitly kept manual coverage text supervisor-owned and limited B002's edit scope to the target page.

## Follow-Up Actions

- Supervisor lifecycle note: this repair pass supplies archive-safe report text for supervisor Gate 1 and later Gate 2/execute handling; after execution, validator-owned lifecycle history on the archived artifact is authoritative. B002 does not run supervisor-owned `execute_report`.
- A-agent actions: none required.
- B002 repair note: no additional B002 research action is part of this report unless supervisor sends a repair callback. Manual coverage remains supervisor-owned.

## Confidence

- Recommendation confidence: high for modest score movement and unchanged owner/emitter/C++ marker.
- Score confidence: high for `88/90`; low for any score above `90` without dispatcher or original-name evidence.
- Remaining uncertainty: dispatcher/caller route, original helper names, and final class layout remain unresolved.

## Validator Results

- Command run: `python .\tools\validator.py --mode file --file by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md --apply --queue-timeout 240 --wait-generated`
- Result: exit code `0`; `command_id: 000000007419`; `command_timestamp: 2026-07-05T22:11:26-04:00`; `ok: 1`; `generated_refresh: completed`; `generated_refresh_command_id: 000000007419`; `generated_refresh_timestamp: 2026-07-05T22:11:26-04:00`.
- Target updates reported by validator: `completion_update 000220 ... 88` and `confidence_update 000220 ... 90`.
- B002 callback-time generated freshness: read-only inspection found `auto-generated/NexusTK/config/RegistryConfig.cpp` header `validator-command-id: 000000007419`, `validator-refreshed-at: 2026-07-05T22:11:26-04:00`; UID000220 appeared at line 684 with `Completion:88 | Confidence:90`, and child helper bodies were present. Generated tracker line 2329 reported `88/90` average `89.0`; generated by-memory coverage line 730 reported `88% : very-strong`. After supervisor execution, validator-owned lifecycle history and refreshed generated outputs are authoritative.
- Unrelated/generated warnings: existing `autogen_children_*` and `autogen_emitter_has_no_code` diagnostics were reported for other items; no target-specific validation error was reported.

## Changed Files

- Created during B002 report pass at working path: `tools/leaser/Agents/Agent-B002/research/000220-RegistryCommandLineParsers-source-quality.md`
- Modified by B002 callback: `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md`; `tools/leaser/Agents/Agent-B002/research/000220-RegistryCommandLineParsers-source-quality.md`.
- Validator-owned/generated side effects from scoped validation: `auto-generated/NexusTK/config/RegistryConfig.cpp`, `auto-generated/-ag-research-tracker.md`, `auto-generated/-ag-coverage-report-by-memory.md`, `project-level/-auto-completion-stats.md`, and `tools/validator.ini`.
- Renamed: none.
- B002 execution boundary: B002 did not run `tools/validator.py execute_report ... --apply`, dry-run execute variants, registry lifecycle commands, manual report moves, or archive moves during the report/callback pass. B agents must never run those commands; validator-owned lifecycle history is authoritative after supervisor execution.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor validation required before implementation: the earlier implementation callback cited Gate 1 pass for the pre-repair report at SHA `CF70E92150450638EF2309AFB981B9C64BAD82AEF33343E91AF4FFAD6278D7F3`; this repaired report text is returned for fresh supervisor Gate 1 review at its current SHA.
- [x] Target/support docs to update: target `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md` applied; manual support `by-memory/-coverage-report.md` explicitly excluded from B002 callback scope and left supervisor-owned.
- [x] Current target state and actual evidence checked recorded: B002 MCP evidence, generated `RegistryConfig.cpp` state, child metadata/C++ state, and stale manual coverage row evidence are recorded in the report.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: C1/C4/C5/C6/C7/C8 applied, C2/C3 already-present, C9 excluded-with-reason.
- [x] Metadata/score changes to apply: target now has `COMPLETION:88`, `CONFIDENCE:90`; owner/emitter/reconstructable/C++ marker unchanged.
- [x] Score-limiting blockers researched to resolution, implementation-ready repair/split recommendation, or rare exact evidence-backed no-improvement proof after all reasonable current routes were exhausted: generated-output blocker resolved; split blocker resolved; dispatcher/original-name/class-method blockers preserved as evidence-backed score caps.
- [x] Owner/emitter/reconstructable changes to apply: none; target preserves [UID:0000N4] owner/emitter and reconstructable true.
- [x] Split/rename/new-child changes to apply: none; target preserves twelve exact children and `0xcc` padding gaps.
- [x] Source-placement, range/split/padding/reclassification, or IDA rename/type/comment changes to apply or confirm not applicable: target prose keeps file-static `RegistryConfig.cpp` placement and no IDA DB edits were requested.
- [x] First-draft C++ or no-code proof to apply: existing comment-only formal block for UID000220 preserved; child C++ remains on exact child pages.
- [x] Third-party import directive to apply or confirm not applicable: not applicable.
- [x] Exact target/support doc facts to incorporate at report-level detail: target evidence note, generated-output state, score rationale, unchanged metadata, rejected alternatives, and open-question conclusions applied; manual coverage row text retained only as supervisor-owned recommendation.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: zero xrefs, zero pointer patterns, no dispatch table proof, no monolithic body, no class-method proof, no successor merge, and no `95+` score preserved in target.
- [x] Wave2/Wave3 mentions or artifacts encountered and ignored/rejected as stale, if applicable: target page now historicalizes the Wave3 zero-byte generated-output blocker; missing current `simroot_v2` path was not used as authority.
- [x] Open questions to close or document as evidence-backed unresolved: dispatcher route, dispatch-table proof, original helper names, class-method promotion, and final class-layout fields remain unresolved score caps.
- [x] Validators to run: scoped file validator was run with command id `000000007419`; no manual coverage validator was run because the manual coverage row was not edited.
- [x] Generated report refresh expected or explicit manual supervisor-owned coverage/tracker text to apply: validator refreshed generated tracker/coverage and generated C++; manual coverage row replacement text remains supplied for supervisor-owned maintenance only.

Implementation callback pass:
- [x] Report accepted by supervisor for implementation: the earlier user callback cited accepted pre-repair report SHA `CF70E92150450638EF2309AFB981B9C64BAD82AEF33343E91AF4FFAD6278D7F3`; this row records that historical implementation callback, not a fresh Gate 1 result for the repaired artifact.
- [x] All accepted target/support doc details incorporated at report-level detail: target page updated with current B002 MCP evidence, generated-output freshness, score rationale, and rejected alternatives; support coverage row excluded by explicit callback boundary.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: see C1-C9 table above.
- [x] Metadata/score/owner/emitter/split/rename/C++ changes applied or explicitly not applied with reason: scores applied; owner/emitter/reconstructable/blank emitter position/comment-only C++ preserved; no split/rename/new child; no support coverage edit by scope.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence preserved: target now marks generated zero-byte blocker historical and preserves no dispatcher/caller, no dispatch table, no class-method promotion, no monolithic body, no successor merge, and no `95+` score.
- [x] Open questions resolved or documented with evidence-backed unresolved rationale: unresolved dispatcher/original-name/class-method/final field questions remain documented score caps.
- [x] Validators run and results recorded: scoped validator command id `000000007419`, timestamp `2026-07-05T22:11:26-04:00`, exit code `0`, `ok: 1`, generated refresh completed.
- [x] Generated report refresh completed by validator or explicit manual supervisor-owned coverage/tracker text supplied/confirmed unchanged: at B002 callback-time, `RegistryConfig.cpp` header matched command id `000000007419` and generated tracker/by-memory coverage rows showed `88/90` / `88%`; manual `by-memory/-coverage-report.md` was not edited. After supervisor execution, refreshed generated reports and validator lifecycle history are authoritative.
- [x] Lease lifecycle completed: B002 leased only `by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md` immediately before editing, ran the validator during the lease window, released it afterward, and verified no active leases in both B002 and shared lease reports.
- [x] Remaining unapplied accepted items listed with exact blocker: no target implementation items remain unapplied; manual coverage text remains supervisor-owned/excluded by callback scope.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000007432","destination_path":"executed-b-agent-research/B002/000220-RegistryCommandLineParsers-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/000220-RegistryCommandLineParsers-source-quality.md","timestamp":"2026-07-05T22:34:21-04:00","uid":"000220"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
