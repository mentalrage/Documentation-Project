** TARGET-REPORT-UID:00031N **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 00031N ScrollVolumePaneSetStateByteRaw Source-Quality Research

Assignment: `B002-report-00031N-scroll-volume-state-byte-20260625`

Target:

- [UID:00031N] `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetStateByteRaw.md`

Report-only result: recommend a narrow source-quality implementation callback. Do not edit target/support by-* docs during this pass.

## Final Recommendation

Rename the target's source-facing helper and field from generic byte-oriented names to the best current source-shaped names:

- Helper: `ScrollVolumePane::SetScrollStyle(unsigned char style)`
- Field: `m_scrollStyle` at `this+0xfa`
- Target filename recommendation: rename the by-memory page to `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetScrollStyleRaw.md` if the supervisor accepts this report.

This is an inferred source-facing name, not recovered original-symbol proof. The binary proves a real initialized byte field and a real invalidating setter, but the byte has no local consumers in the current `ScrollVolumePane` island. The strongest semantic inference is a scroll/slider visual style or style-state selector, because the identical raw setter pattern exists in the related scroll-pane family at `0x0055f530` and current ScrollSpellInventoryPane docs already classify the analogous `+0xfa` byte as unresolved scroll style/state.

Keep the current direct owner/emitter route:

- `CANONICAL_OWNER:0001H2`
- `EMITTER_UIDS:0001H2`
- source route through [UID:0000CO] `ScrollVolumePane` and [UID:0000NK] `ScrollVolumePane.cpp`

Recommended target score: `COMPLETION:88`, `CONFIDENCE:90`.

Reason: current MCP confirms exact bytes, no-function/no-xref/no-pointer route, constructor initialization, sibling setter and predicate context, a full local field-use census, and a scroll-pane-family comparative pattern. Keep below final-audit quality because the original source spelling and the live dispatch route to this raw start remain unrecovered.

## Current Target State

Current source page state:

- `COMPLETION:86`, `CONFIDENCE:89`
- `CANONICAL_OWNER:0001H2`
- `RECONSTRUCTABLE:TRUE`
- `EMITTER_UIDS:0001H2`
- formal C++ currently emits:

```cpp
void ScrollVolumePane::SetStateByte(unsigned char state)
{
    if (m_stateByte == state) {
        return;
    }

    m_stateByte = state;
    InvalidateRect(&m_bounds);
}
```

Existing useful facts to preserve:

- Exact range `0x005647f0-0x00564814`, exclusive end.
- Raw method-shaped body, not modeled as an IDA function.
- Compares byte argument against `this+0xfa`, stores only on change, and invalidates `this+0x44` through primary vtable slot `+0x20`.
- Constructor initializes the containing dword at `+0xf8` to `0x00010000`, setting `+0xf8` to `0`, `+0xfa` to `1`, and `+0xfb` to `0`.
- The previous sibling [UID:00031M] writes word `+0xf8`; the following modeled `SetRange` starts at `0x00564820`.
- No direct xrefs or obvious pointer-pattern route to `0x005647f0` are visible in the current IDB.

Current stale/incomplete facts:

- The target still says final helper/field semantics are open after using `SetStateByte` and `m_stateByte`.
- The current generated C++ at `auto-generated/NexusTK/ui/controls/ScrollVolumePane.cpp` includes the existing body, but its generated header still shows stale `Completion:76 | Confidence:84` for this UID. Treat generated output as a lead only.
- The generated tracker and generated by-memory coverage rows also still show `76/84`; do not edit those files manually.

## Evidence Checked

Local docs read in this pass:

- Target [UID:00031N] `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetStateByteRaw.md`
- Parent [UID:0001H2] `by-memory/0x00564710-0x005654ec.ScrollVolumePane.md`
- Class [UID:0000CO] `by-class/ScrollVolumePane.md`
- File [UID:0000NK] `by-file/ScrollVolumePane.md`
- Layout [UID:0001W2] `by-type/by-struct/ScrollVolumePaneLayout.md`
- Sibling raw pages [UID:00031M] and [UID:00031O]
- Interaction helper pages [UID:0002LB], [UID:0002LC], [UID:0001H3], [UID:0001H5], and [UID:0002LD]
- Executed reports: B014 `0000CO-ScrollVolumePane-class-source-quality.md`, B003 raw interaction report, B001 notify-helper report, B005 begin-interaction report
- Generated output lead only: `auto-generated/NexusTK/ui/controls/ScrollVolumePane.cpp`
- Comparative scroll-pane docs: B002 `0001GM-ScrollSpellInventoryPane-source-quality.md`, `ScrollSpellInventoryPaneLayout.md`, and `ScrollInventoryPaneLayout.md`

IDA MCP was mandatory and available.

MCP setup and session facts:

| Check | Result |
| --- | --- |
| `Test-NetConnection 127.0.0.1 -Port 13337` | `TcpTestSucceeded: True` |
| JSON-RPC `initialize` id `1` | server `ida-pro-mcp`, protocol `2025-06-18` |
| `tools/list` ids `2`, `4`, `5`, `6` | schema checked; active tools include `idb_list`, `server_health`, `lookup_funcs`, `xrefs_to`, `find_bytes`, `get_bytes`, `insn_query`, `search_text`, and `decompile` |
| `idb_list` id `3` | active session `80de0a67`, `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, backend `worker`, PID/worker PID `26892` |
| `server_health` id `10` | `status: ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready |

MCP function and route evidence:

- `lookup_funcs` id `11`:
  - `0x00564710` is modeled as `sub_564710`, size `0xa1`.
  - `0x005647c0`, `0x005647f0`, `0x00564814`, `0x005648f0`, `0x00565170`, `0x005651e0`, and `0x00565490` are `Not a function`.
  - Positive controls resolve normally: `0x00564820` is `sub_564820`, `0x00564910` is `sub_564910`, `0x00564e30` is `sub_564E30`, `0x00565010` is `sub_565010`, `0x005652a0` is `sub_5652A0`, and `0x00565360` is `sub_565360`.
- `xrefs_to` id `12`:
  - `0x005647f0`: zero xrefs.
  - `0x005647c0`: zero xrefs.
  - `0x005648f0`: zero xrefs.
  - Positive controls: `0x00564820` has two caller xrefs from `sub_5403B0`; `0x00564e30` has callers at `0x005649f1` and `0x0056530f`; `0x00565010` has 13 xrefs including raw-span xrefs; `0x005652a0` has three xrefs; `0x00565360` has two xrefs.
- `find_bytes` id `14` found zero matches for target/sibling pointer patterns:
  - `f0 47 56 00` target VA, `f0 47 16 00` RVA-style, `f0 45 16 00` raw-offset-style.
  - `c0 47 56 00` for [UID:00031M] start.
  - `f0 48 56 00` for [UID:00031O] start.
  - No ordinary VA/RVA/raw-offset literal route was found.
- `get_bytes` id `13`:
  - Target bytes at `0x005647f0`: `55 8b ec 8a 45 08 38 81 fa 00 00 00 74 12 8b 11 88 81 fa 00 00 00 8d 41 44 89 45 08 5d ff 62 20 5d c2 04 00`.
  - Trailing bytes at `0x00564814-0x00564820` are twelve `0xcc` bytes before `SetRange`.

MCP raw/constructor disassembly:

- `insn_query` id `16` over `0x005647c0-0x00564814` confirms:
  - [UID:00031M] compares/stores `[ecx+0F8h]` and invalidates via `[edx+20h]`.
  - [UID:00031N] starts at `0x005647f0`, compares `[ecx+0FAh]` with `al`, stores `[ecx+0FAh]`, and invalidates via `[edx+20h]`.
  - Both spans are `fn:null`.
- `insn_query` id `16` over the constructor confirms:
  - `0x00564743`: constructor argument stored to `[esi+0FCh]`.
  - `0x0056474d`: zero dword to `[esi+0FEh]`.
  - `0x0056477b`: `mov dword ptr [esi+0F8h], 10000h`.
  - `0x00564785`: `mov word ptr [esi+102h], 0FF01h`.
  - `0x0056478e`: `mov byte ptr [esi+104h], 0FFh`.

MCP decompilation highlights:

- `decompile(0x00564710)` id `200` confirms constructor shape:
  - `*(BYTE *)(this + 252) = a2` for `m_volumeType`.
  - `*(DWORD *)(this + 254) = 0` for value/range zeroing.
  - `*(DWORD *)(this + 248) = 0x10000`, setting `+0xfa` to default `1`.
  - `*(WORD *)(this + 258) = -255`, setting `+0x102 = 1` and `+0x103 = 0xff`.
  - `*(BYTE *)(this + 260) = -1` for active part.
- `decompile(0x00564820)` id `201` confirms `SetRange` uses words `this[127]`/`this[128]`, i.e. `+0xfe/+0x100`, not `+0xfa`.
- `decompile(0x00564880)` id `202` confirms `SetValue` writes `+0xfe`.
- `decompile(0x005648b0)` id `203` and `decompile(0x005648d0)` id `204` confirm `Enable`/`Disable` write `+0x102`.
- `decompile(0x00564910)`, `0x00564b50`, `0x00565010`, `0x005652a0`, and `0x00565360` ids `205-209` confirm local interaction/render/value paths use `+0xfb`, `+0xfc`, `+0xfe`, `+0x100`, `+0x102`, `+0x103`, `+0x104`, and `+0x108/+0x10c`, but not `+0xfa`.

MCP field-use census by scoped `search_text` over `0x00564710-0x005654ec`:

| Field | Hits | Interpretation |
| --- | ---: | --- |
| `0FAh` | 2 | Only target setter compare at `0x005647f6` and store at `0x00564800`. |
| `0FBh` | 4 | Paint/hit-test/part-rect drag-override reads at `0x00564c00`, `0x00564cf6`, `0x00564ee4`, and `0x005650b8`. |
| `0F8h` | 3 | Constructor packed dword store plus [UID:00031M] compare/store. |
| `0FCh` | 2 | Constructor stores volume type; notify helper passes it to `NewOptionPane::OnVolumeChanged`. |
| `0FEh` | 10 | Current value reads/writes. |
| `100h` | 11 | Range/max value reads/writes. |
| `102h` | 11 | Enabled byte reads/writes and `CanAdjust`. |
| `103h` | 13 | Highlight part reads/writes. |
| `104h` | 7 | Active part reads/writes. |
| `108h` | 3 | Drag-offset point formation/storage. |
| `10Ch` | 5 | Horizontal component of drag offset. |

This field census is the strongest new evidence for the assignment question: `+0xfa` is real state but has no local rendering, hit-test, interaction, range, value, enabled, highlight, active, volume-type, or drag-offset role inside this island.

Comparative scroll-pane evidence:

- Current ScrollSpellInventoryPane docs record the analogous raw setter at `0x0055f530-0x0055f554` as an unresolved `+0xfa` scroll style/state byte setter.
- MCP `insn_query` id `300` confirms that comparative body is byte-for-byte the same shape at the same offsets: `cmp [ecx+0FAh], al`, `mov [ecx+0FAh], al`, `lea eax,[ecx+44h]`, and `jmp dword ptr [edx+20h]`.
- ScrollSpellInventoryPane layout says use descriptive `scroll style/state byte` wording only; ScrollInventoryPane layout similarly leaves `+0xfa` as an initialized state byte. That cross-family pattern supports `m_scrollStyle` as a better source-facing name than generic `m_stateByte`, while still requiring an inference caveat.

## Heuristic / Inference Reanalysis

### Claim: `0x005647f0-0x00564814` is source-authored ScrollVolumePane code

Confidence: strong.

Evidence:

- It sits inside the exact `ScrollVolumePane` implementation island between constructor and `SetRange`.
- It is function-shaped code with a normal prologue, one argument, compare/store, invalidation call, and `retn 4`.
- Its field and invalidation behavior match the source-shaped sibling [UID:00031M] and comparative scroll-pane setters.
- Parent/class/file docs and generated output all route it through `ScrollVolumePane.cpp`.

Counter-evidence:

- IDA does not model a function object at the raw start.
- No xrefs or pointer-pattern route to the raw start were found.

Decision:

Keep it as a reconstructable raw method-shaped helper under [UID:0001H2]. Do not create an IDA function or claim live direct callers.

### Claim: `+0xfa` is not any already-named ScrollVolumePane runtime field

Confidence: very strong for the negative classification.

Rejected alternatives:

- `m_volumeType`: constructor argument stores to `+0xfc`, and notify helper reads `+0xfc`.
- `m_thumbDragActive`: local reads are at `+0xfb`; no target write touches `+0xfb`.
- `m_value` or `m_range`: words at `+0xfe/+0x100`.
- `m_enabled`: byte at `+0x102`.
- `m_highlightPart` or `m_activePart`: bytes at `+0x103/+0x104`.
- `m_thumbDragOffset`: point at `+0x108/+0x10c`.
- padding/dead byte: rejected because constructor initializes it to `1`, a raw helper can change it, and the helper invalidates the pane bounds on change.

Decision:

Document `+0xfa` as an inferred scroll/slider style-state byte rather than a known interaction field.

### Claim: best source-facing name is `m_scrollStyle` / `SetScrollStyle`

Confidence: medium-high as a source-shape inference, not original-name proof.

Evidence for:

- The body is an invalidating setter, which is the source shape of a visual/configuration field more than a transient event field.
- The analogous ScrollSpellInventoryPane raw setter is documented as an unresolved `scroll style/state byte`.
- `ScrollVolumePane` is in the same scroll-pane family and uses the same constructor-store layout around `+0xf8/+0xfa/+0xfb`.
- `SetStateByte`/`m_stateByte` is accurate but type-shaped and less plausible as human source than a role name.

Counter-evidence:

- The current `ScrollVolumePane` island never reads `+0xfa` outside the setter.
- No direct caller exists to reveal the API call site or argument name.
- No string/resource/debug evidence names the byte.

Decision:

Use `SetScrollStyle(unsigned char style)` and `m_scrollStyle` as the best current source-facing names, while preserving a page note that the exact original spelling and exact visual effect remain unrecovered. This is stronger than leaving `SetStateByte`, but it should not raise the target to final-audit confidence.

### Claim: existing first-draft C++ is behavior-correct but should be renamed

Confidence: strong.

The existing formal body exactly matches behavior. The improvement is source-facing naming only. Replacing `SetStateByte/state/m_stateByte` with `SetScrollStyle/style/m_scrollStyle` preserves behavior and moves the output away from type-shaped reverse-engineering names.

Recommended exact replacement for the target formal `RECONSTRUCTION_CPP CODE` block:

```cpp
void ScrollVolumePane::SetScrollStyle(unsigned char style)
{
    if (m_scrollStyle == style) {
        return;
    }

    m_scrollStyle = style;
    InvalidateRect(&m_bounds);
}
```

The class declaration should be synchronized at the same time:

```cpp
void SetScrollStyle(unsigned char style);
unsigned char m_scrollStyle;               // +0xfa
```

## Score / Metadata Disposition

Recommended target metadata after implementation:

| Field | Current | Recommended | Rationale |
| --- | --- | --- | --- |
| `COMPLETION` | `86` | `88` | The open naming/field blocker has been investigated with current MCP and resolved to an implementation-ready source-facing inference. |
| `CONFIDENCE` | `89` | `90` | Exact bytes, field census, route negatives, constructor init, and comparative pattern are current; cap below final audit for no direct caller and unrecovered original spelling. |
| `CANONICAL_OWNER` | `0001H2` | unchanged | Exact child of the ScrollVolumePane implementation island. |
| `RECONSTRUCTABLE` | `TRUE` | unchanged | Custom source-authored helper. |
| `EMITTER_UIDS` | `0001H2` | unchanged | Existing child-body route through the aggregate/class/file is correct. |
| `EMITTER_POSITION_OPTIONAL` | blank | blank | Existing child ordering through aggregate positions is sufficient; no new order needed unless validator/autogen requires it. |

Recommended target title/summary:

- Title: `# 0x005647f0-0x00564814 - ScrollVolumePane Set Scroll Style Raw`
- Item Summary: `Raw ScrollVolumePane helper stores inferred scroll/slider style byte +0xfa, initialized to 1 by the constructor, and invalidates bounds when changed; current MCP finds no other +0xfa consumers and no direct raw-start route, so SetScrollStyle/m_scrollStyle is source-facing inference rather than original-name proof.`

## Target And Support Doc Changes If Accepted

Apply these only after supervisor implementation callback and leases.

1. Target [UID:00031N]
   - Rename file to `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetScrollStyleRaw.md`.
   - Raise metadata to `88/90`.
   - Replace formal C++ with `SetScrollStyle(unsigned char style)` block above.
   - Replace `SetStateByte`, `m_stateByte`, and generic "state byte" prose with `SetScrollStyle`, `m_scrollStyle`, and "inferred scroll/slider style-state byte" wording.
   - Add the current MCP evidence: session `80de0a67`, `lookup_funcs`, `xrefs_to`, `get_bytes`, `find_bytes`, constructor decompile, field-use census, and comparative ScrollSpellInventory setter.
   - Preserve no-function/no-xref/no-pointer-route caveat.

2. Parent [UID:0001H2] `ScrollVolumePane`
   - Update the covered row for [UID:00031N] from `SetStateByte(unsigned char)` to `SetScrollStyle(unsigned char)`.
   - Mention `+0xfa` as inferred `m_scrollStyle`, initialized to `1`, with no local consumers outside the setter.
   - Keep aggregate no-code policy and owner/emitter unchanged.
   - Do not change sibling [UID:00031M] beyond link-label consistency unless B001's sibling report is accepted.

3. Class [UID:0000CO] `ScrollVolumePane`
   - Update declaration from `void SetStateByte(unsigned char state);` to `void SetScrollStyle(unsigned char style);`.
   - Update field from `unsigned char m_stateByte; // +0xfa` to `unsigned char m_scrollStyle; // +0xfa`.
   - Update method notes and layout notes to explain the name is inferred from the scroll-pane family and target setter behavior.
   - Keep exact method bodies on by-memory children.

4. File [UID:0000NK] `ScrollVolumePane`
   - Update proposed contents row for [UID:00031N] to `SetScrollStyle` / inferred scroll style-state byte.
   - Keep source placement `NexusTK/ui/controls/ScrollVolumePane.cpp`.

5. Layout [UID:0001W2] `ScrollVolumePaneLayout`
   - Update row `+0xfa` to `m_scrollStyle`, size 1, inferred scroll/slider style-state byte.
   - Add current field-census evidence: scoped MCP `search_text` found `0FAh` only in [UID:00031N], while `0FBh`, `0FC`, `0FE`, `100h`, `102h`, `103h`, `104h`, and `108h/10Ch` have separate established roles.
   - Preserve `+0xfb` as distinct `m_thumbDragActive`/drag override with no local writer found.

6. Generated outputs and trackers
   - Do not manually edit generated reports, project-level files, validator state/cache, IDA DB, or any coverage report.
   - Let scoped validators refresh generated/autogen views. Current generated `-ag-research-tracker` and `-ag-coverage-report-by-memory` rows are stale at `76/84`; source metadata should be the fix.

7. IDA rename/type/comment recommendations only, no DB edit in this pass
   - Rename/comment raw start `0x005647f0` as `ScrollVolumePane::SetScrollStyle`.
   - Name/comment field `+0xfa` as `m_scrollStyle`: inferred scroll/slider style-state byte, default `1`, no local reads outside setter.
   - Keep a comment that no direct caller or pointer route is currently visible.

## Validators Expected After Implementation

Run from `source-3/project-documentation` after accepted edits:

> Executable block R001 was removed from this report and preserved verbatim in [00031N-ScrollVolumePaneSetStateByteRaw-source-quality-removed.md](00031N-ScrollVolumePaneSetStateByteRaw-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

If the validator reports rename/reference repair requirements, use validator-supported file scans/lifecycle behavior only. Do not hand-edit validator state.

Expected generated/autogen effect:

- `auto-generated/NexusTK/ui/controls/ScrollVolumePane.cpp` should emit `SetScrollStyle` and `m_scrollStyle`.
- Generated memory coverage and research tracker rows should refresh from source metadata.
- If generated refresh is deferred, report queue/generated-header freshness in the implementation result.

## Remaining Uncertainty

- Exact original source spelling is still unrecovered.
- No direct caller, function pointer, vtable slot, VA literal, RVA literal, or raw-offset-style pattern targets `0x005647f0`.
- The exact runtime effect of changing `+0xfa` is not visible inside the `ScrollVolumePane` island; no local consumer reads it.
- `m_scrollStyle` is therefore an inferred source-facing name. It is preferable to `m_stateByte` for final source shape, but it should be documented as inferred until a caller or consumer is found.

These uncertainties cap final confidence but do not block first-draft C++. The target is already eligible under the active reconstructable/nonblank-emitter/combined-score gate.

## Report-Only Write/Validation Status

Changed files in this pass:

- `tools/leaser/Agents/Agent-B002/research/00031N-ScrollVolumePaneSetStateByteRaw-source-quality.md`

Leases:

- None. Report-only research in the agent folder does not need leases.

Validators:

- Not run. No by-* files were edited during this report-only pass.

Generated/project/tool-state/coverage/IDA DB edits:

- None.

## Implementation Tracking Checklist

- [x] Lease only the immediate target/support docs to be edited, after supervisor acceptance and immediately before editing. Proof: `python .\tools\leaser\leaser.py B002 lease ...` succeeded for old target path plus [UID:0001H2], [UID:0000CO], [UID:0000NK], and [UID:0001W2] before edits.
- [x] Rename [UID:00031N] target file to `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetScrollStyleRaw.md`, or record supervisor decision if the old filename is intentionally retained. Proof: old path `by-memory/0x005647f0-0x00564814.ScrollVolumePaneSetStateByteRaw.md` is absent, new path exists, and validator `000000001677` recorded `path_update 00031N ... was ...SetStateByteRaw.md`.
- [x] Update [UID:00031N] metadata to `COMPLETION:88`, `CONFIDENCE:90`, owner/emitter unchanged. Proof: target header now has `COMPLETION:88`, `CONFIDENCE:90`, `CANONICAL_OWNER:0001H2`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0001H2`, and blank `EMITTER_POSITION_OPTIONAL`.
- [x] Replace [UID:00031N] formal C++ with the exact `ScrollVolumePane::SetScrollStyle(unsigned char style)` block from this report. Proof: target `RECONSTRUCTION_CPP CODE` block now emits `SetScrollStyle`, compares/stores `m_scrollStyle`, and calls `InvalidateRect(&m_bounds)`.
- [x] Update [UID:00031N] summary/body/title/evidence with current MCP session `80de0a67`, field-use census, constructor initialization, no-route evidence, comparative scroll-pane pattern, and inferred-name caveat. Proof: target title is `ScrollVolumePane Set Scroll Style Raw`; item summary names inferred `+0xfa`; body records MCP session `80de0a67`, exact bytes/disassembly, no-function/no-xref/no-pointer-route evidence, constructor default `+0xfa = 1`, scoped field-use census, and comparative `ScrollSpellInventoryPane` setter evidence.
- [x] Update [UID:0001H2] `ScrollVolumePane` parent row/support notes for `SetScrollStyle`/`m_scrollStyle`, while leaving aggregate C++ blank. Proof: parent covered row points to `...SetScrollStyleRaw.md`, names `SetScrollStyle(unsigned char)` and inferred `m_scrollStyle`, adds B002 MCP/census/comparative evidence, and its formal C++ block remains blank.
- [x] Update [UID:0000CO] `ScrollVolumePane` declaration and field list from `SetStateByte`/`m_stateByte` to `SetScrollStyle`/`m_scrollStyle`. Proof: class C++ declaration now has `void SetScrollStyle(unsigned char style);` and `unsigned char m_scrollStyle; // +0xfa`; method/layout/source-quality notes preserve the inferred-name caveat.
- [x] Update [UID:0000NK] `ScrollVolumePane` file proposed contents and source-quality notes. Proof: file page proposed contents row points to `...SetScrollStyleRaw.md`, describes the inferred `m_scrollStyle` `+0xfa` setter, and source-quality sync records the 2026-06-25 B002 evidence.
- [x] Update [UID:0001W2] `ScrollVolumePaneLayout` row/evidence for `+0xfa` as inferred `m_scrollStyle`; preserve distinct `+0xfb` drag-override field. Proof: layout row `+0xfa` is `m_scrollStyle`; B002 evidence note records `0FAh` only in the setter and separates `+0xfb` as drag override used by paint/hit-test/geometry.
- [x] Do not edit [UID:00031M] target during this callback unless the supervisor explicitly includes B001's sibling acceptance; only keep support-doc references consistent with this target. Proof: no edit was made to `by-memory/0x005647c0-0x005647e7.ScrollVolumePaneSetLowStateWordRaw.md`; support docs still leave [UID:00031M] as `SetLowStateWord`.
- [x] Do not manually edit generated reports, project-level generated files, validator state/cache, IDA DB, or any `-coverage-report.md`. Proof: manual edits were limited to the accepted target/support docs and this B002 report. Validator commands updated validator-owned/generated state as expected; validator `000000001677` also propagated the renamed UID link into legacy `by-memory/-coverage-report.md`, which was not manually edited and is reported as a validator side effect.
- [x] Run scoped validators listed above and report command, working directory, `command_id`, `command_timestamp`, exit code, `ok` count, warnings, and generated-refresh status. Proof from `E:\NTK\GhidraBridge\source-3\project-documentation`: target validator `000000001677` at `2026-06-25T15:02:00-04:00`, exit 0, `ok: 1`, generated refresh deferred/caught up; parent validator `000000001678` at `2026-06-25T15:02:26-04:00`, exit 0, `ok: 1`, warning only pre-existing stale `0001GJ`; class validator `000000001679` at `2026-06-25T15:02:33-04:00`, exit 0, `ok: 1`; file validator `000000001680` at `2026-06-25T15:02:42-04:00`, exit 0, `ok: 1`; layout validator `000000001681` at `2026-06-25T15:02:50-04:00`, exit 0, `ok: 1`. `python .\tools\validator.py --queue-status` returned no queued or processing generated refresh jobs.
- [x] Release any leases immediately after the edit/validator batch and report final lease cleanup. Proof: cleanup command reported no active B002 leases, and `tools/leaser/Agents/current_leases.md` had no B002 rows after validation; only unrelated Agent-B007/B010 leases remained.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B002","command_id":"000000004165","destination_path":"executed-b-agent-research/B002/00031N-ScrollVolumePaneSetStateByteRaw-source-quality.md","details":"legacy registry reconstruction","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/00031N-ScrollVolumePaneSetStateByteRaw-source-quality.md","timestamp":"2026-06-25T15:08:58","uid":"00031N"} -->
<!-- {"agent":"B002","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 00031N-ScrollVolumePaneSetStateByteRaw-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B002/00031N-ScrollVolumePaneSetStateByteRaw-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"00031N"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
