** TARGET-REPORT-UID:0002FJ **
** TARGET-REPORT-ADDITIONAL-UIDS:0002FK **
** AUTHOR-AGENT-ID:B002 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID0002FJ ChattingVarietySelectPane HandlePointerOrMouseEvent Source-Quality Research


## Finalized Report / Current Recommendation

- Current ordinary-document result: primary [UID:0002FJ] and supporting additional target [UID:0002FK] are `COMPLETION:94`, `CONFIDENCE:95`, retain each target's `CANONICAL_OWNER:00001Z`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00001Z`, and blank optional position, and each exact source-ready method body is physically present once in its own formal CPP channel.
- Final disposition: this is the ordinary source-authored `bool ChattingVarietySelectPane::HandlePointerOrMouseEvent(Event *event)` override. It is neither compiler glue nor an unresolvable decompiler-only dispatcher.
- Header disposition: both by-memory H channels remain blank because [UID:00001Z] now owns the sole class declarations `virtual bool HandlePointerOrMouseEvent(Event *event);` and `virtual bool OnDismiss(Event *event);`; the stale zero-argument declaration is absent. The exact shared say-mode enum/helper block is physically present in UID0002RX's formal H route and documented by [UID:0000N9], never either Chatting child H channel.
- Ordinary support result: [UID:00001Z], [UID:0001KO], [UID:0000N9], and [UID:0002RX] contain the complete accepted method/helper/header closure while retaining their existing scores and aggregate policies. The primary supervisor also applied and validated the exact manual coverage payloads and completed the coherent generated refresh/readback; those supervisor-owned outcomes are recorded below without assigning their authorship to B002.
- IDA disposition: the primary target name/type/comment remained protected. Primary-supervisor Gate 2B completed the adjacent dismiss method and five row opener improvements as thirteen atomic persisted mutations: six names, six function-regular comments, and one dismiss type correction preserving the physical one-argument `retn 4` ABI. The saved transaction is dated evidence; every later gate still rebinds canonical disk and a healthy session dynamically.
- Lifecycle authority: report execution/archive state is authoritative only from the report's current path plus validator-owned status/history metadata. This report body does not claim a current lifecycle disposition.

## Supporting Research

- Callback prestate target snapshot was SHA256 `318D586FE455F06BA3247841F91BD8F6B7FE7E583D06E2A85B7C74C6B82955F0`, 12,660 bytes, 114 lines. Post-validator target is SHA256 `1F6074938C58F5999CC779B845901CA6255CB3E8666B48356AC7AB589BE41BFD`, 19,140 bytes, 209 lines.
- Callback prestate additional-target snapshot was SHA256 `11384712A6F4F27400FE7725B77266CDF0A68C8503A6B7161832D6B28BB89839`. Post-validator UID0002FK is SHA256 `58C747AF507A65A91520D061785E0A17916B955733EA106CAC9FAA2BD0F8E654`, 8,811 bytes, 92 lines; its exact body is present and the class no longer has the stale zero-argument declaration.
- Callback prestate support snapshots were class `495FCAC8E58E1F43E52040469697D56E1333657AD6119AA0BA4E58842706EBA4`, UID0002RX `0D27C1A4C35501647D89EE04D76A0555F77B8DEDB00001EB06C24FAF8FBD1205`, UID0001KO `05AAE1C8100A6EFCF2FBFE566AB425E11CAEDC6B6680ABB0349B6F404038B120`, and SayInputPanes file `C8AA2BB30572D060D7983642B63DD20F5FA143B560B3B7E3CA5DD2D9A2A4509F`.
- Post-validator support snapshots are class SHA256 `EA00A1945974F171BAC12E684A4E5C2FF262BEB9AC59700E0DB7B4FC1ADA7B91`, 25,984 bytes/158 lines; UID0001KO SHA256 `E52C38D51AFD19E443B604F5A5E7FBC964D3A9F03A8C8D4E66CA720207C4630E`, 24,492 bytes/167 lines; UID0000N9 SHA256 `7416EFCFC243C48CF6DD1F98C10116D9E24ECB46C5F2D4C92A5C65B9769F5406`, 49,531 bytes/236 lines; and UID0002RX SHA256 `47F1496A491DF78C5586FE3315769B4B50BF1560E00BC170194CC38339DD5C83`, 13,997 bytes/159 lines.
- [UID:000106] hit-test remains the unchanged support snapshot SHA256 `BE6B665DD1CD12FE6B01A33472981A1177903C77CDF5AF34AD710B93F7C4A600`; callback-time reread found no semantic drift requiring expansion into Event, Pane, ChattingVarietyPane, ChattingColorPane, globals, or UID000106.
- Additional current documentation read: `by-class/Event.md` SHA256 `8C949A52AAF5D810407FDFFCDFDCE7397DF791E295E9C68D7301A4C4B1C93419`; `by-class/Pane.md` SHA256 `445E2ACD469248D25C3965B8D6B22A1EC4BC45B803A9AD2672D47E017F54D1A5`; `by-class/ChattingVarietyPane.md` SHA256 `356945596D97B29B13D324492E5C3958003B3DC7FEB032ABAB9BF54EA70E3A5C`; `by-class/ChattingColorPane.md` SHA256 `92E3396914162C0CDE0CEEA8E6D91E7D4B01685235864596D63B995AE5240DD6`; `by-global/g_pCollectionData.md` SHA256 `3341473A64A33E4D9D206B799BE40810078C86C1ED30B3E5BAFBC6AD8A1C9290`; and `by-global/g_pChattingVarietyPane.md` SHA256 `11569E25A2056737286F43B660F69C2E33E7C801D5D490E282FCD436A9181059`.
- Matching executed reports were read and revalidated rather than copied: B003 selector-family SHA256 `4054973D0885D39F0892667D9A4E9904E3C420341B58FAF67D883CC3DAB61C91`; B003 UID000106 SHA256 `4F1FE3A0A67C8FF66751DA82A0E6FB0B93D3B6E137AC710B0178E3E213B711A5`; B001 raw-helper closure SHA256 `587F6DB1E55E3622DD49BC3256014A4202DCB77253808F8456192B1D7CCB0132`; B002 selector singleton SHA256 `C81C70F56018F4FEAC3846B1B386891470ED1185A4FBD1D64B4EB495FD554E11`; B004 compact-pane singleton SHA256 `80E6A6EA3944DF90285AF7BD6D889038E1E1AD770FAF0508F0427AA5352E137A`; and B009 mode-dispatch report SHA256 `67032072C1159FBC597ED621BD20642E284C51E86B3BF03447F4C7AFD3070903`.
- Historical assumptions retained but superseded: `OnMouseEvent`, reversed X/Y payload fields, unresolved `PaneMouseEvent`, zero-argument `OnDismiss()`, unresolved `this` adjustment, unresolved `sub_5A*` semantics, and the polluted STL name at `0x005a5450` were reasonable intermediate labels. Current Event/Pane/class/helper documentation plus live IDA closes them as `HandlePointerOrMouseEvent`, Y then X, one ignored `Event *` dismiss argument, complete-object fields/bounds, and human source opener names.
- Stale Wave2/Wave3 mentions encountered in historical material were ignored as instructed; none is used as current evidence.
- Primary-supervisor completion evidence: Gate 2B applied and persisted C0002FJ-024 through -032 from byte-identical backup/P0 SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D` to saved S1 SHA256 `2ACC15A3372DB71C34296F263700AC30C18ADDD39D4E1F3BB5D6035B278B1024`, with all six names, one OnDismiss type, six function-regular comments, complete protections, fresh-reopen persistence, and one save. A 2026-08-06 read-only disk recheck still found canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` at S1, 143,197,232 bytes; live-session identity remains dynamic rather than report-pinned.
- Primary-supervisor manual/generated evidence: commands `000000021325`, `000000021326`, and `000000021327` applied the exact by-memory/by-class/by-file payloads with exit `0`/`ok:1`; coherent autogen command `000000021328` completed and produced the accepted target bodies, corrected declaration, blank child-H routes, shared SayInputPanes header, coded ag-memory rows, and `94/95` tracker rows. Later unrelated project work advanced shared physical hashes without changing those report-specific semantics; the current read-only identities are recorded under Current Target State.

## Target

- Primary UID/path: [UID:0002FJ] `by-memory/0x004811c0-0x00481461.ChattingVarietySelectPaneOnMouseEvent.md`.
- Additional target UID/path: [UID:0002FK] `by-memory/0x00481470-0x0048148f.ChattingVarietySelectPaneOnDismiss.md`.
- Exact executable range: `[0x004811c0,0x00481461)`, 673 bytes / `0x2a1`, body SHA256 `2B3B9A31FD6344668DE5BCBA88B6A343CD030E6EA39D434D5E67F658508F6E0A`.
- Exact fences: four `0xcc` bytes at `[0x004811bc,0x004811c0)`, SHA256 `8843B54D2DF63CA265CF4A05D27DD2B29A74FB476D296DD44A0E171D74B441CA`; fifteen `0xcc` bytes at `[0x00481461,0x00481470)`, SHA256 `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C`.
- Exact vtable ownership: the only inbound reference is data slot `0x00614f84`; the 28-byte slice `[0x00614f70,0x00614f8c)` is `0x00481490, 0x004816d0, 0x00481750, 0x006444a4, 0x00483a85, 0x004811c0, 0x00481470`, SHA256 `8A6936015BE1BD4D0028751C7D6F6B485D927AE23626936830C858CEAED64218`.
- Source route: class child of [UID:00001Z] and file child of [UID:0000I5] `Chatting`, generated to `auto-generated/NexusTK/social/Chatting.cpp`.
- [UID:0002FK] is declared because correcting its ABI, class declaration, formal CPP, score, IDA type, and manual/generated rows is required to make the primary target's human source call complete. Other pages remain bounded support destinations, not additional full-report targets.

## Current Target State

- Ordinary-document state: UID0002FJ and UID0002FK are each `94/95`, owner/emitter `00001Z`, reconstructable true, blank position, exact formal CPP present once, and blank formal H. Callback-time class owner state was `92/94`; later unrelated accepted work advanced the current class to `93/94` at SHA256 `4F9F36A9187AE21AFBE32AA3CCCBA6BD7180D88102CF29CFC20CD3D954A4F023` while preserving both sole declarations, including corrected `OnDismiss(Event *event)`.
- The target Item Summary, Status, Behavior, Selector Rows, Source Shape, exact body/fence/vtable evidence, positive/negative evidence, Score Rationale, and Changes now contain the complete accepted source closure. The historical `OnMouseEvent` filename remains for UID/path stability only.
- UID0001KO remains `86/88`, false/non-emitting with blank aggregate formals while recording all five selector opener roles and the exact no-synthetic-body rationale. UID0000N9 remains `89/90`; UID0002RX remains `90/92`, with CPP definition only and the exact shared formal H block.
- Historical pre-callback generated snapshot: `auto-generated/-ag-memory-coverage.md` SHA256 `BF35AA00AD95A88EEABFB7DBDE1C0BFB54720C455B81EB657823400344AF27E2` routed both children through owner/emitter `00001Z` with CPP/H no; tracker SHA256 `3A4FEE87EBFA72B9653E8B55A5B677617266927F0F26CC5D2166825C4EAF34D5` scored both `86/91`; and Chatting.cpp SHA256 `9F7E1B690A08546A62F8867106FD4388387965C4B620341EC9597D6ABD25AD2E` contained their empty-emitter markers. These are superseded history.
- Dated accepted command-21328 snapshot: Chatting.cpp `CF3EF4544B2836EA5470823C1D98E8C805C31EDC8F231A095B402147703E8B7B`, Chatting.h `FCEDA924540BAFC882458000512A66A830154929C4AB23051890152C37D061FB`, SayInputPanes.h `39B9D6E43BC64DEB3D9DBB618DEDAA88EEEA7B3B63CAC77DCE251639C1152F15`, ag-memory `5EA0CC0C141090B952494EBE6763EDA7484A2AEC5B14BA84A23D3A276796FC62`, and tracker `8751F07252CC0D86B58E3B06C31ECFCB881EBF5057FF0854D9CC5329D265252F` passed the exact semantic checks supplied by the primary supervisor.
- Current read-only physical snapshot after unrelated later project refreshes: Chatting.cpp SHA256 `C6079EC44BD3635F2F12E966529DBEEFDB6AFA862D7CBAEF92560524658BF23D`, 73,506 bytes/2,246 lines; Chatting.h `92A7C528E6A461E6F53CA60FAF50C3ADFA6021FAE7E39196B48A75015D527CD7`, 1,442 bytes/40 lines; SayInputPanes.h `39B9D6E43BC64DEB3D9DBB618DEDAA88EEEA7B3B63CAC77DCE251639C1152F15`, 1,235 bytes/41 lines; ag-memory `C07A44E275034E11BA517F359FDB36ACEB5B6DFF4A6D9A778C7CE5E5DB89AC8A`; tracker `961CADC8D20281A0D0CADD9A8C9BDC043DD685F9238EA5662FA5E485BEA90119`. UID0002FJ/UID0002FK remain coded once in Chatting.cpp at `94/95`, owner/emitter `00001Z`, CPP yes/H no; the corrected OnDismiss(Event*) route and SayInputPanes declarations remain present, and both tracker rows remain `94/95`, true, `0/0/0`.
- These fixed hashes are dated evidence, not permanent authority. Every supervisor gate and post-lifecycle audit must reread current physical files and dynamically bind the current healthy MCP session and canonical disk IDB.

## Executive Recommendation

- Ordinary callback completed: both exact bodies, blank-child-H rationale, `94/95` target metadata, full target behavior/source/evidence/history, corrected class declaration, mixed helper closure, and shared SayInputPanes formal H route are physically present and scoped-validated.
- No callback-time semantic drift required expanding ordinary scope beyond the six authorized destinations.
- The primary supervisor completed thirteen atomic IDA outcomes with full protection and persistence proof. Section 21 now records only the accepted declarative name/type/comment outcomes, exact prestates, protections, and expected readbacks; removed operational material is preserved in the inert companion.

## Supervisor Active Recheck

- Dated read-only MCP snapshot at 2026-08-05T16:47-16:50-04:00: sole session `b007-uid0003yt-persist-reopen`, PID/worker PID `23164`, canonical IDB path, active, owned/adopted, not analyzing. `server_health` returned `status:ok`, image base `0x400000`, auto-analysis ready, Hex-Rays ready, and string cache ready with 2,068 entries.
- Dated canonical disk snapshot at 2026-08-05T16:47-04:00: `E:/NTK/Resources/NexusTK/NexusTK.exe.i64`, 143,196,749 bytes, last write `2026-08-05T16:38:40.6717821-04:00`, SHA256 `23B89BF35334D876FE63C04AE15C4F90AEF4DF41FAF9D79580F25D244A214283`.
- Earlier read-only rebind at 2026-08-05T16:59-04:00: the first worker had rotated normally; `idb_list` returned sole replacement session `b005-uid0001ny-d1-rollback-reopen`, PID/worker PID `22636`, on the same canonical IDB. `server_health` remained `status:ok`, Hex-Rays/string cache ready, and bounded `lookup_funcs` returned the same target/dismiss/five-helper names, addresses, and sizes. `auto_analysis_ready:false` did not constitute an outage because valid IDB-backed queries succeeded.
- Bounded self-audit recheck at 2026-08-05T17:08-17:12-04:00 used the same healthy replacement session read-only. Exact `0x481470` disassembly is `push esi; mov esi,ecx; ...; xor al,al; pop esi; retn 4`, body bytes `56 8b f1 8b 0d d8 ad 67 00 8b 01 ff 50 48 8d 8e 60 ff ff ff e8 07 32 0c 00 32 c0 5e c2 04 00`. IDA's pre-type prototype already exposes one explicit `int` argument even though the pre-type physical `stack_frame` list omits the logical argument row and contains only the return row. Same-slot selector callbacks `0x555ae0` and `0x5bd3a0` also end `retn 4`; current Vote/Menu source documentation models that slot with one ignored event pointer. This disproves the earlier zero-argument recommendation.
- Latest pre-transaction bounded rebind at 2026-08-05T17:20-17:22-04:00 followed another supervisor-owned IDB transaction: sole healthy session `b005-uid0001ny-postsave-reopen`, PID/worker PID `24380`; canonical disk `E:/NTK/Resources/NexusTK/NexusTK.exe.i64`, 143,196,991 bytes, last write `2026-08-05T17:20:02-04:00`, SHA256 `115491A99AE02621692E9F7AF4C9E3F011E77DD9678AF996A39A86AA02837695`. Bounded read-only analysis reconfirmed the exact target, dismiss, and five-helper names/types/sizes/caller-callee sets and internal comments used below. That session/disk identity is dated pre-transaction history superseded by the accepted P0-to-S1 transaction.
- Historical failed-attempt evidence proved I002's deterministic post-type frame is exactly `__return_address +0x4/4/_UNKNOWN *` plus `event +0x8/4/Event *`, confirming the one-explicit-argument `retn 4` ABI. The attempt was not persisted; canonical IDB identity remained SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes, and it receives no credit.
- Historical `GATE2B_FAILED_PRE_SAVE` evidence established literal API row `size 0x2`, type `wchar_t`, for the listed I003/I005/I006 buffer rows, distinct from inferred storage spans. No mutation persisted; P0 remained SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes, and the audit grants no action credit.
- Accepted primary Gate 2B outcome: thirteen atomic actions persisted from P0 SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D` to S1 SHA256 `2ACC15A3372DB71C34296F263700AC30C18ADDD39D4E1F3BB5D6035B278B1024`, both 143,197,232 bytes. All six names, the exact OnDismiss type, six function-regular comments, and every item/frame/comment/byte/xref/UDT/sibling protection passed independent persisted readback.
- Endpoint-feasibility audit after the controlled MCP restart: current `rename` schema exposes `pure:true`, implemented for function/data renames as `idaapi.set_name` while skipping `/vibe/` function-directory creation/linking and `refresh_decompiler_ctext`. All six reproducible function rename calls below therefore require `pure:true`; no local or stack rename is requested. `set_function_comments` calls `idc.set_func_cmt(function_start, text, False)` and changes only the regular function-comment channel. `set_address_comments` calls `idaapi.set_cmt(address, text, False)` and changes only regular disassembly/address comments, but this report requests no address-comment mutation, so that endpoint is prohibited in the transaction and all address regular/repeatable channels remain protected unchanged.
- Session and runtime identities in this report are dated evidence only. Any future IDA access must establish current authority outside this report.
- Fresh `lookup_funcs` reconfirmed target `ChattingVarietySelectPane__HandlePointerOrMouseEvent` size `0x2a1`, `sub_481470` size `0x1f`, exact `ChattingVarietySelectPane__HitTest` size `0x8b`, and the five `0x005a*` opener bodies with sizes `0x227`, `0x54`, `0xca`, `0xca`, and `0x81`.
- Fresh target `analyze_function` reconfirmed prototype `bool __thiscall(ChattingVarietySelectPane *this, Event *event)`, only vtable inbound reference `0x00614f84`, ten exact callees, 30 basic blocks, cyclomatic complexity 12, and the complete guard/hover/press/release/row behavior.

## Inference Research Guidance Check

- Binary-exact behavior has priority: every branch, state write, point order, row mapping, helper call, refresh, deletion condition, allocation size, and return value in the formal body is constrained by live code.
- Human source shape is also mandatory: compiler labels, offsets, adjusted pointers, cookie state, and temporary-register variables are replaced with current project class/field/helper vocabulary.
- Lack of original symbols is not a reason to leave `sub_`, `dword_`, or the false STL alias in final source. The report ranks names from literals, constructors, sibling docs, callers, and mode writes and selects one source-facing result for each.
- Naming consistency is subordinate to exact execution and plausible human source, but the selected `Open...InputPane` helper family matches existing `SayInputPanes` vocabulary and the existing accepted `OpenInputPaneForCurrentSayMode` name.
- The report does not treat no-xref evidence as no-source proof. All helper bodies are live through the target or sibling callers, and the target is vtable-owned.
- No blocker is deferred as "needs investigation." Residual uncertainty is limited to exact original lexical spelling, is explicitly ranked, and does not prevent source-ready CPP/H decisions.

## Heuristic / Inference Reanalysis And Validation

- Method identity: `HandlePointerOrMouseEvent` outranks historical `OnMouseEvent` because the canonical EventHandler class surface and multiple current pane classes use that source-facing override, and IDA already carries the exact class-qualified name/type. `OnMouseEvent` survives only in the historical filename/title.
- Event model: `event+4` is `Event::m_type`; `event+8` and `event+0xc` are `m_payload.m_pointer.m_y` and `.m_x`. Event kinds `0`, `1`, and `3` are cursor move, left-button down, and left-button up. Both HitTest call sequences and both `PointInRect` call sequences independently prove Y then X.
- Adjusted receiver: the decompiler's local receiver is the complete selector plus `0xa0`, explaining apparent `+0x58/+0x59/-0x5c/-0xa0`. Source fields are complete-object `m_pressCaptured +0xf8`, `m_hotRow +0xf9`, inherited `m_visibleBounds +0x44`, and the complete `Pane` receiver.
- Modal guard: `g_pUserPane->m_inputLocked` is the documented byte at `UserPane +0x13eb1d`; the exact immediate false return is intentional input suppression.
- Hover: signed `HitTest` result is stored in `m_hotRow`, the visible bounds are invalidated, and the method returns true only for `0..5`.
- Press: an inside point sets `m_pressCaptured`, invalidates the visible bounds, and returns true. An outside point performs the same compact refresh/deferred-delete sequence as `OnDismiss` and returns false.
- Release: no captured press returns false. A captured press atomically becomes `false/-1`, then HitTest selects row `0..5`. Rows `0..4` write modes Default/Shout/Whisper/Group/Clan. Row `5` constructs `ChattingColorPane` without a mode write.
- Known-row close shape: the repeated compact refresh and `MarkForDeletion` sequence is the compiler-inlined `OnDismiss(event)` body. The stand-alone callback ends `retn 4`, same-slot selector callbacks retain one ignored event pointer, and the target already has the exact `Event *event` object. Calling `OnDismiss(event)` is more plausible than six handwritten duplicate sequences and preserves exact behavior when inlined.
- Miss/default release: compact mode display refreshes unconditionally. The popup is marked for deletion only when the release is outside `m_visibleBounds`; a gap/inside miss stays open. This branch cannot be collapsed to `OnDismiss(event)`.
- `0x005a5110`: accepted file-level `OpenInputPaneForCurrentSayMode(UserPane *)`; exact modes, recent-recipient behavior, group/clan literals, and five caller routes are already documented by UID0002RX.
- `0x005a5710`: `OpenShoutInputPane(UserPane *)` is strongest because it stores mode 1 and creates `ShoutInputPane` with `L"! "`; only the target, current-mode dispatcher, and UserPane key route call it.
- `0x005a5450`: `OpenSayToUserNameInputPane()` is stronger than the polluted `___std_parallel_algorithms_hw_threads@0_10`; the sole target call follows mode 2, the body allocates `0x10c`, and invokes the documented recipient-name pane constructor.
- `0x005a5570`: `OpenSayToGroupMessageInputPane()` is exact from its sole target mode-3 call and literals `L"!!"` / `L"Group => "`.
- `0x005a5640`: `OpenSayToClanMessageInputPane()` is exact from its sole target mode-4 call and literals `L"!"` / `L"Clan => "`; the older Plan class filename is UID continuity, not a reason to emit "Plan" in source.
- Header placement: the target body consumes `SayInputMode` and five file-level helpers that belong to `SayInputPanes.h`. Their exact source declarations are resolved below; placing them in a Chatting child H block or relying on undeclared cross-translation-unit calls is rejected.
- `0x004824e0`: row 5 is `ChattingColorPane::ChattingColorPane()` from allocation size `0x2ac`, constructor body, class docs, and sole selector caller. This report protects the constructor rather than overlapping UID0002FX's own IDA ownership.
- Rejected alternatives: synthetic member methods on `ChattingVarietySelectPane`, STL hardware-thread logic, `Plan` for the clan branch, X/Y reversal, raw `sub_` calls, explicit pointer adjustment, and direct repeated close code are less consistent with current evidence.

## Evidence Standards Used

- Direct IDA facts: exact items, names, types, ranges, bytes/hashes, vtable data, xrefs, call graph, decompilation, comments, stack frames, literals, and canonical-disk/session identities.
- Documentation facts: current by-* metadata/formals, class layout, Event layout/enums, Pane bounds/deferred deletion, compact-pane refresh, global field aliases, helper constructor behavior, and file/emitter routes.
- Historical evidence: matching executed reports are retained as dated corroboration and superseded-assumption history, never as current authority when live/current documentation differs.
- Inference: source spellings and inlining/source-file shape are selected only after exact runtime roles are closed. Each chosen name has direct literal/caller/class-family support and no stronger contradictory candidate.
- Negative evidence: vtable-only target ownership, exact caller sets for the opener helpers, zero name collisions at the dated snapshot, unchanged fences, and rejected class/source placements constrain overreach.
- Gate standard: all fixed session, disk, generated, tracker, and manual hashes are dated snapshots. Primary-supervisor gates must dynamically reread current state and fail closed on drift.

## Evidence Checked

- Live MCP: `idb_list`, `server_health`, `lookup_funcs`, `analyze_function`, `get_comments`, `inspect_items`, `stack_frame`, `xrefs_to`, and `get_bytes` on target, dismiss, HitTest, all five row helpers, fences, and vtable slice.
- Capability audit established support for side-effect-bounded function-name changes and channel-specific comments. Section 21 is now declarative; all former endpoint and operating material resides only in the inert companion.
- Raw hashes: target, both fences, vtable slice, dismiss, and five helper bodies were recomputed from the current live IDB.
- Direct support: target/class/Event/Pane/compact pane/color pane/HitTest/OnDismiss/current-mode dispatcher/mixed helper/global alias docs and current manual/generated rows.
- Historical support: six matching executed reports listed in Supporting Research, including the earlier target-family and helper-specific investigations.
- Negative checks: sole target vtable xref; helper caller sets; constructor/source-family rejection; source-name collision query for all six then-proposed, now accepted names returned zero candidates at the dated session; no alternate event ABI or row mapping survived current evidence.
- Report-only research phase history: IDA mutation/save, by-* edits, manual/generated edits, validators, and lifecycle commands were intentionally skipped before initial Gate 1. The accepted B002 callback later changed only the six authorized ordinary by-* destinations and ran their six scoped validators. Primary-supervisor work subsequently applied/persisted the IDA package, manual rows, and coherent generated refresh as separately attributed below; lifecycle remains external.

## Claim And Incorporation Ledger

All 48 rows preserve binding actor ownership. The accepted ordinary callback physically completed C0002FJ-001 through -023 and -036 through -039. The primary supervisor subsequently completed C0002FJ-024 through -035, -040, -041, and -044. Fresh exact-artifact gates, lifecycle, post-lifecycle refresh, moved-artifact audit, and final report verification remain unchecked.

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination doc/section | Actor | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C0002FJ-001 | 0002FJ | Preserve exact target range, body hash, fences, vtable slice, and vtable-only ownership | Exact | Post-validator target SHA `1F607493...BFD`; exact body/fence/vtable hashes and sole slot `0x00614f84` are physically present | target / Range and IDA evidence | B002 callback | incorporate | applied |
| C0002FJ-002 | 0002FJ | Canonical source identity is `bool ChattingVarietySelectPane::HandlePointerOrMouseEvent(Event *event)` | Very strong | Post-validator target has one exact formal signature and canonical Status/Source Shape text | target / Status and Source shape | B002 callback | incorporate | applied |
| C0002FJ-003 | 0002FJ | Event ABI is type at +4, mouseY at +8, mouseX at +c, kinds 0/1/3 | Exact | Post-validator target Behavior/formal CPP physically records type/Y/X and all three event kinds | target / Behavior and ABI | B002 callback | incorporate | applied |
| C0002FJ-004 | 0002FJ | Adjusted receiver maps to `m_pressCaptured +f8`, `m_hotRow +f9`, and inherited `m_visibleBounds +44` | Very strong | Post-validator Source Shape and formal CPP contain all complete-object fields and reject raw authored adjustment | target / Source shape | B002 callback | incorporate | applied |
| C0002FJ-005 | 0002FJ | `g_pUserPane->m_inputLocked` causes immediate false return | Exact | Exact formal body and Behavior contain the guard/false return once | target / Behavior | B002 callback | incorporate | applied |
| C0002FJ-006 | 0002FJ | Cursor-move HitTest/store/invalidate/boolean-return behavior | Exact | Exact formal body and Behavior contain signed HitTest, store, invalidate, and boolean result | target / Behavior | B002 callback | incorporate | applied |
| C0002FJ-007 | 0002FJ | Left-down inside captures/invalidates/returns true; outside dismisses/returns false | Exact | Exact formal body and Behavior physically contain both paths | target / Behavior | B002 callback | incorporate | applied |
| C0002FJ-008 | 0002FJ | Left-up requires capture and clears capture/hot row to false/-1 before HitTest | Exact | Exact formal body and Source Shape physically contain the gate and paired clear | target / Behavior | B002 callback | incorporate | applied |
| C0002FJ-009 | 0002FJ | Row 0 writes Default and calls `OpenInputPaneForCurrentSayMode` | Very strong | Target formal CPP/Selector Rows and UID0002RX support readback agree | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-010 | 0002FJ | Row 1 writes Shout and calls `OpenShoutInputPane` | Very strong | Target formal CPP/Selector Rows and helper/file support readback agree | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-011 | 0002FJ | Row 2 writes Whisper and calls `OpenSayToUserNameInputPane`; STL alias is polluted | Very strong | Target/helper/file docs contain the accepted name plus sole-caller/allocation evidence and preserve the polluted alias historically | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-012 | 0002FJ | Row 3 writes Group and calls `OpenSayToGroupMessageInputPane` | Very strong | Target/helper/file docs contain the exact mode/call and group literal evidence | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-013 | 0002FJ | Row 4 writes Clan and calls `OpenSayToClanMessageInputPane` | Very strong | Target/helper/file docs contain the exact mode/call and Clan-vs-Plan literal resolution | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-014 | 0002FJ | Row 5 constructs `ChattingColorPane` with allocation size 0x2ac and no mode write | Exact | Target formal CPP, Selector Rows, and negative evidence physically preserve construction and absent mode store | target / Selector rows | B002 callback | incorporate | applied |
| C0002FJ-015 | 0002FJ | Release miss refreshes compact display and deletes only when outside bounds | Exact | Target formal CPP/Behavior/negative evidence physically preserve the conditional miss path | target / Behavior | B002 callback | incorporate | applied |
| C0002FJ-016 | 0002FJ | Known rows and outside press use source-level `OnDismiss(event)` whose one-argument body was inlined | Very strong | Target source-shape closure and UID0002FK exact `retn 4`/sibling evidence are present | target / Source shape | B002 callback | incorporate | applied |
| C0002FJ-017 | 0002FJ | Human helper names replace all `sub_`/polluted labels without changing behavior | Very strong | Target source/formal code uses only accepted human names; UID0001KO retains current IDA labels as historical evidence | target / Source names | B002 callback | incorporate | applied |
| C0002FJ-018 | 0002FJ | Insert the exact formal CPP body from this report | Very strong | One exact formal block in target SHA `1F607493...BFD`; H remains blank | target / RECONSTRUCTION_CPP | B002 callback | incorporate | applied |
| C0002FJ-019 | 0002FJ/0002FK | Keep both child H channels blank because UID00001Z owns both sole declarations | Exact | Both child H channels physically blank; class SHA `EA00A194...7B91` has each corrected declaration once and stale zero-argument form zero times | targets / RECONSTRUCTION_H rationale | B002 callback | already-present | already-present |
| C0002FJ-020 | 0002FJ/0002FK | Raise both targets to 94/95 and replace stale Item Summaries/score rationales | Strong | Both target headers/readbacks are `94/95` with exact summaries/rationales; routing metadata unchanged | targets / Metadata and summary | B002 callback | incorporate | applied |
| C0002FJ-021 | 00001Z/0002FK | Add both method closures to class/support notes, correct `OnDismiss(Event *event)`, and insert exact UID0002FK formal CPP | Very strong | Callback class SHA `EA00A194...7B91` and current class SHA `4F9F36A9...023` preserve both declarations/closure; dismiss SHA `58C747AF...E654` preserves the exact body; later class score `93/94` is unrelated accepted work | class and UID0002FK / declaration, behavior, formal CPP | B002 callback | incorporate | applied |
| C0002FJ-022 | 0001KO/0000N9/0002RX | Resolve 5a5110/5450/5570/5640/5710 opener roles, retain the mixed aggregate, and document the exact `SayInputPanes.h` enum/prototype route | Strong | Post-validator helper/file/dispatcher hashes prove five roles, unchanged `86/88` false/non-emitting aggregate, exact header block, and CPP-definition-only route | helper aggregate/file/dispatcher support | B002 callback | incorporate | applied |
| C0002FJ-023 | 0002FJ/0002FK | Preserve and historicalize reversed XY, OnMouseEvent, zero-argument OnDismiss, unresolved helper/header, and blank-C++ assumptions | Exact | All six destinations retain dated history and explicitly mark these interpretations superseded | target/support / Changes and history | B002 callback | historicalize | applied |
| C0002FJ-024 | 0002FJ | Protect the literal seven-row I001 frame plus the already-source-quality target name/type/comments/item/bytes/xrefs | Exact | Accepted primary Gate 2B full readback preserved every I001 row and protection through fresh-reopen S1 | IDA / 0x4811c0 | Primary supervisor | already-present | already-present |
| C0002FJ-025 | 0002FK | Apply and persist atomic I002-R/I002-T/I002-C operations at `0x481470` as `ChattingVarietySelectPane__OnDismiss(ChattingVarietySelectPane *this, Event *event)`, preserving `retn 4` while proving the pre-type one-row frame and deterministic post-type return-plus-event frame | Very strong | Accepted primary one-save transaction and fresh-reopen S1 proved exact name/type/comment/frame/body/xref poststate | IDA / 0x481470 | Primary supervisor | incorporate | applied |
| C0002FJ-026 | 0002RX | Rename/comment 0x5a5110 as `OpenInputPaneForCurrentSayMode` without type/frame change and protect every literal I003 API frame row plus the separately inferred `var_210`/`Buffer`/`Destination` storage spans | Very strong | Accepted primary Gate 2B persisted exact name/comment and unchanged API rows, storage spans, body, xrefs, and other comment channels | IDA / 0x5a5110 | Primary supervisor | incorporate | applied |
| C0002FJ-027 | 0002FJ | Rename/comment 0x5a5450 as `OpenSayToUserNameInputPane` without type/frame change and protect every literal I004 frame row | Very strong | Accepted primary Gate 2B persisted exact name/comment and unchanged I004 frame/body/xrefs/comments | IDA / 0x5a5450 | Primary supervisor | incorporate | applied |
| C0002FJ-028 | 0002FJ | Rename/comment 0x5a5570 as `OpenSayToGroupMessageInputPane` without type/frame change and protect every literal I005 API frame row plus the separately inferred `Buffer` storage span | Very strong | Accepted primary Gate 2B persisted exact name/comment and unchanged API rows/storage span/body/xrefs/literals | IDA / 0x5a5570 | Primary supervisor | incorporate | applied |
| C0002FJ-029 | 0002FJ | Rename/comment 0x5a5640 as `OpenSayToClanMessageInputPane` without type/frame change and protect every literal I006 API frame row plus the separately inferred `Buffer` storage span | Very strong | Accepted primary Gate 2B persisted exact name/comment and unchanged API rows/storage span/body/xrefs/literals | IDA / 0x5a5640 | Primary supervisor | incorporate | applied |
| C0002FJ-030 | 0002FJ | Rename/comment 0x5a5710 as `OpenShoutInputPane` without type/frame change and protect every literal I007 frame row | Very strong | Accepted primary Gate 2B persisted exact name/comment and unchanged I007 frame/body/xrefs/literal | IDA / 0x5a5710 | Primary supervisor | incorporate | applied |
| C0002FJ-031 | 0002FJ/0002FK | Protect the literal I001/I003-I007 API frame rows, the separately inferred I003/I005/I006 buffer storage spans, HitTest, PointInRect, MarkForDeletion, color constructor, one-argument dismiss ABI/siblings, UDTs, neighbors, all comment channels, and all bytes/xrefs | Exact | Accepted primary full readback and fresh-reopen persistence preserved the complete corrected protection inventory | IDA / protection rows | Primary supervisor | already-present | already-present |
| C0002FJ-032 | 0002FJ | Preserve the accepted thirteen-action IDA outcome and complete corrected protection inventory. | Exact | Persisted P0-to-S1 evidence and independent readback | IDA / Gate 2B result | Primary supervisor | incorporate | applied |
| C0002FJ-033 | 0002FJ/0002FK/0002RX | Apply exact by-memory manual coverage replacement rows for both declared targets and the dispatcher header-route support | Exact | Command `000000021325`, exit `0`, `ok:1`; current rows preserve exact accepted payloads | by-memory/-coverage-report.md | Primary supervisor | incorporate | applied |
| C0002FJ-034 | 00001Z | Apply exact by-class manual coverage replacement row | Exact | Command `000000021326`, exit `0`, `ok:1`; current UID00001Z row preserves exact accepted payload | by-class/-coverage-report.md | Primary supervisor | incorporate | applied |
| C0002FJ-035 | 0001KO/0000N9 | Apply exact helper-aggregate and SayInputPanes file coverage replacement rows | Exact | Commands `000000021325` and `000000021327`, exit `0`, `ok:1`; current rows preserve exact accepted payloads | by-memory/by-file manual coverage reports | Primary supervisor | incorporate | applied |
| C0002FJ-036 | 0002FJ | Run scoped target validator after authorized callback edits | Exact | Command `000000021233`, exit `0`, `ok: 1`, generated refresh deferred | target validator | B002 callback | incorporate | applied |
| C0002FJ-037 | 00001Z | Run scoped class validator after authorized callback edits | Exact | Command `000000021235`, exit `0`, `ok: 1`, generated refresh deferred | class validator | B002 callback | incorporate | applied |
| C0002FJ-038 | support | Run scoped validators for UID0002FK, UID0001KO, UID0000N9, and UID0002RX after authorized callback edits | Exact | Commands `000000021234/21238/21241/21243`, each exit `0`, `ok: 1`, generated refresh deferred | four support validators | B002 callback | incorporate | applied |
| C0002FJ-039 | 0002FJ | Physically read back every ordinary destination/hash and reconcile report states | Exact | Six post-validator hashes/field blocks physically reread; report reconciled to 27 checked/21 unchecked | report/destination readback | B002 callback | incorporate | applied |
| C0002FJ-040 | 0002FJ/0002FK | Refresh and verify both Chatting.cpp bodies once, corrected class declaration once, blank child H channels, and shared say helper declaration route | Exact | Command `000000021328` completed; accepted and later current readbacks preserve every required source/header semantic | generated source/header | Primary supervisor | incorporate | applied |
| C0002FJ-041 | 0002FJ/0002FK | Verify current ag-memory/tracker scores, owner/emitter, CPP/H, routes, and dynamic hashes for both declared targets | Exact | Command-21328 accepted snapshot plus latest read-only physical reread preserve coded owner/emitter `00001Z`, CPP yes/H no, and `94/95` true `0/0/0` rows | generated coverage/tracker | Primary supervisor | incorporate | applied |
| C0002FJ-042 | 0002FJ | Run fresh exact-artifact Gate 1 against all 33 sections and current report hash | Exact | Supervisor gate standard | audit | Primary supervisor | incorporate | proposed |
| C0002FJ-043 | 0002FJ | Run fresh Gate 2A against every checked ordinary claim after callback | Exact | Supervisor gate standard | audit | Primary supervisor | incorporate | proposed |
| C0002FJ-044 | 0002FJ | Run fresh Gate 2B against every IDA action/protection under dynamic current authority | Exact | Primary-supervisor Gate 2B passed C024-C032 with one-save P0-to-S1 persistence and complete protections | audit/IDA | Primary supervisor | incorporate | applied |
| C0002FJ-045 | 0002FJ | Keep lifecycle authority external to ordinary prose and derive it from current path/history | Exact | Current skill/lifecycle rules | report/lifecycle boundary | Primary supervisor | already-present | proposed |
| C0002FJ-046 | 0002FJ | After lifecycle action, perform coherent generated refresh and direct semantic readback | Exact | Supervisor lifecycle standard | generated/post-move | Primary supervisor | incorporate | proposed |
| C0002FJ-047 | 0002FJ | Audit the exact moved artifact and post-move current authority before count/closure | Exact | Supervisor audit standard | audit/archive | Primary supervisor | incorporate | proposed |
| C0002FJ-048 | 0002FJ | Preserve actor boundary, exact 33 headings, mirrored 48 claims, 42 checked/6 unchecked, one terminal marker, and zero active B002 leases | Exact | Physical report/readback reserved for post-edit supervisor verification | report | B002 report | incorporate | proposed |

## Positive Evidence Summary

- The target is a named, typed, vtable-owned 673-byte function with one exact data slot and no competing owner.
- Typed decompilation exposes `Event`, `g_pUserPane`, `PointInRect`, `ChattingVarietySelectPane::HitTest`, and `Pane_MarkForDeletion`; current by-* docs independently close every field and method contract used by source.
- The six selector outcomes are structurally explicit. Each of rows `0..4` has a unique mode write and opener call; row 5 has the unique `0x2ac` color-pane construction.
- Exact helper bodies and literals resolve the former raw names: recipient-name constructor for Whisper, `!!`/Group, `!`/Clan, `! ` and mode 1 for Shout, and a complete accepted dispatcher for Default/current mode.
- The current class formal already declares the primary target and `HitTest` plus both state fields. Its stale zero-argument `OnDismiss()` declaration is now explicitly corrected to `OnDismiss(Event *event)` from the physical ABI rather than silently inherited into formal source.
- The failed no-save Gate 2B retry supplies direct positive type-system evidence: exact I002-T materializes the already-logical event argument as a physical `event @ +0x8 size4 Event *` frame row while preserving the return row and `retn 4`. The deterministic frame-row addition strengthens, rather than changes, the accepted one-argument source ABI.
- The accepted later Gate 2B transaction independently confirms that the corrected name/type/comment package survives one save and fresh reopen while all protected target/helper frames, bodies, xrefs, UDTs, siblings, and non-target comment channels remain exact.
- The strongest inference chain is: vtable/class identity -> canonical Event ABI -> exact state/bounds offsets -> exact row writes/calls -> helper literal/class identities -> existing file/source vocabulary. No link depends solely on a guessed symbol.

## IDA MCP Facts

- Target: `ChattingVarietySelectPane__HandlePointerOrMouseEvent`, `bool __thiscall(ChattingVarietySelectPane *this, Event *event)`, `[0x4811c0,0x481461)`, size 673, body SHA256 `2B3B9A31FD6344668DE5BCBA88B6A343CD030E6EA39D434D5E67F658508F6E0A`.
- Target entry function regular comment is exactly `ChattingVarietySelectPane pointer-event dispatcher. Hit-tests hover and release points, commits Talk/Shout/Whisper/Group/Clan rows, and opens ChattingColorPane for row 5.` Function repeatable, address regular, and address repeatable entry comments are blank.
- Target frame snapshot: local `var_10 @ +0x10 size0x4 _DWORD`, local `var_C @ +0x14 size0x4 _DWORD`, local `var_4 @ +0x1c size0x4 _DWORD`, saved-register row `__saved_registers @ +0x20 size0x4 _DWORD`, return-address row `__return_address @ +0x24 size0x4 _UNKNOWN *`, unaffected first stack-argument row `Block @ +0x28 size0x4 void *` backing the already-typed logical `Event *event`, and protected analyzer/EH artifact `arg_4 @ +0x2c size0x4 _DWORD` rather than a second source argument. The current function type supplies source parameters; Gate 2B protects every physical row unchanged.
- Target internal regular comments: `0x481245 mouseX`, `0x481248 mouseY`, `0x48124b this`, `0x48127d this`, `0x4812b4 this`, `0x4812eb this`, `0x481322 this`, `0x481359 this`, `0x48139f this`, `0x4813bb bounds`, `0x4813bf x`, `0x4813c2 y`, `0x4813d1 this`, `0x4813dd bounds`, `0x4813de x`, `0x4813e1 y`, `0x4813fd this`, `0x481422 mouseX`, `0x48142b mouseY`, `0x48142e this`; cookie/unwind addresses retain their current comments.
- Dismiss pre-type state: `sub_481470`, current type `char __thiscall(char *this, int)`, `[0x481470,0x48148f)`, SHA256 `67C3DCC1402F8F3F8013C72EC829C576042615B7BE17DB84BBBA6B75560E0638`, only inbound vtable slot `0x614f88`, function/address entry regular/repeatable comments blank, internal address regular comment `0x48147e = this`, and physical `retn 4`. Before I002-T, IDA's complete `stack_frame` list exposes exactly one row, `__return_address @ +0x4 size4 _UNKNOWN *`, while the prototype and cleanup prove one logical 4-byte explicit argument at caller-stack `+0x8`. Historical retry1 proves the deterministic post-I002-T state is exactly two rows: the unchanged return row plus `event @ +0x8 size4 Event *`. The corrected source argument is `Event *event`, not removal or addition of a logical argument.
- HitTest: current exact name/type `signed char __thiscall ChattingVarietySelectPane__HitTest(ChattingVarietySelectPane *this, int mouseY, int mouseX)`, `[0x481ad0,0x481b5b)`, SHA256 `AFD9F146918CF5A289F5FD0D7724FD34C16B02E619F638196DDDCD53D1BE6E6B`; only target call sites `0x48124d` and `0x481430`.
- Mode-dispatcher P0 prestate: `sub_5A5110`, type `LineInputPane *__thiscall(_DWORD *this)`, size 551, SHA256 `C7C5B00C13BEBACFE92EEB09B64CA8D259F101162518C0BCDAC3680D8FA8F577`, target/sibling caller family, blank entry comments.
- Whisper-opener P0 prestate: polluted `___std_parallel_algorithms_hw_threads@0_10`, type `void *__cdecl()`, size 84, SHA256 `C54792EEE833166EFDF1F214BA82DAA9AB7ECB4F53A727E32FE28D87CE4BDD96`, sole target caller, blank entry comments.
- Group-opener P0 prestate: `sub_5A5570`, type `LineInputPane *()`, size 202, SHA256 `2447BA89E838C0F3141C710B4766BCDBF6F1E3BEB6D636C45837F47787489BC9`, sole target caller, group literals, blank entry comments.
- Clan-opener P0 prestate: `sub_5A5640`, type `LineInputPane *()`, size 202, SHA256 `1C924917235AAA8C7E5BEF682F34264A97D1C257EE46B1C9573D33371AFD4F6E`, sole target caller, clan literals, blank entry comments.
- Shout-opener P0 prestate: `sub_5A5710`, type `LineInputPane *__thiscall(_DWORD *this)`, size 129, SHA256 `A0B1F67870F040E42498A2553AB897EA3659CE2EF70BFA3C11F05953ACC2B800`, target/current-mode/UserPane caller family, blank entry comments.
- Accepted saved S1 state: the primary supervisor persisted `ChattingVarietySelectPane__OnDismiss`, `OpenInputPaneForCurrentSayMode`, `OpenSayToUserNameInputPane`, `OpenSayToGroupMessageInputPane`, `OpenSayToClanMessageInputPane`, and `OpenShoutInputPane`; the exact OnDismiss type and six function-regular comments below; and no permitted frame/body/xref/UDT/sibling/other-comment-channel delta. The old labels and blank comments in the preceding bullets are explicitly P0/history, not current saved state.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `[0x4811c0,0x481461)` | 0002FJ target | Pointer-event override | true | 00001Z | 86/91 -> 94/95 | Source-ready body |
| `[0x481470,0x48148f)` | 0002FK additional target | One-argument dismiss/refresh virtual | true | 00001Z | 86/91 -> 94/95 | Exact formal body, ABI, support, and persisted IDA closure |
| `[0x481ad0,0x481b5b)` | 000106 | Six-row signed HitTest | true | 00001Z | 92/94 | Already source-ready/protected |
| `[0x5a5110,0x5a5337)` | 0002RX | Current-mode opener | true | 0000N9 | 90/92 | Existing exact helper/formal; IDA name/comment persisted |
| `[0x5a5450,0x5a54a4)` | within 0001KO | Whisper recipient-name opener | mixed index support | 0001KO | aggregate 86/88 | Source name/comment persisted; no synthetic aggregate emission |
| `[0x5a5570,0x5a563a)` | within 0001KO | Group message opener | mixed index support | 0001KO | aggregate 86/88 | Source name/comment persisted |
| `[0x5a5640,0x5a570a)` | within 0001KO | Clan message opener | mixed index support | 0001KO | aggregate 86/88 | Source name/comment persisted |
| `[0x5a5710,0x5a5791)` | within 0001KO | Shout opener | mixed index support | 0001KO | aggregate 86/88 | Source name/comment persisted |
| `0x4b7e80` | supporting Pane geometry | `PointInRect` | true support | existing owner | existing | Protected |
| `0x544690` | supporting Pane core | `Pane_MarkForDeletion` | true support | existing owner | existing | Protected |
| `[0x4824e0,0x482b01)` | 0002FX | `ChattingColorPane` constructor | true | 00001T | existing | Source identity resolved; protected from overlapping mutation |

No split, child creation, merge, ignored subrange, or reclassification is required for UID0002FJ. The exact fences remain outside the target.

## Direct Xref / Caller Inventory

- Target `0x4811c0`: only inbound data xref `0x614f84`; no ordinary code caller is expected for a vtable-only override.
- OnDismiss `0x481470`: only inbound data xref `0x614f88`; target contains repeated inlined equivalents, not a surviving direct call. The exact `retn 4` and same-slot `0x555ae0`/`0x5bd3a0` siblings prove one ignored event argument despite the callback body never reading it.
- HitTest `0x481ad0`: exactly two target call sites, release `0x48124d` and hover `0x481430`.
- `0x5a5110`: target `0x48126b`, ChattingHandle `0x481ef6`, icons/action routes `0x4cf704` and raw case `0x4cf937`, and UserPane input route `0x5a6351`.
- `0x5a5450`: sole call from target `0x4812d9` after mode 2.
- `0x5a5570`: sole call from target `0x481310` after mode 3.
- `0x5a5640`: sole call from target `0x481347` after mode 4.
- `0x5a5710`: target `0x4812a2`, current-mode dispatcher `0x5a5170`, and UserPane key route `0x5a63a7`.
- `0x4824e0`: target row-5 call `0x481386` is the constructor's selector route.
- Caller sets support file-level opener helpers and reject methods owned by the selector class itself.

## Documentation Evidence And IDA Status

- [UID:00001Z] emits the primary method declaration, state fields, HitTest declaration, and source file route. It now contains corrected `OnDismiss(Event *event)` exactly once and no stale zero-argument declaration; both child H channels remain blank because the class owns both declarations.
- `Event.md` defines the pointer payload and event kinds; `Pane.md` defines inherited bounds, invalidation, and deferred deletion; `ChattingVarietyPane.md` defines compact refresh; globals identify `g_pUserPane`, mode/input-lock fields, and `g_pChattingVarietyPane`.
- UID0002RX supplies source-level `SayInputMode` values and the accepted central opener name. The four smaller opener identities follow its existing naming family. The enum and five cross-translation-unit prototypes are now physically present in its formal `SayInputPanes.h` block and absent from its CPP-local preamble; UID0000N9 documents the same route.
- UID0002FK's former blank formal and zero-argument declaration were target CPP blockers. The exact one-argument ABI, body, and human source form are now closed; its own formal CPP is populated by the accepted callback so the primary target's `OnDismiss(event)` calls have a real definition.
- IDA target state remained source-quality and protected. The primary supervisor persisted the six narrow helper/dismiss name/comment improvements and one dismiss type correction; the broad color constructor and exact dependencies remained unchanged.
- The historical retry1 no-save failure refines only I002-T frame expectations: pre-type physical state is return-row-only; post-type physical state is return plus typed event. It does not change the accepted name, source prototype, body, vtable ownership, or `retn 4`, and it produced no saved IDB delta.
- Dated generated/manual hashes are evidence snapshots only. Dynamic currentness is a supervisor-gate obligation and not encoded as permanent report truth.

## Ranked Ownership Analysis

### 1. ChattingVarietySelectPane class child in Chatting.cpp

- Evidence for: vtable slot, exact class-qualified type/name, complete-object fields, sibling methods, current owner/emitter, and source declaration.
- Evidence against: none.
- Decision: accepted with very-strong confidence; retain owner/emitter `00001Z` and file route [UID:0000I5].

### 2. EventHandler/Pane base implementation

- Evidence for: adjusted EventHandler facet explains decompiler receiver and canonical virtual surface.
- Evidence against: all state writes and six-row semantics are selector-specific; vtable is selector-specific.
- Decision: rejected as source owner; base classes only provide the virtual contract and inherited services.

### 3. SayInputPanes helper source

- Evidence for: row openers live in the SayInputPanes helper family.
- Evidence against: UID0002FJ itself owns event state, hit testing, close behavior, and color selection and sits in the selector class vtable.
- Decision: helpers remain external calls; they do not own or absorb the target method.

### Proposed new file/grouping, if applicable

- None. Existing `Chatting.cpp` placement is exact. Creating a synthetic dispatcher file or moving the method into `SayInputPanes.cpp` would split a class virtual from its class source and contradict current emitter routing.

## Source Placement

- Place the body in `Chatting.cpp` through [UID:00001Z] and keep the method declaration in the class block already emitted by [UID:00001Z].
- The helper declarations/definitions belong to their current `SayInputPanes` route. Exact declarations are placed in `SayInputPanes.h`; calls from Chatting code do not require embedding helper bodies or prototypes in UID0002FJ.
- Reject `Pane.cpp`, `Event.cpp`, `SayInputPanes.cpp` as the target body destination and reject a stand-alone `ChattingVarietySelectPaneOnMouseEvent.cpp` reverse-engineering artifact.
- No source-placement question remains open. Exact original file capitalization is governed by existing project routing, not this child.

## Range / Split / Padding / Reclassification Analysis

- Exact target `[0x4811c0,0x481461)` is one modeled function and needs no split.
- Four predecessor and fifteen successor `0xcc` bytes are alignment, not authored source or child ranges.
- The vtable data is separate class/compiler data and must not be absorbed into the method.
- `OnDismiss`, HitTest, row helpers, and color constructor remain separate existing functions/targets; this method calls or inlines their source behavior but does not merge their address ranges.
- [UID:0001KO] remains a non-reconstructable mixed index. Naming four contained helpers does not justify a synthetic aggregate body or reclassification.

## Negative Evidence Summary

- No direct code caller to the target exists; its sole vtable slot is positive virtual ownership evidence, not liveness failure.
- No alternate Event field order survives both HitTest and PointInRect call setup; historical X/Y reversal is rejected.
- No evidence supports an authored `this - 0xa0` adjustment, raw offset fields, or a second selector state object; these are decompiler consequences of the EventHandler facet.
- No STL hardware-thread behavior, imports, thread APIs, or relevant callers exist at `0x5a5450`; its current name is demonstrably polluted.
- No row helper has a caller pattern or literals supporting placement as a selector member. File-level `Open...InputPane` names fit the broader caller family.
- No evidence supports writing mode 5 for Color; the branch deliberately omits a mode store.
- No evidence supports closing the popup on an inside gap release; the exact default branch refreshes but deletes only outside bounds.
- No safe evidence supports changing the broad IDA helper return/member types at `0x5a5110/5450/5570/5640/5710`; caller-visible results are ignored. IDA therefore keeps those current types. Human source uses `void` file-level declarations because all known call sites consume only side effects and UID0002RX already uses the accepted `void` source form; this resolves source placement without pretending an incidental EAX residue is a contract.
- No target-specific header code is missing: the class page already emits the declaration. A second H block would duplicate source.
- No shared-header obligation remains implicit: the exact `SayInputMode`/opener declarations are assigned to `SayInputPanes.h`, and the enum must not remain defined only inside UID0002RX's CPP block.
- No evidence supports the former claim that I002-T leaves the physical frame return-row-only. The exact failed no-save readback contradicts that claim and permits only the deterministic `event @ +0x8 size4 Event *` row addition; no broader frame or ABI change is accepted.

## IDA Rename / Type / Comment Recommendations

The authoritative executable transaction material formerly in this section is preserved only in [0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md](0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md). That companion is non-authoritative and must never be executed.

The following rows are declarative recommendations and protection/readback contracts, not MCP requests or an execution sequence. Historical failed attempts receive no action credit. They established the corrected literal `wchar_t` API row size of `0x2` and left P0 SHA256 `29AA9F5B584D9C771DEB25D7C0B7B9A4B8641C0B19B8AAA860B0C66CD591028D`, 143,197,232 bytes, unchanged. The accepted supervisor result persisted all thirteen actions from that P0 to S1 SHA256 `2ACC15A3...1024`; its saved poststate and protection proof remain historical receipt evidence.

| ID | Address | Literal current prestate / prerequisite | Exact one-item action | Required immediate readback |
| --- | --- | --- | --- | --- |
| I001 | `0x4811c0` | Function `ChattingVarietySelectPane__HandlePointerOrMouseEvent`; type `bool __thiscall(ChattingVarietySelectPane *this, Event *event)`; function regular comment exact target dispatcher sentence; function repeatable/address regular/address repeatable blank; code item `[0x4811c0,0x481461)`; exact seven-row frame is listed below | No change; protect the exact current entity. | Same name/type/all four comment channels, every literal seven-row frame name/offset/width/type, body hash, vtable xref, bounded inbound/outbound xrefs, and internal comments |
| I002-R | `0x481470` | Gate-start name `sub_481470`; type `char __thiscall(char *this, int)`; four entry comment channels blank; internal address-regular `0x48147e = this`; code item `[0x481470,0x48148f)`; bytes end `c2 04 00`; complete pre-type `stack_frame` is exactly `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`; no destination-name collision | Proposed human outcome: rename function to `ChattingVarietySelectPane__OnDismiss`. | Exact new name and response without `error`, `dir`, or `dir_error`; old type; exact one-row pre-type frame; four entry channels, internal `this`, item, bytes, inbound xref `0x614f88`, and bounded outbound xrefs unchanged |
| I002-T | `0x481470` | I002-R name present; old type still `char __thiscall(char *this, int)`; physical tail bytes `c2 04 00`; complete pre-type `stack_frame` is exactly one row, `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`; logical current `arg1` is one 4-byte stack argument | Proposed human outcome: assign the exact function type `bool __thiscall ChattingVarietySelectPane__OnDismiss(ChattingVarietySelectPane *this, Event *event)`. | Inspection-only `lookup_funcs` exact-address/name readback returns the exact new function type with one `Event *event`; `stack_frame` returns exactly two rows: unchanged `__return_address` offset `0x4`, size `0x4`, type `_UNKNOWN *`, plus permitted `event` offset `0x8`, size `0x4`, type `Event *`; `get_bytes` retains exact body hash/tail `c2 04 00`; item, xrefs, and all comment channels remain exact; no decompile or other analysis-producing readback is permitted |
| I002-C | `0x481470` | I002-R/I002-T name/type present; complete post-type frame is exactly the I002-T return-plus-event two-row state; function-regular blank; function-repeatable/address-regular/address-repeatable blank; internal address-regular `0x48147e = this` | Proposed human outcome: set the function-regular comment exactly `ChattingVarietySelectPane dismissal handler. Ignores the Event argument, refreshes the compact variety pane, marks this popup for deferred deletion, and returns false.` | Exact function-regular text; exact two-row post-type frame unchanged; other three entry channels blank; internal `0x48147e = this`; name/type/body/xrefs unchanged |
| I003-R | `0x5a5110` | Function `sub_5A5110`; type `LineInputPane *__thiscall(_DWORD *this)`; four entry channels blank; body `[0x5a5110,0x5a5337)`; no destination collision | Proposed human outcome: rename function to `OpenInputPaneForCurrentSayMode`. | Exact new name and response without `error`, `dir`, or `dir_error`; current type, all comments, complete literal API frame rows and separately inferred storage spans, body, and bounded inbound/outbound xrefs unchanged |
| I003-C | `0x5a5110` | I003-R name present; type `LineInputPane *__thiscall(_DWORD *this)`; function-regular blank; other three entry channels blank | Proposed human outcome: set the function-regular comment exactly `Dispatches UserPane::m_currentSayMode to default, shout, whisper, group, or clan input-pane creation.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage spans/body/bounded inbound-outbound xrefs/internal comments unchanged |
| I004-R | `0x5a5450` | Function `___std_parallel_algorithms_hw_threads@0_10`; type `void *__cdecl()`; four entry channels blank; body `[0x5a5450,0x5a54a4)`; no destination collision | Proposed human outcome: rename function to `OpenSayToUserNameInputPane`. | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal frame/body/sole inbound xref unchanged |
| I004-C | `0x5a5450` | I004-R name present; type `void *__cdecl()`; function-regular blank; other three entry channels blank | Proposed human outcome: set the function-regular comment exactly `Opens the recipient-name input pane used by the Whisper selector row.` | Exact new function-regular; name/type/other channels/complete literal frame/body/sole inbound xref/internal comments unchanged |
| I005-R | `0x5a5570` | Function `sub_5A5570`; type `LineInputPane *()`; four entry channels blank; body `[0x5a5570,0x5a563a)`; no destination collision | Proposed human outcome: rename function to `OpenSayToGroupMessageInputPane`. | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals unchanged |
| I005-C | `0x5a5570` | I005-R name present; type `LineInputPane *()`; function-regular blank; other three entry channels blank | Proposed human outcome: set the function-regular comment exactly `Opens the group-message input pane with the "!!" command prefix and "Group => " prompt.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals/internal comments unchanged |
| I006-R | `0x5a5640` | Function `sub_5A5640`; type `LineInputPane *()`; four entry channels blank; body `[0x5a5640,0x5a570a)`; no destination collision | Proposed human outcome: rename function to `OpenSayToClanMessageInputPane`. | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals unchanged |
| I006-C | `0x5a5640` | I006-R name present; type `LineInputPane *()`; function-regular blank; other three entry channels blank | Proposed human outcome: set the function-regular comment exactly `Opens the clan-message input pane with the "!" command prefix and "Clan => " prompt.` | Exact new function-regular; name/type/other channels/complete literal API frame rows/separately inferred storage span/body/sole inbound xref/literals/internal comments unchanged |
| I007-R | `0x5a5710` | Function `sub_5A5710`; type `LineInputPane *__thiscall(_DWORD *this)`; four entry channels blank; body `[0x5a5710,0x5a5791)`; no destination collision | Proposed human outcome: rename function to `OpenShoutInputPane`. | Exact new name and response without `error`, `dir`, or `dir_error`; current type/comments/complete literal frame/body/three inbound xrefs/literal unchanged |
| I007-C | `0x5a5710` | I007-R name present; type `LineInputPane *__thiscall(_DWORD *this)`; function-regular blank; other three entry channels blank | Proposed human outcome: set the function-regular comment exactly `Sets shout mode and opens a ShoutInputPane initialized with the "! " prompt.` | Exact new function-regular; name/type/other channels/complete literal frame/body/three inbound xrefs/literal/internal comments unchanged |

Complete literal physical-frame protections and row roles:

For `stack_frame`, `size` is the literal API-reported size of the row's declared scalar type, not the allocation extent inferred for an array-like local. Every affected `wchar_t` row must read back with exact API `size 0x2` and type `wchar_t`. Its storage span is protected separately from that row's offset through the next frame-row offset. An inferred storage span must never be compared to or substituted for the API row size.

- I001 target frame, protected with no permitted row delta: local `var_10`, offset `0x10`, width `0x4`, type `_DWORD`; local `var_C`, offset `0x14`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x1c`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x20`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x24`, width `0x4`, type `_UNKNOWN *`; unaffected first stack-argument row `Block`, offset `0x28`, width `0x4`, type `void *`, backing the already-typed logical `Event *event` without authorizing a frame edit; protected analyzer/EH artifact `arg_4`, offset `0x2c`, width `0x4`, type `_DWORD`, not a second source argument.
- I002 pre-type frame has no local or saved-register row and exactly one protected return-address row, `__return_address`, offset `0x4`, width `0x4`, type `_UNKNOWN *`; the old prototype's logical four-byte `int arg1` is not materialized as a physical row. The sole permitted frame delta from I002-T is addition of argument row `event`, offset `0x8`, width `0x4`, type `Event *`; the return row is protected unchanged and no local, saved-register, second argument, or third frame row may appear.
- I003 frame, protected with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `var_210`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local buffer `Buffer`, offset `0x10c`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0x10c,0x18c)` / `0x80` bytes; local buffer `Destination`, offset `0x18c`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0x18c,0x20c)` / `0x80` bytes; local `var_10`, offset `0x20c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x210`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x218`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x21c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x220`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x228`, API size `0x4`, type `_DWORD`, not a source argument. The implicit `this` remains register-carried in ECX and has no frame row.
- I004 frame, protected with no permitted row delta: local `Block`, offset `0x4`, width `0x4`, type `void *`; local `var_C`, offset `0x8`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x10`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x14`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x18`, width `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x20`, width `0x4`, type `_DWORD`, not a source argument. The preserved cdecl prototype has no source argument.
- I005 frame, protected with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `Buffer`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local `var_10`, offset `0x10c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x110`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x118`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x11c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x120`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x128`, API size `0x4`, type `_DWORD`, not a source argument. The preserved prototype has no source argument.
- I006 frame, protected independently with no permitted API-row or storage-span delta: local `Block`, offset `0x8`, API size `0x4`, type `void *`; local buffer `Buffer`, offset `0xc`, literal MCP API size `0x2`, type `wchar_t`, separately inferred storage span `[0xc,0x10c)` / `0x100` bytes; local `var_10`, offset `0x10c`, API size `0x4`, type `_DWORD`; local `var_C`, offset `0x110`, API size `0x4`, type `_DWORD`; local `var_4`, offset `0x118`, API size `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x11c`, API size `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x120`, API size `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x128`, API size `0x4`, type `_DWORD`, not a source argument. The preserved prototype has no source argument.
- I007 frame, protected with no permitted row delta: local `Block`, offset `0x8`, width `0x4`, type `void *`; local `var_C`, offset `0xc`, width `0x4`, type `_DWORD`; local `var_4`, offset `0x14`, width `0x4`, type `_DWORD`; saved-register row `__saved_registers`, offset `0x18`, width `0x4`, type `_DWORD`; return-address row `__return_address`, offset `0x1c`, width `0x4`, type `_UNKNOWN *`; protected analyzer/EH artifact `arg_4`, offset `0x24`, width `0x4`, type `_DWORD`, not a source argument. The implicit `this` remains register-carried in ECX and has no frame row.

No I001/I003-I007 API frame row may be renamed, retyped, added, removed, resized, or moved, and none of the separately inferred I003/I005/I006 storage spans may change. I002's `event +0x8/0x4 Event *` materialization is the only permitted physical-frame delta in the complete transaction.

Complete internal regular-comment protections from the latest bounded readback; every corresponding repeatable channel is also protected unchanged:

- I002: `0x48147e this`.
- I003: `0x5a519e Source`, `0x5a51a5 SizeInWords`, `0x5a51a7 Destination`, `0x5a51e6 Source`, `0x5a5220 promptText`, `0x5a5222 this`, `0x5a5238 SizeInWords`, `0x5a523d ArgList`, `0x5a5268 BufferCount`, `0x5a526d Buffer`, `0x5a52b2 promptText`, `0x5a52b4 this`, `0x5a52ca SizeInWords`, `0x5a52cf ArgList`, `0x5a52fa BufferCount`, `0x5a52ff Buffer`, `0x5a532c StackCookie`, `0x60a576 block`, `0x60a584 block`, `0x60a58c this`, `0x60a59d block`, `0x60a5a5 this`, `0x60a5bd StackCookie`, `0x60a5c7 StackCookie`.
- I004: `0x60a601 block`, `0x60a613 StackCookie`.
- I005: `0x5a55bc promptText`, `0x5a55be this`, `0x5a55d4 SizeInWords`, `0x5a55d9 ArgList`, `0x5a5604 BufferCount`, `0x5a5609 Buffer`, `0x5a562f StackCookie`, `0x60a660 block`, `0x60a668 this`, `0x60a680 StackCookie`, `0x60a68a StackCookie`.
- I006: `0x5a568c promptText`, `0x5a568e this`, `0x5a56a4 SizeInWords`, `0x5a56a9 ArgList`, `0x5a56d4 BufferCount`, `0x5a56d9 Buffer`, `0x5a56ff StackCookie`, `0x60a6a1 block`, `0x60a6a9 this`, `0x60a6c1 StackCookie`, `0x60a6cb StackCookie`.
- I007: `0x5a5760 this`, `0x60a6df block`, `0x60a6f1 StackCookie`.

## First-Draft C++ Recommendation

- Eligible: yes for both declared targets. Runtime behavior, source owner, signatures, fields, Event ABI, helper roles, and close behavior are closed.
- Primary CPP block disposition: replace UID0002FJ's blank formal CPP with exactly:

```cpp
bool ChattingVarietySelectPane::HandlePointerOrMouseEvent(Event *event)
{
    if (g_pUserPane->m_inputLocked)
        return false;

    const int mouseY = event->m_payload.m_pointer.m_y;
    const int mouseX = event->m_payload.m_pointer.m_x;

    switch (event->m_type)
    {
    case kEventCursorMove:
        m_hotRow = HitTest(mouseY, mouseX);
        InvalidateRect(&m_visibleBounds);
        return m_hotRow != -1;

    case kEventLeftButtonDown:
        if (PointInRect(mouseY, mouseX, &m_visibleBounds))
        {
            m_pressCaptured = true;
            InvalidateRect(&m_visibleBounds);
            return true;
        }
        OnDismiss(event);
        return false;

    case kEventLeftButtonUp:
        if (!m_pressCaptured)
            return false;

        m_pressCaptured = false;
        m_hotRow = -1;

        switch (HitTest(mouseY, mouseX))
        {
        case 0:
            g_pUserPane->m_currentSayMode = kSayInputModeDefault;
            OpenInputPaneForCurrentSayMode(g_pUserPane);
            break;
        case 1:
            g_pUserPane->m_currentSayMode = kSayInputModeShout;
            OpenShoutInputPane(g_pUserPane);
            break;
        case 2:
            g_pUserPane->m_currentSayMode = kSayInputModeWhisper;
            OpenSayToUserNameInputPane();
            break;
        case 3:
            g_pUserPane->m_currentSayMode = kSayInputModeGroup;
            OpenSayToGroupMessageInputPane();
            break;
        case 4:
            g_pUserPane->m_currentSayMode = kSayInputModeClan;
            OpenSayToClanMessageInputPane();
            break;
        case 5:
            new ChattingColorPane();
            break;
        default:
            g_pChattingVarietyPane->Refresh();
            if (!PointInRect(mouseY, mouseX, &m_visibleBounds))
                MarkForDeletion();
            return false;
        }

        OnDismiss(event);
        return true;

    default:
        return false;
    }
}
```

- Additional UID0002FK CPP block disposition: replace its blank formal CPP with exactly:

```cpp
bool ChattingVarietySelectPane::OnDismiss(Event *)
{
    g_pChattingVarietyPane->Refresh();
    MarkForDeletion();
    return false;
}
```

- Chatting child H disposition: both UID0002FJ and UID0002FK remain exactly blank. [UID:00001Z] emits `virtual bool HandlePointerOrMouseEvent(Event *event);` and the corrected `virtual bool OnDismiss(Event *event);` exactly once inside `ChattingVarietySelectPane`.
- Shared support H disposition: `SayInputMode` must move from UID0002RX's CPP-only block into the `SayInputPanes.h` route, together with the cross-translation-unit declarations consumed by Chatting. The exact support H text is:

```cpp
class UserPane;

enum SayInputMode
{
    kSayInputModeDefault = 0,
    kSayInputModeShout = 1,
    kSayInputModeWhisper = 2,
    kSayInputModeGroup = 3,
    kSayInputModeClan = 4,
};

void OpenInputPaneForCurrentSayMode(UserPane *userPane);
void OpenShoutInputPane(UserPane *userPane);
void OpenSayToUserNameInputPane();
void OpenSayToGroupMessageInputPane();
void OpenSayToClanMessageInputPane();
```

  UID0002RX's formal CPP keeps only the function definition after that enum move. The five declarations are support-header ownership evidence for the exact helper bodies; they are not emitted by either Chatting child and do not authorize a synthetic UID0001KO CPP body.
- Exact behavior: direct returns preserve the input lock, unhandled kinds, uncaptured release, inside/outside press, hover result, known-row success, and miss semantics. `OnDismiss(event)` is an inlining-compatible source call for the exact repeated refresh/delete body; the separate definition preserves the ignored stack argument proven by `retn 4`.
- Human source shape: the code uses the current class hierarchy, fields, Event payload, enum names, helper family, and normal early-return/switch style plausible for this codebase. It contains no addresses, raw offsets, decompiler temporaries, cookie/EH mechanics, or `sub_` names.
- Third-party import: not applicable; this is project class code, not an embedded third-party source range.

## Final Recommendation

- The ordinary callback implemented both declared target bodies and `94/95` metadata while preserving owner/emitter/reconstructability/position.
- The class method evidence, corrected one-argument dismiss declaration, mixed helper map, and shared SayInputPanes header route are synchronized without broad refactoring or support-score inflation.
- Exact historical evidence remains present and explicitly marks old blank-C++/helper-name blockers and `OnMouseEvent`/reversed-X-Y labels as superseded rather than deleting chronology.
- The primary supervisor completed and persisted the narrow IDA package with all protected target/dependency state intact. The literal package remains the reproducible audit contract and now uses `rename` with `pure:true`, channel-specific comments, `run_auto_analysis:false` for every open, and process/file-lock closure proof. Do not change the target itself, exact HitTest, geometry/deletion helpers, color constructor, or helper types that are not deterministic.
- Do not create new by-memory children for the helper bodies in this callback. Their eventual exact child split is a broader UID0001KO container decision; this report resolves the names needed by UID0002FJ and records the aggregate map without synthesizing source.
- Lifecycle handling remains supervisor-owned and externally authoritative.

## Recommended Target Doc Changes

- Applied header: `COMPLETION:94`, `CONFIDENCE:95`; `CANONICAL_OWNER:00001Z`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00001Z`, and blank position remain unchanged.
- Applied formal placement: exact CPP occurs once; formal H is blank with the class-owner proof.
- Item Summary replacement: `Source-ready ChattingVarietySelectPane pointer-event override with exact Event Y/X ABI, adjusted EventHandler receiver mapping, input-lock guard, hover/capture/release state, signed six-row HitTest, Talk/Shout/Whisper/Group/Clan/Color dispatch, compact refresh, conditional miss dismissal, and class-owned header declaration.`
- Status/Behavior: use canonical `HandlePointerOrMouseEvent`; retain historical filename/title note; state exact Event kinds and complete-object fields/bounds.
- Selector Rows: replace every raw helper name with the accepted source-facing name and retain exact mode writes/literals/color allocation.
- Source Shape: replace the blank-code blocker with exact formal body rationale and inlined `OnDismiss(event)` evidence.
- IDA evidence/score: add current body/fence/vtable hashes, live current name/type/comment/frame facts, helper closure, and `94/95` rationale.
- Changes: append this report's closure; retain all earlier dated work as history.

## Recommended Support Doc Changes

- Applied `by-class/ChattingVarietySelectPane.md`: callback retained `92/94` and owner/emitter, corrected `OnDismiss(Event *event)`, and added full source-ready child evidence. Later unrelated accepted work advanced current class score to `93/94` without changing this closure.
- Applied UID0002FK: promoted to `94/95`, preserved routing metadata, inserted exact formal CPP, kept H blank, and closed the one-argument ABI/refresh/deletion/false-return evidence.
- Applied UID0001KO: retained `86/88`, false/non-emitting/no-emitter/blank formals while adding the five exact roles, callers/literals, header route, and no-synthetic-body rationale.
- Applied UID0000N9/UID0002RX: retained `89/90` and `90/92`, documented the exact header route, moved the enum from UID0002RX CPP into formal H with all five declarations, and retained only the function definition in CPP.
- Verify-only, no ordinary edit unless callback-time semantic drift is proven: Event, Pane, ChattingVarietyPane, ChattingColorPane, globals, and UID000106. Their current facts already support the declared targets.
- Do not edit any manual coverage file as B002. Exact supervisor payloads follow separately.

## Score And Metadata Recommendation

| Field | Current | Recommended | Rationale |
| --- | ---: | ---: | --- |
| Completion | 94 | 94 | Exact formal body, method/field/Event/helper names, branch semantics, source route, hashes, history, and support integration are physically applied. |
| Confidence | 95 | 95 | Binary behavior/ABI/ownership are exact; source spellings are strongly inferred from current project vocabulary, literals, and sibling bodies. |
| Canonical owner | 00001Z | 00001Z | Vtable/class ownership is exact. |
| Reconstructable | true | true | Ordinary source-authored virtual. |
| Emitter | 00001Z | 00001Z | Class child body route is correct. |
| Position | blank | blank | Existing address-sorted child ordering is sufficient. |
| Formal CPP | exact body above | exact body above | One physical block is present and scoped-validated. |
| Formal H | blank | blank | Sole declaration already emitted by class owner. |

- Additional UID0002FK: `94/95` is physically applied, owner/emitter/reconstructability/position remain unchanged, exact CPP body is present once, and H is blank because the corrected class-owned declaration is unique.
- Completion remains below 95 because the exact original lexical spellings of the five file-level opener helpers are inferred rather than symbol-recovered and the mixed aggregate has not been split into exact child pages. This is a final-audit confidence cap, not a source-code blocker or a request for further target research.
- Confidence reaches 95 because all runtime, ABI, layout, owner/emitter, and branch facts are direct and the chosen names are high-probability human source names with literal/class/caller support.

## Open Questions With Attempted Resolution

- `OnMouseEvent` versus `HandlePointerOrMouseEvent`: resolved to `HandlePointerOrMouseEvent` from current class surface, exact live name/type, and project-wide EventHandler vocabulary. The filename remains historical only.
- Event point order and kinds: resolved to Y at +8, X at +c; cursor move 0, left down 1, left up 3 from current Event docs and exact calls.
- Adjusted receiver/fields: resolved to EventHandler-facet adjustment with complete-object `m_pressCaptured`, `m_hotRow`, and inherited `m_visibleBounds`; no raw-offset source survives.
- Helper names: resolved to the six selected `Open...` names. Exact original capitalization/spelling cannot be symbol-proven, but each selected name has a substantially higher probability than alternatives and is safe for formal source.
- `OnDismiss` argument: resolved to one ignored `Event *` argument. `0x481470`, `0x555ae0`, and `0x5bd3a0` all end `retn 4`; the current target/class Event vocabulary outranks the older zero-argument declaration and legacy `PaneEvent` spelling.
- `0x5a5640` Plan versus Clan: resolved to Clan from mode 4 plus `L"Clan => "`; Plan remains a legacy UID/class filename only.
- Helper return types/member status: current evidence does not deterministically support IDA type changes, so IDA preserves current types while target source uses side-effect calls. This is an evidence-backed narrow no-change, not a target blocker.
- H channels: both Chatting child H blocks are resolved blank because declarations are physically owned by UID00001Z; shared mode/helper declarations are resolved to `SayInputPanes.h` with exact formal text above.
- No unanswered target behavior, owner, emitter, source placement, formal CPP/H, range, split, or liveness question remains.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

Historical proposed payload now applied by primary-supervisor command `000000021325` (exit `0`, `ok:1`). Current read-only `by-memory/-coverage-report.md` is SHA256 `2655C2F290F95E2F52FF8CC9DF298836A9EDD0F5585CC4443B1FD48B6D3234F9`, 2,180,613 bytes/4,895 lines; UID0002FJ line 827 exactly equals:

```text
        - [UID:0002FJ][0x004811c0-0x00481461.ChattingVarietySelectPaneOnMouseEvent](by-memory/0x004811c0-0x00481461.ChattingVarietySelectPaneOnMouseEvent.md) 0x004811c0-0x00481461 | class method | ChattingVarietySelectPane::HandlePointerOrMouseEvent : reconstructable : 94% : very-strong : Source-ready vtable-owned selector pointer dispatcher with exact Event mouseY/mouseX ABI, adjusted EventHandler receiver mapping, input-lock guard, hover/capture/release state, signed six-row HitTest, Talk/Shout/Whisper/Group/Clan/Color dispatch, compact refresh, conditional miss dismissal, exact formal CPP, and class-owned header declaration.
```

In the same current manual file, UID0002FK line 829 exactly equals:

```text
        - [UID:0002FK][0x00481470-0x0048148f.ChattingVarietySelectPaneOnDismiss](by-memory/0x00481470-0x0048148f.ChattingVarietySelectPaneOnDismiss.md) 0x00481470-0x0048148f | class method | ChattingVarietySelectPane::OnDismiss : reconstructable : 94% : very-strong : Source-ready one-argument Event dismissal callback with exact retn-4 ABI, vtable-only ownership, ignored event parameter, compact ChattingVarietyPane refresh, Pane deferred deletion, false return, exact formal CPP, and class-owned header declaration.
```

In the same current manual file, UID0001KO line 3943 exactly equals:

```text
    - [UID:0001KO][0x005a4b60-0x005a5791.SayModeHelpers](by-memory/0x005a4b60-0x005a5791.SayModeHelpers.md) 0x005a4b60-0x005a5791 | non-emitting mixed helper index | SayModeHelpers : not_reconstructable : 86% : strong : Mixed say-mode and adjacent UI action helper neighborhood with nineteen modeled functions, exact source-ready UID0002RX current-mode dispatcher, resolved source-facing Default/Shout/Whisper/Group/Clan opener roles at 0x005a5110/0x005a5710/0x005a5450/0x005a5570/0x005a5640, exact UserPane-owned UID0004US EnsureNormalBulletinSession child, characterized excluded user-list opener, boundary padding, caller/source-family distinctions, and no synthetic aggregate source body.
```

In the same current manual file, UID0002RX line 3946 exactly equals:

```text
        - [UID:0002RX][0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode](by-memory/0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode.md) 0x005a5110-0x005a5337 | source-ready say-input dispatcher | OpenInputPaneForCurrentSayMode : reconstructable : 90% : very-strong : Exact current say-mode input dispatcher owned by SayInputPanes.cpp with unchanged source body, mode/field/helper evidence, and score/owner/emitter; shared SayInputMode and cross-translation-unit opener declarations are routed through the formal SayInputPanes.h support block rather than left CPP-local.
```

Historical proposed payload now applied by primary-supervisor command `000000021326` (exit `0`, `ok:1`). Current read-only `by-class/-coverage-report.md` is SHA256 `45614081E00144793934AACFA92E4CFD2C5AE363C6383F0ED2D048CC04D15205`, 275,617 bytes/626 lines; UID00001Z line 97 exactly equals:

```text
- [UID:00001Z][ChattingVarietySelectPane](by-class/ChattingVarietySelectPane.md) : reconstructable : 92% : very-strong : Popup selector constructor/destructor, vtable methods, row helpers, and state layout are documented; current source closure resolves the class as Pane plus Singleton<ChattingVarietySelectPane> with EBO, m_pressCaptured +0xf8, m_hotRow +0xf9, implicit tail padding, canonical EventHandler surface, external singleton declaration, compiler-only publication/clear/vtable/scalar handling, source-ready HandlePointerOrMouseEvent behavior, and the corrected one-argument OnDismiss(Event *event) callback with compact refresh/deferred deletion/false-return semantics.
```

Historical proposed payload now applied by primary-supervisor command `000000021327` (exit `0`, `ok:1`). Current read-only `by-file/-coverage-report.md` is SHA256 `4D4C1FABC5312CBF0D1A927415A7E814FB5A302BE1CEB80F85C83CC8CC273FA4`, 167,045 bytes/319 lines; UID0000N9 line 234 exactly equals:

```text
- [UID:0000N9][SayInputPanes](by-file/SayInputPanes.md) : reconstructable : 89% : very-strong : Social chat input source root at `NexusTK/social/SayInputPanes.cpp` with declarations in `SayInputPanes.h`, exact source-ready SayInputPane constructor/Event-history/submit child routes, shared UserPane history and typed TextEditPane dependencies, current helper/vtable/current-mode dispatcher evidence, exact shared SayInputMode plus Default/Shout/Whisper/Group/Clan opener declaration route, and retained broader group/clan/chat source-split caveats.
```

Placement outcome: the primary supervisor applied exact in-place replacements under the existing UID rows with no report-authored manual-file edit. Later unrelated row movement/content growth did not alter these six accepted payloads.

## Follow-Up Actions

- B002 ordinary callback is complete for C0002FJ-001 through -023 and -036 through -039: six destinations were edited under short leases, six scoped validators returned `exit 0`/`ok 1`, every post-validator destination was physically reread and hashed, and this same report was reconciled.
- Primary supervisor completed IDA Gate 2B, manual commands `21325`-`21327`, and coherent autogen/readback command `21328`. Because this additive report reconciliation changes the exact artifact, fresh exact-artifact Gate 1 and independent Gate 2A over all 42 checked claims remain required before supervisor-owned lifecycle handling.
- The historical retry1 and `GATE2B_FAILED_PRE_SAVE` attempts retain zero credit and unchanged-P0 evidence. They are superseded operationally by the accepted later one-save Gate 2B transaction from P0 `29AA9F5B...1028D` to S1 `2ACC15A3...1024`, but remain important negative evidence for the corrected frame-size/span and transaction-safety rules.
- Future B-agent work: none is required to make UID0002FJ/UID0002FK source-ready after the accepted callback. A separately assigned UID0001KO split pass may create exact helper children later, but exact source names and the shared header route are resolved here rather than deferred.

## Confidence

- Recommendation confidence: very strong (`95`) for target source reconstruction and metadata.
- Score confidence: strong. `94/95` accurately reflects complete behavior/source readiness while leaving a narrow lexical/helper-child audit margin.
- IDA action confidence: very strong for thirteen atomic mutations: six names, six function-regular comments, and the one-argument dismiss signature; exact safe no-type-change for the other five helpers.
- Remaining uncertainty: exact historical spelling/access scope of file-level opener helpers. It does not change execution, source placement, formal target body, or owner/emitter.

## Validator Results

- B002 ran exactly the six callback-authorized scoped file validators from `source-3/project-documentation`; no generated refresh was requested and every command reported `generated_refresh: deferred`:
  - command `000000021233`: `by-memory/0x004811c0-0x00481461.ChattingVarietySelectPaneOnMouseEvent.md`, exit `0`, `ok: 1`.
  - command `000000021235`: `by-class/ChattingVarietySelectPane.md`, exit `0`, `ok: 1`.
  - command `000000021234`: `by-memory/0x00481470-0x0048148f.ChattingVarietySelectPaneOnDismiss.md`, exit `0`, `ok: 1`.
  - command `000000021238`: `by-memory/0x005a4b60-0x005a5791.SayModeHelpers.md`, exit `0`, `ok: 1`.
  - command `000000021241`: `by-file/SayInputPanes.md`, exit `0`, `ok: 1`; it retained pre-existing missing-reference warnings while still returning `ok: 1`.
  - command `000000021243`: `by-memory/0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode.md`, exit `0`, `ok: 1`.
- Primary-supervisor manual commands `000000021325`-`21327` each returned exit `0`/`ok:1`; coherent autogen command `000000021328` completed. Lifecycle remains supervisor-owned after fresh gates on this reconciled artifact.

## Changed Files

- B002 callback-authored ordinary destinations, with prestate -> post-validator SHA256:
  - `by-memory/0x004811c0-0x00481461.ChattingVarietySelectPaneOnMouseEvent.md`: `318D586FE455F06BA3247841F91BD8F6B7FE7E583D06E2A85B7C74C6B82955F0 -> 1F6074938C58F5999CC779B845901CA6255CB3E8666B48356AC7AB589BE41BFD`.
  - `by-memory/0x00481470-0x0048148f.ChattingVarietySelectPaneOnDismiss.md`: `11384712A6F4F27400FE7725B77266CDF0A68C8503A6B7161832D6B28BB89839 -> 58C747AF507A65A91520D061785E0A17916B955733EA106CAC9FAA2BD0F8E654`.
  - `by-class/ChattingVarietySelectPane.md`: `495FCAC8E58E1F43E52040469697D56E1333657AD6119AA0BA4E58842706EBA4 -> EA00A1945974F171BAC12E684A4E5C2FF262BEB9AC59700E0DB7B4FC1ADA7B91`.
  - `by-memory/0x005a4b60-0x005a5791.SayModeHelpers.md`: `05AAE1C8100A6EFCF2FBFE566AB425E11CAEDC6B6680ABB0349B6F404038B120 -> E52C38D51AFD19E443B604F5A5E7FBC964D3A9F03A8C8D4E66CA720207C4630E`.
  - `by-file/SayInputPanes.md`: `C8AA2BB30572D060D7983642B63DD20F5FA143B560B3B7E3CA5DD2D9A2A4509F -> 7416EFCFC243C48CF6DD1F98C10116D9E24ECB46C5F2D4C92A5C65B9769F5406`.
  - `by-memory/0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode.md`: `0D27C1A4C35501647D89EE04D76A0555F77B8DEDB00001EB06C24FAF8FBD1205 -> 47F1496A491DF78C5586FE3315769B4B50BF1560E00BC170194CC38339DD5C83`.
- B002 also reconciled this same report under its own JIT lease after all ordinary and supervisor-supplied readbacks. B002 performed no manual coverage, generated/tracker, audit/catalog, goal/assignment, IDA/MCP, report lifecycle/archive, or `execute_report` action.
- Historical primary-supervisor Gate 2B retry1 changed only the discarded in-memory worker: six renames and I002-T were not saved, PID `25088` was terminated, and canonical/backup identity remained `29AA9F5B...1028D`, 143,197,232 bytes. No IDB file is listed as changed by that attempt.
- Historical primary `GATE2B_FAILED_PRE_SAVE` for report SHA `2A690241...69ECD` changed no in-memory or disk state: two fresh exact-P0 workers exposed the helper API-row-size/storage-span mismatch, then both transaction worker and verifier were absent; zero mutations, zero save, P0 `29AA9F5B...1028D`, 143,197,232 bytes, mtime `2026-08-05T22:14:13.0368626Z`, and no disk delta. No Gate 2B credit is claimed.
- Accepted primary-supervisor IDA change: the one-save transaction persisted thirteen atomic actions from P0 `29AA9F5B...1028D` to S1 `2ACC15A3...1024`; complete protections and fresh-reopen persistence passed. This line records supervisor-owned evidence and does not attribute the IDB edit to B002.
- Accepted primary-supervisor manual/generated changes: commands `21325`-`21327` applied/validated the six exact manual rows, and command `21328` generated/read back the exact Chatting/SayInputPanes/ag-memory/tracker semantics. Later unrelated refreshes advanced shared hashes while preserving those semantics.
- Validator-owned registry/projected-stat bookkeeping occurred only as a consequence of the six explicitly authorized scoped commands; B002 made no manual edits to validator state.
- Current lifecycle state is not asserted here. It is external and authoritative only from current path plus validator-owned status/history metadata.

## Implementation Tracking Checklist

All 48 rows mirror the ledger's exact Actor, Action, and Verification-state values. Current split is 42 checked and 6 unchecked: ordinary callback claims C0002FJ-001 through -023 and -036 through -039 are physically proved; primary-supervisor claims C0002FJ-024 through -035, -040, -041, and -044 are reconciled to accepted saved/manual/generated evidence; C0002FJ-042, -043, -045, -046, -047, and -048 remain proposed/unchecked for fresh gates, lifecycle boundary, post-lifecycle refresh/audit, and final exact-artifact verification.

| Claim ID | Actor | Action | Exact action/readback and inline proof | Verification state |
| --- | --- | --- | --- | --- |
| C0002FJ-001 | B002 callback | incorporate | [x] Target SHA `1F607493...BFD` physically contains exact range/body/fence/vtable hashes and sole slot `0x00614f84`. | applied |
| C0002FJ-002 | B002 callback | incorporate | [x] Target physically contains one canonical formal `bool ChattingVarietySelectPane::HandlePointerOrMouseEvent(Event *event)`. | applied |
| C0002FJ-003 | B002 callback | incorporate | [x] Target Behavior/formal CPP physically contains Event +4/+8/+c and kinds 0/1/3. | applied |
| C0002FJ-004 | B002 callback | incorporate | [x] Target physically maps the adjusted facet to `m_pressCaptured`, `m_hotRow`, and inherited `m_visibleBounds`. | applied |
| C0002FJ-005 | B002 callback | incorporate | [x] Exact input-lock guard and false return are present in Behavior and formal CPP. | applied |
| C0002FJ-006 | B002 callback | incorporate | [x] Exact cursor-move HitTest/store/invalidate/return path is present. | applied |
| C0002FJ-007 | B002 callback | incorporate | [x] Exact left-down inside capture and outside `OnDismiss(event)` paths are present. | applied |
| C0002FJ-008 | B002 callback | incorporate | [x] Captured-release requirement and false/-1 state clear are present. | applied |
| C0002FJ-009 | B002 callback | incorporate | [x] Row-0 Default write/current-mode opener is present in target and support readback. | applied |
| C0002FJ-010 | B002 callback | incorporate | [x] Row-1 Shout write/`OpenShoutInputPane` is present in target and support readback. | applied |
| C0002FJ-011 | B002 callback | incorporate | [x] Row-2 Whisper/recipient-name opener and polluted-alias history are present across target/helper/file docs. | applied |
| C0002FJ-012 | B002 callback | incorporate | [x] Row-3 Group opener and literal evidence are present across target/helper/file docs. | applied |
| C0002FJ-013 | B002 callback | incorporate | [x] Row-4 Clan opener, literals, and Plan rejection are present across target/helper/file docs. | applied |
| C0002FJ-014 | B002 callback | incorporate | [x] Row-5 `ChattingColorPane` construction and absent mode write are physically documented. | applied |
| C0002FJ-015 | B002 callback | incorporate | [x] Release-miss refresh and outside-only deletion behavior are present. | applied |
| C0002FJ-016 | B002 callback | incorporate | [x] Target/UID0002FK physically contain source-level `OnDismiss(event)`, exact `retn 4`, and sibling ABI proof. | applied |
| C0002FJ-017 | B002 callback | incorporate | [x] Formal target source uses accepted human helper names; raw/polluted names remain only as historical evidence. | applied |
| C0002FJ-018 | B002 callback | incorporate | [x] Exact formal target CPP occurs once in SHA `1F607493...BFD`. | applied |
| C0002FJ-019 | B002 callback | already-present | [x] Both Chatting child H channels are blank; class SHA `EA00A194...7B91` owns each corrected declaration once and stale `OnDismiss()` zero times. | already-present |
| C0002FJ-020 | B002 callback | incorporate | [x] Both targets physically read `94/95` with exact summaries/rationales and unchanged routing metadata. | applied |
| C0002FJ-021 | B002 callback | incorporate | [x] Callback/current class hashes preserve both closures and corrected declaration; dismiss hash preserves one exact body; later current class `93/94` is unrelated accepted work. | applied |
| C0002FJ-022 | B002 callback | incorporate | [x] Helper/file/dispatcher hashes prove all five roles, unchanged mixed aggregate policy, exact H block, and CPP-definition-only route. | applied |
| C0002FJ-023 | B002 callback | historicalize | [x] All six destinations preserve and explicitly supersede reversed XY, OnMouseEvent, zero-argument OnDismiss, unresolved-helper/header, and blank-C++ history. | applied |
| C0002FJ-024 | Primary supervisor | already-present | [x] Accepted Gate 2B protected the literal seven-row I001 frame plus the source-quality target name/type/comments/item/bytes/xrefs through persisted S1. | already-present |
| C0002FJ-025 | Primary supervisor | incorporate | [x] Accepted one-save Gate 2B persisted exact I002-R/I002-T/I002-C name/type/function-regular comment, deterministic return-plus-event frame, body/xrefs, and `retn 4`. | applied |
| C0002FJ-026 | Primary supervisor | incorporate | [x] Accepted Gate 2B persisted `OpenInputPaneForCurrentSayMode` plus exact function-regular comment and preserved all I003 API rows, spans, body, xrefs, and other comment channels. | applied |
| C0002FJ-027 | Primary supervisor | incorporate | [x] Accepted Gate 2B persisted `OpenSayToUserNameInputPane` plus exact function-regular comment and preserved all I004 protections. | applied |
| C0002FJ-028 | Primary supervisor | incorporate | [x] Accepted Gate 2B persisted `OpenSayToGroupMessageInputPane` plus exact function-regular comment and preserved all I005 rows/span/body/xrefs/literals. | applied |
| C0002FJ-029 | Primary supervisor | incorporate | [x] Accepted Gate 2B persisted `OpenSayToClanMessageInputPane` plus exact function-regular comment and preserved all I006 rows/span/body/xrefs/literals. | applied |
| C0002FJ-030 | Primary supervisor | incorporate | [x] Accepted Gate 2B persisted `OpenShoutInputPane` plus exact function-regular comment and preserved all I007 protections. | applied |
| C0002FJ-031 | Primary supervisor | already-present | [x] Accepted full readback/fresh reopen preserved every literal frame/span/dependency/comment/byte/xref/UDT/sibling/neighbor protection. | already-present |
| C0002FJ-032 | Primary supervisor | incorporate | [x] Accepted P0-to-S1 result persisted all thirteen actions and the complete corrected protection inventory. | applied |
| C0002FJ-033 | Primary supervisor | incorporate | [x] Command `000000021325` applied and validated exact UID0002FJ/UID0002FK/UID0002RX by-memory rows, exit `0`, `ok:1`; current rows match. | applied |
| C0002FJ-034 | Primary supervisor | incorporate | [x] Command `000000021326` applied and validated exact UID00001Z by-class row, exit `0`, `ok:1`; current row matches. | applied |
| C0002FJ-035 | Primary supervisor | incorporate | [x] Commands `000000021325/21327` applied and validated exact UID0001KO by-memory and UID0000N9 by-file rows, exit `0`, `ok:1`; current rows match. | applied |
| C0002FJ-036 | B002 callback | incorporate | [x] Target validator command `000000021233` returned exit `0`, `ok: 1`, generated deferred. | applied |
| C0002FJ-037 | B002 callback | incorporate | [x] Class validator command `000000021235` returned exit `0`, `ok: 1`, generated deferred. | applied |
| C0002FJ-038 | B002 callback | incorporate | [x] Support commands `000000021234/21238/21241/21243` each returned exit `0`, `ok: 1`, generated deferred. | applied |
| C0002FJ-039 | B002 callback | incorporate | [x] All six destinations were physically reread at recorded post-validator hashes; this report is reconciled claim by claim. | applied |
| C0002FJ-040 | Primary supervisor | incorporate | [x] Autogen command `000000021328` and later current physical reread prove both exact Chatting.cpp bodies once, corrected declaration route, blank child H, and shared SayInputPanes declarations. | applied |
| C0002FJ-041 | Primary supervisor | incorporate | [x] Command-21328 and later current readback prove coded owner/emitter `00001Z`, CPP yes/H no, Chatting.cpp route, and tracker `94/95`, true, `0/0/0` for both targets. | applied |
| C0002FJ-042 | Primary supervisor | incorporate | [ ] Record fresh exact-artifact Gate 1 against current report hash and all 33 sections. | proposed |
| C0002FJ-043 | Primary supervisor | incorporate | [ ] Record fresh Gate 2A against every current checked ordinary claim. | proposed |
| C0002FJ-044 | Primary supervisor | incorporate | [x] Primary-supervisor Gate 2B passed C024-C032 with exact one-save P0-to-S1 persistence and complete protections. | applied |
| C0002FJ-045 | Primary supervisor | already-present | [ ] Preserve lifecycle-neutral report policy and derive state only from current path/history. | proposed |
| C0002FJ-046 | Primary supervisor | incorporate | [ ] Perform post-lifecycle coherent generated refresh and exact semantic readback. | proposed |
| C0002FJ-047 | Primary supervisor | incorporate | [ ] Audit exact moved artifact and post-move current authority before count/closure. | proposed |
| C0002FJ-048 | B002 report | incorporate | [ ] Preserve actor boundary, exact 33 headings, mirrored 48 claims, 42 checked/6 unchecked, one terminal marker, and zero active B002 leases; primary supervisor verifies the exact post-edit artifact. | proposed |

READY_FOR_SUPERVISOR_GATE1_REVIEW

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"additional_uids":["0002FK"],"agent":"B002","command_id":"000000021366","destination_path":"executed-b-agent-research/B002/0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B002/research/0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md","timestamp":"2026-08-06T01:25:08-04:00","uid":"0002FJ"} -->
<!-- {"additional_uids":["0002FK"],"agent":"B002","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B002/0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"0002FJ"} -->
<!-- {"additional_uids":["0002FK"],"agent":"B002","command_id":"000000023052","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B002/0002FJ-ChattingVarietySelectPaneOnMouseEvent-source-quality.md","timestamp":"2026-08-13T03:35:41-04:00","uid":"0002FJ"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
