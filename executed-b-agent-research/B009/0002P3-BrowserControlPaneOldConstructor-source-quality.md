** TARGET-REPORT-UID:0002P3 **
** AUTHOR-AGENT-ID:B009 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | Medium **
# 0002P3 BrowserControlPaneOldConstructor Source-Quality Research


## Finalized Report / Current Recommendation
- Current recommendation: keep [UID:0002P3] `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md` owned/emitted by [UID:000016] `BrowserControlPaneOld`, keep it reconstructable, retain implemented documentation quality `90/92`, and keep the formal first-draft constructor body now inserted in the target.
- Final disposition after Gate 1 repair: current MCP evidence plus current support docs now resolve the prior blank-C++ blockers to implementation-ready support changes. The target can emit clean source if support docs add a Browser initial-URL constructor/helper, a BrowserThread constructor/member declaration for the Browser pointer/host-init flag/bounds, and a small old-pane constructor declaration using existing `ControlPane` and `MSGHandler` bases. No raw vtable stores, raw offsets, EH scaffolding, or Browser private-field writes are required in the target body.
- Implementation status: target and support docs have been updated with the MCP-backed support declarations, and the formal `RECONSTRUCTION_CPP CODE` body is inserted in UID0002P3.
- Confidence: strong for range/behavior/ownership/support-shape evidence; medium-high for exact original helper names such as `RectBounds` and `StartThread`.

## Supporting Research
- Lifecycle/status notes: this report historically passed supervisor Gate 1 at SHA `1A29255C6F69CD71C541A0E92590613DEF638DB47C9D0C56C685C31E0F4481E8` and was implemented by B009 in the callback on 2026-07-03. The later SHA `83232F7AACE5A42491B1A3E7E6C27137BA25BD3BE285243E29176D78247D025B` failed Gate 1 for stale current-state wording; this same report has now been repaired in place and awaits supervisor Gate 1 recheck / Gate 2 verification. It has not been executed or archived.
- Supervisor override on 2026-07-03 restored MCP and named usable session `b010_00032w_20260703`; all MCP facts below use that existing session. No `idb_open`, `idb_close`, restart, lifecycle, archive, coverage-report edit, generated-file edit, or `execute_report` command was run.
- Matching executed reports used as leads include B006 `00032Y-BrowserNonDeletingDestructor-empty-emitter-source-quality.md`, B010 `0002AD-BrowserThreadHostGlobals-source-quality.md`, B010 `000333-BrowserLegacyHostWndProc-source-quality.md`, B011 `00032S-BrowserControlPaneOldNonDeletingDestructor-source-quality.md`, and B013 `0000Z3-BrowserDialogOldCore-source-quality.md`.

## Target
- Target UID: `0002P3`.
- Target path: `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md`.
- Historical source queue/report row at original assignment time: `auto-generated/-ag-research-tracker.md` by-memory not-covered reconstructable row, score `85/89`, combined `87.0`, reports `0`. This row is superseded by the implemented callback and validator refresh described below.
- Current supervisor classification: accepted B-agent implementation callback for an old browser control-pane constructor.
- Current implemented scores and parent state: target is `COMPLETION:90`, `CONFIDENCE:92`, `CANONICAL_OWNER:000016`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000016`, optional position remains blank, and the formal constructor C++ block is inserted. Direct class parent [UID:000016] `BrowserControlPaneOld` records UID0002P3 as source-ready `90/92`; Browser file root [UID:0000HV] records UID0002P3 as source-emitting through BrowserControlPaneOld.

## Current Target State
- Existing metadata: exact half-open range `0x0046ff50-0x00470159`, class constructor, parent memory cluster [UID:0000ZF], likely source root [UID:0000HV] Browser.
- Existing owner/emitter/reconstructable state: owned/emitted by `BrowserControlPaneOld`, reconstructable true. This remains correct.
- Existing C++/emitter state: generated `auto-generated/NexusTK/browser/Browser.cpp` now emits UID0002P3 constructor C++ and includes the Browser initial-URL constructor declaration.
- Existing open questions/blockers: the former target text saying Browser field names were incomplete has been superseded; current Browser class docs and B006 evidence resolve the used field names, and this callback implements the constructor source shape through support declaration recommendations for Browser and BrowserThread.
- Related target/support docs checked: target page, [UID:000016] `BrowserControlPaneOld`, [UID:000013] `Browser`, [UID:00001A] `BrowserThread`, [UID:0000HV] `Browser`, [UID:0000QE] `g_pBrowserThread`, [UID:0002AD] `BrowserThreadHostGlobals`, [UID:00032S], [UID:00032X], [UID:000333], [UID:00032W], [UID:0000Z3], generated Browser.cpp, generated by-memory coverage, and research tracker.
- Current artifact/lifecycle status: implementation callback applied; awaiting supervisor Gate 1 recheck and Gate 2 verification; no `execute_report` run by B009.

## Executive Recommendation
- Keep the direct owner as [UID:000016] `BrowserControlPaneOld`; the constructor constructs the old pane object and stores its Browser pointer at outer `+0x110`.
- Keep source placement under the Browser module [UID:0000HV] / `NexusTK/browser/` because the constructor depends on Browser, BrowserThread, BrowserDialogOld, browser globals, Browser legacy WndProc, and browser COM helpers.
- Target is now `90/92` after callback. The old `Browser field names incomplete` blocker is replaced with the implemented source-shape plan: `Browser(initialUrl)` owns the inline Browser setup and private URL-buffer copy, `BrowserThread(browser, *bounds)` owns the derived-thread member setup, and UID0002P3 emits only old-pane orchestration.
- No split, rename, owner change, or new child page is recommended for this target.

## Supervisor Active Recheck
- Triggering instruction: user/supervisor assigned UID0002P3 to Agent-B009 for a new report-only source-quality pass, then restored MCP session `b010_00032w_20260703`.
- Split repair: not required. MCP confirms `sub_46FF50` size `0x209`, ending at `0x00470159`, followed by `0xcc` padding and next function `0x00470160`.
- Source-bearing children in scope: this pass covers only UID0002P3. Neighboring Browser, BrowserThread, destructor, WndProc, and global pages were used as support evidence and do not need new child creation.

## Inference Research Guidance Check
- `by-structure.md` IDA MCP Output Discipline was followed: exact-address `server_health`, `lookup_funcs`, `decompile`, `disasm` with `max_instructions=180`, `callees` with `limit=80`, `xrefs_to` with `limit=50`, `get_bytes`, and bounded `type_query` were used.
- Existing docs were treated as leads. Current MCP reconfirmed most target facts and corrected the blocker framing: Browser field names are no longer the main blocker, and accepted source declaration shape can be reached with the support edits recommended here.
- IDA facts are separated from documentation evidence and inference throughout this report.
- Stale Wave2/Wave3/generated naming was ignored as authority. Generated Browser.cpp was read only to confirm current empty-emitter state.

## Heuristic / Inference Reanalysis And Validation
| Issue | Evidence checked | Best inference / decision | Rejected alternatives and impact |
| --- | --- | --- | --- |
| Constructor range | MCP `lookup_funcs 0x0046ff50`, bounded `disasm`, and `get_bytes` around start/end. | Keep exact half-open range `0x0046ff50-0x00470159`; `retn 8` at `0x00470156`, padding starts at `0x00470159`, next function `0x00470160`. | Reject merging with prior WndProc table/padding or next function. |
| Caller | MCP `xrefs_to 0x0046ff50` and decompile of `0x00469290`. | Sole direct code xref is `0x0046936e` inside `BrowserDialogOld` construction, allocating a `0x114` old pane and passing dialog-local bounds plus URL. | Reject BrowserWindow/new BrowserControlPane ownership. |
| ControlPane/MSGHandler setup | Disassembly shows `sub_4949E0(this,8,bounds)` and `sub_528D60(this+0x108)`, then vtable stores at `+0`, `+0xa0`, `+0xa4`, `+0x108`. B011 destructor/scalar evidence matches. | `MSGHandler` is a secondary base at outer `+0x108`; `m_browser` remains separate at `+0x110`. | Reject old composition/ambiguous-member wording for `MSGHandler`. |
| Browser object layout | MCP decompile/disasm: `operator new(0x234)`, five Browser vtable stores at `+0/+4/+8/+0xc/+0x10`, clears `+0x14`, `+0x18`, `+0x228`, writes `+0x22c = 0x0101`, constructs `+0x230`. Browser class [UID:000013] now names these as `m_hWnd`, `m_webBrowser`, `m_navigationBuffer`, `m_stateFlags`, and `m_currentUrl`. | Field-name blocker is mostly resolved for documentation. `+0x22c` should remain `m_stateFlags` / state word, not a final enum. | Reject raw `v4[138]`/`*((WORD*)v4+278)` labels and reject treating `0x0101` as proven enum. |
| URL ownership | MCP shows length scan, allocation via `unknown_libname_19`, store at Browser `+0x228`, and `_wcscpy_s` copy from the input URL. Destructor support frees the same field. | The constructor gives Browser an owned heap copy of the initial navigation URL, best named `initialUrl`/`m_navigationBuffer`. | Reject caller-retained URL lifetime or a borrowed pointer model. |
| BrowserThread setup | MCP shows `sub_4F4AA0(0x80)`, `sub_596250(thread,1)`, `unk_67AB88` store/clear, BrowserThread vtable `0x00613744`, Browser pointer at `+0x68`, byte `+0x6c = 0`, bounds copy to `+0x70`, and `sub_5965E0`. B010 [UID:0002AD] confirms `g_pBrowserThread` as `BrowserThread *`. | BrowserThread has a Browser pointer at `+0x68`, a flag byte at `+0x6c`, and a bounds RECT at `+0x70`; thread start is Browser-owned behavior using generic Thread infrastructure. | Reject Thread-file ownership of the global and reject raw `DWORD` storage. |
| Browser support declaration | MCP target body constructs a `0x234` Browser object, initializes five interface views, clears `m_hWnd`, `m_webBrowser`, and `m_navigationBuffer`, writes `m_stateFlags = 0x0101`, constructs `m_currentUrl`, allocates/copies the input wide URL to `m_navigationBuffer`, and later Browser destructor frees that buffer. Current `by-class/Browser.md` names these fields and keeps them private. | Add a support-level `Browser::Browser(const wchar_t *initialUrl)` or equivalent private helper so UID0002P3 can use `new Browser(initialUrl)` without direct private-field writes. This is inference, but it is implementation-ready and behavior-preserving because all hidden writes are Browser-owned initialization. | Reject source that writes `browser->m_navigationBuffer` or `browser->m_stateFlags` from the pane, and reject raw `operator new(0x234)`/vtable stores. |
| BrowserThread support declaration | MCP target body calls generic `Thread` construction with argument `1`, writes BrowserThread vtable, stores Browser at `+0x68`, clears byte `+0x6c`, copies 16 bytes from `bounds` to `+0x70`, writes `g_pBrowserThread`, then starts/resumes via `sub_5965E0`. Current `by-class/BrowserThread.md` already documents `+0x68`, `+0x6c`, `+0x70`, and `RunMessageLoop` use. | Add a support-level `BrowserThread::BrowserThread(Browser *browser, const RectBounds &bounds)` with `Thread(true)`, `m_browser(browser)`, `m_hostInitialized(false)`, and `m_bounds(bounds)`, plus `StartThread()`/inherited thread-start naming for `0x005965e0`. UID0002P3 can then call `new BrowserThread(browser, *bounds)`, assign `g_pBrowserThread`, and start it. | Reject Thread-file ownership and reject leaving the BrowserThread members as raw offsets. The exact final field name for `+0x6c` can remain `m_hostInitialized` or a documented equivalent. |
| MSGHandler/source shape | MCP `0x00528d60` sets the MSGHandler vtable and stores `Application::ExchangeMSGHandler(this)` at slot `+4`; current `by-class/MSGHandler.md` emits `MSGHandler()`, virtual destructor, `DispatchMessage(MSG*)`, and private `m_previousHandler`. BrowserControlPaneOld docs resolve it as a secondary base at outer `+0x108`. | No UID0002P3 blocker remains. Constructor source should use `MSGHandler()` in the initializer list; the base handles the active-handler swap. | Reject composition wording and reject a target-local manual call to `Application::ExchangeMSGHandler`. |
| Null bounds/default rectangle branch | MCP disassembly at `0x0046ff98-0x0046ffe6` checks `bounds == NULL` and calls the simple four-int rectangle constructor/helper `sub_4B78D0` with zero `this`; MCP `0x004b78d0` writes four integer slots. The only direct caller from `BrowserDialogOld` passes a real local bounds object. | Preserve this as a source-visible legacy/null-guard quirk in the first draft with a temporary `RectBounds(0, 0, g_clientWidth, g_clientHeight);` statement, while keeping the real BrowserThread handoff as `*bounds`, matching the binary's later unconditional copy. | Reject inventing a fallback that replaces `bounds` with a local default because the binary never does that before copying into BrowserThread. |
| Formal C++ readiness | The prior blockers have been researched to resolution: Browser private-field access moves into a Browser support constructor; BrowserThread raw member setup moves into a support constructor; MSGHandler already has accepted declaration shape; ControlPane already has accepted `ControlPane(unsigned char, const RectBounds *)`. | UID0002P3 should receive formal first-draft C++ now. Support declarations must be applied with it or before it. | Reject keeping the formal block blank solely because support docs need callback edits; this repair provides exact implementation-ready support changes. |
| Score movement | MCP restored and current support reports resolve range/caller/layout/source-shape blockers, with exact formal C++ now available. | Raise to `90/92`; keep below final because exact original names for `RectBounds`, `StartThread`, final BrowserThread source split, and `m_stateFlags` bit semantics remain inferred. | Reject unchanged/88-only score because the code/no-code blocker is resolved to a draft body. Reject 95+ because support spellings are still inferred. |

## Evidence Standards Used
- IDA MCP direct evidence: health, function lookup, bounded decompile/disasm, direct callees, xrefs to constructor/global/vtables, boundary bytes, and bounded type queries.
- Documentation evidence: current by-memory, by-class, by-file, by-global, executed B reports, and generated Browser.cpp empty-emitter row.
- Negative evidence: no IDA local UDTs for `Browser`, `BrowserThread`, or `MSGHandler`; no extra constructor callers beyond `0x0046936e`; no proof that the original source used the exact helper names `RectBounds` or `StartThread`.
- Evidence strength: strong for binary behavior, owner, and support declaration feasibility; medium-high for exact final source spelling because original names/access control remain inferred.

## Evidence Checked
- IDA MCP/manual-disassembly/raw-byte checks performed: `server_health` on `b010_00032w_20260703`; `lookup_funcs` for `0x0046ff50`, caller/support callees and neighbors; `decompile 0x0046ff50`, `0x00469290`, `0x0046eff0`, `0x00470a00`, `0x004949e0`, `0x004b78d0`, `0x00528d60`, `0x00596250`, `0x005965e0`, and `0x0046f010`; `disasm 0x0046ff50 max_instructions=180 include_total=true` plus narrowed pages at `0x0046ff50` and `0x00470060`; `callees 0x0046ff50 limit=80`; `xrefs_to` for `0x0046ff50`, `0x0067ab88`, BrowserControlPaneOld vtables, Browser vtable, BrowserThread vtable; `get_bytes` around `0x0046ff48`, `0x00470150`, and `0x0067ab88`; bounded `type_query` for Browser/Thread/MSGHandler UDTs.
- by-* docs, support docs, old reports, generated reports, and trackers checked: target page; `by-class/BrowserControlPaneOld.md`; `by-class/Browser.md`; `by-class/BrowserThread.md`; `by-file/Browser.md`; `by-global/g_pBrowserThread.md`; `by-memory/0x0067ab88-0x0067ab90.BrowserThreadHostGlobals.md`; `by-memory/0x00470a00-0x00470dc9.BrowserThreadInitializeBrowserHost.md`; `by-memory/0x00469290-0x0046963c.BrowserDialogOldCore.md`; generated Browser.cpp; generated by-memory coverage; research tracker; matching executed B reports named above.
- Negative checks performed: `type_query` returned no UDT entries for Browser, Thread, or MSGHandler in the active IDB; `xrefs_to 0x0046ff50` found only one direct caller. Historical generated Browser.cpp empty-emitter state is superseded by validator command `000000005619`/generated refresh through `000000005623`.
- Failed, unavailable, or intentionally skipped checks and why: no broad callgraph, broad binary search, generated-file edits by hand, or lifecycle commands were run. Scoped validators were run only after accepted callback edits.

## Claim And Incorporation Ledger
| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | UID0002P3 exact range remains `0x0046ff50-0x00470159`, with padding after `retn 8` and next function at `0x00470160`. | High | MCP `lookup_funcs`, `disasm`, `get_bytes`. | Target Scope / IDA MCP Evidence / Range section. | incorporate | applied |
| C2 | Direct owner/emitter remains [UID:000016] `BrowserControlPaneOld`; Browser file [UID:0000HV] remains source root. | High | Constructor body, caller, BrowserControlPaneOld class layout, Browser file source-root docs. | Target metadata/source placement. | incorporate | applied |
| C3 | `MSGHandler` is resolved as secondary base at outer `+0x108`; `m_browser` is separate at outer `+0x110`. | High | MCP vtable stores plus B011 destructor/scalar evidence. | Target layout section and class support doc. | incorporate | applied |
| C4 | Browser field names are no longer the main blocker: `m_hWnd`, `m_webBrowser`, `m_navigationBuffer`, `m_stateFlags`, and `m_currentUrl` are supported. | Medium-high | MCP constructor, Browser class, B006 destructor report. | Target Browser Field Evidence; Browser support sync. | incorporate | applied |
| C5 | Formal constructor C++ should be inserted using implementation-ready support declarations: `Browser(initialUrl)`, `BrowserThread(browser, *bounds)`, accepted `MSGHandler()` secondary base, and accepted `ControlPane(8, bounds)`. | Medium-high | MCP target body; current Browser/BrowserThread/MSGHandler/ControlPane docs; support decompiles `0x004b78d0`, `0x00528d60`, `0x00596250`, `0x005965e0`, `0x0046f010`. | Target Reconstruction Notes / First-Draft C++ Recommendation and support docs. | incorporate | applied |
| C6 | Score should move from `85/89` to `90/92`, not higher. | Medium-high | Restored MCP evidence, resolved stale blocker, formal first-draft body now available, remaining inferred spelling caveats. | Target metadata / Score Rationale. | incorporate | applied |
| C7 | `g_pBrowserThread` handoff uses `0x0067ab88`, stores BrowserThread pointer at `0x004700ef` or clears at `0x004700f6`; constructors/destructors make it Browser module state. | High | MCP xrefs and B010 [UID:0002AD]. | Target behavior; `g_pBrowserThread` support doc if synced. | incorporate | applied |

## Positive Evidence Summary
- Direct facts supporting the chosen recommendation: MCP confirms exact constructor, sole caller, base construction, vtable stores, Browser allocation/layout, URL copy, BrowserThread setup, global storage, and start-thread call.
- Corroborating documentation/generated-report evidence: current Browser, BrowserControlPaneOld, BrowserThread, g_pBrowserThread, BrowserThreadHostGlobals, and B006/B010/B011 reports agree with the direct MCP facts; generated Browser.cpp now records UID0002P3 as source-emitting constructor code.
- Strongest inference chain: the constructor is source-authored and reconstructable, and the source-clean body is now available by moving private Browser and BrowserThread initialization into their own support constructors while keeping UID0002P3 responsible only for old-pane orchestration.

## IDA MCP Facts
- Function/range facts: `server_health` reports module `NexusTK.exe`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, `auto_analysis_ready true`, `hexrays_ready true`, strings cache ready. `lookup_funcs 0x0046ff50` returns `sub_46FF50` size `0x209`; `lookup_funcs 0x00470160` returns next function `unknown_libname_10` size `0xe`.
- Data/table/padding facts: bytes at `0x0046ff4c-0x0046ff4f` are `0xcc` before the constructor; bytes at `0x00470156` are `c2 08 00`, then `0xcc` padding starts at `0x00470159`; `0x0067ab88-0x0067ab90` reads as zero-initialized bytes.
- Xref facts: `xrefs_to 0x0046ff50` has one code xref at `0x0046936e` in `sub_469290`. `xrefs_to 0x0067ab88` returns eleven refs, including constructor stores `0x004700ef`/`0x004700f6`, destructor clears `0x0046eff6`/`0x004706fc`, and BrowserDialogOld/Browser invoke consumers.
- Vtable/global/type facts: BrowserControlPaneOld vtables `0x00613780`, `0x006137e8`, `0x00613818`, and `0x00613824` are referenced by constructor, destructor, and scalar wrapper. Browser vtable `0x00613830` is referenced by Browser destructor, this constructor, and Browser scalar wrapper. BrowserThread vtable `0x00613744` is referenced by BrowserThread destructor, this constructor, and BrowserThread scalar wrapper. Bounded `type_query` found no Browser/Thread/MSGHandler UDTs in IDA.
- Negative IDA facts: no second caller to `0x0046ff50`; no IDA UDT declaration proves a safe constructor/member access model.

## Function / Child Inventory
| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x0046ff50-0x00470159` | [UID:0002P3] `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md` | BrowserControlPaneOld constructor | true | [UID:000016] | implemented `90/92` | exact target, first-draft C++ inserted |
| `0x00469290-0x0046963c` | [UID:0000Z3] `BrowserDialogOldCore` | caller/legacy dialog core | true | Browser/BrowerDialogOld route | `86/91` | support |
| `0x0046f1e0-0x0046f216` | [UID:00032S] old-pane ordinary destructor | destructor/layout parity | true | [UID:000016] | `88/90` generated row context | support |
| `0x004704f0-0x00470579` | [UID:00032X] scalar deleting destructor/adjustors | compiler glue proving layout/size | false | [UID:000016] | `87/90` context | support/no-code |
| `0x0067ab88-0x0067ab90` | [UID:0002AD] BrowserThreadHostGlobals | global declarations | true | [UID:0000HV] | `90/92` generated row context | support, emits declarations |

## Direct Xref / Caller Inventory
| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0046936e` | only direct code xref to `0x0046ff50` | BrowserDialogOld constructor creates the old pane with bounds and URL. |
| `0x004949e0` | callee | ControlPane/base constructor with style/id `8`. |
| `0x00528d60` | callee | `MSGHandler` base constructor at `this+0x108`. |
| `0x00582b20` | callee | constructs Browser `m_currentUrl`/string member at `+0x230`. |
| `0x004f4aa0` | callee | allocation helper for `0x80` BrowserThread object. |
| `0x00596250` | callee | Thread base construction/setup for BrowserThread with argument `1`. |
| `0x005965e0` | callee | starts/resumes the BrowserThread. |
| `0x004700ef`, `0x004700f6` | xrefs to `0x0067ab88` | store BrowserThread pointer or clear global. |

## Documentation Evidence And IDA Status
- Existing docs that support the conclusion: target page already records most constructor behavior; Browser class now provides source-facing field names; BrowserThreadHostGlobals now emits `static BrowserThread *g_pBrowserThread;`; BrowserControlPaneOld destructor/scalar reports settle layout and object size.
- Existing docs that were stale, incomplete, or contradicted before callback: target page said Browser object field names remained incomplete as the main C++ blocker. That has been replaced in the target/support docs with resolved field names, implementation-ready support declaration text, and the formal first-draft constructor body.
- Historical generated/coverage state before the implementation callback: `auto-generated/NexusTK/browser/Browser.cpp` had UID0002P3 as an `Empty Emitter Marker`; `auto-generated/-ag-memory-coverage.md` marked UID0002P3 as emits through [UID:000016] but `coded no`; the research tracker reported `85/89`, reports `0`.
- Current generated/coverage state after validator command `000000005623`: `auto-generated/NexusTK/browser/Browser.cpp` emits UID0002P3 constructor C++, includes `Browser(const wchar_t *initialUrl);`, and the target/support docs record implemented `90/92` source-ready status. B009 did not edit generated files manually.

## Ranked Ownership Analysis

### 1. [UID:000016] BrowserControlPaneOld
- Evidence for: constructor receives `this` as old-pane object, calls pane base constructor, constructs `MSGHandler` at outer `+0x108`, installs BrowserControlPaneOld vtables, stores `m_browser` at outer `+0x110`, and is called by BrowserDialogOld to create the child pane.
- Evidence against: body also allocates Browser and BrowserThread and writes Browser module globals, but these are owned dependencies instantiated by the old pane.
- Decision: direct canonical owner/emitter remains [UID:000016].

### 2. [UID:0000HV] Browser file source root
- Evidence for: Browser file groups Browser, BrowserThread, BrowserControlPaneOld, BrowserDialogOld, old host WndProc, COM helpers, and global storage; generated output places UID0002P3 in `auto-generated/NexusTK/browser/Browser.cpp`.
- Evidence against: by-file is a source root, not the narrow direct class owner for the constructor metadata.
- Decision: source placement remains Browser module; do not reowner the by-memory target away from the class.

### 3. [UID:000013] Browser / [UID:00001A] BrowserThread
- Evidence for: constructor creates and initializes both objects.
- Evidence against: neither object owns the old-pane constructor; BrowserThread and Browser destructors/host methods are separate children. Treating UID0002P3 as Browser or BrowserThread owned would hide the old pane's `m_browser` field and MSGHandler base setup.
- Decision: support docs only.

### Proposed new file/grouping, if applicable
- Proposed owner/name/path: no new file/grouping. Keep under [UID:0000HV] `NexusTK/browser/`; optional `BrowserLegacy.cpp` remains plausible but unproven.
- Likely full contents: current Browser root already groups this old-pane constructor with Browser, BrowserThread, BrowserDialogOld, BrowserControlPaneOld, legacy host WndProc, and browser globals.
- Candidate related items that belong: UID000333, UID0002AD, UID00032W, UID00032S/32T/32U/32X, Browser class children, BrowserDialogOld support.
- Candidate related items rejected: Thread file ownership for `g_pBrowserThread`; BrowserWindow/new BrowserControlPane ownership.
- Standalone, narrow, or broad source-file inference: broad browser module remains best current inference.

## Source Placement
- Recommended source file/class/global/module placement: [UID:000016] `BrowserControlPaneOld` method emitted under [UID:0000HV] Browser source root, currently `NexusTK/browser/Browser.cpp` or later accepted browser-legacy split.
- Why this placement fits source-tree and subsystem context: direct caller is BrowserDialogOld; allocated objects are Browser and BrowserThread; globals and WndProc are Browser-owned; generated output places the target in Browser.cpp.
- Rejected placements and why: BrowserWindow/new BrowserControlPane do not call or own this old path; Thread owns base infrastructure but not browser singleton state; Browser class owns COM methods, not the old pane constructor.
- Remaining placement uncertainty: one broad `Browser.cpp` versus `BrowserLegacy.cpp`/`BrowserOld.cpp` is not proven. It does not block metadata or the first-draft constructor body.

## Range / Split / Padding / Reclassification Analysis
- Exact range/boundary facts: function starts at `0x0046ff50`, size `0x209`, returns at `0x00470156` with `retn 8`, half-open end `0x00470159`, `0xcc` padding through `0x0047015f`, next function begins `0x00470160`.
- Children/subranges to create, repair, merge, leave ignored, or leave no-owner: none. SEH cleanup landing pads in disassembly are compiler support for this constructor, not new source children.
- Padding/table/data/code distinctions: prior WndProc owns switch/table bytes ending before `0x0046ff50`; UID0002P3 owns the function body only through `0x00470159`.
- Parent/container impact: [UID:0000ZF] broad BrowserOleLegacyAndHelpers remains non-emitting split inventory; no parent metadata change is required.

## Negative Evidence Summary
- No active IDA UDTs for `Browser`, `BrowserThread`, or `MSGHandler` were found by bounded `type_query`; current documentation declarations therefore remain reconstruction docs, not direct IDA local-type proof.
- The exact original spellings of `Browser::Browser(const wchar_t *)`, `BrowserThread::BrowserThread(Browser *, const RectBounds &)`, `RectBounds`, `g_clientWidth`, `g_clientHeight`, and `StartThread` are inferred source-facing names. This limits score/confidence but no longer blocks a formal first draft because the member ownership and behavior are directly supported.
- Consumer xrefs to `g_pBrowserThread` prove shared Browser module state, not Thread-file ownership.
- The null-bounds branch is odd: MCP shows a call to the simple rectangle constructor/helper with zero `this`, then the later BrowserThread path still copies from `*bounds`. Because the only direct caller passes a non-null local bounds object, do not invent a replacement local fallback in source.

## IDA Rename / Type / Comment Recommendations
- Proposed source-facing names/types/comments:
  - Constructor signature for documentation: `BrowserControlPaneOld::BrowserControlPaneOld(const RECT *bounds, const wchar_t *initialUrl)` or project-local rectangle equivalent. Use `initialUrl`, not raw `Source`.
  - `this+0x110`: `Browser *m_browser`.
  - Browser fields used here: `m_hWnd`, `m_webBrowser`, `m_navigationBuffer`, `m_stateFlags`, `m_currentUrl`.
  - BrowserThread fields for support docs: `m_browser` at `+0x68`, a launch/show flag byte at `+0x6c` with exact final name still not proven, and a bounds rectangle at `+0x70`.
  - `0x0067ab88`: `g_pBrowserThread` as `BrowserThread *`.
- Evidence for each proposed name/type/comment: MCP decompile/disasm and current Browser/B010/B011 support docs.
- Items intentionally left unchanged and why: do not create final enum name for `0x0101`; do not name the BrowserThread `+0x6c` byte beyond descriptive flag until `RunMessageLoop`/InitializeHost source shape is accepted; do not rename IDA DB items in this report-only pass.
- Whether IDA DB edits are safe, unsafe, or not requested: not requested; report-only pass.

## First-Draft C++ Recommendation
- Eligible for draft C++: yes. The source-authored constructor can be expressed cleanly once the support declarations below are accepted. The target body should not expose Browser private fields, BrowserThread raw offsets, raw vtable stores, allocation sizes, or SEH cleanup.
- Recommended code: insert this exact formal block into UID0002P3.

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
BrowserControlPaneOld::BrowserControlPaneOld(const RectBounds *bounds, const wchar_t *initialUrl)
    : ControlPane(8, bounds),
      MSGHandler(),
      m_browser(NULL)
{
    if (bounds == NULL)
        RectBounds(0, 0, g_clientWidth, g_clientHeight);

    Browser *browser = new Browser(initialUrl);
    BrowserThread *thread = new BrowserThread(browser, *bounds);

    g_pBrowserThread = thread;
    thread->StartThread();

    m_browser = browser;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

- Third-party import directive, when applicable: not applicable.
- Reason it preserves exact original behavior: `ControlPane(8, bounds)` matches `sub_4949E0(this,8,bounds)`; `MSGHandler()` matches `sub_528D60(this+0x108)`; `new Browser(initialUrl)` hides the exact Browser initialization, URL-buffer allocation/copy, `m_stateFlags = 0x0101`, and `m_currentUrl` construction inside Browser-owned support; `new BrowserThread(browser, *bounds)` hides the `Thread(true)` base setup plus Browser pointer, host-init byte, and copied bounds members; `g_pBrowserThread = thread`, `thread->StartThread()`, and `m_browser = browser` match the observed singleton handoff, thread start, and old-pane Browser pointer store. The null-bounds statement preserves the binary's observed guard shape without inventing a replacement bounds object before the unconditional `*bounds` copy.
- Reason it matches the most plausible original precompiled mid-2000s developer source shape: the source reads as ordinary VC-era constructor orchestration in the Browser module. The compiler, Browser constructor, BrowserThread constructor, and Thread base constructor account for vtable stores, raw allocation checks, and EH cleanup, so the target does not contain decompiler-shaped scaffolding.
- Inferred source-facing names/types/fields used instead of IDA/decompiler labels: `bounds`, `initialUrl`, `m_browser`, `m_navigationBuffer`, `m_stateFlags`, `m_currentUrl`, `g_pBrowserThread`, BrowserThread `m_browser`/bounds fields.
- Naming/coding style convention used and evidence for consistency: Browser module docs use `m_` member names and `g_p...` browser globals; current [UID:000013] and [UID:0002AD] support docs already use that convention.
- Support declarations required for this target body:
  - `Browser::Browser(const wchar_t *initialUrl);` in [UID:000013], implemented as Browser-owned initialization of `m_hWnd = NULL`, `m_webBrowser = NULL`, `m_navigationBuffer = NULL`, `m_stateFlags = 0x0101`, construction of `m_currentUrl`, and owned heap copy of `initialUrl` into `m_navigationBuffer`. Do not initialize unrelated Browser fields unless separate evidence supports those writes.
  - `BrowserThread::BrowserThread(Browser *browser, const RectBounds &bounds);` in [UID:00001A], implemented as `Thread(true)`, derived BrowserThread setup, `m_browser = browser`, `m_hostInitialized = false`, and `m_bounds = bounds`.
  - `Thread::StartThread()` or accepted equivalent name for `0x005965e0`, which calls the virtual pre-start hook and resumes the thread handle.
  - `ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)` and `MSGHandler::MSGHandler()` are already present/accepted enough for this target.
- Exact no-code proof, if not eligible: not applicable after this repair. The earlier blank-C++ conclusion is superseded by the implementation-ready support declarations and formal body above.

## Final Recommendation
- Exact changes applied or recommended: by-* changes applied. UID0002P3 target page now has current MCP evidence, source-facing field names, support declaration dependencies, formal first-draft C++, and `90/92` metadata.
- Exact parent assignments applied or recommended: kept `CANONICAL_OWNER:000016`, `EMITTER_UIDS:000016`, `RECONSTRUCTABLE:TRUE`.
- Exact items left no-owner/non-emitting and why: none. UID0002P3 remains reconstructable and should now be source-emitting.
- Exact future work outside this assignment scope: after callback, refine final exact helper names for `RectBounds`, `g_clientWidth/g_clientHeight`, `StartThread`, and BrowserThread source split if stronger source evidence appears. These are score/caveat items, not blockers for the first draft.

## Recommended Target Doc Changes
- Target path: `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md`.
- Exact report facts to incorporate:
  - Add current MCP session `b010_00032w_20260703` evidence: health ok, `sub_46FF50` size `0x209`, sole xref `0x0046936e`, 166-instruction disassembly, direct callees list, vtable/global xrefs, boundary bytes, and no IDA UDT entries for Browser/Thread/MSGHandler.
  - Replace stale "Browser object field names are incomplete" blocker with: Browser field names are mostly resolved by [UID:000013]/B006, and formal C++ is now ready if the Browser and BrowserThread support declarations from this report are applied.
  - Document Browser fields as `m_hWnd`, `m_webBrowser`, `m_navigationBuffer`, `m_stateFlags`, and `m_currentUrl`; keep `m_stateFlags = 0x0101` as state word, not final enum.
  - Document BrowserThread setup as Browser pointer at `+0x68`, flag byte at `+0x6c`, bounds RECT at `+0x70`, and start through `0x005965e0`.
- Add the formal first-draft C++ block from this report, with the support declaration prerequisites listed under `Recommended Support Doc Changes`.
- Metadata/score/owner/emitter/reconstructable/C++ changes: set completion/confidence to `90/92`; keep owner/emitter/reconstructable unchanged; replace the blank formal C++ block with the recommended constructor body.
- Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: preserve rejection of raw/decompiler labels, BrowserWindow ownership, Thread-file ownership, direct Browser private-field writes from the pane, and raw vtable/SEH/operator-new source.

## Recommended Support Doc Changes
- Support path: `by-class/BrowserControlPaneOld.md`.
- Exact report facts to incorporate: constructor blocker is resolved to a source-ready child; `MSGHandler` and `m_browser` layout remain resolved; target should be listed as `90/92` with first-draft constructor C++ dependent on Browser and BrowserThread support declarations.
- Metadata/link/score/coverage/source-placement changes: score can remain `87/89`; no metadata change required.

- Support path: `by-class/Browser.md`.
- Exact report facts to incorporate: add support for `Browser::Browser(const wchar_t *initialUrl)` or exact equivalent helper. The constructor owns the initialization observed in UID0002P3: set `m_hWnd` and `m_webBrowser` null, set `m_navigationBuffer` null then to an owned copy of `initialUrl`, write `m_stateFlags = 0x0101`, and construct `m_currentUrl`. Keep this inside Browser so UID0002P3 does not access private fields directly.
- Metadata/link/score/coverage/source-placement changes: no score change required.

- Support path: `by-class/BrowserThread.md`.
- Exact report facts to incorporate: add support for `BrowserThread::BrowserThread(Browser *browser, const RectBounds &bounds)` with `Thread(true)`, `m_browser = browser`, `m_hostInitialized = false`, and `m_bounds = bounds`. Add/confirm the source-facing start method name `StartThread()` for `0x005965e0`, or document the accepted inherited equivalent.
- Metadata/link/score/coverage/source-placement changes: no score change required.

- Support path: `by-class/MSGHandler.md` / `by-file/MSGHandler.md`.
- Exact report facts to incorporate: no declaration blocker remains. UID0002P3 uses the already accepted secondary-base `MSGHandler()` and inherited `m_previousHandler`; do not add target-local `Application::ExchangeMSGHandler` code.
- Metadata/link/score/coverage/source-placement changes: no score change required.

- Support path: `by-file/ControlPane.md` / [UID:0002PB] ControlPane constructor.
- Exact report facts to incorporate: no declaration blocker remains. UID0002P3 should use the already accepted `ControlPane(8, bounds)` constructor route.
- Metadata/link/score/coverage/source-placement changes: no score change required.

- Support path: `by-file/Browser.md`.
- Exact report facts to incorporate: UID0002P3 should remain in Browser source root and now become source-emitting via BrowserControlPaneOld, with Browser/BrowserThread support declarations carried in the same Browser module/source root.
- Metadata/link/score/coverage/source-placement changes: no score change required.

## Score And Metadata Recommendation
- Historical pre-callback score/metadata: `COMPLETION:85`, `CONFIDENCE:89`, owner/emitter [UID:000016], reconstructable true, formal C++ blank.
- Current implemented score/metadata: `COMPLETION:90`, `CONFIDENCE:92`, owner/emitter unchanged, reconstructable true, optional position blank, formal C++ inserted.
- Score rationale and reason not higher/lower: higher than the historical pre-callback state because MCP was restored, current support docs resolve stale field/layout blockers, and this repair resolves the support-declaration blockers to a formal first-draft body. Not higher because the exact original names for `RectBounds`, `g_clientWidth/g_clientHeight`, `StartThread`, Browser initial-URL constructor spelling, and the BrowserThread file split remain inferred.
- Score-improvement attempt:
  - Range/caller/blocker: resolved by MCP lookup/xrefs/decompile/disasm.
  - Browser field blocker: resolved for implementation by Browser class support and B006; callback applied the Browser constructor helper route so UID0002P3 does not write private Browser fields directly.
  - MSGHandler blocker: resolved through B011 and current constructor/disasm.
  - BrowserThread/global blocker: resolved for type/handoff by B010 and MCP; resolved for formal constructor/member source shape by recommending support `BrowserThread(Browser *, const RectBounds &)`.
  - C++ blocker: resolved to the exact formal `RECONSTRUCTION_CPP CODE` insertion above.
- Metadata fields changed or left unchanged by callback: completion/confidence changed to `90/92` and formal C++ inserted; owner, emitter, reconstructable, optional position, and nested state left unchanged.

## Open Questions With Attempted Resolution
- Open question: can formal constructor C++ be inserted now?
  - Evidence checked: current MCP decompile/disasm, Browser class declaration, BrowserThread class page, B006/B010/B011 reports, generated Browser.cpp.
  - Best supported resolution: yes; the implementation callback applied the exact support declarations in this report. The target body does not need private Browser access or raw BrowserThread offsets.
  - Remaining unresolved evidence required: none that blocks first draft. Exact original helper names remain confidence caveats.
- Open question: are Browser fields still too unknown?
  - Evidence checked: Browser class field table, B006 destructor report, MCP constructor and InitializeHost.
  - Best supported resolution: no, most used fields are now source-facing enough for docs.
  - Remaining unresolved: exact final enum/bit semantics of `m_stateFlags = 0x0101`.
- Open question: old browser source file split.
  - Evidence checked: Browser by-file docs, generated Browser.cpp, caller/callee cluster, matching reports.
  - Best supported resolution: keep broad Browser root now; optional legacy split remains unproven and should not block metadata.
- Open question: can the null-bounds branch be source-modeled safely?
  - Evidence checked: MCP disassembly at `0x0046ff98-0x0046ffe6`, `sub_4B78D0` decompile, and sole caller decompile.
  - Best supported resolution: include the source-visible temporary/default rectangle statement but do not replace `bounds`; the sole observed caller passes non-null bounds, and the binary later copies from `*bounds` unconditionally.
  - Remaining unresolved: exact original global names for `word_66DA94`/`word_66DA98`, represented in first draft as `g_clientWidth`/`g_clientHeight`.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text
- Applicable: not applicable. No manual `-coverage-report.md` or supervisor-owned tracker edit is required. Validator-owned generated outputs already refreshed through scoped validators during the callback; any future execution refresh is supervisor-only.

## Follow-Up Actions
- Supervisor actions: Gate 1 recheck this repaired report, then perform Gate 2 verification against the already-applied target/support doc updates and scoped validator results. If both gates pass, only the supervisor may run report execution.
- A-agent actions: none.
- B009 future research actions: none for this callback unless supervisor requests another repair. Later helper-name/source-split refinements can be handled only if stronger evidence appears; no separate pass is needed before first-draft constructor insertion because insertion is already applied.

## Confidence
- Recommendation confidence: strong.
- Score confidence: medium-high.
- Remaining uncertainty: exact original Browser/BrowserThread constructor or helper spelling, exact `RectBounds`/client-size global names, `m_stateFlags` bit semantics, and final Browser source split.

## Validator Results
- Commands run from `source-3/project-documentation`:
  - `python .\tools\validator.py --mode file --file by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md --apply --queue-timeout 240 --wait-generated`
    - `command_id: 000000005619`
    - `command_timestamp: 2026-07-03T16:38:34-04:00`
    - Exit code: `0`
    - `ok: 1`
    - Generated refresh: `completed`; `auto-generated/NexusTK/browser/Browser.cpp` updated.
  - `python .\tools\validator.py --mode file --file by-class/BrowserControlPaneOld.md --apply --queue-timeout 240`
    - `command_id: 000000005620`
    - `command_timestamp: 2026-07-03T16:38:45-04:00`
    - Exit code: `0`
    - `ok: 1`
  - `python .\tools\validator.py --mode file --file by-class/Browser.md --apply --queue-timeout 240`
    - `command_id: 000000005621`
    - `command_timestamp: 2026-07-03T16:38:47-04:00`
    - Exit code: `0`
    - `ok: 1`
  - `python .\tools\validator.py --mode file --file by-class/BrowserThread.md --apply --queue-timeout 240`
    - `command_id: 000000005622`
    - `command_timestamp: 2026-07-03T16:38:49-04:00`
    - Exit code: `0`
    - `ok: 1`
  - `python .\tools\validator.py --mode file --file by-file/Browser.md --apply --queue-timeout 240`
    - `command_id: 000000005623`
    - `command_timestamp: 2026-07-03T16:38:51-04:00`
    - Exit code: `0`
    - `ok: 1`
- Generated Browser.cpp freshness proof: after queue status command `000000005625` reported zero queued/processing jobs and zero queued/processing generated refresh jobs, `auto-generated/NexusTK/browser/Browser.cpp` header read `validator-command-id: 000000005623`, `validator-refreshed-at: 2026-07-03T16:38:51-04:00`, and contains `Browser(const wchar_t *initialUrl);` plus UID0002P3 `BrowserControlPaneOld::BrowserControlPaneOld(...)` at generated lines 565-566.
- Unresolved validator warnings/errors: none target-specific. `by-file/Browser.md` validator still reports existing `missing_ref_uid` diagnostics for older Browser file references; these predate this callback and the command exited `0` with `ok: 1`. Target validator reported known generated/autogen notices such as child marker and no-code rows while exiting `0`.

## Changed Files
- Modified in place: `tools/leaser/Agents/Agent-B009/research/0002P3-BrowserControlPaneOldConstructor-source-quality.md`.
- Modified by B009 callback:
  - `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md`
  - `by-class/BrowserControlPaneOld.md`
  - `by-class/Browser.md`
  - `by-class/BrowserThread.md`
  - `by-file/Browser.md`
- Already present at same-or-greater detail, no edit needed:
  - `by-class/MSGHandler.md`
  - `by-file/MSGHandler.md`
  - `by-file/ControlPane.md`
  - `by-memory/0x004949e0-0x00494a81.ControlPaneConstructor.md`
- Validator-owned generated side effects observed:
  - `auto-generated/NexusTK/browser/Browser.cpp`
  - generated coverage/metadata/research tracker/projected stats outputs reported by validator command `000000005619` and later generated refresh.
- Renamed: none.
- Lease status: B009 leased the five edited by-* docs for the callback. Release attempt after edits returned `Rejected[No active lease]` for each leased path, and the active lease report showed no remaining B009 leases.
- Report execution: not run. B agents must never run `tools/validator.py execute_report ... --apply`, dry-run variants, registry lifecycle commands, manual report moves, or archive commands.

## Implementation Tracking Checklist
- [x] Supervisor validation required before implementation: Gate 1 accepted by supervisor for SHA `1A29255C6F69CD71C541A0E92590613DEF638DB47C9D0C56C685C31E0F4481E8`.
- [x] Target/support docs to update: edited target `by-memory/0x0046ff50-0x00470159.BrowserControlPaneOldConstructor.md`; edited support `by-class/BrowserControlPaneOld.md`, `by-class/Browser.md`, `by-class/BrowserThread.md`, and `by-file/Browser.md`; marked `by-class/MSGHandler.md`, `by-file/MSGHandler.md`, `by-file/ControlPane.md`, and [UID:0002PB] ControlPane constructor already-present at same-or-greater detail.
- [x] Current target state and actual evidence checked recorded: target and support docs now incorporate MCP session `b010_00032w_20260703` facts for range, caller, Browser layout/URL copy, BrowserThread setup, no UDTs, support callees, and null-bounds quirk.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: C1-C7 are `applied`.
- [x] Metadata/score changes to apply: UID0002P3 set to `COMPLETION:90`, `CONFIDENCE:92`; owner/emitter/reconstructable unchanged.
- [x] Score-limiting blockers researched to resolution, implementation-ready repair/split recommendation, or exact evidence-backed no-improvement proof: stale field-name blocker replaced with Browser and BrowserThread support declarations plus formal first-draft constructor C++.
- [x] Owner/emitter/reconstructable changes to apply: none; [UID:000016] owner/emitter and reconstructable true preserved.
- [x] Split/rename/new-child changes to apply: none.
- [x] Source-placement, range/split/padding/reclassification, or IDA rename/type/comment changes to apply or confirm not applicable: range and Browser source root preserved; no IDA DB rename/type/comment edits requested.
- [x] First-draft C++ or no-code proof to apply: formal first-draft constructor body inserted into UID0002P3; support declarations added/synced.
- [x] Third-party import directive to apply or confirm not applicable: not applicable.
- [x] Exact target/support doc facts to incorporate at report-level detail: constructor range/caller/callees, Browser fields, URL ownership, BrowserThread fields/global handoff, source-placement, support declarations, first-draft C++ body, and score rationale incorporated.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: stale "Browser field names incomplete" superseded; BrowserWindow/Thread-file ownership, raw vtable/SEH source, arbitrary target-local constructor inventions, and direct private-field access without support policy rejected in target/support docs.
- [x] Wave2/Wave3 mentions or artifacts encountered and ignored/rejected as stale, if applicable: generated/Wave rows remain leads only; generated docs were refreshed by validator, not edited by hand.
- [x] Open questions to close or document as evidence-backed unresolved: range/caller/layout/field-name/source-shape questions closed; exact helper spelling, `m_stateFlags` bit semantics, null-bounds source quirk, and source split remain bounded unresolved items.
- [x] Validators to run: scoped validators run for each changed by-* file with command IDs `000000005619`-`000000005623`, all exit `0`, all `ok: 1`.
- [x] Generated report refresh expected or explicit manual supervisor-owned coverage/tracker text to apply: generated Browser.cpp refreshed through validator; no manual coverage/tracker text required.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B009","command_id":"000000005666","destination_path":"executed-b-agent-research/B009/0002P3-BrowserControlPaneOldConstructor-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B009/research/0002P3-BrowserControlPaneOldConstructor-source-quality.md","timestamp":"2026-07-03T16:52:22-04:00","uid":"0002P3"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
