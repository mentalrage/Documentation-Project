** TARGET-REPORT-UID:0000ZG **
** AUTHOR-AGENT-ID:B009 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# UID0000ZG AutoInit Scalar Deleting Destructor Source-Quality Report


## Finalized Report / Current Recommendation

- Current recommendation: keep [UID:0000ZG] `by-memory/0x00470300-0x0047032a.AutoInit.md` assigned to [UID:0000HN] `by-file/AutoInit.md`, keep `RECONSTRUCTABLE:TRUE`, keep `EMITTER_UIDS:0000HN`, and keep formal target C++ blank.
- Final disposition: the target is a well-bounded MSVC scalar deleting destructor wrapper for the same AutoInit/OLE teardown policy documented by the ordinary destructor [UID:0000ZE], startup helper [UID:0000W8], static shutdown thunk [UID:0001O6], static object slot [UID:000277], and vtable [UID:0001X0]. It needs B-report coverage, not a source-owner or metadata rewrite.
- Required action: supervisor may validate this report as source-quality coverage for UID0000ZG. No report-only by-* edits are recommended before supervisor acceptance.
- Confidence: high for behavior, range, owner/emitter route, and no-wrapper-C++ recommendation; medium-high for final historical source-file spelling because `AutoInit.cpp` versus a later `PlatformApi.cpp` fold remains inferred.

## Supporting Research

This is a report-only B-agent pass for UID0000ZG. I did not edit target/support by-* docs, generated files, coverage files, validator state, lifecycle/archive files, or supervisor ledgers. I did not run validators, leases, `execute_report`, dry-run/probing lifecycle commands, registry lifecycle commands, or manual report moves.

The resumed MCP evidence pass used active IDA MCP session `supervisor_recovery_20260705` after the supervisor restored MCP. `server_health` returned `status:ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`. `idb_list` returned one active adopted worker session, not analyzing.

The earlier UID0000ZG pause in `tools/leaser/Agents/Agent-B009/notes.md` is historical incident context only. It is no longer the current state of this report.

## Target

- Target UID: `0000ZG`.
- Target path: `by-memory/0x00470300-0x0047032a.AutoInit.md`.
- Source queue/report row: `auto-generated/-ag-research-tracker.md` row for UID0000ZG in the by-memory report queue, currently `85/90`, reconstructable `true`, B-report count `0`.
- Current supervisor classification: report-only source-quality research for a reconstructable scalar deleting destructor with no prior UID0000ZG B report.
- Current scores and parent state: target `85/90`, canonical owner [UID:0000HN] `AutoInit`, reconstructable `TRUE`, emitter [UID:0000HN], blank formal C++; parent [UID:0000HN] `AutoInit` is `85/87`, and class [UID:00000Q] `AutoInit` is `85/89`.

## Current Target State

- Existing metadata: `COMPLETION:85`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000HN`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000HN`, blank `EMITTER_POSITION_OPTIONAL`, blank formal `RECONSTRUCTION_CPP CODE`.
- Existing owner/emitter/reconstructable state: correct. The scalar deleting destructor is part of the AutoInit source policy, even though the emitted wrapper itself should be regenerated by the compiler from a source-level destructor/static object model.
- Existing C++/emitter state: formal target C++ is blank and should remain blank. The source-facing behavior belongs to the ordinary destructor/source class model, not to a handwritten `scalar_deleting_destructor` body.
- Existing open questions, blockers, placeholder names, stale assumptions, or contradictory claims: no target-blocking ownership issue remains. The only durable caveat is final historical source placement and object spelling: `NexusTK/platform/AutoInit.cpp` versus a possible [UID:0000ML] `PlatformApi` fold.
- Related target/support docs checked: `by-file/AutoInit.md`, `by-class/AutoInit.md`, `by-global/InitializeOleSupport.md`, `by-memory/0x00419ee0-0x00419ef4.InitializeOleSupport.md`, `by-memory/0x0046efe0-0x0046efeb.AutoInitNonDeletingDestructor.md`, `by-memory/0x0060c0f0-0x0060c100.AutoInitStaticShutdownThunk.md`, `by-memory/0x0066d42c-0x0066d430.AutoInitStaticObjectVptr.md`, `by-type/by-vtable/AutoInitVtable.md`, `by-file/PlatformApi.md`, `by-file/Browser.md`, and `by-memory/0x0046f010-0x004710b8.BrowserOleLegacyAndHelpers.md`.
- Current artifact/lifecycle status: this file is the current active Agent-B009 research report at `tools/leaser/Agents/Agent-B009/research/0000ZG-AutoInit-source-quality.md`.

## Executive Recommendation

UID0000ZG should remain an exact AutoInit child and should not be split, renamed, demoted, reassigned, or populated with wrapper C++. The target's current by-* documentation already contains the required behavior, range, raw-code, owner, and no-wrapper-source facts at report-level detail. This report supplies the missing B coverage and current MCP-backed source-quality decision.

No remaining condition blocks the existing owner/emitter assignment. The only source-quality limitation is final source-file spelling, which affects final class/file C++ shape but not UID0000ZG's direct owner, emitter route, or blank target C++ decision.

## Supervisor Active Recheck

- Resume instruction: continue the same Agent-B009 UID0000ZG assignment using active MCP session `supervisor_recovery_20260705`; do not start a new task; do not edit by-* docs or lifecycle/generator state in report-only mode.
- Split repair: not required. MCP `lookup_funcs` confirms `0x00470300` is one exact function of size `0x2a` / 42 decimal (Verified with `int_convert.py`), `0x0047032a` is not a function start, and successor `0x00470330` is a separate function.
- Source-bearing child state: UID0000ZG is already exact, gate-cleared, and parent-attached. Sibling startup/shutdown/static-data pages are already documented and do not require report-only edits.

## Inference Research Guidance Check

The inference discipline matters here because a decompiler-visible scalar deleting destructor can be behaviorally clear while still being the wrong source artifact to hand-write. I treated target/support docs and generated source as leads, then rechecked the key claims with current MCP.

Direct IDA facts: exact function bounds, raw bytes, vtable slot, xrefs, callees, startup-table route, static slot, and `OleInitialize`/`OleUninitialize` import use.

Documentation evidence: current AutoInit docs already describe the source-policy owner, ordinary destructor, scalar wrapper, startup helper, shutdown thunk, vtable, and static object slot.

Inference: source should express a tiny file-local/static AutoInit or equivalent platform/OLE lifetime object; the compiler should emit the scalar deleting wrapper, vtable/static data, and startup/shutdown glue.

No Wave2/Wave3 evidence was used as proof. `by-type/by-vtable/AutoInitVtable.md` still records an old Wave3 `vtable_count:0` issue as stale generated metadata, but current IDA and by-* docs supersede it.

## Heuristic / Inference Reanalysis And Validation

Source-quality issue 1: direct owner and emitter route.

- Best inference: [UID:0000HN] `AutoInit` remains the correct source-policy owner and emitter route for UID0000ZG.
- Validation: current MCP xrefs tie `0x0061373c` to the ordinary destructor at `0x0046efe0`, the target's vtable store at `0x00470306`, the shutdown thunk at `0x0060c0f0`, and static slot `0x0066d42c`. The target's only xref-to is the vtable slot at `0x0061373c`.
- Rejected alternatives: Browser ownership is rejected because process-wide startup/shutdown OLE evidence exists outside browser instance routing; PlatformApi is only a possible later fold target; no-owner is too weak because the AutoInit chain is coherent and gate-cleared.

Source-quality issue 2: formal target C++.

- Best inference: formal C++ stays blank for UID0000ZG. The source-facing destructor semantics should be represented through the ordinary destructor/class/static object model, not a handwritten scalar deleting wrapper.
- Validation: current MCP decompiles `sub_470300` as vtable store, `OleUninitialize`, optional delete helper when `(flags & 1) != 0`, and return `this`. Disassembly shows the delete path pushes `4`, i.e. object size `0x4` / 4 decimal (Verified with `int_convert.py`), then calls `sub_5C7526`.
- Rejected alternatives: a hand-authored `AutoInit::scalar_deleting_destructor(unsigned int flags)` is rejected as ABI glue; a source `operator delete` body on this target is rejected because the delete helper is shared runtime/delete support, not AutoInit-specific source.

Source-quality issue 3: startup/shutdown relation.

- Best inference: UID0000ZG is one teardown entry point in a static OLE lifetime policy whose startup side is UID0000W8 and shutdown glue is UID0001O6.
- Validation: current MCP `analyze_function 0x00419ee0` reports `OleInitialize(0); return atexit(sub_60C0F0);` with no callers and data xref `0x0060d6a4`. `analyze_function 0x0060c0f0` writes `off_66D42C = AutoInit vftable` then `OleUninitialize`. `xrefs_to 0x0060d650` reports `OleUninitialize` refs from the ordinary destructor, target scalar wrapper, and shutdown thunk paths.
- Rejected alternatives: separate application-level OLE teardown systems are rejected; these paths are alternate ABI/static-lifetime manifestations of one AutoInit policy.

Source-quality issue 4: score/metadata movement.

- Best inference: keep target `85/90` and support docs unchanged. Current docs already carry the relevant facts; a higher score would require final historical declaration/file spelling, not more wrapper evidence.
- Validation: target/support docs already preserve exact range, raw body, vtable/static object evidence, owner route, rejected Browser/PlatformApi caveats, and blank-C++ policy. Generated `auto-generated/NexusTK/platform/AutoInit.cpp` currently contains a UID0000ZG empty marker, which is expected for this wrapper until source-level AutoInit class/file C++ is finalized.
- Rejected alternatives: score bump solely because this B report exists is rejected; report execution should count B coverage, not inflate by-* completion beyond unresolved final-source spelling.

## Evidence Standards Used

I used exact-address MCP calls first: `server_health`, `idb_list`, `lookup_funcs`, `analyze_function`, `decompile`, `disasm`, `xrefs_to`, `callees`, `get_int`, and `get_bytes`, all bounded and target-specific. I then compared those facts against current by-* docs, generated AutoInit source state, and matching executed B reports.

The evidence is strong enough for owner/emitter/no-wrapper-C++ disposition because the target's function body, vtable slot, startup/shutdown relation, static slot, and OLE import refs agree across current MCP, current docs, and prior accepted reports.

Confidence is held below final-source quality because no original source metadata proves the exact filename, static object spelling, or whether the helper lived in standalone `AutoInit.cpp` versus a platform/OLE source fold.

## Evidence Checked

IDA MCP checks from session `supervisor_recovery_20260705`:

- `server_health`: `status:ok`, module `NexusTK.exe`, imagebase `0x400000`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, Hex-Rays and strings ready.
- `idb_list`: one active session, `supervisor_recovery_20260705`, `is_analyzing:false`, worker pid `19604`.
- `lookup_funcs` for `0x00470300`, `0x0047032a`, `0x00470330`, `0x0046efe0`, `0x00419ee0`, `0x0060c0f0`: target `sub_470300` size `0x2a` / 42 decimal (Verified with `int_convert.py`); `0x0047032a` not a function; successor `sub_470330` size `0xd6`; ordinary destructor `sub_46EFE0` size `0x0c` / 12 decimal (Verified with `int_convert.py`); startup helper `sub_419EE0` size `0x14` / 20 decimal (Verified with `int_convert.py`); shutdown thunk `sub_60C0F0` size `0x10` / 16 decimal (Verified with `int_convert.py`).
- `analyze_function 0x00470300`: prototype `_DWORD *__thiscall(_DWORD *Block, char)`, 3 basic blocks, callees `OleUninitialize` and `sub_5C7526`, no callers, xref-to from `0x0061373c`.
- `decompile 0x00470300`: stores AutoInit vtable, calls `OleUninitialize`, conditionally calls `sub_5C7526(Block)` when `(a2 & 1) != 0`, returns `Block`.
- `disasm 0x00470300`: 16 instructions; vtable store at `0x00470306`, call through `OleUninitialize` at `0x0047030c`, flag test at `0x00470312`, optional `push 4` / `push esi` / `call sub_5C7526`, return `retn 4`.
- `callees 0x00470300`: `OleUninitialize` at `0x0060d650` and `sub_5C7526` at `0x005c7526`.
- `xrefs_to` for `0x00470300`, `0x0061373c`, `0x0066d42c`, `0x0060c0f0`, `0x0046efe0`, `0x00419ee0`: target has one data xref from `0x0061373c`; vtable pointer has refs from `0x0046efe0`, `0x00470306`, `0x0060c0f0`, and `0x0066d42c`; static slot has one xref from `0x0060c0f0`; shutdown thunk has one xref from `0x00419ee8`; ordinary destructor has no xrefs to the function start; startup helper has one data xref from `0x0060d6a4`.
- `get_int`: `0x0061373c` value converts to `0x00470300` (Verified with `int_convert.py` from decimal `4653824`); `0x0066d42c` value converts to `0x0061373c` (Verified with `int_convert.py` from decimal `6371132`); startup table values at `0x0060d6a0`, `0x0060d6a4`, and `0x0060d6a8` convert to `0x00419ec0`, `0x00419ee0`, and `0x00419f00` (Verified with `int_convert.py`).
- `get_bytes`: target bytes include function body and six `0xcc` bytes from `0x0047032a`; vtable neighborhood bytes are `4c 31 64 00 00 03 47 00 5c 32 64 00`; static slot bytes are `3c 37 61 00`; startup table bytes are `c0 9e 41 00 e0 9e 41 00 00 9f 41 00`.
- Related `analyze_function`/`disasm` for `0x00419ee0`, `0x0046efe0`, and `0x0060c0f0`: current session reconfirmed startup `OleInitialize(0); atexit(sub_60C0F0)`, ordinary destructor vtable store plus `OleUninitialize`, and shutdown thunk static-slot write plus `OleUninitialize`.
- `xrefs_to 0x0060d650` and `0x0060d668`: `OleUninitialize` refs come from the scalar wrapper, ordinary destructor, and shutdown thunk; `OleInitialize` refs come from startup helper `0x00419ee2`.

Local docs and generated context checked:

- Target/support docs listed in `Current Target State`.
- `auto-generated/-ag-research-tracker.md` and `auto-generated/-ag-coverage-report-by-memory.md` for current UID0000ZG row and B-report count.
- `auto-generated/NexusTK/platform/AutoInit.cpp` read-only header: validator command `000000006679`, refreshed `2026-07-05T02:31:48-04:00`, contains empty markers for class/vtable/ordinary destructor/UID0000ZG/static object; UID0000W8 marker is absent after B013's executed reclassification.

Old report searches:

- Search terms used: `0000ZG`, `0x00470300`, `0x0047032a`, `AutoInit`, `OleUninitialize`, `scalar deleting destructor`, `InitializeOleSupport`, `AutoInitStaticShutdownThunk`, and source-family terms around AutoInit/OLE startup/shutdown.
- Relevant reports opened: `executed-b-agent-research/B001/0000ZF-BrowserOleLegacyAndHelpers.md` and `executed-b-agent-research/B013/0000W8-InitializeOleSupport-source-quality.md`.
- Broad generic scalar-destructor hits were intentionally not opened because they were not target/source-family matches.

Failed, unavailable, or intentionally skipped checks:

- No MCP unavailability occurred after supervisor restoration. A malformed local PowerShell wrapper batch sent invalid `arguments` before the corrected calls; those rejected calls were discarded and are not used as evidence.
- No validators were run because this is report-only and no by-* edits were made.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- |
| ZG-C01 | UID0000ZG is exact function `0x00470300-0x0047032a`, size `0x2a` / 42 decimal (Verified with `int_convert.py`), with `0x0047032a-0x00470330` padding and successor function at `0x00470330`. | 0.99 | Current MCP `lookup_funcs`, `get_bytes`, `disasm`. | Target `Scope`, `Evidence`, `Raw Code Evidence`; aggregate Browser/OLE child inventory for successor. | already-present | already-present: target records range, size, and padding; `BrowserOleLegacyAndHelpers` records successor `0x00470330`; no edit required |
| ZG-C02 | The body stores the AutoInit vtable, calls `OleUninitialize`, tests deletion flag bit 0, optionally calls delete helper `sub_5C7526`, and returns `this`. | 0.99 | Current MCP `analyze_function`, `decompile`, `disasm`, `callees`. | Target `Behavior`, `Raw Code Evidence`, `C++ Reconstruction Candidate`. | already-present | already-present: target behavior/evidence/raw-code/C++ sections record the full body shape; no edit required |
| ZG-C03 | The optional delete path pushes object size `0x4` / 4 decimal (Verified with `int_convert.py`), matching a vptr-only static helper object and scalar deleting destructor wrapper. | 0.97 | Current MCP `disasm 0x00470300`; static object doc. | Target `Evidence`, `Reconstruction Notes`; static object doc. | already-present | already-present: target records size-4 delete and vptr-only layout; static-object doc records four-byte vptr storage; no edit required |
| ZG-C04 | The only xref-to the target entry is the AutoInit vtable slot at `0x0061373c`; there are no ordinary direct code callers to `0x00470300`. | 0.98 | Current MCP `xrefs_to 0x00470300`, `analyze_function` callers `[]`. | Target `Evidence`, `Reconstruction Notes`; AutoInit vtable doc. | already-present | already-present: target and vtable docs record vtable-only entry xref/no ordinary direct callers; no edit required |
| ZG-C05 | Vtable `0x0061373c` is referenced from ordinary destructor `0x0046efe0`, target vtable store `0x00470306`, shutdown thunk `0x0060c0f0`, and static object slot `0x0066d42c`. | 0.98 | Current MCP `xrefs_to 0x0061373c`, `get_int`, `get_bytes`. | Target/support AutoInit docs. | already-present | already-present: target, ordinary-destructor, vtable, shutdown-thunk, static-slot, class, and file docs record the complete xref set; no edit required |
| ZG-C06 | Startup helper `0x00419ee0` calls `OleInitialize(0)` and registers `0x0060c0f0` through `_atexit`; it has no ordinary callers and is startup-table-only from `0x0060d6a4`. | 0.97 | Current MCP `analyze_function`, `disasm`, `xrefs_to`; B013 executed report. | Support AutoInit/InitializeOleSupport docs. | already-present | already-present: InitializeOleSupport memory/global and AutoInit class/file docs record `OleInitialize(0)`, `_atexit(0x0060c0f0)`, no ordinary callers, and startup table `0x0060d6a4`; no edit required |
| ZG-C07 | Shutdown thunk `0x0060c0f0` writes static slot `0x0066d42c = 0x0061373c` and jumps to `OleUninitialize`; it is static-lifetime glue, not standalone source. | 0.98 | Current MCP `analyze_function`, `disasm`, `xrefs_to`, `get_bytes`; shutdown thunk doc. | Support shutdown/static-slot docs. | already-present | already-present: shutdown-thunk/static-slot docs record the write, tail jump, static-lifetime glue disposition, and non-standalone source status; no edit required |
| ZG-C08 | The correct source model is an AutoInit/static OLE lifetime policy; UID0000ZG should not emit handwritten scalar deleting destructor C++. | 0.93 | Current MCP body/xrefs; B013 source-route decision; current AutoInit class/file docs; generated AutoInit.cpp empty marker state. | Target C++ block and AutoInit class/file docs. | already-present | already-present: target C++ section and AutoInit class/file docs record static OLE lifetime policy, blank final C++, and no handwritten scalar-wrapper emission; no edit required |
| ZG-C09 | Browser ownership is rejected. UID0000ZG sits inside the old Browser/OLE address island, but current and prior evidence classify it as process-wide AutoInit support. | 0.94 | B001 `0000ZF` report; Browser docs; current MCP startup/vtable evidence. | Target `Reconstruction Notes`, Browser/AutoInit docs. | already-present | already-present: Browser, BrowserOle aggregate, AutoInit, and PlatformApi docs classify the item as process-wide AutoInit/OLE support rather than Browser-owned code; no edit required |
| ZG-C10 | No by-* score or metadata change is recommended in this report-only pass. Existing docs are already at same-or-greater detail; B-report execution should address coverage count. | 0.88 | Current target/support docs, tracker B-report count, generated source state. | Supervisor report lifecycle, target metadata. | not-applicable | excluded-with-reason: no by-* implementation item; accepted disposition keeps UID0000ZG metadata/C++ unchanged and leaves coverage lifecycle to supervisor execution |

## Positive Evidence Summary

- Current MCP reconfirms exact target bounds and body: `sub_470300` is `0x00470300-0x0047032a`, size `0x2a` / 42 decimal (Verified with `int_convert.py`), with a vtable store, `OleUninitialize`, delete-flag branch, optional delete helper, and `retn 4`.
- The target is anchored by the one-slot AutoInit vtable: `0x0061373c -> 0x00470300`, and the same vtable value is used by the ordinary destructor, target scalar wrapper, shutdown thunk, and static object slot.
- Startup/shutdown evidence is coherent: `0x00419ee0` initializes OLE and registers `0x0060c0f0`; `0x0060c0f0` writes `off_66D42C` and calls `OleUninitialize`; UID0000ZG is the virtual/delete wrapper around the same teardown semantics.
- Current support docs and executed B013 report already resolved UID0000W8 as non-emitting startup glue while preserving AutoInit source-policy ownership.
- Executed B001 mixed-range report already rejected assigning the broader Browser/OLE island to Browser because UID0000ZG is a process-wide AutoInit child.

## IDA MCP Facts

- Function/range facts: `lookup_funcs` reports `0x00470300` as `sub_470300`, size `0x2a`; `0x0047032a` is not a function; `0x00470330` is successor `sub_470330`.
- Target decompile facts: `_DWORD *__thiscall sub_470300(_DWORD *Block, char a2)` stores AutoInit vtable, calls `OleUninitialize`, conditionally calls `sub_5C7526(Block)`, and returns `Block`.
- Target disassembly facts: vtable store at `0x00470306`, `call ds:OleUninitialize` at `0x0047030c`, flag test at `0x00470312`, optional `push 4` and delete helper call at `0x0047031b`, return at `0x00470327`.
- Data/table/padding facts: target bytes are followed by `0xcc` padding from `0x0047032a`; vtable neighborhood `0x00613738-0x00613743` contains RTTI pointer, slot pointer, and adjacent next RTTI; static slot `0x0066d42c` contains `3c 37 61 00`.
- Xref facts: target has one data xref from vtable slot `0x0061373c`; vtable pointer has refs from `0x0046efe0`, `0x00470306`, `0x0060c0f0`, and `0x0066d42c`; ordinary destructor start has no direct xrefs.
- OLE import facts: `xrefs_to 0x0060d650` reports `OleUninitialize` refs from target, ordinary destructor, and shutdown thunk; `xrefs_to 0x0060d668` reports `OleInitialize` refs from startup helper.
- Negative IDA facts: no code caller proves a source-call route to the scalar deleting destructor; no extra AutoInit vtable slot exists between RTTI at `0x00613738`, slot `0x0061373c`, and adjacent next data at `0x00613740`.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x00419ee0-0x00419ef4` | [UID:0000W8] `by-memory/0x00419ee0-0x00419ef4.InitializeOleSupport.md` | startup-table OLE initializer glue | false | source-policy owner [UID:0000HN] | `89/92` | executed B013 report says non-emitting startup glue |
| `0x0046efe0-0x0046efec` | [UID:0000ZE] `by-memory/0x0046efe0-0x0046efeb.AutoInitNonDeletingDestructor.md` | ordinary AutoInit destructor body | true | [UID:0000HN] | `90/92` | source-facing teardown body; blank formal C++ pending final source spelling |
| `0x00470300-0x0047032a` | [UID:0000ZG] `by-memory/0x00470300-0x0047032a.AutoInit.md` | scalar deleting destructor wrapper | true | [UID:0000HN] | `85/90` | current target; keep metadata and blank C++ |
| `0x0060c0f0-0x0060c100` | [UID:0001O6] `by-memory/0x0060c0f0-0x0060c100.AutoInitStaticShutdownThunk.md` | static cleanup thunk | false | none; source policy via AutoInit | `86/91` | non-emitting compiler/static-lifetime glue |
| `0x0061373c` | [UID:0001X0] `by-type/by-vtable/AutoInitVtable.md` | one-slot AutoInit vtable | true generated-binary | [UID:00000Q] | `86/90` | source-declared/generated-binary evidence |
| `0x0066d42c-0x0066d430` | [UID:000277] `by-memory/0x0066d42c-0x0066d430.AutoInitStaticObjectVptr.md` | static object vptr slot | true generated-binary | [UID:0000HN] | `86/92` | source-declared/generated-binary static object storage |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0061373c -> 0x00470300` | data xref to target entry | Vtable slot is the only inbound target xref; confirms virtual/scalar wrapper role. |
| `0x00470306 -> 0x0061373c` | target vtable store | Target reinstalls the AutoInit vtable before teardown. |
| `0x0047030c -> 0x0060d650` | call to `OleUninitialize` | Target performs OLE teardown. |
| `0x0047031b -> 0x005c7526` | call to delete helper after `push 4` | Optional scalar-delete branch, not AutoInit-specific handwritten source. |
| `0x0046efe0 -> 0x0061373c` | ordinary destructor vtable store | Ordinary destructor shares the same teardown policy. |
| `0x0060c0f0 -> 0x0061373c` and `0x0066d42c` | shutdown thunk writes static object vptr | Static cleanup glue belongs to the same AutoInit policy. |
| `0x00419ee8 -> 0x0060c0f0` | startup helper registers thunk | OLE startup and shutdown are paired through `_atexit`. |
| `0x0060d6a4 -> 0x00419ee0` | startup initializer table entry | Startup helper is table-only, not source-called. |

## Documentation Evidence And IDA Status

- Existing docs that support the conclusion: target doc already records exact behavior, raw code, vtable ownership, optional size-4 delete, startup/shutdown pairing, post-function padding, and AutoInit parent assignment. AutoInit file/class docs preserve the platform/OLE source-policy owner and final file-fold caveat.
- Existing docs that are stale, incomplete, or contradicted: no target/support doc contradiction was found for UID0000ZG. The target lacks a B report but not core technical evidence. `PlatformApi.md` remains a plausible fold context, not a direct owner override.
- Generated/coverage report state: `auto-generated/NexusTK/platform/AutoInit.cpp` contains a UID0000ZG empty marker under [UID:0000HN] `AutoInit`. This is consistent with the recommendation that formal wrapper C++ stays blank until the source-level AutoInit class/file declaration is ready.

## Ranked Ownership Analysis

### 1. [UID:0000HN] AutoInit file / [UID:00000Q] AutoInit class

- Evidence for: target vtable slot is named AutoInit; current MCP xrefs tie target, ordinary destructor, shutdown thunk, and static object slot to the same `0x0061373c` vtable; startup helper and shutdown thunk implement one OLE lifetime policy; current docs already gate-clear file/class parent.
- Evidence against: final historical filename and source-facing static object spelling are inferred, not source-proven.
- Decision: keep as direct owner/emitter route. The caveat affects final source spelling, not ownership.

### 2. [UID:0000ML] PlatformApi fold candidate

- Evidence for: `PlatformApi.md` describes platform/Windows compatibility support and explicitly includes AutoInit/OLE lifetime as a possible fold.
- Evidence against: direct vtable/static object/destructor docs are modeled under AutoInit, and no current proof shows PlatformApi as the exact source file for this wrapper.
- Decision: keep as a possible later source-file fold only; do not replace AutoInit as UID0000ZG owner.

### 3. [UID:0000HV] Browser / Browser OLE island

- Evidence for: UID0000ZG lies inside the historic `0x0046f010-0x004710b8` Browser/OLE address island, and Browser code is the largest visible COM consumer.
- Evidence against: B001's executed `0000ZF` split report explicitly separates UID0000ZG as process-wide AutoInit support; current MCP proves startup/static-lifetime evidence rather than Browser instance reachability.
- Decision: rejected for UID0000ZG ownership.

### Proposed new file/grouping, if applicable

- Proposed owner/name/path: no new owner is recommended.
- Likely full contents: existing AutoInit source-policy group already covers startup helper, ordinary destructor, scalar deleting destructor, shutdown thunk, vtable, and static object slot.
- Candidate related items that belong: UID0000W8, UID0000ZE, UID0000ZG, UID0001O6, UID0001X0, UID000277.
- Candidate related items rejected: Browser scalar destructors and Browser COM helpers adjacent in memory; they belong to Browser-specific owners, not AutoInit.
- Standalone, narrow, or broad source-file inference: narrow platform/OLE static lifetime helper, currently `NexusTK/platform/AutoInit.cpp` with possible later PlatformApi fold.

## Source Placement

Recommended source placement remains `NexusTK/platform/AutoInit.cpp` under [UID:0000HN], with [UID:0000ML] `PlatformApi` retained only as a possible later fold target. The likely source shape is a tiny file-local/static OLE lifetime object whose construction initializes OLE and whose teardown uninitializes it.

This placement fits the subsystem context because current IDA proves process startup table initialization and static cleanup, not Browser object lifetime. Browser ownership remains rejected even though Browser code consumes COM/OLE APIs.

Remaining placement uncertainty: exact original source filename and object/class spelling are not source-proven. That uncertainty justifies keeping by-* scores below final-audit quality and keeping target formal C++ blank.

## Range / Split / Padding / Reclassification Analysis

- Exact range/boundary facts: current MCP `lookup_funcs` confirms `0x00470300-0x0047032a`; `0x0047032a` is not a function; successor `0x00470330` starts the next function.
- Padding facts: `get_bytes` reports six `0xcc` bytes at `0x0047032a-0x00470330`.
- Children/subranges: no split or new child is required. The target is one scalar deleting destructor wrapper.
- Reclassification: keep `RECONSTRUCTABLE:TRUE` because the wrapper is reconstructable evidence for the source-level AutoInit destructor/static object, but keep formal target C++ blank because the wrapper itself is compiler ABI output.
- Parent/container impact: B001's `0000ZF` Browser/OLE aggregate split remains correct; UID0000ZG is the non-Browser AutoInit child inside that old mixed island.

## Negative Evidence Summary

- No direct code caller reaches `0x00470300`; only the vtable slot references the target. That supports virtual/scalar wrapper treatment rather than a callable source helper.
- `OleUninitialize` appears in ordinary destructor, scalar wrapper, and shutdown thunk paths; this repetition does not prove three separate handwritten OLE teardown systems.
- The optional delete branch does not prove a source-authored AutoInit `operator delete`; it is scalar deleting destructor ABI glue around a vptr-only object.
- Browser COM usage and address adjacency do not prove Browser ownership. Current startup table/static object/vtable evidence points to process-wide AutoInit.
- A generated empty marker in `AutoInit.cpp` does not require hand-authored wrapper C++; it reflects unresolved final source declaration/code emission for the owning AutoInit group.

## IDA Rename / Type / Comment Recommendations

- Proposed source-facing names: keep documentation names `AutoInit_ScalarDeletingDestructor` / `AutoInit scalar deleting destructor` for the target, `AutoInit_NonDeletingDestructor` for the ordinary destructor, `InitializeOleSupport` for the startup helper index, and `AutoInit_StaticShutdownThunk` for the cleanup thunk.
- Evidence for names: current MCP names are raw `sub_470300`, `sub_46EFE0`, `sub_419EE0`, and `sub_60C0F0`, but data names identify the anonymous AutoInit vtable and current by-* docs consistently use the source-facing descriptive names.
- Items intentionally left unchanged: no IDA rename/type/comment edits are requested in this report-only pass.
- Whether IDA DB edits are safe, unsafe, or not requested: not requested.

## First-Draft C++ Recommendation

- Eligible for draft C++: not on UID0000ZG as a standalone function body.
- Recommended code: keep UID0000ZG formal `RECONSTRUCTION_CPP CODE` blank.
- Reason it preserves exact original behavior: the rebuilt source should express the ordinary AutoInit destructor/static OLE lifetime policy and allow the MSVC-compatible toolchain or reconstruction ABI layer to emit the scalar deleting wrapper, optional delete branch, and vtable slot.
- Reason it matches plausible original source shape: a late-1990s/early-2000s Visual C++ project would normally contain a small RAII/static helper destructor, not a handwritten method named `scalar_deleting_destructor` with a delete flag.
- Inferred source-facing names/types/fields: file-local/static `AutoInit` or equivalent OLE lifetime guard; one vptr-sized object represented by source declarations rather than raw address storage.
- Naming/coding style convention used: documentation keeps descriptive AutoInit names because no original symbol spelling is proven.
- Reason code should remain blank: UID0000ZG is compiler-generated scalar deleting destructor wrapper evidence, not the source-authored destructor body. Writing the wrapper into this target would duplicate ordinary destructor semantics and encode ABI mechanics as project source.
- Exact no-code proof: current MCP shows target entry is reached only through the vtable slot, reinstalls the vtable, calls `OleUninitialize`, tests the scalar-delete flag, optionally pushes object size `4` and calls a delete helper, then returns `this`. That is the standard scalar deleting wrapper shape for a vptr-only object.

## Final Recommendation

- Exact changes recommended: no target/support by-* text changes are required before supervisor validation. Treat current docs as already present at report-level detail.
- Exact parent assignments recommended: keep UID0000ZG owned/emitted by [UID:0000HN] `AutoInit`.
- Exact items left no-owner/non-emitting and why: sibling startup helper UID0000W8 and shutdown thunk UID0001O6 remain non-emitting glue per existing docs and B013; target UID0000ZG remains reconstructable but blank-C++ because it is ABI wrapper evidence for AutoInit source policy.
- Exact future work outside assignment scope: final source-layout pass may decide `AutoInit.cpp` versus `PlatformApi.cpp` fold and static object spelling. That is not a UID0000ZG blocker.

## Recommended Target Doc Changes

- Target path: `by-memory/0x00470300-0x0047032a.AutoInit.md`.
- Exact report facts to incorporate: none required; current target doc already records exact range, behavior, raw disassembly, vtable slot, `OleUninitialize`, optional size-4 delete, sibling startup/shutdown/ordinary-destructor relationship, static object slot, AutoInit owner, rejected low-level wrapper source policy, and padding.
- Metadata/score/owner/emitter/reconstructable/C++ changes: no change recommended. Keep `85/90`, `CANONICAL_OWNER:0000HN`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000HN`, and blank formal C++.
- Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: preserve that Browser ownership is rejected; preserve that wrapper C++ is not source-authored; preserve that final standalone file/object spelling remains below final-code confidence.

## Recommended Support Doc Changes

- Support path: `by-file/AutoInit.md`, `by-class/AutoInit.md`, `by-global/InitializeOleSupport.md`, `by-memory/0x00419ee0-0x00419ef4.InitializeOleSupport.md`, `by-memory/0x0046efe0-0x0046efeb.AutoInitNonDeletingDestructor.md`, `by-memory/0x0060c0f0-0x0060c100.AutoInitStaticShutdownThunk.md`, `by-memory/0x0066d42c-0x0066d430.AutoInitStaticObjectVptr.md`, `by-type/by-vtable/AutoInitVtable.md`, `by-file/Browser.md`, and `by-file/PlatformApi.md`.
- Exact report facts to incorporate: no support edits required. The current support docs already preserve the AutoInit source-policy owner, startup/shutdown/ordinary/scalar/static-slot/vtable relationship, non-emitting UID0000W8/UID0001O6 classification, Browser rejection, and PlatformApi fold caveat.
- Metadata/link/score/coverage/source-placement changes: no support metadata changes recommended in this report-only pass.

## Score And Metadata Recommendation

- Current score/metadata: target `85/90`, owner [UID:0000HN], reconstructable `TRUE`, emitter [UID:0000HN], blank formal C++.
- Recommended score/metadata: unchanged.
- Score rationale and reason not higher/lower: the target already clears the assignment gate and has high-confidence exact evidence. Do not raise completion solely for B-report coverage because the unresolved final source-file/static-object spelling and broader AutoInit final C++ declaration still cap this wrapper below final-audit quality. Do not lower because current MCP reconfirms all key behavior and ownership facts.
- Score-improvement attempt: checked target/support docs, current MCP function/xref/bytes/decompile/disasm/callee evidence, old B reports, generated AutoInit output, Browser/PlatformApi alternatives, and C++ disposition. The only remaining blocker is final source spelling/fold, which cannot be resolved safely from current evidence.
- Metadata fields to change or leave unchanged: leave all target and support metadata unchanged.

## Open Questions With Attempted Resolution

| Question | Investigation | Resolution / impact |
| --- | --- | --- |
| Is the current owner/emitter route through [UID:0000HN] AutoInit still correct? | Current MCP xrefs, target/support docs, B001/B013 reports, Browser/PlatformApi checks. | Yes. Keep owner/emitter [UID:0000HN]. |
| Should UID0000ZG emit formal C++? | Current MCP body, scalar-delete flag/delete helper shape, ordinary destructor relation, generated AutoInit source. | No. Keep formal C++ blank; source model should be ordinary destructor/static object, not raw wrapper. |
| Should the target become non-reconstructable like UID0000W8/UID0001O6? | Compared target role to startup/shutdown glue and generated-binary static data. | No. Keep reconstructable TRUE as source-level destructor evidence, but no standalone wrapper C++. |
| Should Browser own the target because of address adjacency? | Opened B001 `0000ZF` report and Browser docs; checked current startup/static evidence. | No. UID0000ZG remains process-wide AutoInit support. |
| Should PlatformApi replace AutoInit as direct owner? | Checked PlatformApi and AutoInit docs. | No. PlatformApi remains possible later fold target only. |
| Should scores change? | Checked current docs against current MCP and score blockers. | No. Report coverage is missing, not core target evidence. |

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

Not applicable. Do not manually edit generated tracker or coverage files. If the supervisor accepts this report, normal report execution should update validator-owned B-report coverage/tracker state.

## Follow-Up Actions

- Supervisor actions: Gate 1 validate this report; if accepted and no by-* callback is needed, handle any supervisor-owned report execution/lifecycle step. B009 must not run it.
- A-agent actions: none requested.
- B009 future research actions: none for UID0000ZG unless supervisor requests a report repair or implementation callback.

## Confidence

- Recommendation confidence: high (`0.93`) for owner/emitter/no-wrapper-C++ recommendation.
- Score confidence: medium-high (`0.88`) for unchanged target/support scores because the remaining cap is source-spelling uncertainty, not missing binary evidence.
- Remaining uncertainty: final historical source-file fold and static object/source-facing class spelling.

## Validator Results

- Commands run: none.
- Results: not applicable; no by-* docs were edited in this report-only pass.
- Any unresolved validator warnings/errors: none observed because no validators were run. Generated AutoInit source was read-only context only.

## Changed Files

- Created: `tools/leaser/Agents/Agent-B009/research/0000ZG-AutoInit-source-quality.md`.
- Modified: none outside this report.
- Renamed: none.
- Report execution: not run. B agents must not run `tools/validator.py execute_report ... --apply`, dry-run/probing variants, registry lifecycle commands, manual report moves, or equivalent lifecycle/archive commands.

## Implementation Tracking Checklist

Initial report-only pass:
- [ ] Supervisor validation required before implementation.
- [ ] Target/support docs to update: no by-* edits recommended; target/support docs are already present at same-or-greater detail.
- [ ] Current target state and actual evidence checked recorded: current target metadata, support docs, generated AutoInit context, old B-report matches, and MCP call results are recorded above.
- [ ] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: initial ledger rows ZG-C01 through ZG-C10 were report-only `proposed`; callback pass below now records the final verification states.
- [ ] Metadata/score changes to apply: none; keep UID0000ZG `85/90`, owner/emitter [UID:0000HN], reconstructable `TRUE`, blank formal C++.
- [ ] Score-limiting blockers researched to resolution, implementation-ready repair/split recommendation, or rare exact evidence-backed no-improvement proof after all reasonable current routes were exhausted: final source file/object spelling remains unresolved after current MCP/support-doc/report checks; no safe score move is recommended.
- [ ] Owner/emitter/reconstructable changes to apply: none; confirm existing owner/emitter/reconstructable state.
- [ ] Split/rename/new-child changes to apply: none; range is exact and successor/padding are documented.
- [ ] Source-placement, range/split/padding/reclassification, or IDA rename/type/comment changes to apply or confirm not applicable: source placement remains AutoInit with PlatformApi fold caveat; no IDA edits requested.
- [ ] First-draft C++ or no-code proof to apply: no-code proof for UID0000ZG wrapper is recorded in `First-Draft C++ Recommendation`; formal target C++ remains blank.
- [ ] Third-party import directive to apply or confirm not applicable, including exact `third_party_embeds/...` paths and confirmation that the multiline C++ block remains blank: not applicable; no third-party import.
- [ ] Exact target/support doc facts to incorporate at report-level detail: no new by-* facts beyond already-present current docs; report records same facts for supervisor coverage.
- [ ] Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: Browser ownership rejected; PlatformApi direct ownership not proven; raw scalar wrapper C++ rejected; final source spelling remains unresolved.
- [ ] Wave2/Wave3 mentions or artifacts encountered and ignored/rejected as stale, if applicable: stale Wave3 vtable count issue noted only as support context; not used as proof.
- [ ] Open questions to close or document as evidence-backed unresolved: all UID0000ZG assignment questions are resolved except final file/object spelling, which is documented as nonblocking.
- [ ] Validators to run: none in report-only mode because no by-* edits were made.
- [ ] Generated report refresh expected or explicit manual supervisor-owned coverage/tracker text to apply: no manual generated/coverage/tracker text; supervisor-owned report execution should update B-report coverage if accepted.

Implementation callback pass:
- [x] Report accepted by supervisor for implementation: Gate 1 passed and supervisor sent the UID0000ZG callback on 2026-07-05.
- [x] All accepted target/support doc details incorporated at report-level detail: ZG-C01 through ZG-C09 were verified already present at same-or-greater detail in the target/support by-* docs; ZG-C10 is not a by-* implementation item.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: rows ZG-C01 through ZG-C09 are callback `already-present`; ZG-C10 is callback `excluded-with-reason`.
- [x] Metadata/score/owner/emitter/split/rename/C++ changes applied or explicitly not applied with reason: accepted disposition keeps UID0000ZG `COMPLETION:85`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000HN`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000HN`, and blank formal C++ unchanged.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence preserved: Browser ownership rejection, PlatformApi fold caveat, and scalar-wrapper no-code proof were verified already present; no edit required.
- [x] Open questions resolved or documented with evidence-backed unresolved rationale: final standalone file/object spelling remains documented as unresolved and nonblocking; no callback edit required.
- [x] Validators run and results recorded: no scoped validators were run because no by-* docs changed.
- [x] Generated report refresh completed by validator or explicit manual supervisor-owned coverage/tracker text supplied/confirmed unchanged: no generated/manual coverage/tracker edit was made or needed; supervisor execution is the only remaining lifecycle step.
- [x] Remaining unapplied accepted items listed with exact blocker: none.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B009","command_id":"000000006730","destination_path":"executed-b-agent-research/B009/0000ZG-AutoInit-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B009/research/0000ZG-AutoInit-source-quality.md","timestamp":"2026-07-05T03:49:33-04:00","uid":"0000ZG"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
