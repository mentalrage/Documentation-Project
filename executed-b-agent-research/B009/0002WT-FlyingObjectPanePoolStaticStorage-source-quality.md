** TARGET-REPORT-UID:0002WT **
** AUTHOR-AGENT-ID:B009 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# 0002WT FlyingObjectPanePoolStaticStorage Source-Quality Report


## Finalized Report / Current Recommendation

- Current recommendation: preserve [UID:0002WT] as the exact reconstructable `0x0069b9d4-0x0069b9fc` file-local `PoolAllocator` definition owned/emitted by [UID:000057] `FlyingObjectPane`, with current `88/93` metadata, current owner/emitter route, exact CPP definition, and blank H block.
- Final disposition: B002 remains the earlier implementation authority for the substantive target/class/file implementation executed on 2026-07-12. Under the supervisor-authorized callback for exact report SHA `F3C732722FBE0C9C761F2F8E690D1962134F2AC13291F4C76D40EE9BEB60330D`, B009 supplementally incorporated the live-MCP/IDA prestate, corrected stale target score citations, preserved the exact CPP/blank H and `88/93`, and did not duplicate ordinary implementation. Supervisor Gate 2B is now applied and independently verified: the target is one typed 40-byte data item with both exact comments; helper UDTs exist at ordinals `934` and `935`; `PoolAllocator` remains exact `NO_CHANGE` at ordinal `444` with `_opaque[40]`. Supervisor catalog entry `0344` is now exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification. This repaired B009 artifact is approved for normal validator execution as an additional direct supplemental report after final gates; it adds independently verified current evidence, IDA poststate, coverage history, and generated closure without replacing B002's earlier authority.
- Required remaining action: fresh supervisor Gate 1/claim verification of this repaired artifact, final supervisor lifecycle-time generated readback, and normal additional-direct B009 report execution/archive remain pending. Gate 2B, ordinary-document reconciliation, target validation, manual coverage replacement/validation, catalog reconciliation, and audit-time command `000000018722` generated semantic verification are complete. Later supervisor validators may advance physical freshness headers; the final lifecycle-time readback is authoritative. After execution, the tracker is expected to count both direct reports, B002 and B009, while preserving B002 as the earlier implementation authority.
- Confidence: high for binary behavior, range, boundaries, xrefs, type layout, owner/emitter route, source placement, CPP/H disposition, and IDA action safety. Confidence remains below final audit because original variable spelling/internal-linkage spelling and whether the developer wrote numeric `0x144` or `sizeof(FlyingObjectPane)` are not recoverable from symbols.

## Supporting Research

- Assignment source: `tools/leaser/Agents/Agent-B009/goal.md`, current report-only target [UID:0002WT] `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`.
- Workflow sources read in full: project-level `ntk-b-agent-workflow` skill; all six referenced workflow/template/MCP/score/supervisor files; and current `by-structure.md` MCP, source-declared/generated-binary data, owner/emitter, coverage, and CPP/H rules.
- Lifecycle/status notes: this named B009 artifact remains active and unexecuted in `Agent-B009/research/` pending final supervisor gates and normal execution as an additional direct supplemental report. It has been repaired in place rather than deleted or replaced with a new artifact. B002's distinct report is already executed and remains the earlier implementation authority; after B009 execution, the tracker is expected to count both B002 and B009 as direct reports without transferring that earlier implementation authority.
- Matching prior reports opened as leads:
  - `executed-b-agent-research/B002/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md`: direct prior report. It implemented the current `88/93` target, formal static definition, and support synchronization; current by-* docs and live MCP were rechecked independently rather than trusting it as authority.
  - `executed-b-agent-research/B002/0000AM-PoolAllocator-class-source-quality.md`: accepted `PoolAllocator` mechanics, distributed concrete static-pool placement, and descriptive name `g_flyingObjectPanePool`.
  - `executed-b-agent-research/B009/0000HJ-AttachedObjectPane-empty-emitter-family-source-quality.md`: accepted analogous object-pane pool declarations for HitBar, DamageNumber, and Balloon pool storage children.
  - `executed-b-agent-research/B006/00034Z-FlyingObjectPaneVtableData-source-quality.md`: current FlyingObjectPane vtable/source-route support; used only to corroborate class/file source route and to avoid treating the pool page as vtable ownership evidence.
- Historical IDA implementation catalog entry `0015` was checked during research. Its claimed comment/item poststate was disproved by the fresh pre-action readback and is retained only as superseded history. Current supervisor catalog entry `0344` is exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification and records the verified Gate 2B application and saved-IDB poststate.
- Stale Wave2/Wave3 material and generated temp files were not used as authority.

## Target

- Target UID: `0002WT`.
- Additional target UIDs: `none`.
- Declared-target inventory: [UID:0002WT] `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`, exact source-declared static storage and sole declared report target.
- Target path: `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`.
- Source queue/report row: audit-time command `000000018722` `auto-generated/-ag-research-tracker.md` covered row lists `88/93`, average `90.5`, reconstructable `true`, one emitted code block, and one direct executed B002 report before approved B009 execution.
- Current supervisor classification: accepted additional-direct supplemental target and post-IDA reconciliation callback. B009 was authorized to edit only UID0002WT, run its scoped validator, and reconcile this report; support/manual/IDA/lifecycle changes remained prohibited to B009, while supervisor Gate 2B was completed independently. Normal supervisor execution of this report after final gates is approved and will register B009 as a second direct report while retaining B002 as the earlier implementation authority.
- Current scores and parent state: `COMPLETION:88`, `CONFIDENCE:93`, `CANONICAL_OWNER:000057`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000057`, blank emitter position, exact CPP definition present, H block blank.

## Current Target State

- Existing metadata: [UID:0002WT] is already `88/93` with correct direct semantic owner/emitter [UID:000057] `FlyingObjectPane`. The emitter chain surfaces through [UID:000057] -> [UID:0000KG] `ItemObjectPane`, whose source route is `NexusTK/map/ItemObjectPane.cpp`.
- Existing owner/emitter/reconstructable state: correct and should remain unchanged.
- Existing C++/emitter state: target CPP already contains, and generated `ItemObjectPane.cpp` emits exactly:

```cpp
static PoolAllocator g_flyingObjectPanePool(0x144, 4);
```

- Existing open questions, blockers, placeholder names, stale assumptions, or contradictory claims:
  - Pre-callback target prose was largely current, but one evidence sentence cited [UID:0000KG] at historical `85/86`; the authorized callback corrected that citation and the separate `86/88` assignment sentence to current `91/93`.
  - Historical pre-coverage state: the manual by-memory row was stale at `82%` with `ff` bytes and below-gate/no-assignment wording. The supervisor applied the exact replacement payload from this report, and dated command checkpoint `000000018675` validated the manual coverage file successfully; catalog entry `0344` now records exact state `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification.
  - Historical Gate 2B prestate had name `g_flyingObjectPanePool` and `PoolAllocator` tinfo at the base, but only a one-byte containing item, 39 separate one-byte successors, and absent comments. That complete prestate remains preserved below as action-safety evidence.
  - Current applied poststate is one `40`-byte `is_data` item of type `struct PoolAllocator`, entity/global `g_flyingObjectPanePool` size `40`, with both exact address comments. `PoolChunkHeader` and `PoolFreeBlock` now exist at ordinals `934` and `935`; `PoolAllocator` remains unchanged at ordinal `444`, size `0x28`, sole `_opaque[40]` member.
  - Exact original variable spelling/linkage is unavailable. The best source-facing reconstruction remains `g_flyingObjectPanePool` with file-local `static`; this is inferred, not original-symbol proof.
- Related target/support docs checked: [UID:000057] `by-class/FlyingObjectPane.md`, [UID:0000KG] `by-file/ItemObjectPane.md`, [UID:00034Z] `FlyingObjectPaneVtableData`, [UID:0002B4] `ObjectPanePoolStaticStorage`, [UID:0002WS] ItemObjectPane pool sibling, [UID:0002WU] Balloon pool successor, [UID:0000TI] `PoolAllocatorStaticInstances`, [UID:0000MM] `PoolAllocator`, [UID:0001VK] `PoolAllocatorLayout`, constructor/destructor wrapper pages, generated `ItemObjectPane.cpp`, generated by-memory coverage/tracker rows, and matching reports listed above.
- Current artifact/lifecycle status: existing unexecuted B009 report repaired in place and reconciled after the ordinary target edit, supervisor Gate 2B, the historical Gate 1 pass for SHA `E1094173949E432B886C0831014FB6982B765931EFF23883782F046E15851828`, target validation command `000000018674`, dated manual coverage validation checkpoint `000000018675`, catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification, and audit-time generated semantic checkpoint `000000018722`. Command `000000018697` and its hashes are retained only as an earlier dated semantic checkpoint. This repaired artifact now awaits fresh supervisor Gate 1/claim verification and final lifecycle-time generated readback; later supervisor validators may advance physical freshness headers, so that final lifecycle-time readback is authoritative. B009 performed no IDA write and no report execution, lifecycle/archive action, manual coverage/generated edit, or support-doc edit. Following final supervisor verification, normal `execute_report` and archive are the approved remaining lifecycle actions; the resulting tracker state should count both direct B002 and B009 reports while leaving B002 as the earlier implementation authority.

## Executive Recommendation

- Best direct owner: [UID:000057] `FlyingObjectPane` remains the correct narrow semantic owner. Two MapPane-side paths allocate from this pool and immediately construct `FlyingObjectPane`; constructor-failure cleanup, scalar-delete return, static construction, and static cleanup use the same base.
- Target assignment/emission: preserve `CANONICAL_OWNER:000057`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000057`, blank position, and the already-present formal definition:

```cpp
static PoolAllocator g_flyingObjectPanePool(0x144, 4);
```

- Split/container disposition: no split, merge, rename, or parent reclassification is required. The half-open target range `0x0069b9d4-0x0069b9fc` is exactly one `0x28` / 40-byte `PoolAllocator` storage object. The predecessor `0x0069b9ac-0x0069b9d4` is the ItemObjectPane pool and the successor `0x0069b9fc-0x0069ba24` is the BalloonObjectPane pool.
- Exact condition before remaining lifecycle action: final supervisor claim-by-claim verification of this coverage-reconciled artifact and final lifecycle-time generated readback are required before normal execution/archive as an additional direct supplemental report. The target is source-covered, Gate 2B hardening is complete, the manual row is current, and audit-time command `000000018722` proves the expected generated semantics. Later supervisor validators may advance physical freshness headers; their final lifecycle-time readback is authoritative. Execution is expected to add B009 beside B002 in direct report counts, not replace B002's earlier implementation authority.

## Supervisor Active Recheck

- Supervisor instruction rechecked: B009 is assigned report-only source-quality research for [UID:0002WT] `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md` with expected report path `tools/leaser/Agents/Agent-B009/research/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md`.
- Split repair requirement: no split repair is required; the exact child page already exists with the correct half-open range and metadata route.
- Source-bearing children in scope: only UID0002WT. Its definition is already present at same-or-greater detail through executed B002 work. No child creation is needed. Related vtable/class/file/aggregate pages were rechecked and must not be reclassified by this additional-direct supplemental report.

## Inference Research Guidance Check

- `by-structure.md` confirms source-declared/generated-binary data includes static globals whose source declarations must exist even though exact runtime bytes are compiler/runtime initialized.
- `CANONICAL_OWNER` must be the narrowest true semantic owner. Because this pool is used by `FlyingObjectPane` allocation/free paths, [UID:000057] is better than [UID:0000KG] as canonical owner.
- `EMITTER_UIDS` is output routing. [UID:000057] routes to [UID:0000KG] `ItemObjectPane`, whose generated file is `auto-generated/NexusTK/map/ItemObjectPane.cpp`; the route is valid.
- Formal by-memory C++ must represent only this range. The existing static `PoolAllocator` definition does so; the blank H block is correct because file-local internal-linkage storage has no header declaration.
- Fact/inference separation:
  - Direct MCP facts: current bytes are zero-filled; direct/interior xrefs and wrapper constants are exact; allocation paths call `PoolAllocator::Allocate` then the `FlyingObjectPane` constructor; failure/scalar paths free to the same pool. The historical IDA data/type/comment prestate and the independently verified applied poststate are both literal below.
  - Documentation evidence: current target/support docs assign owner/emitter and emit the definition; [UID:0000TI] records `g_flyingObjectPanePool`; in the current pre-B009-execution tracker, B002 is the sole executed direct report and remains the earlier implementation authority. Approved normal B009 execution will add this artifact as a second direct supplemental report without replacing that authority.
  - Inference: exact original global spelling and `static` internal linkage are not directly proven; `static PoolAllocator g_flyingObjectPanePool` is the best source-facing reconstruction from current naming convention and accepted sibling pool reports.
- Wave2/Wave3: no stale Wave2/Wave3 claim was used as authority.

## Heuristic / Inference Reanalysis And Validation

- Byte-state blocker: resolved. Current `get_bytes` and `get_global_value` return forty zero bytes. Old all-`ff` wording remains historical only and current target/generated summaries are already corrected.
- Empty-emitter blocker: resolved before this repair. Current target CPP and generated `ItemObjectPane.cpp` contain the exact definition and no UID0002WT empty marker.
- Declaration spelling/linkage blocker: resolved to best defensible inference. `g_flyingObjectPanePool` follows the accepted concrete-pool convention and current IDA name. File-local `static` is the most plausible source shape for an implementation-private object with only internal and compiler-lifetime routes. This is inferred rather than symbol-proven and caps final-audit confidence, but it does not justify an IDA/decompiler label in source.
- Numeric expression blocker: preserve direct constants `0x144` and `4`. `sizeof(FlyingObjectPane)` is plausible but current IDA has no `FlyingObjectPane` UDT, while wrapper and sized-delete evidence directly prove `0x144`; numeric source is behavior-exact and avoids claiming an unavailable final class-layout expression.
- IDA target-boundary blocker: resolved and applied. Historical prestate had forty one-byte items; supervisor Gate 2B safely recreated one exact 40-byte `PoolAllocator` data item without crossing either neighbor. All 40 bytes remain zero, all nine checked interior dword addresses remain at zero xrefs, predecessor/successor remain separate, and the exact six base operands remain unchanged.
- IDA type-layout blocker: resolved and applied with a mixed exact disposition. `PoolChunkHeader` and `PoolFreeBlock` now exist at ordinals `934` and `935`, each size `4` with one self-pointer `next` at `+0`. `PoolAllocator` remains exact `NO_CHANGE` at ordinal `444`, size `0x28`, sole `_opaque[40]` member; it was not redeclared, replaced, renamed, or commented.
- Ownership/source-placement blocker: resolved. `FlyingObjectPane` is proven by both allocation/constructor paths, shared unwind free, scalar deleting destructor, and vtable stores; [UID:000057] remains the narrow owner and [UID:0000KG] the source-root route.
- Generated function names encountered: `sub_512D60`, `sub_537CB0`, and `sub_53D1A0` already have separate exact by-memory owners; `sub_41A0A0`, `sub_514BA0`, and `sub_60C350` are compiler startup/EH/cleanup artifacts. This report resolves their semantic roles but does not duplicate their IDA ownership or create source-authored wrapper names. `sub_50DFA0` is a MapPane-side creation helper in a broad owner page; it is not a score/C++ blocker for this storage target and is protected from an unsupported rename here.
- Vtable/aggregate recheck: [UID:00034Z] remains class-generated marker support; [UID:0002B4] remains a mixed-owner non-emitting container. Neither should absorb this storage definition.
- Rejected alternatives:
  - Reapply B002's target/class/file implementation: rejected as duplicate work; current docs/generated source already contain it.
  - Emit raw bytes or a byte array: rejected because this is source-declared static object storage.
  - Assign to generic `PoolAllocator`, direct by-file, MapPane caller code, or the aggregate: rejected because [UID:000057] is the narrow proven owner.
  - Merge with ItemObjectPane or Balloon storage: rejected by exact `0x28` boundaries and distinct xref sets.
  - Leave the target as forty one-byte items: rejected because exact type, range, and boundary safety were proven; the target is now one exact typed item. Preserve the existing opaque `PoolAllocator` UDT itself under the exact `NO_CHANGE` disposition above.

## Evidence Standards Used

- Direct IDA MCP facts were required for bytes, xrefs, wrappers, function boundaries, disassembly, decompilation, and class/vtable support.
- Current by-* docs were used as documentation evidence and checked for stale claims; generated reports were used only for current coverage/generation state.
- Existing B reports were treated as leads/precedent only after targeted search and opening of matching reports.
- Inference was allowed for source-facing static name and `static` linkage because exact original symbols are unavailable, but it was constrained by accepted project naming convention, sibling pool implementations, source route, and plausible late-1999 to mid-2000s C++ static object shape.
- Evidence is strong enough to preserve the formal C++ and verify the applied Gate 2B result because exact bytes, six base routes, zero interior routes, wrapper constants, constructor/destructor behavior, exact UDT sizes/members, exact comments, and exact neighbors agree.
- Confidence remains below final audit because original source filename/header inclusion and exact global spelling/linkage are inferred rather than proven.

## Evidence Checked

- IDA MCP availability/schema:
  - JSON-RPC `initialize` returned `serverInfo.name:"ida-pro-mcp"`, version `1.0.0`, and server protocol `2025-06-18`.
  - MCP client session ID: `8b65c786-e32a-4aeb-90fe-11bdabe1d421`.
  - `tools/list` was rechecked before use.
  - `idb_list` returned active database `5a570ede`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
  - `server_health` returned `status:"ok"`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:false`, `hexrays_ready:true`, and `strings_cache_ready:true`. Repeated bounded IDB reads succeeded, so the service was live under the project MCP rules.
- MCP/read-only checks performed for the historical Gate 2B prestate:
  - `get_bytes` on `0x0069b9ac`, `0x0069b9d4`, and `0x0069b9fc`, size 40 each: all current bytes read `0x0`.
  - `get_bytes` on `0x0069ba24`, size 4: four `0x0` bytes outside the pool child cluster.
  - `lookup_funcs`: target `0x0069b9d4` is not a function; `0x0041a0a0` `sub_41A0A0` size `0x1d`; `0x0060c350` `sub_60C350` size `0x0a`; `0x0050dfa0` `sub_50DFA0` size `0x157`; `0x00512d60` `sub_512D60` size `0x1bc`; `0x00514ba0` `sub_514BA0` size `0x3e`; `0x0053d1a0` `sub_53D1A0` size `0x97`; allocator helpers `0x004b13d0`, `0x004b1400`, `0x004b14c0`, and `0x004b1520` retain sizes `0x30`, `0xb8`, `0x2f`, and `0x65`.
  - `xref_query` on `0x0069b9d4`: exactly six direct refs at `0x0041a0a7`, `0x0050e06b`, `0x00512e8e`, `0x00514bc5`, `0x0053d1f0`, and `0x0060c350`, no truncation.
  - `xref_query` on neighboring `0x0069b9ac` and `0x0069b9fc`: distinct ItemObjectPane and BalloonObjectPane xref sets, proving neighbor separation.
  - Historical `entity_query` globals/names returned `g_itemObjectPanePool` at `0x0069b9ac`, `g_flyingObjectPanePool` at `0x0069b9d4`, and `g_pBowGaugeObjectPane` at `0x0069ba24`; target global size was one byte.
  - Historical `inspect_items` reported target base head/end `0x69b9d4-0x69b9d5`, size `1`, type `struct PoolAllocator`; interiors at `0x69b9d5` and `0x69b9fb` were independent one-byte items; successor started at `0x69b9fc`.
  - Historical `get_comments` returned empty regular and repeatable comments at target base and checked interiors.
  - Historical `type_inspect` returned `PoolAllocator` size `40` with sole member `_opaque` at `+0`, size `40`; `CRITICAL_SECTION` and `_RTL_CRITICAL_SECTION` each existed at size `24`; `PoolChunkHeader`, `PoolFreeBlock`, and `FlyingObjectPane` types were absent.
  - `read_struct` reads target as `PoolAllocator` with `_opaque[40]`, all zero.
  - `xref_query` on interior dwords `0x0069b9d8` through `0x0069b9f8` returns zero refs for every checked offset.
  - `find_bytes "D4 B9 69 00"` returns exactly six operand matches at `0x41a0a8`, `0x50e06c`, `0x512e8f`, `0x514bc6`, `0x53d1f1`, and `0x60c351`; RVA pattern `D4 B9 29 00` returns zero.
  - `disasm 0x0041a0a0`: `push 4`, `push 144h`, `mov ecx, 69B9D4h`, call allocator constructor, register `sub_60C350` with `_atexit`.
  - `disasm 0x0060c350`: `mov ecx, 69B9D4h`; jump to allocator destructor.
  - `disasm 0x00514ba0`: pushes the destructor/unwind argument, loads `unk_69B9D4` into `ecx`, and calls `sub_4B14C0`.
  - `analyze_function 0x0050dfa0`: decompilation calls `sub_4B1400(unk_69B9D4)` and then `sub_537CB0(...)`, adds the object through `sub_5314A0`, and schedules a timer through `sub_597910`.
  - `analyze_function 0x00512d60`: packet/update handler parses payload fields, removes/replaces an existing object when present, calls `sub_4B1400(unk_69B9D4)`, then `sub_537CB0(...)`, adds the result through `sub_5314A0`, and schedules timer state.
  - `analyze_function 0x0053d1a0`: scalar deleting destructor restores inherited item/object vtables, calls base cleanup, and conditionally calls allocator free on `0x69B9D4`.
  - `lookup_funcs` and `analyze_function 0x00537cb0`: `sub_537CB0` is size `0xb0`, is called from `sub_50DFA0` and `sub_512D60`, initializes FlyingObjectPane motion fields, and stores vtable bases at `0x0062049c`, `0x0062050c`, and `0x0062053c`.
  - `get_bytes` and `xref_query` on `0x00620498`, `0x0062053c`, `0x00620544`, `0x0062049c`, `0x0062050c`, and `0x0062053c`: vtable support remains tied to `sub_537CB0` and [UID:00034Z] source-route evidence; the pool does not own vtable data.
  - Allocator constructor/allocate/free/destructor analysis confirms fields at `+0/+4/+8/+0xc/+0x10`, embedded lock size, free-list push/pop, chunk-list teardown, and exact `0x28` object size.
  - Final historical pre-return liveness recheck: `server_health` remained `status:ok`, IDB `5a570ede`, Hex-Rays/string cache ready; bounded 40-byte target read again returned all zero and target regular/repeatable comments remained absent.
- Supervisor-supplied independently verified Gate 2B poststate:
  - `PoolChunkHeader` ordinal `934` and `PoolFreeBlock` ordinal `935` each exist at size `4` with one self-pointer `next` member at `+0`.
  - `PoolAllocator` remains exact `NO_CHANGE`: ordinal `444`, size `0x28`, sole `unsigned __int8 _opaque[40]` at `+0`.
  - `[0x0069b9d4,0x0069b9fc)` is one 40-byte `is_data` item of type `struct PoolAllocator`; entity/global readback retains `g_flyingObjectPanePool` at `0x69b9d4`, size `40`.
  - Regular comment is exactly `FlyingObjectPane file-local PoolAllocator storage; block size 0x144, 4 blocks per chunk.`
  - Repeatable comment is exactly `Source reconstruction: static PoolAllocator g_flyingObjectPanePool(0x144, 4); startup, unwind, scalar-delete, and shutdown wrappers are compiler-generated.`
  - Forty target bytes and checked neighboring bytes remain zero; six base xrefs remain exact; checked interiors remain at zero xrefs; predecessor/successor items and protected helper/wrapper identities remain unchanged.
  - Saved IDB SHA256 is `A48E4C7BB251D16ED4BB7AC337CDA8E9B93D92FAE4DD5C711C9F91FD9495D88E`, size `143189795`, last write `2026-07-29T00:36:31-04:00`; supervisor catalog entry `0344` is exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification, and the supervisor Gate 2 audit is complete.
- by-* docs, support docs, old reports, generated reports, and trackers checked:
  - Target [UID:0002WT] page.
  - [UID:000057] `by-class/FlyingObjectPane.md`.
  - [UID:0000KG] `by-file/ItemObjectPane.md`.
  - [UID:00034Z] `by-memory/0x00620498-0x00620544.FlyingObjectPaneVtableData.md`.
  - [UID:0002B4] `by-memory/0x0069b894-0x0069ba24.ObjectPanePoolStaticStorage.md`.
  - Sibling/neighbor pages [UID:0002WS] and [UID:0002WU].
  - [UID:0000TI] `by-global/PoolAllocatorStaticInstances.md`.
  - [UID:0000MM] `by-file/PoolAllocator.md`, [UID:0001VK] `PoolAllocatorLayout`.
  - Constructor/destructor wrapper pages [UID:0000WC] and [UID:0001O8].
  - Audit-time command `000000018722` generated research/coverage rows show target `88/93`, coded/emits-code true, and one direct B002 executed report before approved B009 execution; its `ItemObjectPane.cpp` checkpoint contains one exact target marker and one exact definition with no target empty marker.
  - Historical pre-replacement manual-row readback: by-memory UID0002WT was stale at `82%`; the supervisor has already replaced it with the exact current payload below. By-class UID000057 and by-file UID0000KG required no target-specific replacement.
  - Matching reports listed in `Supporting Research`.
- Report search terms used:
  - `TARGET-REPORT-UID:0002WT`, `0002WT`, `0x0069b9d4`, `0069b9d4`, `FlyingObjectPanePoolStaticStorage`, `g_flyingObjectPanePool`, `flyingObjectPanePool`, `unk_69B9D4`, `byte_69B9D4`, `sub_41A0A0`, `sub_60C350`, `sub_50DFA0`, `sub_512D60`, `sub_514BA0`, `sub_53D1A0`, `PoolAllocatorStaticInstances`, `FlyingObjectPane`, `00034Z`, `0002B4`, `ObjectPanePoolStaticStorage`, `static PoolAllocator`, and `Empty Emitter Marker`.
- Negative checks performed:
  - No direct function at the target data address.
  - Current descriptive IDA name exists, but no original debug/public symbol proves that spelling or linkage.
  - No hidden seventh absolute pointer, no RVA pointer, no interior dword xref, no nonzero byte, no target function, no range gap/overlap, and no external-header use requiring an H declaration.
  - No direct evidence supports `PoolAllocator.cpp` as owner of all concrete static pools.
  - No evidence supports a single source-level object for [UID:0002B4]'s mixed pool cluster.
  - No evidence supports raw byte array emission for this zero-filled static object storage.
- Failed, unavailable, or intentionally skipped checks:
  - One `entity_query kind:any` payload was rejected because `any` is unsupported; corrected bounded `globals` and `names` calls succeeded. This was caller/schema error, not MCP failure.
  - No valid required MCP call failed and the listener/session remained available.
  - The report-only evidence pass ran no validator. The later authorized callback ran only the scoped UID0002WT validator recorded below; generated changes were validator-owned side effects and were not manually edited.
  - B009 ran no `execute_report`, lifecycle, archive/manual move, manual generated-file edit, IDA DB write, or MCP start/stop/restart/kill action. The supervisor separately completed the accepted Gate 2B mutation/readback/save. The authorized ordinary callbacks leased only UID0002WT immediately before editing and released it immediately after validation/readback. Normal supervisor `execute_report` and archive remain expected after final gates and will register B009 as an additional direct supplemental report.

## Claim And Incorporation Ledger

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C001 | 0002WT | Range is exactly `0x0069b9d4-0x0069b9fc`, one `0x28` / 40-byte `PoolAllocator` static object. | High | Live bytes, exact neighbors, allocator size, no interior refs. | Target `Storage`/`Evidence` | already-present; current MCP refresh incorporated | verified in target lines 31-43 and 65-123 |
| C002 | 0002WT | All 40 input bytes are zero; all-`ff` is superseded history. | High | Live `get_bytes` and `get_global_value`. | Target history/evidence | already-present; supplemental negatives preserved | verified in target evidence/history |
| C003 | 0002WT | Startup wrapper constructs `(0x144, 4)` and registers cleanup. | High | Live disassembly `0x0041a0a0`. | Target evidence/CPP rationale | already-present | verified unchanged |
| C004 | 0002WT | Cleanup wrapper destroys this exact pool. | High | Live disassembly `0x0060c350`. | Target evidence | already-present | verified unchanged |
| C005 | 0002WT | Six exhaustive base routes cover construction, two allocations, unwind free, scalar free, and cleanup. | High | Live xrefs plus six absolute-pointer matches. | Target evidence/lifecycle | current refresh and negative checks incorporated | verified in target supplemental evidence |
| C006 | 0002WT | [UID:000057] remains narrow owner/emitter; [UID:0000KG] remains source root. | High | Constructor/vtable/free chain; current metadata. | Target ownership/source placement | preserved | verified metadata `000057` and source route |
| C007 | 0002WT | [UID:0002B4] remains mixed non-emitting aggregate and [UID:00034Z] class-generated vtable support. | High | Current docs and exact boundaries. | Target rejected alternatives | preserved | verified unchanged; support docs no-change |
| C008 | 0002WT | Exact CPP remains `static PoolAllocator g_flyingObjectPanePool(0x144, 4);`. | High | Wrapper constants, lifecycle, accepted route, and audit-time command `000000018722` generated semantics. | Target CPP block | preserved | verified target formal block; historical command `000000018378` and audit-time command `000000018722` each show the exact definition |
| C009 | 0002WT | H remains blank because file-local `static` has no external declaration. | High | C++ linkage semantics and no external-header evidence. | Target H block | preserved/no-header-code | verified blank formal H; validator generated no `ItemObjectPane.h` |
| C010 | 0002WT | Preserve score/metadata `88/93`, owner/emitter `000057`, reconstructable true. | High | Current page completeness and exhaustive refreshed evidence. | Target metadata | no change | verified target header and generated UID row |
| C011 | 0002WT | Correct stale target support-score sentence from file `85/86` to current `91/93` and add current MCP/IDA prestate evidence. | High | Current [UID:0000KG] header/manual row; live MCP. | Target evidence/change history | incorporated | verified target lines 63-123 and 164-172; both stale current citations corrected |
| C012 | 0002WT | Historical IDA prestate had the correct name but forty one-byte items and absent comments; current poststate is one exact typed data item with both comments. | High | Historical `entity_query`/`inspect_items`/`get_comments`; supervisor independent readback. | Target evidence; supervisor Gate 2B | historical prestate preserved and current poststate incorporated | verified one 40-byte item, size-40 global, exact comments, unchanged bytes/xrefs/neighbors |
| C013 | 0002WT | Existing `PoolAllocator` UDT remains exact `NO_CHANGE`. | High | Historical and post-action `type_query`/`type_inspect`. | Target evidence; supervisor Gate 2B | no-change disposition applied and verified | ordinal `444`, size `0x28`, sole `_opaque[40]` member unchanged |
| C014 | 0002WT | Recreate the exact 40-byte data item while preserving `g_flyingObjectPanePool`, after declaring source-quality helper types. | High | Zero interior refs; exact boundaries; exact UDT size; supervisor readback. | Supervisor Gate 2B | applied and independently verified | helper ordinals `934`/`935`; target one 40-byte typed item; name/global size `40` |
| C015 | 0002WT | Add exact target regular/repeatable comments. | High | Historical comments empty; exact supervisor application/readback; catalog entry `0344`. | Supervisor Gate 2B | applied and independently verified | both exact comments present; saved-IDB proof recorded; catalog entry `0344` is `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification |
| C016 | 0002WT | Historical manual by-memory coverage row was stale and required the exact supervisor replacement payload. | High | Historical row read directly; exact payload below; dated supervisor command checkpoint `000000018675`. | `by-memory/-coverage-report.md` | supervisor applied and validated | UID0002WT row matched the report payload at the dated checkpoint; exit `0`, `ok: 1`; subsequent independent reconciliation is included in catalog state `APPLIED_VERIFIED_RECONCILED` |
| C017 | 0002WT | Class/file manual rows need no target-specific replacement. | High | Current manual UID000057 and UID0000KG rows read directly. | Manual coverage audit | no change | support docs and their manual rows untouched |
| C018 | 0002WT | B002 remains the earlier implementation authority, while B009 is approved for normal execution as an additional direct supplemental report carrying independent current evidence, IDA poststate, coverage history, and generated closure. | High | Current pre-execution tracker, executed B002 report, and supervisor-approved lifecycle disposition. | Report lifecycle/final recommendation | approved for normal additional-direct execution | current tracker credits B002 before B009 execution; after execution it is expected to count both direct B002 and B009 reports while preserving B002's earlier implementation authority |

## Positive Evidence Summary

- Direct facts supporting the chosen recommendation:
  - Current MCP reads [UID:0002WT] as a zero-filled 40-byte storage object at exactly `0x0069b9d4-0x0069b9fc`.
  - Static constructor wrapper `0x0041a0a0` passes exact constants `0x144` and `4` to `PoolAllocator` initialization for this address.
  - Cleanup wrapper `0x0060c350` destructs the same address through `sub_4B1520`.
  - Allocation paths `0x0050dfa0` and `0x00512d60` call `sub_4B1400(unk_69B9D4)` and then `sub_537CB0`, the `FlyingObjectPane` constructor.
  - Constructor-unwind free wrapper `0x00514ba0` and scalar deleting destructor `0x0053d1a0` both return blocks to `unk_69B9D4`.
- Corroborating documentation/generated-report evidence:
  - Target metadata already records owner/emitter [UID:000057].
  - [UID:0000TI] records `0x0069b9d4` as `g_flyingObjectPanePool` with FlyingObjectPane module placement.
  - Generated `ItemObjectPane.cpp` now routes and emits UID0002WT exactly; in the current pre-B009-execution state, B002 is the sole executed direct report and remains the earlier implementation authority. Approved normal execution of B009 will add independent supplemental coverage history as a second direct report.
  - Current IDA preserves the descriptive global name/type lead and now also has the verified full-range data item, supporting helper UDTs, and exact address comments; the historical item/comment/type inspection below proves why those bounded changes were safe.
- Strongest inference chain and why it is sufficient:
  - Source declares a static `PoolAllocator` instance in the ItemObjectPane/FlyingObjectPane module; compiler/runtime zero-initializes the storage, static initialization calls the constructor with `0x144, 4`, and static cleanup calls the destructor. This is exactly what the existing module-scope definition expresses, while a blank H block avoids false external linkage.

## IDA MCP Facts

- Function/range facts:
  - `0x0041a0a0` `sub_41A0A0`, size `0x1d`: static constructor wrapper for `unk_69B9D4`.
  - `0x0060c350` `sub_60C350`, size `0x0a`: static cleanup wrapper for `unk_69B9D4`.
  - `0x0050dfa0` `sub_50DFA0`, size `0x157`: MapPane-side creation/update path; allocates from `unk_69B9D4` and calls `sub_537CB0`.
  - `0x00512d60` `sub_512D60`, size `0x1bc`: packet/update handler; allocates from `unk_69B9D4` and calls `sub_537CB0`.
  - `0x00514ba0` `sub_514BA0`, size `0x3e`: constructor-failure/unwind free wrapper for `unk_69B9D4`.
  - `0x0053d1a0` `sub_53D1A0`, size `0x97`: FlyingObjectPane scalar deleting destructor path; conditionally frees through `unk_69B9D4`.
  - `0x00537cb0` `sub_537CB0`, size `0xb0`: FlyingObjectPane constructor, called from `sub_50DFA0` and `sub_512D60`.
  - `0x00537d60`, `0x00537d80`, and `0x00537f20` remain modeled FlyingObjectPane non-deleting destructor, bounds, and animation tick anchors with sizes `0x1f`, `0x19d`, and `0x18f`.
- Data/table/padding facts:
  - `0x0069b9d4-0x0069b9fc` reads as 40 zero bytes in the active IDB session.
  - Predecessor `0x0069b9ac-0x0069b9d4` and successor `0x0069b9fc-0x0069ba24` also read as separate 40-byte zero-filled pool storages, proving same-size neighboring pool boundaries.
  - `0x0069ba24` is outside the object-pane pool cluster and reads as a separate four-byte zero-filled global slot.
- Xref facts:
  - `0x0069b9d4` xrefs: `0x0041a0a7` static constructor, `0x0050e06b` allocation path, `0x00512e8e` allocation path, `0x00514bc5` unwind free wrapper, `0x0053d1f0` scalar deleting destructor free, `0x0060c350` cleanup wrapper.
  - `0x0069b9ac` ItemObjectPane neighbor xrefs are distinct: `0x0041a0e7`, `0x00505e66`, `0x00514c45`, `0x0053d330`, `0x0060c370`.
  - `0x0069b9fc` BalloonObjectPane successor xrefs are distinct: `0x0041a047`, `0x00469007`, `0x0050e17f`, `0x005118b9`, `0x00514b05`, `0x0060c320`.
- Vtable/global/type facts:
  - `sub_537CB0` stores FlyingObjectPane vtable bases at `0x0062049c`, `0x0062050c`, and `0x0062053c`.
  - `get_bytes 0x00620498 size 16` starts with RTTI pointer bytes followed by slot `0x0053d1a0`, tying the scalar destructor to the FlyingObjectPane vtable child.
  - `PoolAllocatorLayout` documents `0x28` byte object size and fields; this target's storage size matches that layout.
  - Historical pre-Gate2B evidence: target name was `g_flyingObjectPanePool`; the containing item was one byte with base tinfo `struct PoolAllocator`; the `PoolAllocator` UDT was `_opaque[40]`; regular/repeatable comments were empty.
  - Current applied poststate: `[0x0069b9d4,0x0069b9fc)` is one 40-byte `is_data` item of type `struct PoolAllocator`; entity/global readback retains `g_flyingObjectPanePool` at `0x0069b9d4`, size `40`; regular comment is exactly `FlyingObjectPane file-local PoolAllocator storage; block size 0x144, 4 blocks per chunk.`; repeatable comment is exactly `Source reconstruction: static PoolAllocator g_flyingObjectPanePool(0x144, 4); startup, unwind, scalar-delete, and shutdown wrappers are compiler-generated.`; `PoolAllocator` remains ordinal `444`, size `0x28`, with sole `unsigned __int8 _opaque[40]` at `+0`.
- Negative IDA facts:
  - `0x0069b9d4` is not a function. Its descriptive current IDA name is not original-symbol proof.
  - All nine checked interior dword addresses have zero xrefs; no RVA pointer encoding exists.
  - No direct evidence supports `PoolAllocator.cpp` as owner of all concrete static pools.
  - No evidence supports a single source-level object for [UID:0002B4]'s entire mixed pool cluster.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x0069b9d4-0x0069b9fc` | [UID:0002WT] `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md` | Exact FlyingObjectPane static `PoolAllocator` storage | TRUE | [UID:000057] | `88/93` | Already source-covered; current evidence/IDA/manual-row repair only |
| `0x0069b894-0x0069ba24` | [UID:0002B4] `ObjectPanePoolStaticStorage` | Mixed container over ten adjacent pool objects | FALSE | NONE | `86/90` | Keep as non-emitting aggregate |
| `0x0069b9ac-0x0069b9d4` | [UID:0002WS] `ItemObjectPanePoolStaticStorage` | Predecessor ItemObjectPane static pool | TRUE | [UID:00006V] | current page controls | Boundary/support evidence only |
| `0x0069b9fc-0x0069ba24` | [UID:0002WU] `BalloonObjectPanePoolStaticStorage` | Successor BalloonObjectPane static pool | TRUE | [UID:00000U] | already implemented at `88/93` by accepted source-family report | Boundary evidence only |
| `0x00620498-0x00620544` | [UID:00034Z] `FlyingObjectPaneVtableData` | Class-generated RTTI/vtable marker support | TRUE | [UID:000057] | `88/92` | Already marker-filled; no target change required |
| `FlyingObjectPane` class | [UID:000057] `by-class/FlyingObjectPane.md` | Narrow semantic owner/emitter for target | TRUE | [UID:0000KG] | `85/88` | Current; no duplicate support edit |
| `ItemObjectPane.cpp` file root | [UID:0000KG] `by-file/ItemObjectPane.md` | Generated source root `NexusTK/map/ItemObjectPane.cpp` | File root | FILE | `91/93` | Current; target stale score mention should be corrected |
| `PoolAllocatorStaticInstances` | [UID:0000TI] `by-global/PoolAllocatorStaticInstances.md` | Cross-cutting non-emitting static pool inventory | FALSE | NONE | `89/92` | Already records `g_flyingObjectPanePool`; no edit required |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x0041a0a7 -> 0x0069b9d4` | In `sub_41A0A0` | Static constructor wrapper loads pool address before `sub_4B13D0`; wrapper pushes `4` and `144h`. |
| `0x0050e06b -> 0x0069b9d4` | In `sub_50DFA0` | MapPane-side object replacement/creation path allocates from this pool via `sub_4B1400`. |
| `0x0050e09b -> 0x00537cb0` | In `sub_50DFA0` | Allocation result is passed to the `FlyingObjectPane` constructor. |
| `0x00512e8e -> 0x0069b9d4` | In `sub_512D60` | Packet/update path allocates from this pool via `sub_4B1400`. |
| `0x00512ebe -> 0x00537cb0` | In `sub_512D60` | Allocation result is passed to the `FlyingObjectPane` constructor. |
| `0x00514bc5 -> 0x0069b9d4` | In `sub_514BA0` | Constructor-failure/unwind free wrapper returns a block to the flying pool. |
| `0x0053d1f0 -> 0x0069b9d4` | In `sub_53D1A0` | Scalar deleting destructor returns `this` to the flying pool when delete flags require free. |
| `0x0060c350 -> 0x0069b9d4` | In `sub_60C350` | Static cleanup wrapper destroys this pool. |
| `0x00537d1c/22/2c -> 0x0062049c/0c/3c` | In `sub_537CB0` | FlyingObjectPane constructor stores the three vtable views, tying the allocation result to the class owner. |

## Documentation Evidence And IDA Status

- Existing docs that support the conclusion:
  - Target [UID:0002WT] already lists direct owner [UID:000057], aggregate split parent [UID:0002B4], allocator mechanics [UID:0000MM], exact range, size, wrapper constants, and xref roles.
  - [UID:000057] records the constructor/bounds/tick/destructor anchors, vtable refs, exact pool child, and MapPane creation paths.
  - [UID:0000KG] records `ItemObjectPane.cpp` as the source root for item/flying companions, with static pools `0x0069b9ac-0x0069b9d4` and `0x0069b9d4-0x0069b9fc`.
  - [UID:0000TI] records `g_flyingObjectPanePool` at `0x0069b9d4`, block size `324`, chunk count `4`, constructor wrapper `0x0041a0a0`, cleanup wrapper `0x0060c350`, and FlyingObjectPane module placement.
  - [UID:0000MM] and [UID:0001VK] document allocator mechanics and distributed static-pool placement.
- Existing docs that are stale, incomplete, or contradicted:
  - Historical pre-callback target evidence named [UID:0000KG] at `85/86`; the authorized callback corrected that sentence and the separate `86/88` assignment sentence, and the current target now explicitly uses `91/93`.
  - Historical manual `by-memory/-coverage-report.md` UID0002WT row was `82%` with all-`ff` and below-gate/no-assignment wording; the supervisor replaced it exactly and validated the row at dated command checkpoint `000000018675`.
  - Historical IDA catalog entry `0015` claimed a comment was appended, but the fresh pre-action `get_comments`/item readback disproved that old poststate. Current catalog entry `0344` is exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification and, with the supervisor Gate 2 audit, records the actual verified full-range item/comments poststate.
- Generated/coverage report state:
  - Research tracker before the approved B009 execution: covered, `88/93`, average `90.5`, one emitted code block, and B002 as the sole executed direct report/earlier implementation authority. Normal B009 execution is expected to make the tracker count both direct reports.
  - Memory coverage: `coded`, owner/emitter `000057`, target-generated route `auto-generated/NexusTK/map/ItemObjectPane.cpp`.
  - Generated coverage: `emits_code:true`, reconstructable `88%`, current zero-byte/lifecycle summary.
  - Generated source contains one exact UID0002WT definition and no UID0002WT empty marker.

## Ranked Ownership Analysis

### 1. [UID:000057] FlyingObjectPane

- Evidence for: current target metadata; allocation paths in `sub_50DFA0` and `sub_512D60` allocate from `unk_69B9D4` then call `sub_537CB0`; `sub_537CB0` stores FlyingObjectPane vtable bases; `sub_514BA0` and `sub_53D1A0` free through the same pool; [UID:000057] and [UID:00034Z] support the constructor/vtable/destructor chain.
- Evidence against: exact original global spelling is not embedded as a symbol; MapPane callers allocate the object as part of caller-side packet/update handling.
- Decision: keep [UID:000057] as canonical owner and emitter. MapPane calls are creation/use evidence, not source ownership transfer.

### 2. [UID:0000KG] ItemObjectPane file root

- Evidence for: `ItemObjectPane.cpp` is the generated file route for both ItemObjectPane and FlyingObjectPane; audit-time command `000000018722` places UID0002WT in `auto-generated/NexusTK/map/ItemObjectPane.cpp` with the exact marker/definition topology recorded below.
- Evidence against: file root is broader than the exact class owner. `CANONICAL_OWNER` should not bypass the class when the class is clear and gate-clearing.
- Decision: use [UID:0000KG] as source placement/source-root support only, not direct canonical owner.

### 3. [UID:0000MM] PoolAllocator

- Evidence for: static constructor, allocation, free, and cleanup all use allocator mechanics at `0x004b13d0`, `0x004b1400`, `0x004b14c0`, and `0x004b1520`.
- Evidence against: [UID:0000MM] owns allocator mechanics, not concrete consumer-owned static instances; [UID:0000TI] and B002 explicitly distribute concrete pool declarations beside consuming modules.
- Decision: reject as canonical owner/emitter for UID0002WT. Keep as allocator-mechanics support.

### 4. [UID:0002B4] ObjectPanePoolStaticStorage aggregate

- Evidence for: physically contains UID0002WT in a contiguous static-pool cluster.
- Evidence against: mixed-owner aggregate over ten distinct concrete pool declarations; current metadata is `RECONSTRUCTABLE:FALSE`, owner `NONE`, blank emitters.
- Decision: keep aggregate non-emitting and parentless; exact children carry the source declarations.

### 5. MapPane caller/update paths

- Evidence for: `sub_50DFA0` and `sub_512D60` allocate FlyingObjectPane objects and add/schedule them from MapPane-side update logic.
- Evidence against: caller-side creation does not own the static pool declaration; constructor/vtable/destructor/free pattern points to FlyingObjectPane class/module ownership.
- Decision: use MapPane paths as xref/call-flow evidence only.

### Proposed new file/grouping, if applicable

- Proposed owner/name/path: none. Existing [UID:0000KG] `ItemObjectPane` under `NexusTK/map/` is the source-root route.
- Likely full contents: existing file already groups `ItemObjectPane` and `FlyingObjectPane`, their vtable children, static pools, and item/flying executable children.
- Candidate related items that belong: [UID:000057] `FlyingObjectPane`, constructor/bounds/tick children, scalar destructor support, vtable marker [UID:00034Z], and pool storage [UID:0002WT].
- Candidate related items rejected: broad [UID:0002B4] aggregate, adjacent [UID:0002WS] and [UID:0002WU], [UID:0000MM] allocator mechanics, and `MapPane` caller/update code.
- Standalone, narrow, or broad source-file inference: narrow class-owned static declaration inside existing `ItemObjectPane.cpp` source file.

## Source Placement

- Recommended source file/class/global/module placement: `NexusTK/map/ItemObjectPane.cpp`, emitted through [UID:000057] `FlyingObjectPane` and [UID:0000KG] `ItemObjectPane`.
- Why this placement fits source-tree and subsystem context: `FlyingObjectPane` is the temporary item-flight companion class documented under `ItemObjectPane.cpp`; the static pool is constructed/freed with `FlyingObjectPane` objects and not with base ObjectPane, AttachedObjectPane, or generic PoolAllocator ownership.
- Rejected placements and why:
  - `PoolAllocator.cpp`: owns generic allocator code, not concrete object-pane pools.
  - `MapPane.cpp`: caller/update code creates the object but does not own the class-specific pool declaration.
  - `ObjectPanePoolStaticStorage` aggregate: binary adjacency only, not a source module.
  - raw data/byte array placement: not source-facing for a zero-initialized static object with constructor/destructor wrappers.
- Remaining placement uncertainty: exact historical `.cpp`/header spelling and exact static variable spelling are inferred. Existing file route and sibling accepted reports make this safe for first-draft declaration but still below final-audit confidence.

## Range / Split / Padding / Reclassification Analysis

- Exact range/boundary facts:
  - Target half-open range is `0x0069b9d4-0x0069b9fc`, length `0x28` / 40 bytes (Verified with `tools/int_convert.py`).
  - Predecessor `0x0069b9ac-0x0069b9d4` is [UID:0002WS] ItemObjectPane pool, also 40 zero bytes in current MCP.
  - Successor `0x0069b9fc-0x0069ba24` is [UID:0002WU] BalloonObjectPane pool, also 40 zero bytes in current MCP.
  - `0x0069ba24` begins a separate four-byte global slot after the pool cluster.
- Children/subranges to create, repair, merge, leave ignored, or leave no-owner: none.
- Padding/table/data/code distinctions: the target is `.data` static object storage, not code, padding, a vtable, or a raw source-authored byte table.
- Parent/container impact: [UID:0002B4] remains a non-emitting aggregate; no container score/metadata change is needed.

## Negative Evidence Summary

- Consumer/read xrefs alone were not used as ownership proof. The two MapPane allocation paths are strong when combined with constructor/vtable/destructor/free evidence, but they remain caller evidence rather than direct MapPane ownership.
- No current MCP data-name/global result proves original `g_flyingObjectPanePool` spelling; the name is an inferred source-facing name from [UID:0000TI] and accepted sibling pool reports.
- No IDA evidence supports the old `ff` byte wording; current bytes are all `0x0`.
- No evidence supports raw byte array emission, broad aggregate emission, or relocation of concrete pool declarations into `PoolAllocator.cpp`.
- No split or new child page is justified because target and neighbors have exact non-overlapping `0x28` boundaries and distinct xref sets.

## IDA Rename / Type / Comment Recommendations

- B009 performed no IDA mutation. The following preserves the exact historical supervisor Gate 2B handoff from fresh read-only MCP client session `f01d54c1-776e-4330-9a29-5d582c5183f2` against active IDB session `5a570ede`. `server_health` returned `status:ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, image base `0x00400000`, Hex-Rays/strings ready, and `auto_analysis_ready:false`; the successful bounded IDB-backed reads established availability and the safe prestate. The supervisor subsequently applied and independently verified every accepted action, with exact poststate recorded after the historical inventory.

| Entity | Literal historical IDA prestate | Applied supervisor action | Evidence / safety | Verified readback | Classification |
| --- | --- | --- | --- | --- | --- |
| UDT `PoolChunkHeader` | `type_query` total `0`; `type_inspect`: `exists:false`, `Type not found: PoolChunkHeader`. Ordinal: none/not allocated. Declaration: absent. Base list: not applicable. Member list: not applicable. UDT comment text/repeatable state: not applicable because no type exists. Member comments: not applicable. | Declare `struct PoolChunkHeader { PoolChunkHeader *next; };`, size `0x4`, `next` at `+0`. | `AllocatePoolChunk` prepends one pointer-sized chunk link; destructor helper walks it. | Exists at ordinal `934`, size `4`, no bases, one `next` member at `0`, member type `PoolChunkHeader *`. | APPLIED / VERIFIED |
| UDT `PoolFreeBlock` | `type_query` total `0`; `type_inspect`: `exists:false`, `Type not found: PoolFreeBlock`. Ordinal: none/not allocated. Declaration: absent. Base list: not applicable. Member list: not applicable. UDT comment text/repeatable state: not applicable because no type exists. Member comments: not applicable. | Declare `struct PoolFreeBlock { PoolFreeBlock *next; };`, size `0x4`, `next` at `+0`. | Allocate/free thread the first dword of free blocks. | Exists at ordinal `935`, size `4`, no bases, one `next` member at `0`, member type `PoolFreeBlock *`. | APPLIED / VERIFIED |
| UDT `PoolAllocator` | Exists at exact ordinal `444`; kind `struct`; literal declaration string `PoolAllocator`; size `40` (`0x28`); base list `[]` (the sole UDT member is not a base); exact projected member list: index `0`, name `_opaque`, offset `0x0`, size `40`, type `unsigned __int8[40]`. The current safe MCP `type_query`/`type_inspect` schema does not project UDT or member comment fields, so this row makes no assertion about those fields. | Exact `NO_CHANGE`: do not redeclare, replace, rename, or comment `PoolAllocator`; preserve ordinal `444`, size `0x28`, and the current `_opaque[40]` member model. This is the final disposition, not deferred inspection. | The target data-item repair needs only the already-present `PoolAllocator` name and exact `0x28` size. Source-level field evidence remains documented, but changing this shared UDT is unnecessary for UID0002WT and cannot be justified from an unsupported comment prestate. | Remains identical: ordinal `444`, kind `struct`, size `40`, sole `_opaque` member at `0x0`, size `40`, type `unsigned __int8[40]`. | NO_CHANGE / VERIFIED |
| Data `0x0069b9d4-0x0069b9fc` | Name query: `g_flyingObjectPanePool` at `0x69b9d4`; global query size `1`. Exact historical affected-item partition and every address-comment state are inventoried below; there were forty separate one-byte items, not one typed `0x28` item. | After the two helper UDT declarations, run `make_data` at `0x0069b9d4` with the existing type `PoolAllocator`, name `g_flyingObjectPanePool`, and replacement of existing byte items. Set regular comment `FlyingObjectPane file-local PoolAllocator storage; block size 0x144, 4 blocks per chunk.` Set repeatable comment `Source reconstruction: static PoolAllocator g_flyingObjectPanePool(0x144, 4); startup, unwind, scalar-delete, and shutdown wrappers are compiler-generated.` | Forty zero bytes, exact existing `0x28` UDT size, no interior dword xrefs, six exhaustive base operands, exact predecessor/successor boundaries. Existing type/name are preserved, not reinvented. | One bounded `is_data` item, head `0x69b9d4`, end `0x69b9fc`, size `40`, type `struct PoolAllocator`; same global name with size `40`; both exact comments; all six xrefs/bytes/neighbors unchanged. | APPLIED / VERIFIED |

Exact historical pre-action affected-item partition for the full replacement interval `[0x0069b9d4,0x0069b9fc)`. The `Width` column is the literal `end - head`; `Name state` preserves both the independent entity/global result and the literal `inspect_items.name` field where they differ.

| Address queried | Literal item head | Literal item end | Size | Width | Name state | Type state | `is_code` / `is_data` | Regular / repeatable comment state |
| --- | --- | --- | ---: | --- | --- | --- | --- | --- |
| `0x0069b9d4` | `0x69b9d4` | `0x69b9d5` | `1` | `1 byte` | entity/global name `g_flyingObjectPanePool`; literal item name `""` | `struct PoolAllocator` | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9d5` | `0x69b9d5` | `0x69b9d6` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9d6` | `0x69b9d6` | `0x69b9d7` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9d7` | `0x69b9d7` | `0x69b9d8` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9d8` | `0x69b9d8` | `0x69b9d9` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9d9` | `0x69b9d9` | `0x69b9da` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9da` | `0x69b9da` | `0x69b9db` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9db` | `0x69b9db` | `0x69b9dc` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9dc` | `0x69b9dc` | `0x69b9dd` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9dd` | `0x69b9dd` | `0x69b9de` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9de` | `0x69b9de` | `0x69b9df` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9df` | `0x69b9df` | `0x69b9e0` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e0` | `0x69b9e0` | `0x69b9e1` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e1` | `0x69b9e1` | `0x69b9e2` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e2` | `0x69b9e2` | `0x69b9e3` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e3` | `0x69b9e3` | `0x69b9e4` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e4` | `0x69b9e4` | `0x69b9e5` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e5` | `0x69b9e5` | `0x69b9e6` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e6` | `0x69b9e6` | `0x69b9e7` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e7` | `0x69b9e7` | `0x69b9e8` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e8` | `0x69b9e8` | `0x69b9e9` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9e9` | `0x69b9e9` | `0x69b9ea` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9ea` | `0x69b9ea` | `0x69b9eb` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9eb` | `0x69b9eb` | `0x69b9ec` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9ec` | `0x69b9ec` | `0x69b9ed` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9ed` | `0x69b9ed` | `0x69b9ee` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9ee` | `0x69b9ee` | `0x69b9ef` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9ef` | `0x69b9ef` | `0x69b9f0` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f0` | `0x69b9f0` | `0x69b9f1` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f1` | `0x69b9f1` | `0x69b9f2` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f2` | `0x69b9f2` | `0x69b9f3` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f3` | `0x69b9f3` | `0x69b9f4` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f4` | `0x69b9f4` | `0x69b9f5` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f5` | `0x69b9f5` | `0x69b9f6` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f6` | `0x69b9f6` | `0x69b9f7` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f7` | `0x69b9f7` | `0x69b9f8` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f8` | `0x69b9f8` | `0x69b9f9` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9f9` | `0x69b9f9` | `0x69b9fa` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9fa` | `0x69b9fa` | `0x69b9fb` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |
| `0x0069b9fb` | `0x69b9fb` | `0x69b9fc` | `1` | `1 byte` | `""` (absent) | `""` (absent) | `false` / `false` | `""` / `""` (both absent) |

Applied supporting declarations from the Gate 2B handoff:

```cpp
struct PoolChunkHeader
{
    PoolChunkHeader *next;
};

struct PoolFreeBlock
{
    PoolFreeBlock *next;
};
```

- Required invariants: `sizeof(PoolChunkHeader) == 4` and `sizeof(PoolFreeBlock) == 4`. Existing `PoolAllocator` must remain ordinal `444`, size `0x28`, with sole `_opaque[40]` member at `+0`; no `PoolAllocator` UDT/comment change is authorized.
- Exact supervisor operation order, now completed and independently verified:
  1. `declare_type` only the two missing helper declarations above; immediately `type_query`/`type_inspect` both and stop if either size/member differs. Re-read `PoolAllocator` only through `type_query`/`type_inspect` and require the exact unchanged ordinal/size/member projection above; issue no `PoolAllocator` declaration or comment action.
  2. `make_data` with `{"addr":"0x0069b9d4","type":"PoolAllocator g_flyingObjectPanePool","name":"g_flyingObjectPanePool","delete_existing":true}`.
  3. `set_comments` with the exact regular text in the action row, then `set_repeatable_comments` with the exact repeatable text.
  4. Re-run `entity_query`, `inspect_items`, `type_query`, `type_inspect`, `get_comments`, `get_bytes`, and `xref_query`; require the expected readback, including unchanged `PoolAllocator`, before Gate 2B acceptance.
- Protected dependencies intentionally left unchanged:
  - `0x0041a0a0` current name `sub_41A0A0`, current disassembly prototype `int sub_41A0A0(void)`, regular/repeatable comments absent: compiler static-construction/atexit wrapper, not source-authored API.
  - `0x0060c350` current name `sub_60C350`, current prototype `void sub_60C350(void)`, regular/repeatable comments absent: compiler cleanup wrapper, not source-authored API.
  - `0x00514ba0` current name `sub_514BA0`, current prototype `void __cdecl sub_514BA0(_DWORD *)`: compiler constructor-unwind free wrapper; no source rename/type action here.
  - `0x0053d1a0` current name `sub_53D1A0`, size `0x97`: compiler scalar deleting destructor with its own exact by-memory owner; no duplicate action here.
  - `0x00512d60` current name `sub_512D60`, current prototype `char __thiscall(_DWORD *, unsigned __int8 *)`; [UID:0003TR] owns its source/IDA naming work.
  - `0x00537cb0` current name `sub_537CB0`, size `0xb0`; [UID:0003HV] owns its constructor naming/type work.
  - `0x0050dfa0` current name `sub_50DFA0`, size `0x157`; its MapPane helper role is understood, but this storage report does not have enough original/API evidence to override its separate broad MapPane owner.
- Applied poststate summary:
  - `PoolChunkHeader`: ordinal `934`, size `4`, sole `PoolChunkHeader *next` at `+0`.
  - `PoolFreeBlock`: ordinal `935`, size `4`, sole `PoolFreeBlock *next` at `+0`.
  - `PoolAllocator`: exact `NO_CHANGE`, ordinal `444`, size `0x28`, sole `unsigned __int8 _opaque[40]` at `+0`.
  - Target: one 40-byte `is_data` item of type `struct PoolAllocator`, global `g_flyingObjectPanePool` size `40`, with both exact regular/repeatable comments from the action row.
  - Invariants: forty target bytes and checked neighboring bytes remain zero; six base xrefs remain exact; checked interiors remain at zero xrefs; predecessor/successor remain separate; all protected identities above remain unchanged.
  - Save/catalog proof: IDB SHA256 `A48E4C7BB251D16ED4BB7AC337CDA8E9B93D92FAE4DD5C711C9F91FD9495D88E`, `143189795` bytes, last write `2026-07-29T00:36:31-04:00`; supervisor catalog entry `0344` is exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification, and the Gate 2 audit is complete.
- Negative constraints: do not overwrite predecessor `0x0069b9ac-0x0069b9d4` or successor `0x0069b9fc-0x0069ba24`; do not change bytes/xrefs; do not create a `FlyingObjectPane` UDT here; do not rename compiler wrappers as source methods; do not emit startup/EH/scalar/cleanup wrappers; do not claim `g_flyingObjectPanePool` as symbol-proven original spelling.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes, and already implemented. UID0002WT is `RECONSTRUCTABLE:TRUE`, has emitter [UID:000057], routes to a valid source root, and is `88/93`.
- CPP block disposition: preserve this exact existing formal block text:

```cpp
static PoolAllocator g_flyingObjectPanePool(0x144, 4);
```

- H block disposition: preserve the formal H block as exactly blank. Exact no-header-code proof: the recommended definition has file-local `static` internal linkage; no caller requires an exported declaration; adding `extern` would contradict that linkage and duplicate the target's source responsibility.
- Third-party import directive, when applicable: not applicable; this is NexusTK-owned static storage, not a third-party embed.
- Reason it preserves exact original behavior: the binary's static constructor wrapper passes `0x144` and `4` for this address, and static cleanup invokes the allocator destructor. A module-scope static object definition produces those effects rather than hand-authored raw bytes.
- Reason it matches plausible original precompiled mid-2000s developer source shape: a file-scope `static PoolAllocator` object beside the class that allocates/frees `FlyingObjectPane` objects is ordinary Visual C++ era source. It avoids synthetic `unk_69B9D4`, raw arrays, decompiler labels, or monolithic pool inventory files.
- Inferred source-facing names/types/fields used instead of IDA/decompiler labels: `g_flyingObjectPanePool` instead of `unk_69B9D4`; `PoolAllocator` instead of untyped 40-byte data.
- Naming/coding style convention used and evidence for consistency: accepted object-pane pool reports and [UID:0000TI] use `g_*ObjectPanePool` names for concrete object-pane pools. The exact original spelling remains inferred.
- Reason CPP should remain blank, if applicable: not applicable; CPP is correctly nonblank.
- Reason H should remain blank: exact file-local internal-linkage proof above.
- Exact no-code proof, if not eligible: not applicable.

## Final Recommendation

- Exact changes recommended:
  - Preserve current target `88/93`, owner/emitter `000057`, reconstructable true, exact CPP, and blank H.
  - Completed under the authorized callback: add current MCP/IDA prestate evidence and correct historical [UID:0000KG] score citations to current `91/93` while preserving B002 detail.
  - Completed by supervisor Gate 2B: applied the two helper UDT declarations plus exact data-item/address-comment handoff, preserved `PoolAllocator` as exact `NO_CHANGE`, and independently verified literal poststate and saved-IDB identity.
  - Completed by supervisor coverage reconciliation: replaced the stale manual by-memory coverage row with the exact text below and validated it at dated command checkpoint `000000018675`; the later independent target/manual/IDA reconciliation is recorded by catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED`.
- Exact disposition and implementation proof for UID0002WT: current target/formal CPP/generated code and tracker prove B002 already performed the earlier ordinary implementation. B009 did not duplicate that ordinary implementation and is approved for normal execution as an additional direct supplemental report containing independently verified current evidence, IDA poststate, coverage history, and generated closure. After execution, the tracker should count both B002 and B009 as direct reports while preserving B002 as the earlier implementation authority.
- Exact parent assignments recommended: keep `CANONICAL_OWNER:000057` and `EMITTER_UIDS:000057`; [UID:0000KG] remains source-root route.
- Exact items left no-owner/non-emitting and why: [UID:0002B4] remains owner `NONE`, non-emitting, because it is a mixed aggregate. [UID:0000TI] remains non-emitting because it is a cross-cutting inventory.
- Exact future work outside this target: separately-owned MapPane/FlyingObjectPane function/type improvements remain with their exact pages. They are not used as excuses or blockers for this storage target.

## Recommended Target Doc Changes

- Target path: `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`.
- Exact report facts to incorporate:
  - Current MCP client session `8b65c786-e32a-4aeb-90fe-11bdabe1d421`, active IDB `5a570ede`, server status OK, successful bounded reads despite `auto_analysis_ready:false`.
  - Current bytes remain forty zero bytes; six base xrefs/pointer matches remain exhaustive; all checked interior dwords have zero xrefs.
  - Historical IDA prestate: name `g_flyingObjectPanePool`, base tinfo `struct PoolAllocator`, one-byte containing item, opaque 40-byte UDT, absent regular/repeatable comments; preserve the full forty-row inventory as safety evidence.
  - Current applied poststate: helper UDTs at ordinals `934`/`935`; unchanged `PoolAllocator` ordinal `444`; one 40-byte typed `is_data` item; retained `g_flyingObjectPanePool` size `40`; both exact comments; unchanged bytes/xrefs/neighbors/protected identities; saved IDB SHA256 `A48E4C7BB251D16ED4BB7AC337CDA8E9B93D92FAE4DD5C711C9F91FD9495D88E`.
  - Direct xrefs: `0x0041a0a7`, `0x0050e06b`, `0x00512e8e`, `0x00514bc5`, `0x0053d1f0`, `0x0060c350`.
  - Static constructor/cleanup bodies and exact constants remain as already documented.
  - Owner-side allocation paths call `sub_4B1400(unk_69B9D4)` then `sub_537CB0`.
  - `sub_537CB0` constructor stores FlyingObjectPane vtable bases and is called only by the two owner-side paths checked here.
  - `sub_53D1A0` conditionally frees `this` through `unk_69B9D4`.
  - Completed: corrected target sentences that reported [UID:0000KG] at historical `85/86` and `86/88`; current score is `91/93`.
- Metadata/score/owner/emitter/reconstructable/C++ changes:
  - No metadata, owner, emitter, reconstructable, CPP, or H change. Preserve exact current values/blocks.
- Historical/stale assumptions, rejected alternatives, and negative evidence to preserve:
  - Older `ff` byte wording is stale.
  - Older no-assignment/below-gate wording is stale because the target now has owner/emitter [UID:000057].
  - Reject raw byte arrays, comment-only empty marker, `PoolAllocator.cpp` direct ownership, aggregate ownership, MapPane caller ownership, direct by-file canonical owner, and neighbor merge.

## Recommended Support Doc Changes

- [UID:000057] `by-class/FlyingObjectPane.md`: inspected; already contains the exact pool definition/lifecycle/source route at report-level detail. No change recommended.
- [UID:0000KG] `by-file/ItemObjectPane.md`: inspected; already contains the exact pool definition/lifecycle/source route and current `91/93` state. No change recommended.
- [UID:00034Z], [UID:0002B4], [UID:0000TI], [UID:0000MM], and [UID:0001VK]: inspected; current roles/details are sufficient. No change recommended.
- Metadata/link/score/coverage/source-placement changes: none for support docs. The stale target score citation and stale manual by-memory row have both been corrected; no ordinary or manual documentation correction remains.

## Score And Metadata Recommendation

- Current score/metadata: `88/93`, owner/emitter [UID:000057], `RECONSTRUCTABLE:TRUE`, exact CPP present, H blank.
- Recommended score/metadata: unchanged `88/93` and unchanged routing/blocks.
- Score rationale and reason not higher/lower:
  - Not lower: range, storage, lifecycle, type, owner/emitter, source placement, CPP, H no-code proof, generated output, and historical corrections are complete and independently reconfirmed.
  - Not higher: current research corroborates rather than materially expands the already-detailed source page; exact original variable spelling/linkage and original numeric-versus-`sizeof` expression remain unrecoverable. The completed IDA cleanup improves database quality, not target documentation certainty enough to justify score inflation.
- Score-improvement attempt:
  - Bytes/emitter/owner/source/range/CPP/H blockers: all resolved and already implemented.
  - Name/linkage blocker: best defensible inference selected; original-symbol absence is not used to retain a raw label.
  - IDA blockers: resolved by verified Gate 2B application. Helper UDTs, target item boundary, and address comments now match the report; existing `PoolAllocator` remains exact final `NO_CHANGE`.
  - Manual coverage blocker: resolved. The supervisor applied the exact replacement text and dated command checkpoint `000000018675` validated the coverage file; catalog entry `0344` now records `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification.
- Metadata fields to change or leave unchanged:
  - Leave every target metadata field and both formal blocks unchanged.

## Open Questions With Attempted Resolution

- Open question: are current bytes `ff` or zero?
  - Evidence checked: current MCP `get_bytes` on target and neighbors.
  - Best supported resolution: zero-filled storage. Old `ff` wording is stale.
  - Score/C++ impact: already incorporated; no current score/code change.
- Open question: can the exact original variable name be proven?
  - Evidence checked: current MCP `entity_query` names/globals, target/support docs, [UID:0000TI], B002/B009 prior reports.
  - Best supported resolution: exact original symbol is unavailable; use inferred `g_flyingObjectPanePool` from accepted project naming convention and source placement.
  - Remaining unresolved impact: confidence cap below final audit; not a blocker for first-draft C++.
- Open question: should the target remain blank because FlyingObjectPane class body C++ is still blank?
  - Evidence checked: by-structure C++ gate, target metadata, generated route, wrapper constants, accepted source-family static pool reports.
  - Best supported resolution: no. The exact source-declared storage already emits independently.
- Open question: should `sizeof(FlyingObjectPane)` replace `0x144`?
  - Evidence checked: wrapper disassembly and class support docs.
  - Best supported resolution: use exact `0x144` until class layout/source body is final-audit ready. This is behavior-preserving and avoids premature dependency on inferred layout expressions.
- Open question: should support docs beyond target/class/file be edited?
  - Evidence checked: aggregate/global/allocator/vtable docs.
  - Best supported resolution: no support-doc edits are needed. Target evidence/stale score citation, manual row, and IDA state have all been reconciled; only final supervisor claim verification and normal additional-direct B009 report execution/archive remain.
- Open question: can IDA safely consolidate the one-byte target item, and should it replace the opaque `PoolAllocator` UDT?
  - Evidence checked: item/type/comment prestate, all bytes, exact neighbors, all base routes, interior dword xrefs, absolute/RVA pointer search, allocator field accesses, `CRITICAL_SECTION` size, and support layout docs.
  - Best supported resolution: applied and verified. The historical forty byte-items were consolidated into one exact `PoolAllocator` item and both address comments were applied, while the shared `PoolAllocator` UDT remained exact `NO_CHANGE`.
- Questions remaining unresolved: only historical source spelling/linkage and numeric-versus-`sizeof` expression. All reasonable current evidence routes were exhausted; neither affects behavior, ownership, range, CPP/H validity, or current `88/93`. There is no deferred "needs investigation" item.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

- Historical pre-application rows inspected:
  - `by-memory/-coverage-report.md` UID0002WT was stale at `82%` with all-`ff` and below-gate/no-assignment wording.
  - `by-class/-coverage-report.md` UID000057 currently describes the accepted owner/inheritance/pool route; no target-specific replacement is needed.
  - `by-file/-coverage-report.md` UID0000KG is current at `91%` and includes separate item/flying pools; no target-specific replacement is needed.
- Supervisor coverage closure: the exact UID0002WT replacement below was applied in `by-memory/-coverage-report.md` between UID0002WS and UID0002WU. Dated command checkpoint `000000018675` validated that file with exit `0`, `ok: 1`; only pre-existing UID00039L/UID0003T6 diagnostics remained, and its readback exactly matched this payload. Current independent target/manual/IDA reconciliation is recorded by catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED`:

```markdown
        - [UID:0002WT][0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage](by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md) 0x0069b9d4-0x0069b9fc | static PoolAllocator storage | FlyingObjectPanePoolStaticStorage : reconstructable : 88% : very-strong : Exact zero-filled 0x28-byte FlyingObjectPane pool storage with PoolAllocator(0x144, 4) startup construction, two allocation-plus-constructor routes, constructor-failure and scalar-delete returns, registered shutdown destruction, formal file-local g_flyingObjectPanePool definition, and FlyingObjectPane owner/emitter route through ItemObjectPane.cpp.
```

- Reason B agent did not apply it directly: manual coverage files are supervisor-owned collision points under the current assignment. The supervisor applied and verified this payload.

## Follow-Up Actions

- Supervisor actions:
  - Initial Gate 1 acceptance and the narrow B009 target callback are complete for exact accepted SHA `F3C732722FBE0C9C761F2F8E690D1962134F2AC13291F4C76D40EE9BEB60330D`; an earlier final Gate 1 passed exact SHA `E1094173949E432B886C0831014FB6982B765931EFF23883782F046E15851828` before the coverage-only reconciliation, and this repaired artifact now awaits fresh supervisor Gate 1/claim verification.
  - Gate 2B IDA application/readback, IDB save, and Gate 2 audit are complete and independently verified; catalog entry `0344` is now exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification.
  - Target validation `000000018674` and dated manual coverage replacement/validation checkpoint `000000018675` are complete. Command `000000018697` is retained only as a dated semantic checkpoint. Audit-time generated checkpoint `000000018722` has the exact CPP/tracker hashes and topology recorded below; later supervisor validators may advance physical freshness headers, and final lifecycle-time readback is authoritative.
  - Remaining supervisor work: final claim-by-claim verification of this report revision, then normal `execute_report`/archive as an additional direct B009 supplemental report. The resulting tracker should count both B002 and B009 while retaining B002 as the earlier implementation authority.
- A-agent actions: none required by this report-only pass.
- B009 future callback actions:
  - None currently. The authorized ordinary target callback, post-IDA reconciliation, target/manual validations, catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED`, audit-time command `000000018722` generated semantic checkpoint, and lease releases are complete; await fresh final supervisor Gate 1/claim verification, authoritative final lifecycle-time generated readback, and normal additional-direct B009 execution/archive.

## Confidence

- Recommendation confidence: high.
- Score confidence: high for unchanged `88/93`.
- Remaining uncertainty:
  - Exact original static variable spelling and linkage are inferred.
  - Original numeric-versus-`sizeof` expression is not recoverable; direct numeric form preserves behavior.
  - IDA boundary/comments/helper UDT work is complete; the opaque `PoolAllocator` UDT correctly retains final exact `NO_CHANGE` disposition.

## Validator Results

- Exact callback command run from `source-3/project-documentation`:

> Executable block R001 was removed from this report and preserved verbatim in [0002WT-FlyingObjectPanePoolStaticStorage-source-quality-removed.md](0002WT-FlyingObjectPanePoolStaticStorage-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

- Result: exit `0`, `ok: 1`, command ID `000000018378`, command timestamp/generated refresh timestamp `2026-07-28T16:00:27-04:00`, and `generated_refresh: completed`.
- Current reconciliation validation: the same scoped command was rerun without any by-* edit on 2026-07-28. It exited `0` with `ok: 1`, command ID `000000018503`, command timestamp `2026-07-28T23:23:13-04:00`, and `generated_refresh: completed`. The only target-specific diagnostic remains the pre-existing `missing_ref_uid 0003HV`; no UID0002WT validation failure was reported.
- Post-IDA ordinary-document reconciliation command run serially from `source-3/project-documentation`:

> Executable block R002 was removed from this report and preserved verbatim in [0002WT-FlyingObjectPanePoolStaticStorage-source-quality-removed.md](0002WT-FlyingObjectPanePoolStaticStorage-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

- Post-IDA reconciliation result: exit `0`, `ok: 1`, command ID `000000018630`, command timestamp `2026-07-29T00:39:43-04:00`. The validator inserted one missing UID000057 link, updated projected stats, and reported only the pre-existing `missing_ref_uid 0003HV`; generated refresh was initially deferred. Final target SHA256 after validator-owned link insertion is `1C5179F06AF204B29B61DE19F250854CBBC97BF7FE6C645A820668A5F8EFD5CD`, size `24586` bytes, last write `2026-07-29T00:39:44.7215509-04:00`.
- Final supervisor target validation: command `000000018674` exited `0` with `ok: 1`; only the pre-existing `missing_ref_uid 0003HV` diagnostic remained.
- Dated supervisor manual by-memory coverage validation checkpoint: command `000000018675` exited `0` with `ok: 1`; only the pre-existing UID00039L/UID0003T6 diagnostics remained, and that command's generated refresh completed.
- Scoped diagnostic: one pre-existing `missing_ref_uid 0003HV` warning remains because UID0003HV is not present in `validator.ini`; validation still completed successfully. The validator inserted missing links for UID000057 and UID0000KG in newly added prose.
- Validator-owned side effects included registry/report/metadata refreshes, `projected_stats_update`, `research_tracker_update`, `memory_auto_coverage_update`, and generated refresh. B009 did not manually edit those files.
- Generated CPP callback readback: `auto-generated/NexusTK/map/ItemObjectPane.cpp` had validator command ID `000000018378`; lines 117-118 contained UID0002WT at `88/93` followed by exactly `static PoolAllocator g_flyingObjectPanePool(0x144, 4);`. Its callback-time SHA256 was `9B0099916BA7B9E0917C82BA24CCDE6918CDFE8DF52384E7D2317FBD9F7138D1`.
- Dated B009 implementation generated CPP semantic checkpoint: concurrent validator work advanced `auto-generated/NexusTK/map/ItemObjectPane.cpp` to command ID `000000018507`, timestamp `2026-07-28T23:23:44-04:00`, and SHA256 `8F734A7E659F295E154374EAAFCEB6C9494DC584A757A195A5123B75D2CCD12F`. Lines 117-118 still contain the exact UID0002WT `88/93` marker and `static PoolAllocator g_flyingObjectPanePool(0x144, 4);` definition. This dated validator-owned refresh does not alter the callback result.
- Dated B009 post-IDA generated CPP semantic checkpoint: later validator command `000000018634`, timestamp `2026-07-29T00:40:30-04:00`, refreshed `auto-generated/NexusTK/map/ItemObjectPane.cpp` to SHA256 `DE2F5A0E3E983704C11630C57245C450C1A33D5A729545E49383523FEEDD39F6`; lines 117-118 still contain UID0002WT at `88/93` and exactly `static PoolAllocator g_flyingObjectPanePool(0x144, 4);`.
- Dated B009 post-IDA generated H semantic checkpoint: no `auto-generated/NexusTK/map/ItemObjectPane.h` existed, matching the target's deliberately blank formal H block.
- Dated generated tracker callback checkpoint before the approved B009 execution: `auto-generated/-ag-research-tracker.md` SHA256 was `16244927353BD3D01F99156F53D086DABC44DD434BACF909B3B7D4630B4694BF`; UID0002WT remained `88/93`, reconstructable, coded, and linked only to the already executed B002 report because B009 had not yet entered the normal execution lifecycle.
- Dated B009 post-IDA tracker semantic checkpoint before the approved B009 execution: SHA256 was `F84181758D5157EDBAE56FA2C4C949C25F4EC112827ADC1A72EF8FF26385D677`; UID0002WT remained `88/93`, reconstructable, coded, direct/additional/total/executed counts `1/0/1/1`, and linked only to the already executed B002 report because B009 was still unexecuted at that historical checkpoint.
- Dated supervisor lifecycle-time generated CPP checkpoint after command `000000018675`: `auto-generated/NexusTK/map/ItemObjectPane.cpp` SHA256 `34DE8B25A86C1259925E13D9602D03BD869EC068274512D0086474D8E1F981B1`; lines 117-118 contained UID0002WT at `88/93` and exactly `static PoolAllocator g_flyingObjectPanePool(0x144, 4);`.
- Dated supervisor lifecycle-time tracker checkpoint after command `000000018675`, before the approved B009 execution: `auto-generated/-ag-research-tracker.md` SHA256 `6822F8AF8D45129B791A774BAABD33673BE0CC70BC12FF9D80C30372ED4C75CB`; UID0002WT remained `88/93` and credited executed B002 as the earlier implementation authority.
- Dated supervisor autogen CPP semantic checkpoint after command `000000018697` at `2026-07-29T01:40:41-04:00`: `auto-generated/NexusTK/map/ItemObjectPane.cpp` SHA256 `4568E5513FE5495F80FFD612E8184AEF633D47AF70F4F030F512CCE963337FE3`; it contained exactly one UID0002WT `88/93` marker, exactly one `static PoolAllocator g_flyingObjectPanePool(0x144, 4);` definition, and no UID0002WT empty-emitter marker.
- Dated supervisor autogen tracker semantic checkpoint after command `000000018697` at `2026-07-29T01:40:41-04:00`: `auto-generated/-ag-research-tracker.md` SHA256 `184BAA75C0E37D1A2292C839BC13FBE0DB35E97B674415BC8CCC571F4EC30878`; UID0002WT remained `88/93` with one direct B002 report before approved B009 execution.
- Audit-time generated CPP semantic checkpoint under command `000000018722`: `auto-generated/NexusTK/map/ItemObjectPane.cpp` SHA256 `8785F49D76042D1B3787CAFA3BCB2B5B0269ED5817F99D18FE549BFA0539890C`, size `4588` bytes, `120` lines. It contains exactly one UID0002WT marker at `88/93`, exactly one `static PoolAllocator g_flyingObjectPanePool(0x144, 4);` definition, zero UID0002WT empty-emitter markers, and one unrelated UID000057 empty-emitter marker.
- Audit-time generated tracker semantic checkpoint under command `000000018722`: `auto-generated/-ag-research-tracker.md` SHA256 `DE9F3554F2E1E15E4D2EA03CD983DFF8FA46CC93243456824CCD4A05BF2202C3`, size `1665936` bytes, `6596` lines. UID0002WT remains `88/93` with one direct B002 report before approved B009 execution.
- Audit-time generated H semantic checkpoint under command `000000018722`: no `auto-generated/NexusTK/map/ItemObjectPane.h` exists, matching the target's deliberately blank formal H block.
- Final current manual coverage reconciliation: UID0002WT exactly matches the replacement payload in `Exact Manual Supervisor-Owned Coverage Or Tracker Text`; this current independent manual reconciliation is included in catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED` rather than attributed to dated command checkpoint `000000018675`.
- Generated provenance authority: every CPP/H/tracker checkpoint through command `000000018697` is dated semantic evidence. Command `000000018722` is the audit-time generated semantic checkpoint. Later supervisor validators may advance physical freshness headers; the final supervisor lifecycle-time readback is authoritative. Catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED` remains the current independent target/manual/IDA reconciliation state.
- No support-doc validator was required because no support by-* document changed. The supervisor-owned manual by-memory coverage file was validated successfully at dated command checkpoint `000000018675`.

## Changed Files

- Created:
  - none.
- Modified:
  - `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`: target-only supplemental evidence/history and stale-score correction; metadata/formal blocks unchanged.
  - `tools/leaser/Agents/Agent-B009/research/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md`: reconciled callback proof, ledger, validator readback, and checklist.
  - Validator-owned generated/audit state refreshed under command `000000018378`; no generated/audit file was manually edited by B009.
- Earlier reconciliation pass: only this existing report was modified to record dated command checkpoint `000000018503`, its then-newer validator-owned generated readback, and the Gate 2 readiness marker. No by-* document was edited in that pass. Validator-owned generated/project state refreshed as a side effect of the scoped command and was not edited manually.
- Post-IDA reconciliation pass:
  - `by-memory/0x0069b9d4-0x0069b9fc.FlyingObjectPanePoolStaticStorage.md`: preserved the full historical prestate and added the verified applied Gate 2B poststate, saved-IDB proof, and reconciliation history; metadata/formal blocks unchanged. Final SHA256 `1C5179F06AF204B29B61DE19F250854CBBC97BF7FE6C645A820668A5F8EFD5CD`.
  - `tools/leaser/Agents/Agent-B009/research/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md`: reconciled current state, evidence, ledger, IDA section, recommendations, validators, changed-file history, checklist, and lifecycle readiness without removing prior research or executed B002 authority.
  - No support by-* document required an edit. B009 leased only the target immediately before editing/validation and released it immediately after command `000000018630`; no active B009/UID0002WT lease remains.
  - Validator-owned generated/project files refreshed as side effects only; B009 did not edit them manually.
- Final supervisor coverage reconciliation pass:
  - Supervisor replaced the UID0002WT row in `by-memory/-coverage-report.md` with the exact report payload and validated it at dated command checkpoint `000000018675`.
  - Supervisor revalidated the ordinary target under command `000000018674`; both commands exited `0` with `ok: 1` and only the documented pre-existing diagnostics.
  - Validator-owned generated state refreshed under dated command checkpoint `000000018675`; that checkpoint is preserved above as history. Command `000000018697` and its generated hashes are also dated semantic evidence. Audit-time CPP/tracker hashes, sizes, line counts, and topology come from command `000000018722`; later supervisor validators may advance physical freshness headers, and final lifecycle-time readback is authoritative. B009 did not edit coverage or generated files.
  - B009 modified only this existing report during this final coverage reconciliation.
- Final lifecycle-disposition repair pass:
  - Only this existing report was modified to record the supervisor-approved normal execution/archive disposition for B009 as an additional direct supplemental report.
  - B002 remains the earlier implementation authority; expected post-execution tracker state counts both direct B002 and B009 reports.
- Current Gate 1 repair pass:
  - Only this existing report was modified to record catalog entry `0344` state `APPLIED_VERIFIED_RECONCILED`, demote command `000000018675` generated output to dated checkpoint evidence, and record supervisor autogen command `000000018697` at `2026-07-29T01:40:41-04:00` as the then-current CPP/tracker semantic readback.
  - No target, support, coverage, generated, IDA, catalog, audit, lifecycle, goal, notes, or validator file was edited or executed in this pass.
- Current bounded stale-currentness repair pass:
  - Only this existing report was modified to historicalize the already-replaced manual row and corrected target-score statements, demote command `000000018697` and its hashes to dated semantic evidence, and record command `000000018722` as the audit-time generated checkpoint.
  - The audit-time checkpoint records the exact supplied CPP/tracker hashes, sizes, line counts, marker/definition topology, unrelated UID000057 empty marker, and absent ItemObjectPane.h. Later supervisor validators may advance physical freshness headers; final lifecycle-time readback remains authoritative.
- Renamed:
  - none.
- Report execution: not yet run. B agents must not run report execution, archive, lifecycle, registry repair, dry-run/probing variants, or manual report moves. After final gates, the supervisor is approved to run normal `execute_report` and archive this B009 artifact as an additional direct supplemental report, producing tracker coverage for both B002 and B009 while preserving B002's earlier implementation authority.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor Gate 1 validation/authorization received for exact report SHA `F3C732722FBE0C9C761F2F8E690D1962134F2AC13291F4C76D40EE9BEB60330D`.
- [x] Earlier final Gate 1 passed exact pre-coverage-reconciliation SHA `E1094173949E432B886C0831014FB6982B765931EFF23883782F046E15851828`; this additive repair preserves all accepted research and implementation evidence and is presented for fresh supervisor Gate 1/claim verification.
- [x] Target/support edit plan completed and applied under authorization: target received current MCP/IDA prestate and stale [UID:0000KG] score corrections; support docs were inspected and correctly remained no-change.
- [x] Additional-UID planning completed: none declared, so no additional destination or validator rows are applicable.
- [x] Current target state and actual evidence checked recorded: historical session/IDB/health, bytes, base/interior xrefs, pointer scans, wrappers, consumers, constructor/vtable support, target/generated code, manual rows, direct B002 report, exact UDT prestate, all forty historical item/comment partitions, and the independently verified applied poststate.
- [x] Claim And Incorporation Ledger completed and reconciled with callback destinations/readback; only final supervisor claim verification and normal additional-direct B009 execution/archive remain pending.
- [x] Metadata/score plan completed: preserve target `88/93` and all current routing/blocks.
- [x] Score-limiting blockers researched to resolution, implementation-ready action, or exhaustive no-change proof: bytes/emitter/route/range/CPP/H resolved; name/linkage inferred; helper UDT/data-item/address-comment and manual-row actions are exact; existing `PoolAllocator` UDT is exact `NO_CHANGE`.
- [x] Owner/emitter/reconstructable plan completed: preserve [UID:000057] owner/emitter and `RECONSTRUCTABLE:TRUE`.
- [x] Split/rename/new-child analysis completed: none recommended.
- [x] Source-placement/range/split/padding/reclassification and IDA recommendation planning completed; helper UDTs, exact `PoolAllocator` no-change, and data-item/address-comment actions are now applied and verified.
- [x] Gate 2B handoff supplied every helper type, existing-`PoolAllocator` no-change invariant, historical item partition, bounded action, safety constraint, and expected readback; supervisor accepted and completed it.
- [x] Supervisor Gate 2B actual IDA application, literal post-action readback, IDB save, and independent verification are complete; catalog entry `0344` is exactly `APPLIED_VERIFIED_RECONCILED` after independent target/manual/IDA reconciliation verification.
- [x] First-draft CPP and H disposition completed: preserve exact current CPP; preserve blank H under internal-linkage proof.
- [x] Third-party import applicability checked: not applicable; target is NexusTK-owned static storage, not a third-party import.
- [x] Exact target/support facts incorporated at report-level detail: historical MCP/IDA prestate, zero/interior negatives, current applied Gate 2B poststate, source score citation, saved-IDB proof, and preserved B002 authority; no support-doc edits.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence identified for preservation: old `ff` byte and below-gate/no-assignment wording is stale; reject raw bytes, comment-only marker, PoolAllocator direct ownership, aggregate ownership, MapPane ownership, direct file canonical ownership, neighbor merge, and `sizeof(FlyingObjectPane)` until class final layout audit.
- [x] Wave2/Wave3 material encountered was ignored/rejected as stale; current MCP and current by-* docs are authority.
- [x] Open-question research completed: only historical spelling/linkage/expression remain unknowable and nonblocking; no deferred-investigation item remains.
- [x] Target scoped validators executed successfully under commands `000000018378`, `000000018503`, `000000018630`, and final supervisor command `000000018674`; all exited `0` with `ok: 1`.
- [x] Supervisor-owned manual coverage replacement text supplied, applied exactly, and validated at dated command checkpoint `000000018675`; that command's generated refresh is preserved as a dated checkpoint.

Implementation callback pass:
- [x] Supervisor-owned IDA changes were applied and independently verified by the supervisor; B009 did not mutate IDA.
- [x] Report accepted by supervisor for target-only supplemental implementation at exact authorized SHA.
- [x] All accepted target details incorporated at report-level detail; all support docs were verified as no-change and remained untouched.
- [x] Every declared primary/additional UID verified independently against its ledger and destination docs: UID0002WT only.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim.
- [x] Metadata/score/owner/emitter/split/rename/CPP/H dispositions applied or explicitly preserved: target remains `88/93`, owner/emitter `000057`, reconstructable true, exact CPP, blank H, no split/rename.
- [x] Historical/stale assumptions, rejected alternatives, negative evidence, and executed-B002 authority preserved.
- [x] Open questions resolved or documented with evidence-backed nonblocking uncertainty; no new deferred ordinary-document investigation remains.
- [x] Target scoped validators ran successfully and exact callback/reconciliation/final results (`000000018378`, `000000018503`, `000000018630`, `000000018674`) are recorded; dated manual coverage command checkpoint `000000018675` also exited `0`, `ok: 1`.
- [x] Command `000000018697` and its supplied generated hashes are preserved as a dated semantic checkpoint. Audit-time command `000000018722` records ItemObjectPane.cpp SHA `8785F49D76042D1B3787CAFA3BCB2B5B0269ED5817F99D18FE549BFA0539890C`, `4588` bytes/`120` lines; tracker SHA `DE9F3554F2E1E15E4D2EA03CD983DFF8FA46CC93243456824CCD4A05BF2202C3`, `1665936` bytes/`6596` lines; one UID0002WT `88/93` marker, one exact static-pool definition, zero target empty markers, one unrelated UID000057 empty marker, and no ItemObjectPane.h. Catalog entry `0344` remains `APPLIED_VERIFIED_RECONCILED`. Later supervisor validators may advance physical freshness headers; final lifecycle-time readback is authoritative, and approved normal B009 execution is expected to make the tracker count both direct B002 and B009 reports while preserving B002 as the earlier implementation authority.
- [x] Supervisor-owned manual coverage row update and scoped validation are complete; B009 did not edit any `-coverage-report.md` file.
- [ ] Fresh supervisor Gate 1/post-reconciliation claim verification, authoritative final lifecycle-time generated readback, normal `execute_report` registration of B009 as an additional direct supplemental report, lifecycle transition, and executed archive remain pending; B009 must not perform these supervisor-owned actions.
- [x] Remaining unapplied accepted items listed with exact ownership/blocker: final supervisor claim verification and normal additional-direct B009 execution/archive only; expected tracker outcome counts both B002 and B009 while preserving B002's earlier implementation authority.

READY_FOR_SUPERVISOR_EXECUTE

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B009","command_id":"000000018765","destination_path":"executed-b-agent-research/B009/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B009/research/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md","timestamp":"2026-07-29T02:55:45-04:00","uid":"0002WT"} -->
<!-- {"agent":"B009","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 0002WT-FlyingObjectPanePoolStaticStorage-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B009/0002WT-FlyingObjectPanePoolStaticStorage-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"0002WT"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
