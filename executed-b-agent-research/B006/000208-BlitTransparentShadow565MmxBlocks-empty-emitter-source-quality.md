** TARGET-REPORT-UID:000208 **
** AUTHOR-AGENT-ID:B006 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 000208 BlitTransparentShadow565MmxBlocks Empty-Emitter Source-Quality Research

## Final Recommendation

- Current recommendation: keep [UID:000208] `BlitTransparentShadow565MmxBlocks` as reconstructable, SoftwareBlend16-owned, and routed through [UID:0000NT] `SoftwareBlend16`, and replace its blank formal `RECONSTRUCTION_CPP CODE` with the implementation-ready MSVC x86 inline-assembly block in this report.
- Final disposition: direct C++ implementation callback ready; no split, no owner/emitter change, no non-emitting reclassification, and no rare no-code exception.
- Required action: update the target and support docs with current MCP facts, insert the formal C++ block, record that the two read-only qword masks can be local source constants for this helper, refresh generated output through the normal validator path, and update coverage/tracker freshness through generated tooling only.
- Recommended score after implementation: `88/91`.

## Target

- Target UID: `000208`
- Target path: `by-memory/0x00460940-0x004609d3.BlitTransparentShadow565MmxBlocks.md`
- Assignment: `B006-report-000208-BlitTransparentShadow565MmxBlocks-empty-emitter-20260629`
- Current target metadata: `COMPLETION:85`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000NT`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000NT`, blank formal C++.
- Current parent state: [UID:0000NT] `SoftwareBlend16` is `86/88`, `CANONICAL_OWNER:FILE`, proposed reconstruction path `NexusTK/render/`.

## Current Target State

- Existing owner/emitter state is correct: UID000208 is a source-authored static/free render helper under SoftwareBlend16.
- Existing range and behavior evidence is mostly correct: exact function `0x00460940-0x004609d3`, sole caller inside `sub_460370` at `0x00460399`, no callees, two read-only qword masks, transparent/sentinel/copy lane behavior, row/pitch stepping, and `emms`.
- The empty-emitter problem is local: `auto-generated/NexusTK/render/SoftwareBlend16.cpp` contains an empty marker for UID000208, and the current generated coverage/tracker rows mark this target as reconstructable but not emitting code.
- The old "wait for the SoftwareBlend16-wide MMX source form" blocker is not strong enough for this target. The accepted Agent-B003 callback for [UID:0000YF] shows that target-local MSVC x86 inline assembly is an acceptable first-draft source form when range, caller contract, constants, and behavior are independently closed. UID000208 needs fewer support artifacts than UID0000YF because it has no writable scratch data.

## Evidence Checked

- Required MCP session: `dad3692b`.
- MCP health: `server_health(database=dad3692b)` returned `status:ok`, module `NexusTK.exe`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`, and `strings_cache_size:2067`.
- MCP tools used successfully before report writing: `server_health`, `lookup_funcs`, `xrefs_to`, `analyze_function`, `callees`, and `get_bytes`.
- MCP schema mistakes corrected during the pass: one `analyze_function` call used `address` instead of `addr`, and one `callees` call used `address` instead of `addrs`. These were client parameter errors, not MCP busy/down states.
- Docs read: target UID000208, parent [UID:0000NT] `SoftwareBlend16`, caller/support [UID:0000YD] `Rgb555565BlitHelpers`, constants [UID:0002OO] `SoftwareBlend16ReadOnlyConstants`, siblings [UID:000209] and [UID:00020A], generated research tracker, generated by-memory coverage, generated `SoftwareBlend16.cpp`, current B003 UID0000YF report, and executed B001 UID0000YG lead.
- Required search terms used across active/executed reports and current docs: `TARGET-REPORT-UID:000208`, `000208`, `0x00460940`, `BlitTransparentShadow565MmxBlocks`, `SoftwareBlend16`, `Rgb555565BlitHelpers`, `transparent shadow`, `Rgb565`, `RGB565`, `shadow`, and related caller/source-family terms.
- Existing exact UID000208 B-agent report: none found before this report. Relevant leads were the C001 notes for the 2026-06-13 advanced-scan repair, the executed B001 SoftwareBlend16 source-form reports, and the current accepted B003 UID0000YF implementation-callback report.

## Current MCP Facts

- `lookup_funcs`:
  - `0x0046093c`: not a function.
  - `0x00460940`: `sub_460940`, size `0x93`.
  - `0x004609d2`: inside `sub_460940`, size `0x93`.
  - `0x004609d3`: not a function.
  - `0x004609e0`: `sub_4609E0`, size `0x119`.
  - `BlitTransparentShadow565MmxBlocks`: not found.
  - `BlitTransparentShadow565`: not found.
- `xrefs_to 0x00460940`: exactly one code xref, `0x00460399`, inside `sub_460370`, size `0xa0`.
- `xrefs_to 0x00610e88`: exactly one data xref, `0x00460951`, inside `sub_460940`.
- `xrefs_to 0x00610e90`: exactly one data xref, `0x0046095b`, inside `sub_460940`.
- `callees`:
  - `0x00460940`: empty callee set.
  - `0x00460370`: one callee, `0x00460940` / `sub_460940`.
- `get_bytes`:
  - `0x0046093c` size `4`: `cc cc cc cc`.
  - `0x004609d3` size `13`: thirteen `cc` bytes.
  - `0x00610e88` size `8`: `de f7 de f7 de f7 de f7`, the packed qword value `0xf7def7def7def7de`.
  - `0x00610e90` size `8`: `01 00 01 00 01 00 01 00`, the packed qword value `0x0001000100010001`.
- `analyze_function 0x00460940`: size `147`, no callees, caller `sub_460370`, seven basic blocks, cyclomatic complexity `3`, no strings, and decompiler pseudo-arguments matching a source pointer, source pitch, destination pointer, destination pitch, four-pixel block count, and row count plus a decompiler-only `mm3` scratch artifact.
- The decompiled behavior matches the docs: source word `0` preserves the destination lane, source word `1` writes `(destination & 0xf7de) >> 1` per word, and all other source words copy to destination. The qword loop advances by eight bytes per four-pixel block and by the supplied row pitches per row, then exits through register restore plus `_m_empty()` / `emms`.

## Positive Evidence Summary

- The target is a precise modeled function, not a raw span and not a mixed range.
- It has one direct caller, no callees, no object state, no imports, and no non-render side effects.
- Its caller [UID:0000YD] `BlitTransparentShadow565` computes `width / 4`, calls this helper for four-pixel blocks, and then handles the scalar tail with the same RGB565 transparent-shadow semantics and word mask `0x7bef`.
- Its only data dependencies are read-only SoftwareBlend16 qword constants [UID:0002OO] at `0x00610e88` and `0x00610e90`.
- Adjacent bytes are confirmed alignment padding: four `0xcc` bytes before the function and thirteen `0xcc` bytes after it before sibling UID000209.
- Parent [UID:0000NT] already owns the SoftwareBlend16 helper island and inventories UID000208, UID000209, UID0000YF, UID0000YG, UID00020A, byte block helpers, read-only masks, and scratch data as one low-level render-helper family.

## Negative Evidence Summary

- Current MCP does not show active IDA names `BlitTransparentShadow565MmxBlocks` or `BlitTransparentShadow565`; it reports `sub_460940` and `sub_460370`.
- No surviving source-file breadcrumb proves exact original helper spelling, declaration visibility, or whether the original source used inline assembly, macros, intrinsics, or an external assembly helper.
- A portable scalar C++ loop would be behavior-equivalent but weaker than the observed MMX source form for this optimized block path.
- Sibling UID000209 and UID00020A still have inherited blank formal C++ blocks from the older B001 source-form decision, but that is not target-local proof that UID000208 cannot emit now.

## Heuristic / Inference Reanalysis And Validation

### Role And Source-Facing Name

Best role: RGB565 transparent-shadow four-pixel MMX block helper used by `BlitTransparentShadow565`.

Best source-facing name remains `BlitTransparentShadow565MmxBlocks` as a descriptive/inferred reconstruction name. It matches the caller's scalar behavior, target filename, SoftwareBlend16 grouping, and sibling naming style. Current MCP does not prove it as an active original symbol, so implementation wording should call it source-facing/descriptive rather than a recovered IDA label.

### Source-Facing Signature

Best source-facing signature:

```cpp
static void __cdecl BlitTransparentShadow565MmxBlocks(
    const unsigned short* sourcePixels,
    int sourcePitchBytes,
    unsigned short* destinationPixels,
    int destinationPitchBytes,
    int blockCount4Pixels,
    int rowCount);
```

The helper is side-effect-only despite decompiler pointer-return artifacts. The caller does not consume a return value, and all meaningful output is through destination qword stores. The block count is a count of four-pixel groups and is shifted to eight-byte qword span length inside the helper.

### Source Form

Best-supported source form: target-local MSVC x86 inline assembly with local qword constants for the shadow mask and sentinel-one vector.

Reasons:

- The binary uses MMX qword operations, explicit `emms`, all-register save/restore, qword read-only constants, and a compact block loop.
- UID000208 has no writable scratch-data dependency; unlike UID0000YF and UID000209, it only needs local read-only constants.
- [UID:0002OO] already records that these constants should be represented as source-level static masks in the SoftwareBlend16 helper module.
- The accepted UID0000YF implementation callback resolves the older family-wide source-form blocker as insufficient for a target-local first draft.

Rejected source forms:

- Portable scalar C++: rejected as a weaker representation of the optimized MMX block helper.
- Hex-Rays `_m_*` intrinsic transcription: rejected because it would preserve analysis artifacts and the spurious `mm3` parameter instead of source-facing arguments.
- External assembly-only helper: rejected for this pass because the generator needs formal C++ text, and inline assembly preserves behavior without requiring a separate build artifact.

### Range / Split / Padding / Reclassification

Keep exact range `0x00460940-0x004609d3`.

- Do not split: the function has one entry, one qword inner loop, one row loop, one epilogue, and no internal padding/data island.
- Do not merge backward: `0x0046093c-0x00460940` is `0xcc` padding after the raw preceding span body.
- Do not merge forward: `0x004609d3-0x004609e0` is thirteen `0xcc` bytes before UID000209.
- Do not reclassify as covered-by/no-code: the block helper is directly called and contains unique behavior not fully emitted elsewhere.
- Do not reclassify as no-owner/non-emitting: it has a valid SoftwareBlend16 owner/emitter route and an implementation-ready source form.

## Ranked Ownership Analysis

### 1. [UID:0000NT] SoftwareBlend16

- Evidence for: contiguous SoftwareBlend16 helper island, RGB565 MMX math, direct use by [UID:0000YD], direct read-only mask dependencies in [UID:0002OO], no object/UI state, no imports, and existing canonical owner/emitter routing.
- Evidence against: exact original filename and helper spelling are inferred rather than proven.
- Decision: accept as owner/emitter.

### 2. [UID:0000YD] Rgb555565BlitHelpers

- Evidence for: direct caller `sub_460370` lives in this by-memory support page and owns the scalar/body wrapper behavior.
- Evidence against: UID000208 is a separate modeled SoftwareBlend16 block primitive and shares constants/source-form with the MMX helper island. The caller should document the call relationship, not own the block helper.
- Decision: reject as direct owner; keep as caller/support context.

### 3. Surface / render callback owners

- Evidence for: higher-level RGB565 sprite/blit callbacks eventually reach the caller.
- Evidence against: no callback dispatch, Surface, GrafPort, pane, resource, or DirectDraw state appears in this helper.
- Decision: reject as too broad.

### 4. No owner / no emitter / covered-by

- Evidence for: no active original symbol proof.
- Evidence against: exact range, caller, constants, behavior, and SoftwareBlend16 route are closed, and formal C++ can improve the generated output.
- Decision: reject. This would fail the rare no-code burden.

## Source Placement

Recommended placement remains `NexusTK/render/SoftwareBlend16.cpp` under [UID:0000NT].

Implementation notes for the supervisor:

1. Insert UID000208 as a `static void __cdecl` MSVC x86 inline-assembly helper.
2. Use local qword constants for `0xf7def7def7def7de` and `0x0001000100010001`.
3. Keep [UID:0002OO] as the binary/source documentation owner of those read-only constants, but do not require external constant declarations before UID000208 can emit.
4. Keep caller [UID:0000YD] as the scalar-wrapper/caller context and document that its MMX block helper is now independently emit-ready.
5. Do not edit generated files by hand; run the normal validator/generator sequence after source-doc insertion.

## First-Draft C++ Recommendation

Eligible for isolated first-draft formal C++ now: yes.

The target's formal `RECONSTRUCTION_CPP` multiline block should contain this insertion text:

```cpp
static void __cdecl BlitTransparentShadow565MmxBlocks(
    const unsigned short* sourcePixels,
    int sourcePitchBytes,
    unsigned short* destinationPixels,
    int destinationPitchBytes,
    int blockCount4Pixels,
    int rowCount)
{
    unsigned __int64 shadowMask = 0xf7def7def7def7deui64;
    unsigned __int64 sentinelOne = 0x0001000100010001ui64;
    const unsigned char* sourceRow = (const unsigned char*)sourcePixels;
    unsigned char* destinationRow = (unsigned char*)destinationPixels;
    int blockBytes = blockCount4Pixels << 3;

    __asm {
        pushad
        pxor mm3, mm3
        movq mm4, qword ptr [shadowMask]
        movq mm7, qword ptr [sentinelOne]
        mov eax, sourceRow
        mov ebx, destinationRow
        mov ecx, rowCount

    rowLoop:
        test ecx, ecx
        jz shadowDone
        mov esi, eax
        mov edi, ebx
        mov edx, blockBytes
        add edx, edi

    blockLoop:
        cmp edx, edi
        jbe nextRow

        movq mm0, qword ptr [esi]
        movq mm1, qword ptr [edi]

        movq mm5, mm0
        pcmpeqw mm5, mm3

        movq mm6, mm0
        pcmpeqw mm6, mm7

        movq mm2, mm6
        pandn mm2, mm0

        movq mm0, mm1
        pand mm0, mm4
        psrlw mm0, 1
        pand mm0, mm6

        movq mm6, mm5
        pandn mm6, mm2

        movq mm2, mm5
        pandn mm2, mm0
        por mm6, mm2

        pand mm1, mm5
        por mm6, mm1

        movq qword ptr [edi], mm6
        add esi, 8
        add edi, 8
        jmp blockLoop

    nextRow:
        add eax, sourcePitchBytes
        add ebx, destinationPitchBytes
        dec ecx
        jmp rowLoop

    shadowDone:
        popad
        emms
    }
}
```

Notes on this block:

- `pxor mm3, mm3` expresses the zero vector that the decompiler shows as a pseudo `mm3` input.
- `shadowMask` is the packed form of the scalar RGB565 shadow mask `0x7bef` after the binary's mask-before-shift representation.
- `sentinelOne` detects source word `1`, the caller's transparent-shadow sentinel.
- Transparent lanes (`source == 0`) keep destination, sentinel lanes (`source == 1`) write half-bright destination, and all other lanes copy source.
- `pushad`/`popad` are the source-level all-register save/restore form. The binary may show operand-size-prefixed `pushaw`/`popaw`; the first-draft source should document the all-register preservation intent rather than block emission over this assembler spelling detail.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Current evidence closes exact range, caller, no-callee status, boundary padding, qword constants, source/destination pointer and pitch roles, four-pixel block count, row count, transparent/sentinel/copy semantics, scalar-tail relationship, and source-form direction. Formal first-draft C++ is now supplied. Remaining gaps are exact original symbol spelling and exact assembler spelling. |
| Confidence | 91 | Current MCP, target docs, support docs, generated marker state, and sibling source-form reports agree. Confidence stays below final-source quality because active IDA still has `sub_460940`, and inline assembly remains a best-supported reconstruction rather than recovered source text. |
| Reconstructable | true | This is source-authored NexusTK software render code with a valid SoftwareBlend16 emitter route and now has implementation-ready formal source. |

## Claim And Incorporation Ledger

| Claim / accepted detail | Action | Destination or exclusion reason | Verification state |
| --- | --- | --- | --- |
| UID000208 should move from `85/90` to `88/91`. | incorporate | Applied in target [UID:000208] metadata header and `Score Rationale`; validator `000000000971` recorded `completion_update 000208 ... 88` and `confidence_update 000208 ... 91`; generated coverage/tracker refreshed from validator output. | applied |
| UID000208 should keep `CANONICAL_OWNER:0000NT`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000NT`. | incorporate | Applied in target [UID:000208] metadata header, `Status`, source-form/ownership prose, and score rationale; values remained unchanged. | applied |
| The target should emit direct C++ now; no split, covered-by/no-code, non-emitting, or rare no-code exception applies. | incorporate | Applied in target [UID:000208] `Source-Facing Signature And Source-Form Policy`, `Reconstruction Evidence`, `Score Rationale`, and formal populated `RECONSTRUCTION_CPP`; generated output now emits code for UID000208. | applied |
| Formal C++ insertion is the exact `static void __cdecl BlitTransparentShadow565MmxBlocks(...)` MSVC x86 inline-assembly block from this report. | incorporate | Applied in target [UID:000208] formal `RECONSTRUCTION_CPP CODE:BEGIN/END` block. | applied |
| Source-facing helper name remains `BlitTransparentShadow565MmxBlocks`, but current MCP does not prove it as an active recovered IDA symbol. | incorporate | Applied in target [UID:000208] source-form policy and 2026-06-29 evidence bullets; phrased as source-facing/descriptive with name-negative MCP evidence. | applied |
| Best source-facing signature is `static void __cdecl BlitTransparentShadow565MmxBlocks(const unsigned short* sourcePixels, int sourcePitchBytes, unsigned short* destinationPixels, int destinationPitchBytes, int blockCount4Pixels, int rowCount)`. | incorporate | Applied in target [UID:000208] `Source-Facing Signature And Source-Form Policy` and formal C++ block header. | applied |
| Return is void-style side-effect behavior; any decompiler pointer return is ignored by the caller. | incorporate | Applied in target [UID:000208] source-facing signature section. | applied |
| MCP session `dad3692b` was current and healthy for this report. | incorporate | Applied in target [UID:000208] 2026-06-29 evidence bullet with `server_health` status and readiness flags. | applied |
| Range is exact `0x00460940-0x004609d3`; `0x00460940` is `sub_460940`, size `0x93`, and `0x004609d3` is not a function. | incorporate | Applied in target [UID:000208] existing `Address Range` plus 2026-06-29 evidence bullet. | applied |
| Boundary padding is four `0xcc` bytes at `0x0046093c-0x00460940` and thirteen `0xcc` bytes at `0x004609d3-0x004609e0`; successor is `sub_4609E0` at `0x004609e0`. | incorporate | Applied in target [UID:000208] 2026-06-29 evidence bullet; no [UID:0000VN] ignored-page edit was required because existing ignored coverage already covers the padding. | applied |
| Caller evidence is exactly one code xref to `0x00460940` from `0x00460399` inside `sub_460370`; caller/support page is [UID:0000YD]. | incorporate | Applied in target [UID:000208] evidence and [UID:0000YD] `Pixel Format Notes`/evidence support note. | applied |
| UID000208 has no callees; [UID:0000YD] caller has callee `0x00460940`. | incorporate | Applied in target [UID:000208] existing callee row plus 2026-06-29 evidence; [UID:0000YD] support note records the caller relationship. | applied |
| Current MCP name-negative evidence found no active functions named `BlitTransparentShadow565MmxBlocks` or `BlitTransparentShadow565`. | incorporate | Applied in target [UID:000208] source-form policy and 2026-06-29 evidence bullet. | applied |
| Data refs are exactly `0x00460951 -> 0x00610e88` and `0x0046095b -> 0x00610e90`. | incorporate | Applied in target [UID:000208] constants evidence and [UID:0002OO] rebuild/change notes. | applied |
| `0x00610e88` bytes/value are `de f7 de f7 de f7 de f7` / `0xf7def7def7def7de`; this is the packed RGB565 transparent-shadow half-bright mask. | incorporate | Applied in target [UID:000208] formal C++ local `shadowMask`, evidence, and [UID:0002OO] rebuild notes. | applied |
| `0x00610e90` bytes/value are `01 00 01 00 01 00 01 00` / `0x0001000100010001`; this is the packed sentinel-one comparator. | incorporate | Applied in target [UID:000208] formal C++ local `sentinelOne`, evidence, and [UID:0002OO] rebuild notes. | applied |
| UID000208 first-draft source may fold `0x00610e88` and `0x00610e90` as local qword constants while preserving binary constant evidence in [UID:0002OO]. | incorporate | Applied in target [UID:000208] source-form policy, [UID:0002OO] rebuild notes, and [UID:0000NT] MMX source-form policy. | applied |
| Pixel behavior is: source word `0` preserves destination, source word `1` writes half-bright destination, all other source words copy source. | incorporate | Applied in target [UID:000208] behavior/C++ notes and [UID:0000YD] caller support note. | applied |
| Qword loop processes four RGB565 pixels/eight bytes per block, advances source/destination by eight bytes per block, and advances rows by supplied source/destination pitches. | incorporate | Applied in target [UID:000208] behavior/signature evidence and [UID:0000YD] caller support note. | applied |
| Caller [UID:0000YD] `BlitTransparentShadow565` computes the block path and then scalar tail; scalar tail uses the same semantics and word mask `0x7bef`. | incorporate | Applied in [UID:0000YD] `Pixel Format Notes` and evidence support note; target [UID:000208] already retained caller/scalar-tail behavior. | applied |
| The old SoftwareBlend16-wide MMX source-form blocker is resolved for UID000208 because target-local inline assembly is sufficient and this target has no writable scratch dependency. | incorporate | Applied in target [UID:000208] source-form policy/score rationale and [UID:0000NT] MMX source-form policy. | applied |
| [UID:0000NT] SoftwareBlend16 remains the direct source root and generated destination is `NexusTK/render/SoftwareBlend16.cpp`. | incorporate | Applied in target [UID:000208] owner/source placement and [UID:0000NT] MMX source-form policy; generated output refreshed by validator only. | applied |
| [UID:0000YD] is caller/support context, not the direct owner/emitter of UID000208. | incorporate | Applied in target [UID:000208] ownership/rejected alternatives and [UID:0000YD] support note; UID0000YD metadata unchanged. | applied |
| Surface/render callback owners are too broad for UID000208. | incorporate | Applied in target [UID:000208] ranked ownership/rejected alternatives. | applied |
| Do not split, merge backward, or merge forward; neighboring bytes are padding and UID000209 starts at `0x004609e0`. | incorporate | Applied in target [UID:000208] range/evidence/score sections; no child files created. | applied |
| Generated `SoftwareBlend16.cpp` empty marker for UID000208 should disappear after the target formal C++ insertion and validator-generated refresh. | incorporate | Applied by validator-generated refresh. `auto-generated/NexusTK/render/SoftwareBlend16.cpp` header is `validator-command-id: 000000000978`, refreshed at `2026-06-29T17:40:11-04:00`, and UID000208 appears as emitted code rather than an empty marker. | applied |
| Generated by-memory coverage and research tracker should refresh from validator state rather than manual edits. | incorporate | Applied by validator-generated refresh. Generated coverage reports UID000208 `emits_code:true`, `88%`, `very-strong`, updated `2026-06-29 17:38:06`; generated research tracker reports `88/91`. | applied |
| Validators are required for the target/support edit batch. | incorporate | Applied: validators `000000000971`, `000000000972`, `000000000973`, and `000000000974` all exited `0` with `ok: 1`. | applied |
| Validator command IDs, timestamps, exits, `ok` counts, generated-refresh state, and generated empty-marker status must be reported after implementation. | incorporate | Applied in this report checklist and final response. Command metadata: `000000000971` at `2026-06-29T17:38:05-04:00`, `000000000972` at `2026-06-29T17:38:11-04:00`, `000000000973` at `2026-06-29T17:38:25-04:00`, `000000000974` at `2026-06-29T17:38:33-04:00`; all exit `0`, `ok: 1`. | applied |
| Leases must be taken only for the immediate target/support edit/validator batch and released immediately after. | incorporate | Leases were taken for the four accepted by-* files only immediately before editing. Cleanup command `python .\tools\leaser\leaser.py B006 unlease ...` reported `Rejected[No active lease]` for each path, and the current lease report shows no active B006 entries. | applied |
| Report-only pass must leave implementation checklist items unchecked. | not-applicable | Superseded by the accepted implementation callback. Checklist items are now checked with proof because implementation is complete. | excluded-with-reason |
| No generated, coverage, validator/tool-state, IDA DB, executed archive, supervisor ledger, lock file, or target/support by-* doc should be edited during this revision. | not-applicable | Superseded by the implementation callback for target/support docs. Generated, coverage, project-level generated, validator/tool-state, IDA DB, executed archive, supervisor ledger, and lock files were not manually edited; target/support docs were edited under callback scope. | excluded-with-reason |

## Required Implementation Checklist

- [x] Lease only the target/support files immediately before editing. Leased `by-memory/0x00460940-0x004609d3.BlitTransparentShadow565MmxBlocks.md`, `by-file/SoftwareBlend16.md`, `by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md`, and `by-memory/0x0045fa00-0x004604f4.Rgb555565BlitHelpers.md` as B006 for the edit/validator batch.
- [x] Update UID000208 metadata to `88/91`, keep `CANONICAL_OWNER:0000NT`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000NT`. Applied in target metadata; validator `000000000971` recorded completion/confidence updates.
- [x] Insert the formal C++ block above into UID000208. Applied in target formal `RECONSTRUCTION_CPP CODE` block; validator `000000000971` recorded registry state `blank -> block`.
- [x] Update UID000208 prose to record MCP session `dad3692b`, current name-negative results, current caller/xref/constants facts, and that the old family-wide source-form blocker is resolved for this target. Applied in target source-form policy, evidence, reconstruction evidence, score rationale, and changes sections.
- [x] Update [UID:0000NT] `SoftwareBlend16` with a UID000208-specific note that this helper can emit as target-local MSVC x86 inline assembly. Applied in `MMX Source-Form Policy` and `Changes`; validator `000000000972` exited `0`, `ok: 1`.
- [x] Update [UID:0002OO] `SoftwareBlend16ReadOnlyConstants` to state that UID000208's first-draft source may fold `0x00610e88` and `0x00610e90` as local qword constants while preserving the binary constant evidence. Applied in `Rebuild Notes` and `Changes`; validator `000000000973` exited `0`, `ok: 1`.
- [x] Update [UID:0000YD] `Rgb555565BlitHelpers` to note that the `BlitTransparentShadow565` block helper is independently emit-ready and shares the scalar transparent-shadow semantics. Applied in `Pixel Format Notes`, `Evidence`, and `Changes`; validator `000000000974` exited `0`, `ok: 1`.
- [x] Release leases immediately after the edit/validator batch. Cleanup command `python .\tools\leaser\leaser.py B006 unlease ...` reported `Rejected[No active lease]` for each of the four paths, meaning the short leases had already expired or cleared; the current lease report confirms no active B006 entries.
- [x] Run target/support validators and generated-output refresh through the normal toolchain; do not hand-edit generated coverage, tracker, or `auto-generated/NexusTK/render/SoftwareBlend16.cpp`. Ran the four callback validators. Generated files refreshed through validator only; no generated files were manually edited.
- [x] Confirm generated `SoftwareBlend16.cpp` no longer has an empty marker for UID000208 and record command IDs/timestamps/exits in the implementation callback. Confirmed generated header `validator-command-id: 000000000978`, `validator-refreshed-at: 2026-06-29T17:40:11-04:00`; UID000208 appears as emitted `BlitTransparentShadow565MmxBlocks` code, not an `Empty Emitter Marker`.

## Generated Freshness And No-Code Disposition

- Generated freshness at report time: generated tracker and generated by-memory coverage still show UID000208 as reconstructable but non-emitting/empty; generated `SoftwareBlend16.cpp` still has the UID000208 empty marker. This is expected before implementation.
- Manual/generated file disposition: no generated, coverage, validator/tool-state, IDA DB, executed archive, supervisor ledger, or lock files were edited during this report-only pass.
- No-code disposition: rejected. The target has direct code-generation value and an implementation-ready formal C++ block; a covered-by/no-code or non-emitting outcome would not meet the rare no-code proof burden.

## Cross-References

- [UID:0000NT][SoftwareBlend16](../../../../../by-file/SoftwareBlend16.md)
- [UID:0000YD][Rgb555565BlitHelpers](../../../../../by-memory/0x0045fa00-0x004604f4.Rgb555565BlitHelpers.md)
- [UID:0002OO][SoftwareBlend16ReadOnlyConstants](../../../../../by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md)
- [UID:000209][Rgb565TransparentAlphaBlendMmxBlocks](../../../../../by-memory/0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks.md)
- [UID:00020A][Rgb565AlphaMapMmxBlocks](../../../../../by-memory/0x00460c90-0x00460d51.Rgb565AlphaMapMmxBlocks.md)

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B006","command_id":"000000004165","destination_path":"executed-b-agent-research/B006/000208-BlitTransparentShadow565MmxBlocks-empty-emitter-source-quality.md","details":"legacy registry reconstruction","event":"executed","source_path":"tools/leaser/Agents/Agent-B006/research/000208-BlitTransparentShadow565MmxBlocks-empty-emitter-source-quality.md","timestamp":"2026-06-29T17:43:18","uid":"000208"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
