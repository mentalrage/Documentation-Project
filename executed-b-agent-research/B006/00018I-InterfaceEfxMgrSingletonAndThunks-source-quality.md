** TARGET-REPORT-UID:00018I **
** AUTHOR-AGENT-ID:B006 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# 00018I InterfaceEfxMgrSingletonAndThunks Source-Quality Report

## Finalized Report / Current Recommendation

- Current recommendation: reclassify [UID:00018I] from reconstructable source helper to non-emitting compiler/EH glue owned semantically by `InterfaceEfxMgr`.
- Final disposition: no split; no handwritten C++ body. The source-authored behavior belongs in the `InterfaceEfxMgr` constructor/destructor and class declarations, not in this exact thunk island.
- Required action after supervisor review: update the target/support docs to close the "clear helper source spelling" question, set `RECONSTRUCTABLE:FALSE`, clear `EMITTER_UIDS`, and replace the `by-memory/-coverage-report.md` row with the exact text below.
- Confidence: strong. The only missing piece is fresh live IDA in this session; the existing docs already contain prior live IDA facts for the exact bytes, xrefs, vtable refs, and singleton storage refs.

## Target

- Target UID: `00018I`
- Target path: `source-3/project-documentation/by-memory/0x004ea060-0x004ea081.InterfaceEfxMgrSingletonAndThunks.md`
- Current target score: `84/90`
- Current metadata: `CANONICAL_OWNER:00006O`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00006O`
- Current route: `00018I -> 00006O InterfaceEfxMgr -> 0000K9 InterfaceEfx.cpp -> auto-generated/NexusTK/ui/InterfaceEfx.cpp`
- Current generated output state: `auto-generated/NexusTK/ui/InterfaceEfx.cpp` contains an empty emitter marker for [UID:00018I].

## Evidence Checked

- Target page [UID:00018I]:
  - Prior live IDA reports three adjacent `0xb` functions:
    - `0x004ea060-0x004ea06b`
    - `0x004ea06b-0x004ea076`
    - `0x004ea076-0x004ea081`
  - Raw bytes match one global clear followed by two `ecx`-adjusting tail jumps.
  - `0x004ea060` decompiles to `dword_69B360 = 0`.
  - `0x004ea06b` decompiles as `sub_4EA090(this - 40, flags)` and disassembles as `sub ecx, 0xa0; jmp sub_4EA090`.
  - `0x004ea076` decompiles as `sub_4EA090(this - 41, flags)` and disassembles as `sub ecx, 0xa4; jmp sub_4EA090`.
  - The only code reference to `0x004ea060` is the `InterfaceEfxMgr` constructor EH cleanup funclet around `0x00600771`.
  - `0x004ea06b` is referenced by secondary vtable slot `0x0061c688`.
  - `0x004ea076` is referenced by tertiary vtable slot `0x0061c6b8`.
  - `0x004ea090` is the scalar deleting destructor target.
- [UID:00006O] `InterfaceEfxMgr` class page:
  - Documents constructor, trigger helper, frame spawner, singleton clear helper, adjustor thunks, and scalar deleting destructor.
  - Prior live IDA confirms constructor writes `g_pInterfaceEfxMgr`, constructor cleanup calls the clear helper, scalar destructor clears the singleton, and vtable refs point at the adjustors.
  - Current class score `86/88` clears the support gate.
- [UID:0000K9] `InterfaceEfx` file page:
  - Routes `InterfaceEfx` and `InterfaceEfxMgr` through `NexusTK/ui/InterfaceEfx.cpp`.
  - States that [UID:00018I] belongs to the manager island, but the clear helper is constructor-cleanup support and the adjustors are vtable-referenced compiler thunks.
  - Current file score `88/87` clears the support gate.
- [UID:0000R8] `g_pInterfaceEfxMgr` global page and [UID:0002VQ] exact storage page:
  - Resolve `dword_69B360` to `g_pInterfaceEfxMgr`.
  - Prior live IDA confirms seven direct refs to the storage, including constructor writes at `0x004e9d4b`/`0x004e9d52`, clear helper `0x004ea060`, scalar destructor clear at `0x004ea0e2`, GeneralPurposePanel read, and map cleanup.
- [UID:0003BS] `InterfaceEfxVtableData` and [UID:00025K] `InterfaceEfxReadOnlyData`:
  - Confirm the manager vtable bases and first slots:
    - `0x0061c63c` primary first slot `0x004ea090`
    - `0x0061c688` secondary first slot `0x004ea06b`
    - `0x0061c6b8` tertiary first slot `0x004ea076`
  - Confirm these vtables are generated by reconstructed class declarations and inherited pane/timer layout.
- [UID:00018G] aggregate page:
  - Places [UID:00018I] inside the compact `InterfaceEfx` / `InterfaceEfxMgr` island.
  - Confirms the successor boundary: `0x004ea121-0x004ea130` is padding before the unrelated `InventoryPane` constructor at `0x004ea130`.
- [UID:00018H] executed B003 report and implemented target:
  - Confirms the accepted `InterfaceEfx.cpp` route and manager member naming for the neighboring source-authored trigger method.
  - Explicitly did not recommend a [UID:00018I] row change.
- Generated reports/output:
  - `auto-generated/-ag-memory-coverage.md` currently lists [UID:00018I] as `emits` through `00006O` to `auto-generated/NexusTK/ui/InterfaceEfx.cpp`.
  - `auto-generated/NexusTK/ui/InterfaceEfx.cpp` shows [UID:00018I] as an empty emitter marker, while [UID:00018H] now emits a real `InterfaceEfxMgr::TriggerInterfaceEffect` body.
- Prior executed B-agent report search:
  - No executed B001/B002/B006 report for [UID:00018I] was found.
  - B001's older [UID:00025K] read-only-data report only confirms the related code anchors and sizes.

No fresh live IDA/MCP facts were collected in this session. `tool_search` exposed no IDA MCP tool, and the documented MCP endpoint `http://127.0.0.1:13337/mcp` did not accept a `tools/list` request (`Unable to connect to the remote server`). This report therefore relies on current documentation and prior live IDA facts already written into those docs.

## Heuristic / Inference Reanalysis And Validation

### Source-authored helper versus compiler glue

Best conclusion: the whole [UID:00018I] island is non-emitting compiler/EH glue, not a normal source-authored helper cluster.

Per-subrange classification:

| Range | Best classification | Source impact |
| --- | --- | --- |
| `0x004ea060-0x004ea06b` | Constructor EH cleanup funclet / source-induced singleton rollback glue | Do not emit a standalone function. Represent the source semantic in the `InterfaceEfxMgr` constructor failure/unwind notes and singleton lifecycle docs. |
| `0x004ea06b-0x004ea076` | MSVC destructor adjustor thunk for secondary manager vtable | Do not emit as source. It should be regenerated by the compiler from the class inheritance/vtable layout and destructor declaration. |
| `0x004ea076-0x004ea081` | MSVC destructor adjustor thunk for tertiary manager vtable | Do not emit as source. It should be regenerated by the compiler from the class inheritance/vtable layout and destructor declaration. |

The important distinction is that `0x004ea060` is not "nothing." It records a real singleton rollback behavior: `g_pInterfaceEfxMgr` is cleared if the constructor fails after publishing the singleton. But the binary evidence does not support a normal source function such as `ClearInterfaceEfxMgrSingleton()`:

- It has no ordinary direct callers.
- It is reached only by a constructor EH cleanup funclet.
- The scalar destructor does not call this helper; it clears the same global inline.
- The body has no `this` argument and no class-state access beyond the global clear.
- It is adjacent to two definite compiler thunks and before the scalar deleting destructor.

The strongest source reconstruction is therefore constructor/destructor logic plus compiler-generated unwind/thunk artifacts, not a handwritten method body for this exact range.

### Helper/type/global names

Recommended global spelling:

```cpp
InterfaceEfxMgr* g_pInterfaceEfxMgr;
```

Keep `dword_69B360` only as an old/raw search alias in evidence sections.

Recommended documentation-only labels:

- `InterfaceEfxMgrConstructorCleanupClearSingleton` for `0x004ea060`, if a label is needed.
- `InterfaceEfxMgr secondary destructor adjustor thunk` for `0x004ea06b`.
- `InterfaceEfxMgr tertiary destructor adjustor thunk` for `0x004ea076`.

Rejected source names:

- `ClearInterfaceEfxMgrSingleton` as a formal source function: too source-like for an EH-only target with no ordinary callers.
- `InterfaceEfxMgr::ClearSingleton()`: unsupported because no `this` use exists and the destructor inlines the clear.
- `InterfaceEfxMgr::~InterfaceEfxMgr` for this exact target: the real scalar deleting destructor starts at `0x004ea090`; the two in-range bodies are adjustors into it.
- `sub_4EA060`, `sub_4EA06B`, `sub_4EA076`, `sub_4EA090`: raw labels only.

### Caller / reachability interpretation

`0x004ea060` reachability:

- The only documented code xref is the constructor EH cleanup funclet around `0x00600771`.
- That reachability proves constructor-failure cleanup, not an ordinary callable helper.

`0x004ea06b` and `0x004ea076` reachability:

- No ordinary callers.
- Reachability is through vtable slots:
  - `0x0061c688` for the secondary manager table.
  - `0x0061c6b8` for the tertiary manager table.
- Both tail-jump to `0x004ea090`, the scalar deleting destructor, after adjusting `ecx`.

`0x004ea090` relationship:

- Outside this target.
- It is the destructor implementation/wrapper that releases persistent effects, clears `g_pInterfaceEfxMgr`, tears down base state, and conditionally frees storage.
- [UID:00018I] should point to it as the adjustor target but should not absorb its source behavior.

### Owner, emitter, and source-file route

Recommended semantic owner:

```text
CANONICAL_OWNER:00006O
```

Recommended emission:

```text
RECONSTRUCTABLE:FALSE
EMITTER_UIDS:
```

Why keep [UID:00006O] as semantic owner:

- The clear helper exists only because the `InterfaceEfxMgr` constructor publishes `g_pInterfaceEfxMgr`.
- The adjustor thunks exist only because `InterfaceEfxMgr` has multiple inherited vtable views.
- The vtable data and scalar destructor relationship are manager-specific.

Why remove [UID:00006O] as emitter for this exact target:

- Hand-emitting these functions as source would duplicate compiler artifacts.
- The compiler should regenerate adjustor thunks from the class declaration and destructor.
- The constructor EH cleanup should be represented in the constructor/unwind source shape, not as a standalone callable helper.
- The current generated empty marker in `InterfaceEfx.cpp` is therefore a modeling problem, not a missing source body.

The broader source route remains valid for real source items:

```text
InterfaceEfxMgr source semantics -> 00006O InterfaceEfxMgr -> 0000K9 InterfaceEfx.cpp -> NexusTK/ui/InterfaceEfx.cpp
```

This target should simply stop emitting.

### Split / range decision

No split is recommended.

A split into one singleton-clear page and two thunk pages is possible mechanically, but it does not improve source reconstruction because all three bodies are non-emitting compiler/EH artifacts with the same semantic owner and adjacent vtable/destructor context. Keeping one exact island is clearer as a compiler-glue record.

Rejected split alternative:

- `0x004ea060-0x004ea06b` as reconstructable source helper plus ignored `0x004ea06b-0x004ea081` thunks. Rejected because the only caller is constructor EH cleanup, the destructor inlines the same global clear, and no source-level callable helper is evidenced.

### Generated-output pollution

Current generated output has an empty marker:

```text
// UID:00018I | ... | Empty Emitter Marker
```

That marker should disappear after metadata is corrected to non-emitting. It should not be replaced by a raw helper such as:

```cpp
void ClearInterfaceEfxMgrSingleton()
{
    g_pInterfaceEfxMgr = 0;
}
```

That code would make the final source look more hand-authored than the evidence supports and would create an artificial function not used by normal source code.

## First-Draft C++ / No-Code Proof

Recommendation: do not populate a formal C++ method/helper body for [UID:00018I].

Exact no-code proof:

- The target contains no ordinary source-called method.
- `0x004ea060` is reached only from the constructor EH cleanup funclet.
- `0x004ea06b` and `0x004ea076` are vtable-referenced `this` adjustor thunks.
- The scalar deleting destructor body that actually represents destructor source behavior is outside this range at `0x004ea090-0x004ea121`.
- The singleton storage and global name are already represented by [UID:0000R8] and [UID:0002VQ].
- The compiler should regenerate the adjustor thunks from the final `InterfaceEfxMgr` class inheritance/destructor declarations.

If a future implementation callback chooses to leave this target as an emitting special case despite this recommendation, the only acceptable formal block should be a comment marker, not a function:

```cpp
// Compiler-generated InterfaceEfxMgr constructor EH singleton-clear funclet and destructor adjustor thunks.
// Source behavior is represented by InterfaceEfxMgr construction, destruction, and class declarations.
```

The stronger recommendation is still to set `RECONSTRUCTABLE:FALSE`, clear `EMITTER_UIDS`, and leave the formal C++ block blank.

## Recommended Exact Doc Changes

Recommended target metadata for `by-memory/0x004ea060-0x004ea081.InterfaceEfxMgrSingletonAndThunks.md`:

```text
COMPLETION: 86
CONFIDENCE: 91
CANONICAL_OWNER: 00006O
RECONSTRUCTABLE: FALSE
EMITTER_UIDS:
```

Score rationale:

- Completion should rise from `84` to `86` because this report resolves the target's source-authorship question, no-code proof, helper/thunk classification, raw-name cleanup, and metadata direction.
- Confidence should rise from `90` to `91` because written prior live IDA evidence is internally consistent across the target, class, file, global, storage, vtable, and aggregate pages. It should not rise higher without fresh IDA access in the current session.

Recommended target body edits:

- Change the disposition/status from reconstructable project code to non-emitting compiler/EH glue with source-induced singleton-clear semantics.
- Replace "singleton clear helper" wording with "constructor EH singleton-clear funclet" or "constructor cleanup singleton rollback helper."
- State explicitly that `0x004ea060` is not recommended as a formal source function.
- State explicitly that `0x004ea06b` and `0x004ea076` are compiler-generated adjustor thunks.
- Preserve all exact byte, padding, vtable, xref, and singleton-storage evidence.
- Add a "No-Code Proof" section matching the reasoning above.
- Keep the cross-references to [UID:00006O], [UID:0000K9], [UID:00018G], [UID:0000R8], and [UID:0003BS]/[UID:00025K].

Recommended [UID:00006O] `InterfaceEfxMgr` class page changes:

- In the method table, change "singleton clear helper" to "constructor EH singleton-clear cleanup funclet; non-emitting compiler/EH glue."
- Keep the adjustor-thunk row but state it should be regenerated by class inheritance/vtable layout, not hand-authored.
- No score change required; optional confidence remains `88`.

Recommended [UID:0000K9] `InterfaceEfx` file page changes:

- Keep `NexusTK/ui/InterfaceEfx.cpp`.
- Update the [UID:00018I] mention to say the target is documentation-only/non-emitting after source-quality review.
- Preserve the optional future `InterfaceEfxMgr.cpp` split caveat.
- No score change required.

Recommended [UID:00018G] aggregate page changes:

- Update the [UID:00018I] row to describe compiler/EH glue, not exact child source emission.
- Preserve blank aggregate C++.
- No score change required.

Recommended [UID:0000R8] and [UID:0002VQ] singleton/global pages:

- No score change required.
- Optionally add that the `0x004ea060` clear is constructor EH cleanup glue, while the scalar destructor clear at `0x004ea0e2` is the normal destructor path.

Recommended [UID:0003BS] vtable data page:

- No score change required.
- It already correctly classifies `0x004ea06b` and `0x004ea076` as vtable slots/adjustors.

## Validator Needs After Implementation Callback

If the supervisor authorizes implementation edits, validate the touched docs and regenerated output:

> Executable block R001 was removed from this report and preserved verbatim in [00018I-InterfaceEfxMgrSingletonAndThunks-source-quality-removed.md](00018I-InterfaceEfxMgrSingletonAndThunks-source-quality-removed.md). The archived block is non-authoritative and must not be executed.

Manual generated-output checks:

- Confirm `auto-generated/NexusTK/ui/InterfaceEfx.cpp` no longer has an empty emitter marker for [UID:00018I].
- Confirm [UID:00018H] still emits `InterfaceEfxMgr::TriggerInterfaceEffect`.
- Confirm no formal `ClearInterfaceEfxMgrSingleton` or raw `sub_4EA060` source helper appears in generated output.

## Exact Pending Coverage Text

Do not edit `by-memory/-coverage-report.md` during this B-agent report. If implementation is later authorized, replace the current [UID:00018I] row in the `0x004e97b0-0x004ea121` block, between [UID:00018H] and the `0x004ea121-0x004ea130` padding row, with:

```text
    - [UID:00018I][0x004ea060-0x004ea081.InterfaceEfxMgrSingletonAndThunks](by-memory/0x004ea060-0x004ea081.InterfaceEfxMgrSingletonAndThunks.md) 0x004ea060-0x004ea081 | compiler-glue/thunk-island | InterfaceEfxMgrSingletonAndThunks : ignored : 86% : strong : B006 source-quality report classifies the island as non-emitting InterfaceEfxMgr compiler glue: 0x004ea060 is a constructor-EH singleton rollback funclet that clears g_pInterfaceEfxMgr/dword_69B360 and has no ordinary callers; 0x004ea06b and 0x004ea076 are MSVC destructor adjustor thunks for secondary/tertiary vtable slots 0x0061c688/0x0061c6b8, tail-jumping to scalar deleting destructor 0x004ea090 after this -= 0xa0/0xa4; source semantics should be represented by the InterfaceEfxMgr constructor/destructor/class declarations, so this exact island should not emit handwritten C++.
```

## Changed Files

- Created: `source-3/project-documentation/tools/leaser/Agents/Agent-B006/research/00018I-InterfaceEfxMgrSingletonAndThunks-source-quality.md`
- Modified: none outside the B006 research directory.
- Preserved: `00011T-FontStyleCore-source-quality.md`
- Preserved: `00015T-RectArea-source-quality.md`

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B006","command_id":"000000004165","destination_path":"executed-b-agent-research/B006/00018I-InterfaceEfxMgrSingletonAndThunks-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:31","uid":"00018I"} -->
<!-- {"agent":"B006","command_id":"000000023039","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 00018I-InterfaceEfxMgrSingletonAndThunks-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B006/00018I-InterfaceEfxMgrSingletonAndThunks-source-quality.md","timestamp":"2026-08-13T00:48:55-04:00","uid":"00018I"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
