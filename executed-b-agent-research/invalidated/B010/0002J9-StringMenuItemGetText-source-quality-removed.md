<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\executed-b-agent-research\B010\0002J9-StringMenuItemGetText-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable / Transaction Material Archive

- Source report: [0002J9-StringMenuItemGetText-source-quality.md](0002J9-StringMenuItemGetText-source-quality.md)
- Status: non-authoritative preservation archive; this is not a B-agent report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: this archive is non-authoritative and must never be executed, imported, evaluated, replayed, or used as operational instruction.
- Scope: exact legacy MCP request schemas, mutation call sequence, canonical-open/attestation/backup/save/reopen/rollback runbook removed from the source report.

## Removed Block R001

~~~text
I02 current public-schema lock, read live on 2026-08-11:

- `rename` requires top-level `batch` and `database`. Batch keys are exactly `func`, `data`, `local`, `stack`, `stop_on_error`, `dry_run`, `allow_overwrite`, and `pure`: function items are `addr`/`name`; data items are `old`/`new`; local and stack items are `func_addr`/`old`/`new`. I02 uses only the function and stack shapes shown below; function-name operations use literal `pure:true` and stack operations use literal `pure:false`.
- `set_type` requires top-level `edits` and `database`; each edit's complete public field set is `addr`, `ty`, `name`, `kind`, `signature`, and `variable`, with `addr` required. I02 uses only `addr`, `kind`, and `signature` and does not use this endpoint to rename a physical stack row.
- `set_repeatable_comments` requires top-level `items` and `database`; each item accepts only `addr` and `comment`. At a function entry it writes only the function-repeatable channel.
- `stack_frame` requires `addrs` and `database`; `get_comments` requires `addrs` and `database`. These readbacks are mandatory after the relevant stage.

I02 ordered deterministic stages:

| Stage | Literal current-schema request | Required intermediate/readback state |
| --- | --- | --- |
| I02-1 function name | Dry-run `rename {"database":"<fresh-canonical-session>","batch":{"func":{"addr":"0x00517070","name":"StringMenuItem__CopyText"},"stop_on_error":true,"dry_run":true,"allow_overwrite":false,"pure":true}}`; only after exact success, repeat with `dry_run:false`. | Name alone becomes `StringMenuItem__CopyText`. Type remains `errno_t __thiscall(const wchar_t *this, wchar_t *Destination, rsize_t SizeInWords)`. All four physical frame rows and all entry/interior comments remain exact prestate. |
| I02-2 function type | `set_type {"database":"<fresh-canonical-session>","edits":{"addr":"0x00517070","kind":"function","signature":"errno_t __thiscall StringMenuItem__CopyText(const StringMenuItem *this, wchar_t *destination, rsize_t sizeInWords)"}}`. | Stored type becomes exactly `errno_t __thiscall(const StringMenuItem *this, wchar_t *destination, rsize_t sizeInWords)`, and decompile renders those lowercase semantic names. The physical frame remains exactly `__saved_registers +0x0/4 _DWORD`, `__return_address +0x4/4 _UNKNOWN *`, `Destination +0x8/4 wchar_t *`, `SizeInWords +0xc/4 rsize_t`; comments remain prestate. If `set_type` changes a physical row name, offset, width, type, or count, stop without save instead of attempting the next stage. |
| I02-3 physical stack names | Dry-run `rename {"database":"<fresh-canonical-session>","batch":{"stack":[{"func_addr":"0x00517070","old":"Destination","new":"destination"},{"func_addr":"0x00517070","old":"SizeInWords","new":"sizeInWords"}],"stop_on_error":true,"dry_run":true,"allow_overwrite":false,"pure":false}}`; only after exact success, repeat with `dry_run:false`. | Stored type must remain exactly `errno_t __thiscall(const StringMenuItem *this, wchar_t *destination, rsize_t sizeInWords)`, and `stack_frame` must return exactly four rows in order: `__saved_registers +0x0/4 _DWORD`; `__return_address +0x4/4 _UNKNOWN *`; `destination +0x8/4 wchar_t *`; `sizeInWords +0xc/4 rsize_t`. No other field changes. The stack rename is not relied upon to rewrite or recase the stored prototype. |
| I02-4 function-repeatable comment | `set_repeatable_comments {"database":"<fresh-canonical-session>","items":{"addr":"0x00517070","comment":"StringMenuItem direct CopyText copies inline m_text[128] with wcscpy_s; eleven dialog call sites use the caller buffer."}}`. | Entry address regular/repeatable and function regular remain empty; function repeatable equals the literal text. Interior regular comments remain `Source`/`SizeInWords`/`Destination` at 0x00517076/0x00517077/0x0051707a and interior repeatable channels remain empty. Final name/type/frame, bytes, range, xrefs, callers, padding, and body are exact. |

Gate 2B runtime-binding, persistence, verification, and rollback contract used for that closure:

| Boundary | Exact supervisor-owned contract | Fail-closed result |
| --- | --- | --- |
| Canonical prestate and backup | Before any mutation, physically hash/size the canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. Construct one durable backup path distinct from the canonical path and any report-owned temporary root using the UID, UTC timestamp, full canonical SHA256, and a fresh nonce; require that path to be absent, create it with fail-if-exists semantics, make no overwrite attempt, hash/size it, require byte identity, and retain it through Gate 2B disposition. Dynamically call `idb_list`, then call public `idb_open` with exact fields `input_path:"E:\NTK\Resources\NexusTK\NexusTK.exe.i64"`, `mode:"force_headless"`, `run_auto_analysis:false`, `build_caches:false`, `init_hexrays:false`, `idle_ttl_sec:600`, and `preferred_session_id:""`. Require `success:true`, the exact canonical path, and a nonempty returned `session.session_id`; every later MCP request must use that exact returned identifier/path, never a preferred, remembered, or report-authored alias. | Any path ambiguity, pre-existing/colliding backup path, overwrite behavior, missing backup, backup/hash mismatch, open field/schema drift, analysis/cache/Hex-Rays initialization, empty session ID, or noncanonical returned path stops before mutation. |
| Public runtime binding | Call only public read-only `runtime_attestation` with exactly `expected_database:<returned-session-id>` and `expected_canonical_path:E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. Require top-level `schema_version:1`, `ok:true`, empty errors, a fresh listener nonce/generation whose PID owns `127.0.0.1:13337`, exactly one active stateful worker route for the returned session/path, exact redirector/worker PID-parent-generation binding, and listener/worker loaded-module manifests with `file`, `spec_origin`, and `resolved_path` equality under the attested source root plus size/timestamps/SHA256. Derive no relative module path until containment under `source_root` is proven. | Missing/partial attestation, any error, stale generation, mismatched route/session/path/process/module, extra active stateful route, or appearance of internal `runtime/attest` in public tools stops without mutation/save. Repeat this binding before each mutating stage, before the sole save, before fresh verifier readback, and before any restore/classification boundary. |
| Mutation and immediate readback | Apply only corrected I02-1 through I02-4 in order after exact dry-run/prestate checks: rename, lowercase `set_type`, physical stack rename, repeatable comment. After each stage, use the public read-only function/item, `stack_frame`, and `get_comments` endpoints to compare every authorized and protected field with that stage's expected state. I01 and I03-A through I07 are verification-only and receive no mutation. | Any extra delta, schema rejection, stale response, frame drift, comment-channel drift, xref/range/byte/type drift, or failed readback stops without continuing or saving. Never infer the stored prototype's case from physical stack-row names or vice versa. |
| Sole save | After all in-memory readbacks and another successful runtime binding, call public `idb_save` exactly once with the bound `database` and no alternate `path`, so the canonical IDB is the sole save destination. Record the save response and post-save canonical hash/size. | A failed/ambiguous save, alternate path, repeated save, binding mismatch, or missing post-save identity triggers failure classification; do not claim persisted credit. |
| Fresh-session persisted readback | Retire only the exact mutation worker/session by its bound identity through supervisor process handling, then dynamically open the canonical path again with the same exact public `idb_open` field set above, including `run_auto_analysis:false`; any preferred fresh identifier remains non-authoritative. Bind the exact returned fresh session/path with public `runtime_attestation`; reread every I01, I02, I03-A through I03-I, and I04-I07 entity, I02 intermediate/final invariant, function/frame/comment channel, vtable/RTTI dword, UDT member, xref, byte range, and padding boundary. Require the post-save disk identity to remain stable across the fresh open/readback. | Reuse of the mutation session, unmatched worker identity, analysis-enabled open, failed fresh binding, any persisted mismatch, or disk drift fails Gate 2B. |
| Rollback | Before the sole save, any failure means no `idb_save`, exact bound-worker retirement, and proof that the canonical hash/size still equals prestate; do not restore over an unchanged canonical file. After a save or ambiguous save, attest the exact canonical/session/process generation, retire only that bound route, restore the immutable verified backup to the exact canonical path, verify restored hash/size byte identity, dynamically open a fresh rollback-verifier session with the same exact `idb_open` fields including `run_auto_analysis:false`, attest it, and reread the complete protected prestate. Record whether the attempt was unsaved or restored; neither provides action credit. | Any save on a pre-save failure, inability to prove exact-route retirement, canonical no-save identity, backup identity, guarded physical restore, fresh attestation, complete prestate, or zero unintended residue leaves Gate 2B failed and forbids execution. |
~~~
