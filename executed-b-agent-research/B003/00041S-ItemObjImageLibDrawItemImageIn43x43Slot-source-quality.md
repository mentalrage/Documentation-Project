** TARGET-REPORT-UID:00041S **
** AUTHOR-AGENT-ID:B003 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID00041S ItemObjImageLib DrawItemImageIn43x43Slot Source Quality


## Finalized Report / Current Recommendation

- Current recommendation: accept the completed ordinary implementation callback. [UID:00041S] is now `92/94`, retains direct class owner/emitter [UID:00006W] `ItemObjImageLib` and `RECONSTRUCTABLE:TRUE`, and carries the accepted first-draft method body in its CPP channel.
- Final disposition: source-authored `void ItemObjImageLib::DrawItemImageIn43x43Slot(...)` in `NexusTK/render/ItemObjImageLib.cpp`; the target owns the body, [UID:0001UU] owns the class declaration, and [UID:00006W] remains the child-emitting class route.
- Ordinary implementation completed: the stale `int`/`Pane *drawOwner` contract is now `void`/`GrafPort *drawPort`; the exact target body preserves the non-value-initialized options local; UID0000TN owns the complete 32-byte options/slot-2 ABI; ItemObjImageLib.cpp and GrafPort.cpp have exact Surface visibility; UID000167's duplicate declaration is removed; and all accepted ordinary support pages are synchronized. The prior `0425` result, later failed unsaved attempt, and rollback remain dated historical evidence. The primary supervisor subsequently completed all nine Gate 2B claims: I41S-05R/I41S-06R were applied with explicit regular-address clears and exact final channel/item protections, the seven no-change claims were revalidated, one save succeeded, and a fresh persisted reopen reproduced every accepted/protected result on canonical IDB SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`. These actions are externally attributed to the supervisor, not B003. Final combined Gate 2 and report execution/archive state remain supervisor-owned and authoritative only from fresh external audit plus the current path and validator-owned history.
- Confidence: very strong for behavior, range, seven callers, ignored result, `GrafPort` receiver, item-row lookup, palette/resource/remap flow, rectangle math, callback order, overlay call, complete 32-byte shared-options layout, and Surface declaration ownership; high for inferred source spellings.
- No target-local source-reconstruction or IDA handoff blocker remains. The exact original lexical spellings are stripped, but current binary facts and project conventions support a human source reconstruction without retaining decompiler labels. Fresh exact-artifact Gate 1, final combined Gate 2, and lifecycle authority remain external supervisor decisions rather than report claims.

## Supporting Research

- Dated 2026-08-11 read-only MCP evidence: the primary research snapshot used database session `supervisor-b003-uid0001NW-retry-20260811`, canonical IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, and then-worker PID `25084`. That ephemeral session was later retired by supervisor activity. The final physical-report recheck used the fresh canonical-IDB session `supervisor-b007-uid0002YJ-rollback-fresh-20260811`, then-worker PID `2388`; `server_health` returned `status:ok`, image base `0x400000`, Hex-Rays ready, strings cache ready, and `auto_analysis_ready:false`, while bounded `lookup_funcs` and a complete target decompile succeeded. Session IDs/PIDs are dated evidence, not permanent authority.
- Current target evidence was re-read from the live function, its complete frame, seven inbound calls, resource operands, remap data, callback storage, support helper, comments, item heads, raw bytes, and named type layouts.
- Matching executed reports searched and used as leads include B005 `00017N-ItemObjImageLibLocalMethodCluster`, B008 `00006W-ItemObjImageLib-class`, B007 `0000KH-ItemObjImageLib-empty-emitter-family`, B003 `00041R-ItemObjImageLibDrawItemImage`, B005 `00041U-ItemObjImageLibDrawItemImageScaled`, B002 `00027I-MapPaneCoordinateClampConstants`, B001 `00029J-ImageLibraryLoadErrorFlag`, B003 `000167-SurfaceSpriteBlitHelper`, and caller reports for RankingRewardInfoDialog, FittingRoom, EmployeeItemPropertyDialogPane, and ItemObjectPane.
- Historical FittingRoom material that once reused UID00041S was treated only as historical address/name evidence. The current UID/path in this report is exclusively `0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md`.
- Report path/status authority is supervisor-owned and derives only from the artifact's current physical path plus validator-owned status/history metadata. This report body makes no transient gate-timing assertion.

## Target

- Target UID: `00041S`.
- Additional target UIDs: none.
- Target path: `by-memory/0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md`.

Declared-target inventory:

| Target UID | Exact path | Report role |
| --- | --- | --- |
| 00041S | `by-memory/0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md` | Sole primary target. Every other UID in this report is support evidence or an implementation destination and receives no additional-target coverage credit. |

- Dated assignment queue evidence: `auto-generated/-ag-research-tracker.md` -> `## by-memory` -> `### Not-Covered Files - Reconstructable` listed this target at `84/88`, combined `86.0`, with zero direct/additional reports when research began. That row is historical assignment evidence, not the current post-callback target state.
- Range: `[0x004defc0,0x004df1d4)`, size `0x214` / 532 bytes.
- Invariant authority classification: the ordinary implementation/revalidation callback is a dated completed fact. Current gate, IDA, manual-coverage, generated-refresh, and lifecycle status is determined only by the supervisor from the artifact's physical path and validator-owned status/history metadata, not by a transient report-body claim.
- Declared source route: target body -> [UID:00006W] `ItemObjImageLib` -> [UID:0000KH] `NexusTK/render/ItemObjImageLib.cpp`.

## Current Target State

- Metadata is now `COMPLETION:92`, `CONFIDENCE:94`, `CANONICAL_OWNER:00006W`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00006W`, blank position, populated formal CPP, and blank formal H.
- Current physical identity after the narrow reconciliation validation is SHA256 `486A45507B9DB6FE972A74AC1222216AE85F5EF11DADC81007CB6D64BA6937CE`, `16285` bytes, and `236` lines. Scoped command `000000022452` at `2026-08-11T20:12:24-04:00` exited `0` with `ok: 1`; generated refresh was disabled, projected-stat processing was an incremental no-op for UID00041S, and the target remained byte-identical to its pre-validation identity.
- The earlier command `000000022039` / SHA256 `07E85396272FDAC0CBED4F3A0C6AAF05D6C0A31EA9F416F47992610B97207BD1` is retained only as the dated original callback receipt. Later valid target-page drift superseded that identity without changing the accepted formal CPP or current behavior/metadata contracts.
- Current signature is `void ItemObjImageLib::DrawItemImageIn43x43Slot(GrafPort *drawPort, RectBounds *destinationBounds, short itemId, unsigned char paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)`.
- The former no-code rationale is retained as superseded historical evidence. Current target/support docs resolve each helper, callback, rectangle, receiver, resource, global, include, and declaration question while preserving the latent partial-initialization behavior.
- Supervisor commands `000000022519` and `000000022520` both exited `0` with `ok: 1` and completed generated refresh. Physical readback of `auto-generated/NexusTK/render/ItemObjImageLib.cpp`, SHA256 `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F`, found exactly one UID00041S block whose 3,386-character method body is character-for-character identical to the target formal CPP, no UID00041S `Empty Emitter Marker`, and no generated UID00041S/header declaration, which is intentional because the target H channel is blank and UID0001UU owns the declaration. Generated output remains validator-owned and was not edited by B003.
- Direct owner/emitter state is already correct. The defect is empty formal source plus stale source-quality prose and support declarations, not ownership.
- The current source-facing `ItemInfo` is exact size `20`: `itemTableId +0`, `paletteSlot +4`, `alpha +8`, `lightImageIndex +0x0c`, `allowPaletteFilter +0x10`, and three reserved bytes.
- The current IDA `SurfaceSpriteBlitOptions` is only an opaque 32-byte UDT. UID00041S proves `kind +0`, `alpha +4`, signed `paletteShift +8`, and signed `secondaryPaletteShift +0x1c`; RGB565 provider `0x004c0f80` plus StaticObj producer `0x004dd2c0` prove `maskPixels +0x0c`, `maskWidth +0x10`, `maskHeight +0x14`, and `maskStride +0x18`. Normal Win32 four-byte alignment supplies only implicit padding at `+1..+3`, `+9..+0xb`, and `+0x1d..+0x1f`. The target also proves that source must not value-initialize the local.
- Historical 2026-08-12 read-only authority used dated sessions `supervisor-uid0000LY-postrollback-20260812` and `supervisor-uid00041S-rollback-20260812` to establish the pre-repair drift and restored prestates; their exact evidence remains in Section 21 and they are not current-session authority.
- Current persisted supervisor receipt: public runtime authority passed; prechange backup `tools/leaser/Agents/ida-backups/NexusTK.exe.uid00041S-prechange-20260812-0719.i64` was `143205116` bytes, SHA256 `A51206AD896FAA5E3F4F798CD8AA1E40F8ECD794E8F376C80247BA78E28FE4C7E`; one post-mutation save succeeded; canonical IDB is `143205131` bytes, SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`, last-write UTC `2026-08-12T11:22:07.8699333Z`. Dated persisted-reopen session `supervisor-uid00041S-persisted-20260812` (listener PID `13936`, redirector PID `14268`, worker PID `23024`, generation `pid:23024;created:134310075005669964`) reproduced every accepted/protected result and unchanged disk identity. Session/PID data is a dated receipt, not permanent authority.
- Current IDA state now has one four-byte `SurfaceSpriteBlitProc` data item at `[0x69b3e8,0x69b3ec)` with entity `g_pfnBlitSprite` size 4, exact repeatable callback comment, regular blank, zero bytes, `114/0/0/0` refs, and exact neighbors; and one one-byte `bool` data item at `0x69b420` with entity `g_imageLibraryLoadError` size 1, exact repeatable latch comment, regular blank, separate `0x69b421-0x69b423` alignment heads, zero bytes, `132/0/0/0` refs, and exact neighbors. Target/helper/resource/remap/dependency protections also reproduced exactly.

## Executive Recommendation

- Emit the exact first-draft body in Section 22 from UID00041S. The method returns `void`; all seven callers ignore EAX, and the binary's apparent pointer return is incidental propagation from the callback or overlay helper.
- Type/name the first explicit argument as `GrafPort *drawPort`. It is loaded into ECX for the slot-2 callback, IDA already types it `GrafPort *`, and callers pass active draw ports/panes whose drawing base is GrafPort-compatible. It is not unused.
- Treat `ITEM.EPF` and `ITEM.EPD` as compiler-pooled wide string literals in source, not named source globals. Descriptive IDA analysis names are still useful for the physical string items.
- Use `g_imageLibraryLoadError` as the best inferred ImageLib-owned source global for byte `0x0069b420`; trailing `0x0069b421-0x0069b423` bytes are alignment and have zero xrefs.
- Preserve `g_pfnBlitSprite` / `SurfaceSpriteBlitProc` as the shared callback name/type. The exact call order is destination receiver, source context, source bounds, destination bounds, mode, palette, nullable options. UID0000TN/Surface.h is the one complete declaration owner because it already owns all other callback typedefs/externs and both installed providers are Surface-initializer assignments; the 114 slot refs span GrafPort, ItemObjImageLib, StaticObj, pane, dialog, text, map, and appearance consumers and reject any caller-local owner. Remove the older UID000167 GrafPort-helper-local duplicate.
- Preserve a normal non-value-initialized POD options local. The kind-3 path intentionally leaves `secondaryPaletteShift +0x1c` unwritten even though an installed RGB565 callback can read it. This is a recovered latent source bug, not permission to zero-initialize or omit the target body. The kind-8 producer/provider route separately proves the four mask-view fields, closing the former broad-layout blocker without inventing caller-specific semantics.

## Supervisor Active Recheck

- The current assignment and accepted implementation callback were both followed: report-only research preceded Gate 1, and ordinary by-* edits began only after the exact accepted SHA `9585C42E23811D64E6C7AEB91A738F4DB32206635BCEB6B23EEEA422B3DAC167` received callback authority.
- No split repair is needed before the master report. The primary body and exact surrounding padding are already separated correctly.
- Every source-bearing item directly required by this method has an implementation-ready disposition: target body, class declaration replacement, complete shared callback/options ABI, exact Surface declaration ownership, ItemObjImageLib/GrafPort include visibility, ImageLib flag declaration/definition, existing remap constants, resource-literal source treatment, and overlay helper call.
- No report-local question is deferred merely because it needs inference. Exact original spellings remain confidence caps, not blockers.

## Inference Research Guidance Check

- Binary facts were kept separate from documentation and inference. IDA establishes the physical writes, reads, call order, item bounds, types, and xrefs; current docs establish project ownership/naming conventions; inference chooses plausible source names and source shape.
- Existing assumptions treated as uncertain and rechecked: `int` return, `Pane *drawOwner`, the claim that the receiver is optimized unused, the claim that a partially initialized options record prevents source reconstruction, `unk_69B420`, pooled-string source ownership, and raw helper labels.
- The source is reconstructed as ordinary late-1990s/mid-2000s C++: direct member access, stack POD locals, explicit `NULL`-style nullable pointers represented as `0` to match current project code, simple branch logic, and no decompiler temporaries or address-derived identifiers.
- Wave2/Wave3 references are stale under the current skill and were ignored. None is used as evidence or instruction.
- Project-wide consistency is subordinate to exact behavior and plausible human source. Names follow the current ItemObjImageLib/Surface/ImageLib conventions where evidence does not require an exception.

## Heuristic / Inference Reanalysis And Validation

| Issue | Reanalysis and resolution | Classification |
| --- | --- | --- |
| Return type | Seven direct callers were checked at their post-call instruction; none consumes EAX. The body has no authored return value and only propagates helper/callback residue. Use `void`. | Strong inference from complete caller set and body. |
| `drawOwner` | The value is saved as `GrafPort *port`, restored to ECX, and used as slot-2 destination receiver. Use `GrafPort *drawPort`; reject `Pane *`, `void *`, and "unused owner". | Direct type/call evidence plus source naming inference. |
| Helper names | `0x457a60`, `0x4d02f0`, `0x543d20`, and `0x543e40` already resolve as EPFTileContext initialization, resource lookup, palette-mode query, and slot-palette lookup. `0x462f20` is the documented AlphaMaskSurface encoded-alpha member. | Current IDA plus accepted support docs. |
| Callback type/order | Slot `0x0069b3e8` has 114 refs and current `SurfaceSpriteBlitProc` tinfo. This call uses destination ECX, then context/source rect/destination rect/mode/palette/options. The semantic declaration/definition belongs to Surface, not a GrafPort helper-local preamble. | Direct disassembly/decompile, provider/consumer support, and current emitter ownership. |
| Options record | UID00041S and sibling item producers prove `kind +0`, `alpha +4`, signed `paletteShift +8`, and signed `secondaryPaletteShift +0x1c`. RGB565 kind-8 code reads `maskPixels +0x0c`, positive `maskWidth +0x10`, positive `maskHeight +0x14`, and `maskStride +0x18`; StaticObj copies those exact four members from `AlphaSurfaceBufferView` before dispatch. Kind 3 omits the +0x1c write. A normal uninitialized aggregate with mode-dependent assignments exactly reproduces this; zero-init would not. | Direct producer/provider disassembly, decompile, stack writes, and support-layout evidence. |
| Rectangle temporaries | Source rectangle is cropped to at most 43 pixels in each dimension; destination is recentered from original destination midpoint and then forced to 43x43. The compiler reuses integer locals, but no extra source type is needed beyond `RectBounds`. | Algebraically equivalent source reconstruction. |
| Item row | Binary copies all 20 bytes from `m_itemInfos.GetAtOrDefault(entry)`. Use a by-value `ItemInfo itemInfo`; reject pointer-alias/decompiler overlay source. | Direct 16-byte plus final-dword copy and current layout. |
| Resources | Exact UTF-16 bytes are `ITEM.EPF` and `ITEM.EPD`; xrefs prove pooled cross-method literals. Use `L"ITEM.EPF"` / `L"ITEM.EPD"` in source. | Direct bytes/xrefs; source-pooling inference. |
| Error global | First byte at `0x69b420` is a shared ImageLib load-failure/render-suspend latch with 132 historical refs and zero trailing-byte refs. Use inferred `bool g_imageLibraryLoadError`. | Strong behavioral/ownership inference. |
| Remap constants | Exact values `0x0972`, `0x0a20`, `0x0a56` and three-consumer xrefs support existing file-static names. | Direct data/xref evidence. |
| Source placement | `this` accesses ItemObjImageLib storage; sibling methods share resource/remap policy; direct class/file routing is already correct. | Direct member use and established emitter graph. |

Rejected alternatives:

- `int` or pointer return: rejected because no caller consumes it and no source return is assigned.
- `Pane *drawOwner` or `void *drawOwner`: rejected because the value is the active `GrafPort` callback receiver.
- Value-initialized options: rejected because it deterministically writes bytes the binary leaves indeterminate.
- Raw `unsigned char options[32]`, casts, offset writes, or a target-local private options type: rejected as decompiler-shaped and duplicate shared ABI ownership.
- Leaving formal source blank: rejected because the previous blocker is now a recoverable source bug with an ordinary C++ representation.
- New globals for `ITEM.EPF`/`ITEM.EPD`: rejected because the operands are shared pooled string literals, not source-owned storage objects.
- UI-pane ownership, Surface ownership, or ImageLib ownership for the method: rejected as dependency/consumer routes.

## Evidence Standards Used

- Direct evidence: current `idb_list`, `server_health`, `lookup_funcs`, `decompile`, `disasm`/post-call neighborhoods, `xrefs_to`, `inspect_items`, `stack_frame`, `get_comments`, `get_bytes`, and `type_inspect`.
- Corroborating evidence: current by-memory target, class/file/type/global support pages, generated ItemObjImageLib output, manual/generated coverage, current research tracker, and matching executed reports.
- Negative evidence: no EAX use after any of seven calls; no function-end overlap; all explicitly checked comment channels absent on each proposed IDA entity; no source need for named resource globals; no target write to options `+0x1c` on kind 3; no xrefs to trailing error-flag bytes.
- Evidence ladder: direct current IDA facts control behavior/range/ABI; multiple independent callers and sibling methods control source contract; current project docs control established ownership/naming; heuristic inference is used only for stripped lexical source shape.
- The target remains below `95/95` because exact original spellings are inferred and the emitted method remains first-draft source rather than final lexical recovery. Supervisor commands `000000022519`/`000000022520` and physical generated readback closed C41S-033; the complete shared options record, declaration route, generated body equality, no-empty-marker result, and intentional no-header route are proven and no longer score blockers.

## Evidence Checked

- MCP availability: the final dated 2026-08-11 canonical-IDB snapshot accepted `server_health`, bounded `lookup_funcs`, and complete target `decompile` requests through then-session `supervisor-b007-uid0002YJ-rollback-fresh-20260811` / PID `2388`. The earlier research session had been cleanly retired; no mutation method was called in either B003 evidence pass.
- Dated pre-Gate-2B function evidence: the 2026-08-11 research snapshot showed `sub_4DEFC0`, start `0x004defc0`, size `0x214`, end `0x004df1d4`, 179 instructions, `ret 0x18`, and decompiler type `int *__thiscall(_DWORD *, GrafPort *, RectBounds *, __int16, unsigned __int8, int *, float)`. Current persisted IDA state is `ItemObjImageLib_DrawItemImageIn43x43Slot` with normalized type `void __thiscall(ItemObjImageLib *this, GrafPort *drawPort, RectBounds *destinationBounds, __int16 itemId, unsigned __int8 paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)`, the accepted regular function comment, and the address regular/repeatable plus function-repeatable channels still absent.
- Complete target decompilation: guard, remap, context initialization, item-row copy, palette filter, resource lookup, category-10 palette, crop/recenter, options dispatch, slot-2 call, and optional overlay.
- Complete frame: all locals, saved/return rows, and six explicit arguments were read; exact rows are recorded in Section 21.
- Xrefs: seven direct target calls; six `ITEM.EPF` refs; four `ITEM.EPD` refs; three refs to each remap constant; 114 refs to slot `0x69b3e8`.
- Bytes: six `CC` bytes at `0x004defba-0x004defc0`, target prologue, twelve `CC` bytes at `0x004df1d4-0x004df1e0`, exact UTF-16 resource strings, and remap values.
- Types: `ItemInfo` 20 bytes with six members; `RectBounds` 16 bytes; `EPFTileContext` 40 bytes; current IDA `SurfaceSpriteBlitOptions` opaque 32 bytes; source-facing options members fully recovered at `+0/+4/+8/+0x0c/+0x10/+0x14/+0x18/+0x1c`; current `SurfaceSpriteBlitProc` slot type.
- Shared ABI/provider evidence: RGB565 provider `0x004c0f80` uses signed-byte reads at options `+8/+0x1c` and its kind-8 path reads mask pointer/width/height/stride at `+0x0c/+0x10/+0x14/+0x18`. StaticObj renderer `0x004dd2c0` calls `BuildClippedAlphaSurfaceView` at `0x004dd66a`, copies `AlphaSurfaceBufferView::{pixels,width,height,stride}` into those four exact option fields, sets kind 8, and dispatches slot 2 at `0x004dd70c`.
- Declaration visibility: UID0000TN formal H contains the exact pointer-only forward declarations `class DLPalette;`, `class GrafPort;`, `struct EPFTileContext;`, and `struct RectBounds;`, and its formal CPP includes `Surface.h`. Dated pre-callback evidence showed GrafPort.cpp with only `#include "GrafPort.h"` and ItemObjImageLib.cpp without an include preamble. The accepted callback and later supervisor-generated closure changed that state: current generated GrafPort.cpp begins with `#include "GrafPort.h"` followed by `#include "Surface.h"`; current generated ItemObjImageLib.cpp begins with `#include "EPFTileContext.h"`, `#include "ImageLib.h"`, and `#include "Surface.h"`. GrafPort.h retains complete EPFTileContext/RectBounds visibility plus pointer-only DLPalette/SurfaceSpriteBlitOptions declarations, and the current include order supplies the ImageLib flag and shared Surface ABI before UID0001UU/UID00041S emit.
- Current generated source consumers naming `g_pfnBlitSprite` and/or `SurfaceSpriteBlitOptions` are GrafPort, UserCreateAppearanceControls, TimerPane, HourPane, UserLookPane, SelfLookPane, EPFImageControlPane, NumericStringControlPane, ScrolledTextControlPane, RankingDialog, Group, UserListDialogPane, TransferServerDialogPane, and Surface; accepted UID00041S adds ItemObjImageLib. Their common physical slot has 114 refs, while only Surface owns provider installation and the callback declaration family. This establishes one shared Surface declaration owner rather than per-consumer typedefs.
- Dated pre-Gate-2B helper evidence: `0x00462f20` was `sub_462F20`, size `0x332`, with the raw nine-argument thiscall shape, complete frame, and all comment channels absent. Current persisted state is `AlphaMaskSurface_DrawEncodedAlphaFrame` over the same range/size with the accepted regular function comment, unchanged prototype/frame/bytes/nine inbound xrefs, and address regular/repeatable plus function-repeatable channels still absent.
- Documentation: UID00041S target, UID00006W class, UID0000KH file, UID0000RA position-0 singleton/preamble emitter, UID0001UU layout, UID0001UT ItemInfo, UID0000TN/UID0001PI Surface callbacks, UID00005V GrafPort class/source preamble, UID000167 helper, UID00017J StaticObj kind-8 producer, UID0001WW AlphaSurfaceBufferView, UID00029J error flag, UID0003ZN remap constants, and UID0000YO overlay helper.
- Search terms: `UID00041S`, `0x004defc0`, `DrawItemImageIn43x43Slot`, `sub_4DEFC0`, `g_pfnBlitSprite`, `SurfaceSpriteBlitOptions`, `unk_69B420`, `ITEM.EPF`, `ITEM.EPD`, and ItemObjImageLib family names.
- One earlier broad `entity_query` timed out while the worker remained healthy. Narrow bounded queries completed the same required facts; no required evidence remains missing.

## Claim And Incorporation Ledger

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C41S-001 | 00041S | Exact target is `[0x004defc0,0x004df1d4)`, size `0x214`, with six leading and twelve trailing `CC` padding bytes. | very high | lookup/bytes/disasm; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Status and Range | incorporate | applied |
| C41S-002 | 00041S | Source return is `void`; all seven direct callers ignore EAX. | very high | complete xrefs and post-call reads; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Signature/Caller Evidence and UID0001UU declaration | incorporate | applied |
| C41S-003 | 00041S | First explicit argument is active `GrafPort *drawPort`, not unused `Pane *drawOwner`. | very high | current tinfo, saved receiver, slot call; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Signature/Behavior and UID0001UU | incorporate | applied |
| C41S-004 | 00041S | Item entry begins as `unsigned short(itemId + 0x4000)` and uses the exact legacy remap/clamp constants when EPF assets are disabled. | very high | decompile/data/xrefs; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| C41S-005 | 00041S | The method initializes `EPFTileContext` and copies a complete by-value `ItemInfo` from `m_itemInfos.GetAtOrDefault`. | very high | decompile/frame/type; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior/Types | incorporate | applied |
| C41S-006 | 00041S | Frame lookup/render occurs when screen palette mode is zero or `itemInfo.allowPaletteFilter` is true. | high | direct branch and ItemInfo type; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| C41S-007 | 00041S | Source uses pooled literals `L"ITEM.EPF"` and `L"ITEM.EPD"`; these are not source globals. | very high | exact bytes and xref fanout; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Resources/Historical Corrections | incorporate | applied |
| C41S-008 | 00041S | Palette lookup is category/slot 10 with `itemInfo.paletteSlot` and `paletteFlags >> 5`. | very high | direct call operands; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| C41S-009 | 00041S | Source bounds are cropped to 43x43 only when larger than destination dimensions, then destination bounds are recentered and forced to 43x43. | very high | direct arithmetic; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Rectangle Semantics | incorporate | applied |
| C41S-010 | 00041S | Slot-2 callback order is drawPort receiver, context, source bounds, destination bounds, mode 1, palette, nullable options. | very high | decompile/disasm/support providers; implementation receipt: target 486A4550 and Surface 00EAF8EE; target validator 000000022452 exit 0, ok 1 | target Callback Contract and Surface support | incorporate | applied |
| C41S-011 | 00041S | Options fields used here are kind +0, alpha +4, signed paletteShift +8, and signed secondaryPaletteShift +0x1c. | very high | target writes and provider signed-byte reads; implementation receipt: target 486A4550 and Surface 00EAF8EE; target validator 000000022452 exit 0, ok 1 | target Options Evidence and Surface support | incorporate | applied |
| C41S-012 | 00041S | The options local must not be value-initialized; kind 3 intentionally leaves +0x1c indeterminate. | very high | absent write plus RGB565 read; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Formal CPP/Historical Correction | incorporate | applied |
| C41S-013 | 00041S | Optional overlay calls `AlphaMaskSurface::DrawEncodedAlphaFrame` after the palette-gated block with alpha `(strength*32)+0.5`. | very high | target call and UID0000YO; implementation receipt: target 486A4550 and overlay B471745B; target validator 000000022452 exit 0, ok 1 | target Overlay Behavior | incorporate | applied |
| C41S-014 | 00041S | Direct owner/emitter remains UID00006W; source file remains UID0000KH. | very high | this-based storage and emitter graph; implementation receipt: owner/emitter confirmed in target 486A4550; validator 000000022452 exit 0, ok 1 | target Ownership/Metadata | already-present | already-present |
| C41S-015 | 00041S | Target metadata should become `92/94`, reconstructable true, owner/emitter UID00006W. | high | blocker closure and first-draft readiness; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target headers/Score Rationale | incorporate | applied |
| C41S-016 | 00041S | Target CPP receives the exact Section 22 method body; target H remains blank because UID0001UU owns the declaration. | high | ownership and formal-channel rules; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target formal blocks | incorporate | applied |
| C41S-017 | 00041S | UID0001UU replaces only the stale fixed-slot declaration with the exact void/GrafPort declaration. | high | complete caller and receiver proof; implementation receipt: UID0001UU 867D0C46 | by-type ItemObjImageLibLayout formal declaration | incorporate | applied |
| C41S-018 | 00041S | UID00006W and UID0000KH replace stale fixed-slot/no-code prose, preserve scores/routes, and record the Surface declaration visibility needed before the method body. | high | current class/file docs and generated source order; implementation receipt: UID00006W 16376035 and UID0000KH C5522B48 | class/file method and source-integration sections | incorporate | applied |
| C41S-019 | 00041S | UID0000TN/Surface.h becomes the sole complete slot-2 typedef/global/options declaration owner, adds the one definition in physical slot order, and preserves partial initialization. | very high | callback-family formal owner, provider assignments, 114 refs; implementation receipt: UID0000TN 00EAF8EE | by-global SurfaceRenderCallbackTable formal CPP/H and slot 2 | incorporate | applied |
| C41S-020 | 00041S | UID0001PI records slot-2 physical storage as one typed four-byte global and no duplicate source. | high | inspect/xrefs/semantic owner; implementation receipt: UID0001PI 4E635682 | physical Surface callback page | incorporate | applied |
| C41S-021 | 00041S | UID000167 removes its duplicate local `SurfaceSpriteBlitOptions` forward/typedef/extern preamble, relies on Surface.h through GrafPort.cpp, adds UID00041S as a direct consumer, and retains only its enum/GrafPort body. | very high | current UID000167 formal CPP, exact GrafPort include route, one-definition ownership; implementation receipt: UID000167 D02649AB and UID00005V AC82E83E | SurfaceSpriteBlitHelper formal CPP and support prose | incorporate | applied |
| C41S-022 | 00041S | UID00029J adopts inferred `bool g_imageLibraryLoadError`, ImageLib ownership, one-byte semantic storage, and trailing alignment. | high | guard, 132-ref history, zero tail refs; implementation receipt: UID00029J 035B6479 | error-flag target/formal CPP/H | incorporate | applied |
| C41S-023 | 00041S | UID0003ZN retains exact three file-static short definitions and adds this target's current evidence; recommend `92/94`. | high | bytes and exact three-consumer xrefs; implementation receipt: UID0003ZN DF81E08E | remap-constants page | incorporate | applied |
| C41S-024 | 00041S | Primary supervisor Gate 2B verified the persisted target rename, source-equivalent prototype, five argument names, accepted function comment, range, bytes, seven callers, and protected frame rows before save and after fresh persisted reopen. | high | External supervisor audit `2026-08-12T07:26:00-04:00`, IDA catalog `0436`, canonical IDB SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-025 | 00041S | Primary supervisor applied I41S-05R as one four-byte `SurfaceSpriteBlitProc` data item `[0x69b3e8,0x69b3ec)` with entity `g_pfnBlitSprite` size 4, exact repeatable callback comment, explicitly cleared regular channel, and exact item/entity/byte/xref/tail/neighbor protections. | high | External supervisor observed the known latent regular text, cleared it through the accepted public action, proved final regular absent/repeatable exact, saved once, and reproduced all state after persisted reopen on canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-026 | 00041S | Primary supervisor applied I41S-06R as one one-byte `bool` data item at `0x69b420` with entity `g_imageLibraryLoadError` size 1, exact repeatable latch comment, explicitly cleared regular channel, retained three separate alignment heads, and exact byte/xref/neighbor protections. | high | External supervisor proved final regular absent/repeatable exact, `132/0/0/0` refs, zero bytes, exact tails/neighbors, one save, and persisted reopen on canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-027 | 00041S | Primary supervisor Gate 2B verified both resource literals remain single 18-byte `const wchar_t[9]` items with exact names/types/UTF-16 bytes/comments/boundaries and `6/4` refs. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-028 | 00041S | Primary supervisor Gate 2B verified the three named signed remap constants and alternating gaps retain exact types, values, comments, boundaries, and `3/0/3/0/3/0` refs. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-029 | 00041S | Primary supervisor Gate 2B verified `AlphaMaskSurface_DrawEncodedAlphaFrame` retains exact name/range/prototype/frame/comments/bytes and nine callers. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-030 | 00041S | Primary supervisor Gate 2B verified already named dependencies and every prohibited unlisted name/type/comment/item/byte/xref state remain unchanged. | very high | External supervisor no-change protections passed before save and after persisted reopen; no B003 action credit. | IDA no-change protections | already-present | applied |
| C41S-031 | 00041S | No split/merge/range change is needed; preserve padding ownership on UID0000VN. | very high | exact boundaries and sibling starts; implementation receipt: target/aggregate 486A4550/C5FE001B; target validator 000000022452 exit 0, ok 1 | target Range and ignored padding | already-present | already-present |
| C41S-032 | 00041S | The exact UID00041S manual by-memory coverage row is physically present once after UID0000VN and before UID0000UQ; no further manual edit is requested. | high | Supervisor command `000000022520`, coverage SHA256 `8D080707A33B24C6A79CC91FE0CA1DFA339F5174F5B79FF94D01F59C35AF425B`, exact row/location readback. | supervisor-owned by-memory/-coverage-report.md | already-present | already-present |
| C41S-033 | 00041S | Supervisor generated refresh/readback confirms exactly one UID00041S generated block, exact formal/generated body equality, no UID00041S empty marker, and intentional no-header output. | high | Supervisor commands `000000022519`/`000000022520`; ItemObjImageLib.cpp SHA256 `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F`; 3,386-character exact body match. | supervisor generated refresh/readback | incorporate | applied |
| C41S-034 | 00041S | Historical `int`, `Pane *`, unused-owner, unresolved-callback, pooled-global, and impossible-partial-init assumptions must be retained as superseded evidence. | high | current docs versus current IDA; implementation receipt: historical corrections retained in target/support; target 486A4550; validator 000000022452 exit 0, ok 1 | target/support Historical Corrections | historicalize | applied |
| C41S-035 | 00041S | Wave2/Wave3 material is stale and must not be incorporated. | very high | current skill/goal; implementation receipt: current skill rejects stale Wave2/Wave3; none incorporated | target evidence provenance | reject-stale | excluded-with-reason |
| C41S-036 | 00041S | The complete 32-byte shared options ABI is kind +0, alpha +4, signed paletteShift +8, maskPixels +0x0c, maskWidth +0x10, maskHeight +0x14, maskStride +0x18, and signed secondaryPaletteShift +0x1c with only implicit Win32 alignment padding. | very high | UID00041S/sibling producers, RGB565 provider, StaticObj kind-8 producer; implementation receipt: UID0000TN 00EAF8EE | Surface formal H and target/support ABI sections | incorporate | applied |
| C41S-037 | 00041S | StaticObj kind 8 copies the exact AlphaSurfaceBufferView pixels/width/height/stride quartet into options +0x0c..+0x18 before slot-2 dispatch. | very high | live 0x4dd66a/0x4dd70c decompile and UID0001WW layout; implementation receipt: UID00017J EC7372FD | UID00017J and Surface options evidence | incorporate | applied |
| C41S-038 | 00041S | Surface.h already has exact pointer-only GrafPort, EPFTileContext, RectBounds, and DLPalette forward declarations; the options struct/typedef belongs immediately after them and the extern/definition follows physical slot order. | very high | UID0000TN formal CPP/H and generated Surface.h/cpp; implementation receipt: UID0000TN 00EAF8EE | UID0000TN formal CPP/H | incorporate | applied |
| C41S-039 | 00041S | ItemObjImageLib.cpp position-0 preamble must include EPFTileContext.h, ImageLib.h, and Surface.h before the singleton/class/method emitters. | very high | current generated source order and complete-type use; implementation receipt: UID0000RA 78387651 and UID0000KH C5522B48 | UID0000RA formal CPP and UID0000KH integration prose | incorporate | applied |
| C41S-040 | 00041S | GrafPort.cpp must include Surface.h immediately after GrafPort.h; GrafPort.h retains only its pointer-use SurfaceSpriteBlitOptions forward declaration. | very high | current UID00005V CPP/H and UID000167 dedup route; implementation receipt: UID00005V AC82E83E and UID000167 D02649AB | UID00005V formal CPP and UID000167 support | incorporate | applied |
| C41S-041 | 00041S | The current 114-ref and generated-consumer inventory proves one cross-subsystem slot-2 ABI and rejects per-caller declarations or ownership. | very high | complete slot xrefs and generated source search; implementation receipt: UID0000TN 00EAF8EE and UID0001PI 4E635682 | UID0000TN consumer inventory and affected support pages | incorporate | applied |
| C41S-042 | 00041S | Primary supervisor Gate 2B bound every I41S-05..11 physical head/span/type/entity-name/comment/value/xref/neighbor protection, including intermediate and final regular/repeatable channel reads for I41S-05R/I41S-06R. | very high | External supervisor complete pre-action, post-action, post-save, and persisted-reopen census under canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| C41S-043 | 00041S | Primary supervisor Gate 2B verified the exact post-I41S-02 target prototype/frame and every protected local/saved/return row before save and after fresh persisted reopen. | very high | External supervisor audit `2026-08-12T07:26:00-04:00`, IDA catalog `0436`, exact persisted target/frame readback; no B003 action credit. | supervisor Gate 2B | incorporate | applied |

## Positive Evidence Summary

- The complete current decompile contains every behavior needed for a source-shaped method; no raw/unmodeled child lies inside the target range.
- All seven caller sites pass a draw receiver and ignore EAX, independently supporting `GrafPort *` and `void`.
- ItemObjImageLib sibling source already uses `m_itemInfos.GetAtOrDefault`, `EPFTileContext::Initialize`, `g_pEPFLib->LookupLayoutEntry`, the same resource literals, and the same remap names.
- Slot-2 storage has current `SurfaceSpriteBlitProc` tinfo and 114 refs; existing Surface docs already accept `g_pfnBlitSprite` and the nullable options pointer.
- RGB565 provider reads and the StaticObj kind-8 producer close every non-padding byte of the 32-byte options aggregate. The exact source representation is an ordinary uninitialized POD local with explicit per-mode writes; this preserves binary behavior while avoiding raw byte arrays, offset casts, or invented reserved storage.
- UID0000TN already owns the public Surface callback family and exact pointer-only forwards. UID0000RA and UID00005V provide deterministic position-0 include insertion points for ItemObjImageLib.cpp and GrafPort.cpp, so UID000167 can be deduplicated without losing declaration visibility.
- Direct class/file ownership, exact range, and padding are already stable and require no structural repair.

## IDA MCP Facts

- Current target function: `ItemObjImageLib_DrawItemImageIn43x43Slot` at `0x004defc0`, size `0x214`, range `[0x004defc0,0x004df1d4)`, 179 instructions, and `ret 0x18`. Persisted normalized type is `void __thiscall(ItemObjImageLib *this, GrafPort *drawPort, RectBounds *destinationBounds, __int16 itemId, unsigned __int8 paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)` with all accepted argument names at their original slots.
- Current target comments: the accepted regular function comment is present; address regular, address repeatable, and function-repeatable channels remain absent. The exact range, bytes, seven incoming xrefs, and protected local/saved/return rows remained unchanged.
- Boundary bytes: `CC` x6 before; function begins `55 8B EC 83 EC 5C`; `CC` x12 after.
- Seven callers: `0x41f7bc`, `0x45bbc5`, `0x45e884`, `0x4a28cc`, `0x4a4644`, `0x4ec3f9`, and `0x4eec03`; `more:false`.
- Current resource items: `ItemEpfResourceLiteral` is one 18-byte `const wchar_t[9]` data item at `[0x0061c288,0x0061c29a)` with exact `ITEM.EPF` UTF-16 bytes, six xrefs, regular comment absent, and the accepted repeatable literal-use comment. `ItemEpdResourceLiteral` is the corresponding one 18-byte `const wchar_t[9]` item at `[0x0061c29c,0x0061c2ae)` with exact `ITEM.EPD` bytes, four xrefs, regular comment absent, and its accepted repeatable literal-use comment.
- Current remap items: `g_legacyItemImageMaxEntry` at `0x0066dad4` is a separate signed `__int16` value `0x0972`; `g_currentItemImageRemapFirstEntry` at `0x0066dad8` is a separate signed `__int16` value `0x0a20`; and `g_currentItemImageRemapLastEntry` at `0x0066dadc` is a separate signed `__int16` value `0x0a56`. Each has three xrefs, regular comment absent, and its accepted repeatable comment; all alternating two-byte gap heads remain separate, unnamed, untyped, blank-comment, zero-value, and zero-ref.
- Current I41S-05R span: `[0x0069b3e8,0x0069b3ec)` is one four-byte code=false/data=true item of type `SurfaceSpriteBlitProc`, with unique entity/global `g_pfnBlitSprite` at `0x69b3e8`, size 4. Tail heads no longer resolve independently. Address regular is absent and repeatable is exactly `Surface sprite/frame blit callback: destination GrafPort receiver, source context and rectangles, mode, palette, and nullable options.` All four bytes are zero; inbound refs remain `114/0/0/0`; the preceding `g_pfnDrawLineDelta` slot and following `g_pfnFillRle` slot retain exact item/type/entity/comment/ref/byte state. The supervisor observed the known latent regular text after the repeatable write, explicitly cleared it, and proved repeatable unchanged.
- Current I41S-06R span: `[0x0069b420,0x0069b421)` is one one-byte code=false/data=true `bool` item with unique entity/global `g_imageLibraryLoadError`, size 1. Address regular is absent and repeatable is exactly `Shared ImageLib load-failure/render-suspend latch; trailing three bytes are alignment.` The supervisor explicitly cleared the regular channel. `0x69b421`, `0x69b422`, and `0x69b423` remain separate unnamed/untyped code=false/data=false alignment heads. All four bytes are zero; inbound refs remain `132/0/0/0`; protected `[0x69b41c,0x69b420)` and `[0x69b424,0x69b428)` neighbors retain exact item/type/entity/comment/ref/byte state.
- Current support helper: `AlphaMaskSurface_DrawEncodedAlphaFrame` at `[0x00462f20,0x00463252)`, size `0x332`, retains its existing raw thiscall prototype and complete frame, bytes, and nine incoming xrefs. The accepted regular function comment is present; address regular/repeatable and function-repeatable channels remain absent.

**Historical prestates and prior closure retained as dated evidence:**

- The target was named `sub_4DEFC0` with type `int *__thiscall(_DWORD *this, GrafPort *, RectBounds *destinationBounds, __int16, unsigned __int8, int *, float)` and all four address/function comment channels blank.
- Slot 2 was split into four one-byte heads: the `0x69b3e8` head was unnamed in `inspect_items`, non-code/non-data, carried `SurfaceSpriteBlitProc` plus the `g_pfnBlitSprite` name-index alias, had 114 refs and blank comments; the three tail heads were unnamed/untyped non-code/non-data bytes with zero refs and blank comments.
- The error latch was one-byte `unk_69B420`, untyped and blank-comment, followed by three separate unnamed/untyped blank-comment zero-ref alignment heads.
- `ITEM.EPF` and `ITEM.EPD` were fragmented compiler-pool data heads with exact UTF-16 bytes and six/four refs; the remap values were separate `word_` heads with values `0x0972`, `0x0a20`, and `0x0a56`, three refs each, and blank comments. Section 21 preserves the complete head-by-head prestates.
- The support helper was `sub_462F20` with its raw thiscall type, complete frame, and all four address/function comment channels blank.
- The prior `0425` supervisor result subsequently recorded I41S-05 as one four-byte data item with an accepted repeatable callback comment and I41S-06 as one one-byte bool data item with an accepted repeatable latch comment. That result and its then-saved IDB SHA256 `6D17FC31F5F1D28576F12B1EC45ED684125AA25A421273DF238313BA4339911E` remain valid historical evidence. The later failed unsaved attempt and rollback are also historical; the persisted 0719 receipt above is authoritative for current item/comment state.

## Function / Child Inventory

| Range / item | UID / path | Role | Reconstructable | Parent | Current score | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004defc0-0x004df1d4` | UID00041S target | Source-authored fixed-slot method | true | UID00006W | 92/94 | Exact Section 22 CPP is already applied and retained; H declaration remains UID0001UU-owned. |
| `0x004defba-0x004defc0` | UID0000VN ignored | Leading alignment | false/ignored | none | 100 | Preserve six `CC`; no source. |
| `0x004df1d4-0x004df1e0` | UID0000VN ignored | Trailing alignment | false/ignored | none | 100 | Preserve twelve `CC`; no source. |
| `0x0069b3e8-0x0069b3ec` | UID0001PI / UID0000TN | Slot-2 callback global | true | Surface | 92/94 physical; 94/95 semantic | One shared declaration/definition; target is consumer only. |
| `0x0069b420-0x0069b424` | UID00029J | Shared ImageLib error latch plus alignment | true | UID0000K2 | 91/94 | Exact bool declaration/definition is applied; preserve tail alignment. |
| `0x0066dad4-0x0066dae0` | UID0003ZN | Three remap shorts plus gaps | true | UID0000KH | 92/94 | Existing exact CPP is retained and evidence/score are synchronized. |
| `0x0061c288` / `0x0061c29c` | pooled literals | ITEM.EPF / ITEM.EPD | literal source | linker pool | n/a | Use literals; descriptive IDA item names only. |
| `0x00462f20-0x00463252` | UID0000YO | AlphaMaskSurface overlay helper | true | UID0000HF | 88/91 | Existing source-facing member call; target is consumer only. |

No child, split, raw helper, table island, or compiler-only subrange exists inside UID00041S. Every required dependency has an explicit ownership and code/no-code disposition above.

## Direct Xref / Caller Inventory

| Call address | Containing function | Immediate post-call evidence | Meaning |
| --- | --- | --- | --- |
| `0x0041f7bc` | `sub_41F2B0` | control jumps onward; no EAX read | Fitting-room/list icon consumer; result ignored. |
| `0x0045bbc5` | `sub_45B630` | control jumps onward; no EAX read | Item UI consumer; result ignored. |
| `0x0045e884` | `RankingRewardInfoDialog_OnPaint` | continues paint path; no EAX read | Ranking reward item icon. |
| `0x004a28cc` | `sub_4A2560` | next operation pushes unrelated argument | Dialog item icon; result ignored. |
| `0x004a4644` | `sub_4A4570` | next operation loads security cookie state | Employee item property icon; result ignored. |
| `0x004ec3f9` | `sub_4EBB20` | next operation loads unrelated local/work state | Item rendering consumer; result ignored. |
| `0x004eec03` | `sub_4EE6F0` | next operation pushes unrelated value | Item rendering consumer; result ignored. |

- Every call supplies six stack arguments and a `this` receiver for ItemObjImageLib.
- The first explicit argument becomes the low-level destination receiver in ECX at the slot-2 call. This is positive use evidence, not an optimized-unused parameter.
- No data xref, vtable slot, thunk, or alternate entry exists for the target.

## Documentation Evidence And IDA Status

- Current UID00041S is physically `92/94`, owner/emitter `00006W`, reconstructable true, blank position, populated formal CPP, and blank formal H. Its signature, complete behavior, callback/options ABI, pooled-resource treatment, ImageLib latch, score rationale, and superseded no-code assumptions are incorporated.
- UID0001UU now carries the accepted `void`/`GrafPort *drawPort` declaration; UID00006W and UID0000KH carry the matching class/file contract and preserve the class/file route.
- UID0000TN is now the sole complete `SurfaceSpriteBlitOptions` / `SurfaceSpriteBlitProc` / `g_pfnBlitSprite` declaration owner. Its Surface H retains the exact `DLPalette`, `GrafPort`, `EPFTileContext`, and `RectBounds` forwards and carries the complete options/typedef/extern payload; its Surface CPP carries the slot-2 definition in physical order.
- UID0000RA now supplies the accepted position-0 `EPFTileContext.h`, `ImageLib.h`, and `Surface.h` preamble before ItemObjImageLib emitters. UID00005V supplies `Surface.h` immediately after `GrafPort.h`; GrafPort.h retains only its pointer-use options forward declaration.
- UID000167's former consumer-local forward/typedef/extern copy is absent, leaving one complete Surface-owned declaration without losing GrafPort.cpp visibility.
- UID00017J records the decisive StaticObj kind-8 `AlphaSurfaceBufferView` pixels/width/height/stride copy into options `+0x0c..+0x18`; this is shared-layout evidence, not ownership transfer or renderer-body promotion.
- UID00029J now carries inferred `bool g_imageLibraryLoadError`, one-byte storage/alignment evidence, and formal ImageLib CPP/H. UID0003ZN carries the reconfirmed three short constants, current consumer evidence, and synchronized score.
- Current validator-owned ItemObjImageLib.cpp physically contains the UID00041S method body at SHA256 `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F`. Supervisor commands `000000022519` and `000000022520` closed C41S-033 with exact formal/generated body equality, no UID00041S empty marker, and the intentional no-header result.
- Section 21 preserves the dated accepted IDA handoff, historical `0425` closure, drift evidence, failed unsaved I41S-05R attempt, rollback proof, and every prestate/safety constraint. It also records the primary supervisor's persisted 0719 closure of all nine Gate 2B rows, including exact I41S-05R/I41S-06R regular-clear/channel readbacks, one save, and fresh persisted reopen. B003 performed no mutation or save and receives no such credit.

## Ranked Ownership Analysis

### 1. ItemObjImageLib class / ItemObjImageLib.cpp

- Evidence for: `this` indexes the embedded `ProtectedArray<ItemInfo>`; sibling methods share ITEM resources and remap constants; all source policy is item-image-specific; current canonical owner/emitter route is already UID00006W -> UID0000KH.
- Evidence against: none.
- Decision: retain as exact direct owner and source file.

### 2. Surface / callback infrastructure

- Evidence for: owns `g_pfnBlitSprite`, its ABI, providers, and shared options type.
- Evidence against: the target neither initializes the callback table nor implements a callback provider; it consumes one slot after item-specific policy.
- Decision: dependency/support owner only.

### 3. ImageLib common state

- Evidence for: owns the shared load-error/render-suspend flag used by many image libraries.
- Evidence against: does not own ItemObjImageLib row lookup, remap, palette, rectangle, or draw behavior.
- Decision: dependency/global owner only.

### Rejected UI-pane ownership

- Callers are consumers from several UI features; none owns the shared item archive/remap/palette policy. No pane class is a source owner for this method.

## Source Placement

- Recommended definition: `NexusTK/render/ItemObjImageLib.cpp`, child body emitted through UID00006W/UID0000KH.
- Recommended declaration: existing class declaration owner UID0001UU; replace the one fixed-slot method declaration, do not duplicate it in UID00041S H.
- Shared callback declaration/definition: Surface-owned UID0000TN.
- Exact shared-header route: `Surface.h` uses only forward declarations for `GrafPort`, `EPFTileContext`, `RectBounds`, and `DLPalette` because the callback ABI contains pointers only. ItemObjImageLib.cpp additionally includes `EPFTileContext.h` for the by-value context and complete nested `RectBounds`, plus `ImageLib.h` and `Surface.h`; GrafPort.cpp includes complete `GrafPort.h` first and then `Surface.h`.
- Shared load-error global: ImageLib-owned UID00029J/UID0000K2.
- File-static remap constants: existing UID0003ZN under ItemObjImageLib.cpp.
- Rejected placements: FittingRoom, RankingRewardInfoDialog, EmployeeDialogPane, PaletteLib, ResourceLayoutTable, AlphaMaskSurface, Surface callback-provider bodies, and a new standalone file.
- Exact original header filenames remain a lexical/source-tree confidence cap, but current formal ownership is deterministic and implementation-ready.

## Range / Split / Padding / Reclassification Analysis

- Target range is exact and already isolated: function begins at `0x004defc0`, ends before `0x004df1d4`, and has no internal child/function/data split.
- Leading `0x004defba-0x004defc0` is six `CC` bytes owned by UID0000VN ignored padding.
- Trailing `0x004df1d4-0x004df1e0` is twelve `CC` bytes before sibling DrawItemImageUnscaled, also UID0000VN ignored padding.
- Keep UID00017N as a non-emitting exact split index. Do not move the method body to the aggregate.
- No merge, new child, covered-by marker, compiler-generated reclassification, or range expansion is warranted.

## Negative Evidence Summary

- No caller uses EAX; this rejects authored `int`, pointer, status, or fluent-return contracts.
- No evidence supports `Pane *` as the static type of the first explicit argument. The target uses it precisely as a `GrafPort` receiver.
- No target write initializes options byte `+0x1c` on kind 3. A zero-initialized struct would change observed behavior and is forbidden.
- No evidence supports named source globals for `ITEM.EPF` or `ITEM.EPD`; multiple methods share pooled literals.
- No source-bearing child or helper lies within the target range.
- Callback-provider breadth rejects caller-specific ownership/name projections and rejects moving the callback global into ItemObjImageLib.
- No provider or producer evidence leaves an unexplained non-padding byte in `SurfaceSpriteBlitOptions`; preserving an opaque/incomplete struct after the kind-8 producer/provider closure would be less accurate than the complete shared declaration.
- The broad shared error-latch fanout rejects ItemObjImageLib ownership for `0x69b420`; ImageLib remains the declaration owner.
- Exact original lexical names are absent, but retaining `sub_`, `unk_`, `word_`, `aIt_`, `a2`, or `v17.opaque` would violate the project reconstruction objective and is not an acceptable fallback.

## IDA Rename / Type / Comment Recommendations

The parser/type matrix, prestates, action rows, protections, and expected readbacks below are the dated accepted non-executable supervisor-owned Gate 2B handoff. In those preserved rows, words such as `current`, `proposed`, `require`, and `expected` describe the pre-Gate-2B handoff state. `<SESSION>` denotes only an exact database identifier freshly accepted by the supervisor's external authority checks. The first closure matrix preserves the historical `0425` result; the drift/failure/rollback material and two-row repair handoff preserve the later repair cycle; the persisted 0719 closure disposition after those rows is authoritative for current IDA state. Operational orchestration remains outside the report.

Current public-schema parser contract: `make_data.items[].type` is a full C declaration containing its declarator name, not a bare type expression. The `name` field repeats the same declarator for deterministic symbol naming. Before I41S-03 or any I41S-05..I41S-11 mutation, the dated accepted handoff required the supervisor to issue the literal read-only named-type preflight `type_inspect {database:"<SESSION>",queries:[{name:"ItemObjImageLib",include_members:false},{name:"GrafPort",include_members:false},{name:"RectBounds",include_members:true,max_members:20},{name:"AlphaMaskSurface",include_members:false},{name:"SurfaceSpriteBlitProc",include_members:false},{name:"wchar_t",include_members:false}]}` and the literal catalog preflight `type_query {database:"<SESSION>",queries:[{filter:"ItemObjImageLib",kind:"any",count:10,include_decl:true,include_relationships:true},{filter:"GrafPort",kind:"any",count:10,include_decl:true,include_relationships:true},{filter:"RectBounds",kind:"any",count:10,include_decl:true,include_relationships:true},{filter:"AlphaMaskSurface",kind:"any",count:10,include_decl:true,include_relationships:true},{filter:"SurfaceSpriteBlitProc",kind:"any",count:10,include_decl:true,include_relationships:true},{filter:"wchar_t",kind:"any",count:10,include_decl:true,include_relationships:true}]}` and compare every result with the matrix below. Built-in C/C++ grammar tokens (`void`, `__thiscall`, `short`, `unsigned char`, `bool`, and `float`) are parser primitives rather than local named-type entries; current `type_inspect` therefore reports `Type not found` for such scalar-token probes, which is not authority to declare replacement types. Every action below supplies a complete parser declaration. A missing/changed named type, wrong ordinal/size/kind/member layout, declarator mismatch, or parser rejection is an unsupported-type failure: the accepted handoff required the entire UID00041S transaction to stop before destructive materialization, comment mutation, or save. It forbade creating or declaring substitute types and forbade deferring type discovery to a later Gate 2B step.

| Consumed by | Parser-visible type/declaration token | Literal current availability/prestate | Required disposition |
| --- | --- | --- | --- |
| I41S-03 | `ItemObjImageLib *this` | `type_inspect`: `exists:true`, declaration `ItemObjImageLib`, incomplete/pointer-only record; `type_query`: ordinal `437`, kind `other`, same declaration; no complete object size is required by this signature. | Exact result required. Missing type, ordinal/name substitution, or incompatible non-class meaning stops the complete transaction; do not synthesize a replacement declaration. |
| I41S-03 | `GrafPort *drawPort` | `type_inspect`: `exists:true`, declaration `GrafPort`, `is_udt:true`, size `0xa0`; `type_query`: ordinal `477`, kind `struct`, same size/declaration. | Exact UDT identity/ordinal/size required; mismatch stops before I41S-03. |
| I41S-03 | `RectBounds *destinationBounds` | `type_inspect`: `exists:true`, declaration `RectBounds`, `is_udt:true`, size `0x10`, members `left +0x0 int`, `top +0x4 int`, `right +0x8 int`, `bottom +0xc int`; `type_query`: ordinal `465`, kind `struct`. | Exact ordinal and four-member layout required; mismatch stops before I41S-03. |
| I41S-03 | `AlphaMaskSurface *overlayMask` | `type_inspect`: `exists:true`, declaration `AlphaMaskSurface`, incomplete/pointer-only record; `type_query`: ordinal `696`, kind `other`, same declaration; no complete object size is required by this signature. | Exact result required. Missing type, ordinal/name substitution, or incompatible non-class meaning stops the complete transaction. |
| I41S-03 | `void`, `__thiscall`, `short`, `unsigned char`, `float` | Current IDA parser grammar already renders the target's `__thiscall`, signed 16-bit, unsigned 8-bit, and `float` components. These tokens are built-in grammar and are not expected to have local-type ordinals. | Use only the literal complete I41S-03 signature. Any parser rejection stops the complete transaction; do not call `declare_type`. |
| I41S-05 | `SurfaceSpriteBlitProc g_pfnBlitSprite` | `type_inspect`: `exists:true`, declaration `SurfaceSpriteBlitProc`, `is_ptr:true`, size `0x4`; `type_query`: ordinal `906`, kind `typedef`, same size/declaration; the current `0x69b3e8` head already carries the same tinfo/name-index alias. | Exact typedef ordinal/pointer kind/size and physical prestate required. Missing/changed typedef stops before any I41S-05..11 action. |
| I41S-06 | `bool g_imageLibraryLoadError` | `bool` is the built-in one-byte C++ parser token; it is not a named local-type entry. The required destination span is one byte and I41S-06 separately protects the three following alignment heads. | Full declaration must parse as size `1`. Any parse result other than one byte stops before deletion/materialization; do not replace it with a project typedef. |
| I41S-07/I41S-08 | `const wchar_t ItemEpfResourceLiteral[9]` / `const wchar_t ItemEpdResourceLiteral[9]` | `type_inspect`: `exists:true`, declaration `wchar_t`, size `0x2`; `type_query`: ordinal `13`, kind `typedef`, same size/declaration; each destination has exact 18-byte UTF-16 storage. | Exact typedef ordinal/two-byte element type and total size `0x12` required. Any mismatch stops before consuming the existing literal heads. |
| I41S-09/I41S-10/I41S-11 | `short g_legacyItemImageMaxEntry`, `short g_currentItemImageRemapFirstEntry`, `short g_currentItemImageRemapLastEntry` | `short` is the built-in signed 16-bit C parser token, not a named local-type entry; each current destination head is exactly two bytes with the recorded signed-sentinel/remap use. | Each full declaration must parse as size `2`. Any other result stops before replacing that head; do not alter adjacent two-byte gaps. |

| ID | Exact entity and literal current pre-state | Proposed action | Evidence / safety constraints | Expected readback |
| --- | --- | --- | --- | --- |
| I41S-01 | Function `0x004defc0`, name `sub_4DEFC0`; current type `int *__thiscall(_DWORD *this, GrafPort *, RectBounds *destinationBounds, __int16, unsigned __int8, int *, float)`; address regular absent; address repeatable absent; function regular absent; function repeatable absent. | Dry-run `rename {database:"<SESSION>",batch:{func:{addr:"0x004defc0",name:"ItemObjImageLib_DrawItemImageIn43x43Slot"},pure:true,dry_run:true,stop_on_error:true,allow_overwrite:false}}`; require clean result, then apply `rename {database:"<SESSION>",batch:{func:{addr:"0x004defc0",name:"ItemObjImageLib_DrawItemImageIn43x43Slot"},pure:true,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; then apply I41S-02/I41S-03. | Destination collision check must be empty; preserve `[0x4defc0,0x4df1d4)` and all xrefs/bytes. | `lookup_funcs {database:"<SESSION>",queries:["0x004defc0"]}` returns the exact new name at the same start/end/type; `inspect_items`, `get_bytes`, and `xrefs_to` return no unlisted change. |
| I41S-02 | Target frame arguments: `arg_0 +0x6c _DWORD`, `destinationBounds +0x70 RectBounds *`, `arg_8 +0x74 _DWORD`, `arg_C +0x78 _BYTE`, `arg_10 +0x7c _DWORD`, `arg_14 +0x80 _DWORD`. | In row order, require each clean dry-run before its exact apply: (1) `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_0",new:"drawPort"},pure:false,dry_run:true,stop_on_error:true,allow_overwrite:false}}`, then `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_0",new:"drawPort"},pure:false,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; (2) `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_8",new:"itemId"},pure:false,dry_run:true,stop_on_error:true,allow_overwrite:false}}`, then `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_8",new:"itemId"},pure:false,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; (3) `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_C",new:"paletteFlags"},pure:false,dry_run:true,stop_on_error:true,allow_overwrite:false}}`, then `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_C",new:"paletteFlags"},pure:false,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; (4) `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_10",new:"overlayMask"},pure:false,dry_run:true,stop_on_error:true,allow_overwrite:false}}`, then `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_10",new:"overlayMask"},pure:false,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; (5) `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_14",new:"overlayStrength"},pure:false,dry_run:true,stop_on_error:true,allow_overwrite:false}}`, then `rename {database:"<SESSION>",batch:{stack:{func_addr:"0x004defc0",old:"arg_14",new:"overlayStrength"},pure:false,dry_run:false,stop_on_error:true,allow_overwrite:false}}`. | Do not rename compiler-aliased locals. `destinationBounds` is already correct. Every occupied stack rename is explicitly `pure:false`; a failure stops before the next row. | `stack_frame {database:"<SESSION>",addrs:["0x004defc0"]}` returns the five exact new names at unchanged physical offsets/types/widths, unchanged `destinationBounds`, and no local/saved/return row addition, deletion, or movement. |
| I41S-03 | Exact post-I41S-02 action-entry frame: `drawPort +0x6c _DWORD`, `destinationBounds +0x70 RectBounds *`, `itemId +0x74 _DWORD`, `paletteFlags +0x78 _BYTE`, `overlayMask +0x7c _DWORD`, and `overlayStrength +0x80 _DWORD`; function return/this/argument types otherwise remain stale. Every protected local/saved/return row in the table below is still exact. | `set_type {database:"<SESSION>",edits:{addr:"0x004defc0",kind:"function",signature:"void __thiscall ItemObjImageLib_DrawItemImageIn43x43Slot(ItemObjImageLib *this, GrafPort *drawPort, RectBounds *destinationBounds, short itemId, unsigned char paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)"}}`. | Enter only after exact post-I41S-02 names/types/offsets match. Preserve physical stack slots and all locals/saved/return rows. Stop if any protected row changes or decompilation loses the exact call flow. | `lookup_funcs {database:"<SESSION>",queries:["0x004defc0"]}` and `stack_frame {database:"<SESSION>",addrs:["0x004defc0"]}` return the exact signature/argument matrix below with unchanged locals/bytes/xrefs/range and unchanged protected frame rows. |
| I41S-04 | At `0x004defc0`, address regular absent; address repeatable absent; function regular absent; function repeatable absent. | `set_function_comments {database:"<SESSION>",items:{addr:"0x004defc0",comment:"Draw an ItemObjImageLib entry centered in a mutable 43x43 destination slot, applying legacy EPF/EPD remap, palette options, sprite-blit dispatch, and an optional alpha-mask overlay."}}`. | Set the regular function channel only; do not initialize address regular/repeatable or function-repeatable channels. | `get_comments {database:"<SESSION>",addrs:["0x004defc0"]}` returns the exact regular function comment; the three protected channels remain absent. |
| I41S-05 | Exact `[0x69b3e8,0x69b3ec)` prestate: `0x69b3e8-0x69b3e9` is size-1, unnamed in `inspect_items`, name-index alias `g_pfnBlitSprite`, type `SurfaceSpriteBlitProc`, code=false/data=false, blank regular/repeatable comments, 114 refs; `0x69b3e9`, `0x69b3ea`, and `0x69b3eb` are separate size-1 unnamed/untyped code=false/data=false heads with blank comments and zero refs. Bytes are `00 00 00 00`. | After the exact parser/type matrix passes, `make_data {database:"<SESSION>",items:{addr:"0x0069b3e8",type:"SurfaceSpriteBlitProc g_pfnBlitSprite",name:"g_pfnBlitSprite",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0069b3e8",comment:"Surface sprite/frame blit callback: destination GrafPort receiver, source context and rectangles, mode, palette, and nullable options."}}`. | `type` is the full parser-visible declaration and its declarator exactly matches `name`. Consume exactly the four listed heads and no neighboring head. Require every literal prestate, exact boundary, same 114 refs, and no byte change. Parser/type mismatch stops the complete transaction before deletion/materialization. | `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` on `0x0069b3e8` return one item `[0x69b3e8,0x69b3ec)`, size 4, code=false/data=true, exact name/type, regular absent, exact repeatable comment, bytes `00 00 00 00`, and the same 114 refs; former tail heads no longer exist independently. |
| I41S-06 | Exact `[0x69b420,0x69b424)` prestate: `0x69b420-0x69b421` is size-1 `unk_69B420`, untyped, code=false/data=false, blank comments, 132 refs; `0x69b421`, `0x69b422`, and `0x69b423` are separate size-1 unnamed/untyped code=false/data=false heads with blank comments and zero refs. Bytes are `00 00 00 00`. | After the exact parser/type matrix passes, `make_data {database:"<SESSION>",items:{addr:"0x0069b420",type:"bool g_imageLibraryLoadError",name:"g_imageLibraryLoadError",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0069b420",comment:"Shared ImageLib load-failure/render-suspend latch; trailing three bytes are alignment."}}`. | `type` is the full parser-visible one-byte declaration and its declarator exactly matches `name`. Materialize exactly one byte. Do not aggregate, rename, type, delete, or comment `0x69b421-0x69b423`. Preserve bytes and all 132/0/0/0 ref counts. Parser result other than size 1 stops before deletion/materialization. | `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return `0x69b420-0x69b421` as one size-1 data=true `bool g_imageLibraryLoadError` with regular absent, exact repeatable comment, byte `00`, and 132 refs; the three tail heads remain separate, unnamed, untyped, non-data, blank-comment, zero-ref bytes. |
| I41S-07 | Exact consumed `[0x61c288,0x61c29a)` prestate: `0x61c288-0x61c28c` size 4 `aIt_5`, type `const wchar_t[]`, data=true, blank comments, six refs; `0x61c28c-0x61c290`, `0x61c290-0x61c294` are unnamed/untyped size-4 data heads with blank comments/zero refs; `0x61c294-0x61c29a` is size 6 `aPf_3`, untyped data, blank comments/zero refs. All four heads are code=false/data=true. Exact bytes are `49 00 54 00 45 00 4d 00 2e 00 45 00 50 00 46 00 00 00`. | After the exact parser/type matrix passes, `make_data {database:"<SESSION>",items:{addr:"0x0061c288",type:"const wchar_t ItemEpfResourceLiteral[9]",name:"ItemEpfResourceLiteral",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0061c288",comment:"Compiler-pooled ITEM.EPF resource filename literal; use L\"ITEM.EPF\" in source."}}`. | `type` is the full parser-visible array declaration and its declarator exactly matches `name`. Consume only the four listed heads. Analysis name only, not a source global. Preserve exact bytes, six refs, following boundary/alignment, and neighboring `0x61c29a` head. Any size other than `0x12` stops before deletion/materialization. | `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return one item `[0x61c288,0x61c29a)`, size 18, code=false/data=true, exact name/type/text, regular absent, exact repeatable comment, same bytes and six refs; no former interior heads remain. |
| I41S-08 | Exact consumed `[0x61c29c,0x61c2ae)` prestate: `0x61c29c-0x61c2a0` size 4 `aIt_4`, type `const wchar_t[]`, data=true, blank comments, four refs; `0x61c2a0-0x61c2a4`, `0x61c2a4-0x61c2a8` are unnamed/untyped size-4 data heads with blank comments/zero refs; `0x61c2a8-0x61c2ae` is size 6 `aPd`, untyped data, blank comments/zero refs. All four heads are code=false/data=true. Exact bytes are `49 00 54 00 45 00 4d 00 2e 00 45 00 50 00 44 00 00 00`. | After the exact parser/type matrix passes, `make_data {database:"<SESSION>",items:{addr:"0x0061c29c",type:"const wchar_t ItemEpdResourceLiteral[9]",name:"ItemEpdResourceLiteral",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0061c29c",comment:"Compiler-pooled ITEM.EPD resource filename literal; use L\"ITEM.EPD\" in source."}}`. | `type` is the full parser-visible array declaration and its declarator exactly matches `name`. Consume only the four listed heads. Analysis name only. Preserve exact bytes, four refs, preceding gap/head, and neighboring `0x61c2ae` boundary. Any size other than `0x12` stops before deletion/materialization. | `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return one item `[0x61c29c,0x61c2ae)`, size 18, code=false/data=true, exact name/type/text, regular absent, exact repeatable comment, same bytes and four refs; no former interior heads remain. |
| I41S-09 | Exact six-head `[0x66dad4,0x66dae0)` model, all code=false/data=true and size 2: `word_66DAD4` `[d4,d6)` untyped/blank comments/value `0x0972`/3 refs; unnamed `[d6,d8)` untyped/blank/zero/0 refs; `word_66DAD8` `[d8,da)` untyped/blank/value `0x0a20`/3 refs; unnamed `[da,dc)` untyped/blank/zero/0 refs; `word_66DADC` `[dc,de)` untyped/blank/value `0x0a56`/3 refs; unnamed `[de,e0)` untyped/blank/zero/0 refs. | After the exact parser/type matrix passes, `make_data {database:"<SESSION>",items:{addr:"0x0066dad4",type:"short g_legacyItemImageMaxEntry",name:"g_legacyItemImageMaxEntry",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0066dad4",comment:"Legacy non-EPF maximum/fallback item image entry (0x0972)."}}`. | `type` is the full parser-visible signed two-byte declaration and its declarator exactly matches `name`. Change only `[d4,d6)`. Preserve all five other literal heads, all widths/values, gap names/types/comments, and ref distribution `3/0/3/0/3/0`. Any size other than 2 stops before deletion/materialization. | Relevant `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return `[d4,d6)` as exact two-byte signed short/name/value, code=false/data=true, regular absent, exact repeatable comment, and three refs; `[d6,e0)` remains exactly the five-head prestate model except later separately authorized I41S-10/I41S-11 actions. |
| I41S-10 | Exact six-head `[0x66dad4,0x66dae0)` action-entry model after I41S-09: named typed/commented `g_legacyItemImageMaxEntry` `[d4,d6)` with value `0x0972`/3 refs; unchanged unnamed `[d6,d8)` zero/0; `word_66DAD8` `[d8,da)` size-2 data=true/untyped/blank/value `0x0a20`/3; unnamed `[da,dc)` zero/0; `word_66DADC` `[dc,de)` value `0x0a56`/3; unnamed `[de,e0)` zero/0. Every head is code=false; unchanged rows retain blank comments and no type. | After exact I41S-09 readback, `make_data {database:"<SESSION>",items:{addr:"0x0066dad8",type:"short g_currentItemImageRemapFirstEntry",name:"g_currentItemImageRemapFirstEntry",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0066dad8",comment:"First current item-image entry remapped for legacy archives (0x0a20)."}}`. | `type` is the full parser-visible signed two-byte declaration and its declarator exactly matches `name`. Change only `[d8,da)`. Preserve the other five heads, exact values, gap state, and ref distribution. Any size other than 2 stops before deletion/materialization. | Relevant `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return `[d8,da)` as exact two-byte signed short/name/value, data=true, regular absent, exact repeatable comment, and three refs; prior I41S-09 result and `[da,e0)` remain exact until I41S-11. |
| I41S-11 | Exact six-head `[0x66dad4,0x66dae0)` action-entry model after I41S-09/10: exact typed/commented named heads at `[d4,d6)` and `[d8,da)`; unnamed/untyped/blank zero-ref gaps `[d6,d8)` and `[da,dc)`; `word_66DADC` `[dc,de)` size-2 data=true/untyped/blank/value `0x0a56`/3 refs; unnamed `[de,e0)` size-2 data=true/untyped/blank/zero/0 refs. Every head is code=false and values/ref distribution remain `0x0972/0/0x0a20/0/0x0a56/0` and `3/0/3/0/3/0`. | After exact I41S-09/I41S-10 readbacks, `make_data {database:"<SESSION>",items:{addr:"0x0066dadc",type:"short g_currentItemImageRemapLastEntry",name:"g_currentItemImageRemapLastEntry",delete_existing:true}}`; then `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0066dadc",comment:"Last current item-image entry remapped for legacy archives (0x0a56)."}}`. | `type` is the full parser-visible signed two-byte declaration and its declarator exactly matches `name`. Change only `[dc,de)`. Preserve the other five heads, especially all three independent gap items, values, bytes, and ref distribution. Any size other than 2 stops before deletion/materialization. | Relevant `inspect_items`, `get_comments`, `get_bytes`, and `xrefs_to` return the final six separate two-byte data heads: three named signed shorts with exact repeatable comments/values/three refs each, alternating with three unnamed/untyped/blank/zero-value/zero-ref gap heads. |
| I41S-12 | Function `0x00462f20`, name `sub_462F20`, size `0x332`, current raw type `int __thiscall(int *this, int *, _DWORD *, _DWORD *, int, unsigned __int8, int, unsigned __int8, int)`; address regular absent; address repeatable absent; function regular absent; function repeatable absent. | Dry-run `rename {database:"<SESSION>",batch:{func:{addr:"0x00462f20",name:"AlphaMaskSurface_DrawEncodedAlphaFrame"},pure:true,dry_run:true,stop_on_error:true,allow_overwrite:false}}`; require clean result, then apply `rename {database:"<SESSION>",batch:{func:{addr:"0x00462f20",name:"AlphaMaskSurface_DrawEncodedAlphaFrame"},pure:true,dry_run:false,stop_on_error:true,allow_overwrite:false}}`; then `set_function_comments {database:"<SESSION>",items:{addr:"0x00462f20",comment:"Blend an encoded EPF alpha-mask frame into this AlphaMaskSurface across a vertical alpha ramp."}}`. | Rename/comment only. Do not set a new prototype in this report because UID0000YO still owns its return-contract audit; preserve exact frame/range/xrefs and protected comment channels. | `lookup_funcs`, `get_comments`, `stack_frame`, `get_bytes`, and `xrefs_to` on `0x00462f20` return the exact new name and regular function comment with unchanged current prototype/frame/bytes/xrefs; address regular/repeatable and function repeatable remain absent. |

Target complete pre-type local protection set:

| Offset | Current local row | Required post-type state |
| --- | --- | --- |
| `+0x08` | `var_5C`, size 4, `char[4]` | unchanged |
| `+0x0c` | `var_58`, size 4, `_DWORD` | unchanged |
| `+0x10` | `var_54`, size 1, `_BYTE` | unchanged |
| `+0x14` | `paletteIndex`, size 0x10, `int[4]` | unchanged; do not source-name this aliased region |
| `+0x24` | `var_40`, size 1, `_BYTE` | unchanged |
| `+0x28` | `var_3C`, size 4, `_DWORD` | unchanged |
| `+0x2c` | `var_38`, size 4, `PaletteLib *` | unchanged |
| `+0x30` | `port`, size 4, `GrafPort *` | unchanged |
| `+0x34` | `var_30`, size 4, `_DWORD` | unchanged |
| `+0x38` | `outContext`, size 0x28, `EPFTileContext` | unchanged |
| `+0x60` | `var_4`, size 4, `_DWORD` | unchanged |
| `+0x64` | `__saved_registers`, size 4 | unchanged |
| `+0x68` | `__return_address`, size 4 | unchanged |

Expected argument readback after I41S-02/I41S-03:

| Offset | Expected name/type/width | Physical protection |
| --- | --- | --- |
| `+0x6c` | `drawPort`, `GrafPort *`, 4 | same stack slot |
| `+0x70` | `destinationBounds`, `RectBounds *`, 4 | unchanged |
| `+0x74` | `itemId`, `short`, logical 2 in a 4-byte argument slot | no following-slot shift |
| `+0x78` | `paletteFlags`, `unsigned char`, logical 1 in a 4-byte argument slot | no following-slot shift |
| `+0x7c` | `overlayMask`, `AlphaMaskSurface *`, 4 | same stack slot |
| `+0x80` | `overlayStrength`, `float`, 4 | same stack slot |

Already-correct dependencies requiring no IDA action from this report: `EPFTileContext_Initialize`, `ResourceLayoutTable_LookupLayoutEntry`, `PaletteLib__GetScreenPaletteMode`, `PaletteLib__GetSlotPalette`, `g_pEPFLib`, `g_pPaletteLib`, and `g_useEpfAssets`.

Historical `0425` externally verified supervisor Gate 2B closure disposition:

| ID | Final disposition | Exact supervisor verification |
| --- | --- | --- |
| I41S-01 | applied | Target is persisted as `ItemObjImageLib_DrawItemImageIn43x43Slot` at unchanged `[0x004defc0,0x004df1d4)` / size `0x214`; seven incoming xrefs and bytes remained exact. |
| I41S-02 | applied | Physical argument names `drawPort`, `itemId`, `paletteFlags`, `overlayMask`, and `overlayStrength` persisted at their exact existing slots; `destinationBounds` and every protected local/saved/return row remained unchanged. |
| I41S-03 | applied | Persisted normalized prototype is `void __thiscall(ItemObjImageLib *this, GrafPort *drawPort, RectBounds *destinationBounds, __int16 itemId, unsigned __int8 paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)`; required named types/signature tokens passed and protected frame rows remained byte-for-byte unchanged. |
| I41S-04 | applied | Exact accepted regular function behavior comment persisted; address regular/repeatable and function-repeatable protections remained unchanged. |
| I41S-05 | applied historically; current drift supersedes this state | The `0425` readback recorded `0x0069b3e8` as one four-byte `SurfaceSpriteBlitProc` data item over `[0x69b3e8,0x69b3ec)` with four zero bytes, 114 head refs, 0/0/0 tail refs, and the exact repeatable callback comment. The accepted logical name was available through existing alias/index semantics although `inspect_items.name` was blank. |
| I41S-06 | applied historically; current drift supersedes this state | The `0425` readback recorded `0x0069b420` as one one-byte `bool g_imageLibraryLoadError` data item with 132 refs and the exact repeatable latch/alignment comment; `0x0069b421-0x0069b423` remained separate undefined zero-ref alignment bytes. |
| I41S-07 | applied | `ItemEpfResourceLiteral` is one 18-byte `const wchar_t[9]` item at `[0x0061c288,0x0061c29a)` with exact UTF-16 bytes, six refs, and repeatable literal-use comment. |
| I41S-08 | applied | `ItemEpdResourceLiteral` is one 18-byte `const wchar_t[9]` item at `[0x0061c29c,0x0061c2ae)` with exact UTF-16 bytes, four refs, and repeatable literal-use comment. |
| I41S-09 | applied | `g_legacyItemImageMaxEntry` is a separate two-byte signed `__int16` item at `0x0066dad4`, value `0x0972`, three refs, with exact repeatable comment and neighboring gap preserved. |
| I41S-10 | applied | `g_currentItemImageRemapFirstEntry` is a separate two-byte signed `__int16` item at `0x0066dad8`, value `0x0a20`, three refs, with exact repeatable comment and neighboring gaps preserved. |
| I41S-11 | applied | `g_currentItemImageRemapLastEntry` is a separate two-byte signed `__int16` item at `0x0066dadc`, value `0x0a56`, three refs, with exact repeatable comment and neighboring gap preserved. |
| I41S-12 | applied | Helper persisted as `AlphaMaskSurface_DrawEncodedAlphaFrame` over unchanged `[0x00462f20,0x00463252)` / size `0x332`, with accepted regular comment and unchanged existing prototype, frame, bytes, and nine incoming xrefs. |
| Already-correct dependencies | already-present | All listed dependency names remained unchanged and no unlisted function rename/type/comment action occurred. |

Historical supervisor closure used a collision-safe prechange backup, public runtime authority, one canonical save, and immediate persisted readback. Its saved canonical IDB identity was SHA256 `6D17FC31F5F1D28576F12B1EC45ED684125AA25A421273DF238313BA4339911E`. B003 did not perform or receive credit for those IDA actions.

Fresh current-IDB drift revalidation and failed unsaved repair evidence:

- Dated initial authority snapshot: session `supervisor-uid0000LY-postrollback-20260812`, canonical path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, public runtime attestation `ok:true`, health `status:ok`, and disk SHA256 `AFC9BE43F0C41CC1FA26F6EDD1AB4DD041A6223F3DF7A08645D7D312B5BEFCD2` / size `143205116`. The supervisor must rediscover and attest authority again at action time rather than treating this ephemeral session ID as permanent.
- Exact collision/name preflight: `entity_query` over the slot family returns `g_pfnDrawLineDelta@0x69b3e4`, `g_pfnBlitSprite@0x69b3e8`, `g_pfnFillRle@0x69b3ec`, and `g_pfnBlitScaledRleTint@0x69b3f0`; exact-name query returns one `g_pfnBlitSprite` entity/global only at `0x69b3e8`, current global size 1. Exact-name query returns one `g_imageLibraryLoadError` entity/global only at `0x69b420`, current global size 1. A missing result, duplicate address, or collision outside the exact destination stops both repairs before `make_data`.
- Exact parser preflight: `SurfaceSpriteBlitProc` exists as pointer typedef ordinal `906`, size 4. `bool` is built-in parser grammar: exact `type_inspect` reports no named local type, while the current head already carries parsed type `bool`, size 1. Do not substitute `BOOL`, `__crt_bool`, `__vcrt_bool`, or another project typedef. A changed pointer typedef or inability to parse the full declarations below stops before materialization.
- Dated failed I41S-05R attempt: every stated item/comment/byte/xref/neighbor/type preflight matched. `make_data` produced exact four-byte `SurfaceSpriteBlitProc` storage at `0x0069b3e8`, entity size 4, with bytes/xrefs/neighbors unchanged. Both address channels were blank immediately before comment mutation. The exact `set_address_repeatable_comments` request succeeded and installed the requested repeatable text, but immediate `get_comments` also exposed regular address text `Surface slot 2 sprite/context blit callback; destination GrafPort receiver in ECX; nullable palette/options.` Because regular-blank protection failed, the supervisor stopped before any save, discarded the worker/session, and reopened the prior canonical IDB. No result from that unsaved worker receives Gate 2B credit.
- Dated rollback proof: fresh session `supervisor-uid00041S-rollback-20260812` reconfirmed both I41S-05R and I41S-06R fragmented item prestates and all target address-comment channels blank. Report-specific byte-identical backup `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\ida-backups\NexusTK.exe.uid00041S-repair-preflight-20260812-064558.i64` is SHA256 `8340231102DC16741A39AE789A2436B75A272B487BF51A214F6399391D299C51`, size `143205069`. The backup is supervisor-owned recovery evidence; B003 did not create, alter, restore, or validate it.
- Cause and safe disposition: materializing the callback item made a latent regular-address comment visible when the repeatable channel was written. `set_address_repeatable_comments` remains the correct repeatable-channel endpoint; public `set_address_comments` with the same address and `comment:""` is the supported regular-address clear. Both repair rows therefore require ordered repeatable set, immediate two-channel observation, explicit regular clear, and immediate final two-channel readback. The same clear is required prospectively for I41S-06R because its materialization may expose latent regular text even though its fragmented prestate is blank.

| Current repair ID | Exact current prestate | Exact supervisor-owned repair action | Complete protection / stop conditions | Required post-action readback |
| --- | --- | --- | --- | --- |
| I41S-05R / C41S-025 | Four separate size-1 code=false/data=false heads at `0x69b3e8`, `0x69b3e9`, `0x69b3ea`, `0x69b3eb`; first head `inspect_items.name:""`, type `SurfaceSpriteBlitProc`, exact entity/global name `g_pfnBlitSprite`; three tails unnamed/untyped. Both address-comment channels blank on all four; function channels N/A. Bytes `00 00 00 00`; inbound refs `114/0/0/0`. Protected neighbors are fragmented slot `[0x69b3e4,0x69b3e8)` (`SurfaceDrawLineDeltaProc`/`g_pfnDrawLineDelta`) and fragmented slot `[0x69b3ec,0x69b3f0)` (`SurfaceFillRleProc`/`g_pfnFillRle`), with all inspected comments blank. | Ordered public actions after every fresh authority/collision/item/comment/byte/xref/neighbor/type prestate matches: (1) `make_data {database:"<SESSION>",items:{addr:"0x0069b3e8",type:"SurfaceSpriteBlitProc g_pfnBlitSprite",name:"g_pfnBlitSprite",delete_existing:true}}`; require `ok:true`, size 4, exact type/entity, and immediate structural/protection readback. (2) Require regular and repeatable blank immediately before comments. (3) `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0069b3e8",comment:"Surface sprite/frame blit callback: destination GrafPort receiver, source context and rectangles, mode, palette, and nullable options."}}`. (4) Immediately read both address channels; repeatable must be exact and regular may be only blank or the dated observed latent text `Surface slot 2 sprite/context blit callback; destination GrafPort receiver in ECX; nullable palette/options.`. (5) Regardless of blank/known-latent intermediate regular state, call `set_address_comments {database:"<SESSION>",items:{addr:"0x0069b3e8",comment:""}}`. (6) Immediately run the complete final readback. | Consume exactly `[0x69b3e8,0x69b3ec)` and no neighbor. Preserve all 16 zero bytes across `0x69b3e4-0x69b3f3`, entity names, `114/0/0/0` target refs, neighbor item models/comments/refs, and final regular-comment absence. Any prestate mismatch, parser result other than 4, make_data/comment error, unexpected intermediate regular text, wrong repeatable text, entity movement/duplication, byte/xref delta, neighbor delta, clear that changes/removes repeatable, or final nonblank regular stops before save and requires rollback under supervisor authority. No later action may begin before each immediate readback passes. | Final `inspect_items` returns one `[0x69b3e8,0x69b3ec)` size-4 code=false/data=true item of type `SurfaceSpriteBlitProc`; `entity_query` returns exactly `g_pfnBlitSprite@0x69b3e8` with global size 4 even if `inspect_items.name` remains blank under alias/index projection. Tail heads no longer exist independently. Final `get_comments` returns regular absent and exact repeatable text; bytes/ref distribution and both protected neighboring four-head slots remain exact. The intermediate two-channel result and explicit clear result must both be recorded by the supervisor. |
| I41S-06R / C41S-026 | Four separate size-1 code=false/data=false heads at `0x69b420`, `0x69b421`, `0x69b422`, `0x69b423`; first head `inspect_items.name:""`, type `bool`, exact entity/global name `g_imageLibraryLoadError`; three tails unnamed/untyped. Both address-comment channels blank on all four; function channels N/A. Bytes `00 00 00 00`; inbound refs `132/0/0/0`. Protected neighbors are fragmented `[0x69b41c,0x69b420)` with `Pane *` on its first head and fragmented `[0x69b424,0x69b428)` with `ImageAnimationTable` on its first head; all inspected neighbor comments are blank. | Ordered public actions after every fresh authority/collision/item/comment/byte/xref/neighbor/parser prestate matches: (1) `make_data {database:"<SESSION>",items:{addr:"0x0069b420",type:"bool g_imageLibraryLoadError",name:"g_imageLibraryLoadError",delete_existing:true}}`; require `ok:true`, size 1, exact type/entity, and immediate structural/protection readback. (2) Require regular and repeatable blank immediately before comments. (3) `set_address_repeatable_comments {database:"<SESSION>",items:{addr:"0x0069b420",comment:"Shared ImageLib load-failure/render-suspend latch; trailing three bytes are alignment."}}`. (4) Immediately read and record both address channels; repeatable must be exact. (5) Prospectively call `set_address_comments {database:"<SESSION>",items:{addr:"0x0069b420",comment:""}}` whether the intermediate regular channel is blank or exposes latent text. (6) Immediately run the complete final readback. | Consume/recreate only `[0x69b420,0x69b421)`. Do not delete, aggregate, name, type, comment, or move `0x69b421-0x69b423`. Preserve all 12 zero bytes across `0x69b41c-0x69b427`, entity names, `132/0/0/0` target refs, both neighboring four-head models/comments/refs, and final regular-comment absence. Any prestate mismatch, parser result other than 1, make_data/comment error, wrong repeatable text, entity movement/duplication, tail/neighbor delta, byte/xref delta, clear that changes/removes repeatable, or final nonblank regular stops before save and requires rollback. The intermediate regular value must be recorded exactly; no later action may begin before each immediate readback passes. | Final `inspect_items` returns `[0x69b420,0x69b421)` as one size-1 code=false/data=true `bool` item; `entity_query` returns exactly `g_imageLibraryLoadError@0x69b420`, global size 1, even if `inspect_items.name` remains blank under alias/index projection. The three tail heads remain separate unnamed/untyped code=false/data=false bytes. Final `get_comments` returns regular absent and exact repeatable text; bytes/ref distribution and both protected neighboring four-head spans remain exact. The intermediate two-channel result and explicit clear result must both be recorded by the supervisor. |

Persisted 0719 supervisor Gate 2B closure disposition:

| Claim(s) | Exact external supervisor disposition |
| --- | --- |
| C41S-024 / C41S-043 | Applied/verified: exact target name, range, normalized prototype, argument/frame rows, four comment channels, bytes, seven callers, and protected locals/saved/return rows reproduced before save and after fresh persisted reopen. |
| C41S-025 / I41S-05R | Applied: one four-byte `SurfaceSpriteBlitProc` data item `[0x69b3e8,0x69b3ec)`, entity `g_pfnBlitSprite` size 4; known latent regular text observed after repeatable set and explicitly cleared; final regular absent/repeatable exact; all 16 zero bytes, `114/0/0/0` refs, tail resolution, and neighbors exact. |
| C41S-026 / I41S-06R | Applied: one one-byte `bool` data item at `0x69b420`, entity `g_imageLibraryLoadError` size 1; regular explicitly cleared; final regular absent/repeatable exact; three alignment heads retained; all 12 zero bytes, `132/0/0/0` refs, and neighbors exact. |
| Claim C41S-027 | Applied/verified no-change: both 18-byte resource literals retain exact names/types/UTF-16 bytes/comments/boundaries and `6/4` refs. |
| Claim C41S-028 | Applied/verified no-change: three signed remap constants and alternating gaps retain exact names/types/values/comments and `3/0/3/0/3/0` refs. |
| Claim C41S-029 | Applied/verified no-change: helper name/range/prototype/frame/comments/bytes and nine callers remain exact. |
| Claim C41S-030 | Applied/verified no-change: dependencies and every prohibited unlisted name/type/comment/item/byte/xref state remain unchanged. |
| Claim C41S-042 | Applied/verified: every I41S-05..11 head/span/type/entity/comment/value/xref/neighbor protection passed, including intermediate and final I41S-05R/I41S-06R comment-channel reads. |

The prechange backup `tools/leaser/Agents/ida-backups/NexusTK.exe.uid00041S-prechange-20260812-0719.i64` is `143205116` bytes, SHA256 `A51206AD896FAA5E3F4F798CD8AA1E40F8ECD794E8F376C80247BA78E28FE4C7E`. One post-mutation save succeeded. Canonical IDB is `143205131` bytes, SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`, last-write UTC `2026-08-12T11:22:07.8699333Z`. Dated fresh persisted reopen `supervisor-uid00041S-persisted-20260812` reproduced every accepted/protected result and unchanged disk hash/size. Supervisor audit timestamp `2026-08-12T07:26:00-04:00` and IDA catalog entry `0436` are the external authority. B003 receives no supervisor-action credit. Fresh exact-artifact Gate 1, final combined Gate 2, and report lifecycle state remain externally determined.

Current disposition: all nine supervisor rows C41S-024..030/C41S-042..043 are externally applied and persisted under the receipt above. No additional IDA action is requested by this report.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes.
- Target CPP disposition: retain the current applied formal CPP method below exactly; it replaced the former blank CPP block during the completed ordinary callback.
- Target H disposition: remain blank. The declaration is class-owned by UID0001UU; duplicating it on the method page would violate one-definition/header ownership.
- Behavioral note: `SurfaceSpriteBlitOptions options;` is intentionally not value-initialized. Do not change it to `options = {}`, `{}`, `memset`, or a constructor that initializes all bytes.

### UID00041S Exact Formal CPP

```cpp
void ItemObjImageLib::DrawItemImageIn43x43Slot(
    GrafPort *drawPort,
    RectBounds *destinationBounds,
    short itemId,
    unsigned char paletteFlags,
    AlphaMaskSurface *overlayMask,
    float overlayStrength)
{
    if (g_imageLibraryLoadError)
        return;

    int itemImageEntry = static_cast<unsigned short>(itemId + 0x4000);

    if (!g_useEpfAssets) {
        if (itemImageEntry >= g_currentItemImageRemapFirstEntry &&
            itemImageEntry <= g_currentItemImageRemapLastEntry) {
            itemImageEntry =
                static_cast<unsigned short>(itemImageEntry + 0xff53);
        } else if (itemImageEntry > g_legacyItemImageMaxEntry) {
            itemImageEntry = g_legacyItemImageMaxEntry;
        }
    }

    EPFTileContext tileContext;
    tileContext.Initialize();

    ItemInfo itemInfo = *m_itemInfos.GetAtOrDefault(itemImageEntry);

    if (g_pPaletteLib->GetScreenPaletteMode() == 0 ||
        itemInfo.allowPaletteFilter) {
        g_pEPFLib->LookupLayoutEntry(
            g_useEpfAssets ? L"ITEM.EPF" : L"ITEM.EPD",
            itemImageEntry,
            &tileContext);

        DLPalette *palette = g_pPaletteLib->GetSlotPalette(
            10,
            itemInfo.paletteSlot,
            paletteFlags >> 5);

        if (tileContext.bounds.right - tileContext.bounds.left >
            destinationBounds->right - destinationBounds->left) {
            tileContext.bounds.right = tileContext.bounds.left + 43;
        }

        if (tileContext.bounds.bottom - tileContext.bounds.top >
            destinationBounds->bottom - destinationBounds->top) {
            tileContext.bounds.bottom = tileContext.bounds.top + 43;
        }

        int centeredLeft =
            (destinationBounds->left + destinationBounds->right -
             tileContext.bounds.right + tileContext.bounds.left) / 2;
        int centeredTop =
            (destinationBounds->top + destinationBounds->bottom -
             tileContext.bounds.bottom + tileContext.bounds.top) / 2;

        destinationBounds->left = centeredLeft;
        destinationBounds->top = centeredTop;
        destinationBounds->right = centeredLeft + 43;
        destinationBounds->bottom = centeredTop + 43;

        SurfaceSpriteBlitOptions options;
        const SurfaceSpriteBlitOptions *blitOptions = 0;

        if (itemInfo.alpha > 0.0f) {
            options.alpha = itemInfo.alpha;

            if (paletteFlags != 0) {
                options.kind = 3;
                options.paletteShift = paletteFlags;
            } else {
                options.kind = 1;
            }

            blitOptions = &options;
        } else if (paletteFlags != 0) {
            options.kind = 2;
            options.paletteShift = paletteFlags;
            options.secondaryPaletteShift = 0;
            blitOptions = &options;
        }

        g_pfnBlitSprite(
            drawPort,
            &tileContext,
            &tileContext.bounds,
            destinationBounds,
            1,
            palette,
            blitOptions);
    }

    if (overlayMask != 0) {
        overlayMask->DrawEncodedAlphaFrame(
            destinationBounds,
            &tileContext,
            destinationBounds,
            destinationBounds->bottom,
            static_cast<unsigned char>(overlayStrength * 32.0f + 0.5f),
            destinationBounds->top,
            0,
            1);
    }
}
```

### UID0001UU Exact Declaration Replacement

Replace only the stale fixed-slot declaration with:

```cpp
void DrawItemImageIn43x43Slot(
    GrafPort *drawPort,
    RectBounds *destinationBounds,
    short itemId,
    unsigned char paletteFlags,
    AlphaMaskSurface *overlayMask,
    float overlayStrength);
```

### UID0000RA Exact ItemObjImageLib CPP Preamble Replacement

UID0000RA is position `0`, so replace its complete formal CPP with this module preamble plus the existing sole singleton definition:

```cpp
#include "EPFTileContext.h"
#include "ImageLib.h"
#include "Surface.h"

class ItemObjImageLib;

ItemObjImageLib *g_pItemObjImageLib = 0;
```

`EPFTileContext.h` supplies the complete by-value context and includes complete `RectBounds`; `ImageLib.h` supplies `g_imageLibraryLoadError`; `Surface.h` supplies the complete options aggregate, callback typedef/global, and pointer-only declarations for `GrafPort` and `DLPalette`. This position-0 route makes every required declaration visible before UID0001UU and UID00041S without adding a target-local ABI copy.

### UID00005V Exact GrafPort CPP Preamble Replacement

Replace the complete UID00005V formal CPP with:

```cpp
#include "GrafPort.h"
#include "Surface.h"

[[CHILDREN]]
```

Keep GrafPort.h's existing `struct SurfaceSpriteBlitOptions;` because its declarations use only pointers. `GrafPort.h` already includes complete `EPFTileContext.h` and `RectBounds.h` and forward-declares `DLPalette`; the following `Surface.h` include supplies the complete options type, slot-2 typedef, and extern before UID000167's body.

### UID0000TN Exact Surface CPP Addition

Insert the definition between the existing physical slot-1 and slot-3 definitions:

```cpp
SurfaceSpriteBlitProc g_pfnBlitSprite;
```

### UID0000TN Exact Surface H Additions

```cpp
struct SurfaceSpriteBlitOptions
{
    unsigned char kind;
    float alpha;
    signed char paletteShift;
    unsigned char *maskPixels;
    int maskWidth;
    int maskHeight;
    int maskStride;
    signed char secondaryPaletteShift;
};

typedef int (__thiscall *SurfaceSpriteBlitProc)(
    GrafPort *destination,
    EPFTileContext *sourceContext,
    const RectBounds *sourceBounds,
    const RectBounds *destinationBounds,
    int mode,
    DLPalette *palette,
    const SurfaceSpriteBlitOptions *options);

extern SurfaceSpriteBlitProc g_pfnBlitSprite;
```

Place the struct/typedef immediately after UID0000TN's already-present `DLPalette`, `GrafPort`, `AlphaMaskSurface`, `EPFTileContext`, and `RectBounds` forward declarations and before `SurfaceDrawPixelProc`; place the extern between existing slot-1 `g_pfnDrawLineDelta` and slot-3 `g_pfnFillRle` externs. On the target 32-bit MSVC layout, ordinary four-byte alignment inserts the only padding at `+1..+3`, `+9..+0xb`, and `+0x1d..+0x1f`, giving exact size `0x20` without source-written padding arrays or packing directives. The declaration is intentionally a trivial aggregate with no constructor/default member initializers. Provider signed-byte reads prove both shift fields; StaticObj's kind-8 producer and RGB565 consumer prove the pointer/width/height/stride quartet.

### UID000167 Exact Shared-Declaration Deduplication

Remove only UID000167's file-local `SurfaceSpriteBlitOptions` forward declaration, `SurfaceSpriteBlitProc` typedef, and `extern g_pfnBlitSprite` declaration from its formal CPP preamble after UID0000TN emits the shared declaration through `Surface.h`. Preserve UID000167's enum and complete `GrafPort::BlitSurface` body. UID00005V's exact preamble above makes the complete declaration visible before this child; do not retain a second local typedef as a generated-source workaround.

### UID00017J Exact Shared-ABI Support Insertion

Add this source-quality evidence without changing UID00017J ownership, score, formal body, or return-contract status:

> The kind-8 branch calls `BuildClippedAlphaSurfaceView` at `0x004dd66a`, copies `AlphaSurfaceBufferView::pixels`, `width`, `height`, and `stride` into `SurfaceSpriteBlitOptions` offsets `+0x0c`, `+0x10`, `+0x14`, and `+0x18`, sets `kind` to 8, and dispatches `g_pfnBlitSprite` at `0x004dd70c`. RGB565 provider `0x004c0f80` consumes those exact fields as mask base, positive bounds, and row stride. This closes the shared kind-8 ABI while Surface.h remains the sole declaration owner.

### UID00029J Exact ImageLib CPP/H Additions

CPP definition:

```cpp
bool g_imageLibraryLoadError = false;
```

H declaration:

```cpp
extern bool g_imageLibraryLoadError;
```

- Why behavior matches: every branch, integer truncation, remap, row fallback, rectangle write, callback mode, partial options write, overlay alpha conversion, and mutable destination side effect mirrors the current body.
- Why source shape is plausible: it uses normal member calls, local structs, direct arithmetic, pooled literals, ordinary globals, and simple pre-standard/early-standard C++ patterns rather than decompiler temporaries.
- Naming style: established project `g_` globals, `m_` members, PascalCase types/methods, and descriptive local camelCase.
- The target remains first-draft rather than final because exact original lexical spellings are inferred. C41S-033 is externally supervisor-verified by commands `000000022519`/`000000022520` and physical generated readback; no options byte, generated-body route, empty-marker state, or header disposition remains unresolved.

## Final Recommendation

- Ordinary implementation and independent supervisor Gate 2A verification are complete. UID00041S is `92/94`, retains owner/emitter UID00006W, and has the accepted formal CPP body.
- UID0001UU, UID00006W, UID0000KH, UID0000RA, UID0000TN, UID0001PI, UID00005V, UID000167, UID00017J, UID00029J, UID0003ZN, UID0000YO, and UID00017N now contain the accepted target/support details. UID0000TN is the sole shared slot-2 declaration/definition owner, and UID000167's obsolete local declaration preamble is absent.
- Keep UID00017N non-emitting and UID0000VN padding unchanged.
- Independent supervisor verification closed Gate 2A `32/32`, manual coverage C41S-032, generated/readback C41S-033, and all nine Gate 2B claims. The persisted 0719 Gate 2B receipt proves exact I41S-05R/I41S-06R materialization/comment clears, seven no-change protections, one save, and fresh persisted reopen on canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`. Those supervisor actions are not performed by or credited to B003. This report does not claim final combined Gate 2; fresh exact-artifact Gate 1/final Gate 2 and current execution/archive truth remain externally supervisor-owned and authoritative only from current audits, the report's physical path, and validator-owned status/history metadata.
- No target-local or shared-ABI research item remains. Independent full callback-provider body reconstruction remains provider-owned work, but the complete options layout, shared declaration owner, and affected include routes are closed here rather than deferred.

## Recommended Target Doc Changes

Target: `by-memory/0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md`.

- Header metadata: `COMPLETION:92`, `CONFIDENCE:94`; owner/reconstructable/emitter unchanged.
- Formal CPP: exact UID00041S body from Section 22.
- Formal H: remain blank with explicit declaration-owner explanation.
- Replace Source-Facing Signature with the exact void/GrafPort declaration.
- Expand Behavior with the full guard/remap/row/palette/resource/crop/recenter/options/callback/overlay sequence.
- Add complete seven-caller ignored-result inventory.
- Add source-quality sections for callback ABI, options partial initialization, pooled resource literals, ImageLib error global, rectangle algebra, and current IDA prestate.
- Historicalize the former no-code proof: the old blocker was real evidence of an uninitialized-field dependency, but the conclusion that source could not be written was too conservative. Preserve why zero-initialization is wrong.
- Replace the score rationale with the `92/94` rationale in Section 26.

## Recommended Support Doc Changes

| Support path | Exact change |
| --- | --- |
| `by-type/by-struct/ItemObjImageLibLayout.md` UID0001UU | Replace only the fixed-slot declaration as in Section 22; add current void-result/GrafPort/partial-options evidence; keep `92/94`, owner/emitter UID00006W, and all other declarations/layout/history. |
| `by-class/ItemObjImageLib.md` UID00006W | Replace the fixed-slot method row with `void ... (GrafPort *drawPort, ...)`, mark UID00041S source-ready with exact behavior, and supersede only the target-specific no-code/draw-owner wording; keep `92/94` and routing. |
| `by-file/ItemObjImageLib.md` UID0000KH | Record UID00041S as source-ready, exact callback/options/flag/resource behavior, the Surface declaration/include visibility required before the method body, and the generated empty-marker repair expectation; keep `92/94` and path. |
| `by-global/g_pItemObjImageLib.md` UID0000RA | Replace its position-0 formal CPP with the exact `EPFTileContext.h`, `ImageLib.h`, and `Surface.h` preamble plus unchanged singleton definition from Section 22; keep `92/94`, owner/emitter, position, and all singleton evidence. |
| `by-global/SurfaceRenderCallbackTable.md` UID0000TN | Become the sole complete slot-2 declaration/definition owner; insert the exact eight-member 32-byte options struct and callback typedef after its existing four required pointer forwards, add the extern/definition in physical slot order, document all current consumer families and kind-8 closure, and preserve non-value-initialized source behavior; keep `94/95` because other slots still own broader final-audit caps. |
| `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md` UID0001PI | Record one physical slot-2 `SurfaceSpriteBlitProc g_pfnBlitSprite`, UID00041S consumer, and no-duplicate covered-by disposition; keep `92/94`. |
| `by-class/GrafPort.md` UID00005V | Replace formal CPP with exact `#include "GrafPort.h"`, `#include "Surface.h"`, blank line, and `[[CHILDREN]]`; retain the pointer-only options forward in formal H and every class/member/body fact. |
| `by-memory/0x004ba250-0x004ba444.SurfaceSpriteBlitHelper.md` UID000167 | Remove only the duplicate local forward/typedef/extern preamble identified in Section 22, rely on UID00005V's `Surface.h` include and the UID0000TN shared declaration, add UID00041S as a direct slot-2 consumer, and preserve the enum, complete GrafPort body, owner/emitter, and `87/90` score. |
| `by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md` UID00017J | Add the exact kind-8 `AlphaSurfaceBufferView::{pixels,width,height,stride}` to options `+0x0c..+0x18` copy and RGB565 provider-consumption evidence from Section 22; preserve `88/90`, class/file route, blank formal body, and independent return-contract blocker. |
| `by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md` UID00029J | Replace the no-definition marker with the exact ImageLib CPP/H declaration/definition, adopt `g_imageLibraryLoadError`, preserve one-byte semantic storage/trailing alignment and all prior fanout evidence; recommend `91/94`, owner/emitter UID0000K2 unchanged. |
| `by-memory/0x0066dad4-0x0066dae0.ItemObjImageLibLegacyItemRemapConstants.md` UID0003ZN | Preserve exact existing formal C++; add current target bytes/xrefs/source use and raise `89/91 -> 92/94`; owner/emitter UID0000KH unchanged. |
| `by-memory/0x00462f20-0x00463252.DrawEncodedAlphaFrame.md` UID0000YO | Add current target as an exact caller with the full argument mapping; retain existing `88/91`, owner/emitter, and helper-body no-code disposition. |
| `by-memory/0x004dec30-0x004df500.ItemObjImageLibLocalMethodCluster.md` UID00017N | Replace only UID00041S's old no-code summary with source-ready/partial-init-bug evidence; aggregate remains non-emitting and `RECONSTRUCTABLE:FALSE`. |

No generated file and no manual coverage file is an agent edit destination.

## Score And Metadata Recommendation

| Item | Dated accepted pre-callback state | Current physical post-callback state | Rationale |
| --- | --- | --- | --- |
| UID00041S | 84/88 | 92/94 | All named target and shared-ABI blockers were re-investigated and resolved into exact source/IDA/doc actions; formal CPP and declaration visibility are ready. |
| UID0001UU | 92/94 | 92/94 | One declaration correction; broader exact historical header spelling still caps final audit. |
| UID00006W | 92/94 | 92/94 | Target-specific method improves, but sibling draw methods retain separate caps. |
| UID0000KH | 92/94 | 92/94 | One empty emitter is repaired; file-wide sibling emitters remain outside this report. |
| UID0000RA | 92/94 | 92/94 | Position-0 module preamble gains exact required includes while the sole singleton definition and ownership remain unchanged. |
| UID0000TN | 94/95 | 94/95 | Slot 2 formal is improved; broader callback table already at cap and other slot lexical details remain. |
| UID0001PI | 92/94 | 92/94 | Physical slot-2 shape becomes exact; page remains covered-by/non-body storage. |
| UID00005V | 88/90 | 88/90 | The class source preamble gains Surface.h visibility; independent class-layout/body caps remain. |
| UID000167 | 87/90 | 87/90 | Duplicate declaration is removed without changing the exact GrafPort body or its independent score caps. |
| UID00017J | 88/90 | 88/90 | Kind-8 evidence closes the shared options ABI, but the renderer's independent return/body blockers remain unchanged. |
| UID00029J | 88/93 | 91/94 | Best source name/type/linkage and exact one-byte/trailing-alignment source model are resolved. |
| UID0003ZN | 89/91 | 92/94 | Existing source names/values are reconfirmed by exact current bytes and three-consumer xrefs. |
| UID0000YO | 88/91 | 88/91 | Target caller mapping improves support only; helper's independent body/return audit remains outside target scope. |

Reason UID00041S is not higher:

- The exact original spelling of `g_imageLibraryLoadError`, callback typedef/global, and options members is inferred rather than symbol-proven.
- The method body remains a first draft after completed ordinary implementation, Gate 2A, manual coverage, generated closure, and externally persisted Gate 2B verification of all nine supervisor claims. Inferred original lexical spellings independently keep the target below the `95/95` barrier.

Reason UID00041S is not lower:

- Exact current MCP proves complete behavior, range, padding, seven callers, result use, callback order, resource/remap values, overlay call, one persisted four-byte callback item, one persisted one-byte latch item with separate alignment tails, exact entity/type/comment channels, and every byte/xref/neighbor protection.
- Current producer/provider evidence proves the complete 32-byte shared options ABI, and exact formal include routes make it visible from both affected generated translation units.
- Every old blocker has a source-ready resolution rather than a deferred question.

Metadata fields unchanged: `CANONICAL_OWNER:00006W`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00006W`, blank position, target H blank.

## Open Questions With Attempted Resolution

| Former question | Investigation | Resolution / score effect |
| --- | --- | --- |
| Is the result really `int`? | Checked all seven post-call paths and target body. | No; use `void`. Closed. |
| Is `drawOwner` a Pane, void pointer, or unused? | Checked current tinfo, saved local, ECX restoration, callback ABI, and callers. | `GrafPort *drawPort`. Closed. |
| Can partial options initialization be represented faithfully? | Mapped target writes and installed callback reads, including missing kind-3 +0x1c write. | Yes: non-value-initialized POD local with explicit writes. Closed; preserve bug. |
| Are options `+0x0c..+0x18` unknown/reserved? | Rechecked RGB565 kind-8 reads and StaticObj's `BuildClippedAlphaSurfaceView` result copy. | No: `maskPixels`, `maskWidth`, `maskHeight`, and `maskStride`. Closed with exact offsets. |
| Who owns the complete options/slot-2 declaration? | Compared UID0000TN's existing callback-family H/CPP, two provider installation routes, 114 slot refs, generated consumers, and UID000167's duplicate local preamble. | Surface.h/Surface.cpp via UID0000TN is sole owner. Closed; consumer-local typedefs rejected. |
| How do ItemObjImageLib.cpp and GrafPort.cpp see the shared ABI? | Read exact emitter order and current formal/generated preambles. | UID0000RA adds EPFTileContext.h/ImageLib.h/Surface.h at position 0; UID00005V adds Surface.h after GrafPort.h; GrafPort.h keeps only its pointer forward. Closed. |
| Are resource operands globals? | Read exact UTF-16 bytes and xref fanout. | Pooled literals; use literal source. Closed. |
| What is `unk_69B420`? | Rechecked guard, ImageLib support, 132-ref history, byte/tail shape. | Inferred `bool g_imageLibraryLoadError`. Closed at high confidence. |
| Are rectangle locals separate structs? | Reduced exact assembly arithmetic to source/destination RectBounds operations. | No extra type required. Closed. |
| Does overlay ownership move here? | Checked helper receiver/ABI and shared caller set. | No; call AlphaMaskSurface member. Closed. |
| Does exact original naming remain unknown? | Searched current IDA names, docs, reports, and subsystem conventions. | Best descriptive source names selected. Lexical uncertainty caps score but is not a blocker. |

There are no unanswered target-local or shared-declaration questions. Independent callback-provider bodies remain owned by their provider pages, but their already-proven reads were fully used here to close the shared ABI; that work is not being deferred as a target blocker.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

Historical accepted manual text: the report requested the exact row below immediately after UID0000VN and before UID0000UQ. Supervisor closure found that row physically present exactly once in that position, so no further manual edit is requested. Command `000000022520` recorded coverage receipt SHA256 `8D080707A33B24C6A79CC91FE0CA1DFA339F5174F5B79FF94D01F59C35AF425B` with exit `0` and `ok: 1`. The current aggregate SHA256 is `2ACD0108D76BE00F5778E148C9C306A740EF71B13B139F5E1C5158A6687B86CF` after unrelated additive coverage work; the exact UID00041S row remains unchanged once at line 1902.

`        - [UID:00041S][0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot](by-memory/0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md) 0x004defc0-0x004df1d4 | ItemObjImageLib member method | ItemObjImageLib::DrawItemImageIn43x43Slot : reconstructable : 92% : very-strong : Source-ready void fixed-slot draw method with exact seven-caller ignored-result proof, GrafPort destination receiver, ITEM.EPF/ITEM.EPD legacy remap, by-value ItemInfo lookup, 43x43 crop/recenter semantics, palette slot 10 and slot-2 callback ordering, intentionally non-value-initialized options behavior, optional AlphaMaskSurface overlay path, exact padding, and class owner/emitter UID00006W.`

The row above remains preserved as the exact historically accepted text and current physical row. Do not manually edit generated research/coverage trackers; their state remains validator-owned.

## Follow-Up Actions

1. Ordinary implementation and external supervisor Gate 2A `32/32` verification are complete.
2. External supervisor Gate 2B is complete for all nine C41S-024..030/C41S-042..043 claims under audit timestamp `2026-08-12T07:26:00-04:00` and IDA catalog `0436`: I41S-05R/I41S-06R were applied with exact regular-clear/final-channel readback, seven no-change protections passed, one save succeeded, and fresh persisted reopen reproduced all accepted state. B003 receives no supervisor-action credit.
3. External supervisor manual/generated closure is complete: C41S-032 is physically present once at the accepted location, and commands `000000022519`/`000000022520` plus physical readback closed C41S-033 with exact body equality, no UID00041S empty marker, and intentional no-header output.
4. Fresh exact-artifact Gate 1, final combined Gate 2, and any current report execution/archive status are determined only by external supervisor audit, the report's physical path, and validator-owned history; this body neither requests nor claims those results.

No new target should be started by B003 before supervisor verification or reassignment.

## Confidence

- Behavior/range/call graph: very high.
- Return and draw receiver: very high.
- ItemInfo/resource/remap/palette/rectangle semantics: very high.
- Callback order and nullable options pointer: very high.
- Partial-initialization and complete 32-byte shared options ABI: very high for offsets/meaning/signed reads and exact missing write; field spellings remain inferred.
- Surface declaration ownership and ItemObj/GrafPort visibility routes: very high.
- Error-global source name/linkage: high.
- Original lexical spellings: medium; best human source names are used and remain the score cap rather than leaving decompiler labels.
- Overall recommended target confidence: `94`.

## Validator Results

- During the original callback, every changed ordinary destination received exactly one scoped validator from `source-3/project-documentation` using `python .\tools\validator.py --mode file --file <relative-by-star-path> --apply --queue-timeout 240 --no-generated-refresh`. The target later received the narrow reconciliation validation recorded below. Every ordinary B003 pass exited `0` with `ok: 1`; generated refresh was skipped in those B003 passes. The separate supervisor closure receipts below subsequently performed generated refresh and readback.
- Missing-reference warnings were limited to pre-existing registry omissions (`0003LZ` or `0003XE`) on six support pages (UID00006W, UID0000KH, UID0000TN, UID0001PI, UID00005V, and UID00017N) plus three target references to then-unregistered UID0003ZN. The UID0003ZN scoped pass registered its current mapping. No warning invalidated the scoped file.

| UID | Command ID / timestamp | Exit / ok / warnings | Final SHA256 | Validator-owned side effects |
| --- | --- | --- | --- | --- |
| 00041S historical original callback receipt | `000000022039` / `2026-08-11T12:46:59-04:00` | `0 / 1 / 3 missing_ref_uid` | `07E85396272FDAC0CBED4F3A0C6AAF05D6C0A31EA9F416F47992610B97207BD1` | Superseded identity retained only as dated history; completion/confidence, registry, reference index, projected stats; four stale low-score rows removed. |
| 00041S current reconciliation receipt | `000000022452` / `2026-08-11T20:12:24-04:00` | `0 / 1 / 0` | `486A45507B9DB6FE972A74AC1222216AE85F5EF11DADC81007CB6D64BA6937CE` | Current physical identity; projected-stats path processed with UID00041S incremental no-op; generated refresh disabled. |
| 0001UU | `000000022056` / `2026-08-11T12:49:45-04:00` | `0 / 1 / 0` | `867D0C4638C24FD444206E38646660378F0220CE9A20ADDEE081D5BA72A46EF4` | registry, two reference-index additions, projected stats |
| 00006W | `000000022061` / `2026-08-11T12:50:39-04:00` | `0 / 1 / 4 missing_ref_uid` | `1637603522F7A1B8C021AE4F150A816F4446F02F1688434AE6E5B413B96A70B4` | reference index and projected stats |
| 0000KH | `000000022065` / `2026-08-11T12:51:34-04:00` | `0 / 1 / 6 missing_ref_uid` | `C5522B4819A4C90936CCE0007E8D33BA6EFE22918B66E10F0174B2207734D383` | reference index and projected stats |
| 0000RA | `000000022079` / `2026-08-11T12:52:08-04:00` | `0 / 1 / 0` | `783876511A2453109AB5E7110D71AB0E89180E8901424488DC4A58C971270EBA` | registry, reference index, projected stats |
| 0000TN | `000000022080` / `2026-08-11T12:53:23-04:00` | `0 / 1 / 3 missing_ref_uid` | `00EAF8EE3BE884D38810AAC003F3A652CBD1267421DA8947F399C5E3078FFD24` | two registry hashes, reference index, projected stats |
| 0001PI | `000000022081` / `2026-08-11T12:54:10-04:00` | `0 / 1 / 3 missing_ref_uid` | `4E63568215B41E93F0D076B37DBB70DE4A22EC8D79E279C3FCEAFE98BC18199B` | two reference-index additions and projected stats |
| 00005V | `000000022082` / `2026-08-11T12:54:53-04:00` | `0 / 1 / 3 missing_ref_uid` | `AC82E83E26B726F7D60A553798ABC7FACB4674660E4F3B49293AAE50081C8E38` | registry, reference index, projected stats |
| 000167 | `000000022083` / `2026-08-11T12:55:47-04:00` | `0 / 1 / 0` | `D02649AB7EEA12EE2DE5742221AF4ABB8E663F85C6A3AED2189593D59633C3B7` | registry, stale reference-index removal, projected stats |
| 00017J | `000000022087` / `2026-08-11T12:56:36-04:00` | `0 / 1 / 0` | `EC7372FD6C308DAB46E7D2AE42050AADEF862A2A94E621BAA476C1754A8391D3` | projected stats only |
| 00029J | `000000022089` / `2026-08-11T12:57:59-04:00` | `0 / 1 / 0` | `035B647962CA8C226A618D45AE93FE8559035AF722182644287EA77CF77CDD86` | completion/confidence plus three registry fields and projected stats |
| 0003ZN | `000000022091` / `2026-08-11T12:58:47-04:00` | `0 / 1 / 0` | `DF81E08EDC78C87CA1AF40179BEA2585C131EC39B38DFC4B9D0638873ED72B4C` | UID mapping, metadata/registry, three link repairs, nine index additions, projected stats |
| 0000YO | `000000022094` / `2026-08-11T12:59:48-04:00` | `0 / 1 / 0` | `B471745BB6605942E57137970FF12CE3972F27870192F66E191E5B3345EFA718` | reference index and projected stats |
| 00017N | `000000022097` / `2026-08-11T13:01:06-04:00` | `0 / 1 / 5 missing_ref_uid` | `C5FE001B6B24A69CB1ADDFA3A906773BF2AED076791FBC69A4EDC24C254CFDA3` | projected stats only |
| 00041S supervisor target/generated closure | `000000022519` / supervisor closure | `0 / 1 / 0` | Target `486A45507B9DB6FE972A74AC1222216AE85F5EF11DADC81007CB6D64BA6937CE`; generated ItemObjImageLib.cpp `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F` | Supervisor-owned generated refresh; physical readback found one UID00041S block, exact 3,386-character formal/generated method-body equality, no UID00041S empty marker, and intentional no-header result. |
| UID00041S manual coverage/generated closure | `000000022520` / supervisor closure | `0 / 1 / unrelated existing UID00039L and UID0003T6 missing-reference notices` | Coverage `8D080707A33B24C6A79CC91FE0CA1DFA339F5174F5B79FF94D01F59C35AF425B`; generated ItemObjImageLib.cpp `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F` | Supervisor-owned coverage/generated refresh; exact row present once after UID0000VN and before UID0000UQ; generated readback unchanged and exact. |
| UID00041S persisted IDA Gate 2B receipt | Supervisor audit `2026-08-12T07:26:00-04:00` / IDA catalog `0436` | Public runtime authority, nine claims passed, one save, fresh persisted reopen | Prechange backup `A51206AD896FAA5E3F4F798CD8AA1E40F8ECD794E8F376C80247BA78E28FE4C7E`; canonical IDB `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`, `143205131` bytes, last-write UTC `2026-08-12T11:22:07.8699333Z` | Supervisor-owned IDA closure only: I41S-05R/I41S-06R applied and persisted; seven no-change claims verified; fresh reopen reproduced every accepted/protected result. No validator, by-* doc, generated, coverage, or lifecycle change is attributed to this IDA receipt. |

- Dated B003 callback receipts are limited to the ordinary scoped validations listed above. IDA, manual-coverage, and generated-readback closure is credited only to the external supervisor evidence recorded here. The IDA receipt establishes persisted Gate 2B claim state, not final combined Gate 2 or report lifecycle state. Current execution/archive authority derives solely from fresh external audit, the artifact's physical path, and validator-owned status/history metadata.

## Changed Files

- Ordinary callback destinations changed: target UID00041S; ItemObj support UIDs 0001UU, 00006W, 0000KH, and 0000RA; Surface/GrafPort support UIDs 0000TN, 0001PI, 00005V, 000167, and 00017J; and flag/remap/overlay/index support UIDs 00029J, 0003ZN, 0000YO, and 00017N.
- The exact formal target body, fixed-slot declaration, ItemObj/GrafPort include routes, sole shared Surface slot-2 ABI, duplicate-declaration removal, error-flag CPP/H, remap evidence/scores, overlay caller map, and aggregate source-readiness state were physically reread at the current hashes in the Validator Results table. The target's formal CPP remains an exact 105-line match to Section 22, its H channel remains blank, and its current identity is the `000000022452` receipt rather than the historical `000000022039` receipt.
- B003's ordinary scoped validators used generated refresh disabled and made only their recorded registry/reference/projected-statistics side effects; B003 did not directly edit or refresh generated/tracker or `-coverage-report.md` files. Later external supervisor commands `000000022519` and `000000022520` performed generated/manual closure, and the primary supervisor separately performed the persisted IDA Gate 2B work recorded above. No audit, assignment, goal, notes, generated file, coverage file, lifecycle state, or `*-removed.md` companion was edited by B003 during this reconciliation.
- Every ordinary callback destination lease was JIT-only and released after its validator/stable reread. The narrow UID00041S reconciliation used a B003 target lease only for command `000000022452` and its stable reread, then released it immediately. This current report-only closure reconciliation uses only a JIT B003 report lease and releases it immediately after the edit; no by-* destination lease or edit is involved.

## Implementation Tracking Checklist

| Done | Claim ID | Target UID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| [x] | C41S-001 | 00041S | Exact target is `[0x004defc0,0x004df1d4)`, size `0x214`, with six leading and twelve trailing `CC` padding bytes. | very high | lookup/bytes/disasm; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Status and Range | incorporate | applied |
| [x] | C41S-002 | 00041S | Source return is `void`; all seven direct callers ignore EAX. | very high | complete xrefs and post-call reads; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Signature/Caller Evidence and UID0001UU declaration | incorporate | applied |
| [x] | C41S-003 | 00041S | First explicit argument is active `GrafPort *drawPort`, not unused `Pane *drawOwner`. | very high | current tinfo, saved receiver, slot call; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Signature/Behavior and UID0001UU | incorporate | applied |
| [x] | C41S-004 | 00041S | Item entry begins as `unsigned short(itemId + 0x4000)` and uses the exact legacy remap/clamp constants when EPF assets are disabled. | very high | decompile/data/xrefs; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| [x] | C41S-005 | 00041S | The method initializes `EPFTileContext` and copies a complete by-value `ItemInfo` from `m_itemInfos.GetAtOrDefault`. | very high | decompile/frame/type; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior/Types | incorporate | applied |
| [x] | C41S-006 | 00041S | Frame lookup/render occurs when screen palette mode is zero or `itemInfo.allowPaletteFilter` is true. | high | direct branch and ItemInfo type; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| [x] | C41S-007 | 00041S | Source uses pooled literals `L"ITEM.EPF"` and `L"ITEM.EPD"`; these are not source globals. | very high | exact bytes and xref fanout; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Resources/Historical Corrections | incorporate | applied |
| [x] | C41S-008 | 00041S | Palette lookup is category/slot 10 with `itemInfo.paletteSlot` and `paletteFlags >> 5`. | very high | direct call operands; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Behavior | incorporate | applied |
| [x] | C41S-009 | 00041S | Source bounds are cropped to 43x43 only when larger than destination dimensions, then destination bounds are recentered and forced to 43x43. | very high | direct arithmetic; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Rectangle Semantics | incorporate | applied |
| [x] | C41S-010 | 00041S | Slot-2 callback order is drawPort receiver, context, source bounds, destination bounds, mode 1, palette, nullable options. | very high | decompile/disasm/support providers; implementation receipt: target 486A4550 and Surface 00EAF8EE; target validator 000000022452 exit 0, ok 1 | target Callback Contract and Surface support | incorporate | applied |
| [x] | C41S-011 | 00041S | Options fields used here are kind +0, alpha +4, signed paletteShift +8, and signed secondaryPaletteShift +0x1c. | very high | target writes and provider signed-byte reads; implementation receipt: target 486A4550 and Surface 00EAF8EE; target validator 000000022452 exit 0, ok 1 | target Options Evidence and Surface support | incorporate | applied |
| [x] | C41S-012 | 00041S | The options local must not be value-initialized; kind 3 intentionally leaves +0x1c indeterminate. | very high | absent write plus RGB565 read; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target Formal CPP/Historical Correction | incorporate | applied |
| [x] | C41S-013 | 00041S | Optional overlay calls `AlphaMaskSurface::DrawEncodedAlphaFrame` after the palette-gated block with alpha `(strength*32)+0.5`. | very high | target call and UID0000YO; implementation receipt: target 486A4550 and overlay B471745B; target validator 000000022452 exit 0, ok 1 | target Overlay Behavior | incorporate | applied |
| [x] | C41S-014 | 00041S | Direct owner/emitter remains UID00006W; source file remains UID0000KH. | very high | this-based storage and emitter graph; implementation receipt: owner/emitter confirmed in target 486A4550; validator 000000022452 exit 0, ok 1 | target Ownership/Metadata | already-present | already-present |
| [x] | C41S-015 | 00041S | Target metadata should become `92/94`, reconstructable true, owner/emitter UID00006W. | high | blocker closure and first-draft readiness; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target headers/Score Rationale | incorporate | applied |
| [x] | C41S-016 | 00041S | Target CPP receives the exact Section 22 method body; target H remains blank because UID0001UU owns the declaration. | high | ownership and formal-channel rules; implementation receipt: target 486A4550; validator 000000022452 exit 0, ok 1 | target formal blocks | incorporate | applied |
| [x] | C41S-017 | 00041S | UID0001UU replaces only the stale fixed-slot declaration with the exact void/GrafPort declaration. | high | complete caller and receiver proof; implementation receipt: UID0001UU 867D0C46 | by-type ItemObjImageLibLayout formal declaration | incorporate | applied |
| [x] | C41S-018 | 00041S | UID00006W and UID0000KH replace stale fixed-slot/no-code prose, preserve scores/routes, and record the Surface declaration visibility needed before the method body. | high | current class/file docs and generated source order; implementation receipt: UID00006W 16376035 and UID0000KH C5522B48 | class/file method and source-integration sections | incorporate | applied |
| [x] | C41S-019 | 00041S | UID0000TN/Surface.h becomes the sole complete slot-2 typedef/global/options declaration owner, adds the one definition in physical slot order, and preserves partial initialization. | very high | callback-family formal owner, provider assignments, 114 refs; implementation receipt: UID0000TN 00EAF8EE | by-global SurfaceRenderCallbackTable formal CPP/H and slot 2 | incorporate | applied |
| [x] | C41S-020 | 00041S | UID0001PI records slot-2 physical storage as one typed four-byte global and no duplicate source. | high | inspect/xrefs/semantic owner; implementation receipt: UID0001PI 4E635682 | physical Surface callback page | incorporate | applied |
| [x] | C41S-021 | 00041S | UID000167 removes its duplicate local `SurfaceSpriteBlitOptions` forward/typedef/extern preamble, relies on Surface.h through GrafPort.cpp, adds UID00041S as a direct consumer, and retains only its enum/GrafPort body. | very high | current UID000167 formal CPP, exact GrafPort include route, one-definition ownership; implementation receipt: UID000167 D02649AB and UID00005V AC82E83E | SurfaceSpriteBlitHelper formal CPP and support prose | incorporate | applied |
| [x] | C41S-022 | 00041S | UID00029J adopts inferred `bool g_imageLibraryLoadError`, ImageLib ownership, one-byte semantic storage, and trailing alignment. | high | guard, 132-ref history, zero tail refs; implementation receipt: UID00029J 035B6479 | error-flag target/formal CPP/H | incorporate | applied |
| [x] | C41S-023 | 00041S | UID0003ZN retains exact three file-static short definitions and adds this target's current evidence; recommend `92/94`. | high | bytes and exact three-consumer xrefs; implementation receipt: UID0003ZN DF81E08E | remap-constants page | incorporate | applied |
| [x] | C41S-024 | 00041S | Primary supervisor Gate 2B verified the persisted target rename, source-equivalent prototype, five argument names, accepted function comment, range, bytes, seven callers, and protected frame rows before save and after fresh persisted reopen. | high | External supervisor audit `2026-08-12T07:26:00-04:00`, IDA catalog `0436`, canonical IDB SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-025 | 00041S | Primary supervisor applied I41S-05R as one four-byte `SurfaceSpriteBlitProc` data item `[0x69b3e8,0x69b3ec)` with entity `g_pfnBlitSprite` size 4, exact repeatable callback comment, explicitly cleared regular channel, and exact item/entity/byte/xref/tail/neighbor protections. | high | External supervisor observed the known latent regular text, cleared it through the accepted public action, proved final regular absent/repeatable exact, saved once, and reproduced all state after persisted reopen on canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-026 | 00041S | Primary supervisor applied I41S-06R as one one-byte `bool` data item at `0x69b420` with entity `g_imageLibraryLoadError` size 1, exact repeatable latch comment, explicitly cleared regular channel, retained three separate alignment heads, and exact byte/xref/neighbor protections. | high | External supervisor proved final regular absent/repeatable exact, `132/0/0/0` refs, zero bytes, exact tails/neighbors, one save, and persisted reopen on canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-027 | 00041S | Primary supervisor Gate 2B verified both resource literals remain single 18-byte `const wchar_t[9]` items with exact names/types/UTF-16 bytes/comments/boundaries and `6/4` refs. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-028 | 00041S | Primary supervisor Gate 2B verified the three named signed remap constants and alternating gaps retain exact types, values, comments, boundaries, and `3/0/3/0/3/0` refs. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-029 | 00041S | Primary supervisor Gate 2B verified `AlphaMaskSurface_DrawEncodedAlphaFrame` retains exact name/range/prototype/frame/comments/bytes and nine callers. | high | External supervisor pre-action and persisted-reopen no-change readbacks under audit `2026-08-12T07:26:00-04:00` / catalog `0436`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-030 | 00041S | Primary supervisor Gate 2B verified already named dependencies and every prohibited unlisted name/type/comment/item/byte/xref state remain unchanged. | very high | External supervisor no-change protections passed before save and after persisted reopen; no B003 action credit. | IDA no-change protections | already-present | applied |
| [x] | C41S-031 | 00041S | No split/merge/range change is needed; preserve padding ownership on UID0000VN. | very high | exact boundaries and sibling starts; implementation receipt: target/aggregate 486A4550/C5FE001B; target validator 000000022452 exit 0, ok 1 | target Range and ignored padding | already-present | already-present |
| [x] | C41S-032 | 00041S | The exact UID00041S manual by-memory coverage row is physically present once after UID0000VN and before UID0000UQ; no further manual edit is requested. | high | Supervisor command `000000022520`, coverage SHA256 `8D080707A33B24C6A79CC91FE0CA1DFA339F5174F5B79FF94D01F59C35AF425B`, exact row/location readback. | supervisor-owned by-memory/-coverage-report.md | already-present | already-present |
| [x] | C41S-033 | 00041S | Supervisor generated refresh/readback confirms exactly one UID00041S generated block, exact formal/generated body equality, no UID00041S empty marker, and intentional no-header output. | high | Supervisor commands `000000022519`/`000000022520`; ItemObjImageLib.cpp SHA256 `C0438B4B9F03F1AC2BF2286D66F1FECB532FA1C9E22D70164F594E3DD8E8C81F`; 3,386-character exact body match. | supervisor generated refresh/readback | incorporate | applied |
| [x] | C41S-034 | 00041S | Historical `int`, `Pane *`, unused-owner, unresolved-callback, pooled-global, and impossible-partial-init assumptions must be retained as superseded evidence. | high | current docs versus current IDA; implementation receipt: historical corrections retained in target/support; target 486A4550; validator 000000022452 exit 0, ok 1 | target/support Historical Corrections | historicalize | applied |
| [x] | C41S-035 | 00041S | Wave2/Wave3 material is stale and must not be incorporated. | very high | current skill/goal; implementation receipt: current skill rejects stale Wave2/Wave3; none incorporated | target evidence provenance | reject-stale | excluded-with-reason |
| [x] | C41S-036 | 00041S | The complete 32-byte shared options ABI is kind +0, alpha +4, signed paletteShift +8, maskPixels +0x0c, maskWidth +0x10, maskHeight +0x14, maskStride +0x18, and signed secondaryPaletteShift +0x1c with only implicit Win32 alignment padding. | very high | UID00041S/sibling producers, RGB565 provider, StaticObj kind-8 producer; implementation receipt: UID0000TN 00EAF8EE | Surface formal H and target/support ABI sections | incorporate | applied |
| [x] | C41S-037 | 00041S | StaticObj kind 8 copies the exact AlphaSurfaceBufferView pixels/width/height/stride quartet into options +0x0c..+0x18 before slot-2 dispatch. | very high | live 0x4dd66a/0x4dd70c decompile and UID0001WW layout; implementation receipt: UID00017J EC7372FD | UID00017J and Surface options evidence | incorporate | applied |
| [x] | C41S-038 | 00041S | Surface.h already has exact pointer-only GrafPort, EPFTileContext, RectBounds, and DLPalette forward declarations; the options struct/typedef belongs immediately after them and the extern/definition follows physical slot order. | very high | UID0000TN formal CPP/H and generated Surface.h/cpp; implementation receipt: UID0000TN 00EAF8EE | UID0000TN formal CPP/H | incorporate | applied |
| [x] | C41S-039 | 00041S | ItemObjImageLib.cpp position-0 preamble must include EPFTileContext.h, ImageLib.h, and Surface.h before the singleton/class/method emitters. | very high | current generated source order and complete-type use; implementation receipt: UID0000RA 78387651 and UID0000KH C5522B48 | UID0000RA formal CPP and UID0000KH integration prose | incorporate | applied |
| [x] | C41S-040 | 00041S | GrafPort.cpp must include Surface.h immediately after GrafPort.h; GrafPort.h retains only its pointer-use SurfaceSpriteBlitOptions forward declaration. | very high | current UID00005V CPP/H and UID000167 dedup route; implementation receipt: UID00005V AC82E83E and UID000167 D02649AB | UID00005V formal CPP and UID000167 support | incorporate | applied |
| [x] | C41S-041 | 00041S | The current 114-ref and generated-consumer inventory proves one cross-subsystem slot-2 ABI and rejects per-caller declarations or ownership. | very high | complete slot xrefs and generated source search; implementation receipt: UID0000TN 00EAF8EE and UID0001PI 4E635682 | UID0000TN consumer inventory and affected support pages | incorporate | applied |
| [x] | C41S-042 | 00041S | Primary supervisor Gate 2B bound every I41S-05..11 physical head/span/type/entity-name/comment/value/xref/neighbor protection, including intermediate and final regular/repeatable channel reads for I41S-05R/I41S-06R. | very high | External supervisor complete pre-action, post-action, post-save, and persisted-reopen census under canonical SHA256 `7991D551958C8E17DC16CEA48C329C719369AE99114011C4A313B9E95BD020BF`; no B003 action credit. | supervisor Gate 2B | incorporate | applied |
| [x] | C41S-043 | 00041S | Primary supervisor Gate 2B verified the exact post-I41S-02 target prototype/frame and every protected local/saved/return row before save and after fresh persisted reopen. | very high | External supervisor audit `2026-08-12T07:26:00-04:00`, IDA catalog `0436`, exact persisted target/frame readback; no B003 action credit. | supervisor Gate 2B | incorporate | applied |

Initial report-only pass:

- [x] Supervisor validation required before implementation. Completed as a dated prerequisite to the ordinary callback.
- [x] For a by-file target, exhaustive whole-file inventory completed and reconciled against IDA, all related by-* pages, generated output, and matching reports; every function and every other code/data/type/resource/range item has an evidence-backed disposition rather than a sampled-subset or generic future-work entry: not applicable; the sole declared target is a by-memory method page.
- [x] For a by-file target, every file-owned inventory item has explicit behavior/role, ownership/emitter/source placement, CPP/H or no-code, metadata/score, destination doc, Claim And Incorporation Ledger, and Implementation Tracking Checklist coverage as applicable; excluded candidates have evidence-backed reasons: not applicable; the sole declared target is a by-memory method page.
- [x] For a by-file target, current generated `.cpp`/`.h` audited against the inventory and every missing/incomplete function, code path, data definition, declaration/type/include/forward declaration, child route/order defect, stub/placeholder/empty marker, and required-but-missing header has an exact formal CPP/H repair or evidence-backed no-code/no-header disposition: not applicable as a by-file requirement; target-specific generated source/header visibility was nevertheless audited and incorporated.
- [x] Target/support docs to update: exact ordinary destinations are enumerated in Sections 24 and 25 and mirrored by C41S-001..023, C41S-031, and C41S-034..041.
- [x] Every declared additional UID has target-specific evidence, recommendation, ledger rows, destination docs, and planned validation: no additional target UID is declared; the sole-target inventory is explicit in Section 3.
- [x] Current target state and actual evidence checked recorded: Sections 4 and 10 contain the current physical state and evidence inventory.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: exact C41S-001..043 ledger is present.
- [x] Metadata/score changes to apply: dated baseline and current physical state are explicit in Sections 24 through 26.
- [x] Score-limiting blockers researched to resolution, implementation-ready repair/split recommendation, or rare exact evidence-backed no-improvement proof after all reasonable current routes were exhausted: target/shared-ABI blockers are resolved and remaining lexical uncertainty is an evidence-backed score cap.
- [x] Owner/emitter/reconstructable changes to apply: direct owner/emitter/reconstructable state is proven correct and retained.
- [x] Split/rename/new-child changes to apply: no range split/new child is required; exact analysis renames are listed as supervisor-owned actions.
- [x] Source-placement, range/split/padding/reclassification, or IDA rename/type/comment changes to apply or confirm not applicable: Sections 17 through 21 provide exact dispositions.
- [x] Supervisor Gate 2B IDA handoff and external closure list every exact address/entity, pre-state, action, supporting type, safety constraint, and expected/observed readback: Section 21 preserves the dated `0425` and failed unsaved attempt as history and records the persisted 0719 I41S-05R/I41S-06R regular-clear closure plus seven no-change verifications without crediting B003 for supervisor actions.
- [x] First-draft CPP block text/no-code proof and H block text/no-header-code proof to apply: Section 22 contains the complete CPP and class-owned-H disposition.
- [x] Third-party import directive to apply or confirm not applicable, including exact `third_party_embeds/...` paths and confirmation that the multiline C++ block remains blank: not applicable; this is client-authored ItemObjImageLib/Surface work, not a third-party embed, and the target formal CPP is intentionally nonblank.
- [x] Exact target/support doc facts to incorporate at report-level detail: Sections 24 and 25 and C41S-001..043 are exact.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence to preserve: Sections 8 and 20 retain them.
- [x] Wave2/Wave3 mentions or artifacts encountered and ignored/rejected as stale, if applicable: stale material was rejected under C41S-035.
- [x] Open questions to close or document as evidence-backed unresolved: Section 27 closes target-local/shared-declaration questions and isolates only score-capping lexical uncertainty.
- [x] Validators to run: ordinary scoped validators and their required no-generated-refresh mode were specified before callback.
- [x] Generated report refresh expected or explicit manual supervisor-owned coverage/tracker text to apply: the dated report-only handoff specified exact supervisor-owned C41S-032/C41S-033 work, and later external evidence closes both claims as recorded in Sections 28 and 31 without crediting B003.

Implementation callback pass:

- [x] Supervisor-owned IDA closure is externally complete: all nine C41S-024..030/C41S-042..043 rows are applied/checked from the persisted 0719 receipt; I41S-05R/I41S-06R and all seven no-change protections passed before save and after fresh persisted reopen. B003 receives no action credit.
- [x] For a by-file target, all accepted whole-file inventory claims were incorporated and verified across the by-file page and every affected support page; no function or other file-owned code/data/type/resource item was silently omitted: not applicable; the sole declared target is a by-memory method page.
- [x] For a by-file target, every accepted missing/incomplete function/code/data/declaration/header/routing repair was applied through owning formal CPP/H channels; a coherent validator refresh completed; current generated `.cpp` and required `.h` were physically reread against the full inventory and contain no unresolved stub, placeholder, empty marker, partial implementation, missing declaration, or ordering defect. If no `.h` exists, the report contains a file-specific verified no-header disposition: not applicable as a by-file requirement; target/support formal CPP/H and visibility changes were physically verified, and external supervisor commands `000000022519`/`000000022520` close C41S-033 with exact generated-body equality, no empty marker, and the intentional no-header result.
- [x] Report accepted by supervisor for implementation: the dated ordinary callback authorization is recorded by the implemented claim allocation and destination receipts.
- [x] All accepted target/support doc details incorporated at report-level detail: 32 ordinary rows are checked with physical destination proof.
- [x] Every declared primary/additional UID verified independently against its ledger and destination docs: sole primary UID00041S and every support destination were physically reread; no additional target UID exists.
- [x] Claim And Incorporation Ledger updated with destination and verification state for every accepted claim: exact 43-row mirror is current.
- [x] Metadata/score/owner/emitter/split/rename/CPP/H changes applied or explicitly not applied with reason: ordinary document/formal, manual-coverage, generated-readback, and all nine externally supervisor-owned Gate 2B rows are closed with B003 action credit excluded.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence preserved: retained in target/support documents and this report.
- [x] Open questions resolved or documented with evidence-backed unresolved rationale: Section 27 is current.
- [x] Validators run and results recorded: Section 31 records each ordinary destination receipt and current target reconciliation receipt.
- [x] Generated CPP/H and report refresh completed by validator; each affected generated file's command ID is current, or explicit manual supervisor-owned coverage/tracker text is supplied/confirmed unchanged: external supervisor commands `000000022519`/`000000022520` and physical readback close C41S-033; B003 claims no supervisor-action credit.
- [x] Remaining accepted technical items reconciled: C41S-032/C41S-033 and all nine Gate 2B claims are externally closed at claim level. Fresh exact-artifact Gate 1, final combined Gate 2, and report execution/archive state remain external supervisor/validator authority rather than report claims.
- [x] Report body is lifecycle-neutral: current execution/archive truth is deferred to the current path and validator-owned status/history metadata, and no body/checklist sentence will become false merely because the supervisor executes or invalidates the report: confirmed.

READY_FOR_SUPERVISOR_EXECUTE

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B003","command_id":"000000022521","destination_path":"executed-b-agent-research/B003/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B003/research/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","timestamp":"2026-08-12T03:14:40-04:00","uid":"00041S"} -->
<!-- {"agent":"B003","command_id":"000000022522","destination_path":"tools/leaser/Agents/Agent-B003/research/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","details":"report returned to active agent research for rework","event":"de-executed-for-rework","source_path":"executed-b-agent-research/B003/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","timestamp":"2026-08-12T03:17:49-04:00","uid":"00041S"} -->
<!-- {"agent":"B003","command_id":"000000022708","destination_path":"executed-b-agent-research/B003/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B003/research/00041S-ItemObjImageLibDrawItemImageIn43x43Slot-source-quality.md","timestamp":"2026-08-12T07:54:10-04:00","uid":"00041S"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
