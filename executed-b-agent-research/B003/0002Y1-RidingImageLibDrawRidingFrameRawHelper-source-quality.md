** TARGET-REPORT-UID:0002Y1 **
** AUTHOR-AGENT-ID:B003 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **
# UID0002Y1 RidingImageLib Draw Riding Frame Raw Helper Ownership / Split Research


## Finalized Report / Current Recommendation

- Implemented recommendation: UID0002Y1 preserves its exact half-open range at validator-renamed `by-memory/0x004dc7e0-0x004dca14.RidingImageLibRenderRiding.md`, is scored `92/94`, emits through UID0000BZ, and contains the complete accepted R1 body as retained `RidingImageLib::RenderRiding` source.
- Final disposition: source-authored, retained, non-inlined `RidingImageLib` member. It is not compiler output, data, padding, an interior tail, or a body that can be discarded merely because IDA missed the function and no entry xref survives.
- Callback result: C01-C20 and R1-R3 are reconciled below; the UID-preserving rename, class/type declarations, parent/file/support synchronization, scoped validators, and waited generated verification are complete. Exact manual coverage text remains supervisor-owned and was not applied by B003.
- Confidence: very strong for range, five-argument ABI, source family, body, helper contracts, owner/emitter, and behavior; strong for the inferred original method and field spellings.
- B003's ordinary-documentation implementation and readback are complete. B003 did not manually edit generated output, manual coverage, tracker/audit/supervisor/validator state, IDA, or lifecycle state and ran no report execution/probe/count/move/archive/lifecycle command.

## Supporting Research

### Current and historical report roots searched

The exact terms `0002Y1`, `0x004dc7e0`, `0x004dca14`, `RidingImageLibDrawRidingFrameRawHelper`, `RidingImageLib`, `RIDINGS.EPF`, `GetFrame`, `g_pfnBlitScaledRleTint`, and `RenderMonster` were searched with exact fixed-string, case-insensitive report scans on 2026-07-14.

| Root | Outcome and target-specific classification |
| --- | --- |
| `tools/leaser/Agents/Agent-B001/research/**` | No direct target/range/RidingImageLib/RIDINGS.EPF report. One active `0002QY` report mentions `GetFrame`/`RenderMonster`; it is caller-family support only. |
| `tools/leaser/Agents/Agent-B002/research/**` | No direct target/range/RidingImageLib/RIDINGS.EPF report. One `0003RB` `RenderMonster` mention is unrelated SelfLook support. |
| `tools/leaser/Agents/Agent-B003/research/**` | No pre-existing direct report or relevant support match before this artifact was created. |
| `tools/leaser/Agents/Agent-B004/research/**` | No direct or relevant match. |
| `tools/leaser/Agents/Agent-B005/research/**` | No direct or relevant match. |
| `executed-b-agent-research/**` | One direct historical lead: B008 `0000N6-RidingImageLib-empty-emitter-family-source-quality.md`. It correctly preserved the body and boundaries but made the now-rejected no-emitter decision from absent function/xref/caller evidence. Relevant independent support includes B007 `0001CM` shared animation tables, B001 `0002R2` LivingObject draw callers, B003 `00017E` Monster image support, B004 `00039V` callback initialization, and B014 `0002V8` projection fields. |
| `archived/**` | Root exists; no exact or relevant match for any searched term. |
| `tools/leaser/Agents/Older-Research/**` | Root exists; no exact or relevant match. |
| `tools/leaser/Agents/SpecialReports/**` | Root exists; no exact or relevant match. |

### Historical lead classification

- Executed B008 is retained as historical evidence for the exact raw range, 169 instructions, cookie, calls, padding, RidingImageLib owner, and `RIDINGS.EPF` role.
- Its statement that no direct entry route prevents a source signature is superseded. Live MCP establishes the complete ABI from `retn 0x14` and stack use, while modeled `MonsterImageLib::RenderMonster` at `0x004db100` is an instruction- and control-flow-equivalent source sibling with one concrete `LivingObjectPane::DrawSpriteFrame` caller.
- B007's `ImageAnimationTable`, `ImageAnimationGroup`, `ImageAnimationFrame`, and `GetFrame` names remain the shared helper authority. This report does not move helper implementation into RidingImageLib.
- B001's current `LivingObjectPane::DrawSpriteFrame` formal proves the source argument order `(GrafPort *, const RectBounds *, const ObjectStatusBlob *, group, frame)` for the Monster sibling. It does not prove a direct call to the retained riding body.
- B004's Surface callback work and fresh callback-target decompilation resolve slot 4 beyond the old generic "composition" label.
- B014's NewHuman report correctly identifies `MapPane +0x3d4/+0x3d8` as projection divisor/shear consumers. Fresh target and callback analysis adds `+0x3d0` as the float tint amount passed to slot 4.

## Target

- Target UID: `0002Y1`.
- Implemented target path: `by-memory/0x004dc7e0-0x004dca14.RidingImageLibRenderRiding.md`.
- Historical pre-callback path: `by-memory/0x004dc7e0-0x004dca14.RidingImageLibDrawRidingFrameRawHelper.md`; validator command `000000012169` registered its UID-preserving path update and the old path no longer exists.
- Historical source queue/report row: the validator-owned tracker showed a by-memory not-covered reconstructable target at `86/90` with zero direct target reports before this assignment. B003 does not assert or manually change its later lifecycle/count state.
- Callback classification: bounded implementation of the exact Gate-1-passed artifact; ordinary implementation is complete and external lifecycle state is neither asserted nor directed here.
- Implemented scores and parent state: target `92/94`, owner/emitter UID0000BZ, reconstructable true, blank position, R1, `Nested:0`; class UID0000BZ `91/92`; file UID0000N6 `91/92`; type UID0001VV `90/92`; parent UID00017F remains `87/91` and non-emitting.

## Current Target State

- Implemented metadata: exact range/owner/reconstructable/Nested state are preserved; completion/confidence are `92/94`; emitter UID0000BZ, blank position, exact Item Summary, and R1 are installed.
- Implemented C++/emitter state: B003's waited validator command `000000012200` generated one UID0002Y1 `RidingImageLib::RenderRiding` definition and zero target or aggregate Empty Emitter Markers. Latest read-only observation after a concurrent validator-owned refresh at command `000000012202` preserves those exact results.
- Historical stale blocker: no modeled IDA function, no target-entry xref, and no direct target caller had been treated as a no-code gate. Those remain valid negative reachability facts but are not no-source proof.
- Implemented behavior documentation covers the RIDINGS.EPF definition/frame selection, duplicate helper call, rectangle/projection branches, callback contract, source sibling, cookie exclusion, exact range/hash, and positive/negative evidence without raw source artifacts.
- Related docs checked: UID00017F parent, UID0000BZ class, UID0000N6 file, UID0001VV definition, UID0001CM shared table helpers, UID00009S ObjectStatusBlob, UID0003NV LivingObject draw caller, UID0000TN/UID0001PI callback table, UID00004I EPFTileContext, UID0002KP ResourceLayout lookup, UID00007Q MapPane, UID0000PR active-map global, RectBounds/OffsetRect support, MonsterImageLib support, generated RidingImageLib.cpp, tracker, and manual coverage rows.
- Artifact/lifecycle boundary: this is B003's research/implementation-callback artifact path before external supervisor lifecycle handling. Validation/execution/count/path/move/archive state is supervisor/validator-owned and is neither asserted nor directed by this report.

## Executive Recommendation

- Direct owner: UID0000BZ `RidingImageLib`; source root UID0000N6 `NexusTK/render/RidingImageLib.cpp`.
- Source role: retained member `void RidingImageLib::RenderRiding(GrafPort *, const RectBounds *, const ObjectStatusBlob *, unsigned int animationGroup, unsigned int animationFrame)`.
- Rename: the stale `DrawRidingFrameRawHelper` title/path was replaced with `RidingImageLibRenderRiding` through the validator-aware UID-preserving workflow. The old name remains only as superseded provenance.
- Emit: target emitter UID0000BZ and R1 are installed; R2 contains the member in the complete UID0000BZ declaration; R3 declares the shared `GetFrame` contract in UID0001VV without duplicating UID0001CM implementation.
- No split or merge: keep `[0x004dc7e0,0x004dca14)` as one method; preserve both adjacent alignment spans outside it.
- Scores: implemented target `92/94`, class `91/92`, file `91/92`, and RidingDefinition `90/92`; support scores otherwise remain unchanged.

## Supervisor Active Recheck

- The Gate-1 research assignment explicitly required reanalysis of the no-emitter/no-C++ decision and complete five-argument ABI recovery rather than repeating the no-route conclusion; that requirement is satisfied and incorporated.
- The assigned item did not require a child split. It remains the exact source-body child of UID00017F.
- The one source-bearing range is fully resolved. Adjacent predecessor, padding, successor, shared helpers, callback targets, types, and source root are separately accounted for.

## Inference Research Guidance Check

- Binary facts, documentation facts, and source inferences are separated throughout this report.
- Existing assumptions treated as uncertain were the no-route no-code conclusion, `DrawRidingFrameRawHelper` name, unknown five arguments, generic callback role, raw MapPane offsets, and whether RidingDefinition can coherently call the shared table helper.
- The recommendation follows `by-structure.md`: exact by-memory method body emits through its direct class owner; class/type declarations remain at class/type pages; shared animation implementation stays at UID0001CM; Surface and MapPane remain dependencies.
- Mid-2000s source style is used: member method, typed records, named rectangles, early returns, shared helper calls, no raw addresses, no decompiler temporaries, no hand-authored cookie, and no explicit compiler lowering.
- Wave2/Wave3 instructions were not used. Any stale references encountered in historical material were ignored unless corroborated by current by-* docs or live MCP.

## Heuristic / Inference Reanalysis And Validation

| Issue | Reanalysis and final disposition |
| --- | --- |
| No function object | IDA still reports no function at start/end. The exact prologue, cookie, 169 decoded instructions, `retn 0x14`, complete epilogue, and padding prove a standalone compiled body. Function-model absence is tool state, not source classification. |
| No entry xrefs | Fresh xref query returns zero. The target remains retained/unreferenced in this build, analogous to source-shaped retained helpers. This reduces source-name certainty but does not suppress source. |
| Five arguments | Physical thiscall ABI is exact: ECX is RidingImageLib; stack arguments are target GrafPort, destination anchor RectBounds, ObjectStatusBlob, animation group, and animation frame. `retn 0x14` proves five four-byte stack arguments. |
| Source family | The first definition/lookup sequence has a byte-pattern twin only at modeled `0x004db100` RenderMonster. The full control flow is the same except Monster archive resolution versus `RIDINGS.EPF` lookup. Accept sibling member source. |
| Method name | `RenderRiding` is the strongest source-facing parallel to accepted `RenderMonster`; `DrawRidingFrame` is descriptive but weaker, and `RawHelper` is rejected. Exact original spelling remains inferred. |
| ObjectStatus field | Current ObjectStatusBlob declaration proves stack argument 3 and `+0x04` as `m_appearanceId`. Do not invent a riding-index field at `+0x04`; riding-specific fields live at `+0x2a..+0x30` and are not used here. |
| Definition index | Exact expression is unsigned-16 wrap of `m_appearanceId + 0x8000`, then unsigned comparison with `ridingDefinitionCount`. |
| Definition/table type | RidingDefinition is exactly 12 bytes and ABI-compatible with ImageAnimationTable. Preserve its riding field aliases and add a `GetFrame(group, frame)` declaration whose implementation remains the shared UID0001CM helper. |
| Duplicate GetFrame | Both target and RenderMonster call `GetFrame` twice with the same receiver/group/frame; the first result supplies `frameOffset`, the second result is discarded after the `+0x05` flag read. Preserve the second call because the out-of-line helper is not pure. |
| Frame index | Signed frame index is `ridingId + frameOffset`; negative exits. Resource lookup receives the signed low 16 bits, matching `movsx eax, di`. |
| Resource path | Target uniquely initializes EPFTileContext and calls `g_pEPFLib->LookupLayoutEntry(L"RIDINGS.EPF", ...)`; this is the riding-specific difference from RenderMonster. |
| Rectangle math | Source bounds are `EPFTileContext::bounds`; destination left/top are caller bounds left/top; width/height are copied from source extents. Preserve integer order. |
| Draw gate | `animationGroup == 0` returns after resource and rectangle preparation. It is not an archive-load failure test. |
| Projection flag | RidingDefinition `+0x05`, currently `defaultBucketIndex`, acts as a centered-projection/suppression flag in both sibling bodies. Use a local descriptive boolean while retaining field-name caveat. |
| Thresholds | Preserve source-bottom thresholds exactly: `< -4`, `>= -4`, and `> 5`; do not normalize them to clipping APIs. |
| MapPane fields | Callback targets prove `+0x3d0` is float tint amount; `+0x3d4` is float vertical projection divisor/scale; `+0x3d8` is float horizontal projection shear. Existing day/night prose is a cross-context alias, not grounds for raw offsets in C++. |
| Cross-class access | RidingImageLib, MonsterImageLib, and NewHumanImageLib all compile direct loads of the same MapPane render fields with no out-of-line getter calls. Public render-state fields, or equivalently inlined public accessors, are therefore required source access; protected access is impossible for these unrelated classes and separate friendship for every image library is a weaker alternative. R1 uses the direct public-field shape. |
| Callback | Slot `0x0069b3f0` is source-facing `g_pfnBlitScaledRleTint`, void, GrafPort receiver plus eight stack arguments. Fresh compat/RGB565 decompiles agree on float tint, int row offset, float vertical scale, and float horizontal shear. |
| Return type | Source return is void. Residual EAX in Hex-Rays' RenderMonster result is not consumed by its void caller and does not establish an integer source return. Target exits only by returning. |
| Compiler artifacts | Stack cookie setup/check, saved registers, SSE dword moves, stack argument staging, and return-pop lowering are compiler output and excluded from R1. |
| Source placement | Riding resource literal, class fields, sibling naming, and surrounding constructor/destructor establish RidingImageLib.cpp. MonsterImageLibTables owns only shared table implementation. |
| Range | Exact standalone range; no interior table/pad/data split. Pre/post `0xcc` are parent-only alignment, not target bytes. |

Rejected alternatives:

1. Keep blank source because no route: rejected by complete ABI and exact modeled source sibling.
2. Merge with destructor or `0x004dca20`: rejected by alignment on both sides and separate prologue/epilogue.
3. Classify as compiler-generated: rejected by feature-specific resource lookup and branch-rich source logic.
4. Route to MonsterImageLib or shared table file: rejected by RidingImageLib receiver fields and RIDINGS.EPF literal.
5. Model as a free/file-local helper: weaker than member ABI because ECX is `this`, reads `this+0x04/+0x08`, and parallels a class member.
6. Use `m_ridingDefinitionIndex` from ObjectStatusBlob: rejected because target reads `+0x04`, while that field is at `+0x2c`.
7. Delete the second GetFrame as redundant: rejected because both compiled sibling bodies retain it.
8. Add a defensive global null check to all paths: rejected because only the source-bottom `>5` branch performs an explicit active-map null test.

## Evidence Standards Used

- Live IDA MCP: `idb_list`, `server_health`, bounded `get_bytes`, `lookup_funcs`, `insn_query`, `xref_query`, `find_bytes`, and `decompile`.
- Exact bytes: whole 564-byte target hash, boundary bytes, prologue/epilogue, alignment, return-pop, and duplicate-signature searches.
- Control flow and ABI: every decoded target instruction, stack slots, register roles, callees, indirect callback, and sibling decompilation.
- Cross-family evidence: RenderMonster body/caller, shared GetFrame helper, callback targets, ObjectStatusBlob declaration, EPFTileContext layout, ResourceLayout API, MapPane consumers.
- Documentation: current by-* pages, generated RidingImageLib.cpp, manual coverage, tracker, active and archived report roots.
- Negative evidence: no target function object, entry xref, VA/RVA pointer bytes, vtable cell, direct caller, duplicate exact RIDINGS sequence, or evidence for compiler/data classification.
- The evidence ladder reaches direct binary proof for behavior/ABI and convergent high-confidence inference for source name/access. Original symbol spelling remains the only material uncertainty.

## Evidence Checked

### IDA MCP/manual-disassembly/raw-byte checks performed

- Fresh `idb_list` discovered database `fa658e1e`, `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, active worker PID `17316`.
- At `2026-07-14T19:36:16-04:00`, `server_health(database=fa658e1e)` returned `status:ok`, module `NexusTK.exe`, image base `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, cache size `2067`.
- Bounded target reads succeeded. Whole target `[0x004dc7e0,0x004dca14)` is 564 bytes and SHA256 `0BF8FB87444BB45BAAB43BA1FA3DF6268EA666AD6E353D0450CBF29D524D6EDD`.
- `insn_query` decoded `169/169` instructions, untruncated, ending in cookie check and `retn 0x14`.
- `lookup_funcs`: predecessor `0x004dc730` size `0xa8`; target start/end not functions; successor `0x004dca20` size `0x23`; sibling `0x004db100` size `0x229`; caller `0x0053bd20` size `0x91`; GetFrame `0x00528b10` size `0x34`.
- `xref_query`: target start zero; sibling one code caller at `0x0053bda8`; callback slot 21 refs including sibling `0x004db310` and target `0x004dc9fb`.
- Distinctive target RIDINGS lookup signature beginning at `0x004dc875` has one match. The common definition-selection signature has exactly two matches, target `0x004dc814` and sibling `0x004db134`.
- Direct VA/RVA byte patterns for target entry have zero matches, corroborating no surviving pointer route without changing source disposition.
- Compat `0x004bdcc0` and RGB565 `0x004c3a50` callback targets independently decompile to the same void receiver/eight-argument signature.
- MapPane helper `0x0050a8b0` independently writes float fields `+0x3d0/+0x3d4/+0x3d8`, with constants `2.0f` and approximately `0.8f`; target/callback consumers establish their tint/projection use.
- `tools/int_convert.py` verified `564 == 0x234`, 8-byte predecessor padding, and 12-byte successor padding.

### Documentation and generated artifacts checked

- Current target, UID00017F aggregate, UID0000BZ class, UID0000N6 file, UID0001VV RidingDefinition, UID0001CM shared tables, UID00009S ObjectStatusBlob, UID0003NV caller, UID0000TN/UID0001PI callback pages, UID00004I EPFTileContext, UID0002KP lookup, UID00007Q MapPane, and relevant globals.
- Historical pre-callback generated `auto-generated/NexusTK/render/RidingImageLib.cpp`, header command `000000012123`, refreshed `2026-07-14T19:34:31-04:00`, contained class/parser/constructor/destructor/type/global output but no UID0002Y1 body because the target emitter was blank. B003's waited command `000000012200` superseded that baseline with one target definition and zero Empty Emitter Markers; latest observed validator-owned command `000000012202` preserves that result.
- Manual by-memory/class/file/type/global coverage rows and tracker entries were read during research; exact supervisor-owned text for the implemented ordinary state is supplied below without asserting external application/lifecycle state.
- All specified active, executed, archived, Older-Research, and SpecialReports roots were searched as recorded under Supporting Research.
- Callback verify-only reread found a direct contradiction in UID00017G and a dated-but-unlabeled equivalent in UID00017F: both described the now-registered/renamed UID0002Y1 child as pending registration. The permitted bounded current/history corrections completed under validators `12198` and `12195`; UID00017G score, metadata, destructor formal, range, padding, behavior, and route remain verify-only and unchanged.

### Negative checks

- No target function model, direct xref, entry-pointer byte, target vtable slot, modeled direct caller, interior jump-table ownership, fallthrough, exception-only wrapper shape, or raw-data interpretation.
- No exact target-specific RIDINGS lookup duplicate beyond target itself.
- No evidence that the raw target belongs to MonsterImageLib, NewHumanImageLib, Surface, MapPane, or ResourceLayoutTable.
- No source reason to hand-write stack cookies, callback target selection, vtable/RTTI, or raw global addresses.

### Failed, unavailable, or intentionally skipped checks

- IDA was not mutated to create a function or apply types; safe read-only alternatives were sufficient.
- No source symbols/PDB survive, so exact original method and private-field spellings remain inferred.
- During the historical report-only evidence pass, no ordinary validators or lifecycle commands were run. The separately authorized callback validators are recorded below; B003 still ran no lifecycle command.

## Claim And Incorporation Ledger

| Claim ID | Claim | Confidence | Evidence | Destination doc/section | Action | Verification state | Implementation proof |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C01 | Healthy mandatory MCP evidence was collected from database `fa658e1e`. | Very high | Fresh idb_list, health, bytes, functions, instructions, xrefs, decompiles. | Target `IDA MCP Evidence`; report Evidence Checked. | incorporate | applied | UID0002Y1 incorporates the evidence-time session, health, bounded-read, function/instruction/xref/decompile facts; scoped validator `12169` returned exit 0/ok 1. The evidence is durable to collection time and does not assert later MCP availability. |
| C02 | Exact target is `[0x004dc7e0,0x004dca14)`, `0x234`/564 bytes, SHA256 `0BF8...6EDD`. | Very high | Whole MCP byte read and int conversion. | Target Address Range/Hash. | incorporate | applied | UID0002Y1 records exact half-open range, hexadecimal/decimal size, whole-body hash, boundary bytes, and alignment; validator `12169` passed. |
| C03 | ABI is void thiscall with RidingImageLib ECX and five stack args: GrafPort, RectBounds, ObjectStatusBlob, group, frame. | Very high | Stack-slot data flow, `retn 0x14`, sibling/caller. | Target ABI; R1; R2. | incorporate | applied | UID0002Y1 ABI/body and UID0000BZ declaration carry the exact five-argument order and void return; validators `12169`/`12177` passed and generated command `12200` emits one matching definition. |
| C04 | Target is retained `RidingImageLib::RenderRiding`, source sibling of RenderMonster. | High | Two-match common signature, full CFG equivalence, RIDINGS-specific lookup. | Target Status/Source Disposition; class/file inventories. | incorporate | applied | Renamed UID0002Y1, UID00017F, UID0000BZ, and UID0000N6 record retained member/source-sibling disposition; validators `12169`, `12172`/`12195`, `12177`, and `12182`/`12199` passed. |
| C05 | Owner UID0000BZ and file UID0000N6 remain exact; shared table file is dependency only. | Very high | Receiver fields, resource literal, current class/file structure. | Target/class/file/parent. | already-present | already-present | UID0002Y1 preserves owner/emitter UID0000BZ and UID0000N6 remains the render source route; UID0001CM reread hash `BE77165EA411C09753639DE3A2FB29E926E35073D06DD3359C04EE75D3BFCB9E` confirms shared implementation remains verify-only. |
| C06 | Zero entry refs is negative reachability, not no-source proof; historical B008 no-code conclusion is stale. | Very high | Live xrefs plus complete body/sibling ABI. | Target history; parent/class/file. | reject-stale | applied | Target/class/file/parent preserve zero-route evidence and explicitly supersede the no-code conclusion; validators `12169`, `12172`/`12195`, `12177`, and `12182`/`12199` passed. |
| C07 | UID-preserving rename to `RidingImageLibRenderRiding` is source-quality correct. | High | Accepted RenderMonster naming parallel and member behavior. | Target path/title and all ordinary links. | incorporate | applied | Validator `12169` recorded `path_update` for UID0002Y1 and updated 15 UID links; current path exists, old path is absent, and later ordinary-page searches find the old name only in explicitly superseded history. |
| C08 | Definition index, 12-byte row selection, first GetFrame, frameOffset, and negative-frame exit are exact. | Very high | Target instructions and current types/helper. | Target behavior/type evidence; R1/R3. | incorporate | applied | UID0002Y1 and UID0001VV carry the exact selection/helper/frame arithmetic; validators `12169`/`12184` passed and R1/R3 hashes match accepted report blocks. |
| C09 | Duplicate discarded GetFrame call is real in both sibling bodies and must remain. | Very high | Target `0x4dc834/0x4dc865`; sibling decompile. | Target behavior/negative alternatives; R1. | incorporate | applied | UID0002Y1 prose and exact R1 retain both calls and the non-purity rationale; validator `12169` passed and generated command `12200` retains the accepted body exactly once. |
| C10 | EPFTileContext initialization and signed-low-16 RIDINGS.EPF lookup are exact. | Very high | Calls `0x457a60`, `0x4d02f0`, literal, `movsx di`. | Target behavior; R1; file. | incorporate | applied | Target and by-file evidence plus exact R1 preserve initialization, literal, signed-low-16 index, and route; validators `12169`, `12182`, and `12199` passed. |
| C11 | Source/destination rectangle construction preserves exact left/top and source width/height arithmetic. | Very high | Instruction-by-instruction local mapping; EPFTileContext/RectBounds docs. | Target rectangle table; R1. | incorporate | applied | UID0002Y1 contains the exact rectangle table and R1 arithmetic; validator `12169` passed and generated command `12200` emits R1 once. |
| C12 | Draw gate, thresholds, offset branches, centered suppression, clipping branch, and explicit branch-local null test are exact. | Very high | Full 169-instruction CFG and sibling equivalence. | Target behavior/branch table; R1. | incorporate | applied | UID0002Y1 branch table/negative alternatives and exact R1 preserve all counterintuitive branches; validator `12169` and generated command `12200` passed. |
| C13 | MapPane `+0x3d0/+0x3d4/+0x3d8` are callback tint/projection inputs; use source aliases, not offsets. | High | Target loads, both callback targets, MapPane/NewHuman support. | Target dependencies; MapPane support prose. | incorporate | applied | UID0002Y1 uses source aliases and UID00007Q records bounded offset/consumer/access evidence at unchanged score/route/formal; validators `12169`/`12187` passed. |
| C14 | Slot 4 is void scaled RLE tint callback with exact receiver/eight stack args. | Very high | Both callback target decompiles and 21 slot xrefs. | Target callback table; Surface support. | incorporate | applied | UID0002Y1, UID0000TN, and UID0001PI carry exact signature/consumer prose; validators `12169`, `12189`, and `12192` passed with support metadata/formals unchanged. |
| C15 | Source return is void; cookie/SSE/register/stack staging are compiler lowering. | High | Target exits, void sibling caller, callback return ignored. | Target ABI/compiler exclusions; R1. | reject-invalid | applied | UID0002Y1/R1 use void and exclude cookies/register/SSE/stack staging; generated command `12200` has no handwritten cookie/EH body. |
| C16 | Preserve 8-byte pre-pad, 12-byte post-pad, predecessor/successor boundaries, no split/merge, Nested 0. | Very high | Bounded bytes and function lookup. | Target range; UID00017F inventory. | incorporate | applied | Target/parent preserve both parent-only pads, exact neighbors, one-body range, and Nested 0; validators `12169`, `12172`, and `12195` passed. |
| C17 | Target becomes `92/94`, owner/emitter 0000BZ, true, blank position, Nested 0, with exact R1. | High | All resolved evidence and source-ready body. | Target metadata/formal. | incorporate | applied | UID0002Y1 current metadata and exact R1 validated by `12169`; normalized managed-block SHA256 is `186389CFEBB8BF4E5BB1BEB3716C6EF330D7D5B6B906E752F127BB5FBC490871`. |
| C18 | Class becomes `91/92` with R2; RidingDefinition becomes `90/92` with R3 shared GetFrame declaration. | High | Complete ABI and source/type blocker closure. | UID0000BZ and UID0001VV. | incorporate | applied | UID0000BZ validator `12177` and UID0001VV validator `12184` passed; normalized R2/R3 hashes are `B2CA1023E7202567223F01DE5A115F12D913B46BB3B8E6B4DB8E7D03F180BD68` and `7641B4B6AAD18D899ED1E7B53EBC4BC41B2A83D8629D347030DFEA1718640DE2`. |
| C19 | Parent/file/support docs must historicalize no-code text and preserve all unrelated lifecycle/compiler facts. | Very high | Pre-callback stale prose and bounded support audit. | UID00017F, UID0000N6, MapPane, Surface. | historicalize | applied | Parent validators `12172`/`12195`, file validators `12182`/`12199`, and bounded contradiction correction UID00017G validator `12198` make registration wording explicitly historical/current; support validators `12187`/`12189`/`12192` preserve unrelated content. |
| C20 | Manual coverage has exact target/class/file/type and bounded support replacement text; generated/tracker files stay validator-owned. | Very high | Manual rows read directly during research; implemented ordinary state rechecked. | Supervisor-owned coverage text below. | incorporate | excluded-with-reason | Exact supervisor-owned row text remains below. B003 made no manual coverage/tracker edit; validator `12169` automatically propagated the renamed link as tool-owned side effect, and final generated refresh `12200` completed without manual generated edits. |

## Positive Evidence Summary

- Standalone source body: normal prologue, 100-byte local frame, security cookie, 169 valid instructions, complete branch CFG, cookie check, `retn 0x14`, and alignment on both sides.
- Exact source sibling: target and RenderMonster share definition selection, two GetFrame calls, flag use, frame arithmetic, destination math, group gate, thresholds, offsets, MapPane values, and slot-4 dispatch.
- Exact riding specialization: the target alone calls the shared resource layout API with literal `RIDINGS.EPF`.
- Exact class receiver: `this+0x04/+0x08` are RidingImageLib's definition vector/count.
- Exact types: current ObjectStatusBlob, RidingDefinition/ImageAnimation records, EPFTileContext, RectBounds, ResourceLayoutTable, and Surface callback evidence close every C++ blocker.
- The strongest inference chain is RenderMonster source call shape -> byte-equivalent target ABI/control flow -> RidingImageLib fields/literal -> `RenderRiding` member. This is sufficient despite route loss.

## IDA MCP Facts

### Function/range facts

- Target start/end are not IDA functions; target nevertheless decodes exactly 169 instructions across 564 bytes.
- Predecessor: `sub_4DC730`, `0xa8`, ending `0x004dc7d8`.
- Successor: `sub_4DCA20`, `0x23`, starting `0x004dca20`.
- Source sibling: `sub_4DB100`, `0x229`, modeled and called once by `sub_53BD20`.
- Shared GetFrame: `sub_528B10`, `0x34`.

### Data/table/padding facts

- `0x004dc7d8-0x004dc7e0`: eight `0xcc` bytes.
- `0x004dca14-0x004dca20`: twelve `0xcc` bytes.
- RIDINGS-specific byte sequence at `0x004dc875` is unique.
- Whole target hash and exact byte count are recorded in C02.

### Xref facts

- Target entry: zero direct code/data refs.
- RenderMonster: one code ref from LivingObjectPane DrawSpriteFrame.
- Callback slot: 21 refs; target and sibling are distinct consumers.
- Shared definition-selection signature: exactly target and sibling.

### Vtable/global/type facts

- Target is not present in the RidingImageLib vtable; retained nonvirtual member is the best source shape.
- `g_pEPFLib` supplies the ResourceLayoutTable/ImageLib facet.
- `g_activeMapPane` supplies three rendering values.
- `g_pfnBlitScaledRleTint` is a runtime-selected typed global callback, not a target-owned function.

### Negative IDA facts

- No direct target route, vtable cell, pointer byte, function model, interior owner, or exact RIDINGS duplicate.
- These facts cap naming/reachability confidence but do not support data/compiler/no-source classification.

## Function / Child Inventory

| Range / Item | UID / Path | Role | Reconstructable | Direct Parent | Score | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `0x004dc730-0x004dc7d8` | UID00017G RidingImageLibDestructor | preceding ordinary destructor | true | UID0000BZ | `87/91` | verify-only metadata/body; bounded stale registration history corrected |
| `0x004dc7d8-0x004dc7e0` | parent-only alignment | eight `0xcc` | no source | UID00017F | n/a | preserve |
| `0x004dc7e0-0x004dca14` | UID0002Y1 `RidingImageLibRenderRiding` | retained source member | true | UID0000BZ | `92/94` | implemented/validated/emitted |
| `0x004dca14-0x004dca20` | parent-only alignment | twelve `0xcc` | no source | UID00017F | n/a | preserve |
| `0x004dca20-0x004dca43` | existing modeled successor | `GetDefinitionOrDefault` | source support | RidingImageLib family | existing | unchanged |
| `0x004db100-0x004db329` | MonsterImageLib modeled sibling | `RenderMonster` | source support | MonsterImageLib | existing | verify-only |
| `0x00528b10-0x00528b44` | UID0001CM child helper | `ImageAnimationTable::GetFrame` | true | UID0000LK | `90/90` aggregate | unchanged |

## Direct Xref / Caller Inventory

| Address / Item | Xref / Caller / Callee | Meaning |
| --- | --- | --- |
| `0x004dc7e0` | zero inbound xrefs | retained/unrouted body in this build |
| `0x004db100` | `0x0053bda8` in UID0003NV | source argument-order and return-use evidence |
| `0x004dc834`, `0x004dc865` | `sub_528B10` twice | first frame record used, second call discarded |
| `0x004dc878` | EPFTileContext Initialize | exact local type |
| `0x004dc890` | ResourceLayoutTable LookupLayoutEntry | RIDINGS.EPF frame context |
| `0x004dc8f2`, `0x004dc930` | OffsetRect | branch-specific destination adjustment |
| `0x004dc9fb` | indirect `g_pfnBlitScaledRleTint` | final composition dispatch |
| `0x004dca09` | security cookie check | compiler lowering, source exclusion |

## Documentation Evidence And IDA Status

- Historical pre-callback target/B008 state correctly classified source authorship and broad behavior but held the now-superseded no-emitter conclusion.
- Historical generated command `000000012123` contained no UID0002Y1 marker/body because the target emitter was blank. B003's waited command `000000012200`, refreshed `2026-07-14T20:29:58-04:00`, emitted one UID0002Y1 definition, no target/aggregate Empty Emitter Marker, no stale raw-helper name, and no duplicate/wrong-range body. Latest observed validator-owned command `000000012202`, refreshed `2026-07-14T20:32:35-04:00`, preserves the same assertions.
- UID0001VV now exposes the exact riding fields plus R3 `GetFrame` declaration while shared implementation ownership remains UID0001CM.
- UID0000BZ now contains R2 with `RenderRiding`; the class closes before generated qualified definitions and preserves constructor/destructor/parser fields plus `[[CHILDREN]]` placement.
- UID00007Q now records bounded render-context proof that `+0x3d0` is consumed as float tint and `+0x3d4/+0x3d8` as projection divisor/scale and shear, with unchanged `89/89` metadata and blank formal disposition.
- UID0000TN/UID0001PI now retain their existing scores/routes/formals while recording the exact slot-4 signature and target/sibling consumers.
- Manual coverage remains supervisor-owned. The exact replacement text below reflects implemented ordinary-page state; B003 did not edit manual coverage.

## Ranked Ownership Analysis

### 1. UID0000BZ RidingImageLib member in UID0000N6

- Evidence for: thiscall receiver fields, exact class layout, riding resource literal, neighboring RidingImageLib lifecycle, RenderMonster source parallel, and current source-tree route.
- Evidence against: no direct target caller or symbol and no vtable cell.
- Decision: accept retained nonvirtual `RenderRiding` member. Negative route evidence caps name certainty only.

### 2. File-local RidingImageLib.cpp helper

- Evidence for: retained unreferenced body and no vtable slot.
- Evidence against: ECX is an object receiver and class fields are directly consumed; source sibling is a member called through a singleton.
- Decision: reject as weaker than member source.

### 3. Shared MonsterImageLibTables/Surface/ResourceLayout owner

- Evidence for: shared GetFrame, resource lookup, and callback dependencies.
- Evidence against: none owns the receiver or RIDINGS-specific policy; each is a callee/dependency.
- Decision: reject ownership change. Preserve support links only.

### Proposed new file/grouping, if applicable

- Not applicable. Keep existing `NexusTK/render/RidingImageLib.cpp` and UID00017F exact range index.
- Do not create a raw-helper file or move the target to MonsterImageLibTables.

## Source Placement

- Implemented placement: member definition in `NexusTK/render/RidingImageLib.cpp`, declaration in RidingImageLib class/header surface.
- The source root already owns RIDINGS.DNA parsing, RIDINGS.EPF package lifetime, singleton, definitions, constructor, and destructor.
- Rejected placements: MonsterImageLib.cpp (wrong receiver/resource), MonsterImageLibTables.cpp (shared helper only), Surface.cpp (callback only), ResourceLayoutTable/ImageLib.cpp (lookup only), MapPane.cpp (runtime parameters only), LivingObjectPane.cpp (Monster caller only).
- Remaining placement uncertainty: exact original header/file casing is not symbol-proven; current project route is coherent and unchanged.

## Range / Split / Padding / Reclassification Analysis

- Exact target range: `[0x004dc7e0,0x004dca14)`, 564/`0x234` bytes.
- Exact predecessor padding: `[0x004dc7d8,0x004dc7e0)`, eight `0xcc`, parent-only.
- Exact successor padding: `[0x004dca14,0x004dca20)`, twelve `0xcc`, parent-only.
- Predecessor and successor remain separate ordinary functions. No byte of either pad belongs in UID0002Y1.
- No interior split: all 169 instructions form one cookie-protected body and all branches converge on its epilogue.
- Reclassification: title/source disposition changes from raw helper with withheld source to retained source member. `RECONSTRUCTABLE` remains true.
- Relative nesting: UID0002Y1 remains `Nested:0`; no child creation changes cumulative levels.
- Parent UID00017F remains non-emitting at its unchanged score/nesting; its stale child wording/link is replaced with the current UID0002Y1 identity while all other inventory facts remain intact.

## Negative Evidence Summary

- Zero target-entry xrefs do not prove dead compiler output; they prove only that the retained member has no surviving direct route in this executable.
- The common prologue is not unique, but the definition-selection sequence has exactly target/sibling matches and the RIDINGS lookup sequence is unique.
- The callback slot has broad fanout, so it is a dependency rather than owner evidence.
- The RenderMonster caller proves ABI/source factorization, not a direct target caller.
- Nearby destructor/successor addresses do not imply merge because both alignment spans are exact.
- `defaultBucketIndex` acting as a render flag does not prove the original field spelling; R1 uses a local role name and preserves the underlying current field.
- The MapPane fields have day/night and projection consumers. This report recommends contextual aliases, not duplicate storage.
- No evidence supports an integer source return, packed/raw types, defensive behavior not in the binary, or hand-authored compiler machinery.

## IDA Rename / Type / Comment Recommendations

- Source-facing target name: `RidingImageLib::RenderRiding`; confidence high by RenderMonster parallel.
- UID-preserving documentation rename: `RidingImageLibDrawRidingFrameRawHelper` -> `RidingImageLibRenderRiding`.
- Signature: void member with `GrafPort *target`, `const RectBounds *bounds`, `const ObjectStatusBlob *objectStatus`, unsigned animation group, unsigned animation frame.
- Type resolution: `objectStatus->m_appearanceId`; `RidingDefinition`; `RidingBucketEntry`/`ImageAnimationFrame`; `EPFTileContext`; `RectBounds`; `MapPane`; typed slot-4 callback.
- RidingDefinition support: add `RidingBucketEntry *GetFrame(unsigned int groupIndex, unsigned int frameIndex);` declaration; shared body stays UID0001CM.
- MapPane contextual aliases: `m_dayNightTintAmount` for `+0x3d0`, accepted `m_newHumanProjectionDivisor` for `+0x3d4`, and accepted `m_newHumanProjectionShear` for `+0x3d8`. Preserve day/night history and exact offsets in prose, not R1 comments.
- MapPane access: document these as public render-state fields for source reconstruction. Direct reads from three unrelated image-library classes reject protected-only access; multiple friend declarations or invented out-of-line getters are weaker than the observed source shape.
- IDA DB edits are not requested. No function creation, rename, prototype, type, or comment was applied.

## First-Draft C++ Recommendation

- Eligible for draft C++: yes. The ABI, body, support types, helpers, source owner, and source placement are complete.
- R1 is the exact managed target destination. R2 is the exact complete class destination. R3 is the exact complete RidingDefinition destination.
- The code preserves source order, duplicate GetFrame, signed-low-16 resource index, all thresholds, destination/source mutations, branch-local null test, and callback values.
- It excludes cookies, raw addresses, decompiler labels, explicit register behavior, vtable/RTTI, and callback initializer machinery.
- Exact original method and some field spellings remain inferred but are source-quality descriptive names, not blockers.
- Third-party import: not applicable; this is NexusTK project source.

### R1 - UID0002Y1 renamed target

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RidingImageLib::RenderRiding(
    GrafPort *target,
    const RectBounds *bounds,
    const ObjectStatusBlob *objectStatus,
    unsigned int animationGroup,
    unsigned int animationFrame)
{
    const unsigned int definitionIndex = static_cast<unsigned short>(
        objectStatus->m_appearanceId + 0x8000);
    if (definitionIndex >= ridingDefinitionCount)
        return;

    RidingDefinition *definition = ridingDefinitions + definitionIndex;
    RidingBucketEntry *frameEntry =
        definition->GetFrame(animationGroup, animationFrame);
    const int frameIndex =
        static_cast<int>(definition->ridingId) + frameEntry->frameOffset;
    const bool useCenteredProjection =
        definition->defaultBucketIndex != 0;

    definition->GetFrame(animationGroup, animationFrame);

    if (frameIndex < 0)
        return;

    EPFTileContext tileContext;
    tileContext.Initialize();
    g_pEPFLib->LookupLayoutEntry(
        L"RIDINGS.EPF",
        static_cast<short>(frameIndex),
        &tileContext);

    RectBounds sourceBounds = tileContext.bounds;
    RectBounds destinationBounds;
    destinationBounds.left = bounds->left;
    destinationBounds.top = bounds->top;
    destinationBounds.right =
        bounds->left + sourceBounds.right - sourceBounds.left;
    destinationBounds.bottom =
        bounds->top + sourceBounds.bottom - sourceBounds.top;

    if (animationGroup == 0)
        return;

    MapPane *mapPane = g_activeMapPane;

    if (sourceBounds.bottom < -4)
    {
        if (useCenteredProjection)
        {
            const int sourceHeight =
                sourceBounds.bottom - sourceBounds.top;
            OffsetRect(
                &destinationBounds,
                0,
                sourceHeight / 2 - sourceBounds.bottom);

            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &sourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                1.5f,
                0.0f);
        }
        else
        {
            OffsetRect(&destinationBounds, 0, -sourceBounds.bottom);

            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &sourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                mapPane->m_newHumanProjectionDivisor,
                mapPane->m_newHumanProjectionShear);
        }
        return;
    }

    if (useCenteredProjection)
        return;

    if (sourceBounds.bottom > 5)
    {
        RectBounds clippedSourceBounds = sourceBounds;
        clippedSourceBounds.bottom = 0;
        destinationBounds.bottom -= sourceBounds.bottom;

        if (mapPane != NULL)
        {
            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &clippedSourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                mapPane->m_newHumanProjectionDivisor,
                mapPane->m_newHumanProjectionShear);
        }
        return;
    }

    g_pfnBlitScaledRleTint(
        target,
        &tileContext,
        &sourceBounds,
        &destinationBounds,
        3,
        mapPane->m_dayNightTintAmount,
        0,
        mapPane->m_newHumanProjectionDivisor,
        mapPane->m_newHumanProjectionShear);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### R2 - UID0000BZ RidingImageLib complete declaration

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct ArchiveMetadataTable;
struct ImageAnimationTable;
struct ImageAnimationGroup;
struct ImageAnimationFrame;
struct RidingDefinition;
struct ObjectStatusBlob;
struct RectBounds;
class GrafPort;

class RidingImageLib : public LObject
{
public:
    RidingImageLib();
    virtual ~RidingImageLib();

    void LoadRidingDefinitions();
    void RenderRiding(
        GrafPort *target,
        const RectBounds *bounds,
        const ObjectStatusBlob *objectStatus,
        unsigned int animationGroup,
        unsigned int animationFrame);
    const RidingDefinition *GetDefinitionOrDefault(
        unsigned int ridingDefinitionIndex) const;

private:
    RidingDefinition *ridingDefinitions;
    unsigned int ridingDefinitionCount;
    ArchiveMetadataTable *ridingEpfPackage;
};

extern RidingImageLib *g_pRidingImageLib;
extern ImageAnimationTable g_ridingDefinitionGroups;

[[CHILDREN]]
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

### R3 - UID0001VV RidingDefinition complete declaration

```cpp
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
typedef ImageAnimationGroup RidingBucket;
typedef ImageAnimationFrame RidingBucketEntry;

struct RidingDefinition
{
    unsigned int ridingId;
    unsigned char bucketCount;
    unsigned char defaultBucketIndex;
    unsigned short animationGroupId;
    RidingBucket *buckets;

    RidingBucketEntry *GetFrame(
        unsigned int groupIndex,
        unsigned int frameIndex);
};
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
```

## Final Recommendation

- R1 is applied to validator-renamed UID0002Y1 and emits through UID0000BZ; R2 is applied to UID0000BZ and R3 to UID0001VV.
- Owner UID0000BZ, exact range, true reconstructable state, blank optional position, and `Nested:0` are preserved.
- Parent UID00017F, predecessor/successor functions, both parent-only padding spans, helper ownership, callback/global ownership, and all unrelated source/compiler facts are preserved.
- B008's no-route no-code decision is historicalized without deleting its valid observations.
- No item remains no-owner or non-emitting inside the target. Shared helper/callback/compiler destinations retain their existing independent dispositions.
- External supervisor/validator owns report validation, callback authorization, execution, count, path, move, and archive state; this artifact neither asserts nor directs those lifecycle outcomes.

## Recommended Target Doc Changes

- Implemented validator-preserving rename: UID0002Y1 is now `by-memory/0x004dc7e0-0x004dca14.RidingImageLibRenderRiding.md` with title `RidingImageLib RenderRiding`; old path absent.
- Implemented metadata: `86/90 -> 92/94`; owner remains `0000BZ`; reconstructable remains true; emitter is `0000BZ`; optional position blank; Nested remains 0.
- Item Summary: `Retained source-authored RidingImageLib::RenderRiding member with exact five-argument ABI, definition/frame lookup, RIDINGS.EPF context load, branch-exact rectangle/projection math, and scaled RLE tint callback dispatch.`
- R1 is inserted exactly; normalized block SHA256 is `186389CFEBB8BF4E5BB1BEB3716C6EF330D7D5B6B906E752F127BB5FBC490871`.
- Exact range/size/hash, ABI table, definition/frame table, resource context, rectangle arithmetic, branch table, callback signature, sibling factorization, padding, negative routes, compiler exclusions, source placement, name/type caveats, and score rationale are incorporated at report-level depth.
- The old path/title, no-function/no-xref facts, and B008 conclusion remain only in explicitly historical/superseded prose.

## Recommended Support Doc Changes

- UID00017F `by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md`: stale child link/name/disposition is replaced by the renamed source member; `87/91`, non-emitting parent, range, child order, padding, and unrelated siblings remain intact. A dated registration blocker is explicitly historical after bounded validator `12195` correction.
- UID0000BZ `by-class/RidingImageLib.md`: implemented `89/90 -> 91/92`, exact R2, method inventory, ABI, retained-route caveat, source sibling, callback/resource dependencies, and historicalized no-code decision; existing fields/lifecycle/globals remain and class closes before `[[CHILDREN]]`.
- UID0000N6 `by-file/RidingImageLib.md`: implemented `90/90 -> 91/92`, renamed inventory link, retained source-ready member, RIDINGS.EPF/dependency split, and preserved parser/constructor/destructor/global/compiler history. A dated pending-registration statement is explicitly historical after validator `12199` correction.
- UID0001VV `by-type/by-struct/RidingDefinition.md`: implemented `88/90 -> 90/92`, exact R3, target call evidence, and shared-body ownership without duplicating UID0001CM.
- UID00007Q `by-class/MapPane.md`: score/route/formal remain unchanged; bounded tint/projection consumer and public render-state access evidence are incorporated while preserving day/night setter evidence and original-spelling caveat.
- UID0000TN `by-global/SurfaceRenderCallbackTable.md` and UID0001PI storage page: scores/routes/formals remain unchanged; exact void GrafPort/EPFTileContext/rectangle/mode/tint/row-offset/vertical-scale/shear signature and target/sibling consumers are incorporated.
- Verify-only pages were reread and left unchanged. Direct contradiction exception was used only for UID00017G's stale pending-registration/current-name prose; its score, metadata, destructor formal, range, padding, behavior, and route are unchanged.

## Score And Metadata Recommendation

| Destination | Pre-callback | Implemented | Rationale |
| --- | --- | --- | --- |
| UID0002Y1 target | `86/90`, blank emitter/formal | `92/94`, emitter 0000BZ, R1 | Complete body, ABI, sibling source identity, support types, callback, source route, hash, padding, and negatives are resolved. |
| UID0000BZ class | `89/90` | `91/92` with R2 | Adds complete missing member declaration and closes retained-family blocker. |
| UID0000N6 file | `90/90` | `91/92` | Source inventory and generated route become complete for this method. |
| UID0001VV type | `88/90` | `90/92` with R3 | Exact GetFrame consumer closes the declaration mismatch while shared implementation remains correctly routed. |
| UID00017F parent | `87/91` | unchanged | Exact split/index already complete; only child disposition/link changes. |
| UID00007Q MapPane | `89/89` | unchanged | Bounded aliases improve evidence but do not close the full class declaration debt. |
| UID0000TN callback global | `89/86` | unchanged | Slot 4 becomes exact, but the twelve-slot aggregate still has broader signature/name caps. |

- Score-improvement attempt: direct caller search stayed negative; full raw decode recovered ABI; sibling body/caller recovered source signature; callback targets recovered parameter types; ObjectStatusBlob closed field type; RidingDefinition R3 closes helper call; MapPane/callback decomp closes float roles; duplicate and range scans close source disposition.
- Reason not higher: original `RenderRiding` spelling, access level, retained-source reason, and final MapPane field spellings are inferred; target has no direct surviving route.
- Metadata unchanged where not explicitly listed: exact owner, reconstructable state, position, Nested, source route, parent ownership, and helper/global routes.

## Open Questions With Attempted Resolution

1. Exact original method name/access:
   - Checked target route, vtable, neighboring class methods, report corpus, RenderMonster caller/formals, source tree, and generated output.
   - Resolution: `RenderRiding` public member is best current source name/access by direct RenderMonster parallel. Original symbol is unavailable; confidence cap only.
2. Why target route is absent:
   - Checked xrefs, VA/RVA pointer bytes, vtable, duplicate bodies, caller families, and adjacent functions.
   - Resolution: retained/unreferenced source in this build. The exact reason (feature removal, dead branch, linker retention) is not binary-provable and does not affect emission.
3. RidingDefinition versus ImageAnimationTable source type:
   - Checked exact layouts, parser fields, helper implementation, target/sibling calls, global fallback table, and accepted B007 source.
   - Resolution: retain riding-specific declaration/aliases and declare `GetFrame`; shared implementation stays UID0001CM. No cast or duplicate raw helper is needed in R1.
4. Duplicate GetFrame intent:
   - Checked both target and sibling instruction sequences and helper purity.
   - Resolution: exact source likely retained an unused/redundant call; preserve it. Its original local-variable intent cannot be proven but behavior is exact.
5. MapPane field spellings:
   - Checked day/night setter, target/sibling consumers, NewHuman projection consumer, and both callback implementations.
   - Resolution: contextual aliases in R1 are source-safe. Three unrelated image-library classes directly load the same state, so public render fields are the best access model; protected access is impossible and per-class friendship is weaker. Exact original names remain inferred and cap confidence; raw offsets are not acceptable.
6. Callback declaration spelling/calling convention:
   - Checked initializer, both concrete targets, 21 consumers, and physical ECX/stack order.
   - Resolution: exact semantic signature is established; the original typedef spelling is not. Existing global alias remains best project name.
7. Source return:
   - Checked all target exits, sibling decompile, and sole sibling caller.
   - Resolution: void. Residual EAX is compiler/decompiler state and is never source-consumed.

No unresolved question blocks owner, emitter, rename, score, or R1-R3.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

B003 does not edit manual coverage or generated tracker files. The following exact supervisor-owned replacement text describes the implemented ordinary target/class/file/type and bounded support state. Validator command `12169` propagated the renamed UID link as an automatic validator side effect, but B003 did not manually apply or assert the complete manual-row/lifecycle state.

### `by-memory/-coverage-report.md`

Replace the current UID0002Y1 row with:

```text
        - [UID:0002Y1][0x004dc7e0-0x004dca14.RidingImageLibRenderRiding](by-memory/0x004dc7e0-0x004dca14.RidingImageLibRenderRiding.md) 0x004dc7e0-0x004dca14 | retained method | RidingImageLib::RenderRiding : reconstructable : 92% : very-strong : Retained source-authored RidingImageLib member with exact 0x234/564-byte cookie-protected body and five-argument thiscall ABI, RenderMonster source-sibling factorization, ObjectStatusBlob appearance selection, shared GetFrame calls including the preserved discarded duplicate, RIDINGS.EPF layout lookup, exact rectangle/projection thresholds and offsets, scaled-RLE-tint callback signature, parent-only alignment, RidingImageLib.cpp owner/emitter route, negative entry-route evidence, and complete first-draft C++.
```

### `by-class/-coverage-report.md`

Replace UID0000BZ with:

```text
- [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md) : reconstructable : 91% : very-strong : Riding/mount image-library class at 91/92 with complete constructor/destructor/parser/RenderRiding declaration and generated route; exact 0x10 layout, RIDINGS.DNA/RIDINGS.EPF ownership, definition/global/singleton lifecycle, retained UID0002Y1 five-argument RenderMonster-sibling body, shared animation-table dependency, compiler/vtable exclusions, historical no-route correction, and complete class-before-children formal declaration are documented.
```

Replace UID00007Q only if its bounded support prose is incorporated into the manual row:

```text
- [UID:00007Q][MapPane](by-class/MapPane.md) : reconstructable : 89% : very-strong : Live game-world pane class at unchanged 89/89 score/route and blank complete-class formal disposition, with source-ready compressed-map load/save methods, exact private tile-buffer allocation contract, canonical map id/width/height/tile fields and six-byte tile type, exact direct-CRT save failure/disposal/monster-cache behavior, packet/render/weather/object families, TimerPane dependency synchronization, and render-consumer proof that +0x3d0 supplies scaled-RLE tint while +0x3d4/+0x3d8 supply projection divisor/scale and horizontal shear.
```

### `by-file/-coverage-report.md`

Replace UID0000N6 with:

```text
- [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md) : reconstructable : 91% : very-strong : NexusTK/render/RidingImageLib.cpp source root at 91/92 with RIDINGS.DNA parser, RIDINGS.EPF package lifecycle, singleton/globals, constructor/ordinary destructor, retained source-ready UID0002Y1 RidingImageLib::RenderRiding, exact five-argument ABI and rectangle/projection/callback behavior, shared MonsterImageLibTables dependency, compiler-wrapper/vtable exclusions, corrected no-route history, and generated one-definition route documented.
```

### `by-type/by-struct/-coverage-report.md`

Replace UID0001VV with:

```text
- [UID:0001VV][RidingDefinition](by-type/by-struct/RidingDefinition.md) : reconstructable : 90% : very-strong : RidingImageLib-owned 12-byte RIDINGS.DNA definition record at 90/92 with exact riding id/count/flag/table/pointer fields, 8-byte ImageAnimationGroup and 10-byte ImageAnimationFrame aliases, parser/default-table/destructor evidence, UID0002Y1 GetFrame/frameOffset consumer proof, complete GetFrame declaration, and preserved shared implementation ownership under UID0001CM MonsterImageLibTables.
```

### `by-global/-coverage-report.md`

Replace UID0000TN only if the bounded slot-4 support prose is incorporated into the manual row:

```text
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) : reconstructable : 89% : strong : Render callback table with storage, slots, initializer behavior, live per-slot xref/fanout evidence, Surface ownership, and exact slot-4 scaled-RLE-tint signature from compat/RGB565 targets and Monster/Riding consumers: void GrafPort receiver plus EPFTileContext, source/destination rectangles, mode, float tint, row offset, vertical scale, and horizontal shear; broader slot-name/typedef caveats remain.
```

- No exact text is written for validator-owned `auto-generated/-ag-*`; the implemented target Item Summary/Nested metadata and scoped validators own generated metadata refresh.
- Reason B003 must not apply these rows: manual coverage and tracker/lifecycle state are supervisor-owned by explicit assignment restriction.

## Follow-Up Actions

- External supervisor/validator owns exact-artifact validation, manual coverage application, execution, count, path, move, and archive state; this artifact neither asserts nor directs those lifecycle outcomes.
- B003's bounded C01-C20/R1-R3 implementation, serial ordinary-page validation, generated readback, report reconciliation, and lease release are complete; no B003 implementation item remains.
- No A-agent action is required by this report.
- No additional B003 research remains for UID0002Y1. The two direct stale-registration contradictions found during callback reread were corrected and validated as recorded below.

## Confidence

- Recommendation confidence: very strong (`94`) for behavior/source disposition; target score recommendation `92/94`.
- Score confidence: high. Every prior score-limiting blocker has a current evidence-backed resolution.
- Remaining uncertainty: original member/access spelling, retained-route cause, and exact MapPane field names. These are explicitly inferred and do not alter behavior or source placement.

## Validator Results

- Every changed ordinary page received a scoped `--mode file --apply --queue-timeout 240` validator from `source-3/project-documentation`; every listed command exited `0` with `ok: 1`.

| Command ID | Timestamp (`-04:00`) | Scoped path/result | Side effects and diagnostics |
| --- | --- | --- | --- |
| `000000012169` | `2026-07-14T20:19:38-04:00` | UID0002Y1 current renamed path; exit 0, ok 1 | Registered UID-preserving `path_update`, `92/94`, owner/emitter/formal metadata, autogen registry, 15 UID-link/reference updates; generated deferred. Three unrelated pre-existing manual by-memory `missing_ref_uid` diagnostics (`0003MT`, `00039L`, `0003T6`); validator automatically propagated the renamed manual link, not a B003 manual coverage edit. |
| `000000012172` | `2026-07-14T20:20:54-04:00` | UID00017F parent; exit 0, ok 1 | Parent source inventory/padding/history sync; generated deferred. |
| `000000012177` | `2026-07-14T20:21:50-04:00` | UID0000BZ class; exit 0, ok 1 | `91/92`, exact R2 and registry metadata; generated deferred. |
| `000000012182` | `2026-07-14T20:22:47-04:00` | UID0000N6 file; exit 0, ok 1 | `91/92`, source inventory/route/history sync; generated deferred. |
| `000000012184` | `2026-07-14T20:23:35-04:00` | UID0001VV type; exit 0, ok 1 | `90/92`, exact R3/shared-helper evidence; generated deferred. |
| `000000012187` | `2026-07-14T20:24:27-04:00` | UID00007Q MapPane; exit 0, ok 1 | Bounded render-state aliases/access evidence, score/formal unchanged; generated deferred. Seventy pre-existing broad-page `missing_ref_uid` diagnostics remain unrelated to this bounded edit. |
| `000000012189` | `2026-07-14T20:25:21-04:00` | UID0000TN callback global; exit 0, ok 1 | Exact slot-4 signature/consumers, score/formal unchanged; generated deferred. Three pre-existing UID0003XE reference diagnostics remain unrelated. |
| `000000012192` | `2026-07-14T20:26:06-04:00` | UID0001PI callback storage; exit 0, ok 1 | Exact slot-4 storage/signature/consumers, score/formal unchanged; generated deferred. Five pre-existing UID0003XE/UID0003PN reference diagnostics remain unrelated. |
| `000000012195` | `2026-07-14T20:27:05-04:00` | UID00017F bounded current/history correction; exit 0, ok 1 | Converted a dated pending-registration statement to explicit superseded history; generated deferred. |
| `000000012198` | `2026-07-14T20:28:52-04:00` | UID00017G bounded contradiction correction; exit 0, ok 1 | Current target link/history normalized; score/metadata/destructor formal/range/padding/behavior/route unchanged; generated deferred. |
| `000000012199` | `2026-07-14T20:29:53-04:00` | UID0000N6 bounded current/history correction; exit 0, ok 1 | Converted dated pending-registration wording to explicit historical/current truth; generated deferred. |
| `000000012200` | `2026-07-14T20:29:58-04:00` | final UID0000N6 `--wait-generated`; exit 0, ok 1 | Generated refresh completed. Project-wide fallback/missing-marker/no-code diagnostics concern unrelated emitters; no RidingImageLib target error was reported. |

- R1-R3 normalized exact-block comparison: R1 `186389CFEBB8BF4E5BB1BEB3716C6EF330D7D5B6B906E752F127BB5FBC490871`, R2 `B2CA1023E7202567223F01DE5A115F12D913B46BB3B8E6B4DB8E7D03F180BD68`, R3 `7641B4B6AAD18D899ED1E7B53EBC4BC41B2A83D8629D347030DFEA1718640DE2`; each destination block is byte-equal after newline normalization to its accepted report block.
- B003 waited generated result: header command `000000012200`, refreshed `2026-07-14T20:29:58-04:00`, observed SHA256 `FF1F3C6A856A47147F3A6AEB8F179E1B247EC7796AD803949A8236DE2B7C49AF`; all assertions below passed. Latest read-only generated state after concurrent validator-owned refresh: `auto-generated/NexusTK/render/RidingImageLib.cpp`, header command `000000012202`, refreshed `2026-07-14T20:32:35-04:00`, filesystem UTC `2026-07-15T00:32:46.7577711Z`, length 9785, SHA256 `6BF6264F0D14E32CD84AE786C0DA4CB3F08E37071D4DF111D0C4F6AEA81C239A`; the same structure/count assertions still pass.
- Generated structure/count proof: RidingImageLib class end offset `1167` precedes first qualified definition offset `1384`; UID0002Y1 marker 1; `void RidingImageLib::RenderRiding(` definition 1; parser definition 1; constructor definition 1; destructor definition 1; target Empty Emitter Marker 0; all Empty Emitter Markers 0; stale `RidingImageLibDrawRidingFrameRawHelper` 0; wrong endpoint `0x004dca13`/`0x004dca15` 0; handwritten vtable array 0; security-cookie/EH symbols 0. Compiler-owned vtable/scalar-wrapper facts occur only in no-code comments.
- Verify-only rereads remained unchanged: UID0001CM `by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md` (`BE77165EA411C09753639DE3A2FB29E926E35073D06DD3359C04EE75D3BFCB9E`); UID00009S `by-class/ObjectStatusBlob.md` (`110F06C13CB4F5006910B5A553C3E9DCE5A4901E7A14CF0F9C096502DE51D39E`); UID0003NV `by-memory/0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md` (`EC4C2097C2F92FC71DFCD4733958682F01E9817A083FB55AD5C562FF1F5149F7`); UID00004I `by-class/EPFTileContext.md` (`7EC740D8A5B1FBA0A74EF46A75FBD6B684408E3EC42A939E4A20ABE7D9347F28`); UID0002KP `by-memory/0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry.md` (`F5C3CC25ACC5616E03847265AFD0BAF5D4F4CD55EAE28FA4067F23A5ADCA8FB1`); UID0000PR `by-global/g_activeMapPane.md` (`28D1E5C381E323031B9AFDAFA62860141D4015974A0339819559E3AB70662AE4`); UID0001VW `by-type/by-struct/RidingImageLibLayout.md` (`5B029E25084441001A480E6BF4CAEAFA83573ACC56B23570FAC2824AFA67B50A`); UID0001YM `by-type/by-vtable/RidingImageLibVtable.md` (`4AD0F3E655BB752A6476E151FC99968EAF0966A1CE31D62B2EE767D2FEB2705F`); UID0000S4 `by-global/g_pRidingImageLib.md` (`9780C646960D3211D0FA44963D927D6604D7DD39F93E106CB45672E64171D19A`); UID0000TJ `by-global/RidingDefinitionGlobalTable.md` (`2182B5E390D5B2BC4D4CBB6E1A90A72359A3392CC656D053A1161564A2E914CC`); UID0001PL `by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md` (`D10D92E0909107758EB689BB7FFFE2A47AAC2E5E782746EF106ED1354FC8F450`); UID0001PO `by-memory/0x0069b444-0x0069b448.g_pRidingImageLib.md` (`834360637833EDB79CD6C20498B1E3FC4F859E70DF5C3F286FBC6D25A6B3BC0F`). UID00017G alone received the documented bounded contradiction correction.
- Unresolved implementation validator errors/blockers: none. All warnings above are preserved as pre-existing unrelated diagnostics.

## Changed Files

- Agent-owned report updated in place: `tools/leaser/Agents/Agent-B003/research/0002Y1-RidingImageLibDrawRidingFrameRawHelper-source-quality.md`.
- UID-preserving ordinary rename: old `by-memory/0x004dc7e0-0x004dca14.RidingImageLibDrawRidingFrameRawHelper.md` removed; current `by-memory/0x004dc7e0-0x004dca14.RidingImageLibRenderRiding.md` registered for UID0002Y1, current SHA256 `34CE795B80565FC1E6E0A0744A8AE39ECEE72A83B77CB93A1712123FAE5C6147`.
- Changed ordinary pages: UID00017F `by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md` (`5A244499FE19265684E294EAAE331D20BDC59B713CB1CF0E8F9183B08C2ED12B`); UID00017G `by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md` (`B688FF89D7304169E47ED386B23AFD83F0C68C27EDA17AC4F21300A775137235`); UID0000BZ `by-class/RidingImageLib.md` (`79C86E96A4C2C5F687A11C7B8E29E52D8F78651DB66EECA239835FDD0DDE1BB5`); UID0000N6 `by-file/RidingImageLib.md` (`B2F3A14D0581BB8313A5F2C3D52880EE99026EC4192B4DE0A02DD0BC1D458398`); UID0001VV `by-type/by-struct/RidingDefinition.md` (`FA3FEACE705F543169F453E2AA7E3179E423CF76FE0EBD8B3249B9E3371E7A99`); UID00007Q `by-class/MapPane.md` (`A43C83B84CD272BFFDE35BB6008FFBF72489C327F36BD89CADA7F8AE80B3D40D`); UID0000TN `by-global/SurfaceRenderCallbackTable.md` (`A15770ABDB6795AB9361FE96067B51269B5DE01992E83320810DFBAA2FAF0174`); UID0001PI `by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md` (`43BEF2F9BA32010FA62CC368437DAC542849F8AADEF9BB7EAEA2C02701CB41C4`).
- Verify-only ordinary pages: the twelve UID/path/hash rereads listed in Validator Results; no edits. RectBounds/OffsetRect and MonsterImageLib support were also reread without contradiction or edit.
- Validator-owned side effects: registered path/reference/metadata changes and generated refresh described above. B003 did not manually edit `auto-generated`, manual `-coverage-report.md`, tracker/audit/supervisor/validator-state/queue/lock/lifecycle/archive, or IDA files.
- Leases: each ordinary page was leased only for its active edit/validation and released before the next page. Final `current_leases.md` search contains no Agent-B003 lease.
- Report execution/lifecycle: B003 ran no execute_report or report execution/probe/count/revalidation/move/archive/lifecycle command. External lifecycle state is not asserted or directed here.

## Implementation Tracking Checklist

Initial report-only pass:
- [x] Supervisor Gate 1 accepted exact report SHA `A6BAF01CE4D871A98D3A2190EA9833BD5291D94E4A3D561DB5F377308B7E14AD` before implementation.
- [x] Target/support scope covered UID0002Y1, UID00017F, UID0000BZ, UID0000N6, UID0001VV, bounded UID00007Q and UID0000TN/UID0001PI; verify-only pages remained unchanged except documented UID00017G contradiction correction.
- [x] Current target state and actual evidence checked are recorded.
- [x] Claim And Incorporation Ledger now uses legal terminal actions/states with destination/validator/generated proof for C01-C20.
- [x] Implemented scores are target `92/94`, class `91/92`, file `91/92`, RidingDefinition `90/92`; bounded supports remain unchanged.
- [x] Score-limiting blockers were researched to resolution: complete ABI, source factorization, type/helper/global/callback names, source route, range, and C++ readiness.
- [x] Owner/emitter/reconstructable state is exact: owner/emitter UID0000BZ and target true.
- [x] UID-preserving target rename completed; no split/new child; Nested remains 0.
- [x] Source placement, range/split/padding/reclassification, and rename/type/comment changes are incorporated.
- [x] First-draft C++ R1-R3 are exact; stale no-code disposition is rejected and historicalized.
- [x] Third-party import directive is not applicable.
- [x] Exact target/support facts are incorporated at report-level detail.
- [x] Historical/stale assumptions, rejected alternatives, and negative evidence are preserved.
- [x] Wave2/Wave3 artifacts were ignored as stale and not used as evidence.
- [x] Open questions are closed to implementation-ready inferences with explicit confidence caps.
- [x] Validators ran serially after authorization and one-file lease acquisition.
- [x] Waited generated refresh passed; exact supervisor-owned manual coverage text is retained for external ownership and was not manually applied by B003.
- [x] C01-C20 use legal final states with exact destination, validator, and generated proof.

Implementation callback pass:
- [x] Exact report artifact was accepted by the supervisor for implementation.
- [x] Target was singly leased/reread, validator-aware UID-preserving renamed, updated with C01-C17/R1, validated by `12169`, and released.
- [x] UID00017F was singly leased, synchronized with preserved score/nesting/ranges/pads/siblings, validated by `12172` and bounded correction `12195`, and released.
- [x] UID0000BZ was singly leased, updated to `91/92` with exact R2/full evidence/history, validated by `12177`, and released.
- [x] UID0000N6 was singly leased, updated to `91/92` with source inventory/history/route, validated by `12182` and bounded correction `12199`, and released.
- [x] UID0001VV was singly leased, updated to `90/92` with exact R3/shared-helper ownership, validated by `12184`, and released.
- [x] UID00007Q was singly leased for bounded tint/projection/access evidence, preserved score/formal/unrelated content, validated by `12187`, and released.
- [x] UID0000TN and UID0001PI were leased serially, updated with exact slot-4 signature/consumers while preserving scores/formals/unrelated slots, validated by `12189`/`12192`, and released.
- [x] Verify-only dependencies were reread and left unchanged absent contradiction; UID00017G's exact stale registration contradiction alone was bounded-corrected under lease, validated by `12198`, and released.
- [x] Complete behavior is preserved: index wrap, two GetFrame calls, signed frame index, EPF lookup, rectangle arithmetic, group gate, thresholds, offsets, centered suppression, clipping, branch-local null test, callback values, and void return.
- [x] Negatives/history/compiler exclusions and rejected owners/names/types/routes are preserved without compression.
- [x] One scoped validator ran for every changed ordinary page; exact command IDs/timestamps/exits/ok/warnings/side effects are recorded above.
- [x] Final waited RidingImageLib refresh `12200` passed and exact generated header/hash/freshness/count/order assertions are recorded.
- [x] No Agent-B003 lease remains.
- [x] Exact supervisor-owned manual coverage text is retained; B003 never edited coverage directly.
- [x] Finalized/Current State, C01-C20, Validator Results, Changed Files, recommendations, and checklist reflect durable callback-complete truth.
- [x] B003 ran no execute_report or report execution/probe/count/revalidation/move/archive/lifecycle command.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B003","command_id":"000000012228","destination_path":"executed-b-agent-research/B003/0002Y1-RidingImageLibDrawRidingFrameRawHelper-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B003/research/0002Y1-RidingImageLibDrawRidingFrameRawHelper-source-quality.md","timestamp":"2026-07-14T20:51:40-04:00","uid":"0002Y1"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
