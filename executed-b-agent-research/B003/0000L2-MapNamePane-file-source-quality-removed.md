<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\Agent-B003\research\0000L2-MapNamePane-file-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Legacy Request Material: UID0000L2

Source report: [0000L2-MapNamePane-file-source-quality.md](0000L2-MapNamePane-file-source-quality.md)

> NON-AUTHORITATIVE ARCHIVE. This file contains removed machine-shaped MCP request material for audit provenance only and must never be executed, replayed, or submitted. Do not treat it as current research evidence. It receives no Gate 1, Gate 2, coverage, score, IDA, validator, or lifecycle credit.

## Removed Material

~~~text
This is a read-only structured handoff. Every mutation is supervisor-owned. The supervisor must obtain a fresh public `runtime_attestation`, use the database identifier returned for canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, reopen all literal prestates below, stop on any mismatch or collision, and perform no save unless every listed action and protection reads back exactly. The requests are represented as inert structured fields, not an executable transaction package.

### Type prerequisites

| Type | Current parser-visible state | Required declaration / invariant | Disposition and stop condition |
| --- | --- | --- | --- |
| `Pane` | Present, size `0xf8`; dated command `000000023008` generated-H snapshot exposes `explicit Pane(unsigned char mode)` | Physical base size remains `0xf8`; exact constructor declaration and every existing member remain unchanged; Gate-time physical reread determines current state | Protect; stop if size, constructor declaration, or existing members differ |
| `EPFTileContext` | Present, size `0x28` | Retain exact existing declaration and `RectBounds bounds` at `+0x10` | Protect; stop on mismatch |
| `Event` | Present, size `0x110` | Retain exact current Event layout; report changes declaration channel, not IDA layout | Protect; stop on mismatch |
| `MapNamePane` | Absent | Supervisor may declare an analysis-only physical UDT with `Pane base` at `+0x000`, `wchar_t m_mapNameText[128]` at `+0x0f8`, `EPFTileContext m_mapNameImage` at `+0x1f8`, and `unsigned char *m_mapNameGlyphs` at `+0x220`; exact size `0x224`. The source H separately records `Pane` plus empty `Singleton<MapNamePane>` inheritance. | Apply only if absent and all prerequisite sizes match; an existing non-identical type is a hard stop |

The complete current-schema public type request is `declare_type {database:"<runtime-attested returned database>",decls:"struct MapNamePane { Pane base; wchar_t m_mapNameText[128]; EPFTileContext m_mapNameImage; unsigned char *m_mapNameGlyphs; };"}`. The supported field is literal `decls`; no `declaration` field is authorized. Expected readback is a UDT named `MapNamePane`, size `0x224`, members at `0x0`, `0xf8`, `0x1f8`, and `0x220`. It is an IDA physical-layout aid only; it must not replace the human source inheritance in Section 22.

### Exact action and protection rows

All current function entries below have blank regular/repeatable address comments and blank regular/repeatable function comments except `0x50380b` and `0x503816`, whose current regular address comment is literal `Block`. Function renames use current public `rename` with `batch.func:[{addr,name}]`, `pure:true`, `stop_on_error:true`, `allow_overwrite:false`; each batch is first submitted with `dry_run:true`, then identically with `dry_run:false` only after all returned old-name and collision checks match. Physical frame-row renames use `rename.batch.stack:[{func_addr,old,new}]`, never `batch.func` or `batch.data`, with literal `pure:false` and separately complete dry-run/apply requests. Function prototypes use `type_apply_batch` with `batch.edits:[{addr,kind:"function",signature}]`. Function-repeatable prose uses `set_repeatable_comments` and is read back from `get_comments.function_repeatable`; regular address comments use `set_address_comments`, while repeatable address comments use the distinct `set_address_repeatable_comments` endpoint and read back from `get_comments.regular`/`repeatable`. These channels are not interchangeable.

| ID | Exact entity and literal current prestate | Proposed public action | Safety constraints and exact expected readback |
| --- | --- | --- | --- |
| I0L2-01 | Raw code `[0x503110,0x5031e1)`: no function object/name/type; 81 decoded instructions; `retn 10h` at `0x5031de`; fourth argument is loaded as one byte then sign-extended; address regular/repeatable comments are both literal blank and function channels are not applicable before definition; `[0x5031e1,0x5031f0)` is fifteen `0xcc` bytes | Exact definition request is `define_func {database:"<runtime-attested returned database>",items:[{addr:"0x503110",end:"0x5031e1"}]}`. The exact function rename, prototype, repeatable-function comment, and readbacks are in the public-request contracts below. No stack rename or stack-type request is authorized for this raw helper. | Protect all bytes, exact end, `retn 10h`, following `0xcc` run, internal calls, zero inbound xrefs, and the complete binary-derived frame matrix below. Read back one function `[0x503110,0x5031e1)`, exact name/signature/function-repeatable comment, blank regular/address-repeatable/function-regular channels, unchanged bytes/xrefs, and no unlisted frame mutation. No source liveness is inferred from definition. |
| I0L2-02 | Function `[0x5031f0,0x5032c3)`, current name `sub_5031F0`, declaration `int __thiscall sub_5031F0(int this)`; all four comment channels are literal blank. Current `stack_frame` has seven rows, exhaustively listed below; only the first `var_14` dword is loader-size output. | Exact function rename/type/function-repeatable-comment requests are below. Exact stack rename dry-run is `rename {database:"<returned>",batch:{stack:[{func_addr:"0x5031f0",old:"var_14",new:"glyphDataSize"}],dry_run:true,pure:false,stop_on_error:true,allow_overwrite:false}}`; apply is identical with only `dry_run:false`. | Preserve all EH rows (`var_10`, `var_C`, `var_4`), opaque `arg_4`, vptr writes, Singleton publication, two callers, and resource operands. Read back the complete post matrix below; only receiver/return typing and `var_14` -> `glyphDataSize` may differ. |
| I0L2-03 | Function `[0x5032d0,0x50334d)`, current name `sub_5032D0`; exact stored declaration from `inspect_items`/`analyze_function` is `void __thiscall(_DWORD *this)`. Hex-Rays renders `void __thiscall sub_5032D0(Pane *this)` by propagating the existing `Pane` type through the terminal base-destructor call; that decompiler rendering is evidence, not the stored type prestate. All four comment channels are literal blank; no direct inbound xref; current `stack_frame` has the four rows listed below. | After the analysis-only `MapNamePane` UDT declaration succeeds and reads back exactly, use the exact function rename/type/function-repeatable-comment requests below; no stack rename request is authorized. | Hard stop unless the stored declaration is exactly `void __thiscall(_DWORD *this)`, the decompiler still presents `Pane *this`, and every body/frame/comment/xref fact matches. Protect exact range, three decorated MapNamePane vptr writes, `+0x220` MemoryMan release, singleton clear, `Pane__Destructor` call, no-direct-xref fact, scalar-wrapper correspondence, and every physical frame row. Only the stored ECX receiver type may change from `_DWORD *` to `MapNamePane *`; complete post matrix below. |
| I0L2-04 | Function `[0x503350,0x50349f)`, current name `sub_503350`, declaration `void __thiscall sub_503350(int this)`; all four comment channels literal blank; the three exact current frame rows, including already source-facing `length`, are listed below | Exact function rename/type/function-repeatable-comment requests are below; no stack rename request is authorized. | Protect vtable ref `0x61e5f8`, unconditional `GetMemoryMan`, both branches, colors/modes/coordinates, and every frame row. Only the ECX receiver type changes; complete post matrix below. |
| I0L2-05 | Function `[0x5034a0,0x503574)`, current name `sub_5034A0`, declaration `char __thiscall sub_5034A0(_WORD *this, int arg1)`; all four comment channels literal blank. Physical receiver is the `EventHandler` subobject at primary `+0xa0`; all seven frame rows are listed below. | Exact function rename/type/function-repeatable-comment requests are below. Exact stack dry-run is `rename {database:"<returned>",batch:{stack:[{func_addr:"0x5034a0",old:"var_108",new:"memoryMan"},{func_addr:"0x5034a0",old:"MultiByteStr",new:"mapNameBytes"},{func_addr:"0x5034a0",old:"arg_0",new:"event"}],dry_run:true,pure:false,stop_on_error:true,allow_overwrite:false}}`; apply is identical with only `dry_run:false`. | Preserve receiver adjustment `this-0xa0`, packet offsets, 256-byte local, ACP conversion edge, virtual invalidation, false return, and all frame widths/offsets. Only return/receiver/argument typing and the three named frame rows may change; complete post matrix below. |
| I0L2-06 | Function `[0x5037f0,0x5037fb)`, current name `sub_5037F0`, exact current declaration `void sub_5037F0(void)`. Complete current frame matrix is one row: `__return_address`, display offset `+0x0`, width `4`, type `_UNKNOWN *`; no arguments, locals, or saved-register rows exist. Address regular/repeatable and function regular/repeatable comments are all literal blank; sole code xref `0x6020b1`; two-instruction body begins `mov dword ptr unk_69B4B4, 0`. | Rename through the exact `batch.func` request below. Exact comment request is `set_repeatable_comments {database:"<returned>",items:[{addr:"0x5037f0",comment:"Compiler-generated constructor-unwind cleanup for Singleton<MapNamePane>; no standalone source function."}]}`. Deterministic type/frame disposition is protected no-change: no `type_apply_batch`, stack rename, or frame edit is authorized. | Protect exact declaration `void sub_5037F0(void)`, one-row frame, 11-byte body, sole EH xref, compiler classification, and blank address/function-regular channels. Expected readback is destination name `MapNamePane_ConstructionUnwindClearSingleton`, unchanged `void (void)` ABI and exact one-row frame, exact function-repeatable comment, unchanged body/xrefs, and no unlisted type/frame delta. |
| I0L2-07 | Function `[0x50380b,0x503816)`, current name `sub_50380B`, exact current declaration `_DWORD *__thiscall sub_50380B(_DWORD *this, char arg1)`. Complete current frame matrix is one row: `__return_address`, display offset `+0x0`, width `4`, type `_UNKNOWN *`; no physical argument, local, or saved-register rows are materialized. Regular address comment is literal `Block`; address repeatable/function regular/function repeatable are literal blank; body subtracts `0xa0` and jumps `0x503840`. | Rename through the exact `batch.func` request below. Exact regular clear is `set_address_comments {database:"<returned>",items:[{addr:"0x50380b",comment:""}]}`; exact repeatable write is `set_address_repeatable_comments {database:"<returned>",items:[{addr:"0x50380b",comment:"Compiler adjustor thunk: EventHandler view -0xa0 to MapNamePane scalar deleting destructor."}]}`. Deterministic type/frame disposition is protected no-change: no type or frame mutation is authorized. | Protect exact `_DWORD *` return, `__thiscall`, `_DWORD *this`, `char arg1`, one-row frame, bytes/jump/vtable xref. Expected readback is destination name `MapNamePane_EventHandler_ScalarDeletingDestructorThunk`, the same return/calling-convention/argument types and exact frame, address regular blank/repeatable exact, both function channels blank, and no type/body/frame mutation. |
| I0L2-08 | Function `[0x503816,0x503821)`, current name `sub_503816`, exact current declaration `_DWORD *__thiscall sub_503816(_DWORD *this, char arg1)`. Complete current frame matrix is one row: `__return_address`, display offset `+0x0`, width `4`, type `_UNKNOWN *`; no physical argument, local, or saved-register rows are materialized. Regular address comment is literal `Block`; address repeatable/function regular/function repeatable are literal blank; body subtracts `0xa4` and jumps `0x503840`. | Rename through the exact `batch.func` request below. Exact regular clear is `set_address_comments {database:"<returned>",items:[{addr:"0x503816",comment:""}]}`; exact repeatable write is `set_address_repeatable_comments {database:"<returned>",items:[{addr:"0x503816",comment:"Compiler adjustor thunk: TimerHandler view -0xa4 to MapNamePane scalar deleting destructor."}]}`. Deterministic type/frame disposition is protected no-change: no type or frame mutation is authorized. | Protect exact `_DWORD *` return, `__thiscall`, `_DWORD *this`, `char arg1`, one-row frame, bytes/jump/vtable xref. Expected readback is destination name `MapNamePane_TimerHandler_ScalarDeletingDestructorThunk`, the same return/calling-convention/argument types and exact frame, address regular blank/repeatable exact, both function channels blank, and no type/body/frame mutation. |
| I0L2-09 | Function `[0x503840,0x5038fe)`, current name `sub_503840`, exact current declaration `_DWORD *__thiscall sub_503840(_DWORD *Block, char arg1)`. Complete current frame matrix is `var_C` display `+0x8`, width `4`, type `_DWORD`; `var_4` `+0x10`, width `4`, type `_DWORD`; `__saved_registers` `+0x14`, width `4`, type `_DWORD`; `__return_address` `+0x18`, width `4`, type `_UNKNOWN *`; `arg_0` `+0x1c`, width `4`, type `int`. All four comments are literal blank; primary-vtable route and both thunk callers are exact; wrapper uses the delete flag and size guard `0x224`. | Rename through the exact `batch.func` request below. Exact comment request is `set_repeatable_comments {database:"<returned>",items:[{addr:"0x503840",comment:"Compiler-generated scalar deleting destructor for MapNamePane; source authority is virtual ~MapNamePane()."}]}`. Deterministic type/frame disposition is protected no-change: do not issue `type_apply_batch`, stack rename, or frame mutation for I0L2-09. | Protect exact `_DWORD *` return, `__thiscall`, `_DWORD *Block`, `char arg1`, all five frame rows, delete-flag behavior, size guard, ordinary-destructor call, operator-delete call, callers, vtable, and blank address/function-regular channels. Expected readback is destination name `MapNamePane_ScalarDeletingDestructor`, unchanged declaration structure and exact five-row frame, exact function-repeatable comment, and no body/type/frame delta. |
| I0L2-10 | Global span `[0x69b4b4,0x69b4b8)`: `inspect_items` displays generated label `unk_69B4B4` at the first head, but `entity_query` resolves neither that label nor `g_pMapNamePane`; therefore there is no current resolvable name-index entity. First item `[0x69b4b4,0x69b4b5)` is type `Pane *`, non-code/non-data; `0x69b4b5`, `0x69b4b6`, and `0x69b4b7` are separate blank one-byte heads. All four bytes are zero; refs are `6/0/0/0`; all four heads have address regular/repeatable comments `""`; function channels are not applicable. Protected next head `[0x69b4b8,0x69b4b9)` is separate, type `Pane *`; destination collision is absent. | Do not issue `rename.data` with generated old label `unk_69B4B4`. After exact generated-label/name-index/four-head/type/comment/byte/ref/tail/neighbor recheck and successful `MapNamePane` UDT readback, exact direct materialization request is `make_data {database:"<returned>",items:[{addr:"0x0069b4b4",type:"MapNamePane *g_pMapNamePane",name:"g_pMapNamePane",delete_existing:true}]}`. Exact repeatable-address request is `set_address_repeatable_comments {database:"<returned>",items:[{addr:"0x0069b4b4",comment:"Process-wide MapNamePane singleton pointer; null before construction and after destruction."}]}`. Exact regular-address protection/clear request is `set_address_comments {database:"<returned>",items:[{addr:"0x0069b4b4",comment:""}]}` after the repeatable write; neither function-comment endpoint applies. | Consumed span is exactly `[0x69b4b4,0x69b4b8)`. Stop if the generated display label, absence of a resolvable old-name entity, destination collision result, any head span/type/comment, any of the four zero bytes, `6/0/0/0` refs, tail resolution, or protected `0x69b4b8` neighbor differs; also stop on parser failure, channel cross-write, or consumption beyond `0x69b4b8`. Expected one four-byte data item `[0x69b4b4,0x69b4b8)`, resolvable entity/name `g_pMapNamePane`, type `MapNamePane *`, regular `""`, repeatable exact text, function channels absent, same zero bytes/six refs, and unchanged neighbor. Read back with `get_comments`, `inspect_items` on all consumed/protected heads, `get_bytes`, `xrefs_to`, and `entity_query` for both old/display and destination aliases. |
| I0L2-11 | UTF-16 bytes `[0x61e6c0,0x61e6d8)` are physically split into `[0x61e6c0,0x61e6c4)` name `off_61E6C0`, type `const wchar_t`, value `M`; blank `[0x61e6c4,0x61e6c8)`; and `[0x61e6c8,0x61e6d8)` name `aAmeEpf_0`, value `AME.EPF`. All 24 bytes decode `MAPNAME.EPF\0`; refs are `1/0/0`; address regular/repeatable comments are blank and function channels are not applicable; the sole ref is constructor operand `0x50328d`. | After exact fragment/byte/ref/neighbor/collision recheck, exact data request is `make_data {database:"<returned>",items:[{addr:"0x0061e6c0",type:"const wchar_t kMapNameEpfResourceName[12]",name:"kMapNameEpfResourceName",delete_existing:true}]}`; exact repeatable request is `set_address_repeatable_comments {database:"<returned>",items:[{addr:"0x0061e6c0",comment:"MapNamePane EPF layout resource name used by the constructor."}]}`; exact regular protection is `set_address_comments {database:"<returned>",items:[{addr:"0x0061e6c0",comment:""}]}`. | Consumed span is exactly `[0x61e6c0,0x61e6d8)`. Stop on any changed fragment, byte, ref, comment, neighbor, name collision, parser failure, channel cross-write, or span extension. Expected one 24-byte data item, exact name/type/full string; address regular blank/repeatable exact; one xref at `0x50328d`; unchanged adjacent items. |
| I0L2-12 | Shared UTF-16 bytes `[0x610fa4,0x610fbc)` are physically split into `[0x610fa4,0x610fa8)` `off_610FA4`, type `const wchar_t`, value `F`, and `[0x610fa8,0x610fbc)` `aMpartPal`, value `MPART.PAL`; all comments blank; refs `3/0` at `0x461683`, `0x503375`, `0x503678` | No change | Shared source literal is not MapName-owned. Protect both exact item spans/names/types, all 24 bytes, blank comments, xrefs, and neighbors; do not consolidate it or create a file-static duplicate in this report. |
| I0L2-13 | Vtable/RTTI bases `0x61e5b4`, `0x61e600`, `0x61e630` have decorated `MapNamePane` names and exact slot routes | No change | Protect all decorated names, table boundaries, RTTI pointers, slots, xrefs, and adjacent data. They regenerate from the H declaration and receive no standalone C++ or synthetic IDA type overwrite. |


### Literal current-schema requests and comment-channel readbacks for I0L2-01 through I0L2-05

| Purpose | Complete inert public MCP request / expected result |
| --- | --- |
| Function definition | `define_func {database:"<runtime-attested returned database>",items:[{addr:"0x503110",end:"0x5031e1"}]}`; result must report start `0x503110`, end `0x5031e1`, size `0xd1`, with no error. |
| Function types | Before this batch, `type_inspect` must read back the exact `MapNamePane` UDT and `inspect_items`/`analyze_function` must still report I0L2-03 stored `void __thiscall(_DWORD *this)` while decompile still renders `Pane *this`; either mismatch is a hard stop. Then use `type_apply_batch {database:"<returned>",batch:{edits:[{addr:"0x503110",kind:"function",signature:"void __thiscall MapNamePane_DrawNumberGlyphString(MapNamePane *this, const wchar_t *text, int x, int y, signed char glyphRow)"},{addr:"0x5031f0",kind:"function",signature:"MapNamePane *__thiscall MapNamePane_Constructor(MapNamePane *this)"},{addr:"0x5032d0",kind:"function",signature:"void __thiscall MapNamePane_Destructor(MapNamePane *this)"},{addr:"0x503350",kind:"function",signature:"void __thiscall MapNamePane_OnPaint(MapNamePane *this)"},{addr:"0x5034a0",kind:"function",signature:"bool __thiscall MapNamePane_HandlePacketEvent(EventHandler *eventHandlerThis, Event *event)"}],stop_on_error:true}}`; exactly five applied, zero failed, and all post matrices above must match. |
| Function-repeatable comments | `set_repeatable_comments {database:"<returned>",items:[{addr:"0x503110",comment:"Draws 9x11 numeric glyphs from the MapNamePane glyph buffer; spaces and slashes advance without drawing."},{addr:"0x5031f0",comment:"Constructs the map-name overlay, initializes EPF state and conditionally loads MAPNAME.EPF and 9X11FONT.BIN."},{addr:"0x5032d0",comment:"Source-authored MapNamePane destructor body; releases the optional 9X11 glyph buffer before compiler-generated Singleton/base teardown."},{addr:"0x503350",comment:"Paints the centered map name using MAPNAME.EPF/FRMPART.PAL or the legacy filled-text fallback."},{addr:"0x5034a0",comment:"Handles opcode 0x15 map-name updates through the EventHandler subobject and always returns false."}]}`. This endpoint writes `function_repeatable`; it is not `set_address_repeatable_comments`, and it must leave address regular/repeatable and function regular channels blank. |
| Comment pre/post readback | `get_comments {database:"<returned>",addrs:["0x503110","0x5031f0","0x5032d0","0x503350","0x5034a0"]}`. Current I0L2-01 address regular/repeatable are `""` and function channels are absent; current I0L2-02..05 have all four channels `""`. Post I0L2-01..05 requires address regular `""`, address repeatable `""`, function regular `""`, and the exact function-repeatable text above. Any cross-channel write is a hard stop/no-save condition. |
| Frame and stored-type readback | Before mutation, `inspect_items` and `analyze_function` must preserve I0L2-03 stored `_DWORD *` while `decompile` preserves the separate inferred `Pane *` presentation. After the type batch, `inspect_items`/`analyze_function` must report exact `void __thiscall MapNamePane_Destructor(MapNamePane *this)`. `stack_frame {database:"<returned>",addrs:["0x503110","0x5031f0","0x5032d0","0x503350","0x5034a0"]}` plus one bounded `disasm {database:"<returned>",addr:"<one exact function address>",offset:0,max_instructions:1,include_total:true}` per function must show I0L2-02..05 matching every post row above exactly; I0L2-01 must preserve every physical EBP-relative span/argument/saved-register row and exact `retn 0x10`. |

Function rename dry run is one complete current-schema request `rename {database:"<runtime-attested returned database>",batch:{func:[{addr:"0x503110",name:"MapNamePane_DrawNumberGlyphString"},{addr:"0x5031f0",name:"MapNamePane_Constructor"},{addr:"0x5032d0",name:"MapNamePane_Destructor"},{addr:"0x503350",name:"MapNamePane_OnPaint"},{addr:"0x5034a0",name:"MapNamePane_HandlePacketEvent"},{addr:"0x5037f0",name:"MapNamePane_ConstructionUnwindClearSingleton"},{addr:"0x50380b",name:"MapNamePane_EventHandler_ScalarDeletingDestructorThunk"},{addr:"0x503816",name:"MapNamePane_TimerHandler_ScalarDeletingDestructorThunk"},{addr:"0x503840",name:"MapNamePane_ScalarDeletingDestructor"}],dry_run:true,pure:true,stop_on_error:true,allow_overwrite:false}}`; apply is the identical request with only `dry_run:false`. Any unexpected old name, destination collision, row failure, unlisted item/type/comment/xref delta, or failed immediate readback requires no save and supervisor rollback. One save and a fresh persisted reopen are allowed only after all listed actions and no-change protections pass.
~~~

## Removed Block R002 - Gate 2B Runtime And Save Sequence

The following source-report text was removed because it encoded runtime-attestation, mismatch, action, readback, and save order. It is non-authoritative, must never be executed, and receives no Gate, coverage, score, IDA, validator, or lifecycle credit.

~~~text
This is a read-only structured handoff. Every mutation is supervisor-owned. The primary supervisor must obtain a fresh public runtime attestation for the then-current canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` session, reproduce every literal prestate below, stop on any mismatch or collision, and save only after every accepted action and protected-state readback succeeds. This report specifies state transitions and evidence; it does not contain a transaction program.
~~~
