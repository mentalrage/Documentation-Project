** TARGET-REPORT-UID:0003RA **
** AUTHOR-AGENT-ID:B003 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.6 | xHigh **

# UID0003RA SelfLookPane2ShowLegendView Source-Quality Report

## Finalized Report / Current Recommendation

- The authorized implementation callback is complete for UID0003RA at exact interval `[0x00570ef0,0x00570fb3)`. C01-C20 and R1 were applied to the six accepted ordinary by-* destinations, every changed page passed its scoped validator, and waited generated command `000000010799` completed successfully.
- UID0003RA now emits one source-authored, nonvirtual `void SelfLookPane2::ShowLegendView()` definition through owner/emitter UID0000CV and file route UID0000NL `NexusTK/ui/panels/SelfLookPane.cpp`.
- Target metadata is implemented at `91/94` from pre-callback `85/89`, preserving `CANONICAL_OWNER:0000CV`, `EMITTER_UIDS:0000CV`, `RECONSTRUCTABLE:TRUE`, blank optional position, `Nested:0`, exact range, and no split.
- Destination R1 is implemented byte-for-byte as complete mid-2000s-style C++. It preserves the exact mode store, child detach/unregister sequence, rectangle, vertical current-to-zero scroll transition, root-layer attachment, pane order, and inherited-bounds invalidation.
- The score remains below final certainty because no original symbols recover the lexical method/access names or physical source filename. Those lexical limits do not block a behaviorally complete formal body.

## Supporting Research

### Current documentation and generated evidence

- Target UID0003RA now records `91/94`, owner/emitter UID0000CV, reconstructable true, blank optional position, exact R1 formal body, `Nested:0`, three internal callers, and the mode-2 role. Its former source-name/blocker and blank-body wording is retained only as pre-callback history.
- UID0000CV `by-class/SelfLookPane2.md` retains its exact managed declaration, including `void ShowLegendView();`, all required source-facing fields/types, and the complete class close before `[[CHILDREN]]`; only bounded method/evidence/history prose was synchronized.
- UID0000NL `by-file/SelfLookPane.md` retains the `NexusTK/ui/panels/` route and now inventories UID0003RA as source-ready after UID0004NA and before the following OnPaint range.
- UID0001HK remains the non-emitting exact class-method split index and now records both UID0003RA's source-ready child contract and the 13-byte successor parent-only alignment span.
- Historical pre-callback generated command `000000010753`, refreshed `2026-07-14T04:39:54-04:00`, contained one UID0003RA Empty Emitter Marker. B003 waited command `000000010799`, refreshed `2026-07-14T05:16:29-04:00`, emitted exactly one UID0003RA definition and zero target Empty Emitter Markers; latest observed external validator command `000000010803`, refreshed `2026-07-14T05:19:10-04:00`, preserves that target result.

### Historical-report search provenance and classification

Exact terms searched read-only were `0003RA`, `0x00570ef0`, `00570ef0`, `SelfLookPane2ShowLegendView`, `sub_570EF0`, and `ShowLegendView`.

| Root searched | Exact outcome | Target-specific classification |
| --- | --- | --- |
| `source-3/project-documentation/executed-b-agent-research/**` | Direct-address/title terms matched B001 UID0001HK, B003 UID0003QZ, and a B001 UID000284 validator diagnostic. Broader `ShowLegendView` also matched B011 and UserLook reports. | No direct UID0003RA body report exists. B001 UID0001HK created the split row at `85/89` and only described a generic helper. B003 UID0003QZ synchronized the OnActivate mode-2 caller and explicitly left UID0003RA score/formal unchanged. B001 UID000284 mentions only a pre-existing mapping diagnostic. B011 supplies a declaration precedent only. UserLook reports are sibling naming/behavior comparators, not target ownership or body evidence. |
| `source-3/project-documentation/archived/**` | Zero matches for every exact term. | No archived direct-target research. |
| `source-3/project-documentation/tools/leaser/Agents/Older-Research/**` | Zero matches for every exact term. | No older-research direct-target evidence. |
| `source-3/project-documentation/tools/leaser/Agents/SpecialReports/**` | Zero matches for every exact term. | No special-report direct-target evidence. |
| `source-3/project-documentation/tools/leaser/Agents/**` | Current B003 goal/audit references plus stale B001/B004 selection-note references. | Current B003 goal is assignment authority. B001/B004 notes describe an earlier UID0003QZ support overlap while those agents are assigned elsewhere; they are not direct UID0003RA research or current ownership. No competing B001-B005 direct report was found. |

Relevant matches were opened and classified rather than copied:

- Executed B001 `0001HK-selflookpane2-source-split-audit.md`: establishes the exact child interval and three generic caller families, but contains neither the body nor source-quality helper contracts; its old "draw" caller wording is historical and superseded by UID0003QZ OnActivate.
- Executed B003 `0003QZ-SelfLookPane2OnDraw-source-quality.md`: proves the renamed OnActivate caller, mode restore, exact class fields, and neighboring raw Profile helper; it deliberately treated UID0003RA as support and left the formal body blank.
- Executed B001 `000284-SpelledPaneWhitespaceDelimiter-source-quality.md`: contains only validator mapping diagnostics for UID0003RA and no source claim.
- Executed B011 `0000NL-SelfLookPane-empty-emitter-family-source-quality.md`: contains `ShowLegendView` in the class declaration but does not recover this function.
- Executed B001 `0001KK-userlookpane-profile-source-split-audit.md`: inventories a separate UserLook mode-3 ShowLegendView range and shared LegendPane ownership; it is a naming/source-shape comparator only.
- Executed B001 `0003RT-UserLookPaneShowBioView-source-quality.md`: mentions the neighboring UserLook ShowLegendView only to establish address/source-family order; it has no UID0003RA evidence.
- Executed B005 `0003RO-UserLookPaneHandleKeyEvent-source-quality.md`: proves the shared internal PageDown name and a four-view UserLook transition graph, but its mode-3 Legend transition is a different class/body.
- Executed B005 `0003RK-UserLookPaneParseLookPacket-source-quality.md`: includes a UserLook class declaration and shared LegendPane inventory only; it is not target body or ownership evidence.
- The current B001 and B004 goals contain assignment-time notes that UID0003RA was formerly a support destination during UID0003QZ. Their actual assigned targets are unrelated, so these notes are stale overlap history rather than competing ownership.
- `tools/leaser/Agents/b-report-validation-audit.md` contains prior assignment/audit context only. It supplies no binary or source conclusion for this new exact artifact.

The four non-executed historical roots returned zero exact or broad-name matches, so there was no additional report to open. The seven executed broad-name matches are all classified above.

## Target

- UID: `0003RA`
- Path: `by-memory/0x00570ef0-0x00570fb3.SelfLookPane2ShowLegendView.md`
- Exact interval: `[0x00570ef0,0x00570fb3)`
- Exact size: `0xc3` / 195 bytes (Verified with `tools/int_convert.py`).
- Existing title hypothesis: `SelfLookPane2ShowLegendView`
- Implemented source identity: `void SelfLookPane2::ShowLegendView()`
- Historical pre-callback metadata: `85/89`, owner/emitter UID0000CV, reconstructable true, blank optional position/formal, `Nested:0`.
- Implemented metadata: `91/94`, with owner/emitter/reconstructable/position/nesting/range preserved and R1 present.

## Current Target State

- The target page now contains the direct byte hash, complete ordered body, ABI/void resolution, exact callee contracts, adjacent parent-only padding dispositions, source/access analysis, negative evidence, score rationale, exact Item Summary, and R1 formal C++.
- Its implemented Item Summary names only the actual `m_spelledPane` and `m_legendPane` detach/unregister behavior; `m_profilePane` remains an explicit negative.
- The existing class declaration remains byte-for-byte intact and supplies every field/declaration needed by R1; the generated definition comes only from UID0003RA.
- The historical Empty Emitter Marker was a documentation gap for a live source method. Waited command `000000010799` verifies that the gap is closed without compiler-only/no-code treatment.
- B003's implementation, scoped validation, readback, ledger, and checklist work is complete. Report validation and all lifecycle/count/path/execution state remain external supervisor/validator-owned state.

## Executive Recommendation

UID0003RA is implemented as one exact source method under UID0000CV. The owner, emitter, source route, range, and relative nesting are preserved; R1 is in the target formal block; exact behavior and negative evidence are in the target; only the bounded parent/class/file/key/event prose was synchronized; and unrelated UID0003QZ/B001 work remains unchanged.

The method is not a LegendPane method: it mutates `SelfLookPane2::m_viewMode`, chooses among SelfLookPane2 child panes, supplies `this` as the attachment parent, and invalidates inherited SelfLookPane2 bounds. LegendPane, Pane, ScrollablePane, and MainUiLayerSlots supply dependencies only.

## Supervisor Active Recheck

- The Gate-1-passed report hash and appended authorized callback were reopened before implementation; UID0003RA remained the sole target.
- UID0003QZ is supervisor-closed and was read only as historical support. No UID0003QZ report or by-* page was modified.
- Target, parent, class, file, caller, sibling, generated, and manual coverage documents were reread before and after the bounded changes.
- Mandatory MCP evidence was collected against IDB session `5288313d` at evidence-collection time. The final health/lookup/byte probes succeeded; the report does not make an indefinite assertion about later session availability.
- Six short one-file leases were acquired serially, and each was released immediately after that file's scoped validator. No B003 lease remains. B003 ran no report probe, `execute_report`, lifecycle, move, or archive command.

## Inference Research Guidance Check

- Binary facts are separated from source-facing inference. Addresses, bytes, hash, instruction order, fields, call sites, virtual offsets, literal bounds, mode value, axis, old/new positions, and padding are direct evidence.
- `ShowLegendView`, `m_spelledPane`, `m_legendPane`, `m_viewMode`, `RectBounds`, `InitRectBounds`, `GetScrollPosition`, `ApplyScrollPosition`, `AddToLayer`, `RemoveFromLayer`, `UnregisterEventHandler`, `SetPaneOrder`, `InvalidateRect`, and `g_mainUiLayerSlots.rootPaneLayerContext` are current project source-facing names backed by class/helper/vtable/family evidence.
- Original lexical spelling and access level are not recovered symbols. The strongest implementation-safe decision is to retain the existing public declaration rather than invent an access move. A private declaration is stylistically plausible but not proven.
- Compiler cookie setup/check and register/stack management are excluded from source C++. No raw offsets, decompiler names, manual vtable operations, or artificial padding members appear in R1.
- The code uses era-consistent `0`, an ordinary local `RectBounds`, and `unsigned short`; it avoids modern-only syntax and unnecessary wrappers.

## Heuristic / Inference Reanalysis And Validation

| Question | Direct evidence and comparator check | Resolution |
| --- | --- | --- |
| Is this a real source method? | Modeled 195-byte function, coherent prologue/epilogue, 63 instructions, three direct class-internal callers, four direct callees plus virtual dispatches, and a complete pane-state sequence. | Yes: retained first-party source-authored class method, not padding, thunk, compiler helper, or dead raw body. |
| Return type | IDA types EAX as `int` because the last virtual `InvalidateRect` result remains in EAX. The body does not form or test a return value, every caller ignores EAX, and the accepted class declaration/family helpers use `void`. | `void` is the behaviorally exact source signature; the decompiler's tail-result propagation is not an intentional source return. |
| Method name | Three callers select it only for mode transition/restore to value 2; existing class/family names use `ShowLegendView`; UserLook has an independent sibling with the same role name. | Retain `ShowLegendView`. Reject raw `sub_570EF0`, `ActivateLegendPane`, `SetLegendMode`, and `DisplayLegend` as weaker or needlessly invented. |
| Access level | Only three internal direct calls; no vtable/data pointer exposure. Binary cannot encode C++ lexical access. Existing complete class declaration exposes the helper publicly with sibling helpers. | Preserve the existing public declaration for source consistency. Record private as an unproven lexical possibility and confidence cap. |
| Exact child order | Direct virtual calls are Spelled `+0x38`, Spelled `+0x40`, Legend `+0x38`, Legend `+0x40`. Current Pane slot evidence maps them to remove then unregister. | Preserve `m_spelledPane` remove/unregister, then `m_legendPane` remove/unregister. Do not normalize or add ProfilePane. |
| Why remove Legend before attaching it? | The body explicitly removes/unregisters the same `m_legendPane` before reattaching; there is no conditional path. Sibling view helpers use reset-before-attach patterns. | Preserve the apparently redundant reset. It ensures clean existing layer/event state and is not removable decompiler noise. |
| Bounds argument order | Push sequence is 239, 183, 10, 3 before `sub_4B7C50`; accepted helper signature is `(out,left,top,right,bottom)`. | `InitRectBounds(&legendBounds, 3, 10, 183, 239)`. |
| Scroll argument order | Target pre-pushes target `0` at `0x570f59`, pushes axis `0` for `GetScrollPosition`, then pushes returned old/current and axis `0` for `ApplyScrollPosition`. Callee `0x55eae0` compares old to target and stores target. | `ApplyScrollPosition(0, currentPosition, 0)`. Reject reversed old/new order. |
| Is a max-scroll call missing? | Target calls no `0x55e910` GetScrollMax. The analogous SelfLook helper at `0x00567380` performs an additional max read/application, proving the compiler would expose such a call when present. | Do not invent a second scroll or bottom-of-list step. UID0003RA resets to vertical zero only. |
| Layer context | Direct data read at `0x570f73` is `0x0069b368`; current MainUiLayerSlots evidence identifies this slot as `rootPaneLayerContext`. | Use `g_mainUiLayerSlots.rootPaneLayerContext`; do not use a raw array index or transfer ownership to the global. |
| Invalidation target | Final primary virtual `+0x20` receives `this+0x44`; current Pane vtable and class layout identify inherited `m_bounds`. | `InvalidateRect(&m_bounds)`. |
| Split or factorization | One straight-line source operation sequence, no internal branch, no separate entry/xref, and exact ret at end. Sibling bodies differ in geometry and extra scroll operations. | No child split, no outlined file helper, and no cross-class body factorization. |

Former blockers are resolved. Only original lexical spelling/access/file uncertainty remains, and it is explicitly reflected in confidence rather than deferred as future investigation.

## Evidence Standards Used

- Mandatory live IDA MCP: fresh `idb_list`, `server_health`, bounded `lookup_funcs`, `get_bytes`, `disasm`, `decompile`, `xrefs_to`, `xref_query`, `callees`, and bounded `find` queries.
- Raw-byte anchoring: exact half-open interval, byte count, SHA256, first/last bytes, predecessor/successor bytes, and ret/padding boundaries.
- Behavioral anchoring: instruction-by-instruction call/data order, helper disassembly, caller conditions, accepted class layout, and Pane/ScrollablePane virtual/helper contracts.
- Reachability anchoring: code/data xrefs, immediate search, VA/RVA pointer-byte searches, function/vtable checks, and caller/callee inventories.
- Documentation comparison: current by-memory/by-class/by-file/global/vtable/generated/manual coverage documents plus exact historical-report roots and terms.
- Source-quality rule: preserve exact binary behavior while using established human source abstractions and excluding compiler lowering.
- Numeric conversions were checked with `tools/int_convert.py`; the 195-byte and 13-byte decimal values are tool-verified.

## Evidence Checked

### Mandatory MCP availability and recovery record

- Endpoint: `http://127.0.0.1:13337/mcp`, streamable JSON-RPC, read-only use.
- Fresh `idb_list` at `2026-07-14T04:50:18-04:00` returned one adopted active worker-backed NexusTK IDB session, `5288313d`, path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID 20244 at evidence-collection time.
- `server_health` for that IDB returned `status:ok`, image base `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and 2,067 cached strings.
- The same bounded pass returned `sub_570EF0`, size `0xc3`; a subsequent fresh stream session returned the real first 16 target bytes at `2026-07-14T04:50:39-04:00`.
- A later full 195-byte read at `2026-07-14T04:52:22-04:00` reproduced the exact hash and boundaries below.
- One earlier broad `analyze_function` request timed out after 130 seconds and the immediately following health request timed out. TCP 13337 and the IDB worker/listener processes remained present. After the required wait, fresh initialize/idb_list/health/lookup/get_bytes calls all succeeded. No MCP process was started, stopped, restarted, or killed, and no fallback-only conclusion is used.

### Current documentation and support checked

- Target UID0003RA; parent UID0001HK; class UID0000CV; file UID0000NL.
- Direct callers UID0003QZ, UID0003RC, and UID0003RD.
- View siblings UID0003R8, UID0003R9, UID0004NA, and the `0x00567380` SelfLook comparator.
- UID000074 LegendPane, ProfilePane, SpelledPane, ScrollablePane/ScrollableControlPane helper contracts, Pane vtable slots, MainUiLayerSlots/global slot evidence, and RectBounds initializer evidence.
- Evidence-time generated `SelfLookPane.cpp` and manual by-memory/by-class/by-file coverage rows.
- All historical roots and exact terms listed in Supporting Research.

## Claim And Incorporation Ledger

Every Action and Verification state below is a permitted final callback enum. Detailed implementation proof is retained claim by claim.

| Claim ID | Atomic claim | Confidence | Evidence | Destination | Action | Verification state | Implementation proof |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C01 | Healthy mandatory MCP evidence was collected against IDB session `5288313d` at evidence-collection time after a bounded timeout recovery; final health/lookup/bytes succeeded. | Very strong | Fresh idb_list/health/lookup/get_bytes records. | Target evidence/change history and report provenance. | incorporate | applied | UID0003RA now records the durable evidence-time session/health/recovery facts; scoped command `000000010778` passed. No IDA mutation occurred. |
| C02 | Target is exactly `[0x00570ef0,0x00570fb3)`, `0xc3`/195 bytes, SHA256 `95E0...B040`, ending in ret at `0x570fb2`. | Very strong | Full MCP bytes, disassembly, hash. | UID0003RA range/boundary evidence and Item Summary. | incorporate | applied | UID0003RA exact-range section records the full `95E0FFBABDFA83E9DAB9677A153E61D7A3B14F5F1A5954A580CC600B52A7B040` hash, size, first/last bytes, and ret; command `000000010778` passed. |
| C03 | `[0x00570eef,0x00570ef0)` is one predecessor CC and `[0x00570fb3,0x00570fc0)` is 13 successor CC bytes; both are parent-only. | Very strong | Bounded pre/post byte reads; neighbor functions. | UID0003RA and UID0001HK boundaries. | incorporate | applied | Both pads are explicit on UID0003RA; UID0001HK now records the successor span as parent-only; commands `000000010778` and `000000010780` passed. |
| C04 | Source signature is `void SelfLookPane2::ShowLegendView()`; IDA's `int` is tail-result propagation from InvalidateRect, not a formed return. | Strong | Disassembly/decompile, ignored EAX at all callers, class/family declaration. | UID0003RA status/signature/formal and UID0000CV method row. | incorporate | applied | Target signature/ABI evidence and R1 use `void`; UID0000CV method evidence is synchronized without changing its declaration; commands `000000010778` and `000000010782` passed. |
| C05 | Body first stores `m_viewMode = ViewMode_Legend` (`2`) after loading `m_spelledPane`. | Very strong | `0x570f03`, `0x570f09`; exact class layout. | UID0003RA behavior/R1/class support. | incorporate | applied | UID0003RA ordered behavior/R1 and UID0000CV evidence preserve the mode store; commands `000000010778`/`000000010782` passed and generated command `000000010799` emits it once. |
| C06 | Exact detach order is Spelled remove/unregister then Legend remove/unregister; ProfilePane is not touched. | Very strong | Virtual calls `0x570f15/20/2b/36`, Pane slots, field offsets. | UID0003RA behavior/R1/negative evidence. | incorporate | applied | Target behavior, field/API, negatives, and R1 record the exact order and Profile negative; `000000010778` passed; generated target slice has no `m_profilePane`. |
| C07 | Local bounds are exactly `(3,10,183,239)` through `InitRectBounds`. | Very strong | Push order and `0x4b7c50` signature. | UID0003RA behavior/R1. | incorporate | applied | UID0003RA records the literal bounds and helper contract; `000000010778` passed and `000000010799` emits the exact call. |
| C08 | Scroll sequence reads axis-0 current position then applies `(axis 0, old/current, target 0)`; no max read or second application occurs. | Very strong | Target push lifetime, helper bodies `0x55e950/0x55eae0`, callee list. | UID0003RA behavior/R1/negative evidence. | incorporate | applied | UID0003RA preserves getter then `ApplyScrollPosition(0,currentPosition,0)` and rejects max/second updates; `000000010778` passed; generated target has no `GetScrollMax`. |
| C09 | LegendPane attaches with bounds, flag 0, parent `this`, and `g_mainUiLayerSlots.rootPaneLayerContext`, then receives `SetPaneOrder(0,this)`. | Very strong | `0x570f73-0x570f96`, virtual slots, global docs. | UID0003RA behavior/R1. | incorporate | applied | Exact attach/order arguments are documented and emitted; `000000010778` and waited `000000010799` passed. |
| C10 | Final source operation is `InvalidateRect(&m_bounds)` through primary slot +0x20 with inherited bounds at +0x44. | Very strong | `0x570f99-0x570fa4`, Pane vtable/class layout. | UID0003RA behavior/R1. | incorporate | applied | UID0003RA documents inherited bounds and emits the exact final operation; `000000010778` and `000000010799` passed. |
| C11 | Exactly three code callers exist: OnActivate mode2 restore, unmodified PageDown from mode1, and mouse/input next-view case4 from mode1. | Very strong | xrefs_to/find code_ref and caller decompilation at `0x5708fb`, `0x571c86`, `0x571fda`. | UID0003RA, UID0003RC, UID0003RD, parent/class/file prose. | incorporate | applied | Target/parent/class/file carry all three calls; UID0003RC exact PageDown prose passed `000000010792`; UID0003RD exact case-4 prose passed `000000010794`; UID0003QZ was verified read-only. |
| C12 | No data/immediate/VA/RVA/vtable route exists; target is a direct class-internal nonvirtual helper. | Very strong | find/xref_query/pointer byte searches/entity checks. | UID0003RA identity/negative evidence. | incorporate | applied | UID0003RA records every negative route and direct-helper identity; `000000010778` passed; generated output contains no manual target vtable/RTTI array. |
| C13 | Owner/emitter remain UID0000CV and source route UID0000NL; LegendPane/Pane/ScrollablePane/global are dependencies only. | Very strong | Receiver fields/state, callers, accepted ownership hierarchy. | UID0003RA metadata and support prose. | already-present | already-present | Validator readback preserves UID0000CV owner/emitter and UID0000NL route; target/class/file commands `000000010778`, `000000010782`, and `000000010790` passed. Dependency pages were verified read-only. |
| C14 | No range split, outlined helper, source duplicate, or compiler/no-code disposition applies; target remains `Nested:0`. | Very strong | Straight-line 63-instruction body, one entry/ret, no interior routes. | UID0003RA/UID0001HK split analysis. | reject-invalid | excluded-with-reason | UID0003RA and UID0001HK explicitly reject split/compiler/no-code alternatives and preserve `Nested:0`; commands `000000010778` and `000000010780` passed. |
| C15 | `ShowLegendView` is the strongest source-facing name; preserve existing public declaration because private is plausible but unprovable. | Strong | Mode2 graph, class/sibling conventions, absence of symbols/data refs. | UID0003RA and UID0000CV prose; formal declaration unchanged. | incorporate | applied | Target/class source-name and access-cap prose was applied; UID0000CV's managed block remained byte-for-byte unchanged; `000000010778`/`000000010782` passed. |
| C16 | Destination R1 is complete human-written source and replaces the generated Empty Emitter Marker without compiler scaffolding or raw offsets. | Very strong | Full body reconstruction and dependency closure. | UID0003RA formal block. | incorporate | applied | R1 was inserted byte-for-byte; `000000010778` passed; waited `000000010799` emits one definition, zero target empty markers, and no rejected target-slice artifacts. |
| C17 | Raise only UID0003RA to `91/94`; preserve owner/emitter/reconstructable/blank position/Nested0/exact range. | Strong | Closed behavior/type/route blockers with lexical caps. | UID0003RA metadata/score rationale/coverage row. | incorporate | applied | Validator `000000010778` reports completion 91/confidence 94 and preserves every listed metadata field. Support scores/routes were unchanged. |
| C18 | UID0001HK, UID0000CV, UID0000NL, UID0003RC, and UID0003RD need bounded prose synchronization; their scores/routes/formals remain unchanged. | Strong | Current-doc comparison and exact caller/boundary evidence. | Five named support pages. | incorporate | applied | Bounded updates passed commands `000000010780`, `000000010782`, `000000010790`, `000000010792`, and `000000010794`; accepted scores/routes/formals remained unchanged. |
| C19 | Historical generic helper, draw-caller, blank-formal, and generated marker descriptions must remain labeled pre-direct-pass history rather than current blockers. | Very strong | Executed reports/current docs/generated command `000000010753`. | UID0003RA/support Changes and historical notes. | historicalize | applied | Target/support Changes sections identify the superseded assumptions as historical; this report contrasts historical command `000000010753`, B003 waited command `000000010799`, and latest observed external refresh `000000010803`. |
| C20 | Manual coverage needs exact parent/class/file row replacements plus target/key/event additions; callback must update ledger/checklist with destination/validator/generated proof. | Very strong | Read-only manual coverage comparison and workflow requirements. | Report exact manual text, ledger, checklist. | incorporate | applied | Exact external manual coverage text is retained below without a coverage edit; this ledger, Validator Results, Changed Files, and all checklist rows now carry final destination/command/readback proof. |

## Positive Evidence Summary

- Exact modeled boundary and full-byte hash anchor one 195-byte function with 63 decoded instructions and a clean `ret`.
- Three direct callers all select the helper as the transition/restore to mode 2; no external or ambiguous caller exists.
- Every receiver offset is already closed by the exact UID0000CV class layout: SpelledPane `+0xfc`, LegendPane `+0x104`, view mode `+0xd7c`, inherited bounds `+0x44`.
- Every direct helper contract is closed by live disassembly and current source-facing documentation.
- Pane primary slots map all six indirect calls to established source APIs.
- The bounds, scroll axis, old/new order, layer context, pane order, and invalidation argument are immediate or data-flow exact.
- The accepted class declaration already supplies the method declaration and closes before child definitions, so R1 has a valid namespace-scope emission route.
- Family comparators independently support the `ShowLegendView` role while their behavioral differences prove this exact body must not be copied or factored away.

## IDA MCP Facts

### Exact bytes and boundary

- Full interval SHA256: `95E0FFBABDFA83E9DAB9677A153E61D7A3B14F5F1A5954A580CC600B52A7B040`.
- First 16 bytes: `55 8B EC 83 EC 14 A1 24 2F 67 00 33 C5 89 45 FC`.
- Last 16 bytes: `20 8B 4D FC 33 CD 5E E8 80 67 05 00 8B E5 5D C3`.
- Predecessor sample `0x00570ee0-0x00570ef0` ends `... 8B E5 5D C3 CC`: UID0004NA returns at `0x00570eee`; `0x00570eef` is one CC.
- Successor sample begins with 13 CC bytes at `0x00570fb3`, followed by OnPaint prologue at `0x00570fc0`.

### Instruction-level source sequence

| Address | Exact binary action | Source-facing action |
| --- | --- | --- |
| `0x570f03` | load `[esi+0xfc]` | load `m_spelledPane` |
| `0x570f09` | store dword 2 at `[esi+0xd7c]` | `m_viewMode = ViewMode_Legend` |
| `0x570f15` | Spelled primary vslot `+0x38` | `m_spelledPane->RemoveFromLayer()` |
| `0x570f20` | Spelled primary vslot `+0x40` | `m_spelledPane->UnregisterEventHandler()` |
| `0x570f2b` | Legend primary vslot `+0x38` | `m_legendPane->RemoveFromLayer()` |
| `0x570f36` | Legend primary vslot `+0x40` | `m_legendPane->UnregisterEventHandler()` |
| `0x570f39-0x570f4b` | pushes 239, 183, 10, 3 and local | initialize `(3,10,183,239)` RectBounds |
| `0x570f59-0x570f5d` | pre-push target 0; push getter axis 0; call `0x55e950` | get vertical current position while preserving target zero on stack |
| `0x570f68-0x570f6e` | zero-extend AX; push old/current; push axis 0; call `0x55eae0` | apply axis 0 from current to zero |
| `0x570f73-0x570f88` | push global `0x69b368`, this, 0, bounds; vslot `+0x30` | add LegendPane to root layer |
| `0x570f8b-0x570f96` | push this, 0; vslot `+0x3c` | `SetPaneOrder(0,this)` |
| `0x570f99-0x570fa4` | pass `this+0x44` to primary vslot `+0x20` | invalidate inherited `m_bounds` |
| `0x570fa4-0x570fb2` | cookie check and epilogue | compiler lowering; no source return expression |

### Direct callees

| Callee | Resolved contract | Ownership/disposition |
| --- | --- | --- |
| `0x004b7c50` | `InitRectBounds(RectBounds *, int left, int top, int right, int bottom)` | Shared rectangle helper; dependency only. |
| `0x0055e950` | `unsigned short GetScrollPosition(char axis)` | Scrollable-pane selected-child helper; axis false selects child pointer at `+0xfc` and reads current word `+0xfe`. |
| `0x0055eae0` | `void ApplyScrollPosition(char axis, unsigned short oldPosition, unsigned short targetPosition)` | ScrollablePane helper; no-op when equal, otherwise stores target and updates geometry/invalidation. |
| `0x005c772f` | security-cookie check | Compiler runtime; exclude from source. |

Indirect slots resolve to Pane `InvalidateRect` `+0x20`, `AddToLayer` `+0x30`, `RemoveFromLayer` `+0x38`, `SetPaneOrder` `+0x3c`, and `UnregisterEventHandler` `+0x40`.

## Function / Child Inventory

| Exact range | Current UID/status | Implemented disposition | Notes |
| --- | --- | --- | --- |
| `[0x00570e20,0x00570eef)` | UID0004NA implemented retained raw source | verify-only, unchanged | Separate ShowProfileView body; no modeled function/live route. |
| `[0x00570eef,0x00570ef0)` | parent-only padding | unchanged | One CC predecessor pad. |
| `[0x00570ef0,0x00570fb3)` | UID0003RA implemented modeled child | R1 emitted at `91/94` | One source method; no internal split. |
| `[0x00570fb3,0x00570fc0)` | parent boundary prose implemented | parent-only padding | 13 CC bytes; not absorbed into target or OnPaint. |
| `[0x00570fc0,0x00571c05)` | UID0003RB existing OnPaint | verify-only, unchanged | Distinct modeled successor. |

UID0003RA contains no child ranges. `Nested:0` is the correct relative address-sorted delta and remains unchanged; it is not a child count. UID0001HK's existing `Nested:0` also remains unchanged.

## Direct Xref / Caller Inventory

`xrefs_to` and code-reference search return exactly three incoming code references and no data references:

| Call address / caller | Exact condition | Post-call behavior outside UID0003RA |
| --- | --- | --- |
| `0x005708fb` / UID0003QZ `SelfLookPane2::OnActivate` | After rebuilding content and refreshing Spelled state: mode 1 calls ShowSpelledView; mode 2 calls ShowLegendView; mode 0 calls neither. | Activation then returns; target does not own packet parsing or row construction. |
| `0x00571c86` / UID0003RC key handler | Translated key `0x94` (`kPaneKeyPageDown`), zero modifier, current mode 1. | Recognized key is consumed. Receiver is adjusted from the EventHandler facet back to the complete SelfLookPane2 object. |
| `0x00571fda` / UID0003RD input handler | Hit-test/action case 4 (next view), current mode 1. Mode 0 calls ShowSpelledView; mode 2/other makes no target call. | Handler updates its own hover/pressed fields and returns true; those stores are not target behavior. |

The complementary previous-view routes do not call UID0003RA: PageUp/case 3 move mode 2 to ShowSpelledView and mode 1 to ShowDefaultView.

## Documentation Evidence And IDA Status

- IDA models target `sub_570EF0` at exact size `0xc3`; `0x00570fb3` is not a function start, and next modeled function is `sub_570FC0` at `0x00570fc0`.
- IDA's decompiler produces one straight-line body and a misleading `int` return inherited from the final virtual call. Disassembly/callers/class declaration resolve it to source `void`.
- `entity_query` found decorated LegendPane and SelfLookPane2 vtable-family names, but no original target symbol. `type_query` found no recovered `SelfLookPane2` type. Source names therefore remain documented inference, not alleged debug symbols.
- The target page's former blank-formal statement was historical pre-callback state. UID0003RA now contains R1 and validator command `000000010778` accepted it.
- The class declaration remains structurally ready and unchanged: it declares `ShowLegendView`, supplies exact fields, and closes before `[[CHILDREN]]`. No class-formal replacement was made.
- Historical evidence-time generated command `000000010753` emitted one UID0003RA Empty Emitter Marker. B003 waited command `000000010799` emitted one exact namespace-scope UID0003RA definition and no target Empty Emitter Marker; latest observed external refresh `000000010803` preserves that definition/negative result. Generated C++ was inspected read-only and never edited directly.

## Ranked Ownership Analysis

| Rank | Candidate | Evidence for | Evidence against / disposition |
| ---: | --- | --- | --- |
| 1 | UID0000CV `SelfLookPane2` | Receiver state and fields, all three callers within class methods, parent passed as `this`, inherited bounds invalidation, existing declaration and emitter route. | No material contradiction. Accepted owner/emitter. |
| 2 | UID0000NL `SelfLookPane.cpp` | Existing class family route, address-neighbor method inventory, generated class/callers, shared source grouping. | Exact original filename not symbol-proven; retain as strongest current route, not as absolute lexical proof. |
| 3 | UID000074 LegendPane | Target invokes LegendPane/ScrollablePane behavior and attaches it. | Target writes SelfLookPane2 mode, manages multiple SelfLookPane2 child pointers, and is called only by SelfLookPane2. Dependency only. |
| 4 | Pane/ScrollablePane support | Virtual/helper implementations supply the called APIs. | They do not own the mode, geometry constants, child selection, or callers. Dependency only. |
| 5 | UID0001HK aggregate | Contains the address and inventories the child. | Deliberately non-emitting split index; duplicate emission would be invalid. |
| 6 | UserLook/SelfLook sibling methods | Similar view-switch source shape and naming. | Different class, fields, modes, bounds, child sets, and scroll behavior. Comparator only; reject ownership/factorization. |

## Source Placement

- The definition emits through UID0000CV in existing UID0000NL route `NexusTK/ui/panels/SelfLookPane.cpp`.
- Blank `EMITTER_POSITION_OPTIONAL` is preserved. Existing class children emit in stable address/source-family order, so no unique explicit position was introduced.
- Source-family order is implemented after UID0004NA ShowProfileView and before UID0003RB OnPaint, matching the exact address inventory.
- UID0001HK, LegendPane, ScrollablePane, a global helper, and manually authored vtable/table source remain rejected emitter routes.
- No new declaration, header, source file, owner, emitter, or child UID was introduced.

## Range / Split / Padding / Reclassification Analysis

- Exact target interval `[0x00570ef0,0x00570fb3)` and size `0xc3`/195 bytes are preserved.
- The one-byte predecessor pad `[0x00570eef,0x00570ef0)` remains parent-only, as already documented.
- The independently proven successor pad `[0x00570fb3,0x00570fc0)` is now in UID0001HK boundaries as 13 CC bytes (Verified with `tools/int_convert.py`), parent-only and excluded from both UID0003RA and UID0003RB.
- Target remains unsplit. It has one entry, one source path, one return, no branch, no interior xref, and one coherent method contract.
- Only source quality was reclassified from blank-formal helper to complete emitting method. Range, title, owner, emitter, reconstructable state, optional position, and `Nested:0` were not reclassified.
- Following child ordering and nesting are preserved. No cumulative relative nesting level changed.

## Negative Evidence Summary

- No access to `m_profilePane +0x100`; do not detach, unregister, attach, initialize, or mode-route ProfilePane here.
- No null check for either child pointer; do not add defensive branches.
- No branch at all; do not conditionalize detach, scroll reset, attach, order, or invalidation.
- No `GetScrollMax`, second `GetScrollPosition`, or second `ApplyScrollPosition`; do not normalize toward the larger SelfLook sibling.
- No LegendPane text construction or selection operation; UID0003QZ owns row construction before restore.
- No registration call after `AddToLayer`; preserve only observed AddToLayer and SetPaneOrder.
- No data/immediate/pointer/vtable target route. This is nonvirtual and directly called, not an omitted vtable member.
- No original target/type symbol. Do not claim exact lexical access or physical filename certainty.
- No compiler-only/no-code rationale: three live callers and a complete body reject an Empty Emitter Marker disposition.
- No source factorization with UserLook/SelfLook sibling bodies; geometries and scroll sequences differ materially.
- No manual vtable, RTTI, cookie, stack frame, raw offset, or explicit padding source.

## IDA Rename / Type / Comment Recommendations

These are documentation/source-facing recommendations only. This report did not mutate IDA.

| Address/entity | Recommended source-facing identity | Rationale / caution |
| --- | --- | --- |
| `0x00570ef0` | `SelfLookPane2::ShowLegendView` | Strongest mode/caller/family name; inferred, not recovered symbol. |
| Function prototype | `void __thiscall SelfLookPane2::ShowLegendView()` at ABI level; source omits explicit `__thiscall` | EAX tail artifact rejected; three callers ignore return. |
| local `[ebp-0x14]` | `RectBounds legendBounds` | Exact 16-byte rectangle local and initializer. |
| `0x0055e950` | `ScrollablePane::GetScrollPosition(char axis)` | Existing accepted helper name; returns selected child current word. |
| `0x0055eae0` | `ScrollablePane::ApplyScrollPosition(char axis, unsigned short oldPosition, unsigned short targetPosition)` | Exact old-before-target contract. |
| `0x0069b368` | `g_mainUiLayerSlots.rootPaneLayerContext` | Existing global-layout identity; exact original global spelling remains inferred. |

Recommended comments should capture only non-obvious behavior: "Reset the existing LegendPane layer/event state before reattaching" and "Apply vertical scroll from the observed current position to zero." Avoid raw offsets or decompiler pseudocode in source comments.

## First-Draft C++ Recommendation

### Destination R1: UID0003RA target formal block

Replace the target's blank managed value with this exact block:

*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SelfLookPane2::ShowLegendView()
{
    m_viewMode = ViewMode_Legend;

    m_spelledPane->RemoveFromLayer();
    m_spelledPane->UnregisterEventHandler();

    m_legendPane->RemoveFromLayer();
    m_legendPane->UnregisterEventHandler();

    RectBounds legendBounds;
    InitRectBounds(&legendBounds, 3, 10, 183, 239);

    const unsigned short currentPosition =
        m_legendPane->GetScrollPosition(0);
    m_legendPane->ApplyScrollPosition(0, currentPosition, 0);
    m_legendPane->AddToLayer(&legendBounds,
                             0,
                             this,
                             g_mainUiLayerSlots.rootPaneLayerContext);
    m_legendPane->SetPaneOrder(0, this);

    InvalidateRect(&m_bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

No second formal destination is required. UID0000CV already contains the exact `void ShowLegendView();` declaration, required fields/types, complete class close, and `[[CHILDREN]]` placement. UID0001HK and all prose-only supports remain blank-formal/non-duplicating as currently defined.

## Final Recommendation

UID0003RA is implemented as a source-ready `SelfLookPane2::ShowLegendView` method with R1 unnormalized. The target is `91/94`, all routing/nesting metadata is preserved, both adjacent parent-only pads are documented, and the exact three-caller transition contract is synchronized into bounded support pages. Historical support reports remain evidence leads only; none substitutes for this direct pass.

The source body must retain the non-obvious removal of the existing LegendPane before reattachment and the exact current-to-zero vertical scroll update. It must not acquire a ProfilePane branch, max-scroll step, return expression, null guard, helper split, or manual compiler data.

## Recommended Target Doc Changes

Implemented for `by-memory/0x00570ef0-0x00570fb3.SelfLookPane2ShowLegendView.md` UID0003RA:

1. Set `COMPLETION:91` and `CONFIDENCE:94`; preserved owner/emitter UID0000CV, reconstructable true, blank optional position, `Nested:0`, exact path/range, and title.
2. Set Item Summary exactly to:

   `SelfLookPane2 mode-2 ShowLegendView method with three class-internal callers, exact Spelled/Legend detach-unregister order, (3,10,183,239) bounds, vertical current-to-zero scroll reset, root-layer attachment, pane ordering, inherited-bounds invalidation, and complete formal C++ through the class emitter.`

3. Replaced the blank formal block with R1 byte-for-byte.
4. Added exact range, 195-byte size, hash, first/last bytes, ret, predecessor one-CC pad, and successor 13-CC pad evidence.
5. Added the full ordered operation table, field/type table, direct/indirect callee contracts, three-caller transition conditions, ABI/void rationale, and source-name/access analysis.
6. Preserved the UID0003QZ mode-restore support and historical B001 split evidence, while historicalizing blank-body/source-blocker wording.
7. Added explicit negatives for ProfilePane, null checks, max scroll, second scroll, branches, data/vtable routes, body factorization, compiler/no-code treatment, and manual table/cookie source.
8. Added a Changes entry citing this report, MCP evidence-time session, exact score/formal transition, preserved metadata, and scoped validator `000000010778`.

## Recommended Support Doc Changes

| Destination | Exact implemented bounded change | Preserved state |
| --- | --- | --- |
| UID0001HK `by-memory/0x0056fe80-0x005729c3.SelfLookPane2.md` | UID0003RA child role is source-ready mode-2 ShowLegendView with exact three callers/R1 behavior; `[0x00570fb3,0x00570fc0)` is recorded as 13 parent-only CC bytes; target hash/body/no-split are present. Validator `000000010780`. | Kept `89/92`, owner UID0000CV, non-emitting/blank formal, route, all children/padding/history, `Nested:0`. |
| UID0000CV `by-class/SelfLookPane2.md` | ShowLegendView method/evidence now records the exact mode store, fields, detach order, geometry, scroll contract, layer/order/invalidation, three callers, source-ready target link, retained public convention, and lexical cap. Validator `000000010782`. | Kept `92/93`, class formal byte-for-byte, layout, owner/emitter, route, all unrelated method/history detail. |
| UID0000NL `by-file/SelfLookPane.md` | UID0003RA is in the source-ready inventory and exact source/compiler evidence after Profile helper and before OnPaint; shared LegendPane ownership remains. Validator `000000010790`. | Kept `90/92`, generated route, formal, score, all SelfLook/UID0003QZ/B011 work. |
| UID0003RC `by-memory/0x00571c10-0x00571cb2.SelfLookPane2HandleKeyEvent.md` | Exact unmodified translated PageDown `0x94` transition now states mode0 -> ShowSpelled, mode1 -> UID0003RA ShowLegend, nonzero other -> consume/no call, with complete-object receiver adjustment context. Validator `000000010792`. | Kept `85/89`, owner/emitter, blank formal, route, range, nesting, and broader unresolved handler blockers. |
| UID0003RD `by-memory/0x00571cc0-0x005720ff.SelfLookPane2HandleEvent.md` | Exact action case4 transition now states mode0 -> ShowSpelled, mode1 -> UID0003RA ShowLegend, other -> no view call; target call precedes caller-owned hover/pressed updates. Validator `000000010794`. | Kept `86/90`, owner/emitter, blank formal, route, range, nesting, and broader unresolved handler blockers. |

Verify-only dependencies were reread and left unchanged because no contradiction was found: UID0003QZ, UID0003R8, UID0003R9, UID0004NA, UID0003RB, UID000074 LegendPane, ProfilePane, SpelledPane, Pane/ScrollablePane support, MainUiLayerSlots, Event/key constants, vtable inventories, and all UserLook/SelfLook sibling pages.

## Score And Metadata Recommendation

| Field | Pre-callback | Implemented | Rationale |
| --- | ---: | ---: | --- |
| Completion | 85 | 91 | Exact bytes/hash/range, complete operation sequence, fields/types, callers/callees, source identity, formal body, padding, ownership, support plan, and coverage text are closed. |
| Confidence | 89 | 94 | Live MCP independently confirms every behavioral input/output and route. Original lexical method/access names and physical source filename remain inferred and cap confidence. |
| Canonical owner | 0000CV | 0000CV | SelfLookPane2 state/callers prove direct ownership. |
| Emitter | 0000CV | 0000CV | Existing complete class declaration is the correct child emitter. |
| Reconstructable | true | true | Live directly called source method with complete body. |
| Optional position | blank | blank | Existing address/source order suffices. |
| Nested | 0 | 0 | No split and no relative-level change. |

No support score changes are justified. Bounded caller prose does not close their remaining full-body blockers, and parent/class/file are already scored for broader evidence sets.

## Open Questions With Attempted Resolution

| Former question | Checks performed | Resolution and score effect |
| --- | --- | --- |
| Is IDA's `int` return source-real? | Disassembly epilogue, decompile, caller EAX use, class declaration, sibling conventions. | No. Use `void`; no remaining C++ blocker. |
| Is `ShowLegendView` exact original spelling? | Current declaration, all caller roles, sibling UserLook names, entity/type searches, historical reports. | Strongest source-facing name, but not symbol-proven. Retain and cap confidence below 100. |
| Should it be private? | Code/data/vtable reachability, caller scope, current class formal. | Internal-only makes private plausible; binary cannot prove access. Preserve current public declaration and record lexical uncertainty. |
| Does it detach ProfilePane? | Full 63-instruction disassembly and field access inventory. | No. Only Spelled and Legend are touched. |
| Is Legend removed before or after unregister? | Exact vslot call order and Pane slot map. | Remove then unregister, exactly. |
| Is scroll old/new order reversed? | Push lifetime across getter, callee argument frame, callee comparison/store. | Old/current first, target zero second. R1 exact. |
| Should the view scroll to maximum? | Target callee list, sibling `0x00567380`, ScrollablePane helper inventory. | No max call in target; preserve reset to zero only. |
| Is the global raw array indexing? | Data address, current MainUiLayerSlots layout and consumers. | Use accepted named `rootPaneLayerContext`; exact original global spelling remains a minor lexical cap. |
| Is an internal split needed? | CFG/disassembly/xrefs/interior routes. | No; one straight-line method. |
| Should LegendPane own it? | State writes, callers, receiver, parent argument, class source route. | No; LegendPane is consumed, not owner. |

No open question remains that blocks R1, the score increase, or the bounded destination list.

## Exact Manual Supervisor-Owned Coverage Or Tracker Text

B003 does not edit manual `-coverage-report.md` files. Read-only comparison found current by-memory coverage has UID0001HK, UID0003QZ, and UID0004NA rows but no UID0003RA/UID0003RC/UID0003RD rows; by-class and by-file rows predate this direct target body. The following is exact external replacement/addition text.

In `by-memory/-coverage-report.md`, replace the current UID0001HK row with:

```markdown
    - [UID:0001HK][0x0056fe80-0x005729c3.SelfLookPane2](by-memory/0x0056fe80-0x005729c3.SelfLookPane2.md) 0x0056fe80-0x005729c3 | non-emitting class-method split index | SelfLookPane2 : not_reconstructable : 89% : very-strong : Exact SelfLookPane2 constructor/destructor/OnActivate/view/paint/input/packet/stat/button/raw-helper inventory with complete 0xd90 layout, retained raw ShowProfileView, source-ready UID0003RA ShowLegendView, all internal padding including the 13-byte post-legend alignment, compiler exclusions, and exact child-only source emission through SelfLookPane2.
```

Immediately after the existing UID0004NA row and before the UID0001HK hierarchy returns to the next top-level row, add these address-ordered rows:

```markdown
        - [UID:0003RA][0x00570ef0-0x00570fb3.SelfLookPane2ShowLegendView](by-memory/0x00570ef0-0x00570fb3.SelfLookPane2ShowLegendView.md) 0x00570ef0-0x00570fb3 | method | SelfLookPane2ShowLegendView : reconstructable : 91% : very-strong : Exact 195-byte/SHA256-anchored mode-2 ShowLegendView with three class-internal callers, Spelled/Legend remove-unregister order, (3,10,183,239) bounds, axis-0 current-to-zero scroll application, root-layer attach, pane order, inherited-bounds invalidation, complete formal C++, and adjacent parent-only padding.
        - [UID:0003RC][0x00571c10-0x00571cb2.SelfLookPane2HandleKeyEvent](by-memory/0x00571c10-0x00571cb2.SelfLookPane2HandleKeyEvent.md) 0x00571c10-0x00571cb2 | key handler | SelfLookPane2HandleKeyEvent : reconstructable : 85% : strong : Vtable-routed key handler with exact unmodified translated PageDown mode0-to-Spelled and mode1-to-UID0003RA Legend transitions; full handler formal C++ remains blocked by broader key/event naming.
        - [UID:0003RD][0x00571cc0-0x005720ff.SelfLookPane2HandleEvent](by-memory/0x00571cc0-0x005720ff.SelfLookPane2HandleEvent.md) 0x00571cc0-0x005720ff | input/event handler | SelfLookPane2HandleEvent : reconstructable : 86% : very-strong : Vtable-routed input handler with exact case-4 next-view mode0-to-Spelled and mode1-to-UID0003RA Legend transitions before caller-owned hover/pressed updates, plus existing button, packet, and helper evidence.
```

In `by-class/-coverage-report.md`, replace the UID0000CV row with:

```markdown
- [UID:0000CV][SelfLookPane2](by-class/SelfLookPane2.md) : reconstructable : 92% : very-strong : Complete PanelPane-derived 0xd90 SelfLookPane2 declaration with exact natural layout, primary +0x48 OnActivate, source-ready UID0003RA ShowLegendView mode/child/scroll/layer contract, resource/Spelled/Profile/Legend pointers, LivingSpriteConfig/equipment/status/toggle/text/view fields, exact method/raw-helper inventory, class closure before child definitions, and descriptive-name confidence caps.
```

In `by-file/-coverage-report.md`, replace the UID0000NL row with:

```markdown
- [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) : reconstructable : 90% : very-strong : NexusTK/ui/panels/SelfLookPane.cpp source grouping with complete SelfLookPane/SelfLookPane2 split inventories, exact UID0003QZ OnActivate and UID0003RA ShowLegendView source, retained raw UID0004NA ShowProfileView, packet/status/equipment/view behavior, class/vtable/compiler distinctions, helper/destructor islands, shared child-pane ownership, and original filename/name confidence caps.
```

No manual tracker row is requested outside these exact coverage rows. Validator-owned generated tracker/coverage state remains external and must not be edited manually.

## Follow-Up Actions

- The authorized C01-C20/R1 implementation, serial scoped validation, final waited refresh, generated readback, ledger reconciliation, and checklist reconciliation are complete; no B003 implementation item remains.
- Verify-only dependencies remained unchanged because the callback reread found no concrete contradiction.
- Exact manual coverage replacement/addition text remains recorded for external supervisor handling; B003 did not edit manual coverage.
- Report validation, manual coverage application, lifecycle/count/path/move/archive state, and execution are external supervisor/validator-owned state and are neither asserted nor directed by this artifact. B003 performed no report probe, `execute_report`, lifecycle, move, or archive command.

## Confidence

- Exact range/bytes/hash/padding: very strong.
- ABI/body/order/fields/types/callers/callees: very strong.
- Owner/emitter/source family: very strong.
- Source-facing method/API/global names: strong to very strong from current project contracts and sibling evidence.
- Exact original access level and physical filename: unresolved lexical details, explicitly capped and non-blocking.
- Implemented target score: completion `91`, confidence `94`.

## Validator Results

Each ordinary page was leased alone immediately before editing, scoped-validated while leased, and released before the next lease. Every command exited `0` with `ok:1`.

| Destination / scope | Command ID | Timestamp | Exit / ok | Exact relevant result and validator-owned side effects |
| --- | --- | --- | --- | --- |
| UID0003RA target | `000000010778` | `2026-07-14T05:10:16-04:00` | `0 / 1` | Accepted `91/94`, R1, and target metadata; autogen registry changed the target from blank body to the managed block; projected stats updated; generated refresh deferred. Five existing missing-support-UID diagnostics were reported. |
| UID0001HK parent | `000000010780` | `2026-07-14T05:11:11-04:00` | `0 / 1` | Accepted source-ready child/padding evidence and preserved parent metadata/formal; projected stats updated; generated refresh deferred. Twenty-six existing unmapped child-reference diagnostics were reported. |
| UID0000CV class | `000000010782` | `2026-07-14T05:11:57-04:00` | `0 / 1` | Accepted bounded method/evidence/history prose, added the UID0003RA reference index edge, and preserved the managed class block; projected stats updated; generated refresh deferred. Seventeen existing missing-reference diagnostics were reported. |
| UID0000NL file | `000000010790` | `2026-07-14T05:13:09-04:00` | `0 / 1` | Accepted exact source inventory/order/evidence, added the UID0003RA reference index edge, and preserved route/score/formal; projected stats updated; generated refresh deferred. Thirty existing missing-reference diagnostics were reported. |
| UID0003RC key caller | `000000010792` | `2026-07-14T05:14:03-04:00` | `0 / 1` | Accepted bounded PageDown contract; validator registered the formerly unmapped existing UID0003RC path/header and added five reference edges; projected stats updated; generated refresh deferred. Two existing UID0003R8 diagnostics were reported. |
| UID0003RD event caller | `000000010794` | `2026-07-14T05:14:49-04:00` | `0 / 1` | Accepted bounded case-4 contract; validator registered the formerly unmapped existing UID0003RD path/header and added five reference edges; projected stats updated; generated refresh deferred. Five existing support-reference diagnostics were reported. |
| Final waited `by-file/SelfLookPane.md` refresh | `000000010799` | `2026-07-14T05:16:29-04:00` | `0 / 1` | `generated_refresh: completed`; generated header is fresh at the same command/timestamp. Validator-owned side effects included registry rebuild (`4750` metadata nodes, `3848` edges), `281` generated metadata refreshes, projected stats update, and thirty existing missing-reference diagnostics. |

Read-only generated verification of `auto-generated/NexusTK/ui/panels/SelfLookPane.cpp` proved the B003 waited result at command `000000010799`; a post-report-edit reread of latest external command `000000010803` confirmed the same target body, uniqueness, structure, and negative assertions:

- B003 waited header command/timestamp was exactly `000000010799` / `2026-07-14T05:16:29-04:00`; latest observed header command/timestamp is external validator-owned `000000010803` / `2026-07-14T05:19:10-04:00`.
- `SelfLookPane2` closes at generated line 656, before child definitions; UID0003RA begins at line 881 after the one UID0004NA `ShowProfileView` definition. B003 command `000000010799` also placed it before the UID0003RB marker. Latest external command `000000010803` omits that non-emitting marker, while the unchanged exact address/source inventory still places UID0003RA before UID0003RB's later `[0x00570fc0,0x00571c05)` range.
- Exactly one UID0003RA marker and one `void SelfLookPane2::ShowLegendView()` definition exist; zero UID0003RA Empty Emitter Markers exist.
- Line-ending-normalized SHA256 for R1 is `E2993169FDB6AA231EE56F7FB77AAB4528DB8A5EF8F66DD1E30FB6A7C9C8767F` in the report, UID0003RA target, and generated definition; all three blocks compare equal.
- Every emitted `SelfLookPane2` qualified definition is unique (`OnActivate`, `ShowProfileView`, and `ShowLegendView`, each once), with no duplicate UID marker or wrong-range emitter.
- The generated UID0003RA body matches R1 and contains no `m_profilePane`, null guard, `GetScrollMax`, return statement, manual vtable/RTTI array, security-cookie source, or raw decompiler artifact.
- Existing compiler no-code text remains prose-only; no manual vtable/RTTI array declaration is present.

All listed missing-reference rows are pre-existing support-registration diagnostics, not command failures and not authorization to broaden this callback. No generated, manual coverage, tracker, audit, supervisor, validator-state, lifecycle, archive, or IDA file was manually edited by B003.

## Changed Files

- `by-memory/0x00570ef0-0x00570fb3.SelfLookPane2ShowLegendView.md` (UID0003RA): target metadata, exact R1, and full accepted evidence/history/score detail.
- `by-memory/0x0056fe80-0x005729c3.SelfLookPane2.md` (UID0001HK): bounded child/padding/source-ready synchronization.
- `by-class/SelfLookPane2.md` (UID0000CV): bounded method/access/caller/evidence synchronization; managed class block unchanged.
- `by-file/SelfLookPane.md` (UID0000NL): bounded source inventory/order/evidence synchronization.
- `by-memory/0x00571c10-0x00571cb2.SelfLookPane2HandleKeyEvent.md` (UID0003RC): bounded PageDown caller contract.
- `by-memory/0x00571cc0-0x005720ff.SelfLookPane2HandleEvent.md` (UID0003RD): bounded case-4 caller contract.
- B003 callback artifact before external lifecycle: `tools/leaser/Agents/Agent-B003/research/0003RA-SelfLookPane2ShowLegendView-source-quality.md` (this claim-by-claim reconciliation).
- Verify-only dependencies changed by B003: none. Manual coverage/generated/tracker/audit/supervisor/validator-state/lifecycle/archive/IDA files manually changed by B003: none. Report lifecycle commands run by B003: none.

## Implementation Tracking Checklist

All report-only and callback items are checked only against completed evidence below.

- [x] Reopened exact accepted report SHA `EB31DA4E5C2731F446475B6A9F6365C4D10EDE59215BE147B0763B2835332919` and confirmed no current contradiction before editing.
- [x] Acquired only a short one-file lease for UID0003RA immediately before edit.
- [x] Applied C01-C20 claim by claim without compression; applied C01-C17 to UID0003RA at full report detail, including `91/94`, exact bytes/hash/range/padding, ABI, sequence, fields, callers/callees, negatives, history, score rationale, Item Summary, and R1.
- [x] Preserved UID0003RA owner/emitter UID0000CV, reconstructable true, blank optional position, `Nested:0`, exact range/title/path, and no split.
- [x] Scoped-validated UID0003RA under command `000000010778` and released its lease immediately.
- [x] Leased/edited/validated/released UID0001HK under command `000000010780` with the exact source-ready child role and 13-byte successor parent-only padding, preserving all other children/history/scores/routes/nesting.
- [x] Leased/edited/validated/released UID0000CV under command `000000010782` with the exact ShowLegendView contract/access-name cap while preserving its managed class block byte-for-byte and keeping `[[CHILDREN]]` after the class close.
- [x] Leased/edited/validated/released UID0000NL under command `000000010790` with the exact source inventory/route update while preserving all unrelated SelfLook/UID0003QZ/B011 evidence and score.
- [x] Leased/edited/validated/released UID0003RC under command `000000010792` with only the exact PageDown transition prose, preserving score/route/formal and broader blockers.
- [x] Leased/edited/validated/released UID0003RD under command `000000010794` with only the exact case-4 transition prose, preserving score/route/formal and broader blockers.
- [x] Reread verify-only UID0003QZ, UID0003R8, UID0003R9, UID0004NA, UID0003RB, LegendPane, ProfilePane, SpelledPane, Pane/ScrollablePane, MainUiLayerSlots, Event/key, vtable, UserLook, and SelfLook support; found no contradiction and left them unchanged.
- [x] Ran final authorized `--wait-generated` command `000000010799`; recorded command ID/timestamp/exit/ok/side effects and matching generated header freshness.
- [x] Verified generated `SelfLookPane.cpp` has the class closed before child definitions, exactly one UID0003RA `ShowLegendView` body, each emitted SelfLookPane2 definition once, zero UID0003RA Empty Emitter Markers, no duplicate/wrong-range emitter, and no manual vtable/RTTI array.
- [x] Reread manual coverage rows and preserved the exact external replacement/addition text without B003 editing coverage files.
- [x] Updated every C01-C20 ledger row to a permitted final callback state with exact destination and validator/generated proof.
- [x] Updated Finalized/Current State, Validator Results, Changed Files, and this checklist only after each fact was independently true.
- [x] Confirmed no B003 lease remains and no forbidden generated/coverage/tracker/audit/supervisor/validator/lifecycle/IDA file was manually changed.
- [x] Preserved report lifecycle wording as external supervisor/validator-owned state; B003 ran no `execute_report`, lifecycle, move, or archive command.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B003","command_id":"000000010811","destination_path":"executed-b-agent-research/B003/0003RA-SelfLookPane2ShowLegendView-source-quality.md","details":"report executed into central archive","event":"executed","source_path":"tools/leaser/Agents/Agent-B003/research/0003RA-SelfLookPane2ShowLegendView-source-quality.md","timestamp":"2026-07-14T05:33:13-04:00","uid":"0003RA"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
