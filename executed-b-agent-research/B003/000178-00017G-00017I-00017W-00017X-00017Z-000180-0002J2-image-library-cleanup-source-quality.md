** TARGET-REPORT-UID:000178 **
** AUTHOR-AGENT-ID:B003 **
** AUTHOR-AGENT-SPECS: CHATGPT | 5.5 | xHigh **
# B003 Image-Library Cleanup Source-Quality Audit

Assignment: `B003-goal2-image-library-destructor-singleton-cleanup-source-quality-000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-20260617`

Agent: Agent-B003

Date: 2026-06-17

Scope: report-only audit for [UID:000178], [UID:00017G], [UID:00017I], [UID:00017W], [UID:00017X], [UID:00017Z], [UID:000180], and [UID:0002J2]. I did not edit by-* docs, generated files, source files, the IDA database, or `by-memory/-coverage-report.md`.

## Target Recommendations

| UID | Target | Current | Recommended | Owner / emitter | C++ recommendation |
| --- | --- | ---: | ---: | --- | --- |
| [UID:000178] | `0x004d19a0-0x004d1a1c.MapTileImageLibDestructor` | `82/90` | `88/92` | keep `CANONICAL_OWNER:00007T`, `EMITTER_UIDS:00007T` | Populate first-draft ordinary destructor C++. |
| [UID:00017G] | `0x004dc730-0x004dc7d8.RidingImageLibDestructor` | `82/90` | `87/91` | keep `CANONICAL_OWNER:0000BZ`, `EMITTER_UIDS:0000BZ` | Populate first-draft ordinary destructor C++. |
| [UID:00017I] | `0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor` | `82/90` | `87/91` | change to `CANONICAL_OWNER:0000E2`, `EMITTER_UIDS:0000E2` | Populate first-draft ordinary destructor C++. |
| [UID:00017W] | `0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper` | `82/90` | `85/90` | keep `CANONICAL_OWNER:0000IY`, `EMITTER_UIDS:0000IY` | Keep formal C++ blank; compiler/static cleanup helper. |
| [UID:00017X] | `0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper` | `82/90` | `85/90` | keep `CANONICAL_OWNER:0000JY`, `EMITTER_UIDS:0000JY` | Keep formal C++ blank; compiler/static cleanup helper. |
| [UID:00017Z] | `0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper` | `82/90` | `85/90` | keep `CANONICAL_OWNER:0000KP`, `EMITTER_UIDS:0000KP` | Keep formal C++ blank; compiler/static cleanup helper. |
| [UID:000180] | `0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper` | `82/90` | `85/90` | keep `CANONICAL_OWNER:0000L5`, `EMITTER_UIDS:0000L5` | Keep formal C++ blank; compiler/static cleanup helper. |
| [UID:0002J2] | `0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor` | `82/90` | `87/92` | keep `CANONICAL_OWNER:00007T`, `EMITTER_UIDS:00007T` | Keep formal C++ blank; compiler scalar deleting wrapper. |

All eight targets clear the active source-quality gate: each is reconstructable, has a nonblank emitter route, and has or should have a combined score above 85. The gate result does not mean every target receives hand-authored C++. The three ordinary destructors are source-authored cleanup bodies and should get first drafts. The four 0xb singleton helpers and the MapTile scalar deleting destructor are generated cleanup/wrapper code and should stay blank in formal C++.

## Evidence Checked

Primary target docs:

- `by-memory/0x004d19a0-0x004d1a1c.MapTileImageLibDestructor.md`
- `by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md`
- `by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md`
- `by-memory/0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper.md`
- `by-memory/0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper.md`
- `by-memory/0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper.md`
- `by-memory/0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper.md`
- `by-memory/0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor.md`

Support docs and accepted precedent checked:

- `by-class/MapTileImageLib.md`, `by-file/MapTileImageLib.md`, `by-type/by-struct/MapTileImageLibLayout.md`, `by-type/by-struct/MapTileRecord.md`, `by-global/g_pMapTileImageLib.md`, and vtable/storage pages.
- `by-class/RidingImageLib.md`, `by-file/RidingImageLib.md`, `by-type/by-struct/RidingImageLibLayout.md`, `by-type/by-struct/RidingDefinition.md`, `by-global/g_pRidingImageLib.md`, and vtable/storage pages.
- `by-class/StaticObjImageLib.md`, `by-file/StaticObjImageLib.md`, `by-type/by-struct/StaticObjImageLibLayout.md`, `by-type/by-struct/StaticObjEntry.md`, `by-type/by-struct/TileClassEntry.md`, `by-global/g_pStaticObjImageLib.md`, and vtable/storage pages.
- `by-item/EPFArchiveMetadataTable.md` for `ArchiveMetadataTable` and `PackedArchiveRecord` layout.
- Sibling helper pages [UID:000183] `RidingImageLibSingletonClearHelper` and [UID:000184] `StaticObjImageLibSingletonClearHelper`, already accepted at `85/90` with by-file cleanup-glue ownership and blank formal C++.
- [UID:0003M2] `StaticObjImageLibScalarDeletingDestructor`, accepted class-owner wrapper precedent.
- Agent-B003 executed LightObjImageLib report, especially [UID:00017Q] ordinary destructor C++ and [UID:0002IY] scalar deleting destructor blank-C++ policy.
- Agent-B003 executed MapTile source-quality report for `tileFrameTable`, `tileRecords`, `paletteIndex`, `allowPaletteFilter`, and `LoadTileEpfMetadata` source placement.
- `source-3/simroot_v2/class_MapTileImageLib.cpp`, `class_RidingImageLib.cpp`, `class_StaticObjImageLib.cpp`, `class_EffectObjImageLib.cpp`, `class_HumanImageLib.cpp`, and `class_LightObjImageLib.cpp` as source leads only.
- `source-3/project-documentation/auto-generated/NexusTK/render/*.cpp`: Effect/Human/MapTile/Riding/Static outputs are still zero length; Light output is populated from the accepted B003 Light pass.

Live IDA MCP was unavailable in this session. A direct endpoint request timed out, and an earlier MCP tools-list attempt could not connect. I therefore used the already documented live IDA evidence in the target/support pages and performed a local PE byte/vtable check against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`.

> The removed literal PowerShell command is preserved only in [000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-image-library-cleanup-source-quality-removed.md](000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-image-library-cleanup-source-quality-removed.md). The archive is non-authoritative and must not be executed.

Local PE byte/vtable facts checked:

```text
MapTile dtor start:    55 8b ec 6a ff 68 e3 f9 5f 00 64 a1 00 00 00 00
Riding dtor start:     55 8b ec 6a ff 68 74 ff 5f 00 64 a1 00 00 00 00
Static dtor start:     55 8b ec 6a ff 68 cd ff 5f 00 64 a1 00 00 00 00
Effect clear:          c7 05 4c b4 69 00 00 00 00 00 c3 cc cc cc cc cc
Human clear:           c7 05 3c b4 69 00 00 00 00 00 c3 cc cc cc cc cc
Light clear:           c7 05 50 b4 69 00 00 00 00 00 c3 cc cc cc cc cc
MapTile clear:         c7 05 5c a7 67 00 00 00 00 00 c3 cc cc cc cc cc
MapTile scalar start:  55 8b ec 6a ff 68 70 a0 5f 00 64 a1 00 00 00 00
MapTile vtable slot:   0x0061b664 -> 0x004e66a0
Riding vtable slot:    0x0061b6f4 -> 0x004e68b0
Static vtable slot:    0x0061b704 -> 0x004e6990
```

The clear-helper island byte run confirms each helper is exactly `mov dword ptr [global], 0; ret` with five `0xcc` alignment bytes before the next helper. This independently supports the existing by-memory boundary docs and the no-code helper policy.

## Heuristic / Inference Reanalysis And Validation

### Gate And Source-Form Policy

The active rule is not the stale `95/95` gate still mentioned in some pages. These exact by-memory children are eligible for first-draft consideration once they are reconstructable, have a valid emitter, and clear the source-quality threshold. The correct next decision is source-form:

- Source-authored ordinary destructor bodies should receive first-draft `RECONSTRUCTION_CPP`.
- Compiler scalar deleting destructors should not receive first-draft C++; the source declaration plus ordinary destructor body should regenerate them.
- Constructor EH/static cleanup helpers that only clear a singleton should stay blank; the source should express the constructor/static lifetime that caused them, not a hand-authored public helper API.

This policy validates the current blank-C++ direction for [UID:0002J2] but contradicts the stale "blank until class-wide final names" wording for [UID:000178], [UID:00017G], and [UID:00017I].

### [UID:000178] MapTileImageLib Ordinary Destructor

Best source-level name and type:

```cpp
MapTileImageLib::~MapTileImageLib()
```

Best field names:

```text
this + 0x04  ArchiveMetadataTable *tileFrameTable
this + 0x08  MapTileRecord *tileRecords
ArchiveMetadataTable + 0x08  PackedArchiveRecord *records
```

Evidence checked:

- Target page documents the exact `0x004d19a0-0x004d1a1c` range, no direct callers, MapTile vtable restore at `0x004d19ca`, `g_pMapTileImageLib` clear at `0x004d19f9`, allocator/free calls `0x00516030` and `0x00516170`, CRT free wrapper `0x005c7799`, and `LObject` cleanup.
- MapTile layout and class docs now standardize `tileFrameTable` and `tileRecords`.
- `EPFArchiveMetadataTable.md` defines the table header and record pointer at `+0x08`.
- Local PE vtable data confirms the deleting wrapper slot at `0x0061b664` points to [UID:0002J2], not this ordinary destructor, matching the no-direct-caller ordinary body pattern.
- [UID:0002J2] duplicates the same cleanup sequence and adds only delete flags.

Rejected alternatives:

- `tileMetadata` is acceptable prose but less precise than `tileFrameTable`, because `DrawTile` consumes the field as a frame-record table.
- `decodedTilePixels` from simroot_v2 is rejected as a final field name. The field at table `+0x08` is the shared `PackedArchiveRecord *records` pointer in the archive metadata table, not literal decoded pixels in this destructor.
- Keeping C++ blank because helper names are not original-spelling proof is rejected. Helper names can remain source-facing aliases in the draft, while the destructor's source behavior and ownership are fully bounded.

Impact:

- Raise to `88/92`.
- Keep owner/emitter `00007T`.
- Populate ordinary destructor C++ in `auto-generated/NexusTK/render/MapTileImageLib.cpp`.

### [UID:00017G] RidingImageLib Ordinary Destructor

Best source-level name and type:

```cpp
RidingImageLib::~RidingImageLib()
```

Best field/helper names:

```text
this + 0x04  RidingDefinition *ridingDefinitions
this + 0x08  uint32_t ridingDefinitionCount
this + 0x0c  ArchiveMetadataTable *ridingEpfPackage
DestroyRidingDefinitionArray(...)
FreeRidingDefinitionArray(...)
```

Evidence checked:

- Target page documents exact range `0x004dc730-0x004dc7d8`, vtable store at `0x004dc75a`, package payload/table frees, vector destructor iterator with element destructor `0x00528950`, allocation free via `0x005c7910`, base cleanup at `0x004dc7b7`, and singleton clear at `0x004dc7bc`.
- Riding layout page standardizes `ridingDefinitions`, `ridingDefinitionCount`, and `ridingEpfPackage`.
- `class_RidingImageLib.cpp` source lead uses `DestroyRidingDefinitionArray` and `FreeRidingDefinitionArray`, which are the best current source-facing aliases for the vector destructor iterator plus vector allocation free.
- [UID:0002Y4] scalar deleting destructor has vtable-only ownership and parity with this ordinary destructor, proving this target is the source-authored cleanup body.

Rejected alternatives:

- Leaving the ordinary destructor blank because the vector element destructor callback has a generated name is rejected. The source-level array/vector destruction intent is clear and can be represented through the two recovered helper aliases.
- Modeling this as a file-level helper is rejected. It has `this`, a vtable restore, class fields, ordinary/scalar destructor parity, and a class vtable relationship.
- Moving ownership to `by-file/RidingImageLib` is rejected for this target. By-structure direct-owner policy and existing scalar/layout pages route class methods and vtable artifacts through [UID:0000BZ] `RidingImageLib`; the class emits through [UID:0000N6].

Impact:

- Raise to `87/91`.
- Keep owner/emitter `0000BZ`.
- Populate ordinary destructor C++.

### [UID:00017I] StaticObjImageLib Ordinary Destructor

Best source-level name and type:

```cpp
StaticObjImageLib::~StaticObjImageLib()
```

Best field names:

```text
this + 0x04  ArchiveMetadataTable *tileCatalog
this + 0x08  TileClassEntry *tileClasses
this + 0x0c  StaticObjEntry **staticObjEntries
this + 0x10  int staticObjCount
StaticObjEntry + 0x08  StaticObjEntry *cacheChain
```

Evidence checked:

- Target page documents exact range `0x004dd1e0-0x004dd2be`, vtable store at `0x004dd20b`, static object pointer/cache-chain cleanup, tile catalog payload/table cleanup, tile-class free through `0x005c7799`, singleton clear at `0x004dd29b`, and base cleanup at `0x004dd2a7`.
- StaticObj layout page is already class-owned [UID:0000E2] and names `tileCatalog`, `tileClasses`, `staticObjEntries`, and `staticObjCount`.
- Static scalar deleting destructor [UID:0003M2] was already accepted with `CANONICAL_OWNER:0000E2`, `EMITTER_UIDS:0000E2`, blank formal C++, and class-owner wrapper policy. The ordinary destructor should use the same direct class owner.
- Current target owner/emitter `0000O7` is stale for a class method under direct-owner policy. It should be changed to class owner `0000E2`, which emits through file `0000O7`.

Rejected alternatives:

- Keeping owner/emitter as [UID:0000O7] is rejected for this ordinary destructor. File ownership is correct for singleton cleanup helpers and file-local loaders, but this function takes `this`, restores the class vtable, frees class fields, and matches the class scalar deleting destructor.
- Treating `StaticObjEntry + 0x08` as a general `next` field is too broad. In this destructor, the field is specifically the optional cache-chain link documented by the layout/target pages.
- Simplifying the chain cleanup to `delete[] staticObjEntries` or freeing every primary entry unconditionally is rejected because the decompile distinguishes "chain present" from "chain absent" and only frees chained nodes in the former branch.

Impact:

- Raise to `87/91`.
- Change owner/emitter to `0000E2`.
- Populate ordinary destructor C++.

### [UID:00017W], [UID:00017X], [UID:00017Z], [UID:000180] Singleton Clear Helpers

Best documentation names:

```text
EffectObjImageLib singleton clear helper
HumanImageLib singleton clear helper
LightObjImageLib singleton clear helper
MapTileImageLib singleton clear helper
```

Best source direction:

- Keep each helper attached to its by-file source root.
- Do not emit a named C++ helper such as `void ClearEffectObjImageLibSingleton()`.
- Document these as compiler/static cleanup or constructor-unwind cleanup helpers that materialize singleton clear state.

Evidence checked:

- Each target page documents an exact `0xb` function and no callees.
- Local PE bytes confirm the four target bodies:
  - `0x004e5b70`: clears `0x0069b44c` / `g_pEffectObjImageLib`.
  - `0x004e5b80`: clears `0x0069b43c` / `g_pHumanImageLib`.
  - `0x004e5bb0`: clears `0x0069b450` / `g_pLightObjImageLib`.
  - `0x004e5bc0`: clears `0x0067a75c` / `g_pMapTileImageLib`.
- Effect/Human/Light target pages already record constructor cleanup funclet jumps at `0x0060008a`, `0x005ffaf2`, and `0x006001d0`.
- MapTile helper page currently says no direct callers, but newer MapTile class/layout support docs record a constructor cleanup/unwind xref at `0x005ff9bb`. The supervisor should refresh [UID:000180] wording to include that evidence if applying this report.
- Accepted sibling helpers [UID:000183] Riding and [UID:000184] StaticObj are already `85/90`, file-owned, and blank formal C++ for the same cleanup-glue reason.

Rejected alternatives:

- Emitting trivial free functions is rejected. The binary function exists, but the source-level construct is not a stable public or file-local API; it is generated cleanup glue for constructor/static lifetime state.
- Class ownership is rejected. These helpers take no `this`, do not dispatch through vtables, and do not access object fields.
- Merging the helper island is rejected. Each 0xb function writes a different singleton global and is separated by alignment bytes.
- Treating the empty `callers` result as proof of unused code is rejected. The xref evidence and sibling pattern identify cleanup funclet reachability.

Impact:

- Raise each helper from `82/90` to `85/90`.
- Keep owner/emitter:
  - [UID:00017W] `0000IY`
  - [UID:00017X] `0000JY`
  - [UID:00017Z] `0000KP`
  - [UID:000180] `0000L5`
- Keep formal C++ blank with target-specific no-code notes.

### [UID:0002J2] MapTileImageLib Scalar Deleting Destructor

Best source-level classification:

```text
compiler-emitted scalar deleting destructor for MapTileImageLib
```

Source declaration impact:

```cpp
virtual ~MapTileImageLib();
```

Evidence checked:

- Target page documents exact range `0x004e66a0-0x004e6748`, vtable slot xref, no direct callers, metadata/table/tile-record cleanup, singleton clear, base cleanup, delete-flag branch, and source-declared/generated-binary handling.
- Local PE vtable dwords confirm `0x0061b664 -> 0x004e66a0`.
- Local PE start bytes confirm the same compiler-prolog function body class as the ordinary destructors.
- [UID:000178] provides the source-authored cleanup body. [UID:0002J2] adds only scalar-delete wrapper flags and optional storage deletion.
- Accepted Light scalar [UID:0002IY], Static scalar [UID:0003M2], Riding scalar [UID:0002Y4], and NewHuman scalar [UID:0002VN] all support blank formal C++ for this wrapper class.

Rejected alternatives:

- Populating C++ with `MapTileImageLib *MapTileImageLib::ScalarDeletingDestructor(unsigned int flags)` is rejected. That would hand-author compiler ABI glue and duplicate the ordinary destructor body.
- Moving ownership to the file root is rejected. The vtable slot and destructor wrapper belong to the class declaration and should emit through [UID:00007T].

Impact:

- Raise to `87/92`.
- Keep owner/emitter `00007T`.
- Keep formal C++ blank with explicit no-code proof.

## First-Draft C++ Recommendation

### [UID:000178] `MapTileImageLib::~MapTileImageLib`

Recommended formal C++:

```cpp
MapTileImageLib::~MapTileImageLib()
{
    MemoryManager *memory = GetMemoryMan();

    if (tileFrameTable != 0) {
        if (tileFrameTable->records != 0) {
            memory->Free(tileFrameTable->records);
        }

        memory->Free(tileFrameTable);
    }

    free(tileRecords);
    g_pMapTileImageLib = 0;
}
```

Notes for supervisor:

- `MemoryManager`, `GetMemoryMan`, and `memory->Free` are source-facing aliases for `0x00516030` and `0x00516170`; preserve the two-level archive-table free even if the exact final helper spelling changes.
- The source block intentionally omits vtable stores, exception state, security-cookie handling, and the implicit base destructor call.
- Do not add optional `delete this`; that belongs only to [UID:0002J2].

### [UID:00017G] `RidingImageLib::~RidingImageLib`

Recommended formal C++:

```cpp
RidingImageLib::~RidingImageLib()
{
    MemoryManager *memory = GetMemoryMan();

    if (ridingEpfPackage != 0) {
        if (ridingEpfPackage->records != 0) {
            memory->Free(ridingEpfPackage->records);
        }

        memory->Free(ridingEpfPackage);
    }

    if (ridingDefinitions != 0) {
        DestroyRidingDefinitionArray(ridingDefinitions, ridingDefinitionCount);
        FreeRidingDefinitionArray(ridingDefinitions);
    }

    g_pRidingImageLib = 0;
}
```

Notes for supervisor:

- `ridingEpfPackage` is the current best source-facing name for `this + 0x0c`; the layout page uses `ImageFrameTablePackage* ridingEpfPackage`.
- `DestroyRidingDefinitionArray` and `FreeRidingDefinitionArray` are source-facing aliases for the vector destructor iterator plus allocation-header free. They match the source-lead file and avoid copying compiler vector-iterator ABI into formal C++.
- The binary calls base cleanup before clearing the singleton; in source C++ the base destructor is implicit and should not be hand-authored. The formal block should express the class-owned resource cleanup and singleton clear only.

### [UID:00017I] `StaticObjImageLib::~StaticObjImageLib`

Recommended formal C++:

```cpp
StaticObjImageLib::~StaticObjImageLib()
{
    MemoryManager *memory = GetMemoryMan();

    if (staticObjEntries != 0) {
        for (int index = 0; index < staticObjCount; ++index) {
            StaticObjEntry *entry = staticObjEntries[index];
            StaticObjEntry *cacheNode = entry->cacheChain;

            if (cacheNode != 0) {
                entry->cacheChain = 0;

                while (cacheNode != 0) {
                    StaticObjEntry *nextNode = cacheNode->cacheChain;
                    memory->Free(cacheNode);
                    cacheNode = nextNode;
                }
            } else {
                memory->Free(entry);
            }
        }

        memory->Free(staticObjEntries);
    }

    if (tileCatalog != 0) {
        if (tileCatalog->records != 0) {
            memory->Free(tileCatalog->records);
        }

        memory->Free(tileCatalog);
    }

    free(tileClasses);
    g_pStaticObjImageLib = 0;
}
```

Notes for supervisor:

- This formal block should be applied only with owner/emitter changed to [UID:0000E2] `StaticObjImageLib`.
- The `cacheChain` branch intentionally follows the documented binary behavior: if a chain exists, clear the primary record link and free the chained nodes; if no chain exists, free the primary record.
- The source block intentionally omits explicit `LObject` cleanup, vtable restores, exception metadata, and delete-flag behavior.

### [UID:00017W] EffectObjImageLib Singleton Clear Helper

No formal C++ should be emitted.

Target-specific proof:

- Binary body is exactly `mov dword_69B44C, 0; ret`.
- It takes no `this`, has no callees, and only clears `g_pEffectObjImageLib`.
- Existing page records the constructor cleanup funclet jump at `0x0060008a`.
- The source-level cleanup should be represented by the constructor/static lifetime path that causes the helper, not by a hand-authored `ClearEffectObjImageLibSingleton` function.

### [UID:00017X] HumanImageLib Singleton Clear Helper

No formal C++ should be emitted.

Target-specific proof:

- Binary body is exactly `mov dword_69B43C, 0; ret`.
- It clears old-human `g_pHumanImageLib`, not NewHuman state.
- Existing page records the constructor cleanup funclet jump at `0x005ffaf2`.
- Simroot's `ClearHumanImageLibSingleton` spelling is source-lead pollution, including a literal escaped newline problem nearby; it should not drive formal generated source.

### [UID:00017Z] LightObjImageLib Singleton Clear Helper

No formal C++ should be emitted.

Target-specific proof:

- Binary body is exactly `mov dword_69B450, 0; ret`.
- Existing page records the constructor cleanup jump at `0x006001d0`.
- The accepted B003 Light report populated constructor/destructor/draw methods but left generated cleanup wrappers blank by policy. This helper should follow that policy.
- Hand-authoring `ClearLightObjImageLibSingleton` would create a source API absent from the accepted `LightObjImageLib.cpp` first draft.

### [UID:000180] MapTileImageLib Singleton Clear Helper

No formal C++ should be emitted.

Target-specific proof:

- Binary body is exactly `mov dword_67A75C, 0; ret`.
- It only clears `g_pMapTileImageLib` and has no object/resource cleanup.
- Current helper page says no direct callers, but newer MapTile class/layout docs record a constructor cleanup/unwind xref at `0x005ff9bb`; either way, the body is cleanup glue rather than a source-authored class or file API.
- The ordinary destructor [UID:000178] and scalar deleting destructor [UID:0002J2] own real object cleanup. This helper should remain a blank-C++ file-level cleanup artifact.

### [UID:0002J2] MapTileImageLib Scalar Deleting Destructor

No formal C++ should be emitted.

Target-specific proof:

- The local PE vtable slot `0x0061b664` points to `0x004e66a0`, confirming this is the compiler vtable deleting destructor.
- The target repeats [UID:000178] cleanup and adds scalar delete flags plus optional storage deletion.
- Source-authored cleanup should be emitted exactly once through `MapTileImageLib::~MapTileImageLib`.
- The C++ source declaration `virtual ~MapTileImageLib();` and the ordinary destructor body should regenerate this wrapper under the compiler ABI. Hand-authoring it would bake ABI glue into project source.

## Metadata And Coverage Recommendations

### Target Metadata Changes

Recommended metadata edits for the supervisor:

```text
[UID:000178]
COMPLETION:88
CONFIDENCE:92
CANONICAL_OWNER:00007T
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:00007T
RECONSTRUCTION_CPP: populate with `MapTileImageLib::~MapTileImageLib` from this report.

[UID:00017G]
COMPLETION:87
CONFIDENCE:91
CANONICAL_OWNER:0000BZ
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000BZ
RECONSTRUCTION_CPP: populate with `RidingImageLib::~RidingImageLib` from this report.

[UID:00017I]
COMPLETION:87
CONFIDENCE:91
CANONICAL_OWNER:0000E2
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000E2
RECONSTRUCTION_CPP: populate with `StaticObjImageLib::~StaticObjImageLib` from this report.

[UID:00017W]
COMPLETION:85
CONFIDENCE:90
CANONICAL_OWNER:0000IY
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000IY
RECONSTRUCTION_CPP: keep blank.

[UID:00017X]
COMPLETION:85
CONFIDENCE:90
CANONICAL_OWNER:0000JY
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000JY
RECONSTRUCTION_CPP: keep blank.

[UID:00017Z]
COMPLETION:85
CONFIDENCE:90
CANONICAL_OWNER:0000KP
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000KP
RECONSTRUCTION_CPP: keep blank.

[UID:000180]
COMPLETION:85
CONFIDENCE:90
CANONICAL_OWNER:0000L5
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:0000L5
RECONSTRUCTION_CPP: keep blank.

[UID:0002J2]
COMPLETION:87
CONFIDENCE:92
CANONICAL_OWNER:00007T
RECONSTRUCTABLE:TRUE
EMITTER_UIDS:00007T
RECONSTRUCTION_CPP: keep blank.
```

### `by-memory/-coverage-report.md` Replacement Rows

Placement context: replace the existing rows for these UIDs in place. Current row locations observed in this session are around lines `1705`, `1708`, `1722`, `1725`, and `1757-1761`.

```text
        - [UID:000178][0x004d19a0-0x004d1a1c.MapTileImageLibDestructor](by-memory/0x004d19a0-0x004d1a1c.MapTileImageLibDestructor.md) 0x004d19a0-0x004d1a1c | destructor | MapTileImageLib::~MapTileImageLib : reconstructable : 88% : very-strong : B003 source-quality audit resolves the stale final-name blocker; destructor should emit first-draft C++ with `tileFrameTable`, `ArchiveMetadataTable::records`, `tileRecords`, `g_pMapTileImageLib` clear, implicit base cleanup, no delete-flag behavior, [UID:00007T][MapTileImageLib](by-class/MapTileImageLib.md) owner/emitter, local PE start-byte confirmation, and scalar-wrapper parity with [UID:0002J2].
        - [UID:0002J2][0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor](by-memory/0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor.md) 0x004e66a0-0x004e6748 | scalar-deleting-destructor | MapTileImageLib scalar deleting destructor : reconstructable : 87% : very-strong : B003 source-quality audit confirms the behavior-complete compiler deleting wrapper, local PE vtable slot `0x0061b664 -> 0x004e66a0`, no direct callers in docs, ordinary-destructor parity for `tileFrameTable`/`tileRecords` cleanup, `g_pMapTileImageLib` clear, `LObject` cleanup, delete-flag branch, [UID:00007T][MapTileImageLib](by-class/MapTileImageLib.md) owner/emitter, and blank formal C++ because the source destructor should regenerate the wrapper.
        - [UID:00017G][0x004dc730-0x004dc7d8.RidingImageLibDestructor](by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md) 0x004dc730-0x004dc7d8 | destructor | RidingImageLib::~RidingImageLib : reconstructable : 87% : strong : B003 source-quality audit confirms the source-authored ordinary destructor body, exact package/vector cleanup sequence, `ridingEpfPackage`, `ridingDefinitions`, `ridingDefinitionCount`, vector helper aliases, singleton clear, implicit base cleanup, scalar-wrapper parity with [UID:0002Y4], [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md) owner/emitter, local PE start-byte confirmation, and first-draft C++ readiness.
    - [UID:00017I][0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor](by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md) 0x004dd1e0-0x004dd2be | destructor | StaticObjImageLib::~StaticObjImageLib : reconstructable : 87% : strong : B003 source-quality audit changes direct owner/emitter to [UID:0000E2][StaticObjImageLib](by-class/StaticObjImageLib.md), matching the accepted scalar-wrapper route; destructor should emit first-draft C++ for `staticObjEntries`, `StaticObjEntry::cacheChain`, `tileCatalog`, `tileClasses`, `g_pStaticObjImageLib` clear, implicit base cleanup, no delete flags, local PE start-byte confirmation, and scalar-wrapper parity with [UID:0003M2].
    - [UID:00017W][0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper](by-memory/0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper.md) 0x004e5b70-0x004e5b7b | static cleanup helper | EffectObjImageLib singleton clear helper : reconstructable : 85% : strong : B003 source-quality audit resolves provenance as file-level constructor/static cleanup glue; exact local PE bytes `c7 05 4c b4 69 00 00 00 00 00 c3`, constructor cleanup jump evidence, no `this`, no callees, singleton-only `g_pEffectObjImageLib` clear, [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md) owner/emitter, and formal C++ intentionally blank.
    - [UID:00017X][0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper](by-memory/0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper.md) 0x004e5b80-0x004e5b8b | static cleanup helper | HumanImageLib singleton clear helper : reconstructable : 85% : strong : B003 source-quality audit resolves provenance as old-Human file-level constructor/static cleanup glue; exact local PE bytes `c7 05 3c b4 69 00 00 00 00 00 c3`, constructor cleanup jump evidence, no `this`, no callees, singleton-only `g_pHumanImageLib` clear, [UID:0000JY][HumanImageLib](by-file/HumanImageLib.md) owner/emitter, and formal C++ intentionally blank.
    - [UID:00017Z][0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper](by-memory/0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper.md) 0x004e5bb0-0x004e5bbb | static cleanup helper | LightObjImageLib singleton clear helper : reconstructable : 85% : strong : B003 source-quality audit resolves provenance as file-level constructor/static cleanup glue; exact local PE bytes `c7 05 50 b4 69 00 00 00 00 00 c3`, constructor cleanup jump evidence, no `this`, no callees, singleton-only `g_pLightObjImageLib` clear, accepted Light source-pass no-code precedent, [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md) owner/emitter, and formal C++ intentionally blank.
    - [UID:000180][0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper](by-memory/0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper.md) 0x004e5bc0-0x004e5bcb | static cleanup helper | MapTileImageLib singleton clear helper : reconstructable : 85% : strong : B003 source-quality audit resolves provenance as file-level constructor/static cleanup glue; exact local PE bytes `c7 05 5c a7 67 00 00 00 00 00 c3`, newer MapTile support docs identify constructor cleanup/unwind reachability at `0x005ff9bb`, no `this`, no callees, singleton-only `g_pMapTileImageLib` clear, [UID:0000L5][MapTileImageLib](by-file/MapTileImageLib.md) owner/emitter, and formal C++ intentionally blank.
```

### Expected `auto-generated/-ag-memory-coverage.md` Rows After Apply

These are generated-state expectations, not instructions to edit the generated report by hand:

```text
| [UID:000178][0x004d19a0-0x004d1a1c.MapTileImageLibDestructor](by-memory/0x004d19a0-0x004d1a1c.MapTileImageLibDestructor.md) | coded | `00007T` | `00007T` |  | yes | `auto-generated/NexusTK/render/MapTileImageLib.cpp` | `by-memory/0x004d19a0-0x004d1a1c.MapTileImageLibDestructor.md` |  |
| [UID:00017G][0x004dc730-0x004dc7d8.RidingImageLibDestructor](by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md) | coded | `0000BZ` | `0000BZ` |  | yes | `auto-generated/NexusTK/render/RidingImageLib.cpp` | `by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md` |  |
| [UID:00017I][0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor](by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md) | coded | `0000E2` | `0000E2` |  | yes | `auto-generated/NexusTK/render/StaticObjImageLib.cpp` | `by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md` |  |
| [UID:00017W][0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper](by-memory/0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper.md) | emits | `0000IY` | `0000IY` |  | no | `auto-generated/NexusTK/render/EffectObjImageLib.cpp` | `by-memory/0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper.md` |  |
| [UID:00017X][0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper](by-memory/0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper.md) | emits | `0000JY` | `0000JY` |  | no | `auto-generated/NexusTK/render/HumanImageLib.cpp` | `by-memory/0x004e5b80-0x004e5b8b.HumanImageLibSingletonClearHelper.md` |  |
| [UID:00017Z][0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper](by-memory/0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper.md) | emits | `0000KP` | `0000KP` |  | no | `auto-generated/NexusTK/render/LightObjImageLib.cpp` | `by-memory/0x004e5bb0-0x004e5bbb.LightObjImageLibSingletonClearHelper.md` |  |
| [UID:000180][0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper](by-memory/0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper.md) | emits | `0000L5` | `0000L5` |  | no | `auto-generated/NexusTK/render/MapTileImageLib.cpp` | `by-memory/0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonClearHelper.md` |  |
| [UID:0002J2][0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor](by-memory/0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor.md) | emits | `00007T` | `00007T` |  | no | `auto-generated/NexusTK/render/MapTileImageLib.cpp` | `by-memory/0x004e66a0-0x004e6748.MapTileImageLibScalarDeletingDestructor.md` |  |
```

## Support-Doc Detail Recommendations

- [UID:000178] should remove the stale "95/95 gate" blocker and replace it with the active source-quality result: ordinary destructor C++ is now recommended, with helper names explicitly provisional.
- [UID:00017G] should replace "pending final helper/type names" as a blocker with a narrower note: vector helper aliases are inferred but source-ready; exact original spelling remains bounded.
- [UID:00017I] should change direct owner/emitter from file [UID:0000O7] to class [UID:0000E2], matching [UID:0003M2] and the layout/vtable pages.
- [UID:00017W], [UID:00017X], [UID:00017Z], and [UID:000180] should explicitly say "constructor/static cleanup glue; blank formal C++ by policy" rather than "leave blank until final helper name." The name is not the blocker; source-form is.
- [UID:000180] should be refreshed with the newer MapTile constructor cleanup/unwind xref at `0x005ff9bb` from MapTile class/layout support docs, or state that this xref is support-doc evidence pending live IDA reconfirmation.
- [UID:0002J2] should keep source-declared/generated-binary handling, but update score/rationale to reference the ordinary destructor first-draft body as the source that regenerates the wrapper.

## Validation Notes

- No protected docs or generated files were edited in this report-only pass.
- `git status --short` was attempted from `e:\NTK\GhidraBridge`; this workspace root is not a Git repository in the current shell context.
- Live IDA MCP was unavailable, so no IDA DB writes or renames were attempted.
- Local PE byte/vtable checks were read-only and confirmed the target helper bodies plus MapTile/Riding/Static vtable-slot relationships used above.

No targeted open question remains unresolved. Exact original spellings for memory-manager and vector helper APIs remain bounded implementation names, but they no longer block first-draft destructor C++ because the behavior, owner, field roles, and generated-wrapper exclusions are all defensible from current docs plus local PE evidence.

<!-- VALIDATOR-REPORT-HISTORY:BEGIN | DO NOT EDIT BY HAND -->
<!-- {"agent":"B003","command_id":"000000004165","destination_path":"executed-b-agent-research/B003/000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-image-library-cleanup-source-quality.md","details":"legacy registry reconstruction","event":"executed","timestamp":"2026-06-23T21:10:26","uid":"000178"} -->
<!-- {"agent":"B003","command_id":"000000023047","details":"supervisor-confirmed removal of executable report automation; inert archive preserved at 000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-image-library-cleanup-source-quality-removed.md","event":"content-change-confirmed","source_path":"executed-b-agent-research/B003/000178-00017G-00017I-00017W-00017X-00017Z-000180-0002J2-image-library-cleanup-source-quality.md","timestamp":"2026-08-13T02:36:38-04:00","uid":"000178"} -->
<!-- VALIDATOR-REPORT-HISTORY:END -->
