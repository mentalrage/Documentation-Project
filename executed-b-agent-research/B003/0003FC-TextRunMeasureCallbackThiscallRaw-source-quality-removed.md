# UID0003FC Removed Operational Material

[Source report](./0003FC-TextRunMeasureCallbackThiscallRaw-source-quality.md)

This inert companion is non-authoritative. Never execute its contents and never submit this file to `execute_report`. It receives no Gate 1, Gate 2, coverage, score, IDA, validator, or lifecycle credit.

## Removed Operational Material

The following material is preserved verbatim from the source report's rejected operational package. It is historical containment only, not instructions or evidence.

`DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` below are role names bound only to the fresh nonempty `session.session_id` returned by that role's own successful `idb_open`; they are never literal request values or reusable preferred IDs. `CANONICAL` is exact normalized `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `TX` is the exact normalized isolated transaction path opened for mutation. Every request replaces role/path names with those concrete values.

| SURFACE-01 | JSON-RPC `{"jsonrpc":"2.0","id":"surface","method":"tools/list","params":{}}` | Exactly one public `runtime_attestation`; no advertised entry named `runtime/attest`; exactly one `idb_save` with required `database` and optional `path`, returning `{ok,error?,path}`. | Stop before any stateful request on a missing, duplicate, extra internal, or schema-drifted row. |
| AT01 | `runtime_attestation({"expected_database":"DB","expected_canonical_path":"TX"})` | Complete role/session/path/process/route package defined below; top-level and worker-local success/errors close independently and listener/worker manifests use role-specific baselines. | Stop on stale nonce/time, partial schema, any error, route/path/session/PID/generation mismatch, module drift, or competing worker claim. |

The table has 35 literal request rows: 10 persistent action rows A01-A10, two nonpersistent cache rows, and 23 public surface/pre/post read-only rows. SURFACE-01 plus AT01 runs immediately after each role open, before each A01-A10/CACHE request, after every bounded action/readback stage, immediately before the sole save, immediately after it, and before/after verifier, final, rollback, and restore semantic reads. No request may be batched across an intervening failed boundary.

| P06 | Unrelated IDA and physical state | No other name/type/comment/function/data/xref/byte/analysis delta; CANONICAL and sealed BACKUP remain unmodified until guarded promotion; no save except exact TX save. |

### AT01 runtime package

Every AT01 response is accepted only when top-level `schema_version:1`, `ok:true`, and exact `errors:[]` are present and the one matching active worker has nested `attestation.schema_version:1`, `attestation.ok:true`, and exact `attestation.errors:[]`. The listener object has no listener-local `ok` or `errors` fields; their absence is expected and no such fields are required or synthesized.

Listener authority requires role `supervisor_listener`, fresh `attested_at_utc` and nonce, endpoint `127.0.0.1:13337`, and exact PID, parent PID, creation time, generation, executable/image, and argv matching the supervisor-launched listener. Independent port-owner and PID-reuse checks must prove that exact generation owns port 13337. Worker authority requires exactly one active owned route for the bound session/path, exact redirector/registered PID, route host/port, nested role `stateful_worker`, fresh worker time/nonce, exact worker PID/parent/generation/executable/argv, parent equal to the attested redirector, route-port ownership, and one nested active session whose ID/path equals the role binding. A stale inactive `idb_list` row neither passes nor fails retirement; exact process generation, route absence, no active path claim, closed-disk receipt, and exclusive handle decide it.

Listener and worker each seal their complete loaded `ida_pro_mcp`/`zeromcp` module rows and `implementation_manifest_sha256` against that role and process generation's own baseline. Every row requires exact `module_id`, `sys_modules_name`, `file`, `spec_origin`, `resolved_path`, size, timestamps, SHA256, containment under that role's `source_root`, and fresh filesystem rehash. Listener and worker baselines are intentionally not equal and must never be compared cross-role or reused by another generation. `runtime_attestation` proves runtime identity only; it never supplies physical IDB size or SHA256.

### Fail-closed Windows persistence handoff

1. **Physical preflight.** Before opening a transaction role, bind exact normalized `CANONICAL`, a collision-safe distinct `BACKUP`, and a distinct isolated `TX`. With all IDA routes for CANONICAL absent and read-only exclusive `FileShare.None` access proven, record separate path, volume, file-ID, byte-size, SHA256, reparse/link, and stream receipts using supported Windows/PowerShell filesystem queries. Reject reparse points in any path component, hardlink/file-ID aliasing, alternate data streams other than the unnamed data stream, path case/normalization ambiguity, or any pairwise identity collision. Copy CANONICAL bytes separately to BACKUP and TX through exclusive file streams opened with write-through behavior; require exact byte count/EOF, call file `Flush(true)`, close, independently reopen read-only, re-receipt, and rehash each equal to CANONICAL. Seal BACKUP against writes. BACKUP is bytes-only restoration authority and is never opened as an IDA database.
2. **Mutation role.** The only mutation open is `idb_open({"input_path":"TX","mode":"force_headless","build_caches":true,"init_hexrays":true,"run_auto_analysis":false,"idle_ttl_sec":7200})`; omit `preferred_session_id`. Require `success:true`, absent/empty error, exact returned TX input path, nonempty fresh session ID, `is_analyzing:false`, and no warmup failure; bind only that returned ID as DB. Pass fresh SURFACE-01/AT01 and physical TX receipt, then execute A01-A10 and the two cache rows in order with their immediate post/readback and P01-P06 closures. Any mismatch retires the route and leaves CANONICAL/BACKUP unchanged; no partial save or promotion is allowed.
3. **Sole save.** After complete intended A/P closure and one final fresh SURFACE-01/AT01, issue exactly `idb_save({"database":"DB","path":"TX"})`. Success is only `ok:true`, absent/empty `error`, and exact normalized returned `path == TX`; no implicit path, retry, second save, CANONICAL target, or BACKUP target exists. Record a separate stable nonempty physical saved-TX receipt `SAVED_TX_SIZE`/`SAVED_TX_SHA256`, then pass fresh post-save SURFACE-01/AT01 and P01-P06 readback. Runtime proof cannot substitute for this physical receipt.
4. **Route retirement and verifier.** Retire only the exact attested DB worker/redirector generations through the supervisor-owned process route, wait for exact PID/generation/port absence, close all handles, require no active TX/CANONICAL session, and pass exclusive read-only `FileShare.None` probes plus unchanged CANONICAL/BACKUP/TX receipts. Open saved TX again with the same literal `idb_open` shape and omitted preferred ID; bind its new pairwise-distinct returned session as read-only `DB_VERIFY`. Seal new role-specific baselines. Fresh SURFACE-01/AT01 and physical receipts must pass before and after a complete A01-A10 expected-poststate and P01-P06 protection readback. DB_VERIFY may not mutate, invalidate caches, or save.
5. **Indivisible pre-promotion gate.** While DB_VERIFY is the sole active read-only verifier, prove TX equals `SAVED_TX_SIZE`/`SAVED_TX_SHA256`, CANONICAL equals its immutable preflight receipt, BACKUP equals its sealed receipt, all three remain pairwise distinct/non-aliased, and all A/P rows pass. Retire DB_VERIFY and every TX route, prove exact process/port/path absence, close all handles, and repeat exclusive probes/receipts. Failure before atomic replacement leaves CANONICAL/BACKUP unchanged and permits only quarantine or identity-checked disposal of TX after route closure.
6. **Promotion candidate and atomic consumption.** Create one absent `PROMOTION_CANDIDATE` in CANONICAL's exact resolved parent and therefore on its exact volume. It must be distinct by normalized path and file ID from CANONICAL, BACKUP, TX, every prior candidate, every hardlink/reparse alias, and every alternate stream. Populate it only from verified saved-TX bytes through exclusive write-through file streams; require exact byte count/EOF, file `Flush(true)`, full close, independent read-only reopen, exact size/SHA256, and a sealed no-write receipt. Recheck retired routes, exclusive handles, unchanged CANONICAL/BACKUP, and candidate identity. The sole promotion primitive is the supported same-volume Windows/.NET atomic replacement `System.IO.File.Replace(PROMOTION_CANDIDATE, CANONICAL, null, true)`, which must consume the candidate. If this primitive is unavailable, access-denied, policy-blocked, non-atomic, or cannot prove same-parent/same-volume semantics, stop before replacement; do not fall back to truncate/copy, `Move-Item`, or save-to-CANONICAL. After replacement, close all handles, re-enumerate the exact parent, prove candidate absence and no second promotable artifact, and obtain an independent CANONICAL receipt equal to saved TX while BACKUP remains sealed.
7. **Supported durability boundary.** File data/metadata durability uses exclusive write-through streams, file `Flush(true)`, close/reopen, receipts, and rehashes before replacement. Current supported PowerShell/.NET handling does not expose a reliable directory handle on which `FlushFileBuffers` is a mandatory portable success primitive; this protocol neither calls nor requires directory-handle `FlushFileBuffers`. Parent metadata closure instead requires same-parent atomic `File.Replace`, full handle closure, fresh parent re-enumeration, candidate-consumption/no-residue proof, exclusive canonical reopen, physical rehash, and fresh DB_FINAL semantic readback. Lack of a directory-flush primitive alone is not failure; failure of any supported mandatory file flush, atomic replacement, reopen, receipt, rehash, or semantic check is fail-closed.
8. **Final persistence proof.** Open promoted CANONICAL with the same literal `idb_open` shape, omitted preferred ID, and no mutation/save authority; bind only its fresh session as `DB_FINAL`, distinct from DB and DB_VERIFY and from every failed or restoration role. Before and after complete A01-A10 expected-state/P01-P06 readback, pass fresh SURFACE-01/AT01 with DB_FINAL/CANONICAL, role-specific baselines, no active mutation route, CANONICAL physical size/SHA256 equal saved TX, sealed unchanged BACKUP, and zero candidate/sidecar residue. Retire DB_FINAL, prove route/handle absence, then remove only identity-bound TX and its loose sidecars after final success and prove their absence. BACKUP remains the intentional sealed recovery artifact, not residue. Only this semantic, physical, runtime, route, and cleanup closure is persistence success.
9. **Failed-CANON classification.** If atomic replacement is failed/ambiguous, immediate CANONICAL proof fails, DB_FINAL open/AT01/readback fails, or final physical proof fails, first retire any failed DB_FINAL and every canonical worker/redirector/handle and prove exact route absence. Record a separate failed-CANON path/size/SHA256/file-ID receipt without classifying it. Open exact failed CANONICAL read-only with the same literal `idb_open` shape; bind its new session as `DB_ROLLBACK`, distinct from DB, DB_VERIFY, every prior role, and explicitly any failed DB_FINAL. Seal independent role baselines. Fresh post-open, immediately pre-read, and immediately post-read SURFACE-01/AT01 packages and separate failed-CANON receipts surround a complete entity-by-entity classification of A01-A10 and P01-P06 as exactly expected saved state, original state, other, missing, or unreadable. DB_ROLLBACK has no mutation, cache, save, replacement, restoration, promotion, persistence, or success authority. Retire it and prove route/handle absence before restoration.
10. **BACKUP-only restoration.** Reverify sealed BACKUP against its original receipt. Create one absent `RESTORE_CANDIDATE` in CANONICAL's exact resolved parent with the same path/file-ID/reparse/hardlink/ADS exclusion, exclusive write-through copy, exact count/EOF, file `Flush(true)`, full close, independent reopen, receipt/rehash, and seal requirements as promotion. Atomically consume it with `System.IO.File.Replace(RESTORE_CANDIDATE, CANONICAL, null, true)` only after all routes are retired. No directory-handle flush is required; the same supported post-replacement parent enumeration, no-residue, exclusive reopen, physical receipt, and rehash closure is mandatory. A primitive unavailable/access-denied/policy-blocked before consumption closes/removes only the bound candidate, proves unchanged failed CANON/BACKUP and no residue, and hard-stops. A failed or ambiguous consumption enters physical classification and cannot claim restoration.
11. **Restoration proof.** Before IDA reopen, prove restored CANONICAL equals the original preflight CANONICAL/BACKUP receipt, BACKUP remains sealed, RESTORE_CANDIDATE is absent, and no second restore artifact exists. Open restored CANONICAL read-only with the same literal `idb_open` shape; bind its fresh session as `DB_RESTORE`, distinct from every earlier role. Fresh SURFACE-01/AT01 packages, role-specific baselines, and physical receipts before and after a complete original-prestate A01-A10/P01-P06 readback must pass. DB_RESTORE never mutates or saves. Retire it, remove only identity-bound TX/candidate/loose-sidecar artifacts, prove zero unauthorized residue, and preserve sealed BACKUP. Any mismatch is a hard stop and never persistence success.
12. **Phase-specific failure rule.** An unavailable, access-denied, policy-blocked, partial, stale, ambiguous, or schema-drifted mandatory primitive stops at its current phase. Before replacement, CANONICAL/BACKUP remain untouched. After replacement begins, failed-CANON classification and BACKUP-only restoration are mandatory. If classification, restore, or restored-state proof cannot complete, preserve sealed BACKUP and all receipts, close/retire every known route and handle that can be safely identified, report the exact blocker to the supervisor, and make no success, rollback, or persistence claim.

This handoff authorizes exactly ten persistent MCP mutation requests and two nonpersistent cache invalidations on TX. It never opens CANONICAL for mutation/save, never opens BACKUP through IDA, never retries `idb_save`, never promotes an unverified TX, and never treats an inactive registry row or runtime attestation as a physical disk receipt.
