*** UID:0001NW | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void VirusChecker::InsertModulePath(const char *path)
{
    m_modulePaths.insert(ModulePathString(path).ToLower());
}

int __stdcall VirusChecker::V3ModuleEnumCallback(
    unsigned long processId,
    unsigned long moduleId,
    const char *modulePath,
    void *context)
{
    VirusChecker *checker = static_cast<VirusChecker *>(context);
    checker->InsertModulePath(modulePath);
    return 1;
}

int __stdcall VirusChecker::V3ProcessEnumCallback(
    unsigned long processId,
    unsigned long unused1,
    unsigned long unused2,
    unsigned long unused3,
    void *context)
{
    s_pfnV3PCtrlEnumModules(processId, V3ModuleEnumCallback, context);
    return 1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready VirusChecker helper family over exact 0x005c0940-0x005c0fe1: three authored definitions implement lower-case module-path insertion and typed stdcall V3 process/module callbacks through caller-cleaned cdecl vendor exports, while six retained bodies are explicitly compiler/Dinkumware container, EH, scalar-destructor, and cleanup lowering; all ranges, 0xcc gaps, xrefs, negative routes, ownership, and no-handwritten-source dispositions are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005c0940-0x005c0fe1 VirusChecker Process Tree Helpers

## Status

- Confidence: strong for boundaries, helper grouping, callback flow, and red-black tree mechanics; medium-high for final local container/type names.
- Covered class: [UID:0000FW][VirusChecker](by-class/VirusChecker.md)
- Likely source file: [UID:0000P5][VirusChecker](by-file/VirusChecker.md)
- Historical generated-output caveat: older generated `class_VirusChecker.cpp` mostly omitted this helper family and emitted recursive cleanup as a recovered standalone helper. Treat generated output as a stale lead unless refreshed by the current validator.
- Disposition: reconstructable project support code.
- Assignment: assigned to direct class parent [UID:0000FW][VirusChecker](by-class/VirusChecker.md). These callbacks, tree allocation/insertion helpers, scalar deleting destructor, and recursive cleanup serve only the `VirusChecker` scanner path set, and the child plus direct class parent now clear the strict 85/85 gate.

## Address Range

- Start: `0x005c0940`
- End: `0x005c0fe1` exclusive
- Size: `0x6a1`
- Next bytes: `0x005c0fe1-0x005c0ff0` are fifteen `0xcc` padding bytes before the [UID:0001NX][0x005c0ff0-0x005c129a.WaitableTimer](by-memory/0x005c0ff0-0x005c129a.WaitableTimer.md) island.

## Functions

| Range | Size | Function | Notes |
| --- | --- | --- |
| `0x005c0940-0x005c09ec` | `0xac` | `VirusChecker::InsertModulePath(const char *path)` | Converts/copies an ANSI module path through the shared string helpers, normalizes the backing buffer length, calls `ModulePathSetFindOrInsert` / `0x005c0ca0(this + 1, ..., key)`, and releases temporaries. The dated MCP session `e63ee655` reports no direct xref or pointer-pattern route to the function start, so this is a retained direct insertion entry without a proven static route; the older `86fb854e` cleanup is historical support. |
| `0x005c09f0-0x005c0aa0` | `0xb0` | `V3ModuleEnumCallback` | Four-argument stdcall V3 module callback, referenced only by the process callback at `0x005c0aa6`. It uses `[ebp+0x10]` as the module path and `[ebp+0x14]` as the `VirusChecker *` context, converts/copies the path, inserts it into the tree rooted at `context + 4` through `0x005c0ca0`, and returns `1` to continue enumeration. |
| `0x005c0aa0-0x005c0ac0` | `0x20` | `V3ProcessEnumCallback` | Five-argument stdcall V3 process callback referenced from the V3 scan path at `0x005c07da`. It invokes `s_pfnV3PCtrlEnumModules(process, 0x005c09f0, context)` using the process argument at `[ebp+8]` and context at `[ebp+0x18]`, then returns `1`. |
| `0x005c0ac0-0x005c0b3a` | `0x7a` | `ModulePathSetAllocateNode` | Allocates one 0x14-byte tree node with `0x005796f0`, writes parent bookkeeping for tree insertion, initializes the color/header word at `+0x0c`, and copies the project string payload into `node + 0x10`. Internal container helper, not public API. |
| `0x005c0b40-0x005c0c94` | `0x154` | `ModulePathSetInsertRebalance` | Red-black-tree style insertion and rotation logic for the module-path set. It checks `this[1] >= 0x0ccccccb`, throws `"map/set<T> too long"` through `0x005c5eac` when exhausted, allocates a node through `0x005c0ac0`, updates left/right/header links, uses rotations at `0x0043ed70` and `0x0043f000`, and forces the root color byte back to black. Internal STL/container behavior. |
| `0x005c0ca0-0x005c0ea7` | `0x207` | `ModulePathSetFindOrInsert` | Searches by path string and inserts a new node only when the path is not present. It compares keys by pulling string data through `0x00584540`, uses a two-byte character loop for one search direction and normalized `strcmp` for the other/equality check, then calls `0x005c0b40` only when the searched key is absent. |
| `0x005c0eb0-0x005c0ebb` | `0x0b` | singleton clear EH helper | Clears `g_pVirusChecker` / `dword_69BF94`; the only external edge is the compiler EH cleanup reference at `0x0060bc76`. Do not model as an ordinary source method. |
| `0x005c0ec0-0x005c0f94` | `0xd4` | compiler scalar deleting destructor | Compiler artifact generated from `VirusChecker::~VirusChecker`. It sets the vtable, unloads `s_hV3Pro32` and `s_hV3PCtrl`, clears the three observed V3/Ahn function pointers, recursively destroys the tree, resets the header sentinel links, clears the singleton global, and conditionally frees `this`; source should only declare `virtual ~VirusChecker()`. |
| `0x005c0fa0-0x005c0fe1` | `0x41` | `DestroyModulePathSetNodes` | Internal path-set recursive cleanup. Recursively destroys right branches, advances down left branches, destroys the path string at `node + 0x10` through `0x00582b30`, and frees 0x14-byte nodes through `0x005c7526`. Live callers are non-deleting destructor `0x005c04e0`, scalar deleting destructor `0x005c0ec0`, and itself. |

## Evidence Notes

- IDA MCP lookup on 2026-05-26 confirms all starts and sizes through `0x005c0fa0`, with `0x005c0ff0` starting the next `WaitableTimer`-shaped function.
- IDA MCP lookup on 2026-05-31 rechecked all listed helper starts and sizes, and `get_bytes` confirms `0x005c0fe1-0x005c0fef` is `0xcc` padding before `WaitableTimer`.
- IDA xrefs show `0x005c0aa0` referenced as process callback data from `0x005c07da`, and `0x005c09f0` referenced as module callback data from `0x005c0aa6`.
- IDA MCP decompilation on 2026-05-31 confirms `0x005c09f0` converts/copies module path strings and inserts through `sub_5C0CA0(a4+4, ...)`, while `0x005c0aa0` calls the module enumeration export through `dword_69BF80(a1, sub_5C09F0, a5)`.
- IDA MCP decompilation on 2026-05-31 confirms `0x005c0ac0`, `0x005c0b40`, `0x005c0ca0`, and `0x005c0fa0` implement tree node allocation, insertion/rebalancing, lookup/insert, and recursive cleanup over the node layout recorded in [UID:0001WG][VirusCheckerProcessTreeLayout](by-type/by-struct/VirusCheckerProcessTreeLayout.md).
- IDA callers for `0x005c0fa0` are the non-deleting destructor at `0x005c04e0`, scalar deleting destructor at `0x005c0ec0`, and itself recursively.
- IDA xrefs to `0x0069bf94` remain local to the VirusChecker constructor/destructor/clear/destructor-wrapper paths.
- The node layout observed here is recorded in [UID:0001WG][VirusCheckerProcessTreeLayout](by-type/by-struct/VirusCheckerProcessTreeLayout.md): left/parent/right at `+0x00/+0x04/+0x08`, color/header bytes at `+0x0c/+0x0d`, and path string payload at `+0x10`.
- Historical generated-output caveat: old generated output only called recovered `DestroyVirusCheckerProcessTreeNodes_5C0FA0`; the tree allocation/insertion/callback helpers were absent from that class file. Use current validator output, not this old generated state, for generated freshness.
- Live IDA MCP refresh on 2026-06-07 reports exact modeled function extents for all nine helpers above, confirms `0x005c0fe1-0x005c0ff0` as `0xcc` padding, and confirms callback/code xrefs: `0x005c07da -> 0x005c0aa0`, `0x005c0aa6 -> 0x005c09f0`, `0x005c09cd/0x005c0a7d -> 0x005c0ca0`, `0x005c0dab -> 0x005c0b40`, and `0x005c0f39/0x005c0fb8 -> 0x005c0fa0`.
- Live IDA MCP decompilation on 2026-06-07 confirms `0x005c0b40` as the insertion/rebalance body with node allocation, root/header updates, red-black recoloring, rotations, and the `"map/set<T> too long"` throw path.
- Live IDA MCP decompilation on 2026-06-07 confirms `0x005c0ca0` as lookup-or-insert logic: it walks from the header root through left/right child links, compares candidate path strings through `0x00584540` and normalized lexical comparisons, and returns both an iterator/node pointer plus an inserted flag byte.
- Live IDA MCP decompilation on 2026-06-07 confirms `0x005c0ec0` clears the V3 and AhnLab function pointers/globals, unloads the two library handles, resets the tree header links/count, and clears `dword_69BF94`.
- 2026-06-11 A003 Batch 200 live IDA MCP refresh reconfirmed all modeled helper extents and complexity: `0x005c0940-0x005c09ec` 53 instructions / 8 blocks, `0x005c09f0-0x005c0aa0` 52 / 8, `0x005c0aa0-0x005c0ac0` 10 / 1, `0x005c0ac0-0x005c0b3a` 41 / 5, `0x005c0b40-0x005c0c94` 123 / 29, `0x005c0ca0-0x005c0ea7` 198 / 51, `0x005c0eb0-0x005c0ebb` 2 / 1, `0x005c0ec0-0x005c0f94` 63 / 11, and `0x005c0fa0-0x005c0fe1` 28 / 3.
- 2026-06-11 A003 xref refresh reconfirmed local call structure: `0x005c0940` and `0x005c09f0` call lookup/insert at `0x005c0ca0`; `0x005c0ca0` calls insert/rebalance at `0x005c0b40`; `0x005c0b40` calls allocator `0x005c0ac0` plus rotations `0x0043ed70` / `0x0043f000`; recursive cleanup `0x005c0fa0` is called by the non-deleting destructor, scalar deleting destructor, and itself.
- 2026-06-11 A003 `decompile` reconfirmed process/module callback flow and tree semantics: process callback passes module callback `0x005c09f0`, lookup/insert walks node links and compares path payloads through `0x00584540`/`strcmp`, insertion increments the container count and rebalances red/black state, and scalar deleting destructor unloads V3 modules, clears V3/AhnLab function pointers, destroys the tree, clears the singleton, and conditionally frees `this`.
- 2026-07-03 B004 UID0001Q3 dated MCP session `e63ee655` supersedes `86fb854e` as the then-current singleton/activation snapshot, while preserving the older cleanup as historical support. It rechecked no direct xref or pointer-pattern route to the direct insert helper `0x005c0940`, confirmed no direct route to singleton clear helper `0x005c0eb0`, and kept the helper family source-local to [UID:0000FW][VirusChecker](by-class/VirusChecker.md). The accepted source-level model is `std::set<String> m_modulePaths` or the project string-wrapper equivalent; exact original typedef spelling remains unresolved.
- B012 2026-06-19 [UID:000245][0x00578f70-0x005797ae.StlTreeStringMapSupport](by-memory/0x00578f70-0x005797ae.StlTreeStringMapSupport.md) recheck separates shared Dinkumware allocation/free support from VirusChecker-specific tree behavior. Shared helpers `0x005796d0`, `0x005796f0`, and `0x00579760` provide header allocation, node-shell allocation, and single-node free support; this page still owns the callback flow, `InsertModulePath`, local node payload copy, red-black insertion/rebalance at `0x005c0b40`, lookup/unique insert at `0x005c0ca0`, and recursive cleanup at `0x005c0fa0`. Reconstruct source as a VirusChecker-owned ordered path set and let the compiler/STL regenerate the shared helper bodies.
- 2026-06-29 B002 accepted UID0001NV implementation reconfirmed the callback route with dated MCP evidence: `ScanLoadedProcessModules` passes `V3ProcessEnumCallback` at `0x005c07da`, and that callback passes `V3ModuleEnumCallback` at `0x005c0aa6`. UID0001NV now emits the scan-side call site, while this page continues to own the helper callback and path-tree family.

All named MCP sessions in this page are dated evidence snapshots, not permanent current authority. Future IDA-dependent decisions require a fresh supervisor `idb_list`, health/canonical-disk identity, and target-prestate readback.

## Source Reconstruction Policy

The helper family is reconstructable, and this exact page now emits the three authored source definitions: `InsertModulePath`, `V3ModuleEnumCallback`, and `V3ProcessEnumCallback`. UID0001NV emits `ScanLoadedProcessModules` and its call to the process callback. The source model uses private class aliases `ModulePathString`, `ModulePathLess`, and `ModulePathSet`; lower-case-copy behavior consumes the shared header-visible inline `StringBase<char>::ToLower() const` specialization from `StringBase.h` or an included `.inl`, and unique insertion is ordinary `std::set` source. VirusChecker's inlined lowering is a consumer, not a definition placement in `VirusChecker.cpp` or `StringBase.cpp`.

The remaining six bodies are retained compiler output, not missing source: node allocation at `0x005c0ac0`, insertion/rebalance at `0x005c0b40`, unique find/insert at `0x005c0ca0`, Singleton EH clear at `0x005c0eb0`, scalar deleting destructor at `0x005c0ec0`, and set-node destruction at `0x005c0fa0`. Their behavior is caused by the class declaration, `m_modulePaths.insert(...)`, implicit exception cleanup, virtual destruction, and `ModulePathSet` lifetime. Do not hand-port those bodies or create child pages for them.

## Assignment Gate

| Item | Score | Gate Decision |
| --- | --- | --- |
| Child [UID:0001NW] | `93/94` | Clears with the complete authored/generated partition, exact callback ABI, formal CPP, and source-placement closure recorded on this page. |
| Direct class parent [UID:0000FW][VirusChecker](by-class/VirusChecker.md) | `91/93` | Clears and is the narrow direct semantic owner for the class-local process-tree helper family. |
| File parent [UID:0000P5][VirusChecker](by-file/VirusChecker.md) | `90/91` | Clears as source placement context through `NexusTK/security/VirusChecker.cpp`. |

`CANONICAL_OWNER` and `EMITTER_UIDS` are set to [UID:0000FW][VirusChecker](by-class/VirusChecker.md). The enclosing memory aggregate [UID:0001NU][0x005c0460-0x005c0fe1.VirusChecker](by-memory/0x005c0460-0x005c0fe1.VirusChecker.md) remains useful containment/context, but routing to the class is the direct semantic ownership choice under by-structure rules.

## Ownership Notes

Keep the whole tree helper family with `VirusChecker`; older Wave3 output that emitted recursive cleanup as a standalone recovered helper is a stale generated-output lead, not source ownership proof. The callbacks and insertion code only serve the V3 process/module path set used by [UID:0001NV][0x005c05a0-0x005c0937.VirusCheckerV3LoadAndScan](by-memory/0x005c05a0-0x005c0937.VirusCheckerV3LoadAndScan.md).

Canonical owner and emitter remain class [UID:0000FW][VirusChecker](by-class/VirusChecker.md), with source root [UID:0000P5][VirusChecker](by-file/VirusChecker.md). Emitter position `30` places the class-owned definitions after module statics at position `10` and load/scan methods at position `20`. Formal H remains blank here because declarations belong to the class H channel; formal CPP contains only the three authored definitions.

Historical superseded assumption: earlier passes kept every helper body blank pending a future child-method pass and final generic string/container names. Current UID0001NW research resolved the concrete private StringBase/set source model, callback ABIs, and generated-body causes. The future-child/no-body policy is retained only as history and no longer controls emission.

## 2026-08-02 Source-Quality Closure

### Exact Range And Padding

- The target remains one physical range, `0x005c0940-0x005c0fe1`, size `0x6a1`; no split or child page is required.
- Exact modeled functions are `0x005c0940-0x005c09ec` (`0xac`), `0x005c09f0-0x005c0aa0` (`0xb0`), `0x005c0aa0-0x005c0ac0` (`0x20`), `0x005c0ac0-0x005c0b3a` (`0x7a`), `0x005c0b40-0x005c0c94` (`0x154`), `0x005c0ca0-0x005c0ea7` (`0x207`), `0x005c0eb0-0x005c0ebb` (`0x0b`), `0x005c0ec0-0x005c0f94` (`0xd4`), and `0x005c0fa0-0x005c0fe1` (`0x41`).
- Every inter-function/boundary gap is `0xcc`: predecessor `0x005c0937-0x005c0940` is 9 bytes; internal gaps are `0x005c09ec-0x005c09f0` (4), `0x005c0b3a-0x005c0b40` (6), `0x005c0c94-0x005c0ca0` (12), `0x005c0ea7-0x005c0eb0` (9), `0x005c0ebb-0x005c0ec0` (5), and `0x005c0f94-0x005c0fa0` (12); successor `0x005c0fe1-0x005c0ff0` is 15 bytes.

### Authored And Generated Partition

| Address | Source disposition | Binary consequence |
| --- | --- | --- |
| `0x005c0940` | authored `VirusChecker::InsertModulePath` | constructs a lower-case ANSI `ModulePathString` copy and performs unique `ModulePathSet::insert` |
| `0x005c09f0` | authored static stdcall `V3ModuleEnumCallback` | treats context as `VirusChecker *`, inserts `modulePath`, returns `1`, and cleans 16 callback bytes |
| `0x005c0aa0` | authored static stdcall `V3ProcessEnumCallback` | calls cdecl `s_pfnV3PCtrlEnumModules(processId, V3ModuleEnumCallback, context)`, returns `1`, and cleans 20 callback bytes |
| `0x005c0ac0` | compiler/Dinkumware lowering | allocates and copies a 0x14-byte ordered-set node caused by `m_modulePaths.insert()` |
| `0x005c0b40` | compiler/Dinkumware lowering | inserts/rebalances the red-black tree and enforces the container length bound |
| `0x005c0ca0` | compiler/Dinkumware lowering | lexically searches for a unique lower-case path and inserts only when absent |
| `0x005c0eb0` | compiler/template EH lowering | clears the `Singleton<VirusChecker>` publication during unwind |
| `0x005c0ec0` | MSVC generated wrapper | scalar deleting destructor generated from virtual `~VirusChecker()` |
| `0x005c0fa0` | compiler/Dinkumware lowering | recursively destroys 0x14-byte nodes caused by `ModulePathSet` lifetime |

### ABI, Edges, And Negative Evidence

- The callback chain is exact: `0x005c07da -> 0x005c0aa0`, `0x005c0aa6 -> 0x005c09f0`, `0x005c09cd/0x005c0a7d -> 0x005c0ca0`, `0x005c0dab -> 0x005c0b40`, and `0x005c0f39/0x005c0fb8 -> 0x005c0fa0`; the class destructor also calls node destruction at `0x005c0558`.
- Caller stack cleanup after all four dynamically resolved vendor exports proves those export pointers are `__cdecl`. Callback epilogues `retn 0x10` and `retn 0x14` prove the module and process callbacks are `__stdcall`.
- The member and module callback share the same StringBase copy/detach/lower-case and unique-insert lowering; the callback source calls `InsertModulePath` rather than duplicating decompiler-shaped code.
- `xrefs_to` finds no direct route to retained `InsertModulePath` or the EH clear entry. Nine bounded little-endian searches covering VA, RVA, and raw-offset encodings for `0x005c0940`, `0x005c0eb0`, and `0x005c0fa0` return zero. Those negatives reject a hidden static pointer table but do not erase retained authored source or compiler lowering.
- An authoritative legacy AhnLab SDK declaration was not found. Binary stack cleanup resolves the ABI exactly; only the original spelling of unused 32-bit callback slots remains inferred (`moduleId`, `unused1..3`). No decompiler placeholder enters formal source.
- No normal activation route has been recovered for the retained scanner class. This caps confidence and runtime-use claims only; it is not a blocker to exact range, behavior, ownership, source placement, or source emission.

### Layout And Source Shape

- `m_modulePaths` is an 8-byte embedded ordered-set storage object at class offset `+0x04` (header/sentinel pointer) and `+0x08` (count), overlapping the empty `Singleton<VirusChecker>` base at `+0x04` through EBO.
- The compiler node is exactly 0x14 bytes: parent/left/right links, color/header state, and StringBase payload pointer. This analysis layout documents lowering; final source uses `ModulePathSet`, not a handwritten raw-node type.
- A stateless `ModulePathLess` performs the observed `strcmp(left.c_str(), right.c_str()) < 0` ordering. `ModulePathString` names the exact ANSI StringBase specialization. These private descriptive names are preferred to raw compiler labels and preserve the period-plausible source structure.

## Cross-References

- [UID:0001NU][0x005c0460-0x005c0fe1.VirusChecker](by-memory/0x005c0460-0x005c0fe1.VirusChecker.md)
- [UID:0001NV][0x005c05a0-0x005c0937.VirusCheckerV3LoadAndScan](by-memory/0x005c05a0-0x005c0937.VirusCheckerV3LoadAndScan.md)
- [UID:0000FW][VirusChecker](by-class/VirusChecker.md)
- [UID:0001WG][VirusCheckerProcessTreeLayout](by-type/by-struct/VirusCheckerProcessTreeLayout.md)
- [UID:0000P5][VirusChecker](by-file/VirusChecker.md)
- [UID:0000SO][g_pVirusChecker](by-global/g_pVirusChecker.md)
- [UID:0001Q7][client_anticheat](by-meta/client_anticheat.md)

## Changes

- 2026-08-06 B003 UID0001NW placement callback: recorded the shared header-visible inline StringBase ToLower specialization, preserved VirusChecker as an inlined consumer, and historicalized named MCP sessions without deleting their findings.
- 2026-05-31 completion/confidence and reconstructable update:
  - What existed before: `COMPLETION:70`, `CONFIDENCE:85`, and blank `RECONSTRUCTABLE`.
  - Changed to: `COMPLETION:78`, `CONFIDENCE:88`, and `RECONSTRUCTABLE:TRUE`.
  - Summary/evidence: IDA MCP rechecked helper function starts/sizes, callback xrefs, node allocation/insertion/lookup/cleanup decompilation, and padding before `WaitableTimer`. Scores remain below 95 because final source-local container names and parent/code emission remain unaudited.
- 2026-06-07 A008 live IDA evidence refresh:
  - What existed before: the page carried `78/88` and summarized the helper family without the exact live size/callee/xref details for each nested helper.
  - Changed to: `COMPLETION:84` and `CONFIDENCE:90`, with an address-range section, exact function sizes, callback/xref flow, red-black insertion/rebalance details, lookup-or-insert behavior, destructor/global cleanup, and explicit assignment-gate reasoning.
  - Summary/evidence: IDA MCP `py_eval` and `decompile` refreshed all helper extents, the `0x005c0fe1-0x005c0ff0` padding, callback refs, tree insert/search/deallocate behavior, and V3/AhnLab cleanup side effects. The page remains unassigned because no direct parent candidate satisfies 85/85 and final source-local names/C++ are still unresolved.
- 2026-06-11 A003 Batch 200:
  - Before: `COMPLETION:84`, `CONFIDENCE:90`, and blank `AUTOGEN_PARENT_UID`.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:91`, and `AUTOGEN_PARENT_UID:0000FW`.
  - Summary/evidence: live IDA MCP reconfirmed exact helper extents, instruction/block counts, callback refs, local call structure, tree lookup/insert/rebalance behavior, recursive cleanup callers, scalar deleting destructor cleanup, and class-local ownership. Assignment is now valid because this child and direct class parent [UID:0000FW][VirusChecker](by-class/VirusChecker.md) both clear 85/85; formal helper-body C++ remains blank by helper-specific no-code policy until exact child method-body pages settle final string/container helper names.
- 2026-06-20 B004 Rule 26 source-quality incorporation:
  - Before: `COMPLETION:85`, `CONFIDENCE:91`.
  - After: `COMPLETION:87`, `CONFIDENCE:91`.
  - Summary/evidence: added source-facing names for `InsertModulePath`, `V3ModuleEnumCallback`, `V3ProcessEnumCallback`, `ModulePathSetAllocateNode`, `ModulePathSetInsertRebalance`, `ModulePathSetFindOrInsert`, and `DestroyModulePathSetNodes`; documented argument roles, no-route caveat for direct insertion, compiler EH/scalar-deleting-destructor policy, and `std::set<String>` / project string-wrapper source model. The old local PE/Capstone proof source is historical after the 2026-06-29 B004 dated MCP evidence cleanup.
- 2026-06-29 B002 accepted implementation sync:
  - Score unchanged at `COMPLETION:87`, `CONFIDENCE:91`.
  - Summary/evidence: recorded that UID0001NV now emits the scan-side `V3ProcessEnumCallback` call while this page retains ownership of the process/module callbacks and path-tree helper bodies; dated MCP evidence reconfirmed callback refs `0x005c07da -> 0x005c0aa0` and `0x005c0aa6 -> 0x005c09f0`.
