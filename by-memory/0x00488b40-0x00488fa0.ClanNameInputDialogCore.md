*** UID:00010J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ClanNameInputDialog::ClanNameInputDialog(const unsigned char *packet)
    : DialogPane(L"", -1, true)
{
    char promptBytes[0x8000];
    wchar_t promptText[0x8000];

    const unsigned int promptLength = packet[3];
    memmove(promptBytes, packet + 4, promptLength);
    promptBytes[promptLength] = '\0';

    const int wideLength = MultiByteToWideChar(
        CP_ACP,
        0,
        promptBytes,
        promptLength,
        promptText,
        0x8000);

    if (static_cast<unsigned int>(wideLength) >= 0x8000) {
        __report_rangecheckfailure();
    }

    promptText[wideLength] = L'\0';

    AddControl(new EPFImageControlPane(L"DLGCLAN1.EPF", Rect(0, 0, 278, 161), 0, true));
    AddControl(new ImageButtonControlPane(14, Rect(65, 117, 137, 133)));
    AddControl(new ImageButtonControlPane(22, Rect(142, 117, 214, 133)));
    AddControl(new StaticTextControlPane(promptText, true, 128, Rect(51, 38, 228, 88)));
    AddControl(new TextEditControlPane(L"", true, 128, Rect(51, 94, 228, 107)));

    SelectControl(4);
    SetFocusedControl(1);
    SetCancelControl(2);
    SetBackgroundResource(L"DLGCLAN1.EPF", 0);
    SetMode(1);
    SetDisplayRect(Rect(155, 12, 433, 173));
    ShowDialog(false);
}

void ClanNameInputDialog::OnButtonClick(int closeContext, int controlId)
{
    if (controlId == 1) {
        wchar_t text[256];
        TextEditControlPane *textEdit =
            static_cast<TextEditControlPane *>(GetChildControl(4));

        textEdit->GetText(text, 255);
        OnSubmitText(text);
    } else if (controlId != 2) {
        return;
    }

    StartClose(closeContext);
    CloseDialog();
}

void ClanNameInputDialog::UpdateOkButtonState()
{
    ImageButtonControlPane *okButton =
        static_cast<ImageButtonControlPane *>(GetChildControl(1));
    TextEditControlPane *textEdit =
        static_cast<TextEditControlPane *>(GetChildControl(4));

    okButton->SetEnabled(!textEdit->IsEmpty());
}

bool ClanNameInputDialog::HandlePointerOrMouseEvent(Event *event)
{
    if (event->m_type == 3) {
        g_pClanStatusPane->HandlePointerOrMouseEvent(event);
    }

    return DialogPane::HandlePointerOrMouseEvent(event);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA confirms exact function bounds and internal padding, packet prompt decoding from `packet+3/+4`, `DLGCLAN1.EPF` control setup, OK/Cancel/text control ids, submit virtual dispatch, vtable-only handlers, 12 constructor refs, and `g_pClanStatusPane` input-event forwarding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00488b40-0x00488fa0 ClanNameInputDialogCore

## 2026-08-14 Event ABI And Source-Placement Resolution

- The exact `0x00488f70-0x00488fa0` definition is `bool ClanNameInputDialog::HandlePointerOrMouseEvent(Event *event)`. Current IDA has `Event` size `0x110`, no `InputEvent` UDT, and a mutable pointer frame argument. The body reads `event->m_type`, forwards the same pointer through `g_pClanStatusPane`'s secondary EventHandler view for type `3`, and returns `DialogPane::HandlePointerOrMouseEvent(event)`.
- Current IDA prestate for supervisor I04 remains `sub_488F70`, type `char __thiscall(void *this, int)`, SHA256 `AC0791E49589C1D43E2D39C108EEF0629F096D48619F4FAB13E0175F60625B34`, xrefs `0x615960,0x615a00,0x615aa0,0x615b40,0x615be0,0x615c80,0x615d20`, and callees `0x488f70,0x49e240`. Expected safe readback changes only source-facing name/type/FR and the frame argument to `event@0x10/0x4/Event *`; B007 does not mutate IDA.
- UID0000IA is now a non-emitting index. Owner/emitter and generated route remain UID0000I8 / `NexusTK/social/Clan.cpp`; no coordinated split remains open.
- Historical `OnInputEvent`, `InputEvent`, reference, and split-candidate wording below is superseded but retained as the prior evidence trail.

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: `ClanNameInputDialog` class core
- Current generated owner hint: `ClanNameInputDialog`
- Likely source module: [UID:0000I8][Clan](by-file/Clan.md)
- Confidence: very strong for exact function bounds, internal padding, packet prompt decoding, vtable/control layout, control ids, derived submit-slot dispatch, and Clan owner/emitter routing; medium-high for final inherited helper spellings, exact input-event return type, button-state slot names, and the future Clan.cpp versus ClanDialogs.cpp split.

## Covered Ranges

| Range | Function | Notes |
| --- | --- | --- |
| `0x00488b40-0x00488eab` | `ClanNameInputDialog::ClanNameInputDialog` | Stores the three `ClanNameInputDialog` vtable views, decodes the packet prompt from `{ len, bytes }` at `packet+3`, converts it to wide text, and builds the `DLGCLAN1.EPF` modal with OK, Cancel, prompt, text-edit, and hidden/default input controls. |
| `0x00488eab-0x00488eb0` | alignment | `align 10h` padding before the button handler. |
| `0x00488eb0-0x00488f24` | `ClanNameInputDialog::OnButtonClick` | For OK id `1`, reads control id `4` into a 255-character stack buffer, dispatches the submit virtual at primary vtable slot `+0x5c`, then closes. Cancel id `2` closes without submit; other ids return. |
| `0x00488f24-0x00488f30` | alignment | `align 10h` padding before the OK-state refresh helper. |
| `0x00488f30-0x00488f67` | `ClanNameInputDialog::UpdateOkButtonState` | Fetches OK control id `1` and text control id `4` from the inherited control host at `+0x1fc`, then jumps through the OK button enable/disable virtual depending on whether the text control is empty. |
| `0x00488f67-0x00488f70` | alignment | `align 10h` padding before the input-event handler. |
| `0x00488f70-0x00488fa0` | `ClanNameInputDialog::HandlePointerOrMouseEvent` | For event type `3`, forwards mutable `Event *` to the `g_pClanStatusPane` secondary EventHandler view, then calls the base handler. |

## Source-Facing Signatures

| Method | Recommended source-facing shape | Evidence / caveat |
| --- | --- | --- |
| constructor | `ClanNameInputDialog::ClanNameInputDialog(const unsigned char *packet)` | Reads the counted prompt length from `packet[3]` and bytes from `packet + 4`; decompiler `int`/`std::_Narrow_char_traits` forms are typing artifacts. |
| button handler | `ClanNameInputDialog::OnButtonClick(...)` | Handles OK logical id `1`, Cancel logical id `2`, and text edit id `4`; exact close-context/event parameter spelling follows the final dialog framework convention. |
| submit hook | `void OnSubmitText(const wchar_t *text)` | Primary vtable slot `+0x5c` / slot index `23` dispatches the submitted wide-text buffer and consumes no result. Fresh current decompilation of all six derived leaves produces no return value, resolving the shared callback as void. |
| OK-state refresh | `ClanNameInputDialog::UpdateOkButtonState()` | Fetches OK id `1` and text edit id `4` through the inherited control host at `+0x1fc`; empty text takes OK-button slot `+0x50`, non-empty text takes slot `+0x4c`. |
| pointer/mouse event | `ClanNameInputDialog::HandlePointerOrMouseEvent(Event *event)` | Checks `event->m_type`; for type `3`, forwards through `g_pClanStatusPane + 0xa0`, then returns the base `DialogPane` handler result. |

## Control And Resource Inventory

| Role | Logical id / resource | Rect | Notes |
| --- | --- | --- | --- |
| EPF background/resource pane | `DLGCLAN1.EPF` | `(0,0)-(278,161)` | Constructed first and added to the dialog. |
| OK image button | image/resource index `14`, logical control id `1` | `(65,117)-(137,133)` | The image index is not the logical control id. |
| Cancel image button | image/resource index `22`, logical control id `2` | `(142,117)-(214,133)` | The image index is not the logical control id. |
| Prompt/static text | decoded packet prompt | `(51,38)-(228,88)` | Uses the counted CP_ACP prompt converted to wide text. |
| Text edit | logical control id `4` | `(51,94)-(228,107)` | Read by OK submit and tested by the OK-state refresh helper. |
| Dialog display rect | n/a | `(155,12)-(433,173)` | Final positioning/display rectangle. |

## Behavior Notes

- The constructor calls the dialog base constructor with an empty wide string, then stores primary/secondary/tertiary vtables at `this+0x000`, `this+0x0a0`, and `this+0x0a4`.
- The packet prompt is counted protocol text, not NUL-terminated protocol text: `packet[3]` is a byte length and `packet + 4` is the byte payload. The constructor appends a local byte terminator, but the `MultiByteToWideChar(CP_ACP, 0, promptBytes, promptLength, promptWide, 0x8000)` call uses the explicit count before writing the wide terminator at `promptWide[wideLength]`.
- Controls are fetched through the inherited control host pointer at `this+0x1fc`; control id `1` is OK, id `2` is Cancel, and id `4` is the text edit field used by OK-state validation and submission.
- `OnButtonClick` reads text edit id `4` into a 255-wide-character stack buffer, dispatches `void OnSubmitText(const wchar_t *text)` through primary vtable slot `+0x5c` without consuming a result, then closes for OK; Cancel closes without submit and other ids return. The existing formal `OnSubmitText(text);` statement therefore matches the live callback contract.
- `UpdateOkButtonState` fetches OK id `1` and text edit id `4`; the empty-text path calls the OK button state slot at `+0x50`, and the non-empty path calls slot `+0x4c`, source-facing as `SetEnabled(!textEdit->IsEmpty())` until the button class finalizes those slot names.
- `OnInputEvent` reads [UID:0002B5][0x0067adc4-0x0067adec.UiChatClanSingletonSlots](by-memory/0x0067adc4-0x0067adec.UiChatClanSingletonSlots.md) slot `dword_67ADE4` (`g_pClanStatusPane`) and dispatches through the secondary view at `+0xa0` when the event byte at `event+4` is `3`, then returns the base `DialogPane` input-event result. The binary body does not directly call `UpdateOkButtonState`; that refresh helper is a separate vtable-backed method likely invoked by framework input flow.

## IDA MCP Evidence

Checked on 2026-05-23: IDA confirms starts at `0x00488b40`, `0x00488eb0`, `0x00488f30`, and `0x00488f70`.

2026-05-26 vtable pass: IDA confirms `ClanNameInputDialog` vtable bases at `0x006158f8`, `0x0061595c`, and `0x0061598c`, with constructor stores at `0x00488b9e`, `0x00488ba4`, and `0x00488bae`.

2026-05-26 layout pass: IDA decompilation confirms the inherited control host at `+0x1fc`. `OnButtonClick` fetches text edit control id `4`, calls the submit virtual at primary vtable slot `+0x5c`, and closes on OK or Cancel. `UpdateOkButtonState` fetches control ids `1` and `4`.

2026-05-27 boundary pass: IDA disassembly shows `sub_488F70` ending with `retn 4` at `0x00488f9d`, whose final immediate byte is at `0x00488f9f`. The correct exclusive end is therefore `0x00488fa0`, not `0x00488f9f`.

2026-06-05 live IDA MCP recheck:

- `lookup_funcs` reports `sub_488B40` size `0x36b`, `sub_488EB0` size `0x74`, `sub_488F30` size `0x37`, and `sub_488F70` size `0x30`; `0x00488fa0` is not a function. The exact exclusive function ends are therefore `0x00488eab`, `0x00488f24`, `0x00488f67`, and `0x00488fa0`.
- Gap disassembly reports `align 10h` padding at `0x00488eab`, `0x00488f24`, and `0x00488f67`.
- Constructor decompilation copies `(unsigned char)packet[3]` bytes from `packet+4`, appends a local byte terminator, converts that multibyte text with `MultiByteToWideChar`, and writes the resulting wide text into the prompt/input control setup.
- `callers`/`xrefs_to` for the constructor show 12 code refs: six dialog-open paths inside [UID:00021C][0x00484f70-0x0048540b.ClanStatusPacketDispatcher](by-memory/0x00484f70-0x0048540b.ClanStatusPacketDispatcher.md) at `0x0048511b`, `0x004851c6`, `0x00485227`, `0x0048527f`, `0x00485332`, and `0x00485393`, plus six raw derived-constructor calls at `0x00488fad`, `0x0048907d`, `0x0048914d`, `0x0048927d`, `0x004893ad`, and `0x004894dd`.
- `xrefs_to` for `0x00488eb0`, `0x00488f30`, and `0x00488f70` are vtable data refs across the seven name/money/text dialog vtables; no direct code callers were reported for those virtual handlers.
- `callees` for the constructor cover dialog/control construction helpers, packet/string copy helpers (`sub_516030`, `sub_516220`), `MultiByteToWideChar`, UI layout/control add helpers, security-cookie support, and range-check failure. The handlers call the text-control read/empty helpers, dialog close helpers, the base input-event helper, and the `g_pClanStatusPane` event-forwarding path described above.

## Ownership Decision

This is the shared base for clan name/money input flows and belongs in the Clan module.

Keep direct owner/emitter [UID:0000I8][Clan](by-file/Clan.md) and generated output under `auto-generated/NexusTK/social/Clan.cpp`. [UID:0000IA][ClanDialogs](by-file/ClanDialogs.md) is a non-emitting family index with path/owner `NONE`, not a future source destination.

## Score Rationale

`88/91`: B002/B009 source-quality reanalysis resolves the stale blank-C++ blocker. Exact function sizes, internal padding, constructor prompt decoding, CP_ACP conversion, control ids/resources, inherited control-host offset, submit virtual slot, derived submitter evidence, OK-state helper polarity, type-3 status-pane forwarding, caller/vtable reachability, and Clan owner/emitter routing support first-draft C++. Completion stays below 90 and confidence below 95 because inherited UI helper names, final `OnButtonClick`/`OnInputEvent` signature spelling, final OK-button state-slot names, and the future Clan.cpp versus ClanDialogs.cpp split remain provisional.

## Cross-References

- [UID:00002J][ClanNameInputDialog](by-class/ClanNameInputDialog.md)
- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:0001X9][ClanDialogVtableFamily](by-type/by-vtable/ClanDialogVtableFamily.md)
- [UID:0001TY][ClanInputDialogLayouts](by-type/by-struct/ClanInputDialogLayouts.md)
- [UID:00021C][0x00484f70-0x0048540b.ClanStatusPacketDispatcher](by-memory/0x00484f70-0x0048540b.ClanStatusPacketDispatcher.md)
- [UID:0002B5][0x0067adc4-0x0067adec.UiChatClanSingletonSlots](by-memory/0x0067adc4-0x0067adec.UiChatClanSingletonSlots.md)

## Changes

- 2026-08-10 B006 UID0002NU family-coherence callback:
  - Resolved the shared submit slot as `void OnSubmitText(const wchar_t *text)` from the ignored base-call result and six current leaf bodies.
  - Confirmed the existing formal `OnSubmitText(text);` statement is already correct and requires no return-value handling.
  - Kept `88/91`, owner/emitter `0000I8`, reconstructable true, and the active `Clan.cpp` route.

- 2026-06-20 B002/B009 Rule 26 source-quality incorporation:
  - Changed from: `84/90`, blank C++, and stale submit-slot/source-placement blockers.
  - Changed to: `88/91`, owner/emitter unchanged as [UID:0000I8][Clan](by-file/Clan.md), and first-draft constructor/`OnButtonClick`/`UpdateOkButtonState`/`OnInputEvent` C++ populated under the active gate.
  - Summary/evidence: incorporated both B-agent reports' valid details without arbitrary pruning: exact method bounds and padding, counted prompt decode from `packet[3]`/`packet+4`, explicit `MultiByteToWideChar(CP_ACP)` count behavior, `0x8000` stack buffer/range-check guard, `DLGCLAN1.EPF` layout, logical ids OK `1`/Cancel `2`/text `4`, image indices `14` and `22`, inherited control host at `+0x1fc`, submit virtual slot `+0x5c` / index `23` now inferred as `OnSubmitText(const wchar_t *)` from the six accepted derived submitters, OK-state polarity through slots `+0x50` and `+0x4c`, `g_pClanStatusPane` event-type `3` forwarding through secondary view `+0xa0`, constructor and vtable reachability, current Clan.cpp owner route over one-off ClanDialogs migration, rejected raw/decompiler names, and residual helper/signature/file-split caveats.
- 2026-06-05: Raised from `70/85` to `84/90`, marked reconstructable under [UID:0000I8][Clan](by-file/Clan.md), corrected function-table ranges to exclusive IDA ends, documented internal alignment, refreshed constructor/button/input-event behavior, and linked the Clan status singleton event-forwarding dependency. Evidence: live IDA MCP `lookup_funcs`, `decompile`, `callers`, `callees`, `xrefs_to`, and `disasm` for `0x00488b40`, `0x00488eb0`, `0x00488f30`, and `0x00488f70`.
- 2026-05-27: Corrected the documented range end from `0x00488f9f` to `0x00488fa0`. Evidence: IDA MCP disassembly of `sub_488F70` shows `retn 4` at `0x00488f9d`, with instruction bytes through `0x00488f9f`.
