*** UID:0002LN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace mystr
{
int WideRangeCompare(const wchar_t *lhsBegin,
                     const wchar_t *lhsEnd,
                     const wchar_t *rhsBegin,
                     const wchar_t *rhsEnd)
{
    const wchar_t *lhs = lhsBegin;
    const wchar_t *rhs = rhsBegin;

    while (lhs != lhsEnd && rhs != rhsEnd)
    {
        if (*lhs != *rhs)
            return *lhs < *rhs ? -1 : 1;
        ++lhs;
        ++rhs;
    }

    const unsigned int lhsLength =
        static_cast<unsigned int>(lhsEnd - lhsBegin);
    const unsigned int rhsLength =
        static_cast<unsigned int>(rhsEnd - rhsBegin);
    if (lhsLength == rhsLength)
        return 0;
    return lhsLength < rhsLength ? -1 : 1;
}
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Lexicographic UTF-16 range comparator with exact half-open range, 29 callers, no callees, word-compare loop, length tie-break, [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md)/[UID:0002LO][0x005845b0-0x005845ec.StringBaseCompareWideLiteral](by-memory/0x005845b0-0x005845ec.StringBaseCompareWideLiteral.md) consumers, direct [UID:0000OA][StringBase](by-file/StringBase.md) route, and formal `WideRangeCompare` C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***



# 0x005840f0-0x0058415a WideRangeCompare

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: helper function / UTF-16 range comparator.
- Likely owner: [UID:0000OA][StringBase](by-file/StringBase.md) / [UID:0001W4][SimpleUStringPointerBackedLayout](by-type/by-struct/SimpleUStringPointerBackedLayout.md).
- Confidence: strong for exact range, behavior, caller spread, no-callee status, and source-file owner; medium-high for final source name.
- Assignment: attached to [UID:0000OA][StringBase](by-file/StringBase.md). This child is now `89/92`, the direct file parent is `91/90`, and the formal `WideRangeCompare` helper is emitted through the StringBase source root.
- Verification basis: IDA MCP `lookup`, `decompile`, xref/caller grouping, outgoing refs, and byte checks refreshed on 2026-06-11.

## Covered Range

- Start: `0x005840f0`
- End: `0x0058415a` exclusive
- Size: `0x6a`
- Next bytes: `0x0058415a-0x00584160` are `0xcc` padding by IDA byte check.

## Behavior

This function compares two UTF-16 ranges. It receives begin/end byte addresses for the left range and begin/end pointers for the right range, compares up to the shorter character count, and returns `-1`, `0`, or `1` based on the first differing character or final length difference.

The return behavior matches a lexicographic wide-string range comparison:

- return `-1` when the left range sorts before the right range;
- return `1` when the left range sorts after the right range;
- return `0` when both ranges have the same length and content.

Observed calling shape:

```text
int WideRangeCompare(const wchar_t *lhsBegin,
                     const wchar_t *lhsEnd,
                     const wchar_t *rhsBegin,
                     const wchar_t *rhsEnd);
```

The implementation computes both character counts by subtracting begin from end and shifting by one, compares UTF-16 words in a tight loop, and only uses the final length comparison when all shared-prefix code units match.

## Evidence

- IDA reports the exact function extent as `0x005840f0-0x0058415a`, size `0x6a`; the following `0x0058415a-0x00584160` bytes are all `0xcc` alignment padding.
- IDA reports no outgoing calls from this function.
- `refs_to_start` reports 29 calls grouped under string compare/search users, UI/layout helpers, network/name-list processing, and the literal wrapper at `0x005845b0`.
- The live decompilation computes the right and left character lengths from byte-address begin/end pairs, chooses the shorter count, compares UTF-16 code units, returns on the first difference, and then resolves equal prefixes by comparing total lengths.
- The disassembly uses a word-compare loop beginning near `0x00584120` and branch paths that normalize results to `-1`, `0`, or `1`, supporting this as a range comparator rather than a boolean predicate.
- 2026-06-11 live IDA MCP recheck reconfirmed the exact `0x005840f0-0x0058415a` function extent, six `0xcc` bytes through `0x00584160`, no modeled callees, 29 start xrefs from 14 caller functions, and the literal-compare wrapper at `0x005845b0` as one of the callers.

## Assignment Gate

`CANONICAL_OWNER` and `EMITTER_UIDS` are set to [UID:0000OA][StringBase](by-file/StringBase.md). This child reaches `89/92`, and the file parent clears the current source-attachment gate at `91/90`. [UID:0000OB][StringUtil](by-file/StringUtil.md) remains broader utility-string context, and [UID:0001J3][0x005832f0-0x00584d7e.StringBaseReleaseFormatAndMutation](by-memory/0x005832f0-0x00584d7e.StringBaseReleaseFormatAndMutation.md) remains an inventory aggregate rather than the direct source parent for this comparator.

[UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) uses this comparator from its `0x00584550` bounded wide substring/literal compare helper. That relationship is a compare consumer, not evidence that the [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) tail is a search/find helper.

## Open Questions

- Exact original source name remains unresolved. Candidate roles are a string traits comparator, `StringBase<wchar_t>::compare` helper, or a local `CompareWideRange` utility.

## Cross-References

- [UID:0001J2][0x00583210-0x005832f0.StringBaseFormatConstructors](by-memory/0x00583210-0x005832f0.StringBaseFormatConstructors.md)
- [UID:0001W4][SimpleUStringPointerBackedLayout](by-type/by-struct/SimpleUStringPointerBackedLayout.md)
- [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md)

## 2026-07-30 UID0002MZ Source And Header Placement

- The implementation remains the namespace-level `mystr::WideRangeCompare` definition represented by this exact by-memory range and routed through [UID:0000OA][StringBase](by-file/StringBase.md) into `NexusTK/util/StringBase.cpp`.
- Its declaration belongs in the shared `NexusTK/util/StringBase.h` surface emitted by [UID:0001WS][StringBaseTemplate](by-type/by-template/StringBaseTemplate.md): `int WideRangeCompare(const wchar_t *lhsBegin, const wchar_t *lhsEnd, const wchar_t *rhsBegin, const wchar_t *rhsEnd);` inside `namespace mystr`.
- Consumers such as [UID:000343][0x004b3350-0x004b33fc.FolderTreeDirectoryEntryNameLess](by-memory/0x004b3350-0x004b33fc.FolderTreeDirectoryEntryNameLess.md) call `mystr::WideRangeCompare`; they must not invent an unqualified external helper, duplicate the declaration in a consumer-local header, or move this source body into `FolderTreePane.cpp`.
- The source-facing name and namespace are inferred from the existing StringBase owner and definitions, while the half-open range behavior, UTF-16 word comparison, signed result, and length tie-break remain fixed by the binary.

## Changes

- 2026-06-30 B011 [UID:0000OA][StringBase](by-file/StringBase.md) empty-emitter implementation:
  - Before: `COMPLETION:87`, `CONFIDENCE:91`, formal C++ blank.
  - After: `COMPLETION:89`, `CONFIDENCE:92`, formal `WideRangeCompare` C++ populated.
  - Summary/evidence: current MCP session `supervisor_resume_20260629` reconfirmed `0x005840f0 sub_5840F0 size 0x6a`, no callees, 29 xrefs, six bytes of successor padding, and lexicographic UTF-16 half-open range comparison with a length tie-break. The source block uses a simple UTF-16 loop rather than mirroring compiler-level word-loop details.
- 2026-06-07 A008: Raised completion/confidence from `76/86` to `86/90` after refreshing IDA evidence for the exact half-open function extent, padding, no-callee status, 29 caller xrefs, word-compare loop, length tie-break, and normalized `-1/0/1` return behavior. Corrected the displayed range end from the last instruction address to the exclusive IDA end. At that time the page remained unassigned because all direct candidate parents appeared to fail the corrected 85/85 assignment gate; this gate state was superseded by the 2026-06-11 [UID:0000OA][StringBase](by-file/StringBase.md) assignment.
- 2026-06-11 A003 Batch191: raised to `87/91` and set current `CANONICAL_OWNER:0000OA` / `EMITTER_UIDS:0000OA` after live IDA MCP reconfirmed the exact bounds, padding, no-callee status, 29 start xrefs from 14 caller functions, decompiled UTF-16 range comparison behavior, and the current [UID:0000OA][StringBase](by-file/StringBase.md) parent score of `88/86`.
- 2026-06-25 B005 support update:
  - Score unchanged.
  - Summary/evidence: corrected stale "left unassigned under parent gate" and `AUTOGEN_PARENT_UID` wording to current `CANONICAL_OWNER`/`EMITTER_UIDS` language, preserving [UID:0000OA][StringBase](by-file/StringBase.md) as both owner and emitter route. Added [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) as a bounded compare consumer through its `CompareWideSubstringWithLiteral` tail, while explicitly keeping that tail distinct from a true find/search helper.
