*** UID:0002IE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void HierList::RemoveByPayload(const void *payload)
{
    HierList *ownerList;
    int index;
    HierListNode *node = FindNodeByPayload(payload, &ownerList, &index);
    if (node == NULL) {
        return;
    }

    if (node->childList != NULL) {
        delete node->childList;
        node->childList = NULL;
    }

    ownerList->RemoveAt(index, 1);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Emits `HierList::RemoveByPayload`, resolving a payload to node/owner/index, deleting any child list, and removing one owner-list row. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ceb00-0x004ceb45 HierListRemoveNode

## Layer Consumer Synchronization - 2026-07-14

[UID:0004NV][0x004f0a60-0x004f0b3d.LayerRemoveChild](by-memory/0x004f0a60-0x004f0b3d.LayerRemoveChild.md) calls the accepted `HierList::RemoveByPayload` body at `0x004f0b25` after Layer has released the node's owned Region and descendant Regions. The argument is the 48-byte LayerNode payload, not a HierListNode header; HierList resolves the owning list/index and owns row removal.

Layer and HierList lifetimes remain separated: Layer owns per-node Regions and Pane-state restoration, while HierList owns child-list/row removal. Existing score, ownership, source name, and formal C++ are preserved.

## Status

- Entity kind: class method
- Owner: [UID:000065][HierList](by-class/HierList.md)
- Likely source file: [UID:0000JV][HierList](by-file/HierList.md)
- Parent aggregate: [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md)
- Rebuild handling: source-authored, reconstructable.
- Autogen status: attached to [UID:000065][HierList](by-class/HierList.md). The class and file docs are high-confidence enough for method ownership, and this exact child now carries formal first-draft `HierList::RemoveByPayload` C++.

## Behavior

Finds a node by caller payload, deletes the node's child `HierList` if present, clears the child pointer, and removes exactly one row from the owning list through the virtual remove-at slot.

## Evidence

- IDA MCP `py_eval` confirmed an IDA function object at `0x004ceb00-0x004ceb45`, size `0x45`.
- IDA caller evidence links this method to [UID:00018W][0x004f0480-0x004f1bf6.Layer](by-memory/0x004f0480-0x004f1bf6.Layer.md) at `0x004f0b25`.
- Live IDA MCP on 2026-06-03 reconfirmed the same modeled function object, one direct caller at `0x004f0b25` inside `sub_4F0A60`, and the `0x004ceb12 -> 0x004cec30` lookup call.
- The method calls [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md), deletes a found child list through node `+0x04` via its scalar deleting destructor, clears node `+0x04`, then dispatches the owner list's remove-at slot `+0x1c` with the found index and count `1`.

## 2026-06-14 Live IDA Pass

Active IDA MCP session `a001_goal2_class_batch` reports `NexusTK.exe.i64` loaded for `NexusTK.exe`, imagebase `0x400000`, with auto-analysis and Hex-Rays ready.

| Query | Result |
| --- | --- |
| `lookup_funcs 0x004ceb00` | Modeled function `sub_4CEB00`, size `0x45`; local `int_convert.py` converts `0x45` to `69` bytes. |
| `lookup_funcs 0x004ceb45/0x004ceb46` | No function at either endpoint; `0x004ceb50` is the next `HierList` function. |
| `xrefs_to 0x004ceb00` | Exactly one code xref, `0x004f0b25` in `sub_4F0A60`, matching the Layer removal path. |
| `callees 0x004ceb00` | One modeled direct callee, `sub_4CEC30`, the recursive node lookup page. |
| `analyze_function 0x004ceb00` | Five basic blocks, cyclomatic complexity `3`, decompilation matching lookup -> optional child-list delete/clear -> owner remove-at dispatch. |
| `get_bytes 0x004ceafc size 0x60` | Four `0xcc` bytes before the function, exact `0x45` byte body ending in `retn 4`, then eleven `0xcc` bytes before `0x004ceb50`. |

The current decompiler shape returns `0` if lookup fails. On success it reads the child pointer from node `+0x04`, calls the child's virtual slot `+0x00` with delete flag `1`, clears `node+0x04`, then calls the owning list's virtual remove-at slot `+0x1c` with the found index and count `1`. The only caller context in `sub_4F0A60` first locates a Layer child payload, performs Layer-side cleanup, and then calls this method with the payload pointer at `0x004f0b25`.

## Confidence And Caveats

The remove-node role, owner, function boundary, caller, callee, and mutation sequence are strong. The final source name is still treated as likely, not final, because original header/API names have not been recovered directly.

## Score Rationale

Raised to `90/92` after the 2026-06-26 B001 implementation callback resolved the previous blank-C++/API-name blocker. Completion now covers current MCP provenance, exact function boundary and padding, one Layer caller, recursive lookup callee, child-list delete/clear sequence, owner-list virtual `RemoveAt(index, 1)` dispatch, source-form analysis, rejected alternatives, and formal first-draft C++ for this exact range. Confidence is strong because disassembly, decompilation, bytes, unique signature, caller/callee/xref evidence, [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md) lookup semantics, [UID:0001UQ][HierListNode](by-type/by-struct/HierListNode.md) `childList`/`payload` layout, and [UID:0002II][0x004cebc0-0x004cebec.HierListRemoveAt](by-memory/0x004cebc0-0x004cebec.HierListRemoveAt.md) remove-at behavior all agree. It remains below final-audit level because the exact original spelling `RemoveByPayload` and `void` return declaration are inferred source-facing choices rather than recovered original symbols, and the broader `HierList` declaration/destructor policy still needs a coordinated class-wide pass.

## 2026-06-26 B001 Accepted Source-Quality Implementation

Active IDA MCP session `80de0a67` was available for the accepted report. The endpoint `http://127.0.0.1:13337/mcp` responded to `initialize` and `tools/list`; `server_health` reported `NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and idle analysis state.

Target evidence from that pass:

- `lookup_funcs 0x004ceb00` reports `sub_4CEB00`, size `0x45`; `lookup_funcs 0x004ceb44` resolves inside the function and `lookup_funcs 0x004ceb45` reports no function.
- `get_bytes 0x004ceafc size 0x60` shows four `0xcc` bytes before the target, the exact `0x45` byte body, eleven `0xcc` bytes after `0x004ceb45`, and successor [UID:0002IF][0x004ceb50-0x004ceb62.HierListGetElementAt](by-memory/0x004ceb50-0x004ceb62.HierListGetElementAt.md) at `0x004ceb50`.
- `make_signature_for_range 0x004ceb00-0x004ceb45` returns a unique wildcarded IDA signature.
- `xrefs_to 0x004ceb00` reports exactly one code xref at `0x004f0b25` inside [UID:00018W][0x004f0480-0x004f1bf6.Layer](by-memory/0x004f0480-0x004f1bf6.Layer.md), the `Layer::RemoveChild` route.
- `callees 0x004ceb00` reports the direct call to [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md), now accepted as `HierList::FindNodeByPayload`.
- `decompile` and `disasm` both show `FindNodeByPayload(payload, &ownerList, &index)`, a null-return path, optional child-list deleting-destructor call with delete flag `1`, `node+0x04` clear, and owner-list vtable slot `+0x1c` dispatch with the found index and count `1`.
- Negative checks found no recovered original symbol and no pointer/data/immediate route to the target start/end; this keeps confidence below final-source level but no longer blocks a human source-facing method name.

Source form decision:

- The emitted method name is `HierList::RemoveByPayload(const void *payload)`. `HierListRemoveNode` remains the stable by-memory/documentation label.
- `RemoveByPayload` is preferred because the explicit argument is the caller payload pointer passed by `Layer`, and the method resolves the actual node through `FindNodeByPayload`; `RemoveNode` would imply a node-pointer API that the binary does not show.
- `void` is the best source-facing return type because the method is a side-effect-only removal helper. The observed `eax` values are residual zero-on-miss or delegated remove-at output, and the only modeled caller is a Layer cleanup path that does not consume a semantic result.
- `delete node->childList; node->childList = NULL;` is the source-level form of the virtual deleting-destructor call plus clear; source should not hand-author the scalar-deleting-destructor ABI flag.
- `ownerList->RemoveAt(index, 1);` preserves the important owner-list behavior when the matched node belongs to a child list rather than the root list.

Rejected alternatives preserved from the accepted report: leave formal C++ blank pending original API proof; emit the primary method name as `HierListRemoveNode`; use generic `Remove`; use an `int` return type; move ownership to `Layer` or `List`; emit this through the non-emitting [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md) aggregate; or hand-code the scalar-deleting destructor wrapper.

## Changes

- 2026-06-26 B001 accepted source-quality implementation:
  - Raised from `86/90` to `90/92`.
  - Inserted formal exact-child `void HierList::RemoveByPayload(const void *payload)` C++.
  - Evidence: B001 live MCP session `80de0a67` reconfirmed the exact `0x004ceb00-0x004ceb45` boundary, `Layer::RemoveChild` caller at `0x004f0b25`, direct lookup call to `FindNodeByPayload`, child-list deleting-destructor call and clear through `node->childList`, owner-list `RemoveAt(index, 1)` dispatch, unique signature, endpoint padding, and negative original-symbol/pointer-route checks.
  - Source-quality decision: `RemoveByPayload` is the best current human source-facing name; `HierListRemoveNode` remains the stable by-memory label.

- 2026-06-17 B001 source-quality execution:
  - Raised from `85/89` to `86/90`.
  - Evidence: B001 rechecked the `Layer::RemoveChild` caller, payload lookup, child-list scalar delete/clear through node `+0x04`, owner-list `RemoveAt(index, 1)` dispatch, and best current source-facing name `RemoveByPayload`.
  - Formal C++ remains blank by accepted class-wide API declaration sequencing.

- 2026-06-03 live IDA recheck and parent attachment:
  - What existed before: scored `70/84`, reconstructable, but had no autogen parent and only a short behavior summary.
  - Changed to: scored `76/88`, attached to [UID:000065][HierList](by-class/HierList.md), and expanded the live caller/callee and deletion-dispatch evidence.
  - Summary/evidence: IDA MCP verified `0x004ceb00-0x004ceb45`, caller `0x004f0b25` inside `sub_4F0A60`, lookup call `0x004ceb12 -> 0x004cec30`, child-list scalar delete through node `+0x04`, clear of node `+0x04`, and owner-list remove-at dispatch through vtable slot `+0x1c`. Final C++ remains blank because source-level naming is not final.

- 2026-06-14 C001 low-completion pass:
  - What existed before: scored `76/88` with correct owner/emitter routing but stale live evidence and no detailed decompiler/byte/caller-context summary from the current IDA session.
  - Changed to: scored `85/89`, added current IDA MCP session, lookup/xref/callee/decompiler/component, byte-boundary, and local integer-conversion evidence. Owner [UID:000065][HierList](by-class/HierList.md), emitter [UID:000065][HierList](by-class/HierList.md), reconstructable flag, and blank final C++ block are unchanged.
  - Summary/evidence: IDA MCP verified active `NexusTK.exe.i64`, modeled `sub_4CEB00` size `0x45`, one `Layer` caller at `0x004f0b25`, sole direct callee `sub_4CEC30`, child-list delete/clear through node `+0x04`, remove-at dispatch through slot `+0x1c`, and surrounding `0xcc` padding. Final C++ remains blank because the source API spelling and declaration form are not final-source quality.
