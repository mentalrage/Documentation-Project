*** UID:0004DS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact live MapPane object screen-bounds updater with 44 code xrefs, tile-origin/movement interpolation and RectBounds operations; ObjectPane slot +0x50 is resolved as ClearOutputRects(paneBounds,objectDataBounds) and slot +0x2c as SetBounds(paneBounds,objectDataBounds), while remaining movement fields and MapPane virtuals preserve its target-specific blank-C++ proof. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00506980-0x00506ad4 MapPane UpdateObjectPaneScreenBounds

## Status

- Disposition: reconstructable MapPane source helper with target-specific blank formal C++.
- Entity kind: exact MapPane child method.
- Working source-facing name: `MapPane::UpdateObjectPaneScreenBounds`.
- Owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md), source root [UID:0000L3][MapPane](by-file/MapPane.md).
- Parent split container: [UID:00037Q][0x00506970-0x00506cdd.MapPaneObjectLookupHelpers](by-memory/0x00506970-0x00506cdd.MapPaneObjectLookupHelpers.md).

## Evidence

Current accepted B005 MCP evidence used live NexusTK IDB session `nexustk_supervisor_20260704`. `server_health` was ok with Hex-Rays ready. Narrow `lookup_funcs` confirmed `sub_506980` at exact range `0x00506980-0x00506ad4`, size `0x154` / 340 bytes; `0x00506ad4` is not a function and successor `sub_506AE0` begins at `0x00506ae0`. Bounded disassembly returned 117 lines and the function has 7 basic blocks. The body has a normal prologue, security-cookie setup/check, and `retn 4`; bytes at the end show the function tail followed by `0xcc` padding from `0x00506ad4` to the successor, while bytes before `0x00506980` show the previous child tail/padding before this prologue.

The helper copies object map position twice through [UID:0003XP][0x005374d0-0x005374eb.ObjectPaneGetMapPosition](by-memory/0x005374d0-0x005374eb.ObjectPaneGetMapPosition.md), converts map coordinates through MapPane tile-origin fields `+0x3fc/+0x400` and tile-dimension globals `word_66DA9C` / `word_66DAA0` (both read as `48` in the MCP pass), applies movement substep interpolation through [UID:00041W][0x00505080-0x00505100.MapPaneDirectionToTileOffset](by-memory/0x00505080-0x00505100.MapPaneDirectionToTileOffset.md) and `byte_66DA96` (read as `1`), offsets two `RectBounds` values through `sub_4B78F0` / `RectBounds::Offset`, optionally inflates or insets a temporary rectangle through `sub_4B7E30`, and then calls object/MapPane virtual paths to update or invalidate the affected screen bounds.

The ObjectPane calls are now partly source-resolved. Slot `+0x50` is [UID:00020R][0x00469050-0x0046907a.ObjectPaneClearOutputRects](by-memory/0x00469050-0x0046907a.ObjectPaneClearOutputRects.md), which initializes `paneBounds` and `objectDataBounds`; slot `+0x2c` is `ObjectPane::SetBounds(const RectBounds *paneBounds, const RectBounds *objectDataBounds)`. These names and output roles agree with the complete ObjectPane declaration and [UID:0003HT][0x00537bf0-0x00537c65.ItemObjectPaneClearOutputRects](by-memory/0x00537bf0-0x00537c65.ItemObjectPaneClearOutputRects.md). Slot `+0x20` and the two MapPane virtual calls remain independently unresolved.

MCP `callees` for the target are `sub_5374D0` (`ObjectPaneGetMapPosition`), `sub_505080` (`DirectionToTileOffset`), `sub_4B78F0` (`RectBounds::Offset`), `sub_4B7E30` (RectBounds/geometry inflate helper), and the stack-cookie check. `xref_query type=code` found 44 code xrefs to `0x00506980`; representative callers include MapPane/object creation and traversal paths at `0x00505e00`, `0x00507c90`, and `0x0050f030`, object-pane update paths such as `0x00537bc0` and `0x0053c290`, the raw relay at `0x0053c37d` that loads `g_activeMapPane`, and several `0x005a...` local-player/living-object update consumers. MCP `xref_query type=data` found no data xrefs to the function address, so the helper is live through calls rather than a callback-table or vtable-entry route.

The function is not an ObjectList method: the receiver is MapPane, the helper uses MapPane coordinate state, and ObjectList remains only a dependency for object storage and lookup.

Current support-page checks are consistent with this route. [UID:00007Q][MapPane](by-class/MapPane.md) already records the object lookup/traversal split, MapPane field names such as `m_tileOriginX`/`m_tileOriginY` and `m_objectList`, and `g_movementSubstepScale` ownership by the MapPane coordinate/movement surface. [UID:0000L3][MapPane](by-file/MapPane.md) routes the split through `NexusTK/map/MapPane.cpp` and records that UID0004DS is an object coordinate/update helper using tile dimensions and movement interpolation. [UID:00037Q][0x00506970-0x00506cdd.MapPaneObjectLookupHelpers](by-memory/0x00506970-0x00506cdd.MapPaneObjectLookupHelpers.md) already preserves the exact child row, `0x00506ad4-0x00506ae0` padding, and blank-C++ disposition for this child. ObjectPane and RectBounds support docs provide dependency evidence but do not transfer ownership away from MapPane.

## No-Code Proof

Formal C++ stays blank even though the owner/emitter route and two ObjectPane slots are confirmed. The accepted B005 report proves the helper is source-authored MapPane code, but the body still depends on remaining movement fields and virtuals that are not source-quality stable enough for a formal source body.

The exact remaining blockers are target-specific: ObjectPane slot `+0x20` and MapPane slots `+0x28`/`+0x20` remain unresolved; movement/object fields at `+0x1c4`, `+0x1c5`, `+0x1c6`, and `+0x1e8` are behaviorally tied to interpolation and refresh state but do not yet have accepted source-facing names. The former `+0x50`, `+0x2c`, `MapPoint`, output-order, and generic rectangle-type blockers are superseded: they now resolve to `ClearOutputRects`, `SetBounds`, exact `MapPoint`, and `RectBounds`.

Writing this method today would still require raw offsets for the unresolved slot/field subset, decompiler temporaries, raw stack locals, invented MapPane/member names, or `this[255]`/`this[256]`-style access. Those would be less source-quality than the current documentation. The correct state is therefore reconstructable MapPane ownership with a nonblank emitter route, a blank formal C++ block, and this narrowed no-code proof; the resolved ObjectPane calls must not be regressed to anonymous vtable offsets.

At report creation, generated `auto-generated/NexusTK/map/MapPane.cpp` carried UID0004DS as an Empty Emitter Marker at `86/88`. The B005 callback scoped validation refreshed generated output to `88/90` while retaining the empty marker, which is the intended state unless a later callback approves formal C++.

## Cross-References

- [UID:00037Q][0x00506970-0x00506cdd.MapPaneObjectLookupHelpers](by-memory/0x00506970-0x00506cdd.MapPaneObjectLookupHelpers.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0002CF][g_movementSubstepScale](by-global/g_movementSubstepScale.md)
- [UID:00020R][0x00469050-0x0046907a.ObjectPaneClearOutputRects](by-memory/0x00469050-0x0046907a.ObjectPaneClearOutputRects.md)
- [UID:0003HT][0x00537bf0-0x00537c65.ItemObjectPaneClearOutputRects](by-memory/0x00537bf0-0x00537c65.ItemObjectPaneClearOutputRects.md)

## Changes

- 2026-07-16 Agent-B005 UID0003HT callback:
  - Preserved `88/90`, owner/emitter UID00007Q, reconstructable true, blank position/formal, `Nested:0`, exact range, 44-call liveness, tile/interpolation/rectangle behavior, caller evidence, and MapPane source route.
  - Resolved ObjectPane slot `+0x50` as `ClearOutputRects(paneBounds, objectDataBounds)` and slot `+0x2c` as `SetBounds(paneBounds, objectDataBounds)`, and historicalized the former MapPoint/RectBounds blockers.
  - Retained blank C++ for independently unresolved ObjectPane/MapPane virtuals and movement fields; no raw offsets or invented body were added.
- 2026-07-04 B005 UID0004DS implementation callback: updated target to `88/90` with current `nexustk_supervisor_20260704` MCP evidence, exact range/size/basic-block/disassembly and boundary facts, 44-code-xref and no-data-xref evidence, helper/callee identities, representative caller behavior, support-doc proof, generated empty-marker expectation, and a target-specific no-code proof for unresolved object virtual slots, movement fields, map-point/type ordering, and missing source-quality UDTs. Owner/emitter/reconstructable state and blank formal C++ were intentionally preserved.
- 2026-07-02 B014 UID00037Q implementation callback: created exact child page with owner/source route, behavior evidence, and target-specific blank-C++ proof.
