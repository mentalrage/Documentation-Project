*** UID:0004J9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
short ChattingPane::GetOverflowCount(char axis)
{
    if (axis != 0)
        return 0;

    const int pixelOffset = m_contentRect.top - m_scrollContentRect.top;
    if (pixelOffset <= 0)
        return 0;

    return static_cast<short>((pixelOffset - 1) / 13 + 1);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChattingPane primary-vtable GetOverflowCount override at 0x0047f5c0-0x0047f5f8; converts positive vertical displacement between inherited fixed and positioned content rectangles into a ceiling-divided 13-pixel unit count and emits through UID00001X. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047f5c0-0x0047f5f8 ChattingPane GetOverflowCount

## Status And Source Route

- Disposition: reconstructable, source-authored `ChattingPane` virtual method.
- Owner/emitter: [UID:00001X][ChattingPane](by-class/ChattingPane.md), routed through [UID:0000I5][Chatting](by-file/Chatting.md) to `NexusTK/social/Chatting.cpp`.
- Split parent: [UID:0002ES][0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals](by-memory/0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals.md).
- ABI: `short ChattingPane::GetOverflowCount(char axis)`; primary vtable slot `+0x4c` at `0x00614d2c`.

## Behavior

- Axis `0` is vertical. Nonvertical axes return `0`.
- For the vertical axis, the method subtracts `m_scrollContentRect.top` from `m_contentRect.top`.
- Nonpositive displacement returns `0`; positive displacement is converted to chat-line units with ceiling division `(pixelOffset - 1) / 13 + 1`.
- The result is a signed 16-bit count. ScrollablePane synchronization consumes the slot result from `AX`, clamps it, and updates scrollbar state.

## Direct Evidence

- Evidence-time IDA MCP lookup identified the exact modeled function `0x0047f5c0-0x0047f5f8`, size `0x38`.
- Disassembly shows one `char`-width axis stack argument, two 32-bit field loads at `this+0x114` and `this+0x124`, signed comparison/subtraction, signed division by literal `13`, and a 16-bit return value.
- The sole incoming reference to `0x0047f5c0` is the ChattingPane primary-vtable data entry at `0x00614d2c`; no direct code caller or alternate owner route exists.
- ScrollablePane core calls slot `+0x4c` during axis-specific synchronization. The separate nonvirtual helper at `0x0055e950` already owns source name/signature `GetScrollPosition(char)`, so applying that same signature to this slot would make the class declaration invalid.

## Field Lifecycle And Types

- Accepted `RectBounds` order is `left, top, right, bottom`.
- ScrollablePane fixed content bounds begin at `this+0x110`, making `this+0x114` `m_contentRect.top`.
- Positioned full-content bounds begin at `this+0x120`, making `this+0x124` `m_scrollContentRect.top`.
- ScrollablePane layout code populates and repositions these rectangles before synchronization; this method observes their vertical displacement and does not own separate ChattingPane viewport fields.

## Range And Padding

- Exact source body range: `0x0047f5c0-0x0047f5f8`.
- Preceding `0x0047f5b7-0x0047f5c0` is nine `0xcc` bytes and following `0x0047f5f8-0x0047f600` is eight `0xcc` bytes. Both are parent-documented alignment, not source children.
- No string, table, secondary entry point, or unrelated bytes occur inside this child.

## Naming And Rejected Alternatives

- `GetOverflowCount` is the canonical inherited slot name supported by the surviving ListPane method inventory and by this slot's state-synchronization role.
- Historical descriptive `GetVerticalScrollPageStepFromRects` wording conflated this metric with the distinct page-size slot `+0x50`; it remains historical role evidence only.
- Historical B002 `GetScrollPosition(char)` for slot `+0x4c` is superseded because `0x0055e950` is the distinct selected-scrollbar current-position helper with exactly that API.
- Treating the pair of offsets as independent `ChattingPane` integer fields or using generic raw `this+0x114/+0x124` names is rejected; their inherited rectangle lifecycle is resolved.

## Score Rationale

- Completion `92`: exact bounds, ABI, slot, field lifecycle, formula, axis, consumer, naming conflict, padding, source route, and first-draft C++ are complete.
- Confidence `93`: bytes and layout behavior are direct; only unavailable original header spelling prevents a final-audit score.
