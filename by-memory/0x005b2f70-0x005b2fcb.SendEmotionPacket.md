*** UID:0001M4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ID | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000ID | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void SendEmotionPacket(int selector)
{
    unsigned char packet[128];

    packet[0] = 0x1d;
    PacketBufferWriteUInt8(static_cast<unsigned char>(selector - 0x0b), packet + 1);
    packet[2] = 0;
    QueueAndSendPacket(g_packetSender, packet, 2);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Position-40 file-static EmotionInputPane packet helper, forward-declared by the class source skeleton, with exact OnAccept-only caller set, packet[128] scratch, opcode 0x1d, selector - 0x0b byte, local zero, two-byte send, sender global, boundaries, and rejected instance/neighbor ownership. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b2f70-0x005b2fcb SendEmotionPacket

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: small packet-send helper
- Current source module: `NexusTK/ui/dialogs/CommandInputPanes.cpp` under [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)
- Related class: [UID:00004B][EmotionInputPane](by-class/EmotionInputPane.md)
- Rebuild handling: source-authored packet helper code.
- Confidence: very strong for boundary, caller ownership, packet layout, and stack-buffer source shape; medium for exact original helper declaration spelling and final source-file split.
- Current score: `89/92`; exact helper C++ is present because this child owns only the small file-local/static packet helper body. The position-0 class source skeleton forward-declares it before the position-10 `OnAccept` body, and this page defines it at position `40` without class-page method-body leakage.

## Current B007 Source Ordering And Preservation

- [UID:00004B][EmotionInputPane](by-class/EmotionInputPane.md) emits `static void SendEmotionPacket(int selector);` before `[[CHILDREN]]`. [UID:0001M3][0x005b29c0-0x005b2f68.EmotionInputPane](by-memory/0x005b29c0-0x005b2f68.EmotionInputPane.md) emits the only three calls from `EmotionInputPane::OnAccept` at position `10`; this page emits the definition at position `40`.
- The formal helper body is unchanged: it keeps `unsigned char packet[128]`, writes opcode `0x1d`, writes `static_cast<unsigned char>(selector - 0x0b)` through `PacketBufferWriteUInt8`, sets local `packet[2] = 0`, and calls `QueueAndSendPacket(g_packetSender, packet, 2)`.
- `packet[2]` remains local scratch/terminator state outside the two transmitted bytes. The caller's unusual `a-l`/`m-n`/`o-p` selector split remains owned by UID0001M3 and is not simplified here.
- The helper remains file-local/static because the function never reads `ecx`, has no pointer-table route, and is called only from the Emotion accept body. It is not an instance method, not part of the following `ChangeItemSlotInputPane`, and not owned by packet/socket infrastructure.
- Historical Wave2/Wave3 ownership or generated-output labels are retained only as dated provenance and do not control the current source route.

## Covered Range

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x005b2f70-0x005b2fcb` | `SendEmotionPacket` / private emotion helper | Writes opcode `0x1d` / decimal `29` (Verified with int_convert.py), writes one transformed emotion selector byte, and queues a two-byte packet through `g_packetSender`. |

## Historical IDA MCP Evidence (Names As Recorded At The Time)

The dated evidence below uses the earlier descriptive caller name `OnCharInput`. Its three call addresses and behavior remain exact; the current canonical caller is `EmotionInputPane::OnAccept`.

Checked on 2026-05-26, reconfirmed on 2026-06-14:

- Live IDA MCP `lookup_funcs 0x005b2f70` reports `sub_5B2F70`, size `0x5b` / decimal `91` (Verified with int_convert.py), end-exclusive `0x005b2fcb`.
- Direct callers are only inside `EmotionInputPane::OnCharInput` at `0x005b2aef`, `0x005b2b15`, and `0x005b2f52`.
- The helper calls the packet byte writer at `0x00575380` and the shared queue/send routine at `0x00574bb0`.
- The assembly returns with `retn 4` and does not read `ecx`; although callers set `ecx` to the pane instance, current evidence is best modeled as a private file-scope helper or static member rather than a normal instance method.
- 2026-06-14 live IDA MCP `lookup_funcs 0x005b2fd0` reports `Not a function`, and raw-byte checks show the following `0x005b2fcb-0x005b2fd0` span is five `0xcc` bytes before the next raw constructor neighborhood.
- IDA decompilation shows `Src = 29` (`0x1d` / decimal `29`, Verified with int_convert.py), writes the helper argument transformed by `a1 - 0x0b` / decimal `11` (Verified with int_convert.py) through packet byte writer `0x00575380`, null-terminates the tiny buffer, and sends length `2` through `0x00574bb0((_BYTE *)dword_67A7EC, &Src, 2)`.

Checked again on 2026-06-21 from B009 PE/Capstone source-quality incorporation:

- Parsed local PE mapping places the helper at raw offset `0x001b2370` and exact half-open VA range `0x005b2f70-0x005b2fcb`, size `0x5b` / `91` bytes.
- Direct rel32 callers are exactly `0x005b2aef`, `0x005b2b15`, and `0x005b2f52`, all inside [UID:0001M3][0x005b29c0-0x005b2f68.EmotionInputPane](by-memory/0x005b29c0-0x005b2f68.EmotionInputPane.md). Whole-image absolute dword scan found no pointer-table hits for `0x005b2f70`.
- The helper returns with `ret 4` and never reads the incoming `ecx` receiver, even though callers set `ecx = this` before calling. Current best source shape is therefore a feature-private file-local/static helper in [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md), not a normal `EmotionInputPane` instance method.
- The earlier behavior-level helper model used a three-byte local scratch packet because only three bytes are directly touched, but that shape is now superseded by the 2026-06-26 target-specific stack-frame audit. The transmitted bytes remain opcode `0x1d` and `selectorArgument - 0x0b`, and local byte `2` remains a zero scratch/terminator outside the two-byte send length.
- Support names are now normalized to [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md) for the value-first byte writer, [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), and [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md).
- Opcode `0x0e` is rejected for this helper; that is the adjacent ChatInputPane route. This helper sends opcode `0x1d`.
- The following `0x005b2fcb-0x005b2fd0` span remains five bytes of `0xcc` alignment before the next raw constructor neighborhood, not part of this helper.

Checked again on 2026-06-26 from B009 accepted implementation callback with live IDA MCP session `80de0a67`:

- `server_health` reported `ok` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, with auto-analysis and Hex-Rays ready.
- `lookup_funcs` reports `0x005b2f70` as `sub_5B2F70`, size `0x5b` / decimal `91` (Verified with int_convert.py), exact half-open range `0x005b2f70-0x005b2fcb`; `0x005b2f68`, `0x005b2fcb`, and `0x005b2fd0` are not function starts.
- `lookup_funcs` also reconfirmed the owning caller functions `0x005b29c0` size `0xb0` and `0x005b2a70` size `0x4f8`, plus packet support `0x00575380` size `0x11` and `0x00574bb0` size `0x63`.
- `xrefs_to 0x005b2f70` reports exactly three code callers, all in `sub_5B2A70`: `0x005b2aef`, `0x005b2b15`, and `0x005b2f52`. Caller decompile/disassembly confirms the selector split remains caller-owned: `0x005b2aef` passes `ch - 0x56`, `0x005b2b15` passes `ch - 0x64`, and `0x005b2f52` passes `ch - 0x58`.
- `callees 0x005b2f70` reports only packet byte writer `0x00575380` / `sub_575380`, queue/send helper `0x00574bb0` / `sub_574BB0`, and `@__security_check_cookie@4`; the body reads `dword_67A7EC` as the sender global now documented as [UID:0000Q5][g_packetSender](by-global/g_packetSender.md).
- `decompile 0x005b2f70` shows `Src = 29`, `sub_575380(a1 - 11, v3)`, `v3[1] = 0`, and `sub_574BB0((_BYTE *)dword_67A7EC, &Src, 2)`, confirming opcode `0x1d`, selector adjustment `0x0b`, a local-only zero byte, and two transmitted bytes.
- `disasm 0x005b2f70` shows `sub esp, 84h`, opcode byte at `[ebp-0x84]`, packet byte 1 at `[ebp-0x83]`, local zero byte at `[ebp-0x82]`, the security cookie at `[ebp-0x4]`, calls to `sub_575380` and `sub_574BB0`, send length `2`, stack-cookie check, and `retn 4`. The 0x84-byte frame gives a contiguous 128-byte packet scratch area before the cookie, so the formal C++ now uses `unsigned char packet[128]`.
- `get_bytes` reports `0x005b2f68-0x005b2f70` as eight `0xcc` bytes and `0x005b2fcb-0x005b2fd0` as five `0xcc` bytes; both spans remain alignment/padding, not source-authored code.
- `find_bytes` found zero matches for little-endian absolute VA `70 2f 5b 00`, RVA-style `70 2f 1b 00`, and PE raw-offset `70 23 1b 00`, supplementing the IDA xref result with no simple pointer-table route to this helper.

## Behavior Notes

The caller accepts emotion letters in the `a` through `p` range and normalizes them before calling this helper. This helper allocates a source-shaped 128-byte local scratch packet buffer, subtracts `0x0b` / decimal `11` (Verified with int_convert.py) from the passed selector, stores opcode `0x1d` / decimal `29` (Verified with int_convert.py), writes local-only zero byte `packet[2]`, and sends length `2`. The prior `packet[3]` formal C++ was behavior-equivalent for the transmitted bytes, but it is superseded by the `packet[128]` local because session `80de0a67` proves the 128-byte stack scratch area before the security cookie.

The three call-site transformations split the accepted range into `a-l`, `m-n`, and `o-p` cases before this helper receives its selector argument. Preserve that caller-side mapping in [UID:0001M3][0x005b29c0-0x005b2f68.EmotionInputPane](by-memory/0x005b29c0-0x005b2f68.EmotionInputPane.md): `a-l` passes `ch - 0x56`, `m-n` passes `ch - 0x64`, and `o-p` passes `ch - 0x58`; this page only owns the final `selector - 0x0b` packet write/send helper.

## Ownership Decision

This function is not part of [UID:00001J][ChangeItemSlotInputPane](by-class/ChangeItemSlotInputPane.md), despite sitting immediately before that class neighborhood. It belongs with [UID:00004B][EmotionInputPane](by-class/EmotionInputPane.md) and the [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md) source candidate.

Attach to [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md). The first-draft helper body uses the accepted source-facing names `SendEmotionPacket`, `PacketBufferWriteUInt8`, `g_packetSender`, and `QueueAndSendPacket`. It is intentionally file-local/static rather than an instance method; a future recovered header could still express the same helper as a private static member without changing the packet behavior or source file route.

Rejected alternatives remain unchanged after the 2026-06-26 source-shape audit: ordinary `EmotionInputPane` instance-method ownership is rejected because the callee ignores `ecx` and object fields; `PacketBuffer.cpp`, `Socket.cpp`, `Chatting.cpp`, and `SystemMessagePanes.cpp` are dependencies or neighboring systems, not feature opcode owners; ChatInputPane opcode `0x0e` is not this route; and retaining `packet[3]` as final C++ is rejected because it hides the source-shaped 128-byte local scratch buffer even though it sends the same two payload bytes.

## Score Rationale

Completion is now `89` because the page records the exact function boundary, caller/callee set, decompile/disassembly behavior, calling-convention clue, opcode, selector transform, two-byte send length, 128-byte local scratch buffer, packet sender global, padding boundaries, negative pointer-route checks, rejected ownership alternatives, superseded `packet[3]` assumption, exact source position, and forward-declaration relationship. Confidence is now `92` because live MCP evidence, the accepted caller page, and current source skeleton agree on the function size, calls, stack frame, no-pointer-table route, no-`ecx` use, formal body, and file-local placement. It remains below final-source levels only because the original helper spelling and exact historical file split remain inferred.

## Cross-References

- [UID:0001M3][0x005b29c0-0x005b2f68.EmotionInputPane](by-memory/0x005b29c0-0x005b2f68.EmotionInputPane.md)
- [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md)
- [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md)

## Changes

- 2026-06-26 B009 accepted implementation callback:
  - Before: `87/90` with behavior-equivalent `packet[3]` formal C++.
  - After: `88/91` with source-shaped `packet[128]` formal C++.
  - Summary/evidence: incorporated live MCP session `80de0a67` boundary, caller/callee, decompile, disassembly, stack-frame, padding, and negative pointer-pattern evidence; documented that `packet[3]` was superseded by the 128-byte local scratch buffer while the transmitted payload remains two bytes; owner/emitter route remains [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md) with no split or merge.
- 2026-06-14 A001 Goal 2 low-completion refresh:
  - Raised `COMPLETION` from `76` to `86` and `CONFIDENCE` from `86` to `88`.
  - Added live IDA reconfirmation, int_convert-verified opcode/selector/size values, score rationale, and the current `90/90+` C++ omission gate.
  - Owner/emitter route remains [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md).
- 2026-06-21 Rule 26 B009 incorporation:
  - Before: `86/88` with blank C++.
  - After: `87/90` with first-draft file-local/static helper C++.
  - Summary/evidence: incorporated PE/Capstone raw offset and route scans, no pointer-table route, three direct callers, no-`ecx` static-helper evidence, normalized packet support names, opcode `0x1d` versus rejected `0x0e`, exact transmitted two-byte packet shape, unsent scratch zero, and following padding boundary.
- 2026-06-02 IDA MCP helper pass:
  - Before: `COMPLETION:50`, `CONFIDENCE:65`, and no autogen parent.
  - Changed to: `COMPLETION:76`, `CONFIDENCE:86`, `RECONSTRUCTABLE:TRUE`, parent [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md).
  - Why: exact boundary, sole caller set, opcode `0x1d`, selector transform, packet length, packet sender global, calling convention clue, and following padding are documented.
- 2026-08-02 B007 UID0002N4 implementation callback:
  - Before: `88/91`, blank emitter position, no current forward-declaration/source-order closure, and a stale Wave3-generated-owner line.
  - After: `89/92`, position `40`, exact formal body preserved, position-0 static forward declaration documented, canonical `OnAccept` caller identity, and current `CommandInputPanes.cpp` route with historical Wave references explicitly non-authoritative.
