*** UID:0003VD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool UserPane::OnTimer(int timerId, int arg0, int arg1)
{
    switch (timerId)
    {
    case kUserPaneMovementStep:
        {
            int direction = arg0;
            const int movementFrameCount = 4 * g_movementSubstepScale;

            m_movementTimerHandled = true;

            if (IsDirectionDelayActive())
            {
                m_moveFrame = static_cast<signed char>(
                    (movementFrameCount + m_moveFrame - 1) %
                    movementFrameCount);
                direction = RemapMovementDirectionForMapScroll(
                    static_cast<unsigned char>(direction));
            }
            else
            {
                m_moveFrame = static_cast<signed char>(
                    (m_moveFrame + 1) % movementFrameCount);
            }

            if (m_moveFrame == 0)
            {
                m_isMoving = false;
                ScheduleBoxRotationStep();
            }
            else if (!IsDirectionDelayActive() &&
                     m_moveFrame >= 2 &&
                     !m_movementReplyReceived)
            {
                if (m_moveFrame == 2)
                {
                    m_movementReplyPending = true;
                    m_movementReplyTimerFamily = 0;
                }
            }
            else
            {
                g_pTimerMgr->ScheduleTimer(
                    static_cast<TimerHandler *>(this),
                    kUserPaneMovementStep,
                    m_objectStatus.m_displayStatusByte /
                        g_movementSubstepScale,
                    direction,
                    0);
            }

            if (m_isMoving)
            {
                PropagateLinkedObjectMode(direction);
            }
            else
            {
                if (!IsDirectionDelayActive())
                {
                    MapPoint offset;
                    DirectionToTileOffset(&offset, m_facing);
                    WarpToPosition(
                        m_mapX + offset.column,
                        m_mapY + offset.row,
                        m_viewportColumn + offset.column,
                        m_viewportRow + offset.row);
                }

                PropagateLinkedObjectMode(direction);
                SyncPositionToLinkedObjects();
            }

            if (!IsDirectionDelayActive() &&
                !m_isMoving &&
                --m_movementReplyCount > 0)
            {
                StartDirectionalAnimation(
                    static_cast<char>(direction));
                m_movementReplyReceived = true;
            }

            return true;
        }

    case kLivingTimerForwardStep:
        {
            int direction = arg0;
            const unsigned char originalDirection =
                static_cast<unsigned char>(direction);
            const int movementFrameCount = 4 * g_movementSubstepScale;

            m_movementTimerHandled = true;

            if (IsDirectionDelayActive())
            {
                m_moveFrame = static_cast<signed char>(
                    (movementFrameCount + m_moveFrame - 1) %
                    movementFrameCount);
                direction = RemapMovementDirectionForMapScroll(
                    static_cast<unsigned char>(direction));
            }
            else
            {
                m_moveFrame = static_cast<signed char>(
                    (m_moveFrame + 1) % movementFrameCount);
            }

            if (m_moveFrame == 0)
            {
                m_isMoving = false;
                ScheduleBoxRotationStep();
            }
            else if (!IsDirectionDelayActive() &&
                     m_moveFrame >= 2 &&
                     !m_movementReplyReceived)
            {
                if (m_moveFrame == 2)
                {
                    m_movementReplyPending = true;
                    m_movementReplyTimerFamily = 1;
                }
            }
            else
            {
                g_pTimerMgr->ScheduleTimer(
                    static_cast<TimerHandler *>(this),
                    kLivingTimerForwardStep,
                    m_objectStatus.m_displayStatusByte /
                        g_movementSubstepScale,
                    direction,
                    0);
            }

            if (m_isMoving)
            {
                PropagateLinkedObjectMode(direction);
                m_mapPane->ApplyMovementSubstepScroll(
                    RemapMovementDirectionForMapScroll(
                        static_cast<unsigned char>(direction)));
            }
            else
            {
                if (!IsDirectionDelayActive())
                {
                    PropagateLinkedObjectMode(direction);

                    MapPoint offset;
                    DirectionToTileOffset(&offset, m_facing);
                    WarpToPosition(
                        m_mapX + offset.column,
                        m_mapY + offset.row,
                        m_viewportColumn,
                        m_viewportRow);

                    m_mapPane->ApplyMovementSubstepScroll(
                        RemapMovementDirectionForMapScroll(
                            static_cast<unsigned char>(direction)));
                    m_mapPane->ScrollViewportByDirection(
                        originalDirection);
                }

                SyncPositionToLinkedObjects();
            }

            if (!IsDirectionDelayActive() &&
                !m_isMoving &&
                --m_movementReplyCount > 0)
            {
                InitializeWalkAnimation(
                    static_cast<unsigned char>(direction));
                m_movementReplyReceived = true;
            }

            return true;
        }

    case kLivingTimerFrameAnimation:
        return LivingObjectPane::OnTimer(
            kLivingTimerFrameAnimation, arg0, arg1);

    default:
        return LivingObjectPane::OnTimer(timerId, arg0, arg1);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-ready UserPane TimerHandler override; handles local event 20 and FWdA four-substep movement, adjusted UserPane+0xa4 TimerHandler ABI, movement-reply latches, coordinate warp/scroll, linked-object propagation/synchronization, FAnD/default base delegation, exact bytes/boundaries, and complete C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005a83b0-0x005a87ad UserPane OnTimer

## Current Source Disposition - 2026-07-30

- Exact source member: `bool UserPane::OnTimer(int timerId, int arg0, int arg1)`.
- Exact range: `[0x005a83b0,0x005a87ad)`, 1,021 bytes, one method, SHA256 `0260F191AA50A541D89BE148F3414DF840E88BEFBA8DABE62AF6E87C85BE8263`.
- Direct owner/emitter: [UID:0000FQ][UserPane](by-class/UserPane.md); source root: [UID:0000P1][UserPane](by-file/UserPane.md), `NexusTK/ui/panels/UserPane.cpp`.
- The method is entered through UserPane's TimerHandler secondary facet at complete-object offset `+0xa4`. Machine `this-0xa4` recovery is compiler lowering; source uses ordinary `UserPane` member access.
- The target H channel is intentionally blank because the single complete declaration belongs to UID0000FQ's formal H channel. This child owns the one out-of-class definition above.
- No split is required. The switch and all compiler lowering remain part of this exact source method.

## Current Behavior And Data Flow

`OnTimer` dispatches three accepted timer families and a default path:

1. `kUserPaneMovementStep` (`20` / `0x14`) advances the local directional-animation substep.
2. `kLivingTimerForwardStep` (`0x41645746`, `FWdA`) advances walk movement and MapPane scrolling.
3. `kLivingTimerFrameAnimation` (`0x446e4146`, `FAnD`) delegates to `LivingObjectPane::OnTimer`.
4. Every unrecognized timer delegates unchanged to `LivingObjectPane::OnTimer(timerId, arg0, arg1)`.

Both movement cases set `m_movementTimerHandled`, update inherited signed `m_moveFrame` modulo `4 * g_movementSubstepScale`, reverse the phase while direction delay is active, and remap the direction through `RemapMovementDirectionForMapScroll`. Frame zero clears movement and calls `ScheduleBoxRotationStep`. Before the reply arrives, frame two sets `m_movementReplyPending` and records the timer family in `m_movementReplyTimerFamily`; other active phases reschedule the same timer using `m_objectStatus.m_displayStatusByte / g_movementSubstepScale` with no invented clamp, zero guard, or null guard.

The local event propagates mode to linked objects while moving. At completion it optionally converts `m_facing` with `DirectionToTileOffset`, warps both map and viewport coordinates by that offset, propagates mode, and synchronizes linked-object positions. The FWdA path additionally applies progressive MapPane substep scrolling while moving. At completion it preserves the original direction for `ScrollViewportByDirection`, warps map coordinates while retaining viewport coordinates, applies the final remapped substep, scrolls the viewport by the original direction, and synchronizes linked objects. If movement is complete and the signed reply countdown remains positive, the local event restarts `StartDirectionalAnimation`, while FWdA restarts `InitializeWalkAnimation`; both then set `m_movementReplyReceived`.

## Exact Field Translation

| Complete-object offset | Source-facing field | Target role |
| --- | --- | --- |
| `+0x1c6` | inherited `signed char m_moveFrame` | four-substep phase, including reverse-delay arithmetic |
| `+0x20c` | `bool m_movementTimerHandled` | set on both movement timer branches |
| `+0x13eaf0` | `unsigned char m_movementMode` | adjacent named state, not read by this method |
| `+0x13eaf1` | `signed char m_movementReplyCount` | decremented after completed movement |
| `+0x13eaf2` | `bool m_movementReplyReceived` | gates frame-two pending state and records continuation |
| `+0x13eaf3` | `bool m_movementReplyPending` | set when frame two waits for a reply |
| `+0x13eaf4` | `bool m_directionDelayActive` | selects reverse-frame and direction-remap behavior |
| `+0x13eb1e` | `unsigned char m_movementReplyTimerFamily` | `0` for local event 20, `1` for FWdA |
| `+0x13eb1f` | `bool m_movementHistoryEnabled` | adjacent named state, not read by this method |

The independent complete-object `+0x210` slot is `LineInputPane *m_activeLineInputPane`, not an ObjectPane target. UID0003VD does not read or write it; preserving that exact source contract is required because the class layout is shared.

## Exact Binary And Boundary Evidence

- Pre-target `[0x005a83a4,0x005a83b0)` is twelve `0xcc` bytes. The 16-byte read beginning at `0x005a83a0` contains `18 18 16 17` followed by the twelve-byte fence and hashes to `F5594071D7BED766FB3C797DAAA31ABE45BF17337F65D0936E0355FD5B9F4FB9`.
- Target bytes hash to `0260F191AA50A541D89BE148F3414DF840E88BEFBA8DABE62AF6E87C85BE8263`.
- Post-target `[0x005a87ad,0x005a87b0)` is exactly `CC CC CC` before separate successor [UID:0003VE][0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex](by-memory/0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex.md). The 19-byte boundary read hashes to `1956EFAF4AAACA4B2E1ACE06C7EFC46FD4A1F06A5339873137747C46667320B3`.
- No jump-table/data island, nested child, exception fragment, or independently owned range occurs inside the target.
- The sole inbound route is the data/vtable reference at `0x0062efc4`; zero direct code callers are expected for this virtual callback and do not imply dead code.
- The current frame's decompiler-local byte is actually an eight-byte `MapPoint`; source arguments are `timerId`, `arg0`, and `arg1`.

## Caller And Callee Evidence

- `ScheduleBoxRotationStep`: target call sites `0x005a845a`, `0x005a849b`, `0x005a864a`, and `0x005a868b`.
- `SyncPositionToLinkedObjects`: target-only call sites `0x005a859e` and `0x005a876a`.
- `WarpToPosition`: target call sites `0x005a8572` and `0x005a874e` among twelve callers.
- `InitializeWalkAnimation`: target call `0x005a85d6`; other callers are `0x005a3424`, `0x005ab143`, and `0x005ab4c1`.
- `StartDirectionalAnimation`: target call `0x005a8796`; its other caller is `0x005a3607`.
- `ApplyMovementSubstepScroll`: target-only calls at `0x005a8524` and `0x005a8589`.
- `ScrollViewportByDirection`: sole call at `0x005a8597`.
- `RemapMovementDirectionForMapScroll`: target calls at `0x005a843a`, `0x005a8515`, `0x005a857a`, and `0x005a862a`, plus MapPane calls at `0x005058c6` and `0x0050be3b`.
- Base `LivingObjectPane::OnTimer`: target delegations at `0x005a83dc` and `0x005a83ef`, plus the LivingObjectPane vtable cell at `0x00620a34`.
- `UserPane::PropagateLinkedObjectMode` is a vtable thunk at `0x005a9240` / cell `0x0062ef88` that tail-forwards to the LivingObjectPane implementation at `0x0053c290`.

## Ownership, Source Placement, And Negative Evidence

UserPane ownership is direct: the TimerHandler vtable cell is UserPane-owned, the body touches UserPane-only movement/reply/MapPane state, and both the class and file pages clear the assignment gate. LivingObjectPane supplies inherited state and base delegation but does not own the derived callback. MapPane, TimerMgr, and the linked-object helpers are dependencies, not owners. The exact child already isolates the method, so the broad UserPane aggregate and a new movement file would duplicate emission.

The adjusted receiver is normal secondary-base multiple-inheritance lowering, not evidence for an artificial wrapper. `OnAnimationStep` is not supported by the TimerHandler contract. Event 20 is not a protocol opcode or Windows message. `FWdA` and `FAnD` are established LivingObjectPane timer constants. No evidence supports null guards, direction/coordinate/scale clamping, a separate helper for the duplicated switch branches, or a code-emitting vtable page.

## Score Rationale

Completion and confidence are `94/94`. Exact behavior, bytes, boundaries, owner/emitter, vtable liveness, source placement, helper contracts, field translations, and complete formal C++ are resolved. The target remains below 95 because stripped private spellings for the one-shot handled flag and two adjacent otherwise-unused state bytes are high-probability source reconstructions rather than original-symbol proof. That lexical uncertainty does not block source-ready C++ and is not a reason to retain decompiler labels.

## C++ Readiness

The formal CPP block is the complete C++03-compatible source definition. It preserves switch order, timer constants, modulo arithmetic, signed countdown, exact rescheduling payloads, no-null-guard behavior, map/linked-object order, and base delegation. Formal H is blank because UID0000FQ owns the one complete class declaration in its formal H channel.

## Historical / Superseded Assumptions - Pre-2026-07-30

The previous `UserPane::OnAnimationStep` label, unresolved local event `0x14`, offset-array fields, provisional helper signatures, adjusted-base blocker, and no-current-improvement formal comment were evidence-time reconstruction stages. Vtable contract, sibling timer schedulers, current support pages, and bounded live IDA evidence supersede them. Their evidence remains below for provenance; none is a current source recommendation.

### Historical Status

- Address range: `0x005a83b0-0x005a87ad`
- Entity kind: method
- Direct owner: [UID:0000FQ][UserPane](by-class/UserPane.md)
- Source file: [UID:0000P1][UserPane](by-file/UserPane.md)
- Rebuild handling: source-authored UserPane animation/update virtual method. B010's accepted 2026-07-01 empty-emitter audit replaces generic blank-C++ wording with the formal no-current-improvement comment above. The `FWdA` and `FAnD` values seen in this method are resolved as LivingObjectPane timer constants, not UserPane-owned unresolved tags; the remaining blocker is the adjusted facet/local movement field model.
- Source-facing signature direction: `bool UserPane::OnAnimationStep(int message, int frameOrDirection, int aux)`; the vtable slot and role are clear. Final enum/tag names are still unsafe for the UserPane-local `0x14` path, while `0x41645746` is `FWdA` and `0x446e4146` is `FAnD` from [UID:0002R1][0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer](by-memory/0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer.md).

### Historical Behavior

This virtual method handles local-user animation/update events. The decompiler shows dispatch on event ids including UserPane-local `0x14`, LivingObjectPane forward-step timer `FWdA` (`0x41645746`), and LivingObjectPane frame-animation timer `FAnD` (`0x446e4146`). It performs per-frame movement phase updates, movement timer scheduling/refresh behavior, map-position helper calls, and calls into LivingObjectPane movement helpers for walk animation and directional animation.

Important callees include `0x0053bf40`, `0x0053c070`, `0x00597910`, `0x005a5a90`, `0x005a36f0`, `0x005a3770`, `0x005058b0`, and `0x005055e0`.

### Historical Evidence

- IDA decompile cache `0x005a83b0.json` reports `sub_5A83B0`, size `0x3fd`, ending at `0x005a87ad`.
- Cache metadata classifies it as a `NexusTK:UserPane` virtual and lists 11 callees.
- The decompiler shows a switch at `0x005a83c1` with cases including UserPane-local event id `20` / `0x14`, LivingObjectPane timer id `1097094982` / `0x41645746` / `FWdA`, and LivingObjectPane timer id `1148076358` / `0x446e4146` / `FAnD`.
- B010's 2026-07-01 fresh MCP `decompile 0x005a83b0` shows the complete top-level dispatch shape: case `20` handles the UserPane-local update path, case `1097094982` handles `FWdA`, case `1148076358` delegates to `sub_53B830(this, 1148076358, a3, a4)`, and the default delegates to `sub_53B830(this, a2, a3, a4)`. The same decompile proves this method is entered through an adjusted facet: local calls recover the `LivingObjectPane` base as `this - 164` (hex `0xa4`), matching the accepted adjusted-receiver evidence.
- B007's 2026-06-26 [UID:0002R1][0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer](by-memory/0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer.md) pass verified `FWdA` and `FAnD` with `tools/int_convert.py` and assigns them to the LivingObjectPane timer callback. They should no longer be listed as unresolved UserPane event tags.
- Raw pointer scan finds exactly one absolute pointer to this method at `0x0062efc4`, matching the UserPane vtable data page.
- The following `0x005a87ad-0x005a87b0` bytes are three `0xcc` padding bytes before the raw LivingObjectPane helper [UID:0003VE][0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex](by-memory/0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex.md).
- The later `UserPane::OnClipBounds` body at `0x005a8840-0x005a88cf` is also UserPane-owned, but it is padding-separated from [UID:0003UH][0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition](by-memory/0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition.md). `0x005a88cf` is one `0xcc` byte, and [UID:0003UH][0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition](by-memory/0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition.md) starts immediately after it as a separate `LivingObjectPane` local-player screen-position/cache method.

### Historical Ownership

Assign to [UID:0000FQ][UserPane](by-class/UserPane.md). LivingObjectPane helpers are callees for local-player movement state, while the virtual dispatch slot and method role are UserPane-owned. [UID:0000FQ][UserPane](by-class/UserPane.md) and [UID:0000P1][UserPane](by-file/UserPane.md) both clear the strict gate.

### Historical Score Rationale

Completion `87` is justified by exact boundary, decompiler event/timer behavior, vtable slot, callee inventory, owner/source placement, resolved `FWdA`/`FAnD` LivingObjectPane timer constants, successor padding/raw-helper separation, and B010's fresh MCP branch-shape/adjusted-facet proof. Confidence `89` is strong because vtable and support docs agree and the no-current-improvement condition is now target-specific; it remains below final-source confidence because the UserPane-local `0x14` path, adjusted local movement field names, and helper signatures are still provisional.

### Historical C++ Readiness

The source owner and role are solved, but formal method-body C++ is not entered because the UserPane-local `0x14` event path, adjusted local-movement fields, and helper signatures are still provisional. The formal block contains a no-current-improvement proof comment instead of a generic blank. `FWdA` and `FAnD` are no longer blockers for this child because they are resolved LivingObjectPane timer constants. Future C++ should be authored on this exact child, not on the broad [UID:0001KL][0x005a2530-0x005b8395.UserPane](by-memory/0x005a2530-0x005b8395.UserPane.md) aggregate.

### Historical Cross-References

- [UID:0000FQ][UserPane](by-class/UserPane.md)
- [UID:0000P1][UserPane](by-file/UserPane.md)
- [UID:0001KL][0x005a2530-0x005b8395.UserPane](by-memory/0x005a2530-0x005b8395.UserPane.md)
- [UID:0002R1][0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer](by-memory/0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer.md)
- [UID:0002YU][0x0062ef0c-0x0062efcc.UserPaneVtableData](by-memory/0x0062ef0c-0x0062efcc.UserPaneVtableData.md)
- [UID:0003VE][0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex](by-memory/0x005a87b0-0x005a87d5.UserPaneGetActiveMovementHistoryRecordByIndex.md)
- [UID:0003UH][0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition](by-memory/0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition.md)

### Historical Changes

- 2026-06-26 B007 accepted [UID:0002R1][0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer](by-memory/0x0053b700-0x0053baa8.LivingObjectPaneAnimationTimer.md) support sync:
  - Resolved `0x41645746` as `FWdA` and `0x446e4146` as `FAnD`, both LivingObjectPane timer constants verified with `tools/int_convert.py`.
  - Preserved blank formal C++ because UserPane-local `0x14`, local movement fields, and helper signatures remain unresolved; only the stale unresolved-tag wording for `FWdA`/`FAnD` changed.
- 2026-06-21 Rule 26 support sync from B008 [UID:0003UH][0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition](by-memory/0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition.md):
  - Added the neighbor/boundary note that `UserPane::OnClipBounds` ends at `0x005a88cf`, one `0xcc` byte separates it from [UID:0003UH][0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition](by-memory/0x005a88d0-0x005a89c9.UserPaneUpdateScreenPosition.md), and `0x005a88d0-0x005a89c9` remains a separate LivingObjectPane local-player screen-position/cache method.
- 2026-06-20 B004 Rule 26 support sync:
  - Scores remain `86/88`.
  - Added the source-facing `bool UserPane::OnAnimationStep(...)` signature direction and documented why exact-child C++ remains deferred. The older unresolved-tag wording for `0x41645746` and `0x446e4146` is superseded by the 2026-06-26 B007 support sync; `0x14` remains the UserPane-local unresolved event.
- 2026-06-16 B001 retry executable split:
  - Created as the exact UserPane animation/update virtual child inside the remaining [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md) largest leaf.
  - Evidence: byte audit, IDA decompile cache, raw vtable pointer scan, and current UserPane support docs.
