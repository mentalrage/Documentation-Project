*** UID:0000XA | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003P | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current MCP session `19917133` confirms exact `0x00439e00-0x00439ea3` / `0xa3` deque cleanup support body with padding boundaries, thirteen xrefs to the start, no xrefs to the exclusive end, heap-free and invalid-parameter callees, and an adjacent thunk at `0x00439f50`; reclassified as non-emitting MSVC/Dinkumware `std::deque` cleanup / `_Tidy`-style support, not custom NexusTK `Deque::Destroy` C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00439e00-0x00439ea3 DequeDestroy

## Status

- Disposition: non-emitting MSVC/Dinkumware `std::deque` four-byte-entry cleanup / `_Tidy`-style support.
- Entity kind: compiler/STL template support function over the current transitional Deque layout grouping.
- Direct owner: [UID:00003P][Deque](by-class/Deque.md) retained only as a transitional documentation grouping while the wider Deque-family audit is pending.
- Emitter route: none. Do not emit a NexusTK `Deque::Destroy` body through [UID:0000IR][Deque](by-file/Deque.md).
- Rebuild handling: `third-party/runtime` / compiler-STL support. Source-level callers should express typed `std::deque` use or the surrounding source operation and allow the toolchain/STL to regenerate equivalent support.
- Confidence: very strong for boundary, behavior, layout offsets, caller spread, callee set, thunk relationship, and no-code disposition; the coordinated UID000149 pass now resolves every sibling helper consistently as compiler/STL support.

## Covered Range

| Range | Entity | Notes |
| --- | --- | --- |
| `0x00439e00-0x00439ea3` | `std::deque` cleanup / `_Tidy`-style support | Drains count/front state, frees each allocated 16-byte block, frees the block map with the large-allocation guard, then frees and clears the proxy-like `+0x00` storage slot. |

## Evidence

- Current MCP session/database `19917133` is active for this evidence pass. `server_health` returned `status:"ok"`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- MCP `lookup_funcs` id `21` reports `0x00439e00` as `sub_439E00`, size `0xa3`; `0x00439ea3` is not a function start. The nearby `0x00439eb0` query resolves to containing/non-contiguous `sub_427310`, not this cleanup body.
- MCP `entity_query` id `27` for `0x00439df0-0x00439ec0` reports only `sub_439E00`, size `0xa3`, in the target span.
- MCP `get_bytes` id `26` confirms seven `0xcc` bytes immediately before the function at `0x00439df9-0x00439e00` and thirteen `0xcc` bytes at the exclusive end before the next code at `0x00439eb0`.
- MCP `xref_query` id `25` and `xrefs_to` id `24` report thirteen code xrefs to the start: `0x439f50`, `0x43a35c`, `0x4a824f`, `0x4a8667`, `0x4b362c`, `0x4b382f`, `0x4b3abf`, `0x4b3d28`, `0x50cff4`, `0x50d822`, `0x5c5047`, `0x60278d`, and `0x6027b3`. The same checks report zero xrefs to `0x00439ea3`, supporting the exclusive-end boundary.
- MCP `analyze_function` id `22` reports ordinary callers at `sub_4A7E70`, `sub_4A8420`, `sub_4B3400`, `sub_4B3650`, `sub_4B3850`, `sub_4B3AF0`, `sub_50C790`, `sub_50D010`, and `sub_5C4BD0`. These cover EventDispatcher, FolderTreePane, MapPane, and WorldMapPane contexts as consumer evidence, not feature ownership.
- MCP `analyze_function` id `32` confirms `0x00439f50` is a five-byte `jmp sub_439E00` thunk. Its xrefs are runtime/EH-style routes; it should not become separate hand-authored C++ and should not be merged into this target's formal source body.
- MCP `analyze_function` id `33` shows the cleanup helper at `0x0043a2b0` calls `sub_439E00((int)this)` at `0x43a35c` after local string/subobject cleanup. This is caller/context evidence only.
- MCP `callees` id `23` reports only `sub_5C7526` and `__invalid_parameter_noinfo_noreturn`. There are no feature callees, vtable calls, resource calls, or product-state dependencies.
- MCP `disasm` id `30` reports a 57-instruction body. It drains `[esi+10h]`, resets `[esi+0Ch]`, walks block pointers from `[esi+4]` using `[esi+8]`, frees each 16-byte block through `sub_5C7526`, applies the large-allocation header guard before freeing the map, clears `[esi+8]` and `[esi+4]`, frees `*(void**)this` through `sub_5C7526` with size `8`, clears `[esi]`, returns at `0x439e9d`, and calls `__invalid_parameter_noinfo_noreturn` at `0x439e9e` only on guard failure.
- MCP `decompile` id `31` and `analyze_function` id `22` produce the same behavior: count drain over `this+0x10`, front/offset reset at `this+0x0c`, block-map teardown through `this+0x04` and `this+0x08`, block-map large-allocation guard, proxy-like `+0x00` free, and final clearing of the map/proxy fields.
- MCP `analyze_component` id `41` over the sibling Deque helper family reports the target beside `EnsureCapacity`, `PushBack`, `ElementAddress`, `Clear`, and `DestroyOwnedEntries`, with only the internal edge `0x4a88e0 -> 0x43e0b0`. The target itself has no internal Deque-family callees.
- [UID:0001FC][0x0054c0c0-0x0054c110.DequeElementAddressHelper](by-memory/0x0054c0c0-0x0054c110.DequeElementAddressHelper.md) is the accepted sibling precedent: B013 reclassified the matching layout/proxy element-address helper as non-emitting MSVC/Dinkumware `std::deque` support. UID0000XA uses the same map/offset/size fields and frees the same proxy-like `+0x00` storage.

## Source-Quality Reclassification

The previous custom NexusTK `Deque::Destroy` route is historical only. Current evidence supports a standard-library generated support classification:

- The layout fields match the same MSVC/Dinkumware `std::deque` support pattern documented for UID0001FC: proxy-like first slot, block map, block count/map size, front offset, and size/count.
- The body performs generic storage teardown only. It has no payload destructor call, product-specific branch, resource/global use, protocol state, UI state, vtable dependency, or feature-specific callee.
- The caller spread crosses unrelated subsystems, so consumers show that many source sites used a similar container shape; they do not prove that a human-authored NexusTK `Deque.cpp` method existed.
- The large-allocation guard and invalid-parameter path match runtime/STL allocation cleanup style rather than game logic.

Rejected alternatives:

- Feature ownership: rejected because EventDispatcher, FolderTreePane, MapPane, and WorldMapPane are only consumers, and no feature-local state is touched.
- Custom `util/Deque.cpp` emitter body: rejected because it would hand-port standard-library lowering that should be regenerated from caller-level source declarations.
- No-owner unknown: rejected because the function is no longer unknown; its exact range, behavior, callees, xrefs, layout, and source category are documented.
- Direct formal C++: rejected because the correct source-level reconstruction is typed container use at callers, not a project method that calls or defines this helper.

## Reconstruction Notes

`RECONSTRUCTABLE:FALSE` is appropriate for this exact range. Keep [UID:00003P][Deque](by-class/Deque.md) as the transitional documentation grouping so existing cross-references remain stable, but clear `EMITTER_UIDS` and keep the formal `RECONSTRUCTION_CPP CODE` block blank.

Target-specific no-code proof: source-facing reconstruction should express the surrounding product source in terms of the original containers, likely `std::deque` instantiations or equivalent source declarations in the callers/owners. The compiler/STL should emit cleanup support. Pasting this lowered body as `Deque::Destroy()` would preserve decompiler shape but would misrepresent standard-library generated support as NexusTK-authored source.

## Cross-References

- [UID:0000IR][Deque](by-file/Deque.md)
- [UID:00003P][Deque](by-class/Deque.md)
- [UID:0001U2][DequeLayout](by-type/by-struct/DequeLayout.md)
- [UID:0001FC][0x0054c0c0-0x0054c110.DequeElementAddressHelper](by-memory/0x0054c0c0-0x0054c110.DequeElementAddressHelper.md)

## Changes

- 2026-07-12 B003 UID000149 full-family synchronization:
  - Changed `90/92` to `92/95`; preserved `RECONSTRUCTABLE:FALSE`, blank emitter, and blank managed C++.
  - Recorded the completed family audit: typed caller source owns `std::deque` declarations/operations, UID00003P is documentation-only, and no custom `Deque::Destroy` or `Deque.cpp` route remains.

- 2026-07-05 B003 UID0000XA implementation callback:
  - Before: `COMPLETION:85`, `CONFIDENCE:90`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00003P`, and the page described the target as reconstructable custom NexusTK `Deque::Destroy` utility source.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:92`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, retained `CANONICAL_OWNER:00003P` as transitional grouping, and blank formal C++.
  - Summary/evidence: current MCP session/database `19917133` confirms exact `0x00439e00-0x00439ea3` range, size `0xa3`, padding bytes before/after, no xrefs to exclusive end, thirteen xrefs to the start, thunk `0x00439f50`, callers across EventDispatcher/FolderTreePane/MapPane/WorldMapPane, callees only to `sub_5C7526` and `__invalid_parameter_noinfo_noreturn`, and the standard deque cleanup body over `+0x00/+0x04/+0x08/+0x0c/+0x10`. B013 UID0001FC provides the accepted same-layout standard-deque support precedent. The old custom `Deque::Destroy` source route is historicalized; feature ownership, custom `Deque.cpp` C++, no-owner unknown, and direct formal C++ are rejected.
- 2026-06-16 A001 Goal 2 Deque support refresh:
  - Before: `COMPLETION:82`, `CONFIDENCE:90`, with stale legacy parent/gate wording.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:90`, rerouted `CANONICAL_OWNER`/`EMITTER_UIDS` from [UID:0000IR][Deque](by-file/Deque.md) to [UID:00003P][Deque](by-class/Deque.md), and documented current live IDA confirmation.
  - Evidence: live `lookup_funcs`, `xrefs_to`, `callees`, and `analyze_component` reconfirmed the exact `0xa3` body, utility caller spread, heap-free/invalid-parameter callees, Deque family placement, and final-source blockers. Final C++ remains blank pending source-quality naming and first-field semantics.
- 2026-06-05 live IDA refresh:
  - What existed before: `COMPLETION:74`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID` blank, and evidence still leaned on non-live leads and a synthetic function label.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:90`, and `AUTOGEN_PARENT_UID:0000IR`.
  - Summary/evidence: live IDA MCP lookup, byte, disassembly, decompile, caller, xref, and callee checks confirm the exact `0xa3` body, padding boundaries on both sides, count/front drain, block-map block frees, large-allocation guard, `+0x00/+0x04/+0x08/+0x0c/+0x10` layout usage, auxiliary-slot free, and caller spread across EventDispatcher, FolderTreePane, MapPane, and WorldMapPane. Existing Deque/container docs support shared utility ownership, while final C++ remains withheld below `95/95`.
- 2026-05-27: Existing range and filename ended at `0x00439ea2`. Changed them to `0x00439ea3`, matching IDA's exclusive function end. Evidence: read-only IDA MCP reports the function at `0x00439e00` spans `0x00439e00-0x00439ea3`, size `0xa3`; the following bytes `0x00439ea3-0x00439eb0` are alignment padding before an unrelated JsonCpp cleanup tail chunk.
- 2026-05-31 layout evidence and reconstructability:
  - What existed before: the notes described reset of `blockMap`, `blockCount`, `frontIndex`, and `count`, but did not account for the separate `+0x00` auxiliary slot; `RECONSTRUCTABLE` was blank.
  - Changed to: the page records the corrected offsets and `+0x00` cleanup, with `COMPLETION:74`, `CONFIDENCE:88`, and `RECONSTRUCTABLE:TRUE`.
  - Summary/evidence: IDA MCP decompilation of this method reads count at `+0x10`, block count at `+0x08`, block map at `+0x04`, and frees `*(void**)this` after block-map cleanup.
