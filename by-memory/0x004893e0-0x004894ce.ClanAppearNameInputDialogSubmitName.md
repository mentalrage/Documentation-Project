*** UID:0002O0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000023 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000023 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ClanAppearNameInputDialog::OnSubmitText(const wchar_t *text)
{
    char convertedName[256];
    unsigned char packet[128];

    PacketBufferWriteUInt8(0x4b, packet + 0);
    PacketBufferWriteUInt8(0x09, packet + 1);
    PacketBufferWriteUInt8(0x01, packet + 2);

    unsigned int convertedLength = WideCharToMultiByte(
        CP_ACP, 0, text, wcslen(text), convertedName, sizeof(convertedName), NULL, NULL);
    convertedName[convertedLength] = 0;
    PacketBufferWriteUInt8(static_cast<unsigned char>(convertedLength), packet + 3);
    memmove(packet + 4, convertedName, convertedLength);

    unsigned int packetLength = convertedLength + 4;
    packet[packetLength] = 0;
    g_packetSender->QueueAndSendPacket(packet, static_cast<short>(packetLength));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 238-byte vtable-referenced `ClanAppearNameInputDialog::OnSubmitText` body; CP_ACP-converts the submitted name, writes `{0x4b,9,1,length,name...}`, and queues exactly `convertedLength + 4` bytes through `g_packetSender`, with compiler range instrumentation omitted from authored source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004893e0-0x004894ce ClanAppearNameInputDialog Submit Name

## 2026-08-15 Accepted Source-Quality Resolution

- Exact modeled identity: `ClanAppearNameInputDialog__OnSubmitText` at half-open range `[0x004893e0,0x004894ce)`, size `0xee` / 238 bytes, 76 instruction heads, six basic blocks, and cyclomatic complexity 3.
- Canonical body SHA256: `FA4958CB9A0E703AE70D84B1A3B2BBEBCAE0C56C54F4E227E9F4417D16C82BD8`. First 16 bytes are `55 8B EC 81 EC 84 01 00 00 A1 24 2F 67 00 33 C5`; last 16 bytes are `E8 6C E2 13 00 8B E5 5D C2 04 00 E8 87 E1 13 00`.
- Source-facing ABI is `void ClanAppearNameInputDialog::OnSubmitText(const wchar_t *text)`. The base dialog dispatches submitted wide text through primary vtable slot `+0x5c` and consumes no return value.
- Direct owner/emitter remains [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md); compilation-unit owner remains [UID:0000I8][Clan](by-file/Clan.md), producing `NexusTK/social/Clan.cpp`. [UID:0000IA][ClanDialogs](by-file/ClanDialogs.md) remains a non-emitting documentation index, not an alternate source route.
- The target formal H channel stays blank because UID000023 owns the C++98-compatible `virtual void OnSubmitText(const wchar_t *text);` declaration in `Clan.h`.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: virtual submitter/method body.
- Owner class: [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md).
- Likely source module: [UID:0000I8][Clan](by-file/Clan.md).
- Reconstruction parent: [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md).
- Rebuild handling: `source-authored`.
- Confidence: very strong for exact body and boundaries, vtable ownership, callback ABI, complete frame, packet/API order, compiler shape, queue ownership, protocol lifecycle, raw-duplicate disposition, and source placement. Original lexical names remain inferred, which caps the target at `94/94` without blocking reconstruction.

## Covered Range

| Range | Entity | Notes |
| --- | --- | --- |
| `[0x004893d5,0x004893e0)` | excluded pre-alignment | Eleven `0xcc` bytes after raw constructor UID0002NZ; not part of this method. |
| `[0x004893e0,0x004894ce)` | `ClanAppearNameInputDialog::OnSubmitText` | One modeled `0xee` / 238-byte function with body SHA256 `FA4958CB9A0E703AE70D84B1A3B2BBEBCAE0C56C54F4E227E9F4417D16C82BD8`. |
| `[0x004894ce,0x004894d0)` | excluded post-alignment | Two `0xcc` bytes before raw constructor UID0002O1; not part of this method. |

## Behavior

This method converts the submitted wide-character name to a CP_ACP multibyte string, builds a length-prefixed clan appearance-name packet, and sends it through `Socket::QueueAndSendPacket` at `0x00574bb0` using [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md). The exact source order is significant:

1. Write opcode `0x4b`, subtype `0x09`, and action `0x01` with value-first `PacketBufferWriteUInt8(value, destination)` calls.
2. Call `WideCharToMultiByte(CP_ACP, 0, text, wcslen(text), convertedName, 256, NULL, NULL)`.
3. Write `convertedName[convertedLength] = 0`; the observed unsigned `0x100` failure edge is compiler range instrumentation around this indexed write, not an authored runtime call.
4. Narrow and write the converted length at packet offset `+3`, then copy `convertedLength` payload bytes to packet offset `+4`.
5. Compute `packetLength = convertedLength + 4`, then write `packet[packetLength] = 0`; the observed post-copy unsigned `0x80` failure edge is compiler range instrumentation around this second indexed write.
6. Queue exactly `convertedLength + 4` protocol bytes. Both caller scratch terminators remain outside the counted send length.

Packet fields:

| Offset | Value | Meaning |
| --- | --- | --- |
| `+0` | `0x4b` | Clan request opcode. |
| `+1` | `0x09` | Appearance-name subtype. |
| `+2` | `0x01` | Submit/confirm action. |
| `+3` | converted byte length | Length returned by `WideCharToMultiByte`. |
| `+4..` | converted name bytes | Multibyte name bytes copied into the packet. |

The sibling money submitters use `_wtol` and `PacketBufferWriteUInt32BE`; those contracts do not apply here. The appearance submitter belongs to the subtype `7-10` name family and uses the same manual length-prefixed source shape as the expel, summon, and change-name methods.

## Physical Frame And Human Source Shape

IDA preserves ten physical frame rows. These rows must not be coalesced in IDA, but the developer source is one `convertedName[256]` array plus one contiguous `packet[128]` array:

| Frame ID | Current IDA row | Offset | Width | Type | Human-source role |
| --- | --- | ---: | ---: | --- | --- |
| F01 | `MultiByteStr` | `+0x8` | `0x100` | `CHAR[256]` | `convertedName[256]` |
| F02 | `Src` | `+0x108` | `0x1` | `_BYTE` | packet opcode byte |
| F03 | `destination` | `+0x109` | `0x1` | `unsigned __int8` | packet subtype byte |
| F04 | `var_82` | `+0x10a` | `0x1` | `unsigned __int8` | packet action byte |
| F05 | `var_81` | `+0x10b` | `0x1` | `unsigned __int8` | converted-name length byte |
| F06 | `var_80` | `+0x10c` | `0x7c` | `_BYTE[124]` | remaining `packet[128]` storage |
| F07 | `var_4` | `+0x188` | `0x4` | `_DWORD` | compiler security-cookie slot |
| F08 | `__saved_registers` | `+0x18c` | `0x4` | `_DWORD` | saved-register bookkeeping |
| F09 | `__return_address` | `+0x190` | `0x4` | `_UNKNOWN *` | return address |
| F10 | `lpWideCharStr` | `+0x194` | `0x4` | `LPCWCH` | source argument `text` |

The formal source deliberately uses descriptive human names and contiguous arrays. It does not expose compiler cookie bookkeeping, physical packet fragments, or explicit `__report_rangecheckfailure` calls.

## IDA MCP Evidence

- `lookup_funcs` and `func_query` report one function at `[0x004893e0,0x004894ce)`, type `void __thiscall(void *this, const wchar_t *text)`, 76 instruction heads, six basic blocks, and exact body hash/edge bytes recorded above.
- Exactly one inbound data xref exists: `0x00615c74 -> 0x004893e0`. The slot is four-byte unnamed/untyped data whose value is `0x004893e0`; ordinary code callers are zero, as expected for vtable dispatch.
- Every one of the 76 instruction heads was checked for external inbound references. Their union is exactly the same one-entry vtable set; there are no interior code/data routes.
- Start VA bytes `e0 93 48 00` occur once at `0x00615c74`. Start RVA `e0 93 08 00`, raw offset `e0 87 08 00`, end VA/RVA/raw-offset patterns, and start/end immediate searches return zero additional routes.
- Exact callees are `GetMemoryMan` `0x00516030`, `PacketBufferWriteUInt8` `0x00575380`, `WideCharToMultiByte` IAT `0x0060d170`, `MemoryMan::MemmoveWrapper` `0x00516220`, `Socket::QueueAndSendPacket` `0x00574bb0`, `__security_check_cookie` `0x005c772f`, and `__report_rangecheckfailure` `0x005c7655`. No string literals occur in the method.
- The vtable window `[0x00615c6c,0x00615c80)` is `E0 F2 49 00 00 FC 49 00 E0 93 48 00 84 4F 64 00 7B C2 48 00`.
- Byte audit confirms eleven-byte pre-alignment and two-byte post-alignment are outside this function and preserves the neighboring constructor/function heads.

## Dependency Notes

| Address / UID | Role | Evidence |
| --- | --- | --- |
| [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) | Memory-manager accessor/reset-side setup before local packet writes. | Live `callees` includes `0x00516030`; sibling name-submitters share the same prologue call. |
| [UID:0001HZ][0x00575380-0x00575abb.PacketBufferHelpers](by-memory/0x00575380-0x00575abb.PacketBufferHelpers.md) | Value-first byte writer for opcode, subtype, action, and converted-name length. | Disassembly proves calls are `PacketBufferWriteUInt8(value, destination)` for `0x4b`, `9`, `1`, and the narrowed converted byte count; destination-first and returned-value forms are rejected. |
| [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) | Copies the converted multibyte name into the local packet body. | Exact call copies `convertedLength` bytes to `packet + 4` after the one-byte length write. |
| [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md) | Socket-owned void queue/send funnel. | It allocates/copies the counted payload, adds its own queue terminator, posts command `8`, and returns void; this caller passes exactly `convertedLength + 4`. |
| [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md) | Shared outbound `Socket *` storage. | The method loads `0x0067a7ec` immediately before `Socket::QueueAndSendPacket`; the queued copy, not either caller array, owns asynchronous send storage. |

## Reachability, Protocol, And Duplicate Resolution

- `ClanNameInputDialog::OnButtonClick` reads the submitted text and invokes primary virtual slot `+0x5c` without consuming a return, proving the callback argument and `void` source ABI.
- Clan status row `10` sends request packet `{0x4b,9,0}`. Incoming opcode `0x43`, subtype `9`, action/result `0` opens `ClanAppearNameInputDialog`; a nonzero action/result is accepted without opening. This method emits the matching subtype-9 action-1 submission.
- Name siblings use the same source shape with subtypes `7`, `8`, and `10`. The money-only `_wtol`/UInt32BE seven-byte contract is explicitly excluded.
- Raw range `[0x004860e0,0x004861ce)` contains matching subtype-9/action-1 semantics but has no modeled function, inbound xref, VA/RVA/raw-offset pointer route, immediate route, or exact-start branch route. It is retained no-route duplicate evidence covered by this live vtable-backed method, not a second emitter.
- The target remains indivisible. No split, merge, range extension, new child, source-route migration, or duplicate definition is warranted.

## Parent Attachment And Reconstruction Notes

- Attached to [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md) because the exact sole vtable route and callback contract make the class the narrow source owner. The parent emits the declaration shell and `[[CHILDREN]]`; this child emits the single out-of-class definition.
- The formal C++ uses the inferred/descriptive source signature `void ClanAppearNameInputDialog::OnSubmitText(const wchar_t *text)`, value-first packet writers, contiguous source arrays, and statement-form void queue/send call. It intentionally omits compiler cookie and range-runtime syntax while preserving the indexed writes that generated those checks.
- Exact body, ABI/frame, packet/API order, conversion, send ownership, protocol lifecycle, duplicate disposition, owner/emitter, source placement, and CPP/H layering are closed. Original lexical names remain inferred, which is the evidence-backed reason the score is `94/94` rather than higher.
- 2026-06-20 B002 class-page refresh keeps this method as the behavior-body emitter for [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md). The parent class page emits only the declaration shell with `[[CHILDREN]]` after the class declaration so this full out-of-class `ClanAppearNameInputDialog::OnSubmitText` definition assembles after the class shell.

## Historical Source-Quality Assumptions

- The former SHA256 `4FEF4535ADDCF22E36850E887C54D953474CCCF4163574736C21537B978FE5F3`, `sub_4893E0`, `int __stdcall(LPCWCH)`, blank-comment prestate, destination-first/broad writer interpretation, returned queue-helper expression, and explicit range-runtime source blocks are dated superseded evidence. They must not be used as current body, ABI, API, or source-shape authority.
- The observed `0x100` and post-copy `0x80` branches remain real binary facts. Only their former presentation as explicit authored `__report_rangecheckfailure` source calls is rejected.

## 2026-06-17 B001 Source-Quality Recheck

- B001 resolved the shared submitter callback shape as `OnSubmitText(const wchar_t *text)` and kept this target class-owned/emitted through [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md).
- The target manually writes opcode `0x4b`, subtype `0x09`, action `0x01`, the converted-name length, and the converted payload bytes. The higher-level PacketBuffer string helper is rejected for this body.
- `WideCharToMultiByte(CP_ACP, ...)` converts into a 256-byte local buffer; the scratch NUL after the converted bytes is outside the counted packet length.
- `0x00575380` / `PacketBufferWriteUInt8` writes opcode, subtype, action, and converted-name length. `0x00516220` is the shared memmove wrapper used to copy the converted payload bytes.
- Historical limitation: class declarations and raw constructor reachability were then follow-up items. Current UID000023/UID0002NZ documentation resolves declaration and constructor emission while preserving the raw-start no-route caveat; this page still emits only the exact vtable-backed submitter body.

## Cross-References

- [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md)
- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:00021L][0x00485cc0-0x0048632e.ClanNameActionPacketHelpers](by-memory/0x00485cc0-0x0048632e.ClanNameActionPacketHelpers.md)
- [UID:00010K][0x00488fe0-0x004895fe.ClanNameDialogSubmitters](by-memory/0x00488fe0-0x004895fe.ClanNameDialogSubmitters.md)
- [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md)
- [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md)
- [UID:0001HZ][0x00575380-0x00575abb.PacketBufferHelpers](by-memory/0x00575380-0x00575abb.PacketBufferHelpers.md)
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md)
- [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md)

## Changes

- 2026-08-15 B006 UID0002O0 accepted implementation callback:
  - Raised the target from `86/91` to `94/94` without changing owner/emitter `000023` or reconstructable state.
  - Replaced the formal CPP with the accepted human-source body and kept the target H channel blank under class-owned declaration layering.
  - Added the current body hash and edge bytes, exact alignment exclusions, complete ten-row frame/source mapping, sole-vtable and all-head route audit, exact callees, value-first packet API, CP_ACP conversion/order, compiler-instrumented guards, Socket-owned counted queue copy, protocol request/open/submit lifecycle, raw duplicate disposition, historical reconciliation, and score ceiling.

- 2026-08-10 B006 UID0002NU family-coherence callback:
  - Changed the formal callback from stale `int` to `void` and changed the void QueueAndSendPacket return expression to a statement.
  - Preserved exact CP_ACP conversion, range checks, packet shape, value-first writer calls, `86/91`, owner/emitter `000023`, and reconstructable true.

- 2026-06-05 A004 live IDA refinement:
  - What existed before: `76/88`, no reconstruction parent, and generated helper/global labels in evidence.
  - Changed to: `82/90`, parent [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md), address/UID-based packet-send dependencies, and no final C++.
  - Summary/evidence: live IDA reconfirmed exact `0xee` bounds, vtable-only xref at `0x00615c74`, no direct callers, submitted-name conversion through `WideCharToMultiByte`, packet bytes `{ 0x4b, 9, 1, converted_length, name_bytes... }`, converted-length and final-packet length guards, send through `0x00574bb0` using packet sender storage `0x0067a7ec`, two bytes of padding after the function, and class-parent eligibility.
- 2026-06-17 B001 source-quality execution:
  - Changed to: `86/91`; owner/emitter remain [UID:000023][ClanAppearNameInputDialog](by-class/ClanAppearNameInputDialog.md), and first-draft C++ is now populated.
  - Evidence: B001 resolved the shared submit callback signature, opcode/subtype/action bytes, CP_ACP conversion, length/payload copy, range guards, queued send through `g_packetSender`, and raw-constructor caveat as class-level only.
- 2026-06-20 B002 class-page implementation note:
  - The parent class now emits only a declaration shell, leaving this page's existing `ClanAppearNameInputDialog::OnSubmitText(const wchar_t *)` C++ as the single behavior-body child.
