*** UID:0002JF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// [UID:0002JF] compiler-generated MenuItem scalar deleting destructor wrapper;
// rebuilt from ordinary virtual MenuItem destructor semantics, not handwritten source.

*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA reconfirms exact 0x3e MenuItem scalar deleting destructor wrapper, base vtable reset at 0x005172e6, cleanup/delete callees, flag-4 helper path, vtable slot xrefs from MenuItem and SeparatorMenuItem, no direct code callers, exact padding, eligible PopupMenuControls parent, and final destructor/helper-name caveats. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005172e0-0x0051731e MenuItem Scalar Deleting Destructor

## Status

- Entity kind: scalar deleting destructor.
- Owner: [UID:00007V][MenuItem](by-class/MenuItem.md).
- Likely source file: [UID:0000MN][PopupMenuControls](by-file/PopupMenuControls.md).
- Autogen parent: [UID:00007V][MenuItem](by-class/MenuItem.md). This wrapper is owned by `MenuItem` and reused by the `SeparatorMenuItem` vtable; [UID:0000MN][PopupMenuControls](by-file/PopupMenuControls.md) remains the source-file parent.
- Rebuild handling: source-declared/generated-binary compiler wrapper. This exact range is the MSVC scalar deleting destructor wrapper generated for ordinary/default `MenuItem` destructor semantics. Keep the target formal C++ block blank; the source should express an ordinary `MenuItem` destructor on the class, and MSVC should regenerate this wrapper.

## Behavior

Compiler deleting-destructor wrapper for the base menu item:

- restores the base `MenuItem` vtable;
- runs base cleanup through [UID:000370][0x004f4a90-0x004f4a97.LObjectDestructorBody](by-memory/0x004f4a90-0x004f4a97.LObjectDestructorBody.md);
- reads the deleting-destructor flags from `[ebp+8]`;
- calls [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) only when `(flags & 1) != 0` and `(flags & 4) == 0`;
- takes the alternate flag-4 path through the one-byte no-op helper at `0x0041b6a0` with `this` and object-size immediate `0x18` / 24 bytes (Verified with `int_convert.py`);
- returns `this`.

## Evidence

- 2026-06-05 live IDA MCP `lookup_funcs(0x005172e0)` reports `sub_5172E0`, start `0x005172e0`, and size `0x3e`.
- 2026-06-05 live IDA decompilation shows `*Block = &MenuItem::vftable` at `0x005172e6`, base cleanup call `sub_4F4A90(Block)` at `0x005172ec`, scalar-delete flag tests, optional `sub_4F4AC0(Block)`, and return of `Block`.
- 2026-06-05 `callees(0x005172e0)` reports `sub_4F4A90`, `sub_4F4AC0`, and `@_guard_check_icall_nop@4` (`0x0041b6a0`) from the flag-4 path.
- 2026-06-05 `callers(0x005172e0)` reports no direct code callers.
- 2026-06-05 `xrefs_to(0x005172e0)` reports data xrefs from `0x0061eb98` (`MenuItem` vtable slot `+0x00`) and `0x0061ebd0` (`SeparatorMenuItem` vtable slot `+0x00`), confirming this wrapper is vtable-owned by both base and separator entries.
- 2026-06-05 `xrefs_to(0x0061eb98)` includes the destructor vtable reset at `0x005172e6`, along with constructor/base-construction writes at `0x00516f41`, `0x00517031`, and `0x005171f1`.
- Raw bytes for the function start with `55 8b ec 56 8b f1 c7 06 98 eb 61 00`, matching the prologue and `MenuItem` vtable reset.
- Padding bounds are exact: `0x005172d4-0x005172e0` is twelve `0xcc` bytes and `0x0051731e-0x00517320` is two `0xcc` bytes before the next modeled function.

## 2026-06-18 Raw PE Recheck

B003 rechecked the target from `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` after live IDA MCP was unavailable. This pass corroborates the existing IDA-backed documentation and closes the previous score blocker around the blank C++ block.

Raw PE section context:

- Image base: `0x00400000`.
- `.text`: `0x00401000-0x0060c600`.
- `.rdata`: `0x0060d000-0x0066c200`.
- Target raw file offset: `0x001166e0`.
- Target body: `0x005172e0-0x0051731e`, `0x3e` / 62 bytes (Verified with `int_convert.py`).
- Preceding padding: `0x005172d4-0x005172e0`, twelve `0xcc` bytes, `0x0c` / 12 bytes (Verified with `int_convert.py`).
- Following padding: `0x0051731e-0x00517320`, two `0xcc` bytes, `0x02` / 2 bytes (Verified with `int_convert.py`).

Target bytes:

```text
55 8b ec 56 8b f1 c7 06 98 eb 61 00 e8 9f d7 fd ff 8b 45 08 a8 01 74 1f a8 04 75 10 56 e8 be d7 fd ff 83 c4 04 8b c6 5e 5d c2 04 00 6a 18 56 e8 8c 43 f0 ff 83 c4 08 8b c6 5e 5d c2 04 00
```

Fresh Capstone disassembly:

```asm
005172e0  push   ebp
005172e1  mov    ebp, esp
005172e3  push   esi
005172e4  mov    esi, ecx
005172e6  mov    dword ptr [esi], 0x0061eb98
005172ec  call   0x004f4a90
005172f1  mov    eax, dword ptr [ebp+8]
005172f4  test   al, 1
005172f6  je     0x00517317
005172f8  test   al, 4
005172fa  jne    0x0051730c
005172fc  push   esi
005172fd  call   0x004f4ac0
00517302  add    esp, 4
00517305  mov    eax, esi
00517307  pop    esi
00517308  pop    ebp
00517309  ret    4
0051730c  push   0x18
0051730e  push   esi
0051730f  call   0x0041b6a0
00517314  add    esp, 8
00517317  mov    eax, esi
00517319  pop    esi
0051731a  pop    ebp
0051731b  ret    4
```

Pointer-route and caller checks:

- Whole-file little-endian dword search for target VA `0x005172e0` found exactly two hits:
  - raw `0x21d598` -> `.rdata:0x0061eb98`, the `MenuItem` vtable slot `+0x00`;
  - raw `0x21d5d0` -> `.rdata:0x0061ebd0`, the `SeparatorMenuItem` vtable slot `+0x00`.
- Search for target RVA `0x001172e0` found no hits.
- Search for target raw file offset `0x001166e0` found no hits.
- `.text` rel32 call/jump scan found no direct call or jump target to `0x005172e0`.
- The `MenuItem` vtable VA `0x0061eb98` appears in the base constructor, `StringMenuItem` base-construction path, `SeparatorMenuItem` base-construction path, this wrapper, and [UID:0002JG][0x00517400-0x00517441.StringMenuItemScalarDeletingDestructor](by-memory/0x00517400-0x00517441.StringMenuItemScalarDeletingDestructor.md).
- The `SeparatorMenuItem` vtable VA `0x0061ebd0` appears in the `SeparatorMenuItem` constructor install path.
- The sibling `StringMenuItem` deleting destructor VA `0x00517400` appears in the `StringMenuItem` vtable slot at `.rdata:0x0061ebb4`, confirming the same wrapper-family route.

Vtable route facts:

```text
0x0061eb94 -> 0x0064b604  MenuItem RTTI/COL pointer
0x0061eb98 -> 0x005172e0  MenuItem scalar deleting destructor wrapper
0x0061eb9c -> 0x004f4b10  inherited runtime-class accessor
0x0061eba0 -> 0x0041b6c0  LObject default no-op virtual

0x0061ebcc -> 0x0064b724  SeparatorMenuItem RTTI/COL pointer
0x0061ebd0 -> 0x005172e0  reused MenuItem scalar deleting destructor wrapper
0x0061ebd4 -> 0x004f4b10  inherited runtime-class accessor
0x0061ebd8 -> 0x0041b6c0  LObject default no-op virtual
0x0061ebe4 -> 0x00517250  separator draw slot
0x0061ebe8 -> 0x00517220  separator fixed-metrics slot
0x0061ebec -> 0x00517240  separator style-flags slot
```

Flag behavior:

```cpp
// Descriptive pseudocode for the compiler wrapper only. Do not emit as target C++.
this->vptr = &MenuItem::vftable;
LObject::~LObject();

if ((flags & 1) != 0) {
    if ((flags & 4) != 0) {
        compiler_noop_sized_delete_path(this, 0x18);
    } else {
        operator delete(this);
    }
}

return this;
```

The flag-4 path is only reachable after flag bit `1` is set. The helper at `0x0041b6a0` is a one-byte `ret` followed by `0xcc` padding, not a `MenuItem` source helper and not a second object-freeing path. The normal free path is only the [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) call reached when `flags & 1` is set and `flags & 4` is clear.

## Score Rationale

Raised from `82/90` to `86/92` after B003 rechecked the wrapper policy and raw evidence. Completion now reflects exact bytes, padding, branch behavior, callee identities, vtable-only reachability, absence of direct callers, owner/emitter validation, and target-specific no-formal-C++ proof. Confidence remains below final-audit because live IDA MCP was unavailable for this session and the original source may have used either an explicit empty destructor or an implicit/default destructor, but this uncertainty no longer blocks 85/85 source-quality routing.

## B003 Source-Quality Reanalysis

B003 classifies this page as compiler-generated deleting-destructor glue, not a human-authored source function named `ScalarDeletingDestructor`. The wrapper resets the base vtable, calls base cleanup, branches on scalar-delete flags, optionally calls the delete helper, and has a flag-4 helper path. It is shared by the `MenuItem` and `SeparatorMenuItem` vtable slots.

The target remains reconstructable because final source must produce equivalent destructor/vtable semantics, but the by-memory formal C++ block must stay blank for this exact wrapper. The source-level representation belongs on the class as an ordinary virtual destructor, for example `MenuItem::~MenuItem()`, with MSVC responsible for generating the deleting wrapper. Handwritten wrapper C++ is rejected as decompiler-shaped and would duplicate compiler responsibility.

## Target No-Code Proof

Do not populate target formal C++. This range is compiler-generated deleting-destructor glue. The source-level representation is an ordinary/default `MenuItem` destructor; MSVC regenerates the deleting-wrapper flag checks, vtable reset, base cleanup call, no-op size path, and optional `operator delete` call.

Handwriting those instructions in final source would encode compiler flags, vtable addresses, and allocation-helper calls directly in project C++. That would be less source-faithful than ordinary C++ destructor semantics. If class-level reconstruction needs a source declaration, the plausible shape is an ordinary `virtual ~MenuItem();` and an empty/default destructor body; the binary does not prove whether the original source spelled it explicitly or relied on implicit/default generation.

`SeparatorMenuItem` reuses this wrapper through its vtable slot `+0x00`, so this page is not evidence for a separate source-authored `SeparatorMenuItem::~SeparatorMenuItem()` body.

## Cross-References

- [UID:0001Y4][MenuItemVtables](by-type/by-vtable/MenuItemVtables.md)
- [UID:0001V5][MenuItemLayouts](by-type/by-struct/MenuItemLayouts.md)
- [UID:0000MN][PopupMenuControls](by-file/PopupMenuControls.md)
- [UID:0002J3][0x00516f00-0x00516f67.MenuItemConstructor](by-memory/0x00516f00-0x00516f67.MenuItemConstructor.md)
- [UID:0002JB][0x005171b0-0x0051721d.SeparatorMenuItemConstructor](by-memory/0x005171b0-0x0051721d.SeparatorMenuItemConstructor.md)

## Changes

- 2026-06-30 B008 empty-emitter implementation:
  - Score remains `86/92`.
  - Added a formal comment-only no-handwritten-wrapper marker. This clears the generated empty marker without emitting decompiler-shaped scalar deleting destructor C++.
  - Evidence: B008 MCP reconfirmed the exact wrapper role, vtable-slot xrefs from `MenuItem` and `SeparatorMenuItem`, no direct code callers, and ordinary virtual destructor semantics; the source action remains the class-level virtual destructor declaration.
- 2026-06-13 A003 parent refresh: `AUTOGEN_PARENT_UID` changed from [UID:0000MN][PopupMenuControls](by-file/PopupMenuControls.md) to [UID:00007V][MenuItem](by-class/MenuItem.md) after the class page reached `86/88`. Live IDA MCP reconfirmed the `0x3e` / 62-byte destructor size (Verified with `int_convert.py`), base vtable reset at `0x005172e6`, destructor helper callees, and `MenuItem`/`SeparatorMenuItem` vtable-slot ownership.
- 2026-06-18 B003 source-quality recheck:
  - Raised from `82/90` to `86/92`.
  - Metadata unchanged: owner/emitter [UID:00007V][MenuItem](by-class/MenuItem.md), `RECONSTRUCTABLE:TRUE`, blank formal C++.
  - Evidence: raw PE bytes/disassembly confirmed the exact wrapper, `0x3e` / 62-byte size, twelve-byte prepad, two-byte postpad, base vtable reset, [UID:000370][0x004f4a90-0x004f4a97.LObjectDestructorBody](by-memory/0x004f4a90-0x004f4a97.LObjectDestructorBody.md) call, exact scalar-deleting flag order, [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) call only for `flags & 1` with `flags & 4` clear, one-byte `0x0041b6a0` no-op sized-delete path with `0x18` / 24-byte object size, target VA pointer hits only in `MenuItem` and `SeparatorMenuItem` vtable slots, no RVA/raw-offset hits, and no `.text` rel32 direct callers. The source action is ordinary/default `MenuItem` destructor semantics, not handwritten wrapper C++.
- 2026-06-05: Raised from `70/86` to `82/90` and set autogen parent to [UID:0000MN][PopupMenuControls](by-file/PopupMenuControls.md).
  - Evidence: live IDA MCP verified exact function size, decompilation, destructor vtable reset, cleanup/delete callees, data xrefs from the `MenuItem` and `SeparatorMenuItem` vtable slots, no direct callers, and surrounding padding. C++ remains blank because final destructor/source-helper spelling is below the 95+ threshold.
- 2026-06-17 B003 source-quality incorporation: kept `82/90`, kept direct owner/emitter [UID:00007V][MenuItem](by-class/MenuItem.md), and recorded explicit no-formal-C++ policy for the compiler-generated scalar deleting destructor wrapper.
  - Evidence: B003 compared the vtable-slot xrefs, base vtable reset, cleanup/delete callees, flag-4 path, wrapper sharing with `SeparatorMenuItem`, and class/source-root docs. The proper source action is an ordinary virtual destructor, not target-level wrapper C++.
