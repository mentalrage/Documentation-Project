*** UID:00017E | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00008N | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00008N | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// MonsterImageLib::GetArchiveBoundsBucket is intentionally not emitted as a
// standalone body yet. [UID:00017E] documents the exact lazy DATA/MON%d.DAT
// bounds-cache behavior, but source-quality C++ still requires final DAT
// archive/vector ownership APIs.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live MCP session 80de0a67 confirms the exact 0x315-byte lazy DATA/MON%d.DAT bounds-bucket loader: it looks up the +0x3c bounds-bucket cache, constructs/opens the missing archive, inserts the reader through the +0x1c open-archive cache, allocates and caches a 0x0c bucket, reads an 8-byte header, allocates 0x18-byte entries plus a zero-bounds sentinel, rebases payload pointers by the DAT payload base, and returns the cached bucket; source owner/emitter remain MonsterImageLib, while formal C++ remains intentionally blank pending source-ready cache/DAT ownership APIs. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dbe60-0x004dc174 MonsterImageLib GetArchiveBoundsBucket

## 2026-08-17 Typed Cache Source Correction

The caches at `+0x1c` and `+0x3c` are source-facing `stdext::hash_map<int, DATFile *>` and `stdext::hash_map<int, MonsterArchiveBoundsBucket *>`. The calls at `0x00457100` and `0x00457580` are the folded old-MSVC/Dinkumware `insert` and `find` implementations for those typed members, not a custom `DATIndexVector` API. This closes the custom-container blocker while preserving the broader no-code disposition: a complete method body still depends on source-ready DAT reader/container ownership and complete MonsterImageLib declarations.

## Status

- Confidence: strong for owner, behavior, exact range, caller/source placement, cache offsets, bucket/entry layout, and no-code disposition; medium-high for final source-facing names because original symbols and source-ready cache/DAT APIs are still unavailable.
- Owner class: [UID:00008N][MonsterImageLib](by-class/MonsterImageLib.md).
- Likely source file: [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md).
- IDA function: `sub_4DBE60`.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact boundary, padding, ABI/callers, raw helper-gap disposition, MonsterImageLib source placement, two cache-index offsets, DAT archive open path, bucket/entry allocation sizes, pointer rebasing, sentinel behavior, cleanup ownership, rejected merge/ownership alternatives, and the formal no-code proof are documented. |
| Confidence | 91 | Live MCP session `80de0a67` reconfirms server/IDB provenance, target range, caller/xref set, string bytes, decompiled `+0x1c/+0x3c` cache behavior, DAT/cache/list/RectBounds callees, and support-API limitations. Confidence stays below final-audit range because original names and a source-ready cache/DAT ownership API are still unavailable. |

## Behavior

This function lazily opens a numbered `DATA/MON%d.DAT` archive and builds the cached bounds bucket used by monster projection/render bounds.

Observed flow:

1. Search the `+0x3c` bounds-bucket cache for `archiveIndex`.
2. If no bucket is cached, format `DATA/MON%d.DAT` and construct a DAT container/reader for that archive.
3. Insert the open archive reader into the `+0x1c` open-archive cache.
4. Allocate a 0x0c-byte [UID:0001VA][MonsterArchiveBoundsBucket](by-type/by-struct/MonsterArchiveBoundsBucket.md), insert it into the `+0x3c` bounds-bucket cache, and read its 8-byte header.
5. Allocate `(entryCount + 1) * 0x18` bytes for [UID:0001VB][MonsterArchiveBoundsEntry](by-type/by-struct/MonsterArchiveBoundsEntry.md) rows.
6. Read each entry's four 16-bit bounds values and two 32-bit payload offsets, convert the offsets to pointers by adding the archive payload base, and write a zero-bounds sentinel entry.
7. Return the cached bucket pointer.

## Layout Evidence

The function is the clearest current proof that [UID:0001VD][MonsterImageLibLayout](by-type/by-struct/MonsterImageLibLayout.md) contains two embedded typed standard-map cache tables:

| Object offset | Role |
| --- | --- |
| `+0x1c` | `stdext::hash_map<int, DATFile *> m_openArchiveIndex`, keyed by numbered `DATA/MON%d.DAT` archive id. |
| `+0x3c` | `stdext::hash_map<int, MonsterArchiveBoundsBucket *> m_boundsBucketIndex`, keyed by the same archive id. |

The accepted source-facing member names are `m_openArchiveIndex` and `m_boundsBucketIndex`, matching the layout support page. The target body still exposes those fields through decompiler storage such as `this + 7` and `this + 15`; those raw dword views are evidence aliases only and are rejected for source-facing prose or final C++.

## Evidence

- 2026-05-31 IDA MCP `lookup_funcs 0x004dbe60` reports `sub_4DBE60` at `0x004dbe60` with size `0x315`, so the exact range is `0x004dbe60-0x004dc174`.
- 2026-06-15 IDA MCP `lookup_funcs` reconfirms `sub_4DBE60` at `0x004dbe60`, size `0x315`; `0x004dc180` is the next modeled local resolver, and bytes at `0x004dc174-0x004dc180` are a trailing null byte plus `0xcc` padding.
- 2026-05-31 IDA MCP `callers 0x004dbe60` reports callers from `0x004db647` (`sub_4DB5C0`, documented as `ComputeRenderBounds`) and `0x004dc1b9` (`sub_4DC180`, another bounds-cache consumer), supporting `MonsterImageLib` ownership rather than a standalone DAT helper.
- 2026-06-15 IDA MCP `xref_query` reports three code refs to `0x004dbe60`: `0x004db647` from `sub_4DB5C0`, `0x004dc1b9` from `sub_4DC180`, and raw/local ref `0x004dc279`; `xrefs_to 0x004dc180` reports the high-level render consumers at `0x004db1a5` and `0x004db47a`.
- 2026-05-31 IDA MCP `callees 0x004dbe60` reports DAT/file construction and reader helpers, DATIndexVector/list helpers (`0x004570e0`, `0x00457100`, `0x00457580`), the shared RectBounds initializer `0x004b7c50`, and allocation/error helpers.
- 2026-06-15 IDA MCP `decompile` reconfirms the lazy cache lookup at `this+0x3c` (`this + 15` dword view), archive reader/cache insertion through `this+0x1c` (`this + 7` dword view), `DATA/MON%d.DAT` formatting, bucket allocation of `0x0c` bytes, entry allocation as `24 * (entryCount + 1)`, per-entry 16-bit bounds reads, two 32-bit payload-offset reads, payload-base rebasing, and a zero-bounds sentinel entry.
- 2026-06-15 IDA MCP `analyze_component` reports direct callees including DAT/open/read helpers `sub_49BE80`, `sub_49C130`, `sub_49C550`, `sub_49C720`, cache/list helpers `sub_4570E0`, `sub_457100`, `sub_457580`, allocation helpers `sub_516030`/`sub_516050`, and RectBounds initializer `sub_4B7C50`.
- IDA decompilation reads and writes the object cache tables through `this + 7` and `this + 15`, matching offsets `+0x1c` and `+0x3c`.
- IDA decompilation allocates 0x0c bytes for the bucket header and `24 * (entryCount + 1)` bytes for bounds entries.
- 2026-06-03 `xrefs_to 0x004dbe60` confirms callers at `0x004db647`, `0x004dc1b9`, and `0x004dc279`; `xrefs_to 0x004dc180` confirms the intermediate resolver is used by `RenderMonster` and `RenderMonsterImage`.
- 2026-06-03 `callees 0x004dbe60` confirms DAT construction/open/read helpers, DATIndexVector/list helpers, RectBounds initialization, and allocation/error helpers.
- Wide string bytes at `0x0061c07c` decode to `DATA/MON%d.DAT`, matching the numbered archive path formatted by this function.
- 2026-06-27 B003 live MCP session `80de0a67` returned `server_health` status `ok` for IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`; no fallback-only evidence or IDA DB edits were used.
- 2026-06-27 `lookup_funcs` reconfirmed `sub_4DBE60` at `0x004dbe60`, size `0x315`; `0x004dc174` resolves inside that function, `0x004dc180` is the next modeled resolver `sub_4DC180` of size `0xb5`, and `0x004dc240`/`0x004dc279` are not modeled functions.
- 2026-06-27 raw boundary bytes reconfirmed the target prologue at `0x004dbe60`; bytes around `0x004dc160-0x004dc180` show the final calls/return sequence followed by `0xcc` alignment before the resolver at `0x004dc180`. The target therefore stays `0x004dbe60-0x004dc174`; `0x004dc175-0x004dc17f` is padding/alignment, not source body.
- 2026-06-27 `xrefs_to 0x004dbe60` reconfirmed three code refs: normal callers `0x004db647` in `ComputeRenderBounds` and `0x004dc1b9` in `ResolveMonsterArchiveBounds`, plus raw local call `0x004dc279`. `xrefs_to 0x004dc180` reconfirmed render consumers at `0x004db1a5` and `0x004db47a`.
- 2026-06-27 `insn_query 0x004dc235-0x004dc2e0` showed an IDA-undefined prologued local helper/gap from `0x004dc240-0x004dc2db` with `retn 8`, a raw call to this target at `0x004dc279`, and alignment at `0x004dc2de`; `xrefs_to 0x004dc240` returned no xrefs to the helper start. This gap is outside [UID:00017E] and does not justify widening or merging the target.
- 2026-06-27 decompilation reconfirmed the miss path: find in `m_boundsBucketIndex`, format UTF-16 `DATA/MON%d.DAT`, allocate a 0x1c-byte DAT container, construct/open the DAT file, insert the opened reader through `m_openArchiveIndex`, allocate the 0x0c bucket, insert it into `m_boundsBucketIndex`, read the 8-byte header, allocate `0x18 * (entryCount + 1)` entry storage, read four 16-bit bounds and two 32-bit payload offsets per row, call [UID:0001VP][RectBoundsLayout](by-type/by-struct/RectBoundsLayout.md) initializer `0x004b7c50`, rebase both payload fields by the DAT payload base, write the zero-bounds sentinel, and return the cached bucket.
- 2026-06-27 `analyze_component` reconfirmed direct callees including DAT/container/read helpers `sub_49BE80`, `sub_49C130`, `sub_49C550`, `sub_49C720`, DATIndexVector/list helpers `sub_4570E0`, `sub_457100`, `sub_457580`, allocation helpers `sub_516030`/`sub_516050`, error helper `sub_4F4AA0`, and RectBounds initializer `sub_4B7C50`.
- 2026-06-27 `get_bytes 0x0061c07c` showed UTF-16LE `DATA/MON%d.DAT` followed by `MON%d.EPF` and `MONSTER.DNA`; `get_string` returning only `DA` is a string-typing limitation, so raw bytes are the authoritative string evidence. `xrefs_to 0x0061c07c` reconfirmed refs from `LoadMonsterArchives` at `0x004dbca1` and this target at `0x004dbeb4`.
- 2026-06-27 `xrefs_to 0x004db8b0` returned zero callers. The old EPF aggregate builder shares the bucket/entry layout vocabulary but is not the live lazy `DATA/MON%d.DAT` loader and is rejected as a merge/owner for this target.
- `tools/int_convert.py` verified the cited sizes/offsets used by this pass: `0x315`, `0x0c`, `0x18`, `0x1c`, `0x3c`, `0x14`, `0x20`, `0x24`, `0x40`, `0x50`, `0xffffffe`, and `268435454` (Verified with int_convert.py).
- Support API audit for the no-code decision resolves cache insertion/lookup as ordinary typed `stdext::hash_map` operations. [UID:0000IM][DATArchive](by-file/DATArchive.md), `DATFile`, `DATFileContainer`, and the bucket/entry/layout docs still do not expose a complete source-ready DAT reader/container lifetime surface for this full method body.

## Reconstructability Notes

- This is NexusTK-owned source behavior and should be reconstructed, so `RECONSTRUCTABLE` is `TRUE`.
- This page routes directly through [UID:00008N][MonsterImageLib](by-class/MonsterImageLib.md), which clears the corrected parent gate and emits through [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md). It is source-bearing and now emits a formal no-code proof comment rather than a blank empty marker until a source-ready method body is safe.
- `RECONSTRUCTION_CPP CODE` is intentionally a target-specific no-code marker. Standard-map `find`/`insert` vocabulary is resolved; a complete source body would still require decompiler-shaped DAT storage or invented DAT reader/container ownership APIs not supported by current documentation. Emitting either would violate source-quality rules.
- Rejected alternatives preserved by the 2026-06-27 pass: raw/decompiler names such as `sub_4DBE60`, `ArgList`, `this + 15`, and temporary `v*` locals are evidence aliases only; `DWORD`/`long`/`void *` source types are rejected in favor of `archiveIndex` and `MonsterArchiveBoundsBucket *`; DAT subsystem, caller, layout/type, old EPF-builder, aggregate, resolver, raw-helper, and padding ownership/merge routes are rejected for this exact child.
- This page owns only `0x004dbe60-0x004dc174`; related structures are documented separately and should not be pasted into this range's C++ block.

## Cross-References

- [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md)
- [UID:00008N][MonsterImageLib](by-class/MonsterImageLib.md)
- [UID:00017C][0x004dac40-0x004dc420.MonsterImageLibLocalMethodCluster](by-memory/0x004dac40-0x004dc420.MonsterImageLibLocalMethodCluster.md)
- [UID:0001VD][MonsterImageLibLayout](by-type/by-struct/MonsterImageLibLayout.md)
- [UID:0001VA][MonsterArchiveBoundsBucket](by-type/by-struct/MonsterArchiveBoundsBucket.md)
- [UID:0001VB][MonsterArchiveBoundsEntry](by-type/by-struct/MonsterArchiveBoundsEntry.md)
- [UID:00003K][DATIndexVector](by-class/DATIndexVector.md)

## Changes

- 2026-06-30 B011 empty-emitter family implementation callback:
  - Score unchanged at `88/91`.
  - Evidence: inserted the accepted formal `RECONSTRUCTION_CPP CODE` no-code marker preserving the target-specific cache/DAT API proof. The page still records exact `0x315` size, caller set `0x004db647`/`0x004dc1b9` plus raw local `0x004dc279`, `m_openArchiveIndex`/`m_boundsBucketIndex`, 0x0c bucket and 0x18 entry behavior, payload rebasing, sentinel row, raw helper-gap exclusion, and rejected generic DAT/caller/layout/EPF-builder ownership alternatives.
- Before: completion/confidence were `50/65` and `RECONSTRUCTABLE` was blank.
- Changed to: `RECONSTRUCTABLE:TRUE`, completion `72`, confidence `86`.
- Summary/evidence: IDA MCP on 2026-05-31 verified the exact function boundary, MonsterImageLib callers, cache offsets, bucket/entry allocations, DAT reader flow, RectBounds initializer calls, and return from the cached bucket pointer. Scores remain below `95` because final C++ source shape and exact helper/local names are not yet near-final.
- 2026-06-03 parent and live evidence update:
  - What existed before: score was `72/86`, autogen parent metadata was blank, and the page still carried generated-output status wording.
  - Changed to: score is `78/88`, parent [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) is attached, generated-output status wording was removed, and fresh caller/callee/resource evidence was added.
  - Summary/evidence: current IDA MCP confirms callers from bounds/render helpers, cache resolver use, DAT/cache/list callees, `DATA/MON%d.DAT` resource bytes, `+0x1c/+0x3c` cache offsets, and bucket/entry allocation behavior. C++ remains blank because final source spelling and ownership wrappers are below `95/95`.
- 2026-06-15 A003 Goal 2 refresh:
  - Changed score from `78/88` to `85/90`.
  - Route changed from file owner/emitter [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) to class owner/emitter [UID:00008N][MonsterImageLib](by-class/MonsterImageLib.md), because the child and class parent now clear the corrected `85/85` gate.
  - Summary/evidence: current IDA MCP reconfirmed the exact `0x315` function, end padding, three local callers, render-resolver consumers, `DATA/MON%d.DAT` lazy open path, `+0x1c/+0x3c` cache indexes, 0x0c bucket allocation, 0x18 entry rows, pointer rebasing, sentinel row, and DAT/cache/list/RectBounds callees.
- 2026-06-27 B003 implementation callback:
  - Changed score from `85/90` to `88/91`; owner/emitter stay [UID:00008N][MonsterImageLib](by-class/MonsterImageLib.md), `RECONSTRUCTABLE:TRUE` stays unchanged, and the formal C++ block stays blank.
  - Summary/evidence: live MCP session `80de0a67` reconfirmed server/IDB provenance, target boundary and alignment, normal callers plus raw helper-gap call, DAT/cache/list/RectBounds callees, raw UTF-16 `DATA/MON%d.DAT` bytes, `m_openArchiveIndex`/`m_boundsBucketIndex` behavior, 0x0c bucket and 0x18 entry allocation, payload-base rebasing, sentinel row, cleanup ownership, and zero callers to the old EPF builder. The pass also audited DATIndexVector/DAT support docs and records an exact no-code proof rather than leaving C++ blank as a generic future task.
