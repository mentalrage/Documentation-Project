*** UID:0002MY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000F9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
/* Superseded hand-lowered concrete std::vector/reset instance retained as history.
#include <new>

template <>
void Tree<FolderTreePane::TreeElem>::ResetToSingleRoot()
{
    typedef mystr::StringBase<wchar_t,
                             mystr::mychar_traits<wchar_t> > FolderTreeName;

    for (TreeNode<FolderTreePane::TreeElem> *node = m_storage.m_begin;
         node != m_storage.m_end;
         ++node)
    {
        node->data.name.~FolderTreeName();
    }

    m_storage.m_end = m_storage.m_begin;

    TreeNode<FolderTreePane::TreeElem> root;
    root.parentIndex = -1;
    root.firstChildIndex = -1;
    root.lastChildIndex = -1;
    root.previousSiblingIndex = -1;
    root.nextSiblingIndex = -1;
    root.data.childrenLoaded = 0;
    root.data.expanded = 0;
    root.disabled = 0;

    if (m_storage.m_end == m_storage.m_capacity)
    {
        m_storage.InsertAt(m_storage.m_end, root);
    }
    else
    {
        new (m_storage.m_end) TreeNode<FolderTreePane::TreeElem>(root);
        ++m_storage.m_end;
    }
}
*/
// Compiler instance of inline Tree<FolderTreePane::TreeElem>::ResetToSingleRoot.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Concrete compiler instance of inline UID0004YK `Tree<T>::ResetToSingleRoot`: clears direct vector storage, default-constructs one 36-byte `TreeNode<FolderTreePane::TreeElem>`, preserves payload row/depth/alignment non-initialization, and lowers append through vector copy/growth support. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b5b00-0x004b5bae FolderTreeResetToSingleRoot

## Current Accepted UID0004YK Generic Instance Disposition

This range is the concrete compiler instance of inline Tree<FolderTreePane::TreeElem>::ResetToSingleRoot in Tree.h: vector clear, default TreeNode construction, and one root push. TreeNode initializes five links and disabled; TreeElem initializes only childrenLoaded and expanded while default-constructing name and leaving rowIndex/depth/alignment untouched. The pooled four-dword -1 load, capacity branch, copy construction, and InsertAt call are compiler/vector lowering. The former concrete formal body remains a non-compiling historical comment, not duplicate authored FolderTreePane.cpp source.

## Status

- Disposition: reconstructable NexusTK project-local tree template support.
- Entity kind: `Tree<FolderTreePane::TreeElem>` reset helper.
- Owner: [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md)
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- Rebuild handling: source-authored once as the generic inline UID0004YK Tree.h method; this exact range is its concrete Folder payload/vector compiler instance, not a CPP specialization.
- Current route: `90/92`, owner/emitter UID0000F9, reconstructable true, with a nonblank route through UID0000JG. No score or ownership change accompanies the accepted field-token synchronization.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b5b00-0x004b5bae` | `Tree<FolderTreePane::TreeElem>::ResetToSingleRoot` | Source-authored reset method. Destroys existing node names, rewinds storage, constructs one root element with sentinel link values, appends it by direct storage copy or `InsertAt`, then destroys the temporary string. |

## Historical Binary Evidence Under Superseded TreeStorage Labels

- `lookup_funcs 0x004b5b00` identifies `sub_4B5B00`, size `0xae`, ending at `0x004b5bae`.
- `callers 0x004b5b00` shows constructor/setup/reset calls at `0x004b1c18`, `0x004b1c95`, and `0x004b262a`.
- IDA decompilation destroys existing element strings from current begin to end, rewinds the storage end pointer, constructs an empty temporary `SimpleUString`, initializes the root record from `xmmword_619660` plus `-1`, and inserts/copies that record through the storage helper.
- The helper uses [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)'s 36-byte record layout and calls `0x004b3d50` when storage capacity is exhausted.
- 2026-06-04 live IDA MCP reconfirmed the exact function range and the same three callers. Live decompilation shows destruction of existing names at `record + 0x14`, rewinding `TreeStorage`'s end pointer, root-record initialization from `xmmword_619660` plus local `v6 = -1`, capacity-growth via `0x004b3d50`, in-place copy construction through `0x004b55e0`, and temporary string cleanup through `0x00582b70`.
- Live byte reads show `xmmword_619660` holds four `0xffffffff` dwords, matching the sentinel root-link initialization; the fifth dword is set to `-1` locally before insertion.
- Boundary checks show `0xcc` padding at `0x004b5af4-0x004b5b00` before this helper and `0x004b5bae-0x004b5bb0` before the iterator first-child helper.
- 2026-06-28 B009 accepted MCP evidence uses database/session `agent_b009_0002my_20260628`. `lookup_funcs` ids `11` and `60` report `sub_4B5B00` at `0x004b5b00`, size `0xae`, with neighboring helpers `0x004b55e0`, `0x004b5bb0`, `0x004b5bd0`, and `0x004b5c20` at their expected boundaries.
- `xrefs_to` id `12` reports exactly three callers: `0x004b1c18` and `0x004b1c95` in the `FolderTreePane` constructor/setup path, and `0x004b262a` in `FolderTreePane::SetRootNodeName` / `SetRootName`. The caller decompilations show construction/setup call reset twice, then set the root name and expand, while `SetRootNodeName` resets, builds an iterator over index `0`, writes the new root name, expands, and refreshes layout.
- `callees` id `50` reports the exact helper set: `0x00582b70` for existing/local `SimpleUString` destruction, `0x00582b20` for local `SimpleUString` default construction, `0x004b55e0` for `FolderTreePane::TreeElem` copy construction in the available-capacity path, and `0x004b3d50` for the `TreeStorage<FolderTreePane::TreeElem>::InsertAt` full-capacity path.
- `decompile` id `16` shows the reset loop destroys each existing record name at `record + 0x14`, sets `m_storage.m_end` back to `m_storage.m_begin`, default-constructs the temporary root `name`, loads four `-1` dwords from `xmmword_619660`, stores a fifth local `-1`, clears loaded/expanded/hidden flag bytes, and then chooses direct copy construction when capacity remains or `InsertAt` when `m_end == m_capacity`.
- `disasm` id `17` confirms the same behavior at instruction level: the loop advances by `0x24`, `mov [edi+4], esi` rewinds the end pointer, `call sub_582B20` constructs the local name, `movaps xmm0, ds:xmmword_619660` loads the first four sentinel dwords, `mov [ebp+var_24], 0FFFFFFFFh` stores the fifth, `cmp [edi+8], eax` performs the capacity test, `call sub_4B55E0` plus `add dword ptr [edi+4], 24h` is the fast append, and `call sub_4B3D50` is the slow `InsertAt` fallback.
- `get_bytes` ids `18` and `57` read `xmmword_619660` as four `0xffffffff` dwords and confirm padding: twelve `0xcc` bytes from `0x004b5af4-0x004b5b00` before this method and two `0xcc` bytes at `0x004b5bae-0x004b5bb0` before the next iterator helper. `basic_blocks` id `59` covers the target-local blocks through `0x004b5bae`; additional blocks are SEH/unwind support outside this method.
- Vtable route evidence remains the concrete tree wrapper: xrefs to `0x0061a500` include constructor/setup stores, cleanup/destructor stores, and the scalar deleting destructor route. This supports [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md) as the owner/emitter rather than moving the method to [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), [UID:0000FC][TreeStorage_struct_FolderTreePane__TreeElem_](by-class/TreeStorage_struct_FolderTreePane__TreeElem_.md), [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md), or no-owner/non-emitting state.

## Current Generic Reconstruction Route

- Keep with the local tree template/wrapper owned by `FolderTreePane`. The accepted source-facing method name is `ResetToSingleRoot`, with storage members `m_storage.m_begin`, `m_storage.m_end`, and `m_storage.m_capacity`.
- Active authored source is UID0004YK `Tree<T>::ResetToSingleRoot()` in `Tree.h`: `m_nodes.clear(); m_nodes.push_back(TreeNode<T>());`. `TreeNode` initializes five links and `disabled`; `FolderTreePane::TreeElem` default-constructs `name`, clears `childrenLoaded`/`expanded`, and deliberately leaves row/depth/alignment bytes untouched.
- The formal block preserves the former concrete specialization only inside a non-compiling historical comment, followed by the compiler-instance disposition marker. No FolderTreePane.cpp method body emits.
- Rejected current alternatives: raw `sub_4B5B00` naming, authored TreeStorage wrappers, value-initialized payload fields, standalone generated Tree source, and any duplicate concrete reset specialization.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | Exact range/callers, binary behavior, root sentinels, payload non-initialization, vector append lowering, historical evidence, and resolved generic source route are documented. |
| Confidence | 92 | Decompilation, bytes, callers, cross-instantiation behavior, and generated Tree.h agree that this is the concrete instance of the generic inline reset. |
| Reconstructable | true | UID0004YK Tree.h owns the active source; this page retains concrete binary-instance evidence and emits no duplicate specialization. |

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md)
- [UID:0001WP][FolderTreePaneTreeTemplates](by-type/by-template/FolderTreePaneTreeTemplates.md)
- [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)

## Historical Superseded 2026-07-29 UID0002MZ Node/Payload Formal Reconciliation

- This remains a concrete CPP specialization because it initializes FolderTree payload state; it does not move into generic `Tree.h`.
- The storage range is `TreeNode<FolderTreePane::TreeElem>`. Existing name destruction at concrete `+0x14` is `node.data.name`, using the accepted one-pointer `mystr::StringBase` family rather than the separate SSO `SimpleUString` source type.
- Root construction writes five generic link sentinels, clears `root.data.childrenLoaded`, `root.data.expanded`, and generic `root.disabled`, and deliberately leaves payload row/depth and implicit alignment untouched exactly as the binary does.
- Direct in-place construction and capacity-exhausted `InsertAt` use full nodes. All exact callers, bytes, padding, sentinel constant, temporary string lifetime, and owner/emitter metadata remain unchanged at `90/92`, UID0000F9.
- Historical assumption: the former formal treated all links/state as fields of a 36-byte `TreeElem` and emitted `hidden` as payload state. That monolithic model is retained above as historical evidence but superseded by cross-specialization node/payload separation.

## Changes

- 2026-07-14 B001 UID0002N0 hidden-state synchronization:
  - Score, route, nesting, and all reset behavior remain unchanged at `90/92`, owner/emitter UID0000F9, reconstructable true, and `Nested:-8`.
  - Replaced only the superseded `root.isHidden = 0` token with exact accepted `root.hidden = 0` in the managed specialization and synchronized current prose.
  - Preserved five `-1` link writes, zeroed `childrenLoaded`/`expanded`/`hidden`, deliberately uninitialized `rowIndex`/`depth`/reserved bytes, storage rewind, copy/grow branches, and temporary-name lifetime.

- 2026-06-28 B009 accepted implementation:
  - Before: `COMPLETION:85`, `CONFIDENCE:90`, owner/emitter [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md), reconstructable, and blank formal C++ produced an empty emitter marker in generated `FolderTreePane.cpp`.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:92`, owner/reconstructable/emitter fields unchanged, and the accepted formal `Tree<FolderTreePane::TreeElem>::ResetToSingleRoot()` body inserted.
  - Evidence: B009 MCP session/database `agent_b009_0002my_20260628` confirmed the `0x004b5b00-0x004b5bae` range/size, three callers at `0x004b1c18`, `0x004b1c95`, and `0x004b262a`, callees `0x00582b70`, `0x00582b20`, `0x004b55e0`, and `0x004b3d50`, disassembly/decompilation for storage rewind and append, `xmmword_619660` sentinel bytes, padding before/after the method, constructor/root-name caller behavior, and the `0x0061a500` tree-wrapper vtable route.
  - Rejected/stale assumptions preserved: old 95/95 code-entry premise, old Wave2/generated wrapper source, value-initialized root records, generated field names, marker-only/no-code treatment, no-owner/non-emitting treatment, standalone generated tree class source, moving ownership to `FolderTreePane`/`TreeStorage`/`TreeElem`, and splitting this exact method as a container.

- 2026-06-10 B001-028 strict-gate repair:
  - Changed to: `COMPLETION:85`, retaining `CONFIDENCE:90`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000F9`.
  - Evidence: the B001-028 parent repair raised [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md) to `85/88`; this page already had exact caller, sentinel, storage-rewind, insertion/copy, cleanup, and boundary evidence.
  - Gate decision: the child and direct parent both clear `85/85`.
- 2026-06-04 live IDA refresh:
  - Changed to: `COMPLETION:82`, `CONFIDENCE:90`, and `AUTOGEN_PARENT_UID:0000F9`.
  - Evidence: live IDA MCP reconfirmed the exact `0x004b5b00-0x004b5bae` range, three `FolderTreePane` callers, decompiled storage rewind and root-record insertion behavior, `xmmword_619660` sentinel dwords, copy/grow helper calls, temporary string cleanup, and padding boundaries.
  - Superseded limit: the 2026-06-28 accepted implementation above resolves the earlier blank-emitter state by adding the formal reset body while preserving the same owner and source route.
