*** UID:0002CJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004W | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:13 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool FileDownloader::SubmitMinimapRequest(int mapId,
                                          const wchar_t *mapCode,
                                          const wchar_t *outputPath)
{
    FileDownloaderMinimapRequest *request = new FileDownloaderMinimapRequest;
    request->state = 0;
    request->contentLength = 0;
    request->bytesDownloaded = 0;
    request->mapId = mapId;
    wcscpy_s(request->mapCode, 0x10, mapCode);
    wcscpy_s(request->outputPath, 0x104, outputPath);

    return DispatchRequest(kDownloadMinimapFileMessage, request, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Allocates minimap request payload and posts downloader message `10000`; live caller passes `dword_67A738` as the FileDownloader queue receiver. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0041b180-0x0041b1f5 FileDownloader Submit Minimap Request

## Summary

- Address range: `0x0041b180-0x0041b1f5`
- Entity kind: downloader request-submission helper
- Message id: `10000` / `0x2710`
- Known caller: `0x00451d18` path, which passes `dword_67A738` as `this`
- Likely owner: [UID:00004W][FileDownloader](by-class/FileDownloader.md) request dispatch, emitted through [UID:0000JC][FileDownloader](by-file/FileDownloader.md), with feature semantics in minimap download code
- Physical/context inventories: [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md) and [UID:0000WJ][0x0041aa00-0x0041b2c9.CashShopVersionAndCatalogDownload](by-memory/0x0041aa00-0x0041b2c9.CashShopVersionAndCatalogDownload.md); [UID:0000WH][0x0041a5d0-0x0041a662.CashShopRequestRawLifecycle](by-memory/0x0041a5d0-0x0041a662.CashShopRequestRawLifecycle.md) is only an early raw constructor/destructor inventory.
- Disposition: reconstructable project code
- Autogen parent: [UID:0000JC][FileDownloader](by-file/FileDownloader.md)
- Confidence: strong for body, exact caller, message id, FileDownloader queue receiver, and payload layout; B001 2026-06-18 resolves the source-facing helper/request direction enough to use `SubmitMinimapRequest` and `FileDownloaderMinimapRequest`.

This helper allocates a `0x238` byte request payload, copies two wide strings into fixed buffers, stores one integer argument, and posts downloader message `10000`. The matching dispatcher branch at `0x0041b110` routes message `10000` to the minimap file download helper at `0x0041a750`.

## Layout

| Offset | Meaning |
| --- | --- |
| `+0x00` | byte/state field initialized to `0` |
| `+0x04` | dword initialized to `0` |
| `+0x08` | dword initialized to `0` |
| `+0x0c` | caller-supplied integer argument |
| `+0x10` | wide buffer copied from argument 3, size `0x104` wchar units |
| `+0x218` | wide buffer copied from argument 2, size `0x10` wchar units |

## Raw Code Evidence

```asm
0041B186  push    238h
0041B18D  call    operator new
0041B1B9  mov     [esi+0Ch], eax
0041B1C5  call    _wcscpy_s        ; destination +0x218, size 0x10
0041B1D6  call    _wcscpy_s        ; destination +0x10, size 0x104
0041B1E3  push    2710h
0041B1E8  call    sub_596960       ; DispatchRequest(this, 10000, payload, 0)
```

## Live IDA Evidence

- 2026-06-04 live IDA reports `0x0041b180-0x0041b1f5` as one function with size `0x75`.
- Decompilation shows allocation of `0x238`, fixed wide-buffer copies to `+0x218` and `+0x10`, storage of the integer argument at `+0x0c`, and queue posting through `sub_596960(this, 10000, payload, 0)`.
- The caller window at `0x00451cf8-0x00451d18` reads `dword_67A738`, moves it into `ecx`, and calls this helper, proving the live receiver is the FileDownloader/download-dispatcher singleton.
- The dispatcher at `0x0041b110` handles message `10000` by calling `0x0041a750` and then freeing the raw request block.

## 2026-06-15 Live IDA Evidence Refresh

- MCP session `a003_objectlist_dispatch_20260615` reported active `NexusTK.exe.i64`, image base `0x00400000`, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` reconfirmed `sub_41B180` at `0x0041b180` with size `0x75`, followed by eleven `0xcc` bytes before [UID:0002CK][0x0041b200-0x0041b26d.FileDownloaderSubmitCashShopCatalogRequest](by-memory/0x0041b200-0x0041b26d.FileDownloaderSubmitCashShopCatalogRequest.md).
- `analyze_function 0x0041b180` reported prototype `BOOL __thiscall(HANDLE *this, int, wchar_t *Source, wchar_t *)`, one caller `sub_451C90`, callees `_wcscpy_s`, `operator new`, and `sub_596960`, four basic blocks, and one code xref at `0x00451d18`.
- Hex-Rays decompilation reconfirmed allocation of `0x238`, nullable payload handling, zero initialization at `+0x00/+0x04/+0x08`, integer storage at `+0x0c`, `_wcscpy_s` to `(payload + 0x218, 0x10)` and `(payload + 0x10, 0x104)`, and `sub_596960(this, 10000, payload, 0)`.
- Disassembly of caller `sub_451C90` reconfirmed the route: `0x00451cf8 mov esi, dword_67A738`, `0x00451d16 mov ecx, esi`, then `0x00451d18 call sub_41B180`.
- `xrefs_to 0x0067a738` reconfirmed the singleton lifetime/use set, including FileDownloader constructor/destructor-family writes plus this minimap caller, the two fitting-room submit callers, and application/fitting-room consumers. This supports FileDownloader queue ownership while preserving feature semantics in the minimap caller.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 85 | Exact bounds, caller, callee set, payload size/layout, queue message id, dispatcher relationship, singleton receiver route, and neighboring padding are now refreshed against the current IDA session. |
| Confidence | 90 | Current decompilation, xrefs, disassembly caller window, and byte-boundary evidence all agree. Confidence stays below final-audit level because source-facing helper and payload type names remain provisional. |

## Reconstructed C++ Candidate

```cpp
bool FileDownloader::SubmitMinimapRequest(int requestArg,
                                          const wchar_t* shortName,
                                          const wchar_t* outputPath)
{
    MinimapDownloadRequest* request = new MinimapDownloadRequest;
    request->state = 0;
    request->field4 = 0;
    request->field8 = 0;
    request->requestArg = requestArg;
    wcscpy_s(request->shortName, 0x10, shortName);
    wcscpy_s(request->outputPath, 0x104, outputPath);
    return DispatchRequest(10000, request, 0);
}
```

Names are source-facing candidates only. The fixed buffers and message id are the stable reconstruction facts.

## 2026-06-18 B001 Source-Quality Sync

B001 recommends `SubmitMinimapRequest` as the source-facing FileDownloader queue helper name and `FileDownloaderMinimapRequest` as the raw request payload name. Payload fields should match the worker-helper report: `state` at `+0x00`, reserved/progress dwords at `+0x04/+0x08`, `mapId` at `+0x0c`, `outputPath[0x104]` at `+0x10`, and `mapCode[0x10]` at `+0x218`. The generated `mapCode[32]` interpretation is rejected because this helper copies the map code with `_wcscpy_s(..., 0x10)` and the allocation ends at `0x238`.

## 2026-07-01 B008 Formal C++ Disposition

B008's accepted report promotes this helper from a candidate-only body to formal first-draft C++. The source body uses `FileDownloader::SubmitMinimapRequest(int mapId, const wchar_t *mapCode, const wchar_t *outputPath)`, allocates `FileDownloaderMinimapRequest`, zeros `state`, `contentLength`, and `bytesDownloaded`, stores `mapId`, copies `mapCode` with size `0x10`, copies `outputPath` with size `0x104`, and posts `kDownloadMinimapFileMessage` through `DispatchRequest`. MiniMap remains feature context; the live receiver and emitter route are FileDownloader.

## Cross-References

- [UID:0000JC][FileDownloader](by-file/FileDownloader.md)
- [UID:00004W][FileDownloader](by-class/FileDownloader.md)
- [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md)
- [UID:0001SF][DownloaderMessageIds](by-type/by-constant/DownloaderMessageIds.md)
- [UID:0000QH][g_pCashShopRequest](by-global/g_pCashShopRequest.md)

## Changes

- 2026-06-15 C001 section-container follow-up:
  - Before: `COMPLETION:82`, `CONFIDENCE:86`, with older live IDA evidence but no current-session caller/byte refresh.
  - After: `COMPLETION:85`, `CONFIDENCE:90`, keeping owner/emitter route through [UID:0000JC][FileDownloader](by-file/FileDownloader.md) and final C++ blank.
  - Summary/evidence: live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirmed exact bounds, decompiled payload layout, `sub_596960(this, 10000, payload, 0)`, the sole caller window passing `dword_67A738` in `ecx`, the singleton xref set, and padding before the catalog submit helper. Source-facing names remain provisional, so the score only clears the gate.
- 2026-06-18 B001 FileDownloader download-helper support sync:
  - Score unchanged.
  - Summary/evidence: source-facing names are now coordinated with [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) `DownloadMinimapFile`: `SubmitMinimapRequest`, `FileDownloaderMinimapRequest`, `mapId`, `outputPath[0x104]`, and `mapCode[0x10]`. The helper remains FileDownloader-owned; MiniMap remains feature/request context only.
- 2026-06-04 live IDA evidence pass:
  - Before: `COMPLETION:75`, `CONFIDENCE:80`, reconstructable/autogen metadata blank, and FileDownloader ownership was only described as likely.
  - After: `COMPLETION:82`, `CONFIDENCE:86`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000JC`.
  - Summary/evidence: live IDA confirms exact bounds, payload layout, queue message `10000`, dispatcher receiver `dword_67A738` at caller `0x00451d18`, and the matching `OnMessage` branch. The score stays below final level because source-facing helper/request names remain provisional.
- 2026-07-01 B008 empty-emitter family implementation:
  - Changed from `85/90` to `88/90`, owner from [UID:0000JC][FileDownloader](by-file/FileDownloader.md) file to [UID:00004W][FileDownloader](by-class/FileDownloader.md) class, and `EMITTER_POSITION_OPTIONAL:13`.
  - Summary/evidence: inserted formal `SubmitMinimapRequest` C++ with `0x238` payload semantics, `0x10`/`0x104` wide-copy sizes, `mapId`, and message `10000` routed through the accepted FileDownloader message constant.
