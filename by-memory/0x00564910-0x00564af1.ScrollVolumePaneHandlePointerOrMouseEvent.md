*** UID:0004XV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ScrollVolumePane::HandlePointerOrMouseEvent(Event *event)
{
    bool handled = false;
    RectBounds partRect;
    const int localY = event->m_pointer.m_y;
    const int localX = event->m_pointer.m_x;

    switch (event->m_type) {
    case kEventPointerMove:
        if (m_activePart != kScrollVolumePartNone) {
            InvalidateRect(&m_bounds);
            handled = true;
        }
        break;

    case kEventLeftButtonDown:
        if (!PointInRect(localY, localX, &m_bounds) || !CanAdjust())
            return false;

        if (m_activePart == kScrollVolumePartNone) {
            const ScrollVolumePart part = HitTestPart(localY, localX);
            if (part != kScrollVolumePartNone) {
                CaptureMouse();
                BeginInteraction(part, localY, localX);
                InvalidateRect(&m_bounds);
            }
        }

        m_thumbDragActive = true;
        handled = true;
        break;

    case kEventLeftButtonUp:
        if (m_activePart != kScrollVolumePartNone) {
            ReleaseMouseCapture();
            ResetInteractionState();
            handled = true;
        }

        m_thumbDragActive = false;
        InvalidateRect(&m_bounds);
        break;

    default:
        break;
    }

    return handled;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: EventHandler pointer/mouse override performs horizontal hit testing, mouse capture, thumb/page interaction, repeat setup, release cleanup, and the resolved +0xfb drag-state writes. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00564910-0x00564af1 ScrollVolumePane HandlePointerOrMouseEvent

## Virtual Identity

- Exact range `[0x00564910,0x00564af1)`, size `0x1e1`; UID0000CO owner/emitter, position 90.
- Secondary vtable `0x00624100` slot `+0x04` points here at `0x00624104`. The entry receiver is the inherited EventHandler view at complete-object `+0xa0`; source is the `ScrollVolumePane::HandlePointerOrMouseEvent(Event *)` override.
- This supersedes historical `OnMouseEvent`/PaneMouseEvent naming without changing behavior.

## Behavior And Field Flow

- Reads event type plus local Y/X coordinates from Event, preserving the project's Y-then-X rectangle convention.
- Pointer move invalidates while an active part exists.
- Left-button down rejects out-of-bounds or non-adjustable state, hit-tests the five-part slider, captures the mouse, begins thumb/page interaction, schedules the initial repeat through BeginInteraction, and invalidates bounds.
- The accepted down path writes `m_thumbDragActive` at `+0xfb` to true. This resolves the former documentation blocker that claimed no writer existed.
- Left-button up releases capture when active, removes timers and clears active/hover state through ResetInteractionState, writes `m_thumbDragActive=false`, and invalidates bounds.

## Evidence And Boundaries

- Live IDA modeled `sub_564910` with vtable xref `0x00624104`; decompilation shows event cases, PointInRect, CanAdjust, HitTestPart, capture/release, timer cleanup, and writes to `+0xfb`.
- `[0x0056490a,0x00564910)` and `[0x00564af1,0x00564b00)` are six and fifteen bytes of `0xcc` alignment.
- `EventHandler` is an inherited Pane facet, not a new direct multiple-inheritance base declaration on ScrollVolumePane.

## Rejected Alternatives And Negative Evidence

- Reject `IsScrollable`, `OnMouseEvent`, vertical-scroll semantics, and TextEditPane ownership.
- The raw helper bodies used by BeginInteraction/ResetInteractionState have no static inbound route, but their source-shaped bodies and homologous family justify source helper calls/inlining; that negative route is a confidence cap, not a reason to omit this virtual.

