*** UID:00010D | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ClanStatusPane::SendClanLeaveResponse(bool confirmed)
{
    unsigned char packet[5];

    PacketBufferWriteUInt8(0x4b, &packet[0]);
    PacketBufferWriteUInt8(3, &packet[1]);
    PacketBufferWriteUInt8(1, &packet[2]);
    PacketBufferWriteUInt8(confirmed ? 1 : 0, &packet[3]);
    packet[4] = 0;

    g_packetSender->QueueAndSendPacket(packet, 4);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-verified Clan leave-response helper that sends four bytes `{0x4b, 3, 1, accept_or_cancel}` through the shared sender, with only confirm/cancel callback callers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00485700-0x00485778 SendClanLeaveResponse

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: `ClanStatusPane` packet helper method
- Documented owner: [UID:00002K][ClanStatusPane](by-class/ClanStatusPane.md)
- Likely source module: [UID:0000I8][Clan](by-file/Clan.md), emitted through the `ClanStatusPane` class route into `social/Clan.cpp`
- Confidence: very strong

## Covered Range

| Range | Function | Notes |
| --- | --- | --- |
| `0x00485700-0x00485778` | `ClanStatusPane::SendClanLeaveResponse(bool confirmed)` | Sends opcode `0x4b`, subtype `3`, action `1`, and the caller-provided confirm/cancel response byte; clears the byte after the sent packet. |

## IDA MCP Evidence

Checked on 2026-05-23: IDA reports a real function at `0x00485700` with size `0x79`. Callers are `ClanLeaveConfirmDialog::OnConfirm` at `0x0048a0a0` and `ClanLeaveConfirmDialog::OnCancel` at `0x0048a0b0`.

2026-05-27 predecessor-gap check confirms [UID:00021H][0x00485690-0x004856f9.SendClanLeaveRequestPacket](by-memory/0x00485690-0x004856f9.SendClanLeaveRequestPacket.md) immediately precedes this response helper, followed by `0x004856f9-0x00485700` `align 10h` padding.

2026-05-27 successor-gap check confirms this function ends before `0x00485779-0x00485780` `align 10h` padding and [UID:00021I][0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers](by-memory/0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers.md).

2026-06-05 IDA MCP recheck confirms a real function at `0x00485700`, size `0x79`, ending end-exclusive at `0x00485779`. The only code xrefs/callers are [UID:0002O7][0x0048a0a0-0x0048a0ae.ClanLeaveConfirmDialogConfirm](by-memory/0x0048a0a0-0x0048a0ae.ClanLeaveConfirmDialogConfirm.md) at `0x0048a0a8` and [UID:0002O8][0x0048a0b0-0x0048a0be.ClanLeaveConfirmDialogCancel](by-memory/0x0048a0b0-0x0048a0be.ClanLeaveConfirmDialogCancel.md) at `0x0048a0b8`.

The helper builds a four-byte packet buffer on the stack. It writes byte `75` (`0x4b`) to `buffer[0]`, byte `3` to `buffer[1]`, byte `1` to `buffer[2]`, the caller-provided response byte to `buffer[3]`, clears the next byte, and sends exactly four bytes through the packet sender at `0x00574bb0` using global `0x0067a7ec`. The byte writer is `0x00575380`; the sender/global pair matches the adjacent opcode `0x4b` Clan helper strip.

The confirm callback decompiles to a call to `0x00485700` with argument `1`, and the cancel callback decompiles to a call to `0x00485700` with argument `0`, making the argument an accept/decline response flag rather than a subtype or action field. B014 reanalysis additionally resolves the source-facing call shape: both direct callers load `g_pClanStatusPane` into `ECX` immediately before pushing `1` or `0` and calling this function. The callee does not read fields, so decompilers can present it as a free helper, but the repeated receiver setup is stronger evidence for a `ClanStatusPane` member helper whose `this` is optimized/dead inside the body.

## Ownership Decision

This is a source-facing [UID:00002K][ClanStatusPane](by-class/ClanStatusPane.md) packet helper method, emitted through [UID:0000I8][Clan](by-file/Clan.md) into `social/Clan.cpp`. The existing short name `SendClanLeaveResponse` remains correct, but the best source-facing qualified name is `ClanStatusPane::SendClanLeaveResponse`.

Ranked ownership:

1. [UID:00002K][ClanStatusPane](by-class/ClanStatusPane.md): accepted as canonical owner. Both direct callers materialize `g_pClanStatusPane` in `ECX` before the call, and neighboring receiver-owned packet helpers such as `SendClanJoinListSelectionPacket` and the enlist-list selection helper use the same pattern.
2. [UID:0000I8][Clan](by-file/Clan.md): retained as the source-file route only. The function still belongs in `social/Clan.cpp`; file-level ownership is less precise than the class method route.
3. [UID:00002I][ClanLeaveConfirmDialog](by-class/ClanLeaveConfirmDialog.md): rejected as canonical owner. The confirm/cancel callbacks decide the boolean response but deliberately route through the status-pane singleton.
4. PacketBuffer, Socket, and `g_packetSender`: rejected as owners. They are shared transport dependencies.
5. The adjacent retained opcode `0x4b` helper strip: useful protocol context, but not the direct source owner for this routed response helper.

## Score Rationale

Completion is `88` and confidence is `92` because the function boundary, only callers, packet byte layout, shared sender global, support helper roles, and confirm/cancel argument meanings are verified or mutually reinforced by existing support docs, and the source-facing receiver shape is now resolved. It remains below final confidence because live IDA MCP was not available during the B014 pass and exact original spelling for `PacketBufferWriteUInt8`, `QueueAndSendPacket`, and the `confirmed`/byte parameter remains inferred.

## Source-Facing Signature

Preferred first-draft signature:

```cpp
void ClanStatusPane::SendClanLeaveResponse(bool confirmed);
```

The semantic `confirmed` parameter is preferred over a raw byte name because the only two direct callers are `ClanLeaveConfirmDialog::OnConfirm()` and `ClanLeaveConfirmDialog::OnCancel()`, passing `1` and `0` respectively. A mechanical `unsigned char response` signature is byte-equivalent, but it hides the UI-level boolean decision that the callsites prove. `SendClanLeaveConfirmResponse` is unnecessarily verbose; the request/response distinction is already clear from the action byte and the existing helper name.

The binary writes the stack byte to packet byte `3`; a source `bool` remains compatible with the observed `push 1` / `push 0` callsites.

## Packet Layout

| Byte | Value | Meaning |
| --- | --- | --- |
| `0` | `0x4b` | Clan packet opcode. |
| `1` | `0x03` | Clan leave subtype. |
| `2` | `0x01` | Leave response action. |
| `3` | `0` or `1` | Cancel/confirm response byte from the leave-confirm dialog. |

Rejected alternatives:

- Treating byte `2` as a length/count is rejected because the sender length is the separate literal `4`, and neighboring clan helpers use the third byte as the request/submit/response action.
- Treating byte `3` as padding is rejected because it is populated from the caller argument and the send length includes it.
- Treating the helper as `void SendClanLeaveResponse(ClanStatusPane *pane, unsigned char response)` is rejected because the status-pane pointer is not pushed as a stack argument; callers load it into the `this` register.

The explicit clear after byte `3` is a local packet-buffer terminator/scratch byte. It is not sent because `QueueAndSendPacket` receives length `4`.

## Helper And Global Names

- `0x00575380` is best modeled as `PacketBufferWriteUInt8`. PacketBuffer support pages identify this helper family as width-specific packet-buffer writers; this member writes one byte and clears the following scratch byte.
- `0x00574bb0` is best modeled as `Socket::QueueAndSendPacket` or equivalent queued socket sender. Support pages show queue allocation/copy behavior and command posting rather than final transport framing, so `SendPacket` alone is too broad.
- `0x0067a7ec` is best modeled as `Socket *g_packetSender`. Global support pages document many packet send xrefs and Socket ownership; feature-specific generated aliases should stay rejected.

These support names are source-quality enough for first-draft reconstruction. Remaining uncertainty is original spelling, not role or source placement.

## Cross-References

- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:00002K][ClanStatusPane](by-class/ClanStatusPane.md)
- [UID:00002I][ClanLeaveConfirmDialog](by-class/ClanLeaveConfirmDialog.md)
- [UID:00021H][0x00485690-0x004856f9.SendClanLeaveRequestPacket](by-memory/0x00485690-0x004856f9.SendClanLeaveRequestPacket.md)
- [UID:00021I][0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers](by-memory/0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers.md)
- [UID:0002O7][0x0048a0a0-0x0048a0ae.ClanLeaveConfirmDialogConfirm](by-memory/0x0048a0a0-0x0048a0ae.ClanLeaveConfirmDialogConfirm.md)
- [UID:0002O8][0x0048a0b0-0x0048a0be.ClanLeaveConfirmDialogCancel](by-memory/0x0048a0b0-0x0048a0be.ClanLeaveConfirmDialogCancel.md)

## Changes

- 2026-05-27: The predecessor gap was previously part of UNKNOWN `0x00484f16-0x00485700`. Added the adjacent [UID:00021H][0x00485690-0x004856f9.SendClanLeaveRequestPacket](by-memory/0x00485690-0x004856f9.SendClanLeaveRequestPacket.md) and confirmed `0x004856f9-0x00485700` as alignment padding before this response helper. Evidence: IDA MCP raw disassembly on 2026-05-27.
- 2026-05-27: The successor gap was previously UNKNOWN `0x00485778-0x00485ac0`. Added [UID:00021I][0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers](by-memory/0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers.md) after one alignment byte range `0x00485779-0x00485780`. Evidence: IDA MCP raw disassembly on 2026-05-27.
- 2026-06-05: Marked the leave-response helper reconstructable and raised the score to `84/90`.
  - Before: the page had caller and boundary notes but still used placeholder score/reconstructability metadata.
  - After: the page documents the exact four-byte opcode `0x4b` packet layout, shared sender call, only callers, and accept/cancel argument mapping.
  - Evidence: live IDA MCP `lookup_funcs`, `decompile`, `callers`, `callees`, and `xrefs_to` for `0x00485700`, `0x0048a0a0`, and `0x0048a0b0`.
- 2026-06-05: Attached the helper to [UID:0000I8][Clan](by-file/Clan.md) and cleaned stale generated-name/global-label wording while preserving the `84/90` score; generated coverage/stat sync is pending because shared report/validator files were leased by another agent.
- 2026-06-19 B014 source-quality incorporation:
  - Raised metadata from `84/90` to `88/92`, changed canonical owner/emitter from [UID:0000I8][Clan](by-file/Clan.md) to [UID:00002K][ClanStatusPane](by-class/ClanStatusPane.md), and populated first-draft C++ as `ClanStatusPane::SendClanLeaveResponse(bool confirmed)`.
  - Evidence: both direct callbacks load `g_pClanStatusPane` into `ECX`, push response byte `1` or `0`, and call this helper; the body writes `{0x4b, 3, 1, response}`, clears the fifth scratch byte, and queues exactly four bytes through `g_packetSender`. The report rejects loose file-helper, explicit stack-passed pane pointer, `ClanLeaveConfirmDialog` ownership, and no-code/compiler-glue interpretations.
