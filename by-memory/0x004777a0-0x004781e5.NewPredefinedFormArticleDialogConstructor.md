*** UID:0002UJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewPredefinedFormArticleDialog::NewPredefinedFormArticleDialog(
    DialogSession *session,
    const wchar_t *previousSubject,
    RectBounds *bounds,
    const unsigned char *formPacket)
    : BulletinDialog(session, 5)
{
    const bool freeTextMode =
        g_pUserPane->m_predefinedFormArticleFreeTextMode;

    if (freeTextMode)
    {
        m_predefinedFormEntryCount = 1;
        m_visibleFormRowCount = 5;
    }
    else
    {
        DeserializePredefinedFormArticleEntries(
            formPacket,
            m_predefinedFormEntries,
            &m_visibleFormRowCount,
            &m_predefinedFormEntryCount);
    }

    RectBounds controlBounds;
    const int visibleRows = m_visibleFormRowCount;

    if (g_useEpfAssets)
    {
        InitRectBounds(&controlBounds, 149, 25 * visibleRows + 139,
            212, 25 * visibleRows + 163);
        AddControl(new ImageButtonControlPane(23, &controlBounds));

        InitRectBounds(&controlBounds, 226, 25 * visibleRows + 139,
            289, 25 * visibleRows + 163);
        AddControl(new ImageButtonControlPane(22, &controlBounds));

        InitRectBounds(&controlBounds, 94, 59, 404, 71);
        AddControl(new StaticTextControlPane(previousSubject, true,
            37, 0, &controlBounds, false, false));

        InitRectBounds(&controlBounds, 94, 86, 404, 98);
    }
    else
    {
        InitRectBounds(&controlBounds, 149, 21 * visibleRows + 108,
            221, 21 * visibleRows + 124);
        AddControl(new ImageButtonControlPane(23, &controlBounds));

        InitRectBounds(&controlBounds, 226, 21 * visibleRows + 108,
            298, 21 * visibleRows + 124);
        AddControl(new ImageButtonControlPane(22, &controlBounds));

        InitRectBounds(&controlBounds, 131, 51, 410, 63);
        AddControl(new StaticTextControlPane(previousSubject, true,
            37, 0, &controlBounds, false, false));

        InitRectBounds(&controlBounds, 131, 71, 410, 83);
    }

    AddControl(new TextEditControlPane(g_emptyWideString, true,
        143, 0, &controlBounds, false, false, false, false, 1.0f, 0));

    if (freeTextMode)
    {
        InitRectBounds(&controlBounds, 36, 110, 410,
            (g_useEpfAssets ? 25 : 21) * visibleRows + 110);

        TextEditControlPane *bodyEdit =
            new TextEditControlPane(g_emptyWideString, true,
                143, 0, &controlBounds, false, true,
                false, false, 0.99f, 0);
        bodyEdit->m_reservedControlState = 1;
        bodyEdit->SetMaxLines(1024);
        bodyEdit->SetMaxLength(4096);
        AddControl(bodyEdit);
    }
    else
    {
        int left = 36;
        int rowTop = 110;

        for (unsigned char rowIndex = 0;
             rowIndex < m_predefinedFormEntryCount;
             ++rowIndex)
        {
            PredefinedFormArticleEntry& entry =
                m_predefinedFormEntries[rowIndex];
            const int titleWidth =
                GetTextWidth(entry.title, entry.titleLength);
            const int titleRight = left + titleWidth;

            InitRectBounds(&controlBounds,
                left, rowTop, titleRight, rowTop + 16);
            AddControl(new StaticTextControlPane(entry.title, true,
                37, 0, &controlBounds, false, false));

            TextEditControlPane *valueEdit;

            if (entry.usesUserImagePane)
            {
                InitRectBounds(&controlBounds,
                    titleRight, rowTop,
                    titleRight + titleWidth, rowTop + 16);
                valueEdit = new TextEditControlPane(entry.body, true,
                    143, 0, &controlBounds, false, false,
                    true, false, 1.0f, 0);

                if (entry.rowAdvanceMode != 0)
                {
                    rowTop += 21;
                    left = 36;
                }
                else
                {
                    left = titleRight + 5 + 6 * entry.textLineCount;
                }
            }
            else
            {
                rowTop += 21;
                InitRectBounds(&controlBounds,
                    36, rowTop, 400,
                    rowTop + 16 * entry.textLineCount);
                valueEdit = new TextEditControlPane(entry.body, true,
                    143, 0, &controlBounds, false, true,
                    true, false, 0.99f, 0);
                valueEdit->m_reservedControlState = 1;
                valueEdit->SetMaxLines(1024);
                valueEdit->SetMaxLength(4000);
                rowTop += 21 *
                    ((16 * entry.textLineCount) / 21 + 1);
                left = 36;
            }

            AddControl(valueEdit);
        }
    }

    InitRectBounds(bounds, 50, 50,
        g_useEpfAssets ? 500 : 496,
        (g_useEpfAssets ? 25 : 21) * visibleRows +
            (g_useEpfAssets ? 254 : 208));
    SetPendingControl(1);
    SetHoverControl(3);
    SetBounds(bounds, false);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-verified constructor boundary, caller, vtable setup, parser calls, and dynamic form-control/layout setup. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004777a0-0x004781e5 NewPredefinedFormArticleDialog Constructor

## Status

- Disposition: reconstructable NexusTK dialog constructor.
- Entity kind: modeled class constructor.
- Direct autogen parent: [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
- Parent aggregate: [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md).
- File root: [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md).
- Rebuild handling: `source-authored`.
- Emission handling: source-owned and reconstructable, but intentionally non-emitting until the constructor has source-quality helper/member/type declarations. The former nonblank class emitter plus blank formal C++ produced a generated empty-emitter marker rather than useful source.
- Confidence: strong for boundary, sole caller, constructor argument roles, vtable setup, parser call sites, dynamic-row/free-text branch structure, source ownership, and the non-emitting no-code proof; medium for final dynamic-control field names, source-quality UI helper names, and exact constructor argument types.

## Address Range

`0x004777a0-0x004781e5`.

2026-07-04 B005 current MCP session `73c77998` reports `sub_4777A0` at this exact half-open range, size `0xa45` / 2629, with terminal `retn 10h` before `0x004781e5`. Current bytes show the immediate predecessor `0x00477790-0x004777a0` is sixteen `0xcc` padding bytes, and the successor `0x004781e5-0x004781f0` is `0xcc` alignment before the separate raw navigate helper at `0x004781f0`.

`lookup_funcs` reports no function at `0x00477790`, `0x004781e5`, or `0x004781f0`, while the next modeled sibling `0x00478240` remains `sub_478240` size `0x62`. The gap after this constructor is not source body and must not be merged into UID0002UJ.

## Behavior

Constructs the predefined form article compose dialog. The constructor initializes the `NewPredefinedFormArticleDialog` vtables, opens the dialog session base, reads the incoming form/template payload, calls `DeserializePredefinedFormArticleEntries_478650` when the packet carries serialized predefined rows, creates dynamic static/edit controls, and sets up the dialog resources/layout for the form article workflow.

The current live caller at `0x00471d4a` allocates `0x5368` / 21352 bytes for the object, prepares the previous subject text in a `0x100`-wide-character buffer, chooses an initial rectangle based on `byte_66DA97`, and passes the packet pointer as the constructor's serialized form argument. The caller decompilation in session `73c77998` shows `Destination[256]`, previous-subject copy from a prior dialog when available, `byte_66DA97` rectangle selection, `sub_4F4AA0(21352)` allocation, and `sub_4777A0(v7, a1, Destination, v11, a3)` at `0x00471d4a`. That call shape supports the working signature:

```text
NewPredefinedFormArticleDialog::NewPredefinedFormArticleDialog(BulletinSession *session, const wchar_t *initialSubject, const Rect &initialRect, const uint8_t *serializedFormPacket)
```

The exact project types are still provisional, but the roles are now strong enough for class parenting, reconstructable status, and non-emitting source ownership.

## Layout And Control Flow

- Entry count is written at `this + 0x274`; visible row count is written at `this + 0x276`; parsed entry records begin at `this + 0x278`.
- The constructor has two parser call sites, `0x00477848` and `0x00477d0c`, one for each top-level `byte_66DA97` layout branch. Both pass the serialized packet, `this + 0x278`, `this + 0x276`, and `this + 0x274`.
- B014's 2026-07-03 UID0002UP current MCP recheck confirms both parser call sites again in session `b010_00032w_20260703` and resolves the parser helper as source-ready through the inferred `PredefinedFormArticleEntry` declaration on [UID:0002UP][0x00478650-0x00478933.DeserializePredefinedFormArticleEntries](by-memory/0x00478650-0x00478933.DeserializePredefinedFormArticleEntries.md). This does not change this constructor's own blank-C++ caveats for dynamic UI/control/helper names.
- If the global mode flag at `dword_67A748 + 0x1340fc` is set, the constructor bypasses parsing and seeds one entry with five visible rows.
- In non-free-text mode, the loop walks `0x40c`-byte entry records, measures each row title with `sub_4BA9A0`, creates a label/static control from the title bytes at `this + 0x27a + index * 0x40c`, and then creates either a compact or multiline edit control from the body text at `this + 0x47e + index * 0x40c`.
- The branch tests `this + 0x47a + index * 0x40c` for an extra user-image/body control path, `this + 0x47b + index * 0x40c` for line count/height, and `this + 0x680 + index * 0x40c` for extra row advancement.
- In the alternate free-text path, it creates one large edit control backed by `word_60DB20`, marks the edit object at offset `0x100`, and sets buffer limits through `sub_498BE0`/`sub_498BD0`.
- The terminal setup builds the final dialog rectangle from the visible row count, calls `sub_49DDD0(1)`, sets mode `3` with `sub_49FC00`, opens/positions the dialog through `sub_544BD0`, returns `this`, and ends with `retn 0x10`.

## Evidence

- 2026-07-04 B005 MCP refresh used active NexusTK IDB session `73c77998`: `server_health` was `ok` with `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- `lookup_funcs` in session `73c77998` reports `0x004777a0 -> sub_4777A0`, size `0xa45`; no function at `0x00477790`, `0x004781e5`, or `0x004781f0`; sibling `0x00478240 -> sub_478240`, size `0x62`; parser `0x00478650 -> sub_478650`, size `0x2e3`; submit sibling `0x00478be0 -> sub_478BE0`, size `0x3ae`; caller `0x00471c00 -> sub_471C00`, size `0x1cd`.
- Current `func_profile` for `sub_4777A0` reports 902 instructions, 100 basic blocks, 21 callees, 285 constants, zero string refs, one caller, and prototype `int __thiscall(int this, int, const unsigned __int16 *, int *, int)`.
- IDA MCP function inventory on 2026-06-03 reports the only direct caller as `0x00471d4a` in `sub_471C00`, the path that validates the incoming form payload before constructing this dialog.
- Current `xrefs_to` in session `73c77998` reports the only direct constructor xref as `0x00471d4a` in `sub_471C00`; no xrefs to `0x004781e5` or `0x004781f0`.
- IDA data references inside the constructor include `??_7NewPredefinedFormArticleDialog@@6B@` at `0x00614050`, secondary/tertiary vtable tables at `0x006140b4` and `0x006140e4`, `byte_66DA97`, and `dword_67A748`. Current `xrefs_to` confirms the vtable refs at constructor addresses `0x004777ee`, `0x004777f4`, and `0x004777fe`.
- 2026-06-21 B011 source-routing sync ties those vtable writes to the exact destructor-thunk layout: `0x004777ee` stores the primary table `0x00614050` at `this+0x00` and that table's first slot is [UID:0000ZS][0x0047ec30-0x0047ec6b.NewPredefinedFormArticleDialogScalarDeletingDestructor](by-memory/0x0047ec30-0x0047ec6b.NewPredefinedFormArticleDialogScalarDeletingDestructor.md); `0x004777f4` stores secondary table `0x006140b4` at `this+0xa0` and its destructor slot points to [UID:0000ZQ][0x0047e931-0x0047e947.NewPredefinedFormArticleDialogAdjustorThunks](by-memory/0x0047e931-0x0047e947.NewPredefinedFormArticleDialogAdjustorThunks.md) thunk `0x0047e931`; `0x004777fe` stores tertiary table `0x006140e4` at `this+0xa4` and its destructor slot points to thunk `0x0047e93c`. The `0xa0`/`0xa4` offsets are therefore class-layout subobject offsets that generate compiler adjustor glue, not source-authored pointer arithmetic inside this constructor.
- Current call inventory confirms parser calls at `0x00477848` and `0x00477d0c` to `sub_478650`, plus repeated dynamic-control/layout calls through UI/control helpers. The full callee set includes `sub_4A1400`, `sub_516030`, `sub_478650`, `sub_4B78D0`, `sub_4F4AA0`, `sub_495BF0`, `sub_49DC10`, `sub_499030`, `sub_498920`, `sub_498BE0`, `sub_498BD0`, `sub_4BA9A0`, `sub_4B7C50`, `sub_49DDD0`, `sub_49FC00`, `sub_544BD0`, and compiler/cleanup helpers.
- [UID:0000UE][DeserializePredefinedFormArticleEntries_478650](by-item/DeserializePredefinedFormArticleEntries_478650.md) and the aggregate parent document the 0x40c-byte parsed entry model that this constructor consumes.
- B014's 2026-07-03 parser callback raises [UID:0002UP][0x00478650-0x00478933.DeserializePredefinedFormArticleEntries](by-memory/0x00478650-0x00478933.DeserializePredefinedFormArticleEntries.md) and [UID:0000UE][DeserializePredefinedFormArticleEntries_478650](by-item/DeserializePredefinedFormArticleEntries_478650.md) to `90/92`, with formal file-local parser C++ and explicit `PredefinedFormArticleEntry` fields for `this+0x278` records. Current MCP command evidence confirms the two constructor calls, no extra parser callers, the input/output pointer signature, `0x40c` stride, `empty` handling, visible-row output, and no recovered named IDA `Predefined*` type.
- B005's 2026-07-04 current session confirms the type-negative blocker independently: `type_query` for `Predefined`, `NewPredefined`, and `FormArticle` returns no local types, and `search_structs` for `Predefined` / `NewPredefined` returns no structs.
- Current `find_bytes` negative checks found no raw pointer pattern for constructor start `A0 77 47 00`, constructor end `E5 81 47 00`, or raw helper start `F0 81 47 00`. Positive controls found sibling pointer pattern `40 82 47 00` at `0x00614098` and constructor-local vtable pointer immediates at `0x004777f0`, `0x004777fa`, and `0x00477804`.
- Current generated `auto-generated/NexusTK/ui/dialogs/NewPredefinedFormArticleDialog.cpp` header shows `validator-command-id: 000000006120` and `validator-refreshed-at: 2026-07-04T05:14:28-04:00`; the UID0002UJ line is an `Empty Emitter Marker`. This is the concrete defect caused by the old nonblank emitter with blank formal C++.
- 2026-06-13 IDA MCP `analyze_function` on `0x004777a0` reports `sub_4777A0` size `0xa45`, 902 instructions, 100 basic blocks, cyclomatic complexity 12, one caller `sub_471C00`, and callees limited to dialog/session setup, rectangle/control creation, parser `sub_478650`, text/control measurement helpers, edit-control buffer setup, stack-cookie support, and allocation cleanup handlers.
- 2026-06-13 IDA MCP `disasm` confirms constructor prologue SEH/stack-cookie setup, `this` in `ecx`, vtable stores at `0x004777ee`, `0x004777f4`, and `0x004777fe`, parser-call branches at `0x00477848` and `0x00477d0c`, the two dynamic row loops at `0x00477ab0` and `0x00477f70`, final dialog setup at `0x00478195-0x004781c0`, and `retn 0x10` at `0x004781e2`.
- 2026-06-13 IDA MCP `analyze_function` on caller `0x00471c00` confirms the allocation/constructor sequence: `sub_4F4AA0(0x5368)` at `0x00471d1f`, constructor call `sub_4777A0(v6, this, Destination, v10, a2)` at `0x00471d4a`, and ownership handoff through `sub_4A0FC0`.
- 2026-06-13 IDA MCP `analyze_component` over constructor/parser/button/paint/submit siblings reports the constructor as the class interface function and confirms the only internal edge from this constructor to the parser helper `0x00478650`; shared globals with related siblings are `word_60DB20`, `byte_66DA97`, `dword_67A748`, and compiler cookie state.

## No-Code Proof And Rejected Alternatives

UID0002UJ is source-authored and reconstructable, but it should not emit formal C++ in this pass. Current MCP session `73c77998` confirms a large constructor body: 902 instructions, 100 basic blocks, 21 callees, 285 constants, two `byte_66DA97` layout branches, parser output at `this+0x274`, `this+0x276`, and `this+0x278`, repeated dynamic static/edit control construction, title measurement, edit buffer-limit calls, and final dialog open/position calls. A source-quality constructor would need named UI/control helper signatures, member fields for the dynamic controls, exact constructor argument declarations, and broader class-layout declarations that are not currently recovered.

Current type checks found no local IDA `Predefined`, `NewPredefined`, or `FormArticle` type/struct. The source-ready parser child proves the `PredefinedFormArticleEntry` row layout and removes parser uncertainty as a blocker, but it does not supply the constructor's dynamic control/helper declarations. Writing constructor C++ now would require either decompiler-shaped code full of raw `sub_` calls, offsets, and temporaries, or invented helper/member APIs. Both would be worse than a documented blank formal block.

Rejected alternatives:

| Alternative | Decision |
| --- | --- |
| Keep `EMITTER_UIDS:000098` while formal C++ is blank | Rejected. Current generated output turns this into a UID0002UJ `Empty Emitter Marker`. |
| Insert a decompiler-shaped constructor | Rejected. The function is too large and helper/member/type names are not source-quality. |
| Mark the target nonreconstructable, padding, data, or compiler glue | Rejected. It is a modeled constructor with caller, vtable writes, parser calls, dynamic UI setup, and `this` return. |
| Reparent directly to the file root [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md) | Rejected. The file root remains the source route, but the function is a class constructor owned by [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md). |
| Treat generated placeholder constructor source as authority | Rejected. Generated source is stale/polluted evidence; direct MCP and by-* docs are authoritative. |

## Open Questions

- Final names for the dynamic-control fields and branch-local helpers are not source-quality.
- Exact project types for the caller's `session`, subject, rectangle, packet buffer, static/control creation helpers, and edit-control helpers remain provisional.
- The generated placeholder/empty-emitter constructor state is historical evidence of the defect, not source authority. Formal C++ remains blank because a correct constructor body would need source-quality dynamic UI helper names, control field names, exact helper signatures, and project types. The old high-threshold gate is not a code-entry requirement and does not apply to the separate [UID:0002EK][0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper](by-memory/0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper.md) child, which owns its own method body.

## Score Rationale

Completion is raised to `88` because the page now records exact current-session range/boundary evidence, direct class/file parent chain, caller allocation and argument roles, parser call sites, major branch structure, record offsets, dynamic row loop behavior, terminal dialog setup, callee inventory, generated empty-emitter defect, and a target-specific no-code proof. It remains below source-ready scores because helper names, field names, exact C++ types, and final formal constructor code are not complete.

Confidence is raised to `91` because fresh IDA MCP session `73c77998` independently confirms the sole caller, function size, vtable writes, parser edges, branch structure, row-record offsets, terminal setup, boundary padding, type-negative evidence, and sibling ownership. It remains below source-final confidence because the dynamic UI helper names and exact field/type names still depend on decompiler-style evidence.

## Cross-References

- [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md)
- [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md)
- [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md)
- [UID:0000UE][DeserializePredefinedFormArticleEntries_478650](by-item/DeserializePredefinedFormArticleEntries_478650.md)
- [UID:0002EK][0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper](by-memory/0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper.md)

## Changes

- 2026-08-14 B004 UID0000LT implementation callback: raised `88/91` to `93/94`, restored emitter [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), and inserted the accepted exact constructor source. The complete body preserves the sole caller's four arguments, `BulletinDialog(session, 5)`, free-text and parsed-form branches, current/legacy geometry, control IDs and limits, naturally padded row layout, exact `0x5368` object end, final pending/hover/bounds calls, and all side-effect order. Earlier blank-source conclusions are retained as superseded historical investigation; no current blocker requires decompiler labels or omission.

- 2026-07-04 B005 implementation callback: raised `COMPLETION:88` and `CONFIDENCE:91`, kept `CANONICAL_OWNER:000098` and `RECONSTRUCTABLE:TRUE`, cleared `EMITTER_UIDS`, and kept formal C++ blank. Added current MCP session `73c77998` evidence for health, function size `0xa45` / 2629, caller/allocation/argument roles, vtable writes, parser calls, callee/dynamic-control helper inventory, type/struct negative checks, boundary padding, raw pointer-pattern negatives, and the current generated UID0002UJ empty-emitter marker. The accepted disposition is reconstructable source-owned constructor with no formal C++ until dynamic UI helper/member/type declarations are source-quality; nonblank emitter with blank formal C++, decompiler-shaped constructor C++, nonreconstructable/padding/data classification, file-root direct ownership, and generated placeholder source are rejected.
- 2026-06-25 B002 support sync: replaced stale high-threshold gate wording with current constructor-specific no-code rationale. This constructor remains blank because dynamic UI/control/helper/type names are not source-quality yet; the adjacent [UID:0002EK][0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper](by-memory/0x004781f0-0x00478238.NewPredefinedFormArticleDialogRawNavigateSelectionHelper.md) is a separate exact child that now emits `NavigateToPreviousArticleSelection()`.
- 2026-07-03 B014 UID0002UP parser support sync: added the current MCP-backed confirmation that the two parser call sites `0x00477848` and `0x00477d0c` still target the exact file-local parser and that [UID:0002UP][0x00478650-0x00478933.DeserializePredefinedFormArticleEntries](by-memory/0x00478650-0x00478933.DeserializePredefinedFormArticleEntries.md) is now source-ready at `90/92` through the inferred `PredefinedFormArticleEntry` declaration. Constructor score/metadata and blank formal C++ are unchanged because dynamic UI/control/helper/type names remain constructor-local blockers.
- 2026-06-21 B011 support sync: added the exact primary/secondary/tertiary vtable-store mapping for `0x004777ee`, `0x004777f4`, and `0x004777fe`, including the destructor slot targets `0x0047ec30`, `0x0047e931`, and `0x0047e93c`; score unchanged because this reinforces existing constructor evidence and does not resolve final dynamic-control field/helper names.
- 2026-06-13 A001 Goal 2 repair: raised `76/84` to `86/88` and changed `AUTOGEN_PARENT_UID` from file [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md) to direct class parent [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md) after the class/file chain already cleared `85/85`. Added fresh IDA MCP evidence for the caller allocation/argument sequence, constructor size and instruction count, parser call sites, row-record offsets, non-free-text/free-text control branches, final dialog setup, and remaining source-quality blockers. Final C++ remains blank.
