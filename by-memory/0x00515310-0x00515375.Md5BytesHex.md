*** UID:0001B7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000L6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0001B6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *Md5BytesHex(unsigned char *bytes, unsigned int byteCount)
{
    MD5 md5;

    md5.Update(bytes, byteCount);
    md5.Final();
    return Md5DigestToHexString(md5.digest);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *Md5BytesHex(unsigned char *bytes, unsigned int byteCount);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact raw MD5 bytes-to-hex wrapper using class MD5 and unsigned-byte input; complete 0x65 body, protected 6/11-byte fences, update/final/hex calls, no direct route, and exact UID0001B6 child placement are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00515310-0x00515375 - Md5BytesHex

## Status

- Confidence: very strong for behavior, exact boundary, class layout, formal source, and MD5 ownership; strong for inferred original helper spelling despite retained no-route evidence.
- Proposed owner: [UID:0000L6][MD5](by-file/MD5.md) in `util/MD5.cpp`
- Parent range: [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md)
- Evidence basis: IDA MCP lookup/xref/decompile/disassembly/byte/signature/string checks, modeled sibling decompilation, read-only PE route scanning, and existing MD5 documentation through B007 session `80de0a67`.
- Reconstruction status: exact child of [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md) at position `20`, using class [UID:0004X5][MD5](by-class/MD5.md). The raw start still had no function object or direct call/pointer route at report time; complete boundaries and family behavior make that a liveness cap rather than a source blocker.

## 2026-07-26 B001 Class And Source Repair

The accepted source signature is `char *Md5BytesHex(unsigned char *bytes, unsigned int byteCount)`. The wrapper constructs a local `MD5`, whose inline constructor calls `Init`, then calls `Update`, `Final`, and `Md5DigestToHexString(md5.digest)`. This replaces the historical `MD5Context` placeholder and manual state/count initialization without altering the observed inlined initialization or return ownership.

Class `MD5` has exact size `0x68`, with `state`, `count`, `buffer`, and `digest` at `+0x00`, `+0x10`, `+0x18`, and `+0x58`. The unsigned-byte signature now matches the class methods and adjacent wrappers. The exact body remains `[0x00515310,0x00515375)`, with six `0xcc` bytes before it and eleven after it; neither fence is part of the function.

No direct xref, VA/RVA pointer, or rel32 route reaches this raw start. Positive-control scans recover modeled MD5 calls, and raw call sites inside the body resolve `Update`, `Final`, and digest formatting. The no-route fact is preserved, but the complete unique body and source-family fit justify emitted source.

## Boundary

This is a function-shaped raw helper that current IDA does not materialize as a function object.

| Range | Status | Role |
| --- | --- | --- |
| `0x0051530a-0x00515310` | alignment gap | Six `0xcc` bytes after `Md5StringHex`; this is not part of the target. |
| `0x00515310-0x00515375` | raw code body | Hashes a byte buffer and returns a heap MD5 hex string; the final instruction is `retn` at `0x00515374`. |
| `0x00515375-0x00515380` | alignment gap | Pads up to [UID:0000V2][PacketDigest_00515380](by-item/PacketDigest_00515380.md) at `0x00515380`. |

## Behavior

The helper takes a byte pointer and byte count, initializes a stack MD5 context with the standard state constants, calls `Md5Update` at `0x00515690`, finalizes through `Md5Final` at `0x00515750`, then passes the 16-byte digest at the stack digest slot to `Md5DigestToHexString` at `0x005151f0`.

The result is a heap-allocated lowercase hex string, consistent with the other hex-returning MD5 wrappers.

The source-facing signature is now accepted as first-draft `char *Md5BytesHex(char *bytes, unsigned int byteCount)`. The raw body loads the source pointer from `[ebp+0x08]`, pushes the byte count from `[ebp+0x0c]`, and the modeled byte-buffer siblings `Md5BytesRaw` and `Md5BytesToBuffer` use the same decompiled `char *Src, unsigned int` argument shape. Stronger spellings such as `const char *`, `const void *`, or `unsigned char *` are semantically plausible but remain unproven stronger constness/signedness claims.

## Caller And Liveness Notes

IDA xrefs on 2026-05-25 show no direct code or data references to `0x00515310`. Keep the body documented anyway because the prologue, constants, update/final calls, digest formatting call, stack-cookie epilogue, and alignment before `0x00515380` make it a real compiled helper body.

The lack of a modeled function object is a data/modeling issue, not evidence that this bytes-to-hex wrapper belongs to a different module.

The missing IDA function object and missing route no longer keep target-level C++ blank. B007 applied the same retained raw/no-route policy accepted for sibling [UID:0001B8][0x00515450-0x00515568.Md5FilePathHex](by-memory/0x00515450-0x00515568.Md5FilePathHex.md): when a raw helper body is complete, unique, source-shaped, and owner-routed, no-route remains a liveness/confidence cap instead of a code-entry blocker.

## 2026-06-01 IDA MCP Evidence

- `lookup_funcs` reports `0x00515310` as not a function, while surrounding modeled functions remain `sub_515290` at `0x00515290` size `0x7a` and `sub_515380` at `0x00515380` size `0x5c`.
- `xrefs_to` and `callers` still show no direct references to `0x00515310`, which keeps liveness confidence below the modeled wrappers.
- Byte checks show `0x0051530a-0x00515310` as `0xcc` alignment, then a prologue at `0x00515310`, stack-cookie setup, standard MD5 initialization constants, calls to `0x00515690`, `0x00515750`, and `0x005151f0`, and `0xcc` alignment at `0x00515375-0x00515380`.
- Internal xrefs from this raw body are visible in target-side xref results as unmodeled-function call sites to `Md5Update`, `Md5Final`, and `Md5DigestToHexString`.

## 2026-06-05 Live IDA Recheck

- IDB: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- `lookup_funcs` still reports `0x00515310`, `0x00515374`, and `0x00515375` as not function objects. Neighboring modeled helpers remain `0x005151f0-0x00515284`, `0x00515290-0x0051530a`, `0x00515380-0x005153dc`, `0x005153e0-0x00515445`, `0x00515570-0x0051565b`, `0x00515690-0x00515744`, `0x00515750-0x00515831`, and `0x00515840-0x00515f48`.
- `xrefs_to` and `callers` for `0x00515310` are still empty. Target-side xrefs to the callee functions include this raw body's unmodeled call sites with `fn:null`, so the body is real but still not directly reached from a modeled function.
- Raw bytes confirm `0x00515300-0x00515309` is the tail of the preceding `Md5StringHex` epilogue, `0x0051530a-0x0051530f` is `0xcc` alignment, the raw prologue starts at `0x00515310`, the final `retn` is at `0x00515374`, and `0x00515375-0x0051537f` is `0xcc` alignment before [UID:0000V2][PacketDigest_00515380](by-item/PacketDigest_00515380.md).
- The raw instruction stream allocates a `0x70`-byte stack frame, saves the security cookie, pushes `[ebp+0x0c]` as byte count, loads `[ebp+0x08]` as the source byte pointer, uses `[ebp-0x70]` as the MD5 context, zeros the bit-count slot at `[ebp-0x60]`, writes the standard MD5 state constants, calls `Md5Update` at `0x0051534e -> 0x00515690`, calls `Md5Final` at `0x00515356 -> 0x00515750`, and passes the stack digest at `[ebp-0x18]` to `Md5DigestToHexString` at `0x0051535f -> 0x005151f0`.
- Outgoing refs from the raw body are limited to the security-cookie global `0x00672f24`, `Md5Update`, `Md5Final`, `Md5DigestToHexString`, and `@__security_check_cookie@4` at `0x005c772f`.
- Neighboring modeled wrapper decompilation matches the interpretation: `Md5StringHex` at `0x00515290` hashes `strlen(Src)` then calls the same hex formatter, `PacketDigest_00515380` hashes `(Src, length)` but returns raw stack digest bytes, and `Md5BytesToBuffer` at `0x005153e0` copies the finalized 16-byte digest to a caller output buffer. This raw helper is the byte-buffer-to-heap-hex variant between those two modeled wrappers.

## 2026-06-15 Live IDA Refresh

- Live IDA MCP against `NexusTK.exe.i64` reconfirms `0x00515310` and `0x00515375` are still not function objects, while the successor [UID:0000V2][PacketDigest_00515380](by-item/PacketDigest_00515380.md) remains a modeled `0x5c` / 92-byte function at `0x00515380-0x005153dc`.
- `xrefs_to 0x00515310` is still empty. Target-side xrefs into MD5 helpers continue to show this raw body as `fn:null`: `Md5Update` at `0x0051534e`, `Md5Final` at `0x00515356`, and `Md5DigestToHexString` at `0x0051535f`.
- Raw bytes reconfirm `0x0051530a-0x00515310` and `0x00515375-0x00515380` are `0xcc` padding, and the `0x65` / 101-byte raw body starts with a `0x70` / 112-byte stack frame, initializes the standard MD5 constants, calls update/final/hex formatter, runs the security-cookie check, and returns at `0x00515374`. Decimal sizes were verified with `int_convert.py`.

## 2026-06-26 B007 Source-Ready MCP Evidence

- IDA MCP session `80de0a67` was health-ready for module `NexusTK.exe` from IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, with auto-analysis, Hex-Rays, and strings ready.
- `lookup_funcs` reconfirmed `0x00515310`, `0x00515374`, and `0x00515375` are not function objects. Immediate modeled neighbors remain `sub_5151F0` at `0x005151f0` size `0x94` = 148 bytes (Verified with `tools/int_convert.py`), `sub_515290` at `0x00515290` size `0x7a` = 122 bytes (Verified with `tools/int_convert.py`), `sub_515380` at `0x00515380` size `0x5c` = 92 bytes (Verified with `tools/int_convert.py`), and `sub_5153E0` at `0x005153e0` size `0x65` = 101 bytes (Verified with `tools/int_convert.py`).
- `get_bytes` and `insn_query` reconfirmed `0x00515300-0x00515309` as the tail of `Md5StringHex`, `0x0051530a-0x00515310` as six `0xcc` bytes, the target raw body at `0x00515310-0x00515375`, `retn` at `0x00515374`, and `0x00515375-0x00515380` as eleven `0xcc` bytes, `0x0b` = 11 (Verified with `tools/int_convert.py`), before `PacketDigest_00515380`.
- `make_signature_for_range 0x00515310-0x00515375` returned a unique body signature. `make_signature_for_range 0x00515375-0x00515380` returned `CC CC CC CC CC CC CC CC CC CC CC`, proving the post-body bytes are padding.
- The target stack frame is `0x70` = 112 bytes (Verified with `tools/int_convert.py`). The MD5 bit-count pair is cleared at context offset `0x10` = 16 bytes (Verified with `tools/int_convert.py`), the staging buffer begins at context offset `0x18` = 24 bytes (Verified with `tools/int_convert.py`) in `Md5Update`, full update blocks are `0x40` = 64 bytes (Verified with `tools/int_convert.py`), and the final digest passed to the hex formatter is at context offset `0x58` = 88 bytes (Verified with `tools/int_convert.py`).
- The raw instruction stream writes the standard MD5 state constants `0x67452301`, `0xefcdab89`, `0x98badcfe`, and `0x10325476`, pushes `[ebp+0x0c]` as `byteCount`, pushes `[ebp+0x08]` as `bytes`, calls `Md5Update` at `0x0051534e`, calls `Md5Final` at `0x00515356`, and calls `Md5DigestToHexString` at `0x0051535f` with `&context.digest`.
- `decompile 0x00515310` still fails because there is no function object. That is an IDA modeling limitation rather than a behavior blocker.
- `xrefs_to 0x00515310` remains empty; `xrefs_to 0x00515374` has only the local epilogue reference at `0x00515373`; `xrefs_to 0x00515375` remains empty. Target-side xrefs into `Md5Update`, `Md5Final`, and `Md5DigestToHexString` still show the raw target body as `fn:null` at `0x0051534e`, `0x00515356`, and `0x0051535f`.
- Modeled siblings prove the source signature and return convention: `Md5StringHex` decompiles as `char *__cdecl(char *Src)` and returns `Md5DigestToHexString`; `Md5BytesRaw` decompiles as `char *__cdecl(char *Src, unsigned int)`; `Md5BytesToBuffer` decompiles as `void *__cdecl(char *Src, unsigned int, _OWORD *)`; and `Md5StreamHex` decompiles as `char *__cdecl(FILE *Stream)` returning the same formatter output after chunked updates.
- `Md5DigestToHexString` formats 16 digest bytes with `%02x`, appends into a local buffer, and returns `_strdup(Source)`. `get_string` reconfirmed `%02x` at `0x0061ead4`, fallback literal `"000011112222333344445555666677778888"` at `0x0061eadc`, and `"rb"` at `0x0060df9c`. This target reaches `%02x` only through the formatter and does not reference the fallback literal.
- `Md5Update` decompiles as a `__thiscall` context helper with source pointer and unsigned byte count, and `Md5Final` decompiles as a `__thiscall` finalizer that references the `PADDING` block at `0x0066ddd8`. The source-facing `MD5Context`, `state`, `count`, `digest`, `Update`, and `Final` names are inferred names matching the accepted [UID:0001B8][0x00515450-0x00515568.Md5FilePathHex](by-memory/0x00515450-0x00515568.Md5FilePathHex.md) style, not original-source proof.
- A read-only PE scan found no absolute VA pointer, RVA pointer, direct `E8` call, or direct `E9` jump route to `0x00515310`. The same scan found expected direct routes to `0x00515380` at `0x00578da5`, `0x00515570` at `0x00558278`, `0x005151f0` at `0x005152f3`/`0x0051535f`/`0x00515540`/`0x00515630`, `0x00515690` including `0x0051534e`, and `0x00515750` including `0x00515356`. The no-route result is therefore meaningful negative evidence, not an unperformed check.

## Source Shape And Rejected Alternatives

The accepted source-facing helper is `char *Md5BytesHex(char *bytes, unsigned int byteCount)`. This replaces the prior unresolved `const void *`-style note. `char *` is intentionally conservative because it matches the decompiled `Md5BytesRaw`, `Md5BytesToBuffer`, and `Md5Update` family. `const`, `void *`, and `unsigned char *` remain plausible but unproven stronger spellings.

Security-cookie setup and checking are compiler artifacts and are intentionally omitted from the formal C++ block.

Rejected owners:

- Not `PacketTransform` and not `Socket`: packet code calls `0x00515380`, the raw digest helper, and current MCP/PE evidence finds no route from packet code to this heap-hex helper.
- Not `RegistryConfig`: registry uses `0x005153e0`, the bytes-to-caller-buffer helper.
- Not `StartupWindow`: startup callers use `Md5StringHex` at `0x00515290`.
- Not `ScreenshotCapture`: screenshot proof uses `Md5StreamHex` at `0x00515570`.
- Not the `%02x` or fallback-literal data pages: this target is executable code and has no direct fallback-literal xref.
- Not no-owner/non-emitting: the helper has a valid [UID:0000L6][MD5](by-file/MD5.md) owner/emitter route and now clears the active code-entry gate.

Rejected splits and merges:

- Do not merge `0x0051530a-0x00515310`; it is six-byte `0xcc` alignment after `Md5StringHex`.
- Do not extend through `0x00515380`; `0x00515375-0x00515380` is eleven-byte `0xcc` padding and `0x00515380` starts the modeled packet-facing raw digest helper.
- Do not merge with `0x00515380-0x005153dc`; that helper returns raw digest bytes for `Socket::BuildEncryptedPacket` and has one direct caller.
- Do not merge with `0x005153e0-0x00515445`; that helper writes a digest to caller-provided storage.
- Do not merge with [UID:0001B8][0x00515450-0x00515568.Md5FilePathHex](by-memory/0x00515450-0x00515568.Md5FilePathHex.md); that raw helper opens a file path and hashes file contents.
- Do not extend backward into `0x00515290-0x0051530a`; that modeled wrapper hashes NUL-terminated strings by `strlen(Src)`.

## Reconstruction Notes

- Marked reconstructable because the body, boundaries, MD5 role, and parent ownership are clear.
- Formal first-draft C++ is now present in the managed block. The code uses the accepted source-facing `Md5BytesHex(char *bytes, unsigned int byteCount)` signature, inferred `MD5Context` object, inline MD5 state/count initialization, `context.Update`, `context.Final`, and `Md5DigestToHexString(context.digest)`.
- The missing IDA function object and no-current-caller/no-pointer route are preserved as confidence/liveness caps. They no longer block target-level emission because the raw body covers a complete unique function-shaped source range and the owner/emitter route through [UID:0000L6][MD5](by-file/MD5.md) is stable.
- Historical note: `MD5Context` and explicit `state`/`count` setup were earlier descriptive reconstruction choices. The accepted class-level evidence supersedes them with `MD5 md5`; exact original wrapper spelling remains inferred and continues to cap confidence below 95.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 89 | The page now has current live IDA-backed raw bytes, exact `0x65` = 101-byte body (Verified with `tools/int_convert.py`), padding before and after the body, MD5 context setup, raw call sites to `Md5Update` / `Md5Final` / `Md5DigestToHexString`, modeled sibling comparison, source-facing signature decision, rejected split/owner alternatives, PE route-scan negative evidence, direct MD5 owner route, and formal first-draft C++. |
| Confidence | 91 | MD5 role, utility ownership, boundary, source shape, and emitted behavior are strong from MCP bytes/xrefs/sibling decompilation plus PE negative checks. Confidence remains capped because this start is still not an IDA function object, has no direct caller or pointer route, and exact original type/member spellings are inferred. |

## Cross-References

- [UID:0000L6][MD5](by-file/MD5.md)
- [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md)
- [UID:0000V2][PacketDigest_00515380](by-item/PacketDigest_00515380.md)

## Changes

- 2026-07-26 B001 UID0001B6 callback:
  - Raised `89/91 -> 91/92`, rerouted the exact child through UID0001B6 at position `20`, replaced `MD5Context` and manual initialization with class `MD5`, changed the source input to `unsigned char *`, added the exact H declaration, and preserved all raw/no-route and boundary history.

- 2026-06-26 B007 accepted implementation:
  - What existed before: `COMPLETION:85`, `CONFIDENCE:88`, blank formal C++, and stale wording that kept C++ blank because the raw helper had no IDA function object, no direct route, and only a `const void *`-style role inference.
  - Changed to: `COMPLETION:89`, `CONFIDENCE:91`, owner/emitter/reconstructable fields unchanged, and formal first-draft `Md5BytesHex(char *bytes, unsigned int byteCount)` C++ added to the managed reconstruction block.
  - Summary/evidence: B007 MCP session `80de0a67` reconfirmed the health-ready NexusTK IDB, no function object at `0x00515310`/`0x00515374`/`0x00515375`, modeled neighbors, exact `0x00515310-0x00515375` raw body, unique body signature, six-byte pre-padding, eleven-byte post-padding, `0x70` stack frame, standard MD5 state constants, bit-count zeroing at context offset `0x10`, digest slot at context offset `0x58`, raw call sites to `Md5Update`, `Md5Final`, and `Md5DigestToHexString`, `char *Src, unsigned int` sibling signature support, and no raw-start xrefs. A read-only PE scan found no VA/RVA pointer or direct rel32 route to `0x00515310` while finding expected routes to modeled/live sibling helpers. The no-route state remains a liveness/confidence cap rather than a target-level C++ blocker.

- 2026-06-15 live IDA refresh:
  - Changed to: `COMPLETION:85`, `CONFIDENCE:88`.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `xrefs_to`, and `get_bytes` reconfirmed the raw no-function/no-caller status, exact `0x00515310-0x00515375` bytes, MD5 helper target-side xrefs, and padding before [UID:0000V2][PacketDigest_00515380](by-item/PacketDigest_00515380.md). `int_convert.py` verified `0x65 == 101` and `0x70 == 112`. Owner/emitter routing remains [UID:0000L6][MD5](by-file/MD5.md), and final C++ remains blank because the raw helper signature and liveness are not source-quality.

- 2026-06-01 IDA MCP rescore:
  - What existed before: `COMPLETION:50`, `CONFIDENCE:65`, reconstructable blank, and no autogen parent.
  - Changed to: `COMPLETION:72`, `CONFIDENCE:82`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000L6`.
  - Summary/evidence: byte checks confirm a complete function-shaped MD5 bytes-to-hex wrapper bounded by `0xcc` padding, while lookup/caller checks confirm the current database still lacks a function object and direct callers.
- 2026-06-05 live IDA recheck:
  - What existed before: `COMPLETION:72`, `CONFIDENCE:82`, with behavior and boundary known but no current instruction/callee inventory or neighboring-wrapper comparison written into the page.
  - Changed to: `COMPLETION:78`, `CONFIDENCE:86`; reconstructable status, parent UID, and blank C++ block are retained.
  - Summary/evidence: live IDA reconfirmed the no-function/no-caller caveat, exact prologue/return/padding bytes, stack-frame layout, MD5 constants, raw call sites to `Md5Update`, `Md5Final`, and `Md5DigestToHexString`, security-cookie refs, and the relationship to neighboring modeled string/raw-digest/output-buffer wrappers.
