*** UID:0004U1 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ON | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5839392 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@00591A20 | UID0000OJ accepted formal channel
void GetScrapFromClipboard(TextEditScrap *scrap)
{
    wchar_t *text = 0;
    int textLength = 0;
    void *payload = 0;
    int payloadSize = 0;

    scrap->Clear();

    bool hasStyledText =
        IsClipboardFormatAvailable(s_styledUnicodeClipboardFormat) != 0;

    OpenClipboard(g_pApplication->GetMainWindowHandle());

    if (hasStyledText)
    {
        HGLOBAL clipboardBlock =
            GetClipboardData(s_styledUnicodeClipboardFormat);
        const unsigned char *clipboardData =
            static_cast<const unsigned char *>(GlobalLock(clipboardBlock));

        textLength = *reinterpret_cast<const int *>(clipboardData);
        clipboardData += sizeof(int);
        text = static_cast<wchar_t *>(
            GetMemoryMan()->AllocateBufferMemory(
                textLength * sizeof(wchar_t)));
        GetMemoryMan()->MemmoveWrapper(
            text, clipboardData, textLength * sizeof(wchar_t));
        clipboardData += textLength * sizeof(wchar_t);

        payloadSize = *reinterpret_cast<const int *>(clipboardData);
        clipboardData += sizeof(int);
        payload = GetMemoryMan()->AllocateBufferMemory(payloadSize);
        GetMemoryMan()->MemmoveWrapper(payload, clipboardData, payloadSize);

        GlobalUnlock(clipboardBlock);
    }
    else
    {
        HGLOBAL clipboardBlock = GetClipboardData(CF_TEXT);
        const char *clipboardText =
            static_cast<const char *>(GlobalLock(clipboardBlock));
        int clipboardLength = strlen(clipboardText);

        textLength = MultiByteToWideChar(
            CP_ACP, 0, clipboardText, clipboardLength, 0, 0);
        text = static_cast<wchar_t *>(
            GetMemoryMan()->AllocateBufferMemory(
                textLength * sizeof(wchar_t)));
        MultiByteToWideChar(
            CP_ACP, 0, clipboardText, clipboardLength, text, textLength);

        GlobalUnlock(clipboardBlock);
    }

    for (int index = 0; index < textLength; ++index)
    {
        if (text[index] == L'\n')
        {
            GetMemoryMan()->MoveBufferMemory(
                text + index,
                text + index + 1,
                (textLength - index) * sizeof(wchar_t));
            --textLength;
            --index;
        }
    }

    if (textLength > 0)
    {
        scrap->m_textLength = textLength;
        scrap->m_text = static_cast<wchar_t *>(
            GetMemoryMan()->AllocateBufferMemory(
                textLength * sizeof(wchar_t)));
        GetMemoryMan()->MemmoveWrapper(
            scrap->m_text, text, textLength * sizeof(wchar_t));
    }

    if (payload != 0 && payloadSize > 0)
    {
        scrap->m_payloadSize = payloadSize;
        scrap->m_payload =
            GetMemoryMan()->AllocateBufferMemory(payloadSize);
        GetMemoryMan()->MemmoveWrapper(
            scrap->m_payload, payload, payloadSize);
    }

    GetMemoryMan()->FreeBufferMemory(text);
    if (payload != 0)
        GetMemoryMan()->FreeBufferMemory(payload);

    CloseClipboard();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready TextEditPane.cpp file-local GetScrapFromClipboard helper at exact [0x00591a20,0x00591c65): clears TextEditScrap, reads styled Unicode or CF_TEXT clipboard data, removes LF units, copies text/payload through MemoryMan, preserves unchecked edge behavior, and remains distinct from UID0004T8 publisher. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00591a20-0x00591c65 GetScrapFromClipboard

## Status And Source Placement

- Exact range: `[0x00591a20,0x00591c65)`, 581 bytes.
- Entity: ordinary source-authored file-local clipboard reader in [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Canonical owner/emitter: UID0000ON. It is not a TextEditPane member and does not emit through class UID0000EO.
- The paired publisher is [UID:0004T8][0x005917d0-0x00591a1c.PutScrapToClipboard](by-memory/0x005917d0-0x00591a1c.PutScrapToClipboard.md). Four bytes of compiler alignment separate the functions; they remain distinct source helpers.
- [UID:0001JO][0x00591300-0x005954b3.TextEditPaneAndTextBoxPaneNoncontiguousAuditMap](by-memory/0x00591300-0x005954b3.TextEditPaneAndTextBoxPaneNoncontiguousAuditMap.md) remains a non-emitting historical map and does not own this body.

## Exact Binary And ABI Evidence

- Exact size is `0x245`; bounded MCP decoding found 210 instructions, 23 basic blocks, cyclomatic complexity 12, and one direct call from [UID:00030F][0x005909f0-0x00590dc5.TextEditPaneOnKeyEvent](by-memory/0x005909f0-0x00590dc5.TextEditPaneOnKeyEvent.md).
- Exact range SHA256: `2B0089BCAEF89BAAB5E401BDA0A26D8A53E5DE3E0ED9F2F5DD259B50E35BA3ED`.
- The one explicit pointer argument is a `TextEditScrap *`. Direct writes cover its text pointer/length and payload pointer/size after an initial `Clear()` call.
- The apparent tail BOOL is the unconsumed return from `CloseClipboard`; the only caller ignores it. Source return is `void`, not `BOOL` or `bool`.
- Predecessor `[0x00591a1c,0x00591a20)` is four `0xcc` bytes. Successor `[0x00591c65,0x00591c70)` is eleven `0xcc` bytes with SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`. Neither padding range belongs to this helper.

## Styled Unicode Route

- Test the registered styled-Unicode clipboard format before opening the clipboard. Preserve that availability result rather than retesting after open.
- Open the clipboard using `g_pApplication->GetMainWindowHandle()` with no source-level success guard.
- Retrieve and lock the custom block, read a leading signed 32-bit UTF-16 code-unit count, copy exactly `count * sizeof(wchar_t)` bytes, then read a signed 32-bit payload size and copy that many opaque payload bytes.
- Preserve the packed layout without inventing NUL terminators, version tags, bounds validation, or malformed-block rejection.
- Unlock the exact custom HGLOBAL after both temporary copies are made.

## CF_TEXT Route

- When styled data is unavailable, retrieve and lock `CF_TEXT`, measure with `strlen`, and convert the non-NUL ACP byte span with `MultiByteToWideChar(CP_ACP, 0, ...)`.
- Allocate exactly the returned UTF-16 code-unit count and perform the second conversion into that buffer. No terminator is appended.
- Unlock the CF_TEXT HGLOBAL after conversion. The route produces no opaque formatting payload.

## Normalization And Commit Behavior

- Scan the temporary UTF-16 sequence in place and remove only `L'\n'`. Each removal shifts `(textLength - index)` code units, decrements both length and loop index, and therefore rechecks the compacted position.
- Do not remove `L'\r'`, normalize CRLF pairs, validate surrogate pairs, or apply line-ending hardening.
- Commit text only when the compacted length is positive: allocate a second exact-sized buffer, write `m_textLength`, and copy all code units.
- Commit payload only when both pointer and size are positive: allocate a second exact-sized buffer, write `m_payloadSize`, and copy all bytes.
- Free both temporary allocations through MemoryMan, conditionally for payload, and close the clipboard on the ordinary tail.

## TextEditScrap Access Contract

- Direct access to the four private stores is proven by the binary and is represented by a file-local friend declaration in [UID:0000EP][TextEditScrap](by-class/TextEditScrap.md).
- The helper must not be rewritten as a TextEditScrap member, public-field model, raw-offset accessor, or chain of setter calls. Those shapes change the exact clear/allocate/copy lifecycle.
- `TextEditScrap::Clear()` remains the source abstraction for clearing any prior owned buffers before clipboard reads.

## Negative And Rejected Evidence

- Rejected merge with UID0004T8: call direction, behavior, range, and the intervening alignment prove independent publisher and reader functions.
- Rejected `bool` return: no source caller consumes the API tail, and no branch computes an independent success value.
- Rejected defensive API checks: the observed source path does not guard failed OpenClipboard, missing handles, failed locks, malformed lengths, allocation failures, or conversion failures.
- Rejected ownership by TextEditPane class: no `this` receiver is present; only the scrap pointer is explicit.
- Rejected compiler-helper disposition: the body contains application-specific clipboard formats, TextEditScrap lifecycle, MemoryMan calls, LF policy, and an ordinary direct source call.

## Score Rationale And History

- `92/94` reflects exact bytes/range/hash, full CFG and caller route, argument and void-return ABI, publisher boundary, clipboard formats, packed data layout, normalization, ownership, complete human body, and padding exclusion.
- Confidence remains below 95 because the original file-local spelling and private friend declaration spelling are stripped. `GetScrapFromClipboard` is the strongest paired and behavior-backed source name.
- Earlier broad-family documents that treated this address as raw or as part of a noncontiguous TextBoxPane map are superseded for this exact body only; their unrelated range history remains preserved.

## Rebuild Policy

- Emit only the managed file-local source helper through UID0000ON.
- Let the compiler regenerate stack cleanup, EH/cookie paths, imported-call lowering, and incidental tail-register state.
- Do not emit raw addresses, decompiler labels, explicit ABI annotations, padding, HGLOBAL internals, or defensive behavior absent from the binary.

## UID0000OJ Whole-File Incorporation - 2026-08-16

The accepted source/global definition remains verbatim in this exact owner page and is assembled once through the consolidated UID0000EO translation-unit channel.

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination | Action | Gate-1 state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C0000OJ-150 | 0004U1 | Preserve generated modeled definition `[0x591a20,0x591c65)` GetScrapFromClipboard and its exact owner/order. | original-proof | read styled/CF_TEXT clipboard data into TextEditScrap; profile in/out 1/7; start-xrefs 1; first 0x590b8f/code. | UID0004U1 formal CPP/H and UID0000ON unit order | already-present | proposed |
