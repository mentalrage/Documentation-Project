*** UID:000209 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __cdecl Rgb565TransparentAlphaBlendMmxBlocks(
    const unsigned short* sourcePixels,
    int sourcePitchBytes,
    unsigned short* destinationPixels,
    int destinationPitchBytes,
    unsigned char sourceWeight32,
    int blockCount4Pixels,
    int rowCount)
{
    const unsigned int sourceWeight = sourceWeight32;
    const unsigned int destinationWeight = static_cast<unsigned char>(32 - sourceWeight32);

    while (rowCount-- > 0) {
        for (int pixelIndex = 0; pixelIndex < blockCount4Pixels * 4; ++pixelIndex) {
            const unsigned int source = sourcePixels[pixelIndex];
            if (source == 0) {
                continue;
            }

            const unsigned int destination = destinationPixels[pixelIndex];
            const unsigned int redBlue =
                (sourceWeight * (source & 0xf81f) +
                 destinationWeight * (destination & 0xf81f)) >> 5;
            const unsigned int green =
                (sourceWeight * (source & 0x07e0) +
                 destinationWeight * (destination & 0x07e0)) >> 5;

            destinationPixels[pixelIndex] = static_cast<unsigned short>(
                (redBlue & 0xf81f) | (green & 0x07e0));
        }

        sourcePixels = reinterpret_cast<const unsigned short*>(
            reinterpret_cast<const unsigned char*>(sourcePixels) + sourcePitchBytes);
        destinationPixels = reinterpret_cast<unsigned short*>(
            reinterpret_cast<unsigned char*>(destinationPixels) + destinationPitchBytes);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms exact `0x119`-byte MMX transparent weighted RGB565 block helper, raw span-body xref at `0x00460818`, no formal IDA caller because the parent span is unmodeled, no callees, caller `width / 4` block split plus scalar tail, row-pitch/block-count handling, weight scratch qwords, RGB565 masks, transparent-lane preservation, `emms`, and boundary padding; B013 adds behavior-preserving source-level C++ using helper-local weights and zero-source preservation. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004609e0-0x00460af9 RGB565 Transparent Alpha Blend MMX Blocks

## Status

- Disposition: reconstructable NexusTK render support code.
- Entity kind: free render helper.
- Likely source file: [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) `SoftwareBlend16`.
- Confidence: very strong for exact boundary, raw call-site xref, no-callee status, source-facing signature, weight scratch writes, RGB565 masks, transparent lane preservation, and SoftwareBlend16 ownership; final source spelling remains open only at the MMX inline-assembly/intrinsic source-form level.
- Rebuild handling: `source-authored`.
- C++ reconstruction: 2026-06-30 B013 populates formal source-level C++. The old family-wide blank-C++ blocker is superseded by accepted SoftwareBlend16 precedent for behavior-preserving C++ loop helpers when the exact MMX behavior is simple channel arithmetic.

## Address Range

`0x004609e0-0x00460af9` (`sub_4609E0`, size `0x119`, 89 decoded instructions).

The following `0x00460af9-0x00460b00` bytes are alignment padding.

## Behavior

This is the MMX four-pixel/block path for transparent weighted RGB565 blending. The raw parent span body computes `width / 4` blocks, passes the source pointer, source pitch, destination pointer, destination pitch, a source-side weight of `32 - alpha`, block count, and row count, then advances both pointers by `blocks * 8` bytes and uses a scalar tail for the remaining pixels.

The helper repeats its byte weight argument into `qword_67A810`; it computes `32 - weight` into `qword_67A818`. The qword loop blends source and destination RGB565 channels with masks `0x001f001f001f001f` and `0x07e007e007e007e0`, preserves destination lanes where the source word is zero, advances eight bytes per block, advances by source/destination row pitches per row, and finishes with `emms`.

## Source-Facing Signature And Source-Form Policy

The best source-facing name and prototype for this helper are:

```cpp
static void __cdecl Rgb565TransparentAlphaBlendMmxBlocks(
    const uint16_t* sourcePixels,
    int sourcePitchBytes,
    uint16_t* destinationPixels,
    int destinationPitchBytes,
    uint8_t sourceWeight32,
    int blockCount4Pixels,
    int rowCount);
```

The return type should be `void` even though decompiler artifacts may show an integer or pointer result; the raw caller discards the register result and uses this helper only for side effects. `sourceWeight32` is the caller's `32 - alpha` value, while this helper builds both source and inverse weight qwords in [UID:00028W][0x0067a7f0-0x0067a830.MmxBlendWeightScratchData](by-memory/0x0067a7f0-0x0067a830.MmxBlendWeightScratchData.md).

Formal C++ remains intentionally blank after the B001 source-quality pass. The helper clears the current numerical/emitter eligibility gate, but the binary shape strongly favors MSVC x86 inline assembly or assembly-backed local helpers: `pushaw`/`popaw`, explicit `emms`, qword MMX memory operands, and writable scratch qwords all need a coherent SoftwareBlend16-wide source-form decision. A behavior-equivalent portable C++ body would not be a defensible original-source representation for this optimized path.

## Rule 26 Incorporated B001 Research

The B001 source-quality report incorporated into this page treats the current descriptive name as the best source-facing direction, not merely a generated label. The raw parent [UID:000207][0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw](by-memory/0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw.md) computes a four-pixel block count from `width / 4`, passes `32 - alpha` as the source-side blend weight, and then advances both row pointers by `blockCount4Pixels * 8` before processing the scalar tail. That call pattern makes this helper the bulk transparent weighted RGB565 path rather than a generic alpha blender.

Ownership was rechecked against multiple alternatives. [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) remains the direct owner because the helper is stateless 16-bit render math in the contiguous SoftwareBlend16 cluster and uses SoftwareBlend16 scratch/constant data. Broader `Surface`, `GrafPort`, and render-callback owners are rejected because they consume the primitive through higher-level drawing paths but do not own the local arithmetic cluster. [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md) is not a candidate for this helper; it applies only as a consumer-side consideration for the byte-span helpers.

The B001 report also resolves the raw-caller caveat. `lookup_funcs 0x004607e0` still reports no IDA function while `xrefs_to 0x004609e0` finds the code xref at `0x00460818`; this is positive raw-parent evidence, not no-use evidence. The page should not lower ownership confidence simply because the parent span body remains raw/unmodeled.

Scratch and constants are part of the source-quality interpretation. [UID:00028W][0x0067a7f0-0x0067a830.MmxBlendWeightScratchData](by-memory/0x0067a7f0-0x0067a830.MmxBlendWeightScratchData.md) records the binary writable qwords used for source and inverse weights. A clean rebuilt source may choose local `__m64` temporaries if the behavior and source style are preserved, but the documentation must retain the binary fact that `qword_67A810` and `qword_67A818` are the observed storage. [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md) owns the RGB565 red/blue and green qword masks used by this helper.

Rejected source-form alternatives are now part of the target evidence. Hex-Rays `_m_*` expressions are an analysis representation and are not proof of original intrinsics. Pure portable C++ could express equivalent behavior, but it would hide the original optimized MMX path. Intrinsics-only source is possible but weaker than MSVC x86 inline assembly or assembly-backed static helpers because the binary explicitly preserves all general registers, uses `emms`, and relies on qword memory operands.

Generated-output impact: `auto-generated/NexusTK/render/SoftwareBlend16.cpp` is expected to remain empty for this helper until the whole MMX/block family has a coherent source-form pass. The blank formal C++ block is therefore an explicit source-quality decision, not an uninvestigated TODO.

## Reconstruction Evidence

| Item | Evidence | Reconstruction impact |
| --- | --- | --- |
| Parent helper | Direct internal call from `0x00460818` in [UID:000207][0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw](by-memory/0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw.md). | This is the MMX block path for the transparent weighted RGB565 span helper. |
| Raw caller caveat | IDA MCP `callers` reports no formal caller because `0x004607e0` is not an IDA function, while `xrefs_to` reports the real code xref at `0x00460818`. | Treat the caller as a raw body relationship, not as absence of use. |
| Caller split | The raw parent computes `width / 4`, calls this helper when positive, then advances source/destination by `blocks * 8` and runs the scalar transparent weighted tail. | Confirms this page covers only the bulk four-pixel path; scalar behavior remains in [UID:000207][0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw](by-memory/0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw.md). |
| Weight scratch | [UID:00028W][0x0067a7f0-0x0067a830.MmxBlendWeightScratchData](by-memory/0x0067a7f0-0x0067a830.MmxBlendWeightScratchData.md) maps `qword_67A810` and `qword_67A818` exclusively to this helper. | Rebuild may use local MMX temporaries, but behavior must preserve source/inverse weight expansion. |
| Component masks | Decompile evidence and [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md) tie RGB565 masks to this block family. | The helper is channel-wise RGB565 blending, not byte-wise alpha blending. |
| Transparency | IDA decompilation preserves destination lanes where source pixel is zero. | The transparent-source skip must survive any final intrinsic/loop rewrite. |
| Callees | IDA MCP `callees` reports an empty callee set for `0x004609e0`. | All behavior is inline MMX/channel math, scratch setup, and row stepping. |
| Constants and scratch refs | Live IDA reads masks `0x001f001f001f001f` at `0x00610e98` and `0x07e007e007e007e0` at `0x00610ea0`; data refs to both masks and to scratch qwords `0x0067a810`/`0x0067a818` are inside this helper. | Confirms the exact RGB565 channel masks and local-only scratch use. |
| Boundary | Function ends at `0x00460af9`; `0x00460af9-0x00460b00` is confirmed alignment padding in [UID:0000VN][-ignored](by-memory/-ignored.md), and the next function starts at `0x00460b00`. | Exact range is closed and attachable to SoftwareBlend16. |

## Evidence

- 2026-06-05 IDA MCP `lookup_funcs` reports `sub_4609E0` at `0x004609e0-0x00460af9`; `disasm` decodes 89 instructions.
- 2026-06-05 IDA MCP `xrefs_to` reports the real code xref from `0x00460818`; `callers` is empty because the call site is in raw span body `0x004607e0-0x0046093c`, which IDA still reports as `Not a function`.
- 2026-06-05 IDA MCP `callees` reports no callees for this helper.
- 2026-06-05 IDA disassembly shows the raw parent pushing row count, block count, `32 - alpha`, destination pitch, destination pointer, source pitch, and source pointer before `call sub_4609E0`, then advancing both pointers by `blocks * 8` and using the scalar transparent weighted tail for leftover pixels.
- 2026-06-05 IDA decompilation and disassembly show initialization of `qword_67A810` from the byte weight argument and `qword_67A818` from `32 - weight`, MMX component weighting, source-zero masking, row/pitch advancement, and `emms`.
- 2026-06-05 IDA `py_eval` confirms `0x00460af9-0x00460b00` is all `0xcc`, mask qwords `0x00610e98 = 0x001f001f001f001f` and `0x00610ea0 = 0x07e007e007e007e0`, and data refs from this helper to scratch qwords `0x0067a810`/`0x0067a818` and both masks.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 86 | B001 source-quality recheck resolves exact bounds, raw xref/caller caveat, no-callee status, caller block/tail split, source-facing signature, source/destination pointer and pitch order, `sourceWeight32`, block and row counts, weight setup, scratch qword refs, RGB565 masks, transparent lane preservation, row/pitch stepping, `emms`, SoftwareBlend16 ownership, and boundary padding. The remaining gap is the final SoftwareBlend16-wide MMX source form. |
| Confidence | 91 | Disassembly, decompilation, raw caller evidence, scratch/mask refs, parent SoftwareBlend16 grouping, sibling helper evidence, and adjacent padding agree. Confidence stays below final-source quality because the exact original spelling and inline-assembly/intrinsic form remain inferred. |
| Reconstructable | true | This is source-authored NexusTK software render code attached to SoftwareBlend16; 2026-06-30 B013 populates behavior-preserving formal C++ and treats the old MMX-family source-form blocker as superseded. |

## Cross-References

- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) SoftwareBlend16.
- [UID:000207][0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw](by-memory/0x004607e0-0x0046093c.Rgb565TransparentAlphaBlendSpanRaw.md)
- [UID:00028W][0x0067a7f0-0x0067a830.MmxBlendWeightScratchData](by-memory/0x0067a7f0-0x0067a830.MmxBlendWeightScratchData.md)
- [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md)

## Changes

- 2026-06-02: Raised from `50/85` to `74/86`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md).
  - Added reconstruction evidence for caller placement, MMX scratch globals, RGB565 component masks, transparent-lane behavior, and alignment-bounded range.
  - Left C++ blank because final inline assembly/intrinsic spelling and helper signature are not final-source quality.
- 2026-06-05: Raised from `74/86` to `82/90`.
  - Added live IDA evidence for exact function size, 89-instruction disassembly, raw call-site xref with no formal `callers` edge, no-callee status, caller block/tail split, weight expansion into `qword_67A810`/`qword_67A818`, RGB565 mask qwords, transparent-lane preservation, row/pitch stepping, `emms`, and post-function `0xcc` padding.
  - Kept final C++ blank because the final original signature and inline assembly/intrinsic spelling are not yet final-source quality.
- 2026-06-17 B001 source-quality execution: Raised from `82/90` to `86/91`.
  - Resolved the source-facing `Rgb565TransparentAlphaBlendMmxBlocks` prototype, argument roles, `void` return policy, raw-parent caller interpretation, scratch/global ownership, and SoftwareBlend16 ownership route.
  - Kept formal C++ blank because isolated portable or Hex-Rays-style output would not be a defensible original source form before the SoftwareBlend16-wide MMX inline-assembly/intrinsic decision.
  - Rule 26 remediation added the report's ownership alternatives, raw-caller interpretation, scratch/constant policy, rejected source forms, and generated-output impact directly to this by-memory page.
