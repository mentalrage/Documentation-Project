*** UID:0002EU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ChattingPane::HandlePacketEvent(Event *event)
{
    const unsigned char *packet =
        static_cast<const unsigned char *>(event->m_payload.m_packet.m_data);

    if (packet[0] == 10)
        return HandleChatPacketType10(this, packet);

    if (packet[0] == 13)
        return HandleServerSayPacket(packet);

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact EventHandler packet-family override with Event packet data at +0x0c, secondary-view +0xa0 compiler adjustment, literal byte-10 dispatch to the preserved free HandleChatPacketType10 helper, literal byte-13 dispatch to private ChattingPane::HandleServerSayPacket, false default/helper return semantics, vtable-only liveness, source-order declarations, and seven-byte alignment before OnPaint. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047f890-0x0047f8c9 ChattingPane Chat Packet Router

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: packet/event router method.
- Owner: [UID:00001X][ChattingPane](by-class/ChattingPane.md), reached through its inherited `EventHandler` secondary view at complete-object offset `+0xa0`.
- Likely source file: [UID:0000I5][Chatting](by-file/Chatting.md).
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Rebuild handling: `source-authored` vtable-dispatched chat packet router.
- Autogen parent: [UID:00001X][ChattingPane](by-class/ChattingPane.md), now `91/93` and attached to [UID:0000I5][Chatting](by-file/Chatting.md).
- Confidence: very strong for routing behavior, exact boundaries, Event ABI, vtable-only reachability, packet-family slot, secondary-view adjustment, helper targets, and source placement; the callback/helper names remain evidence-backed descriptive spellings rather than recovered symbols.

## Behavior

This small `EventHandler` override receives an [UID:00004L][Event](by-class/Event.md), extracts the packet data pointer from `event->m_payload.m_packet.m_data` at Event `+0x0c`, and dispatches on byte zero of the pointed packet:

| Packet byte | Target |
| --- | --- |
| `10` | `0x0047ff80` chat packet type-10 handler |
| `13` | [UID:0002EZ][0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket](by-memory/0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket.md) |
| other | returns `false` |

IDA decompilation shows `this - 10` on a decompiler `__int128 *` temporary; live disassembly resolves that to `add ecx, 0xffffff60`, or `ecx -= 0xa0`, before tail-jumping into either helper. The incoming receiver is the inherited `EventHandler` secondary view at complete-object offset `+0xa0`. Source expresses an ordinary `ChattingPane::HandlePacketEvent(Event *)` override; it must not hand-write the pointer adjustment.

Historical 2026-06-17 B003 source-quality work raised this page to `86/90` and populated a first draft using `OnChatPacketMessage(const Message *)` / `HandleChatPacketEvent` alternatives. Current Event/EventHandler and dispatcher evidence supersedes only that type/name uncertainty: the exact project-facing contract is `HandlePacketEvent(Event *)`. The historical opcode, helper-target, owner, receiver-adjustment, and source-family findings remain valid.

The dispatched bodies remain separate Chatting source definitions ([UID:0002F0][0x0047ff80-0x00480640.ChatPacketType10Handler](by-memory/0x0047ff80-0x00480640.ChatPacketType10Handler.md) and [UID:0002EZ][0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket](by-memory/0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket.md)) rather than inline router-owned code. The [UID:00001X][ChattingPane](by-class/ChattingPane.md) declaration now supplies the private target member and preserves the existing post-class free UID0002F0 prototype before `[[CHILDREN]]`. The router's ABI proves this mixed source shape: opcode 10 explicitly forwards the adjusted complete-object pointer to the free helper, while opcode 13 places only the packet pointer on the stack and tail-jumps with the adjusted complete-object pointer in `ecx` to the member.

2026-06-17 B001 reanalysis of [UID:0002EZ][0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket](by-memory/0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket.md) confirms that `HandleChatPacketType13` also returns `false` after both side-effect message appends and ignored packets. The router C++ should therefore treat the helper return as the binary callback result, not as a handled/unhandled semantic signal.

## Event Payload And Virtual Contract

- [UID:00004L][Event](by-class/Event.md) has inherited vptr `+0x00`, unsigned type byte `+0x04`, alignment through `+0x07`, and a payload union beginning at `+0x08`.
- `EventPacketPayload` places event-level packet kind at Event `+0x08`, data pointer at `+0x0c`, and size at `+0x10`. Packet factory `0x004a9fa0` independently writes type `18`, kind `1`, pointer, and size in that order before dispatch.
- The router loads only Event `+0x0c`, dereferences that pointer, and compares pointed packet byte zero. It does not use the Event packet-kind byte as a network opcode and does not read the packet size.
- EventDispatcher `0x004a77d0` selects exact packet type `18` through `Event::IsPacketEvent()` and invokes EventHandler virtual offset `+0x10`. ChattingPane secondary table word `0x00614d64`, also offset `+0x10`, points to this method.
- The complete secondary table order is scalar deleting destructor; pointer/mouse `+0x04`; key/text `+0x08`; IME `+0x0c`; packet `+0x10`; system/control `+0x14`; type 19 `+0x18`; then dispatcher-forward, two pair-output, and acceptance slots.
- EventDispatcher frees type-18 data after routing. This method borrows the packet pointer for the duration of dispatch and does not own or free it.

## 2026-07-14 B005 Accepted Server-Say Dispatch Resolution

- The accepted callback preserves the binary's comparison order and raw opcode literals: byte `10` is tested first and calls free `HandleChatPacketType10(this, packet)`; byte `13` is tested second and calls private `HandleServerSayPacket(packet)`; every other value returns `false`.
- Disassembly establishes the source ABI distinction. Both taken branches perform the compiler-only `ecx -= 0xa0` secondary-to-complete-object adjustment. The opcode-10 branch then supplies that complete object as an explicit first stack argument to `0x0047ff80`; the opcode-13 branch supplies no explicit object argument and reaches `0x0047fd70` with the complete object in `ecx`.
- This change does not rename, memberize, rewrite, rescore, or otherwise change UID0002F0. Its provisional Type10 name and existing free-function contract remain an independent verify-only source-quality surface.
- `ServerSaySubtype` is local to UID0002EZ's method definition. Neither this router nor a shared packet header emits an opcode or subtype enum; [UID:00026H][0x00625008-0x0062ce50.ServerPacketNameTable](by-memory/0x00625008-0x0062ce50.ServerPacketNameTable.md) remains string evidence only.
- The target body was reconfirmed during B005 IDA MCP evidence sessions on 2026-07-13; this page records evidence-time provenance and does not assert that any transient session remains active.

## Negative Evidence And Rejected Alternatives

- [UID:000084][Message](by-class/Message.md) is the unrelated ChangeMan/message-dialog object with `m_type` and `SortedList *m_entries`; it has no packet payload at `+0x0c`. Historical `Message *`, `OnChatPacketMessage`, and `HandleChatPacketEvent` drafts are superseded for current source.
- No ordinary code caller reaches the target; its sole data xref from the ChattingPane secondary vtable is positive virtual-liveness evidence, not dead-code evidence.
- No null, packet-size, alignment, exception, or opcode-range guard exists in the complete target body. Such checks must not be invented.
- Empty direct-callee output is expected because both helper transfers are tail jumps. Each helper has exactly one inbound code xref from this router.
- No source should emit `__int128 *`, `this - 10`, an explicit vtable cast, or the `ecx -= 0xa0` adjustment. Those are decompiler/compiler ABI artifacts.
- Event.cpp and EventDispatcher.cpp own the record and dispatch infrastructure, not this ChattingPane body. MessageDialogs, ChangeMan, Socket, FolderTreePane, and a new packet-router file are rejected owners/source routes.
- No split, child registration, emitter position, range merge, IDA mutation, helper-body rewrite, or manual vtable emission is required.

## Raw Code Evidence

```asm
0047F893  mov     eax, [ebp+arg_0]
0047F896  mov     eax, [eax+0Ch]
0047F899  mov     dl, [eax]
0047F89B  cmp     dl, 0Ah
0047F89E  jz      short loc_47F8BA
0047F8A0  cmp     dl, 0Dh
0047F8A3  jz      short loc_47F8AB
0047F8A5  xor     al, al
0047F8A8  retn    4
0047F8AB  mov     [ebp+arg_0], eax
0047F8AE  add     ecx, 0FFFFFF60h
0047F8B5  jmp     sub_47FD70
0047F8BA  mov     [ebp+arg_0], eax
0047F8BD  add     ecx, 0FFFFFF60h
0047F8C4  jmp     sub_47FF80
```

## Live IDA Evidence

- IDA MCP against MD5 `4247e04e20b65d6414c7238aa8ff5515` reports `sub_47F890` at `0x0047f890-0x0047f8c9`; `0x0047f8c9` is not a function and [UID:0002EV][0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint](by-memory/0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint.md) begins at `0x0047f8d0`.
- Byte review for `0x0047f890-0x0047f8d0` confirms the `0x39`-byte body followed by seven `0xcc` alignment bytes.
- `xrefs_to 0x0047f890` reports a single data xref at vtable slot `0x00614d64`; nearby vtable words include `0x0047f6f0` and `0x0055f130` before this router and `0x00544df0`, `0x00544e00`, and `0x004a89f0` after it.
- `callers 0x0047f890` reports no ordinary direct callers, matching vtable-only dispatch.
- The `callees` tool reports no direct callees because the handler transfers are tail jumps. `xrefs_to 0x0047fd70` reports the type-13 tail jump at `0x0047f8b5`, and `xrefs_to 0x0047ff80` reports the type-10 tail jump at `0x0047f8c4`.
- Hex-Rays decompilation extracts `event+0x0c`, checks packet byte `10` before packet byte `13`, returns `0` for all other packet bytes, and forwards the packet pointer as the handler argument.
- 2026-07-13 Agent-B001 live MCP session `160045b9` reconfirmed server status `ok`, ready analysis/Hex-Rays/strings, target size `0x39`, full 20-instruction body, and sole data xref `0x00614d64`.
- The same session analyzed EventDispatcher `0x004a77d0`: packet predicate routing calls vtable offset `+0x10`, system/control uses `+0x14`, and exact type 19 uses `+0x18`. Packet Event factory `0x004a9fa0` writes type 18, kind 1, data at `+0x0c`, and size at `+0x10`.
- A bounded `get_bytes` read at `0x00614d50` returned the complete ChattingPane secondary table through `0x00614d7f`; its fifth method dword is `0x0047f890`. A bounded target-region read also reconfirmed all target bytes and seven trailing `0xcc` bytes.
- Live helper analysis reconfirmed sizes `0x1f0` and `0x656`; UID0002EZ exposes only zero-return paths, while UID0002F0's current complete accepted body and evidence preserve false on every path. The router forwards each helper result exactly.

## Score Rationale

Completion is `92` because the page now resolves the exact Event/EventHandler signature, payload member and offsets, packet-family vtable slot, complete receiver adjustment, ordered opcode dispatch, helper declaration/order/linkage contract, return semantics, owner/emitter route, source placement, range/padding, rejected alternatives, and formal C++. Confidence is `94` because target, dispatcher, factory, vtable, xref, byte, and current type evidence agree. The remaining cap is absent original symbol/header spelling and unprovable original helper storage-class intent, not behavior, ABI, placement, or compilability.

## Parent Attachment

[UID:00001X][ChattingPane](by-class/ChattingPane.md) is `91/93` and attached to [UID:0000I5][Chatting](by-file/Chatting.md), now `87/90`. This router is `92/94`; owner/emitter remain UID00001X, reconstructable remains true, position remains blank, and `Nested:0` remains unchanged.

## Remaining Naming Notes

- Exact original symbols remain absent, but the current project EventHandler family resolves the strongest source-facing name and signature to `HandlePacketEvent(Event *)`; historical `OnChatPacketMessage` and `HandleChatPacketEvent` are no longer current alternatives.
- The `ecx -= 0xa0` adjustment is compiler multiple-inheritance glue and should not be written manually in the source body.
- `HandleChatPacketType10` remains a descriptive but source-ready free-helper name. `HandleServerSayPacket` is the accepted private target-member name supported by the opcode-name table, packet consumers, and exact member ABI; historical `HandleChatPacketType13` is superseded for UID0002EZ only.
- Exact header factoring remains unknown and does not block the current UID00001X -> UID0000I5 emitter route.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:0002EZ][0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket](by-memory/0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket.md)
- [UID:0002F0][0x0047ff80-0x00480640.ChatPacketType10Handler](by-memory/0x0047ff80-0x00480640.ChatPacketType10Handler.md)
- [UID:0002EV][0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint](by-memory/0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint.md)
- [UID:00001X][ChattingPane](by-class/ChattingPane.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)

## Changes

- 2026-05-31: Created exact router page and set completion/confidence to `78/88`. Evidence is IDA MCP function lookup, decompilation, and xref/byte-boundary audit for `0x0047f890`.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and left the parent blank.
  - Reasoning: existing documentation identifies source-owned chat packet routing, but the page is `78/88` and fails the child-side 80/80 attachment gate.
- 2026-06-05: Raised from `78/88` to `82/90` and attached [UID:00001X][ChattingPane](by-class/ChattingPane.md). Live IDA MCP reconfirmed exact `0x0047f890-0x0047f8c9` bounds, vtable-only data xref `0x00614d64`, seven-byte `0xcc` padding before `OnPaint`, packet-byte dispatch for `10` and `13`, secondary-view `ecx -= 0xa0` adjustment, tail jumps to the type-13 and type-10 handlers, and no ordinary direct callers. Final C++ remains blank pending final source method/vtable names.
- 2026-06-17 B003 ChattingPane core source-quality refresh:
  - Raised completion from `82` to `86` and populated first-draft C++.
  - Summary/evidence: B003 rechecked vtable reachability, local PE bytes/tail jumps, packet helper pages, and prior Chatting packet research. The method is now documented as a secondary-view `ChattingPane::OnChatPacketMessage` / `HandleChatPacketEvent` callback that dispatches to file-local `HandleChatPacketType10` and `HandleChatPacketType13`; original spelling remains inferred but no longer blocks first-draft source.
- 2026-06-17 B001 packet type-13 support update:
  - Added the type-13 helper return-value clarification from [UID:0002EZ][0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket](by-memory/0x0047fd70-0x0047ff80.ChattingPaneHandleServerSayPacket.md): the helper returns false after both appended and ignored packets.
- 2026-07-13 B001 accepted UID0002EU implementation callback:
  - Raised `86/90` to `92/94`; owner/emitter UID00001X, reconstructable true, blank position, and `Nested:0` are unchanged.
  - Replaced the historical `Message`/`OnChatPacketMessage` draft with the exact managed `ChattingPane::HandlePacketEvent(Event *)` body using `m_payload.m_packet.m_data` at Event `+0x0c`.
  - Incorporated live MCP session `160045b9` target, EventDispatcher, packet-factory, helper, byte, vtable, and xref evidence; resolved packet slot `+0x10`, complete-object adjustment `-0xa0`, byte-10/13 order, false return behavior, helper declarations/linkage, negative evidence, and source placement.
  - Historical pre-C++ and B003 Message-based wording is preserved only as superseded provenance; no target blocker remains.
- 2026-07-14 B005 accepted UID0002EZ implementation callback:
  - Replaced only the opcode-13 free-helper call with the exact private `HandleServerSayPacket(packet)` member call while preserving literal opcode order, Event payload extraction, opcode-10 free-helper ABI, default false return, score, metadata, range, and nesting.
  - Recorded the branch-local calling-convention proof, local subtype-enum ownership, source ordering, and the explicit no-change disposition for UID0002F0. Historical Type13/free-target wording remains only as superseded provenance.
