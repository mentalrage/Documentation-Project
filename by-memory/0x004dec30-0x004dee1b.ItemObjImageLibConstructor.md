*** UID:0002SD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006W | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006W | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ItemObjImageLib::ItemObjImageLib()
    : Singleton<ItemObjImageLib>()
{
    m_itemInfos.m_count = 0;
    m_itemInfos.m_entries = 0;
    m_itemInfos.m_defaultCapacity = 10;

    m_itemInfos.m_defaultEntry.itemTableId = -1;
    m_itemInfos.m_defaultEntry.paletteSlot = -1;
    m_itemInfos.m_defaultEntry.alpha = 0.0f;
    m_itemInfos.m_defaultEntry.lightImageIndex = -1;
    m_itemInfos.m_defaultEntry.allowPaletteFilter = false;

    DATFile itemTable;
    itemTable.Open(g_useEpfAssets ? L"ITEM.TBL" : L"ITEM.TBD");

    int itemCount = 0;
    ReadEncodedTableInt(&itemTable, &itemCount);

    if (itemCount > 0 && m_itemInfos.m_count != itemCount) {
        ItemInfo *newEntries =
            static_cast<ItemInfo *>(operator new(sizeof(ItemInfo) * itemCount));

        if (m_itemInfos.m_entries != 0) {
            int copyCount = itemCount;
            if (m_itemInfos.m_count < copyCount)
                copyCount = m_itemInfos.m_count;

            memmove(newEntries, m_itemInfos.m_entries, sizeof(ItemInfo) * copyCount);
            free(m_itemInfos.m_entries);
        }

        m_itemInfos.m_count = itemCount;
        m_itemInfos.m_entries = newEntries;
    }

    for (int index = 0; index < itemCount; ++index) {
        ItemInfo itemInfo;
        ReadEncodedTableInt(&itemTable, &itemInfo.itemTableId);
        ReadEncodedTableInt(&itemTable, &itemInfo.paletteSlot);
        ReadEncodedTableFloat(&itemTable, &itemInfo.alpha);
        ReadEncodedTableInt(&itemTable, &itemInfo.lightImageIndex);

        int allowPaletteFilter = 0;
        ReadEncodedTableInt(&itemTable, &allowPaletteFilter);
        itemInfo.allowPaletteFilter = allowPaletteFilter != 0;

        ItemInfo *slot = &m_itemInfos.m_defaultEntry;
        if (index >= 0 && index < m_itemInfos.m_count)
            slot = &m_itemInfos.m_entries[index];

        *slot = itemInfo;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact startup-only ItemObjImageLib source constructor with direct Singleton<ItemObjImageLib> base initialization, compiler-lowered +0x04 EBO publication and unwind clear, complete ProtectedArray<ItemInfo> initialization, ITEM.TBL/ITEM.TBD selection, 20-byte row growth/copy/free, five-field row loading, default fallback row, exact range/padding, and no duplicate handwritten singleton assignment. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dec30-0x004dee1b ItemObjImageLib Constructor

## UID0001OT Direct Singleton Constructor Source Correction - 2026-07-20

- The exact source body is preserved from the accepted constructor. The only source-shape correction is replacing the explicit body assignment with direct `Singleton<ItemObjImageLib>()` base initialization and routing the method through class UID00006W at position `10`.
- RTTI proves a direct nonvirtual Singleton base at PMD `+0x04`; EBO overlaps the base with `m_itemInfos`. Constructor lowering receives the `this+4` base receiver and publishes the recovered complete object at `0x004dec7b`.
- The apparent `a1 == -4` branch and zero store at `0x004dec82` are the compiler's null-adjustment side of complete-object recovery, not a source-authored fallback/guard branch. No handwritten global assignment or pointer arithmetic belongs in the body.
- The constructor EH state records base construction before protected-array/member work. Its cleanup funclet loads the saved complete object, adds `4`, and jumps at `0x00600158` to the eleven-byte `Singleton<ItemObjImageLib>` base destructor at `0x004e5ba0`.
- All authored behavior remains exact: initialize count, entries, provisional default capacity, and the five-field embedded default row; select `ITEM.TBL` or `ITEM.TBD`; read item count; allocate `20 * count`; preserve/copy/free prior entries; and load every five-field `ItemInfo` into an in-range or fallback slot.
- Exact binary range is `0x1eb` bytes with SHA256 `87FD11568820B71E40D1BEFE6E43C7169A1CF7291D110A4748B86E50A35B8B81`; the sole direct caller remains Application startup at `0x004f602f`, and `0x004dee1b-0x004dee20` remains alignment padding.
- Vtable stores, ProtectedArray setup lowering, security/EH mechanics, Singleton publication, and unwind clear remain compiler output even though their binary effects are documented. The source body does not reproduce those operations manually.

## Summary

- Address range: `0x004dec30-0x004dee1b`, end-exclusive.
- Entity kind: class constructor / startup singleton constructor.
- Owner class: ItemObjImageLib.
- Owner file: ItemObjImageLib.
- Parent aggregate: `0x004dec30-0x004e65dc.ItemObjImageLib`.
- Rebuild handling: source-authored constructor body.
- Autogen status: reconstructable constructor attached to [UID:0000KH][ItemObjImageLib](by-file/ItemObjImageLib.md); B005 populated first-draft C++ after resolving helper roles and descriptive `ItemInfo` field names.
- C++ reconstruction: first-draft source is populated. B008 layout correction now models the fallback row as `m_itemInfos.m_defaultEntry` instead of a separate top-level class field. Exact original spellings for `ItemInfo +0x00/+0x0c`, the protected-array capacity/reserved field, and final DAT scalar-reader method names remain descriptive/inferred, which caps final-audit confidence but no longer blocks source emission.

## Behavior

Constructs the process-wide item object image library used by item drawing paths. Direct Singleton base construction initializes the singleton storage; compiler lowering installs vtables. The authored body seeds the `ProtectedArray<ItemInfo>` inline default row, opens the item metadata table, grows the protected item-info array, and reads rows from `ITEM.TBL` or alternate `ITEM.TBD`.

The class/file docs describe this as the owner of item-specific render table rows, legacy item-entry remapping, `ITEM.EPF` / `ITEM.EPD` frame use, palette slot `10`, and the item draw variants. This constructor is the startup loading side of that module.

## Live IDA Pass, 2026-06-10 A001

- `lookup_funcs` reports `sub_4DEC30` as the exact IDA function at `0x004dec30`, size `0x1eb` / 491 bytes (Verified with int_convert.py), ending at `0x004dee1b`; `0x004dee1b` and `0x004dee20` are not IDA functions.
- `callers(0x004dec30)` reports one caller: `sub_4F5FB0` at call site `0x004f602f`, matching the startup singleton construction path.
- `callees(0x004dec30)` reports ten direct callees: `sub_4F4A80`, `sub_49C130`, `sub_49C180`, `sub_49C4A0`, `sub_49C4F0`, `sub_49C160`, allocator `unknown_libname_19`, `_memmove`, `j_j_j___free_base`, and `@__security_check_cookie@4`.
- Disassembly confirms the constructor stores `g_pItemObjImageLib` / `dword_67A758` at `0x004dec7b` or clears it at `0x004dec82` if the adjusted protected-array subobject is null.
- Disassembly confirms the primary vtable store at `0x004dec8c` and the embedded `ProtectedArray<ItemInfo>` vtable store at `0x004dec92`.
- The protected array starts at `this + 0x04`; the constructor writes default capacity `0x0a` / 10 (Verified with int_convert.py), count `0`, and pointer `0`.
- The fallback/default `ItemInfo` at `this + 0x14` is the embedded `ProtectedArray<ItemInfo>::m_defaultEntry`, seeded from a stack row with `-1, -1, 0, -1, false`, then copied as a 16-byte block plus the final bool/dword.
- `byte_66DA97 == 1` selects the inline UTF-16 string record at `0x0061c260`, whose bytes spell `ITEM.TBL`; otherwise the constructor passes `0x0061c274`, whose bytes spell `ITEM.TBD`.
- `sub_49C4A0(&v16)` reads the table row count. If positive and different from the current protected-array count, the constructor allocates `0x14` / 20 bytes per row (Verified with int_convert.py), preserves existing rows with `_memmove`, frees old storage, then updates count and data pointer.
- The row-load loop reads five fields per `ItemInfo`: four integer-like reads through `sub_49C4A0`, one bool-like byte derived from a fifth `sub_49C4A0` result, and one field read through `sub_49C4F0` at offset `+0x08`.
- Bounds fallback is explicit: if the current row index is outside the protected-array count, the row is copied into the fallback slot at `this + 0x14`; otherwise it writes to `data + rowIndex * 0x14`.
- The constructor destroys the local table/path object with `sub_49C160(&v14)` before returning `this`.
- `py_eval` byte check confirms `0x004dee1b-0x004dee20` is five `0xcc` alignment bytes before the raw ordinary destructor body at `0x004dee20`.

## Evidence

- Existing aggregate evidence reports IDA MCP exact function bounds for the constructor as `0x004dec30-0x004dee1b`.
- Existing startup evidence records the constructor's only executable caller as `Application::Startup` call site `0x004f602f`.
- Existing singleton evidence records writes to [UID:0000RA][g_pItemObjImageLib](by-global/g_pItemObjImageLib.md) / `dword_67A758` at `0x004dec7b` and `0x004dec82`.
- Existing vtable evidence records the constructor vtable store at `0x004dec8c` for [UID:0001XW][ItemObjImageLibVtable](by-type/by-vtable/ItemObjImageLibVtable.md).
- Existing file/class docs record that the constructor loads `ITEM.TBL` or `ITEM.TBD`, initializes the [UID:0001UT][ItemInfo](by-type/by-struct/ItemInfo.md) default/fallback state inside `ProtectedArray<ItemInfo>`, and uses [UID:0001UU][ItemObjImageLibLayout](by-type/by-struct/ItemObjImageLibLayout.md) / [UID:0000MU][ProtectedArray](by-file/ProtectedArray.md) storage for item rows.
- Existing aggregate evidence records alignment padding after the constructor at `0x004dee1b-0x004dee20`, before the unmodeled ordinary destructor body.

## Touched State And Resources

| Item | Use |
| --- | --- |
| [UID:0000RA][g_pItemObjImageLib](by-global/g_pItemObjImageLib.md) | Published by direct Singleton-base construction; the apparent zero fallback is compiler null-adjustment lowering. |
| [UID:0001XW][ItemObjImageLibVtable](by-type/by-vtable/ItemObjImageLibVtable.md) | Constructor installs the class vtable. |
| [UID:0001UU][ItemObjImageLibLayout](by-type/by-struct/ItemObjImageLibLayout.md) | Object layout for the protected item row array and inline default/fallback item row. |
| [UID:0001UT][ItemInfo](by-type/by-struct/ItemInfo.md) | Metadata row type loaded from the item table resource. |
| `ITEM.TBL` / `ITEM.TBD` | Item render metadata table selected by the modern/legacy item-table path. |

## Constructor Flow Notes

1. Calls the base `LObject`-style initializer (`sub_4F4A80`) before installing class-owned state.
2. Direct Singleton-base construction publishes the process singleton; compiler lowering installs class/template vtables, then the authored body initializes `ProtectedArray<ItemInfo>` state at `this + 0x04`.
3. Builds the default/fallback row and opens the selected table resource path according to `g_useEpfAssets`.
4. Reads the row count and resizes the protected array to exactly that count, using 20-byte `ItemInfo` rows.
5. Reads every table row into stack storage and copies each row into the protected-array slot, with fallback-slot handling for out-of-range indexes.
6. Releases the local table/path object and returns the constructed object pointer.

## UID0003TX Light-Image Field Synchronization - 2026-07-16

- The constructor's two `ItemInfo +0x0c` source references now use `lightImageIndex`, matching the complete row declaration and the direct UID0002QN MapPane consumer. The default row remains `-1`, and each encoded table row still reads one signed int at the same position.
- No behavior, row order, layout, allocation/copy/free flow, DATFile API, score, owner/emitter, or constructor source shape changes. This is a compile-required source-name synchronization, not a new interpretation of the table format.
- Historical `renderFlagsOrMode` is superseded because MapPane tests the field for nonnegative and passes it as the lighting image index. Exact original spelling remains inferred, but flags/mode semantics are no longer defensible.

## Source-Quality Notes

- B005 2026-06-18 resolves the former generated helper-name blocker for first-draft source: `sub_49C130`, `sub_49C180`, and `sub_49C160` are the `DATFile` constructor, `DATFile::Open`, and `DATFile` destructor; `sub_49C4A0` and `sub_49C4F0` are DATFile encoded scalar table readers. First-draft `ItemObjImageLib` source may use local aliases `ReadEncodedTableInt` and `ReadEncodedTableFloat`, matching the accepted LightObjImageLib constructor precedent, while DATFile owner docs retain `ReadAllEntries` / `ReadAllEntriesAlt` pending a DATFile-specific pass.
- Current descriptive `ItemInfo` field names are `itemTableId`, `paletteSlot`, `alpha`, `lightImageIndex`, and `allowPaletteFilter`. `lightImageIndex` is directly consumed by MapPane; exact original lexical spelling of it and `itemTableId` remains unavailable, which caps final-audit confidence without blocking source.
- B008 2026-06-19 resolves the structural fallback-field caveat: the `this + 0x14` row is the embedded protected-array default entry, not an independent top-level `ItemObjImageLib` field. First-draft C++ should therefore access it as `m_itemInfos.m_defaultEntry`; `m_itemInfos.m_defaultCapacity` remains a provisional name for object `+0x10`.
- Source C++ should omit compiler-generated vtable stores, SEH state, security-cookie checks, and raw `operator new` overflow scaffolding except where expressing the visible allocation/copy/free behavior is needed.

## Cross-References

- [UID:0000KH][ItemObjImageLib](by-file/ItemObjImageLib.md)
- [UID:00006W][ItemObjImageLib](by-class/ItemObjImageLib.md)
- [UID:00017N][0x004dec30-0x004df500.ItemObjImageLibLocalMethodCluster](by-memory/0x004dec30-0x004df500.ItemObjImageLibLocalMethodCluster.md)
- [UID:0000RA][g_pItemObjImageLib](by-global/g_pItemObjImageLib.md)
- [UID:0001OT][0x0067a758-0x0067a75c.g_pItemObjImageLib](by-memory/0x0067a758-0x0067a75c.g_pItemObjImageLib.md)
- [UID:0001XW][ItemObjImageLibVtable](by-type/by-vtable/ItemObjImageLibVtable.md)
- [UID:0001UU][ItemObjImageLibLayout](by-type/by-struct/ItemObjImageLibLayout.md)
- [UID:0001UT][ItemInfo](by-type/by-struct/ItemInfo.md)
- [UID:0000MU][ProtectedArray](by-file/ProtectedArray.md)

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | The exact range, sole startup caller, complete authored body, direct Singleton base, PMD/EBO publication and unwind, protected-array/default-row layout, resource selection, allocation/copy/free, row load order, source owner/emitter/position, and no-duplicate policy are closed. |
| Confidence `94` | Current live IDA and existing evidence agree on exact bounds, startup reachability, data dependencies, helper roles, PMD/EBO, EH funclet, resources, row width, and following padding. Exact original field/helper/header spellings remain descriptive and cap lexical confidence. |

## Changes

- 2026-07-20 B002 UID0001OT accepted source-quality callback: raised `87/90 -> 92/94`, moved owner/emitter from file UID0000KH to class UID00006W, set position `10`, added direct `Singleton<ItemObjImageLib>()` initialization, removed only the explicit global assignment, and incorporated complete PMD/EBO/EH/source/compiler/no-duplicate evidence while preserving the accepted table body exactly.

- 2026-06-02: Created exact constructor child page split from the broader ItemObjImageLib aggregate so the large range can move toward nested by-memory coverage.
- 2026-06-10 A001: Raised score from `72/84` to `84/88` after live IDA MCP confirmed the `0x1eb`-byte function size, sole startup caller, full callee set, singleton/vtable/protected-array stores, `ITEM.TBL` versus `ITEM.TBD` inline UTF-16 path selection, 20-byte `ItemInfo` row allocation, row-load loop, fallback slot behavior, and `0x004dee1b-0x004dee20` alignment padding. Final C++ remains blank because table helper names and exact `ItemInfo` field names are not source-quality.
- 2026-06-18 B005 source-quality pass: raised score from `84/88` to `87/90` and populated first-draft constructor C++. Local IDA exports and `memory.bin` byte checks resolved DATFile constructor/open/destructor helpers, encoded int/float table-reader aliases, `g_useEpfAssets` `ITEM.TBL`/`ITEM.TBD` selection, fallback `ItemInfo` defaults, and descriptive `ItemInfo` field names enough for first-draft source while keeping final spellings below 95/95.
- 2026-06-19 B008 layout sync: kept score `87/90` but updated first-draft constructor C++ and prose to model `this +0x14` as `m_itemInfos.m_defaultEntry` instead of a separate `fallbackItemInfo` field; `m_itemInfos.m_defaultCapacity` remains provisional pending final `ProtectedArray<T>` declaration proof.
