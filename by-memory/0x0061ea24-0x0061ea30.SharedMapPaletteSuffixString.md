*** UID:0003JG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000L3,0000PB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact UTF-16 `.PAL` pooled palette suffix; kept `CANONICAL_OWNER:NONE` with `EMITTER_UIDS:0000L3,0000PB` because direct MapPane and WorldMapPane source-use contexts are proven but no single declaration owner or shared constants file is recovered. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0061ea24-0x0061ea30 Shared Map Palette Suffix String

## Range

- Address range: `0x0061ea24-0x0061ea30`
- Entity kind: UTF-16 string literal plus alignment.
- Decoded payload: `.PAL`
- Rebuild handling: `source-declared/generated-binary`
- Canonical owner: `NONE`; shared by MapPane and WorldMapPane construction paths with no proven single declaration owner.
- Emitter routing: `EMITTER_UIDS:0000L3,0000PB`; the exact pooled suffix should surface through the proven MapPane and WorldMapPane source-use file roots.

## Summary

This exact child contains the `.PAL` UTF-16 suffix used while building map and world-map palette filenames. It was split out of the former [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) mixed child because live IDA shows the suffix is shared by both [UID:0000L3][MapPane](by-file/MapPane.md) and [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) consumers.

## Evidence

- IDA MCP `get_bytes` on 2026-06-12 C001 reports bytes `2e 00 50 00 41 00 4c 00 00 00 00 00` for `0x0061ea24-0x0061ea30`, matching UTF-16 `.PAL`, a terminator, and alignment before the `wm` string.
- IDA MCP `get_string` decodes `0x0061ea24` as `.PAL`.
- IDA MCP `xrefs_to 0x0061ea24` reports three direct refs: `0x00510b57` and `0x00511041` inside MapPane helper `sub_510960`, and `0x005c2eae` inside WorldMapPane constructor-family function `sub_5C2AC0`.
- IDA MCP `disasm` shows all three refs are `push offset aPal; ".PAL"` instructions.
- IDA MCP `analyze_component` over `0x00510960`, `0x00511db0`, `0x005c2ac0`, and `0x005c5200` reports shared global `0x0061ea24` accessed by `sub_510960` and `sub_5C2AC0`.

## Assignment Gate

Do not set either [UID:0000L3][MapPane](by-file/MapPane.md) or [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) as the canonical owner. Both parents clear `85/85`, but the exact suffix is materially shared and no single declaration owner is proven. Under the current owner/emitter split, both file roots are valid emitters because each has direct source-use evidence.

## B001 Ownership Review

- 2026-06-13 B001 review retained the blank parent. The report found strong evidence that the `.PAL` suffix is a real standalone UTF-16 literal with direct xrefs from MapPane `sub_510960` at `0x00510b57`/`0x00511041` and WorldMapPane constructor-family `sub_5C2AC0` at `0x005c2eae`, but no debug/source metadata, unique owner-only xref pattern, or narrower semantic grouping that proves one source parent. The item remains reconstructable and parentless rather than being guessed into either consumer file.

## Current No-Owner Recheck

B001's current no-owner recheck on 2026-06-14 kept the same route after comparing the target page, generated no-owner rows, related by-file/by-memory docs, live IDA MCP session `b001_0002bd`, and an independent PE byte/reference scan. The generated state at the time was already the intended state: no owner, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, output path `auto-generated/NexusTK/map/MapPane.cpp`, and blank generated MapPane/WorldMapPane C++ files because this page has no standalone reconstruction snippet. That empty generated output is not an invalid emitter route; it only means the future source should spell the suffix at use sites.

Byte and reference evidence from that pass:

- Live bytes at `0x0061ea20` include `2e 00 50 00 41 00 4c 00 00 00 00 00` at `0x0061ea24`, proving UTF-16LE `.PAL`, its null terminator, and zero-word alignment before the following `wm` family.
- `xrefs_to 0x0061ea24` returned exactly the three direct source-use refs already modeled here: `0x00510b57` and `0x00511041` in MapPane `sub_510960`, plus `0x005c2eae` in WorldMapPane `sub_5C2AC0`.
- No xref was found to the interior/alignment address `0x0061ea2c`.
- Function membership confirmed `sub_510960` starts at `0x00510960` and has size `0x9e7`; `sub_5C2AC0` starts at `0x005c2ac0` and has size `0x7ea`; `sub_543D70` starts at `0x00543d70` and has size `0xce`; `0x0061ea24` is data, not a function.
- Decompilation of `sub_510960` shows two MapPane appends equivalent to `wcscat_s(..., L".PAL")`; decompilation of `sub_5C2AC0` shows a WorldMapPane append equivalent to `sub_584470(&a3, L".PAL", 4u)`, followed by string materialization and a call to PaletteLib-style lookup `sub_543D70`.
- `sub_543D70` receives a caller-provided `wchar_t *Source`, compares it with palette names, and can call the palette load helper, but it has no direct ref to `0x0061ea24` and does not construct the suffix. PaletteLib is therefore a downstream consumer of completed names, not this literal's source owner.

Sibling contrast from the same pass supports the current split instead of a merge:

- `0x0061ea30`, `0x0061ea38`, and `0x0061ea40` are MapPane-only sibling strings referenced from `sub_511DB0`.
- `0x0061ea48` is the separate shared [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md), with refs from MapPane `sub_511DB0` and WorldMapPane helper `sub_5C5200`.
- `0x0061ea50` through `0x0061ea68` are again MapPane-only sibling strings. Adjacent bytes alone are not an ownership signal because the nearby literals have different xref sets.

Raw PE scan facts from `NexusTK.exe` also support the no-owner/multiple-emitter route:

- Image base `0x00400000`; target RVA `0x0021ea24`; target raw offset `0x0021d424` in `.rdata`.
- The exact 12-byte target pattern appeared 69 times overall, with the target raw offset present.
- UTF-16LE `.PAL` plus null appeared 119 times overall, so the spelling is too duplicated/pooled to prove a unique source owner by string text alone.
- `push 0x0061ea24` appeared only at raw offsets mapping to VA `0x00510b57`, `0x00511041`, and `0x005c2eae`.
- Absolute dword `0x0061ea24` appeared at the corresponding operand dwords; RVA dword `0x0021ea24` had zero hits.

Historical alternatives rejected by this recheck:

- MapPane remains the strongest forced existing owner only because it has two direct refs, but that would still overclaim because WorldMapPane has an independent direct source-use ref.
- WorldMapPane remains a valid emitter but not a canonical owner because MapPane has two direct source-use refs and owns adjacent MapPane-only comparison strings.
- PaletteLib, Palette, DAT/resource ownership, the mixed [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) split index, and a synthetic shared constants file are all rejected because no direct suffix declaration, debug/source path, initializer object, named global, or source module evidence exists.
- Do not split, merge, reclassify, or IDA-rename this page. The current child is exactly the `.PAL` suffix plus terminator/alignment, and `SharedMapPaletteSuffixString` is an accurate owner-neutral documentation name.

## Second-Pass Ownership/Emitter Addendum

B001's archived `0003JG-SharedMapPaletteSuffixString-second-pass` report independently rechecked the target after the owner/emitter split model was in use. It recommended no metadata, score, split, merge, reclassification, IDA repair, parent repair, generated-output repair, C++ snippet, or `by-memory/-coverage-report.md` change. The retained route is still `COMPLETION:87`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000L3,0000PB`.

Second-pass scope and rule basis:

- The pass read the target page, the [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) split index, shared sibling [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md), [UID:0000L3][MapPane](by-file/MapPane.md), [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md), [UID:0000MB][PaletteLib](by-file/PaletteLib.md), MapPane and WorldMapPane executable-range docs, proposed source-tree context, generated memory coverage, the manual coverage report, and the no-owner tracker.
- The applied structure rule was that string literals are `source-declared/generated-binary` artifacts when source-use contexts are proven, while `CANONICAL_OWNER` records semantic ownership and `EMITTER_UIDS` records source-output routing. A pooled/shared literal may remain ownerless when no single semantic declaration owner is proven, and a synthetic owner must not be invented merely to avoid `NONE`.
- This target is reconstructable because the literal bytes and source-use contexts are proven, but it remains ownerless because the single physical suffix is directly used by two separately documented map source families.

Second-pass live IDA facts:

- IDA MCP state was input `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, image base `0x00400000`, with auto-analysis and Hex-Rays ready.
- `get_bytes(0x0061ea24, 0x4c)` decoded the split-index neighborhood as `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, and `WM4`.
- `get_string` confirmed `.PAL` at `0x0061ea24`, `wm` at `0x0061ea30`, `WM` at `0x0061ea38`, `wm2` at `0x0061ea40`, `WM2` at `0x0061ea48`, `wm3` at `0x0061ea50`, `WM3` at `0x0061ea58`, `wm4` at `0x0061ea60`, and `WM4` at `0x0061ea68`.
- `xrefs_to 0x0061ea24` again returned exactly `0x00510b57` and `0x00511041` in MapPane `sub_510960`, plus `0x005c2eae` in WorldMapPane constructor/resource function `sub_5C2AC0`; `search_text("aPal")` over the relevant code ranges independently located the same three pushes and no additional target-address users in those ranges.
- Function identity remained `sub_510960` at `0x00510960`, `sub_511DB0` at `0x00511db0`, `sub_5C2AC0` at `0x005c2ac0`, `sub_5C5200` at `0x005c5200`, and PaletteLib loaded-palette lookup helper `sub_543D70` at `0x00543d70`.
- `analyze_component` confirmed `sub_511DB0 -> sub_5C2AC0` and `sub_5C2AC0 -> sub_543D70`. Those call edges explain how MapPane can drive WorldMapPane creation and how completed palette names reach PaletteLib, but they do not transfer source ownership of this suffix to MapPane or PaletteLib.
- `analyze_function(0x00543d70)` showed `sub_543D70` takes a caller-provided `wchar_t *Source`, compares it with loaded palette names, and may call a loader helper when absent. It does not reference `0x0061ea24` and does not construct the `.PAL` suffix.
- Source/debug breadcrumb searches for `.pdb`, `RSDS`, `NB10`, `.cpp`, `.cxx`, and `.h` found no stripped-source metadata that could promote the literal to a single source file or shared constants owner.

Second-pass raw PE facts:

- The target is in `.rdata`; file offset `0x21d424-0x21d430` contains `2e 00 50 00 41 00 4c 00 00 00 00 00`.
- The report counted 119 terminator-inclusive wide `.PAL` occurrences in the executable. Earlier scans in this page counted 69 standalone `.PAL` byte-pattern hits under a different pattern definition. Both counts support the same conclusion: textual `.PAL` spelling is too duplicated to prove unique ownership.
- Exact target address push occurrences were three, target VA immediate occurrences were three, target RVA immediate occurrences were zero, and no rel32 references to the target were found.
- The exact pooled address evidence still proves source-use contexts for MapPane and WorldMapPane, but text uniqueness does not prove a source declaration owner.

Second-pass ownership and emitter decision:

- Keep MapPane as emitter `0000L3` because MapPane `sub_510960` directly references the target at `0x00510b57` and `0x00511041` while building palette resource names.
- Keep WorldMapPane as emitter `0000PB` because WorldMapPane `sub_5C2AC0` directly references the target at `0x005c2eae` while preparing a world-map palette lookup name.
- Reject PaletteLib emitter/owner routing because PaletteLib receives completed names downstream and has no direct xref to this pooled literal.
- Reject the split index [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) as owner because it is a non-reconstructable mixed-child index, not a source emitter.
- Reject forced MapPane ownership because use count is not declaration proof and WorldMapPane has an independent direct source-use ref.
- Reject forced WorldMapPane ownership because MapPane has two independent direct source-use refs and owns adjacent MapPane-only comparison strings.
- Reject a synthetic shared constants owner because there is no source/debug metadata, table abstraction, initializer object, or documented source unit proving such a declaration.

## Rule 26 Ownership Audit

B001's `0003JG-SharedMapPaletteSuffixString` report treated the parentless route as the strongest outcome, not as an unanalyzed fallback. The ranked ownership conclusions were:

- Keep this page parentless with `CANONICAL_OWNER:NONE` and `EMITTER_UIDS:0000L3,0000PB`. Confidence `88/100`. The exact `.PAL` object is materially referenced by both MapPane and WorldMapPane code, and the binary has no source/debug evidence proving one declaration owner for the pooled copy.
- If a forced existing owner were required, [UID:0000L3][MapPane](by-file/MapPane.md) would be the strongest forced candidate at only `55/100`, because two of the three direct xrefs are in MapPane `sub_510960`. This is still below the ownership threshold because the WorldMapPane constructor-family reference is direct and semantically independent.
- A forced [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) route is weaker at `43/100`: WorldMapPane directly uses the suffix while constructing/loading palette resources, but MapPane has two direct resource-name construction uses and owns nearby MapPane-only comparison strings.
- Palette or PaletteLib ownership is rejected at about `20/100`. Palette code consumes completed palette filenames and manages palette slots; this target is the caller-side suffix used before palette loading begins.
- A new shared-owner source file/category such as `SharedResourceSuffixLiterals` is speculative at about `18/100`. Reuse and linker pooling are plausible, but no PDB/source path, debug metadata, or source module evidence proves a real owner.
- The mixed split index [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) is not an owner candidate. It is a non-emitting index over adjacent children with different ownership.

## Consumer Behavior

MapPane consumer evidence:

- `sub_510960` is called from the MapPane dispatcher path through `sub_507C90 -> sub_510960` at `0x00507ffa`.
- In `sub_510960`, IDA shows the map/resource base name copied from `this + 1758`, followed by suffix appends. The same helper builds both `.E` and `.PAL` forms; the `.PAL` path uses the target literal at `0x0061ea24`.
- The report's representative decompilation is consistent with the current direct xrefs: `wcscpy_s(..., (const wchar_t *)(this + 1758))`, then `wcscat_s(..., L".PAL")` at the two MapPane refs.
- These MapPane refs prove real source-use evidence, not a generic string table scan, but they do not prove the single emitted literal was declared in MapPane.

WorldMapPane consumer evidence:

- `sub_5C2AC0` is the WorldMapPane constructor-family resource initialization path and is reached from `sub_511DB0 -> sub_5C2AC0` at `0x005126f9` and `sub_50F270 -> sub_5C2AC0` at `0x0050f2fa`.
- The WorldMapPane path uses the same `.PAL` literal while preparing a palette resource string, then calls downstream palette-loading behavior through `sub_543D70`.
- The palette-loader call does not transfer ownership of the suffix to Palette or PaletteLib. The suffix is appended in the WorldMapPane caller/source context before palette-manager ownership begins.

Supporting sibling/container evidence:

- [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md) independently uses the same modeling pattern: `WM2` has direct refs from MapPane `sub_511DB0` and WorldMapPane helper `sub_5C5200`, so it remains parentless with MapPane and WorldMapPane emitter routes.
- [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) correctly remains non-reconstructable and non-emitting because the contiguous island contains shared `.PAL`, MapPane-only `wm`/`WM`/`wm2`, shared `WM2`, and MapPane-only `wm3`/`WM3`/`wm4`/`WM4`.

## Rejected Alternatives

- Do not assign this item to MapPane merely because MapPane has two direct xrefs. Consumer count is not source-declaration proof, and `sub_5C2AC0` is modeled as WorldMapPane source behavior.
- Do not assign this item to WorldMapPane merely because WorldMapPane uses the suffix in constructor/resource initialization. MapPane has two direct uses in its own resource-building path, and WorldMapPane already has a separate local resource string block at [UID:00035I][0x00631490-0x00631528.WorldMapPaneResourceStrings](by-memory/0x00631490-0x00631528.WorldMapPaneResourceStrings.md).
- Do not assign this item to Palette, PaletteLib, a DAT/resource category, or a packaged-resource owner. The bytes are the UTF-16 source suffix literal `.PAL`, not a palette payload or DAT archive member.
- Do not create a new owner solely for this target. The nearby/shared `.EPF` suffix reuse pattern suggests common resource-suffix pooling, but the current evidence still does not prove a real source file, header, or constants module.

## Split And Source-Declaration Review

- The current child boundary is exact: `0x0061ea24` starts UTF-16 `.PAL`, includes its terminator and two bytes of alignment, and ends before the next child at `0x0061ea30` (`L"wm"`).
- No merge with [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md) is justified. `.PAL` and `WM2` are adjacent and both shared, but they have different meanings and different direct xref sets.
- No structural repair, IDA rename/type repair, or split-index promotion is recommended for this target.
- B001 found no RSDS, NB10, PDB path, source-path, or debug metadata that would identify a source owner. Raw-byte review found many `.PAL` substrings in `.rdata`, mostly as parts of full palette filenames; this standalone UTF-16 suffix object is the pooled/shared suffix next to the `wm` string cluster.

## Goal 2 Verification Addendum

B001's archived `0003JG-SharedMapPaletteSuffixString-goal2-verification` pass independently rechecked the no-owner-with-emitters route against current docs, generated coverage, IDA MCP session `a001_goal2_class_batch`, and a fresh raw PE scan. It recommended no change: keep `87/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, blank optional emitter position, and blank target-level C++. It also recommended no split, merge, reclassification, new child page, IDA repair, documentation repair, or `by-memory/-coverage-report.md` replacement row.

Current-state documentation checks from that pass:

- The active source queue was the 2026-06-14 Goal 2 no-owner memory verification pass, with `Agent-B002/research/executed/0003JG-SharedMapPaletteSuffixString-live-goal2-no-owner-pass.md` used only as prior evidence.
- Generated memory coverage listed this item as no-owner with owner `NONE`, emitters `0000L3`,`0000PB`, no target-level code, and a generated path under `auto-generated/NexusTK/map/MapPane.cpp`. This generated path is routing evidence only and does not make [UID:0000L3][MapPane](by-file/MapPane.md) the canonical owner.
- [UID:0000L3][MapPane](by-file/MapPane.md) was `89/85` with `NexusTK/map/` file ownership, and [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) was `90/86` with `NexusTK/map/` file ownership. Both are valid source-use emitters, but neither proves a single declaration owner.
- [UID:0000MA][Palette](by-file/Palette.md) and [UID:0000MB][PaletteLib](by-file/PaletteLib.md) remain rejected as owners because their docs cover palette manager policy, `g_pPaletteLib`, named palette loading, slots, and filters, not declaration or construction of this exact suffix.
- [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) remains a non-emitting split index. Its children intentionally differ: this `.PAL` child is shared; [UID:0003JH][0x0061ea30-0x0061ea48.MapPaneWorldMapNameComparisonStrings](by-memory/0x0061ea30-0x0061ea48.MapPaneWorldMapNameComparisonStrings.md) is MapPane-only; [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md) is shared; [UID:0003JJ][0x0061ea50-0x0061ea70.MapPaneWorldMapNameThreeFourStrings](by-memory/0x0061ea50-0x0061ea70.MapPaneWorldMapNameThreeFourStrings.md) is MapPane-only.

IDA MCP facts from the goal2 pass:

- `server_health` for session `a001_goal2_class_batch` reported `NexusTK.exe`, image base `0x00400000`, input `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, ready auto-analysis, ready Hex-Rays, and ready strings cache.
- `get_bytes 0x0061ea24 size 0x4c` decoded the complete parent island as `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, `WM4`, followed by successor `%s\Mscfg.dll` at `0x0061ea70`.
- `get_bytes 0x0061ea24 size 0x0c` returned `2e 00 50 00 41 00 4c 00 00 00 00 00`, confirming UTF-16LE `.PAL`, null terminator, and alignment. `int_convert.py` verified `0x0c` as decimal `12`; the parent island length `0x4c` as decimal `76`; and the report's unrelated `0x2710` check as decimal `10000`.
- `get_string` decoded the sibling boundaries as `.PAL` at `0x0061ea24`, `wm` at `0x0061ea30`, `WM` at `0x0061ea38`, `wm2` at `0x0061ea40`, `WM2` at `0x0061ea48`, `wm3` at `0x0061ea50`, `WM3` at `0x0061ea58`, `wm4` at `0x0061ea60`, `WM4` at `0x0061ea68`, and `%s\Mscfg.dll` at `0x0061ea70`.
- `lookup_funcs` resolved `0x00510960`/`0x00510b57`/`0x00511041` to `sub_510960` size `0x9e7`, `0x00511db0` to `sub_511DB0` size `0xa8a`, `0x005c2ac0`/`0x005c2eae` to `sub_5C2AC0` size `0x7ea`, and `0x005c5200` to `sub_5C5200` size `0x1d0`; the target and successor string addresses are not functions.
- `xrefs_to 0x0061ea24` returned exactly three direct refs: `0x00510b57` and `0x00511041` in MapPane `sub_510960`, and `0x005c2eae` in WorldMapPane `sub_5C2AC0`.
- Sibling xref profiles support the current split: `0x0061ea30`, `0x0061ea38`, `0x0061ea40`, `0x0061ea50`, `0x0061ea58`, `0x0061ea60`, and `0x0061ea68` are MapPane-only refs from `sub_511DB0`; `0x0061ea48` has refs from MapPane `sub_511DB0` and WorldMapPane helper `sub_5C5200`; successor `%s\Mscfg.dll` at `0x0061ea70` belongs to a separate child with refs in the MapPane packet/helper neighborhood.
- Disassembly of `sub_510960` shows two `push offset aPal` instructions followed by `_wcscat_s` calls at `0x00510b68` and `0x00511052`. Hex-Rays shows MapPane copying a map/resource base name and appending `L".PAL"` at those source-use sites.
- Disassembly of `sub_5C2AC0` shows `0x005c2eae push offset aPal`, a call through `sub_584470`, and then palette lookup through `sub_543D70`. Hex-Rays shows WorldMapPane appending the same suffix to a world-map resource/name value before handing the completed name to the palette/resource load path.
- `analyze_component` over `0x00510960`, `0x00511db0`, `0x005c2ac0`, and `0x005c5200` reported shared global `0x0061ea24` used by `sub_510960` and `sub_5C2AC0`, shared sibling `0x0061ea48` used by `sub_511DB0` and `sub_5C5200`, and call edge `sub_511DB0 -> sub_5C2AC0`. That call edge proves MapPane can drive WorldMapPane creation, but it does not make the WorldMapPane constructor-family implementation MapPane-owned.

Raw PE evidence from `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`:

- MD5 was `4247e04e20b65d6414c7238aa8ff5515`; image base was `0x00400000`.
- `.rdata` maps raw `0x20ba00-0x26ac00` to RVA `0x20d000-0x26c200`.
- The target maps to file offset `0x21d424`, and the full parent island `0x0061ea24-0x0061ea70` also starts at raw `0x21d424`.
- The full parent island byte sequence occurs exactly once at file offset `0x21d424` / VA `0x0061ea24`, but standalone `.PAL` bytes occur 69 times in the executable. The suffix spelling alone is therefore not ownership evidence.
- The exact target VA immediate `0x0061ea24` occurs exactly three times, at operand VAs `0x00510b58`, `0x00511042`, and `0x005c2eaf`, matching the IDA instruction-start xrefs at `0x00510b57`, `0x00511041`, and `0x005c2eae`.
- The target RVA immediate `0x0021ea24` has zero hits, and RSDS, NB10, `.pdb`, and `.cpp` source/debug breadcrumbs all had zero hits in the checked executable.

The goal2 pass's ranked analysis matches the current page: `CANONICAL_OWNER:NONE` plus emitters `0000L3,0000PB` is the strongest least-overclaiming model; forced MapPane ownership is weakened by direct WorldMapPane use; forced WorldMapPane ownership is weakened by two direct MapPane uses; Palette/PaletteLib ownership confuses a caller-provided suffix with palette-manager implementation; the split index is not a source owner; and inventing a shared constants file has no source/debug/table/initializer evidence. The only remaining uncertainty is original source declaration style, and current owner/emitter rules handle that uncertainty with no canonical owner and two source-use emitters.

## Post-Migration Recheck Addendum

B001's archived `0003JG-SharedMapPaletteSuffixString-post-migration-recheck` pass revalidated the already-accepted route after the post-migration generated/report state had settled. It recommended no metadata, score, split, merge, reclassification, IDA repair, coverage-row, generated-row, or standalone C++ change. The exact retained state is `COMPLETION:87`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, blank optional emitter position, and blank target-level C++.

Current generated/report disposition from that pass:

- Manual `by-memory/-coverage-report.md` text was already accurate: it should continue to describe this as exact UTF-16 `.PAL` suffix data with no canonical owner, emitters `0000L3,0000PB`, direct MapPane refs at `0x00510b57`/`0x00511041`, direct WorldMapPane ref at `0x005c2eae`, no PaletteLib owner, no split, no merge, and no reclassification.
- Generated no-owner/source-output rows may display `auto-generated/NexusTK/map/MapPane.cpp` as the first surfaced path. That output-path display is routing metadata only; it does not make MapPane the canonical owner and does not remove WorldMapPane as an emitter.
- The parent [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) may still appear in scanner warning contexts as a non-emitting split index, but that warning does not change this exact child's ownership/emitter decision.

IDA MCP session `b001_0003gy` facts from the post-migration pass:

- The live IDB was `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64` for input `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, image base `0x00400000`, with auto-analysis, Hex-Rays, and strings cache ready.
- `get_bytes 0x0061ea24 size 0x4c` decoded the complete local string island as `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, and `WM4`; the successor string at `0x0061ea70` remains `%s\Mscfg.dll`.
- `get_bytes 0x0061ea24 size 0x0c` returned `2e 00 50 00 41 00 4c 00 00 00 00 00`, proving this page covers the UTF-16LE `.PAL` suffix, null terminator, and zero-word alignment exactly.
- `get_string` decoded sibling boundaries as `.PAL` at `0x0061ea24`, `wm` at `0x0061ea30`, `WM` at `0x0061ea38`, `wm2` at `0x0061ea40`, `WM2` at `0x0061ea48`, `wm3` at `0x0061ea50`, `WM3` at `0x0061ea58`, `wm4` at `0x0061ea60`, and `WM4` at `0x0061ea68`.
- `xrefs_to 0x0061ea24` returned exactly the three modeled direct data refs: `0x00510b57` and `0x00511041` in MapPane `sub_510960`, plus `0x005c2eae` in WorldMapPane `sub_5C2AC0`.
- `lookup_funcs` confirmed `sub_510960` at `0x00510960` size `0x9e7`, `sub_511DB0` at `0x00511db0` size `0xa8a`, `sub_5C2AC0` at `0x005c2ac0` size `0x7ea`, `sub_5C5200` at `0x005c5200` size `0x1d0`, and PaletteLib-style lookup `sub_543D70` at `0x00543d70` size `0xce`. The target address is data, not a function.
- `analyze_component` over `0x00510960`, `0x00511db0`, `0x005c2ac0`, and `0x005c5200` reported `0x0061ea24` accessed by `sub_510960` and `sub_5C2AC0`, sibling `0x0061ea48` accessed by `sub_511DB0` and `sub_5C5200`, and call edges including `sub_511DB0 -> sub_5C2AC0` and `sub_5C2AC0 -> sub_543D70`. These edges explain how completed names flow to palette lookup code; they do not transfer suffix ownership to MapPane or PaletteLib.
- Scoped instruction queries found operand `0x0061ea24` only in the expected MapPane scope `0x00510000-0x00513000` at `0x00510b57`/`0x00511041` and WorldMapPane scope `0x005c2000-0x005c6000` at `0x005c2eae`; the PaletteLib scope `0x00543000-0x00544500` had no operand match.
- Disassembly shows the two MapPane refs as `push offset aPal` followed by `_wcscat_s` calls while constructing MapPane-side palette names. The WorldMapPane ref is a `push offset aPal` feeding suffix/name construction through `sub_584470` before the completed name is passed to `sub_543D70`.
- `find_bytes "24 ea 61 00"` and `find_bytes "68 24 ea 61 00"` each found only the three modeled VA-immediate/push sites. `find_bytes "24 ea 21 00"` found no RVA-form refs, and source/debug regex checks for `.pdb`, `RSDS`, `NB10`, `.cpp`, `.cxx`, and `.h` found no declaration breadcrumb.

Raw PE facts from the same pass:

- The executable is PE machine `0x14c`; `.rdata` maps `0x0060d000-0x0066c200`; the target maps to file offset `0x21d424`.
- The exact target bytes are `2e 00 50 00 41 00 4c 00 00 00 00 00`. The full local prefix containing this `.PAL` child followed by `wm` occurs once at this address, but standalone `.PAL` text appears broadly in the binary. Textual duplicate count is therefore not an owner signal.
- Absolute target-VA immediates occur only at operand VAs `0x00510b58`, `0x00511042`, and `0x005c2eaf`, corresponding to instruction starts `0x00510b57`, `0x00511041`, and `0x005c2eae`; no RVA-form refs or hidden pointer-table refs were found.

Post-migration ownership ranking:

- The accepted route remains no canonical owner with MapPane and WorldMapPane emitters. Both source-use routes are proven, and neither route proves a single declaration owner for a pooled/shared literal.
- Forced MapPane ownership is rejected even though MapPane has two direct refs, because WorldMapPane independently appends the same suffix in constructor/resource initialization code.
- Forced WorldMapPane ownership is rejected because MapPane has two direct resource-name construction uses and owns adjacent MapPane-only comparison strings.
- Palette and PaletteLib ownership/emission is rejected because PaletteLib receives completed names through `sub_543D70`; it has no direct `0x0061ea24` ref and does not construct this suffix.
- The mixed split index, a synthetic shared constants module, DAT/resource ownership, and text-similar `.PAL` duplicates are rejected because they lack direct source declaration, source/debug, table, initializer, or exact-address evidence.

## B002 Current No-Owner Sweep Addendum

B002's archived `0003JG-SharedMapPaletteSuffixString-current-no-owner-sweep` report independently audited the same target after the current owner/emitter model was active. It recommended keeping `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, `COMPLETION:87`, `CONFIDENCE:93`, and a blank target-level C++ block. The report also recommended no by-memory edit, no `by-memory/-coverage-report.md` edit, no generated coverage edit, no split, no merge, and no reclassification. Its accepted interpretation is that this is a valid no-owner string-data case: the binary proves two MapPane source-use refs and one WorldMapPane source-use ref, but it does not prove a single source declaration owner or a real shared constants module.

B002 applied the current `by-structure.md` owner/emitter rule for pooled literals and small read-only data: a canonical owner requires source-declaration evidence stronger than "most xrefs", while emitters require proven source-use output routes. Under that rule, MapPane and WorldMapPane are both valid emitters, PaletteLib is not an emitter, and a synthetic standalone owner must not be invented merely to avoid `NONE`.

Current-documentation and generated-state checks from the B002 sweep:

- This page already had the correct header state: `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, `COMPLETION:87`, `CONFIDENCE:93`, and blank C++.
- [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) was confirmed as a non-emitting split index over exact children `0003JG`, `0003JH`, `0003JI`, and `0003JJ`.
- [UID:0000L3][MapPane](by-file/MapPane.md) already lists the neighboring MapPane-owned literal children but leaves this `.PAL` child and the shared `WM2` child parentless because WorldMapPane refs are material.
- [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) remains a separate map-domain source file for the scrollable world-map/travel UI and is a source-use emitter for this suffix.
- [UID:0000MB][PaletteLib](by-file/PaletteLib.md) owns palette manager APIs and `g_pPaletteLib`; it does not own caller-side map/world-map filename suffix literals.
- The proposed source tree places `MapPane.cpp` and `WorldMapPane.cpp` under `map/`, and PaletteLib under render/palette code. That placement supports separate source-use emitters, not a single PaletteLib declaration owner.
- Generated no-owner and emit-route rows were already consistent with the report: owner `NONE`, emitters `0000L3`,`0000PB`, no target-level code, and first surfaced output path under `auto-generated/NexusTK/map/MapPane.cpp`. That generated path is routing display, not proof that MapPane is the canonical owner.

B002 live IDA MCP evidence used session `b001_0003gy` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` / `NexusTK.exe.i64`, image base `0x00400000`, with auto-analysis, Hex-Rays, and the strings cache ready. Function lookup results were:

| Query | Live IDA result | B002 conclusion |
| --- | --- | --- |
| `0x00510960` | `sub_510960`, size `0x9e7` | MapPane helper family containing the two `.PAL` refs. |
| `0x00510b57` | inside `sub_510960` | first MapPane `.PAL` use. |
| `0x00511041` | inside `sub_510960` | second MapPane `.PAL` use. |
| `0x005c2ac0` | `sub_5C2AC0`, size `0x7ea` | WorldMapPane constructor/resource range. |
| `0x005c2eae` | inside `sub_5C2AC0` | WorldMapPane `.PAL` use. |
| `0x005c5200` | `sub_5C5200`, size `0x1d0` | WorldMapPane helper used by the sibling `WM2` shared case. |
| `0x00543d70` | `sub_543D70`, size `0xce` | PaletteLib lookup helper, not a literal declaration site. |
| `0x0061ea24` | not a function | target is data. |

Direct xrefs to `0x0061ea24` were exactly three: `0x00510b57` and `0x00511041` inside MapPane `sub_510960`, plus `0x005c2eae` inside WorldMapPane `sub_5C2AC0`. The local instruction windows in the report showed the two MapPane uses as `push offset aPal; ".PAL"` immediately after `_wcscpy_s`-style setup and before `lea` instructions for stack string buffers, and the WorldMapPane use as `push offset aPal; ".PAL"` after `call sub_582A90`, before `push 4`/`lea ecx, [ebp+arg_4]` string handling. Those instruction windows prove source-use emission in both file roots; they do not prove a shared declaration owner.

B002 also rechecked the sibling split-index inventory:

| Address | Decoded literal | Direct refs / ownership result |
| --- | --- | --- |
| `0x0061ea24` | `.PAL` | MapPane `sub_510960` and WorldMapPane `sub_5C2AC0`; this shared no-owner child. |
| `0x0061ea30` | `wm` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea38` | `WM` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea40` | `wm2` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea48` | `WM2` | MapPane `sub_511DB0` and WorldMapPane `sub_5C5200`; separate shared no-owner sibling. |
| `0x0061ea50` | `wm3` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea58` | `WM3` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea60` | `wm4` | MapPane `sub_511DB0`; MapPane-only sibling. |
| `0x0061ea68` | `WM4` | MapPane `sub_511DB0`; MapPane-only sibling. |

The sibling table supports the existing split instead of a merge: `.PAL` and `WM2` are shared children, while the neighboring `wm`/`WM`/`wm2` and `wm3`/`WM3`/`wm4`/`WM4` groups are MapPane-only children. Adjacent placement in `.rdata` is therefore not an ownership signal by itself.

PaletteLib was specifically rejected by B002 after decompiling `sub_543D70`. That helper takes a caller-provided `wchar_t *Source`, compares it against loaded palette names using `wcscmp`, may call a palette load helper, and returns a palette object or fallback object. It does not directly reference `0x0061ea24`, does not construct `.PAL`, and has broad caller fan-in as a shared palette lookup API. PaletteLib is therefore downstream of the completed map/world-map name and must not be listed as this literal's owner or emitter.

B002 raw PE evidence used the target VA `0x0061ea24`, image base `0x00400000`, target RVA `0x0021ea24`, and `.rdata` file offset `0x21d424`. Bytes at `0x0061ea24-0x0061ea70` decoded as:

```text
0x0061ea24 ".PAL"
0x0061ea30 "wm"
0x0061ea38 "WM"
0x0061ea40 "wm2"
0x0061ea48 "WM2"
0x0061ea50 "wm3"
0x0061ea58 "WM3"
0x0061ea60 "wm4"
0x0061ea68 "WM4"
```

The exact target bytes were `2e 00 50 00 41 00 4c 00 00 00 00 00`, covering UTF-16LE `.PAL`, a null terminator, and zero-word alignment before `wm`. A raw absolute-reference scan found exactly three `.text` hits for the target VA, corresponding to operand VAs `0x00510b58`, `0x00511042`, and `0x005c2eaf` and instruction starts `0x00510b57`, `0x00511041`, and `0x005c2eae`. The scan found no RVA-form hits for `0x0021ea24`. The report also counted many duplicate `.PAL` spellings in the executable - 119 terminator-inclusive wide `.PAL` hits and 69 padded standalone `.PAL` pattern hits under a narrower pattern - so textual suffix spelling is not unique enough to prove a source owner.

B002's accepted and rejected ownership results:

- Accepted: keep no canonical owner and emit through [UID:0000L3][MapPane](by-file/MapPane.md) plus [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md). Both source-use routes are directly proven.
- Rejected: MapPane sole ownership. Two MapPane refs are not declaration proof because WorldMapPane has an independent direct constructor/resource ref.
- Rejected: WorldMapPane sole ownership. Its direct ref is material, but MapPane has two independent direct resource-name construction refs and owns adjacent MapPane-only strings.
- Rejected: Palette, PaletteLib, DAT/resource, by-type, by-global, or packaged-resource ownership. This is a UTF-16 source suffix literal used before palette-manager lookup, not a palette payload, resource record, enum/table, or state global.
- Rejected: [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) as owner. It is a non-emitting mixed-child split index, not a source emitter.
- Rejected: a synthetic shared constants file such as `MapPaletteSuffixConstants` or `SharedResourceSuffixLiterals`. No named global, initializer object, table abstraction, PDB/source path, debug metadata, or broader repeated shared-constant source unit proves that such a file existed.

No split, merge, name repair, type repair, or range repair follows from the B002 sweep. The current range already isolates the `.PAL` literal plus terminator/alignment and ends before `L"wm"` at `0x0061ea30`; `SharedMapPaletteSuffixString` remains an accurate owner-neutral documentation name.

B002's C++ implication matches the current blank target-level C++: do not introduce a standalone helper, external binary helper, shared constants module, or PaletteLib-owned declaration for this page. Future MapPane and WorldMapPane first-draft C++ should spell the ordinary source literal `L".PAL"` locally where each file constructs its palette filename or lookup name.

## B002 Fresh No-Owner Pass Audit

The archived B002 report `0003JG-SharedMapPaletteSuffixString-fresh-no-owner-pass` was incorporated as a no-change Rule 26 closeout. Its final recommendation is accepted: keep `COMPLETION:87`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, blank `EMITTER_POSITION_OPTIONAL`, and blank target-level reconstruction C++. No split, merge, rename, IDA repair, owner change, emitter change, generated-row repair, or shared coverage-row repair is required.

Report scope and current-state claims:

| B002 fresh-pass claim | Rule 26 disposition |
| --- | --- |
| The active source queue row was the 2026-06-14 remaining no-owner sweep for `0003JG`, and the previous B001 report was used as evidence only. | Incorporated. The accepted route does not depend on B001 as authority; the report rechecked target docs, generated memory/file coverage, split/sibling pages, MapPane, WorldMapPane, PaletteLib, IDA, and raw PE evidence. |
| Current generated memory coverage already described this page as no-owner with emitters `0000L3` and `0000PB`, no target-level code, and first surfaced output path under `auto-generated/NexusTK/map/MapPane.cpp`. | Incorporated. The surfaced MapPane path is routing display, not canonical ownership proof, and it does not remove WorldMapPane from the emitter set. |
| [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) is the non-emitting split index over `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, and `WM4`. | Incorporated. This parent remains non-reconstructable and non-emitting because the island contains shared `.PAL`, MapPane-only groups, shared `WM2`, and more MapPane-only strings with different owner/emitter results. |
| [UID:0000L3][MapPane](by-file/MapPane.md) and [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) are both valid generated file roots under `NexusTK/map/`. | Incorporated. MapPane is the emitter for the two `sub_510960` appends; WorldMapPane is the emitter for the `sub_5C2AC0` constructor/resource append. Neither source-use route proves a single declaration owner. |
| [UID:0000MB][PaletteLib](by-file/PaletteLib.md) is downstream for completed palette names, not the source owner or emitter of this suffix. | Incorporated. `sub_543D70` receives a caller-provided `wchar_t *Source`, compares and loads palette entries, and has no direct ref to `0x0061ea24` or suffix-construction behavior. |

Fresh IDA evidence retained from session `a001_goal2_class_batch`:

| Evidence point | B002 finding retained here |
| --- | --- |
| IDB state | Input `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, IDB `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`, image base `0x00400000`, auto-analysis ready, Hex-Rays ready. |
| Target bytes | `get_bytes 0x0061ea20 size 96` included `2e 00 50 00 41 00 4c 00 00 00 00 00` at `0x0061ea24`, decoding as UTF-16LE `.PAL` plus terminator/alignment. IDA names the item `aPal`; the nearby names are `aWm`, `aWm_0`, `aWm2`, `aWm2_0`, `aWm3`, `aWm3_0`, `aWm4`, and `aWm4_0`. |
| Direct target refs | Exactly three target refs were found: `0x00510b57` and `0x00511041` in MapPane `sub_510960`, plus `0x005c2eae` in WorldMapPane `sub_5C2AC0`. Each instruction is `push offset aPal; ".PAL"`. |
| Interior and neighbor refs | `0x0061ea2c` has no refs; `0x0061ea30`, `0x0061ea38`, `0x0061ea40`, `0x0061ea50`, `0x0061ea58`, `0x0061ea60`, and `0x0061ea68` are MapPane-only sibling refs from `sub_511DB0`; `0x0061ea48` is a separate shared `WM2` sibling with refs from MapPane `sub_511DB0` and WorldMapPane `sub_5C5200`. |
| Function membership | `0x00510960`, `0x00510b57`, and `0x00511041` resolve to `sub_510960` size `0x9e7`; `0x00511db0` resolves to `sub_511DB0` size `0xa8a`; `0x005c2ac0` and `0x005c2eae` resolve to `sub_5C2AC0` size `0x7ea`; `0x005c5200` resolves to `sub_5C5200` size `0x1d0`; `0x00543d70` resolves to `sub_543D70` size `0xce`; `0x0061ea24` is data, not a function. |
| Decompiler behavior | `sub_510960` appends `L".PAL"` twice while building MapPane palette filenames. `sub_5C2AC0` appends `L".PAL"` through the local wide-string builder, materializes the name, and passes it to `sub_543D70`. `sub_543D70` compares and possibly loads a caller-provided name, but does not construct or directly reference the suffix literal. |
| Component analysis | The analyzed component over `sub_510960`, `sub_511DB0`, `sub_5C2AC0`, `sub_5C5200`, and `sub_543D70` reported `0x0061ea24` accessed by `sub_510960` and `sub_5C2AC0`, `0x0061ea48` accessed by `sub_511DB0` and `sub_5C5200`, and call edges such as `sub_511DB0 -> sub_5C2AC0` and `sub_5C2AC0 -> sub_543D70`. Those call edges explain name flow to palette lookup but do not transfer declaration ownership to MapPane or PaletteLib. |

Fresh raw PE evidence retained from B002:

| Raw PE check | Result |
| --- | --- |
| Executable identity | `NexusTK.exe`, file size `0x28e200`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, image base `0x00400000`. |
| Section mapping | `.text` VA `0x00401000` raw `0x00000400`; `.rdata` VA `0x0060d000` raw `0x0020ba00`; target VA `0x0061ea24` maps to raw `0x0021d424`. |
| Target bytes | `2e 00 50 00 41 00 4c 00 00 00 00 00`, decoded as `.PAL`. |
| Text uniqueness | The full 12-byte `.PAL` target pattern appeared 69 times and UTF-16LE `.PAL` plus null appeared 119 times, so text identity is duplicated and cannot prove a source owner. |
| Exact address references | `push 0x0061ea24` appeared at raw `0x10ff57` / VA `0x00510b57`, raw `0x110441` / VA `0x00511041`, and raw `0x1c22ae` / VA `0x005c2eae`; absolute dword refs match the same operand sites. |
| Negative references | RVA dword `0x0021ea24` had zero hits, interior address `0x0061ea2c` had zero hits, successor `0x0061ea30` had one MapPane-only sibling ref, and shared-sibling `0x0061ea48` had two separate refs. |

B002's ownership and structural decisions are accepted:

- Keep `CANONICAL_OWNER:NONE` because direct source-use evidence is split between MapPane and WorldMapPane, while no source/debug metadata, named global, initializer object, pointer table, header/constants module, or unique object-file boundary proves a single declaration owner.
- Keep emitters `0000L3,0000PB`; removing either one would drop a proven source-use route.
- Reject MapPane canonical ownership even though MapPane has two direct refs. Ref count is not declaration ownership, and WorldMapPane has an independent constructor/resource ref.
- Reject WorldMapPane canonical ownership because MapPane has two independent resource-name construction refs and owns the neighboring MapPane-only comparison strings.
- Reject Palette, PaletteLib, DAT/resource, by-type, by-global, packaged-resource, split-index, and synthetic shared-constants ownership. The observed item is a UTF-16 source suffix literal used before palette lookup, not palette manager state, a resource payload, a mixed aggregate, an enum/table, or a proven source constants object.
- Reject splitting, widening, merging with [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md), merging back into [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md), non-reconstructable reclassification, and IDA rename/type repair. The current range exactly covers `.PAL` plus terminator/alignment, and `SharedMapPaletteSuffixString` is an accurate owner-neutral name.

The report's optional coverage replacement was provenance-only and is not required for correctness. The current shared coverage semantics should continue to say this is exact UTF-16 `.PAL` string data, reconstructable, no canonical owner, emitters `0000L3,0000PB`, direct MapPane refs at `0x00510b57` and `0x00511041`, direct WorldMapPane ref at `0x005c2eae`, no interior/RVA/PaletteLib direct owner refs, and no supported split, merge, reclassification, or IDA repair.

## B002 Live Goal 2 No-Owner Pass Audit

The archived B002 report `0003JG-SharedMapPaletteSuffixString-live-goal2-no-owner-pass` was incorporated as another no-change Rule 26 closeout. Its final recommendation is accepted: keep `COMPLETION:87`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, blank `EMITTER_POSITION_OPTIONAL`, blank target-level C++, no split, no merge, no reclassification, no new child pages, and no required shared coverage-row edit. The report treated prior reports and existing prose as leads only; the decision was revalidated from current documentation, live IDA MCP, and an independent PE byte/reference scan.

Current documentation checks retained from the live pass:

| Document or context | Live-pass conclusion |
| --- | --- |
| Target page [UID:0003JG] | Exact child `0x0061ea24-0x0061ea30`, decoded UTF-16 `.PAL`, current owner `NONE`, emitters `0000L3,0000PB`, and blank target C++ were confirmed. |
| Split index [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) | The parent remains a necessary non-emitting index over children with different source-use profiles. |
| Sibling [UID:0003JH][0x0061ea30-0x0061ea48.MapPaneWorldMapNameComparisonStrings](by-memory/0x0061ea30-0x0061ea48.MapPaneWorldMapNameComparisonStrings.md) | `wm`, `WM`, and `wm2` are MapPane-only, supporting separation from this shared `.PAL` suffix. |
| Sibling [UID:0003JI][0x0061ea48-0x0061ea50.SharedWorldMap2UpperString](by-memory/0x0061ea48-0x0061ea50.SharedWorldMap2UpperString.md) | The separate shared `WM2` child uses the same no-owner plus MapPane/WorldMapPane emitter pattern, but it is a different literal and xref set. |
| Sibling [UID:0003JJ][0x0061ea50-0x0061ea70.MapPaneWorldMapNameThreeFourStrings](by-memory/0x0061ea50-0x0061ea70.MapPaneWorldMapNameThreeFourStrings.md) | `wm3`, `WM3`, `wm4`, and `WM4` are MapPane-only, so adjacency does not justify merging the island back together. |
| [UID:0000L3][MapPane](by-file/MapPane.md) | Valid emitter route for the two `sub_510960` source-use refs under `NexusTK/map/MapPane.cpp`; not a proven canonical declaration owner. |
| [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) | Valid emitter route for the direct `sub_5C2AC0` source-use ref under `NexusTK/map/WorldMapPane.cpp`; not a proven canonical declaration owner. |
| [UID:0000MB][PaletteLib](by-file/PaletteLib.md) and palette lookup docs | Downstream consumer of completed palette names; no direct `0x0061ea24` ref and no suffix-construction behavior. |

Live IDA MCP session `a001_goal2_class_batch` was against `NexusTK.exe` from `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, image base `0x00400000`, with auto-analysis, Hex-Rays, and the strings cache ready. The report reconfirmed:

| Check | Live result |
| --- | --- |
| Target bytes | `2e 00 50 00 41 00 4c 00 00 00 00 00`, decoding as UTF-16 `.PAL`, a terminator, and alignment. |
| Parent island | `get_bytes 0x0061ea24 size 0x4c` shows `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, and `WM4`; successor `%s\Mscfg.dll` begins at `0x0061ea70`. |
| Function membership | `0x00510b57` and `0x00511041` are in MapPane helper `sub_510960` at `0x00510960`, size `0x9e7`; `0x005c2eae` is in WorldMapPane constructor-family function `sub_5C2AC0` at `0x005c2ac0`, size `0x7ea`; `0x0061ea24` is data, not a function. |
| Direct target xrefs | Exactly three refs to `0x0061ea24`: `0x00510b57`, `0x00511041`, and `0x005c2eae`. |
| Instruction text | All three target refs are direct `push offset aPal; ".PAL"` operands. |
| Decompiler context | `sub_510960` copies a map/resource base name and appends `L".PAL"` in two MapPane branch variants; `sub_5C2AC0` appends `L".PAL"` before passing a completed name to palette/resource lookup. |
| Component analysis | `0x0061ea24` is accessed by `sub_510960` and `sub_5C2AC0`; `0x0061ea48` is a separate shared sibling accessed by `sub_511DB0` and `sub_5C5200`; `sub_511DB0 -> sub_5C2AC0` explains MapPane-to-WorldMapPane creation flow without collapsing declaration ownership. |

Independent PE evidence retained from the live pass:

| Raw PE check | Result |
| --- | --- |
| Executable MD5 | `4247e04e20b65d6414c7238aa8ff5515`. |
| Section mapping | `.text` raw `0x400-0x20ba00` maps to RVA `0x1000-0x20c600`; `.rdata` raw `0x20ba00-0x26ac00` maps to RVA `0x20d000-0x26c200`. |
| Full parent-island bytes | The `.PAL, wm, WM, wm2, WM2, wm3, WM3, wm4, WM4` sequence has one full-island hit at file offset `0x21d424`, mapping to VA `0x0061ea24`. |
| Target VA immediate | `0x0061ea24` appears as exactly three dword operand hits at file offsets `0x10ff58`, `0x110442`, and `0x1c22af`, mapping to operand VAs `0x00510b58`, `0x00511042`, and `0x005c2eaf`. |
| Target RVA immediate | `0x0021ea24` has no hits. |
| Generic text caveat | Generic UTF-16 `.PAL` extension occurrences are numerous and are not ownership evidence; only the exact full-island and target-address reference evidence is used for this page. |

The accepted owner/emitter decision remains ownerless with two emitters. `CANONICAL_OWNER:NONE` is the best current result because the exact object is a pooled/shared UTF-16 suffix with material direct uses in both MapPane and WorldMapPane and no source metadata, initializer table, named global declaration, PDB/source path, or exclusive data-owner evidence proves a single declaration owner. `EMITTER_UIDS:0000L3,0000PB` remains required because the two MapPane refs and the one WorldMapPane ref are concrete source-use routes.

Rejected alternatives from the live pass:

- Reject `CANONICAL_OWNER:0000L3`: MapPane has two direct refs and adjacent MapPane-only sibling strings, but WorldMapPane directly uses the same exact `.PAL` object.
- Reject `CANONICAL_OWNER:0000PB`: WorldMapPane has a direct constructor-family use, but MapPane has two independent uses of the same object.
- Reject `CANONICAL_OWNER:0003IX`: the parent is a documentation split index, not a source owner or generated output root.
- Reject PaletteLib, Palette, DAT/resource, by-type, by-global, packaged-resource, and synthetic shared-constants ownership: no direct suffix declaration, direct target xref, initializer object, source metadata, or source unit proves such an owner.
- Reject merging back into the full island: adjacent children have different xref profiles and meanings.
- Reject non-reconstructable reclassification: this is source-declared/generated-binary string data with proven source-use contexts.

The report's optional coverage replacement was provenance-only. The current coverage row may remain unchanged because it already records exact UTF-16 `.PAL` string data, `CANONICAL_OWNER:NONE`, `EMITTER_UIDS:0000L3,0000PB`, direct MapPane refs at `0x00510b57`/`0x00511041`, direct WorldMapPane ref at `0x005c2eae`, and no justified PaletteLib/shared-constants owner, split, merge, or reclassification.

## B003 Post-Migration Recheck Audit

Agent-B003's archived `0003JG-SharedMapPaletteSuffixString-post-migration` report was incorporated as the ownership/emitter transition pass for this page. The report snapshot still described the old post-migration problem state with blank `EMITTER_UIDS` and a coverage row saying "parent blank"; its required action was to preserve `CANONICAL_OWNER:NONE` but add the proven output routes `EMITTER_UIDS:0000L3,0000PB`. That action has already been applied, and this section records the report-specific evidence so the by-memory target no longer depends on the archived B-agent report.

| B003 post-migration claim | Rule 26 disposition |
| --- | --- |
| The target is exact source-authored/linker-pooled UTF-16LE `.PAL` suffix data at `0x0061ea24-0x0061ea30`, with bytes `2e 00 50 00 41 00 4c 00 00 00 00 00`. | Incorporated. The range remains reconstructable source string data and should not be reclassified as a mixed index or ignored artifact. |
| The old blank-emitter state was invalid under the separated owner/emitter model because direct source-use contexts exist in both MapPane and WorldMapPane. | Incorporated. `CANONICAL_OWNER:NONE` records no proven declaration owner; `EMITTER_UIDS:0000L3,0000PB` records source-output routes. |
| Live IDA session `b001_0003gy` found exactly three direct xrefs: `0x00510b57` and `0x00511041` in MapPane `sub_510960`, and `0x005c2eae` in WorldMapPane `sub_5C2AC0`; all are `push offset aPal ; ".PAL"` sites. | Incorporated. These three refs are the complete known source-use set for this exact address. |
| `sub_510960` is MapPane resource/name construction code, while `sub_5C2AC0` is WorldMapPane constructor/resource initialization code; PaletteLib `sub_543D70` receives completed names and has no direct suffix xref. | Incorporated. MapPane and WorldMapPane are emitters; PaletteLib is downstream context only. |
| Raw PE scanning found the target at file offset `0x21d424`, exact target VA immediates only at the three modeled code sites, no target RVA immediates, and many duplicate `.PAL` spellings elsewhere. | Incorporated. Exact-address evidence proves the two emitters, while text duplication rejects a unique text-based owner. |
| The surrounding island decodes as `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, `WM4`, then successor `%s\\Mscfg.dll`; sibling xref profiles differ. | Incorporated. No merge with [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) or neighboring children is justified. |
| Forced MapPane ownership, forced WorldMapPane ownership, Palette/PaletteLib ownership, DAT/resource ownership, the mixed split index, and a synthetic shared suffix/constants file were all rejected. | Incorporated. Each candidate either covers only one source-use family, is downstream of completed names, is only physical adjacency, or lacks source/debug/table/initializer evidence. |
| No final C++ should be entered on this page during the report pass. | Accepted. Future MapPane/WorldMapPane function drafts should append local `L".PAL"` at use sites; this data page stays a pooled-literal record. |

## B003 Current Goal 2 No-Owner Pass Audit

Agent-B003's archived `0003JG-SharedMapPaletteSuffixString-current-goal2-pass` report was incorporated as another no-change Rule 26 audit. Its recommendation is accepted with no metadata, split, merge, reclassification, IDA, coverage, or target-level C++ change: keep `COMPLETION:87`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000L3,0000PB`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++. The report's confidence was `93/100` for the no-owner-with-emitters model; the unresolved uncertainty is only original source declaration style, not the observed byte range or source-use routes.

Current-state and rule claims retained from the B003 pass:

| B003 claim | Rule 26 disposition |
| --- | --- |
| Generated memory coverage listed `0003JG` as owner `NONE`, emitters `0000L3` and `0000PB`, with first surfaced output under `auto-generated/NexusTK/map/MapPane.cpp`. | Incorporated. The generated path is emitter-order display, not MapPane canonical ownership. |
| [UID:0000L3][MapPane](by-file/MapPane.md) and [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md) remain valid file-root emitters for `NexusTK/map/`. | Incorporated. MapPane covers the two `sub_510960` source-use refs; WorldMapPane covers the direct `sub_5C2AC0` source-use ref. |
| [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md) is a non-emitting split index, not an owner. | Incorporated. The island mixes shared `.PAL`, MapPane-only `wm`/`WM`/`wm2`, shared `WM2`, and MapPane-only `wm3`/`WM3`/`wm4`/`WM4`, so the current child split is required. |
| [UID:0000MB][PaletteLib](by-file/PaletteLib.md) is relevant downstream palette-manager context but not a source owner or emitter for this suffix. | Incorporated. `sub_543D70` receives a caller-supplied `wchar_t *Source`, has no direct `0x0061ea24` ref, and does not append `.PAL`. |
| `by-structure.md` and `inference_research.md` require separating semantic declaration ownership from source-use output routing and warn against inferring ownership from `.rdata` adjacency or string pooling alone. | Incorporated. This is why the page remains ownerless while still reconstructable and emitting through both proven source-use roots. |

B003 live IDA evidence used session `b001_0003gy` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` / `.i64`, image base `0x00400000`, with auto-analysis, Hex-Rays, and string cache ready. The pass used `idb_list`, `server_health`, `lookup_funcs`, `get_bytes`, `get_string`, `xrefs_to`, `analyze_component`, `analyze_function`, `decompile`, `find_regex`, and `find_bytes`. Its retained facts are:

| Evidence point | B003 current result |
| --- | --- |
| Function map | `0x00510b57` and `0x00511041` are in MapPane `sub_510960` size `0x9e7`; `0x005c2eae` is in WorldMapPane `sub_5C2AC0` size `0x7ea`; `0x00511db0` is `sub_511DB0` size `0xa8a`; `0x005c5200` is `sub_5C5200` size `0x1d0`; `0x00543d70` is `sub_543D70` size `0xce`; `0x0061ea24`, `0x0061ea30`, and `0x0061ea48` are data, not functions. |
| String island | `get_bytes 0x0061ea24 size 0x4c` and `get_string` decode `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, `WM4`, then successor `%s\\Mscfg.dll` at `0x0061ea70`. |
| Target xrefs | `xrefs_to 0x0061ea24` returned exactly `0x00510b57`, `0x00511041`, and `0x005c2eae`; `find_bytes "24 EA 61 00"` found only their operand dwords at `0x00510b58`, `0x00511042`, and `0x005c2eaf`. |
| Caller context | `xrefs_to 0x00510960` found `sub_507C90 -> sub_510960` at `0x00507ffa`, placing the two MapPane refs in MapPane packet/helper flow. `xrefs_to 0x005c2ac0` found `0x0050f2fa` and `0x005126f9`, placing the WorldMapPane ref in world-map construction/show flow. |
| Component analysis | `0x0061ea24` / `aPal` is accessed by `sub_510960` and `sub_5C2AC0`; sibling `0x0061ea48` / `aWm2_0` is accessed by `sub_511DB0` and `sub_5C5200`. Internal edges such as `sub_511DB0 -> sub_5C2AC0` and `sub_5C2AC0 -> sub_543D70` explain flow without collapsing ownership. |
| Negative source evidence | Source/debug regex checks for `.pdb`, `RSDS`, `NB10`, `.cpp`, `.cxx`, and `.h` found no declaration breadcrumb. |

B003 raw PE evidence mapped the target to `.rdata` file offsets `0x21d424-0x21d430`; bytes `2e 00 50 00 41 00 4c 00 00 00 00 00` decode as UTF-16LE `.PAL` plus two zero words. The full local island decodes as `.PAL`, `wm`, `WM`, `wm2`, `WM2`, `wm3`, `WM3`, `wm4`, and `WM4`. The 12-byte target pattern appeared 69 times and the wide `.PAL` string with terminator appeared 119 times, so the text spelling is not unique owner evidence. Exact target VA `0x0061ea24` appeared in `.text` only at the three modeled absolute immediate sites; no RVA-form or rel32 target references were found.

Accepted and rejected B003 ownership results:

- Accepted: keep `CANONICAL_OWNER:NONE` with emitters `0000L3,0000PB`. Direct source-use routes are proven and no single source declaration owner is recovered.
- Rejected: canonical MapPane ownership. It has two refs and owns adjacent MapPane-only siblings, but WorldMapPane has a direct material ref.
- Rejected: canonical WorldMapPane ownership. Its constructor/resource use is direct, but MapPane has two independent uses.
- Rejected: PaletteLib ownership/emission. It is downstream lookup/load code that receives completed names and has no target ref.
- Rejected: synthetic `MapPaletteConstants` / `MapWorldPaletteConstants`, split-index ownership, DAT/resource/by-type/by-global/package ownership, split, merge, and non-reconstructable reclassification. None is supported by source/debug, table, initializer, exact-address, or range evidence.

The report recommended no shared coverage replacement. The current coverage row can stay unchanged because it already states the exact `.PAL` data, no-owner/two-emitter route, direct MapPane and WorldMapPane refs, PaletteLib rejection, and no split/merge/reclassification result.

## Draft C++ Implication

This page should not introduce a standalone helper, synthetic constants module, or PaletteLib-owned declaration. First-draft C++ in the MapPane and WorldMapPane emitters should use a normal wide string literal at the point of filename construction, for example appending `L".PAL"` to the map/world-map resource base name. The standalone by-memory page remains a source-declared/generated-binary data record for the pooled literal and should not expand into unrelated filename-building code.

## Cross-References

- [UID:0003IX][0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings](by-memory/0x0061ea24-0x0061ea70.MapWorldPaletteResourceStrings.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md)
- [UID:00035I][0x00631490-0x00631528.WorldMapPaneResourceStrings](by-memory/0x00631490-0x00631528.WorldMapPaneResourceStrings.md)

## Changes

- 2026-06-12 C001 Batch C001-021: Created exact shared suffix child from the former mixed Map/WorldMap palette string range after live IDA MCP reconfirmed bytes, decoded text, three direct refs, and cross-owner use.
- 2026-06-13 B003 post-migration recheck kept `CANONICAL_OWNER:NONE` but changed `EMITTER_UIDS` to `0000L3,0000PB`. Live IDA MCP and raw PE evidence confirm exact source-use refs from MapPane `sub_510960` at `0x00510b57`/`0x00511041` and WorldMapPane constructor-family `sub_5C2AC0` at `0x005c2eae`, while no PaletteLib, synthetic shared-owner, split-index, debug metadata, or single declaration-owner route is proven.
- 2026-06-22 Rule 26 post-migration closeout for archived B003 report `0003JG-SharedMapPaletteSuffixString-post-migration`: added dedicated report accounting for the old blank-emitter problem state, accepted `EMITTER_UIDS:0000L3,0000PB`, `b001_0003gy` IDA xrefs/function context, raw PE exact-address evidence, sibling-island/split rationale, rejected owner alternatives, and no standalone-C++ decision. Metadata, score, split, coverage, and target-level C++ remain unchanged.
- 2026-06-13 B001 second-pass audit kept `87/93`, `CANONICAL_OWNER:NONE`, and `EMITTER_UIDS:0000L3,0000PB`. Rule 26 closeout now preserves the report's full second-pass detail: live IDA state, sibling decode, exact `aPal` refs, PaletteLib negative, source/debug negative, raw PE section/offset bytes, 119 terminator-inclusive wide `.PAL` hits, exactly three target-address pushes/VA immediates, zero RVA/rel32 refs, ranked owner rejections, no coverage-row change, and no evidence for a single semantic declaration owner, split, merge, reclassification, or IDA repair.
- 2026-06-13 B001: Closed ownership review as researched/no-parent-change under the old combined parent model; the post-migration model now keeps canonical owner none while routing emission through both proven source-use file roots.
- 2026-06-22 Rule 26 goal2-verification closeout for archived B001 report `0003JG-SharedMapPaletteSuffixString-goal2-verification`: incorporated the report's current no-owner queue context, current generated/source-root checks, `a001_goal2_class_batch` IDA provenance, complete sibling xref profile, function-size lookups, instruction/decompiler/component evidence, raw PE MD5/section map/target offset/uniqueness and immediate scans, no source-debug breadcrumb result, ranked owner analysis, and explicit no-coverage/no-standalone-C++ recommendation. Metadata and owner/emitter route stay unchanged.
- 2026-06-22 Rule 26 post-migration recheck closeout for archived B001 report `0003JG-SharedMapPaletteSuffixString-post-migration-recheck`: incorporated the `b001_0003gy` session provenance, generated/report no-op disposition, current manual coverage-row semantics, exact PE/IDA direct-reference inventory, scoped instruction and byte-search negatives, raw PE section/offset facts, and the renewed owner/emitter rejection ranking. Metadata, scores, split, coverage, and C++ remain unchanged.
- 2026-06-22 Rule 26 current no-owner sweep closeout for archived B002 report `0003JG-SharedMapPaletteSuffixString-current-no-owner-sweep`: incorporated the report's current documentation/generated-state checks, by-structure owner/emitter standard, `b001_0003gy` IDA function lookup and three-ref inventory, local instruction windows, sibling split table, PaletteLib negative decompile, source/debug negative result, raw PE offset/byte/reference/count evidence, accepted dual-emitter no-owner route, rejected owner alternatives, no split/merge/name/range repair result, and blank target-level C++ implication. Metadata, coverage, and generated state remain unchanged.
- 2026-06-22 Rule 26 fresh no-owner pass closeout for archived B002 report `0003JG-SharedMapPaletteSuffixString-fresh-no-owner-pass`: added explicit report accounting for the active no-owner sweep context, generated route state, MapPane/WorldMapPane/PaletteLib support-doc checks, `a001_goal2_class_batch` IDA evidence, raw PE MD5/section/offset/reference facts, duplicate `.PAL` text counts, ranked owner rejection, structural no-op decision, provenance-only coverage disposition, and use-site-literal C++ implication. Metadata, score, owner, emitters, split, coverage, and target-level C++ remain unchanged.
- 2026-06-22 Rule 26 live Goal 2 no-owner closeout for archived B002 report `0003JG-SharedMapPaletteSuffixString-live-goal2-no-owner-pass`: added dedicated report accounting for the final no-change recommendation, current support-doc checks, live `a001_goal2_class_batch` IDA bytes/xrefs/function/decompiler/component evidence, raw PE MD5/section/offset/full-island/target-VA/RVA-scan facts, accepted no-owner/two-emitter route, rejected owner/split/merge/reclassification alternatives, provenance-only coverage disposition, and normal use-site `L".PAL"` C++ implication. Metadata, score, owner, emitters, split, coverage, and target-level C++ remain unchanged.
- 2026-06-22 Rule 26 current Goal 2 no-owner closeout for archived B003 report `0003JG-SharedMapPaletteSuffixString-current-goal2-pass`: added dedicated report accounting for the final no-change recommendation, `b001_0003gy` IDA tool/session provenance, generated/support-doc rule checks, exact string-island/xref/function/component facts, raw PE `.rdata` offset/text-duplicate/reference-form evidence, ranked owner rejection, no split/merge/reclassification/no-coverage disposition, and use-site literal C++ implication. Metadata, score, owner, emitters, split, coverage, and target-level C++ remain unchanged.
