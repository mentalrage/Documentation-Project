*** UID:0002ER | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingPane::AddIncomingMessage(ChattingMessage *message)
{
    unsigned char modifierState;
    ChattingMessage *clonedMessage;

    if (g_useEpfAssets == 1)
    {
        g_pEventMan->GetModifierState(&modifierState);
        clonedMessage = message->Clone();
        m_pChatMessageList->Append(1, &clonedMessage);

        if (!HasMouseCapture() &&
            (modifierState & kEventModifierShift) == 0)
        {
            SyncScrollbars();
            short scrollPosition = GetScrollPosition(0);
            ApplyScrollPosition(0, scrollPosition, GetScrollMax(0));
        }

        if (m_pChatMessageList->GetCount() > 1000)
        {
            ChattingMessage **oldestMessage =
                static_cast<ChattingMessage **>(
                    m_pChatMessageList->GetElementAt(0));
            delete *oldestMessage;
            m_pChatMessageList->RemoveAt(0, 1);
            SyncScrollbars();
        }
    }
    else
    {
        g_pEventMan->GetModifierState(&modifierState);
        clonedMessage = message->Clone();
        m_pChatMessageList->Append(1, &clonedMessage);
        SyncScrollbars();

        if (!HasMouseCapture() &&
            (modifierState & kEventModifierShift) == 0)
        {
            short scrollPosition = GetScrollPosition(0);
            ApplyScrollPosition(0, scrollPosition, GetScrollMax(0));
        }

        if (m_pChatMessageList->GetCount() > 1000)
        {
            ChattingMessage **oldestMessage =
                static_cast<ChattingMessage **>(
                    m_pChatMessageList->GetElementAt(0));
            delete *oldestMessage;
            m_pChatMessageList->RemoveAt(0, 1);

            short scrollPosition = GetScrollPosition(0);
            if (scrollPosition > 0)
            {
                ApplyScrollPosition(0, scrollPosition, scrollPosition - 1);
            }
            else
            {
                ApplyScrollPosition(0, scrollPosition, 0);
                InvalidateRect(&m_bounds);
            }

            SyncScrollbars();
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChattingPane::AddIncomingMessage body clones and appends one owned ChattingMessage pointer, suppresses bottom autoscroll while Shift or mouse capture is active, preserves distinct current/legacy synchronization and legacy trim-scroll correction, and removes/deletes the oldest entry only after count exceeds 1000; live IDA confirms exact bytes, callers, List/ScrollablePane/Pane contracts, source owner, and formal human C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047f370-0x0047f512 ChattingPane AddIncomingMessage

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: method/helper.
- Owner: [UID:00001X][ChattingPane](by-class/ChattingPane.md); prior `FolderTreePane` naming was generated owner pollution.
- Likely source placement: [UID:0000I5][Chatting](by-file/Chatting.md), not the real filesystem [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) control core.
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Autogen parent: [UID:00001X][ChattingPane](by-class/ChattingPane.md); B001 reclassified the stale `FolderTreePane` owner as generated owner pollution.
- Rebuild handling: source-authored `ChattingPane::AddIncomingMessage(ChattingMessage *)` method with an exact formal first-draft body and no observed EH frame in the modeled body.
- Confidence: very strong for exact range, callers, message/list ownership, Shift/capture autoscroll gate, branch-specific scroll behavior, 1000-message trim, direct ChattingPane owner route, and padding. The remaining cap is final compiler-equivalence and stripped lexical spelling, not an open behavior or source-placement question.
- Header disposition: this child intentionally emits no H text because [UID:00001X][ChattingPane](by-class/ChattingPane.md) already owns `void AddIncomingMessage(ChattingMessage *message);`; duplicating the declaration here would violate owner-level declaration placement.

## Behavior

This method clones the incoming polymorphic `ChattingMessage`, appends the resulting owned pointer to `m_pChatMessageList` at `this + 0x130`, conditionally scrolls to the newest message, and enforces a post-insert maximum of 1000 stored messages. `List` owns the four-byte pointer records; `ChattingPane` owns and deletes the pointed-to messages.

Both display-mode branches:

- call `g_pEventMan->GetModifierState(&modifierState)`, where producer `0x004a96a0` and [UID:00004L][Event](by-class/Event.md) prove bit `0x04` is `kEventModifierShift`;
- call virtual slot `+0x0c` on the incoming `ChattingMessage` interface to obtain a heap-owned deep clone; [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) resolves the concrete implementation as `ChattingMessage *ColorStringChattingMessage::Clone()`;
- call `List::Append(1, &clonedMessage)` through slot `+0x18`, where `1` is the number of four-byte pointer records to append, not an insertion index;
- suppress forced bottom scrolling while `HasMouseCapture()` is true or `modifierState & kEventModifierShift` is nonzero;
- trim the oldest entry when the list count at `list + 0x0c` exceeds `1000`.

When trimming, the method obtains element-record zero through `List::GetElementAt(0)`, interprets it as `ChattingMessage **`, deletes `*oldestMessage`, and removes exactly one record through `RemoveAt(0, 1)`. It appends first and trims once only when count is greater than 1000; it neither rejects the newest message nor loops.

The current-layout (`g_useEpfAssets == 1`) branch calls `SyncScrollbars()` only in the permitted-autoscroll path and after trimming. The legacy branch synchronizes immediately after append. Its trim path then reads `GetScrollPosition(0)`: positive signed positions are decremented by one through `ApplyScrollPosition(0, old, old - 1)`, while nonpositive positions are clamped to zero and inherited `m_bounds` is invalidated before a final sync. Axis `0` is vertical. Both branches use `GetScrollMax(0)` as the bottom target when autoscroll is allowed.

## Raw Code Evidence

The accepted B007 live read-only MCP pass used transport session `4643bab8-2dcd-4ceb-848c-a54b6b74325f`, IDB session `5a570ede`, and input `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. It reconfirmed the then-raw `sub_47F370` as a single `0x1a2`-byte modeled function over `[0x0047f370,0x0047f512)`, 152 instructions, 18 basic blocks, and exact body SHA256 `FEEBAED50A1C896EADF9F7954DE9A91E2A65A303EA172D63595430A902356A00`. First bytes are `55 8B EC 83 EC 08 8D 45`; final bytes are `5F 5E 8B E5 5D C2 04 00`.

That report-only pass recorded the historical pre-Gate-2B declaration `void __thiscall sub_47F370(__int128 *this, int a2)`, raw local `var_8` / Hex-Rays `v16` as `int`, raw `arg_0` / Hex-Rays `a2` as an integer argument, and `modifierState` as `unsigned __int8`. Those values remain prestate evidence rather than current identities.

Supervisor Gate 2B used prestate IDB SHA256 `8E1AB47A7FDE155EFAADCFFC7EBB431B6C7DC8B85D1DC45A24174182B1D7FAA7`, size `143189782` bytes, with byte-identical backup `E:\NTK\Resources\NexusTK\NexusTK.exe.i64.bak-UID0002ER-prestate-20260729-0106`. It declared only incomplete `ChattingMessage` ordinal `938` and incomplete `ColorStringChattingMessage` ordinal `939`; existing `ChattingPane` remained incomplete ordinal `440`. No full layout, inheritance, member, size, or alignment was invented in IDA.

Current persisted target readback is `void __thiscall ChattingPane__AddIncomingMessage(ChattingPane *this, ChattingMessage *message)`. Its function-regular comment is exactly `Clones and appends one owned chat message, conditionally scrolls to the newest entry unless Shift or mouse capture suppresses it, and removes the oldest entry above 1000 messages.` All address comments and repeatable comments remain blank. Hex-Rays models the clone-result storage as `ChattingMessage *clonedMessage`, and `Append` receives `&clonedMessage`; `modifierState` is unchanged. The alternate raw frame table still exposes the same storage as `var_8:int` at `+0x8`. That bounded normalization is intentional: after Hex-Rays became exact, the supervisor did not blindly reapply a conflicting rename/type through the alternate raw stack view.

The target modeled extent, first/final items, one-byte left alignment, fourteen-byte right alignment, and protected predecessor/successor names/types/comments remain unchanged. `idb_save` returned `ok:true`; persisted IDB SHA256 is `0FEABCD16E84D54EF66E40A9B69C10948E4AC580EEDE2D8C5AB91B392ABA0DF9`, size `143189857` bytes, last write `2026-07-29T01:07:24.1509766-04:00`. Supervisor IDA mutation catalog entry is `0346`.

Historical live IDA MCP on 2026-06-05 against input MD5 `4247e04e20b65d6414c7238aa8ff5515` reported `lookup_funcs 0x0047f370` as the then-current `sub_47F370`, size `0x1a2`, giving end-exclusive range `0x0047f512`. The preceding byte `0x0047f36f-0x0047f370` is `cc` padding after [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md). Address `0x0047f512` is not a function, [UID:0002ES][0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals](by-memory/0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals.md) starts at `0x0047f520`, and bytes `0x0047f512-0x0047f520` are fourteen `cc` alignment bytes.

Key disassembly:

```asm
0047F379  cmp     byte_66DA97, 1
0047F384  mov     ecx, dword_67A754
0047F391  call    sub_4A90B0
0047F39B  call    dword ptr [eax+0Ch]
0047F39E  mov     ecx, [edi+130h]
0047F3AF  call    dword ptr [eax+18h]
0047F3BC  call    sub_544A00
0047F3C5  call    sub_55E990
0047F3D7  call    sub_55E950
0047F3DA  call    sub_55E910
0047F3E8  call    sub_55EAE0
0047F3FA  cmp     dword ptr [edi+130h+0Ch], 3E8h
0047F407  call    dword ptr [eax+10h]
0047F411  call    dword ptr [eax]
0047F41F  call    dword ptr [eax+1Ch]
0047F424  call    sub_55E990
0047F431  call    sub_4A90B0
0047F43B  call    dword ptr [eax+0Ch]
0047F44F  call    dword ptr [eax+18h]
0047F454  call    sub_55E990
0047F45B  call    sub_544A00
0047F47A  call    sub_55E910
0047F488  call    sub_55EAE0
0047F49A  jle     short loc_47F50A
0047F4A0  call    dword ptr [eax+10h]
0047F4AD  call    dword ptr [eax]
0047F4BB  call    dword ptr [eax+1Ch]
0047F4C2  call    sub_55E950
0047F4D6  call    sub_55EAE0
0047F4E3  call    dword ptr [edx+20h]
0047F4E8  call    sub_55E990
0047F4FE  call    sub_55EAE0
0047F505  call    sub_55E990
```

IDA xrefs to this function start are exactly two call sites, both from [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md):

- `0x0047f2d8` in the current-layout branch.
- `0x0047f32b` in the fallback/old-layout branch.

The historical pre-rename `callees 0x0047f370` result reported `sub_4A90B0`, `sub_544A00`, `sub_55E950`, `sub_55E910`, `sub_55E990`, and `sub_55EAE0` beyond the indirect virtual calls. `xrefs_to` confirmed the local call sites to those scroll helpers, the two calls to `sub_4A90B0`, and only one data/vtable xref to the next scroll metric helper start at `0x00614d28`. Current Gate 2B identity for `0x0055e910` is `ScrollablePane_GetScrollMax`; the remaining historical names here preserve the dated evidence capture rather than assert current IDA naming.

## Source Shape Notes

The formal CPP block preserves the duplicated current/legacy branches because the binary duplicates modifier read, virtual clone, pointer-record append, and branch-specific synchronization. Hoisting common operations would be behavior-equivalent but would be less faithful to the observed compiler shape.

Accepted inherited identities are `ScrollablePane::GetScrollMax`, `GetScrollPosition`, `SyncScrollbars`, and `ApplyScrollPosition(axis, oldPosition, targetPosition)`. Pane dependencies are `HasMouseCapture()` and `InvalidateRect(&m_bounds)`. The message dependency is the abstract `ChattingMessage` Clone/MeasureLines/Draw interface documented on [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md). The source uses ordinary virtual calls, normal `delete`, and the existing generic fixed-record `List` interface; it does not preserve raw vtable dispatch or decompiler temporaries.

Historical assumptions that kept this emitter blank are now superseded: `FolderTreePaneAddIncomingMessage` was generated owner pollution, list argument `1` is an append count rather than index/position one, EventMan bit `0x04` is Shift rather than a generic interaction flag, and the ScrollablePane helper names are resolved by their current owner documentation. These statements remain recorded as history below but are not active blockers.

## Historical 2026-06-16 B001 Source-Quality Resolution

B001 rejects the generated `FolderTreePane` owner for this range. `0x0047f370` is best documented as `ChattingPane::AddIncomingMessage` or `ChattingPane::AppendIncomingMessage`: it clones the incoming `ColorStringChattingMessage` via virtual slot `+0x0c`, appends the owned clone into the ChattingPane message list at `this + 0x130` through list vslot `+0x18`, refreshes inherited ScrollablePane state, conditionally autoscrolls to the newest message, and enforces a post-insert maximum of 1000 stored messages.

The 2026-06-21 B006 clone pass resolves the `ColorStringChattingMessage` implementation behind this virtual call. For that concrete class, slot `+0x0c` / vtable VA `0x00615278` allocates a `0x1c` / 28-byte duplicate, deep-copies `m_text` through the constructor, preserves `m_foregroundColor`, `m_backgroundColor`, `m_useCustomRgb`, `m_customForegroundRgb`, and `m_customBackgroundRgb`, and returns a polymorphic `ChattingMessage *`. This confirms that this helper owns the returned heap clone in the list, not the temporary/source message object. The generic `ChattingMessage` base declaration remains inferred from RTTI and vtable use; do not demote the call to "uncalled" merely because the concrete clone has no ordinary direct xrefs.

The 1000-message trim is oldest-entry deletion, not rejection. After insertion, if `list->count > 1000`, the helper fetches index `0` through list vslot `+0x10`, calls the message deleting-destructor path when the entry is non-null, removes one element at index `0` through list vslot `+0x1c`, and refreshes scroll state. The old-layout branch additionally decrements/clamps the scroll position after trimming and invalidates the `this + 0x44` region when clamping to zero; the current-layout branch uses the shorter trim refresh path.

`0x004a90b0(dword_67A754, &flags)` was correctly separated from the cursor-position helper at `0x004a9090`, but B001's generic active-input/suppression interpretation of bit `0x04` is superseded by B007's producer-backed `kEventModifierShift` resolution. Likewise, the earlier unresolved append-index and helper-name wording is retained only as historical provenance.

## 2026-07-29 B007 Source-Quality Closure

- Exact owner/source route remains [UID:00001X][ChattingPane](by-class/ChattingPane.md) through [UID:0000I5][Chatting](by-file/Chatting.md); FolderTreePane and shared-helper alternatives have no receiver, caller, field, vtable, or neighborhood support.
- The abstract source contract is `ChattingMessage : LObject` with virtual Clone/MeasureLines/Draw slots at `+0x0c/+0x10/+0x14`; the concrete ColorString implementation deep-clones owned UTF-16 text and all color/style fields.
- `List::Append(1, &clonedMessage)` appends one pointer record. `List` owns record storage only, while ChattingPane deletes the heap object before removing record zero.
- EventMan key-producer code and the Event enum identify modifier bit `0x04` as Shift. The method masks this bit rather than comparing the entire modifier byte.
- ScrollablePane helper identity/order, vertical axis zero, Pane capture/invalidation, branch-specific sync, signed legacy trim correction, exact 1000-entry cap, and source-level local names are closed strongly enough for the formal first draft.
- No split, merge, child, range extension, covered-by disposition, non-reconstructable reclassification, or owner/emitter change is needed. Prefix `[0x0047f36f,0x0047f370)` and suffix `[0x0047f512,0x0047f520)` remain alignment.

## Score Rationale

Raised from `87/90` to `93/94` because B007 resolved every active source-quality blocker and supplied exact formal CPP. Live evidence now closes boundaries/hash, two wrapper callers, direct ChattingPane ownership, polymorphic deep-clone ownership, List append/count/removal semantics, producer-proven Shift handling, Pane capture/invalidation, inherited scroll identities/order, branch-specific synchronization, and post-insert oldest-message trimming. The score remains below `95/95` because this is a strongly supported first draft rather than a completed compiler-equivalence audit and stripped local/private spellings remain inferred.

## Parent Attachment

Attached to [UID:00001X][ChattingPane](by-class/ChattingPane.md). The direct class parent owns the message list and scroll state; [UID:0000I5][Chatting](by-file/Chatting.md) remains the source file route.

## Resolved Open Questions

- EventMan bit `0x04`: resolved as `kEventModifierShift` from key-producer behavior and the current Event enum.
- List argument `1`: resolved as `List::Append` element count one, with `&clonedMessage` as the source record; it is not an insertion index.
- ScrollablePane helpers: resolved as GetScrollMax, GetScrollPosition, SyncScrollbars, and ApplyScrollPosition with axis/old/target ordering.
- Message contract: resolved as an abstract `ChattingMessage : LObject` interface with Clone/MeasureLines/Draw and caller-owned heap clones. The declaration is emitted through existing [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md), so no duplicate no-range class page is required.
- Formal source: resolved and inserted in the CPP channel. H remains blank only because the owning class already declares the method.
- Remaining exact historical local/private spellings are represented by the best project-consistent human names; no behavioral, ownership, placement, range, or source-eligibility question remains open.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md)
- [UID:0002ES][0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals](by-memory/0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals.md)
- [UID:0002EW][0x0047fbd0-0x0047fc6b.ChattingPaneSetVisibleRowCount](by-memory/0x0047fbd0-0x0047fc6b.ChattingPaneSetVisibleRowCount.md)
- [UID:00028L][0x0067a754-0x0067a758.g_pEventMan](by-memory/0x0067a754-0x0067a758.g_pEventMan.md)
- [UID:00001X][ChattingPane](by-class/ChattingPane.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md)
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md)
- [UID:0000UO][FolderTreePaneOwnerPollution_47f280_58a8e0](by-item/FolderTreePaneOwnerPollution_47f280_58a8e0.md)
- [UID:000079][List](by-class/List.md)
- [UID:0000CF][ScrollablePane](by-class/ScrollablePane.md)
- [UID:00004L][Event](by-class/Event.md)
- [UID:00004O][EventMan](by-class/EventMan.md)

## Changes

- 2026-07-29 supervisor Gate 2A/Gate 2B reconciliation:
  - Recorded successful supervisor Gate 2A and persisted Gate 2B state without changing target score, owner/emitter/reconstructable metadata, formal CPP/H, behavior, range, or historical evidence.
  - Current IDA target is `ChattingPane__AddIncomingMessage` with exact `ChattingPane *this` / `ChattingMessage *message` prototype and function-regular comment; incomplete prerequisite type ordinals are `938`/`939`, while incomplete ChattingPane remains `440`.
  - Hex-Rays clone storage is exact `ChattingMessage *clonedMessage` and Append receives its address. The same raw frame storage remains `var_8:int` at `+0x8`; no blind alternate-view overwrite was attempted. `modifierState`, address/repeatable comments, boundaries, padding, and protected neighbors remain unchanged.
  - Preserved prestate/backup and persisted save hashes, sizes, last-write time, successful save result, and supervisor catalog `0346` for independent lifecycle verification.
- 2026-07-29 B007 UID0002ER source-quality implementation callback:
  - Raised `87/90 -> 93/94`; retained canonical owner/emitter [UID:00001X][ChattingPane](by-class/ChattingPane.md), reconstructable true, blank position, exact range, and `Nested:0`.
  - Inserted the accepted formal `ChattingPane::AddIncomingMessage(ChattingMessage *)` CPP body and kept H blank because the owning class already declares the method.
  - Incorporated exact bytes/hash and caller evidence, polymorphic deep-clone/List pointer-record ownership, Shift/capture autoscroll suppression, current/legacy synchronization differences, post-insert 1000-entry oldest delete/remove, signed legacy scroll repair, source placement, range/padding, rejected alternatives, and complete open-question closure.
  - Historical FolderTree owner pollution, index/position-one insertion, generic modifier-state meaning, and unresolved helper-name blockers remain only as explicitly superseded provenance.

- 2026-05-30: Created exact child page from the aggregate `ChattingUI` parent and set completion/confidence to `74/84`. Evidence is IDA MCP `lookup_funcs`, `xrefs_to`, decompilation, and raw disassembly for `0x0047f370`, plus byte-boundary confirmation that `0x0047f512-0x0047f520` is padding.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and left the parent blank.
  - Reasoning: existing documentation identifies source-owned chat incoming-message list logic, but the page is `74/84` and fails the child-side 80/80 attachment gate.
- 2026-06-05 A001 target update:
  - Raised from `74/84` to `82/88` and attached [UID:0000I5][Chatting](by-file/Chatting.md).
  - Live IDA MCP against MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirmed exact bounds, adjacent padding, two direct callers from [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md), EventMan flag read, clone/insert virtual slots, scroll sync/range calls, 1000-message trim, oldest-message delete/remove path, old-layout scroll correction, and the complete modeled callee set.
  - Removed the below-threshold final-output-style C++ candidate; `RECONSTRUCTION_CPP` remains blank until owner, list semantics, and scroll-helper names are final-audit ready.
- 2026-06-16 B001 source-quality execution:
  - Raised from `82/88` to `87/90`, renamed from the stale generated `FolderTreePaneAddIncomingMessage` path to `ChattingPaneAddIncomingMessage`, and moved direct owner/emitter from [UID:0000I5][Chatting](by-file/Chatting.md) to [UID:00001X][ChattingPane](by-class/ChattingPane.md).
  - Evidence: B001 rechecked direct callers, clone/insert/remove/delete behavior, `this+0x130` message list ownership, `EventMan` state-byte gate, old/current layout trim differences, 1000-message cap semantics, ScrollablePane sync calls, and rejected FolderTreePane/ScreenshotCapture/EventMan ownership. Final C++ remains blank pending exact EventMan/List/ScrollablePane names.
- 2026-06-21 B006 Rule 26 support update:
  - Score unchanged at `87/90`.
  - Summary/evidence: cross-referenced the resolved [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) implementation behind virtual slot `+0x0c`, documented that concrete clone returns a heap-owned polymorphic message pointer after deep-copying text and preserving color/custom fields, and kept `ChattingPane` ownership limited to the list insertion/lifetime logic rather than the concrete message clone body.
