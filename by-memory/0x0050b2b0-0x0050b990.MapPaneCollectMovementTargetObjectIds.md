*** UID:0004QC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
extern unsigned int s_selectObjectTargetId;

void MapPane::CollectMovementTargetObjectIds(
    std::vector<unsigned int> &objectIds) const
{
    MapPoint userPosition;
    g_pUserPane->GetMapPosition(&userPosition);

    if (g_pUserPane->m_actionMode == 2)
    {
        MapRect visibleBounds;
        GetClampedVisibleTileBounds(&visibleBounds);

        for (int row = visibleBounds.top; row < visibleBounds.bottom; ++row)
        {
            List *objects = m_objectList->GetFrontRowBucket(row);
            for (int index = 0; index < objects->GetCount(); ++index)
            {
                ObjectPane *object =
                    *static_cast<ObjectPane **>(objects->GetElementAt(index));
                MapPoint objectPosition;
                object->GetMapPosition(&objectPosition);

                bool includeObject = false;
                switch (g_pUserPane->m_facing)
                {
                case 0:
                    includeObject = objectPosition.row < userPosition.row &&
                        (objectPosition.column == userPosition.column ||
                         (g_pUserPane->m_includeAdjacentMovementTargets == true &&
                          (objectPosition.column == userPosition.column - 1 ||
                           objectPosition.column == userPosition.column + 1)));
                    break;

                case 1:
                    includeObject = objectPosition.column > userPosition.column &&
                        (objectPosition.row == userPosition.row ||
                         (g_pUserPane->m_includeAdjacentMovementTargets == true &&
                          (objectPosition.row == userPosition.row - 1 ||
                           objectPosition.row == userPosition.row + 1)));
                    break;

                case 2:
                    includeObject = objectPosition.row > userPosition.row &&
                        (objectPosition.column == userPosition.column ||
                         (g_pUserPane->m_includeAdjacentMovementTargets == true &&
                          (objectPosition.column == userPosition.column - 1 ||
                           objectPosition.column == userPosition.column + 1)));
                    break;

                case 3:
                    includeObject = objectPosition.column < userPosition.column &&
                        (objectPosition.row == userPosition.row ||
                         (g_pUserPane->m_includeAdjacentMovementTargets == true &&
                          (objectPosition.row == userPosition.row - 1 ||
                           objectPosition.row == userPosition.row + 1)));
                    break;
                }

                if (includeObject)
                    objectIds.push_back(object->m_objectId);
            }
        }
        return;
    }

    ObjectPane *target = m_objectList->FindByObjectId(s_selectObjectTargetId);
    if (target != NULL)
        objectIds.push_back(target->m_objectId);

    if (g_pUserPane->m_includeAdjacentMovementTargets == true)
    {
        MapPoint targetPosition;
        target->GetMapPosition(&targetPosition);

        MapRect visibleBounds;
        GetClampedVisibleTileBounds(&visibleBounds);
        for (int row = visibleBounds.top; row < visibleBounds.bottom; ++row)
        {
            List *objects = m_objectList->GetFrontRowBucket(row);
            for (int index = 0; index < objects->GetCount(); ++index)
            {
                ObjectPane *object =
                    *static_cast<ObjectPane **>(objects->GetElementAt(index));
                MapPoint objectPosition;
                object->GetMapPosition(&objectPosition);
                if (objectPosition.column >= targetPosition.column - 1 &&
                    objectPosition.column <= targetPosition.column + 1 &&
                    objectPosition.row >= targetPosition.row - 1 &&
                    objectPosition.row <= targetPosition.row + 1 &&
                    (objectPosition.column != targetPosition.column ||
                     objectPosition.row != targetPosition.row))
                {
                    objectIds.push_back(object->m_objectId);
                }
            }
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: MapPane movement-target object-id collector; exact directional lane scan, selected-target fallback, optional eight-neighbor expansion, attached switch table, stale-id null edge, and source-ready formal body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***


# MapPane CollectMovementTargetObjectIds

## Address Range And Attached Data

- Exact owned range: `[0x0050b2b0,0x0050b990)`, 1760 bytes, SHA-256 `268DD2349753DF0A781DB635366C82DDE06FB4CA3703832B7EFA1F1E1735D64B`.
- IDA models 532 instructions in 82 basic blocks with one caller function and six callee records. Executable flow returns by `0x0050b97d`.
- The trailing `0F 1F 00` at `[0x0050b97d,0x0050b980)` is compiler alignment. Dwords `[0x0050b980,0x0050b990)` target `0x0050b33f`, `0x0050b44f`, `0x0050b574`, and `0x0050b6a5`; the dispatch at `0x0050b338` directly references the table. Both stay attached to this method and are not separate source rows.
- Canonical owner/emitter is MapPane. The only direct caller is UID0003UI UserPane movement sending, which invokes this collector before the distance sorter.

## Directional Action-Mode Branch

- The method first obtains the local user's map point and tests UserPane `m_actionMode == 2`.
- In action mode 2 it gets exact clamped visible tile bounds, walks each front-row ObjectList bucket in half-open row order, extracts each ObjectPane pointer, and reads its map position.
- Facing values 0, 1, 2, and 3 select north/east/south/west half-planes. The primary lane requires equal perpendicular coordinate. When `m_includeAdjacentMovementTargets == true`, the two immediately adjacent parallel lanes are included.
- Included objects contribute `m_objectId` in traversal order. No type, alive, visibility, duplicate, distance, or maximum-count filter occurs in this collector.
- The formal switch and attached jump-table behavior are human source. Do not hand-author the table, index guard, or compiler lowering.

## Selected-Target Branch And Edge Case

- Other action modes look up externally linked `s_selectObjectTargetId` through `ObjectList::FindByObjectId` and append its ID when non-null.
- If `m_includeAdjacentMovementTargets == true`, the binary then dereferences the target to obtain its map point without a second null guard. It scans visible buckets and appends objects in the surrounding 3-by-3 neighborhood, excluding the target's exact center tile.
- Therefore a stale/missing selected ID plus an enabled adjacency flag can dereference null. The formal preserves this source edge exactly; it must not add a guard that changes behavior.
- The direct cross-translation-unit read proves `s_selectObjectTargetId` cannot remain file-static in TargetSelectionInputPanes. The other four saved-target globals retain internal linkage.

## Naming, Source Placement, And Dependencies

- `CollectMovementTargetObjectIds` is the best evidence-backed name: the output is a `std::vector<unsigned int>` of object IDs and UID0003UI serializes those IDs into a movement packet. Historical `BuildLocalMovementPath` is false because no waypoints, segments, or path coordinates are produced.
- Source-facing signature is `void MapPane::CollectMovementTargetObjectIds(std::vector<unsigned int> &objectIds) const` plus the minimum `extern unsigned int s_selectObjectTargetId` declaration.
- Emit through [UID:00007Q][MapPane](by-class/MapPane.md) and [UID:0000L3][MapPane](by-file/MapPane.md) in `NexusTK/map/MapPane.cpp`.
- Required dependencies are UserPane position/action/facing/adjacency state, ObjectList bucket/find APIs, ObjectPane type/id/position access, MapPoint/MapRect/List, and the selected-target global.

## Negative Evidence, History, And Confidence

- Reject path building, route compaction, deduplication, or sorting names. Sorting is a separate following method and path segments are absent.
- Reject a nine-object cap here. UID0003UI caps only serialized IDs after this complete vector is collected and sorted.
- Reject selecting only living objects or excluding the local user by inferred policy; the body applies no such check.
- Reject a file-local selected-target declaration, a synthetic accessor, or moving the definition into MapPane. The target-selection source remains the producer/owner; external linkage is the exact minimum correction.
- Completion `91` reflects exact split, CFG, table attachment, directional and selected branches, stale-ID edge, linkage, dependencies, and complete source. Confidence `93` remains below final certainty because private names are reconstructed from behavior rather than symbols.

## Related Documentation

- [UID:00037X][0x0050b080-0x0050bcd8.MapPaneInteractionMapChangeCore](by-memory/0x0050b080-0x0050bcd8.MapPaneInteractionMapChangeCore.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0003UI][0x005a8cf0-0x005a8e67.UserPaneSendMovementPacket](by-memory/0x005a8cf0-0x005a8e67.UserPaneSendMovementPacket.md)
- [UID:0000FQ][UserPane](by-class/UserPane.md)
- [UID:00009Q][ObjectList](by-class/ObjectList.md)
- [UID:00009R][ObjectPane](by-class/ObjectPane.md)
- [UID:0002AB][0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals](by-memory/0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals.md)
