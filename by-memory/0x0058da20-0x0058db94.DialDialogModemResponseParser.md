*** UID:0001JH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char DialDialog::ParseModemResponse(SocketPacket *packet)
{
    enum ModemResponseState {
        RESPONSE_NONE = 0,
        RESPONSE_CONNECT_C = 1,
        RESPONSE_CONNECT_CO = 2,
        RESPONSE_CONNECT_CON = 3,
        RESPONSE_CONNECT_CONN = 4,
        RESPONSE_CONNECT_CONNE = 5,
        RESPONSE_CONNECT_CONNEC = 6,
        RESPONSE_BUSY_B = 7,
        RESPONSE_BUSY_BU = 8,
        RESPONSE_BUSY_BUS = 9
    };

    const char *data = packet->m_data;
    const int length = packet->m_length;

    if (length > 100) {
        SlideCloseVertical();
        CloseDialog();
        return 0;
    }

    for (int index = 0; index < length; ++index) {
        const char ch = data[index];

        switch (m_modemResponseState) {
        case RESPONSE_NONE:
            if (ch == 'C') {
                m_modemResponseState = RESPONSE_CONNECT_C;
            } else if (ch == 'B') {
                m_modemResponseState = RESPONSE_BUSY_B;
            }
            break;

        case RESPONSE_CONNECT_C:
            m_modemResponseState = (ch == 'O') ? RESPONSE_CONNECT_CO : RESPONSE_NONE;
            break;

        case RESPONSE_CONNECT_CO:
            m_modemResponseState = (ch == 'N') ? RESPONSE_CONNECT_CON : RESPONSE_NONE;
            break;

        case RESPONSE_CONNECT_CON:
            m_modemResponseState = (ch == 'N') ? RESPONSE_CONNECT_CONN : RESPONSE_NONE;
            break;

        case RESPONSE_CONNECT_CONN:
            m_modemResponseState = (ch == 'E') ? RESPONSE_CONNECT_CONNE : RESPONSE_NONE;
            break;

        case RESPONSE_CONNECT_CONNE:
            m_modemResponseState = (ch == 'C') ? RESPONSE_CONNECT_CONNEC : RESPONSE_NONE;
            break;

        case RESPONSE_CONNECT_CONNEC:
            if (ch == 'T') {
                SlideCloseVertical();
                CloseDialog();
            } else {
                m_modemResponseState = RESPONSE_NONE;
            }
            break;

        case RESPONSE_BUSY_B:
            m_modemResponseState = (ch == 'U') ? RESPONSE_BUSY_BU : RESPONSE_NONE;
            break;

        case RESPONSE_BUSY_BU:
            m_modemResponseState = (ch == 'S') ? RESPONSE_BUSY_BUS : RESPONSE_NONE;
            break;

        case RESPONSE_BUSY_BUS:
            if (ch == 'Y') {
                SlideCloseVertical();
                CloseDialog();
            } else {
                m_modemResponseState = RESPONSE_NONE;
            }
            break;

        default:
            break;
        }
    }

    return 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: DialDialog modem-response parser with current MCP-confirmed vtable-slot callback ownership, SocketPacket argument shape, adjusted receiver/state offset, CONNECT/BUSY state machine, close-helper route, and implementation-ready formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0058da20-0x0058db94 DialDialog Modem Response Parser

## Status

- Confidence: strong for range, dial-dialog ownership, vtable/data-slot callback role, parser behavior, state offset, parent source placement, source-facing callback prototype, `SocketPacket` packet shape, `m_modemResponseState`, and inherited close-helper route.
- Covered file/module: [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md)
- Parent attachment: this child is currently `92/94` and is owned/emitted directly by [UID:00003R][DialDialog](by-class/DialDialog.md), currently `94/95`; [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md), currently `90/92`, remains the final source-file route rather than the method's semantic owner.
- Current generated-source state: validator-generated `PhoneBookDialog.cpp` emits this child as the exact `DialDialog::ParseModemResponse(SocketPacket *packet)` body from the formal block; no parser empty-emitter marker remains.
- Source name and signature: `char DialDialog::ParseModemResponse(SocketPacket *packet)`.
- C++ reconstruction: implementation-ready. The callback prototype, vtable slot name, `SocketPacket` fields, parser-state field, and close-helper route are resolved for first-draft formal source.

## Function Role

This callback parses incoming modem response bytes for the dialing dialog. It recognizes `CONNECT` and `BUSY` response tokens, then closes or advances the dialog through the same slide/close path used by the OK action.

## Behavior Notes

- Reads `packet->m_data` and `packet->m_length` from the incoming `SocketPacket` object at packet offsets `+0x0c` and `+0x10`.
- If the input length is greater than 100 bytes, immediately closes the adjusted `DialDialog` instance.
- Tracks `m_modemResponseState` at offset `0x24c` relative to the callback subobject, which corresponds to full-object offset `0x2ec` after the secondary receiver adjustment.
- State sequence recognizes `CONNECT` from `C O N N E C T` and `BUSY` from `B U S Y`.
- On oversized input or a recognized terminal state, calls inherited `SlideCloseVertical()` (`0x0049eb90`) and `CloseDialog()` (`0x0049dad0`) on the adjusted full dialog instance.
- Live IDA shows this as a callback subobject: the state appears as `this[147]` in the callback view, and the close path adjusts back to the full dialog with `this - 40` dwords (`this - 0xa0` bytes).
- The source C++ should not expose the `this - 0xa0` adjustment; that is a secondary-base ABI artifact, while the source method is a normal `DialDialog` member.

## IDA MCP Evidence

- 2026-06-27 B001 current IDA MCP pass used active database/session `b001_000241_20260627` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` was `ok`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- Current `lookup_funcs 0x0058da20` reports `sub_58DA20`, size `0x175`. The function starts at `0x0058da20` and ends at `0x0058db95` exclusive, matching this page's last-byte range spelling `0x0058da20-0x0058db94`.
- Current `xrefs_to 0x0058da20` reports exactly one data reference at `0x0062ddb4` and no code callers. `trace_data_flow 0x0062ddb4 backward` reports `dd offset sub_58DA20`; `trace_data_flow 0x0058da20 backward` reports the same `0x0062ddb4 -> 0x0058da20` data edge.
- Current `entity_query 0x0062dd44-0x0062dddc` reports `??_7DialDialog@@6B@` at `0x0062dd44`, `??_7DialDialog@@6B@_0` at `0x0062dda4`, `??_7DialDialog@@6B@_1` at `0x0062ddd4`, and successor `aAtz` at `0x0062dddc`. Constructor vtable-head writes occur at `0x0058d7a6 -> 0x0062dd44`, `0x0058d7ae -> 0x0062dda4`, and `0x0058d7b8 -> 0x0062ddd4`.
- The adjacent action slot is `0x0062dd8c -> 0x0058d9f0`; the parser slot is `0x0062ddb4 -> 0x0058da20`; destructor/vtable evidence includes `0x0062dd44 -> 0x0058dc20` and destructor adjustor jumps at `0x0058dbf1` and `0x0058dbfc`.
- Current decompilation reads the packet data pointer and signed length from `packet + 0x0c` and `packet + 0x10`, rejects lengths greater than `100`, loops while `index < length`, reads/writes state at adjusted receiver offset `+0x24c`, recognizes `CONNECT` and `BUSY`, and calls only `sub_49EB90` and `sub_49DAD0`.
- Current disassembly corroborates the entry loads `mov ecx, [eax+0Ch]` and `mov eax, [eax+10h]`, the `cmp eax, 64h` oversize guard, loop state loads at `[edi+24Ch]`, and close calls through `lea ecx, [edi-0A0h]` before `sub_49EB90` and `sub_49DAD0`.
- Constructor decompilation for `0x0058d750` installs the three `DialDialog` vtable heads at full-object offsets `+0x00`, `+0xa0`, and `+0xa4`, sends modem setup traffic including `"ATX3DT"` and `"\n\r"`, and initializes the full-object state field `*(this + 748) = 0`. The parser's adjusted field offset `0x24c` plus secondary receiver adjustment `0xa0` equals full-object offset `0x2ec`.
- Source-facing names resolved by this pass: method `DialDialog::ParseModemResponse`, packet type `SocketPacket`, packet fields `m_data` and `m_length`, state field `m_modemResponseState`, and close helpers `SlideCloseVertical()` and `CloseDialog()`.
- `lookup_funcs 0x0058da20` reports `sub_58DA20`, size `0x175`, ending at `0x0058db95`.
- `xrefs_to 0x0058da20` reports a data reference at `0x0062ddb4`, consistent with callback/vtable slot ownership.
- `callers 0x0058da20` reports no direct code callers.
- Decompilation shows the `CONNECT` and `BUSY` byte-state parser and the close path described above.
- 2026-06-16 A002 live IDA MCP refresh on session `c001_midiplayer_rdata_20260615` reconfirmed `sub_58DA20` size `0x175`, no direct code callers, and the single data xref at `0x0062ddb4`.
- `analyze_function 0x0058da20` reports provisional prototype `char __thiscall(_DWORD *this, int)` / userpurge callback form, reads incoming buffer fields at `a3 + 0x0c` and `a3 + 0x10`, rejects length greater than `100`, switches over `this[147]`, and advances the `CONNECT` and `BUSY` recognizers byte-by-byte.
- The same analysis reports the only product callees as `sub_49EB90` and `sub_49DAD0`; on `CONNECT` or `BUSY` completion it calls them with the adjusted full-dialog pointer (`this - 0xa0` bytes).
- `entity_query 0x0062d714-0x0062df94` places `0x0062ddb4` inside the `DialDialog` three-head vtable island (`0x0062dd44`, `0x0062dda4`, `0x0062ddd4`), before the adjacent `ATZ`/TerminalPane callback data and phone/terminal resource strings.
- `make_signature_for_function` produced the unique parser entry signature `55 8B EC 51 8B 45 ? 56 57 8B F9`.
- Wave2 command audit: `method 0x0058da20` and `xrefs 0x0058da20` return no method/global record, while `filter --class DialDialog --all-methods` lists the constructor, action handler, destructor, and adjustor thunks only. This supports treating the parser as an IDA-modeled `DialDialog` callback missing from Wave2 import.

Corroborating documentation:

- [UID:0001JG][0x0058d750-0x0058dc5b.DialDialog](by-memory/0x0058d750-0x0058dc5b.DialDialog.md) is currently a `93/95` non-emitting split index that documents this method family, routes authored methods through [UID:00003R][DialDialog](by-class/DialDialog.md), retains final source placement under [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md), and records explicit boundary/thunk exclusions.
- [UID:00003R][DialDialog](by-class/DialDialog.md) records the parser as an IDA-confirmed helper that reads buffers up to length 100, tracks state at `this + 0x24c`, recognizes `CONNECT` and `BUSY`, and closes through the dialog slide/close path.
- [UID:00026N][0x0062da10-0x0062df94.TerminalPhoneReadOnlyData](by-memory/0x0062da10-0x0062df94.TerminalPhoneReadOnlyData.md) places `DialDialog` vtables at `0x0062dd44-0x0062ddd8`; the `0x0062ddb4` data reference sits inside that `DialDialog` vtable family.
- [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md) groups this parser with `DialDialog` inside `login/PhoneBookDialog.cpp`.
- [UID:0001R1][proposed-source-tree](by-project-structure/proposed-source-tree.md) identifies `0x0058da20` as the dial modem-response parser in the terminal phone-book/modem dialing flow.

## Boundary Notes

- Start boundary: `0x0058da20`, immediately after `DialDialog::OnControlCommand` at `0x0058d9f0-0x0058da1e` and the one-byte alignment gap before this callback. Historical `OnDialogAction` is superseded.
- IDA function end: `0x0058db95`.
- Documented file range end remains `0x0058db94`, matching the last byte before the IDA-exclusive end address.
- The following documented code in the local island is the ignored TerminalPane constructor EH clear helper at `0x0058dbe0-0x0058dbeb`, separated from this parser by unpromoted local gap bytes.

## Ownership Decision

The direct semantic owner/emitter is [UID:00003R][DialDialog](by-class/DialDialog.md); final placement remains `NexusTK/login/PhoneBookDialog.cpp` under [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md). It is physically adjacent to the dial dialog and uses DialDialog state. The absence of direct code callers reflects vtable dispatch, not standalone or file-direct ownership.

## Source-Quality Blocker Audit

- Resolved for this target: data-slot placement, parser state behavior, callback subobject adjustment, missing-Wave2-method status, byte signature, source-facing callback name, `SocketPacket *packet` prototype, packet field names, `m_modemResponseState`, and the inherited close-helper route.
- Formal C++ is no longer blocked by a blank-code disposition. The C++ body in the formal block uses source-facing names and avoids IDA labels, raw offset casts, decompiler temporaries, and explicit secondary-base pointer subtraction.
- Remaining score limit: the surrounding `DialDialog` class and aggregate are now complete for this source-quality pass. This parser remains `92/94` because original identifier spelling and the historical header/translation-unit split are inferred and this bounded page does not claim final-audit verification of every inherited dependency.
- Historical 2026-06-27 documentation pass did not mutate IDA. Supervisor Gate2B catalog entry 0364 later applied and verified the report's exact source-quality identity, ABI-honest type, and function-repeatable comment without changing bytes, range, vtable liveness, state-machine behavior, or source disposition.

## Rejected Alternatives

- `bool DialDialog::ParseModemResponse(...)`: rejected for this pass. IDA and the adjacent `TerminalPane::ParseTerminalStream(SocketPacket *packet)` analogue support `char` return with `return 0`.
- `OnModemResponse` / `HandleModemResponse`: rejected as less consistent with the parser role and the accepted terminal parser naming pattern.
- A new packet type name: rejected. `SocketPacket` already describes the adjacent terminal parser callback ABI with data at `+0x0c` and length at `+0x10`.
- `packet->GetData()` / `packet->GetLength()`: rejected because the binary uses direct field loads and no inline accessor evidence is present.
- Raw packet offset casts, `sub_49EB90` / `sub_49DAD0` calls, and explicit `this - 0xa0` source code are rejected as reverse-engineering scaffolding.
- Returning immediately after `CONNECT` or `BUSY`, reprocessing mismatching bytes, or resetting state after successful close are rejected because the binary closes and continues scanning, advances after mismatches, and leaves success states `6` or `9` in place.

## Cross-References

- [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md)
- [UID:00003R][DialDialog](by-class/DialDialog.md)
- [UID:0001JG][0x0058d750-0x0058dc5b.DialDialog](by-memory/0x0058d750-0x0058dc5b.DialDialog.md)
- [UID:00026N][0x0062da10-0x0062df94.TerminalPhoneReadOnlyData](by-memory/0x0062da10-0x0062df94.TerminalPhoneReadOnlyData.md)
- [UID:0001R1][proposed-source-tree](by-project-structure/proposed-source-tree.md)

## B006 Split-Index And Direct-Class Reconciliation - 2026-07-29

- Exact function range remains `[0x0058da20,0x0058db95)`, size `0x175`. The source definition above remains the accepted `char DialDialog::ParseModemResponse(SocketPacket *packet)` body and was not rewritten.
- The direct inbound route is data cell `0x0062ddb4`, the `DialDialog+0xa0` EventHandler-view virtual slot. There are no direct code callers. The compiled receiver is adjusted, while the human source method remains a normal DialDialog member.
- Packet fields remain `m_data` at `+0x0c` and `m_length` at `+0x10`. Parser state is full-object `m_modemResponseState` at `+0x2ec`, observed as `+0x24c` from the EventHandler-view receiver. The ten-state switch recognizes `CONNECT` and `BUSY`, closes the full dialog on terminal states or input longer than 100 bytes, and otherwise returns zero.
- The complete range parent is now the non-emitting [UID:0001JG][0x0058d750-0x0058dc5b.DialDialog](by-memory/0x0058d750-0x0058dc5b.DialDialog.md). The following support bytes are independently documented by [UID:0004YG][0x0058db95-0x0058dbdf.DialDialogParserTailAndRetainedCloseWrappers](by-memory/0x0058db95-0x0058dbdf.DialDialogParserTailAndRetainedCloseWrappers.md); they do not belong to this method body.
- Direct ownership/emission changes from historical file-direct UID0000MJ to class UID00003R. UID0000MJ remains the final source-file route, not the method's semantic owner. The historical Wave2 wording in older evidence is stale provenance and is not used as current authority.
- Historical pre-Gate2B IDA state was raw `sub_58DA20` with an adjusted-receiver `__userpurge` declaration and blank function-start comment channels. Current verified state is `DialDialog_ParseModemResponse`, exact ABI-honest type `char __thiscall(EventHandler *eventHandlerThis, SocketPacket *packet)`, and function-repeatable comment `DialDialog EventHandler-view (+0xA0) parser override; reads packet data/length, advances CONNECT/BUSY state at full DialDialog +0x2EC, and closes the full dialog on success or length >100. Source signature is DialDialog::ParseModemResponse(SocketPacket *).` Regular, repeatable, and function-regular comments remain blank.
- Measured post-prototype frame state preserves `var_4 +0x08`, saved registers `+0x0c`, and return address `+0x10`, then exposes `packet +0x14 SocketPacket *`; each entry is four bytes and no extra frame member appeared. Current UDT readback is `SocketPacket` size `0x14` with `_opaque00[12] +0`, `m_data +0x0c char *`, and `m_length +0x10 int`. The parser bytes, range, switch, sole vtable-data route, adjusted `EventHandler` receiver semantics, and full-object `+0x2ec` state route are unchanged.
- Applied/readback evidence is bound to catalog entry 0364 and saved application checkpoint `905D1AB1...604C1`. A bounded read-only no-drift check through active MCP database `1da2b2ae` against current authoritative saved IDB SHA256 `412DA7E81557538D283BBFBC18875CACC6C1DBBC662E88BEC71307E0AE0519CD`, 143,191,140 bytes, saved `2026-07-29T15:00:32.0998001-04:00`, reconfirmed the exact parser/UDT/frame state.
- Score `92/94` reflects exact bounds, byte signature, vtable liveness, packet layout, state offsets, switch behavior, close path, source name/prototype, direct class/file route, exact formal C++, and negative caller evidence. It remains below final-audit confidence because original identifier spelling and the final historical header split are inferred.

## Changes

- 2026-07-29 B006 UID0001JG callback: raised `89/90 -> 92/94`, rerouted owner/emitter from file UID0000MJ to direct class UID00003R, preserved the exact parser CPP, synchronized the split-index/tail relationship, and recorded the then-current raw-IDB/adjusted-ABI Gate2B handoff. Supervisor catalog entry 0364 subsequently applied and verified that handoff; the raw state is historical.

- 2026-06-27 B001 source-quality implementation callback:
  - Before: `86/88`, formal C++ blank, and status text still treated callback prototype, parser-state field, and close-helper names as blockers.
  - After: `89/90`, formal `char DialDialog::ParseModemResponse(SocketPacket *packet)` C++ body inserted, owner/emitter kept as [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md), and source-facing names documented.
  - Summary/evidence: current IDA MCP session `b001_000241_20260627` confirms function size `0x175`, vtable slot `0x0062ddb4`, packet fields at `+0x0c/+0x10`, adjusted/full parser-state offsets `+0x24c/+0x2ec`, close callees `0x0049eb90`/`0x0049dad0`, and constructor initialization of the full-object state field.
  - Rejected alternatives preserved: bool return, alternate callback names, invented packet type/accessors, raw offsets/callee labels, explicit secondary-base subtraction, early return after success, mismatch reprocessing, and success-state reset.
- 2026-06-16 A002 `A002-goal2-lowconfidence-dialog-parser-20260615-22` refresh:
  - Before: `82/84`.
  - After: `86/88`.
  - Summary/evidence: live IDA MCP reconfirmed the exact function boundary, data-only vtable xref `0x0062ddb4`, no direct callers, incoming buffer field use, `CONNECT`/`BUSY` state machine, callback-subobject adjustment back to the full dialog, `DialDialog` vtable-island placement, and a unique entry signature; Wave2 audit confirmed the callback is absent from imported `DialDialog` methods/globals.
  - Historical remaining blockers: final callback prototype, source-facing slot/member/helper names, and exact emitted source shape were unresolved at that time; this parser-specific blocker is superseded by the 2026-06-27 B001 implementation callback above.
- 2026-06-01: raised from `50/65` to `82/84`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md).
  - Summary/evidence: existing IDA-backed docs confirm the exact function boundary, `0x0062ddb4` vtable/data reference, no direct callers, CONNECT/BUSY parser behavior, `DialDialog` field usage, parent source placement, and source-tree ownership.
  - Historical remaining gaps: no reconstructed C++ because final callback signature, virtual slot name, and parser-state field name were below source-quality confidence at that time; superseded for this parser by the 2026-06-27 B001 implementation callback above.
