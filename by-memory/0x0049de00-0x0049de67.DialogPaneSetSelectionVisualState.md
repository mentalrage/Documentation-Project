*** UID:0003KN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003T | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003T | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void DialogPane::SetSelectionVisualState(int controlId, unsigned char visualState)
{
    if (m_selectedControlId != -1 &&
        m_selectedControlId != controlId &&
        m_selectionVisualState != 10)
    {
        GetChild<ControlPane>(m_selectedControlId)->SetVisualState(10);
    }

    m_selectedControlId = controlId;
    m_selectionVisualState = visualState;

    if (controlId != -1)
        GetChild<ControlPane>(controlId)->SetVisualState(visualState);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact source body for `DialogPane::SetSelectionVisualState(int,unsigned char)`: conditionally neutralizes the previous selected child, stores the new id/state pair, applies the new state to a valid child, preserves `-1` and state `10` sentinels, and emits through DialogPane. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049de00-0x0049de67 DialogPane Set Selection Visual State

## Status

- Exact range: `0x0049de00-0x0049de67`, end-exclusive.
- Entity kind: `DialogPane` selected-control visual-state helper.
- Owner: [UID:00003T][DialogPane](by-class/DialogPane.md).
- Containing inventory: [UID:00012S][0x0049dae0-0x0049dfc4.DialogControlPaneHelpers](by-memory/0x0049dae0-0x0049dfc4.DialogControlPaneHelpers.md).
- Rebuild handling: source-authored method body.

## Behavior

Tracks the current selected/hovered control id at `this+0x224` and visual-state byte at `this+0x228`. If the previous selected id differs from the new one and is not already state `0x0a`, it resets that previous control through its vtable slot `+0x48`. It then stores the new id/state and applies the new state to the new control when present.

Exact ordering matters: the previous child reset occurs before either member write; both members are then updated even when the incoming id is `-1`; the new child's virtual `SetVisualState` call is skipped only for id `-1`. The helper does not perform hit testing, change focus, activate a command, or redraw a rectangle itself.

## IDA MCP Evidence

- 2026-06-12 `lookup_funcs 0x0049de00` reports `sub_49DE00` size `0x67`.
- `decompile 0x0049de00` shows state reads/writes at `this+548` and `this+552`, control-manager entry lookup through slot `+0x10`, and item visual-state dispatch through control vtable slot `+0x48`.
- `analyze_function 0x0049de00` reports callers from `sub_494F70`, the raw helper at `0x0049de70`, `sub_49DF20`, `sub_49E240`, `sub_49E6E0`, `sub_4A0840`, `sub_4FAE90`, and `sub_520870`.

The eight modeled caller families produce 16 code xrefs and establish this as shared DialogPane state management rather than a ModelessDialogPane-local helper. The lack of a surviving source symbol is negative naming evidence only; body semantics support the accepted human name and reject raw `sub_49DE00` in emitted code.

## Historical And Source-Shape Notes

- Earlier documentation recorded behavior but left formal CPP blank and treated naming as provisional. The exact body and shared callers now justify source emission and `SetSelectionVisualState`.
- State value `10` remains numeric because no stable project-wide enum member spelling is proven. Inventing an enum label would be less faithful than the accepted byte parameter.
- The class declaration owns the method declaration; this exact by-memory page owns the body, so formal H remains blank and no duplicate aggregate body is emitted.

## Score Rationale

Score `92/95` reflects exact range, complete source body, side-effect order, sentinel behavior, shared caller set, owner/emitter route, and formal CPP. The absent original visual-state enum spelling caps only lexical finality, not behavior or reconstructability.

## Changes

- 2026-06-12 Agent-A001 Goal 2: Split exact child from the helper aggregate.
