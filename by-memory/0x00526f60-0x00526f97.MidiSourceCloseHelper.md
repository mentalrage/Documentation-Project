*** UID:0002C0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void MidiCloseSource(MidiDocument* document)
{
    if (document->datFileSource != 0) {
        if (document->datFile.IsOpen()) {
            document->datFile.Close();
        }
        return;
    }

    if (document->fileHandle != INVALID_HANDLE_VALUE) {
        CloseHandle(document->fileHandle);
        document->fileHandle = INVALID_HANDLE_VALUE;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Closes adapter-backed or Win32 file-backed MIDI sources; live IDA records exact raw bytes, no-entry-xref caveat, adapter close branch, `CloseHandle` path, handle reset, and padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00526f60-0x00526f97 MidiSourceCloseHelper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw function-shaped helper
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for source-family ownership, exact raw bytes, branch behavior, adapter validation, `CloseHandle` cleanup, handle reset, and adjacent padding; medium-high for direct reachability because IDA does not model it as a function and has no entry xrefs.
- Autogen status: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because the child and parent file now meet the >=80 confidence threshold.

## Score Rationale

The score was raised from `68/76` to `72/82` because the page tied the close helper to the validated `MidiPlayer` source owner and recorded the shared source abstraction state used by neighboring source helpers.

Raised again to `80/88` on 2026-06-06 after live IDA MCP confirmed the exact raw body bytes, no modeled function at the start, no entry xrefs, adapter-close branch, Win32 `CloseHandle` branch, handle reset, and padding around the helper. It is not raised higher because IDA still does not model the start as a function and the final adapter/source type names remain provisional.

Raised to `85/90` on 2026-06-16 after current IDA reconfirmed the raw body, adapter validation helper xrefs, virtual close slot, Win32 `CloseHandle` path, exact padding, and source-type blocker checks. The 2026-06-30 B013 bounded callback raises completion to `87` after accepting retained private `MidiCloseSource` C++. Confidence remains `90` because the helper is still not an IDA-modeled function, no direct entry xref exists, and the adapter/context type and final helper declaration remain inferred.

## Address Range

`0x00526f60-0x00526f97`.

IDA does not model this start as a function, but live disassembly shows a compact source-close body.

## Behavior

Closes the active MIDI document source. It dispatches adapter cleanup when the adapter-backed source is active; otherwise it calls `CloseHandle` on the Win32 file handle and stores `-1` back to the handle slot.

## Source-State Notes

| State or dependency | Meaning |
| --- | --- |
| `this + 0x14` | Adapter-active flag deciding adapter cleanup versus Win32 file-handle cleanup. |
| `this + 0x18` | Adapter/source subobject cleaned through the adapter path. |
| `this + 0x2c` | Win32 file handle closed and reset to `-1`. |
| `CloseHandle` at `0x00526f88` | Confirms the file-backed cleanup path. |

## Evidence

- Parent page records this as a raw close source helper.
- IDA live analysis shows adapter cleanup through `sub_49C540` and imported `CloseHandle` at `0x00526f88`.
- The helper checks `dword ptr [esi+0x14]`, the same adapter-active flag used throughout the source abstraction.

## 2026-06-06 Live IDA MCP Evidence

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms:

- No IDA function object starts at `0x00526f60`, and there are no direct entry xrefs to the raw helper start.
- The exact body is 55 bytes from `0x00526f60` through the `ret` at `0x00526f96`; the next byte is padding at `0x00526f97`.
- The body checks adapter-active flag `this+0x14`, validates the adapter/source subobject `this+0x18` through `sub_49C540`, then tail-jumps through vtable slot `+0x10` when the adapter path is live.
- The Win32 path reads handle `this+0x2c`, skips `-1`, calls `CloseHandle` at `0x00526f88`, and stores `-1` back to `this+0x2c`.
- Bytes before the helper include the prior `retn 4` and `0xcc` padding; bytes after `0x00526f97` are padding until [UID:0002C1][0x00526fa0-0x00526ff6.MidiSourceSeekHelper](by-memory/0x00526fa0-0x00526ff6.MidiSourceSeekHelper.md).

## 2026-06-16 Source-Core Refresh

Live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed the close helper and its support evidence:

- `lookup_funcs` still reports `0x00526f60` and `0x00526f97` as not function objects, and `xrefs_to 0x00526f60` reports no entry xrefs.
- `insn_query 0x00526f10-0x00526fa0` shows the close body at `0x00526f60-0x00526f96`, with `0xcc` padding at `0x00526f97-0x00526fa0` before the source-seek raw helper.
- Adapter mode checks `this+0x14`, calls `sub_49C540` on `this+0x18`, and tail-jumps through virtual slot `+0x10` only when the adapter validates as live.
- File mode reads `this+0x2c`, skips the sentinel `0xffffffff`, calls `CloseHandle` at `0x00526f88`, and stores `0xffffffff` back to `this+0x2c`.
- `xrefs_to 0x0049c540` reports this raw close helper, the raw source-size/ready helper, [UID:0001CI][0x005270e0-0x005271a3.MidiDocumentRelease](by-memory/0x005270e0-0x005271a3.MidiDocumentRelease.md), and the SMF document loader. `func_profile 0x0049c540` shows a tiny 8-byte helper with callers only from this MIDI source/document family in the current check, supporting the adapter-validation role but not a final source name.
- IDA `type_query`/`search_structs` and by-type searches found no project-defined MIDI source adapter or document type, so the vtable slot names remain role-based.

## Reconstruction Notes

This should be a private close method/helper for the MIDI document reader abstraction. The formal C++ block now emits retained private `MidiCloseSource` with accepted role-based `MidiDocument`/embedded `DATFile` names; the raw non-function/no-entry-xref and adapter/context naming caveats remain confidence caps.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)
- [UID:0002BZ][0x00526f10-0x00526f57.MidiSourceOpenHelper](by-memory/0x00526f10-0x00526f57.MidiSourceOpenHelper.md)
- [UID:0002C1][0x00526fa0-0x00526ff6.MidiSourceSeekHelper](by-memory/0x00526fa0-0x00526ff6.MidiSourceSeekHelper.md)
- [UID:0002C2][0x00527000-0x00527039.MidiSourceRawReadHelper](by-memory/0x00527000-0x00527039.MidiSourceRawReadHelper.md)

## Changes

- 2026-06-30 B013 bounded empty-emitter implementation:
  - Changed to: score `87/90`; owner/emitter route remains [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md).
  - Summary/evidence: populated the formal C++ block with retained private `MidiCloseSource`. Current MCP raw disassembly proves the adapter-active flag branch, `sub_49C540`/virtual close slot path, Win32 `CloseHandle` fallback, handle reset to `INVALID_HANDLE_VALUE`, exact padding, and no-entry-xref caveat.
- 2026-06-02 low-score pass:
  - What existed before: range, behavior, and evidence were documented, but metadata stayed at `58/72` and reconstructable was blank.
  - Changed to: scores `68/76`, `RECONSTRUCTABLE:TRUE`, explicit autogen deferral, and score rationale.
  - Summary/evidence: adapter-active flag, `sub_49C540` adapter cleanup, `CloseHandle`, handle reset to `-1`, and neighboring source helpers support reconstructable project-code status without final C++.
- 2026-06-03 parent attachment:
  - What existed before: the helper remained unattached because its source ownership was treated as provisional.
  - Changed to: score `72/82` and `AUTOGEN_PARENT_UID:0000LD`.
  - Summary/evidence: the owning `MidiPlayer` file is now at `80/84`; this page records shared source-state offsets and sibling relationships. Final C++ remains blank.
- 2026-06-06 live IDA MCP pass:
  - Changed to: score `80/88`.
  - Summary/evidence: exact raw bytes, no modeled function or entry xrefs, adapter vtable close path, Win32 `CloseHandle` path, `this+0x2c` reset, and surrounding padding. Final C++ remains blank below the source-grade threshold.
- 2026-06-16 source-core refresh:
  - What existed before: score `80/88` with exact close behavior but below the current completion gate.
  - Changed to: score `85/90`; owner/emitter route remains [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), and final C++ remains blank.
  - Summary/evidence: live IDA session `b001_mappane_0001AW_20260616` reconfirmed no function object or entry xrefs, exact raw bytes/padding, adapter validation through `sub_49C540`, virtual close slot `+0x10`, Win32 `CloseHandle` cleanup, handle reset to `-1`, adapter-validation xrefs within the MIDI source/document family, and active type searches showing no source-quality adapter/context declaration yet.
