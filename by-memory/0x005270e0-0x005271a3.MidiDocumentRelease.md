*** UID:0001CI | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void MidiReleaseDocument(MidiDocument* document)
{
    if (document->datFileSource != 0) {
        if (document->datFile.IsOpen()) {
            document->datFile.Close();
        }
    } else if (document->fileHandle != INVALID_HANDLE_VALUE) {
        CloseHandle(document->fileHandle);
        document->fileHandle = INVALID_HANDLE_VALUE;
    }

    if (document->tracks != NULL) {
        for (DWORD i = 0; i < document->trackCount; ++i) {
            MidiTrackState* track = document->tracks + i;
            if (track->buffer != NULL) {
                HGLOBAL block = GlobalHandle(track->buffer);
                GlobalUnlock(block);
                block = GlobalHandle(track->buffer);
                GlobalFree(block);
            }
        }

        HGLOBAL table = GlobalHandle(document->tracks);
        GlobalUnlock(table);
        table = GlobalHandle(document->tracks);
        GlobalFree(table);
        document->tracks = NULL;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: MIDI document/parser cleanup helper attached to `MidiPlayer.cpp`; live IDA evidence confirms isolated `0xc3` body, three direct callers, `CloseHandle`/`GlobalUnlock`/`GlobalFree` cleanup, 40-byte track-entry table walk, and source-name caveat. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005270e0-0x005271a3 Midi Document Release

## Status

- Confidence: strong for modeled bounds, direct caller set, cleanup behavior, and field-offset map; medium for final source type and field names.
- Entity kind: internal MIDI document/resource cleanup helper.
- Source owner: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), as private `MidiDocument` support in `audio/MidiPlayer.cpp`; no separate private-file route is evidenced.

## Behavior

This helper releases the MIDI document/parser state used by the WinMM stream pipeline.

IDA MCP decompilation shows:

- If a stream/parser object at offset `0x18` is active, it checks and invokes the virtual cleanup/close method at that subobject.
- Otherwise, if the file handle at offset `0x2c` is valid, it closes that handle and stores `-1`.
- If the track table pointer at offset `0x10` is non-null, it walks each 40-byte `MidiTrackState` entry and frees any global-memory block stored at entry offset `0x0c`.
- It then unlocks/frees the backing global-memory block and clears the pointer at offset `0x10`.

## Caller Evidence

IDA MCP callers:

- `0x00525af2` from [UID:0001CE][0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers](by-memory/0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers.md).
- `0x00525d91` from [UID:0001CG][0x00525be0-0x00525dbb.MidiStreamCallback](by-memory/0x00525be0-0x00525dbb.MidiStreamCallback.md).
- `0x00526514` from `MidiPlayer_ResetStream`.

The adjacent [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md) page covers the file/adapter read helpers, and [UID:00023C][0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers](by-memory/0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers.md) covers the SMF load/stream-fill logic that owns the same document state.

Additional 2026-06-01 IDA MCP evidence:

- `lookup_funcs 0x005270e0` reports `sub_5270E0`, size `0xc3`, ending at `0x005271a3`.
- `callees 0x005270e0` reports `sub_49C540`, `CloseHandle`, `GlobalUnlock`, and `GlobalFree`.
- Decompilation confirms the helper dispatches virtual cleanup for the active parser subobject at `this + 0x18`, closes the file handle at `this + 0x2c` when no parser object is active, walks `this + 0x10` as an array of 40-byte entries, frees per-entry global-memory blocks at entry offset `0x0c`, then unlocks/frees the backing table and clears `this + 0x10`.
- `get_bytes 0x005270d5 size 11` and `get_bytes 0x005271a3 size 13` confirm `0xcc` padding before and after this helper, placing it between the MIDI parser wrapper and SMF stream-fill helper.

## 2026-06-06 Live IDA MCP Evidence

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms the cleanup helper:

- `lookup_funcs` reports modeled `sub_5270E0` at `0x005270e0`, size `0xc3`; `0x005271a3` is not a modeled entry, and the next modeled function is `sub_5271B0` at `0x005271b0`.
- Direct entry callers are `0x00525af2` from [UID:0001CE][0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers](by-memory/0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers.md), `0x00525d91` from [UID:0001CG][0x00525be0-0x00525dbb.MidiStreamCallback](by-memory/0x00525be0-0x00525dbb.MidiStreamCallback.md), and `0x00526514` from `MidiPlayer_ResetStream`.
- Callees are `sub_49C540`, `CloseHandle`, `GlobalUnlock`, and `GlobalFree`; focused instruction inspection also confirms the repeated `GlobalHandle` calls before each unlock/free pair.
- Bytes immediately before `0x005270e0` are eleven `0xcc` bytes, and bytes after the final `ret` through `0x005271b0` are thirteen `0xcc` bytes, so the documented `0x005270e0-0x005271a3` bounds are isolated from the neighboring MIDI helpers.

## Source-State Cleanup Map

| State | Evidence |
| --- | --- |
| Adapter/source active flag | `this + 0x14`; when set, the helper tests the adapter/source subobject and dispatches cleanup instead of closing a Win32 handle. |
| Adapter/source subobject | `this + 0x18`; passed to `sub_49C540`, then cleaned through vtable slot `+0x10` when active. |
| Win32 file handle | `this + 0x2c`; closed with `CloseHandle` when not `-1`, then reset to `-1`. |
| Track table pointer | `this + 0x10`; when non-null, the helper walks the table, frees per-track blocks, then unlocks/frees the backing table and clears this field. Historical/generated text may call this an event table, but the source-quality name is track table. |
| Track count | `this + 0x0c`; controls the cleanup loop. |
| `MidiTrackState` entry shape | Entries are 40 bytes (`0x28`) apart; entry offset `+0x0c` holds the per-track global-memory buffer pointer released with `GlobalHandle`, `GlobalUnlock`, and `GlobalFree`. |

## 2026-07-14 B004 Loader-Failure Cleanup Sync

- UID0002C7's corrected `MidiDocument::Load` sends every source-open/read/tag/length/allocation/short-read/VLQ failure to this exact release operation before returning `MIDI_LOAD_FAILED` (`1`). The source-facing `MidiReleaseDocument(this)` cleanup is therefore behaviorally identical to the loader's converged binary failure tail.
- The loader does not clear `allocatedPayloadCount` on entry. This release path remains responsible for active adapter/Win32 source closure and global-memory track/table cleanup; its existing three external callers from stream open, callback, and reset are unchanged.
- The exact 0x28 `MidiTrackState` stride and buffer pointer at `+0x0c` align with the corrected loader allocation. Per-track `GlobalHandle`/`GlobalUnlock`/`GlobalFree`, table unlock/free, and final `tracks = NULL` remain preserved.
- UID00023C is now a false/non-emitting index; UID0002C7 owns the loader body and this UID0001CI page retains the standalone cleanup body. No cleanup code is duplicated into the parent index or replaced by abstract containers.
- Score `88/90`, owner/emitter UID0000LD, reconstructable true, blank optional position, `Nested:-4`, and the complete formal body remain unchanged. The callback adds only accepted source-contract/link evidence.

## Score Rationale

Raised from `84/90` to `85/90` on 2026-06-14. The current page has live IDA confirmation for the modeled function boundary, padding before and after the helper, all direct callers, callees, branch shape, document-state field offsets, per-track table stride, and exact global-memory cleanup sequence. The 2026-06-14 refresh reconfirmed `sub_5270E0` size `0xc3` and the caller relationship from stream open, callback, and reset. The 2026-06-30 B013 bounded callback raises completion to `88` after accepting formal `MidiReleaseDocument` C++; confidence remains `90` because source names for the MIDI document type, adapter/source object, track table entries, and nearby helper split are still inferred.

## Reconstruction Notes

This is not a generic file utility despite `CloseHandle` and `GlobalFree` use. Its only observed callers are the MIDI stream helpers, so keep it with the `MidiPlayer` source family unless a broader MIDI parser/document module is recovered.

Marked `RECONSTRUCTABLE:TRUE` and attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) on 2026-06-01. The formal C++ block emits `MidiReleaseDocument`; role-based `MidiDocument`/`MidiTrackState` field names are accepted. Recovered original spellings remain a confidence cap, while the searched but unsupported private-SMF file alternative is rejected for current routing.

B001's 2026-06-17 source-quality pass clarifies that the 40-byte entries should be treated as `MidiTrackState` records, not generic event entries. Keep older `event table` wording only as historical/generated terminology when needed for traceability.

## Change Log

- 2026-07-14 B004 UID00023C callback: score/formal/metadata unchanged; recorded exact equivalence between UID0002C7's converged load-failure tail and `MidiReleaseDocument`, preserved all source/track/table cleanup behavior, and resolved current source routing to private MidiPlayer.cpp support without duplicating cleanup.
- 2026-06-30 B013 bounded empty-emitter implementation: changed score from `85/90` to `88/90`, kept owner/emitter route [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), and populated the formal C++ block with `MidiReleaseDocument`. Current MCP decompilation proves the DATFile/Win32 close branches, three direct callers, 40-byte track-entry walk, per-track `GlobalHandle`/`GlobalUnlock`/`GlobalFree` cleanup, track-table free, and table-pointer clear.
- 2026-06-14: Raised to `85/90` after live IDA MCP session `b001_0003gy` reconfirmed the modeled boundary, direct callers from stream open/callback/reset, cleanup callees, and neighboring MIDI helper boundaries. Final C++ remains blank under the active `90/90+` code-entry gate.
- 2026-06-17 B001 MIDI source-quality execution: score unchanged at `85/90`; updated terminology from generic event/data table to `MidiTrackState` track table while preserving the historical/generated wording caveat for traceability.
- 2026-06-06: Raised to `84/90` after live IDA MCP reconfirmed modeled bounds, direct caller set, callee set, cleanup branch shape, document-state offsets, track/event table stride, and padding. Final C++ remains blank below the source-grade threshold.

## Cross-References

- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:0001CF][0x00525b10-0x0052664b.MidiPlayerAndWinMMHelpers](by-memory/0x00525b10-0x0052664b.MidiPlayerAndWinMMHelpers.md)
- [UID:0001CE][0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers](by-memory/0x00525920-0x00525b0d.MidiStreamOpenAndPrimeBuffers.md)
- [UID:0001CG][0x00525be0-0x00525dbb.MidiStreamCallback](by-memory/0x00525be0-0x00525dbb.MidiStreamCallback.md)
- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:00023C][0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers](by-memory/0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers.md)
