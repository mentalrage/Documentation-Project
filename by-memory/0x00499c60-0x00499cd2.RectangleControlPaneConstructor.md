*** UID:00011P | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
RectangleControlPane::RectangleControlPane(int drawColor, const RectBounds *bounds)
    : ControlPane(8, bounds)
{
    SetDrawColor(drawColor);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact RectangleControlPane constructor; live IDA confirms `0x72` size, kind-8 ControlPane base call, three vtable installs, color setter, sole MiniMapDialog caller, and SEH cleanup routed directly to inherited Pane teardown rather than UID0004HL. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00499c60-0x00499cd2 - RectangleControlPane Constructor

## Status

- Confidence: strong for IDA function boundary and ownership.
- Related class: [UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md)
- Related file: [UID:0000N1][RectangleControlPane](by-file/RectangleControlPane.md)
- Live IDA size: `0x72`
- Current reconstruction-code gate clears: this page is `RECONSTRUCTABLE:TRUE`, routes through [UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md) to [UID:0000N1][RectangleControlPane](by-file/RectangleControlPane.md) / `NexusTK/ui/controls/RectangleControlPane.cpp`, and the current `92/94` score gives `(92 + 94) / 2 = 93`.
- Formal C++ is populated for the source constructor only. Compiler vptr stores, SEH scaffolding, and destructor adjustor thunks are compiler output and are not handwritten source.

## Covered Range

| Range | Owner | Role |
| --- | --- | --- |
| `0x00499c60-0x00499cd2` | `RectangleControlPane::RectangleControlPane` | Constructs `ControlPane` type `8`, installs rectangle vtables, and applies the configured color. |

## Evidence Notes

- Live IDA `lookup_funcs` reports size `0x72`, giving end-exclusive boundary `0x00499cd2`.
- The constructor calls the shared `ControlPane` base constructor at `0x004949e0` with kind `8`, installs the three Rectangle vtables, and calls the inherited color/draw-state setter at `0x004b9660` with the supplied color argument.
- `0x004b9660` is source-facing `SetDrawColor`/GrafPort draw-state dependency evidence, not the stale generated `FittingRoomListPane::SetTextColor` owner/name; [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) documents that helper as writing the active draw color to inherited field `this+0x74`.
- The only ordinary code caller is `0x0045252c` inside [UID:00008C][MiniMapDialog](by-class/MiniMapDialog.md), where minimap symbol rendering creates rectangle-backed visual controls. MiniMap is a consumer, not the constructor owner.
- The next modeled function is the non-deleting destructor helper at `0x00499ce0`; `0x00499cd2-0x00499ce0` is fourteen `0xcc` alignment bytes.

## Source Shape

The source-facing constructor is `RectangleControlPane::RectangleControlPane(int drawColor, const RectBounds *bounds)`. The caller at `0x0045252c` pushes a local bounds pointer and color `0x80`; the constructor passes the bounds pointer to `ControlPane(8, bounds)` and passes the color to the inherited active draw-color setter at `0x004b9660`.

The explicit stores to `0x00618434`, `0x0061849c`, and `0x006184cc` are compiler-generated installation of the `RectangleControlPane` primary, secondary, and tertiary vtable views. Source reconstruction should rely on the class declaration and constructor definition rather than handwritten vptr assignments.

Rejected source-shape alternatives:

- `RectangleControlPane(unsigned char controlType, const RectBounds *bounds)` is rejected because the `8` control kind is an internal base-constructor argument, not a caller-supplied parameter.
- `RectangleControlPane(const RectBounds *bounds, int drawColor)` is rejected because the caller pushes the bounds pointer first and `0x80` second; the constructor then reads `[ebp+0x08]` as the color and `[ebp+0x0c]` as the bounds pointer.
- Handwritten vptr assignment C++ is rejected because the three stores are compiler output from constructing a polymorphic derived class.

## Binary Evidence

The accepted byte-level source shape for the constructor is:

```text
0x00499c89  push dword ptr [ebp+0x0c]
0x00499c8c  push 8
0x00499c8e  call 0x004949e0
0x00499c93  push dword ptr [ebp+0x08]
0x00499c9f  mov dword ptr [esi], 0x00618434
0x00499ca5  mov dword ptr [esi+0xa0], 0x0061849c
0x00499caf  mov dword ptr [esi+0xa4], 0x006184cc
0x00499cb9  call 0x004b9660
0x00499ccf  ret 8
```

This establishes `[ebp+0x08]` as `drawColor`, `[ebp+0x0c]` as `bounds`, and `ret 8` as the two-argument `thiscall` cleanup. A local PE direct-call scan found the sole constructor caller at `0x0045252c`; no split is needed because the half-open range closes at `0x00499cd2` and the next sibling helper starts at `0x00499ce0`.

The sole ordinary call site is in `MiniMapDialog::RenderSymbols`:

```text
0x00452521  lea ecx, [ebp-0x50]
0x00452524  push ecx
0x00452525  push 0x80
0x0045252a  mov ecx, eax
0x0045252c  call 0x00499c60
```

That call sequence proves the source call shape as `drawColor = 0x80, bounds = &localRect`. It is consumer evidence only; MiniMap does not own the reusable control constructor.

`ControlPane::ControlPane` at `0x004949e0` receives the internal kind `8` and the optional bounds pointer. The supporting base-constructor docs describe it as calling `Pane` construction with mode `1`, writing the control-type byte at `this+0xfe`, installing the ControlPane vtable views, applying the optional bounds through `0x00544bd0`, and initializing shared control state fields.

The draw-state helper called at `0x004b9660` is the inherited active draw-color setter:

```text
0x004b9663  mov eax, [ebp+0x08]
0x004b9666  mov [ecx+0x74], eax
0x004b966a  ret 4
```

[UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) supports treating this as `SetDrawColor`/active draw-state behavior rather than a feature-owned generated helper name.

## Vtable Evidence

The constructor writes RectangleControlPane vtable bases also documented by [UID:0002OE][0x00618430-0x006184d4.RectangleControlPaneVtableData](by-memory/0x00618430-0x006184d4.RectangleControlPaneVtableData.md):

- `0x00618434` primary view, whose scalar deleting destructor entry points at `0x0049b490`.
- `0x0061849c` secondary view, whose adjustor thunk points at `0x0049afc1`.
- `0x006184cc` tertiary view, whose adjustor thunk points at `0x0049afcc`.

The same vtable block contains the paint virtual at `0x00618478 -> 0x00499d00` and `HitTestPart(int,int)` at `0x00618494 -> 0x0049b8f0`. Those are sibling methods, not source bytes owned by this constructor page. The pre-2026-08-16 fixed type-id interpretation is retained only as superseded historical provenance; `retn 8`, the terminal primary slot, and same-slot sibling overrides establish the current HitTestPart identity.

## Constructor-Unwind Destructor Evidence

- This constructor contains SEH/security-cookie scaffolding. Its cleanup funclet at `0x005fd191` reloads the object pointer and jumps to `0x00421370`.
- `0x00421370` is a one-instruction thunk that jumps directly to inherited Pane teardown at `0x00544580`.
- The unwind path does not call or point to ordinary RectangleControlPane destructor UID0004HL at `0x00499ce0`. A full-image little-endian pointer search also found no target pointer.
- Therefore UID0004HL is not a compiler-required constructor-unwind helper. The strongest source cause for that separate function, placed after this constructor and before OnPaint, is the authored out-of-line empty class destructor documented by UID0004HL.
- The constructor's own source body, metadata, score, and formal C++ remain unchanged. SEH cleanup, vptr stores, and base teardown remain compiler output.

## Negative Evidence

- Direct `.text` call/jump scanning found exactly one ordinary call to the constructor, at `0x0045252c`.
- No absolute dword pointer/table reference to `0x00499c60` was found, so the constructor is ordinary directly called code rather than a vtable slot or callback table entry.
- The paint virtual `0x00499d00` and `HitTestPart(int,int)` virtual `0x0049b8f0` have no direct ordinary call/jump hits in the checked executable; their observed reachability is vtable-based.
- `0x00499cd2-0x00499ce0` is all `0xcc` padding, closing the constructor before the local non-deleting destructor helper.

## Ownership And Rejected Routes

[UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md) remains the direct semantic owner because the constructor installs RectangleControlPane vtables and belongs to the class method family. [UID:0000N1][RectangleControlPane](by-file/RectangleControlPane.md) remains the generated source root through the class route.

Rejected direct owners:

- MiniMap/MiniMapDialog: the sole direct caller consumes the control but does not own its constructor, vtables, destructor, paint virtual, or `HitTestPart(int,int)` virtual.
- ControlPane: the base constructor is a dependency; the source body is for the derived `RectangleControlPane` constructor.
- GrafPort/draw-state helpers: `0x004b9660` is a setter dependency, not the owner of the constructor.
- File root direct ownership: [UID:0000N1][RectangleControlPane](by-file/RectangleControlPane.md) is the generated source root, but the class is the narrower direct semantic owner.

## Score And Gate Rationale

Current completion is `92` because the page records the active code-entry gate, exact source constructor shape, argument order, inherited field effect, vtable/source ownership, caller and EH evidence, closed range boundary, formal C++, and resolved whole-file placement. Current confidence is `94` because the small linear body, local PE bytes, live IDA evidence, class layout, and generated source agree. The score stays below final-audit `95+` because exact original parameter/helper spelling remains inferred rather than source-symbol proven. Earlier `88/92` values are retained only in the dated 2026-07-14 and 2026-06-19 history below.

## Cross-References

- [UID:00011Q][0x00499c60-0x00499d37.RectangleControlPaneCore](by-memory/0x00499c60-0x00499d37.RectangleControlPaneCore.md)
- [UID:00011R][0x00499d00-0x00499d37.RectangleControlPanePaint](by-memory/0x00499d00-0x00499d37.RectangleControlPanePaint.md)
- [UID:0000LE][MiniMap](by-file/MiniMap.md)
- [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md)

## 2026-08-16 B006 Whole-File Reconciliation

- Exact binary identity is `[0x00499c60,0x00499cd2)`, 114 bytes, SHA256 `327363D27A59A1FA412FD0B26772EB1A226085D2ECCAD156F9121FD21EA23538`. The next 14 bytes are alignment, not constructor source.
- The source constructor is `RectangleControlPane(int drawColor, const RectBounds *bounds) : ControlPane(8, bounds)`, followed by `SetDrawColor(drawColor)`. Three derived-view vptr stores are MSVC construction mechanics and are intentionally absent from formal source.
- The sole direct caller is callsite `0x0045252c` in MiniMap function `0x004523d0`; that consumer does not own the generic control class. Compiler EH continuation `0x005fd191` tears down inherited Pane state through the established base route and does not make the ordinary Rectangle destructor an EH helper.
- Complete-object size `0x108` and primary/EventHandler/TimerHandler views at offsets `0`, `0xA0`, and `0xA4` are class-layout evidence. This constructor does not access Rectangle-specific data members, supporting no added fields beyond `ControlPane`.
- Source position `20` follows the class declaration and precedes the ordinary destructor, `OnPaint`, and `HitTestPart`. `92/94` is justified by exact body, boundary, caller, EH, owner, class layout, and source form; only original lexical spellings remain inferred.

## Changes

- 2026-07-14 B001 UID0004HL support update:
  - Preserved `88/92`, UID0000BT owner/emitter, reconstructable true, blank position, `Nested:0`, and the complete constructor formal block.
  - Added the exact `0x005fd191 -> 0x00421370 -> 0x00544580` unwind chain and its exclusion of UID0004HL, closing the possibility that the ordinary destructor exists only for constructor cleanup.

- 2026-06-19 B004 accepted source-quality implementation:
  - Before: `COMPLETION:84`, `CONFIDENCE:90`, correct owner/emitter route, but the page still carried stale final-C++ blocker text citing the obsolete `95/95` threshold.
  - After: `COMPLETION:88`, `CONFIDENCE:92`, unchanged [UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md) owner/emitter route, and formal first-draft constructor C++ under the active combined-score/emitter gate.
  - Evidence: B004 source-quality research and local PE byte checks confirm the exact half-open range `0x00499c60-0x00499cd2`, no needed split, sibling destructor helper start at `0x00499ce0`, constructor argument order, `ControlPane(8, bounds)` base setup, three RectangleControlPane vtable installs as compiler output, inherited draw-color setter call to `this+0x74`, sole direct MiniMapDialog consumer call at `0x0045252c`, and MiniMap/ControlPane/GrafPort/file-root owner alternatives rejected as direct owner.
- 2026-06-05 A004 live refresh:
  - Before: the page was a 70/85 boundary stub with no parent attachment and a raw IDA function label.
  - After: raised to `84/90`, attached to [UID:0000BT][RectangleControlPane](by-class/RectangleControlPane.md), removed the raw function label, and kept final C++ blank.
  - Evidence: live IDA confirms exact size, constructor caller, base-constructor kind, vtable installs, color setter call, and the alignment gap before the local destructor helper.
