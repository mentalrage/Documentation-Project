*** UID:00045J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IntegrateMacroDialog::AdvanceMacroPage()
{
    if (m_currentPage >= m_pageCount - 1)
        return;

    ++m_currentPage;

    GetChild<ImageButtonControlPane>(kMacroPreviousPageCommand)->Enable();
    GetChild<ImageButtonControlPane>(kMacroNextPageCommand)->Deactivate();

    if (m_currentPage == m_pageCount - 1)
        GetChild<ImageButtonControlPane>(kMacroNextPageCommand)->Disable();

    RefreshMacroEditFields();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained out-of-line `IntegrateMacroDialog::AdvanceMacroPage()` helper with no current inbound route, exact class-field/button semantics, tail-call refresh, and code-ready formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00542100-0x00542172 IntegrateMacroDialogAdvanceMacroPageRaw

## Status

- Address range: `0x00542100-0x00542172` half-open, size `0x72` / decimal `114` (Verified with int_convert.py).
- Current IDA/MCP function: not modeled as a function at this address in session `supervisor_20260628_resume`.
- Direct semantic owner and emitter: [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) `IntegrateMacroDialog`.
- Source file route: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) `MacroDialogs`, projected to `NexusTK/ui/dialogs/MacroDialogs.cpp`.
- Parent split/container: [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md) `0x00541b30-0x00542265.IntegrateMacroDialog`.
- Rebuild handling: retained out-of-line `IntegrateMacroDialog::AdvanceMacroPage()` helper. The current binary has no proven inbound route to the helper entry, but the source-shaped class body is complete and now emits formal C++ through [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md).
- Formal C++ disposition: populated with the accepted first-draft `IntegrateMacroDialog::AdvanceMacroPage()` body. The old split-stage blank-C++ wording is superseded by this child-specific source-shape decision.

## Behavior

This helper advances the integrated macro editor to the next page when a later page exists. The decoded body checks whether `m_currentPage` (`this+0x270`, decimal `624`, Verified with int_convert.py) is below `m_pageCount - 1` (`this+0x26c`, decimal `620`, Verified with int_convert.py), increments the current page, updates the previous and next buttons, disables the next button when the new page is the last page, and refreshes the visible macro rows.

The control route comes through the dialog child-control list at `this+0x1fc` / decimal `508` (Verified with int_convert.py). Pushed `0x0c` is control id `12` (Verified with int_convert.py), the previous-page button. Pushed `0x0d` is control id `13` (Verified with int_convert.py), the next-page button. Virtual slot `+0x4c` is the image-button enable/show operation, slot `+0x58` is inherited `ControlPane::Deactivate()`, and slot `+0x50` is the image-button disable/hide operation.

The helper does not clear row edit fields before changing pages. That clearing is present in the reachable `OnButtonClick` command `13` path, so this range represents only the retained page-advance sub-operation.

## Live MCP Evidence

B010 rechecked this target with live MCP session `supervisor_20260628_resume` on 2026-06-28. `idb_list` showed the active worker session for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` request ids `20` and `36` returned `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`, and strings cache size `2067`.

MCP `lookup_funcs` request id `21` confirmed the local function/range facts:

| Address | Result |
| --- | --- |
| `0x00541fa0` | `sub_541FA0`, size `0x111`, modeled `OnButtonClick`. |
| `0x005420d0` | `sub_5420D0`, size `0x2d`, modeled `ClearMacroEditFields`. |
| `0x00542100` | not a function. |
| `0x00542172` | not a function; padding begins. |
| `0x00542180` | not a function; paired retreat raw helper begins. |
| `0x005421e3` | not a function; padding begins. |
| `0x005421f0` | `sub_5421F0`, size `0x75`, modeled `RefreshMacroEditFields`. |
| `0x00542265` | not a function; successor padding begins. |

MCP `get_bytes` request id `26` confirmed `0x005420fd-0x00542100` is three `0xcc` bytes, `0x00542100-0x00542172` is executable helper code, `0x00542172-0x00542180` is fourteen `0xcc` bytes, and `0x005421e3-0x005421f0` is thirteen `0xcc` bytes.

MCP `insn_query` request id `24` decoded 39 instructions for this range, all with `fn:null`:

```asm
0x00542100  push esi
0x00542101  mov esi, ecx
0x00542103  mov eax, [esi+26Ch]
0x00542109  mov ecx, [esi+270h]
0x0054210f  dec eax
0x00542110  cmp ecx, eax
0x00542112  jge short 0x00542170
0x00542114  lea eax, [ecx+1]
0x00542117  mov ecx, [esi+1FCh]
0x0054211d  mov [esi+270h], eax
0x00542123  push 0Ch
0x00542127  call dword ptr [eax+10h]
0x0054212e  call dword ptr [eax+4Ch]
0x00542137  push 0Dh
0x0054213b  call dword ptr [eax+10h]
0x00542142  call dword ptr [eax+58h]
0x00542145  mov eax, [esi+26Ch]
0x0054214c  cmp [esi+270h], eax - 1
0x00542152  jnz short 0x00542168
0x0054215a  push 0Dh
0x0054215e  call dword ptr [eax+10h]
0x00542165  call dword ptr [eax+50h]
0x00542168  mov ecx, esi
0x0054216a  pop esi
0x0054216b  jmp sub_5421F0
0x00542170  pop esi
0x00542171  retn
```

MCP `xrefs_to` request id `22` reported zero cross-references to `0x00542100` and zero cross-references to paired raw retreat entry `0x00542180`. The same request reported three code references to refresh helper `0x005421f0`: live modeled call `0x0054207b` inside `OnButtonClick`, raw tail-jump `0x0054216b`, and raw tail-jump `0x005421dc`. It also reported one data ref to `0x00541fa0` from vtable entry `0x00621178` and three `OnButtonClick` code refs to `0x005420d0` at `0x00541fc0`, `0x0054201e`, and `0x00542085`.

MCP `find_bytes` request id `23` found zero absolute VA/RVA pointer encodings for `0x00542100` and `0x00542180`: `00 21 54 00`, `00 21 14 00`, `80 21 54 00`, and `80 21 14 00` all had zero matches.

MCP `find` request ids `33` through `35` found zero `data_ref`, `immediate`, or `code_ref` matches to `0x00542100`, and found zero `data_ref` or `immediate` matches to `0x005421f0` while preserving the three known `code_ref` hits to `0x005421f0` at `0x0054207b`, `0x0054216b`, and `0x005421dc`.

MCP `decompile 0x00542100` request id `27` failed with `Decompilation failed at 0x542100`, matching the no-function-entry state. MCP `decompile 0x00541fa0` request id `28` confirmed `OnButtonClick` command `13` clears fields first, then duplicates the page-advance logic, enabling previous control `12`, deactivating next control `13`, disabling next at the last page, and calling `RefreshMacroEditFields`. MCP `decompile 0x005421f0` request id `29` confirmed `RefreshMacroEditFields` loops child ids `2..11` and binds rows from `g_pConfig + 0x28f2ec + (currentPage * 10 + rowIndex) * 0x108`. MCP `callees` request id `30` confirmed `OnButtonClick` calls `ClearMacroEditFields`, `RefreshMacroEditFields`, dialog close/remove helpers, and `SaveUserSettings_50ABA0`.

## Supplemental PE Route Scan

A read-only scan of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` found no direct route to this helper entry:

| Target | File offset | VA dword hits | RVA dword hits | direct `E8`/`E9` rel32 hits |
| --- | --- | --- | --- | --- |
| `0x00542100` | `0x141500` | `0` | `0` | none |
| `0x00542180` | `0x141580` | `0` | `0` | none |
| `0x005421f0` | `0x1415f0` | `0` | `0` | `0x0054207b`, `0x0054216b`, `0x005421dc` |

The zero-inbound-route result remains visible as a reachability caveat. It is not rewritten as solved by xrefs; the accepted source resolution is that the complete class-owned helper body is reconstructable as retained out-of-line source even though the current binary has no proven caller, pointer, immediate, or direct branch to the helper entry.

## Source Shape And Name Rationale

Best source-facing interpretation: `IntegrateMacroDialog::AdvanceMacroPage()`. The exact original spelling is not proven, but this name is a stronger source reconstruction than keeping `sub_542100` or an address-shaped raw label because the body only uses `this` fields, performs one page-advance operation, updates the previous/next page buttons, and tail-calls the already modeled `RefreshMacroEditFields()`.

The helper is intentionally kept as a separate retained out-of-line class method rather than a covered-by marker for [UID:00045G][0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand](by-memory/0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand.md). `OnButtonClick` command `13` includes an extra `ClearMacroEditFields()` prelude before the duplicated page-advance logic; this helper represents only the page-advance sub-operation after clearing has already happened elsewhere. Keeping the helper emitted records the retained source-shaped bytes without inventing a current call edge.

## Rejected Alternatives

- Blank no-code proof alone: rejected. The no-route fact remains true, but the body is complete, self-contained, class-field-specific, and implementation-ready.
- Covered-by marker to [UID:00045G][0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand](by-memory/0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand.md): rejected. The live command handler includes `ClearMacroEditFields()` before the duplicated advance behavior, so a covered-by marker would lose this range's exact source semantics.
- Padding or non-reconstructable reclassification: rejected. MCP decodes executable class helper code between verified `0xcc` padding spans.
- File-root owner bypass through [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) only: rejected. [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) is the source file route, but the direct semantic owner/emitter is [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) `IntegrateMacroDialog`.
- Aggregate parent C++ through [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md): rejected. The parent is a non-emitting split/container index spanning many children and padding; this exact child owns the helper body.

## Boundaries

- Previous range `0x005420fd-0x00542100` is three bytes of `0xcc` padding after `ClearMacroEditFields`.
- This range `0x00542100-0x00542172` is the retained `AdvanceMacroPage()` helper body.
- Next range `0x00542172-0x00542180` is fourteen bytes of `0xcc` padding before the raw retreat helper [UID:00045K][0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw](by-memory/0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw.md).
- The broad parent [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md) remains non-emitting; only this exact child receives the accepted formal C++.

## Score Rationale

Completion is raised from `86` to `90` because the child-specific source decision is resolved, the raw body is instruction-mapped, the no-route searches were redone with current MCP, and the formal C++ is now present in the target's reconstruction block. Confidence is raised from `88` to `91` because live MCP reconfirmed function state, xrefs, byte patterns, raw disassembly, and modeled handler/refresh relationships. The score remains below final-audit levels because the original helper name is inferred, the current binary still has no proven inbound route to `0x00542100`, and the paired retreat helper [UID:00045K][0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw](by-memory/0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw.md) remains a separate target not repaired by this page.

## Changes

- 2026-06-28 B010 implementation callback:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`, blank `EMITTER_UIDS`, and blank formal C++ with split-stage wording.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`, `EMITTER_UIDS:00006L`, and formal C++ for retained `IntegrateMacroDialog::AdvanceMacroPage()`.
  - Preserved the zero-inbound-route caveat from current MCP and supplemental PE scanning. The accepted resolution is source-shape inference, not a discovered caller/xref.
  - Rejected blank no-code proof alone, covered-by marker to [UID:00045G][0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand](by-memory/0x00541fa0-0x005420b1.IntegrateMacroDialogOnControlCommand.md), padding/non-reconstructable disposition, file-root-only ownership, and aggregate parent emission through [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md).
