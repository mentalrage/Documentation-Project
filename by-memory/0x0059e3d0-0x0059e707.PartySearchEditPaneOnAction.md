*** UID:00047G | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000A7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "../auth/PasswordGuard.h"

void PartySearchEditPane::OnControlCommand(int controlIndex, int notifyCode)
{
    (void)notifyCode;

    if (controlIndex == 2)
    {
        CheckBoxTextControlPane *checkBox =
            GetChild<CheckBoxTextControlPane>(2);
        checkBox->SetChecked(!checkBox->GetChecked());
        checkBox->Invalidate();
        return;
    }

    if (controlIndex != 1)
        return;

    SlideCloseVertical();

    g_pUserPane->SetHuntersListFlag(
        GetChild<CheckBoxTextControlPane>(2)->GetChecked());

    wchar_t sourceText[256];
    GetChild<TextEditControlPane>(3)->ReadText(sourceText, 256);
    ValidateChatInputOrThrowPasswordError(sourceText);
    SanitizeWideTextForChat(sourceText,
                            static_cast<unsigned int>(wcslen(sourceText)));
    wcscpy_s(g_pUserPane->GetPartySearchSourceText(), 256, sourceText);

    if (g_pUserListDialogPane != NULL)
    {
        wchar_t localPlayerName[128];
        GetLocalPlayerName(localPlayerName, 128);

        UserListRecord *localRecord = NULL;
        for (int index = 0;
             index < g_pUserListDialogPane->m_allUserRecords->GetCount();
             ++index)
        {
            UserListRecord *record = static_cast<UserListRecord *>(
                g_pUserListDialogPane->m_allUserRecords->GetElementAt(index));
            if (wcscmp(record->name, localPlayerName) == 0)
            {
                localRecord = record;
                break;
            }
        }

        if (localRecord->huntersListFlag !=
                g_pUserPane->GetHuntersListFlag() ||
            wcscmp(localRecord->sourceText,
                   g_pUserPane->GetPartySearchSourceText()) != 0)
        {
            localRecord->huntersListFlag =
                g_pUserPane->GetHuntersListFlag();
            localRecord->hasSourceText = 0;
            wcscpy_s(localRecord->sourceText, 256,
                     g_pUserPane->GetPartySearchSourceText());

            unsigned char packet[259];
            PacketBufferWriteUInt8(0x84, &packet[0]);
            PacketBufferWriteUInt8(localRecord->huntersListFlag, &packet[1]);
            short packetSize = 2;

            if (localRecord->huntersListFlag == 1)
            {
                char encodedText[256];
                const int encodedLength = WideCharToMultiByte(
                    CP_ACP, 0, localRecord->sourceText,
                    static_cast<int>(wcslen(localRecord->sourceText)),
                    encodedText, 256, NULL, NULL);
                PacketBufferWriteUInt8(
                    static_cast<unsigned char>(encodedLength), &packet[2]);
                memcpy(&packet[3], encodedText, encodedLength);
                packetSize = static_cast<short>(encodedLength + 3);
            }

            g_packetSender->QueueAndSendPacket(packet, packetSize);
        }
    }

    g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(this);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: PartySearchEditPane::OnControlCommand(int,int) handles submit control 1 and checkbox control 2: it closes, persists named UserPane party-search state, validates/sanitizes text, mirrors the local packed UserListRecord, conditionally sends opcode 0x84 with a one-byte MBCS length payload, queues deferred deletion, or toggles and invalidates the checkbox. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0059e3d0-0x0059e707 - PartySearchEditPaneOnControlCommand

## Status

- Entity kind: source-authored virtual `PartySearchEditPane::OnControlCommand(int,int)`.
- Exact half-open range: `[0x0059e3d0,0x0059e707)`.
- Owner: [UID:0000A7][PartySearchEditPane](by-class/PartySearchEditPane.md).
- Source emitter: [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md), optional position blank, `Nested:0`.
- Rebuild handling: emit the complete human source above; no packet/text/control blocker remains.

## Signature And Virtual Evidence

- PartySearch primary vtable base `0x0062ea40` stores this function at cell `0x0062ea88`, slot `+0x48`. Accepted DialogPane slot parity names it `OnControlCommand`.
- Two stack arguments follow `this`; the first is the child control index/id and the second notification code is unused. The exact source signature is `void PartySearchEditPane::OnControlCommand(int controlIndex, int notifyCode)`.
- The stale one-argument `OnAction` identity came from generated semantics rather than the inherited virtual ABI. The physical UID/path is retained for validator stability, but the H1, Item Summary, formal, and current prose use the correct source identity.

## Control Branches And Ordering

- Control index `2` obtains child 2 as `CheckBoxTextControlPane`, negates its current checked state, applies the accepted setter, invalidates the child, and returns. The observed machine-level direct byte toggle at child `+0x108` is a normal inlining/lowering result; it does not transfer generic setter ownership.
- Any control index other than `1` or `2` returns without side effects.
- Submit control `1` closes the dialog first, then copies child-2 checked state into `g_pUserPane->SetHuntersListFlag(...)`, processes child-3 text, optionally updates/sends through the active UserList dialog, and queues this pane for deferred deletion at the end.
- `notifyCode` is intentionally ignored. Source acknowledges it without inventing a branch or changing the virtual signature.

## Text Validation And UserPane State

- Child 3 reads into `wchar_t sourceText[256]` with capacity 256. Password-disclosure validation runs before `SanitizeWideTextForChat`; sanitization length is the current `wcslen` converted to unsigned int.
- `_wcscpy_s` persists the sanitized string into the 256-wide buffer returned by `g_pUserPane->GetPartySearchSourceText()`.
- UserPane current source fields are `m_huntersListFlag` at complete-object `+0x3cb1`, `m_partySearchSourceText[256]` at `+0x3cb2`, and two residual bytes. Public inline accessors preserve direct binary reads/writes without new function entries, raw offsets, friends, or reinterpret access.
- Historical `g_pPlayerData`, `m_preCollectionState[0x203]`, `m_partySearchListed`, and `m_partySearchText` spellings identify the same storage/semantics but are superseded by the accepted typed UserPane union.

## Local UserListRecord Mirror

- The optional `g_pUserListDialogPane` branch obtains the local player name in a 128-wide buffer and linearly scans private `m_allUserRecords` using `GetCount`, `GetElementAt`, and exact `wcscmp` equality on packed-row `name`.
- When the active dialog exists, machine code dereferences the search result without a null guard. The source preserves that program invariant; adding a defensive guard would change observable behavior on an invalid list state.
- The packed `UserListRecord` remains exactly `0x414` bytes. This method reads/writes `huntersListFlag` at `+0x20e`, clears `hasSourceText` at `+0x20f`, and copies into `sourceText[256]` at `+0x210` only when either the flag or text differs from current UserPane state.
- `friend class PartySearchEditPane;` on `UserListDialogPane` is the bounded compile-legal access needed for `m_allUserRecords`. No UserPane friendship or public row-list exposure is required.

## Packet And Send Contract

- Outbound packet byte 0 is opcode `0x84`; byte 1 is `huntersListFlag`. Initial/send size is 2.
- Only when the flag equals exactly `1`, convert `wcslen(sourceText)` UTF-16 characters through `WideCharToMultiByte(CP_ACP,...)` without a source NUL, write the narrowed one-byte encoded length at packet byte 2, copy exactly `encodedLength` bytes at byte 3, and send `encodedLength + 3` bytes.
- `PacketBufferWriteUInt8` uses the accepted value-first contract. `g_packetSender->QueueAndSendPacket(packet, packetSize)` is the current Socket member send route. No NUL terminator is part of the protocol payload.
- Fixed `encodedText[256]` and `packet[259]` arrays plus normal subscripting explain compiler range-check calls at boundary values. The source keeps the ordinary array expressions and excludes explicit range-check-failure scaffolding.
- Write/send return handling is intentionally absent because the original method does not branch on those results. The outbound packet is skipped entirely when row flag/text already match current UserPane state.

## Cleanup, Compiler Separation, And Edge Cases

- Deferred deletion is queued after submit processing regardless of whether `g_pUserListDialogPane` is null, no packet change was needed, or the packet was sent. Checkbox-toggle and unrelated-control paths do not queue deletion.
- Source excludes direct child-vtable dispatch spelling, raw offsets, compiler EH/cookie state, stack cookies, range-check helpers, allocation mechanics, vptr stores, and decompiler labels.
- No handwritten packet wrapper, synthetic storage accessor, scalar destructor, RTTI/vtable array, or new helper is needed; every used API is already declared in accepted support pages.
- Exact original local names and formatting are inferred. This lexical uncertainty caps confidence at 93 but does not affect protocol, layout, method identity, branch behavior, or compile legality.

## Negative And Rejected Evidence

- Rejected: one-argument `OnAction`, UserPane field friendship/public raw fields, g_pUserListDialogPane-owned local settings, raw offset/reinterpret access, a null guard for the local record, a NUL byte in the sent text payload, and generic-checkbox ownership transfer.
- No evidence supports moving this method to UserPane.cpp, a packet module, a new PartySearch source file, or a standalone helper. Caller/class/vtable/resource/record dependencies all converge on UserListDialogPane.cpp.
- The compiler's direct checkbox byte toggle and failure-check lowering are not evidence for hand-authored raw/decompiler source.

## Related Docs

- [UID:0001KI][0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane](by-memory/0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane.md) mixed non-emitting split index.
- [UID:0000A7][PartySearchEditPane](by-class/PartySearchEditPane.md) complete class owner.
- [UID:0000FN][UserListDialogPane](by-class/UserListDialogPane.md) packed record, private master list, and bounded friendship.
- [UID:00047D][0x0059e0d0-0x0059e393.PartySearchEditPaneConstructor](by-memory/0x0059e0d0-0x0059e393.PartySearchEditPaneConstructor.md) constructor/control indices and initial state.
- [UID:00047F][0x0059e3c0-0x0059e3c1.PartySearchEditPaneOnDestroyChildControlsNoop](by-memory/0x0059e3c0-0x0059e3c1.PartySearchEditPaneOnDestroyChildControlsNoop.md) adjacent empty `OnPaint` virtual.

## Score Rationale

- `86/90 -> 92/93`: exact virtual signature, branch behavior, controls, typed state, text validation/sanitization, packed row access, opcode/payload/send ABI, cleanup route, source owner, and complete C++ are resolved.
- Confidence remains below 94 because exact original local spellings and the compiler's originating range-check source expression are inferred; no behavioral or source-emission blocker remains.

## Changes

- 2026-08-17 B001 UID0000MG dependency implementation: score/body/ownership unchanged; added exact `../auth/PasswordGuard.h`. The existing source-text validator call at binary `0x0059e480` remains before sanitization and party-search storage.

- 2026-07-19 B005 UID00047D accepted implementation callback: corrected stale `OnAction` identity to two-argument `OnControlCommand`, set emitter UID0000OZ, raised `86/90 -> 92/93`, installed the complete source body, and incorporated full branch/text/state/record/packet/send/cleanup/compiler/negative/history evidence.
- Historical B003 UID000477 support synchronization supplied the accepted UserPane fields/accessors while intentionally preserving this method as independent direct-target debt. Its earlier blank packet-builder/text-control blocker is now superseded by the complete existing declaration route used in the formal body.
