*** UID:0004EE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void PrettyButtonControlPane2::SetState(unsigned char state)
{
    if (m_visualState != state)
    {
        if (state == 0x0b)
        {
            m_visualStateFrameActive = true;
            m_timerHandler.ScheduleTimer(0, 150, 0, 0);
        }
        else
        {
            m_visualStateFrameActive = false;
            m_timerHandler.RemovePendingTimers();
        }
    }

    ControlPane::SetVisualState(state);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready PrettyButtonControlPane2 visual-state/animation child split from UID0001F9; live MCP session 9fb70afc confirms function 0x0054ba30 size 0x62, vtable-only slot 0x006223c4, inherited m_visualState byte +0x103 comparison, class2 m_visualStateFrameActive byte +0x11d toggling, inherited m_timerHandler +0xa4 150 ms start/remove behavior around raw state literal 0x0b, and delegation to ControlPane::SetVisualState. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0054ba30-0x0054ba92 PrettyButtonControlPane2 SetVisualState

## Evidence

- B004 live MCP session `9fb70afc` was healthy against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`: `server_health` returned `status: ok`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, Hex-Rays ready, and strings cache ready.
- MCP `lookup_funcs` confirms `0x0054ba30 -> sub_54BA30` size `0x62` / decimal 98 (Verified with `int_convert.py`); `0x0054ba92` is not a function.
- MCP `disasm 0x0054ba30` returns 35 instructions from prologue at `0x54ba30` through `retn 4` at `0x54ba8f`. MCP `get_bytes` confirms the complete 98-byte body at `0x0054ba30`, twelve `0xcc` bytes before it at `0x0054ba24-0x0054ba30`, and fourteen `0xcc` bytes after it at `0x0054ba92-0x0054baa0`.
- MCP `analyze_function 0x0054ba30` reports prototype `int __thiscall(char *this, unsigned __int8)`, five basic blocks, cyclomatic complexity `2`, no strings, no direct callers, and exactly three callees: `sub_5975E0`, `sub_494B80`, and `sub_597600`.
- MCP decompilation/disassembly shows the method compares inherited byte `this+0x103` / decimal 259 (Verified with `int_convert.py`) with the incoming state. The comparison gates only the timer/frame-flag side effects; the method delegates to `ControlPane::SetVisualState` through `sub_494B80` even when the state is unchanged.
- If the state changes to raw literal `0x0b` / decimal 11 (Verified with `int_convert.py`), the method writes class2 byte `this+0x11d` / decimal 285 (Verified with `int_convert.py`) to `1`, calls `sub_5975E0(this+0xa4, 0, 150, 0, 0)`, and delegates to `sub_494B80(this, 0x0b)`. The timer delay `0x96` is decimal 150 (Verified with `int_convert.py`).
- If the state changes to any non-`0x0b` value, the method writes `this+0x11d = 0`, calls `sub_597600(this+0xa4)`, and delegates to `sub_494B80(this, state)`. The timer facet address `this+0xa4` is decimal 164 (Verified with `int_convert.py`) and matches inherited `m_timerHandler` usage.
- [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md) documents and emits `TimerHandler::ScheduleTimer` for `sub_5975E0` and `TimerHandler::RemovePendingTimers` for `sub_597600`. Live MCP `analyze_function` confirms this target calls `sub_5975E0` at `0x54ba65` and `sub_597600` at `0x54ba7f`.
- [UID:0003N2][0x00494b80-0x00494bad.ControlPaneSetVisualState](by-memory/0x00494b80-0x00494bad.ControlPaneSetVisualState.md) documents `sub_494B80` as the inherited `ControlPane::SetVisualState` path for byte `+0x103`; live MCP confirms callers include `sub_54BA30` and that it writes `this+0x103` and invalidates through virtual slot `+0x20` on change.
- Vtable page [UID:0002UX][0x00622378-0x0062241c.PrettyButtonControlPane2VtableData](by-memory/0x00622378-0x0062241c.PrettyButtonControlPane2VtableData.md) records slot `0x006223c4 -> 0x0054ba30`. Live MCP `xrefs_to`/`xref_query` returns one data xref to `0x0054ba30` at `0x006223c4`, no direct code callers, and no xrefs to the slot address itself.
- MCP `get_bytes` reads `0x006223c4` as `30 ba 54 00`, and the class1 counterpart slot `0x00622320` as `50 b7 54 00`.
- Local PE pointer-pattern scanning of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` found the VA bytes for `0x0054ba30` exactly once at file offset `0x220dc4`, matching vtable slot VA `0x006223c4`; the RVA bytes for `0x0014ba30` were absent; bytes for the slot address `0x006223c4` were absent. Positive controls found class1 VA `0x0054b750` once at `0x220d20` and class2 OnPaint VA `0x0054baa0` once at `0x220dc0`.
- Sibling [UID:0004E7][0x0054b750-0x0054b7b2.PrettyButtonControlPaneSetVisualState](by-memory/0x0054b750-0x0054b7b2.PrettyButtonControlPaneSetVisualState.md) has the same accepted source shape and helper names, but toggles class1 flag byte `this+0x119`; UID0004EE toggles class2 flag byte `this+0x11d`.

## Ownership And C++ Policy

Owner remains [UID:0000AS][PrettyButtonControlPane2](by-class/PrettyButtonControlPane2.md), emitted through the [UID:0000MP][PrettyButtonControlPane](by-file/PrettyButtonControlPane.md) source module. Direct file ownership is rejected because this is a class virtual method; class1 ownership is rejected because class1 owns the separate sibling at `0x0054b750`; `ControlPane`/`ButtonControlPane` ownership is rejected because UID0004EE adds class2-specific animation flag/timer behavior before delegating to the base setter; CreateUser dialog ownership is rejected because constructor fan-in is consumer evidence, while the method is vtable-routed reusable control code.

Formal C++ is now source-ready as `void PrettyButtonControlPane2::SetState(unsigned char state)`. The method name follows the class row and accepted UID0004E7 counterpart. `m_visualState` names inherited byte `+0x103`, `m_visualStateFrameActive` names the class2 frame-active flag at `+0x11d`, and `m_timerHandler` names the inherited timer facet at `+0xa4`. These names are source-facing inferences, not recovered original symbols, so confidence stays below final-audit range. The raw state literal `0x0b` intentionally remains a literal rather than an invented enum.

The formal body uses `void` despite raw decompiler `int` propagation through `sub_494B80`; the source shape is a side-effecting visual-state setter, and no caller return-value use is recovered. The unconditional final `ControlPane::SetVisualState(state)` preserves both unchanged-state and changed-state paths, including the `0x0b` branch.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact live MCP function size/body, half-open boundary, padding, vtable-only reachability, PE pointer-pattern negatives and controls, inherited state byte, class2 frame-active flag, inherited timer facet, helper roles, sibling class1 source-shape parity, owner/source-placement rationale, and formal first-draft C++ are now documented. It remains below final-audit range because the exact original method spelling and enum name for `0x0b` are not recovered. |
| Confidence | 91 | Current IDA/MCP facts, byte reads, vtable xrefs, helper support docs, class/file route, and accepted class1 counterpart all agree. Confidence remains capped because the state enum and final class declaration/member spellings remain inferred rather than symbol-proven. |

## Rejected Alternatives

- No direct [UID:0000MP][PrettyButtonControlPane](by-file/PrettyButtonControlPane.md) ownership: the file is the source route, while [UID:0000AS][PrettyButtonControlPane2](by-class/PrettyButtonControlPane2.md) is the narrow class owner and emitter.
- No class1 ownership: [UID:0004E7][0x0054b750-0x0054b7b2.PrettyButtonControlPaneSetVisualState](by-memory/0x0054b750-0x0054b7b2.PrettyButtonControlPaneSetVisualState.md) is the counterpart, not the owner; UID0004EE is pointed to by class2 vtable slot `0x006223c4` and uses class2 byte `+0x11d`.
- No base-control ownership: `ControlPane::SetVisualState` is an inherited dependency; UID0004EE wraps it with PrettyButtonControlPane2-specific timer/frame behavior.
- No boolean `SetEnabled` wording: the argument is a byte state and state `0x0b` is special-cased.
- No invented enum name for `0x0b`: behavior is resolved, but original enum spelling is not.
- No no-owner/non-emitting disposition: the direct class vtable slot, known owner, valid emitter route, accepted helper names, and compact source-shaped body are sufficient for first-draft emission.

## Changes

- 2026-07-03 B004 implementation callback for accepted UID0004EE report:
  - Changed metadata from `84/88`, blank emitter, blank formal C++ to `88/91`, `EMITTER_UIDS:0000AS`, and formal first-draft `PrettyButtonControlPane2::SetState(unsigned char state)` C++.
  - Added live MCP session `9fb70afc` evidence for exact function size/body, vtable-only slot `0x006223c4`, no direct callers, no slot-address xrefs, padding before/after, helper callees, inherited `m_visualState` byte `+0x103`, class2 `m_visualStateFrameActive` byte `+0x11d`, inherited `m_timerHandler` `+0xa4`, raw literal `0x0b`, decimal conversions, PE pointer-pattern negative evidence, sibling UID0004E7 parity, owner/source-placement rationale, rejected alternatives, and score rationale.
