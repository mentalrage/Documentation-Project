*** UID:0002GD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ColorStringChattingMessage::ColorStringChattingMessage(
    const wchar_t *text,
    int foregroundColor,
    int backgroundColor,
    unsigned char textStyleFlag,
    int customForegroundRgb,
    int customBackgroundRgb)
{
    MemoryMan *memoryMan = GetMemoryMan();

    size_t byteCount = 0;
    const wchar_t *scan = text;
    do {
        byteCount += sizeof(wchar_t);
    } while (*scan++ != L'\0');

    m_text = static_cast<wchar_t *>(memoryMan->AllocateBufferMemory(byteCount));
    memoryMan->MemmoveWrapper(m_text, text, byteCount);
    m_foregroundColor = foregroundColor;
    m_backgroundColor = backgroundColor;
    m_textStyleFlag = textStyleFlag;
    m_customForegroundRgb = customForegroundRgb;
    m_customBackgroundRgb = customBackgroundRgb;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: First-draft source-ready `ColorStringChattingMessage` constructor duplicates owned UTF-16 text through MemoryMan allocation/memmove, stores foreground/background/style/custom RGB fields, and is backed by current MCP range, caller, callee, vtable, and padding proof. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00483490-0x00483541 - ColorStringChattingMessage Constructor

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored constructor.
- Entity kind: class constructor.
- Owner: [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md).
- Parent aggregate: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Formal C++: first-draft constructor body is present in the managed block above.
- Confidence: strong for range, owner, object layout writes, text allocation/copy, direct callers, vtable evidence, padding, class parent, and first-draft C++ readiness. Exact original member/helper declaration spellings remain inferred.

## Behavior

This constructor initializes a colored chat-message object. It constructs the `LObject`-style base, installs the `ColorStringChattingMessage` vtable, computes the source wide-string byte length including the terminator, allocates owned string storage through `GetMemoryMan()->AllocateBufferMemory`, copies the exact byte count through `MemoryMan::MemmoveWrapper`, then stores foreground/background palette IDs, a one-byte text style selector, and two optional custom RGB values.

Approximate object fields from the binary:

| Offset | Source-facing field | Meaning |
| --- | --- | --- |
| `+0x04` | `m_text` | owned wide-string pointer |
| `+0x08` | `m_foregroundColor` | foreground palette/color id |
| `+0x0c` | `m_backgroundColor` | background palette/color id |
| `+0x10` | `m_textStyleFlag` | one-byte style selector; nonzero selects the outlined/two-pass current-layout path, while RGB override fields are separate |
| `+0x14` | `m_customForegroundRgb` | custom foreground RGB packed value |
| `+0x18` | `m_customBackgroundRgb` | custom background RGB packed value |

The 2026-06-20 destructor pass names `+0x04` as source-facing `wchar_t *m_text`. This constructor is the allocation/ownership source for that field: it computes the UTF-16 byte count including the terminator, allocates through the MemoryMan allocation path, copies the incoming text, and stores the owned pointer at `+0x04`. [UID:0002GE][0x00483550-0x004835a9.ColorStringChattingMessageDestructorBody](by-memory/0x00483550-0x004835a9.ColorStringChattingMessageDestructorBody.md) pairs with this allocation by freeing only `m_text` through `GetMemoryMan` / `FreeBufferMemory`; the color/custom fields remain scalar state and need no destructor cleanup.

The 2026-06-21 clone pass confirms this same parameter/field order from [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md): `m_text`, `m_foregroundColor`, `m_backgroundColor`, `m_textStyleFlag`, `m_customForegroundRgb`, and `m_customBackgroundRgb`. The clone page calls this constructor after a `0x1c` / 28-byte allocation, so constructor source names should stay aligned with the clone and draw pages rather than reverting to generated `field_*` labels. B009's later reconciliation rejects a literal `m_useCustomRgb` name for `+0x10`: draw uses the byte as a style/outline selector, and the actual RGB values are stored separately at `+0x14/+0x18`.

## Source Reconstruction Notes

The formal C++ block intentionally writes ordinary constructor source rather than compiler mechanics. The binary's call to the `LObject`-style base constructor is represented by normal C++ base-constructor emission; do not hand-write `LObject::LObject();` as a statement inside the constructor body.

The earlier prose-only `DuplicateWideString(text)` shape is superseded. No accepted project helper page provides that wrapper name, and current helper docs resolve this body as explicit `GetMemoryMan()` context, `MemoryMan::AllocateBufferMemory(size_t)` allocation, and `MemoryMan::MemmoveWrapper(void *, const void *, size_t)` copying. The source-facing field and parameter names remain reconstructed, but field offsets and ownership semantics are binary evidence.

## IDA MCP Evidence

- IDA function iteration reports `sub_483490` at exact range `0x00483490-0x00483541`.
- Direct callers are `0x0047f051`, `0x0047f0bf`, `0x0047f0fa`, `0x0047f15e`, `0x0047f2c6`, `0x0047f319`, and clone method call site `0x00483604`.
- Decompilation calls `sub_4F4A80`, writes the `ColorStringChattingMessage` vtable, computes the UTF-16 string length manually, allocates with `sub_516050`, copies with `sub_516220`, and writes fields at offsets `+0x08`, `+0x0c`, `+0x10`, `+0x14`, and `+0x18`.
- The following bytes `0x00483541-0x00483550` are confirmed `0xcc` alignment.

Live IDA MCP evidence from 2026-06-05:

- `lookup_funcs` reconfirms `sub_483490` at `0x00483490`, size `0xb1`; `0x00483541` is not a function, the next modeled function starts at `0x00483550`, and the scalar deleting destructor starts at `0x00483e60`.
- `lookup_funcs` resolves the constructor call sites at `0x0047f051`, `0x0047f0bf`, `0x0047f0fa`, and `0x0047f15e` inside `sub_47EFB0`, the call sites at `0x0047f2c6` and `0x0047f319` inside `sub_47F280`, and the clone call at `0x00483604` inside `sub_4835B0`.
- `decompile 0x00483490` confirms the `sub_4F4A80` base setup, vtable write, UTF-16 terminator scan, byte-count calculation including the terminator, allocation through `sub_516050`, copy through `sub_516220`, and writes to text pointer plus foreground/background/custom-color fields at `+0x04`, `+0x08`, `+0x0c`, `+0x10`, `+0x14`, and `+0x18`.
- `callers 0x00483490` and `xrefs_to 0x00483490` report the same seven call sites: four constructor-path calls in `sub_47EFB0`, two add-message calls in `sub_47F280`, and one clone call in `sub_4835B0`.
- `callees 0x00483490` reports `sub_4F4A80`, `sub_516030`, `sub_516050`, and `sub_516220`.
- Vtable audit resolves the constructor write to `0x0061526c`; xrefs to that vtable include chat-pane setup stack-object stores, this constructor, the destructor body, and the scalar deleting destructor.
- Caller-window audit confirms high-resolution and fallback chat-message construction branches in `sub_47F280` and a clone path that forwards the stored text pointer and color fields from offsets `+0x04`, `+0x08`, `+0x0c`, `+0x10`, `+0x14`, and `+0x18`.
- Raw byte audit reconfirms `0x00483541-0x00483550` as fifteen `0xcc` alignment bytes.
- Autogen parent is [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md) because that class scores `86/91`, is marked `RECONSTRUCTABLE:TRUE`, and is already attached to [UID:0000I5][Chatting](by-file/Chatting.md).

Current IDA MCP evidence from session `279422f0` on 2026-06-29:

- `idb_list` showed active session `279422f0`, worker PID `16380`, database `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, `is_active:true`, and `is_analyzing:false`.
- `server_health` returned `status:ok` for module `NexusTK.exe`, imagebase `0x400000`, with auto-analysis, Hex-Rays, and strings cache ready.
- `lookup_funcs` resolved `0x00483490` to `sub_483490`, size `0xb1` / 177 bytes (Verified with `int_convert.py`), and reported no function at `0x00483541`.
- `lookup_funcs` resolved support functions `0x00483550`, `0x004835b0`, `0x00483630`, `0x004836a0`, `0x00483e60`, `0x00516030`, `0x00516050`, `0x00516220`, and `0x004f4a80`.
- `xrefs_to 0x00483490` returned seven code xrefs: `0x0047f051`, `0x0047f0bf`, `0x0047f0fa`, and `0x0047f15e` in `sub_47EFB0`; `0x0047f2c6` and `0x0047f319` in `sub_47F280`; and `0x00483604` in `sub_4835B0`.
- `callees 0x00483490` returned only `sub_4F4A80`, `sub_516030`, `sub_516050`, and `sub_516220`, corresponding to base setup, `GetMemoryMan`, `AllocateBufferMemory`, and `MemmoveWrapper`.
- `get_bytes 0x00483487 size 9` returned nine `0xcc` bytes before the constructor; `get_bytes 0x00483541 size 15` returned fifteen `0xcc` bytes after it.
- `insn_query 0x00483490-0x00483541` confirmed the base call, vtable write to `ColorStringChattingMessage::vftable`, `GetMemoryMan`, UTF-16 terminator scan, byte-count calculation including the terminator, allocation, copy, six field stores, and `retn 18h`.
- `analyze_function` / `decompile 0x00483490` confirmed the text scan, allocation, copy, and field writes at `+0x04`, `+0x08`, `+0x0c`, byte `+0x10`, `+0x14`, and `+0x18`.
- `decompile 0x0047f280` confirmed current-layout calls preserve all six arguments and old-layout calls zero the style/custom arguments; `decompile 0x004835b0` confirmed clone allocates `0x1c` / 28 bytes (Verified with `int_convert.py`) and forwards all stored fields to this constructor.
- `xrefs_to 0x0061526c` confirmed this constructor writes the `ColorStringChattingMessage` vtable; `get_bytes` around `0x0061526c` showed sibling vtable entries for scalar deleting destructor, clone `0x004835b0`, measure `0x00483630`, and draw `0x004836a0`.
- `find_bytes` found no absolute VA/RVA pointer pattern for the constructor start/end; this is negative pointer evidence only and does not weaken the seven direct code xrefs.

## Rejected Alternatives

- Leaving the nonblank emitter with blank formal C++ is rejected because it preserves the generated UID0002GD empty marker even though the target is source-authored, reconstructable, and above the code-entry gate.
- Clearing `EMITTER_UIDS`, using a comment-only marker, or treating this as covered-by aggregate code is rejected because the body is a direct `ColorStringChattingMessage` constructor with complete source-ready behavior.
- `DuplicateWideString(text)` is rejected because no accepted helper page supplies that wrapper and it hides the documented MemoryMan allocation/copy path.
- Raw `sub_516050` / `sub_516220` labels are rejected for source output; use the accepted `GetMemoryMan`, `AllocateBufferMemory`, and `MemmoveWrapper` names while preserving raw addresses in evidence.
- `m_useCustomRgb` is rejected as a literal field name for `+0x10`; B009 clone/draw reconciliation keeps neutral `m_textStyleFlag`, while custom RGB values are stored separately at `+0x14/+0x18`.

## Cross-References

- [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md)

## Changes

- 2026-06-05: Raised completion/confidence to `88/92`, marked the page `RECONSTRUCTABLE:TRUE`, and attached `AUTOGEN_PARENT_UID:00002Z` after live IDA reconfirmed the boundary, seven caller sites, text allocation/copy semantics, color-field writes, callees, vtable xrefs, clone forwarding path, and padding.
- 2026-06-20 B002 Rule 26 support update: linked the constructor allocation/copy path to [UID:0002GE][0x00483550-0x004835a9.ColorStringChattingMessageDestructorBody](by-memory/0x00483550-0x004835a9.ColorStringChattingMessageDestructorBody.md) destructor ownership. Field `+0x04` is now documented as owned `wchar_t *m_text`, with destructor cleanup through MemoryMan and no destructor cleanup needed for scalar color/custom fields.
- 2026-06-21 B006 Rule 26 support update: aligned constructor field names and argument order with [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md): `m_text`, `m_foregroundColor`, `m_backgroundColor`, `m_useCustomRgb`, `m_customForegroundRgb`, and `m_customBackgroundRgb`; documented that clone confirms the constructor call shape after `0x1c` / 28-byte object allocation.
- 2026-06-22 B009 Rule 26 reconciliation: updated the `+0x10` argument/field wording to neutral `m_textStyleFlag` after clone/draw reanalysis showed old `m_useCustomRgb` wording was historical shorthand. The byte is copied by clone and consumed by draw as the current-layout style/outline selector; packed RGB override values remain the separate `+0x14/+0x18` fields.
- 2026-06-29 B004 UID0002GD empty-emitter implementation: raised completion from `88` to `90`, kept confidence `92`, owner/emitter [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md), and `RECONSTRUCTABLE:TRUE`, and inserted first-draft formal `ColorStringChattingMessage::ColorStringChattingMessage(...)` C++. Current MCP session `279422f0` reconfirmed the exact `0x00483490-0x00483541` / `0xb1` / 177-byte range (Verified with `int_convert.py`), seven direct constructor call sites, four callees, UTF-16 byte-count/allocation/copy flow through `GetMemoryMan`, `AllocateBufferMemory`, and `MemmoveWrapper`, field order `m_text`, `m_foregroundColor`, `m_backgroundColor`, `m_textStyleFlag`, `m_customForegroundRgb`, `m_customBackgroundRgb`, vtable write to `0x0061526c`, nine-byte prepad, fifteen-byte postpad, and negative pointer-pattern evidence. The implementation supersedes the prose-only `DuplicateWideString`/explicit-`LObject::LObject()` sketch and resolves the generated UID0002GD empty marker.
