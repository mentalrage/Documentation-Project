*** UID:0001HX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Socket::BuildAndSendFriendNameListSync()
{
    unsigned char packet[1024];
    FriendNameSet countedNames;
    FriendNameSet emittedNames;

    packet[0] = 0x77;
    for (int index = 0; index < 20; ++index) {
        const WideFriendName& friendName = g_pConfig->m_friendNames[index];
        if (!friendName.empty())
            countedNames.insert(friendName);
    }

    packet[1] = static_cast<unsigned char>(countedNames.size());
    int packetSize = 2;
    packet[2] = 0;

    for (int index = 0; index < 20; ++index) {
        const WideFriendName& friendName = g_pConfig->m_friendNames[index];
        if (friendName.empty() || !emittedNames.insert(friendName).second)
            continue;

        const NarrowFriendName narrowName(friendName);
        packet[packetSize++] = 0x0c;
        GetMemoryMan()->MemmoveWrapper(
            packet + packetSize, narrowName.c_str(), 12);
        packetSize += 12;
    }

    QueueAndSendPacket(packet, static_cast<short>(packetSize));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Socket-owned opcode 0x77 friend-name sync over 20 Config slots: two-pass unique counting/emission, first-occurrence output order, exact two-byte header, 13-byte records, 1,024-byte local packet capacity, 262-byte maximum payload, command-8 queueing, two modeled callers, and one raw code ref. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00574e50-0x0057536b BuildAndSendFriendNameListSync

## Status

- Disposition: reconstructable NexusTK project code with corrected friend/name-list sync naming
- Entity kind: Socket queued-send helper
- Direct source owner: [UID:0000DD][Socket](by-class/Socket.md). The former `CashShopRequest::BuildAndSendFriendNameListSync` and `CashShopRequest::BuildAndSendInventoryData` labels are retained only as stale/generated search provenance.
- Former generated name: `CashShopRequest::BuildAndSendInventoryData`
- Semantic role: friend/name-list opcode `0x77` upload helper
- Parent attachment: attached to [UID:0000DD][Socket](by-class/Socket.md). This child is `93/94` with exact packet/container/source closure.
- Confidence: strong for boundary, packet construction, caller evidence, receiver-origin evidence, send-disabled guard, queue command, and Socket dispatcher ownership.
- Evidence basis: existing IDA-backed docs plus read-only IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, `callees`, `disasm`, and raw-byte checks on 2026-05-25/2026-05-26 and 2026-06-07; live IDA MCP receiver-origin/dispatcher/field-use audit on 2026-06-11. Generated names are treated as search/provenance hints, not authority.

## Autogen Status

- Reconstructable: true. This is NexusTK-owned packet serialization and queueing code.
- Autogen parent: attached to [UID:0000DD][Socket](by-class/Socket.md). The current file/class labels from generated docs are useful search context only; the B001 owner audit found that the receiver is [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), the send-disabled guard is a Socket field, and command `8` is consumed by Socket's dispatcher.
- Reconstruction status: exact source-shaped C++ is emitted through Socket. It uses the existing `StringBase` converting constructor, `WideRangeCompare`, MemoryMan copy wrapper, two ordered uniqueness containers, and established Config friend-name fields; no placeholder conversion/copy helper remains.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Exact boundary, callers/raw ref, opcode/header, 20-slot Config source, two-pass equality, first-occurrence emission order, 1,024-byte capacity, 13-byte records, 262-byte maximum, queue path, owner, dependencies, and formal source are closed. |
| Confidence | 94 | Binary behavior and Socket ownership are exact; human container/local spellings remain inferred within established project types. |

## Function Boundary

IDA MCP reports `sub_574E50` at `0x00574e50` with size `0x51b` (1307 bytes; Verified with int_convert.py), so the exact modeled range is `0x00574e50-0x0057536b`.

## Behavior

This helper builds an outbound opcode `0x77` (119; Verified with int_convert.py) packet from up to 20 saved config string slots at `g_pConfig +0x2911e0` (2691552; Verified with int_convert.py) through `g_pConfig +0x291230` (2691632; Verified with int_convert.py).

Observed high-level flow:

1. Initialize a local packet buffer and write opcode `0x77` at offset `0`.
2. Walk 20 config string slots, each stored as a pointer-sized string reference.
3. Skip empty strings and insert nonempty unique names into a tree-backed set.
4. Write the unique-name count at packet offset `1`.
5. Walk the same slots again in original index order, use a second tree only as a seen-set, convert each first-occurrence wide string through the existing `StringBase` converting constructor, and append fixed 13-byte records.
6. Each record starts with marker `0x0c` (12; Verified with int_convert.py), followed by 12 copied bytes of narrow string data.
7. If the send-disabled byte at `this +0x3aa4e` (240206; Verified with int_convert.py) is clear, allocate `packetSize + 1`, copy the packet, append zero, and dispatch request code `8` through `0x00596960`.
8. Clear both tree containers before returning.

The local packet array is exactly 1,024 bytes. The two-byte header is opcode `0x77` plus the first-pass unique count. At most 20 records are emitted, so the largest payload is exactly `2 + 20 * 13 = 262` bytes. The write to `packet[2]` is scratch: the first record overwrites it, while an empty list queues only the two-byte header. Count and emitted-cardinality equality follow from both passes traversing the same 20 slots with the same comparator.

The current documentation names the config range as friend-name slots and this method as `BuildAndSendFriendNameListSync`. The old inventory wording was a generated-name artifact; caller and config-slot evidence point to a friend/name-list update, not item inventory data.

## Ordered-String Container Evidence

B012's 2026-06-19 [UID:000245][0x00578f70-0x005797ae.StlTreeStringMapSupport](by-memory/0x00578f70-0x005797ae.StlTreeStringMapSupport.md) source-routing pass resolves the local tree use as generated MSVC/Dinkumware wide-string ordered-tree/set support, not a project-owned `StringMap` module and not a CashShopRequest source owner. This function reaches the shared helper island through direct calls to `0x00579150` at `0x00575017` and `0x005751c7`; that helper then dispatches to comparator `0x00579540`, lower-bound/unique-insert `0x00579300`, and insert/rebalance `0x00578ff0`. It also initializes tree sentinels through `0x005796d0` at `0x00574eb3` and `0x00575077`, and clears the local sets through `0x00579710` at `0x005752c7` and `0x00575316`.

The best source-facing model is therefore a temporary ordered unique string container, likely `std::set<SimpleUString>` or a project typedef over the same wide-string wrapper shape. The exact typedef spelling remains below final-source quality, but the source should express the deduplication container here and let the selected MSVC-compatible STL/runtime regenerate the helper bodies. Do not introduce a handwritten `StlTreeStringMapSupport.cpp`, do not assign the helper island to EventDispatcher without a concrete xref, and do not treat the historical `CashShopRequest::BuildAndSendInventoryData` generated name as ownership evidence.

## Generated-State Caveat

The generated source state recorded by earlier documentation is useful only as context:

- `simroot_v2/class_CashShopRequest.cpp` emits `CashShopRequest::BuildAndSendFriendNameListSync`, `kFriendNameSlotStart`, `friendNameSlots`, and `kFriendNameRecordType`.
- `simroot_v2/class_FriendListDialog.cpp` gates the call through `g_friendNameListSyncEnabled != 0`.
- `simroot_v2/class_MapPane.cpp` declares and updates `g_friendNameListSyncEnabled`; the canonical definition is owned by `class_FriendListDialog.cpp`.

Do not treat that generated ownership as proof of original source ownership. The IDA caller/xref evidence below is the basis for the current name and behavior.

IDA MCP recheck: `lookup_funcs` reports `0x00574e50` as a real `0x51b`-byte function. On 2026-06-07, `callers` lifted the two defined callers at `0x00508b14` inside `MapPane::HandlePacket` and `0x0053f8ed` inside `FriendListDialog::OnOkButton`; `xrefs_to 0x00574e50` additionally reported a raw code ref at `0x005143ad` in an IDA-undefined island. `xrefs_to 0x0066dee0` reported matching flag refs at `0x00508aff`, `0x0051439c`, and `0x0053f8dc`.

2026-06-11 IDA MCP refresh: `py_eval` / `decompile` reconfirmed `sub_574E50` at `0x00574e50-0x0057536b`, direct refs from `0x00508b14` / `sub_507C90`, raw island ref `0x005143ad`, and `0x0053f8ed` / `sub_53F830`, the `dword_67A7C8 +0x2911e0` slot walk, count write at packet offset `1`, second 20-slot pass, ANSI conversion path, 13-byte record append, send-disabled check at `this +0x3aa4e`, packet-buffer helper calls, queue primitive `0x00596960`, two tree cleanup calls through `0x00579710`, and five `0xcc` bytes before `sub_575370`.

## Callers

IDA MCP callers on 2026-05-25:

- `0x00507c90` / `sub_507C90`, documented as `MapPane::HandlePacket` in [UID:0000L3][MapPane](by-file/MapPane.md). The exact caller is inbound case `0x6a` at `0x00508b14`; it calls this helper only when the case payload byte is nonzero.
- `0x0053f830` / `sub_53F830`, documented as `FriendListDialog::OnOkButton` in [UID:0000JN][FriendListDialog](by-file/FriendListDialog.md) and [UID:00005J][FriendListDialog](by-class/FriendListDialog.md).

[UID:00005J][FriendListDialog](by-class/FriendListDialog.md) writes the same 20 config string slots at `g_pConfig +0x2911e0` (2691552; Verified with int_convert.py) before this helper is called, then checks [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md) before sending. The other live caller is [UID:0000UP][FriendNameListSyncOpcodes](by-item/FriendNameListSyncOpcodes.md) inbound opcode `0x6a`, which writes that flag and immediately calls this helper when the server-provided byte is nonzero.

There is also a raw IDA code reference that is not lifted by the `callers` query because the containing island is not currently a defined function. Disassembly around `0x00514390` shows `setnz byte_66DEE0` at `0x0051439c`, a conditional branch, `mov ecx, dword_67A7EC` at `0x005143a7`, and `call sub_574E50` at `0x005143ad`, matching the same flag-gated friend-name-list sync pattern.

## Ownership Decision

Attach this function to [UID:0000DD][Socket](by-class/Socket.md). It is a queued-send helper invoked through the Socket-lifetime [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), checks the Socket send-disabled byte at `this + 0x3aa4e`, and queues command `8`, which `Socket::HandleSocketCommand` consumes.

Feature ownership remains social/account-name-list behavior: `FriendListDialog` owns editing and saving the names, while this helper serializes and queues the opcode `0x77` upload. Do not move this helper into `FriendListDialog.cpp` solely from caller evidence; it also has `MapPane::HandlePacket` and raw flag-gated call paths. [UID:0001HT][0x00574b90-0x00575377.CashShopRequestSendQueue](by-memory/0x00574b90-0x00575377.CashShopRequestSendQueue.md) is now only a non-emitting split inventory, not a parent.

## Cross-References

- [UID:0001HT][0x00574b90-0x00575377.CashShopRequestSendQueue](by-memory/0x00574b90-0x00575377.CashShopRequestSendQueue.md)
- [UID:00001H][CashShopRequest](by-class/CashShopRequest.md)
- [UID:0000I0][CashShopRequest](by-file/CashShopRequest.md)
- [UID:0000DD][Socket](by-class/Socket.md)
- [UID:0000NS][Socket](by-file/Socket.md)
- [UID:0000JN][FriendListDialog](by-file/FriendListDialog.md)
- [UID:00005J][FriendListDialog](by-class/FriendListDialog.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0000UP][FriendNameListSyncOpcodes](by-item/FriendNameListSyncOpcodes.md)
- [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md)
- [UID:0001OM][0x0066dee0-0x0066dee1.FriendNameListSyncEnabled](by-memory/0x0066dee0-0x0066dee1.FriendNameListSyncEnabled.md)
- [UID:0001QH][client_network](by-meta/client_network.md)

## 2026-08-17 B003 UID0000MV Accepted Callback

The formal source now preserves the complete binary shape: a 1,024-byte local packet, opcode/count header, separate counted and emitted ordered sets, two passes over `Config::m_friendNames[20]`, first-occurrence emission order, existing `StringBase` wide-to-narrow conversion, 13-byte records, exact count equality, and a 262-byte maximum payload. The initial `packet[2]` scratch byte is overwritten by the first record or excluded from an empty two-byte packet. No placeholder Config accessor, conversion helper, or copy helper remains.

## Changes

- 2026-08-17 B003 UID0000MV accepted callback: raised `90/91` to `93/94` and replaced the sorted-single-set/placeholder-helper draft with binary-exact two-pass first-occurrence source and dependency closure.
- 2026-06-30 B007 implementation callback: raised score from `87/90` to `90/91`; formal C++ now emits the accepted opcode `0x77` friend-name sync helper with an ordered unique-name container and fixed 13-byte per-name records. Current MCP kept the boundary and three refs; `GetFriendNameListEntry`, `WideStringToNarrowFixed`, and `CopyPacketQueueMemory` remain source-facing helper placeholders pending recovered header names.
- 2026-06-11 Agent-B001 B001-051: raised completion/confidence from `86/89` to `87/90`, rerouted `AUTOGEN_PARENT_UID` from aggregate [UID:0001HT][0x00574b90-0x00575377.CashShopRequestSendQueue](by-memory/0x00574b90-0x00575377.CashShopRequestSendQueue.md) to [UID:0000DD][Socket](by-class/Socket.md), and added B001 ownership rationale. The live IDA audit showed all callers pass `g_packetSender` directly or through simple aliases, Socket consumes command `8`, and the send-disabled guard is part of the Socket field family.
- 2026-06-11 Agent-A005 Batch 189: raised completion/confidence from `84/88` to `86/89`, attached direct parent [UID:0001HT][0x00574b90-0x00575377.CashShopRequestSendQueue](by-memory/0x00574b90-0x00575377.CashShopRequestSendQueue.md), added live IDA MCP refresh evidence, verified key decimal offsets with `int_convert.py`, and kept final C++ blank because tree/local helper names and the higher request-sender owner are not final-source quality.
- 2026-06-07 Agent-A002 Batch 008 pass: raised completion/confidence from `82/86` to `84/88`, added the corrected assignment-gate rationale, documented the raw `0x005143ad` code ref and paired `byte_66DEE0` flag ref, refreshed live IDA boundary/caller/callee/padding evidence, and kept `AUTOGEN_PARENT_UID` and C++ blank.
- 2026-06-06 A005 filename/name-alignment pass:
  - What existed before: filename and many cross-references still used stale `BuildAndSendInventoryData` even though the page title and evidence already identified friend/name-list sync behavior; score was `70/85`.
  - Changed to: filename `0x00574e50-0x0057536b.BuildAndSendFriendNameListSync.md`, score `82/86`, autogen/source-gate rationale, and generated-state caveat; final C++ and parent remain blank.
  - Summary/evidence: existing IDA-backed documentation records `sub_574E50` at `0x00574e50-0x0057536b`, callers from MapPane inbound opcode `0x6a` and `FriendListDialog::OnOkButton`, `byte_66DEE0` flag linkage, 20 config friend/name slots, outbound opcode `0x77`, 13-byte records, request-code `8` queueing, and the corrected former-alias interpretation.
