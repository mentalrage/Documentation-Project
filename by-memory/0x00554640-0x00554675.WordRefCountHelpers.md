*** UID:00023P | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA refresh confirms the single modeled initializer, raw increment/release helpers, zero refs to all helper starts, all-0xcc surrounding padding, the release edge to sub_5C7526, and no direct owner; left unassigned under the corrected gate. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00554640-0x00554675 - Word Ref Count Helpers

## Status

- Disposition: reconstructable helper code; no existing direct owner or source-use emitter is proven, but the best if-forced source-family hypothesis is a narrow new utility owner rather than an adjacent feature owner.
- Entity kind: tiny intrusive word reference-count / lifetime helper island.
- Nearby owners: [UID:0001FT][0x00553c10-0x00554635.ConnectionClosedReconnectDialogs](by-memory/0x00553c10-0x00554635.ConnectionClosedReconnectDialogs.md) and [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md)
- Last live IDA check: 2026-06-12 against MCP session `b001_nexustk` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`.
- Confidence: very strong for bytes, helper boundaries, raw-helper status, reference absence, pattern uniqueness, and behavior; medium for final source owner because IDA reports no callers, pointer-table entries, source metadata, or type names for the helper island.
- 2026-06-12 A003 Batch316 status: fresh MCP and local PE evidence reconfirmed the helper island exactly, found no owner references, and produced unique byte signatures for all three helper bodies. No by-file parent was created or assigned because the proposed narrow utility owner is still an inference rather than a proven direct source parent.

## Covered Range

| Range | IDA status | Behavior |
| --- | --- | --- |
| `0x00554640-0x0055464b` | modeled function `sub_554640` | Initializes `word [this]` to `1` and returns `this`. |
| `0x0055464b-0x00554650` | padding | Five `0xcc` bytes before the raw increment helper. |
| `0x00554650-0x00554654` | raw function-shaped bytes | Increments `word [this]` and returns. |
| `0x00554654-0x00554660` | padding | Twelve `0xcc` bytes before the raw release helper. |
| `0x00554660-0x00554675` | raw function-shaped bytes | Decrements `word [this]`; if the signed count is not positive, frees `this` through `sub_5C7526`. |

## Behavior Notes

- The initializer writes `1` to the first word of the object and returns `ecx`.
- The increment helper is a four-byte `inc word ptr [ecx]; retn` raw helper.
- The release helper decrements the same word, compares it to zero as a signed word, returns while the count is still positive, and otherwise pushes `ecx` into `sub_5C7526`.
- IDA decompiles `sub_5C7526` as a CRT free wrapper that forwards the block pointer to `j_j___free_base`; the extra pushed constant is stack-cleaned by the caller and does not change the decompiled one-argument free wrapper behavior.
- The `push 2; push ecx; call sub_5C7526; add esp, 8` release form resembles a deallocation helper called with an ignored extra flag/size argument, not a normal C++ method call. Treat the source shape as a small lifetime helper until an owning type is found.

## IDA Evidence

- 2026-06-01 IDA MCP byte/disassembly review confirms exact helper and padding ranges:
  - `0x00554640-0x0055464b`: `b8 01 00 00 00 66 89 01 8b c1 c3`.
  - `0x0055464b-0x00554650`: five `0xcc` bytes.
  - `0x00554650-0x00554654`: `66 ff 01 c3`.
  - `0x00554654-0x00554660`: twelve `0xcc` bytes.
  - `0x00554660-0x00554675`: `66 ff 09 66 83 39 00 7f 0b 6a 02 51 e8 b5 2e 07 00 83 c4 08 c3`.
- IDA function inspection models only `0x00554640-0x0055464b` as `sub_554640`; the increment and release helpers are not function objects, but decode cleanly as function-shaped raw bytes with `retn` endpoints.
- IDA MCP `xrefs_to` and `callers` report no refs to `0x00554640`, `0x00554650`, or `0x00554660`.
- A raw pointer-byte scan found no dword table entries for `0x00554640`, `0x00554650`, or `0x00554660`.
- Boundary evidence: previous code ends with `retn 4` at `0x00554632-0x00554634`, followed by `0x00554635-0x00554640` padding; the next documented island begins at `0x00554680`, after `0x00554675-0x00554680` padding.
- 2026-06-06 live IDA MCP refresh:
  - `lookup_funcs` reports only `sub_554640` at `0x00554640-0x0055464b`; `0x00554650`, `0x00554660`, and `0x00554675` are not modeled as functions, while the next modeled function is `sub_554680` at `0x00554680-0x005546e1`.
  - `decompile 0x00554640` confirms `_WORD *__thiscall sub_554640(_WORD *this) { *this = 1; return this; }`.
  - `disasm 0x00554640` confirms the four-instruction initializer: `mov eax, 1`, `mov [ecx], ax`, `mov eax, ecx`, `retn`.
  - IDA Python byte/disassembly scan of `0x00554635-0x00554680` confirms eleven leading `0xcc` bytes, the modeled initializer, five `0xcc` bytes, raw increment helper, twelve `0xcc` bytes, raw release helper, and eleven trailing `0xcc` bytes.
  - `CodeRefsTo`, `DataRefsTo`, and a loaded-segment dword pointer scan all return empty for `0x00554640`, `0x00554650`, and `0x00554660`.
  - The previous modeled function is `sub_5545F0` ending at `0x00554635`; the next modeled function is `sub_554680` beginning at `0x00554680`.
  - The only outgoing code reference from this island is the release helper's `0x0055466c -> 0x005c7526` call; `sub_5C7526` decompiles as `void __cdecl sub_5C7526(void *Block) { j_j___free_base(Block); }`.
- 2026-06-07 live IDA MCP refresh against the same `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` reconfirmed that only `0x00554640-0x0055464b` is an IDA-modeled function in this island; `0x00554650` and `0x00554660` remain raw function-shaped byte helpers.
- The 2026-06-07 xref refresh again returned zero code/data refs to all three helper starts, and the padding checks still report all `0xcc` bytes for `0x00554635-0x00554640`, `0x0055464b-0x00554650`, `0x00554654-0x00554660`, and `0x00554675-0x00554680`.
- The same pass reconfirmed the release helper's only external code edge as `0x0055466c -> 0x005c7526` (`sub_5C7526`).
- 2026-06-11 live IDA MCP `py_eval` reconfirmed `NexusTK.exe` as the IDB root, the previous modeled function ending at `0x00554635`, the next modeled function beginning at `0x00554680`, the single modeled helper at `0x00554640-0x0055464b`, and no modeled functions at `0x00554650` or `0x00554660`.
- The same 2026-06-11 byte pass reconfirmed exact bytes for all seven spans: eleven leading `0xcc` bytes, initializer bytes `b8 01 00 00 00 66 89 01 8b c1 c3`, five `0xcc` bytes, increment bytes `66 ff 01 c3`, twelve `0xcc` bytes, release bytes `66 ff 09 66 83 39 00 7f 0b 6a 02 51 e8 b5 2e 07 00 83 c4 08 c3`, and eleven trailing `0xcc` bytes.
- 2026-06-11 `xrefs_to` still returned no code or data refs to `0x00554640`, `0x00554650`, or `0x00554660`, so the direct owner remains unproven after another serious reference pass.
- 2026-06-11 B001-050 live IDA MCP refresh against `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` repeated the harder negative scans:
  - `CodeRefsTo`, `DataRefsTo`, loaded-segment dword pointer scans, and direct `call`/`jmp` rel32 scans all return zero hits for `0x00554640`, `0x00554650`, and `0x00554660`.
  - Exact byte-pattern scans across all loaded segments found the initializer sequence, raw increment helper, raw release helper, and `push 2; push ecx; call sub_5C7526; add esp, 8; retn` tail exactly once each, all inside this range.
  - `sub_5C7526` has 617 direct code refs in the current IDB, but only three `push 2; push <arg>; call sub_5C7526` sites. The other two are `MiniMapVersionManager` destructor bodies at `0x00456480` and `0x00457480`, where IDA decompiles the call as freeing list-node payload strings at `i[3]`; that makes the `push 2` form an allocator/deleting-destructor idiom, not an ownership signal for this helper island.
  - IDA name/string scans found no `refcount` or `referencecount` symbol/string. `StringBase`/`mystr`, `LObject`, `Region`, `Motion`, `ReconnectDialog`, and `CopyWindow` RTTI/name families exist elsewhere, but none reference these helper starts or this byte pattern.
  - `StringBase` release helpers at `0x005832f0` and `0x005833a0` use `InterlockedDecrement(data - 0x0c)`, 32-bit refcounts, string sentinels, and fixed string-buffer pools. This contradicts direct `StringBase` ownership for the target's 16-bit `word [ecx]` object-head counter that frees `this` directly.
- 2026-06-12 A003 Batch316 live MCP refresh:
  - `server_health` reports session `b001_nexustk`, module `NexusTK.exe`, input path `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, and strings cache ready.
  - `lookup_funcs` reports `sub_554640` at `0x00554640` size `0x0b`, no functions at `0x00554635`, `0x0055464b`, `0x00554650`, `0x00554654`, `0x00554660`, or `0x00554675`, next function `sub_554680` at `0x00554680` size `0x61`, and free wrapper `sub_5C7526` at `0x005c7526` size `0x0e`.
  - `xrefs_to` reports zero xrefs to `0x00554640`, `0x00554650`, and `0x00554660`.
  - `decompile 0x00554640` still returns `_WORD *__thiscall sub_554640(_WORD *this) { *this = 1; return this; }`; `disasm 0x00554640` still has four instructions: `mov eax, 1`, `mov [ecx], ax`, `mov eax, ecx`, `retn`.
  - `make_signature` produced unique signatures for each helper start: `B8 01 00 00 00 66 89 01` at `0x00554640`, `66 FF 01` at `0x00554650`, and `66 FF 09 66 83 39 00` at `0x00554660`.
  - `entity_query` name/string searches for `refcount`, `ref count`, `word ref`, and `intrusive` returned no matches, so the source-facing type/module name remains inferred.
- 2026-06-12 A003 read-only PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` reconfirmed the exact bytes and padding for all seven spans in this page. It found no absolute-VA dword hits, no RVA dword hits, and no `E8/E9`, `0F 8x`, or short conditional branch targets for `0x00554640`, `0x00554650`, or `0x00554660`. The full initializer, increment, and release byte patterns each occur exactly once, at their documented starts; the shortest MCP unique signatures also occur exactly once. The only `push 2; ...; call sub_5C7526` sites found by this scan are `0x004564b6`, `0x004574b6`, and this island's `0x00554669`.

## Raw Disassembly

```asm
00554640  mov     eax, 1
00554645  mov     [ecx], ax
00554648  mov     eax, ecx
0055464a  retn
0055464b  cc cc cc cc cc
00554650  inc     word ptr [ecx]
00554653  retn
00554654  cc cc cc cc cc cc cc cc cc cc cc cc
00554660  dec     word ptr [ecx]
00554663  cmp     word ptr [ecx], 0
00554667  jg      short 0x00554674
00554669  push    2
0055466b  push    ecx
0055466c  call    sub_5C7526
00554671  add     esp, 8
00554674  retn
```

## Rebuild Handling

Keep this as source-authored or source-required helper logic until stronger evidence proves it is compiler/runtime output. It is executable lifetime-management code, not padding, but no current ref path proves whether it belongs to a project-local intrusive word-count type, stale dead helper code, or a compiler-emitted helper around an owning declaration.

Do not attach it to `ReconnectDialog`, `CopyWindow`, `Region`, `LObject`, `StringBase`, or `StringUtil`: current IDA evidence shows no incoming references or source metadata for those owners, and the string/base candidates have incompatible layout and refcount mechanics. Also keep `EMITTER_UIDS` and final C++ blank because no caller, vtable, table pointer, source-use context, or source-module evidence proves the intended output route, API surface, or source spelling.

## Assignment Gate

`CANONICAL_OWNER` remains `NONE` and `EMITTER_UIDS` remains blank. The corrected `85/85` documentation gate is satisfied by this item, but no existing parent has enough direct evidence for canonical ownership or source emission:

- `ReconnectDialog`/`ConnectionClosedDialog` and `CopyWindow` are rejected because the prior aggregate ends at `0x00554635`, the helper island is separated by `0xcc` alignment, and no xref links their methods, vtables, globals, or RTTI to any helper start.
- `Region`/`Motion` are rejected because the following aggregate starts at `0x00554680` after another `0xcc` alignment run and its constructors/methods use `LObject`/rectangle state rather than a word counter.
- `StringBase`/`StringUtil` are rejected as direct parents because their documented ref-counted string family uses a 12-byte header, 32-bit interlocked counters, sentinel pointers, and pool/global ownership; the target is a standalone 16-bit object-head counter with direct heap free.
- `LObject` is rejected because its actual source shell is a vtable/runtime-class object; the target has no vtable, no runtime-class data, and no links to `LObject` RTTI or vtable stores.

The best actionable routing inference remains a narrow utility owner, tentatively `WordRefCountHelpers` or `IntrusiveWordRefCount`, under `NexusTK/util/`. Treat it as a standalone/narrow source helper family unless a future PDB/linker map, source artifact, or hidden indirect-call table proves a different direct owner. Batch316 and the 2026-06-13 B002 second-pass did not create or assign a new `by-file` parent because a file root based only on this no-xref helper island would not be a proven true direct parent at `85/85`; `CANONICAL_OWNER` stays `NONE` and `EMITTER_UIDS` stays blank.

## Ownership Notes

- The previous aggregate [UID:0001FT][0x00553c10-0x00554635.ConnectionClosedReconnectDialogs](by-memory/0x00553c10-0x00554635.ConnectionClosedReconnectDialogs.md) ends before the `0x00554635-0x00554640` alignment padding; no xref links the helper island back into the reconnect dialog code.
- The following aggregate [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md) starts after the `0x00554675-0x00554680` alignment padding and has its own Region/Motion vtable and rectangle-helper evidence.
- Because all three helper starts have no incoming code refs, data refs, direct rel32 calls/jumps, or pointer-table hits, the safest current representation is an unassigned reconstructable helper island with an explicit recommendation to create/use a narrow utility owner rather than force it into adjacent feature code.
- B001-050 ranks the likely ownership outcomes as:
  1. New narrow `util/WordRefCountHelpers` / `IntrusiveWordRefCount` owner: best source-placement inference because the three unique helper bodies form a complete init/addref/release family and no existing owner has positive xrefs.
  2. Existing `StringBase`/`StringUtil`: plausible only by abstract "refcount" theme, rejected by 16-bit counter width, direct object free, lack of sentinels/pools, and zero metadata/xrefs.
  3. Existing adjacent `ReconnectDialog`/`CopyWindow`/`Region`/`Motion`: physical-neighborhood evidence only, rejected by padding boundaries and unrelated vtable/global/field patterns.
  4. Existing `LObject`: rejected because the true `LObject` shell is vtable/runtime-class code, not a word-counted heap block.

## Score Rationale

- Completion is `88` because the page now documents exact helper and padding ranges, modeled/raw function status, raw bytes and disassembly, decompiled initializer behavior, free-wrapper callee behavior, repeated no-xref/no-pointer evidence, PE-level no-reference evidence, unique signatures, name/string negative searches, allocator-idiom comparison sites, rejected candidate parents, and the strict parent-gate decision.
- Confidence is `92` because boundary, behavior, uniqueness, and unreferenced status are backed by repeated IDA/MCP checks plus an independent read-only PE scan. Confidence remains below final-source quality because live reachability, source-file owner, source-facing type name, and intended API spelling are still unresolved.

## 2026-06-13 B002 Second-Pass Owner/Emitter Audit

B002 independently rechecked the current owner/emitter state and recommended no metadata or score change: keep `COMPLETION:88`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++. The second pass reconfirmed the coherent unique three-helper island, modeled initializer at `0x00554640-0x0055464b`, raw increment helper at `0x00554650-0x00554654`, raw release/free helper at `0x00554660-0x00554675`, exact surrounding/internal `0xcc` padding, `_WORD` initializer decompilation, zero xrefs/xref signatures to all three starts, zero absolute-VA/RVA/relative-branch references, unique exact and wildcarded full-range signatures, unique per-helper byte patterns, no local names around `0x00554600-0x00554700`, no `refcount`/`intrusive`/PDB metadata strings, and MiniMapVersionManager-only negative controls for the `push 2` free-wrapper idiom.

Existing ReconnectDialog, CopyWindow, Region, Motion, StringBase, StringUtil, LinkedList, PoolAllocator, LObject, MiniMapVersionManager, and CRT owners remain rejected. A narrow `util/WordRefCountHelpers` or `IntrusiveWordRefCount` source-family remains the best if-forced hypothesis, but it does not clear owner/emitter assignment without caller, table, type, source, or object-file evidence.

## 2026-06-18 B002 Current Source-Routing Reanalysis

B002 rechecked the current read-only executable bytes and source-route context after the earlier score-gate work. Metadata remains unchanged: `COMPLETION:88`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++.

Fresh PE input was `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, size `2679296`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`, image base `0x00400000`, with `.text` at VA `0x00401000`, virtual size `0x20b4ac`, raw offset `0x00000400`, and raw size `0x20b600`. The relevant VA-to-file offsets are `0x00554635 -> 0x00153a35`, `0x00554640 -> 0x00153a40`, `0x0055464b -> 0x00153a4b`, `0x00554650 -> 0x00153a50`, `0x00554654 -> 0x00153a54`, `0x00554660 -> 0x00153a60`, `0x00554675 -> 0x00153a75`, `0x00554680 -> 0x00153a80`, and `0x005c7526 -> 0x001c6926`.

The fresh byte window `0x00554635-0x00554680` is:

```text
cc cc cc cc cc cc cc cc cc cc cc
b8 01 00 00 00 66 89 01 8b c1 c3
cc cc cc cc cc
66 ff 01 c3
cc cc cc cc cc cc cc cc cc cc cc cc
66 ff 09 66 83 39 00 7f 0b 6a 02 51 e8 b5 2e 07 00 83 c4 08 c3
cc cc cc cc cc cc cc cc cc cc cc
```

The helper behavior still resolves as a three-body intrusive 16-bit counter family: `0x00554640-0x0055464b` writes `1` to `word [ecx]` and returns `ecx`; `0x00554650-0x00554654` increments `word [ecx]`; `0x00554660-0x00554675` decrements `word [ecx]`, uses signed-positive `jg` to return only when the count remains greater than zero, and otherwise executes `push 2; push ecx; call sub_5C7526; add esp, 8; retn`. The called `sub_5C7526` remains the current free wrapper decompiled as `void __cdecl sub_5C7526(void *Block) { j_j___free_base(Block); }`; the extra `2` is caller-cleaned deallocation/deleting idiom evidence, not a source-owner signal.

B002's fresh uniqueness checks found the full initializer sequence, initializer signature, full increment body, increment signature, full release body, release signature, release tail, and full island including internal padding exactly once, all at this island. Fresh no-reference checks found zero absolute-VA dword hits, zero RVA dword hits, and zero executable-section direct branch/call targets for `0x00554640`, `0x00554650`, `0x00554660`, and `0x00554675`; the branch/call scan covered `E8/E9` rel32, `0F 8x` rel32, `70-7F` rel8, and `EB` rel8 encodings. The current prewave exported-function record for `0x00554640` still exposes only `sub_554640`, size `11`, `_WORD *__thiscall(_WORD *this)`, with zero callers, callees, `xrefs_to`, and `xrefs_from`.

The fresh free-wrapper negative control found `617` direct calls to `sub_5C7526`, but only three nearby `push 2` sites: `0x004564b6 -> call 0x004564bb` in MiniMapVersionManager destructor cleanup, `0x004574b6 -> call 0x004574bb` in MiniMapVersionManager scalar deleting destructor cleanup, and `0x00554669 -> call 0x0055466c` in this release helper. The two MiniMapVersionManager sites free node payload/version strings at node offset `+0x0c`; they prove the `push 2` shape is a shared free/deallocation idiom and not evidence for MiniMapVersionManager ownership, CRT ownership, or a deleting-destructor owner.

Fresh source-marker searches found no ASCII or UTF-16LE hits for `refcount`, `ref count`, `referencecount`, `reference count`, `intrusive`, `WordRef`, `WordRefCount`, `554640`, `554650`, or `554660`, and no `.pdb`, `RSDS`, or `NB10` debug path marker. Current generated/source-route searches still show UID `00023P` as no-owner/no-emitter with no output route; repository hits for the helper names/addresses are limited to this target, generated no-owner coverage, prewave/cache records, supervisor/research text, and ignored/coverage references. The proposed source tree has no `WordRefCountHelpers.cpp`, `IntrusiveWordRefCount.cpp`, or comparable refcount utility file under `NexusTK/util/`.

Candidate owners remain rejected after the fresh pass:

- `StringBase` / `StringUtil`: rejected because the documented string-buffer family uses data pointers to 12-byte headers, 32-bit interlocked refcounts, sentinel pointers, and fixed string-buffer pools. This island uses a direct 16-bit counter at object offset `+0x00` and frees `this`.
- `LObject`: rejected because the true shell is vtable/runtime-class object code; this island has no vtable store, runtime-class record, RTTI/vtable pointer, inherited-object field evidence, or route into `LObject`.
- `ReconnectDialog`, `ConnectionClosedDialog`, and `CopyWindow`: rejected because the previous aggregate ends at `0x00554635`, the island is separated by alignment, and no calls, pointers, vtables, singleton globals, local names, or shared byte patterns link the helper starts to those UI/dialog owners.
- `Region`, `Motion`, and `RectBounds`: rejected because the following aggregate begins at `0x00554680` after alignment and carries rectangle/vtable/motion evidence that does not reference this word-count island.
- `MiniMapVersionManager`: rejected as an owner despite the two `push 2` free-wrapper negative controls; those sites belong to map-version list/index teardown at different addresses and do not transfer ownership.
- CRT/free-wrapper/deleting-destructor idioms: rejected as owners because `sub_5C7526` has hundreds of callers and this island lacks a delete flag parameter, vtable reset, base/member destructor calls, `retn 4`, or class-specific RTTI/vtable route.
- `PoolAllocator`, `LinkedList`, STL list support, and adjacent feature owners: rejected because their documented layouts, pools, list/node state, or source families do not match the direct word counter, and adjacency alone crosses explicit alignment boundaries.

The valid heuristic inference is behavioral, not source-routing: the bytes form a coherent init/addref/release family for an intrusive 16-bit reference count stored at object offset `+0x00`. The best if-forced source-placement hypothesis remains a narrow `NexusTK/util/WordRefCountHelpers` or internal `IntrusiveWordRefCount` family, but current evidence does not prove a source file, type declaration, header placement, member-versus-free-function API, original helper names, or live use site. Therefore the target remains reconstructable for behavior but non-emitting for source.

Do not populate C++ from this evidence. The numeric documentation gate is satisfied, but no emitter route is proven; the increment/release bodies are not IDA-modeled functions; names like `WordRefCountHelpers`, `IntrusiveWordRefCount`, `AddRef`, and `Release` are descriptive reverse-engineering names; the object type, signedness contract, overflow policy, destructor obligations, and API surface remain unresolved; and emitting under an invented utility owner would falsely assert source placement and declaration shape.

## 2026-06-22 Archived B-Agent Rule 26 Cluster Incorporation

The older B-agent report cluster for `00023P` was audited claim-by-claim after the newer B002 current source-routing pass. The audited reports are:

| Report | Incorporated result |
| --- | --- |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers.md` | Preserved the original B001-050 ownership research: a coherent unique three-helper 16-bit refcount island, strong rejection of existing owners, and the historical recommendation to create or accept a narrow utility owner only if the supervisor accepted that source model. |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers-B001-00023P.md` | Preserved the stricter follow-up: keep the parent blank; a one-target utility file based only on no-xref helper behavior is below the assignment threshold unless independent source-owner evidence or an explicit project-level modeling policy appears. |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers-current-goal2-pass.md` | Preserved the current-goal2 split/merge/reclassification decision: no split of the init/addref/release helpers, no merge into predecessor/successor ranges, no runtime/compiler-only reclassification, no new by-file owner, and no emitter assignment. |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers-current-no-owner-research.md` | Preserved the generated-source and `simroot` negative: generated/source-route searches produce only coverage/research references and no generated output route for this UID. |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers-current-no-owner-sweep.md` | Preserved the no-action sweep outcome: target metadata, score, ownership, emitter state, split/merge status, IDA repair state, and C++ state all remain unchanged. |
| `Agent-B001/research/executed/older/00023P-WordRefCountHelpers-post-migration-recheck.md` | Preserved the post-migration conclusion that the island may be retained/dead or only reachable through evidence not present in the static binary; this uncertainty does not justify owner/emitter assignment or `RECONSTRUCTABLE:FALSE`. |
| `Agent-B002/research/executed/older/00023P-WordRefCountHelpers-fresh-no-owner-pass.md` | Preserved the raw PE contrast that the following Region constructor at `0x00554680` has `89` rel32 references while this helper island has none; this strengthens the separation from the following owner. |
| `Agent-B002/research/executed/older/00023P-WordRefCountHelpers-goal2-verification.md` | Preserved the verification result: `88/92`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank C++, no split/reclassification/IDA repair, and no coverage-row replacement required by that pass. |
| `Agent-B002/research/executed/older/00023P-WordRefCountHelpers-live-goal2-no-owner-pass.md` | Preserved the live Goal 2 conclusion that this is source-shaped and reconstructable but non-emitting because source-use context, caller, table, type, source, and object-file evidence are still absent. |
| `Agent-B002/research/executed/older/00023P-WordRefCountHelpers-second-pass.md` | Preserved the owner/emitter audit and exact B002 coverage wording that later reports reused: all existing candidate owners and emitters remain rejected; narrow utility source-family stays best-if-forced only. |
| `Agent-B003/research/executed/older/00023P-WordRefCountHelpers-current-goal2-pass.md` | Preserved the stronger branch/callee wording: there are no external branch/call routes to helper starts; the only in-range control transfer is the release helper's internal `jg` and its outgoing free-wrapper call. |
| `Agent-B003/research/executed/older/00023P-WordRefCountHelpers-current-no-owner-research.md` | Preserved the B003 session `b001_0002bd` no-owner restatement and the explicit rejection of source-use/emitter routes without caller, table, type, source, object-file, or other source-use evidence. |
| `Agent-B003/research/executed/older/00023P-WordRefCountHelpers-post-migration.md` | Preserved the current-model emitter analysis: `EMITTER_UIDS:0000OA`/`StringBase`, `0000OB`/`StringUtil`, adjacent UI/dialog routes, Region/Motion routes, LinkedList, PoolAllocator, LObject, MiniMapVersionManager, and CRT/free-wrapper routes are all rejected. |

Historical assumption state:

- Superseded: the earliest B001-050 recommendation said the supervisor could create or accept a narrow `NexusTK/util/WordRefCountHelpers` or `IntrusiveWordRefCount` owner before assigning this row. Later B001/B002/B003 passes supersede that as an automatic action. The current position is narrower: the utility family is the best if-forced source-placement hypothesis, but a new by-file owner should not be created or used unless future caller/table/type/source/PDB/object-file evidence proves it, or unless a deliberate project-level policy accepts no-xref retained helper islands as modeled source roots.
- Superseded: old coverage/autogen wording used blank `AUTOGEN_PARENT_UID` and older unassigned terminology. Under the current metadata model, the same decision is `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++.
- Retained: the helper island is not ignored padding and not pure CRT/compiler glue. The initializer/addref/release sequence is custom source-shaped lifetime behavior, but current evidence cannot prove whether it was live, retained/dead, indirectly reached, or compiled from an unmodeled utility type.

Additional preserved evidence from the archived cluster:

- The current IDA/PE evidence rejects splitting because all three helper bodies are one homogeneous lifetime family and share the same unresolved source-owner problem. Splitting the initializer, increment, and release helpers would not create stronger owner evidence.
- The current IDA/PE evidence rejects merging with [UID:0001FT][0x00553c10-0x00554635.ConnectionClosedReconnectDialogs](by-memory/0x00553c10-0x00554635.ConnectionClosedReconnectDialogs.md) or [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md) because both sides are separated by `0xcc` alignment and each neighboring island has independent stronger source evidence that does not reference this range.
- The following Region constructor at `0x00554680` is a positive control for a live, referenced adjacent island: B002's fresh pass found `89` rel32 references to that constructor while `0x00554640`, `0x00554650`, and `0x00554660` still have zero absolute-VA, RVA, xref-signature, or direct branch/call routes.
- The generated-source/`simroot` negative is part of the assignment decision: repository searches find only this target, generated no-owner coverage, prewave/cache records, supervisor/research text, and ignored/coverage references. They do not find a source-output route or generated C++ site for this UID.
- Candidate emitter UIDs are rejected for the same reason as candidate owners. Assigning `0000OA`, `0000OB`, `0000KR`, `0000MM`, `0000KV`, `0000LF`, adjacent UI/dialog owners, Region/Motion owners, or MiniMap/CRT/free-wrapper routes would claim source placement from theme, adjacency, or shared deallocation idioms rather than evidence.
- The proposed narrow owner, if future evidence proves it, should contain only this init/addref/release family and document internal padding as alignment. It should not absorb the predecessor reconnect/copy cleanup, successor Region/Motion rectangle code, StringBase/string-buffer helpers, LinkedList, PoolAllocator, LObject, MiniMapVersionManager, or CRT/free-wrapper code.
- Open questions are intentionally retained: original source file, original helper/type names, member-vs-free-function API shape, object type and layout beyond the first word, signedness/overflow policy, destructor obligations before freeing, live caller path, and whether the helpers are dead retained code. These are the blockers for C++ emission and owner assignment, not blockers for documenting the binary behavior.

## Cross-References

- [UID:0001FT][0x00553c10-0x00554635.ConnectionClosedReconnectDialogs](by-memory/0x00553c10-0x00554635.ConnectionClosedReconnectDialogs.md)
- [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-06-12 A003 Batch316:
  - What existed before: score `87/91`, with strong prior B001 ownership research and a recommendation for a future narrow utility owner.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:92`; `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, and blank C++ remain unchanged.
  - Summary/evidence: live MCP session `b001_nexustk` reconfirmed the single modeled initializer, raw increment/release starts, zero xrefs to all helper starts, decompiled `_WORD` initializer, and unique signatures for all three helper starts. A read-only PE scan reconfirmed exact bytes/padding, zero absolute-VA/RVA/relative-branch references, unique full byte patterns, and only three `push 2` free-wrapper sites. Name/string searches still found no source-facing `refcount` metadata. Assignment remains blocked because no true direct parent is proven above `85/85`; a new `util/WordRefCountHelpers` parent remains only a placement inference.

- 2026-06-11 A002 Batch 178:
  - What existed before: score `82/88`, with strong byte/range evidence but still below the supervisor `85/85` checkpoint threshold.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:90`; this predates the owner/emitter split, and the old blank parent route now corresponds to `CANONICAL_OWNER:NONE` plus blank `EMITTER_UIDS`.
  - Summary/evidence: live IDA MCP reconfirmed the previous/next modeled function boundaries, exact helper/padding bytes, single modeled initializer, raw increment/release helpers, and zero code/data refs to all three helper starts. The page now clears `85/85`, but strict parent assignment is still blocked because no caller, table entry, class, or source file proves direct ownership.
- 2026-06-11 B001-050 ownership research:
  - What existed before: score `85/90`, with the page clearing the child gate but stopping at owner-unknown/unassigned.
  - Changed to: `COMPLETION:87`, `CONFIDENCE:91`; `RECONSTRUCTABLE:TRUE`, old blank parent routing, and blank C++ remain unchanged. Under the current model this means `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS`.
  - Summary/evidence: live IDA MCP reconfirmed no code/data refs, no loaded-segment dword pointers, no direct rel32 calls/jumps, exact byte uniqueness for all three helper bodies, no `refcount`/`referencecount` metadata, and no StringBase/LObject/Region/Reconnect/CopyWindow reference path. Existing candidate owners are explicitly rejected; the actionable recommendation is a narrow new utility owner under `NexusTK/util/` if the supervisor wants this row assigned.
- 2026-06-06 A007 live IDA refresh:
  - What existed before: score was `72/82`; the page had the helper byte ranges and no-xref note, but did not record current IDB metadata, modeled-function boundaries around the island, raw disassembly in page form, outgoing free-wrapper confirmation, or the ownership rationale against adjacent aggregates.
  - Changed to: score `80/88`; `RECONSTRUCTABLE:TRUE` remains unchanged, and the old parent/C++ fields remained blank.
  - Summary/evidence: live IDA MCP `idb_meta`, `lookup_funcs`, `decompile`, `disasm`, `xrefs_to`, `callers`, `callees`, and an IDA Python byte/reference scan confirmed the exact helper/padding layout, no incoming code/data/pointer refs for all three helper starts, previous/next modeled function boundaries, and the release helper's sole call to `sub_5C7526`.
- 2026-06-01:
  - Raised completion/confidence from `50/65` to `72/82` and marked reconstructable.
  - Added exact helper/padding ranges, raw byte evidence, free-wrapper callee evidence, no-xref/no-pointer-table evidence, and ownership caveats.
  - Left the old parent and C++ fields blank because no caller, pointer-table, or class-owner evidence currently proves where these helpers belong in final source. Under the current model this maps to `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS`.
- 2026-06-07 A008 assignment-gate refresh:
  - What existed before: score was `80/88`; the page already had detailed byte/range evidence, but did not explicitly record the corrected `85/85` no-parent decision.
  - Changed to: score `82/88`; `RECONSTRUCTABLE:TRUE` remains unchanged, and the old parent/C++ fields remained blank.
  - Summary/evidence: live IDA MCP reconfirmed the single modeled initializer, raw increment/release helpers, zero xrefs to all helper starts, all-`0xcc` surrounding padding, and the release helper's sole free-wrapper edge. No direct owner is proven, so assignment was deliberately not made.
