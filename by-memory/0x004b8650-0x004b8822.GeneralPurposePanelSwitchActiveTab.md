*** UID:00015X | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00005Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00005Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void GeneralPurposePanel::SwitchActiveTab(
    int tabIndex,
    const unsigned char *packetData)
{
    if (g_useEpfAssets == 1) {
        if (tabIndex >= 7)
            return;

        if (tabIndex == m_activeChildIndex &&
            tabIndex != 0 &&
            tabIndex != 1)
            return;

        if (g_pTotemFrame != NULL)
            g_pTotemFrame->MarkForDeletion();

        Pane *oldChild = NULL;
        if (m_activeChildIndex != -1)
            oldChild = m_childPanes[m_activeChildIndex];

        if (oldChild != NULL) {
            oldChild->Collapse();
            oldChild->UnregisterEventHandler();
        }

        m_activeChildIndex = tabIndex;

        Pane *newChild = NULL;
        if (tabIndex != -1)
            newChild = m_childPanes[tabIndex];

        RectBounds bounds;
        bounds.SetLTRB(832, 79, 1021, 367);
        newChild->SetBounds(&bounds, 0);
        newChild->AddToLayer(
            &bounds,
            0,
            g_pBackPane,
            g_mainUiLayerSlots.rootPaneLayerContext);
        newChild->SetPaneOrder(0, g_pBackPane);
        newChild->OnActivate(packetData);
        ReleaseDynamicAboveFrame();
        RecreateDynamicAboveFrame();
    } else {
        if (tabIndex >= 4)
            return;

        if (tabIndex == m_activeChildIndex &&
            tabIndex != 0 &&
            tabIndex != 1)
            return;

        if (g_pTotemFrame != NULL)
            g_pTotemFrame->MarkForDeletion();

        Pane *oldChild = NULL;
        if (m_activeChildIndex != -1)
            oldChild = m_childPanes[m_activeChildIndex];

        if (oldChild != NULL) {
            oldChild->Collapse();
            oldChild->UnregisterEventHandler();
        }

        m_activeChildIndex = tabIndex;

        Pane *newChild = NULL;
        if (tabIndex != -1)
            newChild = m_childPanes[tabIndex];

        RectBounds bounds;
        bounds.SetLTRB(434, 12, 626, 300);
        newChild->SetBounds(&bounds, 0);
        newChild->AddToLayer(
            &bounds,
            0,
            g_pBackPane,
            g_mainUiLayerSlots.rootPaneLayerContext);
        newChild->SetPaneOrder(0, g_pBackPane);
        if (packetData != NULL)
            newChild->OnActivate(packetData);
        g_pInterfaceEfxMgr->TriggerInterfaceEffect(m_activeChildIndex);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact GeneralPurposePanel::SwitchActiveTab source body with 0x004b8650-0x004b8823 half-open interval, EPF seven/legacy four limits, same-tab 0/1 exceptions, Pane child array and active index, Collapse/unregister ordering, exact branch rectangles and activation/null behavior, FrameChrome and InterfaceEfx dependencies, 35-call fan-in, padding, stale SpellOneArgInputPane ownership rejection, and source position 40. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b8650-0x004b8822 GeneralPurposePanel SwitchActiveTab

## Scope

- Covered file/module: [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md)
- Class: [UID:00005Q][GeneralPurposePanel](by-class/GeneralPurposePanel.md)
- Stale generated owner: `SpellOneArgInputPane`
- IDA name: `sub_4B8650`
- Status: real project method attached to `GeneralPurposePanel`.
- Rebuild handling: source-authored method with exact managed C++. B005's target-specific no-code proof is preserved as historical evidence, but UID0004KU `Pane::Collapse`, `UpdateRenderRegion`, and common `OnActivate(const unsigned char *)` now resolve all three former blockers.

## Function Boundary

IDA MCP `lookup_funcs` on 2026-05-25 reports a real function at `0x004b8650` with size `0x1d3`, ending at `0x004b8822`.

Live IDA MCP rechecked this on 2026-06-06: `lookup_funcs 0x004b8650` still reports `sub_4B8650` size `0x1d3`, and `lookup_funcs 0x004b8822` still resolves inside the same function while the adjacent `GeneralPurposePanel2` constructor starts at `0x004b8830`. A002 rechecked the same boundary on 2026-06-16 against session `c001_midiplayer_rdata_20260615`; the size, `0x004b8822` final-byte inclusion, and `0x004b8823-0x004b8830` padding remain unchanged.

2026-07-08 B005 current IDA MCP session `b007_uid0002oc_20260708` reconfirms `sub_4B8650` size `0x1d3`: `0x004b8820` begins the `c2 08 00` return, `0x004b8822` is still inside the function, `0x004b8823` is not a function, and the bytes after the return are `0xcc` padding through the `0x004b8830` prologue for `sub_4B8830` / `GeneralPurposePanel2` constructor. Treat the source interval as `0x004b8650-0x004b8823` half-open, while the filename's `0x004b8822` remains the final included byte.

## Behavior

This is the main tab/page switch helper for the seven-child [UID:00005Q][GeneralPurposePanel](by-class/GeneralPurposePanel.md).

Observed flow:

1. Selects the valid tab count from [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md): seven tabs in EPF/new-layout mode, four tabs in legacy mode.
2. Rejects out-of-range tab requests and most redundant active-tab requests. Same-index requests for tabs `2+` return early, while same-index requests for tabs `0` and `1` continue through the refresh/switch path.
3. If [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md) is active, calls [UID:0001EA][0x00544460-0x00545086.PaneCore](by-memory/0x00544460-0x00545086.PaneCore.md) `Pane::MarkForDeletion` at `0x00544690` before switching the panel.
4. Hides/collapses the old active child pane through helper `0x00544ee0`, then unregisters that old child through the inherited `Pane::UnregisterEventHandler` slot `+0x40` when one is active.
5. Stores the new active child index.
6. Uses [UID:00015Q][0x004b78d0-0x004b7ae8.RectBoundsMethods](by-memory/0x004b78d0-0x004b7ae8.RectBoundsMethods.md) `RectBounds::SetLTRB` to build the target rectangle, then calls the new child inherited Pane sequence: `Pane::SetBounds` (`+0x2c`), `Pane::AddToLayer` (`+0x30`), `Pane::SetPaneOrder` (`+0x3c`), and `Pane::OnActivate(const unsigned char *packetData)` (`+0x48`).
7. In the new-layout branch, runs [UID:0000YI][0x004615b0-0x00461658.AboveFrameRefreshHelpers](by-memory/0x004615b0-0x00461658.AboveFrameRefreshHelpers.md): `ReleaseDynamicAboveFrame()` at `0x004615b0` releases the dynamic frame slot and `RecreateDynamicAboveFrame()` at `0x004615d0` rebuilds the `0x0067a840` `AboveFrame` slot from `g_pGeneralPurposePanel->m_activeChildIndex + 5`.
8. In the legacy branch, passes the newly stored active child index directly as `effectId` to [UID:00018H][0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect](by-memory/0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect.md) through [UID:0000R8][g_pInterfaceEfxMgr](by-global/g_pInterfaceEfxMgr.md).

The two layout rectangles observed in IDA are:

| Mode | Rectangle |
| --- | --- |
| EPF/new layout | `(832, 79, 1021, 367)` |
| Legacy layout | `(434, 12, 626, 300)` |

## Caller Evidence

B005 current MCP `xref_query to 0x004b8650` returns 35 code refs, proving this is a broad side-panel shell method. Representative callers include:

- `MapPane::HandlePacket` at `0x00507c90`, with switch calls at `0x00508040`, `0x00508679`, and `0x00508cb2`.
- `IntegrateMacroDialog` construction at `0x00541b30`, direct call address `0x00541b9c`.
- [UID:0003UT][0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket](by-memory/0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket.md) callsites at `0x005abde3`, `0x005abe3d`, and `0x005abe55`.
- Other callers at `0x005144e2`, `0x0053edfc`, `0x005a5f9d`, `0x005a6067`, and small `0x005b....` tab-opening wrappers such as `0x005b13df`, `0x005b304f`, `0x005b329f`, and `0x005b622f` that select inventory, spell, collection, self-look, or user-look pages.

This broad caller set supports a shared side-panel shell method, not a spell-input-pane method.

## Callee And State Evidence

Live IDA MCP `decompile`/`callees` on 2026-06-06 shows:

- EPF/new-layout mode allows tab indexes `< 7`; legacy mode allows indexes `< 4`, using [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md).
- The old active child is read from `this[8]`, then hidden/collapsed through helper `0x00544ee0` and vtable offset `0x40` when present.
- The new active index is stored back to `this[8]`, accepted source-facing `m_activeChildIndex`; child pointers are read from `this[index + 1]`.
- New-layout bounds use `(832, 79, 1021, 367)` and always call `OnActivate(packetData)`, then call frame-refresh helpers at `0x004615b0` and `0x004615d0`.
- Legacy bounds use `(434, 12, 626, 300)`, conditionally call `OnActivate(packetData)` when the pointer is non-null, then call [UID:00018H][0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect](by-memory/0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect.md) through [UID:0000R8][g_pInterfaceEfxMgr](by-global/g_pInterfaceEfxMgr.md) with the active child index.
- Both paths clear [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md) first through `0x00544690` when the totem frame singleton is active.

### Legacy InterfaceEfxMgr Lifetime Contract

- At `0x004b8805` the legacy branch loads [UID:0000R8][g_pInterfaceEfxMgr](by-global/g_pInterfaceEfxMgr.md) from physical storage `0x0069b360`; at `0x004b880b` it directly calls [UID:00018H][0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect](by-memory/0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect.md).
- There is intentionally no local null comparison between that load and call. The exact human source therefore remains `g_pInterfaceEfxMgr->TriggerInterfaceEffect(m_activeChildIndex);` rather than adding a defensive guard absent from the binary.
- The direct-call precondition is established by lifecycle ordering: old-layout startup allocates and constructs the `0x104`-byte manager in [UID:00019K][0x004f7d10-0x004f8b2a.InitializeMainUiGraph](by-memory/0x004f7d10-0x004f8b2a.InitializeMainUiGraph.md) before this tab-switch path is reachable, and [UID:0002QH][0x005047f0-0x00504b6b.MapPaneExitToMenuTeardown](by-memory/0x005047f0-0x00504b6b.MapPaneExitToMenuTeardown.md) deletes it during exit teardown after active UI use ends.
- This proof is specific to the legacy branch. It does not imply that all singleton consumers may omit guards or that the physical storage page emits a second definition.

2026-06-16 A002 live IDA MCP evidence:

- `decompile 0x004b8650` still shows the EPF/new-layout branch limited to tab indexes `< 7`, the legacy branch limited to indexes `< 4`, old-child hide/collapse through `0x00544ee0` and vtable slot `+0x40`, new child layout/show dispatch through vtable slots `+0x2c/+0x30/+0x3c/+0x48`, and active-index storage at `this[8]`.
- `callees 0x004b8650` returns only shared pane/frame/effect dependencies: `0x00544690`, `0x00544ee0`, `0x004b78d0`, `0x004615b0`, `0x004615d0`, security-cookie check, and `0x004e9ee0`.
- `xrefs_to 0x004b8650` returns 35 side-panel callers including MapPane packet dispatcher call sites at `0x00508040`, `0x00508679`, and `0x00508cb2`, the macro-dialog path at `0x00541b9c`, user-pane paths at `0x005a5f9d` and `0x005a6067`, and multiple small input-pane tab-opening wrappers.
- `get_bytes 0x004b8822` confirms the end-of-function byte followed by alignment padding before `0x004b8830`; there is no hidden helper between the switch body and the `GeneralPurposePanel2` constructor.

2026-07-08 B005 current IDA MCP evidence:

- `callees 0x004b8650` returns `0x00544690`, `0x00544ee0`, `0x004b78d0`, `0x004615b0`, `0x004615d0`, security-cookie support, and `0x004e9ee0`.
- `0x004b78d0` is [UID:00015Q][0x004b78d0-0x004b7ae8.RectBoundsMethods](by-memory/0x004b78d0-0x004b7ae8.RectBoundsMethods.md) `RectBounds::SetLTRB`; it writes the four rectangle edges and has no callees.
- `0x00544690` is [UID:0001EA][0x00544460-0x00545086.PaneCore](by-memory/0x00544460-0x00545086.PaneCore.md) `Pane::MarkForDeletion`; it sets Pane delete marker `+0xf4` and queues through BlackHole deferred deletion.
- `0x00544ee0-0x00544f2e` is [UID:0004KU][0x00544ee0-0x00544f2e.PaneCollapse](by-memory/0x00544ee0-0x00544f2e.PaneCollapse.md), with only `sub_4B8650` and `sub_4B89A0` callers. It calls `RemoveFromLayer`, clears a local zero rectangle, then calls inherited `UpdateRenderRegion`.
- The old child then calls inherited slot `+0x40`, supported by [UID:0003JA][PaneVtables](by-type/by-vtable/PaneVtables.md) as `Pane::UnregisterEventHandler`.
- The new child calls inherited `SetBounds`, `AddToLayer`, `SetPaneOrder`, then common `OnActivate(const unsigned char *packetData)` at `+0x48`.
- `0x004615b0` and `0x004615d0` are FrameChrome dynamic AboveFrame release/recreate helpers, and `0x004e9ee0` is `InterfaceEfxMgr::TriggerInterfaceEffect(int effectId)`.

## Packet Dispatch Notes

The map dispatcher uses this helper for server-driven panel tab changes. IDA disassembly shows opcode case `0x34` calling it with tab `1`, case `0x39` calling it with tab `0`, and another dispatcher case calling it with tab `5`. These are side-panel page switches, not dialog-message helper calls.

[UID:0003UT][0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket](by-memory/0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket.md) is a separate `LivingObjectPane` server-packet handler that depends on this method for opcode `0x3e` UI-panel action bytes. Both EPF/current and legacy tables call `GeneralPurposePanel::SwitchActiveTab(2, NULL)` for `payload[1] == 0` to open inventory and `GeneralPurposePanel::SwitchActiveTab(3, NULL)` for `payload[1] == 2` to open spell inventory. That caller evidence reinforces the child-index map and side-panel ownership, but it does not move packet parsing or ACK behavior into `GeneralPurposePanel.cpp`.

## Ownership Notes

`source-3/simroot_v2/class_SpellOneArgInputPane.cpp` currently emits this function as `SpellOneArgInputPane::SwitchActiveTab`. That owner is contradicted by the object layout, constructor ownership, and caller mix. Reconstruct it as `GeneralPurposePanel::SwitchActiveTab` or an equivalently named method on the side-panel shell.

The frame refresh callees belong to [UID:0000JL][FrameChrome](by-file/FrameChrome.md), not to the panel shell. This method depends on them after tab changes and supplies the `m_activeChildIndex` value they consume indirectly through `g_pGeneralPurposePanel`.

B005 source-quality reconciliation resolves the earlier broad helper-name blockers as follows: `0x004b78d0` is `RectBounds::SetLTRB`, `0x00544690` is `Pane::MarkForDeletion`, old-child slot `+0x40` is `Pane::UnregisterEventHandler`, and new-child slots `+0x2c/+0x30/+0x3c` are `Pane::SetBounds` / `Pane::AddToLayer` / `Pane::SetPaneOrder` or ordering helper. `m_childPanes[7]` remains accepted for the child pointer array and `m_activeChildIndex` remains accepted for raw `this[8]`.

The former exact formal-C++ blockers are resolved by UID0004KU `Pane::Collapse`, inherited `UpdateRenderRegion`, and common `OnActivate(const unsigned char *)`. Original public spelling remains inferred rather than symbol-proven and caps confidence without blocking source emission.

## Source-Quality / No-Code Proof

B005's 2026-07-08 target-specific no-code proof is retained as a historical decision that was correct before the shared Pane contracts were resolved. It identified exactly three blockers: helper `0x00544ee0`, its inherited `+0x0c` call, and the new-child `+0x48` call.

The 2026-07-13 B004 pass discharges each blocker rather than deleting that history:

- [UID:0004KU][0x00544ee0-0x00544f2e.PaneCollapse](by-memory/0x00544ee0-0x00544f2e.PaneCollapse.md) supplies exact `Pane::Collapse()` source and proves its only callers.
- Raw Pane/PanelPane vtable cells and the independent `+0x2c` mapping resolve helper `+0x0c` as `UpdateRenderRegion`, not `SetBounds`.
- PanelPane `__purecall` plus eight derived primary-table entries resolve `+0x48` as `OnActivate(const unsigned char *packetData)`. Packet-parsing overrides consume the pointer; option/group/inventory/system-message overrides may ignore it.
- Representative MapPane callers pass EDI payload pointers as the second argument, directly rejecting `bool showChild`.

The formal block is therefore populated. Raw vtable calls, invented wrappers, inlined collapse behavior, bool activation, and family-specific base names remain rejected. The proof remains useful as blocker history but no longer justifies an empty emitter.

## 2026-07-13 B004 Exact Body Reconciliation

- Both branches preserve their separate initial mode decision; the source does not re-read `g_useEpfAssets` after old-child virtual calls.
- EPF/current mode rejects `tabIndex >= 7`; legacy rejects `tabIndex >= 4`. Negative indexes remain unguarded exactly as compiled.
- Same-index tabs two and above, including sentinel `-1`, return; tabs zero and one are intentionally refreshed.
- In both branches the old child is captured, collapsed, and unregistered before `m_activeChildIndex = tabIndex`. This differs from UID00015Y and is preserved.
- The binary computes null for `tabIndex == -1` and then dereferences it in the layout path. The source block deliberately adds no robustness guard because that would change exact behavior.
- EPF mode always dispatches `OnActivate(packetData)` and then FrameChrome release/recreate. Legacy mode dispatches only for a non-null pointer and then triggers InterfaceEfx with the stored active index.
- Score `91/94` reflects exact body, branch ordering, ABI, caller fan-in, source owner, dependencies, padding, and rejected stale owner. Original source identifiers remain the principal confidence cap.

## 2026-07-20 UID00028X Callback Reconciliation

- Metadata remains `91/94`, owner/emitter UID00005Q, with source position 40 after UID0004UL constructor, UID0004UM ordinary destructor, and UID00015W accessors.
- The accepted managed body above is preserved byte-for-byte. Its exact range SHA256 remains `133DB046A6045925EACDFA87D14852DF1D9B8127CE34A8C849845A7D3D9CE0B4`.
- The source interval remains `0x004b8650-0x004b8823`; filename end `0x004b8822` is the inclusive last byte, and `0x004b8823-0x004b8830` remains compiler padding before the separate GeneralPurposePanel2 constructor.
- Direct `LObject` plus `Singleton<GeneralPurposePanel>` inheritance, PMD `+0x04`, EBO, and the exact 0x24-byte layout replace the stale PanelPane assumption without changing tab behavior or field offsets.
- UID0004UN and UID0003BC remain compiler-only support and do not duplicate this authored method. FrameChrome/AboveFrame and InterfaceEfx remain callees/dependencies, not owners.

## Cross References

- [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md)
- [UID:00005Q][GeneralPurposePanel](by-class/GeneralPurposePanel.md)
- [UID:00015W][0x004b85f0-0x004b8643.GeneralPurposePanelChildAccessors](by-memory/0x004b85f0-0x004b8643.GeneralPurposePanelChildAccessors.md)
- [UID:00015V][0x004b83d0-0x004b8bd3.GeneralPurposePanel](by-memory/0x004b83d0-0x004b8bd3.GeneralPurposePanel.md)
- [UID:0000R0][g_pGeneralPurposePanel](by-global/g_pGeneralPurposePanel.md)
- [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md)
- [UID:0000PN][FrameChromeSlots](by-global/FrameChromeSlots.md)
- [UID:0000YI][0x004615b0-0x00461658.AboveFrameRefreshHelpers](by-memory/0x004615b0-0x00461658.AboveFrameRefreshHelpers.md)
- [UID:00015Q][0x004b78d0-0x004b7ae8.RectBoundsMethods](by-memory/0x004b78d0-0x004b7ae8.RectBoundsMethods.md)
- [UID:0001EA][0x00544460-0x00545086.PaneCore](by-memory/0x00544460-0x00545086.PaneCore.md)
- [UID:0003JA][PaneVtables](by-type/by-vtable/PaneVtables.md)
- [UID:00028J][0x0067a740-0x0067a744.g_pBackPane](by-memory/0x0067a740-0x0067a744.g_pBackPane.md)
- [UID:00029M][0x0069b364-0x0069b370.MainUiLayerSlotsHead](by-memory/0x0069b364-0x0069b370.MainUiLayerSlotsHead.md)
- [UID:0000R8][g_pInterfaceEfxMgr](by-global/g_pInterfaceEfxMgr.md)
- [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md)

## Changes

- 2026-07-20 B002 UID00028X accepted callback: retained `91/94`, set position 40, preserved the formal body byte-for-byte, synchronized exact hash/range/padding and direct Singleton hierarchy, linked the real lifecycle children, corrected the remaining stale present-tense `+0x48` wording to `OnActivate`, and preserved all B004/B005 behavior, caller, dependency, negative, and historical detail.
- 2026-07-13 B004 accepted UID00015Y support callback: raised `88/93 -> 91/94`; inserted exact two-branch managed C++; replaced bool activation with packet data; linked UID0004KU Collapse; resolved `+0x0c`/`+0x48`; preserved all limits, same-tab exceptions, old-child-before-store ordering, rectangles, null behavior, FrameChrome/InterfaceEfx calls, 35 callers, padding, ownership rejection, and historical B005 no-code proof.
- 2026-07-08 B005 implementation callback: raised target metadata from `85/91` to `88/93`, incorporated current IDA MCP session `b007_uid0002oc_20260708` boundary/padding proof, refreshed the 35-caller fan-in with MapPane and LivingObjectPane callsites, resolved `RectBounds::SetLTRB`, `Pane::MarkForDeletion`, `Pane::UnregisterEventHandler`, `Pane::SetBounds`, `Pane::AddToLayer`, and `Pane::SetPaneOrder` names for prose, documented helper `0x00544ee0-0x00544f2e` behavior and remaining `+0x0c`/`+0x48` formal-C++ blockers, and kept the formal C++ block blank under the target-specific no-code proof.
- 2026-06-20 B003 Rule 26 source-quality incorporation: accepted `m_childPanes[7]` and `m_activeChildIndex` as source-facing field names for the tab switch body, kept `GeneralPurposePanel::SwitchActiveTab(int tabIndex, bool showChild)` as the best source-facing method name, and preserved RectBounds, PaneCore, TotemFrame, FrameChrome, and InterfaceEfx calls as external dependencies rather than `GeneralPurposePanel.cpp` implementation bodies. Method-body C++ remains deferred until those helper/virtual-slot names are cleaner.
- 2026-06-20 B004 [UID:0003UT][0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket](by-memory/0x005abda0-0x005abee8.UserPaneHandleUIPanelSwitchPacket.md) support sync: added the opcode `0x3e` caller context where `payload[1] == 0` opens inventory tab `2` and `payload[1] == 2` opens spell inventory tab `3`. Ownership remains `GeneralPurposePanel` for this helper and `LivingObjectPane` for the packet handler.
- 2026-06-20 B003 AboveFrame source-quality implementation: corrected the FrameChrome refresh helper link/range to `0x004615b0-0x00461658` and accepted `m_activeChildIndex` as the source-facing name for raw `this[8]`. The dynamic frame refresh still belongs to FrameChrome; this method only calls it after updating the active child.
- 2026-06-17 B003 InterfaceEfx trigger support sync: clarified that the legacy branch passes the active child index directly to `InterfaceEfxMgr::TriggerInterfaceEffect(effectId)`. Score unchanged because this is caller-context wording for [UID:00018H][0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect](by-memory/0x004e9ee0-0x004e9f2d.InterfaceEfxMgrTriggerInterfaceEffect.md).
- 2026-06-16 A002 source-quality refresh:
  - Before: `COMPLETION:84`, `CONFIDENCE:90`.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:91`.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x004b8650-0x004b8822` boundary, padding before `GeneralPurposePanel2`, the 35-entry side-panel caller set, decompiled EPF/legacy tab limits and rectangles, child-array/active-index layout, frame-refresh and legacy-effect dependencies, and absence of spell-input local state. The stale `SpellOneArgInputPane` generated owner is now resolved as pollution; final C++ remains blank pending source-quality helper/member names.
- 2026-06-06: Raised from `70/85` to `84/90` and attached to [UID:00005Q][GeneralPurposePanel](by-class/GeneralPurposePanel.md). Live IDA MCP reconfirmed exact bounds, caller fan-in, EPF/legacy tab limits and rectangles, active-index/child-array layout, totem-frame cleanup, old-child hide/collapse, frame-refresh and legacy-effect callees, and stale `SpellOneArgInputPane` generated ownership.
