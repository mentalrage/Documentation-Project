*** UID:00031N | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollVolumePane::SetScrollStyle(unsigned char style)
{
    if (m_scrollStyle == style) {
        return;
    }

    m_scrollStyle = style;
    InvalidateRect(&m_bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw ScrollVolumePane helper stores inferred scroll/slider style byte +0xfa, initialized to 1 by the constructor, and invalidates bounds when changed; MCP finds no other +0xfa consumers and no direct raw-start route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

## 2026-07-26 Accepted Aggregate-Split Reconciliation

- Direct semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), position 30; UID0001H2 is only the non-emitting address index.
- Exact body remains `[0x005647f0,0x00564814)`, size `0x24`, bounded by nine and twelve all-`0xcc` bytes.
- Current live checks found no IDA function, inbound xref, direct branch, or VA/RVA/raw-offset pointer route. Preserve that negative route as a confidence cap without treating the coherent compare/store/invalidate body as padding.
- Constructor default `+0xfa=1`, exact byte access, sibling setter shape, and the homologous ScrollSpell control support accepted descriptive `SetScrollStyle` / `m_scrollStyle` names.
- Rejected alternatives include generic `SetStateByte`, drag/type/part/value meanings, aggregate-owned code, compiler glue, and exact-original-name deferral. Formal C++ remains on this exact child.

# 0x005647f0-0x00564814 - ScrollVolumePane Set Scroll Style Raw

## Status

- Address range: `0x005647f0-0x00564814`, exclusive end.
- Verified size: `0x24` / 36 bytes (Verified with int_convert.py).
- Disposition: reconstructable `ScrollVolumePane` raw helper with inferred source-facing first-draft C++.
- Source-facing helper/field: `ScrollVolumePane::SetScrollStyle(unsigned char style)` writes inferred `m_scrollStyle` at `this + 0xfa`.
- Owner evidence: function-shaped code between the constructor and `SetRange` inside [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md).
- Proposed class/source: [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md) / [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md).
- Parent assignment: attached to [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) at position `30`; both child and direct parent clear the strict `85/85` gate.
- Naming caveat: `SetScrollStyle` and `m_scrollStyle` are inferred source-facing names. Current evidence does not recover the original spelling, live dispatch route, or exact visual effect of the byte.

## Behavior

This raw helper compares an 8-bit argument with the byte at `this + 0xfa`. If the value changes, it stores the new byte and invalidates the pane bounds rectangle at `this + 0x44` through the primary vtable slot `+0x20`; otherwise it returns without side effects.

The constructor initializes the containing packed dword at `this + 0xf8` to `0x00010000`, which sets `+0xf8` to `0`, `+0xfa` to default value `1`, and `+0xfb` to `0`. The local `ScrollVolumePane` island never reads `+0xfa` outside this setter, so `m_scrollStyle` remains an inference from the invalidating setter shape and the related scroll-pane family rather than direct caller or consumer proof.

## Raw Code Evidence

B002 MCP recheck used live IDA MCP session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `status: ok`, auto-analysis ready, Hex-Rays ready, and strings cache ready.

`get_bytes` returned the exact target bytes:

```text
55 8b ec 8a 45 08 38 81 fa 00 00 00 74 12 8b 11
88 81 fa 00 00 00 8d 41 44 89 45 08 5d ff 62 20
5d c2 04 00
```

IDA MCP bounded disassembly confirms the body:

```asm
0x005647f0  push ebp
0x005647f1  mov  ebp, esp
0x005647f3  mov  al, [ebp+8]
0x005647f6  cmp  [ecx+0fah], al
0x005647fc  jz   0x00564810
0x005647fe  mov  edx, [ecx]
0x00564800  mov  [ecx+0fah], al
0x00564806  lea  eax, [ecx+44h]
0x00564809  mov  [ebp+8], eax
0x0056480c  pop  ebp
0x0056480d  jmp  dword ptr [edx+20h]
0x00564810  pop  ebp
0x00564811  retn 4
```

## Route And Boundary Evidence

- `lookup_funcs` reports `0x005647f0` as `Not a function`; this remains a raw/projected helper rather than an IDA-modeled function.
- `xrefs_to` reports zero direct xrefs to `0x005647f0`.
- `find_bytes` found zero matches for target pointer patterns `f0 47 56 00`, `f0 47 16 00`, and `f0 45 16 00`; no ordinary VA, RVA, or raw-offset literal route is visible.
- The previous raw helper [UID:00031M][0x005647c0-0x005647e7.ScrollVolumePaneSetTrackStateWordRaw](by-memory/0x005647c0-0x005647e7.ScrollVolumePaneSetTrackStateWordRaw.md) ends at `0x005647e7`; `0x005647e7-0x005647f0` is `0x09` bytes of `0xcc` alignment.
- The next modeled function, `SetRange`, starts at `0x00564820`; `0x00564814-0x00564820` is `0x0c` / 12 bytes of `0xcc` alignment.

## Constructor And Field-Use Evidence

Constructor decompilation at `0x00564710` confirms the packed initialization around this field:

- `*(BYTE *)(this + 252) = a2` stores `m_volumeType` at `+0xfc`.
- `*(DWORD *)(this + 254) = 0` clears `m_value` and `m_range` at `+0xfe/+0x100`.
- `*(DWORD *)(this + 248) = 0x10000` initializes `+0xf8` to `0`, `+0xfa` to `1`, and `+0xfb` to `0`.
- `*(WORD *)(this + 258) = -255` initializes `m_enabled` at `+0x102` to `1` and `m_hoverPart` at `+0x103` to `0xff`; historical `m_highlightPart` wording is superseded.
- `*(BYTE *)(this + 260) = -1` initializes `m_activePart` at `+0x104` to `0xff`.

Scoped MCP `search_text` over `0x00564710-0x005654ec` found:

| Field | Hits | Source-quality interpretation |
| --- | ---: | --- |
| `0FAh` | 2 | Only this target's compare at `0x005647f6` and store at `0x00564800`. |
| `0FBh` | 4 | Paint, hit-test, and part-rectangle drag-override reads; this is distinct `m_thumbDragActive`. |
| `0F8h` | 3 | Constructor packed dword store plus [UID:00031M][0x005647c0-0x005647e7.ScrollVolumePaneSetTrackStateWordRaw](by-memory/0x005647c0-0x005647e7.ScrollVolumePaneSetTrackStateWordRaw.md) compare/store. |
| `0FCh` | 2 | Constructor volume-type store and notify callback use. |
| `0FEh` | 10 | Current value reads/writes. |
| `100h` | 11 | Range/max value reads/writes. |
| `102h` | 11 | Enabled byte reads/writes and `CanAdjust`. |
| `103h` | 13 | Highlight part reads/writes. |
| `104h` | 7 | Active part reads/writes. |
| `108h` | 3 | Thumb-drag offset point formation/storage. |
| `10Ch` | 5 | Horizontal component of the thumb-drag offset. |

This census rejects reusing `m_volumeType`, `m_thumbDragActive`, `m_value`, `m_range`, `m_enabled`, `m_hoverPart`, `m_activePart`, or `m_thumbDragOffset` for `+0xfa`. The byte is real initialized state because the constructor sets it, this helper changes it, and the helper invalidates the pane bounds on change; it is not documented as padding or dead storage.

## Comparative Scroll-Pane Evidence

The related `ScrollSpellInventoryPane` family has the same raw setter pattern at `0x0055f530-0x0055f554`: MCP disassembly shows `cmp [ecx+0FAh], al`, `mov [ecx+0FAh], al`, `lea eax,[ecx+44h]`, and invalidation through `[edx+20h]`. Current `ScrollSpellInventoryPaneLayout` wording treats that analogous byte as an unresolved scroll style/state byte.

That family match is why this page uses `SetScrollStyle` and `m_scrollStyle` instead of the older type-shaped `SetStateByte` and `m_stateByte`. The name is stronger source shape than a generic byte name, but it remains below final-audit confidence until a caller, consumer, or source symbol proves exact spelling and visual semantics.

## Parent Gate And Score Rationale

The direct parent [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is `87/90`, already attached to [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), and documents this helper as an exact nested child between the low-state-word setter and `SetRange`.

This child is `88/90` because the byte range, raw bytes, disassembly, constructor initialization, field-use census, invalidation call, lack of modeled function object, absence of direct xrefs, absence of raw pointer hits, surrounding padding, and comparative scroll-pane setter evidence are all recorded. Completion and confidence stay below final-audit quality because the exact original source spelling, live dispatch route, and external consumer/effect for `+0xfa` remain unrecovered.

## Cross-References

- [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md)
- [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md)
- [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)
- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md)
- Comparative family lead: `ScrollSpellInventoryPane` `0x0055f530-0x0055f554` raw `+0xfa` setter.

## Changes

- 2026-06-08 A008 Batch 137 split:
  - What existed before: the parent aggregate jumped from constructor end `0x005647b1` to `SetRange` at `0x00564820`, and this raw setter-shaped body was not documented as an exact child.
  - Changed to: exact raw helper page for `0x005647f0-0x00564814`.
  - Evidence: live IDA MCP disassembly, `lookup_funcs`, `xrefs_to`, and byte-gap review confirmed the raw helper body, field write at `+0xfa`, invalidation call through vtable slot `+0x20`, no direct xrefs, and surrounding alignment.
- 2026-06-12 A005 Batch245:
  - Before: `76/84`, parent blank under the strict gate.
  - Changed to: `85/88`, `AUTOGEN_PARENT_UID:0001H2`, position `30`; reconstruction C++ remained blank below the then-active code-entry bar.
  - Evidence: restarted IDA MCP rechecked `lookup_funcs`, `xrefs_to`, no pointer-pattern hits, exact `0x24` / 36-byte body size, `0x0c` / 12-byte trailing alignment, `+0xfa` byte write, `+0x44` invalidation rect, and vtable slot `+0x20`; [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is the direct enclosing `85/88` parent.
- 2026-06-19 B014 class source-quality incorporation:
  - Changed completion/confidence from `85/88` to `86/89` and populated descriptive first-draft `ScrollVolumePane::SetStateByte(unsigned char)` C++.
  - Evidence: B014 class pass accepted this as a source-shaped private helper for `+0xfa`, preserved the no-xref/raw-start route caveat, and capped the score because the exact historical field/helper name remained unresolved.
- 2026-06-25 B002 source-quality implementation:
  - Changed completion/confidence from `86/89` to `88/90`, renamed the page from `ScrollVolumePaneSetStateByteRaw` to `ScrollVolumePaneSetScrollStyleRaw`, and replaced the formal C++ with `ScrollVolumePane::SetScrollStyle(unsigned char style)` using inferred `m_scrollStyle`.
  - Evidence: current IDA MCP session `80de0a67` verified exact bytes, no IDA function at the raw start, zero direct xrefs, zero VA/RVA/raw-offset pointer-pattern hits, constructor initialization of `+0xfa` to `1`, a scoped field-use census with `0FAh` appearing only in this setter, and the same `+0xfa` invalidating setter pattern in the related `ScrollSpellInventoryPane` family. Exact original spelling and live route remain unrecovered, so the page preserves the inferred-name caveat.
