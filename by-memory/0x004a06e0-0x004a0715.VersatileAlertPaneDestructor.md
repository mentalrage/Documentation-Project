*** UID:00012Y | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FU | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FU | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
VersatileAlertPane::~VersatileAlertPane()
{
    delete m_resultCallback;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ordinary VersatileAlertPane destructor source carrier; deletes the owned m_resultCallback once, while compiler-generated vptr resets and DialogPane base teardown reproduce the unique IDA-unmodeled 0x004a06e0 body despite its absent entry route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004a06e0-0x004a0715 VersatileAlertPane Destructor

## Status

- Disposition: reconstructable exact ordinary/non-deleting destructor source semantics with an IDA-unmodeled entry and corrected half-open range.
- Entity kind: ordinary class destructor source carrier; not a standalone raw-cleanup helper and not a scalar deleting wrapper.
- Direct semantic owner/emitter: [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md)
- Source module: [UID:0000HE][AlertPanes](by-file/AlertPanes.md)
- Parent method index: [UID:00012X][0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland](by-memory/0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland.md).
- Source order: after constructor [UID:0004NB][0x004a0690-0x004a06d7.VersatileAlertPaneConstructor](by-memory/0x004a0690-0x004a06d7.VersatileAlertPaneConstructor.md), with blank position and relative `Nested:0`.
- Confidence: very strong for boundary, effects, ownership, source body, and compiler/source separation; retained uncertainty is only why the linker kept an unreferenced ordinary copy.
- Final C++: exact ordinary destructor body shown in the managed block above.

## Behavior

The binary body reinstalls the three `VersatileAlertPane` vtable views, releases `m_resultCallback` at offset `+0x270` through the shared `FunctionObject` virtual deleting slot when present, clears no singleton/global state, and tail-jumps to shared `DialogPane` teardown at `0x0049d9f0`.

IDA currently labels the target `0x0049d9f0` as `boost::exception::~exception`, while generated text often labels the same base teardown as `TransferReplyAlert`. Both labels are type-owner pollution. The vtable stores and callback offset tie this body to `VersatileAlertPane`.

The human source is one normal virtual destructor with `delete m_resultCallback;`. C++ `delete` reproduces the observed null-safe virtual destruction of the owned pointer. Derived vptr resets, implicit base/member teardown, and the tail transfer are compiler output and therefore stay out of the formal body. This exact page carries the ordinary source destructor; UID000130 remains the separate compiler ABI wrapper island for B004 phase 2.

## Current Source-Quality Resolution - 2026-07-14

### Exact range, bytes, and liveness

- Exact half-open range: `0x004a06e0-0x004a0715`, 53 coherent bytes.
- Exact SHA256: `63B15AE95D640C3CB7A16D3DDD866CD8E68AAD835102C6F4C0F66C20B6D3968D`.
- Exact bytes: `56 8b f1 8b 8e 70 02 00 00 c7 06 a4 8b 61 00 c7 86 a0 00 00 00 0c 8c 61 00 c7 86 a4 00 00 00 3c 8c 61 00 85 c9 74 06 8b 01 6a 01 ff 10 8b ce 5e e9 db d2 ff ff`.
- `0x004a0714` is the final byte of the tail-jump immediate; `0x004a0715-0x004a0720` is eleven bytes of `0xcc` alignment before [UID:0004NC][0x004a0720-0x004a0732.VersatileAlertPaneOnPrimaryButton](by-memory/0x004a0720-0x004a0732.VersatileAlertPaneOnPrimaryButton.md).
- IDA does not model `0x004a06e0` as a function. `xrefs_to`, code-only xrefs, direct VA/RVA scans, and filtered `.text` rel32 scans find no entry route. The exact body bytes occur once in the executable.

The absent entry route is retained negative provenance, not a semantic blocker. The unique body has complete ordinary destructor behavior, and the live scalar deleting wrapper reproduces the same sequence before storage-deletion logic. Original linker/COMDAT retention cannot be recovered without build artifacts and does not justify suppressing the source destructor required by the class declaration and vtables.

### Body effects and base identity

The body loads `m_resultCallback` from `+0x270`; writes vtable bases `0x00618ba4`, `0x00618c0c`, and `0x00618c3c` at object offsets `+0x00`, `+0xa0`, and `+0xa4`; null-checks the pointer; invokes callback virtual slot `+0x00` with deleting value `1`; and tail-jumps to `0x0049d9f0`.

Current decompilation of `0x0049d9f0` writes `DialogPane` vtables, decrements dialog state, releases DialogPane-owned objects/lists, and calls ordinary Pane cleanup. Historical IDA `boost::exception::~exception` and generated `TransferReplyAlert` labels are type pollution. Source order is explicit callback deletion followed by implicit trivial `AlertPane` and inherited `DialogPane`/Pane/GrafPort/LObject/interface teardown.

The body does not clear `m_resultCallback`, mutate a global/singleton, free complete-object storage, inspect delete flags, or perform the scalar wrapper's `0x274` guarded size path. Those absent effects distinguish the ordinary destructor from UID000130 compiler ABI support.

### Ownership, type, and source placement

[UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) is the direct owner and emitter. [UID:0000HE][AlertPanes](by-file/AlertPanes.md) is the highest-probability `AlertPanes.cpp`/probable header source route. [UID:0001WQ][FunctionObjectTemplates](by-type/by-template/FunctionObjectTemplates.md) supplies the virtual destructor and `FunctionObjectT<unsigned long>` interface; it is a dependency, not the pane owner.

The callback is an owning raw pointer. Both constructor callers allocate member-function callback objects and transfer them to the pane; neither caller later frees them. Constructor storage, both invokes, this ordinary destructor, and the scalar wrapper converge on exactly one `m_resultCallback` deletion.

### Source/compiler disposition and rejected alternatives

- Accepted source: `VersatileAlertPane::~VersatileAlertPane() { delete m_resultCallback; }`.
- Compiler output: all vptr resets, explicit base tail call, adjusted-this thunks, scalar delete flags/free/guard handling, RTTI/vtable arrays, and alignment.
- Standalone `VersatileAlertPaneRawCleanup` is rejected because there is no call contract and the bytes are the unique ordinary destructor copy.
- Constructor-unwind-only cleanup is rejected because the live deleting wrapper and all three vtable views require the same ordinary virtual destructor semantics.
- Emitting the destructor from UID000130 is rejected because that range is compiler ABI support with flags and optional storage deletion; B004 owns its split/non-emitting conversion.
- `ModelessDialogPane`, direct `AlertPane`, FunctionObjects, Boost, and TransferReplyAlert ownership are rejected by derived vtable stores, `+0x270` field access, dependency direction, and the corrected base identity.
- `CANONICAL_OWNER:NONE` is rejected because class ownership is direct even though entry liveness is absent.

### Superseded no-code conclusion

The 2026-06-25 no-code decision was reasonable evidence-time caution but is now superseded. Current shared template declarations close call/delete spelling; exact source/compiler reduction identifies this page as the ordinary destructor carrier; class/vtable/RTTI/object-size evidence eliminates helper and ABI-wrapper alternatives; and the accepted formal body is behavior-identical. All older statements below that say this page must remain blank or cannot carry the public destructor are historical only.

### Score rationale

Completion rises from `86` to `94` because the page now has exact ordinary-destructor classification, complete bytes/hash/boundary/liveness evidence, body effects, base identity, callback ownership/type, compiler exclusions, source placement, formal C++, and rejected alternatives. Confidence rises from `89` to `96` because unique-body scans, vtable stores, class declaration, live scalar mirror, RTTI/sizes, and source policy converge. Missing original linker/PDB provenance prevents 100.

## Boundary Evidence

The dated evidence sections below are preserved at full detail. Their former no-code/source-deferral conclusions are superseded by the current resolution above; their raw-byte, xref, boundary, and negative-route facts remain valid.

- Current IDA MCP `lookup_funcs` reports `0x004a06e0`, `0x004a0710`, and `0x004a0714` as "Not a function"; `0x004a0720` is the next modeled function and starts `VersatileAlertPane::OnPrimaryButton` / accept behavior.
- Raw IDA disassembly shows a coherent body from `0x004a06e0` through the 5-byte tail jump at `0x004a0710-0x004a0715`, followed by alignment padding from `0x004a0715` through `0x004a071f`. The page was renamed from the stale `0x004a06e0-0x004a0714` filename because the old half-open end excluded the final byte of the jump immediate.
- IDA MCP `xrefs_to` for the three `VersatileAlertPane` vtables reports raw data references inside this body at `0x004a06e9`, `0x004a06ef`, and `0x004a06f9`.
- IDA MCP `xrefs_to 0x004a06e0` reports no callers in the current database.
- Earlier Ghidra records `0x004a06e0` as a 53-byte function with the same vtable stores, `m_resultCallback` release, and base teardown call. Its `TransferReplyAlert` owner/type label is inherited pollution from the shared alert base destructor.
- Existing sibling docs now agree on the raw-body split: [UID:00012X][0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland](by-memory/0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland.md) records the modeled constructor at `0x004a0690`, callback functions at `0x004a0720` and `0x004a0740`, and the same unmodeled cleanup-shaped body at `0x004a06e0`; [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) records the same three vtable-store refs and no direct callers.

## Fresh IDA Recheck - 2026-06-25

B008 rechecked this target through the current IDA MCP endpoint `http://127.0.0.1:13337/mcp`. The active MCP session was `80de0a67` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `status:"ok"`, auto-analysis ready, Hex-Rays ready, strings cache ready, module `NexusTK.exe`, imagebase `0x400000`, and input image `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`. The active schema requires `database` on IDB-backed calls; `decompile` and `disasm` use `addr,database`, `lookup_funcs` uses `queries,database`, and `xrefs_to` uses `addrs,database`.

Current `lookup_funcs` facts:

- `0x004a0690` is modeled as `sub_4A0690` with size `0x47`.
- `0x004a0720` and `0x004a0740` are modeled `0x12` callback functions.
- `0x004a0760` is the next modeled function after the callback cluster.
- `0x004a0cb0`, `0x004a0cbb`, and `0x004a0cd0` are modeled destructor/thunk starts.
- `0x004a06d7`, `0x004a06e0`, `0x004a0710`, `0x004a0714`, and `0x004a0715` are not modeled as functions.

Current `insn_query 0x004a06d0-0x004a0720` confirms the constructor epilogue ends with `retn 14h` at `0x004a06d4`, alignment begins at `0x004a06d7`, the raw cleanup instructions start at `0x004a06e0`, the 5-byte tail jump starts at `0x004a0710`, and `0xcc` alignment begins at `0x004a0715`.

Current byte evidence:

- `get_bytes 0x004a06d0 size 0x50`, `get_bytes 0x004a06e0 size 0x40`, and `get_bytes 0x004a0714 size 0x10` were checked.
- The cleanup body bytes run through `e9 db d2 ff ff`; byte `0x004a0714` is the final `0xff` byte of that jump immediate, followed by `0xcc` padding at `0x004a0715`.
- The exact body signature from `make_signature_for_range 0x004a06e0 0x004a0715` is:

```text
56 8B F1 8B 8E 70 02 00 00 C7 06 A4 8B 61 00 C7 86 A0 00 00 00 0C 8C 61 00 C7 86 A4 00 00 00 3C 8C 61 00 85 C9 74 06 8B 01 6A 01 FF 10 8B CE 5E E9 DB D2 FF FF
```

Current instruction semantics:

- `0x004a06e0` pushes `esi`; `0x004a06e1` moves `this` from `ecx` into `esi`.
- `0x004a06e3` loads `[esi+270h]`, the `m_resultCallback` field documented on [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) and [UID:0001TO][AlertPaneLayout](by-type/by-struct/AlertPaneLayout.md).
- `0x004a06e9`, `0x004a06ef`, and `0x004a06f9` reinstall the three `VersatileAlertPane` vtable views `0x00618ba4`, `0x00618c0c`, and `0x00618c3c`.
- `0x004a0703-0x004a070b` null-checks the callback, pushes `1`, and calls callback vtable slot `+0x00`.
- `0x004a0710` tail-jumps to `0x0049d9f0`.

Current xref and negative-route facts:

- `xrefs_to 0x004a06e0` returns zero entry references, and code-only `xref_query to 0x004a06e0` also returns zero.
- `xrefs_to 0x004a0710` reports only the local code xref from `0x004a070f`; `xref_query from 0x004a0710` resolves the tail jump to `0x0049d9f0`.
- `xrefs_to 0x00618ba4`, `0x00618c0c`, and `0x00618c3c` each reports the same three store sites: constructor stores at `0x004a06b6`/`0x004a06bc`/`0x004a06c6`, this raw cleanup body stores at `0x004a06e9`/`0x004a06ef`/`0x004a06f9`, and destructor stores at `0x004a0cdc`/`0x004a0ce2`/`0x004a0cec`.
- `xrefs_to 0x004a0720` and `0x004a0740` report vtable-data refs at `0x00618c00` and `0x00618c04`; no direct code callers were reported for the virtual callbacks.
- `xrefs_to 0x004a0cd0` reports thunk code refs at `0x004a0cb6` and `0x004a0cc1` plus the primary vtable data ref at `0x00618ba4`.
- `find_bytes` found no direct VA pointer encoding `e0 06 4a 00` and no direct RVA pointer encoding `e0 06 0a 00` for the raw cleanup entry.
- Read-only PE scanning of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` found the exact target body bytes once at raw file offset `0x9fae0`, found no direct VA/RVA pattern for `0x004a06e0`, and found no filtered `.text` `E8`/`E9`/near-conditional rel32 route to `0x004a06e0`.

Current sibling comparison:

- Constructor `0x004a0690` calls alert base construction at `0x0049feb0`, stores the non-forwarded callback argument at `this+0x270`, and installs the same three `VersatileAlertPane` vtables.
- `0x004a0720` and `0x004a0740` load `m_resultCallback`, null-check it, and call callback slot `+0x0c` with result values `1` and `0`.
- Scalar deleting destructor body `0x004a0cd0` loads `m_resultCallback`, writes the same three vtables, calls callback slot `+0x00` with flag `1`, calls the same base teardown at `0x0049d9f0`, and then handles scalar-deleting/free flags.
- Decompilation of base teardown `0x0049d9f0` is source-relevant as shared alert/DialogPane teardown, not as IDA's polluted `boost::exception::~exception` label: it writes `DialogPane` vtables, updates dialog/modal state, releases list/control children, and chains through dialog cleanup routines.

## Fresh IDA Recheck - 2026-06-13

- Active IDA MCP session targets `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`; `server_health` reported image base `0x400000`, auto-analysis ready, and Hex-Rays ready.
- `lookup_funcs` confirms the surrounding modeled starts: constructor `0x004a0690` size `0x47` / 71 bytes (Verified with int_convert.py), `OnPrimaryButton` / accept behavior at `0x004a0720` size `0x12` / 18 bytes (Verified with int_convert.py), `OnSecondaryButton` / cancel behavior at `0x004a0740` size `0x12` / 18 bytes (Verified with int_convert.py), `ModelessDialogPane` successor `0x004a0760` size `0xc7` / 199 bytes (Verified with int_convert.py), and deleting destructor `0x004a0cd0` size `0x67` / 103 bytes (Verified with int_convert.py).
- `insn_query 0x004a06d0-0x004a0720` shows constructor return bytes through `0x004a06d4`, `0xcc` alignment at `0x004a06d7-0x004a06df`, the unmodeled cleanup body at `0x004a06e0-0x004a0715`, and `0xcc` alignment beginning at `0x004a0715`.
- `get_bytes(0x004a06e0, 0x34)` read 52 bytes (Verified with int_convert.py) and showed the final byte is still inside the `jmp` immediate; `get_bytes(0x004a0714, 12)` showed byte `0xff` at `0x004a0714` followed by `0xcc` padding, proving the exact body is 53 bytes and the half-open end is `0x004a0715`.
- `xrefs_to 0x004a06e0` and `xref_query direction=to` both report zero entry references. `xref_query direction=from 0x004a0710` resolves the tail jump to `0x0049d9f0`, currently polluted as `boost::exception::~exception`.
- `xrefs_to` the vtable bases `0x00618ba4`, `0x00618c0c`, and `0x00618c3c` shows each has exactly three refs: constructor stores at `0x004a06b6`/`0x004a06bc`/`0x004a06c6`, this cleanup body stores at `0x004a06e9`/`0x004a06ef`/`0x004a06f9`, and deleting-destructor stores at `0x004a0cdc`/`0x004a0ce2`/`0x004a0cec`.
- 2026-06-17 B002 source-quality recheck names the released `+0x270` field `m_resultCallback` and ties the cleanup slot to the shared `FunctionObject` family. The body should remain documented as IDA-unmodeled raw cleanup with no direct entry route; final source should emit normal `VersatileAlertPane` destruction semantics rather than duplicate this body and the scalar deleting wrapper as separate hand-written functions.

## 2026-06-01 Documentation Pass

- Marked reconstructable and parented to [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) based on the three class vtable stores and callback release at `+0x270`.
- Kept the score below destructor/source-ready level because IDA still does not model `0x004a06e0` as a normal function and current xrefs do not prove whether this should surface as a non-deleting destructor, unwind cleanup, or unreferenced duplicate cleanup body.

## 2026-06-06 Existing-Docs Reconciliation

- Cross-checked the callback aggregate and class page: both now independently preserve the raw `0x004a06e0` cleanup body as `VersatileAlertPane` cleanup glue rather than a `ModelessDialogPane`, `TransferReplyAlert`, or `Clan`-owned body.
- Raised completion from `72` to `76` because ownership/body effects are now corroborated across the by-memory alias, callback aggregate, class page, and file page. Confidence remains `84`; final C++ remains blank until the source rewrite chooses the destructor/unwind/unreferenced-glue representation.

## 2026-06-13 Goal 2 Refresh

- Renamed the page to the corrected half-open range `0x004a06e0-0x004a0715` after fresh IDA bytes showed the old end excluded the final byte of the tail-jump immediate.
- Raised completion/confidence from `76/84` to `86/89`. The page now records live IDA function-boundary, byte, instruction, xref, vtable-store, constructor/destructor sibling, padding, and parent-gate evidence.
- Kept `RECONSTRUCTABLE:TRUE` and direct parent [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) because source-level `VersatileAlertPane` destruction must still recreate the callback-release/base-teardown semantics even if this exact emitted body later proves to be suppressible unwind or duplicate cleanup glue.
- Kept final C++ blank because the exact source representation of the unreferenced body remains below source-ready confidence even though the class-level destructor semantics are now understood.
- 2026-06-17 B002 source-quality execution kept the score at `86/89` and refreshed wording to the corrected `m_resultCallback` field name, shared `FunctionObject` cleanup treatment, no-entry-route caveat, and one-source-destructor final-C++ policy.
- 2026-06-21 B005 source-quality Rule 26 incorporation kept the score at `86/89` and linked this raw cleanup body to the [UID:0002P1][0x00618ba0-0x00618c44.VersatileAlertPaneVtableData](by-memory/0x00618ba0-0x00618c44.VersatileAlertPaneVtableData.md) vtable-data no-code policy: it is destructor-shaped support for the same class semantics, not a separate hand-authored helper, unless future xrefs prove a callable source entry.
- 2026-06-25 B008 implementation callback kept score and metadata unchanged at `86/89`, [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) owner/emitter, `RECONSTRUCTABLE:TRUE`, and blank formal C++. Added current MCP/PE no-entry-route evidence, the exact `0x004a06e0-0x004a0715` signature and boundary proof, constructor/callback/destructor sibling comparison, rejected alternatives, and the above-gate target-specific no-code proof.

## Historical Pre-Callback Reconstruction Decision (Superseded)

Keep this range with `VersatileAlertPane` as a lifecycle cleanup candidate, but do not expose it as a normal user-facing source method until the source rewrite decides whether it is:

- a non-deleting destructor helper;
- constructor-unwind cleanup emitted for exception paths;
- an unreferenced duplicate cleanup body that should be modeled only through ordinary C++ destructor semantics.

Do not merge this range into [UID:0000LH][ModelessDialogPane](by-file/ModelessDialogPane.md); that module begins at `0x004a0760` after the `VersatileAlertPane` callback functions and alignment gap.

## Historical No-Code Proof - 2026-06-25 (Superseded)

This target mechanically clears the minimum formal-C++ gate: it is `RECONSTRUCTABLE:TRUE`, has `EMITTER_UIDS:0000FU`, and its `86/89` score is above the active over-85 combined-score threshold. The formal `RECONSTRUCTION_CPP CODE` block still stays blank for target-specific reasons:

- Current IDA and read-only PE checks find no entry route to `0x004a06e0`: no IDA function, no MCP xrefs or code-only xrefs to the entry, no direct VA/RVA byte-pattern hits, and no filtered `.text` rel32 route.
- The body is real class lifecycle code, not dead padding, because it has unique bytes, reinstalls all three `VersatileAlertPane` vtable views, reads and releases `m_resultCallback`, and tail-jumps to alert/DialogPane base teardown.
- Its semantics duplicate the coordinated destructor sequence documented on [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md): vtable reset, `m_resultCallback` cleanup through the shared `FunctionObject` slot, and base teardown. The modeled destructor island then adds scalar-deleting/free handling.
- Final source should express one `VersatileAlertPane` destructor shape coordinated with [UID:00012X][0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland](by-memory/0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland.md), [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md), [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md), and the shared `FunctionObjects` declarations, not a separate public helper or second hand-written method for [UID:00012Y].
- A marker comment that claims this exact range is already covered by [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md) would be premature while [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md) still has blank method-body C++. This page should document the no-entry cleanup evidence and remain no-code until one accepted destructor body is available.

Rejected alternatives remain closed under the current evidence:

- Standalone `VersatileAlertPaneRawCleanup` source helper is rejected because no entry route exists and the behavior duplicates destructor semantics.
- A public destructor body emitted from [UID:00012Y] is rejected because the modeled vtable/thunk destructor route is [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md), while this range has no known callable source entry.
- Dead padding is rejected because the bytes form coherent cleanup code and have vtable store xrefs.
- `ModelessDialogPane` ownership is rejected because `0x004a0760` starts after the callback cluster and this target writes `VersatileAlertPane` vtables.
- Direct `AlertPane` ownership is rejected because `AlertPane` supplies inherited base teardown, not the derived callback field or derived vtable reset.
- `FunctionObjects` ownership is rejected because the callback object is a dependency being released, not the owner of the pane lifecycle body.
- Boost, `TransferReplyAlert`, and `DialogPane` labels are rejected as type/owner pollution for this range.
- `CANONICAL_OWNER:NONE` is rejected because [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md) remains the strong semantic owner even though formal C++ is blank.
- The stale `by-memory/0x004a06e0-0x004a0714.VersatileAlertPaneRawCleanup.md` path is missing and must not be recreated; validator/report execution should refresh generated stale-path state rather than hand-editing generated files or validator state.

## Historical 2026-06-30 B011 Empty-Emitter Implementation (Superseded)

- Score and metadata unchanged at `86/89`, owner/emitter [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md).
- Added the formal no-entry raw-cleanup marker. It preserves the exact `0x004a06e0-0x004a0715` half-open range, no IDA function status, zero entry-route/xref proof, vtable stores, `m_resultCallback` release, tail jump to alert/DialogPane teardown, and rejection of standalone helper, direct destructor-body emission from this page, dead padding, `ModelessDialogPane`, direct `AlertPane`, `FunctionObjects`, Boost/TransferReply, and no-owner alternatives.

## Cross References

- [UID:00012X][0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland](by-memory/0x004a0690-0x004a0752.VersatileAlertPaneMethodIsland.md)
- [UID:000130][0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland](by-memory/0x004a0cb0-0x004a0d37.VersatileAlertPaneDeletingDestructorAbiIsland.md)
- [UID:0001Z0][VersatileAlertPaneVtables](by-type/by-vtable/VersatileAlertPaneVtables.md)
- [UID:0000FU][VersatileAlertPane](by-class/VersatileAlertPane.md)
- [UID:0000HE][AlertPanes](by-file/AlertPanes.md)

## Changes

- 2026-07-14 B002 phase-1 UID-preserving rename and source resolution:
  - Before: `VersatileAlertPaneRawCleanup`, `86/89`, blank comment-only formal C++, and a current no-code policy because no entry route survived.
  - Changed to: `VersatileAlertPaneDestructor`, `94/96`, exact ordinary virtual destructor formal body, retained owner/emitter UID0000FU, blank position, and `Nested:0`.
  - Evidence: unique exact body/hash, corrected half-open range, three derived vtable resets, owning `m_resultCallback` deletion, DialogPane base tail, live scalar-wrapper mirror, class/vtable/RTTI/size requirements, current FunctionObject declaration, and exhaustive no-entry scans.
  - Historicalized the standalone raw-cleanup/no-code conclusion. No-entry and linker-retention uncertainty remain recorded as provenance, while handwritten vptr/base/scalar/thunk behavior remains rejected compiler output.
