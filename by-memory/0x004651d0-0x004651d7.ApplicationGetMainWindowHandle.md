*** UID:0002GN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
HWND Application::GetMainWindowHandle() const
{
    return m_hMainWnd;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready exact Application `HWND` getter now emits `HWND Application::GetMainWindowHandle() const { return m_hMainWnd; }`; MCP session `3fa0535f` reconfirmed exact 0x7 body bytes `8b 81 28 08 00 00 c3`, `mov eax,[ecx+0x828]; retn`, one block, no callees, 78 code refs, and `0xcc` padding before/after. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004651d0-0x004651d7 Application GetMainWindowHandle

## Scope

- Range: `0x004651d0-0x004651d7`
- Entity kind: class method
- Owner: [UID:00000D][Application](by-class/Application.md)
- Parent range: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)
- Disposition: reconstructable project code.

## Behavior

`Application::GetMainWindowHandle` is source-ready first-draft C++ as `HWND Application::GetMainWindowHandle() const`. It returns the main client window handle stored in the Application object at offset `0x828`, source-facing `m_hMainWnd`.

## IDA Evidence

- IDA MCP `lookup_funcs` on 2026-06-05 reports a defined function at `0x004651d0` with size `0x7`, giving the exclusive range end `0x004651d7`.
- IDA MCP `decompile` returns dword index `522`; `522 * 4 == 0x828`.
- IDA MCP `xrefs_to` on 2026-06-05 reports broad direct use across fatal-error, browser, input, main UI, ScreenPane, worker, and network/display paths. Representative documented consumers include [UID:0002VI][0x00465cb0-0x00465ce0.FatalErrorDialogAndBreak](by-memory/0x00465cb0-0x00465ce0.FatalErrorDialogAndBreak.md), BrowserThread host setup, InputMan/IME completion paths, and ScreenPane message handling.
- B005's 2026-06-23 [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md) implementation identifies this getter as the `HWND` accessor used by `ScreenPane::RestoreDirectDrawState()` before `IDirectDraw::SetCooperativeLevel(hwnd, DDSCL_NORMAL)`. The helper is a consumer of the Application main-window handle, not an Application-owned method body.
- Neighboring padding is already recorded in [UID:0000VN][-ignored](by-memory/-ignored.md): `0x004651c4-0x004651d0` before this getter and `0x004651d7-0x004651e0` before the running-state getter.

## 2026-06-29 B006 Current MCP Source-Ready Evidence

B006 implementation incorporates the accepted report `tools/leaser/Agents/Agent-B006/research/0002GN-ApplicationGetMainWindowHandle-empty-emitter-source-quality.md`. The report used current MCP session `3fa0535f`; `idb_list` showed active worker session `3fa0535f` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, and `server_health(database=3fa0535f)` returned `status:"ok"`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.

Current-session facts:

| Evidence | Result |
| --- | --- |
| `lookup_funcs` | `0x004651d0` is `sub_4651D0`, size `0x7`; `0x004651d7` is not a function; `0x004651e0` starts sibling `sub_4651E0`, size `0x7`. |
| raw bytes | Region `0x004651c4` size `28` is twelve `0xcc` bytes, target bytes `8b 81 28 08 00 00 c3`, then nine `0xcc` bytes. |
| disassembly/decompile | `mov eax, [ecx+828h]; retn`; Hex-Rays returns `int __thiscall sub_4651D0(_DWORD *this) { return this[522]; }`. |
| structure | one basic block, cyclomatic complexity `1`, no strings, no constants, no callees. |
| xrefs | `xrefs_to(0x004651d0, limit=1000)` returns `xref_count:78`, `more:false`, all code refs. Representative callers include `sub_465CB0`, `sub_46B520`, `sub_46BA90`, `sub_46F010`, `sub_470A00`, `_WinMain@16`, `sub_557470`, `sub_5576A0`, `sub_558840`, `sub_57B490`, and `sub_5917D0`. |
| offset conversion | MCP `int_convert` verifies `0x828` as decimal `2088`, byte form `28 08`; `522` as `0x20a`; and `0x7` as decimal `7`. The decompiler's `this[522]` is dword indexing, so `522 * 4 == 2088 == 0x828`. |

Field provenance:

- `Application::Initialize` / `0x004639d0` creates the main window and stores it at `Application+0x828`: decompile shows `v24 = (HWND)unk_69BE44(...)` at `0x464387`, `*(_DWORD *)(v15 + 2088) = v24` at `0x464393`, then `GetClientRect(v24, &rc)` and `ClientToScreen(*(HWND *)(v15 + 2088), &Point)`. The alternate window-mode branch stores the same offset at `0x46444c`.
- `Application::Startup` / `0x004f5fb0` consumes the field as a window handle through `ShowWindow(this[522], 5)`, `UpdateWindow(this[522])`, and `SetForegroundWindow(this[522])` at `0x4f61d6-0x4f61ee`.
- `Application::CleanupResources` / `0x00464a60` consumes the same field as `HWND` through `ShowWindow(*(HWND *)(this + 2088), 0)` at `0x464b5c`.

This resolves the old blank-C++ rationale. `m_hMainWnd` is inferred rather than original-symbol proof, but it is now the established source-facing field spelling in Application startup/cleanup/BaramApp/ScreenPane support. The method is read-only, has no callees or side effects, and matches neighboring Application getter style, so `const` is appropriate for first-draft source.

## 2026-06-06 Live IDA Boundary And Byte Evidence

Live IDA MCP against the current `NexusTK.exe` reconfirms the exact accessor shape:

| Evidence | Result |
| --- | --- |
| `lookup_funcs` | `0x004651d0` is `sub_4651D0`, size `0x7`; `0x004651c4` and `0x004651d7` are not function starts; `0x004651e0` starts the next modeled accessor. |
| raw bytes | `0x004651d0-0x004651d7` is `8b 81 28 08 00 00 c3`. |
| disassembly | `mov eax, [ecx+828h]; retn`. |
| decompile | `int __thiscall sub_4651D0(_DWORD *this) { return this[522]; }`. |
| callees | none. |
| entry xrefs/callers | 78 code refs and no data refs. Representative caller sites include `0x00465cc4`, `0x0046b770`, `0x0046bb87`, `0x0046f0bd`, `0x0046f140`, `0x00470b89`, `0x004901b2`, and `0x0049104b`. |
| preceding bytes | `0x004651c4-0x004651d0` is twelve `0xcc` bytes after [UID:0002GM][0x004651c0-0x004651c4.ApplicationGetInstanceHandle](by-memory/0x004651c0-0x004651c4.ApplicationGetInstanceHandle.md). |
| following bytes | `0x004651d7-0x004651e0` is nine `0xcc` bytes before [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md). |

The 78 caller refs are all code refs, matching this getter's role as the central `HWND` access point for fatal-error dialogs, browser hosting, window/input routing, UI panes, ScreenPane paths, and startup/window-management code.

## Owner / Emitter / Source Placement

Keep `CANONICAL_OWNER:00000D`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:00000D`. The owner is Application because the body is a `thiscall` Application field read, Application initialization writes `Application+0x828`, and Application lifecycle/startup/cleanup paths consume the same member. The emitter route through [UID:00000D][Application](by-class/Application.md) reaches [UID:0000HG][Application](by-file/Application.md) / `NexusTK/app/Application.cpp`; the former empty marker was not an owner or route failure.

BaramApp, ScreenPane, WinMain, browser/input/UI, DirectX/Surface, and fatal-error paths are consumers of the returned handle, not owners of the field or method. A global/free helper form is rejected because the binary body is `__thiscall` and reads `ecx+0x828`; source placement belongs in `app/Application.cpp` as `Application::GetMainWindowHandle()`.

## Rejected Alternatives

- No split/range repair: current MCP proves an exact 7-byte function with `0xcc` padding before and after.
- No target-specific no-code marker: this is ordinary source-level Application member code, not compiler support, data, a thunk, mixed range, or third-party code.
- No owner/emitter change: current metadata already routes to the correct Application source file.
- No global/free helper ownership: all hard evidence reads through `this`, and accepted source style uses an Application class method.
- No `95/95` blank-code gate: current `by-structure.md` uses `RECONSTRUCTABLE:TRUE`, a valid nonblank emitter route, and combined score greater than `85` as the C++ eligibility floor. `95+` remains a rare final-audit score, not a first-draft C++ requirement.

## Score Rationale

Completion is now `88` because the page records exact modeled bounds, current MCP session evidence, raw bytes, disassembly, decompilation, one-block/no-callee state, caller count with representative refs, adjacent padding byte counts, int-convert offset proof, field writer/consumer provenance, parent/source placement, owner/emitter route, formal first-draft C++, and rejected alternatives. It remains below final-audit range because the complete `Application` layout and full 78-caller semantic classification are not exhaustively written here.

Confidence is now `93` because the local getter behavior, boundaries, return offset, `HWND` type, field name, owner, emitter route, and formal C++ are directly supported by current MCP plus accepted Application support docs. It remains below `95` until a broader `Application` layout audit and all caller families are fully mapped.

## Reconstruction Notes

- Keep this as an exact child method under `Application`.
- The source-level return type is `HWND`; formal first-draft code uses `HWND Application::GetMainWindowHandle() const`.
- The accepted source-facing field name is `m_hMainWnd`. `m_mainWindowHandle` may remain a descriptive prose alias, but emitted source should use the established `m_hMainWnd` spelling.
- The old `95/95` blank-code gate is historical and superseded. The remaining exact original spelling/header-exposure uncertainty only caps the score below `95+`; it does not block the getter body.

## Cross-References

- File: [UID:0000HG][Application](by-file/Application.md)
- Class: [UID:00000D][Application](by-class/Application.md)
- Parent aggregate: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)

## Changes

- 2026-06-29 B006 GetMainWindowHandle empty-emitter implementation:
  - Changed score from `86/92` to `88/93`; kept `CANONICAL_OWNER:00000D`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00000D`, and blank emitter position.
  - Inserted formal first-draft C++: `HWND Application::GetMainWindowHandle() const { return m_hMainWnd; }`.
  - Superseded the stale `95/95` blank-code gate. Current by-structure code-entry rules allow first-draft C++ for reconstructable items with a valid emitter route and combined score above `85`; `95+` remains a rare final-audit score, not a getter-code prerequisite.
  - Summary/evidence: accepted B006 report `tools/leaser/Agents/Agent-B006/research/0002GN-ApplicationGetMainWindowHandle-empty-emitter-source-quality.md`, MCP session `3fa0535f`, health ok, exact `0x004651d0-0x004651d7` range, bytes `8b 81 28 08 00 00 c3`, twelve `0xcc` bytes before and nine after, `mov eax,[ecx+828h]; retn`, Hex-Rays `return this[522]`, `int_convert` proof that `this[522]` maps to `Application+0x828` / decimal `2088`, one block, no callees, and 78 code refs with `more:false`.
  - Field/source-placement evidence: `Application::Initialize` stores the created main `HWND` into `Application+0x828`, `Application::Startup` shows/updates/foregrounds the same field, and `Application::CleanupResources` hides it. BaramApp, ScreenPane, WinMain, browser/input/UI, DirectX/Surface, and fatal-error paths are consumers only; a global/free helper and owner/emitter moves are rejected.

- 2026-06-23 B005 SurfaceDirectDrawRestoreMaintenance dependency sync:
  - Score unchanged at `86/92`.
  - Replaced stale Surface-owned wording for [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md) with the accepted ScreenPane route. This getter remains an Application-owned dependency used by `ScreenPane::RestoreDirectDrawState()` before `SetCooperativeLevel`.

- 2026-06-20 B002 SurfaceDirectDrawRestoreMaintenance dependency sync:
  - Score unchanged at `86/92`.
  - Superseded by the 2026-06-23 B005 reroute above. This entry originally recorded [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md) as a Surface-owned consumer of `Application::GetMainWindowHandle`.
- 2026-06-05 live caller refresh:
  - What existed before: the page emitted a final-looking `HWND` getter body and only recorded the basic boundary/offset evidence.
  - What changed: cleared the C++ block, raised completion to `78`, and added current caller-family and neighboring-padding evidence.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `decompile`, and `xrefs_to` on 2026-06-05 reconfirmed the exact `0x004651d0-0x004651d7` body, Application offset `0x828`, and broad direct use by window/input/UI paths.
- 2026-06-06 A009 boundary/byte pass:
  - What existed before: score `78/90` and caller-family evidence, but the page lacked raw bytes, exact padding byte counts, no-callee proof, and a written caller count.
  - Changed to: score `86/92`; C++ remains blank below the final-source gate.
  - Summary/evidence: live IDA MCP reconfirmed exact `0x004651d0-0x004651d7` range, raw bytes `8b 81 28 08 00 00 c3`, `mov eax,[ecx+0x828]; retn`, no callees, 78 code refs and no data refs, twelve `0xcc` bytes before the getter, and nine `0xcc` bytes before the running-state getter.

- 2026-05-31 initial split:
  - What existed before: this method was only implied by a small-helper bullet inside the broad Application lifecycle aggregate.
  - What it was changed to: created this exact child method page and attached its range-local C++ snippet under `Application`.
  - Summary/evidence: IDA MCP `lookup_funcs` and `decompile` confirm the `0x004651d0` boundary and Application main-window-handle return.
