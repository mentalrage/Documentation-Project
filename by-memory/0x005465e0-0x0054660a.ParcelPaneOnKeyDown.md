*** UID:0002KD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000A6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000A6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ParcelPane::OnKeyDown(const PaneKeyEvent *event)
{
    if (event->eventKind == kPaneKeyDown)
        g_pEventMan->TranslateEventKey(event->payload[0], event->keyState);

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: MCP-backed ParcelPane secondary key-down virtual at `0x00621c98`; formal `ParcelPane::OnKeyDown(const PaneKeyEvent *event)` checks key-down type `8`, calls `g_pEventMan->TranslateEventKey` with event bytes `+0x08/+0x10a`, ignores the returned byte, and returns false for every path; `PaneKeyEvent` field names and exact helper spelling remain descriptive project names. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005465e0-0x0054660a ParcelPaneOnKeyDown

## B001 UID0002ZO Accepted Callback - 2026-07-21

- Exact `0x2a` secondary-vtable callback is reached only through slot `0x00621c98`; absence of direct callers is expected virtual dispatch, not dead code.
- It tests event kind `8`, forwards key byte `+0x08` and modifier/state byte `+0x10a` to `g_pEventMan->TranslateEventKey`, ignores that result, and always returns false.
- It accesses no ParcelPane fields. `PaneKeyEvent` and helper names are descriptive source-facing inferences tied to producer offsets, not recovered symbols.
- CRT/locale interpretations and consume-true behavior are rejected. Score `91/93` and position 50 reflect complete behavior with limited lexical uncertainty.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method.
- Parent class: [UID:0000A6][ParcelPane](by-class/ParcelPane.md).
- Aggregate range: [UID:0001EH][0x00545e40-0x005470ad.ParcelNotificationPanes](by-memory/0x00545e40-0x005470ad.ParcelNotificationPanes.md).
- Likely source module: [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
- Confidence: very strong for boundary, parent/source ownership, secondary-vtable slot placement, exact event-byte behavior, and the source-ready `ParcelPane::OnKeyDown(const PaneKeyEvent *event)` method shape; medium-high for the exact original `PaneKeyEvent` field names and EventMan helper spelling, which remain accepted descriptive project names.

## Autogen Status

- Reconstructable: true. This is a source-owned ParcelPane input callback.
- Autogen parent: [UID:0000A6][ParcelPane](by-class/ParcelPane.md), retained from existing metadata.
- Reconstruction status: first-draft formal C++ is emitted through [UID:0000A6][ParcelPane](by-class/ParcelPane.md) / [UID:0000MF][ParcelPane](by-file/ParcelPane.md). The source body intentionally preserves the compiled key-normalization side-effect call and always-false result.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 91 | Exact range, vtable route, event offsets, helper side effect, always-false behavior, owner, position, rejected alternatives, and formal C++ are complete. |
| Confidence | 93 | Binary behavior is exact; inferred event/helper spellings cap the result below 94. |

## Behavior

Small secondary-vtable key-down callback. The body takes a single pane key-event pointer, not an owner pointer, and does not read or write `ParcelPane` fields. When event byte `+0x04` equals `8` (`kEventKeyDown` / source-facing `kPaneKeyDown`), it passes event bytes `+0x08` and `+0x10a` through `g_pEventMan->TranslateEventKey`, ignores the returned byte, and returns false. Non-matching events also return false, so this callback preserves a key-normalization side-effect without consuming the key.

## IDA MCP Evidence

- 2026-06-27 B011 MCP session `398b87c1` and the earlier 2026-06-15 pass both report `sub_5465E0` at `0x005465e0` with size `0x2a` / 42 bytes (verified with `int_convert.py`); `0x0054660a` is not a function start.
- 2026-06-27 B011 MCP decompilation shows a short body that checks `event[4] == 8`, calls the IDA-mislabeled `std::ctype<char>::do_narrow(event[8], event[0x10a])`, ignores the return value, and returns `0`. The `__stdcall` shape is an artifact of the hidden member `this` value in `ecx` being unused; the vtable slot and source route still identify a `ParcelPane` member override.
- Disassembly confirms the exact operand offsets: `cmp byte ptr [ecx+4], 8`, `movzx eax, byte ptr [ecx+10Ah]`, `movzx eax, byte ptr [ecx+8]`, `mov ecx, dword_67A754`, call `0x004a8b10`, `xor al, al`, and `retn 4`.
- 2026-06-27 B011 MCP `xrefs_to 0x005465e0` reports only the `.rdata` secondary-vtable slot at `0x00621c98`; there are no ordinary code callers, which is expected for this virtual callback.
- 2026-06-27 B011 MCP `callees 0x005465e0` reports only `0x004a8b10`, the IDA-mislabeled `std::ctype<char>::do_narrow` helper. Its disassembly is the two-argument adaptor body `mov al, [ebp+arg_0]; retn 8`, so it returns the first key byte and ignores the second argument.
- [UID:00014C][0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers](by-memory/0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers.md) and [UID:00004O][EventMan](by-class/EventMan.md) reject the CRT/locale interpretation and identify `0x004a8b10` as `EventMan::TranslateEventKey` / `TranslateEventKeyByte`.
- [UID:00028L][0x0067a754-0x0067a758.g_pEventMan](by-memory/0x0067a754-0x0067a758.g_pEventMan.md) identifies `dword_67A754` as the process-wide `EventMan *` singleton `g_pEventMan`.
- B011 rechecked EventMan key-down producer `0x004a96a0`: it writes type byte `8` at event offset `+0x04`, translated/narrowed key byte at `+0x08`, modifier/state byte at `+0x10a` / 266 (verified with `int_convert.py`), and dispatches the event. The paired key-up path at `0x004a98a0` writes type `9` with the same key/modifier payload layout, confirming type `8` as key-down.
- Vtable documentation places this as the ParcelPane secondary-view input callback: [UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md) records `0x00621c98 -> 0x005465e0`, and [UID:0001YE][ParcelNotificationVtableFamily](by-type/by-vtable/ParcelNotificationVtableFamily.md) records the ParcelPane secondary `+0x08` slot.
- [UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md) records `0x00621c98 -> 0x005465e0` as source-ready `ParcelPane::OnKeyDown(const PaneKeyEvent *)` in the secondary handler view.
- [UID:0001YE][ParcelNotificationVtableFamily](by-type/by-vtable/ParcelNotificationVtableFamily.md) places the same target at secondary slot `+0x08`, adjacent to [UID:0002KE][0x00546610-0x00546807.ParcelPaneOnMouseEvent](by-memory/0x00546610-0x00546807.ParcelPaneOnMouseEvent.md) at slot `+0x04`.
- [UID:0001VI][ParcelNotificationPaneLayouts](by-type/by-struct/ParcelNotificationPaneLayouts.md) documents the secondary callback context for neighboring methods. This specific body does not use a secondary `this` pointer; its sole stack argument behaves as the event payload.
- Current generated projections, local filename, ParcelPane class docs, key-down event type, and accepted GroupListPane precedent support `ParcelPane::OnKeyDown` as the source-facing method name. It is source-ready as a no-consume key-down filter; `OnKeyEvent` remains a weaker rejected generic spelling for this exact secondary `+0x08` key-down slot.

## Source-Quality Resolution

- Event structure: use `PaneKeyEvent` for the pane-facing callback parameter. The accepted field names in first-draft C++ are `eventKind`, `payload[0]`, and `keyState`; they are descriptive project names derived from EventMan producer offsets rather than recovered original symbols.
- Helper/global naming: replace stale `dword_67A754` / `std::ctype<char>::do_narrow` wording with `g_pEventMan` and `EventMan::TranslateEventKey`. Raw IDA labels remain search aliases only.
- Source method name: use `ParcelPane::OnKeyDown(const PaneKeyEvent *event)`. The slot is a secondary key-down dispatch slot at `0x00621c98`, adjacent to `OnMouseEvent` at `0x00621c94`.
- Return behavior: the method always returns `false`; the helper return is not consumed and no ParcelPane fields are mutated.
- Rejected alternatives: CRT/locale `std::ctype` identity, treating `dword_67A754` as a locale facet, generic `OnKeyEvent` naming for this slot, dead/no-code disposition, and consumed-key return semantics.

## Reconstruction Notes

This page now emits a first-draft source body because the source owner, vtable slot, event offsets, EventMan helper/global interpretation, and always-false behavior are settled. Keep the remaining uncertainty localized to exact original field/helper spelling; do not regress the formal C++ back to raw `dword_67A754` or `std::ctype<char>::do_narrow` wording.

## Cross-References

- [UID:0000A6][ParcelPane](by-class/ParcelPane.md)
- [UID:0001YE][ParcelNotificationVtableFamily](by-type/by-vtable/ParcelNotificationVtableFamily.md)
- [UID:0001VI][ParcelNotificationPaneLayouts](by-type/by-struct/ParcelNotificationPaneLayouts.md)
- [UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md)
- [UID:0002KE][0x00546610-0x00546807.ParcelPaneOnMouseEvent](by-memory/0x00546610-0x00546807.ParcelPaneOnMouseEvent.md)

## Changes

- 2026-06-27 B011 accepted ParcelPaneOnKeyDown implementation sync:
  - Before: score was `86/90`, formal C++ was blank, and the page still used stale `dword_67A754` / `std::ctype<char>::do_narrow` wording.
  - Changed to: score is `90/92`; owner/emitter stay [UID:0000A6][ParcelPane](by-class/ParcelPane.md); formal C++ now emits `bool ParcelPane::OnKeyDown(const PaneKeyEvent *event)`.
  - Summary/evidence: B011 MCP session `398b87c1` reconfirmed exact size `0x2a` / 42, exclusive end `0x0054660a`, vtable-only xref `0x00621c98`, EventMan key-down producer offsets `+0x04/+0x08/+0x10a`, `g_pEventMan` global ownership, `0x004a8b10` as the accepted EventMan key-translation adaptor, ignored helper return, and always-false/no-consume behavior. Remaining field/helper spellings are inferred descriptive project names, not blockers for target-level first-draft C++.
- 2026-06-15 A003 Goal2 parcel input refresh:
  - Before: score was `80/86`.
  - Changed to: score is `86/90`, with the existing [UID:0000A6][ParcelPane](by-class/ParcelPane.md) owner/emitter route retained.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x2a` function boundary, exclusive-end non-function, sole vtable data xref at `0x00621c98`, one-callee body to `0x004a8b10`, and decompiled event-byte gate/always-false return. Final C++ remains blank because the source-facing event type and method name remain provisional.
- 2026-06-02 score rationale update:
  - What existed before: score was `58/70` despite reconstructable metadata and parent attachment.
  - Changed to: score is `70/76`, with explicit autogen status, rationale, and a clearer reconstruction caveat.
  - Summary/evidence: the page already records exact IDA function enumeration, short decompiled shape, and vtable-family placement; confidence remains below 80 because the event structure and intended key behavior still need review.
- 2026-06-03 vtable-slot confidence update:
  - What existed before: score was `70/76` and the page did not link the exact parcel vtable-data row or layout-offset evidence.
  - Changed to: score is `74/80`, still with no reconstructed C++.
  - Summary/evidence: the exact `ParcelPane` secondary key-event slot is documented in [UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md), the vtable-family page identifies the slot as secondary `+0x08`, and the layout page records secondary callback offset normalization. Confidence is still capped because the key event payload fields and generated body typing remain unresolved.
- 2026-06-06 key-filter behavior update:
  - What existed before: score was `74/80` and the page still suggested a possible `ParcelPane` owner-field touch.
  - Changed to: score is `80/86`, still with no reconstructed C++.
  - Summary/evidence: live IDA MCP confirms the body takes a single event pointer, has only the vtable-data xref at `0x00621c98`, checks `event[4] == 8`, feeds `event[8]` and `event[0x10a]` through the shared `dword_67A754`/`0x004a8b10` key-normalization stub, ignores the result, and always returns false.
