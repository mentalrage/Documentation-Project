*** UID:0002SF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000034 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000034 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// [UID:0002SF] by-memory/0x0047e510-0x0047e694.ConfirmDeleteMailAlertSendDeleteRequest.md
void ConfirmDeleteMailAlert::SendDeleteRequest()
{
    MailDialog *mailContext = static_cast<MailDialog *>(m_layoutReference);

    if (m_multiDelete) {
        MailListPane *mailList = mailContext->GetReplyListPane();
        int selectedIndices[256];
        int selectedCount = mailList->GetSelectedIndices(selectedIndices, 256);

        for (int i = 0; i < selectedCount; ++i) {
            const MailListEntryRow *row = mailList->GetItemAt(selectedIndices[i]);

            unsigned char packet[7];
            PacketBufferWriteUInt8(0x3b, packet + 0);
            PacketBufferWriteUInt8(0x05, packet + 1);
            PacketBufferWriteUInt16BE(mailList->GetMailType(), packet + 2);
            PacketBufferWriteUInt16BE(row->mailId, packet + 4);
            packet[6] = 0;
            g_packetSender->QueueAndSendPacket(packet, 6);
        }
    } else {
        const MailListEntryRow *row = mailContext->GetCurrentMailEntry();

        unsigned char packet[7];
        PacketBufferWriteUInt8(0x3b, packet + 0);
        PacketBufferWriteUInt8(0x05, packet + 1);
        PacketBufferWriteUInt16BE(row->mailType, packet + 2);
        PacketBufferWriteUInt16BE(mailContext->GetCurrentMailId(), packet + 4);
        packet[6] = 0;
        g_packetSender->QueueAndSendPacket(packet, 6);
    }

    if (g_pUserPane != 0) {
        g_pUserPane->m_activeDialogHostPane = mailContext;
    }

    CreateScreenDimmer();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Mail delete-confirm action; live IDA MCP on 2026-06-03 confirms exact function boundary, vtable slot `0x00614724`, single/multi delete packet paths, opcode `0x3b`, subcommand `0x05`, socket send length `6`, and successor padding at `0x0047e694-0x0047e6a0`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047e510-0x0047e694 ConfirmDeleteMailAlert Send Delete Request

## Status

- Address range: `0x0047e510-0x0047e694` (IDA function end-exclusive; final `retn` is at `0x0047e693`).
- Entity kind: modeled alert action / packet sender.
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored method body.
- Likely owner: [UID:000034][ConfirmDeleteMailAlert](by-class/ConfirmDeleteMailAlert.md), with file-level ownership through [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md) or private [UID:0000KZ][MailDialogs](by-file/MailDialogs.md) placement.
- Current evidence status: C001 Wave2 plus live IDA MCP decompile, call graph, vtable-xref, and boundary checks were refreshed on 2026-06-15.

## Behavior

This confirmed action sends mail delete requests after the user accepts the delete confirmation alert.

The method reads the mail context pointer from `this+0x26c` and the multi-delete flag from `this+0x270`. When multi-delete is enabled, it resolves the selected-row list through the mail context provider at `context+0x1fc`, virtual slot `+0x10`, node id `7`, then enumerates up to 256 selected entries with `sub_4F3E80` and sends one delete packet per row. When multi-delete is not enabled, it serializes one packet from the active selected mail pointer at `context+0x270` and the stored reply/delete id at `context+0x274`.

Each packet uses opcode `0x3b`, subcommand `0x05`, two 16-bit ids, and raw `sub_574BB0(dword_67A7EC, packet, 6)`, source-facing `g_packetSender->QueueAndSendPacket(packet, 6)`. After packet serialization, the action stores the mail context into `dword_67A748+0x214` / `g_pUserPane->m_activeDialogHostPane` when the player-data global is present, then calls raw `sub_4A12B0`, now source-faced as `CreateScreenDimmer`.

## Packet And State Map

| Item | Evidence-backed role | Remaining uncertainty |
| --- | --- | --- |
| Opcode `0x3b` | Bulletin/mail packet family. | Exact symbolic enum name remains open. |
| Subcommand `0x05` | Delete request subcommand shared with the board/article delete-confirm path. | Shared delete subcommand naming should be coordinated with board/article docs. |
| `this+0x26c` | Mail-context pointer used by both branches. | Source-quality field name still needs the constructor/layout pass. |
| `this+0x270` | Multi-delete flag on the alert object. | Source-quality field name still needs the constructor/layout pass. |
| `context+0x1fc` | Multi-delete provider; virtual slot `+0x10` is called with node id `7`. | Provider class and method name remain unresolved. |
| provider result `+0x108` | Dereferenced list object used for selected-row enumeration. | Exact owner type is still open. |
| list object `+0x14c` | First 16-bit payload id in the multi-delete packets. | Likely mailbox/thread id, but symbolic meaning is not final. |
| `sub_4F3DC0(list object)` | Supplies the current selected-row object; its first word is the second payload id. | Iterator/list API names remain unresolved. |
| `context+0x270` / selected pointer `+0x102` | First 16-bit payload id in the single-delete packet. | The selected pointer's concrete type is still open. |
| `context+0x274` | Second 16-bit payload id in the single-delete packet. | Exact source field name remains open. |
| `g_packetSender` / raw `dword_67A7EC` | Socket/packet transport global passed to `sub_574BB0` with length `6`, source-facing `QueueAndSendPacket(packet, 6)`. | Accepted source-facing global supersedes raw alias outside evidence prose. |
| `g_pUserPane->m_activeDialogHostPane` / raw `dword_67A748+0x214` | Active player mail context slot updated before the dimmer factory call. | Field role is source-quality first-draft; exact original spelling remains a confidence cap. |

## Evidence

- Live IDA MCP `decompile` on 2026-06-03 confirms the branch on `*(this+0x270)`, the mail context load from `this+0x26c`, the multi-delete selected-row enumeration, and the single-delete packet path.
- Live IDA MCP function metadata on 2026-06-03 reports `sub_47E510` as `0x0047e510-0x0047e694`; `0x0047e694` is outside any function, and `0x0047e6a0` begins the following raw constructor area.
- Live IDA MCP call graph on 2026-06-03 records calls to the indirect provider slot at `0x0047e549`, selected-list helpers `sub_4F3E80`/`sub_4F3DC0`, packet writers `sub_575380`/`sub_5753A0`, sender `sub_574BB0`, raw helper `sub_4A12B0` / source-facing `CreateScreenDimmer`, and the security-cookie check.
- Live IDA MCP vtable-xref review on 2026-06-03 confirms the function pointer at `0x00614724` inside the `ConfirmDeleteMailAlert` primary vtable.
- 2026-06-15 C001 Wave2 method-analysis reports effective signature `int ConfirmDeleteMailAlert::SendDeleteRequest()`, grade `100`, vtable data xref `0x00614724`, and the documented mail-delete flow with multi-delete flag at `this+0x270`, context at `this+0x26c`, opcode/subopcode `0x3b/0x05`, selected-entry helper `0x004f3dc0`, packet send, player mail-context update, and bulletin queue processing.
- 2026-06-15 live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirmed `sub_47E510` at `0x0047e510-0x0047e694`, one data xref from `0x00614724`, callees `sub_4F3E80`, `sub_4F3DC0`, `sub_575380`, `sub_5753A0`, `sub_574BB0`, `sub_4A12B0`, and `@__security_check_cookie@4`, and `0x0047e694-0x0047e6a0` as `0xcc` padding before the following raw constructor area.
- 2026-06-15 live bytes and pointer scan found the vtable pointer hit for `0x0047e510` at `0x00614724` and no contradictory code/data reachability. Current Hex-Rays prototype recovery for `sub_4F3E80` omits one explicit list-context argument, but Wave2/disassembly still show the selected-list object carried in `ECX` before enumeration.
- [UID:0000ZM][0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions](by-memory/0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions.md) records prior IDA MCP `lookup_funcs` confirmation for real function start `0x0047e510`.
- [UID:000034][ConfirmDeleteMailAlert](by-class/ConfirmDeleteMailAlert.md) records this exact action as the confirmed `SendDeleteRequest` method and summarizes the single/multi delete packet behavior.
- [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md) lists `0x0047e510-0x0047e694` as the mail delete confirmation action in the shared alert wrapper family.
- [UID:0001Y2][MailDialogVtableFamily](by-type/by-vtable/MailDialogVtableFamily.md) records `ConfirmDeleteMailAlert` vtable bases and constructor/vtable-store xrefs in the mail dialog family.
- [UID:0000KZ][MailDialogs](by-file/MailDialogs.md) and [UID:00007K][MailDialog](by-class/MailDialog.md) are the current mail-context ownership references.

## Boundary Notes

- Start `0x0047e510` is IDA-modeled and has a vtable data xref from `0x00614724`.
- End-exclusive `0x0047e694` is confirmed by live IDA: the final instruction is `retn` at `0x0047e693`, followed by `0xCC` alignment padding at `0x0047e694-0x0047e6a0`.
- The following raw constructor area starts at `0x0047e6a0`, while the next IDA-modeled function starts at `0x0047e6f0`. This page intentionally excludes the padding and constructor area.

## Autogen Status

- Reconstructable: true.
- Parent: [UID:000034][ConfirmDeleteMailAlert](by-class/ConfirmDeleteMailAlert.md).
- C++: intentionally blank. The packet behavior and boundary are documented, but field names, helper prototypes, and constructor/class layout are not final-source quality.

## Open Questions

- Recover source-quality names for the mail context fields, selected-list provider, and packet helper prototypes.
- Confirm the projected `ConfirmDeleteMailAlert` constructor boundary and how it initializes `this+0x26c`/`this+0x270`.
- Decide whether final source places this class in `BulletinReplyAlerts.cpp`, `MailDialogs.cpp`, or a compact `BulletinDialogs.cpp`.

## B006 2026-06-29 Implementation Callback

- Accepted C05: this row now emits first-draft source C++ for `ConfirmDeleteMailAlert::SendDeleteRequest`. Current MCP evidence verifies range `0x0047e510-0x0047e694`, packet opcode `0x3b`, subcommand `0x05`, selected-index stack buffer, single/multi mail delete paths, `g_packetSender` sends, `g_pUserPane` dialog-host write, and final `CreateScreenDimmer`.
- Historicalized alternative: previous raw helper/global aliases and queue-refresh wording are replaced by source-facing helper/global names supported by the current mail dialog/list docs.

## Changes

- 2026-06-15 C001 Wave2/live-IDA refresh:
  - Before: the page was `80/88` with 2026-06-03 live boundary and packet evidence.
  - After: raised to `85/90`, owner/emitter unchanged as [UID:000034][ConfirmDeleteMailAlert](by-class/ConfirmDeleteMailAlert.md), final C++ still blank.
  - Summary/evidence: Wave2 reports `ConfirmDeleteMailAlert::SendDeleteRequest()` at grade `100`; live IDA reconfirms the exact range, vtable xref at `0x00614724`, callee set, packet byte/word writers, player mail-context update, bulletin queue call, and successor padding. C++ remains blank because source-quality context/list field names and helper prototypes still need a layout pass.

- 2026-06-02:
  - Created exact child page split from [UID:0000ZM][0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions](by-memory/0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions.md).
  - Scored as `70/80` and marked reconstructable based on prior IDA MCP function-start evidence, existing class/file packet summaries, and sibling board/article delete-confirm evidence.
  - C++ remains blank because final field names, helper prototypes, and the projected constructor boundary are not source-quality.
- 2026-06-03:
  - Before: the page used the stale `0x0047e510-0x0047e693` half-open range and only cited prior aggregate/class evidence.
  - After: renamed to the IDA-confirmed half-open range `0x0047e510-0x0047e694`, scored as `80/88`, and documented the live decompile, call graph, vtable slot, packet layout, and successor-padding evidence.
  - Historical note: C++ was previously left blank under an old `95/95` threshold and unresolved source-name pass; B006 supersedes that blocker with the formal first-draft body above.
