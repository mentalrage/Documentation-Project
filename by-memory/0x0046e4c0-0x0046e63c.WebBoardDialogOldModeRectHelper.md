*** UID:000211 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000G3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000G3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __stdcall GetWebBoardDialogOldModeRect(Rect *out,
                                                   int selector,
                                                   int mode)
{
    if (out == NULL)
        return;

    switch (mode) {
    case 0:
        switch (selector) {
        case 0: out->Set(162, 134, 862, 634); break;
        case 1: out->Set(318, 468, 381, 492); break;
        case 2: out->Set(11, 42, 689, 460); break;
        case 3: out->Set(0, 0, 0, 0); break;
        default: break;
        }
        break;

    case 4:
        switch (selector) {
        case 0: out->Set(303, 101, 721, 667); break;
        case 1: out->Set(318, 468, 381, 492); break;
        case 2: out->Set(11, 42, 407, 526); break;
        case 3: out->Set(0, 0, 0, 0); break;
        default: break;
        }
        break;

    case 6:
        switch (selector) {
        case 0: out->Set(112, 84, 912, 684); break;
        case 1: out->Set(368, 568, 431, 592); break;
        case 2: out->Set(11, 42, 789, 560); break;
        case 3: out->Set(0, 0, 0, 0); break;
        default: break;
        }
        break;

    default:
        break;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-verified raw helper that returns WebBoardDialogOld mode/selector rectangles; all mode `0`/`4`/`6` selector entries, clear-rect case, and compiler-generated jump tables are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046e4c0-0x0046e63c WebBoardDialogOld Mode Rect Helper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw class helper plus compiler switch tables
- IDA ownership state: raw helper with no IDA function object and no direct xrefs; ownership inferred from exact rectangle constants shared with neighboring WebBoardDialogOld layout code
- Direct owner: [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md), as a static/stack-cleaning old-dialog mode rectangle helper. The helper does not consume `ecx` as `this`, so it should not be described as an ordinary instance `__thiscall` method.
- Source module route: [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md), matching the old fixed-art WebBoard implementation and current `simroot_v2/ui/dialogs/WebBoardDialog.cpp` route.
- Generated-source caveat: current `simroot_v2` names the helper `SetModeControlRect`, but its invalid-mode/invalid-selector default behavior does not exactly match the binary, so B010 uses `GetWebBoardDialogOldModeRect` and preserves no-write defaults.
- Rebuild handling: source-authored helper logic plus compiler/linker-generated switch tables
- Confidence: strong for byte boundaries, behavior, and accepted first-draft source shape; medium for original source name because IDA has no function object or direct xrefs

## Covered Range

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x0046e4c0-0x0046e609` | raw mode/control rectangle helper | Writes one of several hard-coded rectangles through `sub_4B78D0` based on an output-rect pointer, control/slot index, and web-board open mode. Handles modes `0`, `4`, and `6`; case `3` clears the rect to zero. |
| `0x0046e609-0x0046e60c` | internal alignment | Three-byte alignment before jump-table data. |
| `0x0046e60c-0x0046e63c` | switch jump tables | Three four-entry jump tables for the source-level mode/control switch blocks. These bytes should be regenerated from source, not hand-authored as data. |

## IDA MCP Evidence

Checked on 2026-05-30:

- IDA MCP confirms no function object at `0x0046e4b6`, `0x0046e4c0`, `0x0046e609`, `0x0046e60c`, or `0x0046e63c`; the next modeled function remains `sub_46E640` at `0x0046e640-0x0046e8be`.
- `xrefs_to 0x0046e4c0` returns empty, matching the raw-helper/unresolved dispatch state.
- The raw helper begins with `mov ecx, [ebp+8]` and returns without changing output when the output rectangle pointer is null.
- The mode selector is `[ebp+0x10]`. The helper accepts only modes `0`, `4`, and `6`; all other modes fall through to the default `retn 0Ch` path without writing a rectangle.
- The slot/control selector is `[ebp+0x0c]`. Selectors `0`, `1`, `2`, and `3` are valid. Selector `3` jumps to a shared block at `0x0046e5d3` that writes zero to all four rectangle fields.
- Jump table evidence:
  - `0x0046e4f4` indexes table `0x0046e60c` for mode `6`.
  - `0x0046e552` indexes table `0x0046e61c` for mode `4`.
  - `0x0046e5af` indexes table `0x0046e62c` for mode `0`.
- The three table blocks are generated switch tables, not source-authored data:
  - `0x0046e60c`: `0x0046e4fb`, `0x0046e512`, `0x0046e52f`, `0x0046e5d3`
  - `0x0046e61c`: `0x0046e559`, `0x0046e573`, `0x0046e590`, `0x0046e5d3`
  - `0x0046e62c`: `0x0046e5b6`, `0x0046e573`, `0x0046e5f2`, `0x0046e5d3`
- The trailing `0x0046e63c-0x0046e640` bytes are tracked separately as ignored padding in [UID:0000VN][-ignored](by-memory/-ignored.md) and the coverage report.

Checked by B001 on 2026-06-16:

- Live IDA MCP still reports no function object at `0x0046e4c0`, `0x0046e609`, `0x0046e60c`, `0x0046e61c`, `0x0046e62c`, or `0x0046e63c`; the neighboring modeled functions remain `sub_46E2A0` and `sub_46E640`.
- `xrefs_to 0x0046e4c0` is empty. Raw PE rel32/VA/RVA scanning also found no calls, jumps, or pointer constants to the helper entry.
- The only references to `0x0046e60c`, `0x0046e61c`, and `0x0046e62c` are the helper's own switch operands at `0x0046e4f4`, `0x0046e552`, and `0x0046e5af`.
- The first 32 bytes at `0x0046e4c0` are unique in the PE (`SHA1 51b8a523c0fee741a9978d7021af5ea627fa91e2`), supporting a specific retained helper rather than a shared generic rectangle utility.

Checked on 2026-05-27:

- IDA function enumeration over `0x0046e4b6-0x0046e640` finds no modeled function start between `sub_46E2A0` and `sub_46E640`.
- Manual disassembly shows a complete raw helper body beginning at `0x0046e4c0` and returning with `retn 0Ch` through `0x0046e606`.
- The helper accepts an output rectangle pointer at stack argument `+8`, a control/slot selector at `+0x0c`, and a mode/open-type selector at `+0x10`.
- It branches on modes `0`, `4`, and `6`, then dispatches selector values `0..3` through jump tables at `0x0046e60c`, `0x0046e61c`, and `0x0046e62c`.
- For normal cases it calls `sub_4B78D0` with hard-coded rectangle coordinates. Selector case `3` clears the output rect fields to zero.
- No direct xrefs to the raw start `0x0046e4c0` exist in the current IDB, matching the unresolved/raw-helper pattern in nearby WebBoardDialogOld code.

## Rectangle Map

The helper is equivalent to a `GetOldModeRect(Rect *out, int selector, int mode)` lookup. Rectangle coordinates are listed as `(left, top, right, bottom)`, matching the `sub_4B78D0` call convention seen in neighboring IDA decompilation.

| Mode | Selector | Role inferred from neighboring layout code | Rectangle |
| --- | ---: | --- | --- |
| `0` | `0` | main dialog/display rectangle | `(162, 134, 862, 634)` |
| `0` | `1` | close button rectangle | `(318, 468, 381, 492)` |
| `0` | `2` | browser/content rectangle | `(11, 42, 689, 460)` |
| `0` | `3` | clear/none | `(0, 0, 0, 0)` |
| `4` | `0` | main dialog/display rectangle | `(303, 101, 721, 667)` |
| `4` | `1` | close button rectangle | `(318, 468, 381, 492)` |
| `4` | `2` | browser/content rectangle | `(11, 42, 407, 526)` |
| `4` | `3` | clear/none | `(0, 0, 0, 0)` |
| `6` | `0` | main dialog/display rectangle | `(112, 84, 912, 684)` |
| `6` | `1` | close button rectangle | `(368, 568, 431, 592)` |
| `6` | `2` | browser/content rectangle | `(11, 42, 789, 560)` |
| `6` | `3` | clear/none | `(0, 0, 0, 0)` |

Invalid modes, invalid selectors, and null output pointers return without modifying the output rectangle.

## Decompiled Shape

This source-shaped form is reconstructed from IDA disassembly. The final function and selector names remain provisional:

```cpp
void __stdcall GetWebBoardDialogOldModeRect(Rect *out, int selector, int mode)
{
    if (!out)
        return;

    switch (mode) {
    case 0:
        switch (selector) {
        case 0: out->Set(162, 134, 862, 634); break;
        case 1: out->Set(318, 468, 381, 492); break;
        case 2: out->Set(11, 42, 689, 460); break;
        case 3: out->Set(0, 0, 0, 0); break;
        }
        break;

    case 4:
        switch (selector) {
        case 0: out->Set(303, 101, 721, 667); break;
        case 1: out->Set(318, 468, 381, 492); break;
        case 2: out->Set(11, 42, 407, 526); break;
        case 3: out->Set(0, 0, 0, 0); break;
        }
        break;

    case 6:
        switch (selector) {
        case 0: out->Set(112, 84, 912, 684); break;
        case 1: out->Set(368, 568, 431, 592); break;
        case 2: out->Set(11, 42, 789, 560); break;
        case 3: out->Set(0, 0, 0, 0); break;
        }
        break;
    }
}
```

The binary returns with `retn 0Ch`, so the helper is stack-cleaning. It does not use `ecx` as `this`; `ecx` is loaded from the first stack argument and is the output rectangle pointer for `sub_4B78D0`.

## Ownership Decision

Attach this directly to [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md) and keep [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) as the source-file route. The helper is not an ordinary instance method because the binary uses three stack arguments, does not consume `this`, and returns with `retn 0Ch`; however, current `simroot_v2/ui/dialogs/WebBoardDialog.cpp` maps `0x0046e4c0` to `WebBoardDialogOld::SetModeControlRect(Rect *, int, int)` and `RefreshOldLayout` calls that helper. Treat this as a static/stack-cleaning old-dialog helper. Reject RankingDialog ownership as generated-source pollution, reject generic rectangle-helper ownership because the constants are WebBoardOld mode/control layout values, and reject dead/non-emitting treatment because the body is unique, source-shaped, and adjacent to live old WebBoard constructor/layout code.

## Remaining Caveats

- B001 and B008 searched for a route through IDA function objects, direct entry xrefs, rel32 calls/jumps, VA/RVA pointer constants, and local switch-table references. No safe dispatch path is currently proven; this is a resolved negative-route fact, not an uninvestigated blocker.
- The best current source-facing role is `GetWebBoardDialogOldModeRect(Rect *out, int selector, int mode)`, probably as a static or stack-cleaning helper rather than an ordinary instance method. Generated `SetModeControlRect` remains a useful search alias only.
- B010 resolves the implementation shape for this callback as `static void __stdcall GetWebBoardDialogOldModeRect(Rect *out, int selector, int mode)`. Generated `SetModeControlRect` remains rejected because it fails to preserve invalid-mode/invalid-selector no-write behavior, and the final original source spelling remains inferred.

## Cross-References

- [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md)
- [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- [UID:0000ZA][0x0046e2a0-0x0046e4b6.WebBoardDialogOldLayoutRefresh](by-memory/0x0046e2a0-0x0046e4b6.WebBoardDialogOldLayoutRefresh.md)
- [UID:0000ZB][0x0046e640-0x0046e8be.WebBoardDialogOldConstructor](by-memory/0x0046e640-0x0046e8be.WebBoardDialogOldConstructor.md)
- [UID:0000BM][RankingDialog](by-class/RankingDialog.md)

## Changes

- 2026-06-30 B010 empty-emitter implementation callback:
  - Raised score to `89/91` and inserted accepted first-draft `static void __stdcall GetWebBoardDialogOldModeRect(Rect *out, int selector, int mode)` formal C++.
  - Evidence/disposition: no IDA function object or entry route remains documented, but null output, modes `0`/`4`/`6`, selectors `0..3`, zero-rect selector case, and invalid-input no-write behavior are resolved enough for this source-quality helper. Generated `SetModeControlRect` is retained only as a rejected/search alias.

- 2026-06-21 B008 old-class source-quality sync:
  - Reframed the route issue from an open dispatch search to a documented negative-route result after B008 repeated the no-function/no-xref/no-pointer/no-rel32 evidence.
  - Summary/evidence: the helper remains direct class owner/emitter [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md), with preferred descriptive role `GetWebBoardDialogOldModeRect(Rect *out, int selector, int mode)`. B010 supersedes the former marker-only policy by accepting the static `__stdcall` first-draft body while preserving the negative-route fact.

- Before: completion/confidence were `50/65`, the page described the helper generally but did not provide the full selector/mode rectangle map.
- Changed to: completion/confidence `82/87`, owner status now records IDA raw-helper state, and the full mode/selector map plus reconstructed source shape are documented.
- Summary/evidence: IDA MCP raw disassembly and table bytes confirm three switch tables at `0x0046e60c`, `0x0046e61c`, and `0x0046e62c`; the target blocks call `sub_4B78D0` with the exact rectangle constants listed above or clear all four output fields at `0x0046e5d3`.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and attached to [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md).
  - Reasoning: the page is `82/87`, the direct parent is `80/84`, and existing documentation identifies this as source-authored old web-board mode rectangle helper logic.
- 2026-06-16 B001 source-route/liveness recheck:
  - Changed to: completion/confidence `86/89`, direct owner/emitter [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md), final C++ still blank.
  - Summary/evidence: live IDA MCP and raw PE scans reconfirmed the raw no-function/no-entry-xref state, exact body/table split, internal alignment, switch-table-only references, unique helper bytes, and old WebBoard route. Current `simroot_v2/ui/dialogs/WebBoardDialog.cpp` maps the helper to `WebBoardDialogOld::SetModeControlRect`, but the generated default/invalid-selector behavior differs from the binary and the original static/calling-convention declaration remains unresolved, so the page should not receive final reconstruction C++ yet.
