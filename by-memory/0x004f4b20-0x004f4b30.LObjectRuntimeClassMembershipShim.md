*** UID:000375 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool LObject::IsKindOf(const RuntimeClass *runtimeClass) const
{
    return GetRuntimeClass()->IsDerivedFrom(runtimeClass);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-level LObject::IsKindOf runtime-class membership method; raw lowering calls virtual GetRuntimeClass and tail-jumps to RuntimeClass::IsDerivedFrom, with the historical no-function/shim model preserved as compiler-boundary evidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f4b20-0x004f4b30 LObjectRuntimeClassMembershipShim

## Status

- Entity kind: raw runtime-class membership shim.
- Owner: [UID:00007D][LObject](by-class/LObject.md), with the source-facing `IsKindOf` API and RuntimeClass declarations resolved.
- Likely source file: [UID:0000KV][LObject](by-file/LObject.md).
- Address range: `0x004f4b20-0x004f4b30`, inclusive start/exclusive end.
- Rebuild handling: source-authored runtime-class support, emitted as raw code with no current IDA function object.
- Disposition: reconstructable NexusTK code.

## Behavior

This method calls virtual slot `+0x04` on the object in `ecx`, moves the returned runtime-class pointer back into `ecx`, restores `ebp`, and tail-jumps to `RuntimeClass::IsDerivedFrom` at `0x005568e0`. It is the source-facing `LObject::IsKindOf(const RuntimeClass *) const` entry point.

## Raw Code Evidence

2026-06-11 IDA MCP `lookup_funcs` reports no function object at `0x004f4b20`, but the bytes disassemble as a complete standalone shim:

```asm
004f4b20  push ebp
004f4b21  mov  ebp, esp
004f4b23  mov  eax, [ecx]
004f4b25  call dword ptr [eax+4]
004f4b28  mov  ecx, eax
004f4b2a  pop  ebp
004f4b2b  jmp  loc_5568E0
```

The following modeled function begins at `0x004f4b30`, so the shim's exclusive end is `0x004f4b30`.

2026-07-02 B012 current MCP refresh used active database session `supervisor_resume_20260629`; `server_health` returned `status:ok`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`, and strings cache size `2067`.

Current schema `lookup_funcs` reports:

| Query | Result |
| --- | --- |
| `0x004f4b10` | `sub_4F4B10`, size `0x6` |
| `0x004f4b20` | not a function |
| `0x004f4b30` | `sub_4F4B30`, size `0x1c` |
| `0x005568d0` | not a function |
| `0x005568e0` | not a function |
| `0x00556910` | `sub_556910`, size `0x14a` |

Current `get_bytes 0x004f4b10 size 0x30` reconfirms the boundary sequence: accessor bytes `b8 44 cf 61 00 c3` at `0x004f4b10-0x004f4b16`, ten `0xcc` bytes at `0x004f4b16-0x004f4b20`, shim bytes `55 8b ec 8b 01 ff 50 04 8b c8 5d e9 b0 1d 06 00` at `0x004f4b20-0x004f4b30`, and the following function prologue at `0x004f4b30`.

Current `disasm 0x004f4b20` decodes the shim as exactly:

```asm
004f4b20  push ebp
004f4b21  mov  ebp, esp
004f4b23  mov  eax, [ecx]
004f4b25  call dword ptr [eax+4]
004f4b28  mov  ecx, eax
004f4b2a  pop  ebp
004f4b2b  jmp  loc_5568E0
004f4b30  push ebp ; sub_4F4B30 begins here
```

## Callers And Relationships

No direct xrefs are currently surfaced by IDA for `0x004f4b20`, which is consistent with a raw helper/shim missed by function-boundary modeling. It belongs with the `LObject` runtime-class surface because it invokes the runtime-class accessor slot and immediately tail-jumps to [UID:00023R][0x005568d0-0x00556907.CallbackAndListMembershipHelpers](by-memory/0x005568d0-0x00556907.CallbackAndListMembershipHelpers.md), whose `0x005568e0` body performs base-chain membership checks.

This exact child covers the raw membership-shim subrange inside [UID:000195][0x004f4a80-0x004f4c0d.LObjectRuntimeShell](by-memory/0x004f4a80-0x004f4c0d.LObjectRuntimeShell.md).

Current MCP `xrefs_to` on 2026-07-02 reports:

| Address | Current result | Meaning |
| --- | --- | --- |
| `0x004f4b20` | zero xrefs | No ordinary direct-call, table-entry, import/export, or modeled function-entry use of the shim start is visible. |
| `0x004f4b2b` | one code xref from `0x004f4b2a`, no owning function | IDA can see the local branch target in raw bytes but still has no containing function object for this shim. |
| `0x005568d0` | zero xrefs | The sibling optional callback/create helper is related runtime-class machinery but is not reached by this shim. |
| `0x005568e0` | exactly one code xref from `0x004f4b2b`, no owning function | UID000375 is the sole current code edge into the base-chain membership helper. |
| `0x0061cf44` | one data xref from `0x004f4b10` | The runtime-class record is returned only by the LObject runtime-class accessor. |
| `0x0061cf68` | three data xrefs from `0x004f4a80`, `0x004f4a90`, and `0x004f4bb8` | Constructor, destructor body, and scalar deleting destructor reset the LObject vtable. |

Current capped `find_bytes` pointer scans for little-endian dwords `20 4b 4f 00`, `d0 68 55 00`, and `e0 68 55 00` returned no matches. This supports the existing no-table-installed-standalone-shim conclusion; it is negative evidence, not proof that the raw bytes are dead, because the tail-jump edge proves live helper relation.

Current helper-island evidence matches [UID:00023R][0x005568d0-0x00556907.CallbackAndListMembershipHelpers](by-memory/0x005568d0-0x00556907.CallbackAndListMembershipHelpers.md): `get_bytes 0x005568d0 size 0x40` and bounded disassembly show `0x005568d0-0x005568dc` reading `[ecx+8]` and jumping through the callback/create pointer when non-null, `0x005568dc-0x005568e0` as four `0xcc` bytes, `0x005568e0-0x00556907` as the base-chain walk that loads target from `[ebp+8]`, compares it to `ecx`, advances through `[ecx+0x0c]`, and returns `al=1` on match or `al=0` on null/miss, followed by alignment at `0x00556907-0x00556910`.

Current runtime-class and vtable evidence ties the shim to `LObject`: `get_bytes 0x0061cf44 size 0x30` shows the runtime-class record begins with pointer `0x0061cf54`, size dword `4`, null callback/base fields for the base record, UTF-16 `LObject`, then the `LObject` vtable entries at `0x0061cf68`; `get_bytes 0x0061cf68 size 0x18` shows `0x004f4b90`, `0x004f4b10`, and `0x0041b6c0` as the scalar deleting destructor, runtime-class accessor, and default no-op virtual entries before adjacent data. Current `xrefs_to 0x004f4b10 limit:20` returns data-table/vtable refs with `more:true`, confirming inherited/shared virtual-slot use rather than ordinary direct calls.

The following Application wrapper cluster remains a rejected owner: current `xrefs_to` reports ordinary code callers for `0x004f4b30` (four refs), `0x004f4b50` (seven refs), and `0x004f4b70` (two refs), while UID000375 has zero direct xrefs and routes to runtime-class helper code. This contrast keeps the boundary at `0x004f4b30` and keeps UID000375 with `LObject`, not Application.

## Source-Quality And Rejected Alternatives

`LObjectRuntimeClassMembershipShim` remains a descriptive filename/history token for IDA's missing function boundary. The accepted human source identity is `LObject::IsKindOf`, paired with `RuntimeClass::IsDerivedFrom`. These names are high-probability inference from exact behavior and the project architecture; original macro/API spelling is not claimed as recovered symbol proof.

Keep direct `CANONICAL_OWNER` and `EMITTER_UIDS` as [UID:00007D][LObject](by-class/LObject.md). [UID:0000KV][LObject](by-file/LObject.md) remains the source-file route to `NexusTK/util/LObject.cpp`. [UID:00023R][0x005568d0-0x00556907.CallbackAndListMembershipHelpers](by-memory/0x005568d0-0x00556907.CallbackAndListMembershipHelpers.md) is callee/helper support, not direct owner of this object-facing shim.

Rejected alternatives:

- Padding or unknown bytes: rejected because current bytes decode to a coherent seven-instruction shim and provide the sole code edge to `0x005568e0`.
- Application wrapper ownership: rejected because `sub_4F4B30` begins after the shim and has ordinary `g_pChangeMan` wrapper callers, while the shim has runtime-class behavior and no direct xrefs.
- Direct UID00023R ownership: rejected because the helper island owns the base-chain walk; UID000375 obtains the current object's runtime-class record and forwards into that helper.
- No-owner/non-emitting: rejected because current bytes, runtime-class/vtable record, LObject parent docs, and generated `NexusTK/util/LObject.cpp` route are enough for owner/emitter assignment.
- Standalone decompiler-shaped shim body: rejected. The formal CPP models the compact human API expression whose optimized lowering is the observed virtual call plus tail jump.

## Confidence And Open Questions

The bytes, boundary, tail-jump behavior, LObject owner/emitter route, helper relation, declaration type, and negative direct-xref/pointer-scan evidence are confirmed. Confidence remains below final-audit level only because exact original API/macro spelling is inferred.

## Reconstruction Notes

Emit the source-level `LObject::IsKindOf` definition in formal CPP. Do not preserve the raw no-function boundary as a reason for comment-only output: optimized source can produce an unmodeled tail-jump-shaped body.

Target-specific no-code proof:

- Current MCP proves no IDA function object at `0x004f4b20` and no direct xrefs to the shim start.
- The complete body is seven instructions of glue: call virtual runtime-class accessor at slot `+4`, move the return value into `ecx`, and tail-jump to the shared helper at `0x005568e0`.
- The shared helper already owns the base-chain walk through offset `+0x0c`; duplicating helper logic or naming a standalone shim body would misrepresent source shape.
- Exact original runtime-class API spelling and metadata declaration type are not proven.
- Generated `NexusTK/util/LObject.cpp` already carries this page's formal comment marker rather than an empty marker.

Therefore, emit the accepted human method and preserve the raw no-function facts only as historical compiler/IDA modeling evidence.

## Historical Assumption Superseded

- Earlier passes treated the absent IDA function object and tail-jump form as proof that this page should emit only a no-code marker. The complete RuntimeClass descriptor/helper closure disproves that source-policy conclusion.
- The old raw-shim evidence remains valid: exact bytes, no direct xrefs, and the sole edge into `0x005568e0` still explain the optimizer/IDA shape. What is superseded is only the claim that source C++ could not be reconstructed.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | Exact range, assembly, no-function status, xrefs, tail-jump/helper relation, descriptor layout, owner/emitter route, source API, formal CPP, and superseded no-code history are complete. |
| Confidence | 93 | Current bytes and RuntimeClass closure agree on the optimized lowering of `LObject::IsKindOf`; inferred original spelling is the remaining cap. |

## Cross-References

- [UID:00007D][LObject](by-class/LObject.md)
- [UID:0000KV][LObject](by-file/LObject.md)
- [UID:000195][0x004f4a80-0x004f4c0d.LObjectRuntimeShell](by-memory/0x004f4a80-0x004f4c0d.LObjectRuntimeShell.md)
- [UID:00023R][0x005568d0-0x00556907.CallbackAndListMembershipHelpers](by-memory/0x005568d0-0x00556907.CallbackAndListMembershipHelpers.md)

## Changes

- 2026-08-01 Agent-B004 UID0003OO support callback:
  - Raised `87/91 -> 92/93` and replaced the historical no-code marker with exact `bool LObject::IsKindOf(const RuntimeClass *) const` CPP.
  - Preserved the raw/no-function/xref evidence as historical compiler-lowering context, resolved the source API against `RuntimeClass::IsDerivedFrom`, and removed the stale pending-name/type blocker.
- 2026-07-02 B012 implementation callback:
  - Changed to: raised scores from `85/88` to `87/91`, kept `CANONICAL_OWNER:00007D`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00007D`, blank emitter position, and the existing formal comment-only no-code marker.
  - Evidence: current MCP session `supervisor_resume_20260629` reports `status:ok`, confirms no IDA function at `0x004f4b20`, confirms neighboring `sub_4F4B10` size `0x6`, `sub_4F4B30` size `0x1c`, helper starts `0x005568d0`/`0x005568e0` as not functions, and next modeled `sub_556910` at `0x00556910` size `0x14a`.
  - Raw bytes/disassembly: exact shim bytes are `55 8b ec 8b 01 ff 50 04 8b c8 5d e9 b0 1d 06 00`; bounded disassembly shows `push ebp`, `mov ebp, esp`, `mov eax,[ecx]`, `call dword ptr [eax+4]`, `mov ecx,eax`, `pop ebp`, and `jmp loc_5568E0`.
  - Xrefs/negative evidence: current MCP reports zero xrefs to `0x004f4b20`, exactly one code xref to `0x005568e0` from `0x004f4b2b`, zero xrefs to `0x005568d0`, no capped pointer-scan hits for `0x004f4b20`/`0x005568d0`/`0x005568e0`, unique `0x0061cf44` xref from `0x004f4b10`, and `0x0061cf68` refs from `0x004f4a80`, `0x004f4a90`, and `0x004f4bb8`.
  - Source-quality decision: preserved `LObjectRuntimeClassMembershipShim` as a descriptive documentation token, rejected padding/Application/UID00023R/no-owner/standalone-C++ alternatives, and kept formal output as a no-code marker because source should express this through `LObject` runtime-class API/declaration/helper machinery.

- 2026-06-11 Agent-A001 Batch 158 split:
  - Created exact child documentation for the raw runtime-class membership shim previously documented only inside the mixed `LObjectRuntimeShell` aggregate.
  - Evidence: live IDA MCP disassembly confirms complete shim bytes at `0x004f4b20-0x004f4b30`, no IDA function object, and tail jump to the runtime-class membership helper at `0x005568e0`.
