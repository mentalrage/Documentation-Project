*** UID:00017Q | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000076 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000076 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
LightObjImageLib::~LightObjImageLib()
{
    for (int index = 0; index < m_lightInfos.m_count; ++index) {
        IntAlphaSurface *frame = m_lightInfos.m_entries[index].generatedFrame;
        if (frame != 0)
            delete frame;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw ordinary destructor; live IDA confirms non-modeled 0x86-byte body, no direct refs to the raw start, frame-delete loop over 0x1c LightInfo rows, vtable/global writes, record-buffer free, base tail-call, and ten-byte cc padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dfab0-0x004dfb36 LightObjImageLib Destructor

## UID0001PR Accepted Authored Destructor And Lowered Cleanup Split - 2026-07-21

- This exact raw ordinary destructor emits under [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) at position `30`. Its lack of an IDA function object does not make the source body dead: the class vtable slot and byte-identical cleanup prefix in [UID:0002IY][0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor](by-memory/0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor.md) establish the ordinary destructor source operation.
- The authored destructor loop deletes each non-null `generatedFrame` in the live `m_lightInfos.m_entries` range. It does not delete the inline default entry, whose generated-frame pointer is initialized null and has no recovered population path.
- The remaining binary sequence is generated lifetime machinery: member destruction restores the ProtectedArray vptr and frees its entry storage; the direct Singleton base clears `g_pLightObjImageLib`; the LObject base destructor performs base teardown; scalar deleting destruction adds delete-flag and optional storage-free behavior. Vptr writes and the tail jump are also compiler output.
- Consequently, explicit `free(m_lightInfos.m_entries)`, `g_pLightObjImageLib = 0`, vtable stores, the base tail-call, and optional `operator delete(this)` are deliberately absent from authored C++. They remain valid binary facts below and are not discarded.
- The historical formal that handwritten the entry-buffer free and singleton clear is superseded by this source-safe body. The exact raw range remains `[0x004dfab0,0x004dfb36)` with ten trailing `0xcc` padding bytes before `0x004dfb40`.

## Status

- Confidence: strong for body boundaries and behavior.
- Owner class: [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md).
- Likely source file: [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).
- IDA function status: raw code body, not currently modeled as a function start.
- Autogen parent: [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) at position `30`; the class emits through [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).
- First-draft C++: populated because the raw bytes, scalar-deleting-destructor parity, frame-delete loop, entry-buffer free, singleton clear, base cleanup tail, and padding establish the source destructor even though IDA does not model the raw start as a function object.

## Behavior

This body is the ordinary non-deleting destructor for `LightObjImageLib`. It is not present in the current 4-method generated class output, but IDA disassembly and vtable/global xrefs show the complete cleanup sequence:

1. Restore the [UID:0001XY][LightObjImageLibVtable](by-type/by-vtable/LightObjImageLibVtable.md) at `this + 0x00`.
2. Iterate the `LightInfo` array using `this + 0x08` as the count and `this + 0x0c` as the entry buffer.
3. For each record, delete the generated frame pointer at record offset `+0x18` through its virtual slot 0 with delete flag `1`.
4. Restore the adjacent `ProtectedArray<LightInfo>` vtable at `this + 0x04`.
5. Free the entry buffer at `this + 0x0c`.
6. Clear [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md) at `0x0069b450`.
7. Tail-call the base `LObject` cleanup at `0x004f4a90`.

Unlike the scalar deleting destructor at `0x004e65e0`, this body does not perform the optional `operator delete(this)` tail.

The accepted C++ intentionally omits explicit vtable restores, the base destructor tail-call, ProtectedArray storage release, and singleton clear. Those are compiler/template output for source-level member/base destruction; the hand-authored destructor expresses only the owned-frame deletion loop. The fallback row is not deleted because B003 found it is seeded with a null generated-frame pointer and no recovered path populates it.

## Evidence

- IDA xrefs to the class vtable at `0x0061b754` land at `0x004dfab9` inside this body.
- IDA xrefs to the adjacent `ProtectedArray<LightInfo>` vtable at `0x0061b74c` land at `0x004dfb14`.
- IDA xrefs to [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md) land at `0x004dfb23`.
- The loop shape and cleanup order match the scalar deleting destructor at `0x004e65e0` before the delete-flag branch.
- IDA `lookup_funcs` does not define a function at `0x004dfab0`, so source reconstruction must preserve this raw-function body explicitly.
- Live IDA MCP/`py_eval` on 2026-06-06 confirms the raw byte span is `0x86` bytes from `0x004dfab0` through exclusive end `0x004dfb36`, followed by ten `0xcc` bytes through `0x004dfb40`.
- The raw disassembly restores the LightObjImageLib vtable at `0x004dfab9`, walks `this+0x08` count with `0x1c`-byte strides from `this+0x0c`, falls back to `this+0x14` on out-of-range checks, calls each frame object's virtual slot 0 with delete flag `1`, restores the `ProtectedArray<LightInfo>` vtable at `0x004dfb14`, frees the record buffer, clears `dword_69B450`, and tail-jumps to `sub_4F4A90`.
- IDA MCP `callers(0x004dfab0)` and `idautils.CodeRefsTo/DataRefsTo(0x004dfab0)` report no direct refs to the raw start; ownership comes from the in-body vtable/global writes and the scalar deleting destructor comparison.

## Score Rationale

Completion is `92` because the exact raw byte span, cleanup order, vtable/global writes, frame-delete loop, member-buffer teardown, base tail-call, no-direct-ref status, scalar deleting destructor relationship, compiler-output exclusions, class position, and authored source body are documented. Confidence is `94` because raw bytes, vtable placement, scalar parity, direct-base layout, and generated source routing resolve the previous body/ownership blockers; only unrecoverable original lexical/style choices remain.

## Cross-References

- [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md)
- [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md)
- [UID:00017P][0x004df7e0-0x004dfd0a.LightObjImageLibLocalMethodCluster](by-memory/0x004df7e0-0x004dfd0a.LightObjImageLibLocalMethodCluster.md)
- [UID:0001XY][LightObjImageLibVtable](by-type/by-vtable/LightObjImageLibVtable.md)
- [UID:0001UX][LightObjImageLibLayout](by-type/by-struct/LightObjImageLibLayout.md)
- [UID:0001UW][LightInfo](by-type/by-struct/LightInfo.md)
- [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md)
- [UID:0002IY][0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor](by-memory/0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor.md)

## Changes

- 2026-05-31 exact split pass:
  - What existed before: `RECONSTRUCTABLE` was blank and scores were `70/85`.
  - Changed to: `RECONSTRUCTABLE:TRUE`, `COMPLETION:78`, and `CONFIDENCE:88`, with a cross-reference to the scalar deleting destructor.
  - Summary/evidence: IDA MCP disassembly verified this raw body's exact cleanup sequence and confirmed it matches the scalar deleting destructor's cleanup prefix before the delete-flag branch. This was the historical pre-B003 state; B003 later resolved first-draft source emission for the ordinary destructor.
- 2026-06-06 A008 raw-body refresh:
  - What existed before: the page was `78/88`, unassigned, and did not record the current raw byte span, no-direct-ref check, or exact tail-call/padding evidence.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:90`, `AUTOGEN_PARENT_UID:0000KP`, and expanded raw disassembly evidence for the frame-delete loop, vtable/global writes, free/base cleanup sequence, and `0xcc` padding.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, and `py_eval`/`idautils` checks confirmed that `0x004dfab0` is not a modeled function, the source-owned raw body is `0x86` bytes, there are no direct refs to the raw start, the in-body vtable/global refs match LightObjImageLib, and the cleanup prefix matches [UID:0002IY][0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor](by-memory/0x004e65e0-0x004e669d.LightObjImageLibScalarDeletingDestructor.md).
- 2026-06-17 B003 source-quality execution:
  - What existed before: the page was `82/90` with C++ blank because the raw start was not an IDA function.
  - Changed to: `88/92` with first-draft `LightObjImageLib::~LightObjImageLib()` C++; explicit vtable restoration, base tail-call, and optional `delete this` remain excluded as compiler output.
  - Summary/evidence: B003 rechecked the raw body, scalar deleting destructor parity, ten-byte padding, frame-delete loop over `0x1c` `LightInfo` rows, record-buffer free, singleton clear, and source-placement route through [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).
