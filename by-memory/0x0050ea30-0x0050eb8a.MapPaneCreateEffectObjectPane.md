*** UID:0003TF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::CreateEffectObjectPane(
    LivingObjectPane *target,
    int mapRow,
    int mapColumn,
    EffectInfo effectInfo,
    int timerAction,
    int sequenceIndex,
    bool renderAfterLivingObjects)
{
    if (timerAction == -1)
        return;

    int rangeStart = effectInfo.secondaryRangeStart;
    int rangeEnd = effectInfo.secondaryRangeEnd;
    if (sequenceIndex == 1)
    {
        rangeStart = effectInfo.primaryRangeStart;
        rangeEnd = effectInfo.primaryRangeEnd;
    }

    int timerDelay = timerAction;
    if (timerDelay > 0)
        timerDelay *= 1000;

    EffectObjectPane *effectObjectPane = new EffectObjectPane(
        target,
        mapRow,
        mapColumn,
        effectInfo,
        sequenceIndex,
        renderAfterLivingObjects);

    if (target != NULL)
    {
        if (sequenceIndex == 0)
            target->AddSecondaryEffectObjectPane(effectObjectPane);
        else if (sequenceIndex == 1)
            target->AddPrimaryEffectObjectPane(effectObjectPane);
    }

    m_objectList->AddObjectPane(effectObjectPane);
    effectObjectPane->StartIdleTimer();

    if (timerDelay != -2 &&
        rangeStart >= 0 &&
        rangeEnd >= 0)
    {
        g_pTimerMgr->ScheduleTimer(
            static_cast<TimerHandler *>(effectObjectPane),
            2,
            timerDelay,
            0,
            0);
    }

    if (target != NULL)
        effectObjectPane->UpdatePosition();

    RectBounds bounds;
    effectObjectPane->GetBounds(&bounds);
    effectObjectPane->InvalidateRect(&bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready MapPane::CreateEffectObjectPane helper at exact range `0x0050ea30-0x0050eb8a`; preserves four typed/null descriptor calls, machine-accurate target/row/column order, by-value 52-byte EffectInfo ABI, timerAction sentinels, ordinary EffectObjectPane creation, primary/secondary LivingObjectPane registration, MapPane ObjectList insertion, timer events 1/2, UpdatePosition, bounds retrieval, and invalidation. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0050ea30-0x0050eb8a MapPane Create EffectObjectPane

## UID0003TF Source-Quality Implementation - 2026-07-15

- Current disposition is source-ready `MapPane::CreateEffectObjectPane` at `92/94`, with owner/emitter [UID:00007Q][MapPane](by-class/MapPane.md), `RECONSTRUCTABLE:TRUE`, blank explicit position, and `Nested:0`. The exact range remains `[0x0050ea30,0x0050eb8a)` and has no split, merge, rename, owner, emitter, padding, or source-route change.
- Fresh bounded MCP evidence identifies `sub_50EA30` as a 346-byte function (`0x15a`), 22 basic blocks, cyclomatic complexity 9, no strings, and full-byte SHA256 `EC68940B3FC220422D1D9E592150F4AA49B110A687C809A07EF0935E039E803B`. The first 16 bytes are `55 8b ec 6a ff 68 9e 29 60 00 64 a1 00 00 00 00`; the last 16 are `8b 4d f0 33 cd e8 ab 8b 0b 00 8b e5 5d c2 4c 00`.
- The explicit `retn 0x4c` and frame use prove a MapPane `thiscall`, by-value `EffectInfo` size `0x34`, and trailing `timerAction`, `sequenceIndex`, and `renderAfterLivingObjects` values at frame `+0x48`, `+0x4c`, and `+0x50`. The source return is `void`; incidental register state is not a source result.
- Exactly four direct callers exist: `0x0050e3ed` and `0x0050e47e` in [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) pass a non-null `LivingObjectPane *` for secondary/primary sequences; `0x0050e945` and `0x0050e9e3` in [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md) pass null for the same sequence order. No fifth caller or non-LivingObjectPane non-null route was found.
- `timerAction == -1` returns before allocation. Sequence `1` selects `EffectInfo` primary ranges at `+0x14/+0x18`; every other value starts with secondary ranges at `+0x2c/+0x30`, while only sequences `0` and `1` enter a target owner list. Positive actions are multiplied by 1000; zero remains zero; `-2` permits creation but suppresses event 2.
- Source uses ordinary `new EffectObjectPane(target, mapRow, mapColumn, effectInfo, sequenceIndex, renderAfterLivingObjects)`. The helper frame is target at `+0x08`, row at `+0x0c`, and column at `+0x10`; all four callers and the EffectObjectPane-to-ObjectPane constructor forwarding independently preserve that order. Pool allocation at `0x004b1400`, the constructor call at `0x005387b0`, vptr stores, allocation cookies, EH cleanup, and failure scaffolding are compiler/class allocation lowering and are intentionally absent from the formal source body.
- Constructor evidence accounts for the accepted fields: borrowed target `+0x128`; selected descriptor header `+0x12c..+0x143`; current frame `+0x144=-1`; effect id `+0x148`; `m_looping` `+0x14c`; `m_renderAfterLivingObjects` `+0x14d`; inferred `m_timerExtensionLocked +0x14e=false`; ObjectPane type `2 * (sequenceIndex != 1) + 2`.
- For non-null targets, sequence `0` calls [UID:0004QL][0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane](by-memory/0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane.md) and sequence `1` calls [UID:0004QK][0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane](by-memory/0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane.md). The created pane always enters MapPane `m_objectList` at `+0x424`, then calls [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) for event 1.
- Event 2 is scheduled through `g_pTimerMgr` on the complete inherited `TimerHandler` adjusted view at `+0xa4` only when the action is not `-2` and both selected ranges are nonnegative. A non-null target then causes inherited `UpdatePosition`; all routes finish with `GetBounds` and primary virtual-slot `+0x20` invalidation.
- Direct callees are `0x004b1400`, `0x005387b0`, `0x0053ae10`, `0x0053adb0`, `0x005314a0`, `0x005388a0`, `0x00597910`, `0x004b8e00`, and security-cookie support. Vtable `0x0062066c -> 0x005388c0` confirms `UpdatePosition`; the MapPane object list, target lists, timer view, descriptor ranges, and render-after field account for every state access and side effect.
- Source placement remains `NexusTK/map/MapPane.cpp`, after UID0003TE and before UID0003TG. EffectObjectPane, EffectObjImageLib, TimerMgr/TimerHandler, and ObjectList are dependencies, not target owners; no new file or grouping is warranted.
- Negative evidence rejects an interior entry, alternate data owner, handwritten pool API, allocation-recovery branch, explicit vptr/EH/cookie/adjusted-this/virtual-slot source, vector substitution, generic AttachedObjectPane insertion, explicit type-number method names, `GetEffectBounds`, or a compiler-only/covered-by target disposition.
- Historical/superseded assumptions: the earlier `ObjectPane *target`, `mapX/mapY`, `compactFlag`, generic type-2/type-4 insertion labels, `GetEffectBounds`, blank-formal rationale, and `86/90` score cap were provisional before the constructor, rendering, caller, vtable, and typed-list evidence was integrated. They are retained only as audit history and must not be read as current source shape.

## UID0003TG Create-Versus-Reuse Action Synchronization - 2026-07-15

- This creation method and source-ready [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md) implement distinct branches of the same descriptor policy. Creation suppresses allocation for `-1`, permits creation but suppresses event 2 for `-2`, preserves zero as a zero-delay schedule when selected ranges are valid, and scales positive delays by 1000 before scheduling.
- Existing-pane reuse receives the same signed `timerAction` but uses different exact semantics: `-1` performs ordinary source delete, zero is a no-op, `-2` cancels event 2, and every other signed action reaches inferred `m_timerExtensionLocked` and the due-tick comparison. It preserves the original unscaled comparison but uses `1000 * timerAction` for replacement scheduling. These differences are intentional and must not be normalized into one common duration helper.
- The constructor's `m_timerExtensionLocked=false` store is the creation-side initialization for the existing-pane replacement gate. Bounded family searches found no observed nonzero direct writer; the descriptive spelling is inferred from true-blocks-replacement polarity and remains distinct from `m_looping` and `m_renderAfterLivingObjects`.
- UID0003TF remains the sole creation definition and retains its complete B001/B005 body, target/row/column order, timer events, registration, bounds flow, `92/94` score, owner/emitter, and source route. UID0003TG remains the adjacent sole existing-pane action definition after six-byte alignment; no body, score, or create-order change is made here.

## UID0003Z4 Effect Registration Synchronization - 2026-07-14

- Sequence selector `1` constructs ObjectPane type 2 and calls [UID:0004QK][0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane](by-memory/0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane.md) at `0x0050eafe`; selector `0` constructs type 4 and calls [UID:0004QL][0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane](by-memory/0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane.md) at `0x0050eaef`.
- Each owner method scans its exact `List *` backward, compares public `EffectObjectPane::m_effectId`, deletes every same-ID existing pane without erasing List slots, then appends one four-byte pointer through `Append(1, &pane)`.
- MapPane remains creator/controller and retains construction, attach, object-list insertion, timers, bounds, and refresh behavior. Historically this synchronization did not alter the then-current `86/90` score or blank formal block; that limitation is superseded by the 2026-07-15 source-quality implementation above.
- Historical type2/type4 field and generic AttachedObjectPane insertion names are superseded by typed primary/secondary methods; no vector/deduplicating-container behavior is inferred.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact MapPane private packet/effect helper child split from [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md).
- Direct owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md); file owner [UID:0000L3][MapPane](by-file/MapPane.md).
- Source-facing name: `MapPane::CreateEffectObjectPane`.
- Confidence: very strong for exact range/bytes, callers, ABI, typed receiver, descriptor selection, ordinary construction, owner registration, list insertion, timers, update/bounds/invalidation behavior, source route, and complete formal C++.
- Reconstruction note: the formal block is complete and emits exactly once through MapPane. Original stripped lexical spellings cap confidence below 95 but leave no target behavior, owner, source-placement, or C++ blocker.

## Address Range

- Start: `0x0050ea30`
- End: `0x0050eb8a` end-exclusive
- IDA function: `sub_50EA30`
- IDA size: `0x15a` bytes
- Preceded by [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md).
- Followed by ignored padding `0x0050eb8a-0x0050eb90` before [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md).

## Behavior

This helper creates and registers an `EffectObjectPane` for MapPane effect descriptor routes. It is called by both the object-id dispatcher [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) and the coordinate/no-target dispatcher [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md). The helper:

- exits without creating a pane when `timerAction` is `-1`;
- selects descriptor coordinate/duration fields based on the effect pass;
- converts positive duration values to milliseconds;
- uses ordinary source-level `new EffectObjectPane`; [UID:0002WO][0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage](by-memory/0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage.md) and `PoolAllocator_Allocate` are class allocator/compiler lowering rather than handwritten target source;
- constructs [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md) through `0x005387b0`;
- attaches the new pane to the pass-specific target-object child collection when a target object is present;
- inserts/registers the object through MapPane object/list state via `0x005314a0`;
- calls source-ready [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) at `0x0050eb14`, which schedules event id `1` through `EffectObjectPane::StartIdleTimer()` on the inherited `+0xa4` TimerHandler adjusted view;
- optionally schedules timer event id `2` on the same `+0xa4` TimerHandler adjusted view as a separate MapPane-controlled loop/reuse timeout path;
- refreshes bounds/virtual state after registration.

## Source Signature And Sequence Mapping

The current formal signature is `MapPane::CreateEffectObjectPane` with a `LivingObjectPane *target`, `mapRow`, `mapColumn`, by-value `EffectInfo`, `timerAction`, `sequenceIndex`, and `renderAfterLivingObjects`. The exact destination-managed block above is authoritative; no separate body-only C++ draft remains.

The `EffectInfo` argument is by value. The helper receives the 52-byte descriptor in stack slots `[ebp+0x14]` through `[ebp+0x47]` and returns with `ret 0x4c`; a `const EffectInfo &` source signature would not match the observed ABI.

Argument roles:

- `target` at `[ebp+0x08]`.
- `mapRow` / `mapColumn` at `[ebp+0x0c]` / `[ebp+0x10]`.
- `timerAction` at `[ebp+0x48]`.
- `sequenceIndex` at `[ebp+0x4c]`.
- `renderAfterLivingObjects` at `[ebp+0x50]`, stored by the constructor at `EffectObjectPane +0x14d` and consumed by complementary early/late render passes.

`sequenceIndex == 0` selects the descriptor secondary sequence and inserts the new pane through [UID:0004QL][0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane](by-memory/0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane.md) / `m_secondaryEffectObjectPanes` at `+0x174`. `sequenceIndex == 1` selects the descriptor primary sequence and inserts through [UID:0004QK][0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane](by-memory/0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane.md) / `m_primaryEffectObjectPanes` at `+0x170`.

`timerAction == -1` suppresses creation. `timerAction == -2` still permits creation but suppresses event-id-2 scheduling. Positive `timerAction` values are seconds and become millisecond delays for timer event id `2` when the selected descriptor range allows scheduling.

## IDA MCP Evidence

- Agent-B004 current MCP session `supervisor_resume_20260629`, call `223`, reconfirmed `sub_50EA30` as size `0x15a` / half-open range `0x0050ea30-0x0050eb8a`; callers from `sub_50E320` and `sub_50E850`; callees including `sub_4B1400`, `sub_5387B0`, `sub_5314A0`, `sub_5388A0`, `sub_53AE10`, `sub_53ADB0`, and `sub_597910`; and the same `EffectInfo` by-value ABI plus timer behavior recorded below.
- B001 live IDA MCP session `b001_mappane_0001AW_20260616` reports `sub_50EA30` at `0x0050ea30`, size `0x15a`, 22 basic blocks, cyclomatic complexity 9.
- `xrefs_to` reports direct call sites at `0x0050e3ed`, `0x0050e47e`, `0x0050e945`, and `0x0050e9e3`, from [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) and [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md).
- `analyze_function` reports direct callees including `PoolAllocator_Allocate`, `sub_5387B0`, `sub_5388A0`, `sub_5314A0`, `sub_53AE10`, `sub_53ADB0`, and `sub_597910`.
- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md) records that `xrefs_to 0x005387b0` and `xrefs_to 0x005388a0` both land in this function.
- [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) records the sole direct caller at `0x0050eb14` inside this function and now emits source-ready `void EffectObjectPane::StartIdleTimer()` calling `TimerHandler::ScheduleTimer(1, 0, 0, 0)`.
- B001 byte-signature review confirms the following `0x0050eb8a-0x0050eb90` bytes are six `0xcc` padding bytes, not part of this child.

## Ownership Decision

This body belongs to MapPane because it uses the MapPane receiver and `m_objectList`, is reached only from MapPane descriptor routes, and decides when and how to create/register a map effect object. `EffectObjectPane` owns the constructed object's constructor, source-ready `StartIdleTimer`, vtable, and pool storage; it does not own the MapPane packet-side creation decision. Event id `1` is the object's idle/animation starter through [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md), while event id `2` remains the separate MapPane-scheduled loop/reuse timeout path on the same inherited `+0xa4` TimerHandler adjusted view.

## Rejected Owners

- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md): owns the constructed class and timer helper, not the MapPane descriptor routing helper.
- [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md): descriptor provider only.
- TimerMgr/TimerHandler support: generic scheduling abstractions only.
- ObjectList: receives the created pane but does not make the descriptor/target/sequence decision.
- File-local/free helper or a new source file: rejected by the MapPane receiver/state, four MapPane callers, and established MapPane.cpp address cluster.

## Score Rationale

Completion is `92` and confidence is `94`: exact bytes/range, CFG, ABI, all callers/callees, constructor fields, typed owner lists, timer sentinels, update/bounds/invalidation tail, ordinary-new source lowering, owner/emitter/source order, rejected alternatives, negative evidence, and complete formal C++ are documented. Confidence remains below 95 only because stripped original lexical spellings cannot be proven.

## Cross-References

- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md)
- [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md)
- [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md)
- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md)
- [UID:0002WO][0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage](by-memory/0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage.md)
- [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-15 B004 UID0003TG accepted source-quality callback:
  - Preserved `92/94`, UID00007Q owner/emitter, the complete B001/B005 formal body, target/row/column constructor order, allocation/registration/timer/update/bounds behavior, source route, and all unrelated evidence.
  - Replaced only bare `guard +0x14e=0` prose with inferred `m_timerExtensionLocked=false` and documented the exact distinct creation-versus-existing action matrix, signed replacement behavior, dimensional quirk, bounded no-writer evidence, adjacency/source order, and superseded generic guard terminology. No formal, score, metadata, or row-before-column correction changed.

- 2026-07-15 B005 UID0003TE accepted source-quality callback:
  - Preserved `92/94`, UID00007Q owner/emitter, exact range, Item Summary detail, timer/registration/allocation/bounds behavior, and complete B001 body while correcting only the source parameter and constructor-call order to target, row, column.
  - Reconfirmed the order from all four caller push sequences, callee frame offsets, GetMapPosition row/column fields, and EffectObjectPane constructor forwarding to ObjectPane. Historical transposed `mapColumn, mapRow` wording is superseded rather than retained as current truth.
  - No score, owner, emitter, position, nesting, source route, allocation policy, timer semantics, or compiler/source boundary changed.

- 2026-07-15 B001 UID0003TF accepted source-quality callback:
  - Raised only this target from `86/90` to `92/94`; retained owner/emitter UID00007Q, reconstructable true, blank position, `Nested:0`, exact no-split range, and six-byte ignored successor padding.
  - Inserted the accepted complete formal body and synchronized exact byte/hash/CFG/ABI/caller/callee/field/timer/update/bounds/source-order evidence.
  - Replaced current-facing `ObjectPane *`, `compactFlag`, generic insertion, `GetEffectBounds`, handwritten pool, and blank-body conclusions with the accepted typed source shape; retained them explicitly as superseded historical assumptions.

- 2026-06-29 Agent-B004 MapPane empty-emitter callback:
  - Added the missing item summary and current MCP evidence from accepted report call `223`.
  - Preserved blank formal C++: the stable signature is available for callers, but the helper-local body is not inserted in this callback because exact field names and final body shape remain below the accepted batch scope.
- 2026-06-25 B002 EffectObjectPane StartIdleTimer support sync:
  - Score/metadata unchanged at `86/90`, owner/emitter [UID:00007Q][MapPane](by-class/MapPane.md), reconstructable true, and blank C++.
  - Summary/evidence: updated the creation sequence to state that the call at `0x0050eb14` reaches source-ready [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md), now modeled as `EffectObjectPane::StartIdleTimer()` scheduling event id `1` with `TimerHandler::ScheduleTimer(1, 0, 0, 0)` through inherited `+0xa4`. MapPane ownership for this creation helper is unchanged, and optional event id `2` scheduling remains documented as the separate MapPane-controlled loop/reuse timeout path on the same TimerHandler adjusted view.
- 2026-06-20 B007 object-effect descriptor implementation support:
  - Promoted the working name to `MapPane::CreateEffectObjectPane` and documented the source signature used by [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md).
  - Recorded that `EffectInfo` is passed by value, not by reference, based on the 52-byte stack argument and `ret 0x4c`.
  - Recorded the pass mapping: `sequenceIndex == 0` uses the secondary/type-4 sequence and `InsertType4AttachedObject`; `sequenceIndex == 1` uses the primary/type-2 sequence and `InsertType2AttachedObject`; `timerAction == -1` suppresses creation while `-2` suppresses event-id-2 scheduling.
- 2026-06-21 B008 source-quality wording sync:
  - Replaced the remaining timer/update wording with `timerAction`; the B008 report's by-value `EffectInfo`, pass mapping, compact flag pass-through, and create-helper ownership evidence were already present from the B007 implementation.
- 2026-06-16 B001 continuation batch3 recommendation: create this exact child from the remaining [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md) broad leaf. Parent remains reconstructable because later source-bearing leaves remain unsplit.
