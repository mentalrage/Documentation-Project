*** UID:0004W7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
extern unsigned char **g_surfaceColorLookup5Bit;
extern unsigned char **g_surfaceColorLookup6Bit;

ScreenPane::~ScreenPane()
{
    if (g_surfaceColorLookup5Bit != 0) {
        for (int index = 0; index < 256; ++index) {
            if (g_surfaceColorLookup5Bit[index] != 0)
                free(g_surfaceColorLookup5Bit[index]);
        }
        free(g_surfaceColorLookup5Bit);
    }

    if (g_surfaceColorLookup6Bit != 0) {
        for (int index = 0; index < 256; ++index) {
            if (g_surfaceColorLookup6Bit[index] != 0)
                free(g_surfaceColorLookup6Bit[index]);
        }
        free(g_surfaceColorLookup6Bit);
    }

    if (g_pDirectX != 0) {
        if (m_renderSurface != 0) {
            m_renderSurface->Release();
            m_renderSurface = 0;
        }

        if (m_grafPort.m_surface != 0) {
            m_grafPort.m_surface->Release();
            m_grafPort.m_surface = 0;
        }

        if (m_primarySurface != 0) {
            m_primarySurface->Release();
            m_primarySurface = 0;
        }
    }

    if (g_pDirectX != 0) {
        LPDIRECTDRAW directDraw = g_pDirectX->m_pDirectDraw;

        if (m_restoreDisplayModePending && ApplicationActiveGate == 0) {
            HRESULT result = directDraw->RestoreDisplayMode();
            if (FAILED(result))
                throw new DDError(result);

            m_restoreDisplayModePending = false;
        }

        if (m_restoreCooperativeLevelPending) {
            HRESULT result = directDraw->SetCooperativeLevel(
                g_pApplication->GetMainWindowHandle(),
                DDSCL_NORMAL);

            if (FAILED(result)) {
                wchar_t formattedError[0x50];
                DDError *errorText = new DDError(result);
                errorText->FormatErrorMessage(formattedError, 0x50);
                throw new DDError(result);
            }

            m_restoreCooperativeLevelPending = false;
        }
    }

    delete m_rootLayerList;
    m_rootLayerList = 0;

    delete m_fadeCompletionCallback;
    m_fadeCompletionCallback = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 628-byte ScreenPane ordinary destructor with two 256-entry Surface lookup-root cleanup loops, three ordered DirectDraw-surface releases, exact display-mode/cooperative-level restoration and DDError failure side effects, root-layer-list and FunctionObject0 callback deletion/nulling, implicit Singleton/FrameHandler/Pane teardown, and source-ready C++03 body excluding vptrs, explicit bases, singleton clear, deleting flags, operator delete, EH, and cookies. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00556a60-0x00556cd4 ScreenPane Destructor

## Status

- Address range: `0x00556a60-0x00556cd4`, end-exclusive, exactly `0x274` / 628 bytes.
- Entity kind: ordinary source-authored virtual destructor for [UID:0000CB][ScreenPane](by-class/ScreenPane.md).
- Source route: [UID:0000NB][ScreenPane](by-file/ScreenPane.md), `NexusTK/ui/core/ScreenPane.cpp`.
- Parent range: [UID:0001G5][0x00556910-0x00557132.ScreenPane](by-memory/0x00556910-0x00557132.ScreenPane.md).
- Disposition: reconstructable NexusTK product code. Compiler-emitted vptr restoration, base teardown, singleton clear, EH cleanup, cookies, adjustor thunks, scalar-deleting flags, and operator-delete dispatch are documented but excluded from the human destructor body.
- Exact body SHA256: `8A826794A8189177FB52EB2E694E53B569F785671B61F31CBF775DCC6565E1B1`.

## Function And Boundary Evidence

- IDA MCP session `9b0396a3` models one function at `0x00556a60` with size `0x274`; the final byte belongs to the destructor at `0x00556cd3`.
- Predecessor `[0x00556a5a,0x00556a60)` is six `0xcc` alignment bytes owned by the parent aggregate.
- Successor `[0x00556cd4,0x00556ce0)` is twelve `0xcc` alignment bytes before `ScreenPane::SetLayerFrame`, also owned by the parent aggregate.
- The sole direct xref to the ordinary destructor is the MSVC scalar deleting wrapper call at `0x00559b56` in [UID:00023U][0x00559b20-0x00559b8b.ScreenPaneDestructorThunkIsland](by-memory/0x00559b20-0x00559b8b.ScreenPaneDestructorThunkIsland.md).
- The deleting wrapper's hidden bit-4 path embeds complete object size `0x5b4`; its bit-1 path conditionally calls `OperatorDeleteWrapper`. Neither hidden behavior belongs in the source destructor signature or body.

## Exact Cleanup Order And Behavior

1. If `g_surfaceColorLookup5Bit` is non-null, iterate exactly 256 entries, `free` each non-null child pointer, then `free` the root. The root is not cleared after the final free.
2. Repeat the same 256-entry cleanup for `g_surfaceColorLookup6Bit`, again without a post-free root clear.
3. When `g_pDirectX` is non-null, release and clear `m_renderSurface`, inherited `m_grafPort.m_surface`, and `m_primarySurface` in that exact order.
4. When `g_pDirectX` is non-null and `m_restoreDisplayModePending` is set while `ApplicationActiveGate == 0`, call `IDirectDraw::RestoreDisplayMode`; throw `new DDError(result)` on failure and clear the flag only on success.
5. When `m_restoreCooperativeLevelPending` is set, call `IDirectDraw::SetCooperativeLevel(g_pApplication->GetMainWindowHandle(), DDSCL_NORMAL)`. On failure, preserve the unusual temporary `DDError` allocation and `FormatErrorMessage(formattedError, 0x50)` side call before throwing a second `new DDError(result)`; clear the flag only on success.
6. Delete and clear `m_rootLayerList` at primary offset `+0x10c`.
7. Delete and clear the owned `FunctionObject0 *m_fadeCompletionCallback` at primary offset `+0x5a8`.
8. The compiled tail clears `g_pScreenPane`, destroys the `FrameHandler` base at `+0xf8`, then destroys `Pane`. Reverse direct-base order and RTTI PMDs prove the clear is inlined `Singleton<ScreenPane>` base destruction, not an explicit destructor statement.

## Resource And Member Mapping

| Primary offset / symbol | Source-facing identity | Destructor behavior |
| --- | --- | --- |
| `g_surfaceColorLookup5Bit` | externally linked 5-bit Surface lookup root | 256 child frees followed by root free. |
| `g_surfaceColorLookup6Bit` | externally linked 6-bit Surface lookup root | 256 child frees followed by root free. |
| `+0x108` | `m_renderSurface` | Release first, then null. |
| inherited `Pane+0x18` | `m_grafPort.m_surface` | Release second, then null. |
| `+0x100` | `m_primarySurface` | Release third, then null. |
| `+0x111` | `m_restoreDisplayModePending` | Gate RestoreDisplayMode and clear only on success. |
| `+0x110` | `m_restoreCooperativeLevelPending` | Gate SetCooperativeLevel and clear only on success. |
| `+0x10c` | `m_rootLayerList` | Owned delete and null. |
| `+0x5a8` | `m_fadeCompletionCallback` (`FunctionObject0 *`) | Owned virtual delete and null. |
| `g_pScreenPane` | singleton source definition | Cleared by implicit `Singleton<ScreenPane>` base destruction. |

## Lookup-Root Linkage Correction

- Live bytes at `0x0069b3d8-0x0069b3e0` are eight zero bytes, and the destructor only tests null/non-null states. There is no branch compatible with a `(unsigned char **)-1` sentinel.
- Existing initializer/consumer evidence gives 12 xrefs to the 5-bit root and 11 to the 6-bit root.
- Because this destructor emits in `ScreenPane.cpp` while the accepted root definitions emit in `Surface.cpp`, the roots require external linkage. File-static definitions would make the direct cross-translation-unit references impossible.
- The source-facing definitions therefore remain Surface-owned, zero-initialized `unsigned char **` globals; this page carries compatible `extern` declarations only.

## DirectDraw Failure Semantics

- Preserve the branch order exactly: display-mode restoration is additionally gated by `ApplicationActiveGate == 0`; cooperative-level restoration is not.
- Both DirectDraw calls use signed HRESULT failure checks.
- The `SetCooperativeLevel` failure path intentionally formats an error message through a temporary allocated `DDError` and then throws a separately allocated `DDError`. The apparently unused formatted buffer and leaked temporary are observed side effects and must not be optimized out of the reconstruction.
- Do not replace the body with a call to `ScreenPane::RestoreDirectDrawState()`. The binary contains the cleanup sequence directly in the destructor; an out-of-line call would not reproduce the observed body under the period compiler model.

## Compiler-Generated Versus Human Source Boundary

Human source includes the lookup cleanup loops, surface releases, DirectDraw restoration, and owned-pointer deletion. Human source excludes:

- vptr writes restoring the primary and secondary ScreenPane views;
- an explicit `g_pScreenPane = 0` singleton clear;
- explicit `FrameHandler::~FrameHandler()` or `Pane::~Pane()` calls;
- the hidden deleting flag argument and return-this convention;
- the conditional operator-delete call and `0x5b4` size guard;
- constructor-unwind cleanup, adjustor thunks, RTTI/vtable arrays, EH state, and stack-cookie scaffolding;
- raw addresses, `this[index]` notation, or decompiler-generated symbol names.

Those operations are regenerated by the virtual destructor declaration, direct-base hierarchy, and `Singleton<ScreenPane>` inheritance recorded on [UID:0000CB][ScreenPane](by-class/ScreenPane.md).

## Positive Evidence

- Complete decompilation and disassembly agree on both 256-entry loops, all calls, branch order, member clears, and destruction tail.
- Constructor `0x00556910` initializes the same surface/list/flag/callback family consumed here.
- Accepted lost-surface, DirectDraw-restoration, callback, and presentation documentation independently converges on the same member identities.
- The scalar wrapper has exactly one ordinary-destructor call and embeds object size `0x5b4`.
- RTTI PMDs and destruction order prove direct bases `Pane`, `FrameHandler`, and `Singleton<ScreenPane>`.
- The singleton clear occurs between the source destructor body and later base destruction, exactly where an inlined empty Singleton base destructor belongs.

## Negative Evidence And Rejected Alternatives

- No ordinary product caller reaches this function directly; virtual destruction reaches it through the compiler wrapper.
- No evidence supports a free cleanup helper, generic `DestructorHelpers.cpp`, `Singleton.cpp`, `Surface.cpp`, `CursorManager.cpp`, Browser ownership, or raw assembly source placement.
- No evidence supports post-free lookup-root nulling, a `-1` sentinel, file-static lookup roots, or moving root ownership into ScreenPane.
- No evidence supports an explicit singleton clear, explicit base destruction, manual vptr writes, deleting flags, operator delete, size guard, EH, or cookie logic in human C++.
- No evidence supports merging this exact source method into the compiler-only [UID:00023U][0x00559b20-0x00559b8b.ScreenPaneDestructorThunkIsland](by-memory/0x00559b20-0x00559b8b.ScreenPaneDestructorThunkIsland.md) island or emitting the broad [UID:0001G5][0x00556910-0x00557132.ScreenPane](by-memory/0x00556910-0x00557132.ScreenPane.md) aggregate as a duplicate body.

## Historical Corrections

- Historical prose treated `0x00559b20` as a source singleton-clear helper and treated the ordinary destructor as only a broad aggregate member. Constructor-EH receiver adjustment to `this+0xfc` proves `0x00559b20` is generated Singleton unwind cleanup; this exact page now owns the ordinary source body.
- Historical field names `m_grafPortSurface` and `m_presentationSurface` are superseded by inherited `m_grafPort.m_surface` and derived `m_renderSurface`.
- Historical `(unsigned char **)-1` file-static lookup definitions are superseded by the live-zero, externally linked Surface-owned model required by this destructor.

## Ownership, Nesting, And Reconstruction

- Canonical owner and emitter: [UID:0000CB][ScreenPane](by-class/ScreenPane.md).
- Source root: [UID:0000NB][ScreenPane](by-file/ScreenPane.md).
- Relative nesting: first exact child of [UID:0001G5][0x00556910-0x00557132.ScreenPane](by-memory/0x00556910-0x00557132.ScreenPane.md), therefore `Nested:+4`; the later browser-handler sibling becomes `Nested:0`, and successor parent [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) remains `Nested:-4`.
- The broad parent becomes a non-emitting method index. This page is the sole exact emitter for the ordinary destructor range.
- Recommended score: completion `92`, confidence `94`. Exact body and compiler/source split are strong; original private names and exact original header organization remain inferred below final-audit certainty.

## Validation Expectations

- Scoped validation must preserve the exact range/path, register a stable UID, attach this page through ScreenPane, and generate one ordinary `ScreenPane::~ScreenPane()` definition.
- Generated output must not contain an additional target helper, deleting-wrapper body, explicit singleton/base teardown, raw vtable/RTTI arrays, EH/cookie scaffolding, or duplicate destructor.
- Parent/internal padding and the separate compiler-only island remain documented at their exact ranges.

## Evidence Provenance

- Primary live evidence session: IDA MCP `9b0396a3`, NexusTK IDB `E:/NTK/Resources/NexusTK/NexusTK.exe.i64`.
- Backing PE SHA256: `9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`.
- Source-quality report: `tools/leaser/Agents/Agent-B003/research/00023U-ScreenPaneDestructorThunkIsland-source-quality.md`, Gate 1 accepted at SHA256 `E0B4475E8D1F8368AE5F3F6A6E687F6A0CAD360E5BB1E38BA57C9C4179404DC8`.
