*** UID:0004N5 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SpelledPane::ParseAndLoadEntries(const wchar_t *text, int length)
{
    static wchar_t delimiters[] = L" \t";

    m_entries.clear();

    LineIterator lines(text, length);
    WideString line;
    lines.ReadNextLine(line);

    while (!line.empty()) {
        const int delimiter =
            line.FindLastOf(delimiters, 0xffffffffU,
                            static_cast<unsigned int>(wcslen(delimiters)));
        if (delimiter != -1) {
            const int textLength =
                line.FindLastNotOf(delimiters,
                                   static_cast<unsigned int>(delimiter),
                                   static_cast<unsigned int>(wcslen(delimiters))) + 1;

            SpelledPaneEntry entry;
            WideString entryText;
            line.SubstringWide(entryText, 0,
                               static_cast<unsigned int>(textLength));
            entry.text = entryText;

            WideString valueText;
            line.SubstringWide(valueText,
                               static_cast<unsigned int>(delimiter + 1),
                               0xffffffffU);
            entry.value = static_cast<int>(valueText.ToLong());

            // The original body leaves entry.group indeterminate.
            m_entries.push_back(entry);
        }

        WideString nextLine;
        lines.ReadNextLine(nextLine);
        line = nextLine;
    }

    RebuildDisplayText();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact SpelledPane serialized-entry parser with two callers, LineIterator/StringBase behavior, one function-local mutable space/tab array and two runtime length scans, empty/no-delimiter branches, 12-byte record and preserved indeterminate third scalar, vector append/growth paths, lifetimes, EH, and complete formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0056c0e0-0x0056c2db SpelledPane Parse And Load Entries

## Status

- Disposition: source-authored `SpelledPane` method.
- Exact range: `0x0056c0e0-0x0056c2db`, size `0x1fb`.
- Direct owner/emitter: [UID:0000DK][SpelledPane](by-class/SpelledPane.md).
- Source family: [UID:0000NZ][SpelledPane](by-file/SpelledPane.md), projected to `NexusTK/ui/panels/SpelledPane.cpp`.
- Parent range: [UID:0001HA][0x0056bb20-0x0056c3f1.SpelledPane](by-memory/0x0056bb20-0x0056c3f1.SpelledPane.md).
- Rebuild handling: emit this exact method body at class position `50`; do not emit the aggregate body or STL/compiler helper bodies here.
- C134 compile closure: `WideString` is the one namespace-scope alias declared by `SpelledPane.h`; the method no longer redeclares a function-local alias with the same token. All locals therefore have the exact canonical `mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> >` identity used by `SpelledPaneEntry`, `LineIterator::ReadNextLine`, assignment, search, substring, and conversion APIs. A local/nested replacement class or decompiler-style facade is rejected.

## Behavior

`SpelledPane::ParseAndLoadEntries` replaces the logical contents of the pane's 12-byte entry vector from a bounded UTF-16 buffer, then rebuilds the display text. It preserves allocated vector capacity, stops at the first empty line, accepts only lines containing at least one space or tab, splits at the last such delimiter, trims the separator run from the text prefix, converts the suffix through the existing wide-string integer conversion, and appends one record.

The local record's first field is a ref-counted wide String and its second field is the converted integer. The third scalar field is copied into the vector but is never defined by this function. The source body therefore uses default initialization, not value initialization, and intentionally does not assign `entry.group`. Replacing `SpelledPaneEntry entry;` with `SpelledPaneEntry entry = {};`, `SpelledPaneEntry entry{};`, or an explicit zero store would change runtime behavior.

## Input And Caller Contract

| Caller | Receiver and arguments | Meaning |
| --- | --- | --- |
| `0x0057068e` in [UID:0003QZ][0x005700e0-0x0057092c.SelfLookPane2OnActivate](by-memory/0x005700e0-0x0057092c.SelfLookPane2OnActivate.md) `SelfLookPane2::OnActivate` | Embedded `SpelledPane` at caller `this+0xfc`; UID0003QZ first converts byte-counted packet text, replaces TAB with LF, copies it into the fixed `wchar_t m_spelledSourceText[256]` member at `this+0xb7c`, measures that copied member, and passes the member pointer plus current character count. | Parses current self-look spelled/status source text during activation packet handling; the caller is a bounded consumer and does not own this parser, delimiter, record, or vector implementation. |
| `0x00588720` in `NewSystemMessagePane::NewSystemMessagePane` | Embedded `SpelledPane` at caller `this+0xf8`; empty 256-wide-character buffer and length zero. | Initializes the embedded parser/vector/display state from empty input during construction. |

Live `xrefs_to(0x0056c0e0)` reports exactly these two code callers and `more:false`. The source signature is reconstructed as a wide source pointer plus character count because the parser writes those values into the stack LineIterator backing-pointer and length fields.

## Vector Clear And Iterator Setup

1. Read begin/end/capacity from `SpelledPane+0x178/+0x17c/+0x180`.
2. Destroy the String subobject at offset zero of each 12-byte existing record.
3. Set vector end equal to begin while retaining capacity.
4. Construct a 16-byte stack [UID:000078][LineIterator](by-class/LineIterator.md) with compiler vptr, input pointer, unsigned wide-character length, and offset zero. UID0000KQ is only the non-source historical family index.
5. Call [UID:000190][0x004f3020-0x004f3060.LineIteratorReadNextLine](by-memory/0x004f3020-0x004f3060.LineIteratorReadNextLine.md) for the first line.

The LineIterator result includes a consumed UTF-16 LF in the returned range. The numeric conversion therefore naturally stops at LF as a nonnumeric character; the parser does not need or contain a separate LF trim branch.

## Parse Branches And Split Semantics

| Condition/operation | Exact behavior |
| --- | --- |
| Current line is empty | Terminate the loop immediately. A first blank row ends the serialized list; later rows are not consumed. |
| `FindLastOf(delimiters, npos, wcslen(delimiters))` returns `-1` | Skip the line without appending a record. |
| Delimiter found | The last space or tab separates text from the numeric suffix. |
| `FindLastNotOf` before the delimiter | Add one to the result and use it as the text-prefix length, trimming the final separator run while preserving internal spaces. |
| Line contains only delimiters | `FindLastNotOf` returns `-1`; adding one yields a zero-length text prefix. |
| Numeric suffix invalid or empty | Existing `ToLong`/CRT conversion behavior supplies zero; no parser-specific rejection or error branch exists. |
| Line has trailing separators after a numeric token | The last separator is selected, so the suffix after it controls conversion; do not invent a separate trailing-whitespace cleanup pass. |

The delimiter backing object is [UID:000284][0x0066fe44-0x0066fe4a.SpelledPaneWhitespaceDelimiter](by-memory/0x0066fe44-0x0066fe4a.SpelledPaneWhitespaceDelimiter.md), exact bytes `20 00 09 00 00 00`. Its writable `.data` placement, constant image initialization, one-method-only use, and four object/length refs support the function-local `static wchar_t delimiters[]` declaration. The target data page emits only a covered-by comment; this method owns the one source declaration.

## Record Layout And Append Paths

| Record offset | Reconstructed role | Binary behavior |
| --- | --- | --- |
| `+0x00` | `entry.text` wide String | Default-constructed, assigned the prefix substring, then copy/share-constructed into vector storage. |
| `+0x04` | `entry.value` integer | Assigned from suffix `ToLong` conversion. |
| `+0x08` | `entry.group` / third scalar | Stack slot `[ebp-0x1c]` is copied but has no defining store in the function; remains indeterminate. |

When capacity remains, the method copy/share-constructs the String at vector end, copies the two scalars, and advances end by 12. When capacity is full, it calls [UID:00039O][0x00572fd0-0x00573232.SpelledPaneEntryVectorInsert](by-memory/0x00572fd0-0x00573232.SpelledPaneEntryVectorInsert.md) at `0x0056c26d` with the same local record. Source-level `m_entries.push_back(entry)` is the correct human-written form; the exact helper remains separately documented compiler/STL support and must not be duplicated in this body.

## String And Lifetime Inventory

| Address/helper | Accepted role in this method |
| --- | --- |
| `0x00582b70` | Destroy/release each existing, local, line, and substring String. |
| `0x004f3020` | `LineIterator::ReadNextLine`, called at `0x0056c15b` and `0x0056c285`. |
| `0x005845f0` | String `empty` test controlling loop termination. |
| `0x005848a0` | Wide `FindLastOf`. |
| `0x00584820` | Wide `FindLastNotOf`. |
| `0x00582b20` | Default construction of the local entry String. |
| `0x00584d00` | Wide substring construction for prefix and suffix. |
| `0x00582c60` | String assignment for entry text and next-line replacement. |
| `0x00582670` | Wide String `ToLong`. |
| `0x00582a90` | String copy/share construction in the in-capacity append path. |
| `0x00572fd0` | Vector growth/insert helper for the full-capacity path. |
| `0x0056bea0` | `SpelledPane::RebuildDisplayText`, always called after parser cleanup. |

C++ EH state transitions protect the current line, local entry String, substring temporaries, next-line temporary, and completed stack LineIterator. The LineIterator cleanup action calls retained UID0001HL through a compiler thunk; human source uses the inline empty virtual destructor and does not add an explicit parser-side teardown call.

## Delimiter Source Shape

Ranked source dispositions:

1. Function-local static mutable wide array: accepted. Writable `.data`, constant initialization, no runtime guard/initializer, one-method-only refs, and two runtime `wcslen` scans match the emitted body.
2. Two direct literal expressions: plausible but weaker because comparison literals are in `.rdata` and would repeat the lexical token in source.
3. File-static mutable array: binary-compatible but unnecessarily broad because no second function uses it.
4. Const local/file array: weaker because the object is in writable `.data`, unlike comparison literals.
5. Class static, shared macro object, exported global, or raw `asc_66FE44`: unsupported and rejected.

The local identifier `delimiters` is inferred; the storage bytes and use count are exact. No source or generated destination should declare a second delimiter object.

## Positive Evidence

- Healthy IDA MCP session `supervisor_nexustk_20260713` on 2026-07-13 returned `status:ok`, ready analysis/Hex-Rays/strings, and `lookup_funcs(0x0056c0e0)` size `0x1fb`.
- Decompilation and disassembly agree on exact vector fields, four-dword LineIterator setup, search order, substring ranges, 12-byte append stride, helper calls, EH cleanup, and final rebuild.
- Target delimiter xrefs are exactly `0x0056c174`, `0x0056c199`, `0x0056c1b1`, and `0x0056c1d1`, all inside this method.
- Parser callers are exactly `0x0057068e` and `0x00588720`.
- Every source-facing helper semantic used in the formal body is already documented by the LineIterator, StringBase, or SpelledPane vector-support pages.

## Negative And Historical Evidence

- No delimiter pointer slot, RVA-form pointer, write, runtime initializer, second SpelledPane consumer, or external owner exists.
- No branch validates the numeric suffix, rejects conversion zero, initializes the third scalar, or continues past the first empty line.
- No evidence supports LF/CR/comma/colon as delimiters for this parser.
- Do not reproduce decompiler labels, manual vector pointer arithmetic, explicit String destructor calls, EH state variables, vtable stores, or the compiler vector helper in human source.
- Historical aggregate/class/file wording treated the delimiter only as broad static parser data and deferred this exact body. This child resolves local scope, mutable type, source declaration, branches, record behavior, and emitter position while preserving the earlier evidence as historical context.

## Source Placement And Reconstruction

- Emit through [UID:0000DK][SpelledPane](by-class/SpelledPane.md) at position `50` into the [UID:0000NZ][SpelledPane](by-file/SpelledPane.md) source family.
- Keep parent [UID:0001HA][0x0056bb20-0x0056c3f1.SpelledPane](by-memory/0x0056bb20-0x0056c3f1.SpelledPane.md) as a blank-C++ partial aggregate/index. This exact child is the only parser body.
- Keep UID000284 as a covered-by/no-duplicate data page at position `40`; its local static declaration occurs only in this formal method block.
- Emit the complete UID000078 LineIterator helper earlier in this same SpelledPane source file, while preserving its class ownership and existing UID000190 body. Calls here are source-facing dependencies, not ownership transfers.

## Score Rationale

Completion is `92` because exact range, two callers, all parser branches, vector clear/append/growth behavior, delimiter declaration, line/string lifetimes, EH, record layout, uninitialized third scalar, helper inventory, owner/emitter/position, source placement, rejected alternatives, and complete formal C++ are documented.

Confidence is `94` because runtime-critical facts come from healthy current IDA and agree with existing LineIterator/StringBase/vector support. It remains below 95 because original method/local/field identifiers and exact local-versus-file lexical scope are inferred.

## Cross-References

- [UID:0001HA][0x0056bb20-0x0056c3f1.SpelledPane](by-memory/0x0056bb20-0x0056c3f1.SpelledPane.md)
- [UID:0000DK][SpelledPane](by-class/SpelledPane.md)
- [UID:0000NZ][SpelledPane](by-file/SpelledPane.md)
- [UID:000284][0x0066fe44-0x0066fe4a.SpelledPaneWhitespaceDelimiter](by-memory/0x0066fe44-0x0066fe4a.SpelledPaneWhitespaceDelimiter.md)
- [UID:000190][0x004f3020-0x004f3060.LineIteratorReadNextLine](by-memory/0x004f3020-0x004f3060.LineIteratorReadNextLine.md)
- [UID:00039O][0x00572fd0-0x00573232.SpelledPaneEntryVectorInsert](by-memory/0x00572fd0-0x00573232.SpelledPaneEntryVectorInsert.md)
- [UID:0003QZ][0x005700e0-0x0057092c.SelfLookPane2OnActivate](by-memory/0x005700e0-0x0057092c.SelfLookPane2OnActivate.md)
- [UID:0001J6][0x00584ea0-0x0058af3b.SystemMessagePanes](by-memory/0x00584ea0-0x0058af3b.SystemMessagePanes.md)

## Changes

- 2026-07-14 Agent-B003 UID0003QZ callback support sync: corrected caller `0x0057068e` from historical `SelfLookPane2::OnDraw` to exact `SelfLookPane2::OnActivate`; recorded the caller's 256-wide fixed member buffer, TAB-to-LF conversion, copied-buffer length, and consumer-only ownership boundary. Parser score, owner/emitter, position, formal body, delimiter scope, record semantics, second caller, and all broader evidence remain unchanged.

- 2026-07-13 Agent-B001 accepted UID000284 callback: created the exact parser child before parent/support linking, with complete live-IDB behavior, one parser-local mutable delimiter declaration, exact caller/branch/lifetime/vector/record evidence, preserved indeterminate third scalar, owner/emitter position, score rationale, negative evidence, and formal C++.

## 2026-07-15 B003 UID0001HL Parser-Lifetime Synchronization

- Parser metadata remains exactly `92/94`, owner/emitter UID0000DK, true, position 50, `Nested:0`, exact range/Item Summary, and complete formal body. No parser branch, caller, delimiter, record, vector, String lifetime, helper, or source expression changed.
- LineIterator stack setup remains exact: backing pointer at `+0x04`, length at `+0x08`, offset zero at `+0x0c`, and compiler vptr at `+0x00`. UID000078 now supplies the complete two-argument inline constructor and exact 16-byte class declaration.
- UID000190 remains the out-of-line nonvirtual scanner called at `0x0056c15b` and `0x0056c285`; its unsigned offset/length logic and LF-inclusive result are unchanged.
- UID0001HL exact seven-byte body is live parser cleanup, not a source-authored parser call. FuncInfo at `0x006650b8` has six states; unwind-map entry zero points to thunk `[0x00607b5d,0x00607b65)`, which loads the stack helper at `[ebp-0x34]` and jumps to UID0001HL.
- The normal parser epilogue has no LineIterator destructor call because the explicit inline empty virtual destructor is optimized away. Human source already expresses lifetime through the automatic `LineIterator lines` object; do not add manual destruction.
- Self-only LineIterator RTTI and one scalar-deleting slot prove the source destructor must be explicitly virtual. UID0001HO is compiler-only and non-emitting; UID00038W is compiler data covered by the class declaration.
- Generated order is UID000078 position 10, UID000190 position 20, UID0001HL position 30, UID00038W position 40, then this parser at 50. The complete class must close before all child definitions.
- Preserve the original uninitialized `entry.group`, two runtime delimiter-length scans, line stop/skip rules, parser-local mutable delimiter, vector helper boundary, and all accepted formal text exactly. Do not add vptr/scalar-delete/operator-delete/RTTI/vtable/EH/cookie code, raw addresses, duplicate helper declarations, or decompiler labels.

## 2026-07-29 Supervisor Gate 2B Protected A07 Readback

- Catalog entry `0361` records deliberate no-change protection for A07 over exact range `[0x0056c0e0,0x0056c2db)`. IDA remains `SpelledPane_ParseAndLoadEntries` with prototype `void __thiscall(struct SpelledPane *this, const wchar_t *text, int length)`.
- The exact retained regular function comment is `SpelledPane parser/loader for a wide source buffer and character count. Uses parser-local writable whitespace storage containing space and tab; the third local scalar is intentionally uninitialized in the observed binary.` Address regular/repeatable and function-repeatable channels remain blank.
- No parser name/type/comment/local/frame/body mutation occurred. Protected readback retained size `0x1fb`, raw SHA256 `7CF6DA6C751D6D2A14B0EDA22B0E84A88A26841C56D1E5B2595D691365E1FD06`, callers `0x0057068e` and `0x00588720`, delimiter storage, LineIterator local, EH, and the deliberately uninitialized third entry scalar.
- The shared Gate 2B backup is `E:\NTK\Resources\NexusTK\backups\NexusTK.exe.pre-B001-UID0001HA-20260729-135636.i64`, byte-identical to prestate `03F10207763C01455EA5F767F330A371FEDD9885FBFD98705F5A486985DBE5DA`. Current session `1da2b2ae` is saved as SHA256 `0E0AF9383DE743CB91E076959498A0D9DAF906C5CA6E620F99425931A09AB481`, 143,190,636 bytes, at `2026-07-29T13:59:50.138-04:00`.

## 2026-07-29 Shared IDB Currentness Recheck

- `0E0AF938...AB481` is the historical UID0001HA applied checkpoint and `03F102...DBE5DA` plus earlier saves are historical. Current authoritative session `1da2b2ae` is `3C8F31781C94DF74AADDB65B3D944CD2CA4387C448918BBBFFD60B41C3625B69`, 143,191,631 bytes, saved `2026-07-29T14:12:44.7229687-04:00`.
- Read-only recheck preserved protected A07 exactly: name, prototype, function comment, body hash, two incoming calls, four delimiter refs, parser bytes, indeterminate scalar, and padding. No UID0001HA dependency drift was found; Agent-B001 did not mutate/save IDA.
- Currentness supersession: `3C8F3178...5B69` is historical; current authoritative session `1da2b2ae` is `296ED21C6665B7C0D8EFBC515B137101F8506FD945C6C7E14C3E6F655063CF8B`, 143,190,676 bytes, last write `2026-07-29T14:22:15.9735862-04:00`, with protected A07 still exact.
- Currentness supersession: `296ED21C6665B7C0D8EFBC515B137101F8506FD945C6C7E14C3E6F655063CF8B` and every earlier identity are now dated history. Current authoritative session `1da2b2ae` is saved SHA256 `905D1AB131C953911CD1DF5E536AF36EAECFA67486E2F800907B36C0673604C1`, 143,190,814 bytes, last write `2026-07-29T14:42:53.5091352-04:00`; bounded read-only target/dependency readback again found zero UID0001HA drift across exact names, ranges, prototypes, four comment channels, body hashes, xref counts, padding, callback slots, literals, helper identities, and relevant type state. A02/A03 retain exact typed analysis with the same bounded Hex-Rays failures at `0x0056bc15` and `0x0056bc84`. Agent-B001 did not mutate or save IDA.
