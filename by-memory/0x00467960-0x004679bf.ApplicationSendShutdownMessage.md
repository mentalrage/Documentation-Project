*** UID:0002GX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static const unsigned int kPaneTagGoog = 0x676f6f67;

void Application::SendShutdownMessage()
{
    Message message(kPaneTagGoog);
    ForwardMessage(&message);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready Application shutdown-message helper; constructs stack Message tag 0x676f6f67, forwards through Application::ForwardMessage, and treats the apparent v2[5] write as compiler EH state rather than payload. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00467960-0x004679bf Application SendShutdownMessage

## Scope

- Range: `0x00467960-0x004679bf`
- Entity kind: class method
- Owner: [UID:00000D][Application](by-class/Application.md)
- Autogen parent: [UID:00000D][Application](by-class/Application.md), position `140`
- Range placement: exact Application method physically interleaved inside [UID:0000YY][0x00467510-0x00467b2d.BackPaneAndBackGroundPane](by-memory/0x00467510-0x00467b2d.BackPaneAndBackGroundPane.md); not contained by the corrected [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) aggregate.
- Disposition: reconstructable project code.
- Rebuild handling: source-authored and formal-C++-ready. The accepted source shape is a stack `Message` constructed with `kPaneTagGoog`, forwarded with `Application::ForwardMessage`, and destroyed by normal automatic-object scope exit. The compiler EH state write and unwind cleanup are documented as generated code, not source statements.

## Behavior

`Application::SendShutdownMessage` builds a small application message with ID/tag `0x676f6f67`, forwards it through `Application::ForwardMessage`, and destroys the temporary stack message object on scope exit. The accepted source-facing tag name for this target is `kPaneTagGoog = 0x676f6f67`; `0x676f6f67` converts to decimal `1735356263` and ASCII bytes `67 6f 6f 67` / `goog` (Verified with `int_convert.py`).

This is an internal application/ChangeMan message path, not a Win32 `SendMessage` wrapper and not direct shutdown-handle API use.

The historical Hex-Rays local `_DWORD v2[6]` and `v2[5] = 0` should not be read as a source-level message payload/status clear. Disassembly and EH metadata show the write at `0x00467998` targets the compiler C++ EH state slot `[ebp+var_4]` after the `Message` constructor succeeds. The source-level local is `Message message(kPaneTagGoog);`.

## IDA Evidence

- Accepted B006 report used mandatory IDA MCP session `da0dc4df-0193-4da8-a3d7-b5f14a7a040d` against active IDB session `80de0a67` at `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned OK with auto-analysis ready, Hex-Rays ready, strings cache ready, and worker PID `26892`. One broad `tools/list` schema extraction and one broad text search for `676F6F67` timed out; read-only listener/process checks and a scoped `server_health` recheck remained OK, so the report used scoped MCP calls rather than fallback-only evidence.
- IDA MCP `lookup_funcs` reports `sub_467960` at `0x00467960` with size `0x5f` / 95 bytes (Verified with `int_convert.py`), giving the exclusive range end `0x004679bf`.
- IDA MCP `lookup_funcs` on 2026-06-12 reconfirmed `sub_467960` at `0x00467960`, size `0x5f`, and `0x004679bf` as the exclusive end before the next BackPane function at `0x004679c0`.
- B001-FB-002 endpoint audit on 2026-06-12 (`a001-b276`) checked the boundary directly: `lookup_funcs(0x004679be)` still resolves inside `sub_467960`, `lookup_funcs(0x004679bf)` returns no function, `lookup_funcs(0x004679c0)` returns `sub_4679C0` with size `0x1d`, and `entity_query 0x00467900-0x00467a20` lists the adjacent functions at `0x00467960`, `0x004679c0`, and `0x004679e0`. The same audit recorded compiler EH cleanup chunks outside the local main-body range, but those chunks do not change this method's source range or justify merging it into the corrected ApplicationLifecycle aggregate.
- IDA MCP `decompile` constructs a stack object through `sub_520E30(v2, (void *)0x676F6F67)`, writes `v2[5] = 0`, calls `sub_4F4B70(this, v2)`, then destroys the stack object through `sub_520ED0(v2)`. The decompiler local is an artifact: the `v2[5] = 0` store corresponds to compiler EH state, not a source message field.
- Target disassembly records `0x00467988` pushing `676F6F67h`, `0x00467990` calling `sub_520E30` / `Message::Message`, `0x00467998` writing `0` to `[ebp+var_4]` EH state, `0x004679a2` calling `sub_4F4B70` / [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md), `0x004679aa` calling `sub_520ED0` / `Message::~Message`, and `0x004679be` returning.
- EH cleanup for the automatic `Message` local is outside the source range at `0x005faa0c/0x005faa0f`, where the cleanup loads the local message address and jumps to `sub_520ED0`; handler data is at `0x005faa14`.
- IDA MCP `analyze_function 0x00467960` on 2026-06-12 reports caller `sub_5047F0` at `0x00504a7e`, callees `sub_520E30`, [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md), and `sub_520ED0`, and the same `0x676f6f67` stack-message sequence.
- The `sub_4F4B70` target is the documented [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) wrapper.
- Direct caller evidence from `MapPane::ExitToMenu` / `sub_5047F0` shows `0x00504a74` loading `dword_67A740`, `0x00504a7a` null-checking it, `0x00504a7e` calling this Application helper, `0x00504a83` pushing `dword_67A740`, and `0x00504a8b` queuing that pane through `sub_469180`. This caller context does not transfer ownership to MapPane or BackPane.
- Support-function facts checked by B006: [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) is `sub_4F4B70`, size `0x16` / 22 bytes (Verified with `int_convert.py`), and forwards `(this, message)` to ChangeMan dispatch; [UID:0000ZZ][0x0047ee20-0x0047ee95.ChangeManDispatchMessage](by-memory/0x0047ee20-0x0047ee95.ChangeManDispatchMessage.md) is size `0x75`, matches owner pointer, handles broadcast scope, compares registered message type with `*(message + 4)`, and invokes listener virtual slot `+0x08`; `Message` constructor `0x00520e30` is size `0x9b`, stores the Message vtable/type/list; `Message` destructor `0x00520ed0` is size `0x25`, resets the vtable, deletes the entries list if present, clears the pointer, and calls the base destructor.
- Consumers of the same `0x676f6f67` message tag include `0x00504b70`, which checks `*(message + 4) == 0x676f6f67`, and `0x004b73b0`, which independently consumes the same tag before queueing/clearing pane slots.

## Message Object Evidence

| Evidence | Meaning |
| --- | --- |
| `sub_520E30(v2, (void *)0x676F6F67)` at `0x00467990` | Constructs the stack `Message` object using the pane-notification tag/ID. |
| `mov [ebp+var_4], 0` at `0x00467998` | Compiler C++ EH state write after successful construction; not a source payload/status field and not part of `Message` source layout. |
| `sub_4F4B70(this, v2)` at `0x004679a2` | Routes the message through documented `Application::ForwardMessage`. |
| `sub_520ED0(v2)` at `0x004679aa` and EH cleanup `0x005faa0c/0x005faa0f` | Destroys the temporary stack message during normal scope exit and exception unwind. |
| `Message` object layout from support docs | Source-visible `Message` state is vtable/base state at `+0`, `MessageType m_type` at `+4`, and `SortedList *m_entries` at `+8`; Hex-Rays' six-dword stack local includes compiler bookkeeping. |

## Source Placement Evidence

| Relationship | Evidence |
| --- | --- |
| Application ownership | Autogen parent `00000D`, the Application class/file pages, and the dispatch through [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) place this method in the `Application` method set. |
| Message dispatch dependency | The only dispatch call is [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md), which forwards app-scoped messages through `ChangeMan`. |
| Boundary confidence | IDA MCP reports the exact start `0x00467960` and size `0x5f`; the page range ends at exclusive `0x004679bf`. |
| Reconstruction readiness | The method body has a direct source-level reconstruction: `Message message(kPaneTagGoog); ForwardMessage(&message);`. Constructor/destructor and EH cleanup are generated from the stack object lifetime. |

## Source-Name And Owner Decisions

- Use `static const unsigned int kPaneTagGoog = 0x676f6f67;` for this target. The tag value is strongly tied to the `goog` pane notification consumers, but a final project-wide `MessageType` enum member is not proven enough in this target.
- Do not use a C++ multi-character literal such as `'goog'` in the emitted body. Existing Message and pane reports warn that source syntax is not proven, so the integer literal plus source-facing constant is the safer first-draft form.
- Keep owner and emitter as [UID:00000D][Application](by-class/Application.md). [UID:0000YY][0x00467510-0x00467b2d.BackPaneAndBackGroundPane](by-memory/0x00467510-0x00467b2d.BackPaneAndBackGroundPane.md) is a physical mixed index only; [UID:00007Q][MapPane](by-class/MapPane.md) is caller context only; [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) ends before this target; [UID:000084][Message](by-class/Message.md) owns constructor/destructor semantics but not this wrapper.

## Cross-Document Evidence

| Source | Confirmation |
| --- | --- |
| [UID:00000D][Application](by-class/Application.md) | Lists `SendShutdownMessage` as an exact child method and describes the `0x676f6f67` message construction plus `ForwardMessage` dispatch. This page now clears `85/85`, and the parent class is `85/89`, so the existing autogen parent remains valid under the current gate. |
| [UID:0000HG][Application](by-file/Application.md) | Places `0x00467960` with the other exact Application accessor/helper methods split from the lifecycle band and assigned to `NexusTK/app/Application.cpp`. |
| [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) | No longer contains this function after the 2026-06-12 range repair. The lifecycle aggregate stops at `0x004674ed` to avoid crossing the BackGroundPane/BackPane source island. |
| [UID:0000YY][0x00467510-0x00467b2d.BackPaneAndBackGroundPane](by-memory/0x00467510-0x00467b2d.BackPaneAndBackGroundPane.md) | Keeps this function as a cross-owned Application child inside a non-emitting mixed index, not as BackPane source. |
| [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) | Documents the dispatch wrapper called by this method and ties it to the global ChangeMan message route, matching the local `sub_4F4B70(this, v2)` call. |
| Autogen memory coverage | Shows this UID assigned to parent `00000D` at position `140` with no emitted C++ body, matching the current below-95 reconstruction policy. |

## Reconstruction Notes

The exact broad project `MessageType` enum member remains open, but that no longer blocks this target's first-draft C++. The source-ready body uses the accepted local constant `kPaneTagGoog`, a stack `Message`, and `ForwardMessage(&message)`.

Completion is `88` because the exact range, stack message lifecycle, dispatch callee, owning class relationship, caller, mixed-index placement, `0x676f6f67` tag consumers, EH-state correction, and formal C++ are now cross-checked across live IDA MCP plus the Application, Message, ForwardMessage, MapPane, and BackPane docs. Confidence is `92` because ownership and boundaries are range-local and repeat across the documented `Application::ForwardMessage` child, Application class/file pages, direct caller evidence, and BackPane split index. Confidence remains below final audit because the exact original method spelling, constant placement/header exposure, and final project-wide message enum naming are inferred rather than symbol-proven.

Do not reintroduce the stale "payload/status clear" source statement. The apparent Hex-Rays `v2[5] = 0` is compiler EH state `[ebp+var_4]`, and the destructor/EH cleanup are compiler output for the automatic `Message` local.

## Cross-References

- File: [UID:0000HG][Application](by-file/Application.md)
- Class: [UID:00000D][Application](by-class/Application.md)
- Forwarder: [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md)
- Adjacent/mixed index: [UID:0000YY][0x00467510-0x00467b2d.BackPaneAndBackGroundPane](by-memory/0x00467510-0x00467b2d.BackPaneAndBackGroundPane.md)
- Corrected lifecycle aggregate: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)

## Changes

- 2026-06-12 Agent-C001 boundary/routing refresh:
  - What existed before: this exact Application method was treated as a contained child of UID `0000YR` while that lifecycle aggregate used range `0x00463310-0x004679bf`, which forced the reconstructable aggregate to cross [UID:0000YY][0x00467510-0x00467b2d.BackPaneAndBackGroundPane](by-memory/0x00467510-0x00467b2d.BackPaneAndBackGroundPane.md).
  - What changed: raised `76/84` to `85/88`, kept direct parent [UID:00000D][Application](by-class/Application.md), replaced lifecycle containment wording with mixed-index placement, and documented that the corrected Application lifecycle aggregate ends at `0x004674ed`.
  - Summary/evidence: live IDA MCP `lookup_funcs` and `analyze_function` on 2026-06-12 reconfirm `sub_467960` size `0x5f`, caller `0x00504a7e`, callees `sub_520E30`, `sub_4F4B70`, and `sub_520ED0`, and the `0x676f6f67` message tag. The parent class is `85/89`, so this exact child now clears the current `85/85` attachment gate without needing physical containment by the lifecycle aggregate.

- 2026-06-12 B001-FB-002 endpoint-audit preservation:
  - What existed before: the earlier endpoint repair report treated the `0x00463310-0x004679bf` ApplicationLifecycle aggregate as the target needing correction from an inclusive-looking `0x004679be` endpoint.
  - What is retained here: the report's range-local facts for this method remain valid: `0x004679be` is inside `sub_467960`, `0x004679bf` is the first byte outside the function, the next function starts at `0x004679c0`, and the body constructs/dispatches/destroys the `0x676f6f67` message through `ApplicationForwardMessage`.
  - Supersession: the broader aggregate recommendation is superseded by the later C001 BackPane-boundary repair, so this method remains an exact Application child physically interleaved inside the BackPane mixed index rather than a contained child of [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md).

- 2026-06-26 B006 source-quality implementation:
  - What existed before: target was `85/88` with blank formal C++ and stale wording that treated `v2[5] = 0` as a payload/status clear while waiting on message-name proof.
  - What changed: raised to `88/92`, preserved `CANONICAL_OWNER:00000D`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00000D`, inserted formal first-draft C++ for `Application::SendShutdownMessage`, and replaced the payload-clear interpretation with the EH-state correction.
  - Summary/evidence: accepted B006 report used MCP session `da0dc4df-0193-4da8-a3d7-b5f14a7a040d` / active IDB `80de0a67`, confirmed target start `0x00467960`, size `0x5f`, exclusive end `0x004679bf`, next function `0x004679c0`, constructor call `0x00467990`, EH state write `0x00467998`, forwarder call `0x004679a2`, destructor call `0x004679aa`, return `0x004679be`, EH cleanup `0x005faa0c/0x005faa0f`, direct caller `MapPane::ExitToMenu` at `0x00504a7e`, and support behavior for `Application::ForwardMessage`, ChangeMan dispatch, Message construction/destruction, and `0x676f6f67` consumers. BackPane, MapPane, ApplicationLifecycle, and Message ownership remain rejected.

- 2026-06-03 cross-documentation consistency pass:
  - What existed before: the exact IDA-derived behavior and parent assignment were documented, but the page did not record the cross-document checks that justify moving it out of the low-completion queue, and the manual coverage row still carried an older `65%` medium entry.
  - What it was changed to: raised `70/82` to `76/84`, added cross-document evidence from the Application class page, Application file page, lifecycle aggregate, forwarder child, and autogen memory coverage, and kept the C++ reconstruction block empty under the below-95 rule.
  - Summary/evidence: existing IDA evidence proves the `0x00467960-0x004679bf` function and stack message sequence; the Application documentation and `Application::ForwardMessage` page confirm owner and dispatch path. Live IDA MCP was unavailable during this pass, so the increase is conservative and based on already written IDA evidence plus documentation consistency checks.
- 2026-06-02 documentation pass:
  - What existed before: the page had the exact behavior and IDA evidence, but retained a provisional C++ snippet using unproven message helper/type names.
  - What it was changed to: raised `65/75` to `70/82`, removed the provisional reconstruction block, and added message-object, source-placement, and score-rationale evidence.
  - Summary/evidence: IDA MCP evidence remains the exact constructor/tag, payload clear, `Application::ForwardMessage` call, and temporary destructor; existing Application parent documentation supports ownership.
- 2026-05-31 initial split:
  - What existed before: this method was only listed inside the broad Application lifecycle aggregate.
  - What it was changed to: created this exact child method page and attached a conservative range-local C++ snippet under `Application`.
  - Summary/evidence: IDA MCP `lookup_funcs` and `decompile` confirm the `0x00467960` boundary, stack message object construction with `0x676f6f67`, zero payload/status write, `Application::ForwardMessage` call, and destructor call.
