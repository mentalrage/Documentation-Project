*** UID:0002CN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated scalar deleting destructor for CashShopVersionRequest.
// Emitted code for this range is covered by [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact compiler-generated scalar deleting destructor for CashShopVersionRequest: vtable slot `0x0060d798` points to this `0x24`-byte wrapper, which restores the class vptr, conditionally invokes the shared delete helper when flag bit 1 is set, and returns `this`. Human source is the inline empty virtual destructor in the BaseRequest-derived CashShopVersionRequest declaration emitted through FileDownloader; formal CPP therefore carries only compiler-generated/covered-by comments and formal H remains blank because this range owns no independent declaration. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0041b5e0-0x0041b604 CashShopVersionRequest Scalar Deleting Destructor

## Summary

- Address range: `0x0041b5e0-0x0041b604`
- Entity kind: scalar deleting destructor
- Owner: [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md)
- Active source module: [UID:0000JC][FileDownloader](by-file/FileDownloader.md); [UID:0000I1][CashShopVersionRequest](by-file/CashShopVersionRequest.md) remains a path-`NONE` historical alias.
- Rebuild handling: `source-authored` class destructor shape plus compiler-emitted scalar-deleting wrapper.
- Autogen parent: [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md)
- Disposition: reconstructable class destructor semantics; do not hand-author this scalar deleting wrapper as standalone final C++.

## Item Summary

- The exact executable range is `[0x0041b5e0,0x0041b604)`, a `0x24`-byte MSVC scalar deleting destructor selected by the sole `CashShopVersionRequest` vtable slot at `0x0060d798`. Five `0xcc` bytes precede it and twelve `0xcc` bytes separate it from the `FileDownloader` scalar deleting destructor at `0x0041b610`.
- The wrapper tests deleting flag bit `1`, restores `CashShopVersionRequest::vftable` through `this`, conditionally calls the shared delete/free helper `sub_5C7526` with the observed `0x10` object size, returns `this`, and performs no derived-field cleanup because the BaseRequest-derived payload has no additional fields.
- The human-source cause is the inline empty virtual destructor in [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md), whose declaration is coordinated through [UID:0000JC][FileDownloader](by-file/FileDownloader.md). The wrapper itself is compiler ABI output, not a standalone source-authored function body.
- Formal CPP contains only `// Compiler-generated scalar deleting destructor for CashShopVersionRequest.` followed by the covered-by comment through [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md). These comments preserve exact ownership/routing without attempting to hand-author compiler glue. Formal H remains blank because this by-memory range contributes no declaration beyond the owning class destructor already expressed by the class H channel.
- Direct ownership by `FileDownloader`, `FittingRoom`, generic runtime support, or a no-owner route remains rejected: those paths trigger, queue, or implement shared deletion, while the vtable cell, vptr restore, and object layout identify `CashShopVersionRequest` as the semantic owner. Explicit table bytes, a standalone scalar-wrapper body, and obsolete standalone `CashShopVersionRequest.cpp/.h` output are likewise rejected.

## Behavior

This function restores the `CashShopVersionRequest` vtable and conditionally deletes the object when the scalar-deleting-destructor flag bit is set. It has no owned heap fields or nested payload cleanup in the current IDA decompilation; the ordinary source-level destructor remains semantically empty.

## Raw Code Evidence

IDA MCP `lookup_funcs` on 2026-06-15 reports `sub_41B5E0` at `0x0041b5e0-0x0041b604` with size `0x24`.

IDA MCP decompilation:

```cpp
_DWORD *__thiscall sub_41B5E0(_DWORD *Block, char flags)
{
  *Block = &CashShopVersionRequest::`vftable';
  if ((flags & 1) != 0)
    sub_5C7526(Block);
  return Block;
}
```

Live IDA bytes for the destructor neighborhood confirm the modeled wrapper and both neighboring alignment gaps:

```text
0x0041b5db-0x0041b5e0: five 0xcc padding bytes after CashShopRequest scalar deleting destructor
0x0041b5e0-0x0041b604: CashShopVersionRequest scalar deleting destructor
0x0041b604-0x0041b610: twelve 0xcc padding bytes before FileDownloader scalar deleting destructor
```

## Source-Level Shape And Autogen Decision

This page intentionally emits no handwritten wrapper body. Its formal CPP channel contains only compiler-generated/covered-by comments, while the formal H channel remains blank. The source-level destructor is empty at the semantic level, and the observed binary body is the compiler scalar-deleting wrapper that resets the vtable and optionally calls the delete helper. The owning class declaration/destructor should cause the compiler to regenerate this wrapper.

The 2026-08-03 coordinated route supplies the final class relationship without changing this no-body disposition. [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md) is a `0x10` [UID:00050F][BaseRequest](by-class/BaseRequest.md)-derived class with no derived fields and an inline empty virtual destructor, emitted through [UID:0000JC][FileDownloader](by-file/FileDownloader.md). That declaration is the human-source cause for the vtable slot and this compiler wrapper; this page remains `88/93`, class-owned/reconstructable, and routes only explanatory no-code comments rather than hand-authoring ABI glue.

## Live IDA Recheck

- 2026-06-03 xrefs show `0x0060d798` contains `dd offset sub_41B5E0`, making this the only `CashShopVersionRequest` vtable slot.
- The adjacent dword at `0x0060d794` points to `??_R4CashShopVersionRequest@@6B@`, while the next RTTI/vtable pair at `0x0060d79c/0x0060d7a0` belongs to `CashShopRequest`.
- The only code xrefs to the `0x0060d798` vtable are the payload allocation helper at `0x0041b2a4` and this destructor at `0x0041b5ea`.
- 2026-06-15 `server_health` reports active `NexusTK.exe`, image base `0x00400000`, Hex-Rays ready, and strings cache ready.
- 2026-06-15 `lookup_funcs` reconfirms `sub_41B5E0` at size `0x24`; `analyze_function` reports prototype `_DWORD *__thiscall(_DWORD *Block, char)`, three basic blocks, no direct code callers, one data xref to `0x0060d798`, and the sole callee `sub_5C7526`.
- 2026-06-15 `decompile` shows only `*Block = &CashShopVersionRequest::vftable`, destructor flag bit `1`, optional `sub_5C7526(Block)`, and return `Block`.
- 2026-06-15 `xrefs_to 0x0060d798` reports exactly two data refs: payload setup `0x0041b2a4` in `sub_41B270` and vtable restore `0x0041b5ea` in this wrapper.
- 2026-06-15 `.rdata` bytes at `0x0060d794` decode `0x0064037c, 0x0041b5e0, 0x00640418, 0x0041b570, 0x00640464, 0x0041b610`, matching the CashShopVersionRequest/CashShopRequest/FileDownloader vtable sequence.

## 2026-06-27 B005 MCP Source-Quality Recheck

B005 rechecked this target against live IDA MCP session `b001_000241_20260627`. `server_health` reported module `NexusTK.exe`, image base `0x00400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and the worker-backed session active and not analyzing. No IDA database edits were made.

Function and boundary facts from the accepted recheck:

| Address | Current MCP result |
| --- | --- |
| `0x0041b570` | `sub_41B570`, size `0x6b`, neighboring `CashShopRequest` scalar deleting destructor |
| `0x0041b5db` | not a function; five `0xcc` bytes before this target |
| `0x0041b5e0` | `sub_41B5E0`, size `0x24`, this scalar deleting destructor wrapper |
| `0x0041b604` | not a function; start of twelve `0xcc` bytes after this target |
| `0x0041b610` | `sub_41B610`, size `0x8f`, neighboring `FileDownloader` scalar deleting destructor |
| `0x0041b270` | `sub_41B270`, size `0x59`, version-request submit helper |
| `0x005c7526` | `sub_5C7526`, size `0x0e`, shared delete/free helper used only by the deleting branch here |

The target assembly is the scalar-deleting-destructor wrapper shape:

```text
41b5e0  push ebp
41b5e1  mov ebp, esp
41b5e3  test [ebp+arg_0], 1
41b5e7  push esi
41b5e8  mov esi, ecx
41b5ea  mov dword ptr [esi], offset ??_7CashShopVersionRequest@@6B@
41b5f0  jz loc_41B5FD
41b5f2  push 10h
41b5f4  push esi
41b5f5  call sub_5C7526
41b5fa  add esp, 8
41b5fd  mov eax, esi
41b5ff  pop esi
41b600  pop ebp
41b601  retn 4
```

The corresponding decompile remains:

```cpp
_DWORD *__thiscall sub_41B5E0(_DWORD *Block, char a2)
{
  *Block = &CashShopVersionRequest::`vftable';
  if ((a2 & 1) != 0)
    sub_5C7526(Block);
  return Block;
}
```

Byte and padding proof:

```text
0x0041b5db-0x0041b5e0: cc cc cc cc cc
0x0041b5e0-0x0041b604: 55 8b ec f6 45 08 01 56 8b f1 c7 06 98 d7 60 00 74 0b 6a 10 56 e8 2c bf 1a 00 83 c4 08 8b c6 5e 5d c2 04 00
0x0041b604-0x0041b610: cc cc cc cc cc cc cc cc cc cc cc cc
```

The `.rdata` sequence at `0x0060d790` proves this wrapper is the `CashShopVersionRequest` vtable slot and is bracketed by the neighboring cash-shop/downloader vtable records:

| Address | Value | Meaning |
| --- | --- | --- |
| `0x0060d794` | `0x0064037c` | `CashShopVersionRequest` RTTI/COL |
| `0x0060d798` | `0x0041b5e0` | this scalar deleting destructor slot |
| `0x0060d79c` | `0x00640418` | `CashShopRequest` RTTI/COL, next record begins |
| `0x0060d7a0` | `0x0041b570` | `CashShopRequest` scalar deleting destructor slot |
| `0x0060d7a4` | `0x00640464` | `FileDownloader` RTTI/COL |
| `0x0060d7a8` | `0x0041b610` | `FileDownloader` scalar deleting destructor slot |

Xref proof:

- `xrefs_to 0x0041b5e0` reports the vtable data xref from `0x0060d798` and no ordinary source-code caller.
- `xrefs_to 0x0060d798` reports the payload vtable store at `0x0041b2a4` and the wrapper vtable restore at `0x0041b5ea`.
- `xref_query 0x0041b5f5` routes the optional deleting branch to `sub_5C7526`.

Submit-helper and dispatcher proof:

- `sub_41B270` allocates `0x10`, zeroes payload fields at `+0x04`, `+0x08`, and `+0x0c`, writes `CashShopVersionRequest::vftable` at `0x0041b2a4`, and posts FileDownloader message `10002` through `sub_596960`.
- The allocation-failure path in the same helper still posts message `10002` with a null payload.
- `xrefs_to 0x0041b270` reports the fitting-room constructor call at `0x0041c21b`, after that caller reads FileDownloader/download-dispatcher singleton `0x0067a738`.
- `sub_41B110` handles case `10002` by calling `DownloadCashShopVersion` and then destroying the payload through `(**Block)(Block, 1)`, which reaches this vtable slot with deleting flag bit `1`.

Rejected alternatives:

- FileDownloader is rejected as this page's direct semantic owner. FileDownloader owns the queue/worker/message route, but this wrapper is reached through the `CashShopVersionRequest` vtable slot and restores that class vtable.
- FittingRoom is rejected as direct owner. FittingRoom triggers the submit path and consumes the version result; it does not own the payload destructor wrapper.
- No-owner/runtime support is rejected. The target is class-specific MSVC ABI glue tied to the `CashShopVersionRequest` vtable, not a generic runtime helper.
- Hand-authored C++ for `sub_5C7526`, `operator delete`, or an explicit scalar deleting destructor is rejected because it would port compiler glue rather than human source. A class declaration or default/empty virtual destructor should regenerate the wrapper.
- Historical pre-2026-08-03 reason for no covered-by marker: no accepted final class destructor/declaration block precisely owned the wrapper. That reason is superseded now that the coordinated class declaration is accepted. The wrapper remains no-body compiler output, but the formal CPP channel records its compiler-generated and covered-by disposition explicitly.

Generated-output caveat: the coordinated output belongs under `auto-generated/NexusTK/network/FileDownloader.cpp`/`.h`; obsolete standalone `CashShopVersionRequest.cpp`/`.h` paths are not authoritative. Generated tracker/coverage and coordinated source output may lag this page until a supervisor-authorized generated refresh; they must never be edited manually.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | The page records exact live function bounds, decompilation, assembly, vtable-cell bytes, xrefs, sole callee, neighboring padding, adjacent vtable records, submit-helper construction, dispatcher virtual-destroy route, owner/emitter route, no-code proof, and stale generated-output caveat. It is not higher because final class declaration spelling, whether the ordinary destructor was implicit/defaulted/inline, and payload/base-class declaration style remain documented on support pages rather than fully solved here. |
| Confidence | 93 | The wrapper is a tiny IDA-modeled vtable-only target with one behavior branch, and current bytes, xrefs, decompile, assembly, vtable data, submit-helper facts, dispatcher context, and sibling destructor records all agree. Confidence remains below final-audit level because original source declaration spelling and final class/header shape are inferred rather than recovered from debug/source artifacts. |

## Cross-References

- [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md)
- [UID:0000I1][CashShopVersionRequest](by-file/CashShopVersionRequest.md)
- [UID:0000JC][FileDownloader](by-file/FileDownloader.md)
- [UID:00050F][BaseRequest](by-class/BaseRequest.md)
- [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md)
- [UID:0000WJ][0x0041aa00-0x0041b2c9.CashShopVersionAndCatalogDownload](by-memory/0x0041aa00-0x0041b2c9.CashShopVersionAndCatalogDownload.md)

## Changes

- 2026-08-06 B008 UID0003FS C058 implementation callback:
  - Preserved `88/93`, [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md) owner/emitter, exact wrapper bytes/xrefs/behavior, BaseRequest inheritance, and all rejected-alternative history.
  - Added the full report-level Item Summary and the exact formal CPP compiler-generated/covered-by comments; formal H remains blank because the range owns no independent declaration.
  - Superseded the historical all-blank formal-channel rationale and corrected generated placement to the coordinated FileDownloader output without hand-authoring the ABI wrapper.

- 2026-08-03 B008 UID0003FS support sync: preserved `88/93`, class owner/emitter, blank formal C++/H, exact wrapper evidence, and no-body disposition; added only the coordinated FileDownloader module relation and BaseRequest-derived source cause, while historicalizing the former missing-class-declaration rationale.

- Before: the destructor page had strong behavior evidence but no reconstructable/autogen parent metadata.
- After: it is marked reconstructable and attached under [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md).
- Summary/evidence: 2026-06-03 IDA MCP recheck confirms exact function bounds, vtable slot `0x0060d798 -> 0x0041b5e0`, and class RTTI adjacency, so parent attachment is high-confidence.
- 2026-06-15 C001 section-container continuation:
  - Before: `80/88`; the page had valid owner metadata but sparse current evidence and a misleading standalone C++ candidate section for a compiler scalar deleting wrapper.
  - Changed to: `85/90`; owner/emitter remain [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md) and final reconstruction C++ remains blank.
  - Summary/evidence: live IDA MCP reconfirmed exact `0x24` bounds, vtable-only reachability from `0x0060d798`, payload-setup/destructor vtable-cell xrefs, sole delete-helper callee, decompiled empty-source-destructor semantics, `.rdata` vtable sequence bytes, and padding before/after the wrapper.
- 2026-06-27 B005 source-quality implementation:
  - Before: `85/90`; the page had correct owner/emitter and blank C++ disposition but lacked current B005 MCP session provenance, full assembly, byte listing, dispatcher virtual-destroy proof, sibling wrapper table, rejected alternatives, and generated-output caveat.
  - Changed to: `88/93`; owner/emitter remain [UID:00001I][CashShopVersionRequest](by-class/CashShopVersionRequest.md), `RECONSTRUCTABLE:TRUE` remains, and final reconstruction C++ remains blank.
  - Summary/evidence: MCP session `b001_000241_20260627` reconfirmed the exact target and neighbor boundaries, deleting-flag test, vtable restore, `push 10h`/`push esi`/`sub_5C7526` branch, `retn 4`, five bytes of leading and twelve bytes of trailing `0xcc`, `CashShopVersionRequest`/`CashShopRequest`/`FileDownloader` vtable adjacency, submit-helper construction of the `0x10` payload, and dispatcher message `10002` destruction through `(**Block)(Block, 1)`.
