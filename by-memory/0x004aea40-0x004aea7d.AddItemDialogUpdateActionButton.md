*** UID:0004BP | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000007 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000007 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void AddItemDialog::UpdateActionButton()
{
    ImageButtonControlPane *actionButton =
        GetChild<ImageButtonControlPane>(1);
    ScrollableControlPane *itemListControl =
        GetChild<ScrollableControlPane>(3);
    ListPane *itemList =
        static_cast<ListPane *>(itemListControl->GetScrollablePane());

    if (itemList->GetSelectionCount() > 0)
        actionButton->Enable();
    else
        actionButton->Disable();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: AddItemDialog virtual action-button updater; resolves action control 1 and scroll-wrapper control 3, unwraps the owned ListPane, queries integer selection count, and tail-dispatches Enable or Disable without null checks. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004aea40-0x004aea7d AddItemDialog UpdateActionButton

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact source-authored virtual method split from [UID:00033Q][0x004ae4c0-0x004aea7d.AddItemDialogCore](by-memory/0x004ae4c0-0x004aea7d.AddItemDialogCore.md).
- Owner: [UID:000007][AddItemDialog](by-class/AddItemDialog.md) AddItemDialog.
- Source file: [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md) ItemDialogs.
- Source order: position `40`, after constructor UID0004BM position `10`, destructor UID0004BN position `20`, and OnAction UID0004BO position `30`.
- ABI: `void __thiscall AddItemDialog::UpdateActionButton()`, with `ECX=this`, no explicit arguments, and source `void` behavior supplied by the two virtual tail exits.

## Exact Range, Instructions, And Control Flow

- Exact range is `[0x004aea40,0x004aea7d)`, 61 bytes, 23 instructions, and three CFG blocks: `[0x004aea40,0x004aea77)`, `[0x004aea77,0x004aea7a)`, and `[0x004aea7a,0x004aea7d)`.
- Exact bytes are `56 8b f1 57 6a 01 8b 8e fc 01 00 00 8b 01 ff 50 10 8b 8e fc 01 00 00 6a 03 8b 38 8b 01 ff 50 10 8b 08 8b 89 08 01 00 00 e8 b3 53 04 00 8b 17 8b cf 5f 5e 85 c0 7f 03 ff 62 50 ff 62 4c`; the sequence has one local-PE hit at raw offset `0xADE40`, mapping to VA `0x004aea40`.
- `0x004aea44-0x004aea4e` retrieves manager insertion index `1`; `0x004aea51-0x004aea5d` retrieves insertion index `3`; the returned manager entries are dereferenced at `0x004aea59` and `0x004aea60`.
- `0x004aea62` loads the selector-3 wrapper's owned pane at `+0x108`, and `0x004aea68` calls [UID:0004M9][0x004f3e20-0x004f3e74.ListPaneGetSelectionCount](by-memory/0x004f3e20-0x004f3e74.ListPaneGetSelectionCount.md).
- `test eax,eax` / `jg` selects virtual slot `+0x4c` for a positive count and slot `+0x50` for zero or a nonpositive value. These are distinct `Enable()` and `Disable()` tail calls, not a boolean setter.

## Selector, Type, And Field Evidence

- UID0004BM constructs controls in zero-based manager order: background `0`, action `ImageButtonControlPane` `1`, cancel button `2`, and `ScrollableControlPane` wrapper `3`. The selector values are insertion indexes, not resource ids.
- Selector `1` is dynamically `ImageButtonControlPane`. Selector `3` is the wrapper, not a direct ListPane; `m_scrollablePane` at wrapper offset `+0x108` is dynamically MyItemListPane and generically ListPane.
- The inherited [UID:00003T][DialogPane](by-class/DialogPane.md) `GetChild<T>(int)` contract accounts for the control-manager lookup. [UID:0000CE][ScrollableControlPane](by-class/ScrollableControlPane.md) supplies `GetScrollablePane()` for the exact `+0x108` load.
- The selection query belongs to ListPane and can return values greater than one in multiple-selection mode. `HasSelection` and an independent `SelectionState` owner are rejected.
- ControlPane byte `+0x101` is inherited `m_enabled`, not pane visibility. Pane visibility is separate inherited state at `+0xb4`; ImageButton overrides use `+0x101` to select normal state `0` on enable and disabled state `2` on disable.

## Reachability And Virtual Routing

- No ordinary code xref reaches the method. Its three inbound data references are primary vtable cells: AddItemDialog `0x00619ed8`, AddEmployeeItemDialog `0x00619120`, and AddMixingItemDialog `0x0061a170`, each relative slot `+0x4c`.
- AddItemWithCountDialog excludes this implementation: its corresponding cell `0x0061a038` points to its separate override at `0x004af480`.
- The constructor has no direct call to `0x004aea40`. It calls `DialogPane::OnCreate` at `0x004ae8b5`; `OnCreate` calls generic refresh wrapper `0x0049fe20` at `0x0049e119`; that wrapper dispatches virtual slot `+0x4c`, synchronously reaching this method for AddItem-family receivers.
- The generic wrapper also has callers at `0x0049e240`, `0x0049e6e0`, `0x0049ea60`, `0x004a0840`, `0x004ad320`, `0x004ade60`, `0x004fae90`, `0x0059b0f0`, and `0x0059b110`. Non-AddItem dynamic receivers route to their own implementations.
- Eight same-size structural siblings at `0x004729a0`, `0x0051ae50`, `0x0051c430`, `0x0051d6c0`, `0x0051e600`, `0x0051f250`, `0x0054fa20`, and `0x00550c40` corroborate the child/count/two-tail source shape. Different selectors and call displacements make them distinct methods; no exact duplicate 61-byte body exists.

## Boundaries, Split, And Source Placement

- Predecessor UID0004BO ends at `0x004aea3a`; `[0x004aea3a,0x004aea40)` is six `0xcc` alignment bytes. `[0x004aea7d,0x004aea80)` is three `0xcc` bytes, and the separate successor begins at `0x004aea80`.
- No target byte is padding, data, EH, a thunk, a second helper, or compiler-only output. No split, merge, inline-only, raw-helper, or reclassification change applies.
- Canonical owner/emitter remain UID000007, source route remains UID0000KE `NexusTK/ui/dialogs/ItemDialogs.cpp`, and relative `Nested:0` remains unchanged.

## Negative And Historical Evidence

- The binary performs no null comparison or branch for the action button, wrapper, or ListPane. Constructor invariants establish the controls; adding defensive checks would change behavior and source shape.
- It has no AddItemDialog field for the wrapper or list, no event/hover/resource/packet/singleton/employee/mixing read, and no boolean `SetEnabled` call.
- Historical B007 reconstruction used `ButtonPane`, `GetDialogButtonBySelector`, direct `GetDialogListBySelector`, `HasSelection`, a null test, and `SetEnabled(bool)`. Those names and operations are retained only as superseded target history.
- The historical direct-constructor-call negative remains true; the broader conclusion that constructor-time update does not occur is superseded by the synchronous `OnCreate` virtual route.
- No IDA mutation, third-party import, vtable-byte emission, or padding emission applies.

## Score Rationale

`92/94` reflects exact boundaries, every instruction and CFG block, ABI, selector insertion order, dynamic control types, wrapper field, complete helper behavior, virtual slots, all vtable routes, constructor refresh timing, structural siblings, source order, owner/emitter route, padding, negative evidence, and final managed C++. The remaining gap is stripped original lexical spelling and exact header organization, not behavior or source readiness.

## Changes

- 2026-07-13 B005 UID0004BP callback: raised `86/90` to `92/94`, set position `40`, replaced the decompiler-shaped placeholder with the exact typed wrapper/ListPane/Enable-or-Disable body, and added complete range, instruction, selector, helper, vtable, caller, refresh, source-order, padding, rejected-alternative, and negative evidence. Owner/emitter, range, reconstructable state, and `Nested:0` remain unchanged.
