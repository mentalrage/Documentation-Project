*** UID:0004KS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000E4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000E4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
StaticTextControlPane2::StaticTextControlPane2(
    const wchar_t *text,
    int x,
    int y,
    int horizontalAlignment,
    int verticalAlignment,
    const FontStyle *fontStyle,
    int horizontalPadding,
    int verticalPadding,
    int lineSpacing)
    : ControlPane(8, NULL),
      m_fillColor(0),
      m_frameColor(0),
      m_fontStyle(),
      m_horizontalPadding(horizontalPadding),
      m_verticalPadding(verticalPadding),
      m_lineSpacing(lineSpacing),
      m_textWidth(0),
      m_textHeight(0),
      m_horizontalAlignment(horizontalAlignment),
      m_verticalAlignment(verticalAlignment),
      m_textPart()
{
    m_fontStyle.SetEnabled(false);
    if (fontStyle != NULL)
        m_fontStyle = *fontStyle;

    const int textLength = static_cast<int>(wcslen(text));
    int lineCount = 1;
    int maxLineWidth = -1;
    int lineStart = 0;

    for (int index = 0; index < textLength; ++index) {
        const wchar_t ch = text[index];
        if (ch != L'\r' && ch != L'\n' && ch != L'\t')
            continue;

        ++lineCount;
        const int lineWidth = GetTextWidth(
            text + lineStart,
            static_cast<short>(index - lineStart));
        if (lineWidth > maxLineWidth)
            maxLineWidth = lineWidth;
        lineStart = index + 1;
    }

    const int tailWidth = GetTextWidth(
        text + lineStart,
        static_cast<short>(textLength - lineStart));
    if (tailWidth > maxLineWidth)
        maxLineWidth = tailWidth;

    m_textWidth = maxLineWidth + (2 * m_horizontalPadding);
    m_textHeight = (lineCount * GetLineHeight())
                 + ((lineCount - 1) * m_lineSpacing)
                 + (2 * m_verticalPadding);

    RectBounds bounds;
    if (m_horizontalAlignment == 0)
        bounds.left = x;
    else if (m_horizontalAlignment == 1)
        bounds.left = x - m_textWidth;
    else
        bounds.left = x - (m_textWidth / 2);
    bounds.right = bounds.left + m_textWidth;

    if (m_verticalAlignment == 3)
        bounds.top = y;
    else if (m_verticalAlignment == 4)
        bounds.top = y - m_textHeight;
    else
        bounds.top = y - (m_textHeight / 2);
    bounds.bottom = bounds.top + m_textHeight;

    const int screenWidth = g_pScreenPane->GetScreenWidth();
    const int screenHeight = g_pScreenPane->GetScreenHeight();
    if (bounds.left < 0)
        OffsetRect(&bounds, -bounds.left, 0);
    else if (bounds.right >= screenWidth)
        OffsetRect(&bounds, screenWidth - bounds.right, 0);
    if (bounds.top < 0)
        OffsetRect(&bounds, 0, -bounds.top);
    else if (bounds.bottom >= screenHeight)
        OffsetRect(&bounds, 0, screenHeight - bounds.bottom);

    wcscpy_s(m_textBuffer, 32, text);
    SetBounds(&bounds, false);

    m_textPart.m_text = m_textBuffer;
    m_textPart.m_horizontalPadding = m_horizontalPadding;
    m_textPart.m_verticalPadding = m_verticalPadding;
    m_textPart.m_lineSpacing = m_lineSpacing;
    m_textPart.m_fontStyle = &m_fontStyle;
    m_alpha = 0.2f;

    ApplyStyleState();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source constructor for StaticTextControlPane2: ControlPane type 8 construction, optional deep FontStyle copy, CR/LF/tab measurement, alignment and screen clamp, checked text copy, embedded child setup, and style application. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049a410-0x0049a8ac StaticTextControlPane2 Constructor

## Status

- Disposition: reconstructable source-authored constructor.
- Direct owner/emitter: [UID:0000E4][StaticTextControlPane2](by-class/StaticTextControlPane2.md), position `10`.
- Source file: [UID:0000O8][StaticTextControlPane](by-file/StaticTextControlPane.md).
- Split parent: [UID:00011V][0x0049a410-0x0049b915.StaticTextControlPane2](by-memory/0x0049a410-0x0049b915.StaticTextControlPane2.md), retained as a non-emitting semantic split index.
- Exact range: `0x0049a410-0x0049a8ac`; IDA modeled size `0x49c`.

## Signature And Callers

Eight direct callers allocate `0x2a8` bytes and pass the same ten source arguments. Four MiniMap setup calls use `NPC`, `Portal`, `X : `, and `Y : `; three other MiniMap label paths constrain center/right alignment and padding; NewMail uses `Keep a copy `. This fan-in supports a reusable static-text control constructor rather than feature-local ownership.

The base route is `ControlPane(8, NULL)`. The historical description as direct Pane type 1 construction is superseded. The optional `const FontStyle *` input is copied through FontStyle assignment, which deep-copies its rule vector; manual allocator or `memmove` source is rejected.

## Behavior

- Initializes fill/frame colors, dimensions, alignments, padding, line spacing, embedded FontStyle, and the embedded `SimpleHelpTextPartPane`.
- Disables the default FontStyle before assigning a non-null caller style.
- Treats CR, LF, and tab independently as line delimiters, so CRLF counts as two breaks. Every segment and the final tail are measured.
- Computes width as maximum segment width plus twice horizontal padding. Computes height as line count times line height, plus interline spacing and twice vertical padding.
- Maps horizontal alignment `0/1/other` to left/right/center and vertical alignment `3/4/other` to top/bottom/center, then clamps the resulting bounds to current screen dimensions.
- Preserves the observed logical checked-copy limit with `wcscpy_s(m_textBuffer, 32, text)`. The physical field is one `wchar_t[40]` interval ending at the next proven member `+0x158`; no independent `+0x148` member or reserve is emitted.
- Assigns the copied text and layout/style references into the embedded child, sets parent alpha to `0.2f`, and applies the complete style-state transition.

## Evidence And Source Shape

Primary, secondary, and tertiary vptr stores occur at `0x0049a489`, `0x0049a48f`, and `0x0049a499`. The constructor reaches FontStyle assignment at `0x0049a583`, screen width/height helpers, rectangle offset logic, checked copy, child StringBase assignment, and the behavior-identical style sequence later retained as `ApplyStyleState`.

The complete `0x2a8` layout is represented on the class page. The embedded by-value child requires its complete nested declaration, while `friend class StaticTextControlPane2` permits the exact direct field stores without inventing calls to B005-owned raw child helpers that have no supported source entry.

## Rejected Alternatives And Confidence Caps

- Direct Pane/type-1 construction, a separate 16-byte record/reserve at `+0x148`, manual FontStyle-vector copying, feature-file ownership, and calls to unproven nested setters are rejected by the exact body/caller/layout evidence.
- Source names for private fields and original header factoring remain inferred. The 40-element physical array count is strongly layout-backed but not symbol-proven; its observed logical copy limit remains exactly 32.
- Score `92/94` records complete behavior and boundaries while retaining those source-spelling/layout-intent caps.

## Cross-References

- [UID:0000E4][StaticTextControlPane2](by-class/StaticTextControlPane2.md)
- [UID:0000O8][StaticTextControlPane](by-file/StaticTextControlPane.md)
- [UID:00011V][0x0049a410-0x0049b915.StaticTextControlPane2](by-memory/0x0049a410-0x0049b915.StaticTextControlPane2.md)
- [UID:00005C][FontStyle](by-class/FontStyle.md)

## Changes

- 2026-07-13 B002 accepted UID00011V callback: created the exact constructor child, assigned direct class ownership/emission at position 10, preserved every measured/clamped/style behavior and caller constraint, and supplied the accepted formal source body.
