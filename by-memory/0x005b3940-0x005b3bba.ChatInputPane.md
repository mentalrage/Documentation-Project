*** UID:0001MC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "../auth/PasswordGuard.h"

bool ChatInputPane::HandleSpecialInput(PaneKeyEvent *event)
{
    const unsigned char key = NarrowInputEventByte(event);

    if (event->imeActive == 0 &&
        event->eventKind == 8 &&
        (key == 0x81 || key == 0x83)) {
        wchar_t historyText[256] = {};

        if (key == 0x81) {
            GetPreviousChatHistoryEntry(g_pCollectionData, &m_chatHistoryCursor,
                                        historyText, 256);
        } else {
            GetNextChatHistoryEntry(g_pCollectionData, &m_chatHistoryCursor,
                                    historyText, 256);
        }

        if (historyText[0] != L'\0') {
            m_textEditPane->SelectText(0, 0x7fff);
            m_textEditPane->InsertText(historyText,
                                       static_cast<int>(wcslen(historyText)),
                                       0,
                                       0);
            m_textEditPane->SelectText(0, 0x7fff);
        }

        return true;
    }

    return MultiLineInputPane::HandleKeyInput(event);
}

short ChatInputPane::SendChatMessage()
{
    short length = LineInputPane::TextLength();
    if (length <= 0) {
        return length;
    }

    if (g_pCollectionData->chatInputSendBlocked != 0) {
        return length;
    }

    wchar_t wideText[256] = {};
    LineInputPane::CopyText(wideText, 255);
    if (wideText[0] == L'\0') {
        return length;
    }

    ValidateChatInputOrThrowPasswordError(wideText);
    SanitizeWideTextForChat(wideText, static_cast<int>(wcslen(wideText)));

    unsigned char packet[300] = {};
    char multibyteText[256] = {};
    void *packetCopyContext = GetMemoryMan();

    PacketBufferWriteUInt8(0x0e, packet + 0);
    PacketBufferWriteUInt16BE(0, packet + 1);

    const int convertedLength = WideCharToMultiByte(
        0, 0, wideText, static_cast<int>(wcslen(wideText)),
        multibyteText, sizeof(multibyteText), 0, 0);
    if (convertedLength >= 0x100) {
        ReportRangeCheckFailure();
    }

    multibyteText[convertedLength] = '\0';
    PacketBufferWriteUInt8(static_cast<unsigned char>(convertedLength), packet + 2);
    CopyPacketBytes(packetCopyContext, packet + 3, multibyteText, convertedLength);

    const int packetLength = convertedLength + 3;
    if (packetLength >= 0x12c) {
        ReportRangeCheckFailure();
    }

    packet[packetLength] = 0;
    QueueAndSendPacket(g_packetSender, packet, packetLength);
    return SaveChatHistoryEntry(g_pCollectionData, wideText);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Chat input history key handler, submit handler, vtable refs, singleton cleanup, packet opcode `0x0e` subtype `0`, player-state gate, and alignment padding documented; C++ blank under the 95+ gate. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b3940-0x005b3bba ChatInputPane

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: exact class method cluster
- Current recovered owner: `ChatInputPane`
- Likely source module: [UID:0000I5][Chatting](by-file/Chatting.md) or a split `social/ChatInputPane.cpp`
- Confidence: very strong for submit/history behavior, vtable identity, packet route, and source placement; medium-high for exact original private field/helper spellings.

## Covered Ranges

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x005b3940-0x005b3a40` | `ChatInputPane::HandleSpecialInput` | Handles chat history up/down navigation and falls back to `MultiLineInputPane` key handling. |
| `0x005b3a40-0x005b3bba` | `ChatInputPane::SendChatMessage` | Validates input, normalizes text, builds opcode `0x0e` chat packet, sends it, saves chat history, and returns a short status/length result. |
| `0x005b3bba-0x005b3bc0` | alignment padding | Six `0xcc` bytes before the adjacent raw packet/input helper at `0x005b3bc0`. |

## Related Ranges

| Range | Current method or group | Notes |
| --- | --- | --- |
| [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md) | adjacent no-route raw packet island | Non-emitting opcode `0x0e` subtype `0` packet primitive immediately before the raw constructor; `0x005b3a40` remains the active virtual ChatInputPane submit/send method. |
| `0x005b38d5-0x005b38e0` | alignment padding | Eleven `0xcc` bytes before the raw constructor. |
| [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) | `ChatInputPane::ChatInputPane` raw constructor | Exact constructor-shaped sibling/child of [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md); outside this target's half-open `0x005b3940-0x005b3bba` range. |
| `0x005b3935-0x005b3940` | alignment padding | Eleven `0xcc` bytes before the confirmed key/history handler; not target-covered executable code. |
| `0x005b3bc0-0x005b3ca5` | post-target raw sender-shaped body | Source-shaped opcode `0x0e` subtype `0` wide-string sender with no rel32/control-flow/pointer route found in B009 local PE scans; should receive a later exact no-route split rather than being absorbed here. |
| `0x005b3ca5-0x005b3cb0` | alignment padding | Eleven `0xcc` bytes before [UID:0001MD][0x005b3cb0-0x005b4219.SayToUserNameInputPane](by-memory/0x005b3cb0-0x005b4219.SayToUserNameInputPane.md) `SayToUserNameInputPane`; ignored padding when the post-target raw body is split. |
| `0x005b77de-0x005b77e8` | destructor adjustor thunk | Wave3-owned `ChatInputPane` thunk. |
| `0x005b7980-0x005b79c4` | scalar deleting destructor | Clears `g_pChatInputPane` and tears down the input-pane base chain. |

## IDA MCP Evidence

Checked on 2026-06-02:

- IDA `lookup_funcs` confirms `sub_5B3940` at `0x005b3940-0x005b3a40`, `sub_5B3A40` at `0x005b3a40-0x005b3bba`, adjustor thunk `0x005b77de-0x005b77e9`, second thunk `0x005b77e9-0x005b77f4`, and scalar deleting destructor `0x005b7980-0x005b79c5`.
- IDA reports no function at `0x005b38e0`, but raw byte/disassembly review proves [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) as a complete constructor-shaped body.
- IDA `xrefs_to` / `search data_ref` reports vtable refs `0x0062fc40 -> 0x005b3940`, `0x0062fc30 -> 0x005b3a40`, `0x0062fbe8 -> 0x005b7980`, and constructor vtable stores to `0x0062fbe8`, `0x0062fc38`, and `0x0062fc68`.
- IDA `callers` reports no direct callers for `0x005b3940`, `0x005b3a40`, or `0x005b7980`; dispatch evidence is via vtables and construction sites.
- IDA `callees` for `0x005b3940` reports base key forwarding through `0x004f2cd0`, edit-control clear/insert helpers `0x0058f2a0`/`0x0058fc30`, chat-history helpers `0x005a4930`/`0x005a4990`, and `std::ctype<char>::do_narrow`.
- IDA `callees` for `0x005b3a40` reports text read helpers `0x004f2310`/`0x004f2300`, password guard `0x00465890`, sanitizer `0x005957c0`, packet byte writers `0x00575380`/`0x005753a0`, `WideCharToMultiByte`, packet send `0x00574bb0`, and history save `0x005a49e0`.
- IDA decompilation shows `0x005b3a40` skips send when player state byte `dword_67A748 + 1261826` is set, writes opcode `0x0e`, subtype `0`, length-prefixed message payload, sends through `dword_67A7EC`, then saves the wide string to chat history.
- Byte reads confirm padding at `0x005b3935-0x005b3940` and `0x005b3bba-0x005b3bc0`.

Checked again on 2026-06-14 by A001 against IDA MCP database `a001_goal2_class_batch`:

- `lookup_funcs` reconfirmed `0x005b38e0` is not an IDA function start, while the modeled method bodies remain `sub_5B3940` at `0x005b3940-0x005b3a40`, `sub_5B3A40` at `0x005b3a40-0x005b3bba`, and scalar deleting destructor `sub_5B7980` at `0x005b7980-0x005b79c5`.
- `xrefs_to` reconfirmed dispatch and teardown anchors: `0x0062fc40 -> 0x005b3940`, `0x0062fc30 -> 0x005b3a40`, and `0x0062fbe8 -> 0x005b7980`, plus adjustor thunk calls at `0x005b77e4` and `0x005b77ef`.
- `analyze_component` shows no internal direct calls between the history and submit handlers, but does group their shared state with `dword_67A748`, `dword_67A754`, `dword_67A7EC`, and security-cookie support.
- Fresh decompilation reconfirms `HandleSpecialInput` detects the two history navigation key values after `do_narrow`, rejects ordinary key events to `MultiLineInputPane`, reads previous/next history through `0x005a4930` / `0x005a4990`, and replaces the edit-control text through `0x0058f2a0` / `0x0058fc30` when nonempty history text is returned.
- Fresh decompilation reconfirms `SendChatMessage` checks pending text length, gates send on `dword_67A748 + 1261826`, reads up to `255` wide chars, applies the password guard and sanitizer, writes opcode `14` / `0x0e` (Verified with int_convert.py), subtype `0`, converts to multibyte text, writes the payload length and bytes, sends through `dword_67A7EC`, and saves the wide string through `0x005a49e0`.
- `get_bytes` reconfirmed `0x005b3935-0x005b3940` as eleven `0xcc` bytes and `0x005b3bba-0x005b3bc0` as six `0xcc` bytes.
- The current autogen report confirms this method cluster, its raw constructor child, prompt string child, and `g_pChatInputPane` storage route through [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md) to `auto-generated/NexusTK/social/Chatting.cpp` without a dead end.
- 2026-06-16 B001 audit clarifies the adjacent [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md) relationship: the raw helper has no IDA function, no inbound xrefs, no rel32/VA/RVA pointer hits, and no class `this` evidence. It is non-emitting no-route packet evidence, while `0x005b3a40` remains the actual `ChatInputPane` virtual submit body.

## Behavior Map

| Body | Confirmed behavior |
| --- | --- |
| Key/history handler | Handles two chat-history navigation key values after `do_narrow`; reads previous/next history through `0x005a4930`/`0x005a4990`, replaces edit text when history returns nonempty text, and otherwise delegates to the multi-line input base handler. |
| Submit handler | Checks pending text length, skips when a player-state gate byte is set, reads up to 255 wide chars, applies the password-disclosure guard and sanitizer, converts to multibyte text, writes opcode `0x0e`, subtype `0`, appends the length-prefixed payload, sends through `dword_67A7EC`, saves the line to chat history, and returns a short status/length result. |
| Destructor | Clears `dword_69BF68`, tears down the base input chain, and conditionally frees storage when scalar-delete flags request it. |

## Ownership Decision

`ChatInputPane` belongs with chat/social code rather than generic command-input panes. It shares line-input patterns with other `0x005b*` input panes, but its packet opcode, history functions, text filtering, and `g_pChatInputPane` global are chat-specific.

## 2026-06-19 B009 Source-Quality Reanalysis

B009 rechecked this page against the current by-* docs, the local PE, and the existing live-IDA-backed evidence. The IDA MCP endpoint was unavailable during that pass, so the new evidence is local PE boundary, pointer, vtable, and rel32 scanning layered on top of the prior IDA MCP records. The result keeps [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md) as the direct owner/emitter and keeps [UID:0000I5][Chatting](by-file/Chatting.md) as the current source-file route. A later `social/ChatInputPane.cpp` split remains plausible but is not required by this target alone.

Boundary closure:

- This page covers exactly the two modeled virtual methods at `0x005b3940-0x005b3a40` and `0x005b3a40-0x005b3bba`, plus the tail padding at `0x005b3bba-0x005b3bc0`.
- [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) is a real `ChatInputPane` constructor-shaped body, but it is outside this page's half-open address range. It remains a class sibling/child under [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md), not target-covered code.
- [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md) is still source-shaped opcode `0x0e` subtype `0` packet code with no route. It remains semantic neighborhood evidence only.
- B009 local PE scanning found another complete raw sender-shaped body at `0x005b3bc0-0x005b3ca5`, followed by `0x005b3ca5-0x005b3cb0` padding. It has no rel32/control-flow hits and no absolute pointer hits to its start/end in that scan. Treat it as a later no-route split candidate, not as part of this page.
- The older coverage-row range for [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) used `0x005b38e0-0x005b3940`; that mixed the exact constructor body with padding. The correct executable body remains `0x005b38e0-0x005b3935`.

Vtable and reachability closure:

- `0x0062fc40 -> 0x005b3940` is the secondary `ChatInputPane` key/history handler slot.
- `0x0062fc30 -> 0x005b3a40` is the primary `ChatInputPane` submit/send slot.
- `0x0062fbe8 -> 0x005b7980` and `0x0062fc38 -> 0x005b77de` are destructor/adjustor slots for the same class family.
- The raw constructor and equivalent open/allocation paths install the same primary, secondary, and tertiary `ChatInputPane` vtable bases and publish [UID:000309][0x0069bf68-0x0069bf6c.g_pChatInputPane](by-memory/0x0069bf68-0x0069bf6c.g_pChatInputPane.md).
- Local PE scans found no direct rel32 call/jump route to the two target methods; dispatch remains vtable-based, which is expected for these overrides.

History handler details for `0x005b3940-0x005b3a40`:

- The slot receives the secondary view of `this` (`primary this + 0xa0`).
- Secondary `this+0x68` maps to primary `this+0x108`, the `m_chatHistoryCursor` field initialized to `0xffff` by [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md).
- Secondary `this+0x5c` maps to primary `this+0xfc`, the inherited `LineInputPane` text-edit child used for replacement text.
- The handler narrows event bytes through the ctype-like support behind `dword_67A754`, accepts only `event+0x10a == 0`, `event+0x04 == 8`, and narrowed key `0x81` or `0x83`, and otherwise delegates to `MultiLineInputPane::HandleKeyInput` at `0x004f2cd0`.
- Key `0x81` calls `0x005a4930(g_pCollectionData, &m_chatHistoryCursor, buffer, 0x100)`, best named `GetPreviousChatHistoryEntry`.
- Key `0x83` calls `0x005a4990(g_pCollectionData, &m_chatHistoryCursor, buffer, 0x100)`, best named `GetNextChatHistoryEntry`.
- Nonempty returned history text is applied by selecting/clearing the inherited edit child through `0x0058f2a0`, inserting through `0x0058fc30`, and selecting the broad `0..0x7fff` range again.
- B014 records the raw return as an `AL` handled/unhandled result. The current first-draft source uses `bool ChatInputPane::HandleSpecialInput(PaneKeyEvent *event)` because nearby input-pane docs have standardized this key-event family on `PaneKeyEvent` and source-facing bool handlers; a raw `char` return remains an ABI-equivalent historical spelling possibility.

Submit/send details for `0x005b3a40-0x005b3bba`:

- The method first calls `LineInputPane::TextLength` at `0x004f2310`; empty input returns without side effects.
- It gates send on byte `g_pCollectionData + 0x134102` (`1261826`, verified by the B009 report), best documented as an inferred `chatInputSendBlocked` / local-player input-state gate until the final field name is proven.
- It copies at most `0xff` wide characters through `LineInputPane::CopyText` at `0x004f2300`, rejects empty copied text, calls [UID:0002VH][0x00465890-0x00465ae2.ValidateChatInputOrThrowPasswordError](by-memory/0x00465890-0x00465ae2.ValidateChatInputOrThrowPasswordError.md), and then calls [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) / `0x005957c0` with the measured text length and the [UID:0000SG][g_pTextFilter](by-global/g_pTextFilter.md) context.
- It initializes packet scratch/context through `0x00516030`, writes opcode `0x0e` to packet byte `0`, writes subtype `0` through the `PacketBufferWriteUInt16BE`-shaped helper at packet byte `1`, converts UTF-16 to multibyte through `WideCharToMultiByte`, range-checks converted length `< 0x100`, writes the one-byte payload length to byte `2`, copies the payload to byte `3+`, range-checks final length `< 0x12c`, and sends exactly `convertedLength + 3` bytes through [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md) using [UID:0000Q5][g_packetSender](by-global/g_packetSender.md).
- The scratch zero written by the two-byte helper after the subtype is overwritten by the later payload length write; the transmitted protocol remains byte `0` opcode `0x0e`, byte `1` subtype `0`, byte `2` payload length, bytes `3..` payload.
- After a successful send, it saves the wide string through `0x005a49e0(g_pCollectionData, wideText)`, best named `SaveChatHistoryEntry` / `AddChatHistoryEntry`; B014 models the method as returning a `short` status/length result rather than a `void` submit callback. Empty input, blocked sends, and empty copied text return the current line length/status without packet side effects, while the successful path returns the history-save helper result.

Best current source-facing names:

| Binary entity | Current role name | Confidence |
| --- | --- | --- |
| `0x005b3940` | `ChatInputPane::HandleSpecialInput` / `HandleHistoryKeyInput` | Strong behavior; exact original callback spelling inferred. |
| `0x005b3a40` | `ChatInputPane::SendChatMessage` | Strong behavior and side effects; exact original virtual name inferred. |
| primary `this+0x108` | `m_chatHistoryCursor` | Strong; constructor initializes it to `-1` and history helpers mutate it. |
| primary `this+0xfc` | inherited `m_textEditPane` / `m_inputEditPane` | Strong inherited edit-child role; exact spelling unresolved. |
| `0x0069bf68` | `g_pChatInputPane` | Strong; exact global/storage pages and xrefs exist. |
| `0x0067a7ec` | `g_packetSender` | Strong sender-global role; current source type remains a best-fit `Socket *` view. |
| `0x0067a748` | `g_pCollectionData` / local-player state pointer | Strong state role; exact source class name remains broader than this target. |
| `g_pCollectionData+0x134102` | `chatInputSendBlocked` / local-player input-state gate | Strong offset and gating role; source spelling inferred. |

B014 accepted the current cluster-emission approach as a temporary source-quality route while still recording the preferred future split. The best final structure is two exact child pages, `0x005b3940-0x005b3a40.ChatInputPaneHandleSpecialInput.md` and `0x005b3a40-0x005b3bba.ChatInputPaneSendChatMessage.md`; until that split exists, this page is allowed to emit both first-draft method bodies because the cluster range consists only of those two vtable-dispatched methods plus the trailing padding already listed above.

Historical assumptions rejected by this pass:

- Do not treat [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) as part of this target's covered range. The previous covered-range table did that for convenience, but B009 confirms the constructor is a related exact sibling outside the half-open method-cluster range.
- Do not emit [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md) or the post-target `0x005b3bc0-0x005b3ca5` raw sender body through this page. Similar opcode bytes are not enough without a caller, pointer, runtime route, or class `this` evidence.
- Do not move this target to `InputPanes.cpp`, `CommandInputPanes.cpp`, `SayInputPanes.cpp`, `Socket.cpp`, `PacketBuffer.cpp`, or `ProtocolSend.cpp`. Those modules own shared dependencies or sibling packet families, while this page owns chat input UI state, history, singleton, and opcode `0x0e` submit behavior.
- Do not use stale generated names such as `g_pCashShopRequest` for `0x0067a7ec` or a `SpellManager`/`g_pSpellMan`-style owner for `0x0067a748`. Current docs normalize the sender as `g_packetSender` and the state pointer as a broad `g_pCollectionData` / local-player state view.

## Reconstruction Notes

The cluster is reconstructable and attached to [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md), with the raw constructor split into [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md). First-draft C++ is now populated because the owner/emitter route, method boundaries, vtable dispatch, packet layout, and history behavior are stable. Remaining caveats are exact original spellings for private fields and some shared helpers; role names are intentionally descriptive and should be upgraded if later source-quality names are proven.

B014 specifically rejects moving this page to `InputPanes.cpp`, `CommandInputPanes.cpp`, `SayInputPanes.cpp`, `Socket.cpp`, `PacketBuffer.cpp`, or a protocol-only file. Those modules provide base classes, sibling prompts, or callees; this page owns chat input UI state, history navigation, the active singleton family, and the opcode `0x0e` submit behavior.

## Cross-References

- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md)
- [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md)
- [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md)
- [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)

## Changes

- 2026-08-17 B001 UID0000MG dependency implementation: score/body/ownership unchanged; added exact `../auth/PasswordGuard.h`. The existing validator call at binary `0x005b3aa1` remains before sanitization and chat packet submission.

- 2026-06-02 IDA evidence refresh:
  - Before: the cluster remained `50/65`, carried an unresolved projected constructor note, and omitted vtable, callee, behavior, and padding detail.
  - After: raised to `78/84`, marked reconstructable, attached to [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md), split the raw constructor into [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md), and cross-linked adjacent raw chat packet helper [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md).
  - Evidence: current IDA function-boundary, vtable/data-ref, caller/callee, decompile, raw byte, and padding checks listed above.
- 2026-06-13 Goal 2 constructor range refresh:
  - Updated [UID:0002S9][0x005b38e0-0x005b3935.ChatInputPaneRawConstructor](by-memory/0x005b38e0-0x005b3935.ChatInputPaneRawConstructor.md) references after live IDA MCP reconfirmed the constructor body ends at `0x005b3934` and `0x005b3935-0x005b3940` is separate alignment padding.
- 2026-06-14 A001 Goal 2 live IDA/method-cluster refresh:
  - Before: `COMPLETION:78`, `CONFIDENCE:84`.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:87`; owner/emitter stayed [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md).
  - Why: live IDA MCP reconfirmed method and destructor bounds, constructor no-function state, vtable-only dispatch anchors, history/submit decompilation details, shared globals/callees, padding bytes, and valid class-to-file autogen route.
  - C++: left blank because exact source names for base/input/edit/history/packet helpers and the player-state gate remain below final-source quality.
- 2026-06-16 B001 adjacent raw sender review:
  - Scores remain `85/87`; clarified that [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md) is a non-emitting no-route packet primitive, not this page's virtual submit body.
  - Evidence: live IDA/raw PE found no call or pointer route into [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md), while this page's `0x005b3a40` method has vtable data at `0x0062fc30` and full UI validation/history behavior.
- 2026-06-19 B009 Rule 26 source-quality incorporation:
  - Before: `COMPLETION:85`, `CONFIDENCE:87`, the covered-range table still listed the raw constructor as if it were target-covered code, and final C++ remained blank under a stale final-name rationale.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`; owner/emitter stayed [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md), and first-draft `HandleSpecialInput` / `SendChatMessage` C++ was added.
  - Evidence: B009 rechecked local PE boundaries, vtable slots `0x0062fc40` and `0x0062fc30`, raw constructor relationship, `g_pChatInputPane` xrefs, no-route status for [UID:0002S8][0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw](by-memory/0x005b37f0-0x005b38d5.ChatPacketRawSenderNoRouteRaw.md), newly flagged no-route raw body `0x005b3bc0-0x005b3ca5`, history cursor offset `+0x108`, inherited edit child offset `+0xfc`, packet opcode `0x0e` subtype `0`, `g_pCollectionData+0x134102` send gate, password/sanitizer path, `g_packetSender` send route, and chat-history save.
- 2026-06-19 B014 Rule 26 source-quality incorporation:
  - Metadata remains `88/90`; owner/emitter stay [UID:00001Q][ChatInputPane](by-class/ChatInputPane.md). The first-draft C++ now uses `PaneKeyEvent` for the key handler and models `SendChatMessage` as a `short`-returning submit path.
  - Evidence: B014 rechecked exact split boundaries, local PE rel32/VA/RVA negatives for adjacent raw sender bodies, vtable dispatch, adjusted-this layout, event fields, history helper roles, `g_pCollectionData+0x134102` send gate, `SanitizeWideTextForChat` / `g_pTextFilter`, PacketBuffer byte/subtype writers, `g_packetSender` / `QueueAndSendPacket`, and `SaveChatHistoryEntry` / `AddChatHistoryEntry`. It recommends future exact method-child splitting, but accepts current cluster emission as long as both method bodies stay inside this page's half-open range.
