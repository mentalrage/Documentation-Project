*** UID:0003UN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "../core/BlackHole.h"

bool UserPane::HandleServerEntryPacket(const unsigned char *packet)
{
    if (g_useEpfAssets == 1)
    {
        SelfLookPane *selfLookPane = static_cast<SelfLookPane *>(
            g_pGeneralPurposePanel->GetChildPaneByIndex(0));
        selfLookPane->RefreshSpelledPane();

        NewSystemMessagePane *systemMessagePane =
            static_cast<NewSystemMessagePane *>(
                g_pGeneralPurposePanel2->GetChildPaneByIndex(0));
        systemMessagePane->RefreshSpelledPane();

        g_packetSender->SetSendDisabled(false);
        if (g_pTransferServerDialog != 0)
        {
            g_pTransferServerDialog->ResetOpacityAndRepaint();
            g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(
                g_pTransferServerDialog);
        }

        m_objectId = PacketBufferReadUInt32BE(packet + 1);
        m_facing = packet[8];
        const unsigned char statusIconId = packet[9];
        if (statusIconId <= 4)
            g_activeUserStatusPane->SetStatusIconId(statusIconId);

        m_mapPane->UpdateObjectPaneScreenBounds(this);

        SimpleUStringVector &blockedNames = g_pConfig->m_blockListenNames;
        if (blockedNames.size() < 0xff)
        {
            for (SimpleUStringVector::const_iterator it = blockedNames.begin();
                 it != blockedNames.end(); ++it)
            {
                if (!it->empty())
                {
                    unsigned char request[0x400];
                    int requestSize = 0;
                    PacketBufferAppendUInt8(0x0d, request, &requestSize);
                    PacketBufferAppendUInt8(0x02, request, &requestSize);

                    StringBase<char, mychar_traits<char> > narrowName(*it);
                    PacketBufferAppendAnsiString8(
                        narrowName.c_str(), request, &requestSize);
                    g_packetSender->QueueAndSendPacket(
                        request, static_cast<short>(requestSize));
                }
            }
        }
    }
    else
    {
        SelfLookPane *selfLookPane = static_cast<SelfLookPane *>(
            g_pGeneralPurposePanel->GetChildPaneByIndex(0));
        selfLookPane->RefreshSpelledPane();

        g_packetSender->SetSendDisabled(false);
        if (g_pTransferServerDialog != 0)
        {
            g_pTransferServerDialog->ResetOpacityAndRepaint();
            g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(
                g_pTransferServerDialog);
        }

        m_objectId = PacketBufferReadUInt32BE(packet + 1);
        m_facing = packet[8];
        const unsigned char statusIconId = packet[9];
        if (statusIconId <= 4)
            g_pOldUserStatusPane->SetStatusIconId(statusIconId);

        m_mapPane->UpdateObjectPaneScreenBounds(this);

        SimpleUStringVector &blockedNames = g_pConfig->m_blockListenNames;
        if (blockedNames.size() < 0xff)
        {
            for (SimpleUStringVector::const_iterator it = blockedNames.begin();
                 it != blockedNames.end(); ++it)
            {
                if (!it->empty())
                {
                    unsigned char request[0x400];
                    int requestSize = 0;
                    PacketBufferAppendUInt8(0x0d, request, &requestSize);
                    PacketBufferAppendUInt8(0x02, request, &requestSize);

                    StringBase<char, mychar_traits<char> > narrowName(*it);
                    PacketBufferAppendAnsiString8(
                        narrowName.c_str(), request, &requestSize);
                    g_packetSender->QueueAndSendPacket(
                        request, static_cast<short>(requestSize));
                }
            }
        }
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact opcode-0x05 UserPane server-entry handler with one dispatcher caller, duplicated EPF/legacy branches, SelfLook/NewSystem SpelledPane refresh, packet-sender re-enable, transfer-overlay repaint/deferred deletion, object-id/facing/status-icon application, MapPane bounds refresh, block/listen add replay through two 0x400-byte branch-local packets, exact unchecked behavior, and complete source-ready C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005aac80-0x005aafe3 User Pane Handle Server Entry Packet

## Current Source Disposition - 2026-07-15

- Current identity: [UID:0003UN] `bool UserPane::HandleServerEntryPacket(const unsigned char *packet)` at exact modeled range `[0x005aac80,0x005aafe3)`.
- Current ownership and route: canonical owner/emitter [UID:0000FQ][UserPane](by-class/UserPane.md), emitted through [UID:0000P1][UserPane](by-file/UserPane.md) as `NexusTK/ui/panels/UserPane.cpp`; reconstructable true, blank optional emitter position, and `Nested:0` are retained.
- The managed body above is the accepted source-ready replacement. It preserves the two binary branches, exact dependencies, packet/state effects, block/listen replay, unchecked behavior, and unconditional true result.
- The pre-callback body that closed login/loading UI, reset receive state, changed CollectionData server mode, invalidated the map, and replayed server addresses was not binary-equivalent. It is retained below only as rejected historical evidence.
- Score `92/94` reflects closed range/hash/CFG/ABI/caller/opcode/callee/field/vector/source evidence. Exact private wrapper/container spellings remain the bounded confidence cap.

## Historical / Superseded Assumptions - Pre-2026-07-14

- Date/source: documentation conclusions incorporated before the 2026-07-14 UID0002QY ownership and source-integration repair.
- Rejected claim: this local-player body belonged to `LivingObjectPane`/`LivingObjectPane.cpp`, used a reverse-engineering placeholder qualification, or should remain blank/no-code until a separate route or declaration was found.
- Disproof evidence: the standalone `LivingObjectPane` object ends at `+0x20c`, while the moved receiver-bearing bodies use UserPane-only state in the `+0x134000..+0x13eb84` extension; no-receiver helpers cluster with and are consumed by the same UserPane source family. The complete UID0000FQ declaration and UID0000P1 route make the accepted definitions source-legal.
- Accepted replacement: source-authored `UserPane` member with the exact current managed definition above. Existing no-route and inline-equivalent observations remain negative/liveness evidence only.
- Evidence preservation: exact bytes, ranges, CFG/callers, field accesses, packet/state behavior, and historical provenance in the nested material below remain retained evidence; only the rejected ownership, qualification, placeholder-name, and blank/no-code conclusions are superseded.

### Status

- Address range: `[0x005aac80,0x005aafe3)`, `0x363` / 867 bytes.
- Entity kind: exact source-authored `UserPane` member.
- Current IDA name: `sub_5AAC80`.
- Disposition: reconstructable source-authored code.
- Canonical owner/emitter: [UID:0000FQ][UserPane](by-class/UserPane.md); source file [UID:0000P1][UserPane](by-file/UserPane.md).
- ABI: `__thiscall`, one packet pointer argument, callee cleanup `retn 4`, boolean true on every path.

### Summary

This opcode-`0x05` server-entry handler refreshes the visible self-look/system-message SpelledPane state, re-enables packet sending, repaints and defers deletion of an optional transfer overlay, installs local-player object/facing/status state, recomputes MapPane bounds, and replays every nonempty block/listen name. It intentionally preserves separate EPF and legacy branches.

### Evidence

- Exact function SHA256: `4B658CF3F99E9145A8AEB8B87F5F291081B8E092431DB3D906BA0F364924807B`.
- Prologue begins `55 8b ec 6a ff 68 96 ae 60 00 64 a1 00 00 00 00`; final epilogue includes `8b e5 5d c2 04 00` before alignment at `0x005aafe3`.
- Main body has 21 control-flow blocks; 26 including compiler EH funclets; cyclomatic complexity 12. Stack allocation is `0x814`, accounting for two independent `0x400` packet arrays, cursors, temporary narrow StringBase handles, and compiler EH state.
- Sole direct caller is `0x005a797f` in [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md). The dispatcher adjusts the receiver by `-0xa0`, routes live opcode `0x05`, and consumes the true result.
- UserPane ownership is jointly established by the adjusted caller receiver, the `m_mapPane` field at UserPane-only offset `+0x1340f8`, current declaration/file routing, and generated ownership. LivingObjectPane ends at `+0x20c` and cannot own the complete receiver state.

### Exact Branch And Packet Behavior

- EPF is selected only when [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) equals `1`. Both branches refresh GeneralPurposePanel child zero through [UID:0003QH][0x005671c0-0x005671de.SelfLookPaneRefreshSpelledPane](by-memory/0x005671c0-0x005671de.SelfLookPaneRefreshSpelledPane.md); only EPF also refreshes GeneralPurposePanel2 child zero through [UID:0004R2][0x00588d50-0x00588d6e.NewSystemMessagePaneRefreshSpelledPane](by-memory/0x00588d50-0x00588d6e.NewSystemMessagePaneRefreshSpelledPane.md).
- Both branches call `g_packetSender->SetSendDisabled(false)`.
- A nonnull transfer overlay receives `ResetOpacityAndRepaint()` and is passed to the BlackHole/application cleanup queue for deferred deletion. No global clear, direct delete, or scalar deleting destructor call occurs.
- `packet[1..4]` is read big-endian into `m_objectId` at `+0xfc`; `packet[8]` is written to `m_facing` at `+0x1c5`; `packet[9]` is a status-icon id and is forwarded only when `<=4`. EPF uses the active UserStatusPane, while legacy uses OldUserStatusPane. Packet bytes 5, 6, and 7 are not read.
- Both branches call `m_mapPane->UpdateObjectPaneScreenBounds(this)`. There is no map invalidation.

### Block And Listen Replay

- Config `+0x2918fc/+0x291900/+0x291904` is the begin/end/capacity triplet for `SimpleUStringVector m_blockListenNames`.
- Replay executes only when vector size is strictly below `0xff`. Every nonempty string is replayed; empty entries are skipped. There is no active-element flag, count clamp, truncation, retry, or null guard.
- Each branch owns a separate uninitialized `0x400` request array and cursor. The request begins with opcode `0x0d`, subcommand `0x02`; the UTF-16 name converts to branch-local narrow StringBase, appends through the ANSI length-8 helper, and sends with the cursor narrowed to Socket's short-length contract.
- Separate buffers, conversion temporaries, loops, and EH cleanup support the duplicated human branches. A synthesized common tail/helper would erase observed source shape and is rejected.

### Complete Callee Roles

| Address | Accepted source role |
| --- | --- |
| `0x004b85f0` | `GeneralPurposePanel::GetChildPaneByIndex(0)` |
| `0x005671c0` | `SelfLookPane::RefreshSpelledPane()` |
| `0x004b8950` | `GeneralPurposePanel2::GetChildPaneByIndex(0)` |
| `0x00588d50` | `NewSystemMessagePane::RefreshSpelledPane()` |
| `0x00574ce0` | `Socket::SetSendDisabled(false)` |
| `0x00599250` | `TransferServerDialogPane::ResetOpacityAndRepaint()` |
| `0x00469180` | `BlackHole::QueuePaneForDeferredDeletion(Pane *)` |
| `0x005754c0` | `PacketBufferReadUInt32BE` |
| `0x00575470` | compiler/inlined narrow-byte conversion |
| `0x005b86e0` | active `UserStatusPane::SetStatusIconId` |
| `0x005bdc70` | old `UserStatusPane::SetStatusIconId` equivalent |
| `0x00506980` | `MapPane::UpdateObjectPaneScreenBounds(ObjectPane *)` |
| `0x005845f0` | wide StringBase `empty()` |
| `0x00575650` | value-first `PacketBufferAppendUInt8` |
| `0x005825a0` | wide-to-narrow StringBase conversion |
| `0x00584540` | narrow StringBase `c_str()` |
| `0x005757d0` | text-first `PacketBufferAppendAnsiString8` |
| `0x00582b30` | narrow temporary release |
| `0x00574bb0` | `Socket::QueueAndSendPacket` |

Security-cookie operations, EH states, temporary cleanup state, and narrow-character trait lowering are compiler support and are not handwritten source.

### Range And Negative Evidence

- Seven all-`0xcc` bytes at `[0x005aac79,0x005aac80)` have SHA256 `F145ECA66705DE68FF53C6D80E34D8F9A5B535FFA157194AA98BDB68AEA4368D`.
- Thirteen all-`0xcc` bytes at `[0x005aafe3,0x005aaff0)` have SHA256 `0D0BB866505BC0E2D4276FCDEE1AFDC8887662D04CCE9E4BBB4C10FBA6D0F1CE`. Retained raw UID0003UO begins at `0x005aaff0`; there is no fallthrough.
- The target has one entry, one receiver, one explicit parameter, and one boolean result. No internal block has an independent xref, callable prologue, pointer route, or separate source signature; no split is warranted and `Nested:0` remains exact.
- No MainWindow, LoginDialogPane, LoadingPane, Application delete, receive-reset, CollectionData, legacy CollectionData, server-address-list, server-mode, map-invalidation, zero-fill, buffer-capacity, string-length clamp, list truncation, send-retry, or required-object null-check route exists.

### Open Questions Resolved

- Source signature is `bool UserPane::HandleServerEntryPacket(const unsigned char *packet)` and dispatcher opcode is `0x05`.
- Packet byte 9 is `statusIconId`, not server mode. Bytes 5..7 remain intentionally unnamed because this body does not read them.
- Replayed data is `m_blockListenNames`, not a server-address list.
- Both wrappers are `RefreshSpelledPane()` and call the accepted `RefreshScrollState()` abstraction; the former world-map identities are rejected.
- Exact original private wrapper/container spellings remain unrecovered, but this is a bounded confidence cap rather than an implementation blocker.

### Cross-References

- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)

## B001 UID0002QY Accepted Callback Synchronization - 2026-07-14

- Destination 37 is applied without changing the accepted score, address range, ABI, callers, packet/state behavior, negative evidence, or retained historical research.
- Receiver fields beyond the LivingObjectPane base establish [UID:0000FQ][UserPane](by-class/UserPane.md) UserPane as canonical owner/emitter and [UID:0000P1][UserPane](by-file/UserPane.md) as source module.
- The formal managed block is authoritative. Earlier LivingObjectPane ownership/qualifiers, Raw or packet-letter placeholder names, and blank-body conclusions are retained only as dated history and are superseded by the accepted semantic source shape.

## B004 UID0003UN Source-Quality Callback - 2026-07-15

- Raised `86/88 -> 92/94` while retaining UID0003UN, owner/emitter UID0000FQ, reconstructable true, blank optional position, `Nested:0`, exact range, and UserPane.cpp route.
- Replaced the false source body with the exact accepted duplicated EPF/legacy implementation and installed the exact Item Summary above.
- Incorporated complete range/hash/CFG/ABI/caller/opcode/callee/field/global/packet/vector/string/padding/source-placement/negative/rejected-alternative evidence at target level.
- Historicalized the former “authoritative/source-ready” claim together with the stale blank-C++/open-question claim: both referred to the rejected pre-callback body and neither remains current.
- Dependencies remain owned by their own source families. This page does not transfer ownership or inflate support scores for Config/StringBase, Socket, TransferServerDialogPane, BlackHole, MapPane, UserStatusPane, SelfLookPane, NewSystemMessagePane, SpelledPane, or FrameHandler.
