*** UID:0003VO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ArgumentedMenuMenuItemList::DrawListEntry(
    int rowIndex, void *rowData, RectBounds *rowBounds)
{
    ArgumentedMenuItemEntry *entry =
        static_cast<ArgumentedMenuItemEntry *>(rowData);
    const bool selected = IsItemSelected(rowIndex);

    RectBounds itemBounds = *rowBounds;
    itemBounds.right -= 28;
    if (selected) {
        SetDrawColor(0x25);
        g_pfnFillRect(this, &itemBounds);
    }
    SetTextColor(selected ? 0x80 : 0x25);

    RectBounds glyphBounds;
    RectBounds imageBounds;
    g_pItemObjImageLib->GetItemGlyphBounds(entry->itemId, &glyphBounds);
    if (g_useEpfAssets) {
        imageBounds.left = 12 -
            (glyphBounds.right - glyphBounds.left) / 4;
        imageBounds.right = imageBounds.left +
            (glyphBounds.right - glyphBounds.left) / 2;
        imageBounds.top = (rowBounds->top + rowBounds->bottom) / 2 -
            (glyphBounds.bottom - glyphBounds.top) / 4;
        imageBounds.bottom = imageBounds.top +
            (glyphBounds.bottom - glyphBounds.top) / 2;
        IntersectRects(&imageBounds, rowBounds, &imageBounds);
        g_pItemObjImageLib->DrawItemImageScaled(
            this, &imageBounds, entry->itemId, entry->itemStyle);
    } else {
        imageBounds.left = 12 -
            (glyphBounds.right - glyphBounds.left) / 2;
        imageBounds.right = imageBounds.left +
            (glyphBounds.right - glyphBounds.left);
        imageBounds.top = (rowBounds->top + rowBounds->bottom) / 2 -
            (glyphBounds.bottom - glyphBounds.top) / 2;
        imageBounds.bottom = imageBounds.top +
            (glyphBounds.bottom - glyphBounds.top);
        g_pItemObjImageLib->DrawItemImage(
            this, &imageBounds, entry->itemId, entry->itemStyle,
            NULL, 0.0f);
    }

    SetTextDrawMode(1);
    SetTextBackFillColor(0);

    wchar_t fittedName[256];
    wcscpy_s(fittedName, _countof(fittedName), entry->name);
    RectBounds nameBounds = *rowBounds;
    nameBounds.left = 30;
    nameBounds.right = 186;
    FitTextAndAppendSuffix(
        fittedName, _countof(fittedName), L"...",
        nameBounds.right - nameBounds.left);
    DrawTextInRect(fittedName, wcslen(fittedName), &nameBounds);

    wchar_t countText[16];
    swprintf_s(countText, _countof(countText),
               L"%d", entry->maxQuantity);
    RectBounds countBounds = *rowBounds;
    countBounds.left = 186;
    countBounds.right = 217;
    DrawTextInRect(countText, wcslen(countText), &countBounds);

    wchar_t priceDigits[32];
    swprintf_s(priceDigits, _countof(priceDigits), L"%u", entry->price);
    wchar_t priceText[48];
    wchar_t *output = priceText;
    if (entry->maxQuantity > 1) {
        wcscpy_s(priceText, _countof(priceText), L"1gold ");
        output += wcslen(priceText);
    }
    const size_t digitCount = wcslen(priceDigits);
    for (size_t i = 0; i < digitCount; ++i) {
        if (i != 0 && (digitCount - i) % 3 == 0)
            *output++ = L',';
        *output++ = priceDigits[i];
    }
    *output = L'\0';

    RectBounds priceBounds = *rowBounds;
    priceBounds.left = 217;
    DrawTextInRect(priceText, wcslen(priceText), &priceBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact 0x44d ArgumentedMenuMenuItemList DrawListEntry override at 0x0051f840; renders selected background, item glyph/style, fitted name, max quantity, comma-grouped price and conditional 1gold prefix from the resolved 0x410 row, with formal CPP emitted through UID00000I. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0051f840-0x0051fc8d ArgumentedMenuMenuItemListDrawMenuItem

## Status

- Entity kind: vtable class method.
- Direct owner/emitter: [UID:00000I][ArgumentedMenuMenuItemList](by-class/ArgumentedMenuMenuItemList.md).
- Confidence: strong for modeled bounds, vtable ref, accepted inherited-slot identity, resolved row fields, renderer/resource behavior, and owner route.

## Behavior

This vtable-backed draw method renders one argumented menu item row, including icon, name, price, quantity/count text, and percent metadata. It uses wide-string copy/concatenate formatting paths and numeric formats including `%d` and `%u`.

2026-06-20 B004 row-layout sync names the consumed [UID:0003VH][0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper](by-memory/0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper.md) fields descriptively:

- `row+0x000` word is `itemId`, used with `row+0x002` `itemStyle` for item-image lookup/draw.
- `row+0x008` is `name[0x100]`, the row display text fitted with ellipsis where needed.
- `row+0x40c` is `maxQuantity`, formatted with `L"%d"` for count/quantity display.
- `row+0x004` is `price`, formatted with `L"%u"` and comma insertion; the draw path uses the `L"1gold "` prefix behavior when `maxQuantity > 1`.
- The helper still has percent-metadata formatting in the surrounding constructor/parser flow, but [UID:0003VH][0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper](by-memory/0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper.md) does not store a separate percent field; the percent decoration is folded into the display string before insertion.

## Evidence

- B001 2026-06-16 IDA MCP reports `sub_51F840`, size `0x44d`, ending at `0x0051fc8d`.
- The method has a vtable data ref at `0x0061f544`.
- The successor `0x0051fc8d-0x0051fc90` span is the existing ignored alignment row before [UID:0001BT][0x0051fc90-0x00520539.ArgumentedItemInputDialogs](by-memory/0x0051fc90-0x00520539.ArgumentedItemInputDialogs.md).
- Decompilation/disassembly show row icon/text/price/count rendering and `_wcscpy_s`/`_wcscat_s` formatting paths.
- B004 [UID:0003VH][0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper](by-memory/0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper.md) source-quality incorporation ties those draw reads to the append helper's `0x410` row construction and first-draft `ArgumentedMenuItemEntry` layout.

## Final C++ Eligibility

Formal C++ emits the inherited `DrawListEntry(int rowIndex, void *rowData, RectBounds *rowBounds)` override. It uses the accepted `ArgumentedMenuItemEntry` layout and existing item-menu renderer APIs to preserve selected-row fill/text colors, legacy/EPF item-image scaling, fitted name, quantity, comma-grouped price, and the conditional `L"1gold "` prefix without adding a separate percent field.

## Cross-References

- [UID:00000I][ArgumentedMenuMenuItemList](by-class/ArgumentedMenuMenuItemList.md)
- [UID:0001BS][0x0051e9a0-0x0051fc8d.ArgumentedMenuDialogs](by-memory/0x0051e9a0-0x0051fc8d.ArgumentedMenuDialogs.md)
- [UID:0001BT][0x0051fc90-0x00520539.ArgumentedItemInputDialogs](by-memory/0x0051fc90-0x00520539.ArgumentedItemInputDialogs.md)

## Changes

- 2026-06-16 B001 supervisor-applied split:
  - Created exact vtable draw-method child from the former [UID:0001BS][0x0051e9a0-0x0051fc8d.ArgumentedMenuDialogs](by-memory/0x0051e9a0-0x0051fc8d.ArgumentedMenuDialogs.md) unsplit tail.
  - Evidence: B001 IDA MCP confirmed function bounds, vtable ref, rendering behavior, and the successor padding boundary.
- 2026-06-20 B004 row-layout sync:
  - Added source-facing row field names from [UID:0003VH][0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper](by-memory/0x0051f3c0-0x0051f44e.ArgumentedMenuMenuItemListRawAppendEntryHelper.md) `AppendItem`: `itemId`, `itemStyle`, `name`, `price`, and `maxQuantity`, and preserved the no-separate-percent-field caveat.
- 2026-08-22 B002 UID0000OP Gate 2A repair:
  - Resolved the inherited slot as `DrawListEntry`, supplied the source-ready renderer from the accepted row fields and shared item-list drawing APIs, and preserved the no-separate-percent-field rule so UID0003VO no longer emits an empty marker.
