*** UID:0001B8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000L6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0001B6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *Md5FilePathHex(const char *path)
{
    FILE *stream;
    MD5 md5;
    unsigned char buffer[1024];
    size_t bytesRead;

    memset(buffer, 0, sizeof(buffer));
    fopen_s(&stream, path, "rb");
    if (stream == NULL)
        return "000011112222333344445555666677778888";

    bytesRead = fread(buffer, 1, sizeof(buffer), stream);
    while (bytesRead != 0)
    {
        md5.Update(buffer, (unsigned int)bytesRead);
        bytesRead = fread(buffer, 1, sizeof(buffer), stream);
    }

    md5.Final();
    fclose(stream);
    return Md5DigestToHexString(md5.digest);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *Md5FilePathHex(const char *path);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact raw MD5 file-path wrapper using class MD5, a 1024-byte unsigned-byte buffer, binary open/read/final/close behavior, shared fallback literal, half-open end 0x515569, protected fences, and UID0001B6 child routing despite retained no-route evidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00515450-0x00515569 - Md5FilePathHex

## Status

- Current callback state: exact source child of [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md) at position `50`, using class [UID:0004X5][MD5](by-class/MD5.md). Score `91/92` reflects complete source behavior and exact boundaries while retaining raw/no-route and original-spelling caps.

## 2026-07-26 B001 Class And Source Repair

The accepted formal source constructs `MD5 md5`, clears an `unsigned char buffer[1024]`, opens the path with `fopen_s(..., "rb")`, hashes each `fread` chunk through `md5.Update`, finalizes, closes the file, and returns `Md5DigestToHexString(md5.digest)`. Open failure returns the shared literal `000011112222333344445555666677778888` represented by [UID:0003C4][0x0061eadc-0x0061eb08.MapPaneDigitPatternBoundaryTailData](by-memory/0x0061eadc-0x0061eb08.MapPaneDigitPatternBoundaryTailData.md).

The source body is half-open `[0x00515450,0x00515569)`, even though the historical filename ends in `0x00515568`. It has an exact `0x474` stack frame, `0x400`-byte I/O buffer, eleven-byte pre-fence, and seven-byte post-fence. No direct xref, VA/RVA pointer, or rel32 route reaches the raw start, but positive controls find the modeled stream/raw-digest siblings. Complete bounded behavior resolves source reconstruction without pretending the raw helper is live-routed.

The former `MD5Context` object and manual state/count initialization are preserved below as historical reconstruction. Exact class layout and raw `MD5::Init` evidence supersede that output with the human class model while preserving the same initialization behavior.

- Confidence: strong for behavior, boundary, source-ready first-draft C++ shape, and MD5 ownership; medium-high for live-call relevance and exact original helper/type spelling.
- Proposed owner: [UID:0000L6][MD5](by-file/MD5.md) in `util/MD5.cpp`
- Parent range: [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md)
- Evidence basis: IDA MCP lookup/xref/disassembly/signature checks, sibling-helper decompilation, and read-only PE route scanning through 2026-06-25 session `80de0a67`.
- Reconstruction status: source-ready first-draft C++ under [UID:0000L6][MD5](by-file/MD5.md). IDA still does not model this raw start as a function object and no current caller or pointer route reaches `0x00515450`, but those are now liveness/confidence caps rather than blank-C++ blockers.

## Boundary

This is a function-shaped raw helper that current IDA does not materialize as a function object.

| Range | Status | Role |
| --- | --- | --- |
| `0x00515450-0x00515569` | raw code body | Opens a file path in binary mode, hashes the contents, and returns a heap MD5 hex string. The filename preserves the legacy final-byte suffix `0x00515568`; the actual exclusive end is `0x00515569`. |
| `0x00515569-0x00515570` | alignment gap | Seven `0xcc` bytes pad up to `Md5StreamHex` at `0x00515570`. |

## Behavior

The helper takes a file path pointer, zeroes a 1024-byte stack buffer (`0x400` = 1024; Verified with `int_convert.py`), initializes a stack MD5 context with the standard state constants, then calls `_fopen_s` with mode `"rb"`.

When the file opens successfully, it reads in 1024-byte chunks with `_fread`, calls `Md5Update` at `0x00515690` for each non-empty chunk, finalizes through `Md5Final` at `0x00515750`, closes the stream with `_fclose`, and converts the final 16-byte digest through `Md5DigestToHexString` at `0x005151f0`.

When the file open fails, it returns the static fallback literal `"000011112222333344445555666677778888"`. Preserve this literal until all file-hash callers are audited.

## Caller And Liveness Notes

IDA xrefs and the 2026-06-25 read-only PE scan show no direct code reference, data reference, absolute VA pointer, RVA pointer, or direct `E8/E9` relative route to `0x00515450`. Keep the body documented and emitted because the file-open/read/update/final/format sequence is a complete compiled MD5 file helper and matches the neighboring source-shaped stream helper at `0x00515570`.

The missing IDA function object and missing route are modeling/liveness issues for reconstruction confidence, not evidence against the helper's MD5 source ownership and no longer a reason to keep the formal C++ block blank. They also are not a reason to merge this body into `RegistryConfig`, `Socket`, `ScreenshotCapture`, `MapPane`, or the fallback-literal data page.

## 2026-06-13 Live IDA Evidence

- IDA MCP session `a001_goal2_0002UM` over `NexusTK.exe` reconfirmed that `0x00515450`, `0x00515568`, and `0x00515569` are not modeled function starts. The neighboring modeled helpers remain `sub_5153E0` at `0x005153e0` size `0x65` and `sub_515570` at `0x00515570` size `0xeb`.
- The corrected half-open body is `0x00515450-0x00515569`, `0x119` = 281 bytes (Verified with `int_convert.py`). The stack frame is `0x474` = 1140 bytes (Verified with `int_convert.py`), with a 1024-byte file buffer and MD5 context locals.
- Disassembly from `0x00515450` shows the prologue, security-cookie setup, `___security_cookie` reference, standard MD5 state stores `0x67452301`, `0xefcdab89`, `0x98badcfe`, and `0x10325476`, `_memset`, `_fopen_s("rb")`, the `_fread` loop, `Md5Update`, `Md5Final`, `_fclose`, `Md5DigestToHexString`, and two explicit return paths.
- `xrefs_to 0x00515450` still reports no references. Target-side xrefs to callees show this raw body as `fn:null` at `_memset` (`0x005154a8`), `_fopen_s` (`0x005154ba`), `_fread` (`0x005154e0` and `0x00515517`), `Md5Update` (`0x005154fe`), `Md5Final` (`0x00515529`), `_fclose` (`0x00515534`), `Md5DigestToHexString` (`0x00515540`), and the security-cookie check (`0x0051554d` and `0x00515560`).
- The fallback literal at `0x0061eadc` has exactly two IDA xrefs in this MD5 island: this raw file helper at `0x00515559` and the stream helper at `0x0051564a`. The `"rb"` literal at `0x0060df9c` is shared, but this helper's use at `0x005154ad` is the MD5 file-open site.
- `make_signature_for_range 0x00515450-0x00515569` returned a unique full-body byte signature. `make_signature_for_range 0x00515569-0x00515570` returned seven `0xcc` bytes, confirming alignment padding before `Md5StreamHex`.

## 2026-06-25 B005 Source-Ready Evidence

- IDA MCP session `80de0a67` over `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` was health-ready for `NexusTK.exe` from `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, with auto-analysis, Hex-Rays, and strings ready.
- `lookup_funcs` still reports no function object at `0x00515445`, `0x00515450`, `0x00515568`, or `0x00515569`; the modeled neighbors remain `sub_5153E0` at `0x005153e0` size `0x65` and `sub_515570` at `0x00515570` size `0xeb`.
- `xrefs_to 0x00515450` remains empty. `xrefs_to 0x00515568` reports only the raw body's own `0x00515567` code reference at `fn:null`, and `xrefs_to 0x00515569` remains empty. In contrast, `xrefs_to 0x00515570` still finds the live stream-helper call from `0x00558278` in `sub_558240`.
- Disassembly of `0x00515450` still returns `<no function>` but decodes the full helper body: prologue at `0x00515450`, `sub esp, 474h`, path argument from `[ebp+8]`, local file buffer at `[ebp-404h]`, `0x400` byte buffer size, standard MD5 constants at `[ebp-470h]`, bit-count zeroing at `[ebp-460h]`, `_memset`, `_fopen_s("rb")`, `_fread`, `Md5Update`, `Md5Final`, `_fclose`, `Md5DigestToHexString`, fallback return of `0x0061eadc`, final `retn` at `0x00515568`, and alignment starting at `0x00515569`.
- `decompile 0x00515450` still fails because IDA has no function object. This is a tool-modeling limitation; the instruction stream itself is source-shaped and complete.
- `analyze_function 0x00515570` decompiles the modeled stream sibling as `char *__cdecl(FILE *Stream)`, size `0xeb` / 235 bytes (Verified with `int_convert.py`), using the same MD5 initialization, 1024-byte read buffer, `fread`/`Md5Update` loop, `Md5Final`, `Md5DigestToHexString`, and fallback literal. Its only current caller is `sub_558240`.
- `analyze_function 0x005151f0` decompiles the hex formatter as a 16-byte digest-to-heap-string helper using `%02x` and `_strdup`. `analyze_function 0x00515690` and `0x00515750` show `Md5Update` and `Md5Final` as `__thiscall` context helpers, supporting the source-facing `MD5Context` / `context.Update` / `context.Final` first-draft code shape.
- `make_signature_for_range 0x00515450-0x00515569` returned a unique full-body byte signature. `make_signature_for_range 0x00515569-0x00515570` returned `CC CC CC CC CC CC CC`, confirming seven bytes of padding before `Md5StreamHex`.
- `int_convert` verified `0x119` = 281 bytes, `0x474` = 1140 bytes, `0x400` = 1024 bytes, `0xeb` = 235 bytes, and `0x151` = 337 bytes.
- A read-only PE scan of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` found no absolute VA, RVA, or direct `E8/E9` relative route to `0x00515450`. The same scan found the expected control routes to `0x00515570` from `0x00558278` and to `0x00515380` from `0x00578da5`, proving the scan was able to find normal direct routes when present.
- The best source-facing signature remains strongly inferred as `char *Md5FilePathHex(const char *path)`: the sole argument is passed to `_fopen_s` as a filename, success returns the heap string from `Md5DigestToHexString`, failure returns the shared fallback literal, and the modeled stream sibling already returns `char *`.

## Assignment Gate

`CANONICAL_OWNER` and `EMITTER_UIDS` remain [UID:0000L6][MD5](by-file/MD5.md). This child is now `89/91`, and the accepted B005 support update raises the direct file parent to `88/87`, so the current combined-score/emitter code-entry gate clears. The physical containing range [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md) is updated in the same implementation batch to `86/89`, so its support evidence also clears the direct-parent side of the documentation gate.

## Score Rationale

Completion is `89` because the raw body's exact range, alignment, stack frame, file I/O loop, MD5 state initialization, internal callees, fallback literal, owner/emitter route, source-facing name, and first-draft formal C++ are now documented from current IDA and PE evidence.

Confidence is `91` for the written claims: the raw bytes, surrounding modeled helpers, internal xrefs, fallback path, MD5 utility ownership, source-facing signature inference, and raw PE negative route scan all agree. Completion does not rise higher because IDA still lacks a function object for `0x00515450`, no direct caller or pointer route reaches the raw start, and exact original `MD5Context` / helper spelling remains inferred. Confidence does not rise higher because the fallback literal's original semantic reason and the exact broader MD5 source type names are not proven from original source metadata.

## 2026-06-01 IDA MCP Evidence

- `lookup_funcs` reports `0x00515450` as not a function, while surrounding modeled functions remain `sub_5153E0` at `0x005153e0` size `0x65` and `sub_515570` at `0x00515570` size `0xeb`.
- `xrefs_to` and `callers` still show no direct references to `0x00515450`, which keeps liveness confidence below the modeled stream wrapper.
- Byte checks show `0x00515445-0x00515450` as `0xcc` alignment, then a prologue at `0x00515450`, a `0x474`-byte stack frame, a zeroed 1024-byte buffer, standard MD5 initialization constants, `_fopen_s` mode pointer usage, repeated `_fread`/`Md5Update` calls, `Md5Final`, `_fclose`, and a call to `Md5DigestToHexString`.
- The same byte check confirms fallback return of the static literal address used for failed file-open/null-stream behavior and `0xcc` alignment at `0x00515568-0x00515570`.

## 2026-06-05 Live Recheck

IDA MCP still does not model `0x00515450` as a function and still reports no
`xrefs_to` or callers for the raw start. The neighboring modeled functions are
unchanged: `sub_5153E0` at `0x005153e0` remains size `0x65`, and `sub_515570`
at `0x00515570` remains size `0xeb`.

Focused disassembly/byte checks confirmed the raw body shape:

- `0x00515445-0x0051544f` is `0xcc` alignment, followed by `push ebp` at
  `0x00515450` and a `0x474`-byte stack frame at `0x00515453`.
- The MD5 context constants are written at stack offsets beginning with
  `0x67452301` at `0x00515475`.
- `_memset` is called at `0x005154a8`, `_fopen_s` at `0x005154ba`, `_fread` at
  `0x005154e0` and `0x00515517`, `Md5Update` at `0x005154fe`, `Md5Final` at
  `0x00515529`, `_fclose` at `0x00515534`, and `Md5DigestToHexString` at
  `0x00515540`.
- IDA target-side xrefs to `Md5Update`, `Md5Final`, and
  `Md5DigestToHexString` all show the call sites from this range with `fn:null`,
  matching the raw/unmodeled helper status.
- The helper returns at `0x00515568`, followed by `0xcc` alignment through
  `0x0051556f`.

## Reconstruction Notes

- Marked reconstructable because the body, boundaries, file-hash role, fallback behavior, and parent ownership are clear.
- Formal source now uses `MD5 md5`, constructor/Init semantics, `unsigned char buffer[1024]`, chunked `fread`, `md5.Update`, `md5.Final`, `fclose`, `Md5DigestToHexString(md5.digest)`, and the shared fallback literal. The prior `MD5Context`/manual-initialization form is historical.
- The missing IDA function object and no-current-caller/no-pointer route are preserved as confidence/liveness caps. They no longer block target-level emission because the raw body covers a complete function-shaped source range and the owner/emitter route through [UID:0000L6][MD5](by-file/MD5.md) is stable.
- `Md5FilePathHex` remains an inferred human wrapper spelling because no original symbol survives; class `MD5` and its method/field model are now the accepted high-probability source shape rather than a lingering blocker.

## Cross-References

- [UID:0000L6][MD5](by-file/MD5.md)
- [UID:0001B6][0x005151f0-0x00515f48.MD5HashHelpers](by-memory/0x005151f0-0x00515f48.MD5HashHelpers.md)
- [UID:0000VF][ScreenshotProofPacket_00558240](by-item/ScreenshotProofPacket_00558240.md)

## Changes

- 2026-07-26 B001 UID0001B6 callback:
  - Raised `89/91 -> 91/92`, rerouted the exact child through UID0001B6 at position `50`, replaced `MD5Context` and manual initialization with class `MD5`, changed the I/O buffer to unsigned bytes, added the H declaration, and preserved the exact `0x515569` end, raw/no-route evidence, fallback behavior, and historical filename suffix.

- 2026-06-25 B005 accepted implementation:
  - What existed before: `COMPLETION:86`, `CONFIDENCE:90`, blank formal C++, and stale wording that treated no IDA function object and no current caller as a blank-C++ blocker.
  - Changed to: `COMPLETION:89`, `CONFIDENCE:91`, owner/emitter/reconstructable fields unchanged, and formal first-draft `Md5FilePathHex(const char *path)` C++ added to the managed reconstruction block.
  - Summary/evidence: current MCP session `80de0a67` reconfirmed the health-ready NexusTK IDB, no function object at `0x00515450`/`0x00515568`/`0x00515569`, modeled neighbors `sub_5153E0` and `sub_515570`, no raw-start xrefs, unique `0x119`-byte half-open body signature, seven `0xcc` padding bytes, `0x474` stack frame, `0x400` buffer, standard MD5 constants, `_fopen_s("rb")`, `_fread`/`Md5Update` loop, `Md5Final`, `_fclose`, `Md5DigestToHexString`, and fallback literal xrefs from the file and stream helpers. A read-only PE scan found no VA/RVA pointer or direct rel32 route to `0x00515450`, while control scans found the expected routes to `Md5StreamHex` and the packet digest helper. The no-route state remains a confidence cap rather than an emitter blocker.

- 2026-06-13 A001 Goal 2 gate repair:
  - What existed before: `COMPLETION:76`, `CONFIDENCE:84`, and prose that treated `0x00515450-0x00515568` as the displayed range without clarifying the half-open exclusive end.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:90`, with `AUTOGEN_PARENT_UID:0000L6` retained and the body documented as half-open `0x00515450-0x00515569` while preserving the legacy filename suffix.
  - Summary/evidence: live IDA MCP session `a001_goal2_0002UM` reconfirmed no function object or direct start xrefs, the unique `0x119`-byte body signature, seven `0xcc` alignment bytes before `Md5StreamHex`, the `0x474` stack frame, standard MD5 constants, `_fopen_s("rb")`, `_fread` loop, `Md5Update`/`Md5Final`/`Md5DigestToHexString` calls, two fallback-literal xrefs in the MD5 island, and direct parent [UID:0000L6][MD5](by-file/MD5.md) at `86/85`.

- 2026-06-01 IDA MCP rescore:
  - What existed before: `COMPLETION:50`, `CONFIDENCE:65`, reconstructable blank, and no autogen parent.
  - Changed to: `COMPLETION:72`, `CONFIDENCE:82`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000L6`.
  - Summary/evidence: byte checks confirm a complete function-shaped MD5 file-path hashing wrapper bounded by `0xcc` padding, while lookup/caller checks confirm the current database still lacks a function object and direct callers.
- 2026-06-05 live IDA recheck:
  - What existed before: `COMPLETION:72`, `CONFIDENCE:82`, with the raw helper documented but no detailed current call-site inventory.
  - Changed to: `COMPLETION:76`, `CONFIDENCE:84`; reconstructable status, parent UID, and blank C++ block are retained.
  - Summary/evidence: live `lookup_funcs`, `xrefs_to`, `callers`, disassembly, byte, and target-side xref checks confirm the same no-function/no-caller caveat, exact stack-frame and padding boundaries, and concrete internal call sites for `_fopen_s`, `_fread`, `Md5Update`, `Md5Final`, `_fclose`, and `Md5DigestToHexString`.
