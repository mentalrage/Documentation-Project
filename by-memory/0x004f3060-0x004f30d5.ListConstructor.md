*** UID:000191 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000079 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000079 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
List::List(int elementSize, int pageSize)
    : LObject()
{
    m_elementSize = elementSize;
    m_pageSize = pageSize;
    m_count = 0;
    m_data = GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: List class parent-attached constructor with exact bounds, next-function boundary, 61 broad generic-container callers, three callees, `0x0061ce2c` vtable install, element-size/page-size/count/backing-buffer field initialization, and initial allocation behavior. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f3060-0x004f30d5 List::List

Advanced-scan identity: 0x004f3060, 0x004f30d5, ListConstructor.

## Status

- Entity kind: constructor
- Function: [UID:000079][List](by-class/List.md)
- Likely source file: [UID:0000KS][List](by-file/List.md)
- Confidence: very strong for constructor boundary, `List::List(int elementSize, int pageSize)` source shape, layout writes, helper route, destructor pairing, broad caller set, ResourceLayout alias rejection, and class/file attachment; capped below final because exact original private member spelling and final MemoryMan helper declaration style remain inferred.
- Current recovered source: `source-3/simroot_v2/class_List.cpp`
- Rebuild handling: source-authored generic container constructor; compiler/runtime should regenerate SEH scaffolding and adjacent alignment.

## Behavior

`List::List(int elementSize, int pageSize)` initializes the shared fixed-width dynamic array container:

```text
this->vtable         = List_vtable
this->m_elementSize  = elementSize
this->m_pageSize     = pageSize
this->m_count        = 0
this->m_data         = GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize)
```

The constructor first chains to `LObject::LObject`, then installs the `List` vtable at `0x0061ce2c`.

## Signature Notes

Working signature from local Ghidra bridge on 2026-06-06 is `List *__thiscall List::List(List *this, int elementSize, int pageSize)`. The return value is `this`; the constructor stores `elementSize` at `this + 0x04`, `pageSize` at `this + 0x08`, zeroes `count` at `this + 0x0c`, and stores the allocated backing buffer at `this + 0x10`.

2026-06-19 B008 source-quality reanalysis keeps the source-facing signature as `List::List(int elementSize, int pageSize)`. The `m_` member names are recommended descriptive source names, not proven original spellings: `m_elementSize` at `+0x04`, `m_pageSize` at `+0x08`, `m_count` at `+0x0c`, and `m_data` at `+0x10`. The raw helper route is resolved as `GetMemoryMan()` at `0x00516030` followed by `ZeroAllocateBufferMemory` at `0x005160d0`, giving source expression `GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize)`.

## Boundary Evidence

- 2026-05-28 IDA MCP recheck shows `sub_4F3060` ending at `0x004f30d5`; byte `0x004f30d4` is the final `00` byte of the constructor's `C2 08 00` `retn 8`.
- `0x004f30d5-0x004f30e0` is compiler/linker alignment before [UID:000192][0x004f30e0-0x004f3139.ListNonDeletingDestructor](by-memory/0x004f30e0-0x004f3139.ListNonDeletingDestructor.md).
- Local Ghidra bridge `containing 0x004f3060` on 2026-06-06 reports function start `0x004f3060`, next function `0x004f30e0`, and signature `List *__thiscall List(List *this, int param_1, int param_2)`, matching the exact end plus the documented `0x004f30d5-0x004f30e0` padding.
- 2026-06-19 B008 raw PE/Capstone pass against the read-only `NexusTK.exe` reconfirmed the exact half-open range `0x004f3060-0x004f30d5`, the `ret 8` at `0x004f30d2`, and `0xcc` padding at `0x004f30d5-0x004f30e0` before [UID:000192][0x004f30e0-0x004f3139.ListNonDeletingDestructor](by-memory/0x004f30e0-0x004f3139.ListNonDeletingDestructor.md).

## Evidence Notes

- Local Ghidra bridge decompile on 2026-06-06 reports 61 callers and 3 callees. The body calls `LObject::LObject` at `0x004f4a80`, setup/helper `0x00516030`, and allocator helper `0x005160d0`.
- The decompile confirms the constructor writes vtable `0x0061ce2c`, stores `elementSize`, `pageSize`, and count zero, allocates `elementSize * pageSize` bytes, stores the returned buffer at `this + 0x10`, restores the exception list, and returns `this`.
- Local Ghidra bridge caller set covers generic container users, including `Config`, `ImageLib`, `IMEPane`, `InputMan`, `HierList`, `PaletteLib`, `ScreenPane`, `TextEditPane`, `ListPane`, `MenuPane`, `SystemMessagePane`, `UserPane`, `LivingObjectPane`, `ObjectList`, `ThreadMan`, and several resource/UI helper constructors. This confirms generic container ownership rather than a feature-local list.
- B008 raw rel32 scan found 61 direct calls to `0x004f3060`. Representative argument patterns confirm that the first stack argument after `this` is the element size in bytes and the second is the page-size / initial allocation quantum in elements: `BlackHole` calls `List(4, 1024)`, `ImageLib` calls `List(44, 10)` for resource-layout records, `HierList` calls `List(payloadSize + 0x0b, 32)`, `TextEditPane` calls `List(2, 128)` and `List(16, 16)`, TextBox/TextEdit invalidation helpers use temporary stack `List(16, 16)` objects, object-list tiers mostly use `List(4, dynamicCapacity)`, and prior ChattingPane evidence records `List(4, 1000)` for the chat-message pointer list.

## Touched State

| State | Evidence | Reconstruction note |
| --- | --- | --- |
| `this + 0x00` vtable | Stores `0x0061ce2c` after `LObject` construction. | Confirms concrete `List` object before derived classes such as `HierList`/`SortedList` overwrite the vtable. |
| `this + 0x04` / `m_elementSize` | Set from first constructor argument. | Fixed-width element stride used by core list methods; name is inferred/descriptive. |
| `this + 0x08` / `m_pageSize` | Set from second constructor argument. | Initial allocation and growth quantum in elements, not a byte count; name is inferred/descriptive. |
| `this + 0x0c` / `m_count` | Set to zero. | Empty list after construction. |
| `this + 0x10` / `m_data` | Receives `GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize)`. | Owned raw backing buffer; `List` does not construct typed elements. |
| Callees | `0x004f4a80`, `0x00516030`, `0x005160d0`. | `LObject::LObject`, `GetMemoryMan`, and `ZeroAllocateBufferMemory`. |

## Source-Quality Reanalysis

2026-06-19 B008 reanalysis resolves the generated/raw names and ownership direction as follows:

| Issue | Evidence checked | Current best direction | Rejected alternative / impact |
| --- | --- | --- | --- |
| Function name and owner | Target page, List class/file docs, vtable data, 61 raw callers, caller docs | [UID:000079][List](by-class/List.md) owns `List::List(int elementSize, int pageSize)` through [UID:0000KS][List](by-file/List.md). | `sub_4F3060` is generated; `ResourceLayoutStore::ResourceLayoutStore` is contradicted by broad generic callers and the `List` vtable. |
| Parameter roles | Stores to `+0x04/+0x08`, allocation formula, caller constants | `elementSize` and `pageSize`. | `param_1`/`param_2` are placeholders. `capacity` alone is weaker because `+0x08` behaves as the growth/initial page quantum, not a byte capacity field. |
| Field names | Constructor stores, destructor cleanup, core List method behavior | `m_elementSize`, `m_pageSize`, `m_count`, `m_data`. | Exact original spelling is not binary-provable; keep these marked inferred/descriptive rather than raw `this[1]` style. |
| Allocation helper | [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md), [UID:0001BE][0x005160d0-0x00516162.ZeroAllocateBufferMemory](by-memory/0x005160d0-0x00516162.ZeroAllocateBufferMemory.md), raw call order | `GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize)`. | `AllocateZeroedBufferOrThrow`, `sub_516030`, and `sub_5160D0` are stale/generated wording for this target. |
| Base and vtable writes | Raw call to `0x004f4a80`; vtable store `0x0061ce2c`; [UID:0003OL][0x0061ce28-0x0061ce84.ListSortedListVtableData](by-memory/0x0061ce28-0x0061ce84.ListSortedListVtableData.md) | Source is a normal `List` constructor with `LObject()` base construction; the vtable store is compiler output from the class declaration. | Do not hand-code `LObject_Constructor(this)` or vtable writes in C++. |
| Owned resource | Constructor allocates `+0x10`; destructor frees `+0x10`; caller-specific docs clean payloads separately | `List` owns only raw backing storage. | Do not add per-element destructor/free logic to `List::List` or `List::~List`; payload ownership is caller-specific. |
| EH and scalar-delete support | Constructor prologue/scopetable/security cookie; [UID:00037L][0x004f3810-0x004f38a7.ListScalarDeletingDestructor](by-memory/0x004f3810-0x004f38a7.ListScalarDeletingDestructor.md) | Keep EH, cookie, vtable stores, and scalar-delete flags out of handwritten C++; compiler regenerates them. | Emitting ABI wrapper/decompiler pseudocode would misrepresent the original source. |
| Source placement | Broad caller fan-in and file/class docs | `NexusTK/util/List.cpp` through [UID:0000KS][List](by-file/List.md). | Render/UI/map-specific files are consumers only. |
| ResourceLayout relationship | `ImageLib::ImageLib` call to `List(44, 10)`, ResourceLayout lookup/accessor docs, stale alias page | The resource registry is a generic `List` of 44-byte [UID:0001VT][ResourceLayoutNameRecord](by-type/by-struct/ResourceLayoutNameRecord.md) rows. | Do not create `ResourceLayoutStore::ResourceLayoutStore` or specialize this constructor for resource-layout behavior. |

## Destructor Pairing

[UID:000192][0x004f30e0-0x004f3139.ListNonDeletingDestructor](by-memory/0x004f30e0-0x004f3139.ListNonDeletingDestructor.md) pairs with this constructor: it reads `m_data` from `+0x10`, calls [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md), calls [UID:0001BF][0x00516170-0x00516184.FreeBufferMemory](by-memory/0x00516170-0x00516184.FreeBufferMemory.md), stores the helper's zero return back to `m_data`, and chains to `LObject` teardown. The scalar deleting destructor [UID:00037L][0x004f3810-0x004f38a7.ListScalarDeletingDestructor](by-memory/0x004f3810-0x004f38a7.ListScalarDeletingDestructor.md) is ABI support around that source destructor and optional object deletion.

## First-Draft C++ Rationale

The target covers the full constructor function, has a resolved direct owner/emitter, uses named source-facing allocation helpers, and has no pending split/ownership blocker. The first-draft C++ intentionally expresses ordinary mid-2000s C++ constructor logic and omits compiler-generated SEH/cookie/vtable/delete-wrapper machinery:

```cpp
List::List(int elementSize, int pageSize)
    : LObject()
{
    m_elementSize = elementSize;
    m_pageSize = pageSize;
    m_count = 0;
    m_data = GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize);
}
```

If later header work chooses bare field names (`elementSize`, `pageSize`, `count`, `data`) instead of `m_` names, the member names may be mechanically adapted without changing the recovered behavior.

## ResourceLayout Significance

IDA MCP decompilation of `ImageLib::ImageLib` at `0x004cffb0` shows:

```text
allocatedObject = AllocateObject(20)
entryList = List::List(allocatedObject, 44, 10)
this[3] = entryList
```

This proves the object previously documented as `ResourceLayoutStore` is a generic `List` instance with 44-byte records, not a separate resource-layout class.

B008 reanalysis adds that `ResourceLayoutStore` is already [UID:0001VU][ResourceLayoutStore](by-type/by-struct/ResourceLayoutStore.md) and has no separate vtable. The registry uses generic `List` vtable slots for `GetElementAt` and `Append`; the source should prefer `List* m_entryList` or documentation-only `List<ResourceLayoutNameRecord>* m_entryList`, not a custom `ResourceLayoutStore` constructor.

## Caller Context

The 61-callsite local Ghidra bridge caller set spans resource tables, UI panes, text edit storage, object lists, image/palette managers, threading, and hierarchical lists. That breadth supports [UID:000079][List](by-class/List.md) as the owning class and [UID:0000KS][List](by-file/List.md) as the likely source file; no single feature subsystem should own this constructor.

## Cross-References

- [UID:000079][List](by-class/List.md)
- [UID:0000KS][List](by-file/List.md)
- [UID:000172][0x004cffb0-0x004d011b.ImageLibLifecycleCluster](by-memory/0x004cffb0-0x004d011b.ImageLibLifecycleCluster.md)
- [UID:000174][0x004d0120-0x004d1860.ResourceLayoutTable](by-memory/0x004d0120-0x004d1860.ResourceLayoutTable.md)
- [UID:0001VU][ResourceLayoutStore](by-type/by-struct/ResourceLayoutStore.md)
- [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md)
- [UID:0001BE][0x005160d0-0x00516162.ZeroAllocateBufferMemory](by-memory/0x005160d0-0x00516162.ZeroAllocateBufferMemory.md)
- [UID:0001BF][0x00516170-0x00516184.FreeBufferMemory](by-memory/0x00516170-0x00516184.FreeBufferMemory.md)

## Changes

### 2026-06-21 - B008 Rule 26 source-quality incorporation

- What existed before: page was `84/90`, had strong constructor identity evidence, but still used stale helper wording (`AllocateZeroedBufferOrThrow` / raw helper names), had no first-draft C++, and did not record B008's caller/destructor/ResourceLayout reconciliation at report-level detail.
- What changed: raised to `92/93`, populated first-draft C++, adopted inferred source-facing field names (`m_elementSize`, `m_pageSize`, `m_count`, `m_data`), resolved helper expression `GetMemoryMan()->ZeroAllocateBufferMemory(elementSize * pageSize)`, added raw PE/Capstone boundary confirmation, caller argument evidence, destructor pairing, heuristic decision table, no-code boundaries for EH/vtable/scalar-delete support, and ResourceLayout alias rejection.
- Evidence: B008 raw PE/Capstone pass, 61-call rel32 scan, existing List class/file/core/destructor/vtable docs, MemoryMan helper pages, ResourceLayout alias/type pages, and representative caller docs for BlackHole, ImageLib, HierList, ObjectList, TextEdit/TextBox invalidation, and ChattingPane usage.

### 2026-06-06 A003 - Added bridge evidence and parent attachment

- What existed before: page was `70/85`, had no autogen parent, and documented behavior from prior IDA notes but lacked current caller/callee/signature/touched-state detail.
- What changed: raised to `84/90`, attached to [UID:000079][List](by-class/List.md), and added local Ghidra bridge signature, caller/callee counts, boundary, touched-state, and caller-context evidence.
- Evidence: local Ghidra bridge `decompile`, `xrefs-to`, `xrefs-from`, and `containing` for `0x004f3060` report the same constructor body, 61 callers, 3 callees, and next-function boundary at `0x004f30e0`; parent [UID:000079][List](by-class/List.md) is `88/88`, satisfying the attachment gate.

### 2026-05-28 - Corrected terminal range byte

- What existed before: the page and filename ended at `0x004f30d4`, leaving `0x004f30d4-0x004f30e0` as an UNKNOWN gap.
- What changed: the page and filename now end at `0x004f30d5`; only `0x004f30d5-0x004f30e0` is treated as alignment before `ListNonDeletingDestructor`.
- Evidence: IDA MCP shows `sub_4F3060` ending at `0x004f30d5`; `0x004f30d4` is the final byte of `retn 8`, and `0x004f30d5-0x004f30df` is `0xcc` alignment before `sub_4F30E0`.
