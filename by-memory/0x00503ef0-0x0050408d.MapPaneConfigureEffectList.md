*** UID:0002I4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::ConfigureScreenEffecterList(signed char effectMode)
{
    enum {
        kMapScreenEffectRedWave = 1,
        kMapScreenEffectWaterFilter = 2,
        kMapScreenEffecterCapacity = 10
    };

    ClearScreenEffecterList();

    if (effectMode == kMapScreenEffectWaterFilter) {
        m_screenEffecters = new ScreenEffecter *[kMapScreenEffecterCapacity];
        m_screenEffecterCount = 1;
        m_screenEffecterCapacity = kMapScreenEffecterCapacity;
        m_screenEffecters[0] = new WaterFilterEffecter();
        return;
    }

    if (effectMode == kMapScreenEffectRedWave) {
        m_screenEffecters = new ScreenEffecter *[kMapScreenEffecterCapacity];
        m_screenEffecterCount = 2;
        m_screenEffecterCapacity = kMapScreenEffecterCapacity;

        m_screenEffecters[0] = new XWaveEffecter(
            g_mapTilePixelWidth * 6,
            g_mapTilePixelHeight / 10.0f,
            7500);
        m_screenEffecters[1] = new SolidColorFilterEffecter(255, 0, 0, 0.6f);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms the exact configure-helper boundary, clear-first call, mode `1`/`2` branches, pointer-array allocation, MapPane effect-list count/capacity/pointer writes, object allocation sizes, constructor-like callees, tile-dimension inputs, no direct start xrefs, and padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00503ef0-0x0050408d - MapPane Configure Effect List

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: method.
- Owner: [UID:00007Q][MapPane](by-class/MapPane.md), emitted through [UID:0000L3][MapPane](by-file/MapPane.md).
- Confidence: strong for boundary, screen-effecter field writes, allocation behavior, effecter object identities, mode dispatch behavior, sibling lifecycle, ownership, and first-draft source shape; medium-high for caller provenance and exact original mode/helper names.
- Autogen parent: [UID:00007Q][MapPane](by-class/MapPane.md). Formal first-draft C++ is populated because B002's 2026-06-18 source-quality pass resolves the prior field/type/helper-name blockers to the MapPane-owned `ScreenEffecter *` list at `+0xf8/+0xfc/+0x100`. The no-direct-caller/no-pointer-route condition remains documented as real PE evidence, but it no longer blocks first-draft C++ because the body is source-authored MapPane list-state configuration with stable field, mode, object, and type roles.

## Covered Range

| Range | IDA model | Role |
| --- | --- | --- |
| `0x00503ef0-0x0050408d` | modeled function, size `0x19d` | Clears and configures the MapPane runtime screen-effecter pointer list for mode `1` or mode `2`. |

## Behavior

The method first calls [UID:0002I6][0x005040a0-0x00504102.MapPaneClearEffectList](by-memory/0x005040a0-0x00504102.MapPaneClearEffectList.md) to destroy any existing runtime screen-effecter list. For mode `2`, it allocates a `0x28` / 40-byte `ScreenEffecter *` pointer array, sets `m_screenEffecterCount` to `1` and `m_screenEffecterCapacity` to `0x0a` / 10, allocates a `0x11c` / 284-byte [UID:0000FZ][WaterFilterEffecter](by-class/WaterFilterEffecter.md) object through the project operator-new wrapper, runs the constructor at `0x0055b6a0`, and stores the result in slot `0`. If object allocation fails, slot `0` is set to null. Numeric conversions were verified with `int_convert.py`.

For mode `1`, it allocates the same `0x28` / 40-byte `ScreenEffecter *` pointer array, sets count `2` and capacity `0x0a` / 10, constructs a `0x10` / 16-byte [UID:0000GC][XWaveEffecter](by-class/XWaveEffecter.md) through the CRT `operator new` path and the constructor at `0x0055b2a0`, then constructs a `0x110` / 272-byte [UID:0000DE][SolidColorFilterEffecter](by-class/SolidColorFilterEffecter.md) through the project operator-new wrapper and the constructor at `0x0055ae00`. The first object uses [UID:0000T7][MapTilePixelDimensions](by-global/MapTilePixelDimensions.md): width is scaled by `6`, height is divided by `10.0`, and the literal duration is `0x1d4c` / 7500. The second object is initialized with RGB `(255, 0, 0)` and opacity `0.60000002`.

B002's 2026-06-18 reanalysis accepts `MapPane::ConfigureScreenEffecterList(signed char effectMode)` as the best source-facing name/signature. The function reads only the low signed byte of the single stack argument (`movsx eax, byte ptr [ebp+8]`) and returns with `ret 4`, so the draft uses `signed char effectMode` intentionally. The first source action is a call to [UID:0002I6][0x005040a0-0x00504102.MapPaneClearEffectList](by-memory/0x005040a0-0x00504102.MapPaneClearEffectList.md) `ClearScreenEffecterList()`. PE direct-call, absolute-VA literal, and RVA literal scans found no inbound route to `0x00503ef0`; this caps final original-name confidence but does not block formal C++.

Best descriptive constants from the same pass:

| Value | Recommended name | Evidence |
| ---: | --- | --- |
| `1` | `kMapScreenEffectRedWave` | Configures an `XWaveEffecter` plus red `SolidColorFilterEffecter`, so it is a red wave/filter screen presentation rather than a plain weather layer. |
| `2` | `kMapScreenEffectWaterFilter` | Configures a single `WaterFilterEffecter`. |
| `10` | `kMapScreenEffecterCapacity` | The pointer-array allocation is `0x28` bytes / 10 x 4-byte pointers, and the capacity field is set to `10`. |

Rejected alternatives:

- `kWeatherRain`, `kWeatherSnow`, or other generic weather-mode names are rejected because weather overlay pane construction lives elsewhere and this helper builds runtime `ScreenEffecter` objects.
- `kFire`, `kLava`, `kDamage`, or `kHeatHaze` remain possible user-facing interpretations of the red wave/filter composition, but no resource string, packet enum, or caller maps mode `1` to those names.
- `SetWeatherEffect` is too broad for this exact body because the function clears and rebuilds a screen-effecter list for two low-byte modes and is not proven to be the public weather packet setter.
- Moving the helper to `Effects.cpp`, `Layer`, or a WeatherLayer source is rejected: Effects owns effecter classes, Layer consumes selected effecters, WeatherLayer owns overlay pane implementations, and MapPane owns the `+0xf8/+0xfc/+0x100` list lifecycle.

## Effect-List Field Relationship

| Field | Access here | Sibling evidence |
| --- | --- | --- |
| `MapPane +0x0f8` / inferred `m_screenEffecterCount` | Writes configured count: `1` for mode `2`, `2` for mode `1`. | [UID:0002I1][0x00503e80-0x00503e8b.MapPaneHasEffectListEntries](by-memory/0x00503e80-0x00503e8b.MapPaneHasEffectListEntries.md) reads it; [UID:0002I2][0x00503e90-0x00503ecd.MapPaneFindActiveEffect](by-memory/0x00503e90-0x00503ecd.MapPaneFindActiveEffect.md) iterates it; [UID:0002I6][0x005040a0-0x00504102.MapPaneClearEffectList](by-memory/0x005040a0-0x00504102.MapPaneClearEffectList.md) clears it. |
| `MapPane +0x0fc` / inferred `m_screenEffecterCapacity` | Writes capacity `10`. | [UID:0002I3][0x00503ed0-0x00503eef.MapPaneResetEffectListFields](by-memory/0x00503ed0-0x00503eef.MapPaneResetEffectListFields.md) and clear helper clear the same capacity field. |
| `MapPane +0x100` / inferred `m_screenEffecters` | Writes the allocated `ScreenEffecter *` array base. | Find reads it; clear iterates and frees it; reset clears it. |

## Allocation And Object Setup

| Path | Pointer array | Stored objects |
| --- | --- | --- |
| mode `2` | `0x28` / 40-byte array, count `1`, capacity `0x0a` / 10 | Slot `0` receives [UID:0000FZ][WaterFilterEffecter](by-class/WaterFilterEffecter.md) built through `0x0055b6a0` after a `0x11c` / 284-byte project allocation, or null on allocation failure. |
| mode `1` | `0x28` / 40-byte array, count `2`, capacity `0x0a` / 10 | Slot `0` receives [UID:0000GC][XWaveEffecter](by-class/XWaveEffecter.md) built through `0x0055b2a0` using tile dimensions and duration `0x1d4c` / 7500; slot `1` receives [UID:0000DE][SolidColorFilterEffecter](by-class/SolidColorFilterEffecter.md) built through `0x0055ae00` using RGB `(255, 0, 0)` and opacity `0.60000002`, or null if allocation fails. |

## Score Rationale

Raised from `87/90` to `88/91` because B002's 2026-06-18 source-quality pass resolves the helper as `MapPane::ConfigureScreenEffecterList(signed char effectMode)`, records the low-byte mode dispatch, identifies mode `1` as a red wave/filter screen-effect list, identifies mode `2` as a water-filter screen-effect list, keeps the real no-direct-caller/no-pointer-route evidence, and supplies first-draft C++. Confidence remains below final because exact original enum/helper spelling and caller provenance are not proven.

## IDA Evidence

- `lookup_funcs` reports a modeled function at `0x00503ef0`, size `0x19d`; `0x0050408d` is not a function start.
- The first internal call is [UID:0002I6][0x005040a0-0x00504102.MapPaneClearEffectList](by-memory/0x005040a0-0x00504102.MapPaneClearEffectList.md) at `0x00503f15`.
- `callees` reports the clear helper, the project operator-new wrapper at `0x004f4aa0`, the CRT `operator new` path, the pointer-array allocator at `0x005c7790`, and constructor-like callees at `0x0055b2a0`, `0x0055ae00`, and `0x0055b6a0`.
- The helper reads [UID:0000T7][MapTilePixelDimensions](by-global/MapTilePixelDimensions.md), using the map-tile width and height globals in the mode `1` first-object path.
- `callers` and `xrefs_to` found no direct edges to the function start, so reachability remains open even though the body is a real modeled function.
- Byte checks show one `0xcc` byte before the function, three `0xcc` bytes after its `ret 4`, then the ignored no-op callback at `0x00504090` before the clear helper.
- 2026-06-16 live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed `sub_503EF0` as a `0x19d` / 413-byte function (Verified with `int_convert.py`) and decompiled the branches as `mode == 2` storing one `WaterFilterEffecter` pointer and `mode == 1` storing `XWaveEffecter` plus `SolidColorFilterEffecter` pointers.
- `xrefs_to` for the constructors reports the configure-helper call sites at `0x00503f6e` (`WaterFilterEffecter`), `0x0050401f` (`XWaveEffecter`), and `0x00504067` (`SolidColorFilterEffecter`). The same constructors also have MapPane paint-lighting callers at `0x0050a5f5`, `0x0050a68c`, and `0x0050a6d4`, confirming the effecter ownership relationship without moving the effect classes out of [UID:0000IZ][Effects](by-file/Effects.md).
- `xrefs_to` still reports no references to `0x00503ef0`, and a loaded-segment byte-pattern search for little-endian pointer bytes `f0 3e 50 00` found no hits. The no-caller condition was rechecked and remains a real source-quality blocker.

## Remaining Source-Name Uncertainty

The body, fields, constructed effecter family, source owner, and first-draft C++ shape are resolved. The exact original mode enum names are not symbol-proven, but the strongest descriptive constants are `kMapScreenEffectRedWave = 1` for the `XWaveEffecter` plus red `SolidColorFilterEffecter` composition and `kMapScreenEffectWaterFilter = 2` for the single `WaterFilterEffecter` composition. PE evidence still shows no direct caller or pointer route to `0x00503ef0`; this caps final original-name confidence but does not block formal C++.

## Cross-References

- [UID:0001AO][0x005039f0-0x00504521.GameServerNationAndMapInit](by-memory/0x005039f0-0x00504521.GameServerNationAndMapInit.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000T7][MapTilePixelDimensions](by-global/MapTilePixelDimensions.md)
- [UID:0000T8][MemoryAllocationHelpers](by-global/MemoryAllocationHelpers.md)
- [UID:0000IZ][Effects](by-file/Effects.md)
- [UID:0000GC][XWaveEffecter](by-class/XWaveEffecter.md)
- [UID:0000DE][SolidColorFilterEffecter](by-class/SolidColorFilterEffecter.md)
- [UID:0000FZ][WaterFilterEffecter](by-class/WaterFilterEffecter.md)
- [UID:0002I1][0x00503e80-0x00503e8b.MapPaneHasEffectListEntries](by-memory/0x00503e80-0x00503e8b.MapPaneHasEffectListEntries.md)
- [UID:0002I2][0x00503e90-0x00503ecd.MapPaneFindActiveEffect](by-memory/0x00503e90-0x00503ecd.MapPaneFindActiveEffect.md)
- [UID:0002I3][0x00503ed0-0x00503eef.MapPaneResetEffectListFields](by-memory/0x00503ed0-0x00503eef.MapPaneResetEffectListFields.md)
- [UID:0002I6][0x005040a0-0x00504102.MapPaneClearEffectList](by-memory/0x005040a0-0x00504102.MapPaneClearEffectList.md)

## Changes

- 2026-06-16 A002 MapPane effect-list refresh:
  - What existed before: `80/88`, with raw constructor labels and unresolved object-type blockers.
  - Changed to: `87/90`, preserving owner/emitter [UID:00007Q][MapPane](by-class/MapPane.md) and blank C++.
  - Historical evidence: live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed the `0x00503ef0-0x0050408d` boundary, branch behavior, clear-first call, object allocation sizes, tile-dimension use, constructor call sites, no direct xrefs to the helper start, no loaded pointer bytes for `0x00503ef0`, and padding around the helper. Support docs identify the stored effect objects as `WaterFilterEffecter`, `XWaveEffecter`, and `SolidColorFilterEffecter`; the older mode/helper-name blocker is superseded by the 2026-06-18 B002 pass, while caller provenance remains below final proof.
- 2026-06-17 B002 support cleanup:
  - What existed before: field/source blockers still included the list triplet and generic effect/object pointer wording.
  - Changed to: preserved `87/90`, identified the pointer list as a `ScreenEffecter *` array, and updated the triplet to inferred `m_screenEffecterCount`, `m_screenEffecterCapacity`, and `m_screenEffecters`.
  - Evidence: B002 resolved the adjacent finder [UID:0002I2][0x00503e90-0x00503ecd.MapPaneFindActiveEffect](by-memory/0x00503e90-0x00503ecd.MapPaneFindActiveEffect.md) to `MapPane::FindActivePixelEffecter()` over this array, while the same configure helper constructs `XWaveEffecter`, `SolidColorFilterEffecter`, and `WaterFilterEffecter` entries.
- 2026-06-18 B002 effect-list helper source-quality execution:
  - Changed from `87/90` to `88/91`.
  - Accepted source-facing `MapPane::ConfigureScreenEffecterList(signed char effectMode)` and populated first-draft C++.
  - Evidence: PE disassembly confirms clear-first behavior, low signed-byte mode dispatch, mode `2` one-entry `WaterFilterEffecter` configuration, mode `1` two-entry `XWaveEffecter` plus red `SolidColorFilterEffecter` configuration, capacity `10`, tile-dimension inputs, duration `7500`, opacity `0.6`, no direct `E8` callers, and no absolute/RVA pointer literal hits for `0x00503ef0`.
  - Rejected alternatives: generic weather names, speculative fire/lava/damage/heat-haze enum names, broad `SetWeatherEffect`, Effects.cpp ownership, Layer ownership, and WeatherLayer ownership.
- 2026-06-06 live IDA refresh:
  - What existed before: `72/82`, parented to `00007Q`, but still used raw helper labels and decompiler-offset wording.
  - Changed to: `80/88`, source-facing MapPane field wording, allocation/object setup table, no-direct-xref status, and refreshed score rationale.
  - Historical evidence: live IDA confirms the exact `0x19d` modeled function, clear-first call, mode `1`/`2` branches, 40-byte pointer-array allocation, `+0x0f8/+0x0fc/+0x100` field writes, object allocation sizes, constructor-like callees, tile-dimension global reads, no direct start xrefs, and adjacent padding. This older blank-C++ rationale is superseded by the 2026-06-18 B002 source-quality pass, which resolves descriptive mode constants and populates first-draft C++.
- Before: the page was `65/78`, reconstructable but unparented.
- Changed to: `72/82`, `AUTOGEN_PARENT_UID:00007Q`, effect-list field relationship, and score rationale.
- Evidence: IDA confirms mode branches, field writes, allocation behavior, and `MapTilePixelDimensions` reads; sibling effect-list pages document the same count/capacity/pointer triplet. Historical blank-C++ reason superseded by the 2026-06-18 B002 pass.
