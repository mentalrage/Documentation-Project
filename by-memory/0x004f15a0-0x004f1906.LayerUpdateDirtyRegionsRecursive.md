*** UID:0004OB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000073 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000073 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:220 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Layer::UpdateDirtyRegionsRecursive(LayerNode *parentNode,
    HierList *list, Region *dirtyRegion)
{
    HierListNode *root = RootNode();
    const int count = list->GetCount();

    for (int index = 0; index < count; ++index) {
        LayerNode *node = NodeAt(list, index);
        HierListNode *header = NodeHeader(node);
        Pane *pane = node->pane;

        if (pane != NULL && pane->HasPendingMotion()) {
            Region motionRegion;
            Region paneRegion;
            pane->GetCurrentMotionRegion(&motionRegion);
            pane->GetRenderRegion(&paneRegion);
            motionRegion.IntersectWith(paneRegion);
            pane->BeginMotionPaint();
            if (!pane->SuppressesMotionRefresh()) {
                pane->RefreshMotionLayout();
            }
            pane->EndMotionPaint();

            HierListNode *ancestor = header;
            Pane *ancestorPane = pane;
            while (ancestor != root) {
                Point ancestorOffset;
                ancestorPane->GetLocalOffset(&ancestorOffset);
                motionRegion.Offset(ancestorOffset.y, ancestorOffset.x);
                ancestor = ancestor->parent;
                ancestorPane = NodeData(ancestor)->pane;
            }

            LayerNode *ancestorNode = NodeData(ancestor);
            ancestorNode->dirty = true;
            ancestorNode->dirtyRegion->UnionWith(motionRegion);
            if (pane->GetMode() != 0) {
                dirtyRegion->UnionWith(motionRegion);
            } else {
                dirtyRegion->Subtract(motionRegion);
            }
        }

        RectBounds currentBounds;
        pane->GetBounds(&currentBounds);
        if (currentBounds != node->bounds) {
            Region currentRegion;
            pane->GetRenderRegion(&currentRegion);
            currentRegion.Offset(currentBounds.top, currentBounds.left);
            currentRegion.IntersectWithRect(&currentBounds);

            RectBounds currentRegionBounds;
            currentRegion.GetRect(&currentRegionBounds);

            Region changedRegion;
            changedRegion.SetRect(&currentRegionBounds);
            changedRegion.UnionWithRect(&node->previousBounds);

            Region previousRegion;
            parentNode->pane->GetRenderRegion(&previousRegion);
            changedRegion.IntersectWith(previousRegion);

            HierListNode *ancestor = header->parent;
            Pane *ancestorPane = NodeData(ancestor)->pane;
            while (ancestor != root) {
                Point ancestorOffset;
                ancestorPane->GetLocalOffset(&ancestorOffset);
                changedRegion.Offset(ancestorOffset.y, ancestorOffset.x);
                ancestor = ancestor->parent;
                ancestorPane = NodeData(ancestor)->pane;
            }

            LayerNode *ancestorNode = NodeData(ancestor);
            ancestorNode->dirty = true;
            ancestorNode->dirtyRegion->UnionWith(changedRegion);
            if (currentRegionBounds != node->previousBounds) {
                dirtyRegion->UnionWith(changedRegion);
            }

            node->bounds = currentBounds;
            node->previousBounds = currentRegionBounds;
        }

        if (header->childList != NULL && !node->blocksChildTraversal) {
            UpdateDirtyRegionsRecursive(node, header->childList, dirtyRegion);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Recursively compares pane bounds, records movement damage, updates node snapshots, and aggregates dirty Regions through visible Layer children. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f15a0-0x004f1906 LayerUpdateDirtyRegionsRecursive

## Status

- Exact exclusive range: `[0x004f15a0,0x004f1906)`; 870 bytes; accepted SHA256 evidence `8290EFF0...E00D`.
- Disposition: source-authored Layer member, reconstructable through [UID:000073][Layer](by-class/Layer.md) at source position 220.
- Split parent: [UID:00018W][0x004f0480-0x004f1bf6.Layer](by-memory/0x004f0480-0x004f1bf6.Layer.md).
- Source family: [UID:0000KL][Layer](by-file/Layer.md), with the Layer class as semantic owner for source-authored members.

## Behavior

The method iterates each LayerNode, samples current pane bounds and visibility/traversal flags, compares bounds against previousBounds, unions old/new movement areas into the supplied dirty Region, updates node bounds snapshots and dirty state, combines each node's owned Region, and recurses into child lists unless the node blocks traversal. It preserves the accepted visibility, movement, and child-recursion order.

bounds and previousBounds are distinct snapshots, visible and blocksChildTraversal are separate bytes, and dirty is not an alias for either. Motion/Region operations remain exact dependencies rather than replacement types.

## Binary, ABI, And Boundary Evidence

- Fresh accepted IDA-backed reanalysis fixes the start at `0x004f15a0`, exclusive end at `0x004f1906`, and exact size at 870 bytes.
- [0x004f1906,0x004f1910) is ten bytes of all-0xcc alignment.
- The function lies wholly inside parent `[0x004f0480,0x004f1bf6)`, whose 6,006 bytes hash to `1444CFBA7D8179ED1BD2A084485C028364BBAC2DAD841AC0D48D1643352C232B`.
- No alternate entry, discontiguous chunk, embedded table, or non-Layer RTTI route intersects this leaf.
- Layer is an eight-byte LObject-derived object whose only data member is `HierList *m_nodes` at `+0x04`.
- Each LayerNode payload is exactly 48 bytes: pane `+0x00`, paneFlags `+0x04`, padding `+0x05..+0x07`, bounds `+0x08`, previousBounds `+0x18`, visible `+0x28`, blocksChildTraversal `+0x29`, dirty `+0x2a`, padding `+0x2b`, and owned Region pointer `+0x2c`. A complete HierList row has the eight-byte parent/childList header, this payload, and the established 59-byte stride.

## Reachability And Dependencies

CollectDirtyRegion enters at 0x004f0fb4; the body self-recurses at 0x004f18a5.

The Layer vtable starts at `0x0061ca48`; its scalar destructor slot, inherited slots, and adjacent RTTI boundary support Layer ownership without moving ordinary source bodies into a vtable/data page. Pane, ScreenPane, MapPane, HierList, Region/Motion, Application, WorldMapPane, and FlyingParcelPane remain callers, clients, or dependencies rather than implementation owners.

## Source Shape And Rejected Alternatives

The populated managed block above is the accepted behavior-identical human source shape. It preserves observed ordering, null/precondition behavior, payload asymmetries, recursion, and ownership while omitting only compiler mechanics.

Historical aggregate wording that treated this leaf as part of a 22-function monolith, left generated names/LayerNode fields unresolved, or proposed render-side ownership is superseded. Raw no-xref status, where applicable, is retained as negative reachability evidence rather than used to erase complete source. PDB spelling is unavailable, so the chosen name is the highest-probability source-facing spelling supported by behavior, callers, class order, and peer conventions.

## Cross-References

- [UID:000073][Layer](by-class/Layer.md)
- [UID:0000KL][Layer](by-file/Layer.md)
- [UID:000065][HierList](by-class/HierList.md)
- [UID:0000A2][Pane](by-class/Pane.md)
- [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md)
- [UID:0003OK][0x0061ca44-0x0061ca54.LayerVtableData](by-memory/0x0061ca44-0x0061ca54.LayerVtableData.md)

## Changes

### 2026-07-14 - Split from UID00018W

Created as an exact Layer leaf from mandatory live-IDA evidence. This page records the complete accepted behavior, range, ABI, ownership, source placement, negative evidence, and historical disposition without changing unrelated support metadata.

