*** UID:0002EZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ChattingPane::HandleServerSayPacket(const unsigned char *packet)
{
    enum ServerSaySubtype
    {
        kServerSayNormal = 0,
        kServerSayShout = 1,
        kServerSayBalloonOnly = 2,
        kServerSayUnfiltered = 3,
        kServerSayCustomColor = 4
    };

    wchar_t text[256];

    if (g_useEpfAssets == 1) {
        GetMemoryMan();

        const unsigned char subtype = packet[1];
        const unsigned char textLength = packet[6];
        const int convertedLength = MultiByteToWideChar(
            0,
            0,
            reinterpret_cast<const char *>(packet + 7),
            textLength,
            text,
            256);
        text[static_cast<unsigned char>(convertedLength)] = L'\0';

        unsigned int foreground;
        unsigned int background;
        if (subtype == kServerSayCustomColor) {
            const unsigned short foregroundOffset =
                static_cast<unsigned short>(textLength + 7);
            const unsigned short backgroundOffset =
                static_cast<unsigned short>(textLength + 11);
            foreground = PacketBufferReadUInt32BE(packet + foregroundOffset);
            background = PacketBufferReadUInt32BE(packet + backgroundOffset);
        }

        UserPane *activeUserPane = g_activeMapPane->m_userPane;

        switch (subtype) {
        case kServerSayNormal:
            if (activeUserPane->m_sayDisplayLevel != 0)
                return false;
            AddChattingMessage(text, 143, 128, 0, 0, 0);
            return false;

        case kServerSayShout:
            if (activeUserPane->m_sayDisplayLevel == 2)
                return false;
            AddChattingMessage(text, 14, 128, 0, 0, 0);
            return false;

        case kServerSayUnfiltered:
            AddChattingMessage(text, 143, 128, 0, 0, 0);
            return false;

        case kServerSayCustomColor:
            AddChattingMessage(text, 253, 254, 0, foreground, background);
            return false;
        }

        return false;
    }

    GetMemoryMan();

    const unsigned char subtype = packet[1];
    const unsigned char textLength = packet[6];
    const int convertedLength = MultiByteToWideChar(
        0,
        0,
        reinterpret_cast<const char *>(packet + 7),
        textLength,
        text,
        256);
    text[static_cast<unsigned char>(convertedLength)] = L'\0';

    UserPane *activeUserPane = g_activeMapPane->m_userPane;

    switch (subtype) {
    case kServerSayNormal:
        if (activeUserPane->m_sayDisplayLevel != 0)
            return false;
        AddChattingMessage(text, 128, 143, 0, 0, 0);
        return false;

    case kServerSayShout:
        if (activeUserPane->m_sayDisplayLevel == 2)
            return false;
        AddChattingMessage(text, 12, 143, 0, 0, 0);
        return false;

    case kServerSayUnfiltered:
        AddChattingMessage(text, 128, 143, 0, 0, 0);
        return false;
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private ChattingPane server-say packet handler with a combined source body/compiler switch tail/alignment disposition; exact object-id/length/ACP text and custom-color trailer layout; Normal/Shout/BalloonOnly/Unfiltered/CustomColor behavior; current/legacy palette and suppression matrices; one ignored GetMemoryMan call before packet reads in each layout branch; current custom trailer reads before the active-map load; active UserPane pointer load on every subtype/default path but display-level dereference only for Normal/Shout; inferred wchar_t[256] plus pre-cookie alignment; unchecked malformed-packet behavior; and unconditional false return. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047fd70-0x0047ff80 ChattingPane Handle Server Say Packet

## Status

- Disposition: reconstructable NexusTK project code with a source-authored private member body and compiler-only switch-table/alignment tail.
- Entity kind: `bool ChattingPane::HandleServerSayPacket(const unsigned char *packet)` plus attached compiler data.
- Canonical owner/emitter: [UID:00001X][ChattingPane](by-class/ChattingPane.md). The stale `FolderTreePane` and source-file-only ownership claims are preserved only as rejected history.
- Source placement: [UID:0000I5][Chatting](by-file/Chatting.md) -> `NexusTK/social/Chatting.cpp`, not the filesystem [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) core.
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Rebuild handling: emit only the source-authored method. The five-entry switch table and trailing `0xcc` alignment remain documented binary material with no source declaration or child split.
- Confidence: very strong for exact behavior, ABI, range, packet/state routes, source placement, and runtime order. The `92` cap preserves uncertainty in stripped subtype/field lexical spellings and the inferred source declaration behind `wchar_t[256]`.
- C++ handling: the accepted formal block uses a method-local `ServerSaySubtype`, direct packet reads, exact branch-local pointer/field order, and no shared opcode enum, packet wrapper, bounds guard, invented scratch initializer, or invented active-state accessor.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | The 2026-07-14 B005 callback closes the member ABI, server-say family/subtype roles, exact packet/state routes, current/legacy runtime order, source local ranking, compiler-tail disposition, and complete human first draft while preserving every malformed-packet edge. |
| Confidence | 92 | Range, CFG, packet offsets, palettes, state gates, caller/callees, ownership, and evaluation order are direct evidence. Exact original spellings for BalloonOnly/Unfiltered and the two state bytes are stripped, and `[256]` is the strongest source declaration inferred from exact frame/access/compiler controls rather than a recovered symbol. |

## Boundary

Live IDA MCP on 2026-06-05 models the function body as `0x0047fd70-0x0047ff60` with size `0x1f0`. The bytes at `0x0047ff60-0x0047ff74` are dispatch-table entries for the handler, followed by `0xcc` alignment through `0x0047ff80`. The previous `0x0047fd68-0x0047fd70` gap is eight `0xcc` bytes, and the next modeled function begins at `0x0047ff80`.

## Behavior

This private member processes server packet byte `13`, whose exact packet-name-table string is `kServerSay`, as routed by [UID:0002EU][0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter](by-memory/0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter.md). It reads a subtype at packet offset `1`, intentionally skips the BE32 speaking/target object id at offsets `2-5`, reads a byte length at offset `6`, converts exactly that many ACP bytes from offset `7` to UTF-16, and forwards accepted messages to [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md). `kServerSay` is packet-name evidence, not proof of a shared emitted opcode enum; the router retains literal `13`.

Current-layout branch:

- accepted subtypes include `0`, `1`, `3`, and `4`;
- subtype `1` appends when the local state byte is not `2`, using first color `14`, and is suppressed when the state byte equals `2`;
- subtype `0` is suppressed when the local state byte is nonzero;
- subtype `4` reads two extra big-endian dword values with [UID:0003YK][0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers](by-memory/0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers.md) helper `PacketBufferReadUInt32BE` at `packet + 7 + textLength` and `packet + 7 + textLength + 4`, then forwards colors `253`/`254` with custom foreground/background values;
- non-custom accepted messages forward color pair `143`/`128`, except subtype `1` uses first color `14`.

Old-layout branch:

- accepted subtypes include `0`, `1`, and `3`;
- subtype `1` appends when the local state byte is not `2`, using first color `12`, and is suppressed when the state byte equals `2`;
- subtype `0` is suppressed when the local state byte is nonzero;
- accepted messages forward color pair `128`/`143`, except subtype `1` uses first color `12`.

The handler always returns `0` / `false`, both after side-effect insertion and after ignored packets; the return value is not an accept/reject indicator.

Subtype role names remain inferred, but all five switch values have behavior-closed source-facing roles:

| Subtype | Role constant | Evidence |
| ---: | --- | --- |
| `0` | `kServerSayNormal` | Accepted in both layouts only when `m_sayDisplayLevel == 0`; uses the layout default color pair. Mirrored outgoing subtype `0` supports the Normal role. |
| `1` | `kServerSayShout` | Accepted in both layouts unless `m_sayDisplayLevel == 2`; uses highlighted first color `14` current or `12` legacy. Mirrored outgoing subtype `1` supports the Shout role. |
| `2` | `kServerSayBalloonOnly` | ChattingPane ignores it in both layouts; the independent MapPane sibling consumes the same object id/text to create a speech balloon. The behavior is exact and the spelling inferred. |
| `3` | `kServerSayUnfiltered` | Accepted in both layouts without reading the display field; uses the layout default color pair. NPC/system/script names are unsupported and rejected. |
| `4` | `kServerSayCustomColor` | Current-layout-only; reads two unchecked `PacketBufferReadUInt32BE` values after the text payload and forwards palette ids `253`/`254` plus both custom RGB values. |

The active state route starts from [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md). Both layout branches load `UserPane *activeUserPane = g_activeMapPane->m_userPane` from MapPane offset `+0x418` after conversion; current subtype `4` performs both unchecked trailer reads before that pointer load. The pointer load occurs for subtype `0-4` and default, so null `g_activeMapPane` faults on every path after conversion. Only subtype `0` and `1` then dereference UserPane byte `+0x134101`, inferred as `unsigned char m_sayDisplayLevel`; therefore null `m_userPane` faults only on those two subtype paths. Subtype `2`, `3`, `4`, and default never read the nested byte. The adjacent `+0x134102`, inferred as `m_saySendLevel`, is initialized/written with the display byte and consumed by outgoing senders; it is support evidence only and is not read by this method.

The prior generated `FolderTreePane` owner and source-file-only UID0000I5 emitter are retained only as superseded history. Receiver ABI, router adjustment, direct `AddChattingMessage` calls, and local class band establish canonical owner/emitter [UID:00001X][ChattingPane](by-class/ChattingPane.md); [UID:0000I5][Chatting](by-file/Chatting.md) remains the source-file route. [UID:0000UO][FolderTreePaneOwnerPollution_47f280_58a8e0](by-item/FolderTreePaneOwnerPollution_47f280_58a8e0.md) continues to document why filesystem-tree ownership is invalid.

## Live IDA Evidence

- `lookup_funcs` reports a modeled function at `0x0047fd70` with size `0x1f0`; lookups at `0x0047fd68`, `0x0047ff60`, and `0x0047ff74` are not functions, while `0x0047ff80` begins the next modeled function.
- `callers` reports no ordinary direct callers, but `xrefs_to 0x0047fd70` reports the router call at `0x0047f8b5` from [UID:0002EU][0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter](by-memory/0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter.md).
- `xrefs_to 0x0047ff60` reports the dispatch-table reference at `0x0047fe3e`; `get_bytes` shows local target entries for `0x0047fe45`, `0x0047fe45`, `0x0047ff4b`, `0x0047fe45`, and `0x0047fe45`, followed by twelve `0xcc` bytes before `0x0047ff80`.
- `callees` reports imported `MultiByteToWideChar`, narrow-character conversion helper `0x00575470`, packet integer decoder `0x005754c0`, setup/helper call `0x00516030`, [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md), and stack-cookie verification.
- Decompilation/disassembly confirms current-layout accepted subtypes `0`, `1`, `3`, and `4`; old-layout accepted subtypes `0`, `1`, and `3`; subtype `4` custom-color decoding through two `0x005754c0` calls; current-layout color pairs `143/128`, `14/128`, and `253/254`; old-layout color pairs `128/143` and `12/143`; and zero return after handled or rejected packets.

## 2026-07-14 B005 Accepted Source-Quality Resolution

The accepted B005 pass used healthy adopted IDB `supervisor_nexustk_20260713`. The report-only evidence was collected across transport sessions `7882ffdd-4cbc-4374-9a12-41606faa3a17` and `77e49c2f-8e3c-4675-aae7-d8e924c8f73f`: database enumeration and health succeeded, analysis/Hex-Rays/strings were ready, and bounded bytes, xrefs, target decompile, complete 149-instruction disassembly, stack frame, sibling frame, and provider checks completed. A whole-image text search and one broader target analysis timed out as expensive requests; neither was repeated unchanged, and successful bounded calls afterward proved the worker remained healthy. No IDA state was mutated.

Exact range/storage evidence:

- Source body `[0x0047fd70,0x0047ff60)` is `0x1f0` / 496 bytes, 25 basic blocks, cyclomatic complexity 16, with target-body SHA256 `0c78eab0fca6cee8efa4cd105ff115466c252744dd61aa140634639df624d6eb`.
- Five-entry switch table `[0x0047ff60,0x0047ff74)` contains raw bytes `45 fe 47 00 45 fe 47 00 4b ff 47 00 45 fe 47 00 45 fe 47 00`, targets `0x0047fe45`, `0x0047fe45`, `0x0047ff4b`, `0x0047fe45`, `0x0047fe45`, and has SHA256 `c3096e8b3b2bc14cb70834f7a5b8e465b7e5007d4b911e72b11638102b84a191`.
- Trailing `[0x0047ff74,0x0047ff80)` is twelve bytes of `0xcc` alignment, SHA256 `a0d08a155aa4e9aadd5da959565de99acc8dc9dbda96ec3852dde76c97330902`; predecessor `[0x0047fd68,0x0047fd70)` is eight `0xcc` bytes and remains excluded. The combined page SHA256 is `8fa27ea820b5f5fb3b35835c44a0f521612ccddcaff2f2b37b6a1bacc7177ce1`.
- All table entries point inside the method and implement its switch lowering. The table and alignment stay in this combined documentation page but emit no C++, child, enum, manual table, or padding source. Successor UID0002F0 remains a separate unchanged page starting exactly at `0x0047ff80`; `Nested:0` is preserved.

Exact packet layout:

| Offset | Width | Meaning | Method behavior |
| ---: | ---: | --- | --- |
| `0` | 1 | Server opcode `0x0d`; packet-name string `kServerSay` | Router selects this member; method does not reread it. |
| `1` | 1 | `ServerSaySubtype` | Values `0-4` index the attached switch; other values take default ignore. |
| `2` | 4 | BE32 speaking/target object id | Intentionally ignored here; independently consumed by the MapPane packet sibling. |
| `6` | 1 | Multibyte text length | Exact ACP input count, maximum 255. |
| `7` | `textLength` | Multibyte text | Converted with codepage 0, flags 0, and output capacity 256; packet NUL is not read or required. |
| `7 + textLength` | 4 | BE32 custom foreground RGB | Read without a size check only for current-layout subtype `4`. |
| `11 + textLength` | 4 | BE32 custom background RGB | Read without a size check only for current-layout subtype `4`. |

Exact runtime evaluation/dereference parity:

| Layout / subtype | `GetMemoryMan()` | Optional trailer reads | Active UserPane pointer load | Display-field read | Final action |
| --- | --- | --- | --- | --- | --- |
| Current / `0` | Once before packet reads | None | Yes, after conversion | Yes, inside case `0` only | Append at level `0` with `143/128`. |
| Current / `1` | Once before packet reads | None | Yes, after conversion | Yes, inside case `1` only | Append unless level `2` with `14/128`. |
| Current / `2` | Once before packet reads | None | Yes, after conversion | No | Ignore. |
| Current / `3` | Once before packet reads | None | Yes, after conversion | No | Append with `143/128`. |
| Current / `4` | Once before packet reads | Two unchecked BE32 reads before pointer load | Yes, after both reads | No | Append `253/254` plus both RGB values. |
| Current / other | Once before packet reads | None | Yes, after conversion | No | Ignore. |
| Legacy / `0` | Once before packet reads | None | Yes, after conversion | Yes, inside case `0` only | Append at level `0` with `128/143`. |
| Legacy / `1` | Once before packet reads | None | Yes, after conversion | Yes, inside case `1` only | Append unless level `2` with `12/143`. |
| Legacy / `2` | Once before packet reads | None | Yes, after conversion | No | Ignore. |
| Legacy / `3` | Once before packet reads | None | Yes, after conversion | No | Append with `128/143`. |
| Legacy / `4` | Once before packet reads | None | Yes, after conversion | No | Ignore. |
| Legacy / other | Once before packet reads | None | Yes, after conversion | No | Ignore. |

`GetMemoryMan()` is an ignored call once at the start of each layout branch before subtype/length reads, including ignored/default paths; the historical invented `InitializePacketDecodeScratch()` and per-append call interpretation are invalid. Loading `g_activeMapPane->m_userPane` dereferences the active MapPane but only obtains a UserPane pointer. Thus null `g_activeMapPane` faults on all paths after conversion, current subtype `4` can fault on its trailer first, and null `m_userPane` faults only when subtype `0` or `1` reads the display byte. Hoisting that byte or adding guards would change behavior.

Source local and conversion proof:

- The method reserves `0x210` stack bytes, places two dword compiler temporaries at `[ebp-0x210]` / `[ebp-0x20c]`, starts text at `[ebp-0x208]`, and stores the security cookie at `[ebp-4]`.
- The API capacity is exactly 256. The only indexed text write narrows the conversion return to unsigned byte before writing the terminator, so accessed indices are `0..255`; conversion failure returns zero and writes `text[0] = L'\0'`.
- Highest-ranked human declaration is `wchar_t text[256]`, occupying `[ebp-0x208,ebp-0x08)`, followed by four bytes of alignment before the cookie. IDA's `WCHAR[258]` coalesces that unused gap and is not source proof; no instruction accesses elements 256 or 257. A smaller array is impossible because a valid result can write terminator element 255.
- Same-toolchain controls at `0x005b3670`, `0x005b3a40`, and `0x005b4260` use accepted 256-wide sender buffers with exact `0x200` physical spans even where IDA labels only one `WCHAR`, proving inferred stack-variable extents are not original declaration evidence.
- The custom-color locals are intentionally uninitialized until the subtype-4 pre-switch reads. Initializing them would add stores absent from the binary; the same subtype value ensures they are consumed only after assignment.

Unsafe edges are intentional: there is no packet pointer, packet size, active pointer, conversion-success, or custom-trailer bounds check. The one-byte length bounds valid conversion output and prevents 16-bit offset wrap, but truncated packet/trailer memory can still be read. Every handled, suppressed, ignored, default, empty-conversion, and legacy/current path returns `false`; the return is not an acceptance indicator.

Naming/source decisions are bounded rather than raw: `Normal`/`Shout` are corroborated by outgoing subtype `0`/`1`; MapPane proves subtype `2` balloon-only; subtype `3` is behaviorally unfiltered; subtype `4` is custom color. Exact original lexical spellings remain stripped. `ServerSaySubtype` is method-local, because UID00026H emits packet-name strings rather than an opcode enum and UID0001SO is a MapPane-scoped blank-formal inventory. UID0002F0 remains the router's existing free helper; no sibling rename/memberization/source completion is introduced here.

Rejected alternatives preserved: `ChatType13_State0Only`, `HighlightUnlessState2`, `UngatedDefault`, NPC/system/script/movement names, a shared/file-scope opcode enum, a packet wrapper, bounds guards, an active-state accessor, `wchar_t[258]`, a smaller array, manual switch-table source, table child split, FolderTreePane ownership, source-file-only emitter ownership, and changes to MapPane/settings/sender/provider/type/global evidence pages.

## Resolved Source-Quality Questions

- B001 2026-06-17 reanalysis corrects subtype `1`: both current and old layouts suppress it when the active-local state byte equals `2`, not when it differs from `2`.
- B001 resolves `0x005754c0` as general [UID:0003YK][0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers](by-memory/0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers.md), not an RGB-specific helper. The decoded values are foreground/background custom color dwords in this handler only because they are passed with color ids `253/254`, matching the packet type-10 custom-color convention.
- B001 resolves the local-state path to [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md) plus active/local object pointer offset `+0x418`, then object byte `+0x134101`. The adjacent `+0x134102` chat-send gate is supporting context, not the exact byte read here.
- B001 rejects `FolderTreePane` ownership as generated owner pollution for this route.
- B005 resolves the target as a private `ChattingPane` member while retaining UID0002F0 as a separate unchanged free helper. Router literals remain `10` and `13`; packet-name strings do not create a shared source enum.
- B005 resolves the source local to inferred `wchar_t[256]` plus four bytes of pre-cookie alignment and historicalizes IDA `WCHAR[258]` as decompiler gap coalescing.
- B005 resolves exact evaluation order: one ignored `GetMemoryMan()` call per layout before packet reads; current custom trailer before active pointer; active pointer load on all subtype/default paths; display byte only in Normal/Shout cases.

## Remaining Source-Name Limits

- Exact original lexical spellings for `BalloonOnly`, `Unfiltered`, `m_sayDisplayLevel`, and `m_saySendLevel` remain unproven. Behavior, route, type width, and value matrix are closed, so the accepted first draft uses those descriptive names without retaining raw offsets or inventing an accessor. The inferred source declaration `wchar_t[256]` is also an explicit confidence cap rather than an unresolved body/layout blocker.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:00001X][ChattingPane](by-class/ChattingPane.md)
- [UID:0002EU][0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter](by-memory/0x0047f890-0x0047f8c9.ChattingPaneChatPacketRouter.md)
- [UID:0002EQ][0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage](by-memory/0x0047f280-0x0047f36f.ChattingPaneAddChattingMessage.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:0000UO][FolderTreePaneOwnerPollution_47f280_58a8e0](by-item/FolderTreePaneOwnerPollution_47f280_58a8e0.md)
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md)
- [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md)
- [UID:0003YK][0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers](by-memory/0x00575480-0x00575574.PacketBufferScalarBigEndianReadHelpers.md)

## Changes

- 2026-07-14 B005 UID0002EZ source-quality callback:
  - Renamed the page from `ChatPacketType13Handler` to `ChattingPaneHandleServerSayPacket`; raised `86/90 -> 92/92`; changed canonical owner/emitter from source-file UID0000I5 to class UID00001X; preserved reconstructable true, blank position, exact range, and `Nested:0`.
  - Applied the accepted private-member formal block with method-local subtype enum, inferred `wchar_t[256]`, one layout-entry `GetMemoryMan()` call, trailer-before-pointer order for current subtype `4`, pointer load on all paths, display-field reads only for subtype `0/1`, exact palettes/custom RGB, unsafe edges, and unconditional false return.
  - Incorporated exact body/table/alignment hashes and boundaries, router-only ABI/caller, complete callee inventory, packet and subtype matrices, active state/global/field route, null/fault parity, stack/cookie/sibling controls, source placement, no-split/no-child disposition, naming confidence caps, and rejected alternatives.
  - Historicalized the prior free/static Type13 helper, source-file-only emitter, `ChatType13_*`, invented scratch/accessor functions, hoisted state read, `[258]` declaration, per-append setup calls, and FolderTreePane owner pollution without deleting the older evidence trail.
- 2026-06-17 B001 source-quality execution:
  - Raised from `84/88` to `86/90` and populated first-draft C++.
  - Incorporated report-level details: exact body/table/alignment and router evidence remain unchanged; packet byte `1` is subtype, packet byte `6` is text length, and packet byte `7` starts the CP/ACP byte text converted through `MultiByteToWideChar`; accepted subtype roles are `State0Only` (`0`), `HighlightUnlessState2` (`1`), `UngatedDefault` (`3`), and current-layout `CustomColor` (`4`); subtype `1` is suppressed when local state equals `2` in both layouts; subtype `4` reads `PacketBufferReadUInt32BE` dwords after the text payload at `packet+7+length` and `packet+7+length+4`; local state comes through `g_activeMapPane +0x418` active object byte `+0x134101`; the adjacent `+0x134102` chat-send gate is supporting context; `FolderTreePane` is rejected as owner pollution; and the handler intentionally returns false after both append and ignored paths.
  - Remaining limits: protocol enum and active-object field spellings remain role-based.
- 2026-06-05 A004 live refresh:
  - What existed before: the page had the right broad packet-handler ownership but remained `72/82`, had blank parent metadata, and still used raw function/global labels in the evidence.
  - Changed to: `84/88`, `RECONSTRUCTABLE:TRUE`, parent [UID:0000I5][Chatting](by-file/Chatting.md), rewritten score/boundary/behavior/evidence sections, and no C++ block.
  - Summary/evidence: live IDA confirms exact `0x0047fd70-0x0047ff60` body bounds, dispatch-table tail through `0x0047ff74`, alignment through `0x0047ff80`, router call at `0x0047f8b5`, imported UTF-16 conversion, current/old layout subtype handling, local-state gates, custom-color decoder calls, AddChattingMessage forwarding, and stack-cookie cleanup. At that time, source emission was blocked by unresolved subtype names, local-state naming, packet integer-decoder naming, and polluted class ownership; B001 2026-06-17 supersedes those blockers with role-based subtype/state names, `PacketBufferReadUInt32BE`, owner-pollution rejection, and first-draft C++.
- 2026-05-31: Created exact packet-handler page and set completion/confidence to `72/82`. Evidence is IDA MCP function lookup, decompilation, router call evidence, and switch-table byte audit for `0x0047fd70-0x0047ff80`.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and left the parent blank.
  - Reasoning: existing documentation identifies source-owned chat packet subtype handling, but the page is `72/82` and fails the child-side 80/80 attachment gate.
