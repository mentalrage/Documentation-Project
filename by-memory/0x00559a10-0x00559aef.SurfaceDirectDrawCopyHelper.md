*** UID:00023S | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void CopyDirectDrawSurfaceToPrimary(ScreenPane *screenPane)
{
    RECT sourceRect = { 0, 0, 2000, 2000 };
    RECT destinationRect = {
        0,
        0,
        g_pApplication->m_presentationWidth,
        g_pApplication->m_presentationHeight
    };

    DDSURFACEDESC surfaceDesc;
    IDirectDrawSurface *scratchSurface = NULL;

    memset(&surfaceDesc, 0, sizeof(surfaceDesc));
    surfaceDesc.dwSize = sizeof(surfaceDesc);
    surfaceDesc.dwFlags = DDSD_CAPS | DDSD_HEIGHT | DDSD_WIDTH;
    surfaceDesc.dwHeight = 2000;
    surfaceDesc.dwWidth = 2000;
    surfaceDesc.ddsCaps.dwCaps = DDSCAPS_OFFSCREENPLAIN | DDSCAPS_SYSTEMMEMORY;

    g_pDirectX->m_pDirectDraw->CreateSurface(&surfaceDesc, &scratchSurface, NULL);
    screenPane->m_primarySurface->Blt(&destinationRect,
                                      scratchSurface,
                                      &sourceRect,
                                      DDBLT_WAIT,
                                      NULL);
    scratchSurface->Release();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready retained Surface DirectDraw copy helper emitted through UID0000OC; B003 MCP session aa3930bd confirms exact 0x00559a10-0x00559aef / 0xdf-byte raw body, no modeled function, zero start/end xrefs, one-byte cc boundaries, unique signature, g_pApplication/g_pDirectX/descriptor-seed refs, _memset, security-cookie check, DirectDraw CreateSurface/Blt/Release vtable semantics, and no-route/original-name confidence caps. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00559a10-0x00559aef Surface DirectDraw Copy Helper

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw DirectDraw helper.
- Parent aggregate: [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md)
- Likely source file: [UID:0000OC][Surface](by-file/Surface.md)
- Confidence: strong for bytes, boundaries, global references, stack descriptor setup, DirectDraw surface behavior, and direct Surface ownership; still capped by raw-helper naming, exact receiver declaration, application field spelling, and inbound liveness because IDA still has no modeled function or caller xrefs.
- Rebuild handling: source-ready retained Surface helper emitted through [UID:0000OC][Surface](by-file/Surface.md). The formal C++ block now uses conservative file-local helper shape; the no-function/no-direct-xref/original-name limitations remain documented confidence caps, not blank-emitter blockers.

## 2026-06-01 IDA Raw Body Map

| Range | IDA status | Evidence |
| --- | --- | --- |
| `0x00559a0f-0x00559a10` | alignment before helper | One `0xcc` byte after `BlitSurfaceToDisplay`. |
| `0x00559a10-0x00559aee` | raw helper body | IDA decodes 60 executable instructions; `lookup_funcs` reports `0x00559a10` is not a modeled function. |
| `0x00559aee` | return | Raw body ends with `retn`. |
| `0x00559aef-0x00559af0` | alignment after helper | One `0xcc` byte before modeled function `sub_559AF0`. |

Neighbor function checks:

- `0x005595d0` is modeled as `sub_5595D0`, size `0x43f`, ending at `0x00559a0f`.
- `0x00559af0` is modeled as `sub_559AF0`, size `0x2d`.
- IDA `callers`, `callees`, and `xrefs_to` for `0x00559a10` remain empty because this body is raw/unmodeled, not because the bytes are padding.

## Behavior

The helper builds a stack DirectDraw surface descriptor, creates or opens a temporary DirectDraw surface through global DirectDraw state, blits/copies through the surface object stored at `this+0x100`, then releases the temporary surface. B005's [UID:00039Z][0x00559170-0x005591fc.SurfaceRestoreLostSurfaces](by-memory/0x00559170-0x005591fc.SurfaceRestoreLostSurfaces.md) pass syncs the source-facing field name for the same ScreenPane receiver slot as `m_primarySurface` / alternate `m_displaySurface`; this raw helper now emits conservative C++ while preserving the liveness caveat that no direct caller/xref route has been recovered.

The first-draft formal C++ deliberately uses `CopyDirectDrawSurfaceToPrimary(ScreenPane *screenPane)` as a source-facing helper name and receiver type. Exact original helper spelling, exact receiver declaration, `g_pApplication +0xa68/+0xa6c` member names, and DirectDraw typedef/header spelling remain confidence caps.

## 2026-07-04 B003 Current MCP Source-Ready Pass

B003 rechecked this target with live MCP session `aa3930bd` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `status ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.

Current MCP facts:

- `lookup_funcs(['0x005595d0','0x00559a0f','0x00559a10','0x00559aef','0x00559af0'])` returned predecessor `sub_5595D0` at `0x005595d0` size `0x43f`, not-functions at `0x00559a0f`, `0x00559a10`, and `0x00559aef`, and successor `sub_559AF0` at `0x00559af0` size `0x2d`.
- The exact helper range remains half-open `0x00559a10-0x00559aef`, size `0xdf` / 223 (Verified with int_convert.py), with a normal prologue at `0x00559a10` and `ret` before the `0x00559aef` padding byte.
- `get_bytes(0x00559a0e,4)` returned `00 cc 55 8b`, proving the predecessor function ends before a one-byte `0xcc` boundary and this raw helper starts at `0x00559a10`.
- `get_bytes(0x00559aef,8)` returned `cc 55 8b ec 8d 45 10 50`, proving a one-byte post-helper `0xcc` before successor `sub_559AF0`.
- `xref_query(to 0x00559a10)` and `xref_query(to 0x00559aef)` both returned total `0`; no direct start/end xref or caller route has been recovered.
- `make_signature_for_range(0x00559a10,0x00559aef,wildcard_operands=false)` returned a unique full-range byte signature.
- `xref_query(to 0x00623470)` returned the descriptor-seed data ref at `0x00559a28`, `fn:null`; bytes at `0x00623470` are `{0,0,2000,2000}`.
- Paged `xref_query(to 0x0067ab1c)` returned the `g_pApplication` target ref at `0x00559a23`, `fn:null`; `xref_query(to 0x0067ab20)` returned the `g_pDirectX` target ref at `0x00559a63`, `fn:null`.
- `lookup_funcs` resolved `_memset` at `0x005ca0b0` size `0x15a` and `@__security_check_cookie@4` at `0x005c772f` size `0x11`.

Current decoded behavior:

- Stack frame reserves `0x94` / 148 bytes (Verified with int_convert.py) and uses the MSVC security cookie.
- The body reads `g_pApplication` at `0x00559a23`, then reads `+0xa68` / 2664 and `+0xa6c` / 2668 (Verified with int_convert.py) into destination rectangle/dimension locals.
- It copies the `0x00623470` source-rectangle seed into a local, then calls `_memset` after pushing `0x58` / 88 bytes (Verified with int_convert.py), consistent with zeroing a stack `DDSURFACEDESC`.
- It stores `dwSize = 0x6c` / 108, `dwFlags = 7`, `ddsCaps.dwCaps = 0x840` / 2112, `dwHeight = 0x7d0` / 2000, and `dwWidth = 0x7d0` / 2000 (Verified with int_convert.py).
- It reads `g_pDirectX` at `0x00559a63`, loads the DirectDraw interface at offset `+4`, and calls vtable slot `+0x18`, consistent with `IDirectDraw::CreateSurface`.
- It reads receiver field `this+0x100` / 256 (Verified with int_convert.py), documented by siblings as `m_primarySurface` / alternate `m_displaySurface`, and calls surface slot `+0x14`, consistent with `IDirectDrawSurface::Blt`.
- It pushes `0x1000000` / 16777216 (Verified with int_convert.py), matching `DDBLT_WAIT`, then calls slot `+0x8` on the temporary surface, consistent with COM `Release`.

## 2026-06-06 Bounded Raw-Body Audit

IDA MCP still reports `0x00559a10` and `0x00559aef` as non-functions, with neighboring modeled functions at `0x005595d0` size `0x43f` and `0x00559af0` size `0x2d`. A bounded `py_eval` instruction walk over `0x00559a10-0x00559aef` confirms exactly 60 instructions from `push ebp` through `retn`, plus one `0xcc` byte at `0x00559a0f` and one `0xcc` byte at `0x00559aef`.

Raw flow:

```asm
00559a10: push ebp
00559a13: sub  esp, 94h
00559a23: mov  eax, dword_67AB1C
00559a28: movaps xmm0, ds:xmmword_623470
00559a32: mov  edx, [eax+0A68h]
00559a3a: mov  eax, [eax+0A6Ch]
00559a5e: call _memset
00559a63: mov  eax, dword_67AB20
00559a71: mov  [ebp-90h], 6Ch
00559a7b: mov  [ebp-8Ch], 7
00559a85: mov  [ebp-28h], 840h
00559a8c: mov  [ebp-84h], 7D0h
00559a96: mov  [ebp-88h], 7D0h
00559ab0: call dword ptr [ecx+18h]
00559ab3: mov  eax, [esi+100h]
00559abe: push 1000000h
00559ad1: call dword ptr [ecx+14h]
00559add: call dword ptr [ecx+8]
00559ae6: call @__security_check_cookie@4
00559aee: retn
```

The bounded byte audit captures `223` body bytes for `0x00559a10-0x00559aef`; the first bytes are `55 8b ec 81 ec 94 00 00 00` and the tail is `8b 4d fc 33 cd 5e e8 44 dc 06 00 8b e5 5d c3`.

Instruction-level evidence:

| Address | Evidence | Interpretation |
| --- | --- | --- |
| `0x00559a13` | Reserves `0x94` stack bytes. | Local descriptor and temporary surface pointer storage. |
| `0x00559a19` / `0x00559ae6` | Reads `___security_cookie`, later calls `@__security_check_cookie@4`. | MSVC protected raw helper body. |
| `0x00559a23` | Reads `dword_67AB1C` / [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md). | Uses application/window presentation state. |
| `0x00559a28` | Loads `xmmword_623470` into the stack descriptor. | Copies a constant descriptor seed. |
| `0x00559a32` / `0x00559a3a` | Reads `g_pApplication + 0x0a68` and `+0x0a6c`. | Pulls display/surface dimension or position fields for the temporary surface descriptor. |
| `0x00559a5e` | Calls `_memset` after pushing `0x58`. | Zeroes most of the stack descriptor. |
| `0x00559a63` | Reads `dword_67AB20` / [UID:000295][0x0067ab20-0x0067ab24.g_pDirectX](by-memory/0x0067ab20-0x0067ab24.g_pDirectX.md). | Uses the process DirectDraw singleton. |
| `0x00559a71`, `0x00559a7b`, `0x00559a85`, `0x00559a8c`, `0x00559a96` | Stores `0x6c`, `7`, `0x840`, `0x7d0`, `0x7d0` into descriptor slots. | Builds a DirectDraw-style surface descriptor with fixed flags/capabilities and dimensions. |
| `0x00559ab0` | Calls through a vtable slot at `+0x18` from the DirectDraw object. | Creates/opens the temporary surface from the descriptor. |
| `0x00559ab3` | Reads `this + 0x100`. | Uses this object's retained target/source DirectDraw surface. |
| `0x00559ad1` | Calls through that surface object's vtable slot `+0x14`. | Blits/copies between the retained surface and temporary surface. |
| `0x00559add` | Calls through the temporary surface vtable slot `+0x8`. | Releases the temporary DirectDraw surface. |

## IDA Evidence

- 2026-06-07 A004 IDA MCP `py_eval` refresh reconfirmed that `ida_funcs.get_func(0x00559a10)` returns no modeled function, the bounded range `0x00559a10-0x00559aef` contains 223 bytes and 60 decoded instructions, and `xrefs_to 0x00559a10` is still empty.
- The same refresh found exactly five call instructions in the raw body: direct `_memset` at `0x00559a5e`, indirect DirectDraw-style calls through `[ecx+0x18]` at `0x00559ab0`, `[ecx+0x14]` at `0x00559ad1`, `[ecx+0x8]` at `0x00559add`, and `@__security_check_cookie@4` at `0x00559ae6`.
- The direct data refs inside the raw body remain `dword_67AB1C` at `0x00559a23`, `dword_67AB20` at `0x00559a63`, and descriptor seed `0x00623470` at `0x00559a28`; the leading bytes are `55 8b ec 81 ec 94 00 00 00 a1 24 2f 67 00 33 c5` and the trailing bytes end `8b 4d fc 33 cd 5e e8 44 dc 06 00 8b e5 5d c3`.
- Boundary bytes still show `0xcc` before and after the raw body at `0x00559a0f` and `0x00559aef`, so the neighboring modeled functions remain outside this page.
- 2026-05-28 IDA MCP disassembly showed a complete raw body at `0x00559a10-0x00559aee`, with `0x00559aef` as alignment before `sub_559AF0`.
- 2026-06-01 IDA MCP `lookup_funcs` reconfirms `0x00559a10` is not a modeled function, while the neighbors are modeled as `sub_5595D0` at `0x005595d0` and `sub_559AF0` at `0x00559af0`.
- 2026-06-06 IDA MCP bounded instruction walk decodes 60 instructions from `0x00559a10-0x00559aef`; the last executable instruction is the `retn` at `0x00559aee`, and `0x00559aef` is `0xcc`.
- The helper reads `dword_67AB1C` / `g_pApplication`, `dword_67AB20` / `g_pDirectX`, and `xmmword_623470`; initializes stack descriptor fields including `0x6c`, `7`, `0x840`, and two `0x7d0` values; and uses vtable calls through DirectDraw-style objects.
- It calls `_memset` and security-cookie checking, matching nearby surface/presentation helper style.
- No direct caller xrefs were found on 2026-05-28 or 2026-06-01, so liveness and final source split remain open even though the byte body is confirmed real code.
- 2026-06-06 IDA MCP `py_eval` bounded the audit to this raw body only: `callers 0x00559a10` is still empty, `callees 0x00559a10` still fails with `No function found`, and manual instruction review identifies the direct `_memset` / `__security_check_cookie` calls plus the indirect DirectDraw calls at vtable slots `+0x18`, `+0x14`, and `+0x8`.
- `xrefs_to 0x00623470` reports the descriptor seed reference at `0x00559a28`; `xrefs_to 0x00559a10` remains empty, reinforcing that this is raw reachable code only through an unmodeled edge or inline/control-flow artifact still to be identified.
- 2026-06-11 A004 Batch 180 live IDA MCP recheck again found no modeled function at `0x00559a10`, no direct callers/xrefs to the helper start, and one-byte `0xcc` boundaries at `0x00559a0f` and `0x00559aef`; the body still decodes as 60 instructions from `push ebp` through `retn`.
- The Batch 180 call/data inventory remains five calls: `_memset`, three indirect DirectDraw-style vtable calls through slots `+0x18`, `+0x14`, and `+0x8`, and the MSVC security-cookie check. Direct refs remain [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md), [UID:000295][0x0067ab20-0x0067ab24.g_pDirectX](by-memory/0x0067ab20-0x0067ab24.g_pDirectX.md), and descriptor seed `0x00623470`.
- Parent evidence changed in this pass: [UID:0000OC][Surface](by-file/Surface.md) now scores `88/85` and explicitly lists this exact helper under its proposed contents, while [UID:0000IW][DirectX](by-file/DirectX.md) remains a DirectDraw singleton/lifetime owner rather than the surface-copy source owner. The containing [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) page is a mixed inventory and is not the semantic direct parent.
- `int_convert.py` verified raw-body and descriptor constants used by this page: body size `223`/`0xdf`, instruction count `60`/`0x3c`, stack frame `0x94`/148, `_memset` size `0x58`/88, descriptor size `0x6c`/108, fixed values `0x840`/2112 and `0x7d0`/2000, and retained surface offset `0x100`/256.

## Rebuild Handling

Keep this under the surface/presentation family, not the screen-dimmer/effect overlay family that starts at `0x00559b90`.

## Source Placement And Rejected Alternatives

- Accepted route: keep canonical owner and emitter as [UID:0000OC][Surface](by-file/Surface.md), emitted into `NexusTK/render/Surface.cpp`. This exact helper is Surface presentation/copy work and belongs with the `0x005595d0` display-blit and `0x00558840` Surface/minimap DirectDraw setup neighborhood.
- DirectX ownership rejected: this body consumes `g_pDirectX` and `IDirectDraw` service calls, but [UID:0000IW][DirectX](by-file/DirectX.md) and [UID:0000QS][g_pDirectX](by-global/g_pDirectX.md) own singleton/lifetime and DirectDraw wrapper state, not this Surface copy policy.
- ScreenPane ownership rejected for this exact target: `this+0x100` is ScreenPane-shaped receiver-family evidence, but there is no direct caller, vtable slot, lifecycle write, or ordinary xref route into `0x00559a10` comparable to sibling ScreenPane reroutes. ScreenPane field names are useful for source readability and remain an inference cap, not canonical-owner proof.
- Mixed parent aggregate emission rejected: [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) is a non-reconstructable mixed-owner inventory; exact child pages carry source ownership and emission.
- Padding/no-code and no-owner/non-emitting treatment rejected: the bytes form a complete MSVC helper with prologue, stack frame, security cookie, `_memset`, DirectDraw globals, three coherent DirectDraw/COM vtable calls, and a return. The adjacent `0xcc` bytes are boundaries, not proof that this body is padding.
- Raw IDA labels rejected for source-facing prose/C++: do not emit `sub_559A10`, `dword_67AB1C`, `dword_67AB20`, `xmmword_623470`, `_DWORD *`, or `this[64]`.

## Score Rationale

Completion is raised to 88 because the page now has current MCP session `aa3930bd` evidence for the exact range, body bytes, unique signature, one-byte boundaries, no-function/no-start-xref state, descriptor seed, global refs, `_memset`, security-cookie check, DirectDraw vtable semantics, direct Surface owner/emitter route, and formal first-draft C++. It remains below final-audit quality because no modeled function, no direct caller/xref route, exact original helper spelling, exact receiver declaration, and exact `g_pApplication +0xa68/+0xa6c` member names remain unresolved confidence caps.

Confidence is raised to 91 because the byte, boundary, DirectDraw behavior, globals, descriptor constants, unique signature, and Surface owner/emitter evidence are direct and current. It stays below final-audit territory because IDA still does not model the range as a function, no direct caller xrefs have been found, and several source-facing names remain inferred.

## Assignment Gate

- Assignment decision: attach to [UID:0000OC][Surface](by-file/Surface.md).
- Child score after this pass: `88/91`; direct parent [UID:0000OC][Surface](by-file/Surface.md) is `88/85`, so the strict child+parent gate and the current emitter/C++ gate clear.
- Ownership basis: this helper belongs with the Surface copy/presentation family because it uses the object's retained surface at `this+0x100` and performs the temporary DirectDraw surface copy/update. [UID:0000IW][DirectX](by-file/DirectX.md) owns DirectDraw singleton/lifetime setup, while [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) is a mixed aggregate and not a semantic parent.

## Cross-References

- [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md)
- [UID:0000OC][Surface](by-file/Surface.md)
- [UID:0000IW][DirectX](by-file/DirectX.md)
- [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md)
- [UID:000295][0x0067ab20-0x0067ab24.g_pDirectX](by-memory/0x0067ab20-0x0067ab24.g_pDirectX.md)
- [UID:0001QI][client_new_rendering_mode](by-meta/client_new_rendering_mode.md)
- [UID:00039Z][0x00559170-0x005591fc.SurfaceRestoreLostSurfaces](by-memory/0x00559170-0x005591fc.SurfaceRestoreLostSurfaces.md)

## UID0003CJ RECT Seed Relationship

The load at `0x00559a28` references exact bytes `0x00623470-0x00623480` inside non-emitting [UID:0003CJ][0x0062317c-0x00623480.ScreenPaneResourceStringData](by-memory/0x0062317c-0x00623480.ScreenPaneResourceStringData.md). Interpreted through the existing 16-byte Win32 `RECT` UDT (`LONG left/top/right/bottom` at offsets `0/4/8/0xc`), the bytes are `{0,0,2000,2000}` with SHA256 `0A27C6865CC43996F0F5347A705E39ACC16B18646C3FAAA0617F3DA0024FD5CC`. This is the seed for local `sourceRect`, not an independent global or descriptor table.

The existing formal `RECT sourceRect = { 0, 0, 2000, 2000 };` is the sole human-source representation. Preserve the helper's raw start/no-start-xref history, DirectDraw call flow, globals, exact bounds, `88/91` score, Surface owner/emitter, and all negative evidence. UID0003CJ must remain false/non-emitting so the compiler may pool this local initializer without a duplicate raw-array/global emitter.

## Changes

- 2026-07-24 B001 UID0003CJ synchronization: preserved `88/91`, Surface route, formal, raw liveness cap, and full helper evidence; added exact seed range/hash/RECT layout/xref, local-source disposition, and no-global/no-duplicate constraint.

- 2026-07-04 B003 source-ready implementation callback:
  - Raised completion/confidence from `85/89` to `88/91`; kept `CANONICAL_OWNER:0000OC`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000OC`, and blank optional emitter position.
  - Inserted formal first-draft C++ for `CopyDirectDrawSurfaceToPrimary(ScreenPane *screenPane)`, a conservative file-local Surface helper that creates a 2000x2000 DirectDraw scratch surface, blits it to `m_primarySurface` with `DDBLT_WAIT`, and releases the scratch surface.
  - Evidence: accepted B003 report `tools/leaser/Agents/Agent-B003/research/00023S-SurfaceDirectDrawCopyHelper-source-quality.md`; MCP session `aa3930bd`; `server_health ok`; exact range `0x00559a10-0x00559aef`; size `0xdf` / 223 bytes; not-functions at start/end; predecessor `sub_5595D0` size `0x43f`; successor `sub_559AF0` size `0x2d`; one-byte `0xcc` boundaries; unique signature; zero xrefs to `0x00559a10` and `0x00559aef`; descriptor-seed ref at `0x00559a28`; `g_pApplication` ref at `0x00559a23`; `g_pDirectX` ref at `0x00559a63`; `_memset`; security-cookie check; and DirectDraw `CreateSurface` / surface `Blt` / COM `Release` vtable semantics.
  - Confidence caps preserved: no modeled IDA function, no direct caller/xref route, exact original helper spelling, exact receiver declaration, exact `g_pApplication` dimension member names, and DirectDraw typedef/header spelling remain inferred. Rejected alternatives preserved: DirectX owner, ScreenPane owner, mixed parent emitter, padding/no-code, raw IDA labels, and no-owner/non-emitting.
- 2026-06-21 B005 SurfaceRestoreLostSurfaces field-name sync:
  - Score unchanged at `85/89`.
  - Added a narrow support note that this helper's `this+0x100` retained DirectDraw surface is the same ScreenPane primary/display surface named by [UID:00039Z][0x00559170-0x005591fc.SurfaceRestoreLostSurfaces](by-memory/0x00559170-0x005591fc.SurfaceRestoreLostSurfaces.md). At that time the raw helper remained blank-C++ and liveness-caveated; the blank-C++ disposition is superseded by the 2026-07-04 B003 source-ready implementation above.
  - Evidence: accepted B005 report `tools/leaser/Agents/Agent-B005/research/00039Z-SurfaceRestoreLostSurfaces-source-quality.md` and the existing raw-body evidence on this page.
- 2026-06-11 A004 Batch 180: Raised completion/confidence from `80/88` to `85/89` and set `AUTOGEN_PARENT_UID:0000OC`.
  - Evidence: live IDA MCP reconfirmed no function/no-xref status, the 223-byte 60-instruction raw body, five calls, direct global/descriptor refs, exact one-byte boundaries, and the direct Surface parent evidence; `int_convert.py` verified the refreshed raw-body, stack, descriptor, dimension, and offset conversions.
  - Rebuild at that time: C++ remained blank because the helper was raw/unmodeled and direct callers/source names were not recovered, but assignment to [UID:0000OC][Surface](by-file/Surface.md) cleared both child and parent gates. Superseded by the 2026-07-04 B003 source-ready implementation above.
- 2026-06-01: Raised completion/confidence from `50/65` to `72/82` and marked reconstructable.
  - Evidence: refreshed IDA MCP lookup/caller/callee/xref checks, instruction-walked the raw body, recorded exact padding bytes, global references, descriptor constants, DirectDraw vtable calls, and the no-xrefs caveat.
  - Rebuild at that time: C++ and autogen parent remained blank because the helper was raw/unmodeled in IDA and the likely Surface parent was below the attachment-confidence gate. Superseded by later UID0000OC assignment and the 2026-07-04 B003 source-ready implementation above.
- 2026-06-06 A009: Raised completion/confidence from `72/82` to `78/86`.
  - Evidence: live IDA MCP `lookup_funcs`, bounded `py_eval` instruction/byte audit, `callers`, `callees`, and `xrefs_to` reconfirmed the non-function status, 60-instruction body, exact `0xcc` boundaries, descriptor seed ref, global refs, indirect DirectDraw calls, and no direct caller xrefs.
  - Rebuild at that time: parent and C++ remained blank because the helper was still raw/unmodeled and inbound reachability/source naming were not final. Superseded by later UID0000OC assignment and the 2026-07-04 B003 source-ready implementation above.
- 2026-06-07 A004 Batch 008: Raised completion/confidence from `78/86` to `80/88`.
  - Evidence: live IDA MCP `py_eval` reconfirmed the no-function/no-xref state, 223-byte 60-instruction body, exact direct and indirect calls, `dword_67AB1C`, `dword_67AB20`, descriptor-seed refs, and both one-byte `0xcc` boundaries.
  - Rebuild at that time: deliberately left `AUTOGEN_PARENT_UID` blank because the child was below 85 completion and direct parent candidates [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) and [UID:0000OC][Surface](by-file/Surface.md) did not satisfy the corrected 85/85 gate. Superseded by the 2026-06-11 assignment to [UID:0000OC][Surface](by-file/Surface.md).
