*** UID:0002Y1 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RidingImageLib::RenderRiding(
    GrafPort *target,
    const RectBounds *bounds,
    const ObjectStatusBlob *objectStatus,
    unsigned int animationGroup,
    unsigned int animationFrame)
{
    const unsigned int definitionIndex = static_cast<unsigned short>(
        objectStatus->m_appearanceId + 0x8000);
    if (definitionIndex >= ridingDefinitionCount)
        return;

    RidingDefinition *definition = ridingDefinitions + definitionIndex;
    RidingBucketEntry *frameEntry =
        definition->GetFrame(animationGroup, animationFrame);
    const int frameIndex =
        static_cast<int>(definition->ridingId) + frameEntry->frameOffset;
    const bool useCenteredProjection =
        definition->defaultBucketIndex != 0;

    definition->GetFrame(animationGroup, animationFrame);

    if (frameIndex < 0)
        return;

    EPFTileContext tileContext;
    g_pEPFLib->LookupLayoutEntry(
        L"RIDINGS.EPF",
        static_cast<short>(frameIndex),
        &tileContext);

    RectBounds sourceBounds = tileContext.bounds;
    RectBounds destinationBounds;
    destinationBounds.left = bounds->left;
    destinationBounds.top = bounds->top;
    destinationBounds.right =
        bounds->left + sourceBounds.right - sourceBounds.left;
    destinationBounds.bottom =
        bounds->top + sourceBounds.bottom - sourceBounds.top;

    if (animationGroup == 0)
        return;

    MapPane *mapPane = g_activeMapPane;

    if (sourceBounds.bottom < -4)
    {
        if (useCenteredProjection)
        {
            const int sourceHeight =
                sourceBounds.bottom - sourceBounds.top;
            OffsetRect(
                &destinationBounds,
                0,
                sourceHeight / 2 - sourceBounds.bottom);

            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &sourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                1.5f,
                0.0f);
        }
        else
        {
            OffsetRect(&destinationBounds, 0, -sourceBounds.bottom);

            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &sourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                mapPane->m_newHumanProjectionDivisor,
                mapPane->m_newHumanProjectionShear);
        }
        return;
    }

    if (useCenteredProjection)
        return;

    if (sourceBounds.bottom > 5)
    {
        RectBounds clippedSourceBounds = sourceBounds;
        clippedSourceBounds.bottom = 0;
        destinationBounds.bottom -= sourceBounds.bottom;

        if (mapPane != NULL)
        {
            g_pfnBlitScaledRleTint(
                target,
                &tileContext,
                &clippedSourceBounds,
                &destinationBounds,
                3,
                mapPane->m_dayNightTintAmount,
                0,
                mapPane->m_newHumanProjectionDivisor,
                mapPane->m_newHumanProjectionShear);
        }
        return;
    }

    g_pfnBlitScaledRleTint(
        target,
        &tileContext,
        &sourceBounds,
        &destinationBounds,
        3,
        mapPane->m_dayNightTintAmount,
        0,
        mapPane->m_newHumanProjectionDivisor,
        mapPane->m_newHumanProjectionShear);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained source-authored RidingImageLib::RenderRiding member with exact five-argument ABI, definition/frame lookup, RIDINGS.EPF context load, branch-exact rectangle/projection math, and scaled RLE tint callback dispatch. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dc7e0-0x004dca14 RidingImageLib RenderRiding

## Status

- Entity kind: retained, source-authored, nonvirtual `RidingImageLib` member.
- Source identity: `void RidingImageLib::RenderRiding(GrafPort *, const RectBounds *, const ObjectStatusBlob *, unsigned int animationGroup, unsigned int animationFrame)`.
- Owner/emitter: [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md); source root [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md).
- Split source: exact child of non-emitting [UID:00017F][0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster](by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md).
- Current disposition: reconstructable and source-ready. The missing IDA function object, zero entry refs, and absent direct caller are bounded reachability evidence, not no-source proof.
- Source-facing name confidence is high by the accepted `MonsterImageLib::RenderMonster` parallel. The former `DrawRidingFrameRawHelper` label survives only as superseded provenance.

## Address Range And Hash

- Exact half-open range: `[0x004dc7e0,0x004dca14)`.
- Exact size: `0x234` / 564 bytes.
- Whole-body SHA256: `0BF8FB87444BB45BAAB43BA1FA3DF6268EA666AD6E353D0450CBF29D524D6EDD`.
- The body decodes as 169 instructions, begins with a normal local-frame/security-cookie prologue, and ends with the cookie check plus `retn 0x14`.
- Predecessor [UID:00017G][0x004dc730-0x004dc7d8.RidingImageLibDestructor](by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md) ends before eight parent-only `0xcc` bytes at `[0x004dc7d8,0x004dc7e0)`.
- Twelve parent-only `0xcc` bytes at `[0x004dca14,0x004dca20)` separate this method from modeled successor `sub_4DCA20`, size `0x23`.
- No interior data, table, padding, fallthrough, or second body exists. `Nested:0` and the no-split/no-merge disposition remain exact.

## Exact ABI

| Physical input | Source type/name | Evidence |
| --- | --- | --- |
| `ecx` | `RidingImageLib *this` | reads `this+0x04/+0x08`, matching definition vector/count |
| stack argument 1 | `GrafPort *target` | receiver passed in ECX to scaled RLE callback |
| stack argument 2 | `const RectBounds *bounds` | left/top anchor used to construct destination bounds |
| stack argument 3 | `const ObjectStatusBlob *objectStatus` | `+0x04` is current `m_appearanceId` |
| stack argument 4 | `unsigned int animationGroup` | first shared GetFrame index and post-lookup zero gate |
| stack argument 5 | `unsigned int animationFrame` | second shared GetFrame index |

`retn 0x14` independently proves five four-byte stack arguments. The source return is `void`: every exit is behaviorally a return, and the modeled sibling's sole caller does not consume residual EAX.

## Definition And Frame Selection

1. Convert `objectStatus->m_appearanceId + 0x8000` through unsigned-16 wrap and compare the result unsigned against `ridingDefinitionCount`.
2. Select the exact 12-byte [UID:0001VV][RidingDefinition](by-type/by-struct/RidingDefinition.md) row from `ridingDefinitions`.
3. Call shared [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md) helper `GetFrame(animationGroup, animationFrame)` and use the returned `frameOffset`.
4. Form signed `frameIndex = ridingId + frameOffset`; read definition byte `+0x05` as the centered-projection/suppression flag.
5. Call `GetFrame(animationGroup, animationFrame)` a second time and discard the result. Both this body and `RenderMonster` retain the duplicate call; it must not be optimized out because the out-of-line helper is not proven pure.
6. Exit when `frameIndex < 0`; otherwise pass the signed low 16 bits to the resource lookup, matching the binary's sign extension of `di`.

The current source-facing field `defaultBucketIndex` is retained because it is part of the accepted riding record. `useCenteredProjection` is a local role name; the original field spelling remains inference-capped.

## Resource And Rectangle Flow

- Construct and initialize a local [UID:00004I][EPFTileContext](by-class/EPFTileContext.md).
- Call `g_pEPFLib->LookupLayoutEntry(L"RIDINGS.EPF", static_cast<short>(frameIndex), &tileContext)`; the target-specific lookup sequence is unique in the image.
- Copy `tileContext.bounds` to `sourceBounds`.
- Set destination left/top from caller `bounds->left/top` and preserve source width/height exactly:
  - `right = bounds->left + source.right - source.left`
  - `bottom = bounds->top + source.bottom - source.top`
- Preserve the counterintuitive `animationGroup == 0` return after resource lookup and rectangle construction. It is not a lookup-failure test.

## Branch-Exact Projection And Dispatch

| Condition | Exact behavior |
| --- | --- |
| `sourceBounds.bottom < -4` and centered flag set | Offset destination Y by `(source.bottom - source.top) / 2 - source.bottom`; dispatch with vertical scale `1.5f` and horizontal shear `0.0f`; return. |
| `sourceBounds.bottom < -4` and centered flag clear | Offset destination Y by `-source.bottom`; dispatch with active MapPane projection divisor/shear; return. |
| `sourceBounds.bottom >= -4` and centered flag set | Return without drawing. |
| `sourceBounds.bottom > 5` and centered flag clear | Copy source bounds, set copied bottom to zero, subtract original source bottom from destination bottom, and dispatch only when `g_activeMapPane != NULL`; return. |
| otherwise | Dispatch with unmodified source/destination rectangles and active MapPane projection divisor/shear. |

Only the `> 5` clipping branch contains an explicit active-map null test. Adding a defensive check to the other paths would change behavior.

The callback is [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) slot 4 at `0x0069b3f0`, source-facing `g_pfnBlitScaledRleTint`. Its exact source contract is `void`, with `GrafPort` receiver plus `EPFTileContext *`, source and destination `RectBounds *`, mode `3`, float tint, integer row offset `0`, float vertical scale, and float horizontal shear. Both compat target `0x004bdcc0` and RGB565 target `0x004c3a50` establish the same signature. The target and sibling use [UID:00007Q][MapPane](by-class/MapPane.md) `+0x3d0` as float tint, `+0x3d4` as vertical projection divisor/scale, and `+0x3d8` as horizontal projection shear.

## Source Sibling And Placement Evidence

- Modeled `MonsterImageLib::RenderMonster` at `0x004db100-0x004db329` has the same definition selection, two GetFrame calls, flag and frame arithmetic, destination construction, group gate, thresholds, offsets, MapPane loads, and slot-4 dispatch.
- Its one concrete caller at `0x0053bda8` in [UID:0003NV][0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame](by-memory/0x0053bd20-0x0053bdb1.LivingObjectPaneDrawSpriteFrame.md) proves the same five source arguments and void use.
- The common definition-selection signature has exactly two matches: target `0x004dc814` and sibling `0x004db134`. The target's RIDINGS lookup signature beginning at `0x004dc875` is unique.
- Riding-specific `this+0x04/+0x08` fields and literal `RIDINGS.EPF` establish UID0000BZ/UID0000N6 ownership. MonsterImageLibTables owns only the shared table implementation; Surface, MapPane, ResourceLayoutTable, EPFTileContext, and LivingObjectPane are dependencies or caller evidence.
- A file-local/free helper is weaker than the member model because ECX is a class receiver and the source sibling is an accepted member.

## IDA MCP Evidence

- Mandatory evidence session `fa658e1e` was healthy at `2026-07-14T19:36:16-04:00`: NexusTK.exe image base `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, cache size 2067.
- Bounded whole-range bytes, 169-instruction decode, function lookup, xref queries, byte searches, and decompiles all succeeded.
- Function lookup: predecessor `0x004dc730` size `0xa8`; no function at target start/end; successor `0x004dca20` size `0x23`; sibling `0x004db100` size `0x229`; sibling caller `0x0053bd20` size `0x91`; shared GetFrame `0x00528b10` size `0x34`.
- Target entry has zero direct refs and direct VA/RVA byte-pattern searches have zero matches. Callback slot has 21 refs, including target `0x004dc9fb` and sibling `0x004db310`.
- Compat and RGB565 callback decompiles independently agree on the typed void receiver/eight-stack-argument contract.

## Positive And Negative Evidence

Positive evidence is the exact standalone body, complete ABI, RidingImageLib receiver fields, unique RIDINGS resource path, instruction/control-flow equivalence with a modeled and called source sibling, exact support types, callback targets, and clean alignment boundaries.

Negative evidence is preserved as a confidence cap: no IDA function object, target-entry xref, surviving pointer, vtable slot, direct caller, exact RIDINGS duplicate, or source symbol/PDB. None supports classifying this branch-rich feature body as data, padding, compiler glue, or a no-code destination. The callback's broad fanout establishes dependency rather than ownership, and the sibling caller proves source factorization rather than a direct route to this retained body.

Rejected alternatives remain: do not merge with the destructor or successor, route to MonsterImageLib/Surface/MapPane/ResourceLayoutTable, use ObjectStatusBlob riding-specific offsets instead of `+0x04`, delete the duplicate GetFrame call, normalize thresholds, invent extra null guards, return an integer, or hand-author raw addresses/vtable/RTTI/cookie machinery.

## Compiler Exclusions And Source Quality

The security cookie setup/check, saved registers, SSE dword transport, stack argument staging, physical ECX handling, and `retn 0x14` lowering are compiler output. The managed block expresses plausible mid-2000s project source with typed locals, member access, helper calls, and exact control flow; it contains no decompiler labels or raw offsets.

The score is `92/94`: body, ABI, owner/emitter, range/hash, source sibling, resource/helper/type/global/callback contracts, source placement, and exact C++ are resolved. It remains below final lexical certainty because `RenderRiding`, public access, retained-route cause, and final MapPane field spellings are inferred without symbols.

## Historical / Superseded Assumptions

- The former path/title `RidingImageLibDrawRidingFrameRawHelper` accurately recorded that IDA missed the function but is superseded as the current source identity.
- The 2026-06-30 B008 rare no-improvement proof correctly preserved source authorship, `retn 0x14`, range, calls, and padding. Its conclusion that absent function/xref/caller evidence required blank emitter/formal is superseded by the complete ABI and exact RenderMonster source sibling/caller factorization.
- No-route facts remain current negative evidence. They must not be restated as proof that no human source should emit.

## Cross-References

- [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md)
- [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md)
- [UID:00017F][0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster](by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md)
- [UID:00017G][0x004dc730-0x004dc7d8.RidingImageLibDestructor](by-memory/0x004dc730-0x004dc7d8.RidingImageLibDestructor.md)
- [UID:0001VV][RidingDefinition](by-type/by-struct/RidingDefinition.md)
- [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md)
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md)

## Changes

- 2026-07-14 B003 accepted source-quality callback:
  - Validator-aware UID-preserving rename from `RidingImageLibDrawRidingFrameRawHelper` to `RidingImageLibRenderRiding`; exact UID and range retained.
  - Raised `86/90` to `92/94`, set emitter UID0000BZ, retained owner/reconstructable/blank position/Nested 0, and installed the accepted complete `RenderRiding` source body.
  - Incorporated whole-body hash, five-argument ABI, duplicate GetFrame, signed-low-16 lookup, rectangle and projection branches, callback contract, source sibling, padding, negative routes, compiler exclusions, source placement, rejected alternatives, and lexical confidence caps.
  - Historicalized the prior no-route no-code conclusion without deleting its valid range/reachability evidence.
- 2026-06-07 A005 Batch 047: created as the exact split child for the IDA-missed body after live boundary and ownership verification.
