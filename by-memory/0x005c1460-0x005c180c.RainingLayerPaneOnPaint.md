*** UID:0003PJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RainingLayerPane::OnPaint()
{
    SetDrawColor(0);
    g_pfnFillRect(this, &m_bounds);

    if (g_activeMapPane->m_waitingForLocalPlayerStatus)
        return;

    const unsigned int rainColor = 143;
    SetDrawColor(rainColor);

    int i;
    for (i = 0; i < 18; ++i) {
        MoveTo(
            2 * (17 * s_rainStreaks[m_currentFrame][i].x / 15),
            2 * (15 * s_rainStreaks[m_currentFrame][i].y / 13));
        g_pfnDrawLineDelta(
            this,
            2 * (-17 * s_rainStreaks[m_currentFrame][i].length / 15),
            2 * (30 * s_rainStreaks[m_currentFrame][i].length / 13));
    }

    for (i = 0; i < 7; ++i) {
        g_pfnDrawPixel(
            this,
            2 * (17 * s_rainSmallSplashes[m_currentFrame][i].x / 15),
            2 * (15 * (s_rainSmallSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
        g_pfnDrawPixel(
            this,
            2 * (17 * (s_rainSmallSplashes[m_currentFrame][i].x + 2) / 15),
            2 * (15 * (s_rainSmallSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
    }

    for (i = 0; i < 7; ++i) {
        g_pfnDrawPixel(
            this,
            2 * (17 * s_rainLargeSplashes[m_currentFrame][i].x / 15),
            2 * (15 * s_rainLargeSplashes[m_currentFrame][i].y / 13),
            rainColor);
        g_pfnDrawPixel(
            this,
            2 * (17 * s_rainLargeSplashes[m_currentFrame][i].x / 15),
            2 * (15 * (s_rainLargeSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
        g_pfnDrawPixel(
            this,
            2 * (17 * (s_rainLargeSplashes[m_currentFrame][i].x + 4) / 15),
            2 * (15 * (s_rainLargeSplashes[m_currentFrame][i].y + 6) / 13),
            rainColor);
        g_pfnDrawPixel(
            this,
            2 * (17 * (s_rainLargeSplashes[m_currentFrame][i].x + 6) / 15),
            2 * (15 * (s_rainLargeSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Behavior-complete `RainingLayerPane::OnPaint`: exact 0x3ac-byte body/hash/signature, vtable-only route, fill-before-MapPane gate, three signed-frame rain loops, exact streak/splash arithmetic and callback ABIs, human C++03 formal source, unique-body proof, and 0xcc boundary padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005c1460-0x005c180c RainingLayerPane OnPaint

## Status

- Disposition: reconstructable, behavior-complete `RainingLayerPane::OnPaint()` with actual formal C++.
- Direct owner/emitter: [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md).
- Parent migration index after split: [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md).
- Source-family route: [UID:0000P8][WeatherLayerPane](by-file/WeatherLayerPane.md), currently generated as `NexusTK/map/WeatherLayerPane.cpp`.
- Signature: `void RainingLayerPane::OnPaint()`; IDA's guessed integer return is residual EAX, not source behavior.

## Evidence

- Live IDA MCP reports `sub_5C1460` at `0x005c1460`, size `0x3ac`, ending at `0x005c180c`.
- Exact 940-byte SHA256: `983E7590BF2011C809F128CBE5AB0B689DE7573D085D2054153B35DA47BDDAB2`.
- Shortest unique wildcarded signature: `56 6A 00 8B F1 E8 ? ? ? ? 8D 46 ? 8B CE 50 FF 15 ? ? ? ? A1`.
- The rain vtable paint slot points to this method at `0x00631264`.
- That vtable cell is the sole incoming route; there are no ordinary code callers. The analyzed CFG has 9 blocks and cyclomatic complexity 5.
- The method uses [UID:0001OW][0x0067a764-0x0067a768.ActiveMapPaneSingleton](by-memory/0x0067a764-0x0067a768.ActiveMapPaneSingleton.md) / `g_activeMapPane` to check the weather-render gate byte before drawing.
- Main-view rain table xrefs are exclusive to this body: `0x00670528 -> 0x005c14d3`, `0x006706d8 -> 0x005c15ad/0x005c1617`, and `0x00670730 -> 0x005c168d/0x005c16f7/0x005c1761/0x005c17ce`.
- The three exclusive objects are [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md)'s writable file-static `s_rainStreaks[3][24]`, `s_rainSmallSplashes[3][7]`, and `s_rainLargeSplashes[3][7]`. `RainStreak` is exactly three signed `short` fields (`x`, `y`, `length`); `RainSplash` is exactly two signed `short` fields (`x`, `y`).
- The method's table loads are signed (`movsx`, not `movzx`). It draws the first `18` streak records from each 24-record frame, leaving six explicit all-zero initializer records, and consumes all seven records in each small/large splash frame.
- These main-view objects are byte-identical to but physically distinct from the target-port family used by [UID:0003PK][0x005c1810-0x005c1bc7.RainingLayerPaneDrawRainOnTarget](by-memory/0x005c1810-0x005c1bc7.RainingLayerPaneDrawRainOnTarget.md). Exact pairwise hashes and the three compiler-alignment gaps are documented by [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md); this method does not consume or alias the target-port objects.
- Callees are the expected GrafPort/render callback dependencies: `0x004b9600`, `0x004b9660`, `0x0069b3fc`, `0x0069b3e4`, and `0x0069b3e0`.
- Local PE scan finds the exact body once, at `0x005c1460`; the only VA dword hit is the expected vtable slot at `0x00631264`.
- Six preceding and four succeeding bytes are compiler `0xcc` alignment; the successor gap `0x005c180c-0x005c1810` is not part of either method, and no split is needed.

## Exact Behavior And Control Flow

1. Call inherited `SetDrawColor(0)`.
2. Call `g_pfnFillRect(this, &m_bounds)` before testing the weather gate.
3. Dereference `g_activeMapPane` directly and return when `m_waitingForLocalPlayerStatus` at MapPane `+0x3f0` is nonzero. There is no null check; active MapPane existence is a runtime invariant.
4. Select palette color `143` (`0x8f`) with `SetDrawColor` for all rain strokes and pixels.
5. Iterate the first 18 records of `s_rainStreaks[m_currentFrame]`:
   - `MoveTo(2 * (17 * x / 15), 2 * (15 * y / 13))`;
   - `g_pfnDrawLineDelta(this, 2 * (-17 * length / 15), 2 * (30 * length / 13))`.
6. Iterate all seven `s_rainSmallSplashes[m_currentFrame]` records and emit two pixels per record after independent scaling: `(x,y+4)` and `(x+2,y+4)`.
7. Iterate all seven `s_rainLargeSplashes[m_currentFrame]` records and emit four pixels per record after independent scaling: `(x,y)`, `(x,y+4)`, `(x+4,y+6)`, and `(x+6,y+4)`.

All coordinate and length fields are signed shorts. The frame at inherited `+0xf8` is a signed byte. Multiplication, signed division, truncation, and final doubling remain in observed order; algebraic reassociation can change negative-coordinate behavior and is not source-equivalent. The machine body reloads `m_currentFrame` around callback calls, so the formal deliberately does not cache the frame or a row pointer.

There is no target-local clip operation, state restoration, defensive singleton check, array bounds check, or target-port table alias. The initial fill occurs even when weather drawing is gated off.

## Render And Field Contracts

- `0x004b9600` is inherited `GrafPort::MoveTo(int,int)`.
- `0x004b9660` is inherited `GrafPort::SetDrawColor(unsigned int)`.
- `0x0069b3fc` is `SurfaceFillRectProc g_pfnFillRect`.
- `0x0069b3e0` is `SurfaceDrawPixelProc g_pfnDrawPixel`.
- `0x0069b3e4` is `SurfaceDrawLineDeltaProc g_pfnDrawLineDelta`, with GrafPort receiver in ECX, signed `int deltaX, int deltaY`, `void` return, and `retn 8` installed targets.
- Weather/Rain object tail:
  - `signed char m_currentFrame` at `+0xf8`;
  - `signed char m_frameCount` at `+0xf9`;
  - `short m_timerInterval` at `+0xfa`;
  - `RectBounds m_weatherBounds` at `+0xfc`;
  - exact Weather and Rain extent `0x10c`, with no Rain-added storage.
- Rain construction establishes three frames and a 120 ms timer interval. Historical 120-frame/frame-step-3 wording is superseded.

## Source Placement And Declaration Dependencies

- Current canonical route is [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md) -> [UID:0000P8][WeatherLayerPane](by-file/WeatherLayerPane.md), so this qualified definition belongs to the current `WeatherLayerPane.cpp` family. A dedicated `RainingLayerPane.cpp` remains a historical alternative without PDB/source-path proof.
- The formal is actual method source, not a future sample. It intentionally relies on normal project headers as the original source would:
  - `Pane.h`: complete Pane/GrafPort/EventHandler/TimerHandler declarations, inherited `m_bounds`, `MoveTo`, `SetDrawColor`, virtual contracts, and `sizeof(Pane)==0xf8`;
  - `MapPane.h`: complete `MapPane` of size `0x910`, public `m_waitingForLocalPlayerStatus` at `+0x3f0`, and `extern MapPane *g_activeMapPane`;
  - `Surface.h`: matching slot-0/1/7 callback typedefs and extern declarations, with one definition each in `Surface.cpp`;
  - `WeatherLayerPane.h`: exact Weather/Rain hierarchy, fields, extents, and constructor/OnTimer/OnPaint/DrawOnTarget declarations.
- The current validator emits `.cpp` roots only and does not yet materialize these headers. No standalone generated-tree compilation is claimed. That surrounding source-tree dependency does not justify omitting the behavior-complete target formal or introducing incomplete local class/layout casts.
- Rejected source shapes: raw offset access to MapPane or Weather fields, an accessor absent from the binary, duplicate local Pane/MapPane/Surface class definitions, duplicate callback storage in the Weather unit, a defensive null guard, or decompiler labels.

## Score Rationale

Completion `92` and confidence `94` are justified by exact bytes/hash/bounds/signature, vtable-only reachability, full CFG, every branch/loop/callback and signed arithmetic operation, accepted table declarations, resolved field/gate/helper/callback names, direct class/file route, and behavior-complete human C++03 formal. The former GrafPort/render-helper, field, callback-declaration, and control-flow blockers are resolved.

The score remains below 95 because original private lexical spellings for `m_currentFrame` and `g_pfnDrawLineDelta` are inferred, the exact historical one-file versus dedicated Rain disk split is not PDB-proven, canonical project headers are not yet emitted by the current validator, and no final rebuild/code-generation comparison has been performed. These limits do not alter the exact method behavior or formal route.

## Cross-References

- [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md)
- [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md)
- [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md)
- [UID:0001OW][0x0067a764-0x0067a768.ActiveMapPaneSingleton](by-memory/0x0067a764-0x0067a768.ActiveMapPaneSingleton.md)
- [UID:0001WJ][WeatherLayerDataLayouts](by-type/by-struct/WeatherLayerDataLayouts.md)

## Changes

- 2026-07-23 B005 UID0003PJ source-closure callback:
  - Raised `86/88 -> 92/94`; preserved owner/emitter [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md), reconstructable true, and the exact unsplit range.
  - Inserted the behavior-complete `void RainingLayerPane::OnPaint()` formal with fill-before-gate ordering, no null guard, color 143, exact 18/7/7 loops, signed arithmetic/truncation, repeated frame loads, and slot-0/1/7 callback contracts.
  - Added exact body hash/signature, CFG/reachability, full field and source-facing dependency model, target-port separation, negative evidence, and the canonical Pane.h/MapPane.h/Surface.h/WeatherLayerPane.h obligations.
  - Historicalized the former blank-formal/helper/field/callback/control-flow blocker state. The current `.cpp`-only generator header gap remains a surrounding build dependency, not a target method blocker.
- 2026-07-22 B005 UID0001ON source-closure callback:
  - Metadata remains `86/88`; owner/emitter remains [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md).
  - Added exact `s_rainStreaks[3][24]`, `s_rainSmallSplashes[3][7]`, and `s_rainLargeSplashes[3][7]` contracts, signed `RainStreak`/`RainSplash` fields, 18-plus-6 streak shape, seven-entry splash shape, and physical-duplication boundary.
  - Removed the then-superseded table-name/type/source-placement blockers. At that historical checkpoint formal C++ remained blank for narrower GrafPort/render-helper, field, callback-declaration, and control-flow blockers; the 2026-07-23 callback resolves those blockers and supersedes that state.
- 2026-06-16 B001 child-split implementation:
  - Created this exact child page from the former [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md) aggregate after B001 verified exact function boundaries, vtable-slot evidence, active-map-pane gating, exclusive main-view rain table refs, unique PE body evidence, and padding.
  - Historical checkpoint: final C++ was blank because GrafPort/helper names, field names, table names, and the one-file versus per-layer weather source split were unresolved. The 2026-07-22 callback later resolved the table/type/source-route portion while retaining the method-level blockers.
