*** UID:0001HV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Socket::QueueHandshakeBlock(unsigned int blockSize,
                                 const void *blockData)
{
    MemoryMan *memoryMan = GetMemoryMan();
    void *queuedBlock = memoryMan->AllocateBufferMemory(blockSize);
    memoryMan->MemmoveWrapper(queuedBlock, blockData, blockSize);
    DispatchRequest(14, blockSize, reinterpret_cast<int>(queuedBlock));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Socket-owned retained QueueHandshakeBlock source: exact raw allocate-copy-dispatch body posts command 14, which Socket::HandleSocketCommand consumes through the handshake/seed builder; zero raw-start xrefs/pointers remain reachability history rather than an emission blocker. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00574d00-0x00574d3f SendRawData Raw Body

## Status

- Disposition: retained source-authored Socket command-14 request helper; exact behavior is reconstructable and emitted even though the raw IDA start remains unmodeled and externally unreferenced.
- Entity kind: raw/unmodeled request-dispatch helper body.
- Source-facing role: retained `Socket::QueueHandshakeBlock` / `Socket::QueuePacketTransformSeedBlock` helper candidate. The old `SendRawData` name is retained in the filename only as historical generated provenance.
- Rebuild handling: emit `Socket::QueueHandshakeBlock`; preserve the empty raw-start route search as historical liveness evidence, not as a reason to omit an intact function-shaped body.
- Direct source owner: [UID:0000DD][Socket](by-class/Socket.md), assigned by best-defensible command-consumer/source-structure evidence despite negative raw-start reachability.
- Parent attachment: attached to [UID:0000DD][Socket](by-class/Socket.md). This child is `92/94`; the accepted Socket owner/emitter and exact retained body clear the source gate.
- Confidence: strong for raw behavior, byte range, allocator/copy/dispatch callsites, neighbor boundaries, matching Socket command case, and final method ownership; historical reachability of the unmodeled start remains the sole material uncertainty.
- Evidence basis: live IDA MCP `lookup_funcs`, `disasm`, `xrefs_to`, `callers`, `decompile`, and `py_eval` byte/pointer/direct-call scans through 2026-06-11.

## Range

IDA does not create a function object at `0x00574d00`; `lookup_funcs` reports `Not a function`. Raw disassembly is nevertheless function-shaped:

- Prologue at `0x00574d00`: `push ebp; mov ebp, esp`
- Body through request dispatch call at `0x00574d31`
- Epilogue and `retn 8` at `0x00574d36-0x00574d3e`
- Alignment byte at `0x00574d3f`
- Next IDA-modeled function starts at `0x00574d40`

Use `0x00574d00-0x00574d3f` as the raw code range, exclusive of the alignment byte.

## Behavior

The raw body implements an allocate-copy-dispatch helper:

1. Saves `this` from `ecx`.
2. Calls the shared allocator manager getter at `0x00516030`.
3. Reads `size` from `[ebp+8]`.
4. Allocates exactly `size` bytes through `0x00516050`.
5. Copies `size` bytes from `[ebp+0x0c]` into the allocated buffer through `0x00516220`.
6. Calls [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) at `0x00596960` with request code `0x0e`, request field `size`, and request field `buffer`.

This differs from [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md), which appends a trailing zero and dispatches request code `8` with payload pointer first and size second. This raw helper dispatches request code `0x0e` and does not append a terminator.

The best current source-facing role is not a direct raw transport send. `Socket::HandleSocketCommand` command `14` routes the queued size/buffer into the handshake/seed block builder, so prefer `Socket::QueueHandshakeBlock`. `Socket::QueuePacketTransformSeedBlock` is a weaker but still plausible name. Reject `SendRawData` as a final source name because it implies bytes are written directly to Winsock/serial transport, which this helper does not do.

## Raw Assembly Shape

```asm
00574d00  push    ebp
00574d01  mov     ebp, esp
00574d07  mov     [ebp-4], ecx
00574d0a  call    sub_516030
00574d14  push    ebx
00574d17  call    sub_516050
00574d1c  push    ebx
00574d1d  push    dword ptr [ebp+0Ch]
00574d24  push    esi
00574d25  call    sub_516220
00574d2a  mov     ecx, [ebp-4]
00574d2d  push    esi
00574d2e  push    ebx
00574d2f  push    0Eh
00574d31  call    sub_596960
00574d3c  retn    8
```

## Source-Shape Note

The source-level operation is reconstructed as `Socket::QueueHandshakeBlock`: allocate `blockSize` bytes, copy the caller-provided block, then post request code `0x0e` with size first and copied pointer second. The intact prologue/body/epilogue, adjacent Socket command-post family, and exact Socket command-14 consumer support retained source emission. Empty xref, direct-call, data-ref, and pointer scans remain documented historical reachability evidence.

## Reachability

IDA MCP `xrefs_to` on 2026-05-25 reports no xrefs to `0x00574d00`. The only xref inside the raw span reported by that compact query is the self-contained instruction/data interpretation at `0x00574d1d -> 0x00574d20`, not an external caller.

2026-05-27 IDA raw-pointer scan across loaded segments found no dword equal to `0x00574d00`, so no obvious untyped callback table points at the raw start.

2026-05-28 IDA MCP recheck still reports `0x00574d00`, `0x00574d20`, and `0x00574d3e` as `Not a function`; the neighboring modeled functions remain `sub_574CE0` at `0x00574ce0-0x00574cf0`, `sub_574CF0` at `0x00574cf0-0x00574d00`, and `sub_574D40` at `0x00574d40-0x00574e44`.

2026-05-28 IDA `py_eval` scan across loaded segments found no dword equal to `0x00574d00`. `xrefs_to 0x00596960` still includes the internal raw-body call at `0x00574d31`, but no xref/caller targets the raw helper start.

2026-06-05 live IDA MCP recheck still reports `0x00574d00`, `0x00574d20`, `0x00574d3c`, `0x00574d3e`, and `0x00574d3f` as `Not a function`. The previous modeled function remains `sub_574CF0` at `0x00574cf0-0x00574d00`; the next modeled function remains `sub_574D40` at `0x00574d40-0x00574e44`.

The same live recheck confirms:

- `xrefs_to 0x00574d00` and `callers 0x00574d00` are empty.
- A loaded-segment dword scan finds no pointer equal to `0x00574d00`.
- Raw bytes for `0x00574d00-0x00574d3f` are `55 8b ec 51 53 56 57 89 4d fc e8 21 13 fa ff 8b 5d 08 8b f8 53 8b cf e8 34 13 fa ff 53 ff 75 0c 8b f0 8b cf 56 e8 f6 14 fa ff 8b 4d fc 56 53 6a 0e e8 2a 1c 02 00 5f 5e 5b 8b e5 5d c2 08 00`.
- The raw helper is followed by exactly one `0xcc` byte at `0x00574d3f` before `sub_574D40`.
- Near-cluster xrefs to `sub_516050`, `sub_516220`, and `sub_596960` include the raw-body calls at `0x00574d17`, `0x00574d25`, and `0x00574d31`, but those xrefs are recorded with no owning IDA function because the raw start is not modeled.

2026-06-07 live IDA MCP `py_eval` reconfirmed the same raw status and added a direct-call scan:

- `0x00574d00`, `0x00574d20`, `0x00574d3c`, and `0x00574d3f` still have no containing IDA function.
- `xrefs_to`, `CodeRefsTo`, and `DataRefsTo` for `0x00574d00` all return zero.
- A `.text` `E8 rel32` sweep found no direct calls targeting `0x00574d00`.
- A loaded-segment dword scan found no pointer equal to `0x00574d00`.
- The raw-body calls remain `0x00574d0a -> 0x00516030`, `0x00574d17 -> 0x00516050`, `0x00574d25 -> 0x00516220`, and `0x00574d31 -> 0x00596960`.
- The byte string remains `55 8b ec 51 53 56 57 89 4d fc e8 21 13 fa ff 8b 5d 08 8b f8 53 8b cf e8 34 13 fa ff 53 ff 75 0c 8b f0 8b cf 56 e8 f6 14 fa ff 8b 4d fc 56 53 6a 0e e8 2a 1c 02 00 5f 5e 5b 8b e5 5d c2 08 00`.

2026-06-11 live IDA MCP reconfirmed and extended the raw-start audit:

- `0x00574d00`, `0x00574d20`, `0x00574d3c`, and `0x00574d3f` still have no containing IDA function.
- `CodeRefsTo(0x00574d00)`, `DataRefsTo(0x00574d00)`, a `.text` direct-call sweep, and a loaded-segment dword scan for `0x00574d00` are all empty.
- Raw-body calls remain `0x00574d0a -> 0x00516030`, `0x00574d17 -> 0x00516050`, `0x00574d25 -> 0x00516220`, and `0x00574d31 -> 0x00596960`.
- The raw byte string and one-byte `0xcc` separator at `0x00574d3f-0x00574d40` are unchanged.
- [UID:0001I1][0x00575d90-0x005797ae.SocketTransportCore](by-memory/0x00575d90-0x005797ae.SocketTransportCore.md) `Socket::HandleSocketCommand` at `0x00575e60` has case `14`, which calls the raw-data consumer path with the queued size and buffer fields. This strengthens Socket ownership for the semantics, but does not prove the unmodeled raw start was reachable.
- Historical 2026-06-20 B006 raw PE route scan found no direct rel32 routes to `0x00574d00`, matching the earlier empty code/data/pointer-ref result. The accepted callback retains that negative evidence but no longer treats it as an emission blocker.

Neighbor checks:

- `0x00574ce0-0x00574cef` is an IDA-modeled setter for send-disabled byte `this + 240206`, with seven direct callers.
- `0x00574cf0-0x00574cff` is an IDA-modeled setter for byte `this + 240205`, with one direct caller.
- `0x00574d40-0x00574e44` is an IDA-modeled position/status packet sender with two direct callers.

Current interpretation: this is a real compiled Socket helper body that IDA did not model as a function. Current raw-start reachability remains unproven, but exact body structure, source-family placement, and the sole command-14 consumer are sufficient to retain `Socket::QueueHandshakeBlock` in reconstructed source.

## Ownership Decision

Attach the body to [UID:0000DD][Socket](by-class/Socket.md) as the best defensible source owner. It posts command `0x0e`, and `Socket::HandleSocketCommand` case `14` consumes the queued size/buffer fields by feeding the handshake/seed builder. The raw start still has no caller/table/pointer/direct-rel32 evidence, but the command semantics, adjacent Socket helper family, `g_packetSender` Socket-lifetime writes, and Socket dispatcher ownership are stronger than leaving the helper parentless or routing it to stale CashShopRequest docs.

Do not use this body as evidence that [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) is `CashShopRequest*`; the sender-global lifetime belongs to [UID:0000NS][Socket](by-file/Socket.md), and `0x00596960` is [UID:0000OR][Thread](by-file/Thread.md) queue infrastructure.

## Autogen Status

This page is reconstructable, assigned to [UID:0000DD][Socket](by-class/Socket.md), and emits the exact retained `QueueHandshakeBlock` body through that owner. Socket H owns its declaration once. The raw start still has no IDA function object, caller, pointer-table reference, direct-call reference, direct rel32 route, or data reference; those facts constrain liveness confidence but do not override the intact body and uniquely matching Socket command consumer.

Under the corrected assignment gate, both child and parent now clear 85/85. The no-caller caveat limits final reconstruction, not source placement: the only positive semantic consumer for command `14` is Socket's dispatcher.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | Exact raw boundaries, assembly/byte shape, allocation-copy-dispatch behavior, command-14 consumer, Socket ownership, retained-source policy, formal CPP, declaration route, neighboring functions, and exhaustive negative reachability scans are all closed. |
| Confidence `94` | Binary behavior and Socket ownership are exact; the remaining uncertainty is historical live reachability of the unmodeled start, not source behavior or placement. |

## Cross-References

- [UID:0001HT][0x00574b90-0x00575377.CashShopRequestSendQueue](by-memory/0x00574b90-0x00575377.CashShopRequestSendQueue.md)
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md)
- [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md)
- [UID:0000DD][Socket](by-class/Socket.md)
- [UID:0000I0][CashShopRequest](by-file/CashShopRequest.md)
- [UID:00001H][CashShopRequest](by-class/CashShopRequest.md)
- [UID:0000OR][Thread](by-file/Thread.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)
- [UID:0000NS][Socket](by-file/Socket.md)
- [UID:0001QH][client_network](by-meta/client_network.md)

## 2026-08-17 B003 UID0000MV Accepted Callback

The accepted source form is `Socket::QueueHandshakeBlock(unsigned int, const void *)`. It allocates exactly `blockSize` bytes through MemoryMan, copies exactly that many bytes, and posts Thread request `14` as `(blockSize, queuedBlock)`. `Socket::HandleSocketCommand` is the unique semantic owner of command 14 and feeds those queued fields into the handshake/seed builder. The historical zero-xref/zero-pointer/direct-call result remains explicit but no longer suppresses source emission.

## Changes

- 2026-08-17 B003 UID0000MV accepted callback: raised `88/91` to `92/94`, replaced the comment-only no-code disposition with the exact retained `QueueHandshakeBlock` body, and historicalized the former reachability-as-emission-blocker interpretation.
- 2026-06-30 B007 implementation callback: raised score from `87/90` to `88/91`; formal no-code proof now records the retained raw `0x00574d00` body as Socket command `14` queueing evidence, not padding and not final `SendRawData` source. Current MCP still shows no modeled function or direct xrefs, while `Socket::HandleSocketCommand` consumes command `14` through the handshake/seed-builder route.
- 2026-06-20 B006 Socket support sync:
  - Reframed the source-facing role from generated `SendRawData` to retained `Socket::QueueHandshakeBlock` / `QueuePacketTransformSeedBlock`, recorded that command `14` feeds the handshake/seed builder rather than directly writing to transport, and added negative direct-rel32 route evidence.
  - No score or C++ change; the helper remains Socket-owned but non-emitting because caller/table/pointer/direct-route evidence is still absent.
- 2026-06-11 Agent-B001 B001-051:
  - Changed to: completion `87`, confidence `90`, keeping `RECONSTRUCTABLE:TRUE`, assigning `AUTOGEN_PARENT_UID:0000DD`, and leaving C++ blank.
  - Summary/evidence: B001 rechecked the whole `0x00574b90-0x00575377` corridor and found that the surrounding modeled helpers are Socket methods, command `14` is consumed by `Socket::HandleSocketCommand`, `g_packetSender` lifetime writes are Socket-owned, and Thread owns only the queue primitive.
  - Assignment rationale: direct reachability remains negative, but source placement is now best assigned to Socket rather than left parentless because no competing owner has positive binary evidence.
- 2026-06-11 Batch188 IDA MCP audit:
  - Changed to: completion `86`, confidence `89`, keeping `RECONSTRUCTABLE:TRUE`, blank `AUTOGEN_PARENT_UID`, and blank C++.
  - Summary/evidence: live IDA MCP reconfirmed raw non-function status, exact bytes, raw call targets, empty code refs/data refs, no direct `.text` calls, no loaded-segment dword pointer hit, one-byte `0xcc` boundary, and `Socket::HandleSocketCommand` case `14` as the matching queued raw-data consumer.
  - Assignment rationale: Socket ownership is now the strongest semantic interpretation, but parent assignment still stays blank because the raw start has no caller/table evidence proving reachability.
- 2026-06-07 Batch008 IDA MCP audit:
  - Changed to: completion `85`, confidence `88`, keeping `RECONSTRUCTABLE:TRUE`, blank `AUTOGEN_PARENT_UID`, and blank C++.
  - Summary/evidence: live IDA MCP reconfirmed the raw non-function status, raw-body call targets, exact bytes, empty xrefs/code refs/data refs, no direct `.text` `E8` calls to the raw start, no loaded-segment dword pointer hit, and the one-byte `0xcc` boundary before `sub_574D40`.
  - Assignment rationale: child score now reaches 85/85, but corrected-gate assignment still fails because the enclosing aggregate is `84/86`, by-file parent candidates do not all clear 85/85, and no direct caller/table evidence proves a raw-start source owner.
- 2026-06-05:
  - Changed to: completion `84`, confidence `86`, keeping `RECONSTRUCTABLE:TRUE`, blank `AUTOGEN_PARENT_UID`, and blank C++.
  - Summary/evidence: live IDA MCP reconfirmed that `0x00574d00` is not a modeled function, has no direct xrefs/callers, has no loaded-segment dword pointer hit, and is followed by a single `0xcc` alignment byte before `sub_574D40`; disassembly and raw bytes confirm the allocate-copy-dispatch body, calls to `sub_516030`, `sub_516050`, `sub_516220`, and `sub_596960`, and request code `0x0e`.
  - Historical parent/C++ rationale before the B007 callback: parent assignment was blank because reachability for the raw start was still unproven and the enclosing request-sender/source-file split remained unresolved; final C++ was blank because the raw helper did not meet the then-applied reconstruction bar.
- 2026-06-02:
  - Before: scored `65/65`, reconstructability blank, and included a C++ behavior sketch despite unresolved owner/reachability.
  - After: scored `72/80`, marked reconstructable, replaced the sketch with a source-shape note, and kept parent/C++ blank.
  - Why: existing IDA-backed evidence is strong enough for reconstructable raw behavior and boundary confidence, but not strong enough for final-source C++ or a parent attachment.
- 2026-05-28:
  - Before: documented as reconstructable/projected code with weak reachability confidence.
  - After: documented as a retained raw outbound request helper whose byte range and behavior are known, but whose live call path remains unproven; completion/confidence raised to medium.
  - Why: IDA MCP recheck still finds no function object, caller, xref, or loaded-segment dword pointer for `0x00574d00`, while raw disassembly and the internal call to `DispatchRequest(0x0e, size, buffer)` remain stable.
