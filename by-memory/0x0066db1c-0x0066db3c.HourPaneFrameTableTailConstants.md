*** UID:0002ZK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: A002's 2026-06-10 IDA MCP recheck classified the range as reviewed non-emitting orphan `.data`: exact bytes are `00 00 7f fe` plus twelve `40 02` words plus `7f fe 00 00`, a full byte-level `XrefsTo` scan across `0x0066db1c-0x0066db3c` found no refs, `0x0066db1c` has no name and remains untyped one-byte `.data`, `HourPane::OnPaint` indexes only `word_66DB04[hour / 2 % 12]`, the preceding frame table has refs at `0x004cf060` and `0x004cf0f7`, and the successor JPEG marker slot has its `0x004e4e92` image-decode ref at `0x0066db3c`; page is `RECONSTRUCTABLE:FALSE` and should be reopened only if a later hidden/value-based consumer is found. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0066db1c-0x0066db3c HourPane Frame Table Tail Constants

## Summary

- Address range: `0x0066db1c-0x0066db3c`
- Entity kind: initialized no-direct-xref constant tail / value-scan-negative orphan data.
- Disposition: reviewed non-emitting orphan data.
- Rebuild handling: unknown initialized data, not a standalone source item. These bytes are initialized `.data`, but current IDA/export evidence does not prove a source owner, source type, source name, or live consumer.
- Parent handling: intentionally unassigned; `RECONSTRUCTABLE:FALSE` means do not emit this tail as an independent reconstructed object unless a later owner/use is found.

This exact split holds the constants that previously trailed the live `HourPane` frame table. They are not reachable through the `HourPane::OnPaint` modulo-12 table index, have no direct xrefs in the current IDA database, and are bounded by live referenced neighbors at `0x0066db04` and `0x0066db3c`. B010's 2026-06-19 source-routing pass rechecked the obvious value and address-consumer leads and kept the same no-owner/no-emitter disposition rather than treating the range as deferred final-C++ work.

## Layout

| Range | Words | Notes |
| --- | --- | --- |
| `0x0066db1c-0x0066db3c` | `0x0000`, `0xfe7f`, twelve `0x0240` words, `0xfe7f`, `0x0000` | Patterned initialized `.data`; `0x0240` is decimal 576 and `0xfe7f` is decimal 65151 (verified with `tools/int_convert.py`). The word view is an audit interpretation only; do not promote it to a source `uint16_t[16]` or any named table without a real consumer. |

## Live IDA Evidence

- 2026-06-07 A007 IDA MCP byte read confirms the 32-byte tail sequence: `00 00 7f fe` followed by twelve `40 02` words, then `7f fe 00 00`.
- IDA reports no name at `0x0066db1c`.
- `xrefs_to` for every even address from `0x0066db1c` through `0x0066db3a` is empty.
- The preceding exact source table [UID:00027K][0x0066db04-0x0066db1c.HourPaneFrameTable](by-memory/0x0066db04-0x0066db1c.HourPaneFrameTable.md) is referenced only through the `0x0066db04` base and a modulo-12 word index.
- The following exact item [UID:00027L][0x0066db3c-0x0066db40.ImageDecodeJpegEoiMarkerSlot](by-memory/0x0066db3c-0x0066db40.ImageDecodeJpegEoiMarkerSlot.md) starts at the first byte with a live image-decode callback xref.
- 2026-06-10 A002 IDA MCP recheck:
  - `py_eval` byte read reconfirmed the exact 32-byte sequence `00 00 7f fe` + twelve `40 02` words + `7f fe 00 00`.
  - Full byte-level `XrefsTo` scan across every byte in `0x0066db1c-0x0066db3c` found no references.
  - IDA item/name audit shows this tail is untyped one-byte `.data` items with no name at `0x0066db1c`; adjacent names remain `word_66DB04` at `0x0066db04`, `unk_66DB3C` at `0x0066db3c`, and `unk_66DB40` at `0x0066db40`.
  - `xrefs_to` reconfirmed `word_66DB04` has two data refs from `HourPane::OnPaint` at `0x004cf060` and `0x004cf0f7`, while `0x0066db3c` has the image-decode fallback ref at `0x004e4e92`.
  - `HourPane::OnPaint` decompilation still indexes `word_66DB04[hour / 2 % 12]`, so the last reachable word is at `0x0066db1a`; the tail starting at `0x0066db1c` is outside the proven frame-table extent.
- 2026-06-19 B010 source-routing recheck:
  - Live IDA MCP was unavailable for the B010 pass, so new claims are limited to existing target/support-page evidence plus local export snapshots under `resources/exported_data/functions`.
  - `resources/exported_data/functions/0x004cf010.json` independently shows both `TIME.EPF` and `TIME.EPD` branches computing `&DAT_0066db04 + (((hour / 2) % 0xc) * 2)`. The maximum modulo index is `11`, so the last reachable word starts at `0x0066db1a`; `0x0066db1c` remains outside the HourPane table.
  - `resources/exported_data/functions/0x004e4e70.json` independently shows the image-decode callback writing `&DAT_0066db3c` and length `2` after the caller buffer is exhausted. That consumer starts at the successor marker and does not address this preceding range.
  - Scoped text/export searches for `0x0066db1c`, `0x66db1c`, `DAT_0066db1c`, `66DB1C`, `0xfe7f`, and `0x0240` found no meaningful source-owner or value-consumer route beyond the false-positive helper described below.

## Value-Consumer Scan

B010 found one suspicious local export hit in `resources/exported_data/functions/0x005adad0.json`: Ghidra prints a pointer-identity guard against `(cls_0x427990 *)0x66db1c`. This is not evidence that [UID:0002ZK] is read or source-owned:

- The export's `data_refs` list is empty.
- The expression is a pointer comparison/bound artifact, not a dereference or load from `0x0066db1c`.
- The helper's actual data source is the neighboring [UID:00027J][0x0066daec-0x0066db04.g_emptySimpleUString](by-memory/0x0066daec-0x0066db04.g_emptySimpleUString.md): the decompilation selects `&DAT_0066daec` or `DAT_0066daec` depending on `DAT_0066db00`, then copies `DAT_0066dafc` bytes.
- The same Ghidra decompilation also prints a guard against `&DAT_0066db04`, reinforcing that the displayed constants are decompiler boundary artifacts around an object/array walk rather than real tail reads.
- The ordinary caller snapshot `resources/exported_data/functions/0x00514640.json` calls `0x005adad0` from a UI/collection update route and also has `data_refs: []`; it does not turn the immediate into a tail-byte consumer.
- The `0xfe7f` and `0x0240` value scan produced only common generated offset/field-name noise, not a documented consumer of this range.

Result: the value-consumer lead is rejected. It supports keeping this page as reviewed orphan initialized data, not routing it to HourPane, ImageLoaders, `g_emptySimpleUString`, or the collection/UI reset helper.

## Reconstruction Notes

- Do not fold this tail into `g_hourFrameTable`; the only proven table consumer cannot index beyond 12 words.
- Do not route this range to [UID:0000K3][ImageLoaders](by-file/ImageLoaders.md) or JPEG decode. The image callback uses exact address `0x0066db3c` and length `2`; no callback path reaches the preceding `0x0066db1c-0x0066db3c` bytes.
- Do not mark this as compiler/linker padding. The `0xfe7f` and repeated `0x0240` values are patterned initialized data, not a normal fill/alignment pattern.
- Do not attach this to `g_emptySimpleUString` or the `0x005adad0` collection reset/helper route. The only local export address hit is a no-data-ref pointer-guard artifact, while the helper actually copies the empty-string storage at `0x0066daec-0x0066db04`.
- Do not emit first-draft C++. A declaration such as `static const uint16_t s_unknownTail[] = {...};` would invent a source name, source type, ownership, and intentional reachability that current evidence does not support.
- Treat this as reviewed non-emitting orphan data for now. If a later owner pass finds a real live IDA xref, relocation, runtime consumer, symbol/map/PDB/object-file evidence, hidden table base, or source-layout evidence, reopen the classification and attach it to the new owner rather than treating this page as final source.

## Source-Routing Decision

Accepted disposition: `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, and blank formal C++.

Rejected owner/source routes:

- [UID:0000JX][HourPane](by-file/HourPane.md) / [UID:0002ZE][g_hourFrameTable](by-global/g_hourFrameTable.md): physical adjacency and word-shaped values are not enough. The only proven consumer uses base `0x0066db04` with `(hour / 2) % 12`; the tail begins after the twelfth word.
- [UID:0000K3][ImageLoaders](by-file/ImageLoaders.md) / [UID:0002ZF][ImageDecodeJpegEoiMarker](by-global/ImageDecodeJpegEoiMarker.md): the callback starts exactly at `0x0066db3c` and returns two bytes. It does not read the preceding tail.
- Compiler/linker padding: the bytes are real patterned initialized data, not `00`/`cc`/alignment fill.
- `g_emptySimpleUString` or collection/UI reset helper ownership: the `0x005adad0` lead has no data ref or dereference of this page and resolves to empty-string copies from the preceding storage.
- Generic hidden source table: possible as stale/dead object-file data, but unproven. Without a source symbol/type/consumer, emitting a source declaration would be less accurate than preserving the bytes as orphan initialized data.

## No-Code Proof

Formal C++ remains blank because:

- No function is proven to read, address, or index any byte in `0x0066db1c-0x0066db3c`.
- The live HourPane consumer stops before the range, and the live JPEG fallback marker consumer starts after it.
- The only address-like local export hit is a non-dereferenced immediate/bound comparison with `data_refs: []`.
- IDA has no source-quality name or type at `0x0066db1c`.
- There is no local symbol/map/PDB/object-file/relocation evidence tying the bytes to a source declaration.

Optional IDA-side analysis only: leave `0x0066db1c` unnamed and untyped as source data. A repeatable analysis comment may note that this is an unreferenced initialized data tail after `g_hourFrameTable`, not HourPane table data, not JPEG marker data, and not padding.

## Cross-References

- [UID:00027K][0x0066db04-0x0066db1c.HourPaneFrameTable](by-memory/0x0066db04-0x0066db1c.HourPaneFrameTable.md)
- [UID:00027L][0x0066db3c-0x0066db40.ImageDecodeJpegEoiMarkerSlot](by-memory/0x0066db3c-0x0066db40.ImageDecodeJpegEoiMarkerSlot.md)
- [UID:0001Z8][0x0066d000-0x0069d000.DataSection](by-memory/0x0066d000-0x0069d000.DataSection.md)

## Changes

- 2026-06-07 A007 Batch 052: Split from the old mixed [UID:00027K][0x0066db04-0x0066db1c.HourPaneFrameTable](by-memory/0x0066db04-0x0066db1c.HourPaneFrameTable.md) `0x0066db04-0x0066db3c.HourPaneFrameTable` page after live IDA confirmed the live HourPane table stops at `0x0066db1c` and this tail has no direct address xrefs.
- 2026-06-10 A002: Classified as `RECONSTRUCTABLE:FALSE` reviewed non-emitting orphan data, raised score from `68/84` to `82/88`, and added byte-level IDA MCP evidence. The recheck confirmed exact bytes, no xrefs to any byte in the range, untyped `.data` items, referenced neighboring starts at `0x0066db04` and `0x0066db3c`, and `HourPane::OnPaint` modulo-12 access proving the live frame table ends before this tail.
- 2026-06-19 B010 source-routing incorporation:
  - Changed `COMPLETION:82` / `CONFIDENCE:88` to `88/91`.
  - Summary/evidence: local export recheck confirmed the lower/upper boundaries through `0x004cf010` HourPane paint and `0x004e4e70` JPEG fallback callback snapshots; rejected the `0x005adad0` `0x66db1c` immediate as a no-data-ref, non-dereference pointer-bound artifact whose actual copies use [UID:00027J][0x0066daec-0x0066db04.g_emptySimpleUString](by-memory/0x0066daec-0x0066db04.g_emptySimpleUString.md); and recorded explicit no-code proof. Owner/emitter stay blank because the best-supported rebuild policy is reviewed orphan initialized data, not a source declaration.
