*** UID:0003W9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: 0x00550dc0-0x00550e16 | no-route retained raw class helper | MenuQuestionItemListAddRowNoRouteRaw : ignored/non-emitting : 88% : strong : B013 2026-06-19 source-routing pass resolves semantic owner as [UID:00007Z][MenuQuestionItemList](by-class/MenuQuestionItemList.md) but keeps the range non-reconstructable and non-emitting: exact `0x56`-byte thiscall helper with stack cookie, signature `AppendMenuQuestionRow(unsigned char optionId, const wchar_t *label)`, `MenuQuestionItemRow` layout `{ optionId at +0, alignment byte at +1, UTF-16 label[0x100] at +2 }`, `_wcscpy_s(row.label, 0x100, label)`, shared [UID:00007A][ListPane](by-class/ListPane.md) append dependency `0x004f3c50` / `ListPane::AddEntry`, `retn 8`, surrounding `0xcc` padding, no rel32/VA/RVA route to `0x00550dc0`, positive-control hits for neighboring live constructor/vtable targets, and active constructor inline equivalents at `0x0054ebe8` and `0x0054f358`; formal C++ stays blank unless a caller/pointer/vtable/runtime route is later proven or the project explicitly emits dead retained helpers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00550dc0-0x00550e16 MenuQuestionItemList Add Row No-Route Raw

## Status

- Entity kind: retained no-route source-shaped class helper.
- Semantic owner: [UID:00007Z][MenuQuestionItemList](by-class/MenuQuestionItemList.md). This is a documentation/source-placement owner only; it is not an emission route.
- Address range: `0x00550dc0-0x00550e16`, inclusive start/exclusive end.
- Rebuild handling: non-emitting retained raw evidence only.
- Disposition: not reconstructable until a caller, pointer, vtable, or runtime route to `0x00550dc0` is proven, or the project deliberately chooses to emit dead retained helpers for binary-shape parity.
- Containing split index: [UID:0001FH][0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs](by-memory/0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs.md).
- Source-family context: [UID:0000LA][MessageDialogs](by-file/MessageDialogs.md).
- Final C++: ineligible in the formal reconstruction block because the helper has no proven live route.

## Inferred Signature

Best current source-facing signature, descriptive rather than original-symbol proof:

```cpp
void MenuQuestionItemList::AppendMenuQuestionRow(
    unsigned char optionId,
    const wchar_t *label);
```

Binary contract:

- `ecx` is the `MenuQuestionItemList` / `ListPane` receiver and is preserved in `esi`.
- `[ebp+8]` is loaded as a single byte and stored at row offset `+0`.
- `[ebp+0xc]` is the UTF-16 label pointer passed to `_wcscpy_s`.
- The body returns with `retn 8`, matching two stack arguments after the `thiscall` receiver.

The exact original method spelling remains inferred. `AppendMenuQuestionRow` / `AddRow` is the current best human-facing name because the body stages one menu-question row record and appends it to the list.

## Behavior

The helper builds a stack row record and appends it to the inherited `ListPane` storage:

1. Allocates a stack-cookie protected frame with a `0x208` byte row local at `ebp-0x208`.
2. Copies `optionId` into `row+0`.
3. Calls `_wcscpy_s(row.label, 0x100, label)` with destination `row+2`.
4. Calls `0x004f3c50` with `ecx=this` and `&row` as the only explicit argument.
5. Validates the security cookie and returns with `retn 8`.

The row shape is:

```cpp
struct MenuQuestionItemRow {
    unsigned char optionId;
    unsigned char alignmentPadding;
    wchar_t label[0x100];
};
```

The byte at `+1` is best treated as alignment/padding. The helper never writes it, while the row renderer consumes the UTF-16 text from `itemData+2`. Do not promote byte `+1` to a named semantic field unless later evidence shows a consumer.

## Raw Disassembly

```asm
00550dc0  push ebp
00550dc1  mov ebp, esp
00550dc3  sub esp, 208h
00550dc9  mov eax, [672f24h]
00550dce  xor eax, ebp
00550dd0  mov [ebp-4], eax
00550dd3  mov edx, [ebp+0Ch]
00550dd6  mov al, [ebp+8]
00550dd9  push esi
00550dda  push edx
00550ddb  mov [ebp-208h], al
00550de1  mov esi, ecx
00550de3  lea eax, [ebp-206h]
00550de9  push 100h
00550dee  push eax
00550def  call 005cd657
00550df4  add esp, 0Ch
00550df7  lea eax, [ebp-208h]
00550dfd  mov ecx, esi
00550dff  push eax
00550e00  call 004f3c50
00550e05  mov ecx, [ebp-4]
00550e08  xor ecx, ebp
00550e0a  pop esi
00550e0b  call 005c772f
00550e10  mov esp, ebp
00550e12  pop ebp
00550e13  ret 8
```

Boundary facts:

- `0x00550db4-0x00550dc0` is twelve bytes of `0xcc` after the live [UID:0003W8][0x00550d50-0x00550db4.MenuQuestionItemListConstructor](by-memory/0x00550d50-0x00550db4.MenuQuestionItemListConstructor.md).
- `0x00550dc0-0x00550e16` is the complete helper body, including `c2 08 00`.
- `0x00550e16-0x00550e20` is ten bytes of `0xcc` before [UID:0003WA][0x00550e20-0x00550e2e.MenuQuestionItemListHasSelectableEntryNoRouteRaw](by-memory/0x00550e20-0x00550e2e.MenuQuestionItemListHasSelectableEntryNoRouteRaw.md).

## Callee And Helper Resolution

`0x005cd657` is `_wcscpy_s` under the observed call contract `(destination, 0x100, source)`.

`0x004f3c50` should be documented with the source-facing dependency name `ListPane::AddEntry` rather than left as `sub_4F3C50`:

- Existing [UID:0000KT][ListPane](by-file/ListPane.md) and [UID:00007A][ListPane](by-class/ListPane.md) docs already use `ListPane::AddEntry` for this append helper.
- The helper appends one primary-list item at the current count, mirrors a cleared byte into the selection-state list, calls the scroll/viewport sync helper at `0x0055e990`, and dispatches the virtual refresh slot through `this+0x110`.
- Local route controls from the B013 report found 47 direct call/jump hits to `0x004f3c50`, including active menu-question constructor calls at `0x0054ebe8`, `0x0054f358`, `0x0054fdf8`, and `0x00550569`, plus this retained helper call at `0x00550e00`.

## Route And Liveness

## Evidence

- B001 2026-06-16 raw-byte audit found the stack-cookie frame, byte/text copy, `_wcscpy_s`, and `0x004f3c50` call.
- IDA does not model a function at `0x00550dc0`.
- IDA `xrefs_to`, raw rel32 call/jump scan, and absolute VA/RVA pointer scan found no route to this start.
- Nearby live list constructor and draw methods establish semantic context but not ownership/emission.
- B013 2026-06-19 source-routing recheck used a local PE parser and Capstone scan against `NexusTK.exe` (`MD5 4247E04E20B65D6414C7238AA8FF5515`, `SHA256 9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`). It independently found no rel32 branch/call, absolute VA, or RVA reference to `0x00550dc0`.
- Positive controls in the same scan found live nearby routes: constructor calls to `0x00550d50` at `0x0054eb04` and `0x0054f276`, vtable entries for `0x00550e30` at `0x006227b0` and `0x00550e40` at `0x006227b4`, larger constructor calls to `0x00550ed0` at `0x0054fd14` and `0x00550489`, larger vtable entries for `0x00550f90` and `0x00550fa0`, 47 hits to `ListPane::AddEntry` at `0x004f3c50`, and 98 hits to the selected-entry accessor at `0x004f3dc0`.
- The same scan found no rel32/VA/RVA route to sibling retained raw starts `0x00550e20`, `0x00550f20`, or `0x00550f80`, while live constructor/vtable controls were recovered.

## Active Constructor Comparison

The live constructors do not call this helper. They inline equivalent row staging before calling `ListPane::AddEntry` directly:

- Normal constructor windows at `0x0054ebe8` and `0x0054f358` build the same `{ optionId, padding, wchar_t label[0x100] }` stack row, copy the label with `_wcscpy_s`, and call `0x004f3c50`.
- Larger constructor windows at `0x0054fdf8` and `0x00550569` use the same pattern for the larger menu-question list.

This is the main reason the page remains non-reconstructable despite the semantic owner now being known. Emitting this helper as live source would imply an out-of-line helper call that current route evidence does not support.

## Ownership Analysis

Accepted semantic owner: [UID:00007Z][MenuQuestionItemList](by-class/MenuQuestionItemList.md).

Evidence:

- The helper is bracketed inside the normal `MenuQuestionItemList` method band, immediately after the live constructor and before normal-list raw/virtual methods.
- It is a `thiscall` helper over a `ListPane` receiver and builds the menu-question row consumed by [UID:0003WC][0x00550e40-0x00550ec1.MenuQuestionItemListDrawRow](by-memory/0x00550e40-0x00550ec1.MenuQuestionItemListDrawRow.md), whose renderer reads label text from `itemData+2`.
- It mirrors the larger retained helper [UID:0003WE][0x00550f20-0x00550f76.MenuQuestionItemListLargerAddRowNoRouteRaw](by-memory/0x00550f20-0x00550f76.MenuQuestionItemListLargerAddRowNoRouteRaw.md).
- [UID:0000LA][MessageDialogs](by-file/MessageDialogs.md) is the source-family context, but the narrow owner is the class-private list widget.

Rejected alternatives:

- `CANONICAL_OWNER:NONE` is now too conservative for source placement. It remains true only for liveness/emission.
- `MessageDialogs` is the file parent, not the direct canonical owner.
- `ListPane` supplies `AddEntry` but does not own the menu-question row-building helper.
- The body is not padding, a compiler thunk, a packet helper, or a vtable-dispatched live method.

## Exact No-Code Proof

Formal C++ stays blank because:

1. There is no static `rel32` call/jump/conditional branch target to `0x00550dc0`.
2. There is no little-endian absolute VA `0x00550dc0` or RVA `0x00150dc0` pointer in the scanned PE ranges.
3. Positive controls in the same scan recovered neighboring live constructor and vtable references, so the negative result is meaningful.
4. Prior B001 IDA evidence found no IDA function object and no `xrefs_to` route to the helper start.
5. The active `MenuQuestionDialog` constructors already inline this row append behavior and call `ListPane::AddEntry` directly.
6. Emitting a callable method now would add dead retained helper code with no proven source-use surface.

Reference-only source shape if a future route is proven:

```cpp
void MenuQuestionItemList::AppendMenuQuestionRow(
    unsigned char optionId,
    const wchar_t *label)
{
    MenuQuestionItemRow row;

    row.optionId = optionId;
    wcscpy_s(row.label, _countof(row.label), label);
    AddEntry(&row);
}
```

Do not place this sketch in the formal `RECONSTRUCTION_CPP` block unless a route is proven or project policy changes for dead retained helpers.

## Score Rationale

Completion rises to `88` because the page now records exact raw disassembly, signature inference, stack row layout, source-facing callee names, route scan results, positive controls, active-constructor comparison, ownership ranking, rejected alternatives, and exact no-code proof.

Confidence rises to `88` because semantic class ownership is strongly supported by adjacency, row layout, draw-method consumption, sibling parity, and the independent B013 route scan. Confidence remains below final audit because no original symbol or live route has been recovered, exact source spelling is inferred, and the helper stays dead-retained/non-emitting.

## Cross-References

- [UID:00007Z][MenuQuestionItemList](by-class/MenuQuestionItemList.md) `MenuQuestionItemList`
- [UID:0001FH][0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs](by-memory/0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs.md) `NpcMessageAndMenuQuestionDialogs`
- [UID:0000LA][MessageDialogs](by-file/MessageDialogs.md) `MessageDialogs`
- [UID:00007A][ListPane](by-class/ListPane.md) `ListPane`
- [UID:0003W8][0x00550d50-0x00550db4.MenuQuestionItemListConstructor](by-memory/0x00550d50-0x00550db4.MenuQuestionItemListConstructor.md)
- [UID:0003WA][0x00550e20-0x00550e2e.MenuQuestionItemListHasSelectableEntryNoRouteRaw](by-memory/0x00550e20-0x00550e2e.MenuQuestionItemListHasSelectableEntryNoRouteRaw.md)
- [UID:0003WC][0x00550e40-0x00550ec1.MenuQuestionItemListDrawRow](by-memory/0x00550e40-0x00550ec1.MenuQuestionItemListDrawRow.md)
- [UID:0003WE][0x00550f20-0x00550f76.MenuQuestionItemListLargerAddRowNoRouteRaw](by-memory/0x00550f20-0x00550f76.MenuQuestionItemListLargerAddRowNoRouteRaw.md)

## Changes

- 2026-06-22 supervisor-worker Rule 26 incorporation of B013 `0003W9-MenuQuestionItemListAddRowNoRouteRaw-source-routing.md`:
  - Before: `85/86`, `CANONICAL_OWNER:NONE`, with only shallow behavior notes and stale wording that helper ownership/source names were unsafe.
  - After: `88/88`, `CANONICAL_OWNER:00007Z`, still `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, and blank formal C++.
  - Summary/evidence: B013 confirmed the exact `0x56` byte body, `thiscall` signature, `MenuQuestionItemRow` layout, `_wcscpy_s` copy contract, `ListPane::AddEntry` dependency, surrounding padding, no rel32/VA/RVA route to `0x00550dc0`, positive-control hits for neighboring live constructor/vtable targets, active constructor inline equivalents, semantic `MenuQuestionItemList` ownership, and exact no-code proof.
