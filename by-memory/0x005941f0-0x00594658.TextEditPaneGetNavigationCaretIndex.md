*** UID:0004U7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5849584 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@005941F0 | UID0000OJ accepted formal channel
enum TextNavigationAction
{
    kTextNavigationCharacterLeft = 0,
    kTextNavigationCharacterRight = 1,
    kTextNavigationVisualUp = 2,
    kTextNavigationVisualDown = 3,
    kTextNavigationWordLeft = 4,
    kTextNavigationWordRight = 5,
    kTextNavigationPageUp = 6,
    kTextNavigationPageDown = 7,
    kTextNavigationLineStart = 8,
    kTextNavigationLineEnd = 9,
    kTextNavigationDocumentStart = 10,
    kTextNavigationDocumentEnd = 11
};

short TextEditPane::GetNavigationCaretIndex(Event *event,
                                             short caretIndex) const
{
    unsigned char modifiers = event->m_payload.m_key.m_modifiers;
    unsigned char key = g_pEventMan->TranslateEventKey(
        event->m_payload.m_key.m_key, modifiers);
    int action = key - kPaneKeyLeft;
    if ((modifiers & kEventModifierControl) != 0)
        action += 4;
    if ((modifiers & kEventModifierAlt) != 0)
        action += 8;

    short textLength = GetTextLength();
    if (action == kTextNavigationCharacterLeft)
        return caretIndex > 0 ? caretIndex - 1 : 0;
    if (action == kTextNavigationCharacterRight)
        return caretIndex < textLength ? caretIndex + 1 : textLength;

    if (action == kTextNavigationVisualUp
        || action == kTextNavigationVisualDown)
    {
        Point point;
        GetCaretLocalPoint(&point, caretIndex);
        point.y += action == kTextNavigationVisualUp
            ? -GetLineHeight() : GetLineHeight();
        signed char hitPart = 0;
        return GetTextOffsetAtPoint(point.y, point.x, &hitPart);
    }

    if (action == kTextNavigationWordLeft
        || action == kTextNavigationWordRight)
    {
        TextEditRange range;
        ::FindWordBoundary(
            static_cast<const wchar_t *>(m_textStorage->GetData(0)),
            textLength,
            caretIndex,
            action == kTextNavigationWordRight,
            &range);
        return action == kTextNavigationWordRight ? range.end : range.start;
    }

    if (action == kTextNavigationPageUp
        || action == kTextNavigationPageDown)
    {
        Point point;
        GetCaretLocalPoint(&point, caretIndex);
        int lineHeight = GetLineHeight();
        int targetY;

        if (action == kTextNavigationPageUp)
        {
            targetY = m_contentRect.top;
            signed char hitPart = 0;
            short result = GetTextOffsetAtPoint(targetY, point.x, &hitPart);
            if (point.y < m_contentRect.top)
                result = GetTextOffsetAtPoint(targetY + lineHeight,
                                              point.x, &hitPart);
            if (result != caretIndex)
                return result;

            int pageHeight = m_contentRect.bottom - m_contentRect.top
                - lineHeight;
            targetY = m_contentRect.top - pageHeight;
            result = GetTextOffsetAtPoint(targetY, point.x, &hitPart);
            if (point.y < targetY)
                result = GetTextOffsetAtPoint(targetY + lineHeight,
                                              point.x, &hitPart);
            return result;
        }

        targetY = m_contentRect.bottom;
        signed char hitPart = 0;
        short result = GetTextOffsetAtPoint(targetY, point.x, &hitPart);
        if (m_contentRect.bottom < point.y + lineHeight)
            result = GetTextOffsetAtPoint(targetY - lineHeight,
                                          point.x, &hitPart);
        if (result != caretIndex)
            return result;

        int pageHeight = m_contentRect.bottom - m_contentRect.top - lineHeight;
        targetY = m_contentRect.bottom + pageHeight;
        result = GetTextOffsetAtPoint(targetY, point.x, &hitPart);
        if (pageHeight + m_scrollContentRect.bottom < point.y + lineHeight)
            result = GetTextOffsetAtPoint(targetY - lineHeight,
                                          point.x, &hitPart);
        return result;
    }

    if (action == kTextNavigationLineStart
        || action == kTextNavigationLineEnd)
    {
        short lineIndex = GetLineIndexForTextOffset(caretIndex);
        TextEditLineRecord *line = static_cast<TextEditLineRecord *>(
            m_lineTable->GetElementAt(lineIndex));
        if (action == kTextNavigationLineStart)
            return line->startIndex;

        if (lineIndex + 1 >= GetLineCount())
            return textLength;
        TextEditLineRecord *nextLine = static_cast<TextEditLineRecord *>(
            m_lineTable->GetElementAt(lineIndex + 1));
        return nextLine->startIndex < textLength
            ? nextLine->startIndex - 1 : textLength;
    }

    if (action == kTextNavigationDocumentStart)
        return 0;
    if (action == kTextNavigationDocumentEnd)
        return textLength;
    return caretIndex;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready short TextEditPane::GetNavigationCaretIndex(Event *, short) const over exact [0x005941f0,0x00594658), including the two local switch tables and complete character, word, visual-line, page, line-boundary, and document-boundary movement matrix. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005941f0-0x00594658 TextEditPane GetNavigationCaretIndex

## Status And Source Placement

- Exact source child: `[0x005941f0,0x00594658)`, 1,128 bytes, including the dispatcher's two compiler-emitted local switch tables.
- The modeled executable function is `[0x005941f0,0x00594618)` (`0x428` bytes); table storage through `0x00594658` belongs to this source switch and must remain with this child.
- Canonical owner/emitter: [UID:0000EO][TextEditPane](by-class/TextEditPane.md); source module [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Parent [UID:0001JS][0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers](by-memory/0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers.md) remains the blank aggregate index.

## Exact Binary And ABI Evidence

- Bounded analysis of the modeled function found 394 instructions, 29 basic blocks, cyclomatic complexity 11, and sole ordinary caller `TextEditPane::HandleNavigationKey`.
- Exact child SHA256, including both local tables, is `9BDB77BDBA792E599817163266220131011866188675A045101882AD0E69F00D8`.
- The member has one `Event *` and one promoted signed-short caret argument, returns a signed short, and makes const-style reads of TextEditPane state.
- Table references at `0x00594226` and `0x0059427f` are compiler lowering of the source action switch. They are not independent globals, raw helper bodies, or handwritten arrays.

## Action Derivation

- Base action is the translated Pane key minus `kPaneKeyLeft`, preserving the contiguous Left/Right/Up/Down key family.
- Control adds four and Alt adds eight, producing the exact 12-action matrix represented by `TextNavigationAction`.
- This mapping preserves observed character, visual-line, word, page, line-boundary, and document-boundary behavior without raw numeric cases in source.

## Character And Visual-Line Movement

- Character left/right clamp at zero/text length and move one UTF-16 code unit.
- Visual up/down convert the caret to a local point, move by one line height, then hit-test with the exact `(y,x,&hitPart)` order.
- The signed hit-part local is an output scratch value; the returned text offset is the navigation result.

## Word Movement

- Obtain the contiguous text pointer from `m_textStorage->GetData(0)`, pass the signed-short text length and caret to file-local `FindWordBoundary`, and select the returned start/end by direction.
- Keep the free helper call explicit. It is not a TextEditPane member, raw offset accessor, or standard-library locale tokenizer.

## Page Movement

- Page up first tests the current content top and, when needed, one-line-below correction; only if that leaves the caret unchanged does it subtract one content page less one line.
- Page down mirrors the bottom/one-line-above correction and then adds one page less one line.
- Preserve the exact `m_contentRect`/`m_scrollContentRect` relationships and signed integer arithmetic; do not normalize these into a symmetric clamp abstraction that changes branch order.

## Line And Document Boundaries

- Resolve current line through `GetLineIndexForTextOffset` and `m_lineTable->GetElementAt` using the typed `TextEditLineRecord` layout.
- Line start returns the current record start. Line end uses the next record start minus one when it lies before the text end; the last line returns text length.
- Document start/end return zero/text length. An unrecognized action returns the original caret.

## Negative And Rejected Evidence

- Rejected split-table pages: both referenced tables are compiler artifacts of this function's source switches.
- Rejected compiler-generated/no-source disposition: the method encodes application-specific editor movement policy and has a live member caller.
- Rejected raw numeric action cases: the contiguous key/modifier derivation supports a source enum.
- Rejected generic STL word/line helpers: exact calls, line records, and whitespace behavior require the project types shown in the managed body.
- Rejected signature widening: selection fields, text lengths, records, and return paths consistently use signed shorts.

## Score Rationale And History

- `91/93` reflects complete range/table ownership, CFG, sole caller, ABI, action matrix, field/type contracts, every movement branch, and complete managed source.
- The score is capped below sibling helpers because the original local enum/helper spellings and exact private const qualifier are inferred from behavior rather than symbols.
- Prior UID0001JS raw-helper treatment is superseded for this exact child and its tables only. Residual code islands remain aggregate debt.

## Rebuild Policy

- Emit only the enum and managed member body through UID0000EO/UID0000ON.
- Let the compiler regenerate jump tables, branch lowering, temporaries, and padding.
- Do not emit raw table addresses, decompiler labels, numeric offset access, handwritten ABI mechanics, or new clamps/guards.
