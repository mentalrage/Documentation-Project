*** UID:0003WR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingCategoryCollection::ParseCategoryListResponse(
    const unsigned char *packet)
{
    m_records.clear();

    short packetOffset = 4;
    const short categoryCount =
        static_cast<short>(PacketBufferReadUInt16BE(packet + 2));
    wchar_t wideTitle[256] = { 0 };

    for (short categoryIndex = 0;
         categoryIndex < categoryCount;
         ++categoryIndex)
    {
        const int categoryId =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;

        const int startDate =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int startTime =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int endDate =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int endTime =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;

        const char stateCode =
            static_cast<char>(packet[packetOffset++]);
        const unsigned char titleLength = packet[packetOffset++];

        char narrowTitle[256];
        GetMemoryMan()->MemmoveWrapper(
            narrowTitle,
            packet + packetOffset,
            titleLength);
        packetOffset =
            static_cast<short>(packetOffset + titleLength);
        narrowTitle[titleLength] = '\0';

        const short wideTitleLength =
            static_cast<short>(
                static_cast<unsigned short>(
                    MultiByteToWideChar(
                        CP_ACP,
                        0,
                        narrowTitle,
                        titleLength,
                        wideTitle,
                        256)));
        wideTitle[wideTitleLength] = L'\0';

        RankingCategoryRecord category;
        category.SetListIndex(categoryIndex);
        category.SetCategoryId(categoryId);
        category.SetStartTimeFromPackedDateTime(startDate, startTime);
        category.SetEndTimeFromPackedDateTime(endDate, endTime);
        category.SetStateCode(stateCode);
        category.SetTitleText(wideTitle);
        m_records.push_back(category);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Source-ready opcode 0x7d subcommand 1 parser; clears m_records, preserves signed-short count/cursor and CP_ACP title conversion, constructs partial-sentinel records, and retains only successful prefixes on failure. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# RankingCategoryCollection ParseCategoryListResponse `0x0045c2a0-0x0045c556`

## Status

- Disposition: source-authored ranking category-list response parser.
- Direct owner: [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md).
- Source route: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Final C++: source-ready in the formal CPP block. The existing class declaration owns the one H declaration, so this child H block remains intentionally blank.
- Exact source destination: `NexusTK/ui/dialogs/RankingDialog.cpp`; compile-visible dependencies come from [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md), `MemoryMan.h`, and the shared PacketBuffer header.
- Score: `93/93`. Exact behavior and the high-probability human source operation are closed: full zero initialization follows the observed `0x200`-byte clear, title copying follows `GetMemoryMan()->MemmoveWrapper`, and the ordinary fixed-array terminator subscript relies on the recovered retail MSVC local-array range instrumentation. Exact retail instrumentation switch/pragma spelling and independent rebuild-codegen confirmation remain the explicit caps, together with stripped retail spellings and unavailable packet-size metadata.

## Evidence

- B002 live IDA recheck reports modeled range `0x0045c2a0-0x0045c556`.
- `RankingDialog_HandleRankingPacket` dispatches opcode `0x7d` subcommand `1` to this method.
- The parser clears existing record user-entry vectors, reads a 16-bit category count at packet offset `+2`, decodes category id, start/end date-time fields, state, title length, and title bytes, converts title text, and appends `0x2b0` records.
- Exact [UID:0002K7][0x0045c050-0x0045c0f9.RankingCategoryRecordSetStartTimeFromPackedDateTime](by-memory/0x0045c050-0x0045c0f9.RankingCategoryRecordSetStartTimeFromPackedDateTime.md) callsite is `0x0045c49c`. The parser supplies the start endpoint as `packedDate` then `packedTime` before calling the paired end endpoint setter.
- Exact [UID:0002K8][0x0045c100-0x0045c1a9.RankingCategoryRecordSetEndTimeFromPackedDateTime](by-memory/0x0045c100-0x0045c1a9.RankingCategoryRecordSetEndTimeFromPackedDateTime.md) callsite is `0x0045c4b3`. The checked sequence is `push [ebp-0x5e0]`, `lea ecx, [ebp-0x5c0]`, `push [ebp-0x5e4]`, `call 0x45c100`; because explicit thiscall arguments are pushed right-to-left, K8 receives end `packedDate = [ebp-0x5e4]` and end `packedTime = [ebp-0x5e0]`.
- This method shares record/user-entry layout evidence with [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md) and [UID:0001VN][RankingCategoryRecordLayout](by-type/by-struct/RankingCategoryRecordLayout.md).

## Exact Dispatch And Wire Contract

- Dispatcher `sub_459210` first requires opcode byte `0x7d`, selects subcommand byte `1`, passes the collection facet at `dialog + 0x1cc` at callsite `0x00459243`, then resolves/invalidate-updates ranking pane index `1` when that pane exists.
- The parser clears the existing collection before reading any record count. The lowering walks `[begin,end)` at stride `0x2b0`, destroys each record's nested vector through `0x0045d5f0`, sets end equal to begin, and retains outer capacity.
- `PacketBufferReadUInt16BE(packet + 2)` is narrowed to signed `short`. Counts `1..32767` parse; zero and wire values `0x8000..0xffff` clear the old collection and append nothing.
- The packet cursor is a signed 16-bit local initialized to `4`. Every packet address sign-extends its current word value, and every fixed/payload advance narrows back to that word-sized cursor.

Per-record layout is exact and relative to the current cursor:

| Relative offset | Width | Meaning | Source behavior |
| ---: | ---: | --- | --- |
| `+0` | 4 | category id | Big-endian UInt32, narrowed to `int` and passed to `SetCategoryId`. |
| `+4` | 4 | start date | Big-endian UInt32, first argument of the start setter. |
| `+8` | 4 | start time | Big-endian UInt32, second argument of the start setter. |
| `+12` | 4 | end date | Big-endian UInt32, first argument of the end setter. |
| `+16` | 4 | end time | Big-endian UInt32, second argument of the end setter. |
| `+20` | 1 | state | Read through a `char` local and sign-extended for the `int` setter; under the recovered MSVC model `0x80..0xff` become negative states. |
| `+21` | 1 | title length | Unsigned byte, `0..255`. |
| `+22` | variable | title bytes | Exactly `titleLength` bytes, followed by the next record. |

The title bytes are copied into a 256-byte narrow local through `GetMemoryMan()->MemmoveWrapper`, terminated, and converted with `MultiByteToWideChar(CP_ACP, 0, narrowTitle, titleLength, wideTitle, 256)`. The wide local is source-level zero-initialized, matching the observed `0x200`-byte clear. The API result flows through unsigned low 16 bits and then signed `short` before the ordinary wide-array terminator subscript. Result zero therefore produces an empty title. Result 256 reaches the compiler-inserted range-check failure block at `0x0045c551` under the recovered retail local-array instrumentation; there is no authored checked wrapper, direct range-helper call, or explicit conversion-error branch.

No packet null check, packet-length check, count sanity check, title encoding check, or title conversion recovery exists. Adding any of those checks would change malformed-packet behavior and is rejected.

## RankingCategoryRecord Construction Baseline

For each parsed category, the stack `RankingCategoryRecord` is initialized with a null `m_userEntries` vector triplet at `+0x258/+0x25c/+0x260`, `m_localPlayerRank = -1` at `+0x268`, `m_localPlayerScore = -1` at `+0x2ac`, and total rank count zero at `+0x254` before the record is appended to collection storage. The category id, state, date/time fields, title, and other packet-supplied category metadata are then filled from the subcommand `1` payload.

This baseline is consistent with [UID:0002KA][0x0045c240-0x0045c256.RankingCategoryRecordResetUserEntries](by-memory/0x0045c240-0x0045c256.RankingCategoryRecordResetUserEntries.md): later page resets express `m_userEntries.clear(); m_localPlayerRank = -1;`, preserving capacity and unrelated footer/score/count state. New records begin with no allocation, while reused records retain allocation until the separate vector destructor frees it.

The physical pointer triplet is compiler layout for one source `std::vector<RankingUserEntry> m_userEntries`, not three authored members. Likewise `+0x268` is local-player rank, not a selected-user index; selected-category state belongs to the enclosing collection. These source-shape corrections preserve the raw initialization evidence without emitting decompiler-style pointer assignments.

The exact shared inlined-construction constant at `0x00610bb0` is 16 bytes `00 00 00 00 00 00 00 00 00 00 00 00 ff ff ff ff`, SHA256 `34885EA2EF8490886FAE553C8CE7FFB5A27563825E5F6CE02784875D1ACE0E47`. It initializes the nested-vector triplet and `m_loadedRowsCategoryId = -1`; its only inbound data xrefs are this parser at `0x0045c43a` and the initial-page parser at `0x0045c710`. Separate stores initialize total rank count `0`, local rank `-1`, and local score `-1`.

The source default constructor must not clear the whole record. List index, category id, state, unassigned `tm` tail fields, title tail, and the reserved `0x40` footer remain indeterminate until parser/consumer assignment. The implicit memberwise copy consequently preserves those fixed spans while deep-copying only the nested vector.

## Category-Record Append Fast And Growth Paths

- After the stack `RankingCategoryRecord` is populated, the parser compares the collection's `m_records` end and capacity pointers. Capacity available takes the in-place append path and calls [UID:0003X4][0x0045d420-0x0045d4f8.RankingCategoryRecordCopyConstructDeep](by-memory/0x0045d420-0x0045d4f8.RankingCategoryRecordCopyConstructDeep.md) at `0x0045c4f1` for one exact `0x2b0`-byte deep-copied element.
- Capacity exhausted takes the slow path and calls [UID:0003X0][0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth](by-memory/0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth.md) at `0x0045c502`. That non-emitting compiler specialization grows by one-half or to the required size, allocates aligned storage when needed, deep-copies the inserted record and surrounding range, destroys old nested vectors, and supplies strong-exception rollback.
- Both branches implement the same source operation: append the fully populated stack record to `RankingCategoryCollection::m_records`. The source should remain an ordinary `std::vector<RankingCategoryRecord>` insertion in `RankingDialog.cpp`; no call to a named growth helper belongs in recovered C++.
- The successful append advances the physical end by one `0x2b0` element. At `0x0045c514` the parser destroys the stack temporary's embedded `m_userEntries` vector after ownership has been duplicated into collection storage.
- The fast/slow branch and temporary teardown are compiler lowering of one append expression and automatic lifetime. They are regenerated by the exact formal method and class declarations; no handwritten growth/copy/destructor helper belongs in source.

## Failure And Lifetime Semantics

- The clear occurs before count/record parsing, so a failure never restores the old collection.
- Each successful append is permanent before the next record begins. A later read, range-check, conversion, title copy, record copy, or allocation failure leaves exactly the successfully appended prefix.
- The vector growth operation itself retains its standard strong guarantee: failed growth does not replace the pre-growth prefix.
- The automatic temporary record's nested vector is destroyed at `0x0045c514` after successful append. The compiler cleanup route also destroys it if an exception leaves the active temporary lifetime.
- Cleanup chunk `[0x005fa290,0x005fa29b)` has bytes `8d 8d 40 fa ff ff e9 b5 1c e6 ff`, SHA256 `93EAFBFF4D72B12D43B283C6812BB8B013D7ED170D925CD709356B7A60D12932`, an EH-map inbound ref at `0x00658ab4`, and an outbound jump to the shared tail.
- Shared tail `[0x0045bf50,0x0045bf5b)` has bytes `81 c1 58 02 00 00 e9 95 16 00 00`, SHA256 `DBD3EB5227E92B186F3502F6E90F91176F26E8248507DABBCB190CD001F41F0A`, and inbound calls from this parser's cleanup and the initial-page parser's cleanup. It adjusts to `m_userEntries` at `+0x258` and tail-jumps to nested-vector destruction.
- `SEH_45C2A0` `[0x005fa29b,0x005fa2c3)` is the 40-byte compiler handler, SHA256 `1AACAA2DF3128953E42E260FEE059829DFB1894E69B2638EAE35493132081859`, referenced from the function prologue at `0x0045c2a5`.
- `stru_658AE0` `[0x00658ae0,0x00658b04)` is the 36-byte MSVC `FuncInfo`, SHA256 `D898E13A8D42E333808FAD499D4F6ADD025BC32C10244EF9DB7D1FE97DD4D223`; it links the handler to the temporary-record unwind cleanup. These entities remain compiler-owned and non-emitting.

## Function Identity, Xrefs, And Boundaries

- Exact function range is `[0x0045c2a0,0x0045c556)`, `0x2b6` bytes, 194 instructions, and 16 CFG blocks including external EH chunks. Body SHA256 is `95AD30865B5B1B4364CC09BE4D8A789BA0B9ED5526E7429C821436DD12E66533`.
- The sole inbound code xref is dispatcher call `0x00459243`. Material outbound calls cover nested-vector destruction, UInt16BE/UInt32BE reads, direct byte access lowering, memory move, Win32 conversion, six record setters, title copy, implicit deep copy, growth, cookie checks, and range-check failure.
- Predecessor thunk is `[0x0045c290,0x0045c295)`. Prefix `[0x0045c295,0x0045c2a0)` is exactly 11 `0xcc` bytes with zero xrefs.
- Suffix `[0x0045c556,0x0045c560)` is exactly 10 `0xcc` bytes with zero xrefs. The initial-page parser begins at `0x0045c560`.
- The target has no local string, global, resource, vtable, RTTI, custom import, alternate owner, or independent helper body.

## Direct Consumers

- `GetCount` callers `0x0045b09a` and `0x0045b373` drive event-list scrollbar state and painting.
- `GetByIndex` callers `0x0045b3ab`, `0x0045b56e`, `0x0045b5c2`, and `0x0045bd74` paint, select, and inspect parsed records.
- `GetTitleText` callers `0x0045bc9d`, `0x0045bcb7`, `0x0045e4c9`, `0x0045e4e3`, `0x0045f47b`, and `0x0045f495` prove persistent wide-title use in event, reward, and user-list painting.
- Start/end callers `0x0045f4fe` and `0x0045f56f`, state callers `0x004596dd`, `0x0045bb06`, and `0x0045bb1a`, plus twelve selected-record callers prove date display, enablement, selection, reward, and row-request use.

## Heuristic / Inference Notes

- Best descriptive name: `RankingCategoryCollection::ParseCategoryListResponse`.
- Rejected: `ParseEventList` and `LoadCategories`; both hide packet-response and category-id behavior.
- Original source spelling remains unproven, but packet dispatch and field flow support the descriptive name at the current score.
- Rejected source placements are `PacketBuffer.cpp`, `RankingEventListPane`, a new `RankingCategoryCollection.cpp`, and runtime/STL helper files. Shared readers stay in PacketBuffer; this feature parser stays in `RankingDialog.cpp`.
- Rejected source shapes are unsigned/int count or cursor, UTF-8 conversion, whole-record zeroing, explicit record copy construction, manual vector growth/cleanup, and collection-level transactional rollback.

## Cross-References

- [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md)
- [UID:0001ZC][0x0045c260-0x0045d4f8.RankingCategoryCollection](by-memory/0x0045c260-0x0045d4f8.RankingCategoryCollection.md)
- [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md)
- [UID:0001VN][RankingCategoryRecordLayout](by-type/by-struct/RankingCategoryRecordLayout.md)

## Changes

- 2026-08-14 B005 UID0003WR repaired source-shape callback:
  - Replaced the predecessor direct `memmove` expression with `GetMemoryMan()->MemmoveWrapper`, preserving the observed singleton/wrapper call route while retaining the proved full `wchar_t[256]` zero initialization, ordinary indexed terminators, and ordinary `m_records.push_back` source shape.
  - Adjusted metadata from the historically applied `94/94` to `93/93`. Exact retail MSVC local-array instrumentation spelling and independent rebuild-codegen confirmation remain required before exact-runtime closure or promotion back to `94/94`.
  - Preserved owner/emitter UID0000BK, `RECONSTRUCTABLE:TRUE`, blank child position, and the intentionally blank formal H channel.

- 2026-08-14 B005 UID0003WR accepted source-quality callback:
  - Historically raised metadata from `87/90` to `94/94`, replaced the empty formal CPP with the predecessor source method, and retained the intentionally blank child H route. The repaired callback above supersedes only its score and direct-copy source shape.
  - Added exact dispatch, signed-short count/cursor, complete wire layout, signed state, CP_ACP conversion/truncation, constructor sentinel/uninitialized-span, clear/append/failure/lifetime, EH/data, hash/xref, padding, consumer, negative-evidence, source-placement, and score-cap detail.
  - Closed the compile dependency through shared PacketBuffer UInt16BE source and historicalized the prior unresolved/blank-emitter conclusion without removing earlier setter/vector evidence.

- 2026-08-06 B002 UID0003X0 accepted caller-route callback:
  - Added the exact capacity-available deep-copy call at `0x0045c4f1`, capacity-exhausted UID0003X0 call at `0x0045c502`, one-record end advance, and stack-temporary nested-vector teardown at `0x0045c514`.
  - Recorded ordinary `m_records` append as the human source cause and kept this parser's `87/90` metadata and blank formal CPP/H unchanged.

- 2026-08-04 B005 UID0002KA constructor-state callback:
  - Preserved parser metadata, exact subcommand/range/date-time evidence, owner/emitter route, and blank formal channels.
  - Added the null user-vector triplet, rank `-1`, score `-1`, total zero baseline; related it to later clear-without-free reset and separate destruction; and historicalized raw-triplet/selected-user source interpretations.

- 2026-06-20 B001 accepted K8 implementation:
  - Added exact K8 callsite `0x0045c4b3` and documented the stack argument flow proving the end endpoint is supplied as `packedDate, packedTime`.
- 2026-06-19 B015 accepted K7 implementation:
  - Added exact K7 callsite `0x0045c49c` and documented that this parser supplies the start endpoint as `packedDate, packedTime`.
- 2026-06-16 B002/supervisor split:
  - Created exact child for opcode `0x7d` subcommand `1` parser evidence.
