*** UID:00047O | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserListPane::DrawUserEntry(int itemIndex,
                                 const UserListRecord *record,
                                 const RectBounds *bounds)
{
    m_textDrawMode = 1;
    SetDrawColor(37);

    const RectBounds rowBounds = *bounds;
    RectBounds iconBounds;
    InitRectBounds(&iconBounds, 0, 0, 0, 0);

    if (record->huntersListFlag == 1)
    {
        EPFTileContext &frame = m_huntersListIconFrame;
        const int frameWidth = frame.bounds.right - frame.bounds.left;
        const int frameHeight = frame.bounds.bottom - frame.bounds.top;

        iconBounds.left = 0;
        iconBounds.top = (rowBounds.top + rowBounds.bottom) / 2 - frameHeight / 2;
        iconBounds.right = iconBounds.left + frameWidth;
        iconBounds.bottom = iconBounds.top + frameHeight;

        g_pfnBlitSprite(this,
                        &frame,
                        &frame.bounds,
                        &iconBounds,
                        1,
                        NULL,
                        NULL);
    }

    if (record->statusIconId >= 1 && record->statusIconId <= 4)
    {
        EPFTileContext &frame = m_statusIconFrames[record->statusIconId - 1];
        const int frameWidth = frame.bounds.right - frame.bounds.left;
        const int frameHeight = frame.bounds.bottom - frame.bounds.top;

        iconBounds.left = 12;
        iconBounds.top = (rowBounds.top + rowBounds.bottom) / 2 - frameHeight / 2;
        iconBounds.right = iconBounds.left + frameWidth;
        iconBounds.bottom = iconBounds.top + frameHeight;

        g_pfnBlitSprite(this,
                        &frame,
                        &frame.bounds,
                        &iconBounds,
                        1,
                        NULL,
                        NULL);
    }

    RectBounds badgeBounds;
    InitRectBounds(&badgeBounds, 0, 0, 0, 0);
    if (record->badgeIconId >= 1 && record->badgeIconId <= 4 &&
        record->bucketIndex >= 1 && record->bucketIndex <= 4)
    {
        const int frameIndex =
            (record->bucketIndex - 1) * 4 + (record->badgeIconId - 1);
        EPFTileContext &frame = m_badgeIconFrames[frameIndex];
        const int frameWidth = frame.bounds.right - frame.bounds.left;
        const int frameHeight = frame.bounds.bottom - frame.bounds.top;

        badgeBounds.left = 24;
        badgeBounds.top = (rowBounds.top + rowBounds.bottom) / 2 - frameHeight / 2;
        badgeBounds.right = badgeBounds.left + frameWidth;
        badgeBounds.bottom = badgeBounds.top + frameHeight;

        g_pfnBlitSprite(this,
                        &frame,
                        &frame.bounds,
                        &badgeBounds,
                        1,
                        NULL,
                        NULL);
    }

    unsigned int textColor = record->displayColor;
    if (IsItemSelected(itemIndex))
    {
        SetDrawColor(record->displayColor);
        g_pfnFillRect(this, bounds);
        textColor = 128;
    }
    SetTextColor(textColor);

    for (int index = 0; index < 20; ++index)
    {
        if (_wcsicmp(g_pConfig->m_profileStringSlots[index].c_str(),
                     record->name) == 0)
        {
            SetTextColor(9);
        }
    }

    const int nameLength = static_cast<int>(wcslen(record->name));
    const int nameWidth = GetTextWidth(record->name, nameLength);
    MoveTo(rowBounds.right - nameWidth,
           (rowBounds.top + rowBounds.bottom) / 2 + 6);
    DrawWideText(record->name, nameLength);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready three-argument UserListPane::DrawUserEntry renders hunters, status, and bucket/badge EPFTileContext frames with exact row-centered geometry; fills selected rows, applies display/selected/friend-name text colors, and right-aligns the packed UserListRecord name through current GrafPort, Surface callback, ListPane, Config, and text helper contracts. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0059ea90-0x0059ed18 - UserListPaneDrawUserEntry

## Status

- Entity kind: source-authored `UserListPane` row draw method.
- Address range: `0x0059ea90-0x0059ed18`.
- Owner: [UID:0000FO][UserListPane](by-class/UserListPane.md) `UserListPane`.
- Source placement/emitter: [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md) `UserListDialogPane`.
- Rebuild handling: emit the complete three-argument source body above; exclude only compiler lowering.

## Behavior And Evidence

Fresh MCP analysis models one exact function at `[0x0059ea90,0x0059ed18)`, size `0x288` (648 bytes), 237 instructions, sixteen blocks, and complexity nine. Exact bytes have SHA256 `84C846F8D3218CEA450A252849AC41B354E82DE2F408C5E3585A96263B5E2ECC`; the shortest unique wildcarded entry signature is `55 8B EC 83 EC 44 A1 ? ? ? ? 33 C5 89 45 ? A1`. Physical `retn 0x0c` and stack use prove three explicit arguments after `this`: full-width item index, `const UserListRecord *record`, and `const RectBounds *bounds`. Its only inbound route is primary vtable cell `0x0062eb5c`; zero ordinary callers is expected virtual reachability.

Entry sets inherited `m_textDrawMode = 1`, calls `SetDrawColor(37)`, copies the row bounds, and initializes local icon bounds. The packed record and complete class provide the exact frame consumers:

- `UserListRecord::statusIconId` at `+0x00c` indexes the four-frame `m_statusIconFrames` bank.
- `UserListRecord::badgeIconId` at `+0x003`, together with `bucketIndex` at `+0x001`, selects from the sixteen-frame `m_badgeIconFrames` bank.
- `UserListRecord::huntersListFlag` at `+0x20e` gates the final `m_huntersListIconFrame` loaded by UID00047H from `SYMBOLS.EPF` frame 223 in EPF mode.

The hunters branch requires `huntersListFlag == 1`, places the frame at left zero, and vertically centers it. Status accepts only ids 1-4 and uses left 12. Badge accepts badge and bucket ids 1-4, indexes `(bucketIndex-1)*4+(badgeIconId-1)`, and uses left 24. Each branch derives width/height from `EPFTileContext::bounds` and calls typed `g_pfnBlitSprite` with the full frame bounds, row-local destination, mode one, null palette, and null options. No crop, scaling, alpha option, palette lookup, selection frame, or resource load occurs here.

`ListPane::IsItemSelected(itemIndex)` controls selection. Selected rows set the fill color to `record->displayColor`, fill the supplied bounds through `g_pfnFillRect`, and use text color 128; unselected rows use `displayColor` directly and do not fill. The method then compares `record->name` case-insensitively against all twenty `g_pConfig->m_profileStringSlots` entries. Any match sets text color nine; the loop intentionally does not break. It calculates the counted name width, moves to `x = right - width`, `y = vertical center + 6`, and draws the complete name.

The current packed `0x414` `UserListRecord`, GrafPort helpers, sprite/fill callbacks, ListPane selection API, `Config::m_profileStringSlots[20]`, `g_pConfig`, RectBounds, and EPFTileContext declarations cover every expression. The former `m_selectionFrame` spelling is superseded: selection is inherited ListPane state, while the final context is hunters-list decoration loaded from `SYMBOLS.EPF` frame 223 only in EPF mode.

Predecessor UID00047N ends at `0x0059ea8f`, leaving one alignment byte before this target. Eight successor `0xcc` bytes occupy `[0x0059ed18,0x0059ed20)` before UID00047P. No split, merge, padding move, child, rename, or nesting change is needed.

## Source Reconstruction And Rejected Alternatives

The managed formal preserves all observed branches and uses only current declared APIs and source-facing fields. Rejected alternatives are the stale two-argument declaration; a selection-frame alias; raw offsets or a synthetic record overlay; a duplicate Config field or RegistryConfig cast; palette/crop/scaling behavior; stopping the friend loop early; and handwritten stack-cookie/decompiler/vtable mechanics. The historical blank-body/API-blocker claim is superseded by the complete direct audit. Score `92/94` reflects exact body, ABI, range, route, types, and source shape while reserving confidence for stripped original local/private names.

## Related Docs

- [UID:0001KI][0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane](by-memory/0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane.md) Parent split/index page.
- [UID:0000FO][UserListPane](by-class/UserListPane.md) Class owner.
- [UID:00047H][0x0059e710-0x0059e899.UserListPaneConstructor](by-memory/0x0059e710-0x0059e899.UserListPaneConstructor.md) Exact frame construction/resource source.

## Changes

- 2026-07-20 B005 UID00047H expanded callback:
  - Raised only `86/90 -> 92/94`, preserved UID/path/title/range, owner UID0000FO, reconstructable true, blank position, `Nested:0`, and set emitter UID0000OZ.
  - Applied the exact three-argument formal and Item Summary; incorporated exact bytes/hash/signature, 237-instruction/16-block/complexity-9 facts, ABI/vtable route, packed record fields, icon predicates/indexes/geometry, callbacks, selection/friend/text order, support declarations, padding, negative/rejected evidence, and score rationale.
  - Superseded the historical blank-body and two-argument assumptions without deleting constructor-callback provenance.

- 2026-07-20 B005 UID00047H accepted support synchronization:
  - Preserved all UID00047O metadata, score `86/90`, range/title, owner UID0000FO, reconstructable true, blank emitter/position/formal, `Nested:0`, rendering behavior, and independent direct-audit disposition.
  - Added exact status/four-frame, badge-and-bucket/sixteen-frame, and hunters-flag/final-frame consumer mappings; replaced only the stale selection alias with `m_huntersListIconFrame` and linked it to UID00047H's EPF-only SYMBOLS frame 223 load. At that bounded stage the independent draw audit remained pending; the expanded callback above supersedes that interim blank-body disposition.
