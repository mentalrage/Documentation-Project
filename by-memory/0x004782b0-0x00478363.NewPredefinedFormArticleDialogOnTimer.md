*** UID:0002UM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool NewPredefinedFormArticleDialog::OnTimer(
    int timerId, int, int)
{
    if (timerId == 0 && IsScreenDimmerActive())
    {
        ReleaseScreenDimmer();
        new TransferReplyPredefinedAlert(false,
            L"Your post didn't go through due to an error.",
            this);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `NewPredefinedFormArticleDialog::HandleEvent` class-emitted event handler that releases active ScreenDimmer state on the null-event path and opens `TransferReplyPredefinedAlert` with the fixed post-error message. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004782b0-0x00478363 NewPredefinedFormArticleDialog OnTimer

## Status

- Disposition: reconstructable NexusTK dialog method.
- Entity kind: modeled virtual event handler.
- Parent aggregate: [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md).
- Direct parent: [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
- Likely source file: [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md).
- Rebuild handling: `source-authored`.
- Final C++: source-ready first-draft C++ is entered in the formal reconstruction block and emits through [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).

## Assignment Gate

`AUTOGEN_PARENT_UID` is assigned to [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), not directly to the file page. This method is a vtable-backed class method in the NewPredefinedFormArticleDialog table: live IDA MCP `trace_data_flow` on 2026-06-13 reports the only inbound edge to `0x004782b0` is data slot `0x006140e8`, whose dword bytes are `b0 82 47 00`.

The child now clears `90/92`, the direct class parent is already `85/86`, and the file parent is already `88/85`, so the corrected `85/85` child/direct-parent gate is satisfied. The adjacent unreferenced helper [UID:0002UR][0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload](by-memory/0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload.md) already uses the same direct class parent, making this routing consistent across the alert-construction portion of the class.

## Address Range

`0x004782b0-0x00478363` end-exclusive.

Live IDA MCP on 2026-06-13 reports `sub_4782B0` at `0x004782b0`, size `0xb3` / 179 bytes (Verified with int_convert.py), with terminal `retn 0Ch` at `0x00478360`. The return pops `0x0c` / 12 bytes (Verified with int_convert.py), matching the three explicit stack arguments after `this`.

Boundary bytes are exact:

| Range | Evidence |
| --- | --- |
| `0x004782a2-0x004782b0` | Fourteen `0xcc` bytes before the function start (`0x0e` / 14, Verified with int_convert.py). |
| `0x00478350-0x00478363` | SEH/security-cookie restore tail ending in `c2 0c 00` at `0x00478360`. |
| `0x00478363-0x00478370` | Thirteen `0xcc` bytes before [UID:0002UN][0x00478370-0x00478379.NewPredefinedFormArticleDialogSetHoverControl](by-memory/0x00478370-0x00478379.NewPredefinedFormArticleDialogSetHoverControl.md) (`0x0d` / 13, Verified with int_convert.py). |

## Behavior

This handler is the post/transfer failure path for predefined-form article dialogs. It ignores non-null event blocks. For a null event block, the compiler-adjusted view first loads `[adjusted_this + 0x1cc]`, but the called predicate/release helpers ignore `ecx` and operate on the global ScreenDimmer state `g_pScreenDimmer`. When `IsScreenDimmerActive()` reports active, the handler calls `ReleaseScreenDimmer()` and opens a `TransferReplyPredefinedAlert` with reply/action mode `false`, the fixed wide message `Your post didn't go through due to an error.`, and this dialog as owner.

The method always returns `1`, including when the event block is non-null, when the shared predicate is false, or when alert allocation fails.

## Raw Code Evidence

IDA decompilation and disassembly on 2026-06-13 show this lowering. Raw labels are kept only where they identify exact binary addresses; source-facing helper/global names are listed below.

```asm
004782D6  cmp     [ebp+Block], 0
004782DA  jnz     loc_47834E
004782DC  mov     ecx, [edi+1CCh]
004782E2  call    0x004a1380        ; IsScreenDimmerActive()
004782E7  test    al, al
004782E9  jz      loc_47834E
004782EB  mov     ecx, [edi+1CCh]
004782F1  call    0x004a1360        ; ReleaseScreenDimmer()
004782F6  push    274h
004782FB  call    0x004f4aa0        ; operator new wrapper
00478313  push    0
00478315  push    offset 0x00613a18 ; shared OK label
0047831A  lea     ecx, [edi-0A4h]
00478320  push    ecx
00478321  push    offset 0x00614990 ; fixed failure message
00478326  mov     ecx, esi
00478328  call    0x0049feb0        ; AlertPane::AlertPane
0047832D  mov     dword ptr [esi], offset ??_7TransferReplyPredefinedAlert@@6B@
00478333  mov     dword ptr [esi+0A0h], offset ??_7TransferReplyPredefinedAlert@@6B@_0
0047833D  mov     dword ptr [esi+0A4h], offset ??_7TransferReplyPredefinedAlert@@6B@_1
00478347  mov     byte ptr [esi+270h], 0
0047834E  mov     al, 1
00478360  retn    0Ch
```

The formal C++ block above is the source-facing representation of this lowering. The observed allocation/null check, `AlertPane::AlertPane` call, three `TransferReplyPredefinedAlert` vtable stores, and byte store at alert `+0x270` are constructor lowering for `new TransferReplyPredefinedAlert(false, fixedMessage, this)`.

## Touched State And Data

| Address or offset | Role |
| --- | --- |
| `this + 0x1cc` | Adjusted compiler-view load before `0x004a1380` and `0x004a1360`. `0x1cc` is 460 bytes (Verified with int_convert.py). The helpers ignore `ecx` and operate on `g_pScreenDimmer`; source C++ therefore uses free/global ScreenDimmer helpers rather than exposing this raw offset. |
| `this - 0xa4` | Adjusted-view correction back to the full `NewPredefinedFormArticleDialog` owner passed into `AlertPane::AlertPane`; `0xa4` is 164 bytes (Verified with int_convert.py). Source C++ passes `this` to the `TransferReplyPredefinedAlert` constructor. |
| `0x00614990` | UTF-16LE fixed alert message. `get_string` decodes it as `Your post didn't go through due to an error.`; raw bytes begin `59 00 6f 00 ... 2e 00 00 00`. |
| `0x00613a18` | Shared UTF-16 `OK` label / dialog OK text passed through the alert constructor path; raw bytes are `4f 00 4b 00 00 00 00 00`. The source constructor supplies this internally, so the target C++ does not expose the raw symbol. |
| `0x00614194`, `0x006141fc`, `0x0061422c` | `TransferReplyPredefinedAlert` primary, secondary, and tertiary vtable bases written at alert offsets `+0x00`, `+0xa0`, and `+0xa4`; `0xa0` is 160 and `0xa4` is 164 bytes (Verified with int_convert.py). |
| alert `+0x270` | Reply/action mode byte set to `0`; `0x270` is 624 bytes (Verified with int_convert.py). |

## Callers And Callees

- `xrefs_to 0x004782b0` reports exactly one data reference, vtable slot `0x006140e8`; there are no ordinary direct callers, as expected for this virtual event handler.
- `callees 0x004782b0` reports only `0x004a1380`, `0x004a1360`, `0x004f4aa0`, and `0x0049feb0`.
- `0x004a1380` is source-facing `IsScreenDimmerActive()` / alias `HasScreenDimmer()`, a global ScreenDimmer predicate over `g_pScreenDimmer`.
- `0x004a1360` is source-facing `ReleaseScreenDimmer()` / alias `DestroyScreenDimmer()`, the paired global ScreenDimmer release helper.
- `0x004f4aa0` is the MemoryMan/global `operator new` wrapper that allocates `0x274` / 628 bytes (Verified with int_convert.py) for the alert object.
- `0x0049feb0` is the shared `AlertPane::AlertPane` constructor helper; sibling pages [UID:0002UQ][0x00478940-0x00478a93.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlert](by-memory/0x00478940-0x00478a93.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlert.md) and [UID:0002UR][0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload](by-memory/0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload.md) use the same OK label and vtable family for packet-driven reply alerts.

## Source-Facing Names

- `NewPredefinedFormArticleDialog::HandleEvent` is the emitted method name.
- `void *eventBlock` is the safe source-facing type/name for the first stack argument because the body only null-tests it; the other two stack arguments remain unnamed until the event-system type pass recovers original names.
- `IsScreenDimmerActive()` and `ReleaseScreenDimmer()` are used in the formal C++ for the `0x004a1380`/`0x004a1360` helper pair over `g_pScreenDimmer`.
- `new TransferReplyPredefinedAlert(false, L"Your post didn't go through due to an error.", this)` is the source-level form of the allocation, alert-base constructor call, vtable stores, and `+0x270` byte store.

## Sibling And Alert-Family Evidence

`analyze_component` on 2026-06-13 compared this method with `0x00478940`, `0x00478aa0`, and `0x004790a0`. The three NewPredefinedFormArticleDialog alert-construction helpers share the UTF-16 `OK` label at `0x00613a18` and the `TransferReplyPredefinedAlert` vtable bases at `0x00614194`, `0x006141fc`, and `0x0061422c`. The packet handlers at `0x00478940` and `0x00478aa0` additionally decode server-supplied text; this method instead uses the fixed failure string at `0x00614990`.

`xrefs_to 0x00614990` reports two users of the same fixed failure string: `0x00477291` in the earlier `NewArticleDialog` handler and `0x00478321` in this predefined-form handler. That duplicate pattern supports interpreting this method as the predefined-form counterpart of the ordinary article post-failure handler, not as a generic alert helper.

## Reconstructed C++

Formal first-draft C++ is emitted in the metadata block above. Original event typedef/private-member spellings remain below final-audit confidence, but they do not block this target-level body.

## Open Question Closure

No target-level open question remains that blocks [UID:0002UM] C++ insertion or the `90/92` score. Remaining original typedef/private spelling uncertainty only caps final-audit confidence.

| Former open question | Closure for [UID:0002UM] | Score/C++ impact |
| --- | --- | --- |
| Event parameter type / raw `Block` name | The first stack argument is only null-tested, so `void *eventBlock` is the safe source-level placeholder. The two other stack arguments are unused and remain unnamed. | No remaining C++ blocker; confidence stays below `95` because the original typedef/name is not recovered. |
| `this + 0x1cc` state name and DialogSession ambiguity | The adjusted compiler view loads the dialog/session field before calls, but `0x004a1380` and `0x004a1360` ignore `ecx` and operate on `g_pScreenDimmer`. Source C++ uses free/global ScreenDimmer helpers. | No remaining C++ blocker; the raw adjusted-view fact is preserved in touched state. |
| Adjusted owner pointer `this - 0xa4` | This is the compiler's adjusted-view correction back to the full `NewPredefinedFormArticleDialog` owner passed into `AlertPane`. | No remaining C++ blocker; source C++ passes `this`. |
| `0x00613a18` role | Raw bytes and support docs identify a shared UTF-16 `OK` label / dialog OK text, not a callback table. | No remaining C++ blocker; the constructor path supplies the OK label internally. |
| Alert initializer / manual vtable stores | The allocation, `AlertPane::AlertPane` call, three predefined-alert vtable stores, and `+0x270` byte store are constructor lowering for `new TransferReplyPredefinedAlert(false, fixed message, this)`. | No remaining C++ blocker. |
| Exact original helper spelling | `IsScreenDimmerActive()` and `ReleaseScreenDimmer()` are accepted source-facing names for this emission; aliases can remain in prose. | No target C++ blocker; confidence remains `92` rather than final-audit. |

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `90` | The page now records exact bounds, predecessor/successor padding, vtable slot, branch behavior, raw instruction flow, decoded fixed error string, shared OK label, ScreenDimmer helper names, allocation size, vtable writes, reply-mode store, callee set, touched state, sibling alert-family comparison, direct class-parent assignment, formal C++ body, and explicit target open-question closure. |
| Confidence `92` | Active MCP evidence confirms the range, only inbound vtable/data xref `0x006140e8`, raw bytes, decompilation, disassembly, shared data refs, fixed string, and callee set. Confidence stays below final-audit levels only because original event typedef spelling, unused parameter names, and broader class/private declaration spellings are not recovered. |

## Cross-References

- [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md)
- [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md)
- [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md)
- [UID:0000F6][TransferReplyPredefinedAlert](by-class/TransferReplyPredefinedAlert.md)
- [UID:0002UQ][0x00478940-0x00478a93.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlert](by-memory/0x00478940-0x00478a93.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlert.md)
- [UID:0002UR][0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload](by-memory/0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload.md)
- [UID:000132][0x004a0f40-0x004a14a7.BulletinSessionDialogStack](by-memory/0x004a0f40-0x004a14a7.BulletinSessionDialogStack.md)
- [UID:000131][0x004a0d80-0x004a15f8.DialogSessionCore](by-memory/0x004a0d80-0x004a15f8.DialogSessionCore.md)

## Changes

- 2026-08-14 B004 UID0000LT implementation callback: renamed this UID-preserving page from `NewPredefinedFormArticleDialogHandleEvent` to `NewPredefinedFormArticleDialogOnTimer`, raised `90/92` to `94/94`, and replaced the generic-event draft with exact `bool OnTimer(int timerId, int, int)` source. The tertiary TimerHandler vtable slot `0x006140e8`, `this+0xa4` adjusted facet, timer id `0`, screen-dimmer release, fixed failure alert, and unconditional true return resolve the historical event-block interpretation. Prior `HandleEvent(void *)` analysis remains above as superseded history, not current source authority.

- 2026-06-25 B005 implementation callback for the accepted [UID:0002UM] source-quality report: raised score from `86/90` to `90/92`, inserted formal `NewPredefinedFormArticleDialog::HandleEvent(void *eventBlock, int, int)` C++, kept owner/reconstructable/emitter route unchanged through [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), synchronized source-facing ScreenDimmer/AlertPane/operator-new/OK-label names, preserved MCP evidence for `0x006140e8`, `0x00614990`, `0x00613a18`, and `0x00614194`/`0x006141fc`/`0x0061422c`, and closed target-level C++ blockers.
- 2026-06-13 Agent-A001 Goal 2 target repair:
  - Before: `76/84`, direct parent [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md), and only a short behavior summary plus stale generated string label.
  - After: raised to `86/90` and changed `AUTOGEN_PARENT_UID` to [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
  - Summary/evidence: live IDA MCP session `a001_goal2_0002UM` confirmed `sub_4782B0` size `0xb3` / 179 bytes (Verified with int_convert.py), vtable slot `0x006140e8`, exact `0xcc` padding before/after, decoded UTF-16 message at `0x00614990`, branch flow through `0x004a1380`/`0x004a1360`, allocation `0x274` / 628 bytes (Verified with int_convert.py), alert vtable stores at `0x00614194`, `0x006141fc`, and `0x0061422c`, reply-mode byte store at `+0x270`, no ordinary direct callers, and sibling alert-construction evidence shared with `0x00478940`/`0x00478aa0`.
