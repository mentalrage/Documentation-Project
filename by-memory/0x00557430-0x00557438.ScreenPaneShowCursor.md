*** UID:0004RU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:140 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScreenPane::ShowCursor()
{
    ::ShowCursor(TRUE);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready zero-argument ScreenPane visibility command that invokes Win32 ShowCursor(TRUE); exact ABI rejects the former boolean-member spelling and incidental API return. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00557430-0x00557438 ScreenPane ShowCursor

## Status

- Disposition: source-authored non-virtual `ScreenPane` method.
- Source placement: [UID:0000CB][ScreenPane](by-class/ScreenPane.md), emitted through [UID:0000NB][ScreenPane](by-file/ScreenPane.md) at position 140.
- Parent index: [UID:0001G7][0x00557380-0x00557467.ScreenCursorHelpers](by-memory/0x00557380-0x00557467.ScreenCursorHelpers.md).
- The historical `ShowCursor(true)` member form is explicitly superseded.

## Address Range And Boundary

- Modeled function: `0x00557430-0x00557439` end-exclusive; inclusive filename end `0x00557438`.
- Exact size: 9 bytes.
- Exact code SHA256: `668818297E121EE134E5288A3608F95E33968C18A94202235A4FAAA1671ABDF7`.
- Four `0xcc` bytes precede it at `0x0055742c-0x00557430`; seven `0xcc` bytes follow at `0x00557439-0x00557440`.

## Signature And ABI

- Source return: `void`; all eight callers ignore the USER32 return left in EAX.
- Receiver: ScreenPane in ECX by source/caller context, although the tiny body does not dereference it.
- Explicit arguments: none. The function pushes its own TRUE constant and returns with plain `retn`.
- Calling convention: ordinary MSVC `__thiscall` member with unused receiver.
- No vtable or data xref targets this method.

## Behavior And Raw Evidence

- Exact body is three instructions in one basic block: push TRUE, call imported USER32 ShowCursor, return.
- Import slot is `0x0060d448`.
- The source body qualifies `::ShowCursor` to distinguish the Win32 API from this member and avoid recursion.
- The process display count semantics and API return are intentionally not modeled because the source callers issue a command and consume no result.

## Caller Inventory

Direct callers are `0x004695fd`, `0x004a7318`, `0x004a7370`, `0x004f5094`, `0x004f5f79`, `0x004f6458`, `0x0058b0cb`, and `0x005c4175`.

- Application startup missing-logo fallback at `0x004f6458` loads the canonical ScreenPane receiver and calls this zero-argument entry.
- BaramApp startup-logo completion callback at `0x004f5f79` uses the same entry.
- LogoPane advance at `0x004f5094` uses the same entry.
- Branch-paired call sites select this method versus the adjacent HideCursor method without passing a boolean.

## Ownership And Source Placement

- ScreenPane ownership follows the canonical receiver at callers and the paired source interface used by ScreenPane consumers.
- A free wrapper is less plausible because callers consistently preserve/load the root object before the call.
- CursorManager ownership is rejected because there is no separate object or source lifetime.
- The method emits after SetActiveCursor and before HideCursor in ScreenPane.cpp.

## Rejected Alternatives And Negative Evidence

- One `ShowCursor(bool)` method is ABI-impossible: this function consumes no stack parameter and has a separate adjacent FALSE function.
- Returning int/bool would expose the Win32 display count or truthiness, but no caller consumes it.
- Inlining is rejected because eight call sites target one out-of-line address.
- No null check, receiver dereference, branch, EH, compiler wrapper, or padding lies inside the range.

## Score Rationale

- Completion `93`: exact bytes/hash/bounds/import/callers/ABI/source body/ownership/padding and rejected overload are documented.
- Confidence `94`: behavior and split are exact; only original private spelling is unavailable.

## Changes

- 2026-07-15 B002 UID0001G7 callback: created as the exact no-argument TRUE visibility child and synchronized against the adjacent HideCursor ABI.
