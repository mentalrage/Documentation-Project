*** UID:0000ZZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChangeMan::DispatchMessage(LObject *owner, Message *message)
{
    ChangeManEntry key;
    key.owner = owner;

    int index = m_changeList->FindFirstEqual(&key);
    if (index < 0)
        return;

    int count = m_changeList->GetCount();
    for (; index < count; ++index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry->owner != owner)
            break;

        if (entry->allMessages == true ||
            (entry->allMessages == false &&
             entry->messageType == message->m_type)) {
            entry->listener->OnChangeMessage(owner, message);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChangeMan::DispatchMessage method; lower-bounds by LObject owner, scans contiguous ChangeManEntry rows, applies bool allMessages/MessageType filtering, and calls LObject::OnChangeMessage through primary vtable slot +0x08. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047ee20-0x0047ee95 ChangeMan DispatchMessage

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: method
- Owner class: [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- Likely source module: [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- Autogen parent: attached to [UID:00001K][ChangeMan](by-class/ChangeMan.md) at position `40`; first-draft formal C++ is populated under the current combined-score/emitter gate.
- Confidence: strong for behavior, medium for final method name.

## Behavior

`ChangeMan::DispatchMessage` lower-bounds the registration list by owner pointer and scans same-owner entries. For each matching entry, it dispatches when either the entry is registered for all messages or its stored message type matches `Message::m_type` at `message + 0x04`.

Matching `LObject` listeners are called through the common virtual `OnChangeMessage(owner, message)` slot at primary-vtable offset `+0x08`.

## 2026-07-13 B003 UID0000ZW Family Synchronization

- The method remains exactly `0x0047ee20-0x0047ee95`, with the same owner lower-bound, cached count, contiguous-owner stop, message test, callback, and index progression. The eight bytes at `0x0047ee18-0x0047ee20` remain parent-only alignment before this child.
- `owner` and `entry->listener` are `LObject *`. The listener call has no secondary-interface adjustment and targets primary slot `+0x08`, which is [UID:0000WM][0x0041b6c0-0x0041b6c3.LObjectDefaultNoOpVirtual](by-memory/0x0041b6c0-0x0041b6c3.LObjectDefaultNoOpVirtual.md). Confirmed AboveFrame, FramePartPane, MapPane, and VideoPlayerPane overrides share that slot and `(LObject *, Message *)` shape.
- `entry->allMessages == true` dispatches unconditionally; `entry->allMessages == false` dispatches only when `entry->messageType == message->m_type`. Invalid one-byte values remain documented negative evidence and do not justify a source enum.
- The sole direct caller is the adjacent inherited `LObject::ForwardMessage(Message *)` wrapper. Current Application callers are inherited uses, not evidence that this ChangeMan method or wrapper is Application-owned.
- Owner/emitter UID00001K, position 40, `Nested:0`, ChangeMan.cpp route, and exact binary behavior are preserved. The former `void *`, `ChangeListener`, `scope`, and provisional-callback statements are historical source drafts superseded by the complete callback-family proof.

## Historical B002 2026-06-16 Source-Quality Recheck

B002 rechecked this method as part of the ChangeMan method source-quality audit. The method remains direct [UID:00001K][ChangeMan](by-class/ChangeMan.md) class code emitted through the ChangeMan source unit and reached through [UID:0000QI][g_pChangeMan](by-global/g_pChangeMan.md). At that historical stage, [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) was described from its Application call site; the complete caller set now proves it is an inherited LObject wrapper, while Application remains only a caller surface.

The historical source-facing draft was `ChangeMan::DispatchMessage(void *owner, Message *message)`. It lower-bounded the [UID:0000DF][SortedList](by-class/SortedList.md) registration list with the owner key, walked contiguous [UID:0001TV][ChangeManEntry](by-type/by-struct/ChangeManEntry.md) records while `owner` matched, dispatched records with byte `scope == 1`, dispatched records with `scope == 0` only when `entry->messageType == Message::m_type`, skipped other byte values, and called virtual slot `+0x08` with `(owner, message)`. Those exact binary facts remain valid; only the draft's `void *`, `scope`, and `ChangeListener` source vocabulary is superseded by the current managed `LObject *`, `bool allMessages`, and `LObject::OnChangeMessage` model above.

The Rule 26 closeout preserves B002's instruction-level checks: `0x0047ee33` loads `this->changeList`, `0x0047ee37` calls the first-equal helper at `0x004f3780`, `0x0047ee4a` reads `changeList->count`, `0x0047ee5b` fetches the indexed entry through virtual slot `+0x10`, `0x0047ee67` reads the boolean storage byte, `0x0047ee70` compares `entry->messageType` against `message->m_type`, and `0x0047ee7a` calls listener slot `+0x08`. `MessageHandler`, `OnNotify`, and raw `slot8`/`func_` callback names remain rejected; the complete callback family supports the current descriptive `OnChangeMessage` spelling.

At that historical stage the score rose first to `86/90` and then to `88/91` as behavior, reachability, field usage, and the initial interface draft were documented. The 2026-07-13 synchronization supersedes those source-type blockers and implements `92/94` with the exact current formal block above.

## Raw Code Evidence

IDA raw disassembly on 2026-05-30 confirms:

- `0x0047ee37` calls `sub_4F3780(m_changeList, &key)` to find the first owner entry.
- `0x0047ee4a` reads the current list count from `m_changeList + 0x0c`.
- `0x0047ee5b` calls the list vtable slot `+0x10` to fetch the current entry.
- `0x0047ee63-0x0047ee65` stops the scan when `entry.owner != owner`.
- `0x0047ee67-0x0047ee78` dispatches when `entry.scope == 1`, or when `entry.scope == 0` and `entry.messageType == message->m_type`.
- `0x0047ee7a-0x0047ee81` calls the listener virtual slot `+0x08` with `(owner, message)`.
- `0x0047ee84-0x0047ee8a` increments the index after either a callback or a skipped nonmatching message record.

## Historical C++ Reconstruction Candidate Before 2026-07-13

This pre-resolution candidate is retained for its exact loop ordering only. Its `void *`, `scope`, and `ChangeMessageScope` vocabulary is not current source C++.

```cpp
void ChangeMan::DispatchMessage(void *owner, Message *message)
{
    ChangeManEntry key;
    key.owner = owner;

    int index = m_changeList->FindFirstEqual(&key);
    if (index < 0)
        return;

    int count = m_changeList->GetCount();
    for (; index < count; ++index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry == NULL || entry->owner != owner)
            break;

        if (entry->scope == ChangeMessageScope_AllTypes ||
            (entry->scope == ChangeMessageScope_MatchType &&
             entry->messageType == message->m_type)) {
            entry->listener->OnChangeMessage(owner, message);
        }
    }
}
```

At that historical stage, the callback spelling, enum spelling, list method names, and owner type were provisional. The current family/caller proof resolves `LObject *owner`, `bool allMessages`, and `LObject::OnChangeMessage`; invalid byte states remain binary negative evidence rather than a source-enum requirement.

## 2026-07-01 B009 Empty-Emitter Implementation

The accepted B009 report promoted that historical reconstruction candidate into the formal block at that time. The 2026-07-13 implementation replaced it with the current managed LObject/bool block without changing the range or loop behavior. Live MCP session `supervisor_resume_20260629` reconfirmed `lookup_funcs 0x0047ee20` as size `0x75`, the sole direct xref at `0x004f4b7d` inside [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md), and the `SortedList::FindFirstEqual` callee at `0x004f3780`.

Bounded disassembly preserves the exact loop shape used by the formal C++: `0x0047ee37` calls the first-equal helper, `0x0047ee4a` reads count at list `+0x0c`, `0x0047ee5b` fetches an entry through slot `+0x10`, `0x0047ee63-0x0047ee65` breaks on owner mismatch, `0x0047ee67-0x0047ee78` accepts only scope byte `1` or scope byte `0` with `entry->messageType == message->m_type`, `0x0047ee81` calls the listener slot `+0x08`, and `0x0047ee87` increments the index for both dispatched and skipped records. Application, BackPane, SortedList, no-owner, and raw callback-slot ownership alternatives remain rejected.

## Evidence

- IDA MCP `lookup_funcs` reports `0x0047ee20` as size `0x75`, giving end-exclusive range `0x0047ee95`.
- IDA MCP `decompile 0x0047ee20` shows lower-bound helper `0x004f3780`, same-owner scan, entry scope byte at offset `0x08`, message type read from `message + 0x04`, and listener virtual call through slot `+0x08`.
- [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md) is the adjacent inherited LObject wrapper for this method.
- 2026-05-26 IDA MCP recheck reconfirmed size `0x75` and the only direct caller/xref at `0x004f4b7d` inside [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md).
- 2026-05-30 IDA raw disassembly recheck confirms the fetch slot `+0x10`, listener callback slot `+0x08`, same-owner break, and post-skip index increment.

## Cross-References

- [UID:0000ZU][0x0047ec70-0x0047efae.ChangeMan](by-memory/0x0047ec70-0x0047efae.ChangeMan.md)
- [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- [UID:00019B][0x004f4b70-0x004f4b86.LObjectForwardMessage](by-memory/0x004f4b70-0x004f4b86.LObjectForwardMessage.md)
- [UID:000084][Message](by-class/Message.md)
- [UID:0001TV][ChangeManEntry](by-type/by-struct/ChangeManEntry.md)

## Changes

- 2026-06-07 A007: Set `AUTOGEN_PARENT_UID` to [UID:00001K][ChangeMan](by-class/ChangeMan.md) with position `40`. Evidence: the page documents the IDA-verified dispatch method and the class page lists this exact range in its method map. No score or final C++ change.
- 2026-06-16 B002 source-quality recheck: raised `82/88` to `86/90`. Evidence: report-only audit rechecked the sole Application wrapper caller, owner lower-bound scan, same-owner walk, `ChangeManEntry` scope semantics, `Message::m_type` read, listener callback virtual slot `+0x08`, ChangeMan owner/emitter route, rejected Application/BackPane ownership alternatives, and final-C++ blockers.
- 2026-05-30: Updated completion/confidence from `50/65` to `82/88`; added raw dispatch-loop evidence, exact sorted-list and listener vtable slot offsets, message-type filtering details, and a C++ reconstruction candidate. Evidence is from IDA MCP decompilation, xrefs, and raw disassembly recheck of `0x0047ee20-0x0047ee95`.
- 2026-05-31: `RECONSTRUCTABLE` changed from blank to `TRUE`. Evidence: the page documents an IDA-verified NexusTK dispatch method called by `Application::ForwardMessage`.
- 2026-07-01 B009 empty-emitter implementation: changed score from `86/90` to `88/91` and inserted formal first-draft `void ChangeMan::DispatchMessage(void *owner, Message *message)` C++. Evidence: live MCP reconfirmed function size, sole Application wrapper xref, first-equal helper, exact scope-byte filtering, listener slot call, and loop increment for skipped records.
