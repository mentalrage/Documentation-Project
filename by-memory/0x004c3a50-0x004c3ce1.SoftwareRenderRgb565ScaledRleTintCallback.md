*** UID:0002TG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __thiscall SoftwareRenderRgb565ScaledRleTintCallback(
    GrafPort *port,
    const EPFTileContext *sourceContext,
    const RectBounds *sourceBounds,
    const RectBounds *destinationBounds,
    unsigned char mode,
    float tintAmount,
    int rowOffset,
    float verticalScale,
    float horizontalShear)
{
    if (!port->m_drawEnabled)
        return;

    RectBounds clipBounds;
    port->GetClipRect(&clipBounds);

    const int projectedRows = static_cast<int>(
        static_cast<float>(
            rowOffset + destinationBounds->bottom - destinationBounds->top) /
        verticalScale);
    int destinationY =
        destinationBounds->bottom - projectedRows + rowOffset - 2;
    float destinationXPosition = static_cast<float>(static_cast<int>(
        static_cast<float>(projectedRows) * horizontalShear +
        static_cast<float>(destinationBounds->left)));
    int destinationX = static_cast<int>(destinationXPosition);
    unsigned short *destinationRow =
        static_cast<unsigned short *>(port->m_surfaceContext.pixelData) +
        destinationX +
        port->m_surfaceContext.rowStridePixels *
            (destinationY - port->m_surfaceContext.bounds.top) -
        port->m_surfaceContext.bounds.left;

    if (mode != 3)
        return;

    const unsigned char tintStrength = static_cast<unsigned char>(
        static_cast<int>(tintAmount * 32.0f + 0.5f));
    const int sourceWidth = sourceBounds->right - sourceBounds->left;
    const int sourceRowCount = sourceBounds->bottom - sourceBounds->top;
    const signed char *encodedRow =
        reinterpret_cast<const signed char *>(
            sourceContext->encodedMaskBytes);
    float sourceRowPosition = 0.0f;
    int sourceRowsConsumed = 0;

    while (sourceRowsConsumed < sourceRowCount)
    {
        if (clipBounds.top <= destinationY &&
            destinationY < clipBounds.bottom)
        {
            const int clipLeft =
                clipBounds.left > destinationX
                    ? clipBounds.left
                    : destinationX;
            const int destinationRight = destinationX + sourceWidth;
            const int clipRight =
                clipBounds.right < destinationRight
                    ? clipBounds.right
                    : destinationRight;
            const signed char *run = encodedRow;
            int runX = destinationX;
            unsigned short *runPixels = destinationRow;

            while (*run != 0)
            {
                const int runLength =
                    static_cast<unsigned char>(*run) & 0x7f;

                if (*run < 0)
                {
                    const int writeLeft =
                        clipLeft > runX ? clipLeft : runX;
                    const int runRight = runX + runLength;
                    const int writeRight =
                        clipRight < runRight ? clipRight : runRight;
                    unsigned short *pixel =
                        runPixels + (writeLeft - runX);

                    for (int count = writeRight - writeLeft;
                         count > 0;
                         --count, ++pixel)
                    {
                        const unsigned short value = *pixel;
                        const unsigned int red =
                            g_surfaceColorLookup5Bit[value >> 11]
                                                     [tintStrength];
                        const unsigned int green =
                            g_surfaceColorLookup6Bit[(value >> 5) & 0x3f]
                                                     [tintStrength] & 0x3f;
                        const unsigned int blue =
                            g_surfaceColorLookup5Bit[value & 0x1f]
                                                     [tintStrength] & 0x1f;
                        *pixel = static_cast<unsigned short>(
                            (red << 11) | (green << 5) | blue);
                    }
                }

                runX += runLength;
                runPixels += runLength;
                ++run;
            }
        }

        destinationXPosition -= horizontalShear;
        ++destinationY;

        const float nextSourceRowPosition =
            sourceRowPosition + verticalScale;
        const int rowsToAdvance =
            static_cast<int>(nextSourceRowPosition + 0.5f) -
            static_cast<int>(sourceRowPosition + 0.5f);

        destinationX = static_cast<int>(destinationXPosition);
        destinationRow =
            static_cast<unsigned short *>(port->m_surfaceContext.pixelData) +
            destinationX +
            destinationY * port->m_surfaceContext.rowStridePixels;

        for (int row = 0; row < rowsToAdvance; ++row)
        {
            while (*encodedRow != 0)
                ++encodedRow;
            ++encodedRow;
        }

        sourceRowPosition = nextSourceRowPosition;
        sourceRowsConsumed += rowsToAdvance;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Surface-owned file-local RGB565 scaled-RLE tint provider for `g_pfnBlitScaledRleTint`; exact 0x291-byte body, two initializer refs, twenty-one slot refs, 25 blocks / 212 instructions, GrafPort clipping, signed zero-delimited EPF mask runs, mode-3 tint gating, 5/6/5 destination lookup transforms, scale/shear projection, and protected switch-table/alignment boundaries. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004c3a50-0x004c3ce1 SoftwareRenderRgb565ScaledRleTintCallback

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: software-render callback target / RGB565 scaled RLE tint callback.
- Rebuild handling: source-authored callback implementation selected through [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md).
- Preferred reconstruction owner: [UID:0000OC][Surface](by-file/Surface.md) / software render callback implementation.
- Current IDA name: `sub_4C3A50`.
- Accepted source-facing name: `SoftwareRenderRgb565ScaledRleTintCallback`.
- Historical generated name: `RankingEventListPane::DrawScaledRLESpriteTinted_565`; retained only as a rejected caller-biased artifact.
- Confidence: very strong for the exact range, callback-table role, receiver-plus-eight-argument contract, source placement, RLE layout, and reconstructed behavior. Original lexical spelling and compile/binary-diff confirmation remain final-audit caps.

## Address Range

| Range | Function | Notes |
| --- | --- | --- |
| `0x004c3a50-0x004c3ce1` | `sub_4C3A50` | RGB565 callback-table slot `dword_69B3F0`; IDA reports size `0x291` / 657 bytes (Verified with int_convert.py). |

The preceding `0x004c3a40-0x004c3a50` bytes are local switch data for `sub_4C3190`. The following `0x004c3ce1-0x004c3cf0` bytes are `0xcc` alignment padding before `sub_4C3CF0`.

## IDA MCP Evidence

- IDA MCP `lookup_funcs` / `py_eval` on 2026-06-03 reports `sub_4C3A50` starts at `0x004c3a50`, ends at `0x004c3ce1`, and has size `0x291`.
- IDA MCP `xrefs_to` reports data references to `0x004c3a50` from raw assignment neighborhood `0x00558829` and from [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) function `0x00558840` at `0x00558ec4`.
- IDA MCP reports no direct code callers to `0x004c3a50`; runtime dispatch is through `dword_69B3F0`.
- IDA decompilation checks `this + 113`, calls [UID:000164][0x004b96c0-0x004b9768.GrafPortGetClipRect](by-memory/0x004b96c0-0x004b9768.GrafPortGetClipRect.md), reads destination buffer state from `this + 32` and pitch from `this + 40`, and walks zero-terminated RLE rows/spans.
- The body uses floating-point row/scale accumulators, skips rows based on scaled deltas, and writes RGB565 pixels through lookup tables `dword_69B3D8` and `dword_69B3DC`, using green-channel six-bit indexing `((pixel >> 5) & 0x3F)`.
- Restarted IDA MCP `py_eval` on 2026-06-03 reconfirmed `sub_4C3A50` as `0x004c3a50-0x004c3ce1`, with previous function `sub_4C3190` ending at `0x004c3a40`, `0xcc` alignment from `0x004c3ce1-0x004c3cf0`, and next function `sub_4C3CF0` starting at `0x004c3cf0`.
- Restarted IDA MCP reports exactly two inbound refs, both data-offset refs: raw assignment address `0x00558829` and `sub_558840` at `0x00558ec4`; no code xrefs target the callback directly.
- Restarted IDA MCP xrefs-from report the only product callee as `sub_4B96C0` at `0x004c3a79`; the remaining call is the runtime stack-cookie check at `0x004c3cd6`.
- Restarted IDA MCP disassembly verifies mode branch `cmp [ebp+arg_C], 3`, tint constants `dword_6104CC` / `dword_6104B0`, RLE row-data load `mov edi, [eax+24h]`, token length mask `0x7f`, and RGB565 channel lookup/masking via `dword_69B3DC`, `dword_69B3D8`, and `0x3f`.
- Current IDA decompiler output agrees with the IDA size `0x291` / 657 bytes (Verified with int_convert.py), shows a receiver plus eight callback arguments, preserves the row-data offset `0x24`, and exposes RGB565 red/green/blue reassembly through the lookup globals. The logical source contract is `void`, `GrafPort *`, `const EPFTileContext *`, two `const RectBounds *` arguments, `unsigned char mode`, `float tintAmount`, `int rowOffset`, `float verticalScale`, and `float horizontalShear`.
- IDA MCP `py_eval` re-audit by A009 on 2026-06-07 reconfirmed `sub_4C3A50` as `0x004c3a50-0x004c3ce1`, with the next modeled function at `0x004c3cf0`, 25 basic blocks, 212 instructions, and the same two data-only initializer refs at `0x00558829` and `0x00558ec4`.
- The current decompile/disassembly highlight set shows the draw-enabled guard at `[this + 0x71]`, destination base/pitch/origin usage through `+0x20`, `+0x28`, `+0x2c`, and `+0x30`, `arg_C == 3` mode gating, float constants `dword_6104CC` and `dword_6104B0`, and RGB565 lookup refs through `dword_69B3DC` and `dword_69B3D8`.
- Data-ref extraction found `dword_69B3DC` at `0x004c3bc6`, `dword_69B3D8` at `0x004c3bd6` and `0x004c3bf7`, `dword_6104CC` at `0x004c3af4`, and `dword_6104B0` at `0x004c3aff`.
- IDA MCP `export_funcs` reports the broad stored prototype `void __thiscall(int this, int, _DWORD *, int *, char, float, int, float, float)`. Its widths and receiver-plus-eight-argument ABI are evidence; the accepted source contract above is resolved from the body, callers, structures, paired provider, and initializer rather than preserving IDA labels.
- Exact target-body SHA256 is `CF941A937FF9CF24FA19A2421020BBE42B03FFBA9EDC02C00FAB427F6C68A6F7` over all 657 bytes.
- Boundary byte review on 2026-06-07 verified `0x004c3ce1-0x004c3cf0` is outside any modeled function and consists entirely of `0xcc` alignment before `sub_4C3CF0`.
- Agent-A005 Batch 182 live IDA MCP `py_eval` on 2026-06-11 reconfirmed `sub_4C3A50 0x004c3a50-0x004c3ce1`, size `0x291` / 657 bytes (Verified with int_convert.py), with no direct code callers and target refs only at raw assignment `0x00558829` and `sub_558840` `0x00558ec4`.
- Batch 182 slot-xref review counted twenty-one refs to `dword_69B3F0`; the broad ref set is consistent with callback-slot reads/writes, while direct function refs remain the two Surface initializer target refs.
- Batch 182 CFG/callee extraction reconfirmed 25 basic blocks, 212 instructions, one product callee to `0x004b96c0`, and the trailing security-cookie check.
- Batch 182 decompilation review reconfirmed the `this + 113` draw guard, destination pointer calculation through `this + 32`, `+40`, `+44`, and `+48`, the `a5 == 3` mode gate, token mask `0x7F`, zero-terminated row scanning, and lookup writes through `dword_69B3D8` and `dword_69B3DC`.
- Batch 182 boundary-byte review reconfirmed the following `0x004c3ce1-0x004c3cf0` span as 15 bytes of `0xcc` alignment (Verified with int_convert.py).

## Behavior Notes

| Role | Evidence |
| --- | --- |
| Callback-table target | Assigned to RGB565 slot `dword_69B3F0` by the surface initializer and has no direct callers. |
| Scaled RLE traversal | Uses float parameters and rounded row positions to duplicate/skip source rows while walking zero-terminated run data. |
| Tint/lookup conversion | Negative runs transform the existing destination RGB565 pixels through the 5-bit and 6-bit lookup roots. Source `pixelData` is not read and run bytes do not supply color/intensity values. |
| Destination clipping | Uses the active GrafPort clip rectangle and bounds checks before writing spans into `this + 32`. |
| Setup and mode order | Tests `m_drawEnabled` and obtains the clip rectangle before projection and the `mode == 3` gate; this ordering is behaviorally significant. |
| Tint strength | Computes the low byte of `int(tintAmount * 32.0f + 0.5f)`, preserving byte-wrap behavior rather than clamping. |
| RLE grammar | Reads signed tokens from `EPFTileContext::encodedMaskBytes`: zero terminates a row, negative tokens transform a foreground span, positive tokens skip a span, and each length is `token & 0x7f`. |
| Projection | Derives projected rows from `(rowOffset + destinationBottom - destinationTop) / verticalScale`, computes destination Y with the original `-2`, and shears X from projected rows before each rounded source-row advance. |
| Row advancement | Uses rounded float accumulator deltas and skips complete zero-terminated encoded rows; it does not seek by source-bounds left/top. |

## Touched State

| State | Evidence |
| --- | --- |
| `this + 32` | Destination pixel buffer base. |
| `this + 40` | Destination stride/pitch used for row increments. |
| `this + 44` / `this + 48` | Destination origin offsets included in the initial buffer pointer calculation. |
| `this + 113` | Draw-enabled guard. |
| `dword_69B3D8` / `dword_69B3DC` | Lookup-table storage used for RGB565 tint conversion. |
| `dword_69B3F0` | Callback table slot that points at this target in the RGB565 family. |
| `sprite + 0x24` | RLE row-data pointer loaded before row/span traversal. |
| `dword_6104CC` / `dword_6104B0` | Tint scale and float-rounding bias used to form the lookup index. |

## Ownership Notes

Surface [UID:0000OC][Surface](by-file/Surface.md) is the canonical owner and emitter because it owns callback storage, pixel-format selection, both compat/RGB565 providers, lookup roots, and the generated `NexusTK/render/Surface.cpp` source root. The generated RankingEventListPane name is callsite-biased. GrafPort is the active receiver dependency, while Riding, Monster, and other slot users are callers only. SoftwareBlend16 is related color-math context, not the provider owner. These rejected ownership alternatives remain recorded because twenty-one broad slot references and the shared initializer contradict any feature-specific placement.

## Reconstruction Notes

- `RECONSTRUCTABLE` is true; this exact callback target emits its file-local provider body through [UID:0000OC][Surface](by-file/Surface.md), while the RGB565 aggregate remains a non-emitting inventory.
- The formal CPP contains the complete source-facing callback. The target H remains blank because this provider is private; shared `SurfaceScaledRleTintProc` and `g_pfnBlitScaledRleTint` declarations belong to semantic [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md).
- Place the static provider in `Surface.cpp` in exact address order after UID0002TF and before UID0002TH. Do not duplicate storage in physical [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md).
- Preserve setup-before-mode gating, the low-byte tint conversion, signed token semantics, clipped destination-only writes, rounded row scaling, and the initial origin-adjusted versus later origin-unadjusted destination-pointer asymmetry.
- Do not add an `encodedMaskByteCount` check, a zero/negative `verticalScale` guard, source-bounds left/top stream seeking, or a normalized pointer formula; none exists in the executable behavior.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | Exact range, body hash, CFG shape, initializer refs, all twenty-one slot refs, receiver and eight arguments, EPF mask layout, control order, scale/shear formulas, clipping, tint conversion, RGB565 lookup writes, source placement, ownership, boundaries, formal CPP, blank-H disposition, and support routing are documented. Remaining work is compile/binary-diff and project-wide final lexical audit. |
| Confidence `94` | Bounds, bytes, references, callers, structures, constants, paired-provider shape, and complete data flow independently corroborate the source contract and behavior. Confidence remains below 95 because original symbols are unavailable and no compile/binary-diff receipt yet proves final source spelling and compiler reproduction. |

## Exact Dispatch Inventory

The callback target has exactly two direct data/setup references, at `0x00558829` and `0x00558ec4`, and no direct code caller. Slot `0x0069b3f0` has exactly twenty-one inbound references: reads at `0x004d7044`, `0x004d70e2`, `0x004d7685`, `0x004d7727`, `0x004d77c9`, `0x004d786b`, `0x004d790d`, `0x004d79af`, `0x004d7a4e`, `0x004d7ad4`, `0x004d7b75`, `0x004d7c1b`, `0x004d7cb9`, `0x004db310`, `0x004dc9fb`, `0x004e44f4`, `0x004e457f`, and `0x004e4cf8`, plus setup/assignment references at `0x00558768`, `0x00558ba8`, and `0x00558ec4`. The two target references and twenty-one slot references are separate populations and must not be conflated.

## Exact Algorithm And Fidelity Constraints

- The function exits immediately when `GrafPort::m_drawEnabled` is false. Otherwise it obtains `RectBounds clipBounds` through `GrafPort::GetClipRect` before projection and before rejecting modes other than `3`.
- Projected source-row count is integer truncation of `(rowOffset + destinationBounds->bottom - destinationBounds->top) / verticalScale`. Destination Y is `destinationBounds->bottom - projectedRows + rowOffset - 2`. Destination X starts from integer truncation of `projectedRows * horizontalShear + destinationBounds->left`.
- The initial destination pointer includes `m_surfaceContext.bounds.top` and `.left` origin subtraction. Later row recomputation uses `destinationX + destinationY * rowStridePixels` without those subtractions. This asymmetry is preserved intentionally.
- `tintStrength` is the low byte of `int(tintAmount * 32.0f + 0.5f)`. There is no clamp. The 32.0f and 0.5f constants are pooled literals with broad users and remain unnamed/unowned by this target.
- The source width and row count come from source-bounds right-minus-left and bottom-minus-top. The stream begins directly at `sourceContext->encodedMaskBytes`; source `pixelData`, source-bounds left/top stream seeking, and `encodedMaskByteCount` are not used.
- Each signed token has length `static_cast<unsigned char>(token) & 0x7f`. A negative token transforms the clipped destination span; a positive token advances without writes; zero ends the encoded row.
- Each destination pixel is read in RGB565 form. Red and blue use the 5-bit lookup root, green uses the 6-bit root, and the transformed channels are reassembled as `(red << 11) | (green << 5) | blue`.
- Vertical advancement uses `int(nextSourceRowPosition + 0.5f) - int(sourceRowPosition + 0.5f)` and skips that many complete zero-delimited stream rows. No guard for zero or negative `verticalScale` is inserted.

## Historical Assumptions Reconciled

- Superseded: the earlier behavior row said the callback combined source intensity/run bytes with destination pixels. Direct data flow proves that run bytes encode only skip/foreground span control and that negative runs transform the existing destination pixel; source pixel data is never read.
- Rejected: `RankingEventListPane::DrawScaledRLESpriteTinted_565`, raw `sub_4C3A50`, and caller-derived Riding/Monster names. They are historical leads, not source identities.
- Rejected: GrafPort, Riding, Monster, RankingEventListPane, and SoftwareBlend16 ownership. The receiver and consumers do not own the slot/provider family.
- Resolved: exact RLE layout, semantic parameters, callback typedef, and source placement are no longer final-C++ blockers. Original lexical spelling remains inferential but is represented with consistent human source names rather than IDA labels.

## Cross-References

- [UID:00016L][0x004c0770-0x004c5fde.SoftwareRenderRgb565Callbacks](by-memory/0x004c0770-0x004c5fde.SoftwareRenderRgb565Callbacks.md)
- [UID:00016J][0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks](by-memory/0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks.md)
- [UID:0002TC][0x004bdcc0-0x004bdf3e.SoftwareRenderCompatScaledRleTintCallback](by-memory/0x004bdcc0-0x004bdf3e.SoftwareRenderCompatScaledRleTintCallback.md)
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md)
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md)
- [UID:0000OC][Surface](by-file/Surface.md)
- [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md)
- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md)
- [UID:000164][0x004b96c0-0x004b9768.GrafPortGetClipRect](by-memory/0x004b96c0-0x004b9768.GrafPortGetClipRect.md)

## Changes

- 2026-06-03: Created exact child page for the RGB565 `dword_69B3F0` target after IDA MCP verified `0x004c3a50-0x004c3ce1`, table-initializer xrefs, no direct callers, scaled RLE traversal, destination state, and RGB565 lookup-table writes.
- 2026-06-03: Raised completion 68 -> 74 and confidence 82 -> 84 after restarted IDA MCP reconfirmed exact bounds, neighboring function/padding boundaries, data-only inbound refs, callee set, mode branch, RLE row-data offset, tint constants, and RGB565 channel lookup masks. Parent/code remain blank because final callback typedef, exact sprite/RLE layout, and source-level owner/name are still unresolved.
- 2026-06-03 parent-nesting consistency pass: Kept scores at `74/84`, recorded manual by-memory nesting under [UID:00016L][0x004c0770-0x004c5fde.SoftwareRenderRgb565Callbacks](by-memory/0x004c0770-0x004c5fde.SoftwareRenderRgb565Callbacks.md), added the [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) cross-reference, and left `AUTOGEN_PARENT_UID` plus C++ blank because the aggregate has no generated source root and the likely [UID:0000OC][Surface](by-file/Surface.md) parent remains below the 80+ confidence gate.
- 2026-06-07 A009 Batch005 IDA audit:
  - Changed to: raised scores from `74/84` to `82/86`, retained blank `AUTOGEN_PARENT_UID`, added CFG/callee/state-offset/LUT/prototype evidence, and documented the corrected `85/85` parent gate failure.
  - Summary/evidence: IDA MCP reconfirmed `sub_4C3A50` as a `0x291`-byte function with data-only initializer refs at `0x00558829` and `0x00558ec4`, 25 basic blocks, 212 instructions, a single product callee to [UID:000164][0x004b96c0-0x004b9768.GrafPortGetClipRect](by-memory/0x004b96c0-0x004b9768.GrafPortGetClipRect.md), draw-enabled guard, destination state offsets, `arg_C == 3` mode gating, shade constants, RGB565 lookup-table globals, and `0xcc` alignment at `0x004c3ce1-0x004c3cf0`. C++ and parent remain blank because final callback names/types and parent gate are unresolved.
- 2026-06-10 B001-033 parent-gate repair:
  - Changed completion from `82` to `85` and assigned `AUTOGEN_PARENT_UID:0000OC`.
  - Evidence: live IDA MCP recheck confirmed `sub_4C3A50` as `0x004c3a50-0x004c3ce1`, no direct code callers, target refs only at raw setup `0x00558829` and `sub_558840` `0x00558ec4`, callback slot `dword_69B3F0`, twenty-one slot refs, 25 basic blocks, 212 instructions, and the same clip/LUT/scaled-RLE behavior. Surface now clears `88/85` and is the direct source parent.
- 2026-06-11 A005 Batch 182 evidence refresh:
  - Changed completion/confidence from `85/86` to `87/90`; kept `AUTOGEN_PARENT_UID:0000OC` and left final C++ blank.
  - Evidence: live IDA MCP reconfirmed the `0x291` / 657-byte size (Verified with int_convert.py), no direct callers, two target refs, twenty-one `dword_69B3F0` slot refs, 25 blocks / 212 instructions, only product callee `0x004b96c0`, `a5 == 3` mode gate, `0x7F` token mask, zero-terminated row scan, RGB565 lookup refs through `dword_69B3D8` / `dword_69B3DC`, tint constants, and the 15-byte `0xcc` padding span at `0x004c3ce1-0x004c3cf0` (Verified with int_convert.py).
- 2026-08-11 B008 UID0002TG source-quality callback:
  - Raised completion/confidence from `87/90` to `92/94`, retained `CANONICAL_OWNER:0000OC`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000OC`, and blank emitter position, populated the exact formal CPP body, and kept formal H blank for the file-local provider.
  - Resolved the receiver-plus-eight-argument source contract, signed zero-delimited EPF mask grammar, destination-only RGB565 tint transformation, projection/scaling formulas, source placement, callback ownership, and slot declaration route from the accepted Gate 1 report.
  - Preserved the stale source-intensity description and caller-biased ownership/name candidates as explicit rejected historical assumptions rather than deleting their provenance.
