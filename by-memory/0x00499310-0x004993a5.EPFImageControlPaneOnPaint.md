*** UID:0004JW | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004G | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004G | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void EPFImageControlPane::OnPaint()
{
    EPFTileContext frame;

    SetDrawColor(0);
    FillRect(&m_bounds);

    g_pEPFLib->LookupLayoutEntry(m_resourceName, m_frameIndex, &frame);
    if (frame.pixelData != NULL)
    {
        void *palette = g_pPaletteLib->GetPaletteByName(m_paletteName);
        g_pfnBlitSprite(this,
                        &frame,
                        &frame.bounds,
                        &m_bounds,
                        0,
                        palette,
                        NULL);
    }

    m_surfaceContext.BuildEncodedMask();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: EPFImageControlPane virtual OnPaint: initialize borrowed frame view, clear pane bounds through slot-7 FillRect, lookup named signed frame, conditionally palette/blit valid pixels, and unconditionally rebuild inherited encoded mask. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00499310-0x004993a5 EPFImageControlPane OnPaint

## Status

- Source method: virtual `EPFImageControlPane::OnPaint`.
- Exact range: `0x00499310-0x004993a5` (`0x95` bytes).
- Direct owner/emitter: [UID:00004G][EPFImageControlPane](by-class/EPFImageControlPane.md), position `30`.
- Source root: [UID:0000J2][EPFImageControlPane](by-file/EPFImageControlPane.md).
- Split index: [UID:00011J][0x004991f0-0x004993a5.EPFImageControlPaneCore](by-memory/0x004991f0-0x004993a5.EPFImageControlPaneCore.md).
- Virtual route: sole direct xref is primary `EPFImageControlPane` vtable cell `0x0061828c`, slot `+0x44` from vtable base `0x00618248`.
- Disposition: ordinary source-authored paint override. Historical `LoadAndRenderImage` is retained only as a superseded descriptive alias.

## Score Rationale

- Completion `92`: the complete virtual route, local initialization, callback order, frame/palette lookup, invalid-frame branch, blit arguments, borrowed-pointer rule, and final mask behavior are documented and represented.
- Confidence `93`: behavior and dependencies are exact. Original private/method/global typedef spellings remain stripped, so accepted descriptive names stay below final-audit confidence without blocking source reconstruction.

## Exact Behavior And Order

1. Construct a local `EPFTileContext` and explicitly call its accepted `Initialize` operation.
2. Set the inherited GrafPort draw color to zero.
3. Invoke callback-table slot 7 with receiver `this` and rectangle `this+0x44`, source-facing as `FillRect(&m_bounds)`.
4. Ask `g_pEPFLib` to look up `m_resourceName` and signed `m_frameIndex` into the local frame view.
5. Test `frame.pixelData`.
6. Only for a valid frame, resolve `m_paletteName` through `g_pPaletteLib` and call accepted slot-2 `g_pfnBlitSprite`.
7. Unconditionally call inherited `m_surfaceContext.BuildEncodedMask()`, including the invalid-frame path.

This order is behaviorally significant. The pane is cleared before resource lookup, palette/blit work is skipped when the returned pixel pointer is null, and mask construction is not part of the valid-frame branch.

## Virtual Dispatch Evidence

- `xref_query` reports no ordinary direct code caller for `0x00499310`.
- The only xref is data cell `0x0061828c -> 0x00499310`.
- `0x0061828c` is `0x44` bytes from primary EPF vtable base `0x00618248`, matching the established virtual paint slot used by control classes.
- Secondary and tertiary EPF vtables are destructor-adjusted views; they do not add a separate paint implementation.
- Virtual-only reachability is positive evidence for an override, not orphan/dead-code evidence.

## Frame Lookup And Failure Behavior

- The lookup call is accepted `g_pEPFLib->LookupLayoutEntry(m_resourceName, m_frameIndex, &frame)` at `0x004d02f0`.
- Constructor storage and lookup use show `m_frameIndex` is a signed 16-bit member; the machine call sign-extends it.
- Current ResourceLayoutTable support establishes that lookup initializes/clears output state on failure and exposes pointers owned by shared resource storage.
- When `frame.pixelData` is null, this method skips palette lookup and sprite blit only. It does not skip initial clear or final `BuildEncodedMask`.
- The local frame is a borrowed view. No `ReleaseBuffers` call exists on the local, and adding one could release shared resource memory not owned by this method.

## Callback And Blit Evidence

### Slot 7

- The raw call is through storage `0x0069b3fc` with `(this, this+0x44)`.
- Installed callback targets are `0x004be680` (`0x6e7` bytes) and `0x004c4380` (`0x340` bytes).
- Both targets consume a GrafPort receiver and rectangle/clip/color state in fill/presentation behavior.
- The accepted source call for this pane paint site is `FillRect(&m_bounds)`.
- Historical generated alias `g_pfnLockSurface` is rejected: there is no one-argument call and no lock-token/result handling.

### Slot 2

- The actual image operation is callback-table slot `0x0069b3e8`, accepted as `g_pfnBlitSprite` / `SurfaceSpriteBlitProc`.
- Arguments are destination receiver `this`, source frame context, source `frame.bounds`, destination `m_bounds`, mode `0`, resolved palette, and null options.
- The final argument is a nullable options pointer, not a scalar flags value.
- Palette service behavior supplies its established fallback semantics; the target does not add an unsupported extra null-palette branch.

## Inherited And Local State

| State | Offset / source role | Use here |
| --- | --- | --- |
| `m_surfaceContext` | inherited at `+0x1c` | Final unconditional `BuildEncodedMask`. |
| `m_bounds` | inherited at `+0x44` | Slot-7 fill rectangle and sprite destination rectangle. |
| `m_resourceName[16]` | tail at `+0x108` | Resource layout lookup key. |
| `m_frameIndex` | signed short at `+0x128` | Sign-extended frame selector. |
| `m_paletteName[16]` | tail at `+0x12a` | Palette lookup key. |
| local `frame` | stack `EPFTileContext` | Initialized borrowed result view and source rectangle/pixels. |

## Boundary And Dependency Evidence

- The method starts at `0x00499310` immediately after the two-byte internal alignment gap.
- IDA reports size `0x95`, giving exact exclusive end `0x004993a5`.
- Bytes `0x004993a5-0x004993b0` are eleven `0xcc` bytes already registered as trailing padding.
- The successor at `0x004993b0` installs `TargetOptionEditControlPane` vtables and belongs to UID00011K; no successor instruction belongs to this paint method.
- Complete call/dependency order is local initialization helpers `0x00457a60` and `0x004b9660`, slot 7 `0x0069b3fc`, lookup `0x004d02f0`, palette lookup `0x00543d70`, slot 2 `0x0069b3e8`, and mask build `0x00457ab0`.

## Negative Evidence

- No direct code caller exists; do not replace the virtual method with a private helper solely because old documentation used `LoadAndRenderImage`.
- No one-argument lock callback exists and no lock result is consumed.
- No flip operation or local flip field is read here.
- No local-frame release occurs. The lookup result is borrowed.
- No palette lookup or blit occurs on the invalid-frame path.
- No evidence makes `BuildEncodedMask` a semantic return value; the source override remains `void`.
- No evidence supports emitting callback-table implementations, ResourceLayoutTable logic, PaletteLib logic, or EPFTileContext ownership in this class.

## Historical Corrections

- Superseded `LoadAndRenderImage` wording described behavior but obscured the vtable-only paint contract. The source-facing method is now `OnPaint` while the old alias remains historical provenance.
- Superseded clear/lock wording for `dword_69B3FC` is narrowed to two-argument pane-region `FillRect` behavior for this call site.
- Superseded cached-image wording could imply the local frame owns decoded buffers. Current lookup evidence establishes borrowed pointers and no local release.
- Superseded flip wording is rejected; the constructor's byte is inherited control flags and mode is configured separately.

## Cross-References

- [UID:00004G][EPFImageControlPane](by-class/EPFImageControlPane.md)
- [UID:0000J2][EPFImageControlPane](by-file/EPFImageControlPane.md)
- [UID:00011J][0x004991f0-0x004993a5.EPFImageControlPaneCore](by-memory/0x004991f0-0x004993a5.EPFImageControlPaneCore.md)
- [UID:00011K][0x004993b0-0x0049980c.TargetOptionEditControlPane](by-memory/0x004993b0-0x0049980c.TargetOptionEditControlPane.md)
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md)
- [UID:0000QW][g_pfnLockSurface](by-global/g_pfnLockSurface.md)
- [ResourceLayoutTable lookup](by-memory/0x004d0120-0x004d182f.ResourceLayoutTable.md)
- [EPFTileContext](by-class/EPFTileContext.md)
- [GrafPort](by-class/GrafPort.md)

## Changes

- 2026-07-13 B001 UID00011J accepted callback: created this exact split child, applied the supervisor-accepted OnPaint block, and incorporated virtual-only routing, FillRect direction, frame/palette/blit behavior, invalid-frame branch, borrowed-frame semantics, unconditional mask build, dependency, boundary, negative, and historical evidence.
