*** UID:00015U | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void BuildFieldMapPath(const wchar_t *sourcePath, wchar_t *destination)
{
    const wchar_t *end = sourcePath;
    while (*end != L'\0')
        ++end;

    const wchar_t *lastDot = 0;
    const wchar_t *scan = end;
    while (scan != sourcePath)
    {
        --scan;
        if (*scan == L'.')
        {
            lastDot = scan;
            break;
        }
    }

    const size_t copyChars = lastDot != 0
        ? (size_t)(lastDot - sourcePath)
        : (size_t)(end - sourcePath);
    memmove(destination, sourcePath, copyChars * sizeof(wchar_t));
    destination[copyChars] = L'\0';
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: FieldMapPane constructor-only static path-stem helper with exact `0x64` boundary, `m_fieldMapEpfPath` destination, last-dot stripping, `_memmove` dependency, one caller at `0x004b0c0d`, adjacent padding, and formal CPP placed after class methods/helpers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b8220-0x004b8284 BuildFieldMapPath

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: file-local/static helper function.
- Address range: `0x004b8220-0x004b8284`, end-exclusive.
- Current IDA label: `sub_4B8220`; source-facing recommendation `BuildFieldMapPath`, with behavior alias `CopyPathStem`.
- Likely source module: [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md), emitted through `NexusTK/map/FieldMapPane.cpp`.
- Rebuild handling: `source-authored`.
- Signature shape: `static void BuildFieldMapPath(const wchar_t *sourcePath, wchar_t *fieldMapPath)`. The binary is caller-cleanup `cdecl` with two stack arguments. It leaves `EAX` zero as a side effect of writing the wide NUL terminator, but no caller observes a return value and no source-level status return is proven.
- Confidence: very strong for exact boundary, padding, single caller, callee set, argument roles, FieldMapPane destination field, caller-side `.EPF` append, and `CharUpperBuffW` uppercase dispatch; high but not final-audit for exact original helper spelling and source input-size precondition.
- Reconstruction note: first-draft formal C++ is populated for the helper body only. Suffix append and uppercase call remain constructor-level behavior, not part of this range.

## Behavior

`BuildFieldMapPath` is a FieldMapPane-local path-stem copy helper. It scans the source wide string to its NUL terminator, scans backward for the last wide `'.'`, copies the source characters before that dot when one exists, copies the whole source string when no dot exists, and writes a wide NUL terminator in the destination. Empty input and input beginning with `'.'` produce an empty destination string. The helper does not check path separators, so it is not a complete path normalizer or basename extractor. It does not append `.EPF`, uppercase, allocate, open files, load resources, touch globals, or validate the destination capacity.

The known caller is the `FieldMapPane` constructor. It passes the constructor source path and `this+0xf8`, then appends the pooled use-site suffix `L".EPF"` with `_wcscat_s(this+0xf8, 0x80, L".EPF")`, computes the buffer length, and calls the wide API dispatch slot `0x0069be58`, resolved as `CharUpperBuffW`, to uppercase the completed EPF path in place.

The destination field should be declared as `wchar_t m_fieldMapEpfPath[0x80]` at `FieldMapPane+0x0f8`. The helper itself has no size parameter, so the source contract is external: for the whole constructor sequence to be valid, the copied stem must be at most `0x7b` wide characters before appending the four-character `.EPF` suffix and the NUL terminator into a `0x80`-wide-character buffer.

## IDA Evidence

- 2026-06-06 A006 IDA MCP `analyze_funcs 0x004b8220` reports `sub_4B8220`, exact `0x64` byte body, and basic blocks ending at `0x004b8284`.
- Decompilation shows `wcslen(sourcePath)`, a backward scan from the last wide character, a `0x2e` wide-dot compare, `_memmove(destination, sourcePath, 2 * copyChars)`, a wide NUL write at `destination + 2 * copyChars`, and `return 0`.
- Disassembly confirms the source pointer comes from `[ebp+Src]`, the destination pointer from `[ebp+arg_4]`, the copy length is doubled with `lea edi, [eax+eax]`, and the terminator write uses `mov [edi+esi], ax` after `xor eax, eax`.
- `callers` and `xrefs_to` are narrow: one code ref at `0x004b0c0d` inside `sub_4B0BB0`, the FieldMapPane constructor.
- `callees` reports only `_memmove`; the helper has no string xrefs and no direct wide API, loader, allocator, file I/O, or packet callees.
- Constructor disassembly at `0x004b0bef-0x004b0c1d` pushes `edi = this + 0xf8` and `[ebp+Src]` before the call, then calls `_wcscat_s(edi, 0x80, L".EPF")`.
- 2026-06-06 A006 IDA `py_eval` confirms `0x0061a44c` begins with wide words `0x002e 0x0045 0x0050 0x0046 0x0000`, the `.EPF` suffix used immediately after the helper returns.
- Boundary check: IDA `py_eval` reports no function at `0x004b821e` or `0x004b8284`, function `sub_4B8220` at `0x004b8220-0x004b8284`, two `0xcc` bytes at `0x004b821e-0x004b8220`, and twelve `0xcc` bytes at `0x004b8284-0x004b8290`.
- B002 2026-06-18 read-only PE reanalysis confirms the exact `0x004b8220-0x004b8284` body, two `0xcc` bytes before it, twelve `0xcc` bytes after it, one external rel32 call at `0x004b0c0d`, no external short branch into the body, no meaningful pointer-table route, and `_memmove` as the only callee.
- The constructor context is `lea edi,[esi+0xf8]`, `push edi`, `push [ebp+8]`, `call 0x004b8220`, `push 0x0061a44c`, `push 0x80`, `push edi`, `_wcscat_s`, inline `wcslen`, and `call dword ptr [0x0069be58]`.
- PE import/initializer parsing resolves `0x0069be58` as the PlatformApi `CharUpperBuffW` dispatch slot: the initializer writes it from IAT cell `0x0060d39c`, which imports `USER32.dll!CharUpperBuffW`.
- The apparent zero return is incidental. The instruction `xor eax,eax; mov [edi+esi],ax` writes the wide terminator and leaves `EAX` zero, but the sole caller ignores it.

## Caller Context

| Call site | Caller | Observed use |
| --- | --- | --- |
| `0x004b0c0d` | [UID:00014Z][0x004b0bb0-0x004b129b.FieldMapPane](by-memory/0x004b0bb0-0x004b129b.FieldMapPane.md) constructor | Builds the `this + 0xf8` wide path buffer from the constructor source path, then appends `.EPF` and submits the full string to the wide API dispatch path helper. |

The single-caller set supports keeping this attached to the FieldMapPane source file for now. A later broader path-helper audit could still rename it as a shared stem-copy helper if matching source-level usage appears elsewhere.

## Touched State And Dependencies

- Reads the source path pointer and writes only the destination wide buffer supplied by the caller.
- Calls `_memmove` for the selected byte count; overlap-safe copying may be intentional because the helper accepts raw source/destination pointers.
- Uses no globals directly. The `.EPF` suffix at [UID:00025B][0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData](by-memory/0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData.md) and the wide API dispatch path helper are caller-side constructor dependencies, not state touched by this helper.
- Does not allocate, free, open files, call resource loaders, mutate `FieldMapPane` fields by itself, or validate the destination capacity.

## Ownership Decision

Keep this as a FieldMapPane file-local/static helper. The constructor-only caller, FieldMapPane destination buffer, pooled `.EPF` append, and immediate `CharUpperBuffW` uppercasing make [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md) the source route. Do not move this to [UID:0000MI][PathUtil](by-file/PathUtil.md) unless a later source-tree pass finds matching source-level usage elsewhere; current binary evidence does not.

## Score Rationale

- Completion `91`: the page records the exact body/padding, cdecl arguments, source/destination roles, `_memmove`, sole constructor caller, negative hidden-route evidence, edge cases, exact path-buffer destination, caller-side suffix/uppercase sequence, capacity contract, source placement, rejected alternatives, formal CPP, and source-order position.
- Confidence `93`: raw PE/IDA behavior, caller context, import parsing, and current FieldMap support agree. Exact original helper spelling and the upstream source-path length guarantee remain inferred rather than symbol- or resource-definition-proven.

## Open Questions / Resolved Source-Quality Notes

- Resolved: source placement remains [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md) `FieldMapPane`, not [UID:0000MI][PathUtil](by-file/PathUtil.md) `PathUtil`, because the helper has exactly one caller and that caller uses a FieldMapPane inline path buffer.
- Resolved: destination field declaration is `wchar_t m_fieldMapEpfPath[0x80]` at `FieldMapPane+0x0f8`.
- Resolved: the caller-side dispatch after suffix append is `CharUpperBuffW`, not an unknown path-finalization helper.
- Resolved: formal first-draft C++ is populated under the active code-entry gate.
- Remaining non-blocking uncertainty: exact original helper spelling is not symbol-proven. Keep `BuildFieldMapPath` as the source-facing FieldMap-local name and record `CopyPathStem` as the behavior alias.

## Cross-References

- [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md)
- [UID:00004U][FieldMapPane](by-class/FieldMapPane.md)
- [UID:00014Z][0x004b0bb0-0x004b129b.FieldMapPane](by-memory/0x004b0bb0-0x004b129b.FieldMapPane.md)
- [UID:00025B][0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData](by-memory/0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData.md)

## 2026-08-18 B007 File-Local Helper Closure

- The helper is source-local to `NexusTK/map/FieldMapPane.cpp`, not a PathUtil/shared-utility definition. Its sole caller is the FieldMapPane constructor at `0x004b0c0d`, and its only callee is `_memmove` at `0x004b8268`.
- Exact source order uses the class preamble's static forward declaration and emits this body at `EMITTER_POSITION_OPTIONAL:40`, after the aggregate, rectangle, and request fragments. This preserves a normal source file while matching the separated linker order of the binary body.
- The formal body copies characters through but not including the last period, or the whole source when no period exists, and always writes a wide NUL. Empty input or a leading period yields an empty destination. It does not append `.EPF`, uppercase, allocate, validate capacity, inspect path separators, or touch globals.
- Caller-only behavior remains on the constructor: append shared literal `L".EPF"`, calculate length, then call `g_pfnCharUpperBuffW`. The destination is `wchar_t m_fieldMapEpfPath[128]` at object `+0x00f8`.
- The accepted name `BuildFieldMapPath` is high-probability source inference. `CopyPathStem` remains a behavior alias, while raw address labels and a generic PathUtil owner are rejected final-source shapes.

## Changes

- 2026-08-18 UID0000N2 boundary reconciliation:
  - Preserved this helper's FieldMap ownership, exact `[0x004b8220,0x004b8284)` source range, behavior, score, emitter, and formal source.
  - Recorded leading `[0x004b821e,0x004b8220)` as exactly 2 `cc` bytes with zero inbound xrefs, separating [UID:00022K][0x004b81f0-0x004b821e.FileExists](by-memory/0x004b81f0-0x004b821e.FileExists.md) `FileExists` from this function (claim C160).
  - Recorded trailing `[0x004b8284,0x004b8290)` as exactly 12 `cc` bytes with zero inbound xrefs, separating this function from [UID:00022L][0x004b8290-0x004b83c5.RectAnchorAxisAdjustHelper](by-memory/0x004b8290-0x004b83c5.RectAnchorAxisAdjustHelper.md) `AdjustRectAnchorAxis` (claim C161).
  - Both spans remain address-ordered no-code entries in `by-memory/-ignored.md`; neither changes this function's owner nor joins the separated RectBounds axis helper to FieldMap source.

- 2026-08-18 B007 whole-file implementation callback:
  - Raised `88/91` to `91/93`, set source order `40`, synchronized the exact static formal body/parameter spelling, and replaced the stale blank-final-C++ item summary.
  - Added separated-linker-order rationale and preserved exact caller, dependency, edge-case, and source-placement evidence.

- 2026-06-06 A006 IDA evidence refresh:
  - Before: score was `76/84`; the page recorded the high-level stem-copy behavior, caller count, and endpoint correction, but not the argument roles, caller-side destination buffer, suffix byte evidence, touched-state separation, empty-string behavior, or score rationale.
  - Changed to: score `84/88`, expanded status/signature, behavior, fresh `analyze_funcs` evidence, constructor call-site context, `.EPF` suffix bytes, padding proof, caller/touched-state tables, ownership rationale, open questions, and explicit final-C++ gate note.
  - Summary/evidence: IDA MCP `analyze_funcs 0x004b8220`, constructor disassembly at `0x004b0bb0`, and `py_eval` checks around `0x004b821e-0x004b8290` and `0x0061a44c` verify the `0x004b8220-0x004b8284` helper boundary, `_memmove`-only dependency, single FieldMapPane constructor caller, `this+0xf8` destination, `.EPF` suffix append context, and adjacent `0xcc` padding.
- 2026-06-18 B002 source-quality reanalysis:
  - Changed to: `COMPLETION:88`, `CONFIDENCE:91`, owner/emitter unchanged [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md), and formal first-draft C++ populated.
  - Summary/evidence: read-only PE reanalysis confirmed exact body/padding, one external caller, no hidden pointer/branch route, `_memmove`-only callee, caller-cleanup two-argument helper shape, `m_fieldMapEpfPath[0x80]` at `FieldMapPane+0x0f8`, caller-side `_wcscat_s(...,0x80,L".EPF")`, and PlatformApi slot `0x0069be58` resolved to `CharUpperBuffW`. The pass rejects PathUtil/shared utility ownership for current evidence and keeps `CopyPathStem` only as a behavior alias.
- 2026-06-01 documentation pass:
  - Before: metadata remained at `50/65`, reconstructable and parent fields were blank, and generated label wording used a file path rather than evidence.
  - Changed to: `76/84`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000JA`.
  - Summary/evidence: IDA MCP reconfirmed the exact function size, single FieldMapPane constructor caller, `_memmove` callee, and path-stem behavior; C++ remains blank pending final source naming.
- Before: this page used exclusive end `0x004b8283`.
- After: this page uses exclusive end `0x004b8284`.
- Summary/evidence: 2026-05-28 IDA MCP `lookup_funcs 0x004b8220` reports `sub_4B8220` size `0x64`; disassembly shows the final `jmp short loc_4B825F` instruction at `0x004b8282-0x004b8283`, so padding starts at `0x004b8284`.
