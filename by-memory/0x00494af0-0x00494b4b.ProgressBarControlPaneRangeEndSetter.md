*** UID:000117 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ProgressBarControlPane::SetMaxValue(short maxValue)
{
    if (maxValue < 0)
        maxValue = 0;
    else if (maxValue > kMaxProgressValue)
        maxValue = kMaxProgressValue;

    if (m_rangeEnd == maxValue)
        return;

    if (m_rangeCurrent > maxValue) {
        m_rangeCurrent = maxValue;
        InvalidateRect(&m_visibleBounds);
    }

    m_rangeEnd = maxValue;
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `ProgressBarControlPane::SetMaxValue(short)`: clamps signed input to `0..30000`, returns without side effects when unchanged, lowers inherited `m_rangeCurrent +0xfa` when above the new maximum, then writes inherited `m_rangeEnd +0xfc`; `InvalidateRect(&m_visibleBounds)` runs zero, one, or two times according to those paths. Independent B003 live MCP and B002 sibling-support evidence confirm the exact 91-byte body/padding, no external entry/xref/pointer route, class/file ownership, and populated formal C++; raw no-route status is a liveness caveat, not a no-code condition. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00494af0-0x00494b4b - ProgressBarControlPane Range-End Setter

## Status

- Confidence: very strong for the exact body/hash/range, signed clamp and field types, branch/write/invalidation order, inherited rectangle/virtual route, class/file ownership, and exhaustive no-route checks; strong for inferred original lexical spellings.
- Related class: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- Related file: [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- IDA function status: code-shaped bytes, not a function object.
- Source shape: `ProgressBarControlPane::SetMaxValue(short maxValue)`.
- C++ status: first-draft source-authored mutator populated; raw/no-caller caveat retained.

## Covered Range

| Range | Owner | Role |
| --- | --- | --- |
| `0x00494af0-0x00494b4b` | `ProgressBarControlPane` | Source-authored `SetMaxValue(short maxValue)` body that clamps the incoming short to `0..30000`, writes inherited `ControlPane::m_rangeEnd` at object offset `+0xfc`, lowers inherited `ControlPane::m_rangeCurrent` at object offset `+0xfa` if needed, and invalidates `GrafPort::m_visibleBounds`. |

## Evidence Notes

- IDA MCP `lookup_funcs 0x00494af0` reports `Not a function`.
- Raw IDA disassembly shows a complete private helper body with receiver register in `ecx`, stack argument at `[ebp+8]`, and `retn 4` ending before alignment at `0x00494b4b`.
- 2026-06-01 IDA MCP byte check confirms the body is 91 bytes, starts with `push ebp`, ends with `retn 4`, and is followed by five `0xcc` alignment bytes before `0x00494b50`.
- Field usage matches [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md): object offset `0xfc` for end/upper bound, object offset `0xfa` for current value, and vtable slot `+0x20` invalidation with the pane rectangle at object offset `0x44`.
- 2026-06-05 live IDA reconfirms `0x00494a90`, `0x00494ae7`, `0x00494af0`, and `0x00494b4b` are not function objects; the next modeled function starts at `0x00494b50`.
- 2026-06-05 `xrefs_to` and `callers` report no references or ordinary direct callers for `0x00494af0`, matching the projected-private-helper classification.
- 2026-06-05 byte reads confirm the exact `0x5b`-byte helper body, clamp ceiling `0x7530`, field writes at object offsets `0xfc` and `0xfa`, vtable slot `+0x20` invalidation, `retn 4` exit, and five alignment bytes at `0x00494b4b-0x00494b50`.
- 2026-06-10 B001-017 IDA MCP audit reconfirms `lookup_funcs 0x00494af0` returns `Not a function`, `xrefs_to` and `callers` for `0x00494af0` are empty, `0x00494af0-0x00494b4b` is a 91-byte code-shaped body ending in `retn 4`, and `0x00494b4b-0x00494b50` is five `0xcc` bytes. The same audit reconfirms the constructor at `0x00494c80-0x00494daa`, draw virtual vtable data xref at `0x00617b78`, and the `ProgressBarControlPane` vtable stores at `0x00494cc5`, `0x00494ccb`, and `0x00494cd5`.

## Cross-Document Evidence

| Source | Confirmation |
| --- | --- |
| [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) | Lists this exact helper as `SetMaxValue(short maxValue)` and now emits the class declaration with child insertion; earlier provisional/no-C++ wording is superseded by the B005 source-quality closure. |
| [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) | Places this helper in `NexusTK/ui/controls/ProgressBarControlPane.cpp` with the constructor, draw virtual, vtables, and sibling projected helpers. |
| [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md) | Nests this body with the range-start setter and records the exact body/alignment map for `0x00494a90-0x00494b4b`. |
| [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md) | Confirms the sibling setter uses the same clamp/current/invalidation pattern for the lower bound field. |
| [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md) | Confirms the constructor initializes the same `0xf8`/`0xfa`/`0xfc` field triplet and installs the progress-bar vtables. |
| [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md) | Confirms the class vtable-data ownership that ties the helper's field family to `ProgressBarControlPane`, not the neighboring `ControlPane` or button-control families. |
| [UID:0001VH][PaneLayout](by-type/by-struct/PaneLayout.md) | Resolves complete-object `+0x44` as inherited `GrafPort::m_visibleBounds`; historical `m_bounds`/`m_localBounds` aliases are superseded. |
| Generated output | Historical command `000000016699` included one UID000117 body inside the class and exposed the old child-insertion defect. Callback command `000000016709` closed the class before qualified definitions, restored UID0003N1, and emitted one UID000117 body; the old below-95/no-code policy statement is superseded. |

## Touched State

| Offset / call | Evidence-backed role | Remaining uncertainty |
| --- | --- | --- |
| signed `short maxValue` | Incoming maximum candidate, clamped to `0..30000`. | Original spelling is inferred; signed width and role are exact. |
| object offset `0xfc` | Inherited signed `short ControlPane::m_rangeEnd`; semantically the ProgressBar maximum. | Physical member identity, offset, type, and role are resolved; the semantic alias `maximum` is explanatory only. |
| object offset `0xfa` | Inherited signed `short ControlPane::m_rangeCurrent`; lowered when above the new maximum. | Physical member identity, offset, type, and role are resolved; no derived shadow member exists. |
| object offset `0x44` | Inherited `GrafPort::m_visibleBounds` passed by address. | Resolved by current PaneLayout/GrafPort authority. |
| vtable slot `+0x20` | `Pane::InvalidateRect(const RectBounds *)` at `0x00544800`. | Resolved by the primary ProgressBar/Pane vtable cells and current Pane docs. |

## Historical B005 Source-Quality Closure

The best source-facing name is `ProgressBarControlPane::SetMaxValue(short maxValue)`. The body is source-authored progress-control logic: it clamps the argument, compares and writes the upper range field, conditionally lowers current, and invalidates through the inherited `ControlPane` virtual slot. The current executable still has no direct branch/call refs, no long conditional branch refs, and no VA/RVA pointer/table refs to `0x00494af0`; that remains a liveness caveat and confidence cap, not a no-code proof.

The B005 pass accepted `m_maxValue` for `+0xfc` and `m_currentValue` for `+0xfa`, but its inherited `m_bounds` spelling and `Invalidate(&m_bounds)` draft are now historical. Current PaneLayout/GrafPort authority resolves `+0x44` as inherited `m_visibleBounds`, and current Pane/vtable authority resolves inherited primary slot `+0x20`, `0x00617b54 -> 0x00544800`, as `Pane::InvalidateRect`. The exact helper body remains bounded by nine `0xcc` bytes at `0x00494ae7-0x00494af0` before it and five `0xcc` bytes at `0x00494b4b-0x00494b50` after it.

## B002 Exact Symmetric Source-Quality Reanalysis

- Healthy IDA MCP session `9b0396a3` independently decoded this sibling while researching UID000115.
- Exact body: `[0x00494af0,0x00494b4b)`, `0x5b` / 91 bytes, PE RVA `0x00094af0`, raw file offset `0x00093ef0`, SHA256 `97E860E2BD72C33445F80E40C704E7DB7B8F1B8583D9AB2547639294CAFC882C`.
- Boundary proof: nine `0xcc` bytes precede it at `0x00494ae7-0x00494af0`; five `0xcc` bytes follow at `0x00494b4b-0x00494b50`, before modeled UID0003N1.
- IDA has no function object at `0x00494af0`; raw disassembly nevertheless proves a complete non-virtual `void __thiscall` method with `ECX=this`, one signed-short stack argument, and `retn 4`.
- Exact symmetric machine flow:
  - signed negative input clamps to zero and signed input above decimal `30000` clamps to `30000`;
  - equality against inherited signed-short `m_rangeEnd` at `+0xfc` returns with no writes and no invalidation;
  - when inherited signed-short `m_rangeCurrent` at `+0xfa` is above the new maximum, current is written first and immediately invalidated;
  - every changed maximum is then written and invalidated;
  - invalidation count is exactly zero unchanged, one changed/current-in-range, or two changed/current-lowered;
  - it never writes inherited `m_rangeStart` at `+0xf8` and does not enforce `min <= max`.
- Reachability/liveness proof: every decoded sibling instruction/padding-start address has only internal flow xrefs and zero external xrefs. Searches found no little-endian VA pointer `F0 4A 49 00`, RVA pointer `F0 4A 09 00`, or raw-offset pointer `F0 3E 09 00`.
- Classification and names: retained source-authored but unreferenced `ProgressBarControlPane::SetMaxValue(short maxValue)`. Symmetry with UID000115, constructor-inline behavior, fields, RTTI/vtables, and PaneLayout reject compiler-only, aggregate-owned, unsigned, `SetRangeEnd`, raw-offset, `Invalidate`, and `m_bounds` alternatives.

## B003 Independent Source-Quality Closure

- Healthy MCP endpoint `http://127.0.0.1:13337/mcp`, HTTP session `f8ccba5f-0e8d-47c1-ae73-176144052b28`, and IDA database session `9b0396a3` independently rechecked this body against `NexusTK.exe.i64`; analysis, Hex-Rays, and strings were ready.
- A bounded read of `[0x00494ae7,0x00494b50)` reconfirmed nine leading `0xcc` bytes, the exact 91-byte source body, and five trailing `0xcc` bytes. Body SHA-256 remains `97E860E2BD72C33445F80E40C704E7DB7B8F1B8583D9AB2547639294CAFC882C`; first bytes are `55 8b ec 56 8b 75 08 57`, and final bytes are `52 20 5f 5e 5d c2 04 00`.
- All 33 instruction starts were screened for incoming references. Every observed edge is internal; `xrefs_to`, code-reference, data-reference, immediate, and little-endian VA/RVA/raw-offset searches returned no external entry route.
- Positive controls remained healthy: modeled current setter `0x00494b50` has PatchPane callers `0x005476ed`/`0x0054783f`, constructor `0x00494c80` has caller `0x005472d7`, draw `0x00494de0` is reached from vtable cell `0x00617b78`, and `0x00617b54` resolves `Pane::InvalidateRect` at `0x00544800`.
- Exact physical layout resolves the remaining source contradiction: [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane` already occupies complete size `0x108` and owns protected `m_rangeStart +0xf8`, `m_rangeCurrent +0xfa`, and `m_rangeEnd +0xfc`. PatchPane allocates exactly `0x108` for ProgressBarControlPane, so separate derived `m_minValue/m_currentValue/m_maxValue` members would duplicate occupied base storage and are rejected.
- Highest-probability human C++03 uses `SetMaxValue(short maxValue)`, inherited range members, class-scoped `kMaxProgressValue = 30000`, and `InvalidateRect(&m_visibleBounds)`. `SetRangeEnd`, derived shadow fields, `m_bounds`, generic `Invalidate`, blank C++, and compiler-residue classifications are retained only as rejected or superseded alternatives.

## Reconstruction Notes

- Reconstructable: true as a private progress-bar range helper.
- Parent: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md).
- C++: first-draft `ProgressBarControlPane::SetMaxValue(short maxValue)` is now populated. The source names are inferred/descriptive rather than PDB-proven, but the implementation stays within the exact helper range and expresses normal mid-2000s C++ class logic.
- Completion is `93` because exact bytes/hash/raw mapping, complete branch/write/call order, zero/one/two invalidation matrix, signed types, inherited physical layout, resolved API/member names, exhaustive liveness checks, symmetric ownership/routing, and storage-correct formal C++ are implementation-ready.
- Confidence is `94` because independent B002/B003 live MCP raw evidence, PE hashing, every decoded-address xref, three pointer encodings, target/constructor/current-setter corroboration, RTTI/vtables, exact ControlPane layout, PaneLayout, and generated/manual readbacks agree. It remains below `95` because no original method/member/constant symbols survive and the body has no surviving static entry route.

## Cross-References

- [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md)
- [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md)
- [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)

## Changes

- Before: completion/confidence were `50/65`, reconstruction metadata was blank, and the aggregate backlink used the stale `0x00494b48` path.
- Changed to: completion/confidence are `64/76`, the helper is marked reconstructable, attached to the `ProgressBarControlPane` class, and linked to the corrected aggregate path.
- Summary/evidence: IDA MCP byte review confirmed the exact helper body and following alignment; confidence remains below high-final levels because IDA still does not model this start as a function and the final source-level name is provisional.
- 2026-06-02:
  - Before: page had exact byte/boundary notes but no touched-state table or reconstruction rationale.
  - After: scored as `70/82` with field/call state mapped to the constructor and draw evidence.
  - Summary/evidence: recorded IDA byte review, constructor field layout, and the shared invalidation pattern support the stronger score; at that time no C++ emitted because final helper/field names were not source-quality. This historical state is superseded by the 2026-06-19 implementation.
- 2026-06-03 cross-documentation consistency pass:
  - Before: the page was assigned and behaviorally documented, but the score did not record the parent/file threshold check, aggregate nesting, sibling comparison, vtable-data support, or autogen no-code state.
  - After: raised `70/82` to `76/84` and added a cross-document evidence table; C++ remained blank at that time, superseded by the 2026-06-19 implementation.
  - Summary/evidence: existing written IDA evidence proves the raw body and alignment; the class/file/aggregate/sibling/constructor/vtable pages confirm ownership and field use.
- 2026-06-05 live IDA refresh:
  - Changed from `76/84` to `82/88`.
  - Removed stale receiver-offset wording and the old unavailable-IDA caveat.
  - Evidence: live `lookup_funcs`, `xrefs_to`, `callers`, and byte reads confirm the helper remains unmodeled and unreferenced, clamps the upper-bound input, conditionally lowers current value, invalidates through the vtable slot, ends at `0x00494b4b`, and is followed by five alignment bytes.
- 2026-06-10 B001-017 duplicate-aggregate audit:
  - Changed from `82/88` to `85/88`.
  - Evidence: live IDA MCP reconfirmed the exact helper bytes, empty xrefs/callers, `0xcc` alignment after the body, and constructor/draw/vtable ownership anchors. The parent class [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) and file [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) already clear `86/85`, so this exact child remains the source-bearing ProgressBarControlPane range while aggregate [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md) becomes non-emitting.
- 2026-06-19 B005 accepted source-quality implementation:
  - Changed from `85/88` to `87/89`.
  - Added first-draft `ProgressBarControlPane::SetMaxValue(short maxValue)` C++ and replaced the stale no-code/field-name blocker with accepted source-facing names, exact padding, clamp/current/invalidation behavior, inherited invalidation slot, and raw no-branch/call/VA/RVA-ref caveat.
- 2026-07-23 B002 UID000115 bounded sibling callback:
  - Changed from `87/89` to `91/93`; owner UID0000AW, reconstructable true, and emitter UID0000AW remain unchanged.
  - Added exact sibling SHA256/RVA/raw-offset, signed symmetric machine flow, zero/one/two invalidation matrix, all-decoded-address xref and VA/RVA/raw-offset pointer negatives, retained-unreferenced-source classification, rejected alternatives, and score rationale.
  - Replaced stale blank-C++/below-95 statements and historical `Invalidate(&m_bounds)` with current populated formal truth and `InvalidateRect(&m_visibleBounds)`, preserving older wording as superseded history.
- 2026-07-23 B003 inherited-storage implementation:
  - Changed from `91/93` to `93/94`; owner/emitter/reconstructable metadata remains unchanged.
  - Replaced impossible derived `m_maxValue/m_currentValue` physical members with inherited `ControlPane::m_rangeEnd/m_rangeCurrent`, added independent MCP/body/liveness controls, exact complete-object layout proof, rejected-alternative closure, and storage-correct C++03 formal source.
