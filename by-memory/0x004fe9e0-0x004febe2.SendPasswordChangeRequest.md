*** UID:0002QC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __stdcall SendPasswordChangeRequest(
    const wchar_t* accountName,
    const wchar_t* currentPassword,
    const wchar_t* newPassword)
{
    char packet[0x100];
    char converted[0x100];

    short accountNameLength = static_cast<short>(wcslen(accountName));
    short currentPasswordLength = static_cast<short>(wcslen(currentPassword));
    short newPasswordLength = static_cast<short>(wcslen(newPassword));

    PacketBufferWriteUInt8(0x26, packet);

    int accountNameByteLength = WideCharToMultiByte(0, 0, accountName, accountNameLength, converted, 0x100, NULL, NULL);
    if (accountNameByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[accountNameByteLength] = 0;
    PacketBufferWriteUInt8(accountNameByteLength, packet + 1);
    memmove(packet + 2, converted, accountNameByteLength);

    unsigned int packetLength = static_cast<unsigned int>(accountNameByteLength + 2);

    int currentPasswordByteLength = WideCharToMultiByte(0, 0, currentPassword, currentPasswordLength, converted, 0x100, NULL, NULL);
    if (currentPasswordByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[currentPasswordByteLength] = 0;
    PacketBufferWriteUInt8(currentPasswordByteLength, packet + packetLength);
    memmove(packet + packetLength + 1, converted, currentPasswordByteLength);
    packetLength += static_cast<unsigned int>(currentPasswordByteLength + 1);

    int newPasswordByteLength = WideCharToMultiByte(0, 0, newPassword, newPasswordLength, converted, 0x100, NULL, NULL);
    if (newPasswordByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[newPasswordByteLength] = 0;
    PacketBufferWriteUInt8(newPasswordByteLength, packet + packetLength);
    memmove(packet + packetLength + 1, converted, newPasswordByteLength);
    packetLength += static_cast<unsigned int>(newPasswordByteLength + 1);
    if (packetLength >= 0x100)
        __report_rangecheckfailure();

    packet[packetLength] = 0;
    g_packetSender->QueueAndSendPacket(
        packet, static_cast<short>(packetLength));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready active opcode `0x26` password-change sender for account/current/new-password fields. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fe9e0-0x004febe2 - SendPasswordChangeRequest

## Status

- Disposition: reconstructable NexusTK login/account packet sender.
- Rebuild handling: source-authored helper.
- Confidence: strong for owner, range, caller, behavior, and source-ready first-draft C++.
- Autogen parent: [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md).
- Parent aggregate: [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).

## Address Range

| Range | Kind | Notes |
| --- | --- | --- |
| `0x004fe9e0-0x004febe2` | function | IDA `sub_4FE9E0`, size `0x202`; three-argument stack-call helper. |
| `0x004febe2-0x004febf0` | following padding | Fourteen `0xcc` bytes before the reply handler; recorded in [UID:0000VN][-ignored](by-memory/-ignored.md). |

## Behavior

Builds and sends the active password-change request packet. The helper takes three UTF-16 strings from the dialog submit path: `accountName`, `currentPassword`, and `newPassword`, corresponding to controls `3`, `4`, and `5` from [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md). It writes opcode `0x26`/decimal `38` (Verified with `tools/int_convert.py`), converts each string to a multibyte packet field with a one-byte length prefix, bounds each converted field and final packet length against `0x100`/decimal `256` (Verified with `tools/int_convert.py`), and sends the assembled packet through the active network/session object.

The zero writes are local string/buffer terminators, not sent protocol fields. `PacketBufferWriteUInt8` writes a scratch zero after one-byte values and the function writes `packet[packetLength] = 0` before send, but the sent length excludes the final zero and field payload copies are length-counted bytes only. This target does not serialize control `6`/confirmation, does not parse `_wtol`, and does not append a `PacketBufferWriteUInt32BE` account/id field; those behaviors belong to retained sibling [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md).

## Evidence Notes

- IDA MCP `lookup_funcs 0x004fe9e0` reports function size `0x202`, ending at `0x004febe2`.
- IDA MCP `callers 0x004fe9e0` and `xrefs_to 0x004fe9e0` both report the direct call at `0x004fe778` inside [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md) submit logic.
- Historical IDA MCP decompile identified an `int __stdcall sub_4FE9E0(const unsigned __int16 *a1, const unsigned __int16 *a2, const unsigned __int16 *a3)` shape. B002's 2026-07-24 current tinfo re-read below supersedes the return claim with the applied current type `void __stdcall(const unsigned __int16 *, const unsigned __int16 *, const unsigned __int16 *)`, matching the accepted source formal.
- IDA MCP decompile shows the opcode write `sub_575380(38, &Src)`, three `WideCharToMultiByte` conversions, length writes through `sub_575380`, payload copies through `sub_516220`, and final `sub_574BB0(dword_67A7EC, &Src, v14)`.
- IDA MCP `callees 0x004fe9e0` reports `sub_575380`, `sub_516220`, `sub_574BB0`, `WideCharToMultiByte`, `sub_516030`, `___report_rangecheckfailure`, and `@__security_check_cookie@4`.
- 2026-06-15 live IDA MCP refresh reconfirmed `sub_4FE9E0` size `0x202`/514 bytes, the sole caller `sub_4FE560` at `0x004fe778`, the same callee set, a 12-block/complexity-7 helper body, and fourteen `0xcc` bytes at `0x004febe2-0x004febf0` before the reply handler.
- 2026-06-15 live IDA MCP decompile/assembly shows the stack packet buffer, opcode `0x26`/38, per-field `0x100`/256-byte conversion buffers, three `>= 0x100` overflow guards, a final aggregate `< 0x100` guard, and the `dword_67A7EC` send through `sub_574BB0`.
- 2026-06-26 B009 MCP-backed source-quality pass used IDB session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, with `server_health` reporting `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, Hex-Rays available, strings cache ready, and worker/process id `26892`.
- B009 `lookup_funcs` reconfirmed `0x004fe9e0` as `sub_4FE9E0`, size `0x202`/decimal 514 (Verified with `tools/int_convert.py`); `0x004febe1` remains inside the function; `0x004febe2` is not a function; and `0x004febf0` starts `sub_4FEBF0`.
- B009 route checks reconfirmed exactly one active code xref to this target: `xrefs_to 0x004fe9e0` and `find code_ref 0x004fe9e0` both return the call at `0x004fe778` inside `sub_4FE560`. The paired retained helper at `0x004fe790` has no code xref, data ref, immediate, or little-endian byte-pattern pointer hit in the same pass.
- B009 range checks reconfirmed the leading boundary/padding relationship, the end path with `retn 0Ch`, the range-check-failure tail, and fourteen `0xcc` bytes at `0x004febe2-0x004febf0` before `sub_4FEBF0`. `make_signature_for_range 0x004fe9e0-0x004febe2` returned `unique:true`.
- B009 `analyze_function` and `decompile` reconfirmed the ABI as `int __stdcall sub_4FE9E0(const unsigned __int16 *a1, const unsigned __int16 *a2, const unsigned __int16 *a3)`, with 12 basic blocks and cyclomatic complexity 7. The decompiler's `int` result is the tail-call value from `QueueAndSendPacket`, not proof of meaningful source-level return use; the accepted source-facing C++ uses `static void __stdcall` like retained sibling [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md).
- B009 body checks reconfirmed `sub_575380(38, &Src)`, three `wcslen` lengths, three `WideCharToMultiByte(0, 0, ..., length, converted, 0x100, NULL, NULL)` conversions, three `>= 0x100` field guards, one-byte length writes, three payload copies through [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md), final aggregate packet-length guard, local-only `packet[packetLength] = 0`, and final `sub_574BB0(dword_67A7EC, &Src, packetLength)`.
- B009 submit-caller checks reconfirmed controls `3`, `4`, `5`, and `6` are read and copied into 16-wide-character buffers, control `5` is digit-checked and compared against control `6`, and the success path calls this helper at `0x004fe778` with controls `3`, `4`, and `5` only. The source-facing parameter names `accountName`, `currentPassword`, and `newPassword` are inferred from this caller and from accepted sibling [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) formal C++.
- Decimal conversions for `0x202`, `0x26`, `0xff`, and `0x100` verified with `tools/int_convert.py`.

## 2026-07-24 Current Container And IDA Handoff Recheck

- B002 re-read the function against live IDA MCP session `dc9b367a-4569-4166-8fa6-4d2bcb44d3d2`, IDB session `1ce787ed`, at shared-IDB checkpoint SHA-256 `F28C8B463F64693561E118790FD5E9D68739F85C697B9B58F391F56E1911ED99`. Later unrelated IDB saves may supersede that file checkpoint without changing the accepted facts.
- Exact current prestate: function `[0x004fe9e0,0x004febe2)`, name `sub_4FE9E0`, size `0x202`, type `void __stdcall(const unsigned __int16 *, const unsigned __int16 *, const unsigned __int16 *)`, entry bytes `55 8B EC 81 EC 14 02 00 00 A1 24 2F 67 00 33 C5`, and absent entry-regular, entry-repeatable, function-regular, and function-repeatable comments.
- The sole start xref remains the direct active call at `0x004fe778` from `SubmitPasswordChange`; there is no fourth argument. The caller reads controls `3/4/5/6`, validates the new-password digit and control-`6` confirmation, then passes only controls `3/4/5` as `accountName`, `currentPassword`, and `newPassword`.
- Runtime body remains opcode `0x26`, three `WideCharToMultiByte` conversions, one-byte field lengths, length-counted payload copies, per-field/final `0x100` guards, local-only terminators, and `QueueAndSendPacket(g_packetSender, packet, packetLength)`. Score/formal/owner/emitter remain `91/91`, the existing source-ready static helper, and UID0000I3.
- Parent relationship is now explicit: UID00022W is a reviewed false/non-emitting physical index, while this exact child remains the active source owner for `[0x004fe9e0,0x004febe2)`. Parent reclassification does not alter this formal or route.
- Supervisor Gate 2B handoff, not applied by B002: rename to `SendPasswordChangeRequest`, apply `void __stdcall SendPasswordChangeRequest(const wchar_t *accountName, const wchar_t *currentPassword, const wchar_t *newPassword)`, and set only the function repeatable comment to `Active ChangePasswordDialogPane opcode 0x26 sender called only from 0x004fe778; serializes accountName, currentPassword, and newPassword as one-byte-length multibyte fields.` Preserve range, bytes, sole xref, and absent function-regular/entry-regular/entry-repeatable comments.

## Score Rationale

Raised to `91/91` by the B009 accepted source-quality implementation on 2026-06-26. The page now has refreshed MCP evidence for exact range, unique signature, caller route, ABI, body layout, callee set, padding boundary, opcode, field packing, bounds checks, local-only terminators, and session-send side effect. The active caller proves the field roles well enough for source-facing names `accountName`, `currentPassword`, and `newPassword`, and accepted sibling [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) supplies matching packet-helper style. The direct [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) owner/emitter route remains unchanged.

Scores stay below final-audit range because the exact original helper name, exact original parameter/local spellings, and source-level void-vs-int return spelling are reconstructed from behavior and sibling style rather than recovered from original symbols.

## Touched State And Calls

| Reference | Role |
| --- | --- |
| `dword_67A7EC` | Active network/session sender object. |
| `sub_574BB0` / `QueueAndSendPacket` | Sends the packet buffer through Socket-owned [UID:0000Q5][g_packetSender](by-global/g_packetSender.md). |
| `sub_575380` / `PacketBufferWriteUInt8` | Writes opcode and one-byte field length values; its spare zero is local scratch, not independent sent payload. |
| `sub_516220` / `MemoryMan::MemmoveWrapper` | Binary payload-copy wrapper represented as source-level `memmove(...)` in the formal C++. |
| `WideCharToMultiByte` | Converts UTF-16 dialog fields to multibyte packet payloads. |
| `___report_rangecheckfailure` | Handles field/buffer overflow guard failures. |

## Reconstruction Notes

Formal C++ is now present as a file-local `static void __stdcall SendPasswordChangeRequest(const wchar_t* accountName, const wchar_t* currentPassword, const wchar_t* newPassword)`. The code keeps the binary behavior: `0x100` stack packet/conversion buffers, `wcslen` source lengths, `WideCharToMultiByte`, range-check failure paths, length-counted field payload copies, final local terminator, and `QueueAndSendPacket(g_packetSender, packet, packetLength)`.

The source-facing names are inferred rather than symbol-proven. They are preferred over raw `a1`/`a2`/`a3` because [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md) proves controls `3`, `4`, and `5` are the active transmitted fields, control `5` is the new password checked against confirmation control `6`, and accepted [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) uses the same first-three-field naming style. Keep this helper with [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) rather than Socket, PacketBuffer, MemoryMan, [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md), or no-owner/no-code: those rejected alternatives are callees/support or no-route siblings, not direct source owners for this active packet builder.

## Cross-References

- [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md)
- [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md)
- [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md)

## Changes

### 2026-08-14 B003 Socket API closure

- The active file-local sender now calls `g_packetSender->QueueAndSendPacket(packet, static_cast<short>(packetLength))`, matching the reconstructed Socket member declaration and eliminating the undocumented free-function facade.
- The source behavior remains exact for `0x004fe9e0-0x004febe2`: opcode `0x26`, three one-byte-length converted fields, local-only terminators, all `0x100` guards, and the sole active caller at `0x004fe778` from `SubmitPasswordChange`.
- UID0000I3 remains owner/emitter at `91/91`; H remains blank because UID00001L's CPP preamble owns the file-local forward and `Socket.h` owns the member/global declarations.

- 2026-07-24 B002 UID00022W aggregate implementation callback support sync:
  - Score/formal/owner/emitter remain `91/91`, existing source-ready active-helper C++, and UID0000I3.
  - Added current exact void tinfo, sole-caller evidence, active field mapping, false-parent relationship, and complete supervisor-only IDA action/readback handoff; historical int-return wording is now explicitly superseded.
- 2026-06-26 B009 accepted source-quality implementation:
  - What existed before: score was `85/89`, formal C++ was blank, behavior text implied a sent zero terminator, and field/helper source names were still open.
  - Changed to: `91/91` with formal source-ready C++ for `static void __stdcall SendPasswordChangeRequest(const wchar_t* accountName, const wchar_t* currentPassword, const wchar_t* newPassword)`.
  - Summary/evidence: B009 MCP session `80de0a67` reconfirmed exact range `0x004fe9e0-0x004febe2`, unique signature, sole active caller `0x004fe778`, three `WideCharToMultiByte` fields, one-byte length writes through `PacketBufferWriteUInt8`, payload copies represented as `memmove(...)`, local-only terminators outside the explicit sent length, and final `QueueAndSendPacket(g_packetSender, packet, packetLength)`.
  - Rejected alternatives preserved: Socket/network owner, PacketBuffer owner, MemoryMan owner, [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) replacement, and no-owner/no-code. Socket owns [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), PacketBuffer owns scalar writers, MemoryMan owns the binary `memmove` wrapper, and [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) remains the retained no-route account/id variant.
