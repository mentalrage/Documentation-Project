*** UID:0004M4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007O | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007O | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool MainMenuPane::HandlePacketEvent(Event *event)
{
    const unsigned char *packet =
        static_cast<const unsigned char *>(event->m_payload.m_packet.m_data);
    if (packet == NULL)
        return false;


## 2026-07-15 UID0003UP Config-Field Synchronization

- The complete dispatcher body, score, owner/emitter, source route, and all packet cases remain unchanged.
- Current base Config layout names the canonical server-directory block `m_serverDirectory`. Case `0x5b` consumes the ANSI `m_loginServerAddress` together with startup port/mode fields; it does not reinterpret the high endpoint or music buffers.
- The launcher storage is a typed protocol-view union. Earlier incomplete/opaque launcher and endpoint-role wording is historicalized, while exact packet ordering, UI behavior, and unrelated cases remain intact.
    switch (packet[0]) {
    case 0x00:
        switch (packet[1]) {
        case 0:
            InitializeMainMenuMetaData();
            g_pMetaMan->LoadMetaDatAndRequestSync();
            break;

        case 1:
            new AlertPane(
                g_pLanguageMan->GetLocalizedString(88),
                this,
                g_mainMenuAlertButtons,
                NULL);
            break;

        case 2:
        case 3: {
            unsigned char patchInfo[0x10606];
            BuildMainMenuPatchInfo(packet, patchInfo, sizeof(patchInfo));
            DestroyActiveScreenDimmer();

            if (packet[1] == 2) {
                new PatchPane(patchInfo);
            } else {
                File patchFile(L"Patch/Info", L"wb");
                if (patchFile.Write(patchInfo, sizeof(patchInfo)) != sizeof(patchInfo))
                    throw Win32Error(GetLastError());
                new PatchPane2(patchInfo);
            }
            return true;
        }
        }
        return false;

    case 0x03:
        return HandleLegacyMainMenuPacket(this, packet);

    case 0x49:
    case 0x6a:
        if (g_pTransferServerDialog != NULL)
            CloseDialogPane(g_pTransferServerDialog);
        return false;

    case 0x56: {
        unsigned char unpacked[2048];
        const unsigned short compressedLength = PacketBufferReadUInt16BE(packet + 1);
        InflatePacketBuffer(packet + 3, compressedLength, unpacked, sizeof(unpacked));

        ServerSelectDirectory &directory = g_pConfig->m_serverDirectory;
        directory.ReleaseOwnedBuffers();
        directory.entryCount = unpacked[0];

        const unsigned char *cursor = unpacked + 1;
        for (int index = 0; index < directory.entryCount; ++index) {
            ServerSelectEntry &entry = directory.entries[index];
            entry.serverId = cursor[0];
            entry.address[0] = cursor[4];
            entry.address[1] = cursor[3];
            entry.address[2] = cursor[2];
            entry.address[3] = cursor[1];
            entry.port = PacketBufferReadUInt16BE(cursor + 5);

            char text[256];
            strcpy_s(text, sizeof(text), reinterpret_cast<const char *>(cursor + 7));
            char *context = NULL;
            char *primary = strtok_s(text, ";", &context);
            char *secondary = strtok_s(NULL, ";", &context);
            if (secondary == NULL) {
                context = NULL;
                primary = strtok_s(text, ":", &context);
                secondary = strtok_s(NULL, ":", &context);
            }

            AssignServerSelectEntryText(&entry.name, primary != NULL ? primary : "");
            AssignServerSelectEntryText(&entry.description, secondary != NULL ? secondary : "");
            AssignServerSelectEntryText(&entry.agreementText, " ");
            cursor += strlen(reinterpret_cast<const char *>(cursor + 7)) + 8;
        }

        if (g_pServerSelectPane == NULL)
            new ServerSelectPane(&directory);
        return false;
    }

    case 0x5b: {
        int offset = 1;
        StringBase addressText;
        PacketBufferReadAnsiBytes16(&addressText, packet, &offset);
        g_pConfig->m_loginServerAddress = addressText;
        g_pConfig->m_loginServerPort = PacketBufferReadUInt16BE(packet + offset);
        offset += 2;
        g_pConfig->m_auxiliaryServerPort = PacketBufferReadUInt16BE(packet + offset);
        offset += 2;
        g_pConfig->m_serverMode = packet[offset];
        return false;
    }

    case 0x5e:
        if (g_pUserPane != NULL)
            g_pTimerMgr->RemovePendingTimersForOwner(&g_pUserPane->m_timerHandler);
        if (g_pTransferServerDialog != NULL)
            return true;
        new TransferServerDialogPane(true);
        return true;

    case 0x66:
        return HandleAnsiTextUrlDialogPacket(packet);

    case 0x6f:
        return g_pMetaMan->HandleMainMenuPacket(packet);
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Pre-login packet override with null-data rejection; exact 0x00 nested metadata/alert/PatchPane/PatchPane2 paths, 0x03 legacy helper, 0x49/0x6a transfer close, 0x56 compressed server directory, 0x5b config endpoint state, 0x5e transfer dialog, 0x66 ANSI URL/text helper, 0x6f MetaMan route, and parent-only compiler tables. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f6d80-0x004f75ba - MainMenuPane HandlePacketEvent

## Exact Range And Route

- Exact half-open range `0x004f6d80-0x004f75ba`, size `0x83a`; SHA256 `db6d1f1bb79304941b4b8284be1cd364a12a63f8f02095fd032cf0477f5f546a`.
- EventHandler secondary vtable slot `+0x10` at `0x0061d2c0` proves `HandlePacketEvent`, superseding historical `OnServerMessage` as the primary name.
- `0x004f75ba-0x004f7660` is parent-only compiler support: two NOPs, nine top-level branch dwords, 112 selector bytes, and four nested dwords. Combined table SHA256 is `1d4e572333cec56af0737c0281d2dbb414bbec7625f024e4bc9ba8b432cdc1f6`.

## Packet Behavior And Data Flow

- Packet data comes from Event packet payload at `event+0x0c`; null returns false.
- Opcode `0x00`: subcommand 0 initializes metadata and requests MetaMan sync; 1 opens localized alert 88; 2/3 build the exact `0x10606` patch-info block, remove active dimmer, and construct PatchPane or write `Patch/Info` then construct PatchPane2. The `0x004f72f7` Application DAT filename call is consumer evidence for patch metadata, not MainMenu field ownership.
- Opcode `0x03` factors through accepted [UID:00019O][0x004f8d00-0x004f8f02.LegacyMainMenuPacket](by-memory/0x004f8d00-0x004f8f02.LegacyMainMenuPacket.md). `0x49/0x6a` close transfer dialog and return false.
- Opcode `0x56` is independent construction evidence for the canonical server-directory types. It inflates a bounded 2048-byte payload, releases old `m_serverDirectory` strings, sets `entryCount`, and fills each 0x18-byte `ServerSelectEntry`: `serverId`; reversed wire-order `address[0..3]`; big-endian unsigned-short `port`; semicolon-or-colon `name` and `description`; and one-space `agreementText`. It then passes the exact `ServerSelectDirectory *` to `ServerSelectPane` only when the singleton is absent.
- Opcode `0x5b` reads ANSI address text, two big-endian ports, and mode into Config. `0x5e` removes UserPane timers, guards transfer-dialog singleton, and creates `TransferServerDialogPane(true)`. `0x66` factors through accepted ANSI URL/text helper [UID:00022Y][0x00502e10-0x0050305c.MainMenuAnsiTextDialogPacketHelpers](by-memory/0x00502e10-0x0050305c.MainMenuAnsiTextDialogPacketHelpers.md); `0x6f` delegates to MetaMan.

## Compiler And Confidence Boundaries

- Top branch dwords are `0x004f7100,0x004f74b0,0x004f7497,0x004f6ddc,0x004f74cf,0x004f743e,0x004f74c1,0x004f7565,0x004f7167`; nested dwords are `0x004f7110,0x004f7124,0x004f7187,0x004f7187`. Data xrefs come only from this switch, so manual table C++ and separate emitting table children are rejected.
- Packet offsets/branches/callees are binary-backed. Descriptive helper and protocol spellings cap confidence but do not leave a no-code blocker.

## 2026-08-10 B010 UID0002PA Accepted Callback Synchronization

- Score, owner/emitter, range, packet dispatch, and every unrelated packet case remain unchanged.
- Case `0x56` now uses the exact `ServerSelectDirectory`/`ServerSelectEntry` contract and directly populates all semantic fields before constructing `ServerSelectPane`; the old reinterpret cast is unnecessary.
- Historical `ConfigEntryBlock`, `ConfigEntry`, selector/flags, primary/secondary/tertiary text, generic address helper, and safe generic-record assumptions are superseded as current source shape but remain represented by the prior dated source history.
