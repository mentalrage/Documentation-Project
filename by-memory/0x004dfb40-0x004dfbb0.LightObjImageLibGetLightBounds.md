*** UID:0002IV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000076 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000076 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void LightObjImageLib::GetLightBounds(int lightIndex, RectBounds *outBounds)
{
    const LightInfo *lightInfo = &m_lightInfos.m_defaultEntry;
    if (lightIndex >= 0 && lightIndex < m_lightInfos.m_count)
        lightInfo = &m_lightInfos.m_entries[lightIndex];
    if (lightInfo->generatedFrame != 0) {
        InitRectBounds(outBounds, -lightInfo->radiusX, -lightInfo->radiusY,
            lightInfo->radiusX + 1, lightInfo->radiusY + 1);
        return;
    }
    InitRectBounds(outBounds, 0, 0, 0, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Selects a LightInfo record or fallback and emits local bounds from the radii when a generated frame exists; live IDA confirms exact 0x70 body, one external caller at 0x0053c82b, no data refs, 0x1c LightInfo stride, fallback this+0x14, frame pointer +0x18 gate, and rectangle setter dependency. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dfb40-0x004dfbb0 LightObjImageLib Get Light Bounds

## UID0001PR Accepted Bounds Source Route - 2026-07-21

- This authored non-virtual helper emits under [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) at position `40`. The class/file route and the sole caller at `0x0053c82b` establish its `void` out-parameter signature despite the decompiler's incidental tail-return value.
- The accepted member expressions use the one `ProtectedArray<LightInfo> m_lightInfos`: the inline default row at `m_defaultEntry`, guarded array selection through `m_count`, and indexed `m_entries` access. Historical direct-field labels below remain binary shorthand and are superseded as source spelling.
- The output behavior is exact: a non-null generated frame yields `{-radiusX,-radiusY,radiusX+1,radiusY+1}`; a null frame yields `{0,0,0,0}`. There is no null check for `outBounds`, no allocation, no draw side effect, and no status return consumed by the caller.
- The exact function range remains `[0x004dfb40,0x004dfbb0)`, following ten bytes of destructor padding and ending directly at DrawLightMode1. Its only modeled callee is the shared four-integer rectangle initializer.

## Status

- Owner class: [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md).
- Likely source file: [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).
- Entity kind: method/helper.
- IDA function: `sub_4DFB40`, exact function bounds `0x004dfb40-0x004dfbb0`.
- Rebuild handling: source-authored NexusTK project code.
- Autogen parent: [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) at position `40`; the class emits through [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).

## Behavior

This helper selects a [UID:0001UW][LightInfo](by-type/by-struct/LightInfo.md) record by index, falls back to the embedded default record at `this + 0x14` when the index is out of range, and writes a rectangle describing the light frame's local bounds.

When the selected record has a generated frame pointer at `LightInfo + 0x18`, the output rectangle is:

```text
left   = -radiusX
top    = -radiusY
right  =  radiusX + 1
bottom =  radiusY + 1
```

If the selected record has no generated frame, the helper writes an empty `0,0,0,0` rectangle. IDA shows the rectangle write is delegated to `sub_4B7C50`, the shared four-integer rectangle setter used throughout the client.

## Evidence

- IDA MCP `lookup_funcs` on 2026-06-06 reports `sub_4DFB40` with size `0x70`; `0x004dfbb0` is the exclusive end and the next modeled child is `DrawLightMode1`.
- IDA MCP decompilation shows `this[2]` as the count, `this[3]` as the entry buffer, `28 * index` as the row stride, and `this + 5` / `this + 0x14` as the fallback record pointer.
- IDA disassembly confirms the function copies the first `0x18` bytes of the selected record to the stack with `movups`/`movq`, checks the generated frame pointer at `+0x18`, negates `radiusX`/`radiusY`, increments positive extents, and calls `sub_4B7C50(outRect, left, top, right, bottom)`.
- IDA MCP `callees(0x004dfb40)` reports only `sub_4B7C50`.
- IDA MCP `xrefs_to(0x004dfb40)` and `idautils.CodeRefsTo` both report a single code ref at `0x0053c82b`; there are no data refs to the helper.

## Caller Context

The single caller is `sub_53C810` at `0x0053c82b`. Its decompilation reads [UID:0001PR][0x0069b450-0x0069b454.g_pLightObjImageLib](by-memory/0x0069b450-0x0069b454.g_pLightObjImageLib.md), passes `this[74]` as the light index, and writes the result into the caller-supplied rectangle before translating it by attachment/world offsets through `sub_4B7E10`. That caller-side use confirms this method returns local bounds rather than drawing or allocating light data.

## Boundaries

The raw ordinary destructor ends before the alignment padding at `0x004dfb36-0x004dfb40`; this helper starts at `0x004dfb40` and ends at `0x004dfbb0`, immediately before `DrawLightMode1`. IDA byte review on 2026-06-06 confirms `0x004dfb36-0x004dfb40` is ten `0xcc` bytes.

## Source Shape

Best source-facing signature: `void LightObjImageLib::GetLightBounds(int lightIndex, RectBounds *outBounds)`. IDA's incidental integer return comes from tail-returning the shared rectangle initializer result; the only caller ignores the return and all semantic output is through `outBounds`.

The caller at `0x0053c810` is [UID:0001DH][0x0053c700-0x0053c92e.AttachmentAnchorResolver](by-memory/0x0053c700-0x0053c92e.AttachmentAnchorResolver.md) consumer context. Fresh 2026-06-17 B003 reanalysis shows the caller loads [UID:0001PR][0x0069b450-0x0069b454.g_pLightObjImageLib](by-memory/0x0069b450-0x0069b454.g_pLightObjImageLib.md) into `ecx`, pushes the output `RectBounds*`, pushes the caller light index at `+0x128`, calls this method at `0x0053c82b`, then translates the returned local rectangle through `sub_4B7E10`. That confirms `0x0053c810` validates the local-bounds role but is not owner/emitter for this LightObjImageLib method.

## Score Rationale

Completion is `92` because the exact body, boundary, only modeled callee, only direct caller, row stride, ProtectedArray/default-row route, generated-frame gate, output rectangle semantics, source-facing name/signature, class position, and exact formal C++ are documented. Confidence is `94` because the method is a compact exact `thiscall` body with one caller, one callee, stable `LightInfo`/`RectBounds` support, and no unresolved split, owner, member, or emission blocker. Original debug-symbol spelling remains unavailable, but the selected source names are behaviorally and structurally constrained.

## Cross-References

- [UID:00017P][0x004df7e0-0x004dfd0a.LightObjImageLibLocalMethodCluster](by-memory/0x004df7e0-0x004dfd0a.LightObjImageLibLocalMethodCluster.md)
- [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md)
- [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md)
- [UID:0001UX][LightObjImageLibLayout](by-type/by-struct/LightObjImageLibLayout.md)
- [UID:0001UW][LightInfo](by-type/by-struct/LightInfo.md)
- [UID:0001PR][0x0069b450-0x0069b454.g_pLightObjImageLib](by-memory/0x0069b450-0x0069b454.g_pLightObjImageLib.md)

## Changes

- 2026-06-06 A008 live IDA refresh:
  - What existed before: the page was `74/84`, had no autogen parent, and listed only broad decompile/disassembly evidence plus the caller address.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID:0000KP`, and expanded evidence for the exact `0x70` body, sole callee, sole direct caller, no data refs, padding, and caller-side bounds use.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `decompile`, `disasm`, `callees`, `xrefs_to`, and `py_eval`/`idautils` checks confirmed the helper's half-open boundary, `0x1c` LightInfo stride, fallback `this + 0x14` record, frame-pointer `+0x18` gate, `sub_4B7C50` rectangle setter dependency, and single caller at `0x0053c82b`.
- 2026-06-17 B003 source-quality execution:
  - Before: `82/88`, with method-name and caller-side ownership still listed as caveats and formal C++ blank.
  - After: `88/92`, owner/emitter unchanged through [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md), and formal first-draft C++ populated for `LightObjImageLib::GetLightBounds`.
  - Summary/evidence: B003 rechecked current docs, IDA MCP function/xref/caller/decompile/disassembly evidence, rectangle helpers, `LightInfo`/`LightObjImageLibLayout` support, and generated route state. The only caller is an `AttachmentAnchorResolver::ComputeScreenBounds` consumer that translates the local rectangle after this method returns.
