*** UID:00042E | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: MapPane-owned raw no-function queue helper for timer id `6`; stores/converts pending target coordinates, sleeps, gates and sets canonical bool `m_queuedMapStateRefresh`, schedules through the inherited TimerHandler facet, calls `Pane::CaptureMouse`, and remains non-emitting because no function, xref, pointer, vtable, generated-output, or source-declaration route is currently proven. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00505c00-0x00505cb1 MapPane Queue Movement Refresh Timer6

## Status

- Disposition: reconstructable NexusTK project code with no current emission route.
- Entity kind: source-shaped raw no-function MapPane queue helper, not padding.
- Direct owner: [UID:00007Q][MapPane](by-class/MapPane.md). The body uses the MapPane object in `ECX`, stores it in `EDI`, touches MapPane state at `+0x39d`, `+0x3fc`, `+0x400`, `+0x8ec`, and `+0x8f0`, and schedules through the MapPane timer facet at `this+0xa4`.
- Emitter route: intentionally blank. Current evidence proves ownership and behavior but not a callable source route, pointer route, vtable route, or generated-output route.
- Formal C++: intentionally blank. A mechanical translation is possible from bytes, but it would fabricate a source declaration for raw code that is not modeled as a function and has no proven entry route.

## Live MCP Recheck

Fresh B010 evidence used narrow, paged, schema-current MCP calls against session `supervisor_resume_20260629`.

- MCP availability: `idb_list` request `5` reported one active IDB session; `server_health` request `6` reported OK.
- Function modeling: `lookup_funcs` request `11` reported `0x00505bf8`, `0x00505c00`, `0x00505cae`, `0x00505cb1`, `0x00505d00`, and `0x00505da0` as not functions. It reported the modeled successor `0x00505cc0` as `sub_505CC0` size `0x39`, plus timer/notify siblings `0x00508f60` as `sub_508F60` size `0xcb` and `0x00509030` as `sub_509030` size `0x409`.
- Cross-references: `xrefs_to` request `12` found no refs to `0x00505c00`, no refs to `0x00505cb1`, and only the internal fallthrough-style code xref to the `0x00505cae` return from `0x00505cad`. It also confirmed the modeled successor `0x00505cc0` has normal refs.
- Bytes and boundaries: `get_bytes` request `13` confirmed `0x00505bf8-0x00505c00` is 8 bytes of `0xcc`, `0x00505c00-0x00505cb1` is the 177-byte raw body, and `0x00505cb1-0x00505cc0` is 15 bytes of `0xcc`.
- Scoped instructions: `insn_query` request `14` over `0x00505c00-0x00505cb1` returned 50 instructions, beginning with `push ebp; mov ebp, esp; cmp byte_66DA97, 1` and ending with `retn 8` at `0x00505cae`.
- Pointer-route checks: exact `find_bytes` request `15` for little-endian `0x00505c00`, `push 0x00505c00`, and the target control pattern returned zero hits.
- Sibling context: decompile requests `16` and `17` covered `0x00509030` and `0x00508f60`, the modeled timer/notify context that owns the live MapPane timer state. Vtable xref request `18` showed data xrefs to those modeled virtual entries but no route to this raw body.
- Paired clearers: decompile requests `19` and `20` showed `sub_505CC0` clearing pending movement state `+0x39d` and cancelling timer ids `6` and `8`, and `sub_505DE0` clearing pending action-poll state `+0x39e` and cancelling timer id `7`.
- Helpers and globals: `lookup_funcs` request `21` identified `sub_5975E0` as the scheduler, `sub_597610` as the cancel helper, and distinct wrappers `sub_5449D0` and `sub_5449F0`. `get_global_value` request `22` recorded the current guard/config globals used by this body.
- Refresh wrapper distinction: decompile requests `23` and `24` showed `0x005449d0` calls `sub_4A6F40(unk_67AB30, this ? this+0xa0 : 0)`, while `0x005449f0` calls `sub_4A6F40(unk_67AB30, 0)`. Xref request `25` confirmed UID00042E uses `0x005449d0`; clear/notify paths use the zero-context wrapper.

## Range And Boundary Evidence

| Span | Evidence | Interpretation |
| --- | --- | --- |
| `0x00505bf8-0x00505c00` | MCP `get_bytes` request `13`: 8 bytes of `0xcc`. | Pre-target alignment. |
| `0x00505c00-0x00505cb1` | MCP `insn_query` request `14`: 50 decoded instructions, normal stack frame, MapPane field access, helper calls, `retn 8`. | Real raw source-shaped body. |
| `0x00505cae` | MCP `lookup_funcs` request `11`: not a function; `xrefs_to` request `12`: only the internal code xref from `0x00505cad`. | Return inside this raw body, not an external entry. |
| `0x00505cb1-0x00505cc0` | MCP `get_bytes` request `13`: 15 bytes of `0xcc`. | Post-target alignment before modeled `sub_505CC0`. |

## Behavior

Source-facing names below are descriptive and are grounded in field use and sibling MapPane docs; they are not proof of original symbol spelling.

- Entry and object: `ECX` is the MapPane object pointer and is saved in `EDI`; the body returns with `retn 8`, consuming two caller-supplied arguments.
- Optional coordinate conversion guard: the body first checks `byte_66DA97 == 1` and then checks a config byte reached through `g_pConfig + 0x28de5b`. If either guard fails, it skips the conversion block.
- Pending coordinate stores: when the conversion block runs, it stores the two arguments from `[ebp+8]` and `[ebp+0xc]` into `MapPane+0x8ec` and `MapPane+0x8f0`.
- Tile conversion: the stored pair is divided through the current tile dimensions (`word_66DAA0` and `word_66DA9C`) and then overwritten with origin-adjusted tile coordinates using MapPane origin fields at `+0x400` and `+0x3fc`.
- Throttle: the body always reaches `Sleep(100)` after the optional conversion path.
- Queue gate: after the sleep, the body requires `MapPane+0x39d == 0` and global busy state `unk_69B4CC == 0`. If either value blocks the queue, the helper returns without scheduling.
- Timer scheduling: when the gate is clear, the body calls the scheduler at `0x005975e0` with timer id `6`, delay `0x2c` / 44 ms, and two zero trailing arguments, using `ECX = MapPane+0xa4`. It then sets `MapPane+0x39d = 1`.
- Capture transition: after queueing timer id `6`, the body calls `0x005449d0` with `ECX = MapPane`. Current Pane-family evidence resolves this nonzero-context helper as inherited `Pane::CaptureMouse()`, which passes the EventHandler facet at `this+0xa0` to the shared manager.
- Related state machine: the modeled clearer at `0x00505cc0` checks canonical bool `MapPane::m_queuedMapStateRefresh` at `+0x39d`, clears it, cancels timer ids `6` and `8` through inherited `TimerHandler::CancelTimer`, and then calls paired inherited `Pane::ReleaseMouseCapture()` at `0x005449f0`. The modeled timer handler at `0x00509030` handles the live MapPane timer events, including timer id `6`.

## UID0003ZT Queue-State And Helper Closure - 2026-07-23

- `MapPane+0x39d` is the single canonical byte-sized source field `bool m_queuedMapStateRefresh`, shared by the timer-6 queue body, timer-8 queue body, modeled clearer [UID:00042F][0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh](by-memory/0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh.md), target EventHandler callback [UID:0003ZT][0x00508f60-0x0050902b.MapPaneNotifyMessageCore](by-memory/0x00508f60-0x0050902b.MapPaneNotifyMessageCore.md), and TimerHandler callback [UID:0003ZS][0x00509030-0x00509439.MapPaneMovementTimerHandler](by-memory/0x00509030-0x00509439.MapPaneMovementTimerHandler.md). Historical `m_movementTimerQueued` and generic queued-refresh aliases are retained only as superseded vocabulary.
- `MapPane+0xa4` is the inherited TimerHandler facet supplied through Pane, not a source member named `m_timerHandler`. Source-facing prose therefore describes the observed call as inherited `TimerHandler::ScheduleTimer(6, 44, 0, 0)` while preserving the exact adjusted-subobject evidence.
- The helper pair is a state transition, not map redraw/refresh: `0x005449d0` is `Pane::CaptureMouse()` on queueing, and `0x005449f0` is paired `Pane::ReleaseMouseCapture()` on drain/cancel paths.
- The exhaustive no-route result remains controlling. This raw executable body stays reconstructable, class-owned, non-emitting, and blank formal C++; canonical naming does not invent a callable source route.

## No-Code / No-Emitter Proof

- IDA function modeling is negative for the target start, return, and end: `lookup_funcs` request `11` reports `0x00505c00`, `0x00505cae`, and `0x00505cb1` are not functions.
- Incoming route evidence is negative: `xrefs_to` request `12` reports no refs to `0x00505c00`; the only `0x00505cae` xref is internal to this raw body.
- Pointer-route evidence is negative: exact little-endian absolute pointer bytes `00 5c 50 00`, exact `push 0x00505c00` bytes `68 00 5c 50 00`, and the scoped control pattern all returned zero hits in `find_bytes` request `15`.
- Vtable route evidence is negative: vtable xref request `18` found data xrefs for the modeled notify/timer functions, not for this raw body.
- Generated-output evidence is negative: `auto-generated/NexusTK/map/MapPane.cpp` has no UID00042E marker, no `0x00505c00` marker, and no emitted target-name body. Expected current output remains through documented modeled MapPane children rather than this raw helper.
- Source route evidence is insufficient: no current support doc, generated file, caller, vtable entry, or source declaration proves a callable C++ home for this exact raw body.
- Result: keep `EMITTER_UIDS`, `EMITTER_POSITION_OPTIONAL`, and the formal `RECONSTRUCTION_CPP CODE` block blank. This page records the behavior and route-negative proof instead of inventing an emitter.

## Owner And Source Placement

- Correct owner: MapPane, [UID:00007Q][MapPane](by-class/MapPane.md). The body mutates MapPane fields, schedules through the MapPane timer facet, and is paired with MapPane timer/clearer children.
- Rejected owners: the timer scheduler/canceller helpers, config/global guard storage, input-blocking/global busy state, standalone helper file, and no-owner classification. Each explains part of the body but not the object state, child family, or source placement.
- Support docs already carry sufficient owner/source-placement context for this callback: [by-class/MapPane.md](by-class/MapPane.md), [by-file/MapPane.md](by-file/MapPane.md), and the aggregate [0x00503ef0-0x0050637a.MapPaneWeatherCoordinateObjectCore](by-memory/0x00503ef0-0x0050637a.MapPaneWeatherCoordinateObjectCore.md) document the raw no-route child policy, MapPane timer fields, and related child pages. No support doc edit is required unless a future callback asks for redundant support synchronization.

## Rejected Alternatives And Stale Leads

- Do not insert first-draft formal C++ from the raw bytes alone. The behavior is known, but the source route and declaration are not.
- Do not make the modeled timer handler or notification core the emitter for this raw body. They prove the surrounding state machine, not a call/inline route to `0x00505c00`.
- Do not collapse `0x005449d0` and `0x005449f0` into one source-facing helper. UID00042E uses the nonzero-context wrapper; clear/drain paths use the zero-context wrapper.
- Older B003/B006 FittingRoom reports that mention UID00042E refer to a historical different target at `0x00421a30`. They are stale UID-collision leads and are not evidence for this MapPane page.

## Score Rationale

- Completion is raised from `85` to `87` because this page now carries fresh MCP session evidence, exact no-function and range proof, instruction-level behavior, route-negative checks, generated-output absence, helper distinction, owner/source-placement reasoning, and rejected alternatives.
- Completion remains below `90` because no callable/source route, original symbol spelling, exact source declaration, or final header-quality field names for the action-state block are proven.
- Confidence is raised from `85` to `90` because the binary facts and negative route checks are direct, current, and scoped. It remains below final audit levels because no route means the original source status cannot be fully reconstructed as emitted C++.

## Cross-References

- [UID:0003ZT][0x00508f60-0x0050902b.MapPaneNotifyMessageCore](by-memory/0x00508f60-0x0050902b.MapPaneNotifyMessageCore.md)
- [UID:0003ZS][0x00509030-0x00509439.MapPaneMovementTimerHandler](by-memory/0x00509030-0x00509439.MapPaneMovementTimerHandler.md)
- [UID:00042F][0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh](by-memory/0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh.md)
- [UID:00042G][0x00505d00-0x00505d9a.MapPaneQueueMovementRefreshTimer8](by-memory/0x00505d00-0x00505d9a.MapPaneQueueMovementRefreshTimer8.md)
- [UID:00042H][0x00505da0-0x00505dd2.MapPaneQueueActionPollTimer](by-memory/0x00505da0-0x00505dd2.MapPaneQueueActionPollTimer.md)
- [UID:00042I][0x00505de0-0x00505dfe.MapPaneClearPendingActionPoll](by-memory/0x00505de0-0x00505dfe.MapPaneClearPendingActionPoll.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [by-file/MapPane](by-file/MapPane.md)
- [0x00503ef0-0x0050637a.MapPaneWeatherCoordinateObjectCore](by-memory/0x00503ef0-0x0050637a.MapPaneWeatherCoordinateObjectCore.md)

## Changes

- 2026-07-01 Agent-B010 implementation of accepted UID00042E report: raised metadata to `COMPLETION:87`, `CONFIDENCE:90`; preserved MapPane owner, reconstructability, blank emitter fields, and blank formal C++; added fresh MCP request evidence, range/padding proof, behavior/guard/timer semantics, `0x005449d0` versus `0x005449f0` distinction, no-code/no-emitter proof, owner/source-placement rationale, support-doc sufficiency, rejected alternatives, and stale UID-collision note.
- 2026-06-25 Agent-B009 implementation of accepted 0001AP report: created raw no-route child page with blank emitter/code and target-specific no-function/no-xref/no-pointer proof.
