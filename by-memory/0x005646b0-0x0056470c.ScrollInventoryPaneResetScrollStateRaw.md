*** UID:0001H1 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:190 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollInventoryPane::ResetScrollState()
{
    RectBounds rect;

    RemovePendingTimers();

    const ScrollInventoryPart oldPart =
        static_cast<ScrollInventoryPart>(m_highlightPart);
    m_activePart = static_cast<signed char>(kScrollInventoryPartNone);

    if (oldPart != kScrollInventoryPartNone) {
        GetPartRect(oldPart, &rect);
        InvalidateRect(&rect);
        m_highlightPart =
            static_cast<signed char>(kScrollInventoryPartNone);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact retained raw ScrollInventoryPane ResetScrollState removes pending timers, clears active state, invalidates and clears any old highlighted part, and preserves zero-route/padding/successor evidence at deterministic source position 190. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005646b0-0x0056470c ScrollInventoryPane ResetScrollState Raw

## Status

- Disposition: reconstructable NexusTK project code with raw-function/no-direct-route caveat.
- Entity kind: raw method-shaped helper and exact source-bearing child.
- Source-facing method: `void ScrollInventoryPane::ResetScrollState()`.
- Direct owner/emitter: [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md); accepted source-file route [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md), source position `190`.
- Confidence: very strong for exact byte/hash boundary, class-local field/helper semantics, direct class ownership and accepted sibling clone; no static caller route remains the explicit confidence cap.

## Behavior

This raw helper clears scrollbar interaction state after capture or hover. It calls `TimerHandler::RemovePendingTimers()` at `0x00597600` through the `this + 0xa4` timer/update-handler view, reads old `m_highlightPart` at `this + 0x103`, clears `m_activePart` at `this + 0x104` to `0xff`, and if the old highlight part was not `0xff`, computes that part rectangle through `ScrollInventoryPane::GetPartRect` at `0x005640a0` and invalidates it through the inherited Pane virtual slot at `+0x20`. It then clears `m_highlightPart` at `this + 0x103` to `0xff`.

## Boundary Notes

IDA does not currently model `0x005646b0` as a function, and there are no direct xrefs, callers, or loaded-segment dword pointer hits to this start in the current IDA database. Raw disassembly still shows aligned function-shaped code from `0x005646b0` through the `retn` at `0x0056470b`, with three `0xcc` bytes after the previous modeled function and four `0xcc` bytes before the next modeled function at `0x00564710`.

B003's 2026-06-18 local PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA1 `c285b73dc7b54c9a0e28ff7591a9bdde2ac55f9a`) independently confirmed the boundary: `0x005646ad-0x005646af` is `cc cc cc`, `0x005646b0` begins the helper prologue, `0x0056470b` returns, `0x0056470c-0x0056470f` is `cc cc cc cc`, and `0x00564710` starts the neighboring ScrollVolumePane constructor. The scan found no absolute-VA dword for `0x005646b0`, no RVA dword for `0x001646b0`, no direct branch/call to the raw start, and no external branch/call into the target span; the only branch target inside the range is the helper's own conditional skip `0x005646dd -> 0x005646fd`.

## Raw Assembly Shape

```asm
005646b0  push    ebp
005646b1  mov     ebp, esp
005646b3  sub     esp, 14h
005646c3  lea     ecx, [esi+0A4h]
005646c9  call    sub_597600
005646ce  mov     al, [esi+103h]
005646d4  mov     byte ptr [esi+104h], 0FFh
005646db  cmp     al, 0FFh
005646dd  jz      short loc_5646FD
005646e3  push    eax
005646e4  mov     ecx, esi
005646e6  call    sub_5640A0
005646eb  mov     eax, [esi]
005646f3  call    dword ptr [eax+20h]
005646f6  mov     byte ptr [esi+103h], 0FFh
00564703  call    @__security_check_cookie@4
0056470b  retn
```

## Evidence

- IDA MCP `lookup_funcs 0x005646b0` reports `Not a function`.
- IDA MCP raw `disasm 0x005646b0` shows prologue, security-cookie setup/check, call to `0x00597600`, old highlighted-part invalidation through `0x005640a0`, and `retn` at `0x0056470b`.
- 2026-05-31 IDA MCP raw disassembly confirms the byte writes: `+0x104` is cleared to `0xff` unconditionally, and `+0x103` is the old hover/highlight part invalidated through `0x005640a0` before being reset to `0xff`.
- 2026-06-03 restarted IDA MCP recheck confirms `ida_funcs.get_func` still returns no function at `0x005646b0`, `0x0056470b`, or `0x0056470c`; the previous modeled function is `sub_564520` at `0x00564520-0x005646ad`, and the next modeled function is `sub_564710` at `0x00564710-0x005647b1`.
- The same recheck finds no xrefs to the raw start. The raw span's outgoing references are `___security_cookie`, `sub_597600` for the `this + 0xa4` cleanup view, `sub_5640A0` for part-rectangle computation, the pane virtual call at `[vtable + 0x20]`, and `@__security_check_cookie@4`.
- Boundary bytes around the raw helper are `... c2 08 00 cc cc cc 55 8b ec ...` before `0x005646b0` and `... 8b e5 5d c3 cc cc cc cc 55 8b ec ...` after the `0x0056470b` return, matching a standalone compiler-shaped body in an IDA function-table gap.
- 2026-06-05 live IDA MCP `lookup_funcs` reports `0x005646b0`, `0x0056470b`, and `0x0056470c` as `Not a function`; previous/next modeled functions remain `sub_564520` at `0x00564520-0x005646ad` and `sub_564710` at `0x00564710-0x005647b1`.
- 2026-06-05 live IDA MCP disassembly confirms the raw body clears `this + 0x104`, conditionally invalidates the previous `this + 0x103` part rectangle through `sub_5640A0` and virtual slot `+0x20`, clears `this + 0x103`, checks the security cookie, and returns at `0x0056470b`.
- 2026-06-05 live IDA MCP `xrefs_to` and `callers` report no direct references to `0x005646b0`; a loaded-segment dword scan also finds no pointer equal to `0x005646b0`.
- 2026-06-05 live IDA MCP confirms three `0xcc` bytes at `0x005646ad-0x005646b0`, four `0xcc` bytes at `0x0056470c-0x00564710`, an unowned raw call to `sub_597600` at `0x005646c9`, and an unowned raw call to `sub_5640A0` at `0x005646e6`.

## 2026-06-18 B003 Source-Quality Reanalysis

B003 rechecked this target under the current source-quality rules and confirmed it should remain an exact source-bearing raw helper child for `ScrollInventoryPane`. The direct semantic owner/emitter is [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md), not file [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md), because the helper's `this` pointer is a `ScrollInventoryPane`, it touches class-local fields, calls class-local `GetPartRect` at `0x005640a0`, and invalidates through the pane virtual on the same object. [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md) is the accepted standalone generated source route. [UID:0000LS][NewInventoryPane](by-file/NewInventoryPane.md) remains a constructor consumer only; its former fold-candidate status is retained as a superseded historical source-organization hypothesis, not a current placement alternative or direct method owner.

Fresh route scanning checked absolute VA dwords, image RVA dwords, direct `.text` calls/jumps to target starts, direct branch/call targets inside `0x005646b0-0x0056470c`, and positive controls for modeled helper calls in the same cluster. Positive controls worked for `0x00564520` (`UpdateFromDrag`, callers at `0x005644bb` and `0x005644e4`), `0x00564460` (active-state dispatcher, three callers), `0x005640a0` (`GetPartRect`, thirteen callers including the call inside this target), `0x00597600` (`TimerHandler::RemovePendingTimers`, forty-five callers including this target), and `0x005975e0` (`TimerHandler` schedule wrapper, 163 callers). The same no-route pattern was seen for sibling raw starts `0x005608a0`, `0x00565170`, `0x005651e0`, and `0x00565490`.

The only pointer-looking interior hits were rejected as instruction-byte artifacts, not data routes: a raw dword beginning at `0x00508505` decoded as `0x005646e8` because it started on an `e8` call opcode byte in unrelated instruction bytes, and an RVA-form hit near `0x004e0330` decoded near `0x005646c7` because it began inside a normal `c7 46 16 00 ...` instruction. No defensible hidden VA/RVA table route to the raw start or body remains.

The best source-facing name is `ScrollInventoryPane::ResetScrollState()`. `ClearHighlight`, `StopTimer`, and `RemovePendingTimers` were rejected as too narrow because the helper removes timers, clears active tracking, invalidates a stale highlighted part, and clears highlight state; `NewInventoryPane::ResetScrollState` and `ScrollBar::ResetScrollState` were rejected because the layout, vtables, constructor, and `GetPartRect` helper are inventory-scrollbar specific. Use `char m_highlightPart` for `+0x103`, `char m_activePart` for `+0x104`, and a tertiary `TimerHandler` / update-handler view for `+0xa4`. Use `ScrollInventoryPane::GetPartRect(char part, RectBounds *outRect)` for `sub_5640A0` and generic `TimerHandler::RemovePendingTimers()` for `sub_597600`.

Historical limitation: earlier analysis treated absent function-table/static-route evidence as blocking safe IDA function creation and retained a standalone-versus-folded source question. The accepted report resolves source emission and standalone placement while preserving the no-route fact; any later IDA definition is supervisor-owned, atomic and fail-closed rather than prohibited categorically.

## Accepted Source And IDA Disposition

- Exact half-open range `[0x005646b0,0x0056470c)`, `0x5c` / 92 bytes; SHA256 `c053ffdfc451279d6ef440acb8ef17e8ddcd0b089d482f6d1270df4b9eec3664`.
- Exact preceding padding is `[0x005646ad,0x005646b0)` (`cc cc cc`); exact successor alignment is `[0x0056470c,0x00564710)` and `0x00564710` begins excluded ScrollVolumePane construction.
- No direct start xref, call/branch, VA/RVA/raw-offset pointer route, or vtable slot is known. The complete raw body, security-cookie frame, class fields/callees, clean fences, and exact accepted sibling establish retained source despite that negative liveness evidence.
- Report stage `I21` specifies supervisor-owned `define_func [0x005646b0,0x0056470c)`, rename `ScrollInventoryPane__ResetScrollState`, type `void __thiscall ...(ScrollInventoryPane *this)`, and repeatable comment `Removes pending timers, clears active state, invalidates the old highlight, and clears highlight state; no static entry route is currently known.`
- Any IDA application must preserve every raw instruction head, exact bytes/hash, zero start-xref set, comment channels, frame, both padding fences, vtables, shared dependencies and successor. B005 performs no IDA mutation.

## Autogen Status

- Reconstructable: true, exact raw source-bearing method-shaped project code.
- Direct owner/emitter: [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md), which routes to [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md) and generated path `auto-generated/NexusTK/ui/inventory/InventoryScrollPane.cpp`.
- C++: exact accepted formal C++ is populated for this bounded helper at source position `190`; enum declaration and method prototype remain class-owned in UID0000CK H.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Exact range/hash/bytes, preceding/following padding, complete reset behavior, fields/callees, raw negative-route evidence, sibling clone, owner/emitter/source order, formal CPP/H disposition and atomic IDA handoff are documented. |
| Confidence | 94 | Independent binary and sibling evidence agree on the retained source method and class semantics; absence of a static start route and original spelling/rebuild proof remain the explicit limits. |

## Cross-References

- [UID:0001GZ][0x00563260-0x0056470c.ScrollInventoryPane](by-memory/0x00563260-0x0056470c.ScrollInventoryPane.md)
- [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md)
- [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md)
- [UID:0000LS][NewInventoryPane](by-file/NewInventoryPane.md)
- [UID:0001H0][0x00564520-0x005646ad.ScrollInventoryPaneUpdateFromDrag](by-memory/0x00564520-0x005646ad.ScrollInventoryPaneUpdateFromDrag.md)

## Changes

- 2026-08-15 B005 accepted UID0000KB implementation callback:
  - Raised `86/89` to `93/94`, set source position `190`, upgraded the formal body to the accepted `ScrollInventoryPart`/signed-byte source form, and documented exact hash, boundaries, clone, negative liveness and I21 supervisor handoff.
  - Finalized standalone InventoryScrollPane placement while preserving the former fold/no-IDA assumptions as corrected historical evidence.

- 2026-06-18 B003 source-quality execution:
  - Changed to: completion `86`, confidence `89`, `CANONICAL_OWNER:0000CK`, `EMITTER_UIDS:0000CK`, and populated first-draft `ScrollInventoryPane::ResetScrollState()` C++.
  - Summary/evidence: B003 local PE scan of `NexusTK.exe` confirmed the exact raw boundary, pre/post padding, zero VA/RVA literals to `0x005646b0`, zero direct branch/call targets to the raw start, zero external direct branches into the span, and only the helper's own internal conditional branch into the range. Positive controls resolved for neighboring helpers and TimerHandler wrappers, and two pointer-looking interior hits were rejected as unaligned instruction-byte artifacts.
  - Historical owner/C++ rationale: direct owner/emitter was correctly resolved as UID0000CK with UID0000KB file route; NewInventoryPane, ScrollBar and TimerHandler ownership were rejected. The then-current categorical IDA block is superseded by the accepted atomic fail-closed I21 handoff while no-route evidence remains score-limiting.
- 2026-07-04 B007 support-summary sync:
  - Rewrote the Item Summary to reflect the current state after B003 execution: direct class owner/emitter [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md), formal `ScrollInventoryPane::ResetScrollState()` C++ present, and raw no-route/function-table/caller-provenance caveat retained as a score cap.
  - No metadata or C++ changed. This sync only removes stale wording that implied direct file ownership and a continuing final-C++ blocker.
- 2026-06-07:
  - Changed to: `AUTOGEN_PARENT_UID:0000KB`, keeping completion/confidence at `84/86` and C++ blank.
  - Summary/evidence: the item-inventory scrollbar parent chain now satisfies the attachment gate: [UID:0000KB][InventoryScrollPane](by-file/InventoryScrollPane.md) is `84/80`, [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md) is `80/80`, and this helper is `84/86`.
  - Historical parent/C++ rationale, superseded by the 2026-06-18 B003 source-quality pass: parent attachment was treated as safe for generated assembly ordering, but final C++ was left blank because IDA still did not model `0x005646b0` as a function and neither direct xrefs nor pointer-table evidence proved a source-level call route.
- 2026-06-05:
  - Changed to: completion `84`, confidence `86`, keeping `RECONSTRUCTABLE:TRUE`, blank `AUTOGEN_PARENT_UID`, and blank C++.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x005646b0-0x0056470b` method-shaped body, previous/next modeled functions, pre/post `0xcc` padding, active/highlight reset writes at `+0x104` and `+0x103`, outgoing calls to `sub_597600` and `sub_5640A0`, virtual invalidation through slot `+0x20`, and the absence of direct xrefs, callers, or loaded-segment pointer hits for the raw start.
  - Historical parent/C++ rationale, superseded by the 2026-06-18 B003 source-quality pass: parent assignment remained blank because the current class/file parents were below the confidence threshold; final C++ was left blank because the entry lacked modeled-function and caller provenance under the then-used 95/95 reconstruction bar.
- 2026-05-31: Raised completion/confidence from `50/65` to `70/78` and marked the raw helper reconstructable.
  - Before: The page documented the raw method-shaped helper but left reconstructable blank and did not capture the latest field-level reset evidence.
  - Changed to: Added explicit IDA-backed byte semantics for active part `+0x104` and hover/highlight part `+0x103` while keeping confidence below the sibling exact function because IDA still does not model `0x005646b0` as a function start.
  - Evidence: IDA MCP `lookup_funcs` still reports `0x005646b0` as not a function; raw disassembly confirms a complete method-shaped prologue/body/security-cookie epilogue ending at `0x0056470b`.
- 2026-06-03: Raised completion/confidence from `70/78` to `72/80` after restarted IDA MCP reconfirmed the raw helper's exact byte span, previous/next modeled functions, field writes, outgoing helper calls, and absence of start xrefs. Parent assignment remains blank because the parent class confidence is still below the attachment threshold; C++ remains blank while the function-table gap and call provenance are unresolved.
