*** UID:0001CM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "MonsterImageLibTables.h"

ImageAnimationTable::ImageAnimationTable()
{
    baseValue = 0;
    groupCount = 0;
    flags = 0;
    tableId = 0;
    groups = NULL;
}

ImageAnimationTable::~ImageAnimationTable()
{
    delete[] groups;
    groupCount = 0;
    groups = NULL;
}

void ImageAnimationTable::AllocateGroups(unsigned int count)
{
    groups = new ImageAnimationGroup[count];
    groupCount = (unsigned char)count;
}

ImageAnimationGroup *ImageAnimationTable::GetGroup(unsigned int groupIndex)
{
    ImageAnimationGroup *group = groups;
    if (groupIndex < groupCount)
        group += groupIndex;
    return group;
}

ImageAnimationFrame *ImageAnimationTable::GetFrame(unsigned int groupIndex, unsigned int frameIndex)
{
    ImageAnimationGroup *group = groups;
    if (groupIndex < groupCount)
        group += groupIndex;

    if (frameIndex < group->frameCount)
        return group->frames + frameIndex;

    return &g_defaultImageAnimationFrame;
}

ImageAnimationGroup::ImageAnimationGroup()
{
    frames = NULL;
    frameCount = 0;
}

ImageAnimationGroup::~ImageAnimationGroup()
{
    delete[] frames;
    frames = NULL;
    frameCount = 0;
}

void ImageAnimationGroup::AllocateFrames(unsigned int count)
{
    frames = new ImageAnimationFrame[count];
    frameCount = (unsigned short)(unsigned char)count;
}

ImageAnimationFrame *ImageAnimationGroup::GetFrame(unsigned int frameIndex)
{
    if (frameIndex < frameCount)
        return frames + frameIndex;

    return &g_defaultImageAnimationFrame;
}

ImageAnimationFrame::ImageAnimationFrame()
{
    frameOffset = 0;
    frameDuration = 0;
    blendAlpha = 0xff;
    auxFrameId = -1;
    paletteIndex = 0;
    effectIndex = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#ifndef NEXUSTK_RENDER_MONSTERIMAGELIBTABLES_H
#define NEXUSTK_RENDER_MONSTERIMAGELIBTABLES_H

struct ImageAnimationFrame
{
    short frameOffset;
    short frameDuration;
    unsigned char blendAlpha;
    unsigned char reserved;
    short auxFrameId;
    unsigned char paletteIndex;
    unsigned char effectIndex;

    ImageAnimationFrame();
};

struct ImageAnimationGroup
{
    ImageAnimationFrame *frames;
    unsigned short frameCount;

    ImageAnimationGroup();
    ~ImageAnimationGroup();

    void AllocateFrames(unsigned int count);
    ImageAnimationFrame *GetFrame(unsigned int frameIndex);
};

struct ImageAnimationTable
{
    int baseValue;
    unsigned char groupCount;
    unsigned char flags;
    unsigned short tableId;
    ImageAnimationGroup *groups;

    ImageAnimationTable();
    ~ImageAnimationTable();

    void AllocateGroups(unsigned int count);
    ImageAnimationGroup *GetGroup(unsigned int groupIndex);
    ImageAnimationFrame *GetFrame(
        unsigned int groupIndex,
        unsigned int frameIndex);
};

extern ImageAnimationFrame g_defaultImageAnimationFrame;

#endif
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Complete shared monster/riding animation table helper island with 13 binary bodies, ten authored definitions in formal CPP, complete guarded ImageAnimationTable/ImageAnimationGroup/ImageAnimationFrame declarations and fallback extern in formal H, exact 12/8/10-byte layouts, parser caller families, callback wiring, compiler-clone/no-op dispositions, and internal padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00528930-0x00528d51 Monster/Riding Table Helpers

## Status

- Confidence: very strong for every function boundary, authored/compiler split, helper behavior, callback wiring, shared caller family, exact layouts, complete formal CPP/H, and source route; exact original lexical names remain inferred.
- 2026-08-17 B008 implementation: UID0001CM now emits definitions-only CPP with its own-header include and complete guarded `ImageAnimationTable`, `ImageAnimationGroup`, and `ImageAnimationFrame` declarations plus the fallback extern in H.
- Owner/emitter route remains [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md). `MonsterImageLib` and `RidingImageLib` are consumers through parser and render/lookup call sites, not direct owners of this helper island.
- No split or rename is needed. Internal gaps are `0xcc` alignment/padding between helper bodies, and the next modeled function begins at `0x00528d60`.
- C010 disposition: adjacent data at `0x0069b424-0x0069b43c` is outside this helper-source implementation. Current MCP reads zeroed storage and raw PE mapping places `0x0069b424` and `0x0069b430` in `.data` virtual-zero-fill, so that byte-state issue has no score, C++, owner/emitter, or source-placement impact here.
- Covered classes:
  - [UID:00008O][MonsterImageLibTable_5289D0](by-class/MonsterImageLibTable_5289D0.md)
  - [UID:00008P][MonsterImageLibTable_528BE0](by-class/MonsterImageLibTable_528BE0.md)
- Likely source file: [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md)

## Functions

| Range | Class/Function | Notes |
| --- | --- | --- |
| `0x00528930-0x00528947` | outer table initializer | Clears the 12-byte outer table object. Called directly by low-address setup helpers and passed as an element constructor callback by parser/allocation code. |
| `0x00528950-0x005289c6` | `ImageAnimationTable` cleanup/destructor helper | Destroys the heap array of 8-byte group records through `0x00528b60`, frees the vector allocation, clears low-byte count and entry pointer. Dated pre-mutation B006 state was `sub_528950` / `void __thiscall(_DWORD *this)` with blank comments. Accepted current A02 state is `ImageAnimationTable__destructor`, `void __thiscall(ImageAnimationTable *this)`, and function regular comment `Destroys one 12-byte ImageAnimationTable: destroys/frees its ImageAnimationGroup array, clears groupCount, and nulls groups. Shared by MonsterImageLib and RidingImageLib table cleanup.` |
| `0x005289d0-0x00528a6e` | `ImageAnimationTable::AllocateGroups` helper | Allocates/resizes nested 8-byte `ImageAnimationGroup` records with constructor callback `0x00528b50` and destructor callback `0x00528b60`; stores the low byte of the requested count at `+0x04`. |
| `0x00528a70-0x00528ae6` | compiler cleanup clone | Zero entry xrefs and duplicate destructor cleanup semantics identify an EH cleanup clone. It emits no second authored destructor. |
| `0x00528af0-0x00528b08` | `ImageAnimationTable::GetGroup` helper | Returns the 8-byte group-record pointer when the requested group is in range, otherwise returns the base group pointer. |
| `0x00528b10-0x00528b44` | `ImageAnimationTable::GetFrame` helper | Resolves an outer group and returns the indexed 10-byte `ImageAnimationFrame` when present, otherwise returns fallback record `0x0066deb0`. |
| `0x00528b50-0x00528b5f` | `ImageAnimationGroup` initializer | Initializes an 8-byte group record with null frame pointer and zero frame count; passed as the constructor callback by `0x005289d0`. |
| `0x00528b60-0x00528bd9` | `ImageAnimationGroup` cleanup/destructor helper | Destroys the 10-byte `ImageAnimationFrame` vector and clears frame pointer/count; called by outer table cleanup/allocation paths. |
| `0x00528be0-0x00528c7c` | `ImageAnimationGroup::AllocateFrames` helper | Allocates/resizes 10-byte `ImageAnimationFrame` records; stores the low byte of the requested count as a 16-bit frame count. |
| `0x00528c80-0x00528cf9` | compiler cleanup clone | Zero entry xrefs and duplicate group-destructor semantics identify an EH cleanup clone. It emits no second authored destructor. |
| `0x00528d00-0x00528d28` | `ImageAnimationGroup::GetFrame` helper | Returns the indexed 10-byte frame record when present, otherwise returns fallback record `0x0066deb0`. |
| `0x00528d30-0x00528d46` | `ImageAnimationFrame` initializer | Initializes a 10-byte frame record with frame offset/duration dword zero, byte `+0x04 = 0xff`, word `+0x06 = 0xffff`, and zeroed tail word. Passed as a constructor callback by `0x00528be0`. |
| `0x00528d50-0x00528d51` | compiler-generated trivial destructor callback | One-byte `retn` referenced only as the frame-vector destructor callback. It emits no authored empty method. |

Internal alignment padding appears between every modeled helper body and consists of `0xcc` bytes, including `0x00528b44-0x00528b50`, `0x00528b5f-0x00528b60`, `0x00528d28-0x00528d30`, `0x00528d46-0x00528d50`, and `0x00528d51-0x00528d60`. The outer boundary padding is tracked in [UID:0000VN][-ignored](by-memory/-ignored.md).

## Accepted Source-Facing Layout Names

The 2026-06-25 B001 implementation accepts neutral source-facing names for method-level C++ while retaining the current page/file names as evidence aliases:

| Shape | Size | Accepted fields | Evidence level |
| --- | ---: | --- | --- |
| `ImageAnimationTable` outer 12-byte table shape | `0x0c` / 12 bytes (Verified with int_convert.py) | `baseValue` at `+0x00`, `groupCount` at `+0x04`, weak `flags`/`tableId` padding fields at `+0x05/+0x06`, group pointer at `+0x08` | Strong for helper behavior and loader/default-table use; `ImageAnimationTable` is inferred because no recovered IDA name exists. |
| `ImageAnimationGroup` | `0x08` / 8 bytes (Verified with int_convert.py) | Source fields `+0x00 frames`, `+0x04 frameCount`; `+0x06` is implicit 32-bit tail padding | Strong for pointer/count/size. The current IDA analysis member at `+0x06` remains protected but is not promoted into source. |
| `ImageAnimationFrame` | `0x0a` / 10 bytes (Verified with int_convert.py) | `+0x00 frameOffset`, `+0x02 frameDuration`, `+0x04 blendAlpha`, `+0x05 reserved`, `+0x06 auxFrameId`, `+0x08 paletteIndex`, `+0x09 effectIndex` | Strong for `frameOffset`, `frameDuration`, and monster `blendAlpha`; weaker for `auxFrameId`, `paletteIndex`, and `effectIndex`. |

The old `MonsterImageLibTable_5289D0` and `MonsterImageLibTable_528BE0` identifiers are documentation/search aliases for the helper pages, not final source-facing type names. Their source declarations and method bodies are emitted here through UID0001CM; the class pages should not duplicate helper C++.

## Evidence Notes

- Historical Wave3 summaries identified both classes as small heap-array owners; current conclusions rely on the live evidence below, not that stale workflow.
- IDA MCP on 2026-05-22 reports the allocation methods are called from both `MonsterImageLib::LoadMonsterTables` and `RidingImageLib::LoadRidingDefinitions`.
- The shared caller family means these helpers should not be treated as arbitrary monster-only spillover.
- IDA MCP on 2026-05-26 confirms [UID:0000TJ][RidingDefinitionGlobalTable](by-global/RidingDefinitionGlobalTable.md) at `0x0069b430` is a global instance of the 12-byte outer helper. The `dword_69B438` references in `LoadRidingDefinitions` are the object's `+0x08` entry pointer field, not a separate standalone global allocation.
- 2026-05-26 IDA `py_eval` recheck confirms modeled function starts at `0x00528950`, `0x005289d0`, `0x00528a70`, `0x00528af0`, `0x00528b10`, `0x00528b60`, `0x00528be0`, `0x00528c80`, and `0x00528d00`. No IDA names contain `MonsterImageLibTable`; only SEH labels include the address-derived helper starts, so current class names are documentation placeholders.
- 2026-05-28 IDA MCP recheck adds modeled helper starts at `0x00528930`, `0x00528d30`, and `0x00528d50`. Xrefs show `0x00528930` is called directly from `0x00419fb0`/`0x00419fd0` and passed as an element constructor callback from `0x004dac40`/`0x004dc420`; `0x00528d30` is passed as a constructor callback by `0x00528be0`; `0x00528d50` is a no-op callback passed by `0x00528b60`, `0x00528be0`, and `0x00528c80`.
- IDA xrefs confirm the allocation helpers at `0x005289d0` and `0x00528be0` each have four parser callers from `MonsterImageLib` and `RidingImageLib`, while fallback frame record `unk_66DEB0` is referenced by both frame-entry getters at `0x00528b10` and `0x00528d00`.
- 2026-06-06 live IDA MCP recheck against `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` enumerates thirteen helper bodies in `0x00528930-0x00528d51`, including previously omitted `sub_528B50` at `0x00528b50-0x00528b5f`. `sub_528B50` zeroes the 8-byte nested group record and is referenced as the vector constructor callback from `0x005289d0`.
- The same 2026-06-06 recheck confirms `0x00528930` xrefs from `0x00419fb5`, `0x00419fd5`, `0x004dace4`, and `0x004dc4b4`; parser callers for `0x005289d0` and `0x00528be0`; callback xrefs from `0x00528b60`/`0x00528be0`/`0x00528c80` to `nullsub_13`; and `unk_66DEB0` fallback references from `0x00528b10` and `0x00528d00`.
- 2026-06-14 C001 IDA MCP recheck reconfirms all thirteen modeled helper starts and sizes, the `0x00528d60` successor boundary, parser/allocation xrefs to `0x005289d0` and `0x00528be0`, constructor/destructor callback wiring through `0x00528b50`, `0x00528b60`, `0x00528d30`, and `0x00528d50`, fallback refs from both lookup helpers to `0x0066deb0`, and internal `0xcc` padding bytes around the helper boundaries.
- The 2026-06-29 B007 callback used the then-current IDA MCP session `b2ae72ec`: `idb_list` showed active `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` with `server_health status:ok`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`. That dated session's `lookup_funcs` normalized half-open endpoints for `0x005289d0-0x00528a6e`, `0x00528af0-0x00528b08`, `0x00528b10-0x00528b44`, `0x00528be0-0x00528c7c`, and `0x00528d00-0x00528d28`.
- Dated `b2ae72ec` xrefs kept `0x005289d0` and `0x00528be0` shared between `MonsterImageLib::LoadMonsterTables` and `RidingImageLib::LoadRidingDefinitions`, while render/lookup callers used `0x00528af0`, `0x00528b10`, and `0x00528d00`. Constructor/destructor callback wiring was `0x005289d0 -> 0x00528b50/0x00528b60` and `0x00528be0 -> 0x00528d30/0x00528d50`; current authority is established dynamically by supervisor Gate 2B.
- Current decompilation supports source-level `new[]` / `delete[]` helper methods: `ImageAnimationTable` zero-initializes and allocates/deletes `ImageAnimationGroup` arrays; `ImageAnimationGroup` allocates/deletes `ImageAnimationFrame` arrays; both lookup helpers return `g_defaultImageAnimationFrame` at `0x0066deb0` on out-of-range input.
- `0x00528a70` and `0x00528c80` are preserved as compiler cleanup/unwind clones, not standalone public source methods. Both duplicate nearby cleanup shapes and have no source-facing xref evidence.
- UID0001CM references but does not define `0x0066deb0`; the storage remains [UID:00027U][0x0066deb0-0x0066debc.MonsterImageLibDefaultFrameRecord](by-memory/0x0066deb0-0x0066debc.MonsterImageLibDefaultFrameRecord.md), already attached to [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md).
- C010 not-applicable disposition: current MCP reads zeroed default-table storage at `0x0069b424-0x0069b43c`, and raw PE mapping places VA `0x0069b424` at RVA `0x0029b424`, `.data`, raw offset `0x00299024`, virtual-zero-fill, and VA `0x0069b430` at RVA `0x0029b430`, `.data`, raw offset `0x00299030`, virtual-zero-fill. Those concrete default globals are outside the UID0001CM helper range and do not affect this C++ or score.

## 2026-08-11 B006 Destructor And Allocation-Release Handoff

- Exact `0x00528950` evidence: half-open `0x00528950-0x005289c6`, 118 bytes, body SHA256 `16699FE91B4F170A208359C4DA2FE9976DF505F7DEBCA73F5BEF90432B99D42F`, and eight refs. The dated pre-mutation B006 state was IDA name `sub_528950`, type `void __thiscall(_DWORD *this)`, and blank comments. The current accepted A02 poststate is name `ImageAnimationTable__destructor`, type `void __thiscall(ImageAnimationTable *this)`, and function regular comment `Destroys one 12-byte ImageAnimationTable: destroys/frees its ImageAnimationGroup array, clears groupCount, and nulls groups. Shared by MonsterImageLib and RidingImageLib table cleanup.` The 12-byte UDT and body semantics are unchanged.
- Current A02 is an already-applied analysis-name/type/comment state, not a pending source change. The formal source already models the source-facing destructor and remains unchanged.
- Shared dependency `0x005c7910-0x005c791e` lies outside this exact range. It is a 14-byte cdecl wrapper around the single runtime free-base callee, body SHA256 `F1379B8CF21C269DF67C6849951BCCA560B07D2C92441ED30FD291070BF6A0EF`, with 17 callers. The dated pre-mutation B006 name was `sub_5C7910`. The accepted current A14 state is name `NexusTK_array_allocation_free_wrapper`, type `void __cdecl(void *Block)`, and function regular comment `Shared array-allocation release wrapper around the runtime free path; source reconstruction should use the owning container/allocation syntax rather than call this compiler-facing wrapper directly.` It remains compiler/runtime evidence with no direct authored-source body or call; human source uses ordinary `new[]`/`delete[]` ownership syntax.
- Both RidingImageLib destructor forms use `0x00528950` as their 12-byte table-element callback and use `0x005c7910` for allocation release. The scalar wrapper duplicates that cleanup inline rather than calling UID00017G, then performs compiler-only deletion handling. These uses do not move either helper or formal source into RidingImageLib.
- Accepted A02 and A14 states above are already present and protected. Any remaining Section 21 IDA actions stay supervisor-owned and must preserve exact ranges, hashes, caller/xref sets, frames, comments, and source ownership; this B008 callback performs no IDA mutation.

## Reconstruction Notes

- Emit the shared helper declarations from this UID's formal H and the ten authored method bodies from its formal CPP through [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md). CPP begins with `#include "MonsterImageLibTables.h"`; this supersedes the earlier mixed CPP/declaration placement.
- Use `ImageAnimationTable` as the inferred source-facing name for the 12-byte outer helper because it is shared by monster and riding logic. Do not promote the address-derived `MonsterImageLibTable_5289D0` or `MonsterImageLibTable_528BE0` names into final source.
- Keep [UID:00027U][0x0066deb0-0x0066debc.MonsterImageLibDefaultFrameRecord](by-memory/0x0066deb0-0x0066debc.MonsterImageLibDefaultFrameRecord.md), [UID:00029K][0x0069b424-0x0069b430.ImageLibraryFallbackRecord](by-memory/0x0069b424-0x0069b430.ImageLibraryFallbackRecord.md), and [UID:0001PL][0x0069b430-0x0069b43c.RidingDefinitionGlobalTable](by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md) as separate data pages. This helper source only declares `extern ImageAnimationFrame g_defaultImageAnimationFrame` for the `0x0066deb0` fallback dependency.
- Preserve the weak-field caveat for `auxFrameId`, `paletteIndex`, `effectIndex`, `flags`, and `tableId`; current behavior proves allocation/initialization/lookup, not every consumer-side semantic.
- Do not split the range. The target is a coherent helper island ending before successor function `0x00528d60`, and internal `0xcc` bytes are alignment between helper bodies.

## 2026-08-17 B008 CPP/H And Whole-Island Closure

Formal source is intentionally human-shaped rather than a call-for-call rendering of compiler helpers. `ImageAnimationTable` and `ImageAnimationGroup` own arrays with `new[]`/`delete[]`; the external 14-byte `NexusTK_array_allocation_free_wrapper` remains compiler/runtime evidence and is not called directly. The table constructor clears all 12 bytes through its five source fields. Its destructor destroys/releases the group array, clears the low-byte count, and nulls the pointer. `AllocateGroups` constructs an array of 8-byte groups and stores only the low byte of `count`. `GetGroup` preserves the observed behavior of returning the base pointer for an out-of-range index rather than a null pointer.

The group constructor initializes only `frames` and `frameCount`; the 8-byte source size follows from 32-bit alignment, so the `+0x06` tail remains implicit. Its destructor releases frames, nulls the pointer, and clears the count. `AllocateFrames` constructs 10-byte frame records and preserves the observed low-byte truncation before storing the 16-bit count. Both frame getters preserve 8-byte/10-byte indexing and return `g_defaultImageAnimationFrame` when a frame index is outside the selected group.

The 10-byte frame constructor writes the two 16-bit zero fields, `blendAlpha = 0xff`, `auxFrameId = -1`, and the two trailing bytes as zero. It does not assign `reserved`; this is necessary because the binary constructor has no `+0x05` write. The source declaration therefore retains the byte for ABI layout while avoiding a fabricated write. `frameDuration` is signed `short`, supported by five sign-extending consumers. `auxFrameId`, `paletteIndex`, `effectIndex`, `flags`, and `tableId` remain best-fit inferred source names rather than recovered symbols.

UID00027U owns the one external definition after this source fragment. UID00008O and UID00008P are non-emitting address-derived evidence aliases. UID0002JN and UID0002LA directly include the H before their unchanged loader definitions. Concrete monster/riding outer records are ABI consumers and remain in their established source modules; no duplicate implementation is emitted here or there.

The exact no-code inventory is closed: `0x00528a70` and `0x00528c80` are EH cleanup clones with zero entry xrefs, and `0x00528d50` is a one-byte trivial-destructor callback with three callback refs. Internal `0xcc` gaps and the tail to `0x00528d60` are alignment. They are retained as compiler/range evidence and have blank formal CPP/H dispositions.

## Score Rationale

- Completion is `94` because the target carries complete formal CPP/H, all thirteen binary-body dispositions, exact allocation/cleanup/lookup behavior, current range/xref/decompile/padding evidence, fallback extern route, and explicit compiler-product/data-boundary handling.
- Confidence is `94` because IDA MCP, loader usage, support class pages, fallback ownership, signed consumers, and owner/emitter routing agree on the shared `MonsterImageLibTables` source unit. The residual reflects inferred lexical names and weaker tail-field semantics, not a missing implementation.

## UID0003U8 Signed Frame-Duration Synchronization - 2026-07-15

- The authoritative `ImageAnimationFrame` declaration now uses signed `short frameDuration` at `+0x02`. Record size remains `0x0a`; every other field, all 13 helper bodies, fallback extern, owner/emitter route, score, range, padding, and cleanup-clone disposition remain unchanged.
- Five independent `ImageAnimationGroup::GetFrame` consumers use signed semantics: `0x0053a30b`, `0x0053aaae`, `0x0053b780`, `0x0053ba59`, and [UID:0003U8][0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation](by-memory/0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation.md) at `0x005a37d6`. Their timing reads sign-extend frame word `+2` before scheduling or timing use.
- [UID:0003U8][0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation](by-memory/0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation.md) selects a `MonsterImageEntry`, obtains an `ImageAnimationGroup`, then obtains the current `ImageAnimationFrame`; the target's load at `0x005a37dd` passes the sign-extended duration to TimerMgr event `20`.
- The loader still reads exactly two serialized bytes and both monster/riding default-table paths still seed decimal `30000`, which is representable as positive signed 16-bit. Signedness changes the canonical source type, not width, layout, initialization, parser byte count, or fallback behavior.
- Historical `unsigned short`/`uint16` duration wording is superseded by the complete consumer audit. `frameCount` and `tableId` remain unsigned, and weaker `auxFrameId`, `paletteIndex`, `effectIndex`, `flags`, and `tableId` spelling caveats remain intact.

## Cross-References

- [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md)
- [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md)
- [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md)
- [UID:0000TJ][RidingDefinitionGlobalTable](by-global/RidingDefinitionGlobalTable.md)

## Changes

- 2026-08-11 B006 UID0002Y4 support synchronization:
  - Added the dated pre-mutation `sub_528950` state and the accepted current `ImageAnimationTable__destructor` name/type/comment, body hash, refs, and cleanup semantics.
  - Documented the external shared `NexusTK_array_allocation_free_wrapper` dependency with exact range/hash/type/caller count and preserved its compiler/runtime source disposition.
  - Recorded RidingImageLib cleanup as shared-consumer evidence without changing UID0001CM formal CPP/H, owner/emitter routing, range, or score.

- 2026-06-29 B007 implementation callback:
  - Changed target metadata from `85/88` to `90/90`; owner/emitter/reconstructable route remains [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md).
  - Inserted formal C++ for `ImageAnimationTable`, `ImageAnimationGroup`, `ImageAnimationFrame`, and `extern ImageAnimationFrame g_defaultImageAnimationFrame`.
  - Summary/evidence: the dated 2026-06-29 MCP session `b2ae72ec` reconfirmed the full thirteen-helper island, normalized half-open endpoints, parser/render xrefs, constructor/destructor callback wiring, fallback-frame refs to `0x0066deb0`, cleanup/unwind clone status for `0x00528a70` and `0x00528c80`, and `0xcc` padding around helper boundaries. C010 adjacent default-table bytes were excluded from that callback because the dated MCP zero-byte readback plus raw PE `.data` virtual-zero-fill proved no UID0001CM score/C++/owner impact.
- 2026-06-25 B001 implementation callback:
  - Score unchanged at `85/88`.
  - Evidence: aligned the helper island with the accepted [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) C++ and field ledger. Added `ImageAnimationGroup` and `ImageAnimationFrame` source-facing roles, recorded the 8-byte/10-byte layouts, preserved raw helper names as evidence aliases, and marked `auxFrameId`, `paletteIndex`, and `effectIndex` as weaker inferred names.
- Before: this page and coverage row covered only `0x00528950-0x00528d28`, leaving helper bodies at `0x00528930`, `0x00528d30`, and `0x00528d50` hidden inside adjacent `UNKNOWN` rows.
- Changed to: range is `0x00528930-0x00528d51`, with the initializer/no-op callback helpers included in the same monster/riding table helper island.
- Summary/evidence: 2026-05-28 IDA MCP reports coherent helper functions at `0x00528930-0x00528947`, `0x00528d30-0x00528d46`, and `0x00528d50-0x00528d51`; xrefs tie them to the same allocation/cleanup helper family already documented here.

- 2026-06-03 autogen consistency pass:
  - Changed to: `RECONSTRUCTABLE:TRUE` and legacy parent metadata for [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md).
  - Summary/evidence: [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) is now projected to `NexusTK/render/`, already documents this exact helper island as the shared monster/riding table helper source, and both parent/child confidence were at or above the then-current attachment threshold. Standalone helper C++ remained blank at that time because exact source declarations were not source-quality.

- 2026-06-06 live IDA MCP refresh:
  - Changed to: completion `82`, confidence `88`; added the `0x00528b50-0x00528b5f` nested group-record initializer to the function table.
  - Summary/evidence: live function enumeration shows thirteen helpers before next function `0x00528d60`, with only `0xcc` alignment bytes between bodies. Decompile/xrefs prove `sub_528B50` initializes 8-byte nested group records and is passed by `0x005289d0`; lookup helper descriptions now reflect the proven `0x0066deb0` fallback behavior. The then-current blank-C++/declaration-pass blocker was superseded by the 2026-06-29 B007 implementation.
- 2026-06-14 C001 class-support refresh:
  - Changed completion from `82` to `85`; confidence remains `88`.
  - Summary/evidence: live IDA 9.1 reconfirmed the full helper inventory, successor boundary, callback wiring, parser caller/xref sets, fallback record refs, and internal padding bytes used by [UID:00008O][MonsterImageLibTable_5289D0](by-class/MonsterImageLibTable_5289D0.md) and [UID:00008P][MonsterImageLibTable_528BE0](by-class/MonsterImageLibTable_528BE0.md). The then-current blank-C++/declaration-pass blocker was superseded by B001 neutral-name acceptance and the 2026-06-29 B007 UID0001CM formal C++ implementation.
