*** UID:0002NM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
IntAlphaSurface *IntAlphaSurface::InitSurfaceView(
    IntAlphaSurface *view,
    const RectBounds *clipBounds) const
{
    view = new (view) IntAlphaSurface();

    if (clipBounds == NULL) {
        view->m_pixels = m_pixels;
        view->m_width = m_width;
        view->m_height = m_height;
        view->m_stride = m_stride;
    } else if (m_pixels != NULL) {
        RectBounds surfaceBounds;
        RectBounds clippedBounds;
        InitRectBounds(&surfaceBounds, 0, 0, m_width, m_height);

        if (IntersectRects(&surfaceBounds, clipBounds, &clippedBounds)) {
            view->m_pixels = m_pixels
                           + clippedBounds.left
                           + clippedBounds.top * m_stride;
            view->m_width = clippedBounds.right - clippedBounds.left;
            view->m_height = clippedBounds.bottom - clippedBounds.top;
            view->m_stride = m_stride;
        } else {
            view->m_pixels = NULL;
        }
    } else {
        view->m_pixels = NULL;
    }

    view->m_ownsBuffer = false;
    return view;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
    IntAlphaSurface *InitSurfaceView(IntAlphaSurface *view,
                                     const RectBounds *clipBounds) const;
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `IntAlphaSurface::InitSurfaceView` member source at child position 7; placement-constructs a caller-supplied `IntAlphaSurface` view, copies or clips the source buffer, clears ownership, preserves partial failure writes, and emits through class owner UID00006K in physical file UID0000HF. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004623c0-0x00462514 InitAlphaSurfaceView

## Accepted Current Source Resolution

- The concrete source identity is `IntAlphaSurface::InitSurfaceView`, emitted through [UID:00006K][IntAlphaSurface](by-class/IntAlphaSurface.md) at child position `7` in [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md).
- The method placement-constructs the output view, copies the full buffer when `clipBounds == NULL`, otherwise intersects a local zero-origin surface rectangle, advances the pixel pointer for a non-empty clip, and marks the returned view non-owning.
- Empty intersections or null source pixels set only `view->m_pixels = NULL`, matching the observed partial-output behavior; the source does not invent clearing for the remaining output fields.
- The historical `AlphaMaskSurface::InitAlphaSurfaceView` spelling and file-level emitter route are superseded. The retained filename/title are UID-stability artifacts rather than current class identity.

## Status

- Entity kind: function / constructor-style helper.
- Address range: `0x004623c0-0x00462514`, half-open, modeled by IDA as `sub_4623C0` size `0x154`.
- Disposition: reconstructable NexusTK render infrastructure.
- Rebuild handling: `source-authored`.
- Accepted owner/emitter: [UID:00006K][IntAlphaSurface](by-class/IntAlphaSurface.md), child position `7`, physically emitted in [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md).
- Completion: 93%.
- Confidence: very strong, 94%; exact member/class/file placement is resolved, while absent direct callers and inferred original spelling remain bounded confidence limits.
- Autogen status: class-owned source with formal CPP/H routed through UID00006K and the UID0000HF physical compilation unit.

## Behavior

This helper initializes a destination alpha-surface view object from a source alpha surface and an optional clip rectangle.

Observed behavior:

- writes [UID:0001XT][IntAlphaSurfaceVtable](by-type/by-vtable/IntAlphaSurfaceVtable.md) / `off_6112EC` to the destination object at offset `+0x00`;
- clears destination pixel pointer, width, height, stride, and bounds state;
- initializes the destination bounds through the rectangle constructor helper at `0x004b7c50`;
- if no clip rectangle is supplied, copies the source view payload from source offsets `+0x14` through `+0x24`;
- if a clip rectangle is supplied, builds the source bounds from source width/height, intersects it through the rectangle-intersection helper at `0x004b7cc0`, and either emits an empty view or computes a clipped non-owning pixel pointer;
- always clears the destination ownership byte at `+0x24` before returning, so the constructed view does not own the source pixel buffer.

The clipped pointer calculation is:

```text
dest->pixels = source->pixels + clip.left + clip.top * source->stride
dest->width  = clip.right - clip.left
dest->height = clip.bottom - clip.top
dest->stride = source->stride
dest->ownsPixels = false
```

## Score Rationale

| Field | Value | Rationale |
| --- | --- | --- |
| `COMPLETION` | `93` | Current live IDA/MCP evidence resolves the exact modeled function, neighboring boundaries, `this` source object, explicit destination pointer, construction/use-site, full/clipped branches, rectangle calls, padding, class/file route, and exact formal CPP/H. |
| `CONFIDENCE` | `94` | RTTI/vtable and whole-file evidence resolve this as `IntAlphaSurface::InitSurfaceView` under UID00006K in UID0000HF. No direct caller/pointer route and inferred lexical spelling remain explicit confidence limits. |
| `RECONSTRUCTABLE` | `TRUE` | Source-authored `IntAlphaSurface` member body and declaration are present in the formal channels. |

## IDA MCP Evidence

2026-06-05 live IDA MCP checks against input MD5 `4247e04e20b65d6414c7238aa8ff5515`:

- `lookup_funcs 0x004623c0` reports `sub_4623C0` at `0x004623c0-0x00462514`, size `0x154`.
- Neighboring modeled functions are `sub_462170` at `0x00462170-0x00462227`, `sub_462320` at `0x00462320-0x004623b5`, and `sub_462520` at `0x00462520-0x00462672`; `0x00462514` itself is not a function start.
- `callers 0x004623c0` and `xrefs_to 0x004623c0` report no direct callers in the current IDA database.
- Code refs from the body call `sub_4B7C50` at `0x0046243b` and `0x0046246f`, `sub_4B7CC0` at `0x0046247f`, and `@__security_check_cookie@4` at `0x00462509`.
- The destination vtable store at `0x00462415` writes [UID:0001XT][IntAlphaSurfaceVtable](by-type/by-vtable/IntAlphaSurfaceVtable.md) / `off_6112EC`.
- `xrefs_to 0x006112ec` ties the same vtable into the raw/default alpha-surface constructor-shaped block at `0x0046212c`, the modeled alpha-surface constructor at `0x0046217b`, the EH/unwind cleanup store at `0x00462237`, this helper at `0x00462415`, and the scalar deleting destructor at `0x0046327a`.
- Decompilation shows the destination vtable store, zero-state initialization, `sub_4B7C50(a2 + 4, 0, 0, 0, 0)`, the no-clip branch copying source offsets `+0x14..+0x24`, the clipped branch building a source bounds rectangle from source width/height, intersection through `sub_4B7CC0`, clipped pointer calculation, and final ownership-byte clear before returning `a2`.
- The post-helper gap `0x00462514-0x00462520` is twelve `0xcc` bytes before the next modeled helper.

2026-06-15 historical/superseded A001 class-placement refresh:

The following dated class-filter absence was useful during discovery but is not current ownership authority. The accepted UID00006K member route above supersedes its source-split caveat.

- Wave2 method-analysis has no method/global record at `0x004623c0`. `filter --class AlphaMaskSurface --all-methods` lists the constructor, `GetBufferInfo`, `ApplyRectAlpha`, and gradient fillers, while `filter --class IntAlphaSurface --all-methods` only lists the scalar deleting destructor. That absence supports the current source-split caveat rather than a final class method declaration.
- Live IDA MCP `lookup_funcs`/`entity_query` reconfirms `sub_4623C0` at `0x004623c0-0x00462514`, no function at `0x00462514`, and the successor `sub_462520` at `0x00462520`.
- Live IDA MCP `analyze_function 0x004623c0` reconfirms no callers, calls to `sub_4B7C50`, `sub_4B7CC0`, and the security-cookie check, the `off_6112EC` destination vtable store, copy/full-view branch, clipped pointer branch, and final ownership-byte clear.
- Live IDA MCP `trace_data_flow 0x004623c0 backward` finds no incoming refs, while `make_signature_for_range 0x004623c0-0x00462514` returns a unique wildcarded range signature.

2026-06-28 B009 source-quality implementation using MCP session `b001_000241_20260627`:

- `server_health` reported status `ok`, active IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, auto-analysis ready, and Hex-Rays ready.
- `lookup_funcs` reconfirmed `sub_4623C0` at `0x004623c0-0x00462514`; `0x00462514` is not a function start, successor `sub_462520` begins at `0x00462520`, and `0x00462320` is the sibling clipped buffer-view helper.
- Decompilation/disassembly prove `ECX` is the source alpha surface object, `arg_0` is a caller-supplied destination object/view storage pointer, and `arg_4` is an optional clip rectangle.
- `xrefs_to` / `xref_query` reconfirm no start/end xrefs for `0x004623c0` or `0x00462514`; `find_bytes` found no direct VA/RVA literal matches for the helper start. This remains a confidence cap, not a no-code blocker.
- `xrefs_to 0x006112ec` keeps the `0x00462415` store as an `IntAlphaSurface` vtable construction/use-site inside this helper, while RTTI still keeps the vtable artifact class-owned by `IntAlphaSurface`.
- Rectangle helper analysis resolves `0x004b7c50` as `InitRectBounds(RectBounds*, left, top, right, bottom)` and `0x004b7cc0` as `IntersectRects(first, second, out)`.
- `get_bytes 0x00462514-0x00462520` reconfirmed twelve `0xcc` bytes after the helper.

## Current Ownership Notes

This page is owned and emitted by [UID:00006K][IntAlphaSurface](by-class/IntAlphaSurface.md), position `7`, and physically belongs to [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md). `ECX` is the source `IntAlphaSurface` receiver and the explicit destination pointer supplies storage for another non-owning `IntAlphaSurface` view. Placement construction is the human source form of the observed destination-vtable and zero-state writes.

## Historical / Superseded Ownership Notes

The following AlphaMaskSurface-helper interpretation predates the accepted RTTI/vtable and whole-file class resolution. Its observations about receiver/out-parameter mechanics remain evidence, but its ownership conclusion is not current.

Keep this page attached to [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md) and nested conceptually under the larger [UID:0000YL][0x00462170-0x00462e03.AlphaMaskSurface](by-memory/0x00462170-0x00462e03.AlphaMaskSurface.md) aggregate. The helper writes the same vtable used by the provisional `IntAlphaSurface` destructor, but the live prototype/disassembly prove `ECX` is the source `AlphaMaskSurface` object and the destination `IntAlphaSurface *` is an explicit caller-supplied out parameter. Therefore the source-facing route is an AlphaMaskSurface helper that constructs/initializes a non-owning `IntAlphaSurface` view, not an `IntAlphaSurface` member method.

The formal C++ models the binary vtable store and zero-state initialization as placement construction of the destination `IntAlphaSurface`. Do not emit a decompiler-style direct `off_6112EC` assignment. The absence of direct callers remains documented as a confidence cap, but no longer blocks first-draft C++ for this exact helper.

## Cross-References

- [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md)
- [UID:0000K8][IntAlphaSurface](by-file/IntAlphaSurface.md)
- [UID:0000YL][0x00462170-0x00462e03.AlphaMaskSurface](by-memory/0x00462170-0x00462e03.AlphaMaskSurface.md)
- [UID:0000U3][BuildClippedAlphaSurfaceView_00462320](by-item/BuildClippedAlphaSurfaceView_00462320.md)
- [UID:0000UU][InitAlphaSurfaceView_004623C0](by-item/InitAlphaSurfaceView_004623C0.md)
- [UID:0001TP][AlphaMaskSurfaceLayout](by-type/by-struct/AlphaMaskSurfaceLayout.md)
- [UID:0001TQ][AlphaSurfaceBufferView](by-type/by-struct/AlphaSurfaceBufferView.md)
- [UID:0001XT][IntAlphaSurfaceVtable](by-type/by-vtable/IntAlphaSurfaceVtable.md)

## Open Questions

- The exact original helper spelling is still inferred; `InitAlphaSurfaceView` is the current source-facing descriptive name and is consistent with the documented behavior.
- The missing direct callers may reflect retained/dead code, an indirect construction pattern not currently modeled, or source-level code that was kept despite no current direct xref. This caps confidence but does not change the accepted `IntAlphaSurface` member disposition.

## Changes

- 2026-08-17 B005 UID0000HF Gate 2A reconciliation:
  - Corrected the Item Summary and every active score/owner/emitter/class statement to `IntAlphaSurface::InitSurfaceView`, `93/94`, UID00006K position `7`, physically routed through UID0000HF.
  - Historicalized the earlier AlphaMaskSurface-helper/source-split conclusion while retaining its no-caller, out-parameter, vtable-write, rectangle, and boundary evidence.

- 2026-06-28 B009 implementation: raised `85/89` to `90/91`, kept owner/emitter [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md), and inserted formal first-draft C++ for `AlphaMaskSurface::InitAlphaSurfaceView(IntAlphaSurface *view, const RectBounds *clipBounds) const`. Current MCP session `b001_000241_20260627` proved source `ECX` object, destination `IntAlphaSurface *` out parameter, optional `RectBounds *`, helper callees, no direct caller/pointer state, `off_6112EC` construction/use-site, and `0xcc` post-helper padding. The former source-split provisional blocker is narrowed: `IntAlphaSurface` remains the destination type/vtable owner, while this helper remains emitted by AlphaMaskSurface.
- 2026-06-15 A001 Goal 2 Wave2/IDA refresh: raised `80/88` to `85/89` after [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md) reached `85/90`. Added Wave2 class-filter absence evidence, current live IDA boundary/no-caller/decompilation evidence, and unique range signature evidence. Owner/emitter remain [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md); final C++ remains blank because the original source form and `IntAlphaSurface` versus `AlphaMaskSurface` placement are still unresolved.
- 2026-06-05 A001: Live IDA refresh raised `76/86` to `80/88`, attached [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md), added MD5-stamped boundary/callee/vtable/padding evidence, and kept final C++ blank because direct callers and final `IntAlphaSurface` versus `AlphaMaskSurface` ownership remain open.
