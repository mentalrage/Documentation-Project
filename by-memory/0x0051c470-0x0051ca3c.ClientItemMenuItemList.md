*** UID:0001BQ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000KF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ClientItemMenuItemList::ClientItemMenuItemList(
    unsigned char packetSubtype,
    unsigned int menuObjectId,
    unsigned short menuOptionId,
    ClientItemMenuDialog *owner)
    : ListPane(sizeof(ClientItemMenuEntry), 0x10, 0xe4, 0x18,
               false, true, 1),
      m_packetSubtype(packetSubtype),
      m_menuObjectId(menuObjectId),
      m_menuOptionId(menuOptionId),
      m_owner(owner)
{
}

void ClientItemMenuItemList::AppendAction(
    unsigned char actionId, unsigned short itemId,
    unsigned char itemStyle, const wchar_t *name)
{
    ClientItemMenuEntry entry;
    entry.actionId = actionId;
    entry.itemId = itemId;
    entry.itemStyle = itemStyle;
    wcscpy_s(entry.name, _countof(entry.name), name);
    AddEntry(&entry);
}

void ClientItemMenuItemList::SendSelectedAction()
{
    if (m_selectedIndex < 0)
        return;

    ClientItemMenuEntry *entry =
        static_cast<ClientItemMenuEntry *>(GetItem(m_selectedIndex));
    SendAction(entry->actionId);
}

void ClientItemMenuItemList::SendAction(unsigned char actionId)
{
    unsigned char packet[0x204];
    PacketBufferWriteUInt8(0x39, packet);
    PacketBufferWriteUInt8(m_packetSubtype, packet + 1);
    PacketBufferWriteUInt32BE(m_menuObjectId, packet + 2);
    PacketBufferWriteUInt16BE(m_menuOptionId, packet + 6);
    PacketBufferWriteUInt8(actionId, packet + 8);
    packet[9] = 0;

    g_packetSender->QueueAndSendPacket(packet, 9);
}

void ClientItemMenuItemList::OnItemActivated()
{
    m_owner->UpdateActionButton();
}

void ClientItemMenuItemList::DrawListEntry(
    int rowIndex, void *rowData, RectBounds *rowBounds)
{
    ClientItemMenuEntry *entry =
        static_cast<ClientItemMenuEntry *>(rowData);
    const bool selected = IsItemSelected(rowIndex);
    if (selected) {
        SetDrawColor(0x25);
        g_pfnFillRect(this, rowBounds);
    }
    SetTextColor(selected ? 0x80 : 0x25);

    RectBounds glyphBounds;
    RectBounds imageBounds;
    g_pItemObjImageLib->GetItemGlyphBounds(entry->itemId, &glyphBounds);
    if (g_useEpfAssets) {
        imageBounds.left = 12 -
            (glyphBounds.right - glyphBounds.left) / 4;
        imageBounds.right = imageBounds.left +
            (glyphBounds.right - glyphBounds.left) / 2;
        imageBounds.top = (rowBounds->top + rowBounds->bottom) / 2 -
            (glyphBounds.bottom - glyphBounds.top) / 4;
        imageBounds.bottom = imageBounds.top +
            (glyphBounds.bottom - glyphBounds.top) / 2;
        IntersectRects(&imageBounds, rowBounds, &imageBounds);
        g_pItemObjImageLib->DrawItemImageScaled(
            this, &imageBounds, entry->itemId, entry->itemStyle);
    } else {
        imageBounds.left = 12 -
            (glyphBounds.right - glyphBounds.left) / 2;
        imageBounds.right = imageBounds.left +
            (glyphBounds.right - glyphBounds.left);
        imageBounds.top = (rowBounds->top + rowBounds->bottom) / 2 -
            (glyphBounds.bottom - glyphBounds.top) / 2;
        imageBounds.bottom = imageBounds.top +
            (glyphBounds.bottom - glyphBounds.top);
        g_pItemObjImageLib->DrawItemImage(
            this, &imageBounds, entry->itemId, entry->itemStyle,
            NULL, 0.0f);
    }

    SetTextDrawMode(1);
    SetTextBackFillColor(0);
    wchar_t fittedName[256];
    wcscpy_s(fittedName, _countof(fittedName), entry->name);
    RectBounds nameBounds;
    nameBounds.left = 50;
    nameBounds.right = rowBounds->right;
    nameBounds.top = (rowBounds->top + rowBounds->bottom) / 2 - 6;
    nameBounds.bottom = nameBounds.top + 12;
    FitTextAndAppendSuffix(
        fittedName, _countof(fittedName), L"...",
        nameBounds.right - nameBounds.left);
    DrawTextInRect(fittedName, wcslen(fittedName), &nameBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 94/94 ClientItemMenuItemList source child with six authored bodies, three retained raw starts, 0x206 row and 0x15c object layouts, fixed nine-byte opcode 0x39 action sends, direct drawing dependencies, implicit ordinary destruction with shared compiler glue, class owner UID00002Q, and ordered ItemMenuDialogs CPP emission. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0051c470-0x0051ca3c ClientItemMenuItemList

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method cluster.
- Current generated owner: `ClientItemMenuItemList`
- Canonical semantic owner: [UID:00002Q][ClientItemMenuItemList](by-class/ClientItemMenuItemList.md)
- Likely source/emitter module: [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md)
- Confidence: strong for behavior, range, constructor refs, vtable identity, corrected row-record layout, opcode `0x39` client action semantics, and source/emitter placement; medium-high for exact original helper/prototype names because several helper starts have no IDA function objects.
- Evidence basis: existing documentation plus targeted IDA MCP `lookup_funcs`, `xrefs_to`, `disasm`, and `get_bytes` checks on 2026-06-01/2026-06-14, B002 2026-06-19/20 Rule 26 aggregate source-quality reanalysis, B013 2026-06-21 packet-length correction, and B015 2026-06-27 rejected-report amendment recheck in current MCP session `b001_000241_20260627`.

## Covered Range

This page covers the contiguous `ClientItemMenuItemList` island from constructor at `0x0051c470` through row drawing virtual ending at exclusive `0x0051ca3c`.

| Range | IDA status | Function or role |
| --- | --- | --- |
| `0x0051c470-0x0051c4dc` | IDA function, size `0x6c` | Constructor; initializes `ListPane` geometry, context fields, and vtables. |
| `0x0051c4e0-0x0051c548` | raw code | Appends a client item row from local inventory record data. |
| `0x0051c550-0x0051c603` | raw code | Sends selected-row action packet. |
| `0x0051c610-0x0051c6ac` | raw code | Sends caller-supplied action packet. |
| `0x0051c6b0-0x0051c6bb` | IDA function, size `0xb` | Activation thunk into shared item-state logic. |
| `0x0051c6c0-0x0051ca3c` | IDA function, size `0x37c` | Row drawing virtual. |

## Parent And Score Rationale

This page is canonically owned by [UID:00002Q][ClientItemMenuItemList](by-class/ClientItemMenuItemList.md) while emitted through [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md); both current pages are `94/94`, and the file owner retains reviewed `NexusTK/ui/dialogs/` placement with distinct `ItemMenuDialogs.cpp`/`.h` output. The surrounding aggregate [UID:0001BO][0x0051a520-0x0051c46d.ItemMenuDialogs](by-memory/0x0051a520-0x0051c46d.ItemMenuDialogs.md) records the server/client item-menu grouping, exact constructor/`OnControlCommand`/`UpdateActionButton` children, and the `0x0051c46d-0x0051c470` padding before this page. Earlier `86/85` file and `88/89` class values were dated intermediate scores superseded by the complete 21-body/four-declaration, ownership, behavior, and formal-source closure that supports the current `94/94` state.

Completion/confidence are `94/94`. Exact ranges and hashes, all six authored bodies, constructor callers, field/vtable map, alignment, `0x206` row layout, fixed nine-byte action behavior, activation/drawing, implicit/compiler destructor split, formal source, class ownership, and emitter order are closed. The cap remains inferred private lexical spelling plus unproven compile/runtime/byte parity; zero raw-start xrefs no longer block source retention.

## Constructor Field Map

| Address | Evidence | Meaning |
| --- | --- | --- |
| `0x0051c475-0x0051c48e` | pushes `1, 1, 0, 0x18, 0xe4, 0x10, 0x206`, then calls `0x004f3a50` | Constructs the `ListPane` base with client item-menu geometry/state. |
| `0x0051c493` | stores byte argument into `this + 0x14c` | Packet/menu subtype byte copied into list state. |
| `0x0051c49c` | stores dword argument into `this + 0x150` | Object/menu id or item context copied into list state. |
| `0x0051c4a5` | stores word argument into `this + 0x154` | Item/menu option context copied into list state. |
| `0x0051c4b0` | stores dword argument into `this + 0x158` | Owning dialog/context pointer used by activation. |
| `0x0051c4bb` | writes vtable `0x0061f0a4` | Primary `ClientItemMenuItemList` vtable. |
| `0x0051c4c1` | writes vtable `0x0061f12c` at `this + 0xa0` | Secondary list-pane/interface vtable. |
| `0x0051c4cb` | writes vtable `0x0061f15c` at `this + 0xa4` | Tertiary list-pane/interface vtable. |

## Row Record Layout

The client list row is a `0x206`-byte record. Evidence: the constructor passes `0x206` as the `ListPane` row stride; append code writes an action byte at `row + 0x000`, item id word at `row + 0x002`, item style byte at `row + 0x004`, and `name[256]` at `row + 0x006`; selected/explicit packet helpers serialize the selected or caller-supplied action byte; and the draw virtual reads item id/style plus the row label.

| Offset | Source-facing field | Evidence |
| --- | --- | --- |
| `+0x000` | `unsigned char actionId` | Raw append writes the first byte here; selected packet helper serializes this value in the opcode `0x39` payload. |
| `+0x001` | `unsigned char reserved` | Padding/copied stack byte with no proven semantic read. Keep reserved until source or packet evidence proves a name. |
| `+0x002` | `unsigned short itemId` | Raw append writes a word here; draw uses it with `itemStyle` for item icon/image lookup. |
| `+0x004` | `unsigned char itemStyle` | Raw append writes a byte here; draw uses it with `itemId` for item imagery. |
| `+0x005` | `unsigned char reserved2` | Padding/copied stack byte with no proven semantic read. Keep reserved in first-draft C++. |
| `+0x006` | `wchar_t name[256]` | Copied by `_wcscpy_s`, drawn as the client item-menu row label, and truncated with ellipsis in draw paths. |

Total size is `0x206`, matching the `ListPane` constructor stride. Unlike the server list, the client row genuinely carries an `actionId`: selected and explicit opcode `0x39` paths serialize the action byte after `m_packetSubtype`, `m_menuObjectId`, and `m_menuOptionId`. B013 narrows the dialog-selected action path to a length-`9` packet and supersedes older trailing-null wording for this client item-menu action byte.

## Evidence

- `lookup_funcs` confirms functions at `0x0051c470`, `0x0051c6b0`, and `0x0051c6c0`.
- `xrefs_to 0x0051c470` reports constructor calls inside `ClientItemMenuDialog` at `0x0051ba35` and `0x0051bf64`.
- Disassembly confirms raw helper-shaped code at `0x0051c4e0`, `0x0051c550`, and `0x0051c610`; current IDA function metadata does not model these starts.
- `lookup_funcs` confirms `0x0051c4e0`, `0x0051c550`, and `0x0051c610` are not IDA function starts, while `0x0051ca40` begins the next `ServerSpellMenuDialog` body.
- `get_bytes 0x0051c46d size 3` returns three `0xcc` bytes before the constructor, confirming alignment from `ClientItemMenuDialog`.
- `get_bytes 0x0051c470 size 108` captures the constructor through `c2 10 00`; `get_bytes 0x0051c4dc size 4` returns four `0xcc` bytes after it.
- `get_bytes 0x0051ca3c size 4` returns four `0xcc` bytes after the draw virtual, confirming the exclusive end and the padding before `0x0051ca40`.
- `xrefs_to` confirms vtable writes from the constructor to `0x0061f0a4`, `0x0061f12c`, and `0x0061f15c`.
- `xrefs_to` confirms virtual slots for activation and draw: `0x0051c6b0` from `0x0061f120` and `0x0051c6c0` from `0x0061f124`.
- `xrefs_to 0x00520c20` reports the shared scalar deleting destructor is referenced from adjustor thunks `0x00520ad1` and `0x00520adc`, plus primary vtable slots `0x0061f0a4`, `0x0061f204`, and `0x0061f364`.
- `0x0051c6b0` is a two-instruction thunk: load `[this + 0x158]` into `ecx`, then jump to `0x0049de70`.
- The draw virtual uses `g_useHighResLayout` to switch between half-scale EPF-style icon drawing and legacy full-size icon drawing.
- 2026-06-14 C001 IDA MCP recheck confirms the constructor, activation thunk, and draw virtual sizes as `0x6c`, `0x0b`, and `0x37c`; confirms `0x0051c4e0`, `0x0051c550`, and `0x0051c610` are raw non-function helper starts with no direct raw-start xrefs; reconfirms constructor callers at `0x0051ba35`/`0x0051bf64`; and reconfirms the successor function at `0x0051ca40`.
- The same live pass reconfirms shared destructor support at `0x00520ad1`, `0x00520adc`, and `0x00520c20`, vtable writes to `0x0061f0a4`/`0x0061f12c`/`0x0061f15c`, activation/draw slots at `0x0061f120`/`0x0061f124`, and `0xcc` alignment bytes at the predecessor, constructor-to-raw-helper, and successor boundaries.
- 2026-06-27 B015 MCP recheck in session `b001_000241_20260627` reconfirms constructor field/vtable stores, the `0x206` append-row layout, selected/explicit opcode `0x39` action send helpers, activation thunk shape, draw virtual row-field reads, vtable refs, constructor xrefs, and `0xcc` padding/range boundaries. The raw exactness concerns are now reflected in the formal C++: `AppendAction` passes the caller `name` pointer directly to `_wcscpy_s`; `SendSelectedAction` returns `void`, checks only a negative selected index, fetches the row without an upper-bound/null-entry guard, and duplicates the packet build; `SendAction` returns `void`; both send paths write a local-only `packet[9] = 0` and call `QueueAndSendPacket(..., 9)`.

## Behavioral Notes

| Range | IDA evidence | Behavior |
| --- | --- | --- |
| `0x0051c4e0-0x0051c548` | Packs action byte, item id word, style byte, reserved padding bytes, and a copied wide label into stack storage, then calls `0x004f3c50`; `_wcscpy_s` receives the caller `name` pointer directly. | Appends a local/client item-menu row without a documented null-name fallback. |
| `0x0051c550-0x0051c603` | Reads selected index from `this + 0x134`, returns only when it is negative, fetches the row via `0x004f3dc0`, writes opcode `0x39`, context fields from `0x14c/0x150/0x154`, selected row action byte, and local scratch zero at byte `9`, then queues length `9`. | Sends the currently selected client item action using the same length-`9` shell documented by B013; the helper does not return a boolean and does not call the explicit-send helper. |
| `0x0051c610-0x0051c6ac` | Writes the same opcode/context shell but uses caller-supplied byte `[ebp+8]` for the action, writes local scratch zero at byte `9`, and queues length `9`. | Sends an explicit client item action without returning a boolean; the scratch zero is not part of the protocol payload. |
| `0x0051c6b0-0x0051c6bb` | Loads owner/context from `this + 0x158` and jumps to `0x0049de70`. | Forwards activation into shared item-action state logic. |
| `0x0051c6c0-0x0051ca3c` | Branches on `byte_66DA97`, uses selected-state helpers, draws icons through EPF/item image paths, and renders row label text in high-resolution and legacy layouts. | Draws one client item-menu row. |

## Generated Data Caveats

- Active `class_ClientItemMenuItemList.cpp` omits the raw helper bodies at `0x0051c4e0`, `0x0051c550`, and `0x0051c610`.
- Active generated ownership of `0x004f3b60` as `ClientItemMenuItemList::~ClientItemMenuItemList` is too narrow; IDA caller fanout shows broad shared `ListPane` cleanup.
- Destructor thunks at `0x00520ad1` and `0x00520adc`, and scalar deleting destructor `0x00520c20`, are shared with spell menu item-list vtables.
- The metadata C++ box now carries first-draft source-style code for the row layout, append helper, selected/explicit action packet helpers, activation forwarding, and draw path. B013 removes the older trailing-null packet wording for the client action helper.
- First-draft C++ caveat: helper, draw-support, UI/control, and packet-writer names remain source-facing inferred names until recovered headers/source spellings prove exact identifiers. The formal C++ block is nevertheless amended to match the raw helper behavior for the current exactness concerns: `AppendAction` uses an uninitialized local row object and passes the caller's `name` pointer directly to `_wcscpy_s`; `SendSelectedAction` returns `void`, only checks for a negative selected index, fetches the row without an upper-bound/null-entry guard, and duplicates the packet build; `SendAction` returns `void`; both action send paths write a local scratch zero after the action byte and call `QueueAndSendPacket(..., 9)`, so that zero is not sent as protocol payload.

## 2026-08-22 UID0000KF Accepted Source

The formal CPP removes the older duplicated selected packet body, synthetic `NotifyOwnerItemActivated`, `DrawContext`, null-row draw guard, and invented icon/text helpers. It uses `GetItem`, a void negative-index-only selected sender delegating to void `SendAction`, direct packet writers and exact length 9, `OnItemActivated()`, UID0000TN fill, UID00006W image methods, and current GrafPort text methods. Exact constructor/action/draw hashes are `C00DFF9C66B68B5FE6BEA454F683E0E4A358815AF8207D99639185A1334B0AA1`, `5D47939801B9C4A7171F805D4AF67ED1EBE071E1F1A3A76B0EA06CD7BA8798EE`, and `1F3D57650A37F69CF97049FCEC132F50B73F691E82BB7A00F9B496F41A050759`; raw append/selected/explicit hashes are `1CE275A806BBAAB2145189BE5D1DB844C70C15B27C102C723DB32094E8A06F82`, `8B9BD3F8135D07B992061C58FAA1E79E4D2B614A1707BAFAFEC4D8E200AA6879`, and `A7C1D460112F263F18667C28CA7AB7CDBB9236911226954A7887C5E064524F59`. UID0001BQ remains owned by UID00002Q, emitted through UID0000KF at position 40, and ends before `[0x0051ca3c,0x0051ca40)` alignment and the SpellMenu successor.

## Cross-References

- [UID:00002Q][ClientItemMenuItemList](by-class/ClientItemMenuItemList.md)
- [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md)
- [UID:00002P][ClientItemMenuDialog](by-class/ClientItemMenuDialog.md)
- [UID:0001BO][0x0051a520-0x0051c46d.ItemMenuDialogs](by-memory/0x0051a520-0x0051c46d.ItemMenuDialogs.md)
- [UID:0001BP][0x0051ae90-0x0051b87a.ServerItemMenuItemList](by-memory/0x0051ae90-0x0051b87a.ServerItemMenuItemList.md)
- [UID:0001BY][0x00520ad1-0x00520ae7.SharedClientSpellMenuListDestructorThunks](by-memory/0x00520ad1-0x00520ae7.SharedClientSpellMenuListDestructorThunks.md)
- [UID:0001C1][0x00520c20-0x00520c5b.SharedListPaneScalarDeletingDestructor](by-memory/0x00520c20-0x00520c5b.SharedListPaneScalarDeletingDestructor.md)
- [UID:0000O2][SpellMenuDialogs](by-file/SpellMenuDialogs.md)

## Changes

### Historical/Superseded Checkpoints: 2026-06-01 Through 2026-06-27

The `50/65`, `78/84`, `82/84`, `86/86`, and `88/89` scores, below-gate parent wording, and no-final-C++/source-name blocker statements below are historical intermediate states. They were superseded by the 2026-08-22 94/94 closure: class UID00002Q is the canonical owner, file UID0000KF is the emitter into distinct `ItemMenuDialogs.cpp`/required `ItemMenuDialogs.h`, all six authored bodies and the exact `0x206` row layout have complete formal source, and the implicit-destructor/compiler-glue split is closed. The still-valid constructor callers, raw-head status, vtable routes, packet length, padding, and field evidence remains preserved as support for that accepted route.

- 2026-06-27 B015 rejected-report amendment implementation: changed `CANONICAL_OWNER` to [UID:00002Q][ClientItemMenuItemList](by-class/ClientItemMenuItemList.md) while keeping `EMITTER_UIDS:0000KF`, `COMPLETION:88`, `CONFIDENCE:89`, and `RECONSTRUCTABLE:TRUE`; removed stale summary wording about the first-draft C++ being unavailable; added current MCP session `b001_000241_20260627` evidence; and replaced the formal C++ block so it matches raw helper exactness for direct `_wcscpy_s(name)`, `void` send helpers, selected negative-index-only guard, duplicated packet build, local-only `packet[9] = 0`, and `QueueAndSendPacket(..., 9)`.
- 2026-06-07 A007 parent-gate sync: raised completion from `78` to `82` while keeping confidence at `84`, added parent/score rationale, and linked the item-menu aggregate plus shared destructor-thunk/scalar-destructor evidence. Evidence: the page already records exact range, constructor caller refs, raw/modeled helper map, vtable writes/slots, padding checks, packet helper behavior, activation/draw behavior, and generated-output caveats; [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md) was below its current confidence score, and [UID:0001BO][0x0051a520-0x0051c46d.ItemMenuDialogs](by-memory/0x0051a520-0x0051c46d.ItemMenuDialogs.md) documents this page as the adjacent client row-list island.
- 2026-06-01: Raised completion/confidence from `50/65` to `78/84`, marked reconstructable, and attached to [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md).
  - Evidence added: constructor field/vtable map, constructor caller refs, raw helper no-function status, exact padding boundaries, vtable slot refs, shared destructor refs, and helper behavior notes.
  - Reconstruction status: no final C++ emitted because source-quality names were not proven.
- 2026-06-14 C001 Goal 2 support refresh:
  - Before: `82/84`, with stale parent-score and old code-entry wording.
  - After: `86/86`, with live IDA 9.1 evidence for modeled function sizes, raw helper non-function/no-xref status, constructor callers, vtable stores/slots, shared destructor refs, and boundary padding. Owner/emitter routing remains [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md); final C++ stays blank until row/packet helper names and fields are source-quality.
- 2026-06-20 B002 Rule 26 source-quality incorporation: raised to `88/89` after the aggregate report proved the `0x206` `ClientItemMenuEntry` layout (`actionId` at `+0x000`, reserved at `+0x001`, `itemId` at `+0x002`, `itemStyle` at `+0x004`, reserved at `+0x005`, `name[256]` at `+0x006`), resolved opcode `0x39` selected/explicit action semantics, named source-facing helpers `AppendAction`, `SendSelectedAction`, and `SendAction`, kept shared destructor thunks/scalar wrapper excluded as compiler glue, and added first-draft C++ under [UID:0000KF][ItemMenuDialogs](by-file/ItemMenuDialogs.md).
- 2026-06-21 B013 Rule 26 source-quality incorporation: score unchanged. Updated selected/explicit action helper wording and first-draft C++ to match the B013 length-`9` client action packet evidence, preserving `AppendAction`, `SendSelectedAction`, and `SendAction` as source-facing helper names.
