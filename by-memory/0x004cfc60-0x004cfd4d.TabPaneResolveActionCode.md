*** UID:00034R | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
signed char TabPane::ResolveTabActionCode(int mouseY, int mouseX) const
{
    for (signed char actionCode = 0; actionCode < 6; ++actionCode) {
        RectBounds rect;
        BuildTabActionRect(actionCode, &rect);

        if (PointInRect(mouseY, mouseX, &rect)) {
            return actionCode;
        }
    }

    return -1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 237-byte TabPane private hit-test resolver: two UID00034O calls, signed-byte 0..5/-1 result, canonical mouseY/mouseX and PointInRect(y,x,bounds) order, six exact rectangles, resolver-local switch table, retained source-level BuildTabActionRect factorization, and no external pointer/table route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cfc60-0x004cfd4d TabPane ResolveActionCode

## Status

- Entity kind: `TabPane` tab hit-test/action-code resolver.
- Direct owner: [UID:0000EB][TabPane](by-class/TabPane.md).
- Source file: [UID:0000OF][TabPane](by-file/TabPane.md).
- Parent aggregate: [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md).
- Rebuild handling: reconstructable source-owned `signed char ResolveTabActionCode(int mouseY, int mouseX) const` helper with complete first-draft C++. The binary resolver does not call [UID:00034Q][0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder](by-memory/0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder.md); it duplicates/inlines the same rectangle construction with its own local table and direct rectangle-helper calls. The formal `BuildTabActionRect` call remains the accepted source-level de-duplication/inlining hypothesis, not observed machine-code reachability.

## Behavior

The helper iterates signed-byte action codes `0..5`, builds a rectangle through `0x004b7c50`, tests `(mouseY, mouseX)` through canonical `PointInRect(int y, int x, const RectBounds *)` at `0x004b7e80`, and returns the matching byte or `-1`. In the executable, this body has its own local switch table at `0x004cfd50`; it does not call the retained raw rectangle builder at `0x004cfb20`.

| Code | Rectangle |
| --- | --- |
| `0` | `(2, 1)-(20, 46)` |
| `1` | `(2, 47)-(20, 93)` |
| `2` | `(2, 95)-(20, 140)` |
| `3` | `(2, 142)-(20, 187)` |
| `4` | `(2, 189)-(20, 234)` |
| `5` | `(2, 236)-(20, 281)` |

## IDA Evidence

- Historical `py_eval` and resumed B003 MCP database `3fc1dcef` on 2026-07-14 report IDA function `sub_4CFC60` at `0x004cfc60-0x004cfd4d`, size `0xed` / decimal 237 (Verified with int_convert.py), 88 instructions, and 14 basic blocks. Fresh `idb_list`/`server_health` established the database as healthy before bounded lookup/get-bytes/analyze/disassembly calls.
- The only direct calls are inside [UID:00034O][0x004cfa10-0x004cfb0a.TabPaneHandlePointerOrMouseEvent](by-memory/0x004cfa10-0x004cfb0a.TabPaneHandlePointerOrMouseEvent.md) at `0x004cfa4c` and `0x004cfaae`.
- Callees are rectangle builder `0x004b7c50`, point/rect test `0x004b7e80`, and security-cookie check `0x005c772f`.
- `py_eval` found no loaded-segment pointer hits to the function start, supporting private helper ownership rather than callback/table ownership.
- The following `0x004cfd4d-0x004cfd6c` bytes are resolver switch/jump-table data, not a separate source function.
- B005's 2026-06-19 PE recheck confirmed direct rel32 calls to this resolver only from `0x004cfa4c` and `0x004cfaae`, both inside [UID:00034O][0x004cfa10-0x004cfb0a.TabPaneHandlePointerOrMouseEvent](by-memory/0x004cfa10-0x004cfb0a.TabPaneHandlePointerOrMouseEvent.md).
- The same PE recheck confirmed no rel32/rel8 route from this resolver to [UID:00034Q][0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder](by-memory/0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder.md) at `0x004cfb20`.
- Resolver switch-table entries at `0x004cfd50` target `0x004cfcee`, `0x004cfc88`, `0x004cfc98`, `0x004cfca8`, `0x004cfcb8`, `0x004cfcca`, and `0x004cfcdc`; these are separate from the raw helper's table at `0x004cfc40`.
- Disassembly keeps the loop/result in `bl`/`al`: `xor bl,bl`, `inc bl`, compare against `6`, `mov al,bl` on a match, and `or al,0xff` on a miss. Both callers consume `al`. This proves the source-facing signed-byte result and rejects the stale full-width `int` return.
- At both calls, UID00034O pushes Event `+0xc` then `+0x8`, establishing first parameter mouseY and second parameter mouseX. This helper pushes the rectangle pointer after the two integer coordinates when calling `0x004b7e80`, matching [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md)'s canonical `PointInRect(y, x, bounds)` declaration.

## Ownership Decision

Attach to [UID:0000EB][TabPane](by-class/TabPane.md). Both direct callers are inside the `TabPane` input handler, the geometry is `TabPane` tab geometry, and no `IconsPane` vtable/global refs appear in the function.

## Score Rationale

Completion `90` reflects exact bounds/size/CFG, two callers, callees, signed-byte return ABI, y/x contract, canonical PointInRect order, rectangle and switch tables, factorization boundary, negative routes, owner, and formal C++. Confidence `93` is strong for behavior and ownership; exact original helper spelling, final tab labels, and whether the source compiler inlined the retained helper remain bounded inference caps.

## Reconstruction Notes

The draft captures the source behavior documented by IDA: test the six fixed rectangles in order with y before x, return the matching signed-byte action code, or return `-1`. The helper call to `BuildTabActionRect` is a source-level factoring candidate, not a literal call in the machine code. If a later source audit rejects the inlining/de-duplication hypothesis, inline the six `InitRectBounds` cases directly while keeping UID00034Q as retained out-of-line/dead helper code; do not regress the return width or PointInRect argument order.

## Cross-References

- [UID:0000EB][TabPane](by-class/TabPane.md)
- [UID:0000OF][TabPane](by-file/TabPane.md)
- [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md)

## Changes

- 2026-07-14 B003 UID00034N focused family repair:
  - Before: `86/90`, stale full-width `int ResolveTabActionCode(int x, int y)` and rect-first `PointInRect` formal despite byte-register returns and canonical geometry-helper order.
  - Changed to: `90/93`, exact `signed char ResolveTabActionCode(int mouseY, int mouseX) const`, signed-byte loop/result, and `PointInRect(mouseY, mouseX, &rect)` while preserving source-level BuildTabActionRect factorization and every binary no-call/table fact.
  - Evidence: resumed live MCP database `3fc1dcef`, `bl`/`al` return flow, both UID00034O push sequences, canonical UID00015S declaration, exact local tables, and two-call-only route.

- 2026-06-21 Rule 26 B005 support incorporation:
  - Before: confidence `89`; the draft noted possible inlining but did not spell out that the executable resolver has its own table and does not call [UID:00034Q][0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder](by-memory/0x004cfb20-0x004cfc5c.TabPaneRawRectangleBuilder.md).
  - Changed to: confidence `90` after adding the direct PE route distinction and resolver-table relationship.
  - Summary/evidence: B005 rechecked rel32 callers from `0x004cfa4c` and `0x004cfaae`, no rel32/rel8 route to `0x004cfb20`, resolver-local table `0x004cfd50`, direct calls to `InitRectBounds` / `PointInRect`, and the source-level de-duplication policy for generated `BuildTabActionRect` use.
