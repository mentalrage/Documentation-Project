*** UID:000207 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:82 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x004607e0-0x0046093c RGB565 Transparent Alpha Blend Span Raw Helper

## Status

- Disposition: reconstructable NexusTK render support code.
- Entity kind: raw/unmodeled free render helper.
- Likely source file: [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) `SoftwareBlend16`.
- Confidence: strong for exact `0x15c`-byte raw boundary, 118 decoded instructions, no raw-start xrefs, block helper call, source/destination argument order, transparent-source test, scalar RGB565 weighted math, row-pitch stepping, and SoftwareBlend16 ownership; medium for final source-facing helper name and reachability because IDA has no function object or external xref to the raw start.
- Rebuild handling: `source-authored`.
- C++ reconstruction: final metadata remains intentionally blank until source-facing name/signature and raw reachability reach final-source confidence; the sketch below is non-final documentation.

## Address Range

`0x004607e0-0x0046093c`, size `0x15c`, 118 decoded instructions.

The helper starts immediately after `0x00460710-0x004607e0` and ends at its `retn` at `0x0046093b`. The following `0x0046093c-0x00460940` bytes are alignment padding.

Live IDA MCP `py_eval` on 2026-06-05 confirms `ida_funcs.get_func(0x004607e0)` is `None`, `idautils.XrefsTo(0x004607e0)` is empty, [UID:000208][0x00460940-0x004609d3.BlitTransparentShadow565MmxBlocks](by-memory/0x00460940-0x004609d3.BlitTransparentShadow565MmxBlocks.md) is the next modeled function, and [UID:000209][0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks](by-memory/0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks.md) is the modeled block helper called by this raw body. This page therefore documents a raw code body that IDA has not promoted to a function.

## Inferred Signature

Current argument use supports this source-level shape:

```cpp
void Rgb565TransparentAlphaBlendSpanRaw(
    const uint16_t* src,
    int srcPitchBytes,
    uint16_t* dst,
    int dstPitchBytes,
    unsigned int alpha32,
    int widthPixels,
    int rowCount);
```

The scalar tail tests the source pixel and skips the write when it is zero. The block path passes a single byte weight, `32 - alpha32`, to [UID:000209][0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks](by-memory/0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks.md); that helper expands the weight and computes the complementary destination-side weight internally.

## Behavior

This is an RGB565 transparent weighted span helper. It uses a caller-supplied 5-bit destination-side weight and a source-side weight of `32 - weight`. The block path calls [UID:000209][0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks](by-memory/0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks.md); the scalar tail skips source pixels whose value is zero and blends nonzero source pixels into the destination using RGB565 masks `0xf81f` and `0x07e0`.

The helper advances source and destination pointers by row pitches and repeats for the row count.

## Reconstruction Evidence

| Evidence | Live IDA result | Documentation impact |
| --- | --- | --- |
| Raw boundary | `0x004607e0-0x0046093c` is `0x15c` bytes and 118 decoded instructions; `0x0046093c-0x00460940` is four `0xcc` bytes; `sub_460940` starts at `0x00460940`. | Exact raw helper range is recoverable even though IDA has no function object at the start. |
| Xrefs and calls | `XrefsTo(0x004607e0)` is empty; `XrefsTo(0x004609e0)` has only raw call `0x00460818`; the raw body has a single call, `0x00460818 -> sub_4609E0`. | Keep the reachability caveat while documenting the MMX block dispatch as direct evidence. |
| Block argument order | The call pushes row count, block count, `32 - [ebp+18h]`, `[ebp+14h]`, `[ebp+10h]`, `[ebp+0ch]`, and `[ebp+8]` before `sub_4609E0`. | Cdecl order is source pointer, source pitch, destination pointer, destination pitch, source-side weight, four-pixel block count, row count. |
| Block/tail split | `widthPixels / 4` is stored at `[ebp-8]`; after the block call, source and destination advance by `blockCount * 4` pixels and `[ebp+1ch]` becomes the scalar tail count. | Confirms the block helper covers only four-pixel blocks and this raw body owns the scalar remainder. |
| Transparent scalar branch | `0x00460870` loads the source word, `0x00460874` tests it, and `0x00460877` skips the blend/write path when the source word is zero. | Confirms transparent-source behavior. |
| Scalar RGB565 math | `0x00460880`/`0x00460897`/`0x004608b9`/`0x004608e2` apply `0xf81f`; `0x004608b3`/`0x004608cc`/`0x004608e8` apply `0x07e0`; shifts by 5 divide the weighted sums by 32; `0x004608f9` stores the destination word. | Confirms weighted RGB565 channel blending and destination write side. |
| Row stepping | `0x0046091d` adds source pitch `[ebp+0ch]`; `0x00460921` adds destination pitch `[ebp+14h]`; `0x0046092f` loops on row count. | Confirms byte-pitch semantics for both row pointers. |

## Reconstructed C++ Candidate

```cpp
static inline uint16_t BlendRgb565TransparentWeighted(uint16_t src, uint16_t dst,
                                                       unsigned int alpha32)
{
    if (src == 0)
        return dst;

    const unsigned int inv = 32 - alpha32;
    const unsigned int rb = ((((src & 0xf81f) * inv) +
                              ((dst & 0xf81f) * alpha32)) >> 5) & 0xf81f;
    const unsigned int g = ((((src & 0x07e0) * inv) +
                             ((dst & 0x07e0) * alpha32)) >> 5) & 0x07e0;
    return static_cast<uint16_t>(rb | g);
}

void Rgb565TransparentAlphaBlendSpanRaw(const uint16_t* src, int srcPitchBytes,
                                        uint16_t* dst, int dstPitchBytes,
                                        unsigned int alpha32,
                                        int widthPixels, int rowCount)
{
    const int blockPixels = (widthPixels / 4) * 4;
    const int tailPixels = widthPixels - blockPixels;

    if (blockPixels > 0)
        Rgb565TransparentAlphaBlendMmxBlocks(src, srcPitchBytes, dst, dstPitchBytes,
                                             32 - alpha32, blockPixels / 4, rowCount);

    src += blockPixels;
    dst += blockPixels;

    for (int y = 0; y < rowCount; ++y)
    {
        for (int x = 0; x < tailPixels; ++x)
            dst[x] = BlendRgb565TransparentWeighted(src[x], dst[x], alpha32);

        src = reinterpret_cast<const uint16_t*>(reinterpret_cast<const unsigned char*>(src) + srcPitchBytes);
        dst = reinterpret_cast<uint16_t*>(reinterpret_cast<unsigned char*>(dst) + dstPitchBytes);
    }
}
```

The exact original helper names still need final source proof, but the source/destination pointer roles, source-side block weight, and scalar formula are supported by the block-helper call, source-zero test, destination store, and row advancement. This candidate is not promoted into the reconstruction metadata because the final source-facing name and raw reachability are not source-quality yet.

## Evidence

- 2026-06-05 live IDA MCP `py_eval` confirms `ida_funcs.get_func(0x004607e0)` and `ida_funcs.get_func(0x0046093c)` are `None`, while `sub_460940` covers `0x00460940-0x004609d3` and `sub_4609E0` covers `0x004609e0-0x00460af9`.
- 2026-06-05 live IDA MCP reports no direct xrefs to `0x004607e0`; `XrefsTo(0x004609e0)` has only the raw body call at `0x00460818`.
- Live disassembly shows the raw body computes `widthPixels / 4`, calls `sub_4609E0` when the block count is positive, passes `32 - alpha32` as the block-helper weight, advances source and destination pointers by `blockCount * 4` pixels, and leaves `widthPixels - blockPixels` for scalar handling.
- The scalar tail loads the source word at `0x00460870`, skips the blend/write when it is zero, blends nonzero source pixels against the destination with masks `0xf81f` and `0x07e0`, shifts weighted sums by 5, and stores the result to the destination word at `0x004608f9`.
- Row advancement uses source pitch `[ebp+0ch]` at `0x0046091d` and destination pitch `[ebp+14h]` at `0x00460921`, confirming byte-pitch argument roles.
- Live byte inspection confirms `0x0046093c-0x00460940` is four `0xcc` bytes and `0x00460940` starts the next modeled helper.

## Score Rationale

- Completion `82`: exact raw extent, block-call wiring, source/destination argument order, source-side block weight, block/tail split, transparent scalar branch, RGB565 weighted formula, row stepping, and sibling helper behavior are live-verified; original source name/reachability and final declaration shape remain open.
- Confidence `88`: repeated live IDA evidence agrees on the body and behavior, but the lack of an IDA function object or raw-start xref keeps this below the fully modeled SoftwareBlend16 siblings.
- Reconstructable `TRUE`: the implementation is source-authored render support code with a bounded prologue/epilogue body and no evidence that it is data, padding, or compiler-only glue.

## Cross-References

- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) SoftwareBlend16.
- [UID:0000YE][0x00460500-0x00460709.AlphaBlendSpan16](by-memory/0x00460500-0x00460709.AlphaBlendSpan16.md) AlphaBlendSpan16.
- [UID:000206][0x00460710-0x004607e0.Rgb565HalfBlendSpanRaw](by-memory/0x00460710-0x004607e0.Rgb565HalfBlendSpanRaw.md) adjacent raw RGB565 half-blend span helper.
- [UID:000209][0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks](by-memory/0x004609e0-0x00460af9.Rgb565TransparentAlphaBlendMmxBlocks.md) four-pixel block helper.

## Changes

- Existing documentation scored this raw helper at `40/65` and summarized only the block call and source-zero skip. It is now scored `70/80` with inferred signature, source/destination row semantics, scalar weighted formula, block-helper relationship, and a cautious reconstructed C++ candidate. Evidence: IDA MCP byte/function/xref review on 2026-05-30.
- 2026-06-03: Raised metadata from `70/80` to `78/86`, marked reconstructable, and attached to [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md). Evidence is restarted IDA MCP confirming the raw prologue/retn boundary, following padding and modeled helper boundary, no raw-start xrefs, the `sub_4609E0` block-helper call, the source-zero branch, and the scalar weighted RGB565 math. C++ remains withheld from the reconstruction header because the raw reachability and final source-facing helper name are still not source-quality.
- 2026-06-05: Raised metadata from `78/86` to `82/88`, corrected the block-helper call description to one source-side weight argument (`32 - alpha32`), and added live IDA evidence for the exact `0x15c`-byte raw body, 118 decoded instructions, empty raw-start xrefs, single `sub_4609E0` call at `0x00460818`, cdecl block-helper argument order, source-zero skip, scalar RGB565 weighted masks, destination store, source/destination byte-pitch row stepping, and following `0xcc` padding. Final reconstruction metadata remains blank.
