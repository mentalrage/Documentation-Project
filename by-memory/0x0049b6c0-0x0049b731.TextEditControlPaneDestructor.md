*** UID:000127 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Compiler scalar-deleting wrapper for the exact ordinary TextEditControlPane destructor child. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049b6c0-0x0049b731 TextEditControlPane Destructor

## Status

- Disposition: reconstructable NexusTK project code.
- Covered class: [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md)
- Likely source file: [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md)
- Rebuild handling: declaration-derived compiler scalar-deleting wrapper; exact ordinary source body is [UID:0004K2][0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor](by-memory/0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor.md). This page's formal C++ remains blank.
- IDA MCP evidence pass: 2026-06-05; B009 local-export source-quality reconciliation 2026-06-21.
- Confidence: very strong for the exact function boundary, vtable routing, embedded-editor cleanup, deleting-destructor flag behavior, and the inherited `StaticTextControlPane` vtable-slot explanation; below final only because the source-level field name and class-level destructor declaration placement remain provisional.

## Function

| Range | Function | Notes |
| --- | --- | --- |
| `0x0049b6c0-0x0049b731` | `TextEditControlPane` scalar deleting destructor | Reinstalls the three `TextEditControlPane` vtables, deletes the embedded `TextEditPane` pointer stored at offset `+0x10c`, clears that field, chains the base pane teardown, and optionally frees the wrapper allocation. This is source-declared/generated-binary compiler glue around `TextEditControlPane::~TextEditControlPane()`, not a source-authored `ScalarDeletingDestructor` method. |

The body mirrors exact [UID:0004K2][0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor](by-memory/0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor.md), with the additional deleting-destructor flag test and object-free path. Aggregate UID00011H is now only the enclosing non-emitting address index.

## Adjustor Thunks

| Range | Role | Evidence |
| --- | --- | --- |
| `0x0049b02f-0x0049b03a` | Secondary-vtable deleting-destructor adjustor | IDA function size `0x0b`; adjusts the receiver back by `40` dwords and jumps to this destructor. Vtable data cells: `0x00618168` and `0x0061820c`. |
| `0x0049b03a-0x0049b045` | Tertiary-vtable deleting-destructor adjustor | IDA function size `0x0b`; adjusts the receiver back by `41` dwords and jumps to this destructor. Vtable data cells: `0x00618198` and `0x0061823c`. |

## Evidence Notes

- `lookup_funcs(0x0049b6c0)` reports size `0x71`, giving exclusive range `0x0049b6c0-0x0049b731`.
- `xrefs_to(0x0049b6c0)` reports four references: the two adjustor thunks and primary vtable cells at `0x00618100` and `0x006181a4`.
- The primary vtable cell at `0x00618100` belongs to the `TextEditControlPane` vtable set. B008 raw RTTI reanalysis resolves the `0x006181a4` `StaticTextControlPane` vtable cell as inherited `TextEditControlPane` base-slot reuse, not `StaticTextControlPane` ownership.
- Decompilation and disassembly both show three `TextEditControlPane` vtable stores at object offsets `+0x000`, `+0x0a0`, and `+0x0a4`.
- The body reads the embedded editor/control pointer at offset `+0x10c`, calls that object's virtual deleting destructor with flag `1` when present, then clears the pointer slot.
- `callees(0x0049b6c0)` reports the base pane teardown helper at `0x00544580`, guard-check helper at `0x0041b6a0`, and object-free helper at `0x004f4ac0`.
- B009 local IDA JSON export decompiles the raw function as `sub_49B6C0(_DWORD *Block, char a2)`: `Block[67]` is `this+0x10c`, `Block[40]` is `this+0xa0`, and `Block[41]` is `this+0xa4`.
- [UID:0001EA][0x00544460-0x00545086.PaneCore](by-memory/0x00544460-0x00545086.PaneCore.md) identifies `0x00544580` as the ordinary `Pane::~Pane()` body. It is the compiler-emitted base destructor chain, not a source-authored call in `TextEditControlPane::~TextEditControlPane()` and not direct `GrafPort` cleanup.
- [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) is reached only by the normal scalar-delete free branch.
- `0x0041b6a0` is treated as `@_guard_check_icall_nop@4` compiler/runtime no-op support through [UID:0000VN][-ignored](by-memory/-ignored.md), not product source logic.
- The deleting flag path frees the wrapper only when bit `0x1` is set and bit `0x4` is clear; otherwise it returns the wrapper without freeing it.
- Boundary padding from live bytes: `0x0049b6bc-0x0049b6c0` is four `0xcc` bytes and `0x0049b731-0x0049b740` is fifteen `0xcc` bytes.

## B008 Raw PE / RTTI Reanalysis (2026-06-19)

B008 could not reach the local IDA MCP endpoint, so this page was rechecked against raw `NexusTK.exe` PE bytes with Capstone and RTTI parsing. The body is exactly `0x0049b6c0-0x0049b731`, bracketed by `0xcc` padding at `0x0049b6bc-0x0049b6c0` and `0x0049b731-0x0049b740`. The wrapper saves `this` in `esi`, reads the embedded child pointer at `this+0x10c`, writes the `TextEditControlPane` vtables at `this+0x0`, `this+0xa0`, and `this+0xa4`, virtually deletes the child with flag `1`, clears `this+0x10c`, calls shared pane teardown `0x00544580`, then follows scalar-delete flags through the project operator-delete wrapper `0x004f4ac0` or the ignored CFG/vector-delete support helper `0x0041b6a0`.

Raw pointer scans found the wrapper only in primary vtable cells `0x00618100` and `0x006181a4`; the adjustor thunks were found only in secondary vtable cells `0x00618168/0x0061820c` and `0x00618198/0x0061823c`. RTTI for `0x00618100`, `0x00618168`, and `0x00618198` names `.?AVTextEditControlPane@@` with `EventHandler` and `TimerHandler` secondary subobjects at `+0xa0` and `+0xa4`. RTTI for `0x006181a4`, `0x0061820c`, and `0x0061823c` names `.?AVStaticTextControlPane@@` but lists `.?AVTextEditControlPane@@` as its base, with the same secondary-subobject offsets. The `StaticTextControlPane` cells therefore reuse inherited `TextEditControlPane` destructor slots; they are not a separate StaticText-authored destructor body and do not change the owner/emitter route.

The exact adjustor thunk bodies are compiler glue:

```asm
0x0049b02f  sub ecx, 0xa0
0x0049b035  jmp 0x0049b6c0

0x0049b03a  sub ecx, 0xa4
0x0049b040  jmp 0x0049b6c0
```

`source-3/simroot_v2/class_TextEditControlPane.cpp` is only a lead for this slice. It names a `ScalarDeletingDestructor` at `0x0049b6c0`, but it also contains polluted base/destructor naming in this control family. Do not copy that generated shape into source documentation; the binary and current class docs support ordinary `TextEditControlPane` destruction with implicit base teardown.

## B009 Source-Quality Reconciliation (2026-06-21)

B009 rechecked the older report against current support docs and local IDA JSON exports after the B008 raw PE/RTTI pass. Its valid findings are incorporated here without lowering the newer `88/92` score. The target remains source-owned by [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md) and emitted through that class/file, but this exact by-memory row remains blank formal C++ because it is MSVC scalar deleting destructor ABI glue.

The exact wrapper behavior is now fully resolved:

- The source-facing destructor is the ordinary `TextEditControlPane::~TextEditControlPane()` body emitted by [UID:0004K2][0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor](by-memory/0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor.md).
- The wrapper restores the primary, secondary, and tertiary `TextEditControlPane` vptrs at `this+0x00`, `this+0xa0`, and `this+0xa4` before running the derived cleanup.
- The child field at `this+0x10c` is best documented as owned `TextEditPane *m_textEditPane`: constructor evidence allocates a `372`-byte `TextEditPane`, stores it at `+0x10c`, and this destructor virtually deletes it with flag `1` and clears the slot.
- Rejected field/source alternatives are preserved: generic `m_editor` is too unspecific, `m_textPane` loses the exact class type, and an inline member object is contradicted by allocation, pointer storage, virtual deleting destructor use, and nulling.
- The direct base callee `0x00544580` is ordinary `Pane::~Pane()`. Source should not explicitly call `Pane::~Pane()` or `GrafPort` cleanup; MSVC emits the base destructor chain.
- The `flags & 1` and `flags & 4` handling is scalar deleting destructor ABI behavior. [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) is called only when `(flags & 1) != 0 && (flags & 4) == 0`; the flag-4 path uses `@_guard_check_icall_nop@4`.
- The target has no ordinary code callers. Reachability is through the primary TextEdit vtable cell `0x00618100`, the inherited StaticText primary cell `0x006181a4`, and the two compiler adjustor thunks `0x0049b02f` and `0x0049b03a`.
- The adjustor thunks subtract `0xa0` and `0xa4` respectively and jump here. They are compiler-generated EventHandler/TimerHandler subobject-view thunks, not handwritten destructor overloads.
- `StaticTextControlPane` vtable references are inherited `TextEditControlPane` base destructor slots by RTTI hierarchy evidence, not a competing owner route and not grounds for a StaticText split.

No split or merge is needed for this wrapper. The range remains exactly `0x0049b6c0-0x0049b731`, bracketed by the four-byte prepad and fifteen-byte postpad. Exact ordinary-destructor child UID0004K2 now emits the handwritten source body; this scalar wrapper stays separate and blank.

Score rationale: `88/92` is retained because the combined B008/B009 evidence resolves the exact body and padding, local export shape, ordinary-helper parity, `m_textEditPane` type/offset, `Pane::~Pane()` identity, delete-flag order, operator-delete callee, guard/no-op path, vtable-only reachability, adjustor offsets, and StaticText inherited-slot caveat. Completion remains below final source-body quality because the exact row is compiler-generated wrapper glue with intentionally blank formal C++; confidence remains below `95` because the original member spelling is inferred even though the field type and ownership are high confidence.

## Source Shape Recommendation

Treat this page as a source-declared/generated-binary scalar deleting destructor wrapper for `TextEditControlPane::~TextEditControlPane()`. The handwritten source effect is the ordinary destructor body that deletes the owned embedded `TextEditPane*` at `+0x10c` and clears the field; base destruction is implicit in C++.

Suggested class-level source shape for a future implementation pass:

```cpp
TextEditControlPane::~TextEditControlPane()
{
    delete m_textEditPane;
    m_textEditPane = NULL;
}
```

`m_textEditPane` is a descriptive first-draft name for `this+0x10c`; final spelling should follow the eventual class layout. Do not hand-code the scalar-delete flag argument, vtable stores, adjustor thunks, `0x004f4ac0` delete wrapper call, `0x0041b6a0` guard/vector-delete path, or raw `0x00544580` base teardown call in final C++.

First-draft C++ policy: keep this page's formal `RECONSTRUCTION_CPP` block blank permanently under the accepted compiler/no-code disposition. The class declaration and UID0004K2 ordinary body now supply the source needed to regenerate this ABI wrapper; never add a fake `ScalarDeletingDestructor` function or marker-only surrogate.

## Neighboring Ranges

| Range | Evidence | Decision |
| --- | --- | --- |
| `0x0049b5f0-0x0049b6bc` | Installs `StaticTextControlPane2` vtables and frees that class's storage. | Neighboring destructor, not this page. |
| `0x0049b6c0-0x0049b731` | TextEditControlPane vtable entry, adjustor thunks, and embedded editor pointer cleanup. | Exact range for this page. |
| `0x0049b740-0x0049b795` | Installs `TargetOptionEditControlPane` vtables. | Neighboring destructor, not this page. |

## Cross-References

- [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md)
- [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md)
- [UID:00011H][0x004988d0-0x00498dd0.TextEditControlPaneCore](by-memory/0x004988d0-0x00498dd0.TextEditControlPaneCore.md)
- [UID:0004K2][0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor](by-memory/0x00498b90-0x00498bcf.TextEditControlPaneOrdinaryDestructor.md)
- [UID:0000EO][TextEditPane](by-class/TextEditPane.md)
- [UID:00011Y][0x0049af11-0x0049b8d5.ButtonChoiceControlDestructors](by-memory/0x0049af11-0x0049b8d5.ButtonChoiceControlDestructors.md)

## Changes

- 2026-07-13 B003 UID00011H implementation support sync:
  - Score `88/92`, owner/emitter, range, reconstructable state, nesting, and blank formal C++ remain unchanged.
  - Replaced historical future-child wording with exact registered UID0004K2, which now emits the ordinary destructor. Preserved scalar-delete flags, object-free/guard routes, vptr/EH/base-teardown machinery, adjustors, inherited StaticText slots, exact pads, and no-handwritten-wrapper policy.

- 2026-06-05: Live IDA refresh.
  - What existed before: the page was scored `80/88`, parent metadata was blank, and evidence still used raw helper-label/decompiler wording.
  - Changed to: completion `84`, confidence `90`, parent [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md), cleaned adjustor/vtable/free-path evidence, and explicit padding.
  - Summary/evidence: live IDA reconfirmed the destructor size, two adjustor thunk sizes, vtable refs, embedded editor cleanup at offset `+0x10c`, base teardown, object-free helper, guard-check path, and neighboring destructor exclusions.
- 2026-06-19 B008 Rule 26 incorporation:
  - What existed before: the page documented the IDA scalar-deleting destructor shape but left the `StaticTextControlPane` vtable reference as a shared-reference caveat and did not state the final source-shape policy.
  - Changed to: completion `88`, confidence `92`, raw PE/Capstone boundary and pointer-scan evidence, RTTI proof that `StaticTextControlPane` derives through `TextEditControlPane`, and explicit class-level destructor source guidance.
  - Summary/evidence: B008 raw disassembly confirmed child deletion at `+0x10c`, `TextEditControlPane` vtable stores, base teardown, delete-helper paths, exact adjustor thunks, and vtable-only references. The formal memory-page C++ block stays blank because the recoverable source is ordinary `TextEditControlPane::~TextEditControlPane()`, while scalar deleting wrappers and thunks should regenerate from declarations.
- 2026-06-21 B009 source-quality reconciliation:
  - Score remains `88/92`; the older `86/92` recommendation is superseded by the already-incorporated B008 raw PE/RTTI evidence plus this B009 local-export recheck.
  - Changed to: added the local IDA JSON `sub_49B6C0(_DWORD *Block, char a2)` interpretation, exact `Block[67]` / `Block[40]` / `Block[41]` offset mapping, `TextEditPane *m_textEditPane` field-confidence rationale and rejected alternatives, explicit `Pane::~Pane()` base-callee identity, scalar-delete flag policy, guard/no-op helper policy, adjustor-thunk roles, StaticText inherited-slot conclusion, and retained no-code proof for this exact scalar wrapper.
  - Summary/evidence: B009 matched [UID:000127] against the non-deleting helper `0x00498b90-0x00498bcf`, Pane/OperatorDelete/TextEditPane support docs, and local IDA exports for the wrapper and `+0xa0/+0xa4` adjustors. Formal C++ remains blank by design.
