*** UID:000115 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ProgressBarControlPane::SetMinValue(short minValue)
{
    if (minValue < 0)
        minValue = 0;
    else if (minValue > kMaxProgressValue)
        minValue = kMaxProgressValue;

    if (m_rangeStart == minValue)
        return;

    if (m_rangeCurrent < minValue) {
        m_rangeCurrent = minValue;
        InvalidateRect(&m_visibleBounds);
    }

    m_rangeStart = minValue;
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `ProgressBarControlPane::SetMinValue(short)`: clamps signed input to `0..30000`, returns without side effects when unchanged, raises inherited `m_rangeCurrent +0xfa` when below the new minimum, then writes inherited `m_rangeStart +0xf8`; `InvalidateRect(&m_visibleBounds)` runs zero, one, or two times according to those paths. B002 live MCP confirms the exact raw body/padding, no external entry/xref/pointer route, class/file ownership, and populated formal C++; raw no-route status is a liveness caveat, not a no-code condition. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00494a90-0x00494ae7 - ProgressBarControlPane Range-Start Setter

## Status

- Confidence: very strong for the exact body/hash/range, signed clamp and field types, branch/write/invalidation order, inherited rectangle/virtual route, class/file ownership, and exhaustive no-route checks; strong for inferred original lexical spellings.
- Related class: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- Related file: [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- IDA function status: code-shaped bytes, not a function object.
- Source shape: `ProgressBarControlPane::SetMinValue(short minValue)`.
- C++ status: source-ready first draft populated with accepted human names; raw/no-caller caveat retained as liveness evidence only.

## Covered Range

| Range | Owner | Role |
| --- | --- | --- |
| `0x00494a90-0x00494ae7` | `ProgressBarControlPane` | Source-authored `SetMinValue(short minValue)` body that clamps the incoming short to `0..30000`, writes inherited `ControlPane::m_rangeStart` at object offset `+0xf8`, raises inherited `ControlPane::m_rangeCurrent` at object offset `+0xfa` if needed, and invalidates `GrafPort::m_visibleBounds`. |

## Evidence Notes

- IDA MCP `lookup_funcs 0x00494a90` reports `Not a function`.
- Raw IDA disassembly shows a complete private helper body: constructor-style prologue, receiver register in `ecx`, stack argument at `[ebp+8]`, and `retn 4` ending before alignment at `0x00494ae7`.
- 2026-06-01 IDA MCP byte check confirms the body is 87 bytes, starts with `push ebp`, ends with `retn 4`, and is followed by nine `0xcc` alignment bytes before `0x00494af0`.
- Field usage matches [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md): object offset `0xf8` for start/lower bound, object offset `0xfa` for current value, and vtable slot `+0x20` invalidation with the pane rectangle at object offset `0x44`.
- 2026-06-05 live IDA reconfirms `0x00494a90`, `0x00494ae7`, `0x00494af0`, and `0x00494b4b` are not function objects; the next modeled function starts at `0x00494b50`.
- 2026-06-05 `xrefs_to` and `callers` report no references or ordinary direct callers for `0x00494a90`, matching the projected-private-helper classification.
- 2026-06-05 byte reads confirm the exact `0x57`-byte helper body, clamp ceiling `0x7530`, field writes at object offsets `0xf8` and `0xfa`, vtable slot `+0x20` invalidation, `retn 4` exit, and nine alignment bytes at `0x00494ae7-0x00494af0`.
- 2026-06-10 B001-017 IDA MCP audit reconfirms `lookup_funcs 0x00494a90` returns `Not a function`, `xrefs_to` and `callers` for `0x00494a90` are empty, `0x00494a90-0x00494ae7` is an 87-byte code-shaped body ending in `retn 4`, and `0x00494ae7-0x00494af0` is nine `0xcc` bytes. The same audit reconfirms the constructor at `0x00494c80-0x00494daa`, draw virtual vtable data xref at `0x00617b78`, and the `ProgressBarControlPane` vtable stores at `0x00494cc5`, `0x00494ccb`, and `0x00494cd5`.

## Cross-Document Evidence

| Source | Confirmation |
| --- | --- |
| [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) | Lists this exact helper as `SetMinValue(short minValue)` and now emits the class declaration with child insertion; earlier provisional/no-C++ wording is superseded by the B005 source-quality closure. |
| [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) | Places this helper in `NexusTK/ui/controls/ProgressBarControlPane.cpp` with the constructor, draw virtual, vtables, and sibling projected helpers. |
| [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md) | Nests this body with the range-end setter and records the exact body/alignment map for `0x00494a90-0x00494b4b`. |
| [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md) | Confirms the sibling setter uses the same clamp/current/invalidation pattern for the upper bound field. |
| [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md) | Confirms the constructor initializes the same `0xf8`/`0xfa`/`0xfc` field triplet and installs the progress-bar vtables. |
| [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md) | Confirms the class vtable-data ownership that ties the helper's field family to `ProgressBarControlPane`, not the neighboring `ControlPane` or button-control families. |
| [UID:0001VH][PaneLayout](by-type/by-struct/PaneLayout.md) | Resolves complete-object `+0x44` as inherited `GrafPort::m_visibleBounds`; historical `m_bounds`/`m_localBounds` aliases are superseded. |
| Generated output | Historical command `000000016699` included one UID000115 body inside the class and exposed the old child-insertion defect. Callback command `000000016709` closed the class before qualified definitions, restored UID0003N1, and emitted one UID000115 body; the old below-95/no-code policy statement is superseded. |

## Touched State

| Offset / call | Evidence-backed role | Remaining uncertainty |
| --- | --- | --- |
| signed `short minValue` | Incoming minimum candidate, clamped to `0..30000`. | Original spelling is inferred; signed width and role are exact. |
| object offset `0xf8` | Inherited signed `short ControlPane::m_rangeStart`; semantically the ProgressBar minimum. | Physical member identity, offset, type, and role are resolved; no derived shadow member exists. |
| object offset `0xfa` | Inherited signed `short ControlPane::m_rangeCurrent`; raised when below the new minimum. | Physical member identity, offset, type, and role are resolved; no derived shadow member exists. |
| object offset `0x44` | Inherited `GrafPort::m_visibleBounds` passed by address. | Resolved by current PaneLayout/GrafPort authority. |
| vtable slot `+0x20` | `Pane::InvalidateRect(const RectBounds *)` at `0x00544800`. | Resolved by the primary ProgressBar/Pane vtable cells and current Pane docs. |

## Historical B005 Source-Quality Closure

The best source-facing name is `ProgressBarControlPane::SetMinValue(short minValue)`. The body is not compiler generated: it is a `thiscall` mutator over the progress-bar field triplet, performs handwritten range clamping, and dispatches the inherited invalidation virtual after observed state mutations. The current executable still has no direct branch/call refs, no long conditional branch refs, and no VA/RVA pointer/table refs to `0x00494a90`; that remains a liveness caveat and confidence cap, not a no-code proof.

The B005 pass accepted `m_minValue` for `+0xf8` and `m_currentValue` for `+0xfa`, but its inherited `m_bounds` spelling and `Invalidate(&m_bounds)` draft are now historical. Current PaneLayout/GrafPort authority resolves `+0x44` as inherited `m_visibleBounds`, and current Pane/vtable authority resolves inherited primary slot `+0x20`, `0x00617b54 -> 0x00544800`, as `Pane::InvalidateRect`. The exact helper body remains bounded by fifteen `0xcc` bytes at `0x00494a81-0x00494a90` before it and nine `0xcc` bytes at `0x00494ae7-0x00494af0` after it.

## B002 Exact Source-Quality Reanalysis

- Healthy IDA MCP session `9b0396a3` independently rederived the target instead of relying on the historical B001/B005 conclusions.
- Exact body: `[0x00494a90,0x00494ae7)`, `0x57` / 87 bytes, PE RVA `0x00094a90`, raw file offset `0x00093e90`, SHA256 `492B0F418A63322765FED4BC638EC5F0C508C298A006820C1A41D40DB7358FFA`.
- Boundary proof: fifteen `0xcc` bytes precede the start at `0x00494a81-0x00494a90`; nine `0xcc` bytes follow at `0x00494ae7-0x00494af0`, before sibling UID000117.
- IDA intentionally has no function object at the raw start: `lookup_funcs`, `analyze_function`, and `decompile` fail there, while raw disassembly decodes a complete non-virtual `void __thiscall` method with `ECX=this`, one signed-short stack argument, and `retn 4`.
- Exact machine flow:
  - `test si,si` plus the negative branch clamps values below zero to `0`;
  - signed comparison against `0x7530` clamps values above decimal `30000`;
  - equality against inherited signed-short `m_rangeStart` at `+0xf8` returns with no writes and no invalidation;
  - when inherited signed-short `m_rangeCurrent` at `+0xfa` is below the new minimum, the method writes current first and immediately calls `InvalidateRect(&m_visibleBounds)`;
  - it then writes `m_rangeStart` and calls `InvalidateRect(&m_visibleBounds)` again;
  - invalidation count is therefore exactly zero for unchanged minimum, one for a changed minimum with current already in range, and two when current is raised;
  - it never writes inherited `m_rangeEnd` at `+0xfc` and does not enforce `min <= max`.
- Reachability/liveness proof: all 34 decoded instruction/padding-start addresses were queried, yielding only 35 internal control-flow xrefs and zero external xrefs. Searches found no little-endian VA pointer `90 4A 49 00`, RVA pointer `90 4A 09 00`, or raw-offset pointer `90 3E 09 00`.
- Classification: retained source-authored but unreferenced method in this executable. The complete handwritten clamp/state/update body, symmetric UID000117, constructor-inline behavior, ProgressBar RTTI/vtables, and exact field family reject compiler-only, inline-residue, aggregate-owned, and non-reconstructable classifications.
- Source names: `SetMinValue`, `minValue`, inherited `m_rangeStart/m_rangeCurrent`, `kMaxProgressValue`, `InvalidateRect`, and inherited `m_visibleBounds` are the strongest human-source reconstruction. Derived `m_minValue/m_currentValue`, `SetRangeStart`, `SetLowerBound`, raw offsets, `_WORD`, `sub_494A90`, `Invalidate`, and `m_bounds` are rejected or superseded alternatives.
- Physical-layout correction: [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane` already proves complete size `0x108` and owns protected range storage at `+0xf8/+0xfa/+0xfc`; PatchPane allocates exactly `0x108` for ProgressBarControlPane. ProgressBar therefore assigns semantic min/current/max roles to inherited storage and cannot redeclare physical derived fields at those offsets.
- Ownership and placement remain UID0000AW `ProgressBarControlPane` and UID0000MT `NexusTK/ui/controls/ProgressBarControlPane.cpp`; UID000116 remains only a non-emitting duplicate inventory.

## Reconstruction Notes

- Reconstructable: true as a private progress-bar range helper.
- Parent: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md).
- C++: first-draft `ProgressBarControlPane::SetMinValue(short minValue)` is now populated. The source names are inferred/descriptive rather than PDB-proven, but the implementation stays within the exact helper range and expresses normal mid-2000s C++ class logic.
- Completion is `93` because exact bytes/hash/raw mapping, complete branch/write/call order, zero/one/two invalidation matrix, signed types, resolved inherited API/member names, exhaustive liveness checks, class/file/aggregate routing, generated repair obligations, and formal C++ are documented and implementation-ready.
- Confidence is `94` because live MCP raw evidence, PE hashing, every decoded-address xref, three pointer encodings, sibling/constructor/current-setter corroboration, RTTI/vtables, PaneLayout, and generated/manual readbacks agree. It remains below `95` because no original method/member/constant symbols survive and the body has no surviving static entry route.

## Cross-References

- [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md)
- [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md)
- [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)

## Changes

- Before: completion/confidence were `50/65`, reconstruction metadata was blank, and the aggregate backlink used the stale `0x00494b48` path.
- Changed to: completion/confidence are `64/76`, the helper is marked reconstructable, attached to the `ProgressBarControlPane` class, and linked to the corrected aggregate path.
- Summary/evidence: IDA MCP byte review confirmed the exact helper body and following alignment; confidence remains below high-final levels because IDA still does not model this start as a function and the final source-level name is provisional.
- 2026-06-02:
  - Before: page had exact byte/boundary notes but no touched-state table or reconstruction rationale.
  - After: scored as `70/82` with field/call state mapped to the constructor and draw evidence.
  - Summary/evidence: recorded IDA byte review, constructor field layout, and the shared invalidation pattern support the stronger score; at that time no C++ emitted because final helper/field names were not source-quality. This historical state is superseded by the 2026-06-19 implementation.
- 2026-06-03 cross-documentation consistency pass:
  - Before: the page was assigned and behaviorally documented, but the score did not record the parent/file threshold check, aggregate nesting, sibling comparison, vtable-data support, or autogen no-code state.
  - After: raised `70/82` to `76/84` and added a cross-document evidence table; C++ remained blank at that time, superseded by the 2026-06-19 implementation.
  - Summary/evidence: existing written IDA evidence proves the raw body and alignment; the class/file/aggregate/sibling/constructor/vtable pages confirm ownership and field use.
- 2026-06-05 live IDA refresh:
  - Changed from `76/84` to `82/88`.
  - Removed stale receiver-offset wording and the old unavailable-IDA caveat.
  - Evidence: live `lookup_funcs`, `xrefs_to`, `callers`, and byte reads confirm the helper remains unmodeled and unreferenced, clamps the lower-bound input, conditionally raises current value, invalidates through the vtable slot, ends at `0x00494ae7`, and is followed by nine alignment bytes.
- 2026-06-10 B001-017 duplicate-aggregate audit:
  - Changed from `82/88` to `85/88`.
  - Evidence: live IDA MCP reconfirmed the exact helper bytes, empty xrefs/callers, `0xcc` alignment after the body, and constructor/draw/vtable ownership anchors. The parent class [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) and file [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) already clear `86/85`, so this exact child remains the source-bearing ProgressBarControlPane range while aggregate [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md) becomes non-emitting.
- 2026-06-19 B005 accepted source-quality implementation:
  - Changed from `85/88` to `87/89`.
  - Added first-draft `ProgressBarControlPane::SetMinValue(short minValue)` C++ and replaced the stale no-code/field-name blocker with accepted source-facing names, exact padding, clamp/current/invalidation behavior, inherited invalidation slot, and raw no-branch/call/VA/RVA-ref caveat.
- 2026-07-23 B002 UID000115 exact source-quality callback:
  - Changed from `87/89` to `93/94`; owner UID0000AW, reconstructable true, and emitter UID0000AW remain unchanged.
  - Replaced the stale blank-C++ Item Summary and obsolete below-95/no-emission wording with current emitted-source truth.
  - Added exact body SHA256/RVA/raw-offset, signed machine-flow and zero/one/two invalidation matrix, all-decoded-address xref search, VA/RVA/raw-offset pointer negatives, retained-unreferenced-source classification, constructor/sibling/current-setter corroboration, rejected alternatives, and exact score rationale.
  - Replaced historical `Invalidate(&m_bounds)` with `InvalidateRect(&m_visibleBounds)` using current Pane/PaneLayout/GrafPort authority while preserving the older assumption as superseded history.
- 2026-07-23 B003 inherited-storage compatibility correction:
  - Preserved B002's `93/94` score, exact machine/liveness evidence, class/file route, and human API names.
  - Replaced derived `m_minValue/m_currentValue` physical aliases with inherited `ControlPane::m_rangeStart/m_rangeCurrent`, recorded the exact `0x108` base-complete layout proof, and retained the rejected derived aliases as historical source-shape assumptions.
