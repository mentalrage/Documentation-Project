*** UID:0004FL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Non-emitting old-MSVC `std::vector<SoundPathString>::reserve` lowering called from local-music scanning; exact allocator, copy construction, partial cleanup, free-wrapper, capacity guard, and storage-commit evidence is preserved while typed vector source regenerates the body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057bd90-0x0057be52 SoundPathVector Reallocate

## Status

- Disposition: compiler/STL `std::vector<SoundPathString>::reserve` reallocation lowering, retained as an exact non-emitting child.
- Parent range: [UID:0001ID][0x0057b860-0x0057bf6e.SoundPathVector](by-memory/0x0057b860-0x0057bf6e.SoundPathVector.md).
- Source route: the scanner's typed `std::vector<SoundPathString>::reserve` expression in [UID:0000NV][SoundManager](by-file/SoundManager.md); [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md) is documentation-only.
- Name status: `Reallocate`, `AllocateSoundPathStringStorage`, and `FreeSoundPathStringStorage` remain historical descriptive names for binary behavior, not recovered handwritten APIs.

## Evidence

Current MCP session `aa3930bd` supersedes the old blank-emitter blocker for this exact child:

- `server_health` returned `status:"ok"` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` id `21` reports `0x0057bd90` as `sub_57BD90`, size `0xc2` / 194 bytes; `0x0057be52` is not a function; successor `0x0057be60` is [UID:0004FP][0x0057be60-0x0057becf.SoundPathStringCopyConstructRange](by-memory/0x0057be60-0x0057becf.SoundPathStringCopyConstructRange.md), size `0x70`.
- Bounded `entity_query` id `25` over `0x0057bd50-0x0057be70` reports only `0x57bd60`, this target at `0x57bd90`, and successor `0x57be60`, preserving the exact split and excluding padding/merge interpretations.
- `xrefs_to` id `22` reports exactly one code xref to this target: `0x0057aea5` in [UID:0001IA][0x0057a890-0x0057afce.SoundManagerMusicDirectoryScan](by-memory/0x0057a890-0x0057afce.SoundManagerMusicDirectoryScan.md). Caller disassembly id `40` and decompile id `41` show that the directory scanner computes the temporary result count, checks it against the scanned-path vector capacity, guards `> 0x3fffffff`, passes that count as requested capacity, and calls with the `SoundPathVector` object in `ecx`.
- `callees` id `23` reports target callees `0x00421520`, `0x005829f0`, `0x0057bce0`, `0x00455a30`, `0x00423e00`, and `__CxxThrowException@8`.
- Disassembly id `24` proves the normal body: `0x0057bdc0-0x0057bdcb` computes current live count as `(m_end - m_begin) >> 2`; `0x0057bdce` calls the allocator with requested capacity; `0x0057bdf6-0x0057be0e` copy-constructs existing four-byte entries into new storage through `0x005829f0`; `0x0057be10-0x0057be1b` pushes requested capacity, current size, and new begin pointer, then calls [UID:0004FN][0x0057bce0-0x0057bd53.SoundPathVectorReplaceStorage](by-memory/0x0057bce0-0x0057bd53.SoundPathVectorReplaceStorage.md) / `SoundPathVector::ChangeArray()`.
- `analyze_function` id `30` identifies allocator `0x00421520` as a capacity-count storage helper: it multiplies by four, rejects capacities above `0x3fffffff`, returns `0` for zero capacity, uses `operator new` for small allocations, and for large allocations returns an aligned pointer while storing the actual base at `result[-1]`.
- `analyze_function` id `33` identifies `0x005829f0` as the ANSI `StringBase` share/private-copy constructor helper used for source-facing `SoundPathString` entries. It uses empty sentinel `off_670290`, refcount increment for shareable non-empty storage, `sub_584B10`, and `memmove`.
- Failure cleanup is explicit: `xrefs_to` id `22` reports raw `0x0057b9b0` cleanup xrefs from this target's handler at `0x006085f3` and from source-ready UID0004FP at `0x00608623`. Disassembly id `24` shows the target tracks new begin/current constructed pointers, jumps from `0x006085f0` to raw `0x0057b9b0` to destroy `[newBegin, constructedEnd)`, then the cleanup block at `0x0057be34-0x0057be4d` frees replacement storage and rethrows.
- `analyze_function` id `32` and disassembly id `51` identify free wrapper `0x00423e00` as the capacity-aware matching deallocator: it uses capacity count, validates saved base for large allocations, and calls `0x005c7526`.
- Disassembly id `50` shows `0x00455a30` is only `mov eax, ecx; retn`; in this cleanup context it is compiler/decompiler glue before the free wrapper, not a source-level SoundPathVector method.

Read-only generated state before this callback showed `auto-generated/NexusTK/audio/SoundManager.cpp` already contained sibling UID0004FN `SoundPathVector::ChangeArray()` and UID0004FP `CopyConstructSoundPathStringRange(...)`, while UID0004FL was absent because `EMITTER_UIDS` was blank. Generated memory coverage listed UID0004FL as `non-emits`, and the research tracker row was still `85/89`, reconstructable true, reports `0`.

## Behavior

`SoundPathVector::Reallocate(int newCapacity)` reserves an exact requested capacity for the current vector. It saves the current live count, allocates replacement `SoundPathString` storage, copy-constructs each existing entry from `[m_begin,m_end)` into that replacement storage, then delegates old-entry destruction, old-storage free, and triplet replacement to source-ready `SoundPathVector::ChangeArray(newBegin, currentSize, newCapacity)`.

If a `SoundPathString` copy construction throws, the C++ EH path destroys only the successfully constructed replacement entries, frees the allocated replacement storage with the same large-allocation convention used by the allocator/free wrappers, and rethrows. The source body intentionally models this as ordinary `try`/`catch` cleanup rather than emitting security-cookie, handler table, or raw landing-pad labels.

The source-facing allocator/free helper names are inferred but behavior-backed:

- `AllocateSoundPathStringStorage(int capacity)` for `0x00421520`.
- `FreeSoundPathStringStorage(SoundPathString *storage, int capacity)` for `0x00423e00`.

These names avoid raw IDA labels and preserve the observed capacity-count semantics. They should be declared near the private `SoundPathVector` helper code or otherwise made available through the SoundPathVector support route.

## Source Placement And Negative Evidence

Direct documentation ownership remains [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md), with no emitter. [UID:0000NV][SoundManager](by-file/SoundManager.md) contains the human-source `reserve` call; the compiler instantiation reads, copies, and commits vector storage without constituting a custom SoundManager or SoundPathVector method.

Rejected alternatives:

- Direct `SoundManager` ownership: rejected because the target touches no SoundManager fields and receives only the vector object; SoundManager is caller/source-root context.
- Parent [UID:0001ID][0x0057b860-0x0057bf6e.SoundPathVector](by-memory/0x0057b860-0x0057bf6e.SoundPathVector.md) aggregate output: rejected because the parent is a reviewed non-emitting split/index and exact children own source decisions.
- Broad [UID:0001I8][0x005797b0-0x0057bf6e.SoundManagerAudioHelperCluster](by-memory/0x005797b0-0x0057bf6e.SoundManagerAudioHelperCluster.md) output: rejected because that range is mixed ownership/index context only.
- Handwritten custom-container ownership: rejected because the three-pointer object, standard length-error path, standard reserve caller, allocator/free convention, uninitialized-copy/EH helpers, and storage commit are one old-MSVC vector instantiation.
- Raw labels in source: rejected. Formal C++ should not contain `sub_421520`, `sub_423E00`, `sub_57B9B0`, `sub_5829F0`, or decompiler local names.
- Plain `operator new[]` spelling: rejected because allocator/free evidence includes zero-capacity return, capacity guard, large-allocation alignment, and saved-base validation.
- Emitting the historical custom method body: rejected because current evidence resolves the source expression as typed standard-vector `reserve`; the compiler body remains documented but blank in formal output.

## Score Rationale

Completion is `92` because the page records exact range/size, one caller, callee set, allocator semantics, copy construction, successful storage commit, failure cleanup/free/rethrow, standard-container source cause, non-emitting disposition, and rejected alternatives. Confidence is `95` because the target blockers are resolved by IDB evidence and cross-family standard-vector morphology; exact compiler-internal helper spelling remains unknowable but no longer limits human-source reconstruction.

## Changes

- 2026-07-04 B010 UID0004FL implementation callback:
  - Raised `85/89 -> 88/91`, set `EMITTER_UIDS:0000DI`, kept `CANONICAL_OWNER:0000DI`, kept `RECONSTRUCTABLE:TRUE`, left optional emitter position blank, and inserted formal first-draft `SoundPathVector::Reallocate(int newCapacity)` C++.
  - Evidence: accepted B010 report on current MCP session `aa3930bd` resolved exact range `0x0057bd90-0x0057be52`, single caller `0x0057aea5`, allocator `0x00421520`, `SoundPathString` copy constructor helper `0x005829f0`, commit call `0x0057be1b` to source-ready `SoundPathVector::ChangeArray()`, cleanup xref `0x006085f3 -> 0x0057b9b0`, free wrapper `0x00423e00`, and generated pre-callback non-emitting state. The update preserves rejected direct SoundManager ownership, parent/broad aggregate output, generic container/STL ownership, raw labels in source, and plain `operator new[]` allocation spelling.
- 2026-07-13 B004 UID0001IA callback:
  - Reclassified the accepted exact body as compiler-generated old-MSVC `std::vector<SoundPathString>::reserve` lowering; set `92/95`, `RECONSTRUCTABLE:FALSE`, blank emitter and managed C++, while retaining all prior allocator, copy, cleanup, commit, caller, range, generated-state, rejected-alternative, and inferred-name evidence as historical binary documentation.
