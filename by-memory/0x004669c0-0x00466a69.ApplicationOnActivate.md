*** UID:0002GJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Application::OnActivate()
{
    if (m_exitRequested)
        return;

    if (g_pEventMan != NULL)
        g_pEventMan->ClearKeyboardState();

    if (ApplicationActiveGate == 0 && g_pScreenPane != NULL) {
        g_pApplication->m_isRunning = true;
        SurfaceFlipSurfaces(g_pScreenPane);
        ::ShowWindow(g_pApplication->GetMainWindowHandle(), SW_RESTORE);
    }

    if (g_pKeySpeedMgr != NULL)
        ApplyFastKeyboardRepeatSettings();

    if (!g_pSoundManager->IsSoundEffectsEnabled())
        g_pSoundManager->RestoreSoundEffectsAfterAppFocus();

    if (g_pConfig->m_musicPlaybackMode == 0 &&
        !g_pSoundManager->IsPlaybackEnabled() &&
        g_pSoundManager->m_musicOptionEnabled) {
        g_pSoundManager->m_musicOptionEnabled = false;
        g_pMidiPlayer->EnableMidiPlayback();
        g_pSoundManager->m_musicPlaybackEnabled = true;
        g_pSoundManager->m_musicOptionEnabled = true;

        if (g_pSoundManager->m_currentTrackIndex != 0 &&
            AIL_stream_status(g_pSoundManager->m_stream) != 4) {
            const int trackIndex = g_pSoundManager->m_currentTrackIndex;
            const int volumeScale = g_pSoundManager->m_currentTrackVolumeScale;
            const int loopCount = g_pSoundManager->m_currentLoopCount;
            g_pSoundManager->m_currentTrackIndex = 0;
            g_pSoundManager->PlayTrackByIndex(trackIndex, volumeScale, loopCount);
        }

        g_pSoundManager->SetStreamVolume(g_pSoundManager->m_streamVolume);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready Application::OnActivate implementation inherited unchanged by BaramApp; exact formal C++ preserves virtual-only dispatch, the shared exit-request guard at +0x83a, foreground/window policy, keyboard reset/repeat, SFX restore, and the caller-owned non-contiguous music tail inline with exact option/runtime fields, pending-track replay, Miles status gate, and stream-volume restore. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004669c0-0x00466a69 Application OnActivate

## UID0003D9 Complete-Class Field Reconciliation - 2026-08-16

The guard read at `Application+0x83a` is the same byte written by `RequestExit`, its private signal helper, and read by `IsExitRequested`; current complete-class source therefore uses `m_exitRequested`. Historical `m_transitionGuard` was a behaviorally descriptive alias for the same byte, not a second field, and is superseded to prevent an impossible duplicate member in `Application.h`.

## UID0003OD Source Identity And Inheritance Correction - 2026-08-12

The Application primary vftable at `0x006125e4` and the BaramApp primary vftable at `0x0061d118` both store the identical `0x004669c0` pointer in slot 5. BaramApp therefore inherits this method unchanged; it does not own a distinct focus-activation override. The source-facing identity for this range is `Application::OnActivate`, owned and emitted by [UID:00000D][Application](by-class/Application.md).

Historical `BaramAppOnActivate` and `BaramApp::OnActivate` labels below record earlier reconstruction stages before the comparative vtable proof. The complete formal CPP keeps every accepted statement unchanged and changes only the class qualifier. The formal H channel remains blank because this method page does not own a partial Application class declaration.

## UID0001I9 Caller-Owned Focus Tail Integration - 2026-07-15

- Historical pre-UID0003OD state used score `90/93`, owner/emitter [UID:00000V][BaramApp](by-class/BaramApp.md), position `30`, reconstructable state, and virtual dispatch. The score, position, reconstructability, and dispatch remain unchanged; current ownership/emission is [UID:00000D][Application](by-class/Application.md).
- The formal body now owns the exact `0x0057a480-0x0057a4d8` activation tail inline. It gates on config music mode zero, runtime playback disabled, and `SoundManager::m_musicOptionEnabled`; toggles option/runtime state around MIDI enable; conditionally replays the pending current track when Miles status is not 4; then reapplies `m_streamVolume`.
- [UID:0000DG][SoundManager](by-class/SoundManager.md) documents the required private-field access relationship for this Application-owned focus path. `m_currentTrackVolumeScale` is the accepted `+0x28` field; `m_musicOptionEnabled` is the accepted `+4` desired-state byte. The older `friend class BaramApp` rationale is retained as historical source-shape evidence and should be reconciled by a later complete class-header pass rather than changing this proven vtable owner.
- The previous `RestoreMusicAfterAppFocus()` abstraction is superseded because no separate SoundManager binary method exists for the caller-attached chunk. It remains historical inference only and is not emitted or declared.
- Exact boundary, vtable-only reachability, `0x00466a62 -> 0x0057a480` chunk ownership, global dependencies, keyboard/window policy, and negative direct-caller evidence remain preserved. The former inferred `m_transitionGuard` spelling is preserved only as rejected historical nomenclature; current source consistently uses the cross-method identity `m_exitRequested`.

## Scope

- Range: `0x004669c0-0x00466a69`
- Entity kind: class method
- Owner: [UID:00000D][Application](by-class/Application.md)
- Parent range: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)
- Disposition: reconstructable project code.
- Rebuild handling: `source-authored`
- Autogen status: parent-attached to [UID:00000D][Application](by-class/Application.md) at optional position `30`. The empty-emitter blocker is closed by the first-draft formal C++ above; the remaining field/helper names are documented as source-facing inferred abstractions rather than raw IDA labels.

## Signature And State

- Source shape: `void __thiscall Application::OnActivate()`.
- `this`: `Application *` in `ecx`; IDA still models it as `_BYTE *` because the final `Application` layout is unresolved.
- Guard byte: `this + 0x83a`; nonzero suppresses all transition work. Current formal C++ uses `m_exitRequested`, matching `RequestExit`, `SignalExitRequested`, and `IsExitRequested`; exact original lexical spelling remains inferred, while `m_transitionGuard` is rejected historical wording rather than an active alias.
- Visible state writes: `g_pApplication + 0x838 = 1` at `0x004669f5`, then map-window show/restore through `g_pApplication + 0x828`. B005 MCP session `261fb29b` ties this byte to [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md) / inferred `m_isRunning`; this page is field-writer evidence, not getter ownership.
- Dispatch shape: no ordinary direct callers; reached through `Application`/`BaramApp` virtual-table slots at `0x006125f8` and `0x0061d12c`.

## Behavior

`Application::OnActivate` is the foreground transition handler inherited unchanged by BaramApp. It exits immediately when the transition guard byte at `this + 0x83a` is set. Otherwise it clears EventMan keyboard state through `g_pEventMan` / `dword_67A754`, restores the map pane/window path when `ApplicationActiveGate` / `dword_67A93C` is clear and [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md) / `dword_67A7CC` is present, applies fast keyboard repeat when [UID:0000RB][g_pKeySpeedMgr](by-global/g_pKeySpeedMgr.md) / `dword_67AB48` is present, and resumes sound/music state through [UID:00028R][0x0067a7d0-0x0067a7d4.g_pSoundManager](by-memory/0x0067a7d0-0x0067a7d4.g_pSoundManager.md), [UID:00028Q][0x0067a7c8-0x0067a7cc.g_pConfig](by-memory/0x0067a7c8-0x0067a7cc.g_pConfig.md), and [UID:00029A][0x0067ab4c-0x0067ab50.g_pMidiPlayer](by-memory/0x0067ab4c-0x0067ab50.g_pMidiPlayer.md).

The visible app-state write is `*(g_pApplication + 0x838) = 1` at `0x004669f5`, followed by [0x00557470-0x0055769d.SurfaceFlipSurfaces](by-memory/0x00557470-0x0055769d.SurfaceFlipSurfaces.md)(`g_pScreenPane`) and `ShowWindow(*(g_pApplication + 0x828), 9)` / `SW_RESTORE`. Current Application support docs treat `Application+0x838` as source-facing `m_isRunning`; [UID:00000D][Application](by-class/Application.md) owns the storage, getter, and `Application::OnActivate` implementation. `BaramApp` inherits this virtual implementation unchanged and therefore reaches the same lifecycle-state writes through its primary vtable. The audio resume tail is not contiguous with this app-lifecycle range: the local function jumps from `0x00466a62` to `0x0057a480`, which IDA includes in the decompilation but which sits in the SoundManager playback-control neighborhood.

The current formal C++ calls the modeled `SoundManager::RestoreSoundEffectsAfterAppFocus()` method for `0x0057a790` and represents the raw app-activation music chunk `0x0057a480-0x0057a4d8` inline in this caller. The earlier `RestoreMusicAfterAppFocus()` abstraction is historical and rejected: no standalone callee exists, and current [UID:0001I9][0x0057a260-0x0057a88e.SoundManagerPlaybackControls](by-memory/0x0057a260-0x0057a88e.SoundManagerPlaybackControls.md) evidence proves the tail is caller-owned.

## IDA Evidence

- IDA MCP `lookup_funcs` on 2026-05-30 reports `sub_4669C0` at `0x004669c0` with size `0xa9`, giving the exclusive range end `0x00466a69`.
- IDA MCP `disasm` shows the method reads `this + 0x83a`, calls `sub_4A90D0(dword_67A754, 1)`, checks `dword_67A93C` and `dword_67A7CC`, writes `g_pApplication + 0x838`, calls `sub_557470`, and calls `ShowWindow(g_pApplication->0x828, 9)`.
- The same disassembly resumes background/audio paths through `dword_67AB48`, `dword_67A7D0`, `dword_67A7C8`, `dword_67AB4C`, and Miles `AIL_stream_status`.
- 2026-06-05 A003 IDA MCP refresh reconfirmed `sub_4669C0` at `0x004669c0` size `0xa9`; `lookup_funcs` reports `0x00466a69` is not a function start. `callers` reports no direct code callers, while `xrefs_to` reports data references from `0x006125f8` and the `BaramApp` vtable slot at `0x0061d12c`, consistent with virtual dispatch.
- The same refresh reports callees `sub_4A90D0`, `sub_557470`, `ShowWindow`, `sub_4EFFA0`, `sub_57A750`, `sub_57A790`, and `sub_57A3D0` from the contiguous app-lifecycle body. `xrefs_to 0x0057a480` reports only the tail jump from `0x00466a62`; querying that tail chunk shows audio callees `sub_5260B0`, `sub_579E30`, `sub_57A340`, and `AIL_stream_status`.
- Historical 2026-06-06 A004 IDA MCP snapshot:
  - `lookup_funcs` reports `sub_4669C0` at `0x004669c0` with size `0xa9`; `0x00466a69` is not a function start. IDA also associates the non-contiguous chunk at `0x0057a480` with the same function.
  - Disassembly confirms the `this + 0x83a` guard, `sub_4A90D0(dword_67A754, 1)`, map-pane checks, `g_pApplication + 0x838 = 1`, `sub_557470(dword_67A7CC)`, `ShowWindow(*(g_pApplication + 0x828), 9)`, background helper resume, sound-state checks, and tail jump from `0x00466a62` to `0x0057a480`.
  - `callers 0x004669c0` reports no ordinary direct callers; `xrefs_to 0x004669c0` reports data refs from `0x006125f8` and `0x0061d12c`.
  - `callees 0x004669c0` reports `sub_4A90D0`, `sub_557470`, `ShowWindow`, `sub_4EFFA0`, `sub_57A750`, `sub_57A790`, and `sub_57A3D0`; the tail chunk at `0x0057a480` calls `sub_5260B0`, `sub_579E30`, `sub_57A340`, and `_AIL_stream_status@4`.
  - Byte audit over `0x00466980-0x00466b30` shows the predecessor padding, the exact `0x004669c0-0x00466a69` function body, seven `0xcc` bytes before `0x00466a70`, the next deactivation body through `0x00466b23`, and alignment after it.
- Historical 2026-06-16 C001 IDA MCP snapshot:
  - Saved function label `BaramAppOnActivate` at `0x004669c0`, size `0xa9`; post-apply `lookup_funcs` resolves both the address and name.
  - `xrefs_to 0x004669c0` again reports only the two vtable data refs at `0x006125f8` and `0x0061d12c`; there are no ordinary direct code callers.
  - `disasm BaramAppOnActivate` confirms the transition guard, event/timer resume, map-pane `g_pApplication + 0x838 = 1` write, `ShowWindow(..., 9)` call, `ApplyFastKeyboardRepeatSettings`, audio state checks, and the tail jump at `0x00466a62 -> 0x0057a480`.
  - `callees BaramAppOnActivate` confirms the contiguous callee set: `sub_4A90D0`, `sub_557470`, `ShowWindow`, `ApplyFastKeyboardRepeatSettings`, `sub_57A750`, `sub_57A790`, and `sub_57A3D0`.
  - In that dated snapshot, `analyze_function` reported Hex-Rays failure at `0x00466a02`, consistent with the documented non-contiguous audio-tail caveat; later evidence below superseded that failure without erasing it.
- 2026-06-23 B005 IsRunning field-writer sync:
  - IDA MCP session `261fb29b` rechecked [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md) and confirmed this activation method's write at `0x004669f5 mov byte ptr [eax+838h], 1` as the foreground writer for inferred `Application::m_isRunning`.
  - This is supporting lifecycle evidence for the Application getter and `Application::OnActivate`, not BaramApp ownership of the field, getter, or method. ScreenPane consumes the getter later; BaramApp reaches the Application-owned writer through inherited virtual dispatch.
- Historical 2026-06-29 B001 implementation snapshot:
  - MCP session `b6b3c97e` health was ok for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, imagebase `0x400000`, auto-analysis and Hex-Rays ready.
  - In that dated session, `lookup_funcs 0x004669c0` reported IDA function `sub_4669C0`, size `0xa9`; `lookup_funcs BaramAppOnActivate` returned not found, so the prior saved-label statement was already historical. UID0003OD now establishes the current source-facing identity as `Application::OnActivate`; `BaramApp::OnActivate` is retained only as historical reconstruction terminology.
  - `lookup_funcs 0x00466a69` reports not a function and `0x00466a70` reports successor `sub_466A70`, size `0xb3`, preserving the exact half-open target range and sibling boundary.
  - `xrefs_to 0x004669c0` reports only two vtable data refs at `0x006125f8` and `0x0061d12c`; no ordinary direct code callers were found.
  - `callees 0x004669c0` reports the contiguous callee set `sub_4A90D0`, `sub_557470`, `ShowWindow`, `sub_4EFFA0`, `sub_57A750`, `sub_57A790`, and `sub_57A3D0`.
  - `analyze_function` and `decompile 0x004669c0` now succeed. This supersedes the older C001 Hex-Rays-failure blocker while preserving the same non-contiguous tail caveat: IDA attaches `0x0057a480` to `sub_4669C0`, and the decompilation includes the SoundManager/MidiPlayer/Miles restore sequence from that raw chunk.
  - `decompile 0x0057a400` confirmed the normal SoundManager unmute body calls `MidiPlayer::EnableMidiPlayback`, sets SoundManager playback/focus bytes, optionally restarts pending stream playback through `sub_579E30`, and reapplies stream volume through `sub_57A340`. Historical drafts abstracted the activation raw chunk as `RestoreMusicAfterAppFocus()` and used `friend class BaramApp`; UID0003OD preserves the exact caller-owned tail while correcting the current method owner to `Application`, so any private SoundManager access must be reconciled for the Application source relationship rather than preserving the old BaramApp-specific friend assumption.

## Reconstruction Notes

- Keep this method under [UID:0000HG][Application](by-file/Application.md) through [UID:00000D][Application](by-class/Application.md). BaramApp reaches it by inherited vtable pointer identity, not separate ownership.
- Helper/global names in the current C++ draft are source-facing descriptive names backed by support docs, not raw decompiler labels. `m_exitRequested` is the accepted cross-method name for `+0x83a`; `m_musicPlaybackMode` and `RestoreSoundEffectsAfterAppFocus` remain inferred spellings. Exact SoundManager private fields are synchronized through the complete friend-enabled class declaration.
- The previous no-C++ disposition is superseded. Current MCP decompiles the function, and current support docs resolve the raw dependencies strongly enough for source. The non-contiguous `0x0057a480` audio tail remains a binary-layout caveat but is now represented by its exact caller-owned statements, not a blocker, standalone child, or unsupported helper abstraction.
- Parent attachment remains valid at optional position `30`; this page remains `RECONSTRUCTABLE:TRUE` because the activation policy is NexusTK-owned source that must be rebuilt.

## Score Rationale

- Completion is `90`: exact function boundary, signature/calling shape, virtual-only reachability, key state writes, contiguous callee set, non-contiguous audio-tail chunk, adjacent padding, successful dated Hex-Rays decompilation, complete inline tail, and source-facing support names are documented. Completion remains below final-audit level because several Application/global spellings and some private SoundManager spellings remain inferred.
- Confidence is `93`: current MCP disassembly/decompilation, xrefs, callers/callees, byte/range checks, and the BaramApp/Application/SoundManager support docs agree on behavior, vtable dispatch, tail ownership, and field meaning. Confidence remains below final-source level because `m_exitRequested` and some global/helper names are evidence-backed inferred source spellings rather than recovered original symbols; the former `m_transitionGuard` alias is rejected history.

## Historical Changes And Current UID0003OD Correction

UID0003OD supersedes the earlier BaramApp-qualified owner/emitter interpretation with current `Application::OnActivate` ownership and UID00000D routing. The dated entries below remain as historical reconstruction provenance; their old `BaramApp::OnActivate`, UID00000V owner/emitter, and friend-class conclusions are not current authority.

- 2026-06-29 B001 empty-emitter implementation:
  - Raised score from `86/91` to `88/92`, set `EMITTER_POSITION_OPTIONAL:30`, and added first-draft formal C++ for `void BaramApp::OnActivate()`.
  - Preserved owner/emitter/reconstructable state: [UID:00000V][BaramApp](by-class/BaramApp.md), `TRUE`, [UID:00000V][BaramApp](by-class/BaramApp.md).
  - Historicalized the prior C001 saved-label and Hex-Rays-failure wording: current MCP session `b6b3c97e` reports the function as `sub_4669C0` and decompiles it successfully, while still attaching the raw `0x0057a480` SoundManager restore chunk to the function.
  - Summary/evidence: MCP `server_health`, `lookup_funcs`, `xrefs_to`, `callees`, `analyze_function`, `decompile`, and `disasm` on session `b6b3c97e`; current support docs for [UID:00028L][0x0067a754-0x0067a758.g_pEventMan](by-memory/0x0067a754-0x0067a758.g_pEventMan.md), [UID:00028Z][0x0067a938-0x0067a940.ApplicationLifecycleFlags](by-memory/0x0067a938-0x0067a940.ApplicationLifecycleFlags.md), [SurfaceFlipSurfaces](by-memory/0x00557470-0x0055769d.SurfaceFlipSurfaces.md), [UID:00018Q][0x004effa0-0x004effbd.ApplyFastKeyboardRepeatSettings](by-memory/0x004effa0-0x004effbd.ApplyFastKeyboardRepeatSettings.md), and [UID:0001I9][0x0057a260-0x0057a88e.SoundManagerPlaybackControls](by-memory/0x0057a260-0x0057a88e.SoundManagerPlaybackControls.md).

- 2026-06-23 B005 IsRunning field-writer sync:
  - Score unchanged at `86/91`; formal C++ remains blank.
  - Refreshed the visible-state write as exact `0x004669f5` Application `m_isRunning` / `+0x838` writer evidence for [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md), while preserving BaramApp as a writer/context owner only.
  - Summary/evidence: accepted B005 report `tools/leaser/Agents/Agent-B005/research/0002GO-ApplicationIsRunning-source-quality.md`, IDA MCP session `261fb29b`, updated target [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md), constructor writer `0x00463511`, deactivation writer `0x00466a9d`, and ScreenPane consumer routes `0x00556dab` / `0x00556e36`.

- 2026-06-16 C001 direct IDA activation refresh:
  - What changed: raised completion/confidence from `80/90` to `86/91`, saved the low-risk IDA function label `BaramAppOnActivate`, and kept owner/emitter routing through [UID:00000V][BaramApp](by-class/BaramApp.md).
  - Summary/evidence: live IDA MCP dry-run accepted the label, post-apply `lookup_funcs` verified it, `xrefs_to` reconfirmed virtual-only reachability through `0x006125f8` and `0x0061d12c`, disassembly reconfirmed the foreground transition policy and `0x00466a62 -> 0x0057a480` audio tail, and `analyze_function` still fails on the non-contiguous body, so prototypes/locals/types/final C++ remain intentionally untouched.
- 2026-06-06 A004 parent attachment and evidence refresh:
  - What existed before: the page was `78/88`, reconstructable, and intentionally unassigned while the class and method were still just below the parent-attachment gate.
  - What it was changed to: raised to `80/90`, attached to [UID:00000V][BaramApp](by-class/BaramApp.md) at optional position `30`, added signature/state notes and score rationale, and kept final C++ blank.
  - Summary/evidence: live IDA MCP reconfirmed exact `0x004669c0-0x00466a69` bounds, no direct callers, vtable data refs, state writes, `ShowWindow(..., 9)`, callee set, `0x00466a62 -> 0x0057a480` tail chunk, tail audio callees, and surrounding padding.
- 2026-06-05 A003 autogen-gate correction:
  - What existed before: the page was scored `72/82`, attached to [UID:00000V][BaramApp](by-class/BaramApp.md), and emitted a C++ paraphrase despite unresolved fields/globals and a parent class below the documented attachment threshold.
  - What it was changed to: raised scores to `78/88`, cleared `AUTOGEN_PARENT_UID` and `AUTOGEN_PARENT_POSITION_OPTIONAL`, removed the premature C++ block, and recorded the virtual-dispatch refs, no-direct-caller status, callee set, and non-contiguous audio tail caveat.
  - Summary/evidence: IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, `callees`, `decompile`, and `disasm` on 2026-06-05 confirm the function boundary, vtable/data references, app-state writes, `ShowWindow(..., 9)`, and the `0x00466a62 -> 0x0057a480` audio-tail jump. Final source remains blocked by unresolved layout/global names and audio helper ownership.
- What existed before: `BaramApp::OnActivate` was only represented as a line item inside the broad [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) range.
- What it was changed to: this exact method page now tracks `0x004669c0-0x00466a69` with behavior evidence; the later A003/A004 passes removed the premature C++ draft and reattached the method only after the documented gate was met.
- Summary/evidence: IDA MCP `lookup_funcs` reports `sub_4669C0` size `0xa9`; IDA MCP `disasm` shows the activation guard, timer/event resume, map-pane `ShowWindow(..., 9)`, background, and audio resume paths.
