*** UID:00034C | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-emitted MSVC/Dinkumware std::_Adjust_heap instantiation for
// FolderTreePane child-directory entry sorting. Recreated by the owning
// FolderTreePane child-directory sort expression; no standalone handwritten source.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Assigned to [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md); MSVC/Dinkumware `std::sort` heap-fallback `_Adjust_heap`-style helper for 12-byte child directory-entry records, reached only from the sort wrapper's exhausted-depth heap path and reconstructed through the owning FolderTreePane child-directory sort expression rather than as a handwritten partition helper. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b4fd0-0x004b5271 FolderTreeDirectoryEntryAdjustHeap

## Status

- Disposition: reconstructable source-derived/template-instantiation support for `FolderTreePane` child-directory sorting.
- Entity kind: MSVC/Dinkumware `std::_Adjust_heap`-style heap adjust / sift-down fallback helper for folder directory-entry sorting.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: file-local helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). Ordinary callers are two sites in `0x004b5280`.
- Source-facing name: `FolderTreeDirectoryEntryAdjustHeap` / `FolderTreeDirectoryEntryHeapSiftDown`. The old `SortPartition` label is retained only as a historical/search alias.
- Rebuild handling: generated-binary heap fallback support for the local child-entry sort expression; the owning source should express the sort, not a handwritten standalone heap helper body.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b4fd0-0x004b5271` | `FolderTreeDirectoryEntryAdjustHeap` / `FolderTreeDirectoryEntryHeapSiftDown` | Heap-adjust fallback helper used by the introsort driver when it reaches the depth-limit path; moves/copies 12-byte directory-entry records using normalized string comparisons and temporary string lifetime handling. Exact binary length is `0x2a1` / 673 bytes. |

## IDA Evidence

- 2026-06-10 IDA MCP `lookup_funcs 0x004b4fd0` reports `sub_4B4FD0`, size `0x2a1`, giving exact exclusive end `0x004b5271`.
- 2026-06-10 IDA MCP `callers 0x004b4fd0` reports two call sites at `0x004b5400` and `0x004b54c0`, both inside `sub_4B5280`.
- 2026-06-10 IDA MCP `callees 0x004b4fd0` reports `0x00582c60`, `0x00583450`, `0x005840f0`, `0x004b5c40`, and `0x00582b70`.
- 2026-06-10 decompilation shows repeated normalized string comparison through `0x004b5c40`/`0x005840f0`, record assignment through `0x00582c60`, and cleanup of temporary strings through `0x00582b70`, all over 12-byte directory-entry records.
- The helper is only reached from the recursive sort wrapper and shares no ordinary callers outside the folder-tree directory-entry sort cluster.
- B008 local PE/Capstone review classifies the two wrapper calls as heap fallback paths, not ordinary partition/merge paths: one call occurs while walking parents from `count / 2`, and the other occurs while moving the heap end toward the beginning.
- This range should no longer be described as `SortPartition`; [UID:00034B][0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore](by-memory/0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md) owns the unguarded partition stage and returns the partition pair used by the sort loop.
- B009 local PE/Capstone reanalysis reconfirmed range `0x004b4fd0-0x004b5271`, MD5 `eebf4f657624fe43e8718883b3df7a9a`, first bytes `55 8b ec 6a ff 68 40 ef 5f 00 64 a1 00 00 00 00`, and last bytes `f4 64 89 0d 00 00 00 00 59 5f 5e 5b 8b e5 5d c3`.
- Fresh `.text` rel32 scanning found exactly two direct call sites to this start: `0x004b5400` and `0x004b54c0`, both inside [UID:00034D][0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper](by-memory/0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper.md). No ordinary non-folder-tree caller was found.
- The helper signature is best modeled descriptively as `FolderTreeDirectoryEntryAdjustHeap(FolderTreeDirectoryEntry *first, int holeIndex, int length, FolderTreeDirectoryEntry *value, FolderTreeDirectoryEntryNameLess pred)`: `[ebp+0x08]` is the base pointer, `[ebp+0x0c]` the current hole, `[ebp+0x10]` the heap length, `[ebp+0x14]` the saved value pointer, and the fifth predicate/template argument is optimized away in this body.
- The body computes child indices `2 * hole + 1` / `2 * hole + 2`, compares candidate child records by normalized names, copies the selected child down into the hole, handles the even-length single-child case, bubbles the saved value back up against parents, and writes the saved value to the final hole.

## Ownership And Parent Gate

The direct semantic owner is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). The two callers are both in the local folder-tree child-entry sort wrapper, and the file parent is `89/85`, satisfying the strict gate. This is not a public `FolderTreePane` method and does not consume `this`; it remains attached to the file-level `FolderTreePane.cpp` sort expression because the concrete template specialization is caused by the local child-directory entry vector and comparator.

Rejected owners:

- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md) is context, not the direct owner, because this helper has no member receiver and represents compiler/library support.
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md) is only a mixed non-emitting split inventory.
- A generic STL/runtime owner explains the algorithm shape, but the emitted specialization is tied to FolderTreePane's local `DirectoryEntry` record and should be routed through [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).

## Reconstruction Notes

- Final source should be folded into a conventional sort implementation over the local child-entry vector, probably `std::sort(entries.begin(), entries.end(), FolderTreeDirectoryEntryNameLess)` or an equivalent local wrapper in `ExpandNode` / `EnsureChildrenLoaded`.
- The formal C++ block is intentionally a comment-only no-code marker. A full helper body would hand-port MSVC/Dinkumware template internals and make the reconstruction less like the original mid-2000s C++ source.
- Exact no-code proof: [UID:00034D][0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper](by-memory/0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper.md) matches a `std::_Sort`/introsort driver over 12-byte records; small ranges go to insertion sort, positive depth/ideal takes the unguarded-partition path, and exhausted depth/ideal builds/sorts a heap through this helper. This target matches `_Adjust_heap`, not a handwritten NexusTK function.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact range, two wrapper callers, callee set, heap child/parent index behavior, 12-byte record movement, local record fields, source owner, rejected partition/owner alternatives, and target-specific no-code proof are documented. Completion remains below final-audit quality because original source spellings and direct `std::sort` versus local-wrapper source expression remain inferred. |
| Confidence | 91 | Existing IDA evidence and B009 PE/Capstone/call-scan evidence agree on boundary, caller set, heap-adjust role, and FolderTreePane source placement. Confidence remains below final audit because live IDA MCP was unavailable for B009 and no symbols/PDB/source names recover the exact Dinkumware helper name. |
| Reconstructable | true | Source-owned child-entry sorting support required by rebuilt `FolderTreePane`. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis reclassifies this from stale `SortPartition` wording to the heap adjust / sift-down fallback helper for the local `DirectoryEntry` sort expression in `ExpandNode` / `EnsureChildrenLoaded`. It operates on 12-byte records and belongs to [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) as compiler-generated binary support, not to a standalone sort source file.

Use the `DirectoryEntry` field names `name`, `rowIndex`, `depth`, `childrenLoaded`, and `expanded` when describing record movement. Keep persistent node fields separate: complete 36-byte `TreeNode<FolderTreePane::TreeElem>` records are populated only after the scratch entries have been sorted, with the 12-byte payload placed under `node.data`.

Rejected stale interpretation: `FolderTreeDirectoryEntrySortPartition` is too misleading for this body because the actual partition helper is [UID:00034B][0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore](by-memory/0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md); this range is the heap fallback component of the same introsort lowering.

## 2026-06-21 B009 Rule 26 Incorporation

B009 rechecked the target as a source-quality/inference problem rather than a score-only cleanup. The accepted conclusion is stronger than the previous B008 note: this exact body is best named `FolderTreeDirectoryEntryAdjustHeap` and corresponds to a MSVC/Dinkumware `_Adjust_heap` / heap sift-down helper emitted from the owning `FolderTreePane` child-directory sort expression.

Fresh binary facts from the B009 report:

- Executable source: direct PE disassembly from `NexusTK.exe`; image base `0x00400000`, `.text` virtual range starts at `0x00401000`, `.rdata` at `0x0060e000`, and `.data` at `0x00670000`.
- Target range: `0x004b4fd0-0x004b5271`, length `0x2a1` / 673 bytes, MD5 `eebf4f657624fe43e8718883b3df7a9a`.
- Direct callers: only `0x004b5400` and `0x004b54c0`, both inside the sort loop [UID:00034D][0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper](by-memory/0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper.md).
- `0x004b5400` is the make-heap style parent-walk path that copies a parent entry into a temporary and adjusts the heap at that hole index over the full count.
- `0x004b54c0` is the sort-heap style shrinking-end path that moves the root to the end, saves the former end in a temporary, and adjusts from hole `0` over the reduced count.
- The target itself reads `first`, `holeIndex`, `length`, and `value` from the first four stack arguments. The predicate/template state is optimized away, matching a stateless comparator instantiation.
- The child-selection loop computes binary-heap child indices, compares right/left child names through the local normalization and string comparison helpers, and copies the selected child record down using string assignment plus payload-field copies.
- The final phase compares the saved value against parents, shifts parents downward as needed, and writes the saved 12-byte record into the final hole.

Local record model:

| Offset | Best name | Evidence status |
| --- | --- | --- |
| `+0x00` | `name` | Strong; all sort comparisons normalize/copy this string and compare UTF-16 ranges. |
| `+0x04` | `rowIndex` / `payloadWord0` | Copied transparently by vector/sort/swap and later into `TreeElem`; final semantic remains better documented on tree-node pages. |
| `+0x06` | `depth` / `payloadWord1` | Copied transparently by this helper; final naming remains inferred from later tree insertion. |
| `+0x08` | `childrenLoaded` / `payloadByte0` | Copied transparently; exact source semantic is inherited from the owning expansion path. |
| `+0x09` | `expanded` / `payloadByte1` | Copied transparently; exact source semantic is inherited from the owning expansion path. |
| `+0x0a-0x0b` | padding/reserved | Required by the 12-byte stride. |

Historical assumptions:

- The old `SortPartition` / partition-merge interpretation is rejected for this target. A partition helper scans around a pivot or returns partition boundaries; this body walks binary-heap child/parent indices and receives `(first, holeIndex, length, value, pred)`-shaped arguments.
- A handwritten custom NexusTK heap helper is possible in theory but lower probability than a compiler-instantiated Dinkumware helper because [UID:00034D][0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper](by-memory/0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper.md) exactly follows the introsort driver shape: insertion-sort cutoff, unguarded partition recursion, and depth/ideal heap fallback.
- The exact original source spelling remains unknown. Use `FolderTreeDirectoryEntryAdjustHeap` as a descriptive documentation name and `_Adjust_heap` only as the inferred compiler-template role, not as a recovered symbol.

## 2026-07-29 DirectoryEntry Versus TreeNode Reconciliation

- The `_Adjust_heap`-style helper continues to move only 12-byte `DirectoryEntry` scratch values. Its callers, field map, no-code proof, metadata, route, and `88/91` score are unchanged.
- The compared persistent family is complete 36-byte `TreeNode<FolderTreePane::TreeElem>` storage. The matching 12-byte `TreeElem` payload lives at `node.data`; generic links and `disabled` remain node-owned and are absent from this heap helper.
- Historical assumption retained: earlier comparison wording called the 36-byte persistent object `TreeElem`. The observed width separation was correct, but the name conflated node and payload and is superseded here.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:000343][0x004b3350-0x004b33fc.FolderTreeDirectoryEntryNameLess](by-memory/0x004b3350-0x004b33fc.FolderTreeDirectoryEntryNameLess.md)
- [UID:000348][0x004b3fb0-0x004b4232.FolderTreeDirectoryEntryVectorInsertAt](by-memory/0x004b3fb0-0x004b4232.FolderTreeDirectoryEntryVectorInsertAt.md)
- [UID:000349][0x004b4240-0x004b44b3.FolderTreeDirectoryEntryInsertionSort](by-memory/0x004b4240-0x004b44b3.FolderTreeDirectoryEntryInsertionSort.md)
- [UID:00034A][0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3](by-memory/0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3.md)
- [UID:00034B][0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore](by-memory/0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md)
- [UID:00034D][0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper](by-memory/0x004b5280-0x004b5510.FolderTreeDirectoryEntrySortWrapper.md)
- [UID:00022E][0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers](by-memory/0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers.md)

## Changes

- 2026-06-10 B001-028 split repair:
  - What existed before: this exact sort partition/helper range was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b4fd0-0x004b5271`.
  - Summary/evidence: IDA MCP confirms the `0x2a1`-byte function, two callers in `0x004b5280`, normalized string comparison helpers, and 12-byte record assignment/destruction behavior.
- 2026-06-21 Rule 26 B008 `00034B` support sync:
  - Updated source-facing role from stale partition/merge helper to `FolderTreeDirectoryEntryAdjustHeap` / heap sift-down fallback.
  - Added the wrapper-call context that distinguishes this range from [UID:00034B][0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore](by-memory/0x004b4520-0x004b4fc5.FolderTreeDirectoryEntrySortCore.md) unguarded partition and clarified no-standalone-source handling.
- 2026-06-21 Rule 26 B009 `00034C` incorporation:
  - Changed to: `COMPLETION:88`, `CONFIDENCE:91`, owner/emitter unchanged at [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), source-facing role `FolderTreeDirectoryEntryAdjustHeap`, and comment-only formal no-code marker.
  - Summary/evidence: incorporated B009 PE/Capstone range hash, exact direct-call scan, `_Adjust_heap` argument model, child-selection loop, single-child case, parent push-up/writeback behavior, local `DirectoryEntry` field model, rejected partition/custom-helper alternatives, and no-standalone-C++ proof.
