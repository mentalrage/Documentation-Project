*** UID:0004FK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact compiler-generated destructor lowering for old-MSVC std::vector<SoundPathString> objects used as SoundManager members and scanner locals. Preserves range/size, five normal callers plus EH refs, ANSI StringBase destruction, guarded storage free, triplet reset, boundary padding, historical custom SoundPathVector body, and rejected alternatives; non-reconstructable/non-emitting with blank formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057bac0-0x0057bb2a SoundPathVector Destructor

## Status

- Disposition: non-reconstructable compiler/STL destructor lowering for `std::vector<SoundPathString>`; no custom `SoundPathVector` destructor is emitted.
- Parent range: [UID:0001ID][0x0057b860-0x0057bf6e.SoundPathVector](by-memory/0x0057b860-0x0057bf6e.SoundPathVector.md).
- Documentation route: [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md); source cause is typed vector members/locals in [UID:0000NV][SoundManager](by-file/SoundManager.md).
- Metadata: `92/95`, `CANONICAL_OWNER:0000DI`, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal C++. The exact destructor bytes are regenerated by the compiler.

Historical pre-UID0001IA state emitted a first-draft `SoundPathVector::~SoundPathVector()` with inferred field/helper names. That managed body was useful as behavioral research but is removed because the standard-vector source category is now resolved; all of its binary facts remain below.

## Evidence

Current MCP session `6a4af54d` replaces the stale `fef9a7c2` session and rechecks the accepted body against the live NexusTK IDB. `lookup_funcs` resolves the exact half-open function range `0x0057bac0-0x0057bb2a`, size `0x6a` / 106 bytes. The predecessor boundary is eleven `0xcc` padding bytes at `0x0057bab5-0x0057bac0`; the successor boundary is six `0xcc` padding bytes at `0x0057bb2a-0x0057bb30` before the SoundManager scalar deleting destructor wrapper at `0x0057bb30`. The separate helper successor at `0x0057bc60` remains outside this target.

The current xref split proves five normal code callers at `0x00579d8c`, `0x00579d97`, `0x0057afa6`, `0x0057bc0e`, and `0x0057bc19`, plus EH/unwind metadata references at `0x00608332`, `0x00608340`, and `0x00608407`. Caller contexts keep the method audio-local: SoundManager constructor/setup cleanup destroys the `+0x109c` and `+0x1028` vectors, the local music directory scan destroys a temporary vector at `0x0057afa6` after vector/deque cleanup, and the scalar deleting destructor wrapper at `0x0057bb30-0x0057bc59` repeats destruction of `SoundManager+0x109c` and `SoundManager+0x1028` after Miles/Redbook shutdown before optional object free.

The body reads `m_begin` at `this+0x0`, `m_end` at `this+0x4`, and `m_capacity` at `this+0x8`. If `m_begin` is null, it returns without a free. Otherwise it walks `[m_begin,m_end)` in four-byte elements, calls `0x00582b30 -> 0x005832f0` for ANSI `StringBase` / `SoundPathString` release semantics, frees the old backing allocation through the MSVC storage-free wrapper at `0x005c7526` with the documented large-allocation invalid-parameter guard, and clears the triplet back to zero.

## Confidence Caps

The emitted code intentionally uses source-facing inferred names. Exact original spelling remains a confidence cap for the member fields `m_begin`, `m_end`, and `m_capacity`; the public or local `SoundPathString` typedef/header spelling; and the storage-free helper/source spelling represented here as `FreeSoundPathVectorStorage(...)`.

## Rejected Alternatives

The current source-category rejection is additive: do not emit a custom destructor or preserve the former managed body. Source-authored SoundManager members and automatic `std::vector` locals regenerate the exact element destruction/free/reset behavior. The prior rejected raw labels, direct SoundManager method ownership, parent aggregate output, generic StringBase ownership, and blank-without-proof routes remain preserved.

## Score Rationale

- Completion `92`: exact range, boundary padding, normal/EH caller split, two SoundManager member contexts, scanner-local context, StringBase release chain, guarded free, triplet reset, historical formal body, and current compiler disposition are complete.
- Confidence `95`: the three-pointer vector ABI, automatic/member destructor contexts, StringBase element semantics, and full standard-vector family prove compiler lowering. Exact old custom helper spellings no longer affect output.

## Changes

- 2026-07-13 B004 UID0001IA callback: raised `89/91 -> 92/95`, changed true/emitter UID0000DI to false/blank, removed the custom destructor managed body, and reclassified the target as compiler-generated `std::vector<SoundPathString>` destruction while preserving every range, caller, EH, destructor, storage-free, reset, padding, historical, and rejected-alternative fact.

Reject blank no-code/padding treatment because current session `6a4af54d` proves a modeled 106-byte destructor body with live code callers and nontrivial entry/storage cleanup. Reject raw `sub_57BAC0` naming because the body is class destructor source, reached through SoundPathVector object addresses and deleting-destructor cleanup. Reject direct SoundManager method ownership because the same body receives `SoundPathVector` subobjects at `SoundManager+0x1028` and `+0x109c` and a directory-scan local vector, not a full SoundManager receiver. Reject parent aggregate emission through UID0001ID and broad SoundManager aggregate emission because those pages are non-emitting split/index containers; this exact child owns the destructor output. Reject generic `StringBase` ownership because the helper owns the vector storage triplet and only delegates element release to `StringBase`.
