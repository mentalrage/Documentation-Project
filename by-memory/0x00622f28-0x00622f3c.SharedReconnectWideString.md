*** UID:0003G4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N0,0000OI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: UTF-16LE `Reconnect` pooled button-label literal with `CANONICAL_OWNER:NONE` and `EMITTER_UIDS:0000N0,0000OI`; B002 current/fresh/live rechecks plus earlier B001/B003 passes confirm exact target bytes at `.rdata` file offset `0x221928`, exactly four source-use refs paired with [UID:0003G3][0x00622f1c-0x00622f28.SharedLeaveWideString](by-memory/0x00622f1c-0x00622f28.SharedLeaveWideString.md) in `ReconnectDialog.cpp` / `ConnectionClosedDialog` prompt construction (`0x00553c44`, `0x00553cf4`, `0x0055435e`) and `TerminalPane::OnDisconnect` (`0x0058b5b1`), four raw `push 0x00622f28` operand hits, no target RVA refs, no refs to false interior `aConnect` at `0x00622f2c` or other interior starts, no ASCII spelling, and no source/debug/shared-label owner evidence; the later `0x006288b0` spelling is only the unreferenced suffix of UTF-16 `kServerReconnect` at `0x006288a2`, while ReconnectDialog-only, TerminalPane-only, AlertPanes, class-level, containing-map, split/merge, and synthetic shared-label ownership alternatives all overclaim the pooled literal. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00622f28-0x00622f3c Shared Reconnect Wide String

## Range

- Address range: `0x00622f28-0x00622f3c`.
- Size: `0x14` / 20 bytes. Verified with `int_convert.py`.
- Entity kind: `string-data`.
- Rebuild handling: source-authored UTF-16 string literal pooled into `.rdata`; recreate the literal at each real source use site, not as a hand-authored address.
- Containing map: [UID:0003CW][0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap](by-memory/0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap.md) `0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap`.

## Bytes And Decoding

IDA byte review on 2026-06-12 shows:

```text
0x00622f28: 52 00 65 00 63 00 6f 00 6e 00 6e 00 65 00 63 00 74 00 00 00
```

This decodes as UTF-16LE `Reconnect`. IDA currently names the start `off_622F28` and the suffix at `0x00622f2c` as `aConnect`; those are string-pooling artifacts from the UTF-16 bytes, not a pointer table followed by a separate source string.

## Xrefs And Ownership

Live IDA xrefs on 2026-06-12:

| Address | Owner evidence | Use |
| --- | --- | --- |
| `0x00553c44` | [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) `ConnectionClosedDialog` constructor variant | Passes the label pair to the alert initializer. |
| `0x00553cf4` | [UID:00038J][0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods](by-memory/0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods.md) `ConnectionClosedDialog` constructor path | Passes the label pair to the alert initializer. |
| `0x0055435e` | [UID:00038L][0x00553f40-0x005544b8.ReconnectDialogMethods](by-memory/0x00553f40-0x005544b8.ReconnectDialogMethods.md) `ReconnectDialog::OnButtonClick` candidate | Constructs a replacement connection-closed dialog inline. |
| `0x0058b5b1` | [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) / [UID:0000EG][TerminalPane](by-class/TerminalPane.md) `TerminalPane::OnDisconnect` | Builds the reconnect/leave alert callback path from the terminal pane. |

The literal is shared by `ReconnectDialog.cpp`/`ConnectionClosedDialog` paths and `TerminalPane.cpp`. Both source families use the same pooled `Reconnect` bytes as an alert button label.

## A003 Batch329 Audit

Local PE audit on 2026-06-12 against
`E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` decoded the exact range:

```text
imageBase=0x00400000
rangeOff=0x221928 len=0x14
rangeBytes=52 00 65 00 63 00 6f 00 6e 00 6e 00 65 00 63 00 74 00 00 00
utf16=Reconnect
```

The same audit found UTF-16 `Reconnect` spellings at `0x00622f28` and
`0x006288b0`. The current child covers only the `0x00622f28` pooled label
inside the reconnect/terminal/copy string tail; the later spelling is a
separate `.rdata` occurrence and is not part of this range.

Scanning for the little-endian pointer/immediate `28 2f 62 00` found exactly
the four expected code hits:

```text
0x00553c45, 0x00553cf5, 0x0055435f, 0x0058b5b2
```

Those are the immediate operands for the documented IDA xrefs at
`0x00553c44`, `0x00553cf4`, `0x0055435e`, and `0x0058b5b1`. This independently
confirms the current use-site set and the shared-ownership blocker.

## B001 Rule 26 Live Recheck

Agent-B001 Batch B001-0003G4 rechecked the target with live IDA MCP on
2026-06-12 against `b001_nexustk`. `server_health` confirmed the `NexusTK.exe`
database with image base `0x400000`, Hex-Rays ready, and strings cache ready.
`get_bytes 0x00622f1c size 56` decoded the full tail as UTF-16LE `Leave`,
UTF-16LE `Reconnect`, UTF-16LE `No`, UTF-16LE `Yes`, and ANSI `baram`. The
target bytes are exactly the single UTF-16LE `Reconnect` literal at
`0x00622f28-0x00622f3c`.

IDA's current `get_string`/name state is not source-facing for this island:
`0x00622f28` reads as `"R"`, `0x00622f2c` reads as `"connect"`, and names such
as `off_622F28`/`aConnect` are UTF-16 decode artifacts. B001 found zero xrefs to
the false interior address `0x00622f2c`; no child, split, rename, or owner should
be created for that suffix.

The exact live xref set remains four references to the target start, each paired
with [UID:0003G3][0x00622f1c-0x00622f28.SharedLeaveWideString](by-memory/0x00622f1c-0x00622f28.SharedLeaveWideString.md):

| Address | Source-family meaning |
| --- | --- |
| `0x00553c44` | `ConnectionClosedDialog` constructor variant calls the alert initializer with `Reconnect`/`Leave`. |
| `0x00553cf4` | Alternate `ConnectionClosedDialog` constructor path calls the same label-pair initializer. |
| `0x0055435e` | `ReconnectDialog::OnButtonClick` replacement connection-closed path constructs the same prompt. |
| `0x0058b5b1` | `TerminalPane::OnDisconnect` constructs an independent callback-backed reconnect/leave prompt. |

Function analysis supports those source families. `sub_553C10` and
`sub_553CC0` install `ConnectionClosedDialog` vtables and pass
`&off_622F28`/`&off_622F1C` into the alert builder. `sub_554210` can send the
`"baram"` reconnect packet and also creates a replacement connection-closed
prompt with the same label pair. `sub_58B470` allocates a callback object, stores
callback target `sub_58B620` at `0x0058b56d`, then builds the
TerminalPane-owned prompt; `sub_58B620` dispatches reconnect versus leave
behavior. The TerminalPane path does not delegate this prompt to
`ReconnectDialog`.

Pattern searches did not reveal hidden ownership. Full and unterminated UTF-16
`Reconnect` occur at `0x00622f28` and `0x006288b0`; ASCII `Reconnect\0` has no
matches. The little-endian absolute VA `28 2f 62 00` has only the four operand
hits listed above, while the interior VA `2c 2f 62 00` and RVA-form
`28 2f 22 00` have no hits.

The second `Reconnect` spelling is negative context only. Bytes around
`0x00628840-0x00628900` show the later occurrence is the suffix of an
unreferenced UTF-16 `kServerReconnect` string starting at `0x006288a2`, not a
second standalone prompt label. B001 checked xrefs to `0x006288a0`,
`0x006288a2`, `0x006288a4`, `0x006288b0`, and `0x006288b4`, absolute/RVA pointer
patterns for the neighborhood, and the full `kServerReconnect` pattern; none
produced a source-owner lead for this target.

Rejected ownership alternatives:

| Candidate | Decision |
| --- | --- |
| [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) | Best if forced by xref count, but rejected because `TerminalPane::OnDisconnect` directly uses the same physical label and owns its callback-backed prompt. |
| [UID:0000OI][TerminalPane](by-file/TerminalPane.md) | Proves shared use and blocks ReconnectDialog-only ownership, but rejected because only one of four target refs belongs to TerminalPane. |
| [UID:0000HE][AlertPanes](by-file/AlertPanes.md) | Rejected because this is a feature-specific four-ref label pair, unlike broad reusable dialog labels such as `OK`; alert infrastructure use is not label ownership. |
| Class-level owners such as `ConnectionClosedDialog`, `ReconnectDialog`, or `TerminalPane` | Rejected because no single class covers all four physical xrefs, and the item is pooled literal data rather than class-static storage. |
| New shared source file or global such as `ReconnectTerminalLabels.cpp`, `SessionRecoveryLabels.cpp`, or `ReconnectLeaveButtonLabels` | Rejected because the only plausible contents are ordinary prompt literals already explained by linker string pooling; no table, initializer, symbol, broad fan-in, or shared declaration was found. |

The B001 post-migration pass used IDA MCP session `b001_0003gy` against
`NexusTK.exe` and explicitly checked `idb_list`, `server_health`, `get_bytes`,
`get_string`, `xrefs_to`, `lookup_funcs`, `trace_data_flow`,
`analyze_component`, `search_text`, and `decompile`. `server_health` reported
image base `0x400000`, Hex-Rays ready, auto-analysis ready, and strings cache
ready. `lookup_funcs 0x00622f28` returned `Not a function`; the four refs map
to `sub_553C10` size `0xaf`, `sub_553CC0` size `0xae`, `sub_554210` size
`0x1f6`, and `sub_58B470` size `0x170`. `lookup_funcs 0x0058b620` returned the
`0x2f`-byte callback target stored by the TerminalPane disconnect prompt path.

Post-migration source-use routing is therefore:

| Emitter route | Proven sites | Source-use meaning |
| --- | --- | --- |
| [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) | `0x00553c44`, `0x00553cf4`, `0x0055435e` | `ConnectionClosedDialog` constructor variants and `ReconnectDialog::OnButtonClick` replacement-alert logic pass `&off_622F28` with sibling `&off_622F1C` into alert builders. |
| [UID:0000OI][TerminalPane](by-file/TerminalPane.md) | `0x0058b5b1` | `TerminalPane::OnDisconnect` allocates a `PlainMemberFunctionObject`, stores callback target `sub_58B620` at `0x0058b56d`, then calls the terminal alert builder with the same `Reconnect`/`Leave` pair. |

The recheck also separated facts from inference. IDA facts are the exact bytes,
four xrefs, paired sibling use with `Leave`, no interior xrefs, containing
functions, and decompiled call-site behavior. Raw PE facts are that the target
VA occurs only as the four expected code immediates, the false interior and
later suffix addresses have no absolute/RVA pointer hits, and the only later
`Reconnect` bytes are the unreferenced suffix of `kServerReconnect`. The
inference is only source-form choice: the original source either repeated local
literals that the linker pooled or used an unrecovered macro/header constant.
Neither possibility creates a single declaration owner in the stripped binary,
so the current owner/emitter model keeps `CANONICAL_OWNER:NONE` while routing
output through both proven source-use file roots.

Raw PE scan details from the same report:

| Pattern | Result | Meaning |
| --- | --- | --- |
| UTF-16LE `Reconnect\0` | two hits at file offsets `0x221928` and `0x2272B0` | Target occurrence plus later suffix occurrence. |
| ASCII `Reconnect\0` | zero hits | No ASCII prompt spelling. |
| UTF-16LE `kServerReconnect\0` | one hit at file offset `0x2272A2` | Later `Reconnect` bytes are only an unreferenced suffix. |
| Absolute VA `0x00622f28` | four hits at file offsets `0x153045`, `0x1530F5`, `0x15375F`, `0x18A9B2` | Immediate operands for the four IDA refs. |
| Absolute VA `0x00622f2c` | zero hits | No hidden interior/suffix pointer. |
| Absolute VA `0x00622f3c` | two hits at file offsets `0x1532BC`, `0x1538C6` | Separate successor `No` child refs, not part of this range. |
| Absolute VAs `0x006288a2` / `0x006288b0` and RVA forms for target/interior/later suffix | zero hits | No hidden pointer table or RVA-form source-owner lead. |

Final C++ should therefore spell `L"Reconnect"` at each semantic prompt
construction site in the owning source files. Do not introduce a standalone
external helper/global just to model the pooled `.rdata` address, and do not
route the physical cell to `ReconnectDialog`, `TerminalPane`, `AlertPanes`, or a
new shared-label owner unless future source/debug/map evidence proves a real
declaration.

## B002 Rule 26 Consolidated Rechecks

The 2026-06-22 B002 `current no-owner sweep`, `fresh no-owner pass`, and
`live Goal 2 no-owner pass` all kept this page at `89/94`,
`CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`,
`EMITTER_UIDS:0000N0,0000OI`, blank emitter position, and blank direct C++.
Those reports were no-change recommendations, but their evidence is preserved
here because Rule 26 requires retaining valid researched detail rather than
only the final disposition.

The B002 current sweep used IDA MCP session `b001_0003gy`; the B002 fresh/live
passes used session `a001_goal2_class_batch`. Both reported
`auto_analysis_ready`, `hexrays_ready`, and `strings_cache_ready`. Raw PE checks
used `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, MD5
`4247e04e20b65d6414c7238aa8ff5515`, image base `0x00400000`, and mapped the
target to `.rdata` file offset `0x221928`.

| Evidence item | B002 result |
| --- | --- |
| target bytes | `52 00 65 00 63 00 6f 00 6e 00 6e 00 65 00 63 00 74 00 00 00`, UTF-16LE `Reconnect` |
| containing tail | UTF-16LE `Leave`, `Reconnect`, `No`, `Yes`, then ANSI `baram` |
| direct target refs | exactly four: `0x00553c44`, `0x00553cf4`, `0x0055435e`, `0x0058b5b1` |
| paired `Leave` refs | matching four refs to [UID:0003G3][0x00622f1c-0x00622f28.SharedLeaveWideString](by-memory/0x00622f1c-0x00622f28.SharedLeaveWideString.md) |
| false-interior refs | zero refs to `0x00622f2c`, `0x00622f30`, `0x00622f34`, or `0x00622f38` |
| later duplicate | `0x006288b0` is only the suffix of unreferenced UTF-16 `kServerReconnect` at `0x006288a2`; zero refs to either address |
| raw operand scan | four `push 0x00622f28` hits and four absolute target-VA dword hits, matching the IDA refs |
| raw negative scans | zero target RVA dword hits, zero false-interior dword hits, zero later-string dword hits, and zero ASCII `Reconnect\0` hits |
| source/debug scan | no PDB/source-path/debug metadata or `.cpp` breadcrumb proving a shared declaration owner |

B002 decompilation and component checks preserved the same source-use split:
`sub_553C10`, `sub_553CC0`, and `sub_554210` call the alert builder
`sub_49FEB0` with `&off_622F28` and `&off_622F1C` in the
ReconnectDialog/ConnectionClosedDialog source family; `sub_58B470` stores
TerminalPane callback target `sub_58B620` at `0x0058b56d` and calls short
wrapper `sub_4A0690`, which reaches `sub_49FEB0`, with the same
`Reconnect`/`Leave` pair. This makes [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md)
and [UID:0000OI][TerminalPane](by-file/TerminalPane.md) both real emitters but
does not make either a canonical declaration owner.

B002 rejected the following alternatives again:

| Candidate | Rejection reason |
| --- | --- |
| [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) as canonical owner | three refs are in that source family, but the independent `TerminalPane::OnDisconnect` ref at `0x0058b5b1` would be overclaimed |
| [UID:0000OI][TerminalPane](by-file/TerminalPane.md) as canonical owner | the TerminalPane prompt is real, but the three ReconnectDialog/ConnectionClosedDialog refs would be overclaimed |
| [UID:0000HE][AlertPanes](by-file/AlertPanes.md) as owner/emitter | the literal is passed into alert infrastructure from feature code; no xref places the declaration in generic AlertPanes source |
| class-level owners | `ConnectionClosedDialog`, `ReconnectDialog`, and `TerminalPane` each cover only part of the physical use set |
| containing map [UID:0003CW][0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap](by-memory/0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap.md) | mixed physical tail with independent `Leave`, `Reconnect`, `No`, `Yes`, and `baram` children, not one source declaration |
| synthetic shared-label file/global | no symbol, source path, source string, initializer, table, or broad fan-in proves a human-authored common label declaration |
| split at `aConnect` / `0x00622f2c` | suffix-only IDA string view with zero IDA or raw PE references |
| merge with sibling strings or later `kServerReconnect` | siblings have distinct ranges and owners; later spelling is outside this range and unreferenced |

Generated no-owner/emitter rows that name
`auto-generated/NexusTK/network/ReconnectDialog.cpp` are output-path artifacts,
not ownership proof. Final reconstruction should keep spelling `L"Reconnect"`
at each semantic prompt-construction site in [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) and [UID:0000OI][TerminalPane](by-file/TerminalPane.md)
instead of emitting a standalone global or address-backed helper for this pooled
`.rdata` cell.

## B003 Current Goal 2 And No-Owner Rechecks

Agent-B003's current Goal 2, current no-owner, and second-pass reports rechecked the accepted B001/B002 state. Most claims were already incorporated by the B001/B002 target updates above; the following distinct B003 details are retained:

- IDA MCP sessions `b001_0003gy` and `b001_0002bd` both reported `NexusTK.exe`, image base `0x00400000`, auto-analysis ready, Hex-Rays ready, and a ready strings cache with `2067` strings.
- The current no-owner report treated the generated destination `auto-generated/NexusTK/network/ReconnectDialog.cpp` as an output-path artifact only; it is not evidence that [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) is sole canonical owner.
- `find_regex` / source-metadata searches for `.pdb`, `RSDS`, `NB10`, `.cpp`, and `.cxx` returned no source/debug metadata that would prove a shared declaration owner.
- A broad blind relative-displacement scan produced one mathematical hit for the inside-target value `0x00622f38` at `0x0057df4c`, but live IDA identifies that instruction as a call to `sub_57E9A0` in `sub_57D9C0`; IDA reports zero data xrefs to `0x00622f38`, so this is not evidence for a suffix string, split, or owner.
- B003 reconfirmed zero IDA/raw refs to the false interiors `0x00622f2c`, `0x00622f30`, `0x00622f34`, and `0x00622f38`, and zero refs to the later `kServerReconnect` start `0x006288a2` or suffix `0x006288b0`.
- B003's current Goal 2 pass explicitly kept no C++ despite the target clearing the minimum score/emitter gate because this page is a pooled literal cell; final source should place `L"Reconnect"` in the consumer methods, not in a standalone address-owned declaration.

These reports therefore close as incorporated/superseded without changing the current metadata or coverage: `89/94`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N0,0000OI`, blank direct C++.

## Owner And Emitter Decision

`CANONICAL_OWNER` remains `NONE`. The child is source-authored and clears the
documentation gate, but no single direct parent owns all observed use sites.
The viable owner candidates checked are [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md)
`ReconnectDialog` at `87/88` and [UID:0000OI][TerminalPane](by-file/TerminalPane.md)
`TerminalPane` at `87/85`; both clear `85/85`, but neither owns the whole
literal because the same pooled bytes are referenced by both source families.

`EMITTER_UIDS` is set to `0000N0,0000OI`. Under the current owner/emitter split,
this is a valid no-owner pooled-literal case: the physical UTF-16 `Reconnect`
cell has no defensible canonical declaration owner, but it must surface through
the proven source-use file roots `ReconnectDialog.cpp`/`ConnectionClosedDialog`
and `TerminalPane.cpp`.

## Score Rationale

Completion is `89` because the page records exact byte decoding, the current
IDA xref table, independent PE pointer/immediate hits for all four references,
the second separate `Reconnect` spelling outside the child, and the
owner/emitter gate result. Confidence is `94` because the IDA xrefs, executable
bytes, pointer scan, containing-map split, and emitter file docs all agree. It
remains below final-audit confidence because final source should still place the
literal at each semantic use site rather than through a standalone shared-address
declaration.

## Cross-References

- [UID:000269][0x00622d24-0x00622f54.QueueReconnectCopyReadOnlyData](by-memory/0x00622d24-0x00622f54.QueueReconnectCopyReadOnlyData.md) containing Queue/Reconnect/Copy read-only-data aggregate.
- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) reconnect/connection-closed dialog source file.
- [UID:0000OI][TerminalPane](by-file/TerminalPane.md) terminal pane source file.
- [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) constructor variant using the label pair.
- [UID:00038J][0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods](by-memory/0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods.md) alternate constructor and handlers using the label pair.
- [UID:00038L][0x00553f40-0x005544b8.ReconnectDialogMethods](by-memory/0x00553f40-0x005544b8.ReconnectDialogMethods.md) reconnect button/timeout method cluster.
- [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) terminal pane executable aggregate.

## Changes

- 2026-06-12 Agent-A002 Batch 263: Created as an exact child split from the former mixed string-tail page. Evidence comes from live IDA byte/xref/decompile checks and `int_convert.py` size verification.
- 2026-06-12 A003 Batch329: Raised `86/91 -> 88/93` after local PE audit reconfirmed the exact UTF-16 bytes, found the second `Reconnect` spelling outside this child at `0x006288b0`, and found exactly four pointer/immediate hits to `0x00622f28` matching the documented reconnect-dialog and terminal-pane xrefs. `AUTOGEN_PARENT_UID` stays blank because [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) and [UID:0000OI][TerminalPane](by-file/TerminalPane.md) both clear `85/85` but neither is the sole direct owner of the pooled label.
- 2026-06-12 Agent-B001 Batch B001-0003G4: Performed ownership/source-family audit. Live IDA MCP against `b001_nexustk` reconfirmed exact UTF-16LE `Reconnect` bytes, exactly four absolute-immediate consumers paired with [UID:0003G3][0x00622f1c-0x00622f28.SharedLeaveWideString](by-memory/0x00622f1c-0x00622f28.SharedLeaveWideString.md), zero refs to false interior `aConnect` at `0x00622f2c`, no ASCII spelling, no RVA-form refs, and no additional referenced standalone `Reconnect` prompt. The later `0x006288b0` spelling was proven to be the unreferenced suffix of `kServerReconnect` at `0x006288a2`, not an owner lead. `ReconnectDialog`, `TerminalPane`, `AlertPanes`, class-level parents, and a new shared-label/source owner were ranked and rejected as overclaiming pooled literal ownership. Parent remains blank; final source should spell `L"Reconnect"` at each semantic use site.
- 2026-06-13 B001 post-migration recheck: retained `CANONICAL_OWNER:NONE` because the UTF-16LE `Reconnect` button-label literal is materially shared by `ReconnectDialog`/`ConnectionClosedDialog` prompt construction and `TerminalPane::OnDisconnect`, with no proven shared declaration owner. Added emitter routing `EMITTER_UIDS:0000N0,0000OI` because the current owner/emitter model allows no-owner pooled literals to emit through every proven source-use file root.
- 2026-06-13 B003 second-pass audit: kept `89/94`, `CANONICAL_OWNER:NONE`, and `EMITTER_UIDS:0000N0,0000OI`. Live IDA MCP and raw PE checks reconfirmed the exact UTF-16LE `Reconnect` bytes, exactly four source-use refs paired with `Leave`, no false-interior `0x00622f2c` refs, no target RVA refs, no refs to the later `kServerReconnect` suffix, no single declaration owner across ReconnectDialog-family and TerminalPane use sites, and no split, merge, reclassification, or IDA repair need.
- 2026-06-22 B002 current/fresh/live no-owner passes: kept `89/94`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N0,0000OI`, blank emitter position, and blank direct C++. These reports reconfirmed the same four `Reconnect` source-use refs and paired `Leave` refs across IDA MCP sessions `b001_0003gy` and `a001_goal2_class_batch`; raw PE section mapping places the target at `.rdata` file offset `0x221928`; raw scans found exactly four `push 0x00622f28` uses, no target RVA refs, no false-interior `0x00622f2c` refs, no referenced `kServerReconnect`/`0x006288b0` suffix route, no ASCII spelling, and no source/debug/shared-label owner evidence. B002 retained both file-root emitters while rejecting ReconnectDialog-only, TerminalPane-only, AlertPanes, class-level, containing-map, split, merge, and synthetic shared-label ownership alternatives.
- 2026-06-22 B003 current Goal 2/current no-owner/second-pass reports: kept the same metadata and source-use route, reconfirmed the two IDA sessions `b001_0003gy`/`b001_0002bd`, source/debug metadata negatives, generated-output path artifact handling, and the false broad-rel32 hit for `0x00622f38` at `0x0057df4c` as a call displacement rather than a data reference. No new owner, emitter, split, merge, coverage, IDA repair, or direct C++ action is required.
