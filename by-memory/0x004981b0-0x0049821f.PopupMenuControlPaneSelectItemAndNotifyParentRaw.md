*** UID:0004IS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void PopupMenuControlPane::SelectItemAndNotifyParent(long index)
{
    DialogPane *parentDialog = static_cast<DialogPane *>(GetParent());

    SetSelectedIndex(index);

    const int controlIndex = parentDialog->FindControlIndex(this);
    if (controlIndex != -1)
        parentDialog->OnControlCommand(controlIndex, 12);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 111-byte source-authored PopupMenuControlPane private selection helper: caches the original parent, source-level calls SetSelectedIndex(long) matching the inlined code-10 path, then independently sends literal notification 12 when the original parent's control index is valid; current IDA models the function, while zero surviving direct or pointer routes cap lexical-name and liveness confidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004981b0-0x0049821f PopupMenuControlPane Select Item And Notify Parent Raw

## Status

- Exact method range: `[0x004981b0,0x0049821f)`, size `0x6f` / 111 bytes.
- Owner/emitter: [UID:0000AN][PopupMenuControlPane](by-class/PopupMenuControlPane.md).
- Split parent: [UID:00011F][0x00498040-0x00498599.PopupMenuControlPaneCore](by-memory/0x00498040-0x00498599.PopupMenuControlPaneCore.md).
- Current IDA model: `PopupMenuControlPane_SelectItemAndNotifyParent`, size `0x6f`. `Raw` in this page's title is retained discovery provenance, not current modeling state.
- Source disposition: retained source-authored private member definition. The absent inbound route caps liveness and original-name confidence but does not convert a coherent authored body into padding, compiler glue, or no-code evidence.

## Exact Binary Contract

- Exact body SHA256: `6DC833EECFD6DDF74E589850D46D0ADA9D992DAD5588742A3C67CC219C3255C7`.
- The body has 49 instructions, seven coherent basic blocks, one entry, one `retn 4`, no embedded data, no EH subrange, and no internal child or split boundary.
- Entry signature: `55 8B EC 53 57 8B F9 8B 07`. Receiver use is ECX-based and the single four-byte stack argument is the selection index.
- Current IDA frame rows are saved registers at `+0x08`, return address at `+0x0c`, and `index` at `+0x10`; each row is four bytes. There is no stable local-stack row.
- The current UDT is `PopupMenuControlPane`, size `0x114`, with inherited/base bytes through `+0x108`, `m_menuPane` at `+0x108`, `m_selectedIndex` at `+0x10c`, and `m_parentDialog` at `+0x110`.
- Nine `0xcc` bytes precede the method at `[0x004981a7,0x004981b0)`. One `0xcc` byte follows at `[0x0049821f,0x00498220)` before [UID:0004IT][0x00498220-0x0049826d.PopupMenuControlPaneSetSelectedIndex](by-memory/0x00498220-0x0049826d.PopupMenuControlPaneSetSelectedIndex.md).

## Current IDA State

- Current bounded lookup models the exact range as `PopupMenuControlPane_SelectItemAndNotifyParent`, size `0x6f`, with source-equivalent `void __thiscall(PopupMenuControlPane *this, int index)` ABI spelling. Source uses `long`; 32-bit MSVC `int` and `long` are ABI-equivalent here, and the sibling member-callback type independently supports `long`.
- Current function regular comment: `Retained source-shaped helper that applies selection and notifies the original parent with command 12. SelectItemAndNotifyParent is descriptive because no direct declaration or live route survives.` Address regular, address repeatable, and function repeatable comment channels are blank.
- Historical state: session `bf5519ae` did not model a function at `0x004981b0`. That pre-curation raw result remains useful history, but it is superseded as current state by the exact modeled function above.

## Exact State And Call Flow

1. The method obtains and caches the original `DialogPane *` parent before changing selection state.
2. The observed first branch is the optimized inline expansion of `SetSelectedIndex(long)`: reject `index == -1` and unchanged selection, write `m_selectedIndex` at `+0x10c`, invalidate inherited bounds, obtain the parent again, find this control's index, and send parent notification code `10`.
3. After that conditional update path, the method uses the original cached parent rather than the setter's second parent value. It performs a second `DialogPane::FindControlIndex(this)` and sends literal notification `12` only when the result is not `-1`.
4. The final cached-parent lookup has no null guard. Adding one would change the original failure behavior, so the formal source preserves the parent invariant and the same null-crash possibility.

The formal source writes the optimized duplicate as the ordinary source-level call `SetSelectedIndex(index)`. This is behaviorally equivalent to the inlined bytes and is more plausible developer source than reproducing compiler-expanded virtual calls and temporary variables.

## Notification Semantics

- Code `10` is the ordinary selection-change notification. [UID:000459][0x004b1920-0x004b1a00.FolderSelectDialogOnCommand](by-memory/0x004b1920-0x004b1a00.FolderSelectDialogOnCommand.md) directly consumes `commandId == 0 && notifyCode == 10`, then reads the selected popup item and updates the folder root.
- Code `12` is proven only as a separate notification to the original parent after a successful control-index lookup. It occurs even when `SetSelectedIndex` makes no state change, provided the final lookup succeeds.
- [UID:000459][0x004b1920-0x004b1a00.FolderSelectDialogOnCommand](by-memory/0x004b1920-0x004b1a00.FolderSelectDialogOnCommand.md) has no code-12 branch. [UID:0003MD][0x00529790-0x00529b70.MusicControlDialogHandleControlCommand](by-memory/0x00529790-0x00529b70.MusicControlDialogHandleControlCommand.md) explicitly ignores `notifyCode` and switches on `controlIndex`. [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) and [UID:0001KG][0x00599cc0-0x0059bc8b.UserInfoDialogPane](by-memory/0x00599cc0-0x0059bc8b.UserInfoDialogPane.md) likewise preserve control-driven handlers whose second notification parameter is unused.
- No known consumer proves that `12` means close, commit, accept, dismiss, or hide. Formal source therefore keeps literal `12` instead of inventing an enum or stronger semantic name.

## Name And Signature

- Source-facing signature: inferred private `void PopupMenuControlPane::SelectItemAndNotifyParent(long index)`.
- `retn 4`, ECX receiver use, one four-byte argument, and no return value establish the `__thiscall` ABI. The constructor's decorated callback type proves the sibling selection API takes `long`.
- `SelectItemAndNotifyParent` is the narrowest name that describes all proved behavior without claiming that selection always changes. `SetSelectedIndexAndNotifyParent` overstates the no-change path; close/commit/accept/dismiss names overstate code-12 semantics.

## Retained Liveness And Reachability

- Current checks find zero incoming xrefs, zero separate code xrefs, zero separate data xrefs, zero little-endian VA hits for `B0 81 49 00`, and zero RVA hits for `B0 81 09 00`.
- No PopupMenuControlPane vtable cell, data slot, direct caller, or callback binding targets `0x004981b0`. The constructor's member-function callback targets active setter `0x00498220`; that setter has sixteen code xrefs plus the constructor data binding.
- Negative route evidence is not used to erase the body. Its unique entry signature, exact fences, class receiver fields, two parent API calls, complete CFG, and instruction-for-instruction setter parity establish a source-authored retained-unused member from the linked translation unit.
- Exact linker/build-option mechanics that retained the unused method are not recoverable. They have no effect on the source representation and remain only a final liveness-confidence cap.

## Source Parity And Formal Channels

- The existing target CPP definition is preserved exactly. It caches the parent before the setter call, calls the source-level setter, uses the cached parent for the final lookup, omits a behavior-changing null guard, tests the final index against `-1`, and sends literal `12`.
- The target H channel stays blank because this method-range page owns only the definition. The private declaration belongs to class [UID:0000AN][PopupMenuControlPane](by-class/PopupMenuControlPane.md)'s formal H channel.
- Owner, emitter, reconstructable state, filename, title, and exact range remain unchanged. No split, merge, child, or source-file reroute is warranted.

## Rejected Alternatives

- Padding/dead bytes: rejected by complete source-shaped control flow, class fields, calls, and return.
- Compiler-generated wrapper/glue: rejected by ordinary member state transitions, source-level setter parity, parent notifications, and absence of thunk/table/deleting-wrapper traits.
- `ClosePopup`, `CommitSelection`, or `DismissMenu`: rejected because notification code 12 has no proven semantic consumer.
- Merge with setter: rejected because this retained body has a distinct second parent notification and exact address range.
- No-owner/non-emitting: rejected by class receiver fields, neighboring class run, parent API use, and duplicated setter semantics.
- Consumer-file ownership or a new raw-helper file: rejected because feature dialogs only consume generic popup APIs and the body belongs to the coherent PopupMenuControlPane class/source route.

## Score Rationale

- Completion `94` reflects the dedicated current function model, exact full-body hash, complete instruction/CFG/frame/UDT/boundary contract, exhaustive route checks, exact state/call flow, four-parent consumer audit, line-by-line formal-source parity, and resolved CPP/H ownership.
- Confidence `93` reflects mutually reinforcing binary, class-layout, setter/callback, parent-consumer, ownership, and generated-route evidence. It remains below final-audit level because no original symbol proves lexical spelling/access, no surviving caller proves live use, no source enum proves the name of code `12`, and the exact historical physical source split remains inferred.
- Those caps do not block behaviorally exact, human-shaped source and do not justify a lower code/readiness disposition.

## Changes

- 2026-08-10 B002 accepted UID0004IS implementation callback: raised `88/89` to `94/93`, replaced stale current raw/unmodeled wording with the exact current function model, added the complete binary/state/consumer/source-parity/reachability record, and preserved the exact CPP, blank H, owner/emitter route, and historical raw discovery state.
- 2026-07-12 B005 accepted UID00011F implementation callback: created this exact raw child with exhaustive liveness negatives, rejected names, and literal formal body.
