*** UID:0002FD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001Y | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001Y | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingVarietyPane::OnPaint()
{
    EPFTileContext tileContext;

    SetTextBackFillColor(0);

    g_pEPFLib->LookupLayoutEntry(
        L"CHATVAR.EPF",
        g_pChattingVarietySelectPane != NULL,
        &tileContext);

    RenderTileFrame(
        &tileContext,
        &tileContext.bounds,
        &m_visibleBounds,
        0,
        L"CHATVAR.PAL",
        NULL);

    wchar_t modeText[128];

    switch (g_pUserPane->m_currentSayMode) {
    case kSayInputModeDefault:
        wcscpy_s(modeText, _countof(modeText), L"Talk");
        break;

    case kSayInputModeShout:
        wcscpy_s(modeText, _countof(modeText), L"Shout");
        break;

    case kSayInputModeWhisper:
        wcscpy_s(modeText, _countof(modeText), L"Whisper");
        break;

    case kSayInputModeGroup:
        wcscpy_s(modeText, _countof(modeText), L"Group");
        break;

    case kSayInputModeClan:
        wcscpy_s(modeText, _countof(modeText), L"Clan");
        break;
    }

    const int textLength = static_cast<int>(wcslen(modeText));
    SetTextColor(128);

    const int textX =
        (m_visibleBounds.left + m_visibleBounds.right) / 2
        - (6 * textLength) / 2 - 2;
    const int textY = m_visibleBounds.bottom - 6;

    MoveTo(textX + 1, textY);
    DrawWideText(modeText, textLength);
    MoveTo(textX, textY);
    DrawWideText(modeText, textLength);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready ChattingVarietyPane::OnPaint over exact body [0x00480e80,0x00480ff1), 369 bytes, SHA256 51D838D5125C26AC11DAA7357271B5BD6CC20884638CE1EC66560862E0F60679, reached only through primary-vtable slot 0x00614ee4. The method initializes a borrowed EPFTileContext, sets text-back fill to 0, resolves CHATVAR.EPF through the ImageLib/ResourceLayoutTable compatibility API with frame 0/1 selected by g_pChattingVarietySelectPane, renders CHATVAR.PAL, maps g_pUserPane->m_currentSayMode values 0..4 to Talk/Shout/Whisper/Group/Clan without inventing a default, sets text color 128, and performs the exact centered same-state x+1/x double-strike. Formal output is the method body only; the parent already declares OnPaint and UID0002RX owns the shared SayInputMode enum in SayInputPanes.h. The trailing 15-byte 0xcc fence and successor forwarder remain outside this method. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00480e80-0x00480ff1 ChattingVarietyPane OnPaint

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: paint/render method.
- Current generated owner: `ChattingVarietyPane`.
- Likely source placement: [UID:0000I5][Chatting](by-file/Chatting.md).
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Confidence: very strong for the body/range/hash, resources, five mode labels, vtable ownership, helper call order and arguments, `UserPane::m_currentSayMode`, text positioning, lifetime, and source placement. Original lexical spelling remains inferred and caps the score below the final `95/95` barrier, but no source-shape blocker remains.
- Formal source disposition: emit exactly one method definition through this page. Keep this page's H channel blank because [UID:00001Y][ChattingVarietyPane](by-class/ChattingVarietyPane.md) already declares `virtual void OnPaint();` and [UID:0002RX][0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode](by-memory/0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode.md) owns the shared `SayInputMode` definition in `SayInputPanes.h`.

## Boundary

IDA models the complete authored body as `[0x00480e80,0x00480ff1)`, size `0x171` / 369 bytes. Its exact body SHA256 is `51D838D5125C26AC11DAA7357271B5BD6CC20884638CE1EC66560862E0F60679`. The next `[0x00480ff1,0x00481000)` span is exactly fifteen `0xcc` alignment bytes and belongs outside this page's emitted method. The distinct successor at `0x00481000` is [UID:0002FE][0x00481000-0x0048100a.ChattingVarietyPaneRefreshForwarder](by-memory/0x00481000-0x0048100a.ChattingVarietyPaneRefreshForwarder.md), a compiler-generated secondary forwarder that must not be merged into the authored body.

## Behavior

This method draws the compact chat-mode button and its current mode label. It initializes a stack [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md) as a borrowed resource view. It does not allocate context-owned buffers, call `ReleaseBuffers`, invoke a destructor, or enter an EH cleanup path.

The first GrafPort state call is shared helper `0x004b9670`, now source-facing `SetTextBackFillColor(0)`. Exact helper bytes `55 8B EC 8B 45 08 89 41 78 5D C2 04 00` store the argument at receiver `+0x78`; current GrafPort layout names this field `m_textBackFillColor`. This is not a draw/display-mode write to `+0x70`.

`g_pEPFLib->LookupLayoutEntry(L"CHATVAR.EPF", g_pChattingVarietySelectPane != NULL, &tileContext)` selects frame `1` while the selector popup exists and frame `0` while it is absent. The accepted source contract is [UID:00006E][ImageLib](by-class/ImageLib.md), with `typedef ImageLib ResourceLayoutTable` as the compatibility facet and one `extern ImageLib *g_pEPFLib`; this method does not require or justify an independent ResourceLayoutTable object or second global.

The render call passes `&tileContext`, `&tileContext.bounds`, `&m_visibleBounds`, draw argument `0`, `L"CHATVAR.PAL"`, and a null final pointer to the shared Surface paint path. `CHATVAR.EPF` at `0x00615334` and `CHATVAR.PAL` at `0x0061534c` each have one direct data xref from this target.

The mode source is [UID:0000FQ][UserPane](by-class/UserPane.md)`::m_currentSayMode`, an `int` at `g_pUserPane + 0x3eb4`. Cases `0..4` map exactly to `Talk`, `Shout`, `Whisper`, `Group`, and `Clan`. Each branch uses a bounds-checked copy into `wchar_t modeText[128]`; length is then obtained from the selected source buffer through the inlined source-level `wcslen` operation. There is no default branch. Adding a fallback string, emptying the buffer, or returning early for an out-of-range value would change the binary's invalid-state behavior.

After label selection the method calls `SetTextColor(128)`, which stores the active text color at GrafPort `+0x7c`. The integer coordinates are `(left + right) / 2 - (6 * textLength) / 2 - 2` and `bottom - 6`. The method draws first at `(textX + 1,textY)` and then at `(textX,textY)` without changing draw state between passes. This is a same-state one-pixel double-strike/overdraw, not a separately colored shadow/foreground sequence.

Mode labels observed in IDA:

- `0`: `off_615364`, wide string `Talk`.
- `1`: `aSh`, wide string `Shout`.
- `2`: `aWh`, wide string `Whisper`.
- `3`: `Group`.
- `4`: `Clan`.

The selected label is horizontally centered with the exact integer expression documented above. The source body belongs in `social/Chatting.cpp`; shared GrafPort, ImageLib, EPFTileContext, UserPane, and selector-global definitions remain in their established owners and are consumed rather than duplicated here.

## Exact Call And State Sequence

1. Default-construct stack `EPFTileContext tileContext`; its constructor performs the observed initialization.
2. Call `SetTextBackFillColor(0)`; no draw-mode field is written.
3. Resolve `CHATVAR.EPF` frame `0` or `1` according to whether `g_pChattingVarietySelectPane` is null.
4. Render the selected frame through the shared Surface path using `CHATVAR.PAL`, target bounds, context bounds, zero draw argument, and null final pointer.
5. Read `g_pUserPane->m_currentSayMode`; bounded-copy exactly one of the five source literals into `modeText[128]`, with no default branch.
6. Compute the source-level `wcslen`, call `SetTextColor(128)`, and calculate exact centered integer coordinates.
7. Draw the same text state at `x+1` and then `x`.
8. Return without release, destructor, allocation cleanup, or EH cleanup for the borrowed tile context.

## Stack And Layout Evidence

- Dated IDA frame: target receiver row at `+0x0c`; `EPFTileContext` at `+0x10`, size `0x28`; 128-wide-character destination beginning at `+0x38` and extending to `+0x138`; cookie/local tail at `+0x138`; saved registers at `+0x13c`; return address at `+0x140`.
- GrafPort is `0xa0` bytes. Relevant fields are `m_visibleBounds +0x44`, current Y/X at `+0x68/+0x6c`, `m_drawMode +0x70`, `m_drawColor +0x74`, `m_textBackFillColor +0x78`, `m_textColor +0x7c`, and `m_textBackColor +0x80`.
- UserPane is `0x13eb84` bytes; `m_currentSayMode` is an `int` at `+0x3eb4`.
- EPFTileContext is `0x28` bytes and its accepted declaration/initializer are already owned by the EPFTileContext pages.

## Reachability And Dependency Direction

- The target has zero ordinary code callers and exactly one inbound data reference, primary-vtable slot `0x00614ee4 -> 0x00480e80`. Vtable-only reachability is expected for this override and is not dead-code evidence.
- Shared helper `0x004b9670` has 11 code refs at `0x467be6`, `0x467e26`, `0x467ec4`, `0x480ead`, `0x48216f`, `0x4b1c7b`, `0x550e87`, `0x550fe7`, `0x57c1a5`, `0x585bf5`, and `0x5bc820`; broad fan-in rejects Chatting ownership.
- `g_pChattingVarietySelectPane` has seven exact references at `0x480d7f`, `0x480dc2`, `0x480dc9`, `0x480eb2`, `0x481115`, `0x48119d`, and `0x483e04`. This target only reads the singleton to select the frame.
- `g_pEPFLib` and `g_pUserPane` are broad shared globals with 231 and 505 dated refs respectively. Their dependency direction cannot move this method out of Chatting.

## Resources And Literal Policy

- The protected resource/label band `[0x00615334,0x006153a8)` is 116 bytes with SHA256 `D56314BDC56B2F809CCF789D01EFFDB75D138D1CA10B8F2804545F0F56B811ED` and contains `CHATVAR.EPF`, `CHATVAR.PAL`, `Talk`, `Shout`, `Whisper`, `Group`, and `Clan`.
- These data items are source literals used by this method, not separate emitted data objects. The formal C++ therefore uses readable wide literals and does not emit `off_`, `aSh`, or raw data-label objects.

## Ownership, Source Placement, And Rejected Alternatives

- Canonical owner/emitter remains [UID:00001Y][ChattingVarietyPane](by-class/ChattingVarietyPane.md), with source route [UID:0000I5][Chatting](by-file/Chatting.md) / `NexusTK/social/Chatting.cpp`.
- GrafPort owns the shared state setters and cursor/text helpers; ImageLib owns the resource singleton/API; UserPane owns the selected-mode field; EPFTileContext owns its layout/initializer; ChattingVarietySelectPane owns only popup state. None owns this vtable slot or body.
- A free helper, a new one-method source file, a GrafPort/ImageLib/UserPane owner move, no canonical owner, compiler-only classification, covered-by marker, split, range expansion, or third-party import is rejected.
- Formal CPP contains only this method. Formal H remains blank because the parent class already declares the override and UID0002RX already emits `SayInputMode` in `SayInputPanes.h`.

## Raw Code Evidence

```asm
00480EA4  call    sub_457A60
00480EAD  call    sub_4B9670
00480ED6  call    sub_4D02F0 ; "CHATVAR.EPF", dword_67ADDC != 0
00480EF5  call    sub_4B9980 ; palette "CHATVAR.PAL"
00480EFF  mov     eax, [dword_67A748+3EB4h]
00480F1A  wcscpy_s(..., aSh)
00480F26  wcscpy_s(..., aWh)
00480F32  wcscpy_s(..., L"Group")
00480F4A  wcscpy_s(..., L"Clan")
00480F52  call    wcslen
00480FA8  call    sub_4B9600
00480FBB  call    sub_4BAB70
00480FC8  call    sub_4B9600
00480FDB  call    sub_4BAB70
```

IDA MCP evidence from 2026-05-31:

- `lookup_funcs 0x00480e80` reports `sub_480E80`, size `0x171`.
- `xrefs_to 0x00480e80` reports a vtable data xref at `0x00614ee4`.
- Decompilation resolves the render asset strings and mode label strings listed above.

Live IDA MCP evidence from 2026-06-05:

- Input MD5 `4247e04e20b65d6414c7238aa8ff5515`.
- `lookup_funcs 0x00480e80` reconfirms `sub_480E80`, exact half-open range `0x00480e80-0x00480ff1`, size `0x171`. The next `0x00480ff1-0x00481000` bytes are fifteen `0xcc` alignment bytes before [UID:0002FE][0x00481000-0x0048100a.ChattingVarietyPaneRefreshForwarder](by-memory/0x00481000-0x0048100a.ChattingVarietyPaneRefreshForwarder.md).
- `xrefs_to 0x00480e80` still reports only the vtable data xref at `0x00614ee4`; nearby slots include `0x00614ee8 -> sub_481000` forwarder and `0x00614ef4 -> sub_480CD0` mouse handler.
- Hex-Rays decompilation confirms the temporary tile-context setup, `CHATVAR.EPF` lookup with `dword_67ADDC != 0` as the frame/state argument, `CHATVAR.PAL` rendering, mode read at `dword_67A748 + 0x3eb4`, label selection, background-color setter `sub_4B9680(this, 128)`, and two `MoveTo`/`DrawWideText` passes at `x+1` and `x`.
- Data probes resolve the referenced wide strings as `CHATVAR.EPF`, `CHATVAR.PAL`, `Talk`, `Shout`, `Whisper`, `Group`, and `Clan`.
- `xrefs_to 0x0067addc` ties this paint path's popup/sibling-state read at `0x00480eb2` to [UID:0002B5][0x0067adc4-0x0067adec.UiChatClanSingletonSlots](by-memory/0x0067adc4-0x0067adec.UiChatClanSingletonSlots.md) `g_pChattingVarietySelectPane` lifecycle refs from the mouse handler, selector constructor body, selector destructor, and destructor glue.

## Open Questions

- No behavior, ownership, range, helper, global, type, shared-enum placement, or formal-C++ blocker remains.
- Original lexical spellings are not symbol-proven. Current project-wide names are evidence-ranked source inferences and cap the score below `95/95`; they do not justify raw IDA labels or an empty emitter.
- Future compile verification must confirm `SayInputPanes.h` is visible to `Chatting.cpp`, exactly one `SayInputMode` definition is emitted there, no duplicate enum or target H declaration is emitted by this page, and exactly one target method body appears.

## Historical / Superseded Assumptions

- The 2026-06-17 interpretation of `0x004b9670` as a draw/display-mode setter is superseded by exact helper bytes and current GrafPort layout: it stores zero to `m_textBackFillColor` at `+0x78`; `m_drawMode` remains at `+0x70` and is not written here.
- The historical description of `sub_4B9680(this,128)` as a background-color setter is superseded: it is source-facing `SetTextColor(128)` over `m_textColor +0x7c`.
- The historical shadow/foreground wording is superseded because no state change separates the two draws. The exact effect is same-state one-pixel double-strike/overdraw.
- Historical `currentChatMode`/`currentChatVarietyMode` prose is superseded by the accepted `UserPane::m_currentSayMode` field at `+0x3eb4` and the shared UID0002RX `SayInputMode` declaration.
- The old blank-C++ blocker is superseded. GrafPort, EPFTileContext, UserPane, ImageLib/ResourceLayoutTable compatibility API, selector singleton, and shared enum now have accepted source-quality routes. The earlier target-local enum draft is retained only in the B006 report as corrected placement history and is not emitted here.

## Parent Attachment

The canonical owner/emitter is [UID:00001Y][ChattingVarietyPane](by-class/ChattingVarietyPane.md). This child is now `94/94`; the class parent is `90/93`, and the established file route is [UID:0000I5][Chatting](by-file/Chatting.md) at `89/93`. No route or attachment change is required.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:00001Y][ChattingVarietyPane](by-class/ChattingVarietyPane.md)
- [UID:00001Z][ChattingVarietySelectPane](by-class/ChattingVarietySelectPane.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md)
- [UID:0002KP][0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry](by-memory/0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry.md)
- [UID:000165][0x004b9820-0x004ba24a.SurfacePaintHelpers](by-memory/0x004b9820-0x004ba24a.SurfacePaintHelpers.md)
- [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md)
- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [UID:0002B5][0x0067adc4-0x0067adec.UiChatClanSingletonSlots](by-memory/0x0067adc4-0x0067adec.UiChatClanSingletonSlots.md)

## Changes

- 2026-08-05 B006 source-quality implementation callback:
  - Raised completion/confidence from `86/91` to `94/94`; owner `00001Y`, reconstructable state, and emitter `00001Y` remain unchanged.
  - Added the exact method-only formal CPP and kept formal H blank because the parent class owns the override declaration and UID0002RX owns the shared `SayInputMode` enum in `SayInputPanes.h`.
  - Incorporated exact body/hash/fence, vtable-only reachability, stack/layout, borrowed-context lifetime, resource/frame arguments, UserPane field and five-mode invariant, bounded copies, source `wcslen`, correct GrafPort state order, integer coordinates, same-state double-strike, literals, ownership, source placement, and rejected alternatives.
  - Historicalized the old display-mode, background-color, separate shadow/foreground, raw mode-field, and blank-C++ assumptions rather than deleting their provenance.
  - Third-party import is not applicable; this is NexusTK project method source.
- 2026-05-31: Created exact paint-method page and set completion/confidence to `78/86`. Evidence is IDA MCP function boundary, decompilation, vtable xref, render strings, mode labels, and padding audit.
- 2026-06-17 B003 method-family source-quality pass:
  - Raised completion/confidence from `82/90` to `86/91`.
  - Evidence: B003 rechecked current target/support docs and IDA facts, resolved the source-facing method as `ChattingVarietyPane::OnPaint`, identified `dword_67A748+0x3eb4` as `currentChatMode` / `currentChatVarietyMode`, rejected generated `Initialize`, confirmed `CHATVAR.EPF`/`CHATVAR.PAL` rendering, popup-state frame selection, Talk/Shout/Whisper/Group/Clan labels, shared GrafPort draw/display-mode/background/move/text dependencies, and centered shadow/foreground label placement.
  - Formal C++ remains blank because EPFTileContext, ResourceLayoutTable, Surface/GrafPort helper declarations, and the `currentChatMode` field declaration are not source-final enough for method-body emission.
- 2026-06-05: Raised completion/confidence to `82/90` and attached [UID:00001Y][ChattingVarietyPane](by-class/ChattingVarietyPane.md). Live IDA reconfirmed the exact body, vtable-only dispatch slot, render-resource path, all five mode labels including default `Talk`, text centering/draw helper calls, `g_pChattingVarietySelectPane` read, and trailing padding; final C++ remains blank because field names and helper names are still below the final-code gate.
