*** UID:00016A | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00005V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00005V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void GrafPort::DrawTiledBackground(const RectBounds *clipRect,
                                   const FrameDrawRecord *frameRecord,
                                   const wchar_t *paletteName)
{
    Region savedClipRegion;
    savedClipRegion.CopyRectFrom(m_clipRegion);
    m_clipRegion.SetRect(clipRect);

    const RectBounds *sourceRect = &frameRecord->sourceRect;
    const int tileWidth = sourceRect->right - sourceRect->left;
    const int tileHeight = sourceRect->bottom - sourceRect->top;

    if (tileWidth > 0 && tileHeight > 0) {
        const int rowCount =
            (m_visibleBounds.bottom - m_visibleBounds.top - 1) / tileHeight + 1;
        const int columnCount =
            (m_visibleBounds.right - m_visibleBounds.left - 1) / tileWidth + 1;

        RectBounds rowRect = m_visibleBounds;
        rowRect.right = rowRect.left + tileWidth;
        rowRect.bottom = rowRect.top + tileHeight;

        for (int row = 0; row < rowCount; ++row) {
            RectBounds destinationRect = rowRect;

            for (int column = 0; column < columnCount; ++column) {
                DLPalette *palette = g_pPaletteLib->GetPaletteByName(paletteName);

                g_pfnBlitSprite(this,
                    reinterpret_cast<const EPFTileContext *>(frameRecord),
                                sourceRect,
                                &destinationRect,
                                m_drawMode,
                                palette,
                                0);

                OffsetRect(&destinationRect, tileWidth, 0);
            }

            OffsetRect(&rowRect, 0, tileHeight);
        }

        m_clipRegion.CopyRectFrom(savedClipRegion);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA MCP recheck confirms exact boundary, 26 direct callers, GrafPort clip/visible-state usage, palette lookup, region save/restore, and tiled dispatch through `dword_69B3E8`; final public method name remains provisional. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ba6b0-0x004ba81d GrafPort DrawTiledBackground

## Status

- Confidence: very strong for the exact function boundary, `thiscall` GrafPort receiver, source-facing helper role, callback-table route, field meanings, and first-draft C++ readiness; remaining uncertainty is limited to final original spellings such as `FrameDrawRecord` versus `TileFrameDrawRecord` and `m_drawMode` versus `m_blitMode`.
- Current generated owner: [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md), emitted as `RankingEventListPane::DrawTiledBackground`; this is caller-biased generated-output pollution.
- Reconstruction owner: [UID:00005V][GrafPort](by-class/GrafPort.md) child in shared [UID:0000JR][GrafPort](by-file/GrafPort.md), with [UID:0000OC][Surface](by-file/Surface.md) kept as callback-table and render-backend dependency context.
- IDA function: `sub_4BA6B0`, size `0x16d`.

## Function

| Range | Current/proposed role | Notes |
| --- | --- | --- |
| `0x004ba6b0-0x004ba81d` | `GrafPort::DrawTiledBackground` | Temporarily replaces `m_clipRegion`, computes rows and columns from a `FrameDrawRecord` source rectangle and `m_visibleBounds`, resolves the named palette through `g_pPaletteLib`, and repeatedly blits the source tile through callback slot `dword_69B3E8`. |

## Signature And Behavior

- Source-facing signature: `void GrafPort::DrawTiledBackground(const RectBounds *clipRect, const FrameDrawRecord *frameRecord, const wchar_t *paletteName)`.
- The helper constructs a temporary saved `Region`, copies `this + 0x54` / `m_clipRegion` into it, applies the caller clip rectangle to `m_clipRegion`, and restores the saved region on the positive-dimension path before stack cleanup. Earlier `this + 0x84` wording was stale.
- `frameRecord + 0x10..0x1f` provides the source tile rectangle; the width is `sourceRect.right - sourceRect.left` and the height is `sourceRect.bottom - sourceRect.top`. `FrameDrawRecord` is the strongest current type name because [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md) produces this shape and [UID:0001GF][0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect](by-memory/0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect.md) already uses it as a caller-side record.
- Non-positive tile dimensions skip the repeated-blit loops and branch directly to local Region cleanup at `0x004ba7f8`; the explicit restore call at `0x004ba7ec-0x004ba7f3` is reached only after the positive tile-dimension block. Valid callers appear to provide positive dimensions, but the first-draft C++ preserves the observed restore placement.
- Visible GrafPort bounds at `this + 0x44..0x50` / `m_visibleBounds` determine the column and row counts. The destination rectangle starts from the visible origin, advances by tile width across each row via `OffsetRect(&destinationRect, tileWidth, 0)`, then advances by tile height for the next row via `OffsetRect(&rowRect, 0, tileHeight)`.
- Each tile blit reads the GrafPort byte at `this + 0x70` (`m_drawMode` / possible `m_blitMode`), resolves the palette name through `g_pPaletteLib->GetPaletteByName(paletteName)`, and calls [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) entry `dword_69B3E8` with `this`, the whole frame record, the source rect, the current destination rect, the GrafPort mode byte, the palette pointer, and zero flags/options. `this + 0x90` (`m_alpha`) is not read directly in this body but is receiver-state consumed by the slot-2 callback, as proven by the WaterFilter caller that sets and restores alpha around this call.

## 2026-06-06 IDA MCP Recheck

- IDB evidence source: `NexusTK.exe`, md5 `4247e04e20b65d6414c7238aa8ff5515`.
- `lookup_funcs` reports `sub_4BA6B0` at `0x004ba6b0` with size `0x16d`, making the half-open body `0x004ba6b0-0x004ba81d`; `0x004ba81d` is not a function and the next modeled function is `sub_4BA820` at `0x004ba820`.
- `callers` reports 26 direct call sites: `0x0045b32e`, `0x0045e017`, `0x0045f429`, `0x0046b09b`, `0x0046c418`, `0x0048172d`, `0x004838d0`, `0x00496d24`, `0x00496e54`, `0x00497132`, `0x00497262`, `0x00497443`, `0x0049838b`, `0x0049f2bf`, `0x00500e04`, `0x00516831`, `0x00545dac`, `0x0055534d`, `0x00555ccd`, `0x0055a613`, `0x0055b673`, `0x0055d3ad`, `0x0055d657`, `0x0055d6b5`, `0x0055d759`, and `0x005bd60d`.
- `callees` reports palette lookup `0x00543d70`, render callback table slot `0x0069b3e8`, region helpers `0x00554680`, `0x00554760`, `0x00554720`, rectangle offset helper `0x004b7e10`, cleanup helper `0x004f4a90`, and the security-cookie check.
- `xrefs_to 0x0069b3e8` includes the use at `0x004ba7b6` inside this helper, plus many other render paths, confirming that this function participates in the shared surface-render dispatch table rather than feature-specific ranking code.

## Evidence Notes

- IDA MCP lookup on 2026-05-25 reports `sub_4BA6B0` size `0x16d`.
- IDA MCP callers report 26 direct call sites, including ranking panes, web-board/dialog panes, button/control draw paths, alert/blue-pane content, screen/map UI, and other shared UI render paths. This breadth rules out ranking-specific ownership.
- Callees include Region/clip helpers at `0x00554680`, `0x00554760`, and `0x00554720`, palette lookup at `0x00543d70`, rectangle offset helper `0x004b7e10`, cleanup at `0x004f4a90`, and callback slot `dword_69B3E8` from [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md).
- IDA decompilation and current `simroot_v2/class_RankingEventListPane.cpp` both show tile dimensions coming from fields at `tileData + 0x10..0x1c`, with row/column counts computed from visible bounds at `this + 0x44..0x50`.
- The helper saves the previous region state, applies the caller clip rectangle, performs tiled blits, and restores the saved region state before cleanup.
- 2026-06-17 B002 WaterFilterEffecter support sync: [UID:0001GF][0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect](by-memory/0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect.md) is a caller that sets GrafPort byte `+0x70` to render mode `2`, copies its caustic alpha source into GrafPort alpha `+0x90`, calls this helper with `L"CAUST.PAL"`, then restores draw mode `0` and the saved alpha. This strengthens the `+0x70` draw/blit mode interpretation.

## Ownership Notes

Keep this helper out of `RankingDialog.cpp`. Ranking panes use it for `WBOARDBK` background drawing, but the helper is a generic tiled-background primitive with broad UI fan-in and no ranking-specific state.

The source route is now `GrafPort.cpp` / [UID:00005V][GrafPort](by-class/GrafPort.md). Surface remains dependency context because the final transfer dispatches through `dword_69B3E8`, but the wrapper itself saves/replaces GrafPort clip state, reads GrafPort visible bounds, forwards GrafPort draw/alpha state, and receives `GrafPort *this` in `ecx`. Caller-owned, Surface-free-function, EPF-only, and raw-descriptor interpretations are rejected.

## 2026-06-18 B002 Source-Quality Reanalysis

B002's report confirms this range as a single source-bearing `GrafPort::DrawTiledBackground` method. Raw PE checks found exact padding around the body: `0x004ba6ad-0x004ba6b0` is three `0xcc` bytes before the function and `0x004ba81d-0x004ba820` is three `0xcc` bytes after the `ret 0x0c`. The function keeps `ecx` as the destination `GrafPort *this`, takes three explicit stack arguments, and directly calls the Region constructor/copy/set helpers, `OffsetRect`, `PaletteLib::GetPaletteByName`, and callback slot `dword_69B3E8`.

The direct caller scan found 26 rel32 calls: `0x0045b32e`, `0x0045e017`, `0x0045f429`, `0x0046b09b`, `0x0046c418`, `0x0048172d`, `0x004838d0`, `0x00496d24`, `0x00496e54`, `0x00497132`, `0x00497262`, `0x00497443`, `0x0049838b`, `0x0049f2bf`, `0x00500e04`, `0x00516831`, `0x00545dac`, `0x0055534d`, `0x00555ccd`, `0x0055a613`, `0x0055b673`, `0x0055d3ad`, `0x0055d657`, `0x0055d6b5`, `0x0055d759`, and `0x005bd60d`. Mapped callers span ranking dialogs, browser dialog/control panes, chatting UI, button/choice controls, popup/dialog panes, blue alert panes, menu panes, editable paper/right-button/vote panes, ScreenEffecter/WaterFilter effect code, ScrollPane draw code, and menu variety panes. This fan-in rejects `RankingEventListPane` ownership.

Accepted helper/type/field names:

- `FrameDrawRecord` is the best current source-facing source-descriptor name; `TileFrameDrawRecord` remains possible original spelling, while raw `uint8_t *`, generated `TileData_004ba6b0`, and EPF-only names are rejected.
- `this + 0x44..0x50` is `m_visibleBounds` / `m_visibleRect`.
- `this + 0x54` is `m_clipRegion` / `m_clipRectRegion`.
- `this + 0x70` is `m_drawMode` / possible `m_blitMode`.
- `this + 0x90` is `m_alpha`, a slot-2 callback-visible receiver dependency proven by [UID:0001GF][0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect](by-memory/0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect.md).
- `0x00554680`, `0x00554760`, and `0x00554720` are Region/Motion constructor/copy/set helpers; `0x004b7e10` is `OffsetRect(RectBounds *, int, int)`; `0x004f4a90` is local Region/LObject cleanup.
- `dword_69B3E8` is best described as `SurfaceSpriteBlitProc` / `g_pfnBlitSprite`, receiving destination `GrafPort`, frame/tile source descriptor, source rectangle, destination rectangle, mode byte, palette pointer, and zero flags/options.

Rejected alternatives:

- `Surface::DrawTiledBackground` or a file-local `Surface.cpp` helper: weaker than the concrete GrafPort receiver and state mutation.
- `RankingEventListPane::DrawTiledBackground`: one caller family among many and generated owner pollution.
- `EPFTileContext *` or `TileData_004ba6b0`: too narrow or generated-only compared with `LoadFrameDrawRecord` and WaterFilter evidence.
- Hoisting palette lookup or moving the clip restore out of the positive-dimension block in first-draft C++: possible source cleanup, but not the observed binary behavior.

The remaining original-spelling uncertainties cap the target below final-audit scores but do not block first-draft code. The formal C++ block intentionally uses descriptive source-facing names and preserves the observed control flow.

## Cross-References

- [UID:0000JR][GrafPort](by-file/GrafPort.md)
- [UID:00005V][GrafPort](by-class/GrafPort.md)
- [UID:0000OC][Surface](by-file/Surface.md)
- [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md)
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md)
- [UID:000167][0x004ba250-0x004ba444.SurfaceSpriteBlitHelper](by-memory/0x004ba250-0x004ba444.SurfaceSpriteBlitHelper.md)
- [UID:000168][0x004ba450-0x004ba53b.GrafPortDrawRectFrame](by-memory/0x004ba450-0x004ba53b.GrafPortDrawRectFrame.md)
- [UID:000169][0x004ba540-0x004ba6ad.CompositePixels16](by-memory/0x004ba540-0x004ba6ad.CompositePixels16.md)
- [UID:00016B][0x004ba820-0x004ba991.TextFitAndSuffixHelpers](by-memory/0x004ba820-0x004ba991.TextFitAndSuffixHelpers.md)

## Changes

- 2026-06-18 B002 source-quality execution:
  - Before: `COMPLETION:84`, `CONFIDENCE:90`, owner/emitter [UID:00005V][GrafPort](by-class/GrafPort.md), blank formal C++, and stale `this+0x84` clip-region wording.
  - After: `COMPLETION:88`, `CONFIDENCE:91`, owner/emitter unchanged through [UID:00005V][GrafPort](by-class/GrafPort.md), with first-draft `GrafPort::DrawTiledBackground(const RectBounds *, const FrameDrawRecord *, const wchar_t *)` C++ populated.
  - Evidence: incorporated B002's report-level boundary, caller, callee, field, type, callback, source-placement, rejected-alternative, coverage, and code-readiness details. The page now records `this+0x54` as `m_clipRegion`, `this+0x44..+0x50` as visible bounds, `this+0x70` as draw/blit mode, `this+0x90` as callback-visible alpha, `FrameDrawRecord+0x10` as the source tile rectangle, `g_pPaletteLib->GetPaletteByName`, `dword_69B3E8` as the slot-2 sprite/tile blit callback, exact 26 direct callers, and the non-positive-tile-dimension restore caveat.
- 2026-06-06: Raised completion/confidence from `70/85` to `84/90` and attached the helper to [UID:00005V][GrafPort](by-class/GrafPort.md). Evidence: live IDA MCP boundary, decompile, callers, callees, and render-callback xref checks confirm a `thiscall` GrafPort tiled-background helper with broad UI/render fan-in; final method name and file split remain provisional, so final C++ remains blank.
- 2026-06-17 B002 WaterFilterEffecter support sync:
  - Score unchanged.
  - Summary/evidence: added [UID:0001GF][0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect](by-memory/0x0055b5d0-0x0055b698.WaterFilterEffecterUpdateCausticEffect.md) as caller evidence for byte `+0x70` as a draw/blit mode forwarded by this helper and for `L"CAUST.PAL"` tiled caustic drawing.
