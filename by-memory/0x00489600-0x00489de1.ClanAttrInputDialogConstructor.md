*** UID:0004HH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000024 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000024 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ClanAttrInputDialog::ClanAttrInputDialog(const unsigned char *packet)
    : DialogPane(L"", -1, true)
{
    char textBytes[256];
    wchar_t headingText[256];
    wchar_t clanCaptionText[256];
    wchar_t descriptionPromptText[256];
    wchar_t attributePromptText[256];
    wchar_t attributeTypePromptText[256];

    const unsigned char *textPacket = packet + 3;
    unsigned int textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textPacket += textLength;
    textBytes[textLength] = '\0';
    unsigned char wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, m_submittedClanName, 256));
    m_submittedClanName[wideLength] = L'\0';

    textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textPacket += textLength;
    textBytes[textLength] = '\0';
    wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, headingText, 256));
    headingText[wideLength] = L'\0';

    textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textPacket += textLength;
    textBytes[textLength] = '\0';
    wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, clanCaptionText, 256));
    clanCaptionText[wideLength] = L'\0';

    textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textPacket += textLength;
    textBytes[textLength] = '\0';
    wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, descriptionPromptText, 256));
    descriptionPromptText[wideLength] = L'\0';

    textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textPacket += textLength;
    textBytes[textLength] = '\0';
    wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, attributePromptText, 256));
    attributePromptText[wideLength] = L'\0';

    textLength = *textPacket++;
    memmove(textBytes, textPacket, textLength);
    textBytes[textLength] = '\0';
    wideLength = static_cast<unsigned char>(MultiByteToWideChar(
        CP_ACP, 0, textBytes, textLength, attributeTypePromptText, 256));
    attributeTypePromptText[wideLength] = L'\0';

    AddControl(new EPFImageControlPane(
        L"DLGCLAN2.EPF", Rect(0, 0, 259, 288), 0, true));
    AddControl(new ImageButtonControlPane(14, Rect(53, 228, 125, 244)));
    AddControl(new ImageButtonControlPane(22, Rect(130, 228, 202, 244)));
    AddControl(new StaticTextControlPane(
        headingText, true, 128, Rect(41, 20, 231, 45)));
    AddControl(new StaticTextControlPane(
        clanCaptionText, true, 128, Rect(41, 49, 231, 74)));
    AddControl(new StaticTextControlPane(
        descriptionPromptText, true, 128, Rect(41, 66, 231, 91)));
    AddControl(new StaticTextControlPane(
        attributePromptText, true, 128, Rect(41, 116, 231, 141)));
    AddControl(new StaticTextControlPane(
        attributeTypePromptText, true, 128, Rect(41, 166, 231, 191)));
    AddControl(new TextEditControlPane(
        L"", true, 128, Rect(41, 91, 218, 104)));
    AddControl(new TextEditControlPane(
        L"", true, 128, Rect(41, 141, 218, 154)));
    AddControl(new TextEditControlPane(
        L"", true, 128, Rect(41, 191, 218, 204)));

    SetHoverControl(8);
    SetFocusedControl(1);
    SetPendingControl(2);
    SetBackgroundResource(L"DLGCLAN2.EPF", NULL);
    SetMode(1);

    RectBounds dialogBounds;
    InitRectBounds(&dialogBounds, 174, 12, 433, 300);
    OnCreate(
        &dialogBounds,
        0,
        NULL,
        g_mainUiLayerSlots.overlayPaneLayerContext);
    OnShow(NULL, g_pBackPane);
    SlideOpenHorizontal(0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-emitting ClanAttrInputDialog constructor at 0x00489600-0x00489de1; current MCP proves the single-packet-pointer ABI, six counted CP_ACP strings, 0x870 layout, eleven DLGCLAN2 controls, accepted dialog helpers, three modeled inline derived callers plus two raw constructor calls, exact padding, Clan.cpp route, and compiler-artifact exclusions. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00489600-0x00489de1 ClanAttrInputDialogConstructor

## 2026-08-14 Current IDA And Source Route

- Formal constructor source remains on this exact child under UID000024/UID0000I8; UID0000IA is a non-emitting index, not a possible future constructor destination.
- Supervisor I08 prestate is `0x00489600-0x00489de1`, size `0x7e1`, SHA256 `697762FD5F094D1C9BAFF4F0C71F181847BF4275FA2FF85E45E66407EF76C3BE`, name `sub_489600`, type `int __thiscall(int this, int)`, blank comments, callsites `0x4852e9,0x487f2f,0x4883e8,0x489f8d,0x489ffd`, and the complete protected FI08 frame including unexplained `arg_4`. Expected safe IDA readback is `ClanAttrInputDialog__ClanAttrInputDialog`, source-quality constructor type with `const unsigned char *packet`, constructor FR, and only the `arg_0 -> packet` frame delta; `arg_4` and all other rows remain protected. B007 did not mutate IDA.
- The exact six counted strings, first persisted field at `+0x26c`, eleven DLGCLAN2 controls, `0x870` derived allocation, overlay/back-pane ordering, and no constructor `g_pClanStatusPane` dependency remain unchanged.

## Status

- Disposition: reconstructable, source-emitting NexusTK project constructor with formal first-draft C++.
- Entity kind: `ClanAttrInputDialog` constructor.
- Owner/emitter: [UID:000024][ClanAttrInputDialog](by-class/ClanAttrInputDialog.md).
- Source route: [UID:0000I8][Clan](by-file/Clan.md) / `NexusTK/social/Clan.cpp`.
- Parent split index: [UID:00010L][0x00489600-0x00489f80.ClanAttrInputDialogCore](by-memory/0x00489600-0x00489f80.ClanAttrInputDialogCore.md).
- ABI: source constructor `ClanAttrInputDialog::ClanAttrInputDialog(const unsigned char *packet)` with implicit `this`; binary `retn 4` cleans the one explicit packet argument and the implicit `this` return is compiler ABI output.
- Confidence: very strong for exact range, ABI, packet contract, class/control layout, resource/helper use, xrefs, and owner/source route. Descriptive local/member names remain below original-symbol proof, and the packet-provided label literals are unavailable by design rather than a code blocker.

## Covered Range

| Range | Function status | Notes |
| --- | --- | --- |
| `0x00489600-0x00489de1` | IDA function `sub_489600`, size `0x7e1` / 2017 bytes (Verified with int_convert.py) | Constructor body for the DLGCLAN2 base clan attribute dialog. The following `0x00489de1-0x00489df0` bytes are `0xcc` alignment before the action handler. |

The boundary dispositions remain exact: predecessor `0x004895fe-0x00489600` is two bytes of excluded `0xcc` alignment, and successor `0x00489de1-0x00489df0` is fifteen bytes of excluded `0xcc` alignment before [UID:0004HI][0x00489df0-0x00489eb7.ClanAttrInputDialogOnControlCommand](by-memory/0x00489df0-0x00489eb7.ClanAttrInputDialogOnControlCommand.md). Do not expand, split, or reclassify this child.

## Behavior

- Calls the dialog base constructor with an empty wide string, then stores the primary, secondary, and tertiary `ClanAttrInputDialog` vtable views at `this+0x000`, `this+0x0a0`, and `this+0x0a4`.
- Decodes six sequential `{ unsigned-byte length, bytes[length] }` packet strings beginning with the first length byte at `packet+3`. Each string is copied to a 256-byte local buffer, locally terminated, converted with `MultiByteToWideChar(CP_ACP, 0, ..., explicitLength, ..., 256)`, and wide-terminated at the returned length cast to `unsigned char`.
- Persists the first converted wide string at `this+0x26c`, the accepted descriptive source field `m_submittedClanName`.
- Uses packet strings two through six for five visible static labels. Their literal text is supplied by the server packet rather than embedded resources, so descriptive locals based on order/geometry are appropriate and missing literals do not block reconstruction.
- The constructor allocates no class-specific child pointers. It builds eleven controls in insertion order, making background id `0`, OK id `1`, Cancel id `2`, labels `3`-`7`, and required text edits `8`-`10`.
- It selects hover control `8`, focused/default action `1`, pending/cancel action `2`, background `DLGCLAN2.EPF`, pane mode `1`, dialog bounds `(174,12,433,300)`, overlay layer attachment, `g_pBackPane` show ordering, and horizontal slide direction `0`.

## Control Inventory

| Logical id | Control / source | Rect `(left,top,right,bottom)` |
| --- | --- | --- |
| `0` | `EPFImageControlPane`, `DLGCLAN2.EPF` | `(0,0,259,288)` |
| `1` | `ImageButtonControlPane`, image index `14` / OK | `(53,228,125,244)` |
| `2` | `ImageButtonControlPane`, image index `22` / Cancel | `(130,228,202,244)` |
| `3` | packet string 2 heading/prompt | `(41,20,231,45)` |
| `4` | packet string 3 clan/context caption | `(41,49,231,74)` |
| `5` | packet string 4 description prompt | `(41,66,231,91)` |
| `6` | packet string 5 attribute prompt | `(41,116,231,141)` |
| `7` | packet string 6 attribute-type prompt | `(41,166,231,191)` |
| `8` | empty `TextEditControlPane` | `(41,91,218,104)` |
| `9` | empty `TextEditControlPane` | `(41,141,218,154)` |
| `10` | empty `TextEditControlPane` | `(41,191,218,204)` |

Image indices `14` and `22` are constructor resource indices, not logical control ids. Later action/validation methods independently prove OK/Cancel ids `1`/`2` and required edit ids `8`/`9`/`10`.

## IDA MCP Evidence

- 2026-07-12 B003 used active MCP session `359c7886` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported module `NexusTK.exe`, image base `0x00400000`, analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs(0x00489600)` returned `sub_489600`, size `0x7e1`; `0x00489de1` is not a function start.
- Boundary bytes reconfirmed two predecessor `0xcc` bytes and fifteen successor `0xcc` bytes; the constructor ends in `retn 4` at exclusive end `0x00489de1` and action child `sub_489DF0` begins at `0x00489df0`.
- Full decompile reconfirmed base construction, vtable stores at `+0`, `+0xa0`, and `+0xa4`, all six packet decodes, first string persistence at `this+0x26c`, exact control construction, and the modal setup tail.
- `xrefs_to(0x00489600)` returned modeled constructor callers at `0x004852e9`, `0x00487f2f`, and `0x004883e8`, plus raw derived-constructor calls at `0x00489f8d` and `0x00489ffd`.
- Vtable base refs corroborate constructor stores to `0x00615d58`, `0x00615dbc`, and `0x00615dec`. `DLGCLAN2.EPF` refs occur at `0x00489914` and `0x00489d5c`.
- The three modeled callers allocate `0x870` bytes, call this constructor, then install `ClanEnlistInputDialog` or `ClanChangeInputDialog` vtables. Raw starts `0x00489f80` and `0x00489ff0` contain equivalent 53-byte derived constructor bodies, but current xref and exact-address pointer searches find no inbound route to either raw start.
- Current type recovery has no `ClanAttrInputDialog` or `TextEditControlPane` UDT. That limits original-name certainty but does not block formal C++ because accepted class/control support supplies the declaration shapes.

## Helper And Route Resolution

- Accepted support maps the old raw calls to `DialogPane(L"", -1, true)`, control constructors, `AddControl`, `SetHoverControl`, `SetFocusedControl`, `SetPendingControl`, `SetBackgroundResource`, `Pane::SetMode`, `InitRectBounds`, `OnCreate`, `OnShow`, and `SlideOpenHorizontal`.
- `OnCreate` receives `g_mainUiLayerSlots.overlayPaneLayerContext`; `OnShow` receives `NULL` and `g_pBackPane`. The constructor does not read or write `g_pClanStatusPane` or a dedicated dialog singleton. `g_pClanStatusPane` is a later input-event/submit dependency.
- All modeled live uses create a derived Enlist/Change object. The base constructor remains source-live because derived source construction requires it even when compiler output inlines or duplicates the base sequence at opener sites.
- Keep class owner/emitter [UID:000024][ClanAttrInputDialog](by-class/ClanAttrInputDialog.md) and source route [UID:0000I8][Clan](by-file/Clan.md). UID0000IA is a non-emitting index, not a constructor destination.

## Reconstruction Notes

The old no-code blocker is superseded. Current MCP plus accepted framework support resolves every helper needed by the formal body, while packet-provided label literals are not executable resources to recover. Descriptive locals and submitted-field names remain source-facing inferences rather than original-symbol claims.

The formal C++ intentionally omits explicit vtable stores, SEH/unwind-state writes, security-cookie checks, allocation null branches, constructor-unwind scaffolding, and the implicit `this` return. Base/member/control constructors and normal `new` expressions regenerate those compiler artifacts. It also does not emit separate code for raw derived starts `0x00489f80`/`0x00489ff0`; their no-route evidence remains on their exact child pages while live inline construction proves the source family.

## Score Rationale

`90/92`: exact boundaries/padding, one-argument constructor ABI, six-record packet flow, class/object fields, complete control geometry/order, accepted helper/global/resource names, five call references, raw-derived no-route caveats, owner/emitter/source placement, compiler exclusions, and formal first-draft C++ are incorporated. Remaining uncertainty is limited to original local/member spelling and a possible future coordinated Clan/ClanDialogs file split; neither changes behavior or current emission.

## Cross-References

- [UID:00010L][0x00489600-0x00489f80.ClanAttrInputDialogCore](by-memory/0x00489600-0x00489f80.ClanAttrInputDialogCore.md)
- [UID:000024][ClanAttrInputDialog](by-class/ClanAttrInputDialog.md)
- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:0001TY][ClanInputDialogLayouts](by-type/by-struct/ClanInputDialogLayouts.md)
- [UID:0001X9][ClanDialogVtableFamily](by-type/by-vtable/ClanDialogVtableFamily.md)
- [UID:0002MN][0x006158f4-0x00615fd8.ClanModalDialogVtableData](by-memory/0x006158f4-0x00615fd8.ClanModalDialogVtableData.md)
- [UID:0003B9][0x00616430-0x00616468.ClanModalDialogResourceStrings](by-memory/0x00616430-0x00616468.ClanModalDialogResourceStrings.md)

## Changes

- 2026-07-12 B003 accepted UID0004HH implementation callback:
  - Raised `86/90` to `90/92`, preserved exact range, owner `000024`, emitter `000024`, position `1`, and `RECONSTRUCTABLE:TRUE`, and replaced the stale no-code proof with the accepted managed constructor body.
  - Incorporated MCP session `359c7886` evidence for ABI, six sequential packet strings, eleven controls and geometry, helper/resource/layer/back-pane setup, `0x870` derived allocations, three modeled inline callers, two raw derived constructor calls with no-route caveats, exact predecessor/successor padding, no constructor singleton route, Clan.cpp ownership, and compiler-only exclusions.
  - Rejected a one-off ClanDialogs move, target split/reclassification, explicit vptr/EH/cookie output, and duplicate source bodies for raw derived constructor starts.
- 2026-07-05 B009 implementation callback: created this exact constructor child from former aggregate [UID:00010L][0x00489600-0x00489f80.ClanAttrInputDialogCore](by-memory/0x00489600-0x00489f80.ClanAttrInputDialogCore.md), attached it to [UID:000024][ClanAttrInputDialog](by-class/ClanAttrInputDialog.md), set optional emitter position `1`, and installed the accepted formal no-code proof from the Gate 1 report.
