*** UID:0004MZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000F7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000F7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void TransferServerDialogPane::OnPaint()
{
    EPFTileContext frame;

    void *palette;
    if (g_useEpfAssets == 1) {
        g_pEPFLib->LookupLayoutEntry(L"TRANSSVR.EPF", m_currentFrame, &frame);
        palette = g_pPaletteLib->GetPaletteByName(L"TRANSSVR.PAL");
    } else {
        g_pEPFLib->LookupLayoutEntry(L"TRANSSVR.EPD", m_currentFrame, &frame);
        palette = g_pPaletteLib->GetPaletteByName(L"TRANSSVR.PAD");
    }

    RectBounds destination = frame.bounds;
    destination.Offset(-frame.bounds.left, -frame.bounds.top);
    g_pfnBlitSprite(this,
                    &frame,
                    &frame.bounds,
                    &destination,
                    1,
                    palette,
                    0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact OnPaint override initializes one EPFTileContext, selects TRANSSVR EPF/PAL or EPD/PAD, looks up the current frame, normalizes destination bounds, and invokes the live slot-2 sprite callback with mode 1 and options 0. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005992f0-0x005993a5 TransferServerDialogPane OnPaint

## Status And Boundary

- Exact function range `[0x005992f0,0x005993a6)`, 182 bytes; SHA256 `E273F05A2CD6C41AE6C29B8E4070B42930AB0577A449A6F90B73CF28CDD4C83C`.
- Seven `0xcc` bytes precede it at `[0x005992e9,0x005992f0)` and ten follow at `[0x005993a6,0x005993b0)`; both are parent-only padding.
- Owner/emitter: [UID:0000F7][TransferServerDialogPane](by-class/TransferServerDialogPane.md); parent [UID:0001KD][0x00598ed0-0x0059943f.TransferServerDialogPane](by-memory/0x00598ed0-0x0059943f.TransferServerDialogPane.md).
- Primary vtable slot at `0x0062e4c4` points here. Virtual-table liveness is sufficient even though there is no ordinary direct call.

## Exact Runtime Behavior

1. Default-constructs one `EPFTileContext frame`; its constructor performs the observed initialization.
2. If `g_useEpfAssets == 1`, looks up `L"TRANSSVR.EPF"` at `m_currentFrame` and gets `L"TRANSSVR.PAL"`; otherwise uses `L"TRANSSVR.EPD"` and `L"TRANSSVR.PAD"`.
3. Copies `frame.bounds` to a destination rectangle and offsets it by negative source left/top, preserving width and height while normalizing origin.
4. Calls `g_pfnBlitSprite(this, &frame, &frame.bounds, &destination, 1, palette, 0)` through the live Surface callback slot.

## Source, Callback, And Resource Evidence

- The four exact UTF-16 names occupy `[0x0062e504,0x0062e574)` and are reproduced only by accepted constructor/paint expressions.
- The callback is the typed render route documented by Surface callback support; this page records its concrete dialog call site without taking ownership of the callback table.
- Mode `1`, selected palette, and final options `0` are literal observed arguments. No blend wrapper, opacity substitution, or palette conversion is inferred.
- `m_currentFrame` at object `+0xf8` is advanced by `OnTimer`; the shared field establishes runtime coupling between the virtual callbacks.

## Rejected Alternatives And Historical Names

- Rejected historical `OnPaintFrame`; the inherited primary-vtable contract and class declaration use `OnPaint()`.
- Rejected release/destructor call for `EPFTileContext`: no such source operation is present after the blit, and adding one changes behavior.
- Rejected caching package/palette names, package objects, or `g_useEpfAssets`; exact branch-local lookups must remain.
- Rejected using `m_alpha` as the callback mode or replacing `g_pfnBlitSprite` with a guessed renderer method. The observed call supplies literal mode 1 and options 0.
- Rejected raw callback slot indexing and structure offsets in source; accepted callback/global/type pages provide typed identities.

## Reconstruction Notes

- Preserve initialization, branch order, exact literals, destination copy/offset, and seven call arguments.
- Do not add null checks for libraries, frame lookup, palette, or callback; none is observed.
- Keep palette as `void *` until its separate source type is independently proven; do not invent ownership or release semantics.
