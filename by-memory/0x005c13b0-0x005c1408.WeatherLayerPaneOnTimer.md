*** UID:0003PH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000G1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000G1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool WeatherLayerPane::OnTimer(int timerId, int, int)
{
    if (timerId == 0) {
        m_currentFrame = (m_currentFrame + 1) % m_frameCount;
        InvalidateRect(&m_visibleBounds);
        OnFrameChanged();
        ScheduleTimer(0, m_timerInterval, 0, 0);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 88-byte/SHA256-anchored bool WeatherLayerPane::OnTimer(int,int,int) entered through the TimerHandler +0xa4 facet; timer ID 0 advances signed frame state, invalidates inherited m_visibleBounds, invokes inferred source hook OnFrameChanged, and reschedules timer 0 from signed-short m_timerInterval, while every ID returns true; Weather/Rain/Snow vtable routes, Rain 3/120, Snow 4/5/200 plus override behavior, Swallow exclusion, padding, source route, and human C++03 formal are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005c13b0-0x005c1408 WeatherLayerPane OnTimer

## Status

- Disposition: reconstructable, source-authored `bool WeatherLayerPane::OnTimer(int timerId, int arg0, int arg1)` override with accepted human C++03 formal.
- Direct owner/emitter: [UID:0000G1][WeatherLayerPane](by-class/WeatherLayerPane.md).
- Parent migration index after split: [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md).
- Source route: [UID:0000P8][WeatherLayerPane](by-file/WeatherLayerPane.md) to `NexusTK/map/WeatherLayerPane.cpp`; declaration belongs in canonical `WeatherLayerPane.h`.

## Evidence

- Live IDA MCP session `supervisor_gate2b_0002OT_20260723` reports current `sub_5C13B0` at exact half-open range `[0x005c13b0,0x005c1408)`, size `0x58` / 88 bytes, 33 instructions, three basic blocks, and cyclomatic complexity two.
- Exact target SHA256 is `D22E0CA8BDB83016F49F9A24D78B13789A403E89147CA9E15E7E07EAB3FFF8D7`; its 22-byte leading signature occurs once in the image.
- Fifteen predecessor bytes `[0x005c13a1,0x005c13b0)` and eight successor bytes `[0x005c1408,0x005c1410)` are all `0xcc`, with SHA256 values `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C` and `3D2F424783DF5853C8D7121B1371650C04241F318E1B0CD46BEDBC805B9164C3`. No adjacent code or data belongs to this page.
- The only incoming machine routes are Weather, Rain, and Snow adjusted TimerHandler vtable cells `0x00631218`, `0x006312a8`, and `0x00631338`. There is no direct code caller and no fourth shared timer slot; vtable-only reachability is expected for this virtual callback.
- The body receives the inherited TimerHandler facet at complete object `+0xa4`. Facet-relative `+0x54/+0x55/+0x56` map to complete-object signed fields `m_currentFrame +0xf8`, `m_frameCount +0xf9`, and `m_timerInterval +0xfa`.
- The first explicit argument is `timerId`. Only ID `0` executes weather work; all timer IDs return `true`, and nonzero IDs do not mutate state or schedule a replacement.
- ID `0` computes `(m_currentFrame + 1) % m_frameCount` with signed extension and signed division, stores the signed-byte remainder, and has no zero-count guard. Adding a defensive guard would change execution and is rejected.
- The primary virtual call at slot `+0x20` receives facet `-0x60`, equal to complete object `+0x44`, and is exact `Pane::InvalidateRect(&m_visibleBounds)`. It does not use `m_weatherBounds` at complete `+0xfc`.
- The primary virtual call at slot `+0x4c` is a Weather-added no-argument frame-transition hook. Weather, Rain, and Swallow use shared no-op `0x0041b6a0`; Snow overrides the slot at `0x005c1f50`. The best source-facing name is inferred `OnFrameChanged()`.
- The sole direct callee is source-quality `TimerHandler__ScheduleTimer` at `0x005975e0`, called as `ScheduleTimer(0, m_timerInterval, 0, 0)` after signed-short promotion.
- Local VA pointer hits for target start are only the three vtable cells; the RVA pattern and target-end checks produce no alternate route.

## Score Rationale

Completion `92` and confidence `94` are justified by exact bytes/hash/bounds, unique body evidence, adjusted TimerHandler ABI, callback arguments and unconditional return, signed field semantics, exact inherited rectangle, both virtual roles, reschedule arguments, Weather/Rain/Snow vtable routes, concrete derived-state behavior, Swallow exclusion, lifecycle coupling, current class/file ownership, and accepted human C++03 formal. The score stays below final-audit `95+` because no original PDB/UDT proves the exact lexical spelling/access of `OnFrameChanged`, canonical headers are not yet materialized, and no rebuilt-binary equivalence test has been completed. Those bounded uncertainties do not block this source body.

## Cross-References

- [UID:0000G1][WeatherLayerPane](by-class/WeatherLayerPane.md)
- [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md)
- [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md)
- [UID:0000DC][SnowingLayerPane](by-class/SnowingLayerPane.md)
- [UID:0001WJ][WeatherLayerDataLayouts](by-type/by-struct/WeatherLayerDataLayouts.md)

## 2026-07-23 UID0003PJ Shared Animation Contract

- The shared Weather tail used here and by [UID:0003PJ][0x005c1460-0x005c180c.RainingLayerPaneOnPaint](by-memory/0x005c1460-0x005c180c.RainingLayerPaneOnPaint.md) is now source-facing:
  - `+0xf8`: `signed char m_currentFrame`;
  - `+0xf9`: `signed char m_frameCount`;
  - `+0xfa`: `short m_timerInterval`;
  - `+0xfc`: `RectBounds m_weatherBounds`.
- The timer callback advances the signed current frame, wraps against the signed frame count, and reschedules using the signed-short interval. The Rain constructor establishes `m_frameCount = 3` and `m_timerInterval = 120`, so historical descriptions of 120 frames or a frame step of 3 are inverted.
- Historical UID0003PJ checkpoint: WeatherLayerPane.h was then expected to declare `bool OnTimer(int timerId, int message, int userData)`, and this page remained blank at `86/88` because that pass reconstructed Rain `OnPaint`, not this timer body. The UID0003PH source-quality closure below supersedes the payload labels with generic `arg0/arg1`, fills the formal, and raises this page to `92/94`.

## 2026-07-24 UID0003PG Constructor And Timer Contract

[UID:0003PG][0x005c12a0-0x005c13a1.WeatherLayerPaneConstructor](by-memory/0x005c12a0-0x005c13a1.WeatherLayerPaneConstructor.md) now emits the actual `WeatherLayerPane::WeatherLayerPane()` body and establishes the initial state consumed here. Live IDA session `supervisor_gate2b_0002OT_20260723` proves the constructor is 257 bytes with SHA256 `86BF00A162D9935D3FA52F922F1E862D93E040760746D83CC7C36CE5EB0B8E09`, is called by Rain/Snow/Swallow constructors, and initializes the exact `0x10c` Weather object after `Pane(true)`.

The constructor zeroes the three distinct inherited animation members:

| Constructor offset | Accepted member | Timer use |
| --- | --- | --- |
| `+0xf8` | `signed char m_currentFrame` | this adjusted timer view reads it at `+0x54`, increments it, and wraps it |
| `+0xf9` | `signed char m_frameCount` | this adjusted timer view reads it at `+0x55` for the signed wrap comparison |
| `+0xfa` | `short m_timerInterval` | this adjusted timer view reads it at `+0x56` and sign-extends it for rescheduling |

The offset difference follows the TimerHandler `this` adjustment: constructor TimerHandler view begins at complete-object `+0xa4`, so timer-view offsets `+0x54/+0x55/+0x56` map exactly to complete-object `+0xf8/+0xf9/+0xfa`. This independently rejects one opaque dword field. `RectBounds m_weatherBounds` occupies complete-object `+0xfc..+0x10b` and is initialized by the constructor but is not modified by this timer method.

Constructor scheduling is exactly inherited `ScheduleTimer(0, 0, 0, 0)` through the TimerHandler facet at `+0xa4`. This method later reschedules through the already typed `TimerHandler__ScheduleTimer` using signed `m_timerInterval`. The accepted source-facing helper/member names and selected UID0000P8 `WeatherLayerPane.cpp` route are therefore current facts.

Historical UID0003PG checkpoint: that support synchronization intentionally did not insert timer C++ and required a dedicated UID0003PH pass to reconstruct virtual syntax, handled return, frame-wrap order, and rescheduling. The current closure completes that work without duplicating UID0003PG constructor source.

## 2026-07-24 UID0003PH Source-Quality Closure

### Exact callback ABI and control flow

- Source contract is `bool WeatherLayerPane::OnTimer(int timerId, int arg0, int arg1)`. At the binary boundary ECX is the inherited TimerHandler facet at complete object `+0xa4`; subtracting `0xa4` recovers the complete WeatherLayerPane for Pane virtual calls.
- `retn 0x0c` proves three explicit stack arguments. `mov al,1` on the common exit proves every timer ID returns handled/true. The second and third payloads are unused, so the definition omits their names while canonical declaration should retain generic `arg0` and `arg1`.
- Timer ID `0` alone advances the signed frame, invalidates inherited visible bounds, invokes the frame-transition hook, and queues the next timer. Nonzero IDs intentionally do no work and do not queue a replacement.

### Field and virtual-call semantics

- `m_currentFrame` and `m_frameCount` are signed bytes, not one opaque dword. The body sign-extends both, increments the current frame, performs signed `idiv`, and stores the remainder byte. `m_timerInterval` is a signed short and is sign-extended before the schedule call.
- The binary contains no `m_frameCount == 0` guard. Concrete users establish nonzero state before queued callback dispatch, so adding a guard would be an observable source-level behavior change.
- The rectangle passed to primary slot `+0x20` is inherited `m_visibleBounds` at complete `+0x44`; `m_weatherBounds` begins at complete `+0xfc` and is not touched by this method.
- Pane's established primary virtual sequence places `OnPaint` at `+0x44` and `OnActivate` at `+0x48`; Weather adds the no-argument slot at `+0x4c`. Calling it immediately after the frame change, with a Snow-only behavioral override, supports source-facing `OnFrameChanged()` rather than the rejected `OnPaint`, `Repaint`, or Snow-specific `RandomizeSnowParticles` base name.

### Derived-class dispatch matrix

| Concrete class | TimerHandler slot | Initial frame/count/interval | Primary `+0x4c` | UID0003PH relationship |
| --- | --- | --- | --- | --- |
| `WeatherLayerPane` | `0x00631218 -> 0x005c13b0` | base constructor clears `0/0/0` and queues ID 0; no direct standalone construction caller | shared no-op `0x0041b6a0` | owns callback; abstract/shared base contract |
| `RainingLayerPane` | `0x006312a8 -> 0x005c13b0` | `m_currentFrame=0`, `m_frameCount=3`, `m_timerInterval=120` | shared no-op `0x0041b6a0` | cycles three rain frames and invalidates every 120 ms |
| `SnowingLayerPane` | `0x00631338 -> 0x005c13b0` | constructor dword `0x00C80504` means `4/5/200` | `0x006312fc -> 0x005c1f50` | first callback wraps 4 to 0, then moves/refills snow particles |
| `SwallowLayerPane` | `0x006313c8 -> 0x005c2500` | separate flight state and IDs `0x100/0x101` | shared no-op `0x0041b6a0` | does not dispatch to UID0003PH; inherited constructor ID 0 is acknowledged by Swallow override without shared frame work |

Snow override `0x005c1f50` skips work while `g_activeMapPane->m_waitingForLocalPlayerStatus` is set. Otherwise it moves all 200 particle records and, when its rolling-refresh flag permits, shifts 180 records and refills 20 leading entries. This body remains separately owned by [UID:0003O2][0x005c1f50-0x005c20d4.SnowingLayerPaneRandomizeSnowParticles](by-memory/0x005c1f50-0x005c20d4.SnowingLayerPaneRandomizeSnowParticles.md); only its virtual role and call origin are dependencies here.

### Lifecycle, ownership, and rejected alternatives

- [UID:0003PG][0x005c12a0-0x005c13a1.WeatherLayerPaneConstructor](by-memory/0x005c12a0-0x005c13a1.WeatherLayerPaneConstructor.md) queues initial timer ID 0 with zero delay. Rain, Snow, and Swallow constructors install final vtables/state before queued dispatch. Inherited TimerHandler destruction unregisters pending owner events; no destructor/cancel source belongs in this range.
- Direct owner/emitter remains [UID:0000G1][WeatherLayerPane](by-class/WeatherLayerPane.md). Current source root is [UID:0000P8][WeatherLayerPane](by-file/WeatherLayerPane.md) at `NexusTK/map/WeatherLayerPane.cpp`, with declarations in canonical `WeatherLayerPane.h`.
- Rejected alternatives are preserved: TimerHandler or MapPane ownership, direct file ownership instead of class ownership, `m_weatherBounds` invalidation, treating `+0x4c` as OnPaint/repaint, a zero-count guard, hand-written vtable/thunk/destructor code, a standalone timer source file, and retaining IDA/decompiler names merely because original lexical spelling is stripped.
- Historical one-file versus per-derived-layer source hypotheses remain documented in file/class pages. They no longer block the selected current route or this exact method body.

## Changes

- 2026-06-16 B001 child-split implementation:
  - Created this exact child page from the former [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md) aggregate after B001 verified exact function boundaries, vtable-slot refs, timer behavior, unique PE body evidence, and padding.
  - Historical checkpoint: final C++ was then blank because exact field/helper names and one-file versus per-layer weather source placement were unresolved. Those shared blockers are now resolved; the timer formal remains blank only pending its own target-specific body pass.
- 2026-07-24 B001 UID0003PG source-quality callback:
  - Historical checkpoint: metadata, route, and blank UID0003PH formal then remained unchanged at `86/88`, UID0000G1, and reconstructable true.
  - Added the constructor-to-timer initialization contract, exact adjusted-offset mapping, accepted signed member names/types, timer-zero setup, current source route, and explicit no-duplicate-constructor policy.
  - Historicalized old shared field/helper/source-split blockers without claiming timer-body completion.
- 2026-07-24 B002 UID0003PH source-quality callback:
  - Renamed this page from `WeatherLayerPaneOnTimerEvent` to `WeatherLayerPaneOnTimer`, changed `86/88 -> 92/94`, kept owner/emitter/reconstructable unchanged, set position `20`, and inserted the exact human C++03 callback formal.
  - Incorporated exact bytes/hash/bounds, adjusted TimerHandler ABI, callback arguments/return, signed state arithmetic, inherited visible-bounds invalidation, inferred `OnFrameChanged` hook, timer reschedule, Weather/Rain/Snow dispatch, concrete Rain/Snow state, Swallow exclusion, construction/teardown coupling, source placement, negative evidence, rejected alternatives, and supervisor-owned IDA handoff context.
