*** UID:00017M | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int EffectObjImageLib::RenderEffectFrame(
    MapPane *mapPaneOrRenderContext,
    RectBounds *destinationBounds,
    int frameResourceId,
    int timerDelayAndRenderArg,
    float renderScalar,
    int lightingAndRenderArg,
    IntAlphaSurface *overlayMaskGate,
    float overlayStrength)
{
    (void)mapPaneOrRenderContext;
    (void)timerDelayAndRenderArg;
    (void)lightingAndRenderArg;

    int result = reinterpret_cast<int>(overlayMaskGate);
    if (g_imageLibraryLoadErrorFlag)
    {
        return result;
    }

    const EffectPixMapInfo *remap = &fallbackFrameRemap;
    if (frameResourceId >= 0 && frameResourceId < effectPixMapInfoArray.count)
    {
        remap = &effectPixMapInfoArray.entries[frameResourceId];
    }

    FrameDrawRecord frameRecord;
    frameRecord.reserved0 = 1;
    frameRecord.payloadStart = 0;
    frameRecord.reserved8 = 0;
    frameRecord.width = 0;
    InitRectBounds(&frameRecord.sourceRect, 0, 0, 0, 0);
    frameRecord.payloadSpan = 0;
    frameRecord.payloadEnd = 0;

    LoadFrameDrawRecord(effectFileInfo, static_cast<short>(frameResourceId), &frameRecord);

    void *palette = g_pPaletteLib->GetSlotPalette(11, remap->frameRemapOrPaletteSlot, 0);

    struct EffectRenderAlphaParams
    {
        unsigned char enabled;
        float scalar;
    };

    EffectRenderAlphaParams alphaParams;
    EffectRenderAlphaParams *alphaParamsPtr = 0;
    if (renderScalar > 0.0f)
    {
        alphaParams.enabled = 1;
        alphaParams.scalar = renderScalar;
        alphaParamsPtr = &alphaParams;
    }

    result = g_surfaceRenderCallbacks.primaryFrameBlit(
        &frameRecord,
        &frameRecord.sourceRect,
        destinationBounds,
        1,
        palette,
        alphaParamsPtr);

    if (overlayMaskGate != 0)
    {
        const unsigned char overlayByte =
            static_cast<unsigned char>(overlayStrength * 32.0f + 0.5f);

        return DrawEncodedAlphaFrame(
            overlayMaskGate,
            destinationBounds,
            &frameRecord,
            &frameRecord.sourceRect,
            destinationBounds->bottom,
            overlayByte,
            destinationBounds->top,
            0,
            1);
    }

    return result;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Attached to `EffectObjImageLib`; live IDA confirms exact boundary, single caller, callback/callee set, frame-remap and archive offsets, alpha scratch block, and optional overlay scaling, with final source parameter names still provisional. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004de190-0x004de29f EffectObjImageLib RenderEffectFrame

## Status

- Confidence: strong for owner, function boundary, caller/callee set, object-field flow, callback dispatch, and overlay path; medium for final source parameter names.
- Owner class: [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md).
- Owner file: [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md).
- Current generated caveat: active `class_EffectObjImageLib.cpp` contains a generated renderer and no `.disabled` companion is present, but metadata still marks the method incomplete/effective-excluded and the project route output is still effectively empty for this body.
- Reconstruction gate: formal first-draft C++ is now attached. B008's score-blocker rework resolved the previous marker-only blocker with current MCP evidence while preserving caveats for final callback typedef names and exact alpha-surface/source-rectangle type spelling.

## Function

| Range | Name | Role |
| --- | --- | --- |
| `0x004de190-0x004de29f` | `EffectObjImageLib::RenderEffectFrame` | Draws one effect frame into a target surface and optionally emits an encoded-alpha overlay mask. |

## Behavior Summary

- Returns without drawing while the effect data load/suspend byte `byte_69B420` is set.
- Resolves a four-byte [UID:0001UA][EffectPixMapInfo](by-type/by-struct/EffectPixMapInfo.md) remap from object offset `+0x50` using `frameIndex`, falling back to the remap at `+0x58`.
- Initializes a frame context, then calls [UID:0000UX][LoadFrameDrawRecord_004D1600](by-item/LoadFrameDrawRecord_004D1600.md) against the archive metadata table at object offset `+0x5c`.
- Selects palette category `11` through the palette library and dispatches through the shared surface/image draw callback.
- When alpha is positive, passes a small alpha parameter block to the draw callback.
- When the overlay-mask gate pointer/value is nonzero, scales overlay strength by `32.0`, rounds it, and calls [UID:0000YO][0x00462f20-0x00463252.DrawEncodedAlphaFrame](by-memory/0x00462f20-0x00463252.DrawEncodedAlphaFrame.md) as an `IntAlphaSurface` member using the second method parameter as the binary receiver/draw target.

## Verified Stack and Parameters

- Live IDA MCP on 2026-06-03 reports the current prototype as `int __thiscall(int this, int, int *, int, int, float, int, int *, float)`, ending with `retn 20h`.
- The stack frame reserves `0x58` bytes and includes a four-byte alpha parameter scratch block at `[ebp-0x58]`, a frame rectangle/output record at `[ebp-0x2c]`, and a six-dword render clip/context block at `[ebp-0x1c]`.
- IDA decompilation keeps the final argument names generic, but the observed use sites identify `arg_10` as the draw-alpha/render scalar, `arg_18` as an overlay-mask gate pointer/value from caller render context, and `arg_1c` as the overlay-strength scalar. The actual `IntAlphaSurface` receiver passed in `ECX` to UID0000YO is the second method parameter, not `arg_18`; the member's explicit arguments begin with destination bounds and EPFTileContext source context.

## Source Shape And Names

Best current source-facing method shape:

```cpp
int EffectObjImageLib::RenderEffectFrame(
    MapPane *mapPaneOrRenderContext,
    IntAlphaSurface *drawTarget,
    int frameResourceId,
    int timerDelayAndRenderArg,
    float renderScalar,
    int lightingAndRenderArg,
    IntAlphaSurface *overlayMaskGate,
    float overlayStrength);
```

This signature is source-shape guidance only, not formal reconstruction C++. `frameResourceId`, `renderScalar`, `overlayMaskGate`, and `overlayStrength` are now supported by the caller and body evidence. `timerDelayAndRenderArg` and `lightingAndRenderArg` preserve the four-field `EffectFrameRecord` forwarding contract from `EffectObjectPane::RenderEffect`, even though this body does not consume them directly.

Resolved field/dependency names for this target:

| Offset / item | Current source-quality name |
| --- | --- |
| `this + 0x4c` | `frameRemapCount` / `effectPixMapInfoArray.count` |
| `this + 0x50` | `frameRemapEntries` / `effectPixMapInfoArray.entries` |
| `this + 0x58` | `fallbackFrameRemap` |
| `this + 0x5c` | `effectArchiveMetadataTable` / `effectFileInfo` |
| `0x0069b420` | [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md), declaration owned by [UID:0000K2][ImageLib](by-file/ImageLib.md) |
| `dword_69B3E8` | `SurfaceRenderCallbackTable::primaryFrameBlit`-style callback slot |

## First-Draft Source Shape

B008's 2026-06-30 implementation callback supersedes the earlier B002 draft. The formal `RECONSTRUCTION_CPP` block above is now the implementation source shape for this page. It preserves the same control flow while replacing the unresolved `EffectFrameDrawState` placeholder with the accepted `FrameDrawRecord` support type and removing the old encoded-alpha target field placeholders from the emitted text.

Remaining caveats are confidence caps, not marker-only blockers: `g_surfaceRenderCallbacks.primaryFrameBlit` still needs a final shared typedef, and `MapPane`, `RectBounds`, `IntAlphaSurface`, and encoded-alpha parameter names remain source-facing documentation names until the broader render callback layer is finalized.

UID0000YO's 2026-07-09 B001 evidence supersedes any free-helper interpretation of the formal call above. The binary route is `IntAlphaSurface::DrawEncodedAlphaFrame(RectBounds *dstRect, EPFTileContext *sourceContext, RectBounds *sourceOrigin, int alphaEndY, unsigned char alphaEnd, int alphaStartY, unsigned char alphaStart, AlphaMaskBlendMode blendMode)`. The existing formal expression `DrawEncodedAlphaFrame(overlayMaskGate, ...)` remains this caller page's receiver-backed expression; it must not be used as free-helper ownership evidence.

## Rejected Names And Ownership Alternatives

- `frameIndex` is weaker than `frameResourceId` because the caller forwards the record's resource id, not the current animation index.
- `paletteId` is rejected because palette selection is resolved from `EFFECT.FRM` remap data and fixed palette category `11`; no caller-supplied palette id is used.
- `alpha` as the only name for the first float is too narrow. It is the record's render scalar and only becomes an alpha parameter when positive.
- `drawFlags` for `arg_14` is rejected because the fourth `EffectFrameRecord` field is preserved by the calling contract but is unused in this body.
- `overlayTarget` for `arg_18` is rejected. The caller supplies a render-context gate value/pointer, while the actual encoded-alpha draw target is the second method parameter.
- Global utility/free-helper ownership is rejected because the body reads `EffectObjImageLib` object fields and is called through `g_pEffectObjImageLib`.
- Compiler-artifact or retained-duplicate treatment is rejected because the function has one real source-level caller, a nontrivial resource/callback body, and no duplicate sibling body.
- `EffectObjectPane` or `MapPane` ownership is rejected because those modules select and schedule frames, while this function owns effect image-library remap, archive, palette, frame-load, and blit behavior.
- Keeping the target marker-only is rejected because the current MCP recheck and accepted support names are sufficient for first-draft source. The remaining callback, draw-state, and alpha-surface declarations are documented caveats rather than blockers for formal first-draft C++.

## Caller Ownership

The sole executable caller at `0x00538ba0` belongs to [UID:0001D9][0x005387b0-0x00538baa.EffectObjectPaneCore](by-memory/0x005387b0-0x00538baa.EffectObjectPaneCore.md). It validates the current effect-frame index, copies one [UID:0001U7][EffectFrameRecord](by-type/by-struct/EffectFrameRecord.md), and forwards all four record fields to this method. The optional overlay policy comes from caller-side map/render context state, but the frame resource lookup, palette remap, draw-state setup, and primary frame blit remain `EffectObjImageLib` source responsibility.

Caller-side overlay policy from the B002 recheck remains outside this target:

- If `EffectObjectPane + 0x11c` is zero or `sub_50AB40(g_activeMapPane)` returns false, the caller passes `overlayMaskGate = 0` and `overlayStrength = 0.0f`.
- Otherwise the caller passes `overlayMaskGate = renderContext + 0x3e0` and `overlayStrength = *(float *)(renderContext + 0x3dc) + *(float *)(EffectObjectPane + 0x11c)`.
- `sub_50AB40` checks active map pane/config state, including `activeMapPane[247] < 0.60000002` and a config byte; this is render-context/map overlay policy and not image-library ownership.

## Callers

IDA MCP on 2026-05-26 confirms one modeled caller:

| Caller | Owner |
| --- | --- |
| `0x00538ba0` | `EffectObjectPane` render path in [UID:0000IZ][Effects](by-file/Effects.md). |

## IDA Evidence

- 2026-05-31 IDA MCP `lookup_funcs 0x004de190` confirms `sub_4DE190` size `0x110`.
- 2026-05-31 IDA MCP decompilation confirms the `byte_69B420` guard, `+0x4c` remap count, `+0x50` remap array, `+0x58` fallback remap, and `+0x5c` archive metadata table usage.
- 2026-05-31 IDA MCP callees include [UID:0000UX][LoadFrameDrawRecord_004D1600](by-item/LoadFrameDrawRecord_004D1600.md), palette lookup at `0x00543e40`, the shared render callback table call through `dword_69B3E8`, and [UID:0000YO][0x00462f20-0x00463252.DrawEncodedAlphaFrame](by-memory/0x00462f20-0x00463252.DrawEncodedAlphaFrame.md).
- 2026-06-03 restarted IDA MCP `py_eval` confirms the exact boundary `0x004de190-0x004de2a0`, previous function `0x004de050-0x004de189`, next function `0x004de2a0-0x004de3d2`, size `0x110`, and terminal `retn 20h`.
- 2026-06-03 IDA MCP xrefs show one code caller at `0x00538ba0` inside `sub_538AF0` (`0x00538af0-0x00538baa`), matching the documented `EffectObjectPane` render path.
- 2026-06-03 IDA MCP xrefs-from/callees confirm direct references to `byte_69B420`, `sub_457A60`, `sub_4D1600`, `dword_67A7E0`, palette lookup `sub_543E40`, shared callback `dword_69B3E8`, overlay constants `dword_6104CC`/`dword_6104B0`, and overlay draw helper `sub_462F20`.
- 2026-06-03 IDA MCP decompilation confirms the remap selection from object offsets `+0x4c`, `+0x50`, and `+0x58`; archive metadata table call at object offset `+0x5c`; palette category `11`; alpha block only when alpha is positive; and overlay call using destination rectangle fields plus `int(overlayStrength * 32.0 + 0.5)`.
- 2026-06-03 IDA MCP global reads confirm `dword_6104CC == 0x42000000` (`32.0f`) and `dword_6104B0 == 0x3f000000` (`0.5f`).
- 2026-06-17 B002 live IDA MCP `analyze_function 0x004de190` reports prototype `int __thiscall(int *this, int, int, int, int, float, int, int, float)`, size `272`, caller `sub_538AF0`, and callees `sub_457A60`, `LoadFrameDrawRecord`, palette lookup `sub_543E40`, callback `dword_69B3E8`, `DrawEncodedAlphaFrame`, and the security-cookie check.
- 2026-06-17 B002 live IDA MCP `analyze_function 0x00538af0` and PE scan confirm exactly one direct caller at `0x00538ba0` and no raw function-entry pointer hits for `0x004de190`; there is no table/indirect-use evidence changing owner or liveness treatment.
- 2026-06-17 B002 decompilation of `0x00457a60` confirms a 40-byte combined draw-state initializer, and `0x004d1600` writes across that same state block before the callback and encoded-alpha helper consume its subregions.
- 2026-06-30 B008 score-blocker rework used current MCP session `supervisor_resume_20260629`, with `server_health` call id `3` confirming module `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, auto-analysis ready, and the matching executable input path.
- B008 decompile/call evidence in calls `8` through `12` and `15` revalidated the guard on [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md), remap selection from `+0x4c/+0x50/+0x58`, archive table use at `+0x5c`, palette category `11`, positive-float alpha scratch setup, callback slot `dword_69B3E8`, and overlay strength conversion `overlayStrength * 32.0f + 0.5f`.

## Cross-References

- [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md)
- [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md)
- [UID:0001U9][EffectObjImageLibLayout](by-type/by-struct/EffectObjImageLibLayout.md)
- [UID:0001UA][EffectPixMapInfo](by-type/by-struct/EffectPixMapInfo.md)
- [UID:0000UX][LoadFrameDrawRecord_004D1600](by-item/LoadFrameDrawRecord_004D1600.md)
- [UID:0000YO][0x00462f20-0x00463252.DrawEncodedAlphaFrame](by-memory/0x00462f20-0x00463252.DrawEncodedAlphaFrame.md)
- [UID:0000IZ][Effects](by-file/Effects.md)

## Changes

- 2026-07-09 B001 UID0000YO member-call reconciliation:
  - Historicalized the free-helper interpretation of the formal encoded-alpha call. Current UID0000YO evidence proves an `IntAlphaSurface` `thiscall` member route at callsite `0x004de289`.
  - Preserved the existing formal call only as an explicit caller-local source-shape placeholder because this page's second-parameter type remains inconsistent with the binary receiver role. No owner, score, or formal-body change was made.
- 2026-06-30 B008 empty-emitter implementation callback:
  - Before: `86/90`, with B002 review-only source text and a blank formal C++ block.
  - After: `88/91`, formal first-draft C++ attached under [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md).
  - Summary/evidence: accepted report used current MCP session `supervisor_resume_20260629`, server-health call id `3`, and score-blocker rework calls `8` through `12` and `15` to preserve the exact boundary, sole `EffectObjectPane` caller, load-error guard, `EffectPixMapInfo` remap/fallback behavior, archive metadata table use, palette slot `11`, alpha scratch block, primary blit callback, encoded-alpha overlay scaling, and rejected owner/marker alternatives. The old review-only `EffectFrameDrawState` and `drawTarget->field_*` placeholders are superseded by the formal source block.

- 2026-06-06 endpoint sync:
  - A002 live IDA MCP `lookup_funcs` confirmed the previous `sub_4DE050` body has local size `0x13a`, `0x004de189` is still in that function, and `0x004de18a` is outside it. The adjacent-boundary evidence now uses the same destructor endpoint as [UID:00017L][0x004de050-0x004de189.EffectObjImageLibDestructor](by-memory/0x004de050-0x004de189.EffectObjImageLibDestructor.md).

- 2026-06-03 restarted IDA MCP evidence and scoring update:
  - Before: scores were `76/86`.
  - After: scores set to `82/88`.
  - Summary/evidence: live IDA MCP revalidated the function boundary, `thiscall` prototype arity, `retn 20h`, single caller, full callee set, object offset flow, alpha scratch block, render context stack blocks, overlay constants, and overlay helper call. Source-level parameter/type names and gated active C++ output remain incomplete, so no C++ reconstruction code was added.
- 2026-06-17 B002 source-quality execution:
  - Before: `82/88`, with stale disabled-output text, generic parameter names, and incorrect `arg_18` overlay-target wording.
  - After: `86/90`, owner/emitter unchanged through [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md), and formal C++ intentionally left for a later source-quality callback under accepted B002 guidance.
  - Summary/evidence: B002 rechecked live IDA/PE evidence, the only EffectObjectPane caller, remap/fallback/archive offsets, palette slot `11`, `SurfaceRenderCallbackTable` slot use, alpha scratch block, overlay gate semantics, current generated-output state, [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md) ownership, and rejected caller-owner/compiler-artifact alternatives. B008's 2026-06-30 callback later superseded the blank block with formal first-draft C++.

- 2026-06-03 autogen attachment and score update:
  - Before: the page was scored `68/82` and had blank autogen parent metadata despite exact owner, boundary, caller, and callee evidence.
  - After: scored `76/86` and attached to [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md) at position `30`.
  - Summary/evidence: parent file/class docs both record the render/image-library ownership, `EffectObjectPane` modeled caller at `0x00538ba0`, the `byte_69B420` render guard, `EFFECT.FRM` remap/fallback offsets, archive metadata at `+0x5c`, palette category `11`, shared draw callback, and optional [UID:0000UH][DrawEncodedAlphaFrame_00462F20](by-item/DrawEncodedAlphaFrame_00462F20.md) overlay pass. B008's 2026-06-30 callback later attached formal first-draft C++ while preserving parameter-name caveats.

- 2026-05-31 IDA evidence and scoring update:
  - Before: `RECONSTRUCTABLE` was blank and scores were `50/65`.
  - After: marked `RECONSTRUCTABLE:TRUE`, with scores set to `68/82`.
  - Summary/evidence: IDA MCP rechecked the exact function size, render guard, remap/fallback/archive fields, palette/render/overlay callees, and modeled caller. Final parameter names and source-level method body remain incomplete, so no C++ reconstruction code was added.
