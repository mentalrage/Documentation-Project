*** UID:00014N | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Emitted code for this retained raw duplicate is covered by [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md case 3.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: ExchangeDialog retained raw money-update duplicate for server opcode `0x42` subcommand `3`; current MCP confirms no function object/xrefs, side byte `packet+2`, amount dword `packet+3`, controls `6`/`9`, zero local `SetExchangeAmount(0)`, 32-WCHAR `%u` text update, live dispatcher duplicate, and corrected exclusive end at `0x004add34`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

Advanced-scan identity: 0x004adca0, 0x004add34, ExchangeDialogRawMoneyUpdate. Historical stale end `0x004add33` is the final byte of `retn 4`, not the half-open exclusive end.

# 0x004adca0-0x004add34 ExchangeDialog Raw Money Update

## Status

- Disposition: reconstructable NexusTK project code retained as a raw/non-IDA duplicate of live dispatcher behavior.
- Entity kind: projected `ExchangeDialog` method/helper body for server opcode `0x42` subcommand `3`.
- Direct owner/emitter: [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md).
- Source-file route: [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md).
- Current generated label caveat: stale generated/simroot material associated this raw start with active `ExchangeDialog::OnExchangePacket`; current IDA/MCP proves the live routed method is [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md), and this page is the retained raw duplicate for its subcommand `3`.
- Confidence: high for packet/control behavior, dispatcher duplication, class ownership, corrected half-open end, and no-route status; limited below final audit because the original helper spelling/call shape is not proven.
- Reconstruction note: the formal C++ block intentionally contains only the covered-by marker. Emitting a standalone helper body here would duplicate the live dispatcher subcommand and imply a route to `0x004adca0` that current evidence rejects.

## Behavior

The raw body updates the exchange money display from an exchange server packet. It is equivalent to [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) dispatcher case `3` under server opcode `0x42`. It reads:

- side byte at `packet+2`;
- big-endian amount at `packet+3`;
- control `6` for the local money edit control when side is local;
- control `9` for the remote money display when side is remote.

If the local amount is zero, it calls `ExchangeDialog::SetExchangeAmount(0)`. It formats the amount with `"%u"` and writes the resulting wide text into the selected control.

## Boundary Notes

IDA MCP does not define a function at `0x004adca0`, and `xrefs_to 0x004adca0` reports no xrefs. Raw disassembly still shows a complete body:

- prologue at `0x004adca0`;
- security-cookie setup/check;
- calls to `0x004ada20`, `0x0041b9b0`, and `0x00498ca0`;
- `retn 4` instruction beginning at `0x004add31` and consuming bytes through `0x004add33`;
- alignment begins at `0x004add34`.

The correct half-open body is `0x004adca0-0x004add34`, length `0x94` / 148 bytes (Verified with int_convert.py). The body duplicates the subcommand `3` money-update logic already present inside the IDA-confirmed dispatcher at [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md). Treat it as a retained raw duplicate with no standalone emission; [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `3` is the covering emitted behavior.

## 2026-06-01 IDA MCP Recheck

- `lookup_funcs 0x004adca0` still reports `Not a function`.
- `xrefs_to 0x004adca0` still reports no xrefs.
- The parent aggregate recheck confirms the surrounding dispatcher `0x004ad320` is a real IDA function of size `0x493`, `SetExchangeAmount` at `0x004ada20` is a real function of size `0x64`, and `0x004add40` begins the next real helper.
- The same recheck confirms no IDA function is present at the raw starts `0x004ae060`, `0x004ae0b0`, and `0x004ae170`, so this page is part of a broader raw-helper cleanup pattern rather than an isolated metadata typo.

## 2026-06-05 Live IDA Recheck

- IDB: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- `lookup_funcs` still reports no function at `0x004adca0`, `0x004add33`, or `0x004add34`; the next modeled helper starts at `0x004add40` (`sub_4ADD40`, size `0x11f`).
- `xrefs_to` reports no direct references to `0x004adca0`, `0x004add33`, `0x004add34`, or `0x004add40`.
- Focused disassembly confirms a complete raw body: stack/security-cookie prologue; side byte read through `packet+2`; amount decode through `packet+3`; remote-side control `9`; local-side zero-amount call to `0x004ada20`; local control `6`; `"%u"` formatting through `0x0041b9b0`; and final text write through `0x00498ca0`.
- Raw-range xrefs confirm calls/data references at `0x004adcec -> 0x004ada20`, `0x004add06 -> "%u"`, `0x004add0e -> 0x0041b9b0`, and `0x004add1c -> 0x00498ca0`.
- The modeled dispatcher block at `0x004ad530-0x004ad59e` inside `sub_4AD320` duplicates the same side/amount/control-selection/format/write flow and jumps back to the dispatcher epilogue, so the raw body remains best treated as a projected duplicate split rather than an independently reachable IDA function.
- Boundary check: the final `retn 4` begins at `0x004add31` and consumes bytes through `0x004add33`; `0x004add34-0x004add3f` is `0xcc` alignment before `sub_4ADD40`.

## 2026-06-15 Live IDA Refresh

- IDA MCP session `a003_objectlist_dispatch_20260615` reports `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and string cache ready.
- `lookup_funcs` still reports `Not a function` for `0x004adca0`, `0x004add31`, `0x004add33`, and `0x004add34`; `xrefs_to 0x004adca0` reports no inbound xrefs. This preserves the raw/projected-helper caveat rather than upgrading the body to a normal IDA-modeled method.
- Focused disassembly confirms the exact raw body: stack/security-cookie prologue at `0x004adca0`, side byte read at `packet+2`, amount decode call `sub_5754C0(packet+3)`, remote-side control id `9`, local-side zero-amount call to `SetExchangeAmount` at `0x004adcec`, local control id `6`, `"%u"` formatting through `sub_41B9B0` with buffer count `0x20` / 32 (Verified with int_convert.py), text write through `sub_498CA0`, and final `retn 4` at `0x004add31`.
- Dispatcher decompilation/disassembly of [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) confirms subcommand `3` contains the same money-update flow at `0x004ad531-0x004ad59e`: side byte from `packet+2`, amount decode from `packet+3`, local control `6`, remote control `9`, zero-amount `SetExchangeAmount(0)`, `"%u"` formatting, and `sub_498CA0` write before returning `1`.
- Historical note: this pass previously recorded the body as `0x93` / 147 bytes through exclusive end `0x004add33`. B006 rechecked the final `retn 4` bytes and corrected that stale half-open boundary: `0x004add33` is the final byte of `retn 4`, the exclusive end is `0x004add34`, and the body is `0x94` / 148 bytes (Verified with int_convert.py). The next modeled helper starts at `0x004add40`; `0x004add34-0x004add3f` is alignment.

## 2026-06-26 B006 Source-Quality Recheck And Disposition

- MCP evidence used active session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `initialize` returned `ida-pro-mcp` version `1.0.0` and protocol `2025-06-18`, `tools/list` returned 65 tools, and `server_health(database=80de0a67)` reported status `ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, string cache ready, and cache size `2067`.
- `lookup_funcs` reports [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) `0x004ad320` as `sub_4AD320`, size `0x493` / 1171 bytes (Verified with int_convert.py); `0x004adca0`, `0x004add31`, `0x004add33`, and `0x004add34` are not functions. The next modeled helper remains `0x004add40` (`sub_4ADD40`, size `0x11f` / 287 bytes, Verified with int_convert.py).
- `xrefs_to 0x004adca0` reports zero cross-references. `xrefs_to 0x004ad320` reports the routed dispatcher vtable data xref at `0x00619d9c`, so the live method route is the dispatcher, not this raw start.
- `insn_query` over `0x004adca0-0x004add40` returned a 58-instruction raw body with no containing IDA function. It begins with a normal stack/security-cookie prologue, loads the packet pointer from `[ebp+8]`, and copies `ecx` into `edi`, confirming the primary `ExchangeDialog` receiver.
- The raw body reads the side byte at `packet+2` through `std::_Narrow_char_traits<char,int>::to_char_type`, decodes the big-endian amount dword from `packet+3` through `sub_5754C0`, selects remote control id `9` or local control id `6` through the control collection at `[this+0x1fc]`, calls [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md) `ExchangeDialog::SetExchangeAmount(0)` via `0x004ada20` when the local amount is zero, formats the amount through wide `"%u"` with a `0x20` / 32-WCHAR buffer (Verified with int_convert.py), and writes text through `sub_498CA0`.
- Byte checks at `0x004add31` return `c2 04 00 cc cc ...`: `retn 4` occupies `0x004add31-0x004add33`, and `0x004add34` is the first alignment byte. The corrected half-open body length is `0x94` / 148 bytes (Verified with int_convert.py).
- Dispatcher case `3` inside [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) duplicates the same logic: side at `packet+2`, amount at `packet+3`, control `9` for remote money, control `6` for local money, local zero `SetExchangeAmount(0)`, `"%u"` formatting, and `sub_498CA0` text write before returning success. The dispatcher decompile also shows the secondary-view receiver adjustment `this - 160` (`0xa0`, Verified with int_convert.py) and control holder offset `+508` (`0x1fc`, Verified with int_convert.py).
- Route-negative scans found zero matches for absolute start pointer `a0 dc 4a 00`, RVA start pointer `a0 dc 0a 00`, and end/alignment pointers `31 dd 4a 00`, `33 dd 4a 00`, and `34 dd 4a 00`. Accepted dispatcher research also found no rel32 branch target, VA dword, RVA dword, or end-pointer route to `0x004adca0`.
- Numeric conversion batches used for these inherited facts were `'["0x93","0x20","0x42","0x4A","0x1fc","0x493","0x11f","0x64","0x2e","0x41","0x26","0x274","0xa0"]' | python tools\int_convert.py --pretty` and `'["0x94","0x93","0x004add34","0x004add33"]' | python tools\int_convert.py --pretty`.
- Owner/emitter decision: direct owner and emitter are [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md). [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) remains the source-file route only. The raw body uses `ecx` as `ExchangeDialog *this`, reaches `[this+0x1fc]` controls, and calls the class-local `SetExchangeAmount`; it is not a file-root free helper.
- Rejected alternatives: [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) as canonical owner is too broad; `ExchangeMoneyEditControlPane` is rejected because control id `6` is only a selected child control, not the receiver; [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) as owner is rejected because it is the live routed method rather than the owning object; no-owner/non-reconstructable is rejected because this is custom NexusTK dialog/protocol behavior; a standalone helper body is rejected because it would duplicate [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `3` and falsely imply a static route to `0x004adca0`.
- Formal C++ disposition: this target remains reconstructable and emitted through [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), but its formal block emits only the covered-by marker naming [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `3`. The marker is a current no-body disposition, not a blank-block deferral.

## Score Rationale

Completion is raised from `85` to `88` and confidence from `88` to `91` because the page now records the current MCP session and health, raw 58-instruction body, corrected half-open end, packet/control/formatting flow, dispatcher case `3` duplicate, vtable route to [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md), zero raw-start xrefs and zero pointer-pattern route hits, direct class-owner repair, and implementation-ready formal covered-by marker.

It does not rise higher because the original helper name/call shape is still not proven. Future evidence could replace the marker with a real private helper only if it proves the original dispatcher called out to a helper equivalent to this raw body; current evidence supports inline dispatcher emission plus this retained duplicate marker.

## Cross-References

- [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md)
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)

## Changes

- 2026-06-26 B006 ExchangeDialogRawMoneyUpdate source-quality implementation:
  - Before: `COMPLETION:85`, `CONFIDENCE:88`, owner/emitter [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md), filename/title used stale exclusive end `0x004add33`, range prose treated `0x93` / 147 bytes as the body length, and the formal block was blank.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:91`, owner/emitter [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), file renamed to `0x004adca0-0x004add34`, body length corrected to `0x94` / 148 bytes (Verified with int_convert.py), and formal C++ now contains only the covered-by marker naming [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `3`.
  - Summary/evidence: current MCP session `80de0a67` confirms no function object or xrefs for `0x004adca0`, the complete raw money-update body, side byte `packet+2`, amount dword `packet+3`, controls `6`/`9`, local zero `SetExchangeAmount(0)`, 32-WCHAR `"%u"` text update, `sub_498CA0` write, final `retn 4` bytes through `0x004add33`, alignment beginning at `0x004add34`, and the live dispatcher duplicate routed through vtable xref `0x00619d9c`.
- 2026-06-15 A001-goal2-exchange-foldertree-memory-20260615-1:
  - Before: `COMPLETION:78`, `CONFIDENCE:86`.
  - Historical changed-to state at that time: `COMPLETION:85`, `CONFIDENCE:88`; owner/emitter remained [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md), and final C++ remained blank. The current B006 entry above supersedes that owner/emitter and formal-block disposition.
  - Historical summary/evidence: live IDA MCP reconfirmed no function object or xrefs for the raw start, refreshed the exact raw instruction flow, confirmed the same money-update behavior inside dispatcher subcommand `3`, and recorded what is now known to be stale `0x93`-byte/exclusive-end wording. B006 supersedes the boundary to `0x94` / 148 bytes through exclusive end `0x004add34`.
- 2026-06-05 live IDA recheck:
  - Before: metadata was `72/82`, based on the 2026-06-01 raw-boundary check and parent aggregate evidence.
  - Changed to: `78/86`; `RECONSTRUCTABLE:TRUE` and `AUTOGEN_PARENT_UID:0000J9` are unchanged.
  - Summary/evidence: live IDA MCP confirmed the exact raw flow, modeled callees, no-function/no-xref raw status, final-instruction/alignment boundary, and byte-for-byte-equivalent dispatcher subcommand `3` block. C++ remains blank because the raw split still has no IDA function object or direct callers.
- 2026-06-01 documentation pass:
  - Before: metadata remained at `50/65`, reconstructable and parent fields were blank, and the status relied on generated naming without separating it from IDA evidence.
  - Changed to: `72/82`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000J9`.
  - Summary/evidence: IDA MCP reconfirmed no function or xrefs at `0x004adca0`, confirmed the surrounding real functions, and matched this raw start to the existing ExchangeDialog aggregate; C++ remains blank because the function split is not final.
