*** UID:0004PT | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool LoginDialogPane::HandlePointerOrMouseEvent(Event *event)
{
    int &mouseY = event->m_payload.m_pointer.m_y;
    int &mouseX = event->m_payload.m_pointer.m_x;
    const int originalY = mouseY;
    const int originalX = mouseX;

    switch (event->m_type) {
    case kEventCursorMove:
        if (m_isTitleDragActive) {
            m_origin.x += mouseX - m_dragOriginX;
            m_origin.y += mouseY - m_dragOriginY;

            if (m_clampDragToBounds) {
                if (m_origin.x < m_dragClampMinX)
                    m_origin.x = m_dragClampMinX;
                if (m_origin.y < m_dragClampMinY)
                    m_origin.y = m_dragClampMinY;

                const int maximumX = m_dragClampMaxX - m_bounds.right;
                const int maximumY = m_dragClampMaxY - m_bounds.bottom;
                if (m_origin.x > maximumX)
                    m_origin.x = maximumX;
                if (m_origin.y > maximumY)
                    m_origin.y = maximumY;
            }
        } else {
            int controlId;
            const char hitType = HitTestControls(mouseY, mouseX, &controlId);
            if (m_pressedControlActive) {
                if (controlId == m_pressedControlId &&
                    hitType == m_pressedHitType) {
                    SetSelectionVisualState(controlId, hitType);
                } else {
                    SetSelectionVisualState(-1, 10);
                }
                DispatchInputToControl(event, m_pressedControlId);
            }
        }
        break;

    case kEventLeftButtonDown:
    {
        int controlId;
        const char hitType = HitTestControls(mouseY, mouseX, &controlId);
        if (hitType != 10 && !m_pressedControlActive && hitType >= 0) {
            if (m_activeControlId != controlId)
                SetHoverControl(controlId);

            m_pressedControlActive = true;
            m_pressedControlId = controlId;
            m_pressedHitType = hitType;
            SetSelectionVisualState(controlId, hitType);
            DispatchInputToControl(event, controlId);
        } else if (hitType == 10 &&
                   !m_isTitleDragActive &&
                   PointInRect(mouseY, mouseX, &m_bounds)) {
            m_dragOriginY = mouseY;
            m_dragOriginX = mouseX;
            m_isTitleDragActive = true;
            m_savedPaneMode = m_mode;
            if (m_mode == 2)
                SetMode(0);
            else if (m_mode == 3)
                SetMode(1);
        }
        break;
    }

    case kEventLeftButtonDoubleClick:
    {
        int controlId;
        HitTestControls(mouseY, mouseX, &controlId);
        if (controlId != -1)
            DispatchInputToControl(event, controlId);
        break;
    }

    case kEventLeftButtonUp:
        if (m_isTitleDragActive) {
            SetMode(m_savedPaneMode);
            if (m_dialogConfigId != 0xffff)
                SavePosition();
            m_isTitleDragActive = false;
        }

        if (m_pressedControlActive) {
            const int pressedControlId = m_pressedControlId;
            const char pressedHitType = m_pressedHitType;
            m_pressedControlActive = false;
            SetSelectionVisualState(-1, 10);
            DispatchInputToControl(event, pressedControlId);
            g_pScreenPane->RenderPresentation();

            int controlId;
            const char hitType = HitTestControls(originalY, originalX, &controlId);
            if (controlId == pressedControlId && hitType == pressedHitType) {
                OnControlCommand(controlId, hitType);
                RedrawSelectedControl();
            }
        }
        break;

    case kEventMouseWheel:
        if (PointInRect(mouseY, mouseX, &m_bounds)) {
            int controlId;
            HitTestControls(mouseY, mouseX, &controlId);
            if (controlId >= 0)
                DispatchInputToControl(event, controlId);
        }
        break;
    }

    if (g_useEpfAssets == 1 &&
        !PointInRect(originalY, originalX, &m_bounds)) {
        RectBounds dialogBounds;
        GetScreenBounds(&dialogBounds);
        mouseX += dialogBounds.left;
        mouseY += dialogBounds.top;

        RectBounds mainMenuBounds;
        g_pMainMenuPane->GetScreenBounds(&mainMenuBounds);
        if (PointInRect(mouseY, mouseX, &mainMenuBounds)) {
            mouseX -= mainMenuBounds.left;
            mouseY -= mainMenuBounds.top;
            g_pMainMenuPane->HandlePointerOrMouseEvent(event);
        }
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact pointer/mouse virtual for move/down/double/up/wheel, control hit/press/selection dispatch, title drag/clamp/save, release command/redraw, presentation refresh, and legacy MainMenu coordinate relay. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fae90-0x004fb2a6 - LoginDialogPane HandlePointerOrMouseEvent

## Status

- Exact range: `[0x004fae90,0x004fb2a6)`, size `0x416`, SHA-256 `5734B1DFA484453974A634D789C6965764D20D0C33CB2B828C4CC482`.
- Source disposition: source-authored EventHandler pointer/mouse virtual owned/emitted by [UID:00007F][LoginDialogPane](by-class/LoginDialogPane.md) at position 50.
- IDA modeled 299 instructions, 54 basic blocks, and cyclomatic complexity 36. The restored session `9df6e9a0` reconfirmed the function's first 16 bytes after the infrastructure pause.

## Event And Control Behavior

- Uses `Event::m_payload.m_pointer.m_y/m_x` by reference and saves both original coordinates before any translation.
- Cursor move while title dragging updates inherited pane origin by the pointer delta and clamps minimum/maximum origin only when `m_clampDragToBounds` is set. Without title drag, an active pressed control receives selection-state maintenance and translated child dispatch.
- Left-button down uses signed `HitTestControls(y,x,&id)`. A non-10 nonnegative hit starts pressed-control state, updates hover when needed, sets selection state, and dispatches to the child. A title/background hit 10 starts title drag inside bounds and maps pane modes 2/3 to temporary 0/1.
- Left-button double click re-hit-tests and dispatches any nonnegative control id.
- Left-button up restores saved mode, saves position only when config id is not `0xffff`, and clears title drag. An active pressed control is cleared, deselected, dispatched, followed by screen presentation refresh; a matching release hit then invokes `OnControlCommand` and redraws selection.
- Mouse wheel dispatches only when the original pointer lies in dialog bounds and hit-test returns a nonnegative child.

## Legacy MainMenu Relay

- After every handled switch path, when `g_useEpfAssets == 1` and the original pointer is outside this dialog, the method translates event coordinates from dialog-local to screen and then MainMenu-local space.
- It relays to `g_pMainMenuPane->HandlePointerOrMouseEvent(event)` only when the translated point lies inside MainMenu bounds.
- The event's coordinate fields remain the translated values on the relay path; the accepted body intentionally does not add restoration absent in the binary.
- The method returns `true` for all event types, including unhandled/default types after the optional relay.

## Helper, Vtable, And Layout Evidence

- EventHandler vtable cell `0x0061d3ec` points directly to `0x004fae90`; no competing direct source caller or owner was found.
- Resumed bounded decompile of DialogPane helper `0x0049fb10` proves signed `char` return, argument order y/x/out-id, disabled-control high-bit encoding, miss result 10, and output id -1.
- `0x0049fc00` temporarily translates Event coordinates for one child and restores them, supporting `DispatchInputToControl(Event *, int)`. `0x0049fa60` stores current bounds into configured dialog position, supporting `SavePosition()`.
- Every accessed member belongs to the accepted DialogPane layout; LoginDialogPane adds no fields. Current Event, Pane, DialogPane, ScreenPane, MainMenuPane, and control declarations support direct source calls.
- `[0x004fae88,0x004fae90)` is eight-byte alignment before this function. `[0x004fb2a6,0x004fb2d0)` is a compiler jump-table/alignment span with hash `9FF23C446B4C54603FE98C0A5C4D7C46196F70E3AC2CF38321596C9DAC3FDC45`; SendLoginRequest starts at exclusive successor `0x004fb2d0`.

## Source Shape And Edge Cases

- Event types are exact current names for values 0,1,2,3,7: cursor move, left down, left double click, left up, and mouse wheel.
- Signed `char` hit type must remain signed because the disabled high-bit encoding and `hitType >= 0` gate are behaviorally significant.
- Title-drag, pressed-control, release-hit, render, command, redraw, and relay ordering is exact and must not be compressed into higher-level framework calls.
- Descriptive helper names follow current DialogPane conventions. Their original symbols are stripped, so lexical uncertainty caps confidence without blocking behavior or ABI.

## Negative And Historical Evidence

- Rejected stale `HandleMouseEvent` and `InputEvent` names; current Event/EventHandler hierarchy and vtable family support `HandlePointerOrMouseEvent(Event *)`.
- Rejected raw member offsets, speculative Login fields, unsigned hit type, x/y argument reversal, unconditional save, coordinate restoration after relay, and direct MainMenu ownership.
- Rejected separate handwritten jump table or table child. The table is compiler lowering for this source switch and has no independent xref/source identity.
- Rejected folding SendLoginRequest into this method or parent; it begins at the exact exclusive successor and remains a file-local helper.

## Score Rationale

- Completion `91`: exact range/hash/CFG, every event branch/order, helper contracts, inherited fields, vtable route, relay, table/successor boundaries, owner/emitter/position, edge cases, negatives, and complete formal body are resolved.
- Confidence `93`: behavior and ABI evidence are very strong; stripped helper/predicate lexical spellings and complex source-expression equivalence keep confidence below 94/95.

## Cross-References

- [UID:0002Q4][0x004fa7a0-0x004fb2d0.LoginDialogPaneCore](by-memory/0x004fa7a0-0x004fb2d0.LoginDialogPaneCore.md) reviewed split index.
- [UID:00007F][LoginDialogPane](by-class/LoginDialogPane.md) class owner/emitter.
- [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md) source-file owner.
- [UID:00003T][DialogPane](by-class/DialogPane.md) inherited layout and helper contract.
- [UID:0002Q5][0x004fb2d0-0x004fb62a.SendLoginRequest](by-memory/0x004fb2d0-0x004fb62a.SendLoginRequest.md) exclusive source successor.
- [UID:0000RF][g_pMainMenuPane](by-global/g_pMainMenuPane.md) legacy relay receiver.

## Changes

- 2026-07-14 B005 UID0002Q4 callback: Created as the exact pointer/mouse child with full event/control/drag/release/relay ordering, helper ABI, table/successor, negative, and lexical-confidence evidence.
