*** UID:0002QY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00008R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00008R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool Motion::OnTimer(int timerId, int, int)
{
    if (timerId != 0)
        return true;

    if (m_owner->m_nameVisible != 1 ||
        m_currentFrame < m_terminalFrame - 2)
    {
        ++m_currentFrame;
    }

    if (m_soundEffectId != -1 &&
        m_currentFrame == m_soundTriggerFrame)
    {
        g_pSoundManager->PlaySample(
            m_soundEffectId,
            (4U * g_pSoundManager->GetSampleVolume()) / 5U);
    }

    if (m_currentFrame < m_terminalFrame)
    {
        g_pTimerMgr->ScheduleTimer(this, 0, m_timerDelay, 0, 0);
        if (m_owner->m_currentEffect != this)
            m_owner->m_currentEffect = this;
    }
    else
    {
        const int effectCount = m_owner->m_activeEffects->GetCount();
        for (int i = 0; i < effectCount; ++i)
        {
            Motion *effect = *static_cast<Motion **>(
                m_owner->m_activeEffects->GetElementAt(i));
            if (effect == this)
            {
                m_owner->m_activeEffects->RemoveAt(i, 1);
                break;
            }
        }

        if (m_owner->m_currentEffect != 0)
            m_owner->m_currentEffect = 0;

        g_pApplicationCleanupQueue->EnqueueIfMissing(this);
    }

    g_activeMapPane->UpdateObjectPaneScreenBounds(m_owner);
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

## B001 UID0002QY Accepted Callback Synchronization - 2026-07-14

- Final source identity: this exact `0x00539fe0-0x0053a10c` body is `bool Motion::OnTimer(int timerId, int, int)`, emitted through [UID:00008R][Motion](by-class/Motion.md) Motion and [UID:0000LL][Motion](by-file/Motion.md) `NexusTK/render/Motion.cpp` at position 50. Metadata remains `92/94`, owner/emitter UID00008R, reconstructable true, and `Nested:0`.
- ABI and receiver: the machine receiver is Motion's secondary TimerHandler view at primary Motion `+4`; ordinary multiple-inheritance adjustment supplies the source `this`. The callback returns true and pops three stack arguments. A nonzero timer ID returns immediately without frame, sound, list, scheduling, cleanup, or map effects.
- Exact state: Motion stores `LivingObjectPane *m_owner`, signed-short sound ID with `-1` sentinel, signed trigger/current/terminal frame bytes, and timer delay. Owner `+0x1bc` is a `List *` containing pointer-width `Motion *` values, owner `+0x1c0` is `Motion *m_currentEffect`, and owner `+0x1d1` is `m_nameVisible`.
- Exact behavior: name visibility can hold the frame at terminal-minus-two; sound fires only at the trigger frame and uses unsigned four-fifths sample volume; continuing animation reschedules event zero with the stored delay and publishes the primary Motion pointer. Completion snapshots count once, dereferences each `Motion **` element slot, removes one identity match, clears any nonzero current pointer, queues deferred cleanup, and reaches one common MapPane bounds refresh.
- Source legality: the complete LivingObjectPane declaration grants `friend class Motion;`. This preserves the accepted direct-field body without public raw fields, invented accessors, opaque casts, a partial class shell, or manual receiver arithmetic.
- Preserved negatives and history: this is not a scheduler wrapper, LivingObjectPane method, free timer helper, aggregate body, compiler thunk, EH wrapper, scalar-delete implementation, or inline destruction path. It performs no explicit destructor, pool free, delete, vptr write, base teardown, or adjustor. The old descriptive timer-tick name, timer-handle interpretation, generic suppression byte, blank body, and blank source position remain historical only.

*** Item Summary: Source-ready Motion::OnTimer override with exact nonzero-event early return, secondary TimerHandler receiver adjustment, name-visible frame hold, signed sound/frame fields, unsigned four-fifths playback volume, reschedule/current-effect publication, active-effects List unlink, deferred BlackHole cleanup, common MapPane bounds refresh, and position-50 Motion.cpp emission. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00539fe0-0x0053a10c Motion OnTimer

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: source-authored `TimerHandler::OnTimer` virtual override on the secondary base view of `Motion`.
- Owner and autogen parent: [UID:00008R][Motion](by-class/Motion.md), with [UID:0000LL][Motion](by-file/Motion.md) as the `NexusTK/render/Motion.cpp` source root. This child is current at `92/94`, position 50.
- Container: [UID:0001DD][0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster](by-memory/0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster.md).
- Rebuild handling: source-authored method body.
- Confidence: very strong for function boundary, callback ABI/name, vtable/data xref, multiple-inheritance adjustment, callees, control flow, field mapping, List behavior, lifetime, and direct Motion ownership; exact original private spellings remain a bounded confidence caveat.
- Reconstruction note: the formal block is complete and behavior-identical. `Motion::OnTimer`, `m_activeEffects`, `m_currentEffect`, `m_nameVisible`, and the packed Motion fields are source-facing inferred names supported by current producers, consumers, and inherited interface evidence.

## Behavior

When invoked with timer ID zero, this method advances the packed current-frame byte unless the `LivingObjectPane` owner's `m_nameVisible` byte at `+0x1d1` is exactly one and the current frame has reached the terminal frame minus two. Motion type 27 calls `ShowName` and deliberately holds at that threshold; type 28 calls `HideName` and continues. When the current frame reaches the trigger frame and the signed-short sound/effect id is not `-1`, it plays through the global sound manager using `0x0057a6c0` and `0x0057a5c0` at unsigned four-fifths sample volume.

If animation frames remain, it schedules the embedded timer handler through the global timer manager and publishes the primary `Motion *` in the owner's `m_currentEffect` field at `+0x1c0` when different. The scheduler return is ignored; this field is not a timer handle. If the animation has completed, it snapshots the count of the `m_activeEffects` List at `+0x1bc`, dereferences each pointer-storage element, removes exactly one matching Motion through `RemoveAt(index, 1)`, clears any nonzero current-effect pointer independently of list-match success, and queues the primary object through BlackHole for deduplicated deferred cleanup.

The method invalidates/refreshes the associated map/object state through `0x00506980` before returning success.

The callback has an early nonnormal path: when the first stack argument is nonzero, `0x00539fe3-0x00539fea` branches directly to the success return without frame, sound, list, scheduler, cleanup, or map side effects. `AL=1` and `retn 0Ch` match the accepted boolean three-argument `TimerHandler::OnTimer` contract.

## Callback State And Flow

IDA shows `ecx` as the embedded `TimerHandler` vtable view at primary `Motion + 4`. Offsets below are relative to that callback view; primary Motion is the machine receiver minus four. Human source uses normal public multiple inheritance and lets the compiler perform this adjustment.

| Offset/global | Evidence | Role |
| --- | --- | --- |
| `this + 0x04` / primary `+0x08` | Read repeatedly at `0x00539ff0`, `0x0053a052`, `0x0053a09b`, and `0x0053a0f6`; construction and owner-only offsets identify the pointee. | `LivingObjectPane *m_owner`. |
| `this + 0x0c` | Checked against `0xffff` at `0x0053a012`, then passed as a signed word at `0x0053a03c`. | Sound/effect id; `-1` suppresses playback. |
| `this + 0x0e`, `this + 0x0f`, `this + 0x10` / primary `+0x12/+0x13/+0x14` | `0x0053a01c`, `0x00539ff3`, `0x0053a04d`; both constructors write the same fields. | Signed-character sound trigger, current frame, and terminal frame. |
| `this + 0x14` / primary `+0x18` | Pushed at `0x0053a0c7` for `0x00597910`. | `int m_timerDelay`. |
| owner `+0x1bc` | `0x0053a05b-0x0053a098` snapshots direct List count, calls `GetElementAt` through slot `+0x10`, dereferences pointer storage, and removes through slot `+0x1c`. | `List *m_activeEffects`. |
| owner `+0x1c0` | Completion clears any nonzero value; ongoing animation stores primary Motion `timerView - 4`; `ScheduleTimer` return is unused. | `Motion *m_currentEffect`, not a timer handle/count/state. |
| owner `+0x1d1` | `0x00539ff6` checks for value one; `ShowName`/`HideName` set/clear the same byte. | `m_nameVisible`. |
| `dword_67A7D0` | `0x0053a021`; calls `0x0057a6c0` then `0x0057a5c0`. | Sound manager path. |
| `dword_67A74C` | `0x0053a0b2`; calls `0x00469120`. | Deferred cleanup/delete queue. |
| `dword_67AB80` | `0x0053a0d6`; calls `0x00597910`. | Timer manager used to reschedule the callback. |
| `dword_67A764` | `0x0053a0f9`; calls `0x00506980`. | Map/object invalidation or refresh target. |

## Normal Tick Flow

1. If `eventCode != 0`, return success without changing frame state.
2. Read signed current frame at timer-view `+0x0f`. Increment unless `m_nameVisible == 1` and current is no longer below terminal minus two.
3. If signed-short sound/effect ID is not `-1` and current matches the trigger byte, read sample volume and play the sample at unsigned `4 * volume / 5`.
4. If signed current remains below signed terminal, reschedule event zero with stored delay and zero payloads. Ignore the scheduler return and publish primary Motion (`timerView - 4`) into `m_currentEffect` when different.
5. Otherwise snapshot active-effect count once, scan from zero, dereference each pointer-storage element, remove one matching Motion, clear any nonzero `m_currentEffect`, and enqueue primary Motion through BlackHole. No destructor, delete, pool free, vptr write, base teardown, EH wrapper, or adjusted thunk is source-authored here.
6. In both normal branches, refresh/invalidate the owner through `dword_67A764`/`0x00506980` and return `1`.

## IDA MCP Evidence

- `lookup_funcs 0x00539fe0` reports `sub_539FE0` with size `0x12c`, giving exact end-exclusive range `0x0053a10c`.
- 2026-06-07 IDA MCP `py_eval` counted `99` in-range instructions and `21` in-range basic blocks for the exact function body.
- `xrefs_to 0x00539fe0` reports a data reference at `0x00620968`, consistent with a Motion vtable/timer callback slot.
- `callers` reports no direct code callers, matching callback-style dispatch.
- `callees` reports `0x00506980`, `0x00469120`, `0x00597910`, `0x0057a5c0`, and `0x0057a6c0`, matching map/object invalidation, deferred cleanup, timer scheduling, and sound playback.
- The function returns `1` through `al` at `0x0053a105-0x0053a109` and uses `retn 0ch`, matching a timer/callback wrapper with three stack arguments.
- Raw byte review shows the old `0x0053a10b` end was one byte short and `0x0053a10c-0x0053a110` is `0xcc` alignment before the LivingObjectPane constructor range.
- 2026-06-11 Batch170 live IDA MCP reconfirmed IDB MD5 `4247e04e20b65d6414c7238aa8ff5515`, exact modeled range `0x00539fe0-0x0053a10c`, vtable data xref `0x00620968`, no direct code callers, direct callees `0x0057a6c0`, `0x0057a5c0`, `0x00469120`, `0x00597910`, and `0x00506980`, and global manager reads at `0x0067a7d0`, `0x0067a74c`, `0x0067ab80`, and `0x0067a764`.
- 2026-06-11 decompiler review confirms the event-code early return, current-frame increment/suppression rule, sound trigger condition, owner-list removal loop, active-motion slot clear/update, deferred cleanup enqueue, timer reschedule, and final map/object refresh.
- 2026-07-14 B001 current MCP session `3fc1dcef` returned healthy analysis/Hex-Rays/string state and reconfirmed `sub_539FE0` size 300, 21 basic blocks, cyclomatic complexity 12, no code callers, one data xref at `0x00620968`, and the same five callees. The exact 300-byte SHA256 is `BBAEEE647EFDE42B9ACECBC03518C63857C411BD49A537CD506AA75FA45B8F4A`.
- The first 16 bytes are `55 8b ec 83 7d 08 00 57 8b f9 0f 85 15 01 00 00`; the final 16 are `a7 67 00 e8 7c c8 fc ff 5e b0 01 5f 5d c2 0c 00`. Both `0x00539fdc-0x00539fe0` and `0x0053a10c-0x0053a110` are four `0xcc` bytes.
- Motion vtable dwords at `0x00620950-0x0062096c` contain the primary/adjusted Motion RTTI locators, primary scalar deleting destructor, adjusted destructor thunk, and this callback at slot address `0x00620968`. Both Motion constructors store primary `0x00620954` and adjusted `0x00620964` vptrs.
- Negative checks found no target strings/constants, direct code caller, local Motion UDT, embedded source path, scheduler-result store, explicit target destruction/free/delete, or ownership route to Region, MessageDialogs, NewHumanImageLib, TimerMgr, SoundManager, MapPane, BlackHole, or LivingObjectPane source files.

## Assignment Gate

This `92/94` child is reconstructable, source-ready, and directly Motion-owned. [UID:00008R][Motion](by-class/Motion.md) carries the complete class declaration and emits through [UID:0000LL][Motion](by-file/Motion.md). The broader [UID:0001DD][0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster](by-memory/0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster.md) remains a non-emitting index rather than the source parent.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | Exact bounds/hash/ABI, all blocks and side effects, multiple-inheritance adjustment, field/global/List/helper meanings, construction/destruction invariants, source name/route/order, negative evidence, and complete formal C++ are documented. |
| Confidence | 94 | Current MCP, vtable, constructor, caller, helper, and owner-field evidence agree. Confidence remains below 95 only because exact original private spellings and physical header/source filename are absent from the stripped binary. |

## Cross-References

- [UID:0001DD][0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster](by-memory/0x00539bc0-0x0053a10c.MotionAnimationConstructorTimerCluster.md)
- [UID:00008R][Motion](by-class/Motion.md)
- [UID:0000LL][Motion](by-file/Motion.md)
- [UID:0002QW][0x00539bc0-0x00539d43.MotionAnimationConstructor](by-memory/0x00539bc0-0x00539d43.MotionAnimationConstructor.md)
- [UID:0002QX][0x00539de0-0x00539f52.MotionAnimationAlternateConstructor](by-memory/0x00539de0-0x00539f52.MotionAnimationAlternateConstructor.md)
- [UID:0003Z5][0x0053b060-0x0053b6b3.LivingObjectPaneEffectMovementAndActionHelpers](by-memory/0x0053b060-0x0053b6b3.LivingObjectPaneEffectMovementAndActionHelpers.md)
- [0x005975d0-0x005975d5 TimerHandler OnTimer pure-virtual body](by-memory/0x005975d0-0x005975d5.TimerHandlerOnTimerPureVirtualBody.md). This path-specific reference intentionally omits a UID because the current ordinary docs contain a duplicate UID000418 registration; the scoped validator otherwise rewrites it to an unrelated NewHumanImageLib page.
- [UID:0000DG][SoundManager](by-class/SoundManager.md)

## Change Log

- 2026-07-14 Agent-B001 UID0002QY callback: validator-aware rename from `MotionAnimationTimerTick` to `MotionOnTimer`; raised `86/90 -> 92/94`; set position 50; installed the accepted complete `Motion::OnTimer` body; resolved the secondary TimerHandler receiver, LivingObjectPane owner, active-effects/current-effect/name-visible fields, unsigned volume scaling, reschedule/unlink/deferred-cleanup behavior, source route/order, and compiler-only exclusions. The old timer-handle, generic suppress-state, blank-code, blank-position, and generic timer-tick conclusions are preserved above only as superseded historical provenance where explicitly identified.
- 2026-06-11 Agent-A006 Batch170: raised from `84/88` to `86/90` and set `AUTOGEN_PARENT_UID:00008R` after live IDA/decompiler review reconfirmed exact boundary, vtable callback xref, direct callees/globals, event-code gate, frame/sound logic, owner-list unlink or timer-reschedule branches, map refresh, and direct Motion class parent gate. Final C++ remains blank below the 95/95 code-emission gate.
- 2026-06-07 (A009): Raised completion/confidence from `78/84` to `84/88` after live IDA MCP review of the callback path. Added timer-view field mapping, owner-side list/active-handle offsets, global manager uses, instruction/block counts, return convention, and the corrected no-assignment rationale under the `85/85` parent gate.
