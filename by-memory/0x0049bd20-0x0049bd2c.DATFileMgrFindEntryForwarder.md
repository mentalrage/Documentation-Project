*** UID:000227 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool DATFileMgr::FindEntryByName(const wchar_t *entryName, DATEntryLocation *outLocation)
{
    return m_impl->FindEntryByName(entryName, outLocation);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Public `DATFileMgr::FindEntryByName` wrapper; exact raw no-function body loads `m_impl`, tail-jumps to `_DATFileMgr::FindEntryByName`, and has no direct wrapper-entry xrefs. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049bd20-0x0049bd2c DATFileMgr FindEntryByName Wrapper

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: public `DATFileMgr` method / source-authored raw no-function wrapper.
- Source-facing signature: `bool DATFileMgr::FindEntryByName(const wchar_t *entryName, DATEntryLocation *outLocation)`.
- Private target: `_DATFileMgr::FindEntryByName(const wchar_t *entryName, DATEntryLocation *outLocation)` at `0x0049cad0`.
- Owner: [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), with source module route [UID:0000IO][DATFileMgr](by-file/DATFileMgr.md) / `NexusTK/archive/DATFileMgr.cpp`.
- Rebuild handling: emit the formal C++ wrapper in this exact child page. The old "C++ remains blank because names/types are unresolved" state is superseded; method spelling, `m_impl`, private callee, and `DATEntryLocation *outLocation` are inferred but implementation-ready.
- Confidence: high for byte behavior, boundaries, owner/emitter, `m_impl` offset, private helper relationship, and output-record type; capped below final-audit quality because IDA still has no function object or direct entry xrefs/pointers for `0x0049bd20`, and exact original source spelling is inferred rather than symbol-proven.

## Covered Range

| Range | Role |
| --- | --- |
| `0x0049bd20-0x0049bd2c` | Loads public wrapper field `m_impl` from `[ecx+0x04]`, preserves the caller's stack arguments, and tail-jumps to `_DATFileMgr::FindEntryByName` at `0x0049cad0`. |

## Raw Code Evidence

```asm
49bd20  push    ebp
49bd21  mov     ebp, esp
49bd23  mov     ecx, [ecx+4]
49bd26  pop     ebp
49bd27  jmp     sub_49CAD0
```

2026-06-26 live IDA MCP session `80de0a67` supplies the current proof set:

- `server_health(database='80de0a67')` returned `status=ok`, module `NexusTK.exe`, imagebase `0x400000`, with auto-analysis, Hex-Rays, and strings cache ready.
- `lookup_funcs` reported `Not a function` for `0x0049bd19`, `0x0049bd20`, `0x0049bd27`, and `0x0049bd2c`; the next modeled function is `sub_49BD30` at `0x0049bd30`, size `0x111`, the adjacent load wrapper is `sub_49BE70` at `0x0049be70`, size `0x0c`, and the private lookup helper is `sub_49CAD0` at `0x0049cad0`, size `0xd5`.
- `get_bytes` over `0x0049bd19` showed seven `0xcc` bytes, then wrapper bytes `55 8b ec 8b 49 04 5d e9 a4 0d 00 00`, then four `0xcc` bytes before the constructor prologue at `0x0049bd30`.
- `insn_query` for `0x0049bd19-0x0049bd30` rendered `align 10h` at `0x49bd19`, the five wrapper instructions at `0x49bd20-0x49bd27`, and `align 10h` at `0x49bd2c`; all entries have `fn:null`.
- `make_signature_for_range 0x0049bd19-0x0049bd30` returned `CC CC CC CC CC CC CC 55 8B EC 8B 49 04 5D E9 A4 0D 00 00 CC CC CC CC`, unique in the current IDB.
- A byte search for the wrapper body found one match at `0x0049bd20`.

## Behavior

This is the public `DATFileMgr` wrapper for the internal `_DATFileMgr::FindEntryByName` helper. It mirrors [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md): the public wrapper object contains `_DATFileMgr *m_impl` at offset `+0x04`, and the public method forwards to the private implementation object.

The wrapper does not inspect or modify the input string or output record. It changes only `ecx` from `DATFileMgr *this` to `this->m_impl`, preserves the caller's two stack arguments, and propagates the private helper's boolean result.

## Signature Evidence

| Item | Evidence | Confidence |
| --- | --- | --- |
| Receiver | Raw body reads `[ecx+4]`, matching the accepted public `DATFileMgr` layout: `vptr` at `+0x00`, `_DATFileMgr *m_impl` at `+0x04`. | Strong |
| Arguments | Stack arguments are untouched before the tail jump, so the public method signature matches `_DATFileMgr::FindEntryByName`. | Strong |
| Return | The tail-jump returns the private helper's byte/bool success flag directly. | Strong |
| Output record | `_DATFileMgr::FindEntryByName` writes the found node's container pointer and entry index into the non-null output pointer, and `DATFile::Open` consumes those two values as a location record. | Strong |
| Final source name | `DATFileMgr::FindEntryByName` matches the private helper, file/class vocabulary, and [UID:00012E][0x0049c700-0x0049c71d.HasDATEntry](by-memory/0x0049c700-0x0049c71d.HasDATEntry.md) formal call. Exact original symbol proof is absent, but the spelling is implementation-ready. | Strong inference |

## Private Helper And Consumer Evidence

- `xrefs_to` / `xref_query` for `0x0049cad0` found four refs: the raw jump at `0x49bd27`, `DATFile::Open` at `0x49c1b8`, [UID:00012E][0x0049c700-0x0049c71d.HasDATEntry](by-memory/0x0049c700-0x0049c71d.HasDATEntry.md) at `0x49c711`, and `_DATFileMgr::LoadDATFileIndex` duplicate-check logic at `0x49c9c0`.
- The 2026-06-26 decompile/analyze pass for `0x0049cad0` shows string normalization, resolver lookup through `0x0049d5e0`, comparison against `this[2]` as the sentinel/no-hit node, `false` return on sentinel, optional output writes from node offsets `+0x0c` and `+0x10`, and `true` return on hit.
- `DATFile::Open` calls the private helper with an output local, throws `FileError` on false, then uses the returned container pointer and entry index to derive the mapped `DATEntryRecord` and payload size.
- `HasDATEntry` reads [UID:0000QQ][g_pDATFileMgr](by-global/g_pDATFileMgr.md), loads the inner pointer at `+0x04`, calls `0x0049cad0` with null output, and returns boolean existence; its formal C++ calls `g_pDATFileMgr->FindEntryByName(entryName, 0)`.
- `_DATFileMgr::LoadDATFileIndex` calls `0x0049cad0` with null output before creating a new node, confirming the helper is also used as a duplicate-name lookup.
- The adjacent public load wrapper at `0x0049be70` decompiles as the same public-to-private `m_impl` forwarding pattern into `_DATFileMgr::LoadDATFileIndex`.

## Negative Evidence

The no-route state is real and remains part of the score rationale:

- IDA has no function object at `0x0049bd20`.
- `xrefs_to` and `xref_query` found no cross-references to the wrapper entry.
- `find code_ref` and `find data_ref` found no refs to `0x0049bd20`.
- A little-endian pointer byte search for `20 bd 49 00` found no stored pointer to the wrapper start.
- No original symbol or source/header path proves the exact public method spelling.

These negatives cap confidence but do not block formal C++ because the wrapper body, owner route, private callee signature, output record, and neighboring public-wrapper pattern are resolved.

## Boundary Evidence

| Boundary | Evidence |
| --- | --- |
| Predecessor | [UID:00012A][0x0049bcf0-0x0049bd19.CriticalSectionLock](by-memory/0x0049bcf0-0x0049bd19.CriticalSectionLock.md) ends before the documented alignment. |
| Pre-wrapper padding | [UID:0000VN][-ignored](by-memory/-ignored.md) records `0x0049bd19-0x0049bd20` as `0xcc` alignment. |
| Covered body | Raw disassembly and MCP bytes cover exactly `0x0049bd20-0x0049bd2c`. |
| Successor padding | [UID:0000VN][-ignored](by-memory/-ignored.md) records `0x0049bd2c-0x0049bd30` as alignment before the DAT manager constructor. |
| Successor | [UID:00012B][0x0049bd30-0x0049d6ed.DATManagers](by-memory/0x0049bd30-0x0049d6ed.DATManagers.md) begins at `0x0049bd30`; the constructor child begins at [UID:000439][0x0049bd30-0x0049be41.DATFileMgrConstructor](by-memory/0x0049bd30-0x0049be41.DATFileMgrConstructor.md). |

## Source-Quality Resolution

Use these source-facing names/types in final reconstruction:

- Public wrapper: `DATFileMgr::FindEntryByName`.
- Private callee: `_DATFileMgr::FindEntryByName`.
- Public wrapper field: `_DATFileMgr *m_impl` at `DATFileMgr +0x04`.
- Output record parameter: `DATEntryLocation *outLocation`.
- Output fields on the private helper's node/location model: `m_container` and `m_entryIndex`.

Rejected alternatives preserved from the source-quality pass:

- `sub_49BD20` and raw address-shaped names are evidence/provenance only, not final source.
- `DATFileMgrFindEntryForwarder` / `ForwardFindEntryByName` describe recovery mechanics, not the most plausible public API name.
- File-local helper ownership is rejected because the body consumes `ecx` as a `DATFileMgr *` and dereferences a class member.
- `_DATFileMgr` direct ownership is rejected for this wrapper because `_DATFileMgr` owns the callee, while this body starts from the public wrapper object and only loads `m_impl`.
- `DATEntryRecord *` is wrong for the output parameter because the output is a two-field location record, not the mapped 17-byte DAT table row.
- `DATEntryNode *` is wrong for the output parameter because nodes are internal resolver storage, while callers receive only container/index location values.
- Raw `_DWORD *` / `void *` output types are decompiler artifacts and should not appear in formal C++.

## Reconstruction Notes

Formal C++ now emits as:

```cpp
bool DATFileMgr::FindEntryByName(const wchar_t *entryName, DATEntryLocation *outLocation)
{
    return m_impl->FindEntryByName(entryName, outLocation);
}
```

This preserves exact runtime behavior: the source call loads the same `m_impl` pointer and passes the same two arguments to the private helper, while propagating the helper's boolean return. It matches the adjacent `DATFileMgr::LoadDATFile` wrapper shape and avoids decompiler labels, raw forwarder names, or raw pointer output types.

## Score Rationale

- Completion is `90` because the target now has the exact byte/range proof, raw no-function status, negative reachability checks, private-helper behavior, consumer semantics, source-facing names/types, owner/emitter route, and formal C++ recorded in the target page.
- Confidence is `91` because the wrapper bytes, `m_impl` field, private helper signature, `DATEntryLocation` output model, `DATFile::Open`/`HasDATEntry`/`LoadDATFileIndex` consumers, and adjacent public load wrapper all agree.
- Confidence remains below [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md)'s adjacent load wrapper and below final-audit levels because this wrapper has no IDA function object, no direct entry xrefs, no stored pointer route, and no original symbol proof for exact public method spelling.
- Owner/emitter/reconstructable metadata remains unchanged: canonical owner [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), emitter [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), reconstructable true, blank emitter position.

## Cross-References

- [UID:0000IO][DATFileMgr](by-file/DATFileMgr.md)
- [UID:00003I][DATFileMgr](by-class/DATFileMgr.md)
- [UID:000004][_DATFileMgr](by-class/_DATFileMgr.md)
- [UID:00043E][0x0049cad0-0x0049cba5._DATFileMgrFindEntryByName](by-memory/0x0049cad0-0x0049cba5._DATFileMgrFindEntryByName.md)
- [UID:00012E][0x0049c700-0x0049c71d.HasDATEntry](by-memory/0x0049c700-0x0049c71d.HasDATEntry.md)
- [UID:00012B][0x0049bd30-0x0049d6ed.DATManagers](by-memory/0x0049bd30-0x0049d6ed.DATManagers.md)
- [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)
- [UID:00012A][0x0049bcf0-0x0049bd19.CriticalSectionLock](by-memory/0x0049bcf0-0x0049bd19.CriticalSectionLock.md)
- [UID:00003G][DATFile](by-class/DATFile.md)
- [UID:0000QQ][g_pDATFileMgr](by-global/g_pDATFileMgr.md)

## Changes

- 2026-06-02: Marked reconstructable, attached to the documented `DATFileMgr` file owner, removed the provisional C++ sketch, and added signature/boundary/reconstruction-gate notes.
- 2026-06-06 A010 live IDA refresh:
  - Changed to: `COMPLETION:78`, `CONFIDENCE:88`.
  - Summary/evidence: live IDA rechecked function modeling around the wrapper, confirmed no direct xrefs to the raw start, confirmed the four refs into `_DATFileMgr::FindEntryByName`, and captured the target helper's byte return plus optional two-dword output-buffer write. C++ remained blank at that time because the target output record type and final public method spelling were not yet final-source quality.
- 2026-06-15 A003 Goal 2 refresh:
  - Changed score from `78/88` to `85/89`.
  - Route changed from file owner/emitter [UID:0000IO][DATFileMgr](by-file/DATFileMgr.md) to class owner/emitter [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), because the raw public wrapper and class parent both clear the corrected `85/85` gate.
  - Summary/evidence: current IDA MCP reconfirmed no function object at the wrapper, exact wrapper bytes, `0xcc` successor padding, the raw tail jump into `sub_49CAD0`, four target refs, and the target's byte return plus optional two-dword output-buffer write. C++ remained blank pending source-quality resolution.
- 2026-06-26 B001 source-quality implementation:
  - Changed score from `85/89` to `90/91`; owner [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), emitter [UID:00003I][DATFileMgr](by-class/DATFileMgr.md), reconstructable true, and blank emitter position are unchanged.
  - Inserted formal C++ for `bool DATFileMgr::FindEntryByName(const wchar_t *entryName, DATEntryLocation *outLocation)`.
  - Summary/evidence: live MCP session `80de0a67` confirmed health, no IDA function at the wrapper, exact bytes/signature, no wrapper entry xrefs/code refs/data refs/pointer bytes, unique body bytes, four refs to `0x0049cad0`, private helper output semantics, `DATFile::Open`/`HasDATEntry`/`LoadDATFileIndex` consumers, and the adjacent `DATFileMgr::LoadDATFile` wrapper comparison. The old blank-C++ rationale is superseded by inferred but implementation-ready source names/types.
