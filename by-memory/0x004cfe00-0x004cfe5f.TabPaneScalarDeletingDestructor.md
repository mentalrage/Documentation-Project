*** UID:00034W | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-generated scalar deleting destructor for TabPane; source is TabPane::~TabPane().
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 95-byte / SHA256 8E5672211BCC387BA09BB34B061A9EF01131C6A379B9ED98DE690596CCE53DC9 class-owned TabPane scalar deleting destructor wrapper: restores primary/EventHandler/TimerHandler vptrs, clears g_pTabPane in the exact five-ref lifecycle, always runs Pane teardown, frees only when deletingFlags bit 0 is set and bit 2 is clear, otherwise routes bit 2 through the protected 0xf8 one-byte runtime no-op, returns original this, has exactly two adjustor code refs plus one primary-vtable data ref, ends before one-byte padding and IdleWatcher at 0x004cfe60, and emits only the existing compiler-wrapper comment because UID00034M owns the sole human TabPane::~TabPane() body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cfe00-0x004cfe5f TabPane Scalar Deleting Destructor

## Status

- Entity kind: class-owned `TabPane` scalar deleting destructor glue.
- Direct owner: [UID:0000EB][TabPane](by-class/TabPane.md).
- Source file: [UID:0000OF][TabPane](by-file/TabPane.md).
- Parent aggregate: [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md).
- Rebuild handling: source-declared/generated-binary scalar deleting destructor support. Keep class-owned `RECONSTRUCTABLE:TRUE` metadata so the rebuilt `TabPane` declaration and virtual destructor regenerate this ABI wrapper, but do not hand-author it as a second source destructor body. Ordinary source teardown belongs to [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md) / `TabPane::~TabPane()`.

## Behavior

The wrapper restores the three `TabPane` vtable views, clears [UID:0000SE][g_pTabPane](by-global/g_pTabPane.md) / `0x0069adfc`, calls inherited [UID:0000A2][Pane](by-class/Pane.md) cleanup at `0x00544580`, and conditionally frees `this` through `0x004f4ac0` when scalar-delete flags allow it.

This wrapper's vtable restores, base destructor call, delete-flag branch, guard helper path, optional free, and return value are compiler-generated ABI/delete support. Source C++ should be emitted through [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md) as `TabPane::~TabPane()` and should not be duplicated here.

IDA decompilation shows:

```c
*this = TabPane::vftable;
this[40] = TabPane::vftable_0;
this[41] = TabPane::vftable_1;
dword_69ADFC = 0;
sub_544580(this);
if ((flags & 1) != 0 && (flags & 4) == 0) sub_4F4AC0(this);
```

## IDA Evidence

- `py_eval` on 2026-06-10 reports IDA function `sub_4CFE00` at `0x004cfe00-0x004cfe5f`, size `0x5f`, 29 instructions, and 5 basic blocks.
- `xrefs_to 0x004cfe00` reports code refs from the two `TabPane` adjustor thunks and a primary vtable/data ref at `0x0061b4d0`.
- Callees are shared pane cleanup `0x00544580`, delete helper `0x004f4ac0`, and guard helper `0x0041b6a0`.
- Vtable refs are to `0x0061b4d0`, `0x0061b51c`, and `0x0061b54c`; those same vtables are installed by `TabPane` constructor and cleanup.
- The following byte `0x004cfe5f` is `0xcc` alignment before [UID:0000K0][IdleWatcher](by-file/IdleWatcher.md) starts at `0x004cfe60`.
- B015's accepted [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md) report rechecked the sibling ordinary destructor and found the same constructor/ordinary/scalar vtable and singleton parity: constructor writes `g_pTabPane` at `0x004cf9a4`, ordinary destructor clears it at `0x004cf9ea`, and this wrapper clears it at `0x004cfe20`.

## Current MCP Refresh

The accepted B007 research used a dated live IDA MCP snapshot captured on `2026-08-10` from session `supervisor-gate2b-batch3-20260810`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, with server health `ok` and Hex-Rays ready. The session name is provenance for these observations, not continuing current/canonical mutation authority; supervisor Gate 2B must enumerate a fresh approved writable session and repeat every literal preflight before applying analysis changes.

- Exact modeled function: `[0x004cfe00,0x004cfe5f)`, size `0x5f` / 95 bytes, 29 instructions, five basic blocks, cyclomatic complexity `2`.
- Exact byte SHA256: `8E5672211BCC387BA09BB34B061A9EF01131C6A379B9ED98DE690596CCE53DC9`.
- Snapshot identity/type: `sub_4CFE00`; item declaration `Pane *__thiscall(Pane *Block, char)`; analyzer prototype `_DWORD *__thiscall(_DWORD *Block, char)`.
- Complete snapshot frame: saved-register row `+0x4`, size `4`, `_DWORD`; return-address row `+0x8`, size `4`, `_UNKNOWN *`; hidden flag row `arg_0 +0xc`, size `4`, `_DWORD`.
- Snapshot comment prestate: entry address regular absent; entry address repeatable absent; function regular absent; function repeatable absent. Internal address-regular comments are exactly `int` at `0x004cfe03` and `block` at `0x004cfe3a`; both internal repeatable channels are absent.
- Proposed destination name collision was absent in the dated snapshot, but this must be rechecked at Gate 2B rather than treated as overwrite authority.

## Exact ABI And Flag Semantics

The decompiler's `char` rendering is not the call ABI. The frame reserves four bytes at `+0xc`, both adjustor thunks preserve the four-byte `[esp+4]` argument, and `retn 4` removes one four-byte argument. `unsigned int deletingFlags` is therefore the safest source-facing analysis type; the body reads its low byte only because the tested masks fit there.

The wrapper always restores the three `TabPane` vptr views, clears `g_pTabPane`, invokes `Pane__Destructor` at `0x00544580`, and returns the original complete-object `this`. Storage disposition follows this exact truth table:

| `deletingFlags & 0x1` | `deletingFlags & 0x4` | Post-destruction action |
| --- | --- | --- |
| clear | clear | Return the original object pointer without freeing storage. |
| set | clear | Call `NexusTK_operator_delete_wrapper` at `0x004f4ac0`, then return the original pointer. |
| clear | set | Pass `this` and complete-object size `0xf8` to the protected one-byte runtime no-op at `0x0041b6a0`; do not free. |
| set | set | The `0x4` path suppresses normal free, calls the same protected no-op with `this` and `0xf8`, and returns without deallocation. |

The `0x4` path is not vector delete, sized delete, or another deallocator: its callee is exactly one byte `c3`, has 682 project refs, and returns immediately. Complete-object size `0xf8` independently agrees with the `TabPane`/`Pane` UDT size, Singleton empty-base placement at `+0xf8`, and the main-UI caller's 248-byte allocation.

## Xref And Lifecycle Inventory

| Entity | Exact refs | Lifecycle meaning |
| --- | --- | --- |
| Wrapper `0x004cfe00` | code refs `0x004cfd88`, `0x004cfd93`; data ref `0x0061b4d0` | EventHandler/TimerHandler adjusted deleting entries plus the primary virtual deleting slot. |
| Primary vtable `0x0061b4d0` | constructor `0x004cf9ab`; ordinary destructor `0x004cf9d0`; wrapper `0x004cfe06` | Exactly the three primary-view lifecycle writers. |
| EventHandler vtable `0x0061b51c` | constructor `0x004cf9b1`; ordinary destructor `0x004cf9d6`; wrapper `0x004cfe0c` | Exactly the three `+0xa0` view lifecycle writers; cell points to the A0 adjustor. |
| TimerHandler vtable `0x0061b54c` | constructor `0x004cf9bb`; ordinary destructor `0x004cf9e0`; wrapper `0x004cfe16` | Exactly the three `+0xa4` view lifecycle writers; cell points to the A4 adjustor. |
| `g_pTabPane` `0x0069adfc` | reads `0x0049e5e6`, `0x00504936`; constructor publish `0x004cf9a4`; ordinary clear `0x004cf9ea`; wrapper clear `0x004cfe20` | Exact five-ref singleton publication, teardown, and consumer set. |

The wrapper's three callees are inherited ordinary `Pane__Destructor` at `0x00544580`, project operator-delete wrapper `0x004f4ac0`, and the protected runtime no-op `0x0041b6a0`. The base call does not transfer ownership to `Pane`; the three class-specific vptr stores, singleton clear, adjusted entries, and complete-object size bind this wrapper to `TabPane`.

## Boundary And Hash Evidence

- Exact function range is `[0x004cfe00,0x004cfe5f)`. The last instruction starts at `0x004cfe5c` and ends at `0x004cfe5f` with `retn 4`.
- The predecessor [UID:00034V][0x004cfda0-0x004cfdff.IconsPaneScalarDeletingDestructor](by-memory/0x004cfda0-0x004cfdff.IconsPaneScalarDeletingDestructor.md) ends at `0x004cfdff`; the byte `0x004cfdff-0x004cfe00` is one external `0xcc` preboundary byte.
- The byte `0x004cfe5f-0x004cfe60` is one external `0xcc` postpadding byte with SHA256 `1DD8312636F6A0BF3D21FA2855E63072507453E93A5CED4301B364E91C9D87D6`.
- [UID:000171][0x004cfe60-0x004cffaf.IdleWatcher](by-memory/0x004cfe60-0x004cffaf.IdleWatcher.md) begins at `0x004cfe60`. No split, merge, ignored conversion, range extension, or parent change is warranted.

## Ownership And Source Placement

Direct semantic ownership and emitter context remain [UID:0000EB][TabPane](by-class/TabPane.md), routed through [UID:0000OF][TabPane](by-file/TabPane.md) to `NexusTK/ui/panels/TabPane.cpp`. [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md) owns the sole human `TabPane::~TabPane()` definition; this page supplies only the exact existing compiler-coverage comment.

Reject `Pane`, `IconsPane`, the parent-blank mixed [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md), a standalone compiler/runtime file, and no-owner treatment. Physical adjacency to `IconsPane` does not outweigh the separate vtables, singleton, thunks, exact boundary, and established source route.

## Negative Evidence

- Target VA bytes occur exactly once, at primary vtable cell `0x0061b4d0`; target RVA bytes `00 FE 0C 00` have zero matches.
- There is no ordinary source caller: all three incoming routes are compiler vtable/thunk routes, as expected for a deleting wrapper.
- No `IconsPane` vtable or `g_pIconsPane` reference appears in this function.
- The `0x4` callee is a one-byte no-op, not vector/sized deletion or another free route.
- The hidden argument is a four-byte stack slot despite the old `char` rendering.
- No source-quality reason supports handwritten vptr stores, explicit base-destructor calls, delete helpers, guard calls, flags, thunks, or returned-`this` mechanics.

## IDA Analysis Disposition

At the dated snapshot, the safe source-facing analysis recommendation was `TabPane__ScalarDeletingDestructor` with type `TabPane *__thiscall TabPane__ScalarDeletingDestructor(TabPane *this, unsigned int deletingFlags)`, an exact regular function comment describing the three vptr restores, singleton clear, Pane teardown, mask `0x1`/`0x4` behavior, `0xf8` no-op path, and human `virtual TabPane::~TabPane()` source cause, plus clearing only stale address-regular `int`/`block` comments. Entry regular/repeatable and function-repeatable channels remain protected absent; bytes, bounds, xrefs, frame structure, vtables, global, and callees are no-change dependencies.

B007 did not mutate IDA. These operations remain supervisor-owned C17/C30/C31 work for Gate 2B and require fresh session selection, literal prestate/collision checks, exact endpoint calls, save, and persisted readback. Until that occurs, `sub_4CFE00`, weak type, and stale internal comments are dated historical analysis state rather than a claimed current result.

## Ownership Decision

Attach to [UID:0000EB][TabPane](by-class/TabPane.md). The wrapper writes `TabPane` vtables, clears the `TabPane` singleton, and is reached by `TabPane` adjustor thunks and primary vtable data. No `IconsPane` state is touched.

Do not reclassify this page to no-owner/non-emitting: rebuilding `TabPane` must still regenerate this scalar deleting wrapper from the virtual destructor declaration and class layout. The no-source-body decision only prevents duplicate handwritten `TabPane::~TabPane()` emission.

## Score Rationale

Completion is `92` and confidence is `94`. The page now records the exact 95-byte hash/range, 29 instructions/five blocks, complete four-byte hidden-flag frame, literal comment prestates, exact mask truth table, all three callees, returned-`this` ABI, `0xf8` complete-object proof, three incoming routes, vtable/singleton lifecycle parity, source placement, predecessor/postpadding/successor boundaries, generated one-body policy, rejected alternatives, and safe supervisor IDA handoff. No behavior, owner, emitter, range, liveness, source-placement, or C++ blocker remains.

The score remains below `95` because no original private symbol or hidden-parameter token survives, exact compiler-version spelling cannot be proven, and original physical co-location with `IconsPane` is inferred. Those bounded lexical/provenance limits do not justify raw labels, reduced reconstruction routing, or handwritten compiler mechanics.

## Reconstruction Notes

Do not emit a handwritten `TabPane::~TabPane()` body from this page. The formal C++ is intentionally comment-only:

```cpp
// Compiler-generated scalar deleting destructor for TabPane; source is TabPane::~TabPane().
```

The source-visible destructor effect is emitted by [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md). This page documents the compiler-generated scalar deleting wrapper that repeats the vtable restoration and singleton clear before inherited base cleanup and optional storage deletion. The delete-flag logic and `0x004f4ac0` free path should be regenerated by the compiler/runtime from `virtual ~TabPane()`, not hand-authored as ordinary project logic.

## Cross-References

- [UID:0000EB][TabPane](by-class/TabPane.md)
- [UID:0000OF][TabPane](by-file/TabPane.md)
- [UID:0000SE][g_pTabPane](by-global/g_pTabPane.md)
- [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md)
- [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md)

## Changes

- 2026-08-10 B007 Gate-1 accepted implementation callback:
  - Changed to: `COMPLETION:92`, `CONFIDENCE:94`; owner `0000EB`, reconstructable true, emitter `0000EB`, blank optional position, exact comment-only formal CPP, and blank formal H remain unchanged.
  - Added: dated MCP snapshot provenance; exact 95-byte SHA256/range/CFG/frame/comment prestate; four-byte `deletingFlags` ABI and complete 0x1/0x4 truth table; exact three-callee, three-incoming-route, vtable-writer, and five-ref singleton inventories; object-size, boundary, pointer-pattern, negative, ownership/source-placement, no-duplicate-source, and supervisor-only IDA disposition evidence.
  - Historical preservation: the 2026-07-02 correction below remains valid and searchable. Its old `sub_4CFE00`/weak-type state is dated evidence, not final source spelling; the duplicate handwritten destructor body remains rejected.

- 2026-07-02 B015 source-quality implementation:
  - Before: `COMPLETION:85`, `CONFIDENCE:88`, and formal C++ duplicated `TabPane::~TabPane()` with `g_pTabPane = NULL;`.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:91`, class-owned scalar deleting destructor glue with comment-only formal C++.
  - Evidence: the wrapper keeps exact evidence for `sub_4CFE00` at `0x004cfe00-0x004cfe5f`, three `TabPane` vtable restores, `g_pTabPane` clear at `0x004cfe20`, inherited `Pane::~Pane()` cleanup at `0x00544580`, optional delete helper `0x004f4ac0`, guard helper path, primary vtable and adjustor-thunk routing, and successor `IdleWatcher` boundary. Ordinary source teardown now belongs to [UID:00034M][0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper](by-memory/0x004cf9d0-0x004cf9f9.TabPaneCleanupHelper.md) `TabPane::~TabPane()`, so this page should not emit a second destructor body.
