*** UID:00014I | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004P | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004P | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ExceptionHandler::GetLogicalAddress(PVOID address,
                                         PTSTR moduleName,
                                         DWORD moduleNameLength,
                                         DWORD &sectionNumber,
                                         DWORD &sectionOffset)
{
    MEMORY_BASIC_INFORMATION memoryInformation;
    if (VirtualQuery(address, &memoryInformation, sizeof(memoryInformation)) == 0) {
        return false;
    }

    const DWORD moduleBase = (DWORD)memoryInformation.AllocationBase;
    if (GetModuleFileName((HMODULE)moduleBase,
                          moduleName,
                          moduleNameLength) == 0) {
        return false;
    }

    PIMAGE_DOS_HEADER dosHeader = (PIMAGE_DOS_HEADER)moduleBase;
    PIMAGE_NT_HEADERS ntHeaders =
        (PIMAGE_NT_HEADERS)(moduleBase + dosHeader->e_lfanew);
    PIMAGE_SECTION_HEADER sectionHeader = IMAGE_FIRST_SECTION(ntHeaders);
    const DWORD relativeAddress = (DWORD)address - moduleBase;

    for (unsigned int index = 0;
         index < ntHeaders->FileHeader.NumberOfSections;
         ++index, ++sectionHeader) {
        const DWORD sectionStart = sectionHeader->VirtualAddress;
        const DWORD sectionEnd =
            sectionStart + std::max(sectionHeader->SizeOfRawData,
                                    sectionHeader->Misc.VirtualSize);

        if (relativeAddress >= sectionStart && relativeAddress <= sectionEnd) {
            sectionNumber = index + 1;
            sectionOffset = relativeAddress - sectionStart;
            return true;
        }
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private static GetLogicalAddress source lineage, exact 149-byte hash/ABI, six call sites, GetModuleFileNameW dispatch identity, VirtualQuery/PE section walk, inclusive one-based section/offset outputs, failure-side mutations, and predecessor/successor padding verified. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004abfa0-0x004ac035 ExceptionHandlerGetLogicalAddress

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: private static `ExceptionHandler::GetLogicalAddress` method
- Likely source module: [UID:0000J8][ExceptionHandler](by-file/ExceptionHandler.md)
- Confidence: very strong for behavior, exact bounds, ABI, six callers, callee identities, source-facing method name, PE-section walk, and formal source; strong for descriptive parameter/local spellings.
- Rebuild handling: source-authored private static class method emitted from this exact child page into `NexusTK/platform/ExceptionHandler.cpp`.
- The parent [UID:00014D][0x004ab480-0x004ac89a.ExceptionHandler](by-memory/0x004ab480-0x004ac89a.ExceptionHandler.md) remains an aggregate with blank formal C++; this exact child owns the source body.

## Behavior

This private static method resolves a process address to module-path text plus a section/logical offset pair for crash-report output. Exact source lineage and the x86 ABI establish the source-facing signature as `bool ExceptionHandler::GetLogicalAddress(PVOID, PTSTR, DWORD, DWORD &, DWORD &)`. The body:

- calls `VirtualQuery` on the address to obtain the allocation base;
- calls the PlatformApi-owned `GetModuleFileNameW` dispatch slot for the module path associated with that allocation base, represented by generic source-era `GetModuleFileName`;
- treats the allocation base as a PE image and walks the section table;
- finds the section containing the address-relative offset;
- writes the one-based section index and section-relative offset to the two output parameters;
- returns `false` when the address cannot be queried, the module path cannot be written, or no section contains the address, and returns `true` in `AL` after both DWORD outputs are stored.

The PE walk is now pinned to these source-level fields:

| Operation | Binary evidence | Meaning |
| --- | --- | --- |
| `VirtualQuery(address, &mbi, 0x1c)` | `0x004abfaf-0x004abfb3` | Obtains the allocation base for the fault/stack address. |
| `GetModuleFileNameW(allocationBase, moduleName, moduleNameLength)` | `0x004abfbd-0x004abfc7` | PlatformApi dispatch slot `0x0069be74` fills the caller-provided wide module/path buffer; all six callers pass capacity `0x104` / 260 characters. |
| `*(base + 0x3c)` | `0x004abfd1` | Reads DOS `e_lfanew` to find the NT headers. |
| `word[nt + 0x06]` | `0x004abfe0` | Reads `NumberOfSections`; zero sections fail. |
| `word[nt + 0x14]` and `+0x28` cursor bias | `0x004abfda`, `0x004abfeb` | Positions the loop cursor at each section header's `SizeOfRawData` field. |
| `section.VirtualAddress`, `max(SizeOfRawData, VirtualSize)` | `0x004abff0-0x004ac005` | Finds the section whose RVA contains the requested address. |
| output stores | `0x004ac024`, `0x004ac029` | Writes one-based section number and section-relative offset. |

## ABI And Source Contract

- Entry: `0x004abfa0`; end-exclusive: `0x004ac035`; exact size: `0x95` / 149 bytes.
- Exact SHA-256: `ceb8bc445299e5e1f8496c8033c79de83b9f185d7f4abffa634cc5749e666eaa`.
- Shape: 67 instructions, 9 basic blocks, 13 control-flow edges, cyclomatic complexity 6.
- Calling convention: five 4-byte stack arguments and callee cleanup `ret 0x14`; no `ECX` receiver is read because the source method is static.
- Stack: one 28-byte `_MEMORY_BASIC_INFORMATION` local at `[ebp-0x1c]`.
- Return: the Nexus source contract is `bool`; success writes `AL=1`, failure clears `AL`, and callers ignore the result. A same-file full-EAX `BOOL` control at `0x004ac040` distinguishes this method's Boolean lowering.
- Parameters:
  - `PVOID address`;
  - `PTSTR moduleName`;
  - `DWORD moduleNameLength`, measured in characters;
  - `DWORD &sectionNumber`;
  - `DWORD &sectionOffset`.
- Source lineage: the adjacent private static [UID:00014H][0x004abd10-0x004abe4a.ExceptionHandlerGetExceptionString](by-memory/0x004abd10-0x004abe4a.ExceptionHandlerGetExceptionString.md) and exact Microsoft `MSJEXHND` header/source lineage establish the class-private static name and declaration shape. Nexus adapts the published return type to `bool` while preserving the source algorithm.

## Evidence

- Fresh mandatory MCP evidence collected for the accepted report used healthy database `64c11373` at evidence time after `idb_list`, `server_health`, and bounded target probes succeeded.
- IDA MCP confirms `sub_4ABFA0` at exact range `[0x004abfa0,0x004ac035)`, size `0x95`, with 67 instructions and the documented CFG.
- Direct callees are imported `VirtualQuery` at `0x0060d2dc` and dispatch slot `0x0069be74`. Seven slot xrefs include three initializer stores of imported `GetModuleFileNameW` and four consumers; the target consumer passes `AllocationBase`, caller buffer, and character capacity.
- Direct target xrefs are exactly six calls: `0x004ab939`, `0x004abc52`, `0x004ac190`, `0x004ac3e1`, `0x004ac440`, and `0x004ac7b4`.
- 2026-06-01 IDA MCP recheck confirms `0x004abfa0` as `sub_4ABFA0`, size `0x95`; callers are the crash-report writer at `0x004ab939` and `0x004abc52`, plus the stack-walk helper at `0x004ac3e1` and `0x004ac440`.
- 2026-06-01 `callees` confirms `VirtualQuery` and the module-path helper slot at `dword_69BE74`.
- 2026-06-06 `lookup_funcs` reconfirms `sub_4ABFA0`, size `0x95`; `0x004ac035` is not a function start, and [UID:00014D][0x004ab480-0x004ac89a.ExceptionHandler](by-memory/0x004ab480-0x004ac89a.ExceptionHandler.md) documents the next raw helper at `0x004ac040`.
- The 2026-06-06 four-call result was limited to modeled containing functions. The complete current bounded xref pass adds raw parent-contained calls `0x004ac190` and `0x004ac7b4`; the current total is six calls in four modeled/raw containing regions.
- 2026-06-06 live `callees` reports only `VirtualQuery` and external helper slot `0x0069be74`.
- 2026-06-06 `decompile` and `disasm` confirm the `_MEMORY_BASIC_INFORMATION` stack frame, `VirtualQuery` length `0x1c`, PE `e_lfanew` read, section count gate, 40-byte section-header stride, `max(SizeOfRawData, VirtualSize)` containment test, and success stores.
- Exact section logic reads DOS `e_lfanew`, NT `NumberOfSections`, `SizeOfOptionalHeader`, and 40-byte section headers; it computes `address - AllocationBase`, uses `max(SizeOfRawData, Misc.VirtualSize)`, and deliberately accepts `relativeAddress == sectionStart + sectionExtent`.
- The method does not validate DOS/NT signatures, machine type, optional-header magic, section names, full-buffer truncation, arithmetic overflow, or a half-open section end.
- Bytes at `0x004ac035-0x004ac03f` are eleven `0xcc` alignment bytes before the raw ImageHlp resolver helper body at `0x004ac040`.

## Caller Inventory

| Call | Containing item | Exact inputs, initialization, and consumption |
| --- | --- | --- |
| `0x004ab939` | [UID:00014G][0x004ab870-0x004abd10.ExceptionCrashReportWriter](by-memory/0x004ab870-0x004abd10.ExceptionCrashReportWriter.md) | Passes `ExceptionRecord->ExceptionAddress`, an uninitialized `WCHAR[260]`, capacity 260, and uninitialized section/offset locals. The Boolean result is ignored and path/section/offset are formatted. |
| `0x004abc52` | UID00014G fallback frame walk | Passes the fallback frame PC, zero-initialized `wchar_t[260]`, capacity 260, section `0`, and offset `0`. The result is ignored; outputs are formatted, the path is compared with the executable path, and the offset can be printed separately. |
| `0x004ac190` | UID00014D raw stack/logical formatter | Passes the current PC, zero-initialized `wchar_t[260]`, capacity 260, section `0`, and offset `0`; all outputs feed `%08X  %08X  %04X:%08X %ws`. |
| `0x004ac3e1` | [UID:00014J][0x004ac230-0x004ac4cb.ImageHlpStackWalkHelper](by-memory/0x004ac230-0x004ac4cb.ImageHlpStackWalkHelper.md) | Passes `STACKFRAME.AddrPC.Offset` with zero-initialized module buffer, section, and offset; ignores the result and formats the logical address. |
| `0x004ac440` | UID00014J executable-relative check | Reuses the same PC, resets the module buffer and offset, deliberately retains the prior section value, ignores the result, compares the path to the executable path, and prints the offset when equal. |
| `0x004ac7b4` | UID00014D raw symbol/detail helper | Passes the current address, zero-initialized `wchar_t[260]`, capacity 260, a section local, and offset `0`; ignores the result and consumes only offset for `L"%08X -> "`. |

The ignored Boolean return is behaviorally important. The method itself does not clear caller outputs. `VirtualQuery` failure occurs before any output mutation; `GetModuleFileName` may mutate the module buffer before returning zero; a later PE/section miss leaves module text populated while both DWORD outputs retain their caller-provided values.

## Touched State

| State | Access | Notes |
| --- | --- | --- |
| caller module/path buffer | passed to PlatformApi-owned `GetModuleFileNameW` through `0x0069be74` | The source-facing body uses generic `GetModuleFileName`; capacity is characters, not bytes. |
| process image headers | reads from `AllocationBase` returned by `VirtualQuery` | Parses DOS/NT headers and 40-byte section headers in the mapped module. |
| `sectionNumber` | writes `sectionIndex + 1` only on success | One-based section number for crash report text. |
| `sectionOffset` | writes `RVA - section.VirtualAddress` only on success | Logical offset inside the containing section. |

## Range And Padding

- Exact target: `[0x004abfa0,0x004ac035)`, size `0x95` / 149.
- Predecessor `0x004abe4a-0x004abf9c` is parent-only `GetExceptionString` switch/data; `0x004abf9c-0x004abfa0` is four-byte `cc cc cc cc` alignment with SHA-256 `8843b54d2df63ca265cf4a05d27dd2b29a74fb476d296dd44a0e171d74b441ca`.
- Successor `0x004ac035-0x004ac040` is eleven-byte `0xcc` alignment with SHA-256 `3682c0a62515050101266cc639bf81f5d8c5395f49e7da93fcc22603018e0943`; modeled `sub_4AC040` starts at `0x004ac040`.
- Combined `[0x004abf9c,0x004ac040)` SHA-256 is `8462e26d5b3c2436f186f46a571d776c3dea72a6ec4a2b53fb8b01dfb8745fc5`.
- No target-internal data or padding exists. No split, merge, extension, shrink, ignored reclassification, or nesting change is warranted; both pads remain parent-only and `Nested:0` remains exact.

## Ownership Notes

The source identity is private static `ExceptionHandler::GetLogicalAddress`, owned and emitted by [UID:00004P][ExceptionHandler](by-class/ExceptionHandler.md) and routed through [UID:0000J8][ExceptionHandler](by-file/ExceptionHandler.md) at `NexusTK/platform/ExceptionHandler.cpp`. All six calls remain inside the ExceptionHandler diagnostic family. Generic PE utility, PlatformApi helper ownership, Application, Crasher, ImageHlp, runtime/import, and a separate crash-diagnostics translation unit are rejected: PlatformApi owns only the `GetModuleFileNameW` dispatch storage, and no non-crash caller or independent utility source identity exists.

## Score Rationale

`92/94` reflects closure of the former source-name, signature, caller-count, slot-identity, ownership, output-contract, source-placement, and formal-C++ blockers. Exact range/hash/ABI/CFG, six calls, PE fields, inclusive containment, failure mutations, padding, source lineage, and emitter route are proven. The remaining confidence cap is limited to descriptive Nexus parameter/local spellings and unrelated complete-class declaration debt on UID00004P.

## Negative Evidence And Historical Corrections

- The earlier four-caller statement counted only modeled callers and omitted two exact raw parent-contained calls; six is the current complete count.
- `0x0069be74` is not an unknown module helper. Current PlatformApi/WideApi documentation and initializer xrefs identify it as `GetModuleFileNameW`.
- The function is not file-static merely because it has no `this`; the exact source header proves a private static class member, which naturally has no `ECX` receiver.
- The two DWORD outputs are references in source, not nullable pointer semantics in the class declaration.
- The Nexus return is source-facing `bool`, not a required `BOOL`; exact `AL` lowering and the same-file full-EAX control support this distinction.
- No output is cleared on entry and no failure path writes either DWORD output.
- No raw dispatch-slot name, address, IDA label, PE-table bytes, vtable/RTTI, security-cookie scaffolding, or decompiler temporary belongs in human C++.
- The old `ExceptionLogicalAddressHelper` title remains historical provenance only; the implemented source-facing title is `ExceptionHandlerGetLogicalAddress`.

## Changes

- 2026-07-16 B003 accepted UID00014I callback:
  - Renamed the page through the validator-aware UID-preserving workflow to `ExceptionHandlerGetLogicalAddress`.
  - Applied `86/91 -> 92/94`, retained owner/emitter UID00004P, reconstructable true, blank optional position, exact range, and `Nested:0`.
  - Installed the exact accepted private static `GetLogicalAddress` formal body and incorporated the complete target hash/ABI/CFG, six-call inventory, `GetModuleFileNameW` slot identity, PE walk, failure mutations, padding, source lineage, rejected alternatives, and historical corrections.
- 2026-06-06 A009 live IDA PE-section refresh:
  - What existed before: the page summarized the logical-address lookup, retained one generated-source evidence bullet, and did not record the PE field offsets, output stores, tail padding, or current caller/callee refresh.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:91`; removed the generated-source evidence bullet and added the PE walk table, touched-state table, exact caller/callee list, padding boundary, and score rationale.
  - Evidence: IDA MCP `lookup_funcs`, `callers`, `callees`, `decompile`, `disasm`, and `py_eval` byte/data-reference extraction on 2026-06-06 for `0x004abfa0`.
- What existed before: this logical-address helper remained an unassigned `50/65` low-score page.
- Changed to: marked reconstructable, parented to [UID:00004P][ExceptionHandler](by-class/ExceptionHandler.md), and raised to `82/88`.
- Evidence: live 2026-06-01 IDA MCP confirmed the exact boundary, crash-report/stack-walk callers, and `VirtualQuery`/module-path callee set.

## Cross-References

- [UID:00014D][0x004ab480-0x004ac89a.ExceptionHandler](by-memory/0x004ab480-0x004ac89a.ExceptionHandler.md)
- [UID:0000J8][ExceptionHandler](by-file/ExceptionHandler.md)
- [UID:00014G][0x004ab870-0x004abd10.ExceptionCrashReportWriter](by-memory/0x004ab870-0x004abd10.ExceptionCrashReportWriter.md)
- [UID:00014J][0x004ac230-0x004ac4cb.ImageHlpStackWalkHelper](by-memory/0x004ac230-0x004ac4cb.ImageHlpStackWalkHelper.md)
- [UID:0001QB][client_crash_diagnostics](by-meta/client_crash_diagnostics.md)
