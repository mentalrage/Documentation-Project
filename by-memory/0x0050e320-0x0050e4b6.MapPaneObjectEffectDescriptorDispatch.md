*** UID:000232 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::RequestObjectEffect(
    unsigned int objectId,
    int effectDescriptorId,
    int timerAction,
    bool renderAfterLivingObjects)
{
    EffectInfo effectInfo;
    EffectObjImageLibGetEffectInfo(&effectInfo, effectDescriptorId);

    ObjectPane *objectPane = m_objectList->FindByObjectId(objectId);
    if (objectPane == NULL)
        return;

    LivingObjectPane *target =
        static_cast<LivingObjectPane *>(objectPane);

    if (effectInfo.secondaryFrameCount > 0)
    {
        bool reused = false;

        if (effectInfo.secondaryRangeStart >= 0 &&
            effectInfo.secondaryRangeEnd >= 0)
        {
            const int count = target->m_secondaryEffectObjectPanes->GetCount();
            for (int index = count - 1; index >= 0; --index)
            {
                EffectObjectPane *effectObject =
                    *static_cast<EffectObjectPane **>(
                        target->m_secondaryEffectObjectPanes->GetElementAt(index));
                if (effectObject != NULL &&
                    effectObject->m_effectId == effectInfo.effectKeyOrId &&
                    effectObject->IsLooping())
                {
                    ApplyEffectObjectPaneTimerAction(effectObject, timerAction);
                    reused = true;
                    break;
                }
            }
        }

        if (!reused)
        {
            MapPoint position;
            target->GetMapPosition(&position);
            CreateEffectObjectPane(
                target,
                position.row,
                position.column,
                effectInfo,
                timerAction,
                0,
                renderAfterLivingObjects);
        }
    }

    if (effectInfo.primaryFrameCount > 0)
    {
        bool reused = false;

        if (effectInfo.primaryRangeStart >= 0 &&
            effectInfo.primaryRangeEnd >= 0)
        {
            const int count = target->m_primaryEffectObjectPanes->GetCount();
            for (int index = count - 1; index >= 0; --index)
            {
                EffectObjectPane *effectObject =
                    *static_cast<EffectObjectPane **>(
                        target->m_primaryEffectObjectPanes->GetElementAt(index));
                if (effectObject != NULL &&
                    effectObject->m_effectId == effectInfo.effectKeyOrId &&
                    effectObject->IsLooping())
                {
                    ApplyEffectObjectPaneTimerAction(effectObject, timerAction);
                    reused = true;
                    break;
                }
            }
        }

        if (!reused)
        {
            MapPoint position;
            target->GetMapPosition(&position);
            CreateEffectObjectPane(
                target,
                position.row,
                position.column,
                effectInfo,
                timerAction,
                1,
                renderAfterLivingObjects);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

## UID0003Z4 Typed Effect-List Synchronization - 2026-07-14

- The complete formal body now uses the established `List *` ABI, dereferences pointer-element storage returned by `GetElementAt`, and names `m_secondaryEffectObjectPanes`/`m_primaryEffectObjectPanes` according to EffectObjectPane constructor selector and registration evidence.
- Reuse compares public `EffectObjectPane::m_effectId` with `EffectInfo::effectKeyOrId`, requires `IsLooping()`, and applies the timer action to only the first matching reverse-scanned pane. Otherwise creation uses an exact `MapPoint` returned by ObjectPane.
- [UID:0004QL][0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane](by-memory/0x0053ae10-0x0053ae66.LivingObjectPaneAddSecondaryEffectObjectPane.md) and [UID:0004QK][0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane](by-memory/0x0053adb0-0x0053ae06.LivingObjectPaneAddPrimaryEffectObjectPane.md) own the corresponding insertion source. Historically the 2026-07-14 bounded list synchronization retained `89/91`; the accepted UID0003TF integration now closes the receiver/final-flag/formal dependency and raises this dispatcher to `92/94`.
- Historical `.Count()`/`.At()`, value-container, type2/type4 field, null-as-integer, and split map-X/map-Y output forms are superseded source drafts rather than current declarations.

*** Item Summary: Source-ready MapPane::RequestObjectEffect dispatcher at exact range `0x0050e320-0x0050e4b6`; performs EffectInfo copy, ObjectList lookup, typed LivingObjectPane conversion, secondary-before-primary List reuse scans, timer-action reuse, and machine-accurate MapPoint row-before-column UID0003TF creation with renderAfterLivingObjects propagation. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0050e320-0x0050e4b6 MapPane Request Object Effect

## UID0003TF Typed Create-Helper Integration - 2026-07-15

- Current metadata is `92/94`, owner/emitter UID00007Q, reconstructable true, blank explicit position, and `Nested:0`. The exact `[0x0050e320,0x0050e4b6)` range, `0x196` size, `retn 0x10`, ten-byte successor alignment, MapPane owner, and MapPane.cpp route are unchanged.
- The complete managed body now names its fourth parameter `renderAfterLivingObjects`, keeps the initial ObjectList result as `ObjectPane *objectPane`, and uses a source-legal `LivingObjectPane *target` for the proven primary/secondary List fields and UID0003TF calls. This is a type synchronization, not an ownership move.
- Secondary descriptors are processed before primary descriptors. A positive frame count enters that pass; nonnegative start/end ranges enable a reverse List scan; each element is a stored `EffectObjectPane *`; reuse requires matching `m_effectId` and `IsLooping()` and applies `timerAction` to the first match. Negative ranges skip reuse but do not suppress creation.
- On a miss, `GetMapPosition` supplies `MapPoint::row/column`, then [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md) receives the typed target, row before column, by-value `EffectInfo`, sequence `0` or `1`, and unchanged render-after flag. UID0003TF's frame offsets/`retn 0x4c`, caller pushes, constructor forwarding, `+0x14d` store, and complementary render passes close the former coordinate/flag/signature blockers.
- The four direct callers of this dispatcher remain `0x005116cc`, `0x00511d34`, `0x0053bac5`, and `0x005a2ee0`; no data/table route or compiler-generated body was found. UID0003TF itself has exactly four internal calls from this dispatcher and the coordinate dispatcher, with no fifth caller.
- Historical/superseded source drafts used `compactFlag`, kept the lookup result as `ObjectPane *target`, introduced a separate `livingTarget`, called UID0003TF with `mapX/mapY`, or described generic type-2/type-4 child lists. Current evidence instead supports the exact managed body, `renderAfterLivingObjects`, `MapPoint::column/row`, and named primary/secondary effect lists. The old `ApplyObjectEffect` alternate remains rejected.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: private `MapPane` class method.
- Source-facing name: `MapPane::RequestObjectEffect(unsigned int objectId, int effectDescriptorId, int timerAction, bool renderAfterLivingObjects)`.
- Generated/descriptive aliases: `MapPaneObjectEffectDescriptorDispatch`, historical `sub_50E320`.
- Direct owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md); file owner [UID:0000L3][MapPane](by-file/MapPane.md).
- Confidence: very strong for function boundary, MapPane ownership, caller set, descriptor lookup, object lookup, child-list scans, create/reuse/timer helper calls, and first-draft C++ shape; source names remain inferred rather than original-symbol proven.
- Rebuild handling: `source-authored` MapPane object-effect request dispatcher emitted through `NexusTK/map/MapPane.cpp`.
- C++ handling: complete managed C++ supplied. The body uses the established `List *` ABI with `GetCount()` and pointer-slot `GetElementAt()` access; no separate body-only draft or list-type blocker remains.

## Address Range

- Start: `0x0050e320`
- End: `0x0050e4b6`
- IDA model: entry at `0x0050e320`, size `0x196`.
- Boundary evidence: IDA MCP reports a modeled function at `0x0050e320-0x0050e4b6`; byte review shows a `retn 0x10` at the end and ten `0xcc` alignment bytes before the next MapPane packet-handler function at `0x0050e4c0`.

## Signature And Behavior

`MapPane::RequestObjectEffect` is a `ret 0x10` thiscall-style method:

```text
ECX       MapPane *this
[ebp+08]  objectId
[ebp+0c]  effectDescriptorId
[ebp+10]  timerAction
[ebp+14]  renderAfterLivingObjects / caller flag byte
```

The method copies a 52-byte [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md) record through [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md), then looks up the target object by id through the MapPane object list at `MapPane +0x424` / `m_objectList` and [UID:0002CD][0x00532370-0x00532443.ObjectListFindByObjectId](by-memory/0x00532370-0x00532443.ObjectListFindByObjectId.md). If the target is missing, it returns without side effects.

If the target exists, the descriptor drives two passes in this order:

- Secondary sequence first: `secondaryFrameCount`, `secondaryRangeStart`, and `secondaryRangeEnd` from `EffectInfo +0x28/+0x2c/+0x30`; reuse scans the target's `m_secondaryEffectObjectPanes` list at `+0x174`; missed reuse calls [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md) with `sequenceIndex == 0`.
- Primary sequence second: `primaryFrameCount`, `primaryRangeStart`, and `primaryRangeEnd` from `EffectInfo +0x10/+0x14/+0x18`; reuse scans the target's `m_primaryEffectObjectPanes` list at `+0x170`; missed reuse calls `MapPane::CreateEffectObjectPane` with `sequenceIndex == 1`.

Each pass is skipped when its frame count is non-positive. Nonnegative range bounds enable a backward reuse scan over the corresponding attached-object list; negative range bounds skip the reuse scan but do not suppress creation when the frame count is positive. Candidate reuse requires both:

- `EffectObjectPane::m_effectId` at `+0x148` equals `EffectInfo::effectKeyOrId` at `+0x00`.
- [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) returns true from `m_looping` at `+0x14c`.

When a reusable child is found, source-ready [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md) applies signed `timerAction`: `-1` performs ordinary source-level delete, `-2` cancels timer event id `2`, `0` no-ops, and every other signed value enters the guarded query/replacement path when inferred `EffectObjectPane::m_timerExtensionLocked +0x14e` is false. The helper preserves zero-on-miss, its signed unscaled remaining-time comparison, cancel-before-schedule, and exact `1000 * timerAction`, `0,0` replacement.

When no reusable child exists, the method obtains target map coordinates through [UID:0003XP][0x005374d0-0x005374eb.ObjectPaneGetMapPosition](by-memory/0x005374d0-0x005374eb.ObjectPaneGetMapPosition.md), then calls `MapPane::CreateEffectObjectPane(target, position.row, position.column, effectInfo, timerAction, sequenceIndex, renderAfterLivingObjects)`. `GetMapPosition` writes row at `+0` and column at `+4`; the create helper receives row at frame `+0x0c` and column at `+0x10`. The helper takes `EffectInfo` by value, matching the observed stack copy and `ret 0x4c` ABI; constructor offset `+0x14d` and complementary render passes prove the final flag's role.

## Source-Name Decision

Use `MapPane::RequestObjectEffect`. The method is called by packet handlers and living-object helpers with an object id and descriptor id, and it may create, reuse, extend, cancel, invoke, or no-op depending on descriptor fields and `timerAction`. `MapPane::ApplyObjectEffect` is a plausible alternative from the B008 report, but it is less precise for the request/reuse/cancel shape and would conflict with the existing first-draft caller in [UID:0003Z6][0x0053bab0-0x0053bb1d.LivingObjectPaneNameplateSpecialEffectHelpers](by-memory/0x0053bab0-0x0053bb1d.LivingObjectPaneNameplateSpecialEffectHelpers.md). Keep `ApplyObjectEffect` documented only as a rejected/alternate name unless the supervisor chooses a global rename.

## Caller Evidence

IDA `callers` and `xrefs_to` on 2026-06-06 report exactly four code callers and no data refs:

- `0x005116cc` inside the MapPane spawn/movement/chat packet helper at `0x00511440-0x00511703`; the caller passes packet-derived object id, descriptor id, `timerAction`, and a compact flag derived from descriptor ids `0xa0-0xa2` when an object-effect mode flag is enabled.
- `0x00511d34` inside the MapPane object packet helper at `0x00511c90-0x00511dae`; the caller first verifies an object-list lookup path, then passes object id, descriptor id, `timerAction`, and the same compact descriptor-range flag.
- `0x0053bac5` inside a small object-pane wrapper at `0x0053bab0-0x0053bacb`; the wrapper forwards [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md), target object id, descriptor id `0x87`, `timerAction == -2`, and flag `0`.
- `0x005a2ee0` inside [UID:0003U6][0x005a2e00-0x005a36f0.UserPaneProcessMovement](by-memory/0x005a2e00-0x005a36f0.UserPaneProcessMovement.md); only exact `g_useEpfAssets == 1` forwards [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md), the local object id, descriptor id 201 (`0xc9`), `timerAction == 0`, and flag `false`, then separately copies the same descriptor for UserPane-side cleanup.

This is MapPane-facing behavior even though two callers are outside the main MapPane source range.

## IDA Evidence

- 2026-06-06 `lookup_funcs` confirms `0x0050e320-0x0050e4b6` as a modeled `0x196`-byte function; `0x0050e4b6` is not a function and `0x0050e4c0` begins the following modeled MapPane packet-handler function.
- 2026-06-06 byte review confirms eight `0xcc` bytes before `0x0050e320`, ten `0xcc` bytes at `0x0050e4b6-0x0050e4c0`, and the target return as `retn 0x10`.
- 2026-06-06 `callees` reports six direct callees: [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md), [UID:0002CD][0x00532370-0x00532443.ObjectListFindByObjectId](by-memory/0x00532370-0x00532443.ObjectListFindByObjectId.md), the object coordinate accessor at `0x005374d0`, [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md), the create/apply helper at `0x0050ea30`, and the reuse/timer helper at `0x0050eb90`.
- 2026-06-06 decompilation of the target shows the descriptor copy, MapPane object-list lookup, two descriptor-gated passes, scans over target child collections at offsets `0x174` and `0x170`, child descriptor-id comparisons at offset `0x148`, [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) gates, reuse-helper calls on matches, and create/apply-helper calls on misses.
- 2026-06-18 B003 source-quality reanalysis corrects the callee owner/name: the `0x00538bb0` receiver is an `EffectObjectPane` child, not the target `LivingObjectPane`; `+0x148` is the copied effect descriptor id and `+0x14c` is the loop/reuse-active byte.
- 2026-06-06 decompilation of the create/apply helper shows allocation from the [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md) pool, EffectObjectPane construction, attachment to the target object, insertion into MapPane's object list, [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md), optional timer scheduling, bounds read, and virtual refresh.
- Historical 2026-06-06 decompilation/disassembly wording described `-1` as a callback and the replacement branch as positive-only. The current target-specific audit resolves `-1` as ordinary source delete and proves every remaining signed action reaches the `m_timerExtensionLocked +0x14e` guard before due-tick comparison and possible event-2 replacement.
- 2026-06-19 B008 local PE/Capstone recheck reconfirmed the exact `0x0050e320-0x0050e4b6` range, `0x196`-byte size, `retn 0x10`, ten-byte trailing padding, four rel32 callers, no pointer/table route, no scalar-delete/EH/base-destructor pattern, the same six callee roles, and the MapPane class owner/emitter route. It also rechecked the descriptive-name alternatives and kept final source-name spelling below symbol-proof confidence.
- 2026-06-06 `xrefs_to` for [UID:0001PQ][0x0069b44c-0x0069b450.g_pEffectObjImageLib](by-memory/0x0069b44c-0x0069b450.g_pEffectObjImageLib.md) includes this helper at `0x0050e331` and the local-player caller follow-up at `0x005a2ee5`, matching the documented effect-descriptor provider.

## Helper And Field Notes

- `MapPane::CreateEffectObjectPane(LivingObjectPane *target, int mapRow, int mapColumn, EffectInfo effectInfo, int timerAction, int sequenceIndex, bool renderAfterLivingObjects)` is the create helper at `0x0050ea30`. `EffectInfo` is by value, not `const EffectInfo &`, because the caller pushes the full 52-byte descriptor and the helper returns with `ret 0x4c`. The earlier transposed `mapColumn, mapRow` declaration is retained only as superseded history.
- `MapPane::ApplyEffectObjectPaneTimerAction(EffectObjectPane *effectObjectPane, int timerAction)` is the source-ready timer/reuse helper at `0x0050eb90`. Its body does not read `this`, but all four descriptor-dispatch call sites set `ECX` to `MapPane *`; the private MapPane method ABI remains authoritative.
- `m_secondaryEffectObjectPanes` and `m_primaryEffectObjectPanes` are `LivingObjectPane` Lists containing `EffectObjectPane *` values. The formal C++ keeps the object-list result as `ObjectPane *objectPane`, then uses a `LivingObjectPane *target` for effect-list scans and the typed create helper. This does not move ownership to LivingObjectPane; it expresses the proven field owner and source-legal contract.
- `EffectObjectPane::m_effectId` is the current class field name for the copied `EffectInfo::effectKeyOrId`; do not rename the class field here.
- `timerAction` is intentionally broader than `duration`: `-1`, `-2`, `0`, and every other signed value have distinct source behavior. Do not impose a positivity gate or repair the observed unscaled compare/millisecond scheduling quirk.
- `renderAfterLivingObjects` is supported by the UID0003TF constructor store at `EffectObjectPane +0x14d` and complementary render-pass tests. Original spelling remains inferred, but the semantic role is resolved.

## Boundary Notes

- The preceding [UID:0001AT][0x00506970-0x0050e320.MapPaneInputPacketRenderCore](by-memory/0x00506970-0x0050e320.MapPaneInputPacketRenderCore.md) ends exactly at this function start.
- `0x0050e4b6-0x0050e4c0` is `0xcc` alignment padding tracked in [UID:0000VN][-ignored](by-memory/-ignored.md).
- The following [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md) is a larger packet-handler aggregate beginning at `0x0050e4c0`.

## Score Rationale

Completion is `92` because all control flow, direct callers, descriptor copy flow, object lookup, typed List semantics, create/reuse/timer helper relationships, complete formal body, and integration with the now-source-ready UID0003TF contract are reconstructed.

Confidence is `94` because the boundary, direct callers, callee set, `EffectInfo` fields, secondary/primary `EffectObjectPane *` List mapping, create-helper ABI/body, `renderAfterLivingObjects` constructor/render evidence, timer semantics, and MapPane owner placement are independently supported. Confidence remains below 95 because stripped original lexical spellings cannot be proven.

## Cross-References

- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0001AT][0x00506970-0x0050e320.MapPaneInputPacketRenderCore](by-memory/0x00506970-0x0050e320.MapPaneInputPacketRenderCore.md)
- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)
- [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md)
- [UID:0000QT][g_pEffectObjImageLib](by-global/g_pEffectObjImageLib.md)
- [UID:0001PQ][0x0069b44c-0x0069b450.g_pEffectObjImageLib](by-memory/0x0069b44c-0x0069b450.g_pEffectObjImageLib.md)
- [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md)
- [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md)
- [UID:0002CD][0x00532370-0x00532443.ObjectListFindByObjectId](by-memory/0x00532370-0x00532443.ObjectListFindByObjectId.md)
- [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md)
- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md)
- [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md)
- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)

## UID0003U6 Effect-201 Request Consumer - 2026-07-15

- Existing `89/91` metadata, UID00007Q owner/emitter route, exact formal body, descriptor validation, MapPane effect-list reuse, timer behavior, callers, source name, and unrelated evidence remain unchanged.
- [UID:0003U6][0x005a2e00-0x005a36f0.UserPaneProcessMovement](by-memory/0x005a2e00-0x005a36f0.UserPaneProcessMovement.md) is a direct external consumer at `0x005a2ee0`. It calls `g_activeMapPane->RequestObjectEffect(m_objectId, 201, 0, false)` only when `g_useEpfAssets == 1`.
- ProcessMovement then performs a separate `EffectObjImageLib::GetEffectInfo(201)` copy and global `g_pUserPane` secondary-before-primary reverse cleanup. That later List scan/deletion is not part of `RequestObjectEffect`, not a hidden callback from this method, and not evidence that UserPane owns this MapPane member.
- The cleanup dereferences pointer slots, matches `m_effectId`, requires `IsLooping`, deletes the first match, and has no pane-null guard. This bounded support record preserves the separation between MapPane request/reuse logic and UserPane post-request cleanup.
- Old broad UID0001KM-only caller wording, generic new-asset gate, and implicit combined-request/cleanup interpretation are superseded; exact descriptor id, arguments, gate polarity, ownership, and source order are current.
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-15 B004 UID0003TG accepted source-quality callback:
  - Preserved `92/94`, UID00007Q owner/emitter, exact formal body, MapPoint row-before-column calls, descriptor/list behavior, callers, source route, and every B005/B001/unrelated fact.
  - Synchronized only the existing-pane dependency to source delete for `-1`, all remaining signed actions, inferred `m_timerExtensionLocked`, typed TimerHandler due-tick/cancel/replace behavior, dimensional quirk, and superseded callback/positive-only/raw-guard history. No body, score, metadata, route, or create order changed.

- 2026-07-15 B005 UID0003TE accepted source-quality callback:
  - Preserved `92/94`, UID00007Q owner/emitter, exact range/padding, all four callers, descriptor/object/list/timer behavior, complete formal body, and unrelated UID0003U6 integration.
  - Corrected only both UID0003TF create calls and related prose to `position.row, position.column`, matching GetMapPosition layout, caller pushes, create-helper frame offsets, and constructor forwarding.
  - Historicalized the transposed `mapColumn, mapRow` helper contract without changing request/reuse/create order, sequence selection, timer/render forwarding, or source placement.

- 2026-07-15 B001 UID0003TF accepted source-quality callback:
  - Raised this caller from `89/91` to `92/94` while preserving UID00007Q owner/emitter, reconstructable state, blank position, exact range/padding, source route, and unrelated caller evidence.
  - Replaced the complete managed body with the accepted typed version: `renderAfterLivingObjects`, distinct ObjectPane lookup local, `LivingObjectPane *target`, established List accessors, and exact UID0003TF calls.
  - Synchronized the target's exact ABI/body/source readiness and historicalized compact/generic list/map-X/map-Y drafts without changing request/reuse/timer behavior.

- 2026-07-14 B005 UID0002QJ nesting closure only: changed relative `Nested:4 -> 0` after registered preceding child [UID:0004PZ][0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer](by-memory/0x0050e2a0-0x0050e2f4.MapPaneAllocateTileBuffer.md) opens the same four-level depth under UID00037Y. Address-sort absolute depth is unchanged. Score, owner/emitter, range, source route, Item Summary, complete formal `MapPane::RequestObjectEffect` body, behavior, callers, negative evidence, and unrelated history remain unchanged.
- 2026-06-20 B007 accepted source-quality implementation:
  - Updated metadata from `84/88` owner/emitter [UID:0000L3][MapPane](by-file/MapPane.md) to `89/90` owner/emitter [UID:00007Q][MapPane](by-class/MapPane.md), keeping file emission through `NexusTK/map/MapPane.cpp`.
  - Promoted the source-facing name to `MapPane::RequestObjectEffect(unsigned int objectId, int effectDescriptorId, int timerAction, bool compactFlag)`, keeping `MapPaneObjectEffectDescriptorDispatch` and `sub_50E320` as generated/descriptive aliases.
  - Added first-draft C++ preserving the observed secondary/type-4 pass before the primary/type-2 pass, backward scans over attached-object lists, `EffectObjectPane::m_effectId == EffectInfo::effectKeyOrId` reuse checks, `IsLooping()` gate, `CreateEffectObjectPane` calls with by-value `EffectInfo`, and timer-action helper calls.
  - Resolved support terminology from the accepted report: `m_type4AttachedObjects` / secondary sequence / `sequenceIndex == 0`; `m_type2AttachedObjects` / primary sequence / `sequenceIndex == 1`; `timerAction` for overloaded `-1`/`-2`/`0`/positive behavior; and `compactFlag` as the caller-derived flag.
  - Rejected B008's `MapPane::ApplyObjectEffect` as the primary name for this implementation while documenting it as a plausible alternate.
- 2026-06-21 B008 source-quality report incorporation:
  - Raised confidence from `90` to `91` after incorporating the report's local PE/Capstone recheck of exact range, padding, direct callers, callee roles, lack of pointer/table route, lack of compiler-generated destructor/EH content, and MapPane owner/emitter route.
  - Kept completion at `89` because the target already carried first-draft source-style C++, owner/emitter [UID:00007Q][MapPane](by-class/MapPane.md), source route [UID:0000L3][MapPane](by-file/MapPane.md), helper signatures, field semantics, and pass order from the accepted B007 implementation.
  - Kept `MapPane::RequestObjectEffect` as the selected source-facing name and retained `MapPane::ApplyObjectEffect` as a documented rejected alternate; B008's proposed name is plausible but less precise for the request/reuse/cancel/no-op behavior and would require a global caller rename.
- 2026-06-02 grading, parent attachment, and premature C++ removal:
  - What existed before: `COMPLETION:60`, `CONFIDENCE:65`, blank `RECONSTRUCTABLE`, no parent, and a C++ reconstruction sketch using provisional descriptor/object/helper names.
  - Changed to: `COMPLETION:78`, `CONFIDENCE:84`, `RECONSTRUCTABLE:TRUE`, parent [UID:0000L3][MapPane](by-file/MapPane.md), and reconstruction notes without final C++.
  - Summary/evidence: IDA MCP reports the exact function boundary, caller set, and padding; the behavior is MapPane object/effect descriptor dispatch backed by descriptor-library lookup and object-list paths. The C++ sketch was removed because nested helper and type names are not 95%+ final-source quality.
- 2026-06-06: Updated from `78/84` to `84/88`, kept parent `0000L3` at position `36`, left final C++ blank, and replaced stale raw-helper/global wording with live IDA evidence for the exact boundary, four callers, no data refs, EffectObjImageLib descriptor copy, ObjectList lookup, two child-collection scan passes, reuse/timer behavior, create/apply behavior, and adjacent padding.
- 2026-06-18 B003 support update: corrected the `0x00538bb0` callee reference from stale `LivingObjectPaneGetEntityId` to [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md), with the `+0x148` compare now documented as `EffectObjectPane::m_effectId`.
