*** UID:0002LA | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "MonsterImageLibTables.h"

void RidingImageLib::LoadRidingDefinitions()
{
    DatFile reader;

    reader.Open(L"RIDINGS.DNA");
    reader.Read(&ridingDefinitionCount, sizeof(ridingDefinitionCount));

    ridingDefinitions = AllocateRidingDefinitionArray(ridingDefinitionCount);

    g_ridingDefinitionGroups.AllocateGroups(0x15);
    for (unsigned int groupIndex = 0; groupIndex < 0x15; ++groupIndex) {
        ImageAnimationGroup *group = g_ridingDefinitionGroups.GetGroup(groupIndex);

        group->AllocateFrames(1);
        group->frames[0].frameOffset = 0;
        group->frames[0].frameDuration = 30000;
        group->frames[0].blendAlpha = 0;
    }

    for (unsigned int ridingIndex = 0; ridingIndex < ridingDefinitionCount; ++ridingIndex) {
        RidingDefinition *definition = ridingDefinitions + ridingIndex;

        reader.Read(&definition->ridingId, sizeof(definition->ridingId));
        reader.Read(&definition->bucketCount, sizeof(definition->bucketCount));
        reader.Read(&definition->defaultBucketIndex, sizeof(definition->defaultBucketIndex));
        reader.Read(&definition->animationGroupId, sizeof(definition->animationGroupId));

        definition->buckets = AllocateRidingBucketArray(definition->bucketCount);
        for (unsigned int bucketIndex = 0; bucketIndex < definition->bucketCount; ++bucketIndex) {
            ImageAnimationGroup *bucket = definition->buckets + bucketIndex;

            reader.Read(&bucket->frameCount, sizeof(bucket->frameCount));
            bucket->AllocateFrames(bucket->frameCount);

            for (unsigned int entryIndex = 0; entryIndex < bucket->frameCount; ++entryIndex) {
                ImageAnimationFrame *entry = bucket->frames + entryIndex;

                reader.Read(&entry->frameOffset, sizeof(entry->frameOffset));
                reader.Read(&entry->frameDuration, sizeof(entry->frameDuration));
                reader.Read(&entry->auxFrameId, sizeof(entry->auxFrameId));
                reader.Read(&entry->blendAlpha, sizeof(entry->blendAlpha));
                reader.Read(&entry->paletteIndex, sizeof(entry->paletteIndex));
                reader.Read(&entry->effectIndex, sizeof(entry->effectIndex));
            }
        }
    }

    reader.Close();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-confirmed constructor-only `RIDINGS.DNA` parser with corrected half-open endpoint, 12-byte riding definitions, 8-byte bucket rows, 10-byte entry rows, and global default riding table writes. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dc420-0x004dc68d RidingImageLibLoadRidingDefinitions

## Status

- Entity kind: method.
- Disposition: reconstructable NexusTK source.
- Rebuild handling: `source-authored`.
- Owner: [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md), likely in [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md).
- Parent aggregate: [UID:00017F][0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster](by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md).
- Confidence: strong for exact bounds, parser order, table allocation helpers, and constructor-only call site; medium for final source-level field names.
- Assignment status: assigned to [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md); this child is `89/91`, the direct class parent is `89/90`, and the constructor-only call site proves direct class ownership.

## Address Range

IDA function body: `0x004dc420-0x004dc68d` half-open.

Live IDA MCP on 2026-06-07 reports `sub_4DC420` at `0x004dc420` with size `0x26d`, so `0x004dc68c` is the final byte and `0x004dc68d` is the exclusive end. Three `0xcc` bytes separate `0x004dc68d` from the constructor start at `0x004dc690`.

## Behavior

This method loads `RIDINGS.DNA` into the `RidingImageLib` runtime definition arrays. It:

- constructs a local DAT/stdio-style reader object and opens `RIDINGS.DNA`;
- reads a 32-bit riding definition count;
- allocates a counted heap vector of 12-byte `RidingDefinition` records and stores the pointer at `this+0x04` and count at `this+0x08`;
- initializes the process-wide default riding definition table at `0x0069b430` with 21 8-byte bucket rows;
- seeds each default bucket with one 10-byte entry using `frameOffset = 0`, `frameDuration = 30000`, and a zero `blendAlpha`/render-flag byte;
- reads each file-backed riding definition as a 12-byte record:
  - `+0x00` dword riding id;
  - `+0x04` byte bucket count;
  - `+0x05` byte default bucket/index-like value;
  - `+0x06` word animation/group-like value;
  - `+0x08` pointer to allocated bucket rows;
- allocates `bucketCount` 8-byte child bucket rows through the shared outer table helper at `0x005289d0`;
- for each bucket, reads a 16-bit entry count at bucket offset `+0x04`, allocates `entryCount` 10-byte bucket entries through `0x00528be0`, then reads each entry in the observed order `+0x00`, `+0x02`, `+0x06`, `+0x04`, `+0x08`, `+0x09`;
- closes/destroys the local reader and returns its close/status helper result.

## IDA MCP Evidence

- `lookup_funcs` reports `sub_4DC420` size `0x26d`, ending at exclusive `0x004dc68d`.
- `callers` reports only one executable caller: `sub_4DC690` at call site `0x004dc6f5`, the `RidingImageLib` constructor.
- The constructor call list is `0x004dc6de -> 0x004f4a80`, `0x004dc6f5 -> 0x004dc420`, and `0x004dc707 -> 0x004d0f50`, confirming this parser runs during singleton construction before the `RIDINGS.EPF` package load.
- The parser call list is DAT reader setup/open/read/close helpers at `0x0049c130`, `0x0049c180`, `0x0049c310`, `0x0049c240`, and `0x0049c160`; allocation helper `unknown_libname_19` at `0x005c7790`; vector constructor iterator `0x005c779e`; and shared table helpers `0x005289d0` and `0x00528be0`.
- Decompilation shows the parser opens literal `L"RIDINGS.DNA"` at `0x004dc464`.
- Decompilation reads `ridingCount` into a stack dword at `0x004dc472`, allocates `count * 0x0c + 4`, writes the count header, constructs each 12-byte record through `0x00528930`, and stores the data pointer/count to `this+0x04` and `this+0x08`.
- At `0x004dc4df`, the parser calls `0x005289d0` with the global default table object at `0x0069b430` and count `0x15`.
- At `0x004dc4e4-0x004dc524`, it walks `0xa8` bytes as 21 8-byte child bucket rows through the global table's `+0x08` pointer field at `0x0069b438`, allocates one child entry for each bucket, and seeds the default frame entry as offset `0`, duration `30000`, and zero alpha/flag.
- At `0x004dc550`, `0x004dc55e`, `0x004dc56c`, and `0x004dc57a`, it reads the four fixed fields of each 12-byte riding definition before allocating child buckets at `0x004dc585`.
- At `0x004dc5a9`, it reads each bucket's 16-bit entry count from bucket offset `+0x04`; at `0x004dc5b4`, it allocates that many 10-byte entries.
- At `0x004dc5da`, `0x004dc5e8`, `0x004dc5f6`, `0x004dc604`, `0x004dc612`, and `0x004dc620`, it reads each 10-byte bucket entry into offsets `+0x00`, `+0x02`, `+0x06`, `+0x04`, `+0x08`, and `+0x09`.
- `xrefs_to` for `0x0069b430` include `0x004dc4ce`; `xrefs_to` for `0x0069b438` include `0x004dc4f0`, `0x004dc500`, `0x004dc50d`, and `0x004dc519`, confirming `0x0069b438` is the table object's `+0x08` field used by this parser rather than a separate owner.
- Current xref counts show `0x0069b430` has 11 refs and `0x0069b438` has exactly the four parser refs above, while `0x004dc420` itself has only the constructor call at `0x004dc6f5`.

## Touched State

| Address/field | Meaning |
| --- | --- |
| `this+0x04` | Pointer to counted heap array of 12-byte `RidingDefinition` records. |
| `this+0x08` | Riding definition count. |
| `0x0069b430-0x0069b43c` | Global default riding definition group table object. |
| `0x0069b438` | `+0x08` pointer field of the global default table object. |

## Shared Frame-Entry Name Sync

The 2026-06-25 B001 accepted `MonsterImageLib::LoadMonsterTables` rework rechecked this parser as the monster loader's sibling. The binary schema is still the same 12-byte definition rows, 8-byte bucket/group rows, and 10-byte frame entries, but two older field names should be treated as superseded for the shared entry type:

- Entry `+0x02` is signed `int16`/C++ `short frameDuration`, not `endFrame` or an unsigned duration. Five scheduler/timing consumers sign-extend the shared field, and both monster/riding default table seed paths write positive value `30000` here.
- Entry `+0x04` is better documented as `blendAlpha` or a shared alpha/render-flag byte, not plain `direction`. Monster render consumes this byte as alpha/mode intensity. Riding-specific draw semantics may add local meaning, but the shared field should not be named `direction` without qualification.
- Entry `+0x06`, `+0x08`, and `+0x09` remain weaker inferred fields (`auxFrameId`, `paletteIndex`, `effectIndex`) until a broader riding draw consumer pass resolves them.

## Cross-References

- [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md)
- [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md)
- [UID:00017F][0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster](by-memory/0x004dc420-0x004dca14.RidingImageLibEarlyMethodCluster.md)
- [UID:0001VV][RidingDefinition](by-type/by-struct/RidingDefinition.md)
- [UID:0000TJ][RidingDefinitionGlobalTable](by-global/RidingDefinitionGlobalTable.md)
- [UID:0001PL][0x0069b430-0x0069b43c.RidingDefinitionGlobalTable](by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md)
- [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md)

## Name Caveats

- Filename endpoint has been corrected to the half-open `0x004dc68d`; no range repair remains.
- The 12-byte/8-byte/10-byte shapes are verified, and this page now emits first-draft parser C++. The shared helper family uses accepted source-facing names `ImageAnimationTable`, `ImageAnimationGroup`, and `ImageAnimationFrame` from [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md). Do not move that helper implementation under RidingImageLib.

The formal CPP now directly includes `MonsterImageLibTables.h` before the unchanged loader definition. This is the first source-bearing riding consumer of `ImageAnimationTable`, `ImageAnimationGroup`, and `ImageAnimationFrame`; a direct include is therefore the C++03-complete dependency route. The include does not transfer helper ownership into RidingImageLib and does not change the parser body, scores, owner, emitter, or formal H disposition.

The shared header preserves the exact 12/8/10-byte ABI and signed `short frameDuration`. The loader continues to own concrete `RidingDefinition` storage, seed the 21-group riding fallback table with one frame per group and duration 30000, parse each riding definition's group/frame records in binary order, and invoke the shared allocation methods. `ImageAnimationGroup`'s `+0x06` tail remains implicit alignment; no concrete riding structure or shared helper definition is duplicated here.
- `DatFile`, `AllocateRidingDefinitionArray`, and `AllocateRidingBucketArray` are inferred source-facing names for the DAT reader and vector/allocation helpers.
- The exact original source names for the weaker bucket-entry fields are not fully proven. Keep `frameDuration`, `blendAlpha`, `auxFrameId`, `paletteIndex`, and `effectIndex` marked as inferred names through the shared helper pages.

## Score And Assignment Rationale

- Completion is `89` after the B008 callback inserted first-draft source C++ and replaced the stale `95+`/blank-C++ blocker with accepted helper-name caveats.
- Confidence is `91` because the parser bounds, sole constructor call site, DAT reader flow, table-helper use, default-table references, nested record layout, and shared helper names are all supported by current IDA evidence or accepted helper reports.
- `CANONICAL_OWNER` and `EMITTER_UIDS` are [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md). The child is `89/91`, [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md) is `89/90`, and the constructor-only call site justifies direct class ownership.

## 2026-06-30 B008 Source-Quality Callback

B008's accepted report resolved the stale blank-C++ blocker for this parser. Current MCP-backed evidence remains: exact half-open range `0x004dc420-0x004dc68d`, sole constructor call at `0x004dc6f5`, `RIDINGS.DNA` literal open, definition count read, `count * 0x0c` definition allocation, stores at `this+0x04/+0x08`, global default table allocation at `0x0069b430` with `0x15` groups, one default entry per group with offset `0`, duration `30000`, and alpha/flag byte `0`, and nested entry reads in order `+0x00`, `+0x02`, `+0x06`, `+0x04`, `+0x08`, `+0x09`.

The formal C++ deliberately uses accepted source-facing helper names rather than address-derived `sub_` or `MonsterImageLibTable_*` labels. Weak field names remain marked as inferred, but they are no longer blockers for source emission.

## UID0003U8 Shared Timing Consumer Note - 2026-07-15

- This riding parser remains `89/91` with unchanged owner/emitter, range, formal body, constructor-only route, record widths, read order, and default-table behavior. It is a support synchronization only.
- Shared frame word `+0x02` is signed `short frameDuration`. Five consumers at `0x0053a30b`, `0x0053aaae`, `0x0053b780`, `0x0053ba59`, and `0x005a37d6` sign-extend it; the last is [UID:0003U8][0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation](by-memory/0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation.md).
- The parser still reads exactly two bytes into the field, and the default `30000` remains valid signed-short data. No riding-specific source ownership or RenderRiding semantics are inferred from the UserPane consumer.

## Changes

- 2026-06-25 B001 implementation callback:
  - Score unchanged at `85/90`.
  - Evidence: narrowly synchronized the sibling parser wording with accepted [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) shared-frame evidence. Replaced stale `endFrame` / `direction` phrasing with `frameDuration` and `blendAlpha`/render-flag wording while preserving the verified parser order and leaving riding-specific C++ blank.
- 2026-06-07 A005 Batch 047 split recheck:
  - Renamed from `0x004dc420-0x004dc68c.RidingImageLibLoadRidingDefinitions` to `0x004dc420-0x004dc68d.RidingImageLibLoadRidingDefinitions`, raised from `82/89` to `85/90`, and set current owner/emitter route [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md).
  - Evidence: live IDA reports `sub_4DC420` size `0x26d` and the associated `RidingImageLib` class parent was refreshed to `85/86`.
  - Assignment decision: assigned to [UID:0000BZ][RidingImageLib](by-class/RidingImageLib.md) because both child and direct parent satisfy the corrected `85/85` gate.

- 2026-06-07 A001: Raised from `76/86` to `82/89` with current IDA boundary, padding, caller/callee, and xref evidence.
  - Evidence: `lookup_funcs` reports `sub_4DC420` as `0x004dc420-0x004dc68d` half-open; bytes `0x004dc68d-0x004dc690` are `cc cc cc`; the only executable caller is `0x004dc6f5` inside the constructor; and the parser call list/xrefs confirm the DAT reader, vector construction, shared table helpers, and `0x0069b430/+0x08` default-table use.
  - Assignment decision: left unassigned because the child is `82/89` and the direct `RidingImageLib` parent candidates are below the corrected 85/85 gate.
