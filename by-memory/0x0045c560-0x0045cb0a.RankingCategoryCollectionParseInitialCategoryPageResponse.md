*** UID:0003WS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingCategoryCollection::ParseInitialCategoryPageResponse(
    const unsigned char *packet)
{
    m_records.clear();

    short packetOffset = 4;
    const short categoryCount =
        static_cast<short>(PacketBufferReadUInt16BE(packet + 2));
    wchar_t wideTitle[256] = { 0 };
    wchar_t wideName[256];
    char narrowText[256];

    for (short categoryIndex = 0;
         categoryIndex < categoryCount;
         ++categoryIndex)
    {
        const int categoryId =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;

        const int startDate =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int startTime =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int endDate =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int endTime =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;

        const char stateCode =
            static_cast<char>(packet[packetOffset++]);
        const unsigned char titleLength = packet[packetOffset++];

        GetMemoryMan()->MemmoveWrapper(
            narrowText,
            packet + packetOffset,
            titleLength);
        narrowText[titleLength] = '\0';
        packetOffset =
            static_cast<short>(packetOffset + titleLength);

        const int wideTitleLength =
            MultiByteToWideChar(
                CP_ACP,
                0,
                narrowText,
                titleLength,
                wideTitle,
                256);
        wideTitle[wideTitleLength] = L'\0';

        RankingCategoryRecord category;
        category.SetListIndex(categoryIndex);
        category.SetCategoryId(categoryId);
        category.SetStartTimeFromPackedDateTime(startDate, startTime);
        category.SetEndTimeFromPackedDateTime(endDate, endTime);
        category.SetStateCode(stateCode);
        category.SetTitleText(wideTitle);
        m_records.push_back(category);
    }

    const int selectedCategoryId =
        static_cast<int>(
            PacketBufferReadUInt32BE(packet + packetOffset));
    packetOffset += 4;

    RankingCategoryRecord *selectedRecord = 0;
    for (unsigned int categoryIndex = 0;
         categoryIndex < m_records.size();
         ++categoryIndex)
    {
        if (m_records[categoryIndex].GetCategoryId() == selectedCategoryId)
        {
            selectedRecord = &m_records[categoryIndex];
            break;
        }
    }

    if (selectedRecord == 0)
        return;

    m_selectedCategoryId = selectedCategoryId;
    selectedRecord->ResetUserEntries();

    const char hasLocalPlayer =
        static_cast<char>(packet[packetOffset++]);
    if (hasLocalPlayer > 0)
    {
        const int localPlayerRank =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int localPlayerScore =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const char localStateCode =
            static_cast<char>(packet[packetOffset++]);

        selectedRecord->m_loadedRowsCategoryId = selectedCategoryId;
        selectedRecord->m_localPlayerScore = localPlayerScore;
        selectedRecord->m_localPlayerRank = localPlayerRank;
        selectedRecord->SetStateCode(localStateCode);
    }

    const short rowCount =
        static_cast<short>(
            PacketBufferReadUInt16BE(packet + packetOffset));
    packetOffset += 2;

    for (short rowIndex = 0; rowIndex < rowCount; ++rowIndex)
    {
        const unsigned char nameLength = packet[packetOffset++];
        GetMemoryMan()->MemmoveWrapper(
            narrowText,
            packet + packetOffset,
            nameLength);
        narrowText[nameLength] = '\0';
        packetOffset =
            static_cast<short>(packetOffset + nameLength);

        const int wideNameLength =
            MultiByteToWideChar(
                CP_ACP,
                0,
                narrowText,
                nameLength,
                wideName,
                256);
        wideName[wideNameLength] = L'\0';

        const int rank =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;
        const int score =
            static_cast<int>(
                PacketBufferReadUInt32BE(packet + packetOffset));
        packetOffset += 4;

        RankingUserEntry entry;
        entry.categoryId = selectedCategoryId;
        entry.score = -1;
        entry.rank = rank;
        wcscpy_s(entry.name, 32, wideName);
        entry.score = score;
        selectedRecord->AppendUserEntry(entry);
    }

    selectedRecord->SetTotalRankCount(
        static_cast<int>(
            PacketBufferReadUInt32BE(packet + packetOffset)));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Source-ready opcode 0x7d subcommand 3 parser: clears and rebuilds categories with signed-short count/cursor, commits selection only on a rebuilt-record match, applies signed-positive local rank/score/state metadata without touching the reserved footer, appends initial rows, stores total last, and preserves incremental state on return or failure. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# RankingCategoryCollection ParseInitialCategoryPageResponse `0x0045c560-0x0045cb0a`

## Status

- Disposition: source-authored combined category-list and selected-row response parser.
- Direct owner: [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md).
- Source route: the formal CPP method emits through UID0000BK into `NexusTK/ui/dialogs/RankingDialog.cpp` using the existing `RankingDialog.h`, `PacketBuffer.h`, `MemoryMan.h`, and Win32 conversion dependencies.
- Formal H remains blank because UID0000BK already owns exactly one `void ParseInitialCategoryPageResponse(const unsigned char *packet);` declaration. [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md) supplies narrow collection friendship for the proved private page-metadata stores.
- Score: `93/93`. Complete behavior, range, dependencies, legal access, source placement, failure semantics, and formal source are closed; stripped retail spellings and exact compiler instrumentation remain bounded confidence caps.

## Evidence

- Live 2026-08-14 read-only MCP revalidated modeled range `[0x0045c560,0x0045cb0a)`, 1,450 bytes, 381 instructions, 34 CFG blocks including external EH chunks, cyclomatic complexity 13, and body SHA256 `B591D9ED33B19C7B1C9E5D7B28705EC6ADE6932AC465656E72E1C4805237968B`.
- `RankingDialog_HandleRankingPacket` dispatches opcode `0x7d` subcommand `3` through the sole inbound code xref at `0x00459276`, ignores the residual `EAX`, updates max-page state, and invalidates ranking panes 1 and 2. No data xref, callback, vtable slot, or alternate route reaches the target.
- The method parses category-list records, reads selected category id, writes collection offset `+0x0c`, finds the selected record, resets row storage, parses optional local-player metadata, parses user rows, and stores total count.
- Exact [UID:0002K7][0x0045c050-0x0045c0f9.RankingCategoryRecordSetStartTimeFromPackedDateTime](by-memory/0x0045c050-0x0045c0f9.RankingCategoryRecordSetStartTimeFromPackedDateTime.md) callsite is `0x0045c770`. The parser supplies the start endpoint as `packedDate` then `packedTime` before calling the paired end endpoint setter.
- Exact [UID:0002K8][0x0045c100-0x0045c1a9.RankingCategoryRecordSetEndTimeFromPackedDateTime](by-memory/0x0045c100-0x0045c1a9.RankingCategoryRecordSetEndTimeFromPackedDateTime.md) callsite is `0x0045c787`. The checked sequence is `push [ebp-0x82c]`, `lea ecx, [ebp-0x80c]`, `push [ebp-0x814]`, `call 0x45c100`; because explicit thiscall arguments are pushed right-to-left, K8 receives end `packedDate = [ebp-0x814]` and end `packedTime = [ebp-0x82c]`.
- The local-player metadata flow writes exactly `m_loadedRowsCategoryId +0x264`, `m_localPlayerScore +0x2ac`, `m_localPlayerRank +0x268`, and `m_stateCode +0x08`. Complete target and sibling-parser store review proves no packet write to `m_reservedLocalPlayerFooter[0x40]` at `+0x26c..+0x2ab`; the displayed footer name comes from global `word_69BEE0`, not this tail.

## Inline User-Page Reset And Local Metadata

After parsing and selecting the category, this combined subcommand `3` handler performs the same logical reset as [UID:0002KA][0x0045c240-0x0045c256.RankingCategoryRecordResetUserEntries](by-memory/0x0045c240-0x0045c256.RankingCategoryRecordResetUserEntries.md): record vector end `+0x25c` is assigned begin `+0x258` and `m_localPlayerRank` at `+0x268` is set to `-1` before replacement rows are loaded. Capacity `+0x260` remains intact, so the source operation is `m_userEntries.clear()` rather than vector destruction.

The parser then reads the optional-local flag as signed `char`. Only values `1..127` enter the block. A present block reads BE32 local rank, BE32 local score, and signed state, then writes loaded category id, score, rank, and state in that order. It never writes the reserved/copied footer-tail region `+0x26c..+0x2ab`. Because every category record was rebuilt from the partial default baseline, an absent block leaves loaded category id, local rank, and local score at `-1` and retains the category-list state.

Parsed `0x4c`-byte `RankingUserEntry` rows are appended to the same record-owned `std::vector` after reset, and the response stores the total count at `+0x254`. The method's inline lowering and UID0002KA are two compiled instances of the same source-level state transition; the physical pointer stores are evidence, not a reason to emit raw member assignments.

Historical selected-user-index wording for `+0x268` is rejected because this parser writes rank metadata and the paint consumer tests it as a positive rank. Collection selection remains `RankingCategoryCollection::m_selectedCategoryId`.

## Exact Packet And Commit Contract

1. The method clears `m_records` before parsing. Existing nested user-entry vectors are destroyed while outer vector capacity is retained.
2. Category count is UInt16BE narrowed to signed `short`; the signed-short packet cursor begins at `4`. Zero or negative narrowed counts append no categories.
3. Each category reads BE32 id, start date, start time, end date, end time, signed state byte, unsigned title length, and title bytes. The title path uses `MemoryMan::MemmoveWrapper`, an explicit narrow terminator, `MultiByteToWideChar(CP_ACP, 0, ..., 256)`, an indexed wide terminator, and the existing secure title setter.
4. Automatic `RankingCategoryRecord` construction supplies an empty row vector, total `0`, and loaded-category/rank/score sentinels `-1`; unrelated fields and tails are not whole-zeroed. Existing setters assign index, id, packed start/end time, state, and title before ordinary `m_records.push_back`.
5. After categories, the method reads selected category id and searches only the rebuilt vector. If no record matches, it returns with the refreshed category list intact but does not overwrite collection selection or commit selected-record row/total state.
6. A match commits `m_selectedCategoryId`, clears the selected row vector without freeing capacity, and resets local rank to `-1` before optional metadata and rows are parsed.
7. Row count is UInt16BE narrowed to signed `short`; a negative count skips row appends and proceeds directly to the total field at the current cursor.
8. Each row reads unsigned name length and bytes, performs the same CP_ACP conversion through a 256-wide temporary, then reads BE32 rank and score. The exact `0x4c` row stores selected category id, score sentinel `-1`, rank, secure `wchar_t[32]` name copy, and final score before ordinary `AppendUserEntry`/vector append.
9. Final BE32 total count is stored only after all requested rows append successfully. No packet-null/length/count, encoding-result, or transactional rollback checks exist.

## Partial-Success And Exception Behavior

- A failure after entry leaves the old category list already removed. Successfully appended category records remain; a failed vector growth itself retains the standard strong guarantee.
- Once a selected record is found, collection selection, row reset, and optional local metadata may already be committed when later parsing or append fails. Successfully appended row prefixes remain, and total count may retain its prior/default value because the total store is last.
- The automatic category temporary's nested vector is destroyed after each successful outer append and by the external compiler EH cleanup on exceptional exits. Security-cookie, range-check, vector growth/deep-copy, and EH funclet code are compiler consequences, not authored helpers.

## Category-Record Append Fast And Growth Paths

- For each stack-built category record, the parser compares the collection's `m_records` end and capacity pointers. Capacity available calls [UID:0003X4][0x0045d420-0x0045d4f8.RankingCategoryRecordCopyConstructDeep](by-memory/0x0045d420-0x0045d4f8.RankingCategoryRecordCopyConstructDeep.md) at `0x0045c7cb` to deep-copy one exact `0x2b0`-byte record in place.
- Capacity exhausted calls [UID:0003X0][0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth](by-memory/0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth.md) at `0x0045c7dc`. That non-emitting compiler specialization grows the outer vector, allocates and aligns storage, deep-copies the inserted record plus prefix/suffix ranges, tears down old nested vectors, and rolls back partial construction on exception.
- Both paths lower one human source operation: append the completed local record to `RankingCategoryCollection::m_records` in `RankingDialog.cpp`. Recovered source must not call or declare the descriptive binary growth-helper name.
- The successful append advances the outer vector end by one `0x2b0` element. At `0x0045c7ee` the parser destroys the stack temporary's embedded `m_userEntries` vector after the collection-owned deep copy is complete.
- This exact compiler/source boundary is represented by the complete formal method above. It raises this target to `93/93`; formal H remains blank only because the owner class already declares the method.

## Range, Dependencies, And Negative Evidence

- Ten `0xcc` bytes at `[0x0045c556,0x0045c560)` and six at `[0x0045cb0a,0x0045cb10)` are zero-xref boundary items. The successor parser begins at `0x0045cb10`; no split, merge, or range extension is supported.
- The complete material callee set is `RankingUserEntryVectorDestroy`, `PacketBufferReadUInt16BE`, `memset`, `PacketBufferReadUInt32BE`, narrow-character lowering, `GetMemoryMan`, `MemoryMan_MemmoveWrapper`, `MultiByteToWideChar`, both packed-time setters, `wcscpy_s`, implicit record deep copy, outer-vector growth, user-entry growth, security-cookie checking, and range-check failure.
- Outer fast/slow append paths and user-row fast/slow append paths are ordinary standard-vector operations. Descriptive binary helper names, raw vector triplets, explicit allocator/EH code, and compiler range-check calls do not belong in source.
- No target-local string, global, resource, RTTI, vtable, independent constant, alternate owner, source-authored helper, or standalone `RankingCategoryCollection.cpp` route exists. The shared constructor constant and EH metadata remain compiler/data support.

## Heuristic / Inference Notes

- Best descriptive name: `RankingCategoryCollection::ParseInitialCategoryPageResponse`.
- Rejected: `ParseCategoryListExtended`; it is safe but vague. Rejected `ParseSelectedCategory`; it omits the category-list refresh. Rejected `ParseCategoryListAndSelectedRowsResponse`; it is accurate but too long for the preferred source-facing method label.
- Original source spelling is unavailable, but the subcommand and data shape are strongly supported.

## Cross-References

- [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md)
- [UID:0001ZC][0x0045c260-0x0045d4f8.RankingCategoryCollection](by-memory/0x0045c260-0x0045d4f8.RankingCategoryCollection.md)
- [UID:0001VN][RankingCategoryRecordLayout](by-type/by-struct/RankingCategoryRecordLayout.md)

## Changes

- 2026-08-14 B001 UID0003WS accepted source-quality callback:
  - Raised metadata from `87/90` to `93/93` while preserving owner/emitter UID0000BK, reconstructable true, blank position, exact range, and child route.
  - Added the complete formal CPP method and retained formal H blank because UID0000BK already owns the declaration; UID0000BL narrow friendship supplies legal private page-metadata access.
  - Added exact signed count/cursor, category/title, match-only selection, signed-positive optional metadata, row/total, partial-success, helper-boundary, body hash/xref, padding, and negative evidence.
  - Superseded the former blank-source blocker and the disproved reserved-footer packet-write claim while preserving their historical provenance below.

- 2026-08-06 B002 UID0003X0 accepted caller-route callback:
  - Added the exact capacity-available deep-copy call at `0x0045c7cb`, capacity-exhausted UID0003X0 call at `0x0045c7dc`, one-record end advance, and stack-temporary nested-vector teardown at `0x0045c7ee`.
  - Recorded ordinary `m_records` append as the human source cause and retained this parser's `87/90` metadata and blank formal CPP/H.

- 2026-08-04 B005 UID0002KA inline-reset callback:
  - Preserved parser metadata, exact range/subcommand/date-time evidence, owner/emitter route, and blank formal channels.
  - Added the exact end=begin/rank=`-1` logical reset, preserved capacity, optional local-player metadata replacement, row append and total-count flow, and the distinction between reserved footer storage and the global displayed name.
  - Historicalized raw pointer assignments and selected-user-index wording without deleting their compiler-layout provenance.

- 2026-06-20 B001 accepted K8 implementation:
  - Added exact K8 callsite `0x0045c787` and documented the stack argument flow proving the end endpoint is supplied as `packedDate, packedTime`.
- 2026-06-22 Rule 26 archive closeout:
  - Preserved the B002 rejected-name analysis for `ParseCategoryListAndSelectedRowsResponse`; the current descriptive name remains preferred because it captures the initial combined list/page role without becoming overly broad.
- 2026-06-19 B015 accepted K7 implementation:
  - Added exact K7 callsite `0x0045c770` and documented that this parser supplies the start endpoint as `packedDate, packedTime`.
  - Replaced stale `localPlayerName[32]` wording with the current `m_reservedLocalPlayerFooter[0x40]` support direction.
- 2026-06-16 B002/supervisor split:
  - Created exact child for opcode `0x7d` subcommand `3` parser evidence.
