*** UID:0002EF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000051 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000051 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FittingRoomDialogItemState::SaveEncodedStateBuffer(std::string source)
{
    FILE* cacheFile = _wfopen(m_cacheFilePath, L"wb");
    if (cacheFile != NULL) {
        const size_t byteCount = source.length();
        char* fileBytes = static_cast<char*>(malloc(byteCount + 1));
        strcpy_s(fileBytes, byteCount + 1, source.c_str());

        TransformPersistedBuffer(fileBytes, static_cast<int>(byteCount));
        fwrite(fileBytes, 1, byteCount, cacheFile);
        fclose(cacheFile);
        free(fileBytes);
    }

    m_cacheLoaded = true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Attached to `FittingRoomDialogItemState`; 2026-07-03 B013 MCP session `e18d1e9b` reconfirms exact `0xef` save-helper bounds, single `HandleFittingAction` caller at `0x0041cc57`, wide `L"wb"` cache open through shared `_wfopen` dispatch, `std::string source` consumption, heap copy/transform/write/close/free path, unconditional `m_cacheLoaded` store at `+0x24c`, surrounding `0xcc` padding, and formal source-ready C++ under UID000051. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# FittingRoomDialogItemState Save Encoded State Buffer `0x004225a0-0x0042268f`

## Status

- Entity kind: method.
- Owner: [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md).
- Disposition: reconstructable NexusTK product code.
- Evidence basis: live IDA MCP lookup/decompile/caller/callee/xref/byte checks on 2026-06-05, C001 direct IDA refresh on 2026-06-16, B013 MCP session `e18d1e9b` recheck on 2026-07-03, and existing fitting-room and platform dispatch documentation.

## Behavior

This method persists an item-shop buffer to the local `ItemShop.jbn` cache. Current source shape is `void FittingRoomDialogItemState::SaveEncodedStateBuffer(std::string source)`: it consumes the by-value `std::string`, writes an encoded copy when the cache file can be opened, and then marks the cache as loaded/written regardless of open success. `m_cacheFilePath`, `m_cacheLoaded`, and `source` are source-facing inferred names supported by current class/file docs and sibling generated methods rather than original symbol proof.

1. Opens `m_cacheFilePath` / `this + 0x18` through the wide file-open dispatch entry at `0x0069be14` with the UTF-16 binary-write mode string `L"wb"` at `0x0060df28`; the source-facing wrapper is direct `_wfopen(m_cacheFilePath, L"wb")`.
2. If the open succeeds, allocates `length + 1` bytes for a temporary copy.
3. Copies `source.c_str()` into the temporary buffer with `strcpy_s`, matching the observed inline/heap small-string selection in the compiled body.
4. Runs [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md) / `TransformPersistedBuffer(char*, int)` over the copy.
5. Writes exactly `length` transformed bytes with `fwrite`.
6. Closes the file handle and frees the temporary buffer.
7. Sets `m_cacheLoaded` / the cache-loaded byte at `this + 0x24c` to `1` after the open/write attempt; this store is outside the successful-open block.
8. Destroys the by-value `std::string source` if it is heap-backed, including the MSVC aligned-allocation validation path.

## Address And Boundary Notes

- IDA models `0x004225a0` as a `0xef` byte function with exact exclusive range `0x004225a0-0x0042268f`.
- The previous address `0x0042259a` is not a function and the next function starts at `0x00422690`.
- Caller: [UID:0002CT][0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction](by-memory/0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction.md) calls this method once at `0x0041cc57` after loading and copying the fitting-room catalog state buffer.
- Confirmed padding before this function: six `0xcc` bytes at `0x0042259a-0x004225a0`.
- Confirmed padding after this function: one `0xcc` byte at `0x0042268f-0x00422690`.

## Live IDA Evidence

- `lookup_funcs` confirms `0x004225a0` as the function start and `0x00422690` as the next function start; `0x0042259a` and `0x0042268f` are not function starts.
- `callers` and `xrefs_to` for `0x004225a0` report one code caller, the fitting action path at `0x0041cc57`.
- `callees` for `0x004225a0` include the allocator/free paths, `strcpy_s`, the transform helper at `0x00422b30`, `fwrite`, `fclose`, the invalid-parameter path, and the wide file-open dispatch entry at `0x0069be14`.
- The decompilation opens `(this + 0x18)`, copies the source buffer into a temporary allocation, transforms that allocation, writes `length` bytes, closes the file, frees the temporary buffer, writes `1` to `this + 0x24c`, then destroys the passed source buffer if its capacity is heap-backed.
- Disassembly anchors the main calls and side effects: file-open dispatch at `0x004225dd`, `strcpy_s` at `0x00422609`, transform at `0x00422618`, `fwrite` at `0x00422624`, `fclose` at `0x0042262c`, temporary free at `0x00422632`, cache flag store at `0x00422642`, source-buffer free at `0x0042266e`, and invalid-parameter handling at `0x0042268a`.
- Existing platform docs identify [UID:0002AS][0x0069be14-0x0069bec4.WideApiDispatchPointerTable](by-memory/0x0069be14-0x0069bec4.WideApiDispatchPointerTable.md) as the wide dispatch table range and [UID:0000WD][0x0041a280-0x0041a4a8.WideApiDispatchInit](by-memory/0x0041a280-0x0041a4a8.WideApiDispatchInit.md) as the initializer that installs `_wfopen` at `0x0069be14`.

## 2026-06-16 C001 Direct IDA Refresh

Live IDA MCP session `b001_000211_20260616` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64` reconfirmed the current save-helper body. `lookup_funcs` reports `sub_4225A0` at `0x004225a0` with size `0xef`, probes at `0x0042259a` and `0x0042268f` return non-functions, and the next modeled function starts at `0x00422690`.

Fresh `xref_query` still reports one code caller, `FittingRoomDialogHandleFittingAction` at `0x0041cc57`. VA/RVA literal pointer scans for `0x004225a0` found zero hits, so no additional function-pointer or table route is currently proven.

The current decompile/disassembly confirms the consumed small-string ABI shape: the source buffer is passed as a 24-byte stack object with inline/heap selection at capacity `0x10`, length in the `ElementCount` stack slot, and capacity in `arg_14`. The method opens `this + 0x18` through `0x0069be14`, allocates `length + 1`, copies the source bytes with `_strcpy_s`, calls [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md), writes `length` bytes with `_fwrite`, closes and frees the temporary buffer, stores `1` to `this + 0x24c`, and destroys the consumed source buffer when heap-backed. Constants checked with `tools/int_convert.py`: `0x18` is decimal `24`, `0x24c` is decimal `588`, and inline capacity `0xf` is decimal `15` (Verified with int_convert.py).

The raw bytes at `0x0060df28` are `77 00 62 00 00 00`, confirming UTF-16 `wb`; IDA's generic `get_string` renders only `w` because it stops at the embedded null. `xref_query` shows `0x0069be14` has broad wide-open dispatch use across the binary, with this helper's reference at `0x004225dd`; this supports `_wfopen` dispatch but does not prove a fitting-room-private wrapper name.

Helper/name blocker investigation:

- [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md) remains a symmetric byte transform using key strings referenced only from the item-state constructor; its behavior is final, but exact source method spelling and buffer typedef are not.
- The cache flag at `this + 0x24c` is strongly a "cache/state written or loaded" byte because this save helper sets it after the write attempt and related load paths manage the same item-state cache, but no source-quality member name is proven.
- The small-string source parameter is consumed by this method. Current IDA types still recover stack arguments generically, so forcing an IDA prototype or final C++ signature would bake in guessed names.

C001 appended a safe IDA function comment at `0x004225a0` summarizing the wide-open/write/transform/cache-flag evidence and saved the IDB. No IDA rename, prototype, struct, member, local, raw function, or final C++ edit was made.

## 2026-07-03 B013 Source-Ready Recheck

B013 MCP session `e18d1e9b` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` reconfirmed the save helper for formal C++ insertion. `lookup_funcs` still reports `0x004225a0` as a `0xef` byte function; `0x0042259a` and `0x0042268f` are not function starts, and the next function starts at `0x00422690`. `get_bytes` preserves six `0xcc` bytes before the body and one `0xcc` byte after it.

Current xref and caller evidence is direct: `xrefs_to 0x004225a0` reports the sole code caller at `0x0041cc57` inside [UID:0002CT][0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction](by-memory/0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction.md). That caller decompile resets the item-state categories, copies the downloaded/request buffer, calls `LoadEncodedStateBuffer`, copies the same source again on success, calls this save helper, then closes/advances the request state. No duplicate emitted route covers this save-on-success behavior.

The rechecked decompile/callee sequence opens `this + 0x18`, allocates `ElementCount + 1`, copies the source bytes with `_strcpy_s`, calls [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md), writes exactly `ElementCount` bytes with `_fwrite`, closes with `_fclose`, frees the temporary buffer, stores `1` to offset `588` / `0x24c`, and then performs the by-value source-string destruction path when the source is heap-backed. The accepted source-facing call is `TransformPersistedBuffer(char*, int)`.

The wide-open facts are current and capped to dependency status: `0x0060df28` bytes are `77 00 62 00 00 00`, the UTF-16 `L"wb"` mode string. `0x0069be14` has broad shared dispatch-table xrefs, and this helper references it at `0x004225dd`; that supports direct `_wfopen(m_cacheFilePath, L"wb")` in source but does not prove or justify a fitting-room-private file-open wrapper.

## Reconstruction Notes

The source-facing method is now formalized as `void FittingRoomDialogItemState::SaveEncodedStateBuffer(std::string source)`. Preserve the ownership behavior: the by-value `source` object is consumed/destroyed by this method even if file open fails.

B005 2026-06-19 resolves the transform helper signature used at `0x00422618` as `FittingRoomDialogItemState::TransformPersistedBuffer(char* buffer, int byteCount)`. B013 2026-07-03 resolves the remaining save-helper C++ blockers to implementation-ready source shape: the consumed source parameter is `std::string source` by sibling/caller ABI evidence; the file-open source is direct `_wfopen(m_cacheFilePath, L"wb")` by sibling cache-loader style and wide-mode bytes; and the cache flag policy is unconditional `m_cacheLoaded = true` after the conditional write block.

Exact original member spellings are not symbol-proven. `m_cacheFilePath`, `m_cacheLoaded`, and `source` remain project source-facing inferred names, and that caps confidence, but they no longer block formal C++. Rebuild the behavior under [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md), not as a standalone file helper, aggregate method, platform wrapper, `ItemCatalog` method, or no-code marker.

## Score Rationale

- Completion is `89` because current live IDA fixes the exact boundary, single caller, direct-call-only reachability, wide `L"wb"` byte evidence, wide dispatch table use, transform/write/close/free sequence, unconditional cache flag store, source-buffer ownership, padding, and generated-output omission, and B013 resolved the prior consumed-source, `_wfopen`, and cache-loaded policy blockers into formal first-draft C++.
- Confidence is `91` because the behavior is corroborated by current MCP decompilation, disassembly, call/xref checks, literal bytes, pointer scans, existing platform dispatch docs, and neighboring generated item-state cache methods. It is not higher because exact original member and parameter spellings remain inferred rather than symbol-proven.

## Cross-References

- [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md)
- [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md)
- [UID:0002CT][0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction](by-memory/0x0041cb70-0x0041cd38.FittingRoomDialogHandleFittingAction.md)
- [UID:0000WU][0x00421a40-0x00422e91.FittingRoomItemShopCatalogState](by-memory/0x00421a40-0x00422e91.FittingRoomItemShopCatalogState.md)
- [UID:0002AS][0x0069be14-0x0069bec4.WideApiDispatchPointerTable](by-memory/0x0069be14-0x0069bec4.WideApiDispatchPointerTable.md)
- [UID:0000WD][0x0041a280-0x0041a4a8.WideApiDispatchInit](by-memory/0x0041a280-0x0041a4a8.WideApiDispatchInit.md)
- [UID:0000JE][FittingRoom](by-file/FittingRoom.md)

## Changes

- 2026-07-03 B013 implementation callback: Raised `COMPLETION` from `86` to `89` and `CONFIDENCE` from `90` to `91` while preserving `CANONICAL_OWNER:000051`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000051`, and blank emitter position. Inserted formal `FittingRoomDialogItemState::SaveEncodedStateBuffer(std::string source)` C++ matching the accepted report. Current MCP session `e18d1e9b` supports exact `0x004225a0-0x0042268f` / `0xef` bounds, one `0x0041cc57` caller, six-byte pre-padding, one-byte post-padding, UTF-16 `L"wb"` bytes, broad shared `_wfopen` dispatch xrefs, heap copy/`TransformPersistedBuffer`/`fwrite`/`fclose`/free sequence, and unconditional `m_cacheLoaded` store. Historical blockers around consumed source spelling, private file-open wrapper uncertainty, and cache-loaded success policy are now resolved; exact member spellings remain inferred as the confidence cap.
- 2026-06-19 B005 support sync: Removed [UID:0002E8][0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer](by-memory/0x00422b30-0x00422b93.FittingRoomCatalogTransformPersistedBuffer.md)'s transform helper signature from this save-helper's blocker list after B005 resolved it as `TransformPersistedBuffer(char* buffer, int byteCount)`; remaining blockers are the consumed source-buffer wrapper/type, `_wfopen` dispatch wrapper spelling, and unconditional cache-flag source policy.
- 2026-06-16 C001 direct IDA refresh:
  - Before: `82/88`.
  - After: `86/90`.
  - Summary/evidence: current live IDA reconfirmed `sub_4225A0` `0x004225a0-0x0042268f`, the sole `0x0041cc57` caller, zero VA/RVA literal pointer hits for the start, raw UTF-16 `wb` bytes at `0x0060df28`, broad `_wfopen` dispatch use through `0x0069be14`, stack small-string consumption, transform/write/free/cache-flag sequence, `this + 0x24c` store, generated `FittingRoom.cpp` omission due blank final C++, and saved a safe IDA function comment. C++ remains blank pending source-quality buffer/cache-field/dispatch/transform names.
- 2026-06-05: A004 refreshed the page from live IDA MCP evidence, raised `COMPLETION` from `72` to `82` and `CONFIDENCE` from `84` to `88`, marked `RECONSTRUCTABLE:TRUE`, attached the page to [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md), removed stale non-live evidence, and documented exact bounds, caller, callee, file-open/write, transform, cache-flag, destruction, and padding evidence.
