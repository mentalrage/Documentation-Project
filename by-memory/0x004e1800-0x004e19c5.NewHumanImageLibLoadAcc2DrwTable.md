*** UID:00041D | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000092 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000092 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewHumanImageLib::LoadAcc2DrwTable(const wchar_t *tableName)
{
    DATFile acc2drwFile;
    char line[4096];
    const int acc2drwCount = m_partEntries[7].descriptorCount;

    acc2drwFile.Open(tableName);
    m_acc2drwEntries.clear();

    for (int index = 0; index < acc2drwCount; ++index)
    {
        if (acc2drwFile.ReadLine(line, sizeof(line)) <= 1)
        {
            DestroyDATFileMgr();
            DestroyExceptionHandler();
            MessageBoxW(0,
                        L"Error on ACC2DRW table!",
                        L"Error",
                        MB_ICONEXCLAMATION);
            __loaddll(0);
        }

        const int flags = ParseUnsignedLongBase10(line);
        Acc2DrwEntry entry = {};

        entry.enabled[0] = (flags % 1000) / 100 != 0;
        entry.enabled[1] = (flags % 100) / 10 != 0;
        entry.enabled[2] = (flags % 10) != 0;

        m_acc2drwEntries.push_back(entry);
    }

    acc2drwFile.Close();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Class-owned `NewHumanImageLib::LoadAcc2DrwTable` loader for `acc2drw.tbl`, producing three-byte `Acc2DrwEntry.enabled[0..2]` rows in `m_acc2drwEntries`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004e1800-0x004e19c5 NewHumanImageLibLoadAcc2DrwTable

## Status

- Address range: `0x004e1800-0x004e19c5` exclusive.
- Source-facing name: inferred `NewHumanImageLib::LoadAcc2DrwTable`.
- Reconstructable: true.
- Owner/emitter disposition: class-owned by [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md), emitted through that class into [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) / `NexusTK/render/NewHumanImageLib.cpp`.
- Current source shape: formal first-draft `void NewHumanImageLib::LoadAcc2DrwTable(const wchar_t *tableName)` C++ is now present. It represents the binary's direct append/full-capacity helper split as `m_acc2drwEntries.push_back(entry)`.
- UID00041E coordination: [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md) is B010-coordinated file-local NewHumanImageLib vector-growth marker/support at `86/90`, not a class method body and not a standalone handwritten helper. This loader references UID00041E as the full-capacity growth support path only.

## Behavior

This loader opens the caller-supplied table path, which the constructor passes as `L"acc2drw.tbl"`. It clears the accessory-draw vector end back to begin, computes the row count from the part table's hair/accessory-count slot, reads text lines, parses decimal values through the inferred `ParseUnsignedLongBase10`, decomposes hundreds/tens/ones digits into three compact flag bytes, appends those bytes as `Acc2DrwEntry` rows into `m_acc2drwEntries`, and reports `Error on ACC2DRW table!` on short-read failure.

Source-level behavior:

1. Construct/open a `DATFile` with `tableName`.
2. Clear `m_acc2drwEntries`; the binary does this by assigning vector end to begin (`this[19] = this[18]`) without releasing capacity.
3. Set `acc2drwCount` from `m_partEntries[7].descriptorCount`. The raw load is `*(DWORD *)(this[1] + 0x210)`, and the accepted layout arithmetic is `0x210 = 7 * 0x44 + 0x34` (all numeric conversions verified with `int_convert.py` in the B009 report).
4. For each row, read a line into a 4096-byte buffer. If the line length is `<= 1`, run the fatal cleanup path.
5. Parse the decimal row value and map `(flags % 1000) / 100`, `(flags % 100) / 10`, and `(flags % 10)` to `entry.enabled[0]`, `entry.enabled[1]`, and `entry.enabled[2]`.
6. Append with `m_acc2drwEntries.push_back(entry)`. In binary, direct space writes two bytes plus one byte and advances end by 3; full capacity calls [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md) at `0x004e195a`.
7. Close the DAT file on normal exit.

The fatal path calls `DestroyDATFileMgr`, `DestroyExceptionHandler`, `MessageBoxW(0, L"Error on ACC2DRW table!", L"Error", MB_ICONEXCLAMATION)`, and `__loaddll(0)`. The `MessageBoxW` icon argument is the observed `0x30` flag (`48`, Verified with `int_convert.py`).

## Evidence

- B009 current MCP session `supervisor_resume_20260629` was available and used with narrow schema-current calls. `server_health` reported `status: ok`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready. No fallback-only evidence or IDA writes were used.
- `lookup_funcs(0x004e1800)` reports `sub_4E1800`, size `0x1c5` (`453`, Verified with `int_convert.py`).
- `lookup_funcs(0x004e19c5)` reports no function, and `get_bytes(0x004e19c5, 16)` shows eleven `0xcc` padding bytes before the next function prologue.
- `lookup_funcs(0x004e19d0)` reports distinct successor `sub_4E19D0`, size `0x557`; `xrefs_to(0x004e19d0)` finds separate constructor and `LoadPartTable` references, so the successor is not part of this range.
- `xrefs_to(0x004e1800)` returns exactly one code xref: the NewHumanImageLib constructor call at `0x004e0188`.
- `xrefs_to(0x0061c324)` returns exactly one constructor data xref at `0x004e0181`. Raw bytes at `0x0061c324` decode as UTF-16LE `acc2drw.tbl`, followed by adjacent `.DSC` and `%s.EPF` literals; IDA's rendered string view was misleading and was not used as authority.
- `analyze_function(0x004e1800)` reports raw prototype `int __thiscall(_DWORD *this, wchar_t *ArgList)`, size `453`, caller `sub_4DFD10`, and the direct behavior used in the formal source block.
- The decompile/disassembly shows the vector clear as `this[19] = this[18]`, row count as `*(_DWORD *)(this[1] + 528)`, 4096-byte line buffer, decimal parse through `sub_5CEA6D`, digit decomposition, direct 3-byte append, and full-capacity call to `sub_4E4F10`.
- `xrefs_to(0x004e4f10)` returns one code xref, at `0x004e195a` inside this function. B010's accepted UID00041E page records that helper as file-local marker-only `m_acc2drwEntries` vector-growth support.
- `get_bytes(0x0061bfe0, 52)` decodes as UTF-16LE `Error on ACC2DRW table!`; `xrefs_to(0x0061bfe0)` includes the target reference at `0x004e19b1`. The same string also has an older HumanImageLib reference at `0x004d4a9e`, but that is shared fatal-message reuse, not ownership evidence.
- `get_bytes(0x00612598, 14)` decodes as UTF-16LE `Error`; `xrefs_to(0x00612598)` includes the target reference at `0x004e19ac`.
- Accepted [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md) support maps `m_acc2drwEntries` to `+0x48/+0x4c/+0x50` and `Acc2DrwEntry` to compact 3-byte rows. Accepted [UID:0002V8][0x004e31f0-0x004e3a2e.NewHumanImageLibCalculateCompositionBounds](by-memory/0x004e31f0-0x004e3a2e.NewHumanImageLibCalculateCompositionBounds.md) consumes those rows as `enabled[0..2]`.

## Rejected Alternatives

- Reject inlining this helper into [UID:0002JR][0x004dfd10-0x004e024a.NewHumanImageLibConstructor](by-memory/0x004dfd10-0x004e024a.NewHumanImageLibConstructor.md); the binary calls this helper out of line.
- Reject raw parse-helper names in final source-facing docs when `ParseUnsignedLongBase10` is behavior-grounded.
- Reject keeping `CANONICAL_OWNER:NONE` and blank emitters. The old B011 blocker depended on unsynchronized `Acc2DrwEntry` and vector-helper context; current layout, destructor, composition, and B010 UID00041E support resolve that blocker.
- Reject direct file-only ownership for this function. The body is a `thiscall` NewHumanImageLib method, has one constructor caller, reads `m_partEntries`, and writes `m_acc2drwEntries`. [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) is the source file route, not the narrow direct owner.
- Reject `by-resource/new-human-motion-tables.md` ownership. That page owns DAT payload provenance for `acc2drw.tbl`, not the source method implementation.
- Reject [UID:0000P3][VectorHelpers](by-file/VectorHelpers.md) ownership for this loader. UID00041E is vector-growth support, but UID00041D owns the table-loader behavior.
- Reject HumanImageLib ownership from the shared `Error on ACC2DRW table!` string. The only target code caller is the NewHumanImageLib constructor.
- Reject merging successor `0x004e19d0` into this page. MCP shows padding after `0x004e19c5` and a distinct successor with separate xrefs.
- Reject a standalone raw `VectorGrowAcc2Drw` call in source C++. The source-level loader appends one typed `Acc2DrwEntry`; UID00041E carries marker-only helper support for full-capacity growth.

## Score Rationale

Completion is raised to `88` because this page now records current MCP provenance, exact range/padding, constructor-only caller, `acc2drw.tbl` literal bytes, row count source, parse/digit behavior, `Acc2DrwEntry.enabled[0..2]` row shape, source-level clear/push-back expression, fatal path, ownership/source route, UID00041E support coordination, and formal first-draft C++.

Confidence is raised to `90` because exact-address MCP evidence directly proves the function body, caller, literal, line buffer, count source, arithmetic, strings, and helper call, while accepted support docs confirm the member/vector names used by the emitted source. It remains below final-audit range because exact original spellings of `m_acc2drwEntries`, `Acc2DrwEntry`, `enabled`, and the final helper/template declaration policy remain inferred rather than original-symbol-proven.

The historical B011 below-gate/no-owner state is preserved as superseded context: it was valid when the child page was created, but current evidence now supports promotion and formal C++.

## Cross-References

- [UID:0002JR][0x004dfd10-0x004e024a.NewHumanImageLibConstructor](by-memory/0x004dfd10-0x004e024a.NewHumanImageLibConstructor.md)
- [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md)
- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md)
- [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md)
- [UID:0001RI][new-human-motion-tables](by-resource/new-human-motion-tables.md)
- [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md)
- [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md)

## Changes

- 2026-06-30 B009 LoadAcc2DrwTable implementation callback:
  - Before: `84/86`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, and blank formal C++.
  - Changed to: `88/90`, `CANONICAL_OWNER:000092`, `EMITTER_UIDS:000092`, and formal first-draft `NewHumanImageLib::LoadAcc2DrwTable` C++.
  - Evidence: current MCP session `supervisor_resume_20260629` confirms raw `sub_4E1800`, size `0x1c5`, exact `0x004e1800-0x004e19c5` range, padding before distinct successor `0x004e19d0`, sole constructor caller `0x004e0188`, UTF-16LE `acc2drw.tbl` literal at `0x0061c324`, vector clear through end-to-begin assignment, row count from `partEntriesBegin + 0x210`, 4096-byte line reads, decimal parse through `sub_5CEA6D`, hundreds/tens/ones flag construction, full-capacity UID00041E call at `0x004e195a`, and fatal `MessageBoxW` path with raw string bytes.
  - Source-quality result: the old B011 no-owner/no-code blocker is superseded. [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md) is B010-coordinated file-local marker support, so this method can emit source-level `m_acc2drwEntries.push_back(entry)` without requiring a standalone vector-helper body.
