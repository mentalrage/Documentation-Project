*** UID:0002A9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OH | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OH | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool g_selectObjectTargetModeActive = false;
bool g_targetObjectTargetModeActive = false;
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Two externally linked TargetSelectionInputPanes mode bools at 0x0069bed9 and 0x0069beda, read or cleared across UserPane.cpp and defined with the target-selection helpers; surrounding zero bytes remain allocation/alignment with no source declarations. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0069bed8-0x0069bee0 Target Selection Mode Flags

## Status

- Address range: `0x0069bed8-0x0069bee0`
- Entity kind: file-scope external-linkage flag cluster
- Disposition: reconstructable TargetSelectionInputPanes bool definitions with adjacent no-source allocation/alignment bytes
- Rebuild handling: `source-declared/generated-binary`
- Proposed owner: [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) TargetSelectionInputPanes
- Confidence: strong for `0x0069bed9`, `0x0069beda`, and the no-source treatment of the surrounding bytes; high for the accepted source-facing declaration names, with original spelling still inferred rather than symbol-proven.
- Final source definitions: `bool g_selectObjectTargetModeActive = false;` at `0x0069bed9` and `bool g_targetObjectTargetModeActive = false;` at `0x0069beda`. UserPane.cpp directly clears both bytes and reads the second, so the former internal-linkage `static s_` declarations are impossible under the accepted separate source roots. The semantic alias `targetObjectMapSelectionActive` remains useful provenance but is not the primary formal name.

## Storage

| Address | IDA name | Role |
| --- | --- | --- |
| `0x0069bed8` | unnamed byte | unreferenced zero allocation/alignment byte inside the same static-data range; B009 found zero xrefs and zero direct address-pattern matches, so this byte has no source declaration. |
| `0x0069bed9` | `byte_69BED9` | `g_selectObjectTargetModeActive`; externally linked `TargetSelectionInputPanes.cpp` bool for the `SelectObjectWithKeyboardPane` temporary target-selection mode. It is cleared by constructor/key/cancel/shared cleanup and by UserPane, and set by the SelectObject forward-tab helper. |
| `0x0069beda` | `byte_69BEDA` | `g_targetObjectTargetModeActive`; externally linked `TargetSelectionInputPanes.cpp` bool for TargetObject target-dispatch mode. It is read and cleared by UserPane; the former `s_targetObjectMapSelectionActive` spelling remains historical semantic provenance. |
| `0x0069bedb-0x0069bedf` | unnamed bytes | unreferenced zero allocation/alignment bytes after the two bool declarations; B009 found zero xrefs and zero direct address-pattern matches, so these bytes have no source declarations. |

## Evidence

- 2026-06-25 B009 MCP pass used active IDA MCP session `80de0a67` against `NexusTK.exe` (`E:\NTK\Resources\NexusTK\NexusTK.exe.i64`), with `server_health` reporting `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`. No IDA DB edits, renames, comments, types, function creation, or MCP process-management actions were made.
- `xrefs_to 0x0069bed8` returns `0` refs.
- `xrefs_to 0x0069bed9` returns 9 refs: `0x005a6f6f`, `0x005ad4ad`, `0x005ad52a`, `0x005ad560`, `0x005ad660`, `0x005af6c4`, `0x005af79f`, `0x005af7eb`, and `0x005afebe`.
- `xrefs_to 0x0069beda` returns 8 refs: `0x005a5e9a`, `0x005a6f76`, `0x005ad4b4`, `0x005ad567`, `0x005ad63e`, `0x005affc4`, `0x005b008e`, and `0x005b0745`.
- `xrefs_to` for every byte from `0x0069bedb` through `0x0069bedf` returns `0` refs.
- `xrefs_to 0x0069bee0` returns 4 refs into UserPane/local-player-name helpers, proving `0x0069bee0` is the next independent object boundary and not part of this flag cluster.
- `get_bytes 0x0069bed8 size 72` returns zero bytes through the local-name buffer lead-in, so this range is zero-initialized at load.
- Direct address-pattern search found 9 matches for `d9 be 69 00`, 8 matches for `da be 69 00`, 0 matches for `d8 be 69 00`, 0 matches for the `0xdb` through `0xdf` byte addresses, and 4 matches for `e0 be 69 00`.
- `entity_query(kind=names/globals, 0x0069bed0-0x0069bf40)` returned zero named entities/globals, so no original symbol names are present in this IDB window.
- `SelectObjectWithKeyboardPane` constructor/key paths clear `byte_69BED9`, and `0x005afe70` sets it when backing a current `dword_69BF28` object selection into map pane temporary target state.
- B005 2026-06-18 key-handler reanalysis names `byte_69BED9` as the file-scope `s_selectObjectTargetModeActive` flag. It is not a class member because all accesses are absolute `.data` references, and it is not the adjacent TargetObject flag because that path uses `byte_69BEDA`.
- `TargetObjectWithKeyboardPane` constructor/key paths clear `byte_69BEDA`, and `0x005b06f0` sets it when backing a current `dword_69BF2C` target-dispatch selection into map pane temporary target state.
- B005 2026-06-18 TargetObject mouse-handler reanalysis validates `byte_69BEDA` as a TargetObject target-dispatch mode flag even though the mouse method itself does not touch it. The role is inferred from constructor clears, keyboard cancel clears, dispatch-forward sets, UserPane clears, and the pairing with `dword_69BF2C` target-object saved-target state.
- `0x005ad500` and `0x005ad660` use these flags while moving or cancelling the active map target highlight and updating the saved target id globals.
- [UID:0003V9][0x005a5bd0-0x005a7422.UserPaneHandleKeyOrTextEvent](by-memory/0x005a5bd0-0x005a7422.UserPaneHandleKeyOrTextEvent.md) reads the second flag at `0x005a5e9a` and clears both at `0x005a6f6f` and `0x005a6f76`. Those cross-translation-unit accesses are the decisive external-linkage evidence.
- `analyze_component` over the flag-touching target-selection helpers links the two set-to-`1` helpers through the internal call graph: `0x005af710` calls `0x005afe70`, and `0x005b0010` calls `0x005b06f0`. The shared-global set also matches the target-selection state family (`dword_67A748`, `dword_67A74C`, `dword_67A754`, `dword_67A764`, and `dword_67A7C8`).
- B009 decompilation cross-checks preserve the same split:
  - `sub_5AF5F0` (`SelectObjectWithKeyboardPane` constructor) clears `unk_69BED9` at `0x005af6c4`.
  - `sub_5AF710` (`SelectObjectWithKeyboardPane::OnKeyEvent`) clears `unk_69BED9` on Enter and cancel paths at `0x005af79f` and `0x005af7eb`.
  - `sub_5AFE70` (`SelectObjectWithKeyboardPane` forward-tab helper) sets `unk_69BED9 = 1` at `0x005afebe` after resolving `unk_69BF28` and priming active map target state.
  - `sub_5AFEF0` (`TargetObjectWithKeyboardPane` constructor) clears `unk_69BEDA` at `0x005affc4`.
  - `sub_5B0010` (`TargetObjectWithKeyboardPane::OnKeyEvent`) clears `unk_69BEDA` on cancel at `0x005b008e` and calls `sub_5B06F0` on Tab/`v` forwarding.
  - `sub_5B06F0` (`TargetObjectWithKeyboardPane` dispatch-forward helper) sets `unk_69BEDA = 1` at `0x005b0745` and copies `unk_69BF2C` to `unk_69BF30`.
  - `sub_5AD450` clears both bytes when exiting active target-selection mode and otherwise constructs `SelectObjectWithKeyboardPane`.
  - `sub_5AD500` compares/clears `unk_69BED9`, clears `unk_69BEDA`, or sets `unk_69BEDA = 1` while entering TargetObject map-selection state and updating `unk_69BF2C`/`unk_69BF30`.
  - `sub_5AD660` gates target-object navigation when `unk_69BED9 != 1`, proving the two adjacent bytes are separate mode flags rather than aliases.
  - `sub_5A5BD0` compares `unk_69BEDA == 1` before dispatching `sub_5AD660` versus `sub_5AD500`, then later clears both bytes at `0x005a6f6f` and `0x005a6f76`.

## Source-Quality Resolution

The accepted source route is `TargetSelectionInputPanes.cpp` through [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md). Both live bytes are absolute `.data` accesses used by the target-selection input-pane source family, not object-relative fields on `SelectObjectWithKeyboardPane`, `TargetObjectWithKeyboardPane`, `MapPane`, or `UserPane`.

Accepted definitions:

```cpp
bool g_selectObjectTargetModeActive = false;
bool g_targetObjectTargetModeActive = false;
```

The former `s_` declarations are historicalized as invalid because UserPane.cpp is a separate accepted source root and directly accesses both objects. The class family and paired saved-target state make the `TargetObject` spelling the better source-facing definition; exact original spellings remain inferred.

Rejected alternatives:

- `struct TargetSelectionModeFlags`: rejected because no code takes the cluster address as a unit, no word/dword access covers both live bytes, no IDB name/global/type evidence exists for a grouped record, and the handlers reason about the SelectObject and TargetObject flags by separate source families.
- explicit padding/dummy variables for `0x0069bed8` or `0x0069bedb-0x0069bedf`: rejected because those bytes have zero xrefs and zero direct address-constant matches. `char pad[...]`, `bool unused`, or fake named bytes would overfit the final binary layout and invent source not evidenced by current code.
- class-member fields: rejected because every access is an absolute `.data` byte reference, and UserPane/shared target-selection helpers clear both flags without an object receiver.
- generic `s_targetSelectionModeActive`: rejected because there are two distinct bytes with separate set/clear ownership; a generic name would collapse the SelectObject and TargetObject flows and lose the pairing with `s_selectObjectTargetId` and `s_targetObjectTargetId`.
- UserPane, MapPane, or generic DataSection ownership: rejected because UserPane only clears the state, MapPane supplies lookup/highlight dependencies, and [UID:0001Z8][0x0066d000-0x0069d000.DataSection](by-memory/0x0066d000-0x0069d000.DataSection.md) is only the broad section container.
- blank/no-code treatment: rejected because this page is reconstructable, has owner/emitter route [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md), clears the active C++ gate with `90/91`, and the remaining name/declaration/packing blockers are now resolved.

## Reconstruction Notes

Reconstruct the referenced live bytes as module-scope target-selection mode flags near the target-selection input pane implementation. Do not create separate source variables for the unnamed allocation/alignment bytes unless later evidence finds direct xrefs or a wider source-level struct.

B013 ItemWho reanalysis clarifies a separate state path: `ItemWhoInputPane` active state is stored on the [UID:00006Y][ItemWhoInputPaneState](by-class/ItemWhoInputPaneState.md) object at field `+0xfa` and cleared through `ItemWhoInputPaneState::SetInputModeFlag(false)`. Do not fold that object field into this global `byte_69BED9` / `byte_69BEDA` cluster, which belongs to SelectObject and TargetObject keyboard-map modes.

No split is recommended. The exact live bytes and allocation/alignment bytes remain recorded inside one `0x8`-byte (`8` decimal, Verified with `int_convert.py`) cluster because the unreferenced bytes have no independent source role or owner; splitting would create padding-only children around two adjacent source declarations without improving ownership evidence. The next independent object starts at `0x0069bee0` and is documented separately by [UID:0002AA][0x0069bee0-0x0069bf20.LocalPlayerNameWideBuffer](by-memory/0x0069bee0-0x0069bf20.LocalPlayerNameWideBuffer.md).

## Autogen Status

This flag cluster is attached to [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) as reconstructable externally linked target-selection state and emits the exact R9 definitions. The block intentionally omits `0x0069bed8` and `0x0069bedb-0x0069bedf`; those bytes remain binary allocation/alignment coverage, not source declarations.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `91` | The page records the exact eight-byte span, zero initial value, live per-byte xref/no-xref split, instruction-level flag reads/writes, target-selection use sites, cross-TU UserPane reads/clears, next-object boundary, no-split/no-source-allocation guidance, exact R9 definitions, owner/emitter route, rejected alternatives, and historical linkage correction. |
| Confidence `93` | Exact absolute accesses, cross-TU UserPane use, target-selection definitions, no-ref padding, and the independent next boundary prove the storage/linkage model. Confidence remains below 95 because exact original lexical spellings are inferred rather than recovered from symbols. |

## Cross-References

- [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) TargetSelectionInputPanes file.
- [UID:0001L8][0x005af390-0x005b050d.TargetSelectionInputPanes](by-memory/0x005af390-0x005b050d.TargetSelectionInputPanes.md) TargetSelectionInputPanes executable range.
- [UID:0000CT][SelectObjectWithKeyboardPane](by-class/SelectObjectWithKeyboardPane.md) SelectObjectWithKeyboardPane class.
- [UID:0000ED][TargetObjectWithKeyboardPane](by-class/TargetObjectWithKeyboardPane.md) TargetObjectWithKeyboardPane class.
- [UID:0001KL][0x005a2530-0x005b8395.UserPane](by-memory/0x005a2530-0x005b8395.UserPane.md) UserPane memory range.

## Changes

- 2026-07-15 B003 UID0003V9 accepted source-quality callback:
  - Raised `90/91` to `91/93`; preserved exact range, owner/emitter UID0000OH, reconstructable state, blank position, `Nested:0`, zero/xref/no-split facts, and no-source padding treatment.
  - Replaced impossible `static s_` definitions with exact external-linkage R9 definitions because UserPane.cpp reads/clears the same bytes across the established file boundary; retained old names only as historical provenance.

- 2026-06-25 B009 accepted TargetSelectionModeFlags implementation:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`; formal C++ block blank; summary/autogen notes still treated final names, declarations, and padding treatment as provisional.
  - After: `COMPLETION:90`, `CONFIDENCE:91`; owner/emitter [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md), `RECONSTRUCTABLE:TRUE`, blank optional emitter position, and `Nested:0` unchanged; formal C++ now emits `static bool s_selectObjectTargetModeActive = false;` and `static bool s_targetObjectTargetModeActive = false;`.
  - Evidence: B009 MCP session `80de0a67` confirmed 9 refs to `0x0069bed9`, 8 refs to `0x0069beda`, zero refs and zero direct address-pattern matches for `0x0069bed8` and `0x0069bedb-0x0069bedf`, zero-initialized bytes, next-object boundary refs at `0x0069bee0`, decompiled set/clear/compare sites in SelectObject/TargetObject/UserPane/shared target-selection helpers, no grouped-struct evidence, absolute-address storage rather than class-member fields, and no reason to emit dummy padding declarations.
- 2026-06-13 A001 refreshed low-score batch:
  - Before: `COMPLETION:78`, `CONFIDENCE:82`.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:88`; kept owner/emitter [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md), and corrected rebuild handling to `source-declared/generated-binary`.
  - Evidence: live IDA MCP confirmed 9 refs to `byte_69BED9`, 8 refs to `byte_69BEDA`, zero refs to `0x0069bed8` and `0x0069bedb-0x0069bedf`, zero-initialized bytes/value for the cluster, set/clear/compare instructions for both live flags, and helper-family ownership through target-selection call/data-flow. No split was made because the unreferenced bytes are documented packing inside the same file-owned static-data cluster.
- 2026-06-18 B005 source-name sync: preferred `s_selectObjectTargetModeActive` for `byte_69BED9` after key/mouse handler reanalysis; score unchanged because final exact spelling remains descriptive.
- 2026-06-18 B005 TargetObject mouse-handler support sync: preferred `s_targetObjectTargetModeActive` / `s_targetObjectMapSelectionActive` for `byte_69BEDA` after constructor/keyboard/dispatch-forward/UserPane cleanup reanalysis; score unchanged because final exact spelling remains descriptive.
- 2026-06-22 B013 ItemWho state clarification: score unchanged; documented that ItemWho active state uses `ItemWhoInputPaneState` field `+0xfa`, not this module-scope flag cluster.
- 2026-06-02:
  - Before: scored `75/65`, reconstructability and parent blank.
  - After: scored `78/82`, marked reconstructable, and attached to [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) with no emitted C++.
  - Why: existing evidence proves the two active target-selection mode flags and their owner family, while final names and padding treatment remain provisional.
