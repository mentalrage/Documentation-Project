*** UID:0004GX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00009F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00009F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool NewUserMiscDialogPane::HandleCreateCharacterReply(const unsigned char *packet)
{
    MemoryMan *memoryMan = GetMemoryMan();
    const unsigned char result = packet[1];

    if (result != 0) {
        if (g_pScreenDimmer != NULL) {
            delete g_pScreenDimmer;

            char narrowText[256];
            wchar_t wideText[256];
            const unsigned int textLength = packet[2];

            memoryMan->MemmoveWrapper(narrowText, packet + 3, textLength);
            narrowText[textLength] = '\0';

            const unsigned char wideLength = static_cast<unsigned char>(
                MultiByteToWideChar(
                    CP_ACP, 0, narrowText, textLength, wideText, 256));
            wideText[wideLength] = L'\0';

            if (wideText[0] != L'\0')
                new AlertPane(wideText, this, kDialogOkButtonText, NULL);
        }

        return true;
    }

    if (g_pScreenDimmer != NULL)
        delete g_pScreenDimmer;

    char narrowText[256];
    wchar_t wideText[256];
    const unsigned int textLength = packet[2];

    memoryMan->MemmoveWrapper(narrowText, packet + 3, textLength);
    narrowText[textLength] = '\0';

    const unsigned char wideLength = static_cast<unsigned char>(
        MultiByteToWideChar(
            CP_ACP, 0, narrowText, textLength, wideText, 256));
    wideText[wideLength] = L'\0';

    if (wideText[0] != L'\0')
        new AlertPane(wideText, this, kDialogOkButtonText, NULL);

    SlideCloseVertical();
    CloseDialog();
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-ready NewUserMiscDialogPane create-character reply handler; unconditionally acquires MemoryMan, parses result byte 1 and length byte 2, copies ANSI text from byte 3, converts through MultiByteToWideChar with low-byte result truncation, conditionally deletes g_pScreenDimmer and shows AlertPane text, closes only on result zero, always returns true, and emits through class UID00009F. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fcb10-0x004fccee - NewUserMiscDialogPane HandleCreateCharacterReply

## Status

- Source-facing inferred signature: `bool NewUserMiscDialogPane::HandleCreateCharacterReply(const unsigned char *packet)`.
- Owner: [UID:00009F][NewUserMiscDialogPane](by-class/NewUserMiscDialogPane.md).
- Emitter/source route: [UID:00009F][NewUserMiscDialogPane](by-class/NewUserMiscDialogPane.md) into `NexusTK/login/NewUserMiscDialogPane.cpp`; this child owns the method body while the class page owns the private H declaration.
- Direct caller: [UID:0004GP][0x004fc5c0-0x004fc5e6.NewUserMiscDialogPaneHandlePacketEvent](by-memory/0x004fc5c0-0x004fc5e6.NewUserMiscDialogPaneHandlePacketEvent.md), which tail-forwards the unchanged packet pointer only for opcode `0x02`.
- Current persisted IDA state after supervisor Gate 2B catalog entry `0437` identifies caller `NewUserMiscDialogPane__HandlePacketEvent` as `bool __thiscall(EventHandlerFacet *eventHandlerFacet, Event *event)` and this reply function `NewUserMiscDialogPane__HandleCreateCharacterReply` as `bool __thiscall(NewUserMiscDialogPane *this, const unsigned __int8 *packet)`. The caller frame argument remains `event +0x8/4 Event *`, and the only reply xref remains code edge `0x004fc5e1`. The analysis-layout `NewUserMiscDialogPane` UDT is exactly 620 bytes (`0x26c`) with the sole member `DialogPane base` at offset `0`, size 620; this improves IDA receiver typing without changing the human source signature or class method body.
- Rebuild handling: source-authored private reply handler, now source-ready at `92/94` with exact CPP emitted through UID00009F. The target H stays blank because UID00009F already contains `bool HandleCreateCharacterReply(const unsigned char *packet);`.

## Evidence

- Persisted supervisor readback at `0x004fcb10` returns `NewUserMiscDialogPane__HandleCreateCharacterReply`, size `0x1de`, with exact prototype `bool __thiscall(NewUserMiscDialogPane *this, const unsigned __int8 *packet)` in the saved and independently reopened canonical database.
- MCP `xrefs_to 0x004fcb10` reports code xref `0x004fc5e1` from wrapper `0x004fc5c0`.
- That edge is the sole inbound route. UID0004GP performs no copy, mutation, ownership transfer, status parsing, or reply inlining before forwarding.
- MCP decompile reads result byte at `payload + 1`; when nonzero, it closes active UI state and reports the message without dismissing the dialog.
- It reads message length at `payload + 2`, copies ANSI bytes from `payload + 3` through `sub_516220`, NUL-terminates, converts with `MultiByteToWideChar`, and shows alert text through `sub_49FEB0`.
- On the success/done path it calls `sub_49EB90` and `sub_49DAD0` to close/dismiss the dialog.
- Callees include `sub_516030`, `sub_516220`, `MultiByteToWideChar`, `sub_4F4AA0`, `sub_49FEB0`, `sub_49EB90`, `sub_49DAD0`, and security-cookie check.
- Packet relationship: byte `0` is opcode `0x02`, byte `1` is result/status, byte `2` is the one-byte ANSI text length, and bytes `3+` are ANSI message text copied into bounded 256-byte narrow and 256-wide buffers.
- Return relationship: the current body returns `true` on every observed path; UID0004GP returns that result directly on the opcode match path.
- UID00009F's complete formal H declaration owns the private method declaration. This page remains the eventual body owner and must not move that declaration or duplicate UID0004GP's bridge body.

## Range And Boundaries

- Exact body: `[0x004fcb10,0x004fccee)`, size `0x1de` / 478 bytes, one modeled function.
- Exact target-body SHA256: `D18BEDE3782D935B13EF0E82354363B3D2D111A9F028203590153AD5AB7E190E`.
- First 16 bytes: `55 8B EC 6A FF 68 C4 1A 60 00 64 A1 00 00 00 00`.
- Last 16 bytes: `8B 4D F0 33 CD E8 47 AA 0C 00 8B E5 5D C2 04 00`.
- [UID:0004GW][0x004fcb0d-0x004fcb10.NewUserMiscDialogPaneReplyPadding](by-memory/0x004fcb0d-0x004fcb10.NewUserMiscDialogPaneReplyPadding.md) owns the exact three-byte leading `0xcc` alignment. Two `0xcc` bytes begin at exclusive end `0x004fccee` and are compiler padding, not source statements.
- No split, merge, child creation, range repair, or reclassification is required.

## Caller And Reachability

- The sole inbound edge is code xref `0x004fc5e1` in UID0004GP. No vtable cell points directly to this private helper.
- UID0004GP reads Event packet data at `+0x0c`, rejects every opcode except `0x02`, performs compiler-only EventHandler-facet normalization, and tail-calls this method with the unchanged packet pointer.
- Every target path returns true; UID0004GP returns that value directly on the opcode match.
- The target's memory, conversion, allocator, alert, dimmer, and dialog-close callees are dependencies, not alternate owners.

## Packet Layout

- `packet[0]`: opcode `0x02`, checked by UID0004GP.
- `packet[1]`: result/status byte. Zero is the completed/success path because only that branch dismisses the dialog.
- `packet[2]`: unsigned one-byte ANSI message length.
- `packet + 3`: ANSI message bytes copied exactly through the retained `MemoryMan` receiver.
- The method trusts the one-byte length and writes the narrow terminator at that index. No additional source-level bounds check is observed or reconstructed.

## Branch Behavior

- `GetMemoryMan()` runs before result dispatch on every invocation and its returned `MemoryMan *` is retained for the copy call.
- Nonzero result with null `g_pScreenDimmer`: bypass deletion, copy, conversion, alert, and dismissal; return true.
- Nonzero result with live `g_pScreenDimmer`: delete the dimmer, process optional alert text, do not call either close helper, and return true.
- Zero result: delete the dimmer only when present, process message text regardless of dimmer presence, call `SlideCloseVertical()`, then `CloseDialog()`, then return true.
- The duplicated branch-local conversion blocks are preserved. Factoring them into a new source helper would invent an unobserved call boundary and alter the source/optimizer shape.

## Text Conversion

- `MemoryMan::MemmoveWrapper` copies exactly `textLength` bytes from `packet + 3` into `char narrowText[256]`; `narrowText[textLength]` is then set to `\0`.
- `MultiByteToWideChar` receives `CP_ACP`, flags zero, the exact byte count, destination `wchar_t wideText[256]`, and capacity 256.
- The machine body retains only the low byte of the conversion return before writing `wideText[wideLength] = L'\0'`; the formal CPP preserves that truncation explicitly.
- If `wideText[0]` is nonzero, the method allocates `AlertPane(wideText, this, kDialogOkButtonText, NULL)`. The ignored allocation result matches the UI ownership/lifetime pattern.

## Ownership And Source Placement

- Direct owner and sole emitter: UID00009F `NewUserMiscDialogPane`.
- CPP destination: `NexusTK/login/NewUserMiscDialogPane.cpp`, after `ShowControlHelp` and before applicable destructor-family source/singleton storage.
- H destination: the existing UID00009F class declaration. This target's H channel intentionally remains blank to prevent duplicate declarations.
- UID0000LX is the source-file route; UID0002Q7 is a non-emitting mixed index; UID0004GP is the thin virtual caller. None is an alternate body emitter.
- Third-party import is not applicable because this is class-specific NexusTK dialog and packet-reply logic.

## IDA Current State

- Persisted catalog `0437` readback identifies `NewUserMiscDialogPane__HandleCreateCharacterReply`, prototype `bool __thiscall(NewUserMiscDialogPane *this, const unsigned __int8 *packet)`, 17 basic blocks, cyclomatic complexity 5, exact range `[0x004fcb10,0x004fccee)`, and exactly one inbound code xref at `0x004fc5e1` from `NewUserMiscDialogPane__HandlePacketEvent`.
- Current frame rows are `memoryMan +0x10/4 void *`, `wideText +0x14/0x200 WCHAR[256]`, `narrowText +0x214/0x100 CHAR[256]`, `var_10 +0x314/4 _DWORD`, `var_C +0x318/4 _DWORD`, `var_4 +0x320/4 _DWORD`, `__saved_registers +0x324/4 _DWORD`, `__return_address +0x328/4 _UNKNOWN *`, `packet +0x32c/4 const unsigned __int8 *`, and EH pseudo-argument `arg_4 +0x330/4 _DWORD`. Decompilation renders `MemoryMan *memoryMan`, `WCHAR wideText[256]`, and `CHAR narrowText[256]`; offsets, widths, storage, compiler rows, and EH rows remain protected unchanged.
- The physical `memoryMan +0x10/4 void *` frame row remains intentionally generic because the compiler coalesces one stack slot across incompatible lifetimes: it first holds the `MemoryMan *` returned by `GetMemoryMan()` for both memmove calls, then is overwritten by allocated `AlertPane *` results and used by exception cleanup. A narrower IDA frame type would misrepresent one of those live ranges; the human C++ still uses properly typed source variables rather than exposing the coalesced storage artifact.
- The function-repeatable comment is exactly `Create-character reply: packet[1] is result, packet[2] is ANSI length, packet+3 is text; delete active screen dimmer, alert nonempty text, close only on result zero, and always return true.` Address regular, address repeatable, and function regular entry channels remain blank; existing interior operand comments remain protected evidence.
- Full-body SHA256 remains `D18BEDE3782D935B13EF0E82354363B3D2D111A9F028203590153AD5AB7E190E`. The three-byte leading `0xcc` alignment and two-byte successor `0xcc` fence remain outside the exact function body. Canonical IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` was independently reopened after the sole save at 143,206,743 bytes, SHA256 `BD6FBD2512070832EC8DE94603D2000B32AF20FA3C817BA0D64CB776D1F0F2C9`, last write UTC `2026-08-12T19:32:50.1492846Z`; all protected names, comments, frame storage, body bytes, xrefs, ranges, and items remained exact. No further IDA action is required for this callback.

## C++ Disposition

Formal first-draft CPP is inserted in the target CPP channel. It preserves unconditional MemoryMan acquisition, the exact nonzero null-dimmer early return, duplicated branch-local copy/conversion code, low-byte conversion-length truncation, optional AlertPane construction, zero-result-only close order, and always-true return. Compiler SEH/security-cookie scaffolding, raw labels, and decompiler casts are excluded. Formal target H remains blank because UID00009F already owns the exact private declaration.

## Historical Assumptions

- The earlier generic `wrapper` wording is superseded by UID0004GP's exact `HandlePacketEvent(Event *)` identity and sole tail edge.
- Raw labels such as `sub_4FCB10` and helper `sub_*` names remain binary-evidence labels only, not finalized source spellings.
- The pre-Gate-2B target state used `sub_4FCB10`, scalar prototype `char __thiscall(Pane *this, int)`, frame names `Block` / `WideCharStr` / `MultiByteStr` with argument `arg_0`, and four blank entry comment channels. Fresh accepted evidence supersedes that state: the human function name, bool byte-pointer prototype, `memoryMan` / `wideText` / `narrowText` / `packet` frame names, and function-repeatable comment are now present, while the other three entry comment channels remain deliberately blank.
- The 2026-08-11 post-callback analysis prototype `bool __thiscall(Pane *this, const unsigned __int8 *packet)` and caller prototype `bool __thiscall(void *eventHandlerFacet, Event *event)` were valid intermediate generic receiver states. Supervisor catalog entry `0437` supersedes only those analysis receiver types with `NewUserMiscDialogPane *` and `EventHandlerFacet *`; it does not change the source-facing class declarations, ABI, behavior, bytes, or routing.
- The prior unchanged blank-emitter/formals state was a bounded caller-support disposition, not proof that the method was compiler-generated or unreconstructable. UID0004GX's exhaustive B009 pass supersedes that temporary state with class emission and exact CPP while retaining this history.

## Changes

- 2026-08-12 B009 UID0004GX post-IDA reconciliation: synchronized current IDA prose with supervisor catalog entry `0437` and independently reopened canonical IDB `BD6F...F2C9`; recorded the exact 620-byte `NewUserMiscDialogPane` analysis UDT, derived target receiver, typed EventHandler-facet caller receiver, unchanged complete frames/comments/ranges/xrefs/body hash, and evidence-backed `void *` disposition for the compiler-coalesced `memoryMan` slot. The prior `Pane *` / `void *eventHandlerFacet` receiver state is retained as dated history. Metadata, Item Summary, formal CPP/H, behavior, owner/emitter/source placement, score, coverage, generated output, and source-facing class code are unchanged.
- 2026-08-11 B009 UID0004GX target-prose callback: synchronized Status, Evidence, and IDA Current State with the accepted fresh name, pointer prototype, frame matrix, exact function-repeatable comment, three protected blank entry channels, unchanged body hash, sole caller, and exact boundaries; historicalized the superseded raw name/scalar argument/old frame/blank-comment state. Metadata, Item Summary, formal CPP/H, range, behavior, owner/emitter/source placement, support pages, prior history, coverage/generated/tracker/audit/lifecycle state, and IDA were not changed.
- 2026-08-11 B009 UID0004GX implementation callback: raised `86/91 -> 92/94`, preserved owner UID00009F/reconstructable true/blank position/Nested 0, set emitter UID00009F, inserted exact formal CPP, kept target H blank for the class-owned declaration, and incorporated complete range/hash/boundary, sole-caller, packet-layout, branch-asymmetry, MemoryMan, conversion, alert, close-order, source-placement, current-frame, and IDA-handoff evidence. No coverage, generated, tracker, audit, lifecycle, goal, or IDA file was manually edited; scoped validation used generated refresh disabled, and its validator-owned registry/statistics bookkeeping is recorded in the B009 report.
- 2026-07-29 B007 post-Gate2B reconciliation: recorded catalog `0363` and the applied caller name/prototype/frame/comment while confirming this reply function, its sole inbound edge, and its blank comments were not mutated. Applied checkpoint SHA256 `296ED21C6665B7C0D8EFBC515B137101F8506FD945C6C7E14C3E6F655063CF8B`, 143,190,676 bytes, saved `2026-07-29T14:22:15.9735862-04:00`, is historical. Following an unrelated supervisor transaction, current saved IDB SHA256 is `905D1AB131C953911CD1DF5E536AF36EAECFA67486E2F800907B36C0673604C1`, 143,190,814 bytes, saved `2026-07-29T14:42:53.5091352-04:00`; bounded read-only recheck confirms `sub_4FCB10` still spans `[0x004fcb10,0x004fccee)`, retains blank address/function comment channels, and has exactly one inbound code xref from cataloged caller `NewUserMiscDialogPane__HandlePacketEvent` at `0x004fc5e1`. Backup `E:\NTK\Resources\NexusTK\backups\NexusTK.exe.pre-B007-UID0004GP-20260729-1420.i64` preserves historical SHA256 `3C8F31781C94DF74AADDB65B3D944CD2CA4387C448918BBBFFD60B41C3625B69`, 143,191,631 bytes. Score, emitter/formals, and separate UID0004GX ownership remain unchanged.
- 2026-07-29 B007 UID0004GP support synchronization: preserved `86/91`, owner UID00009F, reconstructable true, blank emitter, and blank CPP/H; added exact private source signature, sole caller/tail route, opcode/result/length/text byte layout, unchanged packet pointer, no-mutation relationship, bounded buffer/conversion behavior, and always-true return. IDA, manual coverage, and generated output were not modified by B007.
