*** UID:00029K | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static ImageAnimationTable g_defaultMonsterAnimationTable;
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Static 12-byte monster default animation table object, source-facing g_defaultMonsterAnimationTable; current B011 MCP shows zero-filled storage, while static initialization constructs the object, the loader allocates 21 groups, seeds default frame duration/alpha, and lookup returns it out of range. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0069b424-0x0069b430 ImageLibraryFallbackRecord

## Summary

- Address range: `0x0069b424-0x0069b430`
- Entity kind: 12-byte monster-image table fallback/default object
- Source-facing inferred name: `g_defaultMonsterAnimationTable`
- Static image value pattern: current MCP shows three zero-filled dwords; construction/static initialization performs setup.
- Disposition: reconstructable NexusTK global data
- Rebuild handling: `source-declared/generated-binary`
- Confidence: strong for layout, lifetime, fallback use, loader writes, and concrete source owner; medium for final source-facing type/name
- Parent status: assigned to [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) because that direct file parent now clears `86/86` and documents this concrete monster default-table storage. [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) remains the shared helper/type-shape context rather than the direct concrete-storage owner.
- Reconstruction note: formal C++ now declares `static ImageAnimationTable g_defaultMonsterAnimationTable;` with no explicit initializer. The accepted method C++ uses this source-facing variable name, while shared helper method bodies remain documented under [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md).

This range is a static 12-byte outer table object used by the monster image table code. Live IDA shows it is constructed during static initialization, destroyed during static shutdown, allocated with `0x15` / 21 nested group slots (Verified with int_convert.py) by the monster table loader, seeded with default one-entry group records, and returned by the monster table lookup helper when a requested table index is out of range.

The existing filename says "fallback record"; the stronger current interpretation is "fallback/default table object." It is not the separate 10-byte inner frame fallback at `0x0066deb0`, which is returned by the shared frame-entry lookup helpers.

## Layout

| Range | Role |
| --- | --- |
| `0x0069b424-0x0069b428` | First dword of the 12-byte outer table object; zeroed by the constructor helper. |
| `0x0069b428-0x0069b42c` | Middle dword containing count/status data used through the object base pointer; no direct global refs in current IDA. |
| `0x0069b42c-0x0069b430` | Entry-pointer field; read directly by the monster table loader while seeding default groups. |

## Live IDA Evidence

Historical IDA MCP checks on 2026-06-06 and 2026-06-11 reported the bytes from `0x0069b420-0x0069b43c` as all `0xff`. B011 current MCP on 2026-06-30 supersedes that stale storage claim for this pass: `get_bytes 0x0069b420` size `28` returned zero bytes, and `get_int` returned `0` at `0x0069b424`, `0x0069b428`, and `0x0069b42c`. Treat the object as zero-filled `.data` storage constructed by static initialization, not as an explicit all-`0xffffffff` initializer.

```text
00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
```

The direct reference counts around the range are:

| Address | Direct refs | Interpretation |
| --- | ---: | --- |
| `0x0069b420` | 132 | Previous image-library load/error flag. |
| `0x0069b424` | 4 | Start of this 12-byte table object. |
| `0x0069b428` | 0 | Middle object field, accessed through the object base rather than a direct global address. |
| `0x0069b42c` | 4 | Entry-pointer field read while seeding default groups. |
| `0x0069b430` | 11 | Next 12-byte riding-definition table object. |

The four refs to the object start are:

| Ref site | Interpretation |
| --- | --- |
| `0x00419fb0` | Static initialization wrapper passes this object to the 12-byte table-object initializer and registers the cleanup wrapper. |
| `0x004dacfe` | Monster table loader passes this object to the shared 12-byte object allocation/grow helper with count `0x15`. |
| `0x004db33d` | Monster table lookup returns this object when the requested outer-table index is out of range. |
| `0x0060c280` | Static shutdown wrapper passes this object to the table-object cleanup helper. |

The four refs to `0x0069b42c` are all inside the monster table loader at `0x004dad20`, `0x004dad30`, `0x004dad3d`, and `0x004dad49`. They read the entry-pointer field while initializing `0x15` eight-byte group records.

2026-06-11 A003 Batch 235 reconfirmed the same `4/0/4` direct refs across this object's start/middle/entry-pointer fields, neighboring `132` refs to [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md), and `11` refs to the adjacent [UID:0001PL][0x0069b430-0x0069b43c.RidingDefinitionGlobalTable](by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md). Its all-`0xff` byte claim is now historicalized by the 2026-06-30 B011 zero-fill correction. The object-start refs remain the static initializer wrapper, `MonsterImageLib::LoadMonsterTables` allocation, out-of-range monster-table lookup fallback, and static cleanup wrapper.

## Lifetime And Behavior

The static initialization wrapper at `0x00419fb0-0x00419fc6` calls the table-object initializer on this range and registers `0x0060c280` for shutdown cleanup. The initializer at `0x00528930-0x00528947` zeroes all three dwords in the object.

The static cleanup wrapper at `0x0060c280-0x0060c28a` calls the cleanup helper at `0x00528950-0x005289c6`. That helper reads the entry pointer from the third dword, destroys each 8-byte group record when the pointer is non-null, frees the backing allocation, clears the count byte inside the middle field, and clears the entry pointer.

The monster table loader at `0x004dac40-0x004daebd` uses this object as a default/fallback outer table:

1. It reads the monster table count from the resource stream and allocates the owning instance table for loaded monster rows.
2. It calls the shared outer-table allocation helper on this static object with count `0x15`.
3. It walks `0xa8` bytes as `0x15` eight-byte child groups through the entry pointer at `0x0069b42c`.
4. For each group, it allocates one 10-byte `ImageAnimationFrame` entry and seeds that entry with `frameOffset = 0`, `frameDuration = 30000`, and `blendAlpha = 0`.
5. It then parses the resource-backed monster table rows into the owning instance table.

The small monster table lookup at `0x004db330-0x004db353` returns `loadedTableBase + 12 * index` when the index is in range; otherwise it returns the address `0x0069b424`. This proves the static object is the out-of-range outer-table fallback for monster image table lookups.

## Boundary Notes

| Boundary | Neighbor | Interpretation |
| --- | --- | --- |
| `0x0069b420-0x0069b424` | [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md) | Previous image-library load/error flag slot ends immediately before this table object. |
| `0x0069b424-0x0069b430` | this page | Static 12-byte monster image fallback/default outer table object. |
| `0x0069b430-0x0069b43c` | [UID:0001PL][0x0069b430-0x0069b43c.RidingDefinitionGlobalTable](by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md) | Adjacent riding-definition global table object uses the same 12-byte helper shape but has separate refs and owner. |

The zero direct refs to `0x0069b428` do not make a gap: the middle field is part of the same 12-byte object and is accessed through helper methods after the object base is passed by address.

## Reconstruction Notes

This storage should reconstruct as a source-level static/global table object, not as an arbitrary byte blob. The concrete storage belongs with `MonsterImageLib` lookup/loading code and should use the source-facing name `g_defaultMonsterAnimationTable`; the construction, cleanup, allocation, and nested-row helper implementation remains documented under the shared monster/riding table-helper source family.

Emit this page as the concrete static table declaration:

```cpp
static ImageAnimationTable g_defaultMonsterAnimationTable;
```

Do not add an explicit initializer because current storage is zero-filled and the static initialization/helper path performs setup. Helper method bodies remain part of the shared monster/riding table-helper source family rather than this data page.

## Autogen Status

`RECONSTRUCTABLE` is `TRUE` because source-level static storage is required for the monster-image table fallback behavior. `CANONICAL_OWNER` and `EMITTER_UIDS` are [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) because this child is `88/92` and the direct file parent is `89/89` with explicit concrete-storage documentation. The C++ block now emits the static `ImageAnimationTable` declaration; [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) references this storage as `g_defaultMonsterAnimationTable`.

Batch 235 parent-gate refresh: Batch 042 correctly withheld assignment while the concrete owner candidate was only `84/84`; that blocker is now stale. [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) now clears the strict gate and records this page as concrete monster-image state allocated and seeded by `MonsterImageLib::LoadMonsterTables`, while [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) owns the shared helper implementation. Routing through `MonsterImageLib` therefore satisfies the child/direct-parent `85/85` gate without assigning the storage to the helper/type-family page.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `88` | The page now records current zero-filled bytes, historical all-`0xff` correction, direct refs to each object field and neighbors, static initializer/destructor behavior, cleanup semantics, monster-loader default-table seeding, out-of-range lookup fallback behavior, distinction from the separate inner frame fallback, accepted source-facing variable name, boundary evidence, direct parent-gate resolution, autogen disposition, and formal static declaration. Completion remains capped because helper method bodies and exact original type spelling live in the shared helper family. |
| Confidence `92` | Live IDA strongly confirms the 12-byte object boundary, current zero-filled storage, object lifetime, entry-pointer field use, loader seeding loop, fallback return, accepted `g_defaultMonsterAnimationTable` role, and clean concrete-owner split between `MonsterImageLib` storage and shared helper implementation. Confidence stays below final-audit range because exact original helper type spelling and full shared declaration shape are still inferred. |

## Cross-References

- [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md) shared monster/riding table helper functions.
- [UID:00017C][0x004dac40-0x004dc420.MonsterImageLibLocalMethodCluster](by-memory/0x004dac40-0x004dc420.MonsterImageLibLocalMethodCluster.md) MonsterImageLib aggregate.
- [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) MonsterImageLib table loader.
- [UID:0000WB][0x00419f86-0x0041a040.StaticInitializerWrappersBeforePoolAllocators](by-memory/0x00419f86-0x0041a040.StaticInitializerWrappersBeforePoolAllocators.md) static initializer wrapper neighborhood.
- [UID:00024G][0x0060c26a-0x0060c320.StaticDestructorWrappersBeforePoolAllocators](by-memory/0x0060c26a-0x0060c320.StaticDestructorWrappersBeforePoolAllocators.md) static destructor wrapper neighborhood.
- [UID:00029J][0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag](by-memory/0x0069b420-0x0069b424.ImageLibraryLoadErrorFlag.md) previous image-library flag boundary.
- [UID:0001PL][0x0069b430-0x0069b43c.RidingDefinitionGlobalTable](by-memory/0x0069b430-0x0069b43c.RidingDefinitionGlobalTable.md) adjacent riding-definition table object.

## Changes

- 2026-06-30 B011 empty-emitter family implementation callback:
  - Score changed from `86/91` to `88/92`.
  - Evidence: inserted the formal `static ImageAnimationTable g_defaultMonsterAnimationTable;` declaration and corrected stale all-`0xff` storage text to current zero-filled storage from B011 MCP `get_bytes`/`get_int`. Preserved static initializer/cleanup refs, loader allocation with `0x15` groups, default frame seeding, out-of-range lookup fallback, adjacent boundary refs, and the split between concrete MonsterImageLib storage and shared helper implementation.
- 2026-06-25 B001 implementation callback:
  - Score unchanged at `86/91`.
  - Evidence: connected this concrete default table storage to the accepted [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) C++ name `g_defaultMonsterAnimationTable`. Updated default seed wording from start/end/flag to `frameOffset`, `frameDuration`, and `blendAlpha`, and replaced stale parent-blank/autogen-parent wording with current owner/emitter fields.
- 2026-06-06: Raised from `74/80` to `84/90` after live IDA MCP confirmed all bytes in `0x0069b420-0x0069b43c`, direct refs to `0x0069b424`/`0x0069b42c`, zero direct refs to `0x0069b428`, static initializer and cleanup wrappers, the table-object initializer/cleanup helpers, monster-loader allocation and `0x15` default-group seeding, out-of-range lookup fallback behavior, and clean neighboring table-object boundaries. Removed stale raw-label/provenance wording and kept parent/C++ blank because final source name and owner remain below the final-source gate.
- 2026-06-07 A002 Batch 042 parent-gate review:
  - What existed before: `COMPLETION:84`, `CONFIDENCE:90`, and blank parent.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:90`, and parent still blank.
  - Summary/evidence: associated helper page [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) was improved to `85/85`, and concrete owner candidate [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) was improved to `84/84`. Assignment remains withheld because the helper page does not necessarily own the concrete static storage and the concrete `MonsterImageLib` parent still does not satisfy the strict 85/85 parent gate.
- 2026-06-11 A003 Batch 235 parent-gate refresh:
  - What existed before: `COMPLETION:85`, `CONFIDENCE:90`, and blank parent because `MonsterImageLib` had been below the strict parent gate.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:91`, and current owner/emitter route [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md).
  - Summary/evidence: live IDA MCP reconfirmed the all-`0xff` bytes, field xref counts, static initializer/cleanup, loader allocation/seeding, lookup fallback, and neighbor boundaries. The direct file parent [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) is now `86/86` and explicitly documents this concrete storage, while [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) remains helper/type-shape context.
