*** UID:00047Q | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserListPane::OnItemActivated()
{
    UserListRecord *record = GetRecordAt(m_selectedIndex);

    g_pUserListDialogPane->CloseDialog();

    if (g_useEpfAssets)
        new NewSayToUserMessageInputPane(record->name);
    else
        new SayToUserMessageInputPane(record->name);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: UserListPane::OnItemActivated is the ListPane +0x7c activation override: after base double-click hit/selection it fetches the indexed UserListRecord, queues the user-list dialog for deferred deletion, and opens the modern or legacy direct-message input pane from record->name according to g_useEpfAssets. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0059ed60-0x0059ee0c - UserListPane::OnItemActivated

## Status

- Entity kind: source-authored `UserListPane` virtual item-activation method.
- Address range: `[0x0059ed60,0x0059ee0c)`, exactly 172 bytes.
- Owner: [UID:0000FO][UserListPane](by-class/UserListPane.md) `UserListPane`.
- Emitter: [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md) `UserListDialogPane`.
- Source ABI: `virtual void OnItemActivated()`; no explicit argument is passed by the base dispatcher and its caller ignores the decompiler-propagated constructor result.
- Rebuild handling: emit the ordinary human-written method above. Keep the target H channel blank because the declaration belongs to UID0000FO's class-definition channel.

## Vtable And Invocation Contract

- `0x0062eb58 -> 0x0059ed60` is the sole inbound target xref and the `UserListPane` primary-vtable slot at `+0x7c`.
- Base `ListPane` mouse dispatch at `0x004f4190` handles event case `2` by requiring bounds containment and a successful item hit, invoking slot `+0x78` to select the row, and then invoking slot `+0x7c` with no explicit argument. The target is therefore entered with a valid selected index.
- `PhoneBookListPane` independently uses the same inherited slot at `0x0062dc60 -> 0x0058d110` for its selected-item action. This shared contract supports the source-facing name `OnItemActivated`; the older behavior-specific name described only this derived override's effect.

## Behavior And Evidence

- The method loads inherited `m_selectedIndex` from complete-pane offset `+0x134` and calls `ListPane_GetItem(this, index)` at `0x004f3dc0`. The class-level typed wrapper is `GetRecordAt(int)`.
- No selected-record null test exists. The returned row is dereferenced immediately, which is valid because the base activation path establishes a successful hit and selection before dispatch. Adding a `GetSelectedRecord()` guard changes the observed control flow.
- `UserListRecord::name[256]` begins at packed-row offset `+0x0e`. Both branch constructors receive that exact UTF-16 field.
- The target loads [UID:0003EI][g_pUserListDialogPane](by-global/g_pUserListDialogPane.md) directly and unconditionally calls inherited `DialogPane::CloseDialog()` at `0x0049dad0`. That method queues deferred deletion; there is no singleton guard and no evidence for a free `ClosePane` helper.
- Deferred deletion preserves the current row long enough for either constructor to copy `record->name` synchronously into its own `m_recipientName[128]`, so the observed close-before-construction order is source-safe without an invented temporary or reordered calls.

## Asset-Mode And Constructor Branches

- The byte at `0x0066da97` is documented as bool [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md). A true value selects the modern class; false selects the legacy class.
- The modern branch allocates `0x208` bytes and calls `NewSayToUserMessageInputPane::NewSayToUserMessageInputPane(const wchar_t *)` at `0x005b1990` from callsite `0x0059edc6`. That constructor copies the recipient and formats `L"To %s : "`.
- The legacy branch allocates `0x208` bytes and calls `SayToUserMessageInputPane::SayToUserMessageInputPane(const wchar_t *)` at `0x005b1570` from sole caller `0x0059edf7`. It copies the same field and formats `L"-> %s: "`.
- The two source-level allocations are ordinary `new` expressions. Raw allocator calls, allocation-result null checks, constructor vptr writes, and constructor-unwind delete calls are compiler lowering rather than handwritten source.

## Range And Compiler Lowering

- The predecessor ends at `0x0059ed5e`; `[0x0059ed5e,0x0059ed60)` is two bytes of `0xcc` parent-owned alignment. `[0x0059ee0c,0x0059ee10)` is four bytes of `0xcc` successor alignment. Neither span belongs to this target.
- Folded `loc_60A213` is exactly `[0x0060a213,0x0060a21e)`, an 11-byte state-`0` modern-constructor cleanup that deletes the allocated block only when construction unwinds. Unwind-map entry `0x00667264` binds that role.
- Folded `loc_60A21E` is exactly `[0x0060a21e,0x0060a229)`, an 11-byte state-`1` legacy-constructor cleanup with the analogous delete sequence. Unwind-map entry `0x0066726c` binds it.
- `SEH_59ED60` is exactly `[0x0060a229,0x0060a244)`, a 27-byte frame-handler wrapper that checks the stack cookie, loads `stru_667274`, and tail-jumps to `__CxxFrameHandler3`; the target prologue references it at `0x0059ed65`.
- All three remote fragments are individually compiler-covered by this method's two `new` expressions and exception machinery. They have no independent source body, split UID, emitter, or formal CPP/H payload.

## Dated IDA Evidence

- Read-only MCP snapshot `supervisor-uid0002OW-gate2b-20260813T0115Z`, attested on 2026-08-13, rendered the target as `sub_59ED60`, `char *__thiscall(ListPane *this)`, 72 instructions and ten blocks, with no ordinary callers and one data xref from `0x0062eb58`. This is dated evidence, not permanent current IDA authority.
- The dated frame was `Block@0x8/0x4/void *`, `var_C@0xc/0x4/_DWORD`, `var_4@0x14/0x4/_DWORD`, `__saved_registers@0x18/0x4/_DWORD`, `__return_address@0x1c/0x4/_UNKNOWN *`, and unaffected `arg_4@0x24/0x4/_DWORD`; all four comment channels were absent.
- The decompiler's `ListPane *` receiver and `char *` result are analysis artifacts. The sole derived-class vtable route and no-argument ignored-result dispatch establish `UserListPane *this` and source `void`.

## Ownership And Source Placement

- The sole `UserListPane` vtable route, use of `m_selectedIndex`, packed `UserListRecord`, adjacent `UserListPane` methods, and social-dialog dependencies preserve UID0000FO as canonical owner and UID0000OZ as source emitter.
- Constructor methods remain owned by `SayInputPanes.cpp`; inherited selection/access and dispatch remain ListPane dependencies. Neither callee/base relationship transfers ownership.
- No new source file, split, merge, or target-specific header is justified. This method emits in `NexusTK/social/UserListDialogPane.cpp` between `OnItemSelected` and the distinct secondary-view `OnDoubleClick(const MouseEvent&)` method.

## C++ Disposition

The formal `RECONSTRUCTION_CPP CODE` block contains the exact accepted implementation and routes through [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md). It intentionally uses a typed accessor, ordinary member call, bool branch, and direct `new` expressions so the source resembles plausible mid-2000s C++ while preserving the observed runtime behavior exactly. The formal H channel remains blank; UID0000FO owns the class declaration.

## Historical And Rejected Assumptions

- `OpenSayToUserMessageInputPane` is retained as the historical behavior-specific label and filename. It is rejected as the current shared virtual name because another ListPane-derived class performs a different action through the same slot.
- The prior `GetSelectedRecord()` plus `record == 0` draft is historical and behaviorally incorrect for this body: no null branch exists, and the base dispatcher provides the validity precondition.
- The prior `ClosePane(g_pUserListDialogPane)` expression is historical and unsupported. The direct receiver/callee contract is `g_pUserListDialogPane->CloseDialog()` with no singleton guard.
- `OnDoubleClick` is not an alternative name because the distinct EventHandler secondary-view override already occupies `0x0059ee10`.

## Score Rationale

- Completion `94`: exact body, range, vtable role, ABI, selected-row contract, record offset, dialog lifetime, branch constructors, compiler fragments, owner/emitter, class declaration route, and complete formal CPP are resolved and incorporated.
- Confidence `95`: independent vtable/base-dispatch, body, constructor, global, class/file, and range evidence converge. Exact original lexical spelling and source basename remain inferred rather than symbol/PDB-proven, which keeps this below final-source confidence.

## Related Docs

- [UID:0001KI][0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane](by-memory/0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane.md) Parent split/index page.
- [UID:0000FO][UserListPane](by-class/UserListPane.md) Class owner.
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) `g_useEpfAssets`.
- [UID:0003EI][g_pUserListDialogPane](by-global/g_pUserListDialogPane.md) `g_pUserListDialogPane`.

## Changes

- 2026-08-13 B001 accepted UID00047Q implementation callback:
  - Raised `87/90 -> 94/95` while preserving owner UID0000FO, emitter UID0000OZ, reconstructable true, blank position, blank H, and `Nested:0`.
  - Replaced the guarded behavior-specific draft with exact `UserListPane::OnItemActivated()` source using `GetRecordAt(m_selectedIndex)`, unconditional `g_pUserListDialogPane->CloseDialog()`, and the modern/legacy `g_useEpfAssets` constructor branch.
  - Added the base-vtable activation precondition, exact ABI/record/lifetime/constructor evidence, exact alignment and three compiler-only folded fragments, source placement, score rationale, and historical rejection reasons without deleting the older assumptions.
