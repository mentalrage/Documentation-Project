*** UID:0003UU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000KU | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000KU | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static bool HandleMessageShowPacket(const unsigned char *packet)
{
    char multiByteText[0x8000];
    wchar_t wideText[0x8000];

    if (packet[1] == 0) {
        if (g_pMessageShowPane != NULL) {
            delete g_pMessageShowPane;
        }

        return true;
    }

    unsigned int textLength = PacketBufferReadUInt16BE(packet + 2);
    memmove(multiByteText, packet + 4, textLength);

    if (textLength >= 0x8000) {
        __report_rangecheckfailure();
    }

    multiByteText[textLength] = '\0';

    unsigned int wideLength = (unsigned short)MultiByteToWideChar(
        0,
        0,
        multiByteText,
        textLength,
        wideText,
        0x8000);

    if (wideLength >= 0x8000) {
        __report_rangecheckfailure();
    }

    wideText[wideLength] = L'\0';

    if (g_pMessageShowPane != NULL) {
        g_pMessageShowPane->SetWrappedText(wideText);
    } else {
        new MessageShowPane(wideText);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B011 2026-06-19 source-quality recheck: exact `0x138` modeled `__stdcall` helper with only rel32 caller `UserPane::OnServerMessage` case `0x58` at `0x005a8172`, no VA/RVA/pointer route, no `LivingObjectPane` `this` usage despite caller seeding `ecx`, packet layout `opcode 0x58`, `payload[1]` show/close flag, `payload+2` big-endian text byte count, `payload+4` encoded text, `GetMemoryMan`/`memmove` copy to `0x8000`-byte scratch, `CP_ACP` `MultiByteToWideChar` into `WCHAR[0x8000]`, existing `g_pMessageShowPane->SetWrappedText`, new `MessageShowPane` allocation size `0x10104`/constructor call, delete path through vtable scalar-deleting destructor flag `1`, eight-byte trailing padding before `0x005ac1b0`, and recommended file-level owner/emitter [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md) rather than [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md) class or [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md) MessageShowPane; first-draft C++ ready as a local-player packet helper. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005ac070-0x005ac1a8 LivingObjectPane Handle Message Show Packet

## Status

- Address range: `0x005ac070-0x005ac1a8`.
- Entity kind: file-local/static local-player server-packet helper in the `LivingObjectPane.cpp` source neighborhood with `MessageShowPane` dependency.
- Current IDA name: `sub_5AC070`.
- Source-facing name direction: `HandleMessageShowPacket` / `HandleServerMessageShowPacket`; `MessageShow` is descriptive, not recovered from the packet-name table.
- Disposition: reconstructable source-authored code.
- Parent source file: [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md) / `NexusTK/map/LivingObjectPane.cpp`.
- Rebuild handling: emit as a file-local or otherwise internal helper, not as `LivingObjectPane::` and not as a `MessageShowPane::` method.

## Summary

This helper creates, updates, or closes the active message-show overlay in response to `UserPane::OnServerMessage` opcode `0x58`. B001 correctly found the live local-player packet route and `MessageShowPane` dependency, but its class-method framing is superseded by the 2026-06-19 B011 source-quality recheck: the body is a one-argument `__stdcall` helper, returns `1`, and never consumes the caller-seeded `ecx` as a `LivingObjectPane *this`.

The safest source shape is a local/static helper in the `LivingObjectPane.cpp` local-player packet neighborhood. `UserPane` owns the virtual dispatcher and exact callsite; `MessageShowPane` owns the overlay class, singleton, constructor/destructor, and `SetWrappedText`; this page owns the packet helper body.

## Dispatch Route And Call Shape

| Evidence | Detail |
| --- | --- |
| Only direct caller | PE-aware rel32 scan found one direct call to `0x005ac070`: `UserPane::OnServerMessage` case `0x58` at `0x005a8172`. |
| No table/pointer route | The same scan found no absolute VA bytes and no target-RVA bytes for `0x005ac070` in the executable scan. |
| Callsite | `0x005a816b push edi`; `0x005a816c lea ecx,[ebx-0xa0]`; `0x005a8172 call 0x005ac070`. |
| Callee argument | First packet pointer load is `0x005ac0a2: mov edi,[ebp+8]`. |
| Return convention | The helper ends with `ret 4`, so it consumes one stack argument and returns handled/success as `1`. |
| Not a thiscall method | The caller seeds adjusted `ecx`, but the callee never stores or dereferences it as `this`; all later `ecx` uses come from `g_pMessageShowPane` or utility-helper lowering. |

Neighboring true local-player methods keep and dereference the caller receiver: `0x005aa480` stores `ecx` in `esi` and writes `esi+0x1340fc`, `0x005ab860` stores `ecx` in `ebx` and reads `ebx+0x13eb0c`, `0x005abda0` stores `ecx` in `esi`, and `0x005ac280` reads fields such as `this+0x3cb0`. This function has no `LivingObjectPane` field loads or stores, so [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md) is a context class, not the direct owner/emitter.

## Function Range And Boundaries

- `0x005ac070-0x005ac1a8` is one modeled function, size `0x138` / 312 bytes.
- Entry bytes begin with the SEH/security-cookie/stack-probe setup `55 8b ec 6a ff 68 84 af 60 00 ... b8 08 80 01 00 e8 b5 b6 01 00`.
- The stack reservation is `0x18008`, matching `char multiByteText[0x8000]` at `ebp-0x8010` and `WCHAR wideText[0x8000]` at `ebp-0x18010`.
- Preceding bytes `0x005ac060-0x005ac070` are `00 cc cc cc cc cc cc cc cc cc cc cc cc cc cc cc`; treat `0x005ac061-0x005ac070` as alignment after the predecessor tail.
- Following bytes `0x005ac1a8-0x005ac1b0` are eight `0xcc` alignment bytes before the raw/projected position-sync helper at `0x005ac1b0`.
- The terminal range-check failure call starts at `0x005ac1a3` and ends exactly at the page endpoint: `e8 ad b4 01 00 call 0x005c7655`.
- No split is needed inside the target. The close, update, create, and range-check paths are one packet helper. The successor `0x005ac1b0` helper stays separate.

## Packet Layout

The upstream dispatcher case proves opcode `0x58`. The server packet name table entry at `0x00627b58` decodes as UTF-16 `#88`, so `MessageShow` and `kServerMessageShow` remain descriptive/inferred names rather than recovered table labels.

| Packet offset | Evidence | Best current role |
| --- | --- | --- |
| `payload[0]` | `UserPane::OnServerMessage` case `0x58`; table slot text `#88`. | Server opcode `0x58`; descriptive message-show packet. |
| `payload[1]` | `0x005ac0aa lea ecx,[edi+1]`, byte accessor call `0x00575470`, `test al,al`. | Show/update flag; zero closes the active overlay, nonzero creates or updates it. |
| `payload+2` | `0x005ac0d5 lea eax,[edi+2]`, call `0x00575480`, `movzx esi,ax`. | Big-endian 16-bit text byte length through `PacketBufferReadUInt16BE`. |
| `payload+4` | `0x005ac0e4 lea eax,[edi+4]`, copy length `esi` through `0x00516220`. | Encoded narrow text bytes. |

The body does not parse a second subtype after opcode `0x58`; `payload[1]` is the state/enable byte.

## Behavior

- Close path: if `payload[1] == 0`, read [UID:0000RK][g_pMessageShowPane](by-global/g_pMessageShowPane.md); if non-null, call the overlay's virtual scalar deleting destructor slot with delete flag `1`; return `true`. The ordinary/scalar destructor clears the singleton.
- Open/update path: read the big-endian text byte length from `payload+2`, copy exactly that many bytes from `payload+4` into a local `char[0x8000]`, fail fast through `__report_rangecheckfailure` if the byte count would index beyond the scratch buffer, and NUL-terminate the narrow text.
- Conversion path: call `MultiByteToWideChar` with code page `0` / `CP_ACP`, flags `0`, source length equal to the byte count, destination `WCHAR[0x8000]`, and capacity `0x8000`; range-check the returned wide length and terminate the wide string.
- Update path: if `g_pMessageShowPane` is already non-null, call [UID:0001C4][0x00522530-0x005226ea.MessageShowPaneSetWrappedText](by-memory/0x00522530-0x005226ea.MessageShowPaneSetWrappedText.md) at `0x005ac150`.
- Create path: if no overlay exists, allocate `0x10104` bytes through the operator-new wrapper at `0x004f4aa0`, then call `MessageShowPane::MessageShowPane(wideText)` at `0x00521da0`; the constructor stores `g_pMessageShowPane`.
- All non-range-failure paths return `1`.

## Helper And Global Name Resolution

| Address or raw name | Current source-facing treatment | Decision |
| --- | --- | --- |
| `0x00575470` / `sub_575470` | tiny byte accessor / `std::_Narrow_char_traits<char,int>::to_char_type` lowering | Do not introduce a project helper; source draft uses `packet[1]`. |
| `0x00575480` / `sub_575480` | `PacketBufferReadUInt16BE` | Use the existing packet-buffer helper name. |
| `0x00516220` / `sub_516220` | `MemmoveWrapper` over `_memmove` | Source draft can call `memmove`. |
| `0x00516030` / `GetMemoryMan` | returns `g_pMemoryMan`; called before copy-helper lowering | Utility/lowering detail, not source-important for this helper. |
| `0x00522530` / `sub_522530` | `MessageShowPane::SetWrappedText` | Use the established method name. |
| `0x004f4aa0` | operator new wrapper | Source draft uses `new MessageShowPane(wideText)`. |
| `0x005c7655` | `__report_rangecheckfailure` | Preserve observed fail-fast range-check behavior unless the future code generator emits this automatically. |
| `dword_69B4F4` | [UID:0000RK][g_pMessageShowPane](by-global/g_pMessageShowPane.md) / [UID:0001PX][0x0069b4f4-0x0069b4f8.g_pMessageShowPane](by-memory/0x0069b4f4-0x0069b4f8.g_pMessageShowPane.md) | Active floating message overlay singleton. |

## Ownership And Source Placement

Accepted owner/emitter: [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md), as a file-local/static helper in `NexusTK/map/LivingObjectPane.cpp`.

Rejected alternatives:

- [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md) as a direct class method is rejected. The old route came from address neighborhood and caller-seeded `ecx`, but the callee is `ret 4`, reads only `[ebp+8]` as the packet argument, and touches no `LivingObjectPane` fields.
- [UID:0000P1][UserPane](by-file/UserPane.md) / [UID:0000FQ][UserPane](by-class/UserPane.md) is plausible from the exact dispatcher call but rejected as canonical owner. The dispatcher owns case `0x58`; the helper body lives in the local-player packet-helper tail and has no `UserPane` field access.
- [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md) / [UID:000087][MessageShowPane](by-class/MessageShowPane.md) is the feature dependency owner, not this helper's canonical owner. The helper manipulates the singleton and calls overlay methods, but it is packet-facing, takes a packet pointer, and is outside the `MessageShowPane` method cluster.
- No-code/compiler-glue is rejected. The helper has one live caller, packet parsing, conversion, UI lifecycle actions, allocation, update/delete branches, and nontrivial project control flow.

## First-Draft C++ Notes

The formal C++ above is intentionally source-shaped and local to this exact function. It should not be reconstructed as a non-static `LivingObjectPane::` method or as `MessageShowPane::` code. The helper declaration style is still inferred: likely file-local `static bool HandleMessageShowPacket(const unsigned char *packet)` or similar.

Implementation notes:

- `delete g_pMessageShowPane` represents the observed virtual scalar deleting destructor call with flag `1`; destructor/scalar wrapper documentation owns the singleton clear.
- `new MessageShowPane(wideText)` represents `operator new(0x10104)` followed by constructor `0x00521da0`.
- Opcode `0x58` is not tested inside this helper because `UserPane::OnServerMessage` already dispatched only that case.
- Explicit `__report_rangecheckfailure` calls preserve the observed compiler/runtime fail-fast range-check paths. A later source-generation policy may lower these from compiler array-bound checks, but the behavior should not be silently changed to an ordinary return.

## Evidence

- B001 records live IDA `lookup_funcs`: start `0x005ac070`, size ending at `0x005ac1a8`.
- B011 Rule 26 recheck used local PE/Capstone route scans, packet table inspection, the target doc, [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md), [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md), LivingObjectPane/UserPane/MessageShowPane owner candidates, `g_pMessageShowPane`, `MessageShowPane::SetWrappedText`, packet-buffer helpers, and the server packet name table.
- PE-aware rel32 scan found the single `0x005a8172` call and no VA/RVA pointer route.
- Callee evidence includes `PacketBufferReadUInt16BE`, `memmove`, `MultiByteToWideChar`, `MessageShowPane::SetWrappedText`, `MessageShowPane::MessageShowPane`, virtual scalar-delete close path, and `__report_rangecheckfailure`.

## Open Questions

- Original translation-unit spelling remains inferred. [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md) is preferred over UserPane by address/source-family locality and over MessageShowPane because this is a server-packet helper outside the overlay class cluster.
- Original packet symbolic name remains unrecovered. The table says `#88`; use `MessageShow` only as a descriptive behavior name.
- Original helper linkage remains inferred: file-local `static` free function, anonymous namespace helper, or equivalent internal helper are all possible. It should not be a non-static member method.

## Cross-References

- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)
- [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md)
- [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md)
- [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md)
- [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md)
- [UID:000087][MessageShowPane](by-class/MessageShowPane.md)
- [UID:0000RK][g_pMessageShowPane](by-global/g_pMessageShowPane.md)
- [UID:0001PX][0x0069b4f4-0x0069b4f8.g_pMessageShowPane](by-memory/0x0069b4f4-0x0069b4f8.g_pMessageShowPane.md)
- [UID:0001C4][0x00522530-0x005226ea.MessageShowPaneSetWrappedText](by-memory/0x00522530-0x005226ea.MessageShowPaneSetWrappedText.md)
- [UID:00026H][0x00625008-0x0062ce50.ServerPacketNameTable](by-memory/0x00625008-0x0062ce50.ServerPacketNameTable.md)

## Changes

- 2026-06-21 Rule 26 incorporation from B011 `0003UU-LivingObjectPaneHandleMessageShowPacket-source-quality.md`:
  - Raised target metadata from `85/86`, class owner [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md), emitter [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md), and blank C++ to `88/90`, file owner/emitter [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md), and first-draft local/static helper C++.
  - Replaced the stale class-method framing with the supported file-local helper route: `ret 4`, one packet pointer argument, no caller-seeded `ecx` use, no `LivingObjectPane` field accesses, one direct `UserPane::OnServerMessage` opcode `0x58` call at `0x005a8172`, and no VA/RVA pointer route.
  - Added exact range, padding, stack-buffer, packet-layout, helper/global-name, MessageShowPane lifecycle, ownership-ranking, and remaining-open-question evidence.
