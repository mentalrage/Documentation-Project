*** UID:00012U | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003T | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003T | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void DialogPane::SlideOpenVertical()
{
    if (!CanRunSlideAnimation())
        return;

    const int height = m_bounds.bottom - m_bounds.top;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int heightOffset = height * 4;
    for (int step = 0; step < 5; ++step)
    {
        RectBounds frameBounds = originalBounds;
        const int dy = heightOffset / -5;
        OffsetRect(&frameBounds, 0, dy);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(0, -dy);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(0, dy);
        Sleep(0x18);
        heightOffset -= height;
    }
}

void DialogPane::SlideCloseVertical()
{
    if (!CanRunSlideAnimation())
        return;

    const int height = m_bounds.bottom - m_bounds.top;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int heightOffset = height;
    for (int step = 0; step < 5; ++step)
    {
        RectBounds frameBounds = originalBounds;
        const int dy = heightOffset / -5;
        OffsetRect(&frameBounds, 0, dy);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(0, -dy);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(0, dy);
        Sleep(0x18);
        heightOffset += height;
    }

    RectBounds frameBounds = originalBounds;
    SetBounds(&frameBounds, 0);
}

void DialogPane::SlideOpenHorizontal(unsigned char direction)
{
    if (!CanRunSlideAnimation())
        return;

    const int width = m_bounds.right - m_bounds.left;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int widthOffset = width * 4;
    for (int step = 0; step < 5; ++step)
    {
        int dx = widthOffset / 5;
        if (direction)
            dx = widthOffset / -5;

        RectBounds frameBounds = originalBounds;
        OffsetRect(&frameBounds, dx, 0);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(-dx, 0);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(dx, 0);
        widthOffset -= width;
    }
}

void DialogPane::SlideCloseHorizontal(unsigned char direction)
{
    if (!CanRunSlideAnimation())
        return;

    const int width = m_bounds.right - m_bounds.left;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int widthOffset = width;
    for (int step = 0; step < 5; ++step)
    {
        int dx = widthOffset / 5;
        if (direction)
            dx = widthOffset / -5;

        RectBounds frameBounds = originalBounds;
        OffsetRect(&frameBounds, dx, 0);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(-dx, 0);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(dx, 0);
        widthOffset += width;
    }

    RectBounds frameBounds = originalBounds;
    SetBounds(&frameBounds, 0);
}

void DialogPane::SlideShrink(unsigned char direction)
{
    if (!CanRunSlideAnimation())
        return;

    const int width = m_bounds.right - m_bounds.left;
    const int height = m_bounds.bottom - m_bounds.top;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int widthOffset = width * 4;
    int heightOffset = height * 4;
    for (int step = 0; step < 5; ++step)
    {
        int dx = widthOffset / 5;
        if (direction)
            dx = widthOffset / -5;
        const int dy = heightOffset / -5;

        RectBounds frameBounds = originalBounds;
        OffsetRect(&frameBounds, dx, dy);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(-dx, -dy);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(dx, dy);
        widthOffset -= width;
        heightOffset -= height;
    }
}

void DialogPane::SlideExpand(unsigned char direction)
{
    if (!CanRunSlideAnimation())
        return;

    const int width = m_bounds.right - m_bounds.left;
    const int height = m_bounds.bottom - m_bounds.top;

    RectBounds originalBounds;
    GetScreenBounds(&originalBounds);

    int widthOffset = width;
    int heightOffset = height;
    for (int step = 0; step < 5; ++step)
    {
        int dx = widthOffset / 5;
        if (direction)
            dx = widthOffset / -5;
        const int dy = heightOffset / -5;

        RectBounds frameBounds = originalBounds;
        OffsetRect(&frameBounds, dx, dy);
        SetBounds(&frameBounds, 0);
        OffsetPresentationRegion(-dx, -dy);
        g_pScreenPane->RenderPresentation();
        OffsetPresentationRegion(dx, dy);
        widthOffset += width;
        heightOffset += height;
    }

    RectBounds frameBounds = originalBounds;
    SetBounds(&frameBounds, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Shared DialogPane slide/open/close/shrink/expand animation cluster parented to [UID:00003T][DialogPane](by-class/DialogPane.md); current MCP repairs the half-open end to 0x0049f081, confirms four modeled helpers plus two raw source-shaped helpers, preserves raw no-xref caveats, rejects Ranking/Clan owner pollution, and now emits six first-draft method bodies through DialogPane. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049eac0-0x0049f081 DialogPane Slide Animation

## Status

- Disposition: reconstructable NexusTK project code; source-authored `DialogPane` slide animation helper family.
- Entity kind: shared dialog animation helper cluster.
- Correct half-open range: `0x0049eac0-0x0049f081`.
- Direct owner: [UID:00003T][DialogPane](by-class/DialogPane.md); source module route is [UID:0000IT][DialogPane](by-file/DialogPane.md).
- Current generated pollution: older generated data labels parts of this cluster under `RankingDialog` or clan owners. Those names are caller-provenance pollution, not source ownership.
- Confidence: strong for shared ownership, range repair, caller/callee evidence, raw helper boundaries, and direct class parent; medium-high for final original spellings of `CanRunSlideAnimation`, `OffsetPresentationRegion`, and `RenderPresentation`.
- Final C++: populated with six first-draft `DialogPane` method bodies. The body insertion is limited to this exact range and uses source-facing helper names accepted by the B008 report.
- Rebuild handling: `source-authored` class helper code. Compiler-generated security-cookie prologues/epilogues around the two raw helpers should be regenerated by the toolchain.

## Covered Ranges

| Range | Working role | Notes |
| --- | --- | --- |
| `0x0049eac0-0x0049eb8d` | `SlideOpenVertical` | IDA function `sub_49EAC0`, size `0xcd` / 205 bytes (Verified with int_convert.py); shifts the dialog clipping rectangle upward over five frames, refreshes the screen pane, and sleeps `0x18` / 24 ms per frame (Verified with int_convert.py). |
| `0x0049eb90-0x0049ec72` | `SlideCloseVertical` | IDA function `sub_49EB90`, size `0xe2` / 226 bytes (Verified with int_convert.py); performs the reverse five-frame vertical slide and restores the original rect through virtual slot `+0x2c`. |
| `0x0049ec80-0x0049ed58` | `SlideOpenHorizontal` | IDA function `sub_49EC80`, size `0xd8` / 216 bytes (Verified with int_convert.py); slides horizontally according to a direction byte and does not perform the final rect restore. |
| `0x0049ed60-0x0049ee42` | `SlideCloseHorizontal` | IDA function `sub_49ED60`, size `0xe2` / 226 bytes (Verified with int_convert.py); horizontal close path with final rect restore through virtual slot `+0x2c`. |
| `0x0049ee50-0x0049ef5c` | retained raw `SlideShrink` body | IDA reports `Not a function`, but disassembly shows a complete security-cookie-protected body with five iterations, width/height deltas, a direction byte at `[ebp+8]`, and `retn 4` at `0x0049ef59`; `0x0049ef5c-0x0049ef60` is alignment. No direct callers/xrefs were found in the current IDB, so the liveness caveat remains. |
| `0x0049ef60-0x0049f081` | retained raw `SlideExpand` body | IDA reports `Not a function`, but disassembly shows a complete security-cookie-protected body with five iterations, width/height deltas, final rect restore, and `retn 4` at `0x0049f07e`; `0x0049f081-0x0049f090` is alignment before `sub_49F090`. No direct callers/xrefs were found in the current IDB, so the liveness caveat remains. |

The previous filename/title end `0x0049f07e` was stale final-instruction style. Current MCP `get_bytes` at `0x0049f07e` returns `c2 04 00` followed by `0xcc` padding, so `0x0049f07e` is the start of the `retn 4` instruction and `0x0049f081` is the correct exclusive end.

## Behavior Summary

All six helpers implement dialog reveal/hide animation around the inherited `DialogPane`/`Pane` rectangle fields. Each helper:

- gates the animation through `sub_544710`, represented in first-draft C++ as `CanRunSlideAnimation()`;
- reads inherited Pane local bounds from offsets `+0x44/+0x48/+0x4c/+0x50`;
- reads the current screen bounds through primary virtual slot `+0x28`, represented as `GetScreenBounds(&bounds)`;
- builds an adjusted temporary `RectBounds` with the shared offset helper at `0x004b7e10`, represented as `OffsetRect`;
- applies temporary bounds through primary virtual slot `+0x2c`, represented as `SetBounds(&bounds, 0)`;
- offsets the drawn/presentation region through `sub_5549D0(this+4, dx, dy)`, represented as `OffsetPresentationRegion`;
- refreshes the root screen pane through `sub_557140(dword_67A7CC)`, where `dword_67A7CC` is [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md);
- offsets the presentation region back after the refresh.

Differences by method:

| Method | Loop accumulator | Direction handling | Sleep | Final restore |
| --- | --- | --- | --- | --- |
| `SlideOpenVertical` | height starts at `4 * height`, decrements by `height` for 5 frames | fixed Y: `heightOffset / -5` | yes, `0x18` / 24 ms (Verified with int_convert.py) | no explicit restore; final frame is original |
| `SlideCloseVertical` | height starts at `height`, increments by `height` for 5 frames | fixed Y: `heightOffset / -5` | yes, `0x18` / 24 ms (Verified with int_convert.py) | yes |
| `SlideOpenHorizontal` | width starts at `4 * width`, decrements by `width` for 5 frames | `direction == 0` positive X, nonzero negative X | no | no explicit restore; final frame is original |
| `SlideCloseHorizontal` | width starts at `width`, increments by `width` for 5 frames | `direction == 0` positive X, nonzero negative X | no | yes |
| `SlideShrink` | width and height start at `4 * span`, decrement for 5 frames | X follows horizontal direction; Y uses negative vertical direction | no | no explicit restore; final frame is original |
| `SlideExpand` | width and height start at `span`, increment for 5 frames | X follows horizontal direction; Y uses negative vertical direction | no | yes |

## Current MCP Boundary Check

2026-06-28 B008 implementation resumed against live MCP database session `supervisor_20260628_resume`:

- `idb_list` reported active worker session `supervisor_20260628_resume` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
- `server_health` returned `status:ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` confirmed modeled helpers at `0x0049eac0` size `0xcd`, `0x0049eb90` size `0xe2`, `0x0049ec80` size `0xd8`, and `0x0049ed60` size `0xe2`.
- `lookup_funcs` reported `0x0049ee50`, `0x0049ef60`, `0x0049f07e`, `0x0049f080`, and `0x0049f081` as not functions; `0x0049f090` is the next modeled function, `sub_49F090`, size `0x132`.
- `get_bytes 0x0049f07e size 16` returned `c2 04 00` followed by `0xcc` padding, proving the old end excluded the return instruction's immediate bytes.
- `get_bytes 0x0049ee3e size 32` returned `5d c2 04 00` followed by padding and the `SlideShrink` prologue at `0x0049ee50`.
- `xrefs_to` found zero refs to raw starts `0x0049ee50` and `0x0049ef60`, one local code-flow ref to the `0x0049f07e` return instruction from `0x0049f07d`, and zero refs to exclusive end `0x0049f081`.

## Callers And Callees

2026-06-13 live IDA MCP session `a001_goal2_0002UM`:

| Helper | Code xrefs | Unique caller functions | Representative callers |
| --- | ---: | ---: | --- |
| `0x0049eac0` | `48` | `45` | `0x00458610`, `0x0045dba0`, `0x0046aa40`, `0x0046d050`, `0x0048c640`, `0x004a4770`, `0x004fb630`, `0x0058c350`, `0x0059bc90` |
| `0x0049eb90` | `74` | `61` | `0x00458a10`, `0x0045dda0`, `0x0048cfb0`, `0x004a10e0`, `0x004a3240`, `0x004a4a30`, `0x004fc010`, `0x0058cb60`, `0x0059e3d0` |
| `0x0049ec80` | `4` | `4` | `0x00488b40`, `0x00489600`, `0x0048a1c0`, `0x0048a810` |
| `0x0049ed60` | `6` | `5` | `0x00488eb0`, `0x00489df0`, `0x0048a6b0`, `0x0048ad10`, `0x0048b630` |
| `0x0049ee50` | `0` | `0` | Raw body; no direct xrefs in current IDB. |
| `0x0049ef60` | `0` | `0` | Raw body; no direct xrefs in current IDB. |

2026-06-25 B005 support sync for [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md):

- Current MCP database `80de0a67` reconfirmed the two modeled horizontal helpers inside this canonical cluster: `0x0049ec80` is size `0xd8` / `216`, and `0x0049ed60` is size `0xe2` / `226`.
- The current horizontal caller set is four open callers and six close xrefs in five close/action functions. The open helper is called from `ClanNameInputDialog`, `ClanAttrInputDialog`, `ClanDepItemDialog`, and `ClanWidItemDialog` constructor paths. The close helper is called from `ClanNameInputDialog::OnButtonClick`, `ClanAttrInputDialog::OnDialogAction`, `ClanDepItemDialog::OnButtonClick`, `ClanWidItemDialog::OnDialogAction`, and two sites in `ClanBankPane::OnDialogAction`.
- The old [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) generated `0x0049ee41` path is stale final-byte-style state; live lookup resolves `0x0049ee41` inside `sub_49ED60`, and `0x0049ee42` is the correct half-open exclusive end after `sub_49ED60`'s `retn 4`.
- [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) remains a non-emitting caller alias for clan provenance only. Source ownership stays on this canonical `DialogPane` slide cluster, not on the clan alias.

The vertical helpers' broad caller fanout across unrelated feature-dialog neighborhoods is the strongest direct evidence that the cluster belongs to shared [UID:00003T][DialogPane](by-class/DialogPane.md) infrastructure, not to `RankingDialog`, clan dialogs, employee dialogs, or any other feature dialog.

Known callees are `sub_544710`, `0x004b7e10` (`RectBounds` offset), `sub_5549D0`, `sub_557140`, `Sleep` for the two vertical helpers, and `@__security_check_cookie@4`. The `dword_67A7CC` load in the raw helpers is the canonical [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md) storage. `sub_557140` sits in the reviewed [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) presentation neighborhood.

## Support Helper Names

These names are accepted first-draft source-facing support names for this target:

| Address / mechanism | Body evidence | Source-facing use in first draft |
| --- | --- | --- |
| `0x00544710` / `sub_544710` | Decompiles as `this[0xb4] && sub_5546F0(this+4) == 0`; this gates visible/clear presentation state before animation. | `CanRunSlideAnimation()` |
| `0x004b7e10` | Cdecl/free helper adds dx to left/right and dy to top/bottom of a four-int rectangle. | `OffsetRect(&bounds, dx, dy)` |
| primary slot `+0x28` / `0x00544b80` | Pane support docs identify this as converting local bounds through event-handler/screen position into caller output. | `GetScreenBounds(&bounds)` |
| primary slot `+0x2c` / `0x00544bd0` | Pane support docs identify this as storing origin and updating local bounds/sizing state; binary passes second argument zero. | `SetBounds(&bounds, 0)` |
| `0x005549d0` / `sub_5549D0` | Offsets the active region/motion/presentation state if it is not empty. | `OffsetPresentationRegion(dx, dy)` |
| `0x00557140` / `sub_557140` | Surface presentation render/refresh path reached through the root screen pane global. | `g_pScreenPane->RenderPresentation()` |

The exact original source spellings for `CanRunSlideAnimation`, `OffsetPresentationRegion`, and `RenderPresentation` are not final-audit proven. That caps confidence only; it no longer blocks formal C++ for this exact target.

## Boundary And Raw-Body Evidence

- `lookup_funcs` on 2026-06-13 confirmed modeled starts/sizes for `0x0049eac0` (`0xcd` / 205), `0x0049eb90` (`0xe2` / 226), `0x0049ec80` (`0xd8` / 216), and `0x0049ed60` (`0xe2` / 226), with each decimal conversion verified by `tools/int_convert.py`.
- The same lookup reports `0x0049ee50`, `0x0049ef60`, `0x0049f07e`, and `0x0049f080` as `Not a function`, and `0x0049f090` as the next modeled function.
- Raw disassembly at `0x0049ee50` starts with `push ebp; mov ebp, esp; sub esp, 0x3c`, loads `___security_cookie`, loads [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md) / `dword_67A7CC`, calls `sub_544710`, runs a five-iteration loop, calls `0x004b7e10`, virtual slot `+0x2c`, `sub_5549D0`, and `sub_557140`, then ends with `@__security_check_cookie@4`, `pop ebp`, and `retn 4`.
- Raw disassembly at `0x0049ef60` has the same security-cookie shape, the same root-screen/presentation callees, and a final rect-restore call through virtual slot `+0x2c` before `retn 4` at `0x0049f07e`.
- Current B008 MCP `get_bytes 0x0049f07e size 16` returned `c2 04 00` followed by `0xcc` padding, proving the correct exclusive end is `0x0049f081`.
- `make_signature_for_range 0x0049ee50-0x0049ef5c`, `0x0049ef60-0x0049f081`, and `0x0049eac0-0x0049f081` returned unique signatures during the B008 report, confirming the two raw bodies are stable code islands even though IDA does not model them as functions.

## Ownership Decision

Keep this cluster with [UID:00003T][DialogPane](by-class/DialogPane.md), not `RankingDialog`, `ClanBank`, or any other feature dialog. Feature constructors and button handlers call these helpers to animate a shared dialog base rectangle over root-screen redraws. The class then routes through [UID:0000IT][DialogPane](by-file/DialogPane.md) for source-module placement.

Rejected alternatives:

| Alternative | Reason rejected |
| --- | --- |
| Ranking ownership | Ranking/generated names are caller-site artifacts over broad DialogPane helpers. They do not own the shared method bodies. |
| Clan ownership | [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) is a historical alias for only the two horizontal helpers; it is non-reconstructable and points back to this canonical target. |
| Feature-dialog ownership | Vertical helpers have 48 and 74 current code xrefs across broad dialog call sites. Horizontal helpers have fewer current callers but still operate only on inherited DialogPane/Pane state and are declared on `DialogPane`. |
| File parent as direct owner | [UID:0000IT][DialogPane](by-file/DialogPane.md) remains the source file route, but exact method bodies belong to the `DialogPane` class owner [UID:00003T][DialogPane](by-class/DialogPane.md). |
| Optional one-method child split | Not needed for this callback. The accepted disposition keeps one repaired six-method target. |

## Cross-References

- [UID:0000IT][DialogPane](by-file/DialogPane.md)
- [UID:00003T][DialogPane](by-class/DialogPane.md)
- [UID:00012R][0x0049d8a0-0x0049feae.DialogPane](by-memory/0x0049d8a0-0x0049feae.DialogPane.md)
- [UID:0001U4][DialogPaneLayout](by-type/by-struct/DialogPaneLayout.md)
- [UID:0000N2][RectBounds](by-file/RectBounds.md)
- [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md)
- [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md)
- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md)

## Score Rationale

- Completion raised from `86` to `90` because the target now has the corrected half-open range, exact six-helper map, current MCP boundary proof, raw helper liveness caveats, helper-name mapping, owner route, rejected alternatives, and formal first-draft C++ for the six method bodies.
- Confidence raised from `90` to `91` because current MCP session `supervisor_20260628_resume` reconfirmed the function map, raw no-function boundaries, `0x0049f07e` return bytes, `0x0049f081` exclusive end, zero xrefs to raw starts, and next modeled function at `0x0049f090`. Confidence remains below final-audit quality because the exact original spellings for `CanRunSlideAnimation`, `OffsetPresentationRegion`, and `RenderPresentation` remain inferred support-name decisions, and raw `SlideShrink`/`SlideExpand` have no incoming direct xref route.

## Changes

- 2026-06-13 Agent-A002 Goal 2:
  - Changed `COMPLETION:76` / `CONFIDENCE:84` to `86/90`.
  - Changed `AUTOGEN_PARENT_UID` from file parent [UID:0000IT][DialogPane](by-file/DialogPane.md) to direct class parent [UID:00003T][DialogPane](by-class/DialogPane.md).
  - Summary/evidence: live IDA MCP session `a001_goal2_0002UM` reconfirmed four modeled helpers, two raw but unique security-cookie-protected bodies, caller totals (`48`, `74`, `4`, and `6` code xrefs), no xrefs to the raw starts, root-screen/presentation and rectangle-helper dependencies, and the `0x0049f07e` `retn 4` boundary before padding and next modeled function `0x0049f090`.
- 2026-06-19 B011 source-quality sync:
  - Kept score `86/90`, owner/emitter [UID:00003T][DialogPane](by-class/DialogPane.md), and blank formal C++.
  - Summary/evidence: B011 resolved the source-facing slide-family names used by the [UID:00003T][DialogPane](by-class/DialogPane.md) declaration, retained direct DialogPane ownership, preserved no-direct-xref caveats for raw `SlideShrink`/`SlideExpand`, and rejected Ranking/Clan labels as generated/caller-provenance pollution.
- 2026-06-26 B005 support sync for accepted [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) report:
  - Kept score `86/90`, owner/emitter [UID:00003T][DialogPane](by-class/DialogPane.md), `RECONSTRUCTABLE:TRUE`, and blank formal C++ unchanged.
  - Summary/evidence: current MCP database `80de0a67` reconfirmed the horizontal helper sizes `0x0049ec80` `0xd8` / `216` and `0x0049ed60` `0xe2` / `226`; the caller set is four horizontal-open constructor callers and six horizontal-close xrefs in five close/action functions across `ClanNameInputDialog`, `ClanAttrInputDialog`, `ClanDepItemDialog`, `ClanWidItemDialog`, and `ClanBankPane`; the old [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) generated `0x0049ee41` path is stale, with `0x0049ee42` the correct half-open exclusive end; [UID:00012V][0x0049ec80-0x0049ee42.ClanDialogSlideAnimation](by-memory/0x0049ec80-0x0049ee42.ClanDialogSlideAnimation.md) remains a non-emitting caller alias while this page remains the canonical source cluster.
- 2026-06-28 B008 implementation callback for accepted report `00012U-DialogPaneSlideAnimation-source-quality.md`:
  - Renamed/repaired the target from `0x0049eac0-0x0049f07e` to `0x0049eac0-0x0049f081`.
  - Changed score from `86/90` to `90/91`, kept owner/emitter [UID:00003T][DialogPane](by-class/DialogPane.md), and populated the formal six-method C++ block.
  - Summary/evidence: MCP session `supervisor_20260628_resume` returned healthy `NexusTK.exe.i64` state; `lookup_funcs` confirmed the four modeled helpers, raw starts `0x0049ee50`/`0x0049ef60` as not functions, stale endpoint `0x0049f07e` and corrected endpoint `0x0049f081` as not functions, and successor `0x0049f090` as `sub_49F090`; `get_bytes` proved `0x0049f07e` begins `c2 04 00` followed by padding; `xrefs_to` found zero refs to raw starts and zero refs to `0x0049f081`. The C++ uses accepted first-draft support names and keeps the raw no-xref liveness caveat.
