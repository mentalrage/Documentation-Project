*** UID:000201 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool EPFTileContext::ContainsIndexedPixelInRanges(const DLPalette *palette) const
{
    if (pixelFormat == kEPFTilePixelFormatRgb16) {
        return false;
    }

    const int rowCount = bounds.bottom - bounds.top;
    const int pixelsPerRow = rowStridePixels;
    const unsigned char *rowPixels = static_cast<const unsigned char *>(pixelData);
    const DLPaletteRange *ranges = palette->m_paletteRanges;
    const int rangeCount = palette->m_paletteRangeCount;

    for (int row = 0; row < rowCount; ++row) {
        for (int column = 0; column < pixelsPerRow; ++column) {
            const unsigned char pixel = rowPixels[column];

            for (int rangeIndex = 0; rangeIndex < rangeCount; ++rangeIndex) {
                const unsigned char rangeStart = ranges[rangeIndex].firstIndex;
                const unsigned char rangeEnd = ranges[rangeIndex].lastIndex;

                if (rangeStart <= pixel && pixel <= rangeEnd) {
                    return true;
                }
            }
        }

        rowPixels += pixelsPerRow;
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current MCP confirms exact bounds, 0xcc padding, no callees, two raw ItemObjImageLib call sites, PaletteLib::GetSlotPalette returning DLPalette*, inclusive scanning of typed DLPaletteRange records through the documented EPFTileContext friendship, unique range signature, no pointer hits, and EPFTileContext owner/emitter assignment; first-draft C++ emits ContainsIndexedPixelInRanges. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00458590-0x00458610 EPFTileContextPixelRangeTest

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class/helper method.
- Current IDA name: `sub_458590`; best descriptive source name `ContainsIndexedPixelInRanges`.
- Proposed owner: [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) / [UID:0000J4][EPFTileContext](by-file/EPFTileContext.md).
- Rebuild handling: `source-authored`.
- Confidence: very strong for range, behavior, parent ownership, raw xref/call-site evidence, and the resolved caller-side `DLPalette *` parameter; medium-high for exact original method/member spellings.
- Evidence basis: live/current IDA MCP lookup, decompile, disassembly, xref, caller/callee, byte, pointer-search, signature, PaletteLib, and generated-output checks on 2026-06-05, 2026-06-16, and 2026-07-09.

## 2026-08-17 Aggregate Source-Order And Header Closure

This accepted child body remains byte-for-byte unchanged and emits through [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) at deterministic position `20`, after the eleven-definition [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md) at position `0` and [UID:000200][0x00458560-0x00458585.EPFTileContextNormalizePostDecodePixels](by-memory/0x00458560-0x00458585.EPFTileContextNormalizePostDecodePixels.md) at position `10`. Its formal H channel remains blank because the complete private declaration is owned by the class header. `Palette.h` supplies the complete `DLPalette`/`DLPaletteRange` dependency and the accepted friendship route; no duplicate type or accessor is introduced here.

## Behavior

This helper scans the decoded indexed/byte pixel buffer in an `EPFTileContext` and returns whether any pixel value falls inside any caller-supplied inclusive byte range.

Live IDA decompilation and disassembly show:

- return `false` when `pixelFormat` at `+0x00` is `kEPFTilePixelFormatRgb16` / zero, because this query only scans indexed byte pixels;
- compute row count as `bottom - top` from `+0x1c - +0x14`;
- use `rowStridePixels` from `+0x0c` as the number of indexed bytes to inspect per row;
- read the pixel buffer from `+0x04`;
- read `DLPalette::m_paletteRanges` at palette `+0x04`;
- read `DLPalette::m_paletteRangeCount` at palette `+0x08`;
- compare each pixel byte against inclusive `[firstIndex, lastIndex]` endpoints in each two-byte `DLPaletteRange`;
- return `true` through the `AL = 1` path as soon as any pixel matches; otherwise return `false` after all rows/pixels/ranges are exhausted.

At source level this is reconstructable as `EPFTileContext::ContainsIndexedPixelInRanges(const DLPalette *palette) const`, returning true when any decoded indexed pixel falls inside any inclusive typed range stored by the caller-provided `DLPalette`. Current caller disassembly resolves the argument to the `DLPalette *` returned by `PaletteLib::GetSlotPalette(10, itemInfo.paletteSlot, 0)`, and [UID:00003Z][DLPalette](by-class/DLPalette.md) documents `DLPaletteRange *m_paletteRanges` and `int m_paletteRangeCount` at the offsets read here. `Palette.h` grants `friend struct EPFTileContext`, the narrow source-level access mechanism required by this direct read; storage ownership remains with DLPalette.

B004's EPFTileContext class source-quality pass resolves the receiver-side field vocabulary for this helper: the first dword is `pixelFormat`, with value `1` meaning indexed8/byte pixels and value `0` meaning 16-bit word/RGB16 pixels. That closes the old `pixelMode` ambiguity for this page. The caller-side type blocker is now also resolved to `DLPalette`; only the exact original method/member spellings remain inferred.

## Caller Evidence

Live IDA MCP `callers` on 2026-06-05 reports no containing function callers because the two direct uses are in raw code not wrapped as function objects. Live `xrefs_to 0x00458590` reports code xrefs at `0x004df70c` and `0x004df7aa`, and `lookup_funcs` reports both call-site addresses are not functions while the following modeled [UID:0002IU][0x004df7e0-0x004dfaa6.LightObjImageLibConstructor](by-memory/0x004df7e0-0x004dfaa6.LightObjImageLibConstructor.md) body starts at `0x004df7e0`.

Raw call-site disassembly on 2026-06-05, 2026-06-16, and 2026-07-09 shows both callers build the range argument through [UID:0000A1][PaletteLib](by-class/PaletteLib.md) `PaletteLib::GetSlotPalette(10, itemInfo.paletteSlot, 0)` / `sub_543E40`, pass an `EPFTileContext` stack object at `ebp-0x2c` as `this`, call `EPFTileContext::ContainsIndexedPixelInRanges` / `sub_458590`, and store the returned byte into `ItemInfo+0x10` (`[ebx+0x10]`).

The current exact raw caller page is [UID:0003ND][0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability](by-memory/0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability.md), not the following LightObj constructor cluster. That page documents both calls at `0x004df70c` and `0x004df7aa`, both storing the boolean result into `ItemInfo+0x10`, and rejects [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) ownership because the raw helper ends before `0x004df7e0` and uses ItemObj table/resource strings. Treat those callers as image-library consumer evidence only; they do not move this generic pixel-range method away from `EPFTileContext`.

## Ownership Decision

Keep this helper with `EPFTileContext` for now. It is a generic query over decoded context pixels, while the only currently known callers use it to classify generated image-library/light records.

## Typed Range And Friendship Model

- `DLPaletteRange` is a two-byte record containing `unsigned char firstIndex` and `unsigned char lastIndex`.
- The compare instructions prove inclusive lower and upper bounds, so these names represent actual cross-consumer semantics rather than only this method's local interpretation.
- [UID:0004HV][0x00542be0-0x00542c95.DLPaletteCopyConstructor](by-memory/0x00542be0-0x00542c95.DLPaletteCopyConstructor.md) deep-copies exactly `sizeof(DLPaletteRange) * m_paletteRangeCount` bytes, and [UID:0004I1][0x00542e60-0x00542f91.DLPaletteLoadFromStream](by-memory/0x00542e60-0x00542f91.DLPaletteLoadFromStream.md) allocates/reads the same two-byte records.
- Friendship is preferred over public fields or invented accessor methods because the binary directly loads palette `+0x04/+0x08` and no call to an accessor exists.
- Historical `ByteRangeSet`, `m_entryMoves`, and `m_entryMoveCount` wording remains useful for search/audit history but is superseded in emitted source.

## Autogen Status

This exact helper is attached to [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) as reconstructable project code. The parent class and file pages meet the current attachment gate, the `DLPalette *` dependency is resolved, and the formal reconstruction C++ block is now populated. The exact original helper name remains inferred rather than proven.

Dated pre-callback generated `auto-generated/NexusTK/render/EPFTileContext.cpp` state emitted [UID:000201] only as an Empty Emitter Marker because this page's formal C++ block was blank at that time. That historical documentation/output omission was not evidence against source-authored ownership. Current generated truth is established only by physical post-validator readback; the accepted source topology emits this `ContainsIndexedPixelInRanges(const DLPalette *palette) const` body through [UID:00004I][EPFTileContext](by-class/EPFTileContext.md).

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `89` | The page documents exact bounds, live behavior, no-callee status, raw xref/call-site evidence, the corrected ItemObjImageLib caller route, owner decision, `DLPalette *` parameter, inclusive range-pair semantics, parent attachment, generated-output cause, and formal first-draft C++. Completion remains below final because exact original method/member spellings are still inferred and the two caller sites remain raw non-function code. |
| Confidence `92` | Current IDA/MCP evidence directly supports the half-open range, `0x80` / 128-byte modeled function length, 60-instruction body, pixel scan behavior, two raw ItemObjImageLib xrefs, no callees, no pointer hits to the helper start, unique signature, `PaletteLib::GetSlotPalette` caller flow, `DLPalette::m_paletteRanges`/`m_paletteRangeCount` offsets and inclusive endpoint semantics, and EPFTileContext ownership. Confidence is not higher because current callers are raw image-library code outside modeled function objects and exact original source spellings are unavailable. |

## 2026-06-16 A001 Source-Quality Refresh

Live IDA MCP session `b001_000211_20260616` confirmed the modeled function at `0x00458590` with size `0x80` / 128 bytes, the prior helper ending at `0x00458585`, eleven bytes of `0xcc` alignment through `0x00458590`, and the next modeled function at `0x00458610`. `make_signature_for_range 0x00458590-0x00458610` produced a unique signature. `find_bytes` found no little-endian pointer hit for `0x00458590`, so current reachability is by the two relative call xrefs rather than by a literal function-pointer table.

The 2026-06-16 decompile/disassembly matches the byte-pair behavior later resolved to `DLPalette`: `this[0]` is a validity guard, `this[7] - this[5]` is the row count, `this[3]` is the row stride/width, `this[1]` is the decoded byte-pixel base, palette `+0x04` points at two-byte min/max pairs, and palette `+0x08` is the pair count. The compare sequence `cmp [edx+eax*2], cl` followed by `cmp cl, [edx+eax*2+1]` proves inclusive lower and upper bounds.

Caller/reachability research checked live xrefs, raw bytes around `0x004df6e0-0x004df7e0`, and the current exact caller page [UID:0003ND][0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability](by-memory/0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability.md). Both xrefs remain raw/non-function calls inside that ItemObjImageLib helper. The following [UID:0002IU][0x004df7e0-0x004dfaa6.LightObjImageLibConstructor](by-memory/0x004df7e0-0x004dfaa6.LightObjImageLibConstructor.md) is an adjacent next-function boundary, not the caller owner.

No split, merge, or ownership change is justified. The method is a generic EPF tile/context pixel-range query and directly reads only the `EPFTileContext` layout plus caller-provided `DLPalette` byte-pair fields; ItemObjImageLib owns the current call sites but not this helper. Formal C++ is now populated because the former `ByteRangeSet`/range-record blocker is superseded by current `PaletteLib::GetSlotPalette` and `DLPalette` evidence. The old EPFTileContext receiver-field naming blocker is closed by B004's class pass.

## Cross-References

- [UID:00004I][EPFTileContext](by-class/EPFTileContext.md)
- [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md)
- [UID:0003ND][0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability](by-memory/0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability.md)
- [UID:00006W][ItemObjImageLib](by-class/ItemObjImageLib.md)
- [UID:0000KH][ItemObjImageLib](by-file/ItemObjImageLib.md)
- [UID:0001UT][ItemInfo](by-type/by-struct/ItemInfo.md)
- [UID:00003Z][DLPalette](by-class/DLPalette.md)
- [UID:0000A1][PaletteLib](by-class/PaletteLib.md)
- [UID:0000RW][g_pPaletteLib](by-global/g_pPaletteLib.md)
- [UID:0002IU][0x004df7e0-0x004dfaa6.LightObjImageLibConstructor](by-memory/0x004df7e0-0x004dfaa6.LightObjImageLibConstructor.md)

## Changes

- 2026-08-04 B002 UID0004HV support synchronization:
  - Score, range, owner/emitter, reconstructability, method signature, caller routes, and exact scan behavior remain unchanged at `89/92`.
  - Updated formal CPP and current prose from raw byte indexing to typed `DLPaletteRange` endpoints, documented `friend struct EPFTileContext`, and retained old ByteRangeSet/entry-move vocabulary as historical evidence.
- 2026-07-09 B005 UID000201 implementation callback:
  - Before: scored `86/90`, emitted as an empty marker, and still treated `ByteRangeSet`/range-record naming as the formal C++ blocker.
  - Changed to: score `89/92`; owner/emitter/reconstructable metadata unchanged (`CANONICAL_OWNER:00004I`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00004I`); formal first-draft C++ now emits `EPFTileContext::ContainsIndexedPixelInRanges(const DLPalette *palette) const`.
  - Summary/evidence: current MCP session `supervisor_nexustk_20260709` confirms exact `0x00458590-0x00458610` function size `0x80`, eleven `0xcc` bytes of pre-padding, successor prolog at `0x00458610`, no callees, raw code xrefs at `0x004df70c` and `0x004df7aa`, unique range signature, no VA/RVA pointer-byte hits, and caller flow through `PaletteLib::GetSlotPalette(10, itemInfo.paletteSlot, 0)` returning a `DLPalette *`. The target consumes `DLPalette::m_entryMoves` at `+0x04` and `m_entryMoveCount` at `+0x08` as inclusive `[first, second]` indexed-pixel ranges, so the old no-code proof and ItemObj/LightObj/Palette ownership alternatives are rejected.
- 2026-06-16 A001 bounded render-helper/source-quality refresh:
  - What existed before: the page was reconstructable and attached to EPFTileContext at `82/88`, but the caller route still described the raw users as light-generation evidence and did not record pointer-search, generated-output, or final-C++ blocker details from the current pass.
  - Changed to: score `86/90`; owner/emitter fields unchanged (`CANONICAL_OWNER:00004I`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00004I`); final C++ remains blank.
  - Summary/evidence: live IDA MCP session `b001_000211_20260616` confirms the modeled `0x80` / 128-byte body, 60-instruction no-callee inclusive pixel-range scan, exact surrounding padding/functions, raw xrefs at `0x004df70c` and `0x004df7aa`, no function objects at those call sites, no little-endian pointer hit to the helper start, unique signature, current caller ownership through [UID:0003ND][0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability](by-memory/0x004df670-0x004df7d2.ItemObjImageLibRefreshItemFrameAvailability.md), generated coverage routed to EPFTileContext with code `no`, and zero-byte generated `EPFTileContext.cpp`.
- 2026-06-05 live IDA evidence refresh:
  - Before: scored `78/82`, already reconstructable and attached to [UID:00004I][EPFTileContext](by-class/EPFTileContext.md), with caller evidence based on an older xref note.
  - After: scored `82/88`; parent and C++ metadata are unchanged.
  - Summary/evidence: live IDA MCP confirms `sub_458590` as `0x00458590-0x00458610`, the previous helper end/padding at `0x00458585-0x00458590`, the next RankingDialog function at `0x00458610`, the 60-instruction no-callee byte-pixel scan body, raw xrefs at `0x004df70c` and `0x004df7aa`, no function objects at the two call sites, and both raw call sites storing the returned byte into a record field at `+0x10`. Final C++ remains blank because the helper/range-record names and caller-side owner are still provisional.
- 2026-06-02:
  - Before: scored `70/65`, reconstructability and parent blank, and included a behavior-sketch C block.
  - After: scored `78/82`, marked reconstructable, attached to [UID:00004I][EPFTileContext](by-class/EPFTileContext.md), and replaced the sketch with prose.
  - Why: existing docs already prove the range, behavior, two caller sites, and EPFTileContext ownership; final C++ remains blocked by provisional range-record and field names.
