*** UID:0002K6 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
RankingUserEntry *RankingCategoryRecord::GetUserEntryAt(int index)
{
    int userEntryCount = static_cast<int>(m_userEntries.size());

    if (userEntryCount <= index)
        return NULL;

    return &m_userEntries[index];
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact signed upper-bound lookup over the RankingCategoryRecord user-entry vector, preserving the binary's no-lower-bound behavior, nullable return, 0x4c row stride, sole ten-row paint caller, corrected endpoint, and source-ready std::vector form. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045bfe0-0x0045c01b RankingCategoryRecordGetUserEntryAt

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method.
- Parent class: [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md).
- Aggregate range: [UID:0000Y9][0x0045bf60-0x0045c257.RankingCategoryRecord](by-memory/0x0045bf60-0x0045c257.RankingCategoryRecord.md).
- Likely source module: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Exact modeled body: `[0x0045bfe0,0x0045c01b)`, `0x3b` / 59 bytes; the former `0x0045c01a` endpoint omitted the last byte of the three-byte `retn 4` under the project's half-open convention.
- Source disposition: non-const `RankingCategoryRecord::GetUserEntryAt(int)` in `NexusTK/ui/dialogs/RankingDialog.cpp`; the class declaration is owned by [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md), so this page's formal H channel remains blank.
- Confidence: very strong for body extent, vector offsets, signed comparison, 76-byte row stride, nullable result, sole caller contract, and inferred `RankingUserEntry` type; only original lexical spellings remain unrecovered.

## Behavior

The function derives a signed row count from the physical vector begin/end slots at record offsets `+0x258/+0x25c`, using the exact `0x4c` / 76-byte `RankingUserEntry` stride. Its only condition is the signed upper-bound test: when `userEntryCount <= index` it returns null; otherwise it returns the address of element `index`.

There is deliberately no `index >= 0` guard. For a negative index and an ordinary nonnegative count, the signed comparison follows the success path and the binary forms a pointer `76 * index` bytes before vector begin. The exact source reconstruction preserves that behavior rather than adding defensive validation, `at()`, an exception path, or a lower-bound check not present in the executable.

The sole external call is `0x0045f637` inside `RankingUserListPane::OnPaint`. That caller initializes its row index to zero, increments by one, stops at ten, null-checks each returned pointer, and reads `rank` at `+0x04`, `name` at `+0x08`, and `score` at `+0x48`. The release caller therefore never exercises the binary's negative-index behavior, but that does not permit changing the method contract.

## IDA MCP Evidence

- Fresh read-only MCP session `9081c1f3-bc94-49a3-a909-a60eafeb0a99` used active database `b005-persist-reopen`, worker/PID `2652`, for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned `status:ok` and bounded IDB-backed reads succeeded.
- `lookup_funcs` reports `sub_45BFE0` at `[0x0045bfe0,0x0045c01b)`, size `0x3b` / 59. The PE body begins at raw offset `0x5b3e0` and has SHA256 `775ce790509e5e305dd258b915880e77e78219e06e4679c6c9b607e988277372`.
- Exact body bytes begin `55 8b ec 56 8b b1 58 02 00 00` and end `6b c1 4c 03 c6 5e 5d c2 04 00`; the complete `retn 4` occupies `0x0045c018-0x0045c01a`.
- Decompilation reads the vector begin at `+0x258`, computes `(end - begin) / 76`, uses a signed greater-than success branch against `index`, and returns `begin + 76 * index` or zero. There is no lower-bound branch, exception path, or callee.
- `xrefs_to` reports one code xref to the target start from `0x0045f637`, zero xrefs to `0x0045c01a` and `0x0045c01b`, and no alternate entry. VA, RVA, raw-offset-pointer, data, and immediate searches found no stored route to the body.
- Fresh Gate2B prestate on 2026-08-04 checked `inspect_items` at every address `0x0045c01b` through `0x0045c01f`. All five addresses resolve to one existing physical item with head `0x0045c01b`, end `0x0045c020`, size `5`, no name, no type, `is_code:false`, and `is_data:true`. Its bytes are `cc cc cc cc cc`, all comment channels are blank, and xrefs are zero. This data item is alignment evidence only and emits no source.
- Target address/function regular and repeatable comments are absent in the current IDB. Original symbols are also absent; the source-facing class, method, argument, member, and row names are evidence-backed reconstruction names rather than recovered labels.

## Range And Padding

- Exact source-bearing body: `[0x0045bfe0,0x0045c01b)`, 59 bytes.
- Exact alignment: `[0x0045c01b,0x0045c020)` is one existing unnamed, untyped, non-code data item of size `5`, containing bytes `cc cc cc cc cc`, with blank comments and zero xrefs. It must be preserved exactly and must not emit source; no function, name, type, comment, or patch belongs on it.
- Predecessor `GetTitleText` is `[0x0045bfd0,0x0045bfd4)`, followed by twelve `0xcc` bytes before this target.
- Successor UID000428 starts at `0x0045c020` as a separate source-shaped `SetListIndex` raw body. Correcting this endpoint neither widens UID0002K6 into the postpad nor changes the successor.

## Reconstruction Notes

This is source-ready as a short, non-const accessor over `std::vector<RankingUserEntry>`. The formal CPP channel uses `static_cast<int>(m_userEntries.size())` so the comparison remains signed, tests only `userEntryCount <= index`, and returns `&m_userEntries[index]`. This removes compiler-layout member spellings while preserving the exact binary contract.

The non-const declaration is the highest-probability human source form because the method returns a mutable `RankingUserEntry *`. Keeping the former `const` declaration would require an unsupported `const_cast`; preserving `m_userEntriesBegin/m_userEntriesEnd`, using unsigned-only comparison, using `at()`, or adding `index >= 0` would make the source less plausible or behaviorally different.

## Cross-References

- [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md)
- [UID:0001VN][RankingCategoryRecordLayout](by-type/by-struct/RankingCategoryRecordLayout.md)
- [UID:0000Y9][0x0045bf60-0x0045c257.RankingCategoryRecord](by-memory/0x0045bf60-0x0045c257.RankingCategoryRecord.md)
- [UID:0000BQ][RankingUserListPane](by-class/RankingUserListPane.md)
- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)

## Changes

- 2026-08-04 B007 C27 Gate2B-prestate correction:
  - Replaced the earlier item-absence statement with the exact physical IDA model: one existing unnamed, untyped, non-code data item spans `[0x0045c01b,0x0045c020)`, size `5`, with bytes `cc cc cc cc cc`, blank comments, and zero xrefs.
  - The superseded statement was incorrect only about IDA item representation. The five bytes remain non-emitting alignment outside the UID0002K6 method body, and the method endpoint, source reconstruction, owner/emitter route, metadata, and successor boundary are unchanged.

- 2026-08-04 B007 UID0002K6 source-quality implementation callback:
  - Renamed the half-open endpoint from `0x0045c01a` to `0x0045c01b`, raised metadata from `86/91` to `94/95`, and preserved canonical owner/emitter UID0000BL with reconstructable state true.
  - Replaced the const raw-pointer-field draft with the exact non-const `std::vector<RankingUserEntry>` formal CPP body while keeping the target H payload blank for the class-owned declaration.
  - Added the 59-byte extent, raw offset/body hash, terminal return bytes, five-byte postpad, sole `0x0045f637` caller and `0..9` contract, signed upper-bound-only comparison, exact negative-index result, source route, and rejected raw-field/const/defensive alternatives.
  - Historical correction: the former endpoint, physical begin/end member source shape, and const declaration remain useful earlier reconstruction evidence but are superseded by the exact range and source-quality decision above.

- 2026-06-05: Raised to `82/90` after live IDA reconfirmed the exact bounds-check body, no callees, user-list paint caller, and 76-byte user-entry stride. Final row type naming remains open.
- 2026-06-17 B002 source-quality execution:
  - Raised from `82/90` to `86/91` and populated first-draft C++.
  - Evidence: B002 rechecked the exact vector begin/end offsets, `0x4c` row stride, nonnegative user-list paint caller, and `RankingUserEntry` support layout.
