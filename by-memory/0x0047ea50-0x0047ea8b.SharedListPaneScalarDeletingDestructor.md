*** UID:0000ZR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:78 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x0047ea50-0x0047ea8b Shared List Pane Scalar Deleting Destructor

## Status

- Confidence: strong for function boundaries, vtable users, and scalar deleting destructor behavior; medium for final source-level owner name.
- Entity kind: shared scalar deleting destructor target for `ListPane`-derived item/list panes.
- IDA function: `sub_47EA50`, size `0x3b`, end-exclusive `0x0047ea8b`.
- Known vtable users: `BoardListPane`, `ClanStringListPane`, `ClanJoinListPane`, `ClanEnlistListPane`, [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md), and [UID:00008W][MyItemListPane](by-class/MyItemListPane.md).

## Behavior

The function:

- saves `this` in `esi`;
- calls `0x004f3b60`, the list-pane destructor/cleanup path;
- checks the scalar deleting destructor flags;
- if `flags & 1` is set and `flags & 4` is clear, frees object storage through `0x004f4ac0`;
- if `flags & 4` is set, takes a compact vector-delete-style branch that passes object size `0x14c` and `this` to the current IDA target named `@_guard_check_icall_nop@4`;
- returns the original object pointer.

The `flags & 4` branch needs final compiler-runtime naming review, but it should still be treated as destructor support code rather than feature logic.

## Raw Code Evidence

IDA disassembly for `sub_47EA50` shows this exact destructor body:

```asm
0047EA50 push    ebp
0047EA51 mov     ebp, esp
0047EA53 push    esi
0047EA54 mov     esi, ecx
0047EA56 call    sub_4F3B60
0047EA5B mov     eax, [ebp+arg_0]
0047EA5E test    al, 1
0047EA60 jz      short loc_47EA84
0047EA62 test    al, 4
0047EA64 jnz     short loc_47EA76
0047EA66 push    esi
0047EA67 call    sub_4F4AC0
0047EA6C add     esp, 4
0047EA6F mov     eax, esi
0047EA71 pop     esi
0047EA72 pop     ebp
0047EA73 retn    4
0047EA76 push    14Ch
0047EA7B push    esi
0047EA7C call    @_guard_check_icall_nop@4
0047EA81 add     esp, 8
0047EA84 mov     eax, esi
0047EA86 pop     esi
0047EA87 pop     ebp
0047EA88 retn    4
```

The companion adjustor thunks are also exact IDA functions:

```asm
0047E8AD sub     ecx, 0A0h
0047E8B3 jmp     sub_47EA50

0047E8B8 sub     ecx, 0A4h
0047E8BE jmp     sub_47EA50
```

## C++ Reconstruction Candidate

This is compiler-emitted destructor glue, not a handwritten source method. A source-level equivalent of the shared target is:

```cpp
void *ListPaneDerivedScalarDeletingDestructor(ListPaneDerived *self, unsigned int flags)
{
    self->~ListPaneDerived();

    if ((flags & 1) != 0) {
        if ((flags & 4) != 0) {
            // Compiler vector-delete/helper path; exact runtime helper name still needs review.
            vector_delete_like_helper(self, 0x14c);
        } else {
            operator_delete_like(self);
        }
    }

    return self;
}
```

Final rebuild work should prefer normal C++ destructors on each owning class and let the compiler regenerate scalar deleting destructors and adjustor thunks.

## Evidence Notes

- IDA `lookup_funcs 0x0047ea50` reports a modeled function at `0x0047ea50` with size `0x3b`.
- IDA disassembly shows secondary and tertiary adjustor thunks at `0x0047e8ad` and `0x0047e8b8` tail-jumping to this function after subtracting `0xa0` or `0xa4` from `ecx`.
- IDA `xrefs_to 0x0047ea50` includes vtable data references at `0x00613ce8` (`BoardListPane`), `0x006156ac` (`ClanStringListPane`), `0x00615770` (`ClanJoinListPane`), `0x00615834` (`ClanEnlistListPane`), `0x00619dc8` (`ExchangeItemListPane`), and `0x00619f28` ([UID:0001Y8][MyItemListPaneVtables](by-type/by-vtable/MyItemListPaneVtables.md)).
- Current generated metadata owns this function under `ExchangeItemListPane`, but IDA vtable evidence shows the target is shared by at least `ExchangeItemListPane` and `MyItemListPane`. Do not use the generated single-owner label as exclusive source ownership.

## Reconstruction Guidance

For final C++ reconstruction, write ordinary destructors for the owning `ListPane`-derived classes and let the compiler emit scalar deleting destructors and secondary/tertiary adjustor thunks. This exact function remains important binary-coverage evidence, but it should not force all vtable users into `ExchangeDialog.cpp`.

## Cross-References

- [UID:0001Y8][MyItemListPaneVtables](by-type/by-vtable/MyItemListPaneVtables.md)
- [UID:00008W][MyItemListPane](by-class/MyItemListPane.md)
- [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)
- [UID:0000ZP][0x0047e855-0x0047e947.DialogAndAlertDestructorAdjustorThunks](by-memory/0x0047e855-0x0047e947.DialogAndAlertDestructorAdjustorThunks.md)
- [UID:0000KT][ListPane](by-file/ListPane.md)

## Changes

- 2026-05-30: Updated completion/confidence from `50/65` to `78/88`; expanded known vtable users from the partial generated-note set to the IDA-confirmed vtable references at `0x00613ce8`, `0x006156ac`, `0x00615770`, `0x00615834`, `0x00619dc8`, and `0x00619f28`; added raw disassembly, adjustor thunk evidence, and a C++ reconstruction candidate. The item remains below full completion because final source-file ownership and the exact runtime name/meaning of the `flags & 4` helper path still need review.
