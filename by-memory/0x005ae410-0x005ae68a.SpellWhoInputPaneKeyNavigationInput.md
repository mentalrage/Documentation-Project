*** UID:0001L0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DX | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DX | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool SpellWhoInputPane::OnKeyEvent(KeyEvent *event)
{
    const unsigned char key = NarrowKey(event->key);
    const unsigned char flags = event->modifierFlags;

    if ((flags & 0x03) != 0)
        return true;

    if (key == kKeyEnter) {
        if (event->kind == kKeyPressed) {
            LivingObjectPane *target =
                g_activeMapPane->FindObjectPaneById(s_spellWhoTargetId);
            if (target != NULL) {
                ClearSpellWhoTarget();

                ObjectListPosition position;
                target->GetObjectListPosition(&position);
                SendTargetedActionPacket(m_spellIndex, target->ObjectId(),
                    position.x, position.y);
            }
        }
        return LineInputPane::OnKeyEvent(event);
    }

    if (key == kKeyEscape) {
        if (event->kind == kKeyPressed) {
            LivingObjectPane *target =
                g_activeMapPane->FindObjectPaneById(s_spellWhoTargetId);
            if (target != NULL)
                ClearSpellWhoTarget();
        }
        return LineInputPane::OnKeyEvent(event);
    }

    if (event->kind != kKeyPressed)
        return true;

    LivingObjectPane *current =
        g_activeMapPane->FindObjectPaneById(s_spellWhoTargetId);

    if (key == kKeySelfTarget) {
        LivingObjectPane *selfTarget = g_pUserPane->GetLocalPlayerObject();
        if (current != selfTarget) {
            if (current != NULL)
                current->SetTargetHighlight(false);
            selfTarget->SetTargetHighlight(true);
            s_spellWhoTargetId = selfTarget->ObjectId();
        }
        return true;
    }

    const TargetTraversalOrder *order =
        ((flags & 0x04) != 0)
            ? &g_pConfig->alternateTargetTraversalOrder
            : &g_pConfig->targetTraversalOrder;

    int traversalDirection;
    if (order->first == 1) {
        if (order->second == 1) {
            traversalDirection = -1;
        } else if (order->second == 0) {
            traversalDirection = 0;
        } else {
            return true;
        }
    } else {
        if (order->first != 0 || order->second != 1)
            return true;
        traversalDirection = 1;
    }

    LivingObjectPane *nextTarget = NULL;
    if (IsPreviousTargetKey(key)) {
        nextTarget = current != NULL
            ? g_activeMapPane->FindPreviousSelectableObject(current, traversalDirection)
            : g_pUserPane->GetLocalPlayerObject();
    } else if (IsNextTargetKey(key)) {
        nextTarget = current != NULL
            ? g_activeMapPane->FindNextSelectableObject(current, traversalDirection)
            : g_pUserPane->GetLocalPlayerObject();
    } else {
        return true;
    }

    if (nextTarget == current)
        return true;

    if (current != NULL)
        current->SetTargetHighlight(false);

    if (nextTarget != NULL) {
        nextTarget->SetTargetHighlight(true);
        s_spellWhoTargetId = nextTarget->ObjectId();
    } else {
        s_spellWhoTargetId = 0;
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-confirmed key handler attached to `SpellWhoInputPane`, with vtable-only dispatch, `do_narrow` key gate, Enter/Escape/self/previous/next behavior, `dword_69BF20` updates, highlight toggles, targeted packet send at `0x005ae4bd`, callee set, and switch-support boundary bytes documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005ae410-0x005ae68a - SpellWhoInputPane Key Navigation Input

## Status

- Entity kind: key-input virtual method.
- IDA function: `sub_5AE410`, size `0x27a`
- Canonical owner: [UID:0000DX][SpellWhoInputPane](by-class/SpellWhoInputPane.md), under [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md)
- Rebuild handling: source-authored.
- Disposition: reconstructable project key-navigation body.
- Confidence: strong for function boundary, vtable dispatch slot, key/event gates, saved-target updates, highlight effects, targeted spell packet dispatch, switch-support bytes, and owner; final event field and helper names remain provisional.

## Covered Range

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x005ae410-0x005ae68a` | `SpellWhoInputPane` key navigation input | Handles Enter/Escape, self-target selection, target navigation, highlight updates, and targeted spell submission for the saved spell target. |

## Reconstruction

The formal reconstruction block now contains the accepted source-facing `SpellWhoInputPane::OnKeyEvent(KeyEvent *event)`. The code preserves the low-bit modifier gate, event-kind gate, Enter/Escape base `LineInputPane::OnKeyEvent` fallback, self-target key, previous/next key groups, traversal option pairs, signed traversal direction, targeted-action send through [UID:0001L9][0x005af4f0-0x005af57b.SendTargetedActionPacket](by-memory/0x005af4f0-0x005af57b.SendTargetedActionPacket.md), and `ObjectListPosition` mapping for `sub_5374D0` outputs.

## Evidence Notes

- IDA MCP `lookup_funcs` on 2026-06-05 reports `0x005ae410` as `sub_5AE410`, size `0x27a`, ending at `0x005ae68a`; `0x005ae403` and `0x005ae68a` are not function starts, and `0x005ae730` starts sibling `sub_5AE730`.
- IDA MCP `py_eval` on 2026-06-05 confirms thirteen `0xcc` alignment bytes at `0x005ae403-0x005ae410`. The following `0x005ae68a-0x005ae730` bytes are switch-support tables and trailing alignment before the pointer-selection method at `0x005ae730`.
- `xrefs_to 0x005ae410` reports a single vtable data ref at `0x0062f268`; `callers 0x005ae410` reports no direct static callers. A live vtable-byte check shows slot `0x0062f268` points to `sub_5AE410`, adjacent to the pointer handler slot at `0x0062f264`.
- Decompilation and disassembly narrow the key byte at event offset `+0x08` through `std::ctype<char>::do_narrow` at `0x005ae430`, read event flags from `+0x10a` at `0x005ae423-0x005ae435`, reject the switch body when `(flags & 3) != 0`, and use the narrowed key to enter the dispatch cases.
- Enter case `13` requires event state byte `+0x04` to equal `8`, looks up saved object id `dword_69BF20` through `dword_67A764` and `sub_506970` at `0x005ae46e-0x005ae47a`, clears the existing highlight with [UID:0001L3][0x005aeb00-0x005aeb1f.DeactivateSavedSpellWhoTarget](by-memory/0x005aeb00-0x005aeb1f.DeactivateSavedSpellWhoTarget.md) at `0x005ae48e`, reads the target object id from `+0xfc`, pulls two target coordinate/argument words through `sub_5374D0` at `0x005ae4a0` and `0x005ae4ae`, sends [UID:0001L9][0x005af4f0-0x005af57b.SendTargetedActionPacket](by-memory/0x005af4f0-0x005af57b.SendTargetedActionPacket.md) at `0x005ae4bd`, and closes or forwards the input through `sub_4F25A0` at `0x005ae4c8`.
- The targeted-action byte sent in the Enter case is read from `this + 0x68` in the adjusted method view, then passed with the saved target id and the two `sub_5374D0` words to the shared packet helper.
- Escape case `27` requires the same event state byte, looks up the saved target at `0x005ae4de-0x005ae4ea`, clears highlight when a target exists at `0x005ae4f9`, closes or forwards through `sub_4F25A0` at `0x005ae4fe`, and returns consumed.
- Self-target case signed key `-111` requires event state byte `8`, loads the local player object from `dword_67A748` at `0x005ae527`, compares it with the current saved target looked up at `0x005ae51b-0x005ae52d`, clears the old highlight if needed at `0x005ae542`, highlights the local player at `0x005ae54b`, and stores `local+0xfc` into `dword_69BF20` at `0x005ae558`.
- Previous/up-style keys `104`, `107`, `-128`, and `-127` require event state byte `8`, select either `dword_67A7C8+0x2911dc` or `dword_67A7C8+0x2911de` from flag bit `0x04`, derive traversal direction `-1`, `0`, or `1` from that two-byte option pair, look up the current saved target at `0x005ae5bc-0x005ae5c8`, call `sub_506C20` when a current target exists, and fall back to `dword_67A748` when no current target exists.
- Next/down-style keys `106`, `108`, `-126`, and `-125` use the same option-byte logic, look up the current saved target at `0x005ae632-0x005ae63e`, call `sub_506C70` when a current target exists, and fall back to `dword_67A748` when no current target exists.
- The common navigation update returns immediately when old and new targets match, clears the old target highlight through `sub_53B1B0` at `0x005ae66c`, highlights a non-null new target through `sub_53B1B0` at `0x005ae54b`, stores the new object id into `dword_69BF20` at `0x005ae558`, or writes zero at `0x005ae679` when no new target exists.
- `callees 0x005ae410` reports `sub_53B1B0`, `std::ctype<char>::do_narrow`, `sub_506C20`, `sub_5374D0`, `sub_506970`, `sub_5AF4F0`, `sub_4F25A0`, `sub_506C70`, and `sub_5AEB00`.
- `xrefs_to dword_69BF20` reports this key handler's saved-target reads and writes, including `0x005ae46e`, `0x005ae4de`, `0x005ae51b`, `0x005ae558`, and `0x005ae5bc`; the remaining key-handler refs are the disassembly-confirmed next/down lookup and zero-save sites at `0x005ae632` and `0x005ae679`.

## Access And Control Sites

| Address or offset | Meaning | Evidence |
| --- | --- | --- |
| `event + 0x08` | raw key byte | Narrowed through `std::ctype<char>::do_narrow` at `0x005ae430`. |
| `event + 0x10a` | event flags | Read at `0x005ae423-0x005ae435`; low bits gate the switch body, and bit `0x04` chooses the target-option byte pair. |
| `event + 0x04` | event state byte | Enter, Escape, self-target, and navigation paths require value `8`. |
| `0x005ae46e-0x005ae47a` | saved-target lookup | Enter path resolves `dword_69BF20` through `dword_67A764` and `sub_506970`. |
| `0x005ae48e` | `call sub_5AEB00` | Clears the active spell-target highlight before confirming. |
| `0x005ae4a0` / `0x005ae4ae` | `call sub_5374D0` | Reads the two target argument words passed to the packet helper. |
| `0x005ae4bd` | `call sub_5AF4F0` | Sends the targeted spell/object action packet. |
| `0x005ae4c8` / `0x005ae4fe` | `call sub_4F25A0` | Closes or forwards consumed Enter/Escape events. |
| `0x005ae542` | `call sub_53B1B0` with `0` | Clears the old target highlight during self-target selection. |
| `0x005ae54b` | `call sub_53B1B0` with `1` | Highlights the newly selected target. |
| `0x005ae558` | `dword_69BF20` write | Saves a newly selected target object's `+0xfc` id. |
| `0x005ae57a` / `0x005ae581` | config option pair choice | Previous/up navigation selects `dword_67A7C8+0x2911dc` or `+0x2911de`. |
| `0x005ae5df` | `call sub_506C20` | Traverses to a previous/up-style living target. |
| `0x005ae5fc` / `0x005ae603` | config option pair choice | Next/down navigation selects `dword_67A7C8+0x2911dc` or `+0x2911de`. |
| `0x005ae651` | `call sub_506C70` | Traverses to a next/down-style living target. |
| `0x005ae66c` | `call sub_53B1B0` with `0` | Clears the old target highlight after navigation. |
| `0x005ae679` | zero save | Clears `dword_69BF20` when navigation produces no target. |

## Ownership Decision

Attach this method to [UID:0000DX][SpellWhoInputPane](by-class/SpellWhoInputPane.md). The class and [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) parent clear the attachment gate, and the live evidence ties this body to the `SpellWhoInputPane` vtable slot, saved spell-target global, sibling constructor/destructor state, and pointer/object-list target-maintenance behavior.

## Cross-References

- [UID:0000DX][SpellWhoInputPane](by-class/SpellWhoInputPane.md)
- [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md)
- [UID:0001KY][0x005ae2a0-0x005aeb1f.SpellWhoInputPane](by-memory/0x005ae2a0-0x005aeb1f.SpellWhoInputPane.md)
- [UID:0001KX][0x005ae2a0-0x005ae372.SpellWhoInputPaneConstructor](by-memory/0x005ae2a0-0x005ae372.SpellWhoInputPaneConstructor.md)
- [UID:0001KZ][0x005ae380-0x005ae403.SpellWhoInputPaneDestructor](by-memory/0x005ae380-0x005ae403.SpellWhoInputPaneDestructor.md)
- [UID:0001L1][0x005ae730-0x005aea53.SpellWhoInputPanePointerSelectionInput](by-memory/0x005ae730-0x005aea53.SpellWhoInputPanePointerSelectionInput.md)
- [UID:0001L2][0x005aea60-0x005aeaf3.SpellWhoInputPaneObjectListVirtual](by-memory/0x005aea60-0x005aeaf3.SpellWhoInputPaneObjectListVirtual.md)
- [UID:0001L3][0x005aeb00-0x005aeb1f.DeactivateSavedSpellWhoTarget](by-memory/0x005aeb00-0x005aeb1f.DeactivateSavedSpellWhoTarget.md)
- [UID:0001NK][0x005b8100-0x005b81c4.SpellWhoInputPaneScalarDeletingDestructor](by-memory/0x005b8100-0x005b81c4.SpellWhoInputPaneScalarDeletingDestructor.md)
- [UID:0001L9][0x005af4f0-0x005af57b.SendTargetedActionPacket](by-memory/0x005af4f0-0x005af57b.SendTargetedActionPacket.md)
- [UID:0002AB][0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals](by-memory/0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals.md)

## Changes

- 2026-06-05: Raised completion/confidence from `70/85` to `86/90`, attached it to [UID:0000DX][SpellWhoInputPane](by-class/SpellWhoInputPane.md), initially left the reconstruction block empty pending source-quality naming, and refreshed the page with live IDA evidence for exact function bounds, vtable-only dispatch, key narrowing, event-state gates, Enter/Escape/self/navigation behavior, saved-target updates, highlight effects, targeted-action dispatch, callee set, no direct static callers, adjacent switch-support bytes, and sibling target-selection relationships.
- 2026-06-26 B004 implementation: Raised completion from `86` to `88`, kept confidence `90`, preserved owner/emitter [UID:0000DX][SpellWhoInputPane](by-class/SpellWhoInputPane.md), replaced stale final-threshold no-code wording, and inserted the accepted formal `OnKeyEvent` C++ with Ctrl/modifier gate, event-kind gate, Enter/Escape fallback, self-target/navigation keys, traversal config pairs, packet send path, and saved-target updates.
