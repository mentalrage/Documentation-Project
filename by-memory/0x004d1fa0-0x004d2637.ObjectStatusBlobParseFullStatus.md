*** UID:0004LL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00009S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00009S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int ObjectStatusBlob::ParseFullStatus(const unsigned char *packet)
{
    const unsigned short kNoPart = 0xffff;

    m_statusKind = HumanObject;
    m_actionState = 11;
    m_gender = packet[0];
    m_compositionMode = packet[1];
    m_ridingAppearanceId = PacketBufferReadUInt16BE(packet + 2);
    m_displayStatusByte = packet[4];

    if (m_ridingAppearanceId != 0)
    {
        if (!g_useEpfAssets)
            m_ridingAppearanceId = 1;

        wchar_t ridingKeyText[128];
        swprintf_s(ridingKeyText,
                   sizeof(ridingKeyText) / sizeof(ridingKeyText[0]),
                   L"%d",
                   static_cast<int>(m_ridingAppearanceId));

        SimpleUString tableName;
        tableName.Assign(L"RidableAnimals", 14);

        SimpleUString ridingKey;
        ridingKey.Assign(ridingKeyText,
                         static_cast<unsigned int>(wcslen(ridingKeyText)));

        const SimpleUString *value =
            g_pMetaMan->FindEntry(&tableName, &ridingKey, 1);
        m_ridingDefinitionIndex =
            static_cast<unsigned short>(_wtol(value->c_str()));

        value = g_pMetaMan->FindEntry(&tableName, &ridingKey, 2);
        m_ridingPaletteFlags =
            static_cast<unsigned short>(_wtol(value->c_str()));

        value = g_pMetaMan->FindEntry(&tableName, &ridingKey, 3);
        m_ridingYOffset = static_cast<short>(_wtol(value->c_str()));
    }

    m_faceId = PacketBufferReadUInt16BE(packet + 5);
    m_hairId = packet[7];
    m_hairColor = packet[8];
    m_faceColor = packet[9];
    m_skinColor = packet[10];

    if (m_faceId != kNoPart &&
        (m_faceId <= 199 ||
         (m_faceId >= 900 && m_faceId <= 999) ||
         m_faceId > 399))
    {
        m_hairId = static_cast<unsigned char>(m_faceId);
        m_faceId = 200;
    }

    if (m_hairId < g_pNewHumanImageLib->m_hairColorEntries.size())
    {
        m_hairColor = static_cast<unsigned char>(
            (m_hairColor & 0xe0) +
            (m_hairColor +
             g_pNewHumanImageLib->m_hairColorEntries[m_hairId]) % 32);
    }

    const unsigned short bodyOrCoatId =
        PacketBufferReadUInt16BE(packet + 11);
    if (bodyOrCoatId <= 9999)
    {
        m_bodyId = bodyOrCoatId;
        m_coatId = kNoPart;
        if (m_bodyId == 0)
            m_bodyId = (m_gender != 0);
    }
    else
    {
        m_coatId = static_cast<unsigned short>(bodyOrCoatId % 10000);
        m_bodyId = kNoPart;
    }

    m_bodyColor = packet[13];
    m_weaponId = PacketBufferReadUInt16BE(packet + 14);
    m_weaponColor = packet[16];
    m_shieldOrArrowId = PacketBufferReadUInt16BE(packet + 17);
    m_shieldOrArrowColor = packet[19];
    m_headgearMode = packet[20];
    m_helmetId = packet[21];
    m_helmetColor = packet[22];
    m_faceDecorationId = PacketBufferReadUInt16BE(packet + 23);
    m_faceDecorationColor = packet[25];
    m_hairDecorationId = PacketBufferReadUInt16BE(packet + 26);
    m_hairDecorationColor = packet[28];
    m_secondFaceDecorationId = PacketBufferReadUInt16BE(packet + 29);
    m_secondFaceDecorationColor = packet[31];
    m_mantleId = PacketBufferReadUInt16BE(packet + 32);
    m_mantleColor = packet[34];
    m_neckId = PacketBufferReadUInt16BE(packet + 35);
    m_neckColor = packet[37];
    m_shoesId = PacketBufferReadUInt16BE(packet + 38);
    m_shoesColor = packet[40];
    m_invisible = packet[41];
    m_faceShape = packet[42];
    m_fullBodyId = kNoPart;

    if (m_coatId == kNoPart)
    {
        if (m_bodyId != 0 &&
            m_bodyId != 1 &&
            m_bodyId != 57 &&
            m_bodyId != 58 &&
            m_bodyId != 160 &&
            m_bodyId != 161)
        {
            m_shoesId = kNoPart;
        }
    }
    else if (m_shoesId == kNoPart)
    {
        m_shoesId = (m_gender != 0);
    }

    if (m_headgearMode == 0)
        m_helmetId = 0xff;
    else if (m_headgearMode == 1 || m_headgearMode == 2)
        m_hairId = 0xff;

    return 43;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready 43-byte full ObjectStatusBlob parser with complete 68-byte field writes, RidableAnimals metadata, HAIRCOL remap, Body/Coat and sentinel normalization, equipment/color tail, and exact caller/callee evidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# ObjectStatusBlob::ParseFullStatus

## Scope and boundary

- Exact modeled body: `0x004d1fa0-0x004d2637` (exclusive end, size `0x697`). The preceding seven-byte and following nine-byte `0xcc` spans at `0x004d1f99-0x004d1fa0` and `0x004d2637-0x004d2640` are alignment, not source statements.
- UID000179 is the non-emitting split index. This source-bearing member is owned/emitted by ObjectStatusBlob UID00009S through ObjectStatusBlob file UID0000M6 at `NexusTK/map/ObjectStatusBlob.cpp`.
- The source signature is member `int(const unsigned char *)`: thiscall lowering supplies the 68-byte receiver, packet memory is read-only, and every direct caller uses the returned consumed length or a fixed packet layout.

## Exact 43-byte payload map

| Packet bytes | Receiver offset/member | Transformation |
| --- | --- | --- |
| `0`, `1` | `+0x21` gender and `+0x22` composition mode | direct bytes; the same storage has compact direction/posture aliases |
| `2..3` | `+0x2a` riding appearance id | BE16; nonzero is forced to 1 when `g_useEpfAssets` is false, then drives metadata lookup |
| `4` | `+0x28` display/default byte | direct byte |
| `5..6`, `7` | `+0x04` face/appearance and `+0x06` hair/variant | BE16 plus direct byte, followed by exact face normalization |
| `8`, `9`, `10` | `+0x33`, `+0x32`, `+0x34` | hair, face, and shared skin/tint colors; HAIRCOL remap follows |
| `11..12` | `+0x08` body or `+0x1c` coat | BE16 split at 9999 with `0xffff` counterpart |
| `13` | `+0x35` | body/coat color |
| `14..15`, `16` | `+0x0a`, `+0x36` | weapon id/color |
| `17..18`, `19` | `+0x0c`, `+0x37` | shield/arrow id/color |
| `20`, `21`, `22` | `+0x20`, `+0x0e`, `+0x38` | headgear mode, helmet id, helmet color |
| `23..24`, `25` | `+0x10`, `+0x39` | first FaceDec id/color |
| `26..27`, `28` | `+0x12`, `+0x3a` | HairDec id/color |
| `29..30`, `31` | `+0x14`, `+0x3b` | second FaceDec id/color |
| `32..33`, `34` | `+0x16`, `+0x3c` | mantle id/color |
| `35..36`, `37` | `+0x18`, `+0x3d` | neck id/color |
| `38..39`, `40` | `+0x1a`, `+0x3e` | shoes id/color |
| `41`, `42` | `+0x3f`, `+0x40` | invisibility/status and face-shape bytes |

- The method also sets status kind to full/human, action state to 11, and full-body id to `0xffff`. Explicit padding at receiver `+0x07`, `+0x0f`, `+0x23`, `+0x29`, and trailing `+0x41..+0x43` is untouched.
- Face normalization preserves the observed predicate exactly: any non-`0xffff` face satisfying `<=199`, `900..999`, or `>399` moves its low byte into the hair/variant slot and forces face id 200. The apparently redundant middle interval is not simplified away.
- Body/coat normalization selects Body for packet values `<=9999`, otherwise Coat modulo 10000; the other slot becomes `0xffff`. Body zero becomes the gender boolean. Without a coat, shoes are invalidated unless Body is 0, 1, 57, 58, 160, or 161. With a coat, `0xffff` Shoes defaults to gender.
- Headgear mode 0 hides Helmet with `0xff`; modes 1/2 hide Hair with `0xff`. No stronger universal posture/direction semantics are asserted for the context-dependent alias bytes.

## Riding, resources, and globals

- Nonzero riding id formats a UTF-16 `%d` key, queries MetaMan `FindEntry` table `RidableAnimals`, and converts columns 1, 2, and 3 through `_wtol` into receiver `+0x2c` riding definition index, `+0x2e` packed palette/render flags, and signed `+0x30` Y offset. The superseded title-metadata interpretation is rejected.
- `g_useEpfAssets` is canonical byte global UID0000SW at `0x0066da97`; it controls the legacy riding-id force. `0x0067a760` is `g_pNewHumanImageLib` UID0000RT, not a generic remap table.
- NewHumanImageLib `m_hairColorEntries` vector begin/end at `+0x54/+0x58` comes from `HAIRCOL.TBL`. In-range hair applies the exact low-five-bit offset modulo 32 while preserving the high three color bits.
- Callees are canonical `PacketBufferReadUInt16BE` at `0x00575480`, `MetaMan::FindEntry` at `0x00523120`, secure wide formatting at `0x0041b9b0`, SimpleUString assignment/cleanup lowering at `0x00421590`/`0x00421310`, and CRT `_wtol` at `0x005cea43`. Normal source-level RAII represents the lowered string cleanup/EH paths.

## Part.tbl consumer map

| Row | Resource name/range | ObjectStatusBlob selector/color |
| --- | --- | --- |
| 0 | `Body`, `0..19999` | body id and body color |
| 1 | `Coat`, `20000..39999` | normalized coat id and body color |
| 2 | `Face`, `200..999` | face id and face color |
| 3 | `Emotion`, `1` | face/emotion selector family and face color |
| 4 | `FaceDec`, `0..9999` | first/second FaceDec ids and colors |
| 5 | `Hair`, `1` | hair id and hair color |
| 6 | `Helmet`, `1` | helmet id and helmet color |
| 7 | `HairDec`, `10000..19999` | hair-decoration id/color |
| 8 | `Sword`, `0..9999` | weapon id/color |
| 9 | `Spear`, `10000..19999` | normalized weapon id, same color |
| 10 | `Bow`, `20000..29999` | normalized weapon id, same color |
| 11 | `Fan`, `30000..39999` | normalized weapon id, same color |
| 12 | `Shield`, `0..9999` | shield/arrow id/color |
| 13 | `Arrow`, `10000..19999` | normalized shield/arrow id, same color |
| 14 | `Mantle` | mantle id/color |
| 15 | `Shoes` | shoes id/color |
| 16 | `Neck` | neck id/color |
| 17 | `All` | full-body id, no separate packet color byte |
| 18 | `Back` | alternate weapon/back classification from weapon id |

## Direct callers and consumers

- Tagged mode-0 delegation occurs at `0x004d1f8d`. MapPane full calls are `0x0050fba3` and `0x00513354`.
- SelfLook full calls are `0x0056949b`, `0x00569540`, and raw sites `0x0056a55d`/`0x0056a600`. SelfLookPane2 full calls are `0x005721e7`, `0x00572294`, and raw sites `0x00572bbd`/`0x00572c60`.
- UserLookPane full call is `0x0059f8dc`. ObjectImage, NewHuman composition/bounds, HeadSelect, profile, dialogs, menus, and map object panes independently consume the resulting fixed 68-byte layout.
- These broad callers establish a shared ObjectStatusBlob API, not ownership by MapPane, UserLook/Profile, SelfLook, dialogs, NewHumanImageLib, MetaMan, or PacketBuffer.

## Negative evidence and score

- No title table/string is referenced; exact `RidableAnimals` and NewHuman riding consumers supersede that historical assumption. No generic profile tail, raw field array, packed class, free/static wrapper, decompiler-temporary source, or aggregate source body is accepted.
- The raw setters at `0x004d26a0`/`0x004d26e0` are outside UID000179's exclusive endpoint and lack modeled function/xref evidence. They are not silently absorbed.
- Score `91/93` is intentionally one point below the small siblings: every behavior, offset, packet read, constant, normalization, callee/global, boundary, and caller family is resolved, while the broad body depends on several descriptive original member/API spellings without debug symbols.

