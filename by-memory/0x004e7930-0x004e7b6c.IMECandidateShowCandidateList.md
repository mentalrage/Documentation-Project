*** UID:00018B | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IMECandidatePane::ShowCandidateList(const Event *event)
{
    const CandidateStringList *candidateStrings = event->imeCandidateStrings;
    const int selectedRow = event->imeCandidateSelectedRow;

    int maxTextWidth = 0;
    int popupHeight = 4;
    const int lineStep = GetLineHeight() + 2;

    for (CandidateStringList::const_iterator it = candidateStrings->begin();
         it != candidateStrings->end();
         ++it) {
        const std::wstring &candidate = *it;
        const int textWidth = MeasureTextWidth(candidate.c_str(), static_cast<int>(candidate.length()));
        if (maxTextWidth < textWidth) {
            maxTextWidth = textWidth;
        }
        popupHeight += lineStep;
    }

    RectBounds popupRect;
    popupRect.left = candidateAnchorX;
    popupRect.top = candidateAnchorY;
    popupRect.right = candidateAnchorX + maxTextWidth + 6;
    popupRect.bottom = candidateAnchorY + popupHeight;

    const int screenWidth = g_pScreenPane->GetScreenWidth();
    const int screenHeight = g_pScreenPane->GetScreenHeight();

    if (popupRect.right >= screenWidth) {
        TranslateRectBounds(&popupRect, screenWidth - popupRect.right, 0);
    }

    if (popupRect.bottom >= screenHeight) {
        if (g_pIMEPane->activeChildPane != NULL) {
            RectBounds activeChildRect;
            g_pIMEPane->activeChildPane->GetBounds(&activeChildRect);
            TranslateRectBounds(&popupRect, 0, activeChildRect.top - popupRect.bottom - 8);
        } else {
            TranslateRectBounds(&popupRect, 0, screenHeight - popupRect.bottom);
        }
    }

    if (popupRect.left < 0) {
        TranslateRectBounds(&popupRect, -popupRect.left, 0);
    }

    SetBounds(&popupRect, 0);

    visibleCandidates.assign(candidateStrings->begin(), candidateStrings->end());
    selectedCandidateRow = selectedRow;
    Invalidate(&bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IME candidate popup helper with exact IDA size 0x23c boundary, sole HandleIMEMessage caller, generated fitting-room owner pollution closed, visible candidate-range measurement, screen/child-pane clamping, candidate-string queue assignment, selected-index update, and parent attachment to IMECandidatePane. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004e7930-0x004e7b6c IMECandidateShowCandidateList

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: `IMECandidatePane` method.
- Historical generated owner: `FittingRoomDownloadControlPane::ShowCandidateList` is generated owner pollution.
- Confirmed owner: `IMECandidatePane::ShowCandidateList` in [UID:0000K5][IMEPanes](by-file/IMEPanes.md)
- Attached parent: [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md).
- Confidence: strong for boundary, sole caller, candidate-pane ownership, event payload semantics, broad behavior, and major touched state; medium-high for final source field/helper spellings and inherited pane virtual names.
- Reconstruction C++: first-draft formal C++ populated by B008 callback. Source-facing names remain inferred but are backed by current MCP behavior, B001 Event producer evidence, and the [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md) class shell.

## Function

| Range | Proposed method | Notes |
| --- | --- | --- |
| `0x004e7930-0x004e7b6c` | `IMECandidatePane::ShowCandidateList` | Parses candidate-list data, measures candidate text, positions and clamps the popup, copies the visible candidate range into the candidate queue, records the selected row, and invalidates the pane. |

## Behavior

- Uses a `thiscall` shape equivalent to `IMECandidatePane::ShowCandidateList(const Event *event)`; the caller at `0x004e7d13` first verifies Event type `15`, then adjusts the secondary handler subobject pointer by `-0xa0` back to the primary candidate pane.
- Reads the candidate-list payload pointer from Event offset `+0x08`; this is a pointer to the visible candidate string deque/list produced by `InputMan`.
- Reads the selected candidate row from Event offset `+0x0c`; producer-side evidence shows this is `CANDIDATELIST.dwSelection - pageStart`, not an absolute IMM candidate index.
- Iterates the visible candidate strings using the deque internals at payload offsets `+0x0c` and `+0x10` as `_Myoff`/`_Mysize`-style fields, measures each string with the shared text-width helper at `0x004ba9a0`, and accumulates row height using `0x004baaa0` plus a two-pixel row gap.
- Builds the popup rectangle from the pane's current candidate anchor fields at offsets `+0x110` and `+0x114`, the maximum measured text width plus six pixels, and the accumulated candidate row height plus four pixels.
- Clamps the popup horizontally and vertically against screen dimensions returned by `0x00557450` and `0x00557460` from the screen singleton at `dword_67A7CC`.
- If the candidate popup would run below the screen and the root IME pane has an active child/popup pane at `g_pIMEPane + 0xfc`, the method asks that pane for its rectangle through vtable slot `+0x28` and moves the candidate list relative to that rectangle; otherwise it clamps directly to the screen bottom.
- Applies the resulting geometry through the inherited pane virtual at vtable slot `+0x2c`, assigns the visible candidate range into the embedded candidate-string container at offset `+0xf8` through [UID:00018C][0x004e8450-0x004e8ae6.IMECandidateQueueAndDestructors](by-memory/0x004e8450-0x004e8ae6.IMECandidateQueueAndDestructors.md), stores the selected-row/index field at offset `+0x10c`, and invalidates the pane through the inherited virtual at slot `+0x20`.

## Evidence Notes

- 2026-06-06 live IDA MCP `lookup_funcs` reports start `0x004e7930` and size `0x23c`; `0x004e7b6b` is still inside `sub_4E7930`, `0x004e7b6c` is outside it, and the next function begins at `0x004e7b70`.
- 2026-06-06 live IDA MCP `callers` shows exactly one direct code caller at `0x004e7d13` inside `0x004e7d00-0x004e7d24`.
- 2026-06-06 live IDA MCP decompilation of `0x004e7d00` shows the caller accepts IME message/event byte `15`, calls `0x004e7930` with the message pointer, and adjusts the subobject pointer by `-40` dwords before the call; this matches `IMECandidatePane::HandleIMEMessage` forwarding to the primary candidate-pane method body.
- 2026-06-06 live IDA MCP callees for `0x004e7930` are the text-width/height helpers, screen dimension helpers, rectangle translator, candidate-string range assignment at `0x004e8450`, and the security-cookie check. No fitting-room-specific callee or state appears in the body.
- IDA MCP decompilation shows candidate-list parsing, [UID:00001G][CandidateStringQueue](by-class/CandidateStringQueue.md) assignment through `0x004e8450`, popup geometry updates, [UID:0001PS][0x0069b458-0x0069b45c.g_pIMEPane](by-memory/0x0069b458-0x0069b45c.g_pIMEPane.md) active child-pane fallback, and screen-bound checks through [UID:0001G7][0x00557380-0x00557467.ScreenCursorHelpers](by-memory/0x00557380-0x00557467.ScreenCursorHelpers.md).
- Existing docs record that generated output still emits this function under `FittingRoomDownloadControlPane`; the live caller, candidate-list payload, local candidate queue update, and contiguous IME pane neighborhood make that a generated-owner pollution case.
- 2026-06-17 B001 source-quality reanalysis used live IDA session `b003_00018a_ime_comp_20260617` plus the current IMEPanes, InputMan, Event, GrafPort, RectBounds, Screen, and FittingRoom documentation. It reconfirmed the exact `0x23c` body, next boundary at `0x004e7b70`, sole direct call from `IMECandidatePane::HandleIMEMessage` at `0x004e7d13`, and vtable reachability of that handler.
- B001 producer-side evidence links `InputMan::HandleWindowMessage` candidate-list notification handling to Event factory `0x004a9e50`: the producer reads IMM `CANDIDATELIST` count/selection/page-start/page-size fields, defaults page size to `9` when IMM gives zero, appends visible-page candidates to the deque helper at `0x004e9710`, and dispatches Event type `15` with the candidate deque pointer and selected row.
- B008's [UID:00014C][0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers](by-memory/0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers.md) report consolidates this producer side as `DispatchCandidateListEvent(int selectedRow, CandidateStringList *visibleCandidates, int tickCount)`, with Event type `15`, candidate deque/list pointer at Event `+0x08`, selected row at Event `+0x0c`, and tick count at Event `+0x10c`.

## Source-Quality Resolution

| Item | Resolution |
| --- | --- |
| Owner/source route | Keep [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md) and [UID:0000K5][IMEPanes](by-file/IMEPanes.md); reject FittingRoom, InputMan, TextEdit/TextBox, Event, GrafPort, RectBounds, and ScreenPane as direct owners. |
| Source-facing method | `void IMECandidatePane::ShowCandidateList(const Event *event)` or reference equivalent. The decompiler return value is an inherited invalidate artifact; the sole caller ignores it. |
| Event payload | Event type `15`, `event + 0x08` candidate deque/list pointer, `event + 0x0c` selected row within the visible candidate page. |
| Deque internals | Candidate container offsets `+0x0c/+0x10` are STL deque `_Myoff`/`_Mysize`-style internals, not product IME payload fields. |
| Pane fields | `this + 0xf8` is the pane-local visible candidate deque, `this + 0x10c` is the selected candidate row, `this + 0x110` is `candidateAnchorY`, and `this + 0x114` is `candidateAnchorX`. |
| Root IME field | `g_pIMEPane + 0xfc` is the active child/popup pane used for alternate vertical clamping when the candidate popup would exceed screen height. |
| Shared helpers | Text width/height belong to GrafPort, rectangle offset belongs to RectBounds, screen width/height belong to screen helper code, Event factory `0x004a9e50` belongs to Event, and candidate deque helpers are STL support under the IME candidate queue/template pages. |
| Range policy | Do not split; `0x004e7930-0x004e7b6c` is one exact function body followed by four bytes of alignment before `0x004e7b70`. |

Remaining bounded uncertainties are exact original spellings for inherited Pane virtual names and adjacent helper declarations. These are source-spelling caps for final audit, not blockers to the accepted first-draft formal C++.

## Reconstruction Notes

- Keep this with [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md) and the candidate string deque/template support documented by [UID:00001G][CandidateStringQueue](by-class/CandidateStringQueue.md) / [UID:0001WO][CandidateStringDequeTemplate](by-type/by-template/CandidateStringDequeTemplate.md).
- Do not use this range as fitting-room evidence; it is an IME candidate popup helper in the contiguous `0x004e70f0-0x004e8ae6` IME neighborhood.
- Normalize `DAT_0069b458` / `dword_69B458` aliases to `g_pIMEPane` when converting generated output.
- The formal block uses accepted source-facing names from the B008 report: `Event::imeCandidateStrings`, `Event::imeCandidateSelectedRow`, `CandidateStringList`, `candidateAnchorX`, `candidateAnchorY`, `visibleCandidates`, `selectedCandidateRow`, and root `g_pIMEPane->activeChildPane`.

## Cross-References

- [UID:0000K5][IMEPanes](by-file/IMEPanes.md)
- [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md)
- [UID:00001G][CandidateStringQueue](by-class/CandidateStringQueue.md)
- [UID:000189][0x004e70f0-0x004e8433.IMEPaneFamily](by-memory/0x004e70f0-0x004e8433.IMEPaneFamily.md)
- [UID:00018C][0x004e8450-0x004e8ae6.IMECandidateQueueAndDestructors](by-memory/0x004e8450-0x004e8ae6.IMECandidateQueueAndDestructors.md)
- [UID:0001PS][0x0069b458-0x0069b45c.g_pIMEPane](by-memory/0x0069b458-0x0069b45c.g_pIMEPane.md)
- [UID:0001G7][0x00557380-0x00557467.ScreenCursorHelpers](by-memory/0x00557380-0x00557467.ScreenCursorHelpers.md)
- [UID:0000JE][FittingRoom](by-file/FittingRoom.md)

## Changes

- 2026-06-30 B008 implementation callback:
  - Before: score was `87/90`, formal C++ was blank, and the page kept only a review-only C++ sketch because shared Event/Pane/IME declarations were not coordinated.
  - Changed to: score `89/91` and first-draft formal `void IMECandidatePane::ShowCandidateList(const Event *event)` C++ in the reconstruction block.
  - Summary/evidence: current MCP decompile confirms Event `+0x08` candidate deque pointer, Event `+0x0c` selected row, text-width and line-height iteration, screen clamping, active-child fallback through `g_pIMEPane+0xfc`, `visibleCandidates` assignment at `+0xf8`, `selectedCandidateRow +0x10c`, `candidateAnchorY +0x110`, `candidateAnchorX +0x114`, and sole caller `0x004e7d13`.
- 2026-06-06 evidence and parent update:
  - Before: scores were `70/85` and `AUTOGEN_PARENT_UID` was blank despite strong IME ownership evidence.
  - After: scores set to `82/88` and attached to [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md); C++ remains blank under the final-source gate.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x004e7930-0x004e7b6c` boundary, sole caller at `0x004e7d13`, caller-side IME candidate-message forwarding, callee set, visible candidate-range iteration, text measurement, screen and child-pane clamping, candidate-string queue assignment, selected-index update, and invalidation behavior. Final field/helper names remain incomplete, so scores stay below near-final audit levels.
- 2026-06-17 B001 source-quality execution:
  - Before: score was `82/88`, with the method attached to `IMECandidatePane` but still carrying open candidate payload, field-name, helper-owner, producer-side Event, and final-C++ blockers.
  - Changed to: score `87/90`, owner/emitter unchanged [UID:00006F][IMECandidatePane](by-class/IMECandidatePane.md), `RECONSTRUCTABLE:TRUE` unchanged, and formal C++ intentionally blank by accepted do-not-populate reasoning.
  - Summary/evidence: B001 rechecked live IDA, caller xrefs, InputMan producer flow, Event factory `0x004a9e50`, shared helper owners, and generated FittingRoom pollution. The page now records Event type `15`, candidate deque pointer at Event `+0x08`, selected row at Event `+0x0c`, STL deque internals at candidate container `+0x0c/+0x10`, pane-local fields `+0xf8/+0x10c/+0x110/+0x114`, active child/popup pane at `g_pIMEPane+0xfc`, exact next boundary `0x004e7b70`, rejected owner alternatives, and review-only C++ shape.
- 2026-06-21 Rule 26 incorporation of B008 EventMan/factory helper report:
  - Score unchanged at `87/90`.
  - Summary/evidence: [UID:00014C][0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers](by-memory/0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers.md) now carries the consolidated Event type/payload map and standardized `DispatchCandidateListEvent` signature used by this consumer.
