*** UID:00020A | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __cdecl Rgb565AlphaMapMmxBlocks(
    unsigned short* pixels,
    int pixelPitchBytes,
    const unsigned char* alphaBytes,
    int alphaPitchBytes,
    int blockCount4Pixels,
    int rowCount)
{
    while (rowCount-- > 0) {
        for (int pixelIndex = 0; pixelIndex < blockCount4Pixels * 4; ++pixelIndex) {
            const unsigned int pixel = pixels[pixelIndex];
            const unsigned int alpha = alphaBytes[pixelIndex];

            const unsigned int redBlue =
                (alpha * (pixel & 0xf81f)) >> 5;
            const unsigned int green =
                (alpha * (pixel & 0x07e0)) >> 5;

            pixels[pixelIndex] = static_cast<unsigned short>(
                (redBlue & 0xf81f) | (green & 0x07e0));
        }

        pixels = reinterpret_cast<unsigned short*>(
            reinterpret_cast<unsigned char*>(pixels) + pixelPitchBytes);
        alphaBytes += alphaPitchBytes;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms exact logical-void cdecl `0xc1`-byte RGB565 alpha-map block helper, sole `ApplyAlphaMap565` caller at `0x0046043f`, no callees, exact six-argument pointer/pitch/block/row contract, caller four-pixel split plus scalar tail, alpha-byte unpacking, RGB565 mask constants, in-place rows, `emms`, boundary padding, and SoftwareBlend16 ownership; incoming MM1 is unconditionally overwritten and is not source ABI. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00460c90-0x00460d51 RGB565 Alpha Map MMX Blocks

## Status

- Disposition: reconstructable NexusTK render support code.
- Entity kind: free render helper.
- Likely source file: [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) `SoftwareBlend16`.
- Confidence: very strong for exact boundary, sole caller, source-facing signature, constants, row/block behavior, and padding; final source spelling remains open only at the MMX inline-assembly/intrinsic source-form level.
- Rebuild handling: `source-authored`.
- C++ reconstruction: 2026-06-30 B013 populates formal source-level C++. The old family-wide blank-C++ blocker is superseded because the exact behavior is a simple per-pixel alpha-map multiply over four-pixel block loops.

## Address Range

`0x00460c90-0x00460d51`.

The following `0x00460d51-0x00460d60` bytes are alignment padding.

## Behavior

This is the four-pixel MMX block path for RGB565 alpha-map multiplication. The parent `sub_460410` helper computes a `width / 4` block count, calls this routine when that count is positive, advances the RGB565 row pointer by `blockCount * 8` bytes and the alpha-map pointer by `blockCount * 4` bytes, then handles the remaining pixels with the scalar tail.

The block helper receives an RGB565 row pointer, RGB565 row pitch, alpha-byte row pointer, alpha row pitch, block count, and row count. Each inner iteration loads four alpha bytes with `movd`, expands them to four words with `punpcklbw`, loads four RGB565 pixels with `movq`, independently scales/clamps the red, green, and blue components, and writes the packed RGB565 result back in place. Row advancement uses the two supplied pitches, and the helper exits through `emms`.

## Source-Facing Signature And Source-Form Policy

The best source-facing name and prototype for this helper are:

```cpp
static void __cdecl Rgb565AlphaMapMmxBlocks(
    uint16_t* rgb565Pixels,
    int rgb565PitchBytes,
    const uint8_t* alphaBytes,
    int alphaPitchBytes,
    int blockCount4Pixels,
    int rowCount);
```

The helper is in-place RGB565 alpha-map multiplication. `rgb565Pixels` and `alphaBytes` are row pointers, the pitch values are byte strides, and `blockCount4Pixels` is the number of four-pixel MMX groups per row. The exact source-facing prototype is logical `void __cdecl Rgb565AlphaMapMmxBlocks(unsigned short *pixels, int pixelPitchBytes, const unsigned char *alphaBytes, int alphaPitchBytes, int blockCount4Pixels, int rowCount)`; no caller consumes a register result.

Historical B001 state left formal C++ blank while the SoftwareBlend16-wide MMX source form was unresolved. That state was superseded by the B013 behavior-preserving formal body now present above. The `pushaw`/`popaw`, `emms`, qword MMX operations, and mask-qword memory operands remain evidence that original source may have used MSVC x86 inline assembly or an assembly-backed helper; they cap final source-form confidence but no longer justify an empty emitter.

The decompiler-propagated incoming MM1 value is not an argument. The helper starts by computing `pandn mm1, mm1`, making MM1 zero independent of incoming state, and the sole caller does not establish a semantic MM1 input. Keep MM1 out of the source prototype and out of the Surface slot-5 callback ABI.

The sole call chain is source-ready [UID:0002TH][0x004c3cf0-0x004c3e93.SoftwareRenderRgb565AlphaLookupBlitCallback](by-memory/0x004c3cf0-0x004c3e93.SoftwareRenderRgb565AlphaLookupBlitCallback.md) at `0x004c3db4` -> [UID:0004BL][0x00460410-0x004604f4.ApplyAlphaMap565](by-memory/0x00460410-0x004604f4.ApplyAlphaMap565.md) -> this helper at `0x0046043f`. Surface owns callback dispatch/clipping; SoftwareBlend16 retains this stateless packed-pixel primitive.

## Rule 26 Incorporated B001 Research

The B001 source-quality report incorporated into this page validates `Rgb565AlphaMapMmxBlocks` as the best current source-facing direction. The sole modeled caller at `0x0046043f` passes an RGB565 row pointer and pitch, an alpha-byte row pointer and pitch, a four-pixel block count, and a row count. The parent then advances the RGB565 pointer by `blockCount4Pixels * 8` and the alpha pointer by `blockCount4Pixels * 4` before running the scalar alpha-map tail. That interaction identifies this routine as the bulk path for in-place RGB565 alpha-map multiplication.

Ownership was rechecked against render-callback and image-surface alternatives. [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) remains the direct owner because the helper is stateless RGB565 channel math in the SoftwareBlend16 helper island and consumes SoftwareBlend16 read-only qword masks. `Surface`, `GrafPort`, and callback families are consumers or dispatch owners, not the source owner of this primitive. No pane or image-library object state is accessed.

The mask constants are part of the target's source-quality evidence. [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md) records the alpha-map clamp masks referenced at `0x00610ec8` and `0x00610ed0`. B001 suggests source-facing names such as `kRgb565AlphaMapRedBlueClampMaskQword` and `kRgb565AlphaMapGreenClampMaskQword` for future constants work, but this method page does not rename the constants on its own.

Rejected source-form alternatives are now retained here. A portable C++ loop can model the effect, but it would not be a faithful mid-2000s source representation of this optimized helper without a deliberate project-wide choice. Hex-Rays intrinsic-like output is also not source proof. The binary's explicit all-register save/restore, MMX qword operations, and `emms` point to MSVC x86 inline assembly or assembly-backed static helpers.

Generated-output impact: this helper should stay blank in `auto-generated/NexusTK/render/SoftwareBlend16.cpp` until the surrounding MMX block helpers and SoftwareBlend16 constants are emitted in one consistent style. The blank block is therefore a documented source-shape decision, not merely missing work.

## Reconstruction Evidence

| Item | Evidence | Reconstruction impact |
| --- | --- | --- |
| Parent helper | Direct call from `0x0046043f` inside [UID:0000YD][0x0045fa00-0x004604f4.Rgb555565BlitHelpers](by-memory/0x0045fa00-0x004604f4.Rgb555565BlitHelpers.md). | This is the MMX block path used by the RGB565 alpha-map row helper. |
| Caller split | Live IDA disassembly of `sub_460410` shows `0x00460416-0x0046045a` computing/passing the block count and `0x00460480-0x004604c2` handling the remaining scalar pixels. | Reconstruction should model one row helper with a four-pixel MMX block path plus an RGB565 scalar tail. |
| Alpha input | IDA decompilation shows byte unpacking with `punpcklbw` before `pmullw`. | Source reconstruction must keep the parallel byte alpha table model. |
| Color math | IDA decompilation shows RGB565 channel extraction/scaling and clamping masks. | This is RGB565 alpha-map multiplication, not general byte-span add/subtract. |
| Constant dependency | [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md) lists `0x00610ec8` and `0x00610ed0` as masks referenced from `0x00460ca4` and `0x00460cab`. | Mask constants belong with the SoftwareBlend16 source group. |
| Callee set | IDA MCP `callees` for `0x00460c90` is empty; data xrefs from the body are only the two mask qwords at `0x00610ec8` and `0x00610ed0`. | The helper has no hidden project-code calls; the remaining uncertainty is source naming and intrinsic/assembly spelling. |
| Boundary | Function ends at `0x00460d51`; `0x00460d51-0x00460d60` is confirmed `0xcc` alignment in [UID:0000VN][-ignored](by-memory/-ignored.md). | Exact helper range is closed and safe to attach to SoftwareBlend16. |

## Evidence

- 2026-06-05 live IDA MCP `lookup_funcs`/`py_eval` reports function `sub_460C90` at `0x00460c90-0x00460d51`, size `0xc1`, with 68 decoded instructions. The previous modeled helper is `sub_460C10` at `0x00460c10-0x00460c8c`, and the next modeled function is `sub_460DF0` at `0x00460df0-0x00460e79`.
- IDA `callers`/`XrefsTo(0x00460c90)` reports one direct call from `0x0046043f`, inside `sub_460410` / `ApplyAlphaMap565` documented in [UID:0000YD][0x0045fa00-0x004604f4.Rgb555565BlitHelpers](by-memory/0x0045fa00-0x004604f4.Rgb555565BlitHelpers.md). `sub_460410` is itself called from RGB565 alpha/color-LUT callback `sub_4C3CF0` at `0x004c3db4`.
- IDA `callees` for `0x00460c90` is empty. The only external xrefs from the body are `movq mm3, ds:qword_610EC8` at `0x00460ca4` and `movq mm4, ds:qword_610ED0` at `0x00460cab`.
- Live constant audit reports `0x00610ec8 = 0x03e003e003e003e0` and `0x00610ed0 = 0x07e007e007e007e0`, each referenced once from `sub_460C90`.
- Parent disassembly at `0x00460416-0x0046045a` computes `(width + sign_adjust) / 4`, pushes RGB565 pointer, RGB565 pitch, alpha pointer, alpha pitch, block count, and row count to `sub_460C90`, then advances the RGB565 pointer and alpha pointer past the completed block columns. The scalar tail at `0x00460480-0x004604c2` processes one RGB565 pixel and one alpha byte per iteration.
- IDA decompilation/disassembly shows `pandn mm1, mm1` zeroing, `movd` four alpha bytes, `punpcklbw`, three `pmullw` channel paths, clamp masks, packed in-place `movq [esi], mm7` stores, row-pitch advances, `popaw`, and `emms`.
- Byte audit confirms `0x00460c8c-0x00460c90` and `0x00460d51-0x00460d60` are `0xcc` alignment padding; `0x00460d50` is the terminal `retn`.

## Cross-References

- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) SoftwareBlend16.
- [UID:0000YD][0x0045fa00-0x004604f4.Rgb555565BlitHelpers](by-memory/0x0045fa00-0x004604f4.Rgb555565BlitHelpers.md) RGB555/RGB565 Blit Helpers.
- [UID:0002OO][0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants](by-memory/0x00610e38-0x00610ed8.SoftwareBlend16ReadOnlyConstants.md)

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 86 | B001 source-quality recheck resolves exact bounds, sole caller, no-callee status, RGB565 and alpha-row pointer/pitch signature, `blockCount4Pixels` and `rowCount`, caller block/tail split, alpha-byte unpacking, mask constants, in-place packed stores, `emms`, SoftwareBlend16 ownership, and boundary padding. The remaining gap is the final SoftwareBlend16-wide MMX source form. |
| Confidence | 91 | Disassembly, decompilation, direct caller evidence, constant refs, parent SoftwareBlend16 grouping, sibling helper docs, and padding agree. Confidence stays below final-source quality because exact original helper spelling and inline-assembly/intrinsic form remain inferred. |
| Reconstructable | true | This is source-authored NexusTK software render code attached to SoftwareBlend16; 2026-06-30 B013 populates behavior-preserving formal C++ and treats the old MMX-family source-form blocker as superseded. |

## Changes

- 2026-06-02: Raised from `50/85` to `74/86`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md).
  - Added reconstruction evidence for RGB565 alpha-map caller placement, alpha-byte unpacking, channel masks, read-only constants, and alignment-bounded range.
  - Left C++ blank because the final helper signature and inline assembly/intrinsic spelling are not source-final.
- 2026-06-05: Raised from `74/86` to `82/90`.
  - Added live IDA MCP evidence for exact `sub_460C90` bounds, 68 decoded instructions, the sole direct caller at `0x0046043f`, the parent RGB565 callback caller at `0x004c3db4`, empty callee set, caller `width / 4` block split plus scalar tail, row-pitch and alpha-pitch advancement, alpha-byte unpacking, RGB565 mask constants, in-place packed stores, `emms`, and boundary padding.
  - Kept `RECONSTRUCTABLE:TRUE`, parent [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md), and blank final C++ metadata unchanged.
- 2026-06-17 B001 source-quality execution: Raised from `82/90` to `86/91`.
  - Resolved the source-facing `Rgb565AlphaMapMmxBlocks` prototype, row pointer/pitch roles, block-count semantics, `void` return policy, mask ownership, and SoftwareBlend16 ownership route.
  - Kept formal C++ blank because isolated portable or Hex-Rays-style output would not be a defensible original source form before the SoftwareBlend16-wide MMX inline-assembly/intrinsic decision.
  - Rule 26 remediation added the report's parent interaction, ownership alternatives, mask-constant source direction, rejected source forms, and generated-output impact directly to this by-memory page.
