*** UID:0002BK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000N4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void ParseLgInternetSessionLauncher(RegistryConfig *config)
{
    struct LgInternetSessionParameters
    {
        char sessionId[32];
        char sessionKey[32];
        char host[32];
        int port;
    };

    typedef int (WINAPI *WaitForSessionParameterProc)(
        LgInternetSessionParameters *parameters);

    wchar_t commandLine[1024];
    char hostText[128];
    char *tokenContext;
    LgInternetSessionParameters sessionParameters;

    wcscpy_s(commandLine, _countof(commandLine), GetCommandLineW());

    wchar_t *modeText = wcschr(commandLine, L'/');
    if (modeText == NULL)
        AbortWithLauncherMessage(L"\uC774 \uBC84\uC804\uC758 \uBC14\uB78C\uC758 \uB098\uB77C\uB294 Channel i \uC804\uC6A9\uC785\uB2C8\uB2E4.");

    size_t modeLength = wcslen(modeText);
    if (modeLength == 4)
    {
        if (wcsncmp(modeText, L"/lgi", 4) != 0)
            AbortWithLauncherMessage(L"\uC774 \uBC84\uC804\uC758 \uBC14\uB78C\uC758 \uB098\uB77C\uB294 Channel i \uC804\uC6A9\uC785\uB2C8\uB2E4.");
    }
    else
    {
        if (modeLength <= 4 || wcsncmp(modeText, L"/lgi ", 5) != 0)
            AbortWithLauncherMessage(L"\uC774 \uBC84\uC804\uC758 \uBC14\uB78C\uC758 \uB098\uB77C\uB294 Channel i \uC804\uC6A9\uC785\uB2C8\uB2E4.");
    }

    HMODULE channelModule = g_pfnLoadLibraryW(L"chigamec.dll");
    WaitForSessionParameterProc waitForSessionParameter =
        reinterpret_cast<WaitForSessionParameterProc>(
            GetProcAddress(channelModule, "WaitForSessionParameter"));

    int sessionResult = waitForSessionParameter(&sessionParameters);
    if (sessionResult != 0)
        AbortWithLauncherMessageFormat(L"Channel i DLL Error : code %d - \uC811\uC18D\uC911\uC778 \uCC44\uB110\uC544\uC774 ID\uC778\uC9C0 \uD655\uC778\uD558\uC2ED\uC2DC\uC624.", sessionResult);

    size_t hostLength = strlen(sessionParameters.host);
    if (hostLength >= sizeof(hostText))
        ___report_rangecheckfailure();

    memcpy(hostText, sessionParameters.host, hostLength);
    hostText[hostLength] = '\0';

    MultiByteToWideChar(CP_ACP,
                        0,
                        hostText,
                        static_cast<int>(hostLength),
                        config->m_startupHostName,
                        128);

    if (isalpha(static_cast<unsigned char>(hostText[0])))
    {
        hostent *host = gethostbyname(hostText);
        if (host == NULL)
            AbortWithLauncherHostMessage("\xC0\xCC\x20\xB9\xF6\xC0\xFC\xC0\xC7\x20\xB9\xD9\xB6\xF7" "\xC0\xC7\x20\xB3\xAA\xB6\xF3\xB4\xC2\x20\x4C\x47\x20\x49" "\x6E\x74\x65\x72\x6E\x65\x74\x20\x43\x68\x61\x6E\x6E\x65" "\x6C\x69\x20\xC0\xFC\xBF\xEB\xC0\xD4\xB4\xCF\xB4\xD9\x2E" "\x28\x20\xC0\xD4\xB7\xC2\xC1\xA4\xBA\xB8\x20\x22\x25\x73" "\x22\x20\xBF\xA1\x20\xBF\xC0\xB7\xF9\xB0\xA1\x20\xC0\xD6" "\xBD\xC0\xB4\xCF\xB4\xD9\x2E", hostText);

        strcpy_s(reinterpret_cast<char *>(config->m_startupAddress),
                 16,
                 host->h_addr_list[0]);
    }
    else
    {
        tokenContext = NULL;
        char *part = strtok_s(hostText, ".", &tokenContext);
        if (part == NULL)
            return;

        config->m_startupAddress[0] =
            static_cast<unsigned char>(strtoul(part, NULL, 10));

        for (int i = 1; i < 4; ++i)
        {
            part = strtok_s(NULL, ".", &tokenContext);
            if (part == NULL)
                return;

            config->m_startupAddress[i] =
                static_cast<unsigned char>(strtoul(part, NULL, 10));
        }
    }

    config->m_startupPort = sessionParameters.port;
    config->m_startupConnectionMode = 5;
    strcpy_s(config->m_lgInternetSessionId, 64, sessionParameters.sessionId);
    strcpy_s(config->m_lgInternetSessionKey, 64, sessionParameters.sessionKey);
    config->m_startupReady = 1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: RegistryConfig file-static `ParseLgInternetSessionLauncher` helper; live IDA verifies `/lgi` matching, `chigamec.dll`/`WaitForSessionParameter`, session block layout, host/IP handling, LG session string and port/state writes, Channel i failure paths, exact padding, no modeled function, and no direct entry/pointer xrefs. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00490200-0x0049050a LgInternetSessionLauncherParser

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: function
- Parent range: [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md)
- Likely source module: [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md), or a config-adjacent launcher/bootstrap helper module
- Rebuild handling: source-authored
- Confidence: very strong for registry/bootstrap ownership, raw range placement, `/lgi` launcher recognition, dynamic session API resolution, session block field use, endpoint writes, and failure paths; capped below final-audit reconstruction because IDA still does not model `0x00490200` as a function object, no dispatcher route is recovered, and exact vendor field names/localized text remain inferred.

## Rebuild Handling

- Reconstructability: true for source-authored launcher/session parser logic under the registry/config startup parser family.
- Parent handling: attach directly to [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md), which is `88/82` and owns the command-line/session parser island. Keep [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) as the aggregate evidence page, but do not use it as parent until it is raised above the attachment threshold.
- C++ handling: emit first-draft file-static C++ as `ParseLgInternetSessionLauncher(RegistryConfig *config)`. The raw exported-start/no-xref status remains a confidence cap, but it no longer blocks child-level C++ because the local helper name, session block layout, dynamic API dependency, startup-state writes, failure paths, and sibling parser naming pattern are now documented.
- Boundary handling: document the range as raw exported code ending before the next exported start at `0x00490510`, not as an IDA-modeled function.

## Address Range

`0x00490200-0x0049050a`, ending at the `align 10h` byte before the raw successor start at `0x00490510`.

Live IDA does not model `0x00490200`, `0x0049050a`, `0x00490510`, `0x00490800`, or `0x00490810` as function objects. The previous modeled function is `sub_48FCA0` at `0x0048fca0-0x004901f0`, and the next modeled function is `sub_490B10` at `0x00490b10-0x004910c0`. The preceding boundary `0x004901f0-0x00490200` is sixteen `0xcc` bytes, and `0x0049050a-0x00490510` is six `0xcc` bytes.

## Behavior

This helper recognizes an `/lgi` LG Internet/Channel i launcher startup mode. It copies the process command line into a local wide buffer, finds the first slash, then accepts either the exact UTF-16 `/lgi` token at `0x00616ec4` or the `/lgi ` prefix at `0x00616ed0`.

The accepted path calls the wide loader slot at `dword_69BE30` with the UTF-16 `chigamec.dll` string at `0x00616f1c`, resolves the ANSI export string `WaitForSessionParameter` at `0x00616f38` through `GetProcAddress`, and invokes the returned function pointer with a stack session block at `[ebp-0xce8]`. A nonzero return code formats the Channel i DLL error string at `0x00616f50` before aborting.

On a zero session-API return, the helper reads the session block fields as host, port, and two small credential/session strings. It copies the host bytes from `[ebp-0xca8]` into a stack buffer, converts them to wide text at `param_1 + 0x2915c4` with `MultiByteToWideChar`, then either resolves alphabetic host names with `gethostbyname` or parses dotted numeric addresses with `_strtok_s`.

The success path writes the resulting address bytes to `param_1 + 0x2915b4`, stores the session port from `[ebp-0xc88]` to `param_1 + 0x2916c4`, copies two session strings from `[ebp-0xce8]` and `[ebp-0xcc8]` to `param_1 + 0x28d9e0` and `param_1 + 0x28da20`, sets `param_1 + 0x2916ca` to `1`, and forces the state byte at `param_1 + 0x28de28` to `5`.

Invalid command-line data, DNS failure, invalid IP tokenization, and session DLL errors abort through localized Channel i/LG Internet dialogs and the common `PostQuitMessage`/`__loaddll` termination path.

## Source Shape And First-Draft C++

Accepted source-facing helper: `static void ParseLgInternetSessionLauncher(RegistryConfig *config)` in `NexusTK/config/RegistryConfig.cpp`.

The helper remains file-static/config-adjacent instead of a `RegistryConfig::` method. The incoming `ecx` object is a real RegistryConfig/startup-state pointer, but current IDA still provides no direct caller, dispatcher selector, vtable route, or stored start-address pointer for this raw island. Sibling parser children under [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) use the same file-static helper pattern when their local behavior is resolved but the whole parser dispatcher remains unrecovered.

Rejected source-name alternatives:

- `ParseLgInternetSessionParameters` describes the external payload but undernames the command-line launcher mode.
- `ParseChannelISessionLauncher` matches the Channel i strings and DLL context but is weaker than the accepted `/lgi`-anchored naming.
- `ParseLgiCommandLine` is too low-level and less consistent with the accepted parser-helper names.
- Historical `RegistryConfig::ParseLgInternetSessionLauncher` member rehome is rejected by the completed whole-island check; the accepted source form remains private file-static `ParseLgInternetSessionLauncher(RegistryConfig *config)`.

The local session block is best represented as:

```cpp
struct LgInternetSessionParameters
{
    char sessionId[32];
    char sessionKey[32];
    char host[32];
    int port;
};
```

`sessionId` and `sessionKey` are inferred source-facing names. The binary proves two 32-byte narrow session strings copied into 64-byte RegistryConfig startup scratch fields, but it does not recover the vendor's original member names. The resolved export type is inferred as `WaitForSessionParameterProc`; the call site's lack of caller stack cleanup supports a `WINAPI` / `__stdcall` external API.

The formal `RECONSTRUCTION_CPP CODE` block intentionally preserves the binary's unusual details: `g_pfnLoadLibraryW(L"chigamec.dll")` through the `0x0069be30` dispatch slot, immediate `GetProcAddress` use without null checks, `MultiByteToWideChar` with the measured host length instead of `-1`, the DNS success `_strcpy_s` copy from `host->h_addr_list[0]` into a 16-byte destination, early returns on incomplete dotted-IPv4 tokens, and `___report_rangecheckfailure()` for host strings of length `0x80` or greater.

## Parser Island Context

- The parent island [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) lists `0x00490200` as one of twelve exported/parser starts between `RegistryConfig` defaults and registry persistence.
- Its predecessor [UID:0002BJ][0x0048fca0-0x004901f0.ThrunetStartupAuthFileParser](by-memory/0x0048fca0-0x004901f0.ThrunetStartupAuthFileParser.md) is the previous IDA-modeled launcher/auth parser.
- Its successor [UID:0002BL][0x00490510-0x00490809.LauncherHostPortPayloadParser](by-memory/0x00490510-0x00490809.LauncherHostPortPayloadParser.md) is the next raw launcher parser and is already attached to [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md).
- [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md) explicitly owns the command-line/session parser helper family as part of registry-backed startup normalization.

## IDA MCP Evidence

- Live IDA MCP session against `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- IDA has no modeled function at `0x00490200`, `0x0049050a`, `0x00490510`, `0x00490800`, or `0x00490810`; previous modeled function is `sub_48FCA0` at `0x0048fca0-0x004901f0`, and next modeled function is `sub_490B10` at `0x00490b10-0x004910c0`.
- Boundary bytes are sixteen `0xcc` bytes at `0x004901f0-0x00490200` and six `0xcc` bytes at `0x0049050a-0x00490510`.
- Live xref and loaded-segment pointer scans found zero xrefs/pointer hits to `0x00490200` and zero xrefs/pointer hits to the successor raw start `0x00490510`.
- The parser prologue reserves `0xcec` stack bytes, saves the security cookie, stores incoming `ecx` in `edi`, calls `GetCommandLineW`, and copies the command line into a `0x400`-wide-character local buffer with `_wcscpy_s`.
- Command-line recognition: `0x00490238` finds the slash marker, `0x00490269/0x0049026f` compares with UTF-16 `/lgi`, and `0x0049029a/0x004902a0` accepts the `/lgi ` prefix.
- Dynamic session API path: `0x004902b0` pushes the UTF-16 `chigamec.dll` string, `0x004902b5` calls the wide loader slot `dword_69BE30`, `0x004902bb` pushes the ANSI `WaitForSessionParameter` string, `0x004902c1` calls `GetProcAddress`, and `0x004902ce` invokes the resolved function pointer with the local session block.
- Session success path: `0x004902d8-0x00490335` copies the host byte string from the session block and converts it into `edi + 0x2915c4` with `MultiByteToWideChar`; `0x00490343` checks whether the first host byte is alphabetic.
- Host handling: alphabetic hosts call `gethostbyname` at `0x00490356` and copy the returned address bytes to `edi + 0x2915b4`; numeric hosts split on `.` with `_strtok_s` at `0x0049039c`/`0x004903ce` and write four parsed bytes to `edi + 0x2915b4`.
- Success-side writes: `0x004903f6` stores the session port to `edi + 0x2916c4`, `0x00490409` writes byte `5` to `edi + 0x28de28`, `0x00490413` copies a session string to `edi + 0x28d9e0`, `0x00490428` copies another to `edi + 0x28da20`, and `0x00490430` writes word `1` to `edi + 0x2916ca`.
- Failure paths: invalid `/lgi` data pushes the Channel i-only message at `0x00616edc`; nonzero session API return formats the Channel i DLL error string at `0x00616f50`; host lookup failure branches to the `NWind` ANSI `MessageBoxA` path; all user-visible failures end in `PostQuitMessage` and `__loaddll`.
- [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) covers the surrounding parser island, and [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md) covers the RegistryConfig ownership context.

## 2026-06-15 Current Verification

- Live IDA MCP session `a003_objectlist_dispatch_20260615` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` returned `status: ok`, imagebase `0x400000`, and Hex-Rays ready.
- `lookup_funcs` still reports `0x00490200`, `0x0049050a`, and `0x00490510` as not IDA-modeled functions. The neighboring modeled anchors remain `sub_48FCA0` at `0x0048fca0-0x004901f0` and `sub_490B10` at `0x00490b10-0x004910c0`.
- `xrefs_to` reports zero cross-references to the raw entry `0x00490200` and zero cross-references to the raw successor start `0x00490510`, preserving the raw launcher-dispatch caveat.
- Current data xrefs confirm the `/lgi` mode operands and dynamic session-loader strings: `/lgi` at `0x00616ec4` from `0x00490269`, `/lgi ` at `0x00616ed0` from `0x0049029a`, `chigamec.dll` at `0x00616f1c` from `0x004902b0`, `WaitForSessionParameter` at `0x00616f38` from `0x004902bb`, and the Channel i DLL error format at `0x00616f50` from `0x00490462`.
- `make_signature_for_range 0x00490200-0x0049050a` returned `unique: true`; the signature starts with the expected stack-frame/security-cookie prologue `55 8B EC 81 EC EC 0C 00 00 A1 24 2F 67 00`.

## 2026-06-27 B009 Source-Quality Verification

- Active MCP session `80de0a67` was checked through `idb_list`, `server_health`, tool schema inspection, function lookups, xref/pointer-route checks, byte/signature checks, string reads, and import queries. No IDA database edits were made.
- The accepted helper remains inside the RegistryConfig launcher/session bootstrap bucket. `ParseLgInternetSessionLauncher(RegistryConfig *config)` is now source-quality enough for first-draft child C++ because the local command mode, dynamic DLL/export route, session block, host parser, field writes, and failure paths are bounded by instruction-level evidence.
- The wide DLL operand is the full UTF-16 `chigamec.dll` string at `0x00616f1c`. IDA's `0x00616f24` string label points into the middle of that literal (`amec.dll`) and must not be used as the actual pushed address.
- The `0x0069be30` wide API dispatch slot is `g_pfnLoadLibraryW`; the parser calls it at `0x004902b5` to load `chigamec.dll` before resolving `WaitForSessionParameter`.
- The helper writes target-specific LG Internet overlays `m_lgInternetSessionId[64]` and `m_lgInternetSessionKey[64]` at `+0x28d9e0` and `+0x28da20`, sharing the same startup scratch area used differently by Thrunet/COM-backed launcher siblings.
- Remaining limits are explicit score caps rather than C++ blockers: no direct start xref or stored VA/RVA pointer route, no IDA-modeled function at `0x00490200`, inferred external vendor names, inferred exact export prototype, summarized rather than fully decoded localized Korean/Channel i message text, and no recovered whole-island dispatcher.

Do not rehome this one helper as a class method in isolation. If a future dispatcher/caller route is recovered, re-evaluate all exact parser children under [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) together so the source shape stays consistent.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Current IDA records exact raw boundaries, padding, no-function status, no xref/pointer hits, `/lgi` recognition, dynamic `chigamec.dll`/`WaitForSessionParameter` resolution, session block field use, host resolution and dotted-IP parsing, success-side state writes, failure paths, unique full-range byte signature, accepted source-facing helper name, inferred session block type, target-specific field overlays, and formal first-draft C++. It remains below final-source completion because the whole-island caller selector and original vendor field/prototype spellings are not recovered. |
| Confidence | 90 | RegistryConfig ownership and LG Internet/Channel i parser behavior are supported by current instruction-level IDA evidence, concrete string operands, exact state writes, surrounding parser-island placement, absence of direct entry/pointer references, `0x0069be30` `LoadLibraryW` dispatch evidence, and a unique range signature. Confidence remains capped because this is still an unmodeled raw start with no known inbound dispatcher edge and partially inferred external/session names. |

## Cross-References

- [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md)
- [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md)
- [UID:0000BW][RegistryConfig](by-class/RegistryConfig.md)
- [UID:0002BJ][0x0048fca0-0x004901f0.ThrunetStartupAuthFileParser](by-memory/0x0048fca0-0x004901f0.ThrunetStartupAuthFileParser.md)
- [UID:0002BL][0x00490510-0x00490809.LauncherHostPortPayloadParser](by-memory/0x00490510-0x00490809.LauncherHostPortPayloadParser.md)

## 2026-08-15 Whole-Island Source-Placement Closure

- Final source placement for this child is private file-static `RegistryConfig.cpp` helper `static void ParseLgInternetSessionLauncher(RegistryConfig *config)`; it is not a `RegistryConfig::` member and has no header declaration.
- This entry is a raw start. Its bounded prologue preserves incoming `ECX` as the Config/RegistryConfig object in `EDI`. That proves the explicit object parameter used by the formal C++ but does not prove member-method syntax.
- Whole-island checks cover all twelve parser starts: every entry has zero external inbound entry xrefs; all 24 little-endian VA/RVA start-address searches return zero matches; only `0x0048fca0`, `0x00490b10`, and `0x004918e0` are modeled functions; and none appears in the five-slot RegistryConfig vtable.
- The common evidence fixes one coherent family of private file-static helpers. The stripped dispatcher/call mechanism and original private symbols remain score and exact-spelling caps only; they are not a source-placement or code-emission blocker.
- Accepted Config storage names are `m_startupAddress`, `m_startupHostName`, union `m_startupPort`/`m_launcherAuthenticationToken`, `m_startupServerId`, `m_startupReady`, `m_startupConnectionMode`, `m_startupLaunchCommand`, `m_startupLauncherId`, `m_thrunetAuthenticationToken`, `m_lgInternetSessionId`, `m_lgInternetSessionKey`, `m_launcherPayload`, `m_launcherAuthenticationObject`, `m_kornetWorldPayload`, and `m_kornetWorldPayloadData`.
- Any earlier prose on this page that called the file-static signature provisional, deferred source placement, or proposed a later class-method rehome is retained only as superseded historical reasoning. The current placement above is final for this reconstruction pass.

## Changes

- 2026-06-27 B009 source-quality implementation:
  - Before: the page was `85/88`, `RECONSTRUCTION_CPP` was blank, and source notes still treated raw-start status plus unresolved external wrapper names as final-C++ blockers.
  - After: raised to `88/90`, added formal file-static `ParseLgInternetSessionLauncher(RegistryConfig *config)` C++ under [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md), and preserved raw-start/no-dispatch caveats as score caps rather than code blockers.
  - Summary/evidence: active MCP session `80de0a67` confirms exact `0x00490200-0x0049050a` boundaries, `0xcc` padding, no modeled function, no direct xrefs or stored VA/RVA pointer route, `/lgi` and `/lgi ` checks, full `chigamec.dll` string at `0x00616f1c`, `WaitForSessionParameter`, `0x0069be30` `g_pfnLoadLibraryW`, session block offsets, host/IP paths, success writes to `+0x2915b4/+0x2915c4/+0x2916c4/+0x28de28/+0x28d9e0/+0x28da20/+0x2916ca`, Channel i/DNS failure paths, `___report_rangecheckfailure` on host overflow, and a unique full-range signature.
- 2026-06-04:
  - Before: the page was `72/80`, included stale non-live range evidence, and summarized the parser mostly by string/API inventory.
  - After: raised to `80/86`, replaced stale non-live evidence with live IDA MCP evidence, documented exact boundaries/padding/no-xref results, `/lgi` matching, `chigamec.dll` loading, `WaitForSessionParameter` resolution/invocation, session block field consumption, host/IP handling, success-side stores, and failure paths.
  - Summary/evidence: live IDA session against MD5 `4247e04e20b65d6414c7238aa8ff5515` verifies the raw `0x00490200-0x0049050a` parser body, `/lgi` strings, `chigamec.dll`, `WaitForSessionParameter`, session host/port/string handling, `+0x2915b4`, `+0x2915c4`, `+0x2916c4`, `+0x2916ca`, `+0x28d9e0`, `+0x28da20`, and `+0x28de28` writes, Channel i/LG Internet failure paths, no modeled function at the raw start, and no direct entry/pointer xrefs. Final C++ remains blank because the score is below the `95/95` bar.
- 2026-06-15:
  - Before: the page was `80/86` and lacked a current-session boundary/signature/xref refresh.
  - After: raised to `85/88` with no owner/emitter/routing change.
  - Summary/evidence: live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirmed the raw unmodeled entry and successor status, zero entry/successor xrefs, exact `/lgi` and session-loader string operand xrefs, and a unique full-range byte signature. Final C++ remains blank because source-quality dispatcher, session wrapper, and field names are still unresolved.
- 2026-06-02:
  - Before: the page was reconstructable in prose but left unmarked, unparented, and still low-scored relative to neighboring parser pages.
  - After: raised to `72/80`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md).
  - Summary/evidence: registry-file ownership at `88/82`, parser-island context [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md), neighboring parser boundaries, `WaitForSessionParameter` and Channel i string evidence, and live IDA disassembly/call mix support source reconstruction while retaining the raw-start caveat.

## B001 UID0003FV Accepted Callback Synchronization - 2026-07-20

- Final source-quality state is `90/92`; owner/emitter, reconstructable route, position, nesting, range, child relations, and unrelated history remain unchanged.
- This page is the accepted Channel i / LG Internet session launcher parser. The /lgi selectors, chigamec.dll name, WaitForSessionParameter export, exact DLL and host errors, and NWind caption retain their observed wide, ANSI, or CP949 encodings at direct use sites. Callback resolution, ACP host conversion, dotted-IP/DNS alternatives, session payload, port and state writes remain behavior-identical.
- Public RegistryConfig inheritance from the complete Config declaration makes every startup-field access source-legal. The target physical range remains ownerless pooled output; it supplies no cross-translation-unit static declaration and no duplicate literal/GUID array.
- Current formal C++ is the complete destination-specific source body. Earlier blank, comment-only, inferred-symbolic, or unresolved-name conclusions are historical and superseded; raw-entry reachability and original lexical-name uncertainty remain documented confidence caps rather than source blockers.
