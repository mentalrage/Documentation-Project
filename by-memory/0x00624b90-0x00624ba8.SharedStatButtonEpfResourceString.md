*** UID:0003F4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000NL,0000P0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B003 advanced-scan repair reclassified the shared `STATBUT.EPF` literal from non-reconstructable/non-emitting to `CANONICAL_OWNER:NONE` with `EMITTER_UIDS:0000NL,0000P0`; live IDA confirms exact UTF-16LE bytes, ten direct xrefs to `aStatbutEpf` (eight from SelfLookPane2 `sub_570FC0`, two from UserLookPane `sub_5A0640`), no interior target refs, and separate successor label-string refs at `0x00624ba8`/`0x00624bbc`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00624b90-0x00624ba8 Shared STATBUT.EPF Resource String

## Range

- Address range: `0x00624b90-0x00624ba8`.
- Size: `0x18` / 24 bytes. Verified with `int_convert.py`.
- Filename label: `SharedStatButtonEpfResourceString`.
- Entity kind: source-declared/generated-binary pooled UTF-16 resource filename literal.
- Disposition: no canonical owner; emit through the proven source-use routes [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) and [UID:0000P0][UserLookPane](by-file/UserLookPane.md). Split from [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md).

## Summary

This child covers the physical UTF-16LE `STATBUT.EPF` string. It is shared by SelfLookPane2 and UserLookPane consumers, so it should not be assigned to one of those source files solely by physical address. Under the current owner/emitter split, this is reconstructable source-use data with `CANONICAL_OWNER:NONE` and multiple emitters, not a non-emitting coverage-only cell.

## IDA Evidence

Current live IDA MCP recheck against session `b001_0003gy` found the exact target bytes:

```text
0x00624b90: 53 00 54 00 41 00 54 00 42 00 55 00 54 00 2e 00 45 00 50 00 46 00 00 00
```

IDA decodes the start as UTF-16LE `STATBUT.EPF`. The target is not a function. The only UTF-16LE `STATBUT.EPF` byte-pattern hit is this range at `0x00624b90`; nearby `STATBUTS.EPF` at `0x00624820` and `STAT.EPF` at `0x00630ee4` are separate strings.

Direct xrefs to the target start:

| Xref | Function | Source-use route |
| --- | --- | --- |
| `0x00571579` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x005715cc` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x00571659` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x005716d6` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x0057175e` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x0057177b` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x00571b62` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x00571bb5` | `sub_570FC0` | SelfLookPane2 stat-button rendering/setup. |
| `0x005a0c73` | `sub_5A0640` | UserLookPane stat-button rendering/setup. |
| `0x005a0ce1` | `sub_5A0640` | UserLookPane stat-button rendering/setup. |

Scoped IDA listing search renders each target xref as `push offset aStatbutEpf ; "STATBUT.EPF"`. Hex-Rays decompilation shows these uses flowing to the resource-frame helper:

```text
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", ..., (int)v51);
sub_5A0640: sub_4D02F0(v2, L"STATBUT.EPF", ..., (int)v124);
```

Negative xref evidence:

- No xrefs target interior addresses `0x00624b94`, `0x00624b98`, `0x00624b9c`, `0x00624ba0`, or `0x00624ba4`.
- Absolute-immediate byte scan for `0x00624b90` found exactly the ten xref operands above.
- Absolute-immediate byte scans for target interior addresses found no matches.
- The successor child starts at `0x00624ba8` and `0x00624bbc` have SelfLookPane2-only refs from `sub_570FC0`, confirming the current child boundary before [UID:0003F5][0x00624ba8-0x00624bd0.SelfLookPane2StatButtonLabelStrings](by-memory/0x00624ba8-0x00624bd0.SelfLookPane2StatButtonLabelStrings.md).

The later B001 live no-owner pass against IDA session `a001_goal2_class_batch` rechecked the physical literal and the source-use routes without relying on earlier B002/B003 reports as authority. It confirmed module `NexusTK.exe`, image base `0x400000`, Hex-Rays readiness, and one unique UTF-16LE range signature:

| Search / probe | Result |
| --- | --- |
| UTF-16LE `STATBUT.EPF\0` | One hit at `0x00624b90`. |
| Little-endian VA bytes `90 4b 62 00` | Ten operand hits, matching the ten direct `push offset aStatbutEpf` instructions. |
| Instruction bytes `68 90 4b 62 00` | Ten direct `push 0x00624b90` hits. |
| Interior VAs `0x00624b94`, `0x00624b98`, `0x00624b9c`, `0x00624ba0`, `0x00624ba4` | Zero hits. |
| Target RVA bytes `90 4b 22 00` | Zero hits. |
| ASCII `STATBUT.EPF\0` | Zero hits. |
| UTF-16LE `STATBUTS.EPF\0` | One separate hit at `0x00624820`. |
| UTF-16LE `STAT.EPF\0` | One separate hit at `0x00630ee4`. |

Representative instruction evidence keeps the uses as source literals flowing into the resource-frame helper rather than as a table/global owner:

```text
0x00571579  push offset aStatbutEpf ; "STATBUT.EPF"
0x0057157e  call sub_4D02F0
0x005715cc  push offset aStatbutEpf ; "STATBUT.EPF"
0x005715d1  call sub_4D02F0
0x005a0c73  push offset aStatbutEpf ; "STATBUT.EPF"
0x005a0c78  call sub_4D02F0
0x005a0ce1  push offset aStatbutEpf ; "STATBUT.EPF"
0x005a0ce6  call sub_4D02F0
```

Filtered Hex-Rays evidence shows SelfLookPane2 using the literal for the stat-button resource frame with dynamic and fixed frame indices, and UserLookPane using the same physical literal from its remote-user rendering path:

```text
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 5 * *(_DWORD *)(this + 3464), (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 5 * *(_DWORD *)(this + 3468) + 1, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 7, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 2, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 8, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 3, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 9, (int)v51);
sub_570FC0: sub_4D02F0(v1, L"STATBUT.EPF", 4, (int)v51);
sub_5A0640: sub_4D02F0(v2, L"STATBUT.EPF", 8, (int)v124);
sub_5A0640: sub_4D02F0(v2, L"STATBUT.EPF", 3, (int)v124);
sub_5A0640: sub_4D02F0(v2, L"STATBUT.EPF", 7, (int)v124);
sub_5A0640: sub_4D02F0(v2, L"STATBUT.EPF", 2, (int)v124);
```

`analyze_component` and backward `trace_data_flow` from `0x00624b90` reached the same component set: `sub_570FC0` and `sub_5A0640` both call `sub_4D02F0`, and the shared global `aStatbutEpf` is accessed by exactly those two functions in the checked component set. This supports source-use emission from both files but still does not prove a single common declaration owner.

## Rule 26 Claim Details

B001-051 originally recommended a parent-blank non-emitting pooled-literal child because `STATBUT.EPF` has eight SelfLookPane2 refs and two UserLookPane refs. The later B003 advanced scan retained the no-canonical-owner conclusion but refined reconstruction policy: this source-use literal must be available through both proven use routes, so this page is reconstructable with `EMITTER_UIDS:0000NL,0000P0` while still having `CANONICAL_OWNER:NONE`.

Rejected routes remain unchanged: neither SelfLookPane nor UserLookPane can be the sole canonical owner of the physical literal, and no broad shared resource-string owner is proven by table, debug path, relocation pattern, or by-resource declaration evidence. The parent [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md) page remains a non-emitting mixed inventory rather than an owner/emitter for this child. `sub_4D02F0` is a resource-frame helper called after the literal is pushed; it explains the filename use, but it is not a declaration owner. The final C++ block stays blank because the literal should appear inside the surrounding SelfLookPane/UserLookPane functions, not as a standalone global declaration from this page.

## 2026-06-22 B002 Current Goal 2 Report Audit

Agent-B002's current Goal 2 pass rechecked this target as an active no-owner memory-sweep item and recommended no metadata, split, merge, coverage, generated-report, or IDA-name change. The report's current-state claim is accepted: [UID:0003F4] remains `COMPLETION:89`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000NL,0000P0`, blank emitter position, and blank C++.

The B002 pass explicitly treated `by-structure.md` and `inference_research.md` as controlling guidance. The important inference is that a pooled string literal may have multiple proven source-use emitters without a single canonical declaration owner. The report rejected creating a standalone shared constants owner only to avoid `NONE`, rejected choosing the consumer with the most xrefs when another independent consumer materially uses the same physical item, and rejected treating the containing mixed `.rdata` inventory as a source declaration owner.

The report's live IDA and raw PE claims are incorporated in this page's evidence tables:

| Claim | Incorporated disposition |
| --- | --- |
| Target range/size | Exact `0x00624b90-0x00624ba8`, `0x18` bytes, UTF-16LE `STATBUT.EPF` plus terminator. |
| Function context | `0x00570fc0` / `sub_570FC0` is the SelfLookPane2 source-use function; `0x005a0640` / `sub_5A0640` is the UserLookPane source-use function; target and sibling strings are data, not functions. |
| Direct xrefs | Ten target-start refs are preserved: eight SelfLookPane2 refs at `0x00571579`, `0x005715cc`, `0x00571659`, `0x005716d6`, `0x0057175e`, `0x0057177b`, `0x00571b62`, `0x00571bb5`, and two UserLookPane refs at `0x005a0c73`, `0x005a0ce1`. |
| Instruction/decompiler shape | Each target ref is a `push offset aStatbutEpf ; "STATBUT.EPF"` feeding `sub_4D02F0`; decompiler evidence shows SelfLookPane2 frame indices derived from `this + 3464` / `this + 3468` plus fixed frames `7`, `2`, `8`, `3`, `9`, and `4`, while UserLookPane renders equivalent branch-selected stat-button frames. |
| Boundary evidence | Interior addresses `0x00624b94`, `0x00624b98`, `0x00624b9c`, `0x00624ba0`, and `0x00624ba4` have no IDA or raw absolute refs, so no interior split is justified. Predecessor `0x00624b74` is a SelfLookPane2-local `SELFLOOK.PAL`/`LOOK.PAL` fragment and successors `0x00624ba8`/`0x00624bbc` are SelfLookPane2-only stat-button label strings. |
| Raw PE evidence | Target maps to `.rdata` file offset `0x223590`; raw bytes match the IDA bytes; UTF-16LE `STATBUT.EPF\0`, `STATBUTS.EPF\0`, and `STAT.EPF\0` each occur once at their separate addresses; ten little-endian absolute target-VA operand hits match the ten IDA xrefs; target RVA-form and interior absolute scans are negative. |
| Generated report interpretation | Generated no-owner/emitter rows correctly show `NONE` with emitters `0000NL,0000P0`. The generated output path currently displaying `SelfLookPane.cpp` is an output-routing artifact, not proof that SelfLookPane is the canonical owner. |
| Report queue/source context | The pass was run from the 2026-06-14 no-owner memory sweep context and compared the current target, generated rows, manual coverage row, `SelfLookPane`, `UserLookPane`, `LegendPane`, the containing [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md) split inventory, predecessor/successor children, `SelfLookPane2`, `UserLookPaneAndProfilePanes`, source-tree placement, live IDA MCP, and raw executable scans. |

The B002 ownership ranking is preserved as follows:

- Accepted: no canonical owner with emitters [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) and [UID:0000P0][UserLookPane](by-file/UserLookPane.md), because both roots have proven direct source-use refs and no single shared declaration owner is proven.
- Rejected: [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) `SelfLookPane` as sole canonical owner because the two UserLookPane refs would be overclaimed.
- Rejected: [UID:0000P0][UserLookPane](by-file/UserLookPane.md) `UserLookPane` as sole canonical owner because the eight SelfLookPane2 refs would be overclaimed.
- Rejected: [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md) `LookGroupCollectionResourceStringData` as owner/emitter because it is a non-emitting mixed split inventory, not a source file or declaration.
- Rejected: [UID:0000KM][LegendPane](by-file/LegendPane.md) `LegendPane` or a broader look/status shared source because the target's direct refs are in SelfLookPane2 and UserLookPane methods, not `LegendPane` construction or vtable data.
- Rejected: a new by-resource/global/shared `STATBUT.EPF` owner because no resource page, global object, constant table, relocation pattern, debug/source path, or decompiler evidence proves a real shared source declaration.

The B002 split/merge/reclassification conclusions are also preserved: do not split the exact UTF-16 literal; do not merge it into SelfLookPane2-local predecessor/successor strings or back into [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md); do not reclassify it as a function, vtable, pointer table, broad container, or non-reconstructable item; do not perform IDA-safe rename repair because `SharedStatButtonEpfResourceString` describes the current evidence; and do not enter standalone C++ here because the likely final source expression is use-site `L"STATBUT.EPF"` inside surrounding SelfLookPane/UserLookPane methods.

## 2026-06-22 B002 No-Owner / Multi-Emitter Recheck Audit

Agent-B002's separate current no-owner/multi-emitter recheck is compatible with and largely duplicated by the current Goal 2 audit above. The recheck was triggered from the generated no-owner/multiple-emitter row in `auto-generated/-ag-memory-coverage.md`, not from a request to raise the score, and it verified that the generated row's `NONE` owner plus `0000NL,0000P0` emitters is correct. The generated source path rooted at `SelfLookPane.cpp` is retained only as output routing; it is not a canonical ownership claim.

Additional report-specific claims from this recheck are accounted for here:

- IDA command coverage included `server_health`, `lookup_funcs`, `get_bytes`, `get_string`, `xrefs_to`, `xref_query`, `find_bytes`, `decompile`, and focused disassembly snippets. Those checks agree with the stored evidence: `0x00624b90`, `0x00624ba8`, and `0x00624bbc` are data labels, not functions; `get_string 0x00624b90` returns `STATBUT.EPF`; and the source-use functions are `sub_570FC0` size `0xc45` and `sub_5A0640` size `0xf3d`.
- `find_bytes` found the UTF-16LE `STATBUT.EPF` pattern only at `0x00624b90`, `STATBUTS.EPF` only at `0x00624820`, and `STAT.EPF` only at `0x00630ee4`; the little-endian immediate pattern `90 4B 62 00` has exactly the ten operand hits already listed, while interior patterns `94 4B 62 00`, `98 4B 62 00`, `9C 4B 62 00`, `A0 4B 62 00`, and `A4 4B 62 00` have zero matches.
- The recheck notes that the two UserLookPane target uses in `sub_5A0640` proceed through `sub_4D02F0(..., L"STATBUT.EPF", ...)` and then through drawing calls such as `sub_4B9980`. This supports the UserLookPane emitter route but does not change ownership, because the literal still enters as a source-use resource filename.
- Address adjacency remains weaker than xrefs: predecessor [UID:0003F3][0x00624b74-0x00624b90.SelfLookPane2PaletteResourceString](by-memory/0x00624b74-0x00624b90.SelfLookPane2PaletteResourceString.md) and successor [UID:0003F5][0x00624ba8-0x00624bd0.SelfLookPane2StatButtonLabelStrings](by-memory/0x00624ba8-0x00624bd0.SelfLookPane2StatButtonLabelStrings.md) are SelfLookPane2-local, but the target's own ten refs cross SelfLookPane2 and UserLookPane, so it cannot inherit the neighbor owner.
- The report's statement that the page is below a strict `90/90+` C++-entry gate is preserved as historical gate context, but it is not the primary current reason for blank direct C++. The stronger current reason is source shape: this page represents a pooled physical literal, and standalone C++ here would invent a shared declaration; final source should surface the literal at the consuming SelfLookPane/UserLookPane use sites unless future source evidence proves a shared helper or declaration.
- The recheck leaves sibling pooled literals as future independent assignments. It does not authorize changing this target's score, owner, emitters, range, name, split, merge, C++ block, or shared coverage row.

## 2026-06-22 B003 Current No-Owner Research Audit

B003's archived `0003F4-SharedStatButtonEpfResourceString-current-no-owner-research` report was audited after the advanced-scan repair and B002 no-owner sections above were already present. Its final recommendation is accepted as a no-metadata-change Rule 26 closeout: keep `COMPLETION:89`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000NL,0000P0`, blank `EMITTER_POSITION_OPTIONAL`, and blank direct reconstruction C++. The report's stale target-page gate wording recommendation is already satisfied below: direct C++ stays blank because this is a pooled source-use literal, not because it fails the current combined-score/emitter gate.

Report-specific claims retained:

| B003 claim | Rule 26 disposition |
| --- | --- |
| The target came from the current Goal 2 no-owner memory sweep; generated memory coverage listed owner `NONE` and emitters `0000NL,0000P0`; the no-owner tracker and supervisor notes were checked; and the prior B002 current Goal 2 pass was treated as background, not authority. | Accepted as process provenance. The current target, generated row, and shared coverage row all match the accepted no-owner/two-emitter state. |
| B003 applied `by-structure.md` and `inference_research.md`: semantic ownership and output routing are separate, compiler/linker-pooled string literals may be `CANONICAL_OWNER:NONE` with multiple emitters, and `.rdata` adjacency alone does not prove source ownership. | Accepted. This is the controlling reason not to force SelfLookPane, UserLookPane, [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md), or a synthetic resource owner. |
| Live IDA MCP session `b001_0002bd` used read-only `NexusTK.exe` / `NexusTK.exe.i64`, image base `0x00400000`, ready auto-analysis, ready Hex-Rays, and ready string cache. | Incorporated as B003 provenance. It independently reconfirmed the same range, bytes, function memberships, xref set, and boundary facts already preserved by the current target. |
| The target is exact range `0x00624b90-0x00624ba8`, size `0x18`, bytes `53 00 54 00 41 00 54 00 42 00 55 00 54 00 2e 00 45 00 50 00 46 00 00 00`, decoded as UTF-16LE `STATBUT.EPF`. | Accepted. This is source-declared/generated-binary data, not a function, vtable, pointer table, or compiler thunk. |
| `find_bytes` found UTF-16LE `STATBUT.EPF\0` only at `0x00624b90`; separate `STATBUTS.EPF\0` and `STAT.EPF\0` hits occurred only at `0x00624820` and `0x00630ee4`. | Accepted. The target is one unique physical resource filename literal and must not be merged with similar neighboring strings. |
| Ten direct xrefs reach the target: eight from SelfLookPane2 `sub_570FC0` at `0x00571579`, `0x005715cc`, `0x00571659`, `0x005716d6`, `0x0057175e`, `0x0057177b`, `0x00571b62`, `0x00571bb5`, and two from UserLookPane `sub_5A0640` at `0x005a0c73`, `0x005a0ce1`. | Accepted. Both [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) and [UID:0000P0][UserLookPane](by-file/UserLookPane.md) are proven source-use emitters; neither can be promoted to sole canonical owner without overclaiming the other. |
| `sub_570FC0` size `0xc45` contains eight `sub_4D02F0(..., L"STATBUT.EPF", ...)` calls with dynamic and constant stat-button frame indices; `sub_5A0640` size `0xf3d` contains two branch-selected equivalent calls. | Accepted. The source should surface `L"STATBUT.EPF"` at reconstructed SelfLookPane/UserLookPane use sites, not as a standalone declaration on this physical by-memory page. |
| Interior xrefs and raw absolute operand scans for `0x00624b94`, `0x00624b98`, `0x00624b9c`, `0x00624ba0`, and `0x00624ba4` were zero; successor refs at `0x00624ba8` and `0x00624bbc` are separate SelfLookPane2-local label-string children. | Accepted. No interior split, tail child, range widening, predecessor merge, successor merge, or parent-container merge is justified. |
| Raw PE mapping placed the target in `.rdata` at raw offset `0x223590`; absolute `0x00624b90` operand hits matched the ten IDA xrefs; target RVA-form `0x00224b90` and interior-address scans were negative. | Accepted. The raw executable independently matches the IDA xref and boundary model and does not reveal a relocation/table owner. |
| B003 rejected SelfLookPane-only, UserLookPane-only, [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md) container, [UID:0000KM][LegendPane](by-file/LegendPane.md), and new by-resource/global shared owners. | Accepted. Direct refs are in SelfLookPane2/UserLookPane render/setup code, not in a proven `LegendPane`, shared resource table, global declaration, debug path, relocation grouping, or source constants object. |
| B003 recommended no owner/emitter/split/range/score/generated-row/shared-coverage changes, with only target wording cleanup for the obsolete `90/90+` C++ gate. | Accepted. The wording cleanup is already reflected in this page, and the current [UID:0003F4] shared coverage row already matches the advanced-scan/no-owner/two-emitter state, so no supervisor coverage patch is needed. |

## Ownership And Reconstruction

Keep `CANONICAL_OWNER:NONE`. The literal is source-authored/source-declared data, but current evidence does not prove a single shared declaration owner. Assigning it to only [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md) would overclaim the UserLookPane call sites, and assigning it to only [UID:0000P0][UserLookPane](by-file/UserLookPane.md) would overclaim the eight SelfLookPane2 call sites. A new shared resource-string owner would be invented structure because no table, global, by-resource page, debug source path, or relocation pattern proves a common declaration.

Set `RECONSTRUCTABLE:TRUE` with `EMITTER_UIDS:0000NL,0000P0`. The rebuilt source needs the `L"STATBUT.EPF"` resource filename at both proven source-use routes. `RECONSTRUCTION_CPP` stays blank even though the item clears the minimum combined-score/emitter gate (`(89 + 92) / 2 = 90.5`, with emitters `0000NL,0000P0`) because this page represents a pooled source-use literal; final source should appear as use-site `L"STATBUT.EPF"` literals when the surrounding SelfLookPane and UserLookPane functions are reconstructed, not as a standalone global declaration.

## UID0003RB Applied Use-Site Synchronization

The accepted [UID:0003RB][0x00570fc0-0x00571c05.SelfLookPane2OnPaint](by-memory/0x00570fc0-0x00571c05.SelfLookPane2OnPaint.md) formal source now materializes all eight SelfLookPane2 uses as direct `L"STATBUT.EPF"` arguments to `RenderTileFrame`: two navigation-button frames and the enabled look/equipment/stats frame selections. This closes the SelfLookPane2 half of the source-use route without changing the physical literal's ownership or creating a separate declaration. The two independent UserLookPane refs remain routed through [UID:0000P0][UserLookPane](by-file/UserLookPane.md). Therefore this page retains `CANONICAL_OWNER:NONE`, `EMITTER_UIDS:0000NL,0000P0`, `RECONSTRUCTABLE:TRUE`, blank optional position, and blank direct C++.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 89 | The page records exact bytes, size, IDA string decoding, all ten direct target xrefs, decompiler context for both source-use functions, negative interior-reference evidence, sibling-boundary evidence, current owner/emitter routing, and the applied UID0003RB use-site source. It remains below 90 because this pooled physical literal intentionally has no standalone declaration and its independent UserLookPane source route remains outside this callback. |
| Confidence | 92 | IDA bytes, string decoding, xrefs, immediate-byte scan, decompiler output, containing split inventory, and current file-owner docs agree that this is a pooled source-use literal shared by SelfLookPane and UserLookPane with no proven single canonical declaration owner. |

## Cross-References

- [UID:0002V2][0x00624788-0x00624c60.LookGroupCollectionResourceStringData](by-memory/0x00624788-0x00624c60.LookGroupCollectionResourceStringData.md)
- [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md)
- [UID:0000P0][UserLookPane](by-file/UserLookPane.md)

## Changes

- 2026-07-14 B002 UID0003RB callback: metadata and blank direct C++ unchanged; synchronized the now-applied SelfLookPane2 `L"STATBUT.EPF"` use sites, preserved the independent UserLookPane route, and updated the UID0003F3 predecessor link after its UID-preserving rename.
- 2026-06-12 Supervisor applying B001-051: created parent-blank pooled-literal child for shared `STATBUT.EPF`.
- 2026-06-14 Agent-B003 advanced-scan repair: added exact `SharedStatButtonEpfResourceString` label text for the generated advanced scan and rechecked the item under the current owner/emitter model. Updated from `85/88`, `RECONSTRUCTABLE:FALSE`, no emitters to `89/92`, `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:NONE`, `EMITTER_UIDS:0000NL,0000P0`. Evidence: live IDA MCP confirms exact UTF-16LE bytes, exactly ten xrefs to `aStatbutEpf` split eight from SelfLookPane2 `sub_570FC0` and two from UserLookPane `sub_5A0640`, no interior target xrefs, and separate successor label-string refs beginning at `0x00624ba8`.
- 2026-06-22 Rule 26 incorporation of B001 live no-owner pass: metadata unchanged. Added the report's detailed search matrix, representative push/call evidence, filtered decompiler call shapes, component/data-flow confirmation, and explicit rejection of `0002V2`/`sub_4D02F0`/synthetic owners. No coverage replacement row is needed because the current coverage row already preserves `CANONICAL_OWNER:NONE` with `EMITTER_UIDS:0000NL,0000P0`.
- 2026-06-22 Rule 26 incorporation of B002 current Goal 2 pass: metadata unchanged. Added the B002 report-specific audit section preserving its owner/emitter ranking, raw PE/IDA evidence, generated-row interpretation, split/merge/reclassification rejections, and no-standalone-C++ conclusion. No coverage replacement row is needed because the current [UID:0003F4] coverage row already preserves the no-owner/two-emitter state.
- 2026-06-22 Rule 26 incorporation of B002 current no-owner/multi-emitter recheck: metadata unchanged. Added the companion audit section preserving the report's generated-row trigger, IDA command coverage, `get_string`/`find_bytes` pattern facts, UserLookPane draw-call tail note, address-adjacency rejection, strict `90/90+` historical gate context, and no-change/no-coverage recommendation.
- 2026-06-22 Rule 26 incorporation of B003 current no-owner research: metadata unchanged. Added the B003 report-specific audit section preserving active no-owner sweep provenance, `b001_0002bd` live IDA session facts, exact bytes/range, ten direct source-use refs, raw PE offset/reference scans, interior/successor boundary checks, accepted no-owner/two-emitter source-use routing, rejected owner/split/merge/reclassification alternatives, and no standalone C++ or shared coverage change.
