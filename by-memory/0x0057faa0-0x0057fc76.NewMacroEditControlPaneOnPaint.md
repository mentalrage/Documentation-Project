*** UID:0004AD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewMacroEditControlPane::OnPaint()
{
    const Rect row = GetClientRect();
    BeginMacroRowPaint();

    const uint16_t normalizedKey = NormalizeSpellKey(m_key);
    const wchar_t *entryName = nullptr;
    if (m_spellMode != 0)
        entryName = g_spellRecords[normalizedKey].displayName;
    else
        entryName = GetLocalInventorySlot(normalizedKey)->displayName;

    const int centerY = (row.Height() + row.top + 12) / 2;
    if (m_shortcutIndex >= 10)
        DrawMacroRowText(row.left, centerY, FormatShortcutNumber(L"Ctrl+%d", m_shortcutIndex == 19 ? 0 : m_shortcutIndex - 9));
    else
        DrawMacroRowText(row.left + 18, centerY, FormatShortcutNumber(L"%d", m_shortcutIndex == 9 ? 0 : m_shortcutIndex + 1));

    DrawMacroRowText(row.left + 48, centerY, m_spellMode != 0 ? L"Spell" : L"Item");
    DrawMacroRowText(row.left + 90, centerY, entryName);

    if (IsFocused())
        DrawMacroRowFocus(row);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: NewMacroEditControlPane paint handler drawing exact 0..19 number/Ctrl-number shortcut labels from m_shortcutIndex, item/spell type text from the raw spell-mode byte, the selected display name, and the focus frame. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057faa0-0x0057fc76 NewMacroEditControlPane OnPaint

## Status

- Address range: `0x0057faa0-0x0057fc76` half-open.
- Entity kind: `NewMacroEditControlPane` virtual paint method.
- Direct owner/emitter: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) `NewMacroEditControlPane`.
- Vtable route: New primary paint slot in [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md) and [UID:0003DB][0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData](by-memory/0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData.md).
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Rebuild handling: source-authored virtual method; emit the formal C++ above.

## Evidence

Current MCP/report evidence gives function size `0x1d6`. The report ties item rows to `LocalInventorySlotRecord::displayName` through [UID:0003U9][0x005a3870-0x005a3889.UserPaneGetInventorySlotAddress](by-memory/0x005a3870-0x005a3889.UserPaneGetInventorySlotAddress.md) and spell rows to the spell-record table; hotkey label formatting uses the macro row format helpers.

- The complete-object dword at `+0x108` is read exactly at `0x0057fb4a`, `0x0057fb5d`, and `0x0057fb78`. The first read performs the signed bank comparison against 10; the next reads calculate the displayed number. Constructor initialization and the sole SetShortcutIndex page-load caller close every write to the field.
- Values `0..9` use the unprefixed number bank and display `1..9,0`, with index 9 mapped to zero. Values `10..19` use the `Ctrl+%d` bank and display `Ctrl+1..Ctrl+9,Ctrl+0`, with index 19 mapped to zero. The arithmetic, branches, offsets, and drawing positions are retained exactly.
- `m_shortcutIndex` is an `int`, not a compact-record identity, pointer, byte, unsigned value, enum, or macro slot. Historical `m_recordIndex` named the caller's storage context instead of the paint semantics and is superseded.

- The complete-object field read at `+0x10e` is the same byte returned by
  [UID:0004A7][0x0057f9c0-0x0057f9c7.NewMacroEditControlPaneGetSpellMode](by-memory/0x0057f9c0-0x0057f9c7.NewMacroEditControlPaneGetSpellMode.md), written by the constructor and
  [UID:0004AB][0x0057fa60-0x0057fa70.NewMacroEditControlPaneSetSpellMode](by-memory/0x0057fa60-0x0057fa70.NewMacroEditControlPaneSetSpellMode.md), and serialized as
  `CompactShortcutRecord::isSpellMode`.
- At `0x0057fac8`, the method tests the byte for zero/nonzero to choose the
  local-inventory record versus the spell-record table. It does not compare
  the value specifically with one.
- The later read at `0x0057fbde-0x0057fc01` uses the same zero/nonzero split to
  draw the literal `Item` or `Spell` with the corresponding exact length.
- Therefore a noncanonical nonzero byte remains spell mode throughout paint.
  The input path's normal zero/one writer does not narrow values arriving
  through construction or page load.

## Source Contract And History

- `m_spellMode` is the strongest source-facing field name because it states
  the item-versus-spell domain and avoids collision with inherited
  `Pane::SetMode(unsigned char)` rendering state at a different offset.
- Historical `m_mode` wording was width-correct but semantically generic. It
  is superseded by the full CompactShortcutRecord constructor/getter/setter/
  save/load lifecycle.
- Historical modifier terminology is rejected: Event key modifiers are a
  separate input value and are never stored at complete-object `+0x10e`.
- `m_shortcutIndex` is the strongest `+0x108` name because this method directly
  interprets it as the configured number/Ctrl-number shortcut ordinal. Historical
  `m_recordIndex`, `SetIndex`, macro-slot, and record-pointer meanings are
  superseded or rejected by the signed `0..19` display domain.
- The B003 formal delta changes only the `+0x108` field token. The helper calls, shortcut
  label arithmetic, item/spell record selection, focus test, strings, drawing
  order, metadata, owner/emitter route, and `88/91` score remain unchanged.

## Negative Evidence

- No comparison against literal one or Boolean conversion occurs before either
  branch.
- No evidence makes this byte an enum, signed character, inherited Pane mode,
  key modifier, or paint-only cache.
- No additional field, guard, fallback, or null check is introduced beyond the
  accepted current body.

## Changes

- 2026-07-16 B003 UID0004A9 implementation callback:
  - Preserved `88/91`, UID000095 owner/emitter, exact range/route, the complete B001 `m_spellMode` unsigned-char union, B005 `wchar_t m_key` union, all helper calls, item/spell selection, focus behavior, strings, drawing positions/order, and prior history.
  - Replaced only historical `m_recordIndex` uses in the exact formal body with `m_shortcutIndex` and documented the three exact `+0x108` reads, signed bank comparison, `1..9,0` and `Ctrl+1..Ctrl+9,Ctrl+0` mappings, zero-key special cases, and rejected record/macro-slot interpretations.

- 2026-07-16 B001 UID0004A7 implementation callback:
  - Replaced the stale `m_mode` token with the accepted `m_spellMode` field in
    both zero/nonzero paint decisions.
  - Added the complete raw-byte lifecycle, noncanonical-nonzero behavior,
    source-name rationale, historical correction, and negative evidence while
    preserving the existing `88/91` score and all unrelated paint behavior.
