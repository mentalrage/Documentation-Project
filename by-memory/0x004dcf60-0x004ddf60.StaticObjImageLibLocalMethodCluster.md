*** UID:00017H | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Non-emitting StaticObjImageLib local source/compiler index ending before EffectObjImageLib, with exact constructor-through-loader inventory, constructor EH state 0 LObject and state 1 Singleton<StaticObjImageLib> cleanup, authored method/helper children, accepted lightImageIndex record semantics, separate non-emitting UID000184 base destructor and UID0003M2 scalar wrapper, exact padding/range boundaries, and no duplicate aggregate source emission. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dcf60-0x004ddf60 StaticObjImageLibLocalMethodCluster

## Status

- Confidence: near-certain for exact inventory/ranges, direct-base constructor EH states, singleton/vtable ownership, resource inputs, helper boundaries, and accepted source-facing field names.
- Covered class: [UID:0000E2][StaticObjImageLib](by-class/StaticObjImageLib.md)
- Likely source file: [UID:0000O7][StaticObjImageLib](by-file/StaticObjImageLib.md)
- Current recovered file: `source-3/simroot_v2/class_StaticObjImageLib.cpp`
- Autogen status: non-emitting local cluster. Exact child/tail pages carry reconstructable ownership; this container does not attach or emit as a separate source item.

## Functions

| Range | Function | Notes |
| --- | --- | --- |
| `0x004dcf60-0x004dd1d8` | `StaticObjImageLib_Constructor` | Constructs `LObject` then direct empty `Singleton<StaticObjImageLib>`, publishes the singleton, establishes EH state 0/1 cleanup, loads `SOBJ.TBL`, selects `TILEC` modern/legacy paths, and validates tile-class counts. |
| `0x004dd1e0-0x004dd2be` | `StaticObjImageLib::~StaticObjImageLib` | [UID:00017I][0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor](by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md); exact exclusive end is `0x004dd2be`, with filename rename pending shared validator/report leases. |
| `0x004dd2c0-0x004dd84a` | `StaticObjImageLib::RenderStaticObject` | [UID:00017J][0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject](by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md); IDA confirms the corrected renderer body and two-callsite caller set, and B007 reroutes it through the class owner [UID:0000E2][StaticObjImageLib](by-class/StaticObjImageLib.md). |
| `0x004dd850-0x004dd8a0` | `StaticObjImageLib::GetStaticObjectBounds` | Computes local bounds from static-object layer count and tile dimensions. |
| `0x004dd8b0-0x004dda22` | `StaticObjImageLib::HitTestStaticObjectPixel` | StaticObjectPane pixel/solid-mask helper; one live caller at `0x00537950` and callee to the alpha/pixel test helper. |
| `0x004dda30-0x004dda57` | `StaticObjImageLib::GetStaticObjectEntry` | Tiny one-based entry lookup used by `MapPane::CreateStaticObjectPaneForTile` and `ObjectList::RefreshStaticObjectLighting`; the complete declaration is now visible through `StaticObjImageLib.h`. |
| `0x004dda60-0x004dde01` | `BuildTilecArchiveTable` | Owned helper with saved IDA label; scans and merges `TILEC%d.EPF` archive shards. |
| `0x004dde10-0x004ddf5e` | `StaticObjImageLib::LoadStaticObjectRecords` | Unreferenced in current IDA but decompiles as an `SOBJ` record loader writing the static-object pointer table. |

This range is contiguous and ends at `0x004ddf60`, immediately before EffectObjImageLib starts. The later Singleton base destructor and scalar deleting wrapper are separate exact non-emitting compiler-support pages, not missing pieces of this aggregate.

Related exact tail pages:

| Range | Function | Notes |
| --- | --- | --- |
| [UID:000184][0x004e5c00-0x004e5c0b.StaticObjImageLibSingletonBaseDestructor](by-memory/0x004e5c00-0x004e5c0b.StaticObjImageLibSingletonBaseDestructor.md) | compiler `Singleton<StaticObjImageLib>` base destructor | Exact eleven-byte EH-only body reached from constructor state 1 with ECX equal to complete `this+4`; blank non-emitting formal channels. |
| `0x004e6990-0x004e6aa6` | [UID:0003M2][0x004e6990-0x004e6aa6.StaticObjImageLibScalarDeletingDestructor](by-memory/0x004e6990-0x004e6aa6.StaticObjImageLibScalarDeletingDestructor.md) | Non-emitting compiler scalar wrapper regenerated by the class's virtual ordinary destructor. |

## Live IDA Function Inventory

| Address | Size/end | Role | Evidence summary |
| --- | --- | --- | --- |
| `0x004dcf60` | `0x278`, ends `0x004dd1d8` | `StaticObjImageLib_Constructor` | Publishes [UID:0000SD][g_pStaticObjImageLib](by-global/g_pStaticObjImageLib.md), installs the vtable, parses `SOBJ.TBL`, allocates the static-object pointer array and variable-size entry records, selects modern `TILEC.EPF`/`TILEC.TBL` or legacy `TILEC.EPD`/`TILEC.TBD`, validates tile-class counts, and fills the tile-class table. |
| [UID:00017I][0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor](by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md) | `0xde`, ends `0x004dd2be` | Ordinary destructor | Compiled body restores the vtable and frees derived resources, then compiler-emitted reverse `Singleton<StaticObjImageLib>` and `LObject` base destruction clears the singleton and tears down the base. Formal CPP contains only authored derived cleanup. |
| [UID:00017J][0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject](by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md) | `0x58a`, ends `0x004dd84a` | Renderer | Draws layered static-object tiles for the two confirmed consumers, with modern/legacy tile-class handling and optional overlay behavior documented in the child page. |
| `0x004dd850` | `0x51`, ends `0x004dd8a1` | Bounds helper | Bounds-checks a one-based static-object id, resolves `this+0x0c`, reads the layer count byte at entry `+0x05`, and returns a `RectBounds` using global tile dimensions `word_66DA9C`/`word_66DAA0`. |
| `0x004dd8b0` | `0x172`, ends `0x004dda22` | Pixel hit-test helper | One caller from StaticObjectPane hit testing; walks static-object layers and checks the target pixel plus immediate neighbors. |
| `0x004dda30` | `0x27`, ends `0x004dda57` | Entry lookup helper | Exactly two callers: MapPane creation at `0x0050f0be` and ObjectList refresh at `0x00530d36`; returns the static-object entry pointer for a valid one-based id. |
| `0x004dda60` | `0x3a2`, ends `0x004dde02` | `BuildTilecArchiveTable` | Constructor-only helper that probes up to 100 `TILEC%d.EPF` shards, opens each archive, accumulates frame counts/extents, builds one packed 24-byte record table plus a sentinel record, and frees all temporary archive readers and arrays before returning the metadata block. |
| `0x004dde10` | `0x14e`, ends `0x004ddf5e` | Static-object record loader | IDA decompilation reads `SOBJ` stream data, allocates `this+0x0c` records, copies per-layer tile ids, and initializes record fields. |

## Evidence Notes

- Existing generated-source summaries identify `StaticObjImageLib` as the static map object sprite library, but this page's ownership and boundary claims are based on IDA evidence for every inventoried function, constructor EH states, direct Singleton RTTI/PMD, destructor/scalar parity, and primary vtable at `0x0061b704`.
- Constructor xrefs show one executable caller from `Application::Startup` / `InitializeSubsystems` at IDA `0x004f607b`.
- `RenderStaticObject` callers are only `StaticObjectPane::RenderStaticObjectForTarget` at `0x005379c3` and `PhotoPane` map-photo composition at `0x00549847`.
- `GetStaticObjectBounds` callers are map/object creation at `0x0050f075`, [UID:0000O6][StaticObjectPane](by-file/StaticObjectPane.md) at `0x00537986`, and [UID:0000MK][PhotoPane](by-file/PhotoPane.md) at `0x00549804`.
- `BuildTilecArchiveTable` xrefs show one caller: `StaticObjImageLib::StaticObjImageLib` at `0x004dd0f0`.
- Singleton storage is [UID:0000SD][g_pStaticObjImageLib](by-global/g_pStaticObjImageLib.md) / `dword_69B448` / current MCP `unk_69B448` at `0x0069b448`.
- `xrefs_to 0x0069b448` reports direct Singleton construction publication/null handling, constructor-unwind base-destructor clear, implicit ordinary/scalar base-destructor clears, shutdown read, map/static-object consumers, and photo composition read.
- `xrefs_to 0x0061b704` reports vtable writes from the constructor at `0x004dcfb8`, ordinary destructor at `0x004dd20b`, and scalar deleting destructor at `0x004e69c4`.
- 2026-05-28 IDA MCP boundary recheck confirmed `0x004e6aa5` is the trailing operand byte of `StaticObjImageLib::DeletingDestructor`'s `retn 4`; the correct exclusive end is `0x004e6aa6`.
- 2026-06-06 IDA MCP boundary recheck confirmed `StaticObjImageLib::~StaticObjImageLib` is `0x004dd1e0-0x004dd2be`; `0x004dd2bd` is still inside `sub_4DD1E0`, while `0x004dd2be-0x004dd2c0` is two-byte `0xcc` alignment before `RenderStaticObject`.
- Live IDA callee checks show the constructor owns `SOBJ.TBL` stream reads, memory allocation, `BuildTilecArchiveTable`, legacy `LoadImageFrameTable`, and error handling for tile-class count mismatch. The renderer's broader frame/palette/callback set stays documented on [UID:00017J][0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject](by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md).
- 2026-06-16 C001 live IDA MCP recheck on session `b001_mappane_0001AW_20260616` saved and verified labels `StaticObjImageLib_Constructor` at `0x004dcf60` and `BuildTilecArchiveTable` at `0x004dda60`. After-save lookup confirms unchanged sizes, `xrefs_to 0x004dda60` still reports the single constructor call at `0x004dd0f0`, and `lookup_funcs 0x004dde10` still reports the separate successor loader. No type/member/prototype edits were made because the static-object record fields, Tilec table fields, and common EPF-loader consolidation are still source-quality blockers.
- 2026-07-01 B010 live IDA MCP recheck on session `supervisor_resume_20260629` reconfirmed `lookup_funcs 0x004dda60` size `0x3a2`, `xrefs_to 0x004dda60` as the single constructor call at `0x004dd0f0`, `analyze_function` / `decompile` details for the `TILEC%d.EPF` archive-table builder, `xrefs_to 0x0069b448` with the same 13 singleton lifecycle/consumer refs, and `get_global_value` / `get_bytes` proving the singleton slot is zero-initialized. The exact [UID:0000U6][BuildTilecArchiveTable_004DDA60](by-item/BuildTilecArchiveTable_004DDA60.md) child now emits first-draft formal C++; this aggregate remains non-emitting support and must not duplicate the child body.

## State And Resource Map

| State/resource | Evidence and interpretation |
| --- | --- |
| constructor EH state 0 | Cleans the `LObject` base at complete-object offset `+0x00` if Singleton construction has not completed. |
| constructor EH state 1 | Adjusts ECX to complete `this+0x04` and tail-jumps to UID000184, destroying the direct empty `Singleton<StaticObjImageLib>` base before `LObject`. RTTI PMD `+4,-1,0` and `0x14` object size prove EBO overlap with `tileCatalog`. |
| `this+0x04` | `tileCatalog`: tile catalog/archive metadata pointer. Constructor writes either modern `BuildTilecArchiveTable(L"TILEC.EPF")` output or legacy `LoadImageFrameTable(L"TILEC.EPD", 0)` output; destructors release its optional backing table and the catalog object. |
| `this+0x08` | `tileClasses`: four-byte [UID:0001WC][TileClassEntry](by-type/by-struct/TileClassEntry.md) table allocated from `TILEC.TBL` or `TILEC.TBD`; each row stores `tileFrameId` plus `allowPaletteFilter` and a reserved byte. |
| `this+0x0c` | `staticObjEntries`: pointer array for variable-size [UID:0001W8][StaticObjEntry](by-type/by-struct/StaticObjEntry.md) records loaded from `SOBJ.TBL`; each record has signed `lightImageIndex`, `lightHeightClass`, `layerCount`, `cacheChain`, and inline `tileIds`. Historical `lightIntensity` is rejected because both live lighting consumers use an image-table selector with a negative absence sentinel. |
| `this+0x10` | `staticObjCount`: static-object record count read from `SOBJ.TBL`, used by constructor allocation, bounds lookup, destructor cleanup, and renderer id validation. |
| [UID:0000SD][g_pStaticObjImageLib](by-global/g_pStaticObjImageLib.md) | Process singleton emitted once as `StaticObjImageLib *g_pStaticObjImageLib = 0;`, published by direct Singleton construction and cleared by constructor-unwind/ordinary/scalar Singleton destruction. Exact UID0001PP storage is zero-initialized and non-emitting. |
| `SOBJ.TBL` | Static object table read by the constructor; the full render view is `lightImageIndex`, `lightHeightClass`, discarded/classification byte, `layerCount`, and `tileIds`. The compact MapPane/GameServerConfig classification view is separate consumer state. |
| `TILEC.EPF` / `TILEC%d.EPF` | Modern tile archive family scanned by `BuildTilecArchiveTable` for static-object tile frames. |
| `TILEC.TBL` / `TILEC.TBD` | Tile-class table source; constructor validates the count against tile catalog frame capacity before allocating `this+0x08`, stores `tileFrameId` as `value & 0x7fff`, and stores `allowPaletteFilter` from the high bit. |
| `TILEC.EPD` | Legacy archive path loaded through the shared image-frame table helper when `byte_66DA97 != 1`. |

## Cross-References

- [UID:0000O7][StaticObjImageLib](by-file/StaticObjImageLib.md)
- [UID:00017I][0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor](by-memory/0x004dd1e0-0x004dd2be.StaticObjImageLibDestructor.md)
- [UID:00017J][0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject](by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md)
- [UID:0000SD][g_pStaticObjImageLib](by-global/g_pStaticObjImageLib.md)
- [UID:0001YX][StaticObjImageLibVtable](by-type/by-vtable/StaticObjImageLibVtable.md)
- [UID:0001W9][StaticObjImageLibLayout](by-type/by-struct/StaticObjImageLibLayout.md)
- [UID:0001W8][StaticObjEntry](by-type/by-struct/StaticObjEntry.md)
- [UID:0001WC][TileClassEntry](by-type/by-struct/TileClassEntry.md)
- [UID:0000U6][BuildTilecArchiveTable_004DDA60](by-item/BuildTilecArchiveTable_004DDA60.md)
- [UID:0000J3][EPFImageResources](by-file/EPFImageResources.md)

## Resolved And Remaining Questions

- `tileCatalog`, `tileClasses`, `staticObjEntries`, `staticObjCount`, `StaticObjEntry::{lightImageIndex, lightHeightClass, layerCount, cacheChain, tileIds}`, and `TileClassEntry::{tileFrameId, allowPaletteFilter, reserved}` are accepted source-facing descriptive names for first-draft C++. `lightIntensity` is preserved only as superseded history. Original spellings remain unproven and cap confidence below final-audit quality.
- The complete `StaticObjImageLib`, `StaticObjEntry`, `TileClassEntry`, accessor, and singleton declarations now live in [UID:0000E2][StaticObjImageLib](by-class/StaticObjImageLib.md) H. This non-emitting aggregate remains inventory/provenance only and does not duplicate that header or any exact child body.
- `BuildTilecArchiveTable` is a saved source-quality helper name for the constructor-only `TILEC%d.EPF` table builder. B010's 2026-07-01 callback resolves the source-form blocker for the exact child [UID:0000U6][BuildTilecArchiveTable_004DDA60](by-item/BuildTilecArchiveTable_004DDA60.md), which now emits first-draft formal C++ using accepted `ArchiveMetadataTable`, `PackedArchiveRecord`, `DATFile`, `HasDATEntry`, `InitRectBounds`, and `DATFile::GetDataPointer` vocabulary. Exact original spellings and possible common EPF-loader consolidation remain confidence caps, not no-code blockers.
- The helper at `0x004dd8b0`, tiny lookup at `0x004dda30`, and loader at `0x004dde10` are now included in this local cluster because live IDA places them before the EffectObjImageLib boundary and their behavior matches StaticObjImageLib record/tile handling.
- The final C++ block stays blank because this aggregate is intentionally non-emitting. Exact child pages carry source ownership; [UID:00017J][0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject](by-memory/0x004dd2c0-0x004dd84a.StaticObjImageLibRenderStaticObject.md) now contains first-draft method C++.
- This aggregate remains non-emitting even after the 2026-06-16 label refresh; exact child/tail pages carry reconstructable source ownership, while this page inventories the contiguous local neighborhood.

## Score Rationale

- `COMPLETION:90`: the page records the complete contiguous constructor-through-loader inventory, exact half-open boundaries/padding, authored child routes, direct-base EH states, separated compiler tails, state/resource map, accepted record names, and non-emitting aggregate policy.
- `CONFIDENCE:95`: function objects, callers/callees, resources, vtable/global xrefs, RTTI PMD/EBO, constructor EH receiver adjustment, and ordinary/scalar parity agree. Remaining uncertainty concerns original private spellings and unrelated child-body final polish.

## Historical Superseded Assumptions

- Earlier inventory used constructor end `0x004dd1d7`; exact function size `0x278` gives half-open end `0x004dd1d8`.
- Earlier prose named UID000184 `ClearStaticObjImageLibSingleton`, assigned generic cleanup-glue semantics, and treated the compiled destructor clear as explicit source. The bytes remain valid evidence, but direct Singleton RTTI, PMD/EBO, adjusted EH receiver, and reverse destruction supersede that source interpretation.
- Earlier aggregate ranges crossed later sibling image libraries. The accepted `0x004dcf60-0x004ddf60` boundary and separate tail pages remain authoritative.

## Changes

- 2026-08-11 B005 UID000184 lifecycle implementation: raised `86/91` to `90/95`, preserved owner `NONE`, non-reconstructable, blank emitter/position/formal channels, and `Nested:-4`; corrected constructor end, added state-0/state-1 base cleanup, direct Singleton PMD/EBO, separate compiler-tail dispositions, implicit destructor lifecycle, and superseded helper/source assumptions.

- 2026-08-11 B003 UID00023D support synchronization: preserved `86/91`, owner `NONE`, reconstructable false, blank emitter/position, `Nested:-4`, and blank CPP/H. Reconciled the exact two-caller accessor inventory, `lightImageIndex`/`lightHeightClass` semantics, the new parent-class H visibility route, and explicit rejected `lightIntensity` history without changing this aggregate's non-emitting disposition.

- 2026-05-28: corrected the page range end from `0x004e6aa5` to `0x004e6aa6`.
  - Before: this page and coverage row ended at `0x004e6aa5`, leaving `0x004e6aa5-0x004e6ab0` as UNKNOWN.
  - After: this page ends at `0x004e6aa6`; `0x004e6aa5` is included as the final byte of `StaticObjImageLib::DeletingDestructor`'s `retn 4`, while `0x004e6aa6-0x004e6ab0` is documented as padding.
  - Evidence: IDA MCP reports the function object `0x004e6990-0x004e6aa6`; disassembly shows `C2 04 00` at `0x004e6aa3-0x004e6aa5` followed by `0xcc` bytes through `0x004e6aaf`.
- 2026-06-06: corrected the ordinary destructor table row to the IDA-exclusive end `0x004dd2be`.
  - Before: the aggregate listed `0x004dd1e0-0x004dd2bd`, using the old last-byte-style end.
  - After: the row records `0x004dd1e0-0x004dd2be`, while the linked child filename remains old pending a validator/report-aware rename.
  - Evidence: IDA MCP `lookup_funcs` reports `sub_4DD1E0` size `0xde`; padding audit confirms `0x004dd2be-0x004dd2c0` is two `0xcc` bytes.
- 2026-06-06 A005 aggregate evidence refresh:
  - Before: the aggregate was scored `70/85` and was not attached to its source file despite stronger child/global/vtable evidence.
  - After: the aggregate is scored `84/88`, attached to [UID:0000O7][StaticObjImageLib](by-file/StaticObjImageLib.md), and records a live IDA function inventory, singleton/vtable xrefs, state/resource map, caller/callee evidence, and final-C++ gate.
  - Evidence: live IDA MCP `lookup_funcs`, `callers`, `callees`, `decompile`, `disasm`, and `xrefs_to` checks on constructor, destructor, renderer, bounds helper, archive-table helper, singleton-clear helper, scalar deleting destructor, `g_pStaticObjImageLib`, and the vtable slot at `0x0061b704`.
- 2026-06-12 C001 barrier repair:
  - What existed before: the file/range `0x004dcf60-0x004e6aa6.StaticObjImageLib` was a reconstructable non-contiguous aggregate crossing EffectObjImageLib, ItemObjImageLib, LightObjImageLib, and NewHumanImageLib sibling owners.
  - Changed to: renamed to `0x004dcf60-0x004ddf60.StaticObjImageLibLocalMethodCluster`, marked this container `RECONSTRUCTABLE:FALSE`, cleared autogen routing, added the previously unlisted local helpers at `0x004dd8b0`, `0x004dda30`, and `0x004dde10`, and split the scalar deleting destructor into an exact standalone page.
  - Summary/evidence: live IDA `entity_query`, `xrefs_to`, `callees`, and `decompile` on 2026-06-12 show the StaticObjImageLib local neighborhood ending at `0x004ddf5e` with padding to EffectObjImageLib at `0x004ddf60`; later singleton/destructor tails are separate exact ranges.
- 2026-06-16 C001 item/IDA support refresh:
  - Changed scores from `85/90` to `86/91`; non-emitting `RECONSTRUCTABLE:FALSE` and owner `NONE` are unchanged.
  - IDA DB changes saved to `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`: `sub_4DCF60 -> StaticObjImageLib_Constructor` and `sub_4DDA60 -> BuildTilecArchiveTable`.
  - Evidence: after-save lookup verified unchanged function sizes, the single constructor xref to `BuildTilecArchiveTable`, and the separate successor loader at `0x004dde10`. Final field/table names and possible shared EPF-loader consolidation remain unresolved, so no prototypes/types/members or final C++ were emitted.
- 2026-06-21 B007 Rule 26 incorporation:
  - Scores unchanged at `86/91`; non-emitting status and owner `NONE` are unchanged.
  - Historical evidence: incorporated B007 descriptive names, class-owned renderer routing, the separate scalar page, and the then-current no-code cleanup-glue policy for [UID:000184][0x004e5c00-0x004e5c0b.StaticObjImageLibSingletonBaseDestructor](by-memory/0x004e5c00-0x004e5c0b.StaticObjImageLibSingletonBaseDestructor.md). UID000184 now supersedes only the helper identity/source explanation while preserving the exact bytes and separate-range decision.
- 2026-07-01 B010 StaticObjImageLib empty-emitter family support update:
  - Scores unchanged at `86/91`; non-emitting status and owner `NONE` are unchanged.
  - Evidence: recorded the current zero-initialized singleton correction and the child [UID:0000U6][BuildTilecArchiveTable_004DDA60](by-item/BuildTilecArchiveTable_004DDA60.md) source-form decision without duplicating child body output. B010 live MCP session `supervisor_resume_20260629` reconfirmed the single constructor xref to `BuildTilecArchiveTable`, the Tilec table-builder behavior, 13 singleton refs, and current zero bytes/value at `0x0069b448`.
