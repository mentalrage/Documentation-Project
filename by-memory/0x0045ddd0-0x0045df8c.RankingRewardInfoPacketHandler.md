*** UID:0001ZH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool RankingRewardInfoDialog::HandlePacketEvent(Event *event)
{
    const unsigned char *packet = event->packet;
    if (packet[0] != 0x7d || packet[1] != 5)
        return false;

    const unsigned int categoryId = PacketBufferReadUInt32BE(packet + 2);
    RankingCategoryRecord *category =
        g_pRankingDialog->m_categories.GetSelectedRecord();

    if (category != NULL &&
        static_cast<unsigned int>(category->GetCategoryId()) == categoryId)
    {
        const short entryCount =
            static_cast<short>(PacketBufferReadUInt16BE(packet + 6));
        int packetOffset = 8;

        for (int index = 0; index < entryCount; ++index)
        {
            RankingRewardEntry entry;
            packetOffset += entry.ParseFromPacket(packet + packetOffset);
            m_rewardEntries.push_back(entry);
        }

        InvalidateRect(&m_bounds);
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-emitting `RankingRewardInfoDialog::HandlePacketEvent(Event *event)` for opcode `0x7d` subcommand `5`; current MCP confirms the secondary EventHandler slot, `Event::packet` payload, selected-category validation, signed-positive count gate, parser call, vector deep-copy/RAII cleanup, full-bounds invalidation, handled result, and boundary padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045ddd0-0x0045df8c RankingRewardInfoPacketHandler

## Status

- Address range: `0x0045ddd0-0x0045df8c`.
- Entity kind: method.
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: `source-authored`.
- Owner: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md).
- Parent memory range: [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md).
- Autogen parent: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), with file-level source route through [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Confidence: strong for the accepted EventHandler signature, packet payload field, opcode/subcommand behavior, selected-category check, parser calls, vector ownership, invalidation shape, and handled result.

## Signature

Current IDA shape:

```c
char __thiscall sub_45DDD0(int *this, int packetContext);
```

The method is reached through the `RankingRewardInfoDialog + 0xa0` secondary `EventHandler` vtable packet-family slot at `0x00610c34`. The accepted project source-facing signature for that slot is `bool HandlePacketEvent(Event *event)`, as established by [UID:00004N][EventHandler](by-class/EventHandler.md) and the source-emitting CollectionPane packet-event overrides. `Event::packet` is the accepted name for the payload pointer stored at event offset `+0x0c`, matching the neighboring RankingDialog and ExchangeDialog packet handlers. The method returns `true` after accepting a reward-info packet and `false` when the packet opcode/subcommand does not match.

## Behavior

The handler reads packet bytes from `event->packet`. It accepts only opcode `0x7d` with subcommand `5`. It then reads a big-endian 32-bit category id at packet offset `+2` and compares it against `g_pRankingDialog->m_categories.GetSelectedRecord()->GetCategoryId()`. If the ids match, it reads the big-endian 16-bit reward-entry count at offset `+6`, preserves the binary's signed-positive count gate, parses entries beginning at offset `+8`, and appends each `RankingRewardEntry` to `m_rewardEntries`. `std::vector::push_back` supplies the observed deep copy and the temporary entry destructor supplies the observed nested-item cleanup. The handler then calls `InvalidateRect(&m_bounds)`, exactly representing the primary Pane vtable slot `+0x20` call with the complete-object bounds at `+0x44`. A matching opcode/subcommand returns `true` even when no category is selected or the category id differs; only a nonmatching packet returns `false`.

The append has two compiler-lowered branches. When capacity remains, `0x0045ded4-0x0045df1c` copy-constructs the fixed fields and nested `items` vector directly at current end, then advances end by `0x230`. When end equals capacity, `0x0045df25-0x0045df2f` passes the current end pointer, stack `entry`, and `m_rewardEntries` triplet to [UID:0003X7][0x0045ec50-0x0045ef1f.RankingRewardEntryVectorInsertWithGrowth](by-memory/0x0045ec50-0x0045ef1f.RankingRewardEntryVectorInsertWithGrowth.md). UID0003X7 is the source-declared/generated-binary full-capacity slow path covered by the existing literal `m_rewardEntries.push_back(entry);`; its inserted-pointer return is intentionally unused. Neither branch creates a separate source helper call.

[UID:0003X9][0x0045ef50-0x0045efd0.RankingRewardItemVectorClone](by-memory/0x0045ef50-0x0045efd0.RankingRewardItemVectorClone.md) is the compiler/STL `std::vector<RankingRewardItem>` copy-constructor specialization used by both branches, not a source-authored clone call. The in-capacity path calls it at `0x0045df05` after fixed fields through `+0x221` and natural alignment before `items +0x224`; outer end advances only after X9 returns. Full-capacity UID0003X7 uses the same specialization for its inserted and relocated entries, while [UID:0004J5][0x0045f1d0-0x0045f285.RankingRewardEntryRangeCopyConstruct](by-memory/0x0045f1d0-0x0045f285.RankingRewardEntryRangeCopyConstruct.md) uses it for generic entry-range copy. Each outer guarded end advances only after successful nested copy, so allocation/length failure excludes the incomplete entry and completed disjoint ranges are destroyed exactly once. All of this lowering is regenerated from the one existing source statement `m_rewardEntries.push_back(entry);`; the handler body and declaration require no additional helper API.

After either append branch completes, call `0x0045df3e` invokes [UID:0004J4][0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy](by-memory/0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy.md) on the stack `RankingRewardEntry entry` nested `items` vector. This is normal packet-handler temporary cleanup, not paint cleanup. UID0004J4 recovers item capacity bytes with `0x208` stride, validates aligned-allocation metadata when required, frees storage, and clears the triplet; automatic local lifetime in the emitted handler regenerates that call.

## Raw Code Evidence

IDA MCP on 2026-06-04 confirms `0x0045ddd0-0x0045df8c` as an IDA function with no direct code callers; the dispatch path is reached through secondary vtable slot `0x00610c34 -> 0x0045ddd0`. Live decompilation confirms the key dispatch and append path:

```c
packet = *(_BYTE **)(packetContext + 12);
if (packet[0] != 125 || packet[1] != 5) {
    return 0;
}

categoryId = read_u32(packet + 2);
selected = GetSelectedCategory((int *)(g_pRankingDialog + 620));
if (selected && RankingCategoryRecord_GetId(selected) == categoryId) {
    count = read_u16(packet + 6);
    offset = 8;
    while (count--) {
        offset += ParseRewardEntry(tempEntry, packet + offset);
        append_or_copy_reward_entry(this + 115, tempEntry);
        DestroyRewardItemVector(tempEntry.items);
    }
    Invalidate(...);
}
return 1;
```

IDA callees include:

- selected-category lookup `0x0045ce70`;
- category id accessor `0x0045bf70`;
- packet integer readers `0x005754c0` and `0x00575480`;
- reward-entry parser `0x0045d7e0`;
- reward-entry vector append helper `0x0045ec50`;
- nested item-vector clone helper `0x0045ef50`;
- nested item-vector destroy helper [UID:0004J4][0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy](by-memory/0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy.md).

Additional live IDA evidence:

- `0x0045ddd0` is nested inside [UID:0001ZG][0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore](by-memory/0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore.md), which now records the constructor, vtable, paint, and destructor context.
- The handler uses `dword_67a7e4 + 0x26c` to reach the selected ranking category collection, then calls `0x0045ce70` and `0x0045bf70` before accepting the response.
- A temporary 560-byte reward entry is initialized on the stack, parsed by [UID:0001ZF][0x0045d7e0-0x0045db54.RankingRewardEntryParseAndAccessors](by-memory/0x0045d7e0-0x0045db54.RankingRewardEntryParseAndAccessors.md), appended or copied into the dialog vector, and then cleaned through UID0004J4 at `0x0045df3e`.
- Boundary bytes show `0xcc` padding after the close handler before `0x0045ddd0` and before the paint method at `0x0045df90`.
- Current MCP session `cfa0ae1a` on 2026-07-12 reconfirmed `server_health:ok`, Hex-Rays/analysis/string cache readiness, `sub_45DDD0` size `0x1bc`, the sole vtable data xref `0x00610c34`, all nine non-runtime callees, and the exact `event + 0x0c` payload load. Sibling decompilation of `0x00459210` and `0x004ad320` reconfirmed the same event payload offset and vtable-only packet-handler pattern.
- At `0x0045df50-0x0045df66`, assembly adjusts the secondary `this` by `-0xa0` to the complete object, passes complete-object `+0x44`, and calls primary vtable slot `+0x20`; accepted Pane/ranking source conventions identify this as `InvalidateRect(&m_bounds)`.

## Reconstruction Notes

Formal C++ now emits the highest-probability behavior-identical source shape. The former blockers are resolved by existing accepted project APIs rather than new inventions: `HandlePacketEvent(Event *event)` is the EventHandler packet-family slot spelling, `event->packet` is the established event payload member, `GetSelectedRecord()` and `GetCategoryId()` are emitted ranking APIs, `m_rewardEntries` is an emitted `std::vector`, `RankingRewardEntry::ParseFromPacket` is the source-facing declaration for the sole parser callee, and `InvalidateRect(&m_bounds)` matches the exact adjusted virtual call. Compiler append/growth/clone/destroy helpers are represented by `std::vector::push_back` and RAII rather than hand-ported.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 91 | Current MCP session `cfa0ae1a` reconfirms the exact function, vtable dispatch slot, event payload offset, opcode/subcommand filter, selected-category validation, packet offsets, signed-positive count gate, parser call, vector append/deep-copy path, temporary nested-vector cleanup, full-bounds invalidation, handled result, and boundary padding. Formal source now represents every source-authored operation. |
| Confidence | 93 | Handler, sibling packet-event slots, EventHandler declarations, accepted `Event::packet` use, ranking accessors, emitted vector members, Pane invalidation conventions, and assembly adjustment all agree. Confidence remains below final-audit certainty only because original private parser spelling and packet constant names are not symbol-proven. |
| Reconstructable | true | This is source-authored ranking reward-info packet behavior attached to the ranking dialog source parent. |

## Cross-References

- [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md)
- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md)
- [UID:0000S2][g_pRankingDialog](by-global/g_pRankingDialog.md)
- [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md)
- [UID:0001ZF][0x0045d7e0-0x0045db54.RankingRewardEntryParseAndAccessors](by-memory/0x0045d7e0-0x0045db54.RankingRewardEntryParseAndAccessors.md)
- [UID:0001ZG][0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore](by-memory/0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore.md)
- [UID:0001ZI][0x0045eae0-0x0045efd0.RankingRewardVectorStorageHelpers](by-memory/0x0045eae0-0x0045efd0.RankingRewardVectorStorageHelpers.md)
- [UID:0003X7][0x0045ec50-0x0045ef1f.RankingRewardEntryVectorInsertWithGrowth](by-memory/0x0045ec50-0x0045ef1f.RankingRewardEntryVectorInsertWithGrowth.md)
- [UID:0004J4][0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy](by-memory/0x0045f160-0x0045f1c6.RankingRewardItemVectorDestroy.md)
- [UID:0004J5][0x0045f1d0-0x0045f285.RankingRewardEntryRangeCopyConstruct](by-memory/0x0045f1d0-0x0045f285.RankingRewardEntryRangeCopyConstruct.md)

## Changes

### 2026-07-12 - B001 UID0001ZJ packet-cleanup relation sync

- Preserved `91/93`, UID0000BP owner/emitter, the complete `HandlePacketEvent(Event *event)` body, literal `m_rewardEntries.push_back(entry);`, packet/category/invalidation semantics, and executed X7/X9 content unchanged.
- Added exact UID0004J4 relation for normal stack-entry nested cleanup at `0x0045df3e`, explicitly correcting the stale paint label, and exact UID0004J5 relation for generic entry-range copy through X9.
- No formal C++ line, score, metadata field, packet behavior, or source operation changed.

### 2026-07-12 - B001 UID0003X9 covered-by relation sync

- Preserved `91/93`, owner/emitter UID0000BP, the complete `HandlePacketEvent(Event *event)` formal body, literal single `m_rewardEntries.push_back(entry);`, packet semantics, and executed UID0003X7 relationship unchanged.
- Added only the target-specific UID0003X9 relation: the direct in-capacity call, slow/range support uses, natural `items +0x224` placement, post-success guarded-end advancement, and no-standalone-copy-helper disposition.

### 2026-07-12 - B002 UID0003X7 covered-by synchronization

- Preserved `91/93`, owner/emitter UID0000BP, the complete `HandlePacketEvent(Event *event)` formal body, packet semantics, and every existing dependency unchanged.
- Added the exact target relationship: the in-capacity branch constructs at end, while full capacity calls UID0003X7 with end and the stack entry; both are compiler lowering of the already-emitted `m_rewardEntries.push_back(entry);`, and the slow-path inserted-pointer return is unused.

### 2026-07-12 - B002 Gate 1 additive packet-handler repair

- Before: `87/91`; formal C++ deferred source under stale claims that packet-listener, selected-category, vector, and invalidation APIs were unresolved.
- Changed to: `91/93`; formal `bool RankingRewardInfoDialog::HandlePacketEvent(Event *event)` now emits through UID0000BP, and the class declaration exposes the override plus `RankingRewardEntry::ParseFromPacket` dependency.
- Summary/evidence: current MCP database `cfa0ae1a` reconfirmed the sole secondary-vtable xref, `event + 0x0c` packet payload, big-endian readers, category accessors, signed-positive entry count, parser, vector deep copy/cleanup, and adjusted primary slot `+0x20` invalidation. Existing EventHandler, ExchangeDialog, RankingCategoryCollection, RankingCategoryRecord, Pane, and vector docs resolve every former source-interface blocker. No compiler temporaries or raw vector helpers are emitted.

### 2026-07-12 - B002 UID0001ZG split context

- The surrounding aggregate UID0001ZG is now a non-emitting split index. Exact sibling source children are UID0004I7 constructor, UID0004I9 OnDialogAction, UID0004IA OnPaint, UID0004IB close-bounds, and UID0004IC request sender.
- This packet handler keeps its accepted metadata and formal no-code decision. Its opcode `0x7d`/subcommand `5`, selected-category validation, `0x230` entry parse/deep-copy, nested cleanup, and invalidation behavior are unchanged; the new request child is the matching outbound path.

### 2026-06-30 - B006 narrowed empty-emitter callback

- Before: `86/90`, metadata still listed [UID:0000MZ][RankingDialog](by-file/RankingDialog.md) as canonical owner/emitter even though the prose owner was [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), and formal C++ was blank.
- Changed to: `87/91`, `CANONICAL_OWNER:0000BP`, `EMITTER_UIDS:0000BP`, and formal C++ now contains the accepted no-code proof.
- Summary/evidence: current MCP request id `2015` reconfirmed opcode `0x7d`/subcommand `5`, selected-category validation through `g_pRankingDialog`, reward-entry count parsing, `0x230` reward-entry append/copy, temporary nested item-vector cleanup, invalidate/redraw behavior, and handled-status return. The body belongs to `RankingRewardInfoDialog`, but packet-context/listener interfaces, selected-category accessor surface, reward-entry vector member/API, and invalidation call shape remain the concrete source-body blockers.

### 2026-06-04 - Live IDA handler refresh and parent assignment

- Before: score was `70/85`, reconstructable/autogen metadata was blank, and the page did not record the live vtable-slot evidence.
- Changed to: score is now `86/90`, marked reconstructable, attached to [UID:0000MZ][RankingDialog](by-file/RankingDialog.md), and removed stale source-output wording.
- Summary/evidence: live IDA confirms exact range `0x0045ddd0-0x0045df8c`, secondary vtable slot `0x00610c34`, opcode `0x7d` subcommand `5`, category-id validation through [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md), parser call `0x0045debf -> 0x0045d7e0`, append/copy helpers `0x0045ec50`/`0x0045ef50`, cleanup helper `0x0045f160`, and surrounding `0xcc` padding.
