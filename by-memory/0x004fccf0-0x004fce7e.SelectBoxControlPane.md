*** UID:00019V | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
enum
{
    kSelectBoxControlType = 8,
    kSelectBoxMouseDownEvent = 3,
    kControlPaneVisualStateNormal = 10
};

SelectBoxControlPane::SelectBoxControlPane(const RectBounds *bounds)
    : ControlPane(kSelectBoxControlType, bounds),
      m_selected(false)
{
    SetMode(1);
}

bool SelectBoxControlPane::IsSelected() const
{
    return m_selected;
}

void SelectBoxControlPane::SetSelected(bool selected)
{
    if (m_selected == selected)
        return;

    m_selected = selected;
    InvalidateRect(&m_bounds);
}

bool SelectBoxControlPane::OnMouseEvent(const EventRecord *event)
{
    if (event != NULL &&
        event->type == kSelectBoxMouseDownEvent &&
        PointInRect(event->y, event->x, &m_bounds) &&
        !m_selected)
    {
        m_selected = true;
        InvalidateRect(&m_bounds);
    }

    return true;
}

void SelectBoxControlPane::OnPaint()
{
    RectBounds frame = m_bounds;

    SetDrawColor(0);
    DrawBackgroundRect(&m_bounds);

    if (!m_selected && m_visualState == kControlPaneVisualStateNormal)
        return;

    SetDrawColor(4);
    DrawRectFrame(&frame);
    InsetRect(&frame, 1, 1);
    DrawRectFrame(&frame);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Coherent SelectBoxControlPane constructor/accessor/setter/mouse/paint source island with exact `bool m_selected` at `+0x108`, complete size `0x10c`, three vtable views, seven NewUser setter callers, Button/choice-control source route, and next-owner boundary. Shared compiler entry UID0002XW corroborates layout but is consumed equally by Button, Select, Weather, and Rain and is not SelectBox-owned source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fccf0-0x004fce7e SelectBoxControlPane

## Status

- Disposition: reconstructable `SelectBoxControlPane` method island.
- Direct owner/emitter: [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md), emitted through [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md).
- Adjacent aggregate: [UID:00019U][0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs](by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md).
- Next owner boundary: [UID:0002Q8][0x004fce80-0x004fd520.UserShapeSelectControlPaneCore](by-memory/0x004fce80-0x004fd520.UserShapeSelectControlPaneCore.md).
- Reconstruction status: formal first-draft C++ is populated because the active code-entry gate is met and B002's 2026-06-18 source-quality pass resolved the selected-state field, receiver adjustment, source placement, and split policy.

## Method Inventory

| Address range | Source-facing name | Evidence and role |
| --- | --- | --- |
| `0x004fccf0-0x004fcd67` | `SelectBoxControlPane::SelectBoxControlPane(const RectBounds *bounds)` | Single-argument constructor; forwards the incoming bounds pointer and control type `8` to `ControlPane::ControlPane`, writes all SelectBox vtable views, clears `m_selected`, calls `Pane::SetMode(1)`, and returns with `ret 4`. |
| `0x004fcd70-0x004fcd77` | `bool SelectBoxControlPane::IsSelected() const` | Loads `al` from `this+0x108` and returns. |
| `0x004fcd80-0x004fcda4` | `void SelectBoxControlPane::SetSelected(bool selected)` | Compares the argument against `m_selected`; on change, writes the byte and invalidates inherited bounds at `this+0x44` through primary vtable slot `+0x20`. |
| `0x004fcdb0-0x004fcdf7` | `bool SelectBoxControlPane::OnMouseEvent(const EventRecord *event)` | EventHandler secondary-view method; filters event type `3`, hit-tests event coordinates against inherited bounds, adjusts back to the primary receiver, sets `m_selected`, invalidates, and returns true. |
| `0x004fce00-0x004fce7e` | `void SelectBoxControlPane::OnPaint()` | Sets draw color `0`, calls the shared background/surface rectangle callback through `dword_69B3FC`, and draws a double frame when selected or when inherited visual state is not `10`. |

`OnMouseEvent` is preferred over `OnMouseDown` because the vtable entry is an EventHandler virtual and the body filters type `3` internally. `HandleMouseDown` remains acceptable prose only if the event-record receiver shape is preserved.

## Selected-State Field

The SelectBox-specific field at `this+0x108` (`+264` decimal) is the boolean selected flag, best named `m_selected`.

- Constructor write: `0x004fcd4b` clears `[esi+0x108]`.
- Accessor: `0x004fcd70` returns only `[ecx+0x108]`.
- Setter: `0x004fcd80` compares and writes only `[ecx+0x108]`, then invalidates when changed.
- Mouse/event handler: `0x004fcdd8-0x004fcde3` uses `[esi+0x68]` relative to the EventHandler receiver, which resolves to `(this+0xa0)+0x68 == this+0x108`, then writes `[ecx+0x108] = 1`.
- Paint: `0x004fce2a` gates frame drawing on `[esi+0x108] != 0` before checking inherited visual state.
- Object size: the shared deleting entry [UID:0002XW][0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor](by-memory/0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor.md) pushes `0x10c` on its bit-4 branch, placing `+0x108` at the SelectBox-specific tail. Direct callee analysis proves this is a one-byte no-op size branch, not vector-element destruction.

Rejected alternatives:

- `selectedByte` / `byte_108`: stale low-level names; the role is a boolean selected state.
- `m_checked`: possible UI vocabulary, but no checkbox resource, check-mark draw, or toggle-off behavior is present.
- `m_pressed` / `m_hot`: rejected because inherited `ControlPane` visual state at `+0x103` carries transient visual state.
- Host index/current choice: rejected because the field never stores an index and higher-level selection-group coordination happens in caller code.

## Host/Callback Negative Finding

No host-selection pointer, callback member, or selection-group field is read by this target.

- `0x004fcdb0-0x004fcdf7` reads the EventHandler secondary receiver, event fields at `+4/+8/+0xc`, inherited bounds at primary `+0x44`, and selected state at primary `+0x108`.
- The only outbound call after a successful hit is inherited invalidation through the primary vtable slot `+0x20`.
- There is no load from an owner dialog pointer, callback table, action id, sibling list, or control id field in the mouse/event method.
- Seven direct setter callers in NewUserMisc perform group reset/selection orchestration outside `SelectBoxControlPane`.

This resolves the old host/callback blocker as negative evidence rather than an unknown SelectBox field.

## Vtables And Receiver Adjustment

`SelectBoxControlPane` uses the inherited `ControlPane`/`Pane` multi-view layout:

| Object offset | Vtable | Role |
| --- | --- | --- |
| `+0x00` | `0x0061d4c0` | Primary `ControlPane`/`SelectBoxControlPane` view. |
| `+0xa0` | `0x0061d528` | `EventHandler` secondary view. |
| `+0xa4` | `0x0061d558` | Tertiary inherited/default handler view. |

Constructor stores:

```asm
0x004fcd30  mov dword ptr [esi],     0x0061d4c0
0x004fcd3a  mov dword ptr [esi+0xa0], 0x0061d528
0x004fcd44  mov dword ptr [esi+0xa4], 0x0061d558
```

Vtable evidence:

- `0x0061d4c0` starts with shared deleting entry `0x0049b0d0`; Button, Weather, and Rain primary vtables point to the same address, so this is layout/compiler evidence rather than SelectBox ownership.
- `0x0061d504` points to `0x004fce00`, this page's paint virtual.
- `0x0061d528` points to adjustor thunk `0x0049af11`.
- `0x0061d52c` points to `0x004fcdb0`, this page's EventHandler mouse/event virtual.
- `0x0061d558` points to adjustor thunk `0x0049af1c`, followed by inherited/default handler `0x00544e90`.

The mouse/event method receives `ecx == this+0xa0`. Its `lea eax, [esi-0x5c]` reaches inherited bounds because `(this+0xa0)-0x5c == this+0x44`; later `lea ecx, [esi-0xa0]` converts back to the primary receiver before writing `m_selected` and invalidating.

## Helper Roles

| Address/global | Source-facing role | Evidence/status |
| --- | --- | --- |
| `0x004949e0` | `ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)` | Constructor passes type `8` and the incoming bounds pointer. |
| `0x005446b0` | `Pane::SetMode(unsigned char mode)` | Constructor calls it with `1` after vtable setup. |
| `0x004b7e80` | `PointInRect` | Mouse/event method passes event coordinates and inherited bounds. Preserve the tested coordinate order from existing geometry docs. |
| `0x004b9660` | `SetDrawColor` / GrafPort draw-state setter | Paint calls with `0`, then `4`. |
| `dword_69B3FC` | shared background/surface rectangle callback | Paint calls it after setting draw color `0`; it is not a SelectBox member. Use the canonical support-doc name when one is recovered. |
| `0x004ba450` | `DrawRectFrame` | Paint calls it twice around a one-pixel rect adjustment. |
| `0x004b7e30` | `InsetRect` / signed rect adjust by `(1, 1)` | Paint copies bounds to a local rect, draws, adjusts, and draws again. |

The exact original source spelling for the shared Event/GrafPort/Rect helpers remains support-level work, but behavior is clear enough for first-draft C++ with descriptive names.

## NewUserMisc Caller Relationship

The seven direct calls to `0x004fcd80` occur in the NewUserMisc dialog action/update code around `0x004fc010`:

- `0x004fc4f5`
- `0x004fc502`
- `0x004fc51f`
- `0x004fc52c`
- `0x004fc541`
- `0x004fc54e`
- `0x004fc55b`

The surrounding code obtains child controls by id from a child/control manager at `this+0x1fc` and uses `SetSelected` to clear competing selections. These call sites prove NewUserMisc is a consumer coordinating a selection group; they do not move ownership or imply a hidden SelectBox callback field.

## Shared Compiler Destructor Relationship

- UID0002XW contains no `m_selected` access, Select vptr restore, member cleanup, or class-specific resource release. It always calls Pane teardown and performs hidden deleting-flags dispatch.
- Select's primary vtable `0x0061d4c0` points directly to UID0002XW. Its `+0xa0` and `+0xa4` vtable views at `0x0061d528` and `0x0061d558` point to the two UID0002XV adjustors.
- The equivalent Button, Weather, and Rain primary/adjusted slots establish four separate class consumers of one class-neutral `0x10c` body. Best explanation is compiler COMDAT/identical-code folding.
- The shared wrapper confirms complete-object size and destructor compatibility, but it does not move owner/emitter routing away from [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md) for this actual method island.
- Keep the class's likely implicit virtual destructor source shape. Do not add a speculative explicit destructor or hand-author UID0002XW/UID0002XV ABI glue in this formal block.

## Source Placement And Split Decision

Keep [UID:00019V] owned by [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md), emitted through [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md) as part of the generic button/choice control family.

Rejected placements:

- NewUserMisc / `UserCreateAppearanceControls.cpp`: rejected because caller locality and construction use are the only supporting facts; this range has generic vtables and no new-user resource dependency.
- New `ChoiceControlPane.cpp` now: plausible as a future source-tree refinement, but there is no current source evidence requiring a new file route.
- Direct `ControlPane.cpp`: rejected because this class writes SelectBox vtables and owns `m_selected`.
- WeatherLayer/Raining ownership: rejected; the relation is shared adjustor/scalar-deleting glue already modeled by [UID:0002XV][0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks](by-memory/0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks.md) and [UID:0002XW][0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor](by-memory/0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor.md).

Do not split [UID:00019V] into per-method child pages now. The constructor, accessor, setter, EventHandler mouse-event virtual, and paint virtual are short, coherent methods with one owner/emitter and one state field. Destructor/thunk ABI glue is already represented by [UID:0002XV][0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks](by-memory/0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks.md) and [UID:0002XW][0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor](by-memory/0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor.md).

## First-Draft C++ Notes

- `EventRecord::type` maps to event offset `+4`; coordinate fields map to `+8/+0xc` in the order consumed by `PointInRect`.
- `m_bounds` is inherited at `+0x44`; do not add it as a SelectBox field.
- `m_visualState` is inherited `ControlPane` state at `+0x103`; do not model it as a SelectBox-specific byte.
- `DrawBackgroundRect` names the `dword_69B3FC` callback until a canonical rendering-support name is recovered.
- `InsetRect` names the signed rect-adjust helper at `0x004b7e30`; keep the raw address in support notes until the shared geometry name is final.

## Remaining Uncertainties

These are support-level uncertainty caps, not blockers for the `88/91` target score or first-draft C++:

- Exact original spelling for `EventRecord`, event type enum, and coordinate field names.
- Exact original spelling for the shared `dword_69B3FC` background/surface callback.
- Exact original spelling for `0x004b7e30`.
- Possible future `ChoiceControlPane.cpp` source split.

## Score Rationale

`COMPLETION:88` reflects exact method inventory, boundaries, vtable views, selected-state field, receiver adjustment, helper roles, seven NewUserMisc setter callers, negative host/callback finding, source-placement decision, no-split decision, support implications, and populated first-draft C++.

`CONFIDENCE:91` is justified because PE disassembly, existing live IDA-backed docs, vtable data, direct setter callers, shared deleting-entry object-size evidence, and adjacent owner boundaries all agree. UID0002XW corroborates the layout without conferring wrapper ownership. Confidence stays below final-audit level because shared Event/GrafPort/Rect names and final source-tree split are still inferred/descriptive.

## Cross-References

- [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md)
- [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md)
- [UID:00019U][0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs](by-memory/0x004fb630-0x004fe782.NewUserMiscShapeAndPasswordDialogs.md)
- [UID:0002Q8][0x004fce80-0x004fd520.UserShapeSelectControlPaneCore](by-memory/0x004fce80-0x004fd520.UserShapeSelectControlPaneCore.md)
- [UID:00025Q][0x0061d264-0x0061e5b4.MainMenuNewUserReadOnlyData](by-memory/0x0061d264-0x0061e5b4.MainMenuNewUserReadOnlyData.md)
- [UID:0002XV][0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks](by-memory/0x0049af11-0x0049af27.SelectBoxControlPaneAdjustorThunks.md)
- [UID:0002XW][0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor](by-memory/0x0049b0d0-0x0049b10b.SelectBoxControlPaneScalarDeletingDestructor.md)

## Changes

- 2026-07-23 B002 UID0002XW source-quality callback:
  - Score, owner/emitter, range, methods, and formal C++ unchanged.
  - Clarified that UID0002XW corroborates `0x10c` layout but is shared equally by four class families and is not SelectBox-owned source.
  - Corrected stale vector-delete wording to the proved bit-4 no-op size branch and preserved implicit-destructor/no-handwritten-ABI source shape.
- 2026-06-18 B002 source-quality execution:
  - Changed score from `82/90` to `88/91`.
  - Kept owner/emitter [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md).
  - Populated formal first-draft C++ for constructor, selected accessor, selected setter, EventHandler mouse-event virtual, and paint virtual.
  - Resolved `+0x108` as `bool m_selected`.
  - Recorded the no-host-callback negative finding, vtable/receiver adjustment model, helper roles, seven NewUserMisc setter callers, source placement through ButtonControlPane, no-split decision, rejected alternatives, and remaining support-level uncertainty caps.
- 2026-06-06 parent attachment and live IDA refresh:
  - Before: reconstructable but unassigned, scored `76/88`, and lacked direct setter caller, vtable-store, destructor-callee, and parent-readiness evidence.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:90`, and parent [UID:0000CQ][SelectBoxControlPane](by-class/SelectBoxControlPane.md).
  - Evidence: live IDA MCP rechecked exact method boundaries, constructor vtable stores, selected byte `+264`, seven direct new-user setter callers, vtable-only mouse/paint refs, destructor wrapper shape, callees, and the immediate boundary before [UID:0002Q8][0x004fce80-0x004fd520.UserShapeSelectControlPaneCore](by-memory/0x004fce80-0x004fd520.UserShapeSelectControlPaneCore.md).
