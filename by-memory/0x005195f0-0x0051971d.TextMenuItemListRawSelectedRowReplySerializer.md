*** UID:0004HE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ET | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000ET | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void TextMenuItemList::SendSelectedTextMenuReply()
{
    if (m_selectedIndex < 0)
        return;

    const TextMenuItemListEntry *entry =
        static_cast<const TextMenuItemListEntry *>(GetItem(m_selectedIndex));

    SendTextMenuReply(entry->itemId);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained TextMenuItemList::SendSelectedTextMenuReply selected-row wrapper; source obtains the selected entry and calls private SendTextMenuReply(itemId), while compiler output inlines that serializer into this retained no-route body and transitively into live vtable-routed TextMenuDialog::OnControlCommand. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005195f0-0x0051971d TextMenuItemListRawSelectedRowReplySerializer

## Status

- Disposition: retained ordinary source wrapper `TextMenuItemList::SendSelectedTextMenuReply()`. Source performs selected-row lookup and calls private [UID:0004HF][0x00519720-0x0051983a.TextMenuItemListRawExplicitIdReplySerializer](by-memory/0x00519720-0x0051983a.TextMenuItemListRawExplicitIdReplySerializer.md) `SendTextMenuReply(entry->itemId)`; the compiler inlines UID0004HF's serializer into this standalone retained body and then transitively into live UID0004HC.
- Owner and emitter: [UID:0000ET][TextMenuItemList](by-class/TextMenuItemList.md), placed in [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md) after `AddTextEntry` and immediately before the retained explicit-id helper UID0004HF.
- Address range: half-open `[0x005195f0,0x0051971d)`, size `0x12d` / 301 bytes.
- ABI: ordinary receiver-only `void __thiscall`; `ECX` carries `TextMenuItemList *this`, there is no logical stack argument, and the normal path ends in plain `retn`.
- Source channels: this by-memory page owns the definition in its CPP channel. The H channel stays blank because the public declaration belongs to UID0000ET's file-local class declaration.

## Exact Range, Boundaries, And Identity

| Region | Half-open range | Size | SHA256 | Disposition |
| --- | --- | --- | --- | --- |
| leading alignment | `[0x005195e8,0x005195f0)` | `0x08` | `3D2F424783DF5853C8D7121B1371650C04241F318E1B0CD46BEDBC805B9164C3` | eight `0xcc` bytes; outside target |
| target body | `[0x005195f0,0x0051971d)` | `0x12d` | `94B5D776499D730276233ECDDBD03FCAC7CD73258CF8B0FEC82868A2EDD16C8A` | retained ordinary method body |
| trailing alignment | `[0x0051971d,0x00519720)` | `0x03` | `E668F0A1B72560CED14C54488A25335D11336448950BB6485267EBC5EB3FECEE` | three `0xcc` bytes before UID0004HF |

The body has its own prologue, security-cookie sequence, normal return, and compiler range-failure tail. It is not an interior slice of UID0004HC or UID0004HF, and neither padding region belongs to the function.

## Behavior And Packet Contract

1. Read inherited `m_selectedIndex` at `ListPane +0x134`; return without sending when it is negative.
2. Call `ListPane::GetItem(m_selectedIndex)` and treat the returned row as `TextMenuItemListEntry`, whose offset `+0` field is unsigned 16-bit `itemId`. The earlier `GetSelectedEntry` label was stale and is superseded.
3. Source-call `SendTextMenuReply(entry->itemId)`.
4. The compiler-observed retained body then contains UID0004HF's inlined behavior: initialize a `0x100`-byte packet/cursor, append opcode `0x39`, `m_menuType` from `+0x14c`, big-endian `m_menuId` from `+0x150`, and big-endian selected `itemId`.
5. The inlined helper follows non-owning `m_ownerDialog` at `+0x154`; when owner `m_hasExtraString` at `+0x279` is true, it appends owner `m_extraString` at `+0x27c` through the ANSI length-8 writer.
6. The inlined helper preserves the `<0x100` cursor guard, writes local `packet[cursor] = 0` without increasing transmitted length, and sends through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / `QueueAndSendPacket` using the pre-terminator cursor cast to signed 16-bit length.

The formal C++ now preserves the recovered source boundary: selected lookup remains here, while packet construction resides in UID0004HF. Security-cookie code, the range-failure helper, raw stack offsets, generated `sub_` labels, and the unused `GetMemoryMan` return remain compiler/decompiler artifacts rather than developer-authored source.

## Active Inline Liveness Evidence

- [UID:0004HC][0x00519210-0x005194b1.TextMenuDialogOnDialogAction](by-memory/0x00519210-0x005194b1.TextMenuDialogOnDialogAction.md), whose current source identity is `TextMenuDialog::OnControlCommand`, contains the complete active inline expansion at `[0x0051938f,0x00519492)`, size `0x103`, SHA256 `23788D4E9028044606F1B0BBF9C19F80E34F4EEBF966B09B474A57ED4813734C`.
- The expansion repeats the target's selected-index read, `GetItem`, row-id load, opcode and numeric field order, optional owner string, cursor guard, local terminator, and socket send before the dialog closes.
- The containing action-handler body SHA256 is `5A392B4E39A6AB238D3B14D9761719AA7D100BF6549FDFE197F3D40B8796F860` and is live through vtable data xref `0x0061ecf4 -> 0x00519210`.
- This is stronger source-liveness evidence than a direct retained-copy call: it proves the same developer-level operation is on an active virtual path while explaining why optimization can leave an unreferenced out-of-line copy.

The nested helper stage map is exact. UID0004HE/UID0004HF pairs are opcode `0x00519636/0x0051974e`, menu type `0x00519659/0x00519771`, menu id `0x0051967a/0x00519792`, item id `0x00519698/0x005197b4`, owner guard `0x005196b6/0x005197d2`, optional append `0x005196d9/0x005197f5`, cursor guard `0x005196ec/0x00519808`, terminator `0x005196f5/0x00519811`, and send `0x00519704/0x00519820`. UID0004HC repeats the same serializer at `0x005193c0`, `0x005193e3`, `0x00519404`, `0x00519422`, `0x0051943f`, `0x00519462`, `0x00519475`, `0x0051947e`, and `0x0051948d`. This three-way alignment supports nested source calls rather than handwritten duplicate serialization.

## Negative Route Evidence

- IDA does not currently model a function at `0x005195f0`; direct decompilation fails for that reason rather than because the bytes lack a coherent body.
- The target start has zero inbound xrefs, exact set `{}`. Every one of the target's 80 instruction heads has zero external inbound xrefs, exact set `{}`.
- No target or sibling VA, RVA, raw-offset, code, data, or immediate pointer pattern was found.
- TextMenuItemList primary vtable `0x0061ed48` and its related views do not point to UID0004HE or UID0004HF; these helpers are nonvirtual.
- No original symbol proves the exact lexical spelling `SendSelectedTextMenuReply`, the inferred friendship, or local variable names.
- No direct call instruction survives between the retained UID0004HE and UID0004HF copies. Exact three-way stage alignment and UID0004HE's unique selected-row prefix nevertheless make the nested helper call the highest-probability source factoring; the absence of a surviving call is retained as confidence-capping inlining evidence rather than a reason to duplicate source.

This negative evidence remains material and caps confidence, but it no longer supports blank emission. Zero direct xrefs do not prove dead source when the exact behavior survives in a live compiler-inline expansion.

## Ownership And Source Placement

- Receiver layout, inherited selection state, `m_menuType`, `m_menuId`, `m_ownerDialog`, physical adjacency to the constructor and `AddTextEntry`, and the corroborating explicit-id sibling all identify `TextMenuItemList` as the direct class owner.
- `TextMenuDialog` is a support/friend/caller owner only. It contributes the private optional-string fields and the active inline caller path, but the target receiver and list-local packet context reject dialog ownership of the definition.
- `ListPane`, PacketBuffer, and Socket are dependencies, not owners: none owns the menu context, owner pointer, selected-row policy, or physical source cluster.
- Do not create a standalone `TextMenuItemList.cpp`. Both constructor callers and the owner relationship place this private helper class in `TextMenuDialogs.cpp`.
- UID0004HF is the distinct retained private `SendTextMenuReply(unsigned short itemId)` body with one logical word argument and `retn 4`. UID0004HE owns selected-row policy and delegates packet serialization to that helper at source level.

## Historical Assumptions And Corrections

- The 2026-07-04 B009 split correctly created this exact child, established its padding and zero-direct-route state, and retained it under UID0000ET. Those observations remain valid.
- B009's resulting blank-C++ inference is superseded by the later target-specific all-head xref audit, exact range/body hashes, and the complete active inline expansion in UID0004HC.
- Earlier prose named the row lookup `ListPane::GetSelectedEntry`. Current ListPane typing and call semantics establish `GetItem(m_selectedIndex)`; the old name is retained here only as a corrected historical assumption.
- The earlier claim that emission would necessarily invent a callsite is false. Reconstructed source now uses a normal class call in UID0004HC, while the compiler-observed action-handler bytes remain explained as an inline expansion.

## Evidence Summary

- Positive: complete application-specific body; exact receiver/row/packet semantics; physical class clustering; retained UID0004HF explicit-id helper; exact three-way nested-inline stage alignment; vtable-routed containing handler; matching support APIs and field layout.
- Negative: no current function object, direct xref, pointer encoding, vtable cell, original symbol, or preserved pre-optimization factoring.
- Decision: emit the compact selected-row wrapper through UID0000ET at `93/94`, calling UID0004HF's retained private helper. Exact lexical spelling and the missing direct call instruction remain bounded confidence limitations, not source blockers.

## Changes

- 2026-08-21 B002 Gate 2A repair: removed stale copied `proposed` claim-mapping rows. The exact wrapper bytes, selected-row behavior, zero-direct-route evidence, nested inline liveness proof, source factoring, and generated disposition remain authoritative here and in the UID0000OP report ledger.
- 2026-08-15 B006 accepted UID0004HF support callback:
  - Replaced the duplicated full serializer formal source with the exact selected-index/GetItem wrapper and `SendTextMenuReply(entry->itemId)` call while preserving metadata `93/94`, owner/emitter UID0000ET, and blank H.
  - Retained the complete packet/send behavior as compiler-observed nested-inline evidence and added exact UID0004HE/UID0004HF/live-UID0004HC stage alignment, source factoring, source order, and negative direct-route rationale.
  - Historicalized the earlier self-contained-source decision after UID0004HF's independent target pass resolved it as the retained explicit-id helper.
- 2026-08-14 B005 accepted UID0004HE implementation callback:
  - Changed metadata from `86/91`, blank emitter, and blank CPP to `93/94`, emitter UID0000ET, and the exact retained `SendSelectedTextMenuReply()` definition; H remains blank by design.
  - Incorporated exact ABI, behavior, packet order, owner fields, range/padding hashes, all-head zero-xref audit, pointer/vtable exclusions, active UID0004HC inline liveness, ownership, source order, compiler exclusions, and source-factoring rationale.
  - Preserved B009's valid split and zero-route evidence while historicalizing only the obsolete blank-emitter and stale `GetSelectedEntry` conclusions.
- 2026-07-04 B009 callback: created the exact raw-helper child from the accepted UID0001BN split/container report and documented its then-known blank/no-route disposition.

## B002 UID0000OP Accepted Whole-File Reconciliation

Retained wrapper SHA256 94B5D776499D730276233ECDDBD03FCAC7CD73258CF8B0FEC82868A2EDD16C8A; selected-row lookup and nested inline liveness.

### Exact Accepted Inventory Disposition

| ID | Exact identity / range | O/E/P | Historical pre-callback -> accepted current score and metadata | CPP/H/compiler and generated disposition | Destination / claim |
| --- | --- | --- | --- | --- | --- |
| I022 | retained `TextMenuItemList::SendSelectedTextMenuReply [0x005195f0,0x0051971d)`, SHA256 `94B5D776499D730276233ECDDBD03FCAC7CD73258CF8B0FEC82868A2EDD16C8A` | O `0000ET`; E `0000ET`; P `0000ET` | UID0004HE stays `93/94`; true; position follows class `20` | child CPP; no H; D25850 retained body present, callback changes only caller spelling as specified | UID0004HE / C036 |

### Exact Caller / Xref Disposition

| ID | Exact inbound code set/count | Exact inbound data set/count | Exact outgoing call disposition |
| --- | --- | --- | --- |
| I022 | `{}` / 0 | `{}` / 0 | targets `{4f3dc0,516030,575380,5753f0,5753a0,584540,5757d0,574bb0,5c772f,5c7655}` |

The formal source channel above is authoritative. Older generated names and body-only sketches are dated provenance; compiler vtables, RTTI, adjustor thunks, and deleting wrappers are regenerated from ordinary source and are not handwritten. This is first-party NexusTK source, so no third-party import directive applies.
