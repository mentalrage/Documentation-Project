*** UID:0003G3 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N0,0000OI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: UTF-16LE `Leave` button label with CANONICAL_OWNER:NONE and EMITTER_UIDS:0000N0,0000OI; B002 current Goal 2 recheck plus B003 current/fresh/post-migration and earlier B001 passes confirm exact bytes, `.rdata` file offset `0x22191c`, exactly one UTF-16 `Leave` spelling, no ASCII spelling, no refs to false interior `aAve`/`0x00622f20` or interiors `0x00622f22`/`0x00622f24`/`0x00622f26`, no target RVA refs, no `0x00622f54` boundary refs, source/debug/PDB/source-path negatives, successor Region-vtable evidence only after the target/map at `0x00622f58`, and exactly four source-use refs paired with [UID:0003G4][0x00622f28-0x00622f3c.SharedReconnectWideString](by-memory/0x00622f28-0x00622f3c.SharedReconnectWideString.md) from `ConnectionClosedDialog` constructors, `ReconnectDialog::OnButtonClick`, and `TerminalPane::OnDisconnect`; [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) and [UID:0000OI][TerminalPane](by-file/TerminalPane.md) are emitters only, while [UID:0000HE][AlertPanes](by-file/AlertPanes.md), class-level parents, the mixed map, and a new shared-label owner are rejected as canonical owners. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00622f1c-0x00622f28 Shared Leave Wide String

## Range

- Address range: `0x00622f1c-0x00622f28`.
- Size: `0x0c` / 12 bytes. Verified with `int_convert.py`.
- Entity kind: `string-data`.
- Rebuild handling: source-authored UTF-16 string literal pooled into `.rdata`; recreate the literal at each real source use site, not as a hand-authored address or standalone global.
- Containing map: [UID:0003CW][0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap](by-memory/0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap.md) `0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap`.

## Bytes And Decoding

IDA byte review on 2026-06-12 shows the target bytes:

```text
0x00622f1c: 4c 00 65 00 61 00 76 00 65 00 00 00
```

This decodes as UTF-16LE `Leave`. IDA currently names the start `off_622F1C` because the first four UTF-16 bytes resemble a pointer value; that is a false data type, not a real pointer table entry. `entity_query strings` does not list this UTF-16 literal as a string, while `get_bytes` over `0x00622f1c-0x00622f54` shows the adjacent pooled island as:

```text
Leave\0, Reconnect\0, No\0, 0x0000 pad, Yes\0, baram\0, 0x0000 pad
```

IDA's current names inside the island are also offset-biased by the decode failure: `aAve` at `0x00622f20`, `aConnect` at `0x00622f2c`, `aNo` at `0x00622f3c`, and `aBaram_0` at `0x00622f4c`.

## Xrefs And Ownership

Live IDA xrefs on 2026-06-12:

| Address | Owner evidence | Use |
| --- | --- | --- |
| `0x00553c3f` | [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) `ConnectionClosedDialog` constructor variant | Passes the label pair to the alert initializer. |
| `0x00553cef` | [UID:00038J][0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods](by-memory/0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods.md) `ConnectionClosedDialog` constructor path | Passes the label pair to the alert initializer. |
| `0x00554359` | [UID:00038L][0x00553f40-0x005544b8.ReconnectDialogMethods](by-memory/0x00553f40-0x005544b8.ReconnectDialogMethods.md) `ReconnectDialog::OnButtonClick` candidate | Constructs a replacement connection-closed dialog inline. |
| `0x0058b5ac` | [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) / [UID:0000EG][TerminalPane](by-class/TerminalPane.md) `TerminalPane::OnDisconnect` | Builds the reconnect/leave alert callback path from the terminal pane. |

The literal is shared by `ReconnectDialog.cpp`/`ConnectionClosedDialog` paths and `TerminalPane.cpp`. Both source families use the same pooled `Leave` bytes as an alert button label.

Representative disassembly checked on 2026-06-12 shows all four sites pass `Leave` and [UID:0003G4][0x00622f28-0x00622f3c.SharedReconnectWideString](by-memory/0x00622f28-0x00622f3c.SharedReconnectWideString.md) as a pair into the alert/message builder:

```text
0x00553c3f: push offset off_622F1C
0x00553c44: push offset off_622F28
0x00553c49: push dword_67A740
0x00553c4f: push 9Ah
0x00553c54: call sub_4F0350
0x00553c5c: call sub_49FEB0

0x00553cef: push offset off_622F1C
0x00553cf4: push offset off_622F28
0x00553cf9: push [ebp+arg_0]
0x00553cfc: push 9Ah
0x00553d01: call sub_4F0350
0x00553d09: call sub_49FEB0

0x00554359: push offset off_622F1C
0x0055435e: push offset off_622F28
0x00554363: push dword_67A740
0x00554369: push 9Ah
0x0055436e: call sub_4F0350
0x00554376: call sub_49FEB0

0x0058b5ac: push offset off_622F1C
0x0058b5b1: push offset off_622F28
0x0058b5b6: push esi
0x0058b5b7: push ebx
0x0058b5b8: push 9Dh
0x0058b5bd: call sub_4F0350
0x0058b5c5: call sub_4A0690
```

`trace_data_flow 0x00622f1c backward max_depth=2` reaches exactly the same four code xrefs, with no additional direct owner discovered.

## Assignment Decision

`CANONICAL_OWNER` remains `NONE`. The child is source-authored and clears the documentation gate, but no single direct owner owns all observed use sites. The viable source-use emitters checked are [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) `ReconnectDialog` and [UID:0000OI][TerminalPane](by-file/TerminalPane.md) `TerminalPane`; the shared literal-pooling evidence is the blocker for canonical ownership, not a lack of candidate modules.

Strict routing audit:

- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) clears `85/85` and covers the `ConnectionClosedDialog` and `ReconnectDialog::OnButtonClick` use sites, but it does not own the `TerminalPane::OnDisconnect` use at `0x0058b5ac`.
- [UID:0000OI][TerminalPane](by-file/TerminalPane.md) clears `85/85` and covers the `TerminalPane::OnDisconnect` use site, but it does not own the `ConnectionClosedDialog`/`ReconnectDialog` use sites.
- Class-level candidates [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), [UID:0000BR][ReconnectDialog](by-class/ReconnectDialog.md), and [UID:0000EG][TerminalPane](by-class/TerminalPane.md) each cover only part of the xref set. None is the actual direct parent for the pooled literal as a whole.

Emitter routing: keep `CANONICAL_OWNER:NONE`, but route generated output through `EMITTER_UIDS:0000N0,0000OI` so the pooled button label can surface in both proven source-use file contexts.

## B001 Rule 26 Ownership Audit

Agent-B001 Batch `B001-0003G3` audited this item as a pooled source literal, not as a missing shared global. The useful result is kept here with the later emitter-route refinement: the B001 report recommended no canonical parent and no standalone shared-label owner; subsequent owner/emitter model updates keep that no-owner decision while allowing `ReconnectDialog.cpp` and `TerminalPane.cpp` to emit their own real source-use literals through `EMITTER_UIDS:0000N0,0000OI`.

Evidence checked:

- Live IDA MCP session `b001_nexustk` reported `NexusTK.exe`, image base `0x400000`, Hex-Rays ready, and strings cache ready.
- `get_bytes 0x00622f1c size 56` decoded the adjacent island as UTF-16LE `Leave`, UTF-16LE `Reconnect`, UTF-16LE `No`, alignment, UTF-16LE `Yes`, ANSI `baram`, and alignment.
- The exact target bytes are one complete UTF-16LE literal: `4c 00 65 00 61 00 76 00 65 00 00 00`.
- IDA string/name state is misleading here: `get_string` saw only `"L"` at `0x00622f1c` and `"ave"` at `0x00622f20`; `entity_query` named the false interior as `aAve`. These are decode artifacts, not separate source objects.
- The complete UTF-16LE `Leave\0` pattern and the unterminated UTF-16LE `Leave` spelling each occur exactly once at `0x00622f1c`. ASCII `Leave\0` does not occur.
- Absolute VA bytes for `0x00622f1c` occur exactly at the four operand sites `0x00553c40`, `0x00553cf0`, `0x0055435a`, and `0x0058b5ad`. The false interior VA `0x00622f20` has no operand hits. The sibling `Reconnect` VA `0x00622f28` has the paired four operand hits at `0x00553c45`, `0x00553cf5`, `0x0055435f`, and `0x0058b5b2`.
- No RVA-form reference, table initializer, independent global, or broad source module was found for the physical `Leave` cell.
- The 2026-06-14 current no-owner sweep independently rechecked the live generated state and found `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N0,0000OI`, no target-page score change, no split/merge/reclassification need, no IDA-safe-name repair, no target-RVA form, no hidden interior refs at `0x00622f20/22/24/26`, no `0x00622f54` boundary refs, no source/debug/PDB breadcrumb, and no common alert/shared-label declaration owner. Those negatives keep the item as a valid no-owner-with-emitters pooled literal rather than a non-emitting defect.

Caller and function-level evidence:

- `sub_553C10` / [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) passes `&off_622F28` and `&off_622F1C` into the alert initializer and publishes `g_pConnectionClosedDialog`.
- `sub_553CC0` / [UID:00038J][0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods](by-memory/0x00553cc0-0x00553e5b.ConnectionClosedDialogMethods.md) repeats the same `Reconnect`/`Leave` label-pair setup on the alternate constructor path.
- `sub_554210` / [UID:00038L][0x00553f40-0x005544b8.ReconnectDialogMethods](by-memory/0x00553f40-0x005544b8.ReconnectDialogMethods.md) uses the same label pair on the replacement connection-closed path; the surrounding reconnect logic also formats/sends the sibling ANSI `baram` packet literal.
- `sub_58B470` / [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) constructs a callback object, stores [UID:0001JD][0x0058b620-0x0058b64f.TerminalPaneReconnectLeaveCallback](by-memory/0x0058b620-0x0058b64f.TerminalPaneReconnectLeaveCallback.md) at `0x0058b56d`, and then passes the same `Reconnect`/`Leave` pair to its alert builder.
- `sub_58B620` dispatches the terminal prompt result: nonzero reconnects through the terminal helper path, while zero calls the application leave/shutdown helper. This proves TerminalPane independently owns one semantic source use of the text, even though it does not own the pooled physical literal.

Rejected ownership routes:

- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) is the strongest consumer if a single owner were forced because it covers three of four xrefs and the reconnect/connection-closed prompt family, but it does not own `TerminalPane::OnDisconnect`.
- [UID:0000OI][TerminalPane](by-file/TerminalPane.md) owns the terminal prompt and callback route, but it covers only one of the four xrefs.
- [UID:0000HE][AlertPanes](by-file/AlertPanes.md) owns generic alert infrastructure, not this feature-specific reconnect/terminal label pair. There is no broad button-label fan-in comparable to a generic `OK` string.
- Class-level owners such as [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), [UID:0000BR][ReconnectDialog](by-class/ReconnectDialog.md), and [UID:0000EG][TerminalPane](by-class/TerminalPane.md) each cover only part of the xref set and would be narrower but still wrong for the complete physical range.
- New shared-label files or globals such as `ReconnectTerminalLabels.cpp`, `SessionRecoveryLabels.cpp`, `SharedDialogLabels.cpp`, or a synthetic `g_reconnectLeaveLabels` object were rejected. No address-taken table, initializer, symbol, source-family grouping, or metadata supports a hand-authored shared declaration.

Final source guidance:

- Treat `0x00622f1c` as the compiler/linker pooled result of ordinary source literals. Final C++ should spell `L"Leave"` at the `ConnectionClosedDialog`, `ReconnectDialog`, and `TerminalPane::OnDisconnect` prompt-construction sites unless future source/debug evidence proves a real shared constant.
- Do not create a standalone global, by-global page, or direct C++ emission for this child. The page stays reconstructable so the literal is not lost, but the direct C++ block stays blank because the literal belongs at the consuming source statements.
- If future debug/PDB/map/source evidence proves a real shared constants table, revisit [UID:0003G3], [UID:0003G4][0x00622f28-0x00622f3c.SharedReconnectWideString](by-memory/0x00622f28-0x00622f3c.SharedReconnectWideString.md), and [UID:0003G7][0x00622f4c-0x00622f54.SharedBaramPacketLiteral](by-memory/0x00622f4c-0x00622f54.SharedBaramPacketLiteral.md) together.

## B002 Current Goal 2 Recheck

Agent-B002's current Goal 2 no-owner pass rechecked the same target in IDA MCP session `b001_0003gy` and an independent raw PE scan. This pass does not change the metadata, but it is retained here because it strengthens the source-quality justification for the no-owner/two-emitter route:

```text
database: b001_0003gy
input: E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe
idb: E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64
module: NexusTK.exe
imagebase: 0x00400000
auto-analysis: ready
Hex-Rays: ready
strings cache: ready
```

The report used `server_health`, `get_bytes`, `get_string`, `xref_query`, `lookup_funcs`, `find_bytes`, `make_signature_for_range`, `trace_data_flow`, `decompile`, `analyze_component`, and `search_text`; then it independently mapped the target through the raw PE sections of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`.

Raw PE placement:

| Item | Value |
| --- | --- |
| Target VA | `0x00622f1c` |
| Section | `.rdata` |
| File offset | `0x22191c` |
| Bytes | `4c 00 65 00 61 00 76 00 65 00 00 00` |
| Decoding | UTF-16LE `Leave` |

Relevant PE section map from the B002 scan:

```text
.text  0x00401000-0x0060c600 raw 0x400-0x20ba00
.rdata 0x0060d000-0x0066c200 raw 0x20ba00-0x26ac00
.data  0x0066d000-0x0069ce24 raw 0x26ac00-0x278400
.rsrc  0x0069d000-0x006b2e00 raw 0x278400-0x28e200
```

The current IDA decoding remains offset-biased: `0x00622f1c` is decoded as `L`, `0x00622f20` as false interior suffix `ave`, `0x00622f28` as `R`, `0x00622f2c` as false interior suffix `connect`, `0x00622f3c` as real successor `No`, `0x00622f44` as `Y`, and `0x00622f4c` as real ANSI successor `baram`. These names are listing artifacts; the target is the complete `Leave` literal.

B002 raw and IDA scans agree on the source-use and negative-reference matrix:

| Pattern or address | Count / result |
| --- | --- |
| UTF-16LE `Leave\0` | one hit at `0x00622f1c` |
| UTF-16LE `Leave` without terminator | one hit at `0x00622f1c` |
| ASCII `Leave\0` | zero hits |
| target VA dword `0x00622f1c` | four hits at operand bytes `0x00553c40`, `0x00553cf0`, `0x0055435a`, `0x0058b5ad` |
| `push 0x00622f1c` | four instructions at `0x00553c3f`, `0x00553cef`, `0x00554359`, `0x0058b5ac` |
| target RVA dword `0x00222f1c` | zero hits |
| false interior `0x00622f20` | zero hits |
| interiors `0x00622f22`, `0x00622f24`, `0x00622f26` | zero hits |
| sibling start `0x00622f28` | four operand hits paired with this target |
| `push 0x00622f28` | four instructions at `0x00553c44`, `0x00553cf4`, `0x0055435e`, `0x0058b5b1` |
| successor boundary `0x00622f54` | zero hits |

`xref_query` and `search_text("off_622F1C")` both returned exactly four real code references:

| Xref | Function | Source-use meaning |
| --- | --- | --- |
| `0x00553c3f` | `sub_553C10`, size `0xaf` | `ConnectionClosedDialog` constructor variant uses `Leave` with paired `Reconnect`. |
| `0x00553cef` | `sub_553CC0`, size `0xae` | alternate `ConnectionClosedDialog` constructor path uses the same label pair. |
| `0x00554359` | `sub_554210`, size `0x1f6` | `ReconnectDialog::OnButtonClick` replacement connection-closed path uses the same label pair. |
| `0x0058b5ac` | `sub_58B470`, size `0x170` | `TerminalPane::OnDisconnect` constructs a reconnect/leave prompt with the same label pair. |

Filtered decompiler evidence preserved by the B002 pass:

```text
0x00553c10:
v2 = sub_4F0350(154);
sub_49FEB0(v2, v4, &off_622F28, &off_622F1C);

0x00553cc0:
v3 = sub_4F0350(154);
sub_49FEB0(v3, a2, &off_622F28, &off_622F1C);

0x00554210:
v8 = sub_4F0350(154);
sub_49FEB0(v8, v11, &off_622F28, &off_622F1C);

0x0058b470:
v10[2] = sub_58B620;
v11 = sub_4F0350(157);
sub_4A0690(v11, v5, v10, &off_622F28, &off_622F1C);

0x004a0690:
sub_49FEB0(a2, a3, a5, a6);
```

`analyze_component` over `0x00553c10`, `0x00553cc0`, `0x00554210`, `0x0058b470`, `0x004a0690`, and `0x0049feb0` found shared globals `off_622F1C` and `off_622F28` in the four feature-use functions. It also confirmed `sub_4A0690` and `sub_49FEB0` are alert infrastructure callees. That infrastructure consumes caller-supplied labels; it does not own this feature text.

The report's ranked ownership conclusion is unchanged:

- `CANONICAL_OWNER:NONE` with `EMITTER_UIDS:0000N0,0000OI` is accepted because current structure rules allow compiler/linker-pooled literals to remain ownerless while routing source-use emission through every proven source root.
- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) is an emitter, not canonical owner, because it explains three refs but not the `TerminalPane::OnDisconnect` ref.
- [UID:0000OI][TerminalPane](by-file/TerminalPane.md) is an emitter, not canonical owner, because it explains the terminal prompt and callback route but not the reconnect/connection-closed refs.
- [UID:0000HE][AlertPanes](by-file/AlertPanes.md) is rejected because the alert constructors receive caller labels and should not own every feature-specific button label.
- Class-level pages, the physical [UID:0003CW][0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap](by-memory/0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap.md), containing aggregate pages, and a new shared-label/constants source are rejected because no table, initializer, source/debug name, local static, or coherent constants module is visible.
- Split, merge, or non-reconstructable reclassification is rejected because the range is already exactly one UTF-16LE source literal plus terminator, has no referenced interiors, and has two valid source-use emitter routes.

## B003 Current And Fresh No-Owner Rechecks

Agent-B003 independently rechecked the same no-owner route after the B001/B002 passes. Those reports are mostly duplicate with the accepted current state, but they add the following retained evidence:

- IDA MCP session `b001_0002bd` and later session `a001_goal2_class_batch` both reported `NexusTK.exe`, image base `0x00400000`, Hex-Rays ready, auto-analysis ready, and a ready strings cache with `2067` strings.
- `make_signature_for_range(0x00622f1c-0x00622f28, wildcard_operands=false)` returned the unique target signature `4C 00 65 00 61 00 76 00 65 00 00 00`.
- `entity_query(strings, 0x00622f1c-0x00622f60)` sees only the ANSI `baram` string in this short range, demonstrating why IDA's string cache is weaker than direct bytes/xrefs for the adjacent wide literals.
- `entity_query(names, 0x00622f1c-0x00622f60)` reports false/interior names `aAve`, `aConnect`, `aNo`, `aBaram_0`, and the successor `??_7Region@@6B@` name at `0x00622f58`; the successor Region vtable evidence starts outside this target and outside [UID:0003CW][0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap](by-memory/0x00622f1c-0x00622f54.ReconnectTerminalCopyStringTailMap.md).
- `xrefs_to(0x00622f58)` has one successor-region xref at `0x005546c1`/operand bytes around `0x005546c3`; that is not a reason to extend this child or the string-tail map.
- `find_regex` for `Leave`, `Reconnect`, `ConnectionClosed`, `TerminalPane`, `AlertPane`, `.pdb`, `.cpp`, and `RSDS` found expected RTTI/type strings and the imported `LeaveCriticalSection`, but no PDB/source-path/debug or shared-label declaration evidence for this literal.
- B003 raw PE passes recorded the same target raw offset `0x0022191c` and an executable hash trail (`sha256 9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632` in the fresh pass; `MD5 4247e04e20b65d6414c7238aa8ff5515` in the post-migration pass). These identify the same inspected `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` image used for the byte and immediate scans.
- The B003 post-migration report originally observed an older blank-`EMITTER_UIDS` state and recommended adding `0000N0,0000OI`. That specific action is superseded by the current target state, which already has the two emitter routes and keeps direct C++ blank.

The B003 reports therefore close as incorporated/superseded without changing the current metadata: `89/94`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N0,0000OI`, blank direct C++.

## Cross-References

- [UID:000269][0x00622d24-0x00622f54.QueueReconnectCopyReadOnlyData](by-memory/0x00622d24-0x00622f54.QueueReconnectCopyReadOnlyData.md) containing Queue/Reconnect/Copy read-only-data aggregate.
- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) reconnect/connection-closed dialog source file.
- [UID:0000OI][TerminalPane](by-file/TerminalPane.md) terminal pane source file.
- [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md) terminal pane executable aggregate.

## Changes

- 2026-06-12 Agent-A002 Batch 263: Created as an exact child split from the former mixed string-tail page. Evidence comes from live IDA byte/xref/decompile checks and `int_convert.py` size verification.
- 2026-06-12 Agent-A002 Batch 328: Raised `86/91` to `88/93` after a fresh live IDA MCP refresh against `b001_nexustk`: `get_bytes` reconfirmed the full `0x38`-byte surrounding island, `xrefs_to` and `trace_data_flow` confirmed exactly four direct `Leave` consumers, and disassembly snippets proved all consumers pair `off_622F1C` with `off_622F28` before alert/dialog construction. Parent remains blank because the verified xref set crosses the ReconnectDialog/ConnectionClosedDialog and TerminalPane source families.
- 2026-06-12 Agent-B001 Batch B001-0003G3: Performed ownership/source-family audit. Live IDA MCP against `b001_nexustk` reconfirmed the exact UTF-16LE `Leave` bytes, exactly one full UTF-16 spelling, no ASCII spelling, exactly four absolute-immediate consumers, zero refs to false interior `aAve` at `0x00622f20`, and the paired `Reconnect` xref set. `ReconnectDialog`, `TerminalPane`, `AlertPanes`, class-level parents, and a new shared-label/source owner were ranked and rejected as overclaiming direct ownership of a pooled literal. Parent remains blank; final source should spell `L"Leave"` at each semantic use site.
- 2026-06-13 B003 post-migration recheck: retained `CANONICAL_OWNER:NONE` because the UTF-16 `Leave` literal is materially shared by reconnect/connection-closed code and `TerminalPane::OnDisconnect`, and no single declaration owner or modeled shared-label source file is proven. Added emitter routing `EMITTER_UIDS:0000N0,0000OI` because the current owner/emitter model allows no-owner pooled literals to emit through every proven source-use file root.
- 2026-06-13 B001 second-pass audit: kept `89/94`, `CANONICAL_OWNER:NONE`, and `EMITTER_UIDS:0000N0,0000OI`. Live IDA MCP and raw PE checks reconfirmed the exact UTF-16LE `Leave` bytes, exactly four source-use refs paired with `Reconnect`, no ASCII spelling, no false-interior `0x00622f20` refs, no target RVA refs, no single declaration owner across ReconnectDialog-family and TerminalPane use sites, and no split, merge, reclassification, or IDA repair need.
- 2026-06-14 B001 current no-owner sweep: kept the same `89/94` metadata and no-owner/two-emitter route after a fresh `b001_0003gy` IDA MCP pass. The sweep verified the current generated no-owner row, the emitter-table route through [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) and [UID:0000OI][TerminalPane](by-file/TerminalPane.md), the absence of hidden ASCII/RVA/interior/boundary references or debug/source breadcrumbs, and the rejection of `ReconnectDialog`, `TerminalPane`, `AlertPanes`, class-level pages, the mixed tail map, and synthetic shared-label files as canonical owners. No coverage-row replacement or direct C++ emission is required; final source should continue spelling `L"Leave"` at each consuming prompt-construction statement.
- 2026-06-14 B001 live Goal 2 no-owner pass: independently reconfirmed the same state in IDA MCP session `a001_goal2_class_batch`. `make_signature_for_range(0x00622f1c, 0x00622f28)` produced the unique signature `4C 00 65 00 61 00 76 00 65 00 00 00`; `tools/int_convert.py` verifies the target as `0x0c` bytes and sibling [UID:0003G4][0x00622f28-0x00622f3c.SharedReconnectWideString](by-memory/0x00622f28-0x00622f3c.SharedReconnectWideString.md) as `0x14` bytes. The pass also recorded that `0x00622f54` has no refs, while the successor-region xref at `0x00622f58` belongs outside this target and outside the tail map; this supports keeping the current exact child boundary and no-owner/multi-emitter model.
- 2026-06-22 B002 current Goal 2 no-owner pass: kept `89/94`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N0,0000OI`, and blank direct C++. The B002 pass rechecked the live `b001_0003gy` IDA MCP state and raw PE image, recorded target file offset `0x22191c`, confirmed the full `.rdata` placement, exact one-spelling UTF-16 `Leave` result, four `push 0x00622f1c` refs paired with four `push 0x00622f28` refs, no ASCII/RVA/interior/boundary refs, filtered decompiler call shapes through `sub_49FEB0`/`sub_4A0690`, and the same rejected ReconnectDialog, TerminalPane, AlertPanes, class-level, physical-container, and synthetic shared-label owners. No split, merge, IDA repair, coverage change, or direct C++ emission is required.
- 2026-06-22 B003 current/fresh/post-migration no-owner reports: verified the current state from `b001_0002bd` and `a001_goal2_class_batch`, preserving the same exact bytes, four source-use refs, paired sibling refs, no target RVA/interior/ASCII/boundary refs, source/debug/PDB/source-path negatives, and the `0x00622f58` Region-vtable successor boundary. The post-migration report's older blank-emitter recommendation is superseded by current `EMITTER_UIDS:0000N0,0000OI`; no C++ or owner change is required.
