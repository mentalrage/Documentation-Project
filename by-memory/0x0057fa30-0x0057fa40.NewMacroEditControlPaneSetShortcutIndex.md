*** UID:0004A9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewMacroEditControlPane::SetShortcutIndex(int shortcutIndex)
{
    m_shortcutIndex = shortcutIndex;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 16-byte NewMacroEditControlPane setter storing the signed 0..19 configured-shortcut ordinal at complete-object +0x108, with one NewMacroDialog page-load caller, number/Ctrl-number paint consumers, MacroDialogs ownership, exact boundaries, and complete source C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057fa30-0x0057fa40 NewMacroEditControlPane SetShortcutIndex

## Status

- Address range: `0x0057fa30-0x0057fa40` half-open.
- Direct owner/emitter: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) `NewMacroEditControlPane`.
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Rebuild handling: source-authored nonvirtual setter; emit the formal C++ above from the MacroDialogs source route.

## Evidence

- Fresh mandatory IDA MCP evidence was collected while database session `64c11373` was healthy: `idb_list`, `server_health`, a real target lookup, and corrected bounded target bytes all succeeded. This records evidence-time health rather than claiming indefinite session availability.
- The exact half-open range is `[0x0057fa30,0x0057fa40)`, size `0x10` / 16 bytes. Exact bytes are `55 8B EC 8B 45 08 89 81 08 01 00 00 5D C2 04 00`; SHA256 is `8DC87C61978B33D8F886DDCB12B75FE359AEE24ED3A6083F4342DF6F99A9AD5F`. The exact sequence and generated stable signature are unique in the executable.
- The function is one six-instruction basic block with cyclomatic complexity 1: establish the EBP frame, load the one stack argument from `[EBP+8]`, store the dword unchanged to `[ECX+0x108]`, restore EBP, and return with `retn 4`. It is an x86 `thiscall` with the complete `NewMacroEditControlPane` receiver in ECX, one four-byte stack argument, and no receiver adjustment.
- Hex-Rays can expose the argument value in EAX at return because EAX transports the value into the store. The sole caller ignores it and overwrites EAX immediately at `0x0053f274`; sibling setters use the same lowering. The source contract is therefore `void`, not a value-returning API.
- The destination is a four-byte `int` at complete-object offset `+0x108`. The caller sign-extends the row-derived value before addition and OnPaint performs a signed comparison against 10, excluding byte, word, unsigned, pointer, enum, record-object, and record-pointer interpretations.
- The complete `+0x108` lifecycle is closed: UID0004A8 constructor initializes it, this setter rewrites it during page changes, and UID0004AD OnPaint reads it three times. Input, key, refresh, save, teardown, and vtable methods do not read or write it.
- The sole direct target call is `0x0053f26f` in UID00049O `NewMacroDialog::LoadMacroPage`. The caller computes `page * 10 + row`, calls this setter, then performs its next child lookup at `0x0053f274`, SetKey at `0x0053f27e`, SetSpellMode at `0x0053f28d`, and Refresh at `0x0053f294`. Two pages and ten rows establish the exact live signed domain `0..19`.
- UID0004AD interprets values `0..9` as number shortcuts `1..9,0` and values `10..19` as `Ctrl+1..Ctrl+9,Ctrl+0`, including the two exact zero-key special cases. The field is thus the configured shortcut ordinal, not a `CompactShortcutRecord` identity.
- Direct xrefs comprise one code call and zero data xrefs. Raw absolute-pointer and RVA searches found no target pointer matches. There is no vtable cell, data route, adjustor, compiler thunk, split, second caller, or target-owned internal/trailing padding.
- The target lies in executable `.text` at raw file offset `0x0017ee30`. UID0004A8 ends at `0x0057fa2b`; five `0xcc` alignment bytes occupy `[0x0057fa2b,0x0057fa30)` with SHA256 `992CF51486F901AA1AA7D1F63E18C150C9156166E08E0B9E59CC3FAF98177329`. UID0004AA begins exactly at `0x0057fa40`; the separate `[0x0057fa52,0x0057fa60)` pad follows UID0004AA and is not target padding.

## Source Quality And Naming

- `SetShortcutIndex` / `m_shortcutIndex` / `shortcutIndex` are the strongest source-facing names because the value selects one of the 20 configured number/Ctrl-number shortcuts and matches the runtime shortcut-index vocabulary. Exact private-symbol spelling is inferred, not original-symbol proof.
- Historical `SetRecordIndex` / `m_recordIndex` is superseded because the member is never consumed as a compact-record identity outside the load caller's lookup expression. `SetIndex` is too generic; `macroSlot` conflates the distinct integrated macro-hotkey layout; `hotkeyIndex` is plausible but weaker than the established shortcut vocabulary.
- `CompactShortcutRecord` is the caller's storage source, not this member's type. A bounds check, normalization, explicit calling-convention annotation, raw offset, vptr manipulation, compiler glue, or decompiler temporary would invent source behavior and is intentionally absent from the human C++.
- The public setter is required because the caller is the distinct `NewMacroDialog` class and no friendship evidence exists. UID000095 remains the direct class owner/emitter and UID0000KY MacroDialogs remains the file route.

## Score Rationale

- Completion rises from 86 to 92 because the exact function body, range, bytes/hash/signature, ABI, source return and parameter/member type, caller/order, full field lifecycle, value domain, boundaries, owner/emitter, source placement, source-authored disposition, naming, negative routes, rejected alternatives, and complete C++ are now documented.
- Confidence rises from 90 to 94 because binary behavior, all direct calls and field uses, layout, and route converge independently. The remaining cap is exact original private method/member spelling and unavailable original source text, not an implementation blocker.
