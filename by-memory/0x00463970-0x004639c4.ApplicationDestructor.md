*** UID:0002GZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// No first-draft body yet: Application::~Application cleanup ordering depends on the unresolved
// Application base/member declaration for StringBase, MSGHandler, g_pApplication, and LObject teardown.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact Application destructor with handle/string/MSGHandler/global cleanup side effects documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00463970-0x004639c4 Application Destructor

## Scope

- Range: `0x00463970-0x004639c4`
- Entity kind: class destructor
- Owner: [UID:00000D][Application](by-class/Application.md)
- Parent aggregate: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)
- Disposition: reconstructable project code.
- Rebuild handling: `source-authored`; recreate as the ordinary `Application` destructor, with compiler cleanup thunks and vtable artifacts produced by the C++ compiler from the final class declaration.

## Signature And ABI

- IDA models the body as `void __thiscall sub_463970(int this)`.
- `ecx` is the `Application` object. The IDA `int` type is not a final class type.
- There are no explicit stack arguments and no returned source value.
- The function ends with a tail jump to [UID:0000KV][LObject](by-file/LObject.md) destructor cleanup at `0x004f4a90`.

## Behavior

Destroys the process-level `Application` object after startup/shutdown code has already run. The destructor:

1. Restores the two `Application` vtable slots at object offsets `0x0` and `0x4`.
2. Reads the closeable handle at `Application + 0x24`; if non-null, calls `CloseHandle` and stores `0` back to `Application + 0x24`.
3. Releases the string-like member at `Application + 0x854` through `sub_582B70`.
4. Releases the string-like member at `Application + 0x1c` through `sub_582B70`.
5. Destroys the embedded/default [UID:0001CN][0x00528d60-0x00528e55.MSGHandler](by-memory/0x00528d60-0x00528e55.MSGHandler.md) subobject at `Application + 0x4` through `sub_528D80`.
6. Clears [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md) / `dword_67AB1C`.
7. Tail-jumps to `LObject::~LObject` at `0x004f4a90`, which restores the base `LObject` vtable.

The body does not call `Application::CleanupResources`; resource-manager and subsystem teardown are handled by the separate cleanup/shutdown methods before this object destructor runs.

## IDA Evidence

- B009 current implementation callback evidence uses MCP session `279422f0` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready. `lookup_funcs` reports `0x0046396e` is not a function, `0x00463970 -> sub_463970` with size `0x54`, `0x004639c4` is not a function, and successor `0x004639d0 -> sub_4639D0`, giving exact half-open range `0x00463970-0x004639c4` / 84 bytes (Verified with MCP `int_convert`: `0x54` = 84).
- B009 `analyze_function` / `decompile` on session `279422f0` models the body as `void __thiscall sub_463970(int this)`, size 84, four basic blocks, cyclomatic complexity 2, callers `_WinMain@16` and `sub_4F66C0`, and callees `CloseHandle`, `sub_582B70`, and `sub_528D80`. The decompile writes Application vtables at `this+0` and `this+4`, conditionally closes `*(this+36)`, calls `sub_582B70((void **)(this+2132))` and `sub_582B70((void **)(this+28))`, calls `sub_528D80((_DWORD *)(this+4))`, clears `unk_67AB1C = 0`, and tail-calls `sub_4F4A90`.
- B009 current disassembly confirms the exact instruction order: `0x00463974 mov eax,[esi+24h]`, vtable stores at `0x00463977` and `0x0046397d`, `CloseHandle` at `0x00463989`, zeroing `Application+0x24` at `0x0046398f`, StringBase release calls at `0x0046399c` and `0x004639a4`, embedded/default `MSGHandler` destructor call at `0x004639ac`, `g_pApplication` clear at `0x004639b4`, and final `jmp sub_4F4A90` at `0x004639bf`.
- B009 current `xrefs_to 0x00463970` reports three references: `_WinMain@16` call `0x004f5e44`, cleanup thunk `0x004f66b0`, and [UID:0002Q2][0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor](by-memory/0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor.md) call `0x004f66c6`. `xrefs_to` for Application vtable constants `0x006125e4` and `0x00612604` reports constructor stores, this destructor's stores, and later stores in the Application scalar deleting destructor variant.
- B009 current `get_bytes 0x00463960` confirms two `0xcc` bytes immediately before the target, the full 84-byte destructor body, and twelve `0xcc` bytes at `0x004639c4-0x004639d0` before `sub_4639D0`. `get_bytes 0x004f66a0` confirms the neighboring cleanup thunk bytes at `0x004f66b0`, secondary adjustor bytes at `0x004f66b5`, three `0xcc` bytes before UID0002Q2, and post-wrapper alignment.
- B009 current helper decompiles confirm the no-code proof: `sub_528D80` is `MSGHandler::~MSGHandler` and calls `sub_465650((_DWORD *)unk_67AB1C, this[1])`, so `g_pApplication` must still be valid during MSGHandler teardown; `sub_582B70` forwards to the StringBase wide release helper; `sub_4F4A90` writes the `LObject` vtable. The verified order is therefore StringBase release, `MSGHandler::~MSGHandler`, `g_pApplication = 0`, then `LObject` tail cleanup.
- Live IDA MCP `lookup_funcs` on 2026-06-06 reports:
  - [UID:0002GY][0x00463310-0x0046396e.ApplicationConstructor](by-memory/0x00463310-0x0046396e.ApplicationConstructor.md) starts at `0x00463310` with size `0x65e`;
  - `0x0046396e` is not a function;
  - `sub_463970` starts at `0x00463970` with size `0x54`, giving exact range `0x00463970-0x004639c4`;
  - `0x004639c4` is not a function;
  - `sub_4639D0` starts at `0x004639d0`.
- Disassembly at `0x0046396e` shows `align 10h` before this destructor, and `0x004639c4` aligns to `0x004639d0` before `Application::Initialize`.
- `analyze_funcs` reports these decompiled side effects:

```c
void __thiscall sub_463970(int this)
{
  void *v2;

  v2 = *(void **)(this + 36);
  *(_DWORD *)this = &Application::`vftable';
  *(_DWORD *)(this + 4) = &Application::`vftable';
  if ( v2 )
  {
    CloseHandle(v2);
    *(_DWORD *)(this + 36) = 0;
  }
  sub_582B70((void **)(this + 2132));
  sub_582B70((void **)(this + 28));
  sub_528D80((_DWORD *)(this + 4));
  dword_67AB1C = 0;
  sub_4F4A90((_DWORD *)this);
}
```

- Current B001 implementation MCP refresh session `80de0a67` independently rechecked the [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md) clear in this body: `0x004639b4 mov dword ptr unk_67AB1C, 0`, with decompiler `unk_67AB1C = 0` after the embedded/default `MSGHandler` and string-member teardown.
- The exact body disassembly is:

```asm
463970  push    esi
463971  mov     esi, ecx
463973  push    edi
463974  mov     eax, [esi+24h]
463977  mov     dword ptr [esi], offset ??_7Application@@6B@
46397d  mov     dword ptr [esi+4], offset ??_7Application@@6B@_0
463984  test    eax, eax
463986  jz      short loc_463996
463988  push    eax
463989  call    ds:CloseHandle
46398f  mov     dword ptr [esi+24h], 0
463996  lea     ecx, [esi+854h]
46399c  call    sub_582B70
4639a1  lea     ecx, [esi+1Ch]
4639a4  call    sub_582B70
4639a9  lea     ecx, [esi+4]
4639ac  call    sub_528D80
4639b1  pop     edi
4639b2  mov     ecx, esi
4639b4  mov     dword_67AB1C, 0
4639be  pop     esi
4639bf  jmp     sub_4F4A90
```

- `callees 0x00463970` reports exactly `CloseHandle` external import `0x0060d110`, `sub_528D80`, and `sub_582B70`; the base cleanup is a tail jump to `sub_4F4A90`.
- `sub_528D80` decompiles as the `MSGHandler` destructor path: it writes the `MSGHandler` vtable and restores the previous handler through `Application::ExchangeMSGHandler` / `sub_465650(dword_67AB1C, this[1])`.
- `sub_582B70` is the pointer-backed wide-string release wrapper documented by [UID:0002RN][0x005829f0-0x00582d80.StringBaseShareAssignAndAccess](by-memory/0x005829f0-0x00582d80.StringBaseShareAssignAndAccess.md).
- `sub_4F4A90` decompiles to `*this = &LObject::vftable`, matching the [UID:0000KV][LObject](by-file/LObject.md) base cleanup notes.
- `xrefs_to` for the two `Application` vtable constants stored here, `0x006125e4` and `0x00612604`, shows constructor stores at `0x0046338c` / `0x00463392`, destructor stores at `0x00463977` / `0x0046397d`, and later stores in `0x00467230`.

## Caller And Wrapper Context

- `callers 0x00463970` reports two direct callers:
  - [UID:00019G][0x004f5c80-0x004f5f17.WinMain](by-memory/0x004f5c80-0x004f5f17.WinMain.md) at `0x004f5e44`;
  - [UID:0002Q2][0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor](by-memory/0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor.md) at `0x004f66c6`.
- The `_WinMain@16` normal path calls `Application::Startup`, `Application::RunMessageLoop`, and `Application::Shutdown`, then falls through to the destructor:

```asm
4f5e78  call    sub_4F5FB0
4f5e83  call    sub_464CD0
4f5e8e  call    sub_4F6490
4f5e93  jmp     short loc_4F5E3E
4f5e3e  lea     ecx, [ebp+var_1A88]
4f5e44  call    sub_463970
```

- The `_WinMain@16` early-exit/update path calls `Application::RequestExit` at `0x004f5e39`, then destroys the same stack `Application` object at `0x004f5e44`.
- `xrefs_to 0x00463970` also reports issued compiler child [UID:0004R1][0x004f66b0-0x004f66b5.WinMainBaramAppCleanupThunk](by-memory/0x004f66b0-0x004f66b5.WinMainBaramAppCleanupThunk.md), whose entire body is `jmp sub_463970`; IDA associates its sole inbound edge with the `_WinMain@16` compiler cleanup context at `0x00600fc6`. Treat UID0004R1 as cleanup support, not a separate source-authored destructor.
- Evidence-time MCP database `46dda60b` places UID0004R1 at exact range `0x004f66b0-0x004f66b5`, size 5, bytes `e9 bb d2 f6 ff`, SHA256 `0505B9DEE2A5853919270FF7492420AF7218237D70A193234D113A4FD822A222`. WinMain EH loads its stack `BaramApp` local with `lea ecx,[ebp+var_1A88]` at `0x00600fc0` and jumps to the child at `0x00600fc6`; no ordinary caller, callee entry, data/pointer reference, or vtable route exists.
- Boundary evidence keeps eleven `0xcc` bytes at `0x004f66a5-0x004f66b0`, then UID0004R1, then distinct [UID:0002Q1][0x004f66b5-0x004f66bd.BaramAppDeletingDestructorThunk](by-memory/0x004f66b5-0x004f66bd.BaramAppDeletingDestructorThunk.md). UID0004R1 is executable compiler support, not padding and not part of the secondary adjustor.
- [UID:0002Q2][0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor](by-memory/0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor.md) delegates to this body, then applies MSVC scalar-delete flags and conditionally frees the object through project delete.
- 2026-06-29 B010 MCP session `b6b3c97e` reconfirmed `0x004f66c6` as the UID0002Q2 call into this destructor and `xrefs_to 0x00463970` as `_WinMain@16`, the `0x004f66b0` cleanup thunk, and the UID0002Q2 wrapper call. UID0002Q2's new marker covers only the compiler wrapper behavior; this page remains the ordinary source-authored `Application` destructor body.

## Reconstruction Notes

- Keep the destructor attached to [UID:00000D][Application](by-class/Application.md). The direct WinMain stack-object destructor, BaramApp deleting-destructor wrapper, constructor/destructor vtable stores, and `g_pApplication` clear all agree with Application ownership.
- This target now carries a formal comment-only no-code marker rather than a blank block. The marker is intentional: the destructor is source-owned and emitting, but no executable first-draft body is safe until the `Application` base/member declaration accounts for StringBase, embedded `MSGHandler`, `g_pApplication`, and `LObject` cleanup ordering.
- Do not write `g_pApplication = NULL;` in an ordinary destructor body yet. A normal C++ destructor body statement would run before compiler-generated member/base teardown, while the binary clears `g_pApplication` after `MSGHandler::~MSGHandler()` and before the `LObject` tail cleanup. `MSGHandler::~MSGHandler()` itself calls through `g_pApplication->ExchangeMSGHandler(m_previousHandler)`, so moving the clear earlier would change behavior.
- Do not hand-write explicit calls to StringBase, `MSGHandler`, or `LObject` destructors inside this method. Those would be decompiler-shaped subobject/base cleanup and can double-destruct once the class declaration is modeled.
- Do not inline or hand-write UID0004R1 or scalar-deleting-destructor behavior into this ordinary destructor; those should fall out of WinMain stack-object lifetime, the class declaration, and compiler-generated destructor support. An explicit Application base-destructor call, EH helper, tail jump, raw vptr store, or deleting flag would be compiler-shaped and may double cleanup.
- Do not add subsystem cleanup calls here. This destructor only closes the handle at `+0x24`, releases two string-like members, tears down the embedded/default message handler, clears the singleton pointer, and tail-calls base cleanup.
- Rejected alternatives from the B009 audit: full executable destructor body, partial handle-only body, manual member/base destructor calls, clearing `EMITTER_UIDS`, `RECONSTRUCTABLE:FALSE`, moving ownership away from Application, or using a BaramApp covered-by marker. UID0002Q2 is only the compiler wrapper over this ordinary destructor; it does not cover this source-owned body.

## Touched State

| State | Access | Evidence |
| --- | --- | --- |
| `Application + 0x0` and `Application + 0x4` vtable slots | Written to Application vtable constants | Stores at `0x00463977` and `0x0046397d`; constructor uses the same vtable constants. |
| `Application + 0x24` closeable handle | Read, conditionally closed, then zeroed | `mov eax, [esi+24h]`, `CloseHandle(eax)`, `mov [esi+24h], 0`; constructor initializes this slot to zero. Preferred inferred source name is `m_hSingleInstanceMutex`; exact original spelling remains unresolved. |
| `Application + 0x854` string-like member | Released | `lea ecx, [esi+854h]`, `call sub_582B70`. |
| `Application + 0x1c` string-like member | Released | `lea ecx, [esi+1Ch]`, `call sub_582B70`; constructor initializes the same member. |
| `Application + 0x4` embedded/default `MSGHandler` | Destroyed/restores previous handler | `lea ecx, [esi+4]`, `call sub_528D80`; linked MSGHandler docs identify the destructor behavior. |
| `dword_67AB1C` / `g_pApplication` | Cleared to zero | Store at `0x004639b4`; B009 session `279422f0` and B001 implementation MCP refresh session `80de0a67` confirm `mov dword ptr unk_67AB1C, 0` / decompiler `unk_67AB1C = 0`; linked global page records constructor publish/fallback and now emits the source-ready definition. The clear is after `MSGHandler::~MSGHandler()` and before `LObject` cleanup. |
| `LObject` base | Tail cleanup | Final instruction `jmp sub_4F4A90`, which writes the `LObject` vtable. |

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `89` | The page records exact current MCP bounds, ABI, decompile/disassembly, field/global side effects, StringBase/MSGHandler/base destructor callees, caller/wrapper context, adjacent alignment, rebuild handling, rejected alternatives, and a formal comment-only no-code marker that resolves the generated empty-emitter defect. Completion stays below final because the complete `Application` class declaration and destructor cleanup factoring are still not source-ready for an executable body. |
| Confidence `92` | Confidence is strong because current MCP session `279422f0` reports a compact exact body, call graph, xrefs, vtable stores, bytes, helper decompiles, and integer conversions that agree with constructor/WinMain/BaramApp/global docs. It remains below final-audit level because exact original field spelling and final class layout are still inferred. |

## Cross-References

- File: [UID:0000HG][Application](by-file/Application.md)
- Class: [UID:00000D][Application](by-class/Application.md)
- Embedded handler class: [UID:00008S][MSGHandler](by-class/MSGHandler.md)
- Constructor pair: [UID:0002GY][0x00463310-0x0046396e.ApplicationConstructor](by-memory/0x00463310-0x0046396e.ApplicationConstructor.md)
- Global singleton: [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md)
- Embedded/default message-handler aggregate: [UID:0001CN][0x00528d60-0x00528e55.MSGHandler](by-memory/0x00528d60-0x00528e55.MSGHandler.md)
- String release helper island: [UID:0002RN][0x005829f0-0x00582d80.StringBaseShareAssignAndAccess](by-memory/0x005829f0-0x00582d80.StringBaseShareAssignAndAccess.md)
- Base object cleanup: [UID:0000KV][LObject](by-file/LObject.md)
- WinMain caller: [UID:00019G][0x004f5c80-0x004f5f17.WinMain](by-memory/0x004f5c80-0x004f5f17.WinMain.md)
- WinMain EH cleanup child: [UID:0004R1][0x004f66b0-0x004f66b5.WinMainBaramAppCleanupThunk](by-memory/0x004f66b0-0x004f66b5.WinMainBaramAppCleanupThunk.md)
- BaramApp deleting-destructor wrapper: [UID:0002Q2][0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor](by-memory/0x004f66c0-0x004f66fb.BaramAppScalarDeletingDestructor.md)

## Changes

- 2026-07-15 B003 UID00019H split support sync:
  - Score, owner/emitter, reconstructable state, exact destructor range, formal marker, and cleanup-order blockers remain unchanged at `89/92`.
  - Replaced the anonymous `sub_4F66B0` support wording with issued UID0004R1 and added exact bytes/hash, sole WinMain EH stack-local route, predecessor/successor topology, negative caller/data/vtable routes, source-cause/callee distinction, and no-handwritten-thunk proof.
- 2026-06-29 B009 ApplicationDestructor empty-emitter implementation:
  - Raised target metadata from `88/91` to `89/92`; owner [UID:00000D][Application](by-class/Application.md), `RECONSTRUCTABLE:TRUE`, emitter [UID:00000D][Application](by-class/Application.md), and blank optional position remain unchanged.
  - Inserted the accepted formal comment-only no-code marker. The destructor remains source-owned/emitting through Application, but an executable body is blocked because a normal destructor body would clear `g_pApplication` before member/base cleanup, while the binary clears it after `MSGHandler::~MSGHandler()` and before `LObject` cleanup.
  - Summary/evidence: accepted B009 report `tools/leaser/Agents/Agent-B009/research/0002GZ-ApplicationDestructor-empty-emitter-source-quality.md`, MCP session `279422f0`, server health OK, exact `0x00463970-0x004639c4` range, size `0x54` / 84, predecessor/successor non-function boundaries, post-target padding `0x004639c4-0x004639d0`, vtable stores, handle close/zero, StringBase releases, MSGHandler teardown, `g_pApplication` clear at `0x004639b4`, `LObject` tail jump, `_WinMain@16` caller, cleanup thunk, UID0002Q2 wrapper call, and generated `Application.cpp` empty-marker cause.
- 2026-06-29 B010 UID0002Q2 cleanup-route support sync:
  - Score unchanged at `88/91`; formal destructor C++ remains blank.
  - Summary/evidence: B010 MCP session `b6b3c97e` reconfirmed `0x004f66c6` as UID0002Q2's call into this body and preserved the distinction between this ordinary `Application` destructor and the BaramApp compiler scalar-deleting wrapper. The UID0002Q2 marker covers wrapper/delete-flag behavior only and does not move this destructor body to BaramApp.
- 2026-06-23 B001 g_pApplication support update:
  - Score unchanged at `88/91`; formal destructor C++ remains blank.
  - Added current MCP-backed proof that this destructor clears [UID:000294][0x0067ab1c-0x0067ab20.g_pApplication](by-memory/0x0067ab1c-0x0067ab20.g_pApplication.md) at `0x004639b4`, and replaced the target-relevant old `95/95` no-code wording with current source-quality blocker wording.
  - Summary/evidence: accepted B001 report `tools/leaser/Agents/Agent-B001/research/000294-g_pApplication-source-quality.md`, implementation MCP refresh session `80de0a67` (accepted report provenance `ff68e691`), destructor disassembly/decompile clear, unchanged class/member/layout/MSGHandler/destructor-factoring blockers, and source-ready global declaration on the exact data page only.

- 2026-06-19 B014 cleanup-thunk support sync:
  - Score unchanged at `88/91`.
  - Summary/evidence: documented exact `0x004f66b0-0x004f66b5` bytes and placement for the jump-only compiler cleanup thunk to this destructor. This does not change `ApplicationDestructor` ownership or C++ eligibility; the real source-owned body remains this ordinary destructor.
- 2026-05-31 initial split:
  - What existed before: the destructor was documented only as a covered-method row inside the broad Application lifecycle aggregate.
  - What it was changed to: created this exact destructor page and attached a range-local C++ draft under `Application`.
  - Summary/evidence: IDA MCP confirms the boundary and destructor side effects listed above.
- 2026-06-06 live IDA refresh:
  - What changed: raised completion/confidence from `75/85` to `88/91`, cleared the below-threshold final C++ block, added exact ABI/decompile/disassembly/caller/callee/touched-state evidence, and separated ordinary destructor behavior from compiler cleanup thunks and scalar-deleting wrapper behavior.
  - Why the score increased: live IDA MCP confirms the exact destructor range, adjacent alignment, handle close/clear branch, string releases, embedded `MSGHandler` destructor call, `g_pApplication` clear, `LObject` tail cleanup, `_WinMain@16` callsite, `BaramApp` deleting-destructor delegation, and thunk context.
