*** UID:0003N9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000038 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000038 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Class-routed raw active-frame helper with exact 0x30-byte source-shaped body after six 0xcc bytes and exhaustive zero function/xref/code-ref/data-ref/immediate/VA/RVA/raw-offset/direct-branch evidence; gates on inherited m_enabled +0x101 and m_controlActive +0x102, then uses bounds +0x44, GrafPort draw mode +0x70, SetDrawColor(0x80), and DrawRectFrame; owner/emitter 000038 and blank formal C++ remain fixed under the no-route/no-declaration proof. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00494c50-0x00494c80 ControlPane Draw Active Frame Helper

## Status

- Address range: `0x00494c50-0x00494c80` half-open.
- Entity kind: raw/unpromoted helper.
- Direct owner: [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane`.
- Likely source file: [UID:0000IG][ControlPane](by-file/ControlPane.md) `ControlPane`.
- Disposition: reconstructable NexusTK UI-core source-shaped helper assigned to the base control class.
- Rebuild handling: `source-authored` candidate.
- Current IDA state: no function object at `0x00494c50`.
- Source-facing role: inferred `ControlPane::DrawActiveFrame` raw active/focused frame drawing helper.
- Autogen status: route through [UID:000038][ControlPane](by-class/ControlPane.md) with formal C++ blank. The helper now has source-quality behavior, field, callee, and owner documentation, but no current function object, entry xref, vtable/table/pointer route, direct branch target, or recovered source declaration proves it should emit as a real method body.

## Boundary Evidence

Live IDA MCP on 2026-06-14 reports `0x00494c50` is not a function. The byte/signature range still shows a complete raw helper ending in `retn` at `0x00494c7f`, immediately before `sub_494C80` begins the [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md) `ProgressBarControlPane` constructor at `0x00494c80`.

Disassembly:

```text
0x00494c50  push esi
0x00494c51  mov esi, ecx
0x00494c53  cmp byte ptr [esi+101h], 0
0x00494c5a  jz short 0x00494c7e
0x00494c5c  cmp byte ptr [esi+102h], 0
0x00494c63  jz short 0x00494c7e
0x00494c65  push 80h
0x00494c6a  call sub_4B9660
0x00494c6f  lea eax, [esi+44h]
0x00494c72  mov byte ptr [esi+70h], 0
0x00494c76  push eax
0x00494c77  mov ecx, esi
0x00494c79  call sub_4BA450
0x00494c7e  pop esi
0x00494c7f  retn
```

## Behavior

The helper checks inherited `m_enabled` at `+0x101` and `m_controlActive` at `+0x102`. If both are nonzero, it selects active draw/fill color `0x80`, clears inherited GrafPort draw/blit mode byte `+0x70` to mode `0`, and calls the GrafPort rectangle-frame drawer for inherited bounds at `this+0x44`. Historical visible/shown wording for `+0x101` is superseded; inherited Pane visibility is a separate byte at `+0xb4`.

## Field And Helper Meanings

| Field/helper | Current source-quality meaning | Notes |
| --- | --- | --- |
| `this+0x101` | inherited `m_enabled` | Paired with [UID:0003N3][0x00494bb0-0x00494bca.ControlPaneEnable](by-memory/0x00494bb0-0x00494bca.ControlPaneEnable.md) and [UID:0003N4][0x00494bd0-0x00494bea.ControlPaneDisable](by-memory/0x00494bd0-0x00494bea.ControlPaneDisable.md), primary slots `+0x4c/+0x50`. Action-button, navigation, ImageButton, and Scrollable consumers prove enablement. Historical visible/show wording is rejected by separate Pane visibility at `+0xb4`. |
| `this+0x102` | `ControlPane` local active/focus state byte, source-facing `m_controlActive` | Paired with [UID:0003N6][0x00494c10-0x00494c2a.ControlPaneDeactivate](by-memory/0x00494c10-0x00494c2a.ControlPaneDeactivate.md) and [UID:0003N7][0x00494c30-0x00494c4a.ControlPaneActivate](by-memory/0x00494c30-0x00494c4a.ControlPaneActivate.md). This is the helper's active-frame gate. |
| `this+0x44` | inherited bounds rectangle, source-facing `m_bounds` | Shared pane/drawing support treats this as the rectangle passed to invalidation and draw-frame helpers. |
| `this+0x70` | inherited GrafPort draw/blit mode byte, source-facing `m_drawMode` | Water/caustic drawing uses mode `2`; this helper restores mode `0` before drawing the frame. It is not a `ControlPane` local boolean. |
| `0x004b9660` | [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md), source-facing `GrafPort::SetDrawColor` | Stores active draw/fill color at `this+0x74`. In this target it is a draw-color setter, not a text-color setter. |
| `0x004ba450` | [UID:000168][0x004ba450-0x004ba53b.GrafPortDrawRectFrame](by-memory/0x004ba450-0x004ba53b.GrafPortDrawRectFrame.md), source-facing `GrafPort::DrawRectFrame` | Consumes the bounds rectangle pointer and inherited draw state to emit the frame. |
| immediate `0x80` | active/focus/frame color index 128 (Verified with `int_convert.py`) | Also appears in progress/frame drawing. No original named palette constant is proven. |

## 2026-06-23 B004 MCP-Backed Source-Quality Recheck

B004 rechecked the target under IDA MCP endpoint `http://127.0.0.1:13337/mcp`, active session `80de0a67`, input `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, image base `0x00400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready. The local PE used for supplemental route scanning was `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`, MD5 `4247e04e20b65d6414c7238aa8ff5515`. Section mapping for that executable: `.text` VA `0x00001000`, virtual `0x00401000-0x0060c600`, raw pointer `0x400`, raw size `0x20b600`, virtual size `0x20b4ac`; `.rdata` VA `0x0020d000`, virtual `0x0060d000-0x0066c200`, raw pointer `0x20ba00`, raw size `0x5f200`; `.data` VA `0x0026d000`, virtual `0x0066d000-0x0069ce24`, raw pointer `0x26ac00`, raw size `0xd800`; `.rsrc` VA `0x0029d000`, virtual `0x0069d000-0x006b2e00`, raw pointer `0x278400`, raw size `0x15e00`.

Current MCP function inventory keeps `0x00494c50` raw. `lookup_funcs` finds modeled neighbors at `0x00494c30` size `0x1a` and `0x00494c80` size `0x12a`, but no function at `0x00494c50`, `0x00494c51`, `0x00494c53`, or `0x00494c7e`. `entity_query` over `0x00494b40-0x00494c90` lists `0x00494b50`, `0x00494b80`, `0x00494bb0`, `0x00494bd0`, `0x00494bf0`, `0x00494c00`, `0x00494c10`, `0x00494c30`, and `0x00494c80`; there is no `0x00494c50` function object. `decompile 0x00494c50` fails because there is no function object. `insn_query` over `0x00494c4a-0x00494c80` shows one `align 10h` at `0x00494c4a`, followed by the helper instructions with `fn:null`.

Current bytes remain exact and unique. `get_bytes 0x00494c4a` shows six `0xcc` bytes before the helper. `get_bytes 0x00494c50` for size `0x30` returns `56 8b f1 80 be 01 01 00 00 00 74 22 80 be 02 01 00 00 00 74 19 68 80 00 00 00 e8 f1 49 02 00 8d 46 44 c6 46 70 00 50 8b ce e8 d2 57 02 00 5e c3`. `make_signature_for_range 0x00494c50-0x00494c80`, with operands not wildcarded, reports the signature as unique.

Route-negative checks are current. MCP `xrefs_to`/`xref_query`, `find code_ref`, `find data_ref`, and `find immediate` find zero route hits to the helper start `0x00494c50` and plausible entry interiors `0x00494c51` and `0x00494c53`. `0x00494c7e` has only the helper's internal short branches and local flow. `0x00494c80` retains the expected positive-control code xref from `0x005472d7` in `sub_5470B0`. `0x00494c30` retains positive-control direct code refs from `0x004b0b4f` and `0x00580640` plus broad vtable/data refs, including `0x00617aec`.

The local PE route scan agrees with MCP. VA/RVA/raw-offset dword patterns for `0x00494c50`, `0x00494c51`, `0x00494c53`, `0x00494c7e`, and `0x00494c80` have zero hits. Positive controls prove the scan was not blind: `0x00494c30` has 46 VA dword hits and direct-transfer hits at `0x004b0b4f` (`E9`) and `0x00580640` (`E9`); `0x00494b80` has 43 VA dword hits and six direct-call hits at `0x005006f5`, `0x00500814`, `0x0054b78d`, `0x0054b7a7`, `0x0054ba6d`, and `0x0054ba87`. Direct control-transfer scan finds zero direct call/jump/conditional/short route to `0x00494c50`, `0x00494c51`, or `0x00494c53`; the only hits inside the target are its two internal `0x74` early-exit branches to `0x00494c7e`.

Vtable checks preserve the owner and no-route split. `get_int` reads `0x00617ad8 -> 0x00494b80`, `0x00617adc -> 0x00494bb0`, `0x00617ae0 -> 0x00494bd0`, `0x00617ae4 -> 0x00494c00`, `0x00617ae8 -> 0x00494c10`, `0x00617aec -> 0x00494c30`, and `0x00617af0 -> 0x0041d690`; the raw helper has no `ControlPane` vtable slot. `0x00617b30` begins the following `ProgressBarControlPane` vtable area, so the `0x00494c80` function is the successor constructor boundary, not part of this helper.

Current callee analysis resolves source-facing names. `analyze_function 0x004b9660` reports a 13-byte `__thiscall` body that stores its argument to `this+0x74`, supporting `GrafPort::SetDrawColor`. `analyze_function 0x004ba450` reports a 235-byte frame routine with draw-enabled checks, rectangle copy/intersection work, and line-delta calls, supporting `GrafPort::DrawRectFrame`. These callees prove behavior, not ownership: the policy gates are `ControlPane` state bytes and the source owner remains `ControlPane`.

## IDA Evidence

- `server_health`: `NexusTK.exe.i64`, imagebase `0x00400000`, auto-analysis ready, Hex-Rays ready.
- `lookup_funcs`: no IDA function object at `0x00494c50`, `0x00494c51`, `0x00494c53`, or `0x00494c7e`; `0x00494c80` is the next modeled function.
- `entity_query`: the modeled function sequence in `0x00494a80-0x00495050` includes `0x00494c30`, then skips to `0x00494c80`; no modeled function starts at `0x00494c50`.
- `xref_query`: zero code refs and zero data refs to `0x00494c50`. Interior refs to `0x00494c51` and `0x00494c53` are local fall-through only; refs to `0x00494c7e` are only the helper's two short early-exit branches plus local flow.
- `get_bytes` and `make_signature_for_range`: six `0xcc` padding bytes at `0x00494c4a-0x00494c50`, followed by a unique `0x30`-byte helper signature (Verified with `int_convert.py`).
- `search_text`: IDA listing confirms the `cmp byte ptr [esi+101h], 0` state check and the `call sub_4BA450` frame draw.
- `get_int`: base `ControlPane` vtable slots end this local state sequence at `0x00617aec -> 0x00494c30`, and the following primary slot at `0x00617af0 -> 0x0041d690`; no `ControlPane` or `ProgressBarControlPane` vtable slot points to `0x00494c50` (integer values verified with `int_convert.py`).
- Raw PE scan: no VA/RVA pointer encoding to `0x00494c50`, `0x00494c51`, `0x00494c53`, or `0x00494c7e`; no dword in `0x00617a8c-0x00617bd4` equals those addresses; no direct branch/call targets `0x00494c50`, `0x00494c51`, or `0x00494c53`.
- 2026-06-16 A001 recheck in IDA MCP session `c001_midiplayer_rdata_20260615` reconfirmed no IDA function at `0x00494c50`, zero xrefs to the start, the complete helper bytes after six `0xcc` bytes at `0x00494c4a-0x00494c50`, and no absolute-VA/RVA/direct `E8`/`E9` hits to `0x00494c50` in a section-mapped PE scan.
- 2026-06-19 B001 source-quality local PE recheck used read-only `NexusTK.exe` with image base `0x00400000`, `.text` VA `0x00001000`/raw pointer `0x400`/raw size `0x20b600`, `.rdata` VA `0x0020d000`/raw pointer `0x20ba00`/raw size `0x5f200`, target RVA `0x00094c50`, and target file offset `0x94050`. The full `0x00494c50-0x00494c80` helper signature was unique in the executable. The byte window `0x00494c4a-0x00494c80` is six `0xcc` bytes followed by `56 8B F1 80 BE 01 01 00 00 00 74 22 80 BE 02 01 00 00 00 74 19 68 80 00 00 00 E8 F1 49 02 00 8D 46 44 C6 46 70 00 50 8B CE E8 D2 57 02 00 5E C3`.
- The same B001 local recheck found zero VA/RVA pointer encodings for `0x00494c50`, `0x00494c51`, `0x00494c53`, `0x00494c7e`, or `0x00494c80`, and found no direct branch/call into the start or plausible interior entries. The only branch hits involving the helper range were its two internal conditional jumps to `0x00494c7e`. Neighboring entries such as `0x00494b80`, `0x00494c30`, and `0x00494c80` did show expected branch/pointer activity, strengthening the negative reachability result for this raw helper.
- Callee targets visible in raw disassembly:
  - [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) `GrafPortDrawStateAccessors` at `0x004b9660`.
  - [UID:000168][0x004ba450-0x004ba53b.GrafPortDrawRectFrame](by-memory/0x004ba450-0x004ba53b.GrafPortDrawRectFrame.md) `GrafPortDrawRectFrame` at `0x004ba450`.

## Ownership Analysis

Evidence for `ControlPane` ownership:

- The helper consumes the generic `ControlPane` state bytes `+0x101` and `+0x102`, which are set/cleared by exact child helpers in this split.
- It draws against the inherited rectangle at `+0x44`, the same rectangle passed to the invalidation helper by neighboring `ControlPane` state methods.
- It sits immediately after the inherited `ControlPane` activation helper and before the `ProgressBarControlPane` constructor boundary.

Evidence that limits final-source confidence:

- IDA has no function object for the start.
- IDA reports no external code or data xrefs to the start.
- No vtable slot, raw pointer, or direct branch/call currently proves how the helper is reached.
- Final source-facing helper name and field names remain inferred.

Decision: assign to [UID:000038][ControlPane](by-class/ControlPane.md) and route through the same class emitter. The helper has no independent file/global/type owner, uses only base `ControlPane` state and rectangle fields, sits inside the exact ControlPane state-helper sequence, and is analogous to other current raw helper pages that are assigned when field use and local class context are strong even without a promoted IDA function object. Keep C++ blank because missing reachability, recovered declaration shape, and original source spelling keep it below formal source-output quality even though source-facing descriptive names are now strong enough for documentation.

Rejected owner/source routes:

- `ProgressBarControlPane`: the successor starts at `0x00494c80`, but this helper ends before that boundary and touches generic `ControlPane` state bytes rather than progress fields `+0xf8/+0xfa/+0xfc`.
- `ButtonControlPane` or another concrete control: no button text, selection, checkbox/radio, or concrete-control-only fields are used.
- `GrafPort`: the helper calls GrafPort draw primitives, but its policy gates are `ControlPane` state bytes `+0x101/+0x102`; it is a UI-control active-frame policy rather than a primitive GrafPort API.
- no-owner/no-emitter: older no-owner treatment was reasonable under stricter caller-required policy, but current raw-helper policy allows ownership when source-family, field use, and range evidence are this strong.

## C++ Readiness

Do not insert formal C++ for this target. The target clears the current average-score/emitter gate and has a valid `ControlPane` emitter route, but a target-specific no-code proof still applies: current MCP and PE evidence prove no function object, no entry xref, no vtable/table/pointer route, no direct branch target, and no recovered source declaration for `0x00494c50`. Emitting a guessed private helper would overstate whether the original was a private member, protected member, source-unused retained helper, local/static helper using `thiscall`-style codegen, or a retained but unreachable source artifact.

Behavior prose, not C++ for insertion: `ControlPane::DrawActiveFrame` is the best source-facing behavior name. When `m_enabled` and `m_controlActive` are both nonzero, the helper selects active-frame color `0x80` through `GrafPort::SetDrawColor`, clears inherited `m_drawMode` to `0`, and frames inherited `m_bounds` through `GrafPort::DrawRectFrame`; otherwise it performs no drawing. No standalone C++ body is carried outside the formal block because it remains blank under the no-route/no-declaration proof above.

`ControlPane::DrawActiveFrame` is the best source-facing name candidate for prose and future source repair. `ControlPaneDrawActiveFrameHelper` remains the documentation-safe descriptive page name until the original helper spelling, declaration, and caller/source representation are proven.

## Archived B-Agent Rule 26 Incorporation Audit

The archived B-agent reports for this target were rechecked as a cluster during Rule 26 incorporation. They contain two historical phases that must both stay visible:

| Report | Historical recommendation | Current disposition |
| --- | --- | --- |
| `Agent-B003/research/executed/older/0003N9-ControlPaneDrawActiveFrameHelper.md` | Keep `COMPLETION:82`, `CONFIDENCE:84`, `CANONICAL_OWNER:NONE`, and blank `EMITTER_UIDS`; treat `ControlPane` as the best forced semantic candidate only. | Superseded as metadata by the later B002 current Goal 2 pass, but its exact no-entry evidence and rejected owner analysis remain valid. |
| `Agent-B002/research/executed/older/0003N9-ControlPaneDrawActiveFrameHelper-second-pass.md` | Keep no-owner/no-emitter below gate after a broader IDA and raw PE scan; optional shared coverage refresh only. | Superseded as metadata, retained as the strongest negative reachability scan for the historical no-owner decision. |
| `Agent-B002/research/executed/older/0003N9-ControlPaneDrawActiveFrameHelper-post-migration-recheck.md` | Keep no-owner/no-emitter after post-migration review; no target edit was required under the stricter caller-required policy. | Superseded as metadata, retained as the policy checkpoint that explains why older docs treated this exact helper as ownerless. |
| `Agent-B002/research/executed/older/0003N9-ControlPaneDrawActiveFrameHelper-current-goal2-pass.md` | Assign `CANONICAL_OWNER:000038` and `EMITTER_UIDS:000038` at `85/86`; keep C++ blank. | Current accepted routing. This report supersedes the older no-owner metadata while preserving their reachability blockers as final-C++ blockers. |

The B003 and early B002 reports all agreed on the exact physical range and behavior: `0x00494c4a-0x00494c50` is six `0xcc` bytes, `0x00494c50-0x00494c80` is a unique `0x30`-byte raw helper, and `0x00494c80` starts the `ProgressBarControlPane` constructor. They also agreed that the body is source-shaped `ControlPane` drawing support, not padding, not a ProgressBar constructor fragment, and not ButtonChoice/ButtonControl code.

The preserved raw bytes from the second-pass reports are:

```text
56 8B F1 80 BE 01 01 00 00 00 74 22 80 BE 02 01 00 00 00 74 19 68 80 00 00 00 E8 F1 49 02 00 8D 46 44 C6 46 70 00 50 8B CE E8 D2 57 02 00 5E C3
```

Archived IDA evidence to retain:

- Session `b001_0003gy` reported `NexusTK.exe.i64`, image base `0x00400000`, auto-analysis ready, and Hex-Rays ready.
- `lookup_funcs` found modeled neighbors at `0x00494c30` and `0x00494c80`, but no function at `0x00494c4a`, `0x00494c50`, `0x00494c51`, `0x00494c53`, or `0x00494c7e`.
- `entity_query` over `0x00494a80-0x00494ec0` listed modeled starts at `0x00494b50`, `0x00494b80`, `0x00494bb0`, `0x00494bd0`, `0x00494bf0`, `0x00494c00`, `0x00494c10`, `0x00494c30`, `0x00494c80`, `0x00494de0`, and `0x00494eb0`; it did not list `0x00494c50`.
- `xrefs_to`/`xref_query` found zero external xrefs to `0x00494c50`; `0x00494c51` and `0x00494c53` had only local sequential flow; `0x00494c7e` had only the helper's two internal short branches and local fall-through; successor constructor `0x00494c80` retained its expected caller at `0x005472d7`.
- `callees 0x00494c50` and decompilation failed because IDA has no function object at the raw helper start; the outgoing calls to `0x004b9660` and `0x004ba450` are visible from bytes/disassembly only.
- `xref_query` showed the callees are broad shared drawing helpers (`0x004b9660` with hundreds of refs, `0x004ba450` with many refs), so they prove behavior but not ownership.

Archived vtable and table evidence to retain:

- The checked `ControlPane`/`ProgressBarControlPane` vtable island around `0x00617a8c-0x00617bd4` contains neighboring slots for `0x00494b80`, `0x00494bb0`, `0x00494bd0`, `0x00494c00`, `0x00494c10`, `0x00494c30`, and ProgressBar-local methods, but no slot to `0x00494c50`, `0x00494c51`, or `0x00494c53`.
- IDA entity/name inspection identified `ControlPane` vtable bases at `0x00617a90`, `0x00617af8`, and `0x00617b28`, followed by `ProgressBarControlPane` bases at `0x00617b34`, `0x00617b9c`, and `0x00617bcc`.
- The no-owner reports correctly rejected a virtual-slot route because the helper is absent from both documented `ControlPane` and `ProgressBarControlPane` vtables.

Archived raw PE evidence to retain:

- The read-only executable scan used image base `0x00400000`; `.text` VA `0x00001000`, raw offset `0x400`, raw size `0x20b600`; `.rdata` VA `0x0020d000`; target RVA `0x00094c50`; target file offset `0x94050`.
- Absolute VA pattern `50 4c 49 00` and RVA pattern `50 4c 09 00` for `0x00494c50` both had count `0`; the same VA/RVA pointer scans for plausible interior entries `0x00494c51`, `0x00494c53`, and return label `0x00494c7e` also had count `0`.
- The direct control-transfer scan found no `E8`/`E9` rel32 call or jump, `0F 80-8F` rel32 conditional branch, short `70-7F` conditional branch, `EB` short jump, or loop/jecxz branch targeting `0x00494c50`, `0x00494c51`, or `0x00494c53`.
- Scanner sanity checks found expected neighboring transfers and pointers, including many references to `0x00494c30` and `0x00494b80`, the internal branches to `0x00494c7e`, and the known call to `0x00494c80` from `0x005472d7`. Therefore the zero results for this helper are meaningful, not a broken scan artifact.

Rejected historical owner routes remain:

- `ProgressBarControlPane`: rejected because the clean successor starts at `0x00494c80`, the helper does not touch progress fields `+0xf8`, `+0xfa`, or `+0xfc`, and ProgressBar vtables do not contain the helper address.
- `ButtonControlPane`, `ButtonChoice`, and later concrete controls: rejected because repaired boundaries place those families later, and this helper uses base `ControlPane` state bytes rather than button/choice fields such as selected-state fields.
- `GrafPort`: rejected as semantic owner because the helper consumes ControlPane state and calls shared GrafPort primitives; the primitive callees are behavior dependencies, not the source owner of this policy helper.
- Standalone/new source file: rejected because no source-file evidence supports an independent helper file. If ever proven reachable, the likely home remains a private or protected helper in `NexusTK/ui/core/ControlPane.cpp`.
- Permanent no-owner/no-emitter: rejected by the current Goal 2 pass as metadata because this raw helper has a strong direct `ControlPane` field/context owner under current raw-helper policy; the older no-owner conclusion remains useful only as historical policy context and final-C++ caution.

Current disposition after the B004 recheck and B005 field correction: keep `88/89`, `CANONICAL_OWNER:000038`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000038`, and blank formal C++. Field/helper meanings are `m_enabled`, `m_controlActive`, `m_bounds`, `m_drawMode`, `GrafPort::SetDrawColor`, and `GrafPort::DrawRectFrame`; remaining confidence caps are no route/declaration proof plus exact helper/color-constant spelling uncertainty.

## Score Rationale

Completion `88` and confidence `89` are appropriate after the B004 2026-06-23 MCP-backed recheck. The exact bytes, boundary, unique signature, behavior, callees, negative xref/pointer/table evidence, local PE route scan with positive controls, owner comparison, field-name resolution, and direct `ControlPane` state-field context are now documented at source-quality detail. Confidence remains below the paired state helpers because IDA still has no function object, vtable slot, caller, raw pointer, direct branch/call target, recovered indirect-call target, or original source declaration for the raw helper.

## Cross-References

- [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane`
- [UID:0000IG][ControlPane](by-file/ControlPane.md) `ControlPane`
- [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) `GrafPortDrawStateAccessors`
- [UID:000168][0x004ba450-0x004ba53b.GrafPortDrawRectFrame](by-memory/0x004ba450-0x004ba53b.GrafPortDrawRectFrame.md) `GrafPortDrawRectFrame`
- [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md) `ProgressBarControlPaneConstructor`
- [UID:0002PD][0x00494b50-0x00494c80.ControlFamilyStateVirtualHelpers](by-memory/0x00494b50-0x00494c80.ControlFamilyStateVirtualHelpers.md) `ControlFamilyStateVirtualHelpers`

## Changes

- 2026-07-13 B005 UID0004BP support correction:
  - Kept score, owner/emitter, reconstructable state, range, `Nested:0`, blank position, raw liveness evidence, and blank formal block unchanged.
  - Changed only current `+0x101` wording to inherited `m_enabled`, updated Enable/Disable links, and labeled the old visible/shown interpretation superseded by separate Pane visibility at `+0xb4`.

- 2026-06-13 B002 split repair: created exact raw-helper page from [UID:0002PD][0x00494b50-0x00494c80.ControlFamilyStateVirtualHelpers](by-memory/0x00494b50-0x00494c80.ControlFamilyStateVirtualHelpers.md). Live IDA MCP confirmed no function object, zero xrefs, complete `0x30`-byte helper-shaped body, dependency calls to `0x004b9660` and `0x004ba450`, and exact handoff to the `0x00494c80` ProgressBar constructor.
- 2026-06-13 B002 second-pass review: kept `82/84`, `CANONICAL_OWNER:NONE`, and blank `EMITTER_UIDS`. Fresh IDA/raw PE checks found no external xrefs, no IDA function object, no ControlPane/ProgressBar vtable slot, no absolute VA/RVA pointer to the start or plausible interior entries, and no direct or conditional branch into `0x00494c50`, `0x00494c51`, or `0x00494c53`. ControlPane remains the strongest semantic candidate, but no owner/emitter route clears the gate.
- 2026-06-16 A001 Goal 2 support refresh: scores unchanged at `85/86`, but active code-entry wording and current evidence were refreshed. IDA MCP reconfirmed no function object and zero xrefs at `0x00494c50`; a section-mapped PE scan reconfirmed zero absolute-VA/RVA/direct-branch hits; final C++ remains blank because reachability/source representation and field/helper names are not final-source quality.
- 2026-06-14 B002 current Goal 2 pass: changed `82/84` to `85/86`, set `CANONICAL_OWNER:000038`, and set `EMITTER_UIDS:000038`. Current live IDA MCP and raw PE evidence still find no function object, no external xrefs, no vtable slot, no pointer encoding, and no direct branch/call into the helper, but the helper is a unique source-shaped `ControlPane` state-drawing body over `+0x101`, `+0x102`, `+0x70`, and `+0x44`, immediately follows the assigned `ControlPane` active-state virtuals, and matches the current assignment policy used for analogous raw no-xref class helpers.
- 2026-06-20 supervisor Rule 26 incorporation of B001 report `0003N9-ControlPaneDrawActiveFrameHelper-source-quality`: scores and routing remain `85/86`, `CANONICAL_OWNER:000038`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000038`, and blank C++. Incorporated the report's local PE facts, unique signature, no-pointer/no-branch recheck, field and helper meaning table, `ControlPane::DrawActiveFrame` source-name candidate, rejected owner/source routes, and explicit no-C++ readiness rationale.
- 2026-06-22 supervisor Rule 26 archived-report incorporation: scores and routing remain unchanged. Audited the B003 initial report plus B002 second-pass, post-migration, and current Goal 2 reports; preserved the superseded no-owner/no-emitter decisions, exact IDA/PE/vtable negative evidence, raw bytes, rejected owner routes, and the reason the current `ControlPane` owner/emitter route still must not emit formal C++.
- 2026-06-23 B004 implementation of accepted MCP-backed source-quality report: raised `COMPLETION:85`, `CONFIDENCE:86` to `COMPLETION:88`, `CONFIDENCE:89`; kept `CANONICAL_OWNER:000038`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000038`, blank emitter position, and blank formal C++. Added current MCP session `80de0a67` evidence, local PE provenance/hash/section facts, exact bytes/signature, no-function/decompile-failure proof, xref/code/data/immediate and local PE route-negative scans with positive controls, vtable checks, callee analyses, source-facing names, rejected alternatives, historical no-owner supersession, no-code proof, and behavior prose for `ControlPane::DrawActiveFrame` without any C++ body outside the formal block.
