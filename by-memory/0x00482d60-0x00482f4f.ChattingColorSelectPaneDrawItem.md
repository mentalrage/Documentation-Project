*** UID:0002G5 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001U | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001U | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingColorSelectPane::DrawItem(
    int row,
    const ChattingColorCategoryItem *item,
    const RectBounds *drawBounds)
{
    static const wchar_t *kChatColorCategoryLabels[ChatColorCategory_Count] = {
        L"Talk",
        L"Shout",
        L"Whisper",
        L"Group",
        L"Clan",
        L"System"
    };

    const int category = item->category;
    RectBounds fillBounds;

    if (IsItemSelected(row)) {
        FillPaletteRect(*drawBounds, 128);

        fillBounds = *drawBounds;
        InsetRectBounds(&fillBounds, 2, 2);
        FillPaletteRect(fillBounds, 143);
    } else {
        fillBounds = *drawBounds;
        InsetRectBounds(&fillBounds, 2, 2);
        FillPaletteRect(fillBounds, 136);
    }

    fillBounds = *drawBounds;
    InsetRectBounds(&fillBounds, 3, 3);
    FillPaletteRect(fillBounds, 0);

    SetTextColor(m_pColorPane->m_chatColorForeground[category]);
    SetTextBackColor(m_pColorPane->m_chatColorBackground[category]);
    MoveTo(drawBounds->left + 4, drawBounds->bottom - 3);

    RectBounds labelBounds = *drawBounds;
    labelBounds.left += 6;
    labelBounds.top += 4;
    labelBounds.right -= 6;
    labelBounds.bottom -= 4;

    const wchar_t *label = kChatColorCategoryLabels[category];
    DrawOutlinedText(label, static_cast<int>(wcslen(label)), &labelBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current MCP recheck confirms exact ChattingColorSelectPane::DrawItem bounds 0x00482d60-0x00482f4f, vtable slot 0x00615164, no direct callers, category item +0x00 selecting Talk/Shout/Whisper/Group/Clan/System labels, owner pointer this+0x14c as m_pColorPane, owner foreground/background arrays +0x278/+0x290, selected palettes 128/143, unselected palette 136, final interior palette 0, text color/background setup through shared GrafPort accessors, MoveTo left+4/bottom-3, label bounds left+6/top+4/right-6/bottom-4, rect-based DrawOutlinedText at 0x004bafa0, one-byte 0xcc padding before OnSelectionChanged, current social/Chatting.cpp route, and first-draft formal C++ readiness. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00482d60-0x00482f4f - ChattingColorSelectPane DrawItem

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method / virtual draw item.
- Owner: [UID:00001U][ChattingColorSelectPane](by-class/ChattingColorSelectPane.md).
- Parent aggregate: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Confidence: strong for range, vtable ownership, labels, drawing behavior, source-facing field roles, and first-draft C++ readiness.

## Behavior

This virtual draw method renders one row in the chat color category selector. It tests whether the row is selected, draws a selected or unselected background, sets foreground/background text colors from the owner color table, draws a small marker/line at the bottom of the row, and writes the category label.

The source-facing signature is `void ChattingColorSelectPane::DrawItem(int row, const ChattingColorCategoryItem *item, const RectBounds *drawBounds)`. The item pointer's first dword is the category value, modeled as `ChattingColorCategoryItem::category` at item offset `+0x00`; no other item fields are proven by this target. `row` is used only for the selected-row test, not for color or label indexing.

Label table:

| Entry value | Label source |
| --- | --- |
| `0` | `Talk` at `0x00615364` |
| `1` | `Shout` at `0x00615370` |
| `2` | `Whisper` at `0x0061537c` |
| `3` | `Group` at `0x0061538c` |
| `4` | `Clan` at `0x00615398` |
| `5` | `System` at `0x00615444` |

The method reads the owner pane pointer from selector offset `this+0x14c`, best source-facing `m_pColorPane`. It then indexes owner-side `m_chatColorForeground[category]` at `+0x278` and `m_chatColorBackground[category]` at `+0x290` through `4 * category`.

## Rendering Flow

1. Calls the inherited list selected-row test helper for the current row, represented in source as `IsItemSelected(row)`.
2. For selected rows, fills the full row with palette `128`, then fills an inset-by-2 rectangle with palette `143`.
3. For unselected rows, fills an inset-by-2 rectangle with palette `136`.
4. For all rows, fills an inset-by-3 rectangle with palette `0`.
5. Sets text foreground/background colors from the owning `ChattingColorPane` foreground/background arrays for the item category.
6. Calls `MoveTo(drawBounds->left + 4, drawBounds->bottom - 3)`.
7. Builds label bounds `(left + 6, top + 4, right - 6, bottom - 4)`, computes the UTF-16 label length, and calls the rect-based outlined text helper at `0x004bafa0`.

## IDA MCP Evidence

- Current MCP session `B006_0002F1_20260629` reports `sub_482D60` at exact range `0x00482d60-0x00482f4f`, size `0x1ef` / decimal `495` (verified with `tools/int_convert.py` in the accepted report).
- IDA vtable data at `0x00615164` points to `0x00482d60`, confirming virtual ownership by `ChattingColorSelectPane`.
- `0x00482f4f` is not a function start; the next modeled function is [UID:0002G6][0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged](by-memory/0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged.md).
- `get_global_value` shows neighboring selector vtable cells `0x0061515c -> 0x00482f50` and `0x00615164 -> 0x00482d60`.
- Decompilation loads the six labels `Talk`, `Shout`, `Whisper`, `Group`, `Clan`, and `System`, calls selection test `sub_4F3F00`, uses palette helpers `sub_5446B0`/`sub_4B9660`, uses `dword_69B3FC` to fill rectangles, sets text colors through `sub_4B9680`/`sub_4B9690`, and draws the label with `sub_4BAFA0`.
- The following byte `0x00482f4f-0x00482f50` is confirmed `0xcc` alignment before the selection-changed virtual.
- 2026-06-16 `analyze_function` reconfirms the vtable-only data xref at `0x00615164`, empty direct callers, label pointer setup for all six categories, selected/unselected palette branches, owner color-byte reads through `*(this+0x14c) + 4*entry + 0x278/+0x290`, and callees `sub_4F3F00`, `sub_4B7C50`, `sub_5446B0`, `sub_4B9660`, `dword_69B3FC`, `sub_4B9680`, `sub_4B9690`, `sub_4B9600`, and `sub_4BAFA0`.
- B012's current MCP recheck reconfirms a single data xref from vtable slot `0x00615164` and no ordinary direct code callers. It also decodes the UTF-16 label bytes at `0x00615364`, `0x00615370`, `0x0061537c`, `0x0061538c`, `0x00615398`, and `0x00615444`, and confirms six `0xcc` bytes before the target plus one `0xcc` byte before [UID:0002G6][0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged](by-memory/0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged.md).

## Source-Quality And Ownership

The direct owner remains [UID:00001U][ChattingColorSelectPane](by-class/ChattingColorSelectPane.md). [UID:00001T][ChattingColorPane](by-class/ChattingColorPane.md) supplies the color arrays, but the receiver and vtable slot are the selector pane. [UID:00001S][ChattingColorListPane](by-class/ChattingColorListPane.md) is rejected because its separate draw method [UID:0002GB][0x004832c0-0x00483414.ChattingColorListPaneDrawItem](by-memory/0x004832c0-0x00483414.ChattingColorListPaneDrawItem.md) draws swatches using `ChattingColorSwatchItem::colorValue`; this target draws category labels and indexes owner colors by category.

The current emitter route remains [UID:00001U][ChattingColorSelectPane](by-class/ChattingColorSelectPane.md) through [UID:0000I5][Chatting](by-file/Chatting.md) to `NexusTK/social/Chatting.cpp`. A future `social/ChatColorDialog.cpp` split remains plausible for the color-dialog family, but it is not the current owner/emitter route and does not block this formal C++.

The earlier blank-C++ rationale is superseded. Current support docs resolve `m_pColorPane`, `m_chatColorForeground`, `m_chatColorBackground`, and analogous draw helper vocabulary well enough for first-draft source. The raw selector helpers [UID:0002G3][0x00482d40-0x00482d4a.ChattingColorSelectPaneRefreshHelperRaw](by-memory/0x00482d40-0x00482d4a.ChattingColorSelectPaneRefreshHelperRaw.md) and [UID:0002G4][0x00482d50-0x00482d5a.ChattingColorSelectPaneSelectionNotifyHelperRaw](by-memory/0x00482d50-0x00482d5a.ChattingColorSelectPaneSelectionNotifyHelperRaw.md) remain no-route blank-C++ helpers, but they are neighboring retained helpers and do not block this modeled virtual method.

Rejected alternatives: do not leave this page as an empty emitter, do not use a no-code or covered-by marker, do not index labels/colors by `row`, do not move ownership to `ChattingColorPane`, `ChattingColorListPane`, `GrafPort`, or Surface, and do not emit raw `sub_` names, vtable writes, stack-cookie code, or manual label-length loops in source C++.

## Score Rationale

Completion is raised to `88` because current documentation now records exact range, vtable-only reachability, category item semantics, label bytes, selected/unselected rendering branches, owner field names, text bounds, helper dependencies, rejected alternatives, source route, boundary padding, and formal first-draft C++.

Confidence is raised to `92` because current MCP and support docs strongly prove behavior and ownership, and the remaining caveats are integration-polish items: exact original helper spellings, exact original `ChattingColorCategoryItem` header spelling, and a possible future `social/ChatColorDialog.cpp` file split. Those caveats no longer justify blank C++ for this modeled virtual method.

## Cross-References

- [UID:00001U][ChattingColorSelectPane](by-class/ChattingColorSelectPane.md)
- [UID:00001T][ChattingColorPane](by-class/ChattingColorPane.md)
- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)

## Changes

- 2026-06-16 A001 class-completion refresh: raised completion `82` to `85` after live IDA reconfirmed exact vtable-owned draw-item behavior, label setup, rendering branches, owner color-byte offsets, full callee set, and boundary padding. Confidence remains `90`; final C++ stays blank pending source-grade UI helper and field names.
- 2026-06-29 B012 empty-emitter implementation: raised `85/90` to `88/92` and inserted first-draft formal `ChattingColorSelectPane::DrawItem(int row, const ChattingColorCategoryItem *item, const RectBounds *drawBounds)` C++. Accepted evidence from current MCP session `B006_0002F1_20260629` confirms exact `0x00482d60-0x00482f4f` / `0x1ef` body, vtable slot `0x00615164`, no direct callers, item category at `+0x00`, labels `Talk/Shout/Whisper/Group/Clan/System`, selector owner `this+0x14c` as `m_pColorPane`, owner foreground/background arrays `+0x278/+0x290`, selected palettes `128/143`, unselected palette `136`, final interior palette `0`, `MoveTo(left+4,bottom-3)`, label bounds `(left+6,top+4,right-6,bottom-4)`, rect-based `DrawOutlinedText` at `0x004bafa0`, one-byte padding before [UID:0002G6][0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged](by-memory/0x00482f50-0x00482fa5.ChattingColorSelectPaneOnSelectionChanged.md), and the current `NexusTK/social/Chatting.cpp` route. Rejected no-code/covered-by treatment, row-based indexing, owner moves, raw helper ownership, raw `sub_` emission, and stale blank-C++ rationale.
