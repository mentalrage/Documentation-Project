*** UID:0000TU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000NV | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:520 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static U32 AILCALLBACK AILFileOpenCallback(const char *filename, U32 *fileHandle)
{
    SoundPathString ansiPath(filename);
    mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > path(ansiPath);
    File *file;

    if (HasDATEntry(path.c_str())) {
        file = new DATFile;
    } else {
        if (!PathExistsViaStat(path.c_str()))
            return 0;
        file = new StdioFile;
    }

    file->Open(path.c_str());
    *fileHandle = reinterpret_cast<U32>(file);
    return 1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: SoundManager file-local Miles open callback converting the ANSI path, preferring DAT-backed files, falling back to an existing loose file, opening through File, and returning the pointer as the 32-bit Miles handle. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# AIL File Open Callback

## UID0001IC Exact Range And Source Integration - 2026-07-24

- UID0000TU moved from the historical by-item path to exact half-open by-memory coverage `[0x0057b630,0x0057b72b)`, preserving its UID, prior evidence, history, and inbound reference identity. The former `0x0057b72a` description was the inclusive last byte; repository range notation now uses the exclusive end.
- The accepted source is a file-static Miles callback canonically owned by [UID:0000NV][SoundManager](by-file/SoundManager.md) and inserted through class router [UID:0000DG][SoundManager](by-class/SoundManager.md) at position `520`. [UID:0004WV][SoundManagerMilesCallbackDeclarations](by-item/SoundManagerMilesCallbackDeclarations.md) owns its sole prototype; this page owns its sole CPP definition and keeps H blank. The insertion route preserves exact generated open/close/seek/read/stream order after UID0004X0 became a class-owned private static member; it does not make this File callback a class member.
- The callback uses the contemporary `U32 AILCALLBACK(const char *, U32 *)` SDK shape. Its one data xref at `0x0057b596` is the open argument to `AIL_set_file_callbacks`; generic File/DATFile/StdioFile calls are dependencies and do not move ownership to FileIO.
- Exact behavior is DAT-first. Convert the ANSI filename through `SoundPathString` to the wide project path type, choose `DATFile` when `HasDATEntry` succeeds, otherwise require `PathExistsViaStat` before allocating `StdioFile`, call virtual `File::Open`, store the pointer in the 32-bit Miles handle, and return one. If neither backing route exists, return zero without a handle.
- The observed body does not validate `File::Open`'s return before reporting success. The draft preserves that weakness; adding failure cleanup or result propagation would change behavior.
- `[0x0057b62a,0x0057b630)` is six bytes of preceding alignment and `[0x0057b72b,0x0057b730)` is five bytes of following alignment. Both are separate all-`0xcc`, unnamed, untyped, uncommented, zero-xref data items.
- Historical statements that the callback typedef, source spelling, or surrounding File declarations still blocked C++ are superseded by the current Miles ABI and class/file split. Historical saved-IDA-label claims remain evidence-time history; current supervisor Gate 2B state is tracked in the accepted report, not inferred from those old passages.

## Status

- Confidence: strong for behavior, callback registration, and SoundManager ownership; medium-high for final source spelling.
- Address range: `0x0057b630-0x0057b72a`
- Current IDA name: `AILFileOpenCallback`
- Proposed source owner: [UID:0000NV][SoundManager](by-file/SoundManager.md)
- Autogen insertion parent: [UID:0000DG][SoundManager](by-class/SoundManager.md); canonical source owner remains [UID:0000NV][SoundManager](by-file/SoundManager.md)
- Related file layer: [UID:0000JD][FileIO](by-file/FileIO.md)

## Function Role

This is the Miles Sound System file-open callback registered by `SoundManager::ReinitializeAudioDriver`. It converts the incoming ANSI path to wide text, first probes the DAT archive via [UID:0000T0][HasDATEntry_49C700](by-global/HasDATEntry_49C700.md), and falls back to local filesystem existence via [UID:0000V6][PathExistsViaStat_00582460](by-item/PathExistsViaStat_00582460.md).

If the name resolves in DAT, it allocates and constructs a [UID:00003G][DATFile](by-class/DATFile.md). If the name exists as a loose file, it allocates and constructs a [UID:0000E6][StdioFile](by-class/StdioFile.md). It then calls the selected reader's virtual `Open` slot and returns the `File*`-style handle to Miles through the output parameter.

## Fresh IDA Evidence

Live IDA MCP on 2026-06-14, session `a001_goal2_class_batch`, reconfirmed:

| Check | Result | Meaning |
| --- | --- | --- |
| `lookup_funcs 0x0057b630` | `sub_57B630`, size `0xfb` | Function bytes run from `0x0057b630` through `0x0057b72a` inclusive, matching the documented callback range. |
| `analyze_function 0x0057b630` | Prototype `int __stdcall(const char *Src, int **)` | Callback returns success/failure and writes the opened file object through the second argument. |
| DAT probe path | Calls `sub_49C700` after ANSI-to-wide conversion | DAT-backed sound resources are tried before loose files. |
| DAT object construction | Allocates `20` bytes and calls `sub_49C130` | Matches the [UID:00003G][DATFile](by-class/DATFile.md) path. |
| Loose-file fallback | Calls `sub_582460`, allocates `12` bytes, then calls `sub_582070` | Matches the [UID:0000V6][PathExistsViaStat_00582460](by-item/PathExistsViaStat_00582460.md) and [UID:0000E6][StdioFile](by-class/StdioFile.md) path. |
| Virtual open dispatch | Loads the vtable and calls slot `+0x0c` with the converted path | The callback does not read file bytes itself; it opens the selected `File` implementation for Miles. |
| Failure path | Returns `0` when neither DAT nor loose-file existence succeeds | Miles receives no file handle for missing resources. |

The same live IDA pass on `0x0057b490` reconfirmed `SoundManager::ReinitializeAudioDriver` as the registration point. It pushes `sub_57B7A0`, `sub_57B750`, `sub_57B730`, and `sub_57B630`, then calls `_AIL_set_file_callbacks@16` at `0x0057b59b` after the digital driver opens successfully.

Live IDA MCP on 2026-06-16, session `b001_mappane_0001AW_20260616`, rechecked and updated source-quality IDA labels:

| Check | Result | Meaning |
| --- | --- | --- |
| `lookup_funcs 0x0057b630` after save | `AILFileOpenCallback`, size `0xfb` | The exact open callback label is now saved in the IDB. |
| `lookup_funcs 0x0057b490` after save | `SoundManager_ReinitializeAudioDriver`, size `0x160` | The registration function now has a stable source-quality support label. |
| `xrefs_to 0x0057b630` | data xref from `0x0057b596` | The open callback is still only passed to the AIL registration site. |
| Callback siblings | `AILFileCloseCallback`, `AILFileSeekCallback`, `AILFileReadCallback` | The close/seek/read sibling labels were saved with the open callback, matching the `AIL_set_file_callbacks` argument order. |
| Decompilation | unchanged `__stdcall(const char *Src, int **outFile)` shape | The callback semantics did not change; the IDA update only made the existing evidence source-quality. |

## Callback Family

IDA shows `SoundManager::ReinitializeAudioDriver` registers these callbacks together through `AIL_set_file_callbacks`:

| Address | Role |
| --- | --- |
| `0x0057b630` | open callback; selects `DATFile` or `StdioFile`. |
| `0x0057b730` | close callback; calls virtual deleting destructor on the `File` object. |
| `0x0057b750` | seek callback; forwards to the virtual seek slot and returns position. |
| `0x0057b7a0` | read callback; clamps read count to remaining bytes and forwards to the virtual read slot. |

## Ownership Notes

Keep these callbacks in `audio/SoundManager.cpp`, not in `util/File.cpp`. They are generic in the sense that they consume the `File` API, but IDA xrefs show they are installed only as Miles AIL callbacks by the sound driver setup path.

Current canonical ownership remains [UID:0000NV][SoundManager](by-file/SoundManager.md), while `EMITTER_UIDS:0000DG` is a validator insertion route into the same `SoundManager.cpp` root. This distinction preserves file-static source ownership and exact position `520` after the class-routed stream callback; it does not transfer semantic ownership. Historical 2026-06-14 state routed both owner and emitter directly through UID0000NV and still left formal C++ blank; that state was superseded by the exact split, SDK typing, source body, and generated-order verification.

## Score Rationale

| Metric | Value | Rationale |
| --- | ---: | --- |
| Completion | 87 | Documents exact callback role, saved IDA callback-family labels, fresh IDA signature/body, DAT-first and loose-file fallback paths, object construction sizes, virtual-open dispatch, registration through `AIL_set_file_callbacks`, SoundManager owner/emitter route, and related File/DAT support pages. Still below final-code level because callback typedef spelling and file-class declaration details are not final-source quality. |
| Confidence | 91 | Strong from live IDA function analysis, saved callback labels, registration disassembly, and the existing SoundManager, DATFile, StdioFile, and FileIO docs. Capped below final audit because the original callback typedef spelling and final source signature remain inferred rather than proven. |

## Cross-References

- [UID:0000NV][SoundManager](by-file/SoundManager.md)
- [UID:0001IC][0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver](by-memory/0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver.md)
- [UID:0000JD][FileIO](by-file/FileIO.md)
- [UID:00003G][DATFile](by-class/DATFile.md)
- [UID:0000E6][StdioFile](by-class/StdioFile.md)
- [UID:0000V6][PathExistsViaStat_00582460](by-item/PathExistsViaStat_00582460.md)

## Changes

- 2026-06-14 A003 Goal 2 low-confidence item refresh:
  - Raised completion/confidence from `80/84` to `86/88`.
  - Evidence: live IDA MCP reconfirmed `sub_57B630` size `0xfb`, `__stdcall(const char*, int**)` callback shape, DAT probe through `sub_49C700`, `DATFile` allocation/construction, loose-file fallback through `sub_582460` plus `StdioFile` construction, virtual `Open` slot dispatch, and registration from `SoundManager::ReinitializeAudioDriver` through `_AIL_set_file_callbacks@16` at `0x0057b59b`.
  - Historical routing at that checkpoint: owner/emitter both remained [UID:0000NV][SoundManager](by-file/SoundManager.md); the later UID0001IC split preserved canonical ownership but changed insertion routing to UID0000DG for exact generated callback order.
  - C++ remains blank because final source typedef/name and surrounding `File` declarations are not final-source quality.

- 2026-06-16 C001 item/IDA refresh:
  - Raised completion/confidence from `86/88` to `87/91`.
  - IDA DB changes saved to `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`: `sub_57B490 -> SoundManager_ReinitializeAudioDriver`, `sub_57B630 -> AILFileOpenCallback`, `sub_57B730 -> AILFileCloseCallback`, `sub_57B750 -> AILFileSeekCallback`, and `sub_57B7A0 -> AILFileReadCallback`.
  - Evidence: live IDA MCP after-save lookup verified the labels and unchanged sizes; `xrefs_to 0x0057b630/0x0057b730/0x0057b750/0x0057b7a0` still shows data refs from the callback registration sequence in `SoundManager_ReinitializeAudioDriver`; decompilation of the open callback still proves DAT-first probing, loose-file fallback, `DATFile`/`StdioFile` construction, virtual `Open`, and output handle storage.
  - C++ remains blank because exact Miles callback typedef spelling and source declaration placement are still not proven.

- 2026-06-05: Reconstructable metadata changed from blank to `TRUE` and attached to [UID:0000NV][SoundManager](by-file/SoundManager.md).
  - Before: the page had IDA-backed callback behavior and SoundManager ownership but remained unclassified in autogen coverage.
  - After: the Miles file-open callback is marked as NexusTK-owned source under the validated SoundManager file root; C++ remains blank because the final callback signature and surrounding declarations are not at the 95/95 final-code bar.
  - Evidence: live IDA MCP `lookup_funcs 0x0057b630` reports `sub_57B630` size `0xfb`; callees include the DAT probe at `0x0049c700`, allocation helpers, string/path helpers, and local-file fallback at `0x00582460`, matching the documented SoundManager callback role.

- 2026-07-24 B002 focused source-legality callback: canonical file ownership and the file-static CPP body remain unchanged; insertion routing moved from UID0000NV to UID0000DG solely to preserve physical callback definition order `520-560` after the stream callback became a private static class member.

- 2026-05-30: Grading changed from `0/0` to `80/84`.
  - Before: page documented the Miles file-open callback, DAT/loose-file selection, callback family, and SoundManager ownership but remained unevaluated.
  - After: score reflects documented callback behavior and ownership, with remaining confidence limited by final callback name polish.
  - Evidence: IDA notes tie the callback family to `SoundManager::ReinitializeAudioDriver` and show DAT probe/local-file fallback plus `DATFile`/`StdioFile` construction.
