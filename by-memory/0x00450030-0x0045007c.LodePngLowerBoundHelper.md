*** UID:0000XJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000KW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000KW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Emitted code for this range is covered by the canonical LodePNG 20160501 source-pair import on [UID:00007E][LodePNGState](by-class/LodePNGState.md); do not emit duplicate definitions.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Static LodePNG `searchCodeIndex` lower-bound helper covered by UID00007E's sole staged LodePNG source-pair import; live IDA confirms `sub_450030` size `0x4c` / 76 bytes and rejects `0x0045004e` as an interior address. This child emits only its compiler-covered CPP comment, with blank inline CPP value and blank H. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00450030-0x0045007c LodePngLowerBoundHelper

## Status

- Confidence: strong for function boundary, algorithm shape, LodePNG helper-island placement, bad `EditablePaperPane` interior-address rejection, and final upstream source match to `searchCodeIndex`.
- Proposed owner: [UID:0000KW][LodePNG](by-file/LodePNG.md)
- Reconstruction disposition: reconstructable bundled third-party source item compiler-covered by the sole `third_party_embeds/lodepng/` source-pair import on UID00007E. This child issues no import and emits only its explanatory CPP comment; inline CPP value and H remain blank. Ignore only as NexusTK-owned handwritten code.
- Current bad/generated owner: disabled `source-3/simroot_v2/class_EditablePaperPane.cpp` interior row at `0x0045004e`.

## Covered Range

| Range | Provisional name | Notes |
| --- | --- | --- |
| `0x00450030-0x0045007c` | `LodePngLowerBoundHelper` / LodePNG `searchCodeIndex` | Searches a sorted dword table for a key-like value and returns the nearest lower/equal index. This matches staged LodePNG `static size_t searchCodeIndex(const unsigned* array, size_t array_size, size_t value)`. The final instruction is `retn` at `0x0045007b`; the next four bytes `0x0045007c-0x00450080` are padding before `sub_450080`. |

## IDA MCP Evidence

Checked on 2026-05-25, 2026-05-26, 2026-06-15, and current B002 implementation evidence on 2026-06-27:

- 2026-06-27 live IDA MCP session `80de0a67` reported `status: ok` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- Current `lookup_funcs` maps `0x00450030` to `sub_450030`, size `0x4c`; maps `0x0045004e` and `0x0045007b` back to the same function; rejects `0x0045007c` as not a function; and maps `0x00450080` to the next function `sub_450080`, size `0x25`.
- Current decompilation gives `unsigned int __cdecl sub_450030(int a1, unsigned int a2, unsigned int a3)`, begins with `result = 1`, narrows `(left/right)` with midpoint `(v5 + result) >> 1`, loads `*(_DWORD *)(a1 + 4 * mid)`, compares to the key argument, and decrements the final result when the selected table value is greater than the key.
- Current disassembly places `0x0045004e` on `shr esi, 1` immediately after `lea esi, [edi+eax]`, so the old `EditablePaperPane` row is an interior-instruction false positive, not a method start.
- Current xrefs show exactly two code refs to the function entry, `0x0044ceb2` and `0x0044ced0`, both inside `sub_44CBE0`; `callees 0x00450030` is empty. `xrefs_to 0x0045004e` has only an internal code ref from `0x45004b` plus data refs, not function-entry callers.
- Current boundary bytes show two `0xcc` bytes before the helper at `0x0045002e-0x00450030`, the 76-byte body at `0x00450030-0x0045007c`, and four `0xcc` bytes at `0x0045007c-0x00450080` before `sub_450080`.
- 2026-06-15 live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirms `sub_450030` at `0x00450030`, size `0x4c` / 76 bytes, so the correct exclusive range is `0x00450030-0x0045007c`. `0x0045007b` maps inside the function and holds the final `retn`; `0x00450080` is the next function. Decimal conversion was verified with `tools/int_convert.py`.
- Current byte evidence shows `0x0045007c-0x00450080` as four `0xcc` padding bytes after this helper. The previous `0x00450030-0x0045007b` label was an off-by-one exclusive-end error.
- Current decompilation shows a binary-search/lower-bound loop over a sorted dword table: it begins with result index `1`, narrows the high/low bounds with midpoint loads from `base + 4 * mid`, and decrements the result when the selected table value is greater than the requested key.
- Current xrefs show exactly two direct callers, `0x0044ceb2` and `0x0044ced0`, both in `sub_44CBE0` inside the LodePNG helper island. `lookup_funcs 0x0045004e` still maps to `sub_450030`, proving that address is interior and not an `EditablePaperPane` method.
- Earlier `lookup_funcs 0x00450030` reports a real function at `0x00450030` with size `0x4c`.
- `lookup_funcs 0x0045004e` maps back to the same function start, proving `0x0045004e` is an interior address rather than a method start.
- `decompile 0x00450030` shows a binary-search/lower-bound style loop over a dword array.
- Direct callers are `0x0044ceb2` and `0x0044ced0`, both inside `0x0044cbe0`, a caller in the existing LodePNG helper island, not `EditablePaperPane`.
- `xrefs_to 0x0045004e` also reports a code xref from inside `0x00450030` plus data refs; it is still not an `EditablePaperPane` entry point.

## Staged Source Match And Current Child Emission

The final source for this helper is the staged LodePNG 20160501 source pair under `source-3/third_party_embeds/lodepng/`:

- `third_party_embeds/lodepng/lodepng.cpp`, length `220220`, SHA-256 `71F9A45829F9AF7A6675200153DAAB5D791846A7EC4C5385241AC6F1E35B629A`.
- `third_party_embeds/lodepng/lodepng.h`, length `82029`, SHA-256 `9B330CA0224E85EB8937DC83627BDFA0285C6CC6691223D3FD51D8987A2D284B`.

`lodepng.cpp` lines 1326-1341 contain `searchCodeIndex`, documented there as a search for the index with the largest value smaller than or equal to a requested value in a sorted array. Its source shape uses `left = 1`, `right = array_size - 1`, midpoint `(left + right) >> 1`, a branch on `array[mid] >= value`, and a final decrement when the selected entry is past the requested value. The live `sub_450030` loop is the compiled form of this source helper. The LodePNG source callers are the deflate length-code and distance-code lookups, which matches the two local callers inside the documented LodePNG helper island and not any product UI class.

Because the exact staged third-party source is available and validator-importable, [UID:00007E][LodePNGState](by-class/LodePNGState.md) carries the sole formal source-pair directive. This child issues no import: its inline `RECONSTRUCTION_CPP CODE` value is blank, its multiline CPP block contains only the compiler-covered explanatory comment above, and its H block is blank. Do not replace this with a duplicate per-child import, copied LodePNG source, decompiler-shaped helper C++, `third-party/...` paths, absolute paths, documentation-root paths, or legacy `// Static embed:` marker comments.

## Ownership Decision

Keep this under the [UID:0000XE][0x00443a60-0x00450c9f.LodePngHelperIslandInventory](by-memory/0x00443a60-0x00450c9f.LodePngHelperIslandInventory.md) unless later callgraph/string evidence proves a different bundled-library owner. Do not import `0x0045004e` as an `EditablePaperPane` virtual method; it is an interior instruction in this helper.

Rejected alternatives: do not assign this range to `EditablePaperPane`, `ImageWriters`, screenshot workflow code, DAT/archive code, or a standalone NexusTK handwritten helper. The direct owner/emitter metadata remains [UID:0000KW][LodePNG](by-file/LodePNG.md); generated source comes from UID00007E's aggregate import, while this child contributes only its compiler-covered comment.

## Cross-References

- [UID:0000KW][LodePNG](by-file/LodePNG.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)
- [UID:0000XE][0x00443a60-0x00450c9f.LodePngHelperIslandInventory](by-memory/0x00443a60-0x00450c9f.LodePngHelperIslandInventory.md)
- [UID:0000IX][EditablePaperPane](by-file/EditablePaperPane.md)
- [Wave3 data issues](../wave3_data_issues.md)

## Changes

- 2026-08-24: Accepted UID0000KW callback removed this page's duplicate source-pair import, retained blank H output, installed the exact compiler-covered CPP comment, and raised the exact `searchCodeIndex` slice to `92/94`. UID00007E is now the sole formal import route.

## Superseded Per-Child Import History

The entries below preserve dated implementation history only. Their per-child import and blank-emitter assertions are superseded by the 2026-08-24 aggregate route recorded above and are not current instructions.

- 2026-06-27 B002 `0000XJ` source-quality implementation:
  - Before: scores were `86/90`, the inline formal C++ value was blank, and the item summary still said size `0x4b`.
  - Changed to: `90/92` with formal validator import directive for `third_party_embeds/lodepng/lodepng.cpp` and `third_party_embeds/lodepng/lodepng.h`; the multiline C++ block remains blank.
  - Summary/evidence: live IDA MCP session `80de0a67` reconfirmed `sub_450030` as `0x00450030-0x0045007c`, size `0x4c` / 76 bytes, with `0x0045004e` as the interior `shr esi, 1` midpoint instruction, `0x00450080` as the next function, two direct callers in `sub_44CBE0`, no callees, and `0xcc` padding before/after. Staged `lodepng.cpp` lines 1326-1341 contain the matching static `searchCodeIndex` helper, and the staged source pair hashes are recorded in this page.
- 2026-06-05: Set `RECONSTRUCTABLE:TRUE` and attached to [UID:0000KW][LodePNG](by-file/LodePNG.md). Evidence: live IDA MCP reconfirmed `sub_450030` at `0x00450030` with size `0x4c`, and `0x0045004e` still maps inside the same function; the page is `80/85`, the LodePNG file parent is `88/84`, and existing docs keep this helper in the bundled LodePNG island rather than `EditablePaperPane`.
- 2026-06-15 A001 Goal 2 refresh:
  - Before: scores were `80/85` and the page/filename used the off-by-one exclusive end `0x0045007b`.
  - Changed to: `86/90` and renamed the page to `0x00450030-0x0045007c.LodePngLowerBoundHelper.md`.
  - Summary/evidence: live IDA MCP reconfirmed the 76-byte function, final `retn` at `0x0045007b`, four-byte padding before `0x00450080`, two LodePNG-local callers, and the interior-address status of `0x0045004e`. Owner/emitter remain [UID:0000KW][LodePNG](by-file/LodePNG.md); final C++ remains blank because the source-quality target is vendored `lodepng.cpp` / `lodepng.h`, not a handcrafted per-helper rewrite.
