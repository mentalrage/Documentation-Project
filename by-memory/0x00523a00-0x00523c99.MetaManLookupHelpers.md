*** UID:0001C9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static unsigned int MetaMan_GetDirectRowValueCount(MetaMan *metaMan,
                                                   const SimpleUString *tableName,
                                                   const SimpleUString *rowKey)
{
    MetaTable *table = metaMan->FindTableByName(tableName);
    if (table == NULL)
        return 0;

    if (!table->m_isLoaded)
        table->MaterializeRowsIfNeeded(tableName->c_str());

    MetaTableRowNode *row = NULL;
    MetaTableRowFindByKey(&row, table->m_rowTreeHeader, rowKey);
    if (row == table->m_rowTreeHeader)
        return 0;

    return static_cast<unsigned int>(row->valuesEnd - row->valuesBegin);
}

static const SimpleUString *MetaMan_FindDirectRowValue(MetaMan *metaMan,
                                                       const SimpleUString *tableName,
                                                       const SimpleUString *rowKey,
                                                       unsigned int valueIndex)
{
    MetaTable *table = metaMan->FindTableByName(tableName);
    if (table == NULL)
        return &g_emptySimpleUString;

    if (!table->m_isLoaded)
        table->MaterializeRowsIfNeeded(tableName->c_str());

    MetaTableRowNode *row = NULL;
    MetaTableRowFindByKey(&row, table->m_rowTreeHeader, rowKey);
    if (row == table->m_rowTreeHeader)
        return &g_emptySimpleUString;

    const unsigned int valueCount =
        static_cast<unsigned int>(row->valuesEnd - row->valuesBegin);
    if (valueIndex >= valueCount)
        return &g_emptySimpleUString;

    return row->valuesBegin + valueIndex;
}

static const SimpleUString *MetaMan_CopyDirectRowValues(MetaMan *metaMan,
                                                        const SimpleUString *tableName,
                                                        const SimpleUString *rowKey,
                                                        wchar_t ***outValues,
                                                        int requestedCount)
{
    wchar_t **values = static_cast<wchar_t **>(operator new(sizeof(wchar_t *) * requestedCount));
    for (int i = 0; i < requestedCount; ++i)
    {
        values[i] = static_cast<wchar_t *>(operator new(0x50));
        values[i][0] = L'\0';
    }
    *outValues = values;

    MetaTable *table = metaMan->FindTableByName(tableName);
    if (table == NULL)
        return &g_emptySimpleUString;

    if (!table->m_isLoaded)
        table->MaterializeRowsIfNeeded(tableName->c_str());

    MetaTableRowNode *row = NULL;
    MetaTableRowFindByKey(&row, table->m_rowTreeHeader, rowKey);
    if (row == table->m_rowTreeHeader)
        return &g_emptySimpleUString;

    const unsigned int availableCount =
        static_cast<unsigned int>(row->valuesEnd - row->valuesBegin);
    const unsigned int copyCount =
        availableCount < static_cast<unsigned int>(requestedCount)
            ? availableCount
            : static_cast<unsigned int>(requestedCount);

    for (unsigned int i = 0; i < copyCount; ++i)
        wcscpy_s(values[i], 0x28, row->valuesBegin[i].c_str());

    return row->valuesBegin;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Attached metadata lookup helpers for value-count, indexed value, and multi-value copy paths; IDA ties callers to `FindEntry`/`LoadStatValues` and callees to MetaTable materialization and row-key lookup. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# MetaMan Lookup Helpers `0x00523a00-0x00523c99`

## Status

- Entity kind: metadata lookup helper cluster with internal alignment padding.
- Proposed owner: [UID:0000LC][MetaMan](by-file/MetaMan.md)
- Confidence: strong for ownership, exact bounds, caller/callee relationships, row-value layout, fallback behavior, and source-facing helper names; medium-high for exact row/value-vector field names.
- Evidence basis: read-only IDA MCP `server_health`, `lookup_funcs`, `analyze_function`, `disasm`, `xrefs_to`, `get_bytes`, and `make_signature_for_range` refreshed on 2026-06-13. `wave2.py`/`wave3.py` source data was not used for this pass under the active supervisor rule.
- Rebuild handling: `source-authored`; this is NexusTK metadata lookup code with compiler-generated SEH/cookie epilogues and `0xcc` alignment inside the island.

## Covered Functions

| Range | IDA name | Proposed role |
| --- | --- | --- |
| `0x00523a00-0x00523a65` | `sub_523A00` | `MetaMan_GetDirectRowValueCount`: direct row value-count helper. Looks up a table and row key, materializes the table if needed, finds the row node, and returns the row's value count or `0`. |
| `0x00523a65-0x00523a70` | padding | `0x0b` / 11 bytes of `0xcc` alignment before the indexed-value helper (Verified with `int_convert.py`). |
| `0x00523a70-0x00523b39` | `sub_523A70` | `MetaMan_FindDirectRowValue`: indexed row-value lookup helper. Returns a pointer to the requested `SimpleUString` value or [UID:0000PW][g_emptySimpleUString](by-global/g_emptySimpleUString.md) on missing table, missing row, or out-of-range index. |
| `0x00523b39-0x00523b40` | padding | `0x07` / 7 bytes of `0xcc` alignment before the multi-value copy helper (Verified with `int_convert.py`). |
| `0x00523b40-0x00523c99` | `sub_523B40` | `MetaMan_CopyDirectRowValues`: multi-value copy helper used by `LoadStatValues`. Allocates an output pointer array and fixed `0x50` / 80-byte buffers (Verified with `int_convert.py`), then copies up to the requested number of row values. |

## Behavior

All three helpers follow the same table access pattern:

1. Call `MetaMan::FindTableByName` (`0x00523330`).
2. If the table exists and `MetaTable::m_isLoaded` at `+0x18` is false, call [UID:0001CC][0x00524870-0x00524c55.MetaTableMaterializeRows](by-memory/0x00524870-0x00524c55.MetaTableMaterializeRows.md).
3. Search the table row tree through [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md), now documented as source-ready `MetaTableRowFindByKey` with `MetaTableRowNode` and `SimpleUStringLessThan` source-facing names.
4. Read or copy values from the matched row node's `std::vector<SimpleUString>`-style value span.

`0x00523a70` and `0x00523b40` return the process-wide empty `SimpleUString` at `0x0066daec` when the lookup fails. That empty return value is a shared fallback, not a heap object owned by the caller.

The row node value span is treated as a `std::vector<SimpleUString>`-style `[begin,end)` pair at node offsets `+0x28` / 40 and `+0x2c` / 44 (Verified with `int_convert.py`). The helpers divide the byte span by `0x18` / 24 (Verified with `int_convert.py`) using the compiler's multiply-by-`0x2AAAAAAB` strength reduction, so a final source rewrite should express this as a value count rather than preserve the arithmetic artifact.

`sub_523B40` allocates caller-visible output storage before table lookup: a pointer array of `4 * requestedCount` bytes and one `0x50` / 80-byte buffer (Verified with `int_convert.py`) per requested value. It writes the pointer array through the caller's out parameter before lookup, initializes each buffer with a leading NUL wide character, then copies each recovered value with `wcscpy_s(destination, 0x28, source)`. `0x28` / 40 wide characters (Verified with `int_convert.py`) is the destination element capacity passed to the safe-copy routine.

These names are direct/static helper names, not CollectionPane-owned method names. The alias-aware public methods [UID:000088][MetaMan](by-class/MetaMan.md) `FindEntry`, `LoadStatValues`, and the `0x00523030` value-count wrapper call these helpers after consulting [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md). The helpers themselves perform direct table/row access only.

## Boundary And Signature Notes

- IDA MCP `lookup_funcs` on 2026-06-13 reports `sub_523A00` size `0x65` / 101 bytes, `sub_523A70` size `0xc9` / 201 bytes, and `sub_523B40` size `0x159` / 345 bytes (all decimal conversions verified with `int_convert.py`).
- `lookup_funcs` also reports `0x00523a65`, `0x00523b39`, and `0x00523c99` are not function starts, while successor `sub_523CA0` starts at `0x00523ca0`.
- IDA MCP `get_bytes` confirms `0x00523a65-0x00523a70` and `0x00523b39-0x00523b40` are all `0xcc`, and that `sub_523B40` ends with `retn 0x10` at `0x00523c96-0x00523c99` followed by `0x07` / 7 bytes of `0xcc` padding before `sub_523CA0` (Verified with `int_convert.py`).
- IDA MCP `make_signature_for_range 0x00523a00-0x00523c99` returned a unique full-range signature for the complete helper cluster.

## Caller Evidence

- `0x00523a00` is called by the unmodeled alias-aware value-count wrapper at `0x00523030-0x00523114`, specifically from `0x00523108` after the direct row count path finds a non-empty row. IDA does not currently mark that wrapper as a function, but disassembly shows a normal `push ebp` prologue, `retn 8` epilogue, and `0xcc` alignment through `0x00523120`.
- `0x00523a70` is called by `MetaMan::FindEntry` at `0x00523120` from the direct and alias-aware lookup branches, by `MetaMan::LoadStatValues` at `0x005231f0`, and by the unmodeled value-count wrapper while walking alias candidates.
- `0x00523b40` is called by `MetaMan::LoadStatValues` at `0x005231f0` from the direct and alias-aware multi-value branches.
- IDA MCP `lookup_funcs` rechecked on 2026-06-01 reports exact helper sizes: `0x00523a00` size `0x65`, `0x00523a70` size `0xc9`, and `0x00523b40` size `0x159`.
- IDA MCP `callers` rechecked on 2026-06-01 reports no direct callers for `0x00523a00`, four callers for `0x00523a70` (`0x0052313c`, `0x0052319f`, `0x005231cd`, `0x0052324c`), and two callers for `0x00523b40` (`0x005232b6`, `0x0052330a`).
- IDA MCP `xrefs_to` rechecked on 2026-06-01 shows all three helpers call `MetaTableMaterializeRows` (`0x00524870`) and `MetaTableRowFindByKey` (`0x005245c0`) before reading row value storage.
- 2026-06-25 B007 synchronization: [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md) now records four real code xrefs to `0x005245c0`: this range's direct calls at `0x00523a34`, `0x00523ae2`, and `0x00523c17`, plus the unmodeled alias-aware `MetaMan::GetValueCount` wrapper call at `0x005230d1`. The latter aligns with this page's existing `0x00523030-0x00523114` wrapper evidence and no longer blocks target C++.
- IDA MCP `xrefs_to 0x0066daec` shows failed lookup return paths from `0x00523b16` inside `sub_523A70` and `0x00523c76` inside `sub_523B40`, matching the shared empty-string fallback.
- IDA MCP `analyze_function 0x00523120` confirms alias-aware `FindEntry` first enumerates [UID:0001OL][0x0066de18-0x0066de30.g_metaAliasTableName](by-memory/0x0066de18-0x0066de30.g_metaAliasTableName.md), compares each alias string with [UID:0000PW][g_emptySimpleUString](by-global/g_emptySimpleUString.md), then retries `sub_523A70` against the alias value before falling back to the caller-provided table name.
- IDA MCP `analyze_function 0x005231f0` confirms `LoadStatValues` preallocates a caller output array, performs the same alias-table loop, calls `sub_523B40` for aliases, frees the speculative array if no alias was found, and then calls `sub_523B40` with the direct caller-provided table name.

## Touched State And Calls

| Address / callee | Role in this range |
| --- | --- |
| `0x00523330` (`MetaMan::FindTableByName`) | Resolves the table name to a `MetaTable*`; absence drives count `0` or empty-string fallback. |
| `0x00524870` (`MetaTableMaterializeRows`) | Lazy materializer called when `MetaTable + 0x18` / 24 loaded flag is false (Verified with `int_convert.py`). |
| `0x005245c0` (`MetaTableRowFindByKey`) | Searches the table row tree rooted at `MetaTable + 0x1c` / 28 (Verified with `int_convert.py`); the returned iterator/node is compared to the sentinel at the same offset. |
| `0x0066daec` (`g_emptySimpleUString`) | Shared fallback returned by failed single-value and multi-value paths. |
| `0x0066de18` (`g_metaAliasTableName`) | Alias-table key used by the immediate `FindEntry`/`LoadStatValues` callers before direct lookup. |
| `0x00516030`, `0x00516050`, `0x00516170` | Allocation/free support used by the multi-value copy path and its caller. |
| `_wcscpy_s` | Copies row values into fixed caller buffers with capacity `0x28` / 40 wide characters (Verified with `int_convert.py`). |

## Ownership Decision

Keep these helpers in `metadata/MetaMan.cpp` beside `MetaMan::FindEntry`, `MetaMan::LoadStatValues`, and `MetaTable`. They are not CollectionPane, ItemHelpPane, or resource-loader code even when those features consume metadata rows.

`AUTOGEN_PARENT_UID` remains [UID:0000LC][MetaMan](by-file/MetaMan.md), not [UID:000088][MetaMan](by-class/MetaMan.md), because the three helpers are source-local/static-style lookup helpers that take explicit table/key arguments and are shared by class methods rather than proven emitted `MetaMan` member methods. The class page remains supporting context and already clears the strict `85/85` parent gate if a later final-source pass proves a class-method representation.

## Reconstruction Notes

- Marked `RECONSTRUCTABLE:TRUE` and attached to [UID:0000LC][MetaMan](by-file/MetaMan.md) on 2026-06-01. Ownership is strong because all executable callers are MetaMan lookup paths or metadata-table materialization support.
- Formal C++ is now populated by the 2026-07-02 Agent-B007 UID0000LC implementation callback with static helper bodies for `MetaMan_GetDirectRowValueCount`, `MetaMan_FindDirectRowValue`, and `MetaMan_CopyDirectRowValues`. The bodies preserve the direct table lookup/materialization/row-find behavior, `g_emptySimpleUString` fallback, output buffer allocation, and `wcscpy_s(destination, 0x28, source)` copy semantics from the accepted report.
- This page must not absorb the alias-aware wrapper at `0x00523030-0x00523114` or the equality helper at `0x00523ca0`; those remain separate covered-range evidence/future exact-child work on [UID:0001C8][0x005227d0-0x00524581.MetaMan](by-memory/0x005227d0-0x00524581.MetaMan.md). `tableName->c_str()` and `SimpleUString::c_str()` are accepted inferred source-facing accessor spellings, not original-symbol proof, and `operator new` represents the allocation helpers at `0x00516030/0x00516050`.
- [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md) is no longer a blank-C++ dependency for the direct helper cluster: it now emits its own exact first-draft helper body and supplies the row lookup call used by all three direct helpers here.

## Cross-References

- [UID:0000LC][MetaMan](by-file/MetaMan.md)
- [UID:000088][MetaMan](by-class/MetaMan.md)
- [UID:0001C8][0x005227d0-0x00524581.MetaMan](by-memory/0x005227d0-0x00524581.MetaMan.md)
- [UID:000089][MetaTable](by-class/MetaTable.md)
- [UID:0001CC][0x00524870-0x00524c55.MetaTableMaterializeRows](by-memory/0x00524870-0x00524c55.MetaTableMaterializeRows.md)
- [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md)
- [UID:0001RH][meta-dat-metadata-cache](by-resource/meta-dat-metadata-cache.md)

## Changes

- 2026-07-02 Agent-B007 UID0000LC implementation callback:
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`.
  - Summary/evidence: inserted the three formal static helper functions from the accepted empty-emitter family report, preserving direct table lookup/materialization, `MetaTableRowFindByKey`, `g_emptySimpleUString` fallback, row value span/count behavior, multi-value output allocation, `wcscpy_s` copy semantics, unmodeled `0x00523030` alias-aware wrapper evidence, and the caveat that `SimpleUString::c_str()` spelling is inferred.
- 2026-06-19 Agent-B009 source-quality pass:
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`.
  - Summary/evidence: adopted the source-facing helper names `MetaMan_GetDirectRowValueCount`, `MetaMan_FindDirectRowValue`, and `MetaMan_CopyDirectRowValues`; clarified these are file-local direct helpers under MetaMan ownership, not CollectionPane or resource-loader code; and bounded formal C++ to a future exact helper-body pass rather than the class declaration pass.
- 2026-06-25 Agent-B007 [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md) synchronization:
  - Score unchanged.
  - Summary/evidence: recorded that [UID:0001CA][0x005245c0-0x0052462a.MetaTableRowFindByKey](by-memory/0x005245c0-0x0052462a.MetaTableRowFindByKey.md) now includes the three direct calls from this helper cluster plus the unmodeled `0x005230d1` alias-aware value-count wrapper call, resolves its source-facing helper/comparator names, and emits formal first-draft C++ on its own exact target page.
- 2026-06-13 Agent-A003 Goal 2 refresh:
  - What existed before: `COMPLETION:76`, `CONFIDENCE:84`, parent [UID:0000LC][MetaMan](by-file/MetaMan.md), with strong ownership but incomplete boundary, caller, row-layout, allocation, and fallback evidence.
  - Changed to: `COMPLETION:87`, `CONFIDENCE:90`; parent retained as [UID:0000LC][MetaMan](by-file/MetaMan.md).
  - Summary/evidence: live read-only IDA MCP confirmed helper sizes, internal padding, successor boundary, unique full-range signature, row-value span arithmetic, lazy table materialization, row-tree lookup, alias-aware callers, shared empty-string fallback, multi-value allocation/copy behavior, and the previously unrecorded `0x00523030-0x00523114` value-count wrapper caller. Confidence remains below final-audit level because final source-facing helper names and the exact struct field names for table rows/value vectors remain open.
