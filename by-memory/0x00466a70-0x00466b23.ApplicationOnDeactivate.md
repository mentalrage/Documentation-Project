*** UID:0002GK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Application::OnDeactivate()
{
    if (m_exitRequested)
        return;

    m_windowInputActive = false;

    if (ApplicationActiveGate == 0 && g_pScreenPane != NULL) {
        g_pApplication->m_isRunning = false;
        ::ShowWindow(g_pApplication->GetMainWindowHandle(), SW_MINIMIZE);
        g_pScreenPane->RestoreDirectDrawState();
    }

    if (g_pEventMan != NULL)
        g_pEventMan->ResetInputState(false);

    if (g_pKeySpeedMgr != NULL)
        g_pKeySpeedMgr->RestoreSystemKeyboardSettings();

    if (g_activeMapPane != NULL)
        g_activeMapPane->ClearQueuedMapStateRefresh();

    if (g_pConfig->m_soundEffectsEnabled)
        g_pSoundManager->SuspendSoundEffectsForApplicationDeactivate();

    if (g_pConfig->m_musicPlaybackMode == 0) {
        if (g_pSoundManager->m_musicPlaybackEnabled)
            g_pSoundManager->m_musicOptionEnabled = true;

        g_pMidiPlayer->DisableMidiPlayback();
        g_pSoundManager->m_musicPlaybackEnabled = false;
        g_pSoundManager->SetStreamVolume(g_pSoundManager->m_streamVolume);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Parent-attached source-ready Application::OnDeactivate implementation inherited unchanged by BaramApp; current MCP confirms exact boundary, virtual-only dispatch, exit guard, window-input clear, Application running-state clear, minimize/DirectDraw restore path, EventMan/keyboard/map cleanup, SFX suspend helper, and UID-owned inline music tail. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00466a70-0x00466b23 Application OnDeactivate

## UID0003OD Source Identity And Inheritance Correction - 2026-08-12

The Application primary vftable at `0x006125e4` and the BaramApp primary vftable at `0x0061d118` both store the identical `0x00466a70` pointer in slot 6. BaramApp therefore inherits this method unchanged; it does not own a distinct focus-deactivation override. The source-facing identity for this range is `Application::OnDeactivate`, owned and emitted by [UID:00000D][Application](by-class/Application.md).

Historical `BaramAppOnDeactivate` and `BaramApp::OnDeactivate` labels below record earlier reconstruction stages before the comparative vtable proof. The complete formal CPP keeps every accepted statement unchanged and changes only the class qualifier. The formal H channel remains blank because this method page does not own a partial Application class declaration.

## UID0001I9 Caller-Owned Focus Tail Synchronization - 2026-07-15

- Historical pre-UID0003OD state used score `90/92`, owner/emitter [UID:00000V][BaramApp](by-class/BaramApp.md), position `40`, reconstructable state, and the complete caller-owned deactivation body. The score, position, reconstructability, and body remain unchanged; current ownership/emission is [UID:00000D][Application](by-class/Application.md).
- The `0x0057a450` music tail now uses accepted `SoundManager::m_musicOptionEnabled` for the `+4` desired-state byte. When runtime music was enabled, deactivation records that desired state, disables MIDI, clears runtime playback, and reapplies the stored stream volume.
- The former `m_restoreMusicOnActivate` spelling is superseded: constructor, mute/unmute, activation, and deactivation evidence show a persistent music-option mirror rather than a one-shot focus flag. The old `friend class BaramApp` access conclusion belongs to the pre-UID0003OD reconstruction; current source placement requires equivalent private SoundManager access to be reconciled for the Application-owned method.
- Exact boundary, virtual-only reachability, `0x00466b1c -> 0x0057a450` chunk ownership, SFX suspension, window/input/map cleanup, inferred Application field spellings, and all prior negative evidence remain preserved.

## Scope

- Range: `0x00466a70-0x00466b23`
- Entity kind: class method
- Owner: [UID:00000D][Application](by-class/Application.md)
- Parent range: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)
- Disposition: reconstructable project code.
- Rebuild handling: `source-authored`
- Autogen status: parent-attached to [UID:00000D][Application](by-class/Application.md) at optional position `40`. Current B008 MCP evidence resolves the prior empty-emitter blockers, so this page now emits first-draft `void Application::OnDeactivate()` source through [UID:0000HG][Application](by-file/Application.md).

## Signature And State

- Source shape: `void Application::OnDeactivate()`.
- `this`: `Application *` in `ecx`; current Hex-Rays still presents byte offsets for some state, but the source route and method signature are no longer blocked.
- Guard byte: `this + 0x83a`, source-facing `m_exitRequested`; nonzero suppresses all transition work.
- Visible state writes: `this + 0x839 = 0`, now source-facing `m_windowInputActive = false`, and `g_pApplication + 0x838 = 0` at `0x00466a9d`, source-facing `g_pApplication->m_isRunning = false`, then `ShowWindow(g_pApplication->GetMainWindowHandle(), SW_MINIMIZE)` through `g_pApplication + 0x828`. B005 MCP session `261fb29b` ties `g_pApplication+0x838` to [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md); current UID0002GK evidence keeps `this+0x839` distinct from `m_isRunning`.
- Dispatch shape: no ordinary direct callers; reached through `Application`/`BaramApp` virtual-table slots at `0x006125fc` and `0x0061d130`.

## Behavior

`Application::OnDeactivate` is the background/minimize transition handler inherited unchanged by BaramApp. It exits immediately when `m_exitRequested` / `this + 0x83a` is set. Otherwise it clears `m_windowInputActive` / `this + 0x839`, and when `ApplicationActiveGate == 0` with [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md) present it clears `g_pApplication->m_isRunning`, minimizes the main window with `SW_MINIMIZE`, and restores the DirectDraw screen-pane state through [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md).

The non-window cleanup is source policy glue over already-owned subsystems. `g_pEventMan->ResetInputState(false)` maps to `sub_4A90D0` with the deactivation argument preserved as `false`; `g_pKeySpeedMgr->RestoreSystemKeyboardSettings()` maps to the accepted keyboard-speed restore helper; `g_activeMapPane->ClearQueuedMapStateRefresh()` maps to the map/parcel-walk cleanup call at `0x00505cc0`; and `g_pSoundManager->SuspendSoundEffectsForApplicationDeactivate()` maps to modeled SoundManager helper `0x0057a780`.

The `0x0057a450` audio tail is UID0002GK-owned inline source rather than a separate no-code blocker. Current `xrefs_to 0x0057a450` finds only the jump from `0x00466b1c` inside this method, and current decompilation includes the tail in `sub_466A70`: if config music playback mode is zero, it remembers whether SoundManager music was enabled, disables MIDI through `g_pMidiPlayer->DisableMidiPlayback()`, clears `g_pSoundManager->m_musicPlaybackEnabled`, and reapplies stream volume with `g_pSoundManager->SetStreamVolume(g_pSoundManager->m_streamVolume)`.

## IDA Evidence

- IDA MCP `lookup_funcs` on 2026-05-30 reports `sub_466A70` at `0x00466a70` with size `0xb3`, giving the exclusive range end `0x00466b23`.
- IDA MCP `disasm` shows the method reads `this + 0x83a`, clears `this + 0x839`, checks `dword_67A93C` and `dword_67A7CC`, writes `g_pApplication + 0x838`, calls `ShowWindow(g_pApplication->0x828, 6)`, and calls [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md).
- The same disassembly pauses event/background/input/audio paths through `dword_67A754`, `dword_67AB48`, `dword_67A764`, `dword_67A7C8`, `dword_67A7D0`, and `dword_67AB4C`.
- 2026-06-05 A003 IDA MCP refresh reconfirmed `sub_466A70` at `0x00466a70` size `0xb3`; `lookup_funcs` reports `0x00466b23` is not a function start. `callers` reports no direct code callers, while `xrefs_to` reports data references from `0x006125fc` and the `BaramApp` vtable slot at `0x0061d130`, consistent with virtual dispatch.
- The same refresh reports callees `sub_4A90D0`, `ShowWindow`, [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md), `sub_4EFF30`, `sub_505CC0`, and `sub_57A780` from the contiguous app-lifecycle body. `xrefs_to 0x0057a450` reports only the tail jump from `0x00466b1c`; querying that tail chunk shows audio callees `sub_5260D0` and `sub_57A340`.
- Historical 2026-06-06 A004 IDA MCP snapshot:
  - `lookup_funcs` reports `sub_466A70` at `0x00466a70` with size `0xb3`; `0x00466b23` is not a function start. IDA also associates the non-contiguous chunk at `0x0057a450` with the same function.
  - Disassembly confirms the `this + 0x83a` guard, `this + 0x839 = 0`, map-pane checks, `g_pApplication + 0x838 = 0`, `ShowWindow(*(g_pApplication + 0x828), 6)`, [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md)(`dword_67A7CC`), event/background/input pauses, parcel-walk cleanup through `dword_67A764`, sound-state checks, and tail jump from `0x00466b1c` to `0x0057a450`.
  - `callers 0x00466a70` reports no ordinary direct callers; `xrefs_to 0x00466a70` reports data refs from `0x006125fc` and `0x0061d130`.
  - `callees 0x00466a70` reports `sub_4A90D0`, `ShowWindow`, [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md), `sub_4EFF30`, `sub_505CC0`, and `sub_57A780`; the tail chunk at `0x0057a450` calls `sub_5260D0` and `sub_57A340`.
  - Byte audit over `0x00466980-0x00466b30` shows the seven-byte `0xcc` gap after `BaramAppOnActivate`, the exact `0x00466a70-0x00466b23` deactivation body, and alignment after the return.
- Historical 2026-06-16 C001 IDA MCP snapshot:
  - Saved function label `BaramAppOnDeactivate` at `0x00466a70`, size `0xb3`; post-apply `lookup_funcs` resolves both the address and name.
  - `xrefs_to 0x00466a70` again reports only the two vtable data refs at `0x006125fc` and `0x0061d130`; there are no ordinary direct code callers.
  - `disasm BaramAppOnDeactivate` confirms the transition guard, `this + 0x839 = 0`, map-pane `g_pApplication + 0x838 = 0` write, `ShowWindow(..., 6)`, [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md)(`dword_67A7CC`), event/background/input pause, parcel-walk cleanup through `dword_67A764`, audio state checks, and the tail jump at `0x00466b1c -> 0x0057a450`.
  - `callees BaramAppOnDeactivate` confirms the contiguous callee set: `ShowWindow`, [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md), `sub_4A90D0`, `KeySpeedMgr_RestoreSystemKeyboardSettings`, `sub_505CC0`, and `sub_57A780`.
  - In that dated snapshot, `analyze_function` reported Hex-Rays failure at `0x00466abb`, consistent with the documented non-contiguous audio-tail caveat; later evidence below superseded that failure without erasing it.
- 2026-06-23 B005 IsRunning field-writer sync:
  - IDA MCP session `261fb29b` rechecked [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md) and confirmed this deactivation method's write at `0x00466a9d mov byte ptr [eax+838h], 0` as the background writer for inferred `Application::m_isRunning`.
  - The earlier `this+0x839 = 0` store remains a distinct Application deactivation/input-state byte and is one reason `m_isRunning` is preferred over `m_isActive` as the emitted `+0x838` field name. This page is supporting lifecycle evidence for the Application getter and method, not BaramApp ownership of either field or method.
- Historical 2026-06-29 B008 MCP implementation snapshot, endpoint `127.0.0.1:13337` / IDB session `B006_0002F1_20260629`:
  - In that dated IDB, `lookup_funcs 0x00466a70` resolved function `sub_466A70`, size `0xb3`; the earlier saved `BaramAppOnDeactivate` IDA label was absent. UID0003OD now establishes the documented source identity as `Application::OnDeactivate`, inherited unchanged by BaramApp.
  - Hex-Rays decompilation now succeeds for `sub_466A70` and includes the non-contiguous tail chunk at `0x0057a450`; the old Hex-Rays-failure/no-final-C++ wording is historical.
  - `xrefs_to 0x00466a70` reports only data references from `0x006125fc` and `0x0061d130`; there are still no ordinary direct code callers, so virtual dispatch through the Application/BaramApp vtable slots remains the correct route.
  - `xrefs_to 0x0057a450` reports only the jump at `0x00466b1c` inside this UID0002GK body, proving the tail is represented inline by this method rather than as a separate child.
  - That dated disassembly/decompile confirmed `m_exitRequested` / `+0x83a`, `m_windowInputActive` / `+0x839`, `g_pApplication->m_isRunning` / `+0x838`, `ShowWindow(..., 6)` / `SW_MINIMIZE`, `g_pScreenPane->RestoreDirectDrawState()`, `g_pEventMan->ResetInputState(false)`, `g_pKeySpeedMgr->RestoreSystemKeyboardSettings()`, `g_activeMapPane->ClearQueuedMapStateRefresh()`, `SoundManager::SuspendSoundEffectsForApplicationDeactivate()` at `0x0057a780`, `g_pMidiPlayer->DisableMidiPlayback()`, and `SoundManager::SetStreamVolume()` in the `0x0057a450` tail.

## Reconstruction Notes

- Keep this method under [UID:0000HG][Application](by-file/Application.md) through [UID:00000D][Application](by-class/Application.md). BaramApp reaches it by inherited vtable pointer identity, not separate ownership.
- Helper/global names in the formal C++ are source-facing accepted names, not recovered public symbols. `ApplicationActiveGate` is the `dword_67A93C` gate, `m_windowInputActive` is the distinct `this+0x839` Application state byte, `g_activeMapPane` is the `dword_67A764` map-pane cleanup object, and the SoundManager/MidiPlayer member names are accepted support abstractions over documented fields.
- Rejected alternatives: keep-empty due to the old Hex-Rays failure, split `0x0057a450` as a standalone SoundManager child, treat `0x0057a450` as a named helper call, retain a distinct BaramApp override despite identical vtable pointers, or emit raw offset/decompiler-style field names. Current MCP evidence makes the first three stale, and comparative vtable identity proves Application as the class owner.
- Parent attachment remains valid at optional position `40`; this page remains `RECONSTRUCTABLE:TRUE` because the deactivation policy is NexusTK-owned source that must be rebuilt.

## Score Rationale

- Completion is `90`: exact function boundary, current function identity, virtual-only reachability, successful current Hex-Rays recovery, key field/global/helper mappings, stale tail-blocker historicalization, owned inline music tail, support-document route, and first-draft formal C++ are documented. Completion remains below final audit because exact original field/member spellings and header declarations remain inferred support names.
- Confidence is `92`: dated IDA disassembly/decompile, xrefs, callers/callees, byte/tail ownership evidence, and the Application/BaramApp/EventMan/SoundManager support docs agree on the source route and behavior. Confidence remains below final-source confidence because names such as `m_windowInputActive`, `ApplicationActiveGate`, and SoundManager private fields are source-facing reconstructions rather than recovered symbols.

## Historical Changes And Current UID0003OD Correction

UID0003OD supersedes the earlier BaramApp-qualified owner/emitter interpretation with current `Application::OnDeactivate` ownership and UID00000D routing. The dated entries below remain as historical reconstruction provenance; their old `BaramApp::OnDeactivate`, UID00000V owner/emitter, and friend-class conclusions are not current authority.

- 2026-06-29 B008 UID0002GK empty-emitter implementation:
  - Raised target metadata from `86/91` to `90/92`, kept owner/emitter as [UID:00000V][BaramApp](by-class/BaramApp.md), set optional emitter position `40`, and inserted first-draft formal `void BaramApp::OnDeactivate()` C++.
  - Summary/evidence: accepted B008 report `tools/leaser/Agents/Agent-B008/research/0002GK-BaramAppOnDeactivate-empty-emitter-source-quality.md`; current MCP endpoint `127.0.0.1:13337` / IDB session `B006_0002F1_20260629`; `lookup_funcs 0x00466a70 -> sub_466A70 size 0xb3`; `xrefs_to 0x00466a70` only `0x006125fc` and `0x0061d130`; `xrefs_to 0x0057a450` only `0x00466b1c`; current Hex-Rays decompile succeeds and includes the music tail. The old saved-label, Hex-Rays-failure, blank-C++, and audio-tail-blocker notes are historical for UID0002GK.

- 2026-06-23 B005 IsRunning field-writer sync:
  - Score unchanged at `86/91`; formal C++ remains blank.
  - Refreshed the visible-state write as exact `0x00466a9d` Application `m_isRunning` / `+0x838` writer evidence for [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md), while preserving `this+0x839` as a separate BaramApp active-state byte and preserving BaramApp as a writer/context owner only.
  - Summary/evidence: accepted B005 report `tools/leaser/Agents/Agent-B005/research/0002GO-ApplicationIsRunning-source-quality.md`, IDA MCP session `261fb29b`, updated target [UID:0002GO][0x004651e0-0x004651e7.ApplicationIsRunning](by-memory/0x004651e0-0x004651e7.ApplicationIsRunning.md), constructor writer `0x00463511`, activation writer `0x004669f5`, and ScreenPane consumer routes `0x00556dab` / `0x00556e36`.

- 2026-06-20 B002 SurfaceDirectDrawRestoreMaintenance caller sync:
  - Score unchanged at `86/91`.
  - Replaced raw `sub_5576A0` wording with [UID:00039R][0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance](by-memory/0x005576a0-0x00557810.SurfaceDirectDrawRestoreMaintenance.md) while preserving BaramApp ownership and blank final C++.
- 2026-06-16 C001 direct IDA deactivation refresh:
  - What changed: raised completion/confidence from `80/90` to `86/91`, saved the low-risk IDA function label `BaramAppOnDeactivate`, and kept owner/emitter routing through [UID:00000V][BaramApp](by-class/BaramApp.md).
  - Summary/evidence: live IDA MCP dry-run accepted the label, post-apply `lookup_funcs` verified it, `xrefs_to` reconfirmed virtual-only reachability through `0x006125fc` and `0x0061d130`, disassembly reconfirmed the background/minimize transition policy and `0x00466b1c -> 0x0057a450` audio tail, and `analyze_function` still fails on the non-contiguous body, so prototypes/locals/types/final C++ remain intentionally untouched.
- 2026-06-06 A004 parent attachment and evidence refresh:
  - What existed before: the page was `78/88`, reconstructable, and intentionally unassigned while the class and method were still just below the parent-attachment gate.
  - What it was changed to: raised to `80/90`, attached to [UID:00000V][BaramApp](by-class/BaramApp.md) at optional position `40`, added signature/state notes and score rationale, and kept final C++ blank.
  - Summary/evidence: live IDA MCP reconfirmed exact `0x00466a70-0x00466b23` bounds, no direct callers, vtable data refs, state writes, `ShowWindow(..., 6)`, callee set, `0x00466b1c -> 0x0057a450` tail chunk, tail audio callees, and surrounding padding.
- 2026-06-05 A003 autogen-gate correction:
  - What existed before: the page was scored `72/82`, attached to [UID:00000V][BaramApp](by-class/BaramApp.md), and emitted a C++ paraphrase despite unresolved fields/globals and a parent class below the documented attachment threshold.
  - What it was changed to: raised scores to `78/88`, cleared `AUTOGEN_PARENT_UID` and `AUTOGEN_PARENT_POSITION_OPTIONAL`, removed the premature C++ block, and recorded the virtual-dispatch refs, no-direct-caller status, callee set, and non-contiguous audio tail caveat.
  - Summary/evidence: IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, `callees`, `decompile`, and `disasm` on 2026-06-05 confirm the function boundary, vtable/data references, app-state writes, `ShowWindow(..., 6)`, and the `0x00466b1c -> 0x0057a450` audio-tail jump. Final source remains blocked by unresolved layout/global names and audio helper ownership.
- What existed before: `BaramApp::OnDeactivate` was only represented as a line item inside the broad [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) range.
- What it was changed to: this exact method page now tracks `0x00466a70-0x00466b23` with behavior evidence; the later A003/A004 passes removed the premature C++ draft and reattached the method only after the documented gate was met.
- Summary/evidence: IDA MCP `lookup_funcs` reports `sub_466A70` size `0xb3`; IDA MCP `disasm` shows the activation guard, active-byte clear, map-pane `ShowWindow(..., 6)`, event/background/input pause, parcel-walk, and audio pause paths.
