*** UID:000459 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000059 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FolderSelectDialog::OnControlCommand(int controlIndex, int notifyCode)
{
    if (controlIndex == 1)
    {
        if (m_resultCallback)
        {
            StringBaseW selectedPath;
            m_folderTreePane->BuildSelectedPath(selectedPath);
            m_resultCallback->Invoke(selectedPath);
        }
        CloseDialog();
    }
    else if (controlIndex == 2)
    {
        if (m_resultCallback)
        {
            StringBaseW emptyPath;
            m_resultCallback->Invoke(emptyPath);
        }
        CloseDialog();
    }
    else if (controlIndex == 0 && notifyCode == 10)
    {
        PopupMenuControlPane *driveList =
            GetChild<PopupMenuControlPane>(0);
        StringMenuItem *item =
            static_cast<StringMenuItem *>(
                driveList->GetItem(driveList->GetSelectedIndex()));
        wchar_t rootPath[100];
        item->CopyText(rootPath, 100);
        m_folderTreePane->SetRootNodeName(rootPath);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-authored FolderSelectDialog::OnControlCommand virtual handler; vtable-routed OK/cancel/drive-list notification behavior with StringBaseW callback dispatch and tree-root reset. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b1920-0x004b1a00 FolderSelectDialogOnCommand

## Status

- Disposition: reconstructable source-authored virtual command handler for [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md).
- Source module: [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md), `NexusTK/ui/dialogs/FolderSelectDialog.cpp`.
- Container split: exact child of [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md).
- Binary range: `0x004b1920-0x004b1a00`, IDA function size `0xe0`.
- Formal C++ status: accepted first-draft body is present and routed through [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md) at position `20`.

## Boundary

| Address / range | Meaning |
| --- | --- |
| `0x004b1920` | `FolderSelectDialog::OnControlCommand` entry; vtable-routed command handler. |
| `0x004b1a00` | Exclusive end and next source-shaped helper entry, `PopulateDriveList`. |
| `0x0061a4a4` | Vtable data slot pointing to `0x004b1920`. |

## Source Behavior

- Handles dialog command traffic for OK, cancel, and drive-list notification paths.
- OK path:
  - Uses `m_folderTreePane` and the selected tree state at `m_folderTreePane + 0x170`.
  - Calls the selected-path construction helper at `0x004b3090` from call site `0x004b197f`.
  - Invokes the stored result callback at `m_resultCallback` through virtual slot `+12` with the selected path.
  - Destroys the temporary string after callback dispatch and closes the dialog.
- Cancel path:
  - Constructs an empty string via `0x00582b20`.
  - Invokes the same result callback virtual slot `+12` with that empty string.
  - Destroys the temporary string through `0x00582b70` and closes the dialog.
- Drive-list notification path:
  - Tests the notification code `10`.
  - Fetches child zero through inherited DialogPane `List *m_controlManager` storage. Machine code at `0x004b19ae` uses the same List virtual `GetElementAt(0)` pointer-slot lookup/dereference as the constructor and [UID:00045A][0x004b1a00-0x004b1b05.FolderSelectDialogPopulateDriveList](by-memory/0x004b1a00-0x004b1b05.FolderSelectDialogPopulateDriveList.md); current source support expresses that contract as `GetChild<PopupMenuControlPane>(0)`.
  - Retrieves the selected index/item and copies the selected drive-list text into the local root buffer.
  - Calls `FolderTreePane::SetRootNodeName` at `0x004b25e0` to reset the folder tree root to the selected drive.
- Does not own the retained helper at `0x004b1a00`; that helper is a separate source-shaped function with no checked inbound route.

## Reachability And Evidence

- IDA function lookup confirms `0x004b1920` as a function start with size `0xe0`.
- No normal direct call xref is expected for this method; it is reached through vtable slot `0x0061a4a4` in [UID:0003BF][0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData](by-memory/0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData.md).
- Cached xrefs report the data reference from the vtable slot to `0x004b1920`.
- Fresh read-only public MCP evidence-method receipt: for `0x004b1920`, incoming-count authority is `xref_query.total`, which returns exactly `1` from data slot `0x0061a4a4`; aggregate `func_profile.caller_count` is not authoritative. Direct outgoing-call authority is public `callees`, which returns exactly eight routes and `more:false`: `0x00582b20` (`sub_582B20`), `0x004b3090` (`sub_4B3090`), `0x0049dad0` (`DialogPane::CloseDialog`), `0x005c772f` (`@__security_check_cookie@4`), `0x004981a0` (`PopupMenuControlPane::GetSelectedIndex`), `0x00498180` (`PopupMenuControlPane::GetItem`), `0x00517070` (`StringMenuItem::CopyText`), and `0x004b25e0` (`sub_4B25E0`).
- The selected-path helper call is at `0x004b197f` and reaches the [UID:00005A][FolderTreePane](by-class/FolderTreePane.md) selected-path builder at `0x004b3090`.
- Drive-root reset behavior calls `FolderTreePane::SetRootNodeName` at `0x004b25e0`.
- Callback ownership agrees with constructor/destructor evidence: constructor stores `m_resultCallback` at `this+0x270`, this handler invokes virtual slot `+12`, and the destructor releases the same object through virtual slot `+0`.
- The former `static_cast<PopupMenuControlPane *>(GetControl(0))` draft used an undefined source abstraction. Current DialogPane/List documentation resolves the inherited element-pointer/double-dereference contract, so the typed `GetChild` spelling is a support-backed correction rather than a behavior change.

## Ownership And Source Placement

- Canonical owner is [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md).
- Emitting source module is [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md).
- [UID:0003BF][0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData](by-memory/0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData.md) supplies the vtable route; the data page does not own this method body.
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md) owns the tree-control implementation reached by this command handler.

## C++ Readiness Notes

- Formal C++ emits the accepted `FolderSelectDialog::OnControlCommand(int controlIndex, int notifyCode)` body at file scope through [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md).
- The body expresses the three proven routes: OK selected-path callback, cancel empty callback, and drive-list notification root reset.
- The body uses the accepted source-facing fields `m_folderTreePane` and `m_resultCallback` plus support-backed drive-list names `PopupMenuControlPane`, `StringMenuItem`, `GetChild`, `GetSelectedIndex`, `GetItem`, `CopyText`, and `SetRootNodeName`.
- Only the shared child-lookup expression changed in the accepted callback. Command values, callback ordering, temporary-string lifetime, item cast/copy, selected-root buffer, tree-root update, score, metadata, and every unrelated formal line remain unchanged.
- The method body stays within `0x004b1920-0x004b1a00` and does not absorb the retained helper at `0x004b1a00`.

## Score Rationale

| Field | Rationale |
| --- | --- |
| Completion `94` | Exact function boundary, vtable reachability, all three control branches, `StringBaseW` callback use, tree helper routes, source ownership, file-scope emitter route, and accepted formal C++ are documented. |
| Confidence `95` | Vtable data, IDA function boundary, branch behavior, support declarations, and behavior-specific call evidence agree. Remaining uncertainty is limited to original local identifier spelling. |

## Cross-References

- [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md)
- [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md)
- [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md)
- [UID:0003BF][0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData](by-memory/0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)

## Changes

- 2026-07-14 B005 accepted UID00045A support synchronization:
  - Replaced only the stale undefined `GetControl(0)` cast with `GetChild<PopupMenuControlPane>(0)` in the complete managed body and documented the inherited List element-slot lowering at `0x004b19ae`.
  - Preserved `87/91`, owner UID000059, emitter UID0000JF, position `20`, reconstructable true, `Nested:0`, and all unrelated OK/cancel/notification behavior and evidence.

- 2026-06-26 B013 accepted report implementation:
  - What existed before: `COMPLETION:85`, emitter routed to [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md), position was blank, and formal C++ was blank under the earlier split-stage instruction.
  - Changed to: `COMPLETION:87`, `EMITTER_UIDS:0000JF`, `EMITTER_POSITION_OPTIONAL:20`, and the accepted `FolderSelectDialog::OnCommand(int commandId, int notifyCode)` first-draft body in the formal C++ block.
  - Summary/evidence: the first-draft body preserves the three proven paths from MCP/decompile evidence: OK selected-path callback, cancel empty callback, and drive-list notification root reset through the hosted [UID:00005A][FolderTreePane](by-class/FolderTreePane.md).
- 2026-06-26 B001 split implementation callback:
  - Created as the exact vtable-routed `OnCommand` child of the former aggregate [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md).
  - Original split-stage formal C++ was blank; this is superseded by the B013 first-draft body above.
