*** UID:0004AC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewMacroEditControlPane::Refresh()
{
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ten-byte NewMacroEditControlPane post-load refresh member; the sole NewMacroDialog caller invokes it after shortcut-index, key, and spell-mode updates, and it invalidates inherited GrafPort::m_visibleBounds through Pane primary slot +0x20 with no parent lookup or source-visible return. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057fa70-0x0057fa7a NewMacroEditControlPane Refresh

## Status

- Address range: `[0x0057fa70,0x0057fa7a)`, exactly ten bytes.
- Entity/source disposition: source-authored, out-of-line, nonvirtual public `NewMacroEditControlPane::Refresh` member. It is not an inline-only duplicate, compiler vtable forwarder, this-adjusting thunk, deleting wrapper, or no-code range.
- Direct owner/emitter: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) `NewMacroEditControlPane`.
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Source file: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md), `NexusTK/ui/dialogs/MacroDialogs.cpp`.
- Rebuild handling: emit the exact formal C++ above through the class child route; do not duplicate it in the aggregate, by-file page, vtable data, or compiler-wrapper pages.

## Exact Binary And Range Evidence

- IDA models `sub_57FA70` at `0x0057fa70` with size `0x0a`; no modeled function begins at the half-open endpoint `0x0057fa7a`.
- Exact bytes: `8b 11 8d 41 44 50 ff 52 20 c3`.
- SHA256 over only those ten bytes: `B84326BE49B27E28BFE460034B009BF9787976F75422D964A6ED9ABC0501E4E8`.
- With imagebase `0x00400000`, the target RVA is `0x0017fa70`. The containing `.text` section has RVA `0x1000` and raw pointer `0x400`, placing the target at raw file offset `0x0017ee70`; raw-PE and MCP byte reads agree.
- [UID:0004AB][0x0057fa60-0x0057fa70.NewMacroEditControlPaneSetSpellMode](by-memory/0x0057fa60-0x0057fa70.NewMacroEditControlPaneSetSpellMode.md) ends exactly at the target start. Six `0xcc` alignment bytes occupy `[0x0057fa7a,0x0057fa80)` before [UID:0001IM][0x0057fa80-0x0057fa9e.NewMacroEditControlPaneTeardownHelper](by-memory/0x0057fa80-0x0057fa9e.NewMacroEditControlPaneTeardownHelper.md); neither neighbor is part of this method.

## Instruction, CFG, And ABI Evidence

- The complete body is five instructions: load the primary vptr from `this`; form `this + 0x44`; push that address; call virtual slot `+0x20`; return normally.
- The function has one basic block, cyclomatic complexity one, no local frame, no branch, no EH scaffold, no security cookie, and no caller-supplied stack arguments.
- ECX remains the complete `NewMacroEditControlPane` receiver. The single pushed argument is consumed by the virtual callee, and the plain `ret` matches a no-argument thiscall member.
- The final indirect call leaves its result in EAX, but the sole caller immediately overwrites EAX during loop bookkeeping. This is incidental ABI propagation, not evidence for a source-visible integer return; the source declaration and formal body therefore remain `void`.

## Called Virtual And Field Resolution

- The `NewMacroEditControlPane` primary table cell at `0x0062d2a8` contains `0x00544800`; the corresponding [UID:0003CA][0x006219e8-0x00621a6c.PaneVtableData](by-memory/0x006219e8-0x00621a6c.PaneVtableData.md) base cell at `0x00621a08` contains the same target. Current Pane evidence identifies this primary slot `+0x20` operation as `Pane::InvalidateRect(const RectBounds *)`.
- The next New primary cell at `0x0062d2ac` points to `0x00544a20`, independently confirming that `0x0062d2a8` is the expected primary-table `+0x20` slot rather than an unrelated data pointer.
- [UID:0001VH][PaneLayout](by-type/by-struct/PaneLayout.md) resolves complete-object `+0x44` as inherited `GrafPort::m_visibleBounds`. The machine body passes the address of that field, so the source operation is own-visible-bounds invalidation.
- Historical `Pane::m_bounds` and `m_localBounds` spellings are superseded aliases. They are retained only as provenance and are not current source-facing field names.

## Caller And Source-Shape Evidence

- Exactly one code xref reaches the function start: `0x0053f294` in [UID:00049O][0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage](by-memory/0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage.md). No data or vtable xref points to this target, and no xref reaches its endpoint.
- The caller invokes `SetShortcutIndex`, `SetKey`, and `SetSpellMode` on the row in that order and then directly calls `Refresh`. That sequencing establishes the semantic post-load refresh role and supports the existing source-facing name.
- A distinct `NewMacroDialog` calls this member through a `NewMacroEditControlPane *`; with no friendship evidence, the existing public declaration is the source-safe access level.
- The dedicated direct call, class declaration, adjacent out-of-line New-row methods, and absence from the class vtable establish a normal source-authored nonvirtual member even though the compact instruction tail is not unique.

## Comparator And Negative Evidence

- The exact ten-byte sequence also occurs at `0x00481000`, `0x00482300`, `0x00482c4a`, `0x00482d40`, `0x00544740`, `0x0057ffa0`, and `0x005bc880`. These sites have mixed ownership and source dispositions, so byte identity alone cannot classify this target as compiler glue.
- `0x00481000` and `0x00482300` are vtable-only compiler secondary forwarders; `0x00482c4a` is a tail inside an owner-specific source helper; `0x00482d40` is unreferenced raw ABI evidence; `0x00544740` is a tail inside `Pane::Show`; [UID:0004AK][0x0057ffa0-0x0057ffaa.IntegrateMacroEditControlPaneRefreshDisplay](by-memory/0x0057ffa0-0x0057ffaa.IntegrateMacroEditControlPaneRefreshDisplay.md) is a direct-call source member; and `0x005bc880` is a separate vtable-only comparator.
- This target has no parent-pointer read, parent lookup, alternate receiver, field write, explicit return-value construction, string, global, direct callee record, branch, null argument, allocator, packet, resource, destructor, or exception behavior.
- `RequestParentRefresh` and the old parent-redraw summary are rejected as behaviorally false: the body reads the complete object's own primary vptr and passes its own inherited visible bounds. No parent participates.
- Raw vtable-call C++, `InvalidateRect(NULL)`, a parameterless invalidation helper, `int Refresh()`, and ownership transfer to Pane/GrafPort are rejected because they either contradict the bytes/caller or expose compiler mechanics instead of the source method.

## Naming, Score, And Generated Contract

- `Refresh` remains the highest-probability original source-facing name. It matches the current complete class and caller union, describes the post-setter role, and is supported by the parallel source member `IntegrateMacroEditControlPane::RefreshDisplay`. Stripped symbols prevent lexical certainty, which is reflected in confidence `94` rather than a higher score.
- Completion `92` reflects closed range, bytes/hash, PE mapping, instructions, CFG/ABI, caller, return liveness, virtual slot, field, owner/emitter, source placement, access, source/compiler disposition, formal body, padding, negative evidence, and generated expectations.
- Final generated verification must contain exactly one UID0004AC definition using inherited `m_visibleBounds` invalidation, zero stale `RequestParentRefresh` in that definition, zero UID0004AC Empty Emitter Marker, and no duplicate handwritten vtable/thunk/destructor ABI source.
