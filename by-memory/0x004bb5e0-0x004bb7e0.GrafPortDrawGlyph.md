*** UID:00016I | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00005V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00005V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void GrafPort::DrawGlyph(wchar_t ch, wchar_t nextCh, bool fillBackground, const unsigned char *glyphColors)
{
    EPFTileContext glyphContext;
    RectBounds backgroundRect;
    RectBounds destinationRect;
    short glyphAdvance = 0;
    short fontWidth = 0;
    short fontHeight = 0;
    unsigned char blitMode;


    if (!m_drawEnabled) {
        return;
    }

    g_pFontImageLib->MeasureGlyphWidth(m_fontId,
                                       ch,
                                       0,
                                       1,
                                       glyphColors,
                                       NULL,
                                       &glyphAdvance,
                                       &glyphContext);

    if (fillBackground) {
        unsigned char savedRenderMode;
        unsigned int savedDrawColor;

        g_pFontImageLib->GetFontMetrics(m_fontId, &fontWidth, &fontHeight);

        backgroundRect.left = m_currentX;
        backgroundRect.top = m_currentY - fontHeight;
        backgroundRect.right = m_currentX + fontWidth;
        backgroundRect.bottom = m_currentY;

        savedRenderMode = m_drawMode;
        savedDrawColor = m_drawColor;
        m_drawMode = m_textDrawMode;
        m_drawColor = m_textBackFillColor;
        g_pfnFillRect(this, &backgroundRect);
        m_drawMode = savedRenderMode;
        m_drawColor = savedDrawColor;
    }

    g_pFontImageLib->GetFontMetrics(m_fontId, NULL, &fontHeight);

    if (static_cast<unsigned short>(ch) <= 0x00ff && m_fontId != 0) {
        destinationRect.left = m_currentX + glyphContext.bounds.left - 2;
        destinationRect.top = m_currentY - fontHeight + glyphContext.bounds.top - 1;
        destinationRect.right = m_currentX + glyphContext.bounds.right - 2;
        destinationRect.bottom = m_currentY - fontHeight + glyphContext.bounds.bottom - 1;
    } else {
        destinationRect.left = m_currentX + glyphContext.bounds.left;
        destinationRect.top = m_currentY - fontHeight + glyphContext.bounds.top + 1;
        destinationRect.right = m_currentX + glyphContext.bounds.right;
        destinationRect.bottom = m_currentY - fontHeight + glyphContext.bounds.bottom + 1;
    }

    if (m_textDrawMode == 0) {
        blitMode = 1;
    } else if (m_textDrawMode == 2) {
        blitMode = 3;
    } else {
        blitMode = m_textDrawMode;
    }

    g_pfnBlitSprite(this,
                    &glyphContext,
                    &glyphContext.bounds,
                    &destinationRect,
                    blitMode,
                    m_palette,
                    0);

    m_currentX += glyphAdvance;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private GrafPort glyph drawer used only by text-run helpers; live MCP confirms exact half-open range, FontImageLib metric/decode calls, optional fill/invalidate, glyph blit callback, draw-mode mapping, and cursor advance; first-draft C++ is populated. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004bb5e0-0x004bb7e0 GrafPort DrawGlyph

## Status

- Confidence: strong for function extent, direct class ownership, caller set, and behavior; medium-high for final method name and field names.
- Current generated owner: `FittingRoomDownloadControlPane`.
- Direct owner: [UID:00005V][GrafPort](by-class/GrafPort.md), routed through [UID:0000JR][GrafPort](by-file/GrafPort.md).
- Direct callers: `0x004bab20` and `0x004bab70`.
- Boundary repair: the legacy filename used final-byte-style end `0x004bb7df`; live IDA confirms the half-open function range ends at `0x004bb7e0`.
- Formal C++ is populated. The former blank-C++ blocker that required a `90/90+` gate is superseded by the current combined-score/emitter rule; this target has a GrafPort owner/emitter route and a supported first-draft body.

## Function

| Range | Current/proposed role | Notes |
| --- | --- | --- |
| `0x004bb5e0-0x004bb7e0` | `GrafPort::DrawGlyph` | Decodes one glyph through `FontImageLib::MeasureGlyphWidth`, optionally fills/invalidates a background rect, computes the destination rectangle from current cursor and font metrics, blits the glyph tile through the render callback table, and advances the GrafPort cursor X by the glyph advance. |

## Evidence Notes

- IDA MCP lookup on 2026-05-25 reports `sub_4BB5E0` size `0x200`.
- IDA MCP lookup on 2026-06-01 confirms `sub_4BB5E0` starts at `0x004bb5e0`, has size `0x200`, and ends at exclusive address `0x004bb7e0`; the next function starts at `0x004bb7e0`.
- IDA caller checks report only two direct callers: [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) at `0x004bab20` and [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) at `0x004bab70`.
- Callees include `FontImageLib::MeasureGlyphWidth`, `FontImageLib::GetFontMetrics`, the EPF tile-context initializer at `0x00457a60`, the text blit callback `dword_69B3E8`, and the invalidation callback `dword_69B3FC`.
- The body reads the same GrafPort-style state block as the surrounding text helpers: text-enabled flag, current font id, current X/Y, draw mode, color state, and callback argument fields.
- The 2026-06-01 decompile initializes an EPF tile context, checks the receiver text-enabled flag at byte offset `113`, decodes/measures a glyph through `dword_67AB24` using font id `this[69]`, optionally invalidates a background/text rect through `dword_69B3FC`, computes source and destination rectangles from current cursor/font metrics, blits through `dword_69B3E8`, and advances the GrafPort cursor X by the glyph advance.

2026-06-13 live IDA MCP refresh:

- `lookup_funcs` confirms `0x004bb5e0` as `sub_4BB5E0`, size `0x200` / decimal `512` bytes (Verified with `int_convert.py`), and `0x004bb7e0` as the next modeled function `sub_4BB7E0`.
- `analyze_function 0x004bb5e0` reports direct callers only from [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md): `0x004bab57` in `sub_4BAB20` and `0x004bac8a` in `sub_4BAB70`.
- 2026-06-18 B001 text-helper dependency correction: direct callers remain [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) `DrawGlyphWithTextColors` and `DrawWideText`. The caller-built six-byte color pattern uses `m_textColor` at `this+0x7c` and `m_textBackColor` at `this+0x80`. `m_textDrawMode` at `this+0x88` is mapped by DrawGlyph for sprite blitting (`0 -> 1`, `2 -> 3`, otherwise preserved), which rejects older dirty-byte labels for this field.
- 2026-06-18 B004 [UID:0003XF][0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw](by-memory/0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw.md) recheck: the adjacent raw helper at `0x004bb5b0-0x004bb5d1` writes the same six-byte text/back-color pattern built inline by this function's two direct callers, [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) `DrawGlyphWithTextColors` and `DrawWideText`. The match validates the pattern semantics but not a call edge; `GrafPortDrawGlyph` still begins at `0x004bb5e0`, and no branch/pointer route to the raw helper is proven.
- Callees are [UID:0000JH][FontImageLib](by-file/FontImageLib.md) helpers `0x004b6020` and `0x004b5ff0`, [UID:0000J4][EPFTileContext](by-file/EPFTileContext.md) initializer `0x00457a60`, render callback `dword_69B3E8`, invalidation callback `dword_69B3FC`, and the security-cookie check.
- The decompile confirms the GrafPort receiver byte at offset `113`, font id at word slot `69`, cursor fields around dword slots `26/27`, draw-mode/color state around byte offset `112` and byte offset `136`, optional invalidation before drawing, and cursor advance after blit.
- Boundary bytes at `0x004bb7d0` end with `8b e5 5d c2 10 00`, then immediately begin the next function prologue `55 8b ec` at `0x004bb7e0`, proving the corrected exclusive end.

2026-06-26 B002 live MCP source-quality pass:

- MCP was available through session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. `server_health` command `#20` returned `status:"ok"`, `auto_analysis_ready:true`, and `hexrays_ready:true`.
- Command IDs used for the accepted implementation evidence are `#20` `server_health`, `#21` `lookup_funcs`, `#22` `analyze_function 0x004bb5e0`, `#25` `xrefs_to`, `#26` `decompile 0x004bb5e0`, `#27` `disasm 0x004bb5e0`, `#28` `analyze_function 0x004bab20`, `#29` `analyze_function 0x004bab70`, and `#30` `lookup_funcs` for direct caller/callee addresses.
- Boundary proof: command `#21` reports `0x004bb5e0` as `sub_4BB5E0`, size `0x200`; `0x004bb7df` is inside that function; `0x004bb7e0` starts the next function `sub_4BB7E0`, size `0xe4`. Command `#27` shows the `retn 10h` epilogue at `0x004bb7dd`, confirming the live half-open range `0x004bb5e0-0x004bb7e0` and keeping the stale `0x004bb7df` name as a final-byte caveat only.
- Direct callers: commands `#22` and `#25` report only `0x004bab57` in `sub_4BAB20` and `0x004bac8a` in `sub_4BAB70`, both in [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md).
- Caller argument evidence: command `#28` shows `0x004bab20` builds `{m_textBackColor, m_textColor, m_textBackColor, m_textColor, m_textColor, m_textBackColor}` from `this+0x80` and `this+0x7c`, then calls this glyph drawer with glyph, `nextGlyph`, background flag `0`, and the pattern. Command `#29` shows `0x004bab70` builds the same pattern and passes the same call shape from its `StringIter` loop.
- The glyph drawer itself keeps the source-facing `nextCh` parameter because callers pass it, but command `#22`/`#27` shows the body passes `0` to the third `FontImageLib::MeasureGlyphWidth` parameter and does not use or forward `nextCh`.
- Callees from command `#22` are `sub_457A60` (`EPFTileContext::Initialize`), `sub_4B6020` (`FontImageLib::MeasureGlyphWidth`), `sub_4B5FF0` (`FontImageLib::GetFontMetrics`), `unk_69B3FC` (`g_pfnFillOrInvalidateRect`), `unk_69B3E8` (`g_pfnBlitSprite` / `SurfaceSpriteBlitProc`), and the compiler security-cookie check.
- `g_pFontImageLib` refs from command `#25` occur at `0x004bb5f4`, `0x004bb6ac`, `0x004bb6f2`, and `0x004bb73a`.
- Callback slots: command `#22`/`#27` shows the optional fill/invalidate callback call to `dword_69B3FC` at `0x004bb688`, and the final tile/glyph blit callback call to `dword_69B3E8` at `0x004bb7c1`.
- Behavior proof: the body initializes the local `EPFTileContext` before the draw-enabled check at `this+0x71`; decodes/measures the glyph through FontImageLib with decode flag `1` and caller-supplied colors; optionally fills a background rect while temporarily replacing `this+0x70` with `this+0x88` and `this+0x74` with `this+0x78`; calls FontImageLib metrics again for placement; subtracts `2/-1` for byte-sized glyphs with nonzero font id and otherwise applies the `+1` vertical placement path; maps text draw mode `0 -> 1` and `2 -> 3`; calls the sprite blit callback with `this+0x84`; and advances `this+0x6c` by the signed glyph advance output.
- Adjacent raw helper [UID:0003XF][0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw](by-memory/0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw.md) remains separate: command `#25` found no xrefs to `0x004bb5b0`, and the live callers build the pattern inline before entering this function.

## Field And Helper Mapping

| Offset / symbol | Source-facing name | Evidence |
| --- | --- | --- |
| `this+0x68` | `m_currentY` | Existing GrafPort text-helper C++ and target rect math. |
| `this+0x6c` | `m_currentX` | Existing GrafPort text-helper C++ and target cursor advance. |
| `this+0x70` | `m_drawMode` | Saved/restored around the fill callback and used by accepted text-helper C++. Historical `m_renderMode` is a superseded alias only. |
| `this+0x71` | `m_drawEnabled` | Early draw gate. |
| `this+0x74` | `m_drawColor` | Saved/restored around the fill callback. |
| `this+0x78` | `m_textBackFillColor` | Temporary fill color for optional glyph background. |
| `this+0x7c` | `m_textColor` | Caller pattern construction in [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md). |
| `this+0x80` | `m_textBackColor` | Caller pattern construction in [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md). |
| `this+0x84` | `m_palette` | Borrowed `DLPalette *` installed through [UID:000163][0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers](by-memory/0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers.md); forwarded as the sprite-blit callback palette argument. |
| `this+0x88` | `m_textDrawMode` | Optional fill mode source and text draw-mode mapping for blit. |
| `this+0x8a` | `m_fontId` | FontImageLib metric/decode calls. Historical `m_fontStyleId` is a superseded alias only. |
| `0x0067ab24` | `g_pFontImageLib` | Four target refs in command `#25`. |
| `0x0069b3e8` | `g_pfnBlitSprite` / `SurfaceSpriteBlitProc` | Final glyph/tile blit callback at `0x004bb7c1`. |
| `0x0069b3fc` | `g_pfnFillOrInvalidateRect` | Optional background fill/invalidate callback at `0x004bb688`. |
| `0x00457a60` | `EPFTileContext::Initialize` | Local tile-context setup. |
| `0x004b5ff0` | `FontImageLib::GetFontMetrics` | Background and placement metric calls. |
| `0x004b6020` | `FontImageLib::MeasureGlyphWidth` | Glyph advance/decode call into the local tile context. |

## Ownership Notes

This function is the private glyph workhorse for the shared text helpers, not fitting-room download/progress code. Keep it with GrafPort text drawing unless later source reconstruction proves a separate text-render source file existed.

The direct semantic owner is the [UID:00005V][GrafPort](by-class/GrafPort.md) class because the function is a `thiscall`-shaped receiver method over GrafPort draw state and is called only by GrafPort text-run helpers. The class routes to [UID:0000JR][GrafPort](by-file/GrafPort.md), so the owner/emitter path clears the current combined-score/emitter gate for first-draft C++.

## Reconstruction Notes

- `RECONSTRUCTABLE` is true because the function has a verified single body, only two direct callers in the GrafPort text-run cluster, and a fully mapped relationship to FontImageLib metrics/decode plus render callbacks.
- Formal C++ is populated. The former `90/90+` blank-C++ blocker is stale; current policy allows first-draft code for an owned/emitting target once the combined score and evidence support it. This target now has exact live half-open range proof, direct caller/callee proof, field/helper mapping, callback-slot mapping, and GrafPort owner/emitter routing.
- Generated source order keeps this class child immediately after the separate file-static [UID:0003XF][0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw](by-memory/0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw.md) helper. UID0003XF remains owned by file UID0000JR and uses class UID00005V only as its assembler emitter, producing `UID0003XE -> UID0003XF -> UID00016I` without changing this method's own class owner/emitter route.
- The source-facing signature keeps `nextCh` because both direct text-run callers pass it, but the body intentionally does not use or forward it; the glyph drawer passes `0` to the third `FontImageLib::MeasureGlyphWidth` parameter.
- `m_palette` is the source-facing spelling for `this+0x84`. UID000163 proves the slot is a borrowed `DLPalette *` through constructor null initialization, no destructor release, and caller save/install/restore or install/clear behavior; the older opaque brush/context spelling remains documented in UID000163 provenance only.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | The page records corrected half-open range, direct class owner/emitter, caller/callee set, local EPFTileContext setup, FontImageLib decode/metric calls, optional fill/invalidate, sprite blit callback, cursor advance, field/helper mapping, stale-range caveat, raw-helper non-merge evidence, and first-draft formal C++. |
| Confidence | 92 | Live MCP session `80de0a67` confirms the `0x200`-byte function extent, next-function boundary, only two direct callers, GrafPort receiver state usage, expected FontImageLib/render callback callees, and exact cursor advance. Confidence stays below final-audit/source-output levels because callback typedefs and some original member spellings remain inferred. |

## Cross-References

- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [UID:0000JR][GrafPort](by-file/GrafPort.md)
- [UID:00005V][GrafPort](by-class/GrafPort.md)
- [UID:0000JH][FontImageLib](by-file/FontImageLib.md)
- [UID:0000J4][EPFTileContext](by-file/EPFTileContext.md)

## Changes

- 2026-08-23 B007 UID0003XF Gate 2A source-order support:
  - Preserved this page's score, class ownership/emitter metadata, formal body, and all glyph behavior evidence.
  - Recorded the adjacent helper's ordering-only UID00005V assembler route after command `000000027223` physically placed UID0003XF immediately before UID00016I.
- 2026-07-12 B004 UID000163 field-name/type synchronization:
  - Score, metadata, method signature, and every non-target formal C++ line remain unchanged.
  - Replaced only the target-related opaque member spelling with `m_palette` in the callback argument, field map, reconstruction note, and historical provenance summary after UID000163 proved `this+0x84` is borrowed `DLPalette *` state.
- 2026-06-26 B002 implementation callback:
  - Before: `COMPLETION:86`, `CONFIDENCE:89`, formal C++ blank, and reconstruction notes still used the stale `90/90+` code-entry blocker.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:92`; `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:00005V`, `EMITTER_UIDS:00005V`, and blank emitter position remain unchanged. First-draft formal `GrafPort::DrawGlyph` C++ is populated.
  - Summary/evidence: accepted B002 report `tools/leaser/Agents/Agent-B002/research/00016I-GrafPortDrawGlyph-source-quality.md` incorporated live MCP session `80de0a67` command IDs `#20-#30`, exact live half-open boundary `0x004bb5e0-0x004bb7e0`, stale `0x004bb7df` caveat, two direct GrafPort text-run callers, FontImageLib metric/decode calls, EPFTileContext initialization, callback slots `dword_69B3FC` and `dword_69B3E8`, GrafPort field mapping including the then-described brush/context slot at `this+0x84` now source-named `m_palette`, ignored `nextCh`/FontImageLib third-parameter `0`, raw helper [UID:0003XF][0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw](by-memory/0x004bb5b0-0x004bb5d1.GrafPortPatternPackRaw.md) non-merge evidence, and cursor advance through `m_currentX += glyphAdvance`.
- 2026-06-13 Agent-A001 Goal 2 refresh:
  - Before: `COMPLETION:76`, `CONFIDENCE:86`, owner/emitter [UID:0000JR][GrafPort](by-file/GrafPort.md), and stale final-byte filename end `0x004bb7df`.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:89`, `CANONICAL_OWNER:00005V`, `EMITTER_UIDS:00005V`, and half-open filename end `0x004bb7e0`.
  - Summary/evidence: live IDA MCP reconfirmed `sub_4BB5E0` size `0x200`, the next function at `0x004bb7e0`, only two direct calls from `GrafPortTextRunHelpers`, FontImageLib/EPFTileContext/render-callback callees, GrafPort draw-state offsets from decompilation, and the `retn 0x10` tail immediately before the next prologue. That pass left C++ blank under the then-used `90/90+` code-entry gate; the 2026-06-26 B002 callback supersedes that blocker.
