*** UID:0002JR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000092 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000092 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewHumanImageLib::NewHumanImageLib()
    : LObject(),
      m_partEntries(),
      m_layerEntries(),
      m_motionEntries(),
      m_compositionDrawCommands(),
      m_loadedPartList(),
      m_acc2drwEntries(),
      m_hairColorEntries()
{
    g_pNewHumanImageLib = this;

    LoadMotionTable(L"Motion.tbl");

    {
        DATFile layerFile;
        unsigned char header[23];
        int layerCount = 0;

        layerFile.Open(L"Layer.tbl");
        layerFile.Read(header, sizeof(header));
        layerFile.Read(&layerCount, sizeof(layerCount));

        for (int index = 0; index < layerCount; ++index)
        {
            NewHumanLayerEntry layer = {};

            layerFile.Read(&layer.layerId, sizeof(layer.layerId));
            layerFile.Read(layer.name, sizeof(layer.name));
            layerFile.Read(&layer.childCount, sizeof(layer.childCount));

            if (layer.childCount > 0)
            {
                layer.childIndexes = new int[layer.childCount];
                layerFile.Read(layer.childIndexes,
                               sizeof(layer.childIndexes[0]) * layer.childCount);
            }

            m_layerEntries.push_back(layer);
        }

        layerFile.Close();
    }

    {
        DATFile partFile;
        unsigned char header[23];
        int partCount = 0;

        partFile.Open(L"Part.tbl");
        partFile.Read(header, sizeof(header));
        partFile.Read(&partCount, sizeof(partCount));

        for (int index = 0; index < partCount; ++index)
        {
            NewHumanPartEntry part = {};
            char partName[256];

            partFile.Read(&part.partId, sizeof(part.partId));
            partFile.Read(partName, 20);
            partFile.Read(&part.drawIndexBase, sizeof(part.drawIndexBase));
            partFile.Read(&part.descriptorBase, sizeof(part.descriptorBase));

            const int partNameLength = strlen(partName);
            MultiByteToWideChar(CP_ACP, 0, partName, partNameLength, part.name, 255);
            part.name[partNameLength] = L'\0';

            part.spriteArchive = ResolveSpritePartPath(part.name);
            m_partEntries.push_back(part);
        }

        partFile.Close();
    }

    LoadPartSpriteDescriptors();
    LoadAcc2DrwTable(L"acc2drw.tbl");

    {
        DATFile hairColorFile;
        char colorText[6];
        const int hairColorCount = m_partEntries[kNewHumanHairPartIndex].descriptorCount;

        hairColorFile.Open(L"HAIRCOL.TBL");

        for (int index = 0; index < hairColorCount; ++index)
        {
            memset(colorText, 0, sizeof(colorText));
            hairColorFile.ReadLine(colorText, sizeof(colorText));
            m_hairColorEntries.push_back(ParseUnsignedLongBase10(colorText));
        }

        hairColorFile.Close();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Initializes singleton/vtable/vector/list state, including the typed per-draw composition-command vector, and loads `Motion.tbl`, `Layer.tbl`, `Part.tbl`, per-part descriptors, `acc2drw.tbl`, and `HAIRCOL.TBL`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004dfd10-0x004e024a NewHumanImageLib Constructor

## Status

- Confidence: strong for the IDA function boundary, singleton/vtable stores, object-vector initialization, resource table load sequence, constructor-local cleanup table, and direct class parent; medium for final source-facing field names and exact row-structure names.
- Owner class: [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md).
- Likely source file: [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md).
- Parent aggregate: [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md).
- Canonical owner/emitter: [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md). The constructor is source-owned and class-owned, and the formal constructor C++ block is now populated under the current combined-score/emitter gate with source-facing inferred names documented below.
- IDA function: `sub_4DFD10`.

## Signature And Frame Evidence

- Observed IDA shape: `_DWORD *__fastcall sub_4DFD10(_DWORD *a1)`, with `ECX` carrying `this` and the body returning the constructed object pointer.
- Source-level signature: `NewHumanImageLib::NewHumanImageLib()`. The return value is the compiler/decompiler constructor convention, not a source return type.
- IDA `analyze_function` on 2026-06-13 reports size `0x53a` / 1338 bytes (Verified with `int_convert.py`), 61 basic blocks, cyclomatic complexity 6, and 389 disassembly instructions including EH/cleanup tail chunks.
- The function reserves `0x1a8` / 424 bytes of local stack storage (Verified with `int_convert.py`) for DAT-file temporaries, row buffers, the 256-byte ANSI part-name buffer, and the temporary wide part-name/record buffer.
- The constructor uses MSVC SEH/security-cookie scaffolding. The source reconstruction should express ordinary C++ member construction and cleanup semantics; it should not preserve `SEH_4DFD10`, stack-cookie checks, or raw unwind-state variables as source code.

## Address Range

- Start: `0x004dfd10`.
- End: `0x004e024a` exclusive.
- Size: `0x53a` bytes.
- Following bytes: `0x004e024a-0x004e0250` are six `0xcc` alignment bytes before [UID:00017S][0x004e0250-0x004e05c4.NewHumanImageLibDestructor](by-memory/0x004e0250-0x004e05c4.NewHumanImageLibDestructor.md).

## Behavior

This is the ordinary constructor for the `NewHumanImageLib` singleton object. It constructs the `LObject` base, records the singleton pointer, installs the `NewHumanImageLib` vtable, initializes all owned vector/list triplets, then loads the table-driven new-human rendering resources.

Observed initialization and load order:

1. Call the `LObject` constructor at `0x004f4a80`.
2. Store `this` into [UID:0000RT][g_pNewHumanImageLib](by-global/g_pNewHumanImageLib.md) / IDA `dword_67A760`, or clear it when constructor unwind passes the `this == -4` sentinel path.
3. Install [UID:0001Y9][NewHumanImageLibVtable](by-type/by-vtable/NewHumanImageLibVtable.md) at object offset `+0x00`.
4. Zero the vector triplets at `+0x04`, `+0x10`, `+0x1c`, `+0x28`, `+0x48`, and `+0x54`.
5. Initialize the list/sentinel state at `+0x34-+0x47` and allocate an 8-byte sentinel node whose first pointer points back to the list header.
6. Call `LoadMotionTable` at `0x004e0e20` for `Motion.tbl`, filling the `+0x1c/+0x20/+0x24` motion vector.
7. Open `Layer.tbl`, read a 23-byte header and 32-bit row count, then append 0x20-byte layer records into the `+0x10/+0x14/+0x18` vector. Each layer row can own a child-index array.
8. Open `Part.tbl`, read a 23-byte header and 32-bit row count, read 32-byte file rows, convert each 20-byte ANSI part name to wide text, resolve the part EPF path through [UID:0000VA][ResolveSpritePartPath_004E19D0](by-item/ResolveSpritePartPath_004E19D0.md), and append 0x44-byte / 68-byte in-memory part records into the `+0x04/+0x08/+0x0c` vector.
9. Call `LoadPartSpriteDescriptors` at `0x004e13b0`, which opens each part-specific descriptor file using the `.DSC` suffix.
10. Call `LoadAcc2DrwTable` at `0x004e1800` for `acc2drw.tbl`, filling the compact `+0x48/+0x4c/+0x50` accessory-draw vector.
11. Open `HAIRCOL.TBL`, read the hair-color count from `[partEntriesBegin + 0x188]` (`0x188` = 392 bytes, Verified with `int_convert.py`), parse six-byte rows as integer palette indices, and append them into the `+0x54/+0x58/+0x5c` hair-color vector.

## Layout Evidence

IDA decompilation confirms the object offsets used by the constructor:

| Object offset | Constructor evidence |
| --- | --- |
| `+0x00` | vtable store to `NewHumanImageLib::vftable` after `LObject` construction. |
| `+0x04/+0x08/+0x0c` | `Part.tbl` vector; 32-byte file rows are expanded into 0x44-byte / 68-byte in-memory records after ANSI-to-wide part-name conversion and EPF path resolution. |
| `+0x10/+0x14/+0x18` | `Layer.tbl` vector; 0x20-byte rows are appended, with optional child-index arrays allocated from the row count. |
| `+0x1c/+0x20/+0x24` | `Motion.tbl` vector loaded by [UID:000419][0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable](by-memory/0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable.md); in-memory `NewHumanMotionEntry` rows are 0x42 bytes and can own per-frame arrays. The file payload is a 33-byte fixed prefix plus count-driven frame data, not a simple fixed 0x42-byte file row. |
| `+0x28/+0x2c/+0x30` | `m_compositionDrawCommands` begin/end/capacity triplet initialized by the constructor and cleaned by shallow 80-byte vector helper `0x004557f0` in the destructor. |
| `+0x34-+0x47` | List/sentinel state; constructor allocates an 8-byte sentinel node and destructor cleans it with `0x004e5c30`. |
| `+0x48/+0x4c/+0x50` | `acc2drw.tbl` vector loaded by `0x004e1800`; rows are compact three-byte boolean flags derived from decimal digits. |
| `+0x54/+0x58/+0x5c` | Hair-color vector loaded from `HAIRCOL.TBL`; rows are parsed through the shared decimal/integer conversion helper. |

## Resource And Row Evidence

The constructor performs all new-human startup table loading in one ordered pass:

| Table/resource | Call site(s) | Constructor evidence | Destination |
| --- | --- | --- | --- |
| `Motion.tbl` | `0x004dfe4f` pushes the UTF-16 literal, `0x004dfe5a` calls `0x004e0e20`. | The callee is `LoadMotionTable`; the constructor passes `this` in `ECX`. | `+0x1c/+0x20/+0x24` motion vector. |
| `Layer.tbl` | `0x004dfe91` references `0x0061c2fc`, then DAT reads at `0x004dfea1` and `0x004dfeb5`. | Reads a `0x17` / 23-byte header, a 4-byte row count, then appends `0x20` / 32-byte rows (Verified with `int_convert.py`). Rows with a positive child count allocate/read a 4-byte child-index array. | `+0x10/+0x14/+0x18` layer vector. |
| `Part.tbl` | `0x004dffec` references `0x0061c310`, then DAT reads at `0x004e0000` onward. | Reads a `0x17` / 23-byte header, a 4-byte row count, clears a `0x44` / 68-byte temporary row (Verified with `int_convert.py`), then reads a 32-byte file row as 4-byte id, 20-byte ANSI name, and two 4-byte scalar fields. It converts the name with `MultiByteToWideChar`, calls `ResolveSpritePartPath` at `0x004e00f0`, and stores the returned EPF metadata pointer at row offset `+0x40`. | `+0x04/+0x08/+0x0c` part vector. |
| Per-part descriptor tables | `0x004e017c` calls `0x004e13b0`. | Runs only after the part vector has been populated. The paired `.DSC` literal at `0x0061c33c` belongs to this descriptor-loading phase. | Part descriptor state owned by part rows. |
| `acc2drw.tbl` | `0x004e0181` references `0x0061c324`, then `0x004e0188` calls `0x004e1800`. | Delegates accessory-to-draw parsing to the local loader helper. | `+0x48/+0x4c/+0x50` accessory-draw vector. |
| `HAIRCOL.TBL` | `0x004e0198` references `0x0061c010`, then `0x004e01a7` opens the DAT file. | Reads the loop count from `[partEntriesBegin + 0x188]`, reads six-byte decimal text rows through `0x0049c370`, converts them via `0x005cea6d`, and appends 4-byte palette indices. The full-capacity append path at `0x004e020d` calls [UID:00030Q][0x00420de0-0x00420eea.PointerVectorGrowInsert](by-memory/0x00420de0-0x00420eea.PointerVectorGrowInsert.md), proving that helper is shared 4-byte vector support and not fitting-room-private. | `+0x54/+0x58/+0x5c` hair-color vector. |

IDA `get_bytes` on 2026-06-13 confirms the relevant UTF-16 bytes: the cluster at `0x0061c2e4` decodes as `Motion.tbl`, `Layer.tbl`, `Part.tbl`, `acc2drw.tbl`, `.DSC`, and `%s.EPF`, while `0x0061c010` decodes as `HAIRCOL.TBL`. This byte-level check avoids relying on IDA's misleading rendered UTF-16 string text.

## Cleanup/Unwind Evidence

The constructor has compiler-generated cleanup tails attached to `SEH_4DFD10`. They are not source-authored logic, but they prove the object members that the constructor considers live at each initialization state:

| Handler/tail | Evidence |
| --- | --- |
| `0x00600230` | Jumps to [UID:000182][0x004e5be0-0x004e5beb.NewHumanImageLibSingletonClearHelper](by-memory/0x004e5be0-0x004e5beb.NewHumanImageLibSingletonClearHelper.md) after adding `4` to the saved object pointer, matching the singleton lifetime registered by this constructor. |
| `0x00600243` | Adds `0x10` and jumps to the generic vector cleanup tail used for the layer vector. |
| `0x00600251` / ref `0x0060025a` | Adds `0x1c` and jumps to [UID:000185][0x004e5dd0-0x004e5e39.DestroyMotionVector](by-memory/0x004e5dd0-0x004e5e39.DestroyMotionVector.md), matching the motion-vector member initialized through `LoadMotionTable`. Current UID000185 MCP session `supervisor_recovery_20260705` treats `0x0060025a` as cleanup-table/lifetime metadata, not an ordinary constructor source call. |
| `0x0060025f` | Adds `0x28` and jumps to `0x004557f0`, matching the shallow `m_compositionDrawCommands` vector triplet. |
| `0x0060026d` | Adds `0x34` and jumps to list/sentinel cleanup helper `0x004e5ce0`. |
| `0x0060027b` | Adds `0x48` and jumps to accessory-vector cleanup helper `0x004e5d00`. |
| `0x00600289` | Adds `0x54` and jumps to the integer-vector cleanup helper `0x00423810`, matching the hair-color vector. |
| `0x00600297`, `0x006002a2`, `0x006002ad` | Clean DAT-file temporaries through `0x0049c160`. |

The `+0x1c` / `0x0060025a` route to [UID:000185][0x004e5dd0-0x004e5e39.DestroyMotionVector](by-memory/0x004e5dd0-0x004e5e39.DestroyMotionVector.md) is specifically `m_motionEntries` vector-storage cleanup metadata and does not require any constructor-body call to `DestroyMotionVector`.

This unwind table supports the class-owner assignment because all cleanup targets are for `NewHumanImageLib` member state initialized by this constructor, not a mixed helper range.

## IDA Evidence

- `lookup_funcs 0x004dfd10` reports `sub_4DFD10`, size `0x53a`, range `0x004dfd10-0x004e024a`.
- IDA byte check reports `0x004e024a-0x004e0250` as `cc cc cc cc cc cc` alignment before the destructor.
- IDA `analyze_function 0x004dfd10` on 2026-06-13 reports one code xref to the constructor at `0x004f60a1`, with the caller function summarized as `sub_4F5FB0`.
- IDA `analyze_function 0x004dfd10` on 2026-06-13 reports callees including `0x004f4a80` (`LObject` construction), DAT/file read helpers `0x0049c130/0x0049c180/0x0049c240/0x0049c310/0x0049c370`, `0x004e0e20` (`LoadMotionTable`), `0x004e13b0` (`LoadPartSpriteDescriptors`), `0x004e1800` (`LoadAcc2DrwTable`), `0x004e19d0` (`ResolveSpritePartPath`), `MultiByteToWideChar`, vector growth helpers `0x004e50a0`, `0x004e5420`, shared 4-byte vector growth helper `0x00420de0`, integer conversion helper `0x005cea6d`, and compiler/runtime helpers.
- IDA `lookup_funcs` on 2026-06-13 reconfirms `0x004e024a` is not a function and the next function is the destructor at `0x004e0250`, while the load helpers are `0x004e0e20` size `0x21f`, `0x004e13b0` size `0x446`, `0x004e1800` size `0x1c5`, and `0x004e19d0` size `0x557`.
- IDA `get_bytes` on 2026-06-13 reports `0x004e024a-0x004e0250` as `cc cc cc cc cc cc`.
- IDA byte reads at `0x0061c2e4`, `0x0061c010`, `0x0061c324`, and `0x0061c33c` confirm the UTF-16 table/suffix literals `Motion.tbl`, `Layer.tbl`, `Part.tbl`, `acc2drw.tbl`, `.DSC`, `%s.EPF`, and `HAIRCOL.TBL`.
- IDA `trace_data_flow 0x0067a760` on 2026-06-13 reconfirms constructor writes at `0x004dfd67` and `0x004dfd6e`, destructor clear at `0x004e059e`, singleton-helper clear at `0x004e5be0`, and many consumer reads from render/look/UI paths.

## Reconstructability Notes

This range is source-authored NexusTK constructor code and must be reconstructed. The formal C++ block is populated under the current code-entry rule: the page is `RECONSTRUCTABLE:TRUE`, has nonblank `EMITTER_UIDS:000092`, routes through [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) to the valid [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) source root, and has `(COMPLETION + CONFIDENCE) / 2 > 85`. The names `DATFile`, `NewHumanLayerEntry`, `NewHumanPartEntry`, `m_partEntries`, `m_layerEntries`, `m_motionEntries`, `m_compositionDrawCommands`, `m_loadedPartList`, `m_acc2drwEntries`, `m_hairColorEntries`, `kNewHumanHairPartIndex`, `ResolveSpritePartPath`, and `ParseUnsignedLongBase10` are source-facing inferred names unless already proven elsewhere.

## 2026-07-14 Composition Draw Command Member Sync

- The constructor's zeroed `+0x28/+0x2c/+0x30` triplet is now identified as the typed per-draw `std::vector<NewHumanCompositionDrawCommand>` member, inferred source name `m_compositionDrawCommands`, rather than an unresolved auxiliary vector.
- [UID:0004DQ][0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport](by-memory/0x004e70a0-0x004e70e9.NewHumanCompositionDrawCommandPushBackCompilerSupport.md) and [UID:0004PA][0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport](by-memory/0x004e5600-0x004e57bb.NewHumanCompositionDrawCommandVectorGrowInsertCompilerSupport.md) independently prove 80-byte append/grow behavior for this member inside `BuildCompositionDrawCommands`.
- The exact shallow row comprises `int partId` at `+0x00`, 40-byte `FrameDrawRecord` at `+0x04`, 32-byte `SurfaceSpriteBlitOptions` at `+0x2c`, and `DLPalette *palette` at `+0x4c`. BuildComposition produces it, RenderPreparedComposition consumes it, Draw reorders/removes and resets the vector end, and destructor helper `0x004557f0` frees retained capacity without element destructors.
- Constructor behavior, score `91/92`, owner/emitter UID000092, `Nested:4`, and all unrelated resource-loading code remain unchanged. The complete formal block changes only the accepted member initializer token.
- `NewHumanCompositionDrawCommand` and `m_compositionDrawCommands` are implementation-ready inferred spellings. Exact offsets, width, lifetime, and compiler-only push_back/grow disposition are proven; no standalone helper body is introduced.

## Assignment And Score Rationale

- Assignment remains [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md), not the file aggregate, because this exact range is the ordinary class constructor: it builds the `LObject` base, installs the `NewHumanImageLib` vtable, publishes `g_pNewHumanImageLib`, initializes the class layout through `+0x5c`, and the constructor unwind table cleans the same class-owned members.
- The direct parent [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) clears the strict gate at `85/85` and records the matching singleton, vtable, layout, method-family, and resource-table ownership. The file parent [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) also clears the broader source-root gate at `87/85`.
- Completion is raised from `86` to `91` because the 2026-06-25 B011 pass adds current MCP-backed helper caller graph evidence, helper decompile summaries, raw string/data evidence for all constructor table literals, cleanup/unwind table member-lifetime evidence, below-gate helper child-page coverage, and formal first-draft constructor C++.
- Confidence is raised from `90` to `92` because the current IDA session directly confirms function lookup, decompile behavior, xrefs, data refs, raw bytes, helper routes, and the distinction between constructor-inline loops and out-of-line loader calls. It remains below final-audit range because several names in the formal block are source-facing inferred names rather than original-symbol-proven names.

## 2026-06-25 B011 Constructor C++ And Helper Sync

- 2026-07-06 B015 DestroyMotionVector implementation sync:
  - Clarified that the UID000185 cleanup-table route at `0x00600251` / `0x0060025a` is compiler lifetime/unwind metadata for `m_motionEntries` vector storage, not an ordinary constructor source call.
  - No constructor formal C++ change was made; the constructor continues to call `LoadMotionTable(L"Motion.tbl")` and leaves destructor/vector cleanup to object lifetime paths.

The accepted B011 implementation updates the constructor under the current combined-score/emitter gate and supersedes the older blank-C++ rationale. The source block deliberately stays inside `0x004dfd10-0x004e024a`: it calls the out-of-line helpers where the binary calls them (`LoadMotionTable`, `LoadPartSpriteDescriptors`, and `LoadAcc2DrwTable`) and keeps the actual constructor-inline `Layer.tbl`, `Part.tbl`, and `HAIRCOL.TBL` loops in the constructor body. It does not paste helper child-page bodies into this range.

B002's 2026-06-30 [UID:000419][0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable](by-memory/0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable.md) implementation now supplies the class-owned helper body that this constructor calls. Current MCP session `supervisor_resume_20260629` reconfirmed `sub_4E0E20` as size `0x21f`, sole constructor caller `0x004dfe5a`, the `Motion.tbl` literal xref at `0x004dfe4f` / `0x0061c2e4`, and the `VectorGrowMotion` capacity-helper call at `0x004e0ff2`. The promoted helper reads the 23-byte header/count, builds 0x42-byte in-memory `NewHumanMotionEntry` objects from a 33-byte fixed file prefix plus per-frame arrays, and appends to `m_motionEntries`; the constructor should continue to call it out of line as shown in the formal constructor C++ block.

Current 2026-06-25 MCP provenance recorded by the B011 report:

- IDA MCP session `80de0a67` was active on `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`.
- `server_health` returned `status: ok`; module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready: true`, `hexrays_ready: true`, `strings_cache_ready: true`, and strings cache size `2067`.
- No IDA DB edits, renames, comments, type changes, or function creation were made.

Current function lookup and boundary facts:

| Address | Current finding |
| --- | --- |
| `0x004dfd10` | Constructor `sub_4DFD10`, size `0x53a`, exact range `0x004dfd10-0x004e024a`. |
| `0x004e024a` | Not a function; followed by `cc cc cc cc cc cc` padding before the destructor at `0x004e0250`. |
| `0x004e0d70` | [UID:000417][0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord](by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md), size `0x5f`. |
| `0x004e0dd0` | [UID:000418][0x004e0dd0-0x004e0dda.NewHumanImageLibGetHeadCount](by-memory/0x004e0dd0-0x004e0dda.NewHumanImageLibGetHeadCount.md), size `0x0a`. |
| `0x004e0e20` | [UID:000419][0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable](by-memory/0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable.md), size `0x21f`. |
| `0x004e1040` | [UID:00041A][0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable](by-memory/0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable.md), size `0x170`, modeled extent `0x004e1040-0x004e11b0` exclusive; now class-owned/emitting at `88/90`; no current direct xrefs. |
| `0x004e11b0` | [UID:00041B][0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable](by-memory/0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable.md), size `0x1f7`; no current direct xrefs; now class-owned/emitting at `88/90` as retained `NewHumanImageLib::LoadPartTable`. |
| `0x004e13b0` | [UID:00041C][0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors](by-memory/0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors.md), size `0x446`. |
| `0x004e1800` | [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md), size `0x1c5`; now class-owned/emitting at `88/90` as the constructor-called `acc2drw.tbl` loader. |
| `0x004e4f10` | [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md), compact 3-byte row grow/insert helper. |
| `0x004e50a0` | [UID:00041F][0x004e50a0-0x004e523e.VectorGrowLayer](by-memory/0x004e50a0-0x004e523e.VectorGrowLayer.md), size `0x19e`. |
| `0x004e5240` | [UID:00017T][0x004e5240-0x004e5415.VectorGrowMotion](by-memory/0x004e5240-0x004e5415.VectorGrowMotion.md), size `0x1d5`. |
| `0x004e5420` | [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md), size `0x1d6`. |

Current helper caller graph:

- The constructor has one code xref from `0x004f60a1` in `sub_4F5FB0`.
- `LoadMotionTable` is called only from the constructor at `0x004dfe5a`.
- `LoadPartSpriteDescriptors` is called only from the constructor at `0x004e017c`.
- `LoadAcc2DrwTable` is called only from the constructor at `0x004e0188`.
- `ResolveSpritePartPath` is called from the constructor at `0x004e00f0` and from `LoadPartTable` at `0x004e12fe`.
- `VectorGrowAcc2Drw` is called only from `LoadAcc2DrwTable` at `0x004e195a`.
- `VectorGrowLayer` is called from the constructor's inline `Layer.tbl` loop at `0x004dffb2` and from `LoadLayerTable` at `0x004e1171`.
- `VectorGrowPart` is called from the constructor's inline `Part.tbl` loop and from `LoadPartTable`.
- `LoadLayerTable` and `LoadPartTable` are reusable source-shaped helper bodies, but current xrefs show no constructor calls to `0x004e1040` or `0x004e11b0`; the constructor inlines those loops. [UID:00041A][0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable](by-memory/0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable.md) now owns formal first-draft `NewHumanImageLib::LoadLayerTable` C++ and [UID:00041B][0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable](by-memory/0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable.md) now owns formal first-draft `NewHumanImageLib::LoadPartTable` C++ for the retained helpers, so this constructor page should keep its formal constructor code unchanged instead of pasting either helper body here.

Raw string/data evidence:

| Address | Literal/source role | Xref evidence |
| --- | --- | --- |
| `0x0061c2e4` | `Motion.tbl` | Constructor xref at `0x004dfe4f`. |
| `0x0061c2fc` | `Layer.tbl` | Constructor xref at `0x004dfe91`. |
| `0x0061c310` | `Part.tbl` | Constructor xref at `0x004dffec`. |
| `0x0061c324` | `acc2drw.tbl` | Constructor xref at `0x004e0181`. |
| `0x0061c010` | `HAIRCOL.TBL` | Old-human xref at `0x004d4910` and constructor xref at `0x004e0198`. |
| `0x0061c33c` | `.DSC` | Descriptor-loader xref at `0x004e1435`. |
| `0x0061c348` | `%s.EPF` | Sprite-path resolver xref at `0x004e1d5e`. |
| `0x0061b850` | `%s%d.EPF` | Old-human resolver refs and new-human resolver refs at `0x004e1a1f`, `0x004e1a4b`, and `0x004e1b27`. |

Current raw byte reads decode the adjacent UTF-16 table cluster at `0x0061c2e4` as `Motion.tbl`, `Layer.tbl`, `Part.tbl`, `acc2drw.tbl`, `.DSC`, and `%s.EPF`. IDA `get_string` can render adjacent UTF-16 literals in this cluster incorrectly, so byte-level decoding remains the preferred evidence for these strings.

Cleanup/unwind evidence from `0x00600230` is compiler lifetime evidence, not source code. The cleanup table clears the singleton through `0x004e5be0`, then tails through member cleanup helpers for offsets `+0x04`, `+0x10`, `+0x1c`, `+0x28`, `+0x34`, `+0x48`, and `+0x54`, plus DAT-file temporary cleanup. This supports the member layout and class ownership but should not be hand-emitted as EH state code.

Rejected alternatives preserved by the accepted B011 report:

- Do not change canonical owner away from [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md); DAT readers, resource pages, EPF metadata, and vector helpers are dependencies, not owners of this constructor.
- Do not make [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md) the canonical owner; it is a non-emitting local-method-cluster aggregate.
- Do not split the constructor range; `0x004dfd10-0x004e024a` is one function followed by padding.
- Do not keep formal C++ blank under retired hard-threshold gate wording; the current code-entry gate is the combined-score/emitter rule.
- Do not insert decompiler-style C++ with `sub_*`, raw `_DWORD *`, EH state variables, or stack temporaries.
- Do not invent constructor source calls to `LoadLayerTable` or `LoadPartTable`; current xrefs show no calls from the constructor to those helpers.
- Do not paste the `LoadAcc2DrwTable` body into this constructor page; current xrefs show the constructor calls the out-of-line loader at `0x004e0188`, and [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md) now owns that formal helper body.

## 2026-06-30 LoadLayerTable Support Sync

B006's accepted implementation promotes [UID:00041A][0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable](by-memory/0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable.md) to a class-owned/emitting helper at `88/90` with formal `void NewHumanImageLib::LoadLayerTable(const wchar_t *tableName)` C++. Current MCP session `supervisor_resume_20260629` confirms raw `sub_4E1040`, size `0x170`, zero direct callers, the modeled exclusive end `0x004e11b0`, 23-byte header/count reads, 32-byte `NewHumanLayerEntry` rows, optional child-index arrays, and the [UID:00041F][0x004e50a0-0x004e523e.VectorGrowLayer](by-memory/0x004e50a0-0x004e523e.VectorGrowLayer.md) capacity-helper call at `0x004e1171`.

This update does not change this constructor's formal C++ block. The constructor still opens `L"Layer.tbl"` inline, reads the same row shape into `m_layerEntries`, and calls [UID:00041F][0x004e50a0-0x004e523e.VectorGrowLayer](by-memory/0x004e50a0-0x004e523e.VectorGrowLayer.md) from its own full-capacity path at `0x004dffb2`; no current xref proves a constructor call to `0x004e1040`. The promoted helper is a retained source-shaped method under the same class/file route, not evidence to fold, duplicate, or move constructor code.

## 2026-06-30 LoadPartTable Support Sync

B007's accepted implementation promotes [UID:00041B][0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable](by-memory/0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable.md) to a class-owned/emitting helper at `88/90` with formal `void NewHumanImageLib::LoadPartTable(const wchar_t *tableName)` C++. Current MCP session `supervisor_resume_20260629` confirms raw `sub_4E11B0`, size `0x1f7`, zero direct callers, exact body `0x004e11b0-0x004e13a7`, padding `0x004e13a7-0x004e13b0`, 23-byte header/count reads, 32-byte `Part.tbl` file rows, 68-byte `NewHumanPartEntry` memory rows, [UID:0000VA][ResolveSpritePartPath_004E19D0](by-item/ResolveSpritePartPath_004E19D0.md) callsite `0x004e12fe`, and [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md) full-capacity callsite.

This update does not change this constructor's formal C++ block. The constructor still opens `L"Part.tbl"` inline, reads the same 32-byte file rows into 68-byte `NewHumanPartEntry` objects, calls [UID:0000VA][ResolveSpritePartPath_004E19D0](by-item/ResolveSpritePartPath_004E19D0.md) from `0x004e00f0`, and calls [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md) from its own full-capacity path. No current xref proves a constructor call to `0x004e11b0`; the promoted helper is a retained source-shaped method under the same class/file route, not evidence to fold, duplicate, or move constructor code.

## 2026-07-01 VectorGrowPart Support Sync

B007's accepted [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md) implementation promotes the constructor's part-vector full-capacity helper to file-local [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) marker support at `87/90`. Current MCP session `supervisor_resume_20260629` confirms raw `sub_4E5420`, size `0x1d6` / 470 bytes, exact body `0x004e5420-0x004e55f6`, padding at `0x004e55f6`, successor `sub_4E5600`, exactly two callers at this constructor's `0x004e0148` and UID00041B's `0x004e1356`, receiver setup as the part vector header at object offset `+4`, and 68-byte `NewHumanPartEntry` grow/insert behavior.

This update does not change this constructor's formal C++ block. The constructor continues to express the inline `Part.tbl` append as `m_partEntries.push_back(part)`; UID00041G records only the out-of-line allocation/memmove/free/update lowering with a formal source-disposition marker. The accepted disposition rejects making UID00041G a `NewHumanImageLib` class method, direct generic [UID:0000P3][VectorHelpers](by-file/VectorHelpers.md) owner, continued no-owner/non-emitting page, or standalone decompiler-shaped helper body.

## 2026-06-30 LoadPartSpriteDescriptors Support Sync

B008's accepted implementation promotes [UID:00041C][0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors](by-memory/0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors.md) to a class-owned/emitting helper at `88/90` with formal `void NewHumanImageLib::LoadPartSpriteDescriptors()` C++. Current MCP session `supervisor_resume_20260629` confirms raw `sub_4E13B0`, size `0x446`, constructor-only caller `0x004e017c`, exact body `0x004e13b0-0x004e17f6`, successor `sub_4E1800`, and `.DSC` suffix byte/xref proof at `0x0061c33c` / `0x004e1435`.

This update does not change this constructor's formal C++ block. The constructor still calls `LoadPartSpriteDescriptors()` out of line after populating `m_partEntries` and before `LoadAcc2DrwTable(L"acc2drw.tbl")`, while the exact child page owns the per-part `.DSC` open loop, 23-byte header/count read, descriptor-count allocation, packed descriptor fields, raw-record count accumulation, and motion-indexed sequence-frame table construction. UID00041C is therefore excluded from the old B011 below-gate helper group.

## 2026-06-30 LoadAcc2DrwTable Support Sync

B009's accepted implementation promotes [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md) to a class-owned/emitting helper at `88/90` with formal `void NewHumanImageLib::LoadAcc2DrwTable(const wchar_t *tableName)` C++. Current MCP session `supervisor_resume_20260629` confirms raw `sub_4E1800`, size `0x1c5`, constructor-only caller `0x004e0188`, the constructor's `acc2drw.tbl` literal xref at `0x004e0181` / `0x0061c324`, the `m_partEntries[7].descriptorCount` row count source, and B010's [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md) marker support on the full-capacity append path.

This update does not change this constructor's formal C++ block. The constructor still calls `LoadAcc2DrwTable(L"acc2drw.tbl")` out of line, while the exact child page owns clearing `m_acc2drwEntries`, reading one text row per `m_partEntries[7].descriptorCount`, parsing decimal flags into `Acc2DrwEntry.enabled[0..2]`, appending via `m_acc2drwEntries.push_back(entry)`, and preserving the fatal short-read path. This keeps the constructor range scoped to construction/orchestration rather than duplicating the helper body.

## Cross-References

- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md)
- [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md)
- [UID:00017R][0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster](by-memory/0x004dfd10-0x004e4d79.NewHumanImageLibLocalMethodCluster.md)
- [UID:00017S][0x004e0250-0x004e05c4.NewHumanImageLibDestructor](by-memory/0x004e0250-0x004e05c4.NewHumanImageLibDestructor.md)
- [UID:0001VF][NewHumanImageLibLayout](by-type/by-struct/NewHumanImageLibLayout.md)
- [UID:0001Y9][NewHumanImageLibVtable](by-type/by-vtable/NewHumanImageLibVtable.md)
- [UID:0000RT][g_pNewHumanImageLib](by-global/g_pNewHumanImageLib.md)
- [UID:0000VA][ResolveSpritePartPath_004E19D0](by-item/ResolveSpritePartPath_004E19D0.md)
- [UID:00041C][0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors](by-memory/0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors.md)
- [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md)
- [UID:00041E][0x004e4f10-0x004e50a0.VectorGrowAcc2Drw](by-memory/0x004e4f10-0x004e50a0.VectorGrowAcc2Drw.md)

## Changes

- 2026-06-30 B008 LoadPartSpriteDescriptors implementation sync:
  - Updated support text to mark [UID:00041C][0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors](by-memory/0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors.md) as a promoted class-owned/emitting helper with formal C++ while preserving this constructor's out-of-line `LoadPartSpriteDescriptors()` call and existing formal constructor body.
  - Evidence: current MCP session `supervisor_resume_20260629` confirms UID00041C constructor-only caller `0x004e017c`, exact size/range/successor, `.DSC` suffix byte/xref proof, descriptor count/allocation behavior, packed descriptor offsets, sequence table shape, and rejected alternatives still forbid folding the loader body into the constructor.
- 2026-06-30 B009 LoadAcc2DrwTable implementation sync:
  - Updated support text to mark [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md) as a promoted class-owned/emitting helper with formal C++ while preserving this constructor's out-of-line `LoadAcc2DrwTable(L"acc2drw.tbl")` call and existing formal constructor body.
  - Evidence: current MCP session `supervisor_resume_20260629` confirms UID00041D constructor-only caller `0x004e0188`, `acc2drw.tbl` literal xref, size/range/padding, `m_partEntries[7].descriptorCount` row count source, `Acc2DrwEntry.enabled[0..2]` digit behavior, clear/push_back source shape, fatal short-read path, and UID00041E marker-only full-capacity support; rejected alternatives still forbid folding the loader body into the constructor.
- 2026-06-30 B007 LoadPartTable implementation sync:
  - Updated support text to mark [UID:00041B][0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable](by-memory/0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable.md) as a promoted class-owned/emitting helper with formal C++ while preserving this constructor's inline `Part.tbl` loop and existing formal constructor body.
  - Evidence: current MCP session `supervisor_resume_20260629` confirms UID00041B zero direct callers, path-argument signature, exact body/padding, 23-byte header/count reads, 32-byte file rows into 68-byte part entries, `ResolveSpritePartPath` callsite `0x004e12fe`, and `VectorGrowPart` full-capacity path; rejected alternatives still forbid inventing constructor calls to `LoadPartTable`.
- 2026-06-30 B006 LoadLayerTable implementation sync:
  - Updated support text to mark [UID:00041A][0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable](by-memory/0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable.md) as a promoted class-owned/emitting helper with formal C++ while preserving this constructor's inline `Layer.tbl` loop and existing formal constructor body.
  - Evidence: current MCP session `supervisor_resume_20260629` confirms UID00041A zero direct callers, path-argument signature, exact body reads, `VectorGrowLayer` callsite `0x004e1171`, and constructor `VectorGrowLayer` callsite `0x004dffb2`; rejected alternatives still forbid inventing constructor calls to `LoadLayerTable`.
- 2026-06-25 B011 accepted implementation:
  - Before: metadata was `86/90`, the formal reconstruction C++ block was blank, the status used stale parent-routing wording, and historical notes still tied code entry to retired hard-threshold rules.
  - Changed to: `91/92`; preserved `CANONICAL_OWNER:000092`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000092`, and blank `EMITTER_POSITION_OPTIONAL`; inserted the accepted formal constructor C++; replaced stale gate wording with the current combined-score/emitter rule; added 2026-06-25 MCP provenance, helper caller graph, helper child-page UID links, raw string evidence, cleanup/unwind evidence, and rejected alternatives.
  - Evidence: B011 report `0002JR-NewHumanImageLibConstructor-source-quality.md`, accepted by supervisor, recorded active MCP session `80de0a67`, exact lookup/xref/decompile facts for the constructor and helper family, and validator-registered helper child pages [UID:000417][0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord](by-memory/0x004e0d70-0x004e0dcf.NewHumanImageLibLoadPartFrameDrawRecord.md) through [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md). Those pages were below-gate at B011 time; [UID:000419][0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable](by-memory/0x004e0e20-0x004e103f.NewHumanImageLibLoadMotionTable.md) is now superseded by B002's 2026-06-30 class-owned/emitting helper implementation, [UID:00041A][0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable](by-memory/0x004e1040-0x004e11af.NewHumanImageLibLoadLayerTable.md) is now superseded by B006's 2026-06-30 class-owned/emitting helper implementation, [UID:00041B][0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable](by-memory/0x004e11b0-0x004e13a7.NewHumanImageLibLoadPartTable.md) is now superseded by B007's 2026-06-30 class-owned/emitting helper implementation, [UID:00041C][0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors](by-memory/0x004e13b0-0x004e17f6.NewHumanImageLibLoadPartSpriteDescriptors.md) is now superseded by B008's 2026-06-30 class-owned/emitting helper implementation, [UID:00041D][0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable](by-memory/0x004e1800-0x004e19c5.NewHumanImageLibLoadAcc2DrwTable.md) is now superseded by B009's 2026-06-30 class-owned/emitting helper implementation, and [UID:00041G][0x004e5420-0x004e55f6.VectorGrowPart](by-memory/0x004e5420-0x004e55f6.VectorGrowPart.md) is now superseded by B007's 2026-07-01 file-local marker-support implementation.
- 2026-06-13 A003 Goal 2 repair:
  - Before: completion/confidence were `74/86`, the page summarized the constructor but lacked enough written evidence for the 85/85 assignment gate, and the `HAIRCOL.TBL` count source incorrectly described `0x188` as decimal `528`.
  - Changed to: `86/90`, corrected `0x188` to 392 bytes (Verified with `int_convert.py`), and added signature/frame evidence, table-row flow, byte-verified UTF-16 resource names, unwind cleanup evidence, fresh caller/callee evidence, singleton xref evidence, and explicit class-parent gate rationale.
  - Evidence: IDA MCP session `a002_batch_clear_timer` on 2026-06-13 rechecked `lookup_funcs`, `analyze_function`, `disasm`, `get_bytes`, and `trace_data_flow` for the constructor, table string bytes, post-function padding, helper boundaries, and singleton lifecycle. The old blank-C++ gate rationale from this pass was superseded by the 2026-06-25 B011 implementation above.
- 2026-06-21 supervisor Rule 26/B010 support sync: Added the `0x004e020d` hair-color append evidence for [UID:00030Q][0x00420de0-0x00420eea.PointerVectorGrowInsert](by-memory/0x00420de0-0x00420eea.PointerVectorGrowInsert.md). This constructor is a non-fitting-room integer-vector consumer of that shared helper, so the helper remains under VectorHelpers rather than this class or FittingRoom.
- Before: the page used legacy generated-parent wording even though the constructor is an exact `NewHumanImageLib` method with a confirmed class owner/emitter route.
- Changed to: current `CANONICAL_OWNER:000092` and `EMITTER_UIDS:000092` wording.
- Evidence: this page records the exact IDA boundary, constructor caller, callee/resource-load sequence, singleton/vtable stores, object offset initialization, and the accepted formal constructor C++; [UID:000092][NewHumanImageLib](by-class/NewHumanImageLib.md) is the confirmed class owner.
