*** UID:0003FC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5848304 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@00593CF0 | UID0003FC source-quality repair
bool TextEditPane::MeasureTextRunSegment(TextEditLineRecord *line,
                                         const TextRunFormatData *format,
                                         const wchar_t *text,
                                         int textOffset,
                                         unsigned short charCount,
                                         unsigned short segmentFlags,
                                         TextRunMeasureState *state)
{
    (void)textOffset;

    int visibleCount = charCount;
    if (segmentFlags <= kTextRunHasNextSegment) {
        state->remainingWidth = state->availableWidth;
        state->scratch = 0;
    }

    int runWidth = 0;
    if (format->object != NULL) {
        runWidth = format->object->m_payload1;
    } else {
        if ((segmentFlags & kTextRunHasNextSegment) == 0) {
            visibleCount = AdjustVisibleCountForTrailingWhitespace(
                text, static_cast<short>(visibleCount));
        }

        if (m_maskedText)
            runWidth = visibleCount * GetTextWidth(L"*", 1);
        else
            runWidth = GetTextWidth(text, visibleCount);
    }

    state->remainingWidth -= runWidth;
    if ((segmentFlags & kTextRunHasNextSegment) == 0) {
        if (state->remainingWidth < 0)
            state->remainingWidth = 0;
        line->remainingWidth = state->remainingWidth;
        line->wrapState = 0;
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact 189-byte compiler-retained private TextEditPane::MeasureTextRunSegment member at half-open [0x00593cf0,0x00593dad), with ECX plus seven stack arguments, retn 0x1c, zero start/immediate/pointer routes, exact CC fences, TextRunMeasureState reset/subtract/final clamp/writeback, embedded-object/plain/masked width paths, inherited final visible-count member call, unconditional false return, complete formal C++, and one TextEditPane class/file emitter route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00593cf0-0x00593dad TextRunMeasureCallback Thiscall Raw

## Status

- Entity kind: source-authored, compiler-retained private `TextEditPane::MeasureTextRunSegment` member. The stable page title retains the historical raw-callback search label, but this body is neither a cdecl callback thunk nor compiler forwarding glue.
- Direct owner: [UID:0000EO][TextEditPane](by-class/TextEditPane.md), because the body receives the editor object in `ecx` and reads TextEditPane fields such as the password-mask flag at `this+0x16a`.
- Source module: [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Related cdecl callback: [UID:0001JQ][0x00593c20-0x00593ce4.TextRunMeasureCallback](by-memory/0x00593c20-0x00593ce4.TextRunMeasureCallback.md).
- Source ABI: `bool __thiscall` with `TextEditPane` in `ECX`, seven stack arguments (`line`, `format`, `text`, unused `textOffset`, `charCount`, `segmentFlags`, and `state`), and callee cleanup `retn 0x1c`.
- C++ reconstruction: the formal CPP channel contains the complete private member, including the inherited final-segment visible-count call omitted by the prior UID0000OJ body. Formal H stays blank because UID0000EO owns the one canonical private declaration.

## Function

| Range | Role | Notes |
| --- | --- | --- |
| `[0x00593cf0,0x00593dad)` | `TextEditPane::MeasureTextRunSegment` | Exact `0xbd` / 189-byte private member body with 72 instruction heads and SHA256 `7D11CC7DD7E195A284EAFFDF7DEAACADF05A891E81844EAD0154A04A1D1E1009`. IDA currently has decoded code but no function object at the start. The filename endpoint is exclusive; the last body byte is `0x00593dac`. |

The exact target first 16 bytes are `55 8B EC 83 EC 08 66 83 7D 1C 01 53 0F B7 5D 1C`; the last 16 are `C9 66 89 48 0C 5F 5E 32 C0 5B 8B E5 5D C2 1C 00`.

## IDA Evidence

- The 2026-08-22 live read-only prestate at `0x00593cf0` is decoded code with no function, name, type, address comments, or function comment channels. `lookup_funcs` returns no function at the start, interior tail probes, or post-pad start.
- The target start has zero inbound code/data xrefs, zero immediate hits for `0x00593cf0`, and zero physical little-endian `F0 3C 59 00` pointer-pattern hits. Positive controls recover four address-taking routes for cdecl UID0001JQ and two for cdecl UID0001JR, so the negative result is route-specific rather than a failed search.
- Disassembly from `0x00593cf0` starts `push ebp; mov ebp, esp; sub esp, 8`, compares the word argument at `[ebp+0x1c]`, loads the width-state pointer from `[ebp+0x20]`, and uses `ecx` as the editor object.
- The body follows the same measurement choices as [UID:0001JQ][0x00593c20-0x00593ce4.TextRunMeasureCallback](by-memory/0x00593c20-0x00593ce4.TextRunMeasureCallback.md). Bit 0 is `kTextRunHasNextSegment`, bit 1 is `kTextRunHasPreviousSegment`, flags `<= 1` reset `remainingWidth` from `availableWidth` and clear `scratch`, and a clear bit 0 finalizes the segment.
- `TextRunMeasureState` is exactly three dwords: `availableWidth +0`, `remainingWidth +4`, and reset-only `scratch +8`. `TextRunFormatData::object` is at `+8`; a non-null object's protected `m_payload1 +0x10` supplies embedded advance width.
- Final non-object segments call inherited `GrafPort::AdjustVisibleCountForTrailingWhitespace(text, static_cast<short>(visibleCount))` at `0x00593d38` before either masked or plain width measurement. Masked measurement is `visibleCount * GetTextWidth(L"*", 1)` using `m_maskedText +0x16a`; plain measurement is `GetTextWidth(text, visibleCount)`.
- The body subtracts run width from `state->remainingWidth`; only final segments clamp a negative result to zero, store it at `TextEditLineRecord +8`, and clear the word at `+0x0c`. No instruction reads the `textOffset` slot, and the member always returns false.
- The final sequence at `0x00593da2-0x00593dad` pops `edi`, `esi`, and `ebx`, restores `esp`/`ebp`, clears `al`, and returns with `retn 1Ch`.
- Boundary bytes are exact: [UID:0000VN][-ignored](by-memory/-ignored.md) records `[0x00593ce5,0x00593cf0)` as eleven `0xcc` bytes with SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`, and `[0x00593dad,0x00593db0)` as three `0xcc` bytes with SHA256 `E668F0A1B72560CED14C54488A25335D11336448950BB6485267EBC5EB3FECEE`.

## Ownership Decision

Keep this with [UID:0000EO][TextEditPane](by-class/TextEditPane.md), not with the stale generated fitting-room owner markers. The raw body reads TextEditPane fields, uses the same GrafPort text-width helpers as the documented TextEditPane cdecl measurement callback, and sits between the documented cdecl measurement and draw callbacks in the TextEditPane text-run callback cluster.

UID0000EO remains the direct class owner/emitter and [UID:0000ON][TextEditPane](by-file/TextEditPane.md) remains the `NexusTK/ui/controls/TextEditPane.cpp` source root. Source order is the cdecl measurement callback, this private member, then the cdecl drawing callback. GrafPort owns only the inherited width/visible-count dependency; zero shipped routes do not change source ownership or reconstructability.

The requested stale slice `0x00593d00-0x00593e6b.FittingRoomPreviewOwnerTableAndTail` is not a valid exact child: it starts after this raw body's prologue and ends inside the following draw callback. This exact page preserves the real raw boundary instead.

## Reconstruction Notes

- Marked reconstructable because this is a complete NexusTK-owned private member algorithm, even though IDA does not model it as a function and no shipped route selects it.
- Emit the formal `TextEditPane::MeasureTextRunSegment` definition exactly once through UID0000EO and keep this page's H channel empty. UID0000EO already declares the exact seven-argument member once.
- Preserve the helper's shipped comparison exactly. It scans backward over space, tab, and carriage return, computes the last visible index, then returns that index only when `count <= lastVisibleIndex`; reconstruction must not silently strengthen this no-op-prone terminal comparison into a conventional trim.
- Create no split, merge, new UID, source file, alternate owner, duplicate helper, or raw-offset access route.

## Score Rationale

- Completion is `92` because the exact bytes/range/padding, 72-instruction body, member ABI, seven parameters, state/object/line layouts, flag flow, inherited helper call, masked/plain widths, update/return behavior, route-negative controls, owner/emitter, source order, and complete formal source are documented.
- Confidence is `93` because live IDA item/disassembly/xref/type evidence, physical PE hashes, four helper receiver call sites, sibling callback parity, and the existing class/file route agree. The cap is limited to exact original lexical spellings and compiler-option provenance, not behavior, range, ownership, placement, type, or source shape.

## Cross-References

- [UID:0000EO][TextEditPane](by-class/TextEditPane.md)
- [UID:0000ON][TextEditPane](by-file/TextEditPane.md)
- [UID:0001JQ][0x00593c20-0x00593ce4.TextRunMeasureCallback](by-memory/0x00593c20-0x00593ce4.TextRunMeasureCallback.md)
- [UID:0001JR][0x00593db0-0x00593ef5.DrawTextRunCallback](by-memory/0x00593db0-0x00593ef5.DrawTextRunCallback.md)
- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [UID:00005V][GrafPort](by-class/GrafPort.md)
- [UID:0000EN][TextEditObject](by-class/TextEditObject.md)

## Changes

- 2026-08-22 B003 UID0003FC accepted implementation callback: raised `85/88` to `92/93`, preserved exact range/owner/emitter/reconstructable routing, reclassified the source entity from callback variant to compiler-retained private member, installed the complete seven-argument formal CPP, restored the final non-object inherited visible-count call, recorded exact bytes/padding/route-negative/state/behavior evidence, and kept formal H blank because UID0000EO owns the declaration.
- 2026-06-12 A003 restart follow-up: Created as an exact TextEditPane-owned raw child after the requested stale `0x00593d00-0x00593e6b` fitting-room slice proved to cut through real TextEditPane callback bodies. Evidence: live IDA MCP `lookup_funcs`, `xrefs_to`, disassembly, and byte checks for the surrounding alignment.
- Historical 2026-06-20 B005 state: score was `85/88` and formal C++ remained blank. Its route-negative and non-FittingRoom evidence remains valid; its unresolved callback/adaptor source classification and blank-formal disposition are superseded by the 2026-08-22 member-ABI/source reconstruction.

## UID0000OJ Whole-File Incorporation - 2026-08-16

All accepted source, compiler/no-code, range, owner, and ordering dispositions for 0003FC are now current; the dated 2026-06-20 blank-formal assumption above is retained only as historical evidence.

The accepted claim/evidence/destination rows incorporated for this owner are preserved verbatim:

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination | Action | Gate-1 state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C0000OJ-238 | 0003FC | Incorporate raw source body `[0x593cf0,0x593dad)` as `bool TextEditPane::MeasureTextRunSegment(..., TextRunMeasureState *)`. | very-strong | initialize per-line width state, measure object/plain/masked content, clamp remaining width, write line wrap state, return false; start-xrefs 0; call instructions 3. | UID0003FC formal CPP/H and UID0000ON source order | incorporate | proposed |
