*** UID:0001IM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Compiler-retained NewMacroEditControlPane complete-object destructor artifact: exact 0x1f-byte vtable-restore tail jump to Pane::~Pane(), no inbound code/data xrefs to the helper itself, constructor/scalar-destructor vtable-store parity, boundary padding, sibling parity with UID0001IL, and no standalone source-body C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057fa80-0x0057fa9e - NewMacroEditControlPane Teardown Helper

## Status

- Confidence: strong for function boundary, behavior, class identity, and final source-quality disposition.
- Related class: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md)
- Source-family file: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md)
- Rebuild handling: class-specific compiler-retained complete-object destructor glue for `NewMacroEditControlPane`; document it as destructor-layout evidence, not as a standalone source method and not as row behavior.
- Autogen handling: non-reconstructable and non-emitting. The exact range should not attach to [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) as an emitting helper. The source-facing obligation belongs to the `NewMacroEditControlPane` class declaration/destructor semantics inside the MacroDialogs source family, and the compiler regenerates this vtable-restore/base-destructor artifact.

## Behavior

This compiler-retained artifact reinstalls the three `NewMacroEditControlPane` vtable pointers and tail-jumps to the shared `Pane::~Pane()` teardown at `0x00544580`.

It does not own macro slot loading, key/modifier accessors, setters, refresh, painting, input behavior, selectability, or delete behavior. Treat it as complete-object destructor layout evidence for the row control. The deleting behavior is in the separate scalar deleting destructor wrapper at `0x00580710`, and source row behavior remains in the constructor, accessors, setters, `Refresh`, `OnPaint`, `OnKeyEvent`, and `IsSelectable`.

## Evidence Notes

- 2026-06-01 IDA MCP `lookup_funcs` reports `0x0057fa80` as function `sub_57FA80`, size `0x1f`, with exclusive end `0x0057fa9f`.
- IDA MCP decompilation writes the primary vtable plus the secondary vtable views at `this + 0xa0` and `this + 0xa4`, then tail-jumps to `0x00544580`.
- IDA MCP `callers` and `xrefs_to` report no current direct references to the helper entry. The 2026-06-28 B002 pass resolves this as retained complete-object destructor glue rather than an unrecovered source helper because constructor/scalar-destructor vtable-store parity and sibling policy now explain the artifact.
- IDA MCP `callees` is empty because the final transfer is a direct jump rather than a normal call edge.
- The raw bytes are `c7 01 88 d2 62 00 c7 81 a0 00 00 00 f0 d2 62 00 c7 81 a4 00 00 00 20 d3 62 00 e9 e1 4a fc ff`.
- The three vtable constants are `0x0062d288`, `0x0062d2f0`, and `0x0062d320`. `xrefs_to` for those addresses ties the same table views to the constructor at `0x0057f9d0`, this teardown helper, and the scalar deleting destructor at `0x00580710`.
- The real scalar deleting destructor wrapper is separate at `0x00580710`; see [UID:0001IN][0x00580640-0x00580686.MacroEditControlPaneVtableThunks](by-memory/0x00580640-0x00580686.MacroEditControlPaneVtableThunks.md).
- 2026-06-15 live IDA MCP refresh reconfirmed `sub_57FA80` size `0x1f`/31 bytes, no direct callers, no normal callee edge, no strings, a two-block/complexity-1 body, and the exact decompile `*this`, `this[40]`, and `this[41]` vtable restores followed by `sub_544580(this)`.
- 2026-06-15 live IDA MCP `get_bytes` reconfirmed six `0xcc` bytes before the helper and a one-byte `0xcc` gap at `0x0057fa9f` after the helper. Decimal conversions for `0x1f`, `0xa0`, and `0xa4` verified with `tools/int_convert.py`.
- 2026-06-27 B004 support-only comparison for [UID:0001IL][0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper](by-memory/0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper.md): live IDA MCP session `80de0a67` found this sibling has the same `0x1f` vtable-restore/tail-jump shape, installs `0x0062d288`, `0x0062d2f0`, and `0x0062d320`, tail-jumps to `0x00544580`, and has no direct xrefs. This callback does not change UID0001IM metadata or source disposition; the comparison is recorded only to support UID0001IL's accepted no-code classification.
- 2026-06-28 B002 implementation callback applies the accepted current-MCP report from session `supervisor_20260628_resume`. `idb_list` request `3` reported active input IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `5124`, `is_active:true`, and `is_analyzing:false`; `server_health` request `2` reported `status:ok`, module `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, and strings cache ready.
- Current MCP `lookup_funcs` request `10` reported: target `0x0057fa80` size `0x1f`, constructor `0x0057f9d0` size `0x5b`, scalar deleting destructor `0x00580710` size `0x55`, `Pane::~Pane()` at `0x00544580` size `0x107`, `OnPaint` at `0x0057faa0` size `0x1d6`, `Refresh` at `0x0057fa70` size `0xa`, sibling `0x0057f7b0` size `0x1f`, and sibling scalar deleting destructor `0x00580770` size `0x55`.
- Current MCP `decompile` request `20` and `disasm` request `31` show the entire target body is four instructions: store `0x0062d288` at `[ecx]`, store `0x0062d2f0` at `[ecx+0xa0]`, store `0x0062d320` at `[ecx+0xa4]`, then `jmp sub_544580`. The refs are only the three `NewMacroEditControlPane` vtable views and `0x00544580`.
- Current MCP `analyze_function` request `42` reports prototype shape `void __thiscall(_DWORD *this)`, size `31`, no strings, no constants, no callers, no normal callees, basic-block count `2`, and cyclomatic complexity `1`; the extra block is the tail-jump successor marker.
- Current MCP `basic_blocks` request `32` reports one real helper block `0x57fa80-0x57fa9f`, size `31`, successor `0x544580`, no predecessors, plus a zero-sized tail-jump successor marker.
- Current MCP `xrefs_to` request `41` reports `xref_count=0` for the helper entry `0x0057fa80`. The same request reports exactly three data refs for each NewMacro vtable view: constructor stores at `0x0057fa03/0x0057fa09/0x0057fa13`, target stores at `0x0057fa80/0x0057fa86/0x0057fa90`, and scalar deleting destructor stores at `0x00580716/0x0058071c/0x00580726`. `xrefs_to 0x00580710` reports adjustor-thunk code refs from `0x0058065b` and `0x00580666` plus primary vtable data ref `0x0062d288`; `xrefs_to 0x0057f9d0` reports the constructor code ref at `0x0053ef2c`.
- Current MCP `decompile` request `21` of constructor `0x0057f9d0` confirms the row construction path calls the ControlPane setup path at `0x004949e0`, stores row state at `+0x108`, key binding at `+0x10c`, modifier/source byte at `+0x10e`, installs the same three NewMacro vtable views at `+0x00/+0xa0/+0xa4`, and sets `+0xff` to `1`.
- Current MCP `decompile` request `22` of scalar deleting destructor `0x00580710` confirms the deleting wrapper installs the same three NewMacro vtable views, calls `0x00544580`, checks scalar-delete flags, and calls delete helper `0x004f4ac0` only on the ordinary delete path. That wrapper, not this target, owns delete behavior.
- Current MCP `decompile` request `23` of `0x00544580` shows ordinary `Pane::~Pane()` behavior: it installs `Pane` primary/secondary/tertiary vtable views at `+0x00/+0xa0/+0xa4` and performs inherited focus/layer/dispatcher and Pane subobject cleanup. This proves the target tail target is inherited base destruction, not a `NewMacroEditControlPane` source helper.
- Current MCP `decompile` request `24` of [UID:0001IL][0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper](by-memory/0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper.md) shows the same instruction shape for the accepted Spell sibling, and `xrefs_to` request `41` reports zero inbound xrefs to that sibling entry. This sibling parity supports the same accepted no-code conclusion for [UID:0001IM].
- `tools/int_convert.py --pretty` verified `0x1f` as decimal `31`, `0xa0` as decimal `160`, `0xa4` as decimal `164`, `0x0057fa80` as decimal `5765760`, `0x0057fa9f` as decimal `5765791`, `0x00580710` as decimal `5768976`, and `0x00544580` as decimal `5522816` (Verified with int_convert.py).

## Boundary Evidence

- `0x0057fa70-0x0057fa7a` is `NewMacroEditControlPane::Refresh`; `0x0057fa7a-0x0057fa80` is six `0xcc` alignment bytes before this helper.
- IDA models this helper as `0x0057fa80-0x0057fa9f`; the historical filename uses the last body byte in the title.
- `0x0057fa9f-0x0057faa0` is a one-byte `0xcc` gap before `NewMacroEditControlPane::OnPaint` at `0x0057faa0`.
- Current MCP `get_bytes` request `34` reconfirmed the exact 31-byte body. Current MCP `get_bytes` request `35`, over `0x0057fa7a` size `38`, reconfirmed six `0xcc` bytes before the helper, the 31-byte body at `0x0057fa80-0x0057fa9f`, and the one-byte `0xcc` at `0x0057fa9f` before `OnPaint`.

## Source Placement And Rejected Alternatives

The exact binary artifact belongs semantically to [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md), while the broader source-family context remains [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md). The artifact is NewMacro-specific because it writes the same three NewMacro vtable views as the constructor and scalar deleting destructor. It is not a `Pane`-owned item because `Pane::~Pane()` is only the inherited tail target after the derived vtable stores. It is not a standalone MacroDialogs-emitting helper because the entry has no inbound xrefs and the body has no source-owned statements beyond compiler vptr restoration and inherited base destruction.

Rejected alternatives:

- Formal helper C++: rejected because the only target-local operations are compiler vptr stores and an implicit base destructor transfer.
- `[[CHILDREN]]` or split/container repair: rejected because the range is one exact 31-byte helper block bounded by padding and has no mixed subranges.
- Covered-by marker: rejected because no source-emitting parent should claim this exact body; the correct representation is class declaration/destructor semantics that let the compiler regenerate the artifact.
- `Pane` ownership: rejected because the derived NewMacro vtable stores determine class identity and the base destructor is only the tail callee.
- Callable `Cleanup`, `Teardown`, or `Destroy` method: rejected because current MCP reports zero inbound xrefs to the entry, no strings/constants, no row-state side effects, and no owned-member cleanup before `Pane::~Pane()`.
- Empty handwritten destructor body on this exact page: rejected because it would make the artifact look source-authored even though the source obligation belongs at class declaration/destructor-semantics level, not as this by-memory target's standalone body.

## No-Code Proof

Formal `RECONSTRUCTION_CPP CODE` for [UID:0001IM] stays blank.

1. The exact body consists only of derived vtable stores at `+0x00`, `+0xa0`, and `+0xa4`, followed by inherited `Pane::~Pane()`.
2. The helper entry has no direct inbound code or data route.
3. The body has no feature behavior: no macro slot/key/modifier access, no painting, no input handling, no selectability, no global writes, no resources, no strings, and no owned-member cleanup.
4. The deleting behavior lives in the separate scalar deleting destructor wrapper at `0x00580710`, reached through the vtable and adjustor thunks.
5. The accepted [UID:0001IL][0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper](by-memory/0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper.md) sibling has the same no-route vtable-restore-to-`Pane::~Pane()` shape and was resolved as non-emitting compiler-retained complete-object destructor glue.
6. Handwriting this target as source would encode compiler vptr mechanics and implicit base destruction as source statements, which is not source-quality C++.

## Migration Note

For source reconstruction, keep this exact range as non-reconstructable compiler-retained destructor glue. Reconstruct the `NewMacroEditControlPane` class declaration, virtual methods, and source-level destructor semantics in the MacroDialogs source family; the compiler will regenerate this complete-object destructor artifact from the class layout. Do not rename it as macro row state, paint logic, input logic, or a callable cleanup helper.

## Score Rationale

Raised to `88/92` on 2026-06-28 because the source-quality blocker is resolved with current MCP evidence rather than left open. Completion rises from `85` to `88` because the page now records exact current function size, bytes, padding, vtable-store triads, zero inbound xrefs, scalar destructor relationship, sibling parity, base destructor identity, direct owner, non-emitting metadata, and no-code proof. Confidence rises from `89` to `92` because current MCP, support docs, and accepted [UID:0001IL][0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper](by-memory/0x0057f7b0-0x0057f7ce.SpellMacroEditControlPaneTeardownHelper.md) policy agree on the exact behavior and the negative evidence is strong. The score remains below final-audit range because the wider `NewMacroEditControlPane` final class declaration and all source method splits are broader source-quality work.

## Cross-References

- [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md)
- [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md)
- [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md)
- [UID:0001IN][0x00580640-0x00580686.MacroEditControlPaneVtableThunks](by-memory/0x00580640-0x00580686.MacroEditControlPaneVtableThunks.md)
- [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md)
- [Wave3 data issue](../wave3_data_issues.md)

## Changes

- 2026-06-28 B002 implementation callback: reclassified [UID:0001IM] from reconstructable [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md)-emitting helper to non-reconstructable [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) complete-object destructor artifact. Metadata changed from `85/89`, owner/emitter `0000KY`, `RECONSTRUCTABLE:TRUE` to `88/92`, owner `000095`, `RECONSTRUCTABLE:FALSE`, blank emitter, and blank formal C++. Added current MCP session `supervisor_20260628_resume` evidence, constructor/scalar-destructor parity, `Pane::~Pane()` tail target identity, boundary proof, source-placement reasoning, rejected alternatives, and exact no-code proof.
- 2026-06-27 B004 implementation callback: added support-only sibling comparison for UID0001IL. No UID0001IM metadata, owner, reconstructability, emitter, or C++ fields were changed.
- 2026-06-01: Raised completion/confidence from `50/65` to `78/88`, marked reconstructable, and attached to [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md).
  - Before: the page identified the teardown behavior but did not record raw bytes, exact exclusive end, vtable constants, vtable xref map, autogen handling, or boundary padding.
  - After: the page records IDA lookup/decompile/caller/callee/xref evidence, exact vtable stores, neighboring functions, and the reason C++ remains blank.
- 2026-06-15: Raised from `78/88` to `85/89` after live IDA MCP reconfirmed the helper body, no direct caller edge, vtable xrefs, padding, and active source-quality gate rationale.
