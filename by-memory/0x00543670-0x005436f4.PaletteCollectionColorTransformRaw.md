*** UID:0002R4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:85 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000MA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000MA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B001-046 assigns this raw non-IDA-modeled palette collection transform to [UID:0000MA][Palette](by-file/Palette.md) as source-family ownership after negative caller/pointer-route scans, sibling raw transform evidence, live UID0001E9 transform-loop evidence, and no competing owner path; live reachability remains unproven, no direct xrefs/function object exist, and C++ stays blank pending final names/wrappers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00543670-0x005436f4 PaletteCollectionColorTransformRaw

## Status

- Confidence: strong that this is real palette-family code; medium for liveness and final helper name.
- Likely source file: [UID:0000MA][Palette](by-file/Palette.md).
- IDA MCP recheck: 2026-06-11.
- Rebuild handling: `source-authored` helper candidate, currently not modeled as an IDA function.
- Autogen status: reconstructable and assigned to [UID:0000MA][Palette](by-file/Palette.md) as source-family ownership. This does not prove live reachability: the raw entrypoint still has no IDA function object, no direct xrefs, no direct relative-call hits, and no immediate-address hits in the current IDA database. B001-046 accepted the assignment because exhaustive negative reference scans found no competing owner path, while raw sibling and live PaletteLib transform evidence tie the helper to the Palette source family strongly enough for the 85/85 gate.

## Function Boundary

IDA does not currently define a function object at `0x00543670`, but raw disassembly shows a complete function-shaped body:

| Range | Function | Notes |
| --- | --- | --- |
| `0x00543670-0x005436f4` | `PaletteCollectionColorTransformRaw` provisional | Walks two list-like palette collections and transforms `0xff` (`255`, verified with `int_convert.py`) native 16-bit entries per paired item using `dword_69B408` and a float-derived strength byte. |

Boundary evidence:

- `0x00543665-0x00543670` is `0xcc` alignment after [UID:0001E7][0x00543450-0x00543665.PaletteListAndEntryMoveHelpers](by-memory/0x00543450-0x00543665.PaletteListAndEntryMoveHelpers.md).
- `0x00543670` begins with `push ebp; mov ebp, esp; push ecx`.
- The body ends with `retn 0x0c` at `0x005436f1-0x005436f3`, so the exclusive end is `0x005436f4`.
- `0x005436f4-0x00543700` is `0xcc` alignment before `PaletteLib::PaletteLib` at `0x00543700`.

## Behavior

The helper takes two collection-like objects and a float weight:

1. If the first collection count at `+0x0c` (`12`, verified with `int_convert.py`) is not positive, it returns immediately.
2. It computes an integer strength byte from `(weight * 32.0f) + 0.5f`; the multiplier corresponds to `0x20` (`32`, verified with `int_convert.py`).
3. For each collection index, it calls the virtual item accessor at vtable slot `+0x10` on both collections.
4. It treats each returned item as a pointer to a palette object, walks `0xff` (`255`, verified with `int_convert.py`) entries beginning at native-color table offset `+0x2e` (`46`, verified with `int_convert.py`), and calls `dword_69B408(baseColor, strength)` for each source color.
5. It writes transformed 16-bit colors into the paired destination palette table.

This is similar in shape to the runtime palette-filter table rebuild work in [UID:0001E9][0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers](by-memory/0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers.md), but no direct code xrefs to `0x00543670` were observed in the current IDA database.

## Touched State And Data Flow

| State / input | Use |
| --- | --- |
| First collection `+0x0c` | Count/iteration gate; non-positive count returns immediately. |
| First and second collection vtable slot `+0x10` | Virtual item accessor used to fetch paired source/destination palette objects by index. |
| Float weight argument | Converted to an integer strength byte with `(weight * 32.0f) + 0.5f`. |
| Palette native-color table offset `+0x2e` | Starting point for the 16-bit color entries transformed by the loop. |
| `dword_69B408` | Global color-transform callback used to derive each destination 16-bit palette color. |

## Relationship To Neighboring Palette Helpers

| Neighbor | Relationship |
| --- | --- |
| [UID:0001E7][0x00543450-0x00543665.PaletteListAndEntryMoveHelpers](by-memory/0x00543450-0x00543665.PaletteListAndEntryMoveHelpers.md) | Immediately precedes this raw helper; the `0x00543665-0x00543670` gap is confirmed `0xcc` alignment. |
| [UID:0001E6][0x005431c0-0x005443ab.PaletteLibMethodCluster](by-memory/0x005431c0-0x005443ab.PaletteLibMethodCluster.md) | Broader palette-manager aggregate containing the nearby constructor and filter helpers. |
| [UID:0001E9][0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers](by-memory/0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers.md) | Later palette-filter helper family that also calls `dword_69B408`; useful semantic context but not direct caller evidence. |
| [UID:0004HZ][0x00542d90-0x00542de3.DLPaletteColorTransformRaw](by-memory/0x00542d90-0x00542de3.DLPaletteColorTransformRaw.md) | B002 current evidence records the split raw sibling in the DLPalette gap. It uses the same `32.0f`/`0.5f` strength formula, `dword_69B408` callback, `+0x2e` native-color table, and `0xff` entry loop for one palette object; it still has no direct live route, and its formal C++ remains blank under a target-specific no-code proof. |
| `0x00543700` `PaletteLib::PaletteLib` | Follows this helper after `0x005436f4-0x00543700` `0xcc` alignment. |

## Evidence Notes

- 2026-06-03 IDA MCP `lookup_funcs` reports `0x00543670`, `0x00543680`, `0x005436f1`, and `0x005436f4` as `Not a function`; this page is therefore a raw helper split, not an IDA-modeled method.
- 2026-06-03 IDA MCP `lookup_funcs` reports the preceding modeled helper as `sub_5435B0` ending at `0x00543665` and the next modeled function as `sub_543700` starting at `0x00543700`.
- 2026-06-03 IDA MCP `xrefs_to` finds no direct xrefs to `0x00543670`; the only local code xref reported in the raw body is the internal branch from `0x0054367d` to `0x00543680`.
- 2026-06-07 IDA MCP still reports no function object at `0x00543670` or `0x005436f4`, no code refs to the entrypoint, and a byte-level scan for direct `E8 rel32` calls to `0x00543670` found no hits in `.text`.
- 2026-06-03 raw disassembly shows `0x00543670` begins with `push ebp; mov ebp, esp; push ecx`, executes a counted loop over paired collection entries, and ends with `retn 0x0c` at `0x005436f1`.
- 2026-06-03 constant reads confirm `dword_6104CC` is `32.0f` and `dword_6104B0` is `0.5f`, matching the strength-byte formula.
- 2026-06-07 `DataRefsTo(0x0069b408)` reports this raw helper's callback call at `0x005436c8`, plus palette/DLPalette/filter sites at `0x00542dc8`, `0x00543fdd`, `0x00544020`, and `0x005440a8`, and rendering sites at `0x00558780`, `0x00558bd0`, and `0x00558eec`. This supports palette-family ownership but still does not prove a direct caller for `0x00543670`.
- 2026-06-11 B001-046 searched loaded segments for absolute VA bytes for `0x00543670`, RVA bytes for `0x00143670`, `E8/E9` rel32 calls/jumps to the target, and decoded operands equal to either target value; all searches were negative. This preserves the liveness caveat while making the negative evidence explicit.
- 2026-06-11 B001-046 confirmed `dword_69B408` has Palette-family transform references at `0x00542dc8`, `0x005436c8`, `0x00543fdd`, `0x00544020`, and `0x005440a8`, plus Surface render callback setup references at `0x00558780`, `0x00558bd0`, and `0x00558eec`. Surface owns the callback provider, but the palette-list/table semantics belong to the Palette source family.
- 2026-06-11 B001-046 checked for source metadata breadcrumbs (`RSDS`, `NB10`, `.pdb`, `.cpp`, `.cxx`) and did not find source-path evidence. The parent assignment is therefore an IDA-evidence inference, not recovered debug metadata.

## 2026-06-11 A004 Live IDA Recheck

Batch 172 rechecked both the helper and its neighboring palette functions:

| Check | Result |
| --- | --- |
| `lookup_funcs` | `0x00543670` and `0x005436f4` remain `Not a function`; `PaletteLib::PaletteLib` still starts at `0x00543700`. |
| `xrefs_to` / `callers` | No xrefs or callers to `0x00543670`; `0x005435b0` still has callers from `0x00543ee0`, and `0x00543f60`/`0x00543f80` still have live render/map callers, so the no-caller result is specific to this raw helper rather than a broken local xref query. |
| direct-use scans | `py_eval` found no direct `E8 rel32` calls to `0x00543670` and no little-endian immediate occurrences of the entry address in `.text`/data scan scope. |
| boundary bytes | `0x00543665-0x00543670` and `0x005436f4-0x00543700` are still all `0xcc` bytes. |
| raw body | Tight disassembly from `0x00543670-0x005436f4` confirms the full helper: count gate at collection `+0x0c`, strength calculation from `dword_6104CC == 32.0f` and `dword_6104B0 == 0.5f`, paired virtual accessor calls through slot `+0x10`, `0xff` palette-entry loop, `dword_69B408` call at `0x005436c8`, destination write, and `retn 0x0c`. |

The recheck improves documentation completeness because the page now records both positive raw-body evidence and the negative liveness scans. It does not make the helper assignable: by-structure parent attachment would claim a live/source-owned child under [UID:0000MA][Palette](by-file/Palette.md), and that direct ownership still lacks caller evidence.

## 2026-06-11 B001 Ownership Review

B001-046 concluded this page should attach to [UID:0000MA][Palette](by-file/Palette.md) as a retained or dormant source-authored Palette helper. The key distinction is that source ownership and live reachability are separate facts. Live reachability remains unresolved, but source-family ownership is now strong enough to apply because no alternate dispatch, pointer-table, vtable, EH, ScreenPane, Surface, or callback-table owner path was found.

The strongest positive evidence is the transform-family match:

| Evidence | Ownership value |
| --- | --- |
| Raw sibling `0x00542d90-0x00542de3` | Single-palette transform uses the same `dword_69B408`, `32.0f`, `0.5f`, `+0x2e`, and `0xff` loop shape. It appears in the DLPalette/Palette code island and has the same dormant/no-xref profile. |
| This page `0x00543670-0x005436f4` | Collection-pair form of the same transform kernel. It pulls paired palette objects through collection vtable slot `+0x10`, then transforms 255 native entries from offset `+0x2e`. |
| Live `0x00543f80` PaletteLib filter rebuild | Actively called through `g_pPaletteLib`, caches the filter weight at `+0x75c`, walks runtime palette slot lists, and contains the same pairwise `dword_69B408` transform loop. |

Rejected parents:

- [UID:0000MB][PaletteLib](by-file/PaletteLib.md) is a plausible narrower scope, but no caller proves this raw helper was a `PaletteLib` method or file-static helper rather than a broader Palette helper. The existing [UID:0000MA][Palette](by-file/Palette.md) umbrella is the safer direct parent.
- [UID:00003Z][DLPalette](by-class/DLPalette.md) provides sibling evidence through the raw single-palette helper, but this page operates on collection-like objects rather than one `DLPalette` instance.
- `ScreenPane` / `ScreenPanePaletteState` triggers adjacent palette reset work but does not call or reference this helper.
- [UID:0000OC][Surface](by-file/Surface.md) and [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) own the `dword_69B408` callback provider, not every palette helper that consumes the callback.
- A standalone `PaletteTransformHelpers.cpp` / `RenderPaletteTransform.cpp` source file is rejected as a source-placement claim. The raw `0x00542d90`, raw `0x00543670`, and live `0x00543f80` transform forms could justify a future Palette-owned documentation subgroup, but no source metadata, object-file boundary, caller cluster, string, or table proves a separate compilation unit.
- Leaving the parent blank is now rejected as overly conservative. The entrypoint still has no live route, but the source family is strongly inferred from neighboring raw/live Palette transform code and the lack of any competing owner path.

Surface remains a dependency rather than the owner: render setup writes `dword_69B408` at `0x00558780`, `0x00558bd0`, and `0x00558eec`, while this helper only consumes that callback from Palette-list semantics. That split preserves callback-table ownership under Surface while keeping this retained transform helper under Palette.

Follow-up: B001-046 found probable raw helper bodies in the current `0x00542cf3-0x00542df0` DLPalette gap. This is outside [UID:0002R4]'s exact range, so it does not block this assignment, but it should be split later instead of treated as one padding span:

| Range | Provisional role | Evidence / disposition |
| --- | --- | --- |
| `0x00542cf3-0x00542d00` | padding | `0xcc` alignment from `DLPalette::FreePaletteData` to the raw copy helper. |
| `0x00542d00-0x00542d81` | `DLPaletteRawCopyHelper` candidate | Copies a `DLPalette` header, optional remap/allocation state, and native table from source object to destination object, including optional remap allocation and `_memmove`; B001-046 found no xrefs or pointer hits. |
| `0x00542d81-0x00542d90` | padding | `0xcc` alignment from the raw copy helper to the single-palette transform helper. |
| `0x00542d90-0x00542de3` | `DLPaletteColorTransformRaw` candidate | Single-`DLPalette` form of this page's collection transform: destination object in `ecx`, source/base pointer at `[ebp+8]`, float at `[ebp+0x0c]`, strength `(weight * 32.0f) + 0.5f`, `+0x2e` native-entry table, `0xff` entries, and `dword_69B408`; B001-046 found no xrefs or pointer hits. |
| `0x00542de3-0x00542df0` | padding | `0xcc` alignment from the raw single-palette transform helper to `DLPalette::LoadFromFile`. |

## Corrected Assignment Gate

This helper attaches to [UID:0000MA][Palette](by-file/Palette.md). The child is `85/88` and the direct parent is `90/86`, so both sides clear the corrected `85/85` gate. Live IDA still finds no function object, no direct caller, no absolute/RVA pointer, and no relative call/jump to `0x00543670`; therefore this assignment does not claim live reachability. The source-owner inference rests on Palette source-island evidence: the raw body is bounded by Palette-family padding, uses the same `dword_69B408` / `32.0f` / `0.5f` / `+0x2e` / 255-entry transform kernel as raw `0x00542d90` and live `PaletteLib::UpdatePaletteFilterTables` at `0x00543f80`, and no ScreenPane, Surface, callback-table, vtable, EH, pointer-table, or alternate source owner path was found. Treat it as a retained/dormant file-static Palette helper until a caller is recovered.

## Reconstruction Notes

Reconstructable is `TRUE` because the body is NexusTK-owned palette transformation logic if it is live or intentionally retained source. The parent UID is [UID:0000MA][Palette](by-file/Palette.md) as a source-family assignment, with the liveness caveat preserved. C++ remains blank because the helper lacks final names, caller context, and typed collection/palette wrappers.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `85` | The page documents the exact raw body bounds, surrounding alignment, argument/state roles, transform callback, destination write behavior, constants, callback xrefs, no-entry-xref status, direct-call and immediate-address scan results, neighboring live palette helper checks, and relationship to neighboring palette helpers. Completion clears the low gate because both positive body semantics and negative liveness evidence are now written down, but remains capped because the helper has no direct caller xrefs, no IDA function object, and no final source-facing collection/palette wrapper names. |
| Confidence `88` | Confidence is strong that the bytes are real NexusTK palette-family code because the body is function-shaped, bounded by confirmed padding, uses palette-table semantics, and calls the same color-transform callback as nearby palette/filter code. Confidence is not higher because liveness and final placement remain unresolved. |

## Cross-References

- [UID:0000MA][Palette](by-file/Palette.md)
- [UID:0001E6][0x005431c0-0x005443ab.PaletteLibMethodCluster](by-memory/0x005431c0-0x005443ab.PaletteLibMethodCluster.md)
- [UID:0001E7][0x00543450-0x00543665.PaletteListAndEntryMoveHelpers](by-memory/0x00543450-0x00543665.PaletteListAndEntryMoveHelpers.md)
- [UID:0001E9][0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers](by-memory/0x00543f60-0x005440f0.PaletteLibPaletteFilterHelpers.md)

## Changes

- 2026-06-02: Raised the page to `70/82`, added explicit autogen-parent gating, touched-state/data-flow notes, neighboring helper relationships, and score rationale. Kept `AUTOGEN_PARENT_UID` blank because direct caller/liveness evidence remains unresolved, and kept final C++ blank because names and wrapper types are not final-source quality.
- 2026-06-03: Re-reviewed as a low-completion row and kept the score and parent assignment unchanged.
  - What stayed unchanged: completion `70`, confidence `82`, blank autogen parent, and blank C++ reconstruction.
  - Summary/evidence: [UID:0000MA][Palette](by-file/Palette.md) and neighboring helper pages still support palette-family ownership, but the raw helper still lacks an IDA function object or direct caller xrefs. The manual coverage row was synced to the page metadata rather than treating the review as new score evidence.
- 2026-06-03 live IDA evidence update:
  - What existed before: score was `70/82`, the page had stale session-status wording, and the callback/constant evidence was not current.
  - Changed to: score is `76/84`; parent and C++ remain blank.
  - Summary/evidence: live IDA MCP confirms the helper is still not an IDA function and still has no direct entry xrefs, but raw disassembly verifies the complete body, `0xcc` boundaries, `255`-entry loop, `32.0f`/`0.5f` strength constants, and the `dword_69B408` callback call at `0x005436c8`. This supports a modest documentation increase without crossing the liveness bar for parent attachment.
- 2026-06-07 A006 Batch007 gate audit:
  - Changed completion/confidence from `76/84` to `80/86`; parent and C++ remain blank.
  - Evidence at that time: live IDA MCP reconfirmed no IDA function object at the raw helper start/end, no entrypoint xrefs, no direct `E8 rel32` call hits to `0x00543670`, exact padding around the helper, and the expanded `dword_69B408` callback ref set. The likely parent [UID:0000MA][Palette](by-file/Palette.md) was `90/86`, but the child still remained below 85 completion and lacked liveness evidence, so assignment was deliberately not made.
- 2026-06-11 A004 Batch 172 raw-helper recheck:
  - Changed completion/confidence from `80/86` to `85/88`; parent and C++ remain blank.
  - Evidence: live IDA MCP reconfirmed no IDA function object or xrefs for `0x00543670`, no direct `E8 rel32` calls, no immediate entry-address uses, exact `0xcc` padding on both sides, and a complete tight disassembly proving the count gate, `32.0f`/`0.5f` strength calculation, `0xff` (`255`, verified with `int_convert.py`) entry loop, `+0x2e` (`46`, verified with `int_convert.py`) table offset, `dword_69B408` call, destination writes, and `retn 0x0c`. The assignment blocker is now classified as missing direct liveness/ownership evidence rather than child score failure.
- 2026-06-11 B001-046 ownership execution:
  - Changed `AUTOGEN_PARENT_UID` from blank to `0000MA`; completion/confidence remain `85/88`; C++ remains blank.
  - Evidence: B001 found no live caller/pointer/rel32/operand route to `0x00543670`, but proved stronger Palette source-family ownership through the raw single-palette transform sibling at `0x00542d90`, the live PaletteLib filter-table transform loops at `0x00543f80`, the shared `dword_69B408`/`32.0f`/`0.5f`/`+0x2e`/255-entry transform kernel, and lack of any alternate ScreenPane/Surface/callback-table/vtable/EH source-owner path.
- 2026-07-06 B002 UID0004HZ support sync:
  - Updated the relationship row for the raw single-palette transform sibling to link the now-split [UID:0004HZ][0x00542d90-0x00542de3.DLPaletteColorTransformRaw](by-memory/0x00542d90-0x00542de3.DLPaletteColorTransformRaw.md), preserving the same transform-kernel evidence, no-direct-live-route caveat, and blank formal C++ disposition.
