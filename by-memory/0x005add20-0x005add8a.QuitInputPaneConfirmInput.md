*** UID:0003ZR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BH | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BH | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void QuitInputPane::OnConfirmInput()
{
    if (GetTextLength() != 1)
        return;

    wchar_t answer = 0;
    CopyText(&answer, 1);

    if (answer != L'y' && answer != L'Y')
    {
        MarkForDeletion();
        return;
    }

    TimerHandler *timerOwner = NULL;
    if (g_pCollectionData != NULL)
        timerOwner = &g_pCollectionData->m_timerHandler;

    g_pTimerMgr->RemovePendingTimersForOwner(timerOwner);
    g_activeMapPane->ExitToMenu(true);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Primary-vtable quit confirmation handler at slot `0x0062f09c`; requires one-character input, copies the UTF-16 answer, accepts `Y/y`, marks the pane for deletion on one-character non-confirmation, removes pending timers for the local-player/UserPane timer owner at `g_pCollectionData + 0xa4`, and calls `MapPane::ExitToMenu(true)`; first-draft handler C++ is ready. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005add20-0x005add8a QuitInputPane Confirm Input

## Status

- Entity kind: virtual `QuitInputPane` submit/confirmation handler.
- Source owner: [UID:0000BH][QuitInputPane](by-class/QuitInputPane.md) `QuitInputPane`, routed through [UID:0000MX][QuitDialogs](by-file/QuitDialogs.md) `QuitDialogs.cpp`.
- Exact parent/split index: [UID:0001KS][0x005adcc0-0x005add8a.QuitInputPaneCore](by-memory/0x005adcc0-0x005add8a.QuitInputPaneCore.md) `0x005adcc0-0x005add8a.QuitInputPaneCore`.
- Disposition: reconstructable exact source member function.
- Confidence: very strong for vtable reachability, handler boundary, one-character `Y/y` behavior, timer cleanup, and `MapPane::ExitToMenu(true)` call; capped only by final source type naming for the broad object behind `g_pCollectionData`.

## Exact Range And Route

| Range | Meaning |
| --- | --- |
| `0x005add20-0x005add8a` | `QuitInputPane::OnConfirmInput()` virtual handler. |
| `0x005add18-0x005add20` | Preceding eight-byte `0xcc` alignment gap owned by the parent split index/coverage accounting. |
| `0x005add8a-0x005add90` | External post-function `0xcc` padding outside the parent aggregate. |

The handler is live through the primary `QuitInputPane` vtable slot at `0x0062f09c -> 0x005add20`. It has no direct rel32 caller, which is expected for this virtual submit callback.

## Disassembly Facts

The accepted source-quality pass disassembled this exact range as:

```asm
005add20 push ebp
005add21 mov ebp, esp
005add23 push ecx
005add24 push esi
005add25 mov esi, ecx
005add27 call 0x004f2310
005add2c cmp ax, 1
005add30 jne 0x005add85
005add32 push 1
005add34 lea eax, [ebp-4]
005add37 mov ecx, esi
005add39 push eax
005add3a call 0x004f2300
005add3f mov eax, dword ptr [ebp-4]
005add42 cmp ax, 0x79
005add46 je 0x005add5a
005add48 cmp ax, 0x59
005add4c je 0x005add5a
005add4e mov ecx, esi
005add50 call 0x00544690
005add55 pop esi
005add56 mov esp, ebp
005add58 pop ebp
005add59 ret
005add5a mov ecx, dword ptr [0x0067a748]
005add60 lea eax, [ecx+0xa4]
005add66 neg ecx
005add68 sbb ecx, ecx
005add6a and ecx, eax
005add6c push ecx
005add6d mov ecx, dword ptr [0x0067ab80]
005add73 call 0x00597a10
005add78 mov ecx, dword ptr [0x0067a764]
005add7e push 1
005add80 call 0x005047f0
005add85 pop esi
005add86 mov esp, ebp
005add88 pop ebp
005add89 ret
```

Resolved roles:

- `0x004f2310`: `LineInputPane::GetTextLength()`, returning a 16-bit length observed through `cmp ax, 1`.
- `0x004f2300`: `LineInputPane::CopyText(wchar_t *out, int count)`.
- `[ebp-4]`: one UTF-16 character buffer for the answer.
- `0x00544690`: `Pane::MarkForDeletion()`.
- `0x0067a748`: canonical current doc name `g_pCollectionData`; behavior here is local-player/UserPane state.
- `g_pCollectionData + 0xa4`: timer-handler owner subobject passed to TimerMgr removal.
- `0x0067ab80`: `g_pTimerMgr`.
- `0x00597a10`: `TimerMgr::RemovePendingTimersForOwner(TimerHandler *owner)` or equivalent direct core helper.
- `0x0067a764`: `g_activeMapPane`.
- `0x005047f0`: `MapPane::ExitToMenu(bool notifyServer)`.

## Behavior

The method returns immediately unless the inherited line-input text length is exactly `1`.

For a one-character answer, it copies one UTF-16 character from the line input. A lowercase `y` (`0x79`) or uppercase `Y` (`0x59`) is an affirmative quit confirmation. Any other single character calls `Pane::MarkForDeletion()` on the prompt and returns without exiting the map session.

On `Y/y`, it computes a timer owner pointer from `g_pCollectionData` only when that global is non-null:

```cpp
TimerHandler *timerOwner = g_pCollectionData != NULL ? &g_pCollectionData->m_timerHandler : NULL;
```

The `neg` / `sbb` / `and` instruction sequence implements the null-safe select. The final source-facing type name of the object behind `g_pCollectionData` is broader than collection-only state, so `m_timerHandler` is a behavior-backed field name for the `+0xa4` owner subobject, not a final global rename.

After computing the owner, the handler calls the TimerMgr removal helper directly through `g_pTimerMgr` and then calls `g_activeMapPane->ExitToMenu(true)`. The `true` argument requests the exit/menu path that notifies the server.

## Heuristic / Inference Validation

Best-supported source-facing name: `QuitInputPane::OnConfirmInput()`.

Acceptable project-convention alias: `QuitInputPane::OnEnterKey()` if the final class hierarchy names this inherited submit slot after the Enter key. This page uses `OnConfirmInput` because the method takes no key-code argument and performs submit/confirm behavior.

Rejected alternatives:

- `OnKeyEvent`: rejected because this function takes only `this`; no key code or raw keyboard event argument is present.
- `Quit`: rejected because the method also handles non-confirming one-character input by deleting only the prompt.
- `OnCancel`: rejected because `Y/y` performs affirmative quit cleanup and map exit.
- Timer helper ownership: rejected because the method only consumes TimerMgr to remove local-player/UserPane pending timers before exit.
- MapPane ownership: rejected because `MapPane::ExitToMenu(true)` is a callee reached after confirmation, not this handler's class owner.

## First-Draft C++ Rationale

The emitted draft uses source-style inherited helper calls and keeps the timer-owner field as a documented local-player/UserPane state subobject:

```cpp
void QuitInputPane::OnConfirmInput()
{
    if (GetTextLength() != 1)
        return;

    wchar_t answer = 0;
    CopyText(&answer, 1);

    if (answer != L'y' && answer != L'Y')
    {
        MarkForDeletion();
        return;
    }

    TimerHandler *timerOwner = NULL;
    if (g_pCollectionData != NULL)
        timerOwner = &g_pCollectionData->m_timerHandler;

    g_pTimerMgr->RemovePendingTimersForOwner(timerOwner);
    g_activeMapPane->ExitToMenu(true);
}
```

If the concrete `g_pCollectionData` containing type is not declared when source is assembled, the implementation may temporarily express `+0xa4` with a byte-offset cast. The documentation should still preserve the stronger semantic role: local-player/UserPane `TimerHandler` owner subobject.

## Cross-References

- [UID:0001KS][0x005adcc0-0x005add8a.QuitInputPaneCore](by-memory/0x005adcc0-0x005add8a.QuitInputPaneCore.md) `0x005adcc0-0x005add8a.QuitInputPaneCore`
- [UID:0000BH][QuitInputPane](by-class/QuitInputPane.md) `QuitInputPane`
- [UID:0000MX][QuitDialogs](by-file/QuitDialogs.md) `QuitDialogs`
- [UID:00026W][0x0062ef0c-0x006307ec.UserPaneInputVtables](by-memory/0x0062ef0c-0x006307ec.UserPaneInputVtables.md) `0x0062ef0c-0x006307ec.UserPaneInputVtables`
- [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md) `0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers`
- [UID:0001K6][0x00597570-0x005986da.TimerMgrAndTimerQueue](by-memory/0x00597570-0x005986da.TimerMgrAndTimerQueue.md) `0x00597570-0x005986da.TimerMgrAndTimerQueue`
- [UID:0002QH][0x005047f0-0x00504b6b.MapPaneExitToMenuTeardown](by-memory/0x005047f0-0x00504b6b.MapPaneExitToMenuTeardown.md) `0x005047f0-0x00504b6b.MapPaneExitToMenuTeardown`
- [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md) `g_pCollectionData`
- [UID:0001OR][0x0067a748-0x0067a74c.g_pCollectionData](by-memory/0x0067a748-0x0067a74c.g_pCollectionData.md) `0x0067a748-0x0067a74c.g_pCollectionData`

## Score Rationale

Completion is `88` because the exact function range, vtable route, disassembly, inherited input helpers, `Y/y` behavior, deletion path, TimerMgr removal path, `g_pCollectionData + 0xa4` field role, `MapPane::ExitToMenu(true)` call, rejected alternatives, and first-draft C++ are documented.

Confidence is `91` because the primary vtable route at `0x0062f09c`, behavior, callees, and support docs agree. It stays below final-audit level only because the final source type/global name for the broad object currently called `g_pCollectionData` remains unresolved outside this target.
