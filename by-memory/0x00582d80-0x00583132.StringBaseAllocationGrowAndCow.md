*** UID:0002RO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0001WS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0001WS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace mystr {

template <>
char *
StringBase<char, mychar_traits<char> >::AllocateAnsiBuffer(unsigned int length)
{
    if (length == 0) {
        m_data = g_emptyAnsiString;
        return m_data;
    }

    RefCountedStringBufferHeader *header = 0;
    if (length <= 64) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_ansiPool64.Allocate());
        header->capacity = 64;
    } else if (length <= 128) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_ansiPool128.Allocate());
        header->capacity = 128;
    } else if (length <= 256) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_ansiPool256.Allocate());
        header->capacity = 256;
    } else if (length <= 512) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_ansiPool512.Allocate());
        header->capacity = 512;
    } else {
        header = static_cast<RefCountedStringBufferHeader *>(
            ::operator new(length +
                           sizeof(RefCountedStringBufferHeader) +
                           sizeof(char)));
        header->capacity = length;
    }

    header->refCount = 1;
    header->length = length;
    char *data = reinterpret_cast<char *>(header + 1);
    data[length] = '\0';
    m_data = data;
    return m_data;
}

template <>
wchar_t *
StringBase<wchar_t, mychar_traits<wchar_t> >::AllocateWideBuffer(
    unsigned int length)
{
    if (length == 0) {
        m_data = g_emptyWideString;
        return m_data;
    }

    RefCountedStringBufferHeader *header = 0;
    if (length <= 64) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_widePool64.Allocate());
        header->capacity = 64;
    } else if (length <= 128) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_widePool128.Allocate());
        header->capacity = 128;
    } else if (length <= 256) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_widePool256.Allocate());
        header->capacity = 256;
    } else if (length <= 512) {
        header = static_cast<RefCountedStringBufferHeader *>(
            g_widePool512.Allocate());
        header->capacity = 512;
    } else {
        header = static_cast<RefCountedStringBufferHeader *>(
            ::operator new(length * sizeof(wchar_t) +
                           sizeof(RefCountedStringBufferHeader) +
                           sizeof(wchar_t)));
        header->capacity = length;
    }

    header->refCount = 1;
    header->length = length;
    wchar_t *data = reinterpret_cast<wchar_t *>(header + 1);
    data[length] = L'\0';
    m_data = data;
    return m_data;
}

template <>
void
StringBase<char, mychar_traits<char> >::GrowAnsiBuffer(
    unsigned int additionalLength)
{
    char *oldData = m_data;
    if (oldData == g_emptyAnsiString) {
        AllocateAnsiBuffer(additionalLength);
        return;
    }

    const RefCountedStringBufferHeader *oldHeader =
        reinterpret_cast<const RefCountedStringBufferHeader *>(oldData) - 1;
    const unsigned int oldLength = oldHeader->length;
    InterlockedIncrement(const_cast<LONG *>(&oldHeader->refCount));

    ReleaseAnsiBuffer();
    AllocateAnsiBuffer(oldLength + additionalLength);
    memmove(m_data, oldData, oldLength);

    reinterpret_cast<StringBase<char, mychar_traits<char> > *>(&oldData)
        ->ReleaseAnsiBuffer();
}

template <>
void
StringBase<wchar_t, mychar_traits<wchar_t> >::GrowWideBuffer(
    unsigned int additionalLength)
{
    wchar_t *oldData = m_data;
    if (oldData == g_emptyWideString) {
        AllocateWideBuffer(additionalLength);
        return;
    }

    const RefCountedStringBufferHeader *oldHeader =
        reinterpret_cast<const RefCountedStringBufferHeader *>(oldData) - 1;
    const unsigned int oldLength = oldHeader->length;
    InterlockedIncrement(const_cast<LONG *>(&oldHeader->refCount));

    ReleaseWideBuffer();
    AllocateWideBuffer(oldLength + additionalLength);
    memmove(m_data, oldData, oldLength * sizeof(wchar_t));

    reinterpret_cast<StringBase<wchar_t, mychar_traits<wchar_t> > *>(&oldData)
        ->ReleaseWideBuffer();
}

template <>
void
StringBase<char, mychar_traits<char> >::DetachAnsiIfShared()
{
    char *oldData = m_data;
    RefCountedStringBufferHeader *oldHeader =
        reinterpret_cast<RefCountedStringBufferHeader *>(oldData) - 1;
    if (oldHeader->refCount <= 1) {
        return;
    }

    const unsigned int length = oldHeader->length;

    AllocateAnsiBuffer(length);
    memmove(m_data, oldData, length);

    reinterpret_cast<StringBase<char, mychar_traits<char> > *>(&oldData)
        ->ReleaseAnsiBuffer();
}

template <>
void
StringBase<wchar_t, mychar_traits<wchar_t> >::DetachWideIfShared()
{
    wchar_t *oldData = m_data;
    RefCountedStringBufferHeader *oldHeader =
        reinterpret_cast<RefCountedStringBufferHeader *>(oldData) - 1;
    if (oldHeader->refCount <= 1) {
        return;
    }

    const unsigned int length = oldHeader->length;

    AllocateWideBuffer(length);
    memmove(m_data, oldData, length * sizeof(wchar_t));

    reinterpret_cast<StringBase<wchar_t, mychar_traits<wchar_t> > *>(&oldData)
        ->ReleaseWideBuffer();
}

} // namespace mystr
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: StringBaseTemplate ANSI/wide allocation, append-growth, and detach-if-shared primitives over the 12-byte ref-counted string buffer header. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00582d80-0x00583132 StringBase Allocation Grow And COW

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: `StringBase` ANSI/wide allocation, append-growth, and copy-on-write detach primitives.
- Parent aggregate: [UID:0001J1][0x00582500-0x00583206.StringBaseConstructionAndCow](by-memory/0x00582500-0x00583206.StringBaseConstructionAndCow.md).
- Likely source file: [UID:0000OA][StringBase](by-file/StringBase.md), generated as `NexusTK/util/StringBase.cpp`.
- Canonical owner/emitter route: [UID:0001WS][StringBaseTemplate](by-type/by-template/StringBaseTemplate.md), emitted through [UID:0000OA][StringBase](by-file/StringBase.md).
- C++ reconstruction: first-draft formal C++ is now populated for the six modeled function bodies only. The block intentionally does not emit `AllocateAnsiHeader`, `AllocateWideHeader`, `HeaderFromData`, lambdas, namespace helpers, local helper functions, or any other standalone 0002RO-owned helper body.

## Covered Range

| Range | IDA function | Source-facing role | Reconstruction route |
| --- | --- | --- | --- |
| `0x00582d80-0x00582e2c` | `sub_582D80` | `AllocateAnsiBuffer` | first-draft formal C++ in this page |
| `0x00582e2c-0x00582e30` | padding | four `0xcc` bytes | no code |
| `0x00582e30-0x00582ee2` | `sub_582E30` | `AllocateWideBuffer` | first-draft formal C++ in this page |
| `0x00582ee2-0x00582ef0` | padding | fourteen `0xcc` bytes | no code |
| `0x00582ef0-0x00582f98` | `sub_582EF0` | `GrowAnsiBuffer` | first-draft formal C++ in this page |
| `0x00582f98-0x00582fa0` | padding | eight `0xcc` bytes | no code |
| `0x00582fa0-0x0058304b` | `sub_582FA0` | `GrowWideBuffer` | first-draft formal C++ in this page |
| `0x0058304b-0x00583050` | padding | five `0xcc` bytes | no code |
| `0x00583050-0x005830bf` | `sub_583050` | `DetachAnsiIfShared` | first-draft formal C++ in this page |
| `0x005830bf-0x005830c0` | padding | one `0xcc` byte | no code |
| `0x005830c0-0x00583132` | `sub_5830C0` | `DetachWideIfShared` | first-draft formal C++ in this page |
| `0x00583132-0x00583140` | parent padding | fourteen `0xcc` bytes before suffix helper successor | no child needed |

Adjacent starts remain outside this child: `0x00582d70` is sibling [UID:0002RN][0x005829f0-0x00582d80.StringBaseShareAssignAndAccess](by-memory/0x005829f0-0x00582d80.StringBaseShareAssignAndAccess.md) `GetWideAt`, and `0x00583140` is sibling [UID:0002RP][0x00583140-0x00583206.StringBaseSuffixChecks](by-memory/0x00583140-0x00583206.StringBaseSuffixChecks.md) suffix-check code.

## Current IDA MCP Evidence

Current B005 source-quality research used live IDA MCP session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. The session was responsive, `hexrays_ready` was true, and IDA reported `is_analyzing:false`.

Function inventory:

- `lookup_funcs` reports `0x00582d80` `sub_582D80`, size `0xac`.
- `lookup_funcs` reports `0x00582e30` `sub_582E30`, size `0xb2`.
- `lookup_funcs` reports `0x00582ef0` `sub_582EF0`, size `0xa8`.
- `lookup_funcs` reports `0x00582fa0` `sub_582FA0`, size `0xab`.
- `lookup_funcs` reports `0x00583050` `sub_583050`, size `0x6f`.
- `lookup_funcs` reports `0x005830c0` `sub_5830C0`, size `0x72`.
- `lookup_funcs` reports no function at `0x00583132`; successor `0x00583140` is `sub_583140`.
- `entity_query` over `0x00582d70-0x00583150` found no hidden modeled functions inside this child beyond the six listed starts.

Boundary and padding bytes:

- `0x00582e2c-0x00582e30`: four `0xcc` bytes.
- `0x00582ee2-0x00582ef0`: fourteen `0xcc` bytes.
- `0x00582f98-0x00582fa0`: eight `0xcc` bytes.
- `0x0058304b-0x00583050`: five `0xcc` bytes.
- `0x005830bf-0x005830c0`: one `0xcc` byte.
- `0x00583132-0x00583140`: fourteen `0xcc` bytes.

Global/xref evidence:

- `0x00670290` / `g_emptyAnsiString` is referenced by `AllocateAnsiBuffer` at `0x00582d8e` and `GrowAnsiBuffer` at `0x00582f1a`.
- `0x00670278` / `g_emptyWideString` is referenced by `AllocateWideBuffer` at `0x00582e3e` and `GrowWideBuffer` at `0x00582fca`.
- ANSI pools are consumed by `AllocateAnsiBuffer`: `0x0069bb6c` at `0x00582da0`, `0x0069bb94` at `0x00582dbb`, `0x0069bbbc` at `0x00582dd6`, and `0x0069bbe4` at `0x00582df1`.
- Wide pools are consumed by `AllocateWideBuffer`: `0x0069bacc` at `0x00582e50`, `0x0069baf4` at `0x00582e6b`, `0x0069bb1c` at `0x00582e86`, and `0x0069bb44` at `0x00582ea1`.
- Each string pool still has the expected constructor/allocation/release/cleanup four-reference pattern. `PoolAllocator::Allocate()` is the source-facing name for `0x004b1400`, and `::operator new(size_t)` is the source-facing name for `unknown_libname_19` at `0x005c7790`.

## Behavior Evidence

### `0x00582d80` - `AllocateAnsiBuffer`

- If `length == 0`, stores `0x00670290` / `g_emptyAnsiString` in `m_data` and returns it.
- For nonzero length, selects storage by requested ANSI capacity:
  - `<= 64`: `g_ansiPool64` at `0x0069bb6c`, then writes `capacity = 64`.
  - `<= 128`: `g_ansiPool128` at `0x0069bb94`, then writes `capacity = 128`.
  - `<= 256`: `g_ansiPool256` at `0x0069bbbc`, then writes `capacity = 256`.
  - `<= 512`: `g_ansiPool512` at `0x0069bbe4`, then writes `capacity = 512`.
  - `> 512`: heap allocates `length + sizeof(RefCountedStringBufferHeader) + sizeof(char)`, observed in IDA as `length + 13`, then writes `capacity = length`.
- Writes `refCount = 1`, `length = requestedLength`, and a trailing `'\0'`.
- Returns and stores the data pointer immediately after the 12-byte header.
- The formal C++ inlines this pool selection and header initialization inside `AllocateAnsiBuffer`; it must not be factored into a local helper or standalone emitted source body.

### `0x00582e30` - `AllocateWideBuffer`

- If `length == 0`, stores `0x00670278` / `g_emptyWideString` in `m_data` and returns it.
- For nonzero length, selects storage by requested UTF-16 capacity:
  - `<= 64`: `g_widePool64` at `0x0069bacc`, then writes `capacity = 64`.
  - `<= 128`: `g_widePool128` at `0x0069baf4`, then writes `capacity = 128`.
  - `<= 256`: `g_widePool256` at `0x0069bb1c`, then writes `capacity = 256`.
  - `<= 512`: `g_widePool512` at `0x0069bb44`, then writes `capacity = 512`.
  - `> 512`: heap allocates `length * sizeof(wchar_t) + sizeof(RefCountedStringBufferHeader) + sizeof(wchar_t)`, observed in IDA as `2 * length + 14`, then writes `capacity = length`.
- Writes `refCount = 1`, `length = requestedLength`, and a trailing `L'\0'`.
- Returns and stores the data pointer immediately after the 12-byte header.
- The formal C++ inlines this wide pool selection and header initialization inside `AllocateWideBuffer`; it must not be factored into a local helper or standalone emitted source body.

### `0x00582ef0` - `GrowAnsiBuffer`

- If current data is the ANSI empty sentinel, calls `AllocateAnsiBuffer(additionalLength)` and returns.
- Otherwise reads the old header at `oldData - 0x0c`, saves `oldLength`, and increments the old `refCount` with `InterlockedIncrement`.
- Releases the current object through [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md) `ReleaseAnsiBuffer`.
- Allocates a new ANSI buffer with `oldLength + additionalLength`.
- Copies exactly `oldLength` bytes with `memmove`.
- Releases the retained old pointer through `ReleaseAnsiBuffer` using the saved one-pointer slot as the receiver.
- This is an append-growth helper. It takes an additional byte count and is distinct from later `0x00584b10` `EnsureAnsiCapacity`.

### `0x00582fa0` - `GrowWideBuffer`

- If current data is the wide empty sentinel, calls `AllocateWideBuffer(additionalLength)` and returns.
- Otherwise reads the old header at `oldData - 0x0c`, saves `oldLength`, and increments the old `refCount` with `InterlockedIncrement`.
- Releases the current object through [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md) `ReleaseWideBuffer`.
- Allocates a new wide buffer with `oldLength + additionalLength`.
- Copies exactly `oldLength * sizeof(wchar_t)` bytes with `memmove`.
- Releases the retained old pointer through `ReleaseWideBuffer` using the saved one-pointer slot as the receiver.
- This is an append-growth helper. It takes an additional UTF-16 code-unit count and is distinct from later `0x00584ba0` `EnsureWideCapacity`.

### `0x00583050` - `DetachAnsiIfShared`

- Reads current data and header.
- If `refCount <= 1`, returns without modifying the object.
- If `refCount > 1`, saves the old pointer and length, allocates a same-length ANSI buffer, copies `length` bytes, and releases the old shared pointer through [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md) `ReleaseAnsiBuffer`.
- The source-facing name is `DetachAnsiIfShared`. `DetachAnsiForWrite` is a weaker synonym, and `CopyOnWriteAnsi` is too broad for the exact branch condition.

### `0x005830c0` - `DetachWideIfShared`

- Reads current data and header.
- If `refCount <= 1`, returns without modifying the object.
- If `refCount > 1`, saves the old pointer and length, allocates a same-length wide buffer, copies `length * sizeof(wchar_t)` bytes, and releases the old shared pointer through [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md) `ReleaseWideBuffer`.
- The source-facing name is `DetachWideIfShared`, matching accepted COW-detach vocabulary from [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md).

## Header, Sentinels, And Pools

The six functions use the shared [UID:0001VQ][RefCountedStringBufferHeader](by-type/by-struct/RefCountedStringBufferHeader.md) immediately before `m_data`:

| Data-relative offset | Field | Use in this target |
| --- | --- | --- |
| `-0x0c` | `refCount` | Written as `1` by allocation; incremented by grow; tested by detach. |
| `-0x08` | `length` | Written by allocation; read by grow/detach for allocation and copy sizes. |
| `-0x04` | `capacity` | Written as pool bucket capacity or requested heap length. |
| `0x00` | data | ANSI or UTF-16 payload, followed by a terminator. |

The empty sentinels and pools are [UID:0000TM][StringBufferSentinelsAndPools](by-global/StringBufferSentinelsAndPools.md) storage. [UID:0000MM][PoolAllocator](by-file/PoolAllocator.md) owns the allocator mechanics only; this target owns the string-side selection and header initialization policy.

## Source-Shape And C++ Reconstruction Rationale

The current reconstruction gate is satisfied: `RECONSTRUCTABLE:TRUE`, nonblank `EMITTER_UIDS:0001WS`, and `(COMPLETION + CONFIDENCE) / 2 = 91.5`, above the current code-entry threshold.

The formal block is deliberately range-faithful:

- It emits only the six modeled target function bodies listed above.
- It inlines ANSI/wide pool selection and header-pointer recovery inside the six target functions.
- It does not emit `AllocateAnsiHeader`, `AllocateWideHeader`, `HeaderFromData`, lambdas, namespace helpers, local helper functions, or any other standalone 0002RO-owned helper body.
- It uses direct one-pointer saved-slot release calls for retained old data pointers, matching the binary call shape where the saved local pointer's address is passed to `ReleaseAnsiBuffer` or `ReleaseWideBuffer`.
- It does not construct `StringBase oldValue`, `StringBase oldString`, or other temporary objects that would imply constructor/destructor bodies outside this target range.
- Calls to `ReleaseAnsiBuffer` and `ReleaseWideBuffer` are dependency calls to [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md), not emitted bodies owned by 0002RO.

## Caller/Callee Evidence

Internal dependency graph:

- `GrowAnsiBuffer` calls `ReleaseAnsiBuffer`, `AllocateAnsiBuffer`, `memmove`, and `InterlockedIncrement`.
- `GrowWideBuffer` calls `ReleaseWideBuffer`, `AllocateWideBuffer`, `memmove`, and `InterlockedIncrement`.
- `DetachAnsiIfShared` calls `AllocateAnsiBuffer`, `memmove`, and `ReleaseAnsiBuffer`.
- `DetachWideIfShared` calls `AllocateWideBuffer`, `memmove`, and `ReleaseWideBuffer`.

Representative caller roles:

- `AllocateAnsiBuffer` and `AllocateWideBuffer` are called by construction, assignment, grow/detach, formatting, split/trim, and capacity helpers across the `StringBase` family.
- `GrowAnsiBuffer` is called from ANSI append slow paths in [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) at `0x00584160`, `0x00584200`, and `0x00584280`.
- `GrowWideBuffer` is called from wide append slow paths in [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) at `0x00584320`, `0x005843d0`, and `0x00584470`.
- `DetachAnsiIfShared` and `DetachWideIfShared` are used by broader StringBase write/detach callers, including later mutation helpers.

## Ownership And Rejected Alternatives

Accepted owner/emitter: [UID:0001WS][StringBaseTemplate](by-type/by-template/StringBaseTemplate.md), emitted through [UID:0000OA][StringBase](by-file/StringBase.md).

Reasoning:

- The functions operate on the one-pointer `StringBase<T>` object layout and the shared 12-byte string buffer header.
- Live compiler metadata elsewhere preserves `mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t>>`.
- Sibling pages [UID:0002RN][0x005829f0-0x00582d80.StringBaseShareAssignAndAccess](by-memory/0x005829f0-0x00582d80.StringBaseShareAssignAndAccess.md), [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md), [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md), and [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md) already route related StringBase method/helper bodies through the `StringBaseTemplate` / `StringBase.cpp` model.

Rejected alternatives:

- `EnsureAnsiCapacity` and `EnsureWideCapacity` are rejected for `0x00582ef0` and `0x00582fa0`; those names belong to later helpers `0x00584b10` and `0x00584ba0`.
- `ReserveAdditionalAnsi` / `ReserveAdditionalWide` are weaker names than the accepted `GrowAnsiBuffer` / `GrowWideBuffer` names already used by [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md).
- `CopyOnWriteAnsi` / `CopyOnWriteWide` are too broad for the exact detach-if-shared branch.
- `PoolAllocator` owns allocator mechanics, not this StringBase buffer policy.
- `RefCountedStringBufferHeader` owns layout evidence, not the methods.
- `StringBufferSentinelsAndPools` owns global storage evidence, not the methods.
- `LObject`, `SimpleUString`, feature caller modules, and stale generated file-only routes are consumer/facade/noise candidates, not direct owners for this byte range.

## Score Rationale

Completion is raised from `85` to `91` because the page now records all six source-facing function names, exact function and padding boundaries, the pool/heap threshold model, sentinel and pool globals, header field meanings, grow and detach behavior, release-helper interactions, rejected helper-name alternatives, direct `StringBaseTemplate` owner/emitter route, and range-faithful formal first-draft C++.

Confidence is raised from `89` to `92` because live IDA MCP confirms the byte range, function inventory, decompiled behavior, xrefs, sentinels, pool globals, dependency helpers, padding, and type/source route. Confidence remains below final-audit level because exact original public API spelling, public/private declaration placement, and possible facade aliases such as `SimpleUString` remain unresolved.

## Changes

- 2026-06-26 B005 implementation sync:
  - Before: `COMPLETION:85`, `CONFIDENCE:89`, `CANONICAL_OWNER:0001WS`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0001WS`, blank formal C++.
  - After: `COMPLETION:91`, `CONFIDENCE:92`, same owner/emitter/reconstructable metadata, and first-draft formal C++ populated for exactly six target functions: `AllocateAnsiBuffer`, `AllocateWideBuffer`, `GrowAnsiBuffer`, `GrowWideBuffer`, `DetachAnsiIfShared`, and `DetachWideIfShared`.
  - Summary/evidence: incorporated B005 accepted report evidence from live MCP session `80de0a67`: exact six-function inventory, internal padding, sentinel/pool xrefs, pool/heap thresholds, `RefCountedStringBufferHeader` field uses, `PoolAllocator::Allocate()` and `::operator new(...)` allocation routes, release-helper dependencies, append-growth callers, COW-detach behavior, and rejected names. The formal block emits no 0002RO-owned helper bodies and uses direct saved-slot release calls instead of temporary `StringBase` objects.
- 2026-06-15 A003 low-completion refresh:
  - Before: `COMPLETION:78`, `CONFIDENCE:86`, canonical owner/emitter [UID:0000OA][StringBase](by-file/StringBase.md).
  - Changed to: `COMPLETION:85`, `CONFIDENCE:89`, canonical owner/emitter [UID:0001WS][StringBaseTemplate](by-type/by-template/StringBaseTemplate.md), which emits through [UID:0000OA][StringBase](by-file/StringBase.md).
  - Summary/evidence: IDA MCP reconfirmed the six contained helper boundaries, ANSI/wide sentinel and pool/heap allocation behavior, grow and detach-on-share paths, internal allocator/release/memmove calls, padding before suffix checks, and the StringBase template parent route. The old blank-C++ rationale is superseded by the 2026-06-26 B005 implementation under the current combined-score/emitter gate.

## Cross-References

- [UID:0001J1][0x00582500-0x00583206.StringBaseConstructionAndCow](by-memory/0x00582500-0x00583206.StringBaseConstructionAndCow.md)
- [UID:0000OA][StringBase](by-file/StringBase.md)
- [UID:0001WS][StringBaseTemplate](by-type/by-template/StringBaseTemplate.md)
- [UID:0001VQ][RefCountedStringBufferHeader](by-type/by-struct/RefCountedStringBufferHeader.md)
- [UID:0000TM][StringBufferSentinelsAndPools](by-global/StringBufferSentinelsAndPools.md)
- [UID:0000MM][PoolAllocator](by-file/PoolAllocator.md)
- [UID:0001VK][PoolAllocatorLayout](by-type/by-struct/PoolAllocatorLayout.md)
- [UID:0002RN][0x005829f0-0x00582d80.StringBaseShareAssignAndAccess](by-memory/0x005829f0-0x00582d80.StringBaseShareAssignAndAccess.md)
- [UID:0002RT][0x005832f0-0x00583714.StringBaseReleaseAndCapacity](by-memory/0x005832f0-0x00583714.StringBaseReleaseAndCapacity.md)
- [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md)
- [UID:0002RS][0x00584160-0x005845af.StringBaseAppendAndFind](by-memory/0x00584160-0x005845af.StringBaseAppendAndFind.md)
- [UID:0001J3][0x005832f0-0x00584d7e.StringBaseReleaseFormatAndMutation](by-memory/0x005832f0-0x00584d7e.StringBaseReleaseFormatAndMutation.md)
