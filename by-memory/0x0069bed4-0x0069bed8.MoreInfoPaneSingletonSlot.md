*** UID:0002A8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000P0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000P0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
MoreInfoPane *g_pMoreInfoPane = 0;
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-retained MoreInfoPane pointer definition best named g_pMoreInfoPane, owned/emitted through UserLookPane.cpp at position 5; writable zero-filled storage has exactly one executable reference, the UID0003RI constructor assignment immediately after m_moreInfoPane, while no reads, second writes, address-taking uses, or destructor clears remain. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0069bed4-0x0069bed8 MoreInfoPane Singleton Slot

## Status

- Address range: `0x0069bed4-0x0069bed8`
- Entity kind: global-data pointer
- Disposition: reconstructable source-retained project global pointer
- Rebuild handling: `source-declared/generated-binary`
- Proposed owner: [UID:0000P0][UserLookPane](by-file/UserLookPane.md) UserLookPane
- Confidence: very strong for storage, exact writer, local child-pane relationship, one-definition source route, and declaration shape; strong for the descriptive spelling/linkage.

## Storage

`0x0069bed4-0x0069bed8` is the four-byte writable storage for the high-probability source-facing global `g_pMoreInfoPane`. The final executable's generated/IDA-style labels `dword_69BED4` and `unk_69BED4` are placeholders, not source-quality names. The slot receives the `MoreInfoPane*` child created by `UserLookPane::UserLookPane`. The live relationship is still the `UserLookPane` member at `this + 0xf8`, best named `UserLookPane::m_moreInfoPane`; this global is a retained singleton/debug/compatibility alias of that same child pointer.

The slot is zero-initialized BSS-style storage inside writable `.data`, not an initialized raw-file dword. The target VA is inside the `.data` virtual span but beyond the section's raw-backed payload, so the loader supplies the initial zero value.

## Evidence

- 2026-06-13 A001 live IDA MCP reports `dword_69BED4` in `.data`, initial bytes/value `0`, exactly one direct xref, and no direct reads.
- `xrefs_to 0x0069bed4` returns only `0x0059f35c` inside `sub_59F260`, and `trace_data_flow` shows the instruction `mov dword ptr unk_69BED4, esi`.
- `UserLookPane::UserLookPane` decompilation allocates a `372`-byte (`0x174`, Verified with `int_convert.py`) `MoreInfoPane`, installs the three `MoreInfoPane` vtable views, stores the child at `UserLookPane + 0xf8` (`248` decimal, Verified with `int_convert.py`), then writes the same pointer into `dword_69BED4`.
- Existing `UserLookPane` docs identify `MoreInfoPane` as a local child pane constructed by the `UserLookPane` constructor.
- 2026-06-20 B004 PE recheck independently confirmed image base `0x00400000`; `.text` at `0x00401000-0x0060c4ac`; `.rdata` at `0x0060d000-0x0066c0be`; `.data` at `0x0066d000-0x0069ce24` with raw `0x26ac00-0x278400`; and zero-fill after the raw payload through the virtual end.
- The same PE scan found exactly one whole-executable little-endian reference to `0x0069bed4` (`d4 be 69 00`): the immediate operand of the write at `0x0059f35c`.
- Instruction-level recheck around the constructor shows the concrete data flow: allocate `0x174` bytes, set `esi` to the returned child pointer or zero on allocation failure, install `MoreInfoPane` vtables at `[esi]`, `[esi+0xa0]`, and `[esi+0xa4]`, store the pointer at `[edi+0xf8]`, then write the same value to `[0x0069bed4]`. If allocation fails, both the member and global receive null. The member is immediately reloaded and passed to `0x005446b0`, while this global has no direct read, clear, destructor reset, address-taken reference, or second writer in the current executable.

## Reconstruction Notes

Keep this with the user-look/profile pane family under [UID:0000P0][UserLookPane](by-file/UserLookPane.md). The managed formal block is the sole null-initialized `MoreInfoPane` pointer definition for this exact storage range. Its file-level emitter position is 5. UID0003RI uses class-child position 10 in the separate UID0000FP child scope, so current generated hierarchy expands that constructor before this file-level definition; UID0000FP's extern makes the global compile-visible at the use site.

The constructor assignment belongs only to [UID:0003RI][0x0059f260-0x0059f4fd.UserLookPaneConstructor](by-memory/0x0059f260-0x0059f4fd.UserLookPaneConstructor.md): it stores the newly constructed child in `m_moreInfoPane`, then mirrors that same member value into this global. [UID:0000FP][UserLookPane](by-class/UserLookPane.md) supplies the compile-visible extern declaration. This page does not duplicate the constructor statements outside its own managed global definition.

No current evidence justifies merging this with the preceding [UID:0002A7][0x0069bed0-0x0069bed4.UserListDialogHuntersListOnlyFlag](by-memory/0x0069bed0-0x0069bed4.UserListDialogHuntersListOnlyFlag.md) or following [UID:0002A9][0x0069bed8-0x0069bee0.TargetSelectionModeFlags](by-memory/0x0069bed8-0x0069bee0.TargetSelectionModeFlags.md). Address adjacency is weaker than the constructor xref/source-family evidence, and both neighbors have unrelated owners and xref families. No separate by-global page is required because the exact source definition, owner, emitter, assignment, and absence of reader/clear/lifecycle APIs are complete here.

## Autogen Status

This pointer slot is attached to [UID:0000P0][UserLookPane](by-file/UserLookPane.md) as reconstructable global/member-adjacent state and emits the sole source definition. Current evidence proves the constructor write target and user-look child-pane relationship. The lack of direct reads means the current binary does not consume the alias after construction, but it does not erase the absolute write or source-level storage; preserving one definition is the faithful source/data reconstruction.

## Rejected Alternatives

- Reject `dword_69BED4` / `unk_69BED4` as final source names: they are generated binary labels with no source meaning.
- Reject `s_pMoreInfoPane` as the primary spelling: internal static linkage is possible, but there are no reads to prove file-local use. A non-static `g_` name is the stronger source-shape explanation for a retained global assignment in a non-LTCG-style build. Exact spelling/linkage remains unprovable without PDB, map, or source metadata.
- Reject a member-only reconstruction as the full answer: `UserLookPane::m_moreInfoPane` at `+0xf8` is the live ownership mechanism, but the absolute store to `0x0069bed4` is a real project-code instruction and still requires source-level storage for faithful reconstruction.
- Reject compiler/linker residue: the compiler can create zero-filled storage, but it does not invent a semantic absolute write to a `MoreInfoPane*` after child-pane construction and vtable installation. The write is ordinary constructor code, not CRT initialization, vtable data, EH metadata, import glue, or relocation-only output.
- Reject [UID:00008Q][MoreInfoPane](by-class/MoreInfoPane.md) as canonical owner for the slot: the type is `MoreInfoPane*`, but `MoreInfoPane` methods do not access this storage. The write is module state in `UserLookPane::UserLookPane`.
- Reject [UID:0000FP][UserLookPane](by-class/UserLookPane.md) as the narrower canonical owner: the storage is not a class member, vtable, or type artifact. It is file/module-level state emitted through `UserLookPane.cpp`.
- Reject [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md) and adjacent `.data` owners: `GeneralPurposePanel` constructs `UserLookPane` but never touches this slot, while the neighboring UserList and TargetSelection slots have unrelated xref families.
- Reject `int`, `DWORD`, flag, table-entry, and vtable interpretations: the only writer stores a heap object pointer after `MoreInfoPane` vtable setup, and the address is writable `.data`, not read-only vtable storage.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `90` | Exact storage, loader zero-fill, one xref, instruction-level writer, whole-PE search, member/global data flow, type/name, owner/emitter, position, class extern, sole definition, constructor assignment, no-read lifecycle, split, and rejected alternatives are complete. |
| Confidence `92` | Live IDA xrefs/data flow, constructor decompilation, whole-PE reference scanning, UserLookPane/MoreInfoPane support, and deterministic source order converge. Exact original spelling/linkage remains unavailable source/PDB/map evidence and is the only material cap. |

## Cross-References

- [UID:00008Q][MoreInfoPane](by-class/MoreInfoPane.md) MoreInfoPane class.
- [UID:0000FP][UserLookPane](by-class/UserLookPane.md) UserLookPane class.
- [UID:0000P0][UserLookPane](by-file/UserLookPane.md) UserLookPane file.
- [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md) UserLookPane and profile pane memory range.

## Changes

- 2026-07-19 B002 UID0003RI accepted callback: raised `88/89 -> 90/92`, retained UID0000P0 owner/emitter and reconstructable state, set position 5, preserved the sole managed definition, closed the former broader-source need, and synchronized the complete class-extern/constructor-assignment/source-order contract while retaining exact zero-fill, one-xref, no-read, split, linkage-cap, and rejected-alternative evidence.
- 2026-06-20 B004 Rule 26 incorporation:
  - Before: `COMPLETION:85`, `CONFIDENCE:86`; the page left source-level retention open and intentionally emitted no C++.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:89`; kept owner/emitter [UID:0000P0][UserLookPane](by-file/UserLookPane.md), closed the source-retention question as a high-probability source-retained `MoreInfoPane*` global alias, and emitted declaration-only C++ `MoreInfoPane *g_pMoreInfoPane = 0;`.
  - Evidence: B004 PE recheck confirmed zero-filled `.data` storage with no raw file offset, exactly one executable reference to `0x0069bed4`, and the `UserLookPane::UserLookPane` sequence that stores the `MoreInfoPane*` at `this+0xf8` before mirroring it into the global at `0x0059f35c`; negative checks found no direct reads, address-taken refs, clear/reset path, or second writer.
- 2026-06-13 A001 refreshed low-score batch:
  - Before: `COMPLETION:78`, `CONFIDENCE:82`; evidence still cited an old `0xffffffff` initial value.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:86`; kept owner/emitter [UID:0000P0][UserLookPane](by-file/UserLookPane.md), and corrected rebuild handling to `source-declared/generated-binary`.
  - Evidence: live IDA MCP shows zero-initialized bytes/value, exactly one xref at `0x0059f35c`, instruction-level write `mov dword ptr unk_69BED4, esi`, and constructor decompilation that stores the `MoreInfoPane` child pointer at `this+0xf8` and then into the global slot. C++ remains blank because no source-level read/retention pattern is recovered.
- 2026-06-02:
  - Before: scored `75/65`, reconstructability and parent blank.
  - After: scored `78/82`, marked reconstructable, and attached to [UID:0000P0][UserLookPane](by-file/UserLookPane.md) with no emitted C++.
  - Why: existing evidence proves the singleton slot write from `UserLookPane::UserLookPane`, but no direct read has been recovered, so final source retention remains provisional.
