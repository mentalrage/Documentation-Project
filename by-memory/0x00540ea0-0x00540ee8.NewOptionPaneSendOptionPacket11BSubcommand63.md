*** UID:0003NO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000M7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000M7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void NewOptionPaneSendOptionPacket11BSubcommand63(unsigned char optionCode, unsigned char selectedIndex)
{
    unsigned char packet[6];
    *reinterpret_cast<unsigned short *>(packet) = 0x011b;
    packet[2] = 0x63;
    packet[3] = optionCode;
    packet[4] = selectedIndex;
    packet[5] = 0;

    g_packetSender->QueueAndSendPacket(packet, 5);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: File-level newer OptionPane/NewOptionPane duplicate of the old subcommand-63 packet sender; sends five-byte 0x011b/0x63/optionCode/selectedIndex through g_packetSender, keeps the zero scratch byte unsent, has no direct inbound route in MCP session 80de0a67, and now has first-draft C++ through inferred NewOptionPane-prefixed helper naming. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00540ea0-0x00540ee8 NewOptionPaneSendOptionPacket11BSubcommand63

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw function-shaped option packet helper.
- Direct owner: [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- Parent aggregate: [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- Confidence: strong for byte-level packet behavior, callee/state dependency, surrounding alignment, and the inferred file-local signature; medium-high for exact original helper spelling because IDA reports no direct inbound xrefs to the raw start.

## Behavior

Builds and queues a five-byte option packet through the global packet sender. The packet begins with word opcode `0x011b` / decimal `283` (Verified with int_convert.py), uses fixed subcommand byte `0x63` / decimal `99` / ASCII `c` (Verified with int_convert.py), copies two caller-supplied low bytes into the following payload slots, appends a zero byte in the local stack buffer, and sends length `5`.

The helper is source-shaped code in the option helper island, but current IDA does not promote it as a function and does not report direct xrefs to the entry. Treat it as a real executable child body with unresolved final call path/source visibility.

B010's old-`OptionPane` source-quality pass identifies [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) as the old-option duplicate-body counterpart. The two helpers share the `0x011b/0x63` two-argument packet shape but remain distinct physical bodies and should not be merged.

## Signature And Packet Layout

Accepted B015 source-facing signature is file-local `static void NewOptionPaneSendOptionPacket11BSubcommand63(unsigned char optionCode, unsigned char selectedIndex)`. The helper name is inferred/descriptive, not original-symbol proof; the `NewOptionPane` prefix distinguishes this newer duplicate from old [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) while keeping both physical bodies in `OptionPane.cpp`.

| Evidence point | Current conclusion |
| --- | --- |
| Calling shape | Callee cleanup `retn 8` consumes two stack arguments. The body only reads the low byte of `[ebp+8]` and `[ebp+0c]`; incoming `ecx` is not used as a pane pointer and is overwritten with `dword_67A7EC` before the send call. |
| Source placement | Keep as an `OptionPane.cpp` file-level helper. Code locality ties it to the newer `NewOptionPane` option helper island, but the body consumes no `this`, so the accepted source shape is a static/file-local helper rather than a `NewOptionPane` method. |
| Rebuild handling | `source-authored` outbound option protocol code. |

Sent packet bytes:

| Sent offset | Stack slot | Value |
| --- | --- | --- |
| `+0x00..+0x01` | `[ebp-0x10]` | word opcode `0x011b` / decimal `283` (Verified with int_convert.py) |
| `+0x02` | `[ebp-0x0e]` | fixed subcommand `0x63` / decimal `99` / ASCII `c` (Verified with int_convert.py) |
| `+0x03` | `[ebp-0x0d]` | low byte of first caller argument `[ebp+8]` |
| `+0x04` | `[ebp-0x0c]` | low byte of second caller argument `[ebp+0c]` |
| not transmitted | `[ebp-0x0b]` | local zero terminator; the send length pushed before `QueueAndSendPacket` is `5` |

## IDA MCP Evidence

- 2026-06-25 B015 MCP session `80de0a67` for `NexusTK.exe.i64` reconfirmed `lookup_funcs 0x00540ea0` as `Not a function`; `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search all found zero inbound routes. Disassembly still shows two low-byte stack arguments, no consumed pane `this`, load of `dword_67A7EC`, packet word `0x011b`, fixed byte `0x63`, payload bytes at sent offsets `+3` and `+4`, local unsent zero byte, call to `sub_574BB0` / `QueueAndSendPacket` with length `5`, and terminal `retn 8`.
- The B015 pass treats the empty direct-route matrix as a confidence cap and source-visibility caveat, not a no-code blocker, because the old duplicate [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) already established the same route-capped source-quality argument roles `optionCode` and `selectedIndex` for the `0x011b/0x63` packet shape.
- 2026-06-23 B001 MCP evidence session `ff68e691` for `NexusTK.exe.i64` was the prior accepted evidence pass: `lookup_funcs 0x00540ea0` reports `Not a function`, and `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search for `a0 0e 54 00` all return zero inbound routes. The older `0b5e057e` pass is historical and agrees on this state.
- The same current evidence session reports sibling raw starts `0x00540ef0`, `0x00540f50`, and `0x00540ff0` as `Not a function`, so this page's raw/no-route state is part of the broader helper-island state rather than an isolated artifact.
- 2026-06-14 session-aware IDA MCP health check reports active database `NexusTK.exe.i64`, imagebase `0x400000`, auto-analysis ready, and Hex-Rays ready.
- 2026-06-14 `lookup_funcs` reports modeled functions at `0x00540db0`, `0x00540e50`, and `0x00541040`, but `0x00540ea0`, `0x00540ee8`, and `0x00540ef0` all report `Not a function`. This matches the parent island's raw-helper split.
- 2026-06-14 `disasm 0x00540ea0` shows a normal stack-cookie prologue, `mov al, [ebp+8]`, `mov ecx, dword_67A7EC`, copies from `[ebp+8]` and `[ebp+0c]`, stack packet initialization, `word ptr [ebp-0x10] = 0x011b`, fixed byte `0x63`, a zero byte at `[ebp-0x0b]`, and call to `0x00574bb0` with pushed length `5`.
- The terminal instruction is `retn 8` at `0x00540ee8`. Byte-level body coverage therefore runs through the return instruction bytes at `0x00540ee8-0x00540eeb`, and listing alignment begins at `0x00540eeb` before the next raw helper at `0x00540ef0`. The current filename/range label uses the observed terminal return-instruction address.
- 2026-06-14 `get_bytes 0x00540e90 size 112` shows the preceding modeled helper's final return byte at `0x00540e90`, fifteen `0xcc` bytes at `0x00540e91-0x00540ea0`, this raw body beginning `55 8b ec 83 ec 10`, return bytes `c2 08 00` at `0x00540ee8-0x00540eeb`, and five `0xcc` bytes at `0x00540eeb-0x00540ef0`.
- 2026-06-14 `xref_query` to `0x00540ea0` returns no inbound xrefs. `trace_data_flow` backward from `0x00540ea0` reaches only the start node and no edges, matching the earlier aggregate-page caveat.
- 2026-06-14 `insn_query` over `0x0053ff90-0x00541b2b` finds four calls to the common queue/send helper `0x00574bb0`: `0x00540b53`, sibling `SendOptionPacket11B` call `0x00540e7c`, this raw helper call `0x00540ed6`, and sibling `SendLowHighDisplayModeOption` call `0x005410fe`. No instruction in the same aggregate has an operand referencing `0x00540ea0`.
- 2026-06-14 `get_global_value 0x0067a7ec` reads the static value as zero, consistent with [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) being runtime-initialized by Socket lifecycle code rather than source-authored constant data.

## Touched State And Dependencies

| Item | Evidence | Meaning |
| --- | --- | --- |
| `dword_67A7EC` / `g_packetSender` | Loaded before the send call. | Global outbound packet sender/context; use the established packet-sender global wording and avoid generated unrelated global names. |
| stack packet word `0x011b` / decimal `283` (Verified with int_convert.py) | Disassembly at `0x00540ec8`. | Option packet opcode family shared with sibling option senders. |
| fixed byte `0x63` / decimal `99` / ASCII `c` (Verified with int_convert.py) | Disassembly at `0x00540ece`. | Specific option subcommand. |
| caller argument byte 1 | `[ebp+8]` copied to packet offset `+0x03`. | First caller-controlled payload byte. |
| caller argument byte 2 | `[ebp+0c]` copied to packet offset `+0x04`. | Second caller-controlled payload byte. |
| `0x00574bb0` | Callee at `0x00540ed6`; sibling queue/send calls occur at `0x00540e7c` and `0x005410fe`. | Common queue/send packet helper. |

## Reconstruction Notes

This is source-authored option protocol code and clears the active numeric code-entry gate (`RECONSTRUCTABLE:TRUE`, nonblank emitter route to [UID:0000M7][OptionPane](by-file/OptionPane.md), and combined score above `85`). B015 accepted the duplicate-body source-quality argument roles and inferred a file-local helper name, so formal first-draft C++ is now present in this page's `RECONSTRUCTION_CPP CODE` block.

Keep it file-level under `OptionPane.cpp` rather than a forced `NewOptionPane` method unless later xrefs prove the source call site and whether an unused pane `this` was passed in `ecx`. The no-direct-route state remains the reason confidence stops below final-audit quality rather than a reason to leave C++ blank.

## Cross-References

- [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- [UID:0001DY][0x00540e50-0x00540e91.SendOptionPacket11B](by-memory/0x00540e50-0x00540e91.SendOptionPacket11B.md) Shared three-byte `SendOptionPacket11B` helper.
- [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) g_packetSender.
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md) QueueAndSendPacket.
- [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) old-option duplicate-body counterpart.

## Changes

- 2026-06-14 A001 split this raw packet helper out of [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md). Live IDA MCP disassembly confirms the half-open boundary, opcode/subcommand bytes, sender dependency, `retn 8`, and lack of current inbound xrefs.
- 2026-06-14 C001 Goal 2 pass:
  - Before: `80/82`, with correct high-level packet behavior but limited written byte evidence and no explicit active-gate/source-code caveat.
  - Changed to: `86/88`; owner, emitter, reconstructable flag, and blank C++ block unchanged.
  - Evidence: live session-aware IDA MCP reconfirmed the raw non-function status, neighboring modeled functions, exact stack packet writes, `dword_67A7EC`, `QueueAndSendPacket` callee, no inbound xrefs or operand refs to `0x00540ea0`, sibling queue/send calls inside the `NewOptionPane` aggregate, static-zero packet-sender storage, and `0xcc` alignment before/after the raw body. `int_convert.py` verified the opcode/subcommand decimal values.
- 2026-06-21 B010 old-option source-quality cross-check: no score change; linked the old duplicate-body counterpart [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) and kept the bodies distinct rather than merged.
- 2026-06-23 B001 active-session recheck: no score, owner, emitter, or C++ change.
  - Evidence: current B001 MCP evidence session `ff68e691` reconfirmed this start as `Not a function`, with no inbound xref/code-ref/data-ref/immediate/pointer route, while preserving the byte-recovered `0x011b`/`0x63` packet behavior and file-level OptionPane placement. Older `0b5e057e` evidence is historical and agrees.
- 2026-06-25 B015 implementation callback:
  - Changed to `88/89`; owner, emitter, and reconstructable state unchanged.
  - Added formal C++ with inferred source-facing signature `static void NewOptionPaneSendOptionPacket11BSubcommand63(unsigned char optionCode, unsigned char selectedIndex)`.
  - Evidence: MCP session `80de0a67` reconfirmed raw no-function status, zero direct-route matrix, exact packet writes, unsent zero scratch byte, `dword_67A7EC` sender dependency, `QueueAndSendPacket` length `5`, and `retn 8`; old duplicate [UID:00040J][0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63](by-memory/0x0053e3d0-0x0053e41b.OptionPaneSendOptionPacket11BSubcommand63.md) preserves the source-quality argument role analogy while the bodies remain physically distinct.
