*** UID:0002AT | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
FrameMgr *g_frameRegistry;

template <>
Singleton<FrameMgr>::Singleton()
{
    g_frameRegistry = static_cast<FrameMgr *>(this);
}

template <>
Singleton<FrameMgr>::~Singleton()
{
    g_frameRegistry = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready loader-zero-filled FrameMgr singleton pointer with exactly 13 live refs, direct Singleton<FrameMgr> publication and ordinary/scalar/unwind cleanup semantics, external-linkage definition, and one-definition declaration/definition handling. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0069ae10-0x0069ae14 g_frameRegistry

## Status

- Entity kind: external-linkage global `FrameMgr *` singleton storage.
- Disposition: reconstructable source-declared/generated-binary storage.
- Exact range: `0x0069ae10-0x0069ae14`, size `0x4` / 4 bytes, no split or internal padding.
- Current bytes: `00 00 00 00`; SHA256 `DF3F619804A92FDB4057192DC43DD748EA778ADC52BC498CE80524C014B81119`.
- Source definition: exact R2 omitted-initializer `FrameMgr *g_frameRegistry;`.
- Declaration route: [UID:00005G][FrameHandler](by-class/FrameHandler.md) carries the `FrameMgr` forward declaration, [UID:0000Q0][g_frameRegistry](by-global/g_frameRegistry.md) carries the sole `extern FrameMgr *g_frameRegistry;` declaration, and this UID0002AT page carries the sole definition.
- Owner/emitter: [UID:0000JM][FrameMgr](by-file/FrameMgr.md), `NexusTK/ui/core/FrameMgr.cpp`.
- Metadata: `92/93`, owner/emitter UID0000JM, reconstructable true, contributor position `20`, `Nested:0`.
- Confidence: very strong for storage, type, lifecycle, owner, linkage, source definition, and boundaries; exact original symbol spelling remains inferred.

## Storage

| Range | Source type/name | Notes |
| --- | --- | --- |
| `0x0069ae10-0x0069ae14` | `FrameMgr *g_frameRegistry` | Active FrameMgr scheduler singleton; loader-zero-filled virtual `.data`, sole source definition on this page. |

## Image And Initialization Evidence

- B003's 2026-07-14 mandatory read-only MCP pass used evidence-time database `4836cd9f`; `server_health` returned `ok`, imagebase `0x00400000`, and ready auto-analysis, Hex-Rays, and string cache. Bounded bytes across `0x0069ae08-0x0069ae1b` were zero. These are captured evidence-time facts, not a claim that the session remains indefinitely active.
- Input image `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` has MD5 `4247E04E20B65D6414C7238AA8FF5515` and SHA256 `9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`.
- `.data` begins at RVA `0x0026d000`, has raw size `0x0d800`, and virtual size `0x2fe24`. Target RVA `0x0029ae10` is `0x20610` bytes beyond the raw end and remains `0x2014` bytes before the virtual end.
- Therefore no on-disk target dword exists. The PE loader supplies zero-filled static storage. Historical `ff ff ff ff` / `0xffffffff` was a stale analysis interpretation, not a source initializer, sentinel, relocation, or pre-CRT value.
- Omitted initialization is the least-assumption late-1999 through mid-2000s source shape and matches the current binary. Explicit `NULL` or `0` is behaviorally equivalent but not token-level proven; `nullptr` is era-inconsistent; `reinterpret_cast`, raw-address binding, section pragmas, and all-ones initialization are rejected.
- Target VA operand bytes `10 AE 69 00` occur exactly 13 times at the live refs. Target RVA bytes `10 AE 29 00` occur zero times. No hidden pointer table, raw initializer route, or alternate storage exists.

## Type, Name, And One-Definition Decision

- `FrameMgr *` is binary-proven by the 24-byte EventDispatcher allocation, FrameMgr constructor call, FrameMgr vtable/RTTI, direct `Singleton<FrameMgr>` base, and every method receiver. `FrameHandler *`, `LObject *`, `void *`, integer, and opaque-handle alternatives lose direct identity and are rejected.
- `g_frameRegistry` remains the best source-facing descriptive spelling because the full family inserts, removes, unregisters, and dispatches frame callbacks. Current IDA has no target entity/name row and disassembly uses `unk_69AE10`; `dword_69AE10` is raw provenance. `g_pIdleDispatchQueue` is a historical caller-context alias that is too narrow.
- External linkage is required by the accepted source model: FrameMgr.cpp owns the definition while EventDispatcher/application source consumes it. File-local `static` would require an unobserved accessor/inlining contract.
- UID0000Q0 carries the forward/extern declaration; this page carries the only definition. Do not emit duplicate global storage, a byte array, a template-static duplicate, explicit constructor/destructor assignments, or raw address syntax.

## Evidence

- Current MCP confirms exactly 13 direct refs:
  - `0x004a6bfa`: raw EventDispatcher cleanup helper reads the pointer, null-checks it, and calls virtual slot `0` with flag `1`; IDA does not currently model the helper as a function.
  - `0x004a6cc9`: [UID:000142][0x004a6c40-0x004a6cda.ApplicationIdleWorkScheduler](by-memory/0x004a6c40-0x004a6cda.ApplicationIdleWorkScheduler.md) reads the singleton before dispatching elapsed 20-tick frame slices.
  - `0x004a7e0c`: modeled EventDispatcher scalar cleanup reads the singleton and closes it through virtual slot `0` with flag `1`.
  - `0x004b6cd9`: `FrameHandler` destructor reads the registry and unregisters the handler through `FrameMgr::RemoveFrame`.
  - `0x004b6d02`: `FrameHandler::ScheduleNextFrame` reads the registry and calls `FrameMgr::Insert` for `currentFrame + 1`.
  - `0x004b6d21`: `FrameHandler::RemoveFrame` wrapper reads the registry and calls `FrameMgr::RemoveFrame`.
  - `0x004b6d35`: delayed schedule wrapper reads the registry and calls `FrameMgr::Insert` for `currentFrame + delay`.
  - `0x004b6d7b`: `FrameHandler` scalar deleting destructor reads the registry during unregister-before-delete.
  - `0x004b6e03`: direct `Singleton<FrameMgr>` base construction publishes the complete object; `0x004b6e0a` is the compiler's adjusted-null fallback.
  - `0x004b6e83`: unmodeled ordinary FrameMgr cleanup/destructor island clears during base/member teardown after vtable restore and list cleanup.
  - `0x004b70b0`: one-instruction constructor-EH Singleton-base destructor target clears the storage.
  - `0x004b70d6`: FrameMgr scalar deleting wrapper clears while lowering the ordinary source destructor plus optional delete.
- RTTI proves direct `LObject` and `Singleton<FrameMgr>` bases. The Singleton base descriptor has PMD `mdisp=4`, `pdisp=-1`, `vdisp=0`; under MSVC EBO it overlaps the first 32-bit FrameMgr field at `+0x04`.
- Constructor `0x004b6dc0` builds LObject, derives `this+4`, publishes the complete object by subtracting 4, installs the FrameMgr vtable, creates list state at `+0x10`, and zeroes two counters plus a dispatch byte.
- The constructor EH funclet at `0x005ff128` loads the complete object, adds 4, and jumps at `0x005ff12e` to `0x004b70b0`. That sole code xref resolves the target as base-destructor cleanup, not a named public source helper.
- Relevant object shape is exactly `0x18`: primary LObject/vptr at `+0x00`, empty Singleton PMD and first counter at `+0x04`, second counter at `+0x08`, dispatch byte at `+0x0c`, natural padding `+0x0d-+0x0f`, and two-dword list state at `+0x10-+0x17`.
- Raw ordinary cleanup and scalar deleting wrapper are compiler realizations of one human destructor. Human C++ must not contain vptr stores, explicit base calls, scalar flags, storage deletion, SEH/cookie code, or direct global clear statements.
- FrameHandler wrappers at `0x004b6d00`, `0x004b6d20`, and `0x004b6d30` call only FrameMgr insert/remove behavior through this pointer. Application idle work computes 20-tick slices and calls due-frame dispatch. Broad consumers prove liveness, not alternate ownership.

## Range, Boundaries, And Negative Evidence

- Predecessor [UID:0001PE][0x0069ae0c-0x0069ae10.g_activeUserStatusPane](by-memory/0x0069ae0c-0x0069ae10.g_activeUserStatusPane.md) has 67 refs and ends exactly at the target start. It remains separate.
- Successor UID0002AU begins exactly at `0x0069ae14`, has no name or ref, and is current zero-filled linker/compiler spacing before [UID:0002AV][0x0069ae18-0x0069af1c.EventDispatcherKeyboardStateGlobals](by-memory/0x0069ae18-0x0069af1c.EventDispatcherKeyboardStateGlobals.md). It is not part of this definition.
- No internal padding, split, relocation, dynamic initializer, sentinel branch, `-1` comparison, target-RVA occurrence, competing writer, string/resource, or raw source table exists.
- FrameChrome, TimerMgr, EventDispatcher/Application, ScreenPane, WorldMapPane, Boost, and LinkedList are rejected owners: they are consumers, separate timing/art systems, caller-biased embeddings, polluted labels, or generic STL support.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | Exact storage/hash/PE state, all 13 refs, type/name/linkage/initializer, direct Singleton source cause, ordinary/scalar/unwind decomposition, owner/source route, one-definition policy, formal R2, boundaries, and negative evidence are complete. |
| Confidence | 93 | MCP bytes/xrefs/instructions, PE mapping, allocation, vtable, and RTTI strongly prove behavior and type. Missing original symbols/source cap lexical name and initializer-token certainty. |

## Cross-References

- [UID:0000Q0][g_frameRegistry](by-global/g_frameRegistry.md)
- [UID:0000JM][FrameMgr](by-file/FrameMgr.md)
- [UID:00005H][FrameMgr](by-class/FrameMgr.md)
- [UID:00015M][0x004b6cb0-0x004b7112.FrameScheduler](by-memory/0x004b6cb0-0x004b7112.FrameScheduler.md)
- [UID:000142][0x004a6c40-0x004a6cda.ApplicationIdleWorkScheduler](by-memory/0x004a6c40-0x004a6cda.ApplicationIdleWorkScheduler.md)

## Changes

- 2026-06-05: Raised the page from `70/85` with blank reconstructable metadata to `86/90`, set `RECONSTRUCTABLE:TRUE`, attached it to [UID:0000JM][FrameMgr](by-file/FrameMgr.md), and removed the below-threshold final-source-style snippet from the Storage section. Evidence: live IDA MCP confirms the exact four-byte `.data` item, initial bytes, all 13 direct xrefs, constructor publish/fallback clear at `0x004b6e03`/`0x004b6e0a`, cleanup/destructor clears at `0x004b6e83`, `0x004b70b0`, and `0x004b70d6`, frame-handler wrapper reads at `0x004b6cd9`, `0x004b6d02`, `0x004b6d21`, `0x004b6d35`, and `0x004b6d7b`, and application/EventDispatcher reader paths at `0x004a6bfa`, `0x004a6cc9`, and `0x004a7e0c`.
- 2026-07-14 B003 source-quality callback:
  - Raised `86/90` to `92/93`; retained owner/emitter UID0000JM, reconstructable true, blank optional position, `Nested:0`, exact range, and no-split state; installed exact R2 sole definition.
  - Fresh healthy MCP and independent PE section mapping supersede only the historical `ff ff ff ff` / `0xffffffff` value. The page now records loader-zero-filled virtual `.data`, exact four-byte hash, all 13 refs, `FrameMgr *` type, inferred `g_frameRegistry` name, external linkage, omitted initializer, and UID0000Q0 declaration carrier.
  - Added direct `Singleton<FrameMgr>` PMD/EBO publication and ordinary/scalar/unwind cleanup source cause, exact EH funclet proof for `0x004b70b0`, 0x18 relevant layout, one-definition/generated policy, boundaries, rejected aliases/types/owners/initializers, and source/compiler exclusions.

## 2026-08-24 B001 UID0000JM Accepted Callback

- Preserved `92/93`, exact range/storage/type, owner/emitter UID0000JM, reconstructable state, `Nested:0`, omitted initializer, and one-definition role; assigned contributor position `20`.
- Expanded the formal CPP from the sole `FrameMgr *g_frameRegistry;` definition to include exact `Singleton<FrameMgr>` constructor/destructor specializations. The constructor publishes the complete FrameMgr object and the destructor clears the pointer; the H channel remains blank because UID0000Q0 supplies the extern declaration.
- RTTI PMD `+4`, constructor publication at `0x004b6e03` with adjusted-null fallback at `0x004b6e0a`, and ordinary/EH/scalar clears at `0x004b6e83`, `0x004b70b0`, and `0x004b70d6` are compiler manifestations of those specializations and implicit object teardown. Human source still excludes vptr stores, explicit base calls, scalar flags, raw deletes, EH/cookie code, and duplicate assignments.
- All thirteen references, virtual-only `.data` zero-fill, exact four-byte hash, adjacent UID0002AU exclusion, rejected initializers/aliases/owners, and lexical confidence cap remain unchanged.
