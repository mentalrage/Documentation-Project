*** UID:000558 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ArgumentedItemConfirmInputDialogPane::OnControlCommand(
    int controlIndex,
    int notifyCode)
{
    (void)notifyCode;

    if (controlIndex == kConfirmControlIndex) {
        if (m_menuContext == NULL)
            return;

        wchar_t priceText[128];
        int enteredPrice = 0;
        TextEditControlPane *priceEdit =
            GetChild<TextEditControlPane>(kTextEditControlIndex);

        priceEdit->ReadText(priceText, 128);
        swscanf(priceText, L"%d", &enteredPrice);

        if (static_cast<unsigned int>(enteredPrice) != m_expectedPrice) {
            new AlertPane(L"Price is different.", this, L"OK", NULL);
            return;
        }

        if (m_maxQuantity <= 1) {
            m_menuContext->SendArgumentedItemQuantityPacket(m_argumentId, 1);
        } else {
            new ArgumentedItemQuantityInputDialogPane(
                g_pLanguageMan->GetLocalizedString(kQuantityPromptStringId),
                m_menuContext,
                m_argumentId);
        }

        SlideCloseVertical();
        CloseDialog();
        return;
    }

    if (controlIndex == kCancelControlIndex) {
        SlideCloseVertical();
        CloseDialog();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact price-confirm handler with mismatch no-close alert, one-item send or quantity-dialog branch, and post-success close ordering. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005203c0-0x00520539 ArgumentedItemConfirmInputDialogPane OnControlCommand

## Status

- Disposition: exact source-emitting virtual action child of [UID:00000F][ArgumentedItemConfirmInputDialogPane](by-class/ArgumentedItemConfirmInputDialogPane.md).
- Exact half-open range: `[0x005203c0,0x00520539)`, size `0x179`; direct owner/emitter UID00000F, source position 20.
- Source unit: [UID:0000HH][ArgumentedItemInputDialogs](by-file/ArgumentedItemInputDialogs.md).
- Score: `93/94`; reconstructable.

## Runtime Behavior

- Implements the exact `DialogPane` primary-vtable `+0x48` `OnControlCommand(int,int)` override.
- Confirm control `1` returns immediately without closing when the copied list context is null.
- Otherwise it reads 128 wide characters from child `4`, initializes the parsed integer to zero, and parses `%d`.
- A dword mismatch against `m_expectedPrice` shows `Price is different.` and returns without closing.
- On a match, `m_maxQuantity <= 1` sends quantity one through the copied list context. Larger maxima open a new quantity dialog using localized string id `246`, the same copied context, and the argument id.
- Successful match then slides/closes; cancel control `2` also slides/closes. Other controls do nothing.

## Binary And IDA Evidence

- Fresh 2026-08-17 read-only MCP: modeled `sub_5203C0`, size `0x179`, persisted presentation `void __userpurge(int *@<ecx>, unsigned int@<ebx>, __int16 *@<edi>, int, int)`.
- Direct virtual route is data xref `0x0061f66c` in the confirm primary vtable.
- Calls `0x005204ec -> 0x0051fc90` for the quantity dialog and `0x00520501 -> 0x0051f640` for the one-item packet route; reads LanguageMan id `246` and constructs `AlertPane` on mismatch.
- Complete frame has 128-wide logical text storage beginning at `Buffer@+0x1c`, two cleanup blocks, cleanup rows at `+0x11c/+0x120/+0x128`, saved/return rows, and two stack args at `+0x134/+0x138`.
- `Price is different.` is the owned UTF-16 literal at `0x0061fa10`; `%d`, `OK`, and localized prompt text remain shared dependencies.

## Boundary And Ownership

- `[0x005203be,0x005203c0)` and `[0x00520539,0x00520540)` are alignment and excluded; `0x00520540` begins unrelated `ObjectImageControlPane` code.
- `SendArgumentedItemQuantityPacket` remains UID0003VL/UID00000I source. Quantity constructor source is [UID:000554][0x0051fc90-0x0051ff6c.ArgumentedItemQuantityInputDialogPaneConstructor](by-memory/0x0051fc90-0x0051ff6c.ArgumentedItemQuantityInputDialogPaneConstructor.md).

## Formal C++

```cpp
void ArgumentedItemConfirmInputDialogPane::OnControlCommand(
    int controlIndex,
    int notifyCode)
{
    (void)notifyCode;

    if (controlIndex == kConfirmControlIndex) {
        if (m_menuContext == NULL)
            return;

        wchar_t priceText[128];
        int enteredPrice = 0;
        TextEditControlPane *priceEdit =
            GetChild<TextEditControlPane>(kTextEditControlIndex);

        priceEdit->ReadText(priceText, 128);
        swscanf(priceText, L"%d", &enteredPrice);

        if (static_cast<unsigned int>(enteredPrice) != m_expectedPrice) {
            new AlertPane(L"Price is different.", this, L"OK", NULL);
            return;
        }

        if (m_maxQuantity <= 1) {
            m_menuContext->SendArgumentedItemQuantityPacket(m_argumentId, 1);
        } else {
            new ArgumentedItemQuantityInputDialogPane(
                g_pLanguageMan->GetLocalizedString(kQuantityPromptStringId),
                m_menuContext,
                m_argumentId);
        }

        SlideCloseVertical();
        CloseDialog();
        return;
    }

    if (controlIndex == kCancelControlIndex) {
        SlideCloseVertical();
        CloseDialog();
    }
}
```

## Historical Assumptions And Corrections

- Stale `OnAction`/userpurge/decompiler parameter forms are superseded by the current virtual `OnControlCommand(int,int)` source contract.
- Mismatch intentionally leaves the dialog open; match closes only after send/new-quantity action. Reordering or unconditional close would not reproduce the original.

## Cross-References

- [UID:00000F][ArgumentedItemConfirmInputDialogPane](by-class/ArgumentedItemConfirmInputDialogPane.md)
- [UID:0003VL][0x0051f640-0x0051f706.ArgumentedMenuSendItemQuantityPacket](by-memory/0x0051f640-0x0051f706.ArgumentedMenuSendItemQuantityPacket.md)
- [UID:00055B][0x0061f99c-0x0061fa3c.ArgumentedItemInputDialogStrings](by-memory/0x0061f99c-0x0061fa3c.ArgumentedItemInputDialogStrings.md)
