*** UID:00046S | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003B | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003B | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool CreateUserDialogPane::HandlePacketEvent(Event *event)
{
    const unsigned char *packet = event->m_payload.m_packet.m_data;

    if (packet[0] != 0x02)
    {
        return false;
    }

    return HandleCreateUserReply(packet);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 38-byte CreateUserDialogPane EventHandler-facet HandlePacketEvent(Event *) override: reads Event packet data at +0x0c, accepts opcode 0x02 without null guards, restores complete this by -0xa0, tail-dispatches HandleCreateUserReply, and returns false for other opcodes. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0052ecd0-0x0052ecf6 CreateUserDialogPane HandlePacketEvent

## Status

- Exact half-open range: `[0x0052ecd0,0x0052ecf6)`, size `0x26` / 38 bytes.
- Owner/emitter: [UID:00003B][CreateUserDialogPane](by-class/CreateUserDialogPane.md); direct non-emitting parent [UID:0002QT][0x0052dd30-0x0052f710.CreateUserDialogPaneCore](by-memory/0x0052dd30-0x0052f710.CreateUserDialogPaneCore.md); source root [UID:0000IJ][CreateUserDialogPane](by-file/CreateUserDialogPane.md).
- Rebuild handling: source-authored `EventHandler` secondary-facet `HandlePacketEvent(Event *)` override. Formal CPP is the ordinary complete-object member source; formal H is blank because UID00003B owns the virtual declaration.

## Evidence And Exact Behavior

2026-07-06 B015 MCP evidence uses session `9600ec5b` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned `ok` with auto-analysis, Hex-Rays, and strings cache ready. `lookup_funcs` identifies `0x0052ecd0` as `sub_52ECD0` size `0x26`; `0x0052ecf6` is not a function, has zero xrefs, and `get_bytes` shows ten `0xcc` padding bytes through the next modeled function at `0x0052ed00`.

The target is byte-identical to UID00043X and has byte SHA256 `B8CDCBD9D1881904B8BBDFD194B737C15FFEAA73A59F52B250E14D1C5803DB4B`. Its four basic blocks load `event->m_payload.m_packet.m_data` from Event object offset `+0x0c`, compare `packet[0]` to marker/opcode `0x02`, return `false` on mismatch through `xor al, al; retn 4`, and on acceptance rewrite the stack argument to the packet pointer, apply the secondary-vtable receiver adjustment `ecx -= 0xa0`, then tail-jump at `0x0052ecf1` to `0x0052f390` / `HandleCreateUserReply`. The tail path preserves that method's Boolean return value.

`xrefs_to` shows the target is reached from data slot `0x0061feac` in the `CreateUserDialogPane` secondary vtable area; `xrefs_to 0x0052f390` shows the sole code route from this bridge. `get_bytes 0x0061fe9c` and the class/core support docs tie this slot group to constructor/fallback/destructor vtable refs for `CreateUserDialogPane`, so ownership remains [UID:00003B][CreateUserDialogPane](by-class/CreateUserDialogPane.md) and not Event/EventDispatcher or a sibling create-user variant.

The reply handler at `0x0052f390` decodes result/status from `packet[1]`, message length from `packet[2]`, and message bytes from `packet + 3`; therefore this bridge's `packet[0] == 0x02` test is an opcode/marker gate, not the reply result byte. Current [UID:00004L][Event](by-class/Event.md) places packet data at `Event +0x0c`, and [UID:00004N][EventHandler](by-class/EventHandler.md) names secondary slot `+0x10` `HandlePacketEvent(Event *)`. The byte-identical accepted UID0004PD and UID00043X precedents independently corroborate this source contract. There is no event or packet null guard, packet-kind read, packet-size read, copy, ownership transfer, or payload mutation.

## Receiver, Boundary, And Source Contract

- The raw function is entered with ECX pointing at the `EventHandler` facet at complete-object `+0xa0`. The binary-facing IDA declaration must therefore use `void *eventHandlerFacet`; it must not claim raw ECX is a complete `CreateUserDialogPane *`. Compiler `-0xa0` adjustment is not written in human source.
- The predecessor UID00046R ends before the target. The target's exclusive endpoint `0x0052ecf6` has zero xrefs and is followed by ten `0xcc` bytes through the next modeled function at `0x0052ed00`; no split at the internal compare or tail block is warranted.
- Source emits `bool CreateUserDialogPane::HandlePacketEvent(Event *event)` and derives `const unsigned char *packet` by direct assignment. It does not synthesize `PacketEvent`, expose raw offsets, add a null guard, or duplicate the declaration outside UID00003B H.

## Current IDA Prestate And Supervisor Handoff

This callback did not mutate IDA. The exact current modeled item is `[0x0052ecd0,0x0052ecf6)`, size `0x26`, named `CreateUserDialogPane_OnCreateUserReplyEvent`, declaration `char __thiscall(void *this, int)`, with frame `__saved_registers +0x0/4 _DWORD`, `__return_address +0x4/4 _UNKNOWN *`, `arg_0 +0x8/4 _DWORD`, and no stable locals. Address-regular and address-repeatable comments are absent. Function-regular comment is exactly `Create-user PacketEvent bridge: accepts payload marker 0x02 and tail-dispatches HandleCreateUserReply.` Function-repeatable comment is absent.

Supervisor Gate 2B owns the pending atomic actions: rename to `CreateUserDialogPane__HandlePacketEvent`; apply `bool __thiscall CreateUserDialogPane__HandlePacketEvent(void *eventHandlerFacet, Event *event)`; clear the stale function-regular comment; and insert function-repeatable comment exactly `CreateUserDialogPane EventHandler-facet HandlePacketEvent override. Reads Event packet data, accepts opcode 0x02, restores complete this by -0xA0, and tail-dispatches HandleCreateUserReply; other opcodes return false; packet[1] is the reply result byte.` Expected automatic argument presentation is `event +0x8/4 Event *` with saved-register/return entries retained; a semantically identical automatic argument label at offset `+0x8`, width `4`, type `Event *` is a bounded presentation variant and must be recorded rather than coerced. Bytes, bounds, xrefs, vtable data, all address comments, and ordinal-592 `CreateUserDialogPane` remain protected.

## Historical And Rejected Interpretations

Executed B015 research correctly established the exact range, four-block behavior, sole `0x0061feac` vtable xref, `-0xa0` receiver adjustment, no-null negative evidence, UID00003B owner/emitter route, UID0002QT parent, and endpoint padding. Its `OnCreateUserReplyEvent(const PacketEvent *)` / `event->payload` source inference followed then-current support precedent but is superseded by the resolved EventHandler slot, Event UDT, and byte-identical accepted handlers. That prior interpretation remains historical evidence rather than current source.

Rejected alternatives: raw `event + 12` arithmetic, raw `this - 0xa0`, a complete-class raw ECX type, a null guard, blank/no-code treatment, a split at `0x0052ecf6`, a custom opcode-specific virtual name, or ownership under Event/EventDispatcher, the family aggregate, or a sibling dialog.

## Score Rationale

- Completion `90`: exact range, bytes/hash, four-block CFG, predecessor/successor fences, vtable route, Event layout, opcode/result distinction, receiver adjustment, tail return, formal CPP/H placement, ordinary owner/parent/file route, literal IDA handoff, and superseded-history disposition are documented.
- Confidence `94`: direct binary/vtable evidence plus Event/EventHandler types and two byte-identical accepted precedents converge on the ABI and human source contract. Remaining uncertainty is limited to stripped original lexical spelling and does not justify raw names or a weaker score.

## Changes

- 2026-07-29 B004 UID00043X support implementation callback: renamed the ordinary page and formal source to `HandlePacketEvent(Event *)`; raised `89/91 -> 90/94`; preserved all valid B015 binary/owner/split evidence; added exact hash/CFG/boundaries, Event payload, no-null, opcode/result, raw-facet ABI, tail-return, literal IDA prestate/handoff, and historicalized only the disproved `PacketEvent`/custom-name inference. IDA remained unchanged for supervisor Gate 2B.
