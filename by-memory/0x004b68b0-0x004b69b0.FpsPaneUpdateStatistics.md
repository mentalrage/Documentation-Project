*** UID:00015H | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00005F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00005F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool FpsPane::OnTimer(int, int, int)
{
    const float fps = static_cast<float>(m_frameCount);
    m_frameCount = 0;
    m_currentFps = fps;

    if (fps < m_minFps)
        m_minFps = fps;

    if (m_maxFps < fps)
        m_maxFps = fps;

    ++m_sampleCount;
    m_totalFps += fps;

    if (g_activeMapPane != 0) {
        MapRect visibleTiles;
        g_activeMapPane->GetClampedVisibleTileBounds(&visibleTiles);

        ObjectList *objectList = g_activeMapPane->m_objectList;
        for (int row = visibleTiles.top; row < visibleTiles.bottom; ++row) {
            m_totalLivingObjects += objectList->GetFrontRowBucket(row)->GetCount();
            m_totalStaticObjects += objectList->GetBackRowBucket(row)->GetCount();
        }

        m_totalBalloonObjects += objectList->GetVisibleObjectList()->GetCount();
    }

    ScheduleTimer(0, 1000, 0, 0);
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact FpsPane::OnTimer(int,int,int) override at [0x004b68b0,0x004b69b0), reached only through TimerHandler-view slot 0x0061a6a0, with normalized owner offsets +0xf8/+0x100/+0x14c..+0x168, FPS and MapPane/ObjectList count accumulation, inherited ScheduleTimer(0,1000,0,0), strict raw-helper successor boundary, and UpdateStatistics retained only as the historical documentation slug. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***


# 0x004b68b0-0x004b69b0 FpsPane OnTimer

## Status

- Disposition: reconstructable NexusTK project code; source-bearing virtual timer/update callback.
- Accepted source-facing method: `bool FpsPane::OnTimer(int, int, int)`; `UpdateStatistics` is the historical documentation alias.
- IDA function: `sub_4B68B0`, exact half-open range `0x004b68b0-0x004b69b0`, size `0x100`.
- Owner: [UID:00005F][FpsPane](by-class/FpsPane.md).
- Source module: [UID:0000JK][FpsPane](by-file/FpsPane.md), `NexusTK/ui/diagnostics/FpsPane.cpp`.
- Layout docs: [UID:0001UO][FpsPaneLayout](by-type/by-struct/FpsPaneLayout.md).
- Vtable route: tertiary FpsPane timer/update vtable base `0x0061a69c`, slot `+0x04` at `0x0061a6a0`, value `0x004b68b0`.
- Current code-entry conclusion: this page clears the active combined-score/emitter reconstruction-code gate. The old 95/95 blank-code blocker is stale and superseded by the active rule for `RECONSTRUCTABLE:TRUE` items with valid emitter route and a resolved source-quality pass.
- Remaining score caps: exact original lexemes, exact `MapRect` spelling, final ObjectList row-tier helper names, and unused callback parameter names remain descriptive rather than symbol-proven.

## Behavior

`FpsPane::OnTimer` samples the one-second frame count produced by [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md), resets that counter, updates current/min/max/average FPS session fields, reads visible map tile bounds, totals front/back/visible object counts through `MapPane` and `ObjectList`, then schedules the next one-second callback.

Exact behavior order:

1. Read `m_frameCount` at owner offset `+0xf8` through timer-facet offset `+0x54`.
2. Reset `m_frameCount` to zero.
3. Convert the sampled count to a float and write `m_currentFps` at owner `+0x100`.
4. Update `m_minFps` at `+0x14c` if the sample is lower.
5. Update `m_maxFps` at `+0x150` if the sample is higher.
6. Increment `m_sampleCount` at `+0x154`.
7. Add the sample to `m_totalFps` at `+0x158`.
8. If [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md) is non-null, fetch visible tile bounds, walk visible rows, and add ObjectList counts to the running object totals at `+0x160`, `+0x164`, and `+0x168`.
9. Reschedule the timer/update facet for another 1000 ms callback through `0x005975e0`.
10. Return true through `AL = 1` and `ret 0x0c`.

## First-Draft C++ Notes

The formal C++ block is intentionally source-facing rather than decompiler-shaped. The binary receiver is the embedded timer/update subobject at `FpsPane +0xa4`; the draft expresses the normalized owner object fields that source code would have used.

The accepted call is inherited `TimerHandler::ScheduleTimer(0, 1000, 0, 0)`. If `MapPane` later exposes an accessor instead of direct `m_objectList`, adapt only that access; if the returned ObjectList bucket/list type has a different accepted count accessor, adapt `GetCount()` without changing the proven count dword at returned object `+0x0c`.

The three callback arguments are binary-proven by `ret 0x0c` but unused in this body. They should stay unnamed until a later TimerHandler interface pass recovers exact parameter semantics.

## 2026-06-19 B013 Source-Quality Reanalysis

B013 rechecked the root-level report for this target and confirmed the current by-* documentation already carries the intended source-quality resolution: [UID:00015H] remains the exact source-bearing `FpsPane::UpdateStatistics` timer/statistics callback owned by [UID:00005F][FpsPane](by-class/FpsPane.md) and emitted through [UID:0000JK][FpsPane](by-file/FpsPane.md) to `NexusTK/ui/diagnostics/FpsPane.cpp`. The report does not require a split, owner change, source-file move, or no-code downgrade.

The accepted source shape remains `bool FpsPane::UpdateStatistics(int, int, int)`. The binary receiver is the `TimerHandler` facet at `FpsPane +0xa4`, so raw offsets in the body are normalized to owner fields. The three callback payloads are proven by `ret 0x0c` but unused, so the first draft deliberately leaves them unnamed instead of inventing parameter semantics. Generic `FpsPane::OnTimer` naming remains plausible as an interface spelling, but `UpdateStatistics` is the best current behavior-specific source name already used by the target, class, and generated route.

B013 reconfirms the stale old blocker: this page should not keep blank C++ merely because it is below an obsolete `95/95` threshold. The target is `RECONSTRUCTABLE:TRUE`, has a nonblank class emitter route, clears the active combined-score/code-entry gate, and has a supported source-facing body. The remaining ObjectList row-tier method names, `MapRect` spelling, local timer-wrapper name, and unused callback payload names are confidence caps rather than first-draft C++ blockers.

Dependency ownership is unchanged. `dword_67A764` is [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md); `0x00505290` is `MapPane::GetClampedVisibleTileBounds(MapRect *outBounds) const`; `0x00532670`, `0x005326d0`, and `0x00532610` are the current descriptive ObjectList front-row, back-row, and visible-list count providers; and `0x005975e0` is the generic timer schedule wrapper locally expressed as `m_updateTimer.StartTimer(0, 1000, 0, 0)`. MapPane, ObjectList, TimerHandler/TimerMgr, MainUiGraph, ParcelPane, raw `sub_*` labels, and stale caller-biased `FittingRoomDialog::meth_0x5975e0` names remain rejected as source owners for this method.

## Local PE / Capstone Evidence

Checked binary:

- Path: `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`
- Size: `2679296`
- MD5: `4247e04e20b65d6414c7238aa8ff5515`
- SHA256: `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`
- Image base: `0x00400000`
- Parsed sections: `.text 0x00401000-0x0060c600`, `.rdata 0x0060d000-0x0066c200`, `.data 0x0066d000-0x0069ce24`, `.rsrc 0x0069d000-0x006b2e00`
- File offset for `0x004b68b0`: `0x000b5cb0` in `.text`.
- Capstone version: `5.0.7`.
- PE section table was parsed directly because `pefile` was not installed.

Exact boundary bytes:

```text
0x004b68a0-0x004b68b0:
33 cd e8 88 0e 11 00 8b e5 5d c3 cc cc cc cc cc

0x004b69b0-0x004b69d0:
55 8b ec 51 57 8b f9 ff 15 0c d5 60 00 83 3d 90 da 66 00 00 0f 57 c0 89 87 48 01 00 00 c7 87 4c
```

The predecessor bytes preserve [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) return sequence at `0x004b68a0-0x004b68aa` and five `0xcc` alignment bytes at `0x004b68ab-0x004b68b0`. No trailing padding follows this body: `0x004b69b0` begins the raw/unmodeled source-facing start-session helper [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md).

Route scan:

- Local `.text` branch scan found zero direct `call`, `jmp`, conditional-branch, or short-branch refs to `0x004b68b0`.
- The same branch scan found zero branch refs to the exclusive end `0x004b69b0`.
- Four-byte literal scan found exactly one absolute `0x004b68b0` pointer: `0x0061a6a0` in `.rdata`.
- No target RVA (`0x000b68b0`), target raw offset (`0x000b5cb0`), exclusive-end VA (`0x004b69b0`), exclusive-end RVA (`0x000b69b0`), or exclusive-end raw offset (`0x000b5db0`) literal hits were found.
- This matches timer/update-vtable dispatch rather than direct call dispatch. Absence of direct callers is expected for this callback route.

## Disassembly-Level Body Evidence

Local Capstone disassembly confirms a normal `__thiscall` callback body with a security-cookie prologue, three stack callback arguments popped by `ret 0x0c`, and receiver in `ECX` saved to `ESI`.

Key instructions:

- `0x004b68c3`: reads timer-facet `+0x54`, owner `+0xf8`, as the sampled frame count.
- `0x004b68c6`: clears timer-facet `+0x54`, owner `+0xf8`, after sampling.
- `0x004b68cd-0x004b68e1`: converts the sampled count to floating point through the MSVC signed/unsigned conversion idiom with correction table `0x0060dfc0`.
- `0x004b68e5`: loads timer `+0xa8`, owner `+0x14c`, current `m_minFps`.
- `0x004b68f0`: writes the current sample to timer `+0x5c`, owner `+0x100`, `m_currentFps`.
- `0x004b68f5-0x004b68f7`: updates timer `+0xa8`, owner `+0x14c`, when the new sample is below `m_minFps`.
- `0x004b68ff-0x004b6908`: updates timer `+0xac`, owner `+0x150`, when the new sample is above `m_maxFps`.
- `0x004b6910`: increments timer `+0xb0`, owner `+0x154`, `m_sampleCount`.
- `0x004b6916-0x004b6921`: promotes the sample to double and adds it to timer `+0xb4`, owner `+0x158`, `m_totalFps`.
- `0x004b6929-0x004b6931`: loads and null-checks `dword_67A764`, best project name `g_activeMapPane`.
- `0x004b6935-0x004b6939`: passes a local four-int rectangle to `0x00505290`, best current name `MapPane::GetClampedVisibleTileBounds(MapRect *outBounds) const`.
- `0x004b693e-0x004b6946`: reloads `g_activeMapPane`, loads local `top` from `[ebp-0x10]`, and reads `MapPane +0x424` as `m_objectList`.
- `0x004b694c-0x004b6977`: loops while local `top < bottom`, where bottom is `[ebp-0x08]`.
- `0x004b6954`: calls `0x00532670`, current descriptive name `ObjectList::GetFrontRowBucket(row)`.
- `0x004b695c-0x004b695f`: reads returned list count at `+0x0c` and adds it to timer `+0xbc`, owner `+0x160`, `m_totalLivingObjects`.
- `0x004b6965`: calls `0x005326d0`, current descriptive name `ObjectList::GetBackRowBucket(row)`.
- `0x004b696b-0x004b696e`: reads returned list count at `+0x0c` and adds it to timer `+0xc4`, owner `+0x168`, `m_totalStaticObjects`.
- `0x004b697b`: calls `0x00532610`, current descriptive name `ObjectList::GetVisibleObjectList()`.
- `0x004b6982-0x004b6985`: reads returned list count at `+0x0c` and adds it to timer `+0xc0`, owner `+0x164`, `m_totalBalloonObjects`.
- `0x004b698b-0x004b6998`: pushes `(0, 0, 1000, 0)`, sets `ECX = ESI`, and calls `0x005975e0`.
- `0x004b69a0`: returns `AL = 1`.
- `0x004b69a5`: calls security-cookie check `0x005c772f`.
- `0x004b69ad`: `ret 0x0c`, confirming three explicit stack callback arguments.

Direct call list:

| Callsite | Target | Source-quality role |
| --- | --- | --- |
| `0x004b6939` | `0x00505290` | `MapPane::GetClampedVisibleTileBounds(MapRect *outBounds) const` |
| `0x004b6954` | `0x00532670` | `ObjectList::GetFrontRowBucket(row)` |
| `0x004b6965` | `0x005326d0` | `ObjectList::GetBackRowBucket(row)` |
| `0x004b697b` | `0x00532610` | `ObjectList::GetVisibleObjectList()` |
| `0x004b6998` | `0x005975e0` | `TimerHandler::ScheduleTimer` / local FpsPane timer start wrapper |
| `0x004b69a5` | `0x005c772f` | MSVC security-cookie check |

There are no internal switch tables, embedded data islands, thunk jumps, destructor/delete behaviors, or split child bodies in this method.

## Timer-Facet Offset Normalization

The binary receiver is the embedded timer/update facet at `FpsPane +0xa4`. Raw offsets in the body normalize by adding `0xa4` to recover owner fields.

| Timer-facet offset | Owner offset | Recommended field/type | Evidence |
| --- | --- | --- | --- |
| `+0x54` | `+0xf8` | `int`/`uint32_t m_frameCount` | [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) increments owner `+0xf8`; this method reads and clears timer `+0x54` once per callback. |
| `+0x5c` | `+0x100` | `float m_currentFps` | This method writes timer `+0x5c`; [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) reads owner `+0x100` and formats it as `L"%5.1f FPS"`. |
| `+0xa8` | `+0x14c` | `float m_minFps` | Compared against the new sample and lowered when the sample is smaller; start-session paths initialize it to `300.0f`; summary writes `minFPS`. |
| `+0xac` | `+0x150` | `float m_maxFps` | Compared against the new sample and raised when the sample is larger; start-session paths initialize it to `0.0f`; summary writes `maxFPS`. |
| `+0xb0` | `+0x154` | `int m_sampleCount` | Incremented once per statistics callback; start-session paths initialize it to `1`; summary divides accumulated totals by it. |
| `+0xb4` | `+0x158` | `double m_totalFps` | Accumulates each sample as a double; summary divides by `m_sampleCount` for average FPS. |
| `+0xbc` | `+0x160` | `int m_totalLivingObjects` | Adds count from `GetFrontRowBucket(row)` for each visible row; summary writes `aveLivingObject`. |
| `+0xc0` | `+0x164` | `int m_totalBalloonObjects` | Adds count from `GetVisibleObjectList()` once per sample; summary writes `aveBalloonObject`. |
| `+0xc4` | `+0x168` | `int m_totalStaticObjects` | Adds count from `GetBackRowBucket(row)` for each visible row; summary writes `aveStaticObject`. |

Rejected field interpretations:

- `+0xf8` as elapsed time or timer id: [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) increments it once per rendered frame, and this callback samples/resets it.
- `+0x100` as average/min/max FPS: min/max and totals live at owner `+0x14c` onward; this field is the displayed current FPS value.
- `+0x160/+0x164/+0x168` as already-averaged values: [UID:00015J][0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw](by-memory/0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw.md) computes averages later by dividing by `m_sampleCount`. This target stores running totals.

## MapPane And ObjectList Diagnostic Counts

Best current mapping:

- `dword_67A764` is [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md), not `g_activeGameServerConfig`, weather state, or FpsPane-owned storage.
- `0x00505290` is best current source name `MapPane::GetClampedVisibleTileBounds(MapRect *outBounds) const`.
- The local four-int rectangle should be represented as `MapRect` or an equivalent tile-rectangle type. Current MapPane docs identify fields as left/top/right/bottom with right/bottom loop-exclusive.
- The target uses only `top` and loop-exclusive `bottom`: local `[ebp-0x10]` becomes `row`, and local `[ebp-0x08]` is the loop bound.
- `MapPane +0x424` is `m_objectList`.
- `0x00532670` is current descriptive `ObjectList::GetFrontRowBucket(row)`.
- `0x005326d0` is current descriptive `ObjectList::GetBackRowBucket(row)`.
- `0x00532610` is current descriptive `ObjectList::GetVisibleObjectList()`.
- The returned list/count object has a count dword at `+0x0c`; first-draft C++ expresses this as `GetCount()` or equivalent list-count access.

Count accumulation order:

| Source data | Accumulator | Evidence |
| --- | --- | --- |
| `GetFrontRowBucket(row)->GetCount()` | `m_totalLivingObjects` owner `+0x160` | `0x004b6954` then add to timer `+0xbc`; summary string labels this total as `aveLivingObject`. |
| `GetBackRowBucket(row)->GetCount()` | `m_totalStaticObjects` owner `+0x168` | `0x004b6965` then add to timer `+0xc4`; summary labels this total as `aveStaticObject`. |
| `GetVisibleObjectList()->GetCount()` | `m_totalBalloonObjects` owner `+0x164` | `0x004b697b` then add to timer `+0xc0`; summary labels this total as `aveBalloonObject`. |

The row-tier names `front`/`back` and object categories `living`/`static`/`balloon` are best supported by current ObjectList and FpsPane summary docs, but the exact original ObjectList method names are not symbol-proven. This is a confidence cap, not a C++ blocker, because the call targets, order, returned count field, and accumulator mapping are stable.

## FPS Timing Model

This method does not call `timeGetTime`, `GetTickCount`, `GetLocalTime`, or any Win32 timing API directly.

The source timing model is:

1. [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) `FpsPane::OnPaint` increments `m_frameCount` and displays `m_currentFps`.
2. [UID:00015H] samples `m_frameCount` once per timer callback, resets it, writes `m_currentFps`, updates min/max/sample/sum fields, and schedules another callback after 1000 ms.
3. [UID:00015F][0x004b64a0-0x004b67a7.FpsPaneUpdateFpsLogSessionRaw](by-memory/0x004b64a0-0x004b67a7.FpsPaneUpdateFpsLogSessionRaw.md), [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md), and [UID:00015J][0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw](by-memory/0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw.md) handle log-session start, time stamp, elapsed tick, and summary file output.

The MSVC `cvtdq2pd` plus correction-table conversion at `0x0060dfc0` is compiler codegen for integer-to-floating conversion. Source C++ should represent it as `static_cast<float>(m_frameCount)`.

## Timer Reschedule Helper

The final call is `sub_5975E0(this, 0, 1000, 0, 0)` with `this` still pointing to the timer/update facet.

Recommended source wording:

- In dependency prose, call `0x005975e0` `TimerHandler::ScheduleTimer` or a generic timer-handler schedule wrapper, as [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md) documents.
- In first-draft FpsPane source, `m_updateTimer.StartTimer(0, 1000, 0, 0)` is acceptable and consistent with current FpsPane sibling drafts. If later TimerHandler API cleanup standardizes on `ScheduleTimer`, only rename the wrapper call; do not change arguments or timing.

Rejected helper interpretations:

- `FittingRoomDialog::meth_0x5975e0` is stale generated owner pollution from older timer-wrapper attribution.
- The delay is `1000` ms / one second, not timer id `1000`. The first argument is event id `0`; the second is the delay/duration.
- The final two zeros are unused payload slots in this path.

## Heuristic / Inference Reanalysis And Validation

### Source-Bearing Versus No-Code

Best conclusion: [UID:00015H] is handwritten/source-authored `FpsPane::UpdateStatistics`, not generated-only timer glue.

Evidence:

- Normal modeled function body at `0x004b68b0-0x004b69b0`.
- Vtable-only dispatch through `0x0061a6a0`, exactly the FpsPane timer/update vtable slot.
- `ret 0x0c` and return value `1`, matching a timer callback interface with three stack callback parameters.
- Direct FpsPane state updates after `+0xa4` receiver normalization: `m_frameCount`, `m_currentFps`, `m_minFps`, `m_maxFps`, `m_sampleCount`, `m_totalFps`, and object-count accumulators.
- Ordinary MapPane/ObjectList read-only diagnostics dependencies and generic TimerHandler reschedule dependency.
- Exact boundary split from [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) and into raw/unmodeled [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md).

Rejected alternatives:

- Compiler-generated thunk: no `this` adjustment stub, no tail jump to another method, no vtable/dtor-only shape, and a full statistics body is present.
- Scalar deleting destructor: no delete flag, no vtable teardown, no base destructor call, no `operator delete`, and the function returns true through `ret 0x0c`.
- TimerHandler/TimerMgr-owned method: `0x005975e0` is only a generic scheduler callee. The target body owns FpsPane fields and should remain FpsPane-owned.
- MapPane/ObjectList owner: those classes provide read-only count and bounds data. The accumulation fields and vtable dispatch are FpsPane-owned.
- No-code/generated-vtable page: the vtable data is [UID:0002MJ][0x0061a61c-0x0061a6a4.FpsPaneVtableData](by-memory/0x0061a61c-0x0061a6a4.FpsPaneVtableData.md); this target is executable source behavior.
- Merge with raw start-session helper: successor `0x004b69b0` begins a separate raw helper with no padding after this method; this target ends at `ret 0x0c` at `0x004b69ad`.

### Name And Signature

Recommended source-facing signature:

```cpp
bool FpsPane::UpdateStatistics(int, int, int);
```

Evidence:

- IDA records `char __thiscall sub_4B68B0(int this, int, int, int)`.
- Local disassembly ends with `mov al, 1` and `ret 0x0c`.
- The three stack arguments are unused. Leaving them unnamed in the first draft is cleaner than inventing false parameter semantics.
- The function is reached through the timer/update vtable facet at owner offset `+0xa4`, so the source-facing class method should normalize `this` back to the containing `FpsPane` and use normal fields.

Rejected signature/name alternatives:

- `bool FpsPane::UpdateStatistics()` is too weak for the binary ABI because `ret 0x0c` proves three callback arguments.
- `char sub_4B68B0(int this, int a2, int a3, int a4)` preserves IDA artifacts rather than source-facing class code.
- `FpsPane::OnTimer` is plausible as a generic timer-interface spelling, but existing target/class docs and behavior-specific source naming support `UpdateStatistics` better. `OnTimer` should remain a low-confidence naming-sweep alternate only.
- `FpsPaneUpdateStatistics` should remain the documentation slug, not the source method spelling.

### Owner / Emitter / Source Placement

Accepted:

- Direct class owner: [UID:00005F][FpsPane](by-class/FpsPane.md).
- Source file/emitter route: [UID:0000JK][FpsPane](by-file/FpsPane.md), `NexusTK/ui/diagnostics/FpsPane.cpp`.
- Method source placement: `FpsPane.cpp`, beside `OnPaint`, `UpdateFpsLogSession`, retained start/summary helpers, and FpsPane globals.

Evidence:

- Tertiary FpsPane timer/update vtable slot at `0x0061a6a0`.
- FpsPane layout normalization ties every modified statistic field to owner offsets.
- [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md) consumes `m_currentFps` and produces `m_frameCount`, proving the pair.
- File doc already assigns the diagnostics overlay/logging module to `NexusTK/ui/diagnostics/FpsPane.cpp`.

Rejected owners:

- `MapPane`: only supplies active map pointer, visible bounds, and `m_objectList` for diagnostics.
- [UID:0001D2][0x00532530-0x0053272e.ObjectListAccessorMethods](by-memory/0x00532530-0x0053272e.ObjectListAccessorMethods.md): only supplies ObjectList row/list count helpers; `MapPaneSpatialIndex` is a historical alias for the same accessor surface.
- `TimerHandler` / `TimerMgr`: only supplies the generic scheduler wrapper.
- `MainUiGraph` / `MainUiLayerSlots`: relevant to session start/layer parentage, not this callback body.
- `ParcelPane`: stale generated metadata pollution; no state or vtable evidence ties this target to ParcelPane.
- `FpsPaneDiagnostics` aggregate page: useful inventory, but this exact method page should carry the method C++.

### Generated-Name Cleanup

The target should not preserve raw generated names as source truth:

- `sub_4B68B0` is the IDA label; source-facing method is `FpsPane::UpdateStatistics`.
- Raw helper labels such as `sub_505290`, `sub_532670`, `sub_5326D0`, `sub_532610`, and `sub_5975E0` should be kept as evidence addresses while prose and draft source use their best current source roles.
- `FittingRoomDialog::meth_0x5975e0` is caller-biased/generated pollution and should not be used for this FpsPane timer reschedule.
- ParcelPane, MapPane, ObjectList, and TimerHandler names should remain dependency or rejected-owner notes, not parent routing for this method.

## Score And Metadata Rationale

- `COMPLETION:88`: target has exact range, padding/successor boundary, vtable-only route, no direct branch refs, body call order, normalized field map, source name/signature, helper roles, relationship to OnPaint/log-summary siblings, rejected alternatives, and first-draft C++.
- `CONFIDENCE:91`: local PE/Capstone evidence, exported IDA/Ghidra records, and current support docs strongly confirm the body, boundary, dispatch route, field usage, helper order, and owner. Confidence remains below final-audit levels because some helper/API names are project-normalized source-facing names rather than recovered original spellings.
- Keep `CANONICAL_OWNER:00005F`: FpsPane owns the timer/update vtable slot, statistics fields, frame-counter/current-FPS pair, and diagnostic accumulators.
- Keep `RECONSTRUCTABLE:TRUE`: this is source-authored code with a valid C++ representation.
- Keep `EMITTER_UIDS:00005F`: class emitter route reaches [UID:0000JK][FpsPane](by-file/FpsPane.md).

## IDA Rename / Type / Comment Recommendations

No IDA DB edits were made during this Rule 26 incorporation. Recommended future IDA cleanup:

- Rename `sub_4B68B0` to `FpsPane_UpdateStatistics` or an IDA-safe equivalent of `FpsPane::UpdateStatistics`.
- Apply source-facing type `bool __thiscall FpsPane::UpdateStatistics(FpsPane *this, int, int, int)` in notes while preserving that the binary receiver is the timer/update subobject at `FpsPane +0xa4`.
- Add a function comment at `0x004b68b0`: `Timer/update callback; receiver is FpsPane+0xa4; normalize offsets by +0xa4.`
- Add or confirm fields `+0xf8 m_frameCount`, `+0x100 m_currentFps`, `+0x14c m_minFps`, `+0x150 m_maxFps`, `+0x154 m_sampleCount`, `+0x158 m_totalFps`, `+0x160 m_totalLivingObjects`, `+0x164 m_totalBalloonObjects`, and `+0x168 m_totalStaticObjects`.
- Name local stack variables `[ebp-0x14..-0x08]` as `MapRect visibleTiles`, `[ebp-0x10]` as `visibleTiles.top`, and `[ebp-0x08]` as `visibleTiles.bottom`.

## Cross-References

- [UID:00015D][0x004b6410-0x004b6caf.FpsPaneDiagnostics](by-memory/0x004b6410-0x004b6caf.FpsPaneDiagnostics.md)
- [UID:00005F][FpsPane](by-class/FpsPane.md)
- [UID:0000JK][FpsPane](by-file/FpsPane.md)
- [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md)
- [UID:00015F][0x004b64a0-0x004b67a7.FpsPaneUpdateFpsLogSessionRaw](by-memory/0x004b64a0-0x004b67a7.FpsPaneUpdateFpsLogSessionRaw.md)
- [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md)
- [UID:00015J][0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw](by-memory/0x004b6ae0-0x004b6c2b.FpsPaneWriteLogSummaryRaw.md)
- [UID:0001UO][FpsPaneLayout](by-type/by-struct/FpsPaneLayout.md)
- [UID:0001XN][FpsPane_vtables](by-type/by-vtable/FpsPane_vtables.md)
- [UID:0002MJ][0x0061a61c-0x0061a6a4.FpsPaneVtableData](by-memory/0x0061a61c-0x0061a6a4.FpsPaneVtableData.md)
- [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md)
- [UID:0001OW][0x0067a764-0x0067a768.ActiveMapPaneSingleton](by-memory/0x0067a764-0x0067a768.ActiveMapPaneSingleton.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:00009Q][ObjectList](by-class/ObjectList.md)
- [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md)

## 2026-08-24 Accepted OnTimer Identity And Source

- The current source-facing method is `bool FpsPane::OnTimer(int, int, int)`. `UpdateStatistics` is retained in this filename and historical sections only as the prior behavior-based alias; it is not the emitted source spelling.
- Exact modeled range is `[0x004b68b0,0x004b69b0)` (`0x100` bytes), with five leading `0xcc` bytes and no padding before raw `StartLogSession` at `0x004b69b0`. The only inbound route is tertiary TimerHandler-view slot `0x0061a6a0`; no direct code caller exists.
- The binary receiver is the `TimerHandler` facet at `FpsPane +0xa4`. Adding `0xa4` normalizes raw accesses to `m_frameCount +0xf8`, `m_currentFps +0x100`, `m_minFps +0x14c`, `m_maxFps +0x150`, `m_sampleCount +0x154`, `m_totalFps +0x158`, and object totals at `+0x160/+0x164/+0x168`.
- Exact outbound calls are `MapPane::GetClampedVisibleTileBounds` at `0x004b6939`, front-row bucket helper `0x00532670`, back-row bucket helper `0x005326d0`, visible-list helper `0x00532610`, inherited `TimerHandler::ScheduleTimer` at `0x004b6998`, and security-cookie check at `0x004b69a5`.
- The three callback payloads are ABI-proven by `ret 0x0c` but unused; formal source leaves them unnamed rather than inventing semantics. The H declaration is centralized on UID00005F as protected virtual `OnTimer`.
- MapPane, ObjectList, and TimerHandler are dependencies rather than owners. The exact formal CPP above preserves signed frame sampling, count accumulation, 1000 ms cadence, and always-true return.
- Metadata remains owner/emitter `00005F`, reconstructable `TRUE`; score is `94/96`.

## Changes

- 2026-06-23 B002 StartLogSession boundary-link sync:
  - Score unchanged.
  - Updated the successor boundary and cross-references to the renamed [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md). Boundary facts are unchanged: this method ends at the exclusive `0x004b69b0` boundary and the renamed child starts immediately after it.

- 2026-06-22 B013 root-report verification:
  - Score unchanged at `88/91`; owner/emitter route remains [UID:00005F][FpsPane](by-class/FpsPane.md) through [UID:0000JK][FpsPane](by-file/FpsPane.md).
  - Confirmed the root-level B013 report is already incorporated at method-body level: first-draft `bool FpsPane::UpdateStatistics(int, int, int)` C++ is present, the old 95/95 no-code rationale is removed, the timer-facet receiver is normalized from `FpsPane +0xa4`, the FPS sample/reset/min/max/sample/sum behavior is documented, MapPane/ObjectList diagnostic count dependencies are source-placed correctly, and the one-second timer reschedule remains represented as the local FpsPane timer wrapper.
  - Added explicit B013 provenance and kept remaining open issues as confidence caps only: final original method spelling, `MapRect` declaration spelling, ObjectList row-tier helper names, TimerHandler wrapper spelling, and unused callback payload names.

- 2026-06-20 B005 source-quality implementation:
  - Before: `84/90`, blank formal C++, stale 95/95 no-code rationale, and mostly raw/generated helper labels.
  - Changed to: `88/91` with first-draft `bool FpsPane::UpdateStatistics(int, int, int)` C++ under owner [UID:00005F][FpsPane](by-class/FpsPane.md) and emitter route [UID:0000JK][FpsPane](by-file/FpsPane.md).
  - Evidence incorporated: local PE/Capstone scan over `NexusTK.exe`, exact `0x100` body, five leading `0xcc` bytes after [UID:00015G][0x004b67b0-0x004b68ab.FpsPaneOnPaint](by-memory/0x004b67b0-0x004b68ab.FpsPaneOnPaint.md), no trailing padding before [UID:00015I][0x004b69b0-0x004b6adc.FpsPaneStartLogSession](by-memory/0x004b69b0-0x004b6adc.FpsPaneStartLogSession.md) at `0x004b69b0`, zero direct branch/call refs to start or end, sole absolute target pointer at vtable slot `0x0061a6a0`, timer-facet receiver normalization from `FpsPane +0xa4`, frame counter reset, current FPS write, min/max/sample/total FPS updates, `g_activeMapPane` visible-tile bounds read, ObjectList front/back/visible count accumulation, one-second timer reschedule, and helper-name cleanup.
  - Source-quality decisions: stale blank-C++/95-gate wording removed; `UpdateStatistics` accepted as best current source-facing name; generated `sub_*`, `FittingRoomDialog::meth_0x5975e0`, ParcelPane ownership, MapPane/ObjectList ownership, TimerHandler ownership, compiler thunk, scalar deleting destructor, no-code, and raw-helper merge interpretations are rejected.

- 2026-06-06 A001 live IDA recheck:
  - Before: `COMPLETION:70`, `CONFIDENCE:85`, and parent [UID:0000JK][FpsPane](by-file/FpsPane.md).
  - Changed to: `COMPLETION:84`, `CONFIDENCE:90`, and parent [UID:00005F][FpsPane](by-class/FpsPane.md); final C++ remained blank under the then-stale 95/95 gate.
  - Evidence: live IDA reconfirmed exact size, timer/update vtable ref, no direct callers, normalized owner offsets, FPS min/max/average/sample updates, MapPane/ObjectList diagnostic count reads, one-second timer reschedule, callee set, and boundary into the raw start-session helper.

- 2026-06-01 autogen parent attachment:
  - What existed before: `RECONSTRUCTABLE:TRUE` with blank `AUTOGEN_PARENT_UID`.
  - Changed to: parent [UID:0000JK][FpsPane](by-file/FpsPane.md).
  - Summary/evidence: the by-file page now has a validated diagnostics path; IDA MCP recheck confirms this method is the timer/update vtable entry via data ref `0x0061a6a0` and decompilation shows FPS sampling plus MapPane/ObjectList count reads.

- 2026-05-31: Reconstructable metadata clarified.
  - Before: `RECONSTRUCTABLE` was blank.
  - After: marked `RECONSTRUCTABLE:TRUE`; parent/code remained blank.
  - Summary/evidence: IDA MCP `lookup_funcs` confirmed `sub_4B68B0` size `0x100`; decompilation confirmed timer-view-relative FPS sampling, object-count reads through MapPane/ObjectList helpers, and timer rescheduling.
