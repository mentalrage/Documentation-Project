*** UID:0001MK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace {
const unsigned char kPostCommandOpcode = 0x34;
const unsigned char kPostHelpPanelTab = 2;
const int kHelpPanelSoundId = 0x198;
const int kHelpPanelSoundVolume = 100;

static void SendPostSelectionPacket(unsigned char postIndex)
{
    char packet[3];

    WritePacketByte(static_cast<char>(kPostCommandOpcode), packet);
    WritePacketByte(static_cast<char>(postIndex), packet + 1);
    packet[2] = 0;

    QueueAndSendPacket(g_packetSender, packet, 2);
}
}

PostInputPane::PostInputPane()
    : CharInputPane(0)
{
    wchar_t playerName[100];
    wchar_t prompt[128];

    g_pCollectionData->GetLocalPlayerName(playerName);
    swprintf_s(prompt, 128, g_pLanguageMan->GetLocalizedString(11), playerName);
    SetPromptText(prompt);
}

bool PostInputPane::HandleKeyOrTextEvent(Event *event)
{
    const unsigned char key = g_pEventMan->TranslateEventKey(
        event->m_payload.m_key.m_key,
        event->m_payload.m_key.m_modifiers);

    if (key == '?' &&
        event->m_payload.m_key.m_modifiers == kEventModifierShift &&
        event->m_type == kEventKeyDown) {
        g_pGeneralPurposePanel->SwitchActiveTab(kPostHelpPanelTab, NULL);
        g_pSoundManager->PlayEffect(kHelpPanelSoundId, kHelpPanelSoundVolume);
        return true;
    }

    return CharInputPane::HandleKeyOrTextEvent(event);
}

void PostInputPane::OnConfirmInput()
{
    if (GetTextLength() != 1)
        return;

    wchar_t text[2] = {0, 0};
    CopyText(text, 1);

    unsigned char postIndex = 0;
    const wchar_t ch = text[0];

    if (ch >= L'a' && ch <= L'z') {
        postIndex = static_cast<unsigned char>(ch - L'a' + 1);
    } else if (ch >= L'A' && ch <= L'Z') {
        postIndex = static_cast<unsigned char>(ch - L'A' + 27);
    } else {
        return;
    }

    if (postIndex < 1)
        return;

    if (postIndex > g_activeUserStatusPane->activeSlotLimit)
        return;

    SendPostSelectionPacket(postIndex);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current IDA confirms PostInputPane constructor/key/confirm method extents, raw opcode 0x34 helper at 0x005b5830-0x005b5890, vtable slots/stores, prompt/localization/player-state dependencies, post-index validation through g_activeUserStatusPane +0x284, two-byte packet send through g_packetSender, internal 0xcc padding, and exclusive boundary before ThrowInputPane at 0x005b5890. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b5630-0x005b5890 PostInputPane

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: class method cluster plus raw packet helper
- Likely source module: [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)
- Class owner: [UID:0000AO][PostInputPane](by-class/PostInputPane.md)
- Confidence: strong for method boundaries, vtable identity, packet behavior, and command-input source placement; medium-high for the raw helper reachability because IDA reports no direct xref to its start.
- Filename note: renamed on 2026-06-17 to match the documented half-open range `0x005b5630-0x005b5890`.

## Covered Ranges

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x005b5630-0x005b56f7` | `PostInputPane::PostInputPane` | Constructs the prompt with player name and installs vtables. |
| `0x005b56f7-0x005b5700` | alignment padding | Nine `0xcc` bytes before the key handler. |
| `0x005b5700-0x005b576c` | `PostInputPane::HandleKeyOrTextEvent` | Handles the exact Shift-only help-panel shortcut and delegates normal input. Historical `OnKeyInput` naming is superseded. |
| `0x005b576c-0x005b5770` | alignment padding | Four `0xcc` bytes before the confirm handler. |
| `0x005b5770-0x005b5830` | `PostInputPane::OnConfirmInput` | Converts a slot/index letter and sends post opcode `0x34`. |
| `0x005b5830-0x005b5890` | raw post packet helper | IDA-unmodeled helper-shaped body that writes opcode `0x34`, writes one signed byte argument, terminates the local packet buffer, and sends two bytes through `g_packetSender`; no direct xref to the helper start. |

## IDA MCP Evidence

Checked on 2026-05-23:

- IDA confirms starts at `0x005b5630`, `0x005b5700`, and `0x005b5770`.
- IDA vtable pass on 2026-05-26 confirms three `PostInputPane` table bases at `0x00630160`, `0x006301b0`, and `0x006301e0`; see [UID:0001XA][CommandInputPaneVtableFamily](by-type/by-vtable/CommandInputPaneVtableFamily.md).

Refreshed on 2026-06-05:

- IDA `analyze_funcs` confirms modeled function extents `0x005b5630-0x005b56f7`, `0x005b5700-0x005b576c`, and `0x005b5770-0x005b5830`.
- A raw byte audit confirms `0x005b56f7-0x005b5700` is nine `0xcc` bytes and `0x005b576c-0x005b5770` is four `0xcc` bytes.
- `0x005b5830-0x005b5890` is not padding: raw disassembly shows a standalone helper body with prologue, stack cookie, two `sub_575380` byte writes, `dword_67A7EC`, `sub_574BB0(packet, 2)`, and `retn 4`.
- IDA `xrefs_to 0x005b5830` reports no direct inbound xrefs. This matches other raw helper islands in the nearby input-pane code where helper-shaped bodies survive without modeled callers.
- `xrefs_to` confirms vtable store refs for `0x00630160`, `0x006301b0`, and `0x006301e0` from the command dispatcher `0x005a5bd0`, open helper `0x005a9a90`, and constructor stores `0x005b5677`, `0x005b567d`, and `0x005b5687`.
- `xrefs_to` confirms `PostInputPane::OnConfirmInput` is vtable-reached through slot `0x006301a8`; `OnKeyInput` is vtable-reached through slot `0x006301b8`.
- The byte immediately after the raw helper is the modeled `ThrowInputPane` constructor start at `0x005b5890`, so `0x005b5890` is the exclusive end of the Post-owned command-input island.

## Behavior Notes

- The constructor calls the shared character-input base constructor at `0x004f28a0`, installs the three `PostInputPane` vfptr views, reads the local-player/name data through [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md) / `dword_67A748`, fetches localized prompt id `11` through [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md), formats a 128-wide-character prompt buffer, and assigns it to the input pane.
- The key handler narrows the incoming event byte through the process `std::ctype<char>` object at [UID:00028L][0x0067a754-0x0067a758.g_pEventMan](by-memory/0x0067a754-0x0067a758.g_pEventMan.md) / `dword_67A754`, accepts only the `?` shortcut when event mode byte `+0x10a` is `4` and event type byte `+0x04` is `8`, switches [UID:0000R0][g_pGeneralPurposePanel](by-global/g_pGeneralPurposePanel.md) to tab `2`, plays/queues sound or effect id `0x198` through `dword_67A7D0`, and delegates all other events to the base key handler at `0x004f2920`.
- The confirm handler reads exactly one submitted wide character, maps `a-z` to `1-26` and `A-Z` to `27-52`, rejects values outside the active user slot limit at [UID:0000PS][g_activeUserStatusPane](by-global/g_activeUserStatusPane.md) offset `+0x284`, writes opcode `0x34` and the selected post index into a local buffer, null-terminates the second payload byte, and queues two bytes through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / `0x00574bb0`.
- The raw helper at `0x005b5830` performs only the final post-packet send shape: opcode `0x34`, one signed byte argument, zero terminator, and two-byte queueing through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md). It is retained as source-authored/reconstructable evidence even though no current static caller reaches the raw start.

## Rule 26 Inference And Source-Quality Notes

- B003's 2026-07-20 Event-contract callback preserves the constructor, retained packet helper, post-index conversion, active-slot bound, and packet send byte-for-byte while replacing only stale `InputEvent`/`OnKeyInput`/`NarrowInputKey` vocabulary. The exact source-facing handler is `bool PostInputPane::HandleKeyOrTextEvent(Event *event)`, calls `g_pEventMan->TranslateEventKey(key, modifiers)`, tests translated `?` with exact Shift-only `0x04` and key-down type `8`, passes `NULL` to `SwitchActiveTab(2, NULL)`, plays effect `0x198` at 100, and otherwise returns `CharInputPane::HandleKeyOrTextEvent(event)`.
- Completion/confidence rise from `88/91` to `90/93`: the shared Event type, translator call, modifiers, method name, return behavior, and base call are now source-ready, while the retained raw helper's direct-route absence and private lexical spellings remain explicit confidence caps.

- Best source shape: `PostInputPane::PostInputPane`, `PostInputPane::HandleKeyOrTextEvent`, `PostInputPane::OnConfirmInput`, and a file-local/private `SendPostSelectionPacket(unsigned char postIndex)` helper. The binary retains the helper body at `0x005b5830`; the draft source calls it from `OnConfirmInput` because helper-retained/source-inlined behavior is the most plausible source-authored shape. Historical `OnKeyInput` is retained only as a rejected alias.
- `0x005b5830-0x005b5890` is not padding, compiler runtime glue, or unrelated dead code. B001 validated the prologue, stack cookie, packet byte writes, `g_packetSender` queue call, opcode `0x34`, and clean `retn 4`; the domain-specific body ties it to Post command handling.
- Packet writer helper `0x00575380` should be described source-facing as `WritePacketByte` or `PacketBufferWriteByte`; the trailing zero written by that helper is local buffer safety/terminator behavior, not a transmitted protocol byte. The transmitted packet length is exactly two bytes.
- Packet queue helper `0x00574bb0` should be described source-facing as `QueueAndSendPacket` under [UID:0000Q5][g_packetSender](by-global/g_packetSender.md); generated `CashShopRequest::QueueAndSendPacket` ownership is rejected as source pollution.
- Shared input helper names in generated output such as `SpellStringInputPane::SetPromptText` and `QuitInputPane::GetInputLength`/`ReadInputText` are owner-pollution leads. The source-facing dependencies should stay on shared `LineInputPane`/`CharInputPane` behavior unless those helper pages are renamed project-wide.
- `event +0x04` is best modeled as an input event type field, `event +0x08` as key/keycode data, and `event +0x10a` as the printable/narrowing mode byte. These field names remain provisional because the shared input-event type is not finalized, so the score stays below final-audit quality.
- [UID:0000PS][g_activeUserStatusPane](by-global/g_activeUserStatusPane.md) offset `+0x284` is best modeled here as `activeSlotLimit` or `maxSelectableSlot`, not `maxPostIndex`, because B001 found broader active-selection usage risk.
- Source-placement alternatives rejected by B001: `InputPanes.cpp` only supplies base helpers; `SpellInputPanes.cpp` shares shortcut idioms but not this opcode/prompt/vtable behavior; `ItemActionInputPanes.cpp` starts at successor `0x005b5890`; standalone `PostInputPane.cpp` is not currently supported by the source-tree evidence.

## Touched State And Dependencies

| State or dependency | Use |
| --- | --- |
| [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md) | Likely source module for the command prompt family. |
| [UID:0000AO][PostInputPane](by-class/PostInputPane.md) | Owning class page and autogen parent. |
| [UID:0002N6][0x006300d4-0x006301e8.GroupPostInputPaneVtableData](by-memory/0x006300d4-0x006301e8.GroupPostInputPaneVtableData.md) | Exact vtable-data page covering the three `PostInputPane` table views. |
| [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md) / `dword_67A748` | Constructor source for local player/name text used in the formatted prompt. |
| [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md) / `dword_67A750` | Supplies localized prompt id `11`. |
| [UID:00028L][0x0067a754-0x0067a758.g_pEventMan](by-memory/0x0067a754-0x0067a758.g_pEventMan.md) / `dword_67A754` | Supplies the ctype/narrowing dependency used by key-event handling. |
| [UID:0000R0][g_pGeneralPurposePanel](by-global/g_pGeneralPurposePanel.md) / `dword_67A874` | Receives the help shortcut tab switch. |
| [UID:0000PS][g_activeUserStatusPane](by-global/g_activeUserStatusPane.md) / `+0x284` | Upper bound for valid post/index letters. |
| [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / `dword_67A7EC` | Queues the two-byte post command packet. |
| Opcode `0x34` | Post command packet opcode. |

## Ownership Decision

`PostInputPane` sits in the same command-input address neighborhood as emotion, group, spell, self-save, and block-list prompts, but its direct behavior is a post/message command selection prompt. Keep it in [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md), with the raw `0x005b5830-0x005b5890` opcode `0x34` helper included in this Post-owned island rather than merging it into the following item-action `ThrowInputPane` range.

## 2026-08-14 Slot, Type, Frame, And Header Closure

### Constructor `0x5b5630`

- Report-time state is `sub_5B5630`, exact range `[0x5b5630,0x5b56f7)`, size `0xc7`, weak type `_DWORD *__thiscall(_DWORD *this)`, and three exact table-store references at `0x5b5677/0x5b567d/0x5b5687`.
- The evidence-backed supervisor-owned IDA handoff is name `PostInputPane__Constructor`, type `struct PostInputPane *__thiscall PostInputPane__Constructor(struct PostInputPane *this)`, and regular comment `Constructs PostInputPane, initializes CharInputPane and the localized player-name prompt, and installs all three PostInputPane vptr views.`
- The exact ten-row report-time frame is: `var_1E0` / `_DWORD` / `+0x8` / `4`; `var_1DC` / `_DWORD` / `+0xc` / `4`; `Buffer` / `WCHAR[128]` / `+0x10` / `0x100`; `ArgList` / `WCHAR[100]` / `+0x110` / `0xc8`; `var_10` / `_DWORD` / `+0x1d8` / `4`; `var_C` / `_QWORD` / `+0x1dc` / `8`; `var_4` / `_DWORD` / `+0x1e4` / `4`; `__saved_registers` / `_DWORD` / `+0x1e8` / `4`; `__return_address` / `_UNKNOWN *` / `+0x1ec` / `4`; inert `arg_4` / `_DWORD` / `+0x1f4` / `4`.
- All ten rows, including inert `arg_4`, the body/range, and three construction refs are protected. A future supervisor update must stop between rename/type/comment stages if any field changes; no frame row should be renamed, retyped, added, or removed.

### EventHandler Override `0x5b5700`

- Report-time state is `sub_5B5700`, exact range `[0x5b5700,0x5b576c)`, size `0x6c`, weak type `char __thiscall(_DWORD *this, _BYTE *)`, and sole vtable xref `0x6301b8` from the adjusted `+0xa0` EventHandler view.
- The source declaration remains `bool PostInputPane::HandleKeyOrTextEvent(Event *event)`. The parser-safe IDA handoff deliberately uses physical adjusted-view semantics without inventing an `EventHandler` or `Event` layout: name `PostInputPane__HandleKeyOrTextEvent`, type `bool __thiscall PostInputPane__HandleKeyOrTextEvent(void *eventHandlerView, void *event)`, and regular comment `Handles the Shift-only question-mark key-down shortcut for the post-help tab and otherwise delegates through the CharInputPane EventHandler view.`
- The exact current three-row frame is `__saved_registers` / `_DWORD` / `+0x8` / `4`; `__return_address` / `_UNKNOWN *` / `+0xc` / `4`; `arg_0` / `_BYTE *` / `+0x10` / `4`. Expected type application changes only `arg_0` to `event` / `void *` at fixed `+0x10`, width `4`: one rename, one retype, no additions/removals, saved/return rows protected.
- The exact range/body/sole xref and all comment channels other than the proposed regular address comment are protected. This page records the semantic handoff and does not claim IDA mutation.

### Confirmation `0x5b5770` And Retained Helper

- Report-time state is `sub_5B5770`, exact range `[0x5b5770,0x5b5830)`, size `0xc0`, weak type `char __thiscall(_DWORD **this)`, and sole primary-vtable xref `0x6301a8`.
- The evidence-backed IDA handoff is name `PostInputPane__OnConfirmInput`, type `void __thiscall PostInputPane__OnConfirmInput(struct PostInputPane *this)`, and regular comment `Maps one alphabetic post selector to a one-based index, checks the active slot limit, and sends opcode 0x34.`
- Its exact seven-row frame is `var_88` / `_DWORD` / `+0x4` / `4`; `Src` / `_BYTE` / `+0x8` / `1`; `destination` / `_BYTE` / `+0x9` / `1`; `var_82` / `_BYTE[126]` / `+0xa` / `0x7e`; `var_4` / `_DWORD` / `+0x88` / `4`; `__saved_registers` / `_DWORD` / `+0x8c` / `4`; `__return_address` / `_UNKNOWN *` / `+0x90` / `4`. No row changes are expected or permitted.
- Raw helper `[0x5b5830,0x5b5890)` remains a complete source-authored opcode-`0x34` packet helper with no direct raw-start xref. The formal source-shaped `SendPostSelectionPacket` retains it without inventing a separate external binary helper or merging it into ThrowInputPane.

### Header And Source Disposition

- [UID:0000AO][PostInputPane](by-class/PostInputPane.md) now emits the complete declaration through H at direct position `60`; this page emits constructor/handler/confirm/helper definitions through CPP as child position `10`. It does not duplicate the declaration.
- The formal CPP body above is preserved exactly. It keeps event source semantics, constants, exact `a-z`/`A-Z` mapping, active-slot limit, opcode/length, local terminator, and base delegation while avoiding raw stack names and compiler artifacts.
- The three table heads `0x630160/0x6301b0/0x6301e0`, eight-base RTTI, `0x108` layout, and no-derived-field/no-explicit-destructor disposition are documented by the class and exact ABI-data pages; raw ABI arrays are not source.
- Score `92/94` reflects complete method/helper behavior, exact slots/frames, parser-safe physical adjusted-view handoff, H/CPP ownership, boundaries/padding, and source route. Remaining uncertainty is private helper/field/original-file spelling rather than runtime behavior.

## Reconstruction Notes

This range is reconstructable as `PostInputPane` source plus one raw packet helper. The first-draft C++ above uses source-facing helper names for the shared input-base methods, packet byte writer, packet queue wrapper, side-panel helper, sound helper, and `g_activeUserStatusPane +0x284` active-slot-limit field. These names remain provisional below final-audit quality, but the behavior and source placement are strong enough to emit a draft method/helper body.

## Cross-References

- [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)
- [UID:0001XA][CommandInputPaneVtableFamily](by-type/by-vtable/CommandInputPaneVtableFamily.md)
- [UID:0000AO][PostInputPane](by-class/PostInputPane.md)
- [UID:0001MJ][0x005b5440-0x005b5547.GroupInputPane](by-memory/0x005b5440-0x005b5547.GroupInputPane.md)
- [UID:0002N6][0x006300d4-0x006301e8.GroupPostInputPaneVtableData](by-memory/0x006300d4-0x006301e8.GroupPostInputPaneVtableData.md)
- [UID:0001ML][0x005b5890-0x005b60c0.ThrowUseEatInputPanes](by-memory/0x005b5890-0x005b60c0.ThrowUseEatInputPanes.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)
- [UID:0000PS][g_activeUserStatusPane](by-global/g_activeUserStatusPane.md)

## Changes

- 2026-08-14 Agent-B010 UID0002N6 callback: raised `90/93 -> 92/94`, set child position `10`, preserved the complete formal CPP body, corrected current handler naming, and added exact constructor/handler/confirm names, weak/current types, vtable refs, all 10/3/7 frame rows, protected deltas, parser-safe adjusted-view semantics, retained raw-helper disposition, H/CPP source ownership, and score rationale.
- 2026-06-17 B001 source-quality execution:
  - Before: score was `84/90`, the file name still ended at `0x005b5830` despite the documented true range through `0x005b5890`, and formal C++ was blank.
  - After: renamed to [UID:0001MK][0x005b5630-0x005b5890.PostInputPane](by-memory/0x005b5630-0x005b5890.PostInputPane.md), raised to `88/91`, kept owner/emitter [UID:0000AO][PostInputPane](by-class/PostInputPane.md), and populated first-draft C++ for the constructor, key handler, confirm handler, and inferred retained post packet helper.
  - Evidence: B001 rechecked IDA modeled extents, `0xcc` padding spans, raw helper bytes at `0x005b5830-0x005b5890`, vtable-only key/confirm reachability, prompt id `11`, local-player name helper, `?` tab-2 help shortcut/effect, `a-z`/`A-Z` index mapping, `g_activeUserStatusPane +0x284` active-slot-limit check, two-byte opcode `0x34` send through `g_packetSender`, and the exclusive successor boundary before [UID:0001ML][0x005b5890-0x005b60c0.ThrowUseEatInputPanes](by-memory/0x005b5890-0x005b60c0.ThrowUseEatInputPanes.md).

- 2026-06-05 live IDA refresh:
  - Before: scored `70/85`, reconstructable/autogen metadata was blank, and the page stopped at the modeled confirm handler end `0x005b5830`.
  - After: raised to `84/90`, marked reconstructable, attached to [UID:0000AO][PostInputPane](by-class/PostInputPane.md), documented the true Post-owned island through `0x005b5890`, added the raw opcode `0x34` helper at `0x005b5830-0x005b5890`, and recorded internal padding at `0x005b56f7-0x005b5700` and `0x005b576c-0x005b5770`.
  - Evidence: IDA MCP `analyze_funcs`, `xrefs_to`, and raw `py_eval` byte/disassembly checks confirm function extents, vtable slots/stores, packet layout, global dependencies, no direct helper-start xref, and the exclusive boundary before [UID:0001ML][0x005b5890-0x005b60c0.ThrowUseEatInputPanes](by-memory/0x005b5890-0x005b60c0.ThrowUseEatInputPanes.md). This old no-C++ note was superseded by the 2026-06-17 B001 source-quality pass, which populated the first-draft C++ above.
