*** UID:00016G | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000IN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000IN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *__stdcall LoadDatFileBuffer(const wchar_t *path, int prefixSize, int *outSize)
{
    DATFile file;
    file.Open(path);

    int payloadSize = file.GetSize();
    int totalSize = payloadSize + prefixSize;
    char *buffer = static_cast<char *>(GetMemoryMan()->AllocateBufferMemory(totalSize));

    if (outSize != NULL) {
        *outSize = totalSize;
    }

    if (prefixSize > 0) {
        memset(buffer, 0, prefixSize);
    }

    file.Read(buffer + prefixSize, payloadSize);
    file.Close();

    return buffer;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
char *__stdcall LoadDatFileBuffer(
    const wchar_t *path, int prefixSize, int *outSize);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready DAT file-buffer loader: constructs a local DATFile, opens the caller path, allocates payload plus signed prefix through MemoryMan, optionally returns total size, zero-fills the prefix, reads payload bytes, and remains owned/emitted by DATFile with alias pages covered by this implementation. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004bb120-0x004bb1d2 LoadDatFileBuffer

## Status

- Disposition: reconstructable project code
- Entity kind: global helper function
- Canonical symbol: [UID:0000T4][LoadDatFileBuffer_4BB120](by-global/LoadDatFileBuffer_4BB120.md)
- Likely source module: [UID:0000IN][DATFile](by-file/DATFile.md)
- Direct owner/emitter: [UID:0000IN][DATFile](by-file/DATFile.md)
- Confidence: very strong for range, lifecycle behavior, direct-call reachability, MemoryMan allocation route, and owner routing; first-draft formal C++ now emits the helper body, while exact original header/typedef spellings remain below final-audit proof.
- Current reconstruction C++: source-ready first-draft global helper body. The source-facing name is `LoadDatFileBuffer`; the address-suffixed global/item pages are documentation aliases covered by this emitter.

## Covered Range

| Range | Current name | Notes |
| --- | --- | --- |
| `0x004bb120-0x004bb1d2` | `LoadDatFileBuffer_4BB120` / IDA `sub_4BB120` | Loads one DAT-backed file into an allocated buffer, optionally leaving a zero-filled prefix before the payload. |

The next IDA function starts at `0x004bb1e0`, so this page uses the half-open end `0x004bb1d2` from IDA size `0xb2`.

## Behavior

The helper wraps the full [UID:00003G][DATFile](by-class/DATFile.md) lifecycle for callers that need a plain memory buffer:

1. Reads the MemoryMan singleton used by the allocation call, then constructs a local `DATFile`.
2. Opens the supplied DAT-backed path.
3. Reads the payload size through `DATFile::GetSize`.
4. Allocates `payloadSize + prefixSize` bytes through `GetMemoryMan()->AllocateBufferMemory`.
5. Stores the total allocation size through `outSize` when that pointer is non-null.
6. Zero-fills the prefix when `prefixSize > 0`.
7. Reads the DAT payload at `buffer + prefixSize`.
8. Closes and destroys the local `DATFile`.

## Live IDA Evidence

Checked on 2026-06-16:

- `lookup_funcs 0x004bb120` reports IDA `sub_4BB120`, size `0xb2`.
- `lookup_funcs 0x004bb1d2` reports `Not a function`, while `lookup_funcs 0x004bb1e0` reports the next function `sub_4BB1E0`, size `0x72`.
- IDA Python inspection reports fourteen `0xcc` padding bytes from `0x004bb1d2` through `0x004bb1df`, confirming the half-open page range and separation from the next function.
- The live decompile reports `char *__stdcall sub_4BB120(wchar_t *a1, int Size, _DWORD *a3)` and shows local DATFile construction, open, size, allocation, optional `outSize`, optional prefix `memset`, read, close, destructor, and return.
- Body call sites are `0x004bb148 -> sub_516030`, `0x004bb152 -> sub_49C130`, `0x004bb164 -> sub_49C180`, `0x004bb16c -> sub_49C2F0`, `0x004bb17d -> sub_516050`, `0x004bb195 -> _memset`, `0x004bb1a7 -> sub_49C310`, `0x004bb1af -> sub_49C240`, and `0x004bb1b7 -> sub_49C160`.
- `CodeRefsTo(0x004bb120)` reports ten direct call sites at `0x0048dc91`, `0x004999a1`, `0x005032a4`, `0x00565693`, `0x0056ca87`, `0x0056fee5`, `0x005b843f`, `0x005b8482`, `0x005babdd`, and `0x005bdac6`.
- Caller contexts include pane/resource constructors such as `CollectionBarControlPane`, `NewGroupPane`, `SelfLookPane2`, and `OldUserStatusPane`, plus repeated `prefixSize == 0x63` calls using `off_61682C`.
- A section-mapped PE scan confirms those ten rel32 call sites and finds zero absolute-VA pointer hits, zero RVA-form pointer hits, and zero direct jumps to the function start.

2026-06-28 B015 implementation callback rechecked MCP availability before editing. `idb_list` reported active session `supervisor_20260628_resume` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health(database='supervisor_20260628_resume')` returned `status: ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready; `lookup_funcs 0x004bb120` again reported `sub_4BB120` size `0xb2`.

Additional accepted B015 facts incorporated from the live 2026-06-28 evidence pass:

- `lookup_funcs` still reports `0x004bb1d2` as not a function and `0x004bb1e0` as the next modeled function, so the target remains one complete helper followed by fourteen `0xcc` alignment bytes.
- MCP decompilation and disassembly confirm the concrete call flow: `GetMemoryMan` / `0x00516030`, `DATFile` constructor, `Open`, `GetSize`, `MemoryMan::AllocateBufferMemory` / `0x00516050`, `_memset`, `Read`, `Close`, and destructor.
- The return instruction is `retn 0Ch`, supporting a three-argument `__stdcall` helper.
- The zero-fill branch uses signed `prefixSize > 0`, so `int prefixSize` is a better source-facing type than an unsigned prefix parameter.
- The out-size argument is null-checked before writing `payloadSize + prefixSize`; caller stack locals and the dword store support `int *outSize` as first-draft source-facing type.
- `xrefs_to 0x004bb120` reports ten direct call xrefs and no pointer-table/callback route.
- `xrefs_to 0x004bb1d2` reports no xrefs, and the successor `0x004bb1e0` has a separate caller from `0x499b93`; do not merge or split this target.
- The path literal bytes at `0x0061682c` decode as UTF-16 `L"9X11FONT.BIN"`. IDA's interior `a11fontBin` label at `0x00616830` and split `get_string` results are string-typing artifacts and evidence only, not source-facing code.
- Nine path-address instruction xrefs point at `0x0061682c`; the ten helper calls include a two-branch caller sharing one path setup.
- Byte-pattern checks found no absolute-VA or RVA-form pointer hits to `0x004bb120`, reinforcing ordinary direct-call reachability.

## Ownership Decision

Keep this helper with `archive/DATFile.cpp`, not with any caller-specific UI or render source file. Its callers are broad resource consumers, but the body is entirely DAT reader lifecycle plus allocation.

The `DATFile` parent is strong enough for attachment because this range is a direct lifecycle wrapper around `DATFile` constructor/open/get-size/read/close/destructor calls, while caller contexts are broad consumers rather than source owners.

2026-06-28 B015 keeps [UID:0000IN][DATFile](by-file/DATFile.md) as both direct owner and emitter. UI/resource caller ownership remains rejected: Collection, NumericString, MapName, SelfLook, Group, and status-pane constructors consume the returned numeric-glyph/resource buffer, but none owns the archive-reader lifecycle.

Reject `DATArchive` or `DATFileMgr` ownership for this helper. The body constructs and uses a `DATFile`; it does not directly probe the global manager except through `DATFile::Open`.

## Source-Quality Resolution

- Caller/reachability evidence is resolved as direct calls only; no pointer table or callback dispatch to `0x004bb120` was found.
- Ownership is resolved to `DATFile`; render/UI/status-pane callers were checked and remain consumers because they do not own archive-reader lifecycle behavior.
- The old final-C++ blocker is resolved enough for first-draft source output. [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) now emits `MemoryMan* GetMemoryMan(){ return g_pMemoryMan; }`, and [UID:0001BD][0x00516050-0x005160ce.AllocateBufferMemory](by-memory/0x00516050-0x005160ce.AllocateBufferMemory.md) now emits `void *MemoryMan::AllocateBufferMemory(size_t size)`.
- This target's disassembly matches the accepted MemoryMan call protocol: call `GetMemoryMan`, move the singleton pointer to `ECX`, push total size, then call `AllocateBufferMemory`.
- Formal source therefore uses `GetMemoryMan()->AllocateBufferMemory(totalSize)`, not raw `sub_516030`, raw `sub_516050`, a free `AllocateBufferMemory`, `malloc`, `new[]`, or a caller-specific allocator.
- The source-facing helper name is `LoadDatFileBuffer`. The address-suffixed by-global and by-item pages remain documentation/search aliases and should emit only a covered-by comment.
- Do not emit a standalone static wide-string for `9X11FONT.BIN` here. [UID:000250][0x006164f0-0x006168d0.CollectionReadOnlyData](by-memory/0x006164f0-0x006168d0.CollectionReadOnlyData.md) already documents the source constant and explicitly treats the file as a shared UI numeric-font resource, not helper-owned data.
- Rejected alternatives: leaving this page blank, clearing `EMITTER_UIDS`, converting it to a `[[CHILDREN]]` container, making this page a no-code covered-by marker, duplicating the implementation in [UID:0000T4][LoadDatFileBuffer_4BB120](by-global/LoadDatFileBuffer_4BB120.md) or [UID:0000UW][LoadDatFileBuffer_004BB120](by-item/LoadDatFileBuffer_004BB120.md), moving ownership to UI/resource files, hard-coding `9X11FONT.BIN` in the generic loader, or using IDA labels such as `sub_4BB120`, `sub_516030`, `sub_516050`, or `a11fontBin` in formal C++.

## Score Rationale

`COMPLETION:88` and `CONFIDENCE:92` reflect the resolved allocation route, formal C++ body, exact range/padding, direct-call-only reachability, path-literal caveat, alias-page disposition, and generated/tracker stale-state explanation. The score remains below final-audit territory because exact original header placement, exact `DATFile::Open`/`GetSize`/`Read` public signatures, and exact project typedef spelling for `outSize` remain inferred source-facing forms rather than recovered symbols.

## Cross-References

- [UID:0000T4][LoadDatFileBuffer_4BB120](by-global/LoadDatFileBuffer_4BB120.md)
- [UID:0000UW][LoadDatFileBuffer_004BB120](by-item/LoadDatFileBuffer_004BB120.md)
- [UID:0000IN][DATFile](by-file/DATFile.md)
- [UID:0000IM][DATArchive](by-file/DATArchive.md)
- [UID:00003G][DATFile](by-class/DATFile.md)
- [UID:0001QC][client_dat_specifications](by-meta/client_dat_specifications.md)

## Changes

- 2026-08-12 B003 UID0000L2 header visibility callback:
  - Retained the exact CPP body, `88/92`, owner/emitter UID0000IN, reconstructable state, blank position, and `Nested:0`.
  - Added the exact H-channel declaration `char *__stdcall LoadDatFileBuffer(const wchar_t *path, int prefixSize, int *outSize);` so `MapNamePane.cpp` and the other ten callers can include the source-owner header instead of relying on a PCH or an undeclared external.
  - The explicit `__stdcall`, return type, three arguments, and parameter order follow the recovered callee cleanup and caller stack contract; no body/name ownership or allocator behavior changed.

- 2026-06-04: Raised grading from `70/85` to `84/90`, set `RECONSTRUCTABLE:TRUE`, and attached `AUTOGEN_PARENT_UID:0000IN`.
  - Before: the page documented the loader lifecycle but still had stale evidence date, no reconstruction metadata, no parent attachment, and weaker caller/boundary detail.
  - After: live IDA MCP and IDA Python evidence documents the exact function boundary, padding, decompile flow, body call sites, ten callers, and archive-module ownership.
  - Evidence: `lookup_funcs`, decompile, and xref/disassembly inspection verify the behavior. Reconstruction C++ remains blank because final allocator naming and source-facing signature details remain below source-grade.
- 2026-06-16: Raised from `84/90` to `86/91` after current evidence refresh.
  - Before: the memory page lacked direct PE reachability checks and still used old final-code wording.
  - After: the page records current direct-call-only reachability, owner/emitter decision, and the exact source-quality blockers that keep C++ blank.
  - Evidence: IDA MCP `lookup_funcs`, `analyze_function`, `decompile`, `xrefs_to`, and a read-only section-mapped PE scan.
- 2026-06-28 B015 implementation callback: raised from `86/91` to `88/92`; owner/emitter metadata remains [UID:0000IN][DATFile](by-file/DATFile.md), `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000IN`; inline C++ remains blank and multiline formal C++ now emits `LoadDatFileBuffer`.
  - Before: the page still said final C++ was blank because the allocator/source name and signature typedefs were not source-grade.
  - After: accepted B015 evidence resolves the allocator route through [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) and [UID:0001BD][0x00516050-0x005160ce.AllocateBufferMemory](by-memory/0x00516050-0x005160ce.AllocateBufferMemory.md), keeps the DATFile owner route, records direct-call and path-literal negative evidence, and marks the by-global/by-item pages as covered aliases rather than duplicate implementations.
  - Evidence: current MCP session `supervisor_20260628_resume` returned healthy server state and reconfirmed `sub_4BB120` size `0xb2`; accepted report `tools/leaser/Agents/Agent-B015/research/00016G-LoadDatFileBuffer-empty-emitter-source-quality.md` records the full decompile/disassembly/xref/bytes/path/allocator evidence.
