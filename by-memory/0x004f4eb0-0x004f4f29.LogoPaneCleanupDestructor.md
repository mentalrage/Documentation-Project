*** UID:0002PM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007G | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007G | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
LogoPane::~LogoPane()
{
    m_logoTileContext.ReleaseBuffers();
    m_logoPalette.FreePaletteData();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Fresh 2026-08-11 IDA MCP confirms the exact `0x79` ordinary `LogoPane::~LogoPane()` body, surrounding `0xcc` alignment, three `LogoPane` vtable resets, `EPFTileContext::ReleaseBuffers` at `+0xf8`, `DLPalette::FreePaletteData` and compiler member destruction at `+0x120`, `Pane` base teardown, no direct static callers, scalar-wrapper parity, exact `0x354` layout closure, and the populated formal human-source destructor body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f4eb0-0x004f4f29 LogoPane Cleanup Destructor

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: non-deleting destructor body.
- Owner class: [UID:00007G][LogoPane](by-class/LogoPane.md).
- Parent aggregate: [UID:00019C][0x004f4c10-0x004f53a8.LogoPane](by-memory/0x004f4c10-0x004f53a8.LogoPane.md).
- Likely source module: [UID:0000O4][StartupLogoPanes](by-file/StartupLogoPanes.md).
- Rebuild handling: source-authored class destructor body with compiler SEH/security-cookie frame support.
- Source-quality closure: B002 2026-06-18 resolves `this + 0xf8` as `EPFTileContext m_logoTileContext` and `this + 0x120` as `DLPalette m_logoPalette`. The ordinary destructor source body releases tile-context buffers and frees live palette data; the later `DLPalette` member destructor, `Pane` base destructor, scalar deleting destructor wrapper, and adjusted-this thunks are compiler lowering.

## Address Range

- Half-open range: `0x004f4eb0-0x004f4f29`.
- Live IDA function: `sub_4F4EB0`, size `0x79`.
- Previous bytes: `0x004f4eac-0x004f4eb0` are four `0xcc` alignment bytes after [UID:0002PL][0x004f4c10-0x004f4eac.LogoPaneConstructor](by-memory/0x004f4c10-0x004f4eac.LogoPaneConstructor.md).
- Following bytes: `0x004f4f29-0x004f4f30` are seven `0xcc` alignment bytes before the unpromoted `LogoPane` advance body covered by [UID:0002PN][0x004f4f30-0x004f50a1.LogoPaneAdvanceAndBlitHandlers](by-memory/0x004f4f30-0x004f50a1.LogoPaneAdvanceAndBlitHandlers.md).

## Signature

- Calling convention: `__thiscall`.
- Receiver: `LogoPane *this` in `ecx`.
- Return value: `void`.
- IDA still names the function `sub_4F4EB0`; the page name is based on behavior, class vtable writes, sibling scalar deleting destructor parity, and the parent `LogoPane` island.

## Behavior

This body performs ordinary `LogoPane::~LogoPane()` cleanup without freeing the object allocation. The binary restores the three `LogoPane` vtable views at offsets `+0x00`, `+0xa0`, and `+0xa4`, calls `EPFTileContext::ReleaseBuffers` on `this + 0xf8` / `m_logoTileContext`, calls `DLPalette::FreePaletteData` on `this + 0x120` / `m_logoPalette`, then runs the compiler-emitted `DLPalette::~DLPalette` member destructor and `Pane::~Pane` base teardown. Source C++ should express the first two cleanup calls in `LogoPane::~LogoPane()` and let the compiler emit the member/base destructor calls and vtable stores.

The body is behaviorally paired with [UID:0002PR][0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor](by-memory/0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor.md), which repeats this cleanup and then applies scalar-delete flag handling. This page should therefore reconstruct as the ordinary `LogoPane` destructor semantics, while the deleting-destructor mechanics remain compiler output.

## IDA MCP Evidence

- 2026-06-06 `lookup_funcs` reports `0x004f4eb0` as `sub_4F4EB0`, size `0x79`. The adjacent probes at `0x004f4eac`, `0x004f4f29`, and `0x004f4f30` are not IDA function starts.
- 2026-06-06 byte checks report `0x004f4eac-0x004f4eb0` as `cc cc cc cc` and `0x004f4f29-0x004f4f30` as seven `0xcc` bytes.
- 2026-06-06 decompilation writes the `LogoPane` vtables, then calls `sub_458500(this + 0xf8)`, `sub_542CD0(this + 0x120)`, `sub_542CA0(this + 0x120)`, and `sub_544580(this)`.
- 2026-06-06 bounded disassembly confirms the body has an SEH/security-cookie prologue, stores the receiver in `edi`, writes vtables at `0x004f4edc`, `0x004f4ee2`, and `0x004f4eec`, runs the four cleanup calls in that order, restores `fs:0`, and returns at `0x004f4f28`.
- 2026-06-06 `callers` and `xrefs_to 0x004f4eb0` report no direct static references in the current IDB. Treat reachability as destructor/unwind/compiler-emission context rather than a normal direct call path until a broader vtable/EH audit proves more.
- 2026-06-06 `callees` reports exactly four internal callees: `0x00458500`, `0x00542cd0`, `0x00542ca0`, and `0x00544580`.
- 2026-06-06 `xrefs_to` the three `LogoPane` vtable constants confirms this destructor writes the same table views as the constructor and scalar deleting destructor: `0x0061cf78` at `0x004f4edc`, `0x0061cfc4` at `0x004f4ee2`, and `0x0061cff4` at `0x004f4eec`.
- Fresh 2026-08-11 read-only MCP used canonical IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, SHA256 `049D1FC92CCDAA8DA17BF816AF35BFE8248B5D70B2FFB4EB5ABB91CB5D3CF92B`, 143,205,130 bytes. Bounded lookup, disassembly, bytes, type, frame, comment, callee, and xref checks reconfirm the exact `0x79` range, four-call teardown sequence, vtable writes, surrounding padding, and absence of ordinary direct callers.
- Current helper evidence resolves `0x00458500` as the 71-consumer `EPFTileContext::ReleaseBuffers` body, `0x00542cd0` as the 3-consumer `DLPalette::FreePaletteData` body, and `0x00542ca0` as the 15-consumer `DLPalette::~DLPalette` body. The final `0x00544580` call remains shared `Pane::~Pane` teardown. These roles are independently owned by their support pages and are not migrated into `LogoPane`.
- The ordinary body and [UID:0002PR][0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor](by-memory/0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor.md) scalar wrapper reset identical primary and two adjusted vtable views and perform the same member/base teardown. The scalar wrapper alone adds deleting flags and optional storage release, proving that this page owns the one source-authored destructor body.

## Touched State And Dependencies

| State or helper | Evidence |
| --- | --- |
| `this + 0x00`, `this + 0xa0`, `this + 0xa4` | Restored to the three `LogoPane` vtable views before member/base teardown. |
| `this + 0xf8` | Passed to `sub_458500`, documented in [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md) as the release-buffers helper for primary, auxiliary, and encoded-mask buffers. |
| `this + 0x120` | Passed first to `sub_542CD0` and then to `sub_542CA0`; [UID:0001E4][0x00542ac0-0x00543149.DLPaletteMethodCluster](by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md) documents these as `DLPalette::FreePaletteData` and `DLPalette::~DLPalette`. |
| `this` | Passed to `sub_544580`, the shared pane/base teardown helper. Live decompilation of that helper writes `Pane` vtables and cleans base pane state, so generated labels that route this through unrelated pane subclasses should not be treated as ownership evidence. |

## LogoPane Member Layout Closure

The destructor member names are now source-quality enough for first-draft C++. `Pane` layout documentation places the first derived field at `+0xf8`. [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) documents a `0x28`-byte context, so `LogoPane +0xf8` through `+0x11f` is best modeled as `EPFTileContext m_logoTileContext`, and the next field starts at `+0x120`.

[UID:00003Z][DLPalette](by-class/DLPalette.md) documents `DLPalette` as an `LObject`-based palette object with optional entry/remap data, a `0x20`-byte header, and a 256-entry 16-bit lookup table through object offset `+0x22b`. Placing `DLPalette m_logoPalette` at `LogoPane +0x120` reaches `LogoPane +0x34c`, exactly where LogoPane's early-click/ready/queued flags begin. The completion callback pointer at `+0x350` then closes at `+0x354`, matching the scalar deleting destructor guard size.

Use `m_logoTileContext` and `m_logoPalette` as source-facing member names. Original spelling is not proven, but the types and field roles are strongly supported by constructor, static-loader, ordinary-destructor, scalar-destructor, EPFTileContext, DLPalette, and Pane layout evidence.

## Reconstruction Notes

Keep this body with `LogoPane`; it is not generic base-pane cleanup even though it delegates to lower-level image, palette, and base-pane destructors.

Formal C++ is now eligible under the active code-entry gate. Use:

```cpp
LogoPane::~LogoPane()
{
    m_logoTileContext.ReleaseBuffers();
    m_logoPalette.FreePaletteData();
}
```

Do not include explicit vtable stores, `m_logoPalette.~DLPalette()`, `Pane::~Pane()`, scalar-delete flag checks, or `operator delete` in this page's source body. Those are compiler-generated lowering from the class declaration, embedded member, base class, and scalar deleting destructor wrapper.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | The page records the exact range and alignment, source signature, vtable resets, typed cleanup chain, no-direct-caller evidence, touched state, helper ownership, scalar/thunk relationship, complete `0x354` member-layout closure, source-versus-compiler lowering, and the populated formal `LogoPane::~LogoPane()` body. |
| Confidence `95` | Fresh 2026-08-11 MCP reconfirms the body, boundaries, vtable stores, call order, helper types/xrefs, and scalar-wrapper parity. Current class and generated H/CPP independently close the field layout and single-source destructor route. Remaining uncertainty is limited to original private member spelling rather than type, offset, behavior, or ownership. |

## Cross-References

- [UID:00007G][LogoPane](by-class/LogoPane.md)
- [UID:00019C][0x004f4c10-0x004f53a8.LogoPane](by-memory/0x004f4c10-0x004f53a8.LogoPane.md)
- [UID:0000O4][StartupLogoPanes](by-file/StartupLogoPanes.md)
- [UID:0000XY][0x00457a60-0x00458610.EPFTileContext](by-memory/0x00457a60-0x00458610.EPFTileContext.md)
- [UID:0001E4][0x00542ac0-0x00543149.DLPaletteMethodCluster](by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md)
- [UID:0002PR][0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor](by-memory/0x004f5300-0x004f53a8.LogoPaneScalarDeletingDestructor.md)

## Changes

- 2026-08-11 B008 UID0002PR implementation callback:
  - Raised `88/91 -> 92/95` while preserving owner/emitter [UID:00007G][LogoPane](by-class/LogoPane.md), `RECONSTRUCTABLE:TRUE`, blank `EMITTER_POSITION_OPTIONAL`, `Nested:0`, the exact formal destructor CPP, and blank formal H.
  - Added fresh canonical-IDB confirmation of the exact body, typed helper chain, vtable parity, no-direct-caller state, and `0x354` layout closure. The earlier unavailable-MCP and blank-final-C++ statements remain below as superseded historical assumptions; the current formal CPP has been populated and physically verified.
### 2026-06-06 - Live destructor evidence refresh

- Before: scored `72/86` with a short behavior summary and earlier boundary/callee notes.
- Changed to: scored `82/90`, kept `RECONSTRUCTABLE:TRUE` and parent [UID:00007G][LogoPane](by-class/LogoPane.md), added live boundary/alignment evidence, raw cleanup order, no-direct-caller/xref caveat, dependency cross-references, score rationale, and change log.
- Summary/evidence: live IDA MCP confirms `sub_4F4EB0` size `0x79`, surrounding `0xcc` alignment, three `LogoPane` vtable writes, calls to EPFTileContext release, DLPalette free/destructor, and shared pane teardown, plus no direct static callers/xrefs for the ordinary cleanup body.
- 2026-06-18 B002 source-quality closure: recommended `88/91`, kept owner/emitter [UID:00007G][LogoPane](by-class/LogoPane.md), resolved `this +0xf8` as `EPFTileContext m_logoTileContext` and `this +0x120` as `DLPalette m_logoPalette`, proved the layout through `Pane` derived start `+0xf8`, `EPFTileContext` size `0x28`, `DLPalette` size through `+0x22b`, LogoPane flags at `+0x34c`, callback pointer at `+0x350`, and scalar-destructor guard size `0x354`, and supplied formal first-draft `LogoPane::~LogoPane()` C++. Fresh IDA MCP was unavailable during this pass, so exact original member spellings remain descriptive/inferred.
