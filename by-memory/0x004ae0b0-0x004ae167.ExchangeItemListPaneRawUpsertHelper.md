*** UID:00014R | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Emitted code for this retained raw no-route upsert helper is covered by [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md case 2.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained raw ExchangeItemListPane no-route upsert helper; row-update behavior is documented, and emitted behavior is covered by dispatcher case 2. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ae0b0-0x004ae167 ExchangeItemListPane Raw Upsert Helper

## Status

- Disposition: reconstructable NexusTK project code, but raw/non-IDA function start
- Entity kind: projected `ExchangeItemListPane` helper
- Direct semantic owner: [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)
- Source route: [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md) -> [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- Current generated label: not emitted in generated/simroot active source
- Best source-facing name/signature: `ExchangeItemListPane::SetOfferItem(unsigned char key, unsigned short itemId, unsigned char color, const wchar_t *name)`.
- Confidence: strong for raw byte boundary, row layout, duplicate remove/insert behavior, and class ownership; medium for final original helper name/spelling.
- Reconstruction note: behavior is reconstructable and class-owned, but the formal block is only a covered-by marker because no inbound route to this raw helper is proven and the live [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) emits equivalent row-update behavior inline.

## Behavior

This raw helper inserts or replaces an offered-item row by key byte. The best descriptive source name is `SetOfferItem`: the helper removes one existing row with the same key and reinserts a replacement at the same index, while a new key appends at the current row count. `InsertOfferItem` is weaker because the duplicate path is not append-only, and `FindOfferItem` belongs to sibling [UID:00014S][0x004ae170-0x004ae1ac.ExchangeItemListPaneRawFindHelper](by-memory/0x004ae170-0x004ae1ac.ExchangeItemListPaneRawFindHelper.md).

The row layout matches `ExchangeItemListPane::DrawItemEntry`:

| Offset | Field |
| --- | --- |
| `+0` | key/slot byte |
| `+1` | padding/reserved byte |
| `+2` | item id word |
| `+4` | color byte |
| `+5` | padding/reserved byte |
| `+6` | wide item name (`wchar_t name[256]`) |

The total row size is `0x206`, matching [UID:00014Q][0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor](by-memory/0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor.md)'s `ListPane` row-size argument and the draw virtual's reads from row `+2`, `+4`, and `+6`. The helper does not zero the entire row record before insertion; it writes the key, item id, color, and copies a wide source name with `_wcscpy_s` capacity `0x100`.

The duplicate path preserves the replaced row's position. The loop calls the shared list count helper, fetches each row by index, compares `row[0]` to the key argument, branches to the tail block at `0x004ae156`, calls the shared remove helper with `(index, 1)`, and jumps back to the row-construction/insert path using the same index. If no matching key exists, the append index is the refreshed row count.

## Boundary Notes

IDA MCP does not define a function at `0x004ae0b0`, and `xrefs_to 0x004ae0b0` reports no xrefs. The 2026-06-01 IDA MCP recheck produced the same result, and the 2026-06-03 IDA MCP recheck again reports no IDA function object or external xrefs at this raw start. Raw disassembly still shows a complete helper.

The previously documented end `0x004ae153` was too short. The `retn 10h` instruction begins at `0x004ae153`, and there is a reachable branch block at `0x004ae156-0x004ae167` used by the duplicate-row removal path before jumping back to the insert path. Alignment begins at `0x004ae167`.

The 2026-06-03 IDA instruction-head pass shows the branch block as `cmp esi,-1` at `0x004ae156`, `jz loc_4AE0F4` at `0x004ae159`, removal arguments at `0x004ae15b-0x004ae15d`, `sub_4F3D60` at `0x004ae160`, and `jmp loc_4AE0FD` at `0x004ae165`; `0x004ae167-0x004ae170` is nine bytes of `0xcc` alignment.

## 2026-06-15 Live IDA Refresh

- IDA MCP `lookup_funcs` still reports `Not a function` for `0x004ae0b0`, the duplicate-removal tail at `0x004ae156`, the exclusive end `0x004ae167`, and successor raw start `0x004ae170`; `xrefs_to 0x004ae0b0` reports no inbound xrefs. This keeps the helper classified as raw/non-IDA code rather than a normal IDA function.
- Focused disassembly confirms the raw helper starts at `0x004ae0b0`, allocates a `0x210`-byte local frame, scans existing rows with `sub_4F3BD0` count and `sub_4F3DC0(index)` row access, compares the row key byte at offset `+0` against argument byte `arg_0`, and either appends at the current row count or removes one existing row through `sub_4F3D60(index, 1)`.
- The insert record is built on the stack at `0x004ae0fd-0x004ae13e`: key byte at `+0`, item id word at `+2`, color byte at `+4`, name copied into `+6` with `_wcscpy_s(..., 0x100, name)`, then inserted with `sub_4F3C00(index, row)`.
- [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md) row layout remains corroborated by the draw virtual at `0x004ae1c0`: IDA decompilation reads item id from row `+2`, color from row `+4`, and wide name from row `+6`, then draws the name at x-coordinate `50`.
- The body is `0xb7` / 183 bytes from `0x004ae0b0` through the exclusive end `0x004ae167` (Verified with int_convert.py). The `retn 10h` cleanup is `0x10` / 16 bytes of arguments (Verified with int_convert.py), and `0x004ae167-0x004ae170` remains alignment before the raw find helper.

## 2026-06-19 B012 Source-Quality Reanalysis

- Direct owner/emitter changed from broad file [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) to direct class [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md). The receiver is the list pane, the body only mutates row-list state, [UID:00014Q][0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor](by-memory/0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor.md) uses the same class owner for construction, ExchangeItemListPane vtable data owns the class ABI evidence, and [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) remains the source-file route through the class.
- Best source-facing signature is `void ExchangeItemListPane::SetOfferItem(unsigned char key, unsigned short itemId, unsigned char color, const wchar_t *name)`. `ecx` is the receiver, `[ebp+8]` becomes row `+0`, `[ebp+0xc]` becomes row `+2`, `[ebp+0x10]` becomes row `+4`, `[ebp+0x14]` is the wide source string, and `ret 10h` cleans four explicit arguments.
- Direct PE/Capstone recheck used `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`, image base `0x00400000`, and `.text` `0x00401000-0x0060c4ac`. The target maps to file offsets `0x000ad4b0-0x000ad567`.
- Outgoing calls are `0x004f3bd0` row count, `0x004f3dc0` indexed row access, another `0x004f3bd0` append-count refresh, `_wcscpy_s` at `0x005cd657`, `0x004f3c00` row insert, `0x005c772f` security-cookie check, and `0x004f3d60` remove rows. These are shared `ListPane` infrastructure calls, not Exchange-owned helper bodies.
- Route scans found no full-file VA, RVA, or file-offset dword hits for `0x004ae0b0`, `0x004ae156`, `0x004ae167`, or `0x004ae170`; no rel32 call/jump/conditional-branch route to those addresses; and no whole-`.text` instruction-immediate hit to `0x004ae0b0`, `0x004ae167`, or `0x004ae170`. The only `0x004ae156` immediate hit is this helper's own internal branch at `0x004ae0ed`.
- Positive-control scans did find normal vtable dword references for successor routed methods: `0x004ae1b0` at `.rdata` VA `0x00619e40` and `0x004ae1c0` at `.rdata` VA `0x00619e48`. Raw constructor/upsert/find starts have no matching vtable/pointer-table route.
- [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) subcommand `2` is the live source path for offered-item row updates. It reads packet fields, converts the counted CP_ACP item name to wide text, selects control `5` or `8`, scans/removes by key, builds an `ExchangeItemListRow`, and inserts it inline. Do not rewrite the live dispatcher to call this raw no-route helper without a future retained-clone policy.
- Rejected alternatives: direct file owner [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) is too broad for the receiver; outer dialog owner [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md) is wrong for a list-pane `this`; no-owner/non-reconstructable is wrong because the body is custom NexusTK UI row mutation rather than compiler/runtime glue; immediate formal C++ is unsafe because it would emit a separate no-route duplicate of behavior already represented by the dispatcher.
- B015's class-level reanalysis independently keeps the same conclusion and adds exact class-wide context: the body is exactly `0xb7` / 183 bytes, followed by nine `0xcc` bytes at `0x004ae167-0x004ae170`; the only internal branch into the removal tail is the helper's own conditional branch to `0x004ae156`; no direct branch, instruction immediate, VA dword, RVA dword, file-offset dword, callback, timer, data-pointer, or vtable route reaches `0x004ae0b0`; and the positive-control scan finds only the successor virtual routes `0x00619e40 -> 0x004ae1b0` and `0x00619e48 -> 0x004ae1c0`. The report bounds the `key`/`slot` naming question by choosing `key` or `offerKey` unless packet byte `+3` is later proven to be an inventory slot, and it bounds `color`/`style` by keeping `color` while noting item-render naming may later standardize the byte as style/palette.

## First-Draft C++ / Covered-By Decision

Formal `RECONSTRUCTION_CPP CODE` now contains only the covered-by marker for [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `2`. The no-standalone-body proof is:

1. Current IDA-backed docs report no function object and no inbound xrefs at `0x004ae0b0`.
2. Direct PE/Capstone scans found no branch, instruction-immediate, VA dword, RVA dword, file-offset dword, or vtable route to the raw start.
3. Positive-control vtable refs prove routed `ExchangeItemListPane` methods are detectable, but none point to this helper.
4. [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) dispatcher subcommand `2` already emits equivalent live row-update logic inline.
5. A separate emitted method would currently be a no-route duplicate, not proven original source flow.

Reference body for a future retained-clone policy only:

```cpp
struct ExchangeItemListRow {
    unsigned char key;
    unsigned char pad1;
    unsigned short itemId;
    unsigned char color;
    unsigned char pad5;
    wchar_t name[256];
};

void ExchangeItemListPane::SetOfferItem(unsigned char key,
                                        unsigned short itemId,
                                        unsigned char color,
                                        const wchar_t *name)
{
    int insertIndex = GetRowCount();

    for (int i = 0; i < insertIndex; ++i) {
        const ExchangeItemListRow *row =
            static_cast<const ExchangeItemListRow *>(GetRow(i));
        if (row->key == key) {
            RemoveRows(i, 1);
            insertIndex = i;
            break;
        }
    }

    ExchangeItemListRow row;
    row.key = key;
    row.itemId = itemId;
    row.color = color;
    wcscpy_s(row.name, 256, name);
    InsertRow(insertIndex, &row);
}
```

This reference body intentionally omits `memset`: the raw helper writes the used fields but does not zero the full `0x206` record. Final helper names may become `GetItemCount`, `GetItemData`, `RemoveItems`, or `InsertItem` if the shared [UID:000194][0x004f3a50-0x004f4a77.ListPane](by-memory/0x004f3a50-0x004f4a77.ListPane.md) naming pass chooses those spellings.

## Score Rationale

Completion is raised from `85` to `86` and confidence from `87` to `89` because the page now records source-facing class ownership, best helper name/signature, row padding, duplicate remove/reinsert semantics, direct PE/Capstone route scans, vtable positive controls, and the dispatcher-inline source-shape decision. It remains capped below final-source quality because no original symbol or inbound route proves that this raw helper should be emitted as a separate source method.

Standalone final C++ remains intentionally absent despite clearing the minimum score/emitter gate because the helper is still raw/non-IDA with no inbound xrefs and no proven original helper declaration, while the live dispatcher contains equivalent logic inline. The formal marker prevents this retained helper from remaining a blank nonblank emitter.

## Cross-References

- [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)
- [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md)
- [UID:00014Q][0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor](by-memory/0x004ae060-0x004ae0a4.ExchangeItemListPaneRawConstructor.md)
- [UID:00014S][0x004ae170-0x004ae1ac.ExchangeItemListPaneRawFindHelper](by-memory/0x004ae170-0x004ae1ac.ExchangeItemListPaneRawFindHelper.md)
- ExchangeItemListPane vtable data at `0x00619dc4-0x00619e88`.

## Changes

- 2026-06-28 B001 empty-emitter implementation:
  - Changed the formal C++ block from blank to the exact covered-by marker for [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `2`.
  - Metadata stays `86/89`, owner/emitter [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md), `RECONSTRUCTABLE:TRUE`; this page now no longer creates an empty nonblank emitter while preserving the raw no-route helper evidence.
- 2026-06-15 A001-goal2-exchange-foldertree-memory-20260615-1:
  - Before: `COMPLETION:78`, `CONFIDENCE:86`.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:87`; owner/emitter remained [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md), and final C++ was blank at that time.
  - Summary/evidence: live IDA MCP reconfirmed no function object or inbound xrefs at the raw start, refreshed the scan/remove/insert instruction flow, tied the row fields to `ExchangeItemListPane::DrawItemEntry`, and verified the `0xb7`-byte body with alignment before the raw find helper.
- 2026-06-19 B012 source-quality reanalysis:
  - Before: `COMPLETION:85`, `CONFIDENCE:87`, direct owner/emitter [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md).
  - Changed to: `COMPLETION:86`, `CONFIDENCE:89`, direct owner/emitter [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md); standalone final C++ stayed absent.
  - Summary/evidence: direct PE/Capstone route scans found no branch/immediate/pointer/vtable route to `0x004ae0b0`, positive-control vtable refs only for successor null/draw virtuals, row layout and duplicate remove/reinsert behavior are class-local to `ExchangeItemListPane`, best descriptive source name is `SetOfferItem`, and [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) dispatcher subcommand `2` remains the live inline source path.
- 2026-06-19 B015 class source-quality reanalysis:
  - Score and metadata unchanged after B012 (`86/89`, owner/emitter [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)); formal C++ was blank until B001 converted it to the dispatcher case `2` marker.
  - Summary/evidence: class-level PE scan reconfirmed the exact `0xb7` body, nine-byte successor padding, no external route to `0x004ae0b0`, positive-control null/draw vtable refs, and the bounded naming decisions `key`/`offerKey` and `color`. The helper remains a named no-route private helper candidate while the live dispatcher keeps equivalent case-2 logic inline.
- 2026-06-01 documentation pass:
  - Before: metadata remained at `50/65`, reconstructable and parent fields were blank, and the raw-helper status was not connected to current IDA evidence.
  - Changed to: `74/84`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000J9`.
  - Summary/evidence: IDA MCP reconfirmed no function/xrefs at `0x004ae0b0`; row layout, duplicate-row branch coverage, and `DrawItemEntry` consistency support reconstructable ExchangeItemListPane behavior.
- 2026-06-03 boundary correction:
  - Before: the documented range ended at `0x004ae166`, which is the start of the final short-jump instruction.
  - Changed to: exact exclusive range `0x004ae0b0-0x004ae167`, scores `78/86`.
  - Summary/evidence: IDA MCP instruction heads prove the duplicate-removal tail reaches the `0x004ae165` two-byte jump and that `0x004ae167-0x004ae170` is alignment padding.
