*** UID:0004RJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace
{
struct PhotoMapCell
{
    unsigned short terrainTileId;
    unsigned short reserved;
    unsigned short staticObjectId;
};

struct PhotoMapFileData
{
    unsigned char header[8];
    PhotoMapCell cells[15][17];
};
}

PhotoPane::PhotoPane(unsigned short mapId,
                     unsigned char avatarShape,
                     int avatarTileX,
                     int avatarTileY,
                     unsigned char avatarDirection)
    : PictureViewPane(),
      m_snapshotPort()
{
    SimpleUString mapFileName;
    mapFileName.Format(L"C%04d.MAP", mapId);

    DATFile mapFile;
    mapFile.Open(mapFileName);

    ObjectStatusBlob appearance;
    appearance.m_statusKind = ObjectStatusBlob::HumanObject;
    appearance.m_appearanceId = avatarShape;
    appearance.m_bodyId = 0;
    appearance.m_weaponId = 0xffff;
    appearance.m_shieldOrArrowId = 0x00ff;
    appearance.m_fullBodyId = 0xffff;
    appearance.m_compositionMode = 0;
    appearance.m_displayStatusByte = 0x50;
    appearance.m_faceColor = 0;
    appearance.m_bodyColor = 0;

    const PhotoMapFileData *mapData =
        (const PhotoMapFileData *)mapFile.GetDataPointer();

    RectBounds snapshotBounds;
    InitRectBounds(&snapshotBounds,
                   0,
                   0,
                   18 * g_mapTilePixelWidth,
                   16 * g_mapTilePixelHeight);
    SetBounds(&snapshotBounds, 0);
    OffsetRectBounds(&snapshotBounds,
                     -snapshotBounds.left,
                     -snapshotBounds.top);
    m_snapshotPort.SetBounds(&snapshotBounds);
    m_snapshotPort.UpdateSurfaceInfo();

    int row;
    int column;
    for (row = 0; row < 15; ++row)
    {
        for (column = 0; column < 17; ++column)
        {
            RectBounds tileBounds;
            InitRectBounds(&tileBounds,
                           column * g_mapTilePixelWidth,
                           row * g_mapTilePixelHeight,
                           (column + 1) * g_mapTilePixelWidth,
                           (row + 1) * g_mapTilePixelHeight);

            g_pMapTileImageLib->DrawTile(
                &m_snapshotPort,
                &tileBounds,
                mapData->cells[row][column].terrainTileId);
        }
    }

    for (row = 0; row < 15; ++row)
    {
        for (column = 0; column < 17; ++column)
        {
            const unsigned short staticObjectId =
                mapData->cells[row][column].staticObjectId;
            if (staticObjectId != 0)
            {
                RectBounds objectBounds;
                g_pStaticObjImageLib->GetStaticObjectBounds(
                    staticObjectId,
                    &objectBounds);
                OffsetRectBounds(&objectBounds,
                                 column * g_mapTilePixelWidth,
                                 row * g_mapTilePixelHeight);
                g_pStaticObjImageLib->RenderStaticObject(
                    &m_snapshotPort,
                    &objectBounds,
                    staticObjectId,
                    0,
                    0.0f,
                    false);
            }
        }
    }

    RectBounds avatarBounds;
    g_pNewHumanImageLib->CalculateBounds(
        &appearance,
        avatarDirection,
        0,
        0,
        &avatarBounds,
        0,
        true,
        true,
        0);
    OffsetRectBounds(&avatarBounds,
                     avatarTileX * g_mapTilePixelWidth,
                     avatarTileY * g_mapTilePixelHeight);
    g_pNewHumanImageLib->Draw(
        &m_snapshotPort,
        &avatarBounds,
        &appearance,
        avatarDirection,
        0,
        0,
        0,
        0,
        0,
        0.0f,
        true,
        true,
        0);

    m_snapshotPort.EndPaint();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact PhotoPane snapshot constructor; opens C%04d.MAP, preserves the observed partial human descriptor seed, renders 15x17 terrain/static cells plus one avatar into m_snapshotPort, and excludes compiler vptr/EH lowering from source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00549620-0x00549911 PhotoPane Constructor

## Status

- Exact half-open range: `[0x00549620,0x00549911)`, `0x2f1` / 753 bytes.
- SHA256: `FFED27EE3CA1F851EB57DBA68EEF6BCBACD153C24336FAB562FC380C046C2820` from the accepted live-IDB byte read.
- Source identity: `PhotoPane::PhotoPane(unsigned short, unsigned char, int, int, unsigned char)`.
- Direct owner/emitter: [UID:0000AG][PhotoPane](by-class/PhotoPane.md); final source route is [UID:0000MK][PhotoPane](by-file/PhotoPane.md) at `NexusTK/map/PhotoPane.cpp`.
- The `Nested:+4` value is a raw four-space delta that opens the first child level under non-emitting UID0001F2. It is not a child count.

## Behavior

- Constructs the `PictureViewPane` base, then constructs the embedded `GrafPort m_snapshotPort` member.
- Formats `C%04d.MAP` from `mapId`, opens that DAT-backed map resource, and treats its payload as an opaque eight-byte prefix followed by `15 * 17` six-byte cells.
- Seeds exactly ten observed `ObjectStatusBlob` fields for a human avatar. The remaining bytes stay unseeded exactly as in the binary; value initialization, aggregate `{}`, `memset`, or guessed defaults would change behavior.
- Establishes an off-screen snapshot rectangle from the global tile dimensions, normalizes it to origin, binds it to `m_snapshotPort`, and refreshes that cached surface.
- Runs a 15-row by 17-column terrain loop using each cell's first word as the terrain tile ID.
- Runs a second 15-by-17 loop, skips zero static-object IDs, obtains bounds from `StaticObjImageLib`, offsets them to the cell, and renders each object into the cached port.
- Calculates avatar bounds from the partially seeded appearance, direction, and exact flags; offsets to the supplied tile coordinates; draws the avatar; and ends cached painting.

## Layout And Type Evidence

- `PhotoMapCell` is exactly six bytes as three `unsigned short` values. Only the first and third words have directly evidenced terrain/static-object roles; the middle word remains conservatively named `reserved`.
- `PhotoMapFileData` preserves the exact eight-byte opaque prefix and the row-major `cells[15][17]` layout without raw byte offsets, record strides, or pointer indexing in emitted source.
- The constructor allocation size is `0x198` / 408 bytes, matching the current `PhotoPane` object and embedded `m_snapshotPort` layout.
- The partial `ObjectStatusBlob` write set preserves exact values including `0xffff`, `0x00ff`, and `0x50`; no unsupported field meaning is introduced.

## Liveness And Dependencies

- Exactly two code callers reach this constructor: packet dispatch at `0x00508bc1` and the opcode-`0x5a` companion/factory route at `0x00514461`.
- The constructor is the sole direct caller of `PictureViewPane::PictureViewPane` at `0x00549a30`, proving the derived/base relation and combined source-family placement.
- Rendering dependencies are `MapTileImageLib::DrawTile`, `StaticObjImageLib::GetStaticObjectBounds`, `StaticObjImageLib::RenderStaticObject`, `NewHumanImageLib::CalculateBounds`, `NewHumanImageLib::Draw`, `RectBounds`, `DATFile`, `SimpleUString`, and `GrafPort`.
- The constructor's compiler EH paths later invoke `PictureViewPane` teardown and `GrafPort` cleanup. Those routes are compiler lowering and are not copied into this human source body.

## Source And Compiler Disposition

- This interval is a live source constructor, not a broad aggregate, raw helper, or compiler-only wrapper.
- Vptr stores, complete-object adjustment, constructor unwind tables, and member/base cleanup sequencing remain compiler-generated and are intentionally absent from the formal C++.
- A separate `PictureViewPane.cpp` remains a bounded source-placement runner-up, but the contiguous island, sole base-constructor relation, shared vtables, and existing route select `NexusTK/map/PhotoPane.cpp`.
- Historical broad-marker/no-child treatment is superseded by this exact source child. Historical generated labels and raw-offset decompiler shapes remain rejected.

## Negative Evidence And Rejected Alternatives

- No evidence assigns semantics to the eight prefix bytes or the middle cell word.
- No evidence supports whole-object zeroing of `ObjectStatusBlob`.
- No evidence supports moving this constructor to `MapPane.cpp`, generic `Pane.cpp`, `EventHandler.cpp`, or a Bulletin source family.
- No source-level vptr, EH cleanup, scalar-delete, or raw-address code belongs in this body.

## Score Rationale

- Completion `92`: exact range/hash, callers, source behavior, data layout, typed body, dependencies, output route, compiler exclusions, negative evidence, and historical correction are documented.
- Confidence `93`: range, bytes, callers, loops, fields consumed, render ordering, and behavior are strongly evidenced. The score remains below 95 because exact original helper spelling, the opaque prefix, reserved cell word, and combined-versus-adjacent physical source split are inferred.

## Cross-References

- [UID:0000AG][PhotoPane](by-class/PhotoPane.md)
- [UID:0000AH][PictureViewPane](by-class/PictureViewPane.md)
- [UID:0000MK][PhotoPane](by-file/PhotoPane.md)
- [UID:0001F2][0x00549620-0x00549bc5.PhotoAndPicturePanes](by-memory/0x00549620-0x00549bc5.PhotoAndPicturePanes.md)
- [UID:0002R8][0x00549920-0x00549950.PhotoPaneCleanupDestructorBodyRaw](by-memory/0x00549920-0x00549950.PhotoPaneCleanupDestructorBodyRaw.md)
