*** UID:0001ZF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int RankingRewardEntry::ParseFromPacket(const unsigned char *packet)
{
    char narrowText[256];
    wchar_t wideText[256] = {0};
    int offset = 0;

    unsigned int length = static_cast<unsigned char>(packet[offset++]);
    GetMemoryMan()->MemmoveWrapper(narrowText, packet + offset, length);
    narrowText[length] = '\0';
    offset += length;
    unsigned int wideLength = MultiByteToWideChar(
        CP_ACP, 0, narrowText, length, wideText, 256);
    wideText[wideLength] = L'\0';
    wcscpy_s(rankStartText, 8, wideText);

    length = static_cast<unsigned char>(packet[offset++]);
    GetMemoryMan()->MemmoveWrapper(narrowText, packet + offset, length);
    narrowText[length] = '\0';
    offset += length;
    wideLength = MultiByteToWideChar(
        CP_ACP, 0, narrowText, length, wideText, 256);
    wideText[wideLength] = L'\0';
    wcscpy_s(rankEndText, 8, wideText);

    length = static_cast<unsigned char>(packet[offset++]);
    GetMemoryMan()->MemmoveWrapper(narrowText, packet + offset, length);
    narrowText[length] = '\0';
    offset += length;
    wideLength = MultiByteToWideChar(
        CP_ACP, 0, narrowText, length, wideText, 256);
    wideText[wideLength] = L'\0';
    wcscpy_s(symbolText, 256, wideText);

    symbolFrameIndex = static_cast<char>(packet[offset++]);
    symbolAttribute = packet[offset++];

    signed char itemCount = static_cast<signed char>(packet[offset++]);
    for (signed char itemIndex = 0; itemIndex < itemCount; ++itemIndex)
    {
        RankingRewardItem item;

        length = static_cast<unsigned char>(packet[offset++]);
        GetMemoryMan()->MemmoveWrapper(narrowText, packet + offset, length);
        narrowText[length] = '\0';
        offset += length;
        wideLength = MultiByteToWideChar(
            CP_ACP, 0, narrowText, length, wideText, 256);
        wideText[wideLength] = L'\0';
        wcscpy_s(item.name, 256, wideText);

        item.quantity = PacketBufferReadUInt32BE(packet + offset);
        offset += 4;
        item.iconId = PacketBufferReadUInt16BE(packet + offset);
        offset += 2;
        item.paletteIndex = packet[offset++];
        items.push_back(item);
    }

    return offset;
}

wchar_t *RankingRewardEntry::GetRankStartText()
{
    return rankStartText;
}

wchar_t *RankingRewardEntry::GetRankEndText()
{
    return rankEndText;
}

int RankingRewardEntry::GetItemCount() const
{
    return static_cast<int>(items.size());
}

wchar_t *RankingRewardEntry::GetSymbolText()
{
    return symbolText;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `RankingRewardEntry` parser plus four accessors; current MCP evidence proves the exact parser and raw item-count bodies, sole source-emitting UID0001ZH caller, opcode `0x7d` subcommand `5` context, CP_ACP counted-string conversion, signed item count, 560-byte entries, 520-byte item rows, std::vector append/count semantics, exact alignment pads, and no-route/original-name confidence caps. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045d7e0-0x0045db54 RankingRewardEntryParseAndAccessors

## Status

- Group range: `0x0045d7e0-0x0045db54`, retained as one coherent same-type/same-owner source group.
- Source bodies: parser `0x0045d7e0-0x0045db06`; getters `0x0045db10-0x0045db13`, `0x0045db20-0x0045db24`, raw item count `0x0045db30-0x0045db4e`, and `0x0045db50-0x0045db54`.
- Alignment: `0x0045db06-0x0045db10`, `0x0045db13-0x0045db20`, `0x0045db24-0x0045db30`, and `0x0045db4e-0x0045db50` are `0xcc` pads; no source body is emitted for them.
- Entity kind: source-ready helper-method group.
- Disposition: reconstructable `RankingRewardEntry` parser and four ordinary source accessors. The raw item-count method is executable code, not padding or a compiler artifact.
- Rebuild handling: source-authored out-of-line bodies through [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md); descriptive method names are retained because no recovered symbols prove original spellings.
- Owner: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Parent memory range: [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md).
- Autogen parent: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), because the parser's sole caller is source-emitting [UID:0001ZH][0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler](by-memory/0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler.md) and the support class declares the complete record layout.
- Confidence: very strong for behavior, boundaries, field offsets, source placement, and `std::vector` source shape; capped by no direct routes for the four accessors, unrecovered original method visibility/spellings, and the descriptive `symbolAttribute` name.

## Layout Inferred

The parser fills a 560-byte reward entry:

| Offset | Size | Meaning |
| --- | ---: | --- |
| `+0x000` | 16 bytes | `wchar_t rankStartText[8]` |
| `+0x010` | 16 bytes | `wchar_t rankEndText[8]` |
| `+0x020` | 512 bytes | `wchar_t symbolText[256]` |
| `+0x220` | 1 byte | `symbolFrameIndex`, default `-1` |
| `+0x221` | 1 byte | `symbolAttribute`, an inferred/descriptive name for the extra packet byte |
| `+0x222` | 2 bytes | alignment/reserved bytes not written by this parser |
| `+0x224` | 12 bytes | `std::vector<RankingRewardItem> items`; MSVC stores begin/end/capacity pointers in this 12-byte object |

Each reward item row is 520 bytes: `wchar_t name[256]`, a 32-bit `quantity`, a 16-bit `iconId`, one `paletteIndex` byte at `+0x206`, and one reserved/alignment byte accounted for by the full `0x208` stride.

## Behavior

Adjacent accepted helpers cover construction, this parser/accessor group, and indexed item access:

| Range | Behavior |
| --- | --- |
| [UID:000202][0x0045d790-0x0045d7c2.RankingRewardEntryVectorInit](by-memory/0x0045d790-0x0045d7c2.RankingRewardEntryVectorInit.md) | source-ready `RankingRewardEntry` default constructor; initializes the nested reward-item vector at `+0x224/+0x228/+0x22c` and default `symbolFrameIndex` byte at `+0x220`, with no-function/no-xref confidence cap |
| [UID:000203][0x0045d7d0-0x0045d7db.RankingRewardEntryItemVectorUnwindThunk](by-memory/0x0045d7d0-0x0045d7db.RankingRewardEntryItemVectorUnwindThunk.md) | compiler cleanup thunk for the nested item vector |
| [UID:000204][0x0045db60-0x0045db9e.RankingRewardEntryItemAt](by-memory/0x0045db60-0x0045db9e.RankingRewardEntryItemAt.md) | source-ready `ItemAt(int index)` using `items.size()` and `&items[index]` while preserving the original no-negative-index check |

`0x0045d7e0` is a `__thiscall`-shaped member with one explicit raw packet pointer and a consumed-byte integer return. The sole caller passes a stack `RankingRewardEntry` as `this`, adds the returned value to its packet offset, appends/deep-copies the parsed entry into the dialog vector, and destroys the temporary nested vector.

The parser preserves this exact packet order:

1. Read one unsigned byte length, copy that many bytes, convert with `MultiByteToWideChar(CP_ACP, 0, ...)`, terminate, and copy into `rankStartText[8]`.
2. Repeat the same counted conversion into `rankEndText[8]`.
3. Repeat the same counted conversion into `symbolText[256]`.
4. Read signed `symbolFrameIndex`, then packet-backed `symbolAttribute`.
5. Read the item count as a signed byte. Counts `0x80-0xff` therefore execute no loop, matching the signed comparison in the binary.
6. For each positive item, convert a counted name into `name[256]`, read big-endian 32-bit `quantity`, big-endian 16-bit `iconId`, read one `paletteIndex` byte, and call `items.push_back(item)`.

There are no packet-bound checks, no stateful cursor object, no UTF-8 conversion, and no defensive normalization. `RankingRewardItem item;` is deliberately not value-initialized: the binary writes the named fields and copies/appends the complete `0x208` row without clearing unused name tail or final alignment byte. Direct `wideText[wideLength]` indexing also preserves the observed compiler range-failure path if conversion returns 256.

The capacity-full lowering of the exact `items.push_back(item)` statement is [UID:0003X6][0x0045eae0-0x0045ec43.RankingRewardItemVectorInsertWithGrowth](by-memory/0x0045eae0-0x0045ec43.RankingRewardItemVectorInsertWithGrowth.md), now documented at `92/93` as source-declared/generated-binary `std::vector<RankingRewardItem>` support. Its formal covered-by marker points back to this parser body; no second source operation, raw-vector wrapper, or standalone growth-helper body should be emitted.

Complete function inventory:

| Range | Behavior |
| --- | --- |
| `0x0045d7e0-0x0045db06` | `int RankingRewardEntry::ParseFromPacket(const unsigned char *packet)`; returns exact bytes consumed |
| `0x0045db10-0x0045db13` | `GetRankStartText()` returns `this`, the mutable rank-start buffer |
| `0x0045db20-0x0045db24` | `GetRankEndText()` returns `this + 0x10`, the mutable rank-end buffer |
| `0x0045db30-0x0045db4e` | raw `GetItemCount() const` computes signed `(itemsEnd-itemsBegin)/0x208`; source form is `static_cast<int>(items.size())` |
| `0x0045db50-0x0045db54` | `GetSymbolText()` returns `this + 0x20`, the mutable symbol-text buffer |

IDA reports no ordinary callers for any of the four accessors. The three text getters are modeled leaf functions; `0x0045db30` has no IDA function object but bounded bytes/disassembly prove a complete 30-byte reciprocal-division method. Their no-route state caps original-name/public-API confidence but does not turn executable source methods into padding or compiler-generated code.

## Raw Code Evidence

Accepted current MCP evidence used for this source-ready decision comes from NexusTK IDB session `359c7886` through healthy transport session `5f9c0aa6-61f3-4f8d-9734-17e5afeab010`. Health request `4` reported `status:ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, image base `0x400000`, and ready auto-analysis, Hex-Rays, and string cache.

- Requests `6-8` confirmed parser function `0x0045d7e0-0x0045db06`, size `0x326` / 806 bytes, one explicit pointer argument, exact decompile, and integer consumed-byte return.
- Requests `9-13` confirmed sole parser caller `0x0045debf` in source-emitting [UID:0001ZH][0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler](by-memory/0x0045ddd0-0x0045df8c.RankingRewardInfoPacketHandler.md), parser stack/callee/byte facts, and the packet-handler temporary-entry copy/cleanup flow.
- Parser callees include `GetMemoryMan`, `MemoryMan::MemmoveWrapper`, `MultiByteToWideChar`, `wcscpy_s`, `PacketBufferReadUInt32BE`, `PacketBufferReadUInt16BE`, item-vector growth helper `0x0045eae0`, and compiler stack-cookie/range-failure support.
- Requests `14-18` bounded the complete `0x0045dae0-0x0045db60` instruction inventory. `0x0045db30-0x0045db4e` is a complete raw method with no IDA function object, no xrefs, expected raw-start decompile failure, and signed reciprocal division by 520 using multiplier `0x7e07e07f`; it returns the nested item count.
- Requests `20-22` confirmed the three modeled text getters return offsets `+0x000`, `+0x010`, and `+0x020` and have no callers.
- Request `23` found zero VA or RVA pointer-pattern hits for the parser and all four accessor starts. Request `24` found no named `RankingReward` or `PacketBuffer` IDA types, preserving the source-name confidence cap.
- Requests `26-31` confirmed paint traversal, the sole growth-helper edge, and inline `std::vector<RankingRewardItem>` access at entry offset `+0x224`.
- Exact conversions checked in request `25`: parser size `0x326`, entry size `0x230`, item size `0x208`, vector offsets `0x224/0x228/0x22c`, raw count size `0x1e`, and text capacity `0x100`.
- Boundary bytes are `0xcc` alignment around every body: parser end to `db10`, text getters to their successors, raw count end `db4e` to `db50`, and UID0001ZF end `db54` to separate [UID:000204][0x0045db60-0x0045db9e.RankingRewardEntryItemAt](by-memory/0x0045db60-0x0045db9e.RankingRewardEntryItemAt.md).

Negative evidence is equally material: there are no accessor xrefs, no absolute pointer routes, no vtable slots, no foreign code/data in the pads, no second parser caller, no second growth-helper caller, and no evidence for UTF-8, a stateful decoder, packet bounds checks, unsigned item count, zero-filled item rows, or a separate source owner.

## Reconstruction Notes

The accepted source shape is `RankingRewardEntry::ParseFromPacket(const unsigned char *packet)` plus `GetRankStartText`, `GetRankEndText`, `GetItemCount`, and `GetSymbolText`. These names are descriptive/inferred, not recovered symbols; no-route evidence prevents a public-API or original-spelling claim, but exact behavior and ordinary source-method shape support out-of-line first-draft emission.

Use `std::vector<RankingRewardItem> items`, not exposed begin/end/capacity fields. Constructor, raw count, `ItemAt`, inline append, growth, clone, destroy, and paint traversal independently establish that source abstraction. Do not introduce a `PacketReader`, bounds-checked cursor, UTF-8 conversion, defensive count normalization, item zero-initialization, or negative-index behavior absent from the binary.

Historical blocker: the 2026-06-30 formal no-code comment was correct for then-missing packet/vector APIs. B002's accepted post-ZG/post-ZH support supplied `std::vector`, scalar readers, parser declaration, and the source-emitting sole caller. The current accepted source body resolves the remaining counted-string and no-caller accessor decisions; the historical no-code state is superseded, not current.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 91 | Current MCP closes the complete parser/four-accessor inventory, discovers and bounds the raw item-count body, confirms sole caller/callees/pads/negative routes, resolves exact packet/scalar/memory/vector source dependencies, and supplies formal source for all five methods. |
| Confidence | 92 | Parser, caller, layout, constructor, count, index, append/growth, clone/destroy, and paint evidence agree on `RankingRewardEntry` and `std::vector<RankingRewardItem>`. Confidence remains below final provenance because original method spellings/visibility are unrecovered, all four accessors lack routes, and `symbolAttribute` is descriptive. |
| Reconstructable | true | This is project-owned ranking reward parsing/accessor behavior attached to the ranking dialog source parent. |

## Cross-References

- [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md)
- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md)
- [UID:0003X6][0x0045eae0-0x0045ec43.RankingRewardItemVectorInsertWithGrowth](by-memory/0x0045eae0-0x0045ec43.RankingRewardItemVectorInsertWithGrowth.md)

## Changes

### 2026-07-12 - B002 UID0003X6 covered-by synchronization

- Preserved this page's `91/92`, complete five-body formal source, exact `RankingRewardItem item;` non-value-initialization, and literal `items.push_back(item)` operation unchanged.
- Added only the accepted target-specific relation: UID0003X6 is the `92/93` compiler/STL full-capacity lowering covered by this parser statement and must not emit a duplicate helper body.

### 2026-07-12 - B001 UID0001ZF accepted source-quality callback

- Before: `86/90`, owner/emitter UID0000BP, reconstructable true, and a formal no-code comment that deferred the parser body and three documented text accessors; raw `0x0045db30-0x0045db4e` was absent from the inventory.
- Changed to: `91/92`, owner/emitter and reconstructable state unchanged, with exact source for `ParseFromPacket`, three text getters, and raw `GetItemCount`; the five methods remain one group and emit through UID0000BP in `RankingDialog.cpp`.
- Summary/evidence: accepted report `0001ZF-RankingRewardEntryParseAndAccessors-source-quality.md` preserves current MCP session `359c7886`, parser range/size/caller/callees, signed counted-string protocol, CP_ACP conversion, consumed-byte return, no packet bounds, `0x230` entry and `0x208` item layouts, non-value-initialized rows, `std::vector` append/count semantics, exact pads, raw reciprocal-division item-count body, no accessor xrefs/pointer routes, descriptive-name caps, and rejected PacketReader/raw-container/defensive rewrites. B002's parser declaration, source-emitting UID0001ZH caller, vector support, and post-ZG exact children remain already-present support.

### 2026-07-12 - B002 UID0001ZH dependency clarification

- Historical state at that callback: documentation/formal no-code proof only; metadata remained `86/90` and this mixed parser/accessor page remained a separate source-quality task.
- Summary/evidence: UID0000BP declared `RankingRewardEntry::ParseFromPacket(const unsigned char *packet)`, UID0001ZH emitted a caller, and accepted PacketBuffer readers plus `std::vector<RankingRewardItem>` resolved the old packet/vector API wording. The then-remaining counted-string/accessor decision is superseded by the B001 accepted source-quality callback above.

### 2026-07-03 - B004 UID000202 adjacent constructor sync

- Historical state at that callback: documentation only; parser metadata and formal no-code proof remained unchanged.
- Summary/evidence: accepted B004 report for [UID:000202][0x0045d790-0x0045d7c2.RankingRewardEntryVectorInit](by-memory/0x0045d790-0x0045d7c2.RankingRewardEntryVectorInit.md) reclassified the adjacent raw initializer as source-ready `RankingRewardEntry::RankingRewardEntry()` with exact no-function/decompile/xref/pointer/padding/temporary-parity evidence. Its then-current packet/vector blocker is superseded by accepted vector support and this B001 callback.

### 2026-06-30 - B006 narrowed empty-emitter callback

- Historical before: `86/90`, direct owner/emitter [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), formal C++ blank.
- Historical change: score and owner/emitter unchanged; formal C++ received the then-accepted no-code proof, now superseded by source-ready formal C++.
- Summary/evidence: current MCP request id `2016` reconfirmed three length-prefixed packet strings parsed into `rankStartText[8]`, `rankEndText[8]`, and `symbolText[256]`, packet-backed bytes at `+0x220/+0x221`, and variable `0x208` `RankingRewardItem` appends. [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md) now emits the support record declarations, leaving packet-reader/string-conversion interfaces and nested item-vector API as the concrete source-body blockers.

### 2026-06-16 - B001 ranking reward/vector source-quality execution

- Before: `82/88`, owner/emitter [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Changed to: `86/90`, direct owner/emitter [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md), final C++ blank.
- Summary/evidence: B001 rechecked parser caller `0x0045debf`, opcode `0x7d` subcommand `5`, the three parsed text fields, `symbolFrameIndex`, packet-backed `symbolAttribute`, 560-byte `RankingRewardEntry` stride, and 520-byte `RankingRewardItem` stride. Numeric `minRank`/`maxRank`, generic title/reward text, and padding interpretations for `+0x220/+0x221` were rejected.

### 2026-06-04 - Live IDA refresh and parent assignment

- Before: score was `70/85`, reconstructable/autogen metadata was blank, and evidence relied on the earlier parser summary without packet-handler copy-path details.
- Changed to: score is now `82/88`, marked reconstructable, and attached to [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Summary/evidence: live IDA confirms parser range `0x0045d7e0-0x0045db06`, sole caller `0x0045debf`, handler opcode `0x7d` subcommand `5`, temporary 560-byte reward entries, 520-byte item rows, append helper `0x0045eae0`, destroy helper `0x0045f160`, tiny accessor bodies, and surrounding `0xcc` padding.

### 2026-05-27 - Added adjacent raw reward-entry helpers

- Before: the page documented the parser and three tiny accessors but left the adjacent UNKNOWN rows unresolved.
- Changed to: linked exact pages for the raw initializer at `0x0045d790`, compiler cleanup thunk at `0x0045d7d0`, and item-index accessor at `0x0045db60`.
- Summary/evidence: IDA MCP byte/disassembly audit shows complete raw bodies at those starts, with `0xcc` padding around them and no modeled IDA functions for the raw initializer/accessor starts.
