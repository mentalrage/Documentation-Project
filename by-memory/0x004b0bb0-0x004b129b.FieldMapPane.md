*** UID:00014Z | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
FieldMapPane::FieldMapPane(
    const wchar_t *sourcePath,
    short entryCount,
    short initialEntryIndex,
    const Point *displayPositions,
    const wchar_t *const *labels,
    const unsigned short *auxiliaryValues,
    const unsigned short *requestFieldIds,
    const Point *requestPositions)
    : Pane(0),
      m_fieldEntryCount(entryCount),
      m_selectedEntryIndex(initialEntryIndex),
      m_cancelEntryIndex(initialEntryIndex),
      m_requestInFlight(false)
{
    BuildFieldMapPath(sourcePath, m_fieldMapEpfPath);
    wcscat_s(m_fieldMapEpfPath, 128, L".EPF");
    g_pfnCharUpperBuffW(m_fieldMapEpfPath, (DWORD)wcslen(m_fieldMapEpfPath));

    for (short i = 0; i < entryCount; ++i)
    {
        FieldMapEntry &entry = m_fieldEntries[i];
        entry.m_displayPosition = displayPositions[i];
        wcscpy_s(entry.m_label, 64, labels[i]);
        entry.m_auxiliaryValue = auxiliaryValues[i];
        entry.m_requestFieldId = requestFieldIds[i];
        entry.m_requestPosition = requestPositions[i];
    }
}

bool FieldMapPane::HandlePointerOrMouseEvent(Event *event)
{
    if (event->m_type == kEventLeftButtonDown)
    {
        for (short i = 0; i < m_fieldEntryCount; ++i)
        {
            RectBounds bounds;
            CalculateFieldItemRect(i, &bounds);
            if (PointInRect(event->m_payload.m_pointer.m_y,
                            event->m_payload.m_pointer.m_x,
                            &bounds) &&
                !m_requestInFlight)
            {
                SendFieldMapRequest(i);
            }
        }
    }
    return true;
}

bool FieldMapPane::HandleKeyOrTextEvent(Event *event)
{
    const unsigned char key = g_pEventMan->TranslateEventKey(
        event->m_payload.m_key.m_key,
        event->m_payload.m_key.m_modifiers);

    if (event->m_type == kEventKeyDown)
    {
        switch (key)
        {
        case 0x0d:
            if (!m_requestInFlight)
                SendFieldMapRequest(m_selectedEntryIndex);
            break;
        case kPaneKeyEscape:
        case 0x71:
            SendFieldMapRequest(m_cancelEntryIndex);
            break;
        case kPaneKeyLeft:
            m_selectedEntryIndex =
                (short)((m_selectedEntryIndex + 1) % m_fieldEntryCount);
            InvalidateRect(&m_visibleBounds);
            break;
        case kPaneKeyRight:
            m_selectedEntryIndex =
                (short)((m_selectedEntryIndex + m_fieldEntryCount - 1) % m_fieldEntryCount);
            InvalidateRect(&m_visibleBounds);
            break;
        }
    }
    return true;
}

void FieldMapPane::OnPaint()
{
    EPFTileContext context;
    g_pEPFLib->LookupLayoutEntry(m_fieldMapEpfPath, 0, &context);
    g_pfnBlitSprite(this, &context, &context.bounds, &m_visibleBounds, 0, 0, 0);

    SetDrawMode(0);
    for (short i = 0; i < m_fieldEntryCount; ++i)
    {
        RectBounds bounds;
        CalculateFieldItemRect(i, &bounds);

        SetDrawColor(i == m_selectedEntryIndex ? 37 : 4);
        SetDrawMode(2);
        g_pfnFillRect(this, &bounds);

        SetDrawMode(0);
        SetDrawColor(0x80);
        DrawRectFrame(&bounds);

        SetDrawMode(1);
        MoveTo(bounds.left + 6, bounds.bottom - 6);
        DrawWideText(
            m_fieldEntries[i].m_label,
            (int)wcslen(m_fieldEntries[i].m_label));
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: live IDA confirms exact modeled endpoints, vtable stores/slots, request/path helper callers, raw rect-helper bytes, and alignment before unrelated readable-string helpers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b0bb0-0x004b129b FieldMapPane

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: class aggregate over known child methods
- Likely source module: [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md)
- Confidence: very strong for all modeled functions, exact method/switch/padding ranges, vtable facets, provider interactions, request/path helpers, and aggregate endpoint; high for retaining the unmodeled rectangle helper as source-authored code despite its absent live caller.
- Evidence basis: live IDA MCP `py_eval` on 2026-06-04 against `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` (imagebase `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`).
- Reconstruction note: this aggregate emits exactly the constructor, pointer/mouse handler, key/text handler, and `OnPaint` definitions. Rectangle, request, and path helpers remain emitted only from their exact child pages, preventing duplicate child coverage while completing the source unit.

## Covered Ranges

| Range | Current method | Notes |
| --- | --- | --- |
| `0x004b0bb0-0x004b0d2b` | `FieldMapPane` | Initializes pane state, EPF path, field metadata, and three `FieldMapPane` vtable views. |
| `0x004b0d30-0x004b0e50` | `HandleMouseEvent` | Hit-tests entries and dispatches selected field request. |
| `0x004b0e50-0x004b0f1b` | `HandleKeyEvent` | Handles keyboard navigation, submit, and cancel. |
| `0x004b0fb0-0x004b112a` | `RenderFieldEntries` | Draws entries, highlight, and labels. |
| `0x004b1130-0x004b119d` | `CalculateFieldItemRect` | Retained private helper that computes a field-entry rectangle from `centerY`, `centerX`, measured `label`, and `RectBounds`; formal C++ belongs to UID 000150, while IDA still has no function object or current xrefs. |
| `0x004b11a0-0x004b1242` | `SendFieldMapRequest` | Builds opcode `0x3f` field-map packet and sets the request flag. |
| `0x004b1242-0x004b124d` | `AdjustorDeletingDestructorA0` | Adjusts `this - 0xa0` and forwards to `0x004b1260`. |
| `0x004b124d-0x004b1258` | `AdjustorDeletingDestructorA4` | Adjusts `this - 0xa4` and forwards to `0x004b1260`. |
| `0x004b1260-0x004b129b` | `ScalarDeletingDestructor` | Runs destructor/base cleanup and conditional free. |

## Evidence

- IDA MCP confirms `0x004b0bb0`, `0x004b0d30`, `0x004b0e50`, `0x004b0fb0`, `0x004b11a0`, `0x004b1242`, `0x004b124d`, and `0x004b1260` as real functions.
- IDA MCP reports `0x004b1130` is not currently a function. A read-only byte scan shows code from `0x004b1130` through `retn 8` at `0x004b119a`, with alignment at `0x004b119d` and no current xrefs to `0x004b1130`.
- `0x004b11a0` current xrefs are only from `HandleMouseEvent` and `HandleKeyEvent`, so it belongs with the FieldMap input/request flow rather than `FittingRoomDownloadControlPane`.
- Behavior is map-domain UI, but the final dialog/module owner needs more caller research.
- 2026-05-28 IDA MCP `lookup_funcs 0x004b129a` confirms it is the final byte of `sub_4B1260`; the exclusive end is `0x004b129b`, followed by `0xcc` padding before unrelated generic string/readable helpers.

## 2026-06-01 IDA MCP Recheck

- `lookup_funcs` confirms real functions at `0x004b0bb0` (`0x17b` bytes), `0x004b0d30` (`0x120`), `0x004b0e50` (`0xcb`), `0x004b0fb0` (`0x17a`), `0x004b11a0` (`0xa2`), `0x004b1242` (`0x0b`), `0x004b124d` (`0x0b`), `0x004b1260` (`0x3b`), and `0x004b8220` (`0x64`).
- `lookup_funcs` still reports `0x004b1130`, `0x004b119a`, and `0x004b119d` as `Not a function`, so the rectangle helper remains raw/boundary debt.
- `xrefs_to` reports vtable/data refs for `HandleMouseEvent` at `0x0061a418`, `HandleKeyEvent` at `0x0061a41c`, and `RenderFieldEntries` at `0x0061a40c`.
- `xrefs_to` reports constructor stores to FieldMapPane vtables at `0x0061a3c8`, `0x0061a414`, and `0x0061a444`.
- `callers 0x004b11a0` reports exactly three callers inside `0x004b0d30`/`0x004b0e50`; `callers 0x004b8220` reports one caller at `0x004b0c0d` inside the constructor; `callers 0x004b1130` reports none.
- `callees` shows the constructor uses `0x004b8220`, wide-string copy/append helpers, and a frame/resource loader; mouse/render paths use `0x004baa70`/`0x004baaa0` text extent helpers; request sending uses packet helpers `0x00575380`, `0x005753a0`, and `0x00574bb0`.

## 2026-06-04 IDA MCP Recheck

- `py_eval` confirms the current binary identity (`NexusTK.exe`, imagebase `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`) and modeled endpoints: constructor `0x004b0bb0-0x004b0d2b`, mouse handler `0x004b0d30-0x004b0e50`, key handler `0x004b0e50-0x004b0f1b`, render method `0x004b0fb0-0x004b112a`, request helper `0x004b11a0-0x004b1242`, adjustors `0x004b1242-0x004b124d` and `0x004b124d-0x004b1258`, scalar destructor `0x004b1260-0x004b129b`, and path helper `0x004b8220-0x004b8284`.
- The constructor stores `FieldMapPane` vtables at `0x0061a3c8`, `0x0061a414`, and `0x0061a444`; vtable slots bind render `0x0061a40c -> 0x004b0fb0`, mouse `0x0061a418 -> 0x004b0d30`, key `0x0061a41c -> 0x004b0e50`, and destructor/adjustors `0x0061a3c8 -> 0x004b1260`, `0x0061a414 -> 0x004b1242`, `0x0061a444 -> 0x004b124d`.
- `0x004b11a0` has exactly three code refs from FieldMap input handlers (`0x004b0dfe`, `0x004b0ea7`, `0x004b0f0e`) and decompiles to opcode `0x3f`, three serialized values from a `0x94`-byte entry record, a seven-byte send through `0x00574bb0`, and request-in-flight byte `this+0x95fe`.
- `0x004b8220` has one code ref from the constructor at `0x004b0c0d` and decompiles to a wide path-stem copy helper that scans backward for `'.'`, copies with `_memmove`, and writes a wide NUL.
- 2026-06-18 B002 source-quality reanalysis resolves the constructor path sequence: `this+0xf8` is `wchar_t m_fieldMapEpfPath[0x80]`; the constructor calls [UID:00015U][0x004b8220-0x004b8284.BuildFieldMapPath](by-memory/0x004b8220-0x004b8284.BuildFieldMapPath.md), appends pooled use-site `L".EPF"` from [UID:0003P9][0x0061a44c-0x0061a458.SharedEpfSuffixString](by-memory/0x0061a44c-0x0061a458.SharedEpfSuffixString.md) with `_wcscat_s(...,0x80,...)`, computes the resulting length, and calls wide API dispatch slot `0x0069be58`, resolved as `CharUpperBuffW`, to uppercase the completed EPF path.
- Raw disassembly shows `0x004b1130-0x004b119d` computes a rectangle from `this+0x1f8 + index*0x94`, label measurement `0x004baa70`, line height `0x004baaa0`, and four rectangle writes before `retn 8`; IDA still reports no function object and no xrefs for `0x004b1130`.
- Bytes `0x004b129b-0x004b12a0` remain alignment before the unrelated `0x004b12a0` helper.

## 2026-06-17 B003 Source-Quality Gate Recheck

- B003 rechecked the active `85/85` gate for this aggregate and recommends raising the page to `85/90` while keeping formal C++ blank.
- Constructor reachability is bounded: IDA reports no direct xrefs to `0x004b0bb0`; a PE-aware direct `call`/`jmp` scan found no edges to `0x004b0bb0`; and a raw absolute-VA byte scan found no pointer bytes for `0x004b0bb0`. This leaves the allocation site unresolved but does not weaken FieldMapPane ownership because the constructor stores the three FieldMapPane vtables and initializes the exact FieldMap path, field-entry, selection, and request state.
- Raw helper liveness is bounded: IDA still has no function object or incoming xrefs for `0x004b1130`; PE-aware direct `call`/`jmp` scanning found no edges to `0x004b1130`; a raw absolute-VA byte scan found no pointer bytes for `0x004b1130`; and the helper signature is unique. Keep [UID:000150][0x004b1130-0x004b119d.FieldMapRectHelper](by-memory/0x004b1130-0x004b119d.FieldMapRectHelper.md) as an exact FieldMapPane child documenting an unreferenced rectangle-helper body.
- Request packet naming was source-quality bounded but structurally resolved: [UID:000151][0x004b11a0-0x004b1242.FieldMapRequestHelper](by-memory/0x004b11a0-0x004b1242.FieldMapRequestHelper.md) has exactly three FieldMapPane callers, opcode `0x3f`, seven-byte send length, and payload order from entry offsets `+0x8a`, `+0x90`, and `+0x8c`.
- The unresolved constructor caller and raw-helper liveness cap aggregate scores above `85/90`, but they no longer justify keeping this aggregate below 85 completion because all ranges, source ownership, child support pages, field layout, request order, and output route are documented.

## 2026-06-27 B006 Request-Helper Source-Quality Update

- B006 accepted implementation for [UID:000151][0x004b11a0-0x004b1242.FieldMapRequestHelper](by-memory/0x004b11a0-0x004b1242.FieldMapRequestHelper.md), so that exact child now carries formal first-draft C++ even though this aggregate stays C++ blank.
- Live IDA MCP session `80de0a67` reconfirmed the request child range `0x004b11a0-0x004b1242`, direct FieldMap-only callers at `0x004b0dfe`, `0x004b0ea7`, and `0x004b0f0e`, opcode `0x3f`, seven-byte send, request-in-flight byte at object `+0x95fe`, unique range signature, boundary bytes before the next adjustor thunk, and negative direct pointer-pattern evidence for `0x004b11a0`, `0x004b1130`, and `0x004b0bb0`.
- Packet helper names are source-quality through support docs and live use counts: `PacketBufferWriteUInt8` (`0x00575380`, 1114 code xrefs), `PacketBufferWriteUInt16BE` (`0x005753a0`, 222 code xrefs), and `Socket::QueueAndSendPacket` (`0x00574bb0`, 416 code xrefs). The sender global is `g_packetSender`.
- The request-facing entry fields are now `requestFieldId` at entry `+0x8a`, `requestX` at `+0x90`, and `requestY` at `+0x8c`. The old `requestCoordA/requestCoordB` names are historical structural placeholders, not final packet names.
- The stale generated `FittingRoomDownloadControlPane::SendFieldMapRequest` name is rejected as owner pollution; the child remains routed through [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md).

## 2026-06-27 B006 Rectangle-Helper Source-Quality Update

- B006 accepted implementation for [UID:000150][0x004b1130-0x004b119d.FieldMapRectHelper](by-memory/0x004b1130-0x004b119d.FieldMapRectHelper.md), so that exact child now carries first-draft `FieldMapPane::CalculateFieldItemRect(short entryIndex, RectBounds *outRect)` C++ while this aggregate stays blank to avoid duplicate child emission.
- Live IDA MCP session `398b87c1` preserves the raw-helper caveat: no modeled function object at `0x004b1130`, no incoming xrefs, no direct `call`/`jmp`, no direct VA/RVA pointer hits, exact `0xcc` padding before and after, final `ret 8` at `0x004b119a-0x004b119c`, and a unique concrete signature for `0x004b1130-0x004b119d`.
- The helper body is now treated as retained source-authored FieldMapPane code rather than a no-code blocker. It indexes `this + 0x1f8 + entryIndex * 0x94`, reads `FieldMapEntry::centerY`, `centerX`, and `label`, calls `GetTextWidth` and `GetLineHeight`, and writes a `RectBounds` output in accepted `left/top/right/bottom` layout.
- Mouse and render siblings duplicate the same rectangle formula inline through their own `0x004baa70`/`0x004baaa0` call pairs. Do not claim they call `0x004b1130` unless later caller evidence proves a direct edge.
- The fitting-room, generic GrafPort, generic RectBounds, padding, and no-code ownership alternatives are rejected for UID 000150; GrafPort text helpers and `RectBounds` remain dependencies, not owners.

## Historical Draft Shape (Superseded)

The following early scalar-field sketch is retained only as superseded provenance. Its physical offsets were useful, but current formal source uses `Point m_displayPosition`, `Point m_requestPosition`, and the complete class declaration on UID `00004U`. The aggregate formal CPP now correctly contains only its four authored method definitions; helper formal CPP remains on exact child UIDs `000150`, `000151`, and `00015U`, so no duplicate emission occurs.

```cpp
struct FieldMapEntry {
    int32_t centerY;          // +0x00
    int32_t centerX;          // +0x04
    wchar_t label[0x40];      // +0x08
    uint16_t auxFieldWord;    // +0x88
    uint16_t requestFieldId;  // +0x8a
    int32_t requestY;         // +0x8c, serialized after requestX
    int32_t requestX;         // +0x90, serialized before requestY
};

class FieldMapPane {
    wchar_t fieldMapEpfPath[0x80]; // object +0x0f8
    FieldMapEntry entries[];       // object +0x1f8, stride 0x94
    uint16_t entryCount;           // object +0x95f8
    uint16_t selectedIndex;        // object +0x95fa
    uint16_t cancelIndex;          // object +0x95fc
    bool requestInFlight;          // object +0x95fe
};
```

## 2026-08-18 B007 Aggregate Behavior And Range Closure

### Exact authored definitions

- Constructor `[0x004b0bb0,0x004b0d2b)` builds and uppercases the EPF path, initializes `m_fieldEntryCount`, selected/cancel indices, and request state, then copies eight source parameters into the exact entry layout. Its direct caller remains unresolved, but its three FieldMapPane vtable stores and complete field initialization prove class ownership.
- Pointer/mouse handler `[0x004b0d30,0x004b0e50)` is entered through EventHandler slot `0x0061a418`. It only processes left-button-down, iterates all entries without breaking, hit-tests `(pointer.y,pointer.x)`, and sends each hit only while no request is in flight.
- Key/text handler `[0x004b0e50,0x004b0f1b)` is entered through EventHandler slot `0x0061a41c`. It translates the key first; Return sends selected only when idle; Escape/`0x71` always send cancel; left advances and right decrements with exact modulo expressions; navigation invalidates the pane. The source preserves binary zero-count modulo behavior.
- `OnPaint` `[0x004b0fb0,0x004b112a)` is the primary Pane slot `0x0061a40c`. It loads EPF layout zero, blits it, then fills, frames, positions, and draws every label in exact draw-mode/color order.

### Complete internal compiler inventory

Alignment is `[0x004b0d2b,0x004b0d30)`, `[0x004b0fa6,0x004b0fb0)`, `[0x004b112a,0x004b1130)`, `[0x004b119d,0x004b11a0)`, `[0x004b1258,0x004b1260)`, and external successor alignment `[0x004b129b,0x004b12a0)`. Key-handler compiler data is one byte at `[0x004b0f1b,0x004b0f1c)`, jump table `[0x004b0f1c,0x004b0f30)`, and selector table `[0x004b0f30,0x004b0fa6)`. These ranges have the literal read/xref relationships documented above and no authored emitter.

The EventHandler and TimerHandler adjustors at `[0x004b1242,0x004b124d)` and `[0x004b124d,0x004b1258)` tail-jump to scalar deleting destructor `[0x004b1260,0x004b129b)`. They are compiler lowering of inherited facets and implicit virtual destruction, not three source-authored bodies. The three vtable facets are documented on UID `0003P8` and are regenerated from source declarations.

### Child and provider boundaries

Retained rectangle helper `[0x004b1130,0x004b119d)`, request helper `[0x004b11a0,0x004b1242)`, and path helper `[0x004b8220,0x004b8284)` emit only from exact children. The request helper has three exact callers; the path helper has one constructor caller; the rectangle helper has no incoming xref but a complete source-shaped body and duplicated formulas in pointer/paint siblings.

Shared providers remain external: `g_pEventMan`, `g_pEPFLib`, `g_packetSender`, `g_pfnBlitSprite`, `g_pfnFillRect`, and `g_pfnCharUpperBuffW`. The aggregate consumes their typed contracts and does not define or re-own them. The accepted formal CPP preserves call ordering, event payload y/x order, packet x-then-y order, draw modes/colors, and all no-guard edge behavior.

Current score is `92/94`. It is not lower because all ranges, behaviors, dependencies, child boundaries, compiler products, and formal method source are resolved. It is not higher because constructor registration, retained-helper liveness, exact original lexical spellings, and final rebuilt integration remain below final-audit proof.

## Ownership Decision

Place under `map/FieldMapPane.cpp`. Include `0x004b11a0` with this module and keep `0x004b1130` as a retained private `CalculateFieldItemRect` helper whose exact body is source-ready but whose liveness remains unproven. The former larger-field-dialog alternative is retained as historical provenance; current evidence found no competing source owner or family.

## Cross-References

- [UID:00004U][FieldMapPane](by-class/FieldMapPane.md)
- [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md)
- [UID:000150][0x004b1130-0x004b119d.FieldMapRectHelper](by-memory/0x004b1130-0x004b119d.FieldMapRectHelper.md)
- [UID:000151][0x004b11a0-0x004b1242.FieldMapRequestHelper](by-memory/0x004b11a0-0x004b1242.FieldMapRequestHelper.md)
- [UID:00022C][0x004b12a0-0x004b13ce.VirtualReadableStringHelpers](by-memory/0x004b12a0-0x004b13ce.VirtualReadableStringHelpers.md)
- [UID:00025B][0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData](by-memory/0x0061a3c4-0x0061a45c.FieldMapPaneReadOnlyData.md)

## Changes

- 2026-08-18 B007 whole-file implementation callback:
  - Raised `85/90` to `92/94`, set `EMITTER_POSITION_OPTIONAL:10`, and populated exact constructor, pointer, key, and paint formal CPP while keeping helper bodies on their exact children.
  - Added exact authored/compiler range inventory, per-method behavior, facet routing, provider boundaries, source placement, score rationale, and superseded early scalar-layout history.

- 2026-06-01 documentation pass:
  - Before: metadata remained at `50/65`, reconstructable and parent fields were blank, and generated owner wording was not separated from IDA evidence.
  - Changed to: `78/84`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000JA`.
  - Summary/evidence: IDA MCP reconfirmed method boundaries, vtable refs, constructor vtable stores, request/path helper callers, raw rect-helper non-function status, and the aggregate end boundary; C++ remains blank because final field/helper naming and the raw helper split are unresolved.
- Existing: this page and filename ended at `0x004b129a`.
- Changed to: endpoint corrected to `0x004b129b`.
- Summary and evidence: IDA MCP `lookup_funcs 0x004b129a` reports it belongs to `sub_4B1260` size `0x3b`, making the exclusive end `0x004b129b`; raw bytes show `0x004b129b-0x004b12a0` is `0xcc` padding before `sub_4B12A0`.
- 2026-06-04: Raised from `78/84` to `82/88`.
  - What existed before: the aggregate had the broad function map, but several rows used last-byte-style endpoints, generated-output wording remained, and the vtable/read-only data linkage was only partially recorded.
  - Changed to: corrected modeled function endpoints to end-exclusive ranges, added current IDA binary identity, vtable slot/store evidence, request/path helper caller evidence, raw rectangle-helper disassembly through the `retn 8`, and alignment confirmation before `0x004b12a0`.
  - Reason for score increase: the aggregate now proves the FieldMapPane method boundaries, vtable ownership, request helper ownership, constructor path helper, and raw helper limits with live IDA evidence; completion remains capped by the unmodeled/unreferenced rectangle helper and unfinished source-quality naming.
- 2026-06-17 B003 source-quality execution:
  - Before: `COMPLETION:82`, `CONFIDENCE:88`, with constructor/no-xref, raw-helper liveness, and request field names treated as below-gate blockers.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:90`; owner/emitter remain [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md), and formal C++ remains blank by explicit B003 recommendation.
  - Evidence: B003 rechecked IDA xrefs, PE-aware direct edge scans, absolute pointer scans, raw-helper signature, exact child support pages, FieldMap-only request helper callers, packet field order, source placement alternatives, and generated owner pollution. The remaining issues now cap final C++ and higher scores rather than the `85/85` documentation gate.
- 2026-06-27 B006 implementation callback:
  - Summary/evidence: incorporated accepted [UID:000151][0x004b11a0-0x004b1242.FieldMapRequestHelper](by-memory/0x004b11a0-0x004b1242.FieldMapRequestHelper.md) source-quality findings. The aggregate keeps formal C++ blank to avoid duplicate child emission; the request helper itself now emits first-draft C++ with `PacketBufferWriteUInt8`, `PacketBufferWriteUInt16BE`, `g_packetSender`, `QueueAndSendPacket`, `requestFieldId`, `requestX`, and `requestY`. The then-current raw `0x004b1130` blocker is superseded by the rectangle-helper callback below.
- 2026-06-27 B006 rectangle-helper implementation callback:
  - Summary/evidence: incorporated accepted [UID:000150][0x004b1130-0x004b119d.FieldMapRectHelper](by-memory/0x004b1130-0x004b119d.FieldMapRectHelper.md) source-quality findings. UID 000150 now emits first-draft `FieldMapPane::CalculateFieldItemRect(short entryIndex, RectBounds *outRect)` on its exact child page, with retained-helper/no-live-caller caveat, session `398b87c1` no-function/no-xref evidence, PE no-edge/no-pointer evidence, exact boundary/signature proof, text-helper xref relationships, and rejection of fitting-room/GrafPort/RectBounds/padding/no-code alternatives. Aggregate C++ remains blank to avoid duplicate child emission.
