*** UID:0002KX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000F1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000F1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
DWORD TimerMgr::FindQueuedTimerDueTick(TimerHandler* owner, int timerId) const
{
    for (int index = static_cast<int>(m_timerQueue.size()) - 1;
         index >= 0;
         --index) {
        const ScheduledTimerEvent& event = m_timerQueue[index];
        if (event.owner == owner && event.timerId == timerId) {
            return event.dueTick;
        }
    }

    return 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Public const TimerMgr query that scans scheduled timer values newest-to-oldest, returns the absolute due tick for a matching owner/timer id, returns zero on miss, has one MapPane caller and no callees, and is bounded by exact alignment padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00597dc0-0x00597e3f TimerMgrFindQueuedTimerDueTick

## 2026-07-19 UID0002KX Standard-Deque Source Closure

- Exact binary identity is `[0x00597dc0,0x00597e3f)`, 127 bytes, RVA `0x00197dc0`, raw PE offset `0x001971c0` in `.text`, with byte SHA256 `36B804A5BFC85DBD385A73AE44740FCB0ABE0493D2C6937430C307D8537404F1`.
- The function is a 61-instruction, 16-basic-block nonvirtual `thiscall` member with two 32-bit explicit arguments, `retn 8`, no callees, and exactly one direct caller at `0x0050ebf1`. Vtable, data-xref, VA, RVA, and raw-pointer scans found no alternate route.
- The receiver is `TimerMgr`. The source signature is the public const query `DWORD TimerMgr::FindQueuedTimerDueTick(TimerHandler* owner, int timerId) const`; the unrelated MapPane caller passes an `EffectObjectPane` TimerHandler facet and timer id `2`.
- The queue object beginning at `TimerMgr +0x04` is the VC6/VC7-era Dinkumware implementation of `std::deque<ScheduledTimerEvent>`, not a project `TimerMgrTimerQueue` class. Its compiler fields are proxy `+0x00`, map `+0x04`, map count `+0x08`, front `+0x0c`, and size `+0x10`; map capacity is a power of two with a grown minimum of eight.
- `ScheduledTimerEvent` is a 20-byte deque value: `TimerHandler* owner` at `+0x00`, `int timerId` at `+0x04`, `DWORD dueTick` at `+0x08`, and generic callback arguments `arg0`/`arg1` at `+0x0c`/`+0x10`. One 20-byte value occupies each deque block in this specialization.
- The machine loop starts at logical `size - 1`, computes the compiler map/block position, reads the value directly, compares owner identity and the full 32-bit timer id, returns the absolute due tick on the first newest match, and returns zero on miss. Active entries are values, so a nullable-event test or per-event allocation/free source abstraction would be wrong.
- The sole caller treats zero as no queued timer. For a hit it subtracts the current tick from the absolute due tick, and cancels/reschedules only when its requested signed action duration exceeds the remaining time.
- Exact predecessor `[0x00597db9,0x00597dc0)` is seven `0xcc` bytes with SHA256 `F145ECA66705DE68FF53C6D80E34D8F9A5B535FFA157194AA98BDB68AEA4368D`; exact successor `[0x00597e3f,0x00597e40)` is one `0xcc` byte with SHA256 `1DD8312636F6A0BF3D21FA2855E63072507453E93A5CED4301B364E91C9D87D6`.
- Historical B015/custom-ring descriptions correctly preserved the reverse search, owner/id predicate, due-tick result, and boundaries, but their named project queue, pointer slots, nullable event, raw start/count/capacity source fields, and private-method assumption are superseded by the standard-deque controls and the unrelated direct caller.
- Rejected source alternatives are a project ring-buffer class, pointer-valued events, a map/proxy-facing helper, a private method, a virtual method, unsigned timer ids, relative-delay return, forward search, null guards, allocation helpers, and a separate source file. The declaration belongs in `NexusTK/util/TimerMgr.h` and the definition in `NexusTK/util/TimerMgr.cpp`.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: scheduler lookup helper.
- Parent aggregate/context: [UID:0001KA][0x00597dc0-0x005986da.TimerMgrQueueHelpers](by-memory/0x00597dc0-0x005986da.TimerMgrQueueHelpers.md)
- Direct owner/emitter: [UID:0000F1][TimerMgr](by-class/TimerMgr.md)
- Likely source module: [UID:0000OT][TimerMgr](by-file/TimerMgr.md)
- Assignment handling: B015 source-quality reanalysis moved the direct owner/emitter from the non-emitting aggregate to [UID:0000F1][TimerMgr](by-class/TimerMgr.md). The aggregate remains range context only.
- Confidence: strong for range, owner, and behavior; `FindQueuedTimerDueTick` is a supported source-facing name while exact original spelling remains provisional.

## Behavior

Scans the embedded TimerMgr queue from newest logical entry backward, looking for a scheduled event whose owner pointer and event id match the two arguments. If a match is found, it returns the event's due tick. If no entry matches, it returns `0`.

The helper operates on the TimerMgr/TimerMgrTimerQueue layout where the queue slot array is reached through the embedded queue root, `this[4]` is the logical start index, and `this[5]` is the queue count. Each located queue record is a [UID:0001VX][ScheduledTimerEvent](by-type/by-struct/ScheduledTimerEvent.md): `record[0]` is owner, `record[1]` is event id, and `record[2]` is due tick.

Source-facing signature after B015 reanalysis: `DWORD TimerMgr::FindQueuedTimerDueTick(TimerHandler *owner, int timerId) const`. This is a private TimerMgr query method, not a standalone aggregate helper and not a `TimerMgrTimerQueue` public method.

## IDA Evidence

- IDA MCP `lookup_funcs 0x00597dc0` reports `sub_597DC0`, size `0x7f`, so the exact function range is `0x00597dc0-0x00597e3f` with exclusive end `0x00597e3f`.
- IDA MCP decompilation reads `this[5] - 1`, iterates downward, indexes the ring slot with `v7 & (capacity - 1)`, compares `a2 == *record` and `record[1] == a3`, and returns `record[2]`.
- IDA MCP `callers 0x00597dc0` reports one caller at `0x0050ebf1` inside `sub_50EB90`, which uses this as a timer-owner/event due-tick query.
- 2026-06-07 live IDA check used `NexusTK.exe` SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632` and reconfirmed the `sub_597DC0` body has no callees.
- Disassembly reads the embedded queue count from `[ecx+0x14]`, logical start from `[ecx+0x10]`, and queue-root chain from `[ecx+0x04]`; the slot lookup masks the logical index with `capacity - 1`, then the match compares owner at record `+0x00` and event id at `+0x04`.
- The match path returns the due tick from record `+0x08`; the exhausted path returns zero. The function uses `retn 8`, matching the two explicit arguments after `this`.
- Caller `sub_50EB90` queries this helper only for event id `2`; when a queued due tick is present and the requested delay would extend it, the caller cancels and reschedules the timer instead of duplicating the event.
- Endpoint bytes show the return at `0x00597e3c-0x00597e3e`, a single `0xcc` pad byte at `0x00597e3f`, and the next helper beginning at `0x00597e40`.
- 2026-06-11 Batch 192 live IDA MCP refresh on `NexusTK.exe` (`md5 4247e04e20b65d6414c7238aa8ff5515`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`) reconfirmed `sub_597DC0` as `0x00597dc0-0x00597e3f`, size `0x7f` / decimal `127` (Verified with `int_convert.py`), 61 disassembled instructions, one code ref to the start from `0x0050ebf1`, no callees, and byte `0xcc` at `0x00597e3f`.

## Score Rationale

| Metric | Before | After | Basis |
| --- | --- | --- | --- |
| Completion | 84 | 86 | Batch 192 adds a fresh IDA boundary/disassembly/caller/callee/byte audit, verified size conversion, and parent assignment under the refreshed direct queue-helper aggregate. |
| Confidence | 89 | 90 | Current IDA lookup/decompile/disassembly/caller evidence agrees with the existing TimerMgr queue model; only final source name and some queue-root type names remain provisional. |

## Cross-References

- [UID:0001KA][0x00597dc0-0x005986da.TimerMgrQueueHelpers](by-memory/0x00597dc0-0x005986da.TimerMgrQueueHelpers.md)
- [UID:0001K6][0x00597570-0x005986da.TimerMgrAndTimerQueue](by-memory/0x00597570-0x005986da.TimerMgrAndTimerQueue.md)
- [UID:0000F1][TimerMgr](by-class/TimerMgr.md)
- [UID:0001VX][ScheduledTimerEvent](by-type/by-struct/ScheduledTimerEvent.md)

## Changes

- 2026-06-06 boundary rename:
  - What existed before: filename, title, and evidence used `0x00597dc0-0x00597e3e`.
  - Changed to: `0x00597dc0-0x00597e3f`.
  - Evidence: live IDA MCP `lookup_funcs 0x00597dc0` reports `sub_597DC0` size `0x7f`, proving exclusive end `0x00597e3f`.
- 2026-06-07 Batch 008 evidence refresh:
  - Raised completion/confidence from `74/86` to `84/89` after documenting the complete backward queue scan, record offset semantics, caller usage, no-callee status, and endpoint padding.
  - Kept `AUTOGEN_PARENT_UID` blank because the child is below 85 completion and the direct TimerMgr parent candidates also fail the corrected 85/85 gate.
- 2026-06-11 Batch 192 parent-gate refresh:
  - Raised completion/confidence from `84/89` to `86/90` and set `AUTOGEN_PARENT_UID:0001KA`.
  - Evidence: live IDA MCP reconfirmed the exact `0x7f` / decimal `127` byte (Verified with `int_convert.py`) function range, 61-instruction body, sole caller at `0x0050ebf1`, no callees, owner/event comparison, due-tick return, zero return on miss, and the one-byte `0xcc` padding fence before `0x00597e40`. The refreshed direct parent [UID:0001KA][0x00597dc0-0x005986da.TimerMgrQueueHelpers](by-memory/0x00597dc0-0x005986da.TimerMgrQueueHelpers.md) now clears `85/85`; no final C++ was emitted because the queue-root type names and original method spelling are still below the `95/95` source-emission gate.
- 2026-06-22 Rule 26 incorporation of B015 TimerMgrQueueHelpers report:
  - Changed direct owner/emitter from [UID:0001KA][0x00597dc0-0x005986da.TimerMgrQueueHelpers](by-memory/0x00597dc0-0x005986da.TimerMgrQueueHelpers.md) to [UID:0000F1][TimerMgr](by-class/TimerMgr.md) because the aggregate is now non-emitting context.
  - Added first-draft child C++ for the private `TimerMgr::FindQueuedTimerDueTick` method and recorded the supported signature, raw rel32 caller `0x0050ebf1`, no-callee status, `ret 8` ABI, newest-to-oldest scan semantics, and source-placement decision.
