*** UID:0003FZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JC,0000LE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: UTF-16LE minimap `000`/`%03d`/`.mnm`/S3 URL/`%s%s/%s%s`/progress literals; B001 current no-owner recheck keeps `CANONICAL_OWNER:NONE` because no single declaration owner covers the pooled literal group, but retains `EMITTER_UIDS:0000JC,0000LE` because current IDA MCP evidence proves source-use contexts in FileDownloader message `10000` (`sub_41A750`) and MiniMapDownloader (`sub_453AA0`), with raw MiniMap-side refs at `0x00454e6f`, `0x00454e7b`, and `0x00454fcc`; both emitter UIDs resolve to valid generated file roots, and no split, merge, reclassification, shared constants owner, IDA repair, or final C++ placement is justified. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0060d7f4-0x0060d8b8 Minimap Download Wide Literals

## Status

- Address range: `0x0060d7f4-0x0060d8b8`.
- Entity kind: UTF-16LE minimap download literal group.
- Disposition: reconstructable source literals, but not assignable to one direct parent.
- Rebuild handling: `source-declared/generated-binary`.
- Canonical owner: `NONE`. The same minimap URL/suffix/progress constants are consumed by both the FileDownloader message `10000` path and the MiniMapDownloader worker path, with additional unmodeled MiniMap-neighborhood xrefs to the suffix, URL, and progress text.
- Post-migration emitter routing: `EMITTER_UIDS:0000JC,0000LE`. B002's 2026-06-13 post-migration review kept the literal group ownerless because no single declaration owner is proven, but changed the old blank-emitter state to two proven source-use file roots: [UID:0000JC][FileDownloader](by-file/FileDownloader.md) and [UID:0000LE][MiniMap](by-file/MiniMap.md).
- Generated output caveat: generated report rows may display the first surfaced path as `auto-generated/NexusTK/network/FileDownloader.cpp`; that path cell is a display artifact of emitter ordering, not a canonical ownership claim and not a reduction of the valid MiniMap emitter.
- 2026-06-12 A003 Batch320 decision: reviewed as a reconstructable source literal group but not routed. The child clears `85/85`, and both candidate file parents clear `85/85`, but neither [UID:0000JC][FileDownloader](by-file/FileDownloader.md) nor [UID:0000LE][MiniMap](by-file/MiniMap.md) is the single direct owner of every physical pooled literal in this range.

## Covered Data

| Range | Literal/data | Representative xrefs |
| --- | --- | --- |
| `0x0060d7f4-0x0060d7fc` | UTF-16LE `000`, with overlapping IDA dword heads at `0x0060d7f4` and `0x0060d7f8` | `0x0041a794`, `0x0041a7a0`, `0x00453ae4`, `0x00453aed`. |
| `0x0060d7fc-0x0060d808` | UTF-16LE `%03d` plus null/alignment | `0x0041a7d2`, `0x00453b12`. |
| `0x0060d808-0x0060d818` | UTF-16LE `.mnm` plus null/alignment | `0x0041a7e2`, `0x00453b22`, raw `0x00454e6f`. |
| `0x0060d818-0x0060d880` | UTF-16LE `https://s3.amazonaws.com/kru-downloads/tk/minimaps/` | `0x0041a7f2`, `0x00453b32`, raw `0x00454e7b`. |
| `0x0060d880-0x0060d894` | UTF-16LE `%s%s/%s%s` | `0x0041a7f7`, `0x00453b37`. |
| `0x0060d894-0x0060d8b8` | UTF-16LE `Downloading : %d\n` | `0x0041a988`, `0x00453cd7`, raw `0x00454fcc`. |

## Live IDA Evidence

- 2026-06-12 A003 IDA MCP decoded all literals above as UTF-16LE and confirmed the next child starts at `0x0060d8b8` with the item-shop version URL.
- `lookup_funcs` maps the `0x0041a7xx` refs to [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md), the FileDownloader message `10000` helper, and maps the `0x00453bxx` refs to [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md), the MiniMapDownloader direct worker helper.
- The `0x00454e6f`, `0x00454e7b`, and `0x00454fcc` refs are not inside an IDA-modeled function. They sit in the MiniMap renderer/downloader neighborhood and are recorded as an evidence gap instead of being used for parent assignment.
- 2026-06-12 A003 Batch320 MCP recheck used active session `b001_nexustk` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`. `xrefs_to` confirmed two modeled consumers for `0x0060d7f4`, `0x0060d7fc`, and `0x0060d880`; two modeled plus one raw MiniMap-side consumer for `0x0060d808`, `0x0060d818`, and `0x0060d894`; and the `0x0060d8b8` successor as a separate `DownloadCashShopVersion` literal child.
- The same pass `lookup_funcs` confirmed the raw refs `0x00454e6f`, `0x00454e7b`, and `0x00454fcc` are not inside modeled functions; `0x00454e30` and `0x00455040` are also not IDA function starts, while the following modeled functions start at `0x00455050`, `0x00455060`, and `0x004550d0`.
- `xrefs_to 0x00454e30` reported no incoming references. `make_signature_for_range 0x00454e30-0x00455040` produced a unique function-shaped byte signature that includes pushes of `szAgent`, `.mnm`, the S3 minimap URL, and `Downloading : %d\n`, so this raw helper strengthens the MiniMap-side consumer evidence but does not establish a callable source owner.
- B007 2026-06-21 keeps this literal group ownerless with FileDownloader/MiniMap emitter routes and adds the raw-range nuance from the renderer/control pass: the raw MiniMap-side body returns at `0x00455040`, alignment starts at `0x00455041`, and the future conceptual renderer helper name should be `DownloadCurrentMapFile` or `DownloadMapImageFile`, not `DownloadMinimapFile`.
- `analyze_component` for `0x0041a750` and `0x00453aa0` reported matching WinINet/file API callee sets and shared globals at `0x0060d7f4`, `0x0060d7f8`, `0x0060d7fc`, `0x0060d808`, `0x0060d818`, `0x0060d880`, and `0x0060d894`, confirming both modeled minimap download helpers consume this full literal group.
- Dispatch-route recheck: `sub_41B110` is the FileDownloader dispatcher and calls `sub_41A750` from the message `10000` case at `0x0041b168`; `sub_453A00` is the MiniMapDownloader task dispatcher and calls `sub_453AA0` for task message `0` at `0x00453a16`, then frees the task block. The helpers have the same minimap download literal/API family but no internal call edge between them.
- A read-only PE byte audit decoded `0x0060d7f4-0x0060d8b8` as UTF-16LE `000`, `%03d`, `.mnm`, `https://s3.amazonaws.com/kru-downloads/tk/minimaps/`, `%s%s/%s%s`, and `Downloading : %d\n`; `0x0060d8b8` starts the next UTF-16LE `https://` URL child.
- PE occurrence scan: the exact `0xc4` target block is unique at `0x0060d7f4`; `L"000"` also occurs at `0x006107b2` and `0x00612840`, `L"%03d"` also occurs at `0x00612e82`, and `L".mnm"` also occurs at `0x006107fa`. The S3 minimap URL prefix, `L"%s%s/%s%s"`, and `L"Downloading : %d\n"` are unique in the PE, and the successor item-shop version URL at `0x0060d8b8` is also unique. These duplicates explain why the short literals alone cannot determine ownership, while the full block and unique long strings still prove this exact minimap download literal child.
- Source/debug negative check: string-cache searches for source/debug markers such as `.pdb`, `.cpp`, `.cxx`, `.h`, `RSDS`, `NB10`, `MinimapDownloadWideLiterals`, `MiniMapConstants`, and `DownloadMinimap` did not find a source path, debug record, or named shared declaration object that could justify a synthetic constants owner.

## Ownership Decision

The literal group is real NexusTK minimap download data, but it is shared by two current source families: [UID:0000JC][FileDownloader](by-file/FileDownloader.md) owns the FileDownloader-dispatched `DownloadMinimapFile` path, while [UID:0000LE][MiniMap](by-file/MiniMap.md) owns the MiniMapDownloader worker path and the raw MiniMap-neighborhood helper body. Because both consumers are legitimate and both parents clear the score gate, `CANONICAL_OWNER:NONE` remains correct. Under the current owner/emitter split, however, the two file roots are proven source-use contexts, so `EMITTER_UIDS:0000JC,0000LE` lets the pooled literals surface through both reconstructed source families without inventing a single declaration owner.

## 2026-06-12 B001 Ownership Closeout Detail

B001's ownership report is incorporated as a no-metadata-change closeout. It agrees with the current `89/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000JC,0000LE` state, but adds the explicit negative evidence that prevents a single direct parent assignment.

PE and pointer evidence:

| Check | Result | Ownership impact |
| --- | --- | --- |
| `.rdata` mapping | `0x0060d7f4-0x0060d8b8` maps to file offsets `0x20c1f4-0x20c2b8`, length `0xc4`. | Confirms the half-open child range as one physical literal block. |
| Duplicate byte search | The exact `0xc4` bytes appear once, at VA `0x0060d7f4`. | Confirms the page covers the only physical pooled instance of these minimap literals. |
| Absolute VA hits | Known `.text` immediates only: two modeled hits for `000`, `%03d`, and `%s%s/%s%s`; two modeled plus one raw MiniMap-side hit for `.mnm`, the S3 URL, and `Downloading : %d\n`; successor `0x0060d8b8` is only the item-shop version child reference from `0x0041aa64`. | There is no hidden owner table or alternate source route beyond the documented modeled/raw consumers. |
| RVA and rel32 scans | No RVA dword hits and no executable `.text` rel32 candidate references for any address in this child. | Rejects an unmodeled registration table, relocation-style constants owner, or hidden relative-code route. |

B001 also compared the two modeled helpers. `sub_41A750` is the FileDownloader message `10000` minimap helper: it reads the map id from `request+0x0c`, opens the output path from `request+0x10`, and formats the URL from the later map-code field. `sub_453AA0` is the MiniMapDownloader worker helper: it reads the map id from the first dword of its task block and advances through a different task layout. Both use the same WinINet/file API family and the same minimap literal group, but there is no call edge between them and the task layouts differ. That supports repeated source literals/linker pooling or shared source-use constants, not one helper owning the other.

Split alternatives were rejected:

| Proposed split | Why it fails |
| --- | --- |
| `000` and `%03d` as a directory-format child | Both modeled helpers consume them, so a smaller child still has the FileDownloader/MiniMap owner conflict. |
| `.mnm`, S3 URL, and progress text as a raw-MiniMap-heavy child | The raw helper adds MiniMap-side evidence, but the FileDownloader message helper still consumes the same literals directly. |
| `%s%s/%s%s` as a separate URL-builder literal | It is a distinct format string, but splitting would only create a smaller parentless child with the same two modeled consumers. |
| `Downloading : %d\n` as a separate progress/debug literal | It is specific to these minimap download loops and still spans FileDownloader, MiniMapDownloader, and raw MiniMap-side code. |
| Widening to `HTTPTEST` or the item-shop URL children | The neighbors have different consumer sets: `HTTPTEST` spans broader downloader helpers, while `0x0060d8b8` starts the FileDownloader item-shop version URL child. |

Ranked owner analysis:

- Keep parent blank: accepted. The bytes, xrefs, raw helper caveat, and PE scan are strong, while source declaration ownership cannot be proven. This is the least misleading state.
- [UID:0000LE][MiniMap](by-file/MiniMap.md): strongest forced semantic owner because the literals are minimap-domain data and the raw helper sits in the MiniMap renderer/control neighborhood. Rejected as `CANONICAL_OWNER` because FileDownloader message `10000` consumes the entire group directly.
- [UID:0000JC][FileDownloader](by-file/FileDownloader.md): strong modeled consumer and valid emitter for `DownloadMinimapFile`. Rejected as exclusive owner because MiniMapDownloader and the raw MiniMap-side helper also consume the literals.
- Synthetic `map/MinimapDownloadShared` or `map/MinimapDownloadConstants`: plausible only if future source/debug evidence proves a shared declaration. Current evidence has no shared table, no address-taken constant block, no pointer owner, no debug/source metadata, and no internal call edge.
- [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md): rejected because it is a non-emitting split-audit container, not a source owner.

Final C++ implication: keep this physical literal child without a formal C++ block. Generated source may surface the literal uses through both FileDownloader and MiniMap emitter roots, but this page must not manufacture a shared global/constant declaration unless later evidence proves one.

## 2026-06-13 B001 Second-Pass Owner/Emitter Audit

B001 independently rechecked the current no-owner-with-emitters state and recommended no metadata or score change: keep `COMPLETION:89`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000JC,0000LE`. This is the accepted state. The second-pass report is incorporated here as claim accounting rather than as a metadata change.

Second-pass evidence basis:

| Evidence source | Details retained |
| --- | --- |
| IDA MCP session | `b001_0003gy`, read-only `NexusTK.exe` input, image base `0x00400000`, auto-analysis and Hex-Rays ready. Tools used included function lookup, xrefs, decompilation, component analysis, byte/string reads, regex search, and a unique-signature check over the raw MiniMap-neighborhood body. |
| Modeled FileDownloader route | `sub_41B110` dispatches downloader message `10000` to `sub_41A750` at `0x0041b168`. `sub_41A750` consumes the full literal group while formatting the S3 minimap URL and writing the requested `.mnm` file. |
| Modeled MiniMap route | `sub_453A00` dispatches MiniMapDownloader task message `0` to `sub_453AA0` at `0x00453a16`. `sub_453AA0` consumes the full literal group through a separate task layout and direct minimap worker path. |
| Raw MiniMap evidence | `0x00454e30-0x00455040` is a unique helper-shaped byte neighborhood and references `.mnm`, the S3 minimap URL prefix, and `Downloading : %d\n`, but `lookup_funcs` does not model `0x00454e30` or `0x00455040` as function starts and `xrefs_to 0x00454e30` has no incoming callers. It supports the existing `MiniMap` emitter; it is not a separate emitter or owner. |
| Component comparison | `sub_41A750` and `sub_453AA0` share the same WinINet/file API family (`InternetOpenW`, `InternetOpenUrlW`, `HttpQueryInfoW`, `InternetQueryDataAvailable`, `InternetReadFile`, `WriteFile`, handle cleanup, formatter, CreateFileW-like import, and DeleteFileW-like import) and the same literal anchors, but component analysis found no internal call edge between them. |

Raw PE facts retained from the second pass:

| Check | Result |
| --- | --- |
| Section mapping | The target maps to `.rdata` file offsets `0x20c1f4-0x20c2b8`, length `0xc4`. The second pass recorded `.text` at `0x00401000-0x0060c600` and `.rdata` at `0x0060d000-0x0066c200`. |
| Full-block uniqueness | The complete `0xc4` byte block appears once, at VA `0x0060d7f4`. |
| Successor boundary | `0x0060d8b8` begins the separate UTF-16LE item-shop version URL child, not this minimap literal page. |
| Duplicate short strings | Short sub-literals have unrelated physical duplicates: `L"000"` also appears at `0x006107b2` and `0x00612840`; `L"%03d"` also appears at `0x00612e82`; `L".mnm"` also appears at `0x006107fa`. These duplicates are why short-string identity alone is not owner proof. |
| Unique long strings | The S3 minimap URL prefix, `L"%s%s/%s%s"`, and `L"Downloading : %d\n"` are unique at this target in the PE scan. |
| Reference forms | All observed target refs are direct absolute `.text` operands from the modeled FileDownloader/MiniMapDownloader helpers and the raw MiniMap-neighborhood body. The scan found no RVA dword hits and no rel32-to-target hits for target addresses. |

Second-pass source-use and owner conclusions:

| Candidate | Decision |
| --- | --- |
| `CANONICAL_OWNER:NONE`; `EMITTER_UIDS:0000JC,0000LE` | Accepted. It matches the owner/emitter split for pooled literals: no single declaration owner is proven, but both source-use roots are proven emitters. |
| [UID:0000LE][MiniMap](by-file/MiniMap.md) as canonical owner | Rejected. The strings are minimap-domain literals and the raw helper is MiniMap-neighborhood evidence, but FileDownloader message `10000` consumes the entire same physical group directly. |
| [UID:0000JC][FileDownloader](by-file/FileDownloader.md) as canonical owner | Rejected. `sub_41A750` consumes the whole group, but `sub_453AA0` and the raw MiniMap-side body are independent MiniMap evidence, and the strings are not generic downloader infrastructure. |
| New shared `MinimapDownloadConstants` / `MinimapDownloadShared` owner | Rejected. No pointer table, global struct, source/debug path, call edge, relocation pattern, or named declaration object proves a shared source owner. |
| Further split of this page | Rejected. Some sub-literals have different raw refs, but every meaningful subrange participates in the same minimap URL/path/progress construction flow and splitting would only fragment the same FileDownloader/MiniMap owner conflict. |
| Compiler-generated or non-reconstructable classification | Rejected. Linker pooling affects the physical layout, but the contents are source-authored NexusTK string literals that should surface at consuming source-use sites. |

Support-doc comparison:

- [UID:0000JC][FileDownloader](by-file/FileDownloader.md) already records `DownloadMinimapFile` as FileDownloader message `10000`, the dispatcher route, and the `network/` source root.
- [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) already carries the FileDownloader-local worker body, request layout, single direct caller, URL construction, progress logging, and failure-delete behavior.
- [UID:0000LE][MiniMap](by-file/MiniMap.md) already records the minimap source family, the direct MiniMapDownloader route, the raw `0x00454e30` no-code proof, and the compact-versus-split source-file caveat.
- [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md) already records the MiniMapDownloader task dispatch, `sub_453AA0` behavior, task-block caveats, and minimap ownership.
- [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) remains a non-emitting split-audit container, while siblings [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md), [UID:0003G1][0x0060d94c-0x0060d958.SharedVersionJsonKey](by-memory/0x0060d94c-0x0060d958.SharedVersionJsonKey.md), and [UID:0003G2][0x0060d958-0x0060d9c0.CashShopCatalogDownloadWideUrl](by-memory/0x0060d958-0x0060d9c0.CashShopCatalogDownloadWideUrl.md) keep their separate routing decisions and prove the target's boundaries.

Final C++ implication: keep this physical literal child without a direct C++ block. Reconstructed FileDownloader and MiniMap code should spell or route the corresponding literals at their consuming use sites. This page must not invent a standalone global, header, or helper declaration unless future source/debug evidence proves one.

## 2026-06-13 B001 Post-Migration Recheck

B001's post-migration recheck supersedes the same older `0003FZ` archive without changing the target. The recheck reviewed the prior B002 post-migration report, B001 ownership report, and B001 second-pass report as background, then reconfirmed that every valid current claim is already represented by the owner/emitter model:

- Keep `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JC,0000LE`, blank `EMITTER_POSITION_OPTIONAL`, blank reconstruction C++, and scores `89/93`.
- Do not split, merge, reclassify as compiler-only, invent a `MinimapDownloadConstants`/shared helper owner, or create a separate raw-helper emitter for `0x00454e30-0x00455040`.
- Treat the raw MiniMap-neighborhood references as evidence for the existing [UID:0000LE][MiniMap](by-file/MiniMap.md) source-use route, not as an independent source root, because IDA does not model `0x00454e30` as a function start and no caller to that start is known.
- Keep direct C++ blank on this literal child. The string literals should surface through the consuming FileDownloader and MiniMap code when those source bodies are reconstructed; this page must not manufacture a standalone global or header declaration unless later source/debug evidence proves one.
- No manual `by-memory/-coverage-report.md` replacement is required; the intended row semantics are the current no-owner/two-emitter explanation.

## 2026-06-22 B002 Post-Migration Owner/Emitter Report Audit

Agent-B002's archived post-migration owner/emitter report is incorporated as claim accounting for the report that originally changed this page from the old blank-emitter state to the current no-owner/two-emitter state. No metadata edit is needed in this pass because the header already carries the accepted result: `COMPLETION:89`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JC,0000LE`, blank `EMITTER_POSITION_OPTIONAL`, and blank direct reconstruction C++.

| B002 post-migration claim | Current disposition |
| --- | --- |
| The old combined parent model correctly rejected a single direct source owner, but the current owner/emitter model should still route the pooled literals through proven source-use contexts. | Accepted. This page keeps `CANONICAL_OWNER:NONE` while using FileDownloader and MiniMap as emitters. The treatment matches the accepted pooled-literal precedent [UID:0003IO][0x0061e6f8-0x0061e700.SharedGmWideString](by-memory/0x0061e6f8-0x0061e700.SharedGmWideString.md): no declaration owner is proven, but multiple source roots legitimately consume the same physical bytes. |
| [UID:0000JC][FileDownloader](by-file/FileDownloader.md) is a valid emitter because message `10000` dispatches from `sub_41B110` to `sub_41A750` at `0x0041b168`, and `sub_41A750` consumes the complete minimap literal group. | Accepted. The current [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) page is stronger than the historical report snapshot: it is now `88/90`, confirms the single FileDownloader dispatcher route, and carries first-draft FileDownloader-local C++ for the message `10000` worker. |
| [UID:0000LE][MiniMap](by-file/MiniMap.md) is a valid emitter because `sub_453A00` dispatches MiniMapDownloader message `0` to `sub_453AA0` at `0x00453a16`, and `sub_453AA0` consumes the complete minimap literal group. | Accepted. [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md) records the MiniMapDownloader task handler, direct `sub_453AA0` download helper, singleton/vtable evidence, and minimap ownership route through [UID:0000LE][MiniMap](by-file/MiniMap.md). |
| Raw MiniMap-neighborhood refs at `0x00454e6f`, `0x00454e7b`, and `0x00454fcc` strengthen MiniMap source-use evidence but should not become a third emitter or owner because `0x00454e30-0x00455040` is not a modeled IDA function and has no incoming xref to its raw start. | Accepted and refined by later MiniMap renderer/control work. The raw body remains support evidence for the existing MiniMap emitter only; it does not justify a separate route, IDA function repair, or standalone raw-helper owner from this report. |
| Xref accounting shows each meaningful target literal is used by the FileDownloader and MiniMapDownloader modeled helpers, while `.mnm`, the S3 URL prefix, and `Downloading : %d\n` also have raw MiniMap-side refs. The successor `0x0060d8b8` item-shop URL is outside this target and has a FileDownloader-only consumer. | Incorporated. The `Covered Data`, live IDA, and later B002 live-pass tables preserve each literal head, modeled/raw xref set, and the `0x0060d8b8` successor boundary. The sibling [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md) remains separate. |
| Raw PE evidence maps the target to `.rdata` file offsets `0x20c1f4-0x20c2b8`, finds the full `0xc4` byte block once at `0x0060d7f4`, decodes the UTF-16LE minimap literal sequence, and finds only known absolute-VA immediate hits. | Incorporated. Current sections retain the raw offset, byte size, decoded string set, uniqueness result, and absolute-reference distribution. No hidden pointer table, RVA-form reference, rel32 reference, or source/debug breadcrumb proves a single declaration owner. |
| [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) is only a mixed split-audit container; adjacent children keep separate routing decisions. | Accepted. `0003AL` stays non-reconstructable/non-emitting, [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md) is another no-owner/two-emitter pooled literal, and [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md)/[UID:0003G1][0x0060d94c-0x0060d958.SharedVersionJsonKey](by-memory/0x0060d94c-0x0060d958.SharedVersionJsonKey.md)/[UID:0003G2][0x0060d958-0x0060d9c0.CashShopCatalogDownloadWideUrl](by-memory/0x0060d958-0x0060d9c0.CashShopCatalogDownloadWideUrl.md) preserve their own boundaries and routes. |
| Sole MiniMap ownership, sole FileDownloader ownership, a synthetic `MinimapDownloadConstants`/`MinimapDownloadShared` owner, smaller literal splits, widening into adjacent children, and non-reconstructable/compiler-only classification were rejected. | Accepted. FileDownloader and MiniMap each own real source-use sites but overclaim as canonical owners; the PE/IDA evidence has no source-level constants object; splitting would fragment a coherent minimap URL/path/progress group without resolving ownership; and linker pooling does not make source-authored literals non-reconstructable. |
| Direct C++ should remain blank on this physical pooled literal page. | Accepted. The final generated source should spell or route these literals at the consuming FileDownloader and MiniMap use sites unless later source/debug evidence proves an actual named shared declaration. This page must not invent a global, header, or helper declaration. |
| The B002 report's generated-row/tracker/shared-coverage wording was supervisor-owned. | Current disposition: no target change is required for generated rows or tracker data here, and no `by-memory/-coverage-report.md` replacement is needed now because the current shared coverage row already states the accepted no-owner/two-emitter semantics. |

## 2026-06-22 B002 Current No-Owner Report Audit

The older B002 current no-owner report for this same UID is incorporated as a no-metadata-change audit. Its recommended state matches the current page: keep `COMPLETION:89`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JC,0000LE`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++. The report's direct evidence is either already present above at same-or-greater detail or superseded by later B001/B007 rechecks; no support-page, coverage-row, IDA, split, merge, owner, emitter, or C++ edit is required.

Claim accounting from that report:

| B002 claim | Current disposition |
| --- | --- |
| Target is one UTF-16LE minimap literal group from `0x0060d7f4-0x0060d8b8`, containing `000`, `%03d`, `.mnm`, the S3 minimap URL, `%s%s/%s%s`, and `Downloading : %d\n`. | Incorporated. The `Covered Data`, byte-audit, uniqueness, and successor-boundary sections record the same literal breakdown and range. |
| FileDownloader message `10000` route calls `sub_41A750`, and that helper references every target literal. | Incorporated and now stronger through [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md), which is `88/90` and contains first-draft C++ for the FileDownloader worker helper. |
| MiniMapDownloader message `0` route calls `sub_453AA0`, and that helper references every target literal. | Incorporated via the MiniMap route and [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md). |
| Raw MiniMap-neighborhood bytes at `0x00454e30-0x00455040` reference `.mnm`, the S3 URL, and `Downloading : %d\n`, but are not modeled as a callable function and have no incoming xrefs. | Incorporated. The raw body is supporting MiniMap evidence only, not a separate owner or emitter. Later B007 wording refines the future conceptual name toward `DownloadCurrentMapFile` or `DownloadMapImageFile`, not FileDownloader's `DownloadMinimapFile`. |
| `analyze_component` shows the two modeled helpers share the same WinINet/file API family and literal anchors, with no internal call edge between them. | Incorporated. This is retained in the live evidence and B001 second-pass sections as support for parallel source-use routes rather than one helper owning the other. |
| PE scan maps the target to `.rdata` file offsets `0x20c1f4-0x20c2b8`, finds the exact `0xc4` block once, finds duplicate short literals elsewhere, and finds the longer S3/format/progress literals unique at this target. | Incorporated. The PE and pointer evidence tables retain the same mapping, uniqueness, and duplicate-short-string reasoning. |
| Absolute reference scans find only the known modeled and raw immediate refs, with no RVA/rel32 hidden owner route; source/debug marker searches for `.pdb`, `.cpp`, `.cxx`, `RSDS`, and `NB10` do not produce a declaration owner. | Incorporated. These negatives remain the reason a synthetic shared constants owner is rejected. |
| [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) is only a non-emitting split-audit container; predecessor [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md) and successor [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md) confirm the target boundaries and sibling routing model. | Incorporated through the parent/sibling discussion and cross-references. |
| `CANONICAL_OWNER:NONE` with emitters `0000JC,0000LE` is the best model; single FileDownloader owner, single MiniMap owner, synthetic shared constants owner, split, merge, and non-reconstructable alternatives are rejected. | Incorporated and accepted. The ranked ownership analysis records the same decisions. |
| No standalone reconstruction C++ should be entered for this physical pooled literal page. | Incorporated. The page stays blank for direct C++ because the literals should surface through consuming FileDownloader and MiniMap code unless later source/debug evidence proves a named shared declaration. |
| The report's optional generated/coverage row text was supervisor-owned and did not require a target edit. | Not applicable to this target page; no coverage row is changed by this audit. |

## 2026-06-22 B002 Current No-Owner Sweep Audit

Agent-B002's current no-owner sweep is incorporated as an additional no-metadata-change audit. It rechecked this target from the active 2026-06-14 `no_owner_b-agent-tracker.md` sweep and from the generated `auto-generated/-ag-memory-coverage.md` row. The generated row's first staged output path remains `auto-generated/NexusTK/network/FileDownloader.cpp`, but that is only the first output route; it is not a canonical ownership claim and does not reduce the accepted MiniMap emitter.

The sweep's final disposition is accepted and matches this page: [UID:0003FZ] is reconstructable source-authored UTF-16LE data, `CANONICAL_OWNER:NONE` remains correct because no single source declaration owner is defensible, and `EMITTER_UIDS:0000JC,0000LE` remains correct because FileDownloader message `10000` and MiniMapDownloader message `0` are both proven source-use routes. The sweep's recommendation confidence `92/100` is compatible with this page's `89/93` score.

Sweep-specific evidence accounting:

| B002 sweep claim | Current disposition |
| --- | --- |
| Current generated state lists owner `NONE`, emitters `0000JC`,`0000LE`, and first staged route `FileDownloader.cpp`. | Incorporated. The `Status` section records the generated output caveat and keeps both emitters. |
| Predecessor [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md) and successor [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md) prove no range repair is needed. | Incorporated. The covered-data and sibling sections keep this target half-open at `0x0060d7f4-0x0060d8b8`. |
| IDA session `b001_0003gy` was healthy, used image base `0x00400000`, and had Hex-Rays/strings ready. | Incorporated as provenance; it is consistent with the B001 second-pass IDA evidence already retained. |
| Function facts include `sub_41A750` size `0x2ae`, `sub_41B110` size `0x70`, `sub_453A00` size `0x2f`, `sub_453AA0` size `0x2af`, raw `0x00454e30`/`0x00454e6f`/`0x00454e7b`/`0x00454fcc` not being modeled functions, and modeled successors `0x00455050`/`0x00455060`/`0x004550d0`. | Incorporated. The live IDA and raw-helper sections preserve the same modeled-vs-raw distinction and successor-function boundary. |
| `sub_41B110` dispatches message `10000` to `sub_41A750`; `sub_453A00` dispatches message `0` to `sub_453AA0`; both then free the message/task block as appropriate. | Incorporated. This is the core accepted FileDownloader/MiniMap route evidence. |
| Component comparison shows both helpers use the same WinINet/file callee family and access shared literal globals, but have no internal call edge. | Incorporated. This supports parallel source-use routes rather than one canonical owner. |
| Source/debug searches for minimap/update terms found no `.pdb`, `.cpp`, `.cxx`, `.h`, `RSDS`, `NB10`, or named declaration breadcrumb proving a shared owner. | Incorporated. This remains the decisive negative against a synthetic shared constants file. |
| Raw PE maps this target to `.rdata` raw `0x20c1f4-0x20c2b8` and finds the full `0xc4` target block exactly once. | Incorporated. The PE tables retain the exact target mapping and full-block uniqueness. |
| Long literals are unique at this target, while short `000`, `%03d`, and `.mnm` have duplicate physical occurrences elsewhere. | Incorporated. Duplicate short strings are explicitly treated as insufficient owner evidence; the complete block and long literals prove this exact child. |
| Reference-form scan records absolute hits for each target head, no RVA hits, and no rel32 hits. | Incorporated. Current raw-PE evidence preserves the absence of hidden RVA/rel32 owner routes. |
| `0x00454e30-0x00455040` contains direct immediates for [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md), `.mnm`, the S3 URL, and progress text, but has no incoming xrefs and is not an exact source root. | Incorporated. This raw body strengthens the MiniMap emitter only and must not become a third emitter. |
| [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) was only `80/84` at sweep time, but live IDA still proved FileDownloader emission because [UID:0000JC][FileDownloader](by-file/FileDownloader.md) cleared the file-root gate. | Superseded and strengthened. The current [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) page is now higher quality and has first-draft C++; the sweep's lower historical score is preserved as past context, not a current blocker. |
| [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md) supports MiniMap emission; [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) is a non-emitting split container, not an owner. | Incorporated. The support-doc comparison keeps both facts. |
| Ranked ownership accepts `NONE` with FileDownloader/MiniMap emitters and rejects sole MiniMap owner, sole FileDownloader owner, synthetic shared constants/helper owner, further split, merge into [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md), and non-reconstructable compiler/linker classification. | Incorporated and unchanged. Linker pooling explains the physical block but does not make the source-authored literals non-reconstructable. |
| No target, generated report, project-level, shared coverage, metadata, C++, split, merge, rename, reclassification, or IDA-safe-name edit is required. | Incorporated. The only Rule 26 edit is this audit text; the target metadata and shared coverage row remain unchanged. |

The sweep's exact supervisor-owned coverage and generated-row recommendations require no action because the current [UID:0003FZ] shared coverage row already states the accepted no-owner/two-emitter model, and generated reports are validator-owned.

## 2026-06-22 B002 Live Goal 2 No-Owner Pass Audit

Agent-B002's archived live Goal 2 no-owner pass is incorporated as another no-metadata-change audit. Its final recommendation is accepted: keep `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JC,0000LE`, no split, no merge, no reclassification, no child creation, no canonical-owner assignment, and no direct reconstruction C++ on this physical pooled literal page. The report's optional shared coverage replacement was supervisor-owned and is not applied here because the current coverage semantics already state the same no-owner/two-emitter result.

Live pass provenance and evidence standard:

| Report claim | Current disposition |
| --- | --- |
| Live IDA MCP session `a001_goal2_class_batch` used read-only input `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, IDB `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`, image base `0x00400000`, and ready auto-analysis/Hex-Rays. | Incorporated as additional provenance. It independently confirms the current direct bytes/xrefs/function-route evidence and does not require an IDA rename or metadata change. |
| The pass applied the owner/emitter split from `by-structure.md`: canonical owner means source declaration ownership, while emitter UIDs mean source-use routing. | Incorporated. This is why the page can be reconstructable and emitter-routed through both source files while keeping `CANONICAL_OWNER:NONE`. |
| The pass treated physical `.rdata` adjacency as only a hypothesis because `inference_research.md` warns against ownership from adjacency alone. | Incorporated. The accepted decision is based on decoded bytes, xrefs, function routes, raw helper evidence, and negative source/global evidence rather than the surrounding literal pool. |

Live IDA facts retained from the pass:

| Address/fact | B002 live evidence | Current impact |
| --- | --- | --- |
| `0x0041a750` | `lookup_funcs` identified `sub_41A750`, size `0x2ae`; all `0x0041a7xx` target refs are inside it. | Confirms the FileDownloader message `10000` source-use route. |
| `0x0041b110` | `xrefs_to 0x0041a750` found the dispatcher call at `0x0041b168` inside `sub_41B110`. | Confirms [UID:0000JC][FileDownloader](by-file/FileDownloader.md) as a valid emitter, not a canonical owner. |
| `0x00453aa0` | `lookup_funcs` identified `sub_453AA0`, size `0x2af`; all modeled `0x00453bxx` target refs are inside it. | Confirms the MiniMapDownloader worker source-use route. |
| `0x00453a00` | `xrefs_to 0x00453aa0` found the task-dispatch call at `0x00453a16` and an unmodeled xref near `0x004539f4`. | Confirms [UID:0000LE][MiniMap](by-file/MiniMap.md) as a valid emitter, not a canonical owner. |
| `0x00454e30`, `0x00454e6f`, `0x00454e7b`, `0x00454fcc` | Not IDA-modeled functions; `xrefs_to 0x00454e30` found no incoming references. | The raw MiniMap-neighborhood body supports the MiniMap emitter but is not a third source root or owner. |
| `0x0060d7f4-0x0060d8b8` | `get_bytes` decoded the exact UTF-16LE minimap block; `0x0060d8b8` begins a separate UTF-16LE item-shop version URL child. | Confirms the current half-open range and successor boundary. |

Literal-start xref accounting from the live pass:

| Literal head | Live xrefs | Disposition |
| --- | --- | --- |
| `0x0060d7f4` (`000`) | `0x0041a794` in `sub_41A750`; `0x00453ae4` in `sub_453AA0`. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d7f8` (overlapping tail/head) | `0x0041a7a0` in `sub_41A750`; `0x00453aed` in `sub_453AA0`. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d7fc` (`%03d`) | `0x0041a7d2` in `sub_41A750`; `0x00453b12` in `sub_453AA0`. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d808` (`.mnm`) | `0x0041a7e2` in `sub_41A750`; `0x00453b22` in `sub_453AA0`; raw `0x00454e6f`. | Shared modeled use plus raw MiniMap-neighborhood support. |
| `0x0060d818` (S3 minimap URL prefix) | `0x0041a7f2` in `sub_41A750`; `0x00453b32` in `sub_453AA0`; raw `0x00454e7b`. | Shared modeled use plus raw MiniMap-neighborhood support. |
| `0x0060d880` (`%s%s/%s%s`) | `0x0041a7f7` in `sub_41A750`; `0x00453b37` in `sub_453AA0`. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d894` (`Downloading : %d\n`) | `0x0041a988` in `sub_41A750`; `0x00453cd7` in `sub_453AA0`; raw `0x00454fcc`. | Shared modeled use plus raw MiniMap-neighborhood support. |
| `0x0060d8b8` successor | One successor ref at `0x0041aa64` in `sub_41AA00`. | Confirms this target must not widen into [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md). |

The live decompiler/component comparison also stays incorporated: `sub_41A750` opens WinINet with `szAgent`, formats the `000`/`%03d` directory and S3 minimap URL from the FileDownloader request payload, writes the requested `.mnm`, and emits the progress text; `sub_453AA0` performs the same minimap download loop from a MiniMapDownloader task block and returns a success byte. Both helpers call the same WinINet/file/debug API family (`InternetOpenW`, `InternetOpenUrlW`, `InternetSetOptionW`, `HttpQueryInfoW`, `InternetQueryDataAvailable`, `InternetReadFile`, `WriteFile`, `OutputDebugStringW`, handle cleanup, formatter, and imported create/delete-file thunks), but no internal call edge makes either helper the owner of the other.

Independent PE-scan details from the live pass:

| Check | B002 live result | Current impact |
| --- | --- | --- |
| Binary identity | Read-only `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`. | Matches the current binary evidence base. |
| Raw mapping | Target block raw file offset `0x20c1f4`, VA range `0x0060d7f4-0x0060d8b8`, size `0xc4`. | Confirms the target's exact physical coverage. |
| Decoded strings | `0x0060d7f4` `000`, `0x0060d7fc` `%03d`, `0x0060d808` `.mnm`, `0x0060d818` S3 minimap URL, `0x0060d880` `%s%s/%s%s`, `0x0060d894` `Downloading : %d\n`; `0x0060d8b8` starts the item-shop version URL. | Incorporated. The target stays a minimap literal page and does not absorb the successor item-shop URL child. |
| Full-block uniqueness | The full `0xc4` byte block occurs exactly once. | Confirms this page covers the only physical pooled instance of the complete minimap literal group. |
| Duplicate short strings | `000`, `0`, `%03d`, and `.mnm` have unrelated duplicates elsewhere. | Short literals are not used as owner proof. |
| Unique long strings | The S3 minimap URL, `%s%s/%s%s`, and `Downloading : %d\n` each occur uniquely at this target. | The meaningful long literals prove this exact minimap download child. |
| Absolute VA operand hits | Hits match IDA: `0x0060d7f4` at `0x0041a796`/`0x00453ae6`, `0x0060d7f8` at `0x0041a7a2`/`0x00453aef`, `0x0060d7fc` at `0x0041a7d3`/`0x00453b13`, `0x0060d808` at `0x0041a7e3`/`0x00453b23`/`0x00454e70`, `0x0060d818` at `0x0041a7f3`/`0x00453b33`/`0x00454e7c`, `0x0060d880` at `0x0041a7f8`/`0x00453b38`, and `0x0060d894` at `0x0041a989`/`0x00453cd8`/`0x00454fcd`. | Confirms the modeled FileDownloader/MiniMap refs plus raw MiniMap-side refs; no hidden owner table is indicated. |
| RVA-form refs | No RVA-form dword hits for any target literal start. | Rejects an alternate RVA-table owner route. |
| IDA string cache | `find_regex` missed this wide block and found only unrelated/minimap-neighbor strings. | The report treats byte/xref evidence as stronger than IDA string-cache output, consistent with this project's wide-string caveats. |

Ownership and source-placement claim accounting:

| Candidate or action | Live pass disposition | Current disposition |
| --- | --- | --- |
| `CANONICAL_OWNER:NONE` with emitters `0000JC,0000LE` | Accepted as the least misleading model because both source-use families are real and no single declaration owner is proven. | Accepted and unchanged. |
| Canonical FileDownloader owner | Rejected: FileDownloader owns the message `10000` consumer route and neighboring item-shop literals, but MiniMapDownloader and raw MiniMap-side code consume this target too. | Rejected; FileDownloader remains only emitter `0000JC`. |
| Canonical MiniMap owner | Rejected: the literals are minimap-domain data and MiniMap owns the worker route, but FileDownloader message `10000` consumes the full same block directly. | Rejected; MiniMap remains only emitter `0000LE`. |
| New shared constants/global/helper owner | Rejected: no source/debug name, global symbol, pointer object, relocation grouping, or explicit declaration artifact proves one. | Rejected; this page must not invent `DownloaderLiterals`, `MiniMapDownloadConstants`, or a similar owner. |
| Split inside [UID:0003FZ] | Rejected: subranges would keep the same FileDownloader/MiniMap conflict or create tiny generic literal pages. | Rejected; current child range remains intact. |
| Merge/widen into [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) or successor children | Rejected: the parent is a non-emitting split-audit container, and neighboring children have different consumer sets. | Rejected; boundaries remain exact. |
| Direct C++ on this page | Rejected for this pass because original source-level literal pooling versus repeated spelling is still unresolved. | Blank C++ remains correct; final source should spell or route the literals at the consuming FileDownloader and MiniMap use sites unless later evidence proves a named shared declaration. |

Support-document comparison found no needed support edit. [UID:0000JC][FileDownloader](by-file/FileDownloader.md), [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md), [UID:0000LE][MiniMap](by-file/MiniMap.md), [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md), [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md), [UID:0003FY][0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString](by-memory/0x0060d7e0-0x0060d7f4.SharedDownloaderUserAgentWideString.md), and [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md) already carry the route, split, or sibling information needed to support this target's current state.

## 2026-06-22 B003 Current Goal 2 No-Owner Pass Audit

Agent-B003's current Goal 2 no-owner pass is incorporated as a no-metadata-change confirmation and provenance audit. The accepted recommendation is unchanged: keep `COMPLETION:89`, `CONFIDENCE:93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JC,0000LE`, blank direct reconstruction C++, no split, no merge, no reclassification, no new shared constants file, and no shared coverage replacement. The report's optional B003 provenance row for `by-memory/-coverage-report.md` is not required because the current shared row already states the same no-owner/two-emitter semantics.

B003 treated the generated first-output path `auto-generated/NexusTK/network/FileDownloader.cpp` as an emitter-order display artifact, not as FileDownloader ownership. The pass also reviewed the older B001 second-pass report as background only, then rechecked the current target, generated memory row, human coverage row, [UID:0000JC][FileDownloader](by-file/FileDownloader.md), [UID:0000LE][MiniMap](by-file/MiniMap.md), [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md), [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md), [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md), the proposed source-tree notes, and the sibling [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md).

Evidence standard retained from B003:

| Evidence source | B003 finding | Current disposition |
| --- | --- | --- |
| `by-structure.md` owner/emitter split | Semantic declaration ownership and generated-output routing are separate. Pooled string literals may keep `CANONICAL_OWNER:NONE` while listing multiple proven source-use emitters. | Accepted. This is the exact model used here. |
| `inference_research.md` caution | Adjacent `.rdata`, consumer xrefs, and string pooling do not alone prove source-file declaration ownership. | Accepted. Ownership remains based on function containment, dispatcher routes, PE references, component comparison, boundaries, and negative source/debug evidence. |
| IDA session | `b001_0003gy` used read-only `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, IDB `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`, image base `0x00400000`, ready auto-analysis, ready Hex-Rays, and a ready string cache with `2067` strings. | Incorporated as another current evidence pass consistent with the B001/B002 evidence above. |
| Tools used | `server_health`, `lookup_funcs`, `xrefs_to`, `analyze_component`, `decompile`, `get_bytes`, `find_regex`, and raw PE section/reference scans. | Incorporated. No IDA rename, type change, source split, or metadata repair follows from this evidence. |

B003 IDA/function facts:

| Address or range | B003 result | Current impact |
| --- | --- | --- |
| `0x0041a750` | `lookup_funcs` maps to `sub_41A750`, size `0x2ae`. | Confirms the FileDownloader message `10000` helper consumes the target literal group. |
| `0x0041b110` | `lookup_funcs` maps to `sub_41B110`, size `0x70`; `0x0041b168` lies inside this dispatcher. Decompilation shows message `10000` calls `sub_41A750`, then frees the request block, while `10001` and `10002` route to the cash-shop helpers and other messages delegate to `sub_596920`. | Confirms [UID:0000JC][FileDownloader](by-file/FileDownloader.md) as an emitter only. |
| `0x00453a00` | `lookup_funcs` maps to `sub_453A00`, size `0x2f`; decompilation shows message `0` calls `sub_453AA0` and frees the task block, while nonzero messages delegate to `sub_596920`. | Confirms the MiniMapDownloader task route. |
| `0x00453aa0` | `lookup_funcs` maps to `sub_453AA0`, size `0x2af`. | Confirms [UID:0000LE][MiniMap](by-file/MiniMap.md) as an emitter only. |
| `0x00454e30`, `0x00454e6f`, `0x00454e7b`, `0x00454fcc`, `0x00455040` | Not IDA-modeled functions; the next modeled functions are `sub_455050`, `sub_455060`, and `sub_4550D0`. `xrefs_to 0x00454e30` found no incoming refs. | The raw body is MiniMap-side support evidence, not a third emitter, not a separate callable source route, and not a direct owner. |
| Component comparison | `analyze_component` found no internal call edge between `sub_41A750` and `sub_453AA0`. Both helpers share the same WinINet/file/debug support family, including `InternetOpenW`, `InternetOpenUrlW`, `HttpQueryInfoW`, `InternetQueryDataAvailable`, `InternetReadFile`, `WriteFile`, handle cleanup, `OutputDebugStringW`, `_memset`, `sub_41B9B0`, `unk_69BE34`, and `unk_69BE60`. | Supports parallel source-use routes and rejects making either helper the canonical declaration owner for the other. |
| Shared literal/global reads | Both modeled helpers access `0x0060d7e0`, `0x0060d7f4`, `0x0060d7f8`, `0x0060d7fc`, `0x0060d808`, `0x0060d818`, `0x0060d880`, `0x0060d894`, and the security cookie. | Confirms both modeled routes consume the entire meaningful minimap literal group. |
| String/debug search | Minimap/update regexes found neighbor strings and MiniMap RTTI names but no source declaration owner; `.pdb`, `.cpp`, `.cxx`, and `.h` searches found no embedded source/debug path evidence. | Rejects synthetic shared-header/helper ownership for this pass. |

Raw PE and literal evidence retained from B003:

| Fact | B003 result | Current impact |
| --- | --- | --- |
| PE layout | Image base `0x00400000`, machine `0x014c`, four sections; `.text` `0x00401000-0x0060c600` from raw `0x400+0x20b600`, virtual size `0x20b4ac`; `.rdata` `0x0060d000-0x0066c200` from raw `0x20ba00+0x5f200`, virtual size `0x5f0be`. | Confirms the same binary/section basis as the other reports. |
| Target mapping | `0x0060d7f4-0x0060d8b8` maps to `.rdata` file offsets `0x20c1f4-0x20c2b8`, length `0xc4`. | Confirms the exact physical half-open child range. |
| UTF-16LE decode | The block decodes as `000`, `%03d`, `.mnm`, `https://s3.amazonaws.com/kru-downloads/tk/minimaps/`, `%s%s/%s%s`, and `Downloading : %d\n`; `0x0060d8b8` starts the separate item-shop version URL child. | Confirms current child contents and successor boundary. |
| Full-block uniqueness | The full `0xc4` target block occurs once in the PE at file offset `0x20c1f4` / VA `0x0060d7f4`. | Confirms this page covers the sole physical pooled instance of the complete minimap literal set. |
| Short literal duplicates | `000` also appears at `0x006107b2` and `0x00612840`; `%03d` also appears at `0x00612e82`; `.mnm` also appears at `0x006107fa`. | Short strings are not used as owner proof. |
| Unique long literals | The S3 minimap URL, `%s%s/%s%s`, and `Downloading : %d\n` are unique at this target. | The meaningful long strings prove this specific minimap download child even though the short strings are pooled elsewhere. |
| Raw MiniMap body | Raw range `0x00454e30-0x00455040` occurs once in `.text`; direct immediates inside it reference `0x0060d7e0` at `+0x23`, `0x0060d808` at `+0x40`, `0x0060d818` at `+0x4c`, and `0x0060d894` at `+0x19d`. | Strengthens MiniMap-side use without creating a third emitter or a source-root repair. |

B003 direct xref and reference-form accounting:

| Literal or address | B003 xrefs / hits | Current disposition |
| --- | --- | --- |
| `0x0060d7f4` `000` | `0x0041a794` in `sub_41A750`; `0x00453ae4` in `sub_453AA0`; absolute hits at `0x0041a796` and `0x00453ae6`; no RVA or rel32 hits. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d7f8` overlapping tail/head | `0x0041a7a0` in `sub_41A750`; `0x00453aed` in `sub_453AA0`; absolute hits at `0x0041a7a2` and `0x00453aef`; no RVA or rel32 hits. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d7fc` `%03d` | `0x0041a7d2` in `sub_41A750`; `0x00453b12` in `sub_453AA0`; absolute hits at `0x0041a7d3` and `0x00453b13`; no RVA or rel32 hits. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d808` `.mnm` | `0x0041a7e2` in `sub_41A750`; `0x00453b22` in `sub_453AA0`; raw `0x00454e6f`; absolute hits at `0x0041a7e3`, `0x00453b23`, and `0x00454e70`; no RVA or rel32 hits. | Shared modeled use plus raw MiniMap-side support. |
| `0x0060d818` S3 minimap URL | `0x0041a7f2` in `sub_41A750`; `0x00453b32` in `sub_453AA0`; raw `0x00454e7b`; absolute hits at `0x0041a7f3`, `0x00453b33`, and `0x00454e7c`; no RVA or rel32 hits. | Shared modeled use plus raw MiniMap-side support. |
| `0x0060d880` `%s%s/%s%s` | `0x0041a7f7` in `sub_41A750`; `0x00453b37` in `sub_453AA0`; absolute hits at `0x0041a7f8` and `0x00453b38`; no RVA or rel32 hits. | Shared modeled FileDownloader/MiniMap use. |
| `0x0060d894` `Downloading : %d\n` | `0x0041a988` in `sub_41A750`; `0x00453cd7` in `sub_453AA0`; raw `0x00454fcc`; absolute hits at `0x0041a989`, `0x00453cd8`, and `0x00454fcd`; no RVA or rel32 hits. | Shared modeled use plus raw MiniMap-side support. |
| `0x0060d8b8` successor | `0x0041aa64` in `sub_41AA00`; absolute hit at `0x0041aa65`; no RVA or rel32 hits. | Separate [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md), not part of this range. |

B003 inventory and support-document conclusions:

| Item | B003 role | Current disposition |
| --- | --- | --- |
| [UID:0003FZ] this target | Pooled minimap download wide literals, `CANONICAL_OWNER:NONE`, `EMITTER_UIDS:0000JC,0000LE`, `89/93`. | Keep unchanged. |
| [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) | FileDownloader message `10000` helper using all target literals. B003 saw the helper below the 85/85 local page gate at the time, but live IDA still proved FileDownloader source use because the file root cleared the route gate. | The route remains valid; the helper page can improve independently and does not block this emitter decision. |
| [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md) | MiniMapDownloader function island including `sub_453AA0`. | Supports MiniMap emitter `0000LE`. |
| Raw `0x00454e30-0x00455040` | Helper-shaped MiniMap-neighborhood bytes using selected target literals. | Evidence only; not a separate emitter. |
| [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) | Mixed split-audit literal container. | Not an owner and not an emitter. |
| [UID:0003G0][0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals](by-memory/0x0060d8b8-0x0060d94c.CashShopVersionDownloadWideLiterals.md) | Successor item-shop version URL child. | Confirms the end boundary at `0x0060d8b8`. |

Ranked B003 ownership disposition:

| Candidate or action | B003 decision | Current disposition |
| --- | --- | --- |
| `CANONICAL_OWNER:NONE`; `EMITTER_UIDS:0000JC,0000LE` | Accepted. Both modeled helpers consume every target literal through distinct dispatch routes; no internal call edge, owner table, relocation pattern, source/debug marker, or declaration artifact proves one source owner. | Accepted and unchanged. |
| Canonical [UID:0000LE][MiniMap](by-file/MiniMap.md) | Rejected. The strings are minimap-domain-specific and MiniMapDownloader consumes the block, but FileDownloader message `10000` also consumes the whole physical block directly. | MiniMap remains emitter `0000LE` only. |
| Canonical [UID:0000JC][FileDownloader](by-file/FileDownloader.md) | Rejected. The FileDownloader helper consumes the block and adjacent item-shop children are FileDownloader-owned, but MiniMapDownloader and raw MiniMap-side code also consume this target. | FileDownloader remains emitter `0000JC` only. |
| New `MinimapDownloadConstants` / `MinimapDownloadShared` owner | Rejected. The near-clone helpers make a shared declaration plausible, but no binary/source/debug evidence proves a shared owner. | Do not create a synthetic owner/file/global. |
| Further split of [UID:0003FZ] | Rejected. The exact `0xc4` block is unique and behaviorally coherent; every subrange participates in the same minimap URL/path/progress construction flow, and the successor is already split. | Keep the current range intact. |
| Non-reconstructable compiler/linker output | Rejected. The physical pooling/layout is generated-binary behavior, but the contents are source-authored NexusTK literals that must surface in consuming source contexts. | Keep `RECONSTRUCTABLE:TRUE`. |

Final C++ implication from B003: keep this physical literal child without a direct C++ block. Reconstructed FileDownloader and MiniMap code should spell or route the corresponding literals at consuming use sites unless later source/debug evidence proves a named shared declaration. No support-doc edit was required for B003 because the existing FileDownloader, MiniMap, `DownloadMinimapFile`, MiniMapDownloader, mixed container, proposed source-tree, and human coverage docs already support the accepted state at equal or greater specificity.

## Score Rationale

- Completion is `89` because the page records exact ranges for every wide literal, byte-level UTF-16LE decoding, xrefs by consumer family, modeled helper comparison, the raw MiniMap-side helper context, the `0x0060d8b8` successor boundary, and the direct-owner blocker.
- Confidence is `93` because live IDA xrefs, component analysis, raw-range signature evidence, byte decoding, and the already documented FileDownloader/MiniMapDownloader behavior all agree. It remains below final-audit range because the source-level literal pooling versus shared declaration decision is still unresolved.

## Cross-References

- [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md)
- [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md)
- [UID:0000XN][0x00453910-0x00453def.MiniMapDownloader](by-memory/0x00453910-0x00453def.MiniMapDownloader.md)
- [UID:0000JC][FileDownloader](by-file/FileDownloader.md)
- [UID:0000LE][MiniMap](by-file/MiniMap.md)
- [UID:0001SF][DownloaderMessageIds](by-type/by-constant/DownloaderMessageIds.md)

## Changes

- 2026-06-22 Rule 26 incorporation of B001-0003FZ ownership report added the missing PE/pointer-scan negatives, rejected split table, ranked owner analysis, and C++/no-shared-declaration implication. Metadata remains `89/93`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000JC,0000LE`; no coverage row change is required.
- 2026-06-22 Rule 26 post-migration recheck closeout incorporated the remaining report-level details from the archived B001 post-migration recheck: generated output-path caveat, exact dispatcher routes, duplicate short-literal occurrence distribution, source/debug search negatives, and the no-coverage/no-C++ disposition. Metadata and coverage semantics remain unchanged.
- 2026-06-22 Rule 26 incorporation of B002 post-migration owner/emitter report: metadata unchanged because the old blank-emitter recommendation has already been applied as `EMITTER_UIDS:0000JC,0000LE`. Added dedicated claim accounting for the owner/emitter split, `0003IO` pooled-literal precedent, FileDownloader message `10000` and MiniMapDownloader message `0` source-use routes, raw MiniMap-neighborhood support refs, PE/file-offset/unique-block evidence, rejected sole-owner/shared-constants/split/merge/non-reconstructable alternatives, and no-C++/no-coverage-change disposition.
- 2026-06-22 Rule 26 incorporation of B002 current no-owner sweep: metadata unchanged. Added the sweep-specific audit section preserving generated-state trigger, active no-owner tracker context, IDA session/function-size facts, component/callee comparison, source/debug negatives, raw PE mapping/reference-form scan, historical [UID:0002TR][0x0041a750-0x0041a9fe.DownloadMinimapFile](by-memory/0x0041a750-0x0041a9fe.DownloadMinimapFile.md) score context, ranked ownership rejections, and no-change/no-coverage recommendation.
- 2026-06-22 Rule 26 incorporation of B002 live Goal 2 no-owner pass: metadata unchanged. Added the live-session provenance (`a001_goal2_class_batch`), explicit literal-start xref table, raw PE MD5/file-offset/reference-hit details, IDA string-cache caveat, support-document comparison, and the report's no-split/no-merge/no-C++/no-coverage disposition.
- 2026-06-22 Rule 26 incorporation of B003 current Goal 2 no-owner pass: metadata unchanged. Added the B003 current-pass provenance (`b001_0003gy`), explicit owner/emitter rule application, FileDownloader/MiniMap dispatcher routes, IDA function-size and raw-function-boundary facts, component/no-call-edge comparison, PE section/raw-offset/decode/uniqueness evidence, per-literal xref/reference-form accounting, support-document comparison, ranked ownership rejections, and no-split/no-merge/no-C++/no-coverage disposition.
- 2026-06-21 B007 MiniMapRendererAndControls sync kept metadata unchanged and added the raw helper range/name nuance for the MiniMap-side literal consumers.
- 2026-06-12 A003 Batch261 split this minimap literal group from [UID:0003AL][0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals](by-memory/0x0060d7e0-0x0060d9c0.CashShopDownloaderLiterals.md) and kept it parentless because the exact strings are shared by the generic FileDownloader minimap message path and the MiniMapDownloader worker path.
- 2026-06-12 A003 Batch320 rechecked live IDA/MCP xrefs, component-level shared globals, raw MiniMap-side helper bytes, and the PE-decoded UTF-16LE range; raised `87/92` to `89/93`, kept `RECONSTRUCTABLE:TRUE`, and left `AUTOGEN_PARENT_UID` blank because the single direct source owner is still unproven.
- 2026-06-13 B002 post-migration owner/emitter review kept `CANONICAL_OWNER:NONE`, but changed `EMITTER_UIDS` from blank to `0000JC,0000LE`. Live IDA/PE evidence proves source-use contexts in FileDownloader message `10000` (`sub_41A750`) and MiniMapDownloader (`sub_453AA0`), with additional raw MiniMap-side refs at `0x00454e6f`, `0x00454e7b`, and `0x00454fcc`. No split, shared constants owner, reconstruction C++, or score change was recommended.
- 2026-06-13 B001 second-pass owner/emitter audit retained `89/93`, `CANONICAL_OWNER:NONE`, and `EMITTER_UIDS:0000JC,0000LE`. B001 reconfirmed the same two source-use emitters, rejected single FileDownloader/MiniMap or synthetic shared constants ownership, and found no split/reclassification/IDA repair requirement.
