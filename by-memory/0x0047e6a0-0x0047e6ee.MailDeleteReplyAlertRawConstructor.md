*** UID:0002T7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// [UID:0002T7] by-memory/0x0047e6a0-0x0047e6ee.MailDeleteReplyAlertRawConstructor.md
MailDeleteReplyAlert::MailDeleteReplyAlert(bool deleteSucceeded,
                                           const wchar_t *messageText,
                                           Pane *layoutReference,
                                           bool deleteAllReplies)
    : AlertPane(messageText, layoutReference, L"OK", 0),
      m_deleteAllReplies(deleteAllReplies)
{
    g_pMailDeleteReplyAlert = this;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-authored retained MailDeleteReplyAlert constructor; exact 78-byte body and ret 0x10 ABI, modeled DeleteReplyAlert sibling, and four optimized construction mirrors resolve four explicit parameters as an intentionally unused delete-result gate, message text, layout reference, and delete-all mode; the body initializes AlertPane, stores m_deleteAllReplies, installs three vtables, and publishes g_pMailDeleteReplyAlert. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047e6a0-0x0047e6ee MailDeleteReplyAlert Raw Constructor

## Status

- Address range: `0x0047e6a0-0x0047e6ee`
- Entity kind: raw constructor-shaped method bytes
- Disposition: reconstructable NexusTK project code
- Rebuild handling: `source-authored` constructor semantics, with compiler-emitted vtable stores generated from class declarations.
- Owner: [UID:00007J][MailDeleteReplyAlert](by-class/MailDeleteReplyAlert.md) `MailDeleteReplyAlert`
- Likely source root: [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md) `BulletinReplyAlerts`, with alternate placement under [UID:0000KZ][MailDialogs](by-file/MailDialogs.md) `MailDialogs`.
- Evidence basis: live IDA MCP/disassembly refreshes on 2026-06-04, 2026-06-06, and 2026-07-22 against `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`. The 2026-07-22 pass used healthy active database session `9b0396a3` and resolved the prior signature/no-code blocker through the modeled `DeleteReplyAlert` sibling and four optimized construction mirrors.

## Behavior

This raw constructor-shaped range initializes a `MailDeleteReplyAlert` alert pane instance, installs the three `MailDeleteReplyAlert` vtable views, records the delete-all/single-delete mode, and writes the mail delete-reply alert singleton.

Live instruction flow:

- calls the shared alert/base constructor helper at `0x0049feb0` with the incoming constructor arguments plus the `0x00613a18` mail/delete alert text pointer and a zero flag;
- stores the incoming mode byte from `[ebp+0x14]` at `this + 0x270`, now best documented as `MailDeleteReplyAlert::m_deleteAllReplies`;
- installs `MailDeleteReplyAlert` vtable views at `this + 0x00`, `this + 0xa0`, and `this + 0xa4`;
- stores `this` into [UID:0002W5][g_pMailDeleteReplyAlert](by-global/g_pMailDeleteReplyAlert.md), the mail delete-reply alert singleton at `0x0067adbc`.

The constructor call shape pushes `0`, shared alert OK/primary-button resource pointer `0x00613a18`, `[ebp+0x10]`, and `[ebp+0x0c]` before calling [UID:00012W][0x0049feb0-0x004a0686.AlertPaneCore](by-memory/0x0049feb0-0x004a0686.AlertPaneCore.md). Treat `0x00613a18` as a shared primary-button/OK resource pointer until the exact source symbol is recovered.

### Source Parameter Map

The `retn 0x10` epilogue accounts for four explicit stack parameters. Current source-facing order and roles are:

| Stack slot | Source-facing parameter | Evidence and role |
| --- | --- | --- |
| `[ebp+0x08]` | `bool deleteSucceeded` | The retained mail body intentionally does not read this slot. Modeled sibling `DeleteReplyAlert::DeleteReplyAlert` receives the corresponding packet byte, normalizes it into `this+0x271`, and its primary action checks that gate before changing the local reply list. All four mail inline mirrors still evaluate the corresponding packet byte before constructing the alert, which is the optimized residue of an unused source argument rather than a hidden compiler parameter. The exact original spelling is not recovered; `deleteSucceeded` is the best descriptive source name. |
| `[ebp+0x0c]` | `const wchar_t *messageText` | Forwarded as the message argument to `AlertPane`. |
| `[ebp+0x10]` | `Pane *layoutReference` | Forwarded as the inherited layout/reference pane and later consumed through `AlertPane::m_layoutReference`. |
| `[ebp+0x14]` | `bool deleteAllReplies` | Stored at derived offset `+0x270` as `m_deleteAllReplies`; inline mirrors use `1` for mail-list bulk deletion and `0` for single-reply mail-dialog deletion. |

The modeled sibling at `0x0047e2f0` has prototype shape `this, char, const wchar_t *, Pane *, char`, cleans the same four explicit stack arguments, and has direct callers at `0x0047442f`, `0x004748c5`, `0x004769c7`, and `0x00476add`. Its first field at `+0x271` controls whether local deletion occurs, while its fourth parameter at `+0x270` controls selected-all versus single-reply behavior. The mail class retains only the fourth field.

## Raw Byte Evidence

2026-06-06 live IDA byte audit:

| Range | Bytes | Interpretation |
| --- | --- | --- |
| `0x0047e694-0x0047e6a0` | `cc cc cc cc cc cc cc cc cc cc cc cc` | Alignment after the prior modeled function. |
| `0x0047e6a0-0x0047e6ee` | `55 8b ec 51 56 6a 00 68 18 3a 61 00 ff 75 10 8b f1 ff 75 0c 89 75 fc e8 f4 17 02 00 8a 45 14 88 86 70 02 00 00 8b c6 c7 06 6c 47 61 00 c7 86 a0 00 00 00 d4 47 61 00 c7 86 a4 00 00 00 04 48 61 00 89 35 bc ad 67 00 5e 8b e5 5d c2 10 00` | Complete raw constructor-shaped body. The final `00` byte completes the `retn 10h` immediate. |
| `0x0047e6ee-0x0047e6f0` | `cc cc` | Alignment before the non-deleting destructor. |

## Evidence Notes

- [UID:0001Y2][MailDialogVtableFamily](by-type/by-vtable/MailDialogVtableFamily.md) records current IDA-backed `MailDeleteReplyAlert` vtable stores, including the constructor-area store at `0x0047e6c7`, and the primary/secondary/tertiary vtable bases `0x0061476c`, `0x006147d4`, and `0x00614804`.
- [UID:0000ZM][0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions](by-memory/0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions.md) records that IDA does not currently define the projected constructor starts in this alert strip as function objects, including `0x0047e6a0`.
- [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md) lists `0x0047e6a0` as the projected `MailDeleteReplyAlert` constructor and keeps the wrapper source grouping conservative because several constructors in this strip are raw-disassembly-only.
- Live IDA MCP on 2026-06-04 reports no function record at `0x0047e6a0`, no function at the inclusive/end padding addresses `0x0047e694`, `0x0047e6ed`, or `0x0047e6ee`, previous function `sub_47E510` at `0x0047e510-0x0047e694`, and next function `sub_47E6F0` at `0x0047e6f0-0x0047e719`.
- Live IDA MCP on 2026-06-06 reconfirms no function record at `0x0047e6a0`, `0x0047e6eb`, `0x0047e6ed`, or `0x0047e6ee`; the next modeled function remains `sub_47E6F0` at `0x0047e6f0-0x0047e719`.
- Live disassembly on 2026-06-06 confirms `0x0047e694-0x0047e6a0` is twelve `0xcc` alignment bytes, the raw constructor body is `0x0047e6a0-0x0047e6ee`, the `retn 0x10` starts at `0x0047e6eb` and consumes bytes through `0x0047e6ed`, and `0x0047e6ee-0x0047e6f0` is two `0xcc` alignment bytes before the destructor.
- Live `xrefs_to 0x0047e6a0` and a loaded-segment pointer scan for little-endian `0x0047e6a0` both return no hits. The range is therefore documented as constructor-shaped source bytes with unresolved reachability, not as a currently referenced constructor entry point.
- Live IDA code-reference scan on 2026-06-06 again reports no direct code references to `0x0047e6a0`.
- B009 2026-06-19 PE/Capstone reanalysis closed the reachability question with a broader negative route proof: no direct `E8`/`E9` `rel32` target, no absolute dword occurrence of `0x0047e6a0`, no recovered vtable or jump-table pointer, and no confirmed static entry into the body interior. This is not padding and not random artifact because the decoded body exactly matches the class constructor pattern and live inline construction semantics.
- The same pass identified four inline construction mirrors that corroborate the constructor source shape:
  - Function `0x0047a3b0` evaluates the packet byte at `0x0047a42b` and discards the result, then `0x0047a4bf/0x0047a4c5/0x0047a4cf` install the three vtables, set `this+0x270 = 1`, and publish `g_pMailDeleteReplyAlert` at `0x0047a4e0`.
  - Function `0x0047a760` evaluates the packet byte at `0x0047a7b9` and discards the result, then `0x0047a84d/0x0047a853/0x0047a85d` install the same vtables, set `this+0x270 = 1`, and publish the singleton at `0x0047a86e`.
  - Function `0x0047c9f0` evaluates the packet byte at `0x0047ca63` and discards the result, then `0x0047caf7/0x0047cafd/0x0047cb07` install the same vtables, set `this+0x270 = 0`, and publish the singleton at `0x0047cb18`.
  - Function `0x0047cb40` evaluates the packet byte at `0x0047cb99` and discards the result, then `0x0047cc2d/0x0047cc33/0x0047cc3d` install the same vtables, set `this+0x270 = 0`, and publish the singleton at `0x0047cc4e`.
- The discarded packet-byte evaluations are decisive signature evidence. They match evaluation of the first constructor argument after the constructor itself was inlined and its unused parameter body was optimized away.
- Healthy live MCP session `9b0396a3` on 2026-07-22 reconfirmed the modeled sibling prototype and all four direct callers, the sibling primary action's `+0x271` gate and `+0x270` mode branch, all four mail mirror decompilations, zero target-start xrefs, the exact target bytes, and the complete vtable/global xref sets.
- Live vtable xrefs on 2026-06-04 confirm constructor stores at `0x0047e6c7`, `0x0047e6cd`, and `0x0047e6d7`; destructor stores at `0x0047e6f0`, `0x0047e6f6`, `0x0047e700`; and scalar destructor stores at `0x0047eb96`, `0x0047eb9c`, and `0x0047eba6`.
- Live singleton xrefs on 2026-06-04 confirm the constructor writes `dword_67ADBC` at `0x0047e6e1`, the destructor clears it at `0x0047e70a`, the getter reads it at `0x0047e720`, and the scalar destructor clears it at `0x0047ebb0`.

## Boundary Notes

The half-open page range is byte-level documented as `0x0047e6a0-0x0047e6ee`. The previous `0x0047e6a0-0x0047e6ed` filename/range omitted the final immediate byte of the three-byte `retn 10h` instruction. IDA still has no modeled function object or direct caller/pointer hit for the start, but that no-route state is now explained by four optimized inline constructions while the retained external-linkage body remains in the binary. The base call, parameter ABI, vtable stores, singleton write, inline residue, sibling signature, and neighboring modeled functions identify this as source-authored constructor logic rather than padding or compiler-only glue.

## Reconstruction Gate

The constructor now emits from the managed C++ header. The four-parameter ABI, first result/gate parameter, message/layout arguments, delete-all mode, base initializer, derived field, vtables, and singleton effect are all accounted for. `deleteSucceeded` is intentionally unused by this mail variant; retaining a normal source parameter preserves the exact ABI and matches the packet-byte evaluation left in all four inline mirrors.

The `L"OK"` initializer is the source-facing form already used by the sibling alert constructors for shared resource pointer `0x00613a18`. Exact historical symbol spelling for that resource and the physical `BulletinReplyAlerts.cpp` versus `MailDialogs.cpp` split remain confidence caps, not code blockers.

## Score Rationale

- Completion is `92` because the exact body/boundaries, four explicit ABI slots, first result/gate parameter, message/layout parameters, delete-all mode, base constructor, `m_deleteAllReplies`, three vtables, singleton publication, modeled sibling behavior/callers, four inline mirrors, owner/emitter, and formal source body are now documented and coherent.
- Confidence is `94` because live IDA MCP independently confirms the raw bytes, modeled sibling signature/action/callers, inline packet-byte residue, mode values, vtable/global xrefs, and absence of a direct route. It remains below final-source certainty because the exact original spelling of `deleteSucceeded` and the physical `BulletinReplyAlerts.cpp` versus `MailDialogs.cpp` split are inferred rather than symbol/PDB-proven.

## Cross-References

- [UID:00007J][MailDeleteReplyAlert](by-class/MailDeleteReplyAlert.md) `MailDeleteReplyAlert`
- [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md) `BulletinReplyAlerts`
- [UID:0000KZ][MailDialogs](by-file/MailDialogs.md) `MailDialogs`
- [UID:0000ZM][0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions](by-memory/0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions.md) `0x00478fe0-0x0047ec6b.BulletinMailAlertCompanions`
- [UID:0000ZO][0x00479110-0x0047ec2b.MailDialogs](by-memory/0x00479110-0x0047ec2b.MailDialogs.md) `0x00479110-0x0047ec2b.MailDialogs`
- [UID:0002T8][0x0047e6f0-0x0047e726.MailDeleteReplyAlertDestructorAndSingletonGetter](by-memory/0x0047e6f0-0x0047e726.MailDeleteReplyAlertDestructorAndSingletonGetter.md) `MailDeleteReplyAlert` destructor/getter neighbor.
- [UID:0001Y2][MailDialogVtableFamily](by-type/by-vtable/MailDialogVtableFamily.md) `MailDialogVtableFamily`
- [UID:0002AI][0x0067adb8-0x0067adc0.BoardMailDeleteReplyAlertSingletons](by-memory/0x0067adb8-0x0067adc0.BoardMailDeleteReplyAlertSingletons.md) `0x0067adb8-0x0067adc0.BoardMailDeleteReplyAlertSingletons`

## Historical B006 2026-06-29 No-Code Decision

- The 2026-06-29 callback treated this as a rare formal no-code exception because `ret 0x10` accounted for four parameters while `[ebp+0x08]` remained unused/unidentified and no direct route existed.
- That evidence remains valid, but the old conclusion is superseded. The 2026-07-22 reanalysis correlated the explicit four-parameter `DeleteReplyAlert` sibling, its direct callers, its `+0x271` local-removal gate, and the discarded packet-byte evaluation in all four MailDeleteReplyAlert inline mirrors. The first slot is therefore a real source-level result/gate parameter intentionally unused by the mail variant, not an unrepresentable hidden parameter.

## Changes

- 2026-07-22 B005 accepted implementation of `0002T7-MailDeleteReplyAlertRawConstructor-source-quality.md`:
  - Changed `87/89 -> 92/94`; owner/emitter remain [UID:00007J][MailDeleteReplyAlert](by-class/MailDeleteReplyAlert.md), reconstructable true.
  - Replaced the historical formal no-code comment with the four-parameter source constructor.
  - Resolved `[ebp+0x08]` as the intentionally unused source-level `deleteSucceeded` result/local-removal gate by comparing modeled `DeleteReplyAlert` constructor/action/callers with four optimized mail construction mirrors.
  - Resolved `[ebp+0x14]` as `deleteAllReplies`, preserved `m_deleteAllReplies`, `g_pMailDeleteReplyAlert`, exact bytes/padding, and all negative route evidence, and historicalized the superseded no-code interpretation.

- 2026-06-21 supervisor-worker Rule 26 incorporation of B009 `00007J-MailDeleteReplyAlert-class-source-quality.md`:
  - Changed score from `85/88` to `87/89`.
  - Added the no-direct-route/no-interior-entry proof, four inline construction mirrors, `m_deleteAllReplies` name for `this+0x270`, `g_pMailDeleteReplyAlert` naming, shared OK/primary-button resource caveat at `0x00613a18`, and reference constructor shape while keeping formal C++ blank under the no-entry policy.
- 2026-06-16 supervisor execution of B001 source-split report: Completion changed from `82` to `85`. The B001 split audit reconfirmed this exact child as the `MailDeleteReplyAlert` raw constructor inside the now non-emitting [UID:0000ZO][0x00479110-0x0047ec2b.MailDialogs](by-memory/0x00479110-0x0047ec2b.MailDialogs.md) split inventory; owner/emitter remain [UID:00007J][MailDeleteReplyAlert](by-class/MailDeleteReplyAlert.md), and final C++ remains blank because raw-constructor reachability and source signature names are unresolved.
- 2026-06-06 A007: Renamed the page from `0x0047e6a0-0x0047e6ed` to `0x0047e6a0-0x0047e6ee` after live IDA confirmed that the constructor `retn 10h` instruction is `c2 10 00` at `0x0047e6eb-0x0047e6ed` and padding starts at `0x0047e6ee`. Added raw byte evidence, reconfirmed the no-function/no-direct-code-reference status, linked the corrected destructor/getter neighbor, and raised the page to `82/88`.
- 2026-06-04 live IDA refresh:
  - What existed before: the page had `72/80`, current-session MCP-unavailable caveats, no raw instruction-flow inventory, and only indirect vtable/singleton evidence.
  - Changed to: completion `80`, confidence `86`, live evidence basis, explicit constructor instruction flow, exact boundary/padding notes, vtable and singleton xref details, and no-xref/no-pointer reachability caveat. Historical note: old blank-C++ reasoning used a `95/95` gate; B006 now keeps this row no-code for the specific unused `[ebp+0x08]` / no-entry-route proof above.
  - Summary/evidence: live IDA MCP `idb_meta` and `py_eval` on 2026-06-04 verified IDB identity, function/nonfunction boundaries, byte/disassembly shape, `sub_49FEB0` base-constructor call, `this + 0x270` mode-byte write, vtable stores to `0x0061476c`, `0x006147d4`, and `0x00614804`, `dword_67ADBC` singleton write, related destructor/getter xrefs, and absence of direct xrefs or pointer hits for `0x0047e6a0`.
