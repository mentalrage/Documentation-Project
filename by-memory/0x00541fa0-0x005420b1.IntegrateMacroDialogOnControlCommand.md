*** UID:00045G | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "../../profile/ProfileStorage.h"

void IntegrateMacroDialog::OnControlCommand(int controlIndex, int /*notifyCode*/)
{
    switch (controlIndex)
    {
    case kDialogCommandOk:
        ClearMacroEditFields();
        Close();
        g_pGeneralPurposePanel->RemoveDialog(this);
        if (g_activeMapPane != NULL)
            SaveUserSettings();
        break;

    case kMacroPreviousPageCommand:
        ClearMacroEditFields();
        if (m_currentPage <= 0)
            break;

        --m_currentPage;

        GetChild<ImageButtonControlPane>(kMacroNextPageCommand)->Enable();
        GetChild<ImageButtonControlPane>(kMacroPreviousPageCommand)->Deactivate();

        if (m_currentPage == 0)
            GetChild<ImageButtonControlPane>(kMacroPreviousPageCommand)->Disable();

        RefreshMacroEditFields();
        break;

    case kMacroNextPageCommand:
        ClearMacroEditFields();
        if (m_currentPage >= m_pageCount - 1)
            break;

        ++m_currentPage;

        GetChild<ImageButtonControlPane>(kMacroPreviousPageCommand)->Enable();
        GetChild<ImageButtonControlPane>(kMacroNextPageCommand)->Deactivate();

        if (m_currentPage == m_pageCount - 1)
            GetChild<ImageButtonControlPane>(kMacroNextPageCommand)->Disable();

        RefreshMacroEditFields();
        break;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact IntegrateMacroDialog primary +0x48 OnControlCommand override for OK, previous-page, and next-page controls, preserving clear-before-close/page-change ordering, g_activeMapPane-gated SaveUserSettings behavior, inline navigation, and image-button enable/deactivate/disable semantics. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00541fa0-0x005420b1 IntegrateMacroDialogOnControlCommand

## ProfileStorage Dependency Closure - 2026-08-25

This first MacroDialogs writer caller contributes the translation-unit-level `../../profile/ProfileStorage.h` include used by all three macro-dialog save paths. The shared include occurs once in generated `MacroDialogs.cpp`; later sibling emitters call `SaveUserSettings` without adding duplicate declarations. Macro-dialog classes and their Config-backed 264-byte records remain owned by the MacroDialogs source family.

## Status

- Address range: `0x00541fa0-0x005420b1` half-open, size `0x111`.
- Current IDA/MCP function: `sub_541FA0`, size `0x111`, ending at `0x005420b1`.
- Direct owner and emitter: [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) `IntegrateMacroDialog`.
- Source file route: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) `MacroDialogs`, projected to `NexusTK/ui/dialogs/MacroDialogs.cpp`.
- Parent split/container: [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md).
- Rebuild handling: source-authored primary `+0x48` virtual control-command handler.
- Formal C++ disposition: populated with first-draft source C++. The earlier split-stage blank-C++ state is superseded by B008's accepted 2026-06-28 source-quality report for UID00045G.

## Behavior

`IntegrateMacroDialog::OnControlCommand` handles the integrated macro dialog's OK, previous-page, and next-page controls. IDA renders the first stack parameter as the switch value and the second stack parameter as unused; the accepted source-facing shape is `void IntegrateMacroDialog::OnControlCommand(int controlIndex, int /*notifyCode*/)` matching the common two-integer primary `+0x48` contract used by MacroDialog, SpellMacroDialog, NewMacroDialog, and IntegrateMacroDialog. Historical `OnButtonClick`, Spell `OnConfirm`, New `OnCommand`, and `MacroDialog::SaveMacros` names are superseded provenance only.

| Command id | Source-facing constant | Behavior |
| --- | --- | --- |
| `1` | `kDialogCommandOk` | Calls `ClearMacroEditFields()`, closes the dialog, removes it from `g_pGeneralPurposePanel`, and calls `SaveUserSettings()` when `g_activeMapPane` is non-null. |
| `12` | `kMacroPreviousPageCommand` | Calls `ClearMacroEditFields()` first, returns if `m_currentPage <= 0`, decrements `m_currentPage`, enables next command `13`, deactivates previous command `12`, disables previous at page zero, then calls `RefreshMacroEditFields()`. |
| `13` | `kMacroNextPageCommand` | Calls `ClearMacroEditFields()` first, returns if `m_currentPage >= m_pageCount - 1`, increments `m_currentPage`, enables previous command `12`, deactivates next command `13`, disables next at the last page, then calls `RefreshMacroEditFields()`. |

The action command ids are separate from constructor image/style ids: image id `14` is OK, image id `18` is previous, and image id `19` is next.

## Source Shape Notes

The formal C++ intentionally duplicates the previous/next page navigation logic inline instead of calling [UID:00045J][0x00542100-0x00542172.IntegrateMacroDialogAdvanceMacroPageRaw](by-memory/0x00542100-0x00542172.IntegrateMacroDialogAdvanceMacroPageRaw.md) `AdvanceMacroPage()` or the raw retreat helper [UID:00045K][0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw](by-memory/0x00542180-0x005421e3.IntegrateMacroDialogRetreatMacroPageRaw.md). The current binary contains duplicated page-advance/page-retreat logic in this virtual handler and has no call from UID00045G to the raw helper entries. The reachable command branches also include the `ClearMacroEditFields()` prelude before page movement, while UID00045J's retained out-of-line helper does not.

The accepted source-facing names replace IDA labels in final C++:

| Binary label or raw evidence | Source-facing interpretation |
| --- | --- |
| `sub_5420D0` | `ClearMacroEditFields()` |
| `sub_5421F0` | `RefreshMacroEditFields()` |
| `sub_49EB90` | inherited `Close()` / close-slide behavior, matching local MacroDialogs formal C++ |
| `sub_469180(dword_67A74C, this)` | `g_pGeneralPurposePanel->RemoveDialog(this)` |
| `dword_67A764` active-map pointer gate in this handler context | `g_activeMapPane` |
| `sub_50ABA0` | `SaveUserSettings()` |
| `this+0x26c` | `m_pageCount` |
| `this+0x270` | `m_currentPage` |
| child-control route `this+0x1fc` | `GetChild<ImageButtonControlPane>(...)` |

## Control Semantics

B008's accepted report rechecked the image-button vtable operations during evidence-time MCP session `supervisor_20260628_resume`:

- `ImageButtonControlPane` vtable slot `+0x4c -> 0x00495d10` sets byte `+0x101` to `1`, sets image state `+0x110` / decimal `272` to `0`, redraws, and is represented as `Enable()`.
- Slot `+0x50 -> 0x00495d80` clears byte `+0x101`, sets image state `+0x110` / decimal `272` to `2`, redraws, and is represented as `Disable()`.
- Slot `+0x58 -> 0x00494c10` clears inherited byte `+0x102` and redraws, so the handler deactivates the clicked direction button after a successful page change. The formal source uses `Deactivate()` for this operation.

## Live MCP Evidence

B008 rechecked UID00045G with MCP session `supervisor_20260628_resume` on 2026-06-28. At evidence time, `server_health` request `21` returned `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready; this does not assert that historical session remains alive.

`lookup_funcs` request `22` confirmed:

| Address | Result |
| --- | --- |
| `0x00541f96` | not a function; predecessor padding begins after `OnDraw`. |
| `0x00541fa0` | `sub_541FA0`, size `0x111`, this target. |
| `0x005420b0` | inside `sub_541FA0`. |
| `0x005420b1` | not a function; successor padding begins. |
| `0x005420d0` | `sub_5420D0`, size `0x2d`, `ClearMacroEditFields`. |
| `0x00542100` | not a function, retained raw `AdvanceMacroPage` helper entry. |
| `0x00542180` | not a function, raw retreat helper entry. |
| `0x005421f0` | `sub_5421F0`, size `0x75`, `RefreshMacroEditFields`. |

`get_bytes` request `25` confirmed `0x00541f96-0x00541fa0` is ten `0xcc` bytes and `0x005420b1-0x005420c0` is fifteen `0xcc` bytes. The existing half-open target boundary is correct and needs no split/container repair.

`xrefs_to` request `23` reported exactly one xref to `0x00541fa0`, a data xref from vtable entry `0x00621178`. `get_int` request `36` read `0x00621178` as decimal `5513120` / `0x00541fa0`, and `get_bytes` request `37` showed the primary-vtable bytes around `0x00621170` contain `0x00541e50` for `OnDraw` followed by `0x00541fa0` for primary `+0x48` `OnControlCommand`. `xrefs_to` request `38` confirmed the related vtable refs for `OnDraw` at `0x00621174`, this target at `0x00621178`, and `HandleKeyInput` at `0x00621198`.

`decompile` request `26` and `disasm` request `27` confirmed the handler has `retn 8`, switches on command id `1`, `12`, and `13`, and has no default body. The exact callee set from `callees` request `24` is `0x005420d0` `ClearMacroEditFields`, `0x005421f0` `RefreshMacroEditFields`, `0x0049eb90` close/slide helper, `0x00469180` general-purpose-panel remove helper, and `0x0050aba0` `SaveUserSettings`.

Helper decompilation supports the formal source names:

- `decompile` request `29` shows `ClearMacroEditFields` loops child ids `2..11` and invokes virtual slot `+0x58`.
- `decompile` request `30` shows `RefreshMacroEditFields` loops child ids `2..11` and binds rows using `g_pConfig + 0x28f2ec + (currentPage * 10 + row) * 0x108`.
- `decompile` requests `33`, `34`, and `35` confirm image/control slot behavior for `Enable()`, `Disable()`, and `Deactivate()`.

## Rejected Alternatives

- Keeping `EMITTER_UIDS:00006L` with blank formal C++ is rejected because it preserves the generated empty-emitter marker despite an exact vtable-routed source handler.
- Clearing `EMITTER_UIDS:` or documenting this page as no-code metadata is rejected. The target is source-authored feature code with command dispatch, row clearing, dialog close/remove, active-map-gated profile persistence, page navigation, and row refresh side effects.
- A covered-by marker to UID00045J or UID00045K is rejected. UID00045G is the reachable virtual command handler and includes behavior not covered by the raw page helpers, especially the OK/save path and clear-before-page-change prelude.
- Rewriting command `13` to call `AdvanceMacroPage()` is rejected. The binary duplicates the page-advance sequence inline and has no call to `0x00542100`.
- Split/container repair is rejected. MCP function inventory, disassembly, and padding bytes confirm the target is an exact single modeled function range.
- Moving ownership away from `IntegrateMacroDialog`/`MacroDialogs` is rejected. The primary vtable slot at `0x00621178` routes directly to this method, and all helper dependencies are subordinate behavior rather than owners.
- Raw labels such as `sub_5420D0`, `dword_67A764`, and vtable-slot arithmetic are rejected in formal C++. Exact global storage `0x0067a764` is the active MapPane singleton pointer, not a settings-dirty boolean; those raw labels remain evidence/search aliases only.
- `OnButtonClick` is rejected as the current virtual name. The exact primary `+0x48` family slot, two-integer ABI, and sibling Macro/Spell/New identities establish `OnControlCommand(int,int)`; button-like behavior does not rename the inherited interface.

## SaveUserSettings Synchronization

- Command 1 order is exact: clear bound macro edit fields, close, remove from the general-purpose panel, test `g_activeMapPane`, then call [UID:0001AV][0x0050aba0-0x0050b078.SaveUserSettings](by-memory/0x0050aba0-0x0050b078.SaveUserSettings.md). Commands 12 and 13 retain their clear-before-page-change behavior and do not call the writer.
- `g_activeMapPane` is caller policy. The target remains a global ProfileStorage function and does not become IntegrateMacroDialog- or MapPane-owned. The historical `g_userSettingsDirty` interpretation is superseded by direct global identity and caller-body evidence.
- Score, range, owner/emitter, position, nesting, vtable route, page logic, button semantics, and all unrelated evidence remain unchanged.

## Boundaries

- Previous range `0x00541f96-0x00541fa0` is ten bytes of `0xcc` padding after [UID:00045F][0x00541e50-0x00541f96.IntegrateMacroDialogOnDraw](by-memory/0x00541e50-0x00541f96.IntegrateMacroDialogOnDraw.md).
- Next range `0x005420b1-0x005420c0` is fifteen bytes of `0xcc` padding before [UID:00045H][0x005420c0-0x005420c9.IntegrateMacroDialogHandleKeyInput](by-memory/0x005420c0-0x005420c9.IntegrateMacroDialogHandleKeyInput.md).
- This child owns only the modeled virtual action handler. `ClearMacroEditFields`, `RefreshMacroEditFields`, retained raw `AdvanceMacroPage`, and raw retreat-page behavior remain exact sibling children.

## Score And Metadata Rationale

The target moves from `91/92` to `92/94` because the exact inherited interface identity is now resolved in addition to the already complete body. The range, padding, primary `+0x48` route, argument roles, command ids, helper callees, close/remove/save behavior, page field use, button slot semantics, inline duplicate logic, and rejected alternatives are documented with mutually reinforcing family evidence. Exact original constant and private-member spellings remain inferred but no longer block source output.

Owner/emitter/reconstructable routing did not change: `CANONICAL_OWNER:00006L`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:00006L` remain correct.

## Changes

- 2026-06-28 B008 implementation callback:
  - Raised the target from `COMPLETION:89` / `CONFIDENCE:91` to `COMPLETION:91` / `CONFIDENCE:92`.
  - Replaced the split-stage blank formal C++ with the accepted `void IntegrateMacroDialog::OnButtonClick(int commandId, int /*controlId*/)` body.
  - Preserved `CANONICAL_OWNER:00006L`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00006L`, and blank `EMITTER_POSITION_OPTIONAL:`.
  - Recorded B008's current MCP evidence from session `supervisor_20260628_resume`: exact function/range/padding facts, vtable slot `0x00621178`, command ids `1`/`12`/`13`, helper callees, close/remove/save behavior, page-count/current-page tests, image-button slot semantics, and rejected no-code/covered-by/split/container alternatives.
- 2026-07-14 B005 SaveUserSettings synchronization:
  - Preserved `91/92`, metadata, complete handler/page-navigation body, and evidence; changed only the command-1 gate from the contradicted dirty-flag name to `g_activeMapPane != NULL`.
  - Synchronized Item Summary, behavior table, source-name table, rejected alternatives, and exact clear -> close/remove -> active-map test -> profile-save order.
- 2026-07-15 B003 UID00049G family synchronization:
  - Validator-preserved UID00045G and exact range while renaming the source-facing page and method from `IntegrateMacroDialogOnButtonClick` to `IntegrateMacroDialogOnControlCommand`.
  - Raised `91/92` to `92/94`, retained owner/emitter UID00006L, true, blank position, and `Nested:0`, and preserved all OK/page/navigation/save behavior.
  - Corrected only the virtual-interface identity and first/second argument roles; historical names remain searchable in this Changes section.
