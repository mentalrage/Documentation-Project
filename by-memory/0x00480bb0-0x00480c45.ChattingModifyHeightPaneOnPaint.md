*** UID:0002F9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001W | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001W | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingModifyHeightPane::OnPaint()
{
    if (!g_pChattingHandlePane->m_secondaryChatEnabled) {
        SetMode(1);
        SetGrafPortColorState(0);
        g_surfaceRenderCallbacks->RefreshPaneRegion(this, &m_rect);
        return;
    }

    EPFTileContext tile;
    g_pEPFLib->LookupLayoutEntry(L"CHATSC.EPF", 0, &tile);
    RenderTileFrame(&tile, &tile.m_bounds, &m_rect, 0, L"invenbut.pal", 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA verifies exact paint bounds, vtable-only reachability, no ordinary callers, full callee set, `CHATSC.EPF` and `invenbut.pal` render assets, enabled and disabled branches, dispatcher refresh, padding, and security-cookie exits. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00480bb0-0x00480c45 ChattingModifyHeightPane OnPaint

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: paint/render method.
- Owner: [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md).
- Likely source placement: [UID:0000I5][Chatting](by-file/Chatting.md).
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Confidence: very strong for boundary, vtable reachability, render asset names, enabled and disabled branches, callee set, state-field names, surface refresh behavior, and padding; medium for exact final shared render helper spellings.

## Boundary

Live IDA models the function at `0x00480bb0-0x00480c45`, size `0x95`. The preceding `0x00480ba3-0x00480bb0` bytes are thirteen `0xcc` alignment bytes after the line-count helper, and `0x00480c45-0x00480c50` is eleven `0xcc` bytes before the next `ChattingVarietyPane` cluster at `0x00480c50`.

## Behavior

This paint method draws the chat-height resize handle when `g_pChattingHandlePane->m_secondaryChatEnabled` is enabled. In the enabled branch it creates a temporary [UID:00004I][EPFTileContext](by-class/EPFTileContext.md) through helper `0x00457a60`, requests `CHATSC.EPF` frame `0` through [UID:0002KP][0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry](by-memory/0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry.md) using [UID:0000QU][g_pEPFLib](by-global/g_pEPFLib.md), and draws with palette `invenbut.pal` through shared [UID:000165][0x004b9820-0x004ba24a.SurfacePaintHelpers](by-memory/0x004b9820-0x004ba24a.SurfacePaintHelpers.md) wrapper `0x004b9980` / `RenderTileFrame`. The source-rectangle argument is `ebp-0x1c`, but the local `EPFTileContext` starts at `ebp-0x2c`, so that pointer is `tile + 0x10` / `tile.m_bounds`; it is not an independent temporary draw record.

The render/refresh destination in both branches is the inherited pane rectangle at receiver offset `+0x44`, expressed in draft C++ as `m_rect` / pane bounds. Earlier wording used the class-local resize geometry field `m_handleRect` at `this+0x100`; that remains valid for constructor, set-line-count, drag, and cancel paths, but this paint body never reads `this+0x100` directly.

When the secondary-chat state is disabled, it calls [UID:0002V7][0x005446b0-0x005446d4.PaneSetMode](by-memory/0x005446b0-0x005446d4.PaneSetMode.md) with mode value `1`, calls the active GrafPort draw/fill color setter [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) `0x004b9660` with color/state `0`, and refreshes/fills the current inherited pane rectangle through [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md) slot `0x0069b3fc`.

First-draft C++ is now populated under the active combined-score/emitter gate: `RECONSTRUCTABLE:TRUE`, nonblank emitter route [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md), and `(88 + 92) / 2 = 90`, above the current 85/85 source-emission threshold. The remaining uncertainty is limited to exact original spellings for the shared EPF/render wrapper names, `SetGrafPortColorState`, and the callback-table member; the branch structure, resources, vtable-only liveness, destination offset, and owner/emitter are no longer blockers.

## Source-Quality Resolution

- Source-facing method: `ChattingModifyHeightPane::OnPaint()`, reached from the [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md) vtable slot at `0x00614e5c`.
- State field: `dword_67ADE0 + 0x118` is best documented as `g_pChattingHandlePane->m_secondaryChatEnabled`, not a temporary paint flag or line-count setting. The same handle state is consumed by neighboring chat back/handle paths.
- Destination field: the body computes `this + 0x44` into `edi` before the branch and passes that inherited pane rectangle to both `RenderTileFrame` and `dword_69B3FC`. The class-local `m_handleRect` at `this + 0x100` is not the direct paint target in this function.
- Render assets: `CHATSC.EPF` at string VA `0x00615300` is the height-handle art for this branch; `invenbut.pal` at string VA `0x00615318` is shared UI palette data and should not be treated as a chat-owned palette object.
- Shared helpers: `0x00457a60` initializes/constructs the 0x28-byte EPF tile context; `0x004d02f0` is the `g_pEPFLib` layout lookup that populates `tile + 0x10` / `tile.m_bounds`; `0x004b9980` is the shared `RenderTileFrame` wrapper; `0x005446b0` is `Pane::SetMode`; `0x004b9660` is `SetGrafPortColorState` / active draw-fill color setter; `0x0069b3fc` is the shared pane-region refresh/fill callback slot.
- Disabled branch interpretation: the best current source interpretation is an inactive/clear repaint path, not a separate hidden-state method. It sets pane mode `1`, sets active draw color `0`, and refreshes/fills the same inherited pane rectangle through the surface callback.
- Source route: keep owner/emitter [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md) under [UID:0000I5][Chatting](by-file/Chatting.md). The EPF, Surface, GrafPort, Pane, and ImageLib helpers are dependencies, not owners.

## Rejected Alternatives

- Moving the method to `ChattingBackPane` or a shared EPF asset helper is rejected. The only start xref is the `ChattingModifyHeightPane` vtable entry, and the surrounding class cluster owns the resize-handle behavior.
- Keeping raw labels such as `dword_67ADE0`, `sub_4B9980`, `sub_5446B0`, or `dword_69B3FC` in source is rejected because support docs now resolve source-facing roles for each dependency.
- Treating `g_pChattingHandlePane->m_secondaryChatEnabled` as the persisted visible-line-count config is rejected. The visible line count lives in the config field documented on the `SetLineCount` path, while this method reads the handle-pane secondary-chat state byte.
- Treating `m_handleRect` at `this+0x100` as the direct paint/refresh destination is rejected. The instruction at `0x00480bd0` computes `esi + 0x44`; no instruction in this body reads `esi + 0x100`.
- Treating the disabled branch as proof that the pane is destroyed or fully hidden is rejected. The body performs mode/color/region refresh work on the existing pane rectangle.

## B011 Local PE Evidence

- B011 2026-06-19 checked local PE bytes from `NexusTK.exe`: `.text` base `0x00401000`, function body `0x00480bb0-0x00480c45`, size `0x95`.
- The preceding bytes `0x00480ba3-0x00480bb0` are thirteen `0xcc` bytes, and the trailing bytes `0x00480c45-0x00480c50` are eleven `0xcc` bytes, so there is no hidden fallthrough, child split, or tail merge.
- No direct rel32 call/jump references target `0x00480bb0`; the one absolute pointer-table reference is `0x00614e5c`, the `ChattingModifyHeightPane` primary vtable paint slot. Vtable-only liveness is expected for this virtual paint method and is not suspicious.
- Absolute reference checks agree with the prose: `0x0067ade0` / `g_pChattingHandlePane` is read at `0x00480bc1`; `0x0067a744` / `g_pEPFLib` is read at `0x00480bdd`; `0x00615300` / `CHATSC.EPF` is used at this callsite; `0x00615318` / `invenbut.pal` is shared and includes this callsite; and `0x0069b3fc` has broad callback-table fanout including the indirect call at `0x00480c2f`.
- Direct target callees are `0x00457a60`, `0x004d02f0`, `0x004b9980`, `0x005446b0`, `0x004b9660`, indirect `0x0069b3fc`, and compiler stack-cookie check `0x005c772f`. The stack-cookie prologue and two stack-cookie exits are compiler instrumentation, not source behavior.

## B015 Raw PE And Vtable Recheck

B015 rechecked the same target bytes from `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` (file size `2679296`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`). The exact body is VA `0x00480bb0-0x00480c45`, PE raw `0x0007ffb0-0x00080045`, length `0x95`, target-byte SHA-256 `be3b5b7def9e1dbfe1d0e0ccc2039407ad00e694af5e1ecc5df9002c7af42a2f`. The prepad `0x00480ba3-0x00480bb0` is thirteen `0xcc` bytes after [UID:0002F8][0x00480ad0-0x00480ba3.ChattingModifyHeightPaneSetLineCount](by-memory/0x00480ad0-0x00480ba3.ChattingModifyHeightPaneSetLineCount.md), and the postpad `0x00480c45-0x00480c50` is eleven `0xcc` bytes before the [UID:0002FA][0x00480c50-0x00480c9f.ChattingVarietyPaneConstructor](by-memory/0x00480c50-0x00480c9f.ChattingVarietyPaneConstructor.md) cluster.

Range-wide raw reference checks found no external rel32 call, rel32 jump, or long conditional branch into `0x00480bb0-0x00480c45`. The only true absolute VA dword hit into the body is `.rdata` VA `0x00614e5c`, value `0x00480bb0`, the [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md) primary vtable paint slot. Naive range dword hits in instruction immediates and unrelated `.rdata` bytes are false positives, and the only true short branch in the range is the internal `0x00480bd3 -> 0x00480c1c` disabled branch.

The relevant vtable context is:

| Vtable entry | Target | Interpretation |
| --- | --- | --- |
| `0x00614e18` | `0x00483c00` | scalar deleting destructor |
| `0x00614e38` | `0x00544800` | inherited `Pane::InvalidateRect`-style slot |
| `0x00614e5c` | `0x00480bb0` | `ChattingModifyHeightPane::OnPaint` |
| `0x00614e68` | `0x00480890` | secondary-interface `OnMouseEvent` |
| `0x00614e74` | `0x00480a80` | secondary-interface key/cancel helper |
| `0x00614e94` | `0x00483a4e` | tertiary destructor adjustor thunk |

This reinforces the source route: this body is a source-authored virtual paint method on `ChattingModifyHeightPane`, not a `ChattingHandlePane` method, Surface/GrafPort helper, EPF helper, or compiler glue island.

The disassembly-level source shape is:

| Address range | Source meaning |
| --- | --- |
| `0x00480bb0-0x00480bd3` | standard prologue, security-cookie setup, `this` saved in `esi`, inherited pane bounds `this+0x44` saved in `edi`, and unconditional read of `g_pChattingHandlePane->m_secondaryChatEnabled` at `0x0067ade0 + 0x118`. |
| `0x00480bd5-0x00480c0b` | enabled branch: initialize stack `EPFTileContext` at `ebp-0x2c`, call `g_pEPFLib->LookupLayoutEntry(L"CHATSC.EPF", 0, &tile)`, then call shared `RenderTileFrame(&tile, &tile.m_bounds, &m_rect, 0, L"invenbut.pal", 0)`. |
| `0x00480c1c-0x00480c34` | disabled branch: `Pane::SetMode(1)`, `SetGrafPortColorState(0)`, then Surface callback-table slot `0x0069b3fc` on `this` and `this+0x44` to fill/refresh the inactive bounds. |
| `0x00480c0c-0x00480c1b` and `0x00480c35-0x00480c44` | duplicated compiler security-cookie epilogues through `0x005c772f`, one per branch. |

## IDA MCP Evidence

- Live IDA function lookup reports a modeled function at `0x00480bb0` with size `0x95`; `0x00480ba3` and `0x00480c45` are not function starts, and the next modeled function begins at `0x00480c50`.
- The only xref to the function start is vtable data entry `0x00614e5c`; there are no ordinary direct callers.
- Live callee analysis reports direct calls to helpers `0x00457a60`, `0x004d02f0`, `0x004b9980`, `0x005446b0`, `0x004b9660`, the dispatcher pointer at `0x0069b3fc`, and the security-cookie checker at `0x005c772f`.
- Byte reads confirm thirteen leading `0xcc` alignment bytes before `0x00480bb0` and eleven trailing `0xcc` bytes at `0x00480c45-0x00480c50`.
- Live disassembly/decompilation confirm the enabled-state read from global state pointer `0x0067ade0` offset `+0x118`, the `CHATSC.EPF` resource request, the `invenbut.pal` draw palette, the receiver rectangle at offset `+0x44`, and the disabled-state refresh path through helpers `0x005446b0`, `0x004b9660`, and `0x0069b3fc`.

## Open Questions

- Exact original names for the EPF context type, `RenderTileFrame` wrapper, and palette lookup helper remain integration-level spelling work for shared render headers.
- Exact public class-header spelling for the inherited receiver rectangle remains final-polish work; current draft C++ uses role name `m_rect` while preserving that the binary passes the pane-local rectangle at receiver offset `+0x44`, not the class-local `m_handleRect` at `+0x100`.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:00001W][ChattingModifyHeightPane](by-class/ChattingModifyHeightPane.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:0002F8][0x00480ad0-0x00480ba3.ChattingModifyHeightPaneSetLineCount](by-memory/0x00480ad0-0x00480ba3.ChattingModifyHeightPaneSetLineCount.md)
- [UID:0002FA][0x00480c50-0x00480c9f.ChattingVarietyPaneConstructor](by-memory/0x00480c50-0x00480c9f.ChattingVarietyPaneConstructor.md)

## Changes

- 2026-05-31: Created exact paint-method page and set completion/confidence to `76/84`. Evidence is IDA MCP function boundary, vtable xref, decompilation, render string evidence, and padding audit.
- 2026-06-05: Refreshed with live IDA evidence, raised to `84/90`, attached to `UID:00001W`, removed raw helper/global labels, documented enabled and disabled branches, and kept C++ blank below the 95/95 gate.
- 2026-06-21 B011 Rule 26 incorporation: raised `84/90` to `86/91` and populated first-draft `ChattingModifyHeightPane::OnPaint()` C++. Incorporated the B011 chat paint/color/message cluster report's valid paint-path facts: `g_pChattingHandlePane->m_secondaryChatEnabled`, `CHATSC.EPF`, `invenbut.pal`, `g_pEPFLib` lookup, shared `RenderTileFrame`, `Pane::SetMode(1)`, `SetDrawColor(0)`, surface slot `0x0069b3fc` refresh, vtable-only reachability, and rejected BackPane/shared-helper ownership. The old 95/95 blank-C++ blocker is now stale; remaining uncertainty is helper/member spelling only.
- 2026-06-22 B011 exact [UID:0002F9] Rule 26 incorporation: raised `86/91` to `87/92`, corrected the draft and prose to use inherited pane bounds `this+0x44` / `m_rect` as the direct render/refresh destination, preserved `m_handleRect` at `this+0x100` as the class-local resize geometry field used by neighboring constructor/set-line-count/cancel paths, and recorded local PE evidence for the exact `0x95` body, `0xcc` padding, vtable-only slot `0x00614e5c`, no direct callers, branch-specific helper sequences, `FrameDrawRecord` scratch role, inactive/clear refresh semantics, and descriptive shared helper-name caveats.
