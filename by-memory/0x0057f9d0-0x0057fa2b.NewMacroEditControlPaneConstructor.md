*** UID:0004A8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewMacroEditControlPane::NewMacroEditControlPane(
    wchar_t key,
    int shortcutIndex,
    unsigned char spellMode,
    const Rect &bounds)
    : ControlPane(8, bounds),
      m_shortcutIndex(shortcutIndex),
      m_key(key),
      m_spellMode(spellMode)
{
    SetSelectable(true);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: NewMacroEditControlPane constructor building a selectable ControlPane row from a CompactShortcutRecord wchar_t code, initial signed shortcut ordinal 0..9, and raw unsigned-char spell-mode byte while installing the three class vtable views and exact +0x108/+0x10c/+0x10e state. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057f9d0-0x0057fa2b NewMacroEditControlPane Constructor

## Status

- Address range: `0x0057f9d0-0x0057fa2b` half-open.
- Entity kind: `NewMacroEditControlPane` constructor.
- Direct owner/emitter: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) `NewMacroEditControlPane`.
- Source-family file: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) `MacroDialogs`.
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Rebuild handling: source-authored constructor; emit the formal C++ above.

## Exact Construction Evidence

- Current IDA MCP reports exact function size `0x5b` at `[0x0057f9d0,0x0057fa2b)`.
- The exact 91-byte body SHA256 is
  `8CEA15F59FD3F3DB9AC299D2884411491AF967ABB156DA6292680AD43B42F6F9`.
- The direct construction callsite is `0x0053ef2c` inside [UID:00049J][0x0053ed90-0x0053f07f.NewMacroDialogConstructor](by-memory/0x0053ed90-0x0053f07f.NewMacroDialogConstructor.md).
- The caller obtains an exact [UID:0001VR][RegistryConfigUserProfileBlock](by-type/by-struct/RegistryConfigUserProfileBlock.md) `CompactShortcutRecord` row, zero-extends the word at row `+0` / `record.code`, supplies the row ordinal `0..9` as the signed shortcut index, zero-extends byte `+2` / `record.isSpellMode`, and supplies the row bounds. The selected callsite sequence at `0x0053ef20-0x0053ef2c` transports both record fields without a compare, mask, or Boolean normalization.
- The constructor calls the `ControlPane(8, bounds)` source base, stores:
  - `int m_shortcutIndex` at complete-object `+0x108`;
  - two-byte `wchar_t m_key` at `+0x10c`;
  - raw `unsigned char m_spellMode` at `+0x10e`.
- It installs NewMacroEditControlPane vtable heads `0x0062d288`, `0x0062d2f0`, and `0x0062d320`, then sets inherited selectable/control state at `+0xff`.
- The exact stack ABI remains four source arguments and `retn 0x10`; replacing an ABI-compatible two-byte integer spelling with `wchar_t` does not change the call frame.
- Only the low byte of the third argument home is loaded and written. A noncanonical nonzero `record.isSpellMode` value therefore survives constructor transport unchanged.

## Type, Layout, And Lifecycle

- Current [UID:000031][Config](by-class/Config.md) declares `CompactShortcutRecord::code` as `wchar_t` and `isSpellMode` as `unsigned char`. Each record is exactly four bytes with natural tail alignment.
- The second constructor argument is not a compact-record identity. UID00049J's ten-row first-page loop supplies the signed ordinal `0..9`, UID00049O later rewrites the same field to `page * 10 + row`, and UID0004AD renders the resulting `0..19` domain as number/Ctrl-number shortcuts. `int shortcutIndex` therefore preserves both the exact dword ABI and the field's source role.
- The strongest constructor parameter spelling is consequently
  `unsigned char spellMode`: it matches the record member, the exact byte
  load/store, the paired getter/setter contract, and the late-1990s/2000s
  project source style without introducing a library typedef.
- [UID:00049O][0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage](by-memory/0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage.md) later copies the same `record.code` through [UID:0004AA][0x0057fa40-0x0057fa52.NewMacroEditControlPaneSetKey](by-memory/0x0057fa40-0x0057fa52.NewMacroEditControlPaneSetKey.md).
- [UID:0004A6][0x0057f9b0-0x0057f9b8.NewMacroEditControlPaneGetKey](by-memory/0x0057f9b0-0x0057f9b8.NewMacroEditControlPaneGetKey.md) returns the same field through AX, and [UID:00049N][0x0053f180-0x0053f203.NewMacroDialogSaveMacroEntries](by-memory/0x0053f180-0x0053f203.NewMacroDialogSaveMacroEntries.md) stores it unchanged back to `record.code`.
- Input and paint consumers preserve the code-unit role: the key/text handler widens accepted ASCII into `m_key`, and OnPaint zero-extends the word before normalization.
- The paired input writer normally produces zero or one, but the constructor,
  getter, setter, save, and load paths preserve arbitrary nonzero byte values.
  Zero means item/inventory mode and any nonzero value means spell mode; this
  value domain rejects a source `bool` boundary.
- `wchar_t` is two bytes with two-byte alignment on this x86/MSVC target. The complete class remains exact size `0x110`: `ControlPane` through `+0x108`, shortcut index through `+0x10c`, key through `+0x10e`, spell-mode byte through `+0x10f`, and one byte of natural tail alignment.

## Source Shape And Historical Correction

- The source body retains ordinary initializer-list construction, no raw offsets, no vptr stores, no exception scaffolding, no explicit tail member, and no decompiler temporaries.
- Historical `uint16_t` and possible `unsigned short` key spellings were ABI-compatible provisional descriptions. They are superseded by the current complete `CompactShortcutRecord::code` lifecycle.
- Historical `uint8_t mode` / `m_mode` wording captured the storage width but
  obscured the field's spell-versus-item meaning and implied a modern typedef.
  It is superseded by the evidence-backed `unsigned char spellMode` /
  `m_spellMode` source contract. `bool` is rejected because the caller and
  constructor preserve noncanonical nonzero bytes rather than normalize them.
- `GetKey`/`SetKey`/`m_key` remain the row-control vocabulary; `code` remains the storage-record vocabulary. The compact table remains distinct from the integrated `MacroHotkeyRecord` array.
- The constructor follows `GetSpellMode` in source order and precedes
  `SetShortcutIndex`, `SetKey`, and `SetSpellMode`. Historical
  `recordIndex`/`m_recordIndex`/`SetRecordIndex` wording is superseded because
  the field selects the configured number/Ctrl-number shortcut ordinal rather
  than identifying the caller's compact storage record.

## Negative Evidence

- No constructor instruction compares, masks, clamps, or canonicalizes the
  spell-mode argument.
- No recovered use gives the byte signed arithmetic, ordering, or sentinel
  semantics; plain `char` is weaker than `unsigned char`.
- No one-byte enum declaration or enum-valued switch was recovered.
- No evidence makes the field an inherited Pane mode; that separate concept is
  already represented elsewhere in the hierarchy.
- The vptr stores, selectable-state write, and compiler exception scaffolding
  are lowering evidence and do not belong in the human-authored source body.

## Score Rationale

- `91` completion reflects a complete source-authored constructor body,
  argument order, base construction, field order, layout, selectable state,
  caller provenance, and byte-value lifecycle.
- `94` confidence reflects exact body bytes/hash, one direct caller, direct
  field stores, current record and class declarations, paired accessors, and
  the generated/source-order union. The remaining uncertainty is limited to
  unavailable original identifier spelling rather than behavior or ABI.

## Changes

- 2026-07-16 B003 UID0004A9 implementation callback:
  - Preserved `91/94`, UID000095 owner/emitter, reconstructable true, blank position, `Nested:0`, exact range/hash/caller, `wchar_t` key transport, unsigned-char spell-mode transport, `ControlPane(8)` base construction, three vtable stores as compiler evidence, selectable-state write, exact `0x110` layout, and all prior history.
  - Rebased the complete formal constructor from historical `recordIndex` / `m_recordIndex` to `shortcutIndex` / `m_shortcutIndex` without changing ABI, field order, or behavior.
  - Added the exact first-page `0..9` caller provenance and the page-load/paint evidence proving the member is a signed configured-shortcut ordinal rather than a compact-record identity.

- 2026-07-16 B001 UID0004A7 implementation callback:
  - Raised the constructor from `87/90` to `91/94` after closing the exact
    body hash, caller transport, byte-value domain, layout, and source-shape
    blockers.
  - Replaced historical `uint8_t mode` / `m_mode` with the accepted
    `unsigned char spellMode` / `m_spellMode` contract while preserving the
    executed B005 `wchar_t` key union and current `SetRecordIndex` /
    `m_recordIndex` vocabulary.
  - Added noncanonical-byte preservation, no-Boolean/no-enum proof, source
    order, negative evidence, and score rationale.
- 2026-07-16 B005 UID0004A6 implementation callback:
  - Applied the accepted `wchar_t` first parameter while preserving `87/90`, UID000095 owner/emitter, true, blank optional position, `Nested:0`, range, constructor order, base call, field initialization, selectable state, and all unrelated metadata.
  - Added exact `record.code` argument provenance, zero-extended caller load, two-byte `+0x10c` store, three-vtable identity, stack ABI, complete compact-record lifecycle, and layout-neutral type rationale.
