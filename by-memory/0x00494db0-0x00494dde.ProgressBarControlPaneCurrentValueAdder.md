*** UID:00011A | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ProgressBarControlPane::AddCurrentValue(short delta)
{
    short value = static_cast<short>(m_rangeCurrent + delta);
    if (m_rangeCurrent == value)
        return;

    m_rangeCurrent = value;
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact unique 46-byte retained source-authored `ProgressBarControlPane::AddCurrentValue(short)` method: adds the signed delta to inherited `m_rangeCurrent +0xfa`, truncates modulo 16 bits, returns without side effects when the stored word would not change, and otherwise writes current before tail-dispatching one `Pane::InvalidateRect(&m_visibleBounds)` through primary slot `+0x20`. Exact SHA-256, six-byte prepad, two-byte postpad, no-function/no-external-entry/no-VA-RVA-raw-pointer-route evidence, class/file ownership, and populated behavior-exact C++ are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00494db0-0x00494dde - ProgressBarControlPane Current-Value Adder

## Status

- Confidence: very strong for ownership, exact bytes/hash/boundary, unique-body classification, current-field update, modulo-16 behavior, invalidation dispatch, exhaustive no-entry status, and behavior-exact C++; strong for inferred original method/parameter/access spellings.
- Related class: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- Related file: [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- IDA function status: code-shaped bytes, not a function object.
- Source shape: `ProgressBarControlPane::AddCurrentValue(short delta)`.
- C++ status: first-draft source-authored mutator populated; raw/no-caller caveat retained.

## Covered Range

| Range | Owner | Role |
| --- | --- | --- |
| `0x00494db0-0x00494dde` | `ProgressBarControlPane` | Source-authored `AddCurrentValue(short delta)` helper that adds the incoming short delta into inherited `ControlPane::m_rangeCurrent` at object offset `+0xfa`, truncates to 16 bits, and invalidates `GrafPort::m_visibleBounds` when the low-word value changes. |

## Evidence Notes

- IDA MCP `lookup_funcs 0x00494db0` reports `Not a function`.
- Raw IDA disassembly shows the receiver register reading the current-value field at object offset `0xfa`, adding the stack short argument, storing the 16-bit result back to the same field when changed, and tail-dispatching vtable slot `+0x20` with the pane rectangle at object offset `0x44`.
- The body ends at `retn 4` before alignment at `0x00494dde`; the confirmed draw virtual starts at `0x00494de0`.
- The helper touches the same current-value field and invalidation pattern documented by the constructor and range setter pages.
- 2026-06-05 live IDA reconfirms `0x00494db0` and `0x00494dde` are not function objects, `0x00494de0` is the modeled `0xd0`-byte draw virtual, and `0x00494eb0` is the next modeled function after draw.
- 2026-06-05 `xrefs_to` reports no references to `0x00494db0` or `0x00494dde`; `0x00494de0` has the expected vtable data reference at `0x00617b78`. `callers` reports no ordinary direct callers for either the raw helper start or draw start.
- 2026-06-05 byte reads confirm the exact helper bytes from `0x00494db0-0x00494dde`, the `retn 4` exit, the tail jump through vtable slot `+0x20`, and two alignment bytes at `0x00494dde-0x00494de0`.
- 2026-06-10 B001-018 IDA MCP audit reconfirms `lookup_funcs 0x00494db0` returns `Not a function`, `xrefs_to` and `callers` for `0x00494db0` are empty, `0x00494db0-0x00494dde` is a 46-byte code-shaped body, and `0x00494dde-0x00494de0` is two `0xcc` bytes before the modeled draw virtual. Disassembly shows a current-field update helper: read `this+0xfa`, add the stack short argument, compare the 16-bit result, store `this+0xfa` only when changed, then tail-dispatch vtable `+0x20` with the pane rectangle at `this+0x44`.
- 2026-07-23 B003 live MCP session `9b0396a3` and independent PE readback establish exact target identity: VA `0x00494db0`, RVA `0x00094db0`, raw file offset `0x000941b0`, length `0x2e`, and SHA-256 `D13A5B1018EA66A5752E53501A6F8E1EDFE0A50C53BA804A54BFA89AF328AA7C`.
- Exact bytes are `55 8b ec 0f b7 91 fa 00 00 00 8b 45 08 03 c2 0f b7 c0 66 3b d0 74 13 8b 11 66 89 81 fa 00 00 00 8d 41 44 89 45 08 5d ff 62 20 5d c2 04 00`.
- Live MCP `find_bytes` and an independent whole-file scan each find that complete 46-byte body exactly once at `0x00494db0` / raw offset `0x000941b0`; the method is not an inline duplicate.
- Every decoded target instruction head was checked. The start has no incoming xref, non-start heads have only internal fall-through, and `0x00494dda` additionally has the internal equal-result branch from `0x00494dc5`; no external alternate entry exists.
- Whole-file searches found zero little-endian start-pointer matches for VA `B0 4D 49 00`, RVA `B0 4D 09 00`, or raw offset `B0 41 09 00`. A decoded `.text` branch/call-target scan found zero external targets inside the body.
- A raw every-byte scan's sole apparent branch into `0x00494dd3` came from byte `0x7e` at `0x00494e11`; bounded IDA listing proves that byte is an operand inside the real `movd eax,xmm0` instruction at `0x00494e0f`, not an instruction or entry route.

## Cross-Document Evidence

| Source | Confirmation |
| --- | --- |
| [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) | Lists this exact helper as `AddCurrentValue(short delta)` and now emits the class declaration with child insertion; earlier provisional/no-C++ wording is superseded by the B005 source-quality closure. |
| [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) | Places this helper in `NexusTK/ui/controls/ProgressBarControlPane.cpp` with the constructor, draw virtual, vtables, and sibling projected helpers. |
| [UID:00011B][0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw](by-memory/0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw.md) | Nests this projected updater before the confirmed draw virtual and records the exact update/alignment/draw map. |
| [UID:00011C][0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar](by-memory/0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar.md) | Confirms the successor draw virtual uses the same `0xf8`/`0xfa`/`0xfc` field triplet and proves the modeled draw boundary at `0x00494de0`. |
| [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md) | Confirms the constructor initializes the current-value field at object offset `0xfa` and installs the progress-bar vtables. |
| [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md) | Confirms the range setters use the same current field and invalidation path, tying this adder to the same state model. |
| [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md) | Confirms the class vtable-data ownership that ties the helper's field family to `ProgressBarControlPane`. |
| [UID:0001VH][PaneLayout](by-type/by-struct/PaneLayout.md) | Resolves complete-object `+0x44` as inherited `GrafPort::m_visibleBounds`, superseding `m_bounds`/`m_localBounds`. |
| Current generated output | The populated child belongs in `ProgressBarControlPane.cpp`; the historical below-95/no-code wording is obsolete. |

## Touched State

| Offset / call | Evidence-backed role | Remaining uncertainty |
| --- | --- | --- |
| signed `short delta` | Delta added to the current progress value, with source-level 16-bit truncation. | Original spelling is inferred; width/role are exact. |
| object offset `0xfa` | Inherited signed `short ControlPane::m_rangeCurrent` read and conditionally updated. | Physical member identity/type/offset are exact; no derived shadow member exists. |
| object offset `0x44` | Inherited `GrafPort::m_visibleBounds` passed after mutation. | Resolved by current PaneLayout/GrafPort authority. |
| vtable slot `+0x20` | `Pane::InvalidateRect(const RectBounds *)` at `0x00544800`. | Current source-facing name resolved. |
| `0x00494dde-0x00494de0` | Alignment before confirmed draw virtual. | Boundary is documented by the aggregate update/draw page. |

## B003 Exact Binary, ABI, And Source-Quality Closure

### Exact Range And Instruction Semantics

The target is one complete 32-bit MSVC thiscall member body. It is preceded by six `0xcc` bytes at `0x00494daa-0x00494db0`, ends exclusively at `0x00494dde`, is followed by two `0xcc` bytes at `0x00494dde-0x00494de0`, and does not overlap the modeled `DrawProgressBar` body beginning at `0x00494de0`.

| Address | Instruction | Source meaning |
| --- | --- | --- |
| `0x00494db0` | `push ebp` | Begin normal stack frame. |
| `0x00494db1` | `mov ebp, esp` | Establish frame. |
| `0x00494db3` | `movzx edx, word ptr [ecx+0FAh]` | Read old inherited `m_rangeCurrent`. |
| `0x00494dba` | `mov eax, [ebp+8]` | Load the argument slot containing `short delta`. |
| `0x00494dbd` | `add eax, edx` | Add old low word and delta. |
| `0x00494dbf` | `movzx eax, ax` | Normalize/truncate the result to the low 16 bits. |
| `0x00494dc2` | `cmp dx, ax` | Compare old and new stored words. |
| `0x00494dc5` | `jz 0x00494dda` | Skip every side effect when the low word is unchanged. |
| `0x00494dc7` | `mov edx, [ecx]` | Load the primary vptr for changed-state dispatch. |
| `0x00494dc9` | `mov [ecx+0FAh], ax` | Store the new inherited `m_rangeCurrent`. |
| `0x00494dd0` | `lea eax, [ecx+44h]` | Form `&m_visibleBounds`. |
| `0x00494dd3` | `mov [ebp+8], eax` | Reuse the stack argument slot for the bounds pointer. |
| `0x00494dd6` | `pop ebp` | Tear down the frame before tail dispatch. |
| `0x00494dd7` | `jmp dword ptr [edx+20h]` | Tail-call inherited virtual `Pane::InvalidateRect`. |
| `0x00494dda` | `pop ebp` | Unchanged-path frame teardown. |
| `0x00494ddb` | `retn 4` | Void thiscall return with one argument slot. |

The machine calculates the new value modulo `2^16`; it does not clamp or saturate. The source expression `short value = static_cast<short>(m_rangeCurrent + delta);` is behavior-exact for the target compiler: both signed shorts promote to 32-bit `int`, their mathematical sum cannot overflow `int`, and target MSVC's conversion back to `short` produces the observed low word. Negative deltas and low-word wraparound are intentional. Equality is tested after truncation. The unchanged path performs no write and no invalidation; the changed path writes first and then invalidates exactly once.

### Retained-Source Classification And Reachability

The complete body is retained source-authored `ProgressBarControlPane` code despite having no surviving entry route. A normal thiscall ABI, receiver-field arithmetic, a conditional branch, a state write, and inherited virtual invalidation reject padding, table data, runtime glue, and compiler-thunk classifications. Exact-body uniqueness rejects an inline duplicate. The modeled `SetCurrentValue`, constructor, draw virtual, accepted `ControlPane` layout, and three ProgressBar vtable views independently anchor the same class family.

No function object, external instruction xref, ordinary caller, decoded external branch/call target, alternate internal-head entry, or VA/RVA/raw pointer route survives. This is exact negative liveness evidence and a confidence cap, not no-code proof. Reconstructing the retained body is required to preserve the original executable's code and behavior even when the original link no longer exposes a caller.

### Human Source Names, Types, And Rejected Alternatives

- `AddCurrentValue` is the strongest method name because it pairs with `SetCurrentValue` and adds a caller-supplied relative value.
- `short delta` is the strongest parameter because width and signed arithmetic are exact, and a negative delta is valid.
- `IncrementCurrentValue` is rejected because the argument is arbitrary and may be negative.
- `AddValue` is rejected as less specific than the established Value-based ProgressBar API.
- `SetCurrentValue` is rejected because this body adds instead of replacing.
- `Step`, `AdvanceProgress`, and `AddProgress` are rejected because they imply positive-only or higher-level behavior not present in the machine code.
- `m_rangeCurrent` is inherited protected `ControlPane` storage at `+0xfa`; exact `ControlPane` size `0x108` leaves no separate derived range field.
- `m_visibleBounds` is inherited `GrafPort` state at complete-object `+0x44`.
- Primary vtable cell `0x00617b54 -> 0x00544800` resolves the virtual as `Pane::InvalidateRect(const RectBounds *)`.
- Public access is the strongest source declaration because the method is grouped with the public range/current API; exact access and lexical spellings remain inferred because no PDB/source symbol survives.

Raw `sub_494DB0`, `_WORD`, offset-only field syntax, direct vtable calls, derived `m_currentValue`, `m_bounds`, and generic `Invalidate` are historical reverse-engineering labels only. They must not appear in finalized source.

### Ownership, Source Placement, And Range Disposition

Keep class owner and emitter [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md), with source root [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md). `ControlPane`, `Pane`, and `GrafPort` own inherited storage/support behavior but not this ProgressBar-specific API. `PatchPane` is a constructor/current-setter consumer only. [UID:00011B][0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw](by-memory/0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw.md) is duplicate inventory over exact source-bearing children and must remain ownerless/non-emitting.

No split, merge, new child, new source file, owner change, emitter change, or IDA database mutation is needed. The existing formal is normal human-written C++03-style source and must remain in the managed C++ header.

### Score Rationale

Completion `93` reflects exact bytes/hash/range/padding, full instruction and ABI semantics, modulo-16 behavior, unique-body proof, exhaustive entry/pointer negatives, owner/emitter/source placement, realistic source names, behavior-exact C++, and implementation-ready support/manual handoffs. Confidence `94` reflects mutually corroborating binary, layout, vtable, sibling, constructor, draw, and generated-source evidence.

The score remains below final/PDB certainty because no original symbol proves the exact method/parameter/access spellings, no surviving caller proves historical runtime reachability, IDA has no function object at the raw start, and the historical header/source filenames are inferred. These limits do not justify a lower score: the symmetric unmodeled range setters carry the same liveness limitation at `93/94`, while UID00011A now has equally exhaustive target-specific closure.

## Historical B005 Source-Quality Closure

The best source-facing name is `ProgressBarControlPane::AddCurrentValue(short delta)`. The helper is source-authored current-value update logic, not generated binary glue: it reads inherited `m_rangeCurrent`, adds the stack argument, truncates the result to 16 bits, writes that inherited member only when the low 16-bit result differs, then invalidates through inherited vtable slot `+0x20`.

The current executable still has no direct branch/call refs, no long conditional branch refs, and no VA/RVA pointer/table refs to `0x00494db0`; that remains a liveness caveat and confidence cap, not a no-code proof. The exact body is bounded by six `0xcc` bytes at `0x00494daa-0x00494db0` before it and two `0xcc` bytes at `0x00494dde-0x00494de0` before the modeled draw virtual. B005 accepted `m_currentValue` for `+0xfa`, but its inherited `m_bounds` spelling is now historical: current PaneLayout/GrafPort authority uses inherited `m_visibleBounds`, and current Pane/vtable authority resolves slot `0x00617b54 -> 0x00544800` as `Pane::InvalidateRect`.

## B002 UID000115 Family Support Sync

- The existing exact 46-byte raw-body, no-function/no-xref/no-pointer-route, six-byte leading padding, and two-byte trailing padding evidence remains authoritative for this bounded support page.
- UID000115/UID000117 reanalysis confirms the same inherited signed `m_rangeCurrent +0xfa` family and primary slot `+0x20`; UID000119 confirms constructor ownership and UID00011C consumes the same current field.
- The source method performs 16-bit addition/truncation before comparing the low-word result. It does not clamp against min/max, and it invalidates only when the stored low word changes.
- Current inherited source names are `Pane::InvalidateRect(&m_visibleBounds)`. Raw `sub_494DB0`, `_WORD`, vtable syntax, `Invalidate`, `m_bounds`, and below-95/blank-C++ wording remain historical only.
- Exact `ControlPane` complete size `0x108` and PatchPane's exact `0x108` ProgressBar allocation prove `+0xfa` is inherited protected `m_rangeCurrent`; the earlier derived `m_currentValue` alias is historical and cannot represent additional physical storage.

## Reconstruction Notes

- Reconstructable: true as a private current-value update helper.
- Parent: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md).
- C++: first-draft `ProgressBarControlPane::AddCurrentValue(short delta)` is now populated. The source names are inferred/descriptive rather than PDB-proven, but the implementation stays within the exact helper range and expresses normal mid-2000s C++ class logic.
- Completion is `93` because exact bytes/hash/range/padding, instruction and ABI behavior, unique-body classification, exhaustive entry/pointer-route negatives, parent/file placement, aggregate disposition, inherited fields/helper, source names, and behavior-exact C++ are now documented.
- Confidence is `94` because live IDA, original PE bytes, independent full-body and pointer scans, modeled siblings, constructor/draw/vtable anchors, accepted layouts, and current generated source agree. It remains below final/PDB confidence because the raw start is unmodeled, no caller survives, and original lexical/access spellings are inferred.

## Cross-References

- [UID:00011B][0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw](by-memory/0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw.md)
- [UID:00011C][0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar](by-memory/0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar.md)
- [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)

## Changes

- Before: completion/confidence were `50/65` and reconstruction metadata was blank.
- Changed to: completion/confidence are `64/76`, and the helper is marked reconstructable and attached to the `ProgressBarControlPane` class.
- Summary/evidence: existing raw IDA disassembly notes identify the exact helper body, current-field update, invalidation dispatch, and boundary before `DrawProgressBar`; confidence remains capped because IDA still does not model the start as a function and the final source-level name is provisional.
- 2026-06-02:
  - Before: page had raw behavior notes but no touched-state table or reconstruction rationale.
  - After: scored as `70/82` with current-value state, invalidation call, and boundary relationship mapped.
  - Summary/evidence: recorded raw IDA disassembly notes plus constructor, range-helper, and draw-page field evidence support the stronger score; at that time no C++ emitted because final helper/field names were not source-quality. This historical state is superseded by the 2026-06-19 implementation.
- 2026-06-03 cross-documentation consistency pass:
  - Before: the page was assigned and behaviorally documented, but the score did not record the parent/file threshold check, aggregate nesting, draw-boundary relationship, range-helper state model, vtable-data support, or autogen no-code state.
  - After: raised `70/82` to `76/84` and added a cross-document evidence table; C++ remained blank at that time, superseded by the 2026-06-19 implementation.
  - Summary/evidence: existing written IDA evidence proves the raw body and boundary before draw; the class/file/aggregate/constructor/draw/range/vtable pages confirm ownership and field use.
- 2026-06-05 live IDA refresh:
  - Changed from `76/84` to `82/88`.
  - Removed stale receiver-offset wording and the old unavailable-IDA caveat.
  - Evidence: live `lookup_funcs`, `xrefs_to`, `callers`, `get_bytes`, and disassembly confirm the raw helper remains unmodeled and unreferenced, updates the current-value field only on change, tail-dispatches the invalidation virtual with the pane rectangle, ends at `0x00494dde`, and is followed by two alignment bytes before the modeled draw virtual.
- 2026-06-10 B001-018 duplicate-aggregate audit:
  - Changed from `82/88` to `85/88`.
  - Evidence: live IDA MCP reconfirmed the exact 46-byte helper body, empty xrefs/callers, two-byte alignment gap, tail dispatch through vtable `+0x20`, and constructor/draw/vtable ownership anchors. The parent class [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) and file [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) already clear `86/85`, so this exact child remains the source-bearing ProgressBarControlPane current-value update helper while aggregate [UID:00011B][0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw](by-memory/0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw.md) becomes non-emitting.
- 2026-06-19 B005 accepted source-quality implementation:
  - Changed from `85/88` to `87/89`.
  - Added first-draft `ProgressBarControlPane::AddCurrentValue(short delta)` C++ and replaced the stale no-code/field-name blocker with accepted source-facing names, 16-bit add/truncate semantics, exact padding, inherited invalidation slot, and raw no-branch/call/VA/RVA-ref caveat.
- 2026-07-23 B002 UID000115 support callback:
  - Scores and owner/emitter metadata remain `87/89`, UID0000AW, reconstructable true, and UID0000AW.
  - Replaced the stale blank-C++ Item Summary and `Invalidate(&m_bounds)` formal with current source-ready truth and `InvalidateRect(&m_visibleBounds)`.
  - Preserved exact raw-body/padding/no-route evidence and integrated current PaneLayout, family naming, and no-clamp/add-truncate behavior without overstating reachability.
- 2026-07-23 B003 inherited-storage correction:
  - Preserved `87/89`, the exact 16-bit add/truncate/compare behavior, raw no-route/padding evidence, owner/emitter route, and source method name.
  - Replaced derived `m_currentValue` with inherited `ControlPane::m_rangeCurrent`, added exact no-shadow-storage layout proof, and retained the superseded alias as historical evidence.
- 2026-07-23 B003 UID00011A source-quality implementation:
  - Changed `87/89 -> 93/94`; owner/emitter/reconstructable metadata and the exact formal body remain unchanged.
  - Added exact SHA-256/PE mapping/bytes, six-byte prepad and two-byte postpad, full-body uniqueness, every-instruction-entry audit, VA/RVA/raw pointer negatives, decoded branch/call negatives, and rejection of the operand-byte false positive.
  - Added complete thiscall ABI, signed-short promotion, modulo-16 truncation, unchanged/changed path order, inherited field/helper, source naming, rejected alternatives, retained-source classification, owner/source placement, and score rationale.
