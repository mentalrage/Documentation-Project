*** UID:0002GB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingColorListPane::DrawItem(
    int row,
    const ChattingColorSwatchItem *item,
    const RectBounds *drawBounds)
{
    RectBounds bounds;

    if (item->colorValue == 0) {
        InitRectBounds(
            &bounds,
            drawBounds->left + 2,
            drawBounds->top + 2,
            drawBounds->right - 2,
            drawBounds->bottom - 2);
        SetMode(1);
        SetDrawColor(136);
        g_pfnFillRect(this, &bounds);
    }

    if (IsItemSelected(row)) {
        InitRectBounds(
            &bounds,
            drawBounds->left,
            drawBounds->top,
            drawBounds->right,
            drawBounds->bottom);
        SetMode(1);
        SetDrawColor(128);
        g_pfnFillRect(this, &bounds);

        InitRectBounds(
            &bounds,
            drawBounds->left + 2,
            drawBounds->top + 2,
            drawBounds->right - 2,
            drawBounds->bottom - 2);
        SetMode(1);
        SetDrawColor(143);
        g_pfnFillRect(this, &bounds);
    }

    InitRectBounds(
        &bounds,
        drawBounds->left + 3,
        drawBounds->top + 3,
        drawBounds->right - 3,
        drawBounds->bottom - 3);
    SetMode(1);
    SetDrawColor(item->colorValue);
    g_pfnFillRect(this, &bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current canonical read-only evidence resolves this source-authored virtual as void ChattingColorListPane::DrawItem(int row, const ChattingColorSwatchItem *item, const RectBounds *drawBounds) over exact range [0x004832c0,0x00483414), 340 bytes, function-byte SHA256 C9AFE1EFFF38BF2C570E789781598F8EEA90A9CDF51AAF374B97FDC99822CDEB. Vtable cell 0x00615228 is the sole inbound xref; empty ordinary callers therefore prove virtual-only dispatch rather than dead code. ChattingColorSwatchItem is an eight-byte {row,colorValue} record and this body reads colorValue at +0x04. The independent zero-color branch draws inset-2 palette 136; the selected-row branch draws full palette 128 then inset-2 palette 143; every row finishes with inset-3 item->colorValue. Each of the four fills explicitly performs InitRectBounds, SetMode(1), SetDrawColor(color), and typed callback g_pfnFillRect(this,&bounds), preserving binary call order without invented FillPaletteRect/InsetRectBounds abstractions or added guards. Twelve 0xCC bytes [0x00483414,0x00483420) align the next OnItemSelected method. Owner/emitter remains ChattingColorListPane UID00001S through Chatting UID0000I5 and social/Chatting.cpp; no split is justified. Formal target CPP is populated with the exact explicit body, coordinated with the complete item/class declaration in the owner CPP channel; target and class H channels intentionally remain blank with range/source-placement proof. Current score is 91/94. Historical blank-C++, raw sub_/dword_ labels, compressed helper draft, and unresolved-name assumptions are retained below only as superseded history. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004832c0-0x00483414 - ChattingColorListPane DrawItem

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored.
- Entity kind: class method / virtual draw item.
- Owner: [UID:00001S][ChattingColorListPane](by-class/ChattingColorListPane.md).
- Parent aggregate: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Exact range: `[0x004832c0,0x00483414)`, 340 bytes (`0x154`), current function-byte SHA256 `C9AFE1EFFF38BF2C570E789781598F8EEA90A9CDF51AAF374B97FDC99822CDEB`.
- Confidence: strong for range, ABI, vtable ownership, item layout, branch/call ordering, source-facing helper/global roles, source placement, and emitted body.
- Autogen handling: attached to [UID:00001S][ChattingColorListPane](by-class/ChattingColorListPane.md). This child owns the out-of-line formal CPP body; the owner page owns the complete `ChattingColorSwatchItem` and `ChattingColorListPane` declaration in its formal CPP channel. Both target and owner H channels stay blank.
- Routing remains `ChattingColorListPane` -> [UID:0000I5][Chatting](by-file/Chatting.md) -> `NexusTK/social/Chatting.cpp`; no one-method or `ChatColorDialog.cpp` split is applied.

## ABI And Source-Facing Types

- Source-facing signature: `void __thiscall ChattingColorListPane::DrawItem(int row, const ChattingColorSwatchItem *item, const RectBounds *drawBounds)`; machine return is `retn 0x0c` for the three explicit four-byte arguments.
- `row` is passed unchanged to inherited `ListPane::IsItemSelected(row)` and does not index the item pointer itself.
- `item` is a pointer to an eight-byte `ChattingColorSwatchItem`: `int row` at `+0x00`, `int colorValue` at `+0x04`. Constructor, find-index, getter, draw, and selection-writeback evidence all agree on this layout.
- `drawBounds` points to a 16-byte `RectBounds` whose `left/top/right/bottom` fields are four-byte integers at offsets `0/4/8/12`.
- The local `bounds` is a 16-byte `RectBounds`. Stack-cookie operations are compiler lowering and are not emitted as source.

## Behavior

This virtual method renders one chat-color swatch row through four explicit draw-state/callback groups. The branches are independent, not an `else if`, and the final swatch fill always runs.

1. If `item->colorValue == 0`, initialize `(left+2, top+2, right-2, bottom-2)`, call inherited `SetMode(1)`, set draw color `136`, and call `g_pfnFillRect(this, &bounds)`.
2. If `IsItemSelected(row)` is true, initialize the full input rectangle, set mode `1`, set color `128`, and fill it.
3. Still in the selected branch, initialize inset-2 bounds, set mode `1`, set color `143`, and fill it.
4. Unconditionally initialize inset-3 bounds, set mode `1`, set `item->colorValue`, and fill it.

The body intentionally repeats the short `InitRectBounds -> SetMode -> SetDrawColor -> g_pfnFillRect` sequence. That call shape is directly observed, looks like ordinary mid-2000s UI source, and avoids inventing a `FillPaletteRect` or target-local `InsetRectBounds` abstraction that the binary never calls. It also preserves the absence of null, range, clipping, callback, or color guards.

B014 2026-06-19 [UID:0002GA][0x00483270-0x004832b9.ChattingColorListPaneFindIndexByColorValue](by-memory/0x00483270-0x004832b9.ChattingColorListPaneFindIndexByColorValue.md) verification independently validates this page's item-field model: the lookup helper scans row items and compares `ChattingColorSwatchItem::colorValue` at `+0x04`, the same field this draw method uses for zero/default swatch handling and palette fill.

## Dependencies And Human Names

- `0x004b7c50`: file-level `void __cdecl InitRectBounds(RectBounds *bounds, int left, int top, int right, int bottom)`.
- `0x004f3f00`: inherited `bool ListPane::IsItemSelected(int row) const` behavior.
- `0x005446b0`: inherited `Pane::SetMode(unsigned char mode)`; this body always passes `1`.
- `0x004b9660`: inherited `GrafPort::SetDrawColor(unsigned int color)`.
- `0x0069b3fc`: typed `SurfaceFillRectProc` callback, source-facing `g_pfnFillRect`; the slot is shared rendering infrastructure, not a target-local helper.
- `ChattingColorSwatchItem`, `row`, `item`, `drawBounds`, `bounds`, and `colorValue` are descriptive human source names supported by object layout and consumers. Raw `sub_4832C0`, `a3`, `a4`, `dword_69B3FC`, and similar IDA spellings are excluded from formal source.

## IDA MCP Evidence

- Current report evidence used the supervisor-reserved read-only session `bpool-readonly-202608060728` as a dated snapshot; session/PID identities are not permanent authority.
- Current exact target bytes hash to `C9AFE1EFFF38BF2C570E789781598F8EEA90A9CDF51AAF374B97FDC99822CDEB` across all 340 bytes.
- Live IDA MCP on 2026-06-05 against binary MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms `sub_4832C0` as a modeled function at exact range `0x004832c0-0x00483414`; `0x00483414` is not a function start and the next modeled function is [UID:0002GC][0x00483420-0x00483487.ChattingColorListPaneOnItemSelected](by-memory/0x00483420-0x00483487.ChattingColorListPaneOnItemSelected.md).
- IDA function iteration reports `sub_4832C0` at exact range `0x004832c0-0x00483414`.
- IDA vtable data at `0x00615228` points to `0x004832c0`, confirming virtual ownership by `ChattingColorListPane`; `xrefs_to 0x004832c0` reports this vtable slot as the only direct xref.
- Decompilation checks `*(a3+4)` for zero, calls `sub_4F3F00(this, row)` for selection state, uses palette indices `136`, `128`, and `143`, and finally calls the fill routine with `*(a3+4)` as the color/palette value.
- Callee enumeration reports `sub_4B7C50`, `sub_5446B0`, `sub_4B9660`, `dword_69B3FC`, `sub_4F3F00`, and the security-cookie check helper. Direct callers are empty, matching vtable-owned dispatch.
- `xrefs_to 0x004f3f00` includes this method's selected-row test at `0x0048332e`; `xrefs_to 0x004b7c50` includes the local rectangle setup calls.
- Decompilation and raw disassembly agree on the draw sequence: zero-color branch builds inset-2 rectangle and fills with palette `136`; selected-row branch builds full rectangle with palette `128` and inset-2 rectangle with palette `143`; final pass builds inset-3 rectangle and fills with `*(a3+4)`.
- Raw bytes include a stack-cookie prologue/epilogue, and the following bytes `0x00483414-0x00483420` are twelve `0xcc` alignment bytes before the selected-item handler.

## Positive Evidence

- Vtable cell `0x00615228 -> 0x004832c0` is the sole inbound xref, proving class ownership and explaining the empty direct-caller list.
- Raw disassembly and decompilation agree on all four rectangle/state/color/callback groups and on independent zero-color and selected branches.
- Constructor, getter, find-index, and OnItemSelected pages independently corroborate the eight-byte item and `colorValue` at `+0x04`.
- Helper prototypes, callback type, vtable placement, surrounding local methods, and existing owner/emitter metadata converge on `ChattingColorListPane` and `social/Chatting.cpp`.
- Exact tail bytes and the next modeled function prove the executable body boundary and twelve-byte alignment island.

## Negative Evidence And Rejected Alternatives

- No binary call supports `FillPaletteRect`; using it would hide four proven mode/color/callback transitions.
- This body calls `InitRectBounds` directly; substituting `InsetRectBounds` would describe a helper that is not called here.
- No evidence supports ownership by `ChattingColorPane`, base `ListPane`, the aggregate page, or a free helper.
- No evidence supports a one-method source file or an immediate `social/ChatColorDialog.cpp` split. That family split remains broader future source-tree cleanup only.
- Empty direct callers do not imply dead or compiler-only code because the vtable cell is the direct dispatch route.
- Do not add null, index, rectangle, callback, palette, or clipping guards absent from the executable.
- Wave2/Wave3 wording and the old compressed helper draft are retained only as stale historical assumptions, not current evidence.

## Source Placement And Formal Channels

- Canonical owner/emitter remains [UID:00001S][ChattingColorListPane](by-class/ChattingColorListPane.md); source root remains [UID:0000I5][Chatting](by-file/Chatting.md).
- This exact function-range page emits only the out-of-line `DrawItem` definition through formal CPP.
- The owner class page formal CPP defines `ChattingColorSwatchItem` before use and declares the complete local-core `ChattingColorListPane : public ListPane`, including this virtual signature, before `[[CHILDREN]]`.
- Target formal H remains blank because this range does not own a legal standalone declaration. Owner formal H also remains blank because the current project route emits the complete declaration through the class-owner CPP channel. This is a resolved placement decision, not a deferred type blocker.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 91 | Exact range/hash, ABI, item layout, vtable liveness, every branch/call/color/bounds transition, typed callback, human helper names, owner/emitter/source route, class-owner declaration, exact target CPP, blank-H proof, negative constraints, and history are documented. |
| Confidence | 94 | Current MCP facts plus independent constructor/getter/find/select/OnItemSelected and support-page evidence resolve all target-local behavior, ownership, helper, type, global, placement, and source-shape questions. Only exact original spelling and unrelated owner-page pollution remain bounded project-wide uncertainty. |

- The score is not higher because original symbols do not survive and later wrong-owner attachments on the class page remain separate cleanup.
- The score is not lower because no target-local source-quality blocker remains deferred.

## Open Questions

- Draw/fill callback: resolved to typed shared global `g_pfnFillRect` at `0x0069b3fc`; reject raw `dword_69B3FC` and an invented local fill helper.
- Rectangle helper: resolved to `InitRectBounds`; reject target use of `InsetRectBounds` because this body calls the initializer directly.
- Draw-state helpers: resolved to inherited `SetMode(1)` and `SetDrawColor(unsigned int)`.
- Selection helper: resolved to inherited `IsItemSelected(row)` returning bool.
- Item type/field: resolved to eight-byte `ChattingColorSwatchItem { int row; int colorValue; }`; exact original spelling is inferred but no raw name is permitted in emitted source.
- Source route: resolved to current `social/Chatting.cpp`; no target-local split.
- Formal placement: resolved through the owner CPP declaration plus target out-of-line CPP; target and owner H remain intentionally blank.
- Reachability: resolved by the sole vtable data xref; ordinary direct callers are not expected.
- No target-local source-quality question remains open.

## Cross-References

- [UID:00001S][ChattingColorListPane](by-class/ChattingColorListPane.md)
- [UID:0002GC][0x00483420-0x00483487.ChattingColorListPaneOnItemSelected](by-memory/0x00483420-0x00483487.ChattingColorListPaneOnItemSelected.md)
- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)

## Changes

- 2026-06-05: Updated completion to `82` while keeping confidence at `90`, removed the below-threshold approximate C++ sketch, and replaced it with source-shape notes. Live IDA against MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirmed exact `sub_4832C0` bounds, vtable-only slot `0x00615228`, empty direct callers, rectangle setup helper `sub_4B7C50`, selected-row test `sub_4F3F00`, palette values `136/128/143`, zero-color/default branch, selected-row branch, final item-color fill from `a3+4`, stack-cookie shape, and twelve-byte trailing padding before [UID:0002GC][0x00483420-0x00483487.ChattingColorListPaneOnItemSelected](by-memory/0x00483420-0x00483487.ChattingColorListPaneOnItemSelected.md). Final reconstruction C++ remains blank below the source-grade threshold.
- 2026-06-17 B002 source-quality execution:
  - Before: `82/90`, vtable-owned behavior documented but formal C++ blank.
  - After: `86/91` with first-draft `DrawItem` C++.
  - Summary/evidence: B002 rechecked vtable-only reachability, no direct callers, stack-cookie body shape, local PE vtable slot/padding, `ChattingColorSwatchItem::colorValue`, zero/default swatch branch, selected-row palette sequence `128/143`, final inset-3 fill, current `Chatting.cpp` route, and final helper-name caveats.
- 2026-06-22 B014 support incorporation from `0002GA`: no score or C++ change. The B014 lookup report corroborates that `item+0x04` is `ChattingColorSwatchItem::colorValue`, shared by the find-index scan and this draw method's swatch fill/default behavior.
- 2026-08-06 B008 Rule 26 implementation callback:
  - Raised the page from `86/91` to `91/94` without changing owner, reconstructable state, emitter, emitter position, or exact range.
  - Replaced the B002 compressed `InsetRectBounds`/`FillPaletteRect` draft with the explicit four-group `InitRectBounds`/`SetMode`/`SetDrawColor`/`g_pfnFillRect` body proven by current binary evidence.
  - Added exact function-byte hash, ABI, item and rectangle layouts, branch/call sequence, typed dependencies, positive/negative evidence, source placement, blank-H proof, closed open questions, and human-name rationale.
  - Coordinated the target body with the complete `ChattingColorSwatchItem` and `ChattingColorListPane` declaration in the owner page formal CPP channel. Historical blank-C++, raw-name, and compressed-draft evidence remains above as labeled chronology rather than current source authority.
