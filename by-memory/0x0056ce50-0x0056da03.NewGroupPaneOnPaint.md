*** UID:0004Z5 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000090 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000090 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:70 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewGroupPane::OnPaint()
{
    EPFTileContext tileContext;
    EPFTileContext commaFrame;
    EPFTileContext barFill;
    EPFTileContext barLeftCap;
    EPFTileContext barRightCap;
    EPFTileContext scratchContext;
    RectBounds imageBounds;
    RectBounds sourceBounds;
    RectBounds destinationBounds;
    wchar_t percentText[128];
    HumanImageRenderParams appearance;

    appearance.m_displayStatusByte = 0x50;
    DLPalette *inventoryPalette = g_pPaletteLib->GetPaletteByName(L"ITEMINV.PAL");

    g_pEPFLib->LookupLayoutEntry(L"COMMA.EPF", 0, &commaFrame);
    g_pEPFLib->LookupLayoutEntry(
        m_expanded ? L"ITEMINVE.EPF" : L"GROUP.EPF",
        m_expanded ? 3 : 0, &tileContext);
    g_pfnBlitSprite(this, &tileContext, &tileContext.bounds,
                    &m_visibleBounds, 0, inventoryPalette, 0);

    RectBounds rowBounds = { 6, 14, 161, 54 };
    SetDrawMode(1);
    SetTextBackColor(0);
    const int visibleRows = m_expanded ? 12 : 6;
    int firstMember = m_groupMemberCount - 6;
    if (m_firstVisibleMember + 6 <= m_groupMemberCount)
        firstMember = m_firstVisibleMember;
    if (firstMember < 0)
        firstMember = 0;
    const int lastMember = firstMember + visibleRows < m_groupMemberCount
        ? firstMember + visibleRows : m_groupMemberCount;
    if (m_expanded)
        rowBounds.Offset(5, 0);

    for (int memberIndex = firstMember;
         memberIndex < lastMember;
         ++memberIndex, rowBounds.Offset(0, 44))
    {
        GroupMemberRecord &member =
            g_pUserPane->m_groupMembers[memberIndex];

        g_pEPFLib->LookupLayoutEntry(L"GROUPCOL.EPF", 0, &tileContext);
        RenderTileFrame(&tileContext, &tileContext.bounds, &rowBounds,
                        0, L"GROUPCOL.PAL", 0);

        appearance.m_ridingAppearanceId = 0;
        appearance.m_gender = 0;
        appearance.m_compositionMode = 0;
        appearance.m_faceId = member.faceId;
        appearance.m_hairId = member.hairId;
        appearance.m_faceColor = member.faceColor;
        appearance.m_hairColor = member.hairColor;
        appearance.m_skinColor = member.skinColor;
        appearance.m_bodyId = 0xffff;
        appearance.m_weaponId = 0xffff;
        appearance.m_bodyColor = 0;
        appearance.m_weaponColor = 0;
        appearance.m_shieldOrArrowId = 0xffff;
        appearance.m_shieldOrArrowColor = 0;
        appearance.m_helmetId = member.helmetId;
        appearance.m_helmetColor = member.helmetColor;
        appearance.m_mantleId = 0xffff;
        appearance.m_neckId = 0xffff;
        appearance.m_mantleColor = 0;
        appearance.m_neckColor = 0;
        appearance.m_shoesId = 0xffff;
        appearance.m_coatId = 0xffff;
        appearance.m_shoesColor = 0;
        appearance.m_invisible = 0;
        appearance.m_faceDecorationId = member.faceDecorationId;
        appearance.m_faceDecorationColor = member.faceDecorationColor;
        appearance.m_hairDecorationId = member.hairDecorationId;
        appearance.m_hairDecorationColor = member.hairDecorationColor;
        appearance.m_secondFaceDecorationId = member.secondFaceDecorationId;
        appearance.m_secondFaceDecorationColor = member.secondFaceDecorationColor;
        appearance.m_fullBodyId = 0xffff;

        g_pNewHumanImageLib->CalculateBounds(
            &appearance, 2, 0, 0, &imageBounds, 0, 1, true, 0);

        RectBounds memberBounds = rowBounds;
        memberBounds.top = memberBounds.bottom - 20;
        memberBounds.left = imageBounds.left + 13;
        memberBounds.right = imageBounds.left + 72;
        const int imageHeight = imageBounds.bottom - imageBounds.top;
        if (imageHeight > 40)
            memberBounds.Offset(0, -(imageHeight - 40) / 2);
        memberBounds.Offset(m_expanded ? 5 : 0, 2);

        g_pNewHumanImageLib->Draw(
            this, &memberBounds, &appearance, 2, 0, 0, 0,
            false, 0, 0.0f, 1, true, 0);

        RectBounds nameBounds = {
            rowBounds.left + 62, rowBounds.top + 6,
            rowBounds.left + 162, rowBounds.top + 20
        };
        const unsigned char nameColor = member.active ? 0x0c : 0x8f;
        SetTextColor(nameColor);
        nameBounds.Offset(-1, -2);
        DrawTextInRect(member.name, wcslen(member.name), &nameBounds);
        nameBounds.Offset(1, 0);
        DrawTextInRect(member.name, wcslen(member.name), &nameBounds);
        SetTextColor(0x8f);

        if (member.currentHitPoints != 0)
        {
            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 0, &barFill);
            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 1, &barLeftCap);
            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 2, &barRightCap);

            RectBounds fullBar = {
                rowBounds.left + 64, rowBounds.top + 20,
                rowBounds.left + 149, rowBounds.top + 34
            };
            RectBounds fillBounds = fullBar;
            fillBounds.right = fullBar.right
                - (fullBar.right - fullBar.left) / 2 - 4;
            if (member.maximumHitPoints != 0)
            {
                if (member.currentHitPoints < member.maximumHitPoints)
                {
                    fillBounds.left = fillBounds.right - MulDiv(
                        fillBounds.right - fillBounds.left,
                        member.currentHitPoints,
                        member.maximumHitPoints);
                }

                sourceBounds = barFill.bounds;
                sourceBounds.left += fillBounds.left - fillBounds.right;
                RectBounds leftCapBounds = fillBounds;
                leftCapBounds.left = fillBounds.left - 3;
                leftCapBounds.right = fillBounds.left;
                RectBounds rightCapBounds = fillBounds;
                rightCapBounds.left = fillBounds.right;
                rightCapBounds.right = fillBounds.right + 3;
                RenderTileFrame(&barLeftCap, &barLeftCap.bounds,
                                &leftCapBounds, 1, L"BAR.PAL", 0);
                RenderTileFrame(&barFill, &sourceBounds, &fillBounds,
                                0, L"BAR.PAL", 0);
                RenderTileFrame(&barRightCap, &barRightCap.bounds,
                                &rightCapBounds, 1, L"BAR.PAL", 0);
            }
            else
            {
                fillBounds.left = fillBounds.right;
            }

            if (m_exposedRegion.IntersectsRect(&fillBounds))
            {
                fillBounds.Offset(-2, 3);
                swprintf_s(percentText, 128, L"%d%%",
                    static_cast<unsigned int>(
                        static_cast<double>(member.currentHitPoints) /
                        static_cast<double>(member.maximumHitPoints) * 100.0));
                fillBounds.left = fillBounds.right - 9 * wcslen(percentText);
                DrawNumberString(percentText, fillBounds.left,
                                 fillBounds.top, 2);
            }

            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 3, &barFill);
            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 4, &barLeftCap);
            g_pEPFLib->LookupLayoutEntry(L"BAR.EPF", 5, &barRightCap);

            fillBounds = fullBar;
            fillBounds.left = fullBar.left
                + (fullBar.right - fullBar.left) / 2 + 4;
            if (member.maximumManaPoints != 0)
            {
                if (member.currentManaPoints < member.maximumManaPoints)
                {
                    fillBounds.left = fillBounds.right - MulDiv(
                        fillBounds.right - fillBounds.left,
                        member.currentManaPoints,
                        member.maximumManaPoints);
                }

                sourceBounds = barFill.bounds;
                sourceBounds.left += fillBounds.left - fillBounds.right;
                RectBounds leftCapBounds = fillBounds;
                leftCapBounds.left = fillBounds.left - 3;
                leftCapBounds.right = fillBounds.left;
                RectBounds rightCapBounds = fillBounds;
                rightCapBounds.left = fillBounds.right;
                rightCapBounds.right = fillBounds.right + 3;
                RenderTileFrame(&barLeftCap, &barLeftCap.bounds,
                                &leftCapBounds, 1, L"BAR.PAL", 0);
                RenderTileFrame(&barFill, &sourceBounds, &fillBounds,
                                0, L"BAR.PAL", 0);
                RenderTileFrame(&barRightCap, &barRightCap.bounds,
                                &rightCapBounds, 1, L"BAR.PAL", 0);
            }
            else
            {
                fillBounds.left = fillBounds.right;
            }

            if (m_exposedRegion.IntersectsRect(&fillBounds))
            {
                fillBounds.Offset(-2, 3);
                swprintf_s(percentText, 128, L"%d%%",
                    static_cast<unsigned int>(
                        static_cast<double>(member.currentManaPoints) /
                        static_cast<double>(member.maximumManaPoints) * 100.0));
                fillBounds.left = fillBounds.right - 9 * wcslen(percentText);
                DrawNumberString(percentText, fillBounds.left,
                                 fillBounds.top, 2);
            }
        }
    }

    g_pEPFLib->LookupLayoutEntry(L"INVENBUT.EPF",
        7 * m_toggleButtonState + (m_expanded ? 3 : 4), &tileContext);
    RectBounds toggleBounds;
    InitRectBounds(&toggleBounds, 168, 37, 185, 54);
    if (m_expanded)
        toggleBounds.Offset(5, 0);
    RenderTileFrame(&tileContext, &tileContext.bounds, &toggleBounds,
                    0, L"invenbut.pal", 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact 2,995-byte primary paint override with completion-ready formal CPP covering normal/expanded backgrounds, six/12 member rows, global GroupMemberRecord access, binary-exact partial HumanImageRenderParams writes, full NewHuman bounds/draw calls and member geometry, names, HP/MP bars/numeric overlays, clipping and toggle rendering; six explicit EPFTileContext Initialize calls; accepted .bounds; frame-0 COMMA.EPF lookup; one reused tileContext; initialized-only scratch local; constructor-free RectBounds geometry; and direct UID0003F2/UID0003F8 source-use reconstruction, with complete NewHuman/Palette H ownership, singleton extern/definition separation, Group file-preamble closure, and no standalone pooled object. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***


# 0x0056ce50-0x0056da03 NewGroupPaneOnPaint

## Status

- Disposition: reconstructable NexusTK source owned by [UID:000090][NewGroupPane](by-class/NewGroupPane.md).
- Source placement: NexusTK/social/Group.cpp through the UID000090 child emitter route.
- Exact physical range: [0x0056ce50,0x0056da03).
- Aggregate container: [UID:0001HC][0x0056c4c0-0x0056e939.GroupPanes](by-memory/0x0056c4c0-0x0056e939.GroupPanes.md).
- Formal CPP: nonblank source-ready implementation above.
- Formal H: intentionally blank; member declarations live in UID000090, while file-local helpers have no public header declaration.

## Accepted Destination-Ready Research

- Destination/range: `by-memory/0x0056ce50-0x0056da03.NewGroupPaneOnPaint.md`, `[0x0056ce50,0x0056da03)`, `0xbb3` / 2,995 bytes, position `70`.
- Role/ABI: primary `+0x44` `void OnPaint()`; binary receives the complete-object pointer while IDA's current `GrafPort *` type reflects Pane inheritance.
- Behavior/control flow: explicitly calls `EPFTileContext::Initialize()` six times in binary order, writes only `appearance.m_displayStatusByte` before resource lookup, resolves `ITEMINV.PAL`, performs the frame-zero `COMMA.EPF` lookup, selects normal/expanded group background, draws up to six/12 visible rows, renders each member's human-image appearance, active/inactive name, HP and MP bar frames and percentages through DrawNumberString, advances 44 pixels per row, and finally draws the normal/expanded toggle button using `m_toggleButtonState`.
- Reachability/evidence: primary cell `0x624598`, two exact DrawNumberString calls, group/member/resource strings, UserPane record reads, and image/palette helpers. No independent free-renderer owner exists.
- Dependency resolution: the packed stack record maps field-for-field to accepted `HumanImageRenderParams`; the binary performs no `Reset()` call and no whole-object clear. It writes only the exact `m_*` members present in the formal source, preserving untouched padding/status/riding fields as indeterminate. `NewHumanImageLib::CalculateBounds` at `0x004e09e0` and `NewHumanImageLib::Draw` at `0x004e06a0` consume the full argument contracts shown above. Existing EPFTileContext, ImageLib/ResourceLayoutTable, PaletteLib, RectBounds, Region, GrafPort, and `SurfaceSpriteBlitProc g_pfnBlitSprite` documentation supplies the remaining APIs. `sub_554A00`, `sub_4BAD70`, `sub_41B9B0`, and `sub_4B9690` are expressed as accepted `Region::IntersectsRect`, `GrafPort::DrawTextInRect`, bounded wide formatting, and `SetTextBackColor` source APIs. The two bars remain expanded in place as the observed frame-load/crop/cap sequence; no invented `DrawStatusBar` helper remains.
- Exact resources and geometry: `ITEMINV.PAL`; expanded `ITEMINVE.EPF` frame 3 or normal `GROUP.EPF` frame 0; row frame `GROUPCOL.EPF`/`GROUPCOL.PAL`; status frames 0/1/2 for HP and 3/4/5 for MP from `BAR.EPF`/`BAR.PAL`; numeric punctuation `COMMA.EPF`; toggle `INVENBUT.EPF` with `invenbut.pal` and frame `7*m_toggleButtonState + (m_expanded ? 3 : 4)`. Initial row bounds are `(6,14,161,54)`, row stride is 44, visible count is six or twelve, and toggle bounds are `(168,37,185,54)` shifted five pixels on X in expanded mode.
- Placement/history: Group.cpp member virtual; never a standalone renderer or vtable body.
- Historical assumption: the prior report state left CPP blank because it treated the appearance record and inherited render APIs as unavailable. Current support formals and exact caller/callee matching disprove that premise. It remains correct to avoid decompiler temporaries and invented helper functions, but it is no longer correct to omit the source body.
- Metadata: `COMPLETION:94`, `CONFIDENCE:93`, `CANONICAL_OWNER:000090`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000090`, `EMITTER_POSITION_OPTIONAL:70`, `Nested:0`, completion-ready formal CPP, blank formal H. Confidence is 93 because exact behavior is closed while original local spellings, const qualifiers, and the initialized-only scratch context's purpose remain inferred; it is not lower because body, resources, row policy, support types, fields, vtable role, and human source expression are resolved.
- Exact formal CPP disposition: the source-ready body is stored in the formal RECONSTRUCTION_CPP CODE channel above.
- The formal block now expands both observed three-frame bars in place. HP uses frames 0/1/2 on the left half, MP uses 3/4/5 on the right half, each fill is right-anchored and source-cropped by the exact `MulDiv` width, cap frames extend three pixels, and percentage text is gated by `m_exposedRegion.IntersectsRect`. The MP sequence remains inside the binary's `currentHitPoints != 0` block; this non-obvious coupling is preserved rather than normalized. No new helper/address child or callback-time source-shape decision remains.
- Exact H disposition: blank; the virtual declaration belongs in the now-complete UID000090 H.
- Validator/generated expectation: `ok:1`; child emits exactly one `NewGroupPane::OnPaint` definition through UID000090, contributes no H, leaves no target empty marker, and creates no duplicate renderer/status-bar address child.
- Scoped callback validation and generated-freshness receipts are recorded in the accepted UID0003F8 report; generated source is validator-owned and must be physically reread rather than edited here.

## UID0003F8 Row Bounds And Corrected Source Dependencies - 2026-08-16

[UID:0003F8][0x00624bf0-0x00624c00.GroupPaneRectConstant](by-memory/0x00624bf0-0x00624c00.GroupPaneRectConstant.md) is the exact compiler-pooled image of this method's automatic `RectBounds rowBounds = { 6, 14, 161, 54 };`. The 16 bytes occur once, have SHA256 `A9CB5BAE7A86BEF48A98DBDFA3BFD6919991F1731C183BF87B24B99C675E03B6`, and have one inbound xref at `0x0056cf78`. The following store at `0x0056cf8b-0x0056cf92` materializes the typed local; no address escape, guard, separate object identity, interior xref, or second consumer exists. UID0003F8 therefore emits only a covered-by marker through UID000090 position 76 and no H declaration or standalone constant definition.

The six context objects have distinct initialization calls but not six persistent semantic roles. `tileContext` is reused for background, member-row, and toggle phases because those uses occupy one non-overlapping physical stack slot. `commaFrame` receives the exact `COMMA.EPF` frame-zero lookup after initialization and has no later direct read; the call is retained for its resource/cache/output-write behavior. Three bar contexts hold fill/left-cap/right-cap entries, and the sixth `scratchContext` is initialized but never read. Omitting either the comma lookup or the sixth initializer would alter observed behavior; inventing a consumer for either would overclaim evidence.

`HumanImageRenderParams` is a packed unconstructed local. Before the loop the binary writes only `m_displayStatusByte=0x50`. Each row then writes the exact selector/color/default set in the formal source: riding/gender/composition zeroes, member face/hair/skin values, absent equipment IDs as `0xffff`, observed colors as zero where absent, decoration IDs/colors, `m_fullBodyId=0xffff`, and final shoes/invisible zeroes. It does not copy `member.bodyColor`, invoke a reset helper, value-initialize the object, or clear untouched fields. The full bounds and draw argument lists, `memberBounds` geometry, direct edge arithmetic, and toggle-only `InitRectBounds` call match binary call order and avoid nonexistent `RectBounds` constructor, `Width()`, or `Height()` APIs.

Compile-visible ownership is separate from this method body: global `GroupMemberRecord` and narrow `NewGroupPane` friendship belong to UserPane H; `HumanImageRenderParams` remains UID00050M; complete NewHuman nested rows/class/extern belong to UID0001VF/UID000092/UID0000RT; complete PaletteLib class/extern belong to UID0000A1/UID0000RW; and the Group translation-unit include preamble belongs to the Group source route. This child contains only the method definition and its formal H remains blank.

## UID0003F2 Physical Resource Provenance - 2026-08-15

[UID:0003F2][0x00624b30-0x00624b74.NewGroupPaneCollectionResourceStrings](by-memory/0x00624b30-0x00624b74.NewGroupPaneCollectionResourceStrings.md) is the exact physical source of this method's row-frame and percentage literals. It remains one 68-byte child because all three literals have the same sole method owner and source-use/no-object disposition.

| Source expression in this formal CPP | Physical cell | Exact proof |
| --- | --- | --- |
| One `L"GROUPCOL.EPF"` row-frame lookup | `[0x00624b30,0x00624b4a)`, 26 bytes | Cell SHA256 `07075DA84F0379446E4E9F7099EA31150EE70CB9CDE9D57A7B30B84EBFC5DB10`; sole xref `0x0056d012`, push bytes `68 30 4B 62 00`, instruction SHA256 `72327BA38DFC654E8B33769E0FF8B5ED903A3743E842A446B7B7D50EAE5A3035`. |
| One `L"GROUPCOL.PAL"` row-frame palette argument | `[0x00624b4c,0x00624b66)`, 26 bytes | Cell SHA256 `CD25106123888EC2D00DC6F8AC8721CC2A360EA44D52FD4E4F681B1877BE4BCD`; sole xref `0x0056d04c`, push bytes `68 4C 4B 62 00`, instruction SHA256 `F8B34BB1A3468F62BA40EADF46FBD55EB2DD722763A0DB5B42C4661C1735D096`. |
| Two `L"%d%%"` HP/MP format expressions | Source envelope `[0x00624b68,0x00624b72)`, 10 bytes | Envelope SHA256 `C5D4AD672797CF035D26435DBE8CE61F75C2D279B025C91B92F867724E8B87EF`; xrefs `0x0056d5c6` and `0x0056d884`, each push bytes `68 68 4B 62 00`, instruction SHA256 `DBC18A9C9017F8C30C0A0493FC5F0EE05646666C32EEDBCAED9B3045B3304A7A`. |

IDA physically splits the format into named head `[0x00624b68,0x00624b6c)` (`aD_10`, `const wchar_t`) and unnamed zero-xref suffix `[0x00624b6c,0x00624b72)`, but bytes decode as one `L"%d%%"`; the old `%d`-only interpretation is historical. Three two-byte zero alignments at `[0x00624b4a,0x00624b4c)`, `[0x00624b66,0x00624b68)`, and `[0x00624b72,0x00624b74)` are non-source padding and remain untouched.

All four references are inside this exact method `[0x0056ce50,0x0056da03)`. No initializer, address-taking use, second consumer, or declaration evidence exists. The method body therefore remains unchanged: ordinary literal expressions are the human source, linker pooling explains one binary copy, and UID0003F2 contributes only a position-75 covered-by marker through [UID:000090][NewGroupPane](by-class/NewGroupPane.md). It contributes no H declaration or duplicate definition.

## UID0003F6 Percentage Scale Literal Dependency - 2026-08-15

[UID:0003F6][0x00624bd0-0x00624be0.GroupPaneScaleConstant](by-memory/0x00624bd0-0x00624be0.GroupPaneScaleConstant.md) is the exact physical pool for both `100.0` multipliers in this method. Its 16-byte range has SHA256 `3F2F2A9DC01360F3C383FA53B3F2C5B2860A2A6D38F0197C2327BE38DAF280F0`: `[0x00624bd0,0x00624bd8)` is the unique loaded-image little-endian double `100.0`, while `[0x00624bd8,0x00624be0)` is zero-xref alignment before the SelfLookPane-owned successor at `0x00624be0`.

| Instruction xref | Exact method expression | Meaning |
| --- | --- | --- |
| `0x0056d5b8` | `static_cast<double>(member.currentHitPoints) / static_cast<double>(member.maximumHitPoints) * 100.0` | HP percentage before unsigned conversion and `L"%d%%"` formatting. |
| `0x0056d876` | `static_cast<double>(member.currentManaPoints) / static_cast<double>(member.maximumManaPoints) * 100.0` | MP percentage before unsigned conversion and `L"%d%%"` formatting. |

These are the complete xrefs to the physical double, and the byte pattern occurs once in the loaded image. The existing inline literals in the formal CPP above are the authoritative human-source representation. UID0003F6 routes a covered-by marker through [UID:0000JS][Group](by-file/Group.md); it does not add a static/global object, class member, macro, table entry, duplicate calculation, or H declaration. The method's `94/95` metadata, UID000090 owner/emitter route, complete CPP, and blank H remain unchanged.

## Boundary And Emission Decision

- The half-open range and adjoining alignment exclusions are fixed by the accepted UID000090 report package.
- This page emits exactly one source body through UID000090 at position 70.
- It does not emit vtable arrays, compiler destructor glue, aggregate GroupPanes code, or a duplicate declaration.
- Compiler-generated and neighboring bytes remain outside this page exactly as documented in the accepted package.

## Historical Assumptions

- The former aggregate-only/no-child disposition is historical. Target-specific live IDA, range, ABI, behavior, and source-placement evidence now supports this exact child page and formal source body.
- Any older generated sub_, raw-address, swapped-slot, or no-route-equals-no-source label is retained only as rejected historical context; it does not enter formal source.
- The older generic description of `qword_624BD0` as GroupPane-family drawing/setup math is superseded by the exact UID0003F6 HP/MP percentage dependency above.
