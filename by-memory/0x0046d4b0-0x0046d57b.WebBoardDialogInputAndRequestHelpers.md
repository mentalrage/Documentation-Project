*** UID:00020Z | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000G2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000G2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static const unsigned char kWebBoardInitialRequestOpcode = 0x73;
static const unsigned int kWebBoardRequestTimerId = 0x572;
static const unsigned int kWebBoardRequestTimerDuration = 20000;

bool WebBoardDialog::OnKeyEvent(InputEvent *event)
{
    const char key =
        NarrowInputCharacter(event->rawKey, event->modifierState);

    if (event->type == 8) {
        if (key == 'b' && (event->modifierState & 7) == 0) {
            CloseOrBackFromBoardOwner();
            return true;
        }

        if (event->rawKey == 0x90)
            return true;
    }

    DialogPane::OnKeyEvent(event);
    return true;
}

void WebBoardDialog::SendInitialBoardRequest(unsigned char boardMode)
{
    unsigned char packet[3];

    PacketBufferWriteByte(kWebBoardInitialRequestOpcode, &packet[0]);
    PacketBufferWriteByte(boardMode, &packet[1]);
    packet[2] = 0;

    QueueAndSendPacket(g_pPacketQueue, packet, 2);
    StartPaneTimer(kWebBoardRequestTimerId,
                   kWebBoardRequestTimerDuration,
                   0,
                   0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms exact WebBoardDialog input/request helper cluster, modeled 0x5e-byte input handler at vtable slot 0x00613658, key/back handling through do_narrow/sub_49DF20/sub_49E6E0, raw non-modeled request helper bytes 0x0046d510-0x0046d57c with no xrefs, opcode 0x73 two-byte send through dword_67A7EC/sub_574BB0, timer 0x572/0x4e20 via sub_5975E0(this+0xa4), response-handler callers, corrected 0x0046d57c padding boundary, WebBoardDialog parent attachment, and first-draft OnKeyEvent/SendInitialBoardRequest C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046d4b0-0x0046d57b WebBoardDialog Input And Request Helpers

## Status

- Address range: live half-open executable coverage is `0x0046d4b0-0x0046d57c`; the filename keeps the historical `0x0046d57b` endpoint, which live bytes show is the final immediate byte of the raw helper's `retn 4`
- Entity kind: class method/helper cluster
- IDA ownership state: `0x0046d4b0` is an IDA-modeled WebBoardDialog vtable slot, while `0x0046d510` is executable function-shaped code that IDA does not currently model as a function
- Current source owner: [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md) / [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- Disposition: reconstructable NexusTK project code with raw-helper boundary caveat
- Rebuild handling: `source-authored` method/helper bodies
- Completion: 85%
- Confidence: very strong for the modeled input handler, WebBoardDialog vtable ownership, raw helper instruction layout, packet/timer side effects, corrected padding boundary, no-reference PE scan, retained-helper classification, and constructor inline duplicate branch; final-source confidence remains capped by original helper/event names.
- Autogen status: attached to [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md); B010 promotes accepted first-draft formal C++ while preserving the raw helper's no-IDAFunc/no-xref liveness caveat.

## Score Rationale

| Field | Value | Reason |
| --- | ---: | --- |
| Completion | 88 | Exact modeled input-handler bounds, adjacent vtable slots, raw request-helper byte layout, packet send/timer calls, response-handler callers, padding boundary, no-reference PE scan, constructor inline duplicate branch, retained-helper classification, and first-draft formal C++ are documented. Current reachability is accounted for by the constructor's inline duplicate request branch rather than an unproven helper call. |
| Confidence | 91 | Function, vtable, xref, decompilation, byte evidence, read-only PE no-reference scans, constructor duplicate evidence, packet-forwarder rejection, generated-source comparison, and accepted B010 source shape strongly support WebBoardDialog ownership and retained source-shaped helper classification. Confidence stays below final audit because exact original names/event types and retention cause remain inferred. |
| Reconstructable | TRUE | The modeled virtual and raw helper are source-authored WebBoardDialog behavior and should route under the active WebBoardDialog class/source family. |

## Covered Functions

| Range | Current role | Evidence summary |
| --- | --- | --- |
| `0x0046d4b0-0x0046d50e` | key/input override | Vtable slot `0x00613658`; narrows the incoming key/character state, handles the board/browser close/back key case, otherwise forwards to shared DialogPane key handling. |
| `0x0046d510-0x0046d57c` | raw initial board request/timer helper | Builds opcode `0x73` packet bytes using the caller argument, queues a two-byte packet, starts timer `0x572` for `0x4e20` ticks on the embedded timer/control subobject at `this + 0xa4`, then returns with `retn 4`. |

Internal alignment padding exists at `0x0046d50e-0x0046d510`. The true post-helper alignment is `0x0046d57c-0x0046d580`; live bytes show `0x0046d57b` is the final `00` immediate byte for `retn 4`, not padding.

## IDA MCP Evidence

- 2026-05-30 IDA MCP confirms `ida_funcs.get_func(0x0046d4b0)` returns `sub_46D4B0` with bounds `0x0046d4b0-0x0046d50e`; `ida_funcs.get_func(0x0046d510)` and `ida_funcs.get_func(0x0046d57b)` return no function object.
- 2026-05-30 IDA MCP vtable walk around `0x00613650` shows `0x00613654 -> 0x0046d3b0`, `0x00613658 -> 0x0046d4b0`, `0x0061365c -> 0x0049ea60`, and `0x00613660 -> 0x0046d480`, with RTTI pointer `??_R4WebBoardDialog@@6B@_0` at `0x0061364c`.
- 2026-05-30 IDA MCP disassembly of the raw helper shows `push 0x73`, a second byte from `[ebp+8]`, two calls to `sub_575380`, a two-byte send through `dword_67A7EC`/`sub_574BB0`, then `sub_5975E0(this + 0xa4, 0x572, 0x4e20, 0, 0)`.
- 2026-05-30 IDA MCP decompilation of `sub_575380` shows it writes a byte and a trailing zero (`*dst = byte; dst[1] = 0`); decompilation of `sub_574BB0` shows it copies the requested byte count plus a terminator into a heap buffer and sends it through `sub_596960(..., 8, ..., len + 1)` when the network object is not disabled.
- 2026-05-30 IDA MCP decompilation of `sub_5975E0` shows it is a timer-manager wrapper around `sub_597910(dword_67AB80, this, id, duration, arg4, arg5)`.
- 2026-05-27 IDA MCP function enumeration reports `sub_46D4B0` at `0x0046d4b0-0x0046d50e`, with a vtable data reference at `0x00613658`.
- IDA MCP decompilation of `0x0046d4b0` checks `a2[4]`, narrows the input character through the locale/ctype helper, handles the `'b'`/modifier case through `sub_49DF20(this - 40)`, and otherwise falls back to `sub_49E6E0`.
- IDA MCP raw disassembly shows a function-shaped body at `0x0046d510-0x0046d57c`, but `ida_funcs.get_func(0x46d510)` returns no function object and `xrefs_to 0x0046d510` is empty.
- The raw helper pushes opcode byte `0x73`, appends the caller-provided byte, queues the two-byte packet through `dword_67A7EC`, and starts timer `0x572` with duration `0x4e20` through the timer helper at `0x005975e0`.
- 2026-06-05 live IDA MCP against `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` confirms `sub_46D4B0` at `0x0046d4b0-0x0046d50e` (`0x5e` bytes), no function object at `0x0046d50e`, `0x0046d510`, or `0x0046d57b`, and the next modeled function as [UID:0000Z9][0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse](by-memory/0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse.md).
- 2026-06-05 live vtable audit shows `0x00613650 -> 0x004702d0`, `0x00613654 -> 0x0046d3b0`, `0x00613658 -> 0x0046d4b0`, `0x0061365c -> 0x0049ea60`, and `0x00613660 -> 0x0046d480`, keeping this helper between the active reposition override and the packet callback.
- 2026-06-05 live xrefs show the input handler has only the vtable data ref at `0x00613658`, the raw helper at `0x0046d510` has no code or data refs, and the response handler has code refs from the constructor at `0x0046d2a6` and packet forwarder at `0x0046d49e`.
- 2026-06-05 live decompilation of `sub_46D4B0` narrows `a2[8]` with `a2[266]`, checks `a2[4] == 8`, handles `v3 == 98` (`'b'`) when `(a2[266] & 7) == 0` by calling `sub_49DF20(this - 40)`, suppresses `a2[8] == 0x90`, otherwise calls `sub_49E6E0(this, a2)`, and returns `1`.
- 2026-06-05 live raw disassembly of `0x0046d510` shows the security-cookie prologue, `sub_575380(0x73, &buf[0])`, `sub_575380([ebp+8], &buf[1])`, explicit `buf[2] = 0`, `sub_574BB0(dword_67A7EC, buf, 2)`, `sub_5975E0(this + 0xa4, 0x572, 0x4e20, 0, 0)`, security-cookie check, and `retn 4` at `0x0046d579-0x0046d57c`.
- 2026-06-05 live byte audit confirms `0x0046d4a3-0x0046d4b0` and `0x0046d50e-0x0046d510` are all `0xcc`; it also corrects the post-helper split to `0x0046d57c-0x0046d580` as four `0xcc` bytes after the `retn 4` immediate byte at `0x0046d57b`.

## Source-Quality Reanalysis

B002's 2026-06-16 source-quality pass classifies `0x0046d510-0x0046d57c` as a retained source-shaped out-of-line request helper body with no surviving inbound edge in the current binary. Read-only PE scanning found no rel32 call/jump/jcc target, absolute VA pointer, or RVA pointer to `0x0046d510`; the helper body is unique from its first 32 bytes onward, with full-body SHA1 `9551699bf3fc36edf83a773f1381eff8d04e3e17`. Internal calls remain `0x0046d529 -> 0x00575380`, `0x0046d535 -> 0x00575380`, `0x0046d54d -> 0x00574bb0`, and `0x0046d566 -> 0x005975e0`.

The active constructor contains the same startup request logic inline at `0x0046d2b7 -> 0x00575380`, `0x0046d2c1 -> 0x00575380`, `0x0046d2d8 -> 0x00574bb0`, and `0x0046d2ef -> 0x005975e0`, so current reachability is accounted for without an inbound call to the raw helper. Best source-facing helper declaration is `void __thiscall WebBoardDialog::SendInitialBoardRequest(unsigned char boardMode)`.

B005 2026-06-19 constructor reanalysis accepts [UID:0000Z7][0x0046d050-0x0046d324.WebBoardDialog](by-memory/0x0046d050-0x0046d324.WebBoardDialog.md) first-draft C++ using `SendInitialBoardRequest(static_cast<unsigned char>(boardMode))` as source-level factoring of that inline opcode `0x73` request branch. This does not create a new binary call edge into `0x0046d510`; it documents that the constructor and the retained raw helper represent the same source-shaped board-request behavior.

Rejected alternatives: packet callback target, shared active/old helper, dead artifact, padding, and generic network helper. The adjacent packet callback is `0x0046d480`, vtable-backed at `0x00613660`, and tails to `0x0046d580`; the old helper uses a different no-argument signature and hard-coded zero second byte. B010 resolves the retained-helper emission policy for this callback with first-draft C++ while preserving the no-surviving-inbound-edge fact.

B002's generated-source comparison uses `source-3/simroot_v2/ui/dialogs/WebBoardDialog.cpp` only as a lead, not original-symbol proof. The best current input-handler names are `WebBoardDialog::OnKeyboardInput` or `WebBoardDialog::OnInputEvent`; this page's `OnInputKey` snippet is descriptive. The generated lead favors `OnKeyboardInput`, but the inherited event structure and final source-facing typedef are still unresolved.

Protocol and helper-name inferences from the same pass:

| Item | Best current source-facing name | Evidence / caveat |
| --- | --- | --- |
| `0x73` | initial web-board request opcode | Used by the raw helper and the constructor inline duplicate branch to request the initial board state. |
| `0x62` | web-board response event opcode | Handled by the adjacent packet forwarder at `0x0046d480`, not by the raw request helper. |
| `0x572` | web-board request timeout timer id | Started with duration `0x4e20` / `20000` after the two-byte request is queued. |
| `0x00575380` | `PacketBufferWriteByte` / byte-plus-zero writer | Decompilation shows it writes one byte and a following zero terminator; final name is descriptive. |
| `0x00574bb0` | `QueueAndSendPacket` / socket queue-send wrapper | Queues the requested byte count plus terminator through the packet send object; final name is descriptive. |
| `0x005975e0` | timer-start wrapper | Wraps the global timer manager call used for timer `0x572`; final member/subobject name remains inferred. |

Ranked ownership result: direct semantic owner is [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md), with [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) as source-module route. Rejected alternatives are [UID:00003T][DialogPane](by-class/DialogPane.md) because the handler is WebBoardDialog vtable-owned, Browser/BrowserControlPane because they are infrastructure dependencies, RankingDialog because old generated placement is owner pollution, PacketBuffer/Socket because they are generic callees only, and `CANONICAL_OWNER:NONE` because class ownership is supported despite the raw helper's no-inbound-reference state.

## Decompiled Shape

`0x0046d4b0` is the modeled virtual input/key handler:

```cpp
char __thiscall WebBoardDialog::OnInputKey(KeyState *key)
{
    char ch = std::ctype<char>::do_narrow(key->rawCharOrVirtualKey, key->modifierByte);

    if (key->eventType == 8) {
        if (ch == 'b' && (key->modifierByte & 7) == 0) {
            CloseOrBackFromBoardOwner(this - 0xa0);
            return 1;
        }

        if (key->rawCharOrVirtualKey == 0x90)
            return 1;
    }

    DialogPaneInputHandler(this, key);
    return 1;
}
```

Names above are reconstruction names, not IDA-applied names. The offsets come from IDA decompilation: `a2[4]`, `a2[8]`, `a2[266]`, and the `this - 0xa0` adjustment before `sub_49DF20`.

`0x0046d510-0x0046d57c` is not an IDA function, but its disassembly is a normal `__thiscall` helper body:

```cpp
void __thiscall WebBoardDialog::RequestInitialBoard(unsigned char requestByte)
{
    char packet[3];

    WriteByteAsCString(0x73, &packet[0]);
    WriteByteAsCString(requestByte, &packet[1]);
    packet[2] = 0;

    g_networkClient->Send(packet, 2);
    this->timer.Start(0x572, 0x4e20, 0, 0);
}
```

The source-level helper name is provisional. The byte writer and send names are inferred from their IDA decompilation and repeated caller pattern; the timer call is supported by the `sub_5975E0` wrapper around `dword_67AB80`/`sub_597910`.

## Ownership Notes

`0x0046d4b0` is another derived input override that should be kept with [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md), not generic [UID:00003T][DialogPane](by-class/DialogPane.md). Its vtable slot sits between the reposition override at `0x00613654` and the packet-forwarder slot at `0x00613660`.

`0x0046d510` is not represented as an IDA function, but the prologue, stack cookie, packet writer calls, timer start, and `retn 4` body make it reconstructable source code. Keep the raw boundary and no-xrefs caveat visible until function-boundary/caller evidence accounts for it. The executable helper's half-open extent is `0x0046d510-0x0046d57c`; the byte at `0x0046d57b` is not padding.

The surrounding alignment padding is tracked in [UID:0000VN][-ignored](by-memory/-ignored.md). The next modeled function is [UID:0000Z9][0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse](by-memory/0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse.md).

## Cross-References

- [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md)
- [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- [UID:00003T][DialogPane](by-class/DialogPane.md)
- [UID:00020Y][0x0046d330-0x0046d475.WebBoardDialogVirtualCompanions](by-memory/0x0046d330-0x0046d475.WebBoardDialogVirtualCompanions.md)
- [UID:0000Z9][0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse](by-memory/0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-06-30 B010 empty-emitter implementation callback:
  - Raised score to `88/91` and inserted accepted first-draft formal C++ for `WebBoardDialog::OnKeyEvent(InputEvent *event)` plus `WebBoardDialog::SendInitialBoardRequest(unsigned char boardMode)`.
  - Evidence/disposition: opcode `0x73`, timer `0x572` / `20000`, two-byte request queueing, raw helper no-xref state, and constructor inline duplicate branch are resolved enough for first-draft source; final original event/helper names remain inferred.

- 2026-06-22 direct Rule 26 closeout for B002 `00020Z-000213-webboard-request-source-quality`:
  - Added the remaining report-level details that were not explicit in the target page: generated-source name lead vs proof boundary, `OnKeyboardInput`/`OnInputEvent` naming alternatives, protocol/helper-name inference table, adjacent response opcode distinction, and ranked owner rejection set. Metadata remains `85/90`, owner/emitter [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md), with final C++ blank pending original helper/event names and retained-helper emission policy.

- Before: completion/confidence were `50/65`, the owner line described the state as "Current generated owner", and the helper evidence did not include the 2026-05-30 IDA confirmation of helper callees.
- Changed to: completion/confidence `74/84`, owner wording based on IDA state, and added IDA evidence for vtable placement, helper boundaries, packet send helpers, and timer wrapper behavior.
- Summary/evidence: IDA MCP confirms `sub_46D4B0` as an actual function at `0x0046d4b0-0x0046d50e` and confirms `0x0046d510` has no IDA function object despite normal callable body shape. Disassembly and helper decompilation directly support the packet opcode `0x73`, two-byte send, and timer start description.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and left the parent blank.
  - Reasoning: existing documentation identifies source-owned WebBoardDialog input/request helper code, but the page is `74/84` and fails the child-side 80/80 attachment gate.
- 2026-06-05 A001 live IDA refresh:
  - Raised metadata from `74/84` to `82/88` and attached [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md) because the child and parent now both satisfy the `80/80` gate.
  - Confirmed `sub_46D4B0` bounds, vtable slot `0x00613658`, modeled handler decompilation, raw helper calls to `sub_575380`, `sub_574BB0`, and `sub_5975E0`, no xrefs to `0x0046d510`, response-handler callers, and the true `0x0046d57c-0x0046d580` padding boundary.
  - Final C++ remains blank because `0x0046d510` is still raw/non-modeled and source-facing helper names and declaration/caller evidence remain provisional.
- 2026-06-16 B002 source-quality reanalysis:
  - Changed score from `82/88` to `85/90`, retaining direct owner/emitter [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md).
  - Summary/evidence: read-only PE scans found no rel32 call/jump/jcc, VA pointer, or RVA pointer route to raw helper `0x0046d510`; the active constructor's inline duplicate `0x73` request/timer branch accounts for current reachability. Treat the raw helper as retained source-shaped WebBoardDialog request helper code with no surviving inbound edge, not a callback/shared/dead/padding artifact. Final C++ stays blank pending original helper/event names.
- 2026-06-19 B005 constructor source-quality execution:
  - Added the accepted relationship from [UID:0000Z7][0x0046d050-0x0046d324.WebBoardDialog](by-memory/0x0046d050-0x0046d324.WebBoardDialog.md) first-draft constructor C++ to this helper: constructor source may call `SendInitialBoardRequest(static_cast<unsigned char>(boardMode))` as source-level factoring, while this page still preserves the no-surviving-inbound-edge binary fact for raw helper `0x0046d510`.
