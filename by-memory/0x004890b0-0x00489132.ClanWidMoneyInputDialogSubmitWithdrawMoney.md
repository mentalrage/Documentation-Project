*** UID:0002NU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002O | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002O | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ClanWidMoneyInputDialog::OnSubmitText(const wchar_t *text)
{
    unsigned char packet[128];
    unsigned long amount = _wtol(text);

    PacketBufferWriteUInt8(0x4b, packet + 0);
    PacketBufferWriteUInt8(0x0b, packet + 1);
    PacketBufferWriteUInt8(0x01, packet + 2);
    PacketBufferWriteUInt32BE(amount, packet + 3);
    packet[7] = 0;

    g_packetSender->QueueAndSendPacket(packet, 7);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact virtual withdraw-submit method: a 128-byte source packet local, value-first PacketBuffer writes for `{0x4b,0x0b,1,amount32-be}`, `_wtol` conversion, explicit seven-byte void send through `g_packetSender`, and no input clamp or send-result branch. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004890b0-0x00489132 ClanWidMoneyInputDialog Submit Withdraw Money

## 2026-08-14 Source Placement Readback

- Owner/emitter remain UID00002O and UID0000I8; formal CPP stays unchanged and emits through `NexusTK/social/Clan.cpp`.
- UID0000IA is a non-standalone, non-emitting index, not a coordinated future split destination. Historical split-candidate language below is retained only as superseded history.
- P01 source-quality IDA identity remains protected for supervisor readback; this callback makes no IDA change.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: virtual submitter/method body.
- Owner class: [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md).
- Likely source module: [UID:0000I8][Clan](by-file/Clan.md).
- Reconstruction parent: [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md).
- Rebuild handling: `source-authored`.
- Confidence: very strong for exact function bounds and hash, vtable ownership, void callback type, 128-byte local-buffer shape, value-first packet API, packet layout, sender ownership, protocol route, and source-facing method-body readiness. Lexical source names remain high-probability inferences rather than recovered symbols.

## Covered Range

| Range | Entity | Notes |
| --- | --- | --- |
| `0x004890b0-0x00489132` | `ClanWidMoneyInputDialog` withdraw-money submitter | Function body at `0x004890b0`, size `0x82`; next bytes `0x00489132-0x00489140` are `0xcc` alignment before the next raw constructor-shaped island. |

## Behavior

This method receives the submitted dialog text, converts it to an unsigned integer, builds a seven-byte clan packet, and sends it through [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md) using [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md).

Packet fields:

| Offset | Value | Meaning |
| --- | --- | --- |
| `+0` | `0x4b` | Clan request opcode. |
| `+1` | `0x0b` | Clan money-withdraw subtype. |
| `+2` | `0x01` | Submit/confirm action. |
| `+3..+6` | converted amount | Big-endian 32-bit numeric amount from the input text, written through `0x005753f0` / `PacketBufferWriteUInt32BE`. |

## IDA MCP Evidence

- 2026-07-05 B010 MCP support sync, session `supervisor_recovery_20260705`:
  - `lookup_funcs(0x004890b0)` reports current IDB function name `sub_4890B0`, start `0x004890b0`, and size `0x82`; `ClanWidMoneyInputDialogSubmitWithdrawMoney` remains the stable source-facing documentation name, not a proven original symbol.
  - `xrefs_to(0x004890b0)` reports the vtable-only data route `0x00615a94 -> 0x004890b0`; exact VA bytes for `0x004890b0` matched that slot and no RVA-pointer route was found.
  - The sibling raw constructor child [UID:0002NT][0x00489070-0x004890a5.ClanWidMoneyInputDialogRawConstructor](by-memory/0x00489070-0x004890a5.ClanWidMoneyInputDialogRawConstructor.md) has no function/decompile/xrefs/pointer route/direct branch route at its raw start/end, so this submitter remains the routed source-bearing withdraw-money method body.
  - Decompilation still shows CRT `_wtol` at `0x005cea43`, byte writes for opcode `0x4b`, subtype `0x0b`, and action `1`, big-endian amount writer `0x005753f0`, `packet[7] = 0`, and `QueueAndSendPacket(..., 7)` through `0x00574bb0`.

- 2026-06-05 live IDA refresh:
  - `lookup_funcs(0x004890b0)` reports a function body at `0x004890b0`, size `0x82`, giving exclusive range `0x004890b0-0x00489132`; `lookup_funcs` reports no function at `0x00489132` or `0x00489140`.
  - `xrefs_to(0x004890b0)` reports a data xref from vtable slot `0x00615a94`.
  - `xrefs_to(0x00489132)` and `xrefs_to(0x00489140)` report no references to the padding/successor starts.
  - `callers(0x004890b0)` reports no direct code callers, consistent with vtable dispatch.
  - `callees(0x004890b0)` reports the conversion helper at `0x005cea43`, byte writer at `0x00575380`, dword writer at `0x005753f0`, queue/send helper at `0x00574bb0`, and security-cookie support.
  - IDA decompilation shows the submitted-text conversion result feeding the dword writer at `0x005753f0`, then the queue/send helper at `0x00574bb0` sending exactly `7` bytes through packet sender slot `0x0067a7ec`.
  - Byte audit confirms `0x00489132-0x00489140` is all `0xcc` padding followed by the prologue bytes of the next raw constructor-shaped island.

## Ownership Decision

Attach this submitter to [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md) because both pages are above the parent-attachment gate and this is a single-owner virtual submit method. The class page carries the [UID:0000I8][Clan](by-file/Clan.md) source-module parent.

This exact vtable-backed method body clears the active reconstruction-code gate after source-quality review. The formal C++ block uses the inferred/descriptive shared submit signature `void ClanWidMoneyInputDialog::OnSubmitText(const wchar_t *text)`. Evidence: `ClanNameInputDialog::OnButtonClick` reads text control id `4`, dispatches primary vtable slot `+0x5c` with `this` plus the submitted wide-text buffer, and consumes no result. All six current submitter leaves likewise produce no return value. The raw constructor/class-declaration caveat belongs to the class/constructor pages and does not block this method body.

## 2026-06-17 B001 Source-Quality Recheck

- B001 resolved the shared submitter callback shape as `OnSubmitText(const wchar_t *text)` for the six exact `ClanNameInputDialog` derived submit overrides.
- `0x005cea43` is CRT `_wtol` / `__wtol`, not a product conversion helper. The method converts the submitted wide text to an amount and sends opcode `0x4b`, subtype `0x0b`, action `0x01`.
- `0x005753f0` is the scalar big-endian writer `PacketBufferWriteUInt32BE`; the old little-endian wording is rejected by the PacketBuffer split.
- `g_packetSender` remains the shared Socket sender storage, with final header spelling still descriptive. The helper-name uncertainty is not a first-draft blocker because the exact packet shape, vtable owner, and emitter route are resolved.
- Final class declarations and raw constructor reachability remain class-level follow-up items; this method page emits only the exact vtable-backed submitter body.

## Historical 2026-06-19 B012 Helper-Signature Recheck

- The method name, owner, packet bytes, CRT `_wtol`, big-endian amount write, and seven-byte send remain source-ready.
- PacketBuffer helper support docs currently favor value-first scalar writer signatures: `PacketBufferWriteUInt8(value, out)` and `PacketBufferWriteUInt32BE(value, out)`.
- Historical defect: this recheck described the then-current body as destination-first even though the formal body was already value-first. Current live helper declarations and cdecl push order independently prove `PacketBufferWriteUInt8(value, destination)` and `PacketBufferWriteUInt32BE(value, destination)`.
- Historical defect: the family still used an `int` callback and sender return. Current base dispatch and all six submitter leaves prove a `void` callback and a void QueueAndSendPacket statement.
- Current authority is the dedicated 2026-08-10 B006 recheck below; the old API uncertainty is resolved rather than deferred.

## 2026-08-10 B006 Dedicated Source-Quality Recheck

- Canonical live MCP session `supervisor-gate2b-batch2-20260810` models one function at `[0x004890b0,0x00489132)`, size `0x82`. Exact body SHA256 is `DD45DB894501355CC30E71B4636608D43E306E011F292509931F0ABEE2EF395E`; `[0x00489132,0x00489140)` is fourteen bytes of `0xcc` alignment outside this target.
- The sole inbound reference is vtable data xref `0x00615a94 -> 0x004890b0`; there are zero ordinary code callers. This preserves owner/emitter [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md) and source route [UID:0000I8][Clan](by-file/Clan.md) / `NexusTK/social/Clan.cpp`.
- The base caller at `0x00488eb0` reads control id `4`, calls virtual slot `+0x5c` with the wide-text buffer, and ignores a result. Fresh decompilation of all six leaf submitters produces no return value. The source contract is therefore `virtual void OnSubmitText(const wchar_t *text)`, not the historical `int` form.
- The exact frame reserves `0x84` bytes: a 128-byte packet-local region followed by the security cookie. `packet[128]` is the source-family-faithful declaration; the former `packet[8]` was only payload-size-equivalent.
- PacketBuffer APIs are value-first: `PacketBufferWriteUInt8(unsigned char value, unsigned char *destination)` and `PacketBufferWriteUInt32BE(unsigned int value, unsigned char *destination)`. Current helper declarations and x86 cdecl push order agree; destination-first is rejected.
- `_wtol(text)` produces a 32-bit signed `long`; assignment to `unsigned long` preserves the Win32 bit pattern consumed by the BE writer. The binary has no null, parse-success, sign, range, overflow, sender-state, or send-result check.
- Exact outgoing bytes are opcode `0x4b`, subtype `0x0b`, action `1`, then amount at offsets `+3..+6` in big-endian order. The UInt32BE helper zeros byte `+7`, and the explicit `packet[7] = 0` repeats it; send length `7` excludes that byte.
- `Socket::QueueAndSendPacket` copies exactly the seven caller bytes into Socket-owned heap storage, appends its own private zero, and queues the copy. It returns no value, so the callback must not return the call result.
- Protocol closure: outgoing `{0x4b,0x0b,0}` requests the withdraw dialog; incoming opcode `0x43`, subtype `11`, result/action `0` constructs it; action `1` is the accepted no-dialog path. This method submits action `1` plus the amount.
- Source-facing names `OnSubmitText`, `text`, `packet`, and `amount` are high-probability descriptive inferences consistent with the base/derived family. They replace IDA placeholders without claiming recovered lexical proof.
- A one-off move to [ClanDialogs](by-file/ClanDialogs.md) is rejected. That page remains only a coordinated modal-family split candidate; active emission stays in `Clan.cpp`.

## Cross-References

- [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md)
- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:00021M][0x00486530-0x004867f2.ClanWithdrawAndNoticePacketHelpers](by-memory/0x00486530-0x004867f2.ClanWithdrawAndNoticePacketHelpers.md)
- [UID:00010K][0x00488fe0-0x004895fe.ClanNameDialogSubmitters](by-memory/0x00488fe0-0x004895fe.ClanNameDialogSubmitters.md)
- [UID:0001HZ][0x00575380-0x00575abb.PacketBufferHelpers](by-memory/0x00575380-0x00575abb.PacketBufferHelpers.md)
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md)
- [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md)

## Changes

- 2026-08-10 B006 dedicated implementation callback:
  - Raised `86/91 -> 94/94`; kept owner/emitter `00002O`, reconstructable true, and blank formal H.
  - Replaced the formal emitter with the exact void callback, 128-byte local, value-first helper calls, and non-returning seven-byte queue call.
  - Added current body hash/boundary, sole vtable route, base/family void proof, packet API/order, conversion/validation behavior, protocol closure, sender ownership, source placement, and historical rejection of stale `int`, destination-first, `packet[8]`, and sender-return assumptions.

- 2026-07-05 B010 UID0002NT support sync:
  - Kept `86/91`, owner/emitter [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md), and the existing formal submitter body.
  - Clarified that this page emits the routed method body, while sibling raw constructor [UID:0002NT][0x00489070-0x004890a5.ClanWidMoneyInputDialogRawConstructor](by-memory/0x00489070-0x004890a5.ClanWidMoneyInputDialogRawConstructor.md) is marker-only/no standalone constructor body.
  - Evidence: MCP session `supervisor_recovery_20260705` reports current IDB function name `sub_4890B0`, size `0x82`, vtable-only route `0x00615a94 -> 0x004890b0`, raw-constructor negative route proof, and the same opcode/subtype/action/amount/send decompile facts.
- 2026-06-05 live IDA refresh:
  - What existed before: `76/88`, blank parent, raw helper-label evidence, packet-sender global label, and a manual coverage row still at `76%`.
  - Changed to: `82/90`, parent `00002O`, address-based live IDA evidence, packet-buffer/sender dependency links, and a synced manual coverage row.
  - Summary/evidence: live IDA MCP reconfirmed exact `0x82` bounds, no function at the successor/padding starts, vtable-only xref at `0x00615a94`, no direct callers, submitted-text conversion, packet bytes `{ 0x4b, 0x0b, 1, amount32 }`, seven-byte send through `0x00574bb0` using packet sender `0x0067a7ec`, adjacent padding, and the then-current class/raw-constructor caveat.
- 2026-06-17 B001 source-quality execution:
  - Changed to: `86/91`; owner/emitter remain [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md), and first-draft C++ is now populated.
  - Evidence: B001 resolved the shared submit callback signature, CRT `_wtol` conversion, opcode/subtype/action bytes, `PacketBufferWriteUInt32BE` big-endian amount writer, seven-byte queued send through `g_packetSender`, and raw-constructor caveat as class-level only.
- 2026-06-19 B012 source-quality recheck:
  - Kept `86/91`, owner/emitter [UID:00002O][ClanWidMoneyInputDialog](by-class/ClanWidMoneyInputDialog.md), and the existing first-draft C++.
  - Evidence: B012 confirmed the vtable-only dispatch route through slot `0x00615a94`, the shared `OnSubmitText(const wchar_t *)` source shape, opcode `0x4b`, subtype `0x0b`, action `1`, `_wtol` amount conversion, big-endian amount write, and seven-byte send.
  - Remaining compile-readiness caveat: the PacketBuffer writer argument order must be standardized across helper docs and sibling Clan submitter C++ before this generated source is treated as final.
