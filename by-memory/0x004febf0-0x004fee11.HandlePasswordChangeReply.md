*** UID:0002QD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace
{
bool IsPasswordChangeAccountNameReplyError(unsigned char result)
{
    switch (result) {
    case 3:
    case 4:
    case 14:
    case 16:
    case 17:
    case 18:
    case 20:
    case 21:
        return true;

    default:
        return false;
    }
}

bool IsPasswordChangeCurrentPasswordReplyError(unsigned char result)
{
    return result == 15;
}

bool IsPasswordChangeNewPasswordReplyError(unsigned char result)
{
    switch (result) {
    case 5:
    case 6:
    case 7:
    case 8:
    case 9:
        return true;

    default:
        return false;
    }
}

}

bool ChangePasswordDialogPane::HandlePasswordChangeReply(const unsigned char *packet)
{
    if (g_pScreenDimmer != NULL) {
        delete g_pScreenDimmer;
    }

    const unsigned char result = packet[1];
    const unsigned char messageLength = packet[2];
    char narrowMessage[0x100];
    wchar_t message[0x100];

    memmove(narrowMessage, packet + 3, messageLength);
    narrowMessage[messageLength] = 0;
    const int wideLength = MultiByteToWideChar(
        CP_ACP, 0, narrowMessage, messageLength, message, 0x100);
    if (wideLength >= 0x100)
        __report_rangecheckfailure();
    message[static_cast<unsigned char>(wideLength)] = L'\0';

    if (result != 0) {
        if (IsPasswordChangeAccountNameReplyError(result)) {
            SetHoverControl(3);
        } else if (IsPasswordChangeCurrentPasswordReplyError(result)) {
            SetHoverControl(4);
        } else if (IsPasswordChangeNewPasswordReplyError(result)) {
            SetHoverControl(5);
        }

        if (message[0] != L'\0')
            new AlertPane(message, this, L"OK", NULL);
        return true;
    }

    if (message[0] != L'\0')
        new AlertPane(message, this, L"OK", NULL);
    SlideCloseVertical();
    CloseDialog();
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready ChangePasswordDialogPane password-change reply method: dispatcher type-2 route, packet `[1]` result / `[2]` length / `[3]` message, `SetHoverControl(3/4/5)` error routing, optional `AlertPane` text, and success `SlideCloseVertical()` / `CloseDialog()` path. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004febf0-0x004fee11 - HandlePasswordChangeReply

## Status

- Disposition: reconstructable NexusTK login/account packet reply handler.
- Rebuild handling: source-authored helper with compiler-generated switch support immediately after the function body.
- Confidence: strong for owner, dispatcher xref, range, behavior, switch support table, packet layout, helper resolution, and formal source C++; current IDA names remain raw but source-facing method naming is evidence-backed.
- Owner/emitter: [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md).
- Parent aggregate: [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).

## Address Range

| Range | Kind | Notes |
| --- | --- | --- |
| `0x004febf0-0x004fee11` | method/helper | Current active MCP still names the function `sub_4FEBF0`, size `0x221` / 545 bytes (Verified with int_convert.py); source-facing owner/name is `ChangePasswordDialogPane::HandlePasswordChangeReply(const unsigned char *packet)`, handling packet type `2` replies dispatched from the password dialog. |
| `0x004fee11-0x004fee14` | following alignment | Three-byte alignment/no-op before local switch data. |
| `0x004fee14-0x004fee37` | following switch support data | Four-dword jump table `[0x004fee14,0x004fee24)` and 19-byte classification table `[0x004fee24,0x004fee37)`, referenced from `0x004fec64` and `0x004fec5d` inside this helper; recorded in [UID:0000VN][-ignored](by-memory/-ignored.md). |

## Behavior

Handles the password-change reply packet dispatched by the dialog. Current MCP proves packet layout `packet[1]` result/status byte, `packet[2]` ANSI message byte length, and `packet + 3` message payload. The method deletes the active `g_pScreenDimmer` object when present, maps selected nonzero result codes to the inherited `DialogPane::SetHoverControl(int)` control helper, decodes the length-prefixed multibyte server message into a `SimpleUString`, and displays nonempty text through `new AlertPane(message.c_str(), this, kDialogOkButtonText, 0)`. On success/result zero it displays the optional reply text, then calls `SlideCloseVertical()` and `CloseDialog()` before returning handled.

## Result-Code Handling

| Result code group | Action |
| --- | --- |
| `3`, `4`, `14`, `16`, `17`, `18`, `20`, `21` | Account-name reply error group. Source code uses `IsPasswordChangeAccountNameReplyError(result)` and calls `SetHoverControl(3)`. |
| `15` | Current-password reply error group. Source code uses `IsPasswordChangeCurrentPasswordReplyError(result)` and calls `SetHoverControl(4)`. |
| `5`, `6`, `7`, `8`, `9` | New-password reply error group. Source code uses `IsPasswordChangeNewPasswordReplyError(result)` and calls `SetHoverControl(5)`. |
| `0` | Success group. Shows optional message, calls `SlideCloseVertical()`, and then `CloseDialog()`. |

## Evidence Notes

- IDA MCP `lookup_funcs 0x004febf0` reports function size `0x221`, ending at `0x004fee11`.
- IDA MCP `xrefs_to 0x004febf0` reports a direct code xref at `0x004fe551` inside the local dispatcher [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md).
- IDA MCP `xrefs_to 0x004fee14` reports a data xref from `0x004fec64` inside this function, confirming the local switch-support table belongs to this handler.
- IDA MCP decompile shows `dword_69AE08` modal/dimmer cleanup, result-code switch handling, `sub_516220` message copy, `MultiByteToWideChar`, `sub_4F4AA0(624)` allocation, `sub_49FEB0` alert construction, and success close via `sub_49EB90`/`sub_49DAD0`.
- IDA MCP `callees 0x004febf0` reports the dialog close helpers, alert allocator/constructor, message conversion/copy helpers, range-check failure, and security-cookie check.
- 2026-06-28 B002 MCP recheck used live session `supervisor_20260628_resume`, active worker PID `5124`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, and Hex-Rays/string caches ready. Current `lookup_funcs` keeps `0x004febf0` as raw `sub_4FEBF0`, `0x004fee40` as raw `sub_4FEE40`, and `0x004fe530` as raw dispatcher `sub_4FE530`; therefore older saved-rename wording is superseded for the current active IDB, while the source-facing method names remain valid documentation/output names.
- B002 `analyze_function 0x004febf0` reports prototype `char __thiscall(int *this, int)`, size `0x221` / 545 bytes (Verified with int_convert.py), 25 basic blocks, complexity 9, and local `CHAR[256]` plus `WCHAR[256]` buffers. The method returns handled (`1`) for both success and error branches, supporting the formal `bool` source return used in analogous accepted login reply handlers.
- B002 `xrefs_to` reports exactly one code xref to the function start, `0x004fe551` inside dispatcher `0x004fe530`; `decompile 0x004fe530` confirms packet type `2` dispatches to this method and otherwise returns `0`.
- B002 switch-table checks report `xrefs_to 0x004fee14` at `0x004fec64` and `xrefs_to 0x004fee24` at `0x004fec5d`, both from this function. Current item/byte evidence corrects the old endpoint: alignment is `0F 1F 00` at `[0x004fee11,0x004fee14)`, switch support is exactly `[0x004fee14,0x004fee37)`, and nine `0xcc` bytes occupy `[0x004fee37,0x004fee40)`. The first four switch dwords decode to `0x004fec6b`, `0x004fec79`, `0x004fec72`, and `0x004fec86`; the following 19 bytes are `00 00 01 01 01 01 01 03 03 03 03 00 02 00 00 00 03 00 00`. Both items are compiler output for source-level switch logic, not source-authored data.
- B002 control-helper resolution decompiled `0x0049fc00`, the ChangePasswordDialogPane primary vtable slot `+0x58` target. It reads `DialogPane::m_controlManager` at `this+0x1fc` / 508 decimal and inherited `m_activeControlId` at `this+0x214` / 532 decimal (both Verified with int_convert.py), deactivates the old control, activates the new one, and stores the new control id. Current `DialogPane` docs expose this source-facing helper as `SetHoverControl(int controlId)`.
- B002 submit-path recheck of `0x004fe560` proves controls `3`, `4`, `5`, and `6` are read, controls `3/4/5` are sent as `accountName`, `currentPassword`, and `newPassword`, and control `6` is confirmation-only. This anchors the reply result groups to `SetHoverControl(3)`, `SetHoverControl(4)`, and `SetHoverControl(5)`.
- B002 helper-name resolution maps `dword_69AE08` to `g_pScreenDimmer`, `0x00613a18` to shared `kDialogOkButtonText`, `0x0049eb90` to `SlideCloseVertical()`, and `0x0049dad0` to `CloseDialog()`. Source-facing C++ must not retain `sub_`, `dword_`, `off_`, or decompiler temporaries for these helpers.
- 2026-06-16 C001 live IDA MCP refresh reconfirmed the active worker session `b001_mappane_0001AW_20260616`, `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, and stable targeted results while `auto_analysis_ready:false`.
- Historical `lookup_funcs` found the exact `0x004febf0-0x004fee11` function and non-function boundaries at `0x004fee11` and `0x004fee14`, but treated `0x004fee38` as a boundary. Current 2026-07-24 item/byte evidence supersedes that off-by-one split with class-table end `0x004fee37` and nine-byte padding through `0x004fee40`. A 2026-06-16 C001 pass reported saved descriptive IDA labels, but later current-IDB evidence again reports raw `sub_4FEBF0` / `sub_4FEE40` names.
- `analyze_function 0x004febf0` reports `25` basic blocks, cyclomatic complexity `9`, stack buffers `CHAR[256]` and `WCHAR[256]`, no listed ordinary callers but a direct code xref from `0x004fe551`, and the switch comments for cases `3,4,14,16-18,20,21`, case `15`, cases `5-9`, and default cases.
- `decompile 0x004fe530` confirms the dispatcher checks packet type `2` at `**(a1 + 12)` and calls this handler at `0x004fe551`; this is the active dispatcher reachability proof.
- `find_bytes` over little-endian pointers found no raw pointer to `0x004febf0`, but found the expected `0x004fee14` switch-table pointer only at `0x004fec67`, matching the decompiler's switch support data.
- Byte checks around `0x004febe0` reconfirm fourteen `0xcc` bytes before this prologue, and the following range still contains three-byte alignment plus the switch table and padding before `0x004fee40`.
- Decimal conversions for `0x221`, `0x26`, `0x58`, `0x270`, `0x100`, and `0xff` were rechecked with `tools/int_convert.py`.

## 2026-07-24 Current Container And IDA Handoff Recheck

- B002 re-read this exact function against live IDA MCP session `dc9b367a-4569-4166-8fa6-4d2bcb44d3d2`, IDB session `1ce787ed`, at shared-IDB checkpoint SHA-256 `F28C8B463F64693561E118790FD5E9D68739F85C697B9B58F391F56E1911ED99`. Later unrelated IDB saves may supersede the file checkpoint without changing the accepted range/body/route evidence.
- Exact current prestate: function `[0x004febf0,0x004fee11)`, name `sub_4FEBF0`, size `0x221`, type `char __thiscall(int *this, int)`, entry bytes `55 8B EC 6A FF 68 98 1C 60 00 64 A1 00 00 00 00`, and absent entry-regular, entry-repeatable, function-regular, and function-repeatable comments.
- The sole function-start xref remains `0x004fe551` from the dialog's packet-type-2 dispatcher. Switch data refs remain `0x004fec64 -> 0x004fee14` and `0x004fec5d -> 0x004fee24`; neither compiler table is part of the function range or handwritten source.
- Binary message handling is more explicit than the source abstraction: the method copies exactly the one-byte `packet[2]` ANSI length from `packet + 3`, terminates the ANSI buffer, calls `MultiByteToWideChar` with a 256-wide-character destination, and has an explicit `>= 256` conversion-result range-check failure path before terminating the wide buffer. The existing `SimpleUString::FromMbcString(packet + 3, packet[2])` helper expression is accepted human source shape only if that helper preserves these bounded conversion/failure semantics.
- Result, focus, alert, dimmer, success-close, score, formal, owner, and emitter decisions remain unchanged at the current `91/92` source-ready child state through UID0000I3. UID00022W is now only a false/non-emitting physical index and does not duplicate this method.
- Supervisor Gate 2B handoff, not applied by B002: rename to `ChangePasswordDialogPane__HandlePasswordChangeReply`, apply `bool __thiscall ChangePasswordDialogPane__HandlePasswordChangeReply(ChangePasswordDialogPane *this, const unsigned char *packet)`, and set only the function repeatable comment to `ChangePasswordDialogPane packet-type-2 reply handler: packet[1] result, packet[2] message length, packet+3 payload; focuses controls 3/4/5, alerts nonempty text, and closes on success.` Preserve exact range/bytes, sole xref, both table refs, and absent function-regular/entry-regular/entry-repeatable comments; do not extend the function into switch support.

## Touched State And Calls

| Reference | Role |
| --- | --- |
| `g_pScreenDimmer` / `0x0069ae08` | Active modal/dimmer object deleted before handling the reply when present. |
| `DialogPane::SetHoverControl(int)` / vtable slot `+0x58` / 88 decimal (Verified with int_convert.py) | Inherited active/hover control update for selected error codes: `3` account name, `4` current password, `5` new password. |
| `SimpleUString::FromMbcString(packet + 3, packet[2])` | Source-facing representation of copying `packet[2]` ANSI bytes from `packet + 3`, terminating a local 256-byte buffer, and converting into a 256-wide-character buffer. |
| `AlertPane` with `kDialogOkButtonText` | Allocates and constructs alert pane messages for nonempty reply text. Binary allocation is `0x270` / 624 decimal (Verified with int_convert.py), and `0x00613a18` is the shared UTF-16 `OK` label. |
| `SlideCloseVertical()` / `CloseDialog()` | Closes and dismisses the password dialog on success. |

## Reconstruction Notes

Model the switch support table as normal source-level `switch` logic. The bytes at `0x004fee14-0x004fee37` are compiler output and should stay in the ignored ledger rather than becoming source data.

Formal C++ is now present on this page and should emit through [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md). The prior blank-emitter blockers are resolved to implementation level: packet bytes are exact, result groups are represented by source-facing predicate helpers with numeric cases, `SetHoverControl(3/4/5)` is the inherited control helper, `g_pScreenDimmer`, `AlertPane`, `kDialogOkButtonText`, `SlideCloseVertical()`, and `CloseDialog()` are the accepted helper names, and the method shape is `bool ChangePasswordDialogPane::HandlePasswordChangeReply(const unsigned char *packet)`.

## Source-Quality Notes

- Owner/emitter decision: keep [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md). The dispatcher at `0x004fe530`, packet sender at `0x004fe9e0`, this reply handler, and the adjacent sibling handler are a coherent password-change packet-helper cluster under [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).
- Range/split decision: keep this page exact at `0x004febf0-0x004fee11`. The `0x004fee14-0x004fee37` switch support and surrounding `[0x004fee11,0x004fee14)` / `[0x004fee37,0x004fee40)` alignment remain compiler-generated/ignored spans, not source-owned child pages.
- Current-IDB naming decision: the source-facing method name `ChangePasswordDialogPane::HandlePasswordChangeReply` is evidence-backed by the dispatcher xref, owner docs, decompilation, and switch/message semantics, but current MCP session `supervisor_20260628_resume` still reports raw `sub_4FEBF0`. Do not cite a saved IDA rename as current proof unless a later IDA session confirms it again.
- Result-code naming decision: exact original protocol enum labels for codes `3,4,5,6,7,8,9,14,15,16,17,18,20,21` remain unrecovered, but that is not a formal-C++ blocker. The accepted source shape uses `IsPasswordChangeAccountNameReplyError`, `IsPasswordChangeCurrentPasswordReplyError`, and `IsPasswordChangeNewPasswordReplyError` predicates with numeric cases, matching existing accepted reply-handler style when symbolic server enum names are unavailable.
- Rejected owner/source alternatives remain rejected: Socket owns `g_packetSender`, PacketBuffer owns scalar writer helpers, DialogPane/AlertPane provide UI dependencies, MainMenuPane/OpenChangePasswordDialog launch the dialog, and RankingDialog references are generated-source pollution rather than ownership evidence. This method belongs to `ChangePasswordDialogPane`.

## Cross-References

- [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md)
- [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md)
- [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

### 2026-08-14 B003 exact reply formal

- The formal body now decodes the length-prefixed message locally with `memmove` and `MultiByteToWideChar(CP_ACP, ...)`, retains the explicit `0x100` range-check failure, uses literal `L"OK"`, and no longer depends on undocumented `SimpleUString`, `DecodePasswordChangeReplyMessage`, `ShowPasswordChangeReplyText`, or `kDialogOkButtonText` facades.
- Result routing remains exact: `3`, `4`, `14`, `16`, `17`, `18`, `20`, and `21` focus account control `3`; `15` focuses current-password control `4`; `5..9` focus new-password control `5`; nonempty messages show an alert; success also closes vertically and destroys the dialog.
- The member remains UID0000I3-owned at `91/92`, reached by the packet-type-2 edge `0x004fe551 -> 0x004febf0`. UID00001L owns the H declaration and all required C-runtime/Win32/Alert includes.

- 2026-07-24 B002 UID00022W aggregate implementation callback support sync:
  - Score/formal/owner/emitter remain `91/92`, existing source-ready member C++, and UID0000I3.
  - Corrected every stale switch/padding boundary to `[0x004fee14,0x004fee37)` / `[0x004fee37,0x004fee40)`; added exact current tinfo/xref/table facts, explicit primary conversion guard semantics, false-parent relationship, and complete supervisor-only IDA action/readback handoff.
- 2026-06-28 B002 accepted implementation callback:
  - Before: `86/91`, current page still had blank formal C++, stale "saved IDA name" proof, raw helper names in behavior prose, unresolved packet-enum/control/close-helper blockers, and the generated file still showed [UID:0002QD] as an empty emitter marker.
  - Changed to: `91/92`; owner/emitter remain [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md), reconstructable `TRUE`, formal `RECONSTRUCTION_CPP CODE` now contains `ChangePasswordDialogPane::HandlePasswordChangeReply(const unsigned char *packet)` plus local decode/result-group/show-message helpers.
  - Summary/evidence: B002 MCP session `supervisor_20260628_resume` reconfirmed the dispatcher xref at `0x004fe551`, raw current IDA names, exact `0x221` / 545-byte range, packet `[1]` result / `[2]` message length / `[3]` message payload layout, switch table xrefs at `0x004fec64` and `0x004fec5d`, `SetHoverControl(3/4/5)` vtable-slot resolution through `0x0049fc00`, `g_pScreenDimmer`, `kDialogOkButtonText`, `AlertPane`, `SlideCloseVertical()`, and `CloseDialog()` helper names, and the sibling [UID:0002QE][0x004fee40-0x004ff03f.HandlePasswordChangeReplySimple](by-memory/0x004fee40-0x004ff03f.HandlePasswordChangeReplySimple.md) no-direct-xref caveat.
  - Remaining cap: original symbolic result enum names and exact original helper-local names are inferred rather than proven, keeping the score below final audit while no longer blocking source emission.
- 2026-06-16 C001 safe IDA/evidence refresh:
  - Before: `80/88`, current IDA name `sub_4FEBF0`, strong high-level behavior but stale name/boundary/xref evidence.
  - Changed to: `86/91`; owner/emitter remain [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md), final C++ remains blank.
  - Summary/evidence: live IDA reconfirmed exact `0x221` / 545-byte (Verified with int_convert.py) range, dispatcher xref at `0x004fe551`, switch support xref at `0x004fec64`, no raw pointer to the function start, stack-buffer message decode, result-code switch groups, dimmer clear, alert construction, success close path, and saved IDA rename to `ChangePasswordDialogPane_HandlePasswordChangeReply`.
