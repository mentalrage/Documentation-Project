*** UID:00021V | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ClanDepMoneyInputDialog::ClanDepMoneyInputDialog(const unsigned char *packet)
    : ClanNameInputDialog(packet)
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact out-of-line ClanDepMoneyInputDialog packet constructor at 0x00488fa0-0x00488fd5; 53-byte base-call/vtable-store body emits through this child, while no-function/no-xref/no-pointer evidence remains an entry-route confidence caveat and protected IDA state. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00488fa0-0x00488fd5 ClanDepMoneyInputDialogRawConstructor

## Current Source Definition (2026-08-14)

- This exact child emits `ClanDepMoneyInputDialog::ClanDepMoneyInputDialog(const unsigned char *packet) : ClanNameInputDialog(packet) {}`.
- The 53-byte range saves `this`, forwards one packet argument at call `0x00488fad`, stores the three class vtables at `0x00488fb2/0x00488fba/0x00488fc4`, returns `this`, and uses `retn 4` before eleven bytes of alignment.
- Zero inbound start xrefs and pointer/immediate routes do not erase retained executable bytes or convert the definition to inline-only/no-code. They remain an entry-route confidence cap and IDA P07 protection.
- Earlier instructions in this file to omit or marker-cover the constructor are retained only as superseded history. Current metadata is `92/93`; owner/emitter remain UID00002A.

## Prior Evidence Status (Source Disposition Superseded Above)

- Disposition: reconstructable NexusTK project code, but with reachability caveat
- Entity kind: raw constructor-shaped bytes
- Current IDA function status: not a function object
- Likely source module: [UID:0000I8][Clan](by-file/Clan.md)
- Owner class: [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md)
- Reconstruction parent: [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md)
- Rebuild handling: `source-authored`
- Confidence: strong for constructor shape, exact bytes, and vtable stores; capped below final-source quality because IDA still reports no function object, no inbound xrefs, and no raw pointer matches for the constructor start.

## Covered Range

| Range | Entity | Notes |
| --- | --- | --- |
| `0x00488fa0-0x00488fd5` | `ClanDepMoneyInputDialog` raw constructor body | Forwards a `const unsigned char *packet` argument to [UID:00010J][0x00488b40-0x00488fa0.ClanNameInputDialogCore](by-memory/0x00488b40-0x00488fa0.ClanNameInputDialogCore.md) base constructor `0x00488b40`, then installs the derived primary and secondary/tertiary vtables. |

## Behavior

The bytes at `0x00488fa0` have the standard derived-constructor shape for the clan one-field dialog family:

1. Save `this`.
2. Forward the constructor packet pointer argument to [UID:00010J][0x00488b40-0x00488fa0.ClanNameInputDialogCore](by-memory/0x00488b40-0x00488fa0.ClanNameInputDialogCore.md) `ClanNameInputDialog::ClanNameInputDialog`.
3. Store the `ClanDepMoneyInputDialog` primary vtable at receiver offset `+0x0`.
4. Store secondary vtables at receiver offsets `+0xa0` and `+0xa4`.
5. Return `this`.

IDA does not define this as a function and the 2026-06-05 live checks found no inbound xrefs, pointer-byte matches, immediate matches, or data-reference matches for `0x00488fa0`. B004's local PE/Capstone scan also found zero loaded VA, RVA, or rel32 branch hits to `0x00488fa0` or the padding boundary `0x00488fd5`. Keep this page as raw constructor-shaped source evidence and do not treat it as a normal reachable function until additional xref evidence proves it.

## Constructor Argument

The single stack argument is best documented as `const unsigned char *packet`, or an eventual clan packet wrapper type, not an integer. This raw body forwards `[ebp+8]` unchanged into `ClanNameInputDialog::ClanNameInputDialog`; the base constructor reads `packet[3]` as prompt byte length and `packet+4` as counted CP_ACP prompt bytes.

The earlier prose-only/no-body conclusion is superseded. This exact child now carries the formal constructor definition. The raw start remains no-route, while active opener sites still corroborate the same base call and vtable stores.

## IDA MCP Evidence

2026-07-05 B009 current MCP refresh against active session `supervisor_recovery_20260705`:

- `idb_list` reported one active adopted worker session for `NexusTK.exe.i64`, not analyzing, and `server_health` reported status `ok`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` reconfirmed `0x00488fa0` and `0x00488fd5` are not function objects. It also reported the predecessor `sub_488F70` at `0x00488f70`, size `0x30`, ending at `0x00488fa0`, and the adjacent submitter as current active-IDB `sub_488FE0` at `0x00488fe0`, size `0x82`.
- Bounded `get_bytes` and `insn_query` reconfirmed the `0x35` / 53-byte constructor-shaped body, the base-constructor call at `0x00488fad`, raw vtable stores at `0x00488fb2`, `0x00488fba`, and `0x00488fc4`, `retn 4` at `0x00488fd2`, and eleven `0xcc` bytes at `0x00488fd5-0x00488fe0`.
- `xrefs_to(0x00488fa0)` and `xrefs_to(0x00488fd5)` returned zero xrefs. `find_bytes` returned zero matches for little-endian loaded-VA bytes `a0 8f 48 00` and RVA bytes `a0 7f 08 00` for `0x00488fa0`.
- `xrefs_to` the three `ClanDepMoneyInputDialog` vtable bases reconfirmed raw constructor store sites plus live inline construction sites at `0x0048511b` and `0x00487ddf`, each calling `sub_488B40` before stores at `0x00485120/0x00485126/0x00485130` and `0x00487de4/0x00487dea/0x00487df4`.
- Current active-IDB naming supersedes the old adjacent-submit rename note for present-state wording: the adjacent submitter currently resolves as `sub_488FE0`, even though historical C001 provenance below records a prior IDA rename attempt.

2026-06-15 C001 section-container child refresh against fresh IDA MCP session `c001_clan_raw_ctor_20260615`:

- `server_health` reports `NexusTK.exe.i64`, image base `0x00400000`, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` reconfirms `0x00488fa0` and `0x00488fd5` are not function objects; predecessor `sub_488F70` ends at `0x00488fa0`, and the adjacent submitter function starts at `0x00488fe0` with size `0x82`.
- `xrefs_to(0x00488fa0)` is still empty, and `find_bytes` for little-endian pointer bytes `a0 8f 48 00` returns zero matches. This MCP build no longer exposes the older immediate/data-reference search tool, so no new immediate/data-ref claim is added beyond the earlier 2026-06-05 result.
- `disasm(0x00488fa0)` reconfirms the base-constructor call at `0x00488fad`, `ClanDepMoneyInputDialog` vtable stores at `0x00488fb2`, `0x00488fba`, and `0x00488fc4`, `retn 4` at `0x00488fd2`, and `align 10h` before the adjacent submitter.
- `get_bytes` reconfirms the exact body bytes and eleven `0xcc` bytes at `0x00488fd5-0x00488fe0`.
- `xrefs_to` the vtable bases `0x00615998`, `0x006159fc`, and `0x00615a2c` reconfirms the raw constructor store sites plus two modeled family construction sites at `0x00485120/0x00485126/0x00485130` and `0x00487de4/0x00487dea/0x00487df4`.
- Historical IDA DB improvement: C001 recorded that the adjacent modeled submitter was renamed from `sub_488FE0` to `ClanDepMoneyInputDialog_SubmitDepositMoney` after a dry-run succeeded, with `lookup_funcs` verifying that name in that session and `idb_save` writing `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`. This is historical provenance only; the 2026-07-05 active MCP session `supervisor_recovery_20260705` currently resolves the adjacent submitter as `sub_488FE0`.

2026-06-19 B004 local PE/Capstone source-quality recheck:

- Local section-mapped scan found zero absolute VA hits, zero RVA hits, and zero rel32 `call`/`jmp`/conditional-branch hits to `0x00488fa0`.
- The same scan found zero absolute VA/RVA/rel32 hits to `0x00488fd5`, confirming the padding boundary is not a hidden entry point.
- Active inline construction sites remain `0x0048511b -> ClanNameInputDialog::ClanNameInputDialog` followed by `0x00485120/0x00485126/0x00485130` vtable stores, and `0x00487ddf -> ClanNameInputDialog::ClanNameInputDialog` followed by `0x00487de4/0x00487dea/0x00487df4` stores.
- Raw constructor stores remain `0x00488fb2` primary `0x00615998`, `0x00488fba` secondary `0x006159fc` at `+0xa0`, and `0x00488fc4` tertiary `0x00615a2c` at `+0xa4`.

2026-06-05 live IDA MCP raw-constructor refresh:

- `lookup_funcs(0x00488f9f)` resolves to the previous modeled function ending at `0x00488fa0`, while `lookup_funcs(0x00488fa0)` and `lookup_funcs(0x00488fd5)` both report no function object.
- `lookup_funcs(0x00488fe0)` reports the adjacent submitter as a modeled function at `0x00488fe0`, size `0x82`.
- `xrefs_to(0x00488fa0)` and `xrefs_to(0x00488fd5)` are empty.
- `find_bytes` for little-endian pointer bytes `a0 8f 48 00`, `search(type=immediate, target=0x00488fa0)`, and `search(type=data_ref, target=0x00488fa0)` all return zero matches.
- Disassembly at `0x00488fa0` shows a standard constructor-shaped prologue, a call to the shared base constructor at `0x00488b40`, vtable stores of `0x00615998`, `0x006159fc`, and `0x00615a2c`, and `retn 4` at `0x00488fd2`.
- `get_bytes(0x00488fa0, 0x40)` confirms the constructor body bytes through `0x00488fd5` followed by eleven `0xcc` padding bytes before the modeled submitter at `0x00488fe0`.
- `xrefs_to` the three vtable bases reports raw constructor store sites at `0x00488fb2`, `0x00488fba`, and `0x00488fc4`, plus shared class-family construction sites in the Clan dialog code at `0x00485120`/`0x00485126`/`0x00485130` and `0x00487de4`/`0x00487dea`/`0x00487df4`.

## Instruction Anchors

| Address | Evidence |
| --- | --- |
| `0x00488fa0` | Constructor-shaped prologue begins. |
| `0x00488fad` | Calls the shared `ClanNameInputDialog` base constructor at `0x00488b40`. |
| `0x00488fb2` | Stores primary `ClanDepMoneyInputDialog` vtable `0x00615998`. |
| `0x00488fba` | Stores secondary vtable `0x006159fc` at receiver offset `+0xa0`. |
| `0x00488fc4` | Stores tertiary vtable `0x00615a2c` at receiver offset `+0xa4`. |
| `0x00488fd2` | Returns `this` with single-argument stack cleanup. |
| `0x00488fd5-0x00488fe0` | Eleven `0xcc` alignment bytes before the submitter body. |

## Open Questions

- Determine whether the original source constructor was emitted but unreferenced, or whether this is a retained constructor-shaped fragment with no remaining live construction path.
- Recheck if future xref or class-layout work identifies an indirect construction path that bypasses IDA's current function model.
- IDA function creation remains intentionally skipped: the current database still has no function object, no start xrefs, and no raw pointer match for `0x00488fa0`; creating a function would improve display but would overstate reachability.

## Parent Attachment And Reconstruction Notes

- Attached to [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md) because both pages are above the parent-attachment gate and the constructor-shaped bytes install only `ClanDepMoneyInputDialog` vtables.
- Class identity is supported by the `ClanNameInputDialog` base call, the three derived vtable stores, and [UID:0001X9][ClanDialogVtableFamily](by-type/by-vtable/ClanDialogVtableFamily.md).
- Keep the exact formal packet-forwarding constructor definition. The byte range is bounded and reconstructable as source-owned constructor code; the absent IDA function object and inbound route remain a confidence caveat and P07 IDA protection, not a reason to blank the accepted body.
- Ownership/source placement decision: keep direct owner/emitter [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md) and the broader source-file route through [UID:0000I8][Clan](by-file/Clan.md) / `NexusTK/social/Clan.cpp`. [UID:0000IA][ClanDialogs](by-file/ClanDialogs.md) remains a non-emitting coordination/index page rather than a direct owner or alternate output destination; [UID:00010K][0x00488fe0-0x004895fe.ClanNameDialogSubmitters](by-memory/0x00488fe0-0x004895fe.ClanNameDialogSubmitters.md) remains a non-emitting aggregate/index, and [UID:00021I][0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers](by-memory/0x00485780-0x00485867.ClanDepositMoneyRequestAndSubmitPacketHelpers.md) remains a retained packet-helper mirror, not this constructor's owner.

## Cross-References

- [UID:0000I8][Clan](by-file/Clan.md)
- [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md)
- [UID:00010J][0x00488b40-0x00488fa0.ClanNameInputDialogCore](by-memory/0x00488b40-0x00488fa0.ClanNameInputDialogCore.md)
- [UID:00010K][0x00488fe0-0x004895fe.ClanNameDialogSubmitters](by-memory/0x00488fe0-0x004895fe.ClanNameDialogSubmitters.md)
- [UID:0001X9][ClanDialogVtableFamily](by-type/by-vtable/ClanDialogVtableFamily.md)

## Changes

All entries below are dated superseded history. Older scores, blank-C++ states, and marker-only/no-body policies do not describe the current `92/93` exact constructor emitter above.

- 2026-06-02 raw constructor cluster pass:
  - What existed before: `COMPLETION:66`, `CONFIDENCE:82`, and blank parent metadata.
  - Changed to: `COMPLETION:72`, `CONFIDENCE:84`, and `AUTOGEN_PARENT_UID:0000I8`.
  - Summary/evidence: attached to the Clan parent using the existing Clan source map, raw byte evidence, base constructor call, vtable-family addresses, and no-function/no-xref caveat. C++ remains blank under the 95+ final-source gate.
- 2026-06-05 live IDA refresh:
  - What existed before: `72/84`, file parent `0000I8`, stale generated-status wording, raw helper labels, receiver-offset wording, and stale manual coverage at `66%`.
  - Changed to: `80/88`, semantic class parent `00002A`, live IDA-only evidence, pointer-search proof, address-based instruction anchors, and synced manual coverage.
  - Summary/evidence: live IDA MCP reconfirmed the no-function constructor start, empty start/padding xrefs, zero pointer/immediate/data-ref matches for `0x00488fa0`, constructor-shaped bytes through `retn 4`, vtable stores at `0x00488fb2`/`0x00488fba`/`0x00488fc4`, eleven padding bytes before the submitter, and the explicit no-code rationale.
- 2026-06-15 C001 section-container child refresh:
  - What existed before: `80/88` with older raw-constructor evidence and no IDA-side name repair.
  - Changed to: `85/90`; owner/emitter route through [UID:00002A][ClanDepMoneyInputDialog](by-class/ClanDepMoneyInputDialog.md) remains unchanged and final C++ remains blank.
  - Summary/evidence: fresh IDA MCP session `c001_clan_raw_ctor_20260615` reconfirmed no function at the constructor start/end, no start xrefs, zero little-endian pointer-byte matches, exact constructor-shaped disassembly and bytes, eleven-byte padding before the submitter, and vtable-store xrefs. C001 also recorded an adjacent modeled submitter rename to `ClanDepMoneyInputDialog_SubmitDepositMoney` and saved the IDB; that name is now historical because B009 2026-07-05 active MCP resolves the adjacent submitter as `sub_488FE0`. The remaining blocker is true raw-constructor reachability, not documentation ownership.
- 2026-06-20 B004 source-quality support sync:
  - Added the source-facing packet-pointer argument decision, active inline construction site evidence, and local PE/Capstone zero VA/RVA/rel32 route proof for raw start `0x00488fa0` and padding boundary `0x00488fd5`. Formal C++ remains blank because the constructor body is source-shaped but not route-proven.
- 2026-07-05 B009 source-quality implementation:
  - Incorporated current MCP session `supervisor_recovery_20260705` as present-state provenance: healthy NexusTK IDB, no functions at `0x00488fa0`/`0x00488fd5`, predecessor `sub_488F70` ending at `0x00488fa0`, current adjacent submitter name `sub_488FE0` size `0x82`, exact `0x35` / 53-byte target body, eleven `0xcc` bytes at `0x00488fd5-0x00488fe0`, raw base-call/vtable-store anchors, zero xrefs to the start/boundary, zero VA/RVA pointer-pattern hits, live inline construction sites, and rejected alternate owners/source placements. Formal C++ and metadata remain unchanged.
