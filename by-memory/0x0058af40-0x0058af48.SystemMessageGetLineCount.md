*** UID:000246 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0004SZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0004SZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int SystemMessage::GetLineCount(GrafPort * /*drawContext*/, short /*width*/)
{
    return 1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact eight-byte `SystemMessage::GetLineCount` default returning one row for `(GrafPort *drawContext, short width)`, inherited by Header/Footer, with two vtable refs, Color comparator, RTTI base proof, and exact padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0058af40-0x0058af48 SystemMessage::GetLineCount

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: source-authored `SystemMessage` default virtual inherited by the fieldless Header/Footer marker classes.
- Current IDA name: `sub_58AF40`.
- Canonical owner/emitter: [UID:0004SZ][SystemMessage](by-class/SystemMessage.md), emitted through [UID:0000OE][SystemMessagePanes](by-file/SystemMessagePanes.md) at position 10.
- Confidence: very strong for range, bytes, behavior, ABI, boundary padding, vtable identity, `int` return, parameter order/types, class ownership, and source placement.
- C++ reconstruction: complete destination-specific body in the managed block. The former blank/comment-only state is preserved below as historical evidence, not current disposition.

## Covered Range

| Range | Current/generated name | Notes |
| --- | --- | --- |
| `0x0058af40-0x0058af48` | `sub_58AF40` | Returns constant `1` for the marker-entry line-count slot and consumes two stack arguments with `retn 8`. |

## Raw Code

```asm
0058af40  b8 01 00 00 00    mov eax, 1
0058af45  c2 08 00          retn 8
```

## Slot Semantics

The live vtable and caller evidence identifies this as the marker-entry implementation of the `+0x18` entry virtual slot. The same slot is [UID:000030][ColorStringSystemMessage](by-class/ColorStringSystemMessage.md)'s line-count helper at `0x00587ae0`, which computes the number of wrapped text lines using the owning pane/context and a width-like argument. Header and footer markers have no text payload, so their shared/folded implementation returns one line unconditionally.

The `retn 8` convention is not just an arbitrary stack cleanup. Indirect call sites pass:

- the owning `SystemMessagePane` or `OldSystemMessagePane` context as the first stack argument;
- a width/measurement argument such as `160` or `184` as the second stack argument.

These argument roles are behaviorally supported by `ColorStringSystemMessage`'s concrete slot body and system-message pane call sites. The source-facing declaration is resolved as `GetLineCount` with draw-context and signed-short width parameters; both are unused by this default implementation.

## Reconstruction Notes

The historical filename label `HeaderFooterSystemMessageDefaultTrueVirtual` was a constant-return placeholder, not a boolean-slot conclusion. Current evidence resolves the `+0x18` slot as the inherited `SystemMessage::GetLineCount` default. The UID-preserving path correction and the managed body supersede the old blank-C++ blocker without duplicating Header/Footer overrides.

## B011 Folded Virtual Disposition

Historical B011 report `0000OE-SystemMessagePanes-empty-emitter-family-source-quality.md` resolved this empty emitter with a formal source-disposition comment rather than a standalone helper body. That was reasonable before the concrete `SystemMessage` RTTI/base contract was recovered. The body is exact `mov eax, 1; retn 8`, size `0x8` / 8 bytes (verified with `int_convert.py`), has no callees, and has exactly two data refs from header/footer vtable cells `0x0062d6c4` and `0x0062d6e8`. There are no direct code callers, address-taken refs outside those vtable cells, or evidence for a source-level free function name.

Current source disposition is class-facing and resolved: [UID:0004SZ][SystemMessage](by-class/SystemMessage.md) owns the one default body; [UID:000061][HeaderSystemMessage](by-class/HeaderSystemMessage.md) and [UID:00005D][FooterSystemMessage](by-class/FooterSystemMessage.md) inherit it; [UID:000030][ColorStringSystemMessage](by-class/ColorStringSystemMessage.md) supplies its separate stateful override. No free helper or duplicate concrete override is emitted.

## IDA MCP Evidence

Checked on 2026-05-28:

- IDA reports `sub_58AF40` as `0x0058af40-0x0058af48`.
- Data xrefs land at `0x0062d6c4` in `HeaderSystemMessage` vtable data and `0x0062d6e8` in `FooterSystemMessage` vtable data.
- The function has no outgoing references and no observed code callers beyond the vtable slots.
- Bytes before it are `0xcc` alignment after `SystemMessagePane` scalar deleting destructor; bytes after it are `0xcc` alignment before `TerminalPane`.
- [UID:00026M][0x0062d5f8-0x0062da10.SystemMessageReadOnlyData](by-memory/0x0062d5f8-0x0062da10.SystemMessageReadOnlyData.md) records the `HeaderSystemMessage`, `FooterSystemMessage`, and `ColorStringSystemMessage` vtable block at `0x0062d6ac-0x0062d6f4`.
- [UID:000061][HeaderSystemMessage](by-class/HeaderSystemMessage.md) records `0x0058af40` as the additional shared helper in the header marker vtable.
- [UID:00005D][FooterSystemMessage](by-class/FooterSystemMessage.md) records the same shared helper family for the footer marker vtable.
- 2026-06-16 C001 live IDA MCP session `a001_goal2_00023O` reports server health `ok`, Hex-Rays ready, strings cache ready, `auto_analysis_ready:false`, and IDB path `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`.
- `lookup_funcs` reports `0x0058af40` as `sub_58AF40` size `0x8`; the predecessor `0x0058af00` is `sub_58AF00` size `0x3b`, and the successor `0x0058af50` is `sub_58AF50` size `0x19e`. `0x0058af3b` and `0x0058af48` are not function starts.
- Raw bytes from `0x0058af30` confirm the predecessor tail ends with `c2 04 00` at `0x0058af38`, followed by five `0xcc` bytes at `0x0058af3b-0x0058af40`, this helper bytes `b8 01 00 00 00 c2 08 00`, eight `0xcc` bytes at `0x0058af48-0x0058af50`, and the successor prologue at `0x0058af50`.
- Current decompilation is `int __stdcall sub_58AF40(int a1, int a2) { return 1; }`; current disassembly has exactly two instructions, `mov eax, 1` and `retn 8`.
- `xrefs_to 0x0058af40` reports exactly two data refs, the `HeaderSystemMessage` slot at `0x0062d6c4` and the `FooterSystemMessage` slot at `0x0062d6e8`; `callees 0x0058af40` reports no targets.
- Raw bytes at `0x0062d6a8-0x0062d6f0` confirm the header slot dword `40 af 58 00` at `0x0062d6c4` and the footer slot dword `40 af 58 00` at `0x0062d6e8`. `find_bytes` finds the absolute pointer pattern `40 AF 58 00` only at those two vtable cells and finds no RVA pattern `40 AF 18 00`.
- `entity_query` over the vtable island confirms `??_7HeaderSystemMessage@@6B@` at `0x0062d6ac`, `??_7FooterSystemMessage@@6B@` at `0x0062d6d0`, and `??_7ColorStringSystemMessage@@6B@` at `0x0062d6f4`; RTTI names include `??_R4HeaderSystemMessage@@6B@` at `0x00650b84` and `??_R4FooterSystemMessage@@6B@` at `0x00650bd4`.
- `xrefs_to` on the two vtable heads reports 14 refs each, matching the class support pages. `xrefs_to` on the individual slot cells reports no inbound refs, so the only direct evidence for the function is vtable use rather than ordinary direct callers.
- Slot-semantics check: `ColorStringSystemMessage` slot `+0x18` is `0x00587ae0`, which calls `0x004bb070` with the entry text, text length, pane/context argument, and width-like argument before clamping to at least one line. `search_text` over `0x00584ea0-0x0058af50` finds 16 local uses of `+18h`, including total-height and paint/layout paths that call entries with pane/context plus width values `160` or `184`.
- C001 appended safe IDA comments at `0x0058af40`, `0x0062d6c4`, and `0x0062d6e8`; `idb_save` returned `ok:true` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`.

## Accepted Source Closure

- Exact identity: `[0x0058af40,0x0058af48)` is eight bytes with SHA256
  `A37F20F22731FA44D5FE0925CE76FBFB5BF2C8A56F260E67AA2F887A08D7F326`.
  Its two instructions are `mov eax, 1` and `retn 8`; the CFG is one block,
  complexity one, with no callees, no ordinary direct callers, and no failure
  path or state mutation.
- ABI: the hidden `this` pointer identifies a member body. The two explicit
  stack slots are the pane/draw context and width parameters. Color's same
  virtual slot sign-extends the second argument from 16 bits, fixing the width
  type as signed `short`; its count calculation and consumers fix the return
  type as `int` rather than `bool` or `short`.
- Direct references: the only absolute pointer hits are Header slot
  `0x0062d6c4` and Footer slot `0x0062d6e8`; there is no RVA hit. Color's
  corresponding `+0x18` slot points to its separate count override at
  `0x00587ae0`. Pane consumers pass widths 160 or 184, and old-pane consumers
  pass 184, while all supply the pane/draw context first.
- Class model: RTTI hierarchy arrays for Header, Footer, and Color insert a
  real `SystemMessage` base between each concrete class and `LObject`. Header
  and Footer are fieldless four-byte objects; Color is a 16-byte object with
  owned wide text, color, and style state. No concrete SystemMessage vtable or
  allocation was found, so the base is abstract and fieldless.
- Minimum override model: SystemMessage supplies both false marker predicates,
  the default one-line count, and no-op draw while leaving clone pure. Header
  overrides only the header predicate and clone; Footer overrides only the
  footer predicate and clone; Color inherits both false predicates and
  overrides clone, count, and draw. This regenerates the observed shared/folded
  helpers and exact concrete vtables without duplicate authored bodies.
- Boundary ownership: five `0xcc` bytes at
  `[0x0058af3b,0x0058af40)` and eight at
  `[0x0058af48,0x0058af50)` remain ignored padding under UID0000VN. Neither
  padding span is absorbed into this exact executable range.
- Source placement: the method is emitted by
  [UID:0004SZ][SystemMessage](by-class/SystemMessage.md) at position 10 in
  `NexusTK/social/SystemMessagePanes.cpp`; UID0000OE remains the file-level
  owner/emitter. Header-only, Footer-only, duplicate-folded, free-helper,
  width-only, and Terminal/Chatting source routes are rejected by the common
  RTTI base, vtable parity, call shape, and source-family inventory.
- Compiler exclusions: vtable/RTTI arrays, explicit vptr stores, deleting
  destructors, adjustor thunks, and absolute-address tables are compiler ABI
  output and are not handwritten. UID0001GG, UID0000WM, and UID0001B5 retain
  their established folded/default/compiler routes.
- Score closure: `92/94` reflects exact range, bytes, hash, CFG, ABI, slot
  identity, parameter order/types, callers, ownership, source placement,
  destination body, and compiler boundary. Remaining uncertainty is limited to
  unrecovered original parameter spelling, represented safely by unused names
  in this definition.

## Ownership Decision

Keep this with the `SystemMessagePanes.cpp` marker-entry family. It is not terminal-pane code even though it sits immediately before `TerminalPane` in `.text`.

The direct canonical owner/emitter is now
[UID:0004SZ][SystemMessage](by-class/SystemMessage.md), while
[UID:0000OE][SystemMessagePanes](by-file/SystemMessagePanes.md) remains the
file-level source owner/emitter. Do not move the method to only
[UID:000061][HeaderSystemMessage](by-class/HeaderSystemMessage.md) or only
[UID:00005D][FooterSystemMessage](by-class/FooterSystemMessage.md): their two
vtable cells share this inherited base body, and duplicate concrete definitions
would add source not required by the minimum-override model.

Keep the neighboring padding ranges in [UID:0000VN][-ignored](by-memory/-ignored.md): `0x0058af3b-0x0058af40` before this helper and `0x0058af48-0x0058af50` after it.

## Cross-References

- [UID:0000OE][SystemMessagePanes](by-file/SystemMessagePanes.md)
- [UID:0004SZ][SystemMessage](by-class/SystemMessage.md)
- [UID:000061][HeaderSystemMessage](by-class/HeaderSystemMessage.md)
- [UID:00005D][FooterSystemMessage](by-class/FooterSystemMessage.md)
- [UID:000030][ColorStringSystemMessage](by-class/ColorStringSystemMessage.md)
- [UID:0001J6][0x00584ea0-0x0058af3b.SystemMessagePanes](by-memory/0x00584ea0-0x0058af3b.SystemMessagePanes.md)
- [UID:00026M][0x0062d5f8-0x0062da10.SystemMessageReadOnlyData](by-memory/0x0062d5f8-0x0062da10.SystemMessageReadOnlyData.md)
- [UID:0003GD][0x0062d6a8-0x0062d6cc.HeaderSystemMessageVtableData](by-memory/0x0062d6a8-0x0062d6cc.HeaderSystemMessageVtableData.md)
- [UID:0003GE][0x0062d6cc-0x0062d6f0.FooterSystemMessageVtableData](by-memory/0x0062d6cc-0x0062d6f0.FooterSystemMessageVtableData.md)
- [UID:0003GF][0x0062d6f0-0x0062d714.ColorStringSystemMessageVtableData](by-memory/0x0062d6f0-0x0062d714.ColorStringSystemMessageVtableData.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-19 B002 accepted UID000246 callback:
  - Validator-supported UID-preserving path correction retained UID000246 and
    exact `[0x0058af40,0x0058af48)` bounds.
  - Raised `86/90` to `92/94`, assigned direct class owner/emitter UID0004SZ,
    set source position 10, and installed the exact inherited default body.
  - Resolved method name, `int` return, `GrafPort *`/signed-short argument
    contract, RTTI base ownership, minimum override model, padding, caller
    widths, compiler exclusions, rejected alternatives, and score rationale.
- 2026-06-30 B011 accepted empty-emitter implementation:
  - Inserted formal folded-virtual disposition comment into `RECONSTRUCTION_CPP CODE`.
  - Added no-standalone proof and future Header/Footer class-representation plan for the `+0x18` marker line-count virtual.
- 2026-06-01: Raised from `50/65` to `82/86`, marked reconstructable, and attached to [UID:0000OE][SystemMessagePanes](by-file/SystemMessagePanes.md).
  - Added parent-attachment rationale, linked read-only-data/class evidence, and recorded the surrounding padding ownership.
  - Removed the provisional C++ sketch from the body because final virtual-slot names remain below the 95% reconstruction threshold.
- 2026-06-16 C001 direct-IDA refresh:
  - Before: `82/86`, with the helper described as a default true virtual and the two `retn 8` stack arguments unresolved.
  - After: `86/90`, retaining [UID:0000OE][SystemMessagePanes](by-file/SystemMessagePanes.md) as owner/emitter and keeping final C++ blank.
  - Summary/evidence: live IDA MCP confirmed exact `0x8` function bounds, predecessor/successor padding, raw bytes, no callees, only two data refs from `0x0062d6c4` and `0x0062d6e8`, exact header/footer vtable bytes, no RVA pointer hits, `ColorStringSystemMessage` slot `+0x18` line-count behavior, 16 local indirect `+18h` uses, saved IDA comments, and unresolved final source method spelling/folded-override treatment.
