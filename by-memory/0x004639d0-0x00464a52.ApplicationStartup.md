*** UID:0002H0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000D | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// No first-draft body yet: Application::Startup is a 0x1082-byte startup
// orchestrator whose fatal, patcher, Win32Error, DAT-load, window-creation,
// and singleton-construction branches still require branch-level source proof.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact Application::Startup base virtual in primary vftable slot 3; BaramApp::Startup directly delegates here at 0x004f5fd7, while the preserved 0x1082-byte startup behavior and branch-level no-code proof prevent an unsafe partial body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004639d0-0x00464a52 Application Startup

## UID0003OD Source-Identity Correction - 2026-08-12

[UID:0003OD][0x006125e4-0x0061260c.ApplicationVtableData](by-memory/0x006125e4-0x0061260c.ApplicationVtableData.md) proves this address is `Application::Startup`, the Application primary-vftable slot-3 base virtual. [UID:0002H7][0x004f5fb0-0x004f6490.BaramAppStartup](by-memory/0x004f5fb0-0x004f6490.BaramAppStartup.md) occupies derived BaramApp slot 3 and calls this base implementation at `0x004f5fd7`; the caller ignores EAX and continues game-client startup. The source-facing signature is therefore `void Application::Startup()`.

The former filename/title/name `ApplicationInitialize` / `Application::Initialize` is retained below only as historical reconstruction terminology from the earlier B006 pass. The correction changes identity and source relationship, not range, bytes, caller, 162-block/complexity evidence, startup phase order, failure behavior, score, owner/emitter, or the accepted no-code proof. Formal H remains blank because this method page does not own an isolated class declaration.

## Scope

- Range: `0x004639d0-0x00464a52`
- Entity kind: class method
- Owner: [UID:00000D][Application](by-class/Application.md)
- Parent aggregate: [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md)
- Disposition: reconstructable project code.
- C++ status: formal comment-only no-code marker. The method is a real Application emitter, but no full, partial, or skeleton behavioral `Application::Startup` body is accepted until branch/failure-path reconstruction is source-quality.

## Score Rationale

| Field | Value | Reason |
| --- | ---: | --- |
| Completion | 86 | The page records exact bounds, parent attachment, startup responsibility, accepted B006 MCP evidence from session `c9671f69`, caller/ignored-return proof, startup phase order, DAT archive families, nine fatal helper sites, patcher and throw paths, inferred field/global names, and an accepted formal no-code marker. It remains below final because no behavioral source body is safe yet. |
| Confidence | 90 | Ownership, range, source placement, caller evidence, Application support docs, startup resource evidence, generated empty-emitter state, and accepted B006 MCP corroboration agree. Confidence is capped because final source-facing field/helper/global declarations and branch-level source code remain reconstructed rather than emitted. |

## Evidence Map

| Evidence | What it supports | Remaining caveat |
| --- | --- | --- |
| [UID:0000HG][Application](by-file/Application.md) | Source placement under `NexusTK/app/` and detailed startup singleton/resource responsibilities. | Exact source rewrite still needs branch-order review. |
| [UID:0000YR][0x00463310-0x004674ed.ApplicationLifecycle](by-memory/0x00463310-0x004674ed.ApplicationLifecycle.md) | Lifecycle containment and neighboring method context. | Aggregate page is not a substitute for this method's full body documentation. |
| [UID:0001R6][application-startup-dat-archives](by-resource/application-startup-dat-archives.md) | Required/optional DAT archive inventory loaded by startup. | Resource load failure branches still need full local sequence mapping. |
| [UID:0000YX][0x00467410-0x004674ed.LoadIndexedDATSeries](by-memory/0x00467410-0x004674ed.LoadIndexedDATSeries.md) | Numbered DAT-series helper called from initialization. | Helper body belongs to its own page; do not inline it here. |
| [UID:0000HR][BlackHole](by-file/BlackHole.md), [UID:0000J8][ExceptionHandler](by-file/ExceptionHandler.md), [UID:0000II][Crasher](by-file/Crasher.md), and manager singleton pages | Startup-owned singleton construction responsibilities. | Concrete manager implementations remain outside `Application.cpp`. |

## Behavior

Initializes the main client process: sets lifecycle/network state, initializes keyboard speed handling, checks patch-continuation files, resolves display mode from registry or DirectDraw probing, creates the single-instance mutex, loads required and optional startup DAT resources, creates the loading dialog and main Win32 window, seeds runtime process state, and constructs the major startup singleton managers.

## Accepted B006 MCP Evidence

- B006 implementation callback incorporates the accepted report `tools/leaser/Agents/Agent-B006/research/0002H0-ApplicationInitialize-empty-emitter-source-quality.md`, using accepted IDA MCP session `c9671f69` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `19076`. `server_health(database=c9671f69)` was OK with auto-analysis, Hex-Rays, and strings cache ready. Supervisor later reported `c9671f69` stale after this evidence was accepted; no fresh MCP-backed verification was needed during the final provenance-wording/validator batch.
- `lookup_funcs(0x004639d0)` reports `sub_4639D0` with size `0x1082` / `4226` bytes; `lookup_funcs(0x00464a52)` reports no function; `lookup_funcs(0x00464a60)` reports successor cleanup method `sub_464A60` with size `0x261`.
- `analyze_function(0x004639d0)` reports 162 basic blocks, cyclomatic complexity `31`, a truncated decompile, and startup strings `Nexon.SingleInstance`, `Global\Nexon.SingleInstance`, `runas`, and `patcher.exe`.
- `xrefs_to(0x004639d0)` reports one direct code caller at `0x004f5fd7` inside [UID:0002H7][0x004f5fb0-0x004f6490.BaramAppStartup](by-memory/0x004f5fb0-0x004f6490.BaramAppStartup.md) and one data xref at `0x006125f0`. The data xref is Application vtable/data-slot evidence, not a second runtime caller.
- The `0x004f5fb0` BaramApp override immediately continues startup after `call sub_4639D0` and does not test or use EAX. The source-facing signature is `void Application::Startup()` when a behavioral body is eventually emitted; IDA's `HANDLE *`/pointer-shaped decompiler return is not source proof.
- Existing file/class documentation ties call sites inside this method to `KeySpeedMgr`, `LanguageMan`, `ChangeMan`, `ImageLib`, `FontImageLib`, `BlackHole`, `InputMan`, `ExceptionHandler`, `Crasher`, `MiscWorkThread`, `CheatDetector`, DirectDraw initialization, and [UID:0001R6][application-startup-dat-archives](by-resource/application-startup-dat-archives.md).
- B002's older `b002_ranking_0001ZC_20260616` evidence and Wave2/prewave exports remain useful historical leads because they agree on the same range, size, caller/callee shape, and startup phase order. The previous note that live MCP was unreachable is stale for this page and is superseded by the accepted B006 session `c9671f69`.
- Read-only generated `auto-generated/NexusTK/app/Application.cpp` at report time had header `validator-command-id: 000000000674`, `validator-refreshed-at: 2026-06-29T11:40:18-04:00`, and still rendered UID0002H0 as `Completion:82 | Confidence:88 | Empty Emitter Marker`. That was generated-output evidence of the blank formal block, not a reason to clear Application ownership.

## Startup Phase Snapshot

| Order | Phase | Evidence / effect |
| ---: | --- | --- |
| 1 | Lifecycle and network bootstrap | Writes startup phase `1`, records current thread id at `this+0x848`, runs `WSAStartup(0x101)`, and records whether hostname resolution succeeds at `this+0x84e`. |
| 2 | Keyboard and license setup | Allocates/constructs `KeySpeedMgr`, loads/restores keyboard-speed state through the `0x004eff00/0x004effa0` family, then calls `Application::ValidateLicense` at `0x00465430`. |
| 3 | Patch-continuation handoff | Opens `Patch\\Info` and `Patch\\Script`; if both exist, shows "Continuing patch...", runs `patcher.exe` with `runas`, and exits. |
| 4 | Display configuration | Reads `DisplayMode`, `WindowWidth`, and `DrawType` under `Software\\KRU\\NexusTK`; if no registry mode exists on NT, probes DirectDraw display mode and may persist an auto-selected display mode. |
| 5 | Windowed/fullscreen globals | Clamps the windowed width to `1024..3840`, aligns it to four pixels, writes display-mode globals, and validates `DrawType` as values `1..3`. |
| 6 | Process singleton guard | Uses `GetVersionExA` to choose `Global\\Nexon.SingleInstance` on NT or `Nexon.SingleInstance` otherwise, then stores the mutex handle at `this+0x24`. |
| 7 | Exception and DAT resource bootstrap | Constructs `ExceptionHandler`; loads required DAT files `MISC`, `CHAR`, `TILE`, `MON`, `MNM`, `BARAM`, `BINT0..2`, and the readonly-data-backed DAT names, with `FatalError("File not found : %s")` on required failures. |
| 8 | Indexed DAT families | Calls [UID:0000YX][0x00467410-0x004674ed.LoadIndexedDATSeries](by-memory/0x00467410-0x004674ed.LoadIndexedDATSeries.md) for face/hair/body/weapon/tile/equipment families; stores the optional sound-DAT result at `this+0x84c` and probes `MUS000` variants from `000` through `999`. |
| 9 | Language/config and loading dialog | Constructs `LanguageMan` and `RegistryConfig`, initializes config persistence, creates loading dialog resource `0x7d`, sets tool-window extended style, centers it, shows it, and updates it. |
| 10 | Main window registration and creation | Builds the class name from the executable/build strings, registers a `WNDCLASSW` with window proc `0x00465f20`, creates fullscreen or configured windowed main window, and records client-origin/client-size fields for the windowed path. |
| 11 | Runtime process state | Seeds `srand(time64())`, calls `GlobalMemoryStatus`, sets process working-set size, and creates the shutdown event handle at `this+0x83c`. |
| 12 | Manager singleton construction | Allocates/constructs `ChangeMan`, `ImageLib`, `FontImageLib`, image-group/BlackHole-adjacent helper, `InputMan`, `DirectX`, `ScreenPane`, `EventDispatcher`, `EventMan`, `Socket`, `SoundManager`, `MidiPlayer`, `Crasher`, `MiscWorkThread`, `CheatDetector`, and `FileDownloader`. |

## Failure Paths And Source-Body Blockers

| Path | Current evidence | Source-quality disposition |
| --- | --- | --- |
| Required-resource fatal handling | Accepted B006 MCP evidence found fatal helper `sub_465CB0` calls at `0x4647b2`, `0x46488a`, `0x4648b7`, `0x4648e4`, `0x464911`, `0x46493e`, `0x46496b`, `0x464998`, and `0x4649c5`. | These are exact behavior sites and must be represented before any behavioral C++ body is safe. They are not placeholders for a generic missing-resource return. |
| Patcher continuation | The patch branch prepares `SHELLEXECUTEINFOA` with `lpVerb="runas"`, `lpFile="patcher.exe"`, `lpParameters=Locale`, `nShow=1`, and calls `ShellExecuteExA` at `0x464855`. | Preserve as exact elevated patcher handoff/exit behavior; do not replace with a generic updater helper. |
| Win32Error throw paths | Tail paths allocate a small exception object through `sub_4A60D0` and call `__CxxThrowException@8` at `0x464a09` and `0x464a4d`. | These throw paths remain source-body blockers because their object type/lifetime and branch triggers must be reconstructed exactly. |
| Compiler EH cleanup | Disassembly shows unwind cleanup fragments outside the normal body with destructor/free helper calls. | Treat as compiler machinery; do not create artificial child source targets from the EH fragments. |

## Source-Facing Names And Uncertainty

- `Application+0x848` is best documented as inferred `m_startupThreadId` / `m_mainThreadId`; direct evidence is the early `GetCurrentThreadId()` store.
- `Application+0x84e` is best documented as inferred `m_hostnameResolved` / `m_hostLookupSucceeded`; direct evidence is hostname lookup success/failure storage after `gethostname` / `gethostbyname`.
- `Application+0x84c` is best documented as inferred `m_soundArchiveLoadResult` / `m_soundDatLoaded`; direct evidence is optional `DATA/SND.DAT` load result storage. Exact boolean/result semantics remain a source-body confidence cap.
- `Application+0x24` remains the single-instance mutex handle; direct evidence is `CreateMutexA` storage after choosing `Nexon.SingleInstance` or `Global\Nexon.SingleInstance`.
- `0x0067a938` is a startup/lifecycle phase flag; direct evidence is the early write of value `1`.
- Display globals around `0x0067a93c`, `0x0066d408`, and `0x0066d40c` are display mode/window sizing state. Keep their exact source-facing names aligned with their own display-global pages rather than inventing UID0002H0-only spellings.

## No-Code Disposition Comparison

| Disposition | Decision | Reason |
| --- | --- | --- |
| Full first-draft `Application::Startup` body | Rejected for this pass. | Current evidence proves route and phase order, but not complete branch-level source. Missing exact failure or throw ordering would alter patcher continuation, required DAT fatal handling, Win32Error throw paths, window setup, singleton construction, and startup state. |
| Partial/skeleton first-draft body | Rejected more strongly than full-body deferral. | `ApplicationStartup` calls this method at `0x004f5fd7` without a return-value guard. A partial/no-op/skeleton body would compile as executable reconstruction while silently omitting networking, license, mutex/window, DAT, shutdown-event, and manager side effects. |
| Comment-only formal marker | Accepted. | Keeps the proven Application emitter route, clears the unexplained generated empty-emitter state, and emits no behavioral C++ for unsafe branches. |
| Clear `EMITTER_UIDS:` / make non-emitting | Rejected. | Application ownership and emitter route are proven by target metadata, Application class/file docs, caller route, and vtable/data evidence. Clearing the emitter would misclassify a real source method. |
| Keep blank formal C++ with nonblank emitter | Rejected. | This was the generated-output failure mode: it left `auto-generated/NexusTK/app/Application.cpp` with an unexplained `Empty Emitter Marker` and no durable no-code proof. |

## Range And Split Decision

- Keep the exact range `0x004639d0-0x00464a52`; `0x00464a52` is not a function, and `0x00464a60` starts the successor cleanup method.
- Do not create by-memory child pages for internal startup phases. Accepted B006 MCP evidence proves one real method with one code caller, not a mixed range containing separable custom functions.
- Document internal phases and failure paths in this page until exact callable helpers exist. EH/unwind cleanup remains compiler-generated support, not source children.

## Reconstruction Notes

- Assigned to the autogen tree with a formal comment-only no-code marker until the full startup sequence is reconstructed at branch-level source quality.
- Keep helper calls as references to their owning subsystem pages; do not inline subsystem constructor logic here.
- Behavioral C++ remains blocked by branch/failure-path reconstruction, local helper names, global singleton source names, startup phase boundaries, and failure labels; these are source-quality blockers, not ownership or emitter-route blockers.
- Do not replace the marker with a full, partial, skeleton, `return`, `throw`, or TODO-style method body unless a later report proves exact behavior for the fatal, patcher, Win32Error, DAT-load, window-creation, and singleton-construction branches.

## Cross-References

- File: [UID:0000HG][Application](by-file/Application.md)
- Class: [UID:00000D][Application](by-class/Application.md)
- Caller: [UID:0002H7][0x004f5fb0-0x004f6490.BaramAppStartup](by-memory/0x004f5fb0-0x004f6490.BaramAppStartup.md)
- Startup resources: [UID:0001R6][application-startup-dat-archives](by-resource/application-startup-dat-archives.md)
- Indexed DAT helper: [UID:0000YX][0x00467410-0x004674ed.LoadIndexedDATSeries](by-memory/0x00467410-0x004674ed.LoadIndexedDATSeries.md)

## Historical Changes And Current UID0003OD Correction

- 2026-08-12 B001 UID0003OD current-source reconciliation: the current source identity is base virtual `Application::Startup`, called by derived `BaramApp::Startup` at `0x004f5fd7`. Earlier `ApplicationInitialize` / `Application::Initialize` spellings and their no-code pass descriptions remain below only as dated historical reconstruction terminology; the exact range, 4226-byte behavior, startup phase/failure evidence, score, owner/emitter route, and formal no-code disposition are unchanged.

- 2026-06-29 B006 ApplicationInitialize empty-emitter implementation:
  - What existed before: score was `85/89`, the formal C++ block was blank, generated `Application.cpp` reported UID0002H0 as an `Empty Emitter Marker`, and the page still carried stale historical language saying live MCP was unavailable.
  - What it was changed to: raised to `86/90`, kept `CANONICAL_OWNER:00000D`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:00000D`, and inserted a formal comment-only no-code marker instead of a behavioral `Application::Initialize` body.
  - Summary/evidence: accepted B006 report `tools/leaser/Agents/Agent-B006/research/0002H0-ApplicationInitialize-empty-emitter-source-quality.md`, accepted MCP session `c9671f69` on `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `19076`, healthy Hex-Rays/analysis/string cache, exact `sub_4639D0` range `0x004639d0-0x00464a52`, size `0x1082` / 4226 bytes, `0x00464a52` not-a-function, successor `sub_464A60` at `0x00464a60`, one code caller at `0x004f5fd7`, caller ignoring EAX so eventual source signature should be `void Application::Initialize()`, 162 basic blocks, complexity `31`, startup strings `Nexon.SingleInstance`, `Global\Nexon.SingleInstance`, `runas`, and `patcher.exe`, nine fatal helper calls, patcher `ShellExecuteExA` at `0x464855`, two `__CxxThrowException@8` paths at `0x464a09` and `0x464a4d`, and no-split proof that this is one real Application method. Full, partial, and skeleton executable bodies are rejected because the startup caller would continue with missing process-visible WSA/license/mutex/window/DAT/shutdown-event/manager side effects; clearing `EMITTER_UIDS:` is rejected because Application ownership and source route are proven.
- 2026-05-31 initial split:
  - What existed before: `Initialize` was documented only inside the broad Application lifecycle aggregate and file page.
  - What it was changed to: created this exact method page and assigned it under `Application` without a speculative C++ body.
  - Summary/evidence: IDA MCP confirms the `0x004639d0-0x00464a52` boundary; existing docs capture the verified startup call graph.
- 2026-06-02 scoring update:
  - What existed before: the page was attached and reconstructable but remained at `55/80` with only a short behavior summary.
  - What it was changed to: raised to `70/84` and added score/evidence tables while keeping the C++ block blank.
  - Summary/evidence: the Application file now records extensive IDA-backed startup singleton/resource evidence, and this page has a confirmed method boundary and parent attachment.
- 2026-06-03 Wave2/prewave startup-phase update:
  - What existed before: score was `70/84`, and the page had exact bounds and broad startup ownership but no local Wave2/prewave packet or phase-order table.
  - What it was changed to: score is `82/88`; added Wave2 status/xref evidence, readonly prewave metadata, a startup phase snapshot, and direct caller/helper links.
  - Summary/evidence: Wave2 restores `Application::Initialize` as an included Application method with `auto_grade=100`, xrefs tie the only code caller to the startup path at `0x004f5fb0`, and the readonly prewave JSON agrees on the `4226`-byte boundary and caller/callee counts. C++ remains blank because the final branch-level rewrite and source-quality helper/global names are still gated.
- 2026-06-16 B002 Application lifecycle source-quality recheck:
  - What existed before: score was `82/88`, with source-quality blockers listed but not fully reconciled against live Application lifecycle context.
  - What it was changed to: raised to `85/89`; owner/emitter remain [UID:00000D][Application](by-class/Application.md), and final C++ remains blank.
  - Summary/evidence: IDA MCP session `b002_ranking_0001ZC_20260616` confirmed `sub_4639D0` as the exact 0x1082-byte Application startup/orchestration method with startup strings `Nexon.SingleInstance`, `Global\Nexon.SingleInstance`, `runas`, and `patcher.exe`, plus WSAStartup, single-instance/window/DAT/license/manager setup, and Application ValidateLicense callees. The remaining blockers are branch-level failure-path reconstruction and final field/global/helper declarations, not owner or source-placement uncertainty.
