*** UID:000116 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x00494a90-0x00494b4b - ProgressBarControlPane Range Setters

## Supervisor Parent-Gate Audit

Leave this aggregate unassigned and non-emitting. The exact child pages now reach `85/88`, and Batch 113 refreshed the direct proposed class parent [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) plus file parent [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) to `86/85`, so the numeric parent gate no longer blocks the children. Assignment is still withheld from this page because it is a duplicate grouping/index aggregate over the exact assigned child pages [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md) and [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md), so normal by-structure/autogen rules do not support setting `AUTOGEN_PARENT_UID` on the aggregate.

2026-06-07 resume audit did not change the assignment decision. A fresh read-only IDA MCP parent-gate check reconfirmed the raw setter starts remain unmodeled and unreferenced, while the modeled progress-bar constructor and draw virtual still provide the class-owner evidence. That supports the exact child pages and this aggregate's documentation role, but it does not create direct source-generation ownership for the aggregate itself.

2026-06-10 B001-017 audit converted this page from `RECONSTRUCTABLE:TRUE` to `RECONSTRUCTABLE:FALSE`. The source-authored code is represented by the exact child pages; this master range is only a non-emitting duplicate inventory over those children plus the internal alignment gap.

## Status

- Confidence: strong for ownership, byte boundaries, helper flow, and no-start-xref status; medium for final source-level names.
- Related class: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- Likely source file: [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- IDA function status: both starts are currently code bytes, not function objects.
- Page role: non-emitting duplicate grouping index only. Exact helper pages are canonical for `by-memory` range ownership.
- Autogen status: `RECONSTRUCTABLE:FALSE`, parent blank, and no C++ because exact child pages are already attached to [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md), and this aggregate would only duplicate those child ranges in generated output.
- Rebuild handling: non-emitting inventory/container. The child helpers are source-authored private helper code; this aggregate itself is not a source-level object.

## Score Rationale

- Completion is `86` because the page now records current IDA evidence for both raw helper bodies, exact bytes and padding around the aggregate, decoded instruction flow, no-xref/no-function status, shared invalidation slot, constructor/draw field linkage, child ownership, duplicate-generation risk, and the corrected non-emitting container decision.
- Confidence is `90` because live IDA `lookup_funcs`, `xrefs_to`, `callers`, constructor/draw/vtable anchor checks, and bounded byte/disassembly reads agree with the child pages and the parent class/file documents. It stays below final-audit confidence because no direct reachability is proven for the two raw starts and the source-level helper/field names remain provisional.

## Covered Ranges

| Range | Provisional role | Notes |
| --- | --- | --- |
| [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md) | range-start/current-lower-bound setter | Clamps the incoming short to `0..30000`, compares/stores field `0xf8`, raises current field `0xfa` if it is below the new value, and dispatches vtable `+0x20` invalidation with the pane rectangle at object offset `0x44`. |
| [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md) | range-end/current-upper-bound setter | Clamps the incoming short to `0..30000`, compares/stores field `0xfc`, lowers current field `0xfa` if it is above the new value, and dispatches the same invalidation path. |

## Evidence Notes

- IDA MCP `lookup_funcs` reports `0x00494a90` and `0x00494af0` as `Not a function`.
- Raw IDA disassembly still shows function-shaped code: `push ebp`, `mov ebp, esp`, `thiscall` use of `ecx`, and `retn 4`.
- The exact end-exclusive boundaries are `0x00494ae7` and `0x00494b4b`; gaps inside the broader page are alignment bytes.
- 2026-06-01 IDA MCP byte check confirms `0x00494a90-0x00494ae7` is 87 bytes ending in `retn 4`, `0x00494ae7-0x00494af0` is nine `0xcc` bytes, `0x00494af0-0x00494b4b` is 91 bytes ending in `retn 4`, and `0x00494b4b-0x00494b50` is five `0xcc` bytes.
- Live IDA MCP evidence on 2026-06-05 used `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, image base `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- 2026-06-05 `lookup_funcs` reconfirms `0x00494a90`, `0x00494af0`, `0x00494ae7`, and `0x00494b4b` are not IDA functions; `0x00494b50` is the next modeled helper, size `0x27`, and `0x00494c80` is the constructor, size `0x12a`.
- 2026-06-05 `xrefs_to` reports no code or data xrefs to either raw helper start at `0x00494a90` or `0x00494af0`; the adjacent modeled `0x00494b50` helper has code callers at `0x005476ed` and `0x0054783f`, confirming the no-xref condition is specific to the raw range-setter starts.
- 2026-06-05 bounded byte reads confirm `0x00494a81-0x00494a90` is fifteen `0xcc` bytes before the range-start setter, `0x00494ae7-0x00494af0` is nine `0xcc` bytes between setters, and `0x00494b4b-0x00494b50` is five `0xcc` bytes before the modeled helper at `0x00494b50`.
- 2026-06-07 IDA MCP compact audit reconfirms no IDA function at `0x00494a90` or `0x00494af0`, the next modeled function at `0x00494b50-0x00494b77`, no xrefs to either raw helper start, two code xrefs to the adjacent `0x00494b50` helper, exact helper sizes `0x57` and `0x5b`, nine `0xcc` bytes at `0x00494ae7-0x00494af0`, five `0xcc` bytes at `0x00494b4b-0x00494b50`, and tail writes through `this+0xf8`/`this+0xfc` followed by `retn 4`.
- 2026-06-07 resume IDA MCP parent-gate check reconfirms `0x00494a90` and `0x00494af0` have no code or data xrefs, `0x00494c80-0x00494daa` is the modeled constructor with the `PatchPane::PatchPane` caller at `0x005472d7`, and `0x00494de0-0x00494eb0` is the modeled draw virtual reached through vtable data at `0x00617b78`.
- 2026-06-08 Batch 113 IDA MCP recheck reconfirms `0x00494a90` and `0x00494af0` are still not function objects and still have no xrefs, the next modeled helper remains `0x00494b50-0x00494b77`, the constructor remains `0x00494c80-0x00494daa` with caller `0x005472d7`, and the byte window preserves the two setter bodies separated by nine `0xcc` bytes and followed by five `0xcc` bytes.
- The helpers operate on the same field triplet used by the confirmed constructor and draw virtual: `0xf8`, `0xfa`, and `0xfc`.
- [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md) confirms primary vtable slot `+0x20` targets `0x00544800`, matching the invalidation calls in both raw helpers.
- The old memory report already classified both starts as likely private `ProgressBarControlPane` helpers.
- Exact child pages [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md) and [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md) now record touched-state tables tying the argument, lower/upper fields `0xf8` and `0xfc`, current field `0xfa`, pane rectangle offset `0x44`, and vtable `+0x20` invalidation call to constructor/draw evidence.

## Decoded Helper Flow

| Helper | Live decoded flow |
| --- | --- |
| `0x00494a90-0x00494ae7` | Loads the incoming short argument, uses `ecx` as the receiver, clamps negative values to `0`, clamps values above `0x7530` (`30000`) to `30000`, compares against object field `0xf8`, returns when unchanged, otherwise uses pane rectangle offset `0x44` for invalidation, raises current field `0xfa` when it is below the new lower value, dispatches vtable `+0x20`, writes field `0xf8`, dispatches vtable `+0x20` again, and returns with `retn 4`. |
| `0x00494af0-0x00494b4b` | Loads the incoming short argument, uses `ecx` as the receiver, applies the same `0..30000` clamp, compares against object field `0xfc`, returns when unchanged, reads current field `0xfa`, lowers current when it is above the new upper value, dispatches vtable `+0x20` with pane rectangle offset `0x44`, writes field `0xfc`, dispatches vtable `+0x20` again, and returns with `retn 4`. |

## Aggregate State Map

| Helper | Input handling | Range field | Current-field synchronization | Invalidation |
| --- | --- | --- | --- | --- |
| range-start setter `0x00494a90-0x00494ae7` | Clamps incoming short to `0..30000`. | Writes lower/start field `0xf8`. | Raises current field `0xfa` if it is below the new start. | Calls vtable `+0x20` with rectangle offset `0x44`. |
| range-end setter `0x00494af0-0x00494b4b` | Clamps incoming short to `0..30000`. | Writes upper/end field `0xfc`. | Lowers current field `0xfa` if it is above the new end. | Calls vtable `+0x20` with rectangle offset `0x44`. |

## Boundary Map

| Range | Kind | Notes |
| --- | --- | --- |
| `0x00494a81-0x00494a90` | alignment | Fifteen `0xcc` bytes after the preceding `ControlPane` constructor/helper function and before the raw range-start setter. |
| `0x00494a90-0x00494ae7` | helper body | Range-start setter, complete code-shaped `thiscall` body ending in `retn 4`. |
| `0x00494ae7-0x00494af0` | alignment | Nine `0xcc` bytes between the two projected helper bodies. |
| `0x00494af0-0x00494b4b` | helper body | Range-end setter, complete code-shaped `thiscall` body ending in `retn 4`. |
| `0x00494b4b-0x00494b50` | alignment | Five `0xcc` bytes before the next modeled helper at `0x00494b50-0x00494b77`. |

## Migration Notes

- Keep these as projected private helpers until IDA function modeling is repaired.
- Do not attach either helper to `ControlPane` or `RadioGroupControlPane`; those classes only surround this address area.
- Keep C++ blank on the aggregate page. The operation is documented by the exact children, but this page is a grouping index and the helper/field names are still provisional.

## Cross-References

- [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md)
- [UID:000119][0x00494c80-0x00494daa.ProgressBarControlPaneConstructor](by-memory/0x00494c80-0x00494daa.ProgressBarControlPaneConstructor.md)
- [UID:00011B][0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw](by-memory/0x00494db0-0x00494eb0.ProgressBarControlPaneUpdateAndDraw.md)

## Changes

- Before: the page title and references used the stale aggregate end `0x00494b48` and left reconstruction metadata blank.
- Changed to: the aggregate uses the verified end-exclusive range `0x00494b4b`, is marked reconstructable, and records the byte-level padding/body evidence.
- Summary/evidence: IDA MCP byte review shows the second helper ends at `0x00494b4b` with `retn 4`; the following five bytes are `0xcc` alignment before the next helper cluster.
- 2026-06-02: Raised the aggregate from `66/78` to `72/82`.
  - Before: the page had corrected boundaries and child links but lacked a concise state map, parent-attachment rationale, and explicit summary of child touched-state evidence.
  - After: the page records the two setter roles side by side, exact alignment/body boundaries, child touched-state linkage, and why the aggregate remains parentless in autogen.
  - Evidence: existing child pages [UID:000115][0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter](by-memory/0x00494a90-0x00494ae7.ProgressBarControlPaneRangeStartSetter.md) and [UID:000117][0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter](by-memory/0x00494af0-0x00494b4b.ProgressBarControlPaneRangeEndSetter.md), plus the constructor/draw/class/file pages.
- 2026-06-03: Updated the autogen rationale without changing scores.
  - Before: the page still said parent confidence was below the attachment threshold.
  - After: the page records that the aggregate remains parentless to avoid duplicating exact child pages, which are now attached under [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md).
- 2026-06-05 live IDA MCP range-setter audit:
  - Before: completion/confidence were `72/82`, and the page relied mostly on earlier child-page evidence without recording a current live no-xref/no-function check, decoded helper flow, or vtable-slot confirmation.
  - After: completion/confidence are `80/88`, with live evidence for raw byte boundaries, padding, no-xref/no-function status, decoded control flow, vtable `+0x20` invalidation, and the no-duplicate autogen rationale.
  - Evidence: live `lookup_funcs`, `xrefs_to`, and byte/disassembly reads on 2026-06-05 confirm both raw helper starts remain unmodeled and unreferenced, both bodies clamp to `0..30000` and update lower/upper fields `0xf8` and `0xfc` plus current field `0xfa`, and the aggregate is bounded by `0xcc` padding before the modeled helper at `0x00494b50`.
- 2026-06-07 A007 Batch 004 corrected-gate pass:
  - Changed to: `COMPLETION:85`, `CONFIDENCE:89`; `AUTOGEN_PARENT_UID` remains blank.
  - Evidence: fresh IDA MCP compact audit reconfirmed function/non-function status, xrefs, exact helper sizes, alignment bytes, and tail stores/returns. Assignment is withheld because the direct proposed parent [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) is `86/84`, and because this aggregate would duplicate exact child pages already attached to that class.
- 2026-06-07 A007 Batch 004 resume check:
  - Changed to: no score or metadata change.
  - Evidence: a fresh read-only IDA MCP parent-gate recheck reconfirmed no xrefs to the raw setter starts, the modeled constructor/draw boundaries, and the vtable/data ownership evidence. The aggregate remains unassigned because the direct parent is still below the corrected gate and direct source-generation ownership is not supported for a duplicate grouping page.
- 2026-06-08 Batch 113 parent-gate refresh:
  - Changed to: no score or metadata change; `AUTOGEN_PARENT_UID` remains blank.
  - Evidence: fresh IDA MCP checks reconfirmed both raw setter boundaries, no-xref/non-function status, constructor/draw ownership anchors, and alignment bytes. [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md) and [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md) were refreshed to `86/85`, but assignment is still withheld because this aggregate would duplicate exact child pages already attached to the class.
- 2026-06-10 B001-017 duplicate-aggregate audit:
  - Changed to: `COMPLETION:86`, `CONFIDENCE:90`, `RECONSTRUCTABLE:FALSE`, parent blank.
  - Evidence: live IDA MCP rechecked the whole range: `lookup_funcs` still reports `0x00494a90` and `0x00494af0` as not functions; `xrefs_to`/`callers` for both helper starts are empty; byte audit confirms `0x00494a90-0x00494ae7` and `0x00494af0-0x00494b4b` as code-shaped helper bodies separated by nine `0xcc` bytes and followed by five `0xcc` bytes; constructor/draw/vtable anchors still tie the exact children to `ProgressBarControlPane`. The exact child pages now carry source ownership at `85/88`, so this aggregate is recorded as a non-emitting duplicate inventory.
