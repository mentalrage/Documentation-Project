*** UID:0001EJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000A5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000A5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ParcelIconPane::OnParcelUpdate(const PaneEvent *event)
{
    if (event->packetData[0] == 8)
        return UpdateParcelCounts(event->packetData);

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-facing ParcelIconPane OnParcelUpdate secondary-vtable wrapper gates packet opcode 8 and calls UpdateParcelCounts; the binary's duplicated decode/update sequence is exact compiler inlining of the retained out-of-line helper, with false returned otherwise. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005461c0-0x0054628d ParcelIconPane Parcel Update Callback

## B001 UID0002ZO Accepted Callback - 2026-07-21

- Exact secondary-vtable callback `[0x005461c0,0x0054628d)` reads `PaneEvent::packetData` at event offset `+0x0c`, gates opcode byte 0 on value 8, and otherwise returns false.
- Human source is the thin gate/call wrapper in the formal block. The compiled body inlines [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md) byte-for-byte after the gate, including offset selection, nibble decode, asymmetric updates, child forwarding, and false return.
- This wrapper/helper split preserves both emitted functions without duplicate handwritten decode source. A monolithic callback, dead raw helper, separate file helper, and opcode gate inside UpdateParcelCounts are rejected.
- Owner/emitter remains ParcelIconPane; source position is 50 and score `92/93`. Original event/helper spellings remain the only confidence cap.

## Status

- Address range: `0x005461c0-0x0054628d` half-open.
- Disposition: reconstructable NexusTK source-authored project code.
- Entity kind: `ParcelIconPane` secondary-vtable packet/update callback.
- IDA function: `sub_5461C0`, size `0xcd`, ending at `0x0054628d`.
- Class owner: [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md), proven by the secondary vtable slot at `0x00621c10`.
- Direct owner/emitter: [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md). The class page now clears the current direct-owner gate and records this callback plus the paired raw helper as class-local packet/update logic.
- Source-family placement: [UID:0000MF][ParcelPane](by-file/ParcelPane.md), the parcel notification module that groups `ParcelIconPane`, `ParcelPane`, `FlyingParcelPane`, globals, and alert resources.
- Rebuild handling: source-authored callback body. B005 accepted first-draft formal C++ now emits `ParcelIconPane::OnParcelUpdate(const PaneEvent *event)`. `PaneEvent::packetData` remains an explicitly documented first-draft wrapper name for the observed packet pointer at event offset `+0x0c`, so the score stays below final-source range.

## Behavior

The callback receives a handler/event object and reads the packet payload pointer from `event + 0x0c`. If the first payload byte is not `8`, it returns `0` without changing state.

For parcel update packets, it reads the flag byte at `payload + 1` through `std::_Narrow_char_traits<char,int>::to_char_type`, then chooses the packed-count byte offset as follows:

- Start at offset `31` when flag `0x40` is set, otherwise offset `2`.
- Add `8` when flag `0x20` is set.
- Add `9` when flag `0x10` is set.
- Add `5` when flag `0x08` is set.

The byte at that calculated payload offset stores two parcel-side counts. The low nibble becomes one side count, the high nibble becomes the other side count, and their sum becomes the total parcel count. In the secondary `ParcelIconPane` view these fields are `this + 0x58`, `this + 0x59`, and `this + 0x5a`; normalized back to the primary owner they match `ParcelIconPane + 0xf8`, `+0xf9`, and `+0xfa`.

The method writes the new total every time a valid packet is parsed. It only forwards to [UID:0002KC][0x005464b0-0x00546575.ParcelPaneSetParcelSlotData](by-memory/0x005464b0-0x00546575.ParcelPaneSetParcelSlotData.md) when the previous total was larger than the new total, or either stored side count is lower than the packet side count. On that path it loads the child pane from secondary `this + 0x5c` (primary owner `+0xfc`), updates the stored side counts, and calls `sub_5464B0(childPane, total, lowNibble, highNibble)`.

## Boundary Evidence

- IDA MCP `lookup_funcs` confirms `sub_5461C0` at `0x005461c0`, size `0xcd`, with no function at `0x0054628d`; the next IDA function is `sub_546290` at `0x00546290`.
- IDA byte review confirms `0x0054628d-0x00546290` is `cc cc cc` padding before the `ParcelPane` constructor.
- The preceding IDA function `sub_546070` ends at `0x005460e8`, followed by eight `0xcc` bytes at `0x005460e8-0x005460f0`.
- The bytes at `0x005460f0-0x005461c0` are the separate [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md), not padding and not part of this page. It starts `55 8b ec 83 ec 0c 53 56 57...`, decodes the same packed parcel counts from a direct payload pointer argument, writes primary-owner fields `+0xf8/+0xf9/+0xfa`, loads the child from `+0xfc`, calls `sub_5464B0` at `0x005461b0`, and returns with `retn 4` at `0x005461bd`.
- The target function begins immediately at `0x005461c0`; its start is therefore exact even though the preceding span is not only padding.

## IDA MCP Evidence

- `decompile 0x005461c0` reports signature shape `char __thiscall sub_5461C0(int this, int a2)`.
- The decompilation reads `*(_BYTE **)(a2 + 12)`, checks first byte `8`, computes the flag-dependent payload offset from bits `0x40/0x20/0x10/0x08`, reads one packed byte, writes `this + 88`, conditionally writes `this + 89` and `this + 90`, loads `this + 92`, and calls `sub_5464B0`.
- `disasm 0x005461c0` confirms the field accesses as `[edi+58h]`, `[edi+59h]`, `[edi+5Ah]`, and `[edi+5Ch]`, plus the `sub_5464B0` call at `0x00546274`.
- `callers 0x005461c0` is empty because the callback is reached virtually.
- `callees 0x005461c0` reports `sub_5464B0` and `std::_Narrow_char_traits<char,int>::to_char_type`.
- `callers 0x005464b0` includes `0x00546274` inside this function and `0x00546dcd` inside `FlyingParcelPane` animation update code.
- `xrefs_to 0x005461c0` reports the data xref at `0x00621c10`.

## Vtable Evidence

[UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md) documents the parcel notification vtable island. Live IDA review reconfirmed the `ParcelIconPane` secondary view:

| Address | Value | Meaning |
| --- | --- | --- |
| `0x00621bfc` | `??_R4ParcelIconPane@@6B@_0` | Secondary RTTI dword before the vtable. |
| `0x00621c00` | `0x00546ea1` | Secondary destructor adjustor thunk. |
| `0x00621c10` | `0x005461c0` | This parcel update callback. |
| `0x00621c2c` | `??_R4ParcelIconPane@@6B@_1` | Tertiary RTTI dword. |
| `0x00621c30` | `0x00546eac` | Tertiary destructor adjustor thunk. |
| `0x00621c34` | `0x00544e90` | Inherited handler-interface slot. |

IDA xrefs to `0x00621c00` come from `0x00545eb1`, `0x005460a1`, and `0x00546f71`; xrefs to `0x00621c30` come from `0x00545ebb`, `0x005460ab`, and `0x00546f7b`. Those are constructor/destructor/scalar-deleting-destructor paths for `ParcelIconPane`, which confirms this callback's class owner.

## Ownership Notes

Earlier docs treated this as a direct `ParcelPane` helper because it forwards to `ParcelPane::SetParcelSlotData`. The vtable evidence makes [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md) the direct class owner, with final source placement in [UID:0000MF][ParcelPane](by-file/ParcelPane.md). The callback reaches the child `ParcelPane` through the icon-pane associated child pointer, so the `ParcelPane::SetParcelSlotData` edge is data flow, not ownership transfer.

The class-below-gate and blank-declaration caveats are historical. [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md) now provides the complete direct-base declaration, count bytes, child pointer, inferred inactive final int, virtual destructor, callback declaration, and `[[CHILDREN]]` route. `EMITTER_UIDS` remains correctly routed through `0000A5`.

## Reconstruction Notes

This is reconstructable project logic emitted as a `ParcelIconPane` packet/update callback in the `ParcelPane.cpp` module alongside `ParcelPane`, `FlyingParcelPane`, and the parcel notification globals. B005 keeps the packet/event surface as first-draft, not final: the binary-proven flow is `*(event + 0x0c)` to packet bytes, packet byte `0` must equal `8`, flags at packet byte `1` choose the packed-count offset, and the decoded nibbles forward to [UID:0002KC][0x005464b0-0x00546575.ParcelPaneSetParcelSlotData](by-memory/0x005464b0-0x00546575.ParcelPaneSetParcelSlotData.md). [UID:0002KC][0x005464b0-0x00546575.ParcelPaneSetParcelSlotData](by-memory/0x005464b0-0x00546575.ParcelPaneSetParcelSlotData.md) already has an accepted first-draft receiver shape, so it is no longer a source-shape blocker for this callback.

The adjacent raw helper [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md) is closely related but not part of this range. It remains no-function/no-entry/non-emitting pending source route and signature proof; do not broaden this callback page to absorb it and do not treat the raw helper as padding.

## 2026-06-30 B005 Empty-Emitter Implementation

B005 raises this page to `88/91` and populates formal first-draft `ParcelIconPane::OnParcelUpdate(const PaneEvent *event)` C++. The accepted body reads `event->packetData` as the source-facing wrapper for the observed `*(event + 0x0c)` packet pointer, rejects non-`8` packet types, computes the count-byte offset from flags `0x40`, `0x20`, `0x10`, and `0x08`, splits the packed byte into left/right nibbles, updates `m_totalParcelCount`, and forwards to `m_parcelPane->SetParcelSlotData(totalCount, leftCount, rightCount)` only when the old total decreased or a side count increased. `PaneEvent::packetData` remains a first-draft wrapper name and is the reason confidence stays below final-source range.

## 2026-07-15 B001 UID0001EO Accepted Callback Synchronization

- Current metadata is `90/92`, owner/emitter [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md), reconstructable true, blank optional position, and `Nested:0`.
- The complete existing managed body is preserved byte-for-byte as the accepted source draft. This callback update changes no packet offsets, flag math, nibble assignment, state-transition condition, call arguments, return value, or `PaneEvent::packetData` confidence cap.
- The class route is no longer an empty-emitter blocker: the current `ParcelIconPane` declaration supplies this protected method declaration, all four accessed fields, the direct Pane/Singleton bases, and the child insertion marker.
- Primary owner fields remain byte counts at `+0xf8`, `+0xf9`, and `+0xfa` plus `ParcelPane *` at `+0xfc`. The secondary callback receiver observes them at `+0x58..+0x5c`; that receiver adjustment is compiler ABI behavior, not an alternate source layout.
- The paired raw body [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md) remains a non-emitting support page and is not merged into this function.
- Remaining confidence limits are source spelling and event-wrapper type only. There is no outstanding class declaration, owner, emitter, child route, or generated-source blocker.

## Cross-References

- [UID:0001EH][0x00545e40-0x005470ad.ParcelNotificationPanes](by-memory/0x00545e40-0x005470ad.ParcelNotificationPanes.md)
- [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md)
- [UID:0000A6][ParcelPane](by-class/ParcelPane.md)
- [UID:0000MF][ParcelPane](by-file/ParcelPane.md)
- [UID:0001EG][0x00545e40-0x0054606a.ParcelIconPaneConstructor](by-memory/0x00545e40-0x0054606a.ParcelIconPaneConstructor.md)
- [UID:0001EI][0x00546070-0x005460e8.ParcelIconPaneDestructor](by-memory/0x00546070-0x005460e8.ParcelIconPaneDestructor.md)
- [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md)
- [UID:0002KC][0x005464b0-0x00546575.ParcelPaneSetParcelSlotData](by-memory/0x005464b0-0x00546575.ParcelPaneSetParcelSlotData.md)
- [UID:0002OH][0x00621bb0-0x00621d50.ParcelNotificationVtableData](by-memory/0x00621bb0-0x00621d50.ParcelNotificationVtableData.md)

## Changes

- 2026-06-26 B007 accepted owner/raw-helper implementation:
  - Changed `CANONICAL_OWNER` and `EMITTER_UIDS` from [UID:0000MF][ParcelPane](by-file/ParcelPane.md) to [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md).
  - Linked the newly created paired raw helper [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md) and replaced the stale "needs a separate coverage pass" wording.
  - Summary/evidence: accepted B007 report and MCP session `80de0a67` preserve the event payload at `a2+12`, first byte `8`, flag-dependent packed-count offset, owner-normalized fields `+0xf8/+0xf9/+0xfa`, child pointer `+0xfc`, `sub_5464B0` call, vtable slot `0x00621c10`, and exact function boundary. At that time formal C++ remained blank for packet/event wrapper type, source signature, raw-helper pairing, side-count naming, and child field declaration blockers; 2026-06-30 B005 supersedes that deferral with the first-draft `OnParcelUpdate` body while keeping `PaneEvent::packetData` as a confidence cap.
- 2026-06-25 B002 accepted ParcelPaneSetParcelSlotData caller sync: no score or metadata change; removed the stale statement that `ParcelPane::SetParcelSlotData` still blocks this callback's final source shape. The remaining callback-level blockers are the packet/event type, side-count meaning names, and child-pane field name.
- 2026-06-05: Raised completion from `70` to `86`, confidence from `85` to `90`, marked the page `RECONSTRUCTABLE:TRUE`, and attached autogen metadata to [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
  - Before: the page had the broad callback purpose and vtable-slot claim but lacked full packet-offset behavior, caller/callee evidence, adjacent-boundary review, parent-gate handling, and reconstructable metadata.
  - After: the page records the exact half-open range, IDA function/neighbor boundaries, flag-dependent payload decoding, packed-count nibble semantics, secondary-to-primary field normalization, `sub_5464B0` call condition, vtable slot, and related raw-helper follow-up.
  - Evidence: IDA MCP `lookup_funcs`, `decompile`, `disasm`, `callers`, `callees`, `xrefs_to`, and byte/vtable `py_eval` checks on `2026-06-05`.
