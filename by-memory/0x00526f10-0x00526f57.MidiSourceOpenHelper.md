*** UID:0002BZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void MidiOpenSource(MidiDocument* document, const wchar_t* path)
{
    if (document->datFileSource != 0) {
        document->datFile.Open(path);
        document->fileHandle = INVALID_HANDLE_VALUE;
        return;
    }

    document->fileHandle = g_pfnCreateFileW(path,
                                            GENERIC_READ,
                                            FILE_SHARE_READ,
                                            NULL,
                                            OPEN_EXISTING,
                                            FILE_ATTRIBUTE_NORMAL,
                                            NULL);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Emits side-effect-only `MidiOpenSource` for adapter-backed or Win32 file-backed MIDI sources; records exact raw bytes/range, no-entry-xref caveat, adapter virtual open path, `g_pfnCreateFileW` arguments, handle sentinel, loader readiness split, type-search cap, and padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00526f10-0x00526f57 MidiSourceOpenHelper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw function-shaped helper
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for source-family ownership, exact byte boundary, adapter/file split, source-open behavior, dispatch-table slot evidence, side-effect-only helper contract, and padding; medium for direct reachability and exact original types because IDA does not model the start as a function, reports no entry xrefs, and has no project-defined MIDI source-adapter/document type.
- Autogen status: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because the child and parent file now meet the >=80 confidence threshold.

## Score Rationale

The score was raised from `72/82` to `80/88` because live IDA MCP recorded the exact bytes, no-function/no-entry-xref status, adapter-backed virtual open path, Win32 open argument order, handle-sentinel behavior, and padding before the adjacent source-close helper.

The 2026-06-16 source-core refresh raised the page to `85/90` because current IDA proved `0x0069be34` is the startup-installed `CreateFileW` slot in the shared wide API dispatch table, reconfirmed this raw body's exact open paths and padding, and checked the unresolved source-adapter/type/name blockers directly. At that time it was not raised higher because IDA still did not model the start as a function, no direct entry xref was known, and the final adapter/context type plus source-facing helper declaration were unsafe.

The 2026-07-05 B007 implementation raises completion to `88` while keeping confidence at `90`. Current MCP evidence resolves the source-facing helper declaration: this page now emits `static void MidiOpenSource(MidiDocument* document, const wchar_t* path)` as a side-effect-only open helper. The raw EAX value is not treated as a source contract because the adapter path returns the adapter open result, the file path returns a Win32 handle, no entry xref consumes that mixed value, and the loader at `0x005273a0` opens inline then performs readiness/size validation separately. The score is not raised above `90` confidence because original type names, exact source-file split, and the raw no-function/no-entry-xref state remain confidence caps.

## Address Range

`0x00526f10-0x00526f57`.

IDA does not model this start as a function, but live disassembly shows a normal prologue and open-source branch body.

## Behavior

Opens the MIDI document source. If the adapter source is active, it dispatches into the adapter path and leaves the Win32 handle slot as `-1`. Otherwise it opens a Win32 file through the shared `g_pfnCreateFileW` dispatch pointer and stores the resulting handle at `this + 0x2c`.

The Win32 path pushes arguments matching `CreateFile`-style read-only open semantics: template handle `0`, attributes/flags `0x80`, creation disposition `3`, security attributes `0`, share mode `1`, desired access `0x80000000`, and the incoming path argument.

The source-facing helper is intentionally `void`. Open success/failure is validated by [UID:0002C3][0x00527040-0x00527078.MidiSourceSizeReadyHelper](by-memory/0x00527040-0x00527078.MidiSourceSizeReadyHelper.md) and the loader, not by a return value from this helper.

## Source-State Notes

| State or dependency | Meaning |
| --- | --- |
| `this + 0x14` | Adapter-active flag deciding adapter-backed versus Win32 file-backed source handling. |
| `this + 0x18` | Adapter/source subobject used by the memory-backed source path. |
| `this + 0x2c` | Win32 file handle slot initialized by this helper and cleared by the close helper. |
| `g_pfnCreateFileW` / `0x0069be34` reference at `0x00526f49` | Shared wide API dispatch-table slot installed from `CreateFileW` at `0x0041a2f3-0x0041a2f8`; source-facing code should use the named dispatch pointer, not raw `dword_69BE34` / `unk_69BE34` labels. |

## Evidence

- Parent page records this as a raw open source helper.
- IDA live disassembly checks `dword ptr [esi+0x14]` and references `dword_69BE34` at `0x00526f49`, consistent with an imported/file-open function pointer or wrapper slot.
- The close/seek/read/size helpers immediately following use the same adapter/file split.
- Current loader evidence at `0x005273a0` opens the same source inline and validates readiness separately, which resolves this helper as side-effect-only source open rather than an `int`/boolean failure-return helper.

## 2026-06-06 Live IDA MCP Evidence

- IDB: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- `lookup_funcs` still reports `0x00526f10` and `0x00526f57` as not function objects; `0x00527000` remains the next modeled helper after the raw source open/close/seek cluster.
- `xrefs_to` and `callers` for `0x00526f10` are empty. IDA records the internal `0x00526f46 -> dword_69BE34` data reference with `fn:null`, matching an unmodeled raw caller range.
- Raw bytes for `0x00526f10-0x00526f60` start `55 8b ec 56 8b f1`, contain both adapter and file-backed return paths, end the live body with `retn 4` at `0x00526f54`, and show `0xcc` padding at `0x00526f57-0x00526f5f` before [UID:0002C0][0x00526f60-0x00526f97.MidiSourceCloseHelper](by-memory/0x00526f60-0x00526f97.MidiSourceCloseHelper.md).
- The adapter path checks `this+0x14`, loads the vtable from `this+0x18`, passes the incoming path argument, calls virtual slot `+0x0c`, then stores `0xffffffff` to `this+0x2c`.
- The file path pushes `0`, `0x80`, `3`, `0`, `1`, `0x80000000`, and the incoming path before calling `dword_69BE34`; the returned handle is stored to `this+0x2c`.

## 2026-06-16 Source-Core Refresh

Live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed this helper and resolved the dispatch-slot evidence:

- `lookup_funcs` still reports `0x00526f10` and `0x00526f57` as not function objects, and `xrefs_to 0x00526f10` reports no entry xrefs.
- `insn_query 0x00526f10-0x00526fa0` shows the open body at `0x00526f10-0x00526f54`, followed by `0xcc` padding at `0x00526f57-0x00526f60` before the close helper.
- Adapter mode checks `this+0x14`, loads the vtable through `this+0x18`, passes the incoming path argument to virtual slot `+0x0c`, stores `0xffffffff` to `this+0x2c`, and returns.
- File mode pushes `0`, `0x80`, `3`, `0`, `1`, `0x80000000`, and the incoming path before calling `unk_69BE34`; the returned handle is stored to `this+0x2c`.
- `xrefs_to 0x0069be34` reports the initializer write at `0x0041a2f8`, this raw helper read at `0x00526f49`, the SMF document loader read at `0x00527400`, and broader file-open consumers. The slot is shared platform dispatch storage, not MidiPlayer-owned global data.
- `insn_query 0x0041a2d0-0x0041a320` shows `0x0041a2f3: mov eax, ds:CreateFileW` followed by `0x0041a2f8: mov dword ptr unk_69BE34, eax`; `get_global_value 0x0069be34` reads `0x0` in the static image, confirming startup-installed dispatch state.
- IDA `type_query`/`search_structs` and by-type searches found no project-defined MIDI source adapter or document type. The helper remains role-named until that context type and final method/static-helper shape are proven.

## 2026-07-05 B007 Source-Open Implementation

B007's accepted source-quality report resolves the formal C++ blocker and implements this exact child as private file-scope `MidiOpenSource` emitted through [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md).

Current MCP session `supervisor_recovery_20260705` supplied the implementation evidence:

- `lookup_funcs` reports `0x00526f10`, `0x00526f57`, and `0x00526f60` as not modeled functions, while sibling helpers remain modeled or raw in the surrounding island.
- `insn_query 0x00526f10-0x00526f60` and `get_bytes 0x00526f10 size 80` confirm the raw body at `0x00526f10-0x00526f57` and `0xcc` padding at `0x00526f57-0x00526f60`.
- `xrefs_to 0x00526f10` reports zero entry xrefs, so the raw EAX result is not treated as a consumed source contract.
- The adapter path tests `this+0x14`, loads the adapter/source subobject at `this+0x18`, passes the incoming `path` to virtual slot `+0x0c`, stores `0xffffffff` / `INVALID_HANDLE_VALUE` to `this+0x2c`, and returns.
- The file path pushes `CreateFileW(path, GENERIC_READ, FILE_SHARE_READ, NULL, OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, NULL)` arguments through `0x0069be34` and stores the returned handle to `this+0x2c`.
- `insn_query 0x0041a2d0-0x0041a320` shows `0x0041a2f3` loading `CreateFileW` and `0x0041a2f8` writing it to `unk_69BE34`; `xrefs_to 0x0069be34` includes this helper at `0x00526f49` and the SMF loader at `0x00527400`.
- `decompile 0x005273a0` shows the loader performing the open inline, storing the handle/sentinel, then checking DATFile readiness or Win32 file size separately. The loader support page now models that source shape as `MidiOpenSource(document, path);` followed by `MidiSourceIsReady(document)`.
- `type_query` for `Midi`, `SMF`, and `Track`, plus by-type searches, found no project-defined MIDI document/source-adapter/track type. The accepted `MidiDocument`, `datFileSource`, `datFile`, and `fileHandle` names remain role-based and cap confidence, but no longer block formal C++.

Historical `static int MidiOpenSource(...)` and `if (MidiOpenSource(document, path) != 0)` assumptions are rejected-stale for current documentation. The raw helper's mixed adapter-result/Win32-handle EAX behavior is an implementation artifact, not the source-facing contract.

## Reconstruction Notes

Keep the raw-start caveat until a modeled function or caller xref confirms exact call entry. Use `g_pfnCreateFileW` for the shared wide API dispatch table's `CreateFileW` slot in source-facing code; keep raw `dword_69BE34` / `unk_69BE34` labels only in evidence sections for searchability.

The final source-facing helper declaration is stable enough for formal C++ as `static void MidiOpenSource(MidiDocument* document, const wchar_t* path)`. Do not add a return-value test to callers. Validation of the opened source belongs to [UID:0002C3][0x00527040-0x00527078.MidiSourceSizeReadyHelper](by-memory/0x00527040-0x00527078.MidiSourceSizeReadyHelper.md) and loader code. Original adapter/source type names and exact source-file split remain unrecovered confidence caps.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)
- [UID:0002C0][0x00526f60-0x00526f97.MidiSourceCloseHelper](by-memory/0x00526f60-0x00526f97.MidiSourceCloseHelper.md)
- [UID:0002C1][0x00526fa0-0x00526ff6.MidiSourceSeekHelper](by-memory/0x00526fa0-0x00526ff6.MidiSourceSeekHelper.md)
- [UID:0002C2][0x00527000-0x00527039.MidiSourceRawReadHelper](by-memory/0x00527000-0x00527039.MidiSourceRawReadHelper.md)
- [UID:0002C3][0x00527040-0x00527078.MidiSourceSizeReadyHelper](by-memory/0x00527040-0x00527078.MidiSourceSizeReadyHelper.md)
- [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md)
- [UID:0000TQ][WideApiDispatchTable](by-global/WideApiDispatchTable.md)

## Changes

- 2026-07-05 B007 implementation callback:
  - What existed before: score `85/90`, blank formal C++, `dword_69BE34` was known as the `CreateFileW` dispatch slot, but the source-facing `MidiOpenSource` contract was blocked by the stale `int`/boolean-return loader abstraction.
  - Changed to: score `88/90`, formal `static void MidiOpenSource(MidiDocument* document, const wchar_t* path)` C++ populated, and owner/emitter/reconstructable metadata preserved as [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) / `RECONSTRUCTABLE:TRUE` / `EMITTER_UIDS:0000LD`.
  - Summary/evidence: current MCP session `supervisor_recovery_20260705` reconfirmed exact raw range `0x00526f10-0x00526f57`, padding `0x00526f57-0x00526f60`, no IDA function model, zero entry xrefs, adapter virtual open slot `+0x0c`, Win32 `CreateFileW` argument order through `g_pfnCreateFileW`, handle storage at `this+0x2c`, loader inline-open plus separate readiness validation, and negative type-query evidence. Historical `static int MidiOpenSource(...)` / boolean-return assumptions are rejected-stale.
- 2026-06-02 low-score pass:
  - What existed before: range, behavior, and evidence were documented, but metadata stayed at `58/72` and reconstructable was blank.
  - Changed to: scores `68/76`, `RECONSTRUCTABLE:TRUE`, explicit autogen deferral, and score rationale.
  - Summary/evidence: raw prologue, adapter-active flag check, `dword_69BE34` file-open path, handle storage at `this + 0x2c`, and neighboring source helpers support reconstructable project-code status without final C++.
- 2026-06-03 parent attachment:
  - What existed before: the helper remained unattached because its source ownership was treated as provisional.
  - Changed to: score `72/82` and `AUTOGEN_PARENT_UID:0000LD`.
  - Summary/evidence: the owning `MidiPlayer` file is now at `80/84`; this page records shared source-state offsets and sibling relationships. Final C++ remains blank.
- 2026-06-06 live IDA evidence pass:
  - What existed before: score `72/82` with broad adapter/file open behavior and source owner.
  - Changed to: score `80/88`; reconstructable status, parent UID, and blank C++ block are retained.
  - Summary/evidence: live IDA reconfirmed no function object and no direct entry xrefs, exact byte boundaries and padding, adapter virtual open slot, Win32 open argument order, `this+0x2c` handle-sentinel behavior, and the unresolved `dword_69BE34` API-wrapper naming caveat.
- 2026-06-16 source-core refresh:
  - What existed before: score `80/88`, with the file-open wrapper still listed as an unresolved local/global name.
  - Changed to: score `85/90`; owner/emitter route remains [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), and final C++ remains blank.
  - Summary/evidence: live IDA session `b001_mappane_0001AW_20260616` reconfirmed no function object or entry xrefs, exact raw bytes/padding, adapter virtual open slot `+0x0c`, Win32 `CreateFileW` argument order, `this+0x2c` handle storage, and dispatch initializer evidence proving `0x0069be34` is installed from `CreateFileW` while final adapter/context/helper declarations remain unproven.
