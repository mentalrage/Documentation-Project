*** UID:0002DV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:85 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000D9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x00421310-0x00421362 SimpleUString Clear

## Status

- Address range: `0x00421310-0x00421362`
- Entity kind: method / string helper
- Assigned owner: [UID:0000D9][SimpleUString](by-class/SimpleUString.md), under [UID:0000OB][StringUtil](by-file/StringUtil.md)
- Disposition: reconstructable project utility code
- Rebuild handling: source-authored method using runtime allocator/free helpers
- Autogen parent: [UID:0000D9][SimpleUString](by-class/SimpleUString.md)
- Confidence: strong for behavior, SSO-7 layout fields, and direct class parent; medium for final public API spelling.

## Behavior

This method resets the early 24-byte UTF-16 SSO-7 string layout to empty. It reads the capacity at `this + 0x14`; capacities below `8` use the inline buffer, while capacities `>= 8` use the heap pointer stored at `this + 0x00`.

For heap storage, it validates the large-allocation header when the byte size is at least `0x1000`, frees the original allocation through `0x005c7526`, then writes:

- length at `+0x10` = `0`;
- capacity at `+0x14` = `7`;
- first UTF-16 character at `+0x00` = `0`.

## Raw Code Evidence

Live IDA decompilation refreshed on 2026-06-04 shows:

- `this[5]` is the capacity gate and is compared against `8`;
- heap frees use `2 * capacity + 2` to decide whether the aligned allocation header at `ptr[-1]` must be validated;
- the reset writes `this[4] = 0`, `this[5] = 7`, and `*(_WORD*)this = 0`.

Live IDA function inventory reports `sub_421310` at `0x00421310-0x00421362`, with 83 xrefs to the function start across 22 caller functions and callees `0x005c7526` plus invalid-parameter support. The next bytes `0x00421362-0x00421370` are `0xcc` padding, and the broader `0x00421310-0x004216cb` island remains mixed, so only this endpoint helper is attached through the [UID:0000D9][SimpleUString](by-class/SimpleUString.md) SSO-7 class parent.

2026-06-10 B001-006 IDA MCP recheck confirms the direct-parent decision:

- `decompile 0x00421310` reads capacity at `this + 0x14`, frees heap storage only when capacity is at least `8`, then writes length `0`, capacity `7`, and a first UTF-16 NUL at `this + 0x00`;
- `callees 0x00421310` reports only `__invalid_parameter_noinfo_noreturn` and the shared free helper `0x005c7526`;
- the target aggregate audit confirms exact end `0x00421362` and all following gap bytes through `0x00421370` as `0xcc`;
- [UID:0000D9][SimpleUString](by-class/SimpleUString.md) is now `85/87` and documents the SSO-7 layout with sibling helpers [UID:0002E3][0x00421590-0x004216cb.SimpleUStringAssignWideCount](by-memory/0x00421590-0x004216cb.SimpleUStringAssignWideCount.md) and [UID:0002U5][0x00423060-0x00423204.FittingRoomEntryPartVectorGrowCopy](by-memory/0x00423060-0x00423204.FittingRoomEntryPartVectorGrowCopy.md), so it is the narrowest valid direct parent under the current `85/85` gate.

## Reconstructed C++ Candidate

```cpp
void SimpleUString::Clear()
{
    if (m_capacity >= 8) {
        wchar_t* storage = m_heapBuffer;

        if ((m_capacity + 1) * sizeof(wchar_t) >= 0x1000) {
            void* raw = reinterpret_cast<void**>(storage)[-1];
            if (reinterpret_cast<unsigned char*>(storage) -
                    reinterpret_cast<unsigned char*>(raw) - 4 > 0x1f) {
                _invalid_parameter_noinfo_noreturn();
            }
            storage = static_cast<wchar_t*>(raw);
        }

        FreeMemory(storage);
    }

    m_length = 0;
    m_capacity = 7;
    m_inlineBuffer[0] = 0;
}
```

The candidate preserves behavior but keeps final names provisional until the surrounding string class layout is reconciled with the later pointer-backed string family.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `85` | The page now records exact bounds, SSO-7 field offsets, clear/free behavior, caller fan-in, callee set, adjacent padding, sibling SSO helper relationship, and the corrected direct class-parent assignment. It remains below final-source quality because final public method spelling and the broader `SimpleUString` versus `StringBase` API split are still open. |
| Confidence `88` | Confidence remains strong because decompilation, callee set, range bytes, sibling SSO helper docs, and the `SimpleUString` parent page agree on the 24-byte SSO-7 object. It is not higher because the original source name/API declaration is still inferred. |

## Cross-References

- [UID:0000WT][0x00421310-0x004216cb.EarlySimpleUStringAndAdjacentHelperIsland](by-memory/0x00421310-0x004216cb.EarlySimpleUStringAndAdjacentHelperIsland.md)
- [UID:0001W5][SimpleUStringSso7Layout](by-type/by-struct/SimpleUStringSso7Layout.md)
- [UID:0000D9][SimpleUString](by-class/SimpleUString.md)
- [UID:0000OB][StringUtil](by-file/StringUtil.md)

## Changes

- 2026-06-04 live IDA attachment pass:
  - Before: `RECONSTRUCTABLE` and `AUTOGEN_PARENT_UID` were blank despite `82/88` scoring and exact SSO-7 behavior.
  - After: set `RECONSTRUCTABLE:TRUE`, `AUTOGEN_PARENT_UID:0000OB`, and replaced stale provenance with live IDA evidence.
  - Evidence: live IDA confirms exact `0x00421310-0x00421362` bounds, 83 callers, capacity-gated heap free, reset writes to `+0x10`, `+0x14`, and the first UTF-16 code unit, callees, and following `0xcc` padding. Score remains `82/88`; C++ stays below final because the exact public class/API split remains unresolved.
- 2026-06-10 B001-006 split-gate repair: Raised completion from `82` to `85` and changed `AUTOGEN_PARENT_UID` from broad [UID:0000OB][StringUtil](by-file/StringUtil.md) to direct [UID:0000D9][SimpleUString](by-class/SimpleUString.md). Evidence: IDA MCP rechecked exact range `0x00421310-0x00421362`, SSO-7 clear semantics, `83` xrefs to the helper, free/invalid-parameter callees, and following `0xcc` padding; the `SimpleUString` class page now clears `85/87` and is the narrowest valid direct parent for SSO-7 methods.
