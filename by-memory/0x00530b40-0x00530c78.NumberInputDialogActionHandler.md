*** UID:0001CY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00009K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00009K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NumberInputDialog::OnControlCommand(int controlId, int /*notifyCode*/)
{
    if (controlId == kNumberInputOkButtonId) {
        const mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > inputText =
            GetChild<TextEditControlPane>(kNumberInputEditControlId)->GetInputText();
        const int value = static_cast<int>(inputText.ToLong());

        if (value < m_minValue || value > m_maxValue) {
            const mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > message =
                g_pLanguageMan->CopyLocalizedString(kNumberInputInvalidNumberStringId);
            new AlertPane(message.c_str(), this, L"OK", 0);
            return;
        }

        if (m_confirmCallback != 0) {
            m_confirmCallback->Invoke(static_cast<unsigned long>(value));
        }

        CloseDialog();
        return;
    }

    if (controlId == kNumberInputCancelButtonId) {
        CloseDialog();

        if (m_cancelCallback != 0) {
            m_cancelCallback->Invoke();
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 0x138-byte DialogPane::OnControlCommand override with SHA256 233B3D805034B708B6C50C491839329A9C5839E3F7A317D97138CCD8A1406570, sole primary-vtable cell 0x0062021c, control-1 parse/range/confirm flow, control-2 close/cancel flow, localized error 217, corrected formal method name, exact frame/comments, and declaration-backed source-ready class route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00530b40-0x00530c78 NumberInputDialog Action Handler

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: virtual dialog action handler
- Owner: [UID:00009K][NumberInputDialog](by-class/NumberInputDialog.md)
- Likely source module: [UID:0000M1][NumberInputDialog](by-file/NumberInputDialog.md)
- Semantic owner/emitter: [UID:00009K][NumberInputDialog](by-class/NumberInputDialog.md)
- Range index: [UID:0001CW][0x00530640-0x00530cf7.NumberInputDialog](by-memory/0x00530640-0x00530cf7.NumberInputDialog.md)
- Confidence: very strong for inherited virtual identity, exact primary/tail bodies, dispatch behavior, API route, and source ownership.

## Evidence

- IDA MCP `lookup_funcs` reports start `0x00530b40` with size `0x138`, giving end-exclusive range `0x00530b40-0x00530c78`.
- `simroot_v2/class_NumberInputDialog.cpp` emits this as `NumberInputDialog::OnConfirm`.
- IDA vtable data refs place this body in the `NumberInputDialog` action slot.
- 2026-06-01 IDA MCP xrefs confirm the action slot data ref at `0x0062021c`.
- 2026-06-01 IDA MCP decompilation confirms button id `1` fetches child control `4`, copies text via `sub_498C20`, parses through `sub_582670`, compares against fields at `+0x274` and `+0x278`, opens localized alert string `217` on failure, and invokes the confirm callback at `+0x26c` on success. Button id `2` closes and invokes the cancel callback at `+0x270`.
- 2026-06-06 IDA MCP reconfirms `sub_530B40` as `0x00530b40-0x00530c78`, with no ordinary callers and a vtable data ref at `0x0062021c`.
- 2026-06-06 callees are text/control copy and parse helpers, localized string lookup `sub_4F0380`, alert construction `sub_49FEB0`, close helper `sub_49DAD0`, string cleanup helpers, and allocation.
- 2026-06-06 decompilation reconfirms button id `1` confirm flow, button id `2` cancel flow, edit control `4`, min/max offsets `+0x274/+0x278`, confirm callback at `+0x26c`, cancel callback at `+0x270`, and localized out-of-range alert id `217`.

## Behavior

Handles button id `1` as confirm and button id `2` as cancel. Confirm reads edit control `4`, parses the text as an integer, checks it against `m_minValue..m_maxValue`, opens an [UID:00000B][AlertPane](by-class/AlertPane.md) with localized string id `217` on range failure, and otherwise calls the confirm callback before closing. Cancel closes and invokes the cancel callback when present.

## Historical 2026-06-17 Source-Quality Closure

B002's source-quality pass resolved this as a generic `NumberInputDialog` action/button dispatch method rather than a confirm-only routine. Its provisional `OnButtonAction(int controlId, int unused)` spelling is historical and is superseded by the exact inherited `DialogPane::OnControlCommand(int,int)` slot identity established below.

Accepted dispatch model:

| Value | Behavior |
| ---: | --- |
| `1` | OK/default action. Fetch edit control `4`, copy text, parse decimal integer, validate inclusive `m_minValue..m_maxValue`, show localized alert id `217` on failure, invoke `m_confirmCallback` with the parsed quantity on success, then close. |
| `2` | Cancel action. Close the dialog, then invoke `m_cancelCallback` if present. |
| other | No meaningful observed side effects; return without action. |

Accepted helper-name direction:

| Binary/helper | Source-facing direction |
| --- | --- |
| `sub_498C20` | `TextEditControlPane::GetInputText` / `CopyInputText`; used through child control id `4`. |
| `sub_582670` | `ParseDecimalInteger` / `StringUtil::ParseInteger`; parses the edit text. |
| `sub_4F0380` | [UID:00040Q][0x004f0380-0x004f03bb.LanguageManCopyLocalizedString](by-memory/0x004f0380-0x004f03bb.LanguageManCopyLocalizedString.md) for STR.RES id `217` / `0xd9`; exact call site is `0x00530c27`. |
| `sub_49FEB0` | shared `AlertPane` creation/presentation path for the invalid-number message. |
| `sub_49DAD0` | shared close/dismiss helper. |

Rejected alternatives:

- `OnConfirm`: rejected as final naming because this same virtual handles OK and cancel.
- Treating ids `1` and `2` as imported Windows `IDOK`/`IDCANCEL` names: rejected as exact naming; they are project UI control/action ids that only resemble that convention.
- Inlining the alert text literal in source: rejected because the binary retrieves localized STR.RES id `217`.
- Preserving raw `sub_` names in first-draft C++: rejected because behavior and subsystem ownership are understood enough for descriptive source placeholders.

## 2026-08-02 B010 Accepted OnControlCommand Reconstruction

### Exact Identity And Virtual Route

| Property | Exact result |
| --- | --- |
| Primary range | `[0x00530b40,0x00530c78)`, `0x138` bytes |
| Primary SHA256 | `233B3D805034B708B6C50C491839329A9C5839E3F7A317D97138CCD8A1406570` |
| External cleanup/EH tails | `[0x0060508b,0x006050d2)`, 71 bytes, SHA256 `C48A8B14EE894A3780ED8CE1A7289A4D174C50B654D89DEBD165A083C7A7D926` |
| Inbound set | sole data xref from primary vtable cell `0x0062021c`; no ordinary callers |
| Virtual identity | primary-vtable slot `+0x48`, equal to declared `DialogPane::OnControlCommand(int,int)` |
| Formal signature | `void NumberInputDialog::OnControlCommand(int controlId, int notifyCode)` |

The current frame has `Block +0x10`, `var_14 +0x14`, `var_10 +0x18`, `var_C +0x1c`, `var_4 +0x24`, saved area `+0x28`, return `+0x2c`, and arguments at `+0x30,+0x34`. The primary body and external tails carry 14 protected in-range regular comments, including `0x00530bf5 = this`, and no internal repeatable comments; these remain exact analysis evidence.

### Dispatch Semantics

| Control | Exact behavior |
| --- | --- |
| `1` / OK | obtains edit control `4`, reads its wide `StringBase`, converts with `ToLong`, performs signed inclusive checks against `m_minValue +0x274` and `m_maxValue +0x278`, emits localized error id `217` through `LanguageMan`/`AlertPane` on failure, otherwise invokes owned `m_confirmCallback +0x26c` with the value, then closes |
| `2` / cancel | closes first, then invokes nullable borrowed `m_cancelCallback +0x270` when non-null |
| other | no observed meaningful side effect |

### Declaration And Dependency Evidence

- `DialogPane` formally declares typed `GetChild<T>(int)` and `CloseDialog`; the exact payload uses `GetChild<TextEditControlPane>(kNumberInputEditControlId)`, never the contradicted raw helper spelling.
- `TextEditControlPane` declares return-by-value `GetInputText()`. UID0001WS declares `StringBase::ToLong() const` and `StringBase::c_str() const`.
- UID0001WQ declares `FunctionObjectT<unsigned long>::Invoke(unsigned long)` through its template and `FunctionObject0::Invoke()` directly.
- [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md) owns the sole `LanguageMan *g_pLanguageMan = 0;` definition in `NexusTK/localization/LanguageMan.cpp`; [UID:000071][LanguageMan](by-class/LanguageMan.md) owns the guarded extern and by-value `CopyLocalizedString(int) const` declaration. The ABI call at `0x00530c27` constructs the const four-byte StringBase result for id `217`, which remains alive through `message.c_str()` and destruction.
- `AlertPane` declares the exact four-argument construction form, and the pooled button text is literal `L"OK"`.
- Invented `ParseDecimalInteger`, `ShowModalAlert`, and convenience wrapper APIs are removed. Formal H remains blank because UID00009K owns the protected virtual declaration.

### Score And Historical Correction

Completion `93` remains below final because original local spellings and rebuilt primary/EH-tail byte identity remain final-audit work. Confidence `95` follows from exact slot equality, hashes, sole xref, frame/comments, declared API dependencies, callback/range behavior, and source route.

The older `OnConfirm`, `OnButtonAction`, generated helper, and invented-wrapper assumptions are retained above as labeled historical stages. Exact vtable-slot comparison and declaration-backed formal CPP supersede them without deleting the earlier evidence.

## Cross-References

- [UID:00009K][NumberInputDialog](by-class/NumberInputDialog.md)
- [UID:0001CW][0x00530640-0x00530cf7.NumberInputDialog](by-memory/0x00530640-0x00530cf7.NumberInputDialog.md)
- [UID:0000HE][AlertPanes](by-file/AlertPanes.md)
- [UID:0000KK][LanguageMan](by-file/LanguageMan.md)
- [UID:00040Q][0x004f0380-0x004f03bb.LanguageManCopyLocalizedString](by-memory/0x004f0380-0x004f03bb.LanguageManCopyLocalizedString.md)

## Changes

- 2026-08-24 B005 UID0000KK support sync: replaced the explicit output-pointer call with const by-value `StringBase<wchar_t,...> message = g_pLanguageMan->CopyLocalizedString(217)`, preserving the exact `0x00530c27` hidden-result lifetime, following AlertPane `message.c_str()` use, callback flow, and all control semantics.

### 2026-06-17 - B002 Source-Quality Execution

- What existed before: this page was `82/90`, emitted mechanically through [UID:0001CW][0x00530640-0x00530cf7.NumberInputDialog](by-memory/0x00530640-0x00530cf7.NumberInputDialog.md), used inherited `OnConfirm` wording, and kept formal C++ blank because method naming/callback declarations were unresolved.
- What changed: the page is now `90/91`, semantically owned/emitted by [UID:00009K][NumberInputDialog](by-class/NumberInputDialog.md), and contains first-draft `OnButtonAction` C++ with OK/cancel paths, edit text parsing, range validation, localized error alert, callbacks, and close behavior.
- Why: B002 resolved the generic action-handler identity, control ids, min/max fields, callback invocation order, helper-name directions, localized string id, rejected confirm-only naming, and C++ readiness. Exact project-wide virtual method naming remains a final polish issue, not a code-entry blocker.

### 2026-06-21 - B007 LanguageMan copy-helper cross-link

- What changed: no score or owner change.
- Why: [UID:00040Q][0x004f0380-0x004f03bb.LanguageManCopyLocalizedString](by-memory/0x004f0380-0x004f03bb.LanguageManCopyLocalizedString.md) is now the exact child for the localized invalid-number copy helper. It preserves this handler's id `217` / `0xd9` call at `0x00530c27` and keeps the existing source API ambiguity until the public string type/API spelling is finalized.

### 2026-06-06 - Live Action Handler Recheck

- What existed before: this page had strong June 1 behavior evidence but remained scored `76/86` and attached directly to the file page.
- What changed: the page is now `82/90` and attached under [UID:0001CW][0x00530640-0x00530cf7.NumberInputDialog](by-memory/0x00530640-0x00530cf7.NumberInputDialog.md).
- Why: live IDA MCP reconfirmed exact bounds, vtable-only dispatch, callee set, confirm/cancel cases, edit-control parse, range validation, localized alert id, callback offsets, and close behavior. Final C++ remains blank below the final reconstruction gate.

### 2026-06-01 - Reconstructable Attachment

- What existed before: reconstructable and parent metadata were blank.
- What changed: the page is marked `RECONSTRUCTABLE:TRUE`, attached to [UID:0000M1][NumberInputDialog](by-file/NumberInputDialog.md), and rescored from `70/85` to `76/86`.
- Why: refreshed IDA MCP xrefs and decompilation confirm the virtual action slot, edit-control parse, range validation, localized alert, and callback dispatch behavior.
