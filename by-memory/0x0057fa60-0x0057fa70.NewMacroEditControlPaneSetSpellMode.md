*** UID:0004AB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000095 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewMacroEditControlPane::SetSpellMode(unsigned char spellMode)
{
    m_spellMode = spellMode;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 16-byte unsigned-char spell-mode setter storing the low argument byte unchanged at complete-object +0x10e, with exact bytes/hash, sole NewMacroDialog load caller, raw noncanonical nonzero preservation, paired GetSpellMode source contract, no-bool/no-enum proof, UID000095 ownership, MacroDialogs emission, and complete source C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057fa60-0x0057fa70 NewMacroEditControlPane SetSpellMode

## Status

- Address range: `0x0057fa60-0x0057fa70` half-open.
- Entity kind: source-authored, nonvirtual, public
  `NewMacroEditControlPane` setter.
- Direct owner/emitter: [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) `NewMacroEditControlPane`.
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Source-family file: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md)
  `NexusTK/ui/dialogs/MacroDialogs.cpp`.
- Rebuild handling: source-authored setter; emit the formal C++ above.

## Exact Binary And ABI Evidence

- Exact bytes are
  `55 8B EC 8A 45 08 88 81 0E 01 00 00 5D C2 04 00`; SHA256 is
  `B35EC9AA6370F008E9A91060AE79FDF1E41E12E2BCA4BCE4CF4AF90F788D1F85`.
- The exact function is one block and sixteen bytes at
  `[0x0057fa60,0x0057fa70)`.
- ABI is ordinary x86 member-call `thiscall`: ECX is the complete-object
  receiver, one stack argument is accepted, only its low byte is read, the
  byte is stored unchanged at receiver `+0x10e`, and the function returns with
  `retn 4`.
- The caller ignores EAX and the method performs only the store, so the
  strongest human source return type is `void`.
- There are no strings, constants, branches, helper calls, vtable/data refs,
  endpoint xrefs, or additional side effects.

## Caller And Storage Lifecycle

- The sole direct caller is
  [UID:00049O][0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage](by-memory/0x0053f210-0x0053f2b6.NewMacroDialogLoadMacroPage.md)
  at `0x0053f28d`.
- The caller zero-extends `CompactShortcutRecord::isSpellMode`, pushes the raw
  byte value, calls this setter, and then calls
  [UID:0004AC][0x0057fa70-0x0057fa7a.NewMacroEditControlPaneRefresh](by-memory/0x0057fa70-0x0057fa7a.NewMacroEditControlPaneRefresh.md).
- No boolean conversion occurs. Any noncanonical nonzero record byte is
  preserved exactly in the row object.
- The paired
  [UID:0004A7][0x0057f9c0-0x0057f9c7.NewMacroEditControlPaneGetSpellMode](by-memory/0x0057f9c0-0x0057f9c7.NewMacroEditControlPaneGetSpellMode.md)
  returns the same byte unchanged, and NewMacroDialog save writes it back to
  `record.isSpellMode`.
- Constructor, input, paint, persistence, and runtime behavior independently
  confirm the zero item/inventory versus nonzero spell/command domain.

## Type, Name, And Source Decision

- `unsigned char` is the strongest source parameter type because the owning
  compact record declares `isSpellMode` with that exact source-era spelling
  and the machine path preserves all eight bits.
- `bool` is rejected because the load/set/get/save API boundary performs no
  normalization.
- Plain `char` is rejected because no signed semantics exist.
- `uint8_t` is ABI-compatible historical wording but weaker than the current
  project declaration.
- An enum is rejected because arbitrary byte values survive and there is no
  one-byte enum or range-check proof.
- `SetSpellMode` and `m_spellMode` are the strongest names. They distinguish
  this semantic state from inherited Pane render mode and from Event keyboard
  modifiers.
- Historical `SetMode`, `m_mode`, and `SetModifier` remain only as superseded
  search provenance.

## Ownership, Range, And Negative Evidence

- Constructor/getter/setter/paint/input use of the same complete-object field
  proves [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md)
  as direct owner and emitter.
- MacroDialogs remains the source family; Config owns persisted storage and
  NewMacroDialog owns page transfer, not this method.
- Address order remains SetKey, this setter, Refresh. The preceding
  `[0x0057fa52,0x0057fa60)` and following
  `[0x0057fa70,0x0057fa7a)` boundaries remain outside this function.
- No split, merge, new child, position change, owner change, emitter change,
  vtable body, thunk, deleting wrapper, or IDA mutation is required.

## Score Rationale

- Completion `92` reflects exact bytes/hash/range/ABI, sole caller, raw storage
  order, type/value-domain/name resolution, paired getter lifecycle,
  ownership/source placement, negative evidence, formal source, history, and
  support synchronization.
- Confidence `94` reflects exact machine behavior and independently agreeing
  load/get/save/constructor/input/paint/persistence/runtime evidence. Original
  private lexical spelling remains the only confidence cap.

## Changes

- 2026-07-16 B001 UID0004A7 implementation callback:
  - Renamed the page UID-preservingly from
    `NewMacroEditControlPaneSetMode` to
    `NewMacroEditControlPaneSetSpellMode`.
  - Raised `86/90` to `92/94`; preserved UID000095 owner/emitter,
    reconstructable true, blank optional position, `Nested:0`, and the exact
    range.
  - Replaced the generic `uint8_t SetMode` source with the accepted
    `void SetSpellMode(unsigned char)` body and incorporated the complete
    setter evidence without changing machine behavior.
