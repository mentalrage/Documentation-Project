*** UID:00015Y | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00005R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00005R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:60 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void GeneralPurposePanel2::SwitchActiveChild(
    int childIndex,
    const unsigned char *packetData)
{
    if (childIndex >= 1)
        return;

    if (g_pTotemFrame != NULL)
        g_pTotemFrame->MarkForDeletion();

    Pane *oldChild = NULL;
    if (m_activeChildIndex != -1)
        oldChild = m_childPanes[m_activeChildIndex];

    m_activeChildIndex = childIndex;

    if (oldChild != NULL) {
        oldChild->Collapse();
        oldChild->UnregisterEventHandler();
    }

    if (m_activeChildIndex != -1) {
        Pane *newChild = m_childPanes[m_activeChildIndex];
        RectBounds bounds;
        bounds.SetLTRB(832, 376, 1021, 641);
        newChild->SetBounds(&bounds, 0);
        newChild->AddToLayer(
            &bounds,
            0,
            g_pBackPane,
            g_mainUiLayerSlots.rootPaneLayerContext);
        newChild->SetPaneOrder(0, g_pBackPane);
        newChild->OnActivate(packetData);
    }

    ReleaseDynamicAboveFrame();
    RecreateDynamicAboveFrame();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-authored GeneralPurposePanel2::SwitchActiveChild body with 0xdf-byte boundary, retn-8 ABI, one-child upper-bound/-1 semantics, Pane child array and active index, TotemFrame deletion, Pane Collapse/unregister sequence, exact bounds/layer/activation calls, one-time UserPane caller, and FrameChrome refresh dependencies documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b89a0-0x004b8a7f GeneralPurposePanel2 SwitchActiveChild

## Scope

- Covered file/module: [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md)
- Class: [UID:00005R][GeneralPurposePanel2](by-class/GeneralPurposePanel2.md)
- IDA name: `sub_4B89A0`
- Status: real project method in the one-child `GeneralPurposePanel2` layout.

## Function Boundary

IDA MCP `lookup_funcs` on 2026-05-25 reports a real function at `0x004b89a0` with size `0xdf`, ending at exclusive address `0x004b8a7f`. Live IDA MCP on 2026-06-04 and Batch 118 on 2026-06-08 rechecked the same function body and decompiled the helper as a `thiscall` method with two stack arguments. The previous `0x004b8a7e` end cut off the final `retn` immediate byte.

## Behavior

This is the one-child analogue of [UID:00015X][0x004b8650-0x004b8822.GeneralPurposePanelSwitchActiveTab](by-memory/0x004b8650-0x004b8822.GeneralPurposePanelSwitchActiveTab.md).

Observed flow:

1. Accepts only child index `0`; returns without switching for any index greater than or equal to `1`.
2. If [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md) is active, marks it for deletion before switching.
3. Finds the old child from `m_childPanes[m_activeChildIndex]` when the previous active index is valid.
4. Stores the new active index at `m_activeChildIndex` (`this + 0x08` / raw `this[2]`).
5. Collapses and hides the old child when present.
6. Sets the active child bounds to `(832, 376, 1021, 641)`.
7. Adds the child to the current UI layer/root and dispatches `OnActivate(packetData)` through the inherited Pane contract.
8. Runs [UID:0000YI][0x004615b0-0x00461658.AboveFrameRefreshHelpers](by-memory/0x004615b0-0x00461658.AboveFrameRefreshHelpers.md) after the optional child branch, including the `-1` sentinel path.

## Caller Evidence

IDA `callers` and `xrefs_to` return a single direct caller inside `UserPane::OnServerMessage` at `0x005a771b`. The singleton data xrefs for [UID:0000R1][g_pGeneralPurposePanel2](by-global/g_pGeneralPurposePanel2.md) also show the same user-pane neighborhood consuming the one-child panel shell.

## Ownership Notes

The helper uses the same one-child layout and active-index field initialized by `GeneralPurposePanel2::GeneralPurposePanel2`, so it should be documented as a `GeneralPurposePanel2` method rather than remaining unknown.

Accepted reconstruction owner and source-facing name: `GeneralPurposePanel2::SwitchActiveChild`. `SwitchActiveTab` remains the seven/four-child sibling name and a rejected weaker alias for this one-child shell.

The `0x004615b0` / adjacent creator pair belongs to [UID:0000JL][FrameChrome](by-file/FrameChrome.md). It is a dependency called after the panel switch, not a `GeneralPurposePanel2`-owned helper. Unlike the seven-child panel, this alternate one-child method does not own `g_pGeneralPurposePanel->m_activeChildIndex`; the refresh helper still reads the primary `g_pGeneralPurposePanel` singleton when rebuilding the dynamic `AboveFrame`.

## Corrected Assignment Gate

`AUTOGEN_PARENT_UID` is [UID:00005R][GeneralPurposePanel2](by-class/GeneralPurposePanel2.md). Batch 118 raised the direct class parent to `85/90` and the source file parent [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md) to `85/86`, so this child and its actual direct class owner now clear the corrected `85/85` gate. The assignment is based on field/slot evidence, not only address proximity: the helper reads and writes the `GeneralPurposePanel2` active-index field, accepts only the one child slot initialized by the constructor, and has one direct user-pane caller through the `g_pGeneralPurposePanel2` UI path.

## 2026-06-04 IDA Evidence

- Live decompilation confirms the bounds check `index < 1`; out-of-range indices return without touching the child slot.
- The previous active child is read from `this[activeIndex + 1]` after checking active index `-1`, then hidden/collapsed through calls at virtual offsets `0x40` and helper `0x00544ee0`.
- The active child is read from `this[index + 1]`, receives bounds `(832, 376, 1021, 641)`, and is then passed through virtual calls at offsets `0x2c`, `0x30`, `0x3c`, and `0x48` with `dword_67A740` and `dword_69B368` as layer/context arguments.
- The helper calls [UID:0000YI][0x004615b0-0x00461658.AboveFrameRefreshHelpers](by-memory/0x004615b0-0x00461658.AboveFrameRefreshHelpers.md) before returning.
- Xrefs still show exactly one direct caller, `0x005a771b` inside `0x005a76c0`.
- Batch 118 IDA `py_eval` reconfirmed `sub_4B89A0` at `0x004b89a0-0x004b8a7f`; bytes at `0x004b8a7e-0x004b8a7f` are the trailing `00` of the `retn 8` immediate, followed by `0xcc` alignment at `0x004b8a7f`.

## 2026-06-07 A008 IDA Evidence

- IDB source: `NexusTK.exe`, imagebase `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`.
- `lookup_funcs` still reports `sub_4B89A0` at `0x004b89a0` with size `0xdf`, matching the current page range.
- `decompile` reports a `thiscall` helper with arguments matching an index and show/activation flag. The first branch returns immediately for `index >= 1`; the taken branch clears the totem/frame helper through `dword_69B35C` / `0x00544690`, reads old child slot `this[activeIndex + 1]`, writes `this[2] = index`, hides/collapses the old child through `0x00544ee0` and virtual `+0x40`, then lays out the new child from `this[index + 1]`.
- The exact new-child setup is still bounds `(832,376,1021,641)` through `0x004b78d0`, followed by virtual calls at `+0x2c`, `+0x30`, `+0x3c`, and `+0x48`, then the frame refresh pair `0x004615b0` / `0x004615d0`.
- `callers` and `xrefs_to` still show one ordinary direct caller at `0x005a771b` inside `sub_5A76C0`; `callees` list only the hide/collapse helpers, rectangle setup, frame-refresh pair, and security-cookie check.

## 2026-07-13 B004 Accepted Source Reconstruction

- Fresh IDA MCP evidence from database `328356ab` reconfirmed `sub_4B89A0` at `0x004b89a0`, size `0xdf`, exact half-open end `0x004b8a7f`, final `retn 8` at `0x004b8a7c`, one `0xcc` alignment byte at `0x004b8a7f`, and separate successor helper `0x004b8a80`.
- Source ABI is `void __thiscall GeneralPurposePanel2::SwitchActiveChild(int childIndex, const unsigned char *packetData)`. ECX carries `this`, the two four-byte arguments account for `retn 8`, the sole caller ignores EAX, and sibling/derived slot evidence resolves the second argument as nullable packet data rather than `bool showChild`.
- The upper-bound test is deliberately only `childIndex >= 1`. Negative values are not generally rejected; `-1` is the explicit no-child sentinel. The formal body does not invent a lower-bound guard.
- Both old and new child reads use scaled `[this + index*4 + 4]` addressing. The source field is `Pane *m_childPanes[1]`, not singular `NewSystemMessagePane *m_systemMessagePane`. Constructor evidence still proves element zero contains a concrete `NewSystemMessagePane`.
- `this+0x08` is `int m_activeChildIndex`. The binary captures the old child, stores the requested index, then collapses/unregisters the captured old child. After those virtual calls it reloads `m_activeChildIndex` before selecting the new child; the managed C++ preserves this observable order.
- [UID:0004KU][0x00544ee0-0x00544f2e.PaneCollapse](by-memory/0x00544ee0-0x00544f2e.PaneCollapse.md) resolves the historical raw-helper blocker as `Pane::Collapse()`: `RemoveFromLayer()` followed by `UpdateRenderRegion` with an all-zero `RectBounds`.
- The new-child sequence is `RectBounds::SetLTRB(832,376,1021,641)`, `SetBounds`, `AddToLayer` with [UID:00028J][0x0067a740-0x0067a744.g_pBackPane](by-memory/0x0067a740-0x0067a744.g_pBackPane.md) and [UID:0000T6][MainUiLayerSlots](by-global/MainUiLayerSlots.md) `rootPaneLayerContext`, `SetPaneOrder`, and `OnActivate(packetData)`.
- Primary slot `+0x48` is the pure/common `OnActivate(const unsigned char *packetData)` contract. Packet-consuming Collection/SelfLook/UserLook overrides and argument-ignoring option/group/inventory/system-message overrides reject the former bool interpretation.
- The sole code caller at `0x005a771b` is inside [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md). It calls this method once with child zero and null packet data before entering its 128-case payload opcode switch, then sets one-time state byte `UserPane+0x3e1a`.
- [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md) remains an external Pane dependency through `MarkForDeletion`. FrameChrome owns `ReleaseDynamicAboveFrame` and `RecreateDynamicAboveFrame`. RectBounds, Pane, layer-context, caller, and FrameChrome facts do not move target ownership.
- Rejected alternatives are an integer return, `bool showChild`, singular child field, target-local collapse wrapper, raw vtable calls, lower-bound guard, UserPane/NewSystemMessagePane/FrameChrome ownership, and monolithic aggregate emission.
- Metadata is now `91/93` because exact body, ABI, range, caller, fields, dependency contracts, and source route are resolved. Exact original identifiers and textual `const` remain inference caps below `95+`.

## Cross References

- [UID:0000JQ][GeneralPurposePanel](by-file/GeneralPurposePanel.md)
- [UID:00005R][GeneralPurposePanel2](by-class/GeneralPurposePanel2.md)
- [UID:00015V][0x004b83d0-0x004b8bd3.GeneralPurposePanel](by-memory/0x004b83d0-0x004b8bd3.GeneralPurposePanel.md)
- [UID:0000R1][g_pGeneralPurposePanel2](by-global/g_pGeneralPurposePanel2.md)
- [UID:0000SK][g_pTotemFrame](by-global/g_pTotemFrame.md)
- [UID:0000PN][FrameChromeSlots](by-global/FrameChromeSlots.md)
- [UID:0000YI][0x004615b0-0x00461658.AboveFrameRefreshHelpers](by-memory/0x004615b0-0x00461658.AboveFrameRefreshHelpers.md)

## Changes

- 2026-07-21 B005 accepted UID0002VS callback: set only emitter position `60` so this accepted body follows UID0004UU/UID0004UV/UID0004UW/UID0004UX/UID0004UY in the complete GP2 class source order. Score, range, owner/emitter, Item Summary, formal body, ABI, behavior, dependencies, negative evidence, and historical detail remain unchanged.
- 2026-07-13 B004 accepted UID00015Y callback: raised `86/90 -> 91/93`; inserted the exact managed `SwitchActiveChild(int, const unsigned char *)` body; replaced singular/bool assumptions with `Pane *m_childPanes[1]` and packet-data activation; linked UID0004KU `Pane::Collapse`; resolved `+0x0c`/`+0x48` contracts, caller initialization semantics, globals, ABI, score rationale, negatives, and source placement while preserving owner/emitter/range/position/`Nested:0` and historical evidence.
- 2026-06-20 B003 Rule 26 source-quality incorporation: accepted `m_systemMessagePane` for `this + 0x04` and `m_activeChildIndex` for `this + 0x08`, kept `GeneralPurposePanel2::SwitchActiveChild(int index, bool showChild)` as the best source-facing method name, and preserved FrameChrome/Panecore helper calls as dependencies. Method-body C++ remains deferred until inherited helper/virtual-slot names are cleaner.
- 2026-06-20 B003 AboveFrame source-quality implementation: corrected the FrameChrome refresh helper link/range to `0x004615b0-0x00461658` and clarified that the helper remains FrameChrome-owned even when called after this `GeneralPurposePanel2` switch.
- 2026-06-08 A006 Batch 118 range and parent repair: changed completion from `85` to `86`, corrected the exclusive range end from `0x004b8a7e` to `0x004b8a7f`, renamed the file accordingly, and set `AUTOGEN_PARENT_UID:00005R`. Summary/evidence: live IDA reports `sub_4B89A0` size `0xdf`, `0x004b8a7e` is the final immediate byte of `retn 8`, the direct caller remains `0x005a771b`, and the helper's one-child active-index/child-slot behavior matches [UID:00005R][GeneralPurposePanel2](by-class/GeneralPurposePanel2.md) after the class/file parent-gate refresh.
- 2026-06-07 A008 corrected-gate audit: Raised completion from `82` to `85`; confidence stayed `90`. Summary/evidence: fresh IDA MCP rechecked the same IDB, exact function size, single direct caller, concrete callee list, one-child bounds check, active-index/child-slot accesses, old-child hide/collapse sequence, rectangle setup, layer/context virtual calls, and frame-refresh dependency. `AUTOGEN_PARENT_UID` remains blank because the child reaches `85/90`, but the direct class/file parent candidates are `84/90` and `84/80`, below the corrected `85/85` parent gate.
- 2026-06-04: Raised completion/confidence from `70/85` to `82/90`. Summary/evidence: live IDA MCP decompiled the helper, confirmed the single-child bounds check, old-child collapse/hide sequence, exact active-child bounds, layer/context virtual calls, frame-chrome refresh dependency, and the single direct caller at `0x005a771b`. Completion remains below final-source quality because the final public method name and full caller-message semantics still need reconstruction.
