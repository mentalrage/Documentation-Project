*** UID:0004A4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool SpellMacroEditControlPane::HandleKeyOrTextEvent(Event *event)
{
    if (event->m_type == kEventKeyDown)
    {
        const unsigned char key = NarrowInputKey(
            event->m_payload.m_key.m_key,
            event->m_payload.m_key.m_modifiers);

        if ((key >= 'a' && key <= 'z') ||
            (key >= 'A' && key <= 'Z'))
        {
            m_spellKey = key;
            InvalidateRect(NULL);
            return true;
        }
    }
    else if (event->m_type == kEventTextInput)
    {
        return true;
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: SpellMacroEditControlPane EventHandler key/text override; stores alphabetic key-down input, invalidates itself, consumes text input, and rejects key-up/nonletter events. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057f930-0x0057f998 SpellMacroEditControlPane HandleKeyOrTextEvent

## Status

- Address range: `0x0057f930-0x0057f998` half-open.
- Exact size: `0x68` / 104 bytes.
- Entity kind: source-authored EventHandler secondary `HandleKeyOrTextEvent(Event *)` override.
- Direct owner/emitter: [UID:0000DQ][SpellMacroEditControlPane](by-class/SpellMacroEditControlPane.md) `SpellMacroEditControlPane`.
- Vtable route: Spell EventHandler secondary `+0x08` cell `0x0062d254` in [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md) and [UID:0003DB][0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData](by-memory/0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData.md).
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Rebuild handling: source-authored virtual method; emit the formal C++ above.

## Evidence

- Live MCP profile gives 41 instructions and eight basic blocks. The method receives one `Event *` argument and returns with `ret 4` through the secondary EventHandler view at complete-object `+0xa0`.
- Event type `8` (`kEventKeyDown`) reads key/modifier payload fields and calls `std::ctype<char>::do_narrow`; project source-facing spelling is `NarrowInputKey(key, modifiers)`.
- Only ASCII `A-Z` or `a-z` is accepted. The accepted byte is widened and stored as the `wchar_t m_spellKey` field at complete-object `+0x108`; the observed two-byte store is unchanged.
- After the store, the method calls complete-object primary virtual slot `+0x20` with `NULL`; Pane support identifies this as `InvalidateRect(NULL)`, not a parent callback.
- Event type `10` (`kEventTextInput`) returns true without changing state. Event type `9` key-up and all other types return false.
- The sole vtable placement and EventDispatcher `+0x08` call contract reject a free helper, `KeyEvent const &`, or no-code/compiler interpretation.

## Spell-Key Lifecycle Context

- The constructor initializes `m_spellKey` from `Config::m_legacyShortcutText[row][0]`. This handler is the row's only recovered editor and preserves that character representation when it widens an accepted ASCII input byte into the two-byte field.
- `OnPaint` reads the same field for key formatting and spell lookup. `GetSpellKey` later returns it through AX, and `SpellMacroDialog::OnControlCommand` stores it unchanged back to `[row][0]` before writing a wide zero terminator to `[row][1]`.
- `SaveUserSettings` serializes the ten legacy shortcut entries through `fputwc`; runtime shortcut dispatch reads the same fixed wide-character table. The complete constructor/edit/display/save/persistence/runtime route therefore supports `wchar_t`, while the earlier generic word/`unsigned short`/`uint16_t` description is retained only as width-first historical inference.
- This lifecycle synchronization changes no branch, event contract, formal body, score, ownership, or vtable placement on this page.

## Source-Quality Decision

- `OnKeyEvent(const KeyEvent &)` is historical/superseded: it has the wrong inherited name, parameter shape, and event model.
- Historical `KeyUp -> true`, `RequestParentRefresh`, `NarrowKey(event)`, and `IsAsciiLetter` expressions are contradicted by the exact branch and call sequence. R4 preserves the binary behavior with the accepted Event model and source-facing helper.
- Historical integer spellings for `m_spellKey` described the two-byte machine access but missed its wide-character storage and persistence semantics; they are superseded by the accepted `wchar_t` class declaration.
- The helper lexical spelling remains descriptive rather than symbol-proven; this caps confidence but does not block complete source emission.
- No receiver adjustment, raw `+0x108` write, vptr operation, decompiler variable, or explicit vtable dispatch belongs in the human-written source.

## Score Rationale

- Completion `92`: exact range/size, interface slot, Event ABI and constants, normalization call, ASCII checks, field store, invalidation, all return paths, owner/route, negative evidence, and final R4 are documented.
- Confidence `94`: body and slot semantics are direct binary facts; only original lexical spellings such as `NarrowInputKey` remain inferred.

## Changes

- 2026-07-16 B005 UID0004A1 implementation callback: synchronized the accepted `wchar_t` field and complete legacy-shortcut lifecycle, while preserving the exact key/text formal, `92/94` score, event branches, ownership, and vtable route.
- 2026-07-14 B003 UID0004A5 implementation callback: validator-aware UID-preserving rename from `SpellMacroEditControlPaneOnKeyEvent`; raised `88/91` to `92/94`; installed exact R4; corrected key-up/text-input behavior and self invalidation; and retained stale names only as explicit history.
