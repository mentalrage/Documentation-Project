*** UID:0002PB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000038 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000038 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)
    : Pane(1),
      m_rangeStart(0),
      m_rangeCurrent(0),
      m_rangeEnd(0),
      m_controlType(controlType),
      m_controlFlags(0),
      m_reservedControlState(0),
      m_enabled(true),
      m_controlActive(false),
      m_visualState(10),
      m_ignoreHitTesting(false)
{
    if (bounds != NULL)
        SetBounds(bounds, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live MCP reanalysis resolves `ControlPane::ControlPane`: exact `0xa1` constructor, `Pane(1)` base call, unsigned control type byte, optional `const RectBounds *` / `SetBounds(bounds,0)` route, ControlPane vtable triplet evidence, inherited range/current triplet zeroing, control flags/reserved/enabled/active/visual-state/hit-test defaults, 52 derived-control refs, and first-draft formal C++ readiness. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004949e0-0x00494a81 ControlPane Constructor

## Status

- Address range: `0x004949e0-0x00494a81` half-open.
- Entity kind: constructor / base UI control initialization.
- Owner: [UID:000038][ControlPane](by-class/ControlPane.md).
- Likely source file: [UID:0000IG][ControlPane](by-file/ControlPane.md).
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: `source-authored`.
- Current IDA name: `sub_4949E0`.
- Source-facing role: `ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)`.
- Formal C++ status: first-draft constructor body is emitted above. It intentionally uses range-safe literals `Pane(1)` and `m_visualState(10)` and does not declare local enums, types, or constants inside this by-memory range.
- Confidence: very strong for constructor role, owner/emitter route, exact range, direct callees, vtable stores, optional bounds route, 52 constructor refs, and per-field offsets; final original member spellings for some inherited state remain below final-audit proof.

## Boundary Evidence

Current B007 MCP session `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` reports `sub_4949E0` at `0x004949e0` with size `0xa1`, ending at `0x00494a81`. `lookup_funcs` reports `0x00494a81` and `0x00494a90` are not function starts. `get_bytes` confirms the body ends with `retn 8`, `0x00494a81-0x00494a90` is fifteen `0xcc` alignment bytes, and `0x00494a90` begins the sibling ProgressBar range-start raw helper. No split is needed: this page covers only the exact constructor body.

## Behavior

The constructor initializes the shared base for clickable/input-bearing UI controls:

- calls the base [UID:0000A2][Pane](by-class/Pane.md) constructor with pane kind/mode `1`;
- stores the unsigned `controlType` byte at object offset `+0xfe`;
- installs the primary, secondary, and tertiary `ControlPane` vtable views at offsets `+0x0`, `+0xa0`, and `+0xa4`;
- optionally applies the incoming `const RectBounds *bounds` through inherited `Pane::SetBounds(bounds, 0)` when the pointer is non-null;
- zeroes the inherited range/current triplet at `+0xf8/+0xfa/+0xfc`, documented here with neutral base aliases `m_rangeStart`, `m_rangeCurrent`, and `m_rangeEnd`;
- writes control-state defaults byte-wise: `m_controlFlags=0` at `+0xff`, `m_reservedControlState=0` at `+0x100`, `m_enabled=true` at `+0x101`, `m_controlActive=false` at `+0x102`, `m_visualState=10` at `+0x103`, and `m_ignoreHitTesting=false` at `+0x104`.

The compact stores are little-endian. The dword `0x00010000` at `+0xff` yields bytes `+0xff=0`, `+0x100=0`, `+0x101=1`, and `+0x102=0`. The word `10` at `+0x103` yields `+0x103=10` and `+0x104=0`.

## IDA Evidence

Current B007 MCP evidence revalidates the older A-agent findings and resolves the source-shape blockers:

| Address / query | Direct fact |
| --- | --- |
| `decompile(0x004949e0)` | Receiver `this`; first explicit byte argument written to `this+0xfe`; second explicit pointer argument null-checked before the bounds helper; direct `Pane(1)` call; three `ControlPane` vptr writes; optional `0x00544bd0(this, bounds, 0)`; packed stores at `+0xff`, `+0xf8`, `+0xfc`, and `+0x103`; returns `this`. |
| `callees(0x004949e0)` | Only `0x00544460` and `0x00544bd0` are direct callees. |
| `0x00494a0b` | Calls `sub_544460` after pushing `1`. |
| `0x00494a13` | Writes `[esi+0xfe]` from the first explicit byte argument. |
| `0x00494a23`, `0x00494a29`, `0x00494a33` | Store primary, secondary, and tertiary `ControlPane` vtables `0x00617a90`, `0x00617af8`, and `0x00617b28`. |
| `0x00494a3d-0x00494a46` | Skips the bounds call on null; otherwise calls `0x00544bd0` with second helper argument `0`. |
| `0x00494a4d` | Stores dword `0x00010000` at `+0xff`. |
| `0x00494a57` | Stores dword zero at `+0xf8`, clearing `+0xf8/+0xfa`. |
| `0x00494a5d` | Stores word zero at `+0xfc`. |
| `0x00494a66` | Stores word `10` at `+0x103`, setting visual state and clearing hit-test suppression. |
| `0x00494a7e` | `retn 8`, matching two explicit `thiscall` arguments. |

`xrefs_to(0x004949e0)` reports 52 code xrefs. The caller set includes progress-bar, rectangle, select-box, text/static controls, object/image controls, browser/minimap/fitting-room controls, selector controls, and dialog factories. `xrefs_to` for the three `ControlPane` vtable bases reports the constructor stores at `0x00494a23`, `0x00494a29`, and `0x00494a33`. These facts support `ControlPane` as shared UI-core base infrastructure rather than a feature-dialog local helper or a ProgressBar-owned physical neighbor.

## Source Shape

The source-facing constructor signature is `ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)`.

- `controlType` is unsigned byte-style source state. Derived constructors pass non-negative construction kinds such as `8`, `22`, and `5`; fixed type-id virtuals are separate methods and should not be folded into this parameter.
- The second explicit argument is a `const RectBounds *bounds` pointer. [UID:0001VP][RectBoundsLayout](by-type/by-struct/RectBoundsLayout.md) documents the 16-byte `left, top, right, bottom` record. [UID:0000A2][Pane](by-class/Pane.md) and [UID:0000MC][Pane](by-file/Pane.md) support identify `0x00544bd0` as `Pane::SetBounds`.
- The inherited helper's second argument is always `0` from this constructor, so it remains an internal `SetBounds(bounds, 0)` detail, not a third `ControlPane` constructor parameter.
- The explicit vtable stores are compiler output from constructing a polymorphic `ControlPane` with primary, secondary, and tertiary views; the source constructor should rely on the class declaration, not handwritten vptr assignments.
- No enum/type/constant declarations are emitted in this by-memory formal block. Candidate shared constants for pane mode `1` or visual-state normal `10` would need support/class ownership; this target uses range-safe literals.

## Packed Field Map

| Offset | Source-facing interpretation | Evidence |
| --- | --- | --- |
| `+0xf8` | inherited range-start/lower-bound short, `m_rangeStart` here | Zeroed by the base constructor. ProgressBar support proves the same inherited field as progress-facing `m_minValue`, but this base constructor only proves neutral range state. |
| `+0xfa` | inherited current/range value short, `m_rangeCurrent` here | Zeroed by the same dword at `+0xf8`. ProgressBar consumes it as `m_currentValue`; the base constructor recommendation uses the neutral alias. |
| `+0xfc` | inherited range-end/upper-bound short, `m_rangeEnd` here | Zeroed by the constructor. ProgressBar consumes it as `m_maxValue`; no base-only evidence proves the original base member name was progress-specific. |
| `+0xfe` | `m_controlType` | Written from the first explicit byte argument. |
| `+0xff` | `m_controlFlags` | Zeroed by the base default. UserShape/UserFace selector support pages prove derived constructors may overwrite this inherited control flag, so it should not be named as shape- or face-specific. |
| `+0x100` | `m_reservedControlState` | Zeroed by the base default; no final consumer found in the current support pass. |
| `+0x101` | `m_enabled` | Set to `true`; Enable/Disable write this byte, ImageButton/Scrollable overrides and action/navigation consumers use it as an availability gate, and raw active-frame helpers test it. The historical `m_controlVisible` spelling is superseded; Pane visibility is separately stored at `+0xb4`. |
| `+0x102` | `m_controlActive` | Set to `false`; activate/deactivate helpers write this byte and active-frame helper tests it. |
| `+0x103` | `m_visualState = 10` | Written as byte `10`; `ControlPaneSetVisualState` writes the same byte and SelectBox paint treats value `10` as the inherited normal/default visual state. |
| `+0x104` | `m_ignoreHitTesting = false` | Cleared by the final word store; `ControlPaneIgnoresHitTesting` returns this byte and the setter writes it. |

## Source Placement

Route this method directly through [UID:000038][ControlPane](by-class/ControlPane.md), which now clears the corrected `85/85` child/direct-parent gate and is itself attached to [UID:0000IG][ControlPane](by-file/ControlPane.md) under the `ui/core/ControlPane.cpp` project-structure candidate. It is not owned by neighboring progress-bar or button-control files despite physical adjacency.

## Complete Base Declaration Support

The current [UID:000038][ControlPane](by-class/ControlPane.md) R7 declaration closes the constructor's source contract at exact complete-object size `0x108`:

- complete inherited `Pane` occupies `+0x000..+0x0f7`;
- this constructor initializes protected `short` range fields at `+0xf8/+0xfa/+0xfc`;
- `m_controlType`, `m_controlFlags`, and `m_reservedControlState` occupy `+0xfe/+0xff/+0x100`;
- protected enabled, active, visual, and hit-testing state occupies `+0x101..+0x104`;
- `+0x105..+0x107` is natural tail alignment, not a source member.

The protected access model is required by current derived-control source shape. `ButtonControlPane` and PrettyButton constructors directly assign inherited `m_controlActive`, `m_controlFlags`, and `m_reservedControlState`; they do not call setters and do not own duplicate fields at those offsets. R7 therefore rejects private base fields, invented setter calls, duplicate derived storage, and raw offset writes.

The constructor's accepted signature remains `ControlPane(unsigned char controlType, const RectBounds *bounds)`. `Pane.h` is complete because it is the base; `RectBounds` is pointer-only and remains forward-declarable. The inline empty virtual destructor and declaration order are human source causes, while vptr stores, deleting wrappers, adjustor thunks, EH/cookie code, and natural padding remain compiler output.

## Open Questions

- Exact original spellings for `m_reservedControlState`, `m_controlFlags`, and the inherited range/current triplet remain descriptive rather than PDB-proven.
- `m_rangeStart`, `m_rangeCurrent`, and `m_rangeEnd` are neutral base-constructor aliases. ProgressBar pages may continue to use `m_minValue`, `m_currentValue`, and `m_maxValue` for the same offsets where progress-specific semantics are proven.
- The fixed value `10` is source-quality enough as the inherited normal/default `m_visualState`, but this target does not own a shared constant declaration for it.

## Rejected Alternatives

- Blank formal C++ is rejected because the accepted MCP-backed report resolves the body, signature shape, field map, owner/emitter route, and score gate enough for first-draft source C++.
- Target-local anonymous enum/type/constant declarations are rejected by range-safety. This by-memory page covers only `0x004949e0-0x00494a81`.
- References to unowned `kControlPaneMode` or `kControlPaneVisualStateNormal` constants are rejected. Current support docs do not own shared declarations for those names, so the formal block uses literals `1` and `10`.
- Progress-specific `m_minValue`/`m_currentValue`/`m_maxValue` names are rejected for this base constructor formal block; they remain valid ProgressBar-facing aliases where progress behavior is proven.
- Raw `int *bounds` or `void *bounds` is rejected because `Pane::SetBounds` and `RectBoundsLayout` prove a 16-byte `RectBounds` pointer.
- Exposing the inherited `SetBounds` flags as a constructor argument is rejected because this constructor always passes `0`.
- Modeling the vtable stores in source is rejected as compiler output.
- Treating `+0xff..+0x102` as one dword field is rejected because later helpers and derived constructors consume individual bytes.
- Treating `+0x103` as a repeat delay is rejected for this target because current direct consumers support visual/control state value `10`, and no timer/repeat caller was found for this byte in the ControlPane helper set.
- ProgressBar, ButtonControlPane, PopupMenu, MiniMap, and dialog callers are rejected as direct owners. They are derived consumers, physical neighbors, or call sites; the constructor installs base `ControlPane` vtables and is called by many unrelated controls.
- Direct file ownership by [UID:0000IG][ControlPane](by-file/ControlPane.md) is rejected because [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane` is the narrower direct owner and clears the gate. The file remains the emitter route through the class.

## Score Rationale

Completion is `89` because current B007 MCP evidence and support-doc parity resolve the prior blockers: exact body, exact boundary/padding, direct callees, vtable-store xrefs, 52 constructor refs, `const RectBounds *` bounds pointer type, packed field map, source placement, rejected alternatives, and first-draft formal C++.

Confidence is `92` because MCP session `80de0a67` directly confirms the modeled function, helper behavior, vtable xrefs, caller set, and padding/sibling boundary, while [UID:000038][ControlPane](by-class/ControlPane.md) and [UID:0000IG][ControlPane](by-file/ControlPane.md) both clear the ownership/emitter route. Confidence remains below final audit because exact original spellings for `m_reservedControlState`, `m_controlFlags`, and the inherited range/current triplet are inferred/descriptive rather than symbol-proven.

## Change Log

- 2026-07-16 B003 UID0004EL support synchronization:
  - Preserved `89/92`, owner/emitter/reconstructable metadata, exact range, `Nested:4`, and the formal constructor byte-for-byte.
  - Added R7 support proving the exact `0x108` complete-object size, protected `+0xf8..+0x104` state band, natural `+0x105..+0x107` alignment, canonical `RectBounds` signature, and direct-derived-access contract.
  - Explicitly rejected private duplicate fields, invented setters, raw offsets, and handwritten compiler vptr/deleting/thunk machinery while preserving all callers, bounds behavior, vtable stores, padding, field aliases, and score caps.

- 2026-07-13 B005 UID0004BP field-contract correction:
  - Preserved `89/92`, exact range, owner/emitter, reconstructable state, `Nested:4`, signature, packed layout, and all constructor evidence.
  - Changed only the accepted formal/member spelling `m_controlVisible` to `m_enabled`, synchronized the Item Summary and field map, and retained visible/show as explicitly superseded history because Pane visibility is a separate `+0xb4` field.

- 2026-06-26 B007 accepted source-quality implementation:
  - Changed score from `85/90` to `89/92`; owner/emitter/reconstructable metadata unchanged.
  - Inserted first-draft formal `ControlPane::ControlPane(unsigned char controlType, const RectBounds *bounds)` C++ using range-safe literals `Pane(1)` and `m_visualState(10)`, with no target-local enum/type/constant declarations.
  - Evidence: B007 live MCP session `80de0a67` reconfirmed the `0xa1` constructor, `0x00494a81-0x00494a90` padding, direct callees `0x00544460`/`0x00544bd0`, vtable-store refs at `0x00494a23`/`0x00494a29`/`0x00494a33`, 52 constructor xrefs, optional `const RectBounds *` bounds route through `Pane::SetBounds(bounds, 0)`, neutral base aliases for `+0xf8/+0xfa/+0xfc`, byte-wise control-state defaults at `+0xff..+0x104`, and rejected alternatives for blank C++, local enum declarations, unowned constants, progress-specific base field names, raw bounds types, one-dword packed state, concrete-control ownership, direct file ownership, and handwritten vptr stores.
- 2026-06-06 A010 live IDA refresh: changed from `72/84` to `78/88` after confirming `lookup_funcs`, `decompile`, `disasm`, `callers`, and `callees` evidence. The refresh pins the field writes at `+0xfe`, `+0xf8`, `+0xfc`, `+0xff`, and `+0x103`, and records the direct callee set as only the `Pane` constructor plus optional bounds helper.
- 2026-06-15 A003 Goal 2 refresh:
  - Changed score from `78/88` to `85/90`.
  - Route changed from file owner/emitter [UID:0000IG][ControlPane](by-file/ControlPane.md) to class owner/emitter [UID:000038][ControlPane](by-class/ControlPane.md), because the constructor and class parent both clear the corrected `85/85` gate.
  - Summary/evidence: current IDA MCP reconfirmed `sub_4949E0` size `0xa1`, `0xcc` end padding, direct callees `sub_544460` and `sub_544BD0`, vtable-store refs at `0x00617a90/0x00617af8/0x00617b28`, packed state writes, and 52 direct callers.

## Cross-References

- [UID:0000IG][ControlPane](by-file/ControlPane.md)
- [UID:000038][ControlPane](by-class/ControlPane.md)
- [UID:000114][0x004949e0-0x00494b50.ControlPaneAndProgressBarPrelude](by-memory/0x004949e0-0x00494b50.ControlPaneAndProgressBarPrelude.md)
- [UID:0000MC][Pane](by-file/Pane.md)
- [UID:0000A2][Pane](by-class/Pane.md)
- [UID:0001YC][PaneCoreVtableFamily](by-type/by-vtable/PaneCoreVtableFamily.md)
