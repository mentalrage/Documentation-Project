*** UID:00027U | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ImageAnimationFrame g_defaultImageAnimationFrame;
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-defined 10-byte g_defaultImageAnimationFrame emitted once through MonsterImageLibTables, with exact zero-initialized/default-constructor bytes, two getter xrefs, two protected alignment bytes, and the RootPathName successor boundary documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0066deb0-0x0066debc Monster/Riding Image Default Frame Record

## Summary

- Address range: `0x0066deb0-0x0066debc`
- Entity kind: default/sentinel image frame-entry record plus alignment tail
- Disposition: reconstructable project data
- Rebuild handling: one external-linkage source definition, declared `extern` by UID0001CM and defined here after that source fragment
- Canonical owner/emitter: [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md)
- Scanner-visible filename label: `MonsterImageLibDefaultFrameRecord`. The retained filename label is accurate for the shared monster/riding default frame record returned by the table lookup helpers.
- Source-facing inferred type: `ImageAnimationFrame` fallback/sentinel record.
- Confidence: strong for bytes, xrefs, lookup behavior, shared helper ownership, and the accepted frame-offset/duration/alpha interpretation; medium for exact original record/type symbol.

This range contains the fallback 10-byte frame-entry record returned by the monster/riding image table getter helpers when an index is out of range, followed by two zero alignment bytes before `RootPathName` at `0x0066debc`.

## Layout

| Range | Accepted field | Notes |
| --- | --- | --- |
| `0x0066deb0-0x0066deb2` | `frameOffset` | Zero. Normal render callers add this signed 16-bit value to the owning row's base frame. |
| `0x0066deb2-0x0066deb4` | signed `short frameDuration` | Zero in the shared sentinel. Five scheduler/timing consumers sign-extend frame entry `+0x02`; monster default table entries seeded by `LoadMonsterTables` write positive signed value 30000 here instead. |
| `0x0066deb4-0x0066deb5` | `blendAlpha` | `0xff` in the shared sentinel; monster render treats frame entry `+0x04` as an alpha/mode byte. |
| `0x0066deb5-0x0066deb6` | `reserved` | Zero byte in the sentinel. |
| `0x0066deb6-0x0066deb8` | `auxFrameId` | Word sentinel `0xffff` / 65535 unsigned (Verified with int_convert.py), used as a signed 16-bit `-1` style sentinel. |
| `0x0066deb8-0x0066deb9` | `paletteIndex` | Zero. Name remains inferred from the shared parser/riding layout. |
| `0x0066deb9-0x0066deba` | `effectIndex` | Zero. Name remains inferred from the shared parser/riding layout. |
| `0x0066deba-0x0066debc` | alignment tail | Zero alignment tail before the next named data item. |

## Evidence

- 2026-06-14 live IDA MCP session `a001_goal2_class_batch` reconfirmed the exact bytes, two direct fallback xrefs, no interior xrefs, successor `RootPathName` refs, and LivingObjectPane successor split. `int_convert.py` verifies the documented span as `0x0c == 12` bytes and the active frame record as `0x0a == 10` bytes plus two alignment bytes.
- IDA MCP on 2026-06-02 reports `0x0066deb0` as `unk_66DEB0` with exactly two data xrefs: `0x00528b2f` and `0x00528d11`.
- Raw bytes for `0x0066deb0-0x0066debc` are `00 00 00 00 ff 00 ff ff 00 00 00 00`.
- `lookup_funcs` places `0x00528b2f` in `sub_528B10` (`0x34` bytes) and `0x00528d11` in `sub_528D00` (`0x28` bytes), matching the two compact frame-table getter helpers.
- 2026-06-14 B002 live IDA MCP recheck on session `b001_0003gy` reconfirmed the exact `0x0066deb0-0x0066debc` bytes as `00 00 00 00 ff 00 ff ff 00 00 00 00`; `make_signature_for_range` reported that sequence as unique.
- The same B002 MCP pass reconfirmed exactly two xrefs to `0x0066deb0`: `0x00528b2f` in `sub_528B10` and `0x00528d11` in `sub_528D00`. It also found no xrefs to interior offsets `0x0066deb4`, `0x0066deb6`, `0x0066deb8`, or `0x0066deba`.
- 2026-06-14 B002 MCP boundary checks found no xrefs to the predecessor padding start `0x0066de6c`, four xrefs to successor `RootPathName` at `0x0066debc`, and two xrefs to the following LivingObjectPane range start `0x0066decc`.
- 2026-06-14 B002 raw PE check mapped `0x0066deb0` to `.data` raw offset `0x26bab0`, found the 12-byte fallback record only once, confirmed the preceding `0x0066de6c-0x0066deb0` padding is 68 zero bytes, and confirmed the next bytes at `0x0066debc` are `58 3a 5c 00 58 00 3a 00 00 00 00 00 50 00 00 00`.
- `0x00528b10` is an outer-table frame lookup helper. It reads the outer entry count at `this+0x04`, selects an 8-byte child table from `this+0x08`, checks that child's word count at `+0x04`, returns `base + 10 * frame_index` when in range, and returns `offset unk_66DEB0` at `0x00528b2f` on failure.
- `0x00528d00` is the inner-table frame lookup helper. It checks `this[2]` against the requested frame index, returns `*this + 10 * index` when in range, and returns `offset unk_66DEB0` at `0x00528d11` on failure.
- `0x00528d30` initializes normal 10-byte `ImageAnimationFrame` records with frame offset/duration dword zero, byte `+0x04 = 0xff`, word `+0x06 = 0xffff`, and zero tail word; the fallback bytes match that default record shape.
- IDA MCP reports `0x0066debc` as the next named data item, `RootPathName`, with data xrefs from `0x00529304`, `0x00529309`, `0x0052a061`, and `0x0052a066`. This confirms the fallback record range ends before the root path buffer.
- The owning code page documents the `0x00528930-0x00528d51` helper island as shared by `MonsterImageLib` and `RidingImageLib` table parsing/allocation paths, so this belongs with [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md), not a monster-only or riding-only module.

## B002 Advanced-Scan Detail

The 2026-06-14 B002 advanced-scan pass treated the `MonsterImageLibDefaultFrameRecord` finding as a real scanner-visible body-label gap only. It did not find a stale filename, generated-name problem, owner/emitter problem, range problem, split/merge problem, or reclassification issue. The exact label is source-documentation useful because it names the shared default frame record that both lookup helpers return on out-of-range input; it is not an IDA-generated `unk_`/`sub_`/`DAT_` label.

Live IDA MCP session `b001_0003gy` reported the following facts for the involved helper and data ranges:

| Address or range | IDA state | B002 interpretation |
| --- | --- | --- |
| `0x00528b10-0x00528b44` | function `sub_528B10`, size `0x34` | Outer/group frame lookup helper; checks the group count at `this+0x04`, indexes an 8-byte child table, checks the child word count at `+0x04`, returns `base + 10 * frameIndex` when in range, and returns `0x0066deb0` on failure. |
| `0x00528d00-0x00528d28` | function `sub_528D00`, size `0x28` | Inner/frame lookup helper; checks `this[2]`, returns `*this + 10 * frameIndex` when in range, and returns `0x0066deb0` on failure. |
| `0x00528d30-0x00528d46` | function `sub_528D30`, size `0x16` | Normal `ImageAnimationFrame` initializer; writes the same zero `frameOffset/frameDuration`, `0xff` blend alpha, `0xffff` auxiliary sentinel, and zero-tail shape present in this fallback record. |
| `0x00528be0-0x00528c7c` | function `sub_528BE0`, size `0x9c` | Allocates the inner vector with element size `0x0a`, constructor callback `0x00528d30`, and no-op destructor callback `nullsub_13`. |
| `0x0066deb0-0x0066debc` | data, not a function | Unique 12-byte fallback record plus alignment tail. |
| `0x0066debc-0x0066decc` | data, not a function | Successor `RootPathName`/drive-root buffer starts at the next address and is not part of this record. |

B002 raw-file verification mapped the documented addresses through the `.data` section of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`:

| VA/range | Raw offset/range | Bytes or count |
| --- | --- | --- |
| `0x0066deb0` | `0x26bab0` | Start of this record. |
| `0x0066debc` | `0x26babc` | Start of successor bytes, not an interior field. |
| `0x0066decc` | `0x26bacc` | Following LivingObjectPane word-slot range. |
| `0x0066de6c-0x0066deb0` | `0x26ba6c-0x26bab0` | 68 zero bytes owned by the predecessor padding page. |
| `0x0066deb0-0x0066debc` | `0x26bab0-0x26babc` | `00 00 00 00 ff 00 ff ff 00 00 00 00`; the 12-byte pattern occurs once in the raw file. |
| `0x0066debc-0x0066decc` | `0x26babc-0x26bacc` | `58 3a 5c 00 58 00 3a 00 00 00 00 00 50 00 00 00`. |

The same raw scan found the little-endian address pattern for `0x0066deb0` twice at raw offsets `0x127f30` and `0x128112`, matching the two lookup-helper xrefs. It found the little-endian address pattern for `0x0066debc` four times at raw offsets `0x128705`, `0x12870b`, `0x129462`, and `0x129468`, matching successor `RootPathName`/drive-root refs. It found the little-endian address pattern for `0x0066decc` twice at raw offsets `0x1a7ff9` and `0x1a803f`, matching the later LivingObjectPane boundary refs.

Negative evidence from the same pass:

- No xrefs target predecessor padding start `0x0066de6c`.
- No xrefs target interior offsets `0x0066deb4`, `0x0066deb6`, `0x0066deb8`, or `0x0066deba`; all source-use routing goes through base address `0x0066deb0`.
- No owner-relevant xrefs target successor `0x0066debc`; those refs belong to the next `RootPathName`/MusicControlDialog buffer.
- No evidence currently supports splitting the span into a 10-byte source object plus an independent 2-byte by-memory item. The two zero tail bytes remain documented as alignment inside this small data item until final type recovery proves the original declaration was a 12-byte record or a 10-byte record followed by linker/source-layout padding.

## Ownership Analysis

The accepted owner remains [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md). The decisive facts are data-flow facts rather than address adjacency: both shared lookup helpers return this address, the normal frame-record initializer writes the same shape into allocated records, and the helper island is used by both `MonsterImageLib` and `RidingImageLib`.

Rejected owner candidates:

| Candidate | Reason rejected as direct owner |
| --- | --- |
| [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md) | Monster rendering/table code consumes the shared helper family, but the two direct refs to this default record are in shared helper functions and riding code uses the same helpers. Monster-only default outer-table storage is the separate [UID:00029K][0x0069b424-0x0069b430.ImageLibraryFallbackRecord](by-memory/0x0069b424-0x0069b430.ImageLibraryFallbackRecord.md), not this inner-frame fallback. |
| [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md) | Riding definition code consumes the helpers, but there are no riding-specific direct refs to this address and the record is not concrete riding-definition global storage. |
| Predecessor or successor data owners | Neighbor adjacency only proves boundaries. The predecessor has no xrefs, and successor refs belong to `RootPathName`/MusicControlDialog and later LivingObjectPane data. |

The B002 report preserved its then-current `82/86` score because the assignment was an advanced-scan label/evidence repair, not a full reconstruction audit. A later refresh raised it to `85/88`. Its recommendation to keep C++ blank was appropriate before the neutral layouts and object/alignment split were closed, but is now historical: UID0001CM supplies the exact declaration/extern contract and this page supplies the one definition.

## Reconstruction Notes

- Reconstruct this as the one external-linkage `ImageAnimationFrame` fallback/sentinel object beside the shared monster/riding table helper definitions. External linkage preserves the accepted separate emitter plus UID0001CM `extern` contract; the binary does not recover the original linkage spelling.
- Marked `RECONSTRUCTABLE:TRUE` because all bytes, lookup xrefs, and helper ownership are now IDA-confirmed.
- Attached to [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) after that file received its projected `NexusTK/render/` path; this keeps the fallback record with the shared monster/riding table helper source.
- Emit exactly `ImageAnimationFrame g_defaultImageAnimationFrame;` from this CPP channel at source position 10. H remains blank because UID0001CM already owns the complete type and extern declaration.
- Keep the two trailing zero bytes as alignment/padding outside the source object. Exact UDT size, constructor writes, zero interior refs, and successor boundary close the former 10-versus-12 blocker.

## 2026-08-17 B008 Source Object And Boundary Closure

The source-bearing object is exactly `[0x0066deb0,0x0066deba)`. Its constructor/static-zero state is `frameOffset = 0`, signed `frameDuration = 0`, `blendAlpha = 0xff`, reserved byte zero from static initialization, `auxFrameId = -1`, `paletteIndex = 0`, and `effectIndex = 0`. This reproduces bytes `00 00 00 00 ff 00 ff ff 00 00`. The frame constructor deliberately performs no reserved-byte write, so the formal definition relies on normal static zero initialization before construction.

Exactly two code references target the object, from the table-level and group-level frame getters. No reference targets either `0x0066deba` or `0x0066debb`; those one-byte zero items remain alignment. The successor item `RootPathName` begins exactly at `0x0066debc` and remains a separate protected `CHAR[]` object with four references. The predecessor zero-padding ownership also remains separate.

The object is emitted once after UID0001CM so its type declaration is already available. Concrete monster and riding default outer tables are different 12-byte objects under their established owners and are not merged into this range. Historical private/static and blank-source alternatives are retained as superseded assumptions; the accepted external definition is the only route consistent with the current separate emitter and extern declaration.

## Score Rationale

- Completion is `94` because the page records the exact 10-byte source object, two-byte alignment tail, full formal definition route, direct getter refs, no-interior-ref checks, initializer/static-zero match, owner/order, accepted fields, and protected successor boundary.
- Confidence is `94` because bytes, type size, constructor writes, fallback xrefs, helper behavior, signed consumers, and ownership all agree. The residual reflects the inferred lexical type/global names and original linkage spelling, not object size or behavior.

## UID0003U8 Signed Duration Evidence - 2026-07-15

- The sentinel's zero word at `+0x02` is the signed `short frameDuration` field of the shared `ImageAnimationFrame` contract. Its raw bytes, 10-byte active record shape, two-byte alignment tail, and owner/emitter route remain unchanged; the completed source pass now emits the exact object definition here.
- Five timing consumers at `0x0053a30b`, `0x0053aaae`, `0x0053b780`, `0x0053ba59`, and `0x005a37d6` sign-extend this field. [UID:0003U8][0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation](by-memory/0x005a3770-0x005a386a.UserPaneStartDirectionalAnimation.md) is the last consumer and schedules event `20` with the selected value.
- Zero has identical signed/unsigned encoding, while normal defaults use positive `30000`. The consumer audit resolves source signedness without changing any byte, fallback route, initializer behavior, or data-boundary conclusion.

## Cross-References

- [UID:0001CM][0x00528930-0x00528d51.MonsterImageLibTables](by-memory/0x00528930-0x00528d51.MonsterImageLibTables.md)
- [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md)
- [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md)
- [UID:0000N6][RidingImageLib](by-file/RidingImageLib.md)
- [UID:0001Z8][0x0066d000-0x0069d000.DataSection](by-memory/0x0066d000-0x0069d000.DataSection.md)

## Changes

- 2026-06-25 B001 implementation callback:
  - Score unchanged at `85/88`.
  - Evidence: synchronized this shared fallback record with accepted [UID:0002JN][0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables](by-memory/0x004dac40-0x004daebc.MonsterImageLibLoadMonsterTables.md) field names. Reinterpreted the first dword as `frameOffset` plus `frameDuration`, kept `blendAlpha` at `+0x04`, and marked `auxFrameId`/`paletteIndex`/`effectIndex` as inferred weaker fields. The C++ block remains blank pending a helper-file declaration pass.
- 2026-06-14 A001 Goal 2 score refresh:
  - Raised from `82/86` to `85/88` after live IDA MCP session `a001_goal2_class_batch` reconfirmed the exact `0x0c`/12-byte span, 10-byte fallback-record body, two alignment bytes, direct refs from `sub_528B10` and `sub_528D00`, no interior xrefs, initializer match, and `RootPathName`/LivingObjectPane successor boundaries.
  - Owner/emitter remain [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md), whose `87/88` score and `NexusTK/render/` route clear the gate. Standalone C++ remains blank pending the shared helper declaration pass.

- 2026-06-14 B002 advanced-scan repair:
  - What existed before: the page had the correct UID, range, owner/emitter route, fallback-record interpretation, and boundary evidence, but the body omitted the exact scanner-visible filename label `MonsterImageLibDefaultFrameRecord`.
  - What changed: added scanner-visible `MonsterImageLibDefaultFrameRecord` body text, documented current canonical owner/emitter wording, and added current live IDA MCP plus raw PE evidence for the exact bytes, xrefs, lookup-helper behavior, and neighbor boundaries.
  - Why: the advanced-scan finding was a real body-label gap, not a generated-name, owner/emitter, range, split, merge, or reclassification problem. No metadata, reconstruction C++, score, owner, or emitter route changed.

- 2026-06-02: Raised completion/confidence and tied the record to the shared table-helper file.
  - Before: completion/confidence were `50/65`, reconstructability was blank, and the page described the bytes as a broad 10-to-12-byte fallback record.
  - After: completion/confidence are `82/86` and `RECONSTRUCTABLE:TRUE`; legacy parent routing stayed blank until the shared helper file received a projected reconstruction path.
  - Evidence: IDA MCP confirms the raw bytes, exact fallback xrefs from both lookup helpers, normal frame-record initializer behavior, and the `RootPathName` boundary at `0x0066debc`.
- 2026-06-03 autogen consistency pass:
  - Changed parent to [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md).
  - Summary/evidence: [UID:0000LK][MonsterImageLibTables](by-file/MonsterImageLibTables.md) now has the projected `NexusTK/render/` path and already documents the shared monster/riding table helper owner; this child has `82/86`, so the parent/child attachment meets the old 80+ rule. Standalone C++ remained blank pending final helper declarations.
