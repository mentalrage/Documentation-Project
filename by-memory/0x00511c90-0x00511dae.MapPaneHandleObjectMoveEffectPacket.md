*** UID:0003TO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool MapPane::HandleObjectMoveEffectPacket(const unsigned char *packet)
{
    const unsigned int objectId = PacketBufferReadUInt32BE(packet + 1);
    const short effectDescriptorId =
        static_cast<short>(PacketBufferReadUInt16BE(packet + 5));
    const short timerAction =
        static_cast<short>(PacketBufferReadUInt16BE(packet + 7));

    if (effectDescriptorId == 134)
    {
        FlashMapColor(143);
        return true;
    }

    const int mapColumn = PacketBufferReadUInt16BE(packet + 9);
    const int mapRow = PacketBufferReadUInt16BE(packet + 11);
    const bool renderAfterLivingObjects =
        effectDescriptorId >= 160 && effectDescriptorId <= 162;

    if (m_objectList != NULL)
    {
        if (objectId == 0)
        {
            RequestCoordinateEffect(
                mapColumn,
                mapRow,
                effectDescriptorId,
                timerAction,
                renderAfterLivingObjects);
        }
        else if (m_objectList->FindByObjectId(objectId) != NULL)
        {
            RequestObjectEffect(
                objectId,
                effectDescriptorId,
                timerAction,
                renderAfterLivingObjects);
        }
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-ready opcode-0x29 MapPane handler over a trusted const-byte packet pointer; reads BE32 object id +1, signed BE16 effect descriptor +5 and timer action +7, and zero-extended BE16 column +9/row +11; descriptor 134 compiles FlashMapColor(143) policy inline, descriptors 160..162 set render-after-living ordering, null ObjectList or failed nonzero lookup produces no request, zero id routes RequestCoordinateEffect, successful nonzero id gates then calls RequestObjectEffect with its intentional repeated lookup, and every path returns true without packet-length checks; exact range/hash/two callers/padding/source placement and rejected raw/decompiler/extra-check alternatives preserved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00511c90-0x00511dae MapPane Handle Object Move Effect Packet

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact MapPane packet-handler child split from [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md).
- Direct owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md); file owner [UID:0000L3][MapPane](by-file/MapPane.md).
- Source-facing name: `MapPane::HandleObjectMoveEffectPacket`; `MapPaneHandleObjectMoveEffectPacket` remains the page title/descriptive alias.
- Packet route: opcode `0x29` object move/effect packet in [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md).
- Source placement: `NexusTK/map/MapPane.cpp`, owned and emitted by UID00007Q; the body is a MapPane packet handler rather than ObjectList, EffectObjectPane, packet-buffer, or compiler support.
- Confidence: exact for range, bytes, ABI, dispatcher/action-handler callers, field offsets and signedness, control flow, callees, early flash policy, null/miss behavior, unconditional success, ownership, and adjacent padding; source lexemes and the compiler-inlining explanation retain the bounded cap reflected by `92/94`.
- Reconstruction note: the former blank-C++/open-packet-declaration conclusion is historical. Direct target, caller, helper-family, promotion, and compiler-shape controls now support the complete managed definition above without inventing packet wrappers, extra checks, or helper bodies.

## UID0003TE Effect Route Synchronization - 2026-07-15

- Packet field offsets are exact: object id `+1` BE32 narrows to `unsigned int`; effect descriptor id `+5` and `timerAction` `+7` are BE16 values followed by `movsx`/`cwde`, fixing both source-facing locals as signed `short`; map column `+9` and map row `+11` use zero extension into `int`. Normal integer promotions feed the MapPane helper signatures.
- Descriptor 134 retains the separate `m_mapFlashColor = 143`, invalidate/present, `Sleep(100)`, clear, invalidate/present sequence. Only descriptor ids 160 through 162 produce `renderAfterLivingObjects == true`; the former `compactFlag` wording is superseded.
- Object id zero calls `RequestCoordinateEffect(mapColumn, mapRow, effectDescriptorId, timerAction, renderAfterLivingObjects)`. A nonzero id is resolved through ObjectList and calls `RequestObjectEffect`; lookup failure produces no effect request.
- UID0003TE is source-ready at `92/94`: it applies clamped half-open bounds, copies EffectInfo once, processes secondary before primary, reverse-scans row buckets, reuses a same-column/effect-id/looping pane, or creates a null-target pane with row before column and sequence 0/1.
- UID000232 and UID0003TF retain their complete source bodies with the machine-accurate row-before-column create contract. UID0003TG remains a verify-only dependency in this callback because B004 independently owns its current source-quality research.
- The 2026-07-15 B005 synchronization preserved the then-current `86/90` and blank formal while broader source reconstruction remained open. This B004 callback supersedes only that historical score/formal state with `92/94` and the complete managed body; owner/emitter UID00007Q, exact range, `Nested:0`, dispatcher/action-handler callers, effect-helper contracts, and adjacent padding remain unchanged.

## UID00037X FlashMapColor Inline Analog

- Opcode `0x29` descriptor `134` reaches exact inline sequence `0x00511d61-0x00511da3`. The handler stores draw color `143` in MapPane dword `+0x4c8`, invalidates the whole pane, presents through `g_pScreenPane`, sleeps 100 ms, clears the dword, invalidates, and presents again.
- UID0004Q9 is the unique independently compiled out-of-line `MapPane::FlashMapColor(int drawColor)` body with the same policy and a general color argument. There is no surviving binary call edge from this packet body, but the exact constant-143 policy match and compiler controls make a human `FlashMapColor(143)` source call that MSVC inlined the highest-probability source explanation. A second handwritten seven-statement flash sequence is the bounded runner-up and is rejected from emitted source.
- UID00037V render code reads the same dword and passes nonzero values to `GrafPort::SetDrawColor(int)`, fixing the field identity as `m_mapFlashColor` rather than generic runtime/effect state.
- The lack of an inbound route to UID0004Q9 remains relevant liveness evidence only. Exact independent ABI/padding, unique body, this inline analog, and the render consumer prove retained source.
- UID0004Q9 remains a separate source method despite zero inbound binary xrefs; this page emits only the source call, not a duplicate helper body. Opcode/effect ownership and unrelated descriptor branches remain unchanged.

## Address Range

- Start: `0x00511c90`
- End: `0x00511dae` end-exclusive
- IDA function: `sub_511C90`
- Size: `286` bytes; SHA256 `61798E098C8CAD9233072C701F85F1A8E811A9950C66A23B87BD8724C6D4F662`.
- Predecessor alignment byte: `[0x00511c8f,0x00511c90)`, one `0xCC`, SHA256 `1DD8312636F6A0BF3D21FA2855E63072507453E93A5CED4301B364E91C9D87D6`.
- Followed by ignored padding `0x00511dae-0x00511db0` before sibling child [UID:0003TP][0x00511db0-0x0051283a.MapPaneHandleObjectWorldMapCreationPacket](by-memory/0x00511db0-0x0051283a.MapPaneHandleObjectWorldMapCreationPacket.md).
- Successor padding is exactly two `0xCC` bytes with SHA256 `E3966E3275BE536A16092EC0CADF1638F718218E616FBBE8FF1C5E67FFF4DEF2`; no split, merge, rename, or child-range correction is required.

## ABI And Control Flow

- The body contains `106` decoded instructions in `8` basic blocks with cyclomatic complexity `3`.
- It is a one-argument MapPane `__thiscall`: `ECX` carries `this`, the stack argument is a trusted `const unsigned char *packet`, and the function returns a boolean in `AL` with `retn 4`.
- Three physical epilogues set `AL = 1`; the descriptor-134 early branch and both ordinary-flow exits therefore implement an unconditional `true` source return.
- No path reads packet byte `0`. The dispatcher supplies the normal opcode packet, while UID0003TQ synthesizes only offsets `+1..+12`; the target's trusted-pointer contract makes both callers source-equivalent.
- The descriptor-134 branch reads only through offset `+8`, so it conditionally requires nine addressable bytes including unused byte `0`. The ordinary path reads through offset `+12`, requiring thirteen. There is no packet-length, packet-null, object-list construction, assertion, clamp, or bounds check in the target.

## Behavior

This packet handler parses an object id, signed descriptor/effect id, signed `timerAction`, and conditionally read coordinates from the trusted packet bytes, then routes either the flash policy, a coordinate effect, an object-specific effect, or an intentional no-op before returning `true`.

- `PacketBufferReadUInt32BE(packet + 1)` yields the unsigned object id. `PacketBufferReadUInt16BE` at `+5` and `+7`, followed by target sign extension, yields signed `short` descriptor and timer action. The ordinary path reads column `+9` and row `+11` as zero-extended `int` values.
- Descriptor `134` branches before either coordinate read. Machine code stores `143` at MapPane `+0x4c8`, invalidates the whole pane, presents through `g_pScreenPane`, calls `Sleep(100)`, clears the field, invalidates, presents again, and returns true. The exact inline byte interval `[0x00511d61,0x00511da3)` hashes to `0524B50E9E6EB71D9181C098526077E632B683868ADADBAFE1E9BC13876CAAE0`; the source-ready body expresses this compiler-inlined policy as `FlashMapColor(143)`.
- The ordinary path computes `renderAfterLivingObjects` as true only for the inclusive signed descriptor interval `160..162`.
- A null `m_objectList` at MapPane `+0x424` suppresses both request paths without changing the success return.
- If object id is zero, calls [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md) / `RequestCoordinateEffect(mapColumn, mapRow, effectDescriptorId, timerAction, renderAfterLivingObjects)`.
- If object id is nonzero, `m_objectList->FindByObjectId(objectId)` is a truth gate. A miss intentionally produces no request. A hit calls [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) / `RequestObjectEffect(objectId, effectDescriptorId, timerAction, renderAfterLivingObjects)`; that helper intentionally repeats the lookup, and the managed source preserves the double-lookup behavior.
- Every branch returns `true`, including null-list and failed-lookup no-op paths.

## Evidence

- IDA function `sub_511C90` covers `0x00511c90-0x00511dae`.
- Direct dispatcher call at `0x005082f6` from the MapPane packet dispatcher case for opcode `0x29`.
- Nested direct call from [UID:0003TQ][0x00512960-0x00512ca4.MapPaneHandleActionPacket](by-memory/0x00512960-0x00512ca4.MapPaneHandleActionPacket.md) at `0x00512c04`, where action packet handling reuses this object-move/effect route.
- Raw PE scan found rel32 branch hits at `0x005082f6` and `0x00512c04`, and no unrelated pointer-table owners.
- UID0003TQ's nested caller builds the trusted packet-shaped local by writing offsets `+1..+12` and deliberately leaves byte `0` uninitialized; the target never reads that byte.
- Seven direct callee identities are resolved: BE32 packet read, BE16 packet read, ObjectList `FindByObjectId`, `RequestObjectEffect`, `RequestCoordinateEffect`, Surface presentation through `g_pScreenPane`, and Win32 `Sleep`.
- The independent UID0004Q9 body hashes to `16C7485D6D080A267BD05D9AD9FC0E8FA35932673D505E64C0E79E23960EEAED`. Its no-xref status is preserved; the exact target policy match is source-shape evidence for compiler inlining, not a fabricated binary call edge.

## Ownership Decision

This body belongs to `MapPane`. The only external dispatcher route is the MapPane server packet dispatcher, and the handler updates MapPane fields/object-list state before delegating to existing MapPane effect helpers. EffectObjectPane-related callees own constructed object implementations, not this packet parser.

The canonical source route is `NexusTK/map/MapPane.cpp`. Packet-buffer helpers retain endian decoding ownership; ObjectList retains lookup ownership; UID0003TE/UID000232 retain effect-construction ownership; UID0004Q9 retains flash-policy source ownership. This target owns the opcode-specific parse, branch, and request policy and therefore remains owner/emitter UID00007Q with blank emitter position and `Nested:0`.

## Negative Evidence And Rejected Alternatives

- No third caller, function-pointer route, import/export route, relocation owner, or separate packet class was found. The exact two direct calls and lack of data xrefs bound liveness.
- Reject decompiler-shaped raw byte offsets, `sub_` names, explicit vtable slots, manual byte swaps, compiler temporaries, and register/stack artifacts in formal C++ because typed packet helpers and named MapPane methods preserve the observed code shape.
- Reject a packed packet struct: the nested caller does not initialize byte `0`, and the target's conditional nine/thirteen-byte consumption is more accurately represented by a trusted byte pointer plus offset helper calls.
- Reject an unsigned descriptor or timer action: target `movsx`/`cwde` operations preserve signed 16-bit semantics across the full input domain. Column and row remain zero-extended.
- Reject an explicit duplicate of the seven-statement flash sequence. `FlashMapColor(143)` is the highest-probability human source form; compiler inlining explains the absent call edge while retaining UID0004Q9 as an independently emitted function.
- Reject packet-null/length checks, object-list assertions, coordinate clamps, failed-lookup errors, deduplicating the object lookup, reordering row/column, or changing no-op branches. None exists in the binary and each would alter behavior or code generation.
- Historical `86/90`, unsigned-descriptor, blank-C++, source-parallel-only flash, and unresolved packet-declaration statements remain provenance for the former documentation state, not current blockers.

## Score Rationale

Completion is `92` because exact bytes/range/padding, ABI and CFG, both callers, conditional read extent, all field offsets and signedness, branch policy, helper identities, null/miss/double-lookup behavior, ownership/source route, complete managed C++, negative evidence, rejected alternatives, and historical corrections are documented. Confidence is `94` because live IDA, raw bytes, caller synthesis, sibling helper contracts, generated-source controls, and the independent FlashMapColor body converge. It remains below `95` because stripped method/constant lexemes and the precise optimizer decision that inlined `FlashMapColor(143)` are inferred rather than directly named by symbols or source.

## Cross-References

- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md)
- [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md)
- [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-15 B004 UID0003TO accepted source-quality callback:
  - Raised the target from `86/90` to `92/94` without changing UID, owner/emitter, exact range, reconstructability, position, or nesting.
  - Installed the complete source-ready `MapPane::HandleObjectMoveEffectPacket(const unsigned char *packet)` definition with signed descriptor/action narrowing, conditional coordinate reads, compiler-inlined `FlashMapColor(143)` source call, null/miss suppression, preserved double lookup, no added checks, and unconditional success.
  - Added exact byte/padding hashes, ABI/CFG/caller/callee/liveness evidence, source placement, historical corrections, negative evidence, rejected alternatives, and bounded score rationale.

- 2026-07-15 B005 UID0003TE accepted source-quality callback:
  - Set the exact Item Summary and synchronized packet field names/types, descriptor 134 flash behavior, descriptor 160..162 render-after meaning, and zero/nonzero object-id helper routing.
  - Replaced current raw/compact call wording with `RequestCoordinateEffect(mapColumn, mapRow, ...)` and `RequestObjectEffect(...)`; retained blank formal, score, owner/emitter, range, callers, and broader packet declaration blockers.
  - Recorded UID0003TG as unchanged verify-only support under B004's separate research lane.

- 2026-06-20 B007 object-effect descriptor implementation support:
  - Replaced raw `sub_50E320` call wording with `MapPane::RequestObjectEffect(objectId, descriptor, updateArg, compactFlag)` while preserving the object-id branch versus coordinate-effect sibling branch.
- 2026-06-21 B008 source-quality wording sync:
  - Replaced remaining `updateArg` wording for UID 000232 calls with `timerAction`, matching the object-effect dispatcher and timer-action helper semantics while preserving broader packet-field uncertainty outside the effect route.
- 2026-06-16 B001 continuation batch8: split from [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md) as direct-call opcode `0x29` MapPane object move/effect packet handler.
