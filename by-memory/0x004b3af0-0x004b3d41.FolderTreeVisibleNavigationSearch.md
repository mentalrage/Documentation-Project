*** UID:000347 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct FolderTreeNavigationContext
{
    TreeItor<FolderTreePane::TreeElem> candidate;
    TreeItor<FolderTreePane::TreeElem> target;
    bool returnPrevious;
    bool found;
};

template <>
template <>
void Tree<FolderTreePane::TreeElem>::Traverse<FolderTreeNavigationContext>(
    TreeItor<FolderTreePane::TreeElem> *start,
    FolderTreeNavigationContext *context)
{
    std::deque<int> pending;
    pending.push_back(start->GetIndex());

    bool targetSeen = false;

    while (!pending.empty())
    {
        const int nodeIndex = pending.back();
        pending.pop_back();

        TreeItor<FolderTreePane::TreeElem> current(this, nodeIndex);

        if (context->returnPrevious)
        {
            if (current.GetTree() == context->target.GetTree() &&
                current.GetIndex() == context->target.GetIndex())
            {
                context->found = context->candidate.GetIndex() != -1;
                break;
            }

            context->candidate = current;
        }
        else if (targetSeen)
        {
            context->candidate = current;
            context->found = true;
            break;
        }
        else if (current.GetTree() == context->target.GetTree() &&
                 current.GetIndex() == context->target.GetIndex())
        {
            targetSeen = true;
        }

        TreeNode<FolderTreePane::TreeElem>& treeNode = m_nodes[nodeIndex];
        if (!treeNode.data.expanded || treeNode.lastChildIndex == -1)
            continue;

        for (int childIndex = treeNode.lastChildIndex; childIndex != -1;
             childIndex = m_nodes[childIndex].previousSiblingIndex)
        {
            pending.push_back(childIndex);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact FolderTree visible-navigation `Tree<T>::Traverse<Context>` specialization: deque-backed depth-first traversal, previous/next target search, candidate iterator updates, payload expansion through `.data`, and generic child/sibling links. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3af0-0x004b3d41 FolderTreeVisibleNavigationSearch

## Current Accepted UID0004YK Consumer Policy

This body remains FolderTreePane-owned visible-navigation policy over the generic Tree traversal shape. Its storage references now use Tree<T>::m_nodes; previous/next target selection, candidate updates, expansion behavior, and the exact 0x004b3af0-0x004b3d41 range remain consumer-specific. The shared deque depth-first skeleton is authored once in Tree.h.

## Status

- Disposition: reconstructable NexusTK source helper.
- Entity kind: `FolderTreePane` visible-tree traversal helper for keyboard navigation.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: file-local helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). The only ordinary callers are two sites in `FolderTreePane::OnKeyEvent` at `0x004b2b80`.
- Rebuild handling: `source-authored`; this is folder-tree keyboard-navigation search logic.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b3af0-0x004b3d41` | `FolderTreeVisibleNavigationSearch` | Traverses visible nodes to locate the previous/next visible candidate around the current iterator, updating caller-provided search state flags. |

## IDA Evidence

- 2026-06-10 IDA MCP `lookup_funcs 0x004b3af0` reports `sub_4B3AF0`, size `0x251`, giving exact exclusive end `0x004b3d41`.
- 2026-06-10 IDA MCP `callers 0x004b3af0` reports calls at `0x004b2c49` and `0x004b2d49`, both inside `sub_4B2B80`, the documented `FolderTreePane::OnKeyEvent` path.
- 2026-06-10 IDA MCP `callees 0x004b3af0` reports traversal/container helpers `0x004a88e0`, `0x00439e00`, `0x005c74f6`, and `0x0043e0b0`.
- 2026-06-10 decompilation shows a traversal queue over visible tree nodes. It compares each visited node/iterator against a caller-provided current/target iterator state, stores candidate iterator fields into that state, sets found/done bytes at offsets in the state object, and descends into children only when the visited node's expanded byte at `TreeElem +0x1d` is set.
- The two `OnKeyEvent` call sites correspond to opposite navigation searches, and the state bytes at `+0x18/+0x19` represent search mode/completion rather than persistent tree-node fields.

## Ownership And Parent Gate

The direct semantic owner is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). This helper is only reached from `FolderTreePane::OnKeyEvent` and searches the pane's visible tree for keyboard movement; no narrower class/type page owns it. The file parent is `89/85`, so the strict direct-parent gate is satisfied.

## Reconstruction Notes

- UID000149 factorization: `pending.push_back(start->GetIndex())` calls shared out-of-line `std::deque<int>::push_back` at `0x004b3b6e`; later child-index appends inline the same push logic and call `_Growmap(1)` at `0x004b3c89` only on demand. Preserve the native source expression and no custom Deque dependency.

- Final source should model this as a private visible-node search helper used by up/down/home/end keyboard handling.
- Emit the accepted first-draft traversal body above. The behavior is clear, but exact state-object and helper names remain inferred and should be renamed only when stronger source evidence appears.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 86 | Exact range, two keyboard caller sites, callee set, visible-node traversal, target/candidate state updates, expanded-child descent, direct file owner, and parent gate are documented. |
| Confidence | 88 | IDA confirms the boundary and caller set; decompiled logic matches keyboard visible-node search. Confidence remains below final audit because original state/helper names are inferred. |
| Reconstructable | true | Source-owned folder-tree navigation helper required by rebuilt `FolderTreePane`. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis keeps this as a file-local previous/next visible navigation helper used by `FolderTreePane::OnKeyEvent`. Its source-facing behavior is visible-row search over `TreeItor<FolderTreePane::TreeElem>` rather than an independent container algorithm. The traversal uses the accepted field names `firstChildIndex`, `nextSiblingIndex`, `parentIndex`, `expanded`, and `hidden` / `skipVisibleTraversal`.

The helper belongs in `FolderTreePane.cpp` with the control's key-navigation logic. It should not be emitted as a standalone template source or renamed into a public `FolderTreePane` API.

## Historical Superseded 2026-06-30 B010 Accepted First-Draft Body
The accepted FolderTreePane callback promotes this page from an empty marker to first-draft formal C++ for `Tree<FolderTreePane::TreeElem>::FindVisibleNavigationNode`. MCP evidence anchors the body at `sub_4B3AF0` size `0x252`, with visible-depth-first traversal, previous/next selection search, iterator candidate storage, and expanded-child descent gating. The route remains this by-memory specialization until a scored shared `Tree<T>` template source owner is accepted.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)

## 2026-07-29 UID0002MZ Node/Payload Navigation Reconciliation

- Formal CPP is the consumer-specific `Traverse<FolderTreeNavigationContext>` specialization in `FolderTreePane.cpp`; no navigation-specific method enters generic `Tree<T>`.
- Generic child/sibling links come from `TreeNode<TreeElem>` while expansion state is `treeNode.data.expanded`.
- Exact two `OnKeyEvent` callsites, candidate/target iterator comparison, previous/next mode, found state, deque traversal, range, and score remain unchanged at `89/90`, UID0000JG.
- The former `FindVisibleNavigationNode` generic API and monolithic `TreeElem` link layout remain historical assumptions superseded by the shared-header model.

## Changes

- 2026-07-12 B003 UID000149 factorization sync: preserved score/route/body and added exact initial-call/later-inline `std::deque<int>` evidence; `<deque>` is supplied once by UID0001WP.

- 2026-06-10 B001-028 split repair:
  - What existed before: this exact keyboard navigation search helper was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b3af0-0x004b3d41`.
  - Summary/evidence: IDA MCP confirms the `0x251`-byte function, two `OnKeyEvent` callers, visible-node traversal, candidate-state updates, and expanded-child descent.
