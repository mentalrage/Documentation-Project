*** UID:0001EG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000A5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000A5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ParcelIconPane::ParcelIconPane()
    : Pane(1),
      Singleton<ParcelIconPane>(),
      m_totalParcelCount(0),
      m_leftParcelCount(0),
      m_rightParcelCount(0),
      m_parcelPane(NULL)
{
    m_parcelPane = new ParcelPane;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `sub_545E40` constructor span `0x00545e40-0x0054606a`, two `0x104` outer-object allocation callers, direct Pane and `Singleton<ParcelIconPane>` base construction, three byte counters, child pointer at `+0xfc`, intentionally uninitialized inactive final int at `+0x100`, `0x124` child `ParcelPane` allocation, and source C++ that expresses inherited Singleton publication and child construction without compiler vptr/EH or inlined child internals. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00545e40-0x0054606a ParcelIconPane Constructor

## B001 UID0002ZO Accepted Callback - 2026-07-21

- Exact range `[0x00545e40,0x0054606a)`, size `0x22a`, has two MainUiGraph callers that allocate `0x104` bytes for the outer object.
- Source initializes direct `Pane(1)` and empty `Singleton<ParcelIconPane>()`, zeros the three count bytes, initializes `m_parcelPane` to `NULL`, and allocates one `0x124` ParcelPane child. `m_notificationState` at `+0x100` is intentionally not initialized.
- Publication/fallback at `0x00545e91/0x00545e98`, all vptr writes, inlined child-constructor mechanics, cookie, and EH cleanup are compiler lowering; no handwritten singleton assignment or child-vptr setup is emitted.
- Exact allocations, direct bases, fields, callers, and complete class declaration close the source shape at `92/94`, position 20. The existing accepted formal is preserved.

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: constructor / child-pane factory
- IDA function: `sub_545E40`, size `0x22a`
- Likely owner: [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md)
- Likely source module: [UID:0000MF][ParcelPane](by-file/ParcelPane.md)
- Autogen parent: attached directly to [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md); that class now clears the 85/85 direct-parent gate and remains attached to [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
- Rebuild handling: source-authored constructor for the parcel notification UI. Formal C++ names the direct `Singleton<ParcelIconPane>` base and uses `new ParcelPane`; singleton publication, vtable stores, stack-cookie/EH machinery, unwind funclets, and inlined child-constructor internals are compiler output.
- Confidence: very strong for the executable boundary, xrefs, vtable writes, singleton writes, child allocation/setup, source-family routing, and target-level first-draft C++ shape; capped below final audit only because the exact original private member spelling for the child pointer is inferred as `m_parcelPane`.

## Behavior

This constructor initializes the outer parcel icon pane and its direct empty `Singleton<ParcelIconPane>` base, whose inlined construction publishes [UID:0000RX][g_pParcelIconPane](by-global/g_pParcelIconPane.md). It installs the three `ParcelIconPane` vtable views, clears the byte counters at `+0xf8/+0xf9/+0xfa`, allocates a `0x124` / 292-byte child [UID:0000A6][ParcelPane](by-class/ParcelPane.md), initializes the inlined child state, stores the child pointer at `+0xfc`, and publishes [UID:0000RY][g_pParcelPane](by-global/g_pParcelPane.md). The inactive outer dword at `+0x100` is not initialized by either construction caller and is intentionally absent from the initializer list.

The binary inlines the child `ParcelPane` constructor after `operator new`, but the source-level constructor should not duplicate that inlined child setup. [UID:0002KB][0x00546290-0x00546436.ParcelPaneConstructor](by-memory/0x00546290-0x00546436.ParcelPaneConstructor.md) already emits accepted `ParcelPane::ParcelPane()` source for the field setup, `SetParcelSlotData(0,0,0)`, `SetPaneOrder(nullptr, g_pBackPane)`, and timer scheduling. This target therefore expresses the child construction as `m_parcelPane = new ParcelPane;`.

## Boundary Evidence

- 2026-06-15 live IDA MCP `lookup_funcs` confirms `sub_545E40` with size `0x22a`, and confirms the exclusive end `0x0054606a` is not a function start.
- 2026-06-15 live IDA MCP `xrefs_to 0x00545e40` reports the two construction callers in `sub_4F7D10` at `0x004f848c` and `0x004f89f3`.
- 2026-06-15 live IDA MCP decompilation shows `ParcelIconPane` vtable writes at `0x00545eab`, `0x00545eb1`, and `0x00545ebb`, then child `ParcelPane` vtable writes at `0x00545f27`, `0x00545f2d`, and `0x00545f37`.
- 2026-06-15 live IDA MCP `xrefs_to` for the three `ParcelIconPane` vtables reports constructor stores to `0x00621bb4`, `0x00621c00`, and `0x00621c30`, destructor stores from `0x00546070`, and scalar-destructor stores from `0x00546f40`.
- 2026-06-15 live IDA MCP `xrefs_to` for `g_pParcelIconPane` and `g_pParcelPane` confirms this constructor writes `0x0069b4d0` at `0x00545e91/0x00545e98` and `0x0069ba28` at `0x00545f03/0x00545f0a`.
- 2026-06-15 live IDA MCP `callees 0x00545e40` reports Pane construction, `operator new`, rectangle initialization, pane/layer helpers, timer setup, and the security-cookie check.
- 2026-06-29 B006 live MCP session `b2ae72ec` reconfirmed one active IDB session for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, worker PID `16112`, `server_health` status `ok`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- 2026-06-29 B006 `lookup_funcs(database=b2ae72ec)` reconfirmed `0x00545e40 -> sub_545E40` with size `0x22a`, `0x0054606a` as not a function, and successor `0x00546070 -> sub_546070`.
- 2026-06-29 B006 `int_convert(database=b2ae72ec)` verified `0x22a == 554`, `0x124 == 292`, `0xf8 == 248`, `0xfc == 252`, `0x100 == 256`, `0x110 == 272`, `0x120 == 288`, and `0x121 == 289`.
- 2026-06-29 B006 `search_text` over `sub_4F7D10` found exactly two `call sub_545E40` hits at `0x004f848c` and `0x004f89f3`, matching the construction-call evidence.
- 2026-06-29 B006 `callees 0x00545e40` returned `sub_544460`, `sub_4F4AA0`, `sub_4B7C50`, `sub_544C50`, `sub_544D30`, `sub_5975E0`, and `@__security_check_cookie@4`.
- 2026-06-29 B006 `xrefs_to` for child `ParcelPane` vtables confirmed this constructor writes `0x00621c3c` at `0x00545f27`, `0x00621c90` at `0x00545f2d`, and `0x00621cc0` at `0x00545f37`; `xrefs_to 0x00546290` reported no direct xrefs, matching the accepted standalone child constructor source method whose body is inlined here.
- Active generated output previously showed this UID as an `Empty Emitter Marker`; after this implementation and validator refresh, generated `ParcelPane.cpp` should emit `ParcelIconPane::ParcelIconPane()` for UID0001EG. The broader [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md) class page may still remain a class-level empty marker.

## Ownership Notes

Keep this under `ParcelIconPane` while grouping it into the original `ParcelPane.cpp` source module. The child `ParcelPane` inline construction explains why generated ownership can blur the two classes in this range.

The constructor has a dual role in source reconstruction: it is the `ParcelIconPane` constructor body, while the optimized binary also contains inlined child `ParcelPane` allocation/setup and publishes both parcel singletons. The direct owner is [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md), which clears `85/86`; that class remains under the source-module parent [UID:0000MF][ParcelPane](by-file/ParcelPane.md), which documents the three-class parcel notification family. The formal C++ intentionally uses `m_parcelPane = new ParcelPane;` instead of copying the inlined child setup, vptr stores, security-cookie code, or constructor unwind details.

## Source C++ Disposition

Selected disposition: first-draft source body. The accepted constructor initializes `Pane(1)`, direct base `Singleton<ParcelIconPane>()`, the three byte counters, and inferred child pointer `m_parcelPane`, then allocates the child `ParcelPane`. It does not duplicate the Singleton base's inlined global publication and does not invent initialization for the inaccessible final dword.

Rejected dispositions:

- Comment-only formal marker: rejected because this is a real source-authored constructor with two construction callers and enough source-quality evidence to express the target without unstable helper signatures.
- Clearing `EMITTER_UIDS` or making the page non-emitting: rejected because this range is not padding, a broad aggregate, compiler cleanup, an adjustor thunk, or a scalar deleting wrapper. It is the `ParcelIconPane` constructor and should emit through UID `0000A5`.
- Keeping blank formal C++ with a nonblank emitter: rejected because generated `ParcelPane.cpp` treated UID0001EG as an empty emitter even though the constructor is source-ready.
- Hand-copying the inlined child `ParcelPane` setup: rejected because [UID:0002KB][0x00546290-0x00546436.ParcelPaneConstructor](by-memory/0x00546290-0x00546436.ParcelPaneConstructor.md) owns and emits the source-level `ParcelPane::ParcelPane()` body. Copying the inlined vptr/field/timer sequence here would double-author child behavior and encode compiler optimization artifacts as handwritten source.

Use the adjacent parcel pages to preserve the split:

- [UID:0001EH][0x00545e40-0x005470ad.ParcelNotificationPanes](by-memory/0x00545e40-0x005470ad.ParcelNotificationPanes.md) is the aggregate parcel notification executable span.
- [UID:0001EI][0x00546070-0x005460e8.ParcelIconPaneDestructor](by-memory/0x00546070-0x005460e8.ParcelIconPaneDestructor.md), [UID:0001EJ][0x005461c0-0x0054628d.ParcelIconPaneParcelUpdateCallback](by-memory/0x005461c0-0x0054628d.ParcelIconPaneParcelUpdateCallback.md), and [UID:0001EO][0x00546f40-0x00546ff9.ParcelIconPaneScalarDeletingDestructor](by-memory/0x00546f40-0x00546ff9.ParcelIconPaneScalarDeletingDestructor.md) carry the other `ParcelIconPane` executable bodies.
- [UID:0001YE][ParcelNotificationVtableFamily](by-type/by-vtable/ParcelNotificationVtableFamily.md) and [UID:0001VI][ParcelNotificationPaneLayouts](by-type/by-struct/ParcelNotificationPaneLayouts.md) record the vtable/layout constraints that explain the secondary-view callback and child `ParcelPane` size.

## Cross-References

- [UID:0001EH][0x00545e40-0x005470ad.ParcelNotificationPanes](by-memory/0x00545e40-0x005470ad.ParcelNotificationPanes.md)
- [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md)
- [UID:0000A6][ParcelPane](by-class/ParcelPane.md)
- [UID:0000MF][ParcelPane](by-file/ParcelPane.md)
- [UID:0000RX][g_pParcelIconPane](by-global/g_pParcelIconPane.md)
- [UID:0000RY][g_pParcelPane](by-global/g_pParcelPane.md)

## Changes

- 2026-06-29 B006 accepted ParcelIconPane constructor implementation:
  - Changed metadata from `COMPLETION:86`, `CONFIDENCE:90` to `COMPLETION:90`, `CONFIDENCE:92`; preserved `CANONICAL_OWNER:0000A5`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000A5`.
  - Inserted the then-current formal `ParcelIconPane::ParcelIconPane()` C++. It used `Pane(1)`, cleared icon-side parcel counters, initialized `m_parcelPane`, manually published `g_pParcelIconPane`, and assigned `m_parcelPane = new ParcelPane`. The manual publication is superseded by the direct Singleton-base source shape below.
## 2026-07-15 B001 UID0001EO Accepted Callback Synchronization

- Current metadata is `91/93`, owner/emitter [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md), reconstructable true, blank optional position, and `Nested:0`.
- Both callers in `sub_4F7D10` allocate `0x104` bytes before invoking this constructor. The scalar wrapper's size path independently uses `0x104`, closing the complete-object size.
- RTTI identifies direct non-virtual bases `Pane` and `Singleton<ParcelIconPane>`, with the Singleton PMD at `mdisp=0xf8`; project-era empty-base optimization leaves the three byte counters at `+0xf8..+0xfa` while Singleton construction still owns publication of `g_pParcelIconPane`.
- The current source body therefore adds `Singleton<ParcelIconPane>()` and removes `g_pParcelIconPane = this`. That store remains preserved as observed binary behavior generated by inlined direct-base construction, not discarded behavior.
- The child pointer remains `m_parcelPane` at `+0xfc`; `new ParcelPane` is the only handwritten child-construction expression. Inlined child vptr, fields, timer, and `g_pParcelPane` publication regenerate through the child constructor and must not be copied here.
- The final dword at `+0x100` has no read or write in the constructor, ordinary destructor, scalar wrapper, callback, or whole-project bounded operand searches. The class declaration represents the highest-ranked source-era shape as `int m_notificationState`, but this constructor deliberately leaves it uninitialized.
- Rejected source forms include manual Singleton publication, explicit vptr stores, explicit EH/unwind code, hand-copied child setup, a reserved-byte tail, a pointer at `+0x100`, or invented default state.
  - Evidence: accepted B006 report and current MCP session `b2ae72ec` prove the exact half-open range, `0x22a` / 554-byte size, exclusive-end non-function, two `sub_4F7D10` call sites, direct `ParcelIconPane` vtable/global ownership, `+0xf8/+0xf9/+0xfa` counter fields, `+0xfc` child pointer, `0x124` / 292-byte child allocation, child `ParcelPane` vtable writes, `g_pParcelPane` publication, and accepted source route through [UID:0000MF][ParcelPane](by-file/ParcelPane.md). [UID:0002KB][0x00546290-0x00546436.ParcelPaneConstructor](by-memory/0x00546290-0x00546436.ParcelPaneConstructor.md) already owns the child constructor source body, so this target must use `new ParcelPane` rather than duplicating compiler-inlined child setup or vptr/EH details.
- 2026-06-15 A003 Goal2 parcel icon pane refresh:
  - Before: completion/confidence were `80/86`, and owner/emitter were attached to [UID:0000MF][ParcelPane](by-file/ParcelPane.md) because the class page was historically below gate.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:90`, `CANONICAL_OWNER:0000A5`, and `EMITTER_UIDS:0000A5`.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x22a` constructor boundary, exclusive-end non-function, two `sub_4F7D10` callers, `ParcelIconPane` and child `ParcelPane` vtable writes, `g_pParcelIconPane` and `g_pParcelPane` publication, child `0x124` allocation/setup, timer setup, and current parent support pages. The direct class route became defensible because [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md) clears `85/86`; this pass's historical blank-C++ hold is superseded by the 2026-06-29 B006 implementation sync above.
- 2026-06-26 B007 stale gate cleanup:
  - Removed the stale item-summary/current-history implication that this page still attaches to [UID:0000MF][ParcelPane](by-file/ParcelPane.md) because [UID:0000A5][ParcelIconPane](by-class/ParcelIconPane.md) is below gate.
  - Superseded 2026-06-26 policy: preserve header owner/emitter `0000A5`; source-family placement remains [UID:0000MF][ParcelPane](by-file/ParcelPane.md); formal C++ was left blank because child allocation/helper/member names and private/helper declaration shape were not yet source-final, not because of an obsolete `95/95` rule. The 2026-06-29 B006 implementation resolves the target-level constructor C++ hold with `m_parcelPane = new ParcelPane`.
  - Report context: this sync is paired with [UID:00043J][0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper](by-memory/0x005460f0-0x005461c0.ParcelIconPaneParcelUpdateRawHelper.md) creation and [UID:0001EH][0x00545e40-0x005470ad.ParcelNotificationPanes](by-memory/0x00545e40-0x005470ad.ParcelNotificationPanes.md) reclassification as a non-emitting aggregate.
- 2026-06-06 A005 parent/coverage reconciliation:
  - Before: completion was `70`, `AUTOGEN_PARENT_UID` was blank, and the page named both owner class and source module without explaining the attachment gate.
  - Changed to: completion `80`, confidence `86`, and `AUTOGEN_PARENT_UID:0000MF`.
  - Summary/evidence: existing IDA-backed by-file, by-class, vtable, layout, global, and aggregate memory docs confirmed the exact constructor span, MainUiGraph construction callers, `ParcelIconPane` vtable writes, child `ParcelPane` allocation/setup, `g_pParcelIconPane` and `g_pParcelPane` publication, timer setup, and source-module grouping under `ParcelPane.cpp`. The old direct-class deferral and `95/95` code-gate wording were historical state only and are superseded by the 2026-06-15 direct class route plus the 2026-06-26 B007 cleanup above.
