*** UID:0004I7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
RankingRewardInfoDialog::RankingRewardInfoDialog()
    : DialogPane(L"", static_cast<unsigned short>(-1), 0)
{
    g_pRankingRewardInfoDialog = this;

    RectBounds closeBounds;
    BuildCloseButtonBounds(0, &closeBounds);
    AddControl(new ImageButtonControlPane(14, &closeBounds));

    RectBounds dialogBounds;
    InitRectBoundsFromSize(&dialogBounds, 202, 104, 620, 500);
    OnCreate(&dialogBounds, 0, NULL, dword_69B36C);
    OnShow(NULL, g_pBackPane);
    SetFocusedControl(0);
    SetPendingControl(0);

    SendRewardInfoRequest();
    SlideOpenVertical();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Constructs and opens the reward-information dialog, installs its close control, initializes its reward vector, and sends the selected-category request. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# RankingRewardInfoDialog Constructor

## Address Range

- Exact half-open range: `0x0045dba0-0x0045dd56` (`0x1b6` bytes).
- The following `0x0045dd56-0x0045dd60` span is ten bytes of verified `0xcc` alignment padding; constructor-unwind cleanup begins at `0x0045dd60`.
- Current IDA models `sub_45DBA0` as one function. The only ordinary caller is `0x00458abd` in `RankingDialog::OnButtonClick` action `5`.

## Owner And Source Placement

- Direct semantic owner: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md).
- Generated source route: [UID:0000BP][RankingRewardInfoDialog](by-class/RankingRewardInfoDialog.md) to [UID:0000MZ][RankingDialog](by-file/RankingDialog.md), `NexusTK/ui/dialogs/RankingDialog.cpp`.
- [UID:0001ZG][0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore](by-memory/0x0045dba0-0x0045f12c.RankingRewardInfoDialogCore.md) is the non-emitting split index containing this exact child.

## Signature And Object Layout

- Source-facing signature: `RankingRewardInfoDialog::RankingRewardInfoDialog()`.
- The caller allocates exactly `0x278` bytes when `g_pRankingRewardInfoDialog` is null. The inherited `DialogPane` region occupies the object through `+0x26b` and `std::vector<RankingRewardEntry> m_rewardEntries` occupies the 12-byte derived tail at `+0x26c/+0x270/+0x274`.
- The three tail dwords are zeroed by vector default construction. They are begin/end/capacity pointers, not three independent source members.

## Exact Behavior

1. Constructs `DialogPane(L"", 0xffff, 0)` and establishes the compiler-generated vtable state for the inherited interface views.
2. Publishes `this` to `g_pRankingRewardInfoDialog`.
3. Initializes the close-button rectangle to `(279,468,63,24)`, allocates a 276-byte `ImageButtonControlPane`, constructs it with image id `14`, and registers it through `AddControl`.
4. Initializes dialog bounds from `(202,104,620,500)`, calls `OnCreate` with `dword_69B36C`, and shows relative to `g_pBackPane`.
5. Clears focused and pending controls.
6. Builds and sends the six-byte ranking reward-info request for the selected category.
7. Opens the dialog with `SlideOpenVertical()`.

The close-bounds and request operations are inlined in this constructor even though exact retained out-of-line copies exist at `0x0045ea20` and `0x0045ea60`. Calling the source methods in the formal body is the most plausible human-written source shape and permits ordinary optimization to reproduce the observed inline bodies.

## Control And API Dependencies

- Fresh MCP decompilation of `0x00495bf0` establishes the reusable declaration `ImageButtonControlPane(int imageId, const RectBounds *bounds)`: the bounds pointer is forwarded to the base control constructor and image id is stored at `+0x10c`.
- Accepted `DialogPane` support supplies `AddControl`, `OnCreate`, `OnShow`, `SetFocusedControl`, `SetPendingControl`, and `SlideOpenVertical`.
- Accepted `RectBounds` support supplies `InitRectBoundsFromSize`.
- Private source helpers are `BuildCloseButtonBounds(short, RectBounds *) const` and `SendRewardInfoRequest()`.

## Call And State Evidence

- Caller `0x00458abd` is reached only from ranking action `5`, checks the singleton, allocates `0x278`, calls this constructor, and stores/uses the resulting dialog object.
- Constructor, packet handler, paint method, vector helpers, and destruction paths all agree on the reward-entry vector at `+0x26c` and entry stride `0x230`.
- Constructor-unwind metadata references the singleton-clear and nested vector cleanup helpers, corroborating source members and initialization order without requiring handwritten cleanup source.

## Reconstructed C++ Rationale

- The formal block preserves every source-authored operation and leaves vtable writes, vector zeroing, security-cookie mechanics, and unwind cleanup to the compiler.
- `BuildCloseButtonBounds` and `SendRewardInfoRequest` intentionally replace duplicated inlined decompiler statements with the accepted private source methods.
- The exact original private method spellings are inferred, but behavior, ABI, ownership, and source route are independently supported; this is a confidence cap rather than a code blocker.

## Score Rationale

- `91` completion: exact boundaries, caller, allocation size, base/control/dialog/request flow, vector tail, dependencies, cleanup relationship, and formal source body are documented.
- `93` confidence: current MCP confirms the function and caller behavior plus the ImageButton constructor ABI. Remaining uncertainty is limited to private helper spelling and historical typedef spelling, not behavior.

## Rejected Alternatives

- Reject direct [UID:0000MZ][RankingDialog](by-file/RankingDialog.md) ownership; that page is the terminal source file, while the constructor is a class method.
- Reject `Browser`-style raw field initialization or a separate reward-dialog source file; class/vtable/singleton clustering and the accepted compact ranking feature module support the current route.
- Reject three raw reward-vector pointers as source declarations; the constructor and generated vector helper family establish one `std::vector` object.
