*** UID:0003NR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000097 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000097 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Raw retained NewOptionPane volume-display refresh body at 0x00540ff0 is not emitted as a source helper yet.
// MCP session 9600ec5b reports no IDA function record and no inbound xref/pointer route; ApplyVolumeSettings and OnVolumeChanged carry the live modeled volume paths.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw no-function NewOptionPane volume-display refresh body; derives pane-side music volume this+0x12bc from g_pConfig MusicVolume at +0x28de54 and pane-side sound volume this+0x12c0 from SoundVolume at +0x28de4c by divide-by-five, calls ApplyVolumeSettings and pane invalidation, has no inbound route in current MCP evidence, and carries a formal no-code proof comment pending final route, source helper name, and type declarations. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00540ff0-0x0054103a NewOptionPaneRefreshVolumeDisplay

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw function-shaped class helper in current B007 MCP evidence session `9600ec5b`; it was historically promoted to a neutral IDA function during the 2026-06-15 C001 IDA pass, but that function record was not retained in current B001/B007 evidence sessions. Older `0b5e057e` evidence is historical and agrees on the raw/no-route state.
- Direct owner: [UID:000097][NewOptionPane](by-class/NewOptionPane.md) NewOptionPane.
- Source root: [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- Parent aggregate: [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- Confidence: strong for volume-display behavior, music/sound config-field mapping, inverse relationship with `0x005403b0`, and byte-level boundaries; still below final-source quality because the source caller route, original helper name, source-quality type declarations, and current active function record remain unresolved.

## Behavior

Refreshes the `NewOptionPane` local volume display fields from process configuration, then refreshes/invalidates the pane. The helper divides `MusicVolume` at `dword_67A7C8 + 0x28de54` and `SoundVolume` at `dword_67A7C8 + 0x28de4c` by five using the standard multiply-by-`0xcccccccd`/shift sequence, stores the derived pane-side music value to `this+0x12bc` and pane-side sound value to `this+0x12c0`, calls `0x005403b0`, and invokes the pane refresh virtual with `this+0x44`.

## IDA MCP Evidence

- 2026-07-06 B007 MCP session `9600ec5b` reconfirmed `lookup_funcs 0x00540ff0` and `lookup_funcs 0x0054103a` as `Not a function`. `xrefs_to 0x00540ff0` returned zero inbound xrefs; `xref_query` to `0x00540ff0` returned total `0`; little-endian VA pointer-byte searches for `f0 0f 54 00` and terminal-boundary bytes `3a 10 54 00` returned zero hits. Bounded listing over `0x00540ff0-0x00541040` shows the body reads `dword_67A7C8+0x28de54`, divides by five, stores `this+0x12bc`, reads `dword_67A7C8+0x28de4c`, divides by five, stores `this+0x12c0`, calls `sub_5403B0` at `0x00541029`, invalidates through vtable slot `+0x20` with `this+0x44`, returns at `0x0054103a`, and aligns before modeled successor `0x00541040`. Bounded padding checks show prior sibling return `0x00540fdf`, alignment at `0x00540fe2`, target return `0x0054103a`, and successor alignment at `0x0054103b`.
- 2026-07-06 B007 config-name check resolved the previous sound/music ordering blocker: registry save/load code maps `dword_67A7C8+0x28de54` to `"MusicVolume"` and `dword_67A7C8+0x28de4c` to `"SoundVolume"`. Therefore `this+0x12bc` is the pane-side music volume field and `this+0x12c0` is the pane-side sound volume field. `type_query` still found no source-quality `NewOptionPane`, Config, or volume declarations, so exact source member spellings remain descriptive.
- 2026-06-25 B015 MCP session `80de0a67` reconfirmed `lookup_funcs 0x00540ff0` as `Not a function`; `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search found zero inbound routes. The raw body still reads `g_pConfig` / `dword_67A7C8` offsets `0x28de54` and `0x28de4c`, divides by five through the `0xcccccccd` multiply/shift sequence, writes pane-local fields `this+0x12bc` and `this+0x12c0`, calls `0x005403b0`, invalidates through the pane virtual path with `this+0x44`, and returns at `0x0054103a`.
- 2026-06-23 B001 MCP evidence session `ff68e691` reports `lookup_funcs 0x00540ff0` as `Not a function`. `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search for `f0 0f 54 00` all found zero inbound routes. The older `0b5e057e` pass is historical and agrees.
- 2026-06-15 pre-pass `lookup_funcs 0x00540ff0` reported `Not a function`; C001 then defined explicit bounds `0x00540ff0-0x0054103b`, and post-pass `lookup_funcs` in that session reported `sub_540FF0` size `0x4b`. This promotion is preserved as historical/session-specific evidence, not the accepted B001 evidence-session state.
- 2026-06-15 instruction query over `0x00540ff0-0x00541040` shows the body starts directly with `mov edx, dword_67A7C8`, saves `esi`, uses `ecx` as the pane pointer, reads config offsets `0x28de54` and `0x28de4c`, stores shifted quotient results at pane offsets `0x12bc` and `0x12c0`, calls `0x005403b0`, calls virtual slot `+0x20` with `this+0x44`, returns at `0x0054103a`, and aligns at `0x0054103b`.
- The next modeled function begins after alignment at `0x00541040`; the IDA function's exclusive end is `0x0054103b`.
- 2026-06-15 `xrefs_to 0x00540ff0`, backward data-flow from the raw start, and little-endian VA byte search for `f0 0f 54 00` also found no inbound xrefs or raw pointer hits.
- 2026-06-15 Wave2 `method`, `decomp`, and `xrefs` have no record for `0x00540ff0`; `filter --class NewOptionPane --all-methods` lists neighboring modeled methods but not this helper.
- 2026-06-15 live `analyze_function 0x005403b0` confirms the inverse path: it clamps the pane-local values, writes five-times-scaled values back to config offsets `0x28de54` and `0x28de4c`, calls sound/volume manager helpers, and is reached by both modeled NewOptionPane paths and this newly defined helper.

## Touched State And Dependencies

| Item | Evidence | Meaning |
| --- | --- | --- |
| `dword_67A7C8 + 0x28de54` | First config read; B007 `9600ec5b` registry string xrefs map this offset to `"MusicVolume"`. | Persisted music volume setting. |
| `dword_67A7C8 + 0x28de4c` | Second config read; B007 `9600ec5b` registry string xrefs map this offset to `"SoundVolume"`. | Persisted sound volume setting. |
| `this+0x12bc` | First derived store from `MusicVolume / 5`; `OnVolumeChanged` writes this field for nonzero `volumeType`. | Pane-side music display/slider value; exact source member spelling remains descriptive. |
| `this+0x12c0` | Second derived store from `SoundVolume / 5`; `OnVolumeChanged` writes this field for zero `volumeType`. | Pane-side sound display/slider value; exact source member spelling remains descriptive. |
| `0x005403b0` | Called after stores. | `NewOptionPane` volume/settings refresh helper. |
| virtual slot `+0x20` with `this+0x44` | Final call. | Pane bounds refresh/invalidation path. |

## Reconstruction Notes

This is class-owned `NewOptionPane` UI/config refresh logic. The persisted volume offsets and pane-side music/sound roles are now source-quality enough for documentation: `+0x28de54` / `this+0x12bc` are music, and `+0x28de4c` / `this+0x12c0` are sound. Keep final C++ blank because there is still no inbound route, no original helper name, no source-quality type declarations for the helper signature or class fields, and no current active function record.

B001 `ff68e691` and B007 `supervisor_resume_20260629` evidence-session state reinforces the no-code decision: the byte body is recoverable and behaviorally clear, but current evidence has no function record, caller, pointer route, or original-name proof for the helper start. The formal block now carries an explicit no-code proof comment rather than staying blank.

B015 `80de0a67` preserved the same no-code rationale with a then-current blocker that final volume/config field names plus sound/music ordering were not source-quality. B007 `9600ec5b` supersedes only that field-order blocker; the no inbound route, no original helper name, no source-quality declarations, and no callable-C++ blockers remain current.

## Cross-References

- [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- [UID:000097][NewOptionPane](by-class/NewOptionPane.md) NewOptionPane.
- [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- [UID:0001DW][0x0053ff90-0x00541b2b.NewOptionPane](by-memory/0x0053ff90-0x00541b2b.NewOptionPane.md) NewOptionPane aggregate memory range.

## Changes

- 2026-06-14 A001 split this raw volume-display helper out of [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md). Live IDA MCP listing confirmed config reads, divide-by-five derivation, pane-local stores, refresh/invalidation calls, exact return at `0x0054103a`, and unresolved no-direct-xref caveat.
- 2026-06-15 C001 refreshed Wave2/live IDA evidence, resolved the raw-helper no-function blocker in that session by defining bounded IDA function record `sub_540FF0` without renaming or typing it, added a start comment, saved the IDB, documented the inverse `0x005403b0` volume/config path and no-xref/no-pointer-hit blocker, and raised scores from `82/84` to `85/88`.
- 2026-06-23 B001 active-session correction: no score, owner, emitter, or C++ change.
  - Evidence: current B001 MCP evidence session `ff68e691` reports this helper start as `Not a function` with zero xref/code-ref/data-ref/immediate/pointer routes. Older `0b5e057e` evidence is historical and agrees. The 2026-06-15 C001 function record is historical/session-specific; behavior, ownership, byte boundary, and no-C++ conclusions remain valid.
- 2026-06-25 B015 implementation callback: metadata and blank C++ unchanged at that time; added current session `80de0a67` route/behavior evidence, no-code rationale, and concise `Item Summary`. This blank-C++ disposition is superseded by the 2026-06-30 formal no-code marker.
- 2026-06-30 B007 implementation callback:
  - Changed from `85/88` to `86/89` and inserted the accepted formal no-code proof comment.
  - Evidence: `lookup_funcs 0x00540ff0` remains `Not a function`, `xrefs_to` has no inbound route, and live modeled volume paths are carried by `ApplyVolumeSettings` and `OnVolumeChanged`. This is retained raw helper evidence, not padding.
- 2026-07-06 B007 implementation callback:
  - Changed from `86/89` to `87/90`, preserved owner `000097`, reconstructable true, emitter `000097`, blank optional emitter position, and formal no-code output.
  - Evidence: MCP session `9600ec5b` reconfirmed no current IDA function record for `0x00540ff0`/`0x0054103a`, zero inbound `xrefs_to`/`xref_query`/pointer-byte route to `0x00540ff0`, exact raw listing behavior, call to `sub_5403B0`, invalidation through vtable slot `+0x20`, and padding before/after the body. The same session resolved the prior volume-order blocker: `g_pConfig+0x28de54` is `MusicVolume`, `g_pConfig+0x28de4c` is `SoundVolume`, `this+0x12bc` is pane-side music volume, and `this+0x12c0` is pane-side sound volume.
