*** UID:000151 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FieldMapPane::SendFieldMapRequest(short entryIndex)
{
    const FieldMapEntry &entry = m_fieldEntries[entryIndex];
    unsigned char packet[0x100];

    PacketBufferWriteUInt8(0x3f, packet);
    PacketBufferWriteUInt16BE(entry.m_requestFieldId, packet + 1);
    PacketBufferWriteUInt16BE(entry.m_requestPosition.x, packet + 3);
    PacketBufferWriteUInt16BE(entry.m_requestPosition.y, packet + 5);
    packet[7] = 0;

    g_packetSender->QueueAndSendPacket(packet, 7);
    m_requestInFlight = true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA confirms three FieldMap-only callers, opcode `0x3f`, exact `m_requestFieldId` then `m_requestPosition.x/y` big-endian payload, seven-byte send via `g_packetSender`, and request-in-flight flag. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b11a0-0x004b1242 FieldMapRequestHelper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: helper function
- Address range: `0x004b11a0-0x004b1242` half-open; `0x004b1241` is the final byte inside `sub_4B11A0`, and the next adjustor thunk starts at `0x004b1242`.
- Likely source module: [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md)
- Prior generated owner conflict: generated material has mislabeled this as `FittingRoomDownloadControlPane::SendFieldMapRequest`; current IDA evidence keeps it with `FieldMapPane`.
- Confidence: strong for behavior, FieldMapPane ownership, packet helper names, and FieldMap entry request-field naming.
- Reconstruction note: reconstructable as the FieldMapPane source-authored `SendFieldMapRequest` helper. Packet helpers are `PacketBufferWriteUInt8`, `PacketBufferWriteUInt16BE`, and `Socket::QueueAndSendPacket`; the sender global is `g_packetSender`; and the accepted entry layout uses `m_requestFieldId` plus `Point m_requestPosition`.

## Behavior

`0x004b11a0` builds and sends a field-map request packet for a selected field entry:

- multiplies the selected index by the 0x94-byte field-entry record size;
- initializes a packet buffer with opcode `0x3f` through `PacketBufferWriteUInt8`;
- writes `m_requestFieldId` from record `+0x8a`, then `m_requestPosition.x` from `+0x90`, then `m_requestPosition.y` from `+0x8c` through `PacketBufferWriteUInt16BE`;
- queues/sends seven packet bytes through `g_packetSender->QueueAndSendPacket`;
- marks the request-in-flight byte at owner offset `0x95fe`.

The helper is invoked by click/enter/cancel handling and guards duplicate requests through the request-in-flight flag.

## IDA Evidence

- `lookup_funcs` confirms `sub_4B11A0`, size `0xa2`.
- `callers` / `xrefs_to` on 2026-05-24 show calls only from:
  - `0x004b0dfe` inside `FieldMapPane::HandleMouseEvent`
  - `0x004b0ea7` inside `FieldMapPane::HandleKeyEvent`
  - `0x004b0f0e` inside `FieldMapPane::HandleKeyEvent`
- Callees are packet-buffer helpers at `0x00575380`, `0x005753a0`, and sender helper `0x00574bb0`, plus the security-cookie check.
- 2026-06-01 IDA MCP recheck reconfirms the same three callers and no non-FieldMap callers.
- 2026-06-01 decompilation confirms opcode `0x3f`, three serialized entry values from the selected `0x94`-byte record, a seven-byte send through `dword_67A7EC`, and request-in-flight byte set at owner offset `0x95fe`.
- 2026-06-15 live IDA MCP recheck on session `a003_objectlist_dispatch_20260615` confirms `sub_4B11A0` size `0xa2`, so the correct half-open range is `0x004b11a0-0x004b1242`; `lookup_funcs(0x004b1242)` resolves the next `0x0b`-byte adjustor thunk, proving the boundary.
- `analyze_function(0x004b11a0)` reports prototype shape `int __thiscall(_BYTE *this, __int16)`, a single basic block, callees `sub_575380`, `sub_5753A0`, `sub_574BB0`, and `@__security_check_cookie@4`, and xrefs at `0x004b0dfe`, `0x004b0ea7`, and `0x004b0f0e`.
- The current decompilation computes `148 * selectedIndex`, writes opcode `0x3f`, serializes field-entry values from owner offsets `+642`, `+648`, and `+644` relative to the selected record, sends exactly seven bytes through `dword_67A7EC`, and sets owner byte `+38398`.
- `make_signature_for_range(0x004b11a0,0x004b1242)` returns a unique wildcarded signature for the corrected helper span.
- 2026-06-27 B006 implementation callback incorporates the accepted source-quality report from live IDA MCP session `80de0a67`. MCP health reported `NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- B006 reconfirmed the target as `sub_4B11A0`, size `0xa2`, with direct xrefs only from `0x004b0dfe` in the mouse handler and `0x004b0ea7`/`0x004b0f0e` in the key handler.
- B006 boundary bytes confirmed the previous function returns at `0x004b119a` followed by `CC CC CC` padding and the target prologue at `0x004b11a0`; target end bytes at `0x004b1238` include the `C2 04 00` epilogue followed by the next this-adjustor thunk at `0x004b1242`.
- `make_signature_for_range(0x004b11a0,0x004b1242)` returned a unique wildcarded signature for the target helper span.
- Negative direct little-endian pointer-pattern checks found no hits for `A0 11 4B 00`, `30 11 4B 00`, or `B0 0B 4B 00`, supporting the current direct-call/range picture without changing the vtable and code-xref evidence.
- Packet helper source names are now resolved from support docs and live MCP corroboration: `0x00575380` is `PacketBufferWriteUInt8` with 1114 code xrefs, `0x005753a0` is `PacketBufferWriteUInt16BE` with 222 code xrefs, and `0x00574bb0` is `Socket::QueueAndSendPacket` with 416 code xrefs. The queue helper sends the caller-provided seven bytes; helper-written zeros are local terminators, not payload bytes.
- FieldMap request fields are source-quality bounded as `m_requestFieldId` at `+0x8a` and `Point m_requestPosition` at `+0x8c`; the physical `y` component precedes `x` in memory, while packet order is opcode `0x3f`, field ID, `m_requestPosition.x`, then `m_requestPosition.y`.
- The prior generated `FittingRoomDownloadControlPane::SendFieldMapRequest` name is rejected as owner pollution. Current FieldMap and fitting-room docs both route this helper to `FieldMapPane` / [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md).

## Ownership Decision

Place this with `map/FieldMapPane.cpp` as `FieldMapPane::SendFieldMapRequest`. The active generated `FittingRoomDownloadControlPane::SendFieldMapRequest` name is a misleading owner label caused by shared UI/helper pollution in the generated data and is explicitly rejected.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | The page records the exact half-open range, three FieldMap callers, packet opcode/order, typed packet/socket helpers, sender global, Point-based request fields and offsets, request flag, callees, boundaries, signature, negative pointer evidence, formal CPP, and source order. |
| Confidence | 94 | Live IDA and provider documentation agree on range, callers, callees, layout, x-then-y wire order, and side effect. Exact original member spellings and final rebuilt integration remain below final-audit proof. |

## Cross-References

- [UID:00004U][FieldMapPane](by-class/FieldMapPane.md)
- [UID:00014Z][0x004b0bb0-0x004b129b.FieldMapPane](by-memory/0x004b0bb0-0x004b129b.FieldMapPane.md)
- [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md)

## 2026-08-18 B007 Request-Helper Source Closure

- The exact entry layout is `unsigned short m_requestFieldId` at `+0x8a` and `Point m_requestPosition` at `+0x8c`, with `y` at `+0x8c` and `x` at `+0x90`. The wire order is field ID, `m_requestPosition.x`, then `m_requestPosition.y`; layout order and serialization order intentionally differ.
- The exact packet is seven bytes: `0x3f`, three big-endian 16-bit values, then no eighth payload byte. The local `packet[7] = 0` is a terminator outside the queued seven-byte payload.
- Direct callers are exactly `0x004b0dfe`, `0x004b0ea7`, and `0x004b0f0e`, all in FieldMapPane input handlers. Outbound calls are one `PacketBufferWriteUInt8`, three `PacketBufferWriteUInt16BE`, `Socket::QueueAndSendPacket`, and the security-cookie check; `g_packetSender` is read at `0x004b120b`.
- The helper sets `m_requestInFlight` only after queue/send. It does not clear the flag, perform selection validation, add a zero-count guard, or alter caller-side gating.
- Earlier dated `requestX`/`requestY` scalar names remain physical-offset history and are superseded for formal source by `Point m_requestPosition`. The stale fitting-room generated owner is rejected; owner/emitter remain UID `0000JA`.
- Formal CPP is synchronized with the class H and `EMITTER_POSITION_OPTIONAL:30` places this definition after the retained rectangle helper. Its declaration emits once from UID `00004U`; this page's H remains blank.

## Changes

- 2026-08-18 B007 whole-file implementation callback:
  - Raised `88/91` to `92/94`, set source order `30`, and synchronized formal CPP to `m_requestFieldId` plus `m_requestPosition.x/y` while preserving exact packet behavior and FieldMap ownership.
  - Historicalized prior scalar request-coordinate spellings and retained the exact x-then-y wire-order distinction.

- 2026-06-01 documentation pass:
  - Before: metadata was `70/85`, reconstructable and parent fields were blank, and generated owner conflict wording was not separated from IDA evidence.
  - Changed to: `78/88`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000JA`.
  - Summary/evidence: IDA MCP reconfirmed the exact helper size, the three FieldMap-only callers, packet helper callees, opcode `0x3f`, serialized field-entry values, seven-byte send, and request-in-flight state update.
- 2026-06-15 A002 Goal 2 memory refresh:
  - Before: scored `78/88` and the filename/title used stale `0x004b1241` end wording even though IDA modeled `sub_4B11A0` as `0xa2` bytes.
  - Changed to: renamed the page to `0x004b11a0-0x004b1242.FieldMapRequestHelper.md`, raised to `85/90`, and kept owner/emitter [UID:0000JA][FieldMapPane](by-file/FieldMapPane.md) unchanged.
  - Summary/evidence: live IDA reconfirmed `sub_4B11A0` size, next-function boundary at `0x004b1242`, three FieldMap-only xrefs, packet helper callees, exact decompiled serialization flow, request flag write, and a unique corrected-range signature. C++ remains blank until packet-buffer and field-entry names are source-quality.
- 2026-06-17 B003 source-quality sync:
  - Summary/evidence: added the then-current negative protocol-name search result and structural payload-name direction. Score stayed `85/90` at that time; this blocker was superseded by the 2026-06-27 B006 implementation callback, which resolved the packet helper and request-field names enough for first-draft C++.
- 2026-06-27 B006 implementation callback:
  - Before: the page was `85/90` with C++ blank because packet-buffer helper names and FieldMap entry fields were treated as unresolved.
  - Changed to: `88/91` with formal first-draft C++ for `FieldMapPane::SendFieldMapRequest(short entryIndex)`.
  - Summary/evidence: accepted B006 report and live IDA MCP session `80de0a67` resolve packet helpers as `PacketBufferWriteUInt8`, `PacketBufferWriteUInt16BE`, and `Socket::QueueAndSendPacket`; resolve `dword_67A7EC` as `g_packetSender`; resolve request fields as `requestFieldId`, `requestX`, and `requestY`; preserve the exact `0x004b11a0-0x004b1242` range, FieldMap-only callers, opcode `0x3f`, seven-byte send, boundary bytes, unique range signature, helper xref counts, and negative pointer-pattern evidence; and reject the stale fitting-room owner/name.
