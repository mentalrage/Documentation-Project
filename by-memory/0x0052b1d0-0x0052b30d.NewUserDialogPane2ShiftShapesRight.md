*** UID:00043N | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00009E | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00009E | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewUserDialogPane2::ShiftShapesRight()
{
    UserShapeSelectControlPane *shapeControls[4];

    for (int i = 0; i < 4; ++i) {
        shapeControls[i] = GetChild<UserShapeSelectControlPane>(8 + i);
    }

    ControlPane *previousButton = GetChild<ControlPane>(6);
    ControlPane *nextButton = GetChild<ControlPane>(7);

    if (m_selectedShapeSlot != 3) {
        shapeControls[m_selectedShapeSlot]->SetSelected(false);
        ++m_selectedShapeSlot;
        shapeControls[m_selectedShapeSlot]->SetSelected(true);
        return;
    }

    if (!nextButton->IsEnabled()) {
        return;
    }

    short edgeResult = shapeControls[3]->StepShapeForward();
    shapeControls[0]->StepShapeForward();
    shapeControls[1]->StepShapeForward();
    shapeControls[2]->StepShapeForward();

    if (edgeResult == -1) {
        nextButton->Disable();
    } else {
        previousButton->Enable();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 317-byte source-authored NewUserDialogPane2 right-shift callback: constructor member-callback plus command/key callers, inherited typed lookup of shape controls 8-11 and navigation controls 6/7, non-edge deselect/increment/select, next-enabled edge gate, forward-step order 11 then 8/9/10, terminal sentinel button update, caller-owned notification, exact padding/CFG, and compiler-cookie exclusion are documented with source-ready C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0052b1d0-0x0052b30d NewUserDialogPane2 ShiftShapesRight

## Status

- Address range: `0x0052b1d0-0x0052b30d`.
- Owner/emitter: [UID:00009E][NewUserDialogPane2](by-class/NewUserDialogPane2.md).
- Rebuild handling: source-authored method.

## Evidence And Behavior

The exact half-open range is `0x0052b1d0-0x0052b30d`, 317 bytes, with SHA256 `CEE065413412B5AA55F2074F4A8256704BA7A176417C370533D75C5EB17E6F41`. Five `0xcc` bytes separate the predecessor and three `0xcc` bytes separate successor `NewUserDialogPane2::SelectNation` at `0x0052b310`. Live analysis reports seven basic blocks, cyclomatic complexity two, no strings, and no allocation, free, exception cleanup, packet work, null check, or bounds check.

Exactly three inbound references prove live source use: command-dispatch call `0x0052b7e7`, key-handler call `0x0052b8f5`, and constructor member-callback binding at `0x0052a994`. The constructor binding and ignored return register resolve the source signature as `void NewUserDialogPane2::ShiftShapesRight()`; IDA's provisional `char` return is residue.

The method uses inherited [UID:00003T][DialogPane](by-class/DialogPane.md) typed child lookup. Controls `8-11` are [UID:0000FR][UserShapeSelectControlPane](by-class/UserShapeSelectControlPane.md) previews, control `6` is the previous/left navigation button, and control `7` is the next/right button. `m_selectedShapeSlot` is the dialog-owned 32-bit field at `+0x274`; `+0x1fc` is inherited DialogPane control-manager storage, not a duplicate derived member.

When the selected slot is not `3`, the exact source operation is to deselect the current shape control, increment `m_selectedShapeSlot`, select the new shape control, and return. It does not call `SelectShapeSlot`, rotate all previews, or notify.

At slot `3`, disabled next-button state at inherited ControlPane offset `+0x101` returns with no side effect. Otherwise the method calls `StepShapeForward()` on control `11` first and retains its signed 16-bit result, then calls controls `8`, `9`, and `10` in that order. Result `-1` disables next through ControlPane virtual slot `+0x50`; any other result enables previous through slot `+0x4c`. The exact call order is preserved because invalidation can be observable.

Command id `7` performs selected-shape notification after this method returns. The key path calls this method without adding that notification. Notification therefore does not belong in this body.

The source-facing name `ShiftShapesRight` is selected over generic selector/update names because the left/right command ids and the paired control-shift behavior are decisive.

The stack pointer array induces the observed MSVC security-cookie prologue/check. Cookie operations, raw virtual dispatch, vptr source, decompiler temporaries, and register-return residue are compiler/decompiler artifacts and are excluded from handwritten source.

The historical 2026-06-26 draft used invented `GetShapeSelectControl` and `ShiftShapeRight` APIs and routed both branches through `SelectShapeSlot`. Fresh MCP evidence rejects that behavior while preserving the valid historical split/name/owner provenance.

## Changes

- 2026-07-22 B002 accepted UID00043N source-quality callback: raised `87/89` to `92/94`, retained owner/emitter UID00009E, reconstructable true, blank optional position and Nested 0, replaced the stale formal body with the exact inherited-lookup/selection/edge-step/button behavior, and added complete range/hash/xref/caller/callee/state/compiler/negative/history evidence.
- 2026-06-26 B004 split implementation: created with formal first-draft C++ and selector-state evidence.
