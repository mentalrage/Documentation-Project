*** UID:0002Q5 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000KX | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000KX | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "../auth/PasswordGuard.h"

static void __stdcall SendLoginRequest(const wchar_t* accountName, const wchar_t* password)
{
    char packet[0x100];
    char converted[0x100];
    unsigned char challenge[13];
    OSVERSIONINFOW versionInfo;
    RECT desktopRect;

    short accountNameLength = static_cast<short>(wcslen(accountName));
    short passwordLength = static_cast<short>(wcslen(password));

    PacketBufferWriteUInt8(0x03, packet);

    int accountNameByteLength = WideCharToMultiByte(0, 0, accountName, accountNameLength, converted, 0x100, NULL, NULL);
    if (accountNameByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[accountNameByteLength] = 0;
    PacketBufferWriteUInt8(accountNameByteLength, packet + 1);
    memmove(packet + 2, converted, accountNameByteLength);

    int passwordByteLength = WideCharToMultiByte(0, 0, password, passwordLength, converted, 0x100, NULL, NULL);
    if (passwordByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[passwordByteLength] = 0;
    PacketBufferWriteUInt8(passwordByteLength, packet + accountNameByteLength + 2);
    memmove(packet + accountNameByteLength + 3, converted, passwordByteLength);

    unsigned int packetLength = static_cast<unsigned int>(accountNameByteLength + passwordByteLength + 3);

    PacketBufferWriteUInt32BE(g_pConfig->m_loginChallengeKey, packet + packetLength);
    packetLength += 4;

    PacketBufferWriteUInt8(0, packet + packetLength);
    ++packetLength;

    versionInfo.dwOSVersionInfoSize = sizeof(versionInfo);
    GetVersionExW(&versionInfo);

    unsigned int osVersion =
        static_cast<unsigned char>(versionInfo.dwMinorVersion) |
        (static_cast<unsigned char>(versionInfo.dwMajorVersion) << 8) |
        (static_cast<unsigned char>(versionInfo.dwPlatformId) << 16);
    PacketBufferWriteUInt32BE(osVersion, packet + packetLength);
    packetLength += 4;

    GetWindowRect(GetDesktopWindow(), &desktopRect);
    PacketBufferWriteUInt8(g_applicationActiveGate == 0, packet + packetLength);
    ++packetLength;

    PacketBufferWriteUInt16BE(static_cast<unsigned short>(desktopRect.right), packet + packetLength);
    packetLength += 2;
    PacketBufferWriteUInt16BE(static_cast<unsigned short>(desktopRect.bottom), packet + packetLength);
    packetLength += 2;

    for (int i = 0; i < 13; ++i)
        challenge[i] = static_cast<unsigned char>(rand());

    memmove(packet + packetLength, challenge, 13);
    packetLength += 13;

    const unsigned char* challengeMask = g_pConfig->m_loginChallengeMask;
    PacketBufferWriteUInt8(challenge[8] ^ challengeMask[4], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[4] ^ challengeMask[1], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[7] ^ challengeMask[6], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[11] ^ challengeMask[12], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[3] ^ challengeMask[8], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[1] ^ challengeMask[10], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[9] ^ challengeMask[14], packet + packetLength);
    ++packetLength;
    PacketBufferWriteUInt8(challenge[2] ^ challengeMask[0], packet + packetLength);
    ++packetLength;

    if (packetLength >= 0x100)
        __report_rangecheckfailure();

    packet[packetLength] = 0;
    Sleep(rand() % 400 + 200);
    QueueAndSendPacket(g_packetSender, packet, packetLength);
    SetProtectedPasswordToken(password);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready login packet sender that serializes account/password text, appends config challenge and client-environment metadata, queues through `g_packetSender`, and updates the protected password token. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fb2d0-0x004fb62a - SendLoginRequest

## Status

- Confidence: strong for bounds, single login-command caller, packet-writing behavior, login source ownership, and first-draft C++ readiness; exact helper and config-field spellings remain inferred.
- Entity kind: source-ready file-local pre-login packet send helper.
- Historical generated alias: recovered global `SendLoginRequest_004FB2D0`; this address-suffixed name is superseded by source-facing `SendLoginRequest`.
- Corrected source module: [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md), emitted in `NexusTK/login/LoginDialogPane.cpp` rather than through the `LoginDialogPane` class UID.

## Covered Range

| Address range | Function/data | Disposition |
| --- | --- | --- |
| `0x004fb2d0-0x004fb62a` | `SendLoginRequest` | Source-ready file-local helper that builds and queues the login packet from account and password text. |
| `0x004fb62a-0x004fb630` | padding | Six `0xcc` bytes before the next function at `0x004fb630`; outside this function. |

## Behavior Summary

The helper writes opcode `3`, converts the account name and password from wide strings to bounded multibyte fields, appends the config login challenge key, a zero/spare byte, packed OS version, application-active byte, desktop width/height, a 13-byte random challenge, and eight challenge-mask XOR bytes, then sleeps for a short randomized delay, sends the packet through the network queue, and updates the protected password token. Source names `SendLoginRequest`, `m_loginChallengeKey`, and `m_loginChallengeMask` are descriptive/inferred; the packet order and offsets are direct binary facts.

## IDA MCP Evidence

- MCP session `6eab6bcb` was healthy for the accepted report: `idb_list` returned the active NexusTK IDB, `server_health` was `ok` with Hex-Rays ready, and `lookup_funcs 0x004fb2d0` reported `sub_4FB2D0` size `0x35a`, so the function covers `0x004fb2d0-0x004fb62a`; `lookup_funcs 0x004fb62a` reports no function.
- Successor/range evidence: bytes at `0x004fb620` include the `retn 8` tail, rangecheck island, and six `0xcc` padding bytes before the separate successor function at `0x004fb630`; endpoint xrefs and raw VA pointer/immediate searches for start/end/last/successor found no route overriding the ordinary code xref evidence.
- Caller lookup reports exactly one caller at `0x004fadbd` inside the `LoginDialogPane` command handler at `0x004fad00`; the caller reads account/password controls and the return value is not used.
- Callees include `WideCharToMultiByte`, `GetVersionExW`, `GetDesktopWindow`, `GetWindowRect`, `Sleep`, PacketBuffer scalar writers `0x00575380` / `0x005753a0` / `0x005753f0`, MemoryMan-backed copy helpers represented as source-level `memmove`, queue helper `0x00574bb0`, `_rand`, rangecheck failure, and the password-token helper [UID:0002VG][0x004657d0-0x00465827.SetProtectedPasswordToken](by-memory/0x004657d0-0x00465827.SetProtectedPasswordToken.md).
- Decompilation and disassembly show two `0x100` conversion buffers, `>= 0x100` range checks, opcode `0x03`, account/password length-prefixed fields, `g_pConfig + 0x28de34` as the login challenge dword, one zero byte, packed `OSVERSIONINFOW` minor/major/platform bytes, `g_applicationActiveGate == 0`, desktop `right`/`bottom` low-16 writes, a 13-byte random challenge, and eight XOR mask writes using `g_pConfig + 0x28de39` sparse indices `4`, `1`, `6`, `12`, `8`, `10`, `14`, and `0`.
- Data refs include [UID:00028Z][0x0067a938-0x0067a940.ApplicationLifecycleFlags](by-memory/0x0067a938-0x0067a940.ApplicationLifecycleFlags.md), [UID:00028Q][0x0067a7c8-0x0067a7cc.g_pConfig](by-memory/0x0067a7c8-0x0067a7cc.g_pConfig.md), and [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), matching a login/account request helper rather than a generic packet utility.

## Reconstruction Notes

- This helper is reconstructable as project code and is narrowly bounded. The older blank-C++ blocker around packet field/global names is historical: helper names now come from current support pages, while `m_loginChallengeKey` and `m_loginChallengeMask` are explicitly marked inferred/descriptive and cap confidence rather than blocking emission.
- The formal source uses `static void __stdcall` because the command-handler caller ignores the apparent tail helper return; sibling login packet helpers use the same source-level void shape for send helpers.
- This page supersedes the old aggregate row that described `0x004fb2d0-0x004fb629` with an inclusive end address and the old recovered `SendLoginRequest_004FB2D0` output alias.

## Corrected Assignment Gate

This helper clears the corrected direct-parent gate and is attached directly to [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md). The child is now `90/92`, the direct parent file page is `88/86`, and direct ownership is supported by the single caller at `0x004fadbd` inside the `LoginDialogPane` command handler, the immediately adjacent placement after the login core, packet/global behavior specific to login request submission, and the lack of any class-member/vtable route. It is not attached to [UID:00007F][LoginDialogPane](by-class/LoginDialogPane.md) because no evidence makes this helper a class virtual/member method.

## Negative Evidence And Rejected Owners

- No class-member route: there is no `this` use, no `LoginDialogPane` vtable slot, and no class declaration evidence; the helper emits through the file page [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md), not the class UID [UID:00007F][LoginDialogPane](by-class/LoginDialogPane.md).
- No dependency-owner route: Socket/queue, PacketBuffer, MemoryMan, Config, Application lifecycle, and password-token helpers are callees or data dependencies only. Their docs supply source names but do not own this login-request body.
- No aggregate/successor/range extension: `0x004fb62a-0x004fb630` is padding, and `0x004fb630` is a separate NewUserMiscDialogPane constructor. No endpoint xref or raw pointer/immediate search makes this a larger mixed range.
- No no-code disposition: the function is live, modeled, directly called, owner/emitter-ready, and has exact behavior; unresolved original spellings are confidence caps, not a target-specific no-code proof.

## Score Rationale

| Metric | Value | Rationale |
| --- | --- | --- |
| Completion | `90` | The page now documents exact function bounds, caller, callees, packet construction, conversion buffers, randomized challenge block, global/config dependencies, send queue path, padding boundary, corrected direct-parent assignment, rejected owners, and formal first-draft C++ in the emitter page. |
| Confidence | `92` | MCP session `6eab6bcb` confirms the size, only direct caller, callee list, decompiled/disassembled packet behavior, data refs, endpoint negatives, and adjacent padding. Confidence remains below final only because exact original helper and config field spellings are inferred. |

## Cross-References

- [UID:00019I][0x004f6700-0x004fb62a.MainMenuLoginAndAccountDialogs](by-memory/0x004f6700-0x004fb62a.MainMenuLoginAndAccountDialogs.md)
- [UID:00007F][LoginDialogPane](by-class/LoginDialogPane.md)
- [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md)
- [UID:0002Q4][0x004fa7a0-0x004fb2d0.LoginDialogPaneCore](by-memory/0x004fa7a0-0x004fb2d0.LoginDialogPaneCore.md)

## Changes

- 2026-08-17 B001 UID0000MG dependency implementation: score/body/ownership unchanged; added the exact `../auth/PasswordGuard.h` preamble before this source definition. The sole setter call remains at binary `0x004fb60b` after `QueueAndSendPacket`, proving LoginDialogPane is a consumer rather than the PasswordGuard owner.

- 2026-07-14 B005 UID0002Q4 structural closure: changed only `Nested:0` to `Nested:-4`. UID0004PJ opens the split LoginDialogPane child group with `Nested:+4`; constructor-unwind, packet, command, action-button, and pointer/mouse children remain at zero relative delta, and this exact exclusive successor closes the cumulative level. Score `90/92`, owner/emitter UID0000KX, formal body, source route, packet evidence, caller semantics, padding boundary, negatives, and all prior history remain unchanged.
- 2026-06-07 A006 Batch006 parent-gate pass: changed completion/confidence from `72/86` to `86/88` and set `AUTOGEN_PARENT_UID:0000KX`. Evidence: live IDA MCP reconfirmed the exact `0x35a` function size, one direct caller from `LoginDialogPane` command handling at `0x004fadbd`, Win32 and packet-writer callees, config/global dependencies, randomized login challenge construction, final send through `0x00574bb0`, and six bytes of `0xcc` padding before `0x004fb630`. The direct parent [UID:0000KX][LoginDialogPane](by-file/LoginDialogPane.md) is `88/86`, so both sides satisfy the corrected 85/85 gate.
- 2026-07-04 B005 UID0002Q5 implementation callback: changed completion/confidence from `86/88` to `90/92`, populated formal `static void __stdcall SendLoginRequest(const wchar_t* accountName, const wchar_t* password)` C++, and historicalized the old empty-emitter/recovered-alias state. Evidence: accepted report MCP session `6eab6bcb` confirmed the exact `0x004fb2d0-0x004fb62a` range, single caller `0x004fadbd`, no raw endpoint route, PacketBuffer/memmove/queue/password-token helper use, config challenge offsets `+0x28de34` and `+0x28de39`, application-active byte, randomized delay, queue send, protected-password update, successor padding boundary, and rejected non-owner alternatives.
