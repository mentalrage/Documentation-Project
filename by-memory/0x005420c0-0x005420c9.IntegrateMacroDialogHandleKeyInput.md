*** UID:00045H | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool IntegrateMacroDialog::HandleKeyInput(KeyEvent *event)
{
    return DialogPane::HandleKeyInput(event);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact secondary-vtable IntegrateMacroDialog::HandleKeyInput forwarder that preserves the key event and returns the inherited DialogPane key-control handler result; current MCP proves the 9-byte frame/tail-jump body, vtable-only slot at 0x00621198, adjacent 0xcc padding, and formal C++ emitter status. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005420c0-0x005420c9 IntegrateMacroDialogHandleKeyInput

## Status

- Address range: `0x005420c0-0x005420c9` half-open, size `0x9`.
- Current IDA/MCP function: `sub_5420C0`.
- Direct source owner/emitter: [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) `IntegrateMacroDialog`, emitted through [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) `MacroDialogs`.
- Parent split/container: [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md) `0x00541b30-0x00542265.IntegrateMacroDialog`.
- Vtable support: [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md) `MacroDialogFamilyVtables`, secondary view `0x00621190-0x006211bc`, slot `+0x08`.
- Rebuild handling: exact source-authored virtual key-input override. The body is compiler-thin, but the vtable slot is a real class override and should emit formal C++.
- Source disposition: exact modeled `IntegrateMacroDialog::HandleKeyInput` secondary-vtable override; first-draft C++ is now accepted. The method preserves the incoming key event and returns the inherited `DialogPane` key-control handler result.
- Corrected stale assumption: the previous split-stage blank-C++ rationale is obsolete. This page is already an exact child, not the broad split parent, and the empty-emitter review resolved the formal C++ body.
- Inherited handler alias note: the inherited target at `0x0049e6e0` is documented in support pages under `DialogPane::HandleKeyInput`, `DialogPane::OnKeyEvent`, and `DialogPane::OnKeyInput` style names. Those are aliases for the same inherited dialog key-control route; they do not change this target's source-facing `HandleKeyInput` method name.

## Behavior

`IntegrateMacroDialog::HandleKeyInput` is a pure forwarding override. It receives the current key event, passes `this` and the unchanged event pointer to the inherited dialog key-control handler at `0x0049e6e0`, and returns that handler's boolean result. It does not inspect key values, change the macro page, mutate `MacroHotkeyRecord` rows, touch row controls, or branch to any dialog-specific helper.

The accepted source-facing body is:

```cpp
bool IntegrateMacroDialog::HandleKeyInput(KeyEvent *event)
{
    return DialogPane::HandleKeyInput(event);
}
```

## Current MCP Evidence

Live IDA MCP evidence came from session `supervisor_20260628_resume` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`. Final implementation precheck request ids `b007-00045h-impl-idb-list` and `b007-00045h-impl-health` reported one active adopted worker session, PID/worker PID `5124`, `status:ok`, `auto_analysis_ready:true`, and `hexrays_ready:true`.

Evidence command ids from the accepted report: `b007-00045h-lookup`, `b007-00045h-bytes`, `b007-00045h-intconvert`, `b007-00045h-getint`, `b007-00045h-disasm`, `b007-00045h-decompile`, `b007-00045h-callees`, `b007-00045h-xrefs-to`, `b007-00045h-xref-query`, `b007-00045h-precedent-decompile`, `b007-00045h-precedent-disasm`, `b007-00045h-sibling-disasm`, and `b007-00045h-sibling2-disasm`.

- `lookup_funcs` reports `0x005420c0` as `sub_5420C0`, size `0x9`, and reports `0x005420c9` is not a function. The inherited target `0x0049e6e0` is `sub_49E6E0`, size `0x379`.
- `int_convert` verifies `0x9` is decimal `9` and `0x379` is decimal `889`.
- `get_bytes 0x005420b1 size 15` returns fifteen `0xcc` bytes before this function, `get_bytes 0x005420c0 size 9` returns `55 8b ec 5d e9 17 c6 f5 ff`, and `get_bytes 0x005420c9 size 7` returns seven `0xcc` bytes after this function.
- `disasm 0x005420c0` contains exactly `push ebp`, `mov ebp, esp`, `pop ebp`, and `jmp sub_49E6E0`.
- `decompile 0x005420c0` returns `char __thiscall sub_5420C0(int this, _BYTE *a2) { return sub_49E6E0(this, a2); }`, proving the inherited result is returned.
- `get_bytes 0x00621190 size 44` shows the secondary `IntegrateMacroDialog` vtable. Bytes at `0x00621198` are `c0 20 54 00`.
- `get_int 0x00621198 u32le` returns decimal `5513408`, which is `0x005420c0`.
- `xrefs_to 0x005420c0` reports one inbound xref, the data xref from vtable entry `0x00621198`.
- `xrefs_to 0x00621190` reports vtable-base stores from constructor `0x00541b79`, non-deleting destructor helper `0x00541e36`, and scalar deleting destructor wrapper `0x0054274c`.
- `xref_query 0x005420c0 direction=both` reports the vtable data xref and no independent direct code caller.
- `xref_query 0x0049e6e0 direction=to xref_type=code` includes this bridge at `0x005420c4 -> 0x0049e6e0` and the accepted `CreateUserDialogPane` twin at `0x0052ecc4 -> 0x0049e6e0`.

## Vtable And Negative Evidence

The vtable-only inbound route is expected for this override and is not a no-code reason. `IntegrateMacroDialog` installs the secondary vtable view at construction/destruction time, and slot `+0x08` points directly to this function. The lack of an independent direct code caller is negative evidence against a hidden ordinary helper route, not evidence that the method is unused or covered elsewhere.

Rejected alternatives:

- Do not keep formal C++ blank: the range is a modeled function, has a real vtable dispatch slot, and returns a base handler result.
- Do not convert this page to `[[CHILDREN]]`: the range is already a nine-byte exact child, not a split parent.
- Do not mark it covered-by/no-code: omitting it would lose the class-level virtual override at `0x00621198`.
- Do not clear `EMITTER_UIDS`: owner/emitter routing through [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) remains correct and emits through [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md).
- Do not use raw `_BYTE *`, `unsigned char *`, or `int` event typing in formal C++ for this pass. The current accepted precedent uses `KeyEvent *`.
- Do not preserve the old source-2 `void IntegrateMacroDialog::HandleKeyInput(int param)` shape. Current MCP returns the inherited handler result, and the accepted twin uses `bool`.

## Source-Shape Precedent

The accepted [UID:00046R][0x0052ecc0-0x0052ecc9.CreateUserDialogPaneOnKeyEvent](by-memory/0x0052ecc0-0x0052ecc9.CreateUserDialogPaneOnKeyEvent.md) page has the same 9-byte frame/tail-jump shape into `0x0049e6e0` and emits:

```cpp
bool CreateUserDialogPane::OnKeyEvent(KeyEvent *event)
{
    return DialogPane::OnKeyEvent(event);
}
```

MCP `decompile 0x0052ecc0` returns the same source shape as this target, and `disasm 0x0052ecc0` is the same four-instruction frame/tail-jump pattern. MCP disassembly of nearby macro-dialog forwarder siblings at `0x0053ed80` and `0x0053f170` shows the same 9-byte forwarder style; their docs already emit source C++ for `SpellMacroDialog::SetVisible` and `NewMacroDialog::SetActiveControl`. A thin forwarder body is therefore not a blank-C++ reason by itself.

Older source-2 material names this target `IntegrateMacroDialog::HandleKeyInput` and routes it to the inherited dialog handler. Its `void/int` typing is stale relative to current return-value evidence and the accepted `KeyEvent *` precedent, but the source-facing method name remains useful corroboration.

## Boundaries

- Previous range `0x005420b1-0x005420c0` is fifteen bytes of `0xcc` padding after `OnButtonClick`.
- Next range `0x005420c9-0x005420d0` is seven bytes of `0xcc` padding before `ClearMacroEditFields`.

## Score Rationale

Completion is raised from `86` to `88` because the empty-emitter disposition is now resolved as an exact formal C++ emitter, the stale split-stage blank rationale has been removed, and the page now carries current MCP boundary, bytes, decompiler, vtable, xref, precedent, and rejected-alternative evidence. Confidence is raised from `90` to `91` because MCP proves the function body, slot address, adjacent padding, inherited target, and no-hidden-caller state. The score remains below final-audit levels because exact original base-handler spelling is still an alias across support docs and the broader macro-dialog family still contains independently tracked helper/thunk caveats.

## Changes

- 2026-06-28 B007 implementation callback:
  - Before: `COMPLETION:86`, `CONFIDENCE:90`, owner/emitter metadata already routed through [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md), but formal C++ was blank with stale split-stage wording.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:91`, same `CANONICAL_OWNER:00006L`, same `RECONSTRUCTABLE:TRUE`, same `EMITTER_UIDS:00006L`, blank `EMITTER_POSITION_OPTIONAL`, and formal `bool IntegrateMacroDialog::HandleKeyInput(KeyEvent *event)` C++ body.
  - Evidence: MCP session `supervisor_20260628_resume` confirmed the `0x9` byte function, adjacent `0xcc` padding, tail-jump to `0x0049e6e0`, secondary-vtable slot `0x00621198`, vtable-only inbound route, no independent direct caller, and same-shape `CreateUserDialogPane::OnKeyEvent` precedent.
