*** UID:00011K | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: TargetOptionEditControlPane source-bearing method inventory with exact 0x110 layout, mode dword at +0x108, user/monster bytes at +0x10c/+0x10d, implicit tail padding, dialog constructor caller, ARROW/SHIFT+ARROW labels, paint/input/hit-test/destructor/scalar/adjustor evidence, and TargetOptionDialog.cpp route; complete class declaration emits through UID0000EF while method-body splitting remains separate. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004993b0-0x0049980c TargetOptionEditControlPane

## Scope

- Covered file/module: [UID:0000OG][TargetOptionDialog](by-file/TargetOptionDialog.md)
- Main class: [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md)

## Function Range

| Range | Current / proposed name | Notes |
| --- | --- | --- |
| `0x004993b0-0x00499401` | `TargetOptionEditControlPane::TargetOptionEditControlPane` | Calls the `ControlPane` base constructor with control kind `8`, stores the option-mode argument and two initial selection bytes, and installs the three TargetOption vtables. |
| `0x00499410-0x0049942e` | non-deleting destructor helper | Restores the three TargetOption vtables, then tail-jumps to the shared pane teardown at `0x00544580`. |
| `0x00499430-0x00499653` | `TargetOptionEditControlPane::OnPaint` | Builds the active/inactive button styles, draws the mode caption and the two target choices, and renders selected button art for the two stored selection bytes. |
| `0x00499660-0x00499664` | fixed return hook | Five-byte virtual hook returning zero through `retn 4`. |
| `0x00499670-0x0049976c` | `TargetOptionEditControlPane::OnMouseEvent` | Handles only mouse event kind `1`, hit-tests the two button rectangles, toggles the corresponding selection byte, and invalidates the inherited control view. |
| `0x00499770-0x0049980c` | `TargetOptionEditControlPane::HitTestButton` | Tests the same two rectangles and returns `0`, `1`, or `-1`. |

## Boundaries

| Range | Evidence |
| --- | --- |
| `0x00499402-0x00499410` | Fourteen `0xcc` alignment bytes after the constructor. |
| `0x0049942f-0x00499430` | One `0xcc` alignment byte before paint. |
| `0x00499654-0x00499660` | Twelve `0xcc` alignment bytes after paint. |
| `0x00499665-0x00499670` | Eleven `0xcc` alignment bytes before mouse handling. |
| `0x0049976d-0x00499770` | Three `0xcc` alignment bytes before hit testing. |
| `0x0049980d-0x00499810` | Three `0xcc` alignment bytes after hit testing. |

## Discontiguous Related Code

- `0x0049b740-0x0049b794` is the scalar deleting destructor wrapper.
- `0x004b9640` is the `SetFontId` helper currently attached to this class but physically located in a shared text/control helper area.

## Evidence

- Live IDA `lookup_funcs` on 2026-06-05 confirms local modeled functions at `0x004993b0` (`0x52` bytes), `0x00499410` (`0x1f`), `0x00499430` (`0x224`), `0x00499660` (`0x5`), `0x00499670` (`0xfd`), and `0x00499770` (`0x9d`). `0x00499810` is not a function.
- The constructor's only ordinary code caller is `TargetOptionDialog::TargetOptionDialog` at `0x005423dc`. The paint, fixed hook, mouse handler, and hit-test methods are reached through TargetOption vtable cells at `0x00618330`, `0x0061835c`, `0x00618358`, and `0x0061834c`.
- The constructor stores an option-mode word-sized value at the control payload field following the inherited pane/control state and two selection bytes immediately after it; the dialog constructor supplies those bytes from config-backed target-option values.
- Paint reads the `On` config style entries for selected and unselected buttons, prepares draw state through shared control helpers, selects `ARROW` or `SHIFT+ARROW` for the mode caption, draws `USER` and `MONSTER`, and paints two button rectangles at x ranges `110-130` and `200-220`.
- Mouse handling ignores event kinds other than `1`. For click events it uses the same two rectangles, toggles the matching stored selection byte, then calls the inherited invalidation virtual through the secondary control view.
- Hit testing is side-effect-free and returns `0` for the first rectangle, `1` for the second, and `-1` outside both.
- The scalar deleting destructor wrapper at `0x0049b740-0x0049b794` restores the three TargetOption vtables, calls the shared pane teardown at `0x00544580`, and frees the object only for the normal deleting flag path. Two adjustor thunks at `0x0049b045` and `0x0049b050` jump to it.
- The shared font setter at `0x004b9640-0x004b9657` writes the font id field only when the argument is `0` or `1`; paint calls it using the current global font-mode helper result, but the setter has broad caller fan-in and is not class-local source.
- 2026-06-16 B002 source-quality reanalysis: raw UTF-16 bytes verify `ARROW`, `SHIFT+ARROW`, `USER`, `MONSTER`, and the `ON` style key. The button-art helper receives a `BUTT...` resource/string pointer; keep the exact resource label descriptive until the shared resource owner is refreshed.
- Field layout is now source-quality enough for documentation: `+0x108` is a 32-bit option-mode value (`0 = ARROW`, `1 = SHIFT+ARROW` as supplied by the dialog constructor), `+0x10c` is the user-target enabled byte, and `+0x10d` is the monster-target enabled byte. These are not [UID:0001SV][SelectionControlPaneSelectedValue_0x10c](by-type/by-field/SelectionControlPaneSelectedValue_0x10c.md) fields; this class reads and writes byte flags.
- Accepted object size is `0x110`: `ControlPane` occupies `0x108`, the fields occupy `+0x108..+0x10d`, and `+0x10e/+0x10f` are implicit class tail padding. No explicit reserve array belongs in source.
- [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md) now emits the complete class declaration and inline const accessors `GetUserTargetEnabled`/`GetMonsterTargetEnabled`. Those accessors regenerate direct byte loads without changing layout or claiming these still-unsplit method bodies.
- Dialog row 0 loads/saves `g_pConfig + 0x2911dc` as ARROW user/monster bytes; row 1 loads/saves `g_pConfig + 0x2911de` as SHIFT+ARROW user/monster bytes.
- Paint, mouse handling, and hit testing all use the same two local target-option rectangles: `(110,0)-(130,20)` maps to the user-target byte and `(200,0)-(220,20)` maps to the monster-target byte. Mouse event kind byte `1` is the handled click/button event; exact original event enum spelling remains unrecovered.
- `0x00499410` is the class non-deleting destructor helper; `0x0049b740` is the scalar deleting destructor wrapper; `0x0049b045` and `0x0049b050` are adjustor thunks. Do not hand-port the scalar wrapper or adjustor thunk bodies as normal source.
- `0x004b9640` is a shared UI font-id/font-mode setter with broad caller fan-in. `TargetOptionEditControlPane::OnPaint` calls it, but this target should not claim it as a class-local method.
- This aggregate's formal body remains blank because it contains several independent source methods and alignment gaps that require their own future split. That is distinct from the complete class/layout/accessor declaration already emitted by UID0000EF at source position `0`; this separate inventory remains at position `10` and does not duplicate the class.

## Source-Quality Reanalysis

- Source placement: `TargetOptionEditControlPane` is the two-row target option edit control owned by [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md) and emitted through [UID:0000OG][TargetOptionDialog](by-file/TargetOptionDialog.md). The constructor's only ordinary code caller is `TargetOptionDialog::TargetOptionDialog` at `0x005423dc`; the paint, mouse, fixed-return, and hit-test bodies are reached through the TargetOption vtables.
- Field layout: `+0x108` is a 32-bit option-mode value (`0 = ARROW`, `1 = SHIFT+ARROW` as supplied by the dialog constructor), `+0x10c` is the user-target enabled byte, `+0x10d` is the monster-target enabled byte, and `+0x10e/+0x10f` are implicit tail padding in the exact `0x110` class.
- Config mapping: dialog row 0 loads/saves `g_pConfig + 0x2911dc` as ARROW user/monster bytes; row 1 loads/saves `g_pConfig + 0x2911de` as SHIFT+ARROW user/monster bytes.
- Paint resources: raw UTF-16 bytes verify `ARROW`, `SHIFT+ARROW`, `USER`, `MONSTER`, and the `ON` style key. The button-art helper receives a `BUTT...` resource/string pointer; keep the exact resource label descriptive until the shared resource owner is refreshed.
- Rectangles: the first option button uses local rectangle `(110,0)-(130,20)` and maps to user target; the second uses `(200,0)-(220,20)` and maps to monster target. Paint, mouse handling, and hit testing all use the same two boxes.
- Mouse behavior: event kind byte `1` is the handled click/button event. Coordinates at event offsets `+8/+12` are hit-tested; hit `0` toggles the user byte, hit `1` toggles the monster byte, and misses return false.
- Compiler/ABI support: `0x00499410` is the class non-deleting destructor helper; `0x0049b740` is the scalar deleting destructor wrapper; `0x0049b045` and `0x0049b050` are adjustor thunks. Do not hand-port the scalar wrapper or adjustor thunk bodies as normal source.
- Shared helper exclusion: `0x004b9640` is a shared UI font-id/font-mode setter with broad caller fan-in. `TargetOptionEditControlPane::OnPaint` calls it, but this target should not claim it as a class-local method.
- Aggregate method C++ remains blank pending exact child splitting; the source-safe class declaration/accessors are no longer blocked and emit through UID0000EF. The two states are intentionally separate.

## Score Rationale

Completion is `86` because source placement, method inventory, vtable reachability, constructor caller, field layout, config mapping, paint resources, hit rectangles, mouse toggle behavior, destructor/adjustor ownership, and shared font-setter exclusion are now documented with current evidence.

Confidence is `90` because live IDA and raw string/config evidence agree across the target, dialog constructor/save flow, vtable cells, and support docs. Confidence stays below final-code quality because inherited virtual slot names, exact event/helper types, resource wrapper names, and original Config field names remain provisional.

## Cross References

- [UID:0000OG][TargetOptionDialog](by-file/TargetOptionDialog.md)
- [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md)
- [UID:0001E2][0x00542270-0x0054259f.TargetOptionDialog](by-memory/0x00542270-0x0054259f.TargetOptionDialog.md)

## Changes

- 2026-07-14 B002 UID0001E2 implementation callback:
  - Preserved `86/90`, owner/emitter UID0000EF, reconstructable status, and blank aggregate formal body; set deterministic position `10`.
  - Added exact `0x110` base/field/implicit-padding contract and clarified that UID0000EF supplies the complete class/accessors without claiming this broad method inventory is split or body-complete. Older blank-C++ wording below is evidence-time history for the aggregate, not a current class-declaration blocker.

- 2026-06-05 A004 live refresh:
  - Before: the page was a 70/85 owner stub with no parent attachment and only constructor-call evidence.
  - After: raised to `82/88`, attached to [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md), kept `RECONSTRUCTABLE:TRUE`, and left final C++ blank.
  - Evidence: live IDA confirms exact method sizes, vtable-only reachability for the virtuals, constructor caller from `TargetOptionDialog`, selection-byte behavior, paint labels and button rectangles, mouse toggle flow, destructor wrapper/adjustor-thunk shape, shared font setter fan-in, and all local `0xcc` alignment gaps.
- 2026-06-16 B002 source-quality audit:
  - Raised from `82/88` to `86/90`; owner/emitter remains [UID:0000EF][TargetOptionEditControlPane](by-class/TargetOptionEditControlPane.md); final C++ stays blank.
  - Evidence: B002 rechecked method/signature roles, field bytes at `+0x108/+0x10c/+0x10d`, config rows at `g_pConfig + 0x2911dc/+0x2911de`, raw UTF-16 labels, click rectangles, fixed false-return hook role, destructor/scalar-wrapper/adjustor ownership, shared font-setter fan-in, and `TargetOptionDialog.cpp` source placement.
