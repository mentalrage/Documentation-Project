*** UID:0002US | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000033 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000033 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ConfirmDeleteAlert::ConfirmDeleteAlert(Pane *layoutReference, bool multiDelete)
    : AlertPane(g_pLanguageMan->GetLocalizedString(0x40),
                layoutReference,
                L"Delete",
                L"Cancel"),
      m_multiDelete(multiDelete)
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready retained constructor calls `g_pLanguageMan->GetLocalizedString(0x40)`, builds the `Delete`/`Cancel` `AlertPane`, stores `m_multiDelete`, and keeps no-route/no-function evidence as a confidence cap. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047e100-0x0047e158 ConfirmDeleteAlert Raw Constructor

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw constructor-shaped function without an IDA function object.
- Rebuild handling: source-authored constructor body.
- Likely owner: [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md).
- Likely source file: [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md), with [UID:0000HT][BoardDialogs](by-file/BoardDialogs.md) retained as consumer/source context rather than the direct owner.
- Autogen handling: attached to [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md); first-draft constructor C++ is now present because helper, resource, field, and base-constructor names are resolved enough for source-shaped output.

## Address Range

`0x0047e100-0x0047e158`.

IDA MCP still does not define a function object at `0x0047e100`, but raw disassembly shows a complete retained constructor body ending with `retn 8` at `0x0047e155`. B002 session `80de0a67` confirms the exact target range is `0x58` / 88 bytes (Verified with int_convert.py): predecessor bytes end `5f 5e c3`, `0x0047e0ff-0x0047e100` is one `0xcc` alignment byte, the raw body begins at `0x0047e100`, the body ends with `c2 08 00` at `0x0047e155`, and `0x0047e158-0x0047e160` is eight `0xcc` alignment bytes before [UID:0002SE][0x0047e160-0x0047e2e4.ConfirmDeleteAlertSendDeleteRequest](by-memory/0x0047e160-0x0047e2e4.ConfirmDeleteAlertSendDeleteRequest.md).

## Behavior

Constructs a `ConfirmDeleteAlert` by calling `LanguageMan::GetLocalizedString(0x40)` through `g_pLanguageMan`, forwarding the localized message, caller-supplied `Pane *layoutReference`, `L"Delete"`, and `L"Cancel"` to `AlertPane::AlertPane`, storing constructor argument `bool multiDelete` in `ConfirmDeleteAlert::m_multiDelete` at `this + 0x270`, and then leaving the three `ConfirmDeleteAlert` vptr stores at offsets `0x0`, `0xa0`, and `0xa4` to compiler output.

## Evidence

- 2026-06-25 B002 MCP session `80de0a67` resolves `sub_4F0350` as `LanguageMan::GetLocalizedString`, `dword_67A750` as `g_pLanguageMan`, and `sub_49FEB0` as `AlertPane::AlertPane`; generated labels remain search aliases only.
- 2026-06-03 IDA raw disassembly begins with `push ebp; mov ebp, esp`, calls `sub_4F0350` with `dword_67A750`, a `0x40` argument, and constructor arguments, then calls `sub_49FEB0` on `this`; the 2026-06-25 source-facing interpretation is `g_pLanguageMan->GetLocalizedString(0x40)` followed by `AlertPane::AlertPane(...)`.
- 2026-06-03 restarted IDA MCP reports no function object for `0x0047e100`; neighboring functions are `sub_47E090` at `0x0047e090-0x0047e0ff`, `sub_47E160` at `0x0047e160-0x0047e2e4`, and `sub_47E2F0` at `0x0047e2f0-0x0047e34b`.
- 2026-06-03 restarted IDA MCP raw-byte audit confirms `0x0047e158-0x0047e160` is eight `0xcc` bytes and `0x0047e0ff-0x0047e100` is one `0xcc` byte.
- 2026-06-03 IDA raw disassembly stores `[ebp+0x0c]` into `[esi+0x270]`, matching the delete-mode flag consumed by `ConfirmDeleteAlert::SendDeleteRequest`.
- 2026-06-03 IDA raw disassembly writes `??_7ConfirmDeleteAlert@@6B@` to `[esi]`, `??_7ConfirmDeleteAlert@@6B@_0` to `[esi+0xa0]`, and `??_7ConfirmDeleteAlert@@6B@_1` to `[esi+0xa4]`.
- 2026-06-13 IDA MCP byte reads resolve the two pushed constructor data operands as UTF-16 `Cancel` at `0x00614cbc` and UTF-16 `Delete` at `0x00612588`, replacing the older misleading rendered `"decoder"` comment for `off_614CBC`.
- 2026-06-03 whole-segment pointer scan found no raw `0x0047e100` address constants; the constructor is reached through ordinary code construction sites or an unmodeled call pattern rather than a vtable slot.
- The preceding `MailTransferReplyAlert` action ends at `0x0047e0ff`; `0x0047e0ff-0x0047e100` is one `0xcc` alignment byte.

## 2026-06-13 IDA MCP Recheck

- Active IDA session `a002_batch_clear_timer` over `NexusTK.exe` still reports `0x0047e100`, `0x0047e155`, and `0x0047e0ff` as not function starts, while `0x0047e160` is `sub_47E160` with size `0x184` and `0x0047e2f0` is `sub_47E2F0` with size `0x5b`.
- `insn_query 0x0047e0f8-0x0047e160` shows the previous modeled function returns at `0x0047e0fe`, one `align 10h` byte sits at `0x0047e0ff`, the raw constructor begins with `push ebp` at `0x0047e100`, and the body ends with `retn 8` at `0x0047e155` followed by `align 10h` at `0x0047e158`.
- The same instruction pass confirms the complete constructor sequence: push `off_614CBC`, push `off_612588`, push `[ebp+8]`, load `dword_67A750`, push `0x40`, call `sub_4F0350`, call `sub_49FEB0` on `esi`, store `[ebp+0x0c]` into `[esi+0x270]`, write the three `ConfirmDeleteAlert` vtables, and return `esi` through `eax`.
- `get_bytes 0x0047e0f8 size 0x68` matches the instruction boundaries: the raw body bytes start `55 8b ec 51 56 68 bc 4c 61 00 ...`, encode vtable pointers `0x00614580`, `0x006145e8`, and `0x00614618`, end with `c2 08 00`, and then eight `0xcc` bytes fill `0x0047e158-0x0047e160`.
- `get_bytes 0x00612588 size 0x20` decodes `off_612588` as UTF-16 `Delete` followed by UTF-16 `Error`; `get_bytes 0x00614ca8 size 0x28` shows UTF-16 tail text ending in `error.`, then UTF-16 `Cancel` beginning at `0x00614cbc`.
- `xrefs_to 0x0047e100` returns no references, but `xrefs_to` the three vtable bases returns the raw constructor stores at `0x0047e137`, `0x0047e13d`, and `0x0047e147` plus four modeled board/article construction sites.
- The four modeled construction sites duplicate the same allocation/init/vtable-store pattern instead of calling this raw start: `0x00473e00` and `0x004740d0` store mode byte `1`, while `0x00476410` and `0x00476820` store mode byte `0`; all four use `dword_67A750`, `off_612588`, `off_614CBC`, `sub_4F0350`, and `sub_49FEB0`.
- `entity_query 0x00614570-0x006146c8` confirms the table order around this class: `MailTransferReplyAlert` tertiary table at `0x00614574`, `ConfirmDeleteAlert` tables at `0x00614580`, `0x006145e8`, and `0x00614618`, then `DeleteReplyAlert` tables at `0x00614624`, `0x0061468c`, and `0x006146bc`, with `ConfirmDeleteMailAlert` beginning at `0x006146c8`.

## 2026-06-25 B002 MCP Source-Quality Pass

- Active MCP session `80de0a67` used `server_health`, `lookup_funcs`, `disasm`, `get_bytes`, `xrefs_to`, `find_bytes`, `make_signature_for_range`, `decompile`, `analyze_function`, and `get_global_value` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; the session reported `status=ok`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` confirms `0x0047e100`, `0x0047e155`, and `0x0047e158` are not IDA-modeled functions. The next modeled method is `0x0047e160` / `sub_47E160`, size `0x184` / 388 bytes (Verified with int_convert.py), and the next modeled helper is `0x0047e2f0` / `sub_47E2F0`, size `0x5b` / 91 bytes (Verified with int_convert.py).
- Current disassembly order is `push off_614CBC`, `push off_612588`, `push [ebp+8]`, load `g_pLanguageMan`, push `0x40`, call `LanguageMan::GetLocalizedString`, pass the returned message to `AlertPane::AlertPane`, copy `[ebp+0x0c]` to `[this+0x270]`, store vptrs `0x00614580`, `0x006145e8`, and `0x00614618`, return `this` in `eax`, and `retn 8`.
- `LanguageMan::GetLocalizedString(int stringId) const` is the accepted source-facing name for `0x004f0350`; decompilation returns `m_strings[stringId]` when `0 <= stringId < m_stringCount` and returns `L"Invalid String ID"` otherwise. The global at `0x0067a750` is `g_pLanguageMan`; `get_global_value` reports the stored value as `0x0`, matching zero-initialized singleton storage.
- `AlertPane::AlertPane(const wchar_t *messageText, Pane *layoutReference, const wchar_t *primaryButtonText, const wchar_t *secondaryButtonText)` is the accepted source-facing base constructor shape for `0x0049feb0`, so `[ebp+8]` is best documented as `Pane *layoutReference`.
- Byte reads decode `off_612588` as UTF-16 `Delete` and `off_614CBC` as UTF-16 `Cancel`; the older IDA-rendered `"decoder"` string for `off_614CBC` is an overlap/string-typing artifact and is rejected.
- Read-only extraction of `STR.RES` from `baram.dat` maps zero-based id `64` / `0x40` (Verified with int_convert.py) to `Are you sure you want to delete this?`. This is resource-owned text; source should call `g_pLanguageMan->GetLocalizedString(0x40)` or a future project string-id constant, not hard-code the English text.
- `xrefs_to 0x0047e100` returns zero xrefs. `find_bytes` for raw-start pointer constants also returns zero matches for VA bytes `00 e1 47 00` and RVA bytes `00 e1 07 00`. No vtable slot routes to this constructor; vtables route the action child at `0x0047e160`, not the raw constructor start.
- `xrefs_to` the three vtable bases returns five refs each: four modeled inline construction mirrors at `0x00473ecc/0x00474134/0x004765b2/0x00476884` and this raw constructor's stores at `0x0047e137/0x0047e13d/0x0047e147`.
- The four modeled mirrors allocate `0x274` / 628 bytes (Verified with int_convert.py), perform the same localized `Delete`/`Cancel` base construction, store the same three vptrs, and set `m_multiDelete` to `1` at `0x00473e00` and `0x004740d0`, or `0` at `0x00476410` and `0x00476820`.
- `make_signature_for_range 0x0047e100-0x0047e158 wildcard_operands=false` returns a unique exact byte signature:

```text
55 8B EC 51 56 68 BC 4C 61 00 68 88 25 61 00 FF 75 08 8B F1 8B 0D 50 A7 67 00 6A 40 89 75 FC E8 2C 22 07 00 50 8B CE E8 84 1D 02 00 8A 45 0C 88 86 70 02 00 00 8B C6 C7 06 80 45 61 00 C7 86 A0 00 00 00 E8 45 61 00 C7 86 A4 00 00 00 18 46 61 00 5E 8B E5 5D C2 08 00
```

- Ownership remains [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md), emitted through that class and source-routed through [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md). [UID:0000HT][BoardDialogs](by-file/BoardDialogs.md), `MailDialogs`, `AlertPane`, `LanguageMan`, packet helpers, and list helpers are consumers/dependencies/providers, not direct constructor owners.

## Score Rationale

| Metric | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | The page records exact half-open bounds, `0x58` / 88-byte size (Verified with int_convert.py), raw bytes, instruction sequence, predecessor/successor padding, no-function/no-start-xref status, no VA/RVA pointer hits, vtable stores/xrefs, decoded `Delete`/`Cancel` operands, `STR.RES` id `0x40` text, duplicate modeled construction sites, direct class/file parent links, source-facing helper/global/field names, and formal first-draft constructor C++. |
| Confidence | 91 | Confidence is high because MCP session `80de0a67` independently verifies the bytes, instruction body, helper/base constructor behavior, resource id/text, data operands, vtable refs, mirrored construction sites, and surrounding boundaries. It remains below final-audit levels because the raw start still has no IDA function object or direct pointer/call route, the exact original source-file split remains inferred, and the original symbolic constant name for string id `0x40` is not recovered. |

## Resolved Questions And Remaining Caps

- `sub_4F0350` is resolved as `LanguageMan::GetLocalizedString(int stringId) const`.
- `dword_67A750` is resolved as `g_pLanguageMan`.
- `sub_49FEB0` is resolved as `AlertPane::AlertPane`.
- `off_612588` and `off_614CBC` are source-facing `L"Delete"` and `L"Cancel"` string literals; the rendered `"decoder"` artifact for `off_614CBC` is rejected by byte decoding.
- `this+0x270` is resolved as `ConfirmDeleteAlert::m_multiDelete`; all modeled construction mirrors store `0` or `1`, and the action child uses nonzero as the selected-list/multi-delete branch.
- Source placement remains [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md) through [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md), with the exact shared-versus-private original file split retained only as a confidence cap.
- Direct route to `0x0047e100` remains unresolved after xrefs and VA/RVA scans; this is a confidence cap, not a C++ blocker.
- The original symbolic constant name for string id `0x40` is not proven, so the formal C++ uses the literal id rather than inventing an enum name.

## Cross-References

- [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md)
- [UID:0000HW][BulletinReplyAlerts](by-file/BulletinReplyAlerts.md)
- [UID:0002SE][0x0047e160-0x0047e2e4.ConfirmDeleteAlertSendDeleteRequest](by-memory/0x0047e160-0x0047e2e4.ConfirmDeleteAlertSendDeleteRequest.md)

## Changes

- 2026-06-25 B002 accepted source-quality implementation:
  - Before: page was `86/89`, had blank formal C++, and still said helper names, class declaration shape, and resource-wrapper names blocked final-source C++.
  - After: scored as `88/91`, kept [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md) as owner/emitter, and inserted first-draft constructor C++ using `g_pLanguageMan->GetLocalizedString(0x40)`, `AlertPane::AlertPane`, `Pane *layoutReference`, `L"Delete"`, `L"Cancel"`, and `m_multiDelete`.
  - Summary/evidence: B002 MCP session `80de0a67` verified the exact `0x58` / 88-byte body, predecessor/successor `0xcc` padding, no IDA function object, zero raw-start xrefs, zero VA/RVA pointer hits, helper/global/base-constructor names, `STR.RES` id `0x40` text `Are you sure you want to delete this?`, `off_614CBC` as `Cancel` rather than the IDA `"decoder"` artifact, vtable stores, four modeled construction mirrors, and the unique exact byte signature. The remaining no-route/source-file uncertainty is a confidence cap rather than a C++ blocker.
- 2026-06-03 live-IDA constructor pass:
  - Before: validator UID [UID:0002US] existed but the page was still scored `0/0`, unattached, and carried a pending-autogen note.
  - After: scored as `74/86`, marked reconstructable, attached under [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md), and, at that time, kept C++ blank because helper/resource parameter names were not final-source quality. This blank-C++ rationale is superseded by the 2026-06-25 B002 source-quality implementation.
  - Summary/evidence: restarted IDA MCP confirms the raw constructor body, the `retn 8` at `0x0047e155`, the `0x0047e158-0x0047e160` and `0x0047e0ff-0x0047e100` `0xcc` padding, no IDA function object, no raw pointer constants to `0x0047e100`, and neighboring function boundaries at `0x0047e090-0x0047e0ff`, `0x0047e160-0x0047e2e4`, and `0x0047e2f0-0x0047e34b`.
- 2026-06-13 Agent-A001 Goal 2 low-score repair:
  - Before: target was assigned to [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md) but remained in the low-completion/low-both queue at `74/86`, and its direct class parent was below the strict `85/85` gate.
  - After: scored as `86/89`; parent remains [UID:000033][ConfirmDeleteAlert](by-class/ConfirmDeleteAlert.md); C++ remained blank at that time. This old final-source-threshold note is superseded by the 2026-06-25 B002 source-quality implementation.
  - Summary/evidence: live IDA MCP session `a002_batch_clear_timer` reconfirmed the raw constructor as a non-modeled function body, exact bytes and `0xcc` padding, decoded `Delete`/`Cancel` operands, vtable bases and refs, and four modeled board/article construction sites that duplicate the constructor body with mode-byte variants. This supports assignment without using generated source output as authority.
