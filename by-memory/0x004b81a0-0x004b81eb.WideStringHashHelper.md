*** UID:00022J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw UTF-16 string hash helper with current IDA-confirmed const-uint16 input, no-call/no-global side-effect profile, exact `0xcc` boundaries, unique decimal `1000003` multiply/XOR site, no IDA function object, and no direct callers/xrefs, so source owner remains unassigned under the corrected gate. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b81a0-0x004b81eb WideString Hash Helper

## Status

- Disposition: reconstructable utility helper.
- Current generated owner: absent from active `simroot_v2` output.
- Current IDA status: C001 defined the previously raw body as function `WideStringHashHelper` at `0x004b81a0-0x004b81eb` after reconfirming the exact prologue/body/return and surrounding padding.
- Proposed owner: unresolved; keep unattached until caller or source-neighborhood evidence proves whether this belongs with text/string utilities, hash-table support, or the adjacent UI geometry/path utility region.
- Confidence: strong for behavior, signature, side-effect profile, byte boundary, IDA function definition, and no-reference state; medium for source ownership because no direct xrefs, callers, pointer constants, table references, or source-family artifact prove a direct parent.
- 2026-06-18 B001 current source-routing recheck: retained `88/92`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank formal C++; IDA MCP was unavailable in the B001 session, so B001 validated the current no-route state with local PE scans and existing IDA-backed docs.

## Signature And State

- Likely signature: `uint32_t __cdecl WideStringHashHelper(const uint16_t *text)`.
- Source-facing descriptive signature, if a route is later proven: `uint32_t __cdecl WideStringHashHelper(const wchar_t *text)` or a file-local `static uint32_t HashWideString(const wchar_t *text)`. The observed reads are 16-bit code units, and Windows `wchar_t` matches that storage in this 32-bit MSVC-era client; the name remains descriptive, not original-proof.
- Parameter source: the only argument is read from `[ebp+8]` into `esi`.
- Return value: `eax` receives the final 32-bit rolling hash from `edx`.
- Side effects: none observed; the raw instruction stream reads 16-bit units from the input string, preserves `esi`/`edi`, calls no functions, and touches no globals.
- Safety assumptions: no null-pointer guard is present; callers must provide a valid NUL-terminated UTF-16/wide-character buffer.
- Algorithm caveat: the recurrence uses the distinctive `1000003` multiply/XOR string-hash shape, but the range does not contain a `-1` hash sentinel repair or any caller evidence that would prove a specific external library owner.

## Behavior

Computes a 32-bit hash over a NUL-terminated UTF-16 string:

- scans the string length in 16-bit code units;
- seeds the hash from the first code unit shifted left by seven bits;
- for each code unit, multiplies the rolling value by `0x0f4243`, then XORs the current code unit;
- XORs the final string length into the hash result.

## Evidence Notes

- 2026-05-28 raw disassembly identifies a prologue/epilogue-bounded function at `0x004b81a0-0x004b81eb`.
- IDA MCP `xrefs_to 0x004b81a0` reports no direct xrefs.
- The loop reads 16-bit elements and stops at the first zero word, matching a wide-string hash helper rather than byte-string hashing.
- 2026-06-01 IDA MCP `lookup_funcs` reports no modeled function at `0x004b8198`, `0x004b81a0`, or `0x004b81eb`, and reports the next modeled function at [UID:00022K][0x004b81f0-0x004b821e.FileExists](by-memory/0x004b81f0-0x004b821e.FileExists.md) size `0x2e`.
- 2026-06-01 IDA MCP `disasm` reconfirms `0x004b81a0` starts with `push ebp; mov ebp, esp`, scans words until zero, seeds from the first word shifted left by seven, loops with `imul edx, 0F4243h` and `xor edx, eax`, XORs the code-unit length into the result, and returns at `0x004b81ea`.
- 2026-06-01 IDA MCP `xrefs_to` and `callers` still report no direct references to `0x004b81a0`; `callees` and `decompile` fail because IDA has no function object at this raw start.
- Boundary evidence: `0x004b8194-0x004b81a0` is the padding after [UID:00022I][0x004b7f90-0x004b8194.RectAnchorTransformHelpers](by-memory/0x004b7f90-0x004b8194.RectAnchorTransformHelpers.md), `0x004b81eb-0x004b81f0` is alignment padding, and `0x004b81f0` begins the adjacent `FileExists` helper. The predecessor padding boundary was corrected from stale `0x004b8198` by A002 byte reads on 2026-06-13.
- 2026-06-03 restarted IDA MCP `py_eval` reconfirms no modeled function at `0x004b8198`, `0x004b81a0`, or `0x004b81eb`; adjacent modeled helpers begin at `0x004b81f0-0x004b821e` and `0x004b8220-0x004b8284`.
- 2026-06-03 restarted IDA MCP `py_eval` finds no `xrefs_to` or literal dword references to `0x004b81a0`; `0x004b81f0` has direct callers at `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`, reinforcing that the reference gap is specific to this hash helper rather than the whole neighborhood.
- 2026-06-03 restarted IDA MCP `py_eval` disassembly confirms the exact raw sequence: word scan at `0x004b81b0-0x004b81b9`, seed `movzx edx, word ptr [esi]` plus `shl edx, 7`, rolling loop `imul edx, 0F4243h`/`xor edx, eax`, final `xor edx, edi`, and `retn` at `0x004b81ea`.
- 2026-06-03 restarted IDA MCP byte scan finds the `imul edx, 0F4243h` byte pattern only at `0x004b81d6` in `.text`.
- 2026-06-03 boundary bytes reconfirm `0x004b8194-0x004b81a0` and `0x004b81eb-0x004b81f0` are `0xcc` alignment padding around the raw helper.
- 2026-06-06 IDA MCP `disasm` reconfirms `[ebp+8]` as the only input, `edx` as the rolling accumulator, `edi` as the saved code-unit length, `eax` as the return register, and no memory writes or calls in the `0x004b81a0-0x004b81eb` body.
- 2026-06-06 IDA MCP `find_insn_operands` finds exactly one `imul` immediate operand equal to decimal `1000003` in `.text`, at `0x004b81d6`; a direct byte scan for `69 d2 43 42 0f 00` also finds only `0x004b81d6`.
- 2026-06-06 IDA MCP `callers`/`xrefs_to` still report no callers or direct xrefs for `0x004b81a0`, while `0x004b81f0` continues to have callers at `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`; this keeps the no-caller finding local to the hash helper.
- 2026-06-06 IDA MCP byte reads confirm `0x004b8198-0x004b81a0` is eight `0xcc` bytes and `0x004b81eb-0x004b81f0` is five `0xcc` bytes, so the helper remains exactly bounded even though IDA has not promoted it to a function.
- 2026-06-07 Batch 004 IDA MCP gate audit reconfirmed no modeled function at `0x004b8198`, `0x004b81a0`, or `0x004b81eb`; no direct xrefs to `0x004b81a0`; the next modeled function at `0x004b81f0-0x004b821e`; the same eight-byte predecessor and five-byte successor `0xcc` padding; and the unique `.text` byte-pattern hit for `imul edx, 0x0f4243` at `0x004b81d6`.
- 2026-06-08 Batch 118 live IDA `py_eval` reconfirmed no function object at `0x004b81a0`, no code xrefs to `0x004b81a0`, exact body bytes from `push ebp` through `retn`, eight predecessor `0xcc` bytes, five successor `0xcc` bytes, and the adjacent modeled `FileExists` function at `0x004b81f0-0x004b821e`.
- Batch 118 parent/source check read the plausible source candidates [UID:0000OB][StringUtil](by-file/StringUtil.md), [UID:0000HZ][CachedHashTable](by-file/CachedHashTable.md), [UID:0000N2][RectBounds](by-file/RectBounds.md), and [UID:0000MI][PathUtil](by-file/PathUtil.md). None can be proven as the actual direct owner without a caller, table reference, or source-family link; physical proximity to RectBounds/path helpers is not enough.
- 2026-06-10 B001-031 live IDA MCP `lookup_funcs` still reports no function at `0x004b8198`, `0x004b81a0`, or `0x004b81eb`, while the neighboring modeled helpers remain `sub_4B81F0` at `0x004b81f0-0x004b821e`, `sub_4B8220` at `0x004b8220-0x004b8284`, and `sub_4B8290` at `0x004b8290-0x004b83ae`.
- 2026-06-10 B001-031 IDA MCP `xrefs_to`, `callers`, `search code_ref`, `search data_ref`, and `find_insn_operands` find no code reference, data reference, caller, immediate operand, or RVA operand for `0x004b81a0` / `0x000b81a0`. A raw IDA Python dword scan over loaded segments likewise finds no little-endian VA or RVA pointer to `0x004b81a0`.
- 2026-06-10 B001-031 IDA MCP contrast checks keep the no-reference finding local to this helper: `FileExists` at `0x004b81f0` still has callers at `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`; `BuildFieldMapPath` at `0x004b8220` has the FieldMapPane constructor caller at `0x004b0c0d`; and `RectAnchorAxisAdjustHelper` at `0x004b8290` has four raw geometry-cluster xrefs from `0x004b803e`, `0x004b809f`, `0x004b80ff`, and `0x004b8161`.
- 2026-06-10 B001-031 IDA MCP `find_insn_operands` finds decimal `1000003` only at `0x004b81d6`. A raw IDA Python byte scan confirms the exact `69 d2 43 42 0f 00` `imul edx, 0x0f4243` encoding occurs only at `0x004b81d6`, and the little-endian immediate bytes occur only at `0x004b81d8`.
- 2026-06-10 B001-031 IDA Python byte reads reconfirm the exact body bytes for `0x004b81a0-0x004b81eb`, eight `0xcc` bytes at `0x004b8198-0x004b81a0`, and five `0xcc` bytes at `0x004b81eb-0x004b81f0`.
- 2026-06-12 C001 IDA MCP recheck reconfirmed no function at `0x004b8198`, `0x004b81a0`, or `0x004b81eb` before the fix; neighboring functions remained `sub_4B81F0` at `0x004b81f0-0x004b821e`, `sub_4B8220` at `0x004b8220-0x004b8284`, and `sub_4B8290` at `0x004b8290-0x004b83ae`.
- C001 `xrefs_to` reconfirmed no references to `0x004b81a0`, while the adjacent helpers still had their expected references: `0x004b81f0` from `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`; `0x004b8220` from `0x004b0c0d`; and `0x004b8290` from the rect-anchor raw cluster at `0x004b803e`, `0x004b809f`, `0x004b80ff`, and `0x004b8161`.
- C001 made the low-risk IDA 9.1 database fix after that verification: `define_func` created the exact `0x004b81a0-0x004b81eb` function, `rename` changed `sub_4B81A0` to `WideStringHashHelper`, and `idb_save` saved the update to `NexusTK.exe.i64`.
- C001 post-fix `lookup_funcs` reports `WideStringHashHelper` at `0x004b81a0`, size `0x4b`, and keeps the successor `sub_4B81F0` at `0x004b81f0`, size `0x2e`. Hex-Rays now decompiles the helper as `int __cdecl WideStringHashHelper(const unsigned __int16 *a1)`, using `wcslen(a1)`, seed `*a1 << 7`, loop recurrence `v5 ^ (1000003 * v3)`, and final `length ^ hash` return.
- 2026-06-13 A002 byte reads corrected only the predecessor padding span: `0x004b8194-0x004b81a0` is all `0xcc`, so the hash helper still starts exactly at `0x004b81a0` and the prior source-owner decision remains unchanged.
- 2026-06-18 B001 read-only PE scan used `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`, size `2679296`, image base `0x00400000`, and `.text` mapping `0x00401000-0x0060c4ac` / raw `0x00000400-0x0020ba00`. The second local copy `e:\NTK\Resources\NexusTK\NexusTK.exe` has the same MD5. The exact `0x4b` / 75-byte body appears once at raw file offset `0x000b75a0` / VA `0x004b81a0`; the predecessor padding `0x004b8194-0x004b81a0` is twelve `0xcc` bytes, and successor padding `0x004b81eb-0x004b81f0` is five `0xcc` bytes.
- The 2026-06-18 scan reconfirmed the exact body bytes: `55 8b ec 56 8b 75 08 8b ce 57 8d 51 02 0f 1f 00 66 8b 01 83 c1 02 66 85 c0 75 f5 2b ca 0f b7 16 d1 f9 8b f9 c1 e2 07 83 e9 01 78 17 0f 1f 40 00 0f b7 06 8d 76 02 69 d2 43 42 0f 00 33 d0 83 e9 01 79 ed 33 d7 5f 8b c2 5e 5d c3`.
- The same scan found the exact `imul edx, 0x0f4243` bytes `69 d2 43 42 0f 00` once at file `0x000b75d6` / VA `0x004b81d6`, and found the decimal `1000003` immediate bytes `43 42 0f 00` once at file `0x000b75d8` / VA `0x004b81d8`. This keeps the multiplier as strong behavior evidence but not source-family proof.
- The 2026-06-18 route scan found zero absolute VA dword hits for `0x004b81a0`, zero RVA dword hits for `0x000b81a0`, zero `push 0x004b81a0` encodings, zero external direct `E8/E9`, short, or conditional branch/call routes to the helper start, zero external direct branch/call routes into the target range, and zero absolute VA dwords into any byte of the body. Three RVA-like dword hits into the body (`0x000b81e8` at VA `0x004a6d6a`, `0x000b81b4` at VA `0x0050f577`, and `0x000b81e8` at VA `0x0057dc7a`) are instruction-byte coincidences, not data/table references.
- B001's positive controls proved the route scan can find ordinary neighboring references: `FileExists` `0x004b81f0` has branch/call hits at `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`; `BuildFieldMapPath` `0x004b8220` has a hit at `0x004b0c0d`; and `RectAnchorAxisAdjustHelper` `0x004b8290` has hits at `0x004b803e`, `0x004b809f`, `0x004b80ff`, and `0x004b8161`. The no-route state is therefore specific to this hash helper, not a broken scan across the mixed neighborhood.

## 2026-06-18 B001 Source-Routing Reanalysis

B001 ranked the current routing outcome and kept no-owner/non-emitting as the only defensible state. The helper is source-shaped utility code, not padding, a thunk, a switch table, import stub, or compiler-only EH/RTTI glue, but no current evidence proves where its original declaration lived. Repeated IDA-backed docs plus the current PE scan show no caller, code xref, data xref, raw VA/RVA pointer, pushed function pointer, external direct branch/call route, function table, vtable slot, or source-use context.

`StringUtil` / `NexusTK/util/StringUtil.cpp` remains the strongest semantic candidate because the helper hashes a NUL-terminated UTF-16/wide string and `StringUtil` owns other accepted string and Unicode helper surfaces. It is still rejected for assignment because [UID:0000OB][StringUtil](by-file/StringUtil.md) explicitly excludes this helper without caller, pointer, table, or source-family evidence, and the accepted `StringUtil` children have concrete routes that this page lacks. If future evidence appears, recheck `StringUtil` first, but do not set `CANONICAL_OWNER:0000OB` or `EMITTER_UIDS:0000OB` now.

`StringBase` is rejected because this helper receives a raw wide pointer and does not touch `StringBase` object state, ref-counted buffer headers, sentinels, pools, capacity fields, copy-on-write state, vector slots, or the `0x00582500+` StringBase helper family. `CachedHashTable` is rejected because its current docs describe a class/vtable/layout-backed fixed-entry cache at `0x004c6160+`; this helper has no `this`, slot state, table walk, per-instance hash bytes, vtable/RTTI relation, caller, or data link to that island.

`RectBounds` / geometry support is rejected despite the physical predecessor [UID:00022I][0x004b7f90-0x004b8194.RectAnchorTransformHelpers](by-memory/0x004b7f90-0x004b8194.RectAnchorTransformHelpers.md) and later [UID:00022L][0x004b8290-0x004b83c5.RectAnchorAxisAdjustHelper](by-memory/0x004b8290-0x004b83c5.RectAnchorAxisAdjustHelper.md). This helper hashes a wide string and does not read or write rectangle layouts, anchor indices, edge pointers, or geometry fields. The predecessor transform cluster calls `0x004b8290`, not `0x004b81a0`, and the current PE positive controls see those calls while still finding no route to the hash helper.

`PathUtil` / [UID:00022K][0x004b81f0-0x004b821e.FileExists](by-memory/0x004b81f0-0x004b821e.FileExists.md) is rejected because the target has no file/path behavior, `_wfopen`, `_wstat`, `_fclose`, dispatch-table read, path literal, or caller relationship to `FileExists`; five bytes of `0xcc` padding separate the two helpers, and the successor's three callers are visible while this helper has none. `FieldMapPane` / [UID:00015U][0x004b8220-0x004b8284.BuildFieldMapPath](by-memory/0x004b8220-0x004b8284.BuildFieldMapPath.md) is rejected because this helper has no path-stem copy, dot scan, `_memmove`, `.EPF`, `this+0xf8`, or constructor caller `0x004b0c0d`. `GeneralPurposePanel` and `GrafPort` are rejected because their evidence is UI panel state or render-port receiver state and no route reaches backward to this helper.

An external/library/runtime hash origin is not enough for reclassification. The `1000003` multiply/XOR shape resembles known string-hash idioms, including Python-like patterns, but this body reads UTF-16 code units, lacks CPython's `hash == -1 ? -2` sentinel repair, and has no Python/third-party import, source string, version marker, namespace, table, or library island. A new standalone `StringHash`/`WideStringHash` utility owner is also rejected because one isolated unreferenced function is not enough to create a synthetic source file or generated output root.

The current helper name `WideStringHashHelper` is a stable descriptive documentation/IDA name, not proven original source spelling. If a future source route is found, source-facing names such as `HashWideString` or `GetWideStringHash` are plausible VC6-era helper spellings, but none should be claimed now. The source-facing signature is strong enough for behavior documentation but not for source emission.

Formal C++ remains blank. The target satisfies `RECONSTRUCTABLE:TRUE` and combined score `(88 + 92) / 2 = 90`, but it fails the active code-entry gate because `EMITTER_UIDS` is blank, no canonical owner or generated source root is proven, and every candidate emitter is either semantic-only, contradicted by body evidence, or synthetic. A review-only behavior sketch would be a `uint32_t` wide-string hash that scans to the NUL terminator, seeds `text[0] << 7`, applies `hash = text[i] ^ (1000003u * hash)` for each code unit, and returns `hash ^ length`; do not enter that sketch into the formal reconstruction block until an emitter/source route is proven.

## Archived B-Agent Rule 26 Incorporation Audit

The older B-agent report cluster for this UID has been reviewed claim-by-claim and incorporated here as historical and current evidence, not as a reason to change the current owner/emitter state. Reports audited:

- `Agent-B001/research/executed/older/00022J-WideStringHashHelper.md`
- `Agent-B001/research/executed/older/00022J-WideStringHashHelper-B001-00022J.md`
- `Agent-B001/research/executed/older/00022J-WideStringHashHelper-current-goal2-pass.md`
- `Agent-B001/research/executed/older/00022J-WideStringHashHelper-post-migration-recheck.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-current-no-owner-recheck.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-current-no-owner-research.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-current-no-owner-sweep.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-fresh-no-owner-pass.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-goal2-verification.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-live-goal2-no-owner-pass.md`
- `Agent-B002/research/executed/older/00022J-WideStringHashHelper-post-migration.md`
- `Agent-B003/research/executed/older/00022J-WideStringHashHelper-restart-no-owner-research.md`
- `Agent-B003/research/executed/older/00022J-WideStringHashHelper-second-pass.md`

The reports agree on the final state: retain `COMPLETION:88`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++. They also agree that the helper is source-shaped and reconstructable, not padding, a thunk, switch data, import glue, RTTI/EH scaffolding, or a compiler-only artifact. The unresolved issue is source routing, not behavior recovery.

Accepted evidence from the cluster:

- B001-031 raised the target through the earlier `85/88` state after live IDA MCP and IDA Python checks proved exact bytes, `0xcc` padding, no function object at that time, no callers/xrefs, no raw VA/RVA pointer to `0x004b81a0`, unique `1000003` multiply evidence, and normal neighboring reference surfaces. C001-009 later superseded only the "no IDA function object" part by defining and naming the function; the owner/emitter conclusion remained unchanged.
- B001-00022J and later B002/B003 passes rechecked the post-C001 state: IDA function `WideStringHashHelper` at `0x004b81a0`, size `0x4b`; no function at `0x004b8194` or `0x004b81eb`; successor starts at `0x004b81f0`, `0x004b8220`, and `0x004b8290`.
- Live sessions named in the reports include `b001_0002bd`, `b001_0003gy`, and `a001_goal2_class_batch`. They consistently reported zero incoming xrefs/callers for the target, no callees, no strings, no global accesses, no code/data refs to the start, no immediates for `0x004b81a0` or RVA `0x000b81a0`, and only target-internal flow edges such as the fall-through to `0x004b81a1` or loop-local xrefs.
- `entity_query` over the local neighborhood listed the modeled starts `WideStringHashHelper`, `sub_4B81F0`, `sub_4B8220`, `sub_4B8290`, and `sub_4B83D0`, reinforcing that this is a mixed linker neighborhood rather than one source file.
- `make_signature_for_range 0x004b81a0-0x004b81eb` with operand wildcarding disabled reported the exact body signature as unique.
- Name/string/import sweeps found no source breadcrumb. Searches for hash/string terms found unrelated `CachedHashTable` RTTI, `StringBase`/callback-template RTTI, and the minimap `HashList.txt` URL, but no PDB/source path, `StringUtil`, `WideStringHash`, Python, third-party module, or import evidence tied to `0x004b81a0`.
- The corrected predecessor padding begins at `0x004b8194`. B002 noted that bytes at `0x004b8190-0x004b8193` are predecessor local switch-table data, so stale `0x004b8198` predecessor-padding wording should not be reused. The clean padding before this helper is twelve `0xcc` bytes at `0x004b8194-0x004b81a0`; the successor padding is five `0xcc` bytes at `0x004b81eb-0x004b81f0`.
- Fresh PE scans used `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, image base `0x00400000`, target RVA `0x000b81a0`, and raw file offset `0x000b75a0`. The full 75-byte body and the `69 d2 43 42 0f 00` / `0x0f4243` multiply pattern occur once.
- The reports used several `.text` mappings across tools, including `.text` VA `0x00401000-0x0060c4ac` with raw `0x00000400-0x0020ba00`, `.text` RVA `0x1000-0x20c600` with raw `0x400-0x20ba00`, and an older raw scan range ending at `0x1f5000`; all successful target-specific checks still mapped the helper to raw `0x000b75a0` / VA `0x004b81a0` and found the same bytes.
- Raw route scans found zero little-endian VA start pointers (`a0 81 4b 00`), zero RVA start pointers (`a0 81 0b 00`), zero `push 0x004b81a0` encodings, zero absolute VA dwords into any byte of the body, zero direct external rel8/rel32 branch or call routes to the helper start, and zero external direct branch/call routes into the body. B003 explicitly noted that six branch hits into the range were target-internal loop/control-flow edges, not external entry routes.
- RVA-like in-body dword values at file/VA `0x000a616a`/`0x004a6d6a` (`0x000b81e8`), `0x0010e977`/`0x0050f577` (`0x000b81b4`), and `0x0017d07a`/`0x0057dc7a` (`0x000b81e8`) are instruction-byte coincidences inside `.text`, not data/table references.
- Adjacent-helper positive controls remain decisive: `0x004b81f0` / `FileExists` has route hits or xrefs at `0x0049c87c`, `0x0049c88f`, and `0x004dbcb5`; `0x004b8220` / `BuildFieldMapPath` has the `0x004b0c0d` FieldMapPane constructor route; `0x004b8290` / `RectAnchorAxisAdjustHelper` has route hits from `0x004b803e`, `0x004b809f`, `0x004b80ff`, and `0x004b8161`. The no-route state is therefore specific to this helper, not a failed scan or a whole-neighborhood IDA issue.

Archived owner/emitter conclusions to preserve:

- `StringUtil` remains the first future candidate to recheck if a caller/table/source artifact appears. It is not assignment-ready now because the only positive evidence is wide-string hash semantics; accepted `StringUtil` children have direct caller, layout, or source-family evidence that this helper lacks.
- `StringBase` is rejected because the body takes a raw wide pointer and never touches ref-counted headers, sentinels, pool globals, capacity fields, copy-on-write state, `StringBase` RTTI/vtables, append/format/range helper callees, or object storage.
- `CachedHashTable` is rejected despite hash-domain similarity because it is a separate class/vtable/layout-backed cache island around `0x004c6160+`; this helper has no receiver, slot state, table walk, per-instance hash bytes, constructor/destructor relation, vtable/RTTI relation, caller, or data link to that island.
- `RectBounds`, `PathUtil`, `FieldMapPane`, `GeneralPurposePanel`, and `GrafPort` are rejected because their nearby ranges have their own positive evidence and this target has no rectangle layout, path/file API, `.EPF` path-stem, UI panel state, render-port state, or caller relationship to those owners.
- A new `HashUtil`, `StringHashUtil`, `StringHash`, or `WideStringHash` source file is rejected for now. One isolated unreferenced helper, with no source root, sibling family, table, or caller, is not enough to create a synthetic file or output route.
- External/library/runtime ownership is only algorithm context. The `1000003` recurrence resembles known string-hash idioms, including Python-like shapes, but this body reads UTF-16 code units, lacks a `hash == -1 ? -2` sentinel repair, and has no Python/third-party import, namespace, version marker, source string, or broader library island.
- `CANONICAL_OWNER:NONE` with one or more emitters is rejected. This helper is a function with zero proven source-use contexts, not pooled literal/shared data with multiple documented consumers.

Historical and superseded report statements:

- Older report language using `AUTOGEN_PARENT_UID` blank or "unassigned" maps to the current `CANONICAL_OWNER:NONE` plus blank `EMITTER_UIDS` model.
- Older "no IDA function object" statements are chronological pre-C001 evidence. The current state is the C001-fixed IDA function `WideStringHashHelper` at `0x004b81a0`, size `0x4b`.
- Older `0x004b8198-0x004b81a0` predecessor-padding wording is stale after A002/B002 correction. The correct predecessor padding span is `0x004b8194-0x004b81a0`.
- Older code-gate phrasing varied between `95/95`, `90/90+`, and combined-average comments. The current practical blocker is the same in all cases: formal C++ is not entered because `EMITTER_UIDS` is blank and no source root/output route is proven, not because the behavior is unknown.
- Older reports sometimes stated no coverage change was required when a then-current coverage row already matched their pass. If the shared manual coverage row still says `82%` or "no IDA function object", that wording is stale relative to the current target state and should be refreshed by the supervisor-owned shared-report process.

## Corrected Assignment Gate

- Child target score after C001-009: `COMPLETION:88`, `CONFIDENCE:92`.
- Assignment decision: keep `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS`. The child clears the strict child evidence gate for behavior, boundaries, side effects, no-reference state, uniqueness of the hash pattern, and current IDA function identity, but no direct source owner or output route is supported because current IDA-backed docs and the 2026-06-18 PE scan still have no callers, code/data xrefs, raw VA/RVA pointer, pushed function pointer, table reference, direct branch/call route, or source-family artifact for `0x004b81a0`.
- Current best owner statement: retained source-authored wide-string hash utility with unresolved direct source owner. `StringUtil` is the best semantic candidate, but the assignment remains blocked because wide-string behavior alone is not a direct owner link. `CachedHashTable`, `RectBounds`, `PathUtil`, `FieldMapPane`, and `GeneralPurposePanel` are rejected as direct parents because their documented ownership evidence is class/layout/path/panel specific and does not reference this helper.
- Follow-up evidence target: find an indirect reference, table entry, import/name record, or source-family artifact that ties the raw helper to a specific string/hash container module. Without that evidence, parent assignment would be proximity or behavior inference rather than a defensible direct-owner assignment.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `88` | The page records exact bounds, boundary padding, ABI shape, full loop behavior, no-call/no-global side-effect profile, no-caller/no-xref/no-raw-pointer state, uniqueness of the `1000003` multiply, adjacent-helper contrast, rejected parent candidates, and the C001 IDA function-definition/name fix. Final C++ remains blank because no direct owner or source-emission context is proven. |
| Confidence `92` | Confidence is strong for the exact range, behavior, IDA function identity, no-reference conclusion, and rejected proximity-only parent arguments. Confidence remains below final-audit level because source ownership is still unresolved despite the now-correct IDA function model. |

## Cross-References

- [UID:0000N2][RectBounds](by-file/RectBounds.md) RectBounds
- [UID:0000OB][StringUtil](by-file/StringUtil.md) StringUtil
- [UID:0000HZ][CachedHashTable](by-file/CachedHashTable.md) CachedHashTable
- [UID:0000MI][PathUtil](by-file/PathUtil.md) PathUtil
- [UID:00022I][0x004b7f90-0x004b8194.RectAnchorTransformHelpers](by-memory/0x004b7f90-0x004b8194.RectAnchorTransformHelpers.md)
- [UID:00022K][0x004b81f0-0x004b821e.FileExists](by-memory/0x004b81f0-0x004b821e.FileExists.md)

## Changes

- 2026-08-18 UID0000N2 boundary reconciliation:
  - Preserved this helper's unrelated ownership, exact `[0x004b81a0,0x004b81eb)` source range, behavior, score, emitter, and formal source.
  - Recorded leading `[0x004b8194,0x004b81a0)` as exactly 12 `cc` bytes with zero inbound xrefs. It separates [UID:00022I][0x004b7f90-0x004b8194.RectAnchorTransformHelpers](by-memory/0x004b7f90-0x004b8194.RectAnchorTransformHelpers.md)'s terminal anchor switch table from this helper (claim C158).
  - Recorded trailing `[0x004b81eb,0x004b81f0)` as exactly 5 `cc` bytes with zero inbound xrefs. It separates this helper from [UID:00022K][0x004b81f0-0x004b821e.FileExists](by-memory/0x004b81f0-0x004b821e.FileExists.md) `FileExists` (claim C159).
  - Both spans remain address-ordered no-code entries in `by-memory/-ignored.md`; neither is assigned to the WideStringHash body or RectBounds formal source.

- 2026-06-22 Rule 26 archived B-agent cluster incorporation:
  - Changed to: documentation only; scores and routing remain `88/92`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++.
  - Summary/evidence: incorporated the older B001/B002/B003 report cluster into the target page. The added audit preserves the intermediate `85/88` repair, C001 post-fix function identity, live IDA sessions `b001_0002bd`, `b001_0003gy`, and `a001_goal2_class_batch`, `make_signature_for_range` uniqueness, entity/name/import search negatives, corrected `0x004b8194` predecessor padding, PE raw-route scans, RVA coincidence analysis, adjacent-helper positive controls, rejected owner/emitter candidates, and historical/superseded assumptions. No formal C++ was added because no emitter/source route is proven.

- 2026-06-18 B001 current source-routing review:
  - Changed to: no metadata change; retained `COMPLETION:88`, `CONFIDENCE:92`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++.
  - Summary/evidence: IDA MCP was unavailable to B001, so B001 rechecked the current binary and docs. The PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515` found the exact 75-byte body once at raw `0x000b75a0`, reconfirmed twelve `0xcc` bytes before the helper and five after it, found unique `1000003` multiply/immediate bytes, found no VA/RVA/push pointer or external branch/call route to the target, and retained positive controls for adjacent `FileExists`, `BuildFieldMapPath`, and `RectAnchorAxisAdjustHelper`. `StringUtil` remains the best semantic candidate but is rejected without direct route evidence; `StringBase`, `CachedHashTable`, `RectBounds`, `PathUtil`, `FieldMapPane`, `GeneralPurposePanel`, `GrafPort`, external-runtime, and new synthetic utility ownership are rejected. Formal C++ remains blank because there is no nonblank emitter/source root.

- 2026-06-12 C001 Batch C001-009:
  - Before: `85/88`, pre-migration parent routing blank, and IDA did not model the exact raw helper body as a function.
  - After: raised to `88/92`, `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS` remain, and C001 applied the safe IDA 9.1 fix `define_func 0x004b81a0-0x004b81eb` plus function rename `WideStringHashHelper`.
  - Summary/evidence: C001 IDA MCP reconfirmed exact isolation from `0xcc` padding, no xrefs to `0x004b81a0`, normal xrefs to adjacent helpers, and post-fix Hex-Rays output matching the documented `wcslen` plus `1000003` wide-string hash recurrence. Ownership remains unassigned because `StringUtil` is only a semantic candidate and no caller, pointer, table, or source-family evidence proves it as the direct parent.
- 2026-06-13 B003 second-pass owner/emitter review:
  - Changed to: kept `88/92`, `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++.
  - Summary/evidence: fresh PE/raw scans found no VA/RVA pointer, no `push` pointer, no external rel8/rel32 branch or call to the helper start, no table/vtable route, and no source-use context; the exact helper body and `1000003` multiply remain unique, while adjacent `FileExists`, `BuildFieldMapPath`, and `RectAnchorAxisAdjustHelper` starts retain normal caller/reference surfaces. `StringUtil` remains the best semantic candidate, but behavior alone is not enough to set a canonical owner or emitter route.
- 2026-06-08 A006 Batch 118 evidence refresh:
  - Before: `COMPLETION:82`, `CONFIDENCE:84`.
  - Changed to: `COMPLETION:83`, `CONFIDENCE:85`.
  - Summary/evidence: live IDA reconfirmed the raw unmodeled body bytes, no direct xrefs, exact padding, and adjacent `FileExists` boundary. Parent/source candidates in StringUtil, CachedHashTable, RectBounds, and PathUtil were checked but not assigned because no caller or table reference proves direct ownership.
- 2026-06-10 B001-031 ownership/reachability finalization:
  - Before: `COMPLETION:83`, `CONFIDENCE:85`, pre-migration parent routing blank.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:88`, pre-migration parent routing still blank.
  - Summary/evidence: live IDA MCP and IDA Python reconfirmed the exact raw helper bytes and padding, absence of a function object, absence of callers/xrefs, absence of raw VA/RVA pointers to `0x004b81a0`, unique `1000003` multiply evidence, and normal reference surfaces for adjacent `FileExists`, `BuildFieldMapPath`, and rect-anchor helpers. The child now clears the behavior/evidence gate, but assignment remains blocked because no direct owner link distinguishes StringUtil from retained external/library-style utility code.
- 2026-06-01: Changed completion/confidence from `50/65` to `70/78` and marked reconstructable.
  - Before: The page had behavior notes but stale low-score metadata and no explicit current IDA model/caller state.
  - After: The page records the current raw-function status, no-xref/no-caller result, next-function boundary, and source-owned reconstructable disposition while leaving pre-migration parent routing blank.
  - Evidence: IDA MCP `lookup_funcs`, `disasm`, `xrefs_to`, `callers`, `callees`, and `decompile` rechecked the range on 2026-06-01.
- 2026-06-03: Changed completion/confidence from `70/78` to `74/80`.
  - Before: The page lacked post-restart IDA MCP evidence and remained just below the strong-confidence threshold.
  - After: The page records refreshed raw-boundary, negative-reference, neighbor-reference, and unique immediate-pattern evidence while keeping source ownership unassigned.
  - Evidence: Restarted IDA MCP `py_eval` confirmed raw/unmodeled status, no direct or literal start-address references, exact wide-string hash instruction sequence, unique `0x0F4243` multiply site, and alignment bytes on 2026-06-03.
- 2026-06-06: Changed completion/confidence from `74/80` to `80/84`.
  - Before: The page described the hash loop but did not explicitly record the argument/return convention, lack of side effects, null-input assumption, or current uniqueness/caller checks.
  - After: The page records the likely `const uint16_t *` input, `uint32_t` return, no-call/no-global side-effect profile, `1000003` uniqueness evidence, exact `0xcc` boundary bytes, and the reason source attachment remains blocked.
  - Evidence: IDA MCP `disasm`, `find_insn_operands`, `callers`, `xrefs_to`, and `py_eval` byte scans rechecked the raw helper on 2026-06-06.
- 2026-06-07 Batch 004 gate audit:
  - Before: `COMPLETION:80`, `CONFIDENCE:84`, pre-migration parent routing blank.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:84`, pre-migration parent routing still blank.
  - Summary/evidence: live IDA reconfirmed the raw unmodeled status, no direct xrefs, exact padding boundaries, adjacent `FileExists` start, and unique `1000003` multiply site. Confidence did not increase because source ownership remains unresolved.
