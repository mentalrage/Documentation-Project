*** UID:0003YS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Reviewed non-emitting 43-byte compiler exception-cleanup helper for partially constructed RankingCategoryRecord ranges: one EH caller, 0x2b0 record stride, +0x258 nested user-entry-vector teardown, exact fences/bytes/xrefs, std::vector growth rollback source cause, UID0000BK semantic ownership, and no standalone CPP/H emitter. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045d5c0-0x0045d5eb RankingCategoryRecordRangeDestroyUserEntries

## Status

- Disposition: reviewed non-emitting compiler-generated exception-cleanup helper.
- Entity kind: compiler/container lowering for category-record range rollback, not a source-authored function.
- Semantic owner: [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md).
- Split parent: [UID:0001ZD][0x0045d530-0x0045d740.RankingCategoryVectorStorageHelpers](by-memory/0x0045d530-0x0045d740.RankingCategoryVectorStorageHelpers.md).
- Final C++: blank; rebuilt source should express the record/vector construction path and let compiler cleanup code regenerate this range cleanup.

## Behavior

Walks a half-open range of `RankingCategoryRecord` objects by the `0x2b0` record stride and destroys each embedded `RankingUserEntry` vector at record offset `+0x258`.

## Evidence

- B002's 2026-06-17 report rechecked this exact function start at `0x0045d5c0`, its `0x0045d5eb` end, and the surrounding `0xcc` padding spans.
- The ordinary caller context is category-record vector growth/copy cleanup, not a user-facing ranking method.
- The body delegates each embedded vector cleanup to [UID:0003YT][0x0045d5f0-0x0045d653.RankingUserEntryVectorDestroy](by-memory/0x0045d5f0-0x0045d653.RankingUserEntryVectorDestroy.md).

## UID0003YT Call Contract

The call at `0x0045d5d6` passes each record's embedded vector triplet at `record + 0x258` to UID0003YT, then this helper advances the current record pointer by `0x2b0` until it reaches the half-open end pointer. UID0003YT is the exact 99-byte `void __thiscall` teardown specialization for `std::vector<RankingUserEntry>`. It derives the capacity allocation span from the `0x4c` entry stride, frees small storage directly, validates the aligned large-allocation header for spans at least `0x1000`, calls exact no-return runtime symbol `__invalid_parameter_noinfo_noreturn` on failure, calls `sub_5C7526` on success, and clears begin/end/capacity. It performs no element-destructor loop because the row type is trivial.

The range helper and target are compiler/container cleanup generated from category-record range construction and each record's vector member. UID0003YT is semantically owned by [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md) but is `RECONSTRUCTABLE:FALSE`, with blank emitter and formal CPP/H. This page now has the same resolved non-emitting disposition under UID0000BK semantic ownership. Final human source must express the record/container lifetime and let the selected compiler regenerate equivalent cleanup rather than introducing either binary helper as a source function.

## Heuristic / Inference Notes

- Best descriptive name: `RankingCategoryRecordRangeDestroyUserEntries`.
- Rejected: direct [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md) owner. The helper is range-level collection cleanup for many records, not a normal record method.
- Rejected: standalone source function emission. It is compiler cleanup/container support; formal C++ remains blank.

## Exact Behavior And ABI

- The exact modeled function is `[0x0045d5c0,0x0045d5eb)`, size `0x2b` / 43 bytes. It has 18 instructions, four basic blocks, cyclomatic complexity three, no strings, and `retn 8` for two explicit four-byte arguments.
- Current binary prototype evidence is `void __stdcall(int, int)`. The two arguments are semantically `RankingCategoryRecord *first` and `RankingCategoryRecord *last`: the body compares the current pointer with the end pointer, advances by exact `sizeof(RankingCategoryRecord) == 0x2b0`, and treats current `+0x258` as the nested vector receiver.
- The complete physical frame is `__saved_registers +0x8/4/_DWORD`, `__return_address +0xc/4/_UNKNOWN *`, `arg_0 +0x10/4/_DWORD`, and `arg_4 +0x14/4/_DWORD`, with no other rows. All four entry comment channels were blank during the accepted read-only audit.
- The loop is half-open and equality-terminated: for every record in `[first,last)`, `lea ecx,[current+0x258]` calls [UID:0003YT][0x0045d5f0-0x0045d653.RankingUserEntryVectorDestroy](by-memory/0x0045d5f0-0x0045d653.RankingUserEntryVectorDestroy.md), then `current += 0x2b0`. Empty ranges perform no call.

## Raw And IDA Evidence

- Exact body bytes are `55 8b ec 56 8b 75 08 57 8b 7d 0c 3b f7 74 16 90 8d 8e 58 02 00 00 e8 15 00 00 00 81 c6 b0 02 00 00 3b f7 75 eb 5f 5e 5d c2 08 00`, SHA256 `F0B538E7F49394DCC6AFEA6B6041D072FA9971DD4C711B8C61416B28F35574FC`.
- The predecessor `[0x0045d5b5,0x0045d5c0)` is one unnamed/untyped non-code data item containing eleven `0xcc` bytes, SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`, with blank comments and zero xrefs.
- The successor `[0x0045d5eb,0x0045d5f0)` is one separate unnamed/untyped non-code data item containing five `0xcc` bytes, SHA256 `992CF51486F901AA1AA7D1F63E18C150C9156166E08E0B9E59CC3FAF98177329`, with blank comments and zero xrefs. UID0003YT begins exactly at `0x0045d5f0`.
- There is exactly one incoming code xref, `0x0045d187`, and no incoming data xref. Searches for target VA bytes `c0 d5 45 00`, RVA bytes `c0 d5 05 00`, and raw-file-offset bytes `c0 c9 05 00` each returned zero matches, rejecting vtable, callback, registration, and standalone API routes.
- The only real external call is `0x0045d5d6 -> 0x0045d5f0`. A `func_profile` self-edge is analysis noise; exact disassembly contains no recursive target call.

## Caller And Exception-Cleanup Context

- The sole caller is [UID:0003X0][0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth](by-memory/0x0045cf50-0x0045d1ad.RankingCategoryRecordVectorInsertWithGrowth.md), a 605-byte category-record vector insertion/growth helper with 227 instructions, 39 blocks, and exactly two parser callers at `0x0045c502` and `0x0045c7dc`.
- The target call is in the caller's exception tail after its ordinary `retn 8`, not in its normal source path. At `0x45d17e` and `0x45d184` the tail pushes the saved partial-range end and start/current pointers; `0x45d187` calls this range cleanup; `0x45d195` calls the category-record storage free helper; `0x45d19e` calls `__CxxThrowException@8` to propagate the exception.
- The exact cleanup prefix `[0x45d17e,0x45d19e)` has SHA256 `2B74BEEB2F4D02D62B144DCB3F0FC0351D687E3036DD7CF04C9420A2204ECF35`. It contains the range call, storage-free arguments/call, and two zero throw arguments; the immediately following instruction is the throw call.
- This ordering is the standard strong-exception-safety rollback for failed `std::vector<RankingCategoryRecord>` growth: destroy every successfully constructed destination record's nested vector, free the failed destination allocation, and rethrow.

## Ownership And Source Placement

- UID0000BK is the semantic owner because the only call is generated by the collection's outer `std::vector<RankingCategoryRecord>` insertion/growth operation. The explicit iterator-like ABI is expected compiler lowering and does not require a collection receiver.
- UID0000BL owns `RankingCategoryRecord::m_userEntries` and UID0003YT's nested teardown semantics, but it does not own this outer two-pointer range operation. A single-record method is therefore rejected.
- UID0000MZ `RankingDialog` is the evidence-backed physical file route (`NexusTK/ui/dialogs/RankingDialog.cpp/.h`) but is too broad to replace UID0000BK as semantic owner. Owner `NONE` is rejected because the source operation is known.
- A standalone helper file or free-function source definition is rejected: the target has no data/pointer/API route and contains only compiler lifetime machinery. The descriptive name `RankingCategoryRecordRangeDestroyUserEntries` is suitable for documentation/IDA but is not claimed as an original source symbol.

## No-Code Proof And Source Consequences

- Formal `RECONSTRUCTION_CPP` and `RECONSTRUCTION_H` remain exactly blank. No body, declaration, covered-by marker, comment-only emitter, allocator transcription, or illustrative sample belongs in either channel.
- The human source cause is ordinary period-appropriate C++: UID0000BK owns a category-record vector, and UID0000BL declares `std::vector<RankingUserEntry> m_userEntries`. The compiler must regenerate equivalent partial-range rollback around vector growth.
- Runtime fidelity still requires destruction of every successfully constructed record's nested vector before storage release and rethrow. Blank target code means compiler regeneration, not omitted behavior.

## Score Rationale And Closed Questions

- Completion `94`: exact body, ABI, frame, comments, branches, caller, callee, EH order, record/member strides, boundaries, hashes, pointer negatives, owner, source placement, no-code decision, and historical correction are documented.
- Confidence `95`: raw bytes, live IDA evidence, the exact caller tail, the `0x2b0` record UDT, UID0003YT's accepted vector teardown, the analogous compiler cleanup funclet, and current source/class documentation converge.
- The body is not handwritten: EH-only reachability, post-return placement, compiler lifetime operations, and zero registration/data routes resolve that alternative. UID0000BL direct ownership, owner `NONE`, standalone source emission, and a separate source file are also rejected.
- The score ceiling reflects only unavailable original private compiler symbol spelling and exact compiler/library minor-version build identity. Those rare unavailable facts do not justify raw names, unresolved output, or a lower source-quality disposition.

## Historical Assumptions

- B002's 2026-06-17 work correctly established this exact range, descriptive role/name, `0x2b0` stride, `+0x258` nested vector, UID0000BK semantic route, split parent, and blank-code policy.
- Its earlier `RECONSTRUCTABLE:TRUE` and `EMITTER_UIDS:0000BK` routing treated semantic ownership as a source-emission route. The dedicated target audit and accepted UID0003YT policy supersede only that routing: UID0003YS is compiler-generated, `RECONSTRUCTABLE:FALSE`, and non-emitting while retaining UID0000BK semantic ownership.

## Cross-References

- [UID:0000BK][RankingCategoryCollection](by-class/RankingCategoryCollection.md)
- [UID:0001ZC][0x0045c260-0x0045d4f8.RankingCategoryCollection](by-memory/0x0045c260-0x0045d4f8.RankingCategoryCollection.md)
- [UID:0001ZD][0x0045d530-0x0045d740.RankingCategoryVectorStorageHelpers](by-memory/0x0045d530-0x0045d740.RankingCategoryVectorStorageHelpers.md)
- [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md)

## Changes

- 2026-08-05 B005 UID0003YS dedicated source-quality implementation:
  - Raised `86/91` to `94/95`, changed reconstructable true to false, cleared the emitter, preserved blank formal CPP/H and `Nested:0`, and populated the exact reviewed Item Summary.
  - Added exact 43-byte body/ABI/frame/comment evidence, pre/post fences and hashes, one-caller/one-real-callee and pointer-negative inventories, the post-return rollback/free/rethrow sequence, ownership ranking, source placement, no-code proof, score rationale, and historical correction.

- 2026-08-04 B007 UID0003YT range-caller callback:
  - Preserved this page's existing metadata and blank formal CPP/H.
  - Added the exact `0x45d5d6` half-open-range call contract, `0x2b0`/`+0x258` layout relation, UID0003YT allocation/free and triplet-clear semantics, exact runtime-callee identity, and compiler-regeneration boundary.

- 2026-06-17 B002 source-quality execution:
  - Created exact child page from the former broad helper island.
  - Evidence: B002 rechecked function boundaries, `0x2b0` range walk, embedded vector cleanup role, owner alternatives, and final-C++ blank policy.
