*** UID:0003NA | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:97 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
TextButtonExControlPane::~TextButtonExControlPane()
{
    delete m_labelTextBox;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact 53-byte retained ordinary TextButtonExControlPane destructor with closed CC fences and unique signature. It restores the three derived vptr views, null-safely deletes owned TextBoxPane *m_labelTextBox at +0x10c, and tail-chains compiler Pane teardown. Human source is exactly delete m_labelTextBox; vptr writes, virtual deleting dispatch, and base call are compiler lowering. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004955e0-0x00495615 - TextButtonExControlPane Raw Teardown

## Status

- Disposition: reconstructable NexusTK project code; exact ordinary teardown now modeled in IDA as `TextButtonExControlPane__Destructor`.
- Rebuild handling: `source-authored` destructor-family helper code inside the corrected [UID:000118][0x00494eb0-0x0049803a.ButtonChoiceControlCore](by-memory/0x00494eb0-0x0049803a.ButtonChoiceControlCore.md) ButtonChoice control aggregate.
- Direct owner: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`.
- Emitter route: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`, which routes to [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md) `ButtonControlPane.cpp`.
- Confidence: very strong for exact bytes, range, vtable identity, field type/ownership, ordinary-destructor role, source body, and applied exact IDA function model.
- C++ reconstruction: exact one-line human destructor at source position `20`; the declaration belongs in [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md), so H remains blank here.

## Covered Range

| Start | End | Size | IDA status |
| --- | --- | ---: | --- |
| `0x004955e0` | `0x00495615` | `0x35` | Exact IDA function `TextButtonExControlPane__Destructor`. |

Boundary notes:

- `0x004955dc-0x004955e0` is four `0xcc` bytes after the `TextButtonExControlPane` constructor at `0x00495450-0x004955dc`.
- `0x00495615-0x00495620` is eleven `0xcc` bytes before the modeled `TextButtonExControlPane::SetState` helper at `0x00495620`.
- Historical pre-Gate2B MCP `lookup_funcs` on 2026-06-13 reported `0x004955e0` as `Not a function` and `0x00495620` as `sub_495620`, size `0x12c`; the raw-start statement is retained only as dated prestate and is superseded by the applied function below.
- MCP `make_signature_for_range 0x004955e0-0x00495615` reports a unique signature for the exact helper bytes.
- Exact 53-byte SHA256: `79C8CC6513105F1AC4BD4DBAD650CF17FB1ED9CA7EC22825358AFFA4F245DF9C`.
- Exact bytes: `56 8B F1 8B 8E 0C 01 00 00 C7 06 24 7D 61 00 C7 86 A0 00 00 00 90 7D 61 00 C7 86 A4 00 00 00 C0 7D 61 00 85 C9 74 06 8B 01 6A 01 FF 10 8B CE 5E E9 6B EF 0A 00`.
- The four-byte pre-fence SHA256 is `8843B54D2DF63CA265CF4A05D27DD2B29A74FB476D296DD44A0E171D74B441CA`; the eleven-byte post-fence SHA256 is `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`.

## Raw Behavior

The helper restores the three `TextButtonExControlPane` vtable views, checks the text-provider child pointer at `this+0x10c`, calls that child object's primary virtual with delete flag `1` if present, and then jumps to shared pane cleanup at `sub_544580`.

This is an ordinary teardown helper paired with the constructor at `0x00495450` and the scalar deleting destructor at `0x0049b860`.

The source/compiler split is exact. The human destructor body is only `delete m_labelTextBox;`. The compiler inserts all three vptr stores, lowers `delete` to the null check plus virtual deleting call with flag `1`, and tail-chains the implicit Pane base destructor. Reproducing those operations explicitly would produce decompiler-shaped duplicate ABI code rather than plausible original C++.

## IDA MCP Evidence

MCP `search_text` over `0x004955e0-0x00495615` returned the raw listing:

| Address | Instruction evidence |
| --- | --- |
| `0x004955e0` | `push esi`; `mov esi, ecx`. |
| `0x004955e3` | `mov ecx, [esi+10Ch]`. |
| `0x004955e9` | Writes primary vtable `0x00617d24`. |
| `0x004955ef` | Writes secondary vtable `0x00617d90` at `this+0xa0`. |
| `0x004955f9` | Writes tertiary vtable `0x00617dc0` at `this+0xa4`. |
| `0x00495603` | Tests the child pointer. |
| `0x00495609` | Pushes delete flag `1` and calls the child virtual slot `[eax]`. |
| `0x00495610` | Jumps to `sub_544580`. |

MCP `xrefs_to` on 2026-06-13 reports no cross-references to `0x004955e0`.

Fresh negative checks also found no encoded VA, RVA, or raw-file-offset pointer to the exact start. The wildcarded byte signature is unique, and the body is bounded by alignment fences. The no-xref result therefore records retained/linker-unreferenced source, not padding or random data.

MCP vtable xrefs on 2026-06-13 report:

- `0x00617d24` referenced at constructor `0x004954dc`, raw helper `0x004955e9`, and scalar deleting destructor `0x0049b86c`.
- `0x00617d90` referenced at constructor `0x004954e2`, raw helper `0x004955ef`, and scalar deleting destructor `0x0049b872`.
- `0x00617dc0` referenced at constructor `0x004954ec`, raw helper `0x004955f9`, and scalar deleting destructor `0x0049b87c`.

## Applied IDA State

- Supervisor Gate 2B defined exactly `[0x004955e0,0x00495615)` as `TextButtonExControlPane__Destructor` with signature `void __thiscall TextButtonExControlPane__Destructor(TextButtonExControlPane *this)`. The function-repeatable comment is exactly: `Source ordinary TextButtonExControlPane destructor. Deletes owned TextBoxPane *m_labelTextBox at +0x10c, then compiler lowering chains Pane teardown. Vptr stores/base call are compiler generated; source body is delete m_labelTextBox.` Address regular/repeatable and function-regular comment channels remain blank.
- IDA's authoritative post-action frame contains only `__return_address` at `+0x4`, not the report-predicted `+0x0`. Its automatic local is `void (__thiscall ***v2)(_DWORD)`, not the predicted two-argument function-pointer form. These are bounded analysis-presentation normalizations; they are recorded as observed and were not coerced.
- The receiver is exact `TextButtonExControlPane *this`; the function contains no source parameters. The 14 instruction heads, exact bytes, four-byte pre-fence, eleven-byte post-fence, successor `[0x00495620,0x0049574c)`, zero-xref state, and unique signature are unchanged.
- The companion constructor, scalar deleting wrapper, and two adjustors now have their exact accepted names/signatures/function-repeatable comments. The only IDA type added for this family is incomplete `TextButtonExControlPane` for pointer prototypes.
- Protected bytes/ranges/xrefs/types/comments remained unchanged. The saved IDB readback is SHA256 `408F73923C6BDD17931E828365DC0D7D67F583FE6F0FDDA50103F45FB84461A7`.

MCP `decompile 0x00495450` confirms the paired constructor writes the same three vtable views, allocates a `372` byte child/provider, stores it at `this+0x10c`, sets its bounds through vtable slot `+0x2c`, and shows it.

## Ownership Analysis

Best owner: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`.

Evidence for:

- The helper writes the exact `TextButtonExControlPane` vtable triplet.
- It releases the `+0x10c` text-provider child documented on [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md).
- The same vtable writes and child-release pattern appear in the class scalar deleting destructor.
- The child and direct parent both clear the strict `85/85` gate after this page is scored.
- The wrapper's `0x110` size path proves the complete layout: `ButtonControlPane` occupies `0x10c`, followed by the owned four-byte `TextBoxPane *m_labelTextBox` field.
- The exact ordinary body is the only retained source-shaped teardown for this derived field and is byte-prefix equivalent to the cleanup portion of the complete-object wrapper before that wrapper's hidden deleting-flags dispatch.

Rejected owners:

- `TextButtonControlPane`: this helper does not free the plain text buffer and writes TextButtonEx vtables, not TextButtonControlPane vtables.
- `ButtonControlPane`: it is a derived-class teardown body, not base button behavior.
- Feature/fitting-room owners: older generated names around `0x00495450` are owner-polluted; current IDA vtable evidence points to generic `TextButtonExControlPane`.

## Source Shape And Compiler Boundary

- Recommended source name: `TextButtonExControlPane::~TextButtonExControlPane`.
- Recommended source body: `delete m_labelTextBox;`.
- The private field name is inferred rather than symbol-proven, but `m_labelTextBox` is the strongest human spelling: constructor type, owned lifetime, current-text use, and label-rectangle helpers all converge on that role. A raw offset or IDA-generated name is not acceptable final source.
- The virtual destructor declaration on UID0000EK causes the primary deleting wrapper UID0002Y7 and both adjusted entries UID0002Y6. Those pages remain compiler evidence and emit no source.
- Explicit vptr writes, `Pane__Destructor`, return-`this`, deleting flags, and manual operator-delete logic are rejected from this CPP body because they are compiler lowering.

## Liveness, Rejected Alternatives, And Historical Corrections

- The absence of direct xrefs did not justify deleting or declassifying this body: its unique coherent instruction sequence, closed fences, exact vtable triplet, owned-child cleanup, and wrapper equivalence identify a retained ordinary destructor with very strong confidence. The earlier lack of a modeled function was historical prestate and has been resolved.
- `TextButtonControlPane` is rejected because its owned state and vtables differ; `ButtonControlPane` is rejected as the direct owner because the body restores derived TextButtonEx vptrs and deletes a derived-only field.
- A separate raw helper API is rejected. This is the ordinary destructor implementation, not an independently named callable helper.
- The prior blank-C++ disposition was a valid cautious checkpoint while class layout and source shape remained open. UID0002Y7 research resolves those blockers; preserving blank CPP now would lose the authored source cause and keep generated output incomplete.

## Score Rationale

Completion is `95`: exact boundaries, bytes/hash, both fences, instruction behavior, vtable writes/xrefs, field layout, child lifecycle, unique signature, pointer-route negatives, constructor/wrapper equivalence, owner/emitter route, exact source body, and source/compiler separation are documented.

Confidence is `97`: constructor, ordinary body, scalar wrapper, three vtable views, allocation size, and field consumers independently converge. It is capped only by unavailable original private spelling and the retained body's lack of a live call route; neither changes its source role or behavior.

## Cross-References

- [UID:000118][0x00494eb0-0x0049803a.ButtonChoiceControlCore](by-memory/0x00494eb0-0x0049803a.ButtonChoiceControlCore.md) `ButtonChoiceControlCore`
- [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`
- [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md) `ButtonControlPane`
- [UID:0002Y7][0x0049b860-0x0049b8c7.TextButtonExControlPaneScalarDeletingDestructor](by-memory/0x0049b860-0x0049b8c7.TextButtonExControlPaneScalarDeletingDestructor.md) `TextButtonExControlPaneScalarDeletingDestructor`
- [UID:0002Y6][0x0049b071-0x0049b087.TextButtonExControlPaneAdjustorThunks](by-memory/0x0049b071-0x0049b087.TextButtonExControlPaneAdjustorThunks.md) `TextButtonExControlPaneAdjustorThunks`

## Changes

- 2026-07-29 B004 post-IDA reconciliation:
  - Replaced the stale current no-function description with the applied exact `TextButtonExControlPane__Destructor` function, signature, function-repeatable comment, and incomplete pointer-only class type.
  - Recorded IDA's authoritative bounded normalization: sole `__return_address +0x4` and auto local `void (__thiscall ***v2)(_DWORD)`; preserved all bytes, fences, successor, xrefs, and source/formal metadata.
  - Recorded saved IDB SHA256 `408F73923C6BDD17931E828365DC0D7D67F583FE6F0FDDA50103F45FB84461A7`.
- 2026-07-28 B004 UID0002Y7 accepted implementation callback:
  - Raised `86/89 -> 95/97`, retained owner/emitter UID0000EK and reconstructable state, and assigned source position `20`.
  - Added exact formal destructor CPP and kept H blank because UID0000EK carries the declaration.
  - Preserved and expanded the complete bytes, historical no-function prestate, boundary, no-xref, unique-signature, vtable, child-lifetime, ownership, and negative evidence; resolved the prior blank-code blocker through the exact human-source/compiler-lowering split.
- 2026-06-13 B003 split repair: created this exact child page for the previously under-documented `0x004955e0-0x00495615` raw teardown body inside UID `000118`. Evidence: MCP `lookup_funcs`, scoped `search_text`, `xrefs_to`, `get_bytes`, `make_signature_for_range`, vtable xrefs, and constructor decompilation confirm exact boundaries, non-function status, vtable identity, and owner routing.
