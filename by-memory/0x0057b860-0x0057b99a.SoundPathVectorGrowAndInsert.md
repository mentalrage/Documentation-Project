*** UID:0004FJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact compiler-generated old-MSVC std::vector<SoundPathString> push/insert slow path used by SoundManager playlist and local-music source: range 0x0057b860-0x0057b99a, three full-vector callers, 1.5x growth, append/middle-insert copy branches, storage commit, EH cleanup/free/rethrow, and padding before 0x0057b9a0. Historical custom SoundPathVector/helper interpretations remain documented, but this child is non-reconstructable/non-emitting and its formal C++ stays blank. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057b860-0x0057b99a SoundPathVector Grow And Insert

## Status

- Disposition: non-reconstructable compiler/STL child for `std::vector<SoundPathString>` `push_back`/insert slow-path lowering. Exact binary evidence is retained; no custom source method is emitted.
- Parent range: [UID:0001ID][0x0057b860-0x0057bf6e.SoundPathVector](by-memory/0x0057b860-0x0057bf6e.SoundPathVector.md).
- Documentation route: non-emitting [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md); the source cause is typed vector operations in [UID:0000NV][SoundManager](by-file/SoundManager.md), including source-ready UID0001IA.
- Generated-output expectation: blank `EMITTER_UIDS` and formal body. This child must not appear as handwritten ABI code in generated `SoundManager.cpp`; the compiler regenerates it from consumer operations.

## Evidence

Historical B007 MCP evidence gathered while session `fef9a7c2` was active confirms `0x0057b860` as `sub_57B860`, size `0x13a`, giving the exact half-open range `0x0057b860-0x0057b99a`. `lookup_funcs` reported `0x0057b99a` is not a function and `0x0057b9a0` is the adjacent `sub_57B9A0`, size `0xb`. Current supervisor-verified live MCP session for future UID0004FJ checks is `6a4af54d`.

Historical `xrefs_to 0x0057b860` on then-active `fef9a7c2` reported exactly three direct code xrefs:

- `0x005798e9` in `sub_5797B0`.
- `0x0057acab` in `sub_57A890`.
- `0x0057aef5` in `sub_57A890`.

Caller decompilation shows the same full-vector pattern at each site. The `0x005797b0` caller formats a `%08d.MP3` path string, checks whether the target vector end equals capacity, calls this grow helper only when the vector is full, and otherwise copy-constructs directly through `sub_5829F0` at `m_end` before incrementing the end pointer. The `0x0057a890` local music directory scanner does the same at `0x57acab` and `0x57aef5`: full-vector paths call this child, non-full paths call `sub_5829F0` and advance the vector end. This makes UID0004FJ a `SoundPathVector` grow/insert helper, not a standalone `SoundManager` method.

The historical `fef9a7c2` callee list is `sub_421520`, `sub_5829F0`, `sub_57BED0`, `sub_57BE60`, `sub_57BCE0`, `sub_57BD60`, `sub_423E00`, `__CxxThrowException@8`, and `sub_421500`. Normal flow allocates storage, constructs/copies entries, and commits replacement storage. Cleanup/error flow uses the destroy/free/rethrow helpers.

The decompiled and disassembled body proves:

- Reads `m_begin`, `m_end`, and `m_capacity` through the three-pointer vector layout.
- Computes current size as `(m_end - m_begin) >> 2` and insert index as `(where - m_begin) >> 2`.
- Rejects size `0x3fffffff` through `sub_421500` at `0x57b995`.
- Computes requested size `oldSize + 1`.
- Computes new capacity as old capacity plus half, with minimum requested size and overflow fallback.
- Allocates the new block through `sub_421520` at `0x57b8de`.
- Constructs the inserted `SoundPathString` first through `sub_5829F0` at `0x57b90a`.
- For append-at-end insertion, calls [UID:0004FQ][0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail](by-memory/0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail.md) / `sub_57BED0` at `0x57b927` to copy the old range into the new block.
- For middle insertion, calls `sub_57BE60` twice at `0x57b933` and `0x57b946` to copy the prefix before the inserted entry and the suffix after it.
- Calls `sub_57BCE0` at `0x57b952` to replace storage with the new block, new live count, and new capacity.
- Returns the inserted slot by adding the saved insert offset to the post-commit `m_begin`.

The cleanup path is material source-shape evidence. Disassembly places cleanup at `0x57b970-0x57b990`: the block pushes the tracked constructed range, calls `sub_57BD60` for partial destruction, pushes the new block/capacity state, frees the block through `sub_423E00`, then rethrows through `__CxxThrowException@8`. This is why a polished source body needs the helper contracts and EH cleanup shape resolved before emission.

Boundary bytes from historical session `fef9a7c2` show the target ends cleanly. Bytes at `0x0057b990` include the rethrow call, the length-error call, then six `0xcc` padding bytes at `0x0057b99a-0x0057b9a0` before the adjacent singleton clear helper starts at `0x0057b9a0`. Bytes before `0x0057b860` show the previous return and padding into this function start. No xrefs target `0x0057b99a`.

This range does not include the adjacent singleton clear helper at `0x0057b9a0`, the raw entry-destroy helper, the Deque destroy child, SoundManager scalar deleting destructor glue, reallocate, clear, or later helper-tail children. Those ranges remain separate child/support docs.

## No-Code Proof

Current proof: `0x00421500` reaches `vector<T> too long`, and this child has the old Dinkumware three-pointer layout, max-size guard, 1.5x growth, append/middle uninitialized-copy branches, storage commit, and automatic cleanup/rethrow expected from `std::vector<SoundPathString>` insertion. It is compiler-generated, not a source-authored `SoundPathVector::GrowAndInsert` method. Therefore `RECONSTRUCTABLE:FALSE`, blank emitter, and blank C++ are the exact source-quality disposition even though every binary helper contract is now documented.

Historical proof below explains why earlier passes kept a reconstructable child blank before the standard-container source category was resolved. It remains useful provenance but no longer represents an open source-code blocker.

Formal C++ remains blank. The earlier "final vector layout names" blocker is superseded: [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md) now accepts `m_begin`, `m_end`, and `m_capacity`, and [UID:0001IF][0x0057bf40-0x0057bf6e.SoundPathVectorClear](by-memory/0x0057bf40-0x0057bf6e.SoundPathVectorClear.md) uses the accepted source-facing `SoundPathString` alias for ANSI `mystr::StringBase<char, mystr::mychar_traits<char> >`.

The remaining no-code proof is narrower and target-specific. A correct `SoundPathVector::GrowAndInsert` source body would need final-safe allocator/free contracts, copy/destroy/replace helper contracts, and integrated EH cleanup semantics for `sub_421520`, `sub_423E00`, [UID:0004FQ][0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail](by-memory/0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail.md) / `sub_57BED0`, `sub_57BE60`, `sub_57BCE0`, and `sub_57BD60`. Some sibling helpers have since been promoted, but the full grow/insert body still depends on the combined allocator/capacity contract, append-tail copy strategy, partial destruction bounds, free/rethrow path, and exact source retention strategy. Emitting this child now would either use raw helper names such as `sub_57B860`/`sub_57BD60` or invent a polished source contract before the remaining helper strategy is accepted.

Rejected alternatives: do not move direct ownership to [UID:0000NV][SoundManager](by-file/SoundManager.md); SoundManager owns the source family and callers, but this body operates on `SoundPathVector` storage. Do not emit decompiler-shaped C++ or raw `sub_57B860` naming. Do not fold adjacent singleton clear, raw helper bytes, Deque cleanup, SoundManager scalar deleting destructor glue, reallocate, or clear ranges into this target. Do not add marker-only coverage because this range is not already covered by another emitted method. Do not treat absence from generated `SoundManager.cpp` as a defect while `EMITTER_UIDS` remains blank.

2026-07-03 B008 support sync: the accepted UID0004FN report resolves the specific storage-replacement callee at `0x0057b952` as source-ready [UID:0004FN][0x0057bce0-0x0057bd53.SoundPathVectorReplaceStorage](by-memory/0x0057bce0-0x0057bd53.SoundPathVectorReplaceStorage.md), emitted as `SoundPathVector::ChangeArray()` through [UID:0000DI][SoundPathVector](by-class/SoundPathVector.md). Historical B008 MCP evidence gathered while session `fef9a7c2` was active shows this grow/insert body passes the newly allocated buffer, new live count, and new capacity to that helper after constructing/copying entries; future MCP work should use supervisor-verified live session `6a4af54d`. This support fact does not make the full grow/insert child source-ready yet; its own allocator/capacity and copy-failure source shape still require a separate accepted pass.

2026-07-03 B004 support sync: the accepted UID0004FO report resolves the later call at `0x0057b979` as [UID:0004FO][0x0057bd60-0x0057bd84.SoundPathStringDestroyRange](by-memory/0x0057bd60-0x0057bd84.SoundPathStringDestroyRange.md), a modeled range-destroy helper used only by this method's EH cleanup/rethrow path. Historical then-active MCP session `fef9a7c2` proved `sub_57BD60` is a `0x24`-byte loop over four-byte ANSI `SoundPathString` entries, calls `0x00582b30 -> 0x005832f0` StringBase release, has the single xref from `0x0057b979`, and has no xref to `0x0057bd84`. The caller block frees `Block` through `sub_423E00` and rethrows through `__CxxThrowException@8`, so UID0004FO remains blank-emitter/no standalone helper; the behavior belongs to future exception-safe `GrowAndInsert` reconstruction.

2026-07-03 B006 support sync: the accepted UID0004FQ report resolves this method's append-at-end copy call at `0x0057b927` as [UID:0004FQ][0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail](by-memory/0x0057bed0-0x0057bf3e.SoundPathStringCopyConstructRangeTail.md). Historical B006 MCP evidence from now-stale session `fef9a7c2` proves modeled `sub_57BED0` is a `0x6e` / 110-byte loop over four-byte ANSI `SoundPathString` entries, calls StringBase share/copy construction at `0x005829f0`, has the single xref from this append-at-end branch, includes final `retn 0Ch` byte `0x0057bf3d`, and is followed by two-byte padding `0x0057bf3e-0x0057bf40` before `SoundPathVector::Clear()`. UID0004FQ remains blank-emitter/no standalone helper because this method ignores the decompiler-shaped return value, its EH cleanup uses raw `0x0057b9b0`, and original helper/source retention remains unresolved.

## Score Rationale

Completion is `91` because the page records the exact range, callers, callees, 1.5x growth, both insert branches, copy/destruction/commit behavior, cleanup/free/rethrow path, padding, rejected alternatives, historical custom-helper contracts, and resolved standard-vector source category.

Confidence is `95` because historical MCP evidence confirms the exact range, body, xrefs, callees, branch behavior, and boundary bytes, while the vector throw literal and complete helper morphology resolve the source category. Original helper spellings no longer block output because this compiler child intentionally emits no standalone C++.

## Changes

- 2026-07-13 B004 UID0001IA callback: raised `87/91 -> 91/95`, changed `RECONSTRUCTABLE:TRUE -> FALSE`, retained owner UID0000DI as documentation grouping, blank emitter/position/formal C++, and reclassified the body as compiler-generated standard-vector push/insert lowering. Preserved all historical range, caller, growth, helper, EH, padding, rejected-alternative, and no-code evidence.
