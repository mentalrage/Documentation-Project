*** UID:0004XP | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:45 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollCollectionPane::GetPartRect(ScrollCollectionPart part,
                                       RectBounds *bounds)
{
    Point cursor;
    Point origin;

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.x == -1000 && origin.y == -1000) {
        return;
    }
    cursor.x -= origin.x;
    cursor.y -= origin.y;

    *bounds = m_bounds;
    const bool horizontal = (m_orientation != 0);
    const int thumbSpan = horizontal
        ? m_bounds.bottom - m_bounds.top
        : kScrollPanePartExtentBySkin[m_scrollSkinIndex];

    if (part == kScrollCollectionPartLeading) {
        if (horizontal) {
            bounds->right = bounds->left;
        } else {
            bounds->bottom = bounds->top;
        }
        return;
    }
    if (part == kScrollCollectionPartTrailing) {
        if (horizontal) {
            bounds->left = bounds->right;
        } else {
            bounds->top = bounds->bottom;
        }
        return;
    }

    if (!CanScroll()) {
        if (part == kScrollCollectionPartThumb) {
            if (horizontal) {
                bounds->left += thumbSpan;
                bounds->right -= thumbSpan;
            } else {
                bounds->top += thumbSpan;
                bounds->bottom -= thumbSpan;
            }
        } else {
            InitRectBounds(bounds, 0, 0, 0, 0);
        }
        return;
    }

    if (horizontal) {
        const int trackStart = m_bounds.left;
        const int thumbLimit = m_bounds.right - thumbSpan;
        const int thumbStart = trackStart +
            (m_scrollPosition * (thumbLimit - trackStart)) /
                m_scrollRange;
        const int thumbEnd = thumbStart + thumbSpan;
        if (part == kScrollCollectionPartThumb) {
            bounds->left = thumbStart;
            bounds->right = thumbEnd;
        } else if (part == kScrollCollectionPartPageBeforeThumb) {
            bounds->right = thumbStart;
        } else if (part == kScrollCollectionPartPageAfterThumb) {
            bounds->left = thumbEnd;
        }
    } else {
        const int trackStart = m_bounds.top;
        const int thumbLimit = m_bounds.bottom - thumbSpan;
        int thumbStart;
        if (m_thumbDragActive) {
            thumbStart = m_bounds.top + cursor.y - m_thumbDragOffset.y;
            if (thumbStart < trackStart) {
                thumbStart = trackStart;
            }
            if (thumbStart > thumbLimit) {
                thumbStart = thumbLimit;
            }
        } else {
            thumbStart = trackStart +
                (m_scrollPosition * (thumbLimit - trackStart)) /
                    m_scrollRange;
        }
        const int thumbEnd = thumbStart + thumbSpan;
        if (part == kScrollCollectionPartThumb) {
            bounds->top = thumbStart;
            bounds->bottom = thumbEnd;
        } else if (part == kScrollCollectionPartPageBeforeThumb) {
            bounds->bottom = thumbStart;
        } else if (part == kScrollCollectionPartPageAfterThumb) {
            bounds->top = thumbEnd;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ScrollCollectionPane geometry helper computes leading, page-before, thumb, page-after, and trailing rectangles for both orientations, including disabled and live vertical-drag behavior. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00562bf0-0x00562e77 ScrollCollectionPane GetPartRect

## Status

- Reconstructable modeled class helper owned/emitted by [UID:0000CG][ScrollCollectionPane](by-class/ScrollCollectionPane.md) through [UID:0000NG][ScrollCollectionPane](by-file/ScrollCollectionPane.md).
- Aggregate: [UID:0001GT][0x00561db0-0x0056325c.ScrollCollectionPane](by-memory/0x00561db0-0x0056325c.ScrollCollectionPane.md); declaration is centralized in the class H block.

## Exact Range And Caller Graph

- Half-open range `[0x00562bf0,0x00562e77)`, `0x287` / 647 bytes; preceded by 3-byte and followed by 9-byte alignment gaps.
- Thirteen target-internal references make this the common geometry service for paint, hit-test, highlight invalidation, press/drag setup, cursor conversion, and reset.
- The broad caller graph, class-tail field set, and immediate adjacency identify direct ScrollCollectionPane ownership.

## Coordinate And Part Semantics

- Gets the global cursor and pane screen origin, returns on the `(-1000,-1000)` invalid-origin sentinel, then converts cursor coordinates to local space.
- Starts from `m_bounds` and computes five `ScrollCollectionPart` rectangles for vertical or horizontal orientation.
- Leading/trailing parts collapse the appropriate edge. Horizontal thumb span is the control height; vertical thumb span uses `kScrollPanePartExtentBySkin[m_scrollSkinIndex]`.
- When scrolling is unavailable, page rectangles are empty while the thumb consumes the interior between fixed extents.

## Scroll And Drag Geometry

- Available horizontal geometry scales signed `m_scrollPosition` over the track span by signed positive `m_scrollRange`.
- Vertical geometry normally uses the same proportional formula. During live thumb drag it derives the thumb start from local cursor Y minus `m_thumbDragOffset.y`, clamped to track limits.
- It returns exact thumb, page-before, or page-after extents without inventing source-visible bounds checks beyond observed behavior.
- Project `Point` storage/order remains `y,x`; the formal source names members while retaining coordinate semantics.

## Source-Quality And History

- `GetPartRect` is selected over older `GetScrollRegionRect` wording because the enum and callers consistently operate on parts.
- Human source centralizes the accepted geometry helper and removes decompiler pointer arithmetic while preserving sentinel, orientation, disabled-state, clamp, and scaling behavior.
- No original symbols prove spelling, but thirteen callers and exact body make blank or compiler-labeled source inappropriate.

## Score Rationale

- Completion `92`: exact boundary, caller count, all five parts, both orientations, drag/disabled behavior, fields, source name, ownership, and C++ are complete.
- Confidence `94`: modeled body and internal graph are direct; only original spelling/source inlining and rebuilt-binary verification remain.

## Cross-References

- [UID:0000CG][ScrollCollectionPane](by-class/ScrollCollectionPane.md)
- [UID:0001GT][0x00561db0-0x0056325c.ScrollCollectionPane](by-memory/0x00561db0-0x0056325c.ScrollCollectionPane.md)
- [UID:0001VZ][ScrollCollectionPaneLayout](by-type/by-struct/ScrollCollectionPaneLayout.md)
- [UID:0001GU][0x005628d0-0x00562bed.ScrollCollectionPaneHitTestScrollRegion](by-memory/0x005628d0-0x00562bed.ScrollCollectionPaneHitTestScrollRegion.md)

## Changes

- 2026-07-26 B005 implementation callback: created this exact geometry child with deterministic class position `45` and source-ready five-part C++.
