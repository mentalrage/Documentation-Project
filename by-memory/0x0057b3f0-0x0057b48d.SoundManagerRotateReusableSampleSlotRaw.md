*** UID:0003ZJ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B009 2026-06-19 source-quality reanalysis keeps owner [UID:0000DG][SoundManager](by-class/SoundManager.md) and blank emitters/formal C++; current raw PE/Capstone confirms `0x0057b3ec-0x0057b3f0` four `0xcc` bytes before the raw body, exact `0x0057b3f0-0x0057b48d` method-shaped slot-rotation body, `0x0057b48d-0x0057b490` three `0xcc` bytes before `ReinitializeAudioDriver`, no rel32/rel8 branch, VA/RVA/file-offset pointer, vtable, jump-table, or interior-entry route to the start/body, and behavior that scans eight 8-byte `m_sampleSlots` at `this+0x48`, selects a reusable `HSAMPLE` with `AIL_sample_status()==2` or slot 7, rotates the selected slot to index 0 through `GetMemoryMan`/`MemmoveWrapper`/`Memmove0Wrapper`, and calls `AIL_init_sample`; relationship to `PlaySample` proves slot dword 0 is `HSAMPLE` and dword 1 is per-play volume scale while payload pointers live at `this+0x88+4*sampleId`, and `ReinitializeAudioDriver` clears/allocates the same eight handles at `0x0057b5a1-0x0057b5d4`; this is sample/SFX maintenance, not Redbook, `RestartMusic`, or driver reinitialization, so formal C++ stays blank until a live entry route is recovered. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057b3f0-0x0057b48d SoundManagerRotateReusableSampleSlotRaw

## UID0001I9 Live Playback Comparison - 2026-07-15

- This page remains `87/90`, owner [UID:0000DG][SoundManager](by-class/SoundManager.md), reconstructable, non-emitting, without a position, and with blank formal C++. No live inbound route was recovered, so the accepted retained/dead source-body disposition is unchanged.
- Live [UID:0004S7][0x0057a5c0-0x0057a6bf.SoundManagerPlaySample](by-memory/0x0057a5c0-0x0057a6bf.SoundManagerPlaySample.md) now emits the complete source behavior that scans the same eight slots, selects the first stopped sample or final slot, rotates that slot to the front, initializes it, loads the cached DAT payload, applies scaled volume, and starts playback.
- The live `PlaySample` body confirms that slot dword 0 is `HSAMPLE`, slot dword 1 is per-play volume scale, and payload pointers remain at `this+0x88 + 4*sampleId`. The class-facing inline convenience spelling is `PlayEffect`; `PlaySample` remains the canonical out-of-line implementation.
- The separately emitted raw [UID:0004SF][0x0057a7b0-0x0057a7d3.SoundManagerSetDigitalAudioMutedRaw](by-memory/0x0057a7b0-0x0057a7d3.SoundManagerSetDigitalAudioMutedRaw.md) is not precedent for emitting this duplicate body: UID0004SF is unique source behavior, whereas this page duplicates the live slot-rotation portion of `PlaySample` and still has no route. Existing padding, no-xref, rejected Redbook/driver names, and negative-route evidence remain authoritative.

## Status

- Disposition: reconstructable NexusTK project code body, currently non-emitting.
- Entity kind: raw method-shaped `SoundManager` sample/SFX slot-rotation helper with no recovered entry route.
- Current owner: [UID:0000DG][SoundManager](by-class/SoundManager.md).
- Source module: [UID:0000NV][SoundManager](by-file/SoundManager.md).
- B009 source-quality note: current PE/Capstone reanalysis confirms a source-shaped but no-entry SoundManager sample/SFX helper. The best descriptive source-facing name is `SoundManager::RotateReusableSampleSlot()`. `Raw` remains documentation status, not a recovered source spelling.
- Rebuild handling: document as no-entry sample-slot maintenance inside the historical Redbook/audio neighborhood; do not emit formal C++ until a live route is recovered.
- Autogen handling: `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, and blank formal C++ because no direct relative-call, rel8 branch, VA/RVA/file-offset pointer, vtable, jump-table, interior-entry, or known computed-call route was found.

## Direct PE / Range Evidence

The exact target range is `0x0057b3f0-0x0057b48d`, end-exclusive, for `0x9d` / 157 bytes (Verified with `tools/int_convert.py`). Current direct PE mapping places the body at RVA `0x0017b3f0` and file offset `0x0017a7f0`.

| Range | Bytes / role | Decision |
| --- | --- | --- |
| `0x0057b3ec-0x0057b3f0` | `cc cc cc cc` | Four bytes of `int3` padding after [UID:0003ZI][0x0057b370-0x0057b3ec.SoundManagerRestartMusic](by-memory/0x0057b370-0x0057b3ec.SoundManagerRestartMusic.md). |
| `0x0057b3f0` | `55 8b ec 83 ec 0c` | Method-shaped prologue. |
| `0x0057b3f0-0x0057b48d` | raw body | `SoundManager` sample-slot rotation/reinit helper. |
| `0x0057b48c` | `c3` | Final `ret`. |
| `0x0057b48d-0x0057b490` | `cc cc cc` | Three bytes of `int3` padding before [UID:0001IC][0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver](by-memory/0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver.md) / `ReinitializeAudioDriver`. |

Raw byte anchors from the current PE:

```text
pre  0x0057b3ec-0x0057b3f0: cc cc cc cc
head 0x0057b3f0: 55 8b ec 83 ec 0c 53 8b 1d b4 d5 60 00 8b c1 56 57 89 45 fc 33 f6 8d 78 48 0f 1f 80 00 00 00 00
tail 0x0057b46d: e8 be ab f9 ff 8b c8 e8 a7 ad f9 ff 8b 06 5f 5e 5b 85 c0 74 07 50 ff 15 f8 d5 60 00 8b e5 5d c3
post 0x0057b48d-0x0057b490: cc cc cc
```

The parent aggregate filename [UID:0001IB][0x0057b020-0x0057b48a.SoundManagerRedbookPlayback](by-memory/0x0057b020-0x0057b48a.SoundManagerRedbookPlayback.md) has a stale `0x0057b48a` suffix, but the current aggregate documentation already records this precise child through `0x0057b48d` and the `0x0057b48d-0x0057b490` padding. Keep this page as the precise body; do not merge it into `RestartMusic` or `ReinitializeAudioDriver`.

## Behavior

The raw body scans eight 8-byte sample slots at `this+0x48`, selects a reusable slot whose `HSAMPLE` handle reports `AIL_sample_status == 2` or falls back to slot 7, rotates the selected 8-byte slot into slot 0, and calls `AIL_init_sample` on the selected handle. It does not look up a payload pointer, set a sample file, update volume, or start playback, so it is sample/SFX slot maintenance rather than Redbook/CD behavior.

Capstone-resolved behavior:

1. `ecx` is `SoundManager *this`.
2. `0x0057b3f7` loads `mss32.dll!_AIL_sample_status@4` through import pointer `0x0060d5b4`.
3. `0x0057b406` starts scanning at `this+0x48`.
4. Each slot stride is `8`; the loop checks up to eight slots.
5. For each non-null slot handle, `0x0057b416-0x0057b419` calls `AIL_sample_status(handle)` and compares the result to `2`.
6. `0x0057b42e` selects slot `7` when no status-2 handle was found.
7. `0x0057b436` multiplies the selected index by `8`.
8. `0x0057b439-0x0057b44c` copies the selected 8-byte slot to stack temp `ebp-0xc` through [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) and [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md).
9. `0x0057b451-0x0057b461` shifts earlier slots from `this+0x48` to `this+0x50` by `selectedIndex * 8` bytes through [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) and [UID:0001BI][0x00516240-0x00516258.Memmove0Wrapper](by-memory/0x00516240-0x00516258.Memmove0Wrapper.md).
10. `0x0057b466-0x0057b474` copies the temp slot back into slot 0 through `GetMemoryMan` and `MemmoveWrapper`.
11. `0x0057b482-0x0057b483` calls `mss32.dll!_AIL_init_sample@4` through import pointer `0x0060d5f8` if the selected handle is non-null.

Relevant imports from the current PE:

| Import pointer | Import | Role in or around this helper |
| --- | --- | --- |
| `0x0060d5b4` | `mss32.dll!_AIL_sample_status@4` | Called by this raw body and `PlaySample` to find reusable sample handles. |
| `0x0060d5b8` | `mss32.dll!_AIL_set_sample_volume@8` | Used by live `PlaySample`, absent from this raw body. |
| `0x0060d5bc` | `mss32.dll!_AIL_start_sample@4` | Used by live `PlaySample`, absent from this raw body. |
| `0x0060d5c0` | `mss32.dll!_AIL_set_sample_file@12` | Used by live `PlaySample`, absent from this raw body. |
| `0x0060d5cc` | `mss32.dll!_AIL_allocate_sample_handle@4` | Used by `ReinitializeAudioDriver` to create the handles this helper rotates. |
| `0x0060d5f8` | `mss32.dll!_AIL_init_sample@4` | Called by this raw body and live `PlaySample` after rotation. |

Redbook imports are present elsewhere in the binary, but this raw body calls none of them.

## Entry Route / Reachability Proof

Current direct PE scans found no entry route to the start or body:

- No `E8`/`E9` rel32 call or jump target to `0x0057b3f0`.
- No `0F 80..8F` rel32 conditional-branch target to `0x0057b3f0`.
- No rel8 short branch, short conditional branch, or loop-family branch target to `0x0057b3f0`.
- No little-endian absolute VA dword pointer hit for `0x0057b3f0`.
- No little-endian RVA dword pointer hit for `0x0017b3f0`.
- No little-endian file-offset dword pointer hit for `0x0017a7f0`.
- A broader scan for any outside branch into `0x0057b3f0-0x0057b48d` found no outside branch target to any byte inside the target range.
- A broader scan for any outside dword pointer to any VA inside `0x0057b3f0-0x0057b48d` found zero hits.
- Existing SoundManager docs report no vtable route and no jump-table route; the current direct pointer and branch scans are consistent with that.

Reachability conclusion: treat this as a no-entry raw body. The best source-quality explanation is a retained/dead private helper or an older out-of-line helper whose logic was duplicated into the live `PlaySample` implementation. It is source-shaped enough to document as reconstructable, but not safe to emit as active generated C++.

## Relationship To PlaySample

Live [UID:0001I9][0x0057a260-0x0057a88e.SoundManagerPlaybackControls](by-memory/0x0057a260-0x0057a88e.SoundManagerPlaybackControls.md) documents `SoundManager::PlaySample` at `0x0057a5c0-0x0057a6bf`. Direct Capstone comparison proves `PlaySample` contains the same slot scan, rotation, and `AIL_init_sample` subset, then continues into payload assignment, volume calculation, and sample start.

Key live `PlaySample` evidence:

| Address | Evidence | Meaning |
| --- | --- | --- |
| `0x0057a5c9` | checks byte `[ebx+0x41]` | SFX enabled gate. |
| `0x0057a5d3` | compares sample id with `0x3e7` | Rejects sample IDs above `999`. |
| `0x0057a5e0` | checks `[ebx+8]` | Requires digital driver handle. |
| `0x0057a5ee` | `lea edi,[ebx+0x48]` | Scans the same sample-slot array. |
| `0x0057a5f8` | calls `0x0060d5b4` | Uses `AIL_sample_status`. |
| `0x0057a662` | calls `0x0060d5f8` | Reinitializes selected handle with `AIL_init_sample`. |
| `0x0057a671` | loads `[ebx + ecx*4 + 0x88]` | Loads DAT-backed sample payload pointer from the separate payload table. |
| `0x0057a680` | calls `0x0060d5c0` | Assigns payload through `AIL_set_sample_file`. |
| `0x0057a689` | writes `[ebx+0x4c]` | Stores the per-play scale into slot 0 dword 1. |
| `0x0057a68c-0x0057a6a2` | uses `[ebx+0x44]`, `0x7f`, and reciprocal divide for `0x2710` | Computes `m_soundEffectVolume * scale * 127 / 10000` for `AIL_set_sample_volume`. |
| `0x0057a6b0` | calls `0x0060d5bc` | Starts the sample with `AIL_start_sample`. |

This comparison proves:

- `this+0x48-0x87` is an array of eight 8-byte `SoundSampleSlot` entries, total `0x40` / 64 bytes (Verified with `tools/int_convert.py`).
- Slot dword 0 is a Miles `HSAMPLE` handle because both functions pass it to `AIL_sample_status` and `AIL_init_sample`; live `PlaySample` also passes it to `AIL_set_sample_file`, `AIL_set_sample_volume`, and `AIL_start_sample`.
- Slot dword 1 is not a payload pointer. `PlaySample` writes the caller's per-play scale to `this+0x4c`, slot 0 dword 1, then uses it in the volume formula.
- DAT-backed sample payload pointers are separate from the slots at `this+0x88 + 4*sampleId`.
- `0x0057b3f0` repeats only reusable-slot selection, rotation, and `AIL_init_sample`. It does not check SFX enabled state, validate a sample ID, look up a payload pointer, call `AIL_set_sample_file`, store the per-play scale, compute volume, call `AIL_set_sample_volume`, or call `AIL_start_sample`.

`SoundManager::PlaySample`, `ReplaySample`, and `StartSample` are rejected names for this raw helper because the live `PlaySample` method already has the full playback behavior and broad gameplay/UI caller set.

## Relationship To ReinitializeAudioDriver

[UID:0001IC][0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver](by-memory/0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver.md) starts at `0x0057b490`, after the three-byte padding following this raw body. It initializes the same slot array but does not call or fall through into `0x0057b3f0`.

Relevant allocation window:

```asm
0x0057b59b: call    dword ptr [0x60d614]    ; AIL_set_file_callbacks
0x0057b5a1: push    0x40                    ; clear 64 bytes
0x0057b5a3: lea     esi, [edi + 0x48]       ; m_sampleSlots
0x0057b5a6: mov     dword ptr [edi + 0x14], 0
0x0057b5b0: call    0x5ca0b0                ; memset-like clear
0x0057b5b8: cmp     dword ptr [edi + 8], 0
0x0057b5be: mov     ebx, 8
0x0057b5c6: call    dword ptr [0x60d5cc]    ; AIL_allocate_sample_handle
0x0057b5cc: mov     dword ptr [esi], eax    ; slot.handle = allocated HSAMPLE
0x0057b5ce: lea     esi, [esi + 8]
0x0057b5d4: jne     0x57b5c3
```

`ReinitializeAudioDriver` produces and stores the `HSAMPLE` handles in each slot's dword 0. `PlaySample` and this raw helper consume and rotate those handles. The producer/consumer relationship strengthens the field model but rejects merging this target into `ReinitializeAudioDriver`.

## Relationship To RestartMusic

[UID:0003ZI][0x0057b370-0x0057b3ec.SoundManagerRestartMusic](by-memory/0x0057b370-0x0057b3ec.SoundManagerRestartMusic.md) ends at `0x0057b3ec`, followed by four `0xcc` bytes before this target. `RestartMusic` calls the stream stop/fade helper `0x0057a260`, Redbook close `0x0057b160`, Redbook play `0x0057b1b0`, and local/stream play helper `0x00579e30`. It has no call to `0x0057b3f0`, no fallthrough across the padding, and no sample-slot behavior. The only relationship is physical adjacency inside the historical Redbook aggregate.

## Touched State

| Storage | Meaning | Evidence |
| --- | --- | --- |
| `SoundManager+0x41` | `m_soundEffectsEnabled`, inferred/descriptive. | Live `PlaySample` gates at `0x0057a5c9` and rechecks before `AIL_start_sample`; this raw helper omits the gate. |
| `SoundManager+0x44` | `m_soundEffectVolume` or `m_sampleVolume`, inferred/descriptive; prefer `m_soundEffectVolume`. | `PlaySample` loads `[ebx+0x44]`, multiplies by per-play scale and `0x7f`, divides by `0x2710` / 10000, and passes the result to `AIL_set_sample_volume`. |
| `SoundManager+0x48-0x87` | `SoundSampleSlot m_sampleSlots[8]`, eight 8-byte entries. | Raw target and `PlaySample` scan and rotate this array; `ReinitializeAudioDriver` clears `0x40` bytes and allocates eight handles into it. |
| Slot dword 0 | `SoundSampleSlot::handle` (`HSAMPLE`). | Passed to `AIL_sample_status`, `AIL_init_sample`, `AIL_set_sample_file`, `AIL_set_sample_volume`, and `AIL_start_sample`. |
| Slot dword 1 | `SoundSampleSlot::playVolumeScale` / `volumeScale`; prefer `playVolumeScale`. | Live `PlaySample` writes the caller's scale to `this+0x4c`, slot 0 dword 1, and uses it in the volume formula. |
| `SoundManager+0x88 + 4*sampleId` | `m_samplePayloads[sampleId]` / `m_sampleData`; prefer `m_samplePayloads`. | Live `PlaySample` loads the DAT-backed sample payload pointer from this table after checking `sampleId <= 999`; this raw helper never touches it. |

Documentation-only struct shape:

```cpp
struct SoundSampleSlot
{
    HSAMPLE handle;
    int playVolumeScale;
};
```

The struct and member spellings are source-facing working names, not recovered debug symbols.

## No-Code Proof

Formal C++ remains blank because liveness is unproven:

- `EMITTER_UIDS` is blank and should remain blank because no live route proves this helper belongs in generated output.
- The current by-structure code-entry gate requires a confirmed nonblank emitter route. The `87/90` score passes the numeric threshold but fails the emitter-route requirement.
- The executable has no direct branch, pointer, vtable, jump-table, or known computed-call route to the helper start or interior.
- `PlaySample` already contains the live slot rotation logic. Emitting an extra private method would manufacture an uncalled source symbol and may not match the original build's intended source surface.
- The safest reconstruction is to document the body and keep formal generated C++ blank until either a caller is recovered or a higher-level source reconstruction deliberately models retained dead functions.

## Non-Formal Reference C++ Shape - Do Not Emit

This is a readable behavior reference only. Do not place it in `RECONSTRUCTION_CPP CODE` under current evidence:

```cpp
void SoundManager::RotateReusableSampleSlot()
{
    int selectedSlot = 7;

    for (int i = 0; i < 8; ++i) {
        HSAMPLE handle = m_sampleSlots[i].handle;
        if (handle != 0 && AIL_sample_status(handle) == 2) {
            selectedSlot = i;
            break;
        }
    }

    SoundSampleSlot selected = m_sampleSlots[selectedSlot];
    memmove(&m_sampleSlots[1],
            &m_sampleSlots[0],
            selectedSlot * sizeof(SoundSampleSlot));
    m_sampleSlots[0] = selected;

    if (m_sampleSlots[0].handle != 0)
        AIL_init_sample(m_sampleSlots[0].handle);
}
```

## Ownership And Rejected Alternatives

Accepted owner/source placement: [UID:0000DG][SoundManager](by-class/SoundManager.md), implemented in [UID:0000NV][SoundManager](by-file/SoundManager.md) / `NexusTK/audio/SoundManager.cpp` if a route is ever recovered. The body is `thiscall`-shaped, uses SoundManager fields, duplicates a subset of `SoundManager::PlaySample`, and consumes handles allocated by `SoundManager::ReinitializeAudioDriver`.

Rejected alternatives:

- Redbook/CD ownership or naming: rejected because the body calls sample-status/init APIs only, touches no Redbook handle/count/order/current fields, uses no Redbook imports, reads no music-source config, and schedules no Redbook timer token.
- `RestartMusic` child or tail: rejected by four bytes of padding before the target, no call/fallthrough from `RestartMusic`, and no stream/Redbook restart behavior.
- `ReinitializeAudioDriver` head or tail: rejected by three bytes of padding after the target, no call/fallthrough into driver setup, and inverse role: driver setup allocates handles while this target rotates/reinitializes existing handles.
- `PlaySample`, `ReplaySample`, or `StartSample`: rejected because the live `PlaySample` method at `0x0057a5c0-0x0057a6bf` has the payload, volume, start behavior and known callers.
- `ResetSampleSlots`: rejected because the body does not clear all slots or allocate handles; it rotates one selected slot and reinitializes slot 0.
- `AllocateSampleSlot`: rejected because allocation is performed in `ReinitializeAudioDriver` through `AIL_allocate_sample_handle`.
- `SetSampleFile` or `LoadSample`: rejected because no payload pointer or `AIL_set_sample_file` call appears.
- MemoryMan/util ownership: rejected because `GetMemoryMan`, `MemmoveWrapper`, and `Memmove0Wrapper` are incidental generic callees while the state and imports are SoundManager audio-specific.
- Compiler/runtime artifact or padding: rejected because the range has a normal method prologue/epilogue, coherent SoundManager field access, Miles sample API calls, and exact padding only outside the body.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 87 | Exact raw range, `0x9d` / 157-byte size, padding bytes, PE route scans to start and interior, import-resolved Capstone behavior, helper names, PlaySample/Reinitialize/RestartMusic relationships, field/type names, ownership, rejected alternatives, and target-specific no-code proof are documented. |
| Confidence | 90 | Very strong for behavior, SoundManager containment, range, no-entry evidence, and sample-slot layout; capped by absent live entry route and unproven original source spelling/reason for retained no-entry body. |

## Cross-References

- [UID:0001IB][0x0057b020-0x0057b48a.SoundManagerRedbookPlayback](by-memory/0x0057b020-0x0057b48a.SoundManagerRedbookPlayback.md)
- [UID:0001I9][0x0057a260-0x0057a88e.SoundManagerPlaybackControls](by-memory/0x0057a260-0x0057a88e.SoundManagerPlaybackControls.md)
- [UID:0001IC][0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver](by-memory/0x0057b490-0x0057b5f0.SoundManagerReinitializeAudioDriver.md)
- [UID:0003ZI][0x0057b370-0x0057b3ec.SoundManagerRestartMusic](by-memory/0x0057b370-0x0057b3ec.SoundManagerRestartMusic.md)
- [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md)
- [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md)
- [UID:0001BI][0x00516240-0x00516258.Memmove0Wrapper](by-memory/0x00516240-0x00516258.Memmove0Wrapper.md)
- [UID:0000DG][SoundManager](by-class/SoundManager.md)
- [UID:0000NV][SoundManager](by-file/SoundManager.md)
