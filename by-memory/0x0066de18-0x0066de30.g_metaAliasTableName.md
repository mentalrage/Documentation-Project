*** UID:0001OL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Exact storage evidence for g_metaAliasTableName is emitted by [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md).
// Keep this by-memory page as the 24-byte SimpleUString layout/lifetime/xref
// proof; do not duplicate the source declaration here.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Static `SimpleUString` metadata alias lookup key attached to MetaMan, with object layout, startup/destructor wrappers, lookup xrefs, and next-item boundary documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0066de18-0x0066de30 g_metaAliasTableName

## Status

- Entity kind: static `SimpleUString` / metadata lookup key object.
- Global doc: [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md)
- Likely source file: [UID:0000LC][MetaMan](by-file/MetaMan.md)
- Owning subsystem: metadata table lookup.
- Confidence: strong for storage, saved IDA labels, and MetaMan ownership, medium for final source symbol name and string literal meaning.
- Rebuild handling: `source-authored` static `SimpleUString` object; concrete static-lifetime wrapper code is compiler/runtime glue.
- Reconstruction handling: marked reconstructable and attached to [UID:0000LC][MetaMan](by-file/MetaMan.md), whose current `91/90` score and `NexusTK/metadata/` route clear the owner/emitter gate. Formal C++ is limited to the no-duplicate storage marker on this exact storage page; [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md) emits the first-draft source declaration with the literal/spelling caveat retained.

## Storage

`0x0066de18-0x0066de30` is the static string object used by [UID:000088][MetaMan](by-class/MetaMan.md) alias-aware lookup helpers. IDA now labels the base object `g_metaAliasTableName`; startup/destructor evidence ties the later length and capacity dwords into the same 24-byte object rather than separate globals.

| Subrange | Observed role |
| --- | --- |
| `0x0066de18-0x0066de28` | Pointer or inline small-string storage area, zeroed in the current image. |
| `0x0066de28-0x0066de2c` | Current string length field, initialized to `0`. |
| `0x0066de2c-0x0066de30` | Small-string capacity/state field, initialized to `7`. |

The next byte at `0x0066de30` is a separate data item in IDA and should not be included in this object range.

## 2026-06-02 IDA Refresh

IDA MCP confirms the current data and lifetime shape:

- `0x0066de18` is now named `g_metaAliasTableName`; bytes `0x0066de18-0x0066de28` are all zero in the image.
- `0x0066de28` is the length field (`dword_66DE28`), initialized to `0`.
- `0x0066de2c` is the capacity/state field (`dword_66DE2C`), initialized to `7`, matching an empty SSO `SimpleUString`.
- Raw bytes at `0x0066de18-0x0066de38` are `00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 07 00 00 00 97 8b 8b 8f c5 d0 d0 92`; `0x0066de30` starts the next item (`byte_66DE30`) with xrefs from `0x00527ca3` and `0x00527d40`.
- `InitializeMetaAliasTableNameStatic` constructs the object with `sub_421590(&g_metaAliasTableName, &word_60DB20, 0)` and registers `atexit(MetaAliasTableNameCleanup)`.
- `word_60DB20` begins with two UTF-16 zero words, followed by neighboring string data such as `Basic` and `Current`; for this object it behaves as the shared empty wide-string initializer, not proof of a recovered non-empty alias-table key literal.
- `MetaAliasTableNameCleanup` checks `dword_66DE2C >= 8`, frees heap-backed string storage when needed, then resets `dword_66DE28 = 0`, `dword_66DE2C = 7`, and `LOWORD(g_metaAliasTableName) = 0`.
- Xrefs to `0x0066de18` are startup construction at `0x00419ff7`, alias lookup use at `0x00523047`, `0x00523137`, and `0x00523247`, and static destructor use at `0x0060c2ab` / `0x0060c2f6`.

## Lookup Evidence

IDA decompilation of `0x00523120` (`MetaManFindEntry`) calls the lower lookup helper with `&g_metaAliasTableName` first, compares the result to the shared empty string object at `0x0066daec`, and only falls back to the caller-provided table name when no alias result applies.

IDA decompilation of `0x005231f0` (`MetaManLoadStatValues`) follows the same pattern for repeated row/value materialization: it probes aliases with `&g_metaAliasTableName`, compares against the shared empty string object, and falls back to the caller-provided table path when no alias rows are available.

## Behavior

Startup code at `0x00419ff0` constructs this object with `sub_421590(&g_metaAliasTableName, &word_60DB20, 0)` and registers the static destructor at `0x0060c2a0`. The destructor tests the large-string capacity guard, frees the heap-backed `g_metaAliasTableName` storage when needed, then resets `dword_66DE28 = 0`, `dword_66DE2C = 7`, and clears the first word of the string storage.

`MetaMan::FindEntry` and `MetaMan::LoadStatValues` pass this object into the direct metadata lookup helper before trying the caller-provided table name. The practical behavior is alias-aware lookup with fallback, even though the literal source string for this global remains uncertain.

## Evidence

- 2026-06-14 live IDA MCP session `a001_goal2_class_batch` reconfirmed the object bytes, six base-object xrefs, length/capacity field refs, constructor/destructor wrapper behavior, and MetaMan lookup uses. `int_convert.py` verifies the object size as `0x18 == 24` bytes.
- 2026-06-16 C001 live IDA MCP session `b001_selflookpane_0001H7_20260616` reconfirmed the same bytes/xrefs and saved labels `g_metaAliasTableName`, `InitializeMetaAliasTableNameStatic`, `MetaAliasTableNameCleanup`, `MetaManFindEntry`, and `MetaManLoadStatValues`. Scores are unchanged because this support page already cleared the gate; the new labels only refresh source-quality naming.
- IDA MCP xrefs to `0x0066de18` include startup construction at `0x00419ff7`, MetaMan lookup reads at `0x00523047`, `0x00523137`, and `0x00523247`, and static destructor accesses at `0x0060c2ab` and `0x0060c2f6`.
- Field-specific xrefs keep the last two dwords inside the same object: `0x0066de28` is read/reset as the string length field, and `0x0066de2c` is the capacity/state field tested against `8` and reset to `7` by `MetaAliasTableNameCleanup`.
- Decompilation at `0x00523120` shows `MetaManFindEntry` calling the direct lookup helper with `&g_metaAliasTableName` before falling back to the caller's table name.
- Decompilation at `0x005231f0` shows `LoadStatValues` using the same object for alias lookup.
- Current generated evidence routes through `source-3/simroot_v2/metadata/MetaMan.cpp`; older flat `class_MetaMan.cpp` output also emitted `extern SimpleUString g_metaAliasTableName;` and used it in the generated MetaMan lookup path.
- The initializer source `word_60DB20` is the shared empty wide string at this address. Treat the generated `g_metaAliasTableName` name as a useful role name, not confirmed original spelling.

## Reconstruction Notes

Keep this storage with `metadata/MetaMan.cpp` alongside `g_pMetaMan`, `g_emptySimpleUString`, and MetaTable helpers. It is a metadata lookup key, not state owned by ItemHelpPane, CollectionPane, or any other UI consumer that happens to request metadata.

Do not hand-port the static initializer/destructor wrapper bodies. Recreate the source-level static `SimpleUString` object in MetaMan-owned source and let the compiler/runtime emit equivalent lifetime glue.

The current by-global first draft is `SimpleUString g_metaAliasTableName;`. This storage page now emits only the formal no-duplicate storage marker because it documents the binary address, object bytes, constructor/destructor xrefs, and exact `0x18` / 24-byte layout without duplicating the source declaration owned by [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md).

## Score Rationale

- Completion is `87` because the page now documents the exact `0x18`/24-byte object layout, construction/destruction, base and field xrefs, alias lookup behavior, successor boundary, owner route, formal no-duplicate storage disposition, and by-global declaration split.
- Confidence is `90` because live IDA evidence directly proves the bytes, lifetime wrapper, field semantics, MetaMan lookup role, and non-duplicate declaration route; it stays below near-final because the original key spelling and source symbol are still unresolved.

## Changes

- 2026-07-02 Agent-B007 UID0000LC implementation callback:
  - Changed to: `COMPLETION:87`, `CONFIDENCE:90`.
  - Summary/evidence: inserted the formal exact-storage no-duplicate marker, preserving the 24-byte `SimpleUString` layout, startup/destructor lifetime, lookup xrefs, empty/default initializer caveat, and split that [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md) emits the source-level declaration.
- 2026-06-19 Agent-B009 source-quality sync:
  - Score unchanged at `COMPLETION:85`, `CONFIDENCE:88`.
  - Summary/evidence: replaced the stale blank-C++ rationale with the current split: [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md) emits the source-level `SimpleUString` declaration, while this by-memory page remains non-emitting address/layout/lifetime evidence and preserves the unresolved literal/source-spelling caveat.
- 2026-06-14: Raised from `82/86` to `85/88` after live IDA MCP session `a001_goal2_class_batch` reconfirmed the `0x18`/24-byte `SimpleUString` layout, constructor/destructor wrappers, length/capacity field refs, MetaMan alias lookup uses, and boundary before the NCA URL bytes. Owner/emitter remain [UID:0000LC][MetaMan](by-file/MetaMan.md); this storage page remains non-emitting.
- 2026-06-16 C001 global-storage/IDA refresh: Score unchanged at `85/88`; refreshed current IDA names after saved labels for the base object, static wrapper/cleanup, and primary MetaMan lookup functions. Final source symbol spelling and alias key meaning remain provisional.
- 2026-06-02 low-score pass:
  - Changed to: `COMPLETION:82`, `CONFIDENCE:86`, `RECONSTRUCTABLE:TRUE`, and parent [UID:0000LC][MetaMan](by-file/MetaMan.md).
  - Summary/evidence: current IDA MCP verifies the exact object layout, initial bytes, constructor/destructor wrappers, alias lookup xrefs, and boundary at `0x0066de30`; the storage page stayed non-emitting because final source spelling and helper names were still provisional.

## Cross-References

- [UID:0000Q2][g_metaAliasTableName](by-global/g_metaAliasTableName.md)
- [UID:0000LC][MetaMan](by-file/MetaMan.md)
- [UID:000088][MetaMan](by-class/MetaMan.md)
- [UID:0001C8][0x005227d0-0x00524581.MetaMan](by-memory/0x005227d0-0x00524581.MetaMan.md)
- [UID:0001RH][meta-dat-metadata-cache](by-resource/meta-dat-metadata-cache.md)
- [UID:00024G][0x0060c26a-0x0060c320.StaticDestructorWrappersBeforePoolAllocators](by-memory/0x0060c26a-0x0060c320.StaticDestructorWrappersBeforePoolAllocators.md)
