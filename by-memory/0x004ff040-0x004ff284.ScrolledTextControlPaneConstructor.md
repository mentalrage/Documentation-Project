*** UID:0004WP | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CI | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ScrolledTextControlPane::ScrolledTextControlPane(
    const wchar_t *resourceName,
    const RectBounds *bounds,
    signed char scrollInterval)
    : ControlPane(8, bounds)
{
    MemoryMan *memoryMan = GetMemoryMan();
    DATFile dataFile;
    TextEditScrap scrap;

    dataFile.Open(resourceName);
    int dataSize = dataFile.GetSize();
    char *data = static_cast<char *>(
        memoryMan->AllocateBufferMemory(1001));
    dataFile.Read(data, dataSize);
    data[dataSize] = '\0';
    dataFile.Close();
    scrap.SetFromClipboardBlock(data, dataSize);
    memoryMan->FreeBufferMemory(data);

    int width = bounds->right - bounds->left;
    int height = bounds->bottom - bounds->top;
    TextEditPane *editor = new TextEditPane(
        width, height, width, height,
        143, 0, 0, 0, 1, IsLegacyAssetMode(), true, false);

    editor->PasteFromClipboard(&scrap);

    RectBounds textBounds;
    InitRectBounds(&textBounds,
                   0,
                   0,
                   width,
                   12 * editor->GetLineCount());
    m_scrollLimit = height + textBounds.bottom - textBounds.top;
    m_scrollOffset = 0;
    m_scrollInterval = scrollInterval;

    editor->UpdateRenderRegion(&textBounds);
    editor->SetViewportBounds(&textBounds);
    editor->SetTextAreaBounds(&textBounds);
    editor->SetEditActiveState(false, false);
    editor->SetMode(1);
    editor->UpdateSurfaceInfo();
    editor->OnDraw();
    editor->GetSurfaceContext()->CopyTo(&m_renderedText);
    m_renderedText.BuildEncodedMask();
    editor->EndPaint();
    delete editor;

    m_timerHandler.ScheduleTimer(0, 1, 0, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact generic scrolled-text constructor: automatic EPFTileContext-then-GrafPort member construction, ControlPane type 8, caller-selected DAT text, fixed 1001-byte temporary storage, TextEditScrap import, temporary 0x174-byte TextEditPane render/copy/mask pipeline, exact 0x1dc layout state, and initial timer schedule. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ff040-0x004ff284 ScrolledTextControlPane Constructor

## Status

- Address range: exact modeled function `[0x004ff040,0x004ff284)`, `0x244` / 580 bytes (Verified with `tools/int_convert.py`).
- Disposition: source-authored constructor for [UID:0000CI][ScrolledTextControlPane](by-class/ScrolledTextControlPane.md).
- Source route: [UID:0000NI][ScrolledTextControlPane](by-file/ScrolledTextControlPane.md), proposed as `NexusTK/ui/controls/ScrolledTextControlPane.cpp`.
- Parent index: [UID:00019W][0x004ff040-0x004ff3fd.ScrolledTextControlPaneCore](by-memory/0x004ff040-0x004ff3fd.ScrolledTextControlPaneCore.md).

## Range, Bytes, And Control Flow

- Live read-only IDA MCP evidence on saved-IDB checkpoint SHA256 `F28C8B463F64693561E118790FD5E9D68739F85C697B9B58F391F56E1911ED99` identified 211 instructions, 15 basic blocks, a unique full signature, and exact function SHA256 `DC6860A7FEED774083F9D534E5B8FB39A2E10C0502E7460F6DBC093599A404E5`.
- The function returns with `retn 0x0c`, matching three explicit 32-bit stack slots: `const wchar_t *resourceName`, `const RectBounds *bounds`, and the promoted signed-byte interval.
- Predecessor byte `[0x004ff03f,0x004ff040)` is one `0xcc`, SHA256 `1DD8312636F6A0BF3D21FA2855E63072507453E93A5CED4301B364E91C9D87D6`.
- Following `[0x004ff284,0x004ff290)` is twelve `0xcc` alignment bytes, SHA256 `A0D08A155AA4E9AADD5DA959565DE99ACC8DC9DBDA96EC3852DDE76C97330902`; it is indexed by UID00019W and is not constructor source.

## Direct Callers And Resources

| Call site | Owning path | Resource and arguments |
| --- | --- | --- |
| `0x004ffc52` | `StaffsDialogPane` constructor | one Staffs branch using bounds `(38,62,230,305)`, interval `50`, and UTF-16 `STAFFS2` / `STAFFS` selection |
| `0x004ffc79` | `StaffsDialogPane` constructor | alternate modifier-state Staffs branch using the same object size, bounds, and interval |
| `0x00500207` | `BackStoryDialogPane` constructor | UTF-16 `BACKTALE`, interval `50`, EPF-mode bounds `(25,56,361,278)` |
| `0x0050033e` | `BackStoryDialogPane` constructor | UTF-16 `BACKTALE`, interval `50`, non-EPF bounds `(36,71,228,265)` |

- Every caller allocates exactly `0x1dc` / 476 bytes (Verified with `tools/int_convert.py`). No caller supports a hard-coded constructor resource name.
- `STAFFS2` is stored at `0x0061e498`, `STAFFS` at `0x0061e4a8`, and `BACKTALE` at `0x0061e4b8`.

## Object Layout And Automatic Construction Order

- Inherited `ControlPane` occupies `+0x000..+0x107`.
- `EPFTileContext m_renderedText` occupies `+0x108..+0x12f` and has exact size `0x28`.
- `GrafPort m_renderPort` occupies `+0x130..+0x1cf` and has compiler-proven size `0xa0`.
- `int m_scrollOffset`, `int m_scrollLimit`, and `signed char m_scrollInterval` begin at `+0x1d0`, `+0x1d4`, and `+0x1d8`; three implicit alignment bytes complete size `0x1dc`.
- The binary calls the routine historically named `EPFTileContext_Initialize` for `this+0x108` at `0x004ff0ae`, then the proven `GrafPort` constructor for `this+0x130` at `0x004ff0b9`, before first body-local work at `0x004ff0c2`. C++ cannot execute a constructor body before a later nontrivial member constructor, so the first call is automatic default construction of `m_renderedText`, not an explicit body call.
- Declaration order must therefore remain `m_renderedText` followed by `m_renderPort`. The managed source deliberately has no `m_renderedText.Initialize()` call; adding one would duplicate initialization and change binary order.

## Exact Constructor Behavior

1. Construct `ControlPane` as control type `8`, then automatically construct `m_renderedText` and `m_renderPort` in declaration order.
2. Obtain `MemoryMan`; construct local `DATFile` and `TextEditScrap`; open the caller-selected resource.
3. Read the reported DAT size into an exact 1001-byte allocation, append `data[dataSize] = '\0'`, close the file, import exactly `dataSize` bytes into the scrap, then free the byte buffer.
4. Compute width and height from the caller's `RectBounds`.
5. Allocate a temporary `TextEditPane` of exact binary size `0x174` / 372 bytes (Verified). Pass width/height twice, background `143`, zero scroll colors/flags, style `1`, `IsLegacyAssetMode()`, and the generic-only final booleans `true, false`.
6. Paste the scrap, create a text rectangle `(0,0,width,12*lineCount)`, set `m_scrollLimit` to pane height plus rendered text height, set offset to zero, and preserve the signed one-byte timer interval.
7. Apply the same rectangle to render region, viewport, and text area; disable edit/selection; set mode `1`; update surface state; draw; copy the editor context into `m_renderedText`; build its encoded mask; end paint; delete the editor.
8. Schedule timer id `0` with initial delay `1` and zero trailing arguments.

The final constructor booleans distinguish this generic control from the made-by sibling, whose currently emitted constructor uses `false, false`. Do not collapse the two bodies merely because the broader pipelines are parallel.

## Lifetime, Failure, And Negative Evidence

- `DATFile` and `TextEditScrap` are ordinary local objects. The temporary buffer is freed after import, and the editor is deleted only after surface copy, mask construction, and `EndPaint`.
- The complete CFG has no file-open, size, read, allocation, editor-allocation, copy, or mask failure branch and no null guard.
- There is no `dataSize <= 1000` check despite the fixed 1001-byte allocation and explicit terminator write. The source preserves this original unsafe behavior.
- Compiler exception-unwind funclets around `0x00601d04..0x00601d55`, vptr stores, cookies, and local cleanup lowering are not handwritten source.
- No persistent DATFile, TextEditScrap, byte buffer, or TextEditPane member exists.

## Source Naming And Ownership

- Best source-facing member names are `m_renderedText`, `m_renderPort`, `m_scrollOffset`, `m_scrollLimit`, and `m_scrollInterval`, supported by lifecycle, offsets, and the accepted made-by sibling layout.
- `PasteFromClipboard`, `GetLineCount`, `UpdateRenderRegion`, `SetViewportBounds`, `SetTextAreaBounds`, `SetEditActiveState`, `UpdateSurfaceInfo`, and `GetSurfaceContext` are dependency-owned helper names selected from established project conventions and exact behavior.
- TextEditPane, GrafPort, DAT archive, Staffs, and BackStory are dependencies or callers; none owns this complete receiver, vtables, or source definition.

## Score Rationale

Completion `92` and confidence `94` reflect exact range/hash/CFG, callers/resources, ABI, object size/layout, construction order, full DAT/TextEdit/render sequence, constants, field writes, lifetime, negative evidence, class/file ownership, and destination-ready C++. The score remains below final-audit quality because stripped original private/helper spellings and project-wide bit-identical rebuild proof remain unavailable.

## Cross-References

- [UID:00019W][0x004ff040-0x004ff3fd.ScrolledTextControlPaneCore](by-memory/0x004ff040-0x004ff3fd.ScrolledTextControlPaneCore.md)
- [UID:0000CI][ScrolledTextControlPane](by-class/ScrolledTextControlPane.md)
- [UID:0000NI][ScrolledTextControlPane](by-file/ScrolledTextControlPane.md)
- [UID:0000EO][TextEditPane](by-class/TextEditPane.md)
- [UID:00019X][0x004ff400-0x004ff7cd.ScrolledTextControlPaneForMadeByCore](by-memory/0x004ff400-0x004ff7cd.ScrolledTextControlPaneForMadeByCore.md)

