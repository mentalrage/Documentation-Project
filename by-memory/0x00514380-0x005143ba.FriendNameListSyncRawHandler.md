*** UID:0001AZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000L3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000L3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// No standalone body: this retained raw duplicate is covered by the live MapPane::HandlePacket opcode 0x6a route in [UID:00037T][0x00507c90-0x00508f60.MapPanePacketDispatcher](by-memory/0x00507c90-0x00508f60.MapPanePacketDispatcher.md).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Duplicates the live `MapPane::HandlePacket` opcode `0x6a` friend/name-list sync body; live IDA confirms exact raw bytes, flag/send-helper xrefs, modeled caller contrast, no raw-start xrefs/callers, no pointer-table hits, and alignment before `sub_5143C0`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00514380-0x005143ba - Friend Name-List Sync Raw Handler

## Status

- Confidence: strong for raw function-shaped body, corrected half-open range, exact behavior, MapPane opcode relationship, global/send-helper xrefs, and current unreferenced status; medium for why the duplicate was retained.
- Entity kind: retained raw duplicate of a `MapPane::HandlePacket` opcode body.
- Rebuild handling: source-authored behavior already represented by the live `MapPane` opcode `0x6a` branch; keep this as retained raw MapPane packet-handler evidence and do not reconstruct it as an independent required helper unless later xref/table evidence proves reachability. The formal C++ block carries a no-code comment because no VA/RVA/raw-offset pointer, vtable/callback-table cell, rel32 call/jump, conditional branch, direct caller, or IDA xref currently reaches `0x00514380`.
- Related global: [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md).
- Related opcode docs: [UID:0000UP][FriendNameListSyncOpcodes](by-item/FriendNameListSyncOpcodes.md).

## Behavior

This 58-byte raw body duplicates the same friend/name-list sync handling seen in [UID:0000L3][MapPane](by-file/MapPane.md) case `0x6a`. It is not the currently proven live route.

1. Initialize a local cursor to `1`.
2. Read one byte from the packet buffer with `0x00575710`.
3. Store [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md) to `readByte != 0`.
4. If the byte is nonzero, call [UID:0001HX][0x00574e50-0x0057536b.BuildAndSendFriendNameListSync](by-memory/0x00574e50-0x0057536b.BuildAndSendFriendNameListSync.md) through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md).
5. Return success.

## Raw Assembly Shape

```asm
00514380  push    ebp
00514381  mov     ebp, esp
00514383  push    ecx
00514384  lea     eax, [ebp-4]
00514387  mov     dword ptr [ebp-4], 1
0051438e  push    eax
0051438f  push    dword ptr [ebp+8]
00514392  call    sub_575710
00514397  add     esp, 8
0051439a  test    al, al
0051439c  setnz   byte_66DEE0
005143a3  test    al, al
005143a5  jz      short loc_5143B2
005143a7  mov     ecx, dword_67A7EC
005143ad  call    sub_574E50
005143b2  mov     al, 1
005143b4  mov     esp, ebp
005143b6  pop     ebp
005143b7  retn    4
```

## Raw Boundary, Bytes, Padding, And PE Mapping

B004 rechecked the local PE image and corrected the target boundary. Image base is `0x00400000`; `.text` has RVA `0x00001000`, virtual size `0x0020b4ac`, raw pointer `0x00000400`, and raw size `0x0020b600`. Target VA `0x00514380` maps to RVA `0x00114380` and raw file offset `0x00113780` by `raw = 0x00000400 + ((0x00514380 - 0x00400000) - 0x00001000)`.

| Range | Meaning | Length | SHA-256 |
| --- | --- | ---: | --- |
| `0x00514380-0x005143b7` | stale documented range; incomplete body ending before `ret 4` | `0x37` / 55 bytes | `cec9731c0b2b846bbfd1730fdf10cf1ca3e7dafb6ad135e16717a82595416a31` |
| `0x00514380-0x005143ba` | corrected complete body | `0x3a` / 58 bytes | `85d29ffc817b1143fdca4f3d14358bcfccd18e9248a0f2f04bd5596fb32b0e3f` |
| `0x005143ba-0x005143c0` | post-body alignment padding | `0x6` / 6 bytes | `33cc4a00a0f04735602819132da3e3fac4cdeb8ec7aee92551dc79ba8488e722` |
| `0x005143b7-0x005143c0` | old omitted `ret 4` plus padding | `0x9` / 9 bytes | `555d98cbeb8abf478da22fcb27418cb26a23babc09b0925c55a2ebe826439460` |

Corrected body plus padding bytes:

```text
00514380: 55 8b ec 51 8d 45 fc c7 45 fc 01 00 00 00 50 ff
00514390: 75 08 e8 79 13 06 00 83 c4 08 84 c0 0f 95 05 e0
005143a0: de 66 00 84 c0 74 0b 8b 0d ec a7 67 00 e8 9e 0a
005143b0: 06 00 b0 01 8b e5 5d c2 04 00 cc cc cc cc cc cc
```

Boundary conclusion: the complete raw body is `0x00514380-0x005143ba`; `0x005143b7-0x005143ba` is the real `ret 4`, not padding; `0x005143ba-0x005143c0` is six `0xcc` bytes; successor `sub_5143C0` starts at `0x005143c0`.

## Live Dispatcher Comparison

The live `MapPane::HandlePacket` case starts at `0x00508ae3`. The narrower `0x00508af3-0x00508b19` comparison span is `0x26` bytes, and the wider semantic case fragment `0x00508ae3-0x00508b1d` is also `0x3a` / 58 bytes.

```asm
00508ae3  lea     eax, [ebp-450h]
00508ae9  mov     dword ptr [ebp-450h], 1
00508af3  push    eax
00508af4  push    edi
00508af5  call    sub_575710
00508afa  add     esp, 8
00508afd  test    al, al
00508aff  setnz   byte_66DEE0
00508b06  test    al, al
00508b08  jz      loc_507DCA
00508b0e  mov     ecx, dword_67A7EC
00508b14  call    sub_574E50
00508b19  mov     al, 1
```

Both bodies initialize a cursor to `1`, call `0x00575710` to read one byte from the packet buffer, store `byte_66DEE0 = (readByte != 0)`, conditionally call `0x00574e50` through `dword_67A7EC`, and return success. The live branch uses the dispatcher packet pointer in `edi` and a large dispatcher local; this raw body uses a standalone `__stdcall`-shaped shell with `[ebp+8]` and `ret 4`. This proves semantic duplication, not raw-body reachability.

## Hidden Route And Xref Recheck

Fresh local PE route scans for `0x00514380` found zero hits for absolute VA dword `80 43 51 00`, RVA dword `80 43 11 00`, raw file-offset dword `80 37 11 00`, `.text` `E8`/`E9` rel32 call or jump targets, and `.text` `0F 80-8F` rel32 conditional branch targets. That rules out the conventional direct pointer-table, callback-table, vtable-cell, jump-table-cell, direct call, direct jump, and direct conditional-branch routes currently available from the executable image.

Positive controls from the same scan keep the negative result meaningful: absolute dword `0x0066dee0` appears exactly three times in loaded `.text` at the live write `0x00508b02`, this raw write `0x0051439f`, and `FriendListDialog` read `0x0053f8de`; absolute dword `0x0067a7ec` appears 489 times, matching the high-use `g_packetSender` global; absolute dword `0x00574e50` has no file hits because its callers encode rel32 calls.

Prewave/export metadata models a Ghidra `FUN_00514380` of size `58` with Ghidra `num_callers = 0`, while IDA fields are null. That corroborates body shape and size but does not prove that the raw start is source-routed.

## Heuristic / Inference Reanalysis

- Range and padding: the stale `0x00514380-0x005143b7` range omitted the three-byte `ret 4`; the corrected body is `0x00514380-0x005143ba`.
- Raw helper role: the bytes decode as a complete compact `__stdcall`-shaped body, but no current caller/table route supports creating a source helper named `FriendNameListSyncRawHandler`.
- Duplicate relationship: the accepted interpretation is retained raw duplicate evidence for the live `MapPane::HandlePacket` opcode `0x6a` branch. Merging it into the dispatcher page would lose the orphan/raw-range and padding-boundary evidence.
- Global name: `byte_66DEE0` is best documented as `g_friendNameListSyncEnabled`. It is set by inbound opcode `0x6a`, then read by `FriendListDialog::OnOkButton` before sending after the 20 configured names are saved.
- Rejected global aliases: `g_isCashShopEnabled` and cash-shop request names are contradicted by the live opcode path, `FriendListDialog` read, outbound opcode `0x77` builder, and separate `0x0067a738` downloader/request storage.
- Send helper: `0x00574e50` remains best named `BuildAndSendFriendNameListSync`; it is called through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), builds outbound opcode `0x77`, iterates 20 friend-name slots, emits 13-byte records with marker `0x0c`, and queues through the Socket send path.
- Packet byte semantics: opcode `0x6a` carries a single control flag at payload offset `1`. Nonzero stores true and immediately sends the current name-list sync reply; zero stores false and suppresses the immediate send.
- Source placement: keep [UID:0000L3][MapPane](by-file/MapPane.md) ownership and emitter routing for retained MapPane packet-handler evidence. Do not move this raw item to `FriendListDialog`, Socket, a protocol item, or `CANONICAL_OWNER:NONE`.

## First-Draft C++ Recommendation

Do not populate this target's formal `RECONSTRUCTION_CPP` block. The source behavior should instead be represented in the live dispatcher branch. A reference-only source shape for that branch is:

```cpp
case 0x6a:
{
    int cursor = 1;
    const bool enabled = PacketBufferReadByteAtCursor(packet, &cursor) != 0;
    g_friendNameListSyncEnabled = enabled;
    if (enabled)
    {
        BuildAndSendFriendNameListSync(g_packetSender);
    }
    return true;
}
```

Name status: `0x6a` is the verified inbound opcode value; `PacketBufferReadByteAtCursor` is descriptive/inferred for `0x00575710`; `g_friendNameListSyncEnabled` is descriptive/inferred for `byte_66DEE0`; `BuildAndSendFriendNameListSync` is descriptive/inferred for `0x00574e50`; `g_packetSender` is the resolved source-facing name for `dword_67A7EC`.

## Source-Shape Note

The raw body is a compact duplicate of the live `MapPane::HandlePacket` opcode `0x6a` branch: read one payload byte, update `g_friendNameListSyncEnabled`, optionally call the outbound opcode `0x77` builder, and return success. The reconstructed C++ block carries a no-code comment because the proven source route is the modeled MapPane dispatcher branch, and current evidence still finds no direct caller, pointer-table hit, vtable/callback-table route, or rel32 control-flow route for raw start `0x00514380`.

## Evidence Notes

- 2026-06-05 IDA MCP `lookup_funcs` reports `0x00514380` and `0x005143b7` as `Not a function`; the next modeled functions are `sub_5143C0` at `0x005143c0-0x0051447c` and `sub_514480` at `0x00514480-0x00514579`.
- 2026-06-05 IDA MCP disassembly shows a complete prologue/body/`retn 4` from `0x00514380` through `0x005143b9`, followed by six `0xcc` alignment bytes at `0x005143ba-0x005143c0`.
- 2026-06-05 IDA MCP `xrefs_to 0x00514380` and `callers 0x00514380` report no direct references.
- 2026-06-05 IDA raw-pointer scan across loaded segments finds no dword equal to `0x00514380`, so no obvious untyped callback table points at this raw start in the current IDB.
- 2026-06-05 IDA MCP `xrefs_to 0x0066dee0` reports the live `MapPane::HandlePacket` write at `0x00508aff`, this raw-body write at `0x0051439c`, and the `FriendListDialog` read at `0x0053f8dc`.
- 2026-06-05 IDA MCP `xrefs_to 0x00574e50` reports the live `MapPane::HandlePacket` call at `0x00508b14`, this raw-body call at `0x005143ad`, and the `FriendListDialog::OnOkButton` call at `0x0053f8ed`. `callers 0x00574e50` only lists the modeled `MapPane` and `FriendListDialog` functions because the raw body is not an IDA function.
- 2026-06-05 live `MapPane` disassembly around `0x00508af3-0x00508b19` matches the raw body's behavior: read one byte, `setnz byte_66DEE0`, conditionally call `sub_574E50`, return success.
- 2026-06-18 B004 PE recheck confirms the body is 58 bytes, not 55 bytes: `0x005143b7-0x005143ba` is `c2 04 00` (`ret 4`), and only `0x005143ba-0x005143c0` is post-body `0xcc` padding.
- 2026-06-18 B004 hidden-route scan found no VA/RVA/raw-offset dword, no rel32 call/jump, and no rel32 conditional branch to `0x00514380`; positive controls still found the expected `byte_66DEE0` and `g_packetSender` references.

## Disposition

Document this as a reviewed retained raw duplicate packet-handler body. Do not create a new source module from it unless later xref/table evidence proves reachability. Its behavior corroborates naming for [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md), but the source-reconstruction path should use the live `MapPane::HandlePacket` opcode `0x6a` route.

## Autogen Status

- Reconstructable: true, as retained MapPane packet-handler project code with exact raw-body evidence.
- Parent: [UID:0000L3][MapPane](by-file/MapPane.md).
- C++: formal no-code comment; final source shape belongs to the live MapPane opcode branch unless reachability for this raw entry is later proven.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 86 | The page now records exact raw bytes, corrected body endpoint, omitted `ret 4`, padding bytes, PE mapping, modeled function absence, next-function boundaries, live `MapPane` opcode equivalence, flag/write and send-helper xrefs, modeled caller contrast, no direct xrefs, no callers, no pointer-table hits, no rel32 route, parent assignment, inferred source names, rejected alternatives, and reconstruction limits. Completion remains capped because the raw duplicate's retention reason is unresolved. |
| Confidence | 90 | Raw PE bytes and existing IDA-backed docs support the retained-duplicate disposition, exact behavior, corrected range, and no-route policy. Confidence remains below final range because `0x00514380` is not a modeled function and no original source symbol, callback table, or linker-retention explanation is proven. |

## Cross-References

- [UID:0000UP][FriendNameListSyncOpcodes](by-item/FriendNameListSyncOpcodes.md)
- [UID:0000PG][g_friendNameListSyncEnabled](by-global/g_friendNameListSyncEnabled.md)
- [UID:0001OM][0x0066dee0-0x0066dee1.FriendNameListSyncEnabled](by-memory/0x0066dee0-0x0066dee1.FriendNameListSyncEnabled.md)
- [UID:0001HX][0x00574e50-0x0057536b.BuildAndSendFriendNameListSync](by-memory/0x00574e50-0x0057536b.BuildAndSendFriendNameListSync.md)
- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)

## Changes

- 2026-06-29 Agent-B004 MapPane empty-emitter callback:
  - Inserted the accepted formal no-code comment for the retained raw opcode `0x6a` duplicate.
  - Evidence: existing B004/B003 no-route proof and the current report's dispatcher evidence keep the live source route in [UID:00037T][0x00507c90-0x00508f60.MapPanePacketDispatcher](by-memory/0x00507c90-0x00508f60.MapPanePacketDispatcher.md), not this raw start.
- 2026-06-18 B004 source-quality:
  - Corrected the target range/title/path from `0x00514380-0x005143b7` to `0x00514380-0x005143ba`; the old range omitted `ret 4` bytes `c2 04 00` at `0x005143b7-0x005143ba`.
  - Changed to: completion `86`, confidence `90`, keeping `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:0000L3`, `EMITTER_UIDS:0000L3`, and blank formal C++.
  - Summary/evidence: local PE mapping places the body at raw offset `0x00113780`; corrected body length is `0x3a` / 58 bytes, followed by six `0xcc` bytes before `sub_5143C0`; the raw body duplicates live `MapPane::HandlePacket` opcode `0x6a`; VA/RVA/raw-offset and rel32 route scans find no path to raw start `0x00514380`; positive controls confirm expected `byte_66DEE0` and `g_packetSender` references. Source behavior remains with the live dispatcher branch.
- 2026-06-05:
  - Changed to: completion `84`, confidence `88`, keeping `RECONSTRUCTABLE:TRUE`, `AUTOGEN_PARENT_UID:0000L3`, and blank C++.
  - Summary/evidence: live IDA MCP revalidated that `0x00514380` is not a modeled function, has no direct xrefs/callers, has no loaded-segment dword pointer hits, and is followed by six `0xcc` alignment bytes before `sub_5143C0`; it also confirmed the raw write to `byte_66DEE0`, raw call to `sub_574E50`, equivalent live `MapPane` opcode `0x6a` branch, and modeled `MapPane`/`FriendListDialog` callers of the send helper. Final C++ remains blank because the raw entry is not proven reachable.
- 2026-05-28:
  - Before: documented as a raw packet-handler-shaped body with weak reachability confidence.
  - After: documented as a retained raw duplicate of the live `MapPane::HandlePacket` opcode `0x6a` body; completion/confidence raised to medium because behavior, boundary, and current unreferenced status are now independently rechecked.
  - Why: IDA MCP still reports no function object, direct xrefs, callers, or loaded-segment pointer hits for `0x00514380`, while the live `MapPane` case and `FriendListDialog` caller remain the proven paths to the same friend/name-list upload helper.
- 2026-06-02:
  - What existed before: the page was `65/65`, unattached, and contained a behavior-sketch C++ block.
  - Changed to: completion `72`, confidence `80`, `RECONSTRUCTABLE:TRUE`, `AUTOGEN_PARENT_UID:0000L3`, and a prose source-shape note instead of C++.
  - Summary/evidence: existing IDA-backed body, xref, no-pointer-scan, live MapPane opcode, and FriendListDialog evidence support retained MapPane ownership while keeping final C++ blank.
