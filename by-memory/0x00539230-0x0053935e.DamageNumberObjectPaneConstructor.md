*** UID:0004SK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003F | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
DamageNumberObjectPane::DamageNumberObjectPane(
    LivingObjectPane *livingObjectPane,
    int mapY,
    int mapX,
    int damageAmount,
    unsigned char style)
    : AttachedObjectPane(
          kObjectPaneTypeDamageNumber,
          livingObjectPane,
          mapY,
          mapX),
      m_style(style),
      m_animationFrame(0),
      m_absoluteAmount(damageAmount),
      m_positive(damageAmount >= 0),
      m_digitFrameOffset(0)
{
    SetMode(3);
    m_alpha = 0;

    if (!m_positive)
        m_absoluteAmount = -damageAmount;

    sprintf_s(
        m_damageText,
        sizeof(m_damageText),
        "%d",
        m_absoluteAmount);

    if (m_style != 0)
    {
        if (!m_positive)
            m_digitFrameOffset += 10;
    }
    else
    {
        m_digitFrameOffset += 20;
        if (m_positive)
            m_digitFrameOffset += 10;
    }

    g_pTimerMgr->ScheduleTimer(
        static_cast<TimerHandler *>(this),
        0,
        20,
        0,
        0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Complete source-ready DamageNumberObjectPane constructor for the exact 0x148 AttachedObjectPane-derived overlay; accepts living owner, map row, map column, signed amount, and byte style, initializes object type 8/mode 3/alpha zero, formats absolute decimal text, selects sign/style digit frames, schedules timer event 0 after 20 ms, preserves four MapPane callers, and excludes pool allocation, vptr, EH cleanup, deleting-destructor, and vtable lowering from handwritten source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00539230-0x0053935e DamageNumberObjectPane Constructor

## Status

- Disposition: reconstructable source-authored constructor.
- Exact source signature: `DamageNumberObjectPane::DamageNumberObjectPane(LivingObjectPane *, int mapY, int mapX, int damageAmount, unsigned char style)`.
- Direct owner/emitter: [UID:00003F][DamageNumberObjectPane](by-class/DamageNumberObjectPane.md); file route [UID:0000HJ][AttachedObjectPane](by-file/AttachedObjectPane.md).
- Source placement: `NexusTK/map/AttachedObjectPane.cpp` with the AttachedObjectPane, HitBarObjectPane, ObjectInfoObjectPane, and DamageNumberObjectPane family.
- The exact child owns the ordinary constructor only. Pool allocation, constructor-call cleanup, vptr stores, scalar deleting wrappers, vtable data, resources, rendering, timer animation, and position updates remain separate children/compiler support.

## Address Range And Binary Identity

- Exact range: `[0x00539230,0x0053935e)`, 302 bytes.
- Executable SHA256: `6F536DA47AA93B32B46DE152F35CA785780F1C8CA3A37BDCC976939B4DA02420`.
- Direct callers: `0x0050ef7a`, `0x0050efa0`, `0x00512a9c`, and `0x00512ac2`; all are MapPane overlay-creation paths.
- Predecessor padding: `[0x0053922b,0x00539230)`, five `0xCC` bytes, SHA256 `992CF51486F901AA1AA7D1F63E18C150C9156166E08E0B9E59CC3FAF98177329`.
- Successor padding: `[0x0053935e,0x00539360)`, two `0xCC` bytes, SHA256 `E3966E3275BE536A16092EC0CADF1638F718218E616FBBE8FF1C5E67FFF4DEF2`.
- No neighboring body, helper, table, or padding byte belongs to the constructor range.

## ABI And Constructor Contract

- `ECX` carries the destination object. Five explicit source arguments are supplied by each caller in owner/map-row/map-column/signed-amount/style order.
- The constructor delegates common owner/type/map placement to AttachedObjectPane with object type 8, then establishes DamageNumber-specific state.
- The full object extent is exactly `0x148`; accepted class fields occupy the derived tail without inventing an external state object.
- `damageAmount` remains signed. The constructor records its sign, stores an absolute magnitude for display, and formats the magnitude as narrow decimal text in a 16-byte buffer.
- `style` remains an unsigned byte. It selects the digit-frame offset family independently of amount sign.

## Exact Behavior And Order

- Base construction occurs before derived field initialization.
- Mode is set to 3 and inherited alpha is cleared to zero.
- Negative values are converted to absolute magnitude after sign capture. The source intentionally preserves ordinary signed negation behavior; no saturation or `INT_MIN` hardening is added.
- Decimal text uses format `%d` and the exact fixed 16-byte destination represented by `m_damageText`.
- Nonzero style adds 10 only for negative values. Zero style adds 20, then adds another 10 only for positive values.
- Timer event 0 is scheduled on this TimerHandler after 20 ms with the two trailing arguments zero.
- No bounds update, ObjectList insertion, owner-slot installation, or invalidation occurs in this constructor; MapPane performs those operations after successful construction.

## Caller And Lifetime Evidence

- Two calls in MapPaneCreateHitBarObjectPaneEventOverlay and two calls in UID0003TQ select style from the target LivingObjectPane status kind.
- MapPane allocates the exact `0x148` object through compiler-lowered ordinary `new`, calls this constructor, installs it on the LivingObjectPane, registers it in ObjectList, sets its 500 ms owner timer, updates position, gets bounds, and invalidates.
- The constructor's own 20 ms timer drives DamageNumber animation; it is distinct from MapPane's later 500 ms lifetime timer.
- Existing DamageNumber timer/render/update/destructor anchors and vtable/pool/resource pages corroborate the class identity and source route.

## Ownership And Source Placement

- Receiver vtable, exact size, fields, timer/render/update virtual family, resource use, and four MapPane construction calls make DamageNumberObjectPane the direct semantic owner.
- MapPane owns call-site policy and post-construction registration, not the constructor body.
- AttachedObjectPane owns the base state and common virtual contract. This derived constructor belongs in the same `AttachedObjectPane.cpp` translation unit without merging sibling bodies.

## Compiler-Lowering Exclusions

- Explicit pool allocator calls, allocation-size constants, null-allocation branches, vptr stores, construction-state cookies, EH cleanup, and scalar deleting-destructor mechanics are compiler/allocator lowering and are not handwritten in this formal.
- The accepted source uses ordinary construction and a normal initializer list. It does not expose raw offsets, `sub_` names, decompiler temporaries, or calling-convention annotations.
- This range does not own the class declaration, destructor, OnTimer, ClearOutputRects, RenderFrame, UpdatePosition, vtable, pool, or resources.

## Negative Evidence And Rejected Alternatives

- The fifth argument is a byte style selector, not a boolean success flag, owner nation, render alpha, or object type.
- Map row precedes map column at this call boundary; reversing them contradicts all four call sites and base placement behavior.
- The displayed amount is signed input rendered by magnitude/sign policy, not an unsigned packet field or floating-point value.
- A factory/free function, MapPane-owned helper, compiler wrapper, raw aggregate body, or no-code disposition is rejected by the receiver construction pattern and retained virtual class family.
- No defensive null owner, buffer-length branch, amount clamp, style clamp, or allocation exception policy is inserted.

## Reconstruction Confidence

- Range, callers, ABI, object extent, base route, field behavior, format, style/sign table, timer parameters, and source class are direct or very-strong evidence.
- Original private field lexemes and exact source formatting are stripped; the descriptive names are behavior-complete and account for the `91/93` confidence cap.
- Historical blank-class/constructor wording is superseded by this exact target-specific pass; unrelated render/timer details remain child-owned rather than inflated here.
