*** UID:0003LQ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B003 current Goal 2 repaired the former no-owner row; live IDA MCP session `b001_0003gy` reconfirmed the five aligned helper functions, boundary padding, raw pointer refs, vtable refs from TextEditControlPane/StaticTextControlPane/ExchangeMoneyEditControlPane, and MSVC RTTI class-hierarchy arrays showing StaticTextControlPane and ExchangeMoneyEditControlPane derive through [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md), so `CANONICAL_OWNER:0000EM` and `EMITTER_UIDS:0000EM`; final C++ remains blank pending source-quality method/field names. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00498dd0-0x00499021 Text-Control Shared State And Border Helpers

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: inherited `TextEditControlPane` virtual helper band for older text-control wrappers.
- Rebuild handling: `source-authored`; final C++ remains blank until source-quality virtual method names, field names, and layout signatures clear the active reconstruction-code gate.
- Parent assignment: [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md). Current B003 Goal 2 IDA MCP evidence confirms [UID:0000E3][StaticTextControlPane](by-class/StaticTextControlPane.md) and [UID:00004T][ExchangeMoneyEditControlPane](by-class/ExchangeMoneyEditControlPane.md) have RTTI class-hierarchy descriptors that list `TextEditControlPane` as the next base after the derived class descriptor, so their vtable cells are inherited base virtual slots rather than independent source ownership claims.
- Emitter assignment: [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md). The class routes through [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md).
- Barrier decision: this page is the exact successor after non-emitting [UID:00011H][0x004988d0-0x00498dd0.TextEditControlPaneCore](by-memory/0x004988d0-0x00498dd0.TextEditControlPaneCore.md) and its last source child [UID:0004KB][0x00498d70-0x00498dc8.TextEditControlPaneEventForwarders](by-memory/0x00498d70-0x00498dc8.TextEditControlPaneEventForwarders.md). The event child ends at `0x00498dc8`; eight parent-only `0xcc` bytes occupy `0x00498dc8-0x00498dd0`; this helper band begins exactly at `0x00498dd0`. [UID:00011I][0x00499030-0x004991ec.StaticTextControlPaneConstructor](by-memory/0x00499030-0x004991ec.StaticTextControlPaneConstructor.md) remains the later successor after post-band alignment.
- IDA MCP evidence passes: 2026-06-12 on session `a002_batch346`; 2026-06-14 B003 Goal 2 recheck on session `b001_0003gy`.

## Covered Range

End-exclusive range: `0x00498dd0-0x00499021`.

| Range | IDA function | Role | Vtable/data refs |
| --- | --- | --- | --- |
| `0x00498dd0-0x00498e0e` | `sub_498DD0`, size `0x3e` | Activates/restores wrapper/editor display state, sets byte `+0x101`, and invalidates the owner bounds through the owner vtable. | `0x0061814c`, `0x006181f0`, `0x0061a2b0` |
| `0x00498e10-0x00498e5e` | `sub_498E10`, size `0x4e` | Captures embedded text-pane state byte `+0xb5`, maps state `2`/`3`, clears byte `+0x101`, and invalidates the owner bounds. | `0x00618150`, `0x006181f4`, `0x0061a2b4` |
| `0x00498e60-0x00498eca` | `sub_498E60`, size `0x6a` | Applies inactive embedded text-pane state, refreshes child bounds, clears byte `+0x102`, and invalidates if the owner was active. | `0x00618158`, `0x006181fc`, `0x0061a2bc` |
| `0x00498ed0-0x00498f4c` | `sub_498ED0`, size `0x7c` | Applies active/selectable embedded text-pane state, selects range `0-0x7fff`, refreshes child bounds, sets byte `+0x102`, and invalidates if needed. | `0x0061815c`, `0x00618200` |
| `0x00498f50-0x00499021` | `sub_498F50`, size `0xd1` | Draws the optional older text-control frame/highlight when byte `+0x109` is set. | `0x00618144`, `0x006181e8`, `0x0061a2a8` |

## Ownership Evidence

The helper bodies are reached through multiple primary vtable families, but current RTTI evidence explains those refs as inheritance from `TextEditControlPane`:

- `0x00618100`, `0x00618168`, and `0x00618198` are named `TextEditControlPane` vtables.
- `0x006181a4`, `0x0061820c`, and `0x0061823c` are named `StaticTextControlPane` vtables.
- `0x0061a264`, `0x0061a2cc`, and `0x0061a2fc` are named `ExchangeMoneyEditControlPane` vtables; the helper refs at `0x0061a2a8`, `0x0061a2b0`, `0x0061a2b4`, and `0x0061a2bc` sit in this vtable cluster.

Current B003 Goal 2 IDA MCP `get_global_value` over the MSVC RTTI class-hierarchy descriptors gives the decisive owner signal:

| Class hierarchy | Descriptor / base array | IDA fact | Meaning |
| --- | --- | --- | --- |
| `TextEditControlPane` | `??_R3TextEditControlPane@@8` at `0x00645d58`, base array `0x00645d68` | base count `7`; first base-class descriptor `0x00645d88` is `??_R1A@?0A@EA@TextEditControlPane@@8` | `TextEditControlPane` owns the base implementation set. |
| `StaticTextControlPane` | `??_R3StaticTextControlPane@@8` at `0x00645de0`, base array `0x00645df0` | base count `8`; first descriptors are `0x00645e14` (`StaticTextControlPane`) then `0x00645d88` (`TextEditControlPane`) | `StaticTextControlPane` derives through `TextEditControlPane` and inherits these slots. |
| `ExchangeMoneyEditControlPane` | `??_R3ExchangeMoneyEditControlPane@?A0x0ff658a5@@8` at `0x006476c0`, base array `0x006476d0` | base count `8`; first descriptors are `0x006476f4` (`ExchangeMoneyEditControlPane`) then `0x00645d88` (`TextEditControlPane`) | `ExchangeMoneyEditControlPane` derives through `TextEditControlPane` and inherits the shared text-control slots it does not override. |

The target functions occupy the same primary-vtable slot offsets in TextEdit and StaticText. ExchangeMoney inherits four of those slots and overrides the active/selectable state slot with its local helper at `0x004b0b40`, explaining why `0x00498ed0` has no ExchangeMoney vtable cell. That asymmetry supports a base-class ownership model rather than a whole-page multi-emitter route.

Decision: this page is not owned by the adjacent TextEdit core range as an aggregate, and it is not owned by the StaticText constructor. It is a separate exact `by-memory` page whose direct semantic owner and emitter are the `TextEditControlPane` class.

## Historical No-Owner Audit And Supersession

B001's 2026-06-13 ownership/split audit is preserved as historical evidence because it correctly captured the physical sharing facts before the later RTTI base-array proof changed the routing decision. That audit recommended `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, and blank reconstruction C++ because ordinary xrefs only reached the five bodies through concrete vtable data, not through normal calls, and because consumer vtable refs alone are not output-routing proof.

The no-owner conclusion is now superseded by the 2026-06-14 B003 RTTI pass. The valid B001 details that still constrain reconstruction are:

- Physical range and split: keep the exact `0x00498dd0-0x00499021` band together. Splitting the five helpers into separate pages would not solve source ownership because each helper still has the same inherited text-control slot ambiguity; the predecessor `0x00498dc8-0x00498dd0` and successor `0x00499021-0x00499030` ranges are alignment padding.
- Positive use evidence: `sub_498DD0`, `sub_498E10`, `sub_498E60`, and `sub_498F50` are referenced by TextEdit, StaticText, and ExchangeMoney vtable cells; `sub_498ED0` is referenced by TextEdit and StaticText cells only.
- Rejected owners from the B001 pass: `StaticTextControlPane` is a concrete consumer adjacent after the padding, not the source home for the whole band; `ExchangeMoneyEditControlPane` consumes only four helpers and owns separate exact wrapper methods at `0x004b0b40` and `0x004b0b60`; `ControlPane` is too broad because the helpers depend on text-wrapper bytes and the embedded editor pointer; `TextEditPane` is the owned child/editor implementation, not the wrapper receiver.
- Rejected synthetic routes: do not create a `TextControlPane`, `TextControlBase`, `TextControlSharedHelpers`, or private helper-file owner from design plausibility alone. B001 found no `TextControlPane`/`TextControlBase` RTTI, no distinct shared vtable/constructor family, and no `.cpp`, `.h`, PDB, `RSDS`, or `NB10` breadcrumb proving such a source entity.
- Rejected multi-emitter route: do not emit the whole physical band into every concrete consumer. The old multi-vtable evidence proves consumption, not separate source definitions or linker folding, and ExchangeMoney lacks the `0x00498ed0` slot.

The only part invalidated is the final B001 routing recommendation. With the later RTTI class-hierarchy arrays, StaticText and ExchangeMoney list `TextEditControlPane` after the derived descriptor, so their cells are inherited base virtual slots. The current owner/emitter route through [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md) should remain unless future source evidence proves a different real base/helper declaration.

## B003 Current Goal2 And Post-Migration Report Accounting

B003 produced two archived `0003LQ` reports with different final recommendations. The current Goal 2 pass is accepted as the authoritative route because it includes the decisive RTTI class-hierarchy evidence and matches the current document/autogen state: `COMPLETION:88`, `CONFIDENCE:92`, `CANONICAL_OWNER:0000EM`, `RECONSTRUCTABLE:TRUE`, and `EMITTER_UIDS:0000EM`. The post-migration recheck's final `CANONICAL_OWNER:NONE`, blank-emitter, `85/89`, no-edit recommendation is retained only as a historical assumption: its physical/xref evidence is valid, but its routing conclusion is superseded by the RTTI-backed B003 Goal 2 pass and the current support docs.

Accepted B003 current Goal 2 details:

- Assignment context: the row started as a reconstructable no-owner memory target in generated memory coverage. The repair changed it from `85/89`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS` to `88/92`, `CANONICAL_OWNER:0000EM`, `EMITTER_UIDS:0000EM`.
- Tooling: live IDA MCP session `b001_0003gy` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`; evidence came from `lookup_funcs`, `xrefs_to`, `get_bytes`, `find_bytes`, `entity_query`, `callees`, `analyze_component`, `decompile`, `get_global_value`, and `search_structs`.
- Boundary proof: the five modeled functions are exactly `sub_498DD0` (`0x3e`), `sub_498E10` (`0x4e`), `sub_498E60` (`0x6a`), `sub_498ED0` (`0x7c`), and `sub_498F50` (`0xd1`), separated by `0xcc` padding and bounded by pre-target padding at `0x00498dc8-0x00498dd0` and post-target padding at `0x00499021-0x00499030`.
- Vtable/data proof: raw pointer and `xrefs_to` checks identify the same vtable cells already recorded in this page. TextEdit and StaticText reference all five helpers; ExchangeMoney references `0x00498f50`, `0x00498dd0`, `0x00498e10`, and `0x00498e60` but not `0x00498ed0`.
- Decisive ownership proof: `get_global_value` over MSVC RTTI shows `StaticTextControlPane` and `ExchangeMoneyEditControlPane` class-hierarchy descriptors list their derived class descriptor first and then the `TextEditControlPane` base descriptor. The multi-vtable references are inherited base-slot use, not independent ownership or linker-folded duplicate source definitions.
- Rejected routes: `CANONICAL_OWNER:NONE` is now weaker than the RTTI evidence; `StaticTextControlPane` is a derived consumer whose constructor starts after the padding at `0x00499030`; `ExchangeMoneyEditControlPane` consumes only four helpers and overrides the missing slot with `0x004b0b40`; `ControlPane` and `TextEditPane` are dependencies rather than the wrapper receiver; and a proposed `TextControlPane`/`TextControlSharedHelpers.cpp` source root remains unsupported by IDA names, RTTI, structs, constructors, vtables, source strings, or project-tree evidence.
- C++ gate: no direct reconstruction C++ is emitted here. Final source still needs source-quality virtual method names, exact signatures, field names for wrapper bytes `+0x101`, `+0x102`, `+0x109`, `+0x10c`, `+0x110`, and callee names. Draft/final C++ should be emitted through the `TextEditControlPane` source route once those names are source-ready.

Valid post-migration recheck details kept as negative evidence:

- It correctly warns that consumer vtable refs alone are not emitter proof and that a whole-page multi-emitter route would overclaim the evidence, especially because ExchangeMoney has no `0x00498ed0` vtable cell.
- It reconfirms there are no ordinary code callers to the five starts, no internal calls among the helper bodies, no strings inside the band, and no IDA `TextControl`/`TextControlPane` struct or name evidence for a separate shared owner.
- Its endpoint notes remain useful process evidence: the active MCP endpoint required an explicit `database=b001_0003gy` argument, and once supplied the same function, xref, byte, pointer, vtable, callee, and decompiler facts were reproducible.
- It correctly keeps the page as one exact `0x00498dd0-0x00499021` helper band instead of splitting into five child pages. A later split is only useful if future source evidence proves function-specific emitter routes.
- Its proposed child names are only descriptive fallbacks if a future split becomes necessary: `TextControlActivateStateSyncHelper`, `TextControlDeactivateStateSyncHelper`, `TextControlInactiveChildStateHelper`, `TextControlActiveSelectableChildStateHelper`, and `TextControlBorderFramePaintHelper`.

Current support-doc alignment after this accounting: [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md) and [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md) route this helper band as inherited `TextEditControlPane` source; [UID:0000E3][StaticTextControlPane](by-class/StaticTextControlPane.md) and [UID:0000O8][StaticTextControlPane](by-file/StaticTextControlPane.md) describe StaticText as a derived consumer rather than owner; [UID:00004T][ExchangeMoneyEditControlPane](by-class/ExchangeMoneyEditControlPane.md) and [UID:0003BK][0x0061a260-0x0061a304.ExchangeMoneyEditControlPaneVtableData](by-memory/0x0061a260-0x0061a304.ExchangeMoneyEditControlPaneVtableData.md) preserve the four-slot consumption and local override boundary. No support-doc edit is needed for this B003 pair.

## Boundary Evidence

- Exact predecessor UID0004KB contains three `0x18` EventHandler forwarding bodies at `0x00498d70-0x00498d88`, `0x00498d90-0x00498da8`, and `0x00498db0-0x00498dc8`, with two internal eight-byte `0xcc` gaps. It ends exactly at `0x00498dc8` and does not overlap this band.
- `make_signature_for_range 0x00498dc8-0x00498dd0` reports eight `0xcc` bytes before this shared helper band.
- `lookup_funcs(0x00498dd0)`, `0x00498e10`, `0x00498e60`, `0x00498ed0`, and `0x00498f50` report exact modeled functions ending at `0x00498e0e`, `0x00498e5e`, `0x00498eca`, `0x00498f4c`, and `0x00499021`.
- Internal padding is confirmed as `0x00498e0e-0x00498e10` (`CC CC`), `0x00498e5e-0x00498e60` (`CC CC`), `0x00498eca-0x00498ed0` (`CC CC CC CC CC CC`), and `0x00498f4c-0x00498f50` (`CC CC CC CC`).
- `make_signature_for_range 0x00499021-0x00499030` reports fifteen `0xcc` bytes after the border helper.
- `lookup_funcs(0x00499030)` reports `sub_499030` size `0x1bc`, so the StaticText constructor starts at `0x00499030` and ends at `0x004991ec`.

## Second-Pass Raw Pointer Audit

B001's second-pass raw PE scan remains relevant as negative evidence even though its no-owner routing recommendation is superseded. It corroborates that all known inbound references are absolute vtable pointer cells and that no hidden code/RVA route named a private helper owner:

| Function start | Raw absolute-pointer count | Mapped hit VAs | Current interpretation |
| --- | ---: | --- | --- |
| `0x00498dd0` | `3` | `0x0061814c`, `0x006181f0`, `0x0061a2b0` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498e10` | `3` | `0x00618150`, `0x006181f4`, `0x0061a2b4` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498e60` | `3` | `0x00618158`, `0x006181fc`, `0x0061a2bc` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498ed0` | `2` | `0x0061815c`, `0x00618200` | TextEdit and StaticText cells only; ExchangeMoney overrides this slot with its local helper. |
| `0x00498f50` | `3` | `0x00618144`, `0x006181e8`, `0x0061a2a8` | TextEdit, StaticText, and ExchangeMoney vtable cells. |

The same pass found zero RVA-literal hits for `0x00098dd0`, `0x00098e10`, `0x00098e60`, `0x00098ed0`, and `0x00098f50`, no ordinary call xrefs to the five function starts, no internal call graph among the five functions, and no string literals inside the band. Those findings reject a standalone call-only helper owner and explain why the historical B001 pass could not safely emit or multi-emit the page before the later RTTI base-array proof identified `TextEditControlPane` as the inherited source route.

## B003 Post-Migration No-Owner Recheck And Supersession

The archived B003 post-migration recheck report `0003LQ-TextControlSharedStateAndBorderHelpers-post-migration-recheck` is preserved here as a superseded proof pass. Its final recommendation at report time was to keep `COMPLETION:85`, `CONFIDENCE:89`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++; that recommendation was valid before the later B003 Goal 2 pass decoded the MSVC RTTI class-hierarchy arrays. The current `88/92` TextEditControlPane owner/emitter route supersedes only the routing conclusion, not the report's physical-boundary, vtable, behavior, split, and negative-evidence findings.

The B003 post-migration pass used live IDA MCP session `b001_0003gy` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`. Its process notes are retained because they explain the evidence provenance: `idb_list` and `tools/list` were live, the worker-backed endpoint required the explicit `database=b001_0003gy` argument, and an initial empty `initialize` / no-database tool call failed before reruns with the database argument succeeded. Tools used included `lookup_funcs`, `xrefs_to`, `get_bytes`, `find_bytes`, `callees`, `entity_query`, `decompile`, and `analyze_component`; documentation leads included this target, generated memory coverage, manual memory coverage, TextEditControlPane, StaticTextControlPane, ExchangeMoneyEditControlPane, ExchangeDialog, and the proposed source tree.

The post-migration function and boundary facts match the current page and remain binding:

| Range | IDA function / size | Post-migration behavior summary |
| --- | --- | --- |
| `0x00498dd0-0x00498e0e` | `sub_498DD0`, `0x3e` | Uses byte `+0x101`, calls `sub_5446B0`, restores/show state, and invalidates the owner rectangle through the vtable. |
| `0x00498e10-0x00498e5e` | `sub_498E10`, `0x4e` | Saves embedded child state from `this[67]+0xb5` into `+0x110`, maps state `2/3`, clears `+0x101`, and invalidates. |
| `0x00498e60-0x00498eca` | `sub_498E60`, `0x6a` | Applies inactive embedded text-pane state, refreshes child bounds, clears `+0x102`, and invalidates if previously active. |
| `0x00498ed0-0x00498f4c` | `sub_498ED0`, `0x7c` | Applies active/selectable embedded text-pane state, selects range `0..0x7fff`, refreshes child bounds, sets `+0x102`, and invalidates when needed. |
| `0x00498f50-0x00499021` | `sub_498F50`, `0xd1` | Draws the optional older border/highlight when byte `+0x109` is set. |

Neighbor and padding checks from the report are also retained: `0x00498dc8` and `0x00499021` are not functions; `0x00498d70` is `sub_498D70` size `0x18`; `0x00499030` is `sub_499030` size `0x1bc`; ExchangeMoney-local helper starts `0x004b0b40` size `0x14` and `0x004b0b60` size `0x36` remain separate. Confirmed padding spans are `0x00498dc8-0x00498dd0` (eight `0xcc` bytes), `0x00498e0e-0x00498e10` (two), `0x00498e5e-0x00498e60` (two), `0x00498eca-0x00498ed0` (six), `0x00498f4c-0x00498f50` (four), and `0x00499021-0x00499030` (fifteen).

The report's vtable and raw-pointer evidence remains valid. `xrefs_to` and `find_bytes` found the same absolute cells for the five function starts:

| Function start | Pointer cells | Post-migration meaning |
| --- | --- | --- |
| `0x00498dd0` | `0x0061814c`, `0x006181f0`, `0x0061a2b0` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498e10` | `0x00618150`, `0x006181f4`, `0x0061a2b4` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498e60` | `0x00618158`, `0x006181fc`, `0x0061a2bc` | TextEdit, StaticText, and ExchangeMoney vtable cells. |
| `0x00498ed0` | `0x0061815c`, `0x00618200` | TextEdit and StaticText vtable cells only; ExchangeMoney uses its local override. |
| `0x00498f50` | `0x00618144`, `0x006181e8`, `0x0061a2a8` | TextEdit, StaticText, and ExchangeMoney vtable cells. |

IDA names checked by B003 were `TextEditControlPane` vtables at `0x00618100`, `0x00618168`, and `0x00618198`; `StaticTextControlPane` vtables at `0x006181a4`, `0x0061820c`, and `0x0061823c`; and `ExchangeMoneyEditControlPane` vtables at `0x0061a264`, `0x0061a2cc`, and `0x0061a2fc`. The report also tied those vtable bases to constructor/destructor store contexts: TextEdit construction/destruction near `sub_498920`, `sub_498B90`, and `sub_49B6C0`; StaticText constructor stores in `sub_499030`; and ExchangeMoney construction/destruction stores in `sub_4AC8A0` and `sub_4B0A60`.

The post-migration behavior/callee evidence remains useful for source reconstruction. The first two helpers call `sub_5446B0`; `0x00498e60` calls `sub_58EA80`, `sub_4B8E00`, and the security-cookie check; `0x00498ed0` calls `sub_58EA80`, `sub_58F2A0`, `sub_4B8E00`, and the security-cookie check; `0x00498f50` calls drawing helpers `sub_4B7E30`, `sub_4B9660`, `sub_4BA450`, `sub_4B9600`, and `sub_4B98F0` plus the security-cookie check. `analyze_component` found no internal calls between the five target functions and no string literals, with `___security_cookie` as the only shared global. Decompilation confirmed wrapper-state and embedded-text-pane use through bytes/pointers at `+0x101`, `+0x102`, `+0x109`, `+0x10c`, and `+0x110`, which rejects a generic `ControlPane` or embedded `TextEditPane` direct-owner route.

The report's rejected routing matrix is retained as historical negative evidence:

| Candidate route in the post-migration report | Report disposition | Current disposition after RTTI proof |
| --- | --- | --- |
| Keep `CANONICAL_OWNER:NONE`, blank emitters | Accepted then, because vtable consumers alone did not prove source-output routing. | Superseded by RTTI base arrays proving TextEditControlPane inheritance. |
| `TextEditControlPane` | Best forced route but rejected then because StaticText and ExchangeMoney vtable consumers could still represent a hidden shared base/helper. | Accepted now because StaticText and ExchangeMoney class-hierarchy descriptors derive through TextEditControlPane. |
| `StaticTextControlPane` | Rejected: constructor begins after confirmed post-target padding and TextEdit/ExchangeMoney also consume the band. | Still rejected as direct owner; it inherits these slots. |
| `ExchangeMoneyEditControlPane` / `ExchangeDialog` | Rejected: only four of five helper starts are consumed and `0x00498ed0` is a local ExchangeMoney override elsewhere. | Still rejected as direct owner; it inherits four slots and overrides one. |
| Multiple emitters across concrete consumers | Rejected: consumer vtable cells are not duplicated source definitions, and whole-page ExchangeMoney emission would overclaim. | Still rejected; the base route emits once through TextEditControlPane. |
| New `TextControlPane` / `TextControlBase` / helper source root | Rejected: no IDA names, RTTI, constructors, vtables, source strings, PDB breadcrumbs, proposed-source-tree entry, or broader candidate contents. | Still rejected unless future source evidence proves a real shared text-control owner. |
| `ControlPane` or `TextEditPane` | Rejected: the receiver is the wrapper/control object, with text-wrapper fields and embedded pane dependencies. | Still rejected. |

The report's split guidance remains historical but useful. It recommended no split because five smaller pages would not have solved ownership and because ExchangeMoney consumes four helpers but not `0x00498ed0`. If future source evidence proves function-specific routes, the proposed child names were `TextControlActivateStateSyncHelper`, `TextControlDeactivateStateSyncHelper`, `TextControlInactiveChildStateHelper`, `TextControlActiveSelectableChildStateHelper`, and `TextControlBorderFramePaintHelper` for the five exact ranges already listed above, with internal `0xcc` spans left in the ignored ledger. The current page stays unsplit because the later RTTI evidence supports one inherited TextEditControlPane helper band.

The post-migration coverage recommendation is superseded. The report said no manual coverage change was needed while the row still described `85%`, no owner, and blank emitters. After the later Goal 2 RTTI repair, the correct manual row is the current `88%` `TextEditControlPane` owner/emitter row; target C++ still remains blank because source-quality method names, field names, signatures, and final layout types are not recovered.

## Touched State

The helper band assumes the same wrapper layout used by the older text-control wrappers:

| Offset | Evidence-backed role |
| --- | --- |
| `this+0x44` | Inherited control rectangle invalidated/drawn through owner vtable callbacks. |
| `this+0x101` | Active/show state bit toggled by `0x00498dd0` and `0x00498e10`. |
| `this+0x102` | Embedded text-pane selectable/active state bit toggled by `0x00498e60` and `0x00498ed0`. |
| `this+0x109` | Optional border/frame paint gate used by `0x00498f50`. |
| `this+0x10c` | Embedded text pane pointer consumed by all helper bodies. |
| `this+0x110` | Cached embedded pane state byte saved/restored by activate/deactivate helpers. |

## Cross-References

- [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md)
- [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md)
- [UID:0000E3][StaticTextControlPane](by-class/StaticTextControlPane.md)
- [UID:0000O8][StaticTextControlPane](by-file/StaticTextControlPane.md)
- [UID:00004T][ExchangeMoneyEditControlPane](by-class/ExchangeMoneyEditControlPane.md)
- [UID:0003BK][0x0061a260-0x0061a304.ExchangeMoneyEditControlPaneVtableData](by-memory/0x0061a260-0x0061a304.ExchangeMoneyEditControlPaneVtableData.md)
- [UID:00011H][0x004988d0-0x00498dd0.TextEditControlPaneCore](by-memory/0x004988d0-0x00498dd0.TextEditControlPaneCore.md)
- [UID:0004KB][0x00498d70-0x00498dc8.TextEditControlPaneEventForwarders](by-memory/0x00498d70-0x00498dc8.TextEditControlPaneEventForwarders.md)
- [UID:00011I][0x00499030-0x004991ec.StaticTextControlPaneConstructor](by-memory/0x00499030-0x004991ec.StaticTextControlPaneConstructor.md)

## Score Rationale

| Metric | Value | Rationale |
| --- | --- | --- |
| Completion | `88` | The page records exact function boundaries, vtable refs, boundary padding, touched state, RTTI class-hierarchy evidence, and the corrected TextEdit owner/emitter route. Completion remains below final-source level because source-quality virtual method names, field names, signatures, and final C++ are still open. |
| Confidence | `92` | IDA MCP boundary/xref evidence is strong, and current RTTI base-array evidence explains the multi-vtable refs as inherited `TextEditControlPane` slots. Confidence stays below near-final because final source names and layout fields are not yet source-ready. |

## Changes

- 2026-07-13 B003 UID00011H implementation support sync:
  - Score `88/92`, owner/emitter, exact range, reconstructable state, blank formal C++, and `Nested:-4` remain unchanged.
  - Added exact predecessor UID0004KB and preserved the two internal event gaps plus parent-only trailing `0x00498dc8-0x00498dd0` eight-byte pad. UID00011H remains the non-emitting index; this page still begins at `0x00498dd0` and keeps its established inherited TextEditControlPane route and historical no-owner/RTTI evidence.

- 2026-06-12 C001 Goal 2 split: created this exact shared helper page while repairing the TextEdit/StaticText barrier crossing.
  - What existed before: [UID:00011H][0x004988d0-0x00498dd0.TextEditControlPaneCore](by-memory/0x004988d0-0x00498dd0.TextEditControlPaneCore.md) claimed `0x004988d0-0x00499020`, while [UID:00011I][0x00499030-0x004991ec.StaticTextControlPaneConstructor](by-memory/0x00499030-0x004991ec.StaticTextControlPaneConstructor.md) claimed `0x00498dd0-0x004991ec`, so both pages overlapped over the same vtable-only state/paint helper band.
  - Changed to: this standalone `0x00498dd0-0x00499021` reconstructable shared helper page with `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS` by design.
  - Evidence: C001 live IDA MCP `lookup_funcs`, `xrefs_to`, `entity_query(kind=names)`, and `make_signature_for_range` verified the five helper functions, their shared vtable refs, the `TextEditControlPane` and `StaticTextControlPane` vtable labels, the exchange-money reuse, and the padding before/after the band.
- 2026-06-13 B001 second-pass owner/emitter review: kept `85/89`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank reconstruction C++. IDA MCP and raw PE pointer scans confirmed the same physical functions are vtable-reached from TextEditControlPane, StaticTextControlPane, and ExchangeMoneyEditControlPane contexts, but did not prove a single source owner, a common helper/base file, or a whole-page multi-emitter route. In particular `0x00498ed0` lacks an ExchangeMoney vtable reference, so routing this entire page through all consumers would overclaim the evidence.
- 2026-06-14 B003 Goal 2 current no-owner memory pass: raised `85/89 -> 88/92`, set `CANONICAL_OWNER:0000EM`, and set `EMITTER_UIDS:0000EM`. Current IDA MCP on session `b001_0003gy` reconfirmed the five function boundaries, padding, raw pointer refs, and vtable names, then decoded the MSVC RTTI class-hierarchy arrays: `StaticTextControlPane` and `ExchangeMoneyEditControlPane` both list `TextEditControlPane` as the next base after the derived class descriptor. That makes the target an inherited `TextEditControlPane` virtual helper band, not an ownerless shared helper or a multi-emitter item. Reconstruction C++ remains blank because final method/field names and signatures are not source-ready.
- 2026-06-22 Rule 26 incorporation for archived B001 report `0003LQ-TextControlSharedStateAndBorderHelpers.md`: preserved the full no-owner audit as historical analysis, retained the useful negative evidence and split/multi-emitter cautions, and explicitly marked its routing recommendation superseded by the later RTTI proof.
- 2026-06-22 Rule 26 incorporation for archived B001 report `0003LQ-TextControlSharedStateAndBorderHelpers-second-pass.md`: added the second-pass raw pointer-count table, zero-RVA-hit result, no-call-xref/no-internal-callgraph negatives, and no-string evidence. These details were accepted as supporting evidence, while the report's `CANONICAL_OWNER:NONE`, blank-emitter, `85/89`, and no-code recommendation remains a historical recommendation superseded by the later RTTI-backed `TextEditControlPane` route.
- 2026-06-22 Rule 26 incorporation for archived B003 reports `0003LQ-TextControlSharedStateAndBorderHelpers-current-goal2-pass.md` and `0003LQ-TextControlSharedStateAndBorderHelpers-post-migration-recheck.md`: accepted the current Goal 2 RTTI-backed `TextEditControlPane` owner/emitter route, preserved the post-migration no-owner recommendation as a superseded historical assumption, and retained the valid negative evidence around consumer-only xrefs, no standalone `TextControl` owner proof, no multi-emitter route, no split need, and blank direct C++ pending source-quality names.
- 2026-06-22 Rule 26 incorporation for archived B003 post-migration report `0003LQ-TextControlSharedStateAndBorderHelpers-post-migration-recheck`: added a superseded no-owner recheck section preserving its live `b001_0003gy` evidence, MCP/database provenance, function sizes, boundary padding, vtable and raw pointer-cell tables, behavior/callee facts, rejected route matrix, proposed child-split names, negative evidence, and stale no-coverage-change recommendation. The report's `85/89`, `CANONICAL_OWNER:NONE`, blank-emitter conclusion remains historical only and is superseded by the RTTI-backed `88/92` TextEditControlPane owner/emitter route.
