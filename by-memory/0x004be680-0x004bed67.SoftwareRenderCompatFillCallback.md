*** UID:0002YF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OC | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __fastcall SoftwareRenderCompatFillCallback(
    GrafPort *port,
    int /*unused*/,
    const RectBounds *bounds)
{
    enum FillMode {
        kFillCopy = 0,
        kFillColorKey = 1,
        kFillBlend = 2,
        kFillColorKeyBlend = 3
    };

    if (!port->m_drawEnabled) {
        return;
    }

    RectBounds clipBounds;
    port->GetClipRect(&clipBounds);

    RectBounds clippedBounds = *bounds;
    IntersectRects(&clipBounds, &clippedBounds, &clippedBounds);
    if (IsRectEmptyOrInvalid(&clippedBounds)) {
        return;
    }

    const int width = clippedBounds.right - clippedBounds.left;
    const int height = clippedBounds.bottom - clippedBounds.top;
    const int rowStridePixels = port->m_surfaceContext.rowStridePixels;
    unsigned short *rowPixels =
        static_cast<unsigned short *>(port->m_surfaceContext.pixelData) +
        clippedBounds.left +
        rowStridePixels *
            (clippedBounds.top - port->m_surfaceContext.bounds.top) -
        port->m_surfaceContext.bounds.left;

    DLPalette *palette = g_pPaletteLib->GetCurrentPalette();
    unsigned short fillPixel = palette->MapColor(port->m_drawColor);
    const unsigned short secondaryPixel =
        palette->MapColor(port->m_textBackFillColor);
    (void)secondaryPixel;

    if (port->m_usesSoftwareBuffer && port->m_drawColor == 128) {
        fillPixel = 1;
    }

    switch (port->m_drawMode) {
    case kFillCopy:
        for (int y = 0; y < height; ++y) {
            FillRgb555Span(rowPixels, width, fillPixel);
            rowPixels += rowStridePixels;
        }
        break;

    case kFillColorKey:
        if (port->m_drawColor == 0) {
            return;
        }

        for (int y = 0; y < height; ++y) {
            FillRgb555Span(rowPixels, width, fillPixel);
            rowPixels += rowStridePixels;
        }
        break;

    case kFillBlend: {
        const unsigned char destinationWeight =
            static_cast<unsigned char>(
                static_cast<int>(port->m_alpha * 32.0f + 0.5f));

        for (int y = 0; y < height; ++y) {
            for (int x = 0; x < width; ++x) {
                rowPixels[x] = BlendRgb555(
                    rowPixels[x], fillPixel, destinationWeight);
            }
            rowPixels += rowStridePixels;
        }
        break;
    }

    case kFillColorKeyBlend: {
        if (port->m_drawColor == 0) {
            return;
        }

        const unsigned char destinationWeight =
            static_cast<unsigned char>(
                static_cast<int>(port->m_alpha * 32.0f + 0.5f));

        for (int y = 0; y < height; ++y) {
            for (int x = 0; x < width; ++x) {
                rowPixels[x] = BlendRgb555(
                    rowPixels[x], fillPixel, destinationWeight);
            }
            rowPixels += rowStridePixels;
        }
        break;
    }

    default:
        return;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready Surface slot-7 compat/RGB555 rectangle-fill callback with exact void GrafPort/const RectBounds ABI, draw guard, clipping and destination-pitch math, global-current-palette mapping including the preserved secondary call, software-buffer color-128 to pixel-1 override, four copy/color-key/weighted-blend modes, exact RGB555 BlendRgb555 direction, scalar/compiler-vectorized lowering, two setup refs, 206 shared-slot refs, exact local switch-table boundaries, and complete formal Surface.cpp C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004be680-0x004bed67 SoftwareRenderCompatFillCallback

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: software-render callback target.
- Callback slot: `dword_69B3FC` in [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md).
- Containing family: [UID:00016J][0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks](by-memory/0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks.md).
- Confirmed source owner and sole emitter route: [UID:0000OC][Surface](by-file/Surface.md) / shared software-render dispatch; not Ranking UI, GrafPort-class, or SoftwareBlend16-owned callback code.

## Range

| Range | IDA function | Notes |
| --- | --- | --- |
| `0x004be680-0x004bed67` | `sub_4BE680`, size `0x6e7` / 1767 bytes (Verified with `int_convert.py`) | Compat/RGB555 clipped fill or invalidation-style callback. |

The preceding `0x004be66e-0x004be680` interval is local switch data/alignment for the bitmask callback, and the following `0x004bed67-0x004bed80` interval is this callback's local switch data/alignment before the RGB555 channel-expansion helper.

## Behavior Evidence

- 2026-06-06 IDA MCP lookup, caller, callee, xref, and decompile evidence was recorded in the parent aggregate.
- No ordinary direct callers were reported; xrefs are callback-table initializer references from the raw setup neighborhood plus [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md).
- The callback checks a GrafPort active flag at `+0x71`, obtains and intersects rectangles through `0x004b96c0`, `0x004b7cc0`, and `0x004b7eb0`, computes a 16-bit destination pointer from pitch/origin fields at `+0x20/+0x28/+0x2c/+0x30`, resolves palette colors through `0x00543d40` and `0x00542ac0`, then dispatches on the mode byte at `+0x70`.
- Observed mode paths include direct fill, conditional direct fill gated by color/index `+0x74`, blended fill using alpha/intensity float `+0x90`, and the same blended path gated by `+0x74`.
- The blended paths consume [UID:0002V6][0x0061a920-0x0061aac0.SoftwareRenderMaskConstants](by-memory/0x0061a920-0x0061aac0.SoftwareRenderMaskConstants.md) RGB555 masks including `xmmword_61A920`, `xmmword_61A930`, `qword_61A940`, and `qword_61A9F0`.

### UID0002V6 constant-pool semantics

- Exact direct loads use RGB555 green-mask blocks `0x0061a920` (`uint32[4]` `0x03e0`) and `0x0061a940` (`uint16[8]` `0x03e0`), red/blue-mask blocks `0x0061a930` (`uint32[4]` `0x7c1f`) and `0x0061a9f0` (`uint16[8]` `0x7c1f`), and the fixed-point scale block `0x0061a990` (`uint8[16]` value 32).
- The scale block has two refs, exactly at `0x004be8e8` and `0x004beb66`. The surrounding subtract-before-widen sequence establishes a 32-based blend weight rather than a color or lookup-table index.
- UID0002V6 is compiler materialization covered by this callback and four peer callbacks. No pool-base/indexed access exists, so reconstructed source should retain scalar mask/weight expressions here rather than introduce a standalone table or raw `xmmword_*` symbols.

## 2026-06-11 A001 Live IDA Refresh

- Live IDA MCP `idb_meta` again identifies `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`, base `0x400000`.
- `lookup_funcs` reconfirms `sub_4BE680` at `0x004be680`, size `0x6e7` / 1767 bytes (Verified with `int_convert.py`), and `0x004bed67` is not a function start. The next modeled callback remains [UID:0002YG][0x004bed80-0x004bedaa.SoftwareRenderCompatRgb555ExpandCallback](by-memory/0x004bed80-0x004bedaa.SoftwareRenderCompatRgb555ExpandCallback.md).
- `callers 0x004be680` is still empty. `xrefs_to 0x004be680` reports only data refs at `0x00558723` and `0x00558bc6` inside [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md).
- `callees` reconfirms the clip/rectangle/color helper set: `sub_4B7CC0`, `sub_4B7EB0`, [UID:000164][0x004b96c0-0x004b9768.GrafPortGetClipRect](by-memory/0x004b96c0-0x004b9768.GrafPortGetClipRect.md), `sub_543D40`, `sub_542AC0`, and `@__security_check_cookie@4`.
- Decompilation reconfirms draw-enabled guard byte `+0x71`, source rectangle clipping, 16-bit destination pointer calculation, active mode byte `+0x70`, color/index gate at `+0x74`, alpha/intensity float at `+0x90`, and SIMD/scalar RGB555 blend paths that consume [UID:0002V6][0x0061a920-0x0061aac0.SoftwareRenderMaskConstants](by-memory/0x0061a920-0x0061aac0.SoftwareRenderMaskConstants.md).
- Raw-byte review of `0x004bed67-0x004bed80` shows a one-byte nop, four local target addresses (`0x004be78f`, `0x004be7f2`, `0x004be862`, `0x004beadc`), then `0xcc` padding. This confirms the interval is switch data for this callback plus alignment, not a missed source function.

## Assignment Decision

Assigned to [UID:0000OC][Surface](by-file/Surface.md) as the direct canonical owner and sole emitter for this callback-table body. Exact source-quality closure raises the child to `92/94`: no ordinary code callers, target refs at `0x00558723` and `0x00558bc6`, 206 shared slot-7 xrefs, complete GrafPort/RectBounds ABI, exact clip/palette/context/mode behavior, local table boundaries, and formal Surface.cpp C++ are all resolved. Do not assign this to [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md); table membership and Surface setup prove shared render ownership. GrafPort is receiver-state support only, and [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) owns the stateless `BlendRgb555` dependency rather than this stateful callback.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | Exact range and boundary islands, callback ABI, two provider refs, 206-slot indirect liveness, clip/setup flow, destination pointer math, global palette and both map calls, receiver/context fields, four exact RGB555 fill/blend modes, raw-color gates, software-buffer override, byte-wrapped weight, scalar/compiler disposition, owner/emitter route, and complete formal C++ are documented. |
| Confidence `94` | Live IDA MCP evidence confirms bounds, bytes, callees, target and slot refs, no direct callers, type layouts, decompiled control/data flow, mask dependencies, and local switch bytes; independent Surface/GrafPort/callback-table docs corroborate the source contract. Only stripped original lexical spellings and exact optimizer inlining/vectorization choices prevent final-audit certainty. |

## Cross-References

- [UID:00016J][0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks](by-memory/0x004bb8d0-0x004c069e.SoftwareRenderCompatCallbacks.md)
- [UID:0000OC][Surface](by-file/Surface.md)
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md)
- [UID:0002V6][0x0061a920-0x0061aac0.SoftwareRenderMaskConstants](by-memory/0x0061a920-0x0061aac0.SoftwareRenderMaskConstants.md)

## 2026-08-04 B005 Source-Quality Closure

### Metadata, range, ABI, liveness, and ownership

- Metadata remains `CANONICAL_OWNER:0000OC`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000OC`, blank emitter position, and `Nested:0`; only score and formal CPP change. The exact body remains `[0x004be680,0x004bed67)`, 1,767 bytes, SHA256 `65018CFDB18B63FA75DE4B99103A930E27D833414AEDEC03B7EB22D658C4B74C`.
- Logical callback ABI is `void __thiscall SoftwareRenderCompatFillCallback(GrafPort *port, const RectBounds *bounds)`. Physical `retn 4`, ECX receiver use, the established `SurfaceFillRectProc`, initializer assignments, RGB565 peer, and consumers jointly reject the provisional `char __userpurge` rendering. The file-local formal uses the project/MSVC-compatible fastcall spelling with unused EDX.
- No ordinary code caller exists. Liveness comes from exact target data refs `0x00558723` in UID00039V and `0x00558bc6` in UID00039W plus 206 xrefs to `g_pfnFillRect` at `0x0069b3fc`. Compat target `0x004be680` is paired with RGB565 target `0x004c4380` and selected by Surface pixel-format setup.
- Surface remains the canonical owner/emitter because both provider routes and fixed slot storage are Surface infrastructure. GrafPort supplies receiver state but has no method/vtable/address route to this body. SoftwareBlend16 supplies stateless RGB555 math but does not own clipping, palette, GrafPort state, or callback selection.

### Exact clipping, fields, palette, and destination flow

- `m_drawEnabled` at `+0x71` rejects the operation before any helper or write. The callback gets the GrafPort clip, copies the requested bounds, intersects clip/request into the request local, and returns when `IsRectEmptyOrInvalid` reports no drawable rectangle.
- The destination is an `unsigned short *` into `m_surfaceContext.pixelData`. First-pixel index is `clipped.left + rowStridePixels * (clipped.top - context.bounds.top) - context.bounds.left`; width and height are right-minus-left and bottom-minus-top, and each row advances by `rowStridePixels`.
- Palette source is always `g_pPaletteLib->GetCurrentPalette()`, not `port->m_palette`. The callback maps `m_drawColor`, then deliberately performs `MapColor(m_textBackFillColor)` even though that result is discarded. Removing the second call would not preserve exact call-side behavior.
- When `m_usesSoftwareBuffer` is true and raw `m_drawColor` equals `128`, mapped `fillPixel` is replaced with RGB555 value `1`. The trigger is the original palette index, not the mapped result.

### Four-mode behavior and RGB555 arithmetic

- `m_drawMode == 0` performs unconditional direct fill. Mode 1 returns when raw `m_drawColor == 0`, otherwise performs the same fill. Direct fill preserves packed two-pixel stores and the exact odd tail represented by existing `FillRgb555Span` source.
- Mode 2 performs weighted blend. Mode 3 first applies the same raw-color-zero gate and then performs weighted blend. Every other mode returns without writing.
- Destination weight is `static_cast<unsigned char>(static_cast<int>(m_alpha * 32.0f + 0.5f))`; both integer truncation and byte wrapping are preserved. Each source expression is `BlendRgb555(existingDestination, fillPixel, destinationWeight)`, so weight 0 selects fill and weight 32 preserves destination.
- The RGB565 peer's mode-3 odd-width pair-plus-extra-pixel anomaly is absent from this exact callback and is rejected for compat source. UID0002YF iterates the exact clipped width in both weighted modes.

### Compiler lowering, boundaries, and source placement

- SSE4.1 dispatch, `__isa_available`, XMM temporaries, RGB555 mask splats, and the scale-32 block in UID0002V6 are compiler lowering of scalar fill/blend expressions. Human source uses `FillRgb555Span` and `BlendRgb555`; it does not emit intrinsics, runtime ISA branches, raw `xmmword_` symbols, or standalone mask arrays.
- Predecessor `[0x004be66e,0x004be680)` has SHA256 `9BF2E0BD9033ECF03CBFCD3BD728503FE72DD564FAA572769228700E44FF4135` over `66 90` plus four pointers for the prior callback's switch. Successor `[0x004bed67,0x004bed80)` has SHA256 `244C814F53AC2A8564DC64295D9366D4F35E831F705D2A0EA80DB3CEA2424564` over one NOP, targets `0x004be78f`, `0x004be7f2`, `0x004be862`, `0x004beadc`, and eight `0xcc` bytes. Neither interval emits source or requires another child.
- Adopt `SoftwareRenderCompatFillCallback`, matching accepted initializer/provider-family vocabulary and a collision-free source namespace. `FillRgb555RectCallback` is plausible but less coherent with the established compat family. Raw `sub_4BE680`, `dword_69B3FC`, address-derived constants, and decompiler locals are evidence names only.
- Place the body file-locally in `NexusTK/render/Surface.cpp` through UID0000OC. Do not move it into GrafPort.cpp or SoftwareBlend16.cpp and do not create a standalone callback file. UID0000TN already owns `SurfaceFillRectProc` and `g_pfnFillRect` in Surface.h/Surface.cpp, so this target's H channel remains blank for a specific non-duplication reason.

### IDA handoff and blocker closure

- Evidence-time IDA name remains `sub_4BE680` with provisional decompiler type and blank function comment. Supervisor Gate 2B recommendation is the source name `SoftwareRenderCompatFillCallback`, semantic `void __thiscall SoftwareRenderCompatFillCallback(GrafPort *port, const RectBounds *bounds)`, and the exact regular comment: `Surface slot-7 RGB555 rectangle-fill callback; clips the requested bounds, maps GrafPort draw colors, and performs copy, color-key, weighted-blend, or keyed-weighted-blend fills.`
- Any supervisor mutation must preserve the exact body/range/hash, 469 instructions, 56 blocks, complexity 36, two xrefs, 18 internal comments, boundary islands, slot-7 zero bytes/name/type/206 refs, GrafPort/EPFTileContext/RectBounds layouts, UID0002V6 constants, and RGB565 peer. The physical frame needs immediate prestate/readback; no direct stack/local mutation is authorized by this documentation edit.
- Former field, signature, branch-table, liveness, naming, SIMD, source-placement, and C++ blockers are all closed by the evidence above. Original private lexical spellings and exact optimizer source decisions remain confidence caps only, not reasons to retain raw labels or blank formal code.

## Changes

- 2026-07-24 B003 UID0002V6 evidence synchronization: preserved `87/90`, metadata, and blank formal while adding the exact RGB555 mask blocks, scale-32 block and sites, fixed-point interpretation, and covered-by compiler-pool relationship. This does not claim the callback's independent body/name/type pass complete.

- 2026-06-07 A010 Batch049: Created as an exact split child for the previously unsplit `dword_69B3FC` compat callback using existing IDA-backed parent evidence; left unassigned below the corrected completion gate.
- 2026-06-10 B001-029 final gate repair:
  - Changed to: `COMPLETION:85`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID:0000OC`.
  - Evidence: rechecked live IDA xrefs/callees/no-caller state and the decompiled clipped fill/blend body. Surface is the direct parent because this function is installed in `dword_69B3FC` and operates on Surface/GrafPort callback state; SoftwareBlend16 remains limited to stateless blend/math helpers.
- 2026-06-11 A001 Batch 176 refresh:
  - Changed to: `COMPLETION:87`, `CONFIDENCE:90`; parent remains [UID:0000OC][Surface](by-file/Surface.md).
  - Evidence: live IDA MCP reconfirmed exact size, empty callers, data-only target refs, callee set, receiver mode/color/alpha fields, RGB555 mask-constant dependencies, and local switch-data bytes at `0x004bed67-0x004bed80`.
