*** UID:0004R7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserPane::BuildInventorySlotRangeText(wchar_t *outText) const
{
    wchar_t *out = outText;
    int runStart = 0;
    const int slotCount = g_activeUserStatusPane->m_inventorySlotCount;

    for (int slot = 1; slot <= slotCount + 1; ++slot)
    {
        const bool active =
            slot <= slotCount && m_inventorySlots[slot - 1].active != 0;
        if (active)
        {
            if (runStart == 0)
            {
                runStart = slot;
                *out++ = slot <= 26
                    ? (wchar_t)(L'a' + slot - 1)
                    : (slot <= 52
                        ? (wchar_t)(L'A' + slot - 27) : L' ');
            }
            continue;
        }

        if (runStart == 0)
            continue;

        const int runEnd = slot - 1;
        if (runEnd - runStart + 1 <= 3)
        {
            for (int item = runStart + 1; item <= runEnd; ++item)
            {
                *out++ = item <= 26
                    ? (wchar_t)(L'a' + item - 1)
                    : (item <= 52
                        ? (wchar_t)(L'A' + item - 27) : L' ');
            }
        }
        else
        {
            *out++ = L'-';
            *out++ = runEnd <= 26
                ? (wchar_t)(L'a' + runEnd - 1)
                : (runEnd <= 52
                    ? (wchar_t)(L'A' + runEnd - 27) : L' ');
        }
        runStart = 0;
    }

    *out = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Complete const UserPane inventory-slot range formatter using the live UserStatusPane slot count, one-based lowercase/uppercase labels, a sentinel iteration, literal expansion for runs of at most three, hyphen compression for longer runs, and exact null termination. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005a40d0-0x005a430c UserPane BuildInventorySlotRangeText

## Status

- Exact range: `[0x005a40d0,0x005a430c)`, size `0x23c` / 572 bytes.
- SHA256: `1B218CA010C92D8A38AE6E3BC4B4EB37F6FBC1D2DBB4C7FFA50FE31B446081D1`.
- Entity kind: `const` UserPane member, owned/emitted through [UID:0000FQ][UserPane](by-class/UserPane.md).
- Exact source-facing signature: `void UserPane::BuildInventorySlotRangeText(wchar_t *outText) const`; spelling is descriptive, while receiver, argument, output, and algorithm are proved.
- Rebuild handling: source-authored project method with exact R10 in the managed block.

## Exact Algorithm

The method obtains the dynamic inventory slot count from `g_activeUserStatusPane->m_inventorySlotCount`, scans one-based slot numbers through `slotCount + 1`, and uses the final iteration as an inactive sentinel that flushes a trailing active run. Each active test reads `m_inventorySlots[slot - 1].active` only when `slot <= slotCount`, preserving short-circuit bounds behavior.

On the first slot of a run, it emits the one-based label: `a..z` for 1..26, `A..Z` for 27..52, and a space outside that range. When a run ends, lengths of one through three are emitted literally by appending each remaining slot label. Runs longer than three emit a hyphen and only the final label. The run marker is reset after each flush, and one wide null terminator is written after the scan.

This exact threshold means a three-slot run such as `abc` stays literal; only four or more compress. The dynamic count and explicit out-of-52 space behavior are retained even though normal inventory counts may not exercise every branch.

## Range, Padding, And Evidence

- The live IDA pass decompiled the complete body and normalized receiver-relative inventory-slot activity reads plus the UserStatusPane count dependency.
- The exact exclusive end is `0x005a430c`. The gap `[0x005a430c,0x005a4310)` is four bytes of parent-only alignment before the spell-slot sibling and is not part of either source body.
- UID0003V9 calls this formatter when constructing inventory command prompt/range text. This call and the receiver fields establish class-member ownership.
- No allocation, packet send, persistent output ownership, STL container, or locale conversion occurs. The caller owns a sufficiently sized writable wide buffer.

## Ownership And Rejected Alternatives

- Accepted owner/emitter UID0000FQ UserPane; the body reads UserPane inventory slot state and has a UserPane receiver.
- Rejected UID0000P1-only static helper: receiver-relative slot reads prove class membership.
- Rejected fixed 52-slot inventory scan: the body reads the live status-pane count.
- Rejected compressing runs of three, emitting commas/spaces between runs, or skipping the sentinel: all contradict exact branches/output writes.
- Rejected no-code/raw-helper treatment because this is a complete separately bounded source method with exact callers and human source shape.

## Score Rationale

Completion `90` records exact range/size/hash, receiver/signature, count source, activity field, sentinel and bounds behavior, label mapping, range threshold, termination, caller, padding, formal source, dependencies, negatives, and rejected alternatives. Confidence `93` follows from the complete body and exact output algorithm; original private member/method spelling remains inferred.

## Cross-References

- [UID:0000FQ][UserPane](by-class/UserPane.md)
- [UID:0000P1][UserPane](by-file/UserPane.md)
- [UID:0003V9][0x005a5bd0-0x005a7422.UserPaneHandleKeyOrTextEvent](by-memory/0x005a5bd0-0x005a7422.UserPaneHandleKeyOrTextEvent.md)

## Changes

- 2026-07-15 B003 UID0003V9 accepted callback: created and submitted for validator UID registration as the exact inventory-slot range formatter with R10 and complete algorithm/range/source evidence.
