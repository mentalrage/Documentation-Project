*** UID:0004U8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5851296 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@005948A0 | UID0000OJ accepted formal channel
short TextEditPane::MeasureTextRunWrapSegment(const wchar_t *text,
                                              short textLength,
                                              int textOffset,
                                              int *remainingWidth,
                                              signed char *hitPart,
                                              unsigned short segmentFlags,
                                              int xOffset) const
{
    (void)textOffset;
    (void)segmentFlags;
    (void)xOffset;

    StringIter iter(text, textLength);
    wchar_t current = iter.GetNextChar();
    wchar_t next = iter.GetNextChar();
    short accepted = 0;
    short previousTotalWidth = 0;

    if (current != 0)
    {
        short totalWidth = 0;
        for (;;)
        {
            short advance = 0;
            g_pFontImageLib->MeasureGlyphWidth(
                m_fontId, current, next, false, 0, 0, &advance, 0);
            totalWidth += advance;
            if (*remainingWidth < totalWidth)
            {
                *hitPart = (totalWidth + previousTotalWidth) / 2
                    < *remainingWidth ? 0 : -1;
                *remainingWidth = -1;
                return accepted;
            }

            ++accepted;
            current = next;
            next = iter.GetNextChar();
            previousTotalWidth = totalWidth;
            if (current == 0)
                break;
        }
    }

    *remainingWidth -= previousTotalWidth;
    *hitPart = 0;
    return accepted + 1;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready short TextEditPane::MeasureTextRunWrapSegment(...) const at exact [0x005948a0,0x005949be): advances a bounded StringIter with current/next glyph context, accumulates measured width, returns the exact overflow hit half and terminal-inclusive count, and preserves three ABI parameters that are unused in this body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005948a0-0x005949be TextEditPane MeasureTextRunWrapSegment

## Status And Source Placement

- Exact member-method range: `[0x005948a0,0x005949be)`, 286 bytes.
- Canonical owner/emitter: [UID:0000EO][TextEditPane](by-class/TextEditPane.md); source module [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Source child of [UID:0001JS][0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers](by-memory/0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers.md).

## Exact Binary And ABI Evidence

- Bounded decoding found 111 instructions, 12 basic blocks, cyclomatic complexity 2, four references, and two modeled callers. Exact SHA256 is `8ACAB5D96085A394C2EC08296A8F9119C549DB4AE4C53EE5F575EAD534A26B13`.
- The seven explicit source parameters are preserved by the observed stack cleanup and caller pushes. `textOffset`, `segmentFlags`, and `xOffset` are ABI-real but unused in this body.
- The return and text length are signed shorts; `remainingWidth` is an in/out int pointer; `hitPart` is a signed-byte out parameter.
- Font measurement uses `m_fontId` and current/next UTF-16 code units, with the measured advance returned through a signed-short local.

## Corrected Iterator And Width Invariant

- Construct one bounded `StringIter`, then prime both `current` and `next` before entering the loop.
- `totalWidth` is cumulative through the candidate current glyph. `previousTotalWidth` is the accepted cumulative width before that candidate and is updated only after acceptance.
- Overflow occurs when available width is strictly less than cumulative width. The hit half compares available width against the midpoint `(totalWidth + previousTotalWidth) / 2` using the binary's ordering.
- On overflow, write hit-part `0` or `-1`, set remaining width to `-1`, and return the number already accepted.
- On complete traversal, subtract only `previousTotalWidth`, write hit-part zero, and return `accepted + 1`. The terminal-inclusive return is intentional and matches the caller invariant; it must not be normalized to `accepted`.

## Negative And Rejected Evidence

- Rejected one-character-late iterator shape: it breaks the current/next glyph context and cumulative midpoint arithmetic.
- Rejected per-glyph remaining-width decrement: the binary carries a cumulative total and performs one success-tail subtraction.
- Rejected omission of unused ABI parameters: callers and cleanup prove they belong to the source declaration even though this implementation does not read them.
- Rejected raw font-vtable lowering: `FontImageLib::MeasureGlyphWidth` is the project-level source call.
- Rejected no-source/helper debt: target-reachable wrapping callers, stable ABI, and complete behavior support a retained member method.

## Score Rationale And History

- `91/93` reflects exact range/hash/CFG, callers, complete ABI, iterator behavior, corrected cumulative arithmetic, overflow outputs, terminal success invariant, and human C++.
- Lexical confidence remains capped because the private method and parameter spellings are inferred and three source parameters are semantically dormant here.
- Earlier drafts that advanced the iterator late or returned only accepted code units are superseded by the fresh instruction-level re-derivation.

## Rebuild Policy

- Emit the managed member body through UID0000EO/UID0000ON.
- Preserve unused parameters explicitly; let the compiler choose stack/register lowering.
- Do not emit raw offsets, decompiler loops, handwritten vtable calls, padding, or a simplified width invariant.
