*** UID:0002BV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static DWORD MidiEventDelta(MidiStreamWriteContext* stream, const MidiEvent* event)
{
    DWORD delta = event->tick - stream->lastEventTick;
    stream->lastEventTick = event->tick;
    return delta;
}

static int MidiWriteShortStreamEvent(MidiStreamWriteContext* stream, DWORD tick, DWORD event)
{
    MIDIEVENT* outEvent;

    if (stream->writeOffset + sizeof(MIDIEVENT) > stream->writeLimit) {
        stream->fullEventPending = 1;
        return MIDI_FILL_OUTPUT_FULL;
    }

    outEvent = (MIDIEVENT*)(stream->buffer + stream->writeOffset);
    outEvent->dwDeltaTime = tick;
    outEvent->dwStreamID = 0;
    outEvent->dwEvent = event;

    stream->writeOffset += sizeof(MIDIEVENT);
    stream->bytesWritten += sizeof(MIDIEVENT);
    stream->fullEventPending = 0;
    return MIDI_FILL_OK;
}

static void MidiFreeEventPayload(MidiDocument* document, const MidiEvent* event)
{
    if (event->payload != NULL && document->allocatedPayloadCount != 0) {
        free(event->payload);
        --document->allocatedPayloadCount;
    }
}

static int MidiWriteStreamEvent(MidiDocument* document, const MidiEvent* event, MidiStreamWriteContext* stream)
{
    DWORD packed;
    DWORD tempo;
    int result;

    if (event->status < MIDI_STATUS_SYSEX) {
        packed = event->status | ((DWORD)event->data1 << 8) | ((DWORD)event->data2 << 16);

        if ((event->status & 0xF0) == MIDI_STATUS_CONTROL_CHANGE &&
            event->data1 == MIDI_CONTROLLER_VOLUME) {
            packed |= 0x40000000;
        }

        return MidiWriteShortStreamEvent(stream, MidiEventDelta(stream, event), packed);
    }

    if (event->status == MIDI_STATUS_SYSEX || event->status == MIDI_STATUS_ESCAPE) {
        MidiFreeEventPayload(document, event);
        return MIDI_FILL_OK;
    }

    if (event->status == MIDI_STATUS_META && event->data1 == MIDI_META_TEMPO) {
        if (event->payloadLength < 3 || event->payload == NULL) {
            MidiFreeEventPayload(document, event);
            return MIDI_FILL_UNSUPPORTED_EVENT;
        }

        tempo = ((DWORD)event->payload[0] << 16) |
                ((DWORD)event->payload[1] << 8) |
                (DWORD)event->payload[2];

        document->tempoUSecPerQuarter = tempo;
        document->tempoTickCache = event->tick;

        result = MidiWriteShortStreamEvent(
            stream,
            MidiEventDelta(stream, event),
            0x01000000 | (tempo & 0x00FFFFFF));

        MidiFreeEventPayload(document, event);
        return result;
    }

    MidiFreeEventPayload(document, event);
    return MIDI_FILL_UNSUPPORTED_EVENT;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Attached to `MidiPlayer.cpp`; live IDA confirms exact boundary, two stream-fill caller sites, 12-byte WinMM record writes, controller-volume tagging, tempo meta-event conversion, negative status returns, and payload cleanup. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005269f0-0x00526ba8 MidiStreamEventEncoder

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: function
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for behavior, modeled boundary, caller/callee set, WinMM stream-record writes, tempo/controller handling, payload cleanup, and MidiPlayer source-family ownership; medium for final helper, status constant, and structure names
- Parent assignment: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), matching the refined MIDI helper sibling pattern where exact parser/encoder children attach directly to the source file while the aggregate records the helper island.

## Address Range

`0x005269f0-0x00526ba8`.

IDA models this exact range as `sub_5269F0`, size `0x1b8`.

## Behavior

Converts a parsed MIDI/meta event into a 12-byte WinMM `midiStreamOut` event record. It handles tempo meta-event `0xff 0x51`, volume controller event tagging, meta/SysEx payload cleanup, and negative status returns for unsupported events or output-buffer exhaustion.

## Verified Stream Encoding Flow

- Live IDA MCP on 2026-06-03 reports the current prototype as `int __thiscall(_DWORD *this, int, _DWORD *)`, with the second argument acting as the parsed 16-byte MIDI event record and the third argument acting as the stream-output context.
- The stream-output context uses base pointer at dword `+0x00`, buffer offset at `+0x40`, capacity/limit at `+0x44`, used byte count at `+0x48`, first-event timestamp/cache at `+0x4c`, and overflow debounce flag at `+0x50`.
- For channel events (`status < 0xf0`), the helper writes one 12-byte stream record: delta at dword `+0`, zero stream ID at dword `+4`, and packed MIDI short message at dword `+8`.
- Controller `0xb0` with controller number `7` is tagged by OR-ing the packed message with `0x40000000`, matching the MIDI callback's later controller-volume handling.
- For SysEx statuses `0xf0` and `0xf7`, the helper does not emit a stream record; it frees the event payload when the parser allocation counter is nonzero.
- For meta type `0x51`, the helper reads the three-byte tempo payload, stores microseconds-per-quarter state at `this + 0x40`, emits a tempo stream record tagged with `0x01000000`, updates the cached tick/tempo divisor at `this + 0x38`, frees the payload, and advances the stream used byte count by `0x0c`.
- Return values observed by IDA are `0` for success/ignored SysEx, `-104` for stream output space or overflow gating, and `-105` for unsupported meta events after payload cleanup.

## Evidence

- IDA `lookup_funcs` confirms `sub_5269F0` at `0x005269f0-0x00526ba8`.
- Parent page decompilation notes identify tempo event `0xff 0x51`, controller 7 volume handling, `0x40000000` tagging, and `-104`/`-105` status returns.
- IDA live analysis shows cleanup through `j___free_base`, matching allocated payload ownership from the event reader.
- 2026-06-03 restarted IDA MCP `py_eval` confirms exact boundary `0x005269f0-0x00526ba8`, size `0x1b8`, previous function `0x00526790-0x005269ea`, next function `0x00526bb0-0x00526cd9`, and terminal `retn 8`.
- 2026-06-03 IDA MCP caller analysis shows two call sites, `0x00527329` and `0x00527357`, both inside [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md).
- 2026-06-03 IDA MCP callees/xrefs-from show no project helper callees; the only callee is `j___free_base`, reached from all allocated-payload cleanup paths.
- 2026-06-03 IDA MCP disassembly confirms output-buffer fields at `+0x40`, `+0x44`, `+0x48`, `+0x4c`, and `+0x50`; controller-volume tag `0x40000000`; status returns `0xffffff98` and `0xffffff97`; tempo state writes at `this + 0x38` and `this + 0x40`; and 12-byte output advancement by `0x0c`.
- 2026-06-03 IDA MCP decompilation confirms the channel-event, SysEx, tempo meta-event, unsupported-meta cleanup, and output-space failure branches.

## 2026-06-06 Live IDA MCP Reconfirmation

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms:

- `lookup_funcs` still models `sub_5269F0` exactly at `0x005269f0-0x00526ba8`, size `0x1b8`.
- Direct callers/xrefs remain `0x00527329` and `0x00527357`, both inside [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md).
- The only callee remains `j___free_base`, matching parser payload cleanup ownership for SysEx/meta payloads.
- Padding bytes are `0xcc` from `0x005269ea-0x005269f0` before the function and `0x00526ba8-0x00526bb0` after it.

## Score Rationale

Raised to `87/91` on 2026-06-17. The page has repeated live IDA confirmation for exact modeled bounds, two stream-fill call sites, payload-cleanup callee, WinMM 12-byte record writes, controller-volume tagging, tempo meta-event conversion, stream-output field map, negative status returns, and padding. B001's source-quality pass resolves draft names to `MidiWriteStreamEvent`, `MidiStreamWriteContext`, `MidiEvent`, and named `MidiFillResult` constants, so formal first-draft C++ is now populated. The score remains below final audit because original symbols and shared declaration pages are not recovered.

## Reconstruction Notes

The negative return values should become named parser/stream-fill status constants once all MIDI stream callers are rewritten.

`RECONSTRUCTABLE:TRUE` is appropriate because this is source-authored WinMM/SMF stream conversion logic with verified caller and data-flow relationships. The formal C++ block is now populated with descriptive/inferred names from B001's 2026-06-17 source-quality pass. Original helper/type names remain unproven, but descriptive names are strong enough for first-draft code.

## 2026-06-17 Source-Quality Naming And C++ Readiness

B001 recommends `MidiWriteStreamEvent` as the best current source-facing helper name. The function consumes a parsed `MidiEvent`, writes WinMM `MIDIEVENT` records through `MidiStreamWriteContext`, frees allocated SysEx/meta payloads, emits tempo meta records, and returns named `MidiFillResult` values.

Relevant shared declaration names for this target:

| Item | Descriptive name | Evidence |
| --- | --- | --- |
| Stream output context | `MidiStreamWriteContext` | Owns buffer pointer, write offset/limit, bytes written, last event tick, and output-full replay flag. |
| Channel events | `MidiWriteShortStreamEvent` | Writes one 12-byte WinMM `MIDIEVENT`. |
| Event timing | `MidiEventDelta` | Converts absolute event ticks into stream delta time while updating `lastEventTick`. |
| Payload cleanup | `MidiFreeEventPayload` | Mirrors the parser allocation counter and frees SysEx/meta payload buffers. |
| Return `-104` | `MIDI_FILL_OUTPUT_FULL` | Not a hard error; stream-fill preserves and retries the pending event. |
| Return `-105` | `MIDI_FILL_UNSUPPORTED_EVENT` | Unsupported meta/status after cleanup. |

Accepted constants include `MIDI_STATUS_CONTROL_CHANGE` (`0xb0`), `MIDI_CONTROLLER_VOLUME` (`7`), `MIDI_STATUS_SYSEX` (`0xf0`), `MIDI_STATUS_ESCAPE` (`0xf7`), `MIDI_STATUS_META` (`0xff`), and `MIDI_META_TEMPO` (`0x51`). The controller-volume path keeps the observed WinMM callback tag `0x40000000`; the tempo path keeps the observed `0x01000000` stream event tag.

Rejected alternatives:

- Naming `-104` as a terminal parser error is rejected because it is output-buffer-full replay state.
- Leaving the C++ block blank until original enum names are recovered is rejected; descriptive constants now preserve the behavior better than raw negative literals.
- Moving this to `SoundManager` or a generic encoder file is rejected because the only callers are the SMF stream-fill helper inside the `MidiPlayer.cpp` route.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:00023C][0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers](by-memory/0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers.md)
- [UID:0002BU][0x00526790-0x005269ea.MidiEventReader](by-memory/0x00526790-0x005269ea.MidiEventReader.md)
- [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)

## Changes

- 2026-06-03 restarted IDA MCP evidence and scoring update:
  - Before: scores were `68/82`, `RECONSTRUCTABLE` was blank, and autogen parent metadata was blank.
  - After: marked `RECONSTRUCTABLE:TRUE`, attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), and set scores to `78/86`.
  - Summary/evidence: live IDA MCP revalidated the exact boundary, `retn 8`, two stream-fill caller sites, payload cleanup callee, stream context fields, 12-byte record writes, controller-7 `0x40000000` tagging, tempo meta-event `0x51` conversion, negative status returns, and cleanup behavior for SysEx/unsupported meta payloads. C++ remains blank because final source-facing event/output context types and status constants are not at the 95+ gate.
- 2026-06-06 live IDA MCP reconfirmation:
  - Changed to: score `82/90`.
  - Summary/evidence: exact modeled boundary, two stream-fill call sites, `j___free_base` payload cleanup callee, encoder behavior already on the page, and clean padding were reconfirmed live. Final C++ remains blank below the source-grade threshold.
- 2026-06-17 B001 MIDI source-quality execution:
  - Changed to: score `87/91`, with formal first-draft C++ populated.
  - Summary/evidence: B001 rechecked the existing IDA-backed docs, support pages, generated coverage route, and current generated-output state; accepted `MidiWriteStreamEvent`, `MidiStreamWriteContext`, `MidiFillResult`, WinMM `MIDIEVENT` packing, controller-volume callback tagging, tempo event handling, payload cleanup, and named output-full/unsupported-event returns; owner/emitter remain [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md).
