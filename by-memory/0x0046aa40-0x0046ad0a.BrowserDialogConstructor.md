*** UID:00040C | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000017 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000HV | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
BrowserDialog::BrowserDialog(int,
                             const OLECHAR *initialUrl,
                             int width,
                             int height)
    : BrowserPane(L"", -1, true),
      Singleton<BrowserDialog>(),
      m_width(width != 0 ? width : 662),
      m_height(height != 0 ? height : 562)
{
    RectBounds childBounds;
    RectBounds dialogBounds;

    if (g_useEpfAssets)
    {
        GetChildRect(0, &childBounds);
        AddControl(new ImageButtonControlPane(14, &childBounds));

        GetChildRect(1, &childBounds);
        m_browserControlPane =
            new BrowserControlPane(&childBounds, initialUrl, this);
        AddControl(m_browserControlPane);

        InitRectBoundsFromSize(&dialogBounds,
                               (1024 - m_width) / 2,
                               (768 - m_height) / 2,
                               m_width,
                               m_height);
        OnCreate(&dialogBounds, 0, NULL, g_layoutContext_69B374);
        OnShow(NULL, NULL);
        SlideOpenVertical();
        SetFocusedControl(0);
    }
    else
    {
        InitRectBounds(&dialogBounds,
                       0,
                       0,
                       g_screenWidth,
                       g_screenHeight);
        InitRectBounds(&childBounds, 0, 0, 63, 24);

        ImageButtonControlPane *titleButton =
            new ImageButtonControlPane(14, &childBounds);
        AddControl(titleButton);

        childBounds = dialogBounds;
        m_browserControlPane =
            new BrowserControlPane(&childBounds, initialUrl, this);
        AddControl(m_browserControlPane);

        OnCreate(&dialogBounds, 0, NULL, g_layoutContext_69B374);
        OnShow(NULL, NULL);
        StoreClampRect(&dialogBounds);
        titleButton->Hide();
    }

    SetPendingControl(0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Evidence collected and reconfirmed during MCP sessions e72f177b and 545b4991 resolves the exact 0x2ca-byte BrowserDialog constructor at 0x0046aa40-0x0046ad0a with four-argument ABI, callers (0, packet URL, 0, 0) and (0, L"help.nexustk.com/wiki", 900, 650), BrowserPane/Singleton/BrowserDialog construction order, owning BrowserControlPane pointer +0x26c, width/height +0x278/+0x27c with 662x562 defaults and EBO overlap, current-assets/legacy layout branches, exact child/rectangle/DialogPane helpers, singleton publication/EH clear, six-byte post-padding, compiler-only vptr/EH/allocation scaffolding, and formal first-draft C++; no continued availability of either evidence session is asserted. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046aa40-0x0046ad0a BrowserDialog Constructor

## Status

- Disposition: reconstructable source-authored BrowserDialog constructor with formal first-draft C++.
- Entity kind: BrowserDialog constructor.
- Owner: [UID:000017][BrowserDialog](by-class/BrowserDialog.md)
- Confidence: very strong for range, owner, caller ABI, singleton/EH route, object layout, field representation, branches, helper signatures, and first-draft C++; capped below final by original identifier spelling and physical Browser source-file packaging.

## Covered Range

| Range | Role | Notes |
| --- | --- | --- |
| `0x0046aa40-0x0046ad0a` | `BrowserDialog::BrowserDialog` | Constructs BrowserPane and Singleton bases, initializes BrowserDialog dimensions, creates/registers title and separately allocated BrowserControlPane children, and completes current or legacy dialog layout. |
| `0x0046ad0a-0x0046ad10` | padding | Existing ignored alignment gap before [UID:00020X][0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals](by-memory/0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals.md). |

## Behavior

- Starts from the BrowserPane/DialogPane base portion, then installs BrowserDialog vtable views.
- Writes [UID:0002WI][0x0067ab98-0x0067ab9c.g_pBrowserDialog](by-memory/0x0067ab98-0x0067ab9c.g_pBrowserDialog.md) / active `unk_67AB98` during singleton setup. Current RTTI identifies `Singleton<BrowserDialog>` as a non-virtual base at complete-object offset `+0x278`, matching the constructor's singleton publication around that boundary.
- Calls the child-rect helper twice, at documented call sites `0x0046ab1d` and `0x0046ab5f`.
- Stores a separately allocated [UID:000015][BrowserControlPane](by-class/BrowserControlPane.md) pointer in `m_browserControlPane` at `+0x26c`; the child allocation is `0x338` bytes and BrowserControlPane owns its hosted child HWND/COM host fields at `+0x11c` and `+0x120`.
- The constructor ABI has four stack arguments and returns with `retn 0x10`: a body-unused `int`, `const OLECHAR *initialUrl`, `int width`, and `int height`. The definition intentionally leaves the first parameter unnamed because both direct callers pass zero and no body read proves a semantic role.
- Effective dimensions are `m_width` at `+0x278` and `m_height` at `+0x27c`; zero inputs select `662` and `562`. The empty `Singleton<BrowserDialog>` base is also RTTI-located at `+0x278` and overlaps `m_width` through empty-base optimization in the `0x280`-byte complete object.
- With `g_useEpfAssets == 1`, child ids `0` and `1` provide title/browser rectangles, image id `14` and BrowserControlPane children are registered, the requested/default size is centered in fixed `1024x768`, then the dialog is created, shown, slid open vertically, and focused on control `0`.
- In the legacy branch, dialog bounds use `(0,0,g_screenWidth,g_screenHeight)`, title bounds use `(0,0,63,24)`, browser bounds copy the full dialog, the clamp rectangle is stored, and the title button dispatches inherited `Pane::Hide()` through vtable slot `+0x18` (`0x00617de4 -> 0x00544750`). Both branches finish with `SetPendingControl(0)`.

## Evidence

- [UID:000017][BrowserDialog](by-class/BrowserDialog.md) records exact constructor range `0x0046aa40-0x0046ad0a`.
- Direct constructor callers are `0x00513ebe` inside a MapPane browser/alert path and `0x005a7079` inside `UserPane::OnKeyEvent`.
- Constructor data refs install BrowserPane vtables `0x006132ec`, `0x00613360`, and `0x00613390`, then BrowserDialog vtables `0x0061339c`, `0x00613410`, and `0x00613440`.
- Constructor writes to singleton storage at `0x0046aab7` and `0x0046aabe`; [UID:0002WI][0x0067ab98-0x0067ab9c.g_pBrowserDialog](by-memory/0x0067ab98-0x0067ab9c.g_pBrowserDialog.md) documents constructor writes, destructor/cleanup clears, and MapPane/UserPane reads.
- The next source-bearing BrowserDialog virtual cluster begins at [UID:00020X][0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals](by-memory/0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals.md), separated by confirmed padding `0x0046ad0a-0x0046ad10`.
- Evidence collected during B002 MCP session `80de0a67` classified the far EH tail chunk at `0x005fac2b-0x005fac32` as the BrowserPane-base destructor unwind action for this constructor: it loads saved `this` from `[ebp+var_3C]` and jumps at `0x005fac2e` to [UID:000408][0x0046a8a0-0x0046a8bf.BrowserPaneNonDeletingDestructor](by-memory/0x0046a8a0-0x0046a8bf.BrowserPaneNonDeletingDestructor.md). The unwind-map data ref at `0x0065976c` points to action label `0x005fac2b`; this is compiler EH cleanup for a completed BrowserPane base, not an ordinary source callsite.
- Evidence collected during B003 MCP session `359c7886` confirmed that BrowserDialog RTTI hierarchy descriptor `0x00642ec0` has nine entries and base descriptor `0x00642f14` names `Singleton<BrowserDialog>` with `mdisp=0x278`, `pdisp=-1`, and `vdisp=0`. The constructor installs BrowserPane vptr views first, publishes `g_pBrowserDialog` at `0x0046aab7` with fallback clear at `0x0046aabe`, then installs BrowserDialog views.
- The compact clear thunk `0x00470220-0x0047022b` only clears `g_pBrowserDialog` and is reached from constructor EH action `0x005fac3c`. It is compiler unwind support for singleton publication, not a second constructor/destructor source owner.
- Evidence collected during MCP database session `e72f177b` reported `sub_46AA40` size `0x2ca`, 261 instructions including far EH funclets, a unique wildcarded exact-range signature, twelve `0xcc` bytes before the function, and six `0xcc` bytes at `0x0046ad0a-0x0046ad10`. No function begins at the end-exclusive boundary; the statement records evidence-time results rather than current session availability.
- Exactly two direct callers exist. `0x00513ebe` allocates `0x280` bytes and calls `(0, packetWideUrl, 0, 0)`; `0x005a7079` allocates `0x280` and calls `(0, L"help.nexustk.com/wiki", 900, 650)`.
- Fresh helper decompilation resolves `GetChildRect(short, RectBounds *)`: id `0` yields `((m_width-63)/2, m_height-32, 63, 24)`, id `1` yields `(11,42,m_width-22,m_height-82)`, and other ids return `childId-1`. `InitRectBounds` writes exact edges; `InitRectBoundsFromSize` writes left/top plus width/height.
- Current support signatures match the raw calls: `ImageButtonControlPane(int,const RectBounds *)`, `BrowserControlPane(const RectBounds *,const OLECHAR *,BrowserPane *)`, `AddControl`, `OnCreate`, `OnShow`, `SlideOpenVertical`, `SetFocusedControl`, `SetPendingControl`, and `StoreClampRect(const RectBounds *)`.
- Constructor EH action `0x005fac2b/2e` destroys the completed BrowserPane base; `0x005fac33/3c` adjusts to `+0x278` and jumps to singleton-clear thunk `0x00470220`; four allocation cleanup actions call the free helper; `0x005fac6d` is the SEH handler. These, concrete vptr stores, allocation null guards, stack cookies, and the constructor return are compiler output.

## Source-Quality Decision

Emit the formal source-shaped constructor above. The four-argument ABI, body-unused first argument, URL/dimension roles, owning pointer field, dimensions/defaults, child/helper signatures, singleton-base route, and both branches are now evidence-backed. Exact original names and `Browser.cpp` versus `BrowserDialog.cpp` packaging remain confidence caps, not C++ blockers.

The source intentionally uses base/member initialization, ordinary locals, `new`, and inherited methods. It excludes concrete BrowserPane/BrowserDialog vptr assignments, singleton pointer arithmetic, EH-state writes/funclets, allocation guards, stack-cookie operations, and `return this`; those are regenerated by the compiler from the class hierarchy and source operations.

Rejected alternatives:

- Emitting this body through the mixed [UID:0000Z5][0x0046a860-0x0046ad0a.BrowserPaneAndDialog](by-memory/0x0046a860-0x0046ad0a.BrowserPaneAndDialog.md) parent aggregate: rejected because the aggregate also contained BrowserPane methods.
- BrowserPane ownership: rejected because this body installs BrowserDialog vtables and writes `g_pBrowserDialog`.
- Separate `BrowserDialog.cpp` route now: rejected for this callback because [UID:0000HV][Browser](by-file/Browser.md) remains the accepted Browser source root and no current evidence requires a physical split.
- `+0x26c` as an embedded object or title button as a persistent field: rejected by separate `0x338` allocation/pointer store and the absence of any title-button member store.
- A named first argument such as `controlId`, `mode`, or `flags`: rejected because the body never reads it and both callers pass zero.
- Legacy `titleButton->Show()`: rejected because the exact inherited slot resolves to `Pane::Hide()` and clears visibility state.

## Cross-References

- Parent split index: [UID:0000Z5][0x0046a860-0x0046ad0a.BrowserPaneAndDialog](by-memory/0x0046a860-0x0046ad0a.BrowserPaneAndDialog.md)
- Class: [UID:000017][BrowserDialog](by-class/BrowserDialog.md)
- File: [UID:0000HV][Browser](by-file/Browser.md)
- BrowserControlPane: [UID:000015][BrowserControlPane](by-class/BrowserControlPane.md)
- Singleton storage: [UID:0002WI][0x0067ab98-0x0067ab9c.g_pBrowserDialog](by-memory/0x0067ab98-0x0067ab9c.g_pBrowserDialog.md)
- Adjacent virtual cluster: [UID:00020X][0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals](by-memory/0x0046ad10-0x0046b51d.BrowserDialogPaneVirtuals.md)

## Changes

- 2026-07-12 B005 UID00040C source-quality implementation callback:
  - Raised `86/90 -> 90/92`; preserved owner `000017`, emitter `0000HV`, reconstructable true, Nested 0, and exact range.
  - Added the accepted formal constructor and MCP proof collected during sessions `e72f177b` and `545b4991` for the four-argument ABI, two caller argument sets, `0x280` object size, `m_browserControlPane` pointer at `+0x26c`, width/height/defaults at `+0x278/+0x27c`, Singleton EBO overlap, both layout branches, helper signatures/formulas, inherited Hide call, RTTI/vtable sequence, singleton/EH cleanup, exact padding/signature, compiler/source boundary, rejected alternatives, and Browser source route. This historical callback note does not assert those sessions remain available.
  - Superseded the historical embedded-object and blank-C++ blocker assumptions without removing the earlier split/destructor evidence.

- 2026-07-12 B003 UID0004DH support sync:
  - Score and blank formal C++ remain unchanged. Added the RTTI-proven `Singleton<BrowserDialog>` `+0x278` base interpretation, BrowserPane-to-BrowserDialog vptr install order, singleton publish/fallback sites, and constructor-EH clear thunk `0x00470220` reached through action `0x005fac3c`.
- 2026-06-20 B002 Rule 26 split implementation:
  - Created exact BrowserDialog constructor child from the former mixed [UID:0000Z5][0x0046a860-0x0046ad0a.BrowserPaneAndDialog](by-memory/0x0046a860-0x0046ad0a.BrowserPaneAndDialog.md) aggregate.
  - Summary/evidence: incorporated direct caller facts, vtable install order, `g_pBrowserDialog` writes, child-rect helper calls, BrowserControlPane child construction, source-split caveat, and blank-C++ decision.
- 2026-06-23 B002 BrowserPane destructor unwind sync:
  - Added the EH action label `0x005fac2b`, jump site `0x005fac2e`, and unwind-map data ref `0x0065976c` proving this constructor's reference to [UID:000408][0x0046a8a0-0x0046a8bf.BrowserPaneNonDeletingDestructor](by-memory/0x0046a8a0-0x0046a8bf.BrowserPaneNonDeletingDestructor.md) is compiler unwind cleanup.
