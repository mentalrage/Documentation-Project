*** UID:0002GG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int ColorStringChattingMessage::MeasureLines(void *drawContext, int width)
{
    const wchar_t *text = m_text;
    int textLength = 0;
    while (text[textLength] != L'\0')
        ++textLength;

    int lineCount = GetSingleLineCount(drawContext, text, textLength, width);
    if ((short)lineCount < 1)
        return 1;

    text = m_text;
    textLength = 0;
    while (text[textLength] != L'\0')
        ++textLength;

    return static_cast<unsigned short>(
        GetSingleLineCount(drawContext, text, textLength, width));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA verifies exact bounds, vtable ownership, owned UTF-16 length scan, width argument forwarding, minimum-one-line clamp, return convention, and padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00483630-0x0048369a - ColorStringChattingMessage Measure Lines

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: virtual line-measure method.
- Owner: [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md).
- Source placement: [UID:0000I5][Chatting](by-file/Chatting.md).
- Parent aggregate: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Rebuild handling: `source-authored`.
- Confidence: strong for exact bounds, vtable ownership, text-length scan, measurement helper call, minimum-one-line behavior, and padding.
- C++ handling: first-draft C++ is now populated for this exact virtual method. The draft preserves the duplicated UTF-16 length scans and duplicated helper calls rather than collapsing the current binary behavior to `return 1`.

## Boundary

Live IDA models the line-measure method at `0x00483630-0x0048369a`, size `0x6a`. The preceding `0x0048362b-0x00483630` bytes are five `0xcc` alignment bytes after clone, and `0x0048369a-0x004836a0` is six `0xcc` bytes before the draw method.

## Behavior

This virtual method measures how many display lines the owned UTF-16 text requires for a requested width. It reads `wchar_t *m_text` from object offset `+0x04`, manually scans the UTF-16 string length, calls shared constant line-count callback [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md), and returns at least `1`.

The helper call shape is source-significant even though [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md) currently returns one. The caller loads the pane/context argument into `ecx`, pushes stack arguments `text`, `length`, and `width`, calls `GetSingleLineCount`, and clamps the result to at least one display row. When the first measurement result is greater than or equal to one, it repeats the UTF-16 length scan and helper call, then returns the 16-bit line count widened to an integer. Do not simplify this method's source-shape documentation to only `return 1;`; the preserved structure is a line-count callback interface shared with `ColorStringSystemMessage::GetLineCount`.

The signature is best represented as `int ColorStringChattingMessage::MeasureLines(void *drawContext, int width)` until a later declaration pass proves a narrower pane/draw-context base type. The method epilogue is `ret 8`, proving two post-`this` arguments. The first argument is forwarded in `ecx` as the draw/context receiver for [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md); it is not the width argument and should not be omitted as "unused" just because the current helper ignores it. The second argument is the width: [UID:0002EV][0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint](by-memory/0x0047f8d0-0x0047fbc5.ChattingPaneOnPaint.md) pushes current-layout width `783` and old-layout width `408` before dispatching the measure vslot.

The current binary behavior is one display line because [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md) is `mov eax, 1; ret 0x0c`. That does not make this method a real text-wrapper today, and final source should avoid names such as `MeasureWrappedTextLines` unless the helper itself is later reconstructed as real wrapping. The source-facing role remains a line-count virtual because callers use the return in chat layout calculations and the call shape preserves text, length, context, and width.

[UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) preserves `m_text` by constructor duplication, so cloned chat-list entries measure the same text after the temporary/source message has been destroyed. This measure method does not consume the color/custom fields that clone also preserves for draw.

The duplicated length scan and duplicated helper call are real byte-level shape, not a decompiler artifact. The first helper result is compared as a signed 16-bit value against `1`; if it is less than one, the method returns `1`. Otherwise the second helper result is returned after zero-extending the low 16 bits. The first-draft source intentionally repeats the scan and helper call to preserve this shape.

## IDA MCP Evidence

- Live IDA function lookup reports a modeled function at `0x00483630` with size `0x6a`; `0x0048362b` and `0x0048369a` are not function starts, and the next modeled function begins at `0x004836a0`.
- `xrefs_to 0x00483630` reports vtable data entry `0x0061527c`; there are no ordinary direct callers.
- Live callee analysis reports direct calls to text measurement helper `0x004bb070`.
- Direct helper call sites are `0x00483658` and `0x0048368c`; both belong to this method and both use the same `ecx` receiver plus `text`, `length`, `width` stack-argument shape.
- Byte reads confirm five leading `0xcc` bytes, the `0x6a`-byte line-measure body, and six trailing `0xcc` bytes.
- Live disassembly/decompilation confirm object text pointer offset `+0x04`, UTF-16 length scanning, use of the width argument, calls to `0x004bb070`, minimum result `1`, and `ret 8`.
- 2026-06-19 B002 accepted [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md) implementation identifies `0x004bb070` as `GetSingleLineCount`, with binary type `int __thiscall GetSingleLineCount(void *context, const wchar_t *text, int length, int width)`. This method is one of the two vtable-backed caller families: direct calls at `0x00483658` and `0x0048368c`, vtable reachability through `0x0061527c`, owned text pointer at message field `+0x04`, hidden context in `ecx`, and explicit stack arguments `text`, `length`, and `width`.

## Heuristic And Rejected Alternatives

| Issue | Best current conclusion | Rejected alternatives |
| --- | --- | --- |
| Method name | `MeasureLines`, matching the chat layout role and existing class/file docs. | `GetTextLength`, `DrawTextMeasure`, and `CountWrappedLines`; they either describe the wrong work or overstate real wrapping. |
| Text field | `m_text` at `+0x04`; constructor allocates/copies it, destructor frees it, clone deep-copies through the constructor, and draw/measure read it as UTF-16. | Borrowed external pointer, `m_label`, `m_message`, `m_pText`, or generated `mbr_0x4` as final names. |
| First argument | Draw/context receiver, best current source-facing type `void *drawContext` because callers pass the pane object and the helper page records the binary as `void *context`; `GrafPort *` or a narrower draw-context base can replace it only after a declaration pass proves that relationship. | Width, event pointer, padding, truly unused argument, or a proven `GrafPort *` type without inheritance/interface evidence. |
| Second argument | Pixel width used by chat layout, observed as `783` and `408` in current/old layout callers. | Context pointer, color id, or line height. |
| Helper behavior | Current helper is constant-one `GetSingleLineCount`; the caller remains a line-count interface. | Private `ColorStringChattingMessage` helper, `ColorStringSystemMessage`-only helper, or actual wrapping helper. |
| Return type | Source `int` with effective 16-bit line-count values. | Formal `short` return only, or unsigned-only interpretation. |
| Split/range | Keep exact `0x00483630-0x0048369a`; surrounding bytes are padding and helper is separate. | Absorb leading/trailing `0xcc` or split out the duplicate scan/call. |

## Score Rationale

Completion is raised to `86` because the Rule 26 pass resolves the two-argument signature, argument order, `m_text` field role, helper call sites, duplicated scan/call semantics, minimum-one-line clamp, current constant-helper caveat, rejected alternatives, and first-draft C++ for this exact method. Confidence remains `90`; remaining uncertainty is exact original helper owner/name and whether the final source type should stay as `void *`, tighten to a broader pane/draw-context type, or eventually prove a `GrafPort *`-compatible interface.

## Cross-References

- [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md)
- [UID:0002GH][0x004836a0-0x004839c0.ColorStringChattingMessageDrawWithTables](by-memory/0x004836a0-0x004839c0.ColorStringChattingMessageDrawWithTables.md)
- [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md)

## Changes

- 2026-06-19 B002 accepted [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md) source-quality implementation:
  - Score unchanged at `84/90`.
  - Summary/evidence: updated the helper call from raw `0x004bb070` wording to `GetSingleLineCount`, recorded that `ecx` carries the ignored pane/context pointer while stack arguments are text, UTF-16 length, and width, and preserved the caller source shape as scan text length, call shared helper, and clamp to at least one display line. This page intentionally does not collapse the method to `return 1;` even though the current helper body returns one, because the same callback interface is also used by `ColorStringSystemMessage::GetLineCount`.
- 2026-06-21 B006 Rule 26 support update: aligned the text field name to `m_text` and recorded the [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) relationship: clone duplicates `m_text` through the constructor so list entries can measure independently after temporary message destruction.
- 2026-06-21 supervisor closeout of B011 `0002GG` report:
  - Score unchanged at `86/90`.
  - Summary/evidence: corrected the first-draft C++ and source-facing signature from the earlier provisional `GrafPort *drawContext` to safer `void *drawContext`. The direct chat callers pass the pane object as the first virtual argument, [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md) records the binary context parameter as `void *`, and no current declaration proof establishes a `GrafPort *` relationship for `ChattingPane::this`. All existing behavior, range, helper-call, clamp, ownership, and score evidence remains valid.
- 2026-06-21 B006 Rule 26 measure-lines incorporation:
  - Score changed from `84/90` to `86/90`.
  - Summary/evidence: populated first-draft `ColorStringChattingMessage::MeasureLines` C++; preserved exact `0x00483630-0x0048369a` range, five-byte leading and six-byte trailing padding, vtable slot `0x0061527c`, direct helper calls at `0x00483658` and `0x0048368c`, `ret 8` two-argument signature, first post-`this` argument forwarded in `ecx`, second post-`this` argument used as width, owned `m_text` UTF-16 pointer at `+0x04`, duplicated scans/calls, signed 16-bit clamp to at least one line, zero-extended 16-bit second result, and constant-one helper caveat. Rejected width-only, true wrapping, private helper, borrowed text, and generated field-name interpretations.
- 2026-06-05: Refreshed with live IDA evidence, raised to `84/90`, attached to `UID:00002Z`, removed the below-threshold C++ sketch, and replaced raw helper labels with address-only evidence.
