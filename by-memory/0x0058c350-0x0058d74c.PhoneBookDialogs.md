*** UID:0001JE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000MJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000MJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Phone-book dialog/list/entry block with IDA boundaries, vtable-family placement, helper callsite evidence, parent attachment, source-tree placement, and boundary padding documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0058c350-0x0058d74c PhoneBook Dialogs

## Status

- Confidence: strong for phone-book dialog/list/entry ownership, IDA function boundaries, vtable-family placement, parent source placement, selected-entry helper ownership, retained no-direct-xref helper inventory, raw helper endpoints, and generated owner-pollution rejection; medium-high for exact original helper names, inherited list/dialog API names, and final render/save child C++.
- Covered file/module: [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md)
- Parent attachment: [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md) is currently `87/86`, and this child is now `88/89`; attaching under the file parent remains justified.
- Current generated-source state: current validator output for `NexusTK/login/PhoneBookDialog.cpp` emits [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) plus empty markers for this aggregate, the classes, vtables, resources, and DialDialog children. It is an output check, not source authority for the missing bodies.
- C++ reconstruction: aggregate stays blank by design. This page spans multiple source methods, multiple classes, local switch data, padding, retained no-direct-xref helpers, and the existing exact emitted child [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md). Exact child pages should carry future method C++ so the aggregate does not duplicate child bodies or emit outside a method scope.

## Covered Ranges

Ranges are half-open unless a linked exact child uses the historical inclusive filename convention.

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x0058c350-0x0058c9b5` | `PhoneBookDialog::PhoneBookDialog` | Builds phone-book UI, installs `PhoneBookDialog` and embedded `PhoneBookListPane` vtables, loads alternating name/number lines from `PHONE.CFG`, and creates the `DLGTEL1.EPD` resource-backed dialog. |
| `0x0058c9b5-0x0058c9c0` | padding | Eleven `0xcc` bytes. |
| `0x0058c9c0-0x0058ca63` | retained `PhoneBookDialog::AppendPhoneBookEntry` / `AddPhoneBookEntry` wrapper | No direct rel32 or absolute refs to the start. The source-facing role is inferred from building a `PhoneBookContactEntry` from flag/name/number arguments and inserting it through the dialog-owned list, then refreshing dialog state. |
| `0x0058ca63-0x0058ca70` | padding | Thirteen `0xcc` bytes. |
| `0x0058ca70-0x0058cb2e` | retained `PhoneBookDialog::ReplaceSelectedPhoneBookEntry` wrapper | No direct rel32 or absolute refs to the start. Behavior is selected-row removal/replacement plus rebuilt row insertion/update through the list pane. |
| `0x0058cb2e-0x0058cb30` | padding | Two `0xcc` bytes. |
| `0x0058cb30-0x0058cb5d` | retained `PhoneBookDialog::RemoveSelectedPhoneBookEntry` / `DeleteSelectedEntry` wrapper | No direct rel32 or absolute refs to the start. It removes the selected row through list helper flow and jumps to the dialog/list refresh path. |
| `0x0058cb5d-0x0058cb60` | padding | Three `0xcc` bytes. |
| `0x0058cb60-0x0058cc37` | `PhoneBookDialog::DialSelectedEntry` | Direct refs at `call@0x0058cdf4` and `jmp@0x0058d116`. Saves/closes phone book and opens `DialDialog` for the selected number. |
| `0x0058cc40-0x0058cccc` | `PhoneBookDialog::UpdateButtonStates` | Enables edit/delete/dial based on list contents. |
| `0x0058ccd0-0x0058ce15` | `PhoneBookDialog::OnCommand` | Handles save/close/add/edit/delete/dial commands. The dispatcher uses a local switch table at `0x0058ce18-0x0058ce30`. |
| `0x0058ce15-0x0058ce18` | alignment | Three-byte `0f 1f 00` nop after `OnCommand`. |
| `0x0058ce18-0x0058ce30` | `PhoneBookDialog::OnCommand` local switch table | Six dwords: `0x0058cd12`, `0x0058cd19`, `0x0058cd2a`, `0x0058cd65`, `0x0058cdc9`, and `0x0058cdf4`. This is local command-dispatch data, not generic padding. |
| `0x0058ce30-0x0058cec7` | `PhoneBookDialog::SavePhoneBook` | Writes `PHONE.CFG` in `wt` mode as alternating name and number lines, calling the CRT/runtime formatted-output wrapper at `0x0058ce94` and `0x0058cea6`; emitted source currently lacks the body, but IDA confirms iteration/write behavior. |
| `0x0058cec7-0x0058ced0` | padding | `0xcc` bytes after `SavePhoneBook`. |
| `0x0058ced0-0x0058cf1d` | retained `PhoneBookListPane::Initialize` / setup helper | No direct rel32 refs to the start. Vptr stores at `0x0058cefe`, `0x0058cf04`, and `0x0058cf0e` install the same three `PhoneBookListPane` vtables used by the embedded constructor path. |
| `0x0058cf1d-0x0058cf20` | padding | Three `0xcc` bytes. |
| `0x0058cf20-0x0058cf9a` | retained `PhoneBookListPane::AppendEntry` / `AddEntry` | No direct rel32 refs to the start. This is the list-side row builder and insert helper. |
| `0x0058cf9a-0x0058cfa0` | padding | Six `0xcc` bytes. |
| `0x0058cfa0-0x0058cfdc` | [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) | Source-bearing `PhoneBookListPane::GetSelectedEntryData`; B001 2026-06-17 resolved the direct owner as [UID:0000AE][PhoneBookListPane](by-class/PhoneBookListPane.md), rejecting generated chat/article owner pollution. |
| `0x0058cfdc-0x0058cfe0` | padding | Four `0xcc` bytes after [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md). |
| `0x0058cfe0-0x0058d075` | retained `PhoneBookListPane::ReplaceSelectedEntryData` helper | No direct xref to start; builds a `PhoneBookContactEntry` from flag/name/number arguments, copies the wide strings with bounded helpers, removes/replaces the selected row, and reinserts through shared `ListPane` storage. The `ret 0x0c` includes byte `0x0058d074`, so the half-open end is `0x0058d075`. |
| `0x0058d075-0x0058d080` | padding | Eleven `0xcc` bytes. |
| `0x0058d080-0x0058d08e` | retained remove-current helper | No direct xref to start; tiny wrapper around the shared list remove helper for the current row. |
| `0x0058d08e-0x0058d090` | padding | Two `0xcc` bytes. |
| `0x0058d090-0x0058d10e` | retained phone-book list save helper | No direct xref to start; opens `PHONE.CFG` in `wt`, iterates list entries, calls the selected/item accessor path, and calls [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) twice per row for name/number. |
| `0x0058d10e-0x0058d110` | padding | Two bytes before `PhoneBookListPane::DialSelectedEntry`. |
| `0x0058d110-0x0058d11b` | `PhoneBookListPane::DialSelectedEntry` | Forwards selected-entry activation to the owner dialog. Existing inclusive filename row ends at `0x0058d11a`. |
| `0x0058d11b-0x0058d120` | padding | Five `0xcc` bytes. |
| `0x0058d120-0x0058d263` | `PhoneBookListPane::RenderEntry` | Draws selected highlight plus name/number columns; emitted source currently lacks the body. Existing inclusive filename row ends at `0x0058d262`. |
| `0x0058d263-0x0058d270` | padding | Thirteen `0xcc` bytes. |
| `0x0058d270-0x0058d536` | `PhoneEntryDialog::PhoneEntryDialog` | Builds add/edit dialog with name and number fields. |
| `0x0058d540-0x0058d59f` | `PhoneEntryDialog::UpdateButtonState` | Enables OK only when both text fields are populated. Existing inclusive filename row ends at `0x0058d59e`. |
| `0x0058d5a0-0x0058d74c` | `PhoneEntryDialog::OnDialogAction` | Applies add/edit to the owner list and closes. |

## Source-Quality Split Policy

[UID:0001JE] is source-bearing, but not one source body. It remains attached to [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md) as the `PhoneBookDialog.cpp` source-family aggregate because several source-bearing subranges are not yet exact child pages. Formal aggregate C++ stays blank by design: emitting method bodies here would duplicate [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) or place multiple class methods outside their exact scopes.

Exact child pages should carry method C++ for `PhoneBookDialog::SavePhoneBook`, `PhoneBookDialog::DialSelectedEntry`, `PhoneBookDialog::UpdateButtonStates`, the retained dialog/list wrappers, `PhoneBookListPane::DialSelectedEntry`, `PhoneBookListPane::RenderEntry`, `PhoneEntryDialog::UpdateButtonState`, and `PhoneEntryDialog::OnDialogAction`. If every source-bearing subrange later receives an exact emitting child, this aggregate can be reconsidered as a non-emitting container.

The retained no-direct-xref helpers are source-authored retained methods, not padding and not compiler glue. They have normal prologs/epilogs, stack-cookie patterns, phone-book row construction, list/string helper calls, and feature-specific behavior. The lack of direct callsites is best explained as unreferenced member/helper code retained from the object file.

## Direct Caller Inventory

| Target | Direct rel32 refs found | Interpretation |
| --- | --- | --- |
| `0x0058c9c0` | none | Retained dialog add/append wrapper; no live callsite found. |
| `0x0058ca70` | none | Retained dialog replace/update wrapper; no live callsite found. |
| `0x0058cb30` | none | Retained dialog remove/delete wrapper; no live callsite found. |
| `0x0058cb60` | `call@0x0058cdf4`, `jmp@0x0058d116` | `OnCommand` dial case and `PhoneBookListPane::DialSelectedEntry` forwarder. |
| `0x0058ce30` | `call@0x0058cbdd`, `call@0x0058cd12` | `DialSelectedEntry` saves before dialing; `OnCommand` save/close case. |
| `0x0058ced0` | none | Retained list setup helper; no live callsite found. |
| `0x0058cf20` | none | Retained list append/add helper; no live callsite found. |
| `0x0058cfa0` | `call@0x0058cd8c` | Edit path obtains selected name/number; direct owner [UID:0000AE][PhoneBookListPane](by-class/PhoneBookListPane.md). |
| `0x0058cfe0` | none | Retained list replace helper; no live callsite found. |
| `0x0058d080` | none | Retained list remove helper; no live callsite found. |
| `0x0058d090` | none | Retained list save helper; no live callsite found. |
| `0x0058d270` | `call@0x0058cd5b`, `call@0x0058cdc2` | Add/edit command cases construct `PhoneEntryDialog`. |
| `0x0058d750` | `call@0x0058cc17` | `PhoneBookDialog::DialSelectedEntry` constructs `DialDialog`. |
| `0x0058dcb0` | `call@0x0058ce94`, `call@0x0058cea6`, `call@0x0058d0db`, `call@0x0058d0ed` | Two modeled `SavePhoneBook` writes and two retained list-save writes; still CRT/runtime wrapper. |

Absolute-reference scan notes: no abs32 refs were found to `0x0058cfa0`, `0x0058cfe0`, `0x0058d080`, `0x0058d090`, `0x0058d750`, or `0x0058dcb0`. `0x0058d110` appears at `0x0062dc60` and `0x0058d120` at `0x0062dc64`, proving the `PhoneBookListPane` activation and render vtable slots. `0x0062db48`, `0x0062dbe4`, and `0x0062dd44` appear as constructor/setup vptr stores in `.text`, matching the vtable child docs.

## Source-Facing Types And Names

`PhoneBookContactEntry` remains the best source-facing row type:

```cpp
struct PhoneBookContactEntry
{
    unsigned char entryFlags;
    wchar_t name[64];
    wchar_t phoneNumber[64];
};
```

`entryFlags` is deliberately neutral. Checked producer paths write zero and no checked consumer decodes nonzero bits, so names such as `status`, `favorite`, `deleted`, or `selected` are too specific.

The strongest descriptive helper/member names are:

| Item | Source-facing name | Confidence |
| --- | --- | --- |
| `0x004f3bd0` | `ListPane::GetItemCount` / `GetEntryCount` | Descriptive, shared-list API name still not original-proof. |
| `0x004f3dc0` | `ListPane::GetItemAt` / `GetEntryAt` | Descriptive, shared-list API name still not original-proof. |
| list panes `this+0x134` | selected row index | Strong role, final member spelling open. |
| `PhoneBookListPane+0x14c` | `m_ownerDialog` | Strong role from constructor/setup writes and activation forwarding. |
| `PhoneBookDialog+0x1fc` | `m_phoneBookList` / embedded phone-book list pointer | High-probability role from list creation and command paths. |
| `PhoneEntryDialog+0x26c` | `m_ownerDialog` | Strong role from constructor store and OK action path. |
| `PhoneEntryDialog+0x270` | `m_addMode` / `m_isNewEntry` | Strong role from OK action append-versus-replace branch. |

## First-Draft C++ Readiness

Do not paste any of these bodies into this aggregate's formal C++ block. They are source-quality sketches for future exact child pages or accepted class/method pages.

```cpp
void PhoneBookDialog::SavePhoneBook()
{
    FILE *file = _wfopen(L"PHONE.CFG", L"wt");
    if (file == NULL)
        return;

    const int count = m_phoneBookList->GetEntryCount();
    for (int index = 0; index < count; ++index)
    {
        const PhoneBookContactEntry *entry =
            static_cast<const PhoneBookContactEntry *>(m_phoneBookList->GetEntryAt(index));

        fwprintf(file, L"%s\n", entry->name);
        fwprintf(file, L"%s\n", entry->phoneNumber);
    }

    fclose(file);
}
```

```cpp
void PhoneBookDialog::DialSelectedEntry()
{
    wchar_t name[64];
    wchar_t phoneNumber[64];

    m_phoneBookList->GetSelectedEntryData(name, _countof(name),
                                          phoneNumber, _countof(phoneNumber));
    SavePhoneBook();
    Close();

    new DialDialog(g_pTerminalPane, phoneNumber);
}
```

```cpp
void PhoneBookDialog::UpdateButtonStates()
{
    const bool hasEntries = (m_phoneBookList->GetEntryCount() > 0);

    EnableControl(kPhoneBookEditButton, hasEntries);
    EnableControl(kPhoneBookDeleteButton, hasEntries);
    EnableControl(kPhoneBookDialButton, hasEntries);
}
```

```cpp
void PhoneBookDialog::AppendPhoneBookEntry(
    unsigned char entryFlags,
    const wchar_t *name,
    const wchar_t *phoneNumber)
{
    m_phoneBookList->AppendEntry(entryFlags, name, phoneNumber);
    UpdateButtonStates();
}

void PhoneBookDialog::ReplaceSelectedPhoneBookEntry(
    unsigned char entryFlags,
    const wchar_t *name,
    const wchar_t *phoneNumber)
{
    m_phoneBookList->ReplaceSelectedEntryData(entryFlags, name, phoneNumber);
    UpdateButtonStates();
}

void PhoneBookDialog::RemoveSelectedPhoneBookEntry()
{
    m_phoneBookList->RemoveSelectedEntry();
    UpdateButtonStates();
}

void PhoneBookListPane::DialSelectedEntry()
{
    m_ownerDialog->DialSelectedEntry();
}
```

```cpp
void PhoneEntryDialog::UpdateButtonState()
{
    const bool hasName = (m_nameEdit->GetTextLength() > 0);
    const bool hasNumber = (m_numberEdit->GetTextLength() > 0);

    EnableControl(kOkButton, hasName && hasNumber);
}

void PhoneEntryDialog::OnDialogAction(int controlId)
{
    if (controlId == kOkButton)
    {
        wchar_t name[64];
        wchar_t phoneNumber[64];

        m_nameEdit->GetText(name, _countof(name));
        m_numberEdit->GetText(phoneNumber, _countof(phoneNumber));

        if (m_addMode)
            m_ownerDialog->AppendPhoneBookEntry(0, name, phoneNumber);
        else
            m_ownerDialog->ReplaceSelectedPhoneBookEntry(0, name, phoneNumber);

        m_ownerDialog->UpdateButtonStates();
        Close();
        return;
    }

    if (controlId == kCancelButton)
        Close();
}
```

`PhoneBookListPane::RenderEntry` is behavior-ready but still helper-name-capped for final audit. The method reads `PhoneBookContactEntry::name` and `phoneNumber`, draws selected-row highlight when selected, truncates each column with ellipsis to fit the name and number column widths, and uses palette/text drawing helpers rather than [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md).

## Rejected Alternatives

- Fold into `TerminalPane.cpp`: plausible historically because this is terminal/modem UI, but weaker than the current source-tree split, resource grouping, vtable-family split, and `PhoneBookDialog.cpp` file page.
- Move `PhoneBookListPane` into a generic list file: rejected because row fields, owner pointer, vtables, resources, and callers are phone-book specific.
- Treat [UID:0001JE] as no-owner/non-emitting now: rejected because it still documents unsplit source-bearing code in [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md).
- Keep compiler-generated or `sub_`/`dword` names for retained wrappers: rejected because the bodies are source-like and feature-specific.
- `ChattingColorListPane` and `ArticleListPane` ownership around this block: rejected as generated owner pollution around shared list helpers.
- [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) as a PhoneBook helper: rejected; it is CRT/runtime `fwprintf`-style glue and reconstructed callers should use normal CRT formatted output.

## IDA MCP Evidence

Checked on 2026-05-23:

- IDA confirms the phone-book constructor, command handlers, list handlers, and phone-entry handlers as real function starts.
- IDA shows `DialDialog::DialDialog` begins immediately after this block at `0x0058d750`.
- 2026-05-28 IDA MCP recheck confirms `sub_58D5A0` ends at `0x0058d74c`; `0x0058d74c-0x0058d750` is `0xcc` alignment before `DialDialog`.
- 2026-05-26 recheck: `xrefs_to 0x0058cfa0` reports only the `PhoneBookDialog::OnCommand` call at `0x0058cd8c`.
- 2026-05-26 recheck: [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) is a small formatted wide write helper used by `SavePhoneBook` and two raw code refs later identified as a no-IDA-function save helper.
- 2026-06-12 A001 Batch 289 live IDA MCP reconfirmed `PhoneBookDialog::PhoneBookDialog` vtable stores at `0x0058c399`, `0x0058c3a5`, and `0x0058c3b2`, the `PhoneBookListPane` successor vtable at `0x0062dbe4`, the six-way `OnCommand` switch at `0x0058cd0b`, `SavePhoneBook` write-mode `PHONE.CFG` loop, `DialSelectedEntry` handoff to `DialDialog`, and `UpdateButtonStates` slot `0x0062db94 -> 0x0058cc40`.
- Older Wave3/generated metadata assigned the selected-entry text helper to `ChattingColorListPane`; current `simroot_v2/class_PhoneBookListPane.cpp` has since promoted `0x0058cfa0` to `PhoneBookListPane`, but the stale/polluted cast remains in `PhoneBookDialog::OnCommand`.
- 2026-06-17 B001 source-quality pass confirms [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) as direct `PhoneBookListPane::GetSelectedEntryData`, resolves the row type as `PhoneBookContactEntry { unsigned char entryFlags; wchar_t name[64]; wchar_t phoneNumber[64]; }`, and reclassifies [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) as CRT/runtime `fwprintf`-style wrapper glue. The `0x0058d0db` and `0x0058d0ed` raw calls are in the raw save-helper span `0x0058d090-0x0058d10e`, not in `PhoneBookListPane::RenderEntry`.

Corroborating documentation:

- [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md) documents `login/PhoneBookDialog.cpp` as the owning source module, with parent score `86/80` and projected path `NexusTK/login/`.
- [UID:00026N][0x0062da10-0x0062df94.TerminalPhoneReadOnlyData](by-memory/0x0062da10-0x0062df94.TerminalPhoneReadOnlyData.md) places the `PhoneBookDialog`, `PhoneBookListPane`, and `PhoneEntryDialog` vtable families at `0x0062db48-0x0062dd44`, immediately after the terminal vtable island and before `DialDialog`.
- [UID:0000V7][PhoneBookListHelpers_58cfa0_58dcb0](by-item/PhoneBookListHelpers_58cfa0_58dcb0.md) records the focused helper audit: `0x0058cfa0` has exclusive end `0x0058cfdc`, one direct caller at `0x0058cd8c`, and copies the selected contact fields; `0x0058dcb0` has modeled calls from `SavePhoneBook` and raw no-function save-helper calls at `0x0058d0db`/`0x0058d0ed`.
- [UID:0001R1][proposed-source-tree](by-project-structure/proposed-source-tree.md) places `PhoneBookDialog`, `PhoneBookListPane`, `PhoneEntryDialog`, and `DialDialog` as the terminal phone-book/modem dialing flow under `login/PhoneBookDialog.cpp`.
- [UID:0001JG][0x0058d750-0x0058dc5b.DialDialog](by-memory/0x0058d750-0x0058dc5b.DialDialog.md) starts immediately after the confirmed `0x0058d74c-0x0058d750` alignment gap, and its constructor is called by `PhoneBookDialog::DialSelectedEntry`.

## Boundary Notes

- Start boundary: `0x0058c350`, immediately after [UID:0001JB][0x0058af50-0x0058c350.TerminalPaneAndSetup](by-memory/0x0058af50-0x0058c350.TerminalPaneAndSetup.md), whose final `TerminalSetupPane::OnCommand` function ends at the same exclusive address.
- End boundary: IDA reports `PhoneEntryDialog::OnDialogAction` ending at `0x0058d74c`.
- Padding: [UID:0000VN][-ignored](by-memory/-ignored.md) records the confirmed `0x0058d74c-0x0058d750` four-byte `0xcc` alignment gap before `DialDialog`.

## Ownership Decision

This is a coherent terminal phone-book block and should migrate under `login/PhoneBookDialog.cpp` or an equivalent terminal-phone source file. [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) is now direct `PhoneBookListPane` class logic inside that source family. Do not migrate it as chat/color-list or article-list code. [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) is runtime/CRT glue and should not be emitted as PhoneBook source.

## Cross-References

- [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md)
- [UID:0000AD][PhoneBookDialog](by-class/PhoneBookDialog.md)
- [UID:0000AE][PhoneBookListPane](by-class/PhoneBookListPane.md)
- [UID:0000AF][PhoneEntryDialog](by-class/PhoneEntryDialog.md)
- [UID:0000V7][PhoneBookListHelpers_58cfa0_58dcb0](by-item/PhoneBookListHelpers_58cfa0_58dcb0.md)
- [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md)
- [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md)
- [UID:0001JG][0x0058d750-0x0058dc5b.DialDialog](by-memory/0x0058d750-0x0058dc5b.DialDialog.md)
- [UID:00026N][0x0062da10-0x0062df94.TerminalPhoneReadOnlyData](by-memory/0x0062da10-0x0062df94.TerminalPhoneReadOnlyData.md)
- [UID:0001R1][proposed-source-tree](by-project-structure/proposed-source-tree.md)

## Changes

- 2026-05-28: range changed from `0x0058c350-0x0058d74b` to `0x0058c350-0x0058d74c`; `PhoneEntryDialog::OnDialogAction` changed from `0x0058d5a0-0x0058d74b` to `0x0058d5a0-0x0058d74c`.
  - Summary/evidence: IDA MCP reports `sub_58D5A0` bounds as `0x0058d5a0-0x0058d74c`, followed by four `0xcc` alignment bytes before `DialDialog::DialDialog` at `0x0058d750`.
- 2026-06-01: raised from `50/65` to `84/84`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000MJ][PhoneBookDialog](by-file/PhoneBookDialog.md).
  - Summary/evidence: the parent file is `86/80` and now has a valid `NexusTK/login/` projected path; existing IDA-backed docs confirm the function block, helper callsites, vtable families, boundary padding, source-tree placement, and dial-dialog handoff.
  - Remaining gaps: no aggregate-level reconstructed C++ because the final list-base API names and `SavePhoneBook`/`RenderEntry` handwritten bodies were not source-quality at the time.
- 2026-06-12 Agent-A001 Batch 289: Raised `84/84 -> 85/86` after live IDA MCP revalidated the PhoneBookDialog constructor/command/save/dial functions, [UID:0003DQ][0x0062db48-0x0062dbe4.PhoneBookDialogVtableData](by-memory/0x0062db48-0x0062dbe4.PhoneBookDialogVtableData.md), and the PhoneBookListPane successor boundary. Aggregate-level C++ remained blank pending source-quality method/field names.
- 2026-06-17 B001 source-quality execution:
  - Resolved [UID:0001JF][0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper](by-memory/0x0058cfa0-0x0058cfdb.PhoneBookSelectedEntryHelper.md) as direct `PhoneBookListPane::GetSelectedEntryData` with `PhoneBookContactEntry` row layout and first-draft C++ in the exact child page.
  - Reclassified [UID:0001JL][0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper](by-memory/0x0058dcb0-0x0058dcd3.PhoneBookFormattedWriteHelper.md) as CRT/runtime `fwprintf`-style glue, with PhoneBook callers expected to use normal formatted file output.
  - Added raw helper inventory for `0x0058cfdc-0x0058d110` and corrected the stale raw render-neighborhood wording to the no-function save helper at `0x0058d090-0x0058d10e`.
