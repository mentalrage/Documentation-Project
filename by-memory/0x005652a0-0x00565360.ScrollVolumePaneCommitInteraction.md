*** UID:0001H4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:170 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollVolumePane::CommitInteraction()
{
    Point cursor;
    Point origin;
    RectBounds partRect;
    int localY;
    int localX;
    ScrollVolumePart activePart;

    g_pEventMan->GetCursorPosition(&cursor);
    if (cursor.x != -1000 || cursor.y != -1000) {
        GetScreenOffset(&origin);
        localY = cursor.y - origin.y;
        localX = cursor.x - origin.x;

        activePart = static_cast<ScrollVolumePart>(m_activePart);
        if (activePart == kScrollVolumePartThumb) {
            NotifyValueChange(localY, localX);
        } else if (activePart != kScrollVolumePartNone &&
                   HitTestPart(localY, localX) == activePart) {
            NotifyValueChange(localY, localX);
        }
    }

    if (m_hoverPart != kScrollVolumePartNone) {
        GetPartRect(static_cast<ScrollVolumePart>(m_hoverPart), &partRect);
        InvalidateRect(&partRect);
        m_hoverPart = kScrollVolumePartNone;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: ScrollVolumePane CommitInteraction converts cursor to local Y/X, dispatches active thumb/page value changes, and clears stale hover state; exact callers, raw-call liveness context, class routing, and formal C++ are resolved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

## 2026-07-26 Accepted Coordinate, Hover, And Routing Repair

- Direct semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), position 170; UID0001H2 is only the non-emitting address index.
- Exact modeled range remains `[0x005652a0,0x00565360)`, size `0xc0`, with callers at `0x00564a83`, `0x00564b28`, and retained raw BeginInteraction call `0x00565272`.
- Cursor conversion now follows current Point and rectangle conventions: `localY=cursor.y-origin.y`, `localX=cursor.x-origin.x`, then `HitTestPart(localY,localX)` and `NotifyValueChange(localY,localX)`.
- Thumb active state always dispatches value calculation. Non-thumb active state dispatches only while the current hit part matches. Any old hover rectangle is invalidated and `m_hoverPart` is cleared afterward.
- Preserve the absent direct route to the retained BeginInteraction helper as liveness context. Rejected alternatives include swapped X/Y calls, vertical geometry, historical/superseded `m_highlightPart`, focus-loss ownership, aggregate emission, and moving owner policy into this helper.


# 0x005652a0-0x00565360 ScrollVolumePane Commit Interaction Helper

## Status

- Confidence: very strong for function boundary, caller/callee evidence, ScrollVolumePane ownership, interaction behavior, and first-draft C++; exact original helper spelling remains descriptive.
- Historical pre-callback generated snapshot: `source-3/simroot_v2/ui/controls/class_ScrollVolumePane.cpp` emitted this body with stale signature `void TextEditPane::HandleScrollbarMouseF()`. Current ordinary documentation and formal C++ use `ScrollVolumePane::CommitInteraction`; renewed supervisor generated-output verification remains pending.
- Direct owner/emitter: [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), with [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md) as source root.
- Role: processes the current slider interaction for immediate and repeated pointer/timer paths, then clears hover state.
- Address index: [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md); this body emits directly at class position `170`.
- Rebuild handling: source-authored helper code. B014 class-level name/field resolution removes the previous no-code blocker; remaining exact spelling uncertainty is documented as a confidence cap rather than a reason to keep the body blank.

## Address Range

| Range | Role | Notes |
| --- | --- | --- |
| `0x005651e0-0x005652a0` | previous raw begin-interaction helper | [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md) calls this helper at `0x00565272`, but IDA still does not model the raw start as a function. |
| `0x005652a0-0x00565360` | this helper | Live IDA MCP on 2026-06-16 reports `sub_5652A0`, size `0xc0`. |
| `0x00565360-0x00565488` | next value-change helper | [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md), IDA `sub_565360`, size `0x128`. |

## Behavior

1. Reads the current input/cursor point through `dword_67A754` using the cursor helper at `0x004a9090`.
2. Fetches the pane origin with `0x005447c0`, then subtracts that origin from the cursor point to convert the coordinates into the pane-local system.
3. Treats `(-1000, -1000)` as the no-position sentinel only when both coordinates match the sentinel value.
4. If the active interaction part at `this+0x104` is part `2` (the thumb), dispatches directly to [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md).
5. Otherwise hit-tests the local point with [UID:0001H3][0x00564e30-0x00565006.ScrollVolumePaneHitTestPart](by-memory/0x00564e30-0x00565006.ScrollVolumePaneHitTestPart.md); only a result matching `this+0x104` is allowed to notify the value-change helper.
6. Clears any stale highlighted part at `this+0x103`: if the byte is not `0xff`, it computes that part's rectangle through `0x00565010`, invalidates it through the pane vtable slot at `+0x20`, then writes `0xff` back to `this+0x103`.
7. B001's 2026-06-19 [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md) pass resolves the notify call as source-facing `NotifyValueChange(int mouseY, int mouseX)`. This commit helper preserves two-coordinate call symmetry when forwarding the local cursor point, but the horizontal ScrollVolumePane value math uses the second coordinate (`mouseX`) and the second/+4 drag-offset component.

## Touched State And Calls

| Item | Use |
| --- | --- |
| `dword_67A754` | Input/cursor source passed into `0x004a9090`. Existing [UID:00004O][EventMan](by-class/EventMan.md) documentation names that callee `EventMan::GetCursorPosition`. |
| `(-1000, -1000)` | No-position sentinel. The helper skips coordinate normalization and notification only when both values match. |
| `this+0x103` | Highlighted part byte; cleared to `0xff` after invalidating the old highlighted rectangle. |
| `this+0x104` | Active interaction part byte; compared with part `2` for thumb drags and with the hit-test result for part-click commits. |
| `0x00564e30` | Hit-test helper for non-thumb part commits. |
| `0x00565010` | Part-rectangle helper used before stale-highlight invalidation. |
| `0x00565360` | Value-change notification helper reached for thumb drags or matching part clicks. |
| `0x004a9090` | Cursor/input point reader; live decompilation copies `this+4` and `this+8` into an output point. |
| `0x005447c0` | Pane/screen origin helper used for local coordinate conversion; live decompilation returns a `0x004f14a0`-derived point when parent/view state is present, otherwise zero. Final source name unresolved. |
| vtable slot `+0x20` | Invalidation call for the stale highlighted part rectangle. |
| `@__security_check_cookie@4` | Compiler security epilogue check, not game logic. |

## Evidence Notes

- IDA MCP lookup on 2026-05-25 reports `0x005652a0` as `sub_5652A0` size `0xc0`.
- IDA caller checks report calls from current `ScrollVolumePane::HandlePointerOrMouseEvent` at `0x00564a83` and `ScrollVolumePane::OnTimer` at `0x00564b28`; historical `OnMouseEvent` and `OnLoseFocus` labels are superseded.
- IDA callee checks show calls to [UID:0001H3][0x00564e30-0x00565006.ScrollVolumePaneHitTestPart](by-memory/0x00564e30-0x00565006.ScrollVolumePaneHitTestPart.md), `GetPartRect` at `0x00565010`, [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md), cursor/screen-position helpers, and a primary vtable invalidation slot.
- 2026-05-26 IDA MCP recheck reports the same size, direct callers, and callee chain.
- The helper reads the adjusted cursor position, checks active interaction part at `+0x104`, dispatches value-change logic for thumb drag or part clicks, and clears a highlighted part at `+0x103` after invalidating its rectangle.
- 2026-05-31 IDA decompilation review confirms the active-part branch, notify calls for thumb or clicked parts, and highlight clear/invalidation path. The raw caller at `0x005651e0-0x005652a0` is now documented separately.
- 2026-05-30 source-map/source check: `class_ScrollVolumePane.cpp.source_map.json` maps method `0x005652a0` to `class_ScrollVolumePane.cpp`, while the C++ body at line 502 still uses a stale `TextEditPane` owner/signature. Keep this page under `ScrollVolumePane` unless a later IDA pass overturns the existing caller/field evidence.
- 2026-06-07 live IDA MCP recheck reports `0x005652a0` as `sub_5652A0` size `0xc0`, callers from `0x00564a83` in `sub_564910` and `0x00564b28` in `sub_564B10`, and callees `sub_565010`, `sub_5447C0`, `sub_564E30`, `sub_4A9090`, `sub_565360`, and `@__security_check_cookie@4`.
- 2026-06-07 decompilation confirms the exact local-coordinate conversion, active-part/thumb branch, hit-test match branch, stale-highlight invalidation, and final `this+0x103 = 0xff` clear.
- 2026-06-16 C001 live IDA MCP session `a001_goal2_00023O` reports server health `ok`, Hex-Rays ready, and `auto_analysis_ready:false`, so checks were targeted and current.
- `lookup_funcs` on 2026-06-16 reports this helper as `sub_5652A0` size `0xc0`; the sibling value helper is `sub_565360` size `0x128`; the rectangle helper is `sub_565010` size `0x15b`; raw starts `0x00565170`, `0x005651e0`, and `0x00565490` still report `Not a function`.
- `xrefs_to 0x005652a0` now reports exactly three code refs: modeled mouse handler call `0x00564a83`, modeled focus handler call `0x00564b28`, and raw-span call `0x00565272` with no containing function. `xrefs_to 0x005651e0`, `0x00565170`, and `0x00565490` still report zero refs to the raw helper starts.
- 2026-06-23 B005 current MCP session `80de0a67` reconfirms the [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md) edge into this helper: `lookup_funcs` reports no function at `0x005651e0` or `0x0056529c`, `0x005652a0` remains `sub_5652A0` size `0xc0`, bounded raw-span disassembly shows the call to this helper at `0x565272`, and `xref_query` reports that call as `0x00565272` with `fn:null`. The same pass finds zero raw-start xrefs and zero VA/RVA/raw-offset pointer patterns for `0x005651e0`, so the raw caller is documented as a source-shaped child body and not promoted to an IDA-modeled function or live direct-caller route.
- `xrefs_to 0x00565360` reports the two expected value-helper calls at `0x005652fb` and `0x00565324`, both inside this helper. `xrefs_to 0x00564e30` reports the two hit-test callers at `0x005649f1` and `0x0056530f`.
- `find_bytes` on 2026-06-16 found zero little-endian VA/RVA pointer hits for commit `0x005652a0`, raw begin `0x005651e0`, notify `0x00565360`, and rectangle helper `0x00565010`. This supports direct call reachability only, not a hidden table pointer route.
- Raw disassembly of `0x005651e0-0x005652a0` shows the begin-interaction span stores active part with `mov [esi+104h], bl` at `0x0056526c`, then calls this commit helper at `0x00565272`, then schedules the `this+0xa4` timer through `sub_5975E0` at `0x00565288`. The raw span ends with `retn 0Ch` at `0x0056529c`, one `0xcc`, then this helper's prologue at `0x005652a0`.
- Live decompilation of `0x004a9090` confirms the existing EventMan documentation: the helper copies stored cursor coordinates from `this+4` and `this+8` to an output point. Live decompilation of `0x005447c0` confirms a pane origin/offset helper, but not a stable original source name.
- C001 appended safe IDA comments at `0x005652a0`, raw call site `0x00565272`, value helper `0x00565360`, cursor helper `0x004a9090`, and origin helper `0x005447c0`; `idb_save` returned `ok:true` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`.
- 2026-06-19 B001 source-quality support note: the sibling notify helper [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md) uses the forwarded local second coordinate as `mouseX`; its first coordinate remains part of the two-coordinate helper ABI and call symmetry but does not participate in the horizontal value formula.

## Ownership Decision

Keep this as a `ScrollVolumePane` private helper. Its callers and field access are entirely inside the slider control; [UID:0000M7][OptionPane](by-file/OptionPane.md) is only notified after a value change.

The final source name is still open. `CommitInteraction`, `CommitScrollChange`, or `CommitVolumeChange` all fit the current behavior. `CommitInteraction` remains the accepted descriptive source label because it does not overclaim the original source spelling.

Do not rename this helper to the generated `TextEditPane::HandleScrollbarMouseF` signature. Current callers, field offsets, value-helper dependency, and ScrollVolumePane layout evidence contradict TextEditPane ownership.

## Generated Output Caveats

- Historical pre-callback generated source referenced `CommitScrollChange()` from superseded `OnMouseEvent` and `OnLoseFocus` labels and emitted a stale `TextEditPane` signature. Current ordinary source identities are `CommitInteraction`, `HandlePointerOrMouseEvent`, and `OnTimer`; renewed supervisor generated verification remains pending.
- The raw/projected helper at current range `0x005651e0-0x0056529f` calls this function from raw disassembly, but IDA does not currently model that raw start as a function.
- Historical/superseded Wave3 issue data recorded stale generated `TextEditPane::*` signatures for the ScrollVolumePane helper trio. Current ordinary docs and formal C++ correct the owner/name; this page does not mutate generated data.

## Reconstruction Notes

- Current direct owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md) at position `170`; [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is only the false/non-emitting address index.
- The helper is source-authored and reconstructable. Current formal C++ uses `m_hoverPart`, `m_activePart`, `ScrollVolumePart`, `HitTestPart`, `NotifyValueChange`, and `GetPartRect`; historical `m_highlightPart` is superseded.
- `+0x103` and `+0x104` are behaviorally resolved as hovered part and active interaction part, respectively. `0xff` / `-1` is `kScrollVolumePartNone` and `2` is `kScrollVolumePartThumb`.
- The raw begin-interaction page documents a raw edge into this range. Current IDA `xrefs_to` now exposes the raw call at `0x00565272`, but `lookup_funcs` still reports no function at `0x005651e0`, no xrefs to the raw start, and no VA/RVA pointer hits. Do not create an IDA function or force a prototype for that raw helper without a stronger caller/source-boundary explanation.
- Current [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md) emits `BeginInteraction(ScrollVolumePart part, int localY, int localX)` and calls this helper after storing `m_activePart`. The absence of a direct raw-start route remains a caveat on UID0002LC, not evidence against this commit helper's modeled callers or formal body.
- `EventMan::GetCursorPosition` is strong enough as a dependency name through [UID:00004O][EventMan](by-class/EventMan.md) and live decompilation. `0x005447c0` remains a pane screen-origin/screen-offset helper; the first draft uses `GetScreenOrigin` as the preferred descriptive name and should be renamed if the broader Pane API standardizes a different spelling.
- The formal body preserves the binary behavior: thumb drags notify directly, non-thumb parts re-hit-test before notify, unchanged/no-position sentinel paths skip notification, and stale hover state is invalidated and cleared after the commit path.

## Cross-References

- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md)
- [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)
- [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md)
- [UID:0001H3][0x00564e30-0x00565006.ScrollVolumePaneHitTestPart](by-memory/0x00564e30-0x00565006.ScrollVolumePaneHitTestPart.md)
- [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md)
- [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md)
- [UID:0000M7][OptionPane](by-file/OptionPane.md)

## Changes

- 2026-05-30: Changed completion/confidence from `50/65` to `58/70`.
  - Before: The page documented prior IDA caller/callee evidence but still described the helper body as omitted from active generated output.
  - After: The page records current active emission in `ui/controls/class_ScrollVolumePane.cpp`, while preserving the ownership caveat caused by the stale `TextEditPane::HandleScrollbarMouseF` generated signature.
  - Evidence: `class_ScrollVolumePane.cpp.source_map.json` maps method `0x005652a0` to `ScrollVolumePane`; the generated source emits `NTK_ScopedInstall(HandleScrollbarMouseF, 0x005652a0)` followed by the stale `TextEditPane` signature.
- 2026-05-31: Changed completion/confidence from `58/70` to `72/82` and marked the helper reconstructable.
  - Before: The page documented the committed helper chain but not the newly split raw caller page.
  - After: The page ties the commit behavior to the active-part/highlight fields and links the raw begin-interaction caller.
  - Evidence: IDA MCP decompilation of `0x005652a0`, caller/callee checks, and raw helper split evidence from `0x005651e0`.
- 2026-06-03 parent attachment update:
  - Before: `AUTOGEN_PARENT_UID` was blank.
  - Historical changed state: `AUTOGEN_PARENT_UID:0001H2`, position `130`, with blank reconstruction C++. Current correction uses owner/emitter UID0000CO, position `170`, and nonblank formal C++.
  - Historical evidence used superseded `OnMouseEvent`/`OnLoseFocus` caller labels. Current identities are `HandlePointerOrMouseEvent` and `OnTimer`; the retained raw call at `0x00565272` and surrounding boundary chain remain valid.
- 2026-06-07: Changed completion/confidence from `72/82` to `82/86`.
  - Before: The page recorded the general commit helper role but lacked exact address-neighbor notes, a state/call table, and current live IDA evidence for the local-coordinate, notify, and highlight-clear paths.
  - After: The page records source-authored rebuild handling, exact boundary/neighbor chain, cursor/local-coordinate conversion, active/thumb/hit-test notify paths, stale-highlight invalidation, parent aggregate attachment, raw-caller caveat, and final-C++ blockers.
  - Evidence: live IDA MCP lookup/caller/callee/decompile pass for `0x005652a0` on 2026-06-07.
- 2026-06-16 C001 direct-IDA refresh: changed completion/confidence from `82/86` to `85/89`.
  - Before: The page had strong 2026-06-07 behavior evidence but still left the raw begin-interaction edge, stale `TextEditPane` signature mismatch, cursor/origin helper names, and final-C++ blocker evidence shallow.
  - After: The page records current IDA function/xref/decompile/disassembly/byte-pattern evidence, the raw call at `0x00565272`, zero raw-start xrefs, zero VA/RVA pointer hits for relevant helper starts, behaviorally resolved `+0x103/+0x104` semantics, dependency-name decisions, saved IDA comments, and precise final-C++ blockers.
  - Evidence: live IDA MCP `lookup_funcs`, `xrefs_to`, `callees`, `decompile`, `insn_query`, `get_bytes`, `find_bytes`, `append_comments`, and `idb_save` on session `a001_goal2_00023O`.
- 2026-06-19 B001 notify-helper support sync:
  - Score remains `85/89`; formal C++ remains blank for this helper pending its own final source-quality body.
  - Evidence: accepted [UID:0001H5][0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange](by-memory/0x00565360-0x00565488.ScrollVolumePaneNotifyValueChange.md) pass corrected the downstream coordinate interpretation: `NotifyValueChange(int mouseY, int mouseX)` is called with two coordinates, but uses `mouseX` and the second/+4 drag-offset component for horizontal value math.
- 2026-06-19 B014 class source-quality incorporation:
  - Changed completion/confidence from `85/89` to `87/90` and populated first-draft `ScrollVolumePane::CommitInteraction()` C++.
  - Historical evidence: B014 resolved the helper under `ScrollVolumePane`, rejected stale `TextEditPane::HandleScrollbarMouseF`, and accepted then-draft `m_highlightPart`. Current correction is `m_hoverPart`; `m_activePart`, `ScrollVolumePart`, cursor/local-coordinate conversion, active-part recheck, thumb/direct notify behavior, and hover invalidation/clear behavior remain accepted.
- 2026-06-23 B005 [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md) support sync:
  - Score remains `87/90`; this helper's formal C++ remains unchanged.
  - Evidence: current MCP session `80de0a67` reconfirmed the raw-span call at `0x00565272`, the zero xrefs/pointer patterns to `0x005651e0`, and the no-function status of the raw begin-interaction start. The child now emits enum-synchronized `BeginInteraction` C++ and continues to call this helper immediately after the `m_activePart` byte write.
