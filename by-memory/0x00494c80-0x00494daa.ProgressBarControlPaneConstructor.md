*** UID:000119 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ProgressBarControlPane::ProgressBarControlPane(const RectBounds *bounds, short minValue, short maxValue)
    : ControlPane(22, bounds)
{
    if (minValue < 0)
        minValue = 0;
    else if (minValue > kMaxProgressValue)
        minValue = kMaxProgressValue;

    if (m_rangeStart != minValue) {
        if (m_rangeCurrent < minValue) {
            m_rangeCurrent = minValue;
            InvalidateRect(&m_visibleBounds);
        }

        m_rangeStart = minValue;
        InvalidateRect(&m_visibleBounds);
    }

    if (maxValue < 0)
        maxValue = 0;
    else if (maxValue > kMaxProgressValue)
        maxValue = kMaxProgressValue;

    if (m_rangeEnd != maxValue) {
        if (m_rangeCurrent > maxValue) {
            m_rangeCurrent = maxValue;
            InvalidateRect(&m_visibleBounds);
        }

        m_rangeEnd = maxValue;
        InvalidateRect(&m_visibleBounds);
    }

    if (m_rangeCurrent != 0) {
        m_rangeCurrent = 0;
        InvalidateRect(&m_visibleBounds);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact modeled `ProgressBarControlPane` constructor at `0x00494c80-0x00494daa`: sole PatchPane call `0x005472d7` passes bounds/0/100 after a `0x108` allocation; source calls `ControlPane(22,bounds)`, applies signed min/max clamps and exact synchronization/reset ordering through inherited `m_rangeStart/m_rangeCurrent/m_rangeEnd`, and calls `Pane::InvalidateRect(&m_visibleBounds)` after each observed state mutation. Three compiler vptr stores, stack/SEH lowering, and six-byte post-padding remain binary evidence rather than handwritten source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00494c80-0x00494daa - ProgressBarControlPane Constructor

## Status

- Confidence: strong for boundary, class ownership, and constructor role; not final-audit confidence.
- Related class: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- Likely source file: [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- Type docs: [UID:0001YH][ProgressBarControlPaneVtables](by-type/by-vtable/ProgressBarControlPaneVtables.md)
- IDA function: modeled at `0x00494c80`, size `0x12a`
- Rebuild handling: `source-authored`; first-draft constructor C++ populated with vptr stores documented as generated-binary side effects rather than handwritten source.

## Score Rationale

- Completion is `88` because the exact function boundary, caller, callees, generated vtable writes, argument handling, clamping sequence, touched fields, invalidation paths, stack/SEH shape, neighboring padding, sibling-range boundary, accepted source-facing names, and first-draft C++ are now recorded.
- Confidence is `91` because lookup, decompilation, caller/callee queries, vtable xrefs, raw bytes, stack-frame data, and the sole PatchPane call site all agree on the constructor behavior. It stays below final-audit confidence because exact original source spelling for field/helper names is inferred.

## Function

| Range | Function | Notes |
| --- | --- | --- |
| `0x00494c80-0x00494daa` | `ProgressBarControlPane::ProgressBarControlPane(const RectBounds *bounds, short minValue, short maxValue)` | `thiscall` constructor with three stack arguments: bounds pointer, initial lower/range-start short, and initial upper/range-end short. Calls inherited `ControlPane(22,bounds)`, installs three generated progress-bar vtable views, clamps both incoming range values to `0..30000`, writes inherited `m_rangeStart +0xf8` and `m_rangeEnd +0xfc`, adjusts inherited `m_rangeCurrent +0xfa` only as needed during range synchronization, and finally forces current back to `0` if it is nonzero. |

## Behavior

1. Builds the inherited `ControlPane` base with control type `22` and the incoming bounds pointer.
2. Installs the primary, secondary, and tertiary `ProgressBarControlPane` vtable views at object offsets `+0x00`, `+0xa0`, and `+0xa4`.
3. Clamps the lower/range-start argument: negative values become `0`; values above `30000` become `30000`.
4. If object offset `0xf8` differs from the clamped lower value, it raises current field `0xfa` to that lower value when current is smaller, calls `Pane::InvalidateRect(&m_visibleBounds)`, then stores the lower field and dispatches the same inherited slot `+0x20`.
5. Clamps the upper/range-end argument by the same `0..30000` rule.
6. If object offset `0xfc` differs from the clamped upper value, it lowers current field `0xfa` when current is above the new upper value and invalidates, stores the upper field, and invalidates again through slot `+0x20`.
7. If current field `0xfa` is nonzero after range setup, it writes `0` and dispatches `Pane::InvalidateRect(&m_visibleBounds)` through slot `+0x20`.
8. Returns the receiver with `retn 0x0c`.

## Evidence Notes

- Live IDA MCP evidence on 2026-06-05 used `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, image base `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- `lookup_funcs` reports `0x00494c80` as a modeled function of size `0x12a`, covering `0x00494c80-0x00494daa`; `0x00494daa` and `0x00494db0` are not IDA function starts, while `0x00494de0` is the next modeled draw function.
- Raw bytes confirm the boundary: first 32 bytes at `0x00494c80` are `55 8b ec 6a ff 68 fb ce 5f 00 64 a1 00 00 00 00 50 51 53 56 57 a1 24 2f 67 00 33 c5 50 8d 45 f4`; the final constructor bytes end with `5d c2 0c 00`; `0x00494daa-0x00494db0` is six `0xcc` bytes before the unmodeled current-value helper prologue.
- Decompilation shows the inherited `ControlPane` constructor call with control type `22`, vtable stores, lower/upper clamp logic, writes to object offsets `0xf8`, `0xfa`, and `0xfc`, and the final current-value reset to zero.
- `xrefs_to` the vtable bases reports constructor data xrefs at `0x00494cc5 -> 0x00617b34`, `0x00494ccb -> 0x00617b9c`, and `0x00494cd5 -> 0x00617bcc`.
- The primary vtable slot used for invalidation is `+0x20`, stored at `0x00617b54 -> 0x00544800`; the constructor calls helper `0x00544800` directly at `0x00494d10` for the lower-bound current adjustment and uses virtual calls through `[vtable+0x20]` at `0x00494d24`, `0x00494d61`, `0x00494d73`, and `0x00494d91`.
- `callers(0x00494c80)` reports a single code caller at `0x005472d7` inside the PatchPane constructor. Decompilation of that caller shows [UID:0000MH][PatchPane](by-file/PatchPane.md) creating the Auto Patch progress bar with a 264-byte allocation followed by constructor arguments `rect, 0, 100`.
- `callees(0x00494c80)` reports only the inherited `ControlPane` constructor at `0x004949e0` and refresh helper `0x00544800` as direct callees; the other refresh calls are virtual calls through the vtable.
- 2026-06-05 live IDA refresh reconfirms the modeled constructor boundary, the sole PatchPane constructor caller, the two direct callees, the three vtable-base references, the `0x00617b54` invalidation slot pointer, and six padding bytes before `0x00494db0`.
- `stack_frame(0x00494c80)` shows SEH/security-cookie locals `var_10`, `var_C`, `var_4`, saved registers, and stack args `arg_0` dword, `arg_4` word, and `arg_8` word, matching a `thiscall` constructor returning `this`.

## Touched State And Calls

| Field / call | Evidence-backed role | Remaining uncertainty |
| --- | --- | --- |
| object offset `0x00` | Primary `ProgressBarControlPane` vptr written with `0x00617b34`. | Compiler-generated store; current class declaration is emitted separately on UID0000AW. |
| object offset `0xa0` | Secondary vptr written with `0x00617b9c`. | Secondary base/interface name remains inherited from control-pane docs. |
| object offset `0xa4` | Tertiary vptr written with `0x00617bcc`. | Tertiary base/interface name remains inherited from control-pane docs. |
| object offset `0x44` | Inherited `GrafPort::m_visibleBounds` passed to `Pane::InvalidateRect`. | Resolved by current PaneLayout/GrafPort and vtable authority. |
| object offset `0xf8` | Inherited signed `short ControlPane::m_rangeStart` after `0..30000` clamp. | Physical member identity/type/offset are exact; minimum is the ProgressBar semantic role. |
| object offset `0xfa` | Inherited signed `short ControlPane::m_rangeCurrent`, synchronized against range changes and then reset to `0`. | Physical member identity/type/offset are exact; no derived shadow member exists. |
| object offset `0xfc` | Inherited signed `short ControlPane::m_rangeEnd` after `0..30000` clamp. | Physical member identity/type/offset are exact; maximum is the ProgressBar semantic role. |
| `0x004949e0` | `ControlPane` constructor with control type `22`. | Final source signature belongs on the `ControlPane` pages. |
| `0x00544800` / vtable `+0x20` | `Pane::InvalidateRect(const RectBounds *)` with `&m_visibleBounds`. | Current source-facing name resolved. |

## Historical B005 Source-Quality Closure

The accepted source-facing constructor is `ProgressBarControlPane::ProgressBarControlPane(const RectBounds *bounds, short minValue, short maxValue) : ControlPane(22, bounds)`. The formal C++ deliberately omits the explicit vptr stores observed at `0x00494cc5`, `0x00494ccb`, and `0x00494cd5`; those are compiler-generated consequences of constructing a derived class with primary, secondary, and tertiary views at object offsets `+0x00`, `+0xa0`, and `+0xa4`.

The sole direct caller remains `PatchPane::PatchPane` at `0x005472d7`, after allocation size `0x108` and pushes for `bounds`, `0`, and `100`. The constructor applies the same range clamp/sync behavior as `SetMinValue` and `SetMaxValue`, then resets inherited `m_rangeCurrent` to zero if needed. B005 left the exact inherited source names implicit; current authority resolves the `ControlPane` range triplet at `+0xf8/+0xfa/+0xfc`, primary slot `+0x20`, `0x00617b54 -> 0x00544800`, as `Pane::InvalidateRect`, and complete-object `+0x44` as inherited `GrafPort::m_visibleBounds`. The six-byte padding span at `0x00494daa-0x00494db0` remains the boundary before `AddCurrentValue`.

## B002 UID000115 Constructor Support Sync

- Healthy MCP session `9b0396a3` reconfirmed the modeled `0x12a` body, caller `0x005472d7`, direct callees `0x004949e0`/`0x00544800`, and vtable stores at `0x00494cc5`, `0x00494ccb`, and `0x00494cd5`.
- The range logic independently corroborates inherited signed-short fields `m_rangeStart +0xf8`, `m_rangeCurrent +0xfa`, and `m_rangeEnd +0xfc`, class-local ceiling `30000`, and the write/invalidation order emitted on UID000115/UID000117.
- The direct call at `0x00494d10` and virtual calls at `0x00494d24`, `0x00494d61`, `0x00494d73`, and `0x00494d91` all represent source-level `InvalidateRect(&m_visibleBounds)` calls; the direct-versus-virtual distinction is compiler dispatch detail preserved in evidence, not raw call syntax in human C++.
- Explicit primary/secondary/tertiary vptr stores, SEH records, security-cookie handling, adjustor thunks, and padding remain compiler-generated evidence and are intentionally absent from formal C++.
- Historical `m_bounds`, `Invalidate`, `sub_544800`, and final-name-open wording are superseded by the current class/Pane/PaneLayout/GrafPort source model.
- Exact `ControlPane` complete size `0x108` and PatchPane's `0x108` allocation prove ProgressBar adds no storage: the constructor's three semantic progress values reuse inherited protected range fields. Derived `m_minValue/m_currentValue/m_maxValue` aliases are retained only as superseded source-shape history.

## Boundary And Neighbor Notes

- The constructor is a real IDA function; neighboring projected helpers are not currently modeled as IDA functions.
- The six-byte `0xcc` span at `0x00494daa-0x00494db0` separates this function from [UID:00011A][0x00494db0-0x00494dde.ProgressBarControlPaneCurrentValueAdder](by-memory/0x00494db0-0x00494dde.ProgressBarControlPaneCurrentValueAdder.md).
- [UID:00011C][0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar](by-memory/0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar.md) consumes the same `0xf8`/`0xfa`/`0xfc` range/current triplet initialized here.
- IDA basic-block reporting includes 21 blocks when the out-of-line SEH cleanup and security-cookie handler blocks at `0x005fcef3` and `0x005fcefb` are included; those are compiler support paths, not separate source-authored progress-bar logic.

## Reconstruction Notes

- Reconstructable: true as source-authored `ProgressBarControlPane` constructor behavior.
- Parent: [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md).
- C++: first-draft constructor body is now populated. The constructor source shape is strong enough for current emitter-gate use; vtable stores remain prose-only generated-binary evidence.

## Cross-References

- [UID:0000MT][ProgressBarControlPane](by-file/ProgressBarControlPane.md)
- [UID:0000AW][ProgressBarControlPane](by-class/ProgressBarControlPane.md)
- [UID:0001YH][ProgressBarControlPaneVtables](by-type/by-vtable/ProgressBarControlPaneVtables.md)
- [UID:0002OL][0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData](by-memory/0x00617b30-0x00617bd4.ProgressBarControlPaneVtableData.md)
- [UID:000116][0x00494a90-0x00494b4b.ProgressBarRangeSetters](by-memory/0x00494a90-0x00494b4b.ProgressBarRangeSetters.md)
- [UID:00011A][0x00494db0-0x00494dde.ProgressBarControlPaneCurrentValueAdder](by-memory/0x00494db0-0x00494dde.ProgressBarControlPaneCurrentValueAdder.md)
- [UID:00011C][0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar](by-memory/0x00494de0-0x00494eb0.ProgressBarControlPaneDrawProgressBar.md)
- [UID:0000IG][ControlPane](by-file/ControlPane.md)
- [UID:0000MH][PatchPane](by-file/PatchPane.md)

## Changes

- Before: completion/confidence were `50/65`, reconstruction metadata was blank, and the range-setter backlink used the stale `0x00494b48` path.
- Changed to: completion/confidence are `72/84`, the constructor is marked reconstructable and attached to the `ProgressBarControlPane` class, and the corrected range-setter aggregate is linked.
- Summary/evidence: IDA MCP confirms the modeled constructor size `0x12a`, the `ControlPane` constructor call with type `22`, constructor vtable stores, range/current field writes, and the `PatchPane` constructor caller. The old no-C++/95+ wording was historical and is superseded by the 2026-06-19 B005 first-draft constructor implementation.
- 2026-06-05 live IDA MCP constructor audit:
  - Before: the page had a concise constructor summary but did not record exact byte boundaries, stack shape, detailed clamp/reset order, sole caller call-site arguments, vtable-slot invalidation evidence, or sibling-padding evidence.
  - After: completion/confidence are `84/90`, with behavior, touched state, caller/callee evidence, vtable-base xrefs, raw byte boundaries, and reconstruction rationale documented; final C++ remained blank at that time, superseded by the 2026-06-19 implementation.
  - Evidence: live `lookup_funcs`, `decompile`, `disasm`, `callers`, `callees`, `xrefs_to`, `stack_frame`, and IDA byte/vtable reads on 2026-06-05 confirm the constructor range `0x00494c80-0x00494daa`, PatchPane call at `0x005472d7` with initial range `0..100`, the `0x00617b34`/`0x00617b9c`/`0x00617bcc` vptr stores, direct callees `0x004949e0` and `0x00544800`, and six `0xcc` bytes before the current-value helper.
- 2026-06-05 stale cleanup:
  - Changed from `84/90` to `86/90`.
  - Replaced synthetic function labels and receiver-offset phrasing with modeled-function, object-offset, and address-based helper wording.
  - Evidence: live `lookup_funcs`, `callers`, `callees`, `xrefs_to`, and byte reads reconfirm the constructor boundary, sole PatchPane constructor caller, direct callee pair, vtable-base references, invalidation slot pointer, and padding before the current-value helper.
- 2026-06-19 B005 accepted source-quality implementation:
  - Changed from `86/90` to `88/91`.
  - Added first-draft constructor C++ with `ControlPane(22, bounds)`, min/max clamps to `0..30000`, current synchronization, final current reset to zero, and no handwritten vptr stores. Recorded allocation size `0x108`, caller `0x005472d7`, args `bounds,0,100`, inherited invalidation slot, and vptr-store caveat.
- 2026-07-23 B002 UID000115 support callback:
  - Scores and owner/emitter metadata remain `88/91`, UID0000AW, reconstructable true, and UID0000AW.
  - Replaced stale open-name and `sub_544800` summary wording with exact accepted fields, `Pane::InvalidateRect(&m_visibleBounds)`, and current formal truth.
  - Preserved exact caller/callee/vptr/SEH/padding evidence, clarified direct-versus-virtual compiler dispatch, and historicalized `Invalidate(&m_bounds)` without deleting provenance.
- 2026-07-23 B003 inherited-storage correction:
  - Preserved `88/91`, the sole PatchPane caller, base-constructor call, vptr/SEH/padding evidence, exact mutation/invalidation order, and compiler-exclusion rationale.
  - Replaced derived `m_minValue/m_currentValue/m_maxValue` aliases with inherited `m_rangeStart/m_rangeCurrent/m_rangeEnd`, and documented the exact `0x108` no-derived-storage proof.
