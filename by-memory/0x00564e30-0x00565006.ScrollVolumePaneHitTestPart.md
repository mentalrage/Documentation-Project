*** UID:0001H3 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:130 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ScrollVolumePart ScrollVolumePane::HitTestPart(int localY, int localX)
{
    Point cursor;
    Point origin;

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);

    if (origin.y == -1000 && origin.x == -1000)
        return kScrollVolumePartNone;

    cursor.y -= origin.y;
    cursor.x -= origin.x;

    for (int partValue = kScrollVolumePartLeading;
         partValue <= kScrollVolumePartTrailing;
         ++partValue) {
        const ScrollVolumePart part = static_cast<ScrollVolumePart>(partValue);
        RectBounds rect = m_bounds;

        if (part == kScrollVolumePartLeading) {
            rect.right = rect.left;
        } else if (part == kScrollVolumePartTrailing) {
            rect.left = rect.right;
        } else if (m_enabled && m_range > 0) {
            const int trackStart = m_bounds.left;
            const int thumbLimit = m_bounds.right - kThumbSpan;
            int thumbLeft;

            if (m_thumbDragActive) {
                const int cursorAlongTrack = cursor.x;
                const int dragOffsetAlongTrack = m_thumbDragOffset.x;

                thumbLeft = cursorAlongTrack - dragOffsetAlongTrack;

                if (thumbLeft < trackStart) {
                    thumbLeft = trackStart;
                }
                if (thumbLeft > thumbLimit) {
                    thumbLeft = thumbLimit;
                }
            } else {
                thumbLeft = trackStart + ((thumbLimit - trackStart) * m_value) / m_range;
            }

            const int thumbRight = thumbLeft + kThumbSpan;

            if (part == kScrollVolumePartThumb) {
                rect.left = thumbLeft;
                rect.right = thumbRight;
            } else if (part == kScrollVolumePartPageBeforeThumb) {
                rect.right = thumbLeft;
            } else if (part == kScrollVolumePartPageAfterThumb) {
                rect.left = thumbRight;
            }
        } else if (part == kScrollVolumePartThumb) {
            rect.left += kThumbSpan;
            rect.right -= kThumbSpan;
        } else {
            SetRect(&rect, 0, 0, 0, 0);
        }

        if (PointInRect(localY, localX, &rect)) {
            return part;
        }
    }

    return kScrollVolumePartNone;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: ScrollVolumePane slider hit-test helper now records source-authored rebuild handling, exact address-range boundaries, five-part slider map, field/touched-state usage from layout docs, stale TextEditPane generated-signature caveat, parent aggregate attachment, and final-C++ blockers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

## 2026-07-26 Accepted Coordinate And Ownership Repair

- Direct semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), position 130; UID0001H2 is the non-emitting address index.
- Exact modeled range remains `[0x00564e30,0x00565006)`, size `0x1d6`, with direct callers `0x005649f1` and `0x0056530f` and ten bytes of following alignment.
- Current `Point` layout is `y@+0`, `x@+4`. The slider is horizontal, so cursor and drag-offset X determine thumb placement, while rectangle APIs retain `(localY, localX)` order.
- The helper constructs and tests all five candidate rectangles inline. It is not a stale `GetPartRect` call-through loop; UID0004XZ owns the separate requested-part helper at `0x00565010`.
- Signed `m_value`/`m_range`, fixed 38-pixel `kThumbSpan`, enabled/drag state, collapsed leading/trailing edges, and disabled thumb inset are all preserved in formal C++.
- Rejected historical assumptions include vertical/top-bottom geometry, `HitTestPart(int x,int y)`, `.y` as the horizontal drag component, TextEditPane ownership, and aggregate-emitted code.

# 0x00564e30-0x00565006 ScrollVolumePane Hit-Test Part Helper

## Status

- Confidence: very strong for function boundary, class ownership, horizontal part model, helper dependencies, and first-draft C++; exact original spelling of `HitTestPart` remains descriptive but source-quality.
- Current generated owner caveat: active historical generated output emitted this body with stale signature `char TextEditPane::TrackScrollButtonF(int mouseX, int mouseY)`. B014 source-quality review rejects that as generated-owner pollution; callers, fields, and source route attach the helper to `ScrollVolumePane`.
- Proposed owner: [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md).
- Rebuild handling: source-authored helper code.
- Role: returns which slider part contains the caller-supplied point, or `-1` when no part is hit.
- Parent handling: attached to the owning aggregate [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) at position `100`; first-draft C++ is populated as a child body under the class declaration route.

## Address Range

| Range | Evidence | Notes |
| --- | --- | --- |
| `0x00564710-0x00564e30` | Parent aggregate indexes constructor, setters, paint, pointer/key/timer handlers, and this helper's preceding neighbors. | Current `HandlePointerOrMouseEvent` calls this hit-test at `0x005649f1`; historical `OnMouseEvent` wording is superseded. |
| `0x00564e30-0x00565006` | IDA MCP lookup records `sub_564E30`, size `0x1d6`. | Exact slider-part hit-test body. |
| `0x00565010-0x0056516b` | Parent/class/file docs treat the following modeled helper as `GetPartRect`. | This page stops before the sibling rectangle calculator used by highlight/commit/value helpers. |

## Behavior

This helper maps a screen-space mouse point to one of the horizontal volume slider's five logical parts:

- reads the current cursor and pane screen offset for drag-override thumb placement;
- iterates known slider part ids `0..4`;
- builds each candidate rectangle from pane bounds, current value, max range, enabled state, drag override state, and thumb metrics;
- uses the shared [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) containment helper; and
- returns the first matching part id, or `-1` when the point is outside all slider parts.

The part ids match [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md): `0` leading/left end, `1` page-before-thumb track, `2` thumb, `3` page-after-thumb track, `4` trailing/right end, and `0xff` / `-1` as the no-part sentinel used by highlight/active fields.

## Touched State And Calls

| Offset / target | Evidence | Interpretation |
| --- | --- | --- |
| `this + 0x44` | Layout docs identify the pane bounds rectangle; geometry helpers read the same rectangle. | Base control rectangle for all part tests. |
| `this + 0xfb` | 2026-05-31 decompilation review and layout docs record a read before rectangle testing. | Drag-position override flag. |
| `this + 0xfc` | Layout docs identify this as `volumeType`; constructors pass `1` and `0` for the two option sliders. | Notifies which slider family this control represents elsewhere; included in local state context. |
| `this + 0xfe` | `SetValue`, `SetRange`, layout, paint, and hit-test evidence identify current slider value. | Used to place the thumb and divide track regions. |
| `this + 0x100` | `SetRange`, layout, paint, and hit-test evidence identify max value/range. | Used for value-to-pixel scaling. |
| `this + 0x102` | Constructor and enable/disable docs identify enabled state. | Disabled sliders should not produce active hit parts. |
| `this + 0x103` | Raw highlight helper pages read/write this byte. | Highlighted part state affected by later callers after hit-test. |
| `this + 0x104` | Raw begin/reset and commit helper pages read/write this byte. | Active interaction part set after hit-test. |
| `this + 0x108` / `this + 0x10c` | Layout docs identify drag point/offset state used by drag paths. | Supports thumb-drag rectangle placement. |
| `0x00565010` | Commit/value/reset helper pages call the adjacent part-rectangle helper; B014 direct PE/Capstone review confirms this target does not call it. | Sibling geometry counterpart, not an implementation dependency of this hit-test body. |
| `0x004b7c30` / `0x004b7e80` family | IDA callee checks list `InitRectBounds` and `PointInRect_4B7E80`. | Shared rectangle initialization and containment checks. |
| `0x004a9090` / `0x005447c0` | IDA callee checks list cursor/screen-position helpers. | Convert screen/cursor input to the coordinate system used by the pane. |

## Evidence Notes

- IDA MCP lookup on 2026-05-25 reports `0x00564e30` as `sub_564E30` size `0x1d6`.
- IDA caller checks report two callers: current `ScrollVolumePane::HandlePointerOrMouseEvent` at `0x005649f1` (historically labeled `OnMouseEvent`) and the interaction commit helper at `0x0056530f`.
- 2026-05-26 IDA MCP recheck reports the same size and caller sites.
- IDA callee checks show use of [UID:0000N2][RectBounds](by-file/RectBounds.md) helpers `InitRectBounds` and `PointInRect_4B7E80`, plus cursor/screen-position helpers `0x004a9090` and `0x005447c0`.
- Historical/superseded 2026-06-15 C001 Wave2 `method`/`xrefs` data reported `0x00564e30` as IDA `sub_564E30`, size `0x1d6`, with callers `0x005649f1` in `sub_564910` and `0x0056530f` in `sub_5652A0`; the address/callee evidence remains useful, but current ordinary source identities are `HandlePointerOrMouseEvent` and `CommitInteraction`, and Wave2 is not current authority.
- 2026-06-19 B014 direct PE/Capstone recheck confirms exact range `0x00564e30-0x00565006`, body size `0x1d6`, `ret 8` at `0x00565003`, 15 bytes of `0xcc` pre-padding at `0x00564e21-0x00564e2f`, 10 bytes of `0xcc` post-padding at `0x00565006-0x0056500f`, direct rel32 callers at `0x005649f1` and `0x0056530f`, and zero absolute-VA dword route hits for `0x00564e30`.
- The same B014 recheck rejects the older `GetPartRect` call-through draft: `0x00564e30` calls cursor/origin/rectangle primitives and builds five candidate rectangles inline, while `0x00565010` is the sibling `GetPartRect` helper used by other ScrollVolumePane paths.
- The helper iterates slider part ids `0..4`, builds each part rectangle from the pane bounds, current value, range, enabled/drag state, and the 38-pixel thumb span, then returns the first part containing the caller-supplied point.
- Historical/superseded 2026-06-15 C001 Wave2 struct data supported object size `272` and tail offsets `+0xf8`, `+0xfc`, `+0xfe`, `+0x102`, `+0x104`, `+0x108`, and `+0x10c`. Current [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md) is authoritative and additionally records `m_hoverPart` at `+0x103` plus the exact byte/word semantics.
- 2026-05-31 IDA decompilation review confirms reads of `+0xfb`, `+0xfe`, `+0x100`, `+0x102`, and the drag-offset point state before testing rectangles; the immediately related raw interaction helpers are now split into their own pages.
- 2026-05-30 source-map/source check: `class_ScrollVolumePane.cpp.source_map.json` maps method `0x00564e30` to `class_ScrollVolumePane.cpp`, while the C++ body at line 344 still uses a stale `TextEditPane` owner/signature. Keep this page under `ScrollVolumePane` unless a later IDA pass overturns the existing caller/field evidence.
- [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md) records the same state bytes/words and the known slider part codes, tying this hit-test helper to the highlight, active-interaction, and value-change pages.
- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md) records this helper as part of the emitted-but-stale generated helper group and keeps the canonical source placement in `ui/controls/ScrollVolumePane.cpp`.
- No new live MCP-only facts were added in this pass; the latest direct MCP calls in this session were timing out for function tools, so this update relies on already written IDA-backed evidence in related pages.

## Ownership Decision

Keep this with [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md). It depends directly on `ScrollVolumePane` local fields at `+0xfb`, `+0xfe`, `+0x100`, `+0x102`, and `+0x10c`, and its only callers are inside the same control.

Do not move this into [UID:0000M7][OptionPane](by-file/OptionPane.md). OptionPane constructs and reacts to the slider, but it does not own slider geometry or hit-testing.

## Generated Output Caveats

- Active `class_ScrollVolumePane.cpp` now emits this helper body separately, but the generated owner/name is polluted by a `TextEditPane` scrollbar variant. Migration should keep the body with `ScrollVolumePane` and rename it only after the surrounding API names are settled.

## Reconstruction Notes

- The page is reconstructable and already attached to [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md), which in turn is rooted under the [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md) source file.
- The B014 class pass resolves the source-facing part enum and helper signatures enough for first-draft C++: use `ScrollVolumePart`, `kScrollVolumeThumbSpan = 38`, the sibling `GetPartRect` declaration, and a pane-origin conversion helper. The remaining uncertainty is exact historical helper spelling, not ownership or behavior.
- Do not rename this helper to the generated `TextEditPane::TrackScrollButtonF` signature. That label is stale generated-output pollution contradicted by ScrollVolumePane callers, field access, value-helper dependency, and the file/class route.
- The formal C++ is intentionally source-shaped rather than decompiler-shaped. It builds each rectangle inline and does not call `GetPartRect`; if the shared Pane API later standardizes the origin helper name differently, update the call name without changing the underlying `0x005447c0` dependency.

## Cross-References

- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md)
- [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)
- [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md)
- [UID:0002LB][0x00565170-0x005651e0.ScrollVolumePaneSetHoverPartRaw](by-memory/0x00565170-0x005651e0.ScrollVolumePaneSetHoverPartRaw.md)
- [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md)
- [UID:0001H4][0x005652a0-0x00565360.ScrollVolumePaneCommitInteraction](by-memory/0x005652a0-0x00565360.ScrollVolumePaneCommitInteraction.md)
- [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md)
- [UID:0000M7][OptionPane](by-file/OptionPane.md)

## Changes

- 2026-05-30: Changed completion/confidence from `50/65` to `58/70`.
  - Before: The page documented prior IDA caller/callee evidence but still described the helper as omitted from active generated output.
  - After: The page records current active emission in `ui/controls/class_ScrollVolumePane.cpp`, while preserving the ownership caveat caused by the stale `TextEditPane::TrackScrollButtonF` generated signature.
  - Evidence: `class_ScrollVolumePane.cpp.source_map.json` maps method `0x00564e30` to `ScrollVolumePane`; the generated source emits `NTK_ScopedInstall(meth_0x564e30, 0x00564e30)` followed by the stale `TextEditPane` signature.
- 2026-05-31: Changed completion/confidence from `58/70` to `72/82` and marked the helper reconstructable.
  - Before: The page documented caller/callee ownership but did not reference the newly split raw interaction helpers or the latest local field review.
  - After: The page records direct field-use evidence and links to the adjacent raw helper pages while keeping final C++ blank below the 95+ gate.
  - Evidence: IDA MCP decompilation and raw helper split pass on the `ScrollVolumePane` interaction island.
- 2026-06-03 parent attachment update:
  - Before: `AUTOGEN_PARENT_UID` was blank.
  - Changed to: `AUTOGEN_PARENT_UID:0001H2`, position `100`; reconstruction C++ remains blank.
  - Evidence: [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is now rooted under the class/file chain, and restarted IDA MCP rechecked the surrounding helper island.
- 2026-06-06: Raised completion/confidence from `72/82` to `82/84` and expanded the helper's address-range, behavior, touched-state, layout, generated-output, and reconstruction-gate documentation.
  - Before: the page had strong ownership and caller/callee notes, but did not spell out the exact neighboring ranges, state offsets, part ids, or why C++ remains deferred.
  - After: the page records source-authored rebuild handling, the slider-part map, [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md) field evidence, current stale `TextEditPane` generated-signature caveat, and final-source blockers.
  - Evidence: existing IDA-backed ScrollVolumePane layout/file/neighbor pages; no new live-MCP-only facts were introduced because function-tool calls timed out during this session.
- 2026-06-15 C001 Goal 2 refresh: raised completion/confidence from `82/84` to `85/86`.
  - Historical evidence: Wave2 `method`/`xrefs` reconfirmed the IDA `sub_564E30` size, callers from then-labeled `OnMouseEvent` and the commit helper, callee set, and tail offsets. Current corrections are `HandlePointerOrMouseEvent`, `CommitInteraction`, and the accepted source-facing field/part vocabulary; this page is now first-draft-ready rather than blocked on those former questions.
- 2026-06-19 B014 class source-quality incorporation:
  - Changed completion/confidence from `85/86` to `88/90` and populated first-draft `ScrollVolumePane::HitTestPart(int mouseX, int mouseY) const` C++.
  - Evidence: B014 class pass resolved stale `TextEditPane::TrackScrollButtonF` pollution, horizontal `ScrollVolumePart` ids `0..4/-1`, fixed `kScrollVolumeThumbSpan = 38`, screen-origin/cursor helpers, rectangle helper pair, and class/file ownership under [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)/[UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md).
- 2026-06-22 Rule 26 report incorporation:
  - Refined the first-draft body from a stale `GetPartRect` loop into the B014-reported inline rectangle-construction source shape, set emitter position `100`, and preserved `88/90`.
  - Evidence: `Agent-B014/research/0001H3-ScrollVolumePaneHitTestPart-source-quality.md` directly rechecked PE/Capstone range, padding, callers, zero route hits, helper callees, horizontal part map, fixed thumb span, drag override fields, and the no-`GetPartRect` implementation distinction.
