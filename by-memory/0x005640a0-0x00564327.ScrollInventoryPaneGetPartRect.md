*** UID:00051Q | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:140 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollInventoryPane::GetPartRect(ScrollInventoryPart part,
                                       RectBounds *bounds)
{
    Point cursor;
    Point origin;

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.x == -1000 && origin.y == -1000) {
        return;
    }
    cursor.x -= origin.x;
    cursor.y -= origin.y;

    *bounds = m_visibleBounds;
    const bool horizontal = (m_orientation != 0);
    const int thumbSpan = horizontal
        ? m_visibleBounds.bottom - m_visibleBounds.top
        : kScrollPanePartExtentBySkin[m_scrollSkinIndex];

    if (part == kScrollInventoryPartLeading) {
        if (horizontal) {
            bounds->right = bounds->left;
        } else {
            bounds->bottom = bounds->top;
        }
        return;
    }
    if (part == kScrollInventoryPartTrailing) {
        if (horizontal) {
            bounds->left = bounds->right;
        } else {
            bounds->top = bounds->bottom;
        }
        return;
    }

    if (!CanScroll()) {
        if (part == kScrollInventoryPartThumb) {
            if (horizontal) {
                bounds->left += thumbSpan;
                bounds->right -= thumbSpan;
            } else {
                bounds->top += thumbSpan;
                bounds->bottom -= thumbSpan;
            }
        } else {
            InitRectBounds(bounds, 0, 0, 0, 0);
        }
        return;
    }

    if (horizontal) {
        const int trackStart = m_visibleBounds.left;
        const int thumbLimit = m_visibleBounds.right - thumbSpan;
        const int thumbStart = trackStart +
            (m_scrollPosition * (thumbLimit - trackStart)) /
                m_scrollRange;
        const int thumbEnd = thumbStart + thumbSpan;
        if (part == kScrollInventoryPartThumb) {
            bounds->left = thumbStart;
            bounds->right = thumbEnd;
        } else if (part == kScrollInventoryPartPageBeforeThumb) {
            bounds->right = thumbStart;
        } else if (part == kScrollInventoryPartPageAfterThumb) {
            bounds->left = thumbEnd;
        }
    } else {
        const int trackStart = m_visibleBounds.top;
        const int thumbLimit = m_visibleBounds.bottom - thumbSpan;
        int thumbStart;
        if (m_thumbDragActive) {
            thumbStart = m_visibleBounds.top + cursor.y - m_thumbDragOffset.y;
            if (thumbStart < trackStart) {
                thumbStart = trackStart;
            }
            if (thumbStart > thumbLimit) {
                thumbStart = thumbLimit;
            }
        } else {
            thumbStart = trackStart +
                (m_scrollPosition * (thumbLimit - trackStart)) /
                    m_scrollRange;
        }
        const int thumbEnd = thumbStart + thumbSpan;
        if (part == kScrollInventoryPartThumb) {
            bounds->top = thumbStart;
            bounds->bottom = thumbEnd;
        } else if (part == kScrollInventoryPartPageBeforeThumb) {
            bounds->bottom = thumbStart;
        } else if (part == kScrollInventoryPartPageAfterThumb) {
            bounds->top = thumbEnd;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ScrollInventoryPane geometry helper computes leading, page-before, thumb, page-after and trailing rectangles with orientation, disabled and drag-specific behavior. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005640a0-0x00564327 ScrollInventoryPane GetPartRect

## Status And Exact Identity

- Reconstructable modeled private method owned/emitted by [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md).
- Half-open range `[0x005640a0,0x00564327)`, `0x287` / 647 bytes; SHA256 `a6c312f64172061f483f39e41f8df29b78a379ec6153b3160d81adf2b7df5565`.
- Fenced by 3-byte `[0x0056409d,0x005640a0)` and 9-byte `[0x00564327,0x00564330)` alignment.

## Geometry Contract

- Reads cursor/screen origin and returns without writing output for the exact `(-1000,-1000)` sentinel; otherwise localizes cursor and starts from inherited `GrafPort::m_visibleBounds`. The earlier `m_bounds` spelling is rejected historical lead material.
- Leading/trailing parts collapse the appropriate edge. Disabled state yields a central thumb rect or empty non-thumb rectangles.
- Horizontal mode derives thumb span from pane height and scales position across width. Vertical mode uses the shared skin-indexed extent and, while dragging, clamps cursor-derived thumb placement.
- Page-before/page-after regions end/start at the computed thumb edges; signed range/position arithmetic is preserved.

## Callers, Dependencies, And Ownership

- Thirteen internal callers from `0x005634d7` through `0x005646e6` cover pointer handling, painting, highlight transitions, drag setup/update and reset.
- Imports `kScrollPanePartExtentBySkin` from [UID:0000NF][ScrollBar](by-file/ScrollBar.md); no class-local duplicate is emitted.
- Dense internal use, exact body/hash and the accepted sibling clone establish a private class helper.

## IDA Recommendation

- Report stage `I16` recommends rename `ScrollInventoryPane__GetPartRect`, type `void __thiscall ...(ScrollInventoryPane *this, ScrollInventoryPart part, RectBounds *bounds)`, and repeatable comment `Computes the rectangle for one scrollbar part using orientation, range, drag, cursor, and shared extent state.`
- The complete frame may replace the byte-like `arg_0` with `part@+0x2c/4:ScrollInventoryPart`; the bounds row and every local/range/hash/xref remain protected. B005 performs no IDA mutation.

## Negative Evidence And Score

- No evidence supports output initialization on invalid origin, local ownership of the extent table, reversed part semantics, or simplified geometry.
- Completion `90` / confidence `92`: exact range/hash, 13 callers, all branches/fields, corrected EventMan/inherited-bounds dependencies, source route and formal source are complete; validator command `000000024570` completed coherent generated CPP/H and dependency verification. Only exact original spellings and the absent rebuilt-binary comparison retain the source-complete cap.

## Cross-References

- [UID:0000CK][ScrollInventoryPane](by-class/ScrollInventoryPane.md)
- [UID:0000NF][ScrollBar](by-file/ScrollBar.md)
- [UID:0001W0][ScrollInventoryPaneLayout](by-type/by-struct/ScrollInventoryPaneLayout.md)

## Changes

- 2026-08-15 B005 accepted UID0000KB implementation callback: created this exact geometry child at source position `140`.
- 2026-08-15 B005 source-contract callback: replaced the rejected `m_bounds` alias with inherited `m_visibleBounds` and bound cursor access to EventMan's normal header declaration; the prior `93/95` score is historical, the current score is `90/92`, and validator command `000000024570` completed coherent generated CPP/H verification. Rebuilt-binary comparison remains absent.
- Cursor acquisition uses the EventMan-owned `GetCursorPosition(Point *)` declaration; this page consumes that dependency without redeclaring EventMan locally.
