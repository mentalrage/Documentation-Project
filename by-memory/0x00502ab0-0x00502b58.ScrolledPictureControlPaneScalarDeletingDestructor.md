*** UID:0001AI | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CH | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Compiler-generated MSVC scalar-deleting destructor clone for ScrolledPictureControlPane; duplicates ordinary frame-table cleanup and Pane teardown, implements exact bit-1/bit-4 deletion ABI, returns complete this, and is reached only by the primary vtable plus two adjusted vtable thunks. Human source is the one virtual ordinary destructor in UID00019Y. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00502ab0-0x00502b58 ScrolledPictureControlPane Scalar Deleting Destructor

## Status

- Confidence: very strong for the exact range, bytes, ABI, local CFG, instruction behavior, vtable/thunk routes, source/compiler decomposition, and no-code disposition.
- Covered class: [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md)
- Likely source file: [UID:0000NH][ScrolledPictureControlPane](by-file/ScrolledPictureControlPane.md)
- Disposition: compiler-generated scalar-deleting-destructor clone; non-reconstructable and non-emitting as a standalone source item.
- Source cause: the one public virtual ordinary destructor emitted by [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md) through [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md).

## Function

| Range | Function | Notes |
| --- | --- | --- |
| `0x00502ab0-0x00502b58` | `ScrolledPictureControlPane::ScalarDeletingDestructor` | Resets the three `ScrolledPictureControlPane` vtables, destroys the owned image block at object offset `0x114`, tears down the base pane/control state, and conditionally deletes object storage when `(flags & 1) != 0` and `(flags & 4) == 0`. |

## Exact Boundary And Bytes

- Half-open function range: `[0x00502ab0,0x00502b58)`, size `0xa8` / 168 bytes.
- SHA256: `3bc9b5a6d6473fcb02f292bd85915664c7e142c020cf115a43ffae13f5ecab1f` over all 168 bytes read from mandatory live IDA MCP session `1feab207` on 2026-07-14.
- First 16 bytes: `55 8b ec 6a ff 68 00 68 5f 00 64 a1 00 00 00 00`.
- Last 16 bytes: `f4 64 89 0d 00 00 00 00 59 5e 8b e5 5d c2 04 00`.
- Predecessor `sub_502A70` occupies `[0x00502a70,0x00502aab)`. Five `cc` bytes `[0x00502aab,0x00502ab0)` are excluded alignment padding.
- Eight `cc` bytes `[0x00502b58,0x00502b60)` are excluded alignment padding. Successor [UID:0003LW][0x00502b60-0x00502c0d.ScrolledTextControlPaneScalarDeletingDestructor](by-memory/0x00502b60-0x00502c0d.ScrolledTextControlPaneScalarDeletingDestructor.md) begins at `0x00502b60`.
- IDA models no function at `0x00502aaf`, `0x00502b58`, or `0x00502b5f`. The page needs no split, merge, endpoint change, padding child, optional position, or nesting change.

## ABI And Receiver Model

- Calling convention: x86 `__thiscall` deleting-destructor ABI. The complete-object receiver arrives in `ecx` and is saved in `esi`.
- Explicit argument: one stack dword of compiler deletion flags. The body tests only `al`, but `retn 4` proves a four-byte stack slot.
- Return ABI: both exits move complete `this` from `esi` to `eax`; the return type is the complete-object `ScrolledPictureControlPane *` at the ABI level.
- Primary vtable cell `0x0061d890` enters the function directly. Secondary and tertiary cells enter through [UID:0001AD][0x00502550-0x00502566.ScrolledPictureControlPaneDestructorThunks](by-memory/0x00502550-0x00502566.ScrolledPictureControlPaneDestructorThunks.md), which subtract `0xa0` and `0xa4` before tail-jumping here.
- The three receiver views are inherited `ControlPane`/`Pane` facets. They are compiler layout evidence, not three source destructors.

## Complete Physical Instruction Inventory

IDA presents 58 instructions because its function view appends eight external SEH/security-handler instructions. The 168-byte physical target contains exactly these 50 instructions:

```text
502ab0 push ebp
502ab1 mov ebp, esp
502ab3 push 0FFFFFFFFh
502ab5 push offset SEH_5C03A0
502aba mov eax, large fs:0
502ac0 push eax
502ac1 push esi
502ac2 mov eax, ___security_cookie
502ac7 xor eax, ebp
502ac9 push eax
502aca lea eax, [ebp+var_C]
502acd mov large fs:0, eax
502ad3 mov esi, ecx
502ad5 push dword ptr [esi+114h]
502adb mov dword ptr [esi], 61D890h
502ae1 mov dword ptr [esi+0A0h], 61D8F8h
502aeb mov dword ptr [esi+0A4h], 61D928h
502af5 mov ecx, dword_67A744
502afb mov [ebp+var_4], 0
502b02 call 4D15D0h
502b07 mov ecx, esi
502b09 call 544580h
502b0e mov eax, [ebp+arg_0]
502b11 test al, 1
502b13 jz 502B44h
502b15 test al, 4
502b17 jnz 502B36h
502b19 push esi
502b1a call 4F4AC0h
502b1f add esp, 4
502b22 mov eax, esi
502b24 mov ecx, [ebp+var_C]
502b27 mov large fs:0, ecx
502b2e pop ecx
502b2f pop esi
502b30 mov esp, ebp
502b32 pop ebp
502b33 retn 4
502b36 push 154h
502b3b push esi
502b3c call 41B6A0h
502b41 add esp, 8
502b44 mov eax, esi
502b46 mov ecx, [ebp+var_C]
502b49 mov large fs:0, ecx
502b50 pop ecx
502b51 pop esi
502b52 mov esp, ebp
502b54 pop ebp
502b55 retn 4
```

The vptr stores, SEH registration, cookie handling, explicit `Pane::~Pane` call, deletion flags, storage-release call, return-this ABI, and epilogues are compiler lowering. None belongs as a handwritten statement in source C++.

## CFG And Flag Matrix

The physical body has five local blocks:

| Local block | Successors | Meaning |
| --- | --- | --- |
| `[0x502ab0,0x502b15)` | `0x502b15`, `0x502b44` | Compiler frame, ordinary destruction, bit-1 test. |
| `[0x502b15,0x502b19)` | `0x502b19`, `0x502b36` | Bit-4 test after bit 1 is known set. |
| `[0x502b19,0x502b36)` | none | Release object storage and return complete `this`. |
| `[0x502b36,0x502b44)` | `0x502b44` | Call no-op guard stub with `this`/`0x154`, then join common return. |
| `[0x502b44,0x502b58)` | none | No-delete/common return. |

IDA's older nine-block total additionally includes external blocks at `0x005c91f2`, `0x005f6800`, and zero-size terminate/frame-handler stubs at `0x005dce60`/`0x005c956c`. Those are not target bytes.

| Flags | Destruction | Storage behavior | Return |
| --- | --- | --- | --- |
| `(flags & 1) == 0` | frame table then base teardown | no free; bit 4 is not examined | complete `this` |
| `(flags & 1) != 0`, `(flags & 4) == 0` | same | `sub_4F4AC0(this)` releases storage | complete `this` |
| `(flags & 1) != 0`, `(flags & 4) != 0` | same | pushes `0x154` and `this` to one-byte `_guard_check_icall_nop@4`; no indirect call and no free | complete `this` |

The flag-4 path is not vector deletion, sized deletion, or a guarded indirect deletion. Callee `0x0041b6a0` is a direct one-byte `ret` function. Object size `0x154` is useful class-layout evidence only.

## Callees And Touched State

| Site / callee | Exact role | Source/compiler disposition |
| --- | --- | --- |
| `0x00502b02 -> 0x004d15d0` | [UID:0002P5][0x004d15d0-0x004d15fc.DestroyOwnedImageBlock](by-memory/0x004d15d0-0x004d15fc.DestroyOwnedImageBlock.md) `DestroyOwnedImageBlock` frees nested records at table `+0x08`, then the outer `ArchiveMetadataTable` from `this+0x114`. | Sole derived source statement is `DestroyOwnedImageBlock(m_frameTable)`. |
| `0x00502b09 -> 0x00544580` | Restores/tears down inherited Pane/ControlPane state. | Compiler-emitted base destructor call; never handwritten in the derived body. |
| `0x00502b1a -> 0x004f4ac0` | [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) MemoryMan-backed object storage release. | Compiler deleting-destructor mechanism; no source `operator delete` call. |
| `0x00502b3c -> 0x0041b6a0` | `_guard_check_icall_nop@4`, one-byte `ret`. | Compiler/runtime no-op; no source helper call. |

The load of `dword_67A744` into `ecx` before the `__stdcall` cleanup helper has no later source-visible use; the helper obtains its own memory manager. Preserve the load as binary evidence but do not invent a source field/global dependency from it.

## Reachability And Vtable Evidence

- Exact xrefs to `0x00502ab0`: code jumps at `0x00502556` and `0x00502561`, plus primary vtable data cell `0x0061d890`; there are no ordinary direct calls.
- A bounded instruction-operand query over `[0x00502000,0x00503000)` scanned 1203 instructions and found only those two jumps.
- Little-endian target VA bytes occur only at `0x0061d890`; target RVA bytes have zero hits. A generic prologue signature is non-unique and was rejected as identity evidence.
- Exact class vtable-data span is `[0x0061d88c,0x0061d930)`: primary locator/base `0x61d88c/0x61d890`, secondary locator/base `0x61d8f4/0x61d8f8`, tertiary locator/base `0x61d924/0x61d928`, paint cell `0x61d8d4 -> 0x4ffa60`, and timer cell `0x61d92c -> 0x4ff9e0`.
- Constructor, ordinary destructor, and this scalar wrapper all install the same three vtable bases at complete-object offsets `+0`, `+0xa0`, and `+0xa4`.

## Source Versus Compiler Decomposition

- Ordinary destructor `[0x004ff970,0x004ff9d7)` has SHA256 `45718a4c81f4bbb4cc88586b8165a98d61d223898576e6edb9915e14e63ea354` and performs the same three vptr writes, `m_frameTable` cleanup, and base teardown without deletion flags or storage release.
- The scalar wrapper duplicates that ordinary cleanup instead of calling the ordinary function in this build. Duplication is compiler lowering, not evidence for a second human destructor.
- Human source owns exactly one virtual `ScrolledPictureControlPane::~ScrolledPictureControlPane()` whose body calls `DestroyOwnedImageBlock(m_frameTable)`. The class declaration and ordinary definition regenerate equivalent deleting entries and adjustors.
- The wrapper therefore remains class-owned documentation but has `RECONSTRUCTABLE:FALSE`, blank emitters, and blank formal C++. This prevents both an Empty Emitter Marker and duplicate destructor source.

## Exact No-Code Proof

The formal block is intentionally blank. The target is reached only through deleting vtable entries; accepts compiler flags; returns complete `this`; duplicates the ordinary body; performs conditional storage deletion; includes SEH/cookie lowering; and is paired with compiler receiver-adjustor thunks. Handwritten C++ for this page would necessarily expose ABI artifacts or duplicate the ordinary destructor. The correct source cause is the virtual destructor declaration in [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md) and sole definition in [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md).

## Evidence Notes

- 2026-06-04 live IDA MCP reports `sub_502AB0` as `0x00502ab0-0x00502b58`.
- IDA decompilation resets vtables at `Block[0]`, `Block[40]`, and `Block[41]`, calls `sub_4D15D0(Block[69])` for owned image cleanup, calls `sub_544580(Block)` for base pane/control teardown, then conditionally calls `sub_4F4AC0(Block)` only when `(flags & 1) != 0` and `(flags & 4) == 0`.
- Live IDA xrefs show the primary vtable reference at `0x0061d890` and code refs from the two adjustor-thunk jumps at `0x00502556` and `0x00502561`.
- This function is the deleting-destructor companion to the non-deleting destructor at `0x004ff970-0x004ff9d7`, which performs the same image cleanup and base teardown without the delete flag path.
- 2026-06-07 A009 IDA MCP re-audit reconfirmed `sub_502AB0` as `0x00502ab0-0x00502b58`, size `0xa8`, with 9 basic blocks and 58 instructions.
- Current `xrefs_to` reports exactly the two adjustor-thunk jumps at `0x00502556` / `0x00502561` plus the primary vtable data ref at `0x0061d890`; there are no ordinary direct call sites to the body.
- Current disassembly confirms the exact vtable reset stores at `this+0x00`, `this+0xa0`, and `this+0xa4`, the owned-image cleanup argument load from `this+0x114`, the `dword_67A744` context load before `sub_4D15D0`, the base teardown call to `sub_544580`, and the delete-flag tests at `0x00502b11` and `0x00502b15`.
- The flag path is explicit: `(flags & 1) == 0` returns after teardown, `(flags & 1) != 0 && (flags & 4) == 0` calls `sub_4F4AC0(this)`, and the bit-4 path calls the direct one-byte no-op `_guard_check_icall_nop@4` with `this`/`0x154` before returning without storage release. There is no indirect call.
- IDA MCP decompiler export still presents a decompiler-shaped `sub_502AB0(_DWORD *Block, char flags)` form. This supports the deleting-destructor wrapper role but is not source-grade final C++.
- 2026-06-11 A007 Batch 161 live IDA refresh reconfirmed the same exact `0x00502ab0-0x00502b58` extent, size `0xa8`, thunk/vtable refs, vtable reset offsets, owned-image cleanup operand from `this+0x114`, `dword_67A744` context load, base teardown through `0x00544580`, and guarded delete through `0x004f4ac0`.
- B004 2026-06-18 revalidation: [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md) proves the ordinary destructor body releases `m_frameTable` / `this+0x114` through the ImageFrameTable destroy helper before compiler-lowered `Pane::~Pane` at `0x00544580`. This scalar deleting destructor remains target-specific ABI glue for the same class. First-draft handwritten destructor source lives with [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md); this page documents vtable restoration, base-teardown lowering, flag-driven delete behavior, and the link to the class destructor.

## Ownership Notes

A prior recovered label called the base teardown `TextButtonExControlPane::~Pane`; that feature owner is historical label pollution. Current Pane/ControlPane support and the complete-object/vtable model identify `0x00544580` as compiler-lowered inherited `Pane` teardown. Keep semantic ownership under [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md), while source emission remains suppressed for this compiler wrapper.

## Assignment Gate

Canonical ownership remains [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md), the narrowest semantic owner proven by all three class vtable views. Ownership is separate from output routing: this page is `92/94`, non-reconstructable, non-emitting, and blank by design. Source definitions route from [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md) through the class and [UID:0000NH][ScrolledPictureControlPane](by-file/ScrolledPictureControlPane.md).

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | The page now records the exact range/size/hash, boundary pads, all 50 physical instructions, five-local versus nine-IDA CFG accounting, ABI/return/receiver model, complete flag matrix, all four callees, exhaustive xref/pointer/vtable routes, ordinary-body comparison, field/helper roles, source/compiler decomposition, rejected source alternatives, and exact blank no-code disposition. |
| Confidence | 94 | Mandatory live MCP bytes, disassembly, CFG, xrefs, vtables, thunks, ordinary destructor, and callee bodies agree on compiler-only scalar-wrapper identity. Confidence stays below final audit because the stripped binary does not recover exact original source identifiers or compiler-version flags beyond the observed ABI. |

## Cross-References

- [UID:0001AD][0x00502550-0x00502566.ScrolledPictureControlPaneDestructorThunks](by-memory/0x00502550-0x00502566.ScrolledPictureControlPaneDestructorThunks.md)
- [UID:00019Y][0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore](by-memory/0x004ff7d0-0x004ffa9e.ScrolledPictureControlPaneCore.md)
- [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md)
- [UID:0000NH][ScrolledPictureControlPane](by-file/ScrolledPictureControlPane.md)
- [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md)

## Changes

- 2026-07-14 B003 UID0001AI source-quality callback:
  - Changed scores from `86/90` to `92/94`, `RECONSTRUCTABLE:TRUE` to `FALSE`, and `EMITTER_UIDS:0000CH` to blank; retained canonical owner 0000CH, blank optional position/formal C++, exact path/range/title, and `Nested:0`.
  - Added mandatory live session `1feab207` range/hash/bytes, complete physical instruction and CFG evidence, exact ABI/flag/callee/xref/vtable inventories, class-layout immediate, ordinary/source decomposition, no-code proof, negative evidence, historical corrections, and score rationale.
  - Corrected the historical indirect-guard interpretation: the flag-4 path directly calls one-byte `_guard_check_icall_nop@4`, performs no indirect call or storage release, and remains compiler-only ABI evidence.
  - Source ownership is explicitly one virtual ordinary destructor in UID00019Y; this page must never emit a duplicate scalar wrapper.

- 2026-06-04: Changed completion/confidence from `70/85` to `82/88`.
  - Before: the page had the destructor behavior but relied on older lookup/xref wording and retained generated-source caveats in the status.
  - After: scored higher because live IDA confirms the exact `0x00502ab0-0x00502b58` extent, three vtable resets, owned-image cleanup, base teardown, delete-flag branch, primary vtable ref, and two thunk jump callers.
  - Remaining uncertainty: base teardown naming and final class field names remain below the final C++ reconstruction bar.
- 2026-06-07 A009 Batch006 IDA audit:
  - Changed to: raised scores from `82/88` to `85/90`, retained blank `AUTOGEN_PARENT_UID`, and added current CFG/instruction count, exact thunk/vtable refs, vtable reset offsets, owned-image cleanup offset, delete-flag split, and parent-gate evidence.
  - Summary/evidence: IDA MCP reconfirmed `sub_502AB0` as a `0xa8`-byte function with 9 basic blocks, 58 instructions, xrefs from `0x00502556`, `0x00502561`, and `0x0061d890`, calls to `sub_4D15D0`, `sub_544580`, and `sub_4F4AC0`, and the `flags & 1` / `flags & 4` delete branch. Assignment remains blocked because candidate direct parents are below the corrected `85/85` gate.
- 2026-06-11 A007 Batch 161:
  - Changed to: raised completion from `85` to `86` and assigned parent [UID:0000CH][ScrolledPictureControlPane](by-class/ScrolledPictureControlPane.md).
  - Summary/evidence: live IDA reconfirmed the exact destructor extent, thunk/vtable refs, vtable reset stores, owned-image cleanup, base teardown, and delete-flag path; the class/file parents were refreshed to `85/88` and `88/85`, clearing the strict parent-side gate.
