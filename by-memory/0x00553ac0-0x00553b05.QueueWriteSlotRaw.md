*** UID:0001FP | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Queue::WriteSlot(const void *slot)
{
    GetMemoryMan()->MemmoveWrapper(
        static_cast<unsigned char *>(m_buffer) + (m_writeIndex * m_slotSize),
        slot,
        m_slotSize);

    if (m_writeIndex >= m_slotCount - 1) {
        m_writeIndex = 0;
    } else {
        ++m_writeIndex;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Queue write-slot helper now emits inferred `Queue::WriteSlot(const void *slot)` C++; copies caller slot bytes into the write slot, wraps `writeIndex`, and preserves raw not-a-function/no-route caveats. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00553ac0-0x00553b05 Queue WriteSlot Raw

## Status

- Disposition: reconstructable NexusTK project code with raw-function boundary caveat
- Entity kind: raw method-shaped helper
- Owner class: [UID:0000BF][Queue](by-class/Queue.md)
- Likely source module: [UID:0000MW][Queue](by-file/Queue.md)
- Source-facing method: inferred `Queue::WriteSlot(const void *slot)`. `QueueWriteSlotRaw` remains the documentation role/range name; `Raw` is not treated as original source spelling.
- Confidence: very strong for behavior, class ownership, slot layout, byte boundaries, copy-helper vocabulary, and first-draft source shape; still below final-audit confidence because IDA does not model the raw start as a function and no caller/name evidence proves the original source spelling or active reachability.

## Behavior

This helper writes one fixed-size record into the circular queue at `writeIndex`. It computes `m_buffer + m_writeIndex * m_slotSize`, copies `m_slotSize` bytes from the caller source pointer through `GetMemoryMan()->MemmoveWrapper`, then advances `m_writeIndex`, wrapping back to zero once it reaches `m_slotCount - 1`.

Layout evidence:

| Offset | Meaning |
| --- | --- |
| `+0x04` | slot byte size |
| `+0x08` | slot count/wrap limit |
| `+0x0c` | backing buffer |
| `+0x14` | write index |

The formal C++ uses `m_slotSize`, `m_slotCount`, `m_buffer`, and `m_writeIndex` as source-facing field names. These names are inferred from the constructor/read/write/empty helper access pattern, not from a recovered local `Queue` UDT.

## Boundary Notes

IDA does not currently model `0x00553ac0` as a function and reports no direct xrefs to this raw start. The bytes are aligned, method-shaped, and end at `retn 4` at `0x00553b02`.

2026-06-01 IDA MCP byte review corrects the end-exclusive range to `0x00553b05` because the `retn 4` instruction at `0x00553b02` is three bytes (`c2 04 00`). The following `0x00553b05-0x00553b10` span is `0xcc` alignment padding before [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md).

## Evidence

- IDA MCP `lookup_funcs 0x00553ac0` reports `Not a function`.
- IDA MCP raw `disasm 0x00553ac0` shows slot copy through memory-manager copy helper `0x00516220`, write-index arithmetic, wrap to zero, and `retn 4`.
- `0x00553ac6` calls `sub_516030`, then `0x00553ade` calls [UID:0000L7][MemoryMan](by-file/MemoryMan.md)-owned copy helper `sub_516220` with destination `buffer + writeIndex * slotSize`, source argument, and copy size `slotSize`.
- `0x00553ae3-0x00553afd` implements wrap handling: compare `slotCount - 1` against `writeIndex`, store zero at `+0x14` when at/after the limit, otherwise store `writeIndex + 1`.
- `xrefs_to` and `callers` still report no direct references to the raw start, matching the compact raw-helper treatment in the queue cluster.
- Current `source-3/simroot_v2/class_Queue.cpp` omits this helper.

2026-06-16 A001 live IDA MCP source-quality pass:

- Session/database: `c001_midiplayer_rdata_20260615`, module `NexusTK.exe`, input path `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, strings cache ready.
- `lookup_funcs` still reports `0x00553ab9`, `0x00553ac0`, and `0x00553b05` as `Not a function`; `0x00553b10` is also not a function because it is the next raw helper start.
- `xrefs_to 0x00553ac0` reports no cross-references. `find_bytes` found no absolute VA pointer bytes `C0 3A 55 00` and no RVA pointer bytes `C0 3A 15 00` for the raw start.
- `callees 0x00553ac0` reports `No function found`; the raw byte body still contains the internal calls at `0x00553ac6` to [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md) and at `0x00553ade` to [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md).
- `get_bytes 0x00553ab9 size 0x57` reconfirmed seven `0xcc` bytes before the prologue, the complete write-slot body, trailing `c2 04 00`, and eleven `0xcc` bytes before [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md).
- Modeled Queue support remains coherent: constructor `0x005539e0` stores `a2` at `this+0x04`, `a3` at `this+0x08`, allocates `a3 * a2`, and clears read/write indices; ordinary/scalar destructors and trace-data-flow to `0x00622d24` tie the same vtable to [UID:0000BF][Queue](by-class/Queue.md). The current class page is `91/92`, and the current [UID:0000MW][Queue](by-file/Queue.md) source-file page is `91/91`, preserving the direct class owner/emitter route.

2026-06-27 B013 accepted source-quality implementation:

- Current IDA MCP session/database: `b001_000241_20260627`, worker PID `23132`, IDB path `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready.
- `lookup_funcs` reports modeled Queue functions at `0x005539e0` (`sub_5539E0`, size `0x7c`), `0x00553a60` (`sub_553A60`, size `0x59`), and `0x00553b70` (`sub_553B70`, size `0x97`); it reports `0x00553ac0`, `0x00553b05`, `0x00553b10`, `0x00553b55`, `0x00553b60`, and `0x00553b6a` as `Not a function`.
- `decompile 0x00553ac0` fails because IDA has no function object at the raw start. Raw `disasm 0x00553ac0` shows a coherent one-argument `thiscall` method body: preserve `this` in `esi`, call [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md), compute `m_buffer + m_writeIndex * m_slotSize`, call [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) with destination/source/size, then wrap or increment `m_writeIndex`.
- Exact instruction evidence: `0x00553acb` loads `[esi+4]` as `m_slotSize`; `0x00553ace` loads `[esi+14h]` as `m_writeIndex`; `0x00553ad1` multiplies index by slot size; `0x00553ad5` pushes the caller source pointer from `[ebp+8]`; `0x00553ad8` adds the backing buffer from `[esi+0Ch]`; `0x00553ade` calls `sub_516220`; `0x00553ae3-0x00553afd` compares against `m_slotCount - 1`, wraps `+0x14` to zero, or stores `m_writeIndex + 1`.
- `get_bytes` reconfirms seven `0xcc` bytes at `0x00553ab9-0x00553ac0`, the exact 69-byte body at `0x00553ac0-0x00553b05`, and eleven `0xcc` bytes at `0x00553b05-0x00553b10`. `make_signature_for_range 0x00553ac0-0x00553b05` returned a unique exact-byte signature for this 69-byte body.
- `xrefs_to 0x00553ac0`, `xrefs_to 0x00553b10`, and `xrefs_to 0x00553b60` return zero incoming references. `find_bytes` found no little-endian absolute VA pointer bytes `C0 3A 55 00` and no RVA pointer bytes `C0 3A 15 00` for this raw start. A read-only local PE route scan found no dword VA/RVA pointer route and no direct `E8`/`E9` call or jump to `0x00553ac0`; a positive control found the scalar deleting destructor pointer at the Queue vtable slot.
- `entity_query` for `Queue|WriteSlot|ReadSlot|IsEmpty|Enqueue|Dequeue` finds Queue RTTI/vtable names and the Queue class string, but no recovered source symbol for `WriteSlot`, `ReadSlot`, `Enqueue`, or `Dequeue`.
- B013 resolves the formal source disposition: keep the direct class owner/emitter route and emit inferred `Queue::WriteSlot(const void *slot)` C++ for this exact child. The raw not-a-function/no-xref/no-pointer/no-direct-route/original-symbol caveats remain confidence caps, not blockers to first-draft C++ for this body.

## Source-Quality Decision

No IDA database metadata was changed. Defining a function or adding IDA-side comments at this raw start remains unnecessary for the documentation implementation: the docs preserve the raw not-a-function state, and source output is now handled through the formal reconstruction block.

`QueueWriteSlotRaw` remains a documentation role name. The accepted source-facing method name is inferred `Queue::WriteSlot(const void *slot)`, not a recovered original symbol. `WriteSlot` is the strongest current source name because the body writes exactly one fixed-size slot, lacks higher-level capacity/full-check behavior, and mirrors the sibling read-slot body without carrying the documentation-only `Raw` suffix into source.

Rejected source names and dispositions:

| Candidate | Reason rejected |
| --- | --- |
| `QueueWriteSlotRaw` / `Queue::WriteSlotRaw` | `Raw` documents the IDA boundary state; it is not plausible source API spelling. |
| `Queue::Write` | Possible but less precise, and no caller/string/symbol proves the shorter spelling. |
| `Queue::Push` / `Queue::Enqueue` | These imply higher-level queue operations and usually capacity/full semantics; this body only copies one fixed-size slot and advances the write index. |
| `Queue::Append` | Not supported by strings, callers, sibling naming, or behavior. |
| `sub_553AC0` or other IDA label | Rejected as reverse-engineering label, not source-facing C++. |
| No-code/metadata fallback | Rejected because the body is source-authored Queue code with a valid owner/emitter route and a complete first-draft C++ shape. |

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `87` | The page records exact bytes/bounds, adjacent padding, slot layout, copy/wrap behavior, direct owner/emitter support, current MCP not-a-function/xref/pointer checks, local PE direct-route negative evidence, MemoryMan helper vocabulary, rejected naming alternatives, and formal first-draft C++. |
| Confidence `90` | Confidence is strong because the raw bytes, Queue layout, accepted MemoryMan helper pages, unique range signature, and Queue class/file support agree. It remains below final-audit range because there is still no modeled function object, direct caller, pointer/branch reachability, local `Queue` UDT, or recovered original method symbol. |

## Raw Bytes

| Range | Bytes / role |
| --- | --- |
| `0x00553ab9-0x00553ac0` | Seven `0xcc` bytes after [UID:0001FO][0x00553a60-0x00553ab9.QueueDestructor](by-memory/0x00553a60-0x00553ab9.QueueDestructor.md). |
| `0x00553ac0-0x00553b05` | `55 8b ec 56 8b f1 ... 89 46 14 5e 5d c2 04 00`; complete raw write-slot body. |
| `0x00553b05-0x00553b10` | Eleven `0xcc` bytes before [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md). |

## Cross-References

- [UID:0001FN][0x005539e0-0x00553c07.Queue](by-memory/0x005539e0-0x00553c07.Queue.md)
- [UID:0000BF][Queue](by-class/Queue.md)
- [UID:0000MW][Queue](by-file/Queue.md)
- [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md)
- [UID:0000L7][MemoryMan](by-file/MemoryMan.md)

## 2026-08-11 Whole-File Reconciliation

- Exact raw boundary: `0x00553ac0-0x00553b05` is a complete `0x45`-byte method body ending in `c2 04 00`. IDA does not currently model the start as a function, but exact disassembly, unique bytes, stack cleanup, and field/call behavior support source-authored `Queue::WriteSlot(const void *slot)`. Seven preceding and eleven following `0xcc` bytes protect the ordinary-destructor and `ReadSlot` boundaries.
- Exact behavior and layout: the body copies `m_slotSize` bytes from caller `slot` to `static_cast<unsigned char *>(m_buffer) + m_writeIndex * m_slotSize`, then resets `m_writeIndex` to zero at the `m_slotCount - 1` boundary or increments it. The participating fields are `+0x04`, `+0x08`, `+0x0c`, and `+0x14` within the exact `0x18` Queue object; historical reversed size/count names remain rejected.
- Exact source route: UID0001FP retains owner/emitter UID0000BF and child position `30`. Its existing formal CPP remains unchanged, its H stays blank, and UID0000BF owns the CPP wrapper/H declaration. Whole-file order is constructor, ordinary destructor, this write method, `ReadSlot`, and `IsEmpty`; scalar-wrapper/vtable/RTTI/alignment are compiler output.
- Reachability and source-facing name: no direct xref/caller, stored VA/RVA pointer, or direct local transfer reaches this raw start. The accepted `WriteSlot` spelling is the highest-probability semantic source name; `Raw`, `Write`, `Push`, `Enqueue`, `Append`, and `sub_553AC0` remain rejected. File-level reachability is 90 percent dormant and 10 percent unresolved indirect/external route.
- Ownership and negative inventory: UID0000BF is the narrow method owner, UID0000MW is the `NexusTK/util/Queue.cpp` source root, and UID0001FN remains no-code range support. No extra Queue global/static/constant/resource/import/helper belongs to this body, and unrelated `BlackHole *` cleanup state is excluded. Stale Wave2/Wave3 material was ignored; older detailed investigations remain historical.

## Changes

- 2026-08-11 B010 accepted UID0000MW whole-file callback:
  - Preserved the formal `Queue::WriteSlot(const void *slot)` CPP byte-for-byte and kept its H channel blank.
  - Added exact raw-boundary/padding, behavior/layout, source order, ownership, dormant reachability, rejected names, and negative inventory required by CQM-028.
- 2026-06-27 B013 accepted source-quality implementation:
  - What existed before: metadata was `85/88`, owner/emitter already routed through direct class [UID:0000BF][Queue](by-class/Queue.md), but the formal C++ block was blank even though the target was reconstructable and emitted.
  - Changed to: `COMPLETION:87`, `CONFIDENCE:90`; owner/emitter/reconstructable/position remain `0000BF`, `TRUE`, `0000BF`, and `30`; formal C++ now emits inferred `Queue::WriteSlot(const void *slot)`.
  - Summary/evidence: accepted B013 report and current MCP session `b001_000241_20260627` confirmed the raw not-a-function state, exact 69-byte body, pre/post `0xcc` padding, `GetMemoryMan()` to `MemoryMan::MemmoveWrapper` copy call, `m_buffer + m_writeIndex * m_slotSize` destination, `m_slotSize` copy count, wrap/increment of `m_writeIndex`, no xrefs, no VA/RVA pointer route, no direct `E8/E9` route, Queue constructor/read/empty layout support, and no recovered original source symbol. `WriteSlot` is documented as inferred source-facing spelling; raw helper reachability and original symbol absence remain confidence caps, not no-code blockers.
- 2026-06-16 A001 low-confidence raw-helper refresh:
  - What existed before: metadata was `82/84`; the page had byte-level behavior but lacked the current raw-start reachability, pointer-pattern, parent/support, and IDA-metadata decision required by the supervisor assignment.
  - Changed to: metadata is `85/88`; A001 kept the then-current [UID:0000MW][Queue](by-file/Queue.md) route, while the later Queue source-quality review below records the current direct class owner/emitter route. Final C++ remains blank pending final method spelling and raw-helper reachability/source representation.
  - Summary/evidence: live IDA MCP database `c001_midiplayer_rdata_20260615` reconfirmed no function at `0x00553ac0`, no xrefs to the raw start, no absolute VA/RVA pointer-byte hits, exact raw bytes and padding, MemoryMan getter/copy-helper call sites, constructor/layout corroboration, Queue vtable support, and current class/file parent gates. No IDA DB metadata change was made.
- 2026-06-16 A002 review of pre-existing C001 owner-route collision edit:
  - Metadata owner/emitter changed from [UID:0000MW][Queue](by-file/Queue.md) to direct class [UID:0000BF][Queue](by-class/Queue.md), position `30`, because the class now clears the active `85/85` gate and `by-structure.md` requires the narrow semantic owner.
  - Live IDA session `a002_goal65_20260616` reconfirmed no function object, no xrefs, no absolute/RVA pointer-byte hits, and the same raw bytes/padding. A002 did not verify or retain an IDA-side source-quality comment for this raw start; `define_func`, rename, prototype, and type edits remain unjustified because no direct reachability or original method spelling is proven.
- 2026-06-01: Raised completion/confidence from `50/65` to `82/84`, marked reconstructable, and attached to [UID:0000MW][Queue](by-file/Queue.md).
  - Corrected the documented end-exclusive address from `0x00553b03` to `0x00553b05` because `retn 4` includes two immediate bytes after opcode `c2`.
  - Added raw byte evidence, exact copy/wrap instruction evidence, adjacent padding evidence, and the no-xrefs caveat.
  - C++ remains blank because IDA still treats this as a raw start and final source method naming is not proven.
