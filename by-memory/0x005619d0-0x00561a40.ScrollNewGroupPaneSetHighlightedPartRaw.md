*** UID:00042L | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:70 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollNewGroupPane::SetHighlightedPart(ScrollNewGroupPart part)
{
    RectBounds rect;
    const ScrollNewGroupPart oldPart =
        static_cast<ScrollNewGroupPart>(m_highlightPart);

    if (oldPart == part)
        return;

    if (oldPart != kScrollNewGroupPartNone) {
        GetPartRect(oldPart, &rect);
        InvalidateRect(&rect);
    }

    if (part != kScrollNewGroupPartNone) {
        GetPartRect(part, &rect);
        InvalidateRect(&rect);
    }

    m_highlightPart = static_cast<signed char>(part);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained ScrollNewGroupPane highlighted-part transition with exact old/new rectangle invalidation, inline-lowering proof, and class-local enum/field ABI. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005619d0-0x00561a40 ScrollNewGroupPane SetHighlightedPart Raw

## Status

- Disposition: retained, reconstructable `ScrollNewGroupPane` out-of-line source method. The current executable has no static entry route to this copy, but the active mouse handler contains two specialized inline lowerings of the same source semantics; this resolves the raw-start liveness question rather than treating no xrefs as evidence of glue or dead data.
- Direct owner and emitter: [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md).
- Source route: [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md) emits through [UID:0000JS][Group](by-file/Group.md).
- Parent split index: [UID:0001GS][0x005619d0-0x00561db0.ScrollNewGroupPaneRawAndInteractionHelpers](by-memory/0x005619d0-0x00561db0.ScrollNewGroupPaneRawAndInteractionHelpers.md).
- Source-facing method name: `SetHighlightedPart`, supported by the exact `ScrollPane`, `RankingEventScrollPane`, and `ScrollCollectionPane` homolog names and by the target's exclusive update of highlight rather than active/pressed state.

## Address Range And Boundary

- Covered range: `[0x005619d0,0x00561a40)`, exactly 112 bytes and 46 decoded instructions. The PE raw offset of the first byte is `0x160dd0`; the exact body SHA256 is `87DB25794833D0D69688ED035EE9DC9668533C2CB1A4899EA09F20B77B471FB2`.
- IDA status: supervisor catalog entry0369 now defines the exact function `[0x005619d0,0x00561a40)` as `ScrollNewGroupPane__SetHighlightedPart`, size `0x70`, with the accepted typed ABI, function-repeatable comment, and normalized rectangle local. The raw/no-function state recorded below is historical prestate.
- End boundary: the body returns at `0x00561a3d` with `retn 4`; the bytes `0x00561a3e-0x00561a40` are the two-byte return immediate `04 00` and are part of this child, not padding or a separate tail row.
- Predecessor boundary: the preceding `0x005619c7-0x005619d0` bytes are `0xcc` padding after [UID:0001GR][0x00561740-0x005619c7.ScrollNewGroupPanePartRect](by-memory/0x00561740-0x005619c7.ScrollNewGroupPanePartRect.md), outside this child.
- Successor boundary: [UID:00042M][0x00561a40-0x00561b00.ScrollNewGroupPaneBeginPartInteractionRaw](by-memory/0x00561a40-0x00561b00.ScrollNewGroupPaneBeginPartInteractionRaw.md) starts at `0x00561a40`.

## Behavior

The method changes the highlighted scrollbar part. It reads signed one-byte `m_highlightPart` at `this + 0x103` and returns without invalidation or a store when the requested value is unchanged. Otherwise it resolves and invalidates the old non-none rectangle first (`0x005619f1-0x00561a09`), resolves and invalidates the new non-none rectangle second (`0x00561a0c-0x00561a25`), then stores the new value once at `0x00561a28`. The return contract is `void`; there is no result state.

## Evidence

- B008 MCP recheck on 2026-06-26 used active session `80de0a67`; `lookup_funcs 0x005619d0` returned `Not a function`. That result remains valid historical evidence, while the liveness inference below supersedes the earlier unresolved confidence-cap wording.
- Historical prestate had decoded code items for all 46 instructions but no function object at `0x005619d0`; the start had no name/type or address comments, and function comments/frame were not applicable. Catalog entry0369 durably replaces that prestate with exact name `ScrollNewGroupPane__SetHighlightedPart`, prototype `void __thiscall ScrollNewGroupPane__SetHighlightedPart(ScrollNewGroupPane *this, ScrollNewGroupPart part)`, blank address-regular/address-repeatable/function-regular channels, and function-repeatable comment `Retained ScrollNewGroupPane::SetHighlightedPart method. If the highlighted part changes, invalidates the old and new non-none part rectangles through GetPartRect and inherited InvalidateRect, then stores m_highlightPart at +0x103; active mouse handling contains constant-part inline lowerings while this out-of-line copy has no static entry route.`
- Durable target frame readback is `rect +0x8/0x10 struct RectBounds`, saved registers `+0x1c/4`, return address `+0x20/4`, and `part +0x24/4 ScrollNewGroupPart`. A predicted `var_4 +0x18/4` did not materialize and is not part of the saved frame.
- Catalog entry0369 saved the IDB as SHA256 `A4A6BC82D18D9BA7E1B0FDFCD87CED2B9FD4551E9AFEF3CE0175237B6DAC02C4`, 143,191,991 bytes, at `2026-07-30T06:48:02.4013060-04:00`; the byte-identical prestate backup is `E:\NTK\Resources\NexusTK\backups\NexusTK.exe.pre-B004-UID00042L-20260729_181833.i64`, SHA256 `64704F813788EA06F4C88861AC351EE128DFA76C59A01DAB1D0BFDFBC7E54642`.
- Incoming code/data xrefs to `0x005619d0` are zero. Exact pointer searches also found zero matches for VA bytes `d0 19 56 00`, RVA bytes `d0 19 16 00`, and raw-file-offset bytes `d0 0d 16 00`. Every interior branch-target xref originates inside this same body, so there is no hidden external interior entry.
- The wildcarded 112-byte method shape occurs exactly eight times: `0x0041e780` FittingRoom `SetHotPart`; `0x0045a990` RankingEvent `SetHighlightedPart`; `0x0055dea0` generic ScrollPane `SetHighlightedPart`; `0x00560520` ScrollSpellInventory `SetHoverPart`; this raw target; `0x00562e80` ScrollCollection `SetHighlightedPart`; `0x00564330` ScrollInventory setter; and `0x00565170` ScrollVolume `SetHoverPart`. Four starts are modeled source functions, including modeled zero-xref copies at `0x00562e80` and `0x00565170`, which disproves the rule that zero incoming xrefs make this shape compiler glue.
- Active [UID:000453][0x00560b00-0x00560ce1.ScrollNewGroupPaneHandleMouseEvent](by-memory/0x00560b00-0x00560ce1.ScrollNewGroupPaneHandleMouseEvent.md) contains a constant-thumb lowering at `[0x00560bfd,0x00560c3a)` and a constant-none clearing lowering at `[0x00560b5f,0x00560b87)`. These are specialized copies of this target's compare, old/new rectangle invalidation, and byte-store semantics. The compiler retained the general out-of-line member while inlining its currently visible uses.
- Accepted parent evidence records raw instructions reading `this + 0x103`, comparing with the stack part argument, calling `GetPartRect` at `0x005619fc` and `0x00561a18`, invalidating through inherited vtable slot `+0x20` at `0x00561a09` and `0x00561a25`, and storing the new highlight at `0x00561a28`.
- The helper uses the shared `ScrollNewGroupPart` enum and `kScrollNewGroupPartNone` sentinel documented on [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md).

## Touched State And Calls

| State / call | Role |
| --- | --- |
| `m_highlightPart` at `this + 0x103` | Signed one-byte old/new highlighted-part storage; adjacent `m_activePart` begins at `+0x104`, and `0xff` is enum sentinel `-1`. |
| Explicit stack parameter | Four-byte `ScrollNewGroupPart`; the low byte is stored, but `retn 4` and modeled homolog frames prove the source parameter is not a one-byte scalar. |
| `GetPartRect(ScrollNewGroupPart, RectBounds *)` at `0x00561740` | Resolves old/new part rectangles at target callsites `0x005619fc` and `0x00561a18`; the helper currently has thirteen code xrefs. |
| `InvalidateRect(RectBounds *)` via inherited vtable slot `+0x20` | Repaints rectangles affected by the highlight transition at `0x00561a09` and `0x00561a25`. |

## Reconstruction Notes

The exact source ABI is `void __thiscall ScrollNewGroupPane::SetHighlightedPart(ScrollNewGroupPart part)`: ECX is the class receiver, one four-byte enum is passed on the stack, and `retn 4` performs callee cleanup. `ScrollNewGroupPart` is a four-byte enum with `kScrollNewGroupPartNone = -1`, leading `0`, page-before-thumb `1`, thumb `2`, page-after-thumb `3`, and trailing `4`. The state member remains signed one-byte storage at `+0x103`; those are distinct source-level type decisions.

The formal C++ block is retained exactly because it preserves the early return, old-before-new invalidation order, sentinel handling, explicit byte-field casts, and final store. The target H channel remains blank because the complete class declaration belongs to the eventual [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md) header audit. The target stays an exact unsplit child; `0x00561a3e-0x00561a40` is part of the `retn 4` instruction, not padding.

The strongest source route is [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md) through [UID:0000JS][Group](by-file/Group.md). Stale generated `TextEditPane` ownership is rejected: RTTI, class-local `+0x103/+0x104` state, `GetPartRect`, the inherited invalidation callback, and the `NewGroupPane` family route all bind this method to `ScrollNewGroupPane`. Compiler glue, dead data, covered-by-only emission, shared generic ownership, and merging with UID00042M are also rejected.

Supervisor catalog entry0369 completed the bounded IDA handoff. It created the four-byte `ScrollNewGroupPart` enum, defined and named this exact function, applied the class/enum prototype and repeatable behavior comment, and normalized the complete 16-byte rectangle local to `RectBounds`. Readback preserved the target body SHA256 `87DB25794833D0D69688ED035EE9DC9668533C2CB1A4899EA09F20B77B471FB2`, zero incoming start xrefs, branches, helper calls, both fences, and the separate successor at `0x00561a40`. The companion `GetPartRect` handoff was also applied without changing its range, thirteen incoming code xrefs, nonargument locals, interior comments, or callees.

## Cross-References

- [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md)
- [UID:0001GS][0x005619d0-0x00561db0.ScrollNewGroupPaneRawAndInteractionHelpers](by-memory/0x005619d0-0x00561db0.ScrollNewGroupPaneRawAndInteractionHelpers.md)
- [UID:0001GR][0x00561740-0x005619c7.ScrollNewGroupPanePartRect](by-memory/0x00561740-0x005619c7.ScrollNewGroupPanePartRect.md)
- [UID:00042M][0x00561a40-0x00561b00.ScrollNewGroupPaneBeginPartInteractionRaw](by-memory/0x00561a40-0x00561b00.ScrollNewGroupPaneBeginPartInteractionRaw.md)

## Changes

- 2026-07-30 B004 UID00042L post-IDA reconciliation:
  - Replaced current raw/no-function wording with catalog entry0369's durably saved `ScrollNewGroupPane__SetHighlightedPart` function, exact typed ABI, function-repeatable comment, and physical frame.
  - Recorded the authoritative IDB save `A4A6BC82...AC02C4`, preserved `64704F81...E54642` as byte-identical prestate/backup, and explicitly rejected the predicted but absent `var_4 +0x18/4` frame member.
  - Preserved all source behavior, clone/liveness evidence, score/route metadata, formal CPP, and blank H.
- 2026-07-29 B004 UID00042L accepted ordinary callback:
  - Raised the page from `87/89` to `92/94`; owner/emitter [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md), reconstructable state, emitter position `70`, exact formal CPP, and blank H are unchanged.
  - Resolved the former raw/no-xref blocker through the eight-copy clone family and active constant-thumb/constant-none inline lowerings in UID000453. The valid B008 raw-start evidence is preserved, while its earlier unresolved-liveness wording is historicalized.
  - Added the exact 112-byte/46-instruction boundary, body SHA, PE raw offset, pointer-route and interior-entry negatives, enum/field ABI, method/helper/callback naming rationale, stale TextEditPane rejection, and supervisor-owned IDA handoff.
- 2026-06-26 B008 implementation: created from the former `0001GS` aggregate split. Metadata set to `87/89`, owner/emitter [UID:0000CL][ScrollNewGroupPane](by-class/ScrollNewGroupPane.md), emitter position `70`, and formal C++ inserted for `SetHighlightedPart`.
