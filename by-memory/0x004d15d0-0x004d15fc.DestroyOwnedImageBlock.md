*** UID:0002P5 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000K1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000K1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void DestroyOwnedImageBlock(ArchiveMetadataTable *block)
{
    GetMemoryMan();

    if (block->records != NULL) {
        FreeBufferMemory(block->records);
    }

    FreeBufferMemory(block);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ImageFrameTable cleanup helper at `0x004d15d0-0x004d15fc`; frees the nested frame-record payload pointer at table `+0x08` when present, then frees the outer table block through MemoryMan helpers, with eight destructor-family callers tied back to `LoadImageFrameTable` construction. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004d15d0-0x004d15fc DestroyOwnedImageBlock

## Status

- Address range: `0x004d15d0-0x004d15fc` half-open.
- Entity kind: global helper / shared cleanup helper.
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: `source-authored`.
- Current IDA name: `sub_4D15D0`.
- Proposed source-facing name: `DestroyOwnedImageBlock`; `DestroyOwnedImageBlock_4D15D0` is a tracking alias, and `DestroyFrameImageBlock` remains an alternate only if later frame-table type-name evidence proves that narrower noun.
- Confidence: strong for range, behavior, constructor/destructor pairing, source-file owner, and first-draft helper body; medium-high for final helper/type noun.
- Parent decision: assigned to [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md), position `20`. The helper is the destructor-side companion for blocks returned by `LoadImageFrameTable`; live IDA constructor/destructor evidence now clears the corrected child/direct-parent 85/85 gate.

## Boundary Evidence

IDA MCP `lookup_funcs` on 2026-06-01 reports `sub_4D15D0` at `0x004d15d0` with size `0x2c`, ending at `0x004d15fc`.

The preceding `0x004d15c5-0x004d15d0` span and following `0x004d15fc-0x004d1600` span are `0xcc` alignment padding. This function is a real code range inside the older aggregate span and must not be swallowed by the `LoadImageFrameTable` or `LoadFrameDrawRecord` pages.

Live IDA MCP on 2026-06-06 reconfirms the boundary: `0x004d15cf` is not inside a function, `0x004d15d0-0x004d15fc` is `sub_4D15D0`, `0x004d15fb` is still inside the helper, `0x004d15fc-0x004d1600` is padding, and `0x004d1600` starts [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md). A byte audit found eleven `0xcc` bytes before the helper and four `0xcc` bytes after it.

Agent-A002 Batch 005 live IDA MCP refresh on 2026-06-07 again reports `sub_4D15D0` as `0x004d15d0-0x004d15fc` with size `0x2c`, four following `0xcc` bytes before `sub_4D1600`, and tail bytes ending in `ret 4`. No endpoint or padding change was found.

## Behavior

IDA decompilation on 2026-06-01:

```c
int __stdcall sub_4D15D0(void **Block)
{
  sub_516030();
  if ( Block[2] )
    sub_516170(Block[2]);
  return sub_516170(Block);
}
```

The helper initializes/obtains the memory manager, frees the owned payload pointer at `block + 0x08` when present, then frees the outer block. Existing generated names describe this as destroying an owned image block; IDA confirms that the helper is shared by destructors that own image/frame resources.

Live disassembly on 2026-06-06 shows the exact control flow:

- call `0x00516030` once and keep the returned memory-manager/context pointer in `esi`;
- load the input block pointer into `edi`;
- test `*(block + 0x08)`;
- when the nested payload pointer is non-null, call `0x00516170` with that nested pointer using the same context pointer in `ecx`;
- always call `0x00516170` on the outer block pointer;
- return with `ret 4`.

## Callers

IDA MCP `callers` on 2026-06-01 reports direct calls from:

- `0x004ff970` (`sub_4FF970`), the non-deleting `ScrolledPictureControlPane` destructor path;
- `0x00502ab0` (`sub_502AB0`), the [UID:0001AI][0x00502ab0-0x00502b58.ScrolledPictureControlPaneScalarDeletingDestructor](by-memory/0x00502ab0-0x00502b58.ScrolledPictureControlPaneScalarDeletingDestructor.md);
- `0x0055a6f0`, `0x0055a8e0`, and `0x0055ac50`, ordinary overlay effecter destructor paths;
- `0x0055bc90`, `0x0055be10`, and `0x0055bef0`, overlay effecter scalar-deleting destructor paths.

The caller set shows this is not a private helper for one UI class. It is shared cleanup for image/effect resources that contain an outer allocation plus a nested payload allocation.

The 2026-06-06 live caller set is unchanged and pins the exact call sites: `0x004ff9bb`, `0x00502b02`, `0x0055a743`, `0x0055a933`, `0x0055aca3`, `0x0055bcea`, `0x0055be6a`, and `0x0055bf4a`. The first pair belongs to `ScrolledPictureControlPane` teardown, while the remaining six are ordinary and scalar-deleting overlay-effecter destructor variants. That split is why this page keeps source placement open instead of attaching directly to [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md).

The 2026-06-07 Batch 005 refresh reconfirmed those same eight direct call sites and caller functions: `sub_4FF970`, `sub_502AB0`, `sub_55A6F0`, `sub_55A8E0`, `sub_55AC50`, `sub_55BC90`, `sub_55BE10`, and `sub_55BEF0`.

The 2026-06-11 A003 owner audit ties the destructor callers back to frame-table construction:

- `ScrolledPictureControlPane::ScrolledPictureControlPane` (`0x004ff7d0`) calls `sub_4D0F50(Source, 0)`, stores the returned block at object offset `+0x114`, calls `sub_4D1600` to fill the first draw record, and its destructor/scalar-deleting destructor pass `this[69]` (`+0x114`) to this helper.
- `OverlayFrameImageEffecter`, `OverlayImageOnPointEffecter`, and `OverlayMovingImageEffecter` constructors (`0x0055a620`, `0x0055a7d0`, `0x0055ab00`) each call `sub_4D0F50(Source, 0)` and store the result at `+0x140`; their ordinary and scalar-deleting destructors pass `this[80]` (`+0x140`) to this helper.
- `WaterFilterEffecter` constructor (`0x0055b6a0`) also stores a `sub_4D0F50` result at `+0x10c`, but its teardown is not in this direct caller set; that confirms the field pattern is a frame-table block while keeping this page limited to the eight direct destroy callers.

That constructor/destructor pairing makes this helper a source-level destroy companion for the frame-table block produced by [UID:0002P4][0x004d0f50-0x004d15c5.LoadImageFrameTable](by-memory/0x004d0f50-0x004d15c5.LoadImageFrameTable.md), not a UI-control or effecter-owned private helper.

## Callees

IDA MCP reports calls to:

- `0x00516030`, the memory-manager accessor/initializer used by nearby allocation helpers;
- `0x00516170`, the freeing helper used for both the nested payload and outer block.

The 2026-06-07 refresh reconfirmed exactly one call to `0x00516030` and two calls to `0x00516170` inside this helper.

## Source Placement

The helper is adjacent to the ImageFrameTable loader/projection helpers and frees the allocation shape returned by `LoadImageFrameTable`. Although its direct callers live in `ScrolledPictureControlPane` and runtime effecter destructors, those callers are consumers releasing their stored frame-table pointers. The direct source owner is therefore [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md), which owns the loader, draw-record accessor, adjacent payload/rect copy helpers, and shared frame-table declarations.

## Touched State And Layout Evidence

| Field / dependency | Evidence | Interpretation |
| --- | --- | --- |
| `block + 0x08` / `Block[2]` | IDA decompilation frees this pointer when non-null before freeing the outer block. | Nested payload allocation owned by the outer image/frame resource block. |
| outer block pointer | Final free call releases `Block` itself. | Two-level allocation cleanup helper. |
| `0x00516030` | Callee evidence identifies memory-manager accessor/initializer. | Cleanup uses the same project memory helper family as nearby allocation paths. |
| `0x00516170` | Called for nested payload and outer block. | Shared free helper. Exact MemoryMan helper docs now make this sufficient for first-draft source emission. |

## Reconstruction Notes

- Keep `RECONSTRUCTABLE:TRUE` because this is source-authored project cleanup code with exact boundaries.
- `CANONICAL_OWNER` and `EMITTER_UIDS` remain [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md), between the load helper and draw-record accessor.
- Corrected assignment gate audit, 2026-06-11: this child page is now `85/90`, and direct parent [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md) is `89/86`; both sides clear the required 85/85 gate. [UID:0000NH][ScrolledPictureControlPane](by-file/ScrolledPictureControlPane.md) and [UID:0000IZ][Effects](by-file/Effects.md) remain consumers because their constructors store `LoadImageFrameTable` results and their destructors release those stored pointers.
- Emit the accepted first-draft formal C++. The behavior is tiny and clear, the owner route is now resolved, and the MemoryMan helper names used in the body match current source-facing support docs. Final helper/type nouns remain inferred but no longer block first-draft output.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `88` | The exact half-open range, neighboring padding, decompilation, disassembly-level free sequence, memory-manager context handling, nested payload offset, full direct caller set, callee set, constructor/destructor field pairing, direct source parent, and accepted first-draft C++ are now recorded with current evidence. |
| Confidence `92` | Live IDA lookup, byte audit, decompile, disassembly, callers, callees, and constructor checks agree that this frees frame-table blocks returned by `LoadImageFrameTable`. Confidence stays below final audit because the original helper/type noun is inferred rather than recovered from source symbols. |

## Open Questions

- Confirm the final original helper name. Existing generated evidence favors `DestroyOwnedImageBlock_4D15D0`, but the exact source-level noun may have been `FrameTable`, `ImageBlock`, or a resource-specific type.
- Confirm whether the final source type was named around `FrameRecordTable`, `ImageBlock`, or another image-resource noun.
- Allocator/free helper names are accepted source-facing names for this first-draft body; original helper/type noun remains inferred.

## Cross-References

- [UID:000176][0x004d0f50-0x004d165d.ImageFrameTable](by-memory/0x004d0f50-0x004d165d.ImageFrameTable.md)
- [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md)
- [UID:0002P4][0x004d0f50-0x004d15c5.LoadImageFrameTable](by-memory/0x004d0f50-0x004d15c5.LoadImageFrameTable.md)
- [UID:0002P6][0x004d1600-0x004d165d.LoadFrameDrawRecord](by-memory/0x004d1600-0x004d165d.LoadFrameDrawRecord.md)
- [UID:0001AI][0x00502ab0-0x00502b58.ScrolledPictureControlPaneScalarDeletingDestructor](by-memory/0x00502ab0-0x00502b58.ScrolledPictureControlPaneScalarDeletingDestructor.md)
- [UID:0000NH][ScrolledPictureControlPane](by-file/ScrolledPictureControlPane.md)
- [UID:0000IZ][Effects](by-file/Effects.md)

## Changes

- 2026-06-11 Agent-A003 Batch 143 owner audit:
  - What existed before: `COMPLETION:82`, `CONFIDENCE:88`; the page had exact cleanup behavior and caller evidence, but assignment remained blocked by child completion and the unresolved caller-spread ownership question.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:90`, `AUTOGEN_PARENT_UID:0000K1`, and `AUTOGEN_PARENT_POSITION_OPTIONAL:20`.
  - Summary/evidence: live IDA MCP rechecked the helper, eight destructor callers, and matching constructors. `ScrolledPictureControlPane` stores `LoadImageFrameTable` at `+0x114` and destroys `this[69]`; `OverlayFrameImageEffecter`, `OverlayImageOnPointEffecter`, and `OverlayMovingImageEffecter` store `LoadImageFrameTable` at `+0x140` and destroy `this[80]`. Direct parent [UID:0000K1][ImageFrameTable](by-file/ImageFrameTable.md) is already `89/86`, so the child and direct parent now clear the corrected 85/85 gate. Consumer files remain cross-references, not direct owners.

- 2026-06-07 Agent-A002 Batch 005 gate audit:
  - What existed before: `COMPLETION:80`, `CONFIDENCE:88`; the page had exact cleanup behavior and caller evidence, but not the corrected child-plus-parent 85/85 gate with current parent scores.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:88`; `AUTOGEN_PARENT_UID` and C++ remain blank.
  - Summary/evidence: live IDA MCP `py_eval` reconfirmed `sub_4D15D0` at `0x004d15d0-0x004d15fc`, the `ret 4` cleanup tail, four following `0xcc` padding bytes, eight direct caller sites/functions, and the one memory-manager plus two free-helper calls. The completion increase reflects the added current gate audit and evidence refresh; assignment remains prohibited because this child is below 85 completion, candidate parents are below 85 confidence, and ownership still spans scrolled-picture and overlay-effecter teardown paths.

- 2026-06-02 low-completion pass:
  - What existed before: `COMPLETION:68`, `CONFIDENCE:82`, with parent intentionally blank.
  - Changed to: `COMPLETION:72`, `CONFIDENCE:84`; parent remains blank intentionally.
  - Summary/evidence: added explicit parent decision, touched-state/layout evidence, reconstruction notes, and sibling helper cross-references. Existing recorded IDA MCP evidence proves the tiny cleanup body, exact range, callers, and callees; IDA MCP was unavailable during this pass, so confidence remains conservative.
- 2026-06-06 A008 live IDA refresh:
  - What existed before: the page had correct high-level cleanup behavior but only prior IDA evidence and no disassembly-level sequence or exact live caller-site list.
  - Changed to: `COMPLETION:80`, `CONFIDENCE:88`; parent and C++ remain blank.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `decompile`, `disasm`, `callers`, `callees`, and byte `py_eval` reconfirmed `0x004d15d0-0x004d15fc`, eleven preceding and four following `0xcc` padding bytes, the memory-manager context reuse, nested `block+0x08` free, outer-block free, eight destructor call sites, and the two allocator/free callees.
