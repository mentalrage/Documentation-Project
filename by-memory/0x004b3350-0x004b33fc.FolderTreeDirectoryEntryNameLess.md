*** UID:000343 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace {

static mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> >
MakeDirectoryEntryCompareName(
    const mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> >& name)
{
    mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > key(name);
    key.DetachWideIfShared();
    _wcsupr_s(const_cast<wchar_t *>(key.c_str()), key.length() + 1);
    return key;
}

static bool DirectoryEntryNameLess(const FolderTreePane::DirectoryEntry& lhs,
                                   const FolderTreePane::DirectoryEntry& rhs)
{
    mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > lhsName =
        MakeDirectoryEntryCompareName(lhs.name);
    mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> > rhsName =
        MakeDirectoryEntryCompareName(rhs.name);

    return mystr::WideRangeCompare(
               lhsName.c_str(), lhsName.c_str() + lhsName.length(),
               rhsName.c_str(), rhsName.c_str() + rhsName.length()) < 0;
}

} // namespace
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: File-local FolderTreePane directory-entry name-less comparator with formal first-draft C++; compares 12-byte scratch-record names by copied, detached, uppercased wide-string keys and WideRangeCompare, with current MCP boundary/caller/callee evidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3350-0x004b33fc FolderTreeDirectoryEntryNameLess

## Status

- Disposition: reconstructable NexusTK source helper.
- Entity kind: directory-entry name comparison helper used by the folder-tree child-entry sort path.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: attached to [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) as a file-local helper. The helper is not a class method; its only ordinary callers are inside the local directory-entry sort dispatcher at `0x004b44c0`.
- Rebuild handling: `source-authored`; this page now emits the local comparison predicate over the 12-byte temporary directory-entry record used by `FindFirstVisibleChild` / `ExpandNode`.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b3350-0x004b33fc` | `FolderTreeDirectoryEntryNameLess` | Copies/normalizes two directory-entry string fields, compares their wide-string ranges, destroys both temporaries, and returns whether the first entry sorts before the second. |

## IDA Evidence

- 2026-06-10 IDA MCP `lookup_funcs 0x004b3350` reports `sub_4B3350`, size `0xac`, giving exact exclusive end `0x004b33fc`.
- 2026-06-10 IDA MCP `callers 0x004b3350` reports only three call sites in `sub_4B44C0` at `0x004b44d0`, `0x004b44ea`, and `0x004b4504`. That dispatcher is the local compare/swap helper used by the directory-entry sort helper at `0x004b4520`.
- 2026-06-10 IDA MCP `callees 0x004b3350` reports string helpers `0x004b5c40`, `0x00583450`, `0x005840f0`, and `0x00582b70`. The `0x004b5c40` callee now has exact child documentation at [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md).
- 2026-06-10 IDA MCP decompilation shows two temporary string objects: it calls `0x004b5c40` / [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md) for each input record, obtains string length/metadata through `0x00583450`, compares the resulting UTF-16 ranges through `0x005840f0`, destroys both temporaries with `0x00582b70`, and returns `compareResult < 0`.
- The caller-only path through `0x004b44c0` and onward through `0x004b4520`/`0x004b5280` ties this helper to the folder-tree directory-entry sort used during child enumeration in `FolderTreePane::FindFirstVisibleChild`, not to a standalone common library.

2026-06-28 B013 implementation evidence refresh:

- Live IDA MCP session/database `agent_b009_0002my_20260628` was used read-only. `server_health` returned `status:ok` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` reported `0x004b3350` as `sub_4B3350`, size `0xac` / 172 bytes (Verified with int_convert.py); `0x004b33fc` is not a function. `get_bytes` showed the predecessor return and `0xcc` padding before `0x004b3350`, the target body ending before `0x004b33fc`, four `0xcc` padding bytes at `0x004b33fc-0x004b3400`, and the next function start at `0x004b3400`.
- `xrefs_to 0x004b3350` reported exactly three ordinary code references, all from [UID:00034A][0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3](by-memory/0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3.md) at `0x004b44d0`, `0x004b44ea`, and `0x004b4504`.
- The live route is `0x004b1d50` (`FindFirstVisibleChild` / `ExpandNode`) -> `0x004b5280` sort loop -> `0x004b4520` unguarded partition -> `0x004b44c0` compare/swap3 -> this comparator. `xrefs_to 0x004b5280` includes the expansion call at `0x004b214e` plus recursive sort calls inside `0x004b5280`.
- `callees 0x004b3350` reported `0x004b5c40`, `0x00583450`, `0x005840f0`, and `0x00582b70`. Disassembly confirms `0x004b3376` normalizes the second source parameter, `0x004b3384` normalizes the first source parameter, `0x004b339f` fast-paths equal normalized string buffers to false, `0x004b33c9` calls the wide-range compare with first begin/end followed by second begin/end, and `0x004b33d6`/`0x004b33de` destroy both temporaries.
- [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md) copy-constructs the source name through `0x00582a90`, detaches/makes the copy writable through `0x005830c0`, reads length metadata through `0x00583450`, uppercases `length + 1` wide characters through `0x00582980`, and returns the comparison key. `xrefs_to 0x004b5c40` found 26 references, all in the folder-tree directory-entry sort neighborhood.
- `type_query` for `*DirectoryEntry*`, `*FolderTreePane*`, and `*TreeElem*`, plus `search_structs` for `DirectoryEntry`, `FolderTreePane`, and `TreeElem`, found no recovered original UDTs. That leaves `FolderTreePane::DirectoryEntry` and exact `SimpleUString`/`StringBase` spelling inferred, but it no longer blocks formal first-draft C++.

## Ownership And Parent Gate

The direct semantic owner is the `FolderTreePane.cpp` source module rather than a class page. This is a free/file-local predicate over temporary directory-entry records, and no evidence shows an owning class/type narrower than the file. [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) currently scores `89/85` and documents the child-directory scratch vector/sort helpers as proposed file contents, so it clears the strict direct-parent gate for this child.

The adjacent aggregate [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md) remains a mixed split/container page and is not the direct autogen parent.

## Reconstruction Notes

- Model the record as the same 12-byte temporary directory-entry shape used by the vector/sort helpers: string handle at offset `+0x00`, two copied words at `+0x04/+0x06`, and flag bytes at `+0x08/+0x09`.
- Treat [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md) as the exact compiler-emitted comparison-key normalizer for this predicate. It copy-constructs each candidate name, detaches/mutates the copied wide buffer, and uppercases it before this helper compares the normalized ranges.
- Emit the formal file-local comparator C++ in this page. Missing original UDT and helper spellings are now documented inference limits, not a reason to keep a blank emitter. The accepted source-facing shape is a file-local `DirectoryEntryNameLess(const FolderTreePane::DirectoryEntry&, const FolderTreePane::DirectoryEntry&)` plus a local `MakeDirectoryEntryCompareName` key builder.
- The formal block uses `mystr::StringBase<wchar_t, mystr::mychar_traits<wchar_t> >`, `DetachWideIfShared`, `_wcsupr_s`, `c_str()`, `length()`, and `WideRangeCompare` because accepted string support docs already use that vocabulary for this pointer-backed wide-string family. UI-facing prose may still describe the field as the `SimpleUString` facade.
- Rejected implementation shapes: blank emitter; marker-only covered by [UID:000155][0x004b1b90-0x004b32c9.FolderTreePaneCore](by-memory/0x004b1b90-0x004b32c9.FolderTreePaneCore.md); public `FolderTreePane` method; C++11 local lambda-only source; ownership by 36-byte `TreeNode<FolderTreePane::TreeElem>` / `TreeStorage` records; and treating this predicate as non-reconstructable sort machinery. The sibling sort pages remain compiler/container support, but this page is the source ordering policy they invoke.

## 2026-07-29 DirectoryEntry Versus TreeNode Reconciliation

- This comparator continues to operate exclusively on 12-byte `FolderTreePane::DirectoryEntry` scratch values collected before persistent tree insertion. Its formal comparator, metadata, owner/emitter route, and `90/91` score are unchanged.
- The compared persistent family is now named precisely: expansion eventually copies each sorted scratch value into `TreeNode<FolderTreePane::TreeElem>::data`, but the complete persistent node is 36 bytes and is not an input to this comparator.
- Historical assumption retained: older cross-family wording called the compared persistent object a 36-byte `TreeElem`. The width distinction was valid, but the source-facing label conflated the complete generic node with its nested 12-byte payload; `TreeNode<FolderTreePane::TreeElem>` is the current term.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | Exact range, padding, caller chain, callee roles, sort context, 12-byte scratch-record layout, direct file owner/emitter, current MCP evidence, rejected alternatives, and formal first-draft comparator C++ are documented. Completion remains below final audit because original `DirectoryEntry` placement and string facade spelling are inferred rather than symbol-proven. |
| Confidence | 91 | Current MCP confirms the boundary, decompile/disassembly semantics, caller chain, normalizer behavior, sort route, and negative IDA UDT evidence. Existing FolderTreePane/StringBase docs corroborate the `DirectoryEntry`, detach, uppercase, `c_str()`, `length()`, and `WideRangeCompare` vocabulary. Confidence stays below final audit because no PDB/source symbol proves the original comparator or type spelling. |
| Reconstructable | true | The helper is NexusTK-owned folder-tree sort support and is now represented by formal file-local comparator C++. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis names the compared records as local `FolderTreePane::DirectoryEntry` / file-local `DirectoryEntry` scratch records, not `TreeElem` records and not public class objects. Each entry is 12 bytes: `name` at `+0x00`, `row` / `rowIndex` at `+0x04`, `depth` / `depthOrIndent` at `+0x06`, `childrenLoaded` at `+0x08`, and `expanded` at `+0x09`. This helper is the source-level less-than predicate used by the local sort expression inside `ExpandNode` / current alias `FindFirstVisibleChild`.

Source reconstruction now models this as a file-local static comparator for sorting directory entries by name. The helper normalizes/copies the two names, compares wide ranges, and destroys temporaries; it is not a handwritten public `FolderTreePane` method and is no longer blocked by inferred local type/string-facade names.

## 2026-06-28 B013 Empty-Emitter Implementation

Accepted B013 report `000343-FolderTreeDirectoryEntryNameLess-empty-emitter-source-quality.md` resolves the stale blank-C++ rationale. UID000343 is not marker-only: it now carries the formal `DirectoryEntryNameLess` comparator body, while neighboring UID000349/00034A/00034B/00034C/00034D/00034E pages remain sort/container support. The source placement is file-local in `FolderTreePane.cpp`, not a public `FolderTreePane` method, and the comparator is the concrete source anchor for [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md).

The accepted remaining uncertainty is naming, not behavior: no recovered IDA UDT proves the original `FolderTreePane::DirectoryEntry` declaration placement, no PDB/source symbol proves the exact `DirectoryEntryNameLess` spelling, and the project-wide `SimpleUString` facade versus `StringBase` spelling remains broader string-header work. Those caveats cap the score below `95+` but do not justify an empty emitter.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:00022E][0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers](by-memory/0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers.md)
- [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md)

## Changes

- 2026-06-28 B013 empty-emitter implementation:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`, owner/emitter [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), blank formal C++, and prose saying final C++ should not emit yet because type/helper names were inferred.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`, owner/emitter unchanged at [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), and formal first-draft `DirectoryEntryNameLess` comparator C++.
  - Evidence: B013 current MCP session/database `agent_b009_0002my_20260628` confirmed the exact `0x004b3350-0x004b33fc` boundary, three callers from [UID:00034A][0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3](by-memory/0x004b44c0-0x004b451b.FolderTreeDirectoryEntryCompareSwap3.md), callee set, parameter-order disassembly, sort route through `0x004b5280`, [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md) behavior, and negative UDT evidence. Rejected alternatives preserved: blank emitter, marker-only, public method, lambda-only, `TreeElem`/`TreeStorage` ownership, and non-reconstructable sort-internal classification.
- 2026-06-26 B009 [UID:00022E][0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers](by-memory/0x004b5c40-0x004b5efc.FolderTreePaneVectorSupportHelpers.md) support sync:
  - Score unchanged.
  - Linked exact normalization child [UID:000446][0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare](by-memory/0x004b5c40-0x004b5cbc.FolderTreeDirectoryEntryNormalizeNameForCompare.md) for the `0x004b5c40` callee and documented its role as the comparison-key normalizer used by this less-than predicate.
- 2026-06-10 B001-028 split repair:
  - What existed before: this exact source-bearing helper was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b3350-0x004b33fc`.
  - Summary/evidence: IDA MCP confirms the exact `0xac`-byte function, three callers in the sort compare/swap dispatcher, string-normalize/compare/destructor callees, and decompiled `compareResult < 0` behavior.
