*** UID:00017Z | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000076 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Two-instruction singleton-clear helper; current IDA MCP session `supervisor_recovery_20260705` is healthy at imagebase `0x400000` and confirms exact `0xb` `sub_4E5BB0` body bytes `c7 05 50 b4 69 00 00 00 00 00 c3`, one-block `unk_69B450 = 0` behavior, no ordinary callers/callees, one constructor cleanup jump at `0x006001d0`, exactly 19 `g_pLightObjImageLib` lifecycle/render xrefs, unique exact body signature, zero VA/RVA pointer-pattern matches to the helper start, five-byte `cc` padding on both sides, generated `LightObjImageLib.cpp` empty-marker routing, and cleanup-glue/no-handwritten-helper policy while final static-lifetime mechanism remains open. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004e5bb0-0x004e5bbb LightObjImageLib Singleton Clear Helper

## UID0001PR Accepted Singleton-Template Cleanup No-Code Proof - 2026-07-21

- This exact two-instruction range is compiler/template cleanup for the direct `Singleton<LightObjImageLib>` base, not a file helper or class API authored by the game source. It is semantically owned by [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) and intentionally has `RECONSTRUCTABLE:FALSE`, blank emitter/position, and blank formal C++.
- The complete source class now inherits `Singleton<LightObjImageLib>` directly. Constructor publication and base destruction regenerate the observed singleton write/clear path, including the cleanup jump from `0x006001d0`; no manual `ClearLightObjImageLibSingleton` function is needed or defensible.
- Exact liveness remains established by the constructor cleanup jump even though there are no ordinary callers. The body bytes, unique exact signature, five-byte padding on both sides, 19 global refs, and neighboring helper-island distinctions remain valid.
- The previous `RECONSTRUCTABLE:TRUE` file-emitter route and generated Empty Emitter Marker were documentation artifacts from before the direct Singleton source base was emitted. The source disposition is now resolved rather than deferred: this range must disappear from generated source while its effect is regenerated by template/base lifetime code.
- Evidence-session names below are time-scoped historical observations. They support the binary conclusion but do not assert that those old MCP sessions remain active.

## Status

- Confidence: strong for exact body, target global, boundary, constructor cleanup reachability, singleton-lifecycle xrefs, signature identity, generated empty-marker route, and pointer-pattern negatives; medium-high for final source spelling/static-lifetime provenance.
- Owner: [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md) / [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md).
- IDA function: `sub_4E5BB0`.
- Disposition: source-non-emitting compiler/template cleanup generated by the direct Singleton base; not a hand-authored source API.
- Autogen disposition: no emitter. The semantic owner is [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md), whose complete declaration regenerates the effect through base lifetime lowering.

## Behavior

| Range | IDA function | Raw body | Effect |
| --- | --- | --- | --- |
| `0x004e5bb0-0x004e5bbb` | `sub_4E5BB0` | `c7 05 50 b4 69 00 00 00 00 00 c3` | Stores `0` to [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md) and returns. |

This is one member of the image-library singleton-clear helper island near other asset-library teardown helpers. The helper is source-owned through the `LightObjImageLib` lifetime route, but the exact binary helper should not be promoted to a source-facing `ClearLightObjImageLibSingleton` API.

## Evidence

- Current B002 IDA MCP recheck on 2026-07-05 used active session `supervisor_recovery_20260705`; `server_health` call id `4` returned `status:ok`, `module:NexusTK.exe`, `imagebase:0x400000`, `auto_analysis_ready:true`, and `hexrays_ready:true`.
- Current IDA MCP `lookup_funcs` call id `5` reports `sub_4E5BB0` at `0x004e5bb0` with size `0xb`; `0x004e5bbb` is not a function; `0x004e5bc0` starts successor `sub_4E5BC0`; `0x004e5ba0` starts predecessor `sub_4E5BA0`; `0x004e5bab` is not a function; and `0x006001d0` is inside constructor function `sub_4DF7E0` size `0x2c6`.
- Current IDA MCP `decompile` call id `6` reduces the body to `unk_69B450 = 0`, matching the source-facing effect of clearing [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md) without proving a source-authored helper API.
- Current IDA MCP `analyze_function` call id `7` reports prototype `void()`, size `11`, one basic block, cyclomatic complexity `1`, no strings, no constants, no callees, no direct callers, one incoming code xref from `0x006001d0`, and outgoing refs to `0x004e5bba` and `0x0069b450`.
- Current IDA MCP `xrefs_to` call id `8` reports one code xref to this helper from `0x006001d0` in `sub_4DF7E0`. That cleanup location is a constructor/static cleanup jump, not an ordinary source callsite.
- Current IDA MCP `xrefs_to` call id `8` also reports exactly 19 xrefs to `0x0069b450`: constructor store/clear at `0x004df82b/0x004df832`, raw ordinary-destructor clear at `0x004dfb23`, this helper at `0x004e5bb0`, scalar deleting destructor clear at `0x004e665d`, shutdown read at `0x004f6642`, attached-object/light render reads at `0x005373fa`, `0x0053754b`, `0x00537618`, `0x00538162`, `0x005381f6`, `0x0053834d`, `0x005383e0`, `0x00538488`, `0x00538527`, `0x0053c6bc`, `0x0053c81d`, `0x0053c9c3`, and `0x0053c9f3`.
- Current IDA MCP `xrefs_to` call id `8` reports three refs to the LightObjImageLib vtable address `0x0061b754`: constructor `0x004df83c`, raw ordinary destructor `0x004dfab9`, and scalar deleting destructor `0x004e65ec`. These support the existing LightObjImageLib owner route but do not make this helper a class method.
- Current IDA MCP `disasm` call id `9` shows exactly two instructions: `mov dword ptr unk_69B450, 0` at `0x004e5bb0` and `retn` at `0x004e5bba`. `callees` call id `10` reports no callees.
- Current IDA MCP `get_bytes` call id `11` confirms the predecessor ItemObj helper body at `0x004e5ba0-0x004e5bab`, five `cc` bytes at `0x004e5bab-0x004e5bb0`, the exact UID00017Z body bytes `c7 05 50 b4 69 00 00 00 00 00 c3`, five `cc` bytes at `0x004e5bbb-0x004e5bc0`, and the successor MapTile helper body beginning at `0x004e5bc0`. The same call reads `0x0069b450` as `00 00 00 00` and bytes at `0x006001d0` as `e9 db 59 ee ff`, a relative jump to `0x004e5bb0`.
- Current IDA MCP `find_bytes` call id `12` finds the exact body pattern `C7 05 50 B4 69 00 00 00 00 00 C3` only at `0x004e5bb0`. The operand-wildcarded singleton-clear shape is non-unique across the helper island, so it is pattern evidence rather than a merge reason.
- Current IDA MCP `find_bytes` call id `12` finds zero absolute VA pointer-pattern matches `B0 5B 4E 00` and zero RVA pointer-pattern matches `B0 5B 0E 00` to the helper start. Reachability evidence is the cleanup jump, not a data pointer route.
- Current IDA MCP `make_signature_for_range` calls `13` and `14` report exact signature `C7 05 50 B4 69 00 00 00 00 00 C3` as unique and wildcard signature `C7 05 ? ? ? ? ? ? ? ? C3` as non-unique.
- Generated output after the B002 callback validators carries UID00017Z in `auto-generated/NexusTK/render/LightObjImageLib.cpp` as `Completion:86 | Confidence:91 | Empty Emitter Marker`. The generated route remains no-code.

### Historical Evidence

- IDA MCP `lookup_funcs` on 2026-06-06 reported `sub_4E5BB0` at `0x004e5bb0` with size `0xb`, so the exclusive end is `0x004e5bbb`. The same query reported `0x004e5bbb` is not a function and `0x004e5bc0` starts the next `0xb` singleton clear helper.
- IDA MCP `decompile` on 2026-06-06 reduced the body to `dword_69B450 = 0`.
- IDA MCP `disasm` on 2026-06-06 showed the complete two-instruction body: `mov dword_69B450, 0` at `0x004e5bb0`, followed by `retn` at `0x004e5bba`.
- IDA MCP/idautils on 2026-06-06 reported one code xref to this helper from `0x006001d0`, a `jmp sub_4E5BB0` inside a constructor-associated cleanup chunk that IDA attaches to [UID:0002IU][0x004df7e0-0x004dfaa6.LightObjImageLibConstructor](by-memory/0x004df7e0-0x004dfaa6.LightObjImageLibConstructor.md).
- IDA MCP `xrefs_to(0x0069b450)` on 2026-06-06 reported 19 direct xrefs, including constructor publish/null-clear stores at `0x004df82b/0x004df832`, raw destructor clear at `0x004dfb23`, this helper at `0x004e5bb0`, scalar deleting destructor clear at `0x004e665d`, shutdown read at `0x004f6642`, and light-mask render consumers in object/attachment paths.

## Reconstruction Notes

- Keep this attached to [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md), not the neighboring ItemObj/MapTile helper pages.
- The body is source-owned cleanup state because it mutates the project singleton, but the final source may express it through static cleanup/unwind glue rather than an explicitly named helper.
- 2026-06-17 B003 policy update remains current: exact helper naming is not the blocker. The source-form is generated cleanup glue: no `this`, no arguments, no callees, a single singleton write, a constructor cleanup jump, non-unique wildcard helper pattern, and zero VA/RVA pointer routes to a source helper address.
- Do not hand-emit `ClearLightObjImageLibSingleton`, `LightObjImageLib::ClearSingleton`, or a one-line file-local helper. The accepted LightObjImageLib source pass emits constructor/destructor/bounds/draw source methods and leaves generated cleanup wrappers blank.
- Do not treat the empty ordinary-caller result as proof that this helper is unused; `xrefs_to` identifies the cleanup jump at `0x006001d0`.
- Do not merge this range with the neighboring helper island; each `0xb` function writes a different singleton global, and UID00017Z is separated from UID00017Y and UID000180 by five-byte `cc` padding on both sides.
- Rejected alternatives: ItemObjImageLib, EPFLib/ImageLib, MapTileImageLib, Application shutdown, and attached-object/light render helpers are not owners. Neighboring image-library helpers are pattern evidence only, and runtime readers are consumers of `g_pLightObjImageLib`.

## Range And Negative Evidence

- Exact range remains `0x004e5bb0-0x004e5bbb`.
- Pre-target padding `0x004e5bab-0x004e5bb0` is five bytes of `cc` alignment after [UID:00017Y][0x004e5ba0-0x004e5bab.ItemObjImageLibSingletonBaseDestructor](by-memory/0x004e5ba0-0x004e5bab.ItemObjImageLibSingletonBaseDestructor.md), not part of this helper.
- Post-target padding `0x004e5bbb-0x004e5bc0` is five bytes of `cc` alignment before [UID:000180][0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonBaseDestructor](by-memory/0x004e5bc0-0x004e5bcb.MapTileImageLibSingletonBaseDestructor.md), not part of this helper.
- Negative source-body evidence: no `this`, no arguments, no callees, no branch structure, no strings, no constants beyond the singleton zero store, no ordinary source callers, and no absolute VA/RVA pointer-pattern matches to the target start.
- Negative ownership evidence: render and shutdown paths read the singleton but do not publish or clear it; global/storage docs own declaration/storage, not this executable cleanup thunk; adjacent helpers clear different singleton slots.

## Score Rationale

Completion is `93` because the exact body, range, cleanup-jump liveness, no-callee/no-ordinary-caller status, global target, full 19-reference family, padding, signatures, pointer-pattern negatives, direct Singleton hierarchy, and final no-code generation contract are resolved. Confidence is `96` because the direct base, PMD/EBO evidence, constructor cleanup jump, destructor clear parity, and sibling helper pattern identify the compiler/template mechanism; original internal compiler naming is irrelevant to source and no longer a blocker.

## Cross-References

- [UID:0000KP][LightObjImageLib](by-file/LightObjImageLib.md)
- [UID:000076][LightObjImageLib](by-class/LightObjImageLib.md)
- [UID:0000RD][g_pLightObjImageLib](by-global/g_pLightObjImageLib.md)
- [UID:0001PR][0x0069b450-0x0069b454.g_pLightObjImageLib](by-memory/0x0069b450-0x0069b454.g_pLightObjImageLib.md)
- [UID:00017Q][0x004dfab0-0x004dfb36.LightObjImageLibDestructor](by-memory/0x004dfab0-0x004dfb36.LightObjImageLibDestructor.md)

## Changes

- 2026-05-31 exact split pass:
  - What existed before: `RECONSTRUCTABLE` was blank and scores were `50/65`.
  - Changed to: `RECONSTRUCTABLE:TRUE`, `COMPLETION:72`, and `CONFIDENCE:86`.
  - Summary/evidence: IDA MCP verified exact function bounds, disassembly of `dword_69B450 = 0; ret`, and xrefs tying the helper to the LightObjImageLib singleton. Completion remains below near-final because original source placement for the static cleanup helper island is still not fully resolved.
- 2026-06-06 A010 live IDA refresh:
  - What existed before: the page had older summary evidence and did not list the current callee/caller/xref set.
  - Changed to: `COMPLETION:78` and `CONFIDENCE:90`.
  - Summary/evidence: IDA MCP confirmed the `0xb` boundary, the complete `mov/retn` body, no direct callers/callees, and the `g_pLightObjImageLib` lifecycle/render-consumer xrefs.
- 2026-06-06 A008 attachment/xref refresh:
  - What existed before: the page was `78/90` and unassigned; it did not record the constructor cleanup chunk jump to this helper.
  - Changed to: `COMPLETION:82`, `AUTOGEN_PARENT_UID:0000KP`, and evidence for the `0x006001d0` cleanup jump.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `callers`, `callees`, `xrefs_to`, and `py_eval`/`idautils` checks reconfirmed the `0xb` body, no ordinary callers/callees, 19 singleton xrefs, five-byte padding, and the constructor-associated jump that explains how this clear helper can be reached without a normal callsite.
- 2026-06-17 B003 source-quality audit: raised completion from `82` to `85` and made the no-code proof explicit: exact PE bytes `c7 05 50 b4 69 00 00 00 00 00 c3`, no `this`, no callees, constructor cleanup jump, singleton-only clear, accepted Light source-pass no-code precedent, file owner/emitter, and generated cleanup-glue policy.
- 2026-07-05 B002 source-quality implementation callback: raised target metadata from `85/90` to `86/91` without changing owner/emitter/reconstructable/nested/formal C++ state. Accepted report evidence used current IDA MCP session `supervisor_recovery_20260705`: `server_health` id `4`, `lookup_funcs` id `5`, `decompile` id `6`, `analyze_function` id `7`, `xrefs_to` id `8`, `disasm` id `9`, `callees` id `10`, `get_bytes` id `11`, `find_bytes` id `12`, and signature ids `13`/`14`. The refresh records exact unique signature versus non-unique wildcard helper pattern, zero VA/RVA pointer-pattern matches to the helper start, both five-byte padding spans, generated empty-marker routing, no-code cleanup-glue policy, and rejection of source helper/API, adjacent-helper merge, consumer ownership, and non-reconstructable alternatives.
