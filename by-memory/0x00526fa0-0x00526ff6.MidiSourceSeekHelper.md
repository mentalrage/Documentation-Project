*** UID:0002C1 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static DWORD MidiSeekSource(MidiDocument* document, LONG offset, DWORD origin)
{
    if (document->datFileSource != 0) {
        DWORD datOrigin = FILE_CURRENT;

        if (origin == FILE_BEGIN) {
            datOrigin = FILE_BEGIN;
        } else if (origin == FILE_END) {
            datOrigin = FILE_END;
        }

        document->datFile.Seek(offset, datOrigin);
        return document->datFile.Tell();
    }

    return SetFilePointer(document->fileHandle, offset, NULL, origin);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Seeks DATFile-backed or Win32 file-backed MIDI sources; current MCP confirms raw no-route/no-function state, exact padding, origin mapping, DATFile `Seek`/`Tell`, Win32 `SetFilePointer`, and first-draft `MidiSeekSource` readiness. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00526fa0-0x00526ff6 MidiSourceSeekHelper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw function-shaped helper
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for source-family ownership, exact raw bytes, seek-origin translation, embedded `DATFile` and Win32 branches, stream-vtable slot roles, imports, and adjacent padding; medium-high for direct reachability because IDA does not model it as a function and has no entry xrefs.
- Autogen status: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because the child and parent file now meet the >=80 confidence threshold.

## Score Rationale

The score was raised from `68/76` to `72/82` because the page tied the seek helper to the validated `MidiPlayer` source owner and recorded the shared source abstraction state used by neighboring source helpers.

Raised again to `80/88` on 2026-06-06 after live IDA MCP confirmed the exact raw body bytes, no modeled function at the start, no entry xrefs, adapter seek-origin translation, adapter seek/tell dispatch, Win32 `SetFilePointer` branch, and padding around the helper.

Raised to `86/89` on 2026-06-16 after read-only IDA MCP and support-doc review resolved the vague adapter/source abstraction to an embedded `DATFile` object at `this+0x18`: [UID:0003H3][DATFileLayout](by-type/by-struct/DATFileLayout.md) records `DATFile` as a 20-byte (`0x14`; decimal 20, Verified with int_convert.py) concrete stream object, [UID:0001XK][FileStreamVtables](by-type/by-vtable/FileStreamVtables.md) maps `DATFile` vtable slots `+0x18` and `+0x14` to `Seek` and `Tell`, and [UID:0002C4][0x00527080-0x005270b5.MidiReaderConstructorInit](by-memory/0x00527080-0x005270b5.MidiReaderConstructorInit.md) constructs that member with `sub_49C130(this+0x18)`. It was not raised higher at that time because the raw start still had no entry xrefs and no recovered original MIDI document/context source type or helper name.

Raised to `88/90` on 2026-06-28 after the accepted B001 empty-emitter source-quality pass resolved those prior no-code blockers into a confidence cap instead of a blank-C++ disposition. Current MCP reconfirmed the exact raw body, padding, DATFile seek/tell slots, Win32 `SetFilePointer` branch, no IDA function object, no entry xrefs, no modeled entity covering the body, and no VA/RVA/raw/rel32 route in the local PE scan. The accepted source-facing helper is `MidiSeekSource`, using the existing `MidiDocument` field model; confidence stays capped at `90` because the body remains raw/no-route and original helper/type symbols are still unrecovered.

## Address Range

`0x00526fa0-0x00526ff6`.

IDA does not model this start as a function, but live disassembly shows a normal prologue and seek body.

## Behavior

Seeks within the MIDI document source. When the document is backed by the embedded `DATFile` member at `this+0x18`, it maps the caller's seek origin and dispatches through `DATFile::Seek` followed by `DATFile::Tell`; otherwise it calls `SetFilePointer` for the Win32 file handle path.

## Source-State Notes

| State or dependency | Meaning |
| --- | --- |
| `this + 0x14` | `DATFile`-backed source flag deciding embedded archive-stream seek versus Win32 seek. |
| `this + 0x18` | Embedded `DATFile` object used for DAT/archive-backed MIDI source repositioning. |
| `this + 0x2c` | Win32 file handle passed to `SetFilePointer`. |
| `SetFilePointer` at `0x00526fec` | Confirms the file-backed seek path. |

## Evidence

- Parent page records this as a raw seek helper.
- IDA live disassembly checks the source-mode flag and references imported `SetFilePointer` at `0x00526fec`.
- The helper sits between the source close helper and raw read helper in the source abstraction cluster.
- `0x00527080` constructs the same `this+0x18` subobject with `sub_49C130`, which the `DATFile` docs identify as the `DATFile` constructor; the stream-vtable docs identify slot `+0x18` as `Seek` and slot `+0x14` as `Tell`.

## 2026-06-06 Live IDA MCP Evidence

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms:

- No IDA function object starts at `0x00526fa0`, and there are no direct entry xrefs to the raw helper start.
- The exact body is 86 bytes from `0x00526fa0` through the `retn 8` at `0x00526ff3-0x00526ff5`; the next byte is padding at `0x00526ff6`.
- The embedded `DATFile` path checks `this+0x14`, translates seek origin argument `[ebp+0x0c]` into origin `0`, `1`, or `2`, passes offset `[ebp+0x08]`, calls vtable slot `+0x18` / `Seek`, then calls vtable slot `+0x14` / `Tell`.
- The Win32 path passes origin `[ebp+0x0c]`, high-distance pointer `0`, offset `[ebp+0x08]`, and handle `this+0x2c` to `SetFilePointer` at `0x00526fec`.
- Padding before the helper separates it from [UID:0002C0][0x00526f60-0x00526f97.MidiSourceCloseHelper](by-memory/0x00526f60-0x00526f97.MidiSourceCloseHelper.md), and padding after `0x00526ff6` continues until [UID:0002C2][0x00527000-0x00527039.MidiSourceRawReadHelper](by-memory/0x00527000-0x00527039.MidiSourceRawReadHelper.md).

## 2026-06-16 Source-Quality Refresh

Read-only IDA MCP session `a002_goal65_20260616` reconfirms the source-quality blockers and resolves the source-member identity:

- `lookup_funcs 0x00526fa0` still reports `Not a function`, and `xrefs_to 0x00526fa0` reports zero direct entry xrefs. The raw-body reachability blocker therefore remains real; no caller was invented.
- `insn_query 0x00526fa0-0x00527000` shows the complete raw body with `fn:null`, the `this+0x14` branch, embedded `DATFile` dispatch through `this+0x18`, and the Win32 `SetFilePointer` call at `0x00526fec`; `get_bytes 0x00526f97` confirms `0xcc` padding before and after the body.
- The seek-origin translation is now documented at source-quality level: caller origins `0`, `1`, and `2` map to the same `FILE_BEGIN`, `FILE_CURRENT`, and `FILE_END` values used by `SetFilePointer`; values outside that set fall through to the current-position style `1` on the embedded `DATFile` path. No project-specific enum name was found.
- IDA `type_query`/`search_structs` for `DATFile`, `Midi`, `SMF`, and `Track` found no project-defined MIDI document/source type in IDA; `find_regex` found WinMM import strings plus RTTI names for `DATFile` and `MidiPlayer`, but no embedded source filename or MIDI document class name. This supported role-based MIDI document naming at the time; the 2026-06-28 accepted source-quality callback now supersedes the blank-C++ blocker with the descriptive `MidiSeekSource` formal body.

## 2026-06-28 Empty-Emitter Source-Quality Implementation

The accepted B001 report for [UID:0002C1] used live MCP session `supervisor_20260628_resume` on `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `status:ok`, module `NexusTK.exe`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.

Current MCP and local route evidence preserve the raw no-route state as a confidence cap:

- `lookup_funcs 0x00526fa0` reports this start is not a function. `entity_query functions 0x00526f00-0x005270c0` returns only `sub_527000`, `sub_527080`, and `sub_5270C0`; no modeled function covers the raw seek body.
- `xrefs_to 0x00526fa0` reports zero entry xrefs. MCP `find_bytes` for absolute VAs `A0 6F 52 00`, `00 70 52 00`, `40 70 52 00`, `10 6F 52 00`, and `60 6F 52 00` found no matches.
- A read-only local PE scan of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` mapped `0x00526fa0` to imagebase `0x00400000`, RVA `0x00126fa0`, raw offset `0x001263a0`, section `.text`, and found zero absolute-VA, RVA, raw-offset, `E8`/`E9`, or `0F 8x` rel32 routes to the raw start. A broad MCP operand scan timed out and is not used as proof.
- `insn_query 0x00526f97-0x00527000` and `get_bytes 0x00526f97 size 105` show nine `0xcc` bytes at `0x00526f97-0x00526fa0`, then the function-shaped body at `0x00526fa0-0x00526ff6`, then ten `0xcc` bytes at `0x00526ff6-0x00527000`.

The instruction evidence supports the formal helper body:

- `0x00526fa3` tests the `document->datFileSource` flag at `this+0x14`.
- The DATFile path defaults the mapped origin to `FILE_CURRENT`, maps caller `FILE_BEGIN` to `FILE_BEGIN`, maps caller `FILE_END` to `FILE_END`, and leaves `FILE_CURRENT` or any other value as current-position behavior.
- The DATFile path uses the embedded `DATFile` object at `this+0x18`, calls vtable slot `+0x18` with `offset` and the mapped origin, then calls vtable slot `+0x14` and returns the tell result.
- The Win32 path passes `origin`, high-distance pointer `NULL`, `offset`, and `document->fileHandle` at `this+0x2c` to `SetFilePointer`.

Support checks confirm the source shape without changing ownership:

- `decompile 0x00527000` confirms the adjacent raw-read helper uses the same `this+0x14` source flag, embedded DATFile member at `this+0x18`, and Win32 handle at `this+0x2c`.
- `decompile 0x0049c260` and `decompile 0x0049c2d0` identify the DATFile `Seek` and `Tell` behavior, while `decompile 0x0049c540` returns the DATFile active-entry check used by sibling source helpers.
- `decompile 0x005273a0` and `insn_query 0x005273a0-0x005277bb` show the modeled SMF loader duplicates the same seek/tell/SetFilePointer logic inline at `0x005275f2-0x0052761b`, `0x00527662-0x0052768b`, and `0x005276d2-0x005276f9`. This is semantic support for the field model and source abstraction, not a route to this raw helper start.
- `find_regex "Midi|SMF|DATFile|Track"` finds WinMM import strings and RTTI names for `DATFile` and `MidiPlayer`, but no recovered MIDI document/source filename or original helper symbol.

Rejected implementation alternatives are explicit: do not mark this covered by [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md) because that page is an aggregate inventory; do not mark it covered by [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md) because the loader contains inline copies but this remains a separate raw byte range; do not convert it to non-emitting metadata because it is source-authored logic, not padding/import/vtable/compiler glue; do not move ownership to `DATFile` because this wrapper chooses between DATFile and Win32 file handles; do not attach it to [UID:00008A][MidiPlayer](by-class/MidiPlayer.md) `MidiPlayer` because the receiver is the private `MidiDocument` source context; and do not create a new `MidiSMFDocument.cpp` route because no recovered filename, by-file page, or stronger source evidence exists than [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) `MidiPlayer.cpp`.

## Reconstruction Notes

The embedded source member is defensible as `DATFile`, and the seek constants match Win32 `FILE_BEGIN`/`FILE_CURRENT`/`FILE_END` values. The formal C++ body is intentionally emitted on this exact raw child as `static DWORD MidiSeekSource(MidiDocument* document, LONG offset, DWORD origin)`: the `MidiDocument` field model is accepted by the MIDI source-quality pass, and generated [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md) draft code already calls `MidiSeekSource`. The no-function/no-xref/no-route state remains documented as the reason confidence is capped at `90`, not as a reason to keep this emitter blank.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)
- [UID:00003G][DATFile](by-class/DATFile.md)
- [UID:0003H3][DATFileLayout](by-type/by-struct/DATFileLayout.md)
- [UID:0001XK][FileStreamVtables](by-type/by-vtable/FileStreamVtables.md)
- [UID:0002BZ][0x00526f10-0x00526f57.MidiSourceOpenHelper](by-memory/0x00526f10-0x00526f57.MidiSourceOpenHelper.md)
- [UID:0002C0][0x00526f60-0x00526f97.MidiSourceCloseHelper](by-memory/0x00526f60-0x00526f97.MidiSourceCloseHelper.md)
- [UID:0002C2][0x00527000-0x00527039.MidiSourceRawReadHelper](by-memory/0x00527000-0x00527039.MidiSourceRawReadHelper.md)

## Changes

- 2026-06-28 B001 empty-emitter source-quality implementation:
  - Changed to: score `88/90` and formal `MidiSeekSource` C++ in the reconstruction block; owner/emitter remain [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), `RECONSTRUCTABLE:TRUE`, and blank `EMITTER_POSITION_OPTIONAL`.
  - Summary/evidence: accepted MCP-backed report `0002C1-MidiSourceSeekHelper-empty-emitter-source-quality.md` reconfirmed no IDA function object, no entry xrefs, no modeled entity covering the raw body, no VA/RVA/raw/rel32 PE route, exact pre/post `0xcc` padding, DATFile origin mapping and `Seek`/`Tell` calls, Win32 `SetFilePointer`, sibling DATFile/source checks, and loader inline-copy evidence. Covered-by, no-code, split/container, non-emitting metadata, `DATFile` owner, class-method owner, and new `MidiSMFDocument.cpp` alternatives are rejected.
- 2026-06-02 low-score pass:
  - What existed before: range, behavior, and evidence were documented, but metadata stayed at `58/72` and reconstructable was blank.
  - Changed to: scores `68/76`, `RECONSTRUCTABLE:TRUE`, explicit autogen deferral, and score rationale.
  - Summary/evidence: raw prologue, adapter-active branch, `SetFilePointer`, source-abstraction placement, and neighboring source helpers support reconstructable project-code status without final C++.
- 2026-06-03 parent attachment:
  - What existed before: the helper remained unattached because its source ownership was treated as provisional.
  - Changed to: score `72/82` and `AUTOGEN_PARENT_UID:0000LD`.
  - Summary/evidence: the owning `MidiPlayer` file is now at `80/84`; this page records shared source-state offsets and sibling relationships. Final C++ remains blank.
- 2026-06-06 live IDA MCP pass:
  - Changed to: score `80/88`.
  - Summary/evidence: exact raw bytes, no modeled function or entry xrefs, adapter seek-origin translation, adapter vtable slots `+0x18`/`+0x14`, Win32 `SetFilePointer` argument order, and surrounding padding. Final C++ remains blank below the source-grade threshold.
- 2026-06-16 source-quality pass:
  - Changed to: score `86/89`.
  - Summary/evidence: read-only IDA MCP `a002_goal65_20260616` reconfirmed the raw non-function/no-entry-xref status, complete instruction body, seek-origin mapping, Win32 `SetFilePointer` path, and `0xcc` padding; support docs identify `this+0x18` as an embedded `DATFile` member with `Seek`/`Tell` vtable slots. Final C++ remains blank because the enclosing MIDI document type and original helper name are still not source-quality.
