*** UID:0004AR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006M | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006M | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IntegrateMacroEditControlPane::AddToLayer(const RectBounds *bounds,
                                                int order,
                                                Pane *previousPane,
                                                Layer *layer)
{
    Pane::AddToLayer(bounds, order, previousPane, layer);

    if (m_textEdit != nullptr)
    {
        RectBounds editBounds;
        m_textEdit->GetBounds(&editBounds);
        m_textEdit->AddToLayer(&editBounds, 0, this, layer);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 99-byte IntegrateMacroEditControlPane layer-addition override at primary slot +0x30; attaches the containing row through Pane first, then conditionally obtains the owned TextEditPane bounds and attaches that child at order zero after this row in the same Layer, excluding compiler security-cookie mechanics. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00580570-0x005805b9 IntegrateMacroEditControlPane AddToLayer

## Status

- Validator-authorized canonical document range/path: `[0x00580570,0x005805b9)` and `by-memory/0x00580570-0x005805b9.IntegrateMacroEditControlPaneAddToLayer.md`.
- Stable UID: `0004AR`; the callback path normalization does not create a new UID or a second source method.
- Evidence-time IDA modeled-function envelope remains `[0x00580550,0x005805b3)`, size `0x63` / 99 bytes. This wider binary envelope is preserved as analysis provenance rather than silently rewritten to match the validator filename endpoints.
- Entity kind: source-authored public virtual `IntegrateMacroEditControlPane::AddToLayer(...)` override.
- Direct semantic owner and source emitter: [UID:00006M][IntegrateMacroEditControlPane](by-class/IntegrateMacroEditControlPane.md).
- Physical source owner: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md); split parent [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) remains non-emitting.
- Metadata disposition: `92/94`, owner/emitter UID00006M, reconstructable true, blank position, `Nested:0`.

## Instructions, CFG, And ABI

- IDA models the evidence-time function at `0x00580550`, size `0x63`, 43 instructions, three basic blocks, and cyclomatic complexity two.
- Source ABI is the accepted inherited four-argument layer contract: bounds pointer, integer order, previous Pane pointer, and Layer pointer on a complete-object thiscall receiver.
- The method first calls `Pane::AddToLayer` at `0x00544c70` with the original four arguments.
- It then loads owned `m_textEdit` at `+0x110`, skips child work when null, obtains child bounds through primary slot `+0x28`, and attaches the child through primary slot `+0x30`.
- The local `RectBounds` causes MSVC security-cookie mechanics. Cookie setup/check and stack-lowering details are compiler-generated and excluded from authored source.
- There is no source-visible result; the accepted virtual contract is `void`.

## Vtable And Source Contract

- IntegrateMacroEditControlPane primary slot `+0x30` points to UID0004AR. Its paired `+0x38` slot points to [UID:0004AS][0x005805c0-0x005805da.IntegrateMacroEditControlPaneRemoveFromLayer](by-memory/0x005805c0-0x005805da.IntegrateMacroEditControlPaneRemoveFromLayer.md).
- Pane primary slot `+0x30` points to `Pane::AddToLayer`; Pane primary slot `+0x38` points to `Pane::RemoveFromLayer`.
- TextEditPane/ScrollablePane primary slot `+0x30` resolves the same child AddToLayer contract, while child slot `+0x28` resolves `GetBounds` with a `RectBounds *` out parameter.
- Strong ScrollableControlPane and TextEditControlPane declarations independently use public `AddToLayer(const RectBounds *, int, Pane *, Layer *)` and `RemoveFromLayer()` overrides.
- Exact slot identity rejects the historical `OnAttachToParent`/Rect/zOrder/flags/parent interpretation.

## Field, Ownership, And Behavior

- Complete-object field `+0x110` is owned `TextEditPane *m_textEdit`; construction stores the child there and the source destructor deletes it.
- Exact authored order is containing-row attachment first, then conditional child attachment. Reversing that order would not match the binary.
- Child bounds are queried into a local `RectBounds`; the child is attached with order `0`, previous pane `this`, and the same Layer pointer passed to the containing row.
- The child null guard is source-significant and preserves valid partial-lifetime states. No child allocation, deletion, field write, fallback attachment, or failure return occurs here.
- Pane inherited behavior is invoked rather than duplicated in the source body.

## Range, Boundary, And Placement Disposition

- Current canonical documentation uses the supervisor callback's validator-supported endpoints `0x00580570-0x005805b9`; every ordinary UID reference must use that current path.
- Historical/binary analysis retains modeled start `0x00580550`, modeled end `0x005805b3`, and 99-byte size as evidence-time facts. These are not a second child and do not authorize restoring the old path.
- UID0004AR remains immediately before UID0004AS in source lifecycle order and emits through the complete IntegrateMacroEditControlPane class in MacroDialogs.cpp.
- No additional child, wrapper, helper, raw table, or source file is required.

## Negative Evidence And Rejected Alternatives

- No exact vtable evidence supports `OnAttachToParent`; primary `+0x30` is the established AddToLayer contract in Pane and child families.
- The binary uses a `RectBounds` out object and four-argument layer calls, not a `Rect` reference plus generic flags.
- No source body should expose compiler security cookies, raw vtable calls, decompiler temporaries, or a return value.
- `ControlPane::OnAttachToParent`, child `OnAttachToParent`, generic parent-attach naming, compiler-only, inline-only, and no-code alternatives are rejected.

## Historical Assumption And Score Rationale

- Superseded history: UID0004AR was documented at `87/90` as `IntegrateMacroEditControlPaneOnAttachToParent` and used a generic attach body. That name/signature/body are retained only as rejected provenance.
- Exact paired slot identity, Pane and child contracts, complete call order, typed owned field, local bounds, source access, compiler-cookie distinction, source placement, and complete C++ resolve the prior score blockers.
- `92/94` reflects source readiness while reserving uncertainty for stripped lexical symbols and the validator-authorized filename endpoint normalization.
