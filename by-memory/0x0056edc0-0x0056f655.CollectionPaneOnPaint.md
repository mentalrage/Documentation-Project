*** UID:0003Y6 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:70 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void CollectionPane::OnPaint()
{
    EPFTileContext tileContext;

    SimpleUString groupName;
    wchar_t groupKeyText[128];
    wchar_t valueText[128];

    DLPalette *inventoryPalette =
        g_pPaletteLib->GetPaletteByName(L"ITEMINV.PAL");
    g_pEPFLib->LookupLayoutEntry(L"CLTINV.EPF", 0, &tileContext);
    g_pfnBlitSprite(this, &tileContext, &tileContext.bounds,
                    &m_visibleBounds, 0, inventoryPalette, 0);

    SetDrawMode(1);
    RectBounds rowBounds = { 6, 14, 161, 62 };
    SetTextBackColor(0);

    CollectionPlayerDataView &collectionData =
        g_pUserPane->GetCollectionData();

    int firstGroup = static_cast<int>(m_groupCountSnapshot) - 5;
    if (static_cast<int>(m_firstVisibleGroup) + 5 <=
        static_cast<int>(m_groupCountSnapshot))
        firstGroup = m_firstVisibleGroup;
    if (firstGroup < 0)
        firstGroup = 0;

    for (int visibleRow = 0, groupIndex = firstGroup;
         visibleRow < 5 && groupIndex < collectionData.groupCount;
         ++visibleRow, ++groupIndex) {
        const CollectionGroupRecord &group =
            collectionData.groups[groupIndex];
        RectBounds textBounds = rowBounds;
        const int completionPercent =
            100 * group.collectedEntries / group.totalEntries;

        g_pEPFLib->LookupLayoutEntry(
            L"CLTLIST.EPF", completionPercent / 100, &tileContext);
        RenderTileFrame(&tileContext, &tileContext.bounds, &rowBounds,
                        0, L"CLTLIST.PAL", 0);

        textBounds.left = rowBounds.left + 55;
        textBounds.right = rowBounds.left + 155;
        textBounds.top = rowBounds.top + 6;
        textBounds.bottom = rowBounds.top + 20;
        swprintf_s(groupKeyText, 128, L"%d", group.groupType);

        SimpleUString key;
        key.Assign(groupKeyText,
                   static_cast<unsigned int>(wcslen(groupKeyText)));
        SimpleUString tableName;
        tableName.Assign(L"GroupNames", 10);
        const SimpleUString *groupNameEntry =
            g_pMetaMan->FindEntry(&tableName, &key, 0);
        if (&groupName != groupNameEntry) {
            const wchar_t *entryText = groupNameEntry->Data();
            groupName.Assign(
                entryText,
                groupNameEntry->Length());
        }
        if (groupName.Data()[0] == L'\0')
            groupName.Assign(L"\xC548\xB098\xC640", 3);

        const wchar_t *groupNameText = groupName.Data();
        const int groupNameLength = static_cast<int>(wcslen(groupNameText));
        SetTextColor(0x80);
        textBounds.Offset(-1, -1);
        if (completionPercent == 100) {
            for (int pass = 0; pass < 3; ++pass) {
                for (int edge = 0; edge < 4; ++edge) {
                    DrawTextInRect(groupNameText, groupNameLength,
                                   &textBounds);
                    textBounds.Offset(0, 1);
                }
                textBounds.Offset(1, -4);
            }
        }

        textBounds.left = rowBounds.left + 55;
        textBounds.right = rowBounds.left + 155;
        textBounds.top = rowBounds.top + 6;
        textBounds.bottom = rowBounds.top + 20;
        SetTextColor(0x8f);
        DrawTextInRect(groupNameText, groupNameLength, &textBounds);
        textBounds.Offset(1, 0);
        SetTextColor(0x8f);
        DrawTextInRect(groupNameText, groupNameLength, &textBounds);

        textBounds.left = rowBounds.left + 55;
        textBounds.right = rowBounds.left + 155;
        textBounds.top = rowBounds.top + 30;
        textBounds.bottom = rowBounds.top + 44;
        swprintf_s(valueText, 128, L"%d %%", completionPercent);
        SetTextColor(0x8f);
        DrawTextInRect(valueText, wcslen(valueText), &textBounds);
        if (completionPercent == 100) {
            SetTextColor(0x0e);
            DrawTextInRect(valueText, wcslen(valueText), &textBounds);
            textBounds.Offset(1, 0);
            DrawTextInRect(valueText, wcslen(valueText), &textBounds);
            textBounds.Offset(-1, 0);
        }

        swprintf_s(valueText, 128, L"%d/%d",
                   group.collectedEntries, group.totalEntries);
        SetTextColor(0x8f);
        textBounds.left = textBounds.right -
            6 * (static_cast<int>(wcslen(valueText)) + 2);
        DrawTextInRect(valueText, wcslen(valueText), &textBounds);
        if (completionPercent == 100) {
            SetTextColor(0x25);
            DrawTextInRect(valueText, wcslen(valueText), &textBounds);
            textBounds.Offset(1, 0);
            DrawTextInRect(valueText, wcslen(valueText), &textBounds);
            textBounds.Offset(-1, 0);
        }

        rowBounds.top += 50;
        rowBounds.bottom += 50;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: CollectionPane primary paint method; draws five visible collection rows using exactly seven UID0003F1 literals for resource lookup, group-name/fallback text, completion percent, and collected/total counts. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0056edc0-0x0056f655 CollectionPaneOnPaint

## Status

- Entity kind: paint/render method.
- Direct owner: [UID:00002X][CollectionPane](by-class/CollectionPane.md).
- Source file: [UID:0000IC][CollectionPane](by-file/CollectionPane.md).
- Parent split index: [UID:0001HD][0x0056e940-0x0056fe75.CollectionPane](by-memory/0x0056e940-0x0056fe75.CollectionPane.md).

## Evidence

- Vtable reference at `0x00624624` targets this paint body.
- Draws the `CLTINV`/`CLTLIST` collection resources, formats group names and completion counts, and renders five visible group rows.
- Reads collection group record bytes for group type/volume, total entries, and collected entries through the `g_pCollectionData` player-state view.
- Uses CollectionPane-local packed rectangle constants and the visible-start scroll state when choosing rows.
- Exact [UID:0003F1][0x00624a98-0x00624b30.CollectionPanePanelResourceStrings](by-memory/0x00624a98-0x00624b30.CollectionPanePanelResourceStrings.md) operands are `L"CLTINV.EPF"` at `0x0056ee4c` for the collection background frame, `L"CLTLIST.EPF"` at `0x0056ef6b` and `L"CLTLIST.PAL"` at `0x0056ef77` for visible-row resource lookup, `L"GroupNames"` at `0x0056f04a` for the display name, `L"\xC548\xB098\xC640"` at `0x0056f1a9` as the missing-name fallback, `L"%d %%"` at `0x0056f3b1` for completion percentage, and `L"%d/%d"` at `0x0056f4b6` for collected/total text.
- The method is exactly `[0x0056edc0,0x0056f655)`, size `0x895`, body SHA256 `E620D25A7EC887E994EE6DB722B184BD4BF99371AFDEB5DF3B762666372639CC`. These seven operands are the complete UID0003F1 use set for this body and correspond to the five visible-row rendering behavior; they do not establish that the broader formal body is complete.
- Live inventory records 575 instructions, 64 basic blocks, 17 profiled callees, zero direct code callers, and one vtable data xref. Cell `0x00624624` identifies primary virtual slot `+0x44` as `OnPaint`.
- [UID:0003FA][0x00624c10-0x00624c20.CollectionPaneRectConstant](by-memory/0x00624c10-0x00624c20.CollectionPaneRectConstant.md) has the sole xref at `0x0056ee7d`; the aligned load initializes typed local `destinationBounds : RectBounds`, which becomes `RectBounds rowBounds = { 6, 14, 161, 62 };`. Direct instructions at `0x0056f5bf` and `0x0056f5c7` advance `top` and `bottom` by 50.
- The physical stack supports one initialized/reused `EPFTileContext`, two exact `wchar_t[128]` buffers, one POD row rectangle, and one reused text rectangle. The accepted source uses `.bounds`, const `SimpleUString::Data()`/`Length()`, and no const-cast `MutableData` workaround.
- Direct declaration routes are `CollectionPane.h`, `UserPane.h`, `../../metadata/MetaMan.h`, `../../render/ImageLib.h`, `../../render/PaletteLib.h`, `../../render/Surface.h`, and `<cwchar>`. These provide collection records, `g_pMetaMan`/`FindEntry`, `g_pEPFLib`/`LookupLayoutEntry`, `g_pPaletteLib`/`GetPaletteByName`, `g_pfnBlitSprite`, `swprintf_s`, and `wcslen` without reduced local declarations.

## Reconstruction Notes

Use source-facing name `CollectionPane::OnPaint()`. The complete dependency-closed body now emits through UID00002X at position `70`; method H remains blank because the owning class declares it once. One initialized tile context serves both layout lookups, the exact seven UID0003F1 literals stay at their use sites, and the UID0003FA rectangle remains a local POD initializer rather than a standalone object. Reduced PaletteLib/MetaMan declarations, duplicate classes/externs, hand-written CRT prototypes, helper-call row increments, and const-cast `MutableData` source are rejected.

## Cross-References

- [UID:00002X][CollectionPane](by-class/CollectionPane.md)
- [UID:0001TZ][CollectionPlayerDataLayout](by-type/by-struct/CollectionPlayerDataLayout.md)
- [UID:0001R8][collection-ui-resources](by-resource/collection-ui-resources.md)
- [UID:0003FA][0x00624c10-0x00624c20.CollectionPaneRectConstant](by-memory/0x00624c10-0x00624c20.CollectionPaneRectConstant.md)
- [UID:0000D9][SimpleUString](by-class/SimpleUString.md)
- [UID:000088][MetaMan](by-class/MetaMan.md)
- [UID:0001HD][0x0056e940-0x0056fe75.CollectionPane](by-memory/0x0056e940-0x0056fe75.CollectionPane.md)
- [UID:0003F1][0x00624a98-0x00624b30.CollectionPanePanelResourceStrings](by-memory/0x00624a98-0x00624b30.CollectionPanePanelResourceStrings.md)

## Changes

- 2026-08-16 Agent-B008 UID0003FA accepted callback: raised `87/90 -> 92/94`, set emitter position `70`, and inserted the complete unified body with one initialized/reused tile context, two exact 128-wide buffers, row/text rectangles, const `Data()`/`Length()`, all seven UID0003F1 literals, exact escaped fallback, direct 50-pixel updates, and dependency-closed owner includes. The earlier blank-body deferral, two-include preamble, const-cast MutableData body, and helper-call row increment are retained only as superseded history.
- 2026-08-15 Agent-B002 UID0003F1 accepted callback: added the exact seven pooled-literal operands, render semantics, and body identity while preserving the independently scoped blank formal body and `87/90` score.
