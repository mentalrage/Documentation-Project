*** UID:0003VR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void TextDialog::OnControlCommand(int controlIndex, int /*notifyCode*/)
{
    switch (controlIndex)
    {
    case 1:
        {
            wchar_t text[256];
            char convertedText[256];
            unsigned char packet[512];

            GetChild<TextEditControlPane>(6)->ReadText(text, 255);

            MemoryMan *memoryMan = GetMemoryMan();
            const unsigned char textLength =
                static_cast<unsigned char>(wcslen(text));

            PacketBufferWriteUInt8(0x3a, packet);
            PacketBufferWriteUInt8(m_dialogType, packet + 1);
            PacketBufferWriteUInt32BE(m_dialogId, packet + 2);
            PacketBufferWriteUInt16BE(m_dialogState, packet + 6);
            PacketBufferWriteUInt16BE(
                static_cast<unsigned short>(m_dialogPageIndex + 1),
                packet + 8);
            PacketBufferWriteUInt8(2, packet + 10);

            const int convertedLength = WideCharToMultiByte(
                CP_ACP,
                0,
                text,
                textLength,
                convertedText,
                sizeof(convertedText),
                NULL,
                NULL);

            convertedText[convertedLength] = '\0';
            PacketBufferWriteUInt8(
                static_cast<unsigned char>(convertedLength),
                packet + 11);
            memoryMan->MemmoveWrapper(
                packet + 12,
                convertedText,
                convertedLength);

            const int packetLength = convertedLength + 12;
            packet[packetLength] = 0;
            g_packetSender->QueueAndSendPacket(
                packet,
                static_cast<short>(packetLength));
            CloseDialog();
        }
        break;

    case 2:
        SendPreviousDialogPacket();
        CloseDialog();
        break;

    case 3:
        SendNextDialogPacket();
        CloseDialog();
        break;

    case 4:
        SendCurrentDialogPacket();
        CloseDialog();
        break;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `TextDialog::OnControlCommand(int controlIndex, int notifyCode)` override at primary-vtable slot `+0x48`; control `1` reads `TextEditControlPane` child `6`, converts at most 255 wide characters through `WideCharToMultiByte(CP_ACP, 0, ...)`, emits the inline opcode `0x3a` typed-text packet from inherited Pursuit fields, sends exactly `convertedLength + 12` bytes, and closes; controls `2/3/4` send inherited previous/next/current packets and close; default is a no-op; formal CPP is source-ready while H remains class-owned by [UID:0000EL][TextDialog](by-class/TextDialog.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00553180-0x00553338 TextDialog OnControlCommand

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: virtual class method.
- Covered class: [UID:0000EL][TextDialog](by-class/TextDialog.md).
- Source module context: [UID:0000OL][TextDialog](by-file/TextDialog.md).
- Parent split index: [UID:0001FJ][0x00552110-0x005534a0.TextDialogCore](by-memory/0x00552110-0x005534a0.TextDialogCore.md).
- Source identity: `TextDialog::OnControlCommand(int controlIndex, int notifyCode)`, proved by the `TextDialog` primary-vtable slot `+0x48` and the inherited `DialogPane` virtual contract.
- Formal CPP: source-ready and emitted here because this page covers exactly one source method body.
- Formal H: intentionally blank. [UID:0000EL][TextDialog](by-class/TextDialog.md) owns the single class declaration and virtual method declaration; duplicating it in this child would violate header ownership.
- Canonical owner/emitter remains [UID:0000EL][TextDialog](by-class/TextDialog.md); no ownership or source-route change is required.

## Behavior

`TextDialog::OnControlCommand` handles the four TextDialog control indices. The current IDA label `sub_553180` is retained only as historical binary provenance. TextDialog directly derives from [UID:0000BE][PursuitMessageDialogPane](by-class/PursuitMessageDialogPane.md), has exact size `0x278`, and adds no source-proven fields beyond that base.

Control `1` reads child index `6` as a `TextEditControlPane` through inherited `m_controlManager` at `+0x1fc`. It calls `ReadText(text, 255)` into a 256-element wide buffer, measures the null-terminated text, narrows that length to one byte, and converts through `WideCharToMultiByte(CP_ACP, 0, text, textLength, convertedText, 256, NULL, NULL)` into a 256-byte local buffer.

The same branch builds an opcode `0x3a` typed-text reply in a 512-byte packet buffer using inherited `m_dialogType`, `m_dialogId`, `m_dialogState`, and `m_dialogPageIndex + 1`. It writes subtype byte `2`, the one-byte converted length, and the converted bytes through `MemoryMan::MemmoveWrapper`, then queues exactly `convertedLength + 12` bytes through `g_packetSender->QueueAndSendPacket`. The converted-text and packet scratch terminators are local safety writes and are not transmitted. Send occurs before `CloseDialog()`.

Controls `2`, `3`, and `4` call inherited protected `SendPreviousDialogPacket()`, `SendNextDialogPacket()`, and `SendCurrentDialogPacket()` from [UID:0001FG][0x0054cc30-0x0054ce0f.DialogNavigationPacketHelpers](by-memory/0x0054cc30-0x0054ce0f.DialogNavigationPacketHelpers.md), then call `CloseDialog()`. These are not free helpers or private TextDialog definitions. The default switch route performs no work and returns without closing.

`CloseDialog()` is the source-facing identity for the shared `0x0049dad0` helper. Its exact body queues the pane for deferred deletion through `BlackHole__QueuePaneForDeferredDeletion(g_pApplicationCleanupQueue, this)`. The target calls it at `0x005532fe` after submission and at `0x0055331d` after navigation.

## ABI And Virtual Dispatch

- Exact function range: `[0x00553180,0x00553338)`, size `0x1b8`.
- Current binary ABI: `__thiscall`, two explicit `int` arguments, and `retn 8`.
- The first explicit argument is `controlIndex`; the second is inherited `notifyCode` and is unused by this override.
- Primary `TextDialog` vtable starts at `0x00622958`. Cell `0x006229a0` is offset `+0x48`, stores `0x00553180`, and is the only inbound reference to the method; virtual reachability does not require an ordinary direct code caller.
- The function has 126 instructions, 13 basic blocks, and cyclomatic complexity 6.

## Packet Layout

| Offset | Width | Source | Meaning |
| ---: | ---: | --- | --- |
| `+0` | 1 | literal `0x3a` | dialog/message reply opcode |
| `+1` | 1 | `m_dialogType` (`this+0x26c`) | inherited dialog type |
| `+2..+5` | 4 | `m_dialogId` (`this+0x270`) | big-endian dialog id |
| `+6..+7` | 2 | `m_dialogState` (`this+0x274`) | big-endian dialog state |
| `+8..+9` | 2 | `m_dialogPageIndex + 1` (`this+0x276`) | big-endian one-based page index |
| `+10` | 1 | literal `2` | text-submit subtype |
| `+11` | 1 | converted byte count | payload length |
| `+12...` | N | converted bytes | ANSI/MBCS payload |

The compiler checks converted length below `0x100` before indexing the converted buffer and packet length below `0x200` before indexing the packet buffer. The emitted source preserves the local-array operations that cause those protections; it does not hand-write the compiler's security-cookie or `___report_rangecheckfailure` lowering.

## Range And Boundary Evidence

- Six `0xcc` bytes at `0x0055317a-0x00553180` precede this independent function.
- The four-entry switch table occupies `0x00553338-0x00553348` and contains `0x005531be`, `0x005531aa`, `0x005531b4`, and `0x00553316` for controls `1`, `2`, `3`, and `4`.
- Eight `0xcc` bytes occupy `0x00553348-0x00553350`.
- [UID:0003VS][0x00553350-0x00553495.TextDialogUnreferencedSubmitPacketRaw](by-memory/0x00553350-0x00553495.TextDialogUnreferencedSubmitPacketRaw.md) begins after that padding and is not a tail block, switch target, or emitted helper of this method.

## Evidence

Live read-only IDA MCP session `b3b2bf88` confirms the exact range, ABI, frame, switch, direct callees, vtable reference, packet stores, conversion arguments, send length, and close order. The stack frame contains a `MemoryMan *`, `WCHAR[256]`, `CHAR[256]`, and a contiguous 512-byte packet aggregate. The current IDA name `sub_553180` and earlier descriptive `HandleDialogAction` terminology are historical artifacts superseded by the exact vtable/base-contract identity `TextDialog::OnControlCommand`.

The following `0x00553338-0x00553348` data is the compiler switch table for this method, not source-authored logic or a separate source function. B007 2026-06-19 route reanalysis confirms all entries stay inside this method; no switch entry branches to [UID:0003VS][0x00553350-0x00553495.TextDialogUnreferencedSubmitPacketRaw](by-memory/0x00553350-0x00553495.TextDialogUnreferencedSubmitPacketRaw.md). Control `1` builds the typed-text submit packet inline, so there is no proven call from this live handler to the raw island.

Historical shared-helper no-owner wording is superseded by Pursuit RTTI and exact packet-tail layout. The raw no-route submit island remains excluded because this live method still sends typed text inline.

## Source-Quality Inference And Negative Evidence

- Original resource-symbol spellings for controls `1..4` and child index `6` were not recovered. Exact numeric values are therefore retained instead of inventing unsupported constants.
- No original PDB preserves local-variable or include-order spellings. Human names `text`, `convertedText`, `packet`, `convertedLength`, and `packetLength` follow established project style and exact data flow rather than exposing decompiler labels.
- `OnControlCommand`, `controlIndex`, and `notifyCode` are not speculative: the TextDialog vtable slot and inherited DialogPane declaration resolve them.
- `TextEditControlPane`, inherited Pursuit fields, navigation methods, packet helper calls, and `CloseDialog()` are resolved by exact types/callees and current support docs.
- The adjacent raw island's historical source origin remains unknown, but its no-route status and physical separation are exact and do not block this method's source.
- These lexical/history uncertainties cap confidence below final-audit 95; they do not block the formal method body because control flow, ABI, fields, packet bytes, conversion, lengths, call order, ownership, and source placement are resolved.

## Cross-References

- [UID:0000EL][TextDialog](by-class/TextDialog.md)
- [UID:0000OL][TextDialog](by-file/TextDialog.md)
- [UID:0001FJ][0x00552110-0x005534a0.TextDialogCore](by-memory/0x00552110-0x005534a0.TextDialogCore.md)
- [UID:0001FG][0x0054cc30-0x0054ce0f.DialogNavigationPacketHelpers](by-memory/0x0054cc30-0x0054ce0f.DialogNavigationPacketHelpers.md)
- [UID:0003DC][0x00622954-0x006229f0.TextDialogVtableData](by-memory/0x00622954-0x006229f0.TextDialogVtableData.md)

## Changes

- 2026-08-01 Agent-B009 UID0003VR implementation callback:
  - Raised `87/92 -> 93/94` while preserving canonical owner/emitter [UID:0000EL][TextDialog](by-class/TextDialog.md).
  - Resolved the virtual source identity to `TextDialog::OnControlCommand(int controlIndex, int notifyCode)` from primary-vtable slot `+0x48` and the DialogPane contract.
  - Added exact ABI, child-control, text-conversion, packet-layout, send-length, close-order, boundary, compiler-protection, negative-evidence, and historical-label analysis.
  - Added the formal CPP method body and retained a blank H channel with exact class-parent declaration proof.

- 2026-07-16 Agent-B004 UID0001FE support callback:
  - Raised `86/90 -> 87/92`.
  - Replaced raw packet offsets with inherited Pursuit member meanings and resolved actions `2/3/4` to inherited protected navigation methods.
  - Preserved blank formal C++, inline typed-submit behavior, switch-table evidence, and the no-route raw-island exclusion.
- Created from B001 report `0001FJ-textdialog-core-source-split-audit.md` and reconciled with final report `0001FJ-textdialog-core-source-quality.md` to split the broad TextDialogCore leaf into exact child pages.
- 2026-06-21 Rule 26 incorporation of B007 report `0003VS-TextDialogUnreferencedSubmitPacketRaw-source-quality.md`: score unchanged; added the switch-table target list and explicit contrast with the following no-route raw island.
