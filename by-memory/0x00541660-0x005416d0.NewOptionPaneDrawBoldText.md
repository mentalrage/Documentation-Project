*** UID:0001DZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000M7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000M7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void DrawOptionPaneSectionHeader(GrafPort *port, const wchar_t *text, int x, int y)
{
    port->SetTextColor(128);
    port->MoveTo(x, y);
    port->DrawText(text, wcslen(text));

    port->MoveTo(x + 1, y);
    port->DrawText(text, wcslen(text));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: OptionPane paint helper/provisional file-local `DrawSectionHeader`; written IDA evidence records a real `0x70` / decimal `112` byte function (Verified with int_convert.py), four `NewOptionPane::OnPaint` callers, no fitting-room callers in the reviewed set, and callee fanout only to shared GrafPort `MoveTo`, background-color, and wide-text helpers; assigned to direct file parent [UID:0000M7][OptionPane](by-file/OptionPane.md) because the helper consumes draw context/text/coordinates rather than class fields and the child `85/88` plus parent `91/85` clear the strict gate. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00541660-0x005416d0 NewOptionPane DrawBoldText

## Scope

- Covered file/module: [UID:0000M7][OptionPane](by-file/OptionPane.md)
- Likely class: [UID:000097][NewOptionPane](by-class/NewOptionPane.md)
- Current generated owner: `FittingRoomDownloadControlPane`
- IDA name: `sub_541660`
- Status: real project helper; current generated class owner is likely wrong.

## Function Boundary

IDA MCP `lookup_funcs` on 2026-05-25 reports a real function at `0x00541660` with size `0x70` / decimal `112` bytes (Verified with int_convert.py); 2026-06-01 recheck records the exclusive endpoint as `0x005416d0`.

The immediately preceding helper family is the `NewOptionPane::InitializeOptions` body ending at `0x00541651`; this function begins after the existing alignment gap at `0x00541660`. The immediate successor is [UID:0001E0][0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem](by-memory/0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem.md), so this page covers only the section-header text helper and does not absorb the following button-entry renderer.

Direct IDA callers are all inside `NewOptionPane::OnPaint` at `0x00540600`:

| Caller offset | Use |
| --- | --- |
| `0x00540639` | First section header draw. |
| `0x0054064c` | Second section header draw. |
| `0x0054065f` | Third section header draw. |
| `0x00540672` | Fourth section header draw. |

No fitting-room caller was observed in the live IDA caller set for this pass.

2026-06-01 IDA MCP recheck confirms the same four direct callers and callees `0x004bab70`, `0x004b9680`, and `0x004b9600`.

2026-06-10 A001 live MCP status: `tools/list` responded, but `lookup_funcs`, `callers`, and a minimal `py_eval` probe timed out during this pass. No fresh live-IDB facts were added beyond the written 2026-05-25 and 2026-06-01 IDA evidence above.

2026-06-23 B001 MCP evidence session `ff68e691` refresh: `lookup_funcs` reports `0x00541660` as `sub_541660` size `0x70`, and xrefs/code refs are still exactly `0x00540639`, `0x0054064c`, `0x0054065f`, and `0x00540672`, all inside `NewOptionPane::OnPaint`. Callees remain GrafPort draw-state/text helpers (`0x004b9680`, `0x004b9600`, and `0x004bab70`). Raw pointer searches for the function VA found no table/global route; text/name/import searches found no binary proof for exact original spellings `DrawSectionHeader` or `DrawOptionButton`. The older `0b5e057e` pass is historical and agrees on the caller/no-name state.

## Signature And Source Placement

| Topic | Current conclusion |
| --- | --- |
| Binary shape | Method-like paint helper that receives the active draw context, a UTF-16 section-header pointer, and X/Y coordinates. The receiver is consumed only through shared GrafPort text/draw-state helpers in the documented callee set. |
| Preferred source name | `DrawSectionHeader` or `NewOptionPane::DrawSectionHeader` remains better than the stale generated `FittingRoomDownloadControlPane::DrawBoldText` name because all observed direct callers are `NewOptionPane::OnPaint` call sites. |
| Direct autogen parent | [UID:0000M7][OptionPane](by-file/OptionPane.md). Current evidence supports a file-local OptionPane paint helper because the body consumes only the draw context, text, and coordinates and does not touch `NewOptionPane` fields; [UID:000097][NewOptionPane](by-class/NewOptionPane.md) remains the contextual caller/consumer, not the proven direct code owner. |
| Rebuild handling | `source-authored` helper body with first-draft formal C++; file-local source shape is accepted for this callback while exact original spelling remains inferred. |

## Behavior

This helper draws a wide string twice at nearly the same position to produce the option-pane bold/shadow section-header effect.

Observed flow:

1. Set the GrafPort background-color field to `0x80` / decimal `128` (Verified with int_convert.py) through the shared [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) accessor at `0x004b9680`.
2. Move the draw cursor to `(x, y)` with [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md) `0x004b9600`.
3. Walk the null-terminated wide string to derive the text length, then draw it with [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) subrange `0x004bab70`.
4. Move the cursor to `(x + 1, y)`.
5. Draw the same text again.

In `NewOptionPane::OnPaint`, the callers pass the page-zero section headers `Show Effects`, `Listen to`, `Semi-trans`, and `System`.

## Caller And Callee Summary

| Address | Evidence meaning |
| --- | --- |
| `0x00540639` | First `NewOptionPane::OnPaint` section-header draw. |
| `0x0054064c` | Second `NewOptionPane::OnPaint` section-header draw. |
| `0x0054065f` | Third `NewOptionPane::OnPaint` section-header draw. |
| `0x00540672` | Fourth `NewOptionPane::OnPaint` section-header draw. |
| `0x004b9680` | Shared GrafPort background-color setter from [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md). |
| `0x004b9600` | Shared GrafPort `MoveTo` draw-cursor setter from [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md). |
| `0x004bab70` | Shared GrafPort wide-text renderer from [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md). |

The callee set is narrow and UI-rendering-only. The helper does not touch option-entry records, config globals, packet state, fitting-room fields, or the sibling button-frame resources handled by [UID:0001E0][0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem](by-memory/0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem.md).

## Ownership Notes

`source-3/simroot_v2/class_FittingRoomDownloadControlPane.cpp` currently emits this helper as `FittingRoomDownloadControlPane::DrawBoldText`, while `source-3/simroot_v2/class_NewOptionPane.cpp` calls an undeclared `DrawSectionHeader` helper from `NewOptionPane::OnPaint`. That generated-source mismatch is consistent with an owner-pollution problem: the implementation exists, but it is attached to the wrong generated class.

For reconstruction, prefer one of these placements after code/data cleanup:

- `NewOptionPane::DrawSectionHeader`
- a file-local `DrawSectionHeader(NewOptionPane*, ...)` helper inside `OptionPane.cpp`
- a neutral UI text helper only if broader non-option callers are later confirmed

Do not migrate it as fitting-room-specific code based solely on the current generated owner.

## Reconstruction Notes

Reconstructable is `TRUE` because this is NexusTK-owned option-pane paint helper code and all current IDA callers are inside `NewOptionPane::OnPaint`. It attaches to [UID:0000M7][OptionPane](by-file/OptionPane.md) because this page is now `88/90` and the file parent is `92/88`, clearing the strict child/direct-parent `85/85` gate. Formal C++ now emits a file-local `DrawOptionPaneSectionHeader`; the source-level helper name and receiver type are inferred, not binary-proven original spellings.

## Cross References

- [UID:0000M7][OptionPane](by-file/OptionPane.md)
- [UID:000097][NewOptionPane](by-class/NewOptionPane.md)
- [UID:0001DW][0x0053ff90-0x00541b2b.NewOptionPane](by-memory/0x0053ff90-0x00541b2b.NewOptionPane.md)
- [UID:0001E0][0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem](by-memory/0x005416d0-0x00541853.NewOptionPaneDrawOptionButtonItem.md)
- [UID:000162][0x004b95e0-0x004b969f.GrafPortDrawStateAccessors](by-memory/0x004b95e0-0x004b969f.GrafPortDrawStateAccessors.md)
- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [Wave3 data issues](../wave3_data_issues.md)

## Changes

- 2026-06-10 A001: Raised completion from `72` to `85` and kept confidence at `88`.
  - Before: the page had the correct owner caveat, function boundary, and broad behavior summary, but the score-gate rationale, exact size conversion, sibling boundary, callee ownership, and direct-parent gate were not written clearly enough to defend the existing parent assignment.
  - After: the page records the `0x70` / decimal `112` byte size conversion, four `NewOptionPane::OnPaint` caller sites, shared GrafPort `MoveTo`/background-color/wide-text callees, section-header-only scope, `OptionPane` file-parent gate, and the current-session MCP timeout caveat.
  - Assignment effect: direct parent remains [UID:0000M7][OptionPane](by-file/OptionPane.md) as a file-local option-paint helper; [UID:000097][NewOptionPane](by-class/NewOptionPane.md) remains the caller context until a true member-method ownership claim is proven.
- 2026-06-23 B001 active-session refresh: no score, owner, emitter, or C++ change.
  - Evidence: current B001 MCP evidence session `ff68e691` reconfirmed the modeled `0x00541660` function, the same four `NewOptionPane::OnPaint` caller sites, the UI text/draw-state callee set, no raw pointer/table route, and no binary proof for exact original `DrawSectionHeader` spelling. Older `0b5e057e` evidence is historical and agrees. Generated `FittingRoomDownloadControlPane` ownership remains rejected as pollution.
- 2026-06-30 B007 implementation callback:
  - Changed from `85/88` to `88/90` and inserted first-draft formal `DrawOptionPaneSectionHeader` C++.
  - Evidence: current MCP session `supervisor_resume_20260629` reconfirmed modeled `sub_541660` size `0x70`, four `NewOptionPane::OnPaint` callers, GrafPort draw-state/text callees, color `128`, two draw passes at `(x,y)` and `(x+1,y)`, and no broader owner route. The helper name remains inferred.
