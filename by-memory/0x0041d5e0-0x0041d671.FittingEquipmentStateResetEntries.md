*** UID:0000WS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:82 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x0041d5e0-0x0041d671 FittingEquipmentState ResetEntries

## Summary

- Address range: `0x0041d5e0-0x0041d671` final observed instruction byte; IDA reports start `0x0041d5e0`, size `0x92`, and end-exclusive `0x0041d672`.
- Disposition: reconstructable NexusTK project code.
- Entity kind: embedded fitting-room item/equipment state method.
- Working owner label: [UID:00004Z][FittingEquipmentState](by-class/FittingEquipmentState.md).
- Likely source module: [UID:0000JE][FittingRoom](by-file/FittingRoom.md), near [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md).
- Rebuild handling: source-authored helper/state method, but probably nested inside the fitting-room dialog item-state implementation rather than a standalone original file.

## Owner And Signature

IDA shows a `__thiscall` method taking `this` in `ecx` and returning the same object pointer:

```cpp
FittingEquipmentState* __thiscall FittingEquipmentState::ResetEntries();
```

The only current code caller is [UID:000050][FittingRoomDialog](by-class/FittingRoomDialog.md) at `0x0041c404`, where `FittingRoomDialog::OnCommand` passes `this + 0x504`. That embeds this state inside the dialog item-state area constructed by `0x00422020`, so `FittingEquipmentState` is best treated as a recovered layout/method label until the whole fitting-room item-state object is rewritten.

## Behavior

The method resets an active vector of fixed-size equipment entries without releasing the vector storage itself. It walks entries from `this + 0x240` to `this + 0x244`, stepping by `0xb4` bytes per entry, releases each entry-local trailing buffer, zeros that buffer triplet, then collapses the vector by assigning `end = begin`.

| Object offset | Meaning |
| --- | --- |
| `+0x240` | `EquipmentEntry* begin` |
| `+0x244` | `EquipmentEntry* end` |
| `+0x248` | likely `EquipmentEntry* capacity`; initialized by the constructor and not modified here. |

Each `0xb4`-byte entry contains a vector/string-like heap buffer at the tail:

| Entry offset | Meaning |
| --- | --- |
| `+0x0a8` | buffer begin/allocation pointer |
| `+0x0ac` | buffer current pointer |
| `+0x0b0` | buffer end pointer |

If the buffer pointer is null, the entry is already empty and is skipped. Otherwise, the helper computes `(end - begin) & 0xfffffffc`. For spans at least `0x1000`, it treats `begin[-1]` as the true allocation base, verifies `begin - allocationBase - 4 <= 0x1f`, and calls `_invalid_parameter_noinfo_noreturn` if the header fails the MSVC aligned-allocation guard. It then frees the allocation base. For smaller spans, it frees `begin` directly through `sub_5C7526`.

After freeing, the method writes zero to the three entry buffer pointers. Once every active entry has been handled, it writes `this[145] = this[144]`, collapsing the active entry range to empty while leaving capacity untouched.

## Raw Code Evidence

IDA MCP `lookup_funcs` on 2026-05-30 reports:

| Address | IDA name | Size |
| --- | --- | ---: |
| `0x0041d5e0` | `sub_41D5E0` | `0x92` |

IDA MCP `callers` reports one caller, `0x0041c404` in `sub_41C310` (`FittingRoomDialog::OnCommand`). IDA MCP `callees` reports `_invalid_parameter_noinfo_noreturn` at `0x005cd607` and the free helper `sub_5C7526` at `0x005c7526`.

The core decompilation structure is:

```cpp
int end = this[145];
int begin = this[144];
if (begin != end) {
    DWORD* bufferEndSlot = (DWORD*)(begin + 0xb0);
    do {
        DWORD* bufferBegin = (DWORD*)bufferEndSlot[-2];
        if (bufferBegin != 0) {
            if (((*bufferEndSlot - (DWORD)bufferBegin) & 0xfffffffc) >= 0x1000) {
                if ((DWORD)bufferBegin - bufferBegin[-1] - 4 > 0x1f)
                    _invalid_parameter_noinfo_noreturn();
                bufferBegin = (DWORD*)bufferBegin[-1];
            }
            sub_5C7526(bufferBegin);
            bufferEndSlot[-2] = 0;
            bufferEndSlot[-1] = 0;
            bufferEndSlot[0] = 0;
        }
        begin += 0xb4;
        bufferEndSlot += 0x2d;
    } while (begin != end);
}
this[145] = this[144];
return this;
```

## Reconstructed C++ Candidate

The source-shape candidate below is illustrative only. The final source should use project-era style and should avoid presenting this helper label as proof of a separate original file.

```cpp
struct FittingEquipmentEntryBuffer
{
    unsigned char* begin;
    unsigned char* current;
    unsigned char* end;
};

struct FittingEquipmentEntry
{
    unsigned char reserved[0xa8];
    FittingEquipmentEntryBuffer buffer;
};

FittingEquipmentState* FittingEquipmentState::ResetEntries()
{
    for (FittingEquipmentEntry* entry = m_begin; entry != m_end; ++entry) {
        unsigned char* block = entry->buffer.begin;
        if (block != 0) {
            if (((entry->buffer.end - entry->buffer.begin) & ~3) >= 0x1000) {
                unsigned char* base = reinterpret_cast<unsigned char**>(block)[-1];
                if (block - base - 4 > 0x1f)
                    _invalid_parameter_noinfo_noreturn();
                block = base;
            }

            FreeHeapBlock(block);
            entry->buffer.begin = 0;
            entry->buffer.current = 0;
            entry->buffer.end = 0;
        }
    }

    m_end = m_begin;
    return this;
}
```

## Callers And Callees

- Caller: [UID:000050][FittingRoomDialog](by-class/FittingRoomDialog.md) command handling at `0x0041c404`, passing the embedded state at `this + 0x504`.
- Callees: `_invalid_parameter_noinfo_noreturn` and `sub_5C7526` heap free helper.
- No current IDA data xrefs to the function start.

## Touched State

- Reads `this + 0x240` and `this + 0x244` as active entry bounds.
- Writes per-entry buffer triplets at `entry + 0x0a8`, `entry + 0x0ac`, and `entry + 0x0b0`.
- Writes `this + 0x244` to match `this + 0x240`.
- Leaves the capacity field at `this + 0x248` intact.

## Confidence And Open Questions

- Confidence is strong for function bounds, caller, loop stride, entry-local buffer offsets, large-allocation guard behavior, and final `end = begin` state update.
- The final source-level owner remains medium-high rather than absolute. IDA proves the method is called on a `FittingRoomDialog` embedded subobject, but it does not prove whether the original source named that subobject `FittingEquipmentState`.
- The exact semantic name of the entry-local buffer is still open; it may represent item-name/state scratch storage rather than a generic byte buffer.

## Cross-References

- [UID:00004Z][FittingEquipmentState](by-class/FittingEquipmentState.md)
- [UID:0001UH][FittingEquipmentStateLayout](by-type/by-struct/FittingEquipmentStateLayout.md)
- [UID:0000JE][FittingRoom](by-file/FittingRoom.md)
- [UID:000050][FittingRoomDialog](by-class/FittingRoomDialog.md)
- [UID:000051][FittingRoomDialogItemState](by-class/FittingRoomDialogItemState.md)
- [UID:0000WR][0x0041ba40-0x004245f5.FittingRoomUiCore](by-memory/0x0041ba40-0x004245f5.FittingRoomUiCore.md)

## Changes

- 2026-05-30: Expanded the file from a short 50/65 summary into a detailed by-memory page and rescored it to 82/88. The update adds exact IDA bounds, caller/callee evidence, entry-layout offsets, raw-code structure, reconstruction candidate, touched state, and open questions. Evidence: IDA MCP `lookup_funcs`, `callers`, `callees`, and decompilation for `0x0041d5e0`.
- 2026-06-03: Replaced generated-source wording with an IDA-only ownership statement while keeping the score unchanged. Live IDA MCP still shows `sub_41D5E0` as a `0x92`-byte helper with one caller at `0x0041c404`, one constructor-backed embedded owner path through `sub_422020`, and callees `sub_5C7526` plus `__invalid_parameter_noinfo_noreturn`.
