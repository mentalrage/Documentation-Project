*** UID:0001IG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
SoundStatusPane::SoundStatusPane()
    : Pane(1), Singleton<SoundStatusPane>()
{
    m_statusLeft = g_screenWidth - 354;
    m_statusRight = m_statusLeft + 114;
    m_statusTop = g_screenHeight - 11;
    m_statusBottom = m_statusTop + 12;

    RectBounds statusBounds;
    InitRectBounds(&statusBounds,
                   m_statusLeft,
                   m_statusTop,
                   m_statusRight,
                   m_statusBottom);
}

SoundStatusPane::~SoundStatusPane()
{
}

bool SoundStatusPane::HandleKeyOrTextEvent(Event *event)
{
    unsigned char modifiers = event->m_payload.m_key.m_modifiers;
    unsigned char key = g_pEventMan->TranslateEventKey(
        event->m_payload.m_key.m_key,
        modifiers);

    if (event->m_type != kEventKeyDown) {
        return false;
    }

    if (key == 'm' && modifiers == kEventModifierControl) {
        if (g_pSoundManager->IsSoundEffectsEnabled()) {
            g_pSoundManager->DisableSoundEffects();
            g_pConfig->m_soundEffectsEnabled = 0;
        } else {
            g_pSoundManager->EnableSoundEffects();
            g_pConfig->m_soundEffectsEnabled = 1;
        }
    } else if (key == 'M' &&
               (modifiers & kEventModifierControl) != 0) {
        if (g_pMidiPlayer->IsPlaybackEnabled()) {
            g_pMidiPlayer->DisableMidiPlayback();
            g_pSoundManager->MuteMusic();
        } else {
            g_pMidiPlayer->EnableMidiPlayback();
            g_pSoundManager->UnmuteMusic();
        }
    } else {
        return false;
    }

    InvalidateRect(&m_visibleBounds);
    return true;
}

void SoundStatusPane::OnPaint()
{
    SetDrawMode(0);
    SetDrawColor(0);
    g_pfnFillRect(this, &m_visibleBounds);

    SetTextDrawMode(0);
    SetTextColor(0x8f);
    SetTextBackFillColor(0);
    MoveTo(0, 11);

    const wchar_t *musicStatus =
        g_pSoundManager->IsPlaybackEnabled() ? L"Music On" : L"Music Off";
    const wchar_t *soundStatus =
        g_pSoundManager->IsSoundEffectsEnabled() ? L"Sound On" : L"Sound Off";

    wchar_t statusText[128];
    swprintf_s(statusText, 128, L"%s/%s", soundStatus, musicStatus);
    DrawWideText(statusText, static_cast<int>(wcslen(statusText)));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact eight-function island: four source methods emit here, while constructor-unwind cleanup, two adjustor thunks, and the scalar deleting destructor remain compiler-generated; five internal and two boundary padding spans are preserved without split. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057bf70-0x0057c2cf - SoundStatusPane

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: `SoundStatusPane` class method island with compiler destructor/thunk support
- Class owner: [UID:0000DJ][SoundStatusPane](by-class/SoundStatusPane.md)
- Likely source file: [UID:0000NX][SoundStatusPane](by-file/SoundStatusPane.md)
- Evidence basis: live IDA MCP function, xref, vtable, decompilation, byte, and data-xref checks revalidated by B003 on 2026-06-17.
- Formal C++ handling: emit exactly the four ordinary source methods in CPP. Keep H blank because UID0000DJ owns every declaration. The constructor-unwind cleanup, adjustor thunks, and scalar deleting destructor are compiler artifacts covered by the ordinary constructor/destructor plus Singleton base lowering and must not be hand-emitted.

## Covered Range

IDA confirms this pane range starts at `0x0057bf70` and ends before `0x0057c2d0`, where `SpellInventoryPane` construction begins.

| Range | Source role | Notes |
| --- | --- | --- |
| `0x0057bf70-0x0057c04d` | `SoundStatusPane::SoundStatusPane()` | Constructor; installs primary and EventHandler subobject vtables, writes singleton, computes and stores screen-relative status bounds. |
| `0x0057c050-0x0057c079` | `SoundStatusPane::~SoundStatusPane()` body / non-deleting cleanup | Reinstalls vtables, clears singleton, and calls shared Pane cleanup. It is not an independent public helper. |
| `0x0057c080-0x0057c15b` | `SoundStatusPane::HandleKeyOrTextEvent` | Exact EventHandler override for sound-effect and MIDI/music toggles. |
| `0x0057c160-0x0057c23e` | `SoundStatusPane::OnPaint` | Exact Pane override that paints `Music On/Off` and `Sound On/Off` status text. |
| `0x0057c240-0x0057c24b` | `ClearSoundStatusPaneSingleton` cleanup helper | Constructor-unwind singleton clear support, referenced from EH/unwind metadata; not a standalone source method. |
| `0x0057c24b-0x0057c256` | adjustor thunk | Subtracts `0xa0` and forwards to scalar deleting destructor. |
| `0x0057c256-0x0057c261` | adjustor thunk | Subtracts `0xa4` and forwards to scalar deleting destructor. |
| `0x0057c270-0x0057c2cf` | scalar deleting destructor | Compiler-generated delete wrapper regenerated from the ordinary destructor/delete path. |

No split is recommended in this pass. The cleanup helper, adjustor thunks, and scalar deleting destructor are class-local compiler support for this method island; splitting them would not improve source placement or C++ readiness.

The five internal padding spans are `0x0057c04d-0x0057c050`, `0x0057c079-0x0057c080`, `0x0057c15b-0x0057c160`, `0x0057c23e-0x0057c240`, and `0x0057c261-0x0057c270`. They and the two boundary spans below are `CC` alignment bytes, not methods or source data.

## Neighboring Code

- `0x0057bf6e-0x0057bf70` is ignored two-byte padding after [UID:0001IF][0x0057bf40-0x0057bf6e.SoundPathVectorClear](by-memory/0x0057bf40-0x0057bf6e.SoundPathVectorClear.md).
- `0x0057c2cf-0x0057c2d0` is ignored one-byte padding before [UID:0001IH][0x0057c2d0-0x0057f742.SpellInventoryPanes](by-memory/0x0057c2d0-0x0057f742.SpellInventoryPanes.md).
- The preceding SoundManager/SoundPathVector helper family ends before this pane island; do not merge SoundStatusPane into SoundManager.

## Behavior Summary

The constructor lowers `Pane(1)` plus direct `Singleton<SoundStatusPane>` construction and computes a 114-by-12 status rectangle near the lower-right edge of the current screen. It stores four distinct integers: `m_statusLeft = g_screenWidth - 354`, `m_statusRight = m_statusLeft + 114`, `m_statusTop = g_screenHeight - 11`, and `m_statusBottom = m_statusTop + 12`. It then builds a non-escaping stack `RectBounds` from those values and passes it to `InitRectBounds`; that local is not hidden object state.

`HandleKeyOrTextEvent` reads modifiers and calls `g_pEventMan->TranslateEventKey` before checking `event->m_type`. Non-`kEventKeyDown` events return false. Lowercase `m` with modifiers exactly equal to `kEventModifierControl` toggles SoundManager effects and mirrors the result to `g_pConfig->m_soundEffectsEnabled`. Uppercase `M` with the Control bit set makes the duplicate paired calls required by the executable: MidiPlayer enable/disable plus SoundManager unmute/mute. Every other key returns false. A handled toggle calls `InvalidateRect(&m_visibleBounds)` and returns true; the binary's `-0xa0` adjustment is implicit in the override thunk/source-level owner view.

`OnPaint` sets draw mode/color zero, fills `m_visibleBounds` through `g_pfnFillRect`, sets text mode zero/color `0x8f`/background fill zero, and moves to `(0, 11)`. It reads SoundManager playback and effects state, selects the four exact wide captions, formats them into `wchar_t statusText[128]` with `L"%s/%s"`, and draws the exact resulting length.

## Direct Singleton RTTI, Layout, And Construction

- RTTI independently names `SoundStatusPane` and `Singleton<SoundStatusPane>`. The Singleton base-class descriptor records PMD `+0xf8,-1,0`, zero contained bases, and attributes `0x40`.
- Current `Pane` type evidence is `0xf8` bytes. Constructor callers at `0x004f8460` and `0x004f89c7` are both in `MainMenuPane_InitializeMainUiGraph`; each route performs `new(0x108)` before calling `0x0057bf70`.
- The best source-level relationship is direct `public Singleton<SoundStatusPane>` inheritance. Empty-base optimization allows that empty base to overlap the first derived status-bound dword at `+0xf8`; the four dwords through `+0x104` account for the complete `0x108` allocation.
- Constructor publication at `0x0057bfc0`, fallback clear at `0x0057bfc7`, ordinary destructor clear at `0x0057c06a`, constructor-unwind clear at `0x0057c240`, and scalar deleting destructor clear at `0x0057c290` are compiler/runtime lowering around the ordinary Singleton-aware constructor/destructor source.
- The cleanup helper, two adjustor thunks, and scalar deleting destructor remain binary coverage facts. Final human source should declare the direct base and ordinary methods and let MSVC regenerate these artifacts instead of hand-emitting decompiler-shaped wrappers.

## Singleton Global And Source Route

- Semantic global [UID:0000SB][g_pSoundStatusPane](by-global/g_pSoundStatusPane.md) is the sole source identity under [UID:0000NX][SoundStatusPane](by-file/SoundStatusPane.md), emitted at position `0` as `SoundStatusPane *g_pSoundStatusPane = NULL;` with a matching H forward/`extern` declaration.
- Physical page [UID:0002WA][0x0069b4d4-0x0069b4d8.g_pSoundStatusPane](by-memory/0x0069b4d4-0x0069b4d8.g_pSoundStatusPane.md) is exact loader-zeroed storage evidence only, owned by UID0000SB but false/non-emitting with blank formals.
- MapPane teardown reads the global at `0x005048e6` from separately documented `MapPane.cpp` code. That cross-translation-unit consumer proves external linkage and the header route; it does not move definition ownership away from `SoundStatusPane.cpp`.
- Reject file-local `static`, SoundManager/MidiPlayer/MapPane ownership, a duplicate physical-page definition, hand-written Singleton registry calls, and IDA/modern source forms such as `dword_69B4D4`, integer casts, `nullptr`, or smart pointers.

## Evidence Checked

- `lookup_funcs` confirms modeled starts at `0x0057bf70`, `0x0057c050`, `0x0057c080`, `0x0057c160`, `0x0057c240`, `0x0057c24b`, `0x0057c256`, `0x0057c270`, and successor `0x0057c2d0`.
- Constructor xrefs are from `InitializeMainUiGraph` at `0x004f8460` and `0x004f89c7`, matching newer and older main UI layout branches.
- Both constructor call paths allocate exactly `0x108` bytes. RTTI type descriptors name `SoundStatusPane` and `Singleton<SoundStatusPane>`; the Singleton BCD records PMD `248,-1,0` (`+0xf8,-1,0`) and attributes `0x40`.
- `g_pSoundStatusPane` refs cover UI teardown read `0x005048e6`, constructor publish/fallback `0x0057bfc0`/`0x0057bfc7`, cleanup clear `0x0057c06a`, constructor-unwind clear `0x0057c240`, and scalar destructor clear `0x0057c290`.
- Primary/secondary/tertiary vtables at `0x0062cef4`, `0x0062cf40`, and `0x0062cf70` match constructor install, cleanup/destructor reinstall, virtual dispatch data, and adjusted destructor thunks.
- `0x0057c240` has a constructor EH/unwind support xref near `0x00608661`, which supports compiler cleanup classification.
- Pane layout docs show derived Pane fields begin at `+0xf8` and EventHandler-style secondary views at `+0xa0` and `+0xa4`. This rejects the stale `TextButtonExControlPane` decompiler/base-name drift.
- SoundManager docs validate the consumed music/playback and sound-effect state helpers; MidiPlayer docs validate that SoundStatusPane is a consumer of MIDI playback helpers, not the MIDI owner.

## Source-Quality Decisions

- Source placement remains `NexusTK/ui/panels/SoundStatusPane.cpp`. SoundManager and MidiPlayer are dependencies; EventDispatcher/Pane provides shared event/layout infrastructure.
- Direct `Singleton<SoundStatusPane>` inheritance is now resolved through RTTI/PMD, two allocation routes, object size, and publication/clear behavior. Model the empty base in human class source; do not invent a physical registry member at `+0xf8` where EBO and the first status-bound dword overlap.
- The inherited-base question is resolved to common Pane layout with EventHandler secondary views at `+0xa0` and `+0xa4`; do not model this class as deriving from `TextButtonExControlPane`.
- The derived SoundStatusPane fields are four distinct integers at `+0xf8/+0xfc/+0x100/+0x104`, named `m_statusLeft`, `m_statusRight`, `m_statusTop`, and `m_statusBottom` in storage order. The inherited invalidation rectangle is `m_visibleBounds`; the constructor's `RectBounds` is a local.
- `event + 0x04` is the event kind; value `8` is the key-event discriminator. `event + 0x08` is the key/character byte. `event + 0x10a` is modifier/filter state; keep it generically named until the shared event enum is recovered.
- `0x0057c240` is constructor-unwind singleton clear support; `0x0057c24b` and `0x0057c256` are compiler adjustor thunks; `0x0057c270` is the scalar deleting destructor. These are documented for binary coverage but should be regenerated from source declarations rather than hand-written.
- The exact CPP channel emits four ordinary methods only. The non-deleting destructor body is represented by the empty ordinary destructor; Singleton clear and Pane teardown are implicit base destruction. Constructor-EH cleanup, two adjustor thunks, scalar deleting destructor, vtables, RTTI, and padding remain non-emitting compiler coverage.
- The H channel remains blank because UID0000DJ owns the complete declaration. UID0000SB separately owns the sole global CPP/H route; neither declaration family is duplicated here.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 94 | Exact formal CPP now covers all four ordinary methods; all four compiler functions and five internal/two boundary padding spans have explicit non-emitting dispositions. The direct Singleton layout, two allocation routes, semantic-global route, MapPane consumer, and blank class-owned H route are complete. |
| Confidence | 95 | Live function bytes/decompilation, callers, allocation sizes, RTTI/PMDs, vtable slots, singleton xrefs, event fields, four constructor stores, direct callees, literals, and padding all converge on the emitted source/compiler split. |

## Cross-References

- [UID:0000DJ][SoundStatusPane](by-class/SoundStatusPane.md)
- [UID:0000NX][SoundStatusPane](by-file/SoundStatusPane.md)
- [UID:0000SB][g_pSoundStatusPane](by-global/g_pSoundStatusPane.md)
- [UID:0002WA][0x0069b4d4-0x0069b4d8.g_pSoundStatusPane](by-memory/0x0069b4d4-0x0069b4d8.g_pSoundStatusPane.md)
- [UID:0000DG][SoundManager](by-class/SoundManager.md)
- [UID:0000NV][SoundManager](by-file/SoundManager.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:0000RM][g_pMidiPlayer](by-global/g_pMidiPlayer.md)
- [UID:0001Q8][client_audio](by-meta/client_audio.md)

## Changes

- 2026-08-24 B009 UID0000NX callback: Raised `89/92` to `94/95`. Replaced the obsolete aggregate-blank policy with exact source for four ordinary methods, retained a class-owned blank H channel, fixed override/field names and behavior order, and recorded all four compiler functions plus five internal and two boundary padding spans as non-emitting coverage.
- 2026-08-01 B010 UID0002WA callback: Raised `88/91` to `89/92`, preserving owner/emitter UID0000DJ and blank aggregate CPP/H. Added direct Singleton RTTI PMD `+0xf8,-1,0`, EBO-compatible `0x108` layout, both allocation callers, exact singleton publication/teardown lowering, sole UID0000SB CPP/H route, MapPane external consumer, physical UID0002WA non-emission, rejected alternatives, and revised score/formal-channel rationale.
- 2026-06-17 B003 source-quality execution: Raised completion/confidence from `82/88` to `88/91`; kept owner/emitter [UID:0000DJ][SoundStatusPane](by-class/SoundStatusPane.md); kept `RECONSTRUCTABLE:TRUE`; kept formal C++ blank by accepted aggregate/compiler-artifact policy.
  - Evidence: B003 live IDA/source-quality audit validated exact function inventory, constructor callers, Pane/EventHandler layout, bounds fields, vtable/subobject layout, event fields, config/MIDI/SoundManager consumer roles, singleton lifecycle, constructor-unwind clear helper, adjustor thunks, scalar deleting destructor, padding boundaries, and source placement.
- 2026-06-06: Raised completion/confidence from `70/85` to `82/88`, attached the method island to [UID:0000DJ][SoundStatusPane](by-class/SoundStatusPane.md), and removed stale generated-output/raw-label wording.
  - Reasoning: current live IDA MCP verifies the full function inventory, constructor callers from both main UI layout branches, exact singleton bytes and six refs, constructor/cleanup/destructor singleton writes, hotkey and caption behavior, adjustor thunks, and adjacent padding bytes.
