*** UID:000136 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
DIBitmap::~DIBitmap()
{
    if (m_hBitmap != NULL) {
        DeleteObject(m_hBitmap);
    }
}

void *DIBitmap::GetBits() const
{
    return m_pBits;
}

HBITMAP DIBitmap::GetBitmapHandle() const
{
    return m_hBitmap;
}

int DIBitmap::GetWidth() const
{
    if (m_pBits == NULL) {
        return 0;
    }

    return m_width;
}

int DIBitmap::GetHeight() const
{
    if (m_pBits == NULL) {
        return 0;
    }

    return m_height;
}

int DIBitmap::GetAlignedWidth() const
{
    if (m_pBits == NULL) {
        return 0;
    }

    return m_bmiHeader.biWidth;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B001-020 repaired DIBitmap destructor/accessor child; true half-open range is `0x004a1740-0x004a17ad`, byte `0x004a17ac` is the final `retn`, and direct parent is [UID:00003V][DIBitmap](by-class/DIBitmap.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004a1740-0x004a17ad DIBitmap Destructor And Accessors

## Status

- Confidence: strong for instruction boundaries, object-field behavior, owner/source route, and first-draft destructor/accessor C++; final original method/member spellings and `const` qualification remain inferred confidence caps.
- Entity kind: small `DIBitmap` method cluster, partly not modeled as IDA functions.
- Direct owner: [UID:00003V][DIBitmap](by-class/DIBitmap.md).
- Likely source file: [UID:0000IV][DIBitmap](by-file/DIBitmap.md).
- Confirmed layout: [UID:0001U6][DIBitmapLayout](by-type/by-struct/DIBitmapLayout.md)
- Address range: `0x004a1740-0x004a17ad` half-open; the last executable byte is `0x004a17ac`.
- Parent attachment: attached to [UID:00003V][DIBitmap](by-class/DIBitmap.md). This child is `90/93`, the direct class parent is `92/94`, and the class flows to [UID:0000IV][DIBitmap](by-file/DIBitmap.md). Emitter position `30` places these definitions after the class declaration at `10` and constructor at `20`.
- Final C++ status: first-draft formal C++ is now present. The old blank-C++ rationale depended on a superseded `95/95` final-source gate; current source-entry rules permit code because this target is reconstructable, has nonblank `EMITTER_UIDS:00003V`, surfaces to generated `DIBitmap.cpp`, and its combined score clears the active gate.
- Evidence basis: current IDA MCP refresh on 2026-07-05 under session `supervisor_recovery_20260705`, IDA MCP `py_eval`/disassembly/caller/xref checks on 2026-06-10 B001-020, earlier IDA MCP checks, and existing documentation treated as prior inference.

## Covered Range

| Range | IDA status | Proposed role |
| --- | --- | --- |
| `0x004a1740-0x004a175f` | raw code, no function start | `DIBitmap::~DIBitmap` non-deleting destructor body or constructor-unwind cleanup; resets the vtable, deletes `m_hBitmap`, then tail-jumps to `LObject` destruction. |
| `0x004a1760-0x004a1764` | IDA function | `DIBitmap::GetBits` provisional; returns `this->m_pBits` from `+0x30`. |
| `0x004a1770-0x004a1774` | IDA function | `DIBitmap::GetBitmapHandle` provisional; returns `this->m_hBitmap` from `+0x2c`. |
| `0x004a1780-0x004a178d` | IDA function | `DIBitmap::GetWidth` provisional; returns zero if `m_pBits` is null, otherwise returns requested width from `+0x34`. |
| `0x004a1790-0x004a179d` | IDA function | `DIBitmap::GetHeight` provisional; returns zero if `m_pBits` is null, otherwise returns requested height from `+0x38`. |
| `0x004a17a0-0x004a17ad` | raw code, no function start | `DIBitmap::GetAlignedWidth` provisional; returns zero if `m_pBits` is null, otherwise returns `BITMAPINFOHEADER.biWidth` from `+0x08`. |

Alignment bytes occupy `0x004a175f`, `0x004a1764-0x004a176f`, `0x004a1774-0x004a177f`, `0x004a178d-0x004a178f`, `0x004a179d-0x004a179f`, and `0x004a17ad-0x004a17af`; they are not reconstructable method code. The raw destructor's jump instruction occupies `0x004a175a-0x004a175e`; only `0x004a175f` is padding in that local gap.

The exact owned span is `0x004a1740-0x004a17ad` half-open. `0x004a17ad-0x004a17af` is alignment and `0x004a17b0` begins the next modeled `LoadPcxImage` function, so the later PCX loader/factory code belongs to [UID:000135][0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders](by-memory/0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders.md) and [UID:0000K3][ImageLoaders](by-file/ImageLoaders.md), not this child page.

## IDA Evidence

- `0x004a1746` stores the [UID:0001XE][DIBitmapDirectXVtables](by-type/by-vtable/DIBitmapDirectXVtables.md) address `0x00618e54`.
- The raw destructor body loads `m_hBitmap` from `+0x2c`, calls `DeleteObject` when non-null, restores `ecx`, and tail-jumps to `sub_4F4A90`, the `LObject` destructor path.
- IDA models the four accessors at `0x004a1760`, `0x004a1770`, `0x004a1780`, and `0x004a1790` as functions, but does not model function starts at `0x004a1740` or `0x004a17a0`.
- `lookup_funcs` reports `0x004a1740` and `0x004a17a0` as not modeled function starts, confirms `sub_4A1760`, `sub_4A1770`, `sub_4A1780`, and `sub_4A1790`, and reports `0x004a17b0` as the next function start.
- The accessors have no static callees; they only return fields or guarded field values.
- Latest modeled callers for `GetBitmapHandle` are `0x00581dd9` and `0x00581df8` inside `sub_581DC0`.
- Latest modeled callers for `GetWidth` are `0x0058127a` and `0x005812dc` inside `sub_581100`, plus `0x00581de5` inside `sub_581DC0`.
- Latest modeled callers for `GetHeight` are `0x00581286` and `0x005812e9` inside `sub_581100`, plus `0x00581dee` inside `sub_581DC0`.
- IDA reports no modeled callers for `GetBits` at `0x004a1760` or the raw aligned-width accessor fragment at `0x004a17a0` in the current database.

## 2026-07-05 B001 Source-Quality MCP Refresh

- MCP session `supervisor_recovery_20260705` was active for `NexusTK.exe.i64`; `server_health` returned `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` confirms the raw starts are still not modeled as IDA functions: `0x004a1740` and `0x004a17a0` both return not-a-function. The modeled middle accessors remain `sub_4A1760` size `0x4`, `sub_4A1770` size `0x4`, `sub_4A1780` size `0xd`, and `sub_4A1790` size `0xd`; `0x004a17b0` remains the next modeled helper start with `sub_4A17B0` size `0xf8`.
- Bounded `insn_query` over `0x004a1740-0x004a17b0` reconfirms the target as the same DIBitmap method cluster:
  - `0x004a1740-0x004a175f`: raw non-deleting destructor body, loading `[esi+2Ch]`, writing `DIBitmap::vftable` at `0x004a1746`, testing the handle, calling imported `DeleteObject`, restoring `ecx`, and tail-jumping to `sub_4F4A90`.
  - `0x004a1760-0x004a1764`: modeled accessor returning `[ecx+30h]`.
  - `0x004a1770-0x004a1774`: modeled accessor returning `[ecx+2Ch]`.
  - `0x004a1780-0x004a178d`: modeled guarded accessor returning zero if `[ecx+30h]` is null, otherwise `[ecx+34h]`.
  - `0x004a1790-0x004a179d`: modeled guarded accessor returning zero if `[ecx+30h]` is null, otherwise `[ecx+38h]`.
  - `0x004a17a0-0x004a17ad`: raw guarded aligned-width accessor returning zero if `[ecx+30h]` is null, otherwise `[ecx+8]`.
- Current bytes reconfirm the boundary: the target bytes start at `0x004a1740` with `56 8b f1 8b 46 2c c7 06 54 8e 61 00`; `0x004a17ac` is the final executable `retn`; and `0x004a17ad-0x004a17b0` is `cc cc cc` alignment padding before `LoadPcxImage`.
- Current vtable bytes at `0x00618e50-0x00618e60` are `d0 67 64 00 10 1b 4a 00 10 4b 4f 00 c0 b6 41 00`. Current xrefs to the DIBitmap vtable address `0x00618e54` are exactly `0x004a1638`, `0x004a1746`, `0x004a195c`, and `0x004a1b19`, tying this target to the constructor, PCX factory construction path, and scalar deleting destructor.
- Current target xref inventory:
  - `0x004a1740`: no xrefs.
  - `0x004a1760`: no xrefs.
  - `0x004a1770`: two code xrefs, `0x00581dd9` and `0x00581df8` in `sub_581DC0`.
  - `0x004a1780`: five code xrefs, `0x0058127a` and `0x005812dc` in `sub_581100`, raw/unmodeled startup-helper refs `0x00581946` and `0x005819a6`, and `0x00581de5` in `sub_581DC0`.
  - `0x004a1790`: five code xrefs, `0x00581286` and `0x005812e9` in `sub_581100`, raw/unmodeled startup-helper refs `0x00581953` and `0x005819b3`, and `0x00581dee` in `sub_581DC0`.
  - `0x004a17a0`: no xrefs.
- Negative ref checks found no code refs, no data refs, and no little-endian pointer byte hits for raw starts `0x004a1740` or `0x004a17a0`. The absence of raw-start xrefs remains a reachability/naming caveat, not an ownership or C++ blocker, because the bounded bytes are source-shaped DIBitmap methods inside the exact class cluster.
- Accessor decompilation still returns `this[12]`, `this[11]`, guarded `this[13]`, and guarded `this[14]` for the four modeled functions. `GetBits` and `GetAlignedWidth` have no direct call xrefs in current IDA, but the accepted [UID:000315][0x004a18b0-0x004a1b0c.CreateDIBitmapFromPcxBuffer](by-memory/0x004a18b0-0x004a1b0c.CreateDIBitmapFromPcxBuffer.md) source shape uses equivalent `DIBitmap` bits and aligned-width operations, while [UID:0004D5][0x00581dc0-0x00581e35.StartupNoticeDrawPcxImage](by-memory/0x00581dc0-0x00581e35.StartupNoticeDrawPcxImage.md) uses the bitmap-handle, width, and height accessors.
- Pre-callback generated state was checked as lead material only: `auto-generated/NexusTK/render/DIBitmap.cpp` at validator command `000000006959`, refreshed `2026-07-05T10:43:05-04:00`, source by-file UID0000IV, still emitted UID000136 as an empty marker before this C++ update.

## 2026-06-10 B001-020 Live IDA MCP Recheck

- `py_eval` against `NexusTK.exe` (`md5=4247e04e20b65d6414c7238aa8ff5515`) confirms modeled starts inside the containing aggregate at `0x004a1600`, `0x004a1760`, `0x004a1770`, `0x004a1780`, `0x004a1790`, `0x004a17b0`, `0x004a18b0`, and `0x004a1b10`. It still reports no modeled function starts at raw fragments `0x004a1740` or `0x004a17a0`.
- Raw disassembly confirms the non-deleting destructor fragment at `0x004a1740-0x004a175f`: load `[this+0x2c]`, store `??_7DIBitmap@@6B@` at `0x004a1746`, call imported `DeleteObject` at `0x004a1751` when the handle is non-null, then jump from `0x004a175a` to `sub_4F4A90`.
- Raw disassembly confirms the modeled accessors: `0x004a1760` returns `[ecx+0x30]`, `0x004a1770` returns `[ecx+0x2c]`, `0x004a1780` returns zero or `[ecx+0x34]`, and `0x004a1790` returns zero or `[ecx+0x38]`.
- Raw disassembly confirms the unmodeled aligned-width accessor at `0x004a17a0-0x004a17ad`; byte `0x004a17ac` is the final `retn`, and the half-open end is `0x004a17ad`.
- `xrefs_to 0x00618e54` reports DIBitmap vtable stores/refs at `0x004a1638`, `0x004a1746`, `0x004a195c`, and `0x004a1b19`, tying this raw destructor/accessor cluster to the same class as the constructor, PCX factory inline construction path, and scalar deleting destructor.
- Caller evidence remains DIBitmap-consumer shaped: `GetBitmapHandle` has two direct callers in `sub_581DC0`; `GetWidth` and `GetHeight` have direct callers in `sub_581100`, unmodeled startup helper bytes around `0x00581946/0x00581953/0x005819a6/0x005819b3`, and `sub_581DC0`. No evidence points to ImageLoaders or DAT ownership for this child.

## Generated-Data Caveat

Generated output is not authority for this page. Before the 2026-07-05 source-quality callback, generated `auto-generated/NexusTK/render/DIBitmap.cpp` was still an empty-marker route for UID000136 at validator command `000000006959`, refreshed `2026-07-05T10:43:05-04:00`. That empty marker reflected missing documentation C++ rather than evidence that the binary method cluster was absent.

Existing generated metadata has previously omitted this `0x004a1740-0x004a17ad` cluster and has reported incomplete DIBitmap vtable/layout details, while current IDA evidence proves the vtable store, `DeleteObject` path, and compact field accesses through `+0x38`.

The field offsets used here are from IDA-confirmed instructions and are documented in [UID:0001U6][DIBitmapLayout](by-type/by-struct/DIBitmapLayout.md). Do not widen this child page from generated metadata alone.

## Constructor, Size, And Failure Relationship

- [UID:000313][0x004a1600-0x004a1738.DIBitmapConstructor](by-memory/0x004a1600-0x004a1738.DIBitmapConstructor.md) clears `m_pBits`, installs the complete embedded header, stores the `CreateDIBSection` handle at `+0x2c`, and then stores requested dimensions at `+0x34/+0x38`. These accessors independently confirm every constructor field name used by the formal class/constructor source.
- [UID:000315][0x004a18b0-0x004a1b0c.CreateDIBitmapFromPcxBuffer](by-memory/0x004a18b0-0x004a1b0c.CreateDIBitmapFromPcxBuffer.md) allocates literal 60 bytes and inlines the constructor, proving exact DIBitmap size `0x3c` / 60 (Verified with `int_convert.py`) rather than the superseded generated `0x60`/96-byte claim.
- `m_hBitmap` owns the GDI DIB section. The ordinary destructor's conditional `DeleteObject` releases both the handle and GDI-managed pixel storage; `m_pBits` is a non-owning view and must not be freed separately.
- The constructor does not reject a failed `CreateDIBSection` result. The factory checks the equivalent `GetBits()` state and invokes deletion, making this ordinary destructor the source cleanup used by failure policy; the scalar deleting wrapper remains compiler-generated support.

## Ownership And Rejected Alternatives

- Keep direct owner [UID:00003V][DIBitmap](by-class/DIBitmap.md) and source route [UID:0000IV][DIBitmap](by-file/DIBitmap.md). The target contains DIBitmap class lifecycle/accessor bodies, uses the DIBitmap vtable and DIBitmap layout fields, and shares vtable refs with the constructor and scalar deleting destructor siblings.
- Reject [UID:0000K3][ImageLoaders](by-file/ImageLoaders.md) as direct owner. ImageLoaders owns the adjacent PCX free helpers beginning at `0x004a17b0` and the PCX-to-DIB factory at `0x004a18b0`; UID000136 is class method code used by those helpers.
- Reject [UID:000135][0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders](by-memory/0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders.md) as an emitting owner. The aggregate remains a non-emitting mixed inventory crossing DIBitmap methods and ImageLoaders helpers.
- Reject `DIBitmapDirectXVtables`, DirectX, DAT/archive, zlib, LodePNG, LibJPEG, and UI feature ownership for this target. They are support, unrelated third-party/library contexts, or consumer contexts; none owns the DIBitmap destructor/accessor declarations.
- Preserve raw-fragment caveat: `0x004a1740` and `0x004a17a0` are not IDA functions and have no direct refs or pointer hits. This affects final-audit confidence and optional IDA hygiene, but it does not justify moving the target, blanking C++, or treating the bytes as padding.

## Score Rationale

`COMPLETION:90` reflects strict-gate coverage of the local instruction spans, corrected half-open endpoint, alignment bytes, direct class parent routing and emitter order, child/aggregate boundary, exact `0x3c` layout relationship, field accesses, GDI ownership/failure cleanup, modeled and raw caller/xref inventory, negative raw-start ref checks, DIBitmap vtable refs, generated-empty-marker history, and accepted formal first-draft C++.

`CONFIDENCE:93` reflects direct IDA disassembly/function boundaries, exact factory allocation, matching constructor/layout evidence, DIBitmap vtable refs, generated-route ordering through [UID:00003V][DIBitmap](by-class/DIBitmap.md) to [UID:0000IV][DIBitmap](by-file/DIBitmap.md), and source-shape support from the PCX factory and StartupNotice draw helper docs. It stays below final-audit quality because original accessor/member spellings and `const` qualification remain inferred, and the destructor/aligned-width bodies are not modeled as functions in IDA.

## Cross-References

- [UID:00003V][DIBitmap](by-class/DIBitmap.md)
- [UID:0000IV][DIBitmap](by-file/DIBitmap.md)
- [UID:0001U6][DIBitmapLayout](by-type/by-struct/DIBitmapLayout.md)
- [UID:000135][0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders](by-memory/0x004a1600-0x004a1b5e.DIBitmapAndPcxLoaders.md)
- [UID:0001XE][DIBitmapDirectXVtables](by-type/by-vtable/DIBitmapDirectXVtables.md)
- [UID:0000K3][ImageLoaders](by-file/ImageLoaders.md)

## Changes

- 2026-07-12 B002 accepted DIB support callback:
  - Changed `88/91 -> 90/93` and emitter position blank -> `30`; preserved the accepted destructor/accessor formal block unchanged.
  - Added exact `0x3c` size proof, constructor field-name/store parity, GDI handle/pixel lifetime, factory failure-policy cleanup, and class/constructor/accessor generated order.

- 2026-07-05 B001 source-quality implementation:
  - Changed from `85/90` to `88/91`.
  - Added first-draft formal C++ for `DIBitmap::~DIBitmap`, `GetBits`, `GetBitmapHandle`, `GetWidth`, `GetHeight`, and `GetAlignedWidth`.
  - Summary/evidence: current IDA MCP session `supervisor_recovery_20260705` returned server `ok` and imagebase `0x400000`; bounded `lookup_funcs`, `insn_query`, `get_bytes`, `xref_query`, `find`, and `find_bytes` reconfirmed the raw destructor/accessor cluster, modeled accessors, padding bytes `cc cc cc` at `0x004a17ad-0x004a17b0`, exact DIBitmap vtable xrefs, no raw-start code/data/pointer refs, and generated pre-callback empty-marker state. The former blank-C++ rationale based on a `95/95` gate is superseded; remaining original-name and raw-fragment uncertainties are confidence caps only.
- 2026-06-10 B001-020 strict-gate repair:
  - Changed from `82/88`, parent [UID:0000IV][DIBitmap](by-file/DIBitmap.md), to `85/90`, parent [UID:00003V][DIBitmap](by-class/DIBitmap.md).
  - Reason: by-structure direct ownership for this class method cluster is the `DIBitmap` class, not the broader file page. The child, class parent, and file parent now all clear the current 85/85 gate.
  - Evidence: live IDA MCP confirms the raw destructor body, tail jump, four modeled accessors, raw aligned-width accessor, corrected `0x004a1740-0x004a17ad` half-open range, alignment bytes, callers, `DeleteObject`/`LObject` destructor path, compact DIBitmap field accesses, and DIBitmap vtable refs.
- 2026-06-06 A002 refresh:
  - What existed before: `COMPLETION:70`, `CONFIDENCE:85`, and blank `AUTOGEN_PARENT_UID`; evidence still referenced generated output as an authority caveat.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID:0000IV`, expanded boundary/caller/callee notes, and generated-output language limited to a non-authoritative caveat.
  - Summary/evidence: live IDA MCP confirms the raw destructor fragment, four modeled accessors, raw aligned-width fragment, alignment bytes, next `LoadPcxImage` boundary, `DeleteObject`/`LObject` destructor path, DIBitmap fields, and modeled callers. Parent attachment is valid because this child and [UID:0000IV][DIBitmap](by-file/DIBitmap.md) both clear the 80/80 gate.
