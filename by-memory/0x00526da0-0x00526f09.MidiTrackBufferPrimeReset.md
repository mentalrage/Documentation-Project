*** UID:0002BY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void MidiPrimeTracks(MidiDocument* document)
{
    MidiTrackState* track;

    document->currentTick = 0;
    track = document->tracks;

    for (DWORD i = 0; i < document->trackCount; ++i, ++track) {
        DWORD bytesRead;
        DWORD firstRead;

        MidiSeekSource(document, (LONG)track->trackStartOffset, FILE_BEGIN);

        firstRead = track->trackLength;
        if (firstRead > 0x400) {
            firstRead = 0x400;
        }

        if (MidiReadRaw(document, track->buffer, firstRead, &bytesRead) == 0 ||
            bytesRead != firstRead) {
            return;
        }

        track->bytesRemaining = track->trackLength - bytesRead;
        track->bufferedBytes = bytesRead;
        track->sourceOffset = MidiSeekSource(document, 0, FILE_CURRENT);
        track->cursor = track->buffer;
        track->flags = 0;
        track->runningStatus = 0;
        track->pendingTick = 0;

        if (track->bufferedBytes != 0 || track->bytesRemaining != 0) {
            if (MidiReadVariableLength(document, track, &track->pendingTick) != 0) {
                return;
            }

            MidiSeekSource(document, (LONG)(track->trackStartOffset + track->trackLength), FILE_BEGIN);
        } else {
            track->flags = MIDI_TRACK_DONE;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: `MidiPrimeTracks` resets document tick state, primes each 40-byte track with a 0x400-byte initial read, seeds first VLQ deltas, marks empty tracks done, and returns early without a status. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00526da0-0x00526f09 MidiTrackBufferPrimeReset

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: function
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for behavior, modeled boundary, direct stream-fill caller, per-track reset flow, source seek/read branches, VLQ dependency, end-state replay call site, SMF loader consistency, and MIDI source-family ownership; medium-high for descriptive helper and track-record names because original symbols remain unrecovered.
- Autogen status: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because this exact function and the parent file meet the >=80 confidence threshold.

## Address Range

`0x00526da0-0x00526f09`.

IDA models this exact range as `sub_526DA0`, size `0x169`.

## Behavior

`MidiPrimeTracks` primes or resets all loaded SMF track records before playback or end-state replay. It clears the document current tick, iterates the per-track table, positions each track at its stored file offset, reads an initial chunk clamped to `0x400` bytes, clears running-status/delta scratch state, decodes each nonempty track's first delta through the VLQ reader, marks empty tracks done, and returns early on read/short-read/VLQ failure without reporting a nonzero status to the caller.

## Evidence

- IDA `lookup_funcs` confirms `sub_526DA0` at `0x00526da0-0x00526f09`.
- IDA live analysis shows calls to `SetFilePointer`, `ReadFile`, and the VLQ reader at `0x00526bb0`.
- Parent page decompilation notes identify iteration across `this + 0x10` track records and resetting running-status/delta fields.

## 2026-06-06 Live IDA MCP Evidence

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms:

- IDA models `sub_526DA0` exactly at `0x00526da0-0x00526f09`, size `0x169`; `0x00526f09` is not a separate function entry.
- The single direct caller is `0x005271fc` inside [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md).
- Callees are `SetFilePointer`, `ReadFile`, and [UID:0002BW][0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader](by-memory/0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader.md).
- Decompilation shows the loop count comes from `this+0x0c`, the track table pointer comes from `this+0x10`, and each track record advances by 40 bytes.
- Per track, the helper seeks to stored track start offset `+0x1c`, reads up to `min(track_length +0x04, 0x400)` bytes into buffer pointer `+0x0c`, and fails the track if the read fails or returns a short count.
- It then writes remaining source bytes `+0x24`, buffered byte count `+0x08`, current source offset `+0x20`, cursor `+0x10`, clears flags at `+0x00`, clears running status byte `+0x18`, clears first delta/current delta field `+0x14`, and uses the VLQ reader to decode the first delta into `+0x14` when data remains.
- Empty tracks set track flag word `+0x00` to `1`; successful tracks seek back to track start plus track length before advancing to the next track record.
- Bytes at `0x00526d9a-0x00526da0` and `0x00526f09-0x00526f10` are `0xcc` padding, isolating the function between [UID:0002BX][0x00526ce0-0x00526d9a.MidiTrackBufferRefill](by-memory/0x00526ce0-0x00526d9a.MidiTrackBufferRefill.md) and [UID:0002BZ][0x00526f10-0x00526f57.MidiSourceOpenHelper](by-memory/0x00526f10-0x00526f57.MidiSourceOpenHelper.md).

## Reconstruction Notes

The exact track record layout should be synchronized with a future by-type struct page for the 40-byte SMF track record.

`RECONSTRUCTABLE:TRUE` is appropriate because this is source-authored track initialization/reset logic in the MIDI reader island. The formal C++ block now emits private file-scope `MidiPrimeTracks` through [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md).

The older blank-C++ blocker is superseded. `MidiPrimeTracks`, `MidiDocument`, `MidiTrackState`, `MidiSeekSource`, `MidiReadRaw`, and `MidiReadVariableLength` are accepted descriptive names for draft source. Missing original type pages for the document and 40-byte track record still cap final-audit confidence, but they no longer block this helper's source body. The helper remains in `MidiPlayer.cpp`; a separate private `MidiSMFDocument.cpp` split is still only a historical possibility.

B013's 2026-06-30 raw-read polarity recheck corrects the formal body: `MidiReadRaw` returns nonzero on successful DATFile/`ReadFile` reads, so the failure test must be `MidiReadRaw(document, track->buffer, firstRead, &bytesRead) == 0 || bytesRead != firstRead`. This matches current MCP decompilation of `sub_526DA0`, which checks `!v8 || bytesRead != requested`.

## Score Rationale

The score was raised because the page records its reconstructable status and parent assignment, with strong existing evidence for the modeled IDA function boundary, per-track iteration, source seek/read calls, and VLQ reader dependency.

Raised again from `74/84` to `80/88` on 2026-06-06 after live IDA confirmed the direct stream-fill caller, callee set, exact per-track reset field map, 0x400-byte initial buffer clamp, first-delta VLQ decode, empty-track flag handling, and padding. Completion remains below high range because the 40-byte track record layout still needs a dedicated type page and final field names.

Raised from `80/88` to `86/90` on 2026-06-16 after current decompilation reconfirmed the one caller at `0x005271fc`, the complete 40-byte track-record iteration, the adapter/Win32 branch equivalence, the first-delta decode, the seek-back behavior after each track is primed, and the absence of a project-defined MIDI document/source-adapter/track-record type in IDA and by-type docs. It remains below final-C++ range because the helper name, context type, track-record struct, and final source-file split are still not proven source declarations.

Raised from `86/90` to `88/91` on 2026-06-29 after Agent-B012's accepted current MCP pass on session `992d23de` resolved the stale C++ blocker, proved the only call is the end-state replay call at `0x005271fc` in [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md), confirmed the `void`/side-effect-only call shape, and supplied the formal `MidiPrimeTracks` body. The score stays below final-audit range because exact original symbols and shared `MidiDocument` / `MidiTrackState` type declarations remain unrecovered.

## 2026-06-16 Source-Core Refresh

Live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed and extended the source-core evidence:

- `lookup_funcs 0x00526da0` reports `sub_526DA0`, size `0x169`; `0x00526f09` is padding, not another function.
- `xrefs_to 0x00526da0` returns the single stream-fill call at `0x005271fc` inside [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md).
- `func_profile` reports 130 instructions, 23 basic blocks, one caller, and callees `SetFilePointer`, `ReadFile`, and [UID:0002BW][0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader](by-memory/0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader.md).
- Current decompilation shows `this+0x0c` track count, `this+0x10` track table, `this+0x14` adapter-active flag, `this+0x18` adapter/source subobject, and `this+0x2c` Win32 handle. Each iteration advances by `0x28` bytes.
- Per-track behavior is fully visible: seek to track `+0x1c`, read `min(+0x04, 0x400)` bytes into `+0x0c`, set remaining bytes `+0x24`, buffered count `+0x08`, source cursor `+0x20`, current cursor `+0x10`, clear flag/running-status/delta at `+0x00/+0x18/+0x14`, decode the first delta through `0x00526bb0`, then seek to `trackStart + trackLength`.
- Empty tracks set the track flag at `+0x00` to `1`; read failure, short read, or VLQ failure returns through the common zero return path without promoting a distinct error code.
- `get_bytes` reconfirmed `0x00526d9a-0x00526da0` and `0x00526f09-0x00526f10` are `0xcc` padding around the modeled body.
- IDA `type_query`/`search_structs` and by-type searches found no project-defined MIDI document, source adapter, SMF, or 40-byte track-record type; only WinMM `midihdr_tag` and unrelated Win32 `tagTRACKMOUSEEVENT` matched. That keeps the field names role-based.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:00023C][0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers](by-memory/0x005271b0-0x005277bb.MidiSMFLoadAndStreamFillHelpers.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)

## 2026-06-29 B012 Source-Quality Implementation

Agent-B012's accepted implementation callback used current MCP session `992d23de` at the project endpoint `127.0.0.1:13337`. The pass preserves the exact `0x00526da0-0x00526f09` range, the `0xcc` padding at `0x00526d9a-0x00526da0` and `0x00526f09-0x00526f10`, the callee set of `SetFilePointer`, `ReadFile`, and [UID:0002BW][0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader](by-memory/0x00526bb0-0x00526cd9.MidiVariableLengthQuantityReader.md), and the negative type-query evidence for recovered project `MidiDocument` / `MidiTrackState` declarations.

The current source-facing field map is: document `+0x0c` track count, `+0x10` track table, `+0x30` `currentTick`; track `+0x00` flags, `+0x04` length, `+0x08` buffered bytes, `+0x0c` buffer, `+0x10` cursor, `+0x14` pending tick, `+0x18` running status, `+0x1c` track start offset, `+0x20` source offset, and `+0x24` remaining source bytes. The emitted helper intentionally returns `void`: the binary exits early on read/short-read/VLQ failure and the only caller does not consume a status. [UID:0002C6][0x005271b0-0x005273a0.MidiStreamBufferFillHelper](by-memory/0x005271b0-0x005273a0.MidiStreamBufferFillHelper.md) records the matching repair that this helper is called only for end-state replay when `allowEndReplay` is true, not during ordinary reset/first-fill.

## Changes

- 2026-06-30 B013 support correction: score unchanged at `88/91`; changed the formal `MidiPrimeTracks` raw-read failure test from `MidiReadRaw(...) != 0` to `MidiReadRaw(...) == 0`. Current MCP proves `MidiReadRaw` returns nonzero success and that the decompiled caller checks `!v8 || bytesRead != requested`.
- 2026-06-03: Changed score from `68/82` to `74/84`, marked the function reconstructable, and attached it to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md).
  - Before: the page documented the modeled function and behavior but had no reconstructable/autogen metadata.
  - After: the page records source-authored MIDI track initialization/reset ownership and why final C++ remains blank.
  - Evidence: existing IDA-backed boundary, `SetFilePointer`/`ReadFile`/VLQ-reader call notes, and the parent `MidiPlayer` file score/placement.
- 2026-06-06: Raised from `74/84` to `80/88` after live IDA MCP reconfirmed exact `sub_526DA0` bounds, stream-fill caller `0x005271fc`, `SetFilePointer`/`ReadFile`/VLQ callees, 40-byte track iteration, field reset/update map, empty-track flagging, first-delta decode, and padding. Final C++ remains blank below the source-grade threshold.
- 2026-06-16 source-core refresh:
  - What existed before: score `80/88`, with exact behavior documented but the current assignment required a fresh caller/type/source-quality check.
  - Changed to: score `86/90`; owner/emitter route remains [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md), and final C++ remains blank.
  - Summary/evidence: live IDA session `b001_mappane_0001AW_20260616` reconfirmed modeled size `0x169`, single stream-fill caller `0x005271fc`, current decompilation of track-table iteration and adapter/Win32 seek/read branches, first-delta VLQ decode, padding on both sides, and active IDA/by-type checks that found no source-quality MIDI context or 40-byte track-record declaration.
- 2026-06-29 B012 implementation:
  - Changed to: score `88/91`; owner/emitter/reconstructable/position unchanged; formal `static void MidiPrimeTracks(MidiDocument* document)` C++ populated.
  - Summary/evidence: current MCP session `992d23de` confirmed exact range/padding, single caller `0x005271fc`, 0x400-byte initial read clamp, per-track field map, document `currentTick`, first-delta VLQ seeding, empty-track flagging, seek-back behavior, no nonzero status semantics, and rejected covered-by/class-owner/`MidiSMFDocument.cpp` alternatives for the current route.
