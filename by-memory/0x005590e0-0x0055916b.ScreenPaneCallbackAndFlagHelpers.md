*** UID:00039Y | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace {
const int kScreenPaneFadeInEvent = 0x4664496e;  // FdIn
const int kScreenPaneFadeOutEvent = 0x46644f74; // FdOt
}

int ScreenPane::QueueFadeInCallback(LObject *callbackObject)
{
    m_fadeTransitionState = 0;
    return TimerHandler::ScheduleTimer(kScreenPaneFadeInEvent, 0, callbackObject, 0);
}

int ScreenPane::QueueFadeOutCallback(LObject *callbackObject)
{
    m_fadeTransitionState = 0;
    return TimerHandler::ScheduleTimer(kScreenPaneFadeOutEvent, 0, callbackObject, 0);
}

void ScreenPane::SetFadeTransitionState(unsigned char state)
{
    m_fadeTransitionState = state;
}

bool ScreenPane::IsFadeTransitionActive() const
{
    return m_fadeTransitionState != 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B005 2026-06-19 source-quality reanalysis reroutes the direct owner/emitter to [UID:0000CB][ScreenPane](by-class/ScreenPane.md) through [UID:0000NB][ScreenPane](by-file/ScreenPane.md), names `sub_5590E0`/`sub_559110` as `QueueFadeInCallback`/`QueueFadeOutCallback` for `FdIn`/`FdOt` screen fade events, resolves `this+0x5a4` as the ScreenPane fade/presentation transition state, preserves raw `0x00559140`/`0x00559160` as retained `SetFadeTransitionState`/`IsFadeTransitionActive` helpers with no direct xrefs, confirms exact internal padding including `0x00559151-0x00559160`, and recommends first-draft C++ while capping completion for raw-helper liveness plus final header/member spelling. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005590e0-0x0055916b ScreenPane Callback And Flag Helpers

## Status

- Disposition: reconstructable NexusTK source-authored ScreenPane callback and transition-state helper cluster.
- Direct semantic owner: [UID:0000CB][ScreenPane](by-class/ScreenPane.md).
- Source route: [UID:0000NB][ScreenPane](by-file/ScreenPane.md), `NexusTK/ui/core/ScreenPane.cpp`.
- Split source: [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md) mixed `SurfacePresentation` aggregate.
- Score after B005 source-quality implementation: `88/90`.
- Rebuild handling: `source-authored`. The modeled queue helpers and retained raw setter/predicate are emitted as a small ScreenPane method cluster because the owner route clears the active C++ gate and no executable subrange is padding or compiler glue.

## Exact Boundary And Split Decision

The target range remains the exact half-open range `0x005590e0-0x0055916b`. No additional split is required before implementation because all executable bodies operate on the same ScreenPane object state and route through the same class/file owner. A future one-function-per-page split would be an output-style decision only; it is not required for ownership, range repair, or first-draft C++.

| Range | Binary status | Source-facing role | Implementation decision |
| --- | --- | --- | --- |
| `0x005590e0-0x00559108` | Modeled `sub_5590E0`, exported size 40 bytes | `ScreenPane::QueueFadeInCallback(LObject *callback)` | Emit as source method. Clears transition state and schedules `FdIn`. |
| `0x00559108-0x00559110` | padding | `0xcc` alignment | Do not emit. Already represented as ignored padding. |
| `0x00559110-0x00559138` | Modeled `sub_559110`, exported size 40 bytes | `ScreenPane::QueueFadeOutCallback(LObject *callback)` | Emit as source method. Clears transition state and schedules `FdOt`. |
| `0x00559138-0x00559140` | padding | `0xcc` alignment | Do not emit. Already represented as ignored padding. |
| `0x00559140-0x00559151` | Raw/Ghidra-modeled helper, no direct caller list | `ScreenPane::SetFadeTransitionState(unsigned char state)` | Emit as retained source-shaped method with liveness caveat. |
| `0x00559151-0x00559160` | padding | Corrected all-`0xcc` span | Do not emit. This is the true padding span; stale `0x0055914f` and `0x00559150` padding starts are invalid. |
| `0x00559160-0x0055916b` | Raw predicate, no exported function file found | `ScreenPane::IsFadeTransitionActive() const` | Emit as retained source-shaped method with liveness caveat. |

The corrected setter boundary is material: `0x0055914e` starts the `retn 4`, and `0x00559150` is the final immediate byte. Padding begins at `0x00559151`, not at `0x0055914f` or `0x00559150`.

## Behavior Summary

This cluster manages ScreenPane fade/presentation callback scheduling and the transition-state field at ScreenPane offset `+0x5a4`.

- `QueueFadeInCallback` and `QueueFadeOutCallback` reset `m_fadeTransitionState` to `0`, then call inherited `TimerHandler::ScheduleTimer` through Pane's TimerHandler facet at primary `+0xa4`.
- `QueueFadeInCallback` uses exact event id `0x4664496e` (`FdIn`).
- `QueueFadeOutCallback` uses exact event id `0x46644f74` (`FdOt`).
- The two constants are stored as exact integers with comments. Do not replace them with a portable four-character macro unless the macro is proven to produce the same integers under the target compiler/source convention.
- `SetFadeTransitionState` zero-extends a byte-sized source argument and stores the result as a dword transition state at `+0x5a4`.
- `IsFadeTransitionActive` returns whether the same dword transition state is nonzero.

## Field Map

| Offset | Best source-facing name | Evidence | Confidence |
| --- | --- | --- | --- |
| `+0xa4` | inherited `TimerHandler` facet owned by `Pane` | Queue helpers adjust the primary ScreenPane receiver by `+0xa4` before [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md); RTTI and ScreenPane layout prove this is an inherited Pane facet, not a derived embedded member. | Very high. |
| `+0x598` | `short m_fadeCurrentValue` | [UID:0003A0][0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers](by-memory/0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers.md) copies it into `+0x59a`; the constructor initializes it. | High; current class declaration and target data flow agree. |
| `+0x59a` | `short m_fadePreviousValue` | `OnTimer` and the retained begin methods copy `+0x598` into it before traversal. Historical `HandleMessage` is an evidence alias. | High; current class declaration and target data flow agree. |
| `+0x5a4` | `int m_fadeTransitionState` | This target clears, sets, and tests it; `OnTimer` and the retained begin methods use values `1`, `2`, and `3`; the constructor initializes it; Surface presentation reads it. | Very high role and strong source-facing name. |
| `+0x5a8` | `FunctionObject0 *m_fadeCompletionCallback` | `ScreenPane::OnTimer` stores the timer payload there after the queue helpers pass caller-created objects; UID0003A0 stores its sole typed `FunctionObject0 *completionCallback` argument directly. | Very high role and type. |
| `+0x5ac` | `int m_fadeStep` | The `FdIn`/fade-in setup stores black endpoint `0x18` / 24, the `FdOt`/fade-out setup stores visible endpoint `0`, and Surface presentation consumes it as an ordinal. | Very high role; exact endpoint semantics are proven. |

Best enum-like interpretation for `+0x5a4`:

| Value | Best interpretation |
| --- | --- |
| `0` | No fade/presentation transition active or transition queue reset. |
| `1` | Temporary preparing/traversal state before a fade branch commits to a direction. |
| `2` | `FdIn` active state after traversal setup. |
| `3` | `FdOt` active state after traversal setup. |

The accepted class/target source-facing names are `m_fadeCurrentValue`, `m_fadePreviousValue`, `m_fadeTransitionState`, `m_fadeCompletionCallback`, and `m_fadeStep`. Historical presentation-state, payload, delay, extent, and countdown alternatives remain provenance only and must not replace these current formal names without stronger original-source evidence.

## Queue Methods Versus Begin Methods

- `QueueFadeInCallback(LObject *callbackObject)` and `QueueFadeOutCallback(LObject *callbackObject)` are caller-facing timer queue methods. They clear `m_fadeTransitionState` and schedule exact `FdIn`/`FdOt` events through inherited `TimerHandler::ScheduleTimer`; they do not construct a Region, traverse the presentation list, commit states 2/3, set fade endpoints, or schedule a frame directly.
- Retained [UID:0003A0][0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers](by-memory/0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers.md) `BeginFadeIn(FunctionObject0 *completionCallback)` and `BeginFadeOut(FunctionObject0 *completionCallback)` are the transition-setup bodies. They snapshot the fade words, enter preparing state 1, use an automatic Region for traversal, store the typed callback at `+0x5a8`, commit state/step 2/24 or 3/0, and call `FrameHandler::ScheduleNextFrame()` through the direct base.
- `ScreenPane::OnTimer` bridges the two contracts: its exact live `FdIn`/`FdOt` branches consume the queued payload and inline the same setup semantics as the retained begin methods. Documentation preserves that inline binary control flow rather than rewriting it as calls.
- The queue methods retain their accepted `LObject *` ABI because their direct callers and TimerHandler payload route are broader callback-object evidence. UID0003A0's explicit stored member contract is the narrower current `FunctionObject0 *`; this distinction is intentional rather than unresolved type drift.
- Current zero-xref/pointer evidence for the begin-method starts is retained/unused liveness uncertainty only. It does not transfer ownership to this queue-helper page and does not make either method compiler glue.

## Caller, Reachability, And Data-Flow Evidence

| Address or item | Evidence | Meaning |
| --- | --- | --- |
| `0x005590e0` | Local export reports one caller at `0x004f4e87` in the LogoPane constructor path. | Startup logo construction queues a ScreenPane fade-in callback, not a LogoPane-owned helper. |
| `0x00559110` | Local export reports callers at `0x00464efa` in the Application request-exit path and `0x004f52c8` in the LogoPane advance path. | Exit and startup-logo advance both queue fade-out/deferred completion work through ScreenPane. |
| `0x00559140` | Local export reports zero callers and zero callees; existing docs record no direct caller xrefs. | Complete ScreenPane thiscall setter body retained with no-live-xref caveat. |
| `0x00559160` | No local export file was found for this raw predicate; existing docs record no direct caller xrefs. | Complete ScreenPane state predicate retained with no-live-xref caveat. |
| `0x005975e0` | TimerHandler wrapper forwards through `g_pTimerMgr` to the scheduler queue helper at `0x00597910`. | Queue helpers are ScreenPane users of generic TimerHandler scheduling; `0x005975e0` is not owned by ScreenPane, LogoPane, or FittingRoom. |
| `0x00556d50-0x00556f61` | `ScreenPane::OnTimer` branches on `0x4664496e` and `0x46644f74`, updates `+0x5a4`, `+0x5a8`, and `+0x5ac`, and inlines transition/traversal setup. Historical `HandleMessage` is an evidence alias. | Confirms `FdIn`/`FdOt` event semantics and bridges queue payloads to begin-method-equivalent setup. |
| `0x00559200` / `0x005592b0` | Retained `BeginFadeIn`/`BeginFadeOut` set the same `+0x5a4` field to `1`, then `2` or `3`, and set `m_fadeStep` to `0x18` or `0`. | Confirms enum-like state, exact endpoints, typed callback storage, and direct frame scheduling rather than queue semantics. |
| Surface presentation helpers | `0x00557140` and `0x005583a0` read `+0x5a4` and use `+0x5ac`. | Surface consumes ScreenPane transition state; it does not own this target. |

## Function-Level Evidence

### `0x005590e0` Queue Fade-In Callback

Local export and existing live-IDA-backed documentation support this source shape:

- IDA name: `sub_5590E0`.
- Size: 40 bytes.
- IDA signature shape: `int __thiscall(_DWORD *this, int)`.
- The body writes `this[361] = 0`, which is ScreenPane offset `+0x5a4`.
- The body calls `sub_5975E0(this + 41, 1180977518, 0, callback, 0)`.
- `this + 41` dword pointer arithmetic is ScreenPane offset `+0xa4`.
- `1180977518` is `0x4664496e`, the `FdIn` message branch handled by `ScreenPane::OnTimer`; `ScreenPane::HandleMessage` is retained only as its historical documentation name.

Best source-facing name: `ScreenPane::QueueFadeInCallback`. `ScheduleFadeInCallback` is acceptable, but `QueueFadeInCallback` mirrors the scheduler queue behavior while keeping the event meaning explicit.

Rejected alternatives:

- `sub_5590E0` / `FUN_005590e0`: generated labels, not source names.
- `TimerHandler::ScheduleTimer`: this helper calls the TimerHandler wrapper; it is not the timer wrapper itself.
- `LogoPaneReadyCallback`: LogoPane is the only direct caller, but the body uses ScreenPane state and the ScreenPane `FdIn` event.
- `SetFlagAndQueueCallback`: too generic; it loses the confirmed fade-in event semantics.

### `0x00559110` Queue Fade-Out Callback

Local export and existing live-IDA-backed documentation support this source shape:

- IDA name: `sub_559110`.
- Size: 40 bytes.
- IDA signature shape: `int __thiscall(_DWORD *this, int)`.
- The body writes `this[361] = 0`, the same ScreenPane offset `+0x5a4`.
- The body calls `sub_5975E0(this + 41, 1180979060, 0, callback, 0)`.
- `1180979060` is `0x46644f74`, the `FdOt` message branch handled by `ScreenPane::OnTimer`; `ScreenPane::HandleMessage` is retained only as its historical documentation name.
- `Application::RequestExit` uses this helper to defer final shutdown signaling through the root screen when a screen pane exists.
- `LogoPane::QueueAdvanceToNextScreen` uses this helper to transition out from the startup logo before invoking the next-screen callback.
- Both caller families create callback objects and pass them as the timer payload argument.

Best source-facing name: `ScreenPane::QueueFadeOutCallback`. `ScheduleFadeOutCallback` is acceptable, but `QueueFadeOutCallback` keeps the scheduler queue behavior visible.

Rejected alternatives:

- `sub_559110` / `FUN_00559110`: generated labels, not source names.
- `ApplicationExitCallback`: too narrow because LogoPane also uses the helper, and ScreenPane owns the state.
- `TimerHandler::ScheduleTimer`: dependency confusion; TimerHandler owns `0x005975e0`, not this ScreenPane wrapper.

### `0x00559140` Raw Transition-State Setter

Local export and existing target evidence support this source shape:

- Ghidra export models a 17-byte `__thiscall` helper at `0x00559140`.
- Signature shape: `void __thiscall FUN_00559140(void *this, byte state)`.
- The body zero-extends the byte argument and stores it as a dword at ScreenPane offset `+0x5a4`.
- Export reports zero callers and zero callees.
- The corrected instruction boundary is exact: `retn 4` consumes bytes through `0x00559150`, so padding begins at `0x00559151`.

Best source-facing name: `ScreenPane::SetFadeTransitionState`. `SetPresentationTransitionState` is acceptable if the broader ScreenPane transition pages standardize on presentation naming.

Type decision: the field is dword/enum-like, not a byte field. The setter takes a byte-sized argument but writes the full dword state. The method name should say "state" because visible values include `0`, `1`, `2`, and `3`.

Liveness caveat: no direct caller is currently documented. The helper is still source-shaped: it is a complete thiscall setter on a ScreenPane field with exact padding on both sides. Keep it as retained source unless a future pointer/reference scan proves it is unreachable compiler debris.

### `0x00559160` Raw Transition-State Predicate

Existing target and support evidence support this source shape:

- The raw body compares the dword at `this+0x5a4` against zero, sets `al` if nonzero, and returns.
- No direct caller xrefs are documented.
- The exact end is `0x0055916b`, followed by ignored `0x0055916b-0x00559170` padding before [UID:00039Z][0x00559170-0x005591fc.SurfaceRestoreLostSurfaces](by-memory/0x00559170-0x005591fc.SurfaceRestoreLostSurfaces.md).

Best source-facing name: `ScreenPane::IsFadeTransitionActive`. `IsPresentationTransitionActive` is acceptable if the broader ScreenPane transition pages standardize on presentation naming.

Rejected alternatives:

- `GetFlag`: loses transition state semantics.
- `HasCallback`: wrong field; callback payload is stored separately at `+0x5a8`.
- Surface or cursor predicate names: rejected because the field semantics trace through the ScreenPane constructor, `ScreenPane::OnTimer` (historically documented as `ScreenPane::HandleMessage`), and ScreenPane transition wrappers.

## Heuristic / Inference Reanalysis And Validation

### Source-Bearing Versus No-Code

Best conclusion: [UID:00039Y] is source-authored ScreenPane helper code and should not remain an empty emitter.

Evidence:

- The two modeled functions are normal thiscall methods over a ScreenPane object and call generic TimerHandler scheduling with ScreenPane fade event IDs.
- The two raw helpers are tiny method-shaped bodies over the same ScreenPane transition-state field.
- The field at `+0x5a4` is initialized and used by documented ScreenPane transition code.
- Padding boundaries are known; none of the executable subranges is alignment.
- The previous generated output had an empty emitter marker, but there was no target-specific no-code proof.

Rejected no-code/compiler-output alternatives:

- Compiler-generated thunk: rejected. None of the four helpers is an adjustor thunk or tail jump.
- TimerMgr wrapper: rejected. `0x005975e0` is the TimerHandler/TimerMgr wrapper; this target only calls it.
- Dead padding: rejected. The raw setter and predicate decode to complete methods between `0xcc` boundaries.
- Callback object generated code: rejected. Callback object allocation and vtable writes occur in LogoPane/Application callers, not in this target.

### Callback ID Naming

Best source-facing constants:

```cpp
const int kScreenPaneFadeInEvent = 0x4664496e;  // FdIn
const int kScreenPaneFadeOutEvent = 0x46644f74; // FdOt
```

Evidence:

- `0x005590e0` schedules `0x4664496e`.
- `0x00559110` schedules `0x46644f74`.
- `ScreenPane::OnTimer` has dedicated branches for exactly those two values; `ScreenPane::HandleMessage` is the superseded historical name.
- Existing ScreenPane docs decode them as `FdIn` and `FdOt`.
- The `FdIn` branch sets transition state `2` and `m_fadeStep` to the black endpoint `0x18` / 24; the `FdOt` branch sets transition state `3` and `m_fadeStep` to the visible endpoint `0`.

Rejected alternatives:

- Treating the event IDs as arbitrary magic integers is no longer justified.
- Naming them as LogoPane events is too narrow because Application exit also uses `FdOt`, and the handler lives on ScreenPane.
- Using Win32 `MAKEFOURCC` or a portable four-character macro is not accepted unless proven to produce these exact integers.

### Placeholder Cleanup

| Placeholder | Replacement | Reason |
| --- | --- | --- |
| `sub_5590E0` / `FUN_005590e0` | `ScreenPane::QueueFadeInCallback` | Schedules `FdIn` through the ScreenPane timer-handler facet. |
| `sub_559110` / `FUN_00559110` | `ScreenPane::QueueFadeOutCallback` | Schedules `FdOt` through the ScreenPane timer-handler facet. |
| `FUN_00559140` | `ScreenPane::SetFadeTransitionState` | Sets ScreenPane `+0x5a4` state. |
| raw `0x00559160` | `ScreenPane::IsFadeTransitionActive` | Tests ScreenPane `+0x5a4` state. |
| `this+0x5a4` | `m_fadeTransitionState` / `m_presentationTransitionState` | Enum-like transition state, not a generic flag. |
| `this+0xa4` | inherited `TimerHandler::ScheduleTimer` access | Pane-owned TimerHandler facet; no duplicate `m_timerHandler` member in ScreenPane. |
| `FittingRoomDialog::meth_0x5975e0` | `TimerHandler::ScheduleTimer` | Ghidra/OOAnalyzer owner pollution; TimerHandler docs prove generic ownership. |

## Ownership Analysis

| Candidate | Evidence | Decision |
| --- | --- | --- |
| [UID:0000CB][ScreenPane](by-class/ScreenPane.md) ScreenPane class | Every executable helper uses a ScreenPane `this`; fields are initialized and used by the ScreenPane constructor/`OnTimer` (historically documented as `HandleMessage`); event IDs are handled by ScreenPane; class parent clears the gate and emits to ScreenPane.cpp. | Accepted as direct semantic owner. |
| [UID:0000NB][ScreenPane](by-file/ScreenPane.md) ScreenPane file | Owns `NexusTK/ui/core/ScreenPane.cpp`; previous route already emitted there; can own file-local helpers. | Keep as source file route, not direct canonical owner. |
| [UID:0000OC][Surface](by-file/Surface.md) Surface | Target sits inside the broad SurfacePresentation neighborhood and Surface presentation functions consume some transition state. | Rejected as owner: Surface consumes ScreenPane fields but does not own this state machine. |
| [UID:0000OT][TimerMgr](by-file/TimerMgr.md) / [UID:0000F0][TimerHandler](by-class/TimerHandler.md) | Queue helpers call `TimerHandler::ScheduleTimer`. | Rejected as direct owner: TimerHandler is a dependency; this target selects ScreenPane event IDs and resets ScreenPane state. |
| LogoPane and Application caller classes | Direct callers create callback objects and pass them to these helpers. | Rejected as owners: caller evidence is consumer evidence; neither owns the ScreenPane timer facet or transition state. |
| [UID:00003E][CursorManager](by-class/CursorManager.md) | Nearby cursor helper island has a provisional cursor-manager typed view. | Rejected: no cursor state is touched here. |

## First-Draft C++ Rationale

This target is eligible for first-draft C++:

- Current score is `88/90`, average `89.0`.
- `RECONSTRUCTABLE:TRUE`.
- `EMITTER_UIDS:0000CB` routes through [UID:0000CB][ScreenPane](by-class/ScreenPane.md) to [UID:0000NB][ScreenPane](by-file/ScreenPane.md), `NexusTK/ui/core/ScreenPane.cpp`.
- Every executable body in the target has a source-shaped role and exact boundary.

The emitted code intentionally includes the raw setter and predicate. They are retained/no-direct-xref helpers, but their bodies are complete ScreenPane methods, their field is documented, and no compiler-generated or padding explanation fits the bytes. If a later policy requires no-caller raw helpers to remain documentation-only, split or blank those two bodies then; the current implementation keeps the accepted report's cluster-level first draft.

Implementation notes:

- The prior `m_timerHandler.ScheduleTimer(...)` draft was source-shape pollution: it invented an embedded member and would shift the complete ScreenPane layout. The exact first draft calls the inherited base method explicitly as `TimerHandler::ScheduleTimer(...)`.
- If the local TimerHandler wrapper requires integral payloads, use `reinterpret_cast<int>(callbackObject)` for the third payload argument during a later compile-pass. The source intent remains "pass callback object as event payload."
- Keep `m_fadeTransitionState` consistent with the accepted ScreenPane class and UID0003A0 formal source. Historical `m_presentationTransitionState` remains provenance rather than an open current naming choice.
- Keep the queue-method `LObject *` parameter and UID0003A0 begin-method `FunctionObject0 *` parameter distinct unless future caller/type evidence proves a safe family-wide signature change. Do not collapse either to `void *`.

## Score Rationale

`COMPLETION` is raised to `88` because source-facing method names, callback ID meanings, field role, class ownership, caller route, split decision, and first-draft C++ shape are now documented and implemented.

`CONFIDENCE` is raised to `90` because local exports for `0x005590e0`, `0x00559110`, and `0x00559140` agree with existing live-IDA-backed ScreenPane, LogoPane, Application, TimerHandler, and SurfacePresentation documentation. Confidence stays at `90`, not higher, because the raw setter/predicate still lack direct xrefs and final original header/member spelling is not recovered.

## Cross-References

- [UID:0000CB][ScreenPane](by-class/ScreenPane.md)
- [UID:0000NB][ScreenPane](by-file/ScreenPane.md)
- [UID:0001G5][0x00556910-0x00557132.ScreenPane](by-memory/0x00556910-0x00557132.ScreenPane.md)
- [UID:0001G6][0x00557140-0x00559aef.SurfacePresentation](by-memory/0x00557140-0x00559aef.SurfacePresentation.md)
- [UID:0003A0][0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers](by-memory/0x00559200-0x00559359.ScreenPanePresentationTransitionHelpers.md)
- [UID:0001K8][0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers](by-memory/0x005975e0-0x0059760d.TimerHandlerScheduleRemoveWrappers.md)
- [UID:0000F0][TimerHandler](by-class/TimerHandler.md)
- [UID:0000OT][TimerMgr](by-file/TimerMgr.md)
- [UID:0000OC][Surface](by-file/Surface.md)

## Changes

- 2026-08-11 B001 UID0003A0 support implementation:
  - Preserved target score `88/90`, owner/emitter metadata, all four callback/state method bodies, exact padding, queue callers, and raw-helper liveness caveats.
  - Added the exact queue-versus-begin contract, `OnTimer` inline bridge, FunctionObject0 callback field/argument relation, automatic Region and direct FrameHandler setup behavior, 2/24 versus 3/0 endpoint semantics, and retained begin-method zero-xref caveat.
  - Replaced unresolved current field/type/countdown language with accepted class members while retaining older alternatives as historical provenance.

- 2026-07-22 B003 UID00023U hierarchy normalization:
  - Preserved the `88/90` metadata, owner/emitter/range/Nested tuple, all four methods, event IDs, callers, raw-helper liveness caveats, exact padding, history, and behavior.
  - Replaced invented embedded `m_timerHandler.ScheduleTimer` access with inherited `TimerHandler::ScheduleTimer`, backed by ScreenPane RTTI, Pane facet `+0xa4`, and the complete `0x5b4` class layout.
  - Recorded the old embedded-member draft as rejected source-shape pollution rather than silently discarding its history.

- 2026-06-19 B005 accepted source-quality implementation:
  - Before: `85/87`, direct owner/emitter [UID:0000NB][ScreenPane](by-file/ScreenPane.md), file-level owner wording, generated labels for the queue helpers, generic `this+0x5a4` flag wording, and an empty C++ emitter.
  - After: `88/90`, direct owner/emitter [UID:0000CB][ScreenPane](by-class/ScreenPane.md), source route through [UID:0000NB][ScreenPane](by-file/ScreenPane.md), exact range/padding table, `QueueFadeInCallback`/`QueueFadeOutCallback` names for `FdIn`/`FdOt`, `SetFadeTransitionState`/`IsFadeTransitionActive` retained raw-helper names, field map for `+0xa4`, `+0x598`, `+0x59a`, `+0x5a4`, `+0x5a8`, and `+0x5ac`, caller/liveness evidence, rejected alternatives, and first-draft C++.
  - Evidence: accepted B005 report `tools/leaser/Agents/Agent-B005/research/00039Y-ScreenPaneCallbackAndFlagHelpers-source-quality.md`, local export facts for `0x005590e0`, `0x00559110`, and `0x00559140`, existing live-IDA-backed ScreenPane/SurfacePresentation/TimerHandler docs, and corrected padding evidence around `0x00559151-0x00559160`.
- 2026-06-11 B001-049 split:
  - Created as a ScreenPane callback/flag child and repaired the stale padding boundary around `0x00559140`.
