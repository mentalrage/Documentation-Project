*** UID:000475 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserListDialogPane::RefreshCategoryView(char step)
{
    m_categoryPage += step;
    if (m_categoryPage > 5)
        m_categoryPage = 5;
    else if (m_categoryPage < 0)
        m_categoryPage = 0;

    switch (m_categoryPage)
    {
    case 0:
        m_selectedNationId = g_activeUserStatusPane->GetNationId();
        break;
    case 1:
        m_selectedNationId = static_cast<short>(
            g_gameServerNationTable->GetNationEntryCount() - 1);
        break;
    case 2:
        m_selectedNationId = 1;
        break;
    case 3:
        m_selectedNationId = 2;
        break;
    case 4:
        m_selectedNationId = 3;
        break;
    case 5:
        m_selectedNationId = 0;
        break;
    }

    for (int index = 0; index < 5; ++index)
    {
        UserListPane *pane = m_bucketPanes[index];
        pane->RemoveItems(0, pane->GetRecordCount());
    }

    const GameServerConfig::NationEntry *nationEntry =
        g_gameServerNationTable->GetNationEntryAtOrDefault(m_selectedNationId);
    const unsigned short selectedNationId = nationEntry->nationId;

    for (int index = 0; index < m_allUserRecords->GetCount(); ++index)
    {
        UserListRecord *record = static_cast<UserListRecord *>(
            m_allUserRecords->GetElementAt(index));

        if ((!g_bShowHuntersListOnly || record->huntersListFlag != 0) &&
            ((selectedNationId == 10 &&
              record->careerVariant == m_selectedCareerVariant) ||
             record->careerId == selectedNationId))
        {
            UserListPane *pane = record->bucketIndex != 0
                ? m_bucketPanes[record->bucketIndex - 1]
                : m_bucketPanes[4];
            pane->AddEntry(record);
        }
    }

    const int selectedSort =
        GetChild<RadioGroupControlPane>(3)->GetSelectedIndex();
    if (selectedSort == 0)
    {
        for (int index = 0; index < 5; ++index)
        {
            UserListPane *pane = m_bucketPanes[index];
            pane->SortRecords(CompareUserListRecordsByStatus);
            pane->Invalidate();
        }
    }
    else if (selectedSort == 1)
    {
        for (int index = 0; index < 5; ++index)
        {
            UserListPane *pane = m_bucketPanes[index];
            pane->SortRecords(CompareUserListRecordsByName);
            pane->Invalidate();
        }
    }

    wchar_t countText[40];
    int visibleCount = 0;
    for (int index = 0; index < 5; ++index)
    {
        TextEditControlPane *countControl =
            GetChild<TextEditControlPane>(index + 9);
        visibleCount += m_bucketPanes[index]->GetRecordCount();
        swprintf_s(countText, 40, L"%d",
                   m_bucketPanes[index]->GetRecordCount());
        countControl->SetText(countText);
    }

    swprintf_s(countText, 40, L"%d", static_cast<short>(visibleCount));
    GetChild<TextEditControlPane>(15)->SetText(countText);

    nationEntry =
        g_gameServerNationTable->GetNationEntryAtOrDefault(m_selectedNationId);
    if (g_useEpfAssets == 1)
    {
        swprintf_s(countText, 40, nationEntry->label);
        GetChild<TextEditControlPane>(2)->SetText(countText);
        GetChild<TextEditControlPane>(18)->SetText(countText);
    }
    else
    {
        GetChild<TextEditControlPane>(2)->SetText(nationEntry->label);
    }

    Invalidate();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-authored UserListDialogPane::RefreshCategoryView(char step) at [0x0059d2b0,0x0059d5fe): clamps six category pages, resolves the active nation/GameServerConfig category entry, clears and rebuilds five UserListPane buckets from packed UserListRecord storage with hunters/career filters, sorts by the selected radio mode, updates count/category labels with exact EPF/legacy behavior, and invalidates the dialog. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0059d2b0-0x0059d5fe - UserListDialogPaneRefreshCategoryView

## Status

- Entity kind: source-authored `UserListDialogPane` method.
- Address range: `0x0059d2b0-0x0059d5fe`.
- Owner: [UID:0000FN][UserListDialogPane](by-class/UserListDialogPane.md) `UserListDialogPane`.
- Source placement and emitter: [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md), `NexusTK/social/UserListDialogPane.cpp`.
- Rebuild handling: source-authored method body with source signature `void UserListDialogPane::RefreshCategoryView(char step)`.
- Exact binary range: `[0x0059d2b0,0x0059d5fe)`, 846 bytes, SHA256 `E78900F97C5F1C456DFFC103BF53474BC057C4E250A73A378B45F341E18E7217`.
- CFG: 278 instructions, 53 basic blocks, cyclomatic complexity 30.
- Source order: after [UID:000474][0x0059d250-0x0059d2af.UserListDialogPaneSortListsByName](by-memory/0x0059d250-0x0059d2af.UserListDialogPaneSortListsByName.md) and before [UID:000477][0x0059d620-0x0059d8f5.UserListDialogPaneHandlePacketEvent](by-memory/0x0059d620-0x0059d8f5.UserListDialogPaneHandlePacketEvent.md). The optional emitter position remains blank to preserve first-seen/address order.

## Behavior

- The signed plain-`char` `step` is added to 16-bit `m_categoryPage`. Values above 5 clamp to 5 and values below 0 clamp to 0.
- The six page values map to nation-table entry indices exactly:
  - page 0 uses `g_activeUserStatusPane->GetNationId()`;
  - page 1 uses `g_gameServerNationTable->GetNationEntryCount() - 1`;
  - pages 2, 3, 4, and 5 store entry indices 1, 2, 3, and 0.
- In this path, the established field name `m_selectedNationId` stores a nation-table entry index. The selected comparison id is then loaded separately from `GameServerConfig::NationEntry::nationId`.
- All five `m_bucketPanes` are cleared through `RemoveItems(0, GetRecordCount())`, including panes whose current count is zero.
- The method walks every packed `UserListRecord` in `m_allUserRecords` through generic `List` count/index access.
- When `g_bShowHuntersListOnly` is set, records with a zero `huntersListFlag` are excluded.
- Selected nation id 10 uses the special `careerVariant == m_selectedCareerVariant` path. The ordinary path compares `careerId` to the selected nation id.
- `bucketIndex == 0` routes to pane 4; nonzero bucket `n` routes to pane `n - 1`. The binary performs no bucket clamp, and the source preserves that unchecked behavior.
- Sort control child 3 selects the post-filter sort:
  - selected index 0 sorts each pane with `CompareUserListRecordsByStatus`;
  - selected index 1 sorts each pane with `CompareUserListRecordsByName`;
  - other values leave insertion order unchanged.
- The sort loops are independent in this function rather than calls to the adjacent `SortListsByStatus` or `SortListsByName` methods. Each sorted pane is invalidated.
- Count controls 9 through 13 receive the five pane counts. Each loop iteration deliberately reads the pane count twice, once for the total and once for formatting.
- Count control 15 receives the total after signed-short conversion.
- The nation entry is resolved a second time before label display:
  - EPF mode passes `nationEntry->label` itself as the `swprintf_s` format string and updates children 2 and 18;
  - legacy mode writes the label directly to child 2 and does not touch child 18.
- The method ends by invalidating the dialog.

## ABI And Callers

- The callee sign-extends one stack byte at `0x0059d2c0`; this rejects the stale `int` declaration and supports plain signed MSVC `char`.
- The source return type is `void`. The final invalidation call leaves an EAX byproduct, but no caller consumes a return value.
- The four direct call xrefs are:
  - `0x0059d8cb`: [UID:000477][0x0059d620-0x0059d8f5.UserListDialogPaneHandlePacketEvent](by-memory/0x0059d620-0x0059d8f5.UserListDialogPaneHandlePacketEvent.md), opcode `0x83`, passes zero;
  - `0x0059d968`: [UID:000478][0x0059d900-0x0059da82.UserListDialogPaneHandleKeyOrTextEvent](by-memory/0x0059d900-0x0059da82.UserListDialogPaneHandleKeyOrTextEvent.md), passes `-1`;
  - `0x0059d9a6`: UID000478, passes `1`;
  - `0x0059dbcc`: [UID:000479][0x0059da90-0x0059dbe3.UserListDialogPaneOnControlCommand](by-memory/0x0059da90-0x0059dbe3.UserListDialogPaneOnControlCommand.md), shared control-command call reached with `1`, `-1`, or `0`.
- Opcode `0x84` in UID000477 updates source text but does not call this method.

## Evidence

### Binary And Boundary Evidence

- Live IDA MCP modeled one function from `0x0059d2b0` through exclusive end `0x0059d5fe`; no internal second function or target-end function exists.
- [UID:000474][0x0059d250-0x0059d2af.UserListDialogPaneSortListsByName](by-memory/0x0059d250-0x0059d2af.UserListDialogPaneSortListsByName.md) is the exact predecessor.
- [UID:000476][0x0059d5fe-0x0059d620.UserListDialogPaneRefreshCategorySwitchTable](by-memory/0x0059d5fe-0x0059d620.UserListDialogPaneRefreshCategorySwitchTable.md) starts at the target end with alignment and the six-entry compiler switch table at `0x0059d600`. The table is referenced from `0x0059d315` and remains non-emitting compiler data.
- Direct callee evidence resolves the pane count/remove/add operations, `List::Sort`, child bounds/lookups, `TextEditControlPane::SetText`, `swprintf_s`, and the final invalidation route.

### Field, Type, And Global Evidence

- `m_categoryPage` is the signed 16-bit field at `+0x26e`.
- The existing `m_selectedNationId` field at `+0x26c` is an entry index in this method; the selected `unsigned short nationId` comes from the resolved entry.
- `m_selectedCareerVariant` is the signed 16-bit filter field at `+0x270`.
- `m_bucketPanes[5]` occupy the exact class tail at `+0x274..+0x284`; `m_allUserRecords` is the generic `List *` at `+0x288`.
- `UserListRecord` is the packed 0x414-byte row already declared on [UID:0000FN][UserListDialogPane](by-class/UserListDialogPane.md).
- `GameServerConfig::NationEntry` is a 68-byte record containing `unsigned short nationId` and `wchar_t label[33]`; invalid indices use `GetNationEntryAtOrDefault`.
- Exact global dependencies are `g_gameServerNationTable`, `g_activeUserStatusPane`, `g_bShowHuntersListOnly`, and `g_useEpfAssets`.
- The count format is exact UTF-16 `L"%d"`; the local buffer is `wchar_t countText[40]`.

### Source And Ownership Evidence

- All callers are `UserListDialogPane` packet, key, or control methods and the receiver fields match the complete [UID:0000FN][UserListDialogPane](by-class/UserListDialogPane.md) class.
- Adjacent source-authored children and file-local helpers already emit through [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md), so UID0000OZ is the exact source emitter while UID0000FN remains the semantic owner.
- Reusable `List`, `ListPane`, `UserListPane`, `GameServerConfig`, `RadioGroupControlPane`, `TextEditControlPane`, and `UserStatusPane` APIs are dependencies, not alternative owners.

## Negative And Rejected Evidence

- Rejected `int step`: the function consumes one signed byte.
- Rejected unsigned `char`: the target uses signed promotion and supports `-1`.
- Rejected source return value: callers ignore EAX.
- Rejected `g_pOldUserStatusPane`: the exact global is `g_activeUserStatusPane`.
- Rejected `L"%s"` cleanup: the EPF branch uses the label as the format string.
- Rejected bucket validation: no clamp or bounds branch exists.
- Rejected calls to UID000473/UID000474: this function contains its own five-pane sort loops.
- Rejected opcode `0x84` refresh: only opcode `0x83` reaches the target.
- Rejected split/new child/new source file: one live source function and one established file route cover the exact range.
- Rejected handwritten switch table, vptr, cookie, or ABI-wrapper source: those are compiler-generated details.

## Historical Superseded No-Code Rationale

The pre-callback page kept formal C++ blank because category tables, label controls, list insertion, and row iteration APIs were considered undeclared. That was a reasonable historical caution, but it is no longer current: accepted support pages now define the exact `GameServerConfig::NationEntry`, List/ListPane/UserListPane, radio-group, text-edit, child-lookup, invalidation, active-nation, global-filter, and packed-record contracts used by the method. The complete managed body above supersedes the old non-emitting disposition without deleting its historical reason.

## Score Rationale

- Completion `93`: exact range, bytes/hash, CFG, callers, callees, fields, globals, boundaries, complete branch/loop behavior, source body, owner, emitter, and support routes are documented.
- Confidence `94`: independent binary, caller, class, file, and support evidence converge on the implementation. Exact original private/local spellings, enum labels, and formatting style remain the bounded cap below 95.

## Related Docs

- [UID:0001KI][0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane](by-memory/0x0059bc90-0x0059f25b.UserListDialogPaneAndUserListPane.md) Parent split/index page.
- [UID:0000FN][UserListDialogPane](by-class/UserListDialogPane.md) Class owner.
- [UID:0000OZ][UserListDialogPane](by-file/UserListDialogPane.md) Source emitter.
- [UID:00005O][GameServerConfig](by-class/GameServerConfig.md) Nation-entry contract.
- [UID:0000FS][UserStatusPane](by-class/UserStatusPane.md) Active nation getter.
- [UID:0002ZX][g_bShowHuntersListOnly](by-global/g_bShowHuntersListOnly.md) `g_bShowHuntersListOnly`.

## Changes

- 2026-07-16 B004 UID000475 implementation callback:
  - Raised `86/90 -> 93/94`, retained owner UID0000FN, reconstructable true, blank optional position, and `Nested:0`, and added emitter UID0000OZ.
  - Installed the accepted complete `void UserListDialogPane::RefreshCategoryView(char step)` body.
  - Added exact byte/hash/CFG/caller/callee/global/boundary, six-page mapping, two-entry-lookup, five-pane clear/rebuild, filter, unchecked bucket, independent sort, duplicate count-read, signed-total, EPF/legacy label, final invalidation, ownership, negative, rejected-alternative, and score evidence.
  - Preserved the former blank-emitter/no-code rationale as explicitly superseded history.
