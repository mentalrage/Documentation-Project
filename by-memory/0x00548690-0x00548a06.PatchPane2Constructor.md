*** UID:0004VS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
PatchPane2::PatchPane2(const unsigned char *patchInfo)
    : DialogPane(L"", static_cast<unsigned short>(-1), 1),
      Singleton<PatchPane2>(),
      m_currentDownloadIndex(0),
      m_activePatchFile(0),
      m_downloadPassCount(0),
      m_patchVersionMajor(patchInfo[0] | (patchInfo[1] << 8)),
      m_patchVersionMinor(patchInfo[2] | (patchInfo[3] << 8))
{
    const int width = 200;
    const int height = 100;
    const int left = (g_pScreenPane->GetScreenWidth() - width) / 2;
    const int top = (g_pScreenPane->GetScreenHeight() - height) / 2;

    RectBounds dialogBounds;
    InitRectBounds(&dialogBounds, left, top, left + width, top + height);

    RectBounds controlBounds;
    InitRectBounds(&controlBounds,
                   width / 4,
                   10,
                   width * 3 / 4,
                   height / 4);
    AddControl(new StaticTextControlPane(
        L"Ready", false, 128, 143, &controlBounds, false, false));

    InitRectBounds(&controlBounds,
                   10,
                   height / 4 + 10,
                   width - 10,
                   height * 3 / 4 - 15);
    AddControl(new StaticTextControlPane(
        L"0", false, 128, 143, &controlBounds, false, false));

    InitRectBounds(&controlBounds,
                   width / 4,
                   height * 3 / 4 - 5,
                   width * 3 / 4,
                   height - 10);
    AddControl(new TextButtonControlPane(L"Cancel", &controlBounds));

    OnCreate(&dialogBounds, 0, 0, g_pStatusPaneLayer);
    OnShow(0, 0);

    const unsigned int fileCount = patchInfo[4];
    for (unsigned int index = 0; index < fileCount; ++index) {
        const char *fileName =
            reinterpret_cast<const char *>(patchInfo + 5 + index * 256);
        m_patchFileNames.push_back(StringBase<wchar_t>(fileName));
    }

    m_finalPatchPath = L"Patcher.exe";
    StringBase<char> oldPath(m_finalPatchPath);
    _unlink(oldPath.c_str());
    m_tempPatchPath = L"Patcher.exe.new";
    SendDownloadRequest();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready PatchPane2 constructor with exact 0x376-byte range/hash, sole MainMenu caller, packed version/count/256-byte filename input, centered 200x100 Ready/count/Cancel dialog, status-layer creation, Patcher path setup, vector population, first HTTP request, and compiler-only Singleton/vptr/EH lowering excluded. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00548690-0x00548a06 PatchPane2 Constructor

## Status

- Disposition: source-bearing PatchPane2 constructor with complete human-authored first draft.
- Owner/emitter: [UID:0000AA][PatchPane2](by-class/PatchPane2.md), routed through [UID:0000MH][PatchPane](by-file/PatchPane.md).
- Exact range: `[0x00548690,0x00548a06)`, `0x376` bytes.
- Exact SHA256: `69D138CF1F820ADFE4178EC415CBAF36A50CD9F71E1AF7E6C4EF5334A7CF7C41`.

## Caller And Input Contract

- The sole code caller is `0x004f7432` inside the MainMenu patch-decision function at `0x004f6d80`.
- The caller builds a packet-derived local buffer: little-endian words at `+0/+2`, file count byte at `+4`, then `count` fixed 256-byte ANSI filename rows at `+5`.
- `const unsigned char *patchInfo` preserves that observed packed contract without inventing a global wire structure or decompiler type.
- The two words are promoted to `unsigned int` fields at object `+0x28c/+0x290`; the file count drives vector insertion and each row is converted to `StringBase<wchar_t>`.

## Construction Behavior

- Constructs `DialogPane` with empty title, command/id `0xffff`, and flag `1`; direct `Singleton<PatchPane2>` inheritance accounts for global publication and unwind cleanup.
- Initializes current file index, active `FILE *`, and retry/pass count to zero; version fields are initialized from the two packed words.
- Centers a `200x100` dialog using `g_pScreenPane->GetScreenWidth()` and `GetScreenHeight()`.
- Adds two `StaticTextControlPane` children (`L"Ready"`, `L"0"`) and one `TextButtonControlPane` (`L"Cancel"`) using exact rectangle arithmetic and observed color/flag arguments.
- Creates/shows the dialog on `g_pStatusPaneLayer`, parses all filenames, sets final/temp paths to `L"Patcher.exe"` and `L"Patcher.exe.new"`, removes the old final narrow path, and sends the first request.

## Field And Layout Evidence

- `+0x26c/+0x270`: final and temporary `StringBase<wchar_t>` paths.
- `+0x274/+0x278/+0x27c`: begin/end/capacity for `std::vector<StringBase<wchar_t> >`.
- `+0x280`: current download index; `+0x284`: active `FILE *`; `+0x288`: pass count.
- `+0x28c/+0x290`: promoted major/minor request values. Exact class size is `0x294`.
- Dialog child storage is inherited through DialogPane; no PatchPane2 `m_statusControls` field is introduced.

## Compiler And Negative Evidence

- Raw primary/secondary/tertiary vtable stores, explicit `g_pPatchPane2` publication, EH state writes, unwind calls, and constructor guard mechanics are compiler/Singleton lowering and are intentionally absent from source.
- No companion `PatchPane2.cpp`, heap-owned input buffer, defensive null check, filename bounds clamp, or additional UI child is supported by the binary.
- Historical descriptions that treated the global write as a handwritten assignment or the class tail as unresolved are superseded; the lifecycle and complete tail layout are now closed.

## Boundary Evidence

- Function lookup reports `sub_548690`, size `0x376`; the exact range hash above was computed from the executable.
- `[0x00548a06,0x00548a10)` is alignment before the raw ordinary destructor at `0x00548a10`.
- The following destructor has no IDA function object but is independently source-bearing; it is not part of this constructor.

## Cross-References

- [UID:0000AA][PatchPane2](by-class/PatchPane2.md)
- [UID:0000RZ][g_pPatchPane2](by-global/g_pPatchPane2.md)
- [UID:0001VJ][PatchPane2Layout](by-type/by-struct/PatchPane2Layout.md)
- [UID:0001EQ][0x005470b0-0x0054940f.PatchPaneAndPatchPane2](by-memory/0x005470b0-0x0054940f.PatchPaneAndPatchPane2.md)
- [UID:0001EY][0x00549220-0x005492cf.PatchPane2SendDownloadRequest](by-memory/0x00549220-0x005492cf.PatchPane2SendDownloadRequest.md)

## Changes

- Created by the accepted UID0002ZP source-quality callback from fresh MCP, PE, caller, literal, layout, and source-family evidence.
- Completion/confidence are `92/94`; uncertainty is limited to original private lexical spellings, not behavior, ABI, ownership, range, or source placement.
