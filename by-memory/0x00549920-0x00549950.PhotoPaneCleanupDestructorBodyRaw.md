*** UID:0002R8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Compiler-emitted PhotoPane destructor cleanup/unwind body.
// Source-level cleanup is generated from the implicit PhotoPane destructor,
// the embedded GrafPort m_snapshotPort member, and the PictureViewPane base
// destructor; no standalone handwritten helper should be emitted for this
// raw non-function range.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 48-byte implicit PhotoPane destructor lowering with protected 15-byte CC fence, three PhotoPane vptr stores, m_snapshotPort cleanup at +0xf8, PictureViewPane destructor tail-call, zero inbound xrefs, bounded IDA function-model handoff, and comment-only source emission from the implicit destructor cause. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***



# 0x00549920-0x00549950 PhotoPane Cleanup Destructor Body Raw

## Status

- Address range: `0x00549920-0x00549950`
- Entity kind: raw non-IDA-modeled destructor cleanup/unwind body
- Owner: [UID:0000AG][PhotoPane](by-class/PhotoPane.md)
- Likely source file: [UID:0000MK][PhotoPane](by-file/PhotoPane.md)
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-declared/generated-binary destructor cleanup/unwind support generated from the implicit `PhotoPane` destructor, embedded member, and virtual base layout, not a standalone source-authored helper or explicit PhotoPane destructor definition.
- Assignment: attached to direct class parent [UID:0000AG][PhotoPane](by-class/PhotoPane.md). The child is `92/94`, the class parent is `94/95`, and the class's file parent [UID:0000MK][PhotoPane](by-file/PhotoPane.md) is `94/95`.

## Behavior

This `0x30`-byte body (48 bytes; Verified with `int_convert.py`) is a compiler-emitted `PhotoPane` destructor-family cleanup/unwind path that IDA does not model as a function start. It:

- preserves `esi` and treats `ecx` as the `PhotoPane` object;
- installs the three `PhotoPane` vtable pointers at `this + 0x0`, `this + 0xa0` (160; Verified with `int_convert.py`), and `this + 0xa4` (164; Verified with `int_convert.py`);
- destroys or releases the embedded `GrafPort`/`m_snapshotPort` subobject at `this + 0xf8` (248; Verified with `int_convert.py`) via `sub_4B8D20`;
- restores `ecx` to the containing `PhotoPane` object and tail-jumps to `0x00549ae0`, the base teardown transition that resets the object to `PictureViewPane` vtables and chains to pane teardown.

This is separate from the scalar deleting destructor at `0x005499c0-0x00549a22`, which performs the same embedded `GrafPort`/`m_snapshotPort` teardown through a normal IDA-modeled function and conditionally frees object storage.

## Raw IDA Evidence

Current IDA MCP session `80de0a67` is live on `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. `server_health` reported status `ok`, module `NexusTK.exe`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and a ready strings cache. The current pass supersedes older `a002_batch346`/stale generated-output assumptions as active evidence; the older no-function/no-xref findings remain useful confidence caps.

| Check | Result |
| --- | --- |
| `lookup_funcs 0x00549920` | Not a function |
| `lookup_funcs 0x0054994b` | Not a function |
| `lookup_funcs 0x00549950` | `sub_549950`, size `0x52` (82 bytes; Verified with `int_convert.py`) |
| `lookup_funcs 0x005499c0` | `sub_5499C0`, size `0x62` (98 bytes; Verified with `int_convert.py`) |
| `lookup_funcs 0x00549ae0` | `sub_549AE0`, size `0x1f` (31 bytes; Verified with `int_convert.py`) |
| `lookup_funcs 0x004b8d20` | `sub_4B8D20`, size `0xa3` (163 bytes; Verified with `int_convert.py`); `GrafPort` cleanup/destructor body |
| `lookup_funcs 0x00549620` | `sub_549620`, size `0x2f1` (753 bytes; Verified with `int_convert.py`); `PhotoPane` constructor |
| `xrefs_to 0x00549920` | none |
| `xrefs_to 0x0054994b` | self tail-jump from `0x0054994a` |
| `xrefs_to 0x00549ae0` | raw tail jump at `0x0054994b`, scalar deleting destructor call at `0x005499ed`, and one constructor cleanup jump at `0x006060c3` |
| `xrefs_to 0x004b8d20` | fourteen total, including raw cleanup call `0x00549943`, scalar deleting destructor call `0x005499e6`, and constructor EH cleanup jump `0x006060d1` |
| `xrefs_to 0x00622034` | constructor store `0x0054966f`, raw-body store `0x00549929`, scalar destructor store `0x005499cc` |
| `xrefs_to 0x00622080` | constructor store `0x00549675`, raw-body store `0x0054992f`, scalar destructor store `0x005499d2` |
| `xrefs_to 0x006220b0` | constructor store `0x0054967f`, raw-body store `0x00549939`, scalar destructor store `0x005499dc` |
| `decompile 0x00549920` | fails with `code:null` / `Decompilation failed at 0x549920`, matching the no-function boundary |
| `make_signature 0x00549920` | unique signature beginning `56 8B F1 8D 8E F8 00 00 00 C7 06 34 20 62 00 C7 86 A0 00 00 00 80 20 62 00 C7 86 A4 00 00 00 B0 20 62 00 E8 D8 F3 F6 FF` |
| `make_signature_for_range 0x00549911-0x00549950` | unique range signature with fifteen leading `CC` bytes before the raw body |
| `disasm 0x00549920` | raw `<no function>` code writes `PhotoPane` vtables, calls `sub_4B8D20`, then jumps to `sub_549AE0` |

The three vtable targets used here are the `PhotoPane` vtable bases `??_7PhotoPane@@6B@` at `0x00622034`, `??_7PhotoPane@@6B@_0` at `0x00622080`, and `??_7PhotoPane@@6B@_1` at `0x006220b0`. Current vtable xrefs show the constructor, this raw body, and the scalar deleting destructor all store the same three `PhotoPane` vtables.

Modeled destructor parity:

- `analyze_function 0x005499c0` decompiles the scalar deleting destructor as writing the same `PhotoPane` vtables, calling `sub_4B8D20`, then calling `sub_549AE0`, with optional object free through `0x004f4ac0`.
- `analyze_function 0x00549ae0` decompiles the non-deleting destructor as resetting `this + 0x0`, `this + 0xa0`, and `this + 0xa4` to `PictureViewPane` vtables before chaining to pane teardown.
- `PhotoPane::PhotoPane` at `0x00549620` calls the `PictureViewPane` constructor, writes the same three `PhotoPane` vtables, constructs the embedded `GrafPort` at `this+0xf8` with `sub_4B8BF0`, and includes constructor EH cleanup jumps to `sub_549AE0` at `0x006060c3` and `sub_4B8D20` at `0x006060d1`.
- `xrefs_to 0x004b8d20` includes the raw cleanup call at `0x00549943`, the scalar deleting destructor call at `0x005499e6`, and the constructor EH cleanup jump at `0x006060d1`, so the constructor unwind path and both destructor-family paths destroy the same embedded `GrafPort`/`m_snapshotPort` member.

Relevant disassembly:

```asm
549920  push    esi
549921  mov     esi, ecx
549923  lea     ecx, [esi+0F8h]
549929  mov     dword ptr [esi], offset ??_7PhotoPane@@6B@
54992F  mov     dword ptr [esi+0A0h], offset ??_7PhotoPane@@6B@_0
549939  mov     dword ptr [esi+0A4h], offset ??_7PhotoPane@@6B@_1
549943  call    sub_4B8D20
549948  mov     ecx, esi
54994A  pop     esi
54994B  jmp     sub_549AE0
```

Exact 48-byte body:

```text
56 8B F1 8D 8E F8 00 00 00 C7 06 34 20 62 00 C7
86 A0 00 00 00 80 20 62 00 C7 86 A4 00 00 00 B0
20 62 00 E8 D8 F3 F6 FF 8B CE 5E E9 90 01 00 00
```

- SHA256: `0BCA5B90399E146377667D6F796D4643CFD5D9F3941729B6B7A5318483162D6B`.
- The protected predecessor is exactly 15 `0xcc` bytes at `[0x00549911,0x00549920)`.
- The exact successor is modeled source method [UID:0004RK][0x00549950-0x005499a2.PhotoPaneOnPaint](by-memory/0x00549950-0x005499a2.PhotoPaneOnPaint.md). A function definition for the raw body must be bounded to `[0x00549920,0x00549950)` and must not consume either fence.
- Current zero-xref evidence is preserved: no inbound reference targets `0x00549920`. The body is retained compiler lowering because its full instruction sequence, all three vptr stores, member cleanup, and base tail jump match the constructor/scalar-destructor family, not because of a direct caller route.

## Touched State

| State | Evidence | Interpretation |
| --- | --- | --- |
| `this + 0x0` | vtable write at `0x00549929` | Primary `PhotoPane` vtable during derived cleanup. |
| `this + 0xa0` | vtable write at `0x0054992f` | Secondary `PhotoPane` vtable/subobject view; offset `0xa0` is 160 (Verified with `int_convert.py`). |
| `this + 0xa4` | vtable write at `0x00549939` | Tertiary `PhotoPane` vtable/subobject view; offset `0xa4` is 164 (Verified with `int_convert.py`). |
| `this + 0xf8` | `lea ecx, [esi+0F8h]`; call `sub_4B8D20` | Embedded `GrafPort`/`m_snapshotPort` subobject destroyed before base teardown; offset `0xf8` is 248 (Verified with `int_convert.py`). |
| `0x00549ae0` | tail jump | Base `PictureViewPane` destructor chain. |

## Boundary Evidence

- `0x00549911-0x00549920` is fifteen `0xcc` padding bytes after `PhotoPane::PhotoPane` (`0xf` bytes; 15 bytes; Verified with `int_convert.py`). Current `make_signature_for_range 0x00549911-0x00549950` includes those fifteen leading `CC` bytes followed by the unique raw-body signature.
- `0x00549920-0x00549950` is executable raw destructor-shaped code, but IDA does not model it as a function and reports no direct callers.
- `0x00549950` starts IDA-modeled `PhotoPane::OnPaint`; live bytes begin `55 8b ec 83 ec 14 a1 24 2f 67 00`, and the `0x00549950-0x005499a2` signature is unique.

## Reconstruction Notes

- Treat the body as destructor-family cleanup behavior even though IDA lacks a function boundary at `0x00549920`.
- Route the body to [UID:0000AG][PhotoPane](by-class/PhotoPane.md), not directly to the file root. The stores target `PhotoPane` vtables, the cleanup matches `PhotoPane::ScalarDeletingDestructor`, and the only continuation is `PhotoPane::~PhotoPane`/`PictureViewPane` base teardown.
- The formal C++ block is intentionally comment-only. This raw body is generated from implicit `PhotoPane` destruction, the `GrafPort m_snapshotPort` member destructor, and the explicit `PictureViewPane` base destructor, not from a standalone `PhotoPane` helper or a body-shaped reconstruction of compiler vptr/base/member sequencing.
- The scalar deleting destructor at `0x005499c0` remains the stronger source-level deletion entry point for ordinary object destruction.
- The no-function/no-xref/no-direct-route evidence remains a confidence cap, not a blank-C++ blocker. The current comment-only marker supersedes the earlier blank-formal-C++/no-code disposition while preserving its valid negative audit evidence.

## Supervisor-Only IDA Function Handoff

This callback did not mutate or save IDA. The accepted Gate 2B handoff is bounded and remains supervisor-owned:

| Exact entity | Current accepted prestate | Proposed action | Safety and expected readback |
| --- | --- | --- | --- |
| `[0x00549920,0x00549950)` | raw code; no function/name/type; address regular/repeatable comments absent; function comments unavailable | define exact function `PhotoPane_Destructor`, type `void __thiscall PhotoPane_Destructor(PhotoPane *this)`, and function regular comment `Implicit PhotoPane destructor lowering: installs the three PhotoPane vtables, destroys m_snapshotPort at +0xf8, then tail-calls PictureViewPane::~PictureViewPane().`; leave repeatable absent | preserve all 48 bytes, the 15-byte predecessor CC fence, and OnPaint successor; read back one exact function/range/name/type/comment |
| `[0x005499a2,0x005499ad)` | `sub_5499A2`; `_DWORD *__thiscall(_DWORD *this, char)`; address regular `Block`; other comments absent | rename `PhotoPane_ScalarDeletingDestructor_AdjustA0`, type `PhotoPane *__thiscall PhotoPane_ScalarDeletingDestructor_AdjustA0(char *adjustedThis, unsigned int flags)`, and replace address regular comment with `Adjusts the EventHandler subobject pointer by -0xa0 and tail-jumps to the PhotoPane scalar deleting destructor.` | preserve exact 11-byte `sub ecx,0xa0` thunk and tail jump to `0x005499c0` |
| `[0x005499ad,0x005499b8)` | `sub_5499AD`; `_DWORD *__thiscall(_DWORD *this, char)`; address regular `Block`; other comments absent | rename `PhotoPane_ScalarDeletingDestructor_AdjustA4`, type `PhotoPane *__thiscall PhotoPane_ScalarDeletingDestructor_AdjustA4(char *adjustedThis, unsigned int flags)`, and replace address regular comment with `Adjusts the TimerHandler subobject pointer by -0xa4 and tail-jumps to the PhotoPane scalar deleting destructor.` | preserve exact 11-byte `sub ecx,0xa4` thunk and tail jump to `0x005499c0` |
| `[0x005499c0,0x00549a22)` | `sub_5499C0`; `PictureViewPane *__thiscall(PictureViewPane *Block, char)`; all comments absent | rename `PhotoPane_ScalarDeletingDestructor`, type `PhotoPane *__thiscall PhotoPane_ScalarDeletingDestructor(PhotoPane *this, unsigned int flags)`, and add function regular comment `PhotoPane scalar deleting destructor: installs all three PhotoPane vtables, destroys m_snapshotPort, chains through PictureViewPane destruction, and frees storage only when (flags & 1) != 0 and (flags & 4) == 0.` | preserve exact range/bytes/delete branch and both adjustor xrefs |

Before any mutation, the supervisor must refresh these literal prestates in the current healthy IDB, confirm the unique `PhotoPane` UDT is still size `0x198`, verify all proposed names remain collision-free, make a verified IDB backup, and protect all adjacent fences and methods. After bounded application, read back every name/type/comment/range and protected neighbor, save the IDB, and record stable post-save identity. The current report-only implementation intentionally performs none of those actions.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | The page records exact 48-byte contents and SHA256, exact fences, raw/function boundary state, zero-start-xref provenance, all three vptr stores, member/base cleanup, constructor/scalar parity, source/comment-only disposition, and an implementation-ready bounded IDA function/type/name/comment handoff. |
| Confidence | 94 | Confidence is very strong for range, behavior, ownership, and compiler/source disposition. The only cap is that the retained compiler body has no inbound route and the descriptive IDA function spelling cannot recover the compiler's original internal label; neither uncertainty changes source emission or behavior. |

## Cross-References

- [UID:0001F2][0x00549620-0x00549bc5.PhotoAndPicturePanes](by-memory/0x00549620-0x00549bc5.PhotoAndPicturePanes.md)
- [UID:0000AG][PhotoPane](by-class/PhotoPane.md)
- [UID:0000AH][PictureViewPane](by-class/PictureViewPane.md)
- [UID:0000MK][PhotoPane](by-file/PhotoPane.md)

## Changes

- 2026-06-12 A004 Goal 2 refresh:
  - Before: `COMPLETION:74`, `CONFIDENCE:84`, and `AUTOGEN_PARENT_UID:0000MK`.
  - Changed to: `COMPLETION:86`, `CONFIDENCE:91`, and `AUTOGEN_PARENT_UID:0000AG`.
  - Evidence: live read-only IDA MCP on database `a002_batch346` reconfirmed `0x00549920` is not a function, has no direct start xrefs, has a unique `0x30`-byte signature, is bracketed by fifteen `0xcc` bytes and `PhotoPane::OnPaint`, writes the three `PhotoPane` vtables at `0x00622034`, `0x00622080`, and `0x006220b0`, calls `sub_4B8D20`, and tail-jumps to `0x00549ae0`. The same pass confirmed scalar deleting destructor parity at `0x005499c0`. Routing now uses direct class parent [UID:0000AG][PhotoPane](by-class/PhotoPane.md), which clears `85/86`, while [UID:0000MK][PhotoPane](by-file/PhotoPane.md) clears `86/85`.
- 2026-06-22 B014 Rule 26 source-quality incorporation:
  - Score unchanged at `86/91`.
  - Summary/evidence: B014 keeps the direct `PhotoPane` owner, resolves `this+0xf8` as cached `GrafPort`/`m_snapshotPort`, confirms zero rel32 refs to the raw start and parity with scalar cleanup, and clarifies that source C++ should model this as destructor/member cleanup or unwind support rather than a separate emitted helper.
- 2026-06-25 B005 implementation of accepted source-quality report:
  - Changed to: `COMPLETION:90`, `CONFIDENCE:92`; preserved `CANONICAL_OWNER:0000AG`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000AG`, and blank `EMITTER_POSITION_OPTIONAL`.
  - Inserted the formal comment-only C++ marker that identifies this as compiler-emitted `PhotoPane` destructor cleanup/unwind support generated from the destructor/member/base layout, not a standalone handwritten helper.
  - Summary/evidence: current MCP session `80de0a67` confirms no modeled function at `0x00549920` or `0x0054994b`, no xrefs to the raw start, exact raw disassembly, unique raw and padding-inclusive signatures, `PhotoPane` vtable store parity across constructor/raw/scalar destructor paths, constructor EH cleanup jumps to `sub_549AE0` and `sub_4B8D20`, `sub_4B8D20` as `GrafPort`/`m_snapshotPort` cleanup at `this+0xf8`, and `sub_549AE0` as the `PictureViewPane` base teardown transition.
  - The earlier blank-formal-C++/no-code disposition is superseded by the formal comment-only marker; its no-function/no-xref/no-direct-route evidence remains as confidence-capping audit evidence.
- 2026-07-15 B004 UID0001F3 source-graph closure: retained `90/92`, owner/emitter UID0000AG, and comment-only compiler disposition; set class-child emission position 30 and `Nested:0`, and corrected the source cause from an explicit `PhotoPane::~PhotoPane` spelling to the implicit PhotoPane destructor plus `m_snapshotPort` and PictureViewPane base destructor.
- 2026-07-26 Agent-B001 UID0003OU source-quality callback:
  - Before: `90/92`; exact body behavior was present, but the page lacked the accepted SHA256/full-byte identity and the implementation-ready supervisor Gate 2B function/thunk/scalar-wrapper handoff.
  - Changed to: `92/94`, exact bytes/hash/fences, explicit zero-route interpretation, refreshed class/file scores, and complete bounded supervisor-only IDA actions while preserving the formal comment-only CPP and blank H dispositions.
  - Evidence: live IDA MCP session `f085b224`, exact raw bytes, unique boundaries, current function/non-function state, vtable-store triads, `PhotoPane` UDT size/layout, `m_snapshotPort` cleanup, and PictureViewPane teardown.
