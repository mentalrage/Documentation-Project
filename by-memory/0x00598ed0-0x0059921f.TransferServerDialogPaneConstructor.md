*** UID:0004MV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000F7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000F7 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
TransferServerDialogPane::TransferServerDialogPane(bool autoClose)
    : Pane(3),
      m_currentFrame(0),
      m_frameCount(34)
{
    g_pTransferServerDialog = this;
    m_alpha = 0.9f;

    RectBounds frameBounds;
    RectBounds frameRect;
    unsigned short frameCount;

    if (g_useEpfAssets == 1) {
        frameCount = g_pEPFLib->GetEntryCount(L"TRANSSVR.EPF");
        for (int frameIndex = 0; frameIndex < frameCount; ++frameIndex) {
            g_pEPFLib->GetEntryRect(L"TRANSSVR.EPF", frameIndex, &frameRect);
            frameBounds.UnionWith(&frameRect);
        }
    } else {
        frameCount = g_pEPFLib->GetEntryCount(L"TRANSSVR.EPD");
        for (int frameIndex = 0; frameIndex < frameCount; ++frameIndex) {
            g_pEPFLib->GetEntryRect(L"TRANSSVR.EPD", frameIndex, &frameRect);
            frameBounds.UnionWith(&frameRect);
        }
    }

    if (frameBounds.right - frameBounds.left > g_screenWidth)
        frameBounds.right = frameBounds.left + g_screenWidth;
    if (frameBounds.bottom - frameBounds.top > g_screenHeight)
        frameBounds.bottom = frameBounds.top + g_screenHeight;

    if (g_activeMapPane != 0) {
        Point unusedOrigin;
        RectBounds mapBounds;
        g_activeMapPane->GetOrigin(&unusedOrigin);
        g_activeMapPane->GetBounds(&mapBounds);

        int offsetX = (mapBounds.right - mapBounds.left - 144) / 2;
        int offsetY = (mapBounds.bottom - mapBounds.top - 144) / 2;
        if (g_useEpfAssets == 1) {
            offsetX -= 100;
            offsetY -= 100;
        }
        frameBounds.Offset(offsetX, offsetY);
    } else {
        frameBounds.Offset((g_pScreenPane->GetScreenWidth() - 144) / 2,
                           (g_pScreenPane->GetScreenHeight() - 144) / 2);
    }

    AddToLayer(&frameBounds, 0, 0, g_mainUiLayerSlots.overlayPaneLayerContext);
    SetPaneOrder(0, 0);
    ScheduleTimer(0, 100, 0, 0);
    if (autoClose)
        ScheduleTimer(1, 5000, 0, 0);
    g_pSoundManager->PlaySound(0x19c, 100);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact TransferServerDialogPane constructor with Pane(3), singleton publish, inherited alpha, 34-frame TRANSSVR scan, unseeded bounds union, clamp/placement, layer/order, timer, and sound behavior; nine direct call sites prove live source use. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00598ed0-0x0059921f TransferServerDialogPane Constructor

## Status And Boundary

- Function: `TransferServerDialogPane::TransferServerDialogPane(bool autoClose)`.
- Exact half-open range `[0x00598ed0,0x00599220)`, 848 bytes; SHA256 `D10CD8D1163258DD09132DDCD6A56ECBCF65217ABA85236D911C9F5717ADE4B2`.
- Predecessor is one `0xcc` byte at `[0x00598ecf,0x00598ed0)` after TotemFrame. The ordinary destructor begins exactly at `0x00599220`.
- Owner/emitter: [UID:0000F7][TransferServerDialogPane](by-class/TransferServerDialogPane.md); source file [UID:0000OW][TransferServerDialogPane](by-file/TransferServerDialogPane.md).
- Parent split index: [UID:0001KD][0x00598ed0-0x0059943f.TransferServerDialogPane](by-memory/0x00598ed0-0x0059943f.TransferServerDialogPane.md).
- `Nested:4` opens the executable child's level relative to the parent.

## Exact Behavior

1. Constructs `Pane(3)`, installs the three derived vtable views through compiler lowering, and publishes `this` to `g_pTransferServerDialog`.
2. Stores inherited `GrafPort::m_alpha = 0.9f`, `m_currentFrame = 0`, and `m_frameCount = 34`; the object allocation and layout are exactly `0x100` bytes.
3. Selects `TRANSSVR.EPF` only when `g_useEpfAssets == 1`, otherwise `TRANSSVR.EPD`; reads the entry count, fetches each entry rectangle, and unions it into `frameBounds`.
4. `frameBounds` is deliberately not value-initialized. Preserving this odd source shape is required for binary parity; seeding it from the first frame or clearing it would be an invented repair.
5. Clamps width and height independently against `g_screenWidth` and `g_screenHeight` by reducing right/bottom from the existing left/top.
6. With an active map pane, calls both `GetOrigin` and `GetBounds`; the origin result is intentionally unused. It centers a 144-by-144 reference and, after a second `g_useEpfAssets` read, subtracts 100 from both offsets for EPF assets.
7. Without an active map pane, centers the same 144-by-144 reference using the screen pane's dimensions.
8. Adds the pane to `g_mainUiLayerSlots.overlayPaneLayerContext`, sets pane order `(0,0)`, schedules timer `0` for 100 ms, conditionally schedules timer `1` for 5000 ms, and plays sound `0x19c` at volume 100.

## Callers And Dependencies

- Nine direct call sites: `0x004f748b`, `0x004f8f7d`, `0x00507d63`, `0x00507dc5`, `0x0050ff65`, `0x0050ffce`, `0x0051012e`, `0x00510299`, and `0x005102fb`, grouped in five caller functions.
- Those callers are launch/consumer routes from login, main UI, map, and session logic. They do not own this implementation.
- Required source surfaces: Pane, Singleton, RectBounds, Point, EPF/ImageLib entry-count and rectangle helpers, active map/screen pane bounds, MainUiLayerSlots, TimerHandler scheduling, and SoundManager.
- Resource literals are exact: `L"TRANSSVR.EPF"` and `L"TRANSSVR.EPD"`. No asset package alias or descriptor is introduced.

## Layout And ABI Evidence

- `Pane` occupies `+0x000..+0x0f7`; inherited `GrafPort::m_alpha` is at `+0x90`.
- EventHandler and TimerHandler secondary facets are inherited at `+0xa0` and `+0xa4`.
- Empty `Singleton<TransferServerDialogPane>` overlaps at `+0xf8` by EBO; the two actual derived fields are `m_currentFrame` at `+0xf8` and `m_frameCount` at `+0xfc`.
- Constructor stores to vtable views at primary object offset 0, EventHandler facet `+0xa0`, and TimerHandler facet `+0xa4`; those stores are compiler effects of the declaration, not source statements.
- The sole constructor-unwind route clears the already-published singleton through the separate compiler-support child at `0x005993b0`.

## Rejected Alternatives And Historical Assumptions

- Rejected `memset`/zero-init of `frameBounds`, first-frame seeding, frame-count clamp, modulo guard, cached asset selector, omission of unused `GetOrigin`, and a unified map/screen helper: each changes observed source shape or binary behavior.
- Rejected direct writes for vptrs, TimerHandler receiver arithmetic, and raw layer slots; declarations and typed helpers own those ABI details.
- Rejected ownership by MainMenuPane, MapPane, EPFTileContext, ResourceLayoutTable, SoundManager, or the callers; all are dependencies/consumers.
- Historical names `OnTimerEvent`/`OnPaintFrame`, raw dword field descriptions, and `class_TransferServerDialogPane.cpp` were useful leads but do not override this source-facing model.

## Reconstruction Notes

- Preserve the managed body byte-for-byte at the report's source-shape level, including variable declaration order and the uninitialized accumulator.
- Do not add exception cleanup, frame-resource release, width/height lower clamps, auto-close cancellation, or singleton guards not present in the evidence.
- Compiler-generated vtable writes, EBO, unwind metadata, and base construction remain outside handwritten C++.
