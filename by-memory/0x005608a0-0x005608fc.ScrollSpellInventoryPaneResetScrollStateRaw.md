*** UID:0001GO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:190 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollSpellInventoryPane::ResetScrollState()
{
    RectBounds rect;
    RemovePendingTimers();
    const ScrollSpellInventoryPart oldPart =
        static_cast<ScrollSpellInventoryPart>(m_hoverPart);
    m_activePart = kScrollSpellInventoryPartNone;
    if (oldPart != kScrollSpellInventoryPartNone) {
        GetPartRect(oldPart, &rect);
        InvalidateRect(&rect);
        m_hoverPart = kScrollSpellInventoryPartNone;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 92-byte retained ScrollSpellInventoryPane::ResetScrollState body at 0x005608a0-0x005608fc, SHA256 3d64e239e2e318d9f95a42ef76be91240a9ae8afbb1279629730d308ea6e6132, with timer removal, active/hover reset, old-part invalidation, zero direct entry xrefs, exact generic sibling clone, successor padding, class/file ownership, CPP position 190 and blank method H. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005608a0-0x005608fc ScrollSpellInventoryPane ResetScrollState Raw
## UID0000NJ Applied Exact-Child Closure - 2026-08-16

This retained raw body is the nineteenth and final source method at position 190. Exact same-offset/same-length sibling bytes, coherent tail-field effects, clean boundaries, and class source ordering support the human `ResetScrollState` name despite zero direct entry xrefs. The formal C++ preserves timer removal, signed enum state, old-hover invalidation, and the 92-byte SHA256 `3d64e239e2e318d9f95a42ef76be91240a9ae8afbb1279629730d308ea6e6132`. The trailing `[0x005608fc,0x00560900)` bytes are four `CC` alignment bytes; successor `0x00560900` is excluded. UID0000CN owns the private H declaration, so this child H channel remains blank.

## UID0000LU Header Route Cross-Link - 2026-08-15

This raw source child continues to emit through UID0000CN; the owner declaration now resides in `ScrollSpellInventoryPane.h`. Raw-item provenance, reset behavior, boundary, and no-IDB-function caveat are unchanged.

## Status

- Disposition: reconstructable NexusTK project code with raw-function/no-direct-route caveat.
- Entity kind: raw method-shaped helper and exact source-bearing child.
- Source-facing method: `void ScrollSpellInventoryPane::ResetScrollState()`.
- Direct owner/emitter: [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md); selected standalone source-file route [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md), with [UID:0000LU][NewSpellInventoryPane](by-file/NewSpellInventoryPane.md) retained only as rejected historical private-fold context.
- Source order: emitter position `190`, immediately after source-ready [UID:0001GN][0x00560710-0x0056089d.ScrollSpellInventoryPaneUpdateFromDrag](by-memory/0x00560710-0x0056089d.ScrollSpellInventoryPaneUpdateFromDrag.md) at position `180`.
- Confidence: strong for exact byte boundary, class-local field/helper semantics, direct class ownership, and rejected hidden direct/table/vtable routes; medium for runtime reachability because no static caller route is proven.

## Behavior

This raw helper clears spell-scrollbar interaction state after capture or hover. It calls `TimerHandler::RemovePendingTimers()` at `0x00597600` through the `this + 0xa4` timer/update-handler view, reads old `m_hoverPart` at `this + 0x103`, clears `m_activePart` at `this + 0x104` to `0xff`, and if the old hover part was not `0xff`, computes that part rectangle through `ScrollSpellInventoryPane::GetPartRect` at `0x00560290` and invalidates it through the inherited Pane virtual slot at `+0x20`. It then clears `m_hoverPart` at `this + 0x103` to `0xff`.

## Boundary Notes

IDA does not currently model `0x005608a0` as a function, and there are no direct xrefs, callers, or loaded-segment dword pointer hits to this start in the current IDA database. Raw disassembly still shows aligned function-shaped code from `0x005608a0` through the `retn` at `0x005608fb`, with three `0xcc` bytes after the previous modeled function and four `0xcc` bytes before the next modeled function at `0x00560900`.

B002's 2026-06-18 local PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA1 `c285b73dc7b54c9a0e28ff7591a9bdde2ac55f9a`) independently confirmed the exact boundary: `0x0056089d-0x0056089f` is `cc cc cc`, `0x005608a0` begins the helper prologue, `0x005608fb` returns, `0x005608fc-0x005608ff` is `cc cc cc cc`, and `0x00560900` starts the next modeled function. The scan found no VA literal for `0x005608a0`, no RVA literal for `0x001608a0`, no direct branch/call target to `0x005608a0`, and no external direct branch/call into `0x005608a0-0x005608fc`; positive controls for nearby modeled helpers and vtable entries resolved, so the no-route result is meaningful.

## Raw Assembly Shape

```asm
005608a0  push    ebp
005608a1  mov     ebp, esp
005608a3  sub     esp, 14h
005608b3  lea     ecx, [esi+0A4h]
005608b9  call    sub_597600
005608be  mov     al, [esi+103h]
005608c4  mov     byte ptr [esi+104h], 0FFh
005608cb  cmp     al, 0FFh
005608cd  jz      short loc_5608ED
005608d3  push    eax
005608d4  mov     ecx, esi
005608d6  call    sub_560290
005608db  mov     eax, [esi]
005608e3  call    dword ptr [eax+20h]
005608e6  mov     byte ptr [esi+103h], 0FFh
005608f3  call    @__security_check_cookie@4
005608fb  retn
```

## Evidence

- IDA MCP `lookup_funcs 0x005608a0` reports `Not a function`.
- IDA MCP raw `disasm 0x005608a0` shows prologue, security-cookie setup/check, call to `0x00597600`, old highlighted-part invalidation through `0x00560290`, and `retn` at `0x005608fb`.
- 2026-05-31 IDA MCP raw disassembly confirms the byte writes: `+0x104` is cleared to `0xff` unconditionally, and `+0x103` is the old hover/highlight part invalidated through `0x00560290` before being reset to `0xff`.
- 2026-06-03 restarted IDA MCP recheck confirms `ida_funcs.get_func` still returns no function at `0x005608a0`, `0x005608fb`, or `0x005608fc`; the previous modeled function is `sub_560710` at `0x00560710-0x0056089d`, and the next modeled function is `sub_560900` at `0x00560900-0x005609a1`.
- The same recheck finds no xrefs to the raw start. The raw span's outgoing references are `___security_cookie`, `sub_597600` for the `this + 0xa4` cleanup view, `sub_560290` for part-rectangle computation, the pane virtual call at `[vtable + 0x20]`, and `@__security_check_cookie@4`.
- Boundary bytes around the raw helper are `... c2 08 00 cc cc cc 55 8b ec ...` before `0x005608a0` and `... 8b e5 5d c3 cc cc cc cc 55 8b ec ...` after the `0x005608fb` return, matching a standalone compiler-shaped body in an IDA function-table gap.
- 2026-06-05 live IDA MCP `lookup_funcs` reports `0x005608a0`, `0x005608fb`, and `0x005608fc` as `Not a function`; previous/next modeled functions remain `sub_560710` at `0x00560710-0x0056089d` and `sub_560900` at `0x00560900-0x005609a1`.
- 2026-06-05 live IDA MCP disassembly confirms the raw body clears `this + 0x104`, conditionally invalidates the previous `this + 0x103` part rectangle through `sub_560290` and virtual slot `+0x20`, clears `this + 0x103`, checks the security cookie, and returns at `0x005608fb`.
- 2026-06-05 live IDA MCP `xrefs_to` and `callers` report no direct references to `0x005608a0`; a loaded-segment dword scan also finds no pointer equal to `0x005608a0`.
- 2026-06-05 live IDA MCP confirms three `0xcc` bytes at `0x0056089d-0x005608a0`, four `0xcc` bytes at `0x005608fc-0x00560900`, an unowned raw call to `sub_597600` at `0x005608b9`, and an unowned raw call to `sub_560290` at `0x005608d6`.

## 2026-06-18 B002 Source-Quality Reanalysis

B002 rechecked the target under the current source-quality rules and confirmed this page should remain an exact source-bearing raw helper, not be merged into the aggregate and not be owned directly by the file. The direct semantic owner/emitter is [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) because the body operates on `ScrollSpellInventoryPane` fields, calls the class-local `GetPartRect`, and invalidates through the pane virtual on the same object. [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md) remains the generated file route; [UID:0000LU][NewSpellInventoryPane](by-file/NewSpellInventoryPane.md) is only a constructor-consumer/fold candidate, not a method owner.

The best source-facing name is `ScrollSpellInventoryPane::ResetScrollState()`. `ResetInteractionState` was rejected because that vocabulary belongs to the accepted ScrollVolumePane precedent, while this target's name, generated lead, and spell/inventory sibling terminology consistently use scroll-state naming. `ClearHoverPart`, `ClearActivePart`, and `StopTimer` were rejected as too narrow because the helper removes pending timers, clears the active part, invalidates any stale hover part, and clears hover state. The ABI consumes no stack arguments and returns no value, so the source signature is `void ScrollSpellInventoryPane::ResetScrollState()`.

Field and helper names should use the source-facing model established by the report: `this + 0xa4` is a tertiary `TimerHandler` / timer-update handler view, not a scalar field; `this + 0x103` is `char m_hoverPart`, with `0xff` / `-1` as the no-part sentinel; `this + 0x104` is `char m_activePart`, not the generated-source-polluted `m_prevActivePart`; `sub_597600` is `TimerHandler::RemovePendingTimers()` on the `+0xa4` view; `sub_560290` is `ScrollSpellInventoryPane::GetPartRect(char part, RectBounds *outRect)`; the vtable slot at `+0x20` is the inherited pane invalidation virtual.

The no-route caveat remains: no direct caller, vtable slot, pointer table, RVA literal, VA literal, or external direct branch enters this span. That constrains IDA function creation and final-audit confidence, but exact bytes, boundaries, field effects, generic sibling equivalence, and source order justify the current `93/94`, `RECONSTRUCTABLE:TRUE` class-owned emission. Sibling UID0001H1 remains independent corroboration rather than an unverified score shortcut.

## Autogen Status

- Reconstructable: true, exact raw source-bearing method-shaped project code.
- Direct owner/emitter: [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md), which routes to [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md) and generated path `auto-generated/NexusTK/ui/inventory/ScrollSpellInventoryPane.cpp`.
- Generated-output caveat: the active generated `ScrollSpellInventoryPane.cpp` was zero bytes before this correction. `simroot_v2/ui/inventory/class_ScrollSpellInventoryPane.cpp` supplies useful lead names (`ResetScrollState`, `SetHoverPart`, `GetPartRect`) but also contains synthetic layout/pad/non-source-quality pollution and should not be copied directly.
- C++: exact formal C++ is populated for this bounded helper. The page is `93/94`, `RECONSTRUCTABLE:TRUE`, emits through UID0000CN at position 190, and retains the raw no-direct-route caveat below final-audit scores.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 87 | The page records exact raw assembly shape, previous/next modeled function bounds, padding bytes, field writes, outgoing helper calls, no direct xrefs/callers, no pointer-table hits, class owner/emitter routing, source-facing name/signature, field/helper names, rejected ownership/name alternatives, sibling-scope limits, and first-draft C++ for the exact body. Completion remains capped because no modeled function or caller route proves runtime reachability. |
| Confidence | 89 | Existing live IDA evidence and B002's independent PE scan agree on the raw helper behavior, exact byte span, no VA/RVA/direct/table/vtable route, and class-local helper/field semantics. Confidence remains below final-audit range because IDA still does not model `0x005608a0` as a function and runtime reachability is caller-unproven. |

## Cross-References

- [UID:0001GM][0x0055f450-0x005608fc.ScrollSpellInventoryPane](by-memory/0x0055f450-0x005608fc.ScrollSpellInventoryPane.md)
- [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md)
- [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md)
- [UID:0000LU][NewSpellInventoryPane](by-file/NewSpellInventoryPane.md)
- [UID:0001GN][0x00560710-0x0056089d.ScrollSpellInventoryPaneUpdateFromDrag](by-memory/0x00560710-0x0056089d.ScrollSpellInventoryPaneUpdateFromDrag.md)

## Changes

- 2026-06-18 B002 source-quality execution:
  - Changed to: completion `87`, confidence `89`, `CANONICAL_OWNER:0000CN`, `EMITTER_UIDS:0000CN`, and populated first-draft `ScrollSpellInventoryPane::ResetScrollState()` C++.
  - Summary/evidence: B002 local PE scan of `NexusTK.exe` confirmed the exact raw boundary, pre/post padding, zero VA/RVA literals to `0x005608a0`, zero direct branch/call targets to the raw start, zero external direct branches into the span, and positive controls for nearby modeled helpers and vtable entries. The body removes timers through the `+0xa4` TimerHandler view, clears `m_activePart` at `+0x104`, conditionally invalidates old `m_hoverPart` at `+0x103` through `GetPartRect` and Pane invalidation, then clears `m_hoverPart`.
  - Owner/C++ rationale: direct owner/emitter is the class [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md), with file route [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md). `NewSpellInventoryPane`, shared scrollbar ownership, and TimerHandler ownership are rejected as consumer/dependency contexts. The raw no-route caveat remains score-limiting and blocks IDA function creation, but it no longer blocks bounded first-draft C++ under the active combined-score/emitter gate.
- 2026-06-07:
  - Changed to: `AUTOGEN_PARENT_UID:0000NJ`, keeping completion/confidence at `84/86` and C++ blank.
  - Summary/evidence: the spell-inventory scrollbar parent chain now satisfies the attachment gate: [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md) is `84/80`, [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) is `80/80`, and this helper is `84/86`.
  - Historical parent/C++ rationale, superseded by the 2026-06-18 B002 source-quality pass: parent attachment was treated as safe for generated assembly ordering, but final C++ was left blank because IDA still did not model `0x005608a0` as a function and neither direct xrefs nor pointer-table evidence proved a source-level call route.
- 2026-06-05:
  - Changed to: completion `84`, confidence `86`, keeping `RECONSTRUCTABLE:TRUE`, blank `AUTOGEN_PARENT_UID`, and blank C++.
  - Summary/evidence: live IDA MCP reconfirmed the exact `0x005608a0-0x005608fb` method-shaped body, previous/next modeled functions, pre/post `0xcc` padding, active/highlight reset writes at `+0x104` and `+0x103`, outgoing calls to `sub_597600` and `sub_560290`, virtual invalidation through slot `+0x20`, and the absence of direct xrefs, callers, or loaded-segment pointer hits for the raw start.
  - Historical parent/C++ rationale, superseded by the 2026-06-18 B002 source-quality pass: parent assignment remained blank because the current class/file parents were below the confidence threshold; final C++ was left blank because the entry lacked modeled-function and caller provenance under the then-used 95/95 reconstruction bar.
- 2026-05-31: Raised completion/confidence from `50/65` to `70/78` and marked the raw helper reconstructable.
  - Before: The page documented the raw method-shaped helper but left reconstructable blank and did not capture the latest field-level reset evidence.
  - Changed to: Added explicit IDA-backed byte semantics for active part `+0x104` and hover/highlight part `+0x103` while keeping confidence below the sibling exact function because IDA still does not model `0x005608a0` as a function start.
  - Evidence: IDA MCP `lookup_funcs` still reports `0x005608a0` as not a function; raw disassembly confirms a complete method-shaped prologue/body/security-cookie epilogue ending at `0x005608fb`.
- 2026-06-03: Raised completion/confidence from `70/78` to `72/80` after restarted IDA MCP reconfirmed the raw helper's exact byte span, previous/next modeled functions, field writes, outgoing helper calls, and absence of start xrefs. Parent assignment remains blank because the parent class confidence is still below the attachment threshold; C++ remains blank while the function-table gap and call provenance are unresolved.
- 2026-07-22 B001 UID0001GN implementation callback:
  - Retained the existing `87/89` score, [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) owner/emitter, exact formal body, and all raw no-route evidence; set emitter position `90` after UID0001GN at position `80`.
  - Updated only current source-order/status wording: standalone UID0000NJ is selected, the former NewSpell fold is historical/rejected, and the modeled-function/caller gap remains a confidence cap rather than a C++ blocker.
