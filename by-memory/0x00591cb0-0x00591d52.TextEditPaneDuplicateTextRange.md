*** UID:000566 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5840048 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@00591CB0 | UID0000OJ accepted formal channel
int TextEditPane::DuplicateTextRange(short start,
                                     short end,
                                     wchar_t **outText) const
{
    if (outText == NULL)
        return 0;
    if (start < 0) start = 0;
    if (end > GetTextLength()) end = GetTextLength();
    if (end <= start) {
        *outText = NULL;
        return 0;
    }
    const int count = end - start;
    *outText = static_cast<wchar_t *>(
        GetMemoryMan()->AllocateBufferMemory(count * sizeof(wchar_t)));
    GetMemoryMan()->MemmoveWrapper(*outText, GetTextBuffer() + start,
                                   count * sizeof(wchar_t));
    return count;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-ready TextEditPane UTF-16 range duplication helper at [0x00591cb0,0x00591d52), routed through TextEditPane with its declaration owned once by the class page. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00591cb0-0x00591d52 TextEditPane DuplicateTextRange

## Status And Source Placement

- Exact range: `[0x00591cb0,0x00591d52)`, 162 bytes.
- Entity: ordinary source-authored private `TextEditPane` member definition.
- Canonical owner and emitter: [UID:0000EO][TextEditPane](by-class/TextEditPane.md); source module [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Exact source order is after [UID:000565][0x00591c70-0x00591ca1.TextEditPaneIsPasteUnavailable](by-memory/0x00591c70-0x00591ca1.TextEditPaneIsPasteUnavailable.md) and before [UID:0001JP][0x00591d60-0x00593c20.TextEditPaneFormattingRunsAndMeasureAdapters](by-memory/0x00591d60-0x00593c20.TextEditPaneFormattingRunsAndMeasureAdapters.md).
- This exact page owns the sole CPP body. Its H channel stays blank because UID0000EO already owns `int DuplicateTextRange(short, short, wchar_t **) const;` once in the private class declaration.

## Exact Boundary And Liveness Evidence

- The accepted whole-file bounded inventory classifies this exact half-open range as raw source body R24: no separate IDA function object, zero physical start xrefs, no first inbound reference, and four outbound direct-call instructions.
- Predecessor padding `[0x00591ca1,0x00591cb0)` is fifteen `0xcc` bytes; successor padding `[0x00591d52,0x00591d60)` is fourteen `0xcc` bytes. Both have zero start xrefs and remain compiler-covered outside this page.
- The accepted calls and data flow obtain the current text length, use the project memory manager for allocation/copy, and read the existing UTF-16 text buffer. No independently owned data head, initializer, destructor, or store route exists in the range.
- Zero start xrefs do not make the body padding. Its branch/call structure, exact R24 boundary, private declaration, and position between clipboard ingestion and format-run serialization retain one source member body.

## Behavior And Data Flow

- Reject `outText == NULL` with return zero and no dereference.
- Clamp a negative start to zero and an end beyond `GetTextLength()` to the current text length. If the resulting `end <= start`, write `NULL` to the valid output slot and return zero.
- For a positive span, compute `count = end - start`, allocate exactly `count * sizeof(wchar_t)` bytes through `MemoryMan`, and copy exactly that many bytes from `GetTextBuffer() + start` into the allocated result.
- Return the copied UTF-16 code-unit count. Do not append an invented terminator, allocate for `count + 1`, normalize reversed ranges, throw on allocation failure, or transfer ownership to another object.

## Name, Signature, And Ownership Reasoning

- `DuplicateTextRange` describes the exact allocation-and-copy behavior without implying a NUL-terminated String result. `CopyTextRange` and `GetTextRange` were rejected because they obscure the caller-owned allocated-output contract.
- Signed-short endpoints match the class's text-offset domain; `wchar_t **outText` matches the explicit allocated result; `int` preserves the copied-count return; trailing `const` is supported because the helper reads but does not mutate editor state.
- The body is a private TextEditPane helper, not a file-local free function or MemoryMan method: it consumes TextEditPane length/buffer state and its accepted declaration is private in UID0000EO.
- UID0000EO's CPP child marker emits this page through UID0000ON between `CPP@00591C70` and `CPP@00591D60`; copying the body into the class page would recreate the duplicate-definition defect this split closes.

## Score Rationale And Historical Correction

- `94/94` reflects exact bounds, neighboring padding, call count, all null/clamp/empty/allocation/copy branches, text and memory-manager data flow, source-facing signature, owner/emitter route, source order, and complete formal C++.
- Confidence remains below 95 because stripped symbols cannot prove the original private lexical spelling independently of the accepted behavior and class-family reconstruction.
- Historical UID0000OJ text that routed R24's definition directly through the class CPP block is superseded only for physical formal ownership: the body and behavior are unchanged, but UID000566 is now the sole exact CPP owner.

## Rebuild Policy

- Emit exactly the managed body through UID0000EO/UID0000ON at `CPP@00591CB0`.
- Keep both adjacent padding spans compiler-covered, retain the declaration only in UID0000EO H, and do not emit raw addresses, xref commentary, or duplicate source.
