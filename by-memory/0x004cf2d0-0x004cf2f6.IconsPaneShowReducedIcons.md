*** UID:0002T0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006B | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006B | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IconsPane::ShowReducedIcons()
{
    if (m_showAllIcons) {
        g_pConfig->m_iconPaneReducedMode = 1;
        m_showAllIcons = false;
        InvalidateRect(&m_bounds);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: `IconsPane::ShowReducedIcons` switches from all-icons mode to reduced mode by persisting reduced-mode byte `1`, clearing `m_showAllIcons`, and invalidating bounds. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cf2d0-0x004cf2f6 IconsPane ShowReducedIcons

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method/state transition.
- Owner: [UID:00006B][IconsPane](by-class/IconsPane.md).
- Likely source placement: [UID:0000JZ][IconsPane](by-file/IconsPane.md).
- Parent aggregate: [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md).
- Rebuild handling: source-authored UI/config state update.
- Confidence: strong for range, owner, behavior, caller route, and first-draft C++ source shape; medium-high for exact original member/global spelling because the names are inferred from binary/source-family evidence rather than original symbols.

## Address Range

`0x004cf2d0-0x004cf2f6`, ending before `OnPaint` at `0x004cf300`.

## Behavior

This method switches the icon strip from all-icons mode to reduced display mode. If `m_showAllIcons` at `this + 0xf8` (`248`, Verified with `int_convert.py`) is true, it writes `g_pConfig->m_iconPaneReducedMode = 1` at config offset `+0x28de32` (`2678322`, Verified with `int_convert.py`), clears `m_showAllIcons`, and invalidates the pane bounds with `InvalidateRect(&m_bounds)`.

The invalidation call is the inherited pane rectangle-invalidation slot at vtable offset `+0x20` (`32`, Verified with `int_convert.py`) with the bounds member at `this + 0x44` (`68`, Verified with `int_convert.py`). If the pane is already reduced (`m_showAllIcons == false`), the function returns without touching config, object state, or invalidation.

Reduced mode makes paint and hit-test logic start at icon index `6`, leaving only the last two icon buttons visible.

## Evidence

- 2026-06-26 B004 live IDA MCP evidence used active session `80de0a67`; `server_health` reported `NexusTK.exe`, imagebase `0x400000`, auto-analysis/Hex-Rays/strings ready.
- `lookup_funcs` confirmed `sub_4CF2D0` is a modeled function at `0x004cf2d0`, size `0x26`; `0x004cf2f6` is not a function start, and the next modeled method is `sub_4CF300` at `0x004cf300`.
- Exact function bytes are `80 b9 f8 00 00 00 01 75 1c a1 c8 a7 67 00 c6 80 32 de 28 00 01 8d 41 44 8b 11 50 c6 81 f8 00 00 00 00 ff 52 20 c3`. The same byte string is a unique range signature for `0x004cf2d0-0x004cf2f6`.
- `disasm` shows the complete guarded transition: `cmp byte ptr [ecx+0F8h], 1`; `jnz` to the return; load `dword_67A7C8`; write byte `[eax+28DE32h] = 1`; `lea eax, [ecx+44h]`; load the vtable; push the bounds pointer; clear `[ecx+0F8h]`; call `dword ptr [edx+20h]`; `retn`.
- `decompile` agrees on the same data flow but shows a synthetic `int` return because the indirect invalidation call's return value is propagated in raw pseudocode. Source shape is `void` because no caller consumes a return and the sibling state mutators are side-effect-only UI methods.
- `xrefs_to` reports exactly one code caller at `0x0053dee0` inside `sub_53DDB0`. The caller window reads OptionPane control id `6`, writes the same config byte, branches on the checkbox byte, calls [UID:0002SZ][0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons](by-memory/0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons.md) `ShowAllIcons` at `0x0053ded2` when the byte is zero, and calls this reduced-mode method at `0x0053dee0` when the byte is nonzero.
- `get_bytes` verified the ten-byte `0xcc` padding span at `0x004cf2f6-0x004cf300`; `xrefs_to` found no references to `0x004cf2f6`. `find_bytes` found no loaded VA or RVA pointer hits for the target start, so there is no current evidence for an address-taken pointer-table route.
- `0xf8 == 248`, `0x28de32 == 2678322`, `0x44 == 68`, and `0x20 == 32` were verified with `tools/int_convert.py`.
- [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md) records this exact range, persisted config byte, state flag, and invalidation behavior.
- [UID:00006B][IconsPane](by-class/IconsPane.md) records this as `ShowReducedIcons` and identifies `showAllIcons` at `+0xf8`.
- The legacy Wave2 report notes this was restored as `IconsPane::ShowReducedIcons` after IDA confirmed it was the inverse `IconsPane` state-transition method.
- Sibling pages [UID:0002SY][0x004cf290-0x004cf297.IconsPaneIsShowingAllIcons](by-memory/0x004cf290-0x004cf297.IconsPaneIsShowingAllIcons.md) and [UID:0002SZ][0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons](by-memory/0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons.md) establish the accepted source-facing state names: `m_showAllIcons`, `g_pConfig->m_iconPaneReducedMode`, `m_bounds`, and inherited `InvalidateRect`.

## Reconstruction Notes

Keep this as the inverse of [UID:0002SZ][0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons](by-memory/0x004cf2a0-0x004cf2c6.IconsPaneShowAllIcons.md) `IconsPane::ShowAllIcons`. The accepted first-draft C++ uses the same source-family names as the sibling methods: `m_showAllIcons` for the runtime bool at `+0xf8`, `g_pConfig->m_iconPaneReducedMode` for the persisted config byte at `+0x28de32`, `m_bounds` for the rectangle at `+0x44`, and inherited `InvalidateRect` for the vtable slot `+0x20` call.

The old blank-C++ blocker is superseded for this exact child. Broader declaration spelling and final class/header placement remain score caps, not blockers to emitting the simple state mutator. The formal body intentionally emits no padding and no sibling code.

Rejected alternatives:

- Raw `dword_67A7C8`, `this[248]`, `field_0xf8`, `byte_28de32`, or raw virtual-call syntax are retained only as evidence/search aliases; they are not plausible final source-facing names.
- `m_iconPaneReducedMode` is rejected for the object field because the runtime bool is inverse-polled as "show all icons"; the config field uses the reduced-mode polarity.
- No-argument `Invalidate()`, `Refresh()`, or `Redraw()` is rejected because the binary pushes a rectangle pointer before the inherited invalidation call.
- Keeping formal C++ blank is rejected because the target clears the current code-entry gate, has a confirmed owner/emitter route, has current MCP-backed range/caller evidence, and uses names already accepted by sibling method pages.

## Score Rationale

Completion `91` / confidence `92`: exact body bytes, disassembly, decompilation, size, padding, caller route, source owner, source placement, field/global/helper names, and sibling polarity are current and documented. The previous target-level blocker was blank C++ caused by broader naming concerns; sibling method implementation and the B004 MCP pass resolve those concerns enough for this exact child.

The score is not lower because no target-local behavior, range, ownership, caller, or first-draft C++ blocker remains. The score stays below `95+` because exact original source spelling for the accepted member/global names and inherited invalidation declaration is inferred rather than symbol-proven, and the full class declaration/header placement remains broader `IconsPane` work.

Open questions that remain broader than this target: final `Config` member spelling, final class declaration placement, and project-wide generated-output refresh after supervisor execution. They do not change this method's behavior, owner/emitter, range, or formal first-draft C++.

## Changes

- 2026-06-26 B004 ShowReducedIcons first-draft implementation sync:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`; owner/emitter/reconstructable already [UID:00006B][IconsPane](by-class/IconsPane.md), `TRUE`, [UID:00006B][IconsPane](by-class/IconsPane.md), but formal C++ was blank under stale broader field/global/invalidation-helper wording.
  - After: `COMPLETION:91`, `CONFIDENCE:92`; owner/emitter/reconstructable unchanged; formal `void IconsPane::ShowReducedIcons()` C++ inserted.
  - Evidence: live MCP session `80de0a67` confirmed `sub_4CF2D0` size `0x26`, exact bytes `80 b9 f8 00 00 00 01 75 1c a1 c8 a7 67 00 c6 80 32 de 28 00 01 8d 41 44 8b 11 50 c6 81 f8 00 00 00 00 ff 52 20 c3`, the guard on `m_showAllIcons`, the persisted reduced-mode write `g_pConfig->m_iconPaneReducedMode = 1`, the clear of `m_showAllIcons`, inherited `InvalidateRect(&m_bounds)`, ten bytes of `0xcc` padding before `OnPaint`, the single OptionPane apply caller at `0x0053dee0`, and no direct VA/RVA pointer hits for the function start.
  - Source-quality scope: raw IDA labels, raw virtual-call syntax, no-argument invalidation, and blank formal C++ are rejected for this exact child; broader class/file raw-dispatch, table/raw-helper gap, action-helper naming, and TabPane grouping caveats remain outside this child.
- 2026-06-03: Split this exact reduced-icon-mode range out of the broader [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md) aggregate and attached it to [UID:00006B][IconsPane](by-class/IconsPane.md) as reconstructable source-owned UI code. Final C++ remains blank under the 95+ gate.
- 2026-06-15 A001 Goal 2 live IDA refresh:
  - Before: scored `78/86`.
  - After: scored `86/88`, still owned/emitted through [UID:00006B][IconsPane](by-class/IconsPane.md), with final C++ blank.
  - Evidence: live IDA MCP confirmed the exact `0x26`-byte function, guarded reduced-mode transition, `g_pConfig + 0x28de32` persistence write, `this + 0xf8` update, vtable `+0x20` invalidation call with `this + 0x44`, ten-byte padding before `OnPaint`, and the single OptionPane apply caller at `0x0053dee0`.

## Cross-References

- [UID:00006B][IconsPane](by-class/IconsPane.md)
- [UID:0000JZ][IconsPane](by-file/IconsPane.md)
- [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md)
- [UID:0000M7][OptionPane](by-file/OptionPane.md)
