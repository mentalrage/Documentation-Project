*** UID:0003JX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000079 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000079 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void List::InsertAt(int index, int count, void *source)
{
    MemoryMan *memoryMan = GetMemoryMan();

    int oldCount = m_count;
    int oldPageCount = 1;
    if (oldCount > 0)
        oldPageCount = ((oldCount - 1) / m_pageSize) + 1;

    int newCount = oldCount + count;
    int newPageCount = 1;
    if (newCount > 0)
        newPageCount = ((newCount - 1) / m_pageSize) + 1;

    if (oldPageCount < newPageCount) {
        size_t byteCount = newPageCount * m_pageSize * m_elementSize;
        if (m_data == NULL)
            m_data = memoryMan->AllocateBufferMemory(byteCount);
        else
            m_data = memoryMan->ReallocateBufferMemory(m_data, byteCount);
    }

    unsigned char *data = static_cast<unsigned char *>(m_data);
    memoryMan->MoveBufferMemory(
        data + ((index + count) * m_elementSize),
        data + (index * m_elementSize),
        (oldCount - index) * m_elementSize);

    m_count += count;
    memoryMan->MemmoveWrapper(
        data + (index * m_elementSize),
        source,
        count * m_elementSize);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Virtual List insert-at/range method that grows storage, shifts the tail, increments count, and copies caller bytes into the gap. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f32e0-0x004f33b5 List Insert Range

## Status

- Entity kind: `List` virtual mutation method.
- Owner: [UID:000079][List](by-class/List.md) `List`.
- Source file: [UID:0000KS][List](by-file/List.md) `List.cpp`.
- Parent aggregate: [UID:00022S][0x004f3140-0x004f35fd.ListCoreMethods](by-memory/0x004f3140-0x004f35fd.ListCoreMethods.md) `ListCoreMethods`.
- Rebuild handling: source-authored container implementation code.

## Behavior

Inserts one or more fixed-width elements at a caller-supplied index. It computes the current and post-insert page counts, grows storage when the new count crosses a page boundary, shifts the existing tail upward, increments `count`, then copies `insertCount * elementSize` bytes from the caller's source pointer into the gap.

The observed memory movement order is tail shift first, then insertion copy. This preserves the original tail while the new records are written.

## Raw Evidence

- 2026-06-12 live IDA MCP `lookup_funcs` reports `sub_4F32E0` at `0x004f32e0`, size `0xd5`, giving exact range `0x004f32e0-0x004f33b5`.
- 2026-06-12 live IDA MCP `analyze_function 0x004f32e0` decompiles old/new page-count calculation, allocation/reallocation through `sub_516050`/`sub_516190`, tail shift through `sub_516240`, count increment at `+0x0c`, and inserted-byte copy through `sub_516220`.
- Current code caller is [UID:0002T0][0x004cf2d0-0x004cf2f6.IconsPaneShowReducedIcons](by-memory/0x004cf2d0-0x004cf2f6.IconsPaneShowReducedIcons.md) `HierListInsertAt` at `0x004ceb70`; vtable/data refs include `0x0061312c`, `0x0061ce40`, and `0x0061ce6c`.
- The next modeled method begins at `0x004f33c0`; [UID:0000VN][-ignored](by-memory/-ignored.md) tracks `0x004f33b5-0x004f33c0` as `0xcc` alignment.

## Touched State And Calls

| Item | Evidence |
| --- | --- |
| `this + 0x04` | element size for allocation, tail move, and inserted copy. |
| `this + 0x08` | page size for grow threshold. |
| `this + 0x0c` | old count read; incremented by insert count. |
| `this + 0x10` | backing storage may be allocated/reallocated and is used for all byte moves. |
| `sub_516050` / `sub_516190` | allocate/reallocate backing storage when page count grows. |
| `sub_516240` | moves the tail upward to open the insertion gap. |
| `sub_516220` | copies caller-provided records into the gap. |

## Source-Facing Context

- 2026-07-01 B009 accepted implementation: this exact child now emits formal source as `void List::InsertAt(int index, int count, void *source)` through [UID:000079][List](by-class/List.md) to [UID:0000KS][List](by-file/List.md).
- Current MCP call id `50` decompiled the old/new page-count calculations, MemoryMan allocation/reallocation, tail shift, `m_count` increment, and caller-byte copy. Call id `42` showed `GetMemoryMan`, `AllocateBufferMemory`, `ReallocateBufferMemory`, `MoveBufferMemory`, and `MemmoveWrapper`. Call id `41` showed vtable/data refs at `0x0061312c`, `0x0061ce40`, and `0x0061ce6c`, plus the local caller at `0x004ceb70`. Call id `44` confirmed `0x004f33b5-0x004f33bf` is 11 bytes of `0xcc` alignment before `List::Append`.
- `InsertRange` remains the child filename/search alias because the method can insert multiple records, but the source-facing virtual API name is `InsertAt`.
- The decompiler's carried helper return value is rejected as an artifact. The class vtable slot, wrapper callers, and source body use `void`.

## Confidence And Open Questions

- Confirmed: exact range, virtual slot, storage growth, tail shift, count update, copy behavior, helper names, source-facing `InsertAt` spelling, and `void` return.
- Remaining caveat: parameter names are inferred source-facing names. Record payload types remain caller-specific because `List` is a fixed-width byte container.

## Changes

- 2026-07-01 Agent-B009 accepted implementation callback:
  - Raised score to `COMPLETION:90`, `CONFIDENCE:92` and inserted formal `void List::InsertAt(int index, int count, void *source)` C++.
  - Incorporated current MCP evidence for allocation/reallocation, old/new page counts, tail movement, inserted-byte copy, vtable/data/code xrefs, padding, and the rejected decompiler return-value artifact.
- 2026-06-12 Agent-A001: Split from [UID:00022S][0x004f3140-0x004f35fd.ListCoreMethods](by-memory/0x004f3140-0x004f35fd.ListCoreMethods.md) with live IDA MCP boundary, decompilation, caller/vtable xref, storage-operation, and padding evidence.
