*** UID:000214 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000P9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000P9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static const char kOldWebBoardUrlReservedChars[] = "$&+,/:=?@";
static const char kOldWebBoardUrlUnsafeChars[] = " \"<>#%{}|\\^~[]`";

static char *__cdecl EscapeOldWebBoardUrl(const char *source,
                                          short sourceLength,
                                          char *dest,
                                          short destCapacity)
{
    if (!s_oldWebBoardUrlEscapeInitialized) {
        for (int ch = 0; ch < 256; ++ch) {
            s_oldWebBoardUrlEscapeTable[ch] =
                (ch < 0x20 || ch >= 0x80) ? 1 : 0;
        }

        for (int i = 0; i < 9; ++i) {
            const unsigned char ch =
                static_cast<unsigned char>(kOldWebBoardUrlReservedChars[i]);
            s_oldWebBoardUrlEscapeTable[ch] = 1;
        }

        for (int i = 0; i < 15; ++i) {
            const unsigned char ch =
                static_cast<unsigned char>(kOldWebBoardUrlUnsafeChars[i]);
            s_oldWebBoardUrlEscapeTable[ch] = 1;
        }

        s_oldWebBoardUrlEscapeInitialized = true;
    }

    short sourceIndex = 0;
    short produced = 0;

    while (sourceIndex < sourceLength && produced < destCapacity - 3) {
        const unsigned char ch =
            static_cast<unsigned char>(source[sourceIndex]);

        if (s_oldWebBoardUrlEscapeTable[ch] == 1) {
            sprintf_s(dest + produced,
                      destCapacity - produced,
                      "%%%2X",
                      static_cast<unsigned int>(ch));
            produced += 3;
        } else {
            dest[produced++] = static_cast<char>(ch);
        }

        ++sourceIndex;
    }

    dest[produced] = '\0';
    return dest;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready file-local old WebBoard URL escape helper at position 2: exact old literals, 94-instruction lazy-table and signed-short bounded encode behavior, `%%%2X` formatting, direct copy, null termination, destination-pointer return, retained no-direct-caller clone disposition, and state declarations emitted once by UID0002AH at position 1. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046ee80-0x0046efda WebBoardDialogOld URL Escape Helper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw file-local helper
- Source-facing helper: file-local `EscapeOldWebBoardUrl`
- Current materialization: old literals and complete first-draft helper C++ emitted through [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) at position `2`; old state declarations are emitted once by UID0002AH at position `1`; binary liveness is a resolved retained no-direct-caller helper disposition
- Class context: [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md), but no `this` pointer is present and this is not modeled as a `WebBoardDialogOld::` member function
- Likely source module: [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- Rebuild handling: source-authored URL escaping helper; surrounding `0xcc` padding is compiler/linker output
- Confidence: very strong for byte boundaries, raw instruction behavior, argument roles, old table state, WebBoardDialog source ownership, declaration order, and no-direct-caller route evidence; strong rather than original-symbol proof only for exact source spelling.
- Reconstruction status: reconstructable under [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md), with complete C++ at position `2`. The absent direct caller/function object remains a confidence limiter, not a code or placement blocker.

## Covered Range

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x0046ee80-0x0046efda` | `EscapeOldWebBoardUrl` | File-local old URL percent-escape helper. Lazily initializes the old URL escape table at `0x0067acb0/0x0067acb8`, then copies or percent-encodes source bytes into a bounded destination buffer using `sub_443a00` and format string `%%%2X`. |

## IDA MCP Evidence

Checked on 2026-06-05 with live IDA MCP and raw IDAPython decoding:

- `lookup_funcs` finds no modeled function at `0x0046ee7a`, `0x0046ee80`, or `0x0046efda`; the next modeled function is `sub_46EFE0` at `0x0046efe0-0x0046efec`.
- The bytes `0x0046ee7a-0x0046ee80` and `0x0046efda-0x0046efe0` are six-byte `0xcc` alignment pads, confirming the raw helper's exclusive span.
- Raw decoding from `0x0046ee80-0x0046efda` yields 94 instructions: standard prologue, lazy table initialization, encode loop, destination null termination, `retn`, and a trailing range-check-failure call.
- Direct xrefs to the raw entry `0x0046ee80` and tail `0x0046efda` are empty. Direct data refs inside the raw body include `byte_67ACB0` at `0x0046ee84` and `0x0046ef54`, `byte_67ACB8` table refs through the initializer and encode loop, and character-set refs at `0x0046eebd` and `0x0046ef40`.
- The initializer clears escape marks for bytes in `0x20-0x7f`, marks all others escaped, then marks the old URL character sets from `0x0066d44c-0x0066d454` (`24 26 2b 2c 2f 3a 3d 3f 40`) and `0x0066d458-0x0066d466` (`20 22 3c 3e 23 25 7b 7d 7c 5c 5e 7e 5b 5d 60`).
- The encode loop uses `[ebp+8]` as the source pointer, `[ebp+0ch]` as a 16-bit source length, `[ebp+10h]` as the destination pointer, and `[ebp+14h]` as a 16-bit destination capacity. It stops before `capacity - 3`, preserving room for a worst-case percent escape.
- Escaped bytes call `sub_443A00` at `0x0046efa0` with destination cursor, remaining capacity, format string `a2x` at `0x00613adc` (`%%%2X`), and the byte value; unescaped bytes are copied directly. The produced count advances by `3` for encoded bytes and `1` for copied bytes.
- The helper null-terminates `dest[produced]`, returns the original destination pointer in `eax`, and has only the compiler range-check-failure call at `0x0046efd5` outside the normal return path.
- The state addresses are the old variant's `0x0067acb0+` escape table, distinct from the active variant's `0x0067aba8+` escape state.

## Behavior Notes

- The helper lazily initializes the legacy URL escape table when `byte_67acb0` is clear.
- It seeds the 256-byte table at `byte_67acb8` by escaping bytes outside printable ASCII and the old web-board character sets around `0x0066d44c-0x0066d458`.
- It scans a caller-provided byte source and writes either the byte itself or a percent-encoded form into a bounded destination buffer, stopping before the last three capacity bytes so the next encoded byte cannot overflow the buffer.
- It uses the `%%%2X` format string for encoded bytes and null-terminates the destination buffer.
- Normal return value is the destination pointer.
- By-memory reconstruction keeps the active and old helpers separate because each emitted body hard-codes its own guard/table state and character-set pair. A future source cleanup could factor the algorithm only if the separate active/old table state and escape character lists remain explicit.

## Source-Quality Implementation

The source-facing helper name is `EscapeOldWebBoardUrl`. This name is preferred over raw `sub_46EE80` and over `WebBoardDialogOld::EscapeBoardInfo` because the body has no `this` pointer, accepts a generic source byte span/destination buffer, and only proves URL percent-escape behavior. It is paired with the active helper candidate `EscapeWebBoardUrl` at [UID:0003V1][0x0046da70-0x0046dbca.WebBoardDialogUrlEscapeHelper](by-memory/0x0046da70-0x0046dbca.WebBoardDialogUrlEscapeHelper.md), but the two helpers must stay separate unless the active and old guard/table/charset pairs remain distinct.

Recommended signature now implemented:

```cpp
static char *__cdecl EscapeOldWebBoardUrl(const char *source,
                                          short sourceLength,
                                          char *dest,
                                          short destCapacity);
```

The two old character-set declarations remain local to this helper page because UID000278 is the exact initialized-data evidence page and must not duplicate either active or old literal definitions. The two mutable old-state declarations are emitted once by [UID:0002AH][0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState](by-memory/0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState.md) at position `1`; this position-`2` page therefore begins with the old literals and retains the complete helper body without duplicate state.

Raw liveness is now a documented negative-route fact rather than an unresolved blocker. Current IDA/Ghidra evidence and local PE scans find no modeled IDA function object, no Ghidra callers, no direct caller, no rel32 call/jump, and no pointer/control-flow reference to `0x0046ee80` or `0x0046efda`; the old response parser at `0x0046eaa0-0x0046ee7a` contains matching inline percent-escape logic rather than a direct call. Treat this as a retained file-local out-of-line clone/source helper whose live old response path is currently inlined or duplicated. This caveat limits confidence to `90`, but it does not block first-draft C++ because the bounded helper body, argument roles, state, charsets, owner route, and padding boundaries are complete.

## Active And Old Split

| Variant | Helper | Guard | Table | Character sets |
| --- | --- | --- | --- | --- |
| active | `0x0046da70-0x0046dbca` / `EscapeWebBoardUrl` | `0x0067aba8` / `s_webBoardUrlEscapeInitialized` | `0x0067abb0` / `s_webBoardUrlEscapeTable` | `0x0066d430` / `kWebBoardUrlReservedChars`, `0x0066d43c` / `kWebBoardUrlUnsafeChars` |
| old | `0x0046ee80-0x0046efda` / `EscapeOldWebBoardUrl` | `0x0067acb0` / `s_oldWebBoardUrlEscapeInitialized` | `0x0067acb8` / `s_oldWebBoardUrlEscapeTable` | `0x0066d44c` / `kOldWebBoardUrlReservedChars`, `0x0066d458` / `kOldWebBoardUrlUnsafeChars` |

The active and old state ranges are BSS/virtual `.data` zero-initialized in the current PE mapping, then lazily filled with `1` for bytes that must be escaped. Do not retain stale `0xff` initialization wording for either state page.

## Ownership Decision

Keep this helper emitted by [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md), with [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md) as old-class context rather than a member-function owner. The helper immediately follows the old response parser, uses the old escape table state at `0x0067acb0+`, and mirrors the old response parser's `boardinfo` cookie encoding behavior. Do not merge it into active [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md) without preserving the distinct active/old table globals.

Rejected owner alternatives:

- [UID:0000HV][Browser](by-file/Browser.md): browser terminology and embedded browser infrastructure are dependencies, but active/old WebBoard escape state and helpers are WebBoardDialog source.
- [UID:0000HT][BoardDialogs](by-file/BoardDialogs.md): too broad; [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) is the precise WebBoard source root.
- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md): generated-output pollution only; the old vtable/singleton/resources and URL escape state belong to WebBoardDialogOld/WebBoardDialog.
- support-data-only ownership: rejected because this is executable URL escape logic, not just character-set data.

## Reconstruction Evidence

| Evidence point | Current documentation support | Reconstruction impact |
| --- | --- | --- |
| File parent | [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) has projected path `NexusTK/ui/dialogs/` and includes this raw old URL escape helper in the old-variant row. | Attach to `ui/dialogs/WebBoardDialog.cpp`. |
| Old class context | [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md) lists this old URL escape helper and now treats it as file-local helper context rather than a `WebBoardDialogOld::` member. | Treat this as legacy web-board code, not active-dialog or generic browser code. |
| Response adjacency | [UID:0000ZD][0x0046eaa0-0x0046ee7a.WebBoardDialogOldHandleBoardResponse](by-memory/0x0046eaa0-0x0046ee7a.WebBoardDialogOldHandleBoardResponse.md) documents the preceding old parser and its cookie/boardinfo flow. | Explains why the escape helper sits immediately after the old response parser. |
| Table state | [UID:0002AH][0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState](by-memory/0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState.md) owns the old init flag/table and lists this helper as a user. | Confirms the helper's state dependency and keeps it distinct from active table `0x0067aba8+`. |
| Boundary padding | [UID:0000VN][-ignored](by-memory/-ignored.md) records `0xcc` padding at `0x0046ee7a-0x0046ee80` and `0x0046efda-0x0046efe0`. | Confirms the raw helper is a bounded standalone body between old response and AutoInit cleanup. |
| Live raw decode | 2026-06-05 IDA MCP/IDAPython decoding proves 94 instructions, stack argument roles, old table initialization, `sub_443A00` percent-format call, bounded output cursor, null terminator, and return value. | Supports first-draft helper C++ while preserving a confidence cap because the current binary has no direct caller route to the retained clone. |

## Reconstruction Notes

Emit the first-draft file-local helper as `EscapeOldWebBoardUrl` at WebBoardDialog.cpp position `2`, after the UID0002AH declaration pair at position `1` and before blank-position old parser methods. Preserve the retained no-direct-caller caveat and do not promote it to a `WebBoardDialogOld::` method without new `this`/caller evidence. The implemented C++ preserves the old state/table split, signed 16-bit length/capacity behavior, and legacy `%%%2X` format string; do not rewrite it to `%02X`.

## Open Questions

- The current binary has no direct dispatch path to the raw helper after IDA/Ghidra/local PE route checks. Future evidence could still explain why the original source retained this out-of-line clone, but current reconstruction should treat the no-direct-caller state as resolved liveness evidence rather than a blocker.
- A future human source cleanup could factor active and old URL escaping behind a shared parameterized routine, but this by-memory target represents a distinct emitted old helper with hard-coded old state/charset references. Keep active and old helpers separate in reconstruction unless later source evidence proves an original shared helper.
- Source placement is closed: UID000278 remains a nonduplicating covered-literal marker, UID0002AH emits the mutable state at position `1`, and this page emits the two old literals plus helper at position `2`. No header, `extern`, duplicate declaration body, or later placement decision is required.

## Cross-References

- [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md)
- [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- [UID:0000ZD][0x0046eaa0-0x0046ee7a.WebBoardDialogOldHandleBoardResponse](by-memory/0x0046eaa0-0x0046ee7a.WebBoardDialogOldHandleBoardResponse.md)
- [UID:0000Z9][0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse](by-memory/0x0046d580-0x0046da63.WebBoardDialogHandleBoardResponse.md)
- [UID:000210][0x0046da70-0x0046e294.WebBoardDialogRenderEscapeAndOldTeardownHelpers](by-memory/0x0046da70-0x0046e294.WebBoardDialogRenderEscapeAndOldTeardownHelpers.md)
- [UID:0000G2][WebBoardDialog](by-class/WebBoardDialog.md)
- [UID:0002AH][0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState](by-memory/0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-21 B002 UID0002AH implementation callback:
  - Raised the helper from `88/90` to `90/92`, retained owner/emitter UID0000P9 and `Nested:0`, and set position `2` so the complete file-local definition precedes old parser use.
  - Removed only duplicate `s_oldWebBoardUrlEscapeInitialized` and `s_oldWebBoardUrlEscapeTable` declarations; UID0002AH now emits them once at position `1`. Preserved both old literal arrays and the accepted helper body byte-for-byte.
  - Preserved exact padding/range, 94-instruction behavior, lazy initialization, old state/charset refs, signed-short bounds, `%%%2X`, direct-copy branch, null terminator, return value, range-failure tail, retained no-direct-caller clone evidence, active/old separation, and rejected member/shared/header alternatives.

- 2026-06-20 B004 accepted source-quality implementation:
  - Before: `COMPLETION:84`, `CONFIDENCE:88`, no formal C++, raw helper wording, and open/no-code text that treated missing dispatch as a blocker.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`, first-draft file-local C++ for `EscapeOldWebBoardUrl`, and source-quality notes for the active/old URL escape split, local support declarations, exact range/padding, owner alternatives, and retained no-direct-caller caveat.
  - Summary/evidence: B004 report acceptance revalidated the `0x0046ee80-0x0046efda` range, six-byte `CC` padding before/after, old guard/table state at `0x0067acb0/0x0067acb8`, old duplicated character sets at `0x0066d44c/0x0066d458`, `%%%2X` percent-escape behavior, no direct caller/pointer evidence, and [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md) as the correct owner/emitter. Support declarations are modeled locally here for this helper-first pass; [UID:000278][0x0066d430-0x0066d468.BrowserUrlEscapeCharacterSets](by-memory/0x0066d430-0x0066d468.BrowserUrlEscapeCharacterSets.md) and [UID:0002AH][0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState](by-memory/0x0067acb0-0x0067adb8.WebBoardDialogOldUrlEscapeState.md) remain formally blank to avoid duplicate generated definitions.
- 2026-06-21 B006 Rule 26 stale-report audit:
  - Rephrased the route caveat from unresolved dispatch to documented retained no-direct-caller liveness after comparing the current page with B006's IDA/Ghidra/local PE negative-route evidence.
  - Summary/evidence: the helper remains `88/90`, source-authored, and first-draft C++ ready; the old parser's inline clone and absence of rel32/pointer/function-object routes cap confidence but no longer justify leaving source-quality naming, state, charset, or C++ work unresolved.
- 2026-06-02 documentation rescore:
  - What existed before: `COMPLETION:55`, `CONFIDENCE:65`, reconstructable blank, no autogen parent, and a candidate C++ scaffold.
  - Changed to: `COMPLETION:74`, `CONFIDENCE:78`, `RECONSTRUCTABLE:TRUE`, and `AUTOGEN_PARENT_UID:0000P9`.
  - Summary/evidence: existing IDA-backed docs confirmed raw helper boundaries, old URL escape state, response adjacency, WebBoardDialogOld ownership, parent source path, and padding. At that time C++ stayed blank because the raw start had no current function object/direct xrefs and final source name/signature were unresolved; the 2026-06-20 B004 implementation supersedes that no-code decision with first-draft file-local helper C++.
- 2026-06-05 live IDA raw decode:
  - Changed from: `COMPLETION:74`, `CONFIDENCE:78`.
  - Changed to: `COMPLETION:84`, `CONFIDENCE:88`.
  - Reason for score increase: live IDA MCP confirmed no modeled function at the raw entry, exact padding boundaries, absence of direct entry/tail xrefs, the 94-instruction body, stack argument roles, exact old character-set bytes, `byte_67ACB0`/`byte_67ACB8` initialization and use, the `sub_443A00(..., "%%%2X", byte)` percent-escape path, direct copy path, bounded output counter, null termination, and destination-pointer return. The later B004/B006 source-quality passes supersede the old final-name blocker with high-probability `EscapeOldWebBoardUrl`; the remaining cap is exact original spelling/declaration placement and the retained no-direct-caller clone state.
