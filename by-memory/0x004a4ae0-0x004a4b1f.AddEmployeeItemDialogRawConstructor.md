*** UID:000139 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:85 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000006 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw constructor bytes through `0x004a4b20`, absent function/xrefs at `0x004a4ae0`, base constructor `0x004ae4c0`, owner store `+0x270`, and vtable-store pairing with inline construction path `0x004a339f` verified. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004a4ae0-0x004a4b1f - AddEmployeeItemDialog Raw Constructor

## Status

- Disposition: reconstructable raw/projected NexusTK code, but not an IDA function object.
- Current generated owner label: `AddEmployeeItemDialog::AddEmployeeItemDialog`
- Likely source module: [UID:0000J0][EmployeeDialogPane](by-file/EmployeeDialogPane.md), with base picker construction in [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md)
- Related class: [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md)
- Confidence: very strong for raw constructor-shaped bytes, class-state effects, and the negative reachability evidence; source placement remains the limiting issue.
- Rebuild handling: retained constructor-shaped source evidence for layout/vtable/owner-field validation, but non-emitting under current evidence.
- Final C++: intentionally blank. B004 2026-06-18 local PE dword/rel32 scans and prior IDA no-function/no-xref evidence support excluding standalone constructor source unless a future caller/table route is found.

## Observed Bytes

IDA disassembly at `0x004a4ae0` shows a complete constructor-shaped sequence:

| Address | Operation |
| --- | --- |
| `0x004a4ae0` | Prologue and `this` save. |
| `0x004a4ae5-0x004a4aee` | Pushes two zero arguments and calls base `AddItemDialog` constructor `0x004ae4c0`. |
| `0x004a4af3-0x004a4af6` | Stores caller argument at `this + 0x270`; this is the owning `EmployeeDialogPane` pointer used by `OnButtonClick`. |
| `0x004a4afe-0x004a4b0e` | Installs `AddEmployeeItemDialog` primary, secondary, and tertiary vtable pointers at `+0x0`, `+0xa0`, and `+0xa4`. |
| `0x004a4b18-0x004a4b1e` | Returns with `retn 4`. |

## Evidence

- 2026-05-26 IDA MCP `lookup_funcs` reports `0x004a4ae0` as `Not a function`.
- 2026-05-26 IDA MCP `xrefs_to 0x004a4ae0` reports no xrefs.
- 2026-05-26 IDA MCP disassembly shows the raw constructor-shaped bytes above and records a direct call to `AddItemDialog` constructor `0x004ae4c0`.
- `xrefs_to 0x004ae4c0` includes a code reference from `0x004a4aee` inside this raw body, plus the live `EmployeeDialogPane::OnButtonAction` construction path at `0x004a339f`.
- 2026-05-26 layout recheck shows the live [UID:0000TT][AddEmployeeItemDialogInlineConstruction_004A339F](by-item/AddEmployeeItemDialogInlineConstruction_004A339F.md) allocates `0x274` bytes, patches the same three vtables, and stores the same owner pointer at `+0x270`.
- 2026-05-27 IDA recheck still reports `0x004a4ae0` as `Not a function`, with no direct xrefs; a raw-pointer scan across loaded segments also found no dword equal to `0x004a4ae0`.
- 2026-06-01 IDA MCP recheck again reports `0x004a4ae0` as `Not a function` with no xrefs. The same pass confirms vtable-store xrefs to `0x006190d4`, `0x00619134`, and `0x00619164` from both the inline live construction path at `0x004a33a4`/`0x004a33aa`/`0x004a33b4` and the raw constructor-shaped bytes at `0x004a4afe`/`0x004a4b04`/`0x004a4b0e`.

2026-06-06 live IDA refresh:

- `lookup_funcs` reports `0x004a4adb`, `0x004a4ae0`, and `0x004a4b1f` as `Not a function`; the next modeled function is `0x004a4b20` (`sub_4A4B20`, size `0x191`), and the base constructor target is `0x004ae4c0` (`sub_4AE4C0`, size `0x435`).
- `xrefs_to 0x004a4ae0` is empty, and a current pointer scan across loaded segments found no little-endian dword equal to `0x004a4ae0`.
- `xrefs_to 0x004ae4c0` includes the raw-body call at `0x004a4aee` with no owning function plus the live inline construction call at `0x004a339f` inside `EmployeeDialogPane::OnButtonAction`.
- Vtable xrefs still pair the raw body with the live inline constructor path: `0x006190d4` from `0x004a33a4` and `0x004a4afe`, `0x00619134` from `0x004a33aa` and `0x004a4b04`, and `0x00619164` from `0x004a33b4` and `0x004a4b0e`.
- Byte review of `0x004a4ae0-0x004a4b20` confirms the exact prologue, two zero arguments to `0x004ae4c0`, owner pointer store at `+0x270`, three vtable stores, `retn 4`, and one `0xcc` alignment byte before `0x004a4b20`.

2026-06-15 A001 live IDA MCP source-placement pass:

- Session/database: `c001_midiplayer_rdata_20260615`, module `NexusTK.exe`, input path `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, strings cache ready.
- `lookup_funcs` reports `0x004a4adb`, `0x004a4ae0`, and `0x004a4b1f` as `Not a function`; the next modeled function remains [UID:00013A][0x004a4b20-0x004a4d3b.AddEmployeeItemDialog](by-memory/0x004a4b20-0x004a4d3b.AddEmployeeItemDialog.md) starting at `0x004a4b20`, and the base constructor target remains `0x004ae4c0`, size `0x435`.
- `xrefs_to 0x004a4ae0` is empty. `find_bytes` found no absolute VA bytes `E0 4A 4A 00` and no RVA bytes `E0 4A 0A 00` for the raw start. The same scan also found no absolute/RVA pointer bytes for the nearby clan raw start `0x0048a0c0`, confirming this was a targeted no-pointer pattern check rather than relying only on function lookup.
- `xrefs_to 0x004ae4c0` still shows the raw-body base-constructor call at `0x004a4aee` with no containing function, plus the live inline construction call at `0x004a339f` inside [UID:0003KY][0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction](by-memory/0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction.md).
- Vtable xrefs still pair raw and live construction stores: `0x006190d4` from `0x004a33a4` and `0x004a4afe`, `0x00619134` from `0x004a33aa` and `0x004a4b04`, and `0x00619164` from `0x004a33b4` and `0x004a4b0e`. The raw stores have no containing function; the live stores are inside `0x004a3240`.
- `get_bytes 0x004a4adb size 0x45` confirms five `0xcc` bytes before the raw prologue, then the exact constructor-shaped body: prologue, two zero pushes, call to `0x004ae4c0`, caller argument store to `this+0x270` / 624, primary/secondary/tertiary vtable stores, `retn 4`, and one `0xcc` byte before `0x004a4b20` (offset conversions Verified with `int_convert.py`).
- Fresh decompilation of `0x004a3240` confirms the runtime construction route remains inline: allocate `0x274` / 628 bytes, call `0x004ae4c0(0, 0)` at `0x004a339f`, store the same three `AddEmployeeItemDialog` vtables, and write the current `EmployeeDialogPane*` into object slot `+0x270` / 624 (Verified with `int_convert.py`).

## Reconstruction Notes

Keep this as raw/projected constructor evidence until function-boundary and source-placement metadata is reconciled. The live source can still reconstruct `AddEmployeeItemDialog` construction by modeling the base `AddItemDialog` initialization, owner pointer at `+0x270`, and vtable installation, but this range should not be treated as an IDA-confirmed callable function start.

The 2026-06-15 A001 investigation does not justify reclassifying the raw island to ignored. The bytes are coherent `AddEmployeeItemDialog` constructor-shaped source semantics and install the same class vtables as the live inline route. The issue is whether the final source tree should contain a standalone out-of-line constructor body for this retained byte island, or whether the final reconstruction should represent only the inline construction path that is actually referenced. That question requires original-source/source-placement inference beyond this A-agent pass.

2026-06-18 B004 no-code proof resolves the current emission decision: local PE dword scans found no pointer-table, vtable, or callback hit to `0x004a4ae0`, and rel32 call/jump/jcc scans found no route to `0x004a4ae0`. Positive controls found the expected routes to `0x004ae4c0`, `0x004a4770`, `0x005975e0`, `0x0049eb90`, and `0x0049dad0`. The raw bytes are constructor-shaped and remain valuable layout evidence, but source emission stays blank/non-emitting unless a future route is found. The live construction behavior is represented by [UID:0000TT][AddEmployeeItemDialogInlineConstruction_004A339F](by-item/AddEmployeeItemDialogInlineConstruction_004A339F.md) around `0x004a339f`.

## B001 Build-Metadata And Source-Placement Audit

B001's 2026-06-15 source-placement audit checked broader PE/build metadata and source-shape heuristics after the A001 raw-start pass. It found no target/support change was needed at that time and recommended keeping the page class-owned, reconstructable, and blank-C++ while treating `0x004a4ae0-0x004a4b1f` as retained out-of-line constructor-shaped evidence whose only proven runtime construction path is the inline sequence inside [UID:0003KY][0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction](by-memory/0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction.md). The later B004 pass superseded only B001's older `EMITTER_UIDS:000006` recommendation by blanking `EMITTER_UIDS`; the B001 class-ownership, reconstructable-evidence, no-standalone-C++, and no-IDA-function conclusions remain valid.

Fresh local PE checks in that B001 pass used `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (`md5=4247e04e20b65d6414c7238aa8ff5515`, image base `0x400000`) and confirmed only `.text`, `.rdata`, `.data`, and `.rsrc` sections. The COFF symbol table is absent (`NumberOfSymbols == 0`, symbol pointer `0x0`). The PE debug directory has one type-13 entry at RVA `0x240270`, size `0x390`, whose payload begins with section/subsection names such as `.text`, `.text$di`, and `.text$mn`; it does not identify functions or source files. Marker scans found no `RSDS`, `NB10`, `.pdb`, `.PDB`, `.cpp`, `.CPP`, `.obj`, or `.OBJ` strings. RTTI type-name strings for `EmployeeDialogPane` and `AddEmployeeItemDialog` exist in `.rdata`, but they prove type identity rather than original source-file placement.

Raw PE route and body-shape checks from the same audit:

| Check | Result |
| --- | --- |
| Exact raw body `0x004a4ae0-0x004a4b1f` | One occurrence, at `0x004a4ae0` only. |
| Little-endian VA hits for `0x004a4adb`, `0x004a4ae0`, and `0x004a4b1f` | Zero. |
| Little-endian RVA hits for `0x000a4adb`, `0x000a4ae0`, and `0x000a4b1f` | Zero. |
| Little-endian pointer values to any address inside `0x004a4ae0-0x004a4b1f` | Zero. |
| `.text` relative `E8`/`E9`/near conditional branches to `0x004a4ae0` | Zero. |
| `.text` relative branches to any address inside `0x004a4ae0-0x004a4b1f` | Zero. |
| Relative calls to base constructor `0x004ae4c0` | `0x004a339f`, `0x004a4aee`, `0x004ad124`, `0x004af924`, and `0x004b0031`. |
| Exact primary vtable store patterns | One inline at `0x004a33a4`, one raw at `0x004a4afe`. |
| Exact secondary vtable store patterns | One inline at `0x004a33aa`, one raw at `0x004a4b04`. |
| Exact tertiary vtable store patterns | One inline at `0x004a33b4`, one raw at `0x004a4b0e`. |
| Exact inline owner store pattern `[edi+0x270]` | One hit at `0x004a33be`. |
| Exact raw base-call relative encoding | One hit at `0x004a4aee`. |
| Exact inline base-call relative encoding | One hit at `0x004a339f`. |

The best build-level explanation remains a retained out-of-line constructor body whose visible construction site was inlined into `EmployeeDialogPane::OnButtonAction`. That explains why the raw body and live inline path share the same base-constructor call, three AddEmployee vtable installs, and `+0x270` owner store while no call/pointer/table route to `0x004a4ae0` exists. The exact compiler/linker cause cannot be resolved from the linked PE because there is no matching PDB, linker map, object file, function-section map, COFF symbol data, compiler/linker flag record, alternate build, or runtime trace entering `0x004a4ae0`.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `85` | The page now records current IDA boundary checks, no-xref/no-pointer-scan evidence, exact raw bytes, base-constructor call, owner-pointer store, vtable stores, the matching inline construction path, and an explicit source-placement decision/handoff. |
| Confidence `90` | Raw-byte semantics and class ownership are very strong because both the raw body and live inline path install the same `AddEmployeeItemDialog` vtables and owner pointer. Confidence remains below final-audit range because no IDA function object or inbound reference proves this body is a callable out-of-line constructor. |

## B-Agent Handoff Question

Resolved current decision: using broader original-source/build heuristics, the retained but unreferenced raw constructor-shaped bytes at `0x004a4ae0-0x004a4b1f` should be excluded from standalone source emission while preserving the documentation evidence. B004's local route scans and positive controls support treating the island as retained constructor-shaped duplicate/orphan evidence. This decision is reviewable only if future evidence finds a caller, table route, or original source layout proof.

Evidence already checked by A001: live IDA reports no function object at the raw start/end, no xrefs to `0x004a4ae0`, no absolute VA/RVA pointer-byte hits for the raw start, exact constructor-shaped bytes with `retn 4`, base-constructor xref at `0x004a4aee` with no containing function, three raw vtable-store xrefs with no containing function, live inline construction at `0x004a339f` inside `EmployeeDialogPane::OnButtonAction`, matching vtable stores at `0x004a33a4`/`0x004a33aa`/`0x004a33b4`, and matching owner store at `+0x270` / 624 (Verified with `int_convert.py`). Binary evidence alone does not prove original source placement for the unreferenced raw island.

## Changes

- 2026-06-15 A001 low-completion/source-placement refresh:
  - What existed before: metadata was `80/88`; the page documented the raw bytes and no-xref caveat but still needed a direct investigation of whether the raw constructor issue was only a final-C++ blocker.
  - Changed to: metadata is `85/90`; owner/emitter remain [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md); final C++ remains blank pending the source-placement question recorded above.
  - Summary/evidence: live IDA MCP database `c001_midiplayer_rdata_20260615` reconfirmed no function at `0x004a4adb`/`0x004a4ae0`/`0x004a4b1f`, no xrefs to `0x004a4ae0`, no absolute VA/RVA byte-pattern hits for the raw start, exact raw bytes through the `retn 4` and successor `0xcc`, base-constructor xrefs from both raw and inline sites, vtable-store pairings from both raw and inline sites, and the runtime inline construction branch in [UID:0003KY][0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction](by-memory/0x004a3240-0x004a33d8.EmployeeDialogPaneOnButtonAction.md). The raw island remains reconstructable source-authored evidence but is not ready for standalone final C++ emission.
- 2026-06-18 B004 non-emitting closure:
  - What existed before: the page was `85/90` but still emitted through [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md) despite stating that standalone constructor C++ was not ready.
  - Changed to: owner remains [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md), reconstructable remains true, and `EMITTER_UIDS` is blank because the island is retained raw constructor-shaped evidence rather than a callable source body under current evidence.
  - Summary/evidence: B004 local PE scans found no dword or rel32 route to `0x004a4ae0`, while positive-control scans found expected nearby routes. The live source construction route remains [UID:0000TT][AddEmployeeItemDialogInlineConstruction_004A339F](by-item/AddEmployeeItemDialogInlineConstruction_004A339F.md) at `0x004a339f`.
- 2026-06-18 Rule 26 older-report incorporation for B001 `000139-addemployee-raw-constructor-source-placement`:
  - What existed before: the page preserved the no-function/no-xref raw-constructor decision, but did not carry B001's PE/debug-directory/source-marker scans, exact full-body uniqueness check, interior-pointer/branch negative checks, base-constructor call-site inventory, or explicit supersession of the older class-emitter recommendation.
  - Changed to: added the B001 build-metadata/source-placement audit section above. The target metadata remains unchanged: class owner [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md), reconstructable true, blank emitters, and blank final C++.
  - Summary/evidence: B001's local PE audit found no COFF symbols, no PDB/source/object markers, no source-file/function debug metadata, one exact raw-body occurrence, no VA/RVA/pointer/rel32 route to the raw start or interior, matching raw/inline vtable-store patterns, and the best current retained-out-of-line-constructor explanation with unresolved compiler/linker cause.
- What existed before: metadata still treated this as an unassigned `50/65` low-score page even though the page contained several IDA checks.
- Changed to: marked reconstructable, parented to [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md), and raised to `72/84`.
- Evidence: live 2026-06-01 IDA MCP recheck preserves the key caveat (`0x004a4ae0` is not a function and has no xrefs) while confirming the raw body's vtable-store relationship to the employee-specific class.
- 2026-06-06 A009 live IDA raw-byte refresh:
  - Changed to: `COMPLETION:80`, `CONFIDENCE:88`.
  - Summary/evidence: live IDA rechecked absent function objects at `0x004a4adb`/`0x004a4ae0`/`0x004a4b1f`, modeled next function `0x004a4b20`, base constructor `0x004ae4c0`, empty xrefs and pointer-scan results for `0x004a4ae0`, raw-body call at `0x004a4aee`, three vtable stores that mirror the inline construction path, and the exact `0xcc` boundary byte before `0x004a4b20`.

## Cross-References

- [UID:000006][AddEmployeeItemDialog](by-class/AddEmployeeItemDialog.md)
- [UID:0000J0][EmployeeDialogPane](by-file/EmployeeDialogPane.md)
- [UID:0000TT][AddEmployeeItemDialogInlineConstruction_004A339F](by-item/AddEmployeeItemDialogInlineConstruction_004A339F.md)
- [UID:00013A][0x004a4b20-0x004a4d3b.AddEmployeeItemDialog](by-memory/0x004a4b20-0x004a4d3b.AddEmployeeItemDialog.md)
- [UID:0002MH][0x006190d0-0x0061916c.AddEmployeeItemDialogVtableData](by-memory/0x006190d0-0x0061916c.AddEmployeeItemDialogVtableData.md)
- [UID:0001TN][AddEmployeeItemDialogLayout](by-type/by-struct/AddEmployeeItemDialogLayout.md)
- [UID:000138][0x004a1d70-0x004a4e6b.EmployeeDialogPanes](by-memory/0x004a1d70-0x004a4e6b.EmployeeDialogPanes.md)
- [UID:0001XH][EmployeeDialogPaneVtables](by-type/by-vtable/EmployeeDialogPaneVtables.md)
- [Wave3 data issues](../wave3_data_issues.md)
