*** UID:0002R6 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000058 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000058 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FlyingParcelPane::StartAnimation(unsigned char parcelCountOrState,
                                      unsigned char leftSlot,
                                      unsigned char rightSlot,
                                      bool isParcel)
{
    RectBounds animationRect;

    if (g_useEpfAssets == 1) {
        g_pSoundManager->PlaySample(200, 100);
        g_pScreenPane->SetLayerFrame(m_animationLayer, 0);

        m_leftSlot = leftSlot;
        m_rightSlot = rightSlot;
        m_parcelCountOrState = parcelCountOrState;
        m_isParcel = isParcel;
        m_animationFrame = 0;

        if (isParcel)
            SetRect(&animationRect, 630, 730, 659, 755);
        else
            SetRect(&animationRect, 667, 730, 698, 747);

        RemoveFromLayer();
        AddToLayer(&animationRect, 0, NULL, m_animationLayer);
        m_origin.y += 30;
        InvalidateRect(&m_bounds);
        m_timerHandler.ScheduleTimer(0, 100, 0, 0);
        return;
    }

    g_pScreenPane->SetLayerFrame(m_animationLayer, 0);

    m_leftSlot = leftSlot;
    m_rightSlot = rightSlot;
    m_parcelCountOrState = parcelCountOrState;
    m_isParcel = isParcel;
    m_animationFrame = 0;

    const int displaySlot = parcelCountOrState <= 4 ? parcelCountOrState : 4;
    SetRect(&animationRect,
            397 + displaySlot * 11,
            4,
            419 + displaySlot * 11,
            26);

    RemoveFromLayer();
    AddToLayer(&animationRect, 0, NULL, m_animationLayer);
    InvalidateRect(&m_bounds);
    m_timerHandler.ScheduleTimer(0, 10, 0, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Configures parcel/letter animation payload, rectangles, helper attachment, pane refresh, and 10/100 ms timer scheduling. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00546bc0-0x00546d64 FlyingParcelPaneStartAnimation

## B001 UID0002ZO Accepted Callback State - 2026-07-21

This source-authored method is complete at `92/93`, owned/emitted by UID000058 at position `40`. The managed C++03 body retains both exact asset modes, payload byte order, frame reset, ScreenPane layer frame, sample `200` at volume `100`, EPF and legacy rectangles, inherited `m_origin.y += 30`, remove/add ordering, invalidation, and `100/10` ms timer choice. `NULL` replaces only the source-era spelling; the raw no-direct-route result and inferred lexical names remain documented confidence caps rather than body blockers.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw class method / animation start and configure body.
- Parent class: [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md).
- Aggregate range: [UID:0001EL][0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart](by-memory/0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart.md).
- Likely source module: [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
- Rebuild handling: source-ready first-draft `FlyingParcelPane::StartAnimation(...)` method body with compiler security-cookie frame/epilogue regenerated by the compiler.
- Confidence: strong for exact bytes, branch behavior, touched fields, timer scheduling, callee set, source-facing helper/global names, and the method-level emitter route through [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md) to [UID:0000MF][ParcelPane](by-file/ParcelPane.md); capped below final audit because no direct caller, pointer, vtable slot, or original declaration route to the raw start survives.

## Address Range

`0x00546bc0-0x00546d64`, exclusive end. Current IDA does not model this start as a function and decompilation fails, but bounded disassembly shows a complete `__thiscall` body ending in `retn 0x10`. This raw-function status remains a confidence cap, not a no-code blocker: the range is exact, the body is source-shaped, and the surrounding FlyingParcelPane children now establish the same accepted class/file emitter route.

## Signature Notes

Observed calling shape is `thiscall` with four stack arguments and no meaningful return value. Best source-facing signature direction is `void FlyingParcelPane::StartAnimation(unsigned char parcelCountOrState, unsigned char leftSlot, unsigned char rightSlot, bool isParcel)`. `StartAnimation` is the preferred source-facing name because the body starts both parcel and letter presentations; `StartDeliveryAnimation` remains a descriptive historical alias only. The first argument is stored as `m_parcelCountOrState` at `this+0xf9`; the next two byte arguments are stored as `m_leftSlot` at `this+0xfb` and `m_rightSlot` at `this+0xfa`; the fourth flag is stored as `m_isParcel` at `this+0xfc` and selects parcel-versus-letter screen rectangles.

## Behavior

Starts a flying parcel/letter animation. It removes or detaches the current helper object from the layer/manager, stores the animation payload fields, builds the screen rectangle for either the old rendering mode or the normal mode, refreshes/positions the pane and owned helper, invalidates the pane rectangle, then schedules the tertiary timer handler at `this+0xa4`.

Two rendering-mode branches are present:

- When [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) is `1`, the method calls `SoundManager::PlaySample(g_pSoundManager, 200, 100)`, attaches/updates the owned layer through `ScreenPane::SetLayerFrame(g_pScreenPane, this+0x100, 0)`, stores the animation payload, chooses one of two hard-coded rectangle groups based on `m_isParcel`, increments inherited `Pane::m_origin.y` at `this+0xac` by `0x1e` / 30, invalidates `m_bounds`, and schedules a `100` ms timer.
- Otherwise, it attaches/updates the owned layer through the same `ScreenPane` helper, stores the same animation payload, builds a smaller rectangle from the first byte argument clamped to display slot `4`, invalidates `m_bounds`, and schedules a `10` ms timer.

## Rectangle And Timing Behavior

| Branch | Condition | Rectangle / delay |
| --- | --- | --- |
| current/EPF | `m_isParcel != 0` | `(630, 730, 659, 755)`, then `m_origin.y += 30`, `100` ms timer. |
| current/EPF | `m_isParcel == 0` | `(667, 730, 698, 747)`, then `m_origin.y += 30`, `100` ms timer. |
| legacy/non-EPF | `m_parcelCountOrState <= 4` | `left = 397 + count * 11`, `top = 4`, `right = 419 + count * 11`, `bottom = 26`, `10` ms timer. |
| legacy/non-EPF | `m_parcelCountOrState > 4` | Same as display slot `4`: `(441, 4, 463, 26)`, `10` ms timer. |

The legacy branch clamps only the display rectangle slot. It still stores the raw first argument in `m_parcelCountOrState` so [UID:0002KK][0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep](by-memory/0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep.md) can later forward the original payload to `ParcelPane::SetParcelSlotData`.

## Raw Code Evidence

IDA MCP bounded disassembly on 2026-06-01, refreshed on 2026-06-15:

| Address | Evidence |
| --- | --- |
| `0x00546bc0-0x00546bcd` | Standard frame setup plus `___security_cookie` storage. |
| `0x00546bd0` | Branches on `byte_66DA97 == 1`. |
| `0x00546be4-0x00546bf1` | Old/new mode setup call to `sub_57A5C0` with `200, 100`. |
| `0x00546bf6-0x00546c04` and `0x00546ca7-0x00546cb5` | Both branches call `sub_556CE0` with `dword_67A7CC` and the owned helper pointer at `+0x100`. |
| `0x00546c14-0x00546c36` | Stores payload bytes at `+0xfb`, `+0xfa`, `+0xf9`, `+0xfc`, and clears frame byte `+0xf8`. |
| `0x00546c67` and `0x00546d0c` | Calls `sub_4B7C50` to build the local rectangle. |
| `0x00546c73`, `0x00546c88`, `0x00546c9a` | Old/new mode path calls pane virtual slots `+0x38`, `+0x30`, and `+0x20`. |
| `0x00546d18`, `0x00546d2d`, `0x00546d38` | Normal mode path calls the same pane virtual slots. |
| `0x00546d47-0x00546d4d` | Schedules the tertiary handler at `this+0xa4` through `sub_5975E0`. |
| `0x00546d59-0x00546d61` | Security-cookie check and `retn 0x10`. |

2026-06-15 live IDA MCP `lookup_funcs` confirms `0x00546bc0` and the exclusive end `0x00546d64` are not modeled function starts, while the successor [UID:0002KK][0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep](by-memory/0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep.md) remains `sub_546D70` with size `0x7e`.

2026-06-15 live IDA MCP `disasm 0x00546bb5` reconfirms:

- `0x00546bb5-0x00546bc0` is alignment padding before the raw start.
- `0x00546bc0` starts with the security-cookie frame setup.
- `0x00546bd0` branches on `byte_66DA97`.
- `0x00546c14-0x00546c36` and `0x00546cba-0x00546cdb` store the payload bytes at `+0xf8..+0xfc`.
- `0x00546d47` schedules the tertiary handler at `this+0xa4` through `sub_5975E0`.
- `0x00546d61` returns with `retn 0x10`, followed by alignment before `0x00546d70`.

2026-06-15 live IDA MCP `xrefs_to` reports no direct references to `0x00546bc0`, matching IDA's raw-body treatment. The same refresh reconfirms the surrounding `FlyingParcelPane` vtable-store family at `0x00621ccc`, `0x00621d18`, and `0x00621d48` from the constructor, raw cleanup body, and scalar destructor.

2026-06-19 B012 local PE recheck found no modeled function at this raw start and no direct VA/RVA/rel32 pointer or branch edge into `0x00546bc0`. That negative route evidence remains a confidence cap and class-declaration caveat, but the accepted B007 2026-06-27 pass supersedes the old target-level no-code conclusion.

## 2026-06-27 B007 Accepted Source-Quality Recheck

B007 rechecked this target through live IDA MCP session `398b87c1` and a supplemental read-only PE scan before supervisor acceptance. MCP was available and responsive; this implementation is not fallback-only.

- MCP request id `3` `idb_list` reported active NexusTK IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, backend `worker`, PID `23700`, and `is_analyzing:false`.
- MCP request id `4` `server_health` reported module `NexusTK.exe`, input `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- MCP request id `10` `lookup_funcs` again found no function at `0x00546bb5`, `0x00546bc0`, `0x00546d64`, or `0x00546b80`; it confirmed successor [UID:0002KK][0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep](by-memory/0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep.md) as `sub_546D70`, size `0x7e`, and nearby modeled siblings at `0x00546ac0`, `0x00546df0`, `0x00556ce0`, `0x005975e0`, `0x0057a5c0`, and `0x004b7c50`.
- MCP request id `11` `xrefs_to` reported zero xrefs to `0x00546bc0` and zero xrefs to `0x00546d64`; it confirmed `0x00546d70` has the vtable data xref from `0x00621d4c` and reconfirmed the constructor/raw cleanup/scalar-wrapper vtable-store family at `0x00621ccc`, `0x00621d18`, and `0x00621d48`.
- MCP request id `12` `get_bytes` confirmed eleven `0xcc` bytes at `0x00546bb5-0x00546bc0`, the exact 420-byte body at `0x00546bc0-0x00546d64`, and twelve `0xcc` bytes at `0x00546d64-0x00546d70`.
- MCP request id `13` `insn_query` returned 126 ownerless instructions for `0x00546bc0-0x00546d64`, all `fn:null`, ending in `retn 10h` at `0x00546d61`.
- MCP request id `18` `decompile 0x00546bc0` returned `code:null` with expected decompilation failure at the raw start; request id `19` `callees` likewise reported no function for the raw start.
- MCP request id `20` confirmed vtable-slot and global-storage values: `0x00621ccc -> 0x00546ed0`, `0x00621d10 -> 0x00546df0`, `0x00621d18 -> 0x00546e8b`, `0x00621d48 -> 0x00546e96`, `0x00621d4c -> 0x00546d70`, zero-initialized global slots at `0x0067a7cc` and `0x0067a7d0`, and `0x0066da97` initialized to `1`.
- MCP request id `21` produced a unique full-range signature for `0x00546bc0-0x00546d64`.

The supplemental PE scan of `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` used imagebase `0x400000`, target VA `0x00546bc0`, and target RVA `0x00146bc0`; it found zero absolute VA dword hits, zero RVA dword hits, and zero direct executable `E8`/`E9` branches to the raw start. This proves the no-direct-route caveat remains real, but the caveat no longer blocks target-level source because the method body is exact, bounded, reconstructable, and routed through the accepted `FlyingParcelPane` class/file family.

## Callers And Callees

- Direct callers: none reported by current IDA MCP because the start is not modeled as a function. The body is a raw class method reachable from parcel notification control flow or generated metadata not modeled by this IDA function table.
- Direct callees: `0x0057a5c0`, `0x00556ce0`, `0x004b7c50`, pane virtual slots `+0x38`, `+0x30`, `+0x20`, `0x005975e0`, and `@__security_check_cookie@4`.

## Touched State

| Offset/global | Access | Meaning |
| --- | --- | --- |
| [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) | read | Asset/layout selector that chooses rectangle/timer behavior. |
| `g_pSoundManager` | read/call receiver | Sound manager used by `SoundManager::PlaySample` with sample `200`, volume `100`. |
| `g_pScreenPane` | read/call receiver | Screen/layer manager used by `ScreenPane::SetLayerFrame`. |
| `this+0xf8` | write zero | `m_animationFrame`. |
| `this+0xf9` | write arg byte | `m_parcelCountOrState`; consumed later by `AnimateStep`. |
| `this+0xfa` | write arg byte | `m_rightSlot`. |
| `this+0xfb` | write arg byte | `m_leftSlot`. |
| `this+0xfc` | write flag | `m_isParcel`, also used by the draw method. |
| `this+0x100` | read | Owned `Layer`/screen-layer entry pointer passed to `ScreenPane::SetLayerFrame` and pane virtual slot `+0x30`. |
| `this+0xac` | write/update | Inherited `Pane` origin/position state adjusted by the old asset path; not a derived `FlyingParcelPane` payload field. |
| `this+0xa4` | timer receiver | Tertiary handler scheduled through `sub_5975E0`. |

## Boundary Evidence

- `0x00546bb5-0x00546bc0` is eleven `0xcc` bytes after [UID:0002R5][0x00546b80-0x00546bb5.FlyingParcelPaneCleanupDestructorBody](by-memory/0x00546b80-0x00546bb5.FlyingParcelPaneCleanupDestructorBody.md).
- `0x00546d64-0x00546d70` is twelve `0xcc` bytes before [UID:0002KK][0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep](by-memory/0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep.md).

## Helper And Source-Name Decisions

- `byte_66DA97` is documented as [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md); the source intentionally compares it to `1`, matching the raw `cmp byte_66DA97, 1`.
- `dword_67A7CC` is [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md), used here through `ScreenPane::SetLayerFrame` for start/setup.
- `dword_67A7D0` is [UID:00028R][0x0067a7d0-0x0067a7d4.g_pSoundManager](by-memory/0x0067a7d0-0x0067a7d4.g_pSoundManager.md), used here as `g_pSoundManager->PlaySample(200, 100)`.
- `0x004b7c50` is the rectangle setter; source-facing C++ uses `SetRect(&animationRect, ...)`.
- `0x005975e0` is the timer scheduling wrapper; source-facing C++ uses `m_timerHandler.ScheduleTimer(...)`.
- Pane vtable slots `+0x20`, `+0x30`, and `+0x38` remain source-facing `Pane::InvalidateRect`, `Pane::AddToLayer`, and `Pane::RemoveFromLayer`.
- `StartAnimation` is the preferred source-facing method name. `StartDeliveryAnimation` remains a descriptive historical/search alias because no caller/source clue proves the exact original spelling.

Rejected alternatives from the accepted B007 pass:

- Keeping formal C++ blank only because `0x00546bc0` has no function/xref/direct route is rejected. The accepted [UID:0002R5][0x00546b80-0x00546bb5.FlyingParcelPaneCleanupDestructorBody](by-memory/0x00546b80-0x00546bb5.FlyingParcelPaneCleanupDestructorBody.md) precedent shows a raw no-route `FlyingParcelPane` method can emit source when the ordinary body is proven by exact bytes, ownership, and source-family context.
- Splitting the range is rejected: current MCP confirms one coherent `thiscall` body, one `retn 10h`, exact padding before/after, and no internal padding/dead island.
- Moving ownership to `ScreenPane`, `SoundManager`, `TimerHandler`, `Layer`, `g_useEpfAssets`, or a new `FlyingParcelPane.cpp` is rejected. Those are dependencies; this method mutates `FlyingParcelPane` fields and routes through [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md) to [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
- Emitting source from [UID:0001EL][0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart](by-memory/0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart.md) is rejected because that aggregate is a non-emitting split inventory.
- Raw labels such as `sub_556CE0`, `sub_57A5C0`, `dword_67A7CC`, `dword_67A7D0`, and `byte_66DA97` are rejected in formal C++; support docs now provide accepted source-facing names.
- Treating `+0xac` as a derived `FlyingParcelPane` payload field is rejected. Corrected Point layout and sibling parity identify it as inherited `Pane::m_origin.y`; the older `.x` interpretation is superseded.
- Treating the fourth argument as a generic animation-state byte is rejected; [UID:0002KL][0x00546df0-0x00546e6b.FlyingParcelPaneDrawParcelOrLetter](by-memory/0x00546df0-0x00546e6b.FlyingParcelPaneDrawParcelOrLetter.md) uses `m_isParcel` to choose parcel versus letter art.

## Reconstruction Notes

This page now emits first-draft `void FlyingParcelPane::StartAnimation(unsigned char parcelCountOrState, unsigned char leftSlot, unsigned char rightSlot, bool isParcel)` through [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md). The formal source block preserves exact binary behavior: both branches call `ScreenPane::SetLayerFrame`, store the raw payload bytes, reset `m_animationFrame`, remove/add the pane layer, invalidate `m_bounds`, and schedule the tertiary timer; the current/EPF branch additionally plays sound sample `200` at volume `100`, selects one of two parcel/letter rectangles, and shifts inherited `m_origin.y` by `30`.

The raw start still has no surviving caller, pointer, vtable slot, or original source declaration. That negative evidence caps confidence and keeps the class-level declaration/header shape conservative, but it is no longer a target-level no-code proof.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | The exact raw range, padding boundaries, branch behavior, payload writes, helper calls, timer scheduling, parent class route, aggregate route, accepted source-facing names, rejected alternatives, generated-state caveat, and formal first-draft C++ are now documented. |
| Confidence | 90 | Current live MCP session `398b87c1`, the supplemental PE no-route scan, existing class/file/layout support docs, and accepted sibling method pages agree on the body and source route. Confidence remains below the destructor/animate/draw children because the start is raw, IDA has no function object/direct xref, no VA/RVA/rel32 route survives, and the exact original declaration/name remains inferred. |

## Cross-References

- [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md)
- [UID:0000MF][ParcelPane](by-file/ParcelPane.md)
- [UID:0001EL][0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart](by-memory/0x00546b80-0x00546d64.FlyingParcelPaneRawLifecycleAndStart.md)
- [UID:0002KK][0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep](by-memory/0x00546d70-0x00546dee.FlyingParcelPaneAnimateStep.md)
- [UID:0002KL][0x00546df0-0x00546e6b.FlyingParcelPaneDrawParcelOrLetter](by-memory/0x00546df0-0x00546e6b.FlyingParcelPaneDrawParcelOrLetter.md)

## Changes

- 2026-07-16 B005 UID0003XX support callback: Preserved `90/90`, metadata, source route, payload, resource, layer, timer, raw-reachability, and negative evidence. Corrected owner offset `+0xac` from the superseded `m_origin.x` interpretation to `Point::y` and changed only that component in the accepted formal body.

- 2026-06-27 B007 accepted StartAnimation implementation:
  - Before: `COMPLETION:85`, `CONFIDENCE:88`, formal C++ blank, and the page treated the raw no-function/no-direct-route evidence as a target-level no-code blocker.
  - After: `COMPLETION:90`, `CONFIDENCE:90`; owner [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md), reconstructable state, emitter route, and blank optional position remain unchanged; the formal block now emits first-draft `FlyingParcelPane::StartAnimation(...)` source through [UID:0000MF][ParcelPane](by-file/ParcelPane.md).
  - Evidence: accepted B007 report used MCP session `398b87c1` request ids `3`, `4`, and `10-21` plus a read-only PE no-route scan. The pass reconfirmed no IDA function, no direct xrefs, no VA/RVA/direct-branch route, exact padding, a unique 420-byte body, 126 ownerless instructions ending in `retn 10h`, helper/global values, rectangle behavior, and the accepted source route. The no-direct-route fact is retained as a confidence cap, not a no-code blocker.
- 2026-06-15 A003 Goal2 flying parcel refresh:
  - Before: score was `80/86`.
  - Changed to: score is `85/88`, with the existing [UID:000058][FlyingParcelPane](by-class/FlyingParcelPane.md) owner/emitter route retained.
  - Summary/evidence: live IDA MCP reconfirmed that `0x00546bc0` and `0x00546d64` are not function starts, that `0x00546d70` is the modeled successor `AnimateStep`, that the body has no direct xrefs, and that bounded disassembly still proves the security-cookie-framed `thiscall` body ending in `retn 0x10`. At that time completion was capped at `85` because source-quality manager/scheduler/field names were unresolved; the 2026-06-17 B002 support refresh below supersedes that specific naming blocker with descriptive source-facing names while preserving the raw no-caller C++ caveat.
- 2026-06-17 B002 source-quality support refresh:
  - Resolved best source-facing direction as `FlyingParcelPane::StartAnimation` / `StartDeliveryAnimation`, documented parameter/field names `m_parcelCountOrState`, `m_leftSlot`, `m_rightSlot`, `m_isParcel`, `m_animationFrame`, and `m_animationLayer`, and replaced raw global/helper placeholders with `g_useEpfAssets`, `g_pScreenPane`, `g_pSoundManager`, `ScreenPane::SetLayerFrame`, `SoundManager::PlaySample`, and timer scheduling semantics. Formal C++ remains blank because no direct raw-start caller/pointer edge survives.
- 2026-06-19 B012 accepted FlyingParcelPane implementation sync:
  - Promoted `StartAnimation` as the preferred source-facing name, demoted `StartDeliveryAnimation` to a descriptive alias, added local PE no-route evidence for the raw start, and clarified that `this+0xac` is inherited `Pane` origin/position state rather than derived flying-animation payload.
