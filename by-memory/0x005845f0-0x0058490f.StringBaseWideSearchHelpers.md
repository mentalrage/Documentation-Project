*** UID:0003NL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OA | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace mystr {

namespace {

bool StringBaseWideCharInSet(wchar_t ch,
                             const wchar_t *set,
                             unsigned int setLength)
{
    for (unsigned int i = 0; i < setLength; ++i) {
        if (set[i] == ch) {
            return true;
        }
    }
    return false;
}

}

template <>
bool
StringBase<wchar_t, mychar_traits<wchar_t> >::empty() const
{
    return reinterpret_cast<const int *>(m_data)[-2] == 0;
}

template <>
StringBase<wchar_t, mychar_traits<wchar_t> > &
StringBase<wchar_t, mychar_traits<wchar_t> >::RemoveWideRange(
    unsigned int start,
    unsigned int count)
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length <= start) {
        return *this;
    }

    unsigned int removeCount = length - start;
    if (count < removeCount) {
        removeCount = count;
    }
    if (removeCount == 0) {
        return *this;
    }

    if (start == 0 && removeCount == length) {
        ReleaseWideBuffer();
        return *this;
    }

    DetachWideIfShared();
    const unsigned int newLength = length - removeCount;
    memmove(m_data + start,
            m_data + start + removeCount,
            (newLength - start) * sizeof(wchar_t));
    m_data[newLength] = L'\0';
    reinterpret_cast<int *>(m_data)[-2] = static_cast<int>(newLength);
    return *this;
}

template <>
int
StringBase<wchar_t, mychar_traits<wchar_t> >::FindSubstring(
    const wchar_t *needle,
    unsigned int start,
    unsigned int needleLength) const
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length < start + needleLength) {
        return -1;
    }

    for (unsigned int index = start;
         index + needleLength <= length;
         ++index) {
        unsigned int matched = 0;
        while (matched < needleLength &&
               m_data[index + matched] == needle[matched]) {
            ++matched;
        }
        if (matched == needleLength) {
            return index == length ? -1 : static_cast<int>(index);
        }
    }

    return -1;
}

template <>
int
StringBase<wchar_t, mychar_traits<wchar_t> >::FindFirstNotOf(
    const wchar_t *chars,
    unsigned int start,
    unsigned int charCount) const
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length < start) {
        return -1;
    }

    for (unsigned int index = start; index < length; ++index) {
        if (!StringBaseWideCharInSet(m_data[index], chars, charCount)) {
            return static_cast<int>(index);
        }
    }

    return -1;
}

template <>
int
StringBase<wchar_t, mychar_traits<wchar_t> >::FindFirstOf(
    const wchar_t *chars,
    unsigned int start,
    unsigned int charCount) const
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length < start) {
        return -1;
    }

    for (unsigned int index = start; index < length; ++index) {
        if (StringBaseWideCharInSet(m_data[index], chars, charCount)) {
            return static_cast<int>(index);
        }
    }

    return -1;
}

template <>
int
StringBase<wchar_t, mychar_traits<wchar_t> >::FindLastNotOf(
    const wchar_t *chars,
    unsigned int start,
    unsigned int charCount) const
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length == 0) {
        return -1;
    }

    unsigned int index = start;
    if (length - 1 < index) {
        index = length - 1;
    }

    for (;;) {
        if (!StringBaseWideCharInSet(m_data[index], chars, charCount)) {
            return static_cast<int>(index);
        }
        if (index == 0) {
            break;
        }
        --index;
    }

    return -1;
}

template <>
int
StringBase<wchar_t, mychar_traits<wchar_t> >::FindLastOf(
    const wchar_t *chars,
    unsigned int start,
    unsigned int charCount) const
{
    const unsigned int length =
        static_cast<unsigned int>(reinterpret_cast<const int *>(m_data)[-2]);
    if (length == 0) {
        return -1;
    }

    unsigned int index = start;
    if (length - 1 < index) {
        index = length - 1;
    }

    for (;;) {
        if (StringBaseWideCharInSet(m_data[index], chars, charCount)) {
            return static_cast<int>(index);
        }
        if (index == 0) {
            break;
        }
        --index;
    }

    return -1;
}

} // namespace mystr
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: StringBase UTF-16 empty/remove/search helper group with seven exact modeled functions, padding/boundary evidence, caller/callee inventory, source-facing `empty`, `RemoveWideRange`, `FindSubstring`, first/last-of, and not-of roles, and formal first-draft C++ through [UID:0000OA][StringBase](by-file/StringBase.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005845f0-0x0058490f StringBase Wide Search Helpers

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact StringBase UTF-16 empty, remove, substring-search, first/last-of, and not-of helper group.
- Rebuild handling: source-authored StringBase implementation code.
- Parent container: [UID:0002RR][0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring](by-memory/0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring.md)
- Direct source owner/emitter: [UID:0000OA][StringBase](by-file/StringBase.md)
- Confidence: strong for exact function inventory and behavior class; medium-high for final helper names.
- C++ reconstruction: formal first-draft C++ is populated for all seven modeled helpers. Final overload names remain inferred, but current MCP confirms the behavior and direct `StringBase` route strongly enough for source output.

## Covered Range

IDA MCP session `b001_0003gy` on 2026-06-13 confirmed these modeled functions. Size conversions were verified with `tools/int_convert.py`.

| Range | Size | Current IDA name | Role |
| --- | ---: | --- | --- |
| `0x005845f0-0x005845fa` | `0x0a` / 10 bytes | `sub_5845F0` | Empty predicate; returns whether `data[-2]` length is zero. |
| `0x00584600-0x00584679` | `0x79` / 121 bytes | `sub_584600` | Remove/erase helper; clamps count, releases all storage for whole-string removal, otherwise shifts the tail and updates `data[-2]`. |
| `0x00584680-0x00584720` | `0xa0` / 160 bytes | `sub_584680` | Forward substring search over UTF-16 data. |
| `0x00584720-0x00584799` | `0x79` / 121 bytes | `sub_584720` | `FindFirstNotOf`: forward search returning the first stored UTF-16 character not present in a caller-provided set. |
| `0x005847a0-0x00584815` | `0x75` / 117 bytes | `sub_5847A0` | `FindFirstOf`: forward search returning the first stored UTF-16 character present in a caller-provided set. |
| `0x00584820-0x00584892` | `0x72` / 114 bytes | `sub_584820` | `FindLastNotOf`: reverse search returning the last stored UTF-16 character not present in a caller-provided set. |
| `0x005848a0-0x0058490f` | `0x6f` / 111 bytes | `sub_5848A0` | `FindLastOf`: reverse search returning the last stored UTF-16 character present in a caller-provided set. Implementation recheck on MCP session `80de0a67` confirmed this complementary role. |

The one byte at `0x0058490f-0x00584910` is alignment padding before [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md). It is not part of this child page.

## Behavior Notes

- Every helper reads the current UTF-16 length from `data[-2]`, matching [UID:0001VQ][RefCountedStringBufferHeader](by-type/by-struct/RefCountedStringBufferHeader.md).
- `sub_584600` is the only mutating helper in this split. It calls `0x005833a0` for full removal and `0x005830c0` before in-place partial removal, then shifts remaining UTF-16 data with `memmove_0`, writes a NUL terminator, and updates the stored length.
- `sub_584680` and `sub_584c40` are the forward/reverse substring search pair; the reverse half lives in sibling [UID:0003NM][0x00584990-0x00584d7e.StringBaseSpliceGrowAndSubstring](by-memory/0x00584990-0x00584d7e.StringBaseSpliceGrowAndSubstring.md) because the physical vector helper and padding divide the original broad range.
- `sub_584720`, `sub_5847a0`, `sub_584820`, and `sub_5848a0` implement set-membership and not-membership searches used by split/tokenization and trim helpers in [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md). B010's current-session proof corrects the older inverted first-of/not-of and last-of/not-of wording: [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md) `TrimLeftWide` calls `FindFirstNotOf`, `SplitWideBySet` alternates `FindFirstOf` then `FindFirstNotOf`, and `TrimRightWide` calls `FindLastNotOf`.

## IDA MCP Evidence

- `entity_query functions` over `0x005845f0-0x00584d80` returned the seven starts above, followed by [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md) at `0x00584910`.
- `analyze_function 0x005845f0` decompiles to `return *(_DWORD *)(*this - 8) == 0` and has direct callers from utility/UI paths such as `0x00529ee0`, `0x0052b730`, `0x0052d1e0`, `0x0052ea50`, `0x005534a0`, `0x00556f70`, `0x0056c0e0`, `0x00574e50`, and `0x005aac80`.
- `analyze_function 0x00584600` reports callees `0x005833a0`, `memmove_0`, and `0x005830c0`, with callers `0x0048ebc0`, `0x0048eed0`, `0x004f95b0`, and trim-right helper `0x00583f70`.
- `analyze_function 0x00584680` has direct caller `0x004918e0` and decompiles as a bounded forward substring scan that returns `-1` when the requested span cannot fit.
- B010 report MCP `analyze_function` id `115` for `0x00584720` reports callers `0x005839c0` and `0x00583e70`; its decompilation skips set members and returns the first index whose character is not in the set. Correct source-facing role: `FindFirstNotOf`.
- B010 report MCP `analyze_function` id `116` for `0x005847a0` reports callers `0x004918e0` and `0x005839c0`; its loop skips non-members and returns the first index whose character is in the set. Correct source-facing role: `FindFirstOf`.
- B010 report MCP `analyze_function` id `117` for `0x00584820` reports callers `0x0056c0e0` and `0x00583f70`; its reverse loop skips set members and returns the last index whose character is not in the set. Correct source-facing role: `FindLastNotOf`.
- B010 implementation MCP `analyze_function` id `203` and `decompile` id `205` for `0x005848a0` show the complementary reverse search that returns the last index whose character is in the caller-provided set. Correct source-facing role: `FindLastOf`.

## Ownership Notes

This split exists because the former broad [UID:0002RR][0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring](by-memory/0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring.md) range also contained the physically embedded but separately owned [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md) vector append helper. The functions in this page are all direct StringBase UTF-16 helpers and route to [UID:0000OA][StringBase](by-file/StringBase.md).

## Changes

- 2026-06-30 B011 [UID:0000OA][StringBase](by-file/StringBase.md) empty-emitter implementation:
  - Before: `COMPLETION:86`, `CONFIDENCE:89`, formal C++ blank below the previous source-code gate.
  - After: `COMPLETION:89`, `CONFIDENCE:91`, formal wide search/removal helper group populated.
  - Summary/evidence: incorporated accepted B011 report `0000OA-StringBase-empty-emitter-family-source-quality.md`. Current MCP session `supervisor_resume_20260629` confirms seven exact modeled functions from `0x005845f0` through `0x0058490f`, xrefs to each start, `0x0058490f-0x00584910` padding before the vector helper, `empty` length test, `RemoveWideRange` full-release and partial-detach behavior, forward substring search, first/last-of and not-of set scans, and direct [UID:0000OA][StringBase](by-file/StringBase.md) ownership. The nearby [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md) remains a separate owner split.

## Cross-References

- [UID:0002RR][0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring](by-memory/0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring.md)
- [UID:0000OA][StringBase](by-file/StringBase.md)
- [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md)
- [UID:0003NM][0x00584990-0x00584d7e.StringBaseSpliceGrowAndSubstring](by-memory/0x00584990-0x00584d7e.StringBaseSpliceGrowAndSubstring.md)
- [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md)
- [UID:0001VQ][RefCountedStringBufferHeader](by-type/by-struct/RefCountedStringBufferHeader.md)

## Changes

- 2026-06-25 B010 [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md) search-helper correction:
  - Score unchanged.
  - Summary/evidence: corrected the first/last set-search roles using current MCP session `80de0a67` evidence from [UID:0002RQ][0x00583970-0x00584052.StringBaseReplaceTrimAndLocks](by-memory/0x00583970-0x00584052.StringBaseReplaceTrimAndLocks.md). `0x00584720` is `FindFirstNotOf`, `0x005847a0` is `FindFirstOf`, `0x00584820` is `FindLastNotOf`, and implementation recheck ids `203`/`205` confirm `0x005848a0` as `FindLastOf`.
- 2026-06-13 A002 Goal 2 split:
  - New exact child page split out of [UID:0002RR][0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring](by-memory/0x005845f0-0x00584d7e.StringBaseSearchSpliceAndSubstring.md).
  - Initial score: `COMPLETION:86`, `CONFIDENCE:89`.
  - Summary/evidence: live IDA MCP confirmed seven in-range function starts and behavior for empty, remove, forward substring search, set search, not-of search, reverse set search, and reverse not-of search. The split keeps the `0x00584910` vector helper with [UID:0001J4][0x00584910-0x0058498b.SimpleUStringVectorPushBack](by-memory/0x00584910-0x0058498b.SimpleUStringVectorPushBack.md) instead of claiming it as StringBase-owned code.
