*** UID:0002QE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
namespace
{
bool IsPasswordChangeSimpleNewPasswordReplyError(unsigned char result)
{
    return result >= 3 && result <= 8;
}
}

bool ChangePasswordDialogPane::HandlePasswordChangeReplySimple(const unsigned char *packet)
{
    if (g_pScreenDimmer != NULL) {
        delete g_pScreenDimmer;
    }

    const unsigned char result = packet[1];
    const unsigned char messageLength = packet[2];
    char narrowMessage[0x100];
    wchar_t message[0x100];

    memmove(narrowMessage, packet + 3, messageLength);
    narrowMessage[messageLength] = 0;
    const int wideLength = MultiByteToWideChar(
        CP_ACP, 0, narrowMessage, messageLength, message, 0x100);
    message[static_cast<unsigned char>(wideLength)] = L'\0';

    if (result != 0) {
        if (IsPasswordChangeSimpleNewPasswordReplyError(result)) {
            SetHoverControl(5);
        }

        if (message[0] != L'\0')
            new AlertPane(message, this, L"OK", NULL);
        return true;
    }

    if (message[0] != L'\0')
        new AlertPane(message, this, L"OK", NULL);
    SlideCloseVertical();
    CloseDialog();
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready retained/simple ChangePasswordDialogPane reply helper with exact bounds, no direct route, packet `[1]/[2]/[3]` fields, result codes `3..8` -> `SetHoverControl(5)`, ANSI-to-UTF16 alert path, success close/dismiss path, and `0x004ff03f` padding before `0x004ff040` verified. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004fee40-0x004ff03f - HandlePasswordChangeReplySimple

## Status

- Disposition: reconstructable NexusTK login/account packet reply helper.
- Rebuild handling: source-authored helper.
- Confidence: strong for behavior, range boundary, local owner family, callees, RankingDialog exclusion, formal retained/simple C++ readiness, and the no-direct-xref caveat; current active MCP still reports raw `sub_4FEE40`, so prior saved-IDB-name wording is historical rather than current proof.
- Owner/emitter: [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md).
- Parent aggregate: [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).

## Address Range

| Range | Kind | Notes |
| --- | --- | --- |
| `0x004fee40-0x004ff03f` | method/helper | Current active MCP still names this `sub_4FEE40`, size `0x1ff` / 511 bytes (Verified with int_convert.py); source-facing descriptive role is `ChangePasswordDialogPane::HandlePasswordChangeReplySimple`, but it remains a sibling reply handler with no current direct start xrefs. |
| `0x004ff03f-0x004ff040` | following padding | One `0xcc` byte before `ScrolledTextControlPane`; recorded in [UID:0000VN][-ignored](by-memory/-ignored.md). |

## Behavior

Parses a simpler password/account reply packet shape. It reads the result byte at packet offset `+1`, clears the active modal/dimmer through `g_pScreenDimmer`, maps nonzero result codes `3` through `8` to inherited `SetHoverControl(5)`, decodes a length-prefixed ANSI message from offsets `+2/+3`, converts it to UTF-16, and displays nonempty text with `AlertPane` and the shared `kDialogOkButtonText` label. For result zero it performs the same message display path and then closes/dismisses the dialog through `SlideCloseVertical()` and `CloseDialog()`. This support sync parallels [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md), while the missing direct start xref/registration path remains a source-placement and liveness confidence cap rather than a formal-C++ blocker.

## Evidence Notes

- IDA MCP `lookup_funcs 0x004fee40` reports function size `0x1ff`, ending at `0x004ff03f`.
- IDA MCP `callers 0x004fee40` and `xrefs_to 0x004fee40` report no current direct references.
- IDA MCP decompile shows the same packet-message pattern as [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md): result byte at `a2 + 1`, message length at `a2 + 2`, message payload at `a2 + 3`, `sub_516220`, `MultiByteToWideChar`, `sub_4F4AA0(624)`, and `sub_49FEB0` alert construction.
- IDA MCP `callees 0x004fee40` reports the same dialog/modal and alert helper family as `0x004febf0`: `sub_49EB90`, `sub_49DAD0`, `sub_516220`, `sub_4F4AA0`, `MultiByteToWideChar`, `sub_49FEB0`, and `sub_516030`.
- IDA MCP raw bytes at `0x004fee00` show the previous function's epilogue, exact switch support through `0x004fee37`, nine `0xcc` padding bytes at `[0x004fee37,0x004fee40)`, and the `0x004fee40` prologue. Raw bytes at `0x004ff030` show the helper epilogue followed by one `0xcc` byte before `0x004ff040`.
- Existing generated-source issue notes record that generated output had grouped `0x004fee40` with `RankingDialog`; IDA behavior and address locality support this password/account reply-helper owner instead.
- 2026-06-28 B002 support sync for [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md) used current MCP session `supervisor_20260628_resume` and reconfirmed `xrefs_to 0x004fee40` is still zero, while [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md) has the direct dispatcher xref at `0x004fe551`. The same pass resolves shared source-facing helper names for this family: `g_pScreenDimmer`, `SetHoverControl`, `AlertPane`, `kDialogOkButtonText`, `SlideCloseVertical()`, and `CloseDialog()`. The earlier blank-C++ disposition is superseded by the 2026-07-05 B006 implementation; the no-route fact remains as a confidence cap.
- 2026-07-05 B006 implementation callback applies the accepted source-quality report with current MCP session `supervisor_recovery_20260705`. MCP `server_health` reported status `ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, input `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready with size `2067`.
- The same B006 pass reconfirmed `lookup_funcs 0x004fee40` as current raw `sub_4FEE40`, size `0x1ff` / 511 bytes (Verified with `tools/int_convert.py`); `lookup_funcs 0x004ff03f` as no function; and `lookup_funcs 0x004ff040` as the separate successor `sub_4FF040`, size `0x244`.
- B006 `analyze_function 0x004fee40` reports prototype `char __thiscall(int *this, int)`, 20 basic blocks, complexity `6`, `CHAR[256]` and `WCHAR[256]` local buffers, `packet[1]` result, `packet[2]` message length, `packet + 3` message bytes, `g_pScreenDimmer` clear, `SetHoverControl(5)` only for nonzero results `3..8`, optional alert text, success `SlideCloseVertical()` / `CloseDialog()`, and handled return `1` on all paths.
- B006 route checks preserve the no-route cap: `xrefs_to 0x004fee40`, `xref_query to 0x004fee40`, and `find_bytes "40 ee 4f 00"` all returned zero. The accepted B005 retained/no-route precedent for [UID:0002QB][0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount](by-memory/0x004fe790-0x004fe9d1.RawPasswordChangePacketWithAccount.md) supports emitting formal source-family C++ when behavior, owner, route, and source shape are exact; for this page the route absence is documented as a confidence/source-placement cap, not a blank-emitter proof.

## 2026-06-06 Live IDA Refresh

- `lookup_funcs` reports `sub_4FEE40` at `0x004fee40` with size `0x1ff`; `0x004ff03f` is not a function; the next function begins at `0x004ff040` as `sub_4FF040` with size `0x244`.
- `callers` and `xrefs_to 0x004fee40` still report no ordinary direct references. Keep the function as a sibling packet-handler entry in the password-change helper aggregate rather than inventing a direct caller.
- `callees 0x004fee40` reports `sub_516030`, `std::_Narrow_char_traits<char,int>::to_char_type`, `sub_516220`, `MultiByteToWideChar`, `sub_4F4AA0`, `sub_49FEB0`, `sub_49EB90`, `sub_49DAD0`, and `@__security_check_cookie@4`.
- Decompilation confirms this is a `char __thiscall` helper that returns `1` unconditionally after handling the reply packet. It uses a 256-byte ANSI stack buffer and a 256-wide-character UTF-16 stack buffer.
- The packet format is `result = packet[1]`, `messageLength = packet[2]`, and `messageBytes = packet + 3`. Both the nonzero-result and zero-result branches copy the message bytes, NUL-terminate the ANSI buffer, convert through `MultiByteToWideChar(0, 0, ..., 256)`, NUL-terminate the wide buffer by returned character count, and construct an alert when the converted message is not empty.
- The nonzero-result branch clears `dword_69AE08` when present, maps result codes `3..8` to virtual slot `+0x58` argument `5`, and leaves the dialog open after optional alert display.
- The zero-result branch clears `dword_69AE08`, optionally displays the converted server message, then calls `sub_49EB90(this, result, this)` and `sub_49DAD0(this)` to close/dismiss the dialog.
- Byte check around the boundaries shows `0x004fee30` contains previous support bytes followed by `0xcc` padding and the `55 8b ec 6a ff` prologue at `0x004fee40`; `0x004ff03f` is a single `0xcc` byte before the `0x004ff040` prologue.
- `xrefs_to 0x004ff040` reports code refs from `0x004ffc52`, `0x004ffc79`, `0x00500207`, and `0x0050033e`, confirming `0x004ff040` belongs to the following `ScrolledTextControlPane`-family function and not this packet helper.

## 2026-06-16 C001 Live IDA Refresh

- Active IDA MCP session: `b001_mappane_0001AW_20260616`, `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready, strings cache ready; targeted function/byte/xref queries returned stable results while `auto_analysis_ready:false`.
- Historical `lookup_funcs` reported `sub_4FEE40` at `0x004fee40`, size `0x1ff`, with no function at the then-assumed padding address `0x004fee38` or at `0x004ff03f`; the old padding split is superseded by current exact class-table end `0x004fee37` and nine-byte alignment `[0x004fee37,0x004fee40)`. That pass then reported a saved descriptive rename, but later current-IDB evidence again found raw `sub_4FEE40`.
- `analyze_function 0x004fee40` reports `20` basic blocks, cyclomatic complexity `6`, stack buffers `CHAR[256]` and `WCHAR[256]`, no listed ordinary callers, no xrefs to the function start, and the same modal/message/alert/close helper family as the primary reply handler.
- `find_bytes` over little-endian pointers found no raw pointer to `0x004fee40`, while `xrefs_to 0x004ff040` still points to the following `ScrolledTextControlPane`/staff-family code, confirming the `0x004ff03f` one-byte padding boundary.
- Decompilation reconfirms `result = packet[1]`, `messageLength = packet[2]`, `messageBytes = packet + 3`; nonzero results clear the active dimmer and map result codes `3..8` to vtable slot `+0x58` argument `5`; result zero displays the optional server message and calls `sub_49EB90`/`sub_49DAD0`.
- The RankingDialog pollution exclusion was rechecked against [UID:0000BM][RankingDialog](by-class/RankingDialog.md): `0x004fee40` remains listed there only as generated-source pollution, while local IDA behavior, adjacent range layout, shared packet/message callees, and the [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md) aggregate support ChangePasswordDialog ownership.
- Decimal conversions for `0x1ff`, `0x58`, `0x270`, `0x100`, and `0xff` were rechecked with `tools/int_convert.py`.

## 2026-07-24 Current Container, Runtime Caveat, And IDA Handoff

- B002 re-read this exact function against live IDA MCP session `dc9b367a-4569-4166-8fa6-4d2bcb44d3d2`, IDB session `1ce787ed`, at shared-IDB checkpoint SHA-256 `F28C8B463F64693561E118790FD5E9D68739F85C697B9B58F391F56E1911ED99`. Later unrelated IDB saves may supersede the file checkpoint without changing the accepted function/range/route evidence.
- Exact current prestate: function `[0x004fee40,0x004ff03f)`, name `sub_4FEE40`, size `0x1ff`, type `char __thiscall(int *this, int)`, entry bytes `55 8B EC 6A FF 68 DC 1C 60 00 64 A1 00 00 00 00`, and absent entry-regular, entry-repeatable, function-regular, and function-repeatable comments. The successor remains separate at `0x004ff040`.
- Current reachability remains exhaustively negative: zero start xrefs and zero code/data/immediate/little-endian pointer routes. The active sibling UID0002QD alone has dispatcher edge `0x004fe551`; no corresponding route is invented here. Locality, receiver calls, message layout, and behavior keep this as retained ChangePasswordDialogPane source and reject RankingDialog pollution.
- Exact runtime caveat: both the nonzero-result and success branches copy exactly the one-byte `packet[2]` ANSI length from `packet + 3`, append an ANSI NUL, call `MultiByteToWideChar(..., 256)`, and the decompiled machine behavior indexes the `WCHAR[256]` destination with the conversion return cast to `unsigned __int8` before writing the wide NUL. Unlike UID0002QD, this body has no explicit `>= 256` conversion-result range-check path. The existing `SimpleUString::FromMbcString(packet + 3, packet[2])` formal is a human source abstraction and must preserve that byte-length/conversion-return behavior when exact execution is implemented.
- Result `3..8` focus behavior, optional alert text, success close, handled return, current `90/91` score, existing formal, and UID0000I3 owner/emitter remain unchanged. UID00022W is now only a reviewed false/non-emitting physical index and does not duplicate this source body.
- Supervisor Gate 2B handoff, not applied by B002: rename to `ChangePasswordDialogPane__HandlePasswordChangeReplySimple`, apply `bool __thiscall ChangePasswordDialogPane__HandlePasswordChangeReplySimple(ChangePasswordDialogPane *this, const unsigned char *packet)`, and set only the function repeatable comment to `Retained ChangePasswordDialogPane reply sibling: packet[1] result, packet[2] message length, packet+3 payload; results 3..8 focus control 5, success closes; no current caller, xref, or pointer route.` Preserve exact range/bytes, zero xrefs, separate successor, and absent function-regular/entry-regular/entry-repeatable comments.

## Touched State And Calls

| Reference | Role |
| --- | --- |
| `g_pScreenDimmer` / `0x0069ae08` | Active modal/dimmer object cleared during reply handling. |
| `SetHoverControl(5)` / vtable slot `+0x58` / 88 decimal (Verified with int_convert.py) | Inherited dialog active/hover control update for result codes `3` through `8`. |
| `sub_516220` / source-level message copy | Copies length-prefixed server message bytes. |
| `MultiByteToWideChar` / `SimpleUString` message decode | Converts server message text to UTF-16. |
| `AlertPane` with `kDialogOkButtonText` | Allocates and constructs alert pane messages. Binary allocation is `0x270` / 624 decimal (Verified with int_convert.py). |
| `SlideCloseVertical()` / `CloseDialog()` | Closes and dismisses the dialog on success. |
| `@__security_check_cookie@4` / `SEH_4FEE40` | Compiler-generated SEH/security-cookie support for the local message buffers. |

## Reconstruction Notes

Keep this helper with the password-change/account-dialog source family unless a future caller proves a different owner. Do not migrate it to ranking code.

Formal C++ is now present as a retained/simple reply helper under [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md). The body intentionally uses the accepted shared reply-text helpers from [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md) and a local numeric predicate for result codes `3..8` instead of inventing unrecovered enum names. The lack of a direct dispatcher xref or pointer-table reference still means the exact original registration path and declaration placement remain unresolved; keep those facts as confidence caps and do not invent a dispatcher edge from [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md).

## Source-Quality Notes

- Owner/emitter decision: keep [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md). The helper shares local adjacency, packet result/message layout, active-dimmer handling, alert construction, and close-helper behavior with [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md).
- RankingDialog exclusion: no live IDA xref, pointer hit, local behavior, or support doc ties this function to RankingDialog. The only RankingDialog relationship found is documented generated-source pollution in [UID:0000BM][RankingDialog](by-class/RankingDialog.md).
- IDA DB/source-name decision: `ChangePasswordDialogPane::HandlePasswordChangeReplySimple` is safe as an inferred source-facing member/helper name, but current active MCP again reports raw `sub_4FEE40`; older C001 saved-rename wording is historical and not current IDB proof.
- Score decision: `90/91`. Completion rises because this page now carries formal retained/simple C++ and current MCP evidence for behavior, range, no-route proof, result-code handling, and boundary facts. Confidence stays below final-audit range because no direct start xref, no pointer hit, no symbolic packet/result enum names, and exact original member declaration/source grouping remain unresolved.
- Remaining caps: no direct start xref, no pointer hit to the function start, unknown packet opcode/result symbolic names, unresolved original declaration style, and unresolved standalone `ChangePasswordDialogPane.cpp` versus broader login/account dialog grouping.

## Cross-References

- [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md)
- [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md)
- [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)
- [wave3 data issues](../wave3_data_issues.md)

## Changes

### 2026-08-14 B003 exact simple-reply formal

- The retained simple handler now performs its own length-prefixed `memmove` plus `MultiByteToWideChar(CP_ACP, ...)` decode and uses literal `L"OK"`; it no longer depends on helpers owned by UID0002QD or on undocumented SimpleUString/dialog-label facades.
- Results `3..8` focus new-password control `5`; nonempty messages show an alert; a zero result also performs `SlideCloseVertical()` and `CloseDialog()`. The lack of an explicit range-check branch is preserved for this sibling.
- UID0000I3 remains owner/emitter at `90/91`, H remains blank under UID00001L, and the zero-direct-xref/no-pointer-route liveness cap remains explicit.

- 2026-07-24 B002 UID00022W aggregate implementation callback support sync:
  - Score/formal/owner/emitter remain `90/91`, existing source-ready retained/simple member C++, and UID0000I3.
  - Added the exact current tinfo/no-route state, corrected `[0x004fee37,0x004fee40)` leading padding, byte-length/conversion-return runtime caveat, false-parent relationship, and complete supervisor-only IDA action/readback handoff without inventing a dispatcher edge.
- 2026-07-05 B006 implementation callback for accepted UID0002QE source-quality report:
  - Score raised from `85/90` to `90/91`; owner [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md), `RECONSTRUCTABLE:TRUE`, emitter [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md), and blank emitter position are unchanged.
  - Added formal `ChangePasswordDialogPane::HandlePasswordChangeReplySimple(const unsigned char *packet)` C++ to the reconstruction block. The emitted body deletes `g_pScreenDimmer`, decodes `packet[1]/[2]/[3]` reply text through the accepted shared helper names, maps nonzero result codes `3..8` to `SetHoverControl(5)`, shows optional server text, closes success replies with `SlideCloseVertical()` / `CloseDialog()`, and returns handled on all paths.
  - Summary/evidence: current MCP session `supervisor_recovery_20260705` reports `server_health` OK, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, current raw function name `sub_4FEE40`, size `0x1ff` / 511 bytes (Verified with `tools/int_convert.py`), no function at `0x004ff03f`, successor `sub_4FF040` at `0x004ff040`, prototype `char __thiscall(int *this, int)`, 20 blocks, complexity `6`, zero `xrefs_to`, zero `xref_query`, zero `find_bytes "40 ee 4f 00"` pointer hits, and one `0xcc` boundary byte before the next function.
  - Preserved the stale C001 saved-rename correction, RankingDialog pollution rejection, and no-direct-route evidence as confidence/source-placement caps. B005's retained/no-route formal-C++ precedent supports this source-family emission; the core dispatcher page still only proves the active UID0002QD packet type `2` route.
- 2026-06-28 B002 support sync from [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md) implementation:
  - Historical state: score unchanged at `85/90`; formal C++ still remained blank at that time. This is superseded by the 2026-07-05 B006 implementation above.
  - Summary/evidence: current MCP session `supervisor_20260628_resume` reported zero direct xrefs to `0x004fee40`, while the primary sibling [UID:0002QD][0x004febf0-0x004fee11.HandlePasswordChangeReply](by-memory/0x004febf0-0x004fee11.HandlePasswordChangeReply.md) gained formal source-ready C++ and the dispatcher xref at `0x004fe551`. This page used the resolved family helper names `g_pScreenDimmer`, `SetHoverControl(5)`, `AlertPane`, `kDialogOkButtonText`, `SlideCloseVertical()`, and `CloseDialog()` in prose, preserved the RankingDialog pollution exclusion, and recorded that current IDB naming was raw `sub_4FEE40` despite older saved-label claims.
- 2026-06-06 A009 live IDA refresh:
  - Before: score was `72/82`; the page had the high-level packet behavior but lacked a current dated change log, explicit callee set, stack-buffer details, and refreshed boundary/xref proof.
  - Changed to: score `80/88`, with exact boundary refresh, no-direct-xref caveat, packet-field layout, branch-specific dialog behavior, callee inventory, local buffer sizes, and following-function separation from `0x004ff040`.
  - Summary/evidence: IDA MCP `lookup_funcs`, `decompile`, `disasm`, `callers`, `callees`, `xrefs_to`, and a corrected `py_eval` byte/xref check verified the `0x004fee40-0x004ff03f` range, message decode/display path, result-code handling, success close path, and `0x004ff03f` padding boundary. The then-current blank-C++ conclusion is superseded by the 2026-07-05 B006 retained/simple formal C++ implementation above.
- 2026-06-16 C001 safe IDA/evidence refresh:
  - Before: `80/88`, current IDA name `sub_4FEE40`, strong behavior/range notes but stale name and source-pollution evidence.
  - Changed to: `85/90`; owner/emitter remained [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md), and final C++ remained blank at that time. The 2026-07-05 B006 implementation supersedes that blank-C++ state.
  - Summary/evidence: live IDA reconfirmed exact `0x1ff` / 511-byte (Verified with int_convert.py) range, no function-start xrefs, no little-endian pointer hit to `0x004fee40`, one-byte padding before `0x004ff040`, shared packet result/message behavior with the primary handler, RankingDialog pollution exclusion, and a then-reported saved IDA rename to `ChangePasswordDialogPane_HandlePasswordChangeReplySimple`; B002's 2026-06-28 current-session check supersedes the saved-name claim and reports raw `sub_4FEE40` again.
