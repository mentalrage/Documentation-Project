*** UID:0003WE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: 0x00550f20-0x00550f76 | no-route raw code island | MenuQuestionItemListLargerAddRowNoRouteRaw : ignored/non-emitting : 88% : strong : B015 source-routing recheck disassembles a complete stack-cookie row append clone: `ret 8` helper writes one-byte `optionId` into a temporary menu-question row prefix, copies the UTF-16 label with `_wcscpy_s` into the aligned `+2` field, and calls [UID:0003U0][0x004f3bb0-0x004f3f20.ListPaneItemSelectionStorageHelpers](by-memory/0x004f3bb0-0x004f3f20.ListPaneItemSelectionStorageHelpers.md) / `ListPane::AddEntry` at `0x004f3c50`. Raw PE VA/RVA/raw-offset and `.text` rel32 scans found no route to `0x00550f20`, while positive controls found constructor calls to `0x00550ed0` and vtable refs to `0x00550f90`/`0x00550fa0`; the active larger dialog constructor already inlines equivalent row insertion, so keep `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank emitter/C++ until a caller, pointer, or table route is proven. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00550f20-0x00550f76 MenuQuestionItemListLarger Add Row No-Route Raw

## Status

- Entity kind: raw source-shaped no-route row-append helper clone.
- Owner: `NONE`.
- Address range: `0x00550f20-0x00550f76`, inclusive start/exclusive end.
- Rebuild handling: non-emitting evidence only.
- Disposition: not reconstructable until a caller/pointer/runtime route is proven.
- Containing split index: [UID:0001FH][0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs](by-memory/0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs.md).
- Source-family context: [UID:0000LA][MessageDialogs](by-file/MessageDialogs.md).
- Best future semantic owner if a route appears: [UID:000080][MenuQuestionItemListLarger](by-class/MenuQuestionItemListLarger.md).
- Final C++: ineligible; the formal C++ block stays blank.

## Summary

This raw helper is a complete custom row append body for the larger menu-question list. It mirrors the normal-list raw add-row helper, stages a temporary row with a one-byte option id and a UTF-16 label, calls the shared `ListPane::AddEntry` append helper, checks the security cookie, and returns with `retn 8`.

Do not emit it or attach a canonical owner under current evidence. No caller, pointer table, vtable entry, switch route, or other runtime route to `0x00550f20` is proven. The active [UID:0003W4][0x0054fb30-0x00550afb.MenuQuestionDialogLargerConstructor](by-memory/0x0054fb30-0x00550afb.MenuQuestionDialogLargerConstructor.md) already performs the same row-population sequence inline and calls `ListPane::AddEntry` directly. Emitting this raw clone would duplicate dead/unrouted behavior.

If a route is discovered later, the best source-facing name is `MenuQuestionItemListLarger::AddRow(unsigned char optionId, const wchar_t *label)` or `AppendOptionRow`. Until then, those names are descriptive only and must not trigger emission.

## Raw Bytes, Boundary, And Padding

Exact helper range: `0x00550f20-0x00550f76`, size `0x56`.

- [UID:0003WD][0x00550ed0-0x00550f1f.MenuQuestionItemListLargerConstructor](by-memory/0x00550ed0-0x00550f1f.MenuQuestionItemListLargerConstructor.md) ends at `0x00550f1f`.
- `0x00550f1f-0x00550f20` is one `0xcc` padding byte.
- `0x00550f20-0x00550f76` is this complete raw helper body.
- `0x00550f76-0x00550f80` is ten `0xcc` padding bytes.
- [UID:0003WF][0x00550f80-0x00550f8e.MenuQuestionItemListLargerHasSelectableEntryNoRouteRaw](by-memory/0x00550f80-0x00550f8e.MenuQuestionItemListLargerHasSelectableEntryNoRouteRaw.md) begins at `0x00550f80`.

Raw bytes:

```text
00550f20: 55 8B EC 81 EC 08 02 00 00 A1 24 2F 67 00 33 C5
00550f30: 89 45 FC 8B 55 0C 8A 45 08 56 52 88 85 F8 FD FF
00550f40: FF 8B F1 8D 85 FA FD FF FF 68 00 01 00 00 50 E8
00550f50: 03 C7 07 00 83 C4 0C 8D 85 F8 FD FF FF 8B CE 50
00550f60: E8 EB 2C FA FF 8B 4D FC 33 CD 5E E8 BF 67 07 00
00550f70: 8B E5 5D C2 08 00
```

Disassembly:

```asm
00550f20  push    ebp
00550f21  mov     ebp, esp
00550f23  sub     esp, 0x208
00550f29  mov     eax, dword ptr [0x00672f24]
00550f2e  xor     eax, ebp
00550f30  mov     dword ptr [ebp - 4], eax
00550f33  mov     edx, dword ptr [ebp + 0x0c]
00550f36  mov     al, byte ptr [ebp + 8]
00550f39  push    esi
00550f3a  push    edx
00550f3b  mov     byte ptr [ebp - 0x208], al
00550f41  mov     esi, ecx
00550f43  lea     eax, [ebp - 0x206]
00550f49  push    0x100
00550f4e  push    eax
00550f4f  call    0x005cd657
00550f54  add     esp, 0x0c
00550f57  lea     eax, [ebp - 0x208]
00550f5d  mov     ecx, esi
00550f5f  push    eax
00550f60  call    0x004f3c50
00550f65  mov     ecx, dword ptr [ebp - 4]
00550f68  xor     ecx, ebp
00550f6a  pop     esi
00550f6b  call    0x005c772f
00550f70  mov     esp, ebp
00550f72  pop     ebp
00550f73  ret     8
```

The body is executable custom code. It is not padding, a switch table, a thunk, CRT code, or compiler-generated glue.

## Behavior And Source Shape

Observed binary contract:

- `ecx`: target list object, semantically a `MenuQuestionItemListLarger` / `ListPane`-derived receiver.
- `[ebp+8]`: one-byte menu option id / row id.
- `[ebp+0x0c]`: UTF-16 label pointer.
- Return: `ret 8`, matching two stack arguments after the `thiscall` receiver.

The helper builds a temporary row record on the stack:

- row offset `+0`: `optionId`, written from the low byte of `[ebp+8]`;
- row offset `+1`: alignment/padding byte, not explicitly written by this helper;
- row offset `+2`: `wchar_t label[0x100]`, copied with `_wcscpy_s(dest, 0x100, label)`.

It then calls [UID:0003U0][0x004f3bb0-0x004f3f20.ListPaneItemSelectionStorageHelpers](by-memory/0x004f3bb0-0x004f3f20.ListPaneItemSelectionStorageHelpers.md) / `ListPane::AddEntry` at `0x004f3c50`, passing the temporary row pointer. Later live paths consume the first row byte as the selected option id, and the larger draw method [UID:0003WH][0x00550fa0-0x00551021.MenuQuestionItemListLargerDrawRow](by-memory/0x00550fa0-0x00551021.MenuQuestionItemListLargerDrawRow.md) reads the UTF-16 label at `itemData+2`.

Recommended row type wording:

```cpp
struct MenuQuestionOptionRow {
    unsigned char optionId;
    unsigned char alignmentPadding;
    wchar_t label[0x100];
};
```

Do not force a `uint16_t optionId`: this helper writes only one byte. The `+1` byte is best treated as alignment/padding for the UTF-16 label until a consumer proves otherwise.

Route-contingent source shape only:

```cpp
// Candidate only. Keep out of the formal C++ block unless a route is proven.
void MenuQuestionItemListLarger::AddRow(unsigned char optionId, const wchar_t *label)
{
    MenuQuestionOptionRow row;
    row.optionId = optionId;
    wcscpy_s(row.label, 0x100, label);
    AddEntry(&row);
}
```

## Route And Liveness Recheck

B015's local PE route scan against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (`MD5 4247E04E20B65D6414C7238AA8FF5515`, `SHA256 9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`) used image base `0x00400000`, `.text` VA `0x00401000`, `.text` raw `0x00000400`, and `.rdata` VA `0x0060d000`.

Negative evidence for `0x00550f20`:

- no little-endian VA pointer hit;
- no little-endian RVA pointer hit;
- no raw-offset pointer hit for file offset `0x00150320`;
- no `.text` `E8`/`E9` rel32 call or jump target;
- no `.text` `0F 8x` rel32 conditional-branch target;
- no vtable data cell points to `0x00550f20`;
- no constructor call points to `0x00550f20`;
- Ghidra/function-data has zero callers and no IDA counterpart.

Positive controls from the same scan:

- live larger-list constructor `0x00550ed0` has rel32 call hits at `0x0054fd14` and `0x00550489`;
- live larger-list selection delegator `0x00550f90` has a VA data ref at `.rdata` VA `0x00622874`;
- live larger-list draw row `0x00550fa0` has a VA data ref at `.rdata` VA `0x00622878`;
- normal draw row `0x00550e40` has a VA data ref at `.rdata` VA `0x006227b4`.

The positive controls show the scan finds nearby real routes. The lack of a route to `0x00550f20` is meaningful.

## Active Constructor Comparison

The active [UID:0003W4][0x0054fb30-0x00550afb.MenuQuestionDialogLargerConstructor](by-memory/0x0054fb30-0x00550afb.MenuQuestionDialogLargerConstructor.md) performs the same source operation inline when it parses larger-menu labels. It allocates/constructs `MenuQuestionItemListLarger`, reads the item count, converts each byte-length-prefixed menu label to UTF-16, stores `++optionIndex` into a row prefix byte, copies the UTF-16 label with `wcscpy_s(..., 0x100, ...)`, and calls `0x004f3c50` directly.

That inline behavior is the likely live source path. The raw helper is best treated as retained dead/private helper code or an unreferenced out-of-line clone from the same source family. A computed function pointer route remains theoretically possible, but no current caller, vtable slot, VA/RVA pointer, switch table, or route evidence supports it.

## Ownership Analysis

### Best future owner if routed: `MenuQuestionItemListLarger`

- Evidence for: the helper sits immediately after the live larger-list constructor and before larger-list probe/select/draw methods; it mirrors the normal menu-question list row helper; it builds the row consumed by larger-list draw/action code; the live larger constructor constructs this list class and performs the same append operation inline.
- Evidence against: no IDA-modeled function, static caller, vtable slot, VA/RVA/raw pointer, or rel32 branch route reaches this exact start; assigning/emitting it now would add dead duplicate output.
- Decision: semantic class if a route appears, but current metadata remains `CANONICAL_OWNER:NONE`.

### Rejected direct owner: `MenuQuestionDialogLarger`

The dialog constructor inlines equivalent row population, but this helper's `ecx` is the list object and the body calls `ListPane::AddEntry` on that receiver. If live, it would be a list helper, not a dialog method.

### Rejected direct owner: `ListPane`

`ListPane::AddEntry` is a callee/dependency. This helper builds a menu-question-specific row with an option id and label and sits in the MessageDialogs item-list cluster, not in the `ListPane` source family.

### Rejected generated owner: `ChattingColorListPane`

Ghidra/OOAnalyzer function-data labels the body as `ChattingColorListPane::meth_0x550f20`, and generated source-map artifacts mention `0x00550f20` under `class_ChattingColorListPane.cpp.source_map.json`. That route is address-owner pollution: the address cluster is MessageDialogs, the normal/larger menu-question sibling bytes and row layout match this target, there are zero Ghidra callers, and known `ListPane::AddEntry` / selected-entry helpers are generated ownership-pollution hotspots.

## Evidence

- B001 2026-06-16 raw-byte audit found the larger-list row insertion clone and confirmed that IDA does not model a function at `0x00550f20`.
- B015 2026-06-19 source-routing report confirmed exact bytes, disassembly, stack row layout, `_wcscpy_s` call, `ListPane::AddEntry` dependency, no-route scans, positive controls, active constructor inline equivalent, and generated `ChattingColorListPane` ownership rejection.
- The combined Ghidra function-data JSON has `meth_0x550f20` with zero callers and no IDA function; its `ChattingColorListPane` route contradicts the MessageDialogs cluster and is rejected as generated owner pollution.
- The active generated `MessageDialogs.cpp` omits this helper while retaining live larger-list constructor/select/draw pages.

## Exact No-Code Proof

Formal C++ stays blank because:

1. No static rel32 call/jump/conditional branch targets `0x00550f20`.
2. No little-endian absolute VA, RVA, or raw-offset pointer routes to the helper start.
3. Nearby positive controls recover live constructor and vtable references, proving the negative result is not a blind scan.
4. Existing IDA/B001 evidence found no function object and no `xrefs_to` route to the helper start.
5. The active larger constructor already inlines row append behavior and calls `ListPane::AddEntry` directly.
6. Emitting this helper now would add dead retained code with no proven source-use surface.

## Score Rationale

Completion rises to `88` because the page now records exact raw bytes and disassembly, padding boundaries, source-shaped signature, stack row layout, `_wcscpy_s` / `ListPane::AddEntry` behavior, active-constructor comparison, no-route evidence with positive controls, generated-owner pollution rejection, ranked owner alternatives, and target-specific no-code proof.

Confidence rises to `88` because behavior and non-emitting disposition are strongly supported. It stays below `90` because no live route or original source symbol is proven, and the precise reason the body was retained is inferred rather than directly known.

## Cross-References

- [UID:000080][MenuQuestionItemListLarger](by-class/MenuQuestionItemListLarger.md) `MenuQuestionItemListLarger`
- [UID:0001FH][0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs](by-memory/0x0054ce10-0x00551021.NpcMessageAndMenuQuestionDialogs.md) `NpcMessageAndMenuQuestionDialogs`
- [UID:0000LA][MessageDialogs](by-file/MessageDialogs.md) `MessageDialogs`
- [UID:00007A][ListPane](by-class/ListPane.md) `ListPane`
- [UID:0003WD][0x00550ed0-0x00550f1f.MenuQuestionItemListLargerConstructor](by-memory/0x00550ed0-0x00550f1f.MenuQuestionItemListLargerConstructor.md)
- [UID:0003WF][0x00550f80-0x00550f8e.MenuQuestionItemListLargerHasSelectableEntryNoRouteRaw](by-memory/0x00550f80-0x00550f8e.MenuQuestionItemListLargerHasSelectableEntryNoRouteRaw.md)
- [UID:0003WH][0x00550fa0-0x00551021.MenuQuestionItemListLargerDrawRow](by-memory/0x00550fa0-0x00551021.MenuQuestionItemListLargerDrawRow.md)

## Changes

- 2026-06-22 supervisor Rule 26 incorporation of B015 `0003WE-MenuQuestionItemListLargerAddRowNoRouteRaw-source-routing.md`:
  - Before: `85/86`, shallow no-route candidate summary, no coverage row, and no target-specific no-code proof.
  - After: `88/88`, still `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`, and blank formal C++.
  - Summary/evidence: B015 confirmed exact `0x56`-byte raw body, stack-cookie row append signature, row layout with option id byte at `+0` and UTF-16 label at `+2`, `_wcscpy_s` and `ListPane::AddEntry` calls, boundary padding, no VA/RVA/raw/rel32/vtable route to `0x00550f20`, positive controls for neighboring live constructor/vtable routes, active larger constructor inline equivalent, generated `ChattingColorListPane` ownership rejection, and exact no-code proof.
