*** UID:0003O7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CW | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SelfSaveInputPane::SendSelfSavePacket()
{
    unsigned char packet[16];

    PacketBufferWriteUInt8(0x25, packet);
    packet[1] = 0;

    g_packetSender->QueueAndSendPacket(packet, 1);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private nonvirtual SelfSaveInputPane packet member retained out of line after its call was inlined into OnConfirmInput; exact 65-byte body uses opcode 0x25, frame/base/cookie tuple 0x14/-0x14/-4, writes offsets 0/1, explicitly clears byte one, and sends length one through Socket-owned g_packetSender; ranked same-binary and compiler controls select inferred unsigned char packet[16] while preserving smaller-array-plus-padding as a confidence cap, and exhaustive no-route evidence retains active-vtable/queue/global positive controls. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b6870-0x005b68b1 SelfSaveInputPane Send Self Save Packet Raw

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: retained raw packet helper-shaped `SelfSaveInputPane` body.
- Owner: [UID:0000CW][SelfSaveInputPane](by-class/SelfSaveInputPane.md) `SelfSaveInputPane` class.
- Best source-facing name: `SelfSaveInputPane::SendSelfSavePacket`.
- Best source-facing signature: private nonvirtual `void SelfSaveInputPane::SendSelfSavePacket()` with implicit `this` and no explicit parameters.
- Source route: [UID:0000NM][SelfSaveInputPane](by-file/SelfSaveInputPane.md) `SelfSaveInputPane` / command-input source family. [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md) remains the likely final consolidation context, but moving only this retained helper would split the SelfSave family.
- Aggregate context: [UID:0001MU][0x005b67c0-0x005b68b1.SelfSaveInputPane](by-memory/0x005b67c0-0x005b68b1.SelfSaveInputPane.md) `SelfSaveInputPane` method island.
- Reconstruction note: the formal definition is the single human-source helper body. The active virtual handler calls it in source, while release optimization inlines the same send core into UID0003O6 and retains this out-of-line definition. The no-route state remains an exact binary liveness caveat, not a compiler-glue, covered-by, or padding classification.

## Address Range

`0x005b6870-0x005b68b1`, exclusive end. IDA currently reports `0x005b6870` as `Not a function`.

The byte at `0x005b68b0` is the helper's final `ret`, not alignment. The following `0x005b68b1-0x005b68c0` bytes are fifteen `0xcc` alignment bytes before the block-list input pane neighborhood at `0x005b68c0`.

| Range | Classification | Evidence |
| --- | --- | --- |
| `0x005b686c-0x005b6870` | predecessor alignment | Four `0xcc` bytes after [UID:0003O6][0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput](by-memory/0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput.md). |
| `0x005b6870-0x005b68b1` | retained raw packet helper | Complete prologue/body/epilogue ending with `pop ebp` at `0x005b68af` and `ret` at `0x005b68b0`. |
| `0x005b68b1-0x005b68c0` | successor alignment | Fifteen `0xcc` bytes before [UID:0001MV][0x005b68c0-0x005b6900.BlockListenInputPaneConstructor](by-memory/0x005b68c0-0x005b6900.BlockListenInputPaneConstructor.md). |

## Behavior

The helper is a send-only self-save packet body:

- Executes exact `sub esp,0x14`, takes the packet base at `[ebp-0x14]`, stores the security cookie at `[ebp-4]`, touches packet offsets `0` and `1`, and sends length `1`. Those are binary facts; the selected `unsigned char packet[16]` source declaration is a high-probability inference, not interval-only proof.
- Writes opcode `0x25` through [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md), best source-facing name `PacketBufferWriteUInt8`.
- Clears `packet[1]` after the byte-write helper. This is a scratch terminator/safety byte and is not transmitted.
- Loads [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) from `0x0067a7ec`.
- Calls [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md) with `ECX = g_packetSender`, represented in source as `g_packetSender->QueueAndSendPacket(packet, 1)`.
- Reads no input bytes, no `SelfSaveInputPane` fields, no object state, no feature flag, and no localized string.

Opcode `0x25` is the self-save confirmation request opcode. [UID:0003O6][0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput](by-memory/0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput.md) contains the same packet-send tail after it validates one-character input as `y` or `Y`, so the active virtual handler sends the packet inline rather than calling this retained helper in the current binary.

B012's 2026-06-19 factory reanalysis confirms [UID:0001KQ][0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory](by-memory/0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory.md) is a retained creation helper only. It allocates and constructs `SelfSaveInputPane` as `CreateSelfSaveInputPane()` but does not call this raw packet helper and does not send opcode `0x25`.

Rejected behavior interpretations: this is not a generic queue/send wrapper, not a `g_packetSender` owner, not `SelfSaveOKPane` behavior, not a compiler helper, and not padding. The body constructs exactly one fixed feature packet and touches only the shared packet sender.

## Raw Body Evidence

- Live IDA MCP `lookup_funcs 0x005b6870` reports `Not a function`.
- Prior live `insn_query 0x005b6870-0x005b68b0` decoded a raw helper-shaped body with security-cookie setup/check and packet sender calls. B007's 2026-06-19 raw PE/Capstone recheck extends that body through the missing `ret` byte at `0x005b68b0`.
- `xrefs_to 0x005b6870` reports no direct references.
- Earlier `get_bytes 0x005b68ac` evidence was one byte short in the written range. Correct interpretation: `0x005b68af` is `pop ebp`, `0x005b68b0` is `ret`, and alignment starts at `0x005b68b1`.

```asm
005B6870  push    ebp
005B6871  mov     ebp, esp
005B6873  sub     esp, 14h
005B6876  mov     eax, dword ptr [672F24h]
005B687B  xor     eax, ebp
005B687D  mov     dword ptr [ebp-4], eax
005B6880  lea     eax, [ebp-14h]
005B6883  push    eax
005B6884  push    25h
005B6886  call    575380h
005B688B  mov     ecx, dword ptr [67A7ECh]
005B6891  lea     eax, [ebp-14h]
005B6894  add     esp, 8
005B6897  mov     byte ptr [ebp-13h], 0
005B689B  push    1
005B689D  push    eax
005B689E  call    574BB0h
005B68A3  mov     ecx, dword ptr [ebp-4]
005B68A6  xor     ecx, ebp
005B68A8  call    5C772Fh
005B68AD  mov     esp, ebp
005B68AF  pop     ebp
005B68B0  ret
```

## Route / Liveness Evidence

B007 2026-06-19 whole-image PE scan found no ordinary static route to this helper:

| Scan target | Result | Meaning |
| --- | --- | --- |
| `0x005b6870` | no absolute VA pointer, no RVA pointer, no raw-offset pointer | No ordinary data/table pointer route to the helper start. |
| `0x005b6870-0x005b68b1` | no pointer to any byte in the corrected target range | No mid-body target-range pointer route. |
| `0x005b6870` and corrected range | no rel32 call, rel32 jump, or rel32 conditional branch hit | No direct code route into the helper. |
| first 32 target bytes | one whole-image signature hit, at this raw offset only | No duplicate routed copy elsewhere. |
| positive control `0x005b6800` | vtable pointer found at raw `0x22f008`, corresponding to slot `0x00630608` | The scanner found the known active confirm-handler vtable route. |
| positive control `0x0067a7ec` | 489 absolute VA hits | The scanner found the high-fanout `g_packetSender` global references. |

Conclusion: the helper remains no-route/liveness-capped. The best current explanation is a retained private source helper in the SelfSave prompt family or an older helper whose live virtual handler was later inlined. No evidence supports a hidden vtable route, a factory route, a pointer table route, or a BlockListen owner.

B012 separately scanned the factory start `0x005aa140` and found no route there either, while finding the same positive controls for the active confirm handler, `CharInputPane::CharInputPane`, and operator-new wrapper. The two retained no-route bodies should remain distinct: this page is the opcode `0x25` packet helper, and [UID:0001KQ][0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory](by-memory/0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory.md) is the `new SelfSaveInputPane` factory.

## Source-Quality Decision

Best-supported source-facing name and signature:

```cpp
void SelfSaveInputPane::SendSelfSavePacket();
```

Private nonvirtual membership is strongest even though the body does not read `ECX`: Wield's sole adjacent helper call at `0x005b64e6` explicitly executes `mov ecx, esi` before calling a helper that likewise does not consume object state, and Wear supplies a second adjacent retained/inlined helper-family parallel. File-static and class-static forms fail to explain that same-family object-call convention; virtual membership is rejected because no helper vtable slot exists.

Rejected names:

- `SelfSaveInputPaneSendSelfSavePacketRaw`: documentation filename label, not plausible original source.
- `SubmitSelfSavePacket`: implies input validation, which this body does not perform.
- `QueueSelfSavePacket`: overstates queue ownership; the body constructs a packet and delegates queue/send.
- `SendSaveProfilePacket`: semantically plausible, but less tied to the documented `SelfSaveInputPane` feature/source names than `SendSelfSavePacket`.

Ownership/source placement stays on [UID:0000CW][SelfSaveInputPane](by-class/SelfSaveInputPane.md) because the helper is class-specific SelfSave prompt behavior and the class parent clears the gate. The private declaration belongs in the class, the out-of-line definition belongs in [UID:0000NM][SelfSaveInputPane](by-file/SelfSaveInputPane.md), and the active call belongs in `OnConfirmInput`. [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md) remains only a coordinated whole-unit historical grouping alternative.

## C++ Reconstruction Notes

The formal C++ block intentionally omits security-cookie setup/check, stack offsets, and IDA temporary labels. It models the source-authored behavior: infer a fixed 16-byte scratch declaration, write the one-byte self-save opcode, preserve the explicit redundant byte-one clear, and queue exactly one byte through the shared Socket sender.

The retained no-route caveat does not block formal source because this is a coherent unique project body, `RECONSTRUCTABLE:TRUE`, owner/emitter UID0000CW is confirmed, and source-level liveness is explained by an inlined call. Covered-by/no-code is rejected: the out-of-line body is retained source, while duplicate handwritten active/helper bodies are rejected in favor of one definition and one source call.

## Score Rationale

Completion is `92` because exact range/hash/instructions, no-route scans, normalized duplicate identity, family member ABI, sender/writer contracts, source placement, formal member definition, and ranked source-capacity decision are resolved.

Confidence is `92` because the exact target and active cores agree after rel32 normalization, Wield/Wear corroborate the member/inlining source shape, and the closest x86 MSVC control favors `[16]`. It remains capped because no ordinary target route, original symbol/header, exact linker-era 14.16 compiler, or conclusive array declaration survives; `packet[2]` plus exact-toolchain padding remains the bounded runner-up.

## Historical 2026-07-13 B002 UID0002N8 Helper Synchronization

The statements in this subsection record the B002 evidence-time file-static/two-byte source model. They are preserved for audit but superseded by the B004 source-family ABI, normalized-clone, and compiler-layout analysis below.

The accepted formal helper body and `87/88` score remain unchanged. Current target revalidation confirms the exact source dependencies and ABI exclusion: raw range `[0x005b6870,0x005b68b1)` ends with `ret` at `0x005b68b0`; `0x005b68b1-0x005b68c0` is successor alignment; no object pointer or SelfSave field is read; and the body has no vtable slot.

The helper allocates `char packet[2]`, passes opcode `0x25` through `PacketBufferWriteUInt8`, clears `packet[1]`, loads shared `g_packetSender` at `0x0067a7ec`, and queues exactly one transmitted byte through `QueueAndSendPacket`. The second byte remains non-transmitted scratch/terminator state. There is no input read, prompt lookup, null check, parse, state mutation, pane close, error branch, or cleanup beyond normal compiler mechanics.

Active [UID:0003O6][0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput](by-memory/0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput.md) is independently routed by primary slot `0x00630608` and duplicates this packet-send tail inline after exact one-character `y`/`Y` validation. It does not call this retained helper. Replacing either body with a source call would change the observed call graph and is rejected.

Whole-image scans still find no rel32 call/jump/conditional branch, absolute VA/RVA/raw-offset pointer, or pointer into this exact range. Positive controls find the active handler vtable pointer and broad `g_packetSender` fanout, so the negative route result is meaningful. The coherent complete body, class-specific opcode, adjacency, and duplicate active send tail support retained source rather than padding, dead bytes, generic network code, SelfSaveOKPane action, or compiler glue.

This helper remains file-local in an unnamed namespace and class-owned/emitted by [UID:0000CW][SelfSaveInputPane](by-class/SelfSaveInputPane.md) at position `30`, after constructor `10` and active handler `20`, before UID0002N8 marker `40`. Immediate output remains [UID:0000NM][SelfSaveInputPane](by-file/SelfSaveInputPane.md). No class-static declaration is added because there is no `this`, vtable, caller, or declaration evidence for membership.

The exact target's scalar deleting destructor, `+0xa0/+0xa4` adjustors, three vtable views, and RTTI cells remain compiler ABI and unrelated to this source helper. The no-route result, inferred original spelling, and historical file grouping remain confidence caps; they do not block the accepted first draft.

## 2026-07-13 B004 Source Disposition And Packet-Layout Reanalysis

The current source decision preserves exact binary evidence separately from inferred human source. Exact target bytes hash to SHA256 `dc9ae9a3ddef298473476fe633df122c17a9cc7d91d8e266c6f0c77549224c4f`. The body is one 65-byte function-shaped sequence with a conventional frame, security cookie, provider calls, epilogue, and plain `ret`; IDA's missing function object is a database-modeling omission rather than padding or a no-code reason. Four predecessor `0xcc` bytes and fifteen successor `0xcc` bytes remain outside the exact range.

The target and active UID0003O6 send cores are 35 bytes each. They differ only at the two position-dependent rel32 call displacements; after zeroing those fields, both hash to `3844f03d9ccc79066d190bb05f2908454eeaaee2844c1b056762124a3f57b093`, and a wildcard whole-image search finds exactly those two hits. The human source therefore has one private helper definition and one call from `OnConfirmInput`; release optimization inlines that call while retaining this out-of-line definition. Emitting two handwritten packet bodies would preserve a decompiler-shaped binary accident rather than the highest-probability source.

Private nonvirtual membership is supported by the same source family. Wield's sole helper call at `0x005b64e6` prepares `ECX=this` with `mov ecx, esi` before calling a helper that itself does not read `ECX`; Wear supplies another adjacent retained-helper/active-inline parallel. These controls show that absence of an object-field read does not imply file scope. A private nonvirtual member also correctly requires no vtable slot. File-static, class-static, virtual, compiler-only clone, covered-by/no-code, generic network ownership, SelfSaveOKPane ownership, BlockListen ownership, and target-only CommandInputPanes migration are rejected.

Exact packet facts are: `sub esp,0x14`, packet address `[ebp-0x14]`, security cookie `[ebp-4]`, writer call at `0x005b6886`, explicit zero at packet offset `1`, `ECX` load from `0x0067a7ec`, send call at `0x005b689e`, and transmitted length `1`. The target reads no input, field, feature flag, localized string, or second payload byte; it performs no sender null guard, pane close/reset, branch, or state mutation. `PacketBufferWriteUInt8` is value-first and already writes a spare zero, but the separate byte-one clear appears in both exact copies and must remain. `g_packetSender` is `Socket *`, so the source call is member syntax.

The 16-byte source declaration is selected by ranked controls rather than claimed from the frame interval alone. IDA proves only the address-taken base, two touched bytes, and cookie boundary. Same-binary MainMenu and Option reconstructions demonstrate that a pre-cookie interval can exceed the reconstructed packet declaration; Wield, Wear, and Emotion demonstrate intentional 128-byte packet locals in the same broad source family. PE headers identify x86 linker `14.16`, but no matching `cl.exe` is available. The closest installed x86 MSVC 14.44 probe used `/O2 /GS /Oy-` plus `strict_gs_check(on)` and produced frame/base/cookie tuples `[1] -> 0x8/-0x5/-0x4`, `[2] -> 0x8/-0x8/-0x4`, `[4] -> 0x8/-0x8/-0x4`, `[8] -> 0xc/-0xc/-0x4`, and `[16] -> 0x14/-0x14/-0x4`. Only `[16]` matches the target tuple. Thus `unsigned char packet[16]` is the highest-probability source form, while `unsigned char packet[2]` plus exact-toolchain padding/alignment remains the bounded runner-up and confidence cap. `packet[1]` is invalid because source explicitly writes index one.

Whole-image liveness checks remain negative: no direct xref, rel32 call/jump/conditional branch, absolute VA, RVA, raw-offset pointer, target-interior pointer, factory edge, constructor edge, table entry, string, or vtable slot reaches `0x005b6870`. The scan's active-handler vtable pointer at `0x00630608`, 416 direct calls to `0x00574bb0`, and 489 absolute references to `0x0067a7ec` are positive controls. This establishes no ordinary runtime entry while preserving source-level liveness through the compiler-inlined call. Original spelling, exact access-token placement, exact compiler flags, exact `[16]` capacity, and standalone-versus-broader translation-unit history remain bounded uncertainties; none changes owner UID0000CW, emitter UID0000CW, position `30`, `Nested:0`, exact behavior, or formal method contract.

## Cross-References

- [UID:0001MU][0x005b67c0-0x005b68b1.SelfSaveInputPane](by-memory/0x005b67c0-0x005b68b1.SelfSaveInputPane.md)
- [UID:0000CW][SelfSaveInputPane](by-class/SelfSaveInputPane.md)
- [UID:0000NM][SelfSaveInputPane](by-file/SelfSaveInputPane.md)
- [UID:0003O6][0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput](by-memory/0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput.md)
- [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md)
- [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)
- [UID:0001P0][0x0067a7ec-0x0067a7f0.g_packetSender](by-memory/0x0067a7ec-0x0067a7f0.g_packetSender.md)
- [UID:0000ID][CommandInputPanes](by-file/CommandInputPanes.md)
- [UID:0000HS][BlockListenInputPanes](by-file/BlockListenInputPanes.md)

## Changes

- 2026-06-13 Agent-A001: Split this exact raw packet helper out of the `SelfSaveInputPane` method island and recorded current raw-start, no-xref, packet-send, and successor-padding evidence.
- 2026-06-19 Agent-B007 implementation: Renamed the helper to the corrected half-open range `0x005b6870-0x005b68b1`, raised `85/86 -> 87/88`, emitted first-draft file-local `SendSelfSavePacket()` C++, and recorded the PE recheck proving `0x005b68b0` is `ret`, padding begins at `0x005b68b1`, no rel32/pointer route is known, and [UID:0003O6][0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput](by-memory/0x005b6800-0x005b686c.SelfSaveInputPaneConfirmInput.md) duplicates the opcode `0x25` send tail inline.
- 2026-06-21 Rule 26 B012 factory sync: Scores unchanged; recorded that [UID:0001KQ][0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory](by-memory/0x005aa140-0x005aa1bf.SelfSaveInputPaneFactory.md) is a separate retained creation helper with no call to this raw packet helper, and that both no-route bodies should remain distinct unless a real route appears.
- 2026-07-13 Agent-B002 UID0002N8 callback: preserved the accepted body and `87/88`, set position `30`, synchronized exact one-byte packet behavior, active-handler inline duplication, no-route positive controls, target/compiler-wrapper distinction, deterministic source order, file-local ownership, and rejected member/generic/dead-code alternatives.
- 2026-07-13 Agent-B004 UID0003O7 callback: raised `87/88 -> 92/92`, replaced the historical file-static/two-byte/free-sender source with private nonvirtual `SelfSaveInputPane::SendSelfSavePacket`, preserved one source definition plus one inlined active call, separated exact frame/base/cookie/write/send facts from inferred `unsigned char packet[16]`, retained `[2]` plus exact-toolchain padding as the bounded runner-up, and preserved all no-route, provider, range, historical grouping, and rejected-alternative evidence.
