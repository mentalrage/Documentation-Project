*** UID:0002PZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00000V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00000V | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
BaramApp::BaramApp(HINSTANCE hInstance,
                   const wchar_t *publisherName,
                   const wchar_t *applicationTitle)
    : Application(hInstance, publisherName, applicationTitle)
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready out-of-line BaramApp constructor body; live IDA confirms exact bytes, Application__Constructor forwarding, primary/secondary BaramApp vtable writes, no promoted function object, no direct start xrefs, and padding boundaries; formal C++ is the empty derived constructor with an Application base initializer. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f5f20-0x004f5f4e BaramApp Constructor Raw Body

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: unpromoted constructor-shaped body.
- Owner class: [UID:00000V][BaramApp](by-class/BaramApp.md).
- Likely source module: [UID:0000HG][Application](by-file/Application.md).
- Parent aggregate: [UID:00019H][0x004f5f20-0x004f66fb.BaramAppAndApplicationStartup](by-memory/0x004f5f20-0x004f66fb.BaramAppAndApplicationStartup.md).
- Autogen status: attached to [UID:00000V][BaramApp](by-class/BaramApp.md) because both the child and class page clear the documented assignment gate; this page now emits the first-draft `BaramApp` constructor through the class route. The original header/source split is still inferred, but that is a confidence cap rather than a no-code blocker.

## Behavior

This raw body constructs the `Application` base object by forwarding the three incoming constructor arguments to `Application__Constructor` at `0x00463310`, then installs the `BaramApp` primary and secondary vtable pointers at offsets `+0` and `+4`. It performs no other member initialization and returns `this` with `retn 0Ch`, matching a three-argument `__thiscall` constructor.

IDA does not currently promote `0x004f5f20` as a function, and no direct code/data xref points at the constructor start. That remains negative IDA-model and liveness evidence, not a proof that the body is not source-authored. The byte sequence is a complete constructor body, and the same base-constructor call plus primary/secondary vtable-write pattern is also observed inline in `_WinMain@16` when it stack-constructs the application shell.

The source-facing reconstruction is an empty `BaramApp::BaramApp(HINSTANCE hInstance, const wchar_t *publisherName, const wchar_t *applicationTitle)` body with an `Application(hInstance, publisherName, applicationTitle)` base initializer. `HINSTANCE` is inferred from `_WinMain@16` and the `Application` constructor store to the instance-handle field. `publisherName` and `applicationTitle` are inferred from the current `WinMain` source-facing `BaramApp app(hInstance, kPublisherName, kApplicationTitle);` shape and the push order into `Application__Constructor`. The exact original parameter names remain inferred.

## IDA MCP Evidence

- `lookup_funcs` on 2026-06-01 reports no function start at `0x004f5f20`, while the previous promoted function is `_WinMain@16` ending at `0x004f5f17` and the next promoted function starts at `0x004f5f50`.
- Raw bytes at `0x004f5f17-0x004f5f20` are nine `0xcc` alignment bytes before this body.
- Disassembly from `0x004f5f20` shows `push ebp`, `mov ebp, esp`, argument pushes from `[ebp+8]`, `[ebp+0xc]`, and `[ebp+0x10]`, a call to `Application__Constructor` at `0x004f5f33`, writes of `0x0061d118` and `0x0061d138`, and `retn 0Ch` ending at `0x004f5f4e`.
- `xrefs_to 0x0061d118` and `xrefs_to 0x0061d138` report data writes at `0x004f5f38` and `0x004f5f40`.
- Bytes at `0x004f5f4e-0x004f5f50` are `0xcc` alignment before `0x004f5f50`.
- 2026-06-05 A003 IDA MCP refresh reconfirmed no function object contains `0x004f5f20` or `0x004f5f33`, while raw disassembly still shows the complete constructor-shaped body, the `Application__Constructor` call, both `BaramApp` vtable writes, and `0xcc` alignment before and after the range.
- 2026-06-06 A004 IDA MCP `lookup_funcs` reconfirmed that `0x004f5f20`, `0x004f5f33`, and `0x004f5f4e` are not promoted function starts, while the next promoted callback function remains `0x004f5f50-0x004f5faf` and the callee remains `Application__Constructor` at `0x00463310-0x0046396e`.
- 2026-06-06 A004 IDA MCP `disasm` confirms `ecx` is saved as `this` in `esi`, the raw body forwards `[ebp+0x8]`, `[ebp+0xc]`, and `[ebp+0x10]` to `Application__Constructor`, writes `??_7BaramApp@@6B@` to `[esi]`, writes `??_7BaramApp@@6B@_0` to `[esi+4]`, returns `esi` through `eax`, and ends with `retn 0Ch`.
- 2026-06-06 A004 IDA MCP `xrefs_to` reports no direct xrefs to `0x004f5f20`; `Application__Constructor` has call refs from `_WinMain@16` at `0x004f5cca` and this raw body at `0x004f5f33`, and the `BaramApp` vtable addresses have paired writes from `_WinMain@16` plus this raw body.
- 2026-06-06 A004 IDA MCP byte audit confirms `0x004f5f17-0x004f5f20` is nine `0xcc` bytes, `0x004f5f20-0x004f5f4e` is the exact constructor body bytes, and `0x004f5f4e-0x004f5f50` is two `0xcc` alignment bytes.
- 2026-06-16 A002 live IDA MCP session `b001_mappane_0001AW_20260616` reconfirmed the current function state: `lookup_funcs` still reports no function object at `0x004f5f20`, `0x004f5f33`, or `0x004f5f4e`; the next function is the modeled startup callback at `0x004f5f50-0x004f5faf`; `get_bytes` shows the constructor body is `0x2e` / 46 bytes, verified with `int_convert.py`; and the surrounding `0x004f5f17-0x004f5f20` and `0x004f5f4e-0x004f5f50` spans remain `0xcc` padding.
- The same 2026-06-16 `insn_query` pass disassembled the raw body as `ecx`-based `this` construction: it saves `ecx` in `esi`, forwards three stack arguments to `Application__Constructor` at `0x004f5f33`, writes the primary `BaramApp` vtable `0x0061d118` to `[esi]`, writes the secondary view `0x0061d138` to `[esi+4]`, returns `esi`, and ends with `retn 0Ch`.
- 2026-06-16 `xrefs_to` again found no references to the constructor start, but confirmed the internal call xref at `0x004f5f30 -> 0x004f5f33` and the vtable data writes at `0x004f5f38` and `0x004f5f40`. This keeps the range documented as real constructor-shaped code while making the no-function/no-start-xref blocker explicit.
- 2026-06-28 B003 accepted report `tools/leaser/Agents/Agent-B003/research/0002PZ-BaramAppConstructorRaw-empty-emitter-source-quality.md` rechecked the same range through MCP session `supervisor_20260628_resume`. `lookup_funcs` still reports no function at `0x004f5f17`, `0x004f5f20`, `0x004f5f33`, or `0x004f5f4e`, while the next function remains `0x004f5f50-0x004f5faf` and `Application__Constructor` remains `0x00463310-0x0046396e`.
- The B003 byte pass records the exact constructor bytes: `55 8b ec 51 56 ff 75 10 8b f1 ff 75 0c 89 75 fc ff 75 08 e8 d8 d3 f6 ff c7 06 18 d1 61 00 8b c6 c7 46 04 38 d1 61 00 5e 8b e5 5d c2 0c 00`. The wider byte pass again shows nine `0xcc` bytes before the constructor and two `0xcc` bytes after it.
- The B003 instruction pass records the same 17-instruction constructor sequence: prologue, save `ecx`/`this` in `esi`, push `[ebp+10h]`, `[ebp+0Ch]`, and `[ebp+8]` into `Application__Constructor`, write `??_7BaramApp@@6B@` to `[esi]`, return `esi` through `eax`, write `??_7BaramApp@@6B@_0` to `[esi+4]`, restore registers/frame, and `retn 0Ch`.
- B003 `xrefs_to(0x004f5f20)` and an absolute-pointer search for `0x004f5f20` found no direct route to the raw start. `xrefs_to(0x00463310)` found the `_WinMain@16` call at `0x004f5cca` and this raw-body call at `0x004f5f33`; `xrefs_to(0x0061d118)` found `_WinMain@16` at `0x004f5ccf` and this body at `0x004f5f38`; `xrefs_to(0x0061d138)` found `_WinMain@16` at `0x004f5cd9` and this body at `0x004f5f40`.
- B003 `get_global_value` over the BaramApp vtable region reconfirmed the primary slots at `0x0061d118-0x0061d134`, RTTI/locator pointer `0x0061d134 -> 0x0064a014`, secondary slot `0x0061d138 -> 0x004f66b5`, and secondary follow-up slot `0x0061d13c -> 0x00465ce0`. `entity_query` still names `0x0061d118` as `??_7BaramApp@@6B@` and `0x0061d138` as `??_7BaramApp@@6B@_0`.
- B003 `decompile(0x004f5f20)` failed because no IDA function object exists. `_WinMain@16` decompilation has a bad-stack warning but still exposes the inlined application construction sequence: a call to `Application__Constructor`, then writes of the primary and secondary `BaramApp` vtables. The source-facing [UID:00019G][0x004f5c80-0x004f5f17.WinMain](by-memory/0x004f5c80-0x004f5f17.WinMain.md) page is the stronger evidence and already reconstructs `BaramApp app(hInstance, kPublisherName, kApplicationTitle);`.

## Reconstruction Notes

Treat this as real `BaramApp` construction logic despite the missing IDA function object. Do not use this body to infer additional `BaramApp` fields beyond the compiler-installed primary and secondary vptrs.

The formal source shape is a derived constructor with an empty body and an `Application` base initializer. The explicit vtable stores are compiler output from the class declaration and constructor; do not write raw vptr-store C++ in the formal block.

Rejected alternatives from the B003 source-quality pass:

- Keeping the formal C++ blank is rejected because the current code-entry gate is met and the constructor has a safe first-draft source shape.
- A comment-only covered-by marker is rejected because the range is not raw vtable bytes, padding, RTTI, or unavoidable compiler-only glue; it is a complete constructor body.
- Clearing `EMITTER_UIDS` or setting `RECONSTRUCTABLE:FALSE` is rejected because this is NexusTK project code.
- Emitting raw vtable writes is rejected because `BaramApp` class declarations and this constructor cause the compiler to produce those stores.
- Moving ownership to `Application` or `_WinMain@16` is rejected. `Application__Constructor` is the base-initializer callee, and `_WinMain@16` proves an inlined stack-construction site, not ownership of this out-of-line body.
- Promoting or renaming an IDA function at `0x004f5f20` is rejected for this documentation pass because the start has no direct route and IDA DB edits are out of scope.

Autogen routing remains through [UID:00000V][BaramApp](by-class/BaramApp.md), which itself routes through [UID:0000HG][Application](by-file/Application.md). A separate `app/BaramApp.cpp` remains possible but unproven; current reconstruction keeps `BaramApp` in the application-shell file route.

## Score Rationale

- Completion is `89`: exact bytes, surrounding padding, raw disassembly, constructor-shaped behavior, callee, vtable writes, sibling `_WinMain@16` evidence, callback/startup-neighborhood context, parent attachment, no-function/no-start-xref negative evidence, rejected alternatives, and formal first-draft constructor C++ are documented.
- Confidence is `92`: repeated live IDA/MCP refreshes match the stored evidence and confirm the byte-level range, call target, vtable writes, `_WinMain@16` inline construction corroboration, and parent class relationship. Confidence stays below final-source level because the start still has no direct xrefs, IDA still does not model it as a function, and the original header/source-file split plus exact parameter names remain inferred.

## Cross-References

- [UID:00000V][BaramApp](by-class/BaramApp.md)
- [UID:0000HG][Application](by-file/Application.md)
- [UID:00019H][0x004f5f20-0x004f66fb.BaramAppAndApplicationStartup](by-memory/0x004f5f20-0x004f66fb.BaramAppAndApplicationStartup.md)
- [UID:00025P][0x0061d118-0x0061d264.BaramAppReadOnlyData](by-memory/0x0061d118-0x0061d264.BaramAppReadOnlyData.md)

## Changes

- 2026-06-28 B003 BaramApp constructor empty-emitter resolution:
  - What existed before: the page was `85/90`, reconstructable, BaramApp-owned/emitted, and documented as constructor-shaped but kept formal C++ blank because no IDA function object, no direct start xref, and source split/name uncertainty were treated as final-source blockers.
  - What changed: raised to `89/92` and inserted formal first-draft `BaramApp::BaramApp(HINSTANCE hInstance, const wchar_t *publisherName, const wchar_t *applicationTitle)` with an `Application(hInstance, publisherName, applicationTitle)` base initializer and empty body.
  - Summary/evidence: accepted B003 report `tools/leaser/Agents/Agent-B003/research/0002PZ-BaramAppConstructorRaw-empty-emitter-source-quality.md`, MCP session `supervisor_20260628_resume`, exact bytes/instructions for `0x004f5f20-0x004f5f4e`, no function object/no direct start xrefs/no absolute pointer hits, `Application__Constructor` call at `0x004f5f33`, primary/secondary vtable stores at `0x004f5f38` and `0x004f5f40`, `_WinMain@16` inlined construction at `0x004f5cca`/`0x004f5ccf`/`0x004f5cd9`, and `0xcc` padding before/after the body. Raw vptr-store C++, comment-only coverage, no-emitter disposition, and moving ownership to `Application` or `_WinMain@16` were rejected.
- 2026-06-16 A002 Goal 2 BaramApp startup/callback refresh:
  - What existed before: the page remained `80/88` even though exact bytes, vtable writes, and parent attachment were already documented; the remaining no-function/no-start-xref blocker had not been rechecked in the current IDA session.
  - What changed: raised to `85/90`, added current `lookup_funcs`, `get_bytes`, `insn_query`, `xrefs_to`, and `int_convert.py` evidence, and kept final C++ blank.
  - Summary/evidence: live IDA confirms the exact `0x004f5f20-0x004f5f4e` constructor-shaped body, `0x2e` / 46-byte size verified with `int_convert.py`, `Application__Constructor` forwarding, primary/secondary `BaramApp` vtable writes, no promoted function object, no direct start xrefs, and `0xcc` padding before and after the raw body. No IDA function definition was applied because promoting a raw no-start-xref body would overstate the callable boundary.
- 2026-06-06 A004 parent-attachment and byte-evidence pass:
  - What existed before: the page was scored `72/86`, reconstructable, and still unassigned because it carried stale text saying [UID:00000V][BaramApp](by-class/BaramApp.md) had not cleared the parent-attachment gate.
  - What it was changed to: raised to `80/88`, attached to [UID:00000V][BaramApp](by-class/BaramApp.md) at position `1`, updated the autogen-status/reconstruction notes, and kept the final C++ block blank.
  - Summary/evidence: IDA MCP `lookup_funcs`, `disasm`, `xrefs_to`, and byte audit on 2026-06-06 reconfirm no promoted function at `0x004f5f20`, exact `0xcc` padding at `0x004f5f17-0x004f5f20` and `0x004f5f4e-0x004f5f50`, argument forwarding to `Application__Constructor`, primary/secondary `BaramApp` vtable writes, and the now-qualified `80/86` parent class.
- 2026-06-05 A003 autogen-gate cleanup:
  - What existed before: the page was `RECONSTRUCTABLE:TRUE` but still attached to [UID:00000V][BaramApp](by-class/BaramApp.md), whose own autogen parent is intentionally blank below the attachment threshold.
  - What it was changed to: cleared `AUTOGEN_PARENT_UID`, kept the range reconstructable/unassigned with no C++ block, and raised confidence from `84` to `86` after a live IDA MCP byte/xref refresh.
  - Summary/evidence: IDA still has no function at `0x004f5f20`, but disassembly proves the constructor-shaped body and vtable writes; source attachment remains deferred until `BaramApp` layout and source ownership are strong enough.
