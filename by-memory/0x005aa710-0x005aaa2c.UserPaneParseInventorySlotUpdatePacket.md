*** UID:0003UM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool UserPane::ParseInventorySlotUpdatePacket(const unsigned char *packet)
{
    MemoryMan *memoryMan = GetMemoryMan();
    size_t offset = 1;
    const char inventorySlot = static_cast<char>(packet[offset++]);
    const unsigned short itemId = PacketBufferReadUInt16BE(packet + offset);
    offset += 2;
    const unsigned char iconStyle = packet[offset++];

    char narrowText[256];
    wchar_t displayName[256];
    unsigned char textLength = packet[offset++];
    memoryMan->MemmoveWrapper(narrowText, packet + offset, textLength);
    offset += textLength;
    narrowText[textLength] = '\0';
    unsigned char convertedLength = static_cast<unsigned char>(
        MultiByteToWideChar(0, 0, narrowText, textLength,
                            displayName, 256));
    displayName[convertedLength] = L'\0';

    wchar_t itemMetaKey[256];
    textLength = packet[offset++];
    memoryMan->MemmoveWrapper(narrowText, packet + offset, textLength);
    offset += textLength;
    narrowText[textLength] = '\0';
    convertedLength = static_cast<unsigned char>(
        MultiByteToWideChar(0, 0, narrowText, textLength,
                            itemMetaKey, 256));
    itemMetaKey[convertedLength] = L'\0';

    const unsigned int quantityOrCount =
        PacketBufferReadUInt32BE(packet, &offset);
    const bool quantityPromptFlag =
        PacketBufferReadUInt8(packet, &offset) != 0;
    const unsigned int currentDurability =
        PacketBufferReadUInt32BE(packet, &offset);
    const unsigned int protectionCount =
        PacketBufferReadUInt8(packet, &offset);

    wchar_t ownerName[256];
    textLength = packet[offset++];
    memoryMan->MemmoveWrapper(narrowText, packet + offset, textLength);
    offset += textLength;
    narrowText[textLength] = '\0';
    convertedLength = static_cast<unsigned char>(
        MultiByteToWideChar(0, 0, narrowText, textLength,
                            ownerName, 256));
    ownerName[convertedLength] = L'\0';

    const bool targetMode = PacketBufferReadUInt8(packet, &offset) != 0;

    if (inventorySlot > 0 &&
        inventorySlot <= g_activeUserStatusPane->m_inventorySlotCount)
    {
        LocalInventorySlotRecord &record =
            m_inventorySlots[inventorySlot - 1];
        record.itemId = itemId;
        record.quantityOrCount = quantityOrCount;
        record.quantityPromptFlag = quantityPromptFlag ? 1 : 0;
        record.currentDurability = currentDurability;
        record.protectionCount = protectionCount;
        record.iconStyle = iconStyle;
        record.active = 1;
        wcscpy_s(record.displayName, 80, displayName);
        wcscpy_s(record.itemMetaKey, 80, itemMetaKey);
        record.targetMode = targetMode ? 1 : 0;
        wcscpy_s(record.ownerName, 80, ownerName);
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready UserPane opcode-0x0f inventory-slot update parser with signed one-based slot validation, exact three-string ACP conversion, 508-byte record writes, active UserStatusPane bound, durability/protection semantics, and preserved invalid-slot/always-false behavior. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005aa710-0x005aaa2c User Pane Parse Inventory Slot Update Packet

## Current Source Disposition - 2026-07-15

- Current identity: [UID:0003UM] `UserPane::ParseInventorySlotUpdatePacket` at `0x005aa710-0x005aaa2c`; the nonblank managed C++ above is the authoritative source body.
- Current ownership and route: canonical owner/emitter [UID:0000FQ][UserPane](by-class/UserPane.md), emitted through [UID:0000P1][UserPane](by-file/UserPane.md) as `NexusTK/ui/panels/UserPane.cpp`.
- Current behavior and evidence: opcode `0x0f` supplies the packet payload; this member parses a signed one-based inventory slot, item id, icon style, three ACP strings, quantity/prompt, current durability, protection count, and target mode, then writes one exact `0x1fc`-byte local inventory record when the slot is valid.
- Current confidence treatment: route, caller, offset, and inline-equivalence evidence below remains valid. Route-negative evidence may cap confidence, but it does not justify LivingObjectPane ownership, blank formal C++, or a no-code disposition.
- Current recommendation: retain this exact UserPane member name, owner/emitter metadata, nonblank formal body, `92/94` score, ABI, range, and source placement.

## Historical / Superseded Assumptions - Pre-2026-07-14

- Date/source: documentation conclusions incorporated before the 2026-07-14 UID0002QY ownership and source-integration repair.
- Rejected claim: this local-player body belonged to `LivingObjectPane`/`LivingObjectPane.cpp`, used a reverse-engineering placeholder qualification, or should remain blank/no-code until a separate route or declaration was found.
- Disproof evidence: the standalone `LivingObjectPane` object ends at `+0x20c`, while the moved receiver-bearing bodies use UserPane-only state in the `+0x134000..+0x13eb84` extension; no-receiver helpers cluster with and are consumed by the same UserPane source family. The complete UID0000FQ declaration and UID0000P1 route make the accepted definitions source-legal.
- Accepted replacement: source-authored `UserPane` member with the exact current managed definition above. Existing no-route and inline-equivalent observations remain negative/liveness evidence only.
- Evidence preservation: exact bytes, ranges, CFG/callers, field accesses, packet/state behavior, and historical provenance in the nested material below remain retained evidence; only the rejected ownership, qualification, placeholder-name, and blank/no-code conclusions are superseded.

### Historical Status - Superseded

- Address range: `0x005aa710-0x005aaa2c`.
- Entity kind: exact `UserPane` member/helper
- Current IDA name: `sub_5AA710`.
- Disposition: reconstructable source-authored code.
- Historical parent claim: [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md). Current owner is [UID:0000FQ][UserPane](by-class/UserPane.md).

### Historical Summary - Superseded

The earlier statement that this handler parsed spell-slot metadata is disproved. The exact body parses and stores local inventory-slot metadata and remains part of the server-message packet-handler family split out of [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md).

### Evidence

- B001 report records live IDA `lookup_funcs`: start `0x005aa710`, size `0x31c`, end `0x005aaa2c`.
- Caller/callee evidence places it in the `UserPane::OnServerMessage` local-player packet dispatch family.

### Historical Open Questions - Resolved

- The former spell-slot record, packet-subtype, and source-signature questions are resolved by the exact inventory record, opcode `0x0f` dispatcher route, and current formal definition.
- The former blank-C++ condition is superseded by the exact managed body above.

### Cross-References

- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)

## B001 UID0002QY Accepted Callback Synchronization - 2026-07-14

- Destination 36 is applied without changing the accepted score, address range, ABI, callers, packet/state behavior, negative evidence, or retained historical research.
- Receiver fields beyond the LivingObjectPane base establish [UID:0000FQ][UserPane](by-class/UserPane.md) UserPane as canonical owner/emitter and [UID:0000P1][UserPane](by-file/UserPane.md) as source module.
- The formal managed block is authoritative. Earlier LivingObjectPane ownership/qualifiers, Raw or packet-letter placeholder names, and blank-body conclusions are retained only as dated history and are superseded by the accepted semantic source shape.

## B004 UID0003UM Direct Source-Quality Implementation - 2026-07-15

### Exact Boundary, Bytes, CFG, ABI, And Liveness

- Fresh IDA MCP database `01f84d61` resolved `sub_5AA710` at exact `[0x005aa710,0x005aaa2c)`, size `0x31c` / 796 bytes. The target-byte SHA256 is `FEE5D907C63EEB2EAAF826EA113672F73D5205FF759757551F18EFC4A7C93160`.
- The modeled function has 212 instructions, four basic blocks, cyclomatic complexity three, a `0x724` stack frame, and compiler security-cookie setup/check. It has no source-level local SEH region and no independently routed interior child.
- The sole direct caller is `UserPane::OnServerMessage` at `0x005a775c`. Dispatcher case opcode `0x0f` passes payload from `Event+0x0c` and adjusts the receiver to UserPane. The adjacent opcode `0x10` case clears an inventory slot and is separate corroborating behavior.
- The one private dispatcher call is sufficient source liveness. There is no vtable slot, function pointer, export, VA/RVA table pointer, or second direct call, but those negatives do not support blank/no-code handling.

### Exact Packet Contract And Conversion Behavior

1. The parser obtains `MemoryMan *` once through `GetMemoryMan`, starts the cursor at packet offset one because byte zero is the dispatcher opcode, and reads the inventory slot as plain signed `char`.
2. It reads a big-endian UInt16 item id from packet bytes two and three, then one icon-style byte.
3. Each of three text fields uses a one-byte byte-count, `MemoryMan::MemmoveWrapper` into one reused `char narrowText[256]`, an explicit narrow terminator, `MultiByteToWideChar(0, 0, ..., 256)`, an explicitly narrowed one-byte converted count, and an explicit wide terminator in a distinct `wchar_t[256]` local.
4. Between the second and third text fields it cursor-reads a big-endian UInt32 quantity/count, a UInt8 quantity-prompt flag, a big-endian UInt32 current durability, and a UInt8 protection count. After the third string it cursor-reads the UInt8 target mode.
5. `0x00575470` is MSVC/Dinkumware `_Narrow_char_traits<char,int>::to_char_type` compiler/runtime lowering, not a human packet helper. The formal therefore expresses direct byte indexing rather than inventing `ReadLengthPrefixedAnsiText`.

### Exact Inventory Record And Store Order

- The signed valid range is `1..g_activeUserStatusPane->m_inventorySlotCount`. The machine dereferences global `0x0069ae0c` and unsigned byte `+0x284`; it does not read CollectionData.
- Invalid slots still consume every scalar and string, perform all three conversions, skip record writes, and return `false`.
- Valid records are `m_inventorySlots[inventorySlot - 1]`: UserPane base `+0x134104`, exact stride `0x1fc` / 508 bytes, 52 records. The machine expression `this + 0x133f08 + slot * 0x1fc` is equivalent to that one-based source form.
- The exact store order is item id `+0x002`, quantity/count `+0x1e8`, prompt byte `+0x1ec`, current durability `+0x1f0`, protection count `+0x1f4`, icon style `+0x004`, active byte `+0x000`, display name `+0x006`, item metadata key `+0x0a6`, target mode `+0x1f8`, then owner name `+0x146`.
- The three record text destinations are exact 80-wide-character arrays. ItemHelpPane consumers prove `displayName`, `itemMetaKey`, and `ownerName`; they also copy `+0x1f0` to current durability and format positive `+0x1f4` values as `PROTECTED (%d)`, proving `protectionCount`.
- Both valid and invalid paths return `false`.

### Negative Evidence, Rejected Alternatives, And Confidence Cap

- The target has no packet-length, text-length clamp, null-pointer, conversion-error, global-pointer, or record-pointer check. The source reconstruction intentionally adds none.
- No spell-command record base/stride, `g_pCollectionData`, SimpleUString/STL string, persistent narrow string, or human text-reader helper appears.
- Rejected current alternatives are LivingObjectPane ownership, spell-slot parsing, an unsigned slot, grouped declaration-order stores, a merged clear-slot successor, and blank/compiler/no-code treatment.
- The `92/94` score reflects exact range, behavior, packet schema, helpers, record layout, field consumers, owner/emitter, source route, and formal C++. Original local/parameter spelling and rebuild-identical compiler output remain the bounded confidence cap.

### Boundary And Source Placement

- Predecessor `[0x005aa702,0x005aa710)` is fourteen `0xcc` bytes, SHA256 `1E875C76536C8496BED8C8760B539327F24ACFFBCF0A4B941877B4BFB4C44356`.
- Immediate successor `[0x005aaa2c,0x005aaa30)` is four `0xcc` bytes, SHA256 `8843B54D2DF63CA265CF4A05D27DD2B29A74FB476D296DD44A0E171D74B441CA`.
- The separate retained clear-slot helper `[0x005aaa30,0x005aaa88)` has SHA256 `5654C2933DFDB08C26166228F7892EED01F7FA44FCBE9895D3D283A6F30EB080`; it matches opcode `0x10` semantics and is not merged into this target.
- Canonical owner/emitter remains [UID:0000FQ][UserPane](by-class/UserPane.md), emitted by [UID:0000P1][UserPane](by-file/UserPane.md) into `NexusTK/ui/panels/UserPane.cpp`. PacketBuffer, MemoryMan, UserStatusPane, ItemHelpPane, and CollectionData remain dependencies/consumers rather than source owners.
