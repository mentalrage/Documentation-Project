*** UID:0000Y4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:70 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:85 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000BO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x0045a400-0x0045a70b RankingEventScrollPane HitTest

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: helper method
- Current Wave3 owner: `RankingEventScrollPane` metadata; missing from active emitted source
- Likely source module: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- Rebuild handling: `source-authored`
- Confidence: strong for helper role, boundary, and caller ownership; medium for final source-level name

## Summary

This helper hit-tests a mouse point against the ranking scrollbar's logical part rectangles and returns a part code. It iterates part codes `0..4`, computes each candidate rectangle using the current orientation, scroll position, max range, and drag state, then checks whether the input point is inside that rectangle.

The helper is not generic input dispatch. It is local scrollbar geometry for `RankingEventScrollPane`, and its only confirmed callers are the modeled mouse handler at `0x00459a60`.

Known part codes match the neighboring scroll-volume pane convention:

| Code | Meaning |
| --- | --- |
| `0` | collapsed leading edge/arrow region |
| `1` | track before the thumb |
| `2` | thumb |
| `3` | track after the thumb |
| `4` | collapsed trailing edge/arrow region |
| `0xff` | no hit |

## IDA MCP Evidence

Checked on 2026-05-25:

- `lookup_funcs` reports `0x0045a400` as `sub_45A400`, size `0x30c`.
- `callers` reports only [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md), with call sites at `0x00459b42` and `0x00459c23`.
- Decompilation reads the same local state used by the rectangle helper: orientation at `+0xfc`, drag flag at `+0xfb`, current scroll position at `+0xfe`, max range at `+0x100`, enabled byte at `+0x102`, and drag anchor at `+0x108`.

Rechecked on 2026-05-27:

- `decompile 0x0045a400` confirms a five-iteration part loop from part `0` through part `4`, returning `0xff` after the final miss.
- `callers 0x0045a400` again reports only `sub_459A60`, with call sites `0x00459b42` and `0x00459c23`.
- `callees 0x0045a400` reports only geometry/input helpers: `0x004b7c50` empty-rectangle/set-rect helper, `0x005447c0` pane origin helper, `0x004a9090` input/screen point helper, `0x004b7e80` point-in-rect test, and the security-cookie check. It does not call unrelated ranking, packet, or render helpers.
- The decompile uses the full object pointer, not the secondary vtable subobject pointer. Offsets line up with [UID:0001VO][RankingEventScrollPaneLayout](by-type/by-struct/RankingEventScrollPaneLayout.md): bounds at `+0x44`, drag flag `+0xfb`, orientation `+0xfc`, current position `+0xfe`, max position `+0x100`, enabled byte `+0x102`, and drag anchor point at `+0x108`.

## Source-Shape Notes

Do not emit final C++ for this helper yet. The control flow and field reads are stable, but final names for scroll-part constants, the shared geometry API, and the internal rectangle-building shape are still provisional.

The helper should eventually reconstruct as local scrollbar hit-testing that loops part codes `0..4`, builds the corresponding rectangle, point-tests it, and returns `0xff` on miss.

The inlined rectangle construction behavior is:

- Part `0` and part `4` collapse to the leading/trailing edge line of the control bounds.
- Part `1` is the track segment before the thumb.
- Part `2` is the thumb rectangle.
- Part `3` is the track segment after the thumb.
- If the scrollbar is enabled and `maxPosition > 0`, the thumb position is derived from `currentPosition / maxPosition`.
- Nonzero orientation uses horizontal/left-right geometry; zero orientation uses vertical/top-bottom geometry.
- While the drag flag is set, the vertical thumb rectangle is derived from the current input point and drag anchor, clamped to the track; otherwise it is derived from `currentPosition`.
- Disabled/no-range track-before and track-after parts collapse to an empty rectangle; the thumb falls back to the remaining inner track area.

Keep `ScrollPart_LeadingEdge`, `ScrollPart_TrailingEdge`, `ScrollPart_None`, and `BuildHitTestRectForPart` as behavior labels only. `BuildHitTestRectForPart` is not a separate function in this binary; it names the inlined rectangle construction logic inside `0x0045a400`. The final orientation enum and geometry type names should come from the shared pane/control layout once those docs are stronger.

## Generated-Source Caveat

This is a modeled IDA function, but current `source-3/simroot_v2/class_RankingEventScrollPane.cpp` does not emit an active C++ method for it. The active source's `OnMouseEvent` body is also missing, so both pieces must be reconstructed from IDA/memory evidence before migration.

## Cross-References

- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md)
- [UID:0001VO][RankingEventScrollPaneLayout](by-type/by-struct/RankingEventScrollPaneLayout.md)
- [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md)
- [UID:0000Y6][0x0045ac10-0x0045adc3.RankingEventScrollPaneDragToPosition](by-memory/0x0045ac10-0x0045adc3.RankingEventScrollPaneDragToPosition.md)

## Changes

- 2026-05-31: Marked reconstructable and attached to [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md); replaced the provisional C++ hit-test candidate with source-shape notes.
  - Before: the page contained a short C++ sketch with descriptive part/geometry helper names.
  - After: the page keeps part-code behavior and IDA evidence without emitting non-final C++.
  - Evidence: IDA confirms exact modeled boundary, five-part loop behavior, offset reads, and caller set, but final shared geometry and enum names remain provisional.
