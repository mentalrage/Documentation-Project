*** UID:0003U8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserPane::StartDirectionalAnimation(char direction)
{
    m_isMoving = true;
    ClearMovementReplyState();
    SetDirectionDelayActive(false);
    m_moveFrame = 0;

    if (m_objectStatus.m_statusKind == ObjectStatusBlob::MonsterObject)
    {
        m_animationGroup = direction + 5;
        MonsterImageEntry *entry =
            g_pMonsterImageLib->GetEntryByIndex(
                m_objectStatus.m_appearanceId - 0x8000);
        ImageAnimationGroup *group = entry->GetGroup(m_animationGroup);
        const ImageAnimationFrame *frame = group->GetFrame(m_moveFrame);

        PlayFacingAnimation();
        CancelBoxRotationStep();
        g_pTimerMgr->ScheduleTimer(
            static_cast<TimerHandler *>(this), kUserPaneMovementStep,
            frame->frameDuration, direction, 0);
        SetMovingState(true);
        m_mapPane->UpdateObjectPaneScreenBounds(this);
        return;
    }

    SetMovingState(true);
    m_moveFrame = 0;
    g_pTimerMgr->ScheduleTimer(
        static_cast<TimerHandler *>(this), kUserPaneMovementStep,
        m_objectStatus.m_displayStatusByte / g_movementSubstepScale,
        direction, 0);
    m_mapPane->UpdateObjectPaneScreenBounds(this);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready UserPane directional-animation starter; exact signed-char ABI, MonsterImageLib frame-duration path, non-monster scaled-delay path, TimerMgr event 20 scheduling, state order, callers, boundaries, negative evidence, and source placement are documented below. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005a3770-0x005a386a User Pane Start Directional Animation

## UID0003VD OnTimer Dependency Synchronization - 2026-07-30

- This starter clears `m_movementReplyReceived` and `m_movementReplyPending` through `ClearMovementReplyState()`, clears `m_directionDelayActive`, initializes inherited signed `m_moveFrame`, and schedules local `kUserPaneMovementStep` / event `20` with direction in the first callback payload. [UID:0003VD][0x005a83b0-0x005a87ad.UserPaneHandleAnimationStep](by-memory/0x005a83b0-0x005a87ad.UserPaneHandleAnimationStep.md) is the exact `UserPane::OnTimer` continuation at `0x005a8796`.
- Both the MonsterObject frame-duration path and the non-monster `m_displayStatusByte / g_movementSubstepScale` path preserve current behavior and schedule the same event family. The source parameter remains plain `char direction`, consistent with the established signed call contract.
- `ClearMovementHistoryToken` was a historical broad spelling for the reply-latch zero store; it does not clear movement-history records. `ClearMovementReplyState` is the current source-facing inline helper.
- Current metadata stays `92/94`; no owner, range, branch-order, or timer-delay change is introduced.

## Current Source Disposition - 2026-07-15

- Current identity: [UID:0003U8] `UserPane::StartDirectionalAnimation` at `0x005a3770-0x005a386a`; the nonblank managed C++ above is the authoritative source body.
- Current ownership and route: canonical owner/emitter [UID:0000FQ][UserPane](by-class/UserPane.md), emitted through [UID:0000P1][UserPane](by-file/UserPane.md) as `NexusTK/ui/panels/UserPane.cpp`.
- Current behavior and evidence: the exact body initializes movement state, branches on `ObjectStatusBlob::MonsterObject`, obtains the monster frame's signed duration through `g_pMonsterImageLib`, schedules TimerMgr event `20`, and performs the branch-local map refresh. The non-monster path uses the signed movement-substep divisor and performs the machine-observed second frame reset.
- Current confidence treatment: exact bytes, CFG, two direct callers, field offsets, helper targets, signed loads, timer push order, source route, and the five-consumer duration audit resolve the prior score blockers. The current score is `92/94`.
- Current recommendation: retain the current UserPane member name, owner/emitter metadata, nonblank formal body, signed plain-char ABI, exact range, blank position, `Nested:0`, and UserPane.cpp source placement.

## Historical / Superseded Assumptions - Pre-2026-07-14

- Date/source: documentation conclusions incorporated before the 2026-07-14 UID0002QY ownership and source-integration repair.
- Rejected claim: this local-player body belonged to `LivingObjectPane`/`LivingObjectPane.cpp`, used a reverse-engineering placeholder qualification, or should remain blank/no-code until a separate route or declaration was found.
- Disproof evidence: the standalone `LivingObjectPane` object ends at `+0x20c`, while the moved receiver-bearing bodies use UserPane-only state in the `+0x134000..+0x13eb84` extension; no-receiver helpers cluster with and are consumed by the same UserPane source family. The complete UID0000FQ declaration and UID0000P1 route make the accepted definitions source-legal.
- Accepted replacement: source-authored `UserPane` member with the exact current managed definition above. Existing no-route and inline-equivalent observations remain negative/liveness evidence only.
- Evidence preservation: exact bytes, ranges, CFG/callers, field accesses, packet/state behavior, and historical provenance in the nested material below remain retained evidence; only the rejected ownership, qualification, placeholder-name, and blank/no-code conclusions are superseded.

### Status

- Address range: `0x005a3770-0x005a386a`.
- Entity kind: exact `UserPane` member/helper
- Current IDA name: `sub_5A3770`.
- Disposition: reconstructable source-authored code.
- Direct source owner: [UID:0000FQ][UserPane](by-class/UserPane.md), whose base is [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md).

### Summary

This method starts directional animation/timer flow for local movement. Monster actors resolve a signed frame duration from the MonsterImageLib table family; non-monster actors derive delay from display status and the signed movement-substep scale. Both paths schedule event `20` through [UID:0000F1][TimerMgr](by-class/TimerMgr.md) and refresh screen bounds through the owning map pane.

### Evidence

- B001 2026-06-16 audit records live IDA `lookup_funcs`: start `0x005a3770`, size `0xfa`, end `0x005a386a`.
- The helper is part of the same movement/action family as `0x005a2e00` and `0x005a36f0`, with TimerMgr scheduling evidence already written on the parent page.

### Historical / Superseded Open Questions - Pre-2026-07-15

- Superseded claim: original source method, animation-delay members, and timer event were unresolved. The current nonblank definition, UserPane/LivingObjectPane/ObjectStatusBlob layouts, MonsterImageLib table route, signed `ImageAnimationFrame::frameDuration`, and TimerMgr event contract now provide a coherent source form.
- Superseded claim: final C++ stayed blank. The managed block is authoritative; only literal stripped private spelling remains a confidence cap.

### Cross-References

- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)
- [UID:0000F1][TimerMgr](by-class/TimerMgr.md)

## B001 UID0002QY Accepted Callback Synchronization - 2026-07-14

- Destination 31 is applied without changing the accepted score, address range, ABI, callers, field/packet behavior, negative evidence, or retained historical research.
- Receiver fields beyond the `LivingObjectPane` base establish [UID:0000FQ][UserPane](by-class/UserPane.md) `UserPane` as canonical owner/emitter and [UID:0000P1][UserPane](by-file/UserPane.md) as source module.
- The formal managed block is authoritative. Earlier `LivingObjectPane` ownership/qualifiers, `Raw*` or packet-letter placeholder names, and blank-body conclusions remain only as dated historical assumptions and are superseded by the accepted semantic source shape.

## Historical / Superseded B004 UID0003U7 Bounded Support Synchronization - 2026-07-15

- Historical state: the bounded B004 support pass retained score `86/88`, UID0003U8, owner/emitter UID0000FQ, reconstructable state, blank position, `Nested:0`, exact range, and unrelated behavior/evidence. The independent UID0003U8 reanalysis now supersedes only that old score and the stale Human/object-image branch interpretation.
- Live disassembly of both machine branches confirms the nonhuman delay path reads byte `UserPane + 0x1a0`, which is [UID:00009S][ObjectStatusBlob](by-class/ObjectStatusBlob.md) `m_displayStatusByte` at embedded offset `+0x28`, then divides it by `g_movementSubstepScale`. Historical synthetic `m_movementSpeed` is rejected.
- Every one of the 23 direct movement-scale readers across 14 functions sign-extends `0x0066da96`; this method's read at `0x005a3836` is one of them. The direct division remains unguarded and uses the MapPane-owned file-static plain-char scale initialized to 1.
- Both machine branches converge semantically on one final source call. Their direct call sites at `0x005a3816` and `0x005a385f` target [UID:0004DS][0x00506980-0x00506ad4.MapPaneUpdateObjectPaneScreenBounds](by-memory/0x00506980-0x00506ad4.MapPaneUpdateObjectPaneScreenBounds.md); the merged source call is therefore `m_mapPane->UpdateObjectPaneScreenBounds(this)`, not historical `InvalidateObject`.
- Superseded body claim: the former bounded block changed only two stale tokens and retained a Human-object frame-group route. Direct target disassembly and the MonsterImageLib/table audit reject that route; the current managed block preserves the still-valid animation group, facing animation, box-step cancellation, moving-state transition, TimerMgr event `20`, direction payload, and screen-bounds refresh while restoring the exact branch-specific order.
- Historical boundary: that bounded support synchronization did not claim a full independent rescore, rename, split, owner change, or broader sibling rewrite. This page's current independent reanalysis provides the accepted rescore and complete body.

## B001 UID0003U8 Accepted Callback Synchronization - 2026-07-15

### Exact Boundary, Bytes, And Control Flow

- The exact function is `[0x005a3770,0x005a386a)`, 250 bytes, with SHA256 `5ECA3299C0462C97F9AFF071B6ECD85207BC1D5782F919DDB3C84E39D3E0010D`. It is one source-authored method with three basic-block regions and two terminal returns, not an aggregate or split candidate.
- [UID:0003U7][0x005a36f0-0x005a3766.UserPaneInitializeWalkAnimation](by-memory/0x005a36f0-0x005a3766.UserPaneInitializeWalkAnimation.md) ends at `0x005a3766`; `[0x005a3766,0x005a3770)` is the already-recorded ten-byte predecessor alignment. `[0x005a386a,0x005a3870)` is six `cc` bytes, SHA256 `33CC4A00A0F04735602819132DA3E3FAC4CDEB8EC7AEE92551DC79BA8488E722`, before [UID:0003U9][0x005a3870-0x005a3889.UserPaneGetInventorySlotAddress](by-memory/0x005a3870-0x005a3889.UserPaneGetInventorySlotAddress.md) at `0x005a3870`.
- No internal EH, cookie, data table, thunk, compiler-only island, or independent child boundary exists. The existing owner/emitter UID0000FQ, `RECONSTRUCTABLE:TRUE`, blank position, and `Nested:0` remain exact.

### ABI, Callers, And Reachability

- The entry `movsx` at `0x005a3774` establishes a signed plain-`char` direction parameter. The method is `void`; both direct callers ignore EAX.
- Exactly two direct code callers exist: the movement path call at `0x005a3607` and the later UserPane path call at `0x005a8796`. No data, vtable, callback, table, or address-taken reference was recovered.
- UserPane-only receiver state and both callers establish [UID:0000FQ][UserPane](by-class/UserPane.md) as direct source owner and [UID:0000P1][UserPane](by-file/UserPane.md) as source route. LivingObjectPane remains the base declaration, not the owner of this body.

### Exact State And Branch Behavior

- Common entry order is exact: set `m_isMoving`, clear the movement-history token word, clear direction-delay state, and set `m_moveFrame` to zero.
- The resource branch is exactly `m_objectStatus.m_statusKind == ObjectStatusBlob::MonsterObject`. It stores signed `direction + 5` in `m_animationGroup`, zero-extends the appearance id, subtracts `0x8000`, and calls `MonsterImageLib::GetEntryByIndex` through [UID:0000RR][g_pMonsterImageLib](by-global/g_pMonsterImageLib.md).
- The table chain is `MonsterImageEntry::GetGroup(m_animationGroup)` followed by `ImageAnimationGroup::GetFrame(m_moveFrame)`. The selected frame's `+0x02` field is signed `short frameDuration`; five independent scheduler/timing consumers sign-extend this shared word.
- The monster branch then calls `PlayFacingAnimation`, cancels the box-rotation step, schedules TimerMgr event `20` with handler `this+0xa4`, signed frame duration, direction, and zero, sets moving state only after scheduling, refreshes bounds through `m_mapPane`, and returns.
- The non-monster branch sets moving state before delay computation, explicitly clears `m_moveFrame` a second time, divides zero-extended `m_displayStatusByte` by signed plain-char `g_movementSubstepScale` with unguarded signed `idiv`, schedules event `20` with the quotient/direction/zero payload, and performs its own map refresh.
- The two direct map calls at `0x005a3816` and `0x005a385f` both resolve to [UID:0004DS][0x00506980-0x00506ad4.MapPaneUpdateObjectPaneScreenBounds](by-memory/0x00506980-0x00506ad4.MapPaneUpdateObjectPaneScreenBounds.md). Their duplicated terminal placement and the monster-only return are preserved rather than merged into a behaviorally different common tail.

### Negative Evidence And Rejected Alternatives

- `HumanObject`, `g_pObjectImageLib`, `ImageFrameGroup`, `ImageFrame`, and a `delay` member are superseded interpretations for this body. The operand at `0x0069b440`, status enum value, helper addresses, and shared table layouts establish the MonsterImageLib route.
- An unsigned direction parameter is rejected by the entry sign extension. A merged `frameDelay` tail is rejected because it changes the observed branch-specific SetMovingState, second frame reset, scheduling, map-call, and return order.
- A guarded divisor, canonicalized timing value, invented source file, new child page, emitter position, or LivingObjectPane ownership is unsupported. The only residual uncertainty is exact original private spelling; it does not block the source-ready managed body.
