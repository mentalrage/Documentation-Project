*** UID:00010S | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ClanBankItemListPane::ClanBankItemListPane(const unsigned char *packet)
    : ListPane(524, 8, 400, 40, 0, true, true),
      m_visibleRowCount(0)
{
    ClearSlotCache();

    const unsigned char opcode = ReadPacketByte(packet);
    if (opcode == 10)
    {
        const unsigned int count = ReadPacketByte(packet + 1);
        const unsigned char *cursor = packet + 2;

        for (unsigned int row = 1; row <= count; ++row)
        {
            const unsigned char slotIndex = ReadPacketByte(cursor++);
            ClanBankItemEntry &entry = m_entries[row];
            entry.slotIndex = slotIndex;
            entry.itemId = ReadPacketUInt16BE(cursor);
            cursor += 2;

            const unsigned int nameLength = ReadPacketByte(cursor++);
            char nameBytes[256];
            memmove(nameBytes, cursor, nameLength);
            nameBytes[nameLength] = '\0';
            cursor += nameLength;

            const int wideLength = MultiByteToWideChar(
                CP_ACP,
                0,
                nameBytes,
                nameLength,
                entry.displayName,
                256);

            if (static_cast<unsigned int>(wideLength) >= 256)
                __report_rangecheckfailure();

            entry.displayName[wideLength] = L'\0';

            const unsigned int detailLength = ReadPacketByte(cursor++);
            cursor += detailLength;
            entry.count = ReadPacketUInt32BE(cursor);
            cursor += 4;

            cursor += 3;
            if (ReadPacketByte(cursor++) != 0)
            {
                const unsigned int extraLength = ReadPacketByte(cursor++);
                cursor += extraLength;
            }

            entry.flags = ReadPacketByte(cursor++);
        }

        std::sort(
            m_entries + 1,
            m_entries + count + 1,
            [](const ClanBankItemEntry &left, const ClanBankItemEntry &right) {
                return right.displayName[0] != L'\0' &&
                       wcscmp(left.displayName, right.displayName) > 0;
            });
    }
    else if (opcode == 15)
    {
        const unsigned char slotIndex = ReadPacketByte(packet + 1);
        m_entries[slotIndex] = ClanBankItemEntry();
    }
    else if (opcode == 16)
    {
        const unsigned int count = ReadPacketUInt16BE(packet + 1);
        const unsigned char *cursor = packet + 3;

        for (unsigned int i = 0; i < count; ++i)
        {
            const unsigned char slotIndex = ReadPacketByte(cursor++);
            ClanBankItemEntry &entry = m_entries[slotIndex];
            entry.slotIndex = slotIndex;
            entry.itemId = ReadPacketUInt16BE(cursor);
            cursor += 2;

            cursor += 1;
            entry.count = ReadPacketUInt32BE(cursor);
            cursor += 4;

            cursor += 1;
            const unsigned int nameLength = ReadPacketByte(cursor++);
            char nameBytes[256];
            memmove(nameBytes, cursor, nameLength);
            nameBytes[nameLength] = '\0';
            cursor += nameLength;

            const int wideLength = MultiByteToWideChar(
                CP_ACP,
                0,
                nameBytes,
                nameLength,
                entry.displayName,
                256);

            if (static_cast<unsigned int>(wideLength) >= 256)
                __report_rangecheckfailure();

            entry.displayName[wideLength] = L'\0';

            const unsigned int detailLength = ReadPacketByte(cursor++);
            cursor += detailLength;
            entry.flags = 0;
        }
    }

    RebuildVisibleRows();
}

void ClanBankItemListPane::ClearSlotCache()
{
    memset(m_entries, 0, sizeof(m_entries));
    m_visibleRowCount = 0;
}

void ClanBankItemListPane::RebuildVisibleRows()
{
    m_visibleRowCount = 0;

    for (unsigned int slotIndex = 1; slotIndex < 256; ++slotIndex)
    {
        ClanBankItemEntry &entry = m_entries[slotIndex];
        if (entry.displayName[0] != L'\0')
        {
            AddRow(&entry);
            ++m_visibleRowCount;
        }
    }
}

void ClanBankItemListPane::DrawListEntry(int rowIndex, const ClanBankItemEntry *entry, const Rect *bounds)
{
    int scratch[4];
    BeginListRowDraw(scratch);

    if (IsRowSelected(rowIndex))
    {
        Rect selectedBounds(bounds->left, bounds->top, bounds->right - 13, bounds->bottom);
        SetTextColor(128);
        DrawSelectedRowBackground(&selectedBounds);
    }

    SetFontColor(143);

    Rect imageBounds;
    GetItemImageBounds(entry->itemId, &imageBounds);
    const int imageLeft = 30 - ((imageBounds.right - imageBounds.left) / 2);
    const int imageTop = ((bounds->top + bounds->bottom) / 2) -
                         ((imageBounds.bottom - imageBounds.top) / 2);

    DrawItemImage(
        g_pItemObjImageLib,
        Rect(imageLeft, imageTop, imageLeft + imageBounds.Width(), imageTop + imageBounds.Height()),
        entry->itemId,
        0,
        0,
        0.0f);

    m_drawImage = true;
    SetTextBackgroundMode(0);

    wchar_t label[256];
    if (entry->count <= 1)
        wcscpy_s(label, 256, entry->displayName);
    else
        swprintf_s(label, 256, L"%s (%d)", entry->displayName, entry->count);

    FitTextWithEllipsis(label, 256, L"...", bounds->right - 70);
    MoveTextCursor(70, ((bounds->top + bounds->bottom) / 2) + 6);
    DrawWideText(label, wcslen(label));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA reconfirms bank inventory list constructor/parser/draw/destructor behavior, vtable anchors, only constructor caller from ClanBankPane, packet cases, sorting/rebuild behavior, text fitting, ClanBank parent, and final-code caveats. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0048b8c0-0x0048c251 ClanBankItemListPane

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: `ClanBankItemListPane` class methods
- Documented owner class: `ClanBankItemListPane`
- Likely source module: [UID:0000I9][ClanBank](by-file/ClanBank.md)
- Confidence: very strong for covered local methods, vtable/resource anchors, and ClanBank parent; medium-high for final entry-struct field names and exact original virtual names

## Covered Ranges

| Range | Function | Notes |
| --- | --- | --- |
| `0x0048b8c0-0x0048c034` | `ClanBankItemListPane::ClanBankItemListPane` | Initializes the list pane, installs vtables, clears 255 entry slots, parses full/remove/update bank packets, sorts full-list rows by display name, and adds visible rows. |
| `0x0048c040-0x0048c251` | `ClanBankItemListPane::DrawListEntry` | Vtable-reached row renderer; draws selected row background, item icon, name/count text, and width-limited label. |

## IDA MCP Evidence

Checked on 2026-05-23: IDA confirms starts at `0x0048b8c0` and `0x0048c040`.

2026-05-27 boundary pass: IDA reports the draw handler at `0x0048c040-0x0048c251`; byte `0x0048c250` is the final immediate byte of `retn 0Ch`. The following `0x0048c251-0x0048c260` span is alignment before [UID:00010T][0x0048c260-0x0048c63f.ClanDestructorBand](by-memory/0x0048c260-0x0048c63f.ClanDestructorBand.md).

2026-06-05 live IDA MCP check:

- `lookup_funcs` reports modeled functions at `0x0048b8c0` size `0x774`, `0x0048c040` size `0x211`, and `0x0048c3d0` size `0x3b`. The metadata outlier addresses still resolve inside containing functions at `0x00430040` and `0x004bee40`, not this local range.
- `callers`/`xrefs_to` reports the constructor is called only from [UID:00010R][0x0048b1c0-0x0048b7dc.ClanBankPane](by-memory/0x0048b1c0-0x0048b7dc.ClanBankPane.md) at `0x0048b45b`; the draw handler has a vtable data xref at `0x0061639c`; the destructor is reached through adjustor thunks at `0x0048c2a7` and `0x0048c2b2` plus vtable data `0x0061631c`.
- Constructor decompilation calls the list base initializer with geometry-like values `524, 8, 400, 40`, installs three `ClanBankItemListPane` vtables at object offsets `0x0`, `0xa0`, and `0xa4`, clears 255 entry records, and then switches on the first packet byte.
- Packet case `10` treats the second byte as a full-list count, reads each entry's slot/order byte, item id word, length-prefixed multibyte name/detail strings, count dword, and flag byte, converts strings with `MultiByteToWideChar`, bubble-sorts active entries by wide display name, then rebuilds the visible list and count at object offset `+0x1fb50`.
- Packet case `15` clears one slot by packet byte; case `16` reads a two-byte update count and updates the listed slots without the full-list sort pass. After all cases, the constructor iterates slot IDs `1..255`, adds active entries with the list-entry helper at `0x004f3c50`, and increments the visible row count.
- Draw decompilation checks whether the row is selected, draws the selected background, renders the item icon through the image/draw helpers, formats a `"%s (%d)"` suffix only when count is greater than one, uses [UID:00016B][0x004ba820-0x004ba991.TextFitAndSuffixHelpers](by-memory/0x004ba820-0x004ba991.TextFitAndSuffixHelpers.md) to fit the string with `...`, then draws the text.

2026-06-16 direct IDA refresh on session `a002_goal65_20260616`:

- `lookup_funcs` still reports `sub_48B8C0` size `0x774`, `sub_48C040` size `0x211`, and support scalar deleting destructor `sub_48C3D0` size `0x3b`; no newer function-boundary split was found inside this page.
- `xrefs_to` reconfirms the sole constructor caller at `0x0048b45b` from [UID:00010R][0x0048b1c0-0x0048b7dc.ClanBankPane](by-memory/0x0048b1c0-0x0048b7dc.ClanBankPane.md), draw-handler vtable anchor `0x0061639c`, and destructor refs at `0x0048c2ad`, `0x0048c2b8`, and `0x0061631c`.
- Vtable support xrefs reconfirm `ClanBankItemListPane` primary/secondary/tertiary stores at `0x0048b92b`, `0x0048b931`, and `0x0048b93b`.
- Resource xrefs reconfirm the draw method's `"%s (%d)"` format string at `0x006164e0 -> 0x0048c185`; byte reads across [UID:0003BA][0x00616484-0x006164f0.ClanBankResourceStrings](by-memory/0x00616484-0x006164f0.ClanBankResourceStrings.md) verify the full UTF-16 resource/format string block despite IDA string-reader truncation at some earlier labels.
- `find_bytes` for little-endian start addresses finds only the draw-handler vtable pointer at `0x0061639c` and destructor vtable pointer at `0x0061631c`; no absolute pointer hit was found for constructor `0x0048b8c0`.
- `analyze_component` reports the constructor/parser as the only interface function in this page. The constructor/parser has packet/string/list callees and high branching complexity for cases `10`, `15`, and `16`; the draw method has text fitting, item image, and GrafPort-style draw callees; the destructor calls list cleanup/delete helpers.
- Decompilation of `0x0048c040` reconfirms selected-row background drawing, item image draw through `g_pItemObjImageLib`, `"%s (%d)"` formatting only when count is greater than one, ellipsis fitting through `sub_4BA820`, and text draw through `sub_4BAB70`.
- Disassembly of support destructor `0x0048c3d0` reconfirms scalar-deleting shape: call list cleanup, optionally free `this`, optional guarded delete path, and `retn 4`. The destructor is adjacent support outside this page's half-open range but informs the vtable/destructor-family evidence.
- Byte checks reconfirm `0x0048c251-0x0048c260` is fifteen `0xcc` alignment bytes before [UID:00010T][0x0048c260-0x0048c63f.ClanDestructorBand](by-memory/0x0048c260-0x0048c63f.ClanDestructorBand.md). `make_signature_for_range 0x0048b8c0-0x0048c251` reports a unique wildcarded signature.
- IDA DB safe pass: appended function comments at `0x0048b8c0`, `0x0048c040`, and support destructor `0x0048c3d0`; no renames, prototypes, locals, type/member edits, or raw function creation were applied. `idb_save` returned `ok:true` for `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe.i64`.

## Ownership Decision

These local methods belong with the clan bank dialog and should migrate with [UID:0000I9][ClanBank](by-file/ClanBank.md). The page is reconstructable and attached to that parent because both this item and the parent clear the active `85/85` routing gate, and the emitter route reaches the valid `NexusTK/social/ClanBank.cpp` source root. Do not include metadata outliers `0x00430041` or `0x004c0043` in this ownership decision; they resolve to unrelated or generic IDA functions. Do not emit final C++ yet because the original entry-struct fields, local names, exact virtual slot names, and final text/image helper declarations are still not source-quality.

## Score Rationale

| Field | Value | Reason |
| --- | ---: | --- |
| Completion | 85 | Constructor/parser and draw boundaries, sole constructor caller, vtable anchors, destructor support refs, packet cases `10/15/16`, row sort/rebuild, decompiled draw behavior, resource-string xrefs, outlier exclusions, pointer-hit scan, padding bytes, unique range signature, reconstructability, generated route, and ClanBank parent are documented. Remaining work is final entry-record field names, virtual method names, local names, and helper declaration placement. |
| Confidence | 90 | Live IDA callers/callees/xrefs/decompilation/disassembly agree with the class, file, vtable, resource, and text-helper support pages. Confidence remains below final because entry structure names and exact virtual/source names are still inferred from behavior rather than recovered source evidence. |

## Cross-References

- [UID:000025][ClanBankItemListPane](by-class/ClanBankItemListPane.md)
- [UID:000026][ClanBankPane](by-class/ClanBankPane.md)
- [UID:0000I9][ClanBank](by-file/ClanBank.md)
- [UID:00010T][0x0048c260-0x0048c63f.ClanDestructorBand](by-memory/0x0048c260-0x0048c63f.ClanDestructorBand.md)
- [UID:00010R][0x0048b1c0-0x0048b7dc.ClanBankPane](by-memory/0x0048b1c0-0x0048b7dc.ClanBankPane.md)
- [UID:0002MO][0x00615fd8-0x006163dc.ClanBankVtableData](by-memory/0x00615fd8-0x006163dc.ClanBankVtableData.md)
- [UID:00016B][0x004ba820-0x004ba991.TextFitAndSuffixHelpers](by-memory/0x004ba820-0x004ba991.TextFitAndSuffixHelpers.md)

## Changes

- 2026-06-30 B009 empty-emitter implementation callback: raised completion to `88` and inserted accepted first-draft C++ for constructor/parser, `ClearSlotCache`, `RebuildVisibleRows`, and `DrawListEntry`. The block preserves packet cases `10`, `15`, and `16`, full-list sort, row rebuild, image/text/count draw behavior, and best-current inferred `ClanBankItemEntry` fields from current parser/draw evidence.
- 2026-05-27: Corrected the page/file range from `0x0048b8c0-0x0048c250` to exclusive end `0x0048c251`. Evidence: IDA MCP reports the draw handler at `0x0048c040-0x0048c251`, with `retn 0Ch` occupying bytes `0x0048c24e-0x0048c250`.
- 2026-06-05: Raised scores from `70/85` to `82/86`, marked reconstructable, and attached parent [UID:0000I9][ClanBank](by-file/ClanBank.md).
  - Before: the page had correct local range boundaries but sparse behavior evidence and blank autogen metadata.
  - After: the page records live IDA function sizes, constructor caller, vtable anchors, destructor thunk path, packet cases `10/15/16`, full-list sort, active row rebuild, text fitting, count formatting, and remaining final-code caveats.
  - Evidence: IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, `callees`, `decompile`, and `disasm` on `0x0048b8c0`, `0x0048c040`, `0x0048c2a7`, `0x0048c2b2`, `0x0048c3d0`, `0x00430041`, and `0x004c0043`.
- 2026-06-05: Cleaned stale generated-owner/helper-label wording while preserving the `82/86` score; manual coverage and generated-stat sync is pending because shared report/validator files were leased by another agent.
- 2026-06-16: Direct C001 IDA refresh raised `82/86 -> 85/90`.
  - Evidence added: current function names/sizes, constructor/draw/destructor/vtable/resource xrefs, pointer-byte scan, component callee summary, decompiled row renderer, destructor support disassembly, padding bytes, unique signature, resource-string byte caveat, and saved IDA comments.
  - Owner/emitter decision: keep [UID:0000I9][ClanBank](by-file/ClanBank.md); no final C++ because source-quality entry-field/local/virtual names and helper declarations remain unresolved.
