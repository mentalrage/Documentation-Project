*** UID:0000Y2 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current MCP confirms all eight raw helper starts, exact return/alignment boundaries, zero raw-start xrefs/pointer routes, RankingEventScrollPane vtable stores, repaired skin/style/range/position field roles, and accepted blank-C++ no-standalone proof. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004598b0-0x00459a59 RankingEventScrollPaneRawCtorAndSetters / RankingEventScrollPane Raw Constructor And Setters

## Status

- Disposition: reconstructable NexusTK project code with raw function-boundary caveats
- Entity kind: raw helper island inside `RankingEventScrollPane`
- Canonical filename/report label: `RankingEventScrollPaneRawCtorAndSetters`
- Likely source module: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- Rebuild handling: `source-authored`
- Evidence basis: live IDA MCP/disassembly against `NexusTK.exe` on 2026-06-04 and 2026-06-13, plus B004 current MCP session `supervisor_nexustk_20260709` on 2026-07-09.
- Confidence: strong for ranking ownership, raw helper boundaries, vtable binding, field behavior, and the no-standalone route proof; medium-high for exact original helper names.

## Covered Ranges

| Range | Working role | Notes |
| --- | --- | --- |
| `0x004598b0-0x0045990c` | constructor/setup body | Calls `Pane::Pane(1)`, installs the three `RankingEventScrollPane` vtables `0x00610a4c`, `0x00610a98`, and `0x00610ac8`, clears the packed `+0xf8` state dword (`m_scrollSkinIndex`, `m_scrollStyle`, and drag flag), stores orientation at `+0xfc`, clears `m_scrollPosition` at `+0xfe` and `m_scrollRange` at `+0x100`, and initializes enabled/highlight/active bytes to `1`, `0xff`, `0xff`. |
| `0x00459910-0x00459936` | skin-index word setter candidate | Stores `m_scrollSkinIndex` / the `+0xf8` word when changed and invalidates the pane rect through the primary vtable. This supersedes stale current-position wording. |
| `0x00459940-0x00459963` | style-state byte setter candidate | Stores `m_scrollStyle` / the `+0xfa` style-state byte when changed and invalidates the pane rect through the primary vtable. |
| `0x00459970-0x004599ca` | max-range setter candidate | Clamps the input to `0..30000`, clamps `m_scrollPosition` when needed, stores `m_scrollRange` at `+0x100`, and invalidates. |
| `0x004599d0-0x004599f6` | scroll-position setter candidate | Stores `m_scrollPosition` at `+0xfe` when changed and invalidates. |
| `0x00459a00-0x00459a19` | enable/show helper candidate | Sets `m_enabled` byte `+0x102` to `1` and invalidates when previously disabled. |
| `0x00459a20-0x00459a39` | disable/hide helper candidate | Sets `m_enabled` byte `+0x102` to `0` and invalidates when previously enabled. |
| `0x00459a40-0x00459a59` | `CanAdjust` predicate candidate | Returns true only when `m_enabled` is nonzero and `m_scrollRange` is greater than zero. |

## Live IDA Evidence

Checked on 2026-06-04 through IDA MCP `py_eval`:

- IDB identity: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x00400000`, MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- IDA has no function object at raw starts `0x004598b0`, `0x00459910`, `0x00459940`, `0x00459970`, `0x004599d0`, `0x00459a00`, `0x00459a20`, or `0x00459a40`.
- Incoming code/data xrefs to each raw start are empty.
- Each helper body is followed by `CC` alignment before the next modeled raw start. The next IDA function object is [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md), `sub_459A60`, with vtable data reference from `0x00610a9c`.

### Boundary And Behavior Inventory

| Start | Return / boundary | Live behavior evidence |
| --- | --- | --- |
| `0x004598b0` | Returns at `0x0045990a`; `CC` alignment to `0x00459910`. | Calls `sub_544460` with argument `1`, stores vtables `0x00610a4c`, `0x00610a98`, and `0x00610ac8`, clears `+0xf8`, stores constructor byte at `+0xfc`, clears `+0xfe`, writes word `0xff01` at `+0x102`, writes byte `0xff` at `+0x104`, and returns `this`. |
| `0x00459910` | Tail-jumps to primary vtable slot `+0x20` when changed; otherwise returns at `0x00459934`; `CC` alignment to `0x00459940`. | Compares incoming word against `m_scrollSkinIndex` at `+0xf8`, stores when different, and invalidates rectangle `this+0x44`. |
| `0x00459940` | Tail-jumps to primary vtable slot `+0x20` when changed; otherwise returns at `0x00459961`; `CC` alignment to `0x00459970`. | Compares incoming byte against `m_scrollStyle` / style-state byte at `+0xfa`, stores when different, and invalidates rectangle `this+0x44`. |
| `0x00459970` | Returns at `0x004599c8`; `CC` alignment to `0x004599d0`. | Clamps incoming word to `0..0x7530` (`30000`, Verified with `int_convert.py`), compares against `m_scrollRange` at `+0x100`, optionally clamps `m_scrollPosition` at `+0xfe`, stores the new range, and invalidates through primary vtable slot `+0x20`. |
| `0x004599d0` | Tail-jumps to primary vtable slot `+0x20` when changed; otherwise returns at `0x004599f4`; `CC` alignment to `0x00459a00`. | Compares incoming word against `m_scrollPosition` at `+0xfe`, stores when different, and invalidates rectangle `this+0x44`. |
| `0x00459a00` | Returns at `0x00459a19`; `CC` alignment to `0x00459a20`. | If `m_enabled` byte `+0x102` is zero, writes `1` and invalidates rectangle `this+0x44`. |
| `0x00459a20` | Returns at `0x00459a39`; `CC` alignment to `0x00459a40`. | If `m_enabled` byte `+0x102` is one, writes `0` and invalidates rectangle `this+0x44`. |
| `0x00459a40` | Returns true at `0x00459a56`, false at `0x00459a59`; `CC` alignment to `0x00459a60`. | Returns true only when `m_enabled` at `+0x102` is nonzero and `m_scrollRange` at `+0x100` is greater than zero. |

### Vtable Evidence

- Constructor vtable stores are live xrefs to `0x00610a4c` from `0x004598c3`, to `0x00610a98` from `0x004598e2`, and to `0x00610ac8` from `0x004598ec`.
- The same vtable bases are installed by inline `RankingEventListPane` child setup at `0x0045aeb2`, `0x0045aeb8`, and `0x0045aec2`, matching the private child-scrollbar ownership model.
- Primary vtable slot `0x00610a90` points to [UID:0001ZA][0x00459ce0-0x0045a3f5.RankingEventScrollPaneOnPaint](by-memory/0x00459ce0-0x0045a3f5.RankingEventScrollPaneOnPaint.md), secondary slot `0x00610a9c` points to [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md), and secondary slot `0x00610aa0` points to [UID:0001Z9][0x00459cd0-0x00459cd4.RankingEventScrollPaneHasActiveEffect](by-memory/0x00459cd0-0x00459cd4.RankingEventScrollPaneHasActiveEffect.md).

### 2026-06-13 Advanced-Scan Recheck

- Live IDA 9.1 MCP session `a001_goal2_0002UM` reconfirmed no function object at raw starts `0x004598b0`, `0x00459910`, `0x00459940`, `0x00459970`, `0x004599d0`, `0x00459a00`, `0x00459a20`, or `0x00459a40`; `0x00459a59` is not a function and the successor modeled function is `sub_459A60` at `0x00459a60`, size `0x26e`.
- `make_signature_for_range 0x004598b0-0x00459a60` returned a unique byte signature containing the constructor, seven helper bodies, their `CC` alignment gaps, and the final `CC` padding before `0x00459a60`.
- `xrefs_to` reconfirmed no incoming xrefs to the eight raw starts. Vtable-base refs remain `0x00610a4c <- 0x004598c3/0x0045aeb2`, `0x00610a98 <- 0x004598e2/0x0045aeb8`, and `0x00610ac8 <- 0x004598ec/0x0045aec2`.
- The advanced-scan warning was a document-label mismatch only: the filename label `RankingEventScrollPaneRawCtorAndSetters` is the intended compact report label, while the range, ownership, score, and raw-helper split remain unchanged.

### 2026-07-09 B004 Current MCP Recheck

- Active MCP session `supervisor_nexustk_20260709` was healthy for `NexusTK.exe.i64`; `server_health` returned `status: ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` reconfirmed that all eight target starts and local returns are not IDA function objects: `0x004598b0`, `0x00459910`, `0x00459940`, `0x00459970`, `0x004599d0`, `0x00459a00`, `0x00459a20`, `0x00459a40`, and the local endpoints through `0x00459a59`. The successor remains [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md), modeled as `sub_459A60`; nearby modeled slots `0x00459cd0` and `0x00459ce0` remain the false virtual and paint method.
- Current `xrefs_to` / `xref_query` returned zero incoming xrefs to each raw start, and `find_bytes` found zero VA pointer-byte hits for raw starts `0x004598b0`, `0x00459910`, `0x00459940`, `0x00459970`, `0x004599d0`, `0x00459a00`, `0x00459a20`, and `0x00459a40`.
- Current byte/signature evidence confirms the exact raw island: `get_bytes 0x004598b0 size 432` covers the constructor, seven helper bodies, and their `0xcc` alignment up to `0x00459a60`; `make_signature_for_range 0x004598b0-0x00459a60` returned a unique range signature.
- Current `insn_query` reconfirmed the constructor call to `sub_544460(1)`, vtable stores to `0x00610a4c`, `0x00610a98`, and `0x00610ac8`, the packed `+0xf8` clear, orientation store at `+0xfc`, current/range clear at `+0xfe/+0x100`, enabled/highlight/active initialization at `+0x102/+0x103/+0x104`, all compare/store/invalidate helper bodies, and the enabled-and-positive-range predicate. `int_convert 0x7530` verified the clamp constant as decimal `30000`.
- Current `entity_query` over the vtable data found `??_7RankingEventScrollPane@@6B@` at `0x00610a4c`, secondary view `0x00610a98`, tertiary view `0x00610ac8`, and the next [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md) boundary at `0x00610ad4`. Vtable xrefs include this raw constructor stores and inline `RankingEventListPane` child setup stores; modeled slot pointers still reference `0x00459a60`, `0x00459cd0`, and `0x00459ce0`.

## Source-Shape Notes

Keep the formal `RECONSTRUCTION_CPP CODE` block blank for this island. The code is source-authored and reconstructable, but current evidence does not prove standalone callable constructor/setter/predicate APIs for the eight raw starts.

The best current source-facing prose labels are:

- constructor/setup with orientation initialization at `+0xfc`;
- `m_scrollSkinIndex` / skin-index word updates at `+0xf8`;
- `m_scrollStyle` / style-state byte updates at `+0xfa`;
- `m_scrollPosition` updates at `+0xfe`;
- `m_scrollRange` clamping at `+0x100`;
- `m_enabled` toggles at `+0x102`;
- the enabled-and-positive-range `CanAdjust` predicate.

Every mutating helper invalidates the inherited pane bounds through primary vtable slot `+0x20` when state changes. Do not copy these labels into standalone C++ bodies from this page until a future class/API pass proves actual callable method declarations.

## Reconstruction Caveat

Target-specific no-standalone proof: current MCP proves no function object, no direct raw-start xrefs, no pointer-byte route, no decompilable raw-start function, and no source declaration/API route for any of the eight helper starts. The constructor/setup body is also mirrored by inline `RankingEventListPane` child setup at `0x0045ae30`, so emitting a standalone constructor from this page would invent an unsupported source route. The setter and predicate bodies are exact enough for prose field/behavior documentation, but standalone formal C++ would still freeze guessed private APIs.

Generated-output note: read-only `auto-generated/NexusTK/ui/dialogs/RankingDialog.cpp` now shows UID0000Y2 as `Completion:88 | Confidence:91 | Empty Emitter Marker` after the scoped validator refresh. Under this no-standalone proof, that marker is expected and does not imply bad ownership.

## Ownership Decision

Keep this island under `RankingEventScrollPane` as a private ranking-dialog scrollbar helper set. It should migrate with [UID:0000MZ][RankingDialog](by-file/RankingDialog.md), not as a generic scrollbar source, unless later evidence proves reuse outside the ranking dialog.

The generated tracker direct-report count `0` for UID0000Y2 was resolved by the accepted B004 report and does not imply bad ownership.

## Cross-References

- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md)
- [UID:0001YK][RankingEventScrollPaneVtables](by-type/by-vtable/RankingEventScrollPaneVtables.md)
- [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md)
- [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md)
- [UID:0001RK][ranking-ui-resources](by-resource/ranking-ui-resources.md)

## Changes

- 2026-07-09 B004 UID0000Y2 implementation callback:
  - Before: `86/90`, stale `+0xf8` current-position wording, stale strict numeric C++ gate wording, and no current `supervisor_nexustk_20260709` evidence section.
  - After: `88/91`; owner/emitter [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md), `RECONSTRUCTABLE:TRUE`, blank optional emitter position, `Nested:0`, and blank formal C++ block remain unchanged. The helper table now treats `+0xf8` as `m_scrollSkinIndex`, `+0xfa` as `m_scrollStyle` / style-state byte, `+0xfe` as `m_scrollPosition`, `+0x100` as `m_scrollRange`, `+0x102` as `m_enabled`, `+0x103/+0x104` as highlight/active part bytes, and `+0x108/+0x10c` as the thumb drag offset.
  - Evidence: accepted B004 report `0000Y2-RankingEventScrollPaneRawCtorAndSetters-source-quality.md`; current MCP session `supervisor_nexustk_20260709` was healthy and reconfirmed exact raw helper boundaries, raw bytes and unique range signature, no function objects, zero incoming raw-start xrefs, zero raw-start pointer-byte hits, vtable labels/xrefs, successor [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md), and `0x7530 == 30000` (Verified with `int_convert.py`). The formal C++ block stays blank under the accepted no-standalone proof because current evidence proves behavior but not standalone callable APIs.
- 2026-06-13: Added the canonical filename/report label `RankingEventScrollPaneRawCtorAndSetters` to the title/status text and recorded the live IDA 9.1 recheck for the advanced-scan warning.
  - Before: the page title/body used only the expanded wording `RankingEventScrollPane Raw Constructor And Setters`, so `memory_ranges.py --advanced-scan report` could not find the compact filename label in document text.
  - After: the compact report label is represented explicitly, with no range, parent, score, reconstructable, or ownership change.
  - Evidence: IDA MCP `lookup_funcs` on 2026-06-13 reconfirmed no functions at the eight raw starts and `sub_459A60` at successor `0x00459a60`; `xrefs_to` reconfirmed zero incoming xrefs for raw starts and the expected vtable-base refs; `make_signature_for_range 0x004598b0-0x00459a60` returned a unique signature for the full raw island plus padding.
- 2026-06-04: Raised `COMPLETION` from `70` to `86` and `CONFIDENCE` from `85` to `90`.
  - Before: the page had older raw-helper notes, leaving exact live boundary, padding, and vtable-slot evidence underdocumented.
  - After: the page records the live IDB identity, every raw helper start/return/alignment gap, empty incoming xrefs for raw starts, constructor vtable-store xrefs, field offsets, invalidation paths, and neighboring modeled vtable slots.
  - Evidence: IDA MCP `py_eval` on 2026-06-04 confirmed no function object at the eight raw starts; confirmed normal returns at `0x0045990a`, `0x00459934`, `0x00459961`, `0x004599c8`, `0x004599f4`, `0x00459a19`, `0x00459a39`, `0x00459a56`, and `0x00459a59`; confirmed `CC` alignment between helpers; and confirmed vtable base refs at `0x004598c3`, `0x004598e2`, `0x004598ec`, `0x0045aeb2`, `0x0045aeb8`, and `0x0045aec2`.
- 2026-05-27: Changed confidence for the raw island from medium boundary confidence to strong boundary/source-authored confidence, while keeping final helper names medium. Evidence: IDA `py_eval` confirmed each raw start has a normal code body/return, no direct xrefs, vtable store xrefs at `0x004598c3`, `0x004598e2`, and `0x004598ec`, and the next modeled function at `0x00459a60`.
- 2026-05-31: Marked reconstructable and attached to [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md); replaced the provisional C++ candidate with source-shape notes.
  - Before: the page contained a non-final C++ sketch with provisional member/helper names.
  - After: the page keeps behavioral reconstruction notes but leaves the validator C++ code block empty until final names and surrounding API confidence meet the near-final threshold.
  - Evidence: by-structure requires emitted reconstruction C++ to be highly plausible final source; this page still records medium-confidence final helper names.
