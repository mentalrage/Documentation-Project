*** UID:0002EV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChattingPane::OnPaint()
{
    if (g_useEpfAssets == 1) {
        Region savedClip;
        Region clippedRegion;
        const unsigned char savedMode = m_mode;

        if (g_pChattingHandlePane->m_secondaryChatEnabled) {
            SetDrawColor(0);
            FillRect(&m_bounds);

            RectBounds clipRect;
            GetClipRect(&clipRect);

            int y = m_scrollContentRect.top;
            const int count = m_pChatMessageList->GetCount();

            for (int index = 0; index < count; ++index) {
                ColorStringChattingMessage *message =
                    *static_cast<ColorStringChattingMessage **>(
                        m_pChatMessageList->GetElementAt(index));

                RectBounds messageRect;
                messageRect.left = 0;
                messageRect.right = 783;

                const short lineCount =
                    static_cast<short>(message->MeasureLines(this, 783));
                messageRect.top = y;
                y += 13 * lineCount;
                messageRect.bottom = y;

                SetMode(savedMode);

                if (IntersectRects(&messageRect, &clipRect, NULL)) {
                    GetClipRegion(&savedClip);

                    if (savedClip.IsEmpty()) {
                        SetClipRect(&messageRect);
                        message->Draw(this, &messageRect);
                        SetClipRegion(&savedClip);
                    } else if (clippedRegion.IntersectWithRect(&messageRect)) {
                        SetClipRegion(&clippedRegion);
                        message->Draw(this, &messageRect);
                        SetClipRegion(&savedClip);
                    }
                }
            }
        } else {
            SetDrawColor(0);
            FillRect(&m_bounds);
        }
    } else {
        Region savedClip;
        Region clippedRegion;

        SetDrawColor(143);
        FillRect(&m_contentRect);

        RectBounds clipRect;
        GetClipRect(&clipRect);

        int y = m_scrollContentRect.top;
        const int count = m_pChatMessageList->GetCount();

        for (int index = 0; index < count; ++index) {
            ColorStringChattingMessage *message =
                *static_cast<ColorStringChattingMessage **>(
                    m_pChatMessageList->GetElementAt(index));

            RectBounds messageRect;
            messageRect.left = 0;
            messageRect.right = 408;

            const short lineCount =
                static_cast<short>(message->MeasureLines(this, 408));
            messageRect.top = y;
            y += 13 * lineCount;
            messageRect.bottom = y;

            if (IntersectRects(&messageRect, &clipRect, NULL)) {
                GetClipRegion(&savedClip);

                if (savedClip.IsEmpty()) {
                    SetClipRect(&messageRect);
                    message->Draw(this, &messageRect);
                    SetClipRegion(&savedClip);
                } else if (clippedRegion.IntersectWithRect(&messageRect)) {
                    SetClipRegion(&clippedRegion);
                    message->Draw(this, &messageRect);
                    SetClipRegion(&savedClip);
                }
            }
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-authored ChattingPane::OnPaint at 0x0047f8d0-0x0047fbc5: vtable-only reachability; current g_useEpfAssets == 1 path uses m_bounds, width 783, saved inherited m_mode, and g_pChattingHandlePane->m_secondaryChatEnabled, while legacy uses m_contentRect and width 408; both build full RectBounds rows from m_scrollContentRect.top, dereference ColorStringChattingMessage ** element storage from List::GetElementAt, narrow MeasureLines to signed short, advance 13 pixels per line, and preserve branch-level automatic Region lifetime plus the observed unseeded clippedRegion nonempty-clip path. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047f8d0-0x0047fbc5 ChattingPane OnPaint

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: paint method.
- Owner: [UID:00001X][ChattingPane](by-class/ChattingPane.md).
- Likely source file: [UID:0000I5][Chatting](by-file/Chatting.md).
- Parent range: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Rebuild handling: `source-authored`.
- Autogen parent: [UID:00001X][ChattingPane](by-class/ChattingPane.md), which emits this exact child through [UID:0000I5][Chatting](by-file/Chatting.md) at `NexusTK/social/Chatting.cpp`.
- Confidence: very strong for range, vtable reachability, message iteration, current/legacy branches, field offsets, full rectangle shape, List/message ABI, clipping, callback direction, and Region lifetime; only exact historical private/helper spellings remain inferred.
- C++ handling: the formal block is implementation-ready, period-appropriate duplicated branch source. It intentionally avoids C++11 lambda/`auto`/defaulted-guard constructs and preserves the binary's unusual unseeded second-Region branch rather than correcting it.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 91 | Fresh live MCP and current support resolve the exact range/padding, sole vtable route, full four-dword `RectBounds`, List element-storage dereference, two-argument message virtuals, inherited bounds/mode fields, current/legacy gates, clip-helper direction, automatic Region lifetime, callback fill role, and period-compatible formal body. |
| Confidence | 93 | Target bytes/disassembly/decompilation, vtable cells, helper bodies, xrefs, accepted layouts, and message/container contracts agree. The remaining cap is only absent original private/helper/typedef spelling; no behavior, ABI, owner, emitter, or source-placement blocker remains. |

## Boundary

Live IDA MCP evidence collected by B004 on 2026-07-13 reports one modeled function at `0x0047f8d0` with size `0x2f5`, giving exclusive end `0x0047fbc5`. The `0x0047f8c9-0x0047f8d0` gap is seven `0xcc` bytes, the `0x0047fbc5-0x0047fbd0` gap is eleven `0xcc` bytes, and the next modeled function begins at `0x0047fbd0`. No split, embedded table, child, or overlap exists inside the method.

## Behavior

This method paints visible chat messages. It selects the current layout only when [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) equals `1`, otherwise selecting the legacy layout. Each selected layout constructs two automatic `Region` objects once at branch scope, queries the effective clip, snapshots the generic `List` count, builds a full four-dword `RectBounds` for each message, intersects that rectangle with the clip rectangle, and dispatches the message draw virtual only when the rectangle is visible and the active Region path accepts it.

Current-layout branch:

- uses message width `783`;
- uses inherited `m_bounds` at receiver offset `+0x44`;
- saves inherited `Pane::m_mode` at `+0xb5` and calls [UID:0002V7][0x005446b0-0x005446d4.PaneSetMode](by-memory/0x005446b0-0x005446d4.PaneSetMode.md) with that saved value before each row visibility/draw gate;
- reads [UID:0002XE][g_pChattingHandlePane](by-global/g_pChattingHandlePane.md) without a null check and gates message iteration on `m_secondaryChatEnabled` at `+0x118`;
- regardless of that gate, sets draw color `0` and calls Surface callback-table slot 7 as source-facing `FillRect(&m_bounds)`;
- when the gate is false, skips `GetClipRect` and the entire message loop while still allowing both branch-scope Regions to clean up automatically.

Old-layout branch:

- uses message width `408`;
- uses inherited `m_contentRect` at receiver offset `+0x110`;
- sets draw color `143` and calls Surface callback-table slot 7 as source-facing `FillRect(&m_contentRect)`;
- has no secondary-chat gate and does not call `SetMode`;
- takes the same count-snapshot/message/clip path whenever the list count is positive.

The message list is the generic `List *m_pChatMessageList` at `this+0x130`. `GetCount()` is the source-facing inline read of list offset `+0x0c`. `GetElementAt(index)` dispatches List vslot `+0x10` and returns element storage; the target immediately loads `[eax]`, so source must dereference the returned `ColorStringChattingMessage **` rather than cast the storage directly to an object. No null check exists for the list or entries. Each concrete entry dispatches [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md) measure vslot `+0x10` and draw vslot `+0x14`.

The measure vslot target for `ColorStringChattingMessage` is [UID:0002GG][0x00483630-0x0048369a.ColorStringChattingMessageMeasureLines](by-memory/0x00483630-0x0048369a.ColorStringChattingMessageMeasureLines.md). This confirms the first-draft call spelling `message->MeasureLines(this, messageWidth)`: the `this` argument is the pane/draw-context receiver forwarded by the target in `ecx` to [UID:00016F][0x004bb070-0x004bb078.ConstantLineCountHelper](by-memory/0x004bb070-0x004bb078.ConstantLineCountHelper.md), while `messageWidth` is the second post-`this` argument used as width. Current-layout calls use width `783`; old-layout calls use width `408`.

Each loop builds a full `RectBounds { left, top, right, bottom }`. `left` is `0`, `top` is the prior running y, `right` is the active width (`783` or `408`), and `bottom` is the updated y. Running y starts at inherited `m_scrollContentRect.top` (`this+0x124`). The low 16 bits returned by `MeasureLines(this, width)` are narrowed to signed `short`, sign-extended, multiplied by `13`, and added to y before `bottom` is stored. `IntersectRects(&messageRect, &clipRect, NULL)` gates row drawing.

Resolved helper roles:

- `0x00554680` is the `Region` constructor. The target calls it four times statically: two objects are constructed once in the current branch and two once in the legacy branch. Normal cleanup and EH funclets route through `0x004f4a90`, producing automatic reverse-order destruction rather than explicit source destructor calls.
- `0x005546f0` is `Region::IsEmpty`; it returns true for empty/invalid stored bounds.
- `0x005547e0` is in-place `Region::IntersectWithRect(const RectBounds *)`, not a normalize/build getter.
- `0x004b9590` copies GrafPort clip state at `+0x54` into a caller Region: `GetClipRegion`.
- `0x004b9580` copies a caller Region into GrafPort clip state: `SetClipRegion`.
- `0x004b95b0` sets GrafPort clip state from a caller `RectBounds`: `SetClipRect`.
- `0x004b9660` is `SetDrawColor`, writing the active draw color at GrafPort `+0x74`; `0x004b96c0` is `GetClipRect`.
- `0x0069b3fc` is slot 7 in [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md). Both installed targets clip the passed rectangle and consume active draw/palette state, so this call site uses `FillRect`. Historical `RefreshPaneRegion` and generated `g_pfnLockSurface` are rejected for this target's source C++.

For each visible row, `GetClipRegion(&savedClip)` snapshots the current Region. If it is empty, `SetClipRect(&messageRect)` applies the row rectangle, `message->Draw(this, &messageRect)` draws, and `SetClipRegion(&savedClip)` restores the saved empty Region. If `savedClip` is nonempty, the target calls `clippedRegion.IntersectWithRect(&messageRect)` on the second Region, which was default-constructed empty once at branch entry and was never seeded from `savedClip`. Only a true result applies `clippedRegion`, draws, and restores `savedClip`. This unusual normally-rejecting branch is observed behavior and must remain uncorrected.

## Live IDA Evidence

- B004's mandatory 2026-07-13 MCP pass used healthy evidence-time database session `160045b9`. `lookup_funcs` and `analyze_function` report `sub_47F8D0`, size `0x2f5`, 249 instructions, 30 basic blocks, and cyclomatic complexity 11.
- `lookup_funcs` reports `0x0047f8d0` size `0x2f5`; lookups at `0x0047f8c9` and `0x0047fbc5` are not functions, and `0x0047fbd0` begins the next modeled function.
- `callers` reports no ordinary direct callers; `xrefs_to 0x0047f8d0` reports only the vtable/data slot at `0x00614d4c`.
- `get_bytes` confirms seven leading `0xcc` bytes before the function and eleven trailing `0xcc` bytes before [UID:0002EW][0x0047fbd0-0x0047fc6b.ChattingPaneSetVisibleRowCount](by-memory/0x0047fbd0-0x0047fc6b.ChattingPaneSetVisibleRowCount.md).
- `callees` reports region/clip construction and destruction helpers at `0x00554680` and `0x004f4a90`; pane region/clip helpers at `0x004b9580`, `0x004b9590`, `0x004b95b0`, `0x004b9660`, `0x004b96c0`, and `0x004b7cc0`; UI byte restore helper `0x005446b0`; additional region tests at `0x005546f0` and `0x005547e0`; draw-state callback `0x0069b3fc`; and stack-cookie verification.
- Decompilation/disassembly confirms current width `783`, legacy width `408`, message-list count at receiver offset `0x130 + 0x0c`, item fetch through List vslot `+0x10` followed by immediate pointer dereference, message measure/draw vslots `+0x10/+0x14`, signed-short narrowing, 13-pixel advance, and full four-dword row rectangles.
- The current branch uses receiver `+0x44` (`m_bounds`), inherited byte `+0xb5` (`m_mode`), and global `0x0067ade0 + 0x118` (`g_pChattingHandlePane->m_secondaryChatEnabled`). The legacy branch uses receiver `+0x110` (`m_contentRect`) and constant draw color `0x8f`; both use inherited `+0x124` (`m_scrollContentRect.top`).
- Helper decompilation proves `0x004b9580` SetClipRegion, `0x004b9590` GetClipRegion, `0x004b95b0` SetClipRect, `0x004b9660` SetDrawColor, `0x004b96c0` GetClipRect, `0x005546f0` IsEmpty, and `0x005547e0` in-place IntersectWithRect.
- Stack/disassembly tracks four contiguous row dwords in each duplicated loop, four Region constructor sites, reverse-order normal/EH cleanup, and no assignment that seeds the second Region before its nonempty-clip intersection.
- Message/container rechecks identify List `GetElementAt` at `0x004f32b0` / vtable cell `0x0061ce3c`, `ColorStringChattingMessage::MeasureLines` at `0x00483630` / cell `0x0061527c`, and `Draw` at `0x004836a0` / cell `0x00615280`.
- Installed slot-7 targets `0x004be680` and `0x004c4380` both clip the caller rectangle and consume active draw/palette state. The evidence-time slot total was 206 xrefs, supporting source-facing `FillRect` for this call site.

## Resolved Source-Quality Questions

- Row representation: full `RectBounds`, not a three-int span; all four fields are live at both intersection and draw calls.
- Container contract: `List::GetElementAt` returns element storage, so the message pointer requires one dereference; `GetCount` is the inline count accessor.
- Inherited fields: `+0x44` is `m_bounds`, `+0x110` is `m_contentRect`, `+0x124` is `m_scrollContentRect.top`, and `+0xb5` is `m_mode`; no target-private aliases are needed.
- Global gate: `0x0067ade0` is `g_pChattingHandlePane`, and `+0x118` is `m_secondaryChatEnabled`; no invented `g_chatUiState` wrapper is accepted.
- Clip direction and lifetime: helper bodies settle Get/Set direction, the second Region remains unseeded, and constructor/EH evidence requires ordinary branch-scope automatic objects rather than a lambda guard or explicit cleanup.
- Callback role: `0x0069b3fc` is shared [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md) slot 7, but both installed targets and this call shape support `FillRect` in target source. It is not a Chatting-owned callback.

## Remaining Source-Name Limits

- Exact original private local names, public clip-wrapper spellings, and the table-wide slot-7 typedef/name are not symbol-recovered. `savedClip`, `clippedRegion`, `GetClipRegion`, `SetClipRegion`, `SetClipRect`, and callsite-facing `FillRect` are the highest-probability source names supported by behavior and current project APIs.
- These spelling limits cap confidence only. They do not affect behavior, ABI, owner/emitter, exact range, source placement, or C++ readiness.

## Historical Superseded Model

- The 2026-06-17 B001 pass lacked live MCP and used a three-int `ChatMessageDrawSpan`, C++11 lambda/`auto`, a defaulted `ClipRegionGuard`, direct object casting from `GetAt`, `m_chatMessagePaintTop`, `m_currentLayoutDrawStateByte`, `g_chatUiState`, `RefreshPaneRegion`, and normalize/build wording for `0x005547e0`.
- Those names and abstractions are retained only as rejected provenance. B004's 2026-07-13 live-MCP pass supersedes them with the exact full-rectangle, element-storage, inherited-field, helper-direction, automatic-lifetime, and latent unseeded-Region model above.

## Cross-References

- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:00001X][ChattingPane](by-class/ChattingPane.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md)
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md)
- [UID:0002XE][g_pChattingHandlePane](by-global/g_pChattingHandlePane.md)
- [UID:0002V7][0x005446b0-0x005446d4.PaneSetMode](by-memory/0x005446b0-0x005446d4.PaneSetMode.md)
- [UID:0003XB][0x004b94f0-0x004b95d5.GrafPortStateAndClipRawHelpers](by-memory/0x004b94f0-0x004b95d5.GrafPortStateAndClipRawHelpers.md)
- [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md)
- [UID:0002EX][0x0047fc70-0x0047fd3b.ChattingPaneComputeLineCount](by-memory/0x0047fc70-0x0047fd3b.ChattingPaneComputeLineCount.md)
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md)

## Changes

- 2026-07-13 B004 UID0002EV source-quality implementation:
  - Raised `86/90` to `91/93`, preserving owner/emitter UID00001X, `RECONSTRUCTABLE:TRUE`, exact range, blank emitter position, and `Nested:0`.
  - Replaced the C++11/decompiler-shaped draft with the accepted period-compatible duplicated current/legacy body. The emitted source uses full `RectBounds`, dereferences `ColorStringChattingMessage **` element storage, narrows MeasureLines to signed short, uses inherited bounds/mode fields, exact secondary-chat gate, exact clip helper directions, automatic branch-level Region lifetime, and source-facing `FillRect`.
  - Preserved exact behavior: current disabled still fills then skips clip/list work; legacy has no gate or SetMode; no null checks were added; the nonempty saved-clip path still intersects the default-empty unseeded second Region and normally rejects drawing.
  - Evidence: mandatory live MCP session `160045b9` confirmed the `0x2f5` body, vtable-only xref `0x00614d4c`, padding, stack rectangles, List/message vslots, inherited offsets, Region constructor/cleanup sites, helper directions, callback installed targets, and negative no-split/no-seed/no-helper facts.
  - The 2026-06-17 B001 lambda/guard/three-int/global/helper model remains below as historical provenance and is explicitly superseded.
- Historical superseded 2026-06-17 B001 source-quality execution:
  - Raised from `84/88` to `86/90` and populated first-draft C++.
  - Incorporated report-level details: exact body/padding/vtable-only evidence; current layout uses `this+0x44`, width `783`, color/state `0`, saved byte `this+0xb5`, the `dword_67ade0 + 0x118` loop gate, callback slot `0x0069b3fc`, and conditional message-loop skip; old layout uses `this+0x110`, width `408`, color/state `143`, and always enters the positive-count message loop; message-list count/fetch is at `this+0x130`, list `+0x0c`, vslot `+0x10`; message measure/draw vslots are `+0x10` and `+0x14`; row spans are `{0, runningTop, width}` with `runningTop` from `this+0x124` and `13 * measuredRows` advance; and GrafPort/Region helper roles are documented for clip save/apply/restore, emptiness, normalization, color-state, clip-rect query, and intersection gating.
  - Historical limit at that time: original field/helper/callback spellings were role-based; B004's 2026-07-13 pass supersedes the behavior/type/source-shape claims as described above.
- 2026-06-21 B006 Rule 26 measure-call sync:
  - Score unchanged at `86/90`.
  - Summary/evidence: added the [UID:0002GG][0x00483630-0x0048369a.ColorStringChattingMessageMeasureLines](by-memory/0x00483630-0x0048369a.ColorStringChattingMessageMeasureLines.md) cross-note proving `message->MeasureLines(this, messageWidth)` is a two-argument virtual call, not a width-only call; the first argument is forwarded in `ecx` as the draw/context receiver and the second argument is the active `783`/`408` width.
- 2026-06-05 A004 live refresh:
  - What existed before: the page had correct broad paint ownership but remained `72/82`, had blank parent metadata, and still used raw placeholder names and decompiler-shaped receiver offsets in the evidence.
  - Changed to: `84/88`, `RECONSTRUCTABLE:TRUE`, parent [UID:00001X][ChattingPane](by-class/ChattingPane.md), rewritten score/boundary/behavior/evidence sections, and no C++ block.
  - Summary/evidence: live IDA confirmed exact `0x0047f8d0-0x0047fbc5` bounds, vtable-only reachability at `0x00614d4c`, no ordinary direct callers, padding before/after, current/old layout widths, message-list fetch/count path, message measure/draw virtual slots, region helper lifecycle, callback/global-state uses, and stack-protected cleanup. At that time, source emission was blocked by unresolved helper/type/callback names; B001 later supplied provisional role names, and B004's 2026-07-13 pass supersedes those provisional source-shape claims.
- 2026-05-31: Created exact paint-method page and set completion/confidence to `72/82`. Evidence is IDA MCP function lookup, decompilation, vtable xref, and byte-boundary audit for `0x0047f8d0`.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and left the parent blank.
  - Reasoning: existing documentation identifies source-owned `ChattingPane` paint logic, but the page is `72/82` and fails the child-side 80/80 attachment gate.
