*** UID:0002UO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewPredefinedFormArticleDialog::OnPaint()
{
    EPFFrame headerFrame;
    EPFFrame rowFrame;
    EPFFrame footerFrame;
    RectBounds drawRect;

    headerFrame.Clear();
    rowFrame.Clear();
    footerFrame.Clear();

    m_redrawPending = false;
    SetPaneDrawMode(0);
    g_surfaceCallbacks.FillOrPresent(this, &m_paintRegion);
    SetPaneDrawMode(0x80);

    if (g_useEpfAssets)
    {
        g_pEPFLib->LoadFrame(L"DLGBBS08.EPF", 0, &headerFrame);
        g_pEPFLib->LoadFrame(L"DLGBBS08.EPF", 1, &rowFrame);
        g_pEPFLib->LoadFrame(L"DLGBBS08.EPF", 2, &footerFrame);

        drawRect.Set(0, 0, 450, 120);
        DrawEPFFrame(&headerFrame, &drawRect, L"DLGBBS08.PAL");

        drawRect.Offset(0, 120);
        drawRect.bottom = drawRect.top + 25;
        for (unsigned char row = 0;
             row < m_visibleFormRowCount;
             ++row)
        {
            DrawEPFFrame(&rowFrame, &drawRect, L"DLGBBS08.PAL");
            drawRect.Offset(0, 25);
            drawRect.bottom = drawRect.top + 25;
        }

        drawRect.bottom = drawRect.top + 84;
        DrawEPFFrame(&footerFrame, &drawRect, L"DLGBBS08.PAL");
        return;
    }

    g_pEPFLib->LoadFrame(L"DLGBBS08.EPD", 0, &headerFrame);
    g_pEPFLib->LoadFrame(L"DLGBBS08.EPD", 1, &rowFrame);
    g_pEPFLib->LoadFrame(L"DLGBBS08.EPD", 2, &footerFrame);

    drawRect.Set(0, 0, 446, 88);
    DrawEPFFrame(&headerFrame, &drawRect, L"NPAL8.PAL");

    drawRect.Offset(0, 88);
    drawRect.bottom = drawRect.top + 21;
    for (unsigned char row = 0;
         row < m_visibleFormRowCount;
         ++row)
    {
        DrawEPFFrame(&rowFrame, &drawRect, L"NPAL8.PAL");
        drawRect.Offset(0, 21);
        drawRect.bottom = drawRect.top + 21;
    }

    drawRect.bottom = drawRect.top + 70;
    DrawEPFFrame(&footerFrame, &drawRect, L"NPAL8.PAL");
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `NewPredefinedFormArticleDialog::OnPaint()` draws variable-height current/legacy `DLGBBS08` form-article chrome through the `g_useEpfAssets` branch, with formal first-draft C++ emitted via the class route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00478380-0x0047864c NewPredefinedFormArticleDialog OnPaint

## Status

- Disposition: reconstructable NexusTK dialog method with formal first-draft C++ entered.
- Entity kind: modeled virtual paint method.
- Direct owner: [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
- Parent aggregate: [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md).
- Likely source file: [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md).
- Rebuild handling: `source-authored`.
- Autogen route: emitted through direct class parent [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), which routes to file root [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md). The method clears the current code-entry gate because it is `RECONSTRUCTABLE:TRUE`, has `EMITTER_UIDS:000098`, the class/file route is valid, and the combined score is above 85.
- Confidence: strong for exact function range, vtable-only route, branch behavior, resource bytes, draw-loop arithmetic, row-count flow, and source-facing first-draft names. Exact original helper, callback-member, and field spellings remain confidence caps, not blank-C++ blockers.

## Address Range

`0x00478380-0x0047864c`, half-open.

- 2026-06-25 B005 active IDA MCP session `80de0a67` used IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; health was ready with `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`, module `NexusTK.exe`, and image base `0x400000`.
- MCP tools used for the accepted source-quality pass included `server_health`, `lookup_funcs`, `analyze_function`, `disasm`, `xrefs_to`, `get_bytes`, `get_string`, `find_bytes`, and `make_signature_for_function`. The live tool list did not expose a general `py_eval`, so the pass used structured MCP tools instead.
- `lookup_funcs(0x00478380)` returned `sub_478380`, size `0x2cc`.
- `lookup_funcs(0x00478379)` and `lookup_funcs(0x0047864c)` did not return a containing function.
- `lookup_funcs(0x00478650)` returned next function `sub_478650`, size `0x2e3`.
- `analyze_function(0x00478380)` reported 716 bytes, 239 instructions, 11 basic blocks, cyclomatic complexity 6, no normal code callers, and one data xref to the function.
- Predecessor bytes at `0x00478379-0x00478380` are seven `0xcc` bytes after [UID:0002UN][0x00478370-0x00478379.NewPredefinedFormArticleDialogSetHoverControl](by-memory/0x00478370-0x00478379.NewPredefinedFormArticleDialogSetHoverControl.md).
- Successor bytes show the stack-cookie epilogue ending in `retn` at `0x0047864b`, then four `0xcc` bytes at `0x0047864c-0x00478650` before the parser function at `0x00478650`.

## Behavior

`NewPredefinedFormArticleDialog::OnPaint()` paints the predefined-form article compose dialog by drawing a header tile, one repeated row/body tile per visible parsed form row, and a footer tile. It selects the current EPF resource path or legacy EPD resource path through [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) / raw `byte_66DA97`.

Shared setup in both branches:

- Clears the instance repaint/redraw byte at `this + 0x70`, represented in first-draft source as `m_redrawPending = false`.
- Calls `SetPaneDrawMode(0)`, dispatches the surface render callback table slot at `0x0069b3fc` with `this` and `this + 0x44`, then calls `SetPaneDrawMode(0x80)`. The C++ uses a source-facing wrapper `g_surfaceCallbacks.FillOrPresent(this, &m_paintRegion)` instead of the stale generated alias `g_pfnLockSurface`.
- Reads the visible form-row count from signed word `this + 0x276`, represented as `m_visibleFormRowCount`. Constructor/parser support docs identify this as distinct from the full parsed-entry count at `this + 0x274`.
- Loops with an unsigned byte row counter; if the visible row count is non-positive, the repeated-row draw loop is skipped and only the footer is drawn.

When `g_useEpfAssets == 1`:

- Loads `DLGBBS08.EPF` frame indexes `0`, `1`, and `2` from [UID:0001OQ][0x0067a744-0x0067a748.g_pEPFLib](by-memory/0x0067a744-0x0067a748.g_pEPFLib.md) into header, row, and footer frame contexts.
- Builds the initial draw rectangle as `(0, 0, 450, 120)` using the rectangle setter.
- Draws the header through `DLGBBS08.PAL`.
- Advances the rectangle by 120 after the header, then draws each visible row with height 25 and advances by 25 after each row.
- Sets the footer height to 84 at the current top and returns the final footer draw result.

When `g_useEpfAssets != 1`:

- Loads `DLGBBS08.EPD` frame indexes `0`, `1`, and `2`.
- Builds the initial draw rectangle as `(0, 0, 446, 88)`.
- Draws the header through `NPAL8.PAL`.
- Advances the rectangle by 88 after the header, then draws each visible row with height 21 and advances by 21 after each row.
- Sets the footer height to 70 at the current top and returns the final footer draw result.

## Evidence

- Route/liveness: `xrefs_to(0x00478380)` returned the vtable/data reference at `0x00614094`; `find_bytes` for the little-endian VA pointer `80 83 47 00` matched exactly `0x00614094`; `find_bytes` for the RVA pattern `80 83 07 00` returned no hits. No ordinary code callers were found, which is expected for this virtual paint handler rather than evidence that it is dead.
- Callees: the method calls the frame initializer/clear helper, pane draw-mode setter, surface render callback slot at `0x0069b3fc`, EPF/EPD frame loader, rectangle setter, resource-frame draw helper, rectangle offset helper, and the stack-cookie check. Stack-cookie setup/check is compiler-only and is not represented as handwritten source.
- Current branch resource facts: `DLGBBS08.EPF` at `0x006149f8` is referenced at `0x004783f6`, `0x00478408`, and `0x0047841a`; `DLGBBS08.PAL` at `0x00614a14` is referenced at `0x00478442`, `0x00478492`, and `0x004784de`.
- Legacy branch resource facts: `DLGBBS08.EPD` at `0x00614a30` is referenced at `0x0047853a`, `0x0047854c`, and `0x0047855e`; `NPAL8.PAL` at `0x0061484c` is referenced at `0x00478586`, `0x004785d2`, and `0x0047861e`.
- `get_bytes` confirms the UTF-16 string bytes for `DLGBBS08.EPF`, `DLGBBS08.PAL`, `DLGBBS08.EPD`, and `NPAL8.PAL`. IDA `get_string` truncates some wide-string renderings to one or two visible characters, so byte-level evidence controls the full resource names.
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) identifies raw `byte_66DA97` as the current EPF/high-layout versus legacy EPD/layout selector. This target uses that source-facing name while preserving the raw address/name as evidence.
- [UID:0001OQ][0x0067a744-0x0067a748.g_pEPFLib](by-memory/0x0067a744-0x0067a748.g_pEPFLib.md) identifies raw `dword_67A744` as the process-wide EPF/EPD image/layout registry pointer used by all frame loads in this method.
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md) identifies raw `dword_69B3FC` as a broad fill/invalidation/presentation callback slot and rejects `g_pfnLockSurface` as a source-quality paint-path name.

## Source-Facing Names And Interpretation

| Raw/generated label | Source-facing interpretation | Disposition |
| --- | --- | --- |
| `sub_478380` | `NewPredefinedFormArticleDialog::OnPaint()` | Formal method name used in C++ block. |
| `byte_66DA97` | `g_useEpfAssets` | Source-facing global name; raw alias kept for evidence/search. |
| `dword_67A744` | `g_pEPFLib` | Source-facing global pointer; exact final pointer type remains a support-doc issue. |
| `unk_69B3FC` / `dword_69B3FC` | `SurfaceRenderCallbackTable` slot wrapped as `g_surfaceCallbacks.FillOrPresent` | Source-facing wrapper used in C++; stale `g_pfnLockSurface` is rejected for this call site. |
| `off_6149F8` | `DLGBBS08.EPF` | Byte-decoded current frame resource literal. |
| `off_614A14` | `DLGBBS08.PAL` | Byte-decoded current palette resource literal. |
| `off_614A30` | `DLGBBS08.EPD` | Byte-decoded legacy frame resource literal. |
| `aNp` / `0x0061484c` | `NPAL8.PAL` | Byte-decoded legacy palette literal. |
| `sub_457A60` | `EPFFrame::Clear()` style frame-context clear/init | First-draft source name; exact helper spelling unresolved. |
| `sub_4B9660` | `SetPaneDrawMode` | First-draft source helper name. |
| `sub_4D02F0` | `g_pEPFLib->LoadFrame` | First-draft source helper name for frame lookup/load. |
| `sub_4B7C50` | `Rect::Set` | First-draft rectangle initializer. |
| `sub_4B7E10` | `Rect::Offset` | First-draft rectangle advance helper. |
| `sub_4B9980` | `DrawEPFFrame` | First-draft frame draw helper. |
| `this + 0x44` | `m_paintRegion` | First-draft field name for callback draw region. |
| `this + 0x70` | `m_redrawPending` | First-draft field name for the cleared paint/redraw state byte. |
| `this + 0x276` | `m_visibleFormRowCount` | First-draft field name supported by constructor/parser row-count flow. |

## Rejected Alternatives

- Keep formal C++ blank because there are no direct callers: rejected. The vtable pointer at `0x00614094` is the expected route for a paint virtual, and class/file ownership is already documented.
- Treat this range as a no-owner helper: rejected. [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md) owns the virtual method and [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md) owns the source-file route.
- Split the function or adjust the range: rejected. MCP function boundaries, predecessor/successor padding, and neighbor lookups support the current half-open range.
- Rename the target for a legacy suffix/resource branch: rejected. Both branches are one `OnPaint` implementation selected by `g_useEpfAssets`.
- Use `g_pfnLockSurface` for `0x0069b3fc`: rejected by [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md), which identifies the slot as broader fill/invalidation/presentation infrastructure and marks the lock-surface name as callsite-biased.
- Treat `DLGBBS08.EPF` and `DLGBBS08.EPD` as unrelated behavior: rejected. They are current and legacy resource families for the same variable-height form-article dialog chrome.
- Create or require a dedicated resource page for `DLGBBS08` during this callback: not required. The strings are target-local in current xref evidence and are incorporated into this target plus class/file support docs.

## Open Questions

- Exact original spellings for the frame context type, rectangle helper, draw helper, surface callback wrapper/member, `m_paintRegion`, `m_redrawPending`, and `m_visibleFormRowCount` are not recovered from symbols. The source-facing names above are accepted first-draft names inferred from behavior, support docs, and surrounding source shape.
- These spelling/type uncertainties cap the target below final-audit `95+` scores. They no longer block formal first-draft C++ because control flow, dimensions, resource strings, branch condition, owner/emitter route, frame indexes, and row-count data flow are exact enough for a source-ready method body.

## Score Rationale

- Completion is raised from `86` to `90` because this page now records the current B005 MCP session `80de0a67`, exact function range and padding, vtable-only route, positive VA pointer hit, negative RVA pointer scan, both paint branches, draw-loop arithmetic, resource bytes, source-facing helper/global/field mappings, rejected alternatives, and formal first-draft C++.
- Confidence is raised from `90` to `92` because current MCP evidence confirms the function size, no normal callers, single vtable data xref, pointer scan result, resource byte strings, branch constants, and support-doc source names. It remains below `95+` because exact original helper/member spellings and the final callback wrapper signature are still inferred rather than recovered.

## Cross-References

- [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md)
- [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md)
- [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md)
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md)
- [UID:0001OQ][0x0067a744-0x0067a748.g_pEPFLib](by-memory/0x0067a744-0x0067a748.g_pEPFLib.md)
- [UID:0001PI][0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable](by-memory/0x0069b3e0-0x0069b410.SurfaceRenderCallbackTable.md)

## Changes

- 2026-08-14 B004 UID0000LT implementation callback: raised `90/92` to `93/94` and corrected the formal contract from historical `int OnPaint()` to exact inherited `void OnPaint()`. The accepted body keeps the current EPF and legacy EPD resource branches, exact `DLGBBS08.EPF`/`DLGBBS08.PAL` and `DLGBBS08.EPD`/`NPAL8.PAL` pairs, frame indices, dimensions, row heights, row-count flow, draw sequencing, callback setup, and explicit current-branch return without fabricating a footer return value.

- 2026-06-25 B005 accepted implementation for [UID:0002UO]: changed scores `86/90 -> 90/92`, inserted formal first-draft `int NewPredefinedFormArticleDialog::OnPaint()` C++, updated Item Summary to source-ready status, replaced stale no-direct-caller/helper-name/old-`95/95` blank-C++ language, and incorporated MCP session `80de0a67` evidence for range, padding, vtable route at `0x00614094`, one VA pointer hit, no RVA pointer hit, resource bytes, branch constants, row-count field, source-facing helper/global names, rejected alternatives, and confidence caps.
- 2026-06-13 A001 Goal 2 [UID:0002UO] low-score repair: changed scores `76/84 -> 86/90`, changed `AUTOGEN_PARENT_UID` from file [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md) to direct class parent [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), and added live IDA MCP evidence for exact bounds, vtable-only reachability, resource branch layout, local byte/string checks, touched state, and loop arithmetic. The older conclusion that final C++ had to stay blank is superseded by the 2026-06-25 B005 source-quality pass and current code-entry gate.
