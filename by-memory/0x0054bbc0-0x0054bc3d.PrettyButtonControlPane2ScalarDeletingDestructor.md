*** UID:0004EL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 125-byte PrettyButtonControlPane2 compiler deleting-destructor clone: duplicates the ordinary destructor teardown, is reachable only through the primary deleting slot and two receiver-adjustor thunks, applies exact bit-1/bit-4 storage-release behavior, returns complete `this`, and is intentionally false/blank because human source is the class virtual destructor plus UID0004EB body. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0054bbc0-0x0054bc3d PrettyButtonControlPane2 Scalar Deleting Destructor

## Exact Range, Bytes, Hash, And Boundaries

- Exact half-open range: `[0x0054bbc0,0x0054bc3d)`.
- Exact size: `0x7d` / decimal 125.
- SHA256 over the exact range: `85FB4BCE2152DA8366C2B5F5CD30DFC676843B1E8453FE7CD416E6EB21E95EC8`.
- First 16 bytes: `55 8b ec 56 8b f1 8b 8e 28 01 00 00 c7 06 7c 23`.
- Last 16 bytes: `56 e8 6d fa ec ff 83 c4 08 8b c6 5e 5d c2 04 00`.
- [UID:0004EK][0x0054bbaa-0x0054bbb5.PrettyButtonControlPaneTertiaryAdjustorThunk](by-memory/0x0054bbaa-0x0054bbb5.PrettyButtonControlPaneTertiaryAdjustorThunk.md) ends at `0x0054bbb5`; `[0x0054bbb5,0x0054bbc0)` is 11 bytes of parent-only `0xcc` alignment.
- `[0x0054bc3d,0x0054bc40)` is 3 bytes of parent-only `0xcc` alignment before class1 sibling [UID:0004EM][0x0054bc40-0x0054bcbd.PrettyButtonControlPaneScalarDeletingDestructor](by-memory/0x0054bc40-0x0054bcbd.PrettyButtonControlPaneScalarDeletingDestructor.md).
- No split, merge, rename, new child, endpoint, position, or nesting change is required. `Nested:0` remains correct relative to the address-sorted predecessor and successor.

## ABI, CFG, And Instruction Evidence

- B011 MCP `lookup_funcs`: `0x0054bbc0` size `0x7d`.
- B011 MCP `analyze_function`: callees include `sub_4F4AC0`, `sub_544580`, `@_guard_check_icall_nop@4`, and `sub_582B70`.
- x86 calling convention: `__thiscall`, complete-object receiver in `ecx`, one four-byte compiler flags stack argument, `retn 4`, complete-object `this` returned in `eax`.
- Modeled shape: 39 instructions, 7 basic blocks, cyclomatic complexity 3.
- Vtable page UID0002UX records primary slot `0x0062237c -> 0x0054bbc0` and constructor/destructor stores to all three class2 views.
- B004 UID0004EB implementation resolves the source-owned ordinary destructor body at `0x0054b990-0x0054b9db` as `PrettyButtonControlPane2::~PrettyButtonControlPane2() { delete m_callback; }`. This wrapper duplicates that vtable/member/base cleanup sequence, then applies scalar deleting destructor flag/free behavior.

## Exact Flag Matrix

| Condition | Binary behavior | Source disposition |
| --- | --- | --- |
| `(flags & 1) == 0` | Perform complete teardown, do not release object storage, return complete `this`. | Compiler destructor entry behavior; no handwritten source branch. |
| `(flags & 1) != 0 && (flags & 4) == 0` | Perform teardown, call global [UID:000197][0x004f4ac0-0x004f4b00.OperatorDeleteWrapper](by-memory/0x004f4ac0-0x004f4b00.OperatorDeleteWrapper.md) `operator delete(this)`, return complete `this`. | Compiler scalar-delete lowering. |
| `(flags & 1) != 0 && (flags & 4) != 0` | Perform teardown, pass object size `0x12c` and `this` to direct helper `0x0041b6a0`, then return without storage release. | Compiler/runtime no-op branch; not vector deletion, sized deletion, or a source callback. |

Live disassembly proves `0x0041b6a0` is a one-byte `retn` body named `@_guard_check_icall_nop@4`. The bit-4 path is therefore a direct no-op compiler/runtime branch. The `0x12c` immediate independently confirms the complete class2 object size.

## Exact Teardown Sequence

The target duplicates the ordinary destructor lowering rather than calling UID0004EB:

1. Load `m_callback` from complete-object offset `+0x128`.
2. Restore class2 primary, secondary, and tertiary vtable views at `+0x00`, `+0xa0`, and `+0xa4`.
3. If the callback is non-null, invoke virtual slot zero with deleting flag `1`, source-shaped as `delete m_callback`.
4. Destroy `m_paletteResourceName` at `+0x124` through `sub_582B70`.
5. Destroy `m_epfResourceName` at `+0x120` through `sub_582B70`.
6. Call `sub_544580` for inherited Pane/ControlPane teardown.
7. Apply the compiler deletion flags and return complete `this`.

The only source-authored derived destructor statement is UID0004EB R6, `delete m_callback;`. Vtable stores, member destructor calls, base teardown, flags, storage release, return-this ABI, and the bit-4 no-op branch are compiler lowering.

## Reachability And Liveness

- Primary deleting slot `0x0062237c` contains UID0004EL directly.
- Secondary cell `0x006223e4` contains [UID:0004EH][0x0054bb89-0x0054bb94.PrettyButtonControlPane2SecondaryAdjustorThunk](by-memory/0x0054bb89-0x0054bb94.PrettyButtonControlPane2SecondaryAdjustorThunk.md), whose two-instruction thunk subtracts `0xa0` and jumps to UID0004EL.
- Tertiary cell `0x00622414` contains [UID:0004EI][0x0054bb94-0x0054bb9f.PrettyButtonControlPane2TertiaryAdjustorThunk](by-memory/0x0054bb94-0x0054bb9f.PrettyButtonControlPane2TertiaryAdjustorThunk.md), whose two-instruction thunk subtracts `0xa4` and jumps to UID0004EL.
- Direct target xrefs are the two thunk jumps plus primary vtable data reference; no ordinary direct call exists.
- The target is live compiler ABI output, not a dead/raw helper and not a separately source-authored destructor.

## Source And Compiler Decomposition

Human source is represented by:

- complete [UID:0000AS][PrettyButtonControlPane2](by-class/PrettyButtonControlPane2.md) virtual destructor declaration and exact `0x12c` layout;
- [UID:0004EB][0x0054b990-0x0054b9db.PrettyButtonControlPane2DestructorBody](by-memory/0x0054b990-0x0054b9db.PrettyButtonControlPane2DestructorBody.md) ordinary destructor body R6, `delete m_callback;`;
- complete [UID:000038][ControlPane](by-class/ControlPane.md) `ControlPane` and [UID:00001E][ButtonControlPane](by-class/ButtonControlPane.md) `ButtonControlPane` declarations that regenerate inherited destruction and compiler tables.

Compiler output represented only as documentation includes this deleting clone, vptr restores, StringBase/member/base destructor lowering, deletion flags, storage release, complete-object return, adjustor thunks, RTTI/vtables, and the no-op bit-4 branch. The narrow semantic owner remains class UID0000AS; the source file route remains UID0000MP. The wrapper itself is not reconstructable and has no emitter.

## Exact No-Code Proof

Formal C++ is intentionally blank because:

1. The target accepts compiler deletion flags and returns complete `this`.
2. It duplicates the ordinary destructor's member/base teardown.
3. It conditionally calls global operator delete.
4. It contains the compiler/runtime bit-4 no-op branch with object-size immediate.
5. It is entered through deleting vtable slots and receiver-adjustor thunks.
6. Human source is already represented by the class virtual destructor and UID0004EB body.
7. Any target body or comment emitter would duplicate source or expose ABI artifacts.

A covered-by comment is also invalid because this page is compiler output rather than a reconstructable source-authored body shared with another emitter.

## Rejected Alternatives And Negative Evidence

- No evidence supports a second source-level destructor contract, ordinary direct caller, vector deletion, sized deletion, manual vptr stores, explicit member/base destructor calls, handwritten free flags, or raw return-this ABI.
- No evidence supports merging the distinct modeled deleting entry into the raw ordinary destructor page.
- No evidence supports direct file, FunctionObjects, StringBase, Pane, MemoryMan/operator-delete, caller, new-wrapper-file, or no-owner semantics over the narrow class owner.
- No evidence supports a target split, merge, rename, new UID, new source file, or manual vtable/RTTI arrays.
- Raw `void **Block`, `char` flags, `sub_` labels, address arithmetic, and decompiler temporaries are not source-facing types or names.
- The old comment-only emitter is retained only as historical pre-callback generated representation; it is not current source policy.

## Score Rationale

Completion is `92` because exact range/size/hash/padding, full ABI/CFG/instruction facts, complete flag matrix, teardown parity, vtable/thunk reachability, source/compiler decomposition, no-code proof, rejected alternatives, owner/file placement, and support dependencies are resolved.

Confidence is `94` because live bytes/disassembly, the primary vtable and two exact adjustor routes, UID0004EB parity, global delete/no-op helper bodies, and the `0x12c` size immediate independently converge on the compiler deleting-destructor disposition. Exact original compiler version and original header/member spellings remain unavailable.

## History

- Pre-callback metadata was `86/90`, reconstructable true, emitter UID0000AS, with a comment-only ABI marker. That representation correctly recognized compiler generation but incorrectly emitted artificial source text.
- The accepted UID0004EL callback reclassifies only the wrapper source semantics. Exact path, UID, title, range, owner, blank position, `Nested:0`, predecessor/successor padding, binary evidence, and ordinary-destructor relationship remain unchanged.
