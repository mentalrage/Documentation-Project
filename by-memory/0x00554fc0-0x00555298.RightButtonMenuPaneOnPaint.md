*** UID:0004FC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000C0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000C0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RightButtonMenuPane::OnPaint()
{
    DrawBackground();
    DrawBorder();

    RectBounds targetNameBounds = m_visibleBounds;
    targetNameBounds.left += 4;
    targetNameBounds.top = 9;
    targetNameBounds.right -= 4;
    targetNameBounds.bottom = 22;

    SetDrawColor(128);
    FillRect(&targetNameBounds);

    SetTextColor(143);

    const int targetNameLength = static_cast<int>(wcslen(m_targetName));
    const int centerX = (m_visibleBounds.left + m_visibleBounds.right) / 2;
    const int targetNameX = centerX - (targetNameLength * 6) / 2;

    MoveTo(targetNameX, 21);
    DrawWideText(m_targetName, targetNameLength);
    MoveTo(targetNameX + 1, 21);
    DrawWideText(m_targetName, targetNameLength);

    SetTextColor(128);

    const int itemTextX = centerX - 24;

    MoveTo(itemTextX, 37);
    DrawWideText(L"Look", 4);
    MoveTo(itemTextX + 1, 37);
    DrawWideText(L"Look", 4);

    MoveTo(itemTextX, 53);
    DrawWideText(L"Trade", 5);
    MoveTo(itemTextX + 1, 53);
    DrawWideText(L"Trade", 5);

    MoveTo(itemTextX, 69);
    DrawWideText(L"Whisper", 7);
    MoveTo(itemTextX + 1, 69);
    DrawWideText(L"Whisper", 7);

    MoveTo(itemTextX, 85);
    DrawWideText(L"Group", 5);
    MoveTo(itemTextX + 1, 85);
    DrawWideText(L"Group", 5);

    MoveTo(itemTextX, 117);
    DrawWideText(L"Info", 4);
    MoveTo(itemTextX + 1, 117);
    DrawWideText(L"Info", 4);

    if (m_highlightedMenuItem >= 0 && m_highlightedMenuItem <= 4)
    {
        RectBounds itemBounds;
        GetRightButtonMenuItemRect(m_highlightedMenuItem, &itemBounds);

        const int left = itemBounds.left + 5;
        const int right = itemBounds.right - 5;

        MoveTo(left, itemBounds.bottom);

        if (m_menuItemPressed)
        {
            SetDrawColor(128);
            LineTo(left, itemBounds.top);
            LineTo(right, itemBounds.top);
            SetDrawColor(143);
        }
        else
        {
            SetDrawColor(143);
            LineTo(left, itemBounds.top);
            LineTo(right, itemBounds.top);
            SetDrawColor(128);
        }

        LineTo(right, itemBounds.bottom);
        LineTo(left, itemBounds.bottom);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Paints the right-click target menu title, Look/Trade/Whisper/Group/Info labels, and selected-row outline using fixed row geometry. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00554fc0-0x00555298 - RightButtonMenuPane OnPaint

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored paint routine.
- Direct owner: [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md) `RightButtonMenuPane`.
- Emitter route: [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), with blank position so class-child address order remains authoritative.
- Parent split container: [UID:0001FX][0x00554b40-0x0055577b.RightButtonMenuPaneCore](by-memory/0x00554b40-0x0055577b.RightButtonMenuPaneCore.md) `RightButtonMenuPaneCore`.
- Formal C++: source-shaped first draft is complete. It uses the accepted GrafPort/pane APIs and direct literals without raw IDA labels.

## Address Range

- Start: `0x00554fc0`
- End: `0x00555298`
- Size: `0x2d8` / 728 bytes, confirmed by MCP `lookup_funcs`; exact range SHA256 is `BB00F6394A79A46E1685CBB595B541D427D18BDE98AE3AA1C2F354AAB4889763`.
- Following bytes: `0x00555298-0x005552a0` are `0xCC` alignment bytes.

## Signature

IDA decompiles the body as `unsigned __int8 __thiscall sub_554FC0(int this)`. Source-facing role is `RightButtonMenuPane::OnPaint()`.

## Behavior

The paint routine draws the background and border through vtable slots, draws the target name centered near the top, draws five right-click menu labels with a shadow/bold effect, and draws a selected-row outline when the hover row is within `0..4`.

## Label Evidence

| Label | Paint y | Operand evidence |
| --- | --- | --- |
| `Look` | `37` | `0x00623084`, pushed twice at `0x005550d0`/`0x005550eb`. |
| `Trade` | `53` | `0x00623090`, pushed twice at `0x00555103`/`0x0055511b`. |
| `Whisper` | `69` | `0x0061537c`, bytes decode UTF-16LE `Whisper`. |
| `Group` | `85` | `0x0061538c`, adjacent to `Whisper` literal. |
| `Info` | `117` | `0x0062309c`, pushed twice at `0x00555193`/`0x005551ab`. |

## Raw Code Evidence

MCP session `1fc4a5a7`:

- `analyze_function(0x00554fc0)` shows vtable calls at `+72` and `+76`, target-name draw from `this+256`, label draw calls through `sub_4B9600`/`sub_4BAB70`, and selected-row outline through `sub_5556A0` and `sub_4B98F0`.
- `disasm(0x00554fc0, offset 100)` captures the exact operands for Look, Trade, Whisper, and Group.
- `get_bytes(0x0061537c, 48)` decodes `Whisper\0Group\0Clan...`.

## Touched State

- Full object `this+248`: selected-row outline color mode / pressed state.
- Full object `this+249`: hover/selected row; if `<=4`, the row rectangle is highlighted.
- Full object `this+256`: UTF-16 target name.
- Full object bounds at `this+68`, `this+72`, `this+76`, and `this+80` are used to center text and draw the top title rectangle.

## Callees

- Vtable slots at offsets `+72` and `+76`.
- Drawing helpers: `sub_4B9660`, `unk_69B3FC`, `sub_4B9680`, `sub_4B9600`, `sub_4BAB70`, `sub_4B98F0`.
- Internal helper: `GetMenuItemRect` at `0x005556a0`.

## Negative Evidence

The command-string child at `0x00623084` covers only Look/Trade/Info. Whisper and Group are pooled literals at `0x0061537c` and `0x0061538c`; do not describe UID0003CZ as the complete five-label string owner.

No table or resource object selects these labels: each label address is pushed directly twice. No separate string-data emitter should be introduced. The generated `Info` y-coordinate must not be normalized to the regular 16-pixel sequence: the binary draws it at `117`, while the hit-test helper still loops only rows `0..4` with row bottoms ending at `102` and `OnEvent` still has case `4`. This is an exact runtime quirk or original defect, not a documentation error.

## Source Closure

The formal body preserves the exact call order: virtual background then border; an inset title rectangle built from `m_visibleBounds`; draw color `128`; fill; text color `143`; `wcslen` and six-pixel centering; two target-name draws at y `21`; text color `128`; two draws per menu literal at y `37`, `53`, `69`, `85`, and `117`; then signed selected-row gating and a four-edge inset frame whose `128`/`143` order reverses when pressed. The inferred human names match established GrafPort and Vote-menu documentation; no `sub_`, `dword_`, or raw offset survives in source.

The H channel remains blank because UID0000C0 supplies `virtual void OnPaint();` exactly once in the class declaration.

## Confidence And Open Questions

Behavior, range, vtable dispatch, fields, literal addresses, draw constants, and source route are strong. The original private spellings are not symbol-proven, but the selected project-consistent names preserve exact runtime behavior and no longer block first-draft source.

## Changes

- 2026-07-28 B001 UID0003CZ accepted implementation callback: raised `89/91 -> 93/95`, attached emission to UID0000C0, inserted the exact source-shaped `OnPaint` body, added the range hash, direct-literal source disposition, and preserved the binary's `Info` y=`117` versus five-row hit-test anomaly rather than normalizing it.
