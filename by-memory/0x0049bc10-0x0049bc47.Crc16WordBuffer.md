*** UID:00032L | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00032J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00032J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
unsigned short Crc16WordBuffer(const unsigned short* words, unsigned int count)
{
    unsigned short crc = 0;
    for (unsigned int i = 0; i < count; ++i) {
        crc = Crc16WordStep(words[i], crc);
    }
    return crc;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Externally linked, dead-retained zero-seeded word-buffer helper over private writable `s_crc16WordTable`; exact 55-byte range, unsigned count, signature/return, `0x1021` word-extension recurrence, inlined-machine/source-step-call disposition, and exhaustive static-image no-route proof are resolved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049bc10-0x0049bc47 Crc16 Word Buffer

## Status

- Address range: `0x0049bc10-0x0049bc47`
- Entity kind: raw callable CRC-16 polynomial-`0x1021` word-extension helper body
- Disposition: reconstructable utility code
- Inferred owner: [UID:00032J][Crc16](by-file/Crc16.md) `Crc16`
- Rebuild handling: `source-authored`
- IDA function status: not promoted as an IDA function object.

## Behavior

This helper computes a 16-bit CRC-family state over a buffer of 16-bit words. It initializes the accumulator to zero, treats the count at `[ebp+0Ch]` as an unsigned loop bound, reads words from the pointer at `[ebp+8]`, and applies the same high-byte table update used by the single-word helper at `0x0049bbf0-0x0049bc0d`. Because every input contributes all 16 bits after the table lookup, this is a project word-symbol extension of the MSB-first polynomial-`0x1021` recurrence, not proof of an exact standard byte CRC-16/CCITT or XMODEM result.

The loop exits immediately when `count == 0`; otherwise it iterates while `edx < count` using an unsigned `jb` branch.

## Draft C++ Notes

The accepted draft keeps `count` unsigned because the binary loop uses `cmp edx, esi` followed by `jb`. `const unsigned short* words` is the least-assumptive exact pointer type: the body reads at scale two and never writes, while no text semantics support replacing it with `wchar_t*`. The function returns `unsigned short`; the binary zero-extends the final `CX` into EAX.

The machine body contains no call instruction and physically inlines the recurrence. The highest-probability human source nevertheless calls the immediately preceding externally linked `Crc16WordStep`: the paired word and byte step/buffer layout, identical recurrence, optimized instruction scheduling, and survival of standalone step bodies support ordinary optimizer inlining. A macro or duplicated expression is machine-equivalent but less explanatory. The accepted formal block therefore preserves source-level utility layering without claiming a literal binary call.

Historical/superseded source assumption: the previous block marked this function `static`, used generic `values`/`checksum` names, and depended on a `static` step over `extern const k*` tables. Complete liveness and writable-section evidence now favor an externally linked dead-retained function over private writable `s_*` tables. External source linkage is not PE export or proven runtime use.

## IDA Evidence

- At the 2026-07-12 evidence collection, MCP database `bf5519ae` reported status `ok`, imagebase `0x400000`, and `0x0049bc10` outside the modeled function table. This is time-scoped research evidence, not a claim of later session availability.
- `xrefs_to`, code/data/immediate searches, absolute VA search, full-image RVA/raw-offset searches, and decoded direct `E8`/`E9` destinations found no entry route to `0x0049bc10`.
- The PE export directory is empty. No vtable, callback registration, feature bridge, or additional table consumer reaches the helper family.
- Disassembly:

```asm
0x0049bc10  push    ebp
0x0049bc11  mov     ebp, esp
0x0049bc13  push    esi
0x0049bc14  mov     esi, [ebp+0Ch]
0x0049bc17  xor     eax, eax
0x0049bc19  xor     edx, edx
0x0049bc1b  test    esi, esi
0x0049bc1d  jz      short 0x0049bc44
0x0049bc1f  push    edi
0x0049bc20  mov     edi, [ebp+8]
0x0049bc23  movzx   ecx, ax
0x0049bc26  shr     ecx, 8
0x0049bc29  shl     eax, 8
0x0049bc2c  mov     cx, word_66D480[ecx*2]
0x0049bc34  xor     cx, [edi+edx*2]
0x0049bc38  inc     edx
0x0049bc39  xor     cx, ax
0x0049bc3c  movzx   eax, cx
0x0049bc3f  cmp     edx, esi
0x0049bc41  jb      short 0x0049bc23
0x0049bc43  pop     edi
0x0049bc44  pop     esi
0x0049bc45  pop     ebp
0x0049bc46  retn
```

## Boundaries

- Previous internal padding `0x0049bc0d-0x0049bc10` is three `0xcc` bytes.
- This body contains no `0xcc` bytes and ends at the `retn` byte at `0x0049bc46`.
- The next bytes `0x0049bc47-0x0049bc50` are nine `0xcc` alignment bytes before the byte-step helper.

## Ownership Notes

The direct table dependency is [UID:00027A][0x0066d47c-0x0066d880.ChecksumLookupTablesData](by-memory/0x0066d47c-0x0066d880.ChecksumLookupTablesData.md) table `word_66D480` at `0x0066d480`; its xref from this body is exactly `0x0049bc2c`. This helper is paired with the single-word step helper in the same narrow [UID:00032J][Crc16](by-file/Crc16.md) source owner.

## 2026-07-12 B002 C01-C20 Callback

- C01 range: exact bytes are `55 8b ec 56 8b 75 0c 33 c0 33 d2 85 f6 74 25 57 8b 7d 08 0f b7 c8 c1 e9 08 c1 e0 08 66 8b 0c 4d 80 d4 66 00 66 33 0c 57 42 66 33 c8 0f b7 c1 3b d6 72 e0 5f 5e 5d c3`; body length is 55 bytes and `retn` is at `0x0049bc46`.
- C02-C03 signature/control: default 32-bit `__cdecl`-compatible stack shape, `const unsigned short* words`, `unsigned int count`, and `unsigned short` result. `count == 0` returns zero without dereferencing; any positive count reads exactly `count` words subject to ordinary valid-buffer preconditions.
- C04-C06 recurrence/source: each iteration computes `s_crc16WordTable[crc >> 8] ^ (crc << 8) ^ words[i]`. The machine recurrence is inline; the formal source calls UID00032K and models compiler inlining. The full 16-bit input makes this a word extension, not a standard byte-only variant.
- C07-C10 liveness/linkage: all direct, indirect-static, immediate, pointer, RVA, raw-offset, and direct-branch routes were negative for all four family starts. The only family RVA-pattern hit was a rejected relative-call displacement for UID00032K. No exports exist. UID00032L is reconstructed as externally linked but dead-retained in this image; file-static/public-export/runtime-live claims are rejected.
- C11-C15 table facts: `0x0066d480` is read only at UID00032K `0x0049bbff` and this target `0x0049bc2c`. Both 512-byte tables are identical with SHA256 `ea5f177f22d32b7e80c132b498ee4b92882484f891f43f1897075f28bb3da4d9`; all 256 entries match MSB-first polynomial `0x1021`. They occupy writable `.data`, so prior `extern const` wording is superseded by private mutable definitions.
- C16-C17 names/order: `Crc16WordBuffer`, `words`, `count`, `crc`, and `s_crc16WordTable` are descriptive inferred names. Table position is `10`; UID00032K-L-M-N positions are `20`, `30`, `40`, `50`.
- C18-C20 ownership/container: owner/emitter [UID:00032J][Crc16](by-file/Crc16.md), reconstructability, exact split, and `Nested:0` remain. UID000225 remains non-emitting. No new child, IDA mutation, manual coverage/tracker edit, or third-party import applies.
- Rejected alternatives preserved: MapPane owns a separate table and consumers; zlib/LodePNG use different checksum families; neighboring Crasher and critical-section code is padding-separated and shares no state or route.

## Score Rationale

Completion is `92` because exact boundaries/bytes, signature/convention/return, zero/unsigned-loop behavior, recurrence, complete table identity and mutability, source-call/inlining, exhaustive liveness, false-positive disposition, dead-retained linkage, owner/source placement, names/types, source order, formal C++, and full family support are resolved. Confidence is `93` because behavior and relationships are directly proven, but original symbols/linkage/header declarations and historical runtime use are unavailable and prevent `95+`.

## Cross-References

- [UID:00032J][Crc16](by-file/Crc16.md) `Crc16`
- [UID:000225][0x0049bbf0-0x0049bca9.ChecksumTableHelpers](by-memory/0x0049bbf0-0x0049bca9.ChecksumTableHelpers.md) aggregate source-family index.
- [UID:00027A][0x0066d47c-0x0066d880.ChecksumLookupTablesData](by-memory/0x0066d47c-0x0066d880.ChecksumLookupTablesData.md) backing CRC lookup tables.
