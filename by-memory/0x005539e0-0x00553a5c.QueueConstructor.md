*** UID:0001FM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
Queue::Queue(int slotSize, int slotCount)
    : LObject()
{
    m_slotSize = slotSize;
    m_slotCount = slotCount;
    m_buffer = GetMemoryMan()->ZeroAllocateBufferMemory(slotSize * slotCount);
    m_readIndex = 0;
    m_writeIndex = 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Queue constructor attached to `Queue.cpp`; current B014 MCP session `eb7ce28b` confirms exact modeled range, `LObject` base construction, Queue vtable install as compiler output, `GetMemoryMan()->ZeroAllocateBufferMemory(slotSize * slotCount)`, corrected `+0x04/+0x08/+0x0c/+0x10/+0x14` layout, vtable/RTTI support, no direct caller/xref/VA-RVA route, no recovered Queue UDT/source method symbols, and first-draft formal `Queue::Queue(int slotSize, int slotCount)` C++ now emits on this child page. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005539e0-0x00553a5c Queue Constructor

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: constructor
- Owner class: [UID:0000BF][Queue](by-class/Queue.md)
- Likely source module: [UID:0000MW][Queue](by-file/Queue.md)
- Parent attachment: [UID:0000BF][Queue](by-class/Queue.md), emitter position `10`. The child remains `88/90`, the current direct class parent is `91/92`, and the current source-file parent [UID:0000MW][Queue](by-file/Queue.md) is `91/91`, preserving the ownership and emitter route through the class to `Queue.cpp`.
- Rebuild handling: source-authored utility/container constructor with compiler-emitted prologue/EH scaffolding.
- Confidence: strong for modeled range, constructor role, field layout, allocation size, vtable install, direct class ownership, and `Queue.cpp` routing; still capped below final-audit level because direct construction-site proof, exact original `Queue.h` spelling, and recovered Queue UDT/source method symbols remain absent.

## Behavior

`Queue::Queue` constructs the `LObject` base, installs the `Queue` vtable, stores the fixed slot byte size at `+0x04`, stores the slot count/wrap limit at `+0x08`, allocates `slotSize * slotCount` bytes through the client memory manager, stores the backing buffer at `+0x0c`, and clears both read/write indices at `+0x10` and `+0x14`.

The formal source body is now emitted as `Queue::Queue(int slotSize, int slotCount) : LObject()`. The vtable store is compiler output from the constructor and is intentionally not hand-authored in C++. The allocation is source-shaped as `GetMemoryMan()->ZeroAllocateBufferMemory(slotSize * slotCount)`, matching MCP `sub_516030` plus `sub_5160D0`.

The current generated field vocabulary for accepted write/read/empty children uses `m_slotSize`, `m_slotCount`, `m_buffer`, `m_readIndex`, and `m_writeIndex`. Older generated or historical text that reverses the first two field names is stale: IDA arithmetic shows `+0x04` is the byte stride and `+0x08` is the wrap limit.

## Range And Boundaries

| Range | Classification | Evidence |
| --- | --- | --- |
| `0x005539d9-0x005539e0` | preceding alignment | [UID:0000VN][-ignored](by-memory/-ignored.md) records `0xcc` padding before the Queue cluster. |
| `0x005539e0-0x00553a5c` | `Queue` constructor | B014 MCP session `eb7ce28b` `lookup_funcs` reports the modeled function at `0x005539e0`, size `0x7c` / 124 bytes (Verified with int_convert.py). |
| `0x00553a5c-0x00553a60` | following alignment | Four-byte gap before [UID:0001FO][0x00553a60-0x00553ab9.QueueDestructor](by-memory/0x00553a60-0x00553ab9.QueueDestructor.md). |

## Constructor Layout Effects

| Site / field | Meaning |
| --- | --- |
| `this+0x00` | `Queue` vtable, shared with the destructor and scalar deleting destructor vtable restores; source C++ relies on the compiler's constructor vtable store. |
| `this+0x04` | `m_slotSize`, the slot byte size. Raw write/read helpers multiply this value by the read/write index and use it as the copy byte count. |
| `this+0x08` | `m_slotCount`, the slot count / wrap limit. Raw write/read helpers compare incremented indices against this value. |
| `this+0x0c` | `m_buffer`, backing buffer pointer allocated as `slotSize * slotCount`. |
| `this+0x10` | `m_readIndex`, initialized to zero. |
| `this+0x14` | `m_writeIndex`, initialized to zero. |

## Evidence Consolidated From Queue Cluster

- IDA MCP `lookup_funcs` reports `0x005539e0` as size `0x7c`, giving end-exclusive range `0x00553a5c`.
- IDA MCP `decompile 0x005539e0` shows `this[1] = a2`, `this[2] = a3`, allocation of `a3 * a2`, and zeroing of indices at `this[4]` and `this[5]`.
- [UID:0001FN][0x005539e0-0x00553c07.Queue](by-memory/0x005539e0-0x00553c07.Queue.md) records the 2026-05-26 recheck: modeled Queue starts at `0x005539e0`, `0x00553a60`, and `0x00553b70`, with no direct callers for the Queue starts in current IDA data.
- [UID:0000BF][Queue](by-class/Queue.md) records the corrected layout: `+0x04` is the copy stride/slot size and `+0x08` is the wrap limit/slot count. Reversed active field names were a historical generated state superseded by dated command `000000023161`. Dated command `000000023178` preserved the corrected layout and is now superseded history; current command `000000023186`, timestamp `2026-08-13T15:06:39-04:00`, preserves generated CPP/H with `m_slotSize` at `+0x04` and `m_slotCount` at `+0x08`, at `Queue.cpp` SHA256 `E50D0A5229A4D2DBD936B945CC01A47C54BCAE68DA0A72665476EA0A36B2A318` and `Queue.h` SHA256 `BE25A3595C58B070A9F93397009063F75D0DD5860FECF8C6D2C3C3AC382211A6`. Replacing only current generated metadata reproduces the superseded `000000023178` hashes, so payload behavior did not change.
- [UID:0001FS][0x00553b70-0x00553c07.QueueScalarDeletingDestructor](by-memory/0x00553b70-0x00553c07.QueueScalarDeletingDestructor.md) records `xrefs_to 0x00622d24` as vtable writes at constructor `0x00553a15`, ordinary destructor `0x00553a88`, and scalar deleting destructor `0x00553b9f`.
- The raw write/read helpers [UID:0001FP][0x00553ac0-0x00553b05.QueueWriteSlotRaw](by-memory/0x00553ac0-0x00553b05.QueueWriteSlotRaw.md) and [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md) independently confirm the `+0x04` stride and `+0x08` wrap-limit interpretation.
- Historical generated `class_Queue.cpp` output emitted this constructor while omitting adjacent Queue helpers. Dated command `000000023161` first superseded that state; dated command `000000023178` also preserved all five methods and is now superseded history. Current command `000000023186` preserves all five source-authored Queue methods with the corrected field layout at the current hashes recorded above; only generated metadata differs from the reproduced `000000023178` payload. Generated output remains corroborating emission evidence rather than binary authority.
- 2026-06-16 A002 review of the pre-existing collision edit replaces the stale timeout caveat: `lookup_funcs` reports `sub_5539E0` size `0x7c`; decompilation stores `a2` to `this[1]`, `a3` to `this[2]`, allocates `a3 * a2` through `sub_5160D0`, and clears `this[4]`/`this[5]`; `callees` returns `sub_4F4A80`, `sub_516030`, and `sub_5160D0`; `analyze_function` reports no callers; `xrefs_to 0x005539e0` and absolute/RVA pointer-byte searches find no construction-site references.
- A002 checked current IDA function comments while reviewing the collision edit and found only generic size/stack-cookie annotations, not a source-quality constructor comment. No IDA rename, prototype, type declaration, function-boundary edit, or verified source-quality comment is used as evidence for this page.
- 2026-07-04 B014 implementation callback incorporates accepted report `0001FM-QueueConstructor-source-quality.md`. Current MCP session `eb7ce28b` reported `server_health` status `ok` for IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, input `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, with auto-analysis and Hex-Rays ready. `lookup_funcs` reports constructor `sub_5539E0` at `0x005539e0`, size `0x7c` / 124 bytes (Verified with int_convert.py); endpoints `0x005539d9` and `0x00553a5c` are not functions. `decompile 0x005539e0` calls `sub_4F4A80`, writes `Queue::vftable` at `0x00553a15`, calls `sub_516030`, stores `a2` to `this[1]`, stores `a3` to `this[2]`, calls `sub_5160D0(a3 * a2)`, stores the result to `this[3]`, and clears `this[4]`/`this[5]`.
- The same B014 pass confirms the route and type caveats: `xrefs_to 0x005539e0` reports zero direct xrefs, `analyze_function` reports no callers and only generic `Size` / `StackCookie` comments, and `find_bytes` finds zero constructor VA/RVA pointer patterns. Positive controls still prove the Queue class route: `xrefs_to 0x00622d24` returns Queue vtable writes at constructor `0x00553a15`, ordinary destructor `0x00553a88`, and scalar deleting destructor `0x00553b9f`; scalar deleting destructor VA bytes match only at the vtable slot `0x00622d24`; Queue vtable pointer bytes match at `0x00553a17`, `0x00553a8a`, and `0x00553ba1`.
- B014 byte evidence records seven `0xcc` bytes before `0x005539e0`, the constructor store/multiply/call sequence `89 56 04`, `0f af d1`, `89 4e 08`, call to `sub_5160D0`, `89 46 0c`, `+0x10/+0x14` zeroing, `c2 08 00`, and four `0xcc` bytes after `0x00553a5c`. `entity_query` for `Queue|WriteSlot|ReadSlot|IsEmpty|Enqueue|Dequeue` returns only Queue RTTI/vtable names; `search_structs Queue` returns none; and `type_query *Queue*` returns no local Queue UDT/type record. `Queue::Queue(int slotSize, int slotCount)` and member spellings are therefore source-quality inference from behavior and current Queue docs/generated children, not recovered original symbols.

## Evidence

- Main dependencies: [UID:000195][0x004f4a80-0x004f4c0d.LObjectRuntimeShell](by-memory/0x004f4a80-0x004f4c0d.LObjectRuntimeShell.md) base construction at `0x004f4a80`, [UID:0001BC][0x00516030-0x00516036.GetMemoryMan](by-memory/0x00516030-0x00516036.GetMemoryMan.md), and [UID:0001BE][0x005160d0-0x00516162.ZeroAllocateBufferMemory](by-memory/0x005160d0-0x00516162.ZeroAllocateBufferMemory.md).
- The source-level constructor signature preserves argument order as `Queue::Queue(int slotSize, int slotCount)`. Exact original header spelling remains unresolved, but the current code-entry rules and B014 evidence make this a confidence cap rather than a no-code proof.
- Rejected alternatives: MemoryMan and LObject are dependencies rather than owners; no-owner/non-emitting is rejected because the Queue class/file route is proven; aggregate/class/file pages should not emit this exact child body; raw/decompiler names such as `sub_5539E0`, `a2`, `a3`, or `this[1]` are not source-quality names; and plain `AllocateBufferMemory` is rejected because the constructor calls the zero-allocation helper.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `88` | The page now records the exact modeled range, direct class owner and emitter route, constructor effects, corrected source-facing field layout, zero-allocation formula, live decompile/disassembly/callee evidence, vtable-store support, raw-helper corroboration, generated-output caveat, IDA-metadata review, rejected alternatives, and formal first-draft constructor C++. Remaining work is exact original `Queue.h` spelling and active construction-site proof. |
| Confidence `90` | Confidence is strong because current MCP session `eb7ce28b` directly confirms the constructor body, class/file/vtable/raw-helper evidence agrees, and the inferred source names match accepted Queue child output. It remains below final-audit confidence because `xrefs_to`, `analyze_function`, and VA/RVA pointer-byte searches still find no direct construction-site reference, and no local Queue UDT/source method symbols or original header parameter names are recovered. |

## Cross-References

- [UID:0001FN][0x005539e0-0x00553c07.Queue](by-memory/0x005539e0-0x00553c07.Queue.md)
- [UID:0000BF][Queue](by-class/Queue.md)
- [UID:0000MW][Queue](by-file/Queue.md)
- [UID:0000L7][MemoryMan](by-file/MemoryMan.md)
- [UID:000195][0x004f4a80-0x004f4c0d.LObjectRuntimeShell](by-memory/0x004f4a80-0x004f4c0d.LObjectRuntimeShell.md)
- [UID:0001BD][0x00516050-0x005160ce.AllocateBufferMemory](by-memory/0x00516050-0x005160ce.AllocateBufferMemory.md)
- [UID:0001BE][0x005160d0-0x00516162.ZeroAllocateBufferMemory](by-memory/0x005160d0-0x00516162.ZeroAllocateBufferMemory.md)
- [UID:0001FP][0x00553ac0-0x00553b05.QueueWriteSlotRaw](by-memory/0x00553ac0-0x00553b05.QueueWriteSlotRaw.md)
- [UID:0001FQ][0x00553b10-0x00553b55.QueueReadSlotRaw](by-memory/0x00553b10-0x00553b55.QueueReadSlotRaw.md)

## 2026-08-11 Whole-File Reconciliation

- Exact placement: this modeled `0x7c` body is the first source-authored Queue method, `0x005539e0-0x00553a5c`, followed by four `0xcc` bytes through `0x00553a60`. Seven preceding `0xcc` bytes at `0x005539d9-0x005539e0` are external entry alignment. The next Queue body is the ordinary destructor at `0x00553a60`.
- Exact source route: UID0001FM retains `CANONICAL_OWNER:0000BF`, `EMITTER_UIDS:0000BF`, and position `10`. Its existing formal `Queue::Queue(int slotSize, int slotCount)` body remains unchanged. UID0000BF now owns the CPP include/child wrapper and the only Queue H declaration; this child H remains blank to prevent duplicate declarations.
- Layout contract: compiler vtable/base state occupies `+0x00`; source fields are `m_slotSize` `+0x04`, `m_slotCount` `+0x08`, `m_buffer` `+0x0c`, `m_readIndex` `+0x10`, and `m_writeIndex` `+0x14`; total object size is `0x18`. The constructor stores the two arguments, zero-allocates `slotSize * slotCount`, and clears both indices. Historical generated names that reversed the first two fields are rejected.
- Whole-file order and ownership: constructor is followed by ordinary destructor, `WriteSlot`, `ReadSlot`, and `IsEmpty`; the scalar deleting destructor at `0x00553b70` is compiler-generated and non-emitting. Aggregate UID0001FN stays blank/no-code, and the source root remains UID0000MW `NexusTK/util/Queue.cpp`.
- Reachability: fresh whole-file evidence found zero direct xrefs/callers, no stored VA/RVA pointer, and no direct transfer route to this start. The Queue vtable store at `0x00553a15` and the matching destructor/scalar stores are positive class-identity controls, not active callers. The accepted file-level classification is 90 percent dormant/10 percent unresolved indirect or external route.
- Negative and historical disposition: no additional constructor fragment, global, static, constant, resource, or import belongs here; `g_pApplicationCleanupQueue` is unrelated `BlackHole *` state. Raw/decompiler naming, MemoryMan ownership, LObject ownership, direct file ownership, aggregate-owned C++, and stale Wave2/Wave3 material remain rejected. Older dated investigations below remain historical evidence.

## Changes

- 2026-08-11 B010 accepted UID0000MW whole-file callback:
  - Preserved the formal constructor CPP byte-for-byte and kept the child H blank.
  - Added the exact whole-file position/padding, `0x18` layout, class/file ownership, source order, dormant reachability, negative inventory, and historical rejected-alternative reconciliation required by CQM-026.
- 2026-07-04 B014 UID0001FM implementation callback:
  - What existed before: score `85/89`, valid direct owner/emitter [UID:0000BF][Queue](by-class/Queue.md), position `10`, blank formal C++, and stale no-code wording treating unresolved exact header spelling and active construction route as final blockers.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`, owner/emitter/reconstructable/position unchanged, and formal first-draft `Queue::Queue(int slotSize, int slotCount)` C++ emitted on this exact child page.
  - Summary/evidence: accepted B014 report and MCP session `eb7ce28b` confirmed exact `0x005539e0-0x00553a5c` range, size `0x7c` / 124 bytes, constructor decompile, `LObject` base construction, Queue vtable store as compiler output, `GetMemoryMan()->ZeroAllocateBufferMemory(slotSize * slotCount)`, field layout `m_slotSize`/`m_slotCount`/`m_buffer`/`m_readIndex`/`m_writeIndex`, byte/padding evidence, vtable/RTTI controls, zero direct caller/xref/VA-RVA route, no recovered Queue UDT/type/source method names, and rejected MemoryMan/LObject/no-owner/aggregate/class/file-emission/raw-name/plain-allocation alternatives. Missing direct construction-site proof and exact original header spelling are retained as confidence caps, not no-code blockers.
- 2026-06-16 A002 review of pre-existing C001 collision edit:
  - What existed before: score `82/86`, source-file owner/emitter route, stale 80/80 parent text, and a timeout caveat for the most recent direct IDA pass.
  - Changed to: `COMPLETION:85`, `CONFIDENCE:89`, direct owner/emitter [UID:0000BF][Queue](by-class/Queue.md), position `10`; final C++ remains blank.
  - Summary/evidence: live IDA MCP session `a002_goal65_20260616` confirmed the modeled `0x7c` constructor boundary, `LObject` base call, Queue vtable store, `+0x04` slot-size and `+0x08` slot-count stores, `slotSize * slotCount` zero-allocation, index zeroing, no direct xrefs/callers, no absolute/RVA pointer-byte hits for the constructor start, and current class/file parents above the active `85/85` gate. A002 reviewed the pre-existing C001 collision text, retained the valid evidence, and removed reliance on unverified IDA-side source-quality comments; no rename, prototype, type, function-boundary, or comment mutation is required before final source parameter names and reachability are proven.
- 2026-06-06 A007 Queue constructor consolidation:
  - What existed before: score `70/85`, reconstructable but unassigned, with a short constructor summary and basic IDA lookup/decompile bullets.
  - Changed to: `COMPLETION:82`, `CONFIDENCE:86`, parent [UID:0000MW][Queue](by-file/Queue.md) at position `10`, expanded field-layout/ownership/evidence notes, and explicit final-C++ gating.
  - Summary/evidence: consolidated existing IDA-backed Queue cluster evidence for the modeled constructor range, `Queue` vtable install, `slotSize * slotCount` allocation, corrected `+0x04/+0x08` layout, raw helper corroboration, and generated-output omissions. A007 live MCP retries timed out, so this remains a conservative documentation improvement rather than a final audit.
