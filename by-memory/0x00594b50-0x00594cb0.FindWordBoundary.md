*** UID:0004U9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ON | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5851984 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@00594B50 | UID0000OJ accepted formal channel
static void __stdcall FindWordBoundary(const wchar_t *text,
                                       short textLength,
                                       short caretIndex,
                                       bool forward,
                                       TextEditRange *range)
{
    if (!forward)
    {
        if (caretIndex <= 0)
        {
            range->start = 0;
            range->end = 0;
            return;
        }

        short index = caretIndex - 1;
        bool separators = text[index] == L' ' || text[index] == L'\t'
            || text[index] == L'\r' || text[index] == L'\n';
        while (index > 0)
        {
            wchar_t previous = text[index - 1];
            bool previousIsSeparator = previous == L' '
                || previous == L'\t' || previous == L'\r'
                || previous == L'\n';
            if (previousIsSeparator != separators)
                break;
            --index;
        }
        range->start = index;
        range->end = caretIndex;
        return;
    }

    if (caretIndex >= textLength)
    {
        range->start = textLength;
        range->end = textLength;
        return;
    }

    short index = caretIndex;
    bool separators = text[index] == L' ' || text[index] == L'\t'
        || text[index] == L'\r' || text[index] == L'\n';
    while (index < textLength)
    {
        wchar_t current = text[index];
        bool currentIsSeparator = current == L' '
            || current == L'\t' || current == L'\r'
            || current == L'\n';
        if (currentIsSeparator != separators)
            break;
        ++index;
    }
    range->start = caretIndex;
    range->end = index;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready TextEditPane.cpp file-local __stdcall FindWordBoundary at exact [0x00594b50,0x00594cb0): scans the contiguous separator or nonseparator run around a signed-short caret using only space, tab, CR, and LF, writes exact TextEditRange endpoints, and preserves five-argument callee cleanup. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00594b50-0x00594cb0 FindWordBoundary

## Status And Source Placement

- Exact range: `[0x00594b50,0x00594cb0)`, 352 bytes.
- Ordinary file-local helper owned/emitted by [UID:0000ON][TextEditPane](by-file/TextEditPane.md); it is not a TextEditPane member.
- Source child of [UID:0001JS][0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers](by-memory/0x00594040-0x00594e60.TextEditPaneInputNavigationWrapAndTextRunHelpers.md).

## Exact Binary And ABI Evidence

- Bounded decoding found 141 instructions, 43 basic blocks, cyclomatic complexity 26, five callsites, and three currently modeled callers. Exact SHA256 is `337982D4F5F98732BC44109BA8B2C26F8B9DC41D368D08DE1C0B397F36422978`.
- Callee cleanup and call pushes establish five source arguments: UTF-16 pointer, signed-short length, signed-short caret, bool direction, and `TextEditRange *` output.
- The function has no `this` read. File-local static `__stdcall` is the compile-legal human source route that matches the retained ABI without raw wrappers.
- Output writes are signed-short `start` and `end` fields in the two-word TextEditRange structure.

## Exact Boundary Behavior

- Separator classification contains exactly four UTF-16 code units: space, tab, carriage return, and line feed.
- Backward movement at/below zero writes `[0,0]`; otherwise it starts at `caret-1` and walks left while separator class remains equal, then writes `[index,caret]`.
- Forward movement at/after text length writes `[length,length]`; otherwise it starts at caret and walks right while separator class remains equal, then writes `[caret,index]`.
- Both separator and nonseparator runs are traversed. This is a class-transition boundary finder, not a whitespace skipper.

## Caller And Type Relationships

- `TextEditPane::GetNavigationCaretIndex` uses the returned start for word-left and end for word-right.
- Other callers share the same text/length/caret/range contract and establish that the helper is retained source rather than a target-only inline fragment.
- The exact `TextEditRange` type belongs to TextEditPane declaration support and must remain distinct from geometric Rect/Point records.

## Negative And Rejected Evidence

- Rejected locale/STL word-break substitution: observed classification is the exact four-code-unit set and has no locale or punctuation handling.
- Rejected member method: there is no TextEditPane receiver.
- Rejected `bool`/range return normalization: the observed ABI writes an explicit output structure and returns no source value.
- Rejected raw/decompiler helper: multiple source callers, stable ABI, application semantics, and complete body support a named file-local function.
- Rejected defensive pointer/length guards: only the exact caret boundary checks exist.

## Score Rationale And History

- `92/94` reflects exact range/hash/CFG, multiple callers, five-argument ABI, source linkage, classification set, both directional loops, output type, and complete C++.
- Confidence remains below 95 only for stripped file-local spelling; `FindWordBoundary` is the strongest behavior-backed name.
- Historical UID0001JS raw-helper debt is superseded for this exact function only.

## Rebuild Policy

- Emit the managed file-local helper through UID0000ON and expose only the declaration needed by TextEditPane.cpp source order.
- Do not emit raw offsets, decompiler labels, locale abstractions, imported tokenizer behavior, compiler padding, or new validation paths.

## UID0000OJ Whole-File Incorporation - 2026-08-16

The accepted source/global definition remains verbatim in this exact owner page and is assembled once through the consolidated UID0000EO translation-unit channel.

| Claim ID | Target UID | Claim | Confidence | Evidence | Destination | Action | Gate-1 state |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C0000OJ-173 | 0004U9 | Preserve generated modeled definition `[0x594b50,0x594cb0)` FindWordBoundary and its exact owner/order. | original-proof | find a word boundary; profile in/out 3/1; start-xrefs 5; first 0x58f9ce/code. | UID0004U9 formal CPP/H and UID0000ON unit order | already-present | proposed |
