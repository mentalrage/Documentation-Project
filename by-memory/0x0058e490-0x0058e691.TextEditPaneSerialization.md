*** UID:0001JN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5825680 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@0058E490 | UID0000OJ accepted formal channel
unsigned char *TextEditPane::EncodeState(unsigned short *outSize) const
{
    MemoryMan *memoryMan = GetMemoryMan();
    Encoder encoder;
    unsigned char stateBuffer[32768];

    short textSize = static_cast<short>(
        static_cast<unsigned short>(m_textStorage->GetCount()));
    short lineTableSize = static_cast<short>(
        static_cast<unsigned short>(m_lineTable->GetCount()) *
        static_cast<unsigned short>(sizeof(TextEditLineRecord)));
    short formatTableSize = static_cast<short>(
        static_cast<unsigned short>(m_formatTable->GetCount()) *
        static_cast<unsigned short>(sizeof(TextEditFormatRecord)));
    short formatRunSize = static_cast<short>(
        static_cast<unsigned short>(m_formatRuns->GetCount()) *
        static_cast<unsigned short>(sizeof(TextEditFormatRun)));

    encoder.Initialize(stateBuffer, 32766);
    encoder.WriteBytes(m_textStorage->GetElementAt(0), textSize);
    encoder.WriteByte(0);
    encoder.WriteInt(0x6b6f6e67);

    encoder.WriteShort(lineTableSize);
    encoder.WriteBytes(m_lineTable->GetElementAt(0), lineTableSize);
    encoder.WriteShort(formatTableSize);
    encoder.WriteBytes(m_formatTable->GetElementAt(0), formatTableSize);
    encoder.WriteShort(formatRunSize);
    encoder.WriteBytes(m_formatRuns->GetElementAt(0), formatRunSize);

    unsigned int encodedSize;
    encoder.Finalize(&encodedSize);

    unsigned char *result = static_cast<unsigned char *>(
        GetMemoryMan()->AllocateBufferMemory(encodedSize));
    memoryMan->MemmoveWrapper(result, stateBuffer, encodedSize);
    *outSize = static_cast<unsigned short>(encodedSize);
    return result;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready class-owned TextEditPane::EncodeState with exact 513-byte body/hash, retained raw TextEditControlPane forwarder, 32768-byte Encoder staging, big-endian text/zero/kong/16-16-4 table stream, low-word wrap and signed-short widening, full-width allocation/copy, 16-bit public size truncation, caller-owned result, paired-decoder corroboration, and compiler-only EH/cookie lowering excluded. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0058e490-0x0058e691 TextEditPane Serialization

## UID0001JN Accepted Source Reconstruction - 2026-07-19

- Exact identity: source-authored `unsigned char *TextEditPane::EncodeState(unsigned short *outSize) const`, one implicit `TextEditPane` receiver in `ECX`, one explicit stack pointer, returned byte buffer in `EAX`, and `retn 4`. IDA's apparent two-argument fastcall includes the unused register argument and is not the source API.
- Exact range is `[0x0058e490,0x0058e691)`, 513 bytes. The matching local PE maps `.text` RVA `0x1000`/raw `0x400`, so target VA `0x0058e490` maps to raw offset `0x0018d890`; extracted target bytes hash to SHA256 `A3BE2FC710F53E49D15C7531676649CD5C712CA7BD6F1019BE88DEA98964810`.
- Complete target analysis yields 146 instructions and five blocks including out-of-line cleanup/handler support. The source body uses an automatic `Encoder`; `__alloca_probe`, security-cookie setup/checks, SEH state, cleanup funclet `0x00609706`, handler `0x00609711`, and `__CxxFrameHandler3` are compiler lowering and are not handwritten source.
- The sole inbound target xref is the raw tail jump at `0x00498c4a`. Retained [UID:0004K6][0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization](by-memory/0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization.md) loads `m_textEditPane` from wrapper offset `+0x10c` and forwards to this member. The wrapper entry has zero inbound xrefs/pointers and is not an IDA function; that liveness negative does not transfer lower-body ownership.
- Exact TextEditPane fields are `List *m_textStorage` at complete-object `+0x134`, `List *m_lineTable` at `+0x138`, `List *m_formatTable` at `+0x13c`, and `List *m_formatRuns` at `+0x140`. Constructor/destructor/copy/decoder/helper evidence fixes element sizes and types at 2-byte `wchar_t`, 16-byte `TextEditLineRecord`, 16-byte `TextEditFormatRecord`, and 4-byte `TextEditFormatRun`.
- Current `TextEditFormatRecord` remains exactly 16 bytes with signed `short referenceCount`; private [UID:0004T0][0x005912b0-0x005912fe.TextEditPaneReleaseFormatObjects](by-memory/0x005912b0-0x005912fe.TextEditPaneReleaseFormatObjects.md) uses that signed field. This encoder copies complete records without interpreting or changing the count, so it preserves the accepted B003 declaration/helper union.
- All four List counts are read through their low 16 bits. The 16/16/4 table byte-length multiplications wrap at 16 bits, each stored `short` is later sign-extended before `WriteBytes`, and a wrapped negative therefore reaches the unsigned byte-count API as a large value. The formal casts preserve this exact machine behavior.
- The automatic 20-byte `Encoder` initializes over a 32766-byte writable window inside a 32768-byte stack array. The first payload comes from `m_textStorage->GetElementAt(0)` and uses the low-word text count directly as bytes; the binary intentionally does not multiply by `sizeof(wchar_t)`.
- Stream order is exact: raw text bytes, one zero delimiter, integer `0x6b6f6e67`, then three big-endian short byte lengths followed by raw line-table, format-table, and format-run payloads. Constructor-default Encoder byte order emits marker bytes `6b 6f 6e 67` (`kong`).
- `Encoder::Finalize(unsigned int *)` appends its terminator, writes a full 32-bit encoded count, and returns `bool`; this target intentionally ignores the boolean. The full dword count controls allocation and copy, and only the final caller-visible `*outSize` store truncates to 16 bits.
- Call order preserves two `GetMemoryMan()` evaluations. The first result is saved before Encoder construction and later receives `MemmoveWrapper`; the second receives `AllocateBufferMemory`. The returned raw MemoryMan block transfers to the caller for eventual release, and the target retains no pointer.
- Exact negative behavior is retained: no null, empty-list, output-pointer, raw-element, capacity, overflow, Finalize-result, allocation-result, or copy-result guard; no clamp or recovery; and no hardening to `size_t`, checked arithmetic, or text-count multiplication. `AllocateBufferMemory` retains its own pointer-throw behavior.
- Raw paired decoder `[0x0058e6a0,0x0058e880)` independently validates delimiter scanning, big-endian `kong`, three 16/16/4 table byte lengths, fallback table defaults, and the text-count-as-bytes asymmetry. It is a separate out-of-range TextEditPane method and is neither absorbed here nor assigned a fabricated UID.
- Class owner/emitter is [UID:0000EO][TextEditPane](by-class/TextEditPane.md); [UID:0000ON][TextEditPane](by-file/TextEditPane.md) remains the translation-unit route. Encoder/BinaryCodec are utilities, TextEditControlPane owns only wrappers, and TextPad/TextEditScrap are payload consumers.
- Predecessor `[0x0058e482,0x0058e490)` is fourteen `0xcc` bytes, SHA256 `1E875C76536C8496BED8C8760B539327F24ACFFBCF0A4B941877B4BFB4C44356`; successor `[0x0058e691,0x0058e6a0)` is fifteen `0xcc`, SHA256 `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C`. Both remain external alignment and are not part of the target.
- Completion rises `86 -> 92` because signature, source body, table types, count semantics, stream, owner/emitter, allocation/output contract, range, padding, and compiler/source boundary are closed. Confidence rises `90 -> 94`; exact behavior is original-proof/very strong, while original local spellings, public/private visibility, and live ordinary reachability remain confidence caps rather than code blockers.
- Historical `EncodeTextEditState` free-helper placement, file-level direct owner/emitter, `m_styleRuns +0x13c`, `m_formatRecords +0x140`, 16-bit Finalize output, blank formal C++, and unresolved-name/no-code conclusions are superseded. They remain below only as dated evidence history.

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: class-owned TextEditPane state-encoding method
- Likely source module: [UID:0000ON][TextEditPane](by-file/TextEditPane.md)
- Wrapper forwarder: [UID:0004K6][0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization](by-memory/0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization.md), retained raw `TextEditControlPane::EncodeState` body `0x00498c40-0x00498c4f`; aggregate [UID:00011H][0x004988d0-0x00498dd0.TextEditControlPaneCore](by-memory/0x004988d0-0x00498dd0.TextEditControlPaneCore.md) is a non-emitting index.
- Helper item: [UID:0000UM][EncodeTextEditState_0058E490](by-item/EncodeTextEditState_0058E490.md)
- Confidence: very strong for exact behavior, bounds, class ownership, wrapper route, all four fields/types, stream and allocation/output contracts; high for descriptive local spellings and original visibility.

## Covered Range

| Range | Function | Notes |
| --- | --- | --- |
| `0x0058e490-0x0058e691` | `TextEditPane::EncodeState` | Serializes exact TextEditPane text/line/format/format-run state through a stack [UID:00004F][Encoder](by-class/Encoder.md), returns an exact-size MemoryMan byte buffer, and reports the low 16 bits of the full encoded size. |

## IDA MCP Evidence

Checked on 2026-05-30 and refreshed on 2026-06-06:

- IDA `ida_funcs.get_func(0x0058e490)` reports `sub_58E490` as `0x0058e490-0x0058e691` size `513`.
- IDA `xrefs_to` finds one code xref at `0x00498c4a`, the tail jump inside an unpromoted [UID:0000EM][TextEditControlPane](by-class/TextEditControlPane.md) forwarding stub. `callers(0x0058e490)` still reports no ordinary function callers.
- Bounded disassembly of `0x00498bf0-0x00498c72` shows the exact forwarder at `0x00498c40-0x00498c4f`: it loads the embedded editor pointer from wrapper offset `+0x10c`, restores `ebp`, and tail-jumps to `sub_58E490`. Exact wrapper ownership and source body now belong to UID0004K6; UID00011H owns only the enclosing address index.
- Hex-Rays shows stack `Encoder` construction, initialization against a 32766-byte temporary buffer, raw payload writes, scalar writes, finalize/count retrieval, heap allocation, copy to the heap buffer, output-size store, and stack Encoder destruction.
- The method reads exact List pointers `m_textStorage` at `+0x134`, `m_lineTable` at `+0x138`, `m_formatTable` at `+0x13c`, and `m_formatRuns` at `+0x140`; each raw payload is obtained through current `List::GetElementAt(0)`, the source-facing form of the virtual call at vtable offset `+0x10`.
- Payload sizes are derived from each table object's word at `+0x0c`: first table count as-is, second and third multiplied by `16`, and fourth multiplied by `4`.
- Encoder helper xrefs show calls from this function to `0x004a4e70`, `0x004a55c0`, `0x004a5480`, `0x004a4ec0`, `0x004a4ff0`, `0x004a4f00`, `0x004a55e0`, and `0x004a4ea0`, matching constructor/init/write/finalize/destructor roles in [UID:00004F][Encoder](by-class/Encoder.md).
- 2026-06-16 A002 live IDA MCP refresh against session `b001_maildialogs_0000ZO_20260616` reconfirmed `sub_58E490` as size `0x201` with exactly one xref at `0x00498c4a` and no modeled callers. `lookup_funcs 0x00498c40` still reports no function, and `make_signature_for_range 0x00498c40-0x00498c4f` returned unique bytes `55 8B EC 8B 89 0C 01 00 00 5D E9 41 58 0F 00`, which is the raw wrapper body loading the embedded editor from `+0x10c` and jumping to this serialization helper.
- Current compact analysis lists the same Encoder/allocation callees and decompiles the body as reading `a1[77]` through `a1[80]`, writing delimiter byte `0`, writing integer `0x6b6f6e67`, length-prefixing the three secondary payloads, finalizing to `Size`, allocating exactly `Size` bytes, copying from the stack buffer, storing `*outSize`, and destroying the stack Encoder.
- 2026-06-16 A002 item/resource refresh against active IDA MCP session `b001_selflookpane_0001H7_20260616` reconfirmed `lookup_funcs 0x0058e490` as `sub_58E490`, size `0x201`; `lookup_funcs 0x00498c40` as `Not a function`; `xrefs_to 0x0058e490` as one raw code xref at `0x00498c4a`; `xrefs_to 0x00498c40` as zero refs; and the same unique wrapper signature `55 8B EC 8B 89 0C 01 00 00 5D E9 41 58 0F 00`.
- The same `analyze_function 0x0058e490` pass reports prototype shape `void *__fastcall(_DWORD *, int, _WORD *)`, no ordinary callers, and callees limited to `Encoder_Constructor`, `Encoder_Initialize`, `Encoder_WriteBytes`, `Encoder_WriteByte`, `Encoder_WriteInt`, `Encoder_WriteShort`, `Encoder_Finalize`, `Encoder_Destructor`, shared setup/allocation/copy helpers, `__alloca_probe`, and the security-cookie check.

## Signature And Data Flow

Current decompiler shape is `void *__fastcall sub_58E490(TextEditPane *this, int unused_edx, uint16_t *outSize)`. Assembly proves source-facing `unsigned char *TextEditPane::EncodeState(unsigned short *outSize) const`: `EDX` is only the unused register slot, one explicit pointer is stack-passed, `ECX` is the receiver, and `retn 4` removes one explicit argument. The return value is an allocated buffer containing the finalized encoded stream, while `*outSize` receives the low 16 bits of its byte count.

Data flow is table/list driven:

- `m_textStorage` (`this+0x134`) supplies the first raw byte payload size from its `+0x0c` word and writes that payload before the delimiter byte. The constructor allocates this slot with the other table/list objects, the destructor releases it first, and [UID:0002O9][0x0058e380-0x0058e3d7.TextEditPaneCopyWideText](by-memory/0x0058e380-0x0058e3d7.TextEditPaneCopyWideText.md) proves this slot is the editor's text storage.
- `m_lineTable` (`this+0x138`) and `m_formatTable` (`this+0x13c`) supply exact 16-byte record counts, then each serialized block is length-prefixed through `Encoder::WriteShort`.
- `m_formatRuns` (`this+0x140`) supplies exact 4-byte record counts, also length-prefixed through `Encoder::WriteShort`.
- The three [UID:00013G][0x004a4f00-0x004a4f59.EncoderWriteShort](by-memory/0x004a4f00-0x004a4f59.EncoderWriteShort.md) call sites at `0x0058e59a`, `0x0058e5c9`, and `0x0058e5f8` serialize the `this+0x138`, `this+0x13c`, and `this+0x140` byte-length prefixes. Because the stack Encoder keeps constructor-default `m_useBigEndian = true`, those 16-bit prefixes are high-byte-first.
- The stream writes delimiter byte `0`, then calls `Encoder::WriteInt(0x6b6f6e67)`. Because the stack Encoder keeps its constructor-default big-endian flag, the stream bytes are `6b 6f 6e 67` (`"kong"`); the native little-endian branch would emit `67 6e 6f 6b`.
- The stack output buffer is `32768` bytes, but the Encoder is initialized with a `32766` byte writable window. After finalize, the function allocates exactly `Size` bytes through the shared allocator helpers and copies `Size` bytes from the stack buffer.
- B003's [UID:00013J][0x004a55c0-0x004a55de.EncoderInitialize](by-memory/0x004a55c0-0x004a55de.EncoderInitialize.md) reanalysis confirms the callsite sequence: the stack `Encoder` is constructed at `ebp-0x802c`, capacity `0x7ffe` is pushed at `0x0058e4ef`, the stack output buffer address `ebp-0x8010` is pushed at `0x0058e54a`, and `call 0x004a55c0` occurs at `0x0058e54b`. The callee consumes both arguments with `ret 8`, stores `m_outputBuffer`/`m_outputCapacity`, resets `m_writePos`, sets `m_isValid`, and preserves `m_useBigEndian`.
- B008's [UID:00013K][0x004a55e0-0x004a5621.EncoderFinalize](by-memory/0x004a55e0-0x004a5621.EncoderFinalize.md) reanalysis confirms the finalize/count path: this caller pushes the address of dword local `[ebp-0x8018]` and calls `Encoder::Finalize(unsigned int *outBytesWritten)` at `0x0058e624`; it then allocates and copies using the full dword count from `[ebp-0x8018]`, and only after the copy writes `word ptr [outSize] = word ptr [ebp-0x8018]`. This proves the Encoder method's output pointer is 32-bit even though the public TextEditPane-facing output-size storage is 16-bit.
- [UID:0000OQ][TextPad](by-file/TextPad.md) consumes this serialization stream through `TextEditScrap` during `TextPad::Open` and `TextPad::Save`. TextPad owns the outer local-file command format (visible UTF-16 text, one-byte zero separator, optional 32-bit payload length, optional payload bytes), while this page remains the TextEditPane-owned inner rich-state serializer for the optional payload.

## Ownership Notes

This method is owned/emitted by [UID:0000EO][TextEditPane](by-class/TextEditPane.md), routes through [UID:0000ON][TextEditPane](by-file/TextEditPane.md), and is cross-referenced by [UID:0000HQ][BinaryCodec](by-file/BinaryCodec.md). It is the only confirmed direct behavioral consumer of [UID:00004F][Encoder](by-class/Encoder.md), but it serializes TextEditPane state rather than implementing codec internals.

The sole incoming xref is retained raw [UID:0004K6][0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization](by-memory/0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization.md). That establishes wrapper exposure, not lower-body ownership: [UID:0000OM][TextEditControlPane](by-file/TextEditControlPane.md) owns source-facing `TextEditControlPane::EncodeState`, while this target belongs with TextEditPane because every high-level object offset points into TextEditPane-owned state. Do not move this range to TextEditControlPane.cpp, `BinaryCodec.cpp`, or Encoder.

## Score Rationale

- Completion `92`: exact range/hash/PE mapping, ABI, all instructions, wrapper/xrefs, fields/types, stream, count behavior, allocation/copy/output contract, source placement, formal source, negative behavior, decoder corroboration, padding, and compiler exclusions are closed.
- Confidence `94`: independent target/wrapper/decoder bytes, exact class/constructor/destructor/helper declarations, Encoder/MemoryMan contracts, and inverse behavior agree. Original local spellings, method visibility, and absent ordinary wrapper reachability keep confidence below 95.

## Cross-References

- [UID:0000UM][EncodeTextEditState_0058E490](by-item/EncodeTextEditState_0058E490.md)
- [UID:0000ON][TextEditPane](by-file/TextEditPane.md)
- [UID:0000EO][TextEditPane](by-class/TextEditPane.md)
- [UID:00004F][Encoder](by-class/Encoder.md)
- [UID:0000HQ][BinaryCodec](by-file/BinaryCodec.md)
- [UID:0004K6][0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization](by-memory/0x00498c40-0x00498c5f.TextEditControlPaneStateSerialization.md)

## Changes

- 2026-07-19 B001 UID0001JN accepted implementation callback:
  - Raised `86/90 -> 92/94`, changed canonical owner/emitter from file UID0000ON to class UID0000EO, retained reconstructable true, blank position, and `Nested:-4`, and installed the exact source-authored `TextEditPane::EncodeState` managed body.
  - Incorporated exact byte/hash/PE, ABI/CFG/EH, wrapper/xref, four-field/type, low-word wrap/sign-extension, stack Encoder, text/zero/kong/16-16-4 stream, Finalize/allocation/copy/truncation, caller ownership, paired-decoder, padding, source-placement, negative, compiler-exclusion, score, and rejected/historical evidence.
  - Preserved the complete current UID0002ZZ/UID0004T0/signed-record/TextEditPane class/file/aggregate union; no decoder child, free helper, new source file, hardened stream behavior, explicit compiler body, or IDA mutation was introduced.

- 2026-07-13 B003 UID00011H implementation support sync:
  - Score `86/90`, owner/emitter, exact range, reconstructable state, nesting, and blank formal C++ remain unchanged.
  - Replaced the former unnamed aggregate/raw-wrapper reference with exact registered UID0004K6 and accepted source-facing `TextEditControlPane::EncodeState`; preserved the unique jump, raw signature, one-xref/no-modeled-caller evidence, TextEditPane lower ownership, Encoder/table semantics, and remaining lower-body naming blockers.

- 2026-06-30 B004 corrected TextEditPane implementation callback:
  - Score unchanged at `86/90`; formal C++ remains blank.
  - Added the revised B004 report-level disposition: this exact serializer child is the canonical page for [UID:0000UM][EncodeTextEditState_0058E490](by-item/EncodeTextEditState_0058E490.md), current MCP analysis keeps range `0x0058e490-0x0058e691`, size `0x201`, five basic blocks, prototype shape `void *__fastcall(_DWORD *, int, _WORD *)`, no ordinary callers, and the single raw TextEditControlPane forwarding jump at `0x00498c4a`.
  - Remaining body blockers are exact Encoder/local-buffer source spellings, final table/list member names at `+0x138/+0x13c/+0x140`, wrapper method naming for `0x00498c40-0x00498c4f`, and final stream helper names. The by-item mirror now points here and does not duplicate body C++.

- 2026-06-22 Rule 26 incorporation of B013 [UID:00013H][0x004a4ff0-0x004a506b.EncoderWriteInt](by-memory/0x004a4ff0-0x004a506b.EncoderWriteInt.md) report:
  - Score unchanged at `86/90`.
  - Confirmed the marker callsite at `0x0058e58b`: the pushed immediate is `0x6b6f6e67`, and the stack [UID:00004F][Encoder](by-class/Encoder.md) keeps its constructor-default nonzero byte-order flag, so `Encoder::WriteInt` emits bytes `6b 6f 6e 67` (`"kong"`). The native little-endian branch would emit `67 6e 6f 6b`, but that is not the observed TextEditPane serialization state.

- 2026-06-21 Rule 26 incorporation of B012 [UID:00013G][0x004a4f00-0x004a4f59.EncoderWriteShort](by-memory/0x004a4f00-0x004a4f59.EncoderWriteShort.md) report:
  - Score unchanged at `86/90`.
  - Added the exact `Encoder::WriteShort` length-prefix semantics: calls at `0x0058e59a`, `0x0058e5c9`, and `0x0058e5f8` serialize the `this+0x138`, `this+0x13c`, and `this+0x140` byte lengths, scaled as `count * 16`, `count * 16`, and `count * 4`; constructor-default Encoder byte-order makes those prefixes high-byte-first. This keeps TextEditPane as the serializer owner while supporting Encoder's exact 16-bit writer body.

- 2026-06-16 A002 item/resource completion refresh:
  - Before: `85/89`.
  - Changed to: `86/90`; owner/emitter remain [UID:0000ON][TextEditPane](by-file/TextEditPane.md); final C++ remains blank.
  - Evidence: active IDA MCP session `b001_selflookpane_0001H7_20260616` reconfirmed the exact range, no modeled caller, raw `0x00498c4a` jump, no function or xrefs at `0x00498c40`, stable unique wrapper signature, current decompiler prototype, and Encoder/allocation callee set. TextEditPane constructor/destructor/copy docs prove `+0x134` is text storage; remaining `+0x138`/`+0x13c`/`+0x140` final field names and wrapper method naming still block final C++.

- 2026-06-20 B003 [UID:00013J][0x004a55c0-0x004a55de.EncoderInitialize](by-memory/0x004a55c0-0x004a55de.EncoderInitialize.md) source-quality incorporation:
  - Before/after score unchanged: `86/90`.
  - Summary/evidence: added exact `Encoder::Initialize` argument/callsite details and corrected the `"kong"` marker explanation to match preserved big-endian byte order. TextEditPane remains the serializer owner; [UID:00013J][0x004a55c0-0x004a55de.EncoderInitialize](by-memory/0x004a55c0-0x004a55de.EncoderInitialize.md) remains an Encoder utility dependency with one direct call at `0x0058e54b`.

- 2026-06-20 B005 Rule 26 source-quality incorporation:
  - Score unchanged at `86/90`.
  - Adopted `m_textStorage`, inferred `m_lineTable`, inferred `m_styleRuns`, and inferred `m_formatRecords` in the serialization data-flow notes, and clarified that `0x6b6f6e67` is written through `Encoder::WriteInt` with byte order controlled by Encoder state.
  - Evidence: B005 report compared this helper against the TextEditPane field map and the current Encoder reanalysis; ownership remains [UID:0000ON][TextEditPane](by-file/TextEditPane.md), not TextEditControlPane, Encoder, or BinaryCodec.
- 2026-06-21 Rule 26 incorporation of B008 [UID:00013K][0x004a55e0-0x004a5621.EncoderFinalize](by-memory/0x004a55e0-0x004a5621.EncoderFinalize.md) report:
  - Score unchanged at `86/90`.
  - Added the exact finalize/count path: `Encoder::Finalize(unsigned int *outBytesWritten)` writes a dword count into `[ebp-0x8018]`, this helper uses that dword for exact allocation and copy, and only then truncates to a 16-bit external output size. This keeps TextEditPane as the serializer owner while supporting Encoder's 32-bit finalize signature.

- 2026-06-21 Rule 26 incorporation of B009 [UID:00013H][0x004a4ff0-0x004a506b.EncoderWriteInt](by-memory/0x004a4ff0-0x004a506b.EncoderWriteInt.md) report:
  - Score unchanged at `86/90`.
  - Clarified the scalar marker byte order: this helper calls `Encoder::WriteInt(0x6b6f6e67)`, the stack Encoder preserves constructor-default big-endian scalar output, and the emitted marker bytes are therefore `6b 6f 6e 67` (`"kong"`). The native little-endian branch would emit `67 6e 6f 6b`, but that branch is not the observed TextEditPane serialization state.

- 2026-06-21 B008 TextPad source-quality sync:
  - Score unchanged at `86/90`.
  - Added the TextPad file-format cross-note: TextPad owns `Open`/`Save` command/file routing, and this page owns the optional rich payload stream that TextPad stores through `TextEditScrap`.

- 2026-06-16 A002 file/item completion refresh:
  - Before: `84/88`.
  - Changed to: `85/89`; owner/emitter remain [UID:0000ON][TextEditPane](by-file/TextEditPane.md); final C++ remains blank.
  - Evidence: live IDA reconfirmed exact function size, single raw forwarder xref, no modeled caller, no function at `0x00498c40`, unique forwarding-stub bytes, Encoder/allocation callee set, table offset reads, delimiter and `"kong"` marker writes, and exact output allocation/copy behavior. The remaining blockers are source-quality names, not ownership uncertainty.

- 2026-06-06: Renamed the page to the IDA end address `0x0058e691`, attached the memory item to [UID:0000ON][TextEditPane](by-file/TextEditPane.md), and raised completion/confidence to `84/88`.
  - Before: the filename ended at `0x0058e690`, parent metadata was blank, and ownership was capped by an unresolved raw-thunk caveat.
  - Changed to: exact IDA end address in the filename/title, parent [UID:0000ON][TextEditPane](by-file/TextEditPane.md), explicit TextEditControlPane forwarding-stub evidence, signature/data-flow notes, and a narrower open naming caveat.
  - Summary/evidence: IDA MCP on 2026-06-06 confirms `sub_58E490` at `0x0058e490-0x0058e691`, a single xref from `0x00498c4a`, no ordinary function callers, bounded wrapper disassembly loading `+0x10c`, and the Encoder/allocation callees used by the serialization path.

- Before: the page used `COMPLETION:50` and `CONFIDENCE:65`, older 2026-05-24/25/26 evidence, and less detailed serialization behavior.
- Changed to: `COMPLETION:72` and `CONFIDENCE:84`, current IDA function bounds, raw-thunk caveat, table offset/size rules, and explicit Encoder helper call evidence.
- Summary/evidence: IDA MCP on 2026-05-30 confirms exact `0x0058e490-0x0058e691` bounds, the raw jump at `0x00498c4a`, and the Encoder call sequence used to build, finalize, allocate, copy, and return the serialized buffer.
