*** UID:0002UL | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000098 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void NewPredefinedFormArticleDialog::SubmitArticle(
    unsigned short boardId)
{
    char packet[0x8000] = {};
    char convertedText[0x1000] = {};
    wchar_t subject[0x100] = {};
    wchar_t body[0x1000] = {};
    wchar_t fieldText[0x1000] = {};

    const int subjectLength = static_cast<TextEditControlPane *>(
        GetControl(3))->ReadText(subject, 0xff);

    int bodyLength = 0;

    if (m_predefinedFormEntryCount != 0)
    {
        if (g_pUserPane->m_predefinedFormArticleFreeTextMode)
        {
            bodyLength = static_cast<TextEditControlPane *>(
                GetControl(4))->ReadText(body, 0xfff);
        }
        else
        {
            for (unsigned char row = 0;
                 row < m_predefinedFormEntryCount;
                 ++row)
            {
                const PredefinedFormArticleEntry& entry =
                    m_predefinedFormEntries[row];

                const int labelLength =
                    static_cast<TextEditControlPane *>(
                        GetControl(4 + row * 2))->
                            ReadText(fieldText, 0xfff);
                memmove(body + bodyLength, fieldText,
                    labelLength * sizeof(wchar_t));
                bodyLength += labelLength;

                if (!entry.usesUserImagePane)
                {
                    body[bodyLength++] = L' ';
                }

                const int valueLength =
                    static_cast<TextEditControlPane *>(
                        GetControl(5 + row * 2))->
                            ReadText(fieldText, 0xfff);
                memmove(body + bodyLength, fieldText,
                    valueLength * sizeof(wchar_t));
                bodyLength += valueLength;

                if (entry.rowAdvanceMode == 0)
                {
                    body[bodyLength++] = L' ';
                }
                else if (entry.rowAdvanceMode == 1)
                {
                    body[bodyLength++] = L'\r';
                }
            }
        }
    }

    body[bodyLength] = L'\0';

    PacketBufferWriteUInt8(0x3b, packet);
    PacketBufferWriteUInt8(0x04, packet + 1);
    PacketBufferWriteUInt16BE(boardId, packet + 2);

    const int subjectByteLength = WideCharToMultiByte(
        CP_ACP, 0, subject, subjectLength,
        convertedText, 0x100, NULL, NULL);
    PacketBufferWriteUInt8(
        static_cast<unsigned char>(subjectByteLength),
        packet + 4);
    memmove(packet + 5, convertedText, subjectByteLength);

    const int bodyByteLength = WideCharToMultiByte(
        CP_ACP, 0, body, bodyLength,
        convertedText, sizeof(convertedText), NULL, NULL);

    char *bodyLengthField = packet + 5 + subjectByteLength;
    PacketBufferWriteUInt16BE(
        static_cast<unsigned short>(bodyByteLength),
        bodyLengthField);
    memmove(bodyLengthField + 2, convertedText, bodyByteLength);

    const int packetLength =
        subjectByteLength + bodyByteLength + 7;
    QueueAndSendPacket(g_packetSender, packet, packetLength);
    TimerHandler::ScheduleTimer(0, 15000, 0, 0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `NewPredefinedFormArticleDialog::SubmitArticle(short boardId)` first-draft C++ for opcode `0x3b`/subcommand `4`: reads subject control `3`, free-text control `4` or parsed-row label/value control pairs, serializes board id plus subject/body ANSI payloads, queues through `g_packetSender`, and starts the 15000 ms post-send wait. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00478be0-0x00478f8e NewPredefinedFormArticleDialog SubmitArticle

## Status

- Disposition: reconstructable NexusTK dialog method.
- Entity kind: modeled class method.
- Direct owner: [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
- Parent aggregate: [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md).
- Likely source file: [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md).
- Rebuild handling: `source-authored`.

## Address Range

`0x00478be0-0x00478f8e`.

2026-06-26 active IDA MCP session `80de0a67` reconfirmed the exact split. `lookup_funcs` command id `11` reports `sub_478BE0` at this exact half-open range, size `0x3ae` / 942 bytes (Verified with int_convert.py), and reports `0x00478f8e` / `0x00478f90` as `Not a function`. `disasm` command id `19` shows the terminal `retn 4` at `0x00478f86`, the range-check failure tail at `0x00478f89`, a `0xD214` stack frame, 253 instructions, and a local `0x8000` byte packet buffer guarded by `packetLength < 0x7fff`. `get_bytes` command id `20` reports `0x00478f8e-0x00478f90` as two `0xcc` alignment bytes before the raw [UID:00021A][0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor](by-memory/0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor.md). The predecessor [UID:0002UR][0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload](by-memory/0x00478aa0-0x00478bd9.NewPredefinedFormArticleDialogHandleTransferReplyPredefinedAlertPayload.md) remains separate: command id `20` reports `0x00478bd9-0x00478be0` as seven `0xcc` padding bytes.

Signature evidence: IDA decompiles the body as a `__thiscall` helper on `ecx` with one stack `__int16` argument. The source-facing signature is `void NewPredefinedFormArticleDialog::SubmitArticle(short boardId)`: the only direct caller [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md) passes the selected board id and ignores any return value, so the decompiler's apparent `int` is only the tail expression from the timeout helper, not a consumed source result.

## Behavior

Serializes and queues a predefined form article submission. The method reads the subject from control id `3`, builds the body text either from free-text control id `4` or from each parsed predefined row's label/value control pair, converts subject and body from wide text to ANSI bytes, builds opcode `0x3b` / 59 (Verified with int_convert.py) with subcommand `4`, appends the selected board id, one-byte subject length and subject bytes, two-byte body length and body bytes, queues the packet through source-facing `QueueAndSendPacket(g_packetSender, packet, packetLength)`, then schedules the dialog/send wait through `StartTransferTimeout(15000)` / raw `sub_5975E0(this + 0xa4, 0, 0x3a98, 0, 0)`.

The non-free-text path loops over `word [this + 0x274]` / source-facing `m_predefinedFormEntryCount` parsed entries and uses the `0x40c` / 1036-byte record stride (Verified with int_convert.py). It reads two controls per row, using ids `4 + row * 2` and `5 + row * 2`, inserts a space when the record-local `usesUserImagePane` byte at `+0x202` / submit absolute offset `this + row * 0x40c + 0x47a` is clear, and inserts either a space or carriage return depending on the record-local `rowAdvanceMode` dword at `+0x408` / submit absolute offset `this + row * 0x40c + 0x680`: value `0` appends a space, value `1` appends `'\r'`, and other values append no separator. The free-text path is selected by `g_pUserPane->m_predefinedFormArticleFreeTextMode` / raw `byte [dword_67A748 + 0x1340fc]` and reads a single body control id `4`.

The submitted packet layout is `[0x3b, 0x04, boardId:u16be, subjectLen:u8, subjectBytes..., bodyLen:u16be, bodyBytes...]`. The local zero byte written after the sent bytes is stack/local termination and is not protocol payload. No validation or chat-text sanitization calls are part of this target; unlike the normal `NewArticleDialog::SubmitArticle` analogue, the current callee inventory and decompilation for this predefined-form submitter contain no `ValidateChatInputOrThrowPasswordError` or `SanitizeWideTextForChat` path.

## Evidence

- 2026-06-26 active IDA MCP session `80de0a67`, `server_health` command id `10` at `2026-06-26T06:38:25.1150588-04:00`, reports `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, and `hexrays_ready:true`.
- `lookup_funcs` command id `11` reports this target as `sub_478BE0`, size `0x3ae` / 942, reports the successor addresses `0x00478f8e` and `0x00478f90` as `Not a function`, and confirms neighboring class-cluster starts including [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md) `sub_478240` size `0x62`, parser `sub_478650` size `0x2e3`, live transfer-reply handler `sub_478940` size `0x153`, retained helper `sub_478AA0` size `0x139`, and normal article submit `sub_477590` size `0x200`.
- `xrefs_to` command id `17` reports the only direct code xref to `0x00478be0` as `0x0047827e` inside [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md). The caller itself is routed through vtable/data slot `0x00614098 -> 0x00478240`; this submit method is a class helper reached by that OK-button path, not a vtable slot on its own.
- `analyze_function` command id `14` reports the decompiler prototype as `int __thiscall(unsigned int this, __int16)`, size 942, 23 basic blocks, cyclomatic complexity 14, caller `sub_478240`, and callees `__alloca_probe`, `sub_516030`, `sub_498C10`, `_wcscpy_s`, `sub_516220`, `sub_575380`, `sub_5753A0`, `WideCharToMultiByte`, `sub_574BB0`, `sub_5975E0`, `@__security_check_cookie@4`, and `___report_rangecheckfailure`.
- `analyze_function` command id `15` on [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md) reports the OK path reading `this+0x270`, using `session+0x102` as the selected board id when present, calling `sub_478BE0(a1, v7)` at `0x0047827e`, storing `this` at `dword_67A748+0x214`, and calling `sub_4A12B0`; the cancel path calls `sub_4A10E0(session, ..., 0)`.
- `decompile` command id `18` shows the subject read through the dialog control container at `this+0x1fc`, control id `3`, and `sub_498C10(..., WideCharStr, 255)`, then body assembly into a `0x1000` / 4096 wide-character destination buffer (Verified with int_convert.py).
- The same decompilation shows non-free-text mode guarded by `!byte [dword_67A748 + 0x1340fc]`, loop bound `word [this + 0x274]`, row stride `1036 * row`, row control ids `2 * row + 4` and `2 * row + 5`, row-format byte at entry-local `+0x202` / submit absolute `+0x47a`, row-advance dword at entry-local `+0x408` / submit absolute `+0x680`, and the free-text branch reading body control id `4` with limit `0xfff`.
- `decompile` command id `18` and `disasm` command id `19` show packet serialization at `0x00478e44-0x00478f55`: byte writer for opcode `0x3b`, byte writer for subcommand `4`, big-endian board id writer, `WideCharToMultiByte` for subject, byte subject length writer, `memmove`/`sub_516220` for subject bytes, `WideCharToMultiByte` for body, big-endian body length writer, `memmove`/`sub_516220` for body bytes, send-length check against `0x7fff` / 32767 (Verified with int_convert.py), and `sub_574BB0(dword_67A7EC, packet, totalLen)`.
- `get_bytes` command id `20` confirms seven left `0xcc` bytes at `0x00478bd9-0x00478be0` and two right `0xcc` bytes at `0x00478f8e-0x00478f90`.
- `int_convert` command id `21` verifies the numeric facts used here: `0x3ae` = `942`, `0x62` = `98`, `0x3b` = `59`, `0x3a98` = `15000`, `0x7fff` = `32767`, `0x40c` = `1036`, `1261820` = `0x1340fc`, `628` = `0x274`, `508` = `0x1fc`, `1146` = `0x47a`, `1664` = `0x680`, `532` = `0x214`, `624` = `0x270`, and `258` = `0x102`.

## Touched State And Data Flow

- Reads `this + 0x1fc` as the dialog control container and uses virtual slot `+0x10`; source-facing C++ models this as `GetControl(controlId)`.
- Calls the text edit read helper `sub_498C10`, modeled as `TextEditControlPane::ReadText`, for subject, free-text body, row labels, and row values.
- Reads `this + 0x274` as source-facing `m_predefinedFormEntryCount` and per-row records at `this + 0x278 + row * 0x40c` as source-facing `m_predefinedFormEntries[row]`.
- Reads record-local `+0x202` as `usesUserImagePane` and record-local `+0x408` as `rowAdvanceMode`; these names are inferred from parser/constructor support docs and remain confidence caps, not C++ blockers.
- Reads global `g_pUserPane->m_predefinedFormArticleFreeTextMode` / raw `dword_67A748 + 0x1340fc` to choose free-text versus parsed-row body assembly.
- Writes and sends a stack packet buffer beginning with opcode `0x3b`, subcommand `4`, board id, subject length/body length fields, and ANSI payloads.
- Uses source-facing destination-first packet writer wrappers `PacketBufferWriteUInt8` / `PacketBufferWriteUInt16BE` for `sub_575380` / `sub_5753A0`, matching current project convention even though the raw ABI push order is value/callee-specific.
- Sends through `g_packetSender` / raw global `dword_67A7EC` via `QueueAndSendPacket` / raw `sub_574BB0`, then calls `StartTransferTimeout(15000)` / raw `sub_5975E0` with timeout `0x3a98` / 15000 (Verified with int_convert.py) against `this + 0xa4`.

## Source-Facing Name Caveats

- The previous blank-C++ blockers are now confidence caps. `GetControl`, `TextEditControlPane::ReadText`, `m_predefinedFormEntryCount`, `m_predefinedFormEntries`, `PredefinedFormArticleEntry`, `usesUserImagePane`, `rowAdvanceMode`, `m_predefinedFormArticleFreeTextMode`, `PacketBufferWriteUInt8`, `PacketBufferWriteUInt16BE`, `QueueAndSendPacket`, `g_packetSender`, and `StartTransferTimeout` are source-facing first-draft names supported by current class/parser/packet/global docs and live behavior, not recovered original declarations.
- The formal C++ is intentionally on this exact child page. The aggregate [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md), class page, and file page remain metadata/route documentation for broader source ownership rather than whole-cluster method-body emitters.

## Rejected Alternatives

- Keeping final C++ blank pending a stale `95+` gate or original private names is rejected. Current workflow allows first-draft source once behavior, range, route, and helper/global names are sufficiently supported; the remaining private-name uncertainty caps confidence below final audit.
- Keeping final C++ blank because row fields and the free-text flag are inferred is rejected. The row offsets, branch values, control ids, packet bytes, and send side effects are exact; inferred source-facing names are documented as caveats.
- Routing ownership to `Socket`, `PacketBuffer`, `TextEditControlPane`, or `UserPane` is rejected. Those are dependencies consumed by this class method. The sole code caller and class cluster support direct ownership by [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md).
- Merging this method into [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md) is rejected. The caller reads the selected board id and performs dialog/session side effects; this target owns body assembly and packet serialization.
- Merging this body into the aggregate is rejected because this project emits method bodies on exact child pages for this class, including `NavigateToPreviousArticleSelection`, `HandleEvent`, and `OnPaint`.
- Modeling the source signature as `int SubmitArticle(...)` is rejected. No caller consumes a return value; the apparent integer is only produced by tail-returning `sub_5975E0`.
- Adding normal article validation/sanitization calls by analogy to [UID:00030K][0x00476c10-0x00477790.NewArticleDialogCore](by-memory/0x00476c10-0x00477790.NewArticleDialogCore.md) `NewArticleDialog::SubmitArticle` is rejected. Current live MCP callee inventory for this exact target lacks those calls.
- Treating `0x00478f8e-0x00478f90` as part of the body or merging with [UID:00021A][0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor](by-memory/0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor.md) is rejected by `lookup_funcs`, disassembly, and byte evidence.

## Score Rationale

Completion is raised from `86` to `90` because the target now carries formal first-draft C++, exact 2026-06-26 MCP session evidence, source-facing packet/helper/global names, row field mappings, route proof, split proof, no-validation/sanitizer negative evidence, and rejected alternatives. Confidence is raised from `87` to `91` because behavior, caller, range, packet layout, row offsets, helper/global routes, and caller-side void-return evidence are all current-MCP and support-doc backed.

The score does not rise higher because `m_predefinedFormArticleFreeTextMode`, `m_predefinedFormEntries`, `PredefinedFormArticleEntry`, `ReadText`, and `StartTransferTimeout` are source-facing first-draft names rather than recovered original declarations, and the constructor/parser pages still lack a final full class layout. Owner/emitter remains [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md) because this is a class helper reached only from the class button handler; packet/socket/text/UserPane dependencies remain support dependencies, not source owners.

## Cross-References

- [UID:0000ZL][0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog](by-memory/0x004777a0-0x00478f8e.NewPredefinedFormArticleDialog.md)
- [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md)
- [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md)
- [UID:00021A][0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor](by-memory/0x00478f90-0x00478fd8.TransferReplyAlertRawConstructor.md)

## Changes

- 2026-08-14 B004 UID0000LT implementation callback: raised `90/91` to `93/94` and replaced the formal block with the accepted exact source. `SubmitArticle` now takes `unsigned short boardId`, preserves subject/free-text or row-control assembly, opcode `0x3b`, subcommand `4`, big-endian board/body lengths, ACP conversion, packet send, and exact inherited `TimerHandler::ScheduleTimer(0, 15000, 0, 0)`. Explicit `__report_rangecheckfailure`, decompiler truncation casts, synthetic terminator checks, and the non-source `StartTransferTimeout` wrapper were removed while preserving the original bounded-array/runtime behavior described in evidence.

- 2026-06-26 B014 [UID:0002UL] implementation sync: raised `COMPLETION:86` / `CONFIDENCE:87` to `90/91`, kept owner/emitter [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md), inserted exact formal first-draft `void NewPredefinedFormArticleDialog::SubmitArticle(short boardId)` C++, and replaced the stale blank-C++/packet-name blocker language. Active MCP session `80de0a67` evidence is incorporated: command ids `10`, `11`, `14`, `15`, `17`, `18`, `19`, `20`, and `21` prove the exact `0x00478be0-0x00478f8e` range, left/right `0xcc` padding, sole caller `0x0047827e` from [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md), [UID:0002UK][0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick](by-memory/0x00478240-0x004782a2.NewPredefinedFormArticleDialogHandleButtonClick.md)'s vtable route `0x00614098 -> 0x00478240`, subject control `3`, free-text control `4`, parsed-row label/value controls, `0x40c` stride, `+0x202` / `+0x408` row fields, opcode `0x3b` / subcommand `4` packet layout, `g_packetSender` send path, 15000 ms post-send wait, no validation/sanitizer callees, void-return rationale, and rejected alternatives.
- 2026-06-15 A001-goal2-newpredefinedformarticle-memory-20260615-1: raised completion/confidence from `78/86` to `86/87` after live IDA MCP reconfirmed the exact `0x3ae`-byte range, only direct caller at `0x0047827e`, free-text and parsed-row body branches, row stride/field offsets, opcode `0x3b` packet serialization, send queue path, range checks, and complete callee list. Final C++ stays blank pending source-final control, row-record, and packet-helper declarations.
- 2026-06-16 C001 direct IDA ownership sync: score unchanged at `86/87`; changed owner/emitter from file [UID:0000LT][NewPredefinedFormArticleDialog](by-file/NewPredefinedFormArticleDialog.md) to direct class [UID:000098][NewPredefinedFormArticleDialog](by-class/NewPredefinedFormArticleDialog.md). Evidence: live IDA reconfirmed the only code xref is the class button handler submit edge `0x0047827e -> 0x00478be0`; this is a class-owned helper rather than a file-level free helper. Final C++ remains blank pending source-final control, row-record, and packet-helper declarations.
