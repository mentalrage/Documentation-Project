*** UID:0001E9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000A1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000A1 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool PaletteLib::SetPaletteFilterActive(bool active)
{
    m_useFilteredPalettes = active;
    return active;
}

float PaletteLib::GetPaletteFilterWeight() const
{
    return m_paletteFilterWeight;
}

void PaletteLib::UpdatePaletteFilterTables(float weight)
{
    if (weight == m_paletteFilterWeight) {
        return;
    }

    m_paletteFilterWeight = weight;

    const unsigned char strength = static_cast<unsigned char>((weight * 32.0f) + 0.5f);

    for (int color = 1; color < 256; ++color) {
        m_filteredBasePalette.m_mappedColors[color] =
            g_pfnTransformPaletteColor(m_basePalette.m_mappedColors[color], strength);
    }

    for (int paletteIndex = 0; paletteIndex < 7; ++paletteIndex) {
        DLPalette& sourcePalette = m_superPalettes[paletteIndex];
        DLPalette& filteredPalette = m_filteredSuperPalettes[paletteIndex];

        for (int color = 1; color < 256; ++color) {
            filteredPalette.m_mappedColors[color] =
                g_pfnTransformPaletteColor(sourcePalette.m_mappedColors[color], strength);
        }
    }

    for (int slot = 0; slot < 25; ++slot) {
        if (slot == 11) {
            continue;
        }

        List* sourceList = m_sourcePaletteSets[slot];
        List* filteredList = m_runtimePaletteSets[slot];
        const int count = sourceList->GetCount();

        for (int index = 0; index < count; ++index) {
            DLPalette* sourcePalette = *reinterpret_cast<DLPalette**>(sourceList->GetAt(index));
            DLPalette* filteredPalette = *reinterpret_cast<DLPalette**>(filteredList->GetAt(index));

            for (int color = 1; color < 256; ++color) {
                filteredPalette->m_mappedColors[color] =
                    g_pfnTransformPaletteColor(sourcePalette->m_mappedColors[color], strength);
            }
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B014 2026-06-26 source-quality pass resolves PaletteLib::SetPaletteFilterActive, raw GetPaletteFilterWeight, and UpdatePaletteFilterTables: +0x758 selects filtered/runtime palette banks, +0x75c caches the weight, IDA session 80de0a67 confirms boundaries/padding, xrefs/callers, no raw-getter split, g_pPaletteLib receiver, dword_69B408 transform dependency, 25-slot runtime loops, skip slot 11, and formal C++ readiness. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00543f60-0x005440f0 PaletteLibPaletteFilterHelpers

## Status

- Confidence: strong for `PaletteLib` ownership, exact helper boundaries, caller set, field roles, raw-getter disposition, and filter-table behavior; final method/field names are inferred source-facing names rather than recovered symbols.
- Covered class: [UID:0000A1][PaletteLib](by-class/PaletteLib.md).
- Source file: focused [UID:0000MB][PaletteLib](by-file/PaletteLib.md), `NexusTK/render/PaletteLib.cpp`; [UID:0000MA][Palette](by-file/Palette.md) remains umbrella support.
- Direct owner/emitter: [UID:0000A1][PaletteLib](by-class/PaletteLib.md), which routes to [UID:0000MB][PaletteLib](by-file/PaletteLib.md) and the broader [UID:0000MA][Palette](by-file/Palette.md) umbrella.
- IDA MCP recheck: 2026-06-26, session `80de0a67`.

## Functions

| Range | Function | Notes |
| --- | --- | --- |
| `0x00543f60-0x00543f70` | `PaletteLib::SetPaletteFilterActive` | IDA function `sub_543F60`; stores the byte argument at object offset `+0x758` and returns it. The field selects source palettes versus filtered/runtime palettes, so `active` is more precise than the older provisional `enabled` wording. |
| `0x00543f70-0x00543f77` | `PaletteLib::GetPaletteFilterWeight` raw getter | Not an IDA function object and has no observed direct xrefs; raw bytes load the float at `+0x75c` and return. Keep it inside this helper page rather than splitting a no-route child. |
| `0x00543f80-0x005440f0` | `PaletteLib::UpdatePaletteFilterTables` | IDA function `sub_543F80`; updates cached 16-bit palette tables from a float weight through the global color-transform callback, then rebuilds runtime slot palettes except slot `11`. |

## Evidence Notes

- IDA `lookup_funcs` in session `80de0a67` reports `sub_543F60` at `0x00543f60-0x00543f70` and `sub_543F80` at `0x00543f80-0x005440f0`; `0x00543f70`, `0x00543f77`, and `0x005440f0` are not IDA function starts.
- IDA callers load `g_pPaletteLib` (`0x0067a7e0`) into `ecx` immediately before calling these helper functions.
- `0x00543f60` has four code refs from map render/tile-update paths: `0x005096e7` and `0x0050a277` in `sub_5094B0`, plus `0x0050d94f` and `0x0050d99f` in `sub_50D8E0`.
- `0x00543f80` has two code refs from map render paths: `0x00509595` in `sub_5094B0` and `0x0050a918` in `sub_50A8B0`.
- `0x00543f60` has no callees; `0x00543f80` calls only `dword_69B408` (`0x0069b408`) directly.
- Decompilation for `0x00543f80` caches the float weight at `+0x75c`, converts it to an integer strength `(weight * 32.0f) + 0.5f`, calls `dword_69B408` to transform base/super palette entries, then walks the 25 runtime slot lists and skips slot `11`.
- Runtime source-list and destination-list pairs are read from the 25-entry list-pointer banks at `this+0x690` and `this+0x6f4`; each non-slot-11 runtime palette entry rewrites 255 16-bit colors beginning at entry offset `+0x2e`.
- Earlier docs attached these functions to `ScreenPanePaletteState_543D30`; that is incorrect for current IDA evidence because every caller uses the `PaletteLib` singleton pointer as `this`.
- 2026-06-14 live IDA MCP database `b001_0003gy` reconfirms `sub_543F60` size `0x10`, `0x00543f70` as `Not a function`, `sub_543F80` size `0x170`, and `0x005440f0` as `Not a function`.
- The 2026-06-14 xrefs reconfirm four callers to `0x00543f60` from `0x005096e7`, `0x0050a277`, `0x0050d94f`, and `0x0050d99f`; two callers to `0x00543f80` from `0x00509595` and `0x0050a918`; zero xrefs to the raw getter at `0x00543f70`; and zero xrefs to successor `0x005440f0`.
- `g_pPaletteLib` at `0x0067a7e0` still has 83 data refs in the active IDB. Color-transform callback/table `0x0069b408` has eight refs, including three inside `0x00543f80` at `0x00543fdd`, `0x00544020`, and `0x005440a8`.
- 2026-06-26 decompilation confirms `0x00543f60` stores the byte argument at offset `+0x758` and returns it. `0x00543f80` returns early when the input float equals cached offset `+0x75c`, then rebuilds base, seven super/reserved palettes, and non-slot-11 runtime slot palettes through `0x0069b408`.
- Accessor recheck resolves `+0x758` as `m_useFilteredPalettes`, not just a generic enable byte: `GetCurrentPalette`, `GetPaletteByName`, and `GetSlotPalette` all branch on the byte to choose source/base palettes versus filtered/runtime palettes. The constructor initializes it to `0`.
- The raw getter at `0x00543f70` is the complete source-shaped body for `PaletteLib::GetPaletteFilterWeight() const`: byte pattern `D9 81 5C 07 00 00 C3`, unique whole-image match, no code refs, and no VA/RVA pointer or immediate hits to `0x00543f70`. It remains inside this page because it shares `m_paletteFilterWeight` with `UpdatePaletteFilterTables`.
- `+0x75c` is `m_paletteFilterWeight`: the constructor initializes it to `0x3f800000` / 1.0f, the raw getter returns it, and the updater compares/stores it before rebuilding derived tables.
- `sub_5094B0` calls `UpdatePaletteFilterTables(g_pPaletteLib, this+0x3dc)` at `0x00509595` when the `g_pConfig+0x28de5e` graphics/shadow byte differs from the cached scratch flag at `0x0069b4f8`, then calls `sub_50D840` and invalidates. The same render body calls `SetPaletteFilterActive(g_pPaletteLib, 1)` at `0x005096e7` and `SetPaletteFilterActive(g_pPaletteLib, 0)` at `0x0050a277`.
- `sub_50A8B0` calls `UpdatePaletteFilterTables(g_pPaletteLib, targetBrightness)` at `0x0050a918` after day/night fade target changes when the graphics/shadow byte is clear. `sub_50D8E0` calls `SetPaletteFilterActive(g_pPaletteLib, 1)` at `0x0050d94f` before a tile-region draw path and restores `0` at `0x0050d99f`.
- The Surface callback slot at `0x0069b408` is the transform dependency consumed here. Use source-facing consumer alias `g_pfnTransformPaletteColor` / `SurfacePaletteTransformProc`; the Surface callback-table pages own the slot and final typedef spelling.

## Field And Source-Name Resolution

| Field / helper | Resolution | Evidence |
| --- | --- | --- |
| `+0x758` / 1880 (Verified with int_convert.py) | `m_useFilteredPalettes` | Constructor initializes the byte to `0`; the setter writes it; `GetCurrentPalette`, `GetPaletteByName`, and `GetSlotPalette` read it to choose source/base palettes or filtered/runtime palettes. |
| `+0x75c` / 1884 (Verified with int_convert.py) | `m_paletteFilterWeight` | Constructor initializes `0x3f800000` / 1.0f; the raw getter returns it; `UpdatePaletteFilterTables` compares and updates it as a cache guard. |
| `0x00543f60` | `PaletteLib::SetPaletteFilterActive(bool active)` | Setter changes the active palette bank selector; B014 resolves this as the source-facing helper name. |
| `0x00543f70` | `PaletteLib::GetPaletteFilterWeight() const` | One-instruction x87 getter for the same cached field; no split is needed because no independent owner, route, or field family exists. |
| `0x00543f80` | `PaletteLib::UpdatePaletteFilterTables(float weight)` | Rebuilds the filtered base, super, and runtime slot palette tables only when the cached weight changes. |

## Boundary And Byte Evidence

| Range | Evidence |
| --- | --- |
| `0x00543f55-0x00543f60` | Eleven `0xcc` alignment bytes after `ScreenPanePaletteState_543D30::ResetSlots` / `sub_543EE0`. |
| `0x00543f60-0x00543f70` | IDA function `sub_543F60`; bytes `55 8b ec 8a 45 08 88 81 58 07 00 00 5d c2 04 00`; stores `[ecx+0x758]`, returns the byte argument, and ends with `retn 4`. |
| `0x00543f70-0x00543f77` | Raw getter-shaped bytes `d9 81 5c 07 00 00 c3`, equivalent to `fld dword ptr [ecx+75Ch]; retn`. |
| `0x00543f77-0x00543f80` | Nine `0xcc` alignment bytes before `sub_543F80`. |
| `0x00543f80-0x005440f0` | IDA function `sub_543F80`; final bytes at `0x005440e7-0x005440ef` are `pop edi; pop esi; pop ebx; mov esp, ebp; pop ebp; retn 4`. |
| `0x005440f0` | Start of a separate raw function-shaped cleanup/body not covered by this page; 2026-06-14 IDA still does not model it as a function and reports zero xrefs to the start. |

## Ownership Decision

Promote these helpers into the `PaletteLib` class under the render palette source family. They are runtime palette-filter state and derived-table updates used by map rendering, not screen palette-bank reset helpers. The raw getter stays documented with this helper group because it is a complete source-shaped accessor for the same `m_paletteFilterWeight` field and has no independent route, owner, or split benefit.

## Reconstruction Notes

- Reconstructable as `PaletteLib` behavior with first-draft C++ now present in the formal reconstruction block. The emitted code uses inferred, source-facing names instead of IDA labels: `m_useFilteredPalettes`, `m_paletteFilterWeight`, `m_filteredBasePalette`, `m_basePalette`, `m_superPalettes`, `m_filteredSuperPalettes`, `m_sourcePaletteSets`, `m_runtimePaletteSets`, and `g_pfnTransformPaletteColor`.
- Preserve the change guard in the table updater: it returns without rebuilding when the incoming float equals the cached `+0x75c` value under the observed `ucomiss`/`lahf` branch sequence.
- Slot `11` is intentionally skipped while rebuilding runtime slot palettes; do not treat this as an off-by-one until the slot table names are final.
- The update helper transforms color entries `1..255`, not entry `0`: `DLPalette::m_mappedColors` starts at `+0x2c`, while the transform loops write 255 entries beginning at `+0x2e`.
- The generated C++ should not add defensive null checks around the source/runtime slot lists. The optimized binary reads the source count from list offset `+0x0c`, calls each list's vtable slot `+0x10` accessor, dereferences each returned `DLPalette*` entry, and expects the matching runtime list to exist.

## Cross-References

- [UID:0000MB][PaletteLib](by-file/PaletteLib.md)
- [UID:0000MA][Palette](by-file/Palette.md)
- [UID:0001E6][0x005431c0-0x005443ab.PaletteLibMethodCluster](by-memory/0x005431c0-0x005443ab.PaletteLibMethodCluster.md)
- [UID:0000RW][g_pPaletteLib](by-global/g_pPaletteLib.md)
- [UID:0001E8][0x00543d30-0x00543f54.ScreenPanePaletteState](by-memory/0x00543d30-0x00543f54.ScreenPanePaletteState.md)

## Score Rationale

Completion is now `91` because the page records exact helper boundaries, raw getter bytes and no-split proof, caller/xref counts, singleton/callback refs, decompiled setter/update behavior, successor no-function status, resolved field roles, map-render/day-night caller contexts, Surface transform dependency, and formal C++ for the full target range.

Confidence is `91` for this exact range's behavior and ownership: current IDA function, xref, byte, and decompile evidence all point to the `PaletteLib` singleton object and the [UID:0000A1][PaletteLib](by-class/PaletteLib.md) class support page. The complete `0x25c8` class layout is now resolved by UID00028T work; this helper score remains unchanged because original method/field spellings and the exact Surface callback typedef for `0x0069b408` are not symbol-proven.

## B001 UID00028T Source-Order And Complete-Class Context - 2026-07-20

- Position `30` places these three existing definitions after the complete PaletteLib class at 10, singleton definition at 20, and screen-state methods at 25, and before the physical storage marker at 40.
- The accepted formal block above is preserved byte-for-byte. No method signature, field spelling, callback spelling, loop, slot-11 skip, low-level behavior, guard, or null-check disposition changed.
- Complete class context now fixes source/runtime list banks at `+0x690/+0x6f4`, filtered selector at `+0x758`, natural alignment and cached weight at `+0x75c`, seven source/filtered super palettes at `+0x760/+0x1694`, and object end at `+0x25c8`.
- Focused `PaletteLib.cpp` owns these methods. Broad Palette remains DLPalette/free-helper umbrella support, and Surface remains callback-provider dependency only.

## Changes

- 2026-07-20 B001 UID00028T synchronization: score/owner/emitter/reconstructable state and the complete managed formal block are unchanged; set position 30 and added focused source order plus resolved complete-class layout context.
- 2026-06-26 B014 source-quality implementation:
  - Before: score was `86/90`, the setter still used a provisional generated name, the raw getter had no source-facing method name, `+0x758/+0x75c` were still blockers, and formal C++ was blank.
  - Changed to: score `91/91`, method names `PaletteLib::SetPaletteFilterActive`, `PaletteLib::GetPaletteFilterWeight`, and `PaletteLib::UpdatePaletteFilterTables`, fields `m_useFilteredPalettes` and `m_paletteFilterWeight`, and first-draft formal C++ for the target range.
  - Summary/evidence: B014 IDA MCP session `80de0a67` reconfirmed exact function starts/sizes, padding, four setter callers, two updater callers, no refs or pointer/immediate hits to the raw getter or `0x005440f0`, `g_pPaletteLib` receiver setup, `dword_69B408` transform dependency, constructor/accessor uses of `+0x758`, constructor/getter/updater uses of `+0x75c`, runtime slot banks `+0x690/+0x6f4`, 25-slot iteration, and intentional skip of slot `11`.

- 2026-06-14 A002 Goal 2 refresh:
  - Before: score was `76/86`, and the page routed directly to the broad [UID:0000MA][Palette](by-file/Palette.md) umbrella.
  - Changed to: `86/90`, direct owner/emitter [UID:0000A1][PaletteLib](by-class/PaletteLib.md), with [UID:0000MB][PaletteLib](by-file/PaletteLib.md) and [UID:0000MA][Palette](by-file/Palette.md) remaining source-file context.
  - Summary/evidence: live IDA MCP database `b001_0003gy` reconfirmed function sizes, no function/xrefs at `0x00543f70` and `0x005440f0`, four setter callers, two updater callers, 83 refs to `g_pPaletteLib`, eight refs to `0x0069b408`, setter decompilation storing `+0x758`, updater decompilation caching `+0x75c` and rebuilding base/super/runtime tables, and exact boundary bytes. C++ remains blank below the active code-entry gate.

- Before: the page used the old inclusive-style range `0x00543f60-0x005440ef` and documented only two helpers.
- Changed to: the page now uses exclusive end `0x005440f0`, includes the raw cached-weight getter at `0x00543f70-0x00543f77`, and records padding around the helper bodies.
- Summary/evidence: 2026-06-01 IDA MCP `lookup_funcs`, callers/callees, xrefs, disassembly, decompilation, and byte reads confirm the exact IDA function ranges, no direct xrefs to the raw getter, the `dword_69B408` transform dependency, and the alignment bytes.
