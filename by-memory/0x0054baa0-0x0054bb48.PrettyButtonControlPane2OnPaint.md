*** UID:0004EF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AS | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int PrettyButtonControlPane2::OnPaint()
{
    int frameId;

    if (!m_enabled) {
        frameId = m_disabledFrameId;
    } else if (m_visualStateFrameActive) {
        frameId = m_pressedFrameId;
    } else if (m_selected) {
        frameId = m_selectedFrameId;
    } else {
        frameId = m_normalFrameId;
    }

    EPFTileContext tileContext;
    tileContext.Init();

    g_pEPFLib->LookupLayoutEntry(m_epfResourceName.c_str(), frameId, &tileContext);

    FrameDrawRecord drawRecord;
    return RenderTileFrame(&tileContext,
                           &drawRecord,
                           &m_bounds,
                           1,
                           m_paletteResourceName.c_str(),
                           0);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready PrettyButtonControlPane2::OnPaint child split from UID0001F9; B005 MCP session 9fb70afc confirms function 0x0054baa0 size 0xa8, vtable-only slot 0x006223c0, constructor-backed frame ids at +0x10c/+0x110/+0x114/+0x118, state bytes +0x11c/+0x11d, EPF/palette strings +0x120/+0x124, configured disabled-frame selection instead of class1 overlay drawing, and shared ResourceLayoutTable/RenderTileFrame helper calls. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0054baa0-0x0054bb48 PrettyButtonControlPane2 OnPaint

## Evidence

- B005 accepted source-quality report used live MCP session `9fb70afc`: `idb_list` reported active worker session for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, and `server_health` returned `status:ok`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.
- MCP `lookup_funcs 0x0054baa0` returned `sub_54BAA0` size `0xa8`, giving exact half-open range `0x0054baa0-0x0054bb48`.
- MCP `xrefs_to 0x0054baa0` reported one data xref from vtable slot `0x006223c0` and no direct code callers. MCP `get_bytes 0x006223c0` returned `a0 ba 54 00`, the little-endian pointer to this method.
- MCP `get_bytes` confirmed `0x0054ba92-0x0054baa0` as fourteen `0xcc` bytes and `0x0054bb48-0x0054bb50` as eight `0xcc` bytes. No split, merge, or boundary repair is needed.
- MCP `analyze_function`, `decompile`, and bounded `disasm` confirm the branch order. If inherited byte `this+0x101` is false, the method selects frame field `this+0x118`; otherwise byte `this+0x11d` selects frame `this+0x110`, byte `this+0x11c` selects frame `this+0x114`, and the default frame is `this+0x10c`.
- MCP constructor checks for `0x0054b8c0` confirm the four frame IDs are constructor arguments stored at `+0x10c/+0x110/+0x114/+0x118`, resource strings are constructed at `+0x120/+0x124`, byte `+0xff` receives the constructor flag, byte `+0x100` is cleared, and callback pointer `+0x128` is cleared.
- MCP visual-state setter checks for `0x0054ba30` confirm byte `+0x11d` is set only for state `0x0b`, with timer scheduling through `0x005975e0` using `0x96` / 150 ms and clearing/removal through `0x00597600` otherwise. UID0004EC documents byte `+0x11c` as the selected byte written by `PrettyButtonControlPane2::SetSelected(bool selected)`.
- MCP `callees 0x0054baa0` reports `0x00457a60`, `0x00584540`, `0x004d02f0`, `0x004b9980`, and `@__security_check_cookie@4`. Current support docs identify these as `EPFTileContext` initialization, string-data extraction, `ResourceLayoutTable::LookupLayoutEntry` through `g_pEPFLib`, shared tile/frame rendering, and security-cookie support.
- Numeric conversions for field offsets were verified with MCP `int_convert`: `257 == 0x101`, `268 == 0x10c`, `272 == 0x110`, `276 == 0x114`, `280 == 0x118`, `284 == 0x11c`, `285 == 0x11d`, `288 == 0x120`, and `292 == 0x124` (Verified with int_convert.py).

## Source-Quality Field And Helper Names

| Offset / item | Source-facing name used for first draft | Evidence / caveat |
| --- | --- | --- |
| `+0x101` | `m_enabled` | Inherited visible/enabled byte; when false, class2 selects a configured disabled frame rather than drawing the class1 overlay. Exact inherited declaration spelling remains inferred. |
| `+0x10c` | `m_normalFrameId` | Constructor stores frame argument and paint uses it as the default frame when enabled with no state flags. |
| `+0x110` | `m_pressedFrameId` | Selected when state-11 visual byte `+0x11d` is set by UID0004EE's visual-state/timer path. |
| `+0x114` | `m_selectedFrameId` | Selected when UID0004EC's selected byte `+0x11c` is set and `+0x11d` is clear. |
| `+0x118` | `m_disabledFrameId` | Selected immediately when `+0x101` is false; this replaces class1's disabled overlay behavior for class2. |
| `+0x11c` | `m_selected` | UID0004EC accepted source-quality evidence resolves this byte as the `SetSelected(bool selected)` storage. |
| `+0x11d` | `m_visualStateFrameActive` | MCP `0x0054ba30` setter evidence sets this byte for state `0x0b` and clears it otherwise. UID0004EE and the complete class declaration use the same source-facing family name; historical `m_state11VisualActive` is superseded provenance only. |
| `+0x120` | `m_epfResourceName` | String field passed through `sub_584540` to `ResourceLayoutTable::LookupLayoutEntry`. |
| `+0x124` | `m_paletteResourceName` | String field passed through `sub_584540` to the shared frame-render helper. |
| `+0x44` | `m_bounds` | Inherited pane bounds passed to `RenderTileFrame`. |
| `0x0067a744` / `dword_67A744` | `g_pEPFLib` | Existing global/support docs identify the process-wide EPF/ImageLib singleton. |
| `0x00457a60` | `EPFTileContext::Init` / tile-context initialization | Existing EPFTileContext docs identify the 0x28-byte context initializer. |
| `0x004d02f0` | `ResourceLayoutTable::LookupLayoutEntry` | Existing ResourceLayout docs identify the EPF/EPD layout lookup that fills the tile context. |
| `0x004b9980` | `RenderTileFrame` | Accepted paint reports and render-support docs use this source-facing role for tile/frame drawing. |

## Negative Evidence And Rejected Alternatives

- No class1-style disabled overlay path exists in this body. MCP target disassembly/callee checks find no call to `GrafPort::SetDrawColor` at `0x004b9660`, no write to inherited draw-mode byte `+0x70`, and no Surface callback-table dispatch through `0x0069b3fc`. The disabled case selects `m_disabledFrameId` and then follows the normal lookup/render path.
- No direct code callers were found. The single xref is the data xref from class2 vtable slot `0x006223c0`, so dialog construction sites are consumers of the class rather than owners of this paint method.
- [UID:0000MP][PrettyButtonControlPane](by-file/PrettyButtonControlPane.md) remains the source-file route, not the direct owner. The direct semantic owner is [UID:0000AS][PrettyButtonControlPane2](by-class/PrettyButtonControlPane2.md) because the target is a class2 vtable method and consumes class2 fields.
- `ButtonControlPane`, ImageLib/ResourceLayoutTable, EPFTileContext, GrafPort/Surface, and CreateUser dialog contexts are dependencies or consumers. They should not own or emit this method.

## Ownership And C++ Policy

Owner remains [UID:0000AS][PrettyButtonControlPane2](by-class/PrettyButtonControlPane2.md), with source placement through [UID:0000MP][PrettyButtonControlPane](by-file/PrettyButtonControlPane.md) / `NexusTK/ui/controls/PrettyButtonControlPane.cpp`. This method is source-ready and emits through the class owner. The previous blank-C++ blocker was unresolved source-quality naming; B005's accepted report resolves it with constructor-backed frame/resource fields, UID0004EC/UID0004EE state-byte support, accepted helper names, and explicit negative evidence that class2 uses a configured disabled frame instead of the class1 disabled overlay.

The C++ block uses inferred source-facing member names rather than recovered original symbols. Exact R4 normalizes only `m_state11VisualActive -> m_visualStateFrameActive` to match the direct writer UID0004EE, class1 family convention, constructor R3, and complete R2 class declaration. Behavior, owner route, helper dependencies, and score remain unchanged.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Raised from 84 because the accepted B005 report resolves the emitter route, exact range/padding, vtable-only reachability, constructor-provided frame IDs, state-byte roles, EPF/PAL resource strings, disabled-frame behavior, helper names, rejected owner alternatives, and formal first-draft C++ for this exact method. |
| Confidence | 90 | Raised from 88 because live MCP session `9fb70afc` independently confirms range, xrefs, no direct callers, branch/resource/render behavior, constructor/setter support, and negative disabled-overlay evidence. It remains capped at 90 because exact original member spellings and final class declaration shape are inferred rather than recovered. |

## Open Questions Resolved

- Blank emitter status: resolved as stale. The target now has a valid class owner/emitter route through UID0000AS and report-backed first-draft C++.
- Frame/resource constants: resolved as constructor-provided member fields, not hard-coded unknown literals.
- Disabled behavior: resolved as configured disabled-frame selection through `+0x118`, not the class1 overlay path.
- State bytes: resolved enough for this target. `+0x11c` is the accepted selected byte from UID0004EC; `+0x11d` is the frame-active byte from UID0004EE's setter and is consistently named `m_visualStateFrameActive`. Exact original spelling remains inferred and caps confidence.

## Changes

- 2026-07-16 B003 UID0004EL support implementation:
  - Preserved `88/90`, owner/emitter/reconstructable metadata, exact range, `Nested:0`, disabled-frame behavior, branch order, resource strings, helper calls, vtable route, padding, negatives, and formal body.
  - Applied exact R4 field normalization `m_state11VisualActive -> m_visualStateFrameActive` in the formal, field table, source policy, and open-question resolution.
  - Retained the old spelling only as superseded search provenance; no behavior, score, route, signature, or source/compiler disposition changed.
