*** UID:0002LB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:150 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollVolumePane::SetHoverPart(ScrollVolumePart part)
{
    RectBounds rect;
    const ScrollVolumePart oldPart =
        static_cast<ScrollVolumePart>(m_hoverPart);

    if (oldPart == part)
        return;

    if (oldPart != kScrollVolumePartNone) {
        GetPartRect(oldPart, &rect);
        InvalidateRect(&rect);
    }

    if (part != kScrollVolumePartNone) {
        GetPartRect(part, &rect);
        InvalidateRect(&rect);
    }

    m_hoverPart = static_cast<signed char>(part);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained ScrollVolumePane SetHoverPart method invalidates old/new part rectangles and stores signed hover state at +0x103; direct static route remains absent but exact behavior, class ownership, source name, and formal C++ are resolved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

## 2026-07-26 Accepted Hover-Role And Routing Repair

- Renamed path/title/source from the historical/superseded storage-shaped `SetHighlightPart` label to current `SetHoverPart`; paint frame selection and homologous scroll controls identify `+0x103` as hover state.
- Direct semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), position 150; UID0001H2 is the non-emitting address index.
- Exact raw body remains `[0x00565170,0x005651e0)`, size `0x70`, bounded by five bytes of leading alignment and the following BeginInteraction body.
- Formal C++ now uses `ScrollVolumePart`, `kScrollVolumePartNone`, and signed-char storage rather than untyped char/-1 literals. It invalidates old and new part rectangles before storing the new hover part.
- Live negative evidence remains zero modeled function/xrefs/pointer routes. The exact coherent body, paint consumer, BeginInteraction use, and family naming justify defining it as a retained source method; do not treat route absence as a future-research excuse.
- Historical `m_highlightPart` / SetHighlightPart wording remains only as corrected history. Rejected alternatives include active-part state, focus selection, generic scrollbar ownership, aggregate emission, padding, and compiler glue.

# 0x00565170-0x005651e0 ScrollVolumePane SetHoverPart Raw

## Status

- Disposition: reconstructable NexusTK project code with function-table caveat.
- Entity kind: raw method-shaped helper.
- Owner class: [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md).
- Likely source module: [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md).
- Address index: [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md), non-emitting after exact-child split.
- Parent handling: direct class owner/emitter UID0000CO at position `150`; formal C++ is populated only on this exact child.
- Confidence: very strong for exact behavior, owner, field role, source-facing name, and source shape; static dispatch remains negative evidence.
- Rebuild handling: source-authored retained raw method for hover invalidation. Supervisor Gate 2B may define the exact IDA function despite the absent static route, with the negative route preserved as liveness context.

## Address Range

| Range | Role | Notes |
| --- | --- | --- |
| `0x00565010-0x0056516b` | previous modeled part-rectangle helper | `sub_565010`, used by this raw helper to compute old/new highlighted part rectangles. |
| `0x0056516b-0x00565170` | boundary gap | Alignment/padding between the modeled rectangle helper and this raw helper. |
| `0x00565170-0x005651e0` | this raw set-highlight helper | IDA does not model a function start, but raw disassembly shows a complete method-shaped helper ending with `retn 4`. |
| `0x005651e0-0x005652a0` | next raw begin-interaction helper | [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md) begins immediately after this helper. |

## Behavior

1. Reads the old highlighted part from `this+0x103`.
2. Compares the old part with the incoming stack byte parameter.
3. If the old part is not `0xff`, computes the old part rectangle through `0x00565010` and invalidates it through the pane virtual slot at `+0x20`.
4. If the new part is not `0xff`, computes the new part rectangle through `0x00565010` and invalidates it through the same pane virtual slot.
5. Stores the incoming part byte to `this+0x103`.
6. Returns with `retn 4`, matching a one-byte/one-argument helper after normal calling-convention widening.

## Touched State And Calls

| Item | Use |
| --- | --- |
| stack byte parameter | New highlighted slider part, with `0xff` meaning no highlighted part. |
| `this+0x103` | Highlighted part byte; old value controls stale invalidation, new value is stored at the end. |
| `0x00565010` | ScrollVolumePane part-rectangle helper for old/new invalidation rectangles. |
| vtable slot `+0x20` | Pane invalidation call used for each non-`0xff` part rectangle. |
| `0x005c772f` | Security-cookie epilogue helper in the raw helper tail. |

## Boundary Notes

IDA does not currently model `0x00565170` as a function start. Raw disassembly still shows a complete method-shaped prologue/body/security-cookie epilogue ending at `retn 4` before the next raw helper at `0x005651e0`.

## Evidence

- IDA MCP `lookup_funcs` reports `0x00565170` as `Not a function`.
- 2026-05-31 IDA MCP raw disassembly shows reads/writes of `+0x103`, calls to `0x00565010`, invalidation through vtable slot `+0x20`, and `retn 4` at `0x005651dd`.
- The helper matches the hover-state pattern used by sibling inventory and spell scrollbar controls, but this page records the `ScrollVolumePane` raw bytes independently.
- Historical/superseded Wave2 lead data named the helper `ScrollVolumePane::SetHighlightPart(char newPart)` and reported Ghidra size `112`, no callers, and callees `0x00565010` and `0x005c772f`. The address/callee observations remain historical support, but current source identity is `ScrollVolumePane::SetHoverPart(ScrollVolumePart part)` and Wave2 is not naming authority.
- 2026-06-03 restarted IDA MCP recheck confirms `ida_funcs.get_func` still returns no function at `0x00565170`, `0x005651dd`, or `0x005651e0`; the previous modeled function is `sub_565010` at `0x00565010-0x0056516b`, and the next modeled function is `sub_5652A0` at `0x005652a0-0x00565360`.
- The same recheck finds no xrefs to the raw start. The raw span reads old hover part `m_hoverPart` at `+0x103`, compares it with the stack byte parameter, invalidates the old non-`0xff` part through `sub_565010` plus the pane virtual slot `[vtable + 0x20]`, invalidates the new non-`0xff` part the same way, stores the new part to `+0x103`, and exits with `retn 4`.
- Boundary bytes show return/alignment before `0x00565170`, a complete raw body ending `... 8b e5 5d c2 04 00`, and the next raw helper prologue beginning immediately at `0x005651e0`. The absence of padding between helpers matches a compiler-shaped raw helper sequence in an IDA function-table gap.
- Historical/superseded 2026-06-15 C001 Wave2 `method` data kept the exported helper under `ScrollVolumePane` with then-name `SetHighlightPart(char newPart)` and `effective_included: true`. Current source identity is `SetHoverPart(ScrollVolumePart part)`; only the high-grade body and old/new invalidation behavior remain useful historical evidence.
- Historical/superseded 2026-06-15 C001 Wave2 `xrefs 0x00565010` data reported instruction-level calls from `0x0056519c` and `0x005651b8` inside this raw span to `GetPartRect`. Current IDA/manual evidence independently preserves those outgoing edges and the no-incoming-route caveat; Wave2 is not current authority.

## Reconstruction Notes

- Current direct owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md) at position `150`; [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is only the false/non-emitting address index.
- Current accepted source identity is `SetHoverPart(ScrollVolumePart part)`. The absent raw-start caller remains a liveness/confidence caveat, not a reason to defer or revert the source name.
- This helper is a raw function-shaped span, not padding and not part of `0x00565010`; preserve the `0x0056516b-0x00565170` gap and the immediate `0x005651e0` next-helper prologue as separate boundaries.
- Historical 2026-06-15 source-quality notes relied partly on Wave2 no-caller output. Current retained conclusion is narrower and evidence-backed: raw behavior, outgoing calls, owner, and boundaries are resolved; IDA still lacks a modeled raw-start function and incoming route. That caveat blocks an unsupervised IDA function claim, not current `SetHoverPart` source reconstruction.
- Historical 2026-06-18 B003 reanalysis used `ScrollVolumePane::SetHighlightPart(char part)` from a stale generated/source-map snapshot. Current accepted correction is `ScrollVolumePane::SetHoverPart(ScrollVolumePart part)`: the body compares `m_hoverPart` at `+0x103`, invalidates old/new part rectangles through `GetPartRect` and Pane vtable slot `+0x20`, and stores the new hover part. The zero VA/RVA/direct-call routes remain valid negative evidence; the old name/signature is superseded.

## Cross-References

- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md)
- [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)
- [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md)
- [UID:0001H3][0x00564e30-0x00565006.ScrollVolumePaneHitTestPart](by-memory/0x00564e30-0x00565006.ScrollVolumePaneHitTestPart.md)
- [UID:0002LC][0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw](by-memory/0x005651e0-0x0056529f.ScrollVolumePaneBeginInteractionRaw.md)
- [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md)
- [UID:0001YT][ScrollVolumePaneVtables](by-type/by-vtable/ScrollVolumePaneVtables.md)

## Changes

- 2026-05-31: Created the exact raw helper page, assigned validator UID `0002LB`, set completion/confidence to `70/78`, and marked it reconstructable.
  - Before: This range existed only as an inline projected row inside the aggregate `ScrollVolumePane` memory page.
  - After: It has its own exact range page recording behavior, boundary caveat, owner, and IDA raw-disassembly evidence; parent/code fields remained blank under the then-current staging policy, now superseded by the 2026-06-18 code-entry update below.
  - Evidence: IDA MCP reports no modeled function start at `0x00565170`, while raw disassembly shows a complete helper ending at `retn 4` before `0x005651e0`.
- 2026-06-03:
  - Before: raised completion/confidence from `70/78` to `74/82` after the restarted IDA MCP recheck, but `AUTOGEN_PARENT_UID` remained blank because the class-level autogen chain was not rooted yet.
  - Historical changed state: `AUTOGEN_PARENT_UID:0001H2`, position `110`, with blank reconstruction C++. Current correction routes this page directly to UID0000CO at position `150` with formal C++.
  - Evidence: the restarted IDA MCP recheck confirmed the raw byte span, old/new hover invalidation paths, outgoing helper calls, neighboring modeled functions, immediate next raw helper prologue, and no start xrefs. [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is now a false/non-emitting address index.
- 2026-06-07: Changed completion/confidence from `74/82` to `82/84`.
  - Before: The page had raw behavior and boundary evidence, but lacked an explicit neighbor/gap table, ordered old/new invalidation sequence, touched-state/call table, and reconstruction blockers.
  - After: The page records source-authored rebuild handling, exact part-rectangle/gap/raw/begin-interaction boundary chain, old/new highlighted-part invalidation steps, touched fields/calls, parent aggregate attachment, and final-C++ blockers.
  - Evidence: prior 2026-05-31 and 2026-06-03 IDA raw-disassembly/no-function-start checks already recorded in this page.
- 2026-06-15 C001 Goal 2 refresh: raised completion/confidence from `82/84` to `85/85`.
  - Historical evidence: Wave2 used the superseded `ScrollVolumePane::SetHighlightPart(char)` name and exposed instruction-level outgoing calls from `0x0056519c` and `0x005651b8`. Current correction is `SetHoverPart(ScrollVolumePart part)` with direct UID0000CO ownership; no incoming raw-start route remains only a confidence/IDA-prestate caveat.
- 2026-06-18 B003 source-quality reanalysis:
  - Historical changed state: `COMPLETION:87`, `CONFIDENCE:89`, with then-draft `ScrollVolumePane::SetHighlightPart(char)` C++ populated. Current formal C++ supersedes it with `ScrollVolumePane::SetHoverPart(ScrollVolumePart part)`.
  - Evidence: fresh PE route scan found zero absolute-VA literals, zero RVA literals, and zero direct branch/call targets to `0x00565170`; positive controls found expected direct calls to modeled ScrollVolumePane helpers. Current naming is accepted from hover semantics and homologous control vocabulary, while supervisor Gate 2B remains responsible for any IDA function creation.
