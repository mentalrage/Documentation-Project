*** UID:0003PK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BJ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RainingLayerPane::DrawOnTarget(
    GrafPort *target,
    const RectBounds *,
    const RectBounds *)
{
    if (g_activeMapPane->m_waitingForLocalPlayerStatus)
        return;

    const unsigned int rainColor = 143;
    target->SetDrawColor(rainColor);

    int i;
    for (i = 0; i < 18; ++i) {
        target->MoveTo(
            2 * (17 * s_targetRainStreaks[m_currentFrame][i].x / 15),
            2 * (15 * s_targetRainStreaks[m_currentFrame][i].y / 13));
        g_pfnDrawLineDelta(
            target,
            2 * (-17 * s_targetRainStreaks[m_currentFrame][i].length / 15),
            2 * (30 * s_targetRainStreaks[m_currentFrame][i].length / 13));
    }

    for (i = 0; i < 7; ++i) {
        g_pfnDrawPixel(
            target,
            2 * (17 * s_targetRainSmallSplashes[m_currentFrame][i].x / 15),
            2 * (15 * (s_targetRainSmallSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
        g_pfnDrawPixel(
            target,
            2 * (17 * (s_targetRainSmallSplashes[m_currentFrame][i].x + 2) / 15),
            2 * (15 * (s_targetRainSmallSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
    }

    for (i = 0; i < 7; ++i) {
        g_pfnDrawPixel(
            target,
            2 * (17 * s_targetRainLargeSplashes[m_currentFrame][i].x / 15),
            2 * (15 * s_targetRainLargeSplashes[m_currentFrame][i].y / 13),
            rainColor);
        g_pfnDrawPixel(
            target,
            2 * (17 * s_targetRainLargeSplashes[m_currentFrame][i].x / 15),
            2 * (15 * (s_targetRainLargeSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
        g_pfnDrawPixel(
            target,
            2 * (17 * (s_targetRainLargeSplashes[m_currentFrame][i].x + 4) / 15),
            2 * (15 * (s_targetRainLargeSplashes[m_currentFrame][i].y + 6) / 13),
            rainColor);
        g_pfnDrawPixel(
            target,
            2 * (17 * (s_targetRainLargeSplashes[m_currentFrame][i].x + 6) / 15),
            2 * (15 * (s_targetRainLargeSplashes[m_currentFrame][i].y + 4) / 13),
            rainColor);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Behavior-complete RainingLayerPane DrawOnTarget virtual override reached through Rain vtable slot 0x00631230; Layer configures target clipping before dispatch, both inherited RectBounds pointers are intentionally unused, and the method applies the active-MapPane readiness gate before color-143 rendering of exact 18/7/7 signed-short target-array loops with preserved integer operation order and no local fill, clip, blend, restore, random, or state update. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005c1810-0x005c1bc7 RainingLayerPane DrawRainOnTarget

## Status

- Disposition: reconstructable, behavior-complete `RainingLayerPane::DrawOnTarget` target-port override with emitted C++.
- Direct owner/emitter: [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md).
- Parent migration index after split: [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md).

## Evidence

- Live IDA MCP reports `sub_5C1810` at `0x005c1810`, size `0x3b7`, ending at `0x005c1bc7`.
- The rain vtable target-draw slot points to this method at `0x00631230`.
- The method uses [UID:0001OW][0x0067a764-0x0067a768.ActiveMapPaneSingleton](by-memory/0x0067a764-0x0067a768.ActiveMapPaneSingleton.md) / `g_activeMapPane` to check the same weather-render gate byte used by `OnPaint`.
- Target-port rain table xrefs are exclusive to this body: `0x006702c8 -> 0x005c1877`, `0x00670478 -> 0x005c1950/0x005c19bd`, and `0x006704d0 -> 0x005c1a3a/0x005c1aa5/0x005c1b10/0x005c1b7e`.
- The three exclusive objects are [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md)'s writable file-static `s_targetRainStreaks[3][24]`, `s_targetRainSmallSplashes[3][7]`, and `s_targetRainLargeSplashes[3][7]`. `RainStreak` is exactly three signed `short` fields (`x`, `y`, `length`); `RainSplash` is exactly two signed `short` fields (`x`, `y`).
- The method's table loads are signed (`movsx`, not `movzx`). It draws the first `18` streak records from each 24-record frame, leaving six explicit all-zero initializer records, and consumes all seven records in each small/large splash frame.
- No minimap-specific caller, string, field, vtable route, or independent data route supports the old label. The proven method receives a supplied target/GrafPort-like object, so `target-port` is the current source-quality description.
- These target-port objects are byte-identical to but physically distinct from the main-view family used by [UID:0003PJ][0x005c1460-0x005c180c.RainingLayerPaneOnPaint](by-memory/0x005c1460-0x005c180c.RainingLayerPaneOnPaint.md). Exact pairwise hashes and the compiler-alignment gaps are documented by [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md); this method does not consume or alias the main-view objects.
- Callees are the expected GrafPort/render dependencies: `0x004b9600`, `0x004b9660`, `0x0069b3e4`, and `0x0069b3e0`.
- Local PE scan finds the exact body once, at `0x005c1810`; the only VA dword hit is the expected vtable slot at `0x00631230`.
- The successor `0x005c1bc7-0x005c1bd0` is 9 bytes of `0xcc` padding before `SnowingLayerPane`.

## Behavior-Complete DrawOnTarget Closure

- Exact modeled function: `0x005c1810-0x005c1bc7`, size `0x3b7` / 951 bytes, 10 basic blocks, cyclomatic complexity 5, and `retn 0Ch`. Its 951-byte SHA256 is `13AF20BD86C583978C8B2EE099182C61F44C5783A14E8062A60E796558BFCE90`; first 16 bytes are `55 8B EC 51 A1 64 A7 67 00 56 8B F1 89 75 FC 80`, and last 16 bytes are `07 0F 8C 42 FE FF FF 5F 5E 8B E5 5D C2 0C 00`.
- Entry signature `55 8B EC 51 A1 64 A7 67 00 56` occurs once. The little-endian target VA occurs once, in Rain primary-vtable cell `0x00631230`; no target RVA hit or ordinary direct caller exists. This is expected virtual-only reachability, not dead-code evidence.
- The exact source contract is `void RainingLayerPane::DrawOnTarget(GrafPort *target, const RectBounds *sourceBounds, const RectBounds *targetBounds)`. `Pane::DrawOnTarget` at `0x0041d680` has that type, and `Layer::RenderRecursive` at `0x004f0ff0` constructs two `RectBounds`, pushes both plus the supplied `GrafPort`, and dispatches Pane vtable slot `+0x10` at `0x004f1250-0x004f125d` after configuring the target clip.
- This override intentionally ignores both inherited rectangle parameters. It performs no local clip operation because caller-side Layer rendering already configured clipping. The stable documentation filename `RainingLayerPaneDrawRainOnTarget` is historical/descriptive; source prose and C++ use the proven virtual-contract name `DrawOnTarget`.
- Entry directly dereferences `g_activeMapPane` and reads public byte `m_waitingForLocalPlayerStatus` at `+0x3f0`. A nonzero value returns before color or draw work. No defensive singleton null check exists and none may be added.
- After the gate, the method calls `target->SetDrawColor(143)`. Decimal `143` is hexadecimal `0x8f` (Verified with `tools/int_convert.py` using `python .\tools\int_convert.py 143 --single`). The same palette index is passed to each pixel callback.
- The streak loop consumes exactly the first 18 entries of `s_targetRainStreaks[m_currentFrame]`, leaving the six explicit zero initializer records in each 24-entry frame unused. For each streak it calls `target->MoveTo(2*(17*x/15), 2*(15*y/13))`, then `g_pfnDrawLineDelta(target, 2*(-17*length/15), 2*(30*length/13))`.
- The seven-entry small-splash loop draws exactly two pixels per record at independently scaled `(x,y+4)` and `(x+2,y+4)`. The seven-entry large-splash loop draws exactly four pixels per record at independently scaled `(x,y)`, `(x,y+4)`, `(x+4,y+6)`, and `(x+6,y+4)`. Totals are 18 line-delta calls and 42 pixel calls.
- `RainStreak` is exactly three signed `short` fields (`x`, `y`, `length`), `RainSplash` is exactly two signed `short` fields (`x`, `y`), and every field/frame load is signed (`movsx`, not `movzx`). The target arrays remain physically distinct source objects from byte-identical main-view arrays because addresses and exclusive consumers differ.
- Preserve every multiplication, division, sign, and truncation in observed order. Algebraic simplification, floating point, precomputed scale constants, or reordered division can change negative-coordinate truncation. Repeated `m_currentFrame` accesses around callbacks are retained rather than hoisted or cached.
- Complete negative behavior: no parameter read, background fill, local clip, blend change, color/clip restore, random call, frame advance, table write, object-state update, target/main-array alias, or array bounds check occurs.
- Adjacent boundaries remain exact: four `0xcc` bytes precede the function after [UID:0003PJ][0x005c1460-0x005c180c.RainingLayerPaneOnPaint](by-memory/0x005c1460-0x005c180c.RainingLayerPaneOnPaint.md), nine `0xcc` bytes occupy `0x005c1bc7-0x005c1bd0`, and `SnowingLayerPane` begins at `0x005c1bd0`.
- Source placement remains the current route through [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md) to [UID:0000P8][WeatherLayerPane](by-file/WeatherLayerPane.md), with declaration in canonical `WeatherLayerPane.h` and this qualified definition in generated `NexusTK/map/WeatherLayerPane.cpp`. A dedicated historical `RainingLayerPane.cpp` remains plausible but unproved and does not change current ownership/emission.

## Score Rationale

Completion is `92` because exact bytes, bounds, signature, unique vtable route, ABI, virtual caller, caller-managed clipping, full control flow, 18/7/7 iteration shape, signed data, callback and table identities, integer operation order, negative behavior, source placement, and destination-ready C++ are all documented. Confidence is `94` because live binary evidence, current UDTs, accepted Pane/Layer contracts, and support pages agree. Scores remain below `95` because original local lexical spellings and the historical one-file versus dedicated Rain source split lack original symbols/PDB/source-path proof, and no rebuilt binary/codegen comparison has established compiler identity.

## Cross-References

- [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md)
- [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md)
- [UID:0001ON][0x006702c8-0x00670784.RainCoordinateTables](by-memory/0x006702c8-0x00670784.RainCoordinateTables.md)
- [UID:0001OW][0x0067a764-0x0067a768.ActiveMapPaneSingleton](by-memory/0x0067a764-0x0067a768.ActiveMapPaneSingleton.md)
- [UID:0001WJ][WeatherLayerDataLayouts](by-type/by-struct/WeatherLayerDataLayouts.md)

## 2026-07-23 UID0003PJ Shared Rain Contract

- The strongest source-facing declaration is `void DrawOnTarget(GrafPort *target, const RectBounds *sourceBounds, const RectBounds *targetBounds)`. The receiver remains `RainingLayerPane`; target-port and rectangle arguments are distinct from the inherited main-view `OnPaint()` state.
- This method shares the exact MapPane weather gate with [UID:0003PJ][0x005c1460-0x005c180c.RainingLayerPaneOnPaint](by-memory/0x005c1460-0x005c180c.RainingLayerPaneOnPaint.md): `g_activeMapPane->m_waitingForLocalPlayerStatus` is the public `+0x3f0` byte, and nonzero suppresses weather drawing. Historical/inverted `m_hasRenderableMap` wording is rejected.
- The source-level render dependencies use the same resolved callback vocabulary: `g_pfnFillRect`, `g_pfnDrawLineDelta`, and `g_pfnDrawPixel`, with GrafPort receiver semantics. Final declarations come from canonical Surface.h; Surface.cpp owns the one definition set.
- Its data remain physically and semantically separate from UID0003PJ's main-view arrays. This target consumes the accepted target-port arrays `s_targetRainStreaks`, `s_targetRainSmallSplashes`, and `s_targetRainLargeSplashes`; UID0003PJ consumes `s_rainStreaks`, `s_rainSmallSplashes`, and `s_rainLargeSplashes`.
- Final WeatherLayerPane.h should declare this method with `OnPaint()` and the Rain constructor. Historical checkpoint: this page formerly remained `86/88` with blank formal C++ because the UID0003PJ pass did not reconstruct its sibling. The UID0003PK pass independently verified this method's exact bytes, virtual caller, ABI, clipping ownership, tables, callbacks, loops, and negative behavior; the page now emits its own behavior-complete formal rather than extrapolating from OnPaint.

## Changes

- 2026-07-24 B005 UID0003PK behavior-complete implementation callback:
  - Changed `86/88 -> 92/94`; owner/emitter remains [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md), reconstructable remains true, and filename/path remains stable.
  - Added exact 951-byte hash/signature/bounds, virtual-only reachability, base/caller-proven `DrawOnTarget` signature, caller-managed clipping, intentional bounds nonuse, active-map gate, palette conversion, exact 18/7/7 render loops, signed data, operation-order constraints, complete negative evidence, current source-family placement, and behavior-complete formal C++.
  - Historicalized earlier minimap, `DrawRainOnTarget` source-name, integer-parameter, and blank-formal assumptions without deleting their provenance.
- 2026-07-22 B005 UID0001ON source-closure callback:
  - Metadata remains `86/88`; owner/emitter remains [UID:0000BJ][RainingLayerPane](by-class/RainingLayerPane.md).
  - Replaced the unsupported minimap label with target-port evidence and added exact `s_targetRainStreaks[3][24]`, `s_targetRainSmallSplashes[3][7]`, and `s_targetRainLargeSplashes[3][7]` contracts, signed `RainStreak`/`RainSplash` fields, 18-plus-6 streak shape, seven-entry splash shape, and physical-duplication boundary.
  - Removed the superseded minimap/table-name/type/source-placement blockers. Final method C++ remains blank for the narrower GrafPort/render-helper, field, callback-declaration, and full-control-flow blockers above.
- 2026-06-16 B001 child-split implementation:
  - Historical checkpoint: created this exact child page from the former [UID:0001NZ][0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes](by-memory/0x005c12a0-0x005c1bc7.WeatherAndRainingLayerPanes.md) aggregate after B001 verified exact function boundaries, vtable-slot evidence, active-map-pane gating, exclusive then-labeled target/minimap rain table refs, unique PE body evidence, and padding.
  - Historical checkpoint: final C++ was blank because GrafPort/helper names, field names, table names, and the one-file versus per-layer weather source split were unresolved. The 2026-07-22 callback later resolved the minimap/table/type/source-route portion while retaining the method-level blockers.
