*** UID:0002IA | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void HierList::InsertLast(const void *parentPayload, const void *payload)
{
    if (parentPayload == NULL) {
        HierListNode *lastNode = static_cast<HierListNode *>(List::GetElementAt(m_count - 1));
        InsertBefore(NULL, lastNode->payload, payload);
        return;
    }

    HierListNode *parentNode = FindNodeByPayload(parentPayload, NULL, NULL);
    HierList *childList = parentNode->childList;
    if (childList != NULL && childList->m_count != 0) {
        HierListNode *lastChild =
            static_cast<HierListNode *>(childList->List::GetElementAt(childList->m_count - 1));
        InsertBefore(parentPayload, lastChild->payload, payload);
    } else {
        InsertBefore(parentPayload, NULL, payload);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: IDA-unmodeled but executable `HierList::InsertLast(parentPayload,payload)` wrapper; chooses the root or parent child-list tail payload, falls back to null reference for empty child lists, and forwards to `InsertBefore` with formal first-draft C++ on this page. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ce7e0-0x004ce839 HierListTailInsertWrapper

## Status

- Entity kind: class method wrapper
- Owner: [UID:000065][HierList](by-class/HierList.md)
- Likely source file: [UID:0000JV][HierList](by-file/HierList.md)
- Parent aggregate: [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md)
- Rebuild handling: source-authored, reconstructable; formal first-draft C++ is attached as `HierList::InsertLast(const void *parentPayload, const void *payload)`.
- Autogen status: attached to [UID:000065][HierList](by-class/HierList.md) for class-method tracking. The raw start is still not an IDA function object, but the source-facing method form is resolved strongly enough for this exact child page to emit first-draft C++.

## Behavior

`HierList::InsertLast(parentPayload, payload)` is a two-argument wrapper around [UID:0002IC][0x004ce860-0x004ce9ad.HierListInsertBefore](by-memory/0x004ce860-0x004ce9ad.HierListInsertBefore.md). It inserts the caller payload at the end of either the root list or a supplied parent's child list by computing the current tail payload and passing that payload as the insert reference.

When `parentPayload` is null, it gets the final root row through the base [UID:0003JW][0x004f32b0-0x004f32da.ListGetElementAt](by-memory/0x004f32b0-0x004f32da.ListGetElementAt.md) accessor, adds `0x08` to use the row payload, and forwards `NULL`, that tail payload, and the caller payload to `InsertBefore`.

When `parentPayload` is supplied, it resolves the parent row through [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md), now source-facing `FindNodeByPayload(parentPayload, NULL, NULL)`. If the parent row has a non-empty `childList`, this wrapper uses that child list's tail payload as the reference. If the parent has no child list or the child list is empty, it forwards a null reference payload so the insert core handles the default placement.

## Control Flow Shape

| Case | Observed behavior | Insert-before call |
| --- | --- | --- |
| Null parent argument | Uses the root list tail payload as the reference by calling the base list element accessor at `0x004f32b0` with `m_count - 1`, then adding `0x08`. | Calls [UID:0002IC][0x004ce860-0x004ce9ad.HierListInsertBefore](by-memory/0x004ce860-0x004ce9ad.HierListInsertBefore.md) at `0x004ce802` as `InsertBefore(NULL, lastNode->payload, payload)`. |
| Non-null parent and found parent node with a non-empty child list | Resolves the parent node through [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md), reads `parentNode->childList`, tests `childList->m_count`, then selects the current child-list tail payload. | Calls the same insert-before core at `0x004ce802` after branching back through the shared tail lookup shape; source form is `InsertBefore(parentPayload, lastChild->payload, payload)`. |
| Non-null parent but missing or empty child list | Falls back to a null reference payload so the core insert routine can handle default placement. | Calls the same insert-before core at `0x004ce82e` as `InsertBefore(parentPayload, NULL, payload)`. |

## Evidence

- IDA did not model this body as a function, but IDA MCP `py_eval` decoded executable code from `0x004ce7e0-0x004ce839`.
- Live IDA MCP on 2026-06-03 confirms there is still no IDA function object at `0x004ce7e0`, no direct xrefs to the raw start, and the decoded body returns with `retn 8`.
- The body calls the base list element accessor at `0x004f32b0`, the recursive node lookup at `0x004cec30`, and `0x004ce860` at call sites `0x004ce802` and `0x004ce82e`.
- The null-parent path reads root count from `this+0x0c`, decrements it, uses `sub_4F32B0(this, count - 1) + 8` as the sibling payload, and forwards the caller payload to the insert-before core. The non-null parent path calls `FindNodeByData(parent, &owner, &index)`, checks node `+0x04` for a child list, uses that child list tail when non-empty, and otherwise forwards a null sibling.
- The preceding span `0x004ce7d1-0x004ce7e0` and following span `0x004ce839-0x004ce840` are `0xcc` alignment padding.
- [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md) includes this raw wrapper in the exact child inventory and records the surrounding internal padding.
- [UID:000065][HierList](by-class/HierList.md) and [UID:0000JV][HierList](by-file/HierList.md) both identify the wrapper as a HierList-local method missing from active generated output.
- 2026-06-14 live IDA MCP session `b001_0002bd` reconfirmed active `NexusTK.exe.i64` with auto-analysis and Hex-Rays ready.
- 2026-06-14 `lookup_funcs` reconfirmed that `0x004ce7e0`, `0x004ce839`, `0x004ce840`, and `0x004ce854` are not IDA function starts, while the surrounding modeled functions remain `sub_4CE760` at `0x004ce760` size `0x71` / 113 bytes and `sub_4CE860` at `0x004ce860` size `0x14d` / 333 bytes (verified with `int_convert.py`).
- 2026-06-14 `xrefs_to` reconfirmed zero direct xrefs to raw start `0x004ce7e0`; the insert core at `0x004ce860` still has raw-wrapper calls from `0x004ce802` and `0x004ce82e`, sibling wrapper call `0x004ce84b`, and Layer call `0x004f0a3b`.
- 2026-06-14 `xrefs_to 0x004cec30` reconfirmed this wrapper's lookup call at `0x004ce812` plus modeled calls from `InsertBefore`, `InsertAfter`, `RemoveNode`, and the recursive lookup body itself.
- 2026-06-14 `get_bytes 0x004ce7d0` reconfirmed the prior destructor `ret` at `0x004ce7d0`, `0xcc` padding through `0x004ce7df`, the wrapper prologue at `0x004ce7e0`, both calls into `0x004ce860`, `retn 8` at the wrapper tail, and `0xcc` padding from `0x004ce839` to the next raw wrapper.
- 2026-06-14 `analyze_component` over the HierList cluster reconfirmed the class-local component, shared HierList vtable `0x0061b344`, `InsertBefore` size `0x14d` / 333 bytes, recursive lookup size `0xdf` / 223 bytes, and internal call graph edges into `FindNodeByData` (verified with `int_convert.py` for the cited sizes).
- 2026-06-26 B003 implementation callback rechecked live IDA MCP before editing. Endpoint `http://127.0.0.1:13337/mcp` initialized session `cbb3bd71-c728-4640-983e-ef6623343878`; `idb_list` showed active IDB session `80de0a67` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned `status:"ok"`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- 2026-06-26 B003 implementation callback checked the live MCP schema before evidence use. Relevant call shapes used for this accepted source-form decision were `lookup_funcs(queries,database)`, `xrefs_to(addrs,database)`, `insn_query(queries,database)`, `get_bytes(regions,database)`, `decompile(addr,database)`, `disasm(addr,database)`, `analyze_function(addr,database)`, `analyze_component(addrs,database)`, `make_signature_for_range(start,end,database)`, and `int_convert(inputs,database)`.
- 2026-06-26 accepted report evidence resolved the source form: `lookup_funcs` still reports `0x004ce7e0`, `0x004ce838`, `0x004ce839`, `0x004ce840`, and `0x004ce854` as not functions; `decompile 0x004ce7e0` fails because there is no IDA function object; `disasm 0x004ce7e0` still decodes the range as executable `<no function>` code.
- 2026-06-26 accepted report evidence: `xrefs_to 0x004ce7e0`, `0x004ce839`, `0x004ce840`, and `0x004ce854` report zero direct xrefs. `xrefs_to 0x004ce860` reports four code refs: this wrapper's `0x004ce802` and `0x004ce82e`, sibling wrapper `0x004ce84b`, and Layer `0x004f0a3b`.
- 2026-06-26 accepted report evidence: `xrefs_to 0x004cec30` includes this wrapper's raw parent-lookup call at `0x004ce812`, three insert-before refs, three insert-after refs, the remove helper ref, and self-recursion. `xrefs_to 0x004f32b0` includes this wrapper's raw tail-lookup call at `0x004ce7f3`.
- 2026-06-26 accepted report evidence: `get_bytes 0x004ce7d0 size 16` shows the prior destructor `ret` byte followed by `0xcc` padding through `0x004ce7df`; `get_bytes 0x004ce7e0 size 89` shows this wrapper from prologue `55 8b ec 56 57` through tail `5f 5e 5d c2 08 00`; `get_bytes 0x004ce839 size 7` and `0x004ce854 size 12` show `0xcc` padding around the sibling wrapper and insert core.
- 2026-06-26 accepted report evidence: `make_signature_for_range 0x004ce7e0-0x004ce839` returns a unique wildcarded IDA signature for this raw body. `analyze_component` over modeled HierList methods keeps the raw wrapper's callees in the class-local island through `InsertBefore`, `FindNodeByPayload`, and base `List::GetElementAt` relationships.

## Source Shape And Rejected Alternatives

Best source-facing form is a source-authored two-argument wrapper method named `HierList::InsertLast(const void *parentPayload, const void *payload)`. It computes the last existing payload in the root list or supplied parent's child list, then delegates insertion to the three-argument insert core.

The insertion-family relationship is now:

| UID | Source-facing role |
| --- | --- |
| [UID:0002IB][0x004ce840-0x004ce854.HierListDefaultInsertWrapper](by-memory/0x004ce840-0x004ce854.HierListDefaultInsertWrapper.md) | accepted `InsertFirst(parentPayload, payload)` / head or null-reference wrapper with formal first-draft C++ on its exact child page. |
| [UID:0002IA][0x004ce7e0-0x004ce839.HierListTailInsertWrapper](by-memory/0x004ce7e0-0x004ce839.HierListTailInsertWrapper.md) | accepted `InsertLast(parentPayload, payload)` / tail wrapper with formal first-draft C++ on this page. |
| [UID:0002IC][0x004ce860-0x004ce9ad.HierListInsertBefore](by-memory/0x004ce860-0x004ce9ad.HierListInsertBefore.md) | `InsertBefore(parentPayload, referencePayload, payload)` core. |
| [UID:0002ID][0x004ce9b0-0x004ceafc.HierListInsertAfter](by-memory/0x004ce9b0-0x004ceafc.HierListInsertAfter.md) | `InsertAfter(parentPayload, referencePayload, payload)` core. |

Rejected alternatives:

1. Leave formal C++ blank because IDA cannot decompile `0x004ce7e0`: rejected because the raw range is fully decoded by `insn_query`/`disasm`, has exact bytes/padding, known callees, a unique signature, and a complete source-shaped wrapper body.
2. Treat the range as padding or a split/index child with no source: rejected because it has a normal thiscall prologue, two control paths, calls into `List`, `FindNodeByPayload`, and `InsertBefore`, and returns with `retn 8`.
3. Move the method to `Layer`: rejected because Layer has no direct xref to this raw wrapper and the body manipulates `HierList` rows/child lists through [UID:000065][HierList](by-class/HierList.md) state.
4. Move the method to `List`: rejected because `List` supplies only the raw element accessor; parent payloads, child `HierList *`, and row `+0x08` payload references are HierList-specific.
5. Rename the by-memory file to `HierListInsertLast`: rejected to avoid path churn; `HierListTailInsertWrapper` remains the stable by-memory page/search label while `InsertLast` is the emitted source spelling.
6. Use `InsertAtTailOfParent` as the emitted method name: rejected as primary spelling because it is accurate but reads like a documentation phrase. It can remain a descriptive alias.
7. Use `Append` as the emitted method name: rejected because [UID:0002IH][0x004cebb0-0x004cebb9.HierListAppend](by-memory/0x004cebb0-0x004cebb9.HierListAppend.md) already owns the vtable/base append wrapper.

## Confidence And Caveats

This is source-looking class-local wrapper code even though IDA lacks a function object for it. Current live IDA confirms its exact control-flow, call targets, class-local component, padding, and direct relationship to the modeled `HierList::InsertBefore` core. The source-facing method form is now resolved as `HierList::InsertLast(parentPayload, payload)`, with the remaining uncertainty limited to original symbol spelling rather than behavior or source placement.

## Score Rationale

Raised from `85/89` to `90/91` because the accepted 2026-06-26 B003 source-quality pass resolves the previous source-name/source-form/C++ blocker. The page now contains formal `HierList::InsertLast` C++, current MCP provenance, raw no-function/decompile-failure evidence, exact instruction/byte/padding evidence, callee xrefs, unique range signature, modeled callee/component context, insertion-family source shape, and rejected alternatives. Confidence remains below final-audit level because `InsertLast` is inferred from source shape and local naming symmetry rather than recovered original symbols, and the raw entry still lacks an IDA function object/direct xrefs.

## Changes

- 2026-06-26 B015 accepted source-quality support update:
  - Updated the insertion-family relationship so [UID:0002IB][0x004ce840-0x004ce854.HierListDefaultInsertWrapper](by-memory/0x004ce840-0x004ce854.HierListDefaultInsertWrapper.md) is now accepted as formal exact-child `HierList::InsertFirst` C++ rather than a likely/no-formal-C++ sibling note.
  - Preserved this page's existing `HierList::InsertLast` formal C++; no code changes were made here.

- 2026-06-26 B003 accepted source-quality implementation:
  - What existed before: scored `85/89`, source-authored and class-owned but formal C++ remained blank because the source name/form blocker was still open.
  - Changed to: scored `90/91`, inserted formal `void HierList::InsertLast(const void *parentPayload, const void *payload)` C++ for the exact raw wrapper range, and replaced the stale blank-C++/source-name caveat with the resolved wrapper-method source shape.
  - Summary/evidence: live MCP session `80de0a67` remained healthy; accepted report evidence confirms no IDA function object or direct xrefs at `0x004ce7e0`, exact bytes/padding for the body and neighboring ranges, raw calls to `List::GetElementAt`, `FindNodeByPayload`, and `InsertBefore`, failed decompilation at the raw start, `<no function>` disassembly decode, a unique range signature, and modeled HierList component context. Rejected alternatives are preserved above.
- 2026-06-02 autogen/source evidence update:
  - What existed before: the page was scored `60/76`, marked reconstructable, but left unattached and had only a short behavior/evidence summary.
  - Changed to: scored `70/84`, attached under [UID:0000JV][HierList](by-file/HierList.md), and expanded the branch/callee evidence.
  - Summary/evidence: the parent aggregate, [UID:000065][HierList](by-class/HierList.md), and [UID:0000JV][HierList](by-file/HierList.md) already document this as a HierList-local raw wrapper. C++ remains blank because the exact source-level overload/helper shape is not final-quality and IDA still lacks a function object at `0x004ce7e0`.
- 2026-06-03 live IDA recheck:
  - What existed before: scored `70/84` with stored raw-wrapper evidence but no current live verification after the IDA MCP restart.
  - Changed to: scored `76/88` with exact live instruction/call evidence and branch-shape notes.
  - Summary/evidence: IDA MCP decoded `0x004ce7e0-0x004ce839`, confirmed no function object or direct start xrefs, confirmed calls at `0x004ce7f3`, `0x004ce802`, `0x004ce812`, and `0x004ce82e`, and verified the root-tail/parent-child-tail fallback logic. Final C++ remains blank.
- 2026-06-14 Goal 2 low-completion pass:
  - What existed before: scored `76/88`, with class-method prose but file-level owner/emitter metadata.
  - Changed to: scored `85/89`, set direct owner/emitter routing to [UID:000065][HierList](by-class/HierList.md), and added current IDA MCP boundary/xref/byte/component evidence.
  - Summary/evidence: live IDA confirmed the raw start remains unmodeled and unreferenced directly, the calls into `0x004f32b0`, `0x004cec30`, and `0x004ce860` remain in-place, the surrounding padding boundaries are byte-confirmed, and the direct class parent already clears the assignment gate at `86/90`. Final C++ remains blank pending source-form and overload-name proof.
