*** UID:0001GN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:180 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollSpellInventoryPane::UpdateFromDrag(int localY, int localX)
{
    Point cursor;
    Point origin;
    RectBounds thumbRect;
    RectBounds leadingRect;
    RectBounds trailingRect;
    const short oldPosition = m_scrollPosition;
    short newPosition;
    GetPartRect(kScrollSpellInventoryPartThumb, &thumbRect);

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.y != -1000 || origin.x != -1000) {
        leadingRect = m_visibleBounds;
        if (m_orientation != 0) {
            leadingRect.right = leadingRect.left;
        } else {
            leadingRect.bottom = leadingRect.top;
        }
    }

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.y != -1000 || origin.x != -1000) {
        trailingRect = m_visibleBounds;
        if (m_orientation != 0) {
            trailingRect.left = trailingRect.right;
        } else {
            trailingRect.top = trailingRect.bottom;
        }
    }

    if (m_orientation != 0) {
        int thumbStart = localX - m_thumbDragOffset.x + 1;
        if (thumbStart < leadingRect.right) {
            thumbStart = leadingRect.right;
        }
        const int maxThumbStart =
            trailingRect.left + thumbRect.left - thumbRect.right;
        if (thumbStart > maxThumbStart) {
            thumbStart = maxThumbStart;
        }

        const int trackSpan =
            trailingRect.left + thumbRect.left -
            thumbRect.right - leadingRect.right;
        newPosition = static_cast<short>(
            (m_scrollRange * (thumbStart - leadingRect.right)) / trackSpan);
    } else {
        int thumbStart = localY - m_thumbDragOffset.y + 1;
        if (thumbStart < leadingRect.bottom) {
            thumbStart = leadingRect.bottom;
        }
        const int maxThumbStart =
            trailingRect.top + thumbRect.top - thumbRect.bottom;
        if (thumbStart > maxThumbStart) {
            thumbStart = maxThumbStart;
        }

        const int trackSpan =
            trailingRect.top + thumbRect.top -
            thumbRect.bottom - leadingRect.bottom;
        newPosition = static_cast<short>(
            ((m_scrollRange + 1) *
             (thumbStart - leadingRect.bottom)) / trackSpan);

        if (newPosition > m_scrollRange) {
            newPosition = m_scrollRange;
        }
    }

    if (oldPosition != newPosition) {
        static_cast<NewSpellInventoryPane *>(GetParentPane())
            ->OnScrollPositionChanged(m_orientation,
                                      oldPosition,
                                      newPosition);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready ScrollSpellInventoryPane::UpdateFromDrag(localY, localX) with exact 397-byte body/hash, two input-bridge callers, Y/X Point-order drag offsets, thumb/track geometry, orientation-specific formulas, parent-pane callback semantics, standalone ScrollSpellInventoryPane.cpp route, and formal first-draft C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00560710-0x0056089d ScrollSpellInventoryPane UpdateFromDrag
## UID0000NJ Applied Exact-Child Closure - 2026-08-16

This exact child now emits at class source position 180 after the seventeen preceding bounded methods. The formal body uses inherited `m_visibleBounds` at binary offset `+0x44`; the former `m_bounds` spelling was a generated-source defect and did not exist in the authoritative declarations. The two calls from `UpdateDragScroll` at `0x005606ab` and `0x005606d4`, 397-byte range, SHA256 `09733ac52ea007586c04d5429f87094fa3f0d75481f0e801beabc55dda5320eb`, orientation-specific track formulas, Y/X `Point` order, and NewSpell owner callback remain unchanged. Its H channel stays blank because UID0000CN owns the private declaration.

## UID0000LU Header Route Cross-Link - 2026-08-15

This existing child continues to emit through UID0000CN; the owner declaration now resides in `ScrollSpellInventoryPane.h`, which NewSpell includes normally. Drag behavior and exact body evidence are unchanged.

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: method
- Owner class: [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md)
- Source method: `void ScrollSpellInventoryPane::UpdateFromDrag(int localY, int localX)`.
- Source module: standalone [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md), projected as `NexusTK/ui/inventory/ScrollSpellInventoryPane.cpp`.
- Confidence: very strong for behavior, ABI, field widths, formulas, caller/callee set, callback semantics, direct class ownership, and exact range; strong for the inferred source spelling and dedicated file placement.
- Owner/emitter decision: direct owner and emitter [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md), with source position `180` after UID00053O and before [UID:0001GO][0x005608a0-0x005608fc.ScrollSpellInventoryPaneResetScrollStateRaw](by-memory/0x005608a0-0x005608fc.ScrollSpellInventoryPaneResetScrollStateRaw.md) at position `190`.
- Standalone placement is selected over the historical private fold into `NewSpellInventoryPane.cpp`. Constructor-only feature containment does not outweigh this class's independent fields, vtables, contiguous implementation island, layout page, and established file route.

## Behavior

This method recomputes the spell-inventory scrollbar position from pointer coordinates and scrollbar geometry. It reads `short m_scrollPosition` at `+0xfe`, computes a new short position from track geometry, and notifies `NewSpellInventoryPane` only when the old and new values differ. It deliberately does not assign `m_scrollPosition` directly; the owner callback synchronizes the child through `SetScrollPosition`, rebuilds visible rows, and invalidates the owner.

The owner callback call is `NewSpellInventoryPane::OnScrollPositionChanged(unsigned char orientation, short oldPosition, short newPosition)` at `0x0057d360`, reached through inherited `Pane::GetParentPane()` at primary vtable slot `+0x1c`. All three arguments remain part of the source contract even though the callback body does not consume `orientation`.

The byte `m_orientation` at `+0xfc` selects the horizontal and vertical paths. Project `Point` order and target argument use establish `+0x108` as `m_thumbDragOffset.y` paired with `localY`, and `+0x10c` as `.x` paired with `localX`. This corrects the reversed mapping in the earlier documentation. The method requests part code `2` (`kScrollSpellInventoryPartThumb`) through `GetPartRect` at `0x00560290`, samples the cursor through `g_pEventMan->GetCursorPosition` at `0x004a9090`, and samples pane origin through `Pane::GetScreenOffset` at `0x005447c0` twice in the original call order.

Horizontal (`m_orientation != 0`) source formula:

`newPosition = (m_scrollRange * (clamp(localX - m_thumbDragOffset.x + 1, leadingRect.right, trailingRect.left + thumbRect.left - thumbRect.right) - leadingRect.right)) / (trailingRect.left + thumbRect.left - thumbRect.right - leadingRect.right)`

Vertical (`m_orientation == 0`) source formula:

`newPosition = ((m_scrollRange + 1) * (clamp(localY - m_thumbDragOffset.y + 1, leadingRect.bottom, trailingRect.top + thumbRect.top - thumbRect.bottom) - leadingRect.bottom)) / (trailingRect.top + thumbRect.top - thumbRect.bottom - leadingRect.bottom)`, followed by the observed upper cap to `m_scrollRange`.

The horizontal path has no corresponding final post-scale cap. Neither path has a zero-track guard, generic lower clamp beyond the observed thumb-start clamp, null-parent guard, or default initialization of the two edge rectangles. Adding any of those would change the original behavior.

The sibling input bridge at `0x00560650` is the only current caller. It first converts global pointer coordinates to local coordinates, then calls this helper directly when active part `+0x104` is `2`, or after a hit-test through `0x0055ff70` when the current part matches the active part. Afterward it invalidates and clears stale highlighted part `+0x103` when needed.

## Evidence

- IDA MCP `lookup_funcs` reports `0x00560710` as size `0x18d`, giving end-exclusive range `0x0056089d`.
- IDA MCP `decompile 0x00560710` shows calls to `GetPartRect` at `0x00560290`, screen-offset helper `0x005447c0`, EPF/event offset helper `0x004a9090`, geometry-based position math, and callback target `0x0057d360`.
- IDA MCP `xrefs_to 0x00560710` reports call sites at `0x005606ab` and `0x005606d4` inside the neighboring scrollbar input helper at `0x00560650`.
- 2026-05-31 IDA MCP decompile confirms the helper reads old position from `+0xfe`, orientation from `+0xfc`, range from `+0x100`, drag point from `+0x108/+0x10c`, and calls `0x0057d360` with `(owner, orientation, oldPosition, newPosition)` only when the computed position changes.
- 2026-06-07 live IDA MCP reconfirms `sub_560710` as `0x00560710-0x0056089d`; `lookup_funcs 0x0056089d` reports no function and the bytes after the return are three `0xcc` bytes before the raw reset helper at `0x005608a0`.
- The 2026-06-07 decompile shows the exact call sequence: `0x00560290` at `0x00560735`, screen/client offset helpers `0x004a9090`/`0x005447c0` at `0x00560744`/`0x0056074f` and again at `0x00560791`/`0x0056079c`, owner virtual lookup through slot `+0x1c` at `0x00560876`, callback `0x0057d360` at `0x00560885`, and security-cookie check at `0x00560892`.
- The same recheck decompiled `0x0057d360`: when the old and new positions differ, the owner stores the new byte at owner offset `+0xf8`, calls scrollbar position helper `0x0055f5c0` on `this[64]`, refreshes visible spell rows through `0x0057d200`, and invalidates through the owner's virtual slot `+0x20`.
- Current xrefs to callback `0x0057d360` are this helper at `0x00560885` plus two calls inside `NewSpellInventoryPane` mouse handling at `0x0057da5e` and `0x0057da79`, which supports the spell-inventory ownership but keeps the exact public method name provisional.
- 2026-06-11 Batch 193 live IDA MCP recheck confirms `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`; `lookup_funcs` still reports `sub_560710` as size `0x18d` and no function at `0x0056089d`.
- 2026-06-11 `callers`/`xrefs_to` report exactly two code references to this helper, `0x005606ab` and `0x005606d4`, both inside the neighboring `0x00560650` input bridge.
- 2026-06-11 `callees` reports the expected local dependency set: `0x00560290` part-rect helper, `0x004a9090`/`0x005447c0` coordinate helpers, owner callback `0x0057d360`, and `@__security_check_cookie@4`.
- B001 live MCP session `9b0396a3` reconfirmed `sub_560710` as 130 instructions, 19 basic blocks, cyclomatic complexity 10, size `0x18d`, and `retn 8` at `0x0056089a`.
- The exact 397 target bytes have SHA256 `09733AC52EA007586C04D5429F87094FA3F0D75481F0E801BEABC55DDA5320EB`; the first 16 bytes are `55 8b ec 83 ec 48 a1 24 2f 67 00 33 c5 89 45 fc`, and the last 16 are `5f 5e 33 cd 5b e8 98 6e 06 00 8b e5 5d c2 08 00`.
- The exact end-exclusive boundary is `0x0056089d`. Bytes `0x0056089d-0x0056089f` are `cc cc cc`, followed by the raw reset child at `0x005608a0`; no function is modeled at the end address.
- Exact target calls are `0x00560735 -> GetPartRect`, `0x00560744/0x00560791 -> GetCursorPosition`, `0x0056074f/0x0056079c -> GetScreenOffset`, `0x00560876 -> GetParentPane`, `0x00560885 -> OnScrollPositionChanged`, and `0x00560892 -> @__security_check_cookie@4`.
- Callback xrefs are this method at `0x00560885` and NewSpell mouse handling at `0x0057da5e` and `0x0057da79`. Its `0x38`-byte body updates the owner scroll byte, calls child `SetScrollPosition`, rebuilds rows through `0x0057d200`, and invalidates only when the positions differ.
- Current parent route is fully assigned: [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) is `89/91`, and [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md) is `90/92`.

## Negative Evidence And Rejected Alternatives

- IDA's generated `int` return and `sub_560710` label are binary-analysis artifacts, not source declarations; stack cleanup and lack of a return contract support `void`.
- The sole NewSpell constructor consumer establishes feature coupling but does not make this a NewSpell member or prove physical folding into `NewSpellInventoryPane.cpp`.
- Sibling scrollbar methods corroborate naming and algorithm family but do not establish this method's signature or coordinate mapping; target argument use and current `Point {y,x}` layout do.
- No third target caller, meaningful return contract, zero-span branch, null-owner branch, default edge-rectangle initialization, generic lower clamp, or direct `m_scrollPosition` assignment exists.
- Both cursor calls are retained despite their apparently unused output because they are observable calls and may have side effects.
- A generic `ScrollBar.cpp` owner is rejected: the accessed state, helpers, vtables, and established emitter chain are specific to `ScrollSpellInventoryPane`.

## Source Placement And Naming Decision

- Selected source placement: `NexusTK/ui/inventory/ScrollSpellInventoryPane.cpp`, through [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) and [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md).
- Selected source-facing names: `UpdateFromDrag`, `m_orientation`, `m_scrollPosition`, `m_scrollRange`, `m_thumbDragOffset`, `GetPartRect`, `kScrollSpellInventoryPartThumb`, `GetScreenOffset`, `GetParentPane`, and `OnScrollPositionChanged`.
- `UpdatePositionFromCursor`, `UpdateScrollPosition`, a NewSpell member, a generic ScrollBar owner, and a physical fold into `NewSpellInventoryPane.cpp` are retained as rejected alternatives. Exact stripped spellings and original physical filename remain inference caps, not implementation blockers.
- The `NewSpellInventoryPane` declaration exposing `OnScrollPositionChanged(unsigned char, short, short)` must be visible through the normal NewSpell class/header route; this source must not define a reduced duplicate class.

## Historical Superseded Assumptions

- Earlier text paired vertical/localY with `+0x10c` and horizontal/localX with `+0x108`. Target argument consumption plus project `Point` ordering prove the opposite: `+0x108 = y`, `+0x10c = x`.
- Earlier text treated the class/file parents as below the attachment gate and left standalone-versus-folded placement open. Current metadata is `89/91` on the class and `90/92` on the file, direct ownership is UID0000CN, and standalone UID0000NJ is the selected source route.
- Earlier text kept the public method spelling provisional. `UpdateFromDrag` is now the best-supported source-facing name; stripped symbols still cap final-audit certainty but do not justify retaining a raw label.

## Cross-References

- [UID:0001GM][0x0055f450-0x005608fc.ScrollSpellInventoryPane](by-memory/0x0055f450-0x005608fc.ScrollSpellInventoryPane.md)
- [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md)
- [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md)
- [UID:0000LU][NewSpellInventoryPane](by-file/NewSpellInventoryPane.md)
- [UID:0001GO][0x005608a0-0x005608fc.ScrollSpellInventoryPaneResetScrollStateRaw](by-memory/0x005608a0-0x005608fc.ScrollSpellInventoryPaneResetScrollStateRaw.md)

## Changes

- 2026-05-31: Raised completion/confidence from `50/65` to `74/82` and marked the method reconstructable.
  - Before: The page documented behavior and IDA function size but had no reconstructable flag and only medium scoring.
  - Changed to: Added field-level IDA evidence for old/new position calculation, orientation, range, drag point, and owner callback.
  - Evidence: IDA MCP decompile of `0x00560710` and caller references from `0x00560650`.
- 2026-06-07: Raised completion/confidence from `74/82` to `86/88` after live IDA MCP rechecked the exact boundary, caller-only provenance from `0x00560650`, sibling input-bridge decompile, full update-helper call sequence, field offsets, vertical/horizontal math split, owner callback decompile, and callback xrefs. `AUTOGEN_PARENT_UID` remains blank because the method clears `85/85` but the direct class/file/fold parent candidates do not satisfy the corrected `85/85` parent gate.
- 2026-06-11 A001 Batch 193: Raised completion/confidence from `86/88` to `87/89` and set `AUTOGEN_PARENT_UID:0000CN`.
  - Before: The method had strong behavior evidence but still carried stale parent-gate text from before the ScrollSpellInventoryPane class/file repairs.
  - Changed to: Attached the method to the direct ScrollSpellInventoryPane class parent and refreshed evidence for boundary, callers, xrefs, callees, callback path, and parent scores.
  - Evidence: live IDA MCP `idb_meta`, `lookup_funcs`, `callers`, `xrefs_to`, `callees`, and decompile evidence for `0x00560710`; [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md) and [UID:0000NJ][ScrollSpellInventoryPane](by-file/ScrollSpellInventoryPane.md) now both clear the strict `85/85` gate.
- 2026-07-22 B001 source-quality implementation callback:
  - Changed from `87/89` to `90/92`, retained owner/emitter [UID:0000CN][ScrollSpellInventoryPane](by-class/ScrollSpellInventoryPane.md), set emitter position `80`, and inserted the accepted formal `UpdateFromDrag(int localY, int localX)` body.
  - Corrected the drag-offset mapping to `+0x108/.y/localY` and `+0x10c/.x/localX`; documented both exact orientation formulas, helper calls, callback signature/behavior/xrefs, exact byte hash and boundary, negative evidence, and standalone source placement.
  - Preserved the former reversed-offset, below-parent-gate, and unresolved-fold statements above as explicitly superseded historical assumptions rather than current truth.
