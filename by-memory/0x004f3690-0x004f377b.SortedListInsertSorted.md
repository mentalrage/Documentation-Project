*** UID:00037J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int SortedList::InsertSorted(const void *element)
{
    int index = 0;
    unsigned char *current = static_cast<unsigned char *>(m_data);
    while (index < m_count) {
        if (m_compare(element, current) < 0) {
            break;
        }

        current += m_elementSize;
        ++index;
    }

    MemoryMan *memoryMan = GetMemoryMan();
    int oldCount = m_count;

    int oldPageCount = 1;
    if (oldCount > 0) {
        oldPageCount = ((oldCount - 1) / m_pageSize) + 1;
    }

    int newPageCount = 1;
    if (oldCount >= 0) {
        newPageCount = (oldCount / m_pageSize) + 1;
    }

    if (oldPageCount < newPageCount) {
        size_t byteCount = newPageCount * m_pageSize * m_elementSize;
        MemoryMan *allocator = GetMemoryMan();
        if (m_data != NULL) {
            m_data = allocator->ReallocateBufferMemory(m_data, byteCount);
        } else {
            m_data = allocator->AllocateBufferMemory(byteCount);
        }
    }

    unsigned char *data = static_cast<unsigned char *>(m_data);
    memoryMan->MoveBufferMemory(
        data + ((index + 1) * m_elementSize),
        data + (index * m_elementSize),
        (oldCount - index) * m_elementSize);

    ++m_count;
    memoryMan->MemmoveWrapper(
        data + (index * m_elementSize),
        element,
        m_elementSize);

    return index;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-authored int SortedList::InsertSorted(const void *element) method at 0x004f3690-0x004f377b. It scans with the new element first and current element second, inserts after existing equal keys, grows page-counted fixed-width storage through MemoryMan, overlap-shifts the suffix, increments the count, copies one element, and returns the inserted index. One ChangeMan caller ignores the result and twelve Message callers immediately consume it. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f3690-0x004f377b SortedListInsertSorted

## Status

- Entity kind: method/helper.
- Owner: [UID:0000DF][SortedList](by-class/SortedList.md).
- Likely source file: [UID:0000NU][SortedList](by-file/SortedList.md).
- Address range: `0x004f3690-0x004f377b`, inclusive start/exclusive end.
- Rebuild handling: source-authored sorted container insertion helper.
- Disposition: reconstructable NexusTK code.
- Formal source route: method body in `NexusTK/util/SortedList.cpp` through [UID:0000DF][SortedList](by-class/SortedList.md) and [UID:0000NU][SortedList](by-file/SortedList.md). The formal H channel remains blank here because the class page owns the declaration.

## Behavior

This helper inserts one fixed-width element while preserving sorted order. It scans forward from `m_data`, advancing by `m_elementSize`, and calls the comparator at `+0x14` as `m_compare(element, current)`. It breaks only when that result is negative. A zero/equal result advances the scan, so a new equal record is placed after every existing equal record rather than before the first equal record.

The method snapshots `m_count` as the old count and computes old/new allocation-page counts with the binary's signed branches and minimum-one-page convention. The old count uses `oldCount > 0 ? ((oldCount - 1) / m_pageSize) + 1 : 1`; the prospective count uses `oldCount >= 0 ? (oldCount / m_pageSize) + 1 : 1`. Growth occurs only when the latter page count exceeds the former. The growth byte count remains the original 32-bit product `newPageCount * m_pageSize * m_elementSize`; a null data pointer selects `MemoryMan::AllocateBufferMemory`, while a non-null pointer selects `MemoryMan::ReallocateBufferMemory`.

After any growth, `MemoryMan::MoveBufferMemory` overlap-shifts `(oldCount - index) * m_elementSize` bytes from the insertion position to the following slot. The method then increments `m_count`, uses `MemoryMan::MemmoveWrapper` to copy exactly `m_elementSize` bytes from `element`, and returns `index`. This order is material: tail shift precedes count increment, and count increment precedes the fixed-width copy.

The source intentionally preserves the binary's failure and invalid-input behavior. It does not add null checks for `element`, `m_compare`, or the returned allocation; it does not add a zero-`m_pageSize` guard, overflow checking, exceptions beyond the allocator's existing throw behavior, or modern container semantics. `AllocateBufferMemory` and `ReallocateBufferMemory` use the accepted project behavior and throw a `Win32Error *` on failure rather than returning a recoverable null result to this method.

Source-facing signature is `int SortedList::InsertSorted(const void *element)`. The final `mov eax, ebx` returns the computed insertion index and `retn 4` confirms one explicit pointer argument. [UID:0000ZW][0x0047ed50-0x0047ed80.ChangeManRegister](by-memory/0x0047ed50-0x0047ed80.ChangeManRegister.md) deliberately ignores the result; its own source API remains `void` because neither its inherited LObject wrapper nor any containing caller tests or stores EAX.

## Raw Code Evidence

2026-06-11 IDA MCP `lookup_funcs` reports `sub_4F3690` as `0x004f3690-0x004f377b`. Decompilation shows:

- comparator callback invoked from `this + 0x14`;
- `count` at `+0x0c`, `elementSize` at `+0x04`, `pageSize` at `+0x08`, and `data` at `+0x10`;
- chunk-count calculation before/after insertion;
- allocation/reallocation helpers `0x00516050` / `0x00516190`;
- tail shift through `0x00516240`;
- inserted element copy through `0x00516220`;
- return of the computed insertion index.
- final `mov eax, ebx` followed by `retn 4`, proving the `int` return and one explicit `const void *` element argument.
- the exact operational order: comparator scan, optional page growth, overlap-safe suffix shift, count increment, fixed-width element copy, and insertion-index return.
- no modern validity checks around the comparator, source pointer, page size, arithmetic, or allocator result.

## Callers And Relationships

The direct-xref inventory contains exactly thirteen code callers. [UID:0000ZW][0x0047ed50-0x0047ed80.ChangeManRegister](by-memory/0x0047ed50-0x0047ed80.ChangeManRegister.md) calls at `0x0047ed75`, passes a 16-byte `ChangeManEntry`, and intentionally ignores EAX. Twelve Message insertion helpers immediately pass EAX to the duplicate-sequence normalizer at `0x00521c80`:

| InsertSorted call | Normalizer call | Caller role |
| --- | --- | --- |
| `0x00520f4c` | `0x00520f54` | Raw Message tag-0 insertion. |
| `0x00520fbc` | `0x00520fc4` | Raw Message tag-1 insertion. |
| `0x0052102e` | `0x00521036` | Raw Message tag-2 insertion. |
| `0x0052109e` | `0x005210a6` | Raw Message tag-3 insertion. |
| `0x0052110c` | `0x00521114` | Raw Message tag-4 insertion. |
| `0x0052117c` | `0x00521184` | Raw Message tag-5 insertion. |
| `0x005211f2` | `0x005211fa` | Raw Message tag-6 insertion. |
| `0x00521261` | `0x00521269` | Raw Message tag-7 insertion. |
| `0x005212cd` | `0x005212d5` | Raw Message tag-8 insertion. |
| `0x0052133d` | `0x00521345` | Raw Message tag-9 insertion. |
| `0x005213ff` | `0x00521407` | Raw Message tag-10 wide-string insertion. |
| `0x005214a2` | `0x005214aa` | Raw Message tag-11 blob insertion. |

Those twelve ordered call pairs establish that the return is the inserted slot index, including for duplicate keys. The following normalizer operates from that exact index; together with the comparator's zero-result advance, this preserves duplicate insertion sequence. The one ChangeMan use deliberately discards the same valid index, which supports its own `void Register` contract without weakening this method's `int` return.

## 2026-07-13 B003 UID0000ZW Return-Contract Resolution

- Live bounded disassembly confirms 96 instructions in 15 blocks across exact range `0x004f3690-0x004f377b` and the existing SortedList field layout: element size `+0x04`, page size `+0x08`, count `+0x0c`, data `+0x10`, comparator `+0x14`.
- The helper finds the insertion position with the comparator, grows by page boundaries, moves the occupied suffix by `elementSize`, increments count, performs one fixed-width source copy into the insertion slot, and returns that slot index through EAX.
- Best source contract is `int InsertSorted(const void *element)`. `const` applies to the copied source bytes; the method mutates the list. Exact original method spelling remains inferred, but return type, argument shape, and behavior are direct.
- UID0000ZW is the sole direct ChangeMan caller and intentionally ignores the return. That ignored use is positive evidence for a `void ChangeMan::Register` API, not evidence that this helper returns void.
- Ownership remains SortedList UID0000DF with source route UID0000NU. No ChangeMan-local helper body, copied implementation, or new formal block is introduced by this target.

This exact child replaces the insert helper row inside [UID:000193][0x004f3600-0x004f3a43.SortedList](by-memory/0x004f3600-0x004f3a43.SortedList.md).

## Source-Quality Resolution

- `InsertSorted` is the adopted source-facing method name. The exact original spelling is not preserved, but this name matches the existing `SortedList` API vocabulary and precisely identifies the behavior; a raw `sub_4F3690` label is not acceptable final source.
- The method is a public non-virtual member. Unrelated ChangeMan and Message consumers call it directly, while the ten-slot vtable does not contain this address.
- `const void *element` preserves the one-pointer ABI and the fact that the method only reads/copies the supplied fixed-width bytes.
- `m_compare` is the private comparator callback at `+0x14`, called new-element first and current-element second. Base storage fields remain `m_elementSize` at `+0x04`, `m_pageSize` at `+0x08`, `m_count` at `+0x0c`, and `m_data` at `+0x10`.
- Standalone `NexusTK/util/SortedList.cpp` and `SortedList.h` are the current source route. Folding into `List.cpp` is retained only as a superseded historical possibility; the dedicated by-file root, proposed tree, generated route, separate RTTI/vtable, and class-local island support the standalone reconstruction.
- No target-affecting source-quality question remains open. Exact original identifier spelling and physical source text are unrecoverable, so the highest-probability human source names are adopted rather than left as compiler-generated labels.

## Score Rationale

Completion is `96` and confidence is `96`. The exact range, owner/emitter route, public non-virtual `int InsertSorted(const void *)` contract, field layout, comparator argument/order rule, after-equals invariant, signed page arithmetic, allocation/reallocation branch, overlap-safe shift, count/copy/return order, exact thirteen callers, twelve index consumers, failure behavior, source placement, and formal C++ body are documented. The score does not claim original symbols or source text survived; the remaining uncertainty is limited to inferred human spelling rather than behavior, ABI, ownership, or code eligibility.

## Cross-References

- [UID:0000DF][SortedList](by-class/SortedList.md)
- [UID:0000NU][SortedList](by-file/SortedList.md)
- [UID:000193][0x004f3600-0x004f3a43.SortedList](by-memory/0x004f3600-0x004f3a43.SortedList.md)
- [UID:0000ZW][0x0047ed50-0x0047ed80.ChangeManRegister](by-memory/0x0047ed50-0x0047ed80.ChangeManRegister.md)
- [UID:000084][Message](by-class/Message.md)

## Changes

- 2026-06-11 Agent-A001 Batch 158 continuation:
  - Created exact sorted-insert child page while splitting the mixed `SortedList` aggregate.
  - Evidence: live IDA MCP decompilation and caller checks.
- 2026-07-13 B003 UID0000ZW implementation:
  - Changed `88/91 -> 91/94`, added the exact `int InsertSorted(const void *element)` contract and final-return proof, and documented UID0000ZW's intentional ignored result without adding duplicate helper C++.
- 2026-08-03 Agent-B005 UID00037J accepted ordinary implementation callback:
  - Changed `91/94 -> 96/96`, populated the formal CPP method body, and kept the H channel blank under class owner [UID:0000DF][SortedList](by-class/SortedList.md).
  - Added the exact thirteen-caller inventory and twelve InsertSorted-to-normalizer call pairs, after-equals duplicate ordering, signed page calculations, MemoryMan growth/failure behavior, exact shift/count/copy/return sequence, absent modern guards, adopted source-facing naming/access, and standalone source route.
