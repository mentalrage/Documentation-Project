*** UID:00012J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
DescPane::DescPane()
    : Pane(1),
      m_pDescriptionSource(NULL),
      m_selectedDescriptionIndex(-1)
{
    g_pDescPane = this;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw `DescPane` constructor body; IDA MCP confirms `Pane` base construction, three vtable stores, provider/index initialization, singleton store, and exact padding. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049d6f0-0x0049d73c DescPane Constructor Raw

## Status

- Disposition: reconstructable NexusTK project code with function-boundary caveat
- Entity kind: raw constructor-shaped method
- Owner class: [UID:00003Q][DescPane](by-class/DescPane.md)
- Likely source module: [UID:0000IS][DescPane](by-file/DescPane.md)
- Confidence: very strong for raw body boundaries, direct Pane construction,
  all three compiler vptr stores, exact tail initialization, singleton publish,
  class/file route, and the retained managed source body.
- The complete class declaration is now supplied by UID00003Q. The source type
  is concrete borrowed `Pane *`; no provisional provider interface or
  incomplete-header blocker remains.

## Behavior

The constructor calls the [UID:0000MC][Pane](by-file/Pane.md) constructor with style `1`, installs the primary and two secondary `DescPane` vtables, clears the description-source pointer at `+0xf8`, initializes selected index at `+0xfc` to `-1`, and stores `this` into [UID:0000QR][g_pDescPane](by-global/g_pDescPane.md). The source-facing field names used for the first draft are `m_pDescriptionSource` and `m_selectedDescriptionIndex`; they are descriptive reconstruction names, not proven original spellings.

## Boundary Notes

IDA does not currently model `0x0049d6f0` as a function and reports no direct xrefs to this raw start. Raw disassembly still shows a complete constructor-shaped body from `0x0049d6f0` through the `retn` at `0x0049d73b`, followed by alignment before the modeled destructor at `0x0049d740`.

2026-06-01 recheck: `lookup_funcs 0x0049d6f0` still reports `Not a function`, and xrefs to this raw start are still empty. The raw body remains valid constructor-shaped code and should stay documented as a `DescPane` constructor until function recovery changes.

| Range | Length | Evidence |
| --- | ---: | --- |
| `0x0049d6ed-0x0049d6f0` | 3 | `cc` alignment after `_DATFileMgr::FindNodeByKey`. |
| `0x0049d6f0-0x0049d73c` | 76 | Constructor-shaped body, no `cc` bytes. |
| `0x0049d73c-0x0049d740` | 4 | `cc` alignment before modeled non-deleting destructor. |
| `0x0049d740-0x0049d769` | 41 | Next modeled function, `DescPane` destructor. |

2026-06-18 B005 source-quality recheck confirmed the same half-open range and padding from raw bytes. The constructor body is exactly 76 bytes; there are no embedded `cc` bytes in the body, the three-byte prepad is outside the target, and the four-byte postpad belongs to the gap before the destructor.

```text
0049d6e0: 45 08 5f 5e 5b 89 08 8b e5 5d c2 08 00 cc cc cc
0049d6f0: 55 8b ec 51 56 8b f1 6a 01 89 75 fc e8 5f 6d 0a
0049d700: 00 c7 06 dc 89 61 00 8b c6 c7 86 a0 00 00 00 28
0049d710: 8a 61 00 c7 86 a4 00 00 00 58 8a 61 00 c7 86 f8
0049d720: 00 00 00 00 00 00 00 c7 86 fc 00 00 00 ff ff ff
0049d730: ff 89 35 f8 ad 69 00 5e 8b e5 5d c3 cc cc cc cc
0049d740: c7 01 dc 89 61 00 c7 81 a0 00 00 00 28 8a 61 00
```

## Evidence

- IDA MCP `lookup_funcs 0x0049d6f0` reports `Not a function`.
- IDA MCP raw `disasm 0x0049d6f0` shows base constructor call `0x00544460`, vtable installs, `+0xf8`/`+0xfc` initialization, and `dword_69ADF8 = esi`.
- `0x0049d6fc` calls [UID:0000A2][Pane](by-class/Pane.md)-family constructor `0x00544460` with style argument `1`.
- `0x0049d701`, `0x0049d709`, and `0x0049d713` install `DescPane` vtable bases `0x006189dc`, `0x00618a28`, and `0x00618a58`.
- `0x0049d71d` clears provider/source pointer `+0xf8`; `0x0049d727` initializes selected index `+0xfc` to `-1`.
- `0x0049d731` stores `this` into [UID:0000QR][g_pDescPane](by-global/g_pDescPane.md) storage `0x0069adf8`.
- IDA MCP xrefs to `0x0069adf8` are confined to the `DescPane` island: constructor store `0x0049d731`, destructor clear `0x0049d75a`, getter read `0x0049d770`, and scalar destructor clear `0x0049d860`.
- IDA MCP xrefs to the three `DescPane` vtable bases confirm constructor stores at `0x0049d701`, `0x0049d709`, and `0x0049d713`, matching destructor/scalar-destructor restores.
- 2026-06-18 B005 raw-memory scan found no raw pointer to `0x0049d6f0`
  and no rel32 call/jump target to `0x0049d6f0`. The same pass also found no
  raw pointer or rel32 route to setter start `0x0049d780`; positive controls
  found the function historically called `RefreshDescription`, now resolved
  as `DescPane::OnPaint`, in vtable slot `0x00618a20`, scalar deleting
  destructor `0x0049d840` in primary slot `0x006189dc`, and rel32 jumps from
  adjustor thunks at `0x0049d824` and `0x0049d82f`.
- The B005 vtable/global immediate scan found vtable base refs at constructor, destructor, and scalar-destructor offsets: `0x006189dc` at `0x0049d703`, `0x0049d742`, and `0x0049d848`; `0x00618a28` at `0x0049d70f`, `0x0049d74c`, and `0x0049d852`; `0x00618a58` at `0x0049d719`, `0x0049d756`, and `0x0049d85c`; and `0x0069adf8` at `0x0049d733`, `0x0049d75c`, `0x0049d771`, and `0x0049d862`.

## Instruction Map

| Address | Bytes / action | Source-quality meaning |
| --- | --- | --- |
| `0x0049d6f0` | `55 8b ec 51 56 8b f1` | Standard thiscall constructor prologue; preserves `this` in `esi`. |
| `0x0049d6f7` | `6a 01` | Pushes base-constructor argument `1`. Keep numeric until the source-quality `Pane` style/mode constant is proven. |
| `0x0049d6fc` | `e8 5f 6d 0a 00` | Calls `0x00544460`, the `Pane`-family constructor. |
| `0x0049d701` | `c7 06 dc 89 61 00` | Compiler emits the primary vtable write to `this+0`. |
| `0x0049d707` | `8b c6` | Returns `this` in `eax`. |
| `0x0049d709` | `c7 86 a0 ... 28 8a 61 00` | Compiler emits the secondary view vtable write at `this+0xa0`. |
| `0x0049d713` | `c7 86 a4 ... 58 8a 61 00` | Compiler emits the tertiary view vtable write at `this+0xa4`. |
| `0x0049d71d` | `c7 86 f8 ... 00 00 00 00` | Source field initialization: `m_pDescriptionSource = NULL`. |
| `0x0049d727` | `c7 86 fc ... ff ff ff ff` | Source field initialization: `m_selectedDescriptionIndex = -1`. |
| `0x0049d731` | `89 35 f8 ad 69 00` | Publishes `this` to `g_pDescPane`. |
| `0x0049d737-0x0049d73b` | `5e 8b e5 5d c3` | Constructor epilogue and return. |

## Heuristic / Inference Reanalysis And Validation

### Constructor Identity

The best source-facing identity is `DescPane::DescPane()`. This is not a standalone init helper because it calls the base constructor and installs all three vtable views before field initialization. It is not a factory because it performs no allocation and returns the incoming `this`. It is not padding or data because the bytes decode as a coherent prologue/body/epilogue with a valid class-local lifecycle counterpart at the destructor and scalar deleting destructor. The raw non-function status is a function-recovery caveat, not a no-code proof.

### Reachability And Confidence Cap

No direct static caller, raw VA pointer, or rel32 call/jump route to
`0x0049d6f0` is proven. Constructors are not expected to be vtable slots, so
the vtable reachability that proves `OnPaint` and the destructor paths does not
prove this raw start. The absence of a static route caps confidence below final
audit level, but the base constructor call, vptr stores, singleton lifecycle,
sibling destructor/getter/setter/paint bodies, and contiguous class island
make the ownership and source body definitive enough for reconstruction.

### Field And Interface Names

| Entity | Best current source-facing name | Evidence | Rejected alternatives |
| --- | --- | --- | --- |
| `this+0xf8` | `m_pDescriptionSource` | Constructor clears it; setter updates it; `OnPaint` treats it as a borrowed Pane and calls primary slot `+0x24`. | Generic `void *data`, owner pane, dialog controller, cached text pointer. No lifetime ownership or text-buffer operations match. |
| `this+0xfc` | `m_selectedDescriptionIndex` | Constructor initializes it to `-1`; setter stores caller-provided value; `OnPaint` rejects `-1` before dispatch. | Count, flags, mode, color, or cached length. The sentinel and call argument identify an index. |
| Source type | `Pane *` | Exact Pane primary slot `+0x24` has the required `(int, wchar_t *)` contract, and no destructor path releases the pointer. | Standalone `DescPaneDescriptionSource`, `void *`, feature-dialog provider, or owned pointer. |
| Pane slot `+0x24` | `GetDescription(int selectedIndex, wchar_t *outBuffer)` | `OnPaint` passes the selected index and a `wchar_t[1024]` output buffer. | Narrow `char *`, setter, command handler, allocator, or ownership transfer. |

These names and types are synchronized with the complete UID00003Q class
declaration. Exact original lexical spelling remains inferred, but no
declaration or source-integration blocker remains.

### Owner And Source Placement Ranking

| Rank | Candidate | Decision | Evidence |
| ---: | --- | --- | --- |
| 1 | [UID:00003Q][DescPane](by-class/DescPane.md) | Keep as canonical owner/emitter. | Constructor writes DescPane vtables, initializes DescPane-only tail fields, publishes the DescPane singleton, and sits inside the contiguous DescPane method island. |
| 2 | [UID:0000IS][DescPane](by-file/DescPane.md) / `NexusTK/ui/controls/DescPane.cpp` | Keep source placement. | Existing project structure and all xrefs support a reusable UI control, not a feature dialog. |
| 3 | `Pane` / `Pane.cpp` | Reject. | `Pane` supplies the base constructor and inherited slots only; the vtables, singleton, tail fields, and refresh override are DescPane-specific. |
| 4 | `DialogPane` / dialog core | Reject. | `DialogPane` RTTI begins at `0x00618a60`; no dialog-title/control-list behavior appears in this constructor. |
| 5 | Feature dialog owners | Reject. | No caller/xref fanout ties this constructor to a particular feature dialog. The provider/index pattern is a generic service. |
| 6 | Generic UI core | Reject as too broad. | The page is higher-level than `Pane` and is already supported as `ui/controls/DescPane.cpp`. |

## First-Draft C++ Rationale

The formal C++ block intentionally omits compiler-generated vtable stores. Source C++ should declare a polymorphic `DescPane`; the compiler emits vtable writes in constructors/destructors. `NULL` is used rather than `nullptr` to match plausible mid-2000s C++ style. `Pane(1)` remains numeric because the original symbolic name for the pane style/mode argument is not proven.

Do not hand-emit vtable data or the secondary/tertiary destructor adjustor thunks from this target. The source constructor body is:

```cpp
DescPane::DescPane()
    : Pane(1),
      m_pDescriptionSource(NULL),
      m_selectedDescriptionIndex(-1)
{
    g_pDescPane = this;
}
```

## Remaining Non-Blocking Issues

| Issue | Decision | Impact |
| --- | --- | --- |
| No proven direct constructor caller | Keep documented as a confidence cap. | Does not change range, owner, or first-draft C++ readiness. |
| Raw non-function start | Keep as raw constructor-shaped method. | Function recovery remains caveated; source identity is still strong. |
| Field and setter original spellings | Use `m_pDescriptionSource`, `m_selectedDescriptionIndex`, and `SetSourceAndIndex` as the best project-era source names. | Lexical confidence cap only; declaration and source are complete. |
| `Pane(1)` mode constant | Emit numeric `1` until a source-quality enum/constant is proven. | Minor readability blocker only. |

## Cross-References

- [UID:00012K][0x0049d6f0-0x0049d89f.DescPane](by-memory/0x0049d6f0-0x0049d89f.DescPane.md)
- [UID:00003Q][DescPane](by-class/DescPane.md)
- [UID:0000IS][DescPane](by-file/DescPane.md)
- [UID:0001U3][DescPaneLayout](by-type/by-struct/DescPaneLayout.md)
- [UID:0000QR][g_pDescPane](by-global/g_pDescPane.md)

## Score Rationale

- `COMPLETION:91`: exact bytes/boundaries, negative raw-pointer and rel32
  route checks, all vptr/global writes, Pane-typed fields, owner/source
  placement, rejected alternatives, formal C++, and class declaration
  integration are complete.
- `CONFIDENCE:93`: class identity, fields, vtables, singleton lifecycle, and
  source body are strongly supported. No direct constructor caller and exact
  original lexical spellings remain the only caps.

## 2026-07-16 B001 UID0002NA Accepted Callback

- Applied source position 10 and retained the exact managed constructor body.
- UID00003Q now declares the complete class before this qualified definition;
  explicit vptr writes remain excluded as compiler output.
- Historical custom-provider, narrow-buffer, RefreshDescription, and
  incomplete-header statements are superseded. Borrowed `Pane *`,
  `wchar_t *` description output, and `OnPaint` are the current source union.
- The raw non-function/no-caller evidence remains preserved as a confidence
  cap and does not change owner, emitter, range, or reconstructability.

## Changes

- 2026-07-16 B001 UID0002NA accepted callback:
  - Raised `88/90 -> 91/93`, set position 10, retained the exact formal body,
    and synchronized the constructor with the complete class declaration.
  - Historicalized the current-facing custom-provider and incomplete-header
    blockers while preserving all raw bytes, padding, no-route evidence,
    compiler vptr-store exclusions, and rejected alternatives.
- 2026-06-18 B005 source-quality incorporation:
  - Before: `COMPLETION:84`, `CONFIDENCE:88`, and a blank formal C++ block because provider/interface names were treated as blocking.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`, formal first-draft `DescPane::DescPane()` C++, report-level raw byte map, negative reachability scan, vtable/global immediate scan, field/interface naming rationale, owner/source-placement ranking, rejected alternatives, and explicit remaining confidence caps.
  - Summary/evidence: B005 verified the exact 76-byte body, three-byte prepad, four-byte postpad, `Pane(1)` base construction, three vtable writes, `+0xf8/+0xfc` initialization, `g_pDescPane` publish, no pointer/rel32 route to raw starts, sibling vtable/destructor/getter/setter/refresh support, and that provider spelling uncertainty does not block this constructor's first-draft C++.
