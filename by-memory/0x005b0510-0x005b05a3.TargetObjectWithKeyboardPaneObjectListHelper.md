*** UID:00036R | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ED | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000ED | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool TargetObjectWithKeyboardPane::OnObjectList(const PacketEvent *event)
{
    const unsigned char *payload = event->payload;
    if (payload[0] != '_')
        return false;

    const unsigned char count = payload[1];
    for (unsigned char i = 0; i < count; ++i) {
        const unsigned int objectId =
            PacketBufferReadUInt32BE(payload + 2 + i * 4);

        if (objectId == s_targetObjectTargetId) {
            LivingObjectPane *oldTarget =
                g_activeMapPane->FindObjectPaneById(s_targetObjectTargetId);
            LivingObjectPane *localPlayer = g_pUserPane->GetLocalPlayerObject();

            s_targetObjectTargetId = localPlayer->GetObjectId();
            oldTarget->SetTargetHighlight(false);
            localPlayer->SetTargetHighlight(true);
            break;
        }
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Secondary-vtable object-list virtual; scans underscore-prefixed payload ids, compares `s_targetObjectTargetId`, falls back to local player when matched, and toggles target highlights. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b0510-0x005b05a3 TargetObjectWithKeyboardPane Object List Helper

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: modeled helper method.
- IDA function: `sub_5B0510`, size `0x93`.
- Direct owner: [UID:0000ED][TargetObjectWithKeyboardPane](by-class/TargetObjectWithKeyboardPane.md).
- Source module context: [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md).
- Rebuild handling: rebuild from the target-selection pane source; do not hand-port raw bytes.
- Best source-facing signature: `bool TargetObjectWithKeyboardPane::OnObjectList(const PacketEvent *event)`.

## Purpose

This source-authored secondary-vtable helper handles underscore-prefixed object-list payloads for `TargetObjectWithKeyboardPane`. It reads the payload pointer from `event+0x0c`, requires leading `_`, treats `payload[1]` as an entry count, scans four-byte big-endian object ids from `payload+2`, and compares each id with `dword_69BF2C` / `s_targetObjectTargetId`. When the payload contains the current saved target id, the helper resolves the old target through `MapPane::FindObjectPaneById`, switches the saved id to the local player object's `+0xfc` id, clears the old target highlight, highlights the local player, and returns `false`.

## Evidence Notes

- A007 live IDA MCP `lookup_funcs` identified `0x005b0510` as modeled function `sub_5B0510` with size `0x93`; the half-open range ends at `0x005b05a3`.
- The exact half-open range is `0x005b0510-0x005b05a3`; no split/merge is indicated. The predecessor bytes `0x005b050d-0x005b0510` are three `0xcc` bytes after [UID:0001LL][0x005b0290-0x005b050d.TargetObjectWithKeyboardPaneMouseEvent](by-memory/0x005b0290-0x005b050d.TargetObjectWithKeyboardPaneMouseEvent.md), and `0x005b05a3-0x005b05b0` is thirteen `0xcc` bytes before [UID:00036S][0x005b05b0-0x005b05cf.TargetObjectWithKeyboardPaneClearCurrentTarget](by-memory/0x005b05b0-0x005b05cf.TargetObjectWithKeyboardPaneClearCurrentTarget.md).
- `xrefs_to 0x005b0510` reports the vtable data reference at `0x0062f414`, with no ordinary direct code callers. `master_vtables.json` places the function in `vftable_0062f404`, entry index `4`, offset `+0x10`, beside the `OnMouseEvent` and `OnKeyEvent` secondary-vtable entries. The IDA exported `num_callers.ida = 1` is the vtable data ref, not a hidden direct callsite.
- Sibling object-list virtuals use the same slot pattern: `SpellWhoInputPane` at `0x005aea60` uses `dword_69BF20`, `ItemWhoInputPane` at `0x005af2f0` uses `dword_69BF24`, `SelectObjectWithKeyboardPane` at `0x005afc90` uses `dword_69BF28`, and this target-object helper uses `dword_69BF2C`.
- The function reads `[event + 0x0c]` as the payload pointer, checks `payload[0] == '_'`, reads the count byte from `payload[1]`, starts the id scan at offset `2`, calls `0x005754c0` for four-byte big-endian ids, compares each id with `dword_69BF2C`, and leaves state unchanged when no id matches.
- The fallback branch is taken on equality with `dword_69BF2C`: it calls `0x00506970` to resolve the old target through `dword_67A764`, reads the local-player object from `dword_67A748`, writes `[localPlayer + 0xfc]` back to `dword_69BF2C`, calls `0x0053b1b0(oldTarget, 0)`, then calls `0x0053b1b0(localPlayer, 1)`.
- `0x00575470` in the decompile is `std::_Narrow_char_traits<char,int>::to_char_type`, effectively a byte load for `payload[1]`; do not model it as project source logic. `0x005754c0` is `PacketBufferReadUInt32BE`, `0x00506970` is `MapPane::FindObjectPaneById`, and `0x0053b1b0` is `LivingObjectPane::SetTargetHighlight` / `SetHighlightState`.
- `dword_69BF2C` is source-facing `s_targetObjectTargetId` / `s_targetObjectSavedTargetId`, absolute file-scope target-selection state, not a class field. `dword_67A764` is `g_activeMapPane`; `dword_67A748` is the local-player `LivingObjectPane *` in this context, represented in current target-selection drafts as `g_pUserPane->GetLocalPlayerObject()`.
- IDA/Ghidra show a one-argument `__stdcall` shape because the hidden `this` register is unused. The vtable slot at `0x0062f414` proves this is a class virtual; MSVC thiscall methods with one stack parameter also end in `retn 4`. The low byte of the return value is always zero, so the source-facing return type should be `bool` with `return false`.
- Owner alternatives rejected: `MapPane` only supplies lookup/traversal dependencies; `LivingObjectPane` only supplies highlight state; `PacketBuffer` only supplies scalar decode; `UserPane`/local-player globals are consumed as fallback dependencies; [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md) is the file route, not the direct memory-child owner; no-owner/non-emitting is rejected because the vtable data ref proves active virtual dispatch reachability.

## Historical Assumptions

- Older notes described the fallback as occurring when the saved target "no longer appears" in an incoming active-object list. The local binary does not prove that interpretation. The branch to fallback occurs when a decoded payload id equals `s_targetObjectTargetId`; the no-match path returns `false` without changing state. A server-side protocol pass may later prove the payload is an invalidation/removal list, but until then use neutral "object-list payload" or "object-list invalidation payload" wording.

## Reconstruction Notes

- First-draft C++ is appropriate: the target is source-authored, vtable-reachable, exactly bounded, attached to [UID:0000ED][TargetObjectWithKeyboardPane](by-class/TargetObjectWithKeyboardPane.md), and has source-quality helper/global names for the body.
- Keep the draft null-check behavior faithful to the binary: after a matched id, the lookup result is passed to `SetTargetHighlight(false)` without a local null guard.
- Remaining non-final names are source spellings, not no-code blockers: exact original method name, exact packet/event struct name, exact upstream protocol name for the underscore payload, and the canonical public spelling for the local-player object accessor.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact boundary/padding, vtable reachability, sibling comparison, payload parser, helper/global names, owner route, fallback semantics, rejected alternatives, and first-draft C++ are documented. |
| Confidence | 90 | Vtable, decompile, and sibling evidence are strong. Confidence is capped by the exact original method name, packet/event struct spelling, and unresolved active-list versus invalidation-list protocol name. |

## Cross-References

- [UID:0000ED][TargetObjectWithKeyboardPane](by-class/TargetObjectWithKeyboardPane.md)
- [UID:0000OH][TargetSelectionInputPanes](by-file/TargetSelectionInputPanes.md)
- [UID:0001LL][0x005b0290-0x005b050d.TargetObjectWithKeyboardPaneMouseEvent](by-memory/0x005b0290-0x005b050d.TargetObjectWithKeyboardPaneMouseEvent.md)
- [UID:00036S][0x005b05b0-0x005b05cf.TargetObjectWithKeyboardPaneClearCurrentTarget](by-memory/0x005b05b0-0x005b05cf.TargetObjectWithKeyboardPaneClearCurrentTarget.md)
- [UID:0002AB][0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals](by-memory/0x0069bf20-0x0069bf34.TargetSelectionSavedTargetGlobals.md)

## Changes

- 2026-06-11 supervisor cleanup created this exact split page from the A007 pending new-file note.
- Evidence: A007 had already gathered live IDA function-size, boundary, and semantic notes; the page now gives those facts a UID-backed child document and assigns it to the now-gated [UID:0000ED][TargetObjectWithKeyboardPane](by-class/TargetObjectWithKeyboardPane.md).
- 2026-06-20 B004 Rule 26 source-quality incorporation:
  - Before: score was `85/86`, formal C++ was blank, and the behavior wording said fallback occurred when the saved target no longer appeared in an incoming list.
  - After: score is `88/90`, first-draft `TargetObjectWithKeyboardPane::OnObjectList` C++ is present, and the behavior is corrected to the locally proven equality-triggered fallback on a decoded payload id matching `s_targetObjectTargetId`.
  - Evidence: B004 checked exported decompilation, vtable data ref `0x0062f414`, exact padding, sibling object-list virtuals, saved-target globals, packet scalar helpers, MapPane object lookup, LivingObjectPane highlight calls, and target-selection class/file support docs.
