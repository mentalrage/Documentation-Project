*** UID:000331 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000013 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000013 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
HRESULT STDMETHODCALLTYPE Browser::Invoke(DISPID dispidMember,
                                          REFIID,
                                          LCID,
                                          WORD,
                                          DISPPARAMS *params,
                                          VARIANT *,
                                          EXCEPINFO *,
                                          UINT *)
{
    switch (dispidMember)
    {
    case kDispIdDownloadComplete:
        if (--m_pendingDownloadCount <= 0)
        {
            if (m_showBrowserAfterLoad)
            {
                ShowCursor(TRUE);
                ShowWindow(m_hWnd, SW_SHOW);
                UpdateWindow(m_hWnd);
                m_showBrowserAfterLoad = false;

                browser::Notification *notification = new browser::Notification(0, NULL);
                PostApplicationNotification(kBrowserNotificationChannel, notification);
            }
            m_pendingDownloadCount = 0;
        }
        return S_OK;

    case kDispIdDownloadBegin:
        if (m_pendingDownloadCount <= 0)
            m_pendingDownloadCount = 0;
        ++m_pendingDownloadCount;
        return S_OK;

    case kDispIdBeforeNavigate2:
    {
        const wchar_t *url = V_BSTR(V_VARIANTREF(&params->rgvarg[5]));
        VARIANT_BOOL *cancel = V_BOOLREF(&params->rgvarg[0]);

        if (wcsstr(url, L"close()") != NULL)
        {
            PostThreadMessageA(g_pBrowserThread->ThreadId(), kBrowserThreadCloseMessage, 0, 0);
            Navigate(L"about:blank");
            *cancel = (VARIANT_BOOL)1;
            return S_OK;
        }

        if (wcsstr(url, L"res:") != NULL)
        {
            PostThreadMessageA(g_pBrowserThread->ThreadId(), kBrowserThreadCloseMessage, 0, 0);
        m_currentUrl.assign(g_pLanguageMan->GetLocalizedString(235));
            return S_OK;
        }

        if (m_allowedUrlPrefix[0] != L'\0' && wcsstr(url, m_allowedUrlPrefix) != url)
            *cancel = (VARIANT_BOOL)1;
        return S_OK;
    }

    case kDispIdNavigateComplete2:
        if (m_allowedUrlPrefix[0] != L'\0')
            return S_OK;
        *V_BOOLREF(&params->rgvarg[0]) = (VARIANT_BOOL)1;
        return S_OK;

    case kDispIdNewWindow2:
        *V_BOOLREF(&params->rgvarg[0]) = (VARIANT_BOOL)0;
        PostBrowserNotification(1, m_currentUrl.c_str());
        return S_OK;

    case kDispIdWindowClosing:
        *V_BOOLREF(&params->rgvarg[0]) = (VARIANT_BOOL)1;
        return S_OK;

    default:
        return DISP_E_MEMBERNOTFOUND;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary:  | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046f810-0x0046fb80 Browser Invoke And Switch Table

## Scope

- Range: `0x0046f810-0x0046fb80` half-open.
- Entity kind: Browser COM dispatch method plus compiler switch table.
- Owner: [UID:000013][Browser](by-class/Browser.md).
- Parent inventory: [UID:0000ZF][0x0046f010-0x004710b8.BrowserOleLegacyAndHelpers](by-memory/0x0046f010-0x004710b8.BrowserOleLegacyAndHelpers.md).
- Likely source file: [UID:0000HV][Browser](by-file/Browser.md).

The modeled function body `0x0046f810-0x0046fab6` is `Browser::Invoke`. The child intentionally includes the associated compiler switch/jump table bytes through `0x0046fb80`.

## IDA Evidence

- 2026-06-10 B001-024 live IDA MCP reports `sub_46F810` at `0x0046f810-0x0046fab6`, followed by switch/table bytes beginning at `0x0046fab6`.
- Vtable data ref `0x006138b4` points to the function body.
- Decompilation shows dispatch cases for browser event IDs including `104`, `106`, `250`, `251`, `263`, and `270`.
- Case `250` inspects URL text for `close()` and resource/redirect markers, posts Browser private message `0x500` to the BrowserThread, calls [UID:00033K][0x00470dd0-0x00470f10.BrowserNavigate](by-memory/0x00470dd0-0x00470f10.BrowserNavigate.md), and can cancel navigation. B013 local PE reanalysis shows this call uses an adjusted Browser interface pointer (`lea ecx,[ecx-8]`), not a BrowserWindow object.
- Case `104` decrements a pending-load count, shows/updates the hosted window, constructs an inline [UID:000014][browser__Notification](by-class/browser__Notification.md) payload, and posts channel `0x42726f73` (`Bros`).
- Case `263` copies/captures the current URL string and calls `PostBrowserNotification` at `0x00470f20`.

## Reconstruction Notes

This is source-authored Browser event dispatch logic. The table bytes are compiler-generated from the switch and should be regenerated from the source switch rather than emitted as raw data.

## 2026-06-20 B007 Source-Quality Sync

`Browser::Invoke` receives the Browser `IDispatch` adjusted view (`Browser base + 8`). Convert raw decompiler offsets back to Browser base offsets before naming fields. Current source-facing field names used by this method are:

| Base offset | Field | Role in Invoke |
| ---: | --- | --- |
| `+0x20`-`+0x221` range | `m_allowedUrlPrefix` | Optional wide prefix used to reject unexpected `BeforeNavigate2` URLs. |
| `+0x222` | `m_showBrowserAfterLoad` | Visibility-after-load flag tested when the pending count reaches zero. |
| `+0x224` | `m_pendingDownloadCount` | Incremented/decremented by download begin/complete style events. |
| `+0x228` | `m_navigationBuffer` | Navigation URL/buffer pointer used by redirect/navigation paths. |
| `+0x230` | `m_currentUrl` | Project wide-string object updated by resource/fallback current-URL paths. |

High-probability dispatch-id meanings for the legacy Browser object:

| DISPID | Meaning |
| ---: | --- |
| `104` | `DownloadComplete` / visibility-complete style event; decrements `m_pendingDownloadCount`, shows/updates the hosted window when `m_showBrowserAfterLoad` is set, restores cursor visibility, clears the flag, and posts `browser::Notification`. |
| `106` | `DownloadBegin` / pending-load increment style event; increments `m_pendingDownloadCount`. |
| `250` | `BeforeNavigate2`-style event; inspects target URL, handles `close()`/`res://`, posts browser private message `0x500`, calls `Browser::Navigate`, stores fallback/current URL text, and cancels navigation where appropriate. |
| `251` | `NavigateComplete2`-style guard/cancel path. |
| `263` | New-window/current-URL notification style event. |
| `270` | Window-closing style event. |

Use the `style` wording until another support doc proves exact imported event constant names for this old Browser sink.

## 2026-07-02 B007 Implementation Callback

Current MCP evidence used by the accepted report came from session `supervisor_resume_20260629`: server health ok, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready. `lookup_funcs` reports the modeled function `sub_46F810` at `0x0046f810` with size `0x2a6`, so the source-authored body ends at `0x0046fab6`; `0x0046fab6` is not a function, and the next Browser method starts at `0x0046fb80`.

Vtable and range facts:

- The `IDispatch` / `DWebBrowserEvents2` vtable slot at `0x006138b4` points to `0x0046f810`; the next DocHost/UI slot at `0x006138dc` points to `0x0046fb80`.
- `xrefs_to` reports `0x0046fab8` referenced only by switch code at `0x0046f84c`, and `0x0046fad4` referenced only by selector code at `0x0046f845`.
- Direct bytes show `0x0046fab6` has `66 90` alignment, `0x0046fab8` has seven 32-bit case target addresses, `0x0046fad4` has a 167-byte selector table for `dispid - 104`, and the range ends with `0xcc` fill before `0x0046fb80`.
- The generated `auto-generated/NexusTK/browser/Browser.cpp` read-only state before this callback was validator command `000000004361`, refreshed `2026-07-02T06:32:49-04:00`, and still showed `UID:000331` as an empty emitter marker.

The switch case map is:

| DISPID | Source-facing case | Behavior |
| ---: | --- | --- |
| `104` | `kDispIdDownloadComplete` | Decrements `m_pendingDownloadCount`; when the count reaches zero and `m_showBrowserAfterLoad` is set, restores cursor/window visibility, updates the hosted HWND, clears the flag, constructs `browser::Notification(0, NULL)`, and posts the `Bros` channel. |
| `106` | `kDispIdDownloadBegin` | Normalizes nonpositive pending count to zero and increments `m_pendingDownloadCount`. |
| `250` | `kDispIdBeforeNavigate2` | Reads the reversed DWebBrowserEvents2 URL argument, handles `close()` and `res:` URLs, posts the private browser-thread close message `0x500`, calls `Browser::Navigate(L"about:blank")` for the close route, updates `m_currentUrl` from localized string id `235` for the resource route, and cancels disallowed prefixes. |
| `251` | `kDispIdNavigateComplete2` | Leaves navigation alone when an allowed prefix is configured; otherwise writes cancel `1` through the reversed cancel argument. |
| `263` | `kDispIdNewWindow2` | Writes cancel `0` and calls `PostBrowserNotification(1, m_currentUrl.c_str())`. |
| `270` | `kDispIdWindowClosing` | Writes cancel `1`. |
| default | `DISP_E_MEMBERNOTFOUND` | Returns `0x80020003` for unknown dispatch ids. |

`Browser::Invoke` receives the adjusted `IDispatch` view (`Browser base + 8`). Raw decompiler offsets in this body therefore use the adjusted pointer and must be converted back to Browser base offsets by adding 8. The accepted field map is `m_allowedUrlPrefix` at base `+0x20`, `m_showBrowserAfterLoad` at base `+0x222`, `m_pendingDownloadCount` at base `+0x224`, `m_navigationBuffer` at base `+0x228`, and `m_currentUrl` at base `+0x230`.

The formal C++ uses standard source-facing OLE macros instead of raw decompiler arithmetic. MCP disassembly proves case `250` reads the URL through the sixth reversed argument (`params->rgvarg[5].pvarVal->bstrVal`, raw `[rgvarg + 0x58]` then `+8`), represented as `V_BSTR(V_VARIANTREF(&params->rgvarg[5]))`. Cancel writes use the first reversed argument (`params->rgvarg[0].pboolVal`, raw `**(_WORD **)(*params + 8)`), represented as `V_BOOLREF(&params->rgvarg[0])` with 16-bit `VARIANT_BOOL` values.

Accepted inferred names and caveats:

- Event constants use source-facing WebBrowser-event names `kDispIdDownloadComplete`, `kDispIdDownloadBegin`, `kDispIdBeforeNavigate2`, `kDispIdNavigateComplete2`, `kDispIdNewWindow2`, and `kDispIdWindowClosing`. Numeric behavior is strong; original project spelling remains inferred.
- Browser message `0x500` / decimal `1280` is the private browser-thread close message. The formal body uses `kBrowserThreadCloseMessage`; support enum documentation also preserves the older `kBrowserThreadClose` hypothesis.
- The notification channel is `kBrowserNotificationChannel` for tag `0x42726f73` / decimal `1114795891` (`Bros`), posted through the inferred source-facing `PostApplicationNotification` dispatcher. The helper route `PostBrowserNotification(1, m_currentUrl.c_str())` remains on [UID:00033L][0x00470f20-0x00470fb9.PostBrowserNotification](by-memory/0x00470f20-0x00470fb9.PostBrowserNotification.md).
- `m_currentUrl.assign(...)` and `m_currentUrl.c_str()` are source-facing wrappers over project wide-string helpers `0x00582d20` and `0x00584540`; the final wrapper type/method spelling is still support-doc dependent.
- `g_pLanguageMan->GetLocalizedString(235)` is the canonical localized resource lookup before assigning the `res:` fallback/current URL text; the former static-looking `LanguageMan::GetString` token was a stale generated alias.

Rejected alternatives preserved by the accepted report: do not emit the switch table as raw source data; do not create a separate table child; do not move this body to `BrowserWindow`, `BrowserThread`, or `BrowserControlPaneOld`; and do not leave the target blank-C++/no-code because the source-authored `Invoke` body is now source-like enough for a first draft while the table bytes are compiler output from the source switch.

Old report search terms checked for stale/duplicate assumptions were `000331`, `0x0046f810`, `0046f810`, `BrowserInvokeAndSwitchTable`, `Browser::Invoke`, `sub_46F810`, and `46F810`.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | The page now records the exact body and table range, vtable route, switch-table byte layout, event case map, adjusted `IDispatch` field conversion, reversed `DISPPARAMS` argument mapping, notification/message/string helper assumptions, rejected split/raw-table alternatives, and first-draft formal C++. |
| Confidence | 90 | Current MCP function boundaries, xrefs, table bytes, string bytes, decompilation/disassembly, Browser class docs, notification docs, and helper xrefs agree. Confidence stays capped at 90 because exact original event/helper/string-wrapper spellings remain inferred. |

## Cross-References

- [UID:000013][Browser](by-class/Browser.md)
- [UID:000014][browser__Notification](by-class/browser__Notification.md)
- [UID:00001A][BrowserThread](by-class/BrowserThread.md)
- [UID:00033K][0x00470dd0-0x00470f10.BrowserNavigate](by-memory/0x00470dd0-0x00470f10.BrowserNavigate.md)
- [UID:0000HV][Browser](by-file/Browser.md)
- [UID:0001SJ][BrowserDispatchEventId](by-type/by-enum/BrowserDispatchEventId.md)

## Changes

- 2026-06-10 B001-024: Created exact Browser `Invoke` child with its dispatch table.
- 2026-06-19 B013 support sync: changed the case `250` navigation helper reference from BrowserWindow ownership to [UID:00033K][0x00470dd0-0x00470f10.BrowserNavigate](by-memory/0x00470dd0-0x00470f10.BrowserNavigate.md), because local PE evidence shows the caller adjusts/passes a Browser interface pointer.
- 2026-06-20 B007 source-quality sync: added adjusted-`IDispatch` pointer warning, source-facing Browser field names, and high-probability dispatch-id meanings for `104`, `106`, `250`, `251`, `263`, and `270`.
- 2026-07-02 B007 UID000331 implementation callback: raised score to `88/90`, inserted the accepted first-draft `Browser::Invoke` body into the formal reconstruction block, preserved owner/emitter/reconstructable state, documented current MCP/table/string/generated-empty-marker evidence, and kept the compiler switch bytes inside this target with no split/table child.
