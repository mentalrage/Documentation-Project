*** UID:00050D | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000TK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000TK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void __stdcall SanitizeWideTextForChat(wchar_t *text, unsigned int textLength)
{
    for (unsigned int i = 0; i < textLength; ++i) {
        wchar_t ch = text[i];
        if (ch == 0)
            break;

        if (ch == L'\n' || ch == L'\r' || (ch >= L' ' && ch <= 0x7f))
            continue;

        switch (ch) {
        case 0x00e9:
        case 0x00e8:
        case 0x00e0:
        case 0x00f9:
        case 0x00f4:
        case 0x00ee:
        case 0x00eb:
        case 0x00ef:
        case 0x00fb:
        case 0x00fa:
        case 0x00e2:
        case 0x00e7:
            continue;
        default:
            text[i] = L'?';
            break;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact 0x10a/266-byte free helper (SHA16 d06aa427134dc3d3) with 14 code call sites across 12 submit/dialog functions, no callees, two stack arguments with retn 8, no ecx/this use, UTF-16 in-place filtering, ASCII/CR/LF/lowercase-accent allowlist, rejected TextFilter member/virtual signatures, owner/emitter [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md), and first-draft SanitizeWideTextForChat C++ emitted here. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005957c0-0x005958ca SanitizeWideTextForChat

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact free helper function.
- Direct owner: [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) `SanitizeWideTextForChat`.
- Source route: [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) -> [UID:0000OO][TextFilter](by-file/TextFilter.md) `TextFilter`, projected to `NexusTK/ui/controls/TextFilter.cpp`.
- Parent aggregate: [UID:0001JU][0x00595760-0x005958fe.TextFilterAndSanitizer](by-memory/0x00595760-0x005958fe.TextFilterAndSanitizer.md) `0x00595760-0x005958fe.TextFilterAndSanitizer`.
- C++ status: first-draft helper C++ belongs on this exact by-memory child. The by-global helper page indexes/routes the helper and must not duplicate the full body if this child emits it.
- Score intent: `93/94`. Behavior, range, all 14 call sites, two-argument `__stdcall` ABI, no-`ecx` evidence, complete body, owner, emitter, bytes, and padding are closed; only the original lexical helper name remains inference-backed.

## Covered Range

| Range | Classification | Evidence / disposition |
| --- | --- | --- |
| `0x005957b1-0x005957c0` | predecessor padding | Fifteen `0xcc` bytes after the destructor body, SHA16 `54cc9dd9593316e8`; ignored padding, not this child. |
| `0x005957c0-0x005958ca` | this target body | `SanitizeWideTextForChat`, size `0x10a` / 266 bytes, SHA16 `d06aa427134dc3d3`. |
| `0x005958ca-0x005958d0` | successor padding | Six `0xcc` bytes, SHA16 `33cc4a00a0f04735`; preserve as ignored padding, not a source child. |

## MCP Evidence

B003's accepted split report used live IDA MCP session `ff68e691` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. `server_health(database='ff68e691')` returned `status:"ok"`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready. Stale session `261fb29b` is superseded.

MCP facts:

- `lookup_funcs` identifies `0x005957c0` as `sub_5957C0`, size `0x10a`, ending at exclusive address `0x005958ca`.
- `lookup_funcs` reports no function at `0x005958ca`, confirming the half-open end and following padding.
- `get_bytes` for this range returns SHA16 `d06aa427134dc3d3`.
- `get_bytes` for `0x005958ca-0x005958d0` returns six `0xcc` bytes, SHA16 `33cc4a00a0f04735`.
- `callees 0x005957c0` reports no callees.
- `xrefs_to 0x005957c0` reports 14 code xrefs across 12 functions:
  - `0x0047762b` and `0x00477641` in `sub_477590`.
  - `0x0047de2c` and `0x0047de3f` in `sub_47DCB0`.
  - `0x0053fe34` in `sub_53FDC0`.
  - `0x0059e4ae` in `sub_59E3D0`.
  - `0x005ae0cf` in `sub_5AE060`.
  - `0x005b16af` in `sub_5B1640`.
  - `0x005b1acf` in `sub_5B1A60`.
  - `0x005b1fef` in `sub_5B1F80`.
  - `0x005b23ff` in `sub_5B2390`.
  - `0x005b3700` in `sub_5B3670`.
  - `0x005b3ad0` in `sub_5B3A40`.
  - `0x005b42e1` in `sub_5B4260`.
- Decompilation models a two-stack-argument helper and the disassembly returns with `retn 8`.
- The callee does not read `ecx`. Several callers load [UID:0000SG][g_pTextFilter](by-global/g_pTextFilter.md) `g_pTextFilter` before calling, but current callee evidence rejects a `TextFilter *` stack parameter and rejects member/virtual signatures.
- `entity_query names *Sanit*` returns no recovered source symbol, so `SanitizeWideTextForChat` is an inferred but established project source-facing name.

## Behavior

The helper edits a UTF-16 buffer in place before user-authored text is serialized into packets:

1. Iterates from index `0` while index `< textLength`.
2. Stops early when it sees a NUL code unit.
3. Preserves LF `0x000a`, CR `0x000d`, printable ASCII `0x20-0x7f`, and a fixed lowercase Latin-1 accent allowlist.
4. Replaces every other nonzero code unit with `?` / `0x003f`.

The allowed accented code units are:

```text
0x00e9, 0x00e8, 0x00e0, 0x00f9, 0x00f4, 0x00ee,
0x00eb, 0x00ef, 0x00fb, 0x00fa, 0x00e2, 0x00e7
```

## Caller Families

| Caller range / site | Current owner context | Use |
| --- | --- | --- |
| `0x0047762b`, `0x00477641` | New article flow | Sanitizes article subject/body before posting. |
| `0x0047de2c`, `0x0047de3f` | New mail flow | Sanitizes mail subject/body before sending. |
| `0x0053fe34` | Profile dialog flow | Sanitizes profile text before copying/saving. |
| `0x0059e4ae` | Party search edit pane | Sanitizes party-search text before opcode `0x84`. |
| `0x005ae0cf` | Spell string input pane | Sanitizes spell-string argument before opcode `0x0f`. |
| `0x005b16af`, `0x005b1acf`, `0x005b1fef`, `0x005b23ff` | Say target/group/plan input pane family | Sanitizes target-message payloads. |
| `0x005b3700` | [UID:0000C3][SayInputPane](by-class/SayInputPane.md) `SayInputPane` | Sanitizes normal say text before opcode `0x0e`, subtype `0`. |
| `0x005b3ad0` | Chat/recipient input path | Sanitizes text before submit. |
| `0x005b42e1` | Shout input pane | Sanitizes shout text before opcode `0x0e`, subtype `1`. |

Caller pages consume the shared helper; they do not own it.

## Source-Quality Names And Signature

Recommended source-facing signature:

```cpp
void __stdcall SanitizeWideTextForChat(wchar_t *text, unsigned int textLength);
```

Rationale:

- The binary uses two stack arguments and `retn 8`.
- The first argument is treated as a writable UTF-16 buffer.
- The second argument is a 32-bit unsigned loop bound.
- The body does not consume `ecx`, so it is not a `TextFilter` member or virtual method.
- `wchar_t *` is the best source-facing type for Windows wide-character caller buffers. Older `unsigned short *` documentation is behaviorally equivalent but less source-like.
- `SanitizeWideTextForChat` is inferred/descriptive because no recovered IDA source symbol exists, but it is behaviorally accurate and already used consistently by accepted caller reports.

Rejected signatures/names:

| Candidate | Decision | Reason |
| --- | --- | --- |
| `TextFilter::SanitizeWideTextForChat(...)` | Rejected | Callee does not read `ecx`; vtable has only the destructor slot. |
| `g_pTextFilter->SanitizeWideTextForChat(...)` | Rejected | Callers often read the singleton before the call, but the callee ABI is free-helper `__stdcall`-like stack cleanup. |
| `void SanitizeWideTextForChat(TextFilter *, wchar_t *, unsigned int)` | Rejected | `retn 8` proves exactly two stack arguments. |
| `NormalizeChatText` / `ApplyOutboundChatFilter` / `g_chatTextFilter` | Historical aliases only | Older local names are not current source-facing decisions and are superseded by MCP-backed helper evidence. |
| Packet/socket ownership | Rejected | The helper runs before packet serialization and does not touch socket state or packet structures. |

## Ownership And Source Placement

Accepted owner route:

- Direct semantic owner: [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) `SanitizeWideTextForChat`.
- Source file route: [UID:0000OO][TextFilter](by-file/TextFilter.md) `TextFilter`, currently `NexusTK/ui/controls/TextFilter.cpp`.
- Reason: the helper is adjacent to the `TextFilter` singleton lifecycle, participates in the same user-text filtering source cluster, and is already routed by the accepted `TextFilter` file. Its broad caller spread proves shared helper status, not caller ownership.

Rejected alternatives:

| Candidate | Decision | Reason |
| --- | --- | --- |
| [UID:0000EQ][TextFilter](by-class/TextFilter.md) `TextFilter` member/virtual method | Rejected | No `ecx` use, no member stack argument, and no vtable slot support this. |
| [UID:0000ON][TextEditPane](by-file/TextEditPane.md) `TextEditPane` | Rejected | Physical adjacency is outweighed by broad caller families and `TextFilter` source route. |
| Caller-feature files | Rejected | Article, mail, profile, say, shout, spell, and party-search callers are consumers. |
| Forced `util/TextFilter.cpp` route | Rejected as primary | No recovered path/symbol/source root proves utility placement; retain only as residual folder caveat. |
| No-owner/non-emitting | Rejected | The helper is live, reconstructable, routed through a source root, and eligible for first-draft C++. |

## First-Draft C++ Decision

This child clears the current reconstruction-code gate:

- `RECONSTRUCTABLE:TRUE`.
- Owner/emitter route through [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) to [UID:0000OO][TextFilter](by-file/TextFilter.md).
- `(93 + 94) / 2 > 85`.
- Exact range, padding, bytes, call sites, stack cleanup, no-callee/no-`ecx` evidence, and behavior are documented.

Formal C++ should use the accepted first-draft free-helper body:

```cpp
void __stdcall SanitizeWideTextForChat(wchar_t *text, unsigned int textLength)
{
    for (unsigned int i = 0; i < textLength; ++i) {
        wchar_t ch = text[i];
        if (ch == 0)
            break;

        if (ch == L'\n' || ch == L'\r' || (ch >= L' ' && ch <= 0x7f))
            continue;

        switch (ch) {
        case 0x00e9:
        case 0x00e8:
        case 0x00e0:
        case 0x00f9:
        case 0x00f4:
        case 0x00ee:
        case 0x00eb:
        case 0x00ef:
        case 0x00fb:
        case 0x00fa:
        case 0x00e2:
        case 0x00e7:
            continue;
        default:
            text[i] = L'?';
            break;
        }
    }
}
```

## Cross-References

- [UID:0000OO][TextFilter](by-file/TextFilter.md) `TextFilter`
- [UID:0000EQ][TextFilter](by-class/TextFilter.md) `TextFilter`
- [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) `SanitizeWideTextForChat`
- [UID:0000SG][g_pTextFilter](by-global/g_pTextFilter.md) `g_pTextFilter`
- [UID:0001JU][0x00595760-0x005958fe.TextFilterAndSanitizer](by-memory/0x00595760-0x005958fe.TextFilterAndSanitizer.md) `0x00595760-0x005958fe.TextFilterAndSanitizer`
- [UID:0000N9][SayInputPanes](by-file/SayInputPanes.md) `SayInputPanes`
- [UID:0000C3][SayInputPane](by-class/SayInputPane.md) `SayInputPane`

## Changes

- 2026-06-23 B003 accepted implementation callback:
  - Created this exact child page from the accepted `B003-report-text-filter-and-sanitizer-source-quality-0001JU-mcp-20260623-revision` split package.
  - Split the sanitizer helper out of [UID:0001JU][0x00595760-0x005958fe.TextFilterAndSanitizer](by-memory/0x00595760-0x005958fe.TextFilterAndSanitizer.md) and kept the global page as helper owner/index rather than a duplicate body emitter.
  - Incorporated current MCP session `ff68e691` evidence, exact bytes/SHA16, 14 call sites, stack-cleanup/no-`ecx` evidence, UTF-16 allowlist behavior, padding boundary, rejected alternatives, and first-draft C++ readiness.

## Current Evidence And Source Shape - 2026-08-03

- Validator-assigned identity is [UID:00050D] for exact modeled function `[0x005957c0,0x005958ca)`, size `0x10a`/266 bytes and SHA16 `d06aa427134dc3d3`; copied UID `00041J` remains canonical only for its unrelated MIDI page.
- ABI is a free `__stdcall` helper: it consumes `wchar_t *text` and `unsigned int textLength` from two stack arguments, does not read `ecx`, and ends with `retn 8`. It is not a `TextFilter` member or virtual method; the TextFilter vtable has only the compiler destructor slot.
- Exact callers are `0x0047762b`, `0x00477641`, `0x0047de2c`, `0x0047de3f`, `0x0053fe34`, `0x0059e4ae`, `0x005ae0cf`, `0x005b16af`, `0x005b1acf`, `0x005b1fef`, `0x005b23ff`, `0x005b3700`, `0x005b3ad0`, and `0x005b42e1`. They span article, mail, profile, spell, party-search, say/chat/recipient, and shout submission paths; caller context does not transfer helper ownership.
- Behavior is instruction-complete: iterate at most `textLength` UTF-16 code units, stop at NUL, preserve CR/LF and `0x20-0x7f`, preserve lowercase allowlist `00e9,00e8,00e0,00f9,00f4,00ee,00eb,00ef,00fb,00fa,00e2,00e7`, and replace every other code unit with `L'?'` in place.
- Formal CPP retains the complete human body and now records proven `__stdcall`. Formal H is blank here because [UID:0000EQ][TextFilter](by-class/TextFilter.md) owns the one header declaration; [UID:0000TK][SanitizeWideTextForChat](by-global/SanitizeWideTextForChat.md) routes `[[CHILDREN]]` without duplicating the body.
- Range protection: preceding `[0x005957b1,0x005957c0)` and following `[0x005958ca,0x005958d0)` are alignment. Owner/emitter remain UID0000TK and reconstructable remains true.
- Naming remains inference-backed rather than symbol-backed: `SanitizeWideTextForChat` is retained because it describes the exact in-place policy and is project-consistent; raw `sub_5957C0` and decompiler-local names are excluded from source.
