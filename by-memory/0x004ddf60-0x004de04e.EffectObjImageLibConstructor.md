*** UID:0002H9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004A | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
EffectObjImageLib::EffectObjImageLib()
    : LObject(),
      Singleton<EffectObjImageLib>(),
      effectInfoArray(10),
      effectPixMapInfoArray(10),
      effectFileInfo(0)
{
    LoadEffectTables();

    effectFileInfo = BuildEffectArchiveTable(L"EFFECT.EPF");
    if (effectPixMapInfoArray.count < effectFileInfo->frameCount)
    {
        DestroyDATFileMgr();
        DestroyExceptionHandler();
        MessageBoxW(0, L"Error on Effect File!", L"Data Error", MB_ICONEXCLAMATION);
        __loaddll(0);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact authored `EffectObjImageLib::EffectObjImageLib()` over `0x004ddf60-0x004de04e`: direct `LObject` then `Singleton<EffectObjImageLib>` bases, two capacity-10 protected arrays, `LoadEffectTables`, `BuildEffectArchiveTable(L"EFFECT.EPF")`, shared `ArchiveMetadataTable::frameCount` validation, and the exact fatal cleanup/message/termination path. The observed `this+4` publication/fallback clear is compiler lowering of the direct empty Singleton base, not handwritten global source; one startup caller, exact padding, resources, bytes, and historical evidence are preserved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ddf60-0x004de04e EffectObjImageLib Constructor

## Status

- Address range: `0x004ddf60-0x004de04e` (`[start,end)`, IDA size `0xee`).
- Entity kind: class constructor / singleton initializer.
- Owner class: [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md).
- Owner file: [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md).
- Parent attachment: [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md) carries the complete direct-base class declaration; this exact constructor page is `92/94`.
- Rebuild handling: `source-authored`.
- Confidence: very strong for boundary, direct base order, singleton compiler lowering, vtable setup, one-caller startup use, loader/archive calls, shared archive metadata field, resource strings, and fatal mismatch behavior; exact original private spellings remain the bounded cap.
- Formal C++ is present because UID0002H9 is reconstructable, has emitter [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md), clears the current combined-score/emitter source-quality gate, and the remaining protected-array/archive/helper-name uncertainty is a score/confidence cap rather than a constructor-code blocker.

## Boundary Evidence

- MCP session `3fa0535f` models `sub_4DDF60` at `0x004ddf60`, size `0xee`, ending exactly at `0x004de04e`.
- `lookup_funcs 0x004de04e` finds no function and `get_bytes 0x004de04e` returns `cc cc`, confirming padding/alignment before the successor.
- The local export reports one direct caller, `0x004f6113` in startup initialization.
- The local export reports six direct callees: base [UID:0000KV][LObject](by-file/LObject.md) construction at `0x004f4a80`, [UID:0002HB][0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables](by-memory/0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables.md), [UID:0000U4][BuildEffectArchiveTable_004DE420](by-item/BuildEffectArchiveTable_004DE420.md), two error-prep helpers at `0x00467380/0x004673a0`, and `__loaddll`.
- `lookup_funcs 0x004de050` finds the successor ordinary destructor, size `0x13a`, so no destructor byte belongs to this constructor.
- The constructor is an exact child of the broader [UID:00017K][0x004ddf60-0x004dec30.EffectObjImageLibLocalMethodCluster](by-memory/0x004ddf60-0x004dec30.EffectObjImageLibLocalMethodCluster.md) ownership page.

## Behavior

`EffectObjImageLib::EffectObjImageLib` constructs the global effect image library instance. The constructor:

- calls direct base `LObject` construction at `0x004f4a80` and constructs empty direct base `Singleton<EffectObjImageLib>` at `this+4`;
- causes the compiler to publish `this` through the Singleton base at `0x0069b44c`, with a null cleanup path for failed construction;
- writes the [UID:0001XG][EffectObjImageLibVtable](by-type/by-vtable/EffectObjImageLibVtable.md);
- initializes `ProtectedArray<EffectInfo>` at object offset `+0x04` with capacity `10`;
- initializes `ProtectedArray<EffectPixMapInfo>` at object offset `+0x48` with capacity `10`;
- calls `EffectObjImageLib::LoadEffectTables` at `0x004de7d0`;
- calls `BuildEffectArchiveTable_004DE420(L"EFFECT.EPF")` and stores the returned archive metadata table at object offset `+0x5c`;
- compares the remap count at object offset `+0x4c` against the archive frame count and shows `Error on Effect File!` before terminating when the effect resources do not match.

The formal source declares direct `LObject()` and `Singleton<EffectObjImageLib>()` base initializers. RTTI names both direct bases and the Singleton base descriptor has PMD `+4/-1/0`; the constructor's adjusted publication and fallback clear are therefore compiler lowering of that empty base. Neither `g_pEffectObjImageLib = this` nor the decompiler's artificial `this == -4` path belongs in authored source.

## Offset Writes

| Offset / Global | Meaning | Constructor evidence |
| --- | --- | --- |
| `0x0069b44c` | `g_pEffectObjImageLib` singleton backing | Compiler-lowered direct Singleton-base publication of `this`, with a null constructor-unwind cleanup path. |
| `+0x00` | `EffectObjImageLib` vptr | Writes `EffectObjImageLib::vftable`; [UID:0001XG][EffectObjImageLibVtable](by-type/by-vtable/EffectObjImageLibVtable.md) confirms the vtable at `0x0061b724`. |
| `+0x04` | `ProtectedArray<EffectInfo>` subobject vptr | Writes `ProtectedArray<EffectInfo>` vtable. |
| `+0x08/+0x0c/+0x10` | `EffectInfo` array storage/count/capacity | Zeroes storage/count and sets capacity to `10`. |
| `+0x20/+0x38` | scratch/fallback descriptor bytes | Zeroes the documented byte fields inside the scratch [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md). |
| `+0x48` | `ProtectedArray<EffectPixMapInfo>` subobject vptr | Writes `ProtectedArray<EffectPixMapInfo>` vtable. |
| `+0x4c/+0x50/+0x54` | remap array count/storage/capacity | Zeroes storage/count and sets capacity to `10`; later validation reads `+0x4c` as the loaded remap count. |
| `+0x5c` | effect EPF archive metadata table | Stores the result from `BuildEffectArchiveTable_004DE420(L"EFFECT.EPF")`. |

See [UID:0001U9][EffectObjImageLibLayout](by-type/by-struct/EffectObjImageLibLayout.md) for the full object layout.

## IDA Evidence

- MCP session `3fa0535f` was active on `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `ok`, Hex-Rays ready, strings cache ready, and auto-analysis ready.
- `lookup_funcs` facts: `0x004ddf60` is `sub_4DDF60`, size `0xee`; `0x004de04e` is not a function and has `cc cc` padding; `0x004de050` is the successor destructor; `0x004f6113` is inside startup function `sub_4F5FB0`; `0x0061b724` and `0x0069b44c` are data, not functions.
- `analyze_function 0x004ddf60` reports prototype `_DWORD *__thiscall(_DWORD *this)`, one caller `sub_4F5FB0`, and callees `__loaddll`, `MessageBoxW`, `sub_4F4A80`, `sub_4DE7D0`, `sub_4DE420`, `sub_467380`, and `sub_4673A0`.
- The decompilation writes `dword_69B44C`, `EffectObjImageLib::vftable`, `ProtectedArray<EffectInfo>::vftable`, and `ProtectedArray<EffectPixMapInfo>::vftable`.
- The decompilation zeroes the embedded `ProtectedArray` storage/count fields, writes initial capacity `10` for both the `EffectInfo` and `EffectPixMapInfo` arrays, and zeroes descriptor scratch bytes at offsets `+0x20` and `+0x38`.
- The decompilation calls `sub_4DE7D0` at `0x004ddffb` and `sub_4DE420(L"EFFECT.EPF")` at `0x004de007`.
- The decompilation stores the archive table pointer into `this[23]` (`+0x5c`) and validates `this[19]` (`+0x4c`) against the first archive-table word.
- Shared UID0000UN names that first archive-table word `ArchiveMetadataTable::frameCount`; the earlier constructor-local `entryCount` spelling was stale and is not retained in formal source.
- `xrefs_to 0x004ddf60` reports exactly one direct code xref at `0x004f6113` in the application startup path.
- `xrefs_to 0x004de7d0` and `xrefs_to 0x004de420` each report one constructor call, so the loader and archive-table bodies stay in their own child/support pages rather than being inlined here.
- `get_string 0x0061c1b8` returns `EFFECT.EPF`; `get_string 0x0061c1d0` returns `Error on Effect File!`; `get_bytes 0x0061b8a8` decodes UTF-16LE `Data Error`.
- `analyze_function 0x00467380` and `0x004673a0`, plus executed B001 helper reports, support source-facing cleanup calls `DestroyDATFileMgr()` and `DestroyExceptionHandler()` immediately before `MessageBoxW` and `__loaddll(0)`.
- [UID:0001PQ][0x0069b44c-0x0069b450.g_pEffectObjImageLib](by-memory/0x0069b44c-0x0069b450.g_pEffectObjImageLib.md) independently confirms the constructor publish site at `0x004ddf9f` and constructor clear site at `0x004ddfa6`.

## Callers And Callees

| Direction | Address | Role |
| --- | --- | --- |
| caller | `0x004f6113` | application startup constructs the effect image library singleton |
| callee | `0x004f4a80` | base `LObject` construction |
| callee | `0x004de7d0` | loads `EFFECT.TBL` and `EFFECT.FRM` |
| callee | `0x004de420` | builds effect EPF archive metadata |
| callee | `0x00467380` / `0x004673a0` | fatal effect-file mismatch cleanup, source-facing `DestroyDATFileMgr` / `DestroyExceptionHandler` |
| callee | `MessageBoxW` | displays the effect-file mismatch error |
| callee | `__loaddll` | termination path after effect-file mismatch |

## Touched State

- [UID:0000QT][g_pEffectObjImageLib](by-global/g_pEffectObjImageLib.md) / [UID:0001PQ][0x0069b44c-0x0069b450.g_pEffectObjImageLib](by-memory/0x0069b44c-0x0069b450.g_pEffectObjImageLib.md)
- [UID:0001U9][EffectObjImageLibLayout](by-type/by-struct/EffectObjImageLibLayout.md)
- [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md)
- [UID:0001UA][EffectPixMapInfo](by-type/by-struct/EffectPixMapInfo.md)
- [UID:0000U4][BuildEffectArchiveTable_004DE420](by-item/BuildEffectArchiveTable_004DE420.md)
- [UID:0002HB][0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables](by-memory/0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables.md)
- `EFFECT.TBL`, `EFFECT.FRM`, and `EFFECT.EPF` resource family.

## Reconstruction Notes

This is NexusTK-owned source and emits the accepted constructor body. The source block uses support-doc field/helper names (`effectInfoArray`, `effectPixMapInfoArray`, `effectFileInfo`, `BuildEffectArchiveTable`, `DestroyDATFileMgr`, `DestroyExceptionHandler`) rather than raw decompiler temporaries, and uses the shared `ArchiveMetadataTable::frameCount` contract. Remaining exact original spellings for private fields and the `ProtectedArray` constructor/member API are confidence caps, not code blockers.

Rejected alternatives preserved for source-quality review:

- Do not move ownership to Application startup. Startup has the only direct caller, but it allocates the dependency; the constructor body owns EffectObjImageLib state.
- Do not move the constructor to generic `ImageLib`, `ImageFrameTable`, `EPFImageResources`, or `ProtectedArray`; those are dependencies/support declarations only.
- Do not emit a standalone `ClearEffectObjImageLibSingleton` API for the constructor fallback clear; [UID:00017W][0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper](by-memory/0x004e5b70-0x004e5b7b.EffectObjImageLibSingletonClearHelper.md) documents that compiler/static cleanup glue separately.
- Do not emit explicit `g_pEffectObjImageLib = this`; direct `Singleton<EffectObjImageLib>` inheritance is the human source contract that generates the observed publication.
- Do not encode the decompiler's artificial `this == -4` path in formal source.
- Do not inline `LoadEffectTables` or `BuildEffectArchiveTable` bodies into this constructor; those remain separately documented targets/support pages.

## Score Rationale

- Completion is `92` because formal source now expresses the complete authored constructor, direct bases, shared frame-count contract, and exact error/resource behavior while excluding compiler-only publication and unwind details.
- Confidence is `94` because the constructor is a single exact `0xee`-byte method with one startup caller, direct RTTI/PMD base proof, constructor-only loader/archive helper calls, literal-backed fatal validation, and corroborated layout/global effects. Exact original private and `ProtectedArray` spellings remain the only caps.

## Cross-References

- [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md)
- [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md)
- [UID:00017K][0x004ddf60-0x004dec30.EffectObjImageLibLocalMethodCluster](by-memory/0x004ddf60-0x004dec30.EffectObjImageLibLocalMethodCluster.md)
- [UID:0001PQ][0x0069b44c-0x0069b450.g_pEffectObjImageLib](by-memory/0x0069b44c-0x0069b450.g_pEffectObjImageLib.md)
- [UID:0001U9][EffectObjImageLibLayout](by-type/by-struct/EffectObjImageLibLayout.md)
- [UID:0001XG][EffectObjImageLibVtable](by-type/by-vtable/EffectObjImageLibVtable.md)
- [UID:0002HB][0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables](by-memory/0x004de7d0-0x004dec23.EffectObjImageLibLoadEffectTables.md)
- [UID:0000U4][BuildEffectArchiveTable_004DE420](by-item/BuildEffectArchiveTable_004DE420.md)
- [UID:0000KV][LObject](by-file/LObject.md)

## Changes

- 2026-07-21 B004 UID0001PQ source-quality callback:
  - Raised `91/92` to `92/94` and replaced only the source-shape defects in the managed constructor: added direct `LObject()` and `Singleton<EffectObjImageLib>()` base initializers, removed the explicit singleton assignment, and changed stale `entryCount` to shared `ArchiveMetadataTable::frameCount`.
  - Preserved exact boundary/padding, sole startup caller, all callees, two capacity-10 arrays, scratch initialization, `EFFECT.EPF`, mismatch comparison, cleanup helpers, message strings, and `__loaddll(0)` behavior.
  - Added direct RTTI/PMD `+4/-1/0` and EBO evidence that classifies publication/fallback clear as compiler lowering rather than authored global source. Historical explicit-assignment and decompiler-guard interpretations remain documented as rejected alternatives.

- 2026-06-29 B002 implementation callback for accepted UID0002H9 report:
  - What existed before: the page was `88/90`, had correct owner/emitter/reconstructable metadata, and recorded strong constructor evidence, but the formal C++ block was empty and the status still used stale final-code-gate wording.
  - Changed to: `COMPLETION:91`, `CONFIDENCE:92`, formal `EffectObjImageLib::EffectObjImageLib()` C++ inserted, owner/emitter/reconstructable metadata unchanged.
  - Summary/evidence: MCP session `3fa0535f` confirmed exact `0x004ddf60-0x004de04e` body, `cc cc` padding at `0x004de04e`, one startup caller at `0x004f6113`, singleton publish/fallback clear evidence, vtable/protected-array setup with initial capacity `10`, constructor-only `LoadEffectTables` and `BuildEffectArchiveTable(L"EFFECT.EPF")`, archive-count validation, `DestroyDATFileMgr`/`DestroyExceptionHandler`, `MessageBoxW(0, L"Error on Effect File!", L"Data Error", MB_ICONEXCLAMATION)`, and `__loaddll(0)`. Rejected owner/no-code alternatives are preserved; the artificial `this == -4` branch is not encoded as source.
- 2026-06-06 A005 documentation pass:
  - What existed before: the page was `72/86`, unparented, and recorded only a compact constructor summary.
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`, and `AUTOGEN_PARENT_UID:00004A`.
  - Summary/evidence: local IDA export confirms exact `0xee`-byte method range, one startup caller, six direct callees, singleton publish/clear behavior, vtable/protected-array setup, `EFFECT.TBL`/`EFFECT.FRM` load call, `EFFECT.EPF` archive metadata build, `+0x4c` versus archive frame-count validation, and the fatal mismatch path. This older pass left final C++ blank; the 2026-06-29 B002 callback supersedes that source-code deferral.
