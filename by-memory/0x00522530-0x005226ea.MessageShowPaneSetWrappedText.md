*** UID:0001C4 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000087 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000087 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MessageShowPane::SetWrappedText(wchar_t *messageText)
{
    RemoveFromLayer();

    m_lineCount = 0;
    m_wrappedLineCount = 0;
    m_maxTextWidth = 0;

    wcscpy_s(m_messageText, 0x8000, messageText);

    wchar_t textCopy[0x8000];
    wcscpy_s(textCopy, 0x8000, messageText);

    wchar_t *lineStart = textCopy;
    wchar_t *lineEnd = wcsstr(lineStart, g_wszNewline);
    while (lineEnd != NULL)
    {
        *lineEnd = 0;
        ++m_lineCount;
        ++m_wrappedLineCount;

        const int lineWidth = GetTextWidth(lineStart);
        if (m_maxTextWidth < lineWidth)
            m_maxTextWidth = lineWidth;

        m_wrappedLineCount += (lineWidth - 1) / 288;
        lineStart = lineEnd + 1;
        lineEnd = wcsstr(lineStart, g_wszNewline);
    }

    ++m_lineCount;
    ++m_wrappedLineCount;

    const int finalLineWidth = GetTextWidth(lineStart);
    if (m_maxTextWidth < finalLineWidth)
        m_maxTextWidth = finalLineWidth;

    m_wrappedLineCount += (finalLineWidth - 1) / 288;

    if (m_maxTextWidth > 288)
        m_maxTextWidth = 288;

    RectBounds bounds;
    bounds.left = 806 - m_maxTextWidth;
    bounds.top = 10;
    bounds.right = 809;
    bounds.bottom = 3 * (m_lineCount + 4 * m_wrappedLineCount + 3);
    AddToLayer(&bounds, 0, 0, g_pRenderLayer1);
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source `MessageShowPane::SetWrappedText` at position 50. One packet-helper caller updates an existing overlay; the method removes/re-adds the pane, resets metrics, copies 0x8000-wide text, performs newline/GetTextWidth wrap math and 288 clamp, constructs RectBounds, and invalidates inherited m_visibleBounds. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00522530-0x005226ea MessageShowPaneSetWrappedText

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: `MessageShowPane` method/helper.
- Owner: [UID:000087][MessageShowPane](by-class/MessageShowPane.md), verified by IDA xrefs and `this`-layout behavior. Current generated `simroot_v2` agrees, but older Wave2/Wave3 data attached this helper to `FittingRoomDownloadControlPane`.
- Likely source module: [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md)
- Confidence: very strong for class ownership, exact bounds, only direct caller, current helper/field contracts, behavior, and standalone source route.
- Autogen status: attached to [UID:000087][MessageShowPane](by-class/MessageShowPane.md) at position `50`; the managed formal block uses the same accepted text metric/layout contracts as constructor UID0004HN.

## Covered Range

| Range | Current or proposed name | Notes |
| --- | --- | --- |
| `0x00522530-0x005226ea` | `MessageShowPane::SetWrappedText` | Replaces overlay text, recomputes line/wrap metrics, reapplies pane bounds, and refreshes child text rows. |

## Behavior

IDA MCP decompilation rechecked on 2026-05-31 shows this method:

- calls inherited `RemoveFromLayer()` through vtable offset `0x38`;
- clears line count, wrapped-line count, and maximum-width fields at `this + 0x100f8`, `this + 0x100fc`, and `this + 0x10100`;
- copies the incoming wide text into the stored message buffer at `this + 0xf8` and into a local 32768-character scratch buffer;
- splits the scratch copy on the shared newline substring, measures each segment through inherited `GetTextWidth` at `0x004baa70`, and adds extra wrapped rows for segments wider than 288 pixels;
- clamps maximum width to 288 pixels;
- builds a `RectBounds` value from `806 - maxWidth`, top `10`, right `809`, and computed height;
- applies the new bounds through `AddToLayer` at vtable offset `0x30` with render layer global `g_pRenderLayer1`, then invalidates inherited `m_visibleBounds` through vtable offset `0x20`.

## Evidence

Checked on 2026-05-26 and rechecked on 2026-05-31:

- `lookup_funcs 0x00522530` reports size `0x1ba`, so the end-exclusive range is `0x005226ea`.
- `lookup_funcs 0x005226ea` reports `Not a function`, confirming the page end is not a hidden child start.
- `callers 0x00522530` and `xrefs_to 0x00522530` report one direct caller at `0x005ac150` inside opcode `0x58` external packet helper [UID:0003UU][0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket](by-memory/0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket.md).
- `callees 0x005ac070` includes `0x00521da0` for new overlay construction and `0x00522530` for existing overlay update.
- `callees 0x00522530` reports `0x004baa70` text measurement, `0x004b78d0` rectangle construction, `_wcsstr`, `_wcscpy_s`, stack probing, and security-cookie support.
- Current `source-3/simroot_v2/class_MessageShowPane.cpp` emits `MessageShowPane::SetWrappedText` at this address. Older generated/cache evidence attached the helper to `FittingRoomDownloadControlPane`; keep that historical owner as a data issue only.

## 2026-06-07 Batch 007 IDA Audit

- IDA MCP `py_eval` confirms this page is exactly one modeled function: `sub_522530` at `0x00522530-0x005226ea`, size `0x1ba`, with `136` instructions and `7` basic blocks. The start is a function start, the end is not a function start, and `0x005226ea-0x005226f0` is six `0xcc` bytes before the next MessageShowPane cleanup helper at `0x005226f0`.
- The preceding modeled function is `0x00522030-0x00522530`, so the start boundary is also the previous function end. Neighbor review shows `0x005226f0-0x005226fb` and `0x005226fb-0x00522706` are the adjacent short cleanup/thunk functions, while the scalar deleting destructor starts at `0x00522720`.
- The only direct code xref remains `0x005ac150` inside [UID:0003UU][0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket](by-memory/0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket.md). B011's source-quality recheck shows that helper is reached from `UserPane::OnServerMessage` opcode `0x58`, decodes `payload[1]` as show/close, `payload+2` as a big-endian text byte count, and `payload+4` as narrow packet text, converts through `MultiByteToWideChar(CP_ACP,0,...)` into a local `WCHAR[32768]`, calls this method when `dword_69B4F4` is already non-null, and otherwise allocates `65796` bytes and calls the constructor at `0x00521da0`.
- The method reserves `0x10014` bytes of stack scratch space, copies the source text into the object buffer at `this + 0xf8` and into a local `wchar_t[32768]`, clears counters at `this + 0x100f8`, `this + 0x100fc`, and `this + 0x10100`, then uses the UTF-16 newline literal `SubStr` at `0x00610668` for line splitting.
- The wrapping loop calls `GetTextWidth`/`sub_4BAA70`, adds `(width - 1) / 288` wrapped rows, clamps stored maximum width to `288`, builds a `RectBounds` value through `sub_4B78D0` with left `806 - maxWidth`, top `10`, right `809`, and height `3 * (lineCount + 4 * wrappedLineCount + 3)`, applies it through `AddToLayer`/vtable slot `0x30` using `g_pRenderLayer1`, then calls `InvalidateRect(&m_visibleBounds)` through vtable slot `0x20`.
- Global review ties `SubStr` xrefs to both the constructor and this update method; `dword_69B364` is read by the constructor and this method as the render-layer/bounds argument; `dword_69B4F4` is read/written by constructor/destructor paths and the message packet handler. This confirms the method updates an existing `MessageShowPane` instance rather than a separate generated-owner class.

## Source Reconstruction

The managed formal C++ block records the accepted `MessageShowPane::SetWrappedText(wchar_t *messageText)` source shape. It uses `m_messageText`, `m_lineCount`, `m_wrappedLineCount`, `m_maxTextWidth`, `RectBounds`, `GetTextWidth`, `g_wszNewline`, `g_pRenderLayer1`, `RemoveFromLayer`, `AddToLayer`, and inherited `m_visibleBounds`.

This source block deliberately omits compiler vtable mechanics and keeps [UID:0003UU][0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket](by-memory/0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket.md) as the external packet-helper caller/dependency, not as owner. The older `FittingRoomDownloadControlPane` generated ownership remains rejected history.

Historical `PaneRect`, `MeasureTextWidth`, and `m_bounds` spellings are superseded by current `RectBounds`, `GetTextWidth`, and inherited `m_visibleBounds` support. They remain search/provenance evidence only.

## Score Rationale

| Metric | Value | Rationale |
| --- | --- | --- |
| Completion | `92` | Exact bounds, caller, callees, stack buffers, fields, newline/global usage, wrap and rectangle math, inherited invalidation, neighboring boundaries, rejected aliases, and final formal C++ are documented. |
| Confidence | `94` | IDA, current RectBounds/GrafPort/Pane support, packet-helper context, singleton/global relationships, and class/file route agree. |
| Assignment | `CANONICAL_OWNER:000087`, `EMITTER_UIDS:000087` | Direct owner [UID:000087][MessageShowPane](by-class/MessageShowPane.md) and this child are above the current code-entry gate, and the only caller plus `this`-layout/global evidence directly support class ownership and emission through the class route. |

## Cross-References

- [UID:000087][MessageShowPane](by-class/MessageShowPane.md)
- [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md)
- [UID:0001C3][0x00520e30-0x005227c6.MessageAndMessageShowPane](by-memory/0x00520e30-0x005227c6.MessageAndMessageShowPane.md)
- [UID:0002Y8][0x00521da0-0x005227c6.MessageShowPaneMethods](by-memory/0x00521da0-0x005227c6.MessageShowPaneMethods.md)
- [UID:0004HN][0x00521da0-0x00521fc0.MessageShowPaneConstructor](by-memory/0x00521da0-0x00521fc0.MessageShowPaneConstructor.md)
- [UID:0004HO][0x00522030-0x00522530.MessageShowPaneOnPaint](by-memory/0x00522030-0x00522530.MessageShowPaneOnPaint.md)
- [UID:0000RK][g_pMessageShowPane](by-global/g_pMessageShowPane.md)
- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)
- [Wave3 data issues](../wave3_data_issues.md)

## Changes

- 2026-07-16 B002 UID0003PC implementation callback:
  - Raised `88/91` to `92/94`, retained owner/emitter UID000087 and reconstructable true, and set emitter position `50`.
  - Installed the exact while-loop/GetTextWidth/RectBounds/m_visibleBounds formal body while preserving the sole packet caller, buffers, field offsets, wrap math, bounds, layer attach, invalidation, neighboring ranges, and historical owner evidence.
  - Historicalized PaneRect, MeasureTextWidth, and `m_bounds` as stale source-facing aliases.
- 2026-07-06 B004 implementation callback:
  - Raised completion/confidence from `86/90` to `88/91`.
  - Inserted formal first-draft `MessageShowPane::SetWrappedText(wchar_t *messageText)` C++ using the accepted constructor-parallel text metric/layout names.
  - Preserved the single packet-helper caller at `0x005ac150`, the rejected historical `FittingRoomDownloadControlPane` ownership, and the `g_pMessageShowPane`/render-layer evidence.
- 2026-06-21 Rule 26 support sync from B011 [UID:0003UU][0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket](by-memory/0x005ac070-0x005ac1a8.LivingObjectPaneHandleMessageShowPacket.md):
  - Updated the only-caller description from generic packet handler to opcode `0x58` external `HandleMessageShowPacket`, including payload layout and conversion/update/create context.
- 2026-05-31: Reclassified metadata from blank reconstructable with `70/85` scoring to `RECONSTRUCTABLE:TRUE`, completion `82`, and confidence `88`; left `AUTOGEN_PARENT_UID` blank. Summary/evidence: live IDA MCP `lookup_funcs`, `callers`, `xrefs_to`, `callees`, and `decompile` verified the exact range, only direct caller, callee set, class-owned `this` layout, and overlay update behavior. C++ reconstruction remains blank because field names and final source expression shape are not `95+` audited, and parent autogen remains blank because `MessageShowPane` has no validated projected file-root chain yet.
- 2026-06-07 Batch 007: changed completion/confidence from `82/88` to `86/90` and set `AUTOGEN_PARENT_UID:000087`. Summary/evidence: IDA MCP audit confirmed one exact `0x1ba` function, `136` instructions, `7` basic blocks, six trailing padding bytes, the single packet-handler caller, the `dword_69B4F4` existing/new overlay branch, `SubStr` newline xrefs, `dword_69B364` layer argument, and exact `this` field offsets. Direct parent [UID:000087][MessageShowPane](by-class/MessageShowPane.md) is `85/85`, so the corrected child/direct-parent assignment gate is satisfied; final C++ remains blank below the `95+` gate.
