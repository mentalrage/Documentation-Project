*** UID:0002PV | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007H | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007H | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool LogoPlayerPane::OnKeyEvent(const KeyEvent *event)
{
    if (event->type == kEventKeyDown) {
        const unsigned char key = event->keyCode;
        if (key == VK_RETURN || key == VK_ESCAPE || key == VK_SPACE)
            AdvanceToNextSegment();
    }

    return true;
}

bool LogoPlayerPane::OnMouseEvent(const MouseEvent *event)
{
    if (event->type == kEventLeftButtonUp)
        AdvanceToNextSegment();

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA reconfirms LogoPlayerPane key/mouse skip handler bounds 0x004f55d0-0x004f561d, vtable-only entry points, no direct callers, single callee LogoPlayerPaneAdvanceToNextSegment, secondary this-0xa0 normalization, key event type 8 with Enter/Escape/Space dispatch, mouse event type 3 dispatch, always-handled returns, padding before/between/after, unresolved final event/interface names, and first-draft formal C++ is populated. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f55d0-0x004f561d LogoPlayerPane Input Skip Handlers

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: adjacent virtual input handlers.
- Owner class: [UID:00007H][LogoPlayerPane](by-class/LogoPlayerPane.md).
- Parent aggregate: [UID:00019D][0x004f53b0-0x004f570c.LogoPlayerPane](by-memory/0x004f53b0-0x004f570c.LogoPlayerPane.md).

## Covered Ranges

| Range | Method | Notes |
| --- | --- | --- |
| `0x004f55d0-0x004f55fc` | `LogoPlayerPane::OnKeyEvent` | For EventMan type `8` (`KeyDown`), translated key byte Enter, Escape, or Space calls `AdvanceToNextSegment`; the handler returns `1` even when it does not advance. |
| `0x004f55fc-0x004f5600` | alignment | Four `0xcc` bytes. |
| `0x004f5600-0x004f561d` | `LogoPlayerPane::OnMouseEvent` | For EventMan type `3` (`LeftButtonUp` / left-button release), calls `AdvanceToNextSegment`; the handler returns `1` even when it does not advance. |

## Signature Notes

- Both handlers are `__thiscall` virtual bodies reached through a secondary pane/event interface view of the object.
- The explicit event argument is a pointer-like record. Offset `+0x4` is the event type byte; the key handler also reads offset `+0x8` as the translated key byte.
- Both handlers adjust `ecx` by `-0xa0` before calling [UID:0002PU][0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment](by-memory/0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment.md), normalizing from the secondary interface `this` pointer back to the `LogoPlayerPane` owner. The callee is now resolved as `void LogoPlayerPane::AdvanceToNextSegment()`, with first-draft C++ populated on the callee page.
- The best source-facing signatures are `bool LogoPlayerPane::OnKeyEvent(const KeyEvent *event)` and `bool LogoPlayerPane::OnMouseEvent(const MouseEvent *event)`. `KeyEvent` and `MouseEvent` are descriptive, not yet final original type names.

## IDA MCP Evidence

- `lookup_funcs` refreshed on 2026-06-06 reports `0x004f55d0` as `sub_4F55D0` size `0x2c`, `0x004f5600` as `sub_4F5600` size `0x1d`, `0x004f5570` as size `0x58`, and the next promoted function `0x004f5620` as size `0x14`.
- Decompilation of `0x004f55d0` checks `*(event + 4) == 8`, reads the key byte from `event + 8`, accepts key codes `13`, `27`, and `32`, calls `sub_4F5570((int)this - 160)` at `0x004f55f1` on those keys, then returns `1`.
- Disassembly of `0x004f55d0` matches that control flow: `cmp byte ptr [eax+4], 8`, key compares against `0Dh`, `1Bh`, and `20h`, `add ecx, 0FFFFFF60h`, `call sub_4F5570`, `mov al, 1`, `retn 4`.
- Decompilation of `0x004f5600` checks `*(event + 4) == 3`, calls `sub_4F5570((int)this - 160)` at `0x004f5612` only on that event type, then returns `1`.
- Disassembly of `0x004f5600` matches that control flow: `cmp byte ptr [eax+4], 3`, `add ecx, 0FFFFFF60h`, `call sub_4F5570`, `mov al, 1`, `retn 4`.
- `xrefs_to` refreshed on 2026-06-06 reports only vtable data xrefs for the handlers: `0x0061d078` to `0x004f55d0` and `0x0061d074` to `0x004f5600`.
- `callers` refreshed on 2026-06-06 reports no direct code callers for either handler, consistent with virtual dispatch only.
- `callees` refreshed on 2026-06-06 reports both handlers call only `0x004f5570`; `xrefs_to 0x004f5570` reports exactly these two handler callsites at `0x004f55f1` and `0x004f5612`.
- Raw bytes refreshed through IDA on 2026-06-06 confirm `0x004f55c8-0x004f55d0`, `0x004f55fc-0x004f5600`, and `0x004f561d-0x004f5620` are `0xcc` alignment, and bytes at `0x004f5620` begin the next function (`56 8b f1 e8`).
- 2026-06-17 B001 source-quality reanalysis reconfirmed key handler size `0x2c`, mouse handler size `0x1d`, no direct code callers, vtable-only reachability through slots `0x0061d078` and `0x0061d074`, and exactly two code xrefs to `LogoPlayerPaneAdvanceToNextSegment`.
- The same B001 pass traced EventMan producers: `0x004a96a0` dispatches type `8` from the key-down path after key translation, `0x004a98a0` constructs key-release type `9`, `0x004a9120` dispatches left-button down/double-click types `1`/`2`, and `0x004a9260` dispatches type `3` before the cursor-state type `0` update. This closes the target event semantics as `KeyDown` and left-button release/up.
- B008's [UID:00014C][0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers](by-memory/0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers.md) report consolidates the same map: type `8` is `kEventKeyDown`, type `9` is `kEventKeyUp`, type `3` is `kEventLeftButtonUp`, and `0x004a8b10` is an EventMan key-translation adaptor rather than CRT/locale support.
- Numeric checks for `0xa0`, `0x0d`, `0x1b`, and `0x20` were verified with `tools/int_convert.py`.

## Reconstruction Notes

These handlers are small source-authored virtual methods, not generic input dispatch and not dead vtable slots. The `this - 0xa0` adjustment confirms they are reached through the Pane/EventHandler secondary view at object offset `+0xa0` and normalize back to the primary `LogoPlayerPane` object before calling the derived segment-advance helper.

Use the always-handled return behavior when reconstructing; the methods suppress further handling even when the event type/key does not trigger segment advance. The formal C++ block is now populated; exact event type names and secondary interface names remain confidence caps, not empty-emitter blockers.

## Resolved Source-Quality Questions

- Event type `8` is the high-probability source `KeyDown` event. EventMan builds it in the key-down path, stores the translated key byte at the payload field consumed here as `event+0x08`, and uses type `9` for the key-release construction path.
- Event type `3` is left-button release/up. EventMan separately dispatches left-button down/double-click as `1`/`2`, right release as `6`, wheel as `7`, and cursor updates as `0`.
- `event+0x08` in the key handler is the translated key byte. Enter/Return (`0x0d`), Escape (`0x1b`), and Space (`0x20`) advance the segment.
- `this - 0xa0` is the normal Pane/EventHandler secondary-interface adjustment for a `LogoPlayerPane` override, not evidence for a separate input-handler owner.
- The lack of direct code callers is expected virtual reachability: both entry points are referenced from `LogoPlayerPane` secondary vtable slots and dispatched through the event system.
- The combined range can remain a source-bearing adjacent virtual-method pair. A future exact split into separate key/mouse method pages would be safe but is not required to resolve ownership, source placement, or score.

## Final C++ Coordination

Formal C++ is now populated by B008 2026-06-30 using the existing `KeyEvent`, `MouseEvent`, `kEventKeyDown`, and `kEventLeftButtonUp` support names. The older wait-for-shared-declarations recommendation is superseded; the following source shape is now reflected in the formal block:

```cpp
bool LogoPlayerPane::OnKeyEvent(const KeyEvent *event)
{
    if (event->type == kEventKeyDown) {
        const unsigned char key = event->keyCode;
        if (key == 0x0d || key == 0x1b || key == 0x20)
            AdvanceToNextSegment();
    }

    return true;
}

bool LogoPlayerPane::OnMouseEvent(const MouseEvent *event)
{
    if (event->type == kMouseLeftButtonUp)
        AdvanceToNextSegment();

    return true;
}
```

## Cross-References

- [UID:00007H][LogoPlayerPane](by-class/LogoPlayerPane.md)
- [UID:00019D][0x004f53b0-0x004f570c.LogoPlayerPane](by-memory/0x004f53b0-0x004f570c.LogoPlayerPane.md)
- [UID:0002PU][0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment](by-memory/0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment.md)

## Changes

- 2026-06-30 B008 UID0000O4 implementation callback: changed score from `87/90` to `88/91`, populated first-draft `LogoPlayerPane::OnKeyEvent` and `LogoPlayerPane::OnMouseEvent` C++, preserved vtable-only/no-direct-caller and `this-0xa0` normalization evidence, and used EventMan type `8` key-down plus type `3` left-button release/up support.
- 2026-06-17 B001 source-quality execution:
  - What existed before: `COMPLETION:82`, `CONFIDENCE:88`, with open questions around event types `8`/`3`, secondary input-interface meaning, no-direct-caller treatment, and final C++ readiness.
  - Changed to: `COMPLETION:87`, `CONFIDENCE:90`; owner/emitter remains [UID:00007H][LogoPlayerPane](by-class/LogoPlayerPane.md), `RECONSTRUCTABLE:TRUE`, and formal C++ remained blank by that then-current accepted do-not-populate recommendation; superseded by B008 2026-06-30 formal `OnKeyEvent`/`OnMouseEvent` population.
  - Summary/evidence: B001 live IDA reanalysis validated exact key/mouse function bounds, vtable-only xrefs, no direct callers, only `LogoPlayerPaneAdvanceToNextSegment` as callee, EventMan type `8` as key-down with translated key byte, EventMan type `3` as left-button release/up, `this-0xa0` as Pane/EventHandler secondary view normalization, always-handled returns, and LogoPlayerPane/StartupLogoPanes ownership over VideoPlayerPane/Pane/Event alternatives. The remaining issue is shared declaration naming, not target behavior or source ownership.
- 2026-06-20 B003 callee sync:
  - Score unchanged at `87/90`.
  - Summary/evidence: [UID:0002PU][0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment](by-memory/0x004f5570-0x004f55c8.LogoPlayerPaneAdvanceToNextSegment.md) is now `87/92` with first-draft `void LogoPlayerPane::AdvanceToNextSegment()` C++; this page now records that the input handlers own the user-skip policy while the callee owns the segment transition, open failure handling, and playback restart.
- 2026-06-21 Rule 26 incorporation of B008 EventMan/factory helper report:
  - Score unchanged at `87/90`.
  - Summary/evidence: [UID:00014C][0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers](by-memory/0x004a8ac0-0x004ab3eb.EventManAndEventFactoryHelpers.md) now carries the consolidated source-facing event enum map consumed here, confirming the `KeyDown` and left-button release/up interpretations.
- Score update: existed as `70/86`; changed to `82/88`. Summary: live IDA MCP refreshed exact handler bounds, decompilation, disassembly, vtable-only xrefs, no direct callers, single-callee behavior, `this - 0xa0` normalization, always-handled returns, and padding boundaries. Confidence remains below final-code level because event enum names and the secondary interface declaration are still unresolved.
