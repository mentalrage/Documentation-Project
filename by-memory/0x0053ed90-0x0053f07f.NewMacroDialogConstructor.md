*** UID:00049J | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000094 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000094 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
NewMacroDialog::NewMacroDialog()
    : DialogPane(kMacroDialogCaption, 6, true),
      m_currentPage(0)
{
    g_pGeneralPurposePanel->SwitchActiveChild(3, false);
    PrepareMacroDialogPalette(g_pMacroDialogPalette, 0x198, 100);

    AddControl(new ResourceImagePane(L"DLGMAC2.EPF", Rect(0, 0, 289, 399), L"DLGMACRO.PAL"));
    AddControl(new ImageButtonControlPane(kMacroOkImageId, Rect(118, 362, 181, 386)));

    for (int row = 0; row < 10; ++row)
    {
        CompactShortcutRecord &record = g_pConfig->m_compactShortcutRecords[row];
        const int y = 54 + row * 29;
        AddControl(new NewMacroEditControlPane(
            record.code,
            row,
            record.isSpellMode,
            Rect(19, y, 268, y + 20)));
    }

    ImageButtonControlPane *previous = new ImageButtonControlPane(kMacroPreviousImageId, Rect(30, 362, 93, 386));
    previous->Disable();
    AddControl(previous);

    AddControl(new ImageButtonControlPane(kMacroNextImageId, Rect(200, 362, 263, 386)));

    SetHoverControl(2);
    SetModal(true);
    SetCloseOnEscape(true);
    SetBackgroundResource(L"DLGMACRO.EPF");
    SetBounds(Rect(130, 10, 419, 409));
    SetLayer(g_pDialogLayer);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact newer paged MacroDialog constructor with natural 0x270 layout, direct CompactShortcutRecord code/isSpellMode rows and initial signed shortcut ordinals 0..9, OK/previous/next controls, inherited SetHoverControl(2), resource/bounds/layer setup, no final Show, complete source C++, and preserved no-direct-xref caveat. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0053ed90-0x0053f07f NewMacroDialogConstructor

## Status

- Address range: `0x0053ed90-0x0053f07f` half-open.
- Entity kind: `NewMacroDialog` constructor.
- Direct owner/emitter target: [UID:000094][NewMacroDialog](by-class/NewMacroDialog.md).
- Source-family owner: [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md).
- Parent split/container: [UID:0001DR][0x0053e520-0x0053f2b6.MacroDialogs](by-memory/0x0053e520-0x0053f2b6.MacroDialogs.md).
- Rebuild handling: source-authored constructor body.

## Behavior

This constructor builds the newer paged macro shortcut dialog. It initializes `m_currentPage` at object offset `+0x26c` to zero, switches the general-purpose panel active child with index `3`, prepares the macro dialog palette, creates the `DLGMAC2.EPF` background and OK button, then creates ten [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) rows at ids `2..11` from direct `g_pConfig->m_compactShortcutRecords[0..9]`. Each exact four-byte row supplies `wchar_t code` and `unsigned char isSpellMode`; the loop variable `row` is passed separately as the initial signed shortcut ordinal `0..9`. The remaining record byte is natural two-byte-alignment tail padding, not an explicit reserved member.

At the row-construction site, the machine code zero-extends the selected
`record.isSpellMode` byte and passes that raw value as the third argument to
[UID:0004A8][0x0057f9d0-0x0057fa2b.NewMacroEditControlPaneConstructor](by-memory/0x0057f9d0-0x0057fa2b.NewMacroEditControlPaneConstructor.md).
The typed source contract is `unsigned char spellMode`; no compare, mask,
Boolean cast, or canonicalization occurs before the call. A noncanonical
nonzero record value therefore reaches `m_spellMode` unchanged.

The same constructor call supplies `row` as the second argument to UID0004A8, which now names it `shortcutIndex` and stores it in `m_shortcutIndex` at complete-object `+0x108`. This is the first-page number-key ordinal, not a `CompactShortcutRecord` identifier or pointer; UID00049O later computes `page * 10 + row` for both pages, and UID0004AD renders the resulting `0..19` domain as number/Ctrl-number labels.

The constructor creates previous/next page image buttons for control ids `12` and `13` using image ids `18` and `19`; it disables the previous-page button on the first page. It calls inherited `SetHoverControl(2)`, sets modal/escape behavior, background `DLGMACRO.EPF`, bounds `(130,10,419,409)`, and the dialog layer. It intentionally does not call `Show()` in this body.

The constructor has no direct current xref in the accepted MCP pass. That negative evidence must stay visible. The class is still source-shaped and reconstructable because the constructor, vtable refs, destructor support, command handler, row-control calls, and compact-record storage form a coherent `NewMacroDialog` class cluster.

## Evidence

- Current MCP report session `b010_0001DR_20260628` revalidated `lookup_funcs` at `0x0053ed90` as size `0x2ef`.
- Fresh `xrefs_to` found no direct constructor xref to `0x0053ed90`.
- The same pass revalidated vtable-base references for `0x00620e48` and method cluster calls around `NewMacroDialog`.
- [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md) records the row-control constructor and getter/setter behavior used by this constructor and by page save/load.
- [UID:0001VR][RegistryConfigUserProfileBlock](by-type/by-struct/RegistryConfigUserProfileBlock.md) documents `CompactShortcutRecord m_compactShortcutRecords[20]` at `+0x28f29c`.
- Fresh UID0003UA-family source-factorization revalidated the same direct row contract across defaults, profile import, settings save, this constructor, page save/load, row-control paint/input, and retained runtime dispatch. This constructor's `record.code` / `record.isSpellMode` reads therefore match the storage lifecycle and do not use `MacroHotkeyRecord`, nested `m_userProfile`, generic `key/mode`, or raw offsets.
- The selected call sequence at `0x0053ef20-0x0053ef2c` performs the raw byte
  zero-extension and constructor call. The callee stores only the low byte at
  complete-object `+0x10e`, matching the current `unsigned char m_spellMode`
  declaration and paired `GetSpellMode` / `SetSpellMode` methods.
- The second constructor argument in that same call sequence is the signed loop row `0..9`. It initializes the independent dword `m_shortcutIndex` at `+0x108`; no record pointer, byte narrowing, bounds conversion, or macro-hotkey slot participates.
- Exact callsite `0x0053effb` passes integer `2` to the New primary `+0x58` forwarding override, which reaches [UID:0004RI][0x0049fc00-0x0049fc4c.DialogPaneSetHoverControl](by-memory/0x0049fc00-0x0049fc4c.DialogPaneSetHoverControl.md).

## Source Shape, Negatives, And Score Rationale

- Allocation evidence is exactly `0x270`: inherited DialogPane `0x26c` plus `int m_currentPage`. Constructor writes, class declaration, and consumers agree without explicit padding members.
- All resource names, palette preparation, row/control ids, compact-record code/isSpellMode inputs, rectangles, previous-button disable, modal/escape/background/bounds/layer order, and absence of final `Show()` are preserved.
- The no-direct-constructor-xref result is retained as a liveness/confidence caveat rather than erased or used to suppress complete source. Vtables, destructor forms, command methods, row APIs, and storage still prove a coherent class.
- `SetActiveControl` is rejected because the exact route is New's `SetHoverControl` override and the base helper performs hover-state transitions. Raw vptr stores, allocation/EH/cookie code, addresses, and decompiler temporaries remain excluded.
- Historical generic mode and modifier labels are superseded by the complete
  `CompactShortcutRecord::isSpellMode` lifecycle. The constructor's formal body
  already passed the exact record member, so no body change is required here.
  The stronger type/name evidence belongs in the declaration and row
  constructor rather than an added cast.
- Historical record-index wording for the second row-control argument is
  superseded by signed shortcut-ordinal semantics. The existing human body
  already passes `row` exactly, so this support update requires no formal or
  score change.
- Completion `90` reflects complete construction, layout, storage, helper route, source ordering, boundaries, and formal body. Confidence `93` reflects exact method/table/helper evidence while preserving the no-direct-xref and lexical-name caps.

## Cross References

- [UID:000094][NewMacroDialog](by-class/NewMacroDialog.md)
- [UID:000095][NewMacroEditControlPane](by-class/NewMacroEditControlPane.md)
- [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md)
- [UID:0001DR][0x0053e520-0x0053f2b6.MacroDialogs](by-memory/0x0053e520-0x0053f2b6.MacroDialogs.md)
- [UID:0001VR][RegistryConfigUserProfileBlock](by-type/by-struct/RegistryConfigUserProfileBlock.md)
- [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md)
- [UID:0004RI][0x0049fc00-0x0049fc4c.DialogPaneSetHoverControl](by-memory/0x0049fc00-0x0049fc4c.DialogPaneSetHoverControl.md)

## Changes

- 2026-07-16 B003 UID0004A9 implementation callback:
  - Preserved `90/93`, UID000094 owner/emitter, exact formal constructor, resources, controls, layout, source order, SetHoverControl route, and no-direct-xref caveat.
  - Documented that the second NewMacroEditControlPane constructor argument is the initial signed shortcut ordinal `0..9`, independent of the direct record's `wchar_t code` and unsigned-char `isSpellMode`, and linked it to UID0004A8/UID0004A9/UID0004AD page-load and paint semantics.

- 2026-07-16 B001 UID0004A7 implementation callback:
  - Preserved the complete constructor body, `90/93`, resources, controls,
    layout, source order, and no-direct-xref caveat.
  - Added the exact raw `isSpellMode` zero-extension/call sequence, typed
    `unsigned char spellMode` destination, noncanonical-byte preservation,
    no-Boolean proof, and accepted spell-mode naming history.
