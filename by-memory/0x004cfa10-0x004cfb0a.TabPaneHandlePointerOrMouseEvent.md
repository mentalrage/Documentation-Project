*** UID:00034O | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool TabPane::HandlePointerOrMouseEvent(Event *event)
{
    if (g_pUserPane->m_tabActionInputLocked) {
        return false;
    }

    if (event->m_type != kEventLeftButtonDown &&
        event->m_type != kEventLeftButtonDoubleClick) {
        return false;
    }

    const signed char actionCode = ResolveTabActionCode(
        event->m_payload.m_pointer.m_y,
        event->m_payload.m_pointer.m_x);
    if (actionCode < 0) {
        return false;
    }

    if (event->m_type == kEventLeftButtonDoubleClick &&
        actionCode != 0 && actionCode != 1 &&
        actionCode != 2 && actionCode != 5) {
        return false;
    }

    switch (actionCode) {
    case 0:
        RequestLocalPlayerStatus(event->m_payload.m_key.m_messageTime);
        return true;

    case 1:
        SwitchGeneralPurposePanelMode2WithClickSound();
        return true;

    case 2:
        SwitchGeneralPurposePanelMode3WithClickSound();
        return true;

    case 3:
        SwitchGeneralPurposePanelMode4WithClickSound();
        return true;

    case 4:
        g_pUserPane->HandleCollectionTabAction();
        return true;

    case 5:
        SwitchGeneralPurposePanelMode6WithClickSound();
        return true;
    }

    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA MCP resolves `0x004cfa10-0x004cfb0a` as `bool TabPane::HandlePointerOrMouseEvent(Event *)`: exact 250-byte secondary EventHandler +0x04 override, left-click/double-click dispatch, canonical pointer y/x ordering, signed-byte tab action, preserved +0x10c overlay read, exact helper ABI classes, and no direct code callers. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cfa10-0x004cfb0a TabPane HandlePointerOrMouseEvent

## Status

- Entity kind: `TabPane` pointer/mouse event override and tab-action dispatcher.
- Direct owner: [UID:0000EB][TabPane](by-class/TabPane.md).
- Source file: [UID:0000OF][TabPane](by-file/TabPane.md).
- Parent aggregate: [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md).
- Rebuild handling: reconstructable source-owned `bool TabPane::HandlePointerOrMouseEvent(Event *event)` override with complete first-draft C++. The inherited interface, event record, coordinate order, return type, and helper ABI classes are resolved; remaining helper/member lexical spellings are explicitly inference-capped rather than deferred blockers.

## Behavior

The method returns a handled-status `bool`. It first reads the local-player input-lock byte at historical `dword_67A748 + 0x13eb1d`; decimal `1305373` converts to `0x13eb1d` (Verified with int_convert.py). If that byte is set, the function returns false without dispatching. The canonical source-facing storage is [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md)'s `UserPane *g_pUserPane`; `g_pCollectionData` is retained only as a historical feature-view alias.

The object is the exact [UID:00004L][Event](by-class/Event.md) record: unsigned `m_type` at `+0x4`, pointer payload `m_y` at `+0x8`, `m_x` at `+0xc`, and the key-payload overlay's `m_messageTime` at `+0x10c`; decimal `268` converts to `0x10c` (Verified with int_convert.py). Only `kEventLeftButtonDown` (`1`) and `kEventLeftButtonDoubleClick` (`2`) are accepted. Both resolver call sites push `m_x` then `m_y`, so the callee receives canonical source order `ResolveTabActionCode(m_y, m_x)`. A left-button-down handles action codes `0..5`; a double-click handles only `0`, `1`, `2`, and `5`. The `+0x10c` action-0 argument is a counterintuitive but exact cross-overlay read and must not be normalized to pointer payload `m_messageTime` at `+0x18`.

| Action code | Accepted event coverage | Helper |
| --- | --- | --- |
| `0` | left down and double-click | `0x005a4f70`, a free `__stdcall`-shape one-argument helper that ignores incoming `ecx`; it receives the exact `EventKeyPayload::m_messageTime` overlay at `+0x10c`, sends opcode `0x2d` / decimal `45` (Verified with int_convert.py), subtype `0`, length `2`, then stores the argument into the opcode extra-value slots. |
| `1` | left down and double-click | `0x005a4db0`; free zero-argument helper sets panel mode `2` and plays effect/sound `0x198` for `0x64` / `100` ticks (Verified with int_convert.py). |
| `2` | left down and double-click | `0x005a4e40`; free zero-argument helper sets panel mode `3` and plays effect/sound `0x198` for `0x64` / `100` ticks. |
| `3` | left down only | `0x005a4e70`; free zero-argument helper sets panel mode `4` and plays effect/sound `0x198` for `0x64` / `100` ticks. |
| `4` | left down only | `0x005a4ea0(dword_67A748)`, a real `UserPane`-receiver helper; source-facing `g_pUserPane->HandleCollectionTabAction()` sends opcode `0x27` length `3` or switches panel mode `5` with optional sound based on UserPane/collection bytes. |
| `5` | left down and double-click | `0x005a4f40`; free zero-argument helper sets panel mode `6` and plays effect/sound `0x198` for `0x64` / `100` ticks. |

## IDA Evidence

- B003 MCP session `60724697` against active `NexusTK.exe.i64` with `server_health` OK confirmed IDA function `sub_4CFA10` at exact half-open range `0x004cfa10-0x004cfb0a`, size `0xfa` / decimal `250` (Verified with int_convert.py), 92 instructions, and 20 basic blocks.
- The exclusive end `0x004cfb0a` is not a function. Six `0xcc` bytes at `0x004cfb0a-0x004cfb10` separate this body from successor `sub_4CFB10` at `0x004cfb10`; pre-boundary padding after the preceding one-byte null virtual keeps `0x004cfa10` as the correct child start. The operand-wildcarded target signature is unique in the binary.
- `xrefs_to 0x004cfa10` reports only the `TabPane` secondary vtable data reference at `0x0061b520`; bounded caller checks report zero direct code callers. `xrefs_to 0x0061b520` also reports zero, so reachability is vtable-slot dispatch rather than a direct call route.
- Vtable bytes at `0x0061b51c` decode as the secondary `TabPane` vtable context containing `0x004cfd82`, slot `0x0061b520 -> 0x004cfa10`, `0x004cfb10`, and `0x00544dd0`.
- Callees are `0x004cfc60`, `0x005a4f70`, `0x005a4db0`, `0x005a4e40`, `0x005a4e70`, `0x005a4ea0`, and `0x005a4f40`. Bounded helper checks show the action-code behavior documented above; none of those shared helpers owns the `TabPane` virtual.
- Decompilation and disassembly show resolver calls for both accepted event branches, return paths that set `al`/return `0` or `1`, double-click accepting only `0`, `1`, `2`, and `5`, and left-button-down accepting `0..5`.
- Resumed B003 MCP database `3fc1dcef` on 2026-07-14 was independently rediscovered by `idb_list`; `server_health` was `ok` with auto-analysis, Hex-Rays, and strings ready. Bounded lookup/get-bytes/analyze/disassembly calls reconfirmed this exact `0xfa` body, 20-block CFG, sole table xref, and both resolver call sequences after the external MCP interruption.
- [UID:00004N][EventHandler](by-class/EventHandler.md)'s secondary interface fixes `+0x04` as `HandlePointerOrMouseEvent(Event *)` and `+0x08` as `HandleKeyOrTextEvent(Event *)`. `TabPane` cell `0x0061b520` is therefore this exact override, not a generic `PaneInputEvent` handler.
- Both resolver calls push `[event+0xc]` followed by `[event+0x8]`; with cdecl/right-to-left stack order and the canonical [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) convention, the source expression is `ResolveTabActionCode(event->m_payload.m_pointer.m_y, event->m_payload.m_pointer.m_x)`.
- Bounded helper disassembly classifies `0x005a4db0`, `0x005a4e40`, `0x005a4e70`, and `0x005a4f40` as zero-argument free helpers that ignore incoming `ecx`; `0x005a4f70` as a one-argument free `__stdcall`-shape helper; and `0x005a4ea0` as a true `UserPane` receiver. The source draft follows those ABI classes without exposing calling-convention syntax.

## Ownership Decision

Attach to [UID:0000EB][TabPane](by-class/TabPane.md) and emit through [UID:0000OF][TabPane](by-file/TabPane.md). The only method xref is the `TabPane` secondary vtable slot at `0x0061b520`, the resolver and geometry are `TabPane`-specific, and the body is bounded by `TabPane` child padding/successor functions. Reject `IconsPane`, `UserPane`/global state ownership, the action-helper island, packet-sender ownership, broad parent-aggregate emission, no-owner/non-emitting treatment, and raw `sub_`/decompiler source names: those explain callees or dependencies but not this virtual slot.

## Score Rationale

Completion is `92` because the page now resolves the exact inherited virtual identity, `Event *` signature, event constants/union fields, canonical y/x order, signed-byte resolver result, helper ABI classes, exact range/size/CFG/xrefs/padding, dispatch behavior, and complete formal C++. Confidence is `94` because the secondary-slot comparison, Event layout, disassembly, and helper bodies agree; no original PDB/source proves the inferred action-helper/member spellings or final physical grouping with `IconsPane`.

## Reconstruction Notes

The draft preserves action codes `0`, `1`, `2`, and `5` for both accepted event types and codes `3` and `4` for left-button-down only. Historical `HandleInputEvent`, `PaneInputEvent`, generic `mode/x/y/tabExtraValue`, `g_pCollectionData`, and `SendOpcode2DAndStoreExtra` source forms are superseded by the inherited EventHandler contract, current Event/UserPane declarations, and exact [UID:0004YC][0x005a4f70-0x005a500f.RequestLocalPlayerStatus](by-memory/0x005a4f70-0x005a500f.RequestLocalPlayerStatus.md). Action `0` passes the exact key-payload `m_messageTime` at `event+0x10c`; it must never be replaced with zero or pointer-payload `+0x18`. The four panel-mode helper names, `HandleCollectionTabAction`, and `m_tabActionInputLocked` remain behavior-grounded source recommendations rather than recovered original spellings.

## Cross-References

- [UID:0000EB][TabPane](by-class/TabPane.md)
- [UID:0000OF][TabPane](by-file/TabPane.md)
- [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md)
- [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md)

## Changes

- 2026-07-29 B009 UID00037S callback: migrated only action `0` to `RequestLocalPlayerStatus(event->m_payload.m_key.m_messageTime)`, preserving the exact `event+0x10c` overlay, every other action, `92/94`, owner/emitter, and blank H.

- 2026-07-14 B003 UID00034N focused family repair:
  - Before: `89/91`, path/title `TabPaneHandleInputEvent`, provisional `PaneInputEvent` fields, x/y call order, generic integer action result, feature-view global spelling, and unresolved adjacent-interface wording.
  - Changed to: `92/94`, validator-preserved UID00034O at `TabPaneHandlePointerOrMouseEvent`, exact `bool TabPane::HandlePointerOrMouseEvent(Event *)` formal, canonical `m_y,m_x` resolver order, signed-byte action code, exact event constants and `+0x10c` overlay read, canonical `g_pUserPane`, and ABI-correct helper classifications.
  - Evidence: resumed live MCP database `3fc1dcef`, EventHandler secondary `+0x04` contract, Event `0x110` layout, exact call pushes and body branches, sole `0x0061b520` table xref, and bounded callee disassembly.

- 2026-07-04 B003 UID00034O implementation callback:
  - Changed to: completion `89`, confidence `91`, with owner/emitter kept at [UID:0000EB][TabPane](by-class/TabPane.md).
  - Historical stage: session `60724697` established the exact range/CFG/xref/padding/dispatch facts and the page then emitted provisional `bool TabPane::HandleInputEvent(const PaneInputEvent *event)`. The 2026-07-14 focused family repair supersedes that lexical/formal shape without invalidating the captured binary facts.
