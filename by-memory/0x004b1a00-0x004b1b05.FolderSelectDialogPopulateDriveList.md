*** UID:00045A | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000059 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JF | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FolderSelectDialog::PopulateDriveList()
{
    PopupMenuControlPane *driveList =
        GetChild<PopupMenuControlPane>(0);
    DWORD driveMask = GetLogicalDrives();

    char currentDirectory[MAX_PATH];
    GetCurrentDirectoryA(sizeof(currentDirectory), currentDirectory);
    int selectedDrive = toupper(currentDirectory[0]) - 'A';

    for (int drive = 0; drive < 32; ++drive)
    {
        if (driveMask & (1UL << drive))
        {
            s_driveRootBuffer[0] = (wchar_t)('A' + drive);
            driveList->AppendItem(new StringMenuItem(s_driveRootBuffer));
            if (drive == selectedDrive)
                driveList->SetSelectedIndex(driveList->GetItemCount() - 1);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private retained/no-entry FolderSelectDialog drive-list population method using inherited typed child access, enumerating a DWORD drive mask, copying each mutable UTF-16 drive root into an owned StringMenuItem, and selecting the last appended current-drive item without inventing a caller or failure guard. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b1a00-0x004b1b05 FolderSelectDialogPopulateDriveList

## Status

- Disposition: reconstructable source-shaped helper for [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md), classified as retained/no-entry in the checked binary.
- Source module: [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md), `NexusTK/ui/dialogs/FolderSelectDialog.cpp`.
- Container split: exact child of [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md).
- Binary range: `0x004b1a00-0x004b1b05`, IDA function size `0x105`.
- Formal C++ status: accepted retained-helper first-draft body is present and routed through [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md) at position `30`. The body preserves the no-entry policy: it reconstructs the helper but does not invent a caller.
- Exact target bytes hash: SHA256 `655962B7DBE4BDA3163366C27DE0A7719309CA5A9072FFE50A2BCE760272C1BF`; the entry signature `55 8B EC 6A FF 68 F7 E8 5F 00` is unique in the checked PE.
- ABI/source shape: nonvirtual thiscall-like member, `FolderSelectDialog *` in ECX, no explicit stack arguments, plain `retn`, and best source return `void`. The DWORD value left in EAX on normal exit is incidental drive-mask liveness, not a meaningful source return.

## Boundary

| Address / range | Meaning |
| --- | --- |
| `0x004b1a00` | Start of source-shaped drive-list helper. |
| `0x004b1b05` | Exclusive end; next bytes are adjustor thunks and padding, not helper body. |
| `0x004b1b05-0x004b1b20` | Compiler-generated destructor adjustor thunks plus padding. |

- The predecessor [UID:000459][0x004b1920-0x004b1a00.FolderSelectDialogOnCommand](by-memory/0x004b1920-0x004b1a00.FolderSelectDialogOnCommand.md) ends exactly at `0x004b1a00` with `retn 8`; there is no fallthrough into this function.
- IDA associates three out-of-line EH/frame-handler blocks at `0x005fe8e9-0x005fe91f` with the function. Those compiler chunks are outside the primary half-open range and are not handwritten source children.

## Source Behavior

- Accepted source-facing name: `PopulateDriveList`.
- Has a normal prologue, SEH/security-cookie setup, WinAPI calls, control-list operations, allocation, `StringMenuItem` construction, and a loop over possible drive letters.
- Calls `GetLogicalDrives`, `GetCurrentDirectoryA`, and `_toupper`.
- Uses [UID:00027D][0x0066da88-0x0066da90.FolderSelectDriveRootBuffer](by-memory/0x0066da88-0x0066da90.FolderSelectDriveRootBuffer.md), accepted source-facing name `s_driveRootBuffer`, by writing the first WCHAR for each drive letter and then passing the drive-root text into list-entry construction.
- Resolves child zero through inherited DialogPane `List *m_controlManager` storage. The machine path calls the List virtual slot `+0x10` for `GetElementAt(0)` and dereferences the returned four-byte `ControlPane *` slot; current source support exposes that contract as `GetChild<PopupMenuControlPane>(0)`. The former undefined `GetControl(0)` abstraction is superseded.
- Control lookup occurs first, then `GetLogicalDrives`, `GetCurrentDirectoryA`, `_toupper`, and the 32-slot mask loop. No null check is present for the manager, element slot, or control pointer.
- The `GetCurrentDirectoryA` return is ignored and the 260-byte local is not initialized before the call. Failure therefore leaves the first byte indeterminate; empty input yields `-65` after normalization, and a UNC-leading backslash yields `27`. The body intentionally adds no fallback or validation.
- The binary sign-extends `currentDirectory[0]` at `0x004b1a5b` before `_toupper`. The former unsigned-character cast was safer library usage but changed binary behavior for high-bit bytes and is retained only as a superseded historical draft.
- Iterates all 32 DWORD mask bits in ascending order. `1u` preserves defined bit-31 source semantics while lowering to the observed x86 shift; the original suffix is not encoded in machine code.
- Writes only `s_driveRootBuffer[0]`; the colon, backslash, and terminator remain intact. Each available drive allocates `0x118` bytes for `StringMenuItem`, whose constructor synchronously copies the root into inline `m_text[0x80]`, then appends the owned item through `PopupMenuControlPane::AppendItem(MenuItem *)`.
- If allocation returns null, compiler lowering passes null to `AppendItem`; no explicit source guard is added. If construction throws, the associated MSVC EH cleanup frees the allocation and unwinds, so no handwritten cleanup belongs in the method.
- `GetLogicalDrives` returning zero does not bypass current-directory normalization or the 32 tests, but it appends and selects nothing. Available drives append in ascending letter order.
- Selects the current drive after comparing the drive index against the normalized letter index. The selected row is `GetItemCount() - 1`, not the drive index, so sparse masks select the item just appended.
- Mirrors the constructor's drive-enumeration source intent, but the checked binary does not contain a direct constructor call to this helper.

## No-Entry Proof

- IDA lookup confirms `0x004b1a00` is a real function start with size `0x105`.
- Cached IDA xrefs report no direct code caller.
- Raw PE rel32 scan found no caller to `0x004b1a00`.
- Raw PE abs32 scan found no function-pointer reference to `0x004b1a00`.
- Raw PE RVA-pointer and immediate-reference scans likewise found no route to `0x004b1a00`.
- [UID:0003BF][0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData](by-memory/0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData.md) has no slot for `0x004b1a00`.
- The successor thunk at `0x004b1b05` does have a vtable data route; that adjacent route does not reach the target.
- Final classification is retained/no-entry private helper in the checked binary. Do not invent a constructor call, vtable route, or external caller.

## Evidence

- Mutable buffer refs in this helper occur at `0x004b1a8b` and `0x004b1aaa`.
- The buffer bytes for [UID:00027D][0x0066da88-0x0066da90.FolderSelectDriveRootBuffer](by-memory/0x0066da88-0x0066da90.FolderSelectDriveRootBuffer.md) match `58 00 3a 00 5c 00 00 00`, the mutable wide string `X:\\`.
- The helper is source-shaped rather than padding or a thunk: it contains API calls, local setup, allocation, item construction, and list selection behavior.
- The lack of inbound routes is specific to liveness/reachability, not to source ownership. The best source placement remains FolderSelectDialog.
- Fresh read-only public MCP evidence-method receipt: for `0x004b1a00`, incoming-count authority is `xref_query.total`, which returns exactly `0` with an empty route list; aggregate `func_profile.caller_count` is not authoritative. Direct outgoing-call authority is public `callees`, which returns exactly nine routes and `more:false`: `0x0060d30c` (`GetLogicalDrives`), `0x0060d1a4` (`GetCurrentDirectoryA`), `0x005df317` (`_toupper`), `0x004f4aa0` (`NexusTK_operator_new`), `0x00516ff0` (`sub_516FF0`), `0x00498170` (`PopupMenuControlPane::AppendItem`), `0x00498190` (`PopupMenuControlPane::GetItemCount`), `0x00498220` (`PopupMenuControlPane::SetSelectedIndex`), and `0x005c772f` (`@__security_check_cookie@4`).
- Fresh IDA evidence counts 96 primary instructions and 13 IDA basic blocks, including three compiler EH blocks; the ten main-flow blocks cover lookup, API setup, bit test, allocation/construction, append, optional selection, loop increment, and return.
- Stack evidence resolves allocation scratch at `ebp-0x11c`, drive mask at `ebp-0x118`, the 260-byte `char currentDirectory[MAX_PATH]` span at `ebp-0x114..ebp-0x11`, cookie at `ebp-0x10`, and compiler SEH state. No explicit source parameter occupies the frame.
- Exact calls in order are inherited List lookup, `GetLogicalDrives`, `GetCurrentDirectoryA`, `_toupper`, allocation helper, `StringMenuItem` construction at `0x00516ff0`, `AppendItem` at `0x00498170`, `GetItemCount` at `0x00498190`, and `SetSelectedIndex(long)` at `0x00498220`.
- The constructor span `0x004b17e0-0x004b188f` independently repeats the same control lookup, APIs, constants, data object, allocation size, constructor, append/count/select calls, and 32-slot limit. It has a complete local implementation rather than a call or outline.

## Ownership And Source Placement

- Canonical owner is [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md).
- Emitting source module is [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md).
- The helper should remain in the class method inventory as retained/no-entry private helper documentation.
- It should not be assigned to [UID:00005A][FolderTreePane](by-class/FolderTreePane.md), [UID:00008U][MusicControlDialog](by-class/MusicControlDialog.md), or the drive-root buffer page. Those pages provide consumers/data, not source ownership.
- Rejected source shapes include padding/compiler glue, a file-local free helper, and a static/fastcall helper. Complete behavior rejects artifact status, while ECX-relative DialogPane state and no explicit receiver argument favor a member method.
- Manual retained duplication or a refactoring remnant ranks above automatic inlining because the target is large, stack-heavy, and EH-bearing. Automatic inlining remains possible; different-build retention is unsupported. This bounded historical ambiguity does not permit a synthetic constructor call.

## C++ Readiness Notes

- Formal C++ now emits this as a private retained helper, `FolderSelectDialog::PopulateDriveList()`, at file scope through [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md).
- The source-facing return type is `void` to match the class declaration and helper role. The decompiler leaves `LogicalDrives` in `EAX`, but no checked caller consumes a return value.
- The body intentionally does not add a constructor call. The constructor contains its own drive enumeration sequence, and current evidence still shows no inbound route to this helper.
- The body uses inferred support-backed drive-list names `PopupMenuControlPane`, `StringMenuItem`, `AppendItem`, `GetItemCount`, and `SetSelectedIndex` rather than raw helper labels.
- The body deliberately preserves signed-byte `_toupper` parity, unsigned 32-bit mask shifting, ignored API results, allocation-null propagation, append order, and append-count selection. It introduces no control guard, API fallback, manual cleanup, early break, list clear, or source-visible return.
- Function-type and source authority are exactly `void __thiscall FolderSelectDialog::PopulateDriveList()` with no explicit argument. The physical IDA frame nevertheless retains stale `arg_4@0x138/0x4:_DWORD`; current public stack endpoints cannot delete that argument member, so the row remains a protected physical artifact and does not authorize a source parameter.

## Score Rationale

| Field | Rationale |
| --- | --- |
| Completion `93` | Exact bytes/hash, ABI, split function/frame authority, CFG/chunks, instruction and call order, data/xref closure, typed child contract, API edge behavior, signedness, allocation/EH behavior, constructor comparison, ownership, no-entry proof, and source-ready formal C++ are documented. |
| Confidence `95` | Direct machine behavior, boundaries, support contracts, data lifetime, source placement, and protected stale-frame readback agree. Remaining uncertainty is only original method/access spelling and manual-copy-versus-inlining history. |

## Cross-References

- [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md)
- [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md)
- [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md)
- [UID:00027D][0x0066da88-0x0066da90.FolderSelectDriveRootBuffer](by-memory/0x0066da88-0x0066da90.FolderSelectDriveRootBuffer.md)
- [UID:0003BF][0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData](by-memory/0x0061a45c-0x0061a4f4.FolderSelectDialogVtableData.md)

## Changes

- 2026-07-14 B005 accepted UID00045A implementation callback:
  - Raised `86/90 -> 92/94` while preserving owner UID000059, emitter UID0000JF, position `30`, reconstructable true, exact range, and `Nested:0`.
  - Replaced the stale undefined `GetControl(0)` cast with inherited typed `GetChild<PopupMenuControlPane>(0)` and removed the unsigned-character cast so `_toupper` receives the signed-byte value observed in the binary.
  - Added exact hash/entry, ABI/stack/CFG/EH, ordered API/control/item behavior, failure and malformed-path semantics, allocation/ownership, exhaustive route negatives, source-shape ranking, constructor duplicate history, and score-cap evidence. No caller, guard, cleanup, split, or metadata route was invented.

- 2026-06-26 B013 accepted report implementation:
  - What existed before: `COMPLETION:85`, emitter routed to [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md), position was blank, and formal C++ was blank under the earlier split-stage instruction.
  - Changed to: `COMPLETION:86`, `EMITTER_UIDS:0000JF`, `EMITTER_POSITION_OPTIONAL:30`, and the accepted `FolderSelectDialog::PopulateDriveList()` first-draft body in the formal C++ block.
  - Summary/evidence: the helper remains retained/no-entry. The formal body reconstructs the drive enumeration/list-population behavior but does not invent a constructor call, because checks found no direct code xref, no absolute pointer route, and no vtable/data route to `0x004b1a00`.
- 2026-06-26 B001 split implementation callback:
  - Created as the exact retained/no-entry `PopulateDriveList` child of the former aggregate [UID:000154][0x004b1590-0x004b1b87.FolderSelectDialog](by-memory/0x004b1590-0x004b1b87.FolderSelectDialog.md).
  - Original split-stage formal C++ was blank; this is superseded by the B013 retained-helper body above.
