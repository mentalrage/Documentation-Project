*** UID:00023Q | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000C0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000C0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __stdcall SendRightButtonTargetActionPacket(int targetId)
{
    unsigned char packet[7];

    PacketBufferWriteByte(0x4a, packet);
    PacketBufferWriteByte(0, packet + 1);
    PacketBufferWriteUInt32BE(targetId, packet + 2);
    packet[6] = 0;

    g_packetSender->QueueAndSendPacket(packet, 6);
}

static void __stdcall SendRightButtonNameCommandPacket(const wchar_t *targetName)
{
    char convertedName[0x100];
    unsigned char packet[0x12c];

    if (targetName[0] == L'\0')
        return;

    PacketBufferWriteByte('.', packet);

    short wideLength = (short)wcslen(targetName);
    int convertedLength = WideCharToMultiByte(
        CP_ACP,
        0,
        targetName,
        wideLength,
        convertedName,
        sizeof(convertedName),
        NULL,
        NULL);

    convertedName[convertedLength] = '\0';
    PacketBufferWriteByte((unsigned char)convertedLength, packet + 1);
    memmove(packet + 2, convertedName, convertedLength);

    int packetLength = convertedLength + 2;
    packet[packetLength] = 0;

    g_packetSender->QueueAndSendPacket(packet, (short)packetLength);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Packet send helpers using opcode `0x4a`; IDA confirms raw body endpoints, packet writer global usage, range checks, internal padding, and no direct caller/pointer xrefs. A002 cleared stale file-parent assignment because the child is `78/82`; future assignment should route through direct class parent [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), not file parent [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00555780-0x005558c2 - RightButtonMenu Packet Helpers

## Status

- Disposition: reconstructable NexusTK project code. This page carries first-draft source-quality C++ for two private/file-local right-click menu packet helpers. The no-direct-caller evidence remains a liveness caveat and score cap, but it no longer blocks source emission because the helper bodies, signatures, packet layouts, owner route, and support names are resolved enough for a first draft.
- Entity kind: raw helper island.
- Owner: [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md).
- Source file: [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md).
- Confidence: strong for behavior, boundaries, packet-sender usage, helper signatures, packet layouts, and right-click menu ownership by locality/source-family context; medium for direct liveness because no static direct caller or pointer refs are proven into either helper body.
- Autogen handling: assigned to direct class parent [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md). This child is now `87/88`; the class parent and file parent [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md) clear the strict gate, so source emission routes through the class without using a file-parent shortcut.

## Covered Range

| Range | IDA status | Behavior |
| --- | --- | --- |
| `0x00555780-0x005557da` | raw function-shaped bytes | `0x5a` / 90-byte (Verified with int_convert.py) packet helper that writes opcode `0x4a`, fixed subcommand/action byte `0`, and the caller's 32-bit `targetId` in big-endian order, then queues exactly six bytes through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md). The explicit zero at packet offset `6` is an unsent terminator/safety byte. |
| `0x005557da-0x005557e0` | padding | `0x6` / 6-byte (Verified with int_convert.py) internal `0xcc` alignment span between the two raw helper bodies. |
| `0x005557e0-0x005558c2` | raw function-shaped bytes | `0xe2` / 226-byte (Verified with int_convert.py) target-name packet helper that sends a dot-prefixed, one-byte-length, CP_ACP-converted name packet. It rejects empty wide strings, converts up to `0x100` bytes into a local ANSI buffer, copies the converted bytes after `'.'` and the length byte, writes an unsent terminator, and queues `convertedLength + 2` bytes through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md). |

## Touched State

| State | Evidence |
| --- | --- |
| Local packet buffer, first helper | Stack packet is seven bytes: six sent bytes plus an unsent terminator. The argument is a 32-bit target id written by `PacketBufferWriteUInt32BE`, not a caller byte. The sent payload is `[0x4a][0x00][targetId byte 3][targetId byte 2][targetId byte 1][targetId byte 0]`. |
| Local packet buffer, second helper | Stack packet buffer begins at `ebp-0x130`, but the source-sized guarded packet array is `0x12c` / 300 bytes (Verified with int_convert.py); the remaining four bytes before `ebp` are the cookie/local trailer. Source should model this as `unsigned char packet[0x12c]`, prefixed with `'.'`, a one-byte converted-name length, converted multibyte bytes, and an unsent zero terminator. |
| Temporary conversion buffer | Stack frame reserves `0x230` / 560 bytes (Verified with int_convert.py); the conversion buffer is filled by `WideCharToMultiByte` with output cap `0x100` / 256 bytes (Verified with int_convert.py). The range-check path triggers if the converted count reaches the cap. |
| Packet writer global | Both helpers load [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / historical `dword_67A7EC` and dispatch through `QueueAndSendPacket`. IDA reports hundreds of references to this global across packet send sites. Treat generated `CashShopRequest::meth_0x574bb0` names as sender-path pollution. |
| Range checks | The second helper checks converted byte count against `0x100` / 256 (Verified with int_convert.py) and packet length `converted+2` against `0x12c` / 300 (Verified with int_convert.py), with failure path to `___report_rangecheckfailure`. |
| Resolved helper names | Use `PacketBufferWriteByte`, `PacketBufferWriteUInt32BE`, `memmove`/`MemmoveWrapper`, `GetMemoryMan`, `QueueAndSendPacket`, and [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) `g_packetSender` (`Socket *`). Do not carry raw `sub_516030`, `sub_516220`, `sub_574BB0`, `sub_575380`, or `sub_5753F0` names into source-facing prose or C++. |

## IDA Evidence

- 2026-06-01 IDA MCP `lookup_funcs` reports `0x00555780` and `0x005557e0` as "Not a function", and decompilation fails at both starts; the bytes still decode as complete function-shaped helper bodies with prologue, stack cookie, call sequence, epilogue, and `retn 4`.
- 2026-06-01 IDA MCP byte review confirms `0x005557da-0x005557e0` is six `0xcc` alignment bytes, while the outer gaps `0x0055577b-0x00555780` and `0x005558c2-0x005558d0` are already tracked as padding.
- 2026-06-12 A005 live IDA MCP recheck confirms `0x00555780` and `0x005557e0` are still not IDA functions, have no `xrefs_to` entries, and have no little-endian pointer-pattern matches for `80 57 55 00` or `e0 57 55 00`. The absence of modeled callers is preserved as a liveness caveat rather than ignored.
- The same raw-range pass confirms total range size `0x142` / 322 bytes (Verified with int_convert.py), first helper size `0x5a` / 90 bytes (Verified with int_convert.py), second helper size `0xe2` / 226 bytes (Verified with int_convert.py), internal padding `0x6` / 6 bytes (Verified with int_convert.py), and trailing packet-to-vote alignment `0xe` / 14 bytes (Verified with int_convert.py).
- The first helper calls `PacketBufferWriteByte` to write opcode `0x4a`, calls `PacketBufferWriteByte` again for the zero subcommand/action byte, calls `PacketBufferWriteUInt32BE` for the 32-bit target id argument, writes the unsent trailing zero, then calls `QueueAndSendPacket` with a length of `6`.
- The second helper calls `GetMemoryMan`, rejects empty wide strings, writes prefix `'.'`, measures the wide string excluding the null terminator, sign-extends the low 16 bits of the wide length before `WideCharToMultiByte(CP_ACP, 0, ...)`, writes a one-byte converted length, copies the converted bytes with `MemmoveWrapper` / source `memmove`, computes packet length as converted byte count plus `2`, and dispatches through `QueueAndSendPacket`.
- IDA caller and xref scans on 2026-06-01 found no direct references to `0x00555780` or `0x005557e0`, and a dword pointer scan found no table entries for either raw start. These may be orphaned/static helper copies or missed raw-call targets.
- `xrefs_to(0x0067a7ec)` reports a large fanout of packet send sites, supporting `dword_67A7EC` as the shared packet writer/sender pointer used by both helpers.
- 2026-06-19 B015 raw PE scan over `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` found zero `.text` `E8`/`E9` rel32 branches and zero whole-file absolute dword references into either helper body range (`0x00555780-0x005557da` or `0x005557e0-0x005558c2`). Stored Ghidra function data likewise reports zero callers for both helpers. This preserves the no-direct-liveness caveat while strengthening the conclusion that the bodies themselves are exact source-authored packet helpers rather than padding.
- 2026-07-03 B005 support sync: MCP session `1fc4a5a7` reconfirmed `xrefs_to(0x00555780)` and `xrefs_to(0x005557e0)` as empty and `lookup_funcs` still reports both raw starts as not functions. The split [UID:0004F9][0x00554c40-0x00554f59.RightButtonMenuPaneOnEvent](by-memory/0x00554c40-0x00554f59.RightButtonMenuPaneOnEvent.md) child now documents row 1/Trade building a 6-byte `0x4a` target-id packet and row 3/Group-name command building a dot-prefixed CP_ACP name packet. Those behaviors match this island's packet shapes, but direct calls into this island remain unproven, so this page keeps neutral helper names and the no-direct-liveness caveat.

## Source-Quality Decisions

| Issue | Best current conclusion | Evidence and rejected alternatives |
| --- | --- | --- |
| Source owner/emitter | Keep direct owner/emitter [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md). | The target metadata, class/file docs, physical placement after the right-click menu core, target id/name behavior, and packet-helper locality all point to the right-click menu. Socket/ProtocolSend ownership is rejected because `g_packetSender` is a dependency, not feature ownership evidence; VoteMenuPane ownership is rejected by the boundary at `0x005558d0`. |
| Source file | Keep source-file root [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md). | The class emits through this file page, and the helpers are adjacent to `RightButtonMenuPaneCore`. A new protocol file or MapPane ownership is not justified by the shared sender or by a separate opcode helper elsewhere. |
| Split policy | Keep [UID:00023Q] as one packet-helper island with two source bodies and one internal padding row. | The exact helper bodies are short, adjacent, and menu-specific; `0x005557da-0x005557e0` is confirmed `0xcc` alignment. Splitting into child pages is optional future cleanup, not a blocker for first-draft source. |
| First helper | Best source-facing name/signature is `static void __stdcall SendRightButtonTargetActionPacket(int targetId)`. | The helper writes opcode `0x4a`, subcommand/action `0`, and a 32-bit big-endian argument. The old "caller byte argument" wording is rejected. The `targetId` name is supported by [UID:0001FX][0x00554b40-0x0055577b.RightButtonMenuPaneCore](by-memory/0x00554b40-0x0055577b.RightButtonMenuPaneCore.md), which stores target id at `this+0xfc`. Row-specific names such as Trade or Group are rejected until caller/row mapping is recovered. |
| Second helper | Best source-facing name/signature is `static void __stdcall SendRightButtonNameCommandPacket(const wchar_t *targetName)`. | The helper consumes a wide target name, converts it through `WideCharToMultiByte(CP_ACP, 0, ...)`, and sends `['.'][convertedNameLength][convertedName bytes...]`. It is probably the right-click name/Whisper route, but `SendWhisperPacket` is not promoted because no direct caller survives. |
| Liveness | No static direct liveness is proven. | Existing IDA no-xref notes, stored Ghidra zero-callers, fresh rel32 scans into full helper body ranges, and fresh absolute dword scans all found zero refs. This caps confidence but is not an exact no-code proof. |
| Range-check code | Treat `___report_rangecheckfailure` as compiler/runtime codegen from fixed local array writes. | The checks guard `convertedName[0x100]` and `packet[0x12c]`; source should use fixed arrays and describe the checks, not emit direct runtime-failure calls as handwritten logic. |

## Rebuild Handling

Treat these as private/file-local right-click menu packet helpers emitted through [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md) into [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md). Keep the island separate from [UID:0001FX][0x00554b40-0x0055577b.RightButtonMenuPaneCore](by-memory/0x00554b40-0x0055577b.RightButtonMenuPaneCore.md) for coverage/range ownership, but source placement is adjacent to the core menu implementation.

The direct parent is [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), not the broader file page and not Socket/ProtocolSend. Direct callers and exact action-row names remain unresolved, so the C++ uses neutral helper names rather than invented `Trade`, `Group`, or `Whisper` names.

## First-Draft C++ Notes

- Do not add a null-pointer guard for `targetName`; the binary only rejects empty strings after dereferencing the pointer.
- Do not emit `___security_check_cookie`, `___report_rangecheckfailure`, `FUN_00555780`, `FUN_005557e0`, raw `sub_*` names, or generated `CashShopRequest::meth_0x574bb0` names in formal source.
- The second helper's `short wideLength` is intentional: the binary sign-extends a 16-bit measured wide length before `WideCharToMultiByte`. Existing `RightButtonMenuPane` target names are fixed-size (`0x21` wide chars), so this does not create practical truncation for the known route.
- Empty wide strings do not send any packet. A non-empty string that converts to zero bytes still sends the two-byte prefix/zero-length packet.

## Parent Gate Analysis

- Child gate: this page now clears `85/85` because exact raw bounds, body sizes, packet construction, byte order, sender dependency, range checks, no-xref/no-pointer caveat, padding boundaries, direct owner reasoning, helper/global names, and first-draft C++ policy are documented. It remains below final-source confidence because both raw starts still lack direct caller/pointer xrefs and final row-specific action names are inferred.
- Direct semantic parent: [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), because the class page lists this island in its method map and the helper behavior/placement match right-click action support.
- Parent status: [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md) is `85/88` and [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md) is `85/86`, so the direct class assignment is now valid.
- File-parent shortcut rejected: the previous `AUTOGEN_PARENT_UID:0000N7` bypassed the direct-owner rule for class-owned code and was cleared on 2026-06-10.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 87 | The page now records the exact island and subrange sizes, raw helper bodies, stack-array sizes, packet byte order, opcode/subcommand layouts, sender dependency, resolved helper/global names, range-check behavior, stronger no-static-liveness evidence, internal/trailing padding, direct class/file route, rejected alternatives, and first-draft C++ policy. |
| Confidence | 88 | Confidence is strong because raw PE, stored Ghidra function data, existing IDA-backed docs, and support pages agree on behavior and signatures. It is capped below final-audit quality because no direct caller/pointer refs into the helper bodies are currently proven and final action-row protocol names remain inferred. |

## Cross-References

- [UID:0001FX][0x00554b40-0x0055577b.RightButtonMenuPaneCore](by-memory/0x00554b40-0x0055577b.RightButtonMenuPaneCore.md)
- [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md)
- [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md)
- [UID:0001FZ][0x00556070-0x00556211.VoteMenuPaneHitTestAndSubmit](by-memory/0x00556070-0x00556211.VoteMenuPaneHitTestAndSubmit.md)

## Changes

- 2026-06-19 B015 Rule 26 incorporation:
  - Raised `85/86` to `87/88`, kept direct owner/emitter [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), and added first-draft C++ for two private/file-local helpers: `SendRightButtonTargetActionPacket(int targetId)` and `SendRightButtonNameCommandPacket(const wchar_t *targetName)`.
  - Corrected the first helper from "caller byte argument" to a 32-bit big-endian `targetId`, corrected the second helper packet buffer from a source-sized `0x130` array to `packet[0x12c]` plus cookie/local trailer, and replaced raw/generated callee names with `PacketBufferWriteByte`, `PacketBufferWriteUInt32BE`, `QueueAndSendPacket`, `GetMemoryMan`, and `memmove`/`MemmoveWrapper`.
  - Evidence: raw PE body/padding hashes, stored Ghidra zero-caller data, existing IDA-backed right-click menu docs, packet-sender/global docs, and a fresh scan that found zero `.text` rel32 branches and zero whole-file absolute dword references into either helper body range.
  - Rejected alternatives: exact no-code proof, Socket/ProtocolSend ownership, VoteMenuPane ownership, merging into [UID:0001FX][0x00554b40-0x0055577b.RightButtonMenuPaneCore](by-memory/0x00554b40-0x0055577b.RightButtonMenuPaneCore.md), and final row-specific helper names such as Trade/Group/Whisper before caller/row mapping is recovered.
- 2026-07-03 B005 implementation callback:
  - Synced support wording after UID0001FX split-first implementation. Current MCP session `1fc4a5a7` still finds no direct xrefs to either raw helper, while [UID:0004F9][0x00554c40-0x00554f59.RightButtonMenuPaneOnEvent](by-memory/0x00554c40-0x00554f59.RightButtonMenuPaneOnEvent.md) now documents row behaviors that match the target-id and name-command packet shapes.
- 2026-06-12 A005 Batch245:
  - Raised `78/82` to `85/86` and assigned direct parent [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md).
  - Evidence: live IDA MCP reconfirmed both raw starts as non-functions with no xrefs, pointer-pattern scans found no raw table entries for the starts, raw disassembly verified the packet-send bodies, and `int_convert.py` verified `0x142`, `0x5a`, `0xe2`, `0x6`, `0xe`, `0x230`, `0x130`, `0x100`, and `0x12c` conversions.
  - Routing: the strict child/direct-parent gate now clears through the class (`85/88`) and file (`85/86`) chain; final C++ remains blank below the `95/95` source-emission gate.
- 2026-06-10 A002 strict-gate repair:
  - Raised completion from `74` to `78` for documenting the corrected direct-parent gate and assignment blocker; confidence remains `82` because no new caller/xref evidence was found beyond the existing raw-byte review.
  - Cleared `AUTOGEN_PARENT_UID:0000N7` and `AUTOGEN_PARENT_POSITION_OPTIONAL:40`. This helper island is reconstructable but below `85/85`; future assignment should route through [UID:0000C0][RightButtonMenuPane](by-class/RightButtonMenuPane.md), not directly through the file page.
- 2026-06-01: Raised completion/confidence from `50/65` to `74/82`.
  - Evidence: added exact raw body ranges, the internal `0xcc` alignment span, packet buffer state, global packet writer evidence, callee sequence, range-check behavior, and negative caller/pointer scan results from IDA MCP.
  - Rebuild: marked reconstructable and attached to [UID:0000N7][RightButtonMenuPane](by-file/RightButtonMenuPane.md), but left C++ blank until direct callers and final helper names are known.
