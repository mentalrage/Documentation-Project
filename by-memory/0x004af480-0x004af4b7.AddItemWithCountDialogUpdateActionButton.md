*** UID:0004BS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void AddItemWithCountDialog::UpdateActionButton()
{
    ImageButtonControlPane *actionButton =
        GetChild<ImageButtonControlPane>(1);
    TextEditControlPane *countInput =
        GetChild<TextEditControlPane>(4);

    if (countInput->IsEmpty())
        actionButton->Disable();
    else
        actionButton->Enable();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: AddItemWithCountDialog virtual action-button updater; retrieves action button selector 1 and count editor selector 4, then disables for empty text and enables for nonempty text through distinct state virtuals. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004af480-0x004af4b7 AddItemWithCountDialog UpdateActionButton

## Status

- Disposition: source-authored, reconstructable NexusTK project code; this is one complete virtual member, not a compiler helper, split candidate, or empty-emitter marker.
- Exact range: end-exclusive `[0x004af480,0x004af4b7)`, size `0x37` (55) bytes.
- Entity kind and ABI: primary-table virtual `void __thiscall AddItemWithCountDialog::UpdateActionButton()` with an ECX receiver, no explicit source argument, and no source-visible return value.
- Canonical owner and immediate emitter: [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md), retained as both `CANONICAL_OWNER` and `EMITTER_UIDS`.
- Final source route: [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md), `NexusTK/ui/dialogs/ItemDialogs.cpp`.
- Source order: position `30`, after the constructor at 10 and control-command/action method at 20, and before the packet-close handler at 40.
- Parent relation: exact emitting child of [UID:00033S][0x004af040-0x004af4b7.AddItemWithCountDialogCore](by-memory/0x004af040-0x004af4b7.AddItemWithCountDialogCore.md); relative `Nested:0` remains unchanged.

## Range, Bytes, Boundaries, And Uniqueness

- Exact body bytes: `56 8B F1 57 6A 01 8B 8E FC 01 00 00 8B 01 FF 50 10 8B 8E FC 01 00 00 6A 04 8B 38 8B 01 FF 50 10 8B 08 E8 B9 97 FE FF 8B 17 8B CF 5F 5E 84 C0 74 03 FF 62 50 FF 62 4C`.
- SHA-256 of those 55 bytes: `03D568AE9A8EDD4C33F2C4DA03B3EC2357A6C47DA67777DE577D482161FFD877`.
- The body contains 22 instructions in three basic blocks with cyclomatic complexity 1. The two terminal virtual jumps are source-level method calls selected by one ordinary `if` branch.
- `[0x004af47a,0x004af480)` is six bytes of `0xcc` aggregate alignment after the preceding action child. `[0x004af4b7,0x004af4c0)` is nine bytes of `0xcc` alignment before the following packet-close child. Neither padding run belongs to this method.
- A complete body-byte search produces one image match at `0x004af480`. No target-internal function start, secondary entry, function chunk, data island, split, merge, or duplicate body exists.

## Exact Instruction And Control-Flow Evidence

```text
004af480  push esi
004af481  mov esi, ecx
004af483  push edi
004af484  push 1
004af486  mov ecx, [esi+1FCh]
004af48c  mov eax, [ecx]
004af48e  call dword ptr [eax+10h]
004af491  mov ecx, [esi+1FCh]
004af497  push 4
004af499  mov edi, [eax]
004af49b  mov eax, [ecx]
004af49d  call dword ptr [eax+10h]
004af4a0  mov ecx, [eax]
004af4a2  call sub_498C60
004af4a7  mov edx, [edi]
004af4a9  mov ecx, edi
004af4ab  pop edi
004af4ac  pop esi
004af4ad  test al, al
004af4af  jz 0x004af4b4
004af4b1  jmp dword ptr [edx+50h]
004af4b4  jmp dword ptr [edx+4Ch]
```

- The first manager call uses selector `1`; the returned entry yields the action button saved in EDI.
- The second manager call uses selector `4`; the returned entry yields the count editor passed to `0x00498c60`.
- `0x00498c60` is the accepted [UID:0004K7][0x00498c60-0x00498c72.TextEditControlPaneIsEmpty](by-memory/0x00498c60-0x00498c72.TextEditControlPaneIsEmpty.md) implementation. It returns the boolean tested in AL.
- Empty text falls through to the receiver's primary virtual slot `+0x50`, accepted as `Disable()`. Nonempty text takes the zero branch and dispatches primary virtual slot `+0x4c`, accepted as `Enable()`.
- The source branch therefore must remain `if (countInput->IsEmpty()) Disable(); else Enable();`. A boolean setter would hide the two distinct observed virtual exits.

## Vtable, Wrapper, Caller, And Liveness Route

- The sole direct target xref is the data cell `0x0061a038 -> 0x004af480`, relative primary-vtable slot `+0x4c` in the table based at `0x00619fec`.
- The same table places the preceding action method at `0x0061a034 -> 0x004af380`. The AddItemWithCountDialog constructor installs the table with the vptr store at `0x004af0ae`.
- No ordinary direct code xref reaches `0x004af480`; that negative does not make the method dead. It is live through virtual dispatch.
- Constructor code calls `DialogPane::OnCreate` at `0x004af337` after all controls and class vptrs exist. `DialogPane::OnCreate` reaches the generic refresh wrapper `0x0049fe20` from `0x0049e119`, and that wrapper dispatches the receiver's primary `+0x4c` slot at `0x0049fe40`/`0x0049fe6c`.
- The generic wrapper has ten direct callers. Only receivers whose dynamic vtable maps `+0x4c` to this function execute UID0004BS; generic wrapper callers are not ten direct target callers.
- This route means initial empty count text synchronously disables the action button during creation even though the constructor has no direct call to UID0004BS.

## Source Types, Selectors, And Predicate

- Selector `1` is the constructor-created `ImageButtonControlPane`; selector `4` is the constructor-created `TextEditControlPane` count editor. The selectors are zero-based insertion indices in the inherited dialog control manager, not resource ids, packet values, or field offsets.
- The accepted source access is inherited `DialogPane::GetChild<T>(int)`, which wraps the exact manager virtual lookup seen twice in this body. Free selector helpers are not needed.
- Source locals are highest-probability `ImageButtonControlPane *actionButton` and `TextEditControlPane *countInput`. These names describe proven roles without introducing persistent object fields.
- The predicate is only `countInput->IsEmpty()`. It does not parse the text or test whether a numeric value is valid, nonzero, selected, or within a range.
- `UpdateActionButton` is retained as the highest-probability source method name because the existing class family and override slot use that concept consistently. Exact stripped lexical spelling remains the residual confidence cap.

## State Effects And Failure Behavior

- Empty text dispatches the distinct `Disable()` virtual; nonempty text dispatches the distinct `Enable()` virtual.
- Accepted ControlPane state bodies show `Enable()` sets inherited enabled state only when it was false and then invalidates bounds; `Disable()` clears it only when it was true and then invalidates bounds. UID0004BS itself performs no state store or invalidation beyond choosing that call.
- The target performs no null check on the control manager, either returned control entry, the action button, or the count input. Constructor invariants establish those controls before the framework refresh route can invoke this method.
- There is no failure return, early return, fallback control, exception path, cleanup path, allocation, free, security cookie, or explicit return instruction in the source contract. Both paths tail-dispatch a `void` virtual method.
- There is no count parsing, zero/range clamp, selected-item or inventory-slot access, exchange-session read, packet send, dialog close, resource lookup, global access, string literal, or persistent control-pointer store.

## Ownership, Source Placement, And Compiler Disposition

- [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md) is the semantic owner and direct emitter: its vtable owns the slot, its constructor creates the selector-indexed controls, and the body operates on its inherited control manager.
- [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md) remains the final source file. The physical family and accepted source order are constructor 10, action 20, updater 30, packet-close handler 40. The retained packet helper remains separate and no-route.
- `DialogPane` owns the typed lookup and generic refresh machinery; ImageButtonControlPane, TextEditControlPane, and ControlPane own called interfaces. They are dependencies, not alternate target owners.
- ExchangeDialog is a consumer/construction caller, not the implementation owner. A free ItemDialogs helper, a new source file, or a generic validation helper would contradict the virtual-table and selector evidence.
- Vptr stores, vtable cells, manager lowering, register saves/restores, final virtual tail jumps, alignment bytes, and any compiler-produced wrapper mechanics are compiler/ABI output and are excluded from handwritten C++.
- The target is source-authored and emits exactly once. It must not be represented as a covered-by marker, compiler-only artifact, blank body, inline-only helper, or duplicate body in a class/file root.

## Historical Assumptions And Rejected Alternatives

- Historical B007 reconstruction used `ButtonPane *button`, `GetDialogButtonBySelector(this, 1)`, `HasValidCountInput()`, and `button->SetEnabled(...)`. That draft preserved the broad intent but was deliberately shallow and is superseded as current source-facing text.
- `HasValidCountInput` is rejected because the body checks only `TextEditControlPane::IsEmpty`; no parse, numeric comparison, or range helper exists.
- `SetEnabled(bool)` is rejected because the binary performs two distinct no-argument virtual tail calls with different slots. The explicit branch is the behavior-identical source shape.
- `ButtonPane` is rejected as too vague for selector 1; constructor allocation and virtual state slots resolve the dynamic/source type as `ImageButtonControlPane`.
- `Show`, `Hide`, `OnMouseEnter`, and `OnMouseLeave` are rejected current names for the target-facing state calls. Pane visibility is separate, and the called slots gate enabled/disabled control state.
- A direct constructor call is rejected. The exact initial update route is the synchronous `OnCreate -> 0x0049fe20 -> virtual +0x4c` path.
- Split, compiler-only, no-owner, no-emitter, inline-only, free-function, and no-code dispositions are rejected by the unique body, class vtable cell, dynamic route, and source-shaped predicate.

## Coordination And Support Status

- B002 owns this UID0004BS target page and exact body. The active B003 UID0004BQ report owns the constructor, [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md) class, [UID:00033S][0x004af040-0x004af4b7.AddItemWithCountDialogCore](by-memory/0x004af040-0x004af4b7.AddItemWithCountDialogCore.md) aggregate, preceding action child, vtable/file support, and related family pages; its current read-only R4 coordination block matches this target body exactly.
- The active B005 UID0004BP report owns ImageButtonControlPane and ControlPane `Enable()`/`Disable()` declarations, bodies, state naming, and related support. Those support pages remain outside this target-only edit even when their current text is older than this accepted target contract.
- The supervisor-executed UID0004BN destructor report and its current by-memory result are read-only sibling evidence. They are not active B001 work and are not an edit destination for this callback.
- No class, aggregate, by-file, vtable, state-method, constructor, action, destructor, generated, coverage, tracker, or validator-owned page is duplicated or modified here.

## Evidence Provenance

- Live IDA evidence was collected during report research from database `supervisor_nexustk_20260713`; health/readiness, bounded target lookup, bytes, instructions, CFG, xrefs, callers, callees, vtable values, helper bodies, and wrapper route all succeeded during that evidence window.
- MCP/session statements are time-scoped evidence observations, not assertions of continuing session availability.
- Documentation checks included current target/parent/class/file/vtable/control pages, current generated ItemDialogs output as read-only evidence, the executed UID0004BN sibling report, and the active B003/B005 coordination reports.
- No IDA mutation, source symbol import, process management, generated/manual coverage edit, or report lifecycle command was used to establish these facts.

## Score Rationale

- `COMPLETION:92`: exact range, bytes, every instruction and CFG edge, padding, uniqueness, ABI, vtable cell, dynamic caller route, selectors, control types, predicate polarity, state effects, negative behavior, owner/emitter, source file, source order, compiler exclusions, and formal C++ are all resolved at report-level detail.
- `CONFIDENCE:94`: behavior, ABI, range, routing, types, and source shape are independently supported by live binary and current documentation. Confidence remains below 95 because original PDB/source symbols are absent and exact local/method lexical spellings plus historical header factoring are inferred.
- The former `86/90` score reflected a correct broad owner/range but shallow helper names, a synthetic boolean setter, vague control type, blank source position, and incomplete liveness/negative evidence. Those assumptions are preserved above as historical and are no longer current blockers.
