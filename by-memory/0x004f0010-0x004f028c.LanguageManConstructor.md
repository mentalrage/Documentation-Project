*** UID:00041V | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000071 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000071 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
LanguageMan::LanguageMan()
{
    MemoryMan* memoryMan = GetMemoryMan();

    DATFile stringResource;
    stringResource.Open(kStringResourceName);
    (void)stringResource.GetSize();

    wchar_t **temporaryStrings =
        static_cast<wchar_t **>(
            memoryMan->AllocateBufferMemory(sizeof(wchar_t *) * kMaxLocalizedStringCount));
    m_stringCount = 0;

    bool reachedEndOfFile = false;
    char multiByteLine[kMaxStringResourceLineBytes * 2];
    wchar_t wideLine[kMaxStringResourceLineBytes];

    do {
        int lineLength = 0;

        while (stringResource.GetPosition() < stringResource.GetSize()) {
            stringResource.Read(&multiByteLine[lineLength], 1);

            if (multiByteLine[lineLength] == '\r') {
                stringResource.Seek(1, 1);
                break;
            }

            if (multiByteLine[lineLength] == '\n' || ++lineLength >= kMaxStringResourceLineBytes)
                break;
        }

        if (stringResource.GetPosition() >= stringResource.GetSize())
            reachedEndOfFile = true;

        for (int i = 0; i < lineLength; ++i) {
            if (multiByteLine[i] == 0x11)
                multiByteLine[i] = '\r';
        }

        const int wideChars = MultiByteToWideChar(
            0,
            0,
            multiByteLine,
            lineLength,
            wideLine,
            kMaxStringResourceLineBytes);
        const size_t byteCount = static_cast<size_t>(wideChars) * sizeof(wchar_t);
        wchar_t *copiedLine =
            static_cast<wchar_t *>(
                memoryMan->AllocateBufferMemory(byteCount + sizeof(wchar_t)));

        temporaryStrings[m_stringCount] = copiedLine;
        memoryMan->MemmoveWrapper(temporaryStrings[m_stringCount], wideLine, byteCount);
        temporaryStrings[m_stringCount][wideChars] = L'\0';

        const int storedIndex = m_stringCount++;
        if (storedIndex >= kMaxLocalizedStringCount)
            throw MyError(kTooManyStringsMessage);
    } while (!reachedEndOfFile);

    m_strings =
        static_cast<wchar_t **>(memoryMan->AllocateBufferMemory(sizeof(wchar_t *) * m_stringCount));
    for (int i = 0; i < m_stringCount; ++i)
        m_strings[i] = temporaryStrings[i];

    memoryMan->FreeBufferMemory(temporaryStrings);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-authored LanguageMan constructor body; direct Singleton<LanguageMan> base construction supplies compiler-lowered singleton publication/fallback cleanup, while the handwritten body opens `str.res`, parses up to 1024 localized records with `0x11` to CR remapping and `MultiByteToWideChar`, allocates/copies owned wide strings through MemoryMan helpers, preserves the post-store MyError overflow path, and installs `m_strings`/`m_stringCount`. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f0010-0x004f028c LanguageMan Constructor

## Status

- Exact range: `0x004f0010-0x004f028c`.
- Current IDA name: `sub_4F0010`.
- Entity kind: source-authored `LanguageMan::LanguageMan` constructor body.
- Covered class: [UID:000071][LanguageMan](by-class/LanguageMan.md).
- Source file route: [UID:0000KK][LanguageMan](by-file/LanguageMan.md).
- Parent aggregate/split index: [UID:00018R][0x004f0010-0x004f0477.LanguageManLocalization](by-memory/0x004f0010-0x004f0477.LanguageManLocalization.md).
- Disposition: class-owned, reconstructable constructor body that emits through [UID:000071][LanguageMan](by-class/LanguageMan.md) into `LanguageMan.cpp`.
- Boundary: exact constructor body ends at `0x004f028c`, followed by four `0xcc` alignment bytes before [UID:00018S][0x004f0290-0x004f0310.LanguageManCleanupDestructor](by-memory/0x004f0290-0x004f0310.LanguageManCleanupDestructor.md).

## Behavior

`LanguageMan::LanguageMan` opens the DAT-backed `str.res` resource, parses the localized-string table, allocates one owned null-terminated wide string for each parsed row, then copies the temporary pointer table into the final `m_strings` table. Direct `Singleton<LanguageMan>` base construction publishes the process-wide singleton through compiler lowering; that assignment is not handwritten in the formal body.

The constructor initializes the confirmed `LanguageMan` object layout:

| Offset | Source-facing field | Constructor behavior |
| --- | --- | --- |
| `+0x00` | `LObject` base / vtable | Calls the LObject shell constructor and installs the LanguageMan vtable at `0x0061c9e0`. |
| `+0x04` | `m_strings` | Receives the final heap-allocated `wchar_t**` pointer table after parsing completes. |
| `+0x08` | `m_stringCount` | Starts at zero, increments once per parsed row, and controls the final pointer-table copy. |

RTTI hierarchy evidence adds direct `Singleton<LanguageMan>` at PMD `mdisp=4`, `pdisp=-1`, `vdisp=0`, attributes `0x40`. Empty-base optimization overlaps the singleton subobject with `m_strings` at `+0x04`; this is source inheritance, not an additional named data field.

Parser behavior:

- Constructs a stack `DATFile`, opens `kStringResourceName` / `L"str.res"`, and makes an initial `GetSize()` call.
- Allocates a temporary 1024-entry pointer table through the project MemoryMan allocation helpers.
- Reads one input byte at a time while `DATFile::GetPosition() < DATFile::GetSize()`.
- Treats CR as a line terminator, then calls `DATFile::Seek(1, 1)` to skip the following byte from the current position.
- Treats LF as a line terminator without incrementing the byte count.
- Caps the input line at `kMaxStringResourceLineBytes` / `0x400`.
- Rewrites byte `0x11` to CR before conversion.
- Calls `MultiByteToWideChar(0, 0, multiByteLine, lineLength, wideLine, kMaxStringResourceLineBytes)`.
- Allocates `wideChars * sizeof(wchar_t) + sizeof(wchar_t)` bytes for each converted line, copies through `memoryMan->MemmoveWrapper`, and appends a UTF-16 terminator.
- Preserves the binary post-store overflow shape: the new pointer is stored and `m_stringCount` is incremented before the old stored index is compared with `kMaxLocalizedStringCount`; overflow throws `MyError(kTooManyStringsMessage)`.

## Source Helper Shape

The accepted source-facing helper spelling is:

| Binary role | Source-facing shape |
| --- | --- |
| DAT stack object and file API | `DATFile stringResource`, `Open`, `GetSize`, `Tell`, `Read`, `Seek`, implicit destructor |
| Memory allocation/free | `MemoryMan* memoryMan = GetMemoryMan()`, `memoryMan->AllocateBufferMemory`, `memoryMan->FreeBufferMemory` |
| Copy wrapper | `memoryMan->MemmoveWrapper` |
| Conversion API | `MultiByteToWideChar(0, 0, ...)` |
| Overflow throw | `throw MyError(kTooManyStringsMessage)` |
| Parser constants | [UID:00040V][0x0061c9d4-0x0061c9dc.LanguageManParserLimitConstants](by-memory/0x0061c9d4-0x0061c9dc.LanguageManParserLimitConstants.md) `kMaxStringResourceLineBytes` and `kMaxLocalizedStringCount` |
| Source literals | [UID:00040X][0x0061c9ec-0x0061ca44.LanguageManStringResourceLiterals](by-memory/0x0061c9ec-0x0061ca44.LanguageManStringResourceLiterals.md) `kStringResourceName` and `kTooManyStringsMessage` |

Do not spell this constructor with raw `malloc`, `free`, CRT `memmove`, IDA local names, a higher-level `StringBase` table, or a `LanguageManager` owner. The binary stores raw `wchar_t*` entries in `m_strings`; the public StringBase uncertainty belongs to [UID:00040Q][0x004f0380-0x004f03bb.LanguageManCopyLocalizedString](by-memory/0x004f0380-0x004f03bb.LanguageManCopyLocalizedString.md), not this constructor.

## Evidence

- B012 accepted MCP rework used active database session `80de0a67` and MCP session `57d9094b-2370-4930-a720-627e62710575`; health was ok, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- MCP `lookup_funcs` reports `0x004f0010` as `sub_4F0010`, size `0x27c`, ending at `0x004f028c`; `0x004f028c-0x004f0290` is four `0xcc` bytes before the destructor child.
- Constructor `0x004f0010` has one code xref from `0x00464108` in `Application::Initialize` / `sub_4639D0`.
- Decompilation shows calls to `LObject` construction, DATFile construction/open/tell/size/read/seek/destruction helpers, `GetMemoryMan`, MemoryMan allocation/free/copy helpers, `MultiByteToWideChar`, `operator new(8)`, `MyError::MyError(L"Too many strings")`, and `_CxxThrowException`.
- The constructor stores the LanguageMan vtable at `0x0061c9e0`. At `0x004f0055-0x004f0066`, `lea this+4`, a null test, and `add -4` publish the complete object pointer to [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md) / [UID:0001OS][0x0067a750-0x0067a754.g_pLanguageMan](by-memory/0x0067a750-0x0067a754.g_pLanguageMan.md); fallback clear is at `0x004f006d`.
- Direct Singleton RTTI/PMD proves those two writes are compiler-lowered base construction/failure handling. The source formal therefore removes only the explicit singleton assignment and preserves every parser, allocation, copy, conversion, throw, field, and resource operation.
- MCP string/xref evidence ties `0x0061c9ec` / `L"str.res"` to the constructor open path and `0x0061c9fc` / `L"Too many strings"` to the overflow throw path.
- Immediate `0x400` uses occur at `0x004f011f` for byte-line capping, `0x004f0167` for the wide conversion buffer capacity, and `0x004f01d1` for the loaded-string count cap.
- Stack-frame evidence identifies the stack `DATFile` object, EOF flag, `WCHAR WideCharStr[1024]`, and a 0x800-byte multibyte local buffer whose first 1024 bytes are used by the parser.

## Owner And Emitter

The direct owner is [UID:000071][LanguageMan](by-class/LanguageMan.md) because this is the complete constructor body for that class: it constructs the direct LObject and Singleton bases, installs the LanguageMan vtable through compiler lowering, and initializes the class string-table fields used by the class lookup/destructor children. The emitter is [UID:000071][LanguageMan](by-class/LanguageMan.md), which routes to [UID:0000KK][LanguageMan](by-file/LanguageMan.md) and `auto-generated/NexusTK/localization/LanguageMan.cpp`.

Rejected alternatives:

- Do not emit this source on [UID:00018R][0x004f0010-0x004f0477.LanguageManLocalization](by-memory/0x004f0010-0x004f0477.LanguageManLocalization.md); that page is the broader mixed split/index over constructor, destructor, file-level zero helper, lookup/copy/clear children, scalar deleting wrapper, and padding.
- Do not keep the constructor child non-emitting or file-owned; the class evidence is direct and the class/file emitter route is valid.
- Do not assign this constructor to [UID:000072][LanguageManager](by-class/LanguageManager.md); current MCP found `LanguageMan` / `Singleton<LanguageMan>` RTTI/name records and no `LanguageManager` RTTI/name records.
- Do not retain an explicit `g_pLanguageMan` assignment for readability. It duplicates direct Singleton base behavior and would misstate the most plausible human source shape.

## Cross-References

- [UID:00018R][0x004f0010-0x004f0477.LanguageManLocalization](by-memory/0x004f0010-0x004f0477.LanguageManLocalization.md)
- [UID:000071][LanguageMan](by-class/LanguageMan.md)
- [UID:0000KK][LanguageMan](by-file/LanguageMan.md)
- [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md)
- [UID:0001OS][0x0067a750-0x0067a754.g_pLanguageMan](by-memory/0x0067a750-0x0067a754.g_pLanguageMan.md)
- [UID:0001RP][str-res-localized-strings](by-resource/str-res-localized-strings.md)
- [UID:00018S][0x004f0290-0x004f0310.LanguageManCleanupDestructor](by-memory/0x004f0290-0x004f0310.LanguageManCleanupDestructor.md)
- [UID:00040V][0x0061c9d4-0x0061c9dc.LanguageManParserLimitConstants](by-memory/0x0061c9d4-0x0061c9dc.LanguageManParserLimitConstants.md)
- [UID:00040W][0x0061c9dc-0x0061c9ec.LanguageManVtableData](by-memory/0x0061c9dc-0x0061c9ec.LanguageManVtableData.md)
- [UID:00040X][0x0061c9ec-0x0061ca44.LanguageManStringResourceLiterals](by-memory/0x0061c9ec-0x0061ca44.LanguageManStringResourceLiterals.md)
- [UID:0000IN][DATFile](by-file/DATFile.md)

## Score Rationale

Completion `92`: exact range, boundary padding, sole Application caller, class owner/emitter route, direct Singleton lowering, DATFile parser behavior, MemoryMan/MyError helper spelling, stack local shape, parser constants/literals, post-store overflow behavior, and source-authored formal constructor C++ are documented.

Confidence `94`: active MCP function inventory, decompilation, xrefs, RTTI/PMD bytes, string refs, immediate scans, stack-frame evidence, and related by-* support docs agree on behavior, ownership, and source routing. Confidence is capped by ordinary original-source spelling/header uncertainty, not by unresolved behavior.

## Changes

- 2026-08-24 B005 UID0000KK callback: raised scores to `94/95`, set emitter position `40`, retained the complete byte-line/CR-LF/0x11/CP0/1024/post-increment-overflow behavior, and corrected the declaration-backed source APIs from stale `DATFile::Tell`/free `MemmoveWrapper` to `DATFile::GetPosition` and `memoryMan->MemmoveWrapper`.

- 2026-07-20 B005 UID0001OS source-quality callback: raised `91/92 -> 92/94` and applied the accepted constructor formal with only the explicit singleton publication removed. Added direct `Singleton<LanguageMan>` RTTI/PMD/EBO proof, exact `this+4/-4` complete-pointer lowering, fallback-clear disposition, one-definition linkage, and the rejection of a handwritten singleton assignment while preserving the entire parser/resource/allocation/error body.
- 2026-06-25 B012 implementation: created this exact constructor child from accepted report `00018R-LanguageManLocalization-source-quality.md`, splitting source-bearing constructor C++ out of the broader [UID:00018R][0x004f0010-0x004f0477.LanguageManLocalization](by-memory/0x004f0010-0x004f0477.LanguageManLocalization.md) island so the aggregate can become a non-emitting split index.
