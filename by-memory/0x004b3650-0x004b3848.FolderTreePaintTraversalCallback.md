*** UID:000345 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct FolderTreePaintTraversalContext;

template <>
template <>
void Tree<FolderTreePane::TreeElem>::Traverse<FolderTreePaintTraversalContext>(
    TreeItor<FolderTreePane::TreeElem> *start,
    FolderTreePaintTraversalContext *context)
{
    const int kStopTraversal = 1;
    const int kSkipChildren = 2;

    std::deque<int> pending;
    pending.push_back(start->GetIndex());

    while (!pending.empty())
    {
        const int nodeIndex = pending.back();
        pending.pop_back();

        TreeItor<FolderTreePane::TreeElem> node(this, nodeIndex);
        const int traversalState =
            FolderTreePane::PaintTreeNode(context, &node);
        if (traversalState == kStopTraversal)
            break;

        if (traversalState == kSkipChildren)
            continue;

        TreeNode<FolderTreePane::TreeElem>& treeNode = m_nodes[nodeIndex];
        for (int childIndex = treeNode.lastChildIndex; childIndex != -1;
             childIndex = m_nodes[childIndex].previousSiblingIndex)
        {
            pending.push_back(childIndex);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Paint-tree traversal helper for FolderTreePane: tree receiver, root iterator seed, deque-backed visible traversal, per-node paint callback, stop/skip return states, and last-child/previous-sibling child descent. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3650-0x004b3848 FolderTreePaintTraversalCallback

## Current Accepted UID0004YK Consumer Policy

This body remains FolderTreePane-owned paint policy over the generic Tree traversal shape. Its storage references now use Tree<T>::m_nodes; clipping, node painting, expansion, visitor state, and the exact 0x004b3650-0x004b3848 range remain consumer-specific. The shared deque depth-first skeleton is authored once in Tree.h.

## Status

- Disposition: reconstructable NexusTK source helper.
- Entity kind: `FolderTreePane` visible-tree traversal callback for paint/update.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: file-local helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). The only ordinary caller is the `FolderTreePane` paint path at `0x004b2a00`.
- Rebuild handling: `source-authored`; this is folder-tree UI paint traversal.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b3650-0x004b3848` | `FolderTreePaintTraversalCallback` | Walks visible folder-tree nodes for painting and delegates per-node draw/search state to `0x004b5810`. |

## Current MCP And Generated Evidence

B007's accepted 2026-06-28 source-quality pass refreshed this target against active MCP session/database `agent_b009_0002my_20260628` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. JSON-RPC `initialize` id `b007-000345-current2-init` succeeded, `idb_list` id `b007-000345-current2-idb-list` returned the active worker session with `is_active:true`, `is_analyzing:false`, PID/worker PID `2412`, and `server_health` id `b007-000345-current2-server-health` returned `status:ok`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`. No IDA DB state was edited.

The generated source snapshot checked during the accepted pass was `auto-generated/NexusTK/ui/controls/FolderTreePane.cpp`, header `validator-command-id: 000000000025`, `validator-refreshed-at: 2026-06-28T21:57:41-04:00`, source by-file UID `0000JG`. It still emitted this page as:

```text
// UID:000345 | by-memory\0x004b3650-0x004b3848.FolderTreePaintTraversalCallback.md | Completion:86 | Confidence:88 | Empty Emitter Marker
```

That marker was stale because the helper now clears the reconstruction-code gate: it is `RECONSTRUCTABLE:TRUE`, has confirmed nonblank `EMITTER_UIDS:0000JG`, averaged above 85, and has an accepted source-facing body.

MCP `lookup_funcs` id `b007-000345-current2-lookup` confirmed the current function inventory used by this page:

| Address | MCP name | Size | Role |
| --- | --- | ---: | --- |
| `0x004b3650` | `sub_4B3650` | `0x1f8` | This exact target. |
| `0x004b2a00` | `sub_4B2A00` | `0x7c` | Sole ordinary caller, `FolderTreePane` paint path. |
| `0x004b5810` | `sub_4B5810` | `0x1c5` | Per-node paint helper [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md). |
| `0x004b3400` | `sub_4B3400` | `0x245` | Content-dimension traversal sibling [UID:000344][0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal](by-memory/0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal.md). |
| `0x004b3850` | `sub_4B3850` | `0x292` | Hit-test traversal sibling [UID:000346][0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal](by-memory/0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal.md). |
| `0x004b3af0` | `sub_4B3AF0` | `0x251` | Visible navigation traversal sibling [UID:000347][0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch](by-memory/0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch.md). |
| `0x004b3010` | `sub_4B3010` | `0x74` | `GetNodeBounds` consumer of row/depth fields. |

MCP `xrefs_to` id `b007-000345-current2-xrefs` reported exactly one xref to this target: a code xref at `0x004b2a67` inside `sub_4B2A00`. No data/pointer/table route was found in the accepted pass. This confirms a private paint traversal helper route, not public callback-table ownership.

MCP `callees` id `b007-000345-current2-callees` reported this target's callees as `operator new` at `0x005c74f6`, pending-container seed helper `0x004a88e0`, per-node paint helper `0x004b5810`, deque/segmented-container grow helper `0x0043e0b0`, and deque/segmented-container destroy helper `0x00439e00`.

## Caller Route And Traversal Behavior

MCP `decompile` id `b007-000345-current2-decompile-paint-caller` for `0x004b2a00` shows `FolderTreePane::PaintTree` setting up this traversal:

- It calls a GrafPort/pane color-state helper with constant `121`.
- It applies a transform/setup call through `unk_69B3FC(this, this + 272)`.
- It initializes a paint traversal context local with `context[0] = this`.
- It constructs a root `TreeItor<FolderTreePane::TreeElem>` with vtable `0x0061a510`, tree pointer `this + 304` (`+0x130`, accepted `m_tree`), and current index `0`.
- It zeroes context slot `+0x0c`.
- It calls `0x004b3650` at `0x004b2a67` with receiver `this + 304`, the root iterator, and the paint context.

This proves the binary receiver for this helper is the embedded `Tree<FolderTreePane::TreeElem> *`, not `FolderTreePane *`. It also proves the helper is specifically the paint traversal behind `FolderTreePane::PaintTree`, not a generic public tree API used across the client.

MCP `decompile` id `b007-000345-current2-decompile-target` and `disasm` id `b007-000345-current2-disasm-target` establish the source-facing behavior:

- `ecx` is loaded as the tree receiver at `0x004b3678`.
- The pending traversal container is seeded through `0x004a88e0` at `0x004b36d0` from the start iterator's current-index field at `a2 + 8`.
- For each pending index, the helper constructs a temporary `TreeItor<FolderTreePane::TreeElem>` over the same tree receiver and the popped node index.
- It calls [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) at `0x004b375f`, passing the paint context and temporary iterator.
- It tests return state `1` at `0x004b3768-0x004b376b` and stops traversal on that state.
- It tests return state `2` at `0x004b3771-0x004b3774` and skips child descent on that state.
- Otherwise it reads `TreeElem::lastChildIndex` at record offset `+0x08` (`0x004b3780`); if no child exists, no descendants are queued.
- For child descent it pushes child indices starting from `lastChildIndex` and follows `TreeElem::previousSiblingIndex` at record offset `+0x0c` (`0x004b380f`) until `-1`.
- Because the container is popped from the back, pushing from the last child backward through previous siblings preserves visible child order.
- The function returns with `retn 8` at `0x004b3845`, matching the documented exclusive end `0x004b3848`.

The observed segmented container is best represented as `std::deque<int>` in the final source: the binary uses explicit `push_back`, `back`, and `pop_back` behavior over a Dinkumware/STL deque-like container. `std::stack<int>` would only describe the high-level shape and would hide the concrete operations; raw deque internals would be decompiler-shaped.

## Per-Node Helper And State Semantics

MCP `decompile` id `b007-000345-current2-decompile-node-helper` for [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) confirms the callback route and return-state meanings:

- The helper receives the paint context as `this` and the current `TreeItor<FolderTreePane::TreeElem> *` as the explicit argument.
- It reads the owning `FolderTreePane *` from paint-context offset `+0x00`.
- It derives screen coordinates from `TreeElem::rowIndex` at `+0x18` and `TreeElem::depth` at `+0x1a`, using pane offsets around `+0x120/+0x124`.
- It calls `GetNodeBounds` at `0x004b3010`.
- It reads `TreeElem::name` at `+0x14`, checks the selected iterator around `FolderTreePane+0x170`, draws folder icon/name state, and returns traversal state.

State meanings for this traversal are:

| State | Meaning | Evidence |
| --- | --- | --- |
| `0` | Continue traversal and descend into children when present. | [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) returns `0` when the current node is expanded. |
| `1` | Stop traversal. | This helper breaks on `1`; sibling hit-test traversal [UID:000346][0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal](by-memory/0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal.md) uses `1` for found/stop. |
| `2` | Process this node but skip child descent. | [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) returns `2` when the node is not expanded; this helper continues without queuing children. |

The formal C++ preserves the stop-state branch even though the current paint node helper decompilation only shows returns `0` and `2`, because this target explicitly tests `1` and the sibling hit-test traversal uses the same traversal-state convention.

## Sibling Traversal And Field Evidence

Current MCP comparison confirms this page is one member of a local `FolderTreePane.cpp` traversal family:

- [UID:000344][0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal](by-memory/0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal.md): same tree receiver, iterator current-index seed through `0x004a88e0`, deque-like pending container, and `lastChildIndex` / `previousSiblingIndex` child-push pattern; its per-node body computes `depth`, writes `rowIndex` and `depth`, measures `name`, and updates content dimensions.
- [UID:000346][0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal](by-memory/0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal.md): same receiver and pending-container pattern; its per-node body computes row bounds, calls the hit-test helper, stores a found iterator in its context, returns `1` to stop on hit, `2` to skip unexpanded children, or `0` to descend.
- [UID:000347][0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch](by-memory/0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch.md): same receiver and child-push pattern, but the context tracks selected/current node and visible-navigation direction.

[UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md) records the relevant field model: `lastChildIndex` at `+0x08`, inferred `previousSiblingIndex` at `+0x0c`, `name` at `+0x14`, `rowIndex` at `+0x18`, `depth` at `+0x1a`, `childrenLoaded` at `+0x1c`, `expanded` at `+0x1d`, and final `hidden` at `+0x20`. This helper itself does not read `hidden`. That is negative evidence against describing it as a hidden-node filtering iterator helper; hidden skipping remains in UID0002N0 and the per-node paint helper, while this aggregate traversal relies on child/sibling links and callback return states. `isHidden` is superseded history and `visibleTraversalSkip` descriptive only.

## Ownership And Parent Gate

The direct semantic owner is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). This helper is reached only from `FolderTreePane` painting and exists to iterate the pane's embedded tree while delegating row-specific rendering to `0x004b5810`. The file parent is `89/85`, so the strict direct-parent gate is satisfied.

## Reconstruction Notes

- UID000149 factorization: `pending.push_back(start->GetIndex())` calls shared out-of-line `std::deque<int>::push_back` at `0x004b36d0`; later child-index appends inline the same push logic and call `_Growmap(1)` at `0x004b37ac` only on demand. Preserve the native source expression and no custom Deque dependency.

- Final source now emits through [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) as `Tree<FolderTreePane::TreeElem>::TraversePaintNodes`. The binary receiver is the embedded tree at `FolderTreePane+0x130`, not `FolderTreePane *`, so the formal body is a concrete `Tree<FolderTreePane::TreeElem>` helper specialized for the paint context.
- `TraversePaintNodes`, `FolderTreePaintTraversalContext`, and `PaintFolderTreeNode` are inferred source-facing names. They replace raw `sub_4B3650`/`sub_4B5810` labels because current evidence proves the behavior and source shape; exact original symbol spelling remains unknown but is no longer a code-entry blocker.
- `std::deque<int>` is the accepted source-facing pending-node container. A higher-level `std::stack<int>` would hide the observed explicit `push_back`, `back`, and `pop_back` operations over a Dinkumware/STL deque-like container, while raw deque internals would be decompiler-shaped.
- The traversal states are preserved as constants: `0` means descend/continue, `1` stops traversal, and `2` skips child descent. The stop branch is required even though the current paint-node helper only shows `0` and `2`, because this function tests `1` directly and sibling hit-test traversal uses state `1` as found/stop.

## 2026-06-28 B007 Implementation Evidence

B007's accepted empty-emitter/source-quality pass refreshed the live MCP evidence through active session/database `agent_b009_0002my_20260628` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. `server_health` returned `status:ok`, module `NexusTK.exe`, input binary `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, and `strings_cache_ready:true`.

Current generated output before this implementation still showed [UID:000345] as:

```text
// UID:000345 | by-memory\0x004b3650-0x004b3848.FolderTreePaintTraversalCallback.md | Completion:86 | Confidence:88 | Empty Emitter Marker
```

The generated file header at that point was `validator-command-id: 000000000025`, refreshed `2026-06-28T21:57:41-04:00`, source by-file UID [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). This confirmed the queue symptom was still active before the accepted C++ block was inserted.

MCP `lookup_funcs` id `b007-000345-current2-lookup` reconfirmed the relevant current function inventory: target `0x004b3650` `sub_4B3650` size `0x1f8`, caller `0x004b2a00` `sub_4B2A00` size `0x7c`, per-node paint helper `0x004b5810` `sub_4B5810` size `0x1c5`, sibling traversal helpers `0x004b3400` size `0x245`, `0x004b3850` size `0x292`, `0x004b3af0` size `0x251`, and bounds helper `0x004b3010` size `0x74`. `xrefs_to` id `b007-000345-current2-xrefs` found exactly one code xref to the target, at `0x004b2a67` inside `sub_4B2A00`; no data/pointer route was found.

The paint caller decompilation id `b007-000345-current2-decompile-paint-caller` proves the source route: `PaintTree` calls a color-state helper with constant `121`, applies transform/setup through `unk_69B3FC(this, this + 272)`, stores the pane pointer in paint context slot `+0x00`, creates a root `TreeItor<FolderTreePane::TreeElem>` with vtable `0x0061a510`, tree pointer `this + 304` (`+0x130`, accepted `m_tree`), and index `0`, zeroes context slot `+0x0c`, then calls this target with receiver `this + 304` at `0x004b2a67`.

Target decompilation id `b007-000345-current2-decompile-target` and disassembly id `b007-000345-current2-disasm-target` prove the formal body shape:

- The function prologue starts at `0x004b3650` and returns with `retn 8` at `0x004b3845`, matching the documented exclusive end `0x004b3848`.
- `ecx` is saved as the tree receiver at `0x004b3678`.
- The pending container is seeded through `0x004a88e0` at `0x004b36d0` from the iterator's `+0x08` current-index field.
- Each popped index is wrapped in a temporary `TreeItor<FolderTreePane::TreeElem>` with vtable `??_7?$TreeItor@UTreeElem@FolderTreePane@@@@6B@`.
- The per-node helper call is at `0x004b375f`.
- Return state `1` is tested at `0x004b3768-0x004b376b` and return state `2` at `0x004b3771-0x004b3774`.
- Child descent reads `TreeElem::lastChildIndex` at `+0x08` (`0x004b3780`) and follows `TreeElem::previousSiblingIndex` at `+0x0c` (`0x004b380f`).
- The helper does not read `TreeElem::hidden` at `+0x20`; hidden filtering remains iterator/per-node behavior, not this traversal loop's own filter.

The callee set from `b007-000345-current2-callees` is `operator new` at `0x005c74f6`, pending-container seed helper `0x004a88e0`, per-node paint helper [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) at `0x004b5810`, deque/segmented-container grow helper `0x0043e0b0`, and deque/segmented-container destroy helper `0x00439e00`.

The per-node helper decompilation id `b007-000345-current2-decompile-node-helper` shows [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) receives the paint context and `TreeItor<FolderTreePane::TreeElem> *`, reads the owner pane from context offset `+0x00`, derives screen coordinates from `TreeElem::rowIndex` (`+0x18`) and `TreeElem::depth` (`+0x1a`), calls `GetNodeBounds` at `0x004b3010`, reads `TreeElem::name` at `+0x14`, compares against the pane selection around `FolderTreePane+0x170`, draws icon/text through GrafPort/tile helpers, returns `0` when expanded, and returns `2` when collapsed/unexpanded. This establishes `PaintFolderTreeNode` as the inferred source-facing name used by this target.

Sibling traversal decompilations show the same local traversal family: [UID:000344][0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal](by-memory/0x004b3400-0x004b3645.FolderTreeContentDimensionsTraversal.md) `0x004b3400` uses the same tree receiver, pending container, and child-link pattern for content dimensions; [UID:000346][0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal](by-memory/0x004b3850-0x004b3ae2.FolderTreeHitTestTraversal.md) `0x004b3850` uses the same state model for hit testing and returns state `1` when a node is found; [UID:000347][0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch](by-memory/0x004b3af0-0x004b3d41.FolderTreeVisibleNavigationSearch.md) `0x004b3af0` uses the same child-link pattern for visible navigation. This target is therefore source-ready as a local `FolderTreePane.cpp` traversal helper, not a public class method and not a standalone shared tree source file.

## Source-Placement And Rejected Alternatives

Accepted placement is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md) `NexusTK/ui/controls/FolderTreePane.cpp`. [UID:00005A][FolderTreePane](by-class/FolderTreePane.md) should document the private paint path at class level, and [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md)/[UID:0001WP][FolderTreePaneTreeTemplates](by-type/by-template/FolderTreePaneTreeTemplates.md) document the concrete tree-helper declaration route, but none of those support pages supersede the by-file owner/emitter route for this exact function.

Rejected alternatives preserved from the accepted B007 report:

- Leaving formal C++ blank because helper names are inferred: rejected because current MCP resolves behavior, receiver, caller, field use, and source shape well enough for first-draft C++.
- Clearing `EMITTER_UIDS:` or using a covered-by/no-code marker: rejected because this is real custom traversal code and no other page emits this deque loop, callback dispatch, stop/skip handling, or child descent.
- Moving canonical ownership to [UID:00005A][FolderTreePane](by-class/FolderTreePane.md): rejected because the class is the semantic pane context, but the binary receiver is `Tree<FolderTreePane::TreeElem> *` and the generated source route remains [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Moving ownership to [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md) or UID0004YK `Tree.h`: rejected because this exact function is paint-specific consumer policy with one ordinary caller from `FolderTreePane::PaintTree`. UID0004YK owns the generic deque-backed `Tree<T>::Traverse<Visitor>` skeleton; `FolderTreePane.cpp` owns the concrete paint context, callback dispatch, stop/skip interpretation, and [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) integration. There is intentionally no authored `Tree.cpp`.
- Folding the body into [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md): rejected because [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) draws one node; this page owns traversal, pending-node storage, callback dispatch, and child descent.
- Using raw decompiler labels (`sub_4B3650`, `sub_4B5810`, temporaries) or raw deque internals: rejected by final-source standards.
- Emitting `std::stack<int>`: rejected as less evidence-specific than the observed deque/back/pop/push behavior.
- Splitting the range: rejected because MCP confirms a single function at `0x004b3650`, size `0x1f8`, with return at `0x004b3845`.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | Current MCP resolves the former code-entry blocker: exact range, sole paint-path caller, tree receiver, pending deque behavior, per-node render-helper delegation, stop/skip states, child-link order, sibling traversal comparison, source placement, and formal first-draft C++ are documented. |
| Confidence | 91 | Live MCP session `agent_b009_0002my_20260628` backs the caller/xref/callee/decompile/disasm evidence. Confidence remains below final audit because original symbol spelling for `TraversePaintNodes`, `FolderTreePaintTraversalContext`, and `PaintFolderTreeNode` is inferred. |
| Reconstructable | true | Source-owned folder-tree paint traversal required by rebuilt `FolderTreePane`. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis keeps this as a file-local paint traversal callback called from `FolderTreePane::PaintTree`. It is source-owned by [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), feeds [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md), and uses the `FolderTreePane::TreeElem` source names `rowIndex`, `depth`, `childrenLoaded`, `expanded`, `hidden` / `skipVisibleTraversal`, and `name`.

Do not model this helper as a public virtual method. The public/virtual paint slot remains on the core class page; this callback is internal traversal machinery for visible rows and folder icon/name drawing.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md)
- [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)
- [UID:0000F9][Tree_struct_FolderTreePane__TreeElem_](by-class/Tree_struct_FolderTreePane__TreeElem_.md)
- [UID:0001WP][FolderTreePaneTreeTemplates](by-type/by-template/FolderTreePaneTreeTemplates.md)

## 2026-07-29 UID0002MZ Generic Traverse Specialization Reconciliation

- Formal CPP is the concrete `Traverse<FolderTreePaintTraversalContext>` specialization in `FolderTreePane.cpp`; no FolderTree-specific method is added to generic `Tree<T>`.
- Generic last-child/previous-sibling links are read from `TreeNode<TreeElem>`. The per-node callback is the legal private static member `FolderTreePane::PaintTreeNode`, not a file-local function reaching private pane state.
- Exact deque seed/pop/push behavior, temporary iterator construction, stop/skip states, child ordering, sole paint caller, target range, and helper call remain unchanged.
- The former `TraversePaintNodes`/`PaintFolderTreeNode` source shape and monolithic node model are retained only as historical assumptions. Score/route/range remain `90/91`, UID0000JG.

## Changes

- 2026-08-24 B002 UID0004YK Gate 2A source-placement correction:
  - Replaced the superseded no-scored-shared-owner rationale with the current boundary: UID0004YK `Tree.h` owns the generic traversal skeleton, while this exact paint specialization remains `FolderTreePane.cpp` consumer policy because of its sole paint caller, concrete context, callback, and state semantics.

- 2026-07-14 B001 UID0002N0 field-name synchronization:
  - Preserved `90/91`, owner/emitter UID0000JG, reconstructable true, blank position, and the complete accepted paint-traversal body.
  - Updated existing negative evidence to final `hidden`: this aggregate traversal does not read `+0x20`; UID0002N0 and UID00034H retain the actual hidden filtering evidence.

- 2026-07-12 B003 UID000149 factorization sync: preserved score/route/body and added exact initial-call/later-inline `std::deque<int>` evidence; `<deque>` is supplied once by UID0001WP.

- 2026-06-28 B007 accepted empty-emitter implementation:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`, owner/emitter [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), blank `Item Summary`, and blank formal C++ caused generated `FolderTreePane.cpp` to emit [UID:000345] only as an `Empty Emitter Marker`.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`, same owner/emitter route [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), concise `Item Summary`, and formal `Tree<FolderTreePane::TreeElem>::TraversePaintNodes` C++ in the validator-managed `RECONSTRUCTION_CPP CODE` block.
  - Summary/evidence: B007 MCP session/database `agent_b009_0002my_20260628` reconfirmed the exact `0x004b3650-0x004b3848` function, sole `PaintTree` caller at `0x004b2a67`, tree receiver at `FolderTreePane+0x130`, callee route to [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md), stop/skip states, `lastChildIndex`/`previousSiblingIndex` child descent, absence of `+0x20` hidden-skip reads in this helper, and sibling traversal family shape. The final C++ names are inferred but source-facing; raw `sub_` labels, blank C++, no-code markers, owner/emitter clearing, standalone tree ownership, and folding into [UID:00034H][0x004b5810-0x004b59d5.FolderTreePaintNodeHelper](by-memory/0x004b5810-0x004b59d5.FolderTreePaintNodeHelper.md) were rejected.

- 2026-06-10 B001-028 split repair:
  - What existed before: this exact paint traversal helper was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b3650-0x004b3848`.
  - Summary/evidence: IDA MCP confirms the `0x1f8`-byte function, sole paint-path caller, `0x004b5810` delegation, and visible-child traversal behavior.
