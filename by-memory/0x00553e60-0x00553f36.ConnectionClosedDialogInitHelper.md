*** UID:0001FU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000036 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000036 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:30 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ConnectionClosedDialog::InitializeConnectionState()
{
    if (g_pActiveTextEditPane != NULL && g_pActiveTextEditPane->GetTextLength() > 0)
        new CopyWindow;

    g_packetSender->PostSocketModeByteCommand(1);
    g_packetSender->WaitForQueueDrain();

    if (g_pScreenDimmer != NULL)
        g_pScreenDimmer->SetDimmed(true);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Formal-ready ConnectionClosedDialog post-construction helper emitted through ReconnectDialog.cpp; exact callers and bounds preserve conditional construction of the same-unit private CopyWindow class, current TextEditPane/LanguageMan dependencies, packet mode byte 1, queue drain, optional dimmer notification, and stale debug-symbol rejection. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00553e60-0x00553f36 - ConnectionClosedDialog Init Helper

## Status

- Disposition: reconstructable NexusTK project code with formal helper C++ ready.
- Entity kind: `ConnectionClosedDialog` post-construction state initializer.
- Confidence: strong for function range, caller set, conditional `CopyWindow` path, packet-sender calls, optional dimmer notification, callee/global references, and endpoint; medium-high for exact original helper name, which remains inferred as `InitializeConnectionState()`.
- Direct owner/emitter: [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), child position `30`; the class then emits through [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md).
- Primary file: [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md)
- Related classes: [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), [UID:0000BR][ReconnectDialog](by-class/ReconnectDialog.md), and [UID:000039][CopyWindow](by-class/CopyWindow.md)
- Evidence basis: IDA MCP decompilation, callers, raw bytes, callee lists, and bounded caller decompilation through session `nexustk_supervisor_20260704`, with prior 2026-05/06 evidence historicalized.

## Anchors

| Address range | Owner | Notes |
| --- | --- | --- |
| `0x00553e60-0x00553f36` | Private `ConnectionClosedDialog::InitializeConnectionState()` helper | Called by both out-of-line `ConnectionClosedDialog` constructor overloads. The third context at `0x005543bb` lies inside a compiler-inlined default-constructor mirror in the reconnect replacement path, so the source method remains private. It constructs `CopyWindow` as a side path when an active text-edit pane has eligible state, then posts socket mode byte `1`, drains the queue, and optionally dims the screen. |

## Behavior Notes

- IDA currently gives this function a stale/bogus name, `?source_line@dbg_eng_data@?A0xbc047679@@QAEIQBX@Z`; MCP session `nexustk_supervisor_20260704` confirms the body is connection/reconnect UI initialization, not debug-engine source-line logic.
- IDA `callers` on 2026-05-25 reports three machine-code call sites: `0x00553ca8` in `ConnectionClosedDialog()`, `0x00553d55` in `ConnectionClosedDialog(Pane *layoutReference)`, and `0x005543bb` inside the reconnect replacement path. Byte/decompile comparison shows the third site follows an inlined mirror of the default constructor (same AlertPane arguments, Singleton publication, and three facet stores), so it is not evidence that unrelated source directly calls a private method.
- 2026-05-26 IDA MCP recheck reports the same function name, size `0xd6`, and the same three callers.
- Historical generated views named the call `InitializeConnectionState()` without emitting the helper body. This page now supplies the formal body under the inferred source-facing name `ConnectionClosedDialog::InitializeConnectionState()`; the name is source-facing and confidence-capped because no original symbol proves it.
- The helper checks [UID:0000Q7][g_pActiveTextEditPane](by-global/g_pActiveTextEditPane.md) / `dword_69BE00`, calls a text-edit query at `0x0058efa0`, and only allocates a `CopyWindow` when the query returns a positive value. B014's source-quality pass identifies `0x0058efa0` as a `TextEditPane::GetTextLength()` / `GetCharCount()`-style helper because it returns `word ptr [[this+0x134]+0x0c]`; exact original spelling remains inferred.
- The side-path `CopyWindow` construction calls `AlertPane::AlertPane` at `0x0049feb0` with the source argument shape `g_pLanguageMan->GetLocalizedString(0xf1)`, `g_pBackPane`, `L"Yes"`, and `L"No"`, then installs `CopyWindow` vtables at offsets `+0x0`, `+0xa0`, and `+0xa4`.
- After the optional `CopyWindow` path, it calls [UID:0003B0][0x00574c20-0x00574c35.PostSocketModeByteCommand](by-memory/0x00574c20-0x00574c35.PostSocketModeByteCommand.md)-style behavior on `g_packetSender` with byte `1`, then the inherited `Thread::WaitForQueueDrain()` helper, and finally calls `g_pScreenDimmer->SetDimmed(true)` if the dimmer singleton is present. Those globals are not owned by `CopyWindow`.
- Rejected owners remain: `CopyWindow` is only an optional side-effect allocation; the ReconnectDialog replacement path contains an inlined ConnectionClosedDialog constructor mirror but does not own this private method; `Socket`, `Thread`, and `ScreenDimmer` provide dependencies only; the stale debug namespace is rejected as a naming/ownership source.

## Touched State

| Address / field | Evidence | Role |
| --- | --- | --- |
| `dword_69BE00` / [UID:0000Q7][g_pActiveTextEditPane](by-global/g_pActiveTextEditPane.md) | Read at `0x00553e84`; queried by `sub_58EFA0`. | Gates the optional CopyWindow prompt when an active text edit pane has eligible state. |
| `0x0058efa0` | Called after loading `g_pActiveTextEditPane`; B014 disassembly shows it returns `word ptr [[this+0x134]+0x0c]`. | Best source-facing direction is `TextEditPane::GetTextLength()` / `GetCharCount()`. This is a TextEditPane dependency, not a CopyWindow method. |
| allocation size `0x270` | Pushed at `0x00553e98`; allocated through `sub_4F4AA0`. | Size of the inline `CopyWindow` object constructed on the side path. |
| [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md) / `dword_67A750` + string id `0xf1` | Used by `sub_4F0350` at `0x00553ed0`. | Resolves localized CopyWindow alert text. |
| [UID:00028J][0x0067a740-0x0067a744.g_pBackPane](by-memory/0x0067a740-0x0067a744.g_pBackPane.md), [UID:0003G6][0x00622f44-0x00622f4c.CopyWindowYesButtonWideString](by-memory/0x00622f44-0x00622f4c.CopyWindowYesButtonWideString.md), [UID:0003G5][0x00622f3c-0x00622f44.CopyWindowNoButtonWideString](by-memory/0x00622f3c-0x00622f44.CopyWindowNoButtonWideString.md) | Passed to `AlertPane` constructor `sub_49FEB0`; `0x00622f44` is source literal `L"Yes"` and current canonical IDA state is one eight-byte `[0x00622f44,0x00622f4c)` item named `aYes`, public type `const wchar_t`, with head refs `{0x00553ec0,0x005544ca}` and zero interior refs. The former `off_622F44` split and two-call handoff are historical pre-Gate-2B evidence only. | Alert layout/reference and primary/secondary label setup for the CopyWindow prompt. |
| `CopyWindow` vtables | Stored at `0x00553edd`, `0x00553ee3`, and `0x00553eed`. | Installs primary/secondary/tertiary CopyWindow vtables on the inline object. |
| `g_packetSender` / `dword_67A7EC` | Passed to `sub_574C20(..., 1)` and `sub_596620`. | Source-facing dependency is `g_packetSender->PostSocketModeByteCommand(1)` followed by inherited `Thread::WaitForQueueDrain()`; this is Socket/Thread consumer evidence, not Socket/Thread ownership. |
| `g_pScreenDimmer` / `dword_69AE08` | Read at `0x00553f16`; virtual call with argument `1` if present. | Optional `g_pScreenDimmer->SetDimmed(true)` notification after the packet-sender queue drain. ScreenDimmer is a dependency, not the helper owner. |

## Boundary Notes

- IDA MCP session `nexustk_supervisor_20260704` reports this as a function at `0x00553e60` with size `0xd6` / 214 bytes, matching exclusive end `0x00553f36`.
- 2026-06-01 IDA MCP byte review shows `0x00553e5b-0x00553e60` is five `0xcc` bytes between `ConnectionClosedDialog::OnLeaveClicked` and this helper.
- 2026-06-01 IDA MCP byte review shows `0x00553f35` is this helper's final `retn` byte and `0x00553f36-0x00553f40` is ten `0xcc` bytes before the next `ReconnectDialog` constructor.
- This function is physically interleaved in the reconnect dialog address island and remains a private ConnectionClosedDialog method. The complete private `CopyWindow` class is emitted earlier in the same `ReconnectDialog.cpp` translation unit, so `new CopyWindow` has a complete declaration without moving helper ownership.
- Keep the actual `CopyWindow` constructor/callback island documented separately in [UID:0001FV][0x005544c0-0x0055454b.CopyWindow](by-memory/0x005544c0-0x0055454b.CopyWindow.md).

## IDA MCP Evidence

| Check | Result |
| --- | --- |
| `lookup_funcs 0x00553e60` | Function start named with stale debug symbol `?source_line@dbg_eng_data...`, size `0xd6`, end-exclusive `0x00553f36`. |
| `callers 0x00553e60` | Three callers: `0x00553ca8` (`sub_553C10`), `0x00553d55` (`sub_553CC0`), and `0x005543bb` (`sub_554210`). |
| `callees 0x00553e60` | `sub_4F0350`, `sub_58EFA0`, `sub_596620`, `sub_4F4AA0`, `sub_574C20`, and `sub_49FEB0`. |
| raw bytes | Function body is 214 bytes from `0x00553e60-0x00553f36`; five pre-bytes at `0x00553e5b-0x00553e60` and ten post-bytes at `0x00553f36-0x00553f40` are `0xcc` padding; final instructions restore SEH state and return at `0x00553f35`. |
| selected decompile/disasm facts | Null-checks `g_pActiveTextEditPane`, calls the `TextEditPane::GetTextLength()` / `GetCharCount()`-style helper, constructs `CopyWindow` inline through `AlertPane` arguments when the count is positive, posts socket mode byte `1`, drains the inherited thread queue, and optionally calls the screen dimmer vtable with `true`. |

## Source Placement And Access

- The method is a private child of [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), not a file-root free function. Its accepted class declaration contains `private: void InitializeConnectionState();`.
- It emits at class-child position `30`, after default constructor position `10` and alternate constructor/callback cluster position `20`, and before covered-by vtable evidence position `40`.
- The existing exact formal body is retained without alteration. It models source-authored control flow and same-unit private-class use while omitting inlined constructor internals, vptr stores, EH scaffolding, and stale raw names that the compiler or owned children regenerate.

## UID0000IH Source-Dependency Closure - 2026-08-18

- The helper's three exact callers remain `0x00553ca8`, `0x00553d55`, and `0x005543bb`; the third is an inlined default-constructor mirror rather than a separate public helper route.
- The active editor is TextEditPane-owned `g_pActiveTextEditPane`. Its text-length query gates allocation; the helper does not own the global or clipboard method.
- `CopyWindow` now emits as a private class through [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) before this method. This resolves the generated undeclared-type defect while retaining direct semantic ownership here.
- The conditional class construction is followed by `g_packetSender->PostSocketModeByteCommand(1)`, `WaitForQueueDrain()`, and `g_pScreenDimmer->SetDimmed(true)` when the dimmer exists. Those behaviors and dependencies are unchanged.
- The historical `GetString` and standalone-CopyWindow-file dependency descriptions are superseded by current `GetLocalizedString` and same-translation-unit source closure.
- Exact original helper spelling is not recoverable from the stale debug symbol. `InitializeConnectionState()` is the selected high-probability human source name because all call contexts are ConnectionClosedDialog construction and the body establishes shared connection-modal/session state. The exact range, three-caller model, source placement, complete formal behavior, and generated closure support the current `92/94`; bounded lexical uncertainty explains why confidence is not higher and is not a reason to blank the formal.

## Historical / Superseded Assumptions

- Earlier text described `0x005543bb` as a direct `ReconnectDialog::OnButtonClick` call after inline replacement construction. The exact machine-code caller remains valid, but the source interpretation is now refined: the surrounding body is a compiler-inlined default `ConnectionClosedDialog` constructor mirror, which preserves private helper access.
- The prior direct file emitter UID0000N0 produced a method definition without the compile-visible class child route. The source file remains ReconnectDialog.cpp, but the correct direct emitter is UID000036 so class ordering and access are explicit.
- The stale `?source_line@dbg_eng_data...` label remains rejected. It is retained as negative evidence and does not cap the source-facing name to decompiler output.

## Cross-References

- [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md)
- [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md)
- [UID:0000BR][ReconnectDialog](by-class/ReconnectDialog.md)
- [UID:0000IH][CopyWindow](by-file/CopyWindow.md)
- [UID:0001FV][0x005544c0-0x0055454b.CopyWindow](by-memory/0x005544c0-0x0055454b.CopyWindow.md)
- [UID:0000EO][TextEditPane](by-class/TextEditPane.md)
- [UID:0000Q7][g_pActiveTextEditPane](by-global/g_pActiveTextEditPane.md)
- [UID:0000NS][Socket](by-file/Socket.md)
- [UID:0000EV][Thread](by-class/Thread.md)

## Changes

- 2026-08-22 B008 C-IH-013 current-truth callback: Preserved the helper's exact `L"Yes"` operand, historicalized the former split/two-call itemization, and recorded the completed persisted one-item `aYes` endpoint without changing helper behavior, ownership, score, callers, or formal source.
- Historical/superseded pre-Gate-2B checkpoint - 2026-08-22 B008 C-IH-013 callback: This callback qualified the helper's exact `L"Yes"` operand against the then-canonical split IDA itemization and linked the then-pending deterministic item handoff to UID0003G6 without changing helper behavior, ownership, score, callers, or formal source. Completed supervisor Gate 2B later persisted one eight-byte `[0x00622f44,0x00622f4c)` item named `aYes` with public type `const wchar_t` through one `make_data` call, so the split/handoff statement is historical evidence only and is not current authority.
- 2026-07-22 B001 accepted UID00023O implementation callback:
  - Retained `89/91`, canonical owner UID000036, reconstructable true, and the exact accepted formal body.
  - Changed direct emitter `0000N0 -> 000036` and assigned class-child position `30`; the class continues through the ReconnectDialog.cpp source root.
  - Refined `0x005543bb` from an apparent external private-method call to a compiler-inlined default-constructor mirror and added exact source-order, private-access, inferred-name, compiler-boundary, and historical evidence without removing prior behavior or dependency detail.

- 2026-07-04 B013 implementation callback:
  - Changed completion/confidence from `86/88` to `89/91`, changed canonical owner from [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) to [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md), and kept emission through [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md).
  - Added the formal `ConnectionClosedDialog::InitializeConnectionState()` helper body under the inferred source-facing name and preserved the confidence cap for exact original spelling.
  - Incorporated MCP session `nexustk_supervisor_20260704`: exact range `0x00553e60-0x00553f36`, size `0xd6` / 214, padding bytes, caller xrefs `0x00553ca8`, `0x00553d55`, and `0x005543bb`, callee list, CopyWindow side-path construction, `g_packetSender->PostSocketModeByteCommand(1)`, inherited queue drain, optional `g_pScreenDimmer->SetDimmed(true)`, and stale debug-symbol rejection.

- 2026-06-01: Raised completion/confidence from `50/65` to `86/88`, marked reconstructable, and attached to [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md).
  - Corrected the documented end-exclusive address from `0x00553f35` to `0x00553f36`.
  - Added touched-state/global table, callee evidence, exact caller map, raw boundary/padding evidence, and the `CopyWindow` side-path details.
  - C++ remains blank because the generated helper name is only a call-site convenience and the stale IDA debug symbol is not source-quality naming evidence.
- 2026-06-22 B014 CopyWindow support sync:
  - Score unchanged at `86/88`.
  - Identified `0x0058efa0` as a TextEditPane text-length/character-count query, corrected the inline `CopyWindow` constructor arguments to `g_pLanguageMan->GetLocalizedString(0xf1)`, `g_pBackPane`, `L"Yes"`, and `L"No"`, and preserved [UID:0000N0][ReconnectDialog](by-file/ReconnectDialog.md) ownership because all callers remain connection/reconnect-flow code.
