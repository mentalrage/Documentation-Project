*** UID:0004HO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000087 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000087 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MessageShowPane::OnPaint()
{
    RectBounds lineBounds;
    GetBounds(&lineBounds);
    SetDrawColor(0);
    FillRect(&lineBounds);

    wchar_t textCopy[0x8000];
    wcscpy_s(textCopy, 0x8000, m_messageText);

    wchar_t *lineStart = textCopy;
    wchar_t *lineEnd = wcsstr(lineStart, g_wszNewline);
    while (lineEnd != NULL)
    {
        *lineEnd = 0;

        const int lineWidth = GetTextWidth(lineStart);
        const int lineAdvance = 12 * ((lineWidth - 1) / 288) + 12;
        const int textLength = static_cast<int>(wcslen(lineStart));

        RectBounds textBounds = lineBounds;
        textBounds.right -= 3;
        textBounds.bottom = textBounds.top + lineAdvance + 2;

        SetTextColor(143);
        textBounds.Offset(1, 0);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(1, 0);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(-2, 1);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(3, 0);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(-2, 1);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(1, 0);
        DrawTextInRect(lineStart, textLength, &textBounds);

        SetTextColor(128);
        textBounds.Offset(-1, -1);
        DrawTextInRect(lineStart, textLength, &textBounds);
        textBounds.Offset(1, 0);
        DrawTextInRect(lineStart, textLength, &textBounds);

        lineBounds.Offset(0, lineAdvance + 3);
        lineStart = lineEnd + 1;
        lineEnd = wcsstr(lineStart, g_wszNewline);
    }

    const int lineWidth = GetTextWidth(lineStart);
    const int lineAdvance = 12 * ((lineWidth - 1) / 288) + 12;
    const int textLength = static_cast<int>(wcslen(lineStart));

    RectBounds textBounds = lineBounds;
    textBounds.right -= 3;
    textBounds.bottom = textBounds.top + lineAdvance + 2;

    SetTextColor(143);
    textBounds.Offset(1, 0);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(1, 0);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(-2, 1);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(3, 0);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(-2, 1);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(1, 0);
    DrawTextInRect(lineStart, textLength, &textBounds);

    SetTextColor(128);
    textBounds.Offset(-1, -1);
    DrawTextInRect(lineStart, textLength, &textBounds);
    textBounds.Offset(1, 0);
    DrawTextInRect(lineStart, textLength, &textBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact vtable-only `void MessageShowPane::OnPaint()` override at position 40. It fills inherited bounds, copies/splits stored wide text, uses GetTextWidth and DrawTextInRect, draws six palette-143 shadow passes and two palette-128 face passes with exact cumulative offsets, advances newline rows by the observed formula, and has no child-button collection. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00522030-0x00522530 MessageShowPane OnPaint

## Status

- Disposition: reconstructable source-authored `MessageShowPane::OnPaint` virtual override.
- Direct owner/emitter: [UID:000087][MessageShowPane](by-class/MessageShowPane.md) `MessageShowPane`.
- Likely source module: [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md) `MessageShowPane.cpp`.
- Confidence is very strong for exact identity, void signature, behavior, helper contracts, draw order, vtable-only liveness, and source placement.

## Covered Range

| Range | Current or proposed name | Notes |
| --- | --- | --- |
| `0x00522030-0x00522530` | `void MessageShowPane::OnPaint()` | Paints the overlay background and wrapped shadow/face text rows. |

## Behavior

- Copies inherited bounds through `GetBounds`.
- Sets draw color zero and fills the pane rectangle.
- Copies stored `m_messageText` from `this + 0xf8` into a local `wchar_t[0x8000]` scratch buffer.
- Splits the scratch copy on `g_wszNewline`.
- Computes each row advance as `12 * ((width - 1) / 288) + 12`.
- For each row, subtracts three from the right edge and sets bottom to top plus line advance plus two.
- Draws six shadow text passes with color `143` and cumulative offsets `(1,0)`, `(1,0)`, `(-2,1)`, `(3,0)`, `(-2,1)`, and `(1,0)`.
- Draws two face text passes with color `128` and cumulative offsets `(-1,-1)` and `(1,0)`.
- Advances the base row by `lineAdvance + 3` between newline-delimited lines.
- Processes the final line with the same eight draw passes but no later row advance.

## Evidence

- Live IDA `lookup_funcs 0x00522030` reports size `0x500`, so this child covers exact half-open range `0x00522030-0x00522530`.
- Vtable data xref `0x0061fa90` points at this method in the primary `+0x44` Pane paint slot, proving virtual-only liveness and `OnPaint` identity despite no ordinary direct calls.
- Callees include `sub_4B8E00`, `sub_4B9660`, `unk_69B3FC`, `_wcscpy_s`, `_wcsstr`, `sub_4BAA70`, `sub_4B9680`, `sub_4B78F0`, and `sub_4BAD70`.
- Current GrafPort/RectBounds support maps those calls to bounds retrieval, draw-color/fill, text color, text width, rectangle offset, and `DrawTextInRect`.
- Boundary padding `0x00522025-0x00522030` separates this method from [UID:000492][0x00521fc0-0x00522025.MessageShowPaneDestructor](by-memory/0x00521fc0-0x00522025.MessageShowPaneDestructor.md).
- Successor [UID:0001C4][0x00522530-0x005226ea.MessageShowPaneSetWrappedText](by-memory/0x00522530-0x005226ea.MessageShowPaneSetWrappedText.md) starts at `0x00522530`, confirming the row-builder end boundary.

## Source Reconstruction

The managed formal block is the sole source destination and uses current accepted contracts: `RectBounds`, `GetBounds`, `SetDrawColor`, `FillRect`, `GetTextWidth`, `SetTextColor`, `RectBounds::Offset`, and `DrawTextInRect`. The broad callback/table at `0x0069b3fc` remains a GrafPort/UI dependency, not a MessageShowPane-owned source global.

Historical `BuildMessageTextButtons`, `PaneRect`, `GetTextButtonBounds`, `ClearTextButtons`, `ApplyMessagePaneBorder`, `MeasureTextWidth`, `SetTextPalette`, `OffsetPaneRect`, and `AddTextButton` names are preserved only as superseded generated/decompiler-shaped assumptions. The binary contains direct painting, not child-button construction.

## Score Rationale

| Metric | Value | Rationale |
| --- | --- | --- |
| Completion | `92` | Exact boundary, vtable slot, no-caller liveness, stored text copy, newline/final-line flow, row math, all colors/offsets/draw calls, source dependencies, rejected aliases, and final formal C++ are documented. |
| Confidence | `94` | IDA control flow/callees, current Pane/GrafPort/RectBounds contracts, vtable position, and class/file docs agree on identity, signature, behavior, and placement. |
| Assignment | `CANONICAL_OWNER:000087`, `EMITTER_UIDS:000087` | The method is directly owned by the `MessageShowPane` class, and that class routes generated source through [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md). |

## Cross-References

- [UID:000087][MessageShowPane](by-class/MessageShowPane.md)
- [UID:0000LB][MessageShowPane](by-file/MessageShowPane.md)
- [UID:0002Y8][0x00521da0-0x005227c6.MessageShowPaneMethods](by-memory/0x00521da0-0x005227c6.MessageShowPaneMethods.md)
- [UID:0001C4][0x00522530-0x005226ea.MessageShowPaneSetWrappedText](by-memory/0x00522530-0x005226ea.MessageShowPaneSetWrappedText.md)
- [UID:0003PC][0x0061fa48-0x0061fad0.MessageShowPaneVtableData](by-memory/0x0061fa48-0x0061fad0.MessageShowPaneVtableData.md)
- [UID:0000RK][g_pMessageShowPane](by-global/g_pMessageShowPane.md)
- [UID:0001PX][0x0069b4f4-0x0069b4f8.g_pMessageShowPane](by-memory/0x0069b4f4-0x0069b4f8.g_pMessageShowPane.md)

## Negative Evidence And Rejected Alternatives

- No ordinary code caller exists, but the primary vtable cell at `0x0061fa90` is an exact live dispatch route.
- No observed call allocates, stores, clears, or owns a child-button collection.
- No evidence supports a non-void source return; residual EAX is decompiler noise and peer Pane paint declarations are void.
- Do not emit a separate border helper, text-button builder, palette helper, or raw `sub_` call when current source contracts represent the exact operations.

## Changes

- 2026-07-16 B002 UID0003PC implementation callback:
  - UID-preserving rename from `MessageShowPaneBuildTextButtons` to `MessageShowPaneOnPaint`.
  - Raised `88/90` to `92/94`, retained owner/emitter UID000087 and reconstructable true, and set emitter position `40`.
  - Installed the exact OnPaint formal body with background fill, newline and final-line flow, six shadow/two face passes, colors, cumulative offsets, row formula, and direct GrafPort/RectBounds helpers.
  - Historicalized the child-button/helper fiction while preserving exact range, vtable-only liveness, callees, padding, and negative evidence.
