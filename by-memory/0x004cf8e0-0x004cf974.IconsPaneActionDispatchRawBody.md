*** UID:00022Q | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __stdcall DispatchIconsPaneAction(int action)
{
    if (g_activeMapPane->m_inputLocked)
        return;

    switch (action)
    {
    case 0:
        SwitchGeneralPurposePanelMode2WithClickSound();
        break;

    case 1:
        SwitchGeneralPurposePanelMode3WithClickSound();
        break;

    case 2:
        RequestLocalPlayerStatus(0);
        break;

    case 3:
        g_pCollectionData->SyncNationEntriesOrDeferredState();
        break;

    case 4:
        EnsureNormalBulletinSession();
        break;

    case 5:
        g_pCollectionData->OpenInputPaneForCurrentSayMode();
        break;

    case 6:
        SendOpcode43MinusOneRequest();
        break;

    case 7:
        OpenQuitPrompt();
        break;

    default:
        break;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Eight-case retained `IconsPane.cpp` action dispatcher with first-draft formal C++; accepted evidence confirms exact table/body bytes, downstream helper roles, matching `IconsPane`/`TabPane` action helper calls, and no function object, direct xref, caller, or entrypoint pointer hit. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cf8e0-0x004cf974 IconsPaneActionDispatchRawBody

## Status

- Disposition: retained raw old-HUD action dispatcher with embedded jump table.
- Entity kind: raw/unmodeled dispatch helper body plus local switch table.
- Likely source owner: [UID:0000JZ][IconsPane](by-file/IconsPane.md) / [UID:00006B][IconsPane](by-class/IconsPane.md), or a small shared old-HUD action helper adjacent to [UID:0000OF][TabPane](by-file/TabPane.md).
- Source-facing helper: first-draft retained file-local helper `DispatchIconsPaneAction(int action)`, using `__stdcall` because the raw body reads the action from `[ebp+8]`, never consumes a `this` pointer, and returns normally after dispatch cases.
- Rebuild handling: emit first-draft helper C++ through the `IconsPane.cpp` owner/emitter route while preserving the no-route evidence. The helper is retained source logic, not padding and not a TabPane-owned method; final placement may still collapse into adjacent `IconsPane` click handling if a later source-shape pass proves that was how the duplicate was generated.
- Confidence: strong for byte boundaries, jump-table contents, behavior, source neighborhood, helper call roles, and current unreferenced status; capped below final audit because no caller/address-taken route to the raw entry has been recovered.

## Range Breakdown

| Range | Type | Notes |
| --- | --- | --- |
| `0x004cf8e0-0x004cf952` | raw dispatcher body | Checks active-map input lock, switches over an action id `0..7`, and calls the matching UI/session helper. |
| `0x004cf952-0x004cf954` | local alignment | `66 90` two-byte nop before the jump table. |
| `0x004cf954-0x004cf974` | jump table | Eight dword targets for cases `0..7`. |

2026-06-04 IDA byte check confirms `0x004cf8d5-0x004cf8e0` is eleven `0xcc` alignment bytes and `0x004cf974-0x004cf980` is twelve `0xcc` alignment bytes. The jump table at `0x004cf954` contains these eight dword targets: `0x004cf906`, `0x004cf90f`, `0x004cf918`, `0x004cf925`, `0x004cf92e`, `0x004cf937`, `0x004cf940`, and `0x004cf949`.

## Behavior

The raw body first reads [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md) / `dword_67A764` and checks byte `+0x3f0` (`+1008`). If that lock byte is set, it returns without dispatching. Otherwise it switches on the incoming action id:

| Case | Target | Meaning evidence |
| --- | --- | --- |
| `0` | `0x005a4db0` | Behavior name `SwitchGeneralPurposePanelMode2WithClickSound`; live IDA shows this helper calls `sub_4B8650(dword_67A874, 2, 0)` and then `sub_57A5C0(dword_67A7D0, 0x198, 0x64)`. Icon-local names such as `OpenArrangeDialog` are rejected. |
| `1` | `0x005a4e40` | Behavior name `SwitchGeneralPurposePanelMode3WithClickSound`; live IDA shows the same sequence with mode value `3`. Icon-local names such as `OpenOptionsDialog` are rejected. |
| `2` | `0x005a4f70(0)` | Calls [UID:0004YC][0x005a4f70-0x005a500f.RequestLocalPlayerStatus](by-memory/0x005a4f70-0x005a500f.RequestLocalPlayerStatus.md) with request time `0`; the helper sends `{0x2d,0}` and writes zero to the EPF or legacy local-player status request-time slot selected by `g_useEpfAssets`. Historical `SendOpcode2DAndStoreExtra` and raw dword names remain search aliases only. |
| `3` | `0x005a5010(dword_67A748)` | Behavior name `SyncNationEntriesOrDeferredState`; shared client-state helper that requests nation entries through `sub_503C70`, sends opcode `0x18` when the local count is below the server count, or forwards deferred data through `sub_4AA050`. Use [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md), not generated `g_pAppMan`, for `dword_67A748`. |
| `4` | `0x005a50a0` | Behavior name `EnsureNormalBulletinSession`; lazy normal bulletin-session initializer; if `dword_67ADC0` is null, it allocates `0x104` bytes and calls the bulletin-session constructor path. `OpenMailDialog` is too UI-label-specific for the helper body. |
| `5` | `0x005a5110(dword_67A748)` | Behavior name `OpenInputPaneForCurrentSayMode`; current say-mode input dispatcher, documented in [UID:0002RX][0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode](by-memory/0x005a5110-0x005a5337.OpenInputPaneForCurrentSayMode.md). |
| `6` | `0x005a5340` | Behavior name `SendOpcode43MinusOneRequest`; builds and sends a ten-byte opcode `0x43` request containing fields `1`, `-1`, `4`, and `0`. |
| `7` | `0x005a5a80` | Behavior name `OpenQuitPrompt` / `OpenQuitPromptThunk`; five-byte thunk to `sub_5A94B0`, the quit-prompt launcher documented by [UID:0000TD][OpenQuitPrompt_5A94B0](by-global/OpenQuitPrompt_5A94B0.md). |

This action mapping matches the click-action switch embedded in [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md) `IconsPane::OnMouseEvent` at `0x004cf6d8-0x004cf712`, including the same `dword_67A764 + 1008` input-lock gate.

The downstream action helpers are also shared with [UID:0000EB][TabPane](by-class/TabPane.md) `HandleInputEvent` at `0x004cfa10`, which calls the same mode/tab/session helpers for tab actions. This makes the raw body a plausible old-HUD shared action-dispatch helper physically emitted between `IconsPane` and `TabPane`, but current IDA still has no caller to its entrypoint.

## Raw Assembly Shape

```asm
004cf8e0  push    ebp
004cf8e1  mov     ebp, esp
004cf8e3  mov     eax, dword_67A764
004cf8e8  cmp     byte ptr [eax+3F0h], 0
004cf8ef  jnz     short def_4CF8FF
004cf8f1  mov     eax, [ebp+8]
004cf8f4  mov     ecx, dword_67A748
004cf8fa  cmp     eax, 7
004cf8fd  ja      short def_4CF8FF
004cf8ff  jmp     ds:jpt_4CF8FF[eax*4]
004cf906  call    sub_5A4DB0
004cf90f  call    sub_5A4E40
004cf918  mov     dword ptr [ebp+8], 0
004cf920  jmp     sub_5A4F70
004cf925  call    sub_5A5010
004cf92e  call    sub_5A50A0
004cf937  call    sub_5A5110
004cf940  call    sub_5A5340
004cf949  call    sub_5A5A80
```

## Source-Shape Note

The body is an eight-case old-HUD action dispatcher that mirrors the live click-action switch in `IconsPane::OnMouseEvent` and overlaps the shared action helpers used by `TabPane::HandleInputEvent`. The no-route evidence no longer keeps this page blank: the retained body is modeled as a file-local `IconsPane.cpp` helper with first-draft formal C++, while the documentation keeps the caller/address-taken caveat explicit. The source-facing shape uses behavior names rather than icon-label names because the callees manipulate panel modes, packet sends, nation/deferred collection state, bulletin sessions, say-mode input, opcode `0x43`, and the quit prompt.

## Evidence Notes

- 2026-06-27 B012 accepted implementation callback preserved the accepted report evidence: prior MCP-backed analysis reported no modeled function at the entry/table/end addresses, successor `sub_4CF980`, exact body/table bytes, local-only jump-table xrefs, zero raw-entry xrefs, the unique full-range signature, downstream helper target analysis, and PE no-route scans for VA/RVA/direct-branch hits. No new MCP claim is made here; this implementation records the accepted report's evidence into the target.
- 2026-06-15 live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirms no function object at `0x004cf8e0`, `0x004cf8ff`, `0x004cf952`, `0x004cf954`, or `0x004cf974`; the next modeled function remains `sub_4CF980` at `0x004cf980`.
- 2026-06-15 live disassembly of the raw span shows the `dword_67A764 + 0x3f0` gate, the `0..7` switch, the indirect jump through `jpt_4CF8FF` at `0x004cf954`, case calls/tail-call to `0x005a4db0`, `0x005a4e40`, `0x005a4f70`, `0x005a5010`, `0x005a50a0`, `0x005a5110`, `0x005a5340`, and `0x005a5a80`, and the padding handoff at `0x004cf974`.
- 2026-06-15 live `xrefs_to` still reports zero references to the entrypoint `0x004cf8e0`, one local data reference to the jump table at `0x004cf8ff -> 0x004cf954`, and downstream-helper xrefs from the modeled `IconsPane`/`TabPane` paths plus this raw span.
- 2026-06-15 `make_signature_for_range 0x004cf8e0-0x004cf974` with operand wildcards returns a unique full-range signature. This supports the byte identity of the orphaned raw body while keeping the source-shape blocker explicit.
- 2026-06-17 B002 PE-aware scan found no VA pointer, RVA pointer, or direct branch to `0x004cf8e0`; the sole VA-pointer-like hit for `0x004cf954` is inside this raw body's own switch-table dispatch. This independently supports the current no-entry/no-address-taken interpretation.
- 2026-06-17 B002 compared the raw switch against live `IconsPane::OnMouseEvent` at `0x004cf6d8-0x004cf712` and confirmed the same eight helper sequence, keeping this page as retained old-HUD action evidence while the 2026-06-27 callback now emits a first-draft retained helper body.
- 2026-06-19 B003 direct PE scan repeated the no-route check while auditing [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md): no loaded VA pointer, RVA pointer, or direct branch targets `0x004cf8e0`, and the only `0x004cf954` pointer-like hit remains the local jump-table operand at `0x004cf902`. This keeps the raw body as retained no-route action evidence even though first-draft source is now emitted for the retained helper.
- 2026-06-04 live IDA reports no function object at `0x004cf8e0`, `0x004cf8ff`, `0x004cf952`, `0x004cf954`, or `0x004cf974`; the successor is modeled as `sub_4CF980` at `0x004cf980-0x004cf9ca`.
- 2026-06-04 live IDA boundary bytes show eleven `0xcc` bytes at `0x004cf8d5-0x004cf8e0`, `66 90` at `0x004cf952-0x004cf954`, eight dword jump-table entries at `0x004cf954-0x004cf974`, and twelve `0xcc` bytes at `0x004cf974-0x004cf980`.
- 2026-06-04 live IDA `xrefs_to 0x004cf8e0` finds no entrypoint references. Xrefs to the case labels are only the internal switch jump at `0x004cf8ff` plus the local table at `0x004cf954`.
- 2026-06-04 live raw pointer scans found no loaded-segment dword equal to `0x004cf8e0`; the only literal `0x004cf954` hit is the switch-jump operand at `0x004cf902`.
- IDA MCP disassembly shows normal function-shaped bytes beginning with `push ebp; mov ebp, esp`, followed by an eight-case indirect jump through the local table at `0x004cf954`.
- 2026-06-04 live IDA confirms the modeled `IconsPane::OnMouseEvent` switch at `0x004cf6d3-0x004cf712` calls the same eight helper targets in the same order, with case `2` passing `0` to `sub_5A4F70`.
- 2026-06-04 live IDA confirms `TabPane` action code at `0x004cfa55-0x004cfa93` calls the same `sub_5A4F70`, `sub_5A4DB0`, and `sub_5A4E40` helpers for tab actions.
- `simroot_v2` should be treated only as a lead source here. The stronger evidence is IDA's raw byte layout plus the duplicate action switch already present in the IDA-modeled `IconsPane::OnMouseEvent`.
- 2026-06-04 live IDA `xrefs_to` for downstream helper targets shows calls from the live `IconsPane` handler at `0x004cf6df..0x004cf712`, this raw body at `0x004cf906..0x004cf949`, and shared UI paths such as `0x004cfa61`, `0x004cfa7a`, `0x004cfa93`, `0x00507721`, `0x005bd12d`, `0x005a6d18`, `0x005abe8c`, and `0x005a708b`.

## Source-Quality Decisions

- `DispatchIconsPaneAction` is the current best source-facing helper name. It describes the local action-id switch without overfitting to icon labels or address-shaped names.
- `__stdcall` is used for first-draft C++ because the raw prologue reads only the stack argument and does not consume an implicit object pointer. The helper is file-local/static in `IconsPane.cpp`.
- `g_activeMapPane->m_inputLocked` is the best source-facing field name for the `dword_67A764 + 0x3f0` gate because the live `IconsPane::OnMouseEvent` path uses the same input-lock condition before allowing action dispatch.
- `g_pCollectionData` remains the object receiver for cases `3` and `5`; those callees are `thiscall` object helpers routed through `dword_67A748`, not free functions on an application manager.
- Rejected alternatives: `TabPane.cpp` ownership is rejected because the raw body physically follows `IconsPane` and mirrors the `IconsPane` click switch before the `TabPane` range; a new old-HUD source file is rejected because no route or source-boundary evidence requires one; no-owner/non-emitting treatment is rejected because the body is live project code with exact behavior and an `IconsPane` owner route; address-shaped or icon-label names are rejected because they are less source-like and less accurate than behavior names.

## Reconstruction Notes

- Do not fold this raw body into padding. It is real executable code even though IDA has not modeled it as a function.
- Do not promote it to an ordinary class method until a caller or source-level use is found. For now, model it as a retained file-local `IconsPane` action-dispatch helper with no-route evidence.
- The final source may not need a separate callable helper if the original compiler emitted this as an unreferenced duplicate of the inline click-action switch, but the first-draft helper body records the exact behavior in source-like form until the build/source explanation is proven.

## Autogen Status

- Reconstructable: true, as retained UI-panel project code with exact body/table evidence.
- Parent: [UID:0000JZ][IconsPane](by-file/IconsPane.md).
- C++: first-draft retained helper emitted here; parent aggregate pages should not duplicate the full body.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Evidence records the corrected exclusive range, modeled-function absence, exact boundary bytes, jump-table contents, internal-only case-label xrefs, negative entrypoint pointer scan, downstream helper starts/sizes, helper-body roles, matching `IconsPane`/`TabPane` action switches, a unique full-range signature, and now a first-draft retained helper C++ body. It remains below final-source completion because the raw entry is still unreferenced and the original source reason for the duplicate/helper body is unresolved. |
| Confidence | 90 | Byte boundaries, jump-table semantics, helper dispatch order, downstream helper identities, current unreferenced status, byte identity, and the source-facing helper route are supported by accepted IDA/PE evidence. Confidence is capped below final audit because IDA does not model this as a function object and no live caller or address-taken use has been recovered. |

## Cross-References

- [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md)
- [UID:000170][0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail](by-memory/0x004cf980-0x004cfe5f.TabPaneAndIconsPaneDestructorTail.md)
- [UID:00006B][IconsPane](by-class/IconsPane.md)
- [UID:0000JZ][IconsPane](by-file/IconsPane.md)
- [UID:0000EB][TabPane](by-class/TabPane.md)
- [UID:0000OF][TabPane](by-file/TabPane.md)
- [UID:0000PR][g_activeMapPane](by-global/g_activeMapPane.md)
- [UID:0000QK][g_pCollectionData](by-global/g_pCollectionData.md)
- [UID:0000HX][BulletinSession](by-file/BulletinSession.md)

## Changes

- 2026-07-29 B009 UID00037S callback: migrated only retained raw action `2` to `RequestLocalPlayerStatus(0)` and semantic request-time storage names. The raw-body/no-route disposition, all unrelated cases, `88/90` score, owner/emitter, and blank H remain unchanged.

- 2026-06-27 B012 implementation callback:
  - Before: `85/88`, owner/emitter [UID:0000JZ][IconsPane](by-file/IconsPane.md), blank formal C++ because the raw entry had no caller/address-taken proof.
  - After: `88/90`, owner/emitter unchanged, formal first-draft C++ now emits `static void __stdcall DispatchIconsPaneAction(int action)` as a retained file-local `IconsPane.cpp` helper.
  - Summary/evidence: accepted report evidence records no modeled function at the entry/table/end, successor `sub_4CF980`, exact body/table bytes, local-only table xrefs, zero raw-entry xrefs, a unique range signature, downstream helper analysis, and PE no-route scans. The no-route caveat remains documented, but it no longer blocks a source-like first draft for the retained helper.
- 2026-06-19 B003 core-gap support sync:
  - No score or owner change.
  - Summary/evidence: repeated direct PE route scans during [UID:00016Z][0x004cf1f0-0x004cf8d5.IconsPaneCore](by-memory/0x004cf1f0-0x004cf8d5.IconsPaneCore.md) review again found no VA/RVA/direct-branch route to `0x004cf8e0`, while the local `0x004cf954` table reference remains internal-only. This was the pre-2026-06-27 blank/marker-only state; the current page now emits first-draft retained helper C++ while keeping the same no-route caveat.
- 2026-05-28:
  - Before: documented as reconstructable raw code with medium-low live-use confidence.
  - After: documented as a retained raw old-HUD action dispatcher/shared-helper candidate; completion/confidence raised to medium because the jump table, padding boundaries, downstream helper calls, and current unreferenced status were rechecked.
  - Why: IDA MCP and byte-level checks still find no function object, direct entrypoint xrefs, callers, or loaded-segment pointer hits for `0x004cf8e0`, while the raw body exactly mirrors old-HUD action helper calls used by live `IconsPane` and `TabPane` code.
- 2026-06-02:
  - What existed before: the range was `65/65`, unattached, and contained a behavior-sketch C++ block.
  - Changed to: completion `72`, confidence `80`, `RECONSTRUCTABLE:TRUE`, `AUTOGEN_PARENT_UID:0000JZ`, and a prose source-shape note instead of C++.
  - Summary/evidence: existing IDA-backed byte, boundary, xref, pointer-scan, and duplicate live-handler evidence supported retained `IconsPane.cpp` ownership while keeping final C++ blank at that time; the 2026-06-27 callback supersedes the blank-C++ state with first-draft retained helper C++.
- 2026-06-04:
  - Before: the page remained in the queue at `72/80`, used stale non-IDA naming evidence for several case meanings, and treated byte `0x004cf974` as part of the raw/table range even though live bytes show it is padding.
  - After: raised to `80/86`, renamed the exact range to `0x004cf8e0-0x004cf974`, removed stale naming evidence, recorded live helper-body roles for all eight cases, and documented `0x004cf974-0x004cf980` as the following padding span.
  - Summary/evidence: live IDA reports no function object at the raw entry/table addresses, successor `sub_4CF980` at `0x004cf980-0x004cf9ca`, eleven leading `0xcc` bytes, `66 90` before the table, eight dword table entries at `0x004cf954-0x004cf974`, twelve trailing `0xcc` bytes at `0x004cf974-0x004cf980`, no direct entrypoint xrefs or entrypoint pointer hits, matching `IconsPane::OnMouseEvent` dispatch at `0x004cf6d3-0x004cf712`, shared `TabPane` helper calls at `0x004cfa55-0x004cfa93`, and downstream helper behavior at `0x005a4db0`, `0x005a4e40`, `0x005a4f70`, `0x005a5010`, `0x005a50a0`, `0x005a5110`, `0x005a5340`, and `0x005a5a80`.
- 2026-06-15:
  - Before: `80/86`, with exact body/table behavior documented but still queued as low completion.
  - After: `85/88`, owner/emitter unchanged as [UID:0000JZ][IconsPane](by-file/IconsPane.md), final C++ still blank at that time.
  - Summary/evidence: live IDA MCP `lookup_funcs`, `disasm`, `xrefs_to`, and `make_signature_for_range` reconfirmed the raw non-function entry, local jump-table dispatch, zero entrypoint xrefs, downstream helper references, successor boundary, and a unique full-range signature.
- 2026-06-17 B002 source-quality audit:
  - No score change.
  - Summary/evidence: B002 rechecked the entry and table with IDA/PE scans, found no VA/RVA/direct-branch evidence to `0x004cf8e0`, confirmed the local-only `0x004cf954` jump-table reference, revalidated the duplicate relationship to live `IconsPane::OnMouseEvent`, and replaced unsafe icon-label guesses with behavior names and rejected alternatives for all eight callees.
