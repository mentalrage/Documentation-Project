*** UID:000401 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000C6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000C6 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
SayToUserMessageInputPane::SayToUserMessageInputPane(const wchar_t *recipientName)
    : LineInputPane(0)
{
    wchar_t prompt[128];

    wcscpy_s(m_recipientName, _countof(m_recipientName), recipientName);
    swprintf_s(prompt, _countof(prompt), L"-> %s: ", m_recipientName);
    SetPromptText(prompt);
}

void SayToUserMessageInputPane::OnSubmitInput()
{
    if (TextLength() > 0) {
        wchar_t messageText[256];
        char convertedText[256];
        unsigned char packet[0x12c];

        CopyText(messageText, 255);
        ValidateChatInputOrThrowPasswordError(messageText);
        SanitizeWideTextForChat(messageText, wcslen(messageText));

        int packetLength = 0;
        PacketBufferWriteUInt8(0x19, packet + packetLength++);

        const unsigned int recipientLength = WideCharToMultiByte(
            CP_ACP, 0, m_recipientName,
            static_cast<int>(wcslen(m_recipientName)),
            convertedText, sizeof(convertedText), 0, 0);
        convertedText[recipientLength] = '\0';
        PacketBufferWriteUInt8(
            static_cast<unsigned char>(recipientLength),
            packet + packetLength++);
        memcpy(packet + packetLength, convertedText, recipientLength);
        packetLength += recipientLength;

        const unsigned int messageLength = WideCharToMultiByte(
            CP_ACP, 0, messageText,
            static_cast<int>(wcslen(messageText)),
            convertedText, sizeof(convertedText), 0, 0);
        convertedText[messageLength] = '\0';
        PacketBufferWriteUInt8(
            static_cast<unsigned char>(messageLength),
            packet + packetLength++);
        memcpy(packet + packetLength, convertedText, messageLength);
        packetLength += messageLength;

        packet[packetLength] = 0;
        g_packetSender->QueueAndSendPacket(
            packet, static_cast<short>(packetLength));
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact legacy direct-message constructor and virtual OnSubmitInput cluster under `SayInputPanes.cpp`: constructor `0x005b1570-0x005b1631`, fifteen-byte alignment `0x005b1631-0x005b1640`, submit `0x005b1640-0x005b1812`, sole direct constructor call `0x0059edf7`, independent inline construction `0x005b419b-0x005b41f9`, primary `+0x48` submit slot `0x0062f744`, exact `0x208` object with `m_recipientName[128]` at `+0x108`, inherited input lifecycle, and exact one-wide-buffer/one-reused-multibyte-buffer/300-byte opcode `0x19` serializer; declarations are class-owned in `SayInputPanes.h`, neighboring helper UID000402 remains no-route and non-emitting, and compiler range/destructor artifacts are not handwritten source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b1570-0x005b1812 SayToUserMessageInputPane Methods

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact legacy direct-message class-method cluster.
- Direct owner: [UID:0000C6][SayToUserMessageInputPane](by-class/SayToUserMessageInputPane.md), routed to [UID:0000N9][SayInputPanes](by-file/SayInputPanes.md).
- Parent split/index: [UID:0001LY][0x005b1570-0x005b2562.SayTargetMessageInputPanes](by-memory/0x005b1570-0x005b2562.SayTargetMessageInputPanes.md).
- C++ status: the formal CPP channel contains the accepted constructor and `OnSubmitInput` definitions for namespace-scope emission through `SayInputPanes.cpp`. The formal H channel remains blank because [UID:0000C6][SayToUserMessageInputPane](by-class/SayToUserMessageInputPane.md) owns the class declaration in `SayInputPanes.h`; the neighboring no-route helper is not emitted as a class dependency.

## Covered Ranges

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x005b1570-0x005b1631` | `SayToUserMessageInputPane::SayToUserMessageInputPane(const wchar_t *recipientName)` | Constructor body ends with `ret 4` at `0x005b162e`; the byte at `0x005b1630` is the `ret` immediate, so the true half-open endpoint is `0x005b1631`. |
| `0x005b1631-0x005b1640` | alignment padding | Fifteen `0xcc` bytes. |
| `0x005b1640-0x005b1812` | `SayToUserMessageInputPane::OnSubmitInput()` / existing `OnSubmit` alias | Vtable-only submit method at primary slot `0x0062f744`; builds the opcode `0x19` target/message packet from `m_recipientName[128]` and current line text. |
| `0x005b1812-0x005b1820` | successor alignment, outside UID000401 | Fourteen `0xcc` bytes separate the submit method from no-route sibling UID000402; this span is compiler alignment, not source. |

## Function Inventory

| Range / item | Source disposition | Formal destination |
| --- | --- | --- |
| `0x005b1570-0x005b1631` | Source-authored `SayToUserMessageInputPane` constructor. | This page's `RECONSTRUCTION_CPP CODE`; declaration is class-owned in UID0000C6 H. |
| `0x005b1631-0x005b1640` | Fifteen-byte compiler alignment span, SHA256 `54CC9DD9593316E80CCAF5D10856E6502A0D75F20473A92FA01D63228EF1624C`. | No source. |
| `0x005b1640-0x005b1812` | Source-authored virtual `OnSubmitInput`; zero ordinary callers are expected because primary vtable slot `+0x48` is the live route. | This page's `RECONSTRUCTION_CPP CODE`; declaration is class-owned in UID0000C6 H. |
| `0x005b1812-0x005b1820` | Fourteen-byte compiler alignment span, SHA256 `1E875C76536C8496BED8C8760B539327F24ACFFBCF0A4B941877B4BFB4C44356`. | Outside the target; no source. |
| `0x005b1820-0x005b1983` | Separate source-shaped helper clone with no modeled function or proven route. | UID000402 remains ownerless, non-reconstructable, and non-emitting. |
| `0x005b7820`, `0x005b782b`, `0x005b7af0-0x005b7b2b` | Shared compiler adjustors and scalar deleting wrapper regenerated from declarations. | No handwritten target body. |

## Evidence

- B001's 2026-06-19 PE/Capstone pass corrected the constructor endpoint from the older `0x005b1630` wording to `0x005b1631`.
- Direct route scan found one constructor call at `0x0059edf7` in `UserListPane::OnItemActivated`. The historical behavior-specific caller spelling `OpenSayToUserMessageInputPane` referred to this same primary-slot activation override and is retained only as dated terminology/path history.
- Independent inline legacy construction at `0x005b419b-0x005b41f9` corroborates the `0x208` allocation, `LineInputPane` base construction, all three vtable stores, recipient copy, and `L"-> %s: "` prompt without creating a second direct constructor xref.
- The submit method has no ordinary direct rel32 callers and is live through vtable slot `0x0062f744 -> 0x005b1640`.
- Legacy primary vtable base `0x0062f6fc` routes the scalar deleting destructor to `0x005b7af0`; secondary/tertiary vtable bases `0x0062f74c` and `0x0062f77c` route to shared adjustor thunks `0x005b7820` and `0x005b782b`.
- The constructor copies the recipient into `this+0x108`, now documented as `m_recipientName[128]`, and formats prompt literal `L"-> %s: "` from `0x00630a20`.
- The constructor body SHA256 is `E3564FA5956BB8CA814AE7593653E1086CF2B4D3405D51205BA2C3F8709CA4BB`; the submit body SHA256 is `91954E6993446B78C61EE1E972C20EC8AE4216DDDF9AB94E33D4C52FD77FC95A`. The three-vtable 136-byte window SHA256 is `DF86A1948CB1C9D5A748945E4B49442DAAF8E04459C878FFD225222A827A9B84`.
- Constructor stack evidence is one `LineInputPane *` EH/base temporary, one `_DWORD` temporary, one 256-wide-character prompt buffer beginning at `Buffer`, three EH temporaries, saved-register/return rows, source argument `Source`, and non-source `arg_4`. Submit stack evidence is one `WCHAR[256]` message buffer beginning at `WideCharStr`, one contiguous 300-byte packet beginning at `Src` (`Src`, `destination`, and following bytes), one reusable `CHAR[256] MultiByteStr`, and EH/saved-register rows.
- The submit path gates on positive `TextLength()`, copies at most 255 wide characters, calls `ValidateChatInputOrThrowPasswordError`, sanitizes through `SanitizeWideTextForChat`, converts recipient and message through the same 256-byte multibyte buffer, writes opcode `0x19` and both one-byte lengths through `PacketBufferWriteUInt8`, copies both payloads, stores a local-only packet terminator, and sends through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / `Socket::QueueAndSendPacket`.
- B013's direct legacy-class pass confirms this method child should not emit a synthetic call to the neighboring no-route helper. The binary submit body performs its own bounded copies, conversion checks, byte writes, payload copies, and final send; the helper-shaped body at `0x005b1820` remains a sibling candidate with no route.
- Neighbor [UID:000402][0x005b1820-0x005b1983.SayTargetMessagePacketHelperNoRouteA](by-memory/0x005b1820-0x005b1983.SayTargetMessagePacketHelperNoRouteA.md) is a sibling no-route helper clone and is not part of this class.

## Layout

- `LineInputPane` is exactly `0x108` bytes. `SayToUserMessageInputPane` appends `wchar_t m_recipientName[128]` at `+0x108`, producing exact complete-object size `0x208`.
- The constructor's `0x80` wchar copy bound and the independently matching inline construction prove the field width. No second target-owned field or tail gap is required.
- Primary, secondary, and tertiary vtable stores target `0x0062f6fc`, `0x0062f74c`, and `0x0062f77c`; these represent the three inherited object views rather than handwritten vtable data.

## Packet Shape

The submit method uses the shared target-message packet family: opcode `0x19`, recipient length, recipient bytes, message length, and message bytes. The physical frame proves one `WCHAR[256]` message buffer, one reused `CHAR[256]` conversion buffer, and one `unsigned char[300]` packet. The sent length is `recipientLength + messageLength + 3`; both conversion terminators and `packet[packetLength]` are local safety writes outside the counted protocol fields. Fixed-array indexing accounts for the observed compiler range-check instrumentation; silent source-level bounds returns, a second conversion array, zero-initialized scratch arrays, and a `0x230` packet are historicalized as unsupported draft assumptions.

## Input Lifecycle

- Inherited `LineInputPane::HandleKeyOrTextEvent(Event *)` owns Enter, Escape, Tab, deferred deletion, owner notification, and forwarding to the text-edit child. Enter dispatches the primary virtual at `+0x48`; UID000401 supplies `SayToUserMessageInputPane::OnSubmitInput()` at `0x0062f744 -> 0x005b1640`.
- Base primary `+0x44 -> 0x004f2790` is the distinct `OnNoOpInputPaneSlot`. Base primary `+0x48 -> 0x0041b6a0` is a one-byte `retn` and reconstructs as the inline empty default `OnSubmitInput()` declaration on `LineInputPane`.
- No target-local key, cancel, deletion, or forwarding method is justified.

## Compiler Artifacts

- Fixed local arrays and indexed terminator writes regenerate the observed `___report_rangecheckfailure` paths; no explicit failure call or silent source return belongs in formal C++.
- Shared `0x005b7af0-0x005b7b2b` scalar deleting behavior plus adjustors `0x005b7820` and `0x005b782b` are compiler output. The source class intentionally omits an explicit derived destructor declaration so its inherited virtual destructor produces the shared/folded wrapper.
- The two `0xcc` spans in the inventory are alignment only. Compiler wrappers, adjustors, vtables, EH state, and security-cookie machinery have no separate source payload.

## Negative Evidence

- Zero ordinary callers do not make `OnSubmitInput` dead; the exact primary-vtable data xref and inherited Enter dispatch prove liveness.
- Proximity and protocol equivalence do not attach UID000402: it has two stack wchar arguments, no `this` receiver, no modeled function, and no xref, VA/RVA pointer, immediate, or vtable route. Formal CPP therefore serializes directly and never calls a synthetic helper.
- Inline construction is independent source-shape corroboration, not a second constructor callsite. `g_packetSender` and prompt data are dependencies, not owners.

## Source Placement

- Definitions emit from this page through UID0000C6 into `NexusTK/social/SayInputPanes.cpp`; the class declaration emits from UID0000C6 H into `NexusTK/social/SayInputPanes.h`.
- The target H channel stays blank to avoid duplicate declarations. UID000402, compiler wrappers/adjustors/vtables, `UserListPane`, and modern direct-message code are rejected as target emitters.
- Third-party import is not applicable: this is first-party NexusTK class-method code reconstructed directly from the preserved client.

## Score Rationale

- Completion `94`: boundaries, padding, two source methods, complete frame/local shape, exact object layout, caller and inline construction, vtable liveness, inherited input lifecycle, packet behavior, compiler artifacts, formal CPP, blank-H proof, owner/emitter, and no-route sibling exclusion are resolved.
- Confidence `95`: independent body, frame, vtable, caller, inline-construction, layout, prompt, and protocol evidence converge. Original private symbols, exact local spellings, and formatting remain unavailable, so the page remains below final-source certainty.

## Cross-References

- [UID:0000C6][SayToUserMessageInputPane](by-class/SayToUserMessageInputPane.md)
- [UID:0000N9][SayInputPanes](by-file/SayInputPanes.md)
- [UID:0001LY][0x005b1570-0x005b2562.SayTargetMessageInputPanes](by-memory/0x005b1570-0x005b2562.SayTargetMessageInputPanes.md)
- [UID:0003JQ][0x00630a18-0x00630a44.DirectWhisperPromptFragments](by-memory/0x00630a18-0x00630a44.DirectWhisperPromptFragments.md)
- [UID:00047Q][0x0059ed60-0x0059ee0c.UserListPaneOpenSayToUserMessageInputPane](by-memory/0x0059ed60-0x0059ee0c.UserListPaneOpenSayToUserMessageInputPane.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)

## Changes

- 2026-08-13 B006 accepted UID000401 ordinary implementation callback:
  - Raised `86/90 -> 94/95` while preserving owner/emitter UID0000C6, reconstructable true, blank optional position, `Nested:0`, exact target range, and blank target H.
  - Replaced only the target formal CPP payload with the accepted constructor and direct `OnSubmitInput` definitions: one `WCHAR[256]` message buffer, one reused `CHAR[256]` conversion buffer, one `unsigned char[300]` packet, fixed-array/compiler-range disposition, local-only terminators, and no synthetic UID000402 call.
  - Added the complete source/Compiler inventory, both alignment spans and hashes, body/vtable hashes, exact frame interpretation, `0x208` layout, sole direct plus inline construction routes, primary `+0x48` liveness, inherited input lifecycle, implicit-destructor source cause, source placement, negative evidence, and score rationale.
  - Reconciled prior reports without loss: B001 remains accepted for split/boundaries and helper no-route evidence; B013 remains accepted for class role/layout/vtables/routes but its two conversion buffers, `0x230` packet, zero initialization, silent bounds returns, explicit derived destructor, and CPP-resident declaration are superseded by current physical evidence; UID00047Q remains accepted caller terminology/lifetime evidence and does not become additional UID000401 report coverage.
  - No additional report target is declared. Independently identified support destinations are UID0000C6, UID000077, UID0001XS, UID0000N9, UID0001LY, UID000402, UID00047Q, UID0003JQ, and UID0000Q5.
  - Gate 2B remains supervisor-owned: before any IDA action, the supervisor must freshly establish canonical IDB path/database identity with `idb_list`, `server_health`, and public `runtime_attestation`, then bind every action/readback to the exact returned database identifier. This documentation callback made no IDA mutation.

- 2026-08-13 B001 UID00047Q accepted implementation callback: preserved the complete legacy constructor/submit formal code, ranges, padding, owner/emitter, score, vtable routes, prompt, packet behavior, and no-route helper exclusion; reconciled only the sole `0x0059edf7` caller terminology to `UserListPane::OnItemActivated` and historicalized the former behavior-specific name.

- 2026-06-20 supervisor Rule 26 incorporation of B001 `0001LY-SayTargetMessageInputPanes-source-quality.md`: created this exact child for the legacy direct-message constructor/submit cluster, recorded the corrected constructor endpoint, padding, direct/vtable routes, `m_recipientName[128]` field inference, opcode `0x19` packet details, sibling no-route helper exclusion, and first-draft source-facing C++.
- 2026-06-21 B013 `0000C6-SayToUserMessageInputPane-class-source-quality.md` sync: refined the constructor draft to use `LineInputPane(0)`, a 128-wide-character prompt buffer, and `swprintf_s`; expanded the submit draft to inline the observed `WideCharToMultiByte`, one-byte length, payload-copy, `< 0x12c` bound, and `g_packetSender->QueueAndSendPacket` behavior instead of routing through an unproven `SendTargetMessagePacket` helper.
