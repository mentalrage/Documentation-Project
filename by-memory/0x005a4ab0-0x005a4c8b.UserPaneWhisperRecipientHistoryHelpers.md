*** UID:00057A | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserPane::GetPreviousRecentRecipientHistory(
    short *historyIndex, wchar_t *recipient, size_t recipientCount)
{
    const int count = m_outgoingWhisperRecipientHistory->GetCount();
    if (count <= 0)
    {
        recipient[0] = L'\0';
        return;
    }

    const int index = *historyIndex == -1
        ? count - 1
        : (count + *historyIndex - 1) % count;
    *historyIndex = static_cast<short>(index);
    wcscpy_s(recipient, recipientCount,
        static_cast<const wchar_t *>(
            m_outgoingWhisperRecipientHistory->GetElementAt(index)));
}

void UserPane::UpdateRecentRecipientList(const wchar_t *recipient)
{
    int foundIndex = -1;
    const int count = m_outgoingWhisperRecipientHistory->GetCount();
    for (int i = 0; i < count; ++i)
    {
        if (wcscmp(static_cast<const wchar_t *>(
                m_outgoingWhisperRecipientHistory->GetElementAt(i)), recipient) == 0)
        {
            foundIndex = i;
            break;
        }
    }

    if (foundIndex >= 0)
        m_outgoingWhisperRecipientHistory->RemoveAt(foundIndex, 1);
    else if (count >= 10)
        m_outgoingWhisperRecipientHistory->RemoveAt(0, 1);

    m_outgoingWhisperRecipientHistory->Append(
        1, const_cast<wchar_t *>(recipient));
    m_whisperHistorySource = 1;
}

void UserPane::GetPreviousFallbackRecipientHistory(
    short *historyIndex, wchar_t *recipient, size_t recipientCount)
{
    const int count = m_incomingWhisperSenderHistory->GetCount();
    if (count <= 0)
    {
        recipient[0] = L'\0';
        return;
    }

    const int index = *historyIndex == -1
        ? count - 1
        : (count + *historyIndex - 1) % count;
    *historyIndex = static_cast<short>(index);
    wcscpy_s(recipient, recipientCount,
        static_cast<const wchar_t *>(
            m_incomingWhisperSenderHistory->GetElementAt(index)));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Exact three-member UserPane whisper-recipient history family at 0x005a4ab0-0x005a4c8b; emits recent-history navigation, deduplicate/cap/append update, and fallback-history navigation through UserPane.cpp with exact list/selector fields, void signatures, callers, and internal alignment. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005a4ab0-0x005a4c8b UserPane Whisper-Recipient History Helpers

## Status

- Disposition: exact source-bearing UserPane helper family.
- Entity kind: three `UserPane` member definitions with one internal gap between modeled bodies.
- Canonical source owner and emitter: [UID:0000FQ][UserPane](by-class/UserPane.md).
- Source placement: `NexusTK/ui/panels/UserPane.cpp`; declarations belong in `UserPane.h`.
- Confidence: very strong for ownership, two history-list roles, selector behavior, parameters, and source placement; lexical private field spellings remain source-facing reconstructions.

## Exact Function Inventory

| Range | Source-facing member | Evidence and behavior |
| --- | --- | --- |
| `0x005a4ab0-0x005a4b0b` | `UserPane::GetPreviousRecentRecipientHistory(short *, wchar_t *, size_t)` | Reads the outgoing/recent fixed-width history list, wraps a signed history index, and copies the selected recipient. Called from the Name pane constructor and history navigation. |
| `0x005a4b60-0x005a4c23` | `UserPane::UpdateRecentRecipientList(const wchar_t *)` | Removes a duplicate, caps the list at ten, appends the recipient, and selects the outgoing list. Called by the current-mode dispatcher, last-recipient opener, and recipient confirmation. |
| `0x005a4c30-0x005a4c8b` | `UserPane::GetPreviousFallbackRecipientHistory(short *, wchar_t *, size_t)` | Reads the incoming/fallback sender history with the same signed-index wrap and bounded wide copy. Called from the Name pane constructor and history navigation. |

## Field And Type Resolution

- The adjusted UserPane layout has the chat-history pointer at complete-object `+0x13eb04`, outgoing/recent `List *m_outgoingWhisperRecipientHistory` at `+0x13eb08`, incoming/fallback `List *m_incomingWhisperSenderHistory` at `+0x13eb0c`, and selector byte `m_whisperHistorySource` at `+0x13eb10`.
- Each list stores up to ten fixed-width `0x40`-byte recipient/sender records. The update helper sets the selector to `1`; incoming packet handling clears it when the fallback list is refreshed.
- The compiler's incidental register result is ignored by all callers. Source-facing `void` is therefore the bounded interface for all three members; the index pointer is signed `short *`, destination is `wchar_t *`, and copy capacity is `size_t`.

## Ownership And Callers

- UserPane ownership is proved by the canonical receiver at every caller, direct access to UserPane's two list pointers and selector, and the complementary incoming-chat packet handler.
- [UID:0000C7][SayToUserNameInputPane](by-class/SayToUserNameInputPane.md) consumes the history API but does not own it. [UID:0000I5][Chatting](by-file/Chatting.md) supplies related incoming-message behavior, not this source placement.
- [UID:0001KO][0x005a4b60-0x005a5791.SayModeHelpers](by-memory/0x005a4b60-0x005a5791.SayModeHelpers.md) remains a mixed, ownerless index. This exact page replaces historical `SayToPlanMessageInputPane` ownership and avoids synthetic aggregate emission.

## Range And Boundary

- The first modeled body is `[0x005a4ab0,0x005a4b0b)`. The bytes through the next modeled start at `0x005a4b60` are excluded alignment/adjacent space, not an invented fourth helper.
- The second body ends at `0x005a4c23`; `0x005a4c23-0x005a4c30` is alignment. The third body ends at the page's exclusive boundary `0x005a4c8b`.
- Exact internal gaps are preserved as compiler/layout evidence and do not change the three-definition source inventory.

## Reconstruction Notes

Emit the three definitions through UserPane CPP and their declarations through UserPane H. Keep the consuming SayToUserNameInputPane methods in SayInputPanes. Do not expose raw adjusted offsets in final C++, create an aggregate class for this range, or emit the mixed UID0001KO page.

## Changes

- 2026-08-25 B002 UID0000N9 accepted callback: created the exact three-helper split, resolved UserPane ownership, field/list/selector semantics, source-facing `void` signatures, caller relationships, and internal alignment boundaries.
