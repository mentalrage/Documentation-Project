*** UID:0003UR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool UserPane::HandleChatMessagePacket(const unsigned char *packet)
{
    if (packet[1] != 0)
        return false;

    const unsigned int textByteCount = PacketBufferReadUInt16BE(packet + 2);
    if (textByteCount >= 0x8000)
        __report_rangecheckfailure();

    char ansiText[0x8000];
    wchar_t wideText[0x8000];
    wchar_t senderScratch[128];
    GetMemoryMan();
    MemmoveWrapper(ansiText, packet + 4, textByteCount);
    ansiText[textByteCount] = 0;

    const int wideCount = MultiByteToWideChar(
        CP_ACP, 0, ansiText, static_cast<int>(textByteCount),
        wideText, 0x8000);
    if (static_cast<unsigned int>(static_cast<unsigned short>(wideCount)) >=
        0x8000)
        __report_rangecheckfailure();
    wideText[static_cast<unsigned short>(wideCount)] = 0;

    if ((wideText[0] == L'-' && wideText[1] == L'>' && wideText[2] == L' ') ||
        wcsstr(wideText, L"> ") != 0)
        return false;

    wchar_t *quoteSpace = wcsstr(wideText, L"\" ");
    if (quoteSpace == 0)
        return false;
    *quoteSpace = 0;
    wcsncpy_s(senderScratch, 128, wideText, 128);
    senderScratch[35] = 0;
    if (wcschr(senderScratch, L' ') != 0)
        return false;

    List *senders = m_incomingWhisperSenderHistory;
    int foundIndex = -1;
    const int count = senders->GetCount();
    for (int i = 0; i < count; ++i)
    {
        const wchar_t *stored =
            static_cast<const wchar_t *>(senders->GetElementAt(i));
        if (wcscmp(stored, senderScratch) == 0)
        {
            foundIndex = i;
            break;
        }
    }

    if (foundIndex != -1)
        senders->RemoveAt(foundIndex, 1);
    else if (count >= 10)
        senders->RemoveAt(0, 1);
    senders->Append(1, senderScratch);
    m_whisperHistorySource = WhisperHistorySource_IncomingSender;
    return false;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready User Pane Handle Chat Message Packet; accepted UserPane source placement, exact binary-equivalent behavior, callers, fields, ABI, packet/state semantics, negative evidence, and historical corrections are documented below. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005ab860-0x005aba7b User Pane Handle Chat Message Packet

## Current Source Disposition - 2026-07-14

- Current identity: [UID:0003UR] `UserPane::HandleChatMessagePacket` at `0x005ab860-0x005aba7b`; the nonblank managed C++ above is the authoritative source body.
- Current ownership and route: canonical owner/emitter [UID:0000FQ][UserPane](by-class/UserPane.md), emitted through [UID:0000P1][UserPane](by-file/UserPane.md) as `NexusTK/ui/panels/UserPane.cpp`.
- Current behavior and evidence: Source-ready User Pane Handle Chat Message Packet; accepted UserPane source placement, exact binary-equivalent behavior, callers, fields, ABI, packet/state semantics, negative evidence, and historical corrections are documented below.
- Current confidence treatment: route, caller, offset, and inline-equivalence evidence below remains valid. Route-negative evidence may cap confidence, but it does not justify LivingObjectPane ownership, blank formal C++, or a no-code disposition.
- Current recommendation: retain the current UserPane member name, owner/emitter metadata, nonblank formal body, score, ABI, range, and source placement.

## Historical / Superseded Assumptions - Pre-2026-07-14

- Date/source: documentation conclusions incorporated before the 2026-07-14 UID0002QY ownership and source-integration repair.
- Rejected claim: this local-player body belonged to `LivingObjectPane`/`LivingObjectPane.cpp`, used a reverse-engineering placeholder qualification, or should remain blank/no-code until a separate route or declaration was found.
- Disproof evidence: the standalone `LivingObjectPane` object ends at `+0x20c`, while the moved receiver-bearing bodies use UserPane-only state in the `+0x134000..+0x13eb84` extension; no-receiver helpers cluster with and are consumed by the same UserPane source family. The complete UID0000FQ declaration and UID0000P1 route make the accepted definitions source-legal.
- Accepted replacement: source-authored `UserPane` member with the exact current managed definition above. Existing no-route and inline-equivalent observations remain negative/liveness evidence only.
- Evidence preservation: exact bytes, ranges, CFG/callers, field accesses, packet/state behavior, and historical provenance in the nested material below remain retained evidence; only the rejected ownership, qualification, placeholder-name, and blank/no-code conclusions are superseded.

### Status

- Address range: `0x005ab860-0x005aba7b`.
- Entity kind: exact `UserPane` member/helper
- Current IDA name: `sub_5AB860`.
- Source-facing name: `LivingObjectPane::HandleChatMessagePacket`; strongest behavioral alias is `HandleIncomingWhisperSenderHistoryPacket`.
- Disposition: reconstructable source-authored code with first-draft C++ emitted using role names.
- Parent class: [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md).
- Source file: [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md).

### Summary

This packet handler is the live `UserPane::HandleServerMessage` opcode `0x0a` route into the adjusted `LivingObjectPane` subobject. The accepted subcase is `packet[1] == 0`. It reads a big-endian text byte count at `packet+2`, copies `packet+4` text bytes into a stack ANSI buffer, converts the payload through `MultiByteToWideChar(CP_ACP)`, rejects outgoing/display forms, extracts the sender before the Chatting quote-space delimiter `L"\" "`, rejects sender prefixes containing spaces, and updates the incoming/fallback whisper sender history list at `this+0x13eb0c`.

The side effect is reply/name-entry history, not direct chat display insertion. The method deduplicates the sender list, caps it to ten fixed-width `0x40` byte entries, inserts the newest sender at index `1`, clears the history-source selector byte at `this+0x13eb10`, and returns `false` on all normal paths. `Chatting` owns the display-format strings and `SayInputPanes` consumes the resulting history state, but the receiver, caller route, and mutated fields keep ownership under `LivingObjectPane`.

### Caller And Opcode Route

- Top-level opcode: `0x0a`, selected from payload byte `packet[0]` by [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md).
- Dispatcher route: the `UserPane::HandleServerMessage` switch subtracts `4` from the first payload byte, maps opcode `0x0a` through selector byte table `0x005a8324` to index `3`, then jumps through table `0x005a82c0` to dispatcher case `0x005a7b33`.
- Direct call: `0x005a7b3a -> 0x005ab860`. A direct rel32 scan found this as the only direct call to the target.
- Receiver adjustment: dispatcher case `0x005a7b33` does `push edi`, `lea ecx, [ebx-0xa0]`, and calls this target. That adjusted receiver matches the local-player `LivingObjectPane` subobject inside the larger `UserPane` aggregate.
- Return handling: the callee sets `al = 0` before its normal epilogue, and the dispatcher preserves that return through the common case epilogue. Treat the return as `false`, not as an append-success flag.

### Packet Layout And Text Processing

| Offset / helper | Role |
| --- | --- |
| `packet[0]` | top-level server-message opcode `0x0a`, decoded by the UserPane dispatcher before the call. |
| `packet[1]` | accepted subtype byte; this method only accepts `0`. The binary uses `0x00575470`, a tiny byte-load/runtime helper, but source should read `packet[1]` directly. |
| `packet+2` | `uint16_t` big-endian text byte count, read by `0x00575480` / `PacketBufferReadUInt16BE`. |
| `packet+4` | ANSI text payload copied into a `0x8000` byte stack buffer before conversion. |
| `MultiByteToWideChar(CP_ACP, 0, ...)` | converts exactly `textByteCount` bytes to a `0x8000` UTF-16 stack buffer and then the function explicitly appends a wide NUL. |

Range checks route to `__report_rangecheckfailure` when the byte count or converted wide count would reach `0x8000`. The code calls `GetMemoryMan` / `0x00516030` and `MemmoveWrapper` / `0x00516220` before conversion; those are memory-manager/copy dependencies, not packet ownership evidence.

### String Filters And Sender Extraction

- `0x00614c54` decodes as UTF-16 `L"> "`. The method rejects any converted text containing this marker.
- The first explicit prefix check rejects outgoing direct-message text that begins `L"-> "`.
- `0x00615284` decodes as UTF-16 `L"\" "`. The method requires this quote-space delimiter, overwrites the delimiter with NUL, and treats the preceding wide text as the sender prefix.
- The prefix is copied through `wcsncpy_s` into local sender scratch, then slot `35` in that scratch is explicitly cleared. This is a defensive sender-name truncation/write barrier, not proof that the list element is a 128-wide-character string.
- `wcschr(sender, L' ')` rejects sender names containing spaces, strongly supporting a player-name/sender-name role rather than arbitrary chat text.
- The neighboring Chatting format strings at `0x00615298`, `0x006152b0`, and `0x006152c0` (`%s%s"%s`, `%s%s]%s`, `%s%s>%s`) stay Chatting-owned display resources. This method only borrows the quote-space delimiter to split incoming subtype-zero display text shaped like `sender" message`.

### History Fields And List Behavior

| Adjusted local-player field | Best current source-facing role | Evidence |
| --- | --- | --- |
| `this+0x13eb08` | `m_recentWhisperRecipients` / outgoing recent direct-message recipient list | `sub_5A4B60` deduplicates/caps/inserts here and sets `this+0x13eb10 = 1`; confirmation paths in `SayToUserNameInputPane` call that helper after the user enters a recipient. |
| `this+0x13eb0c` | `m_incomingWhisperSenderHistory` / incoming fallback whisper sender list | This method deduplicates/caps/inserts here and clears `this+0x13eb10`; fallback SayToUserNameInputPane paths read this list. |
| `this+0x13eb10` | `m_whisperHistorySource` / selector between outgoing and incoming history | The outgoing update helper sets it to `1`; this incoming packet handler clears it to `0`. `SayToUserNameInputPane` constructor uses the byte to decide whether `sub_5A4AB0` or `sub_5A4C30` preloads the name. |

The constructor-created history lists are project `List` containers with `List(0x40, 0x0a)`, so the entry size is `0x40` bytes and the history capacity/page size is ten. This target reads count at list offset `+0x0c`, uses vslot `+0x10` to fetch existing entries, vslot `+0x1c` to remove one entry, and vslot `+0x18` to insert one entry. The algorithm scans for a duplicate sender, removes the duplicate if found, otherwise removes index `0` when the count is already ten, then inserts the sender at index `1`.

### Helper / Runtime Name Resolution

| Address / generated label | Best-supported source role | Notes |
| --- | --- | --- |
| `0x00575470` | runtime byte load / `std::_Narrow_char_traits<char,int>::to_char_type` style helper | Source should use `packet[1]`, not introduce a new packet-buffer byte helper. |
| `0x00575480` | `PacketBufferReadUInt16BE` | Reads `(p[0] * 0x100) + p[1]` for the payload text byte count. |
| `0x00516030` | `GetMemoryMan` | Memory manager touch before stack-buffer copy. |
| `0x00516220` | `MemmoveWrapper` / `MemoryMan::CopyBytes` | Copies `textByteCount` bytes from `packet+4` to the ANSI stack buffer. |
| `0x0060d120` | `MultiByteToWideChar` import | Called with code page and flags both zero (`CP_ACP`). |
| `0x005ca676` | `wcsstr` | Used for both `L"> "` and `L"\" "` searches. |
| `0x005ddbec` | `wcsncpy_s` | Secure wide copy into sender scratch. |
| `0x005ca50d` | `wcschr` | Searches sender scratch for a space. |
| `0x005c7655` | `__report_rangecheckfailure` | Range-check failure tail for stack buffer bounds. |
| `0x005c7740` / `0x005c772f` | `__alloca_probe` / stack-cookie support | Compiler/runtime support for the large stack frame. |

### Evidence

- B001 records live IDA `lookup_funcs`: start `0x005ab860`, size `0x21b`, end `0x005aba7b`.
- The local PE/capstone reanalysis decodes the dispatcher route from [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md): opcode `0x0a` maps through selector table index `3` to `0x005a7b33`, which calls this target at `0x005a7b3a` with `ecx = ebx - 0xa0`.
- Direct rel32 scanning found exactly one direct caller to `0x005ab860`, the dispatcher call at `0x005a7b3a`.
- Local string decoding identifies `0x00614c54` as `L"> "` and `0x00615284` as `L"\" "`. The latter is also documented in [UID:0003AU][0x00615284-0x00615460.ChattingStringResourceData](by-memory/0x00615284-0x00615460.ChattingStringResourceData.md) and aggregate [UID:00024X][0x00614cd0-0x00615560.ChattingReadOnlyData](by-memory/0x00614cd0-0x00615560.ChattingReadOnlyData.md) as the one Chatting format fragment with a `sub_5AB860` reference.
- `UserPane` constructor/destructor evidence shows three history-list child pointers near `+0x13eb04/+0x13eb08/+0x13eb0c`; this target and the say-input helpers refine `+0x13eb08/+0x13eb0c/+0x13eb10` into outgoing-recipient, incoming-sender, and selector roles.

### Ownership And Rejected Alternatives

- Accepted owner: [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md), emitted through [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md).
- `Chatting` rejected as owner: this method has no `ChattingPane` receiver, no `g_pChattingDisplayPane` access, no `AddChattingMessage` or `AddIncomingMessage` call, and no route from the Chatting packet router. It only uses Chatting-owned read-only delimiter/format literals.
- `SayInputPanes` rejected as owner: Say input panes consume the resulting history state, but they do not call this handler. The live caller is `UserPane::HandleServerMessage`.
- `UserPane` rejected as direct owner for the callee: UserPane owns the dispatcher, but the case adjusts the receiver to the `LivingObjectPane` subobject before the call, matching other local-player `LivingObjectPane` packet handlers.
- No split or merge is recommended. The function is cohesive from prologue through `ret 4`; the pre-target `0x005ab75d-0x005ab860` raw/prologue-shaped body and post-target body after five `0xcc` bytes at `0x005aba7b-0x005aba80` are separate neighboring issues.

### Remaining Source-Quality Caveats

- Exact original enum and member spellings remain inferred. Current role names `ServerOpcode_ChatMessage`, `m_recentWhisperRecipients`, `m_incomingWhisperSenderHistory`, and `m_whisperHistorySource` are suitable for draft C++ and documentation but should be revisited during final declaration work.
- The list entry typedef is not symbol-proven. The executable evidence supports a project `List` of fixed `0x40` byte entries and ten slots, not `std::wstring`.
- The method return remains modeled as `false` even after successful insertion because the observed binary writes `al = 0` on all normal paths.

### Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact range, live caller/opcode route, subtype, packet layout, text conversion, literal meanings, list fields, duplicate/cap/insert algorithm, owner/source placement, rejected alternatives, split decision, helper roles, open-question closure, and first-draft C++ are now documented. Completion remains below final because original enum/member spellings and the final fixed-name-slot typedef are not symbol-proven. |
| Confidence | 91 | Direct byte/disassembly, decoded switch route, direct-call scan, string decoding, support-doc agreement, and related say-input history consumers strongly validate the behavior. Confidence remains below final because the latest pass was local PE/read-only evidence and exact source spellings remain inferred. |

### Cross-References

- [UID:0001KM][0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions](by-memory/0x005a2de0-0x005ad441.UserPaneLocalPlayerExtensions.md)
- [UID:0003VB][0x005a76c0-0x005a82bd.UserPaneHandleServerMessage](by-memory/0x005a76c0-0x005a82bd.UserPaneHandleServerMessage.md)
- [UID:0003VC][0x005a82bd-0x005a83b0.UserPaneHandleServerMessageSwitchTables](by-memory/0x005a82bd-0x005a83b0.UserPaneHandleServerMessageSwitchTables.md)
- [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md)
- [UID:0000KU][LivingObjectPane](by-file/LivingObjectPane.md)
- [UID:0001KO][0x005a4b60-0x005a5791.SayModeHelpers](by-memory/0x005a4b60-0x005a5791.SayModeHelpers.md)
- [UID:0001MD][0x005b3cb0-0x005b4219.SayToUserNameInputPane](by-memory/0x005b3cb0-0x005b4219.SayToUserNameInputPane.md)
- [UID:0001ME][0x005b3e80-0x005b4072.SayToUserNameInputPaneHistoryNavigation](by-memory/0x005b3e80-0x005b4072.SayToUserNameInputPaneHistoryNavigation.md)
- [UID:0000I5][Chatting](by-file/Chatting.md)
- [UID:0003AU][0x00615284-0x00615460.ChattingStringResourceData](by-memory/0x00615284-0x00615460.ChattingStringResourceData.md)

### Changes

- 2026-06-20 supervisor Rule 26 incorporation of B001 `0003UR-LivingObjectPaneHandleChatMessagePacket-source-quality.md`:
  - Raised `COMPLETION:85` / `CONFIDENCE:87` to `88/91`.
  - Kept owner/emitter [UID:00007B][LivingObjectPane](by-class/LivingObjectPane.md) `LivingObjectPane` and emitted first-draft `LivingObjectPane::HandleChatMessagePacket(const unsigned char *packet)` C++.
  - Incorporated the resolved opcode `0x0a` route, subtype `packet[1] == 0`, `PacketBufferReadUInt16BE(packet+2)`, ANSI payload at `packet+4`, `MultiByteToWideChar(CP_ACP)` conversion, outgoing/display-form filters, quote-space delimiter split, sender truncation and space rejection, incoming/fallback sender-history list update at `this+0x13eb0c`, selector byte clear at `this+0x13eb10`, helper-name directions, owner rejections, exact split/range decision, and remaining source-spelling caveats.

## B001 UID0002QY Accepted Callback Synchronization - 2026-07-14

- Destination 26 is applied without changing the accepted score, address range, ABI, callers, packet/state behavior, negative evidence, or retained historical research.
- Receiver fields beyond the LivingObjectPane base establish [UID:0000FQ][UserPane](by-class/UserPane.md) UserPane as canonical owner/emitter and [UID:0000P1][UserPane](by-file/UserPane.md) as source module.
- The formal managed block is authoritative. Earlier LivingObjectPane ownership/qualifiers, Raw or packet-letter placeholder names, and blank-body conclusions are retained only as dated history and are superseded by the accepted semantic source shape.
