*** UID:0000YX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000HG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static bool LoadIndexedDATSeries(const wchar_t* basePath)
{
    wchar_t fileName[64];

    for (int index = 0; index < 99; ++index) {
        swprintf_s(fileName, L"%s%d.DAT", basePath, index);

        FILE* stream = NULL;
        _wfopen_s(&stream, fileName, L"rb");
        if (stream == NULL) {
            break;
        }

        fclose(stream);

        if (!g_pDATFileMgr->LoadDATFile(fileName)) {
            wchar_t message[128];
            swprintf_s(message, L"File not found : %s", fileName);
            FatalError(message);
        }
    }

    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x00467410-0x004674ed LoadIndexedDATSeries

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: file-local/global startup helper function
- Canonical symbol: [UID:0000T5][LoadIndexedDATSeries](by-global/LoadIndexedDATSeries.md)
- Likely source module: [UID:0000HG][Application](by-file/Application.md)
- Confidence: strong for behavior, caller ownership, source placement, and helper name; medium-high for exact original return type.

## Covered Range

| Range | Current name | Notes |
| --- | --- | --- |
| `0x00467410-0x004674ed` | `LoadIndexedDATSeries` / IDA `sub_467410` | Loads a numbered DAT archive family for one startup resource prefix. |

IDA reports size `0xdd`, so this page uses the half-open end `0x004674ed`. The next IDA function starts at `0x004674f0`.

## Behavior

`LoadIndexedDATSeries` is the repeated resource-startup helper used by `Application::Initialize` for numbered archive families:

1. Iterates indexes `0..98`.
2. Formats `L"%s%d.DAT"` into a local wide filename buffer.
3. Probes the loose archive file through `_wfopen_s(..., L"rb")`.
4. Stops the series when the next numbered file is absent.
5. Closes each successfully opened probe stream immediately.
6. Calls [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md) on [UID:0000QQ][g_pDATFileMgr](by-global/g_pDATFileMgr.md) to load/index the same archive.
7. Formats `L"File not found : %s"` and calls the fatal error path at `0x00465cb0` if the DAT manager rejects a file that exists on disk.
8. Returns `1`.

The helper treats a missing next-numbered file as normal end-of-series, but treats an existing file that cannot be indexed as fatal startup failure.

## Startup Prefixes

Generated `simroot_v2` evidence and IDA caller locations show `Application::Initialize` using this helper for clear resource prefixes including:

- `DATA/FACEDEC`
- `DATA/EMOTION`
- `DATA/HAIRDEC`
- `DATA/NECK`
- `DATA/BODY`
- `DATA/SWORD`
- `DATA/SPEAR`
- `DATA/FAN`
- `DATA/SHIELD`
- `DATA/TILE`
- `DATA/TILEC`
- `DATA/EFX`
- `DATA/BOW`
- `DATA/HAIR`
- `DATA/FACE`
- `DATA/HELMET`
- `DATA/MANTLE`
- `DATA/SHOES`
- `DATA/COAT`

See [UID:0001R6][application-startup-dat-archives](by-resource/application-startup-dat-archives.md) for the full startup archive inventory.

## IDA MCP Evidence

Checked on 2026-05-25:

- `lookup_funcs 0x00467410` reports IDA `sub_467410`, size `0xdd`.
- `lookup_funcs 0x004674ed` reports `Not a function`; `lookup_funcs 0x004674f0` reports the next function `sub_4674F0`, size `0x11`.
- `callers 0x00467410` reports 19 direct call sites, all inside `Application::Initialize` / IDA `sub_4639D0`.
- `callees 0x00467410` reports [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md), wide formatting/string helpers, `_wfopen_s`, `_fclose`, and the fatal error path at `0x00465cb0`.
- `decompile 0x00467410` shows the `i < 99` numbered loop, `L"%s%d.DAT"` formatting, `_wfopen_s` probe, and manager-load failure path.
- 2026-05-30 IDA MCP `lookup_funcs` recheck reports `sub_467410` size `0xdd`; `0x004674ec` is inside the function, `0x004674ed` is not a function, and the next modeled function starts at `0x004674f0`.
- 2026-05-30 IDA MCP `callers`/`xrefs_to` recheck still reports exactly 19 code call sites, all inside `Application::Initialize` / IDA `sub_4639D0`, at `0x00463edb` through `0x00463f92`.
- 2026-05-30 IDA MCP `callees` recheck reports the DAT manager wrapper `0x0049be70`, wide string/format helpers `0x00583280` and `0x00584540`, fatal error helper `0x00465cb0`, another local helper `0x0041b9b0`, `_fclose`, `_wfopen_s`, and the security-cookie check.
- `simroot_v2/recovered/LoadIndexedDATSeries_00467410.cpp` matches the verified source shape: `wchar_t fileName[64]`, loop `index < 99`, `_wfopen_s` probe, immediate `fclose`, `g_pDATFileMgr->LoadDATFile(fileName)`, fatal `L"File not found : %s"` message, and return `1`.

## Reconstruction Notes

Source reconstruction should keep this as a private startup helper near `Application::Initialize`:

```cpp
static bool LoadIndexedDATSeries(const wchar_t* basePath)
{
    wchar_t fileName[64];

    for (int index = 0; index < 99; ++index) {
        swprintf_s(fileName, L"%s%d.DAT", basePath, index);

        FILE* stream = NULL;
        _wfopen_s(&stream, fileName, L"rb");
        if (stream == NULL) {
            break;
        }

        fclose(stream);

        if (!g_pDATFileMgr->LoadDATFile(fileName)) {
            wchar_t message[128];
            swprintf_s(message, L"File not found : %s", fileName);
            FatalError(message);
        }
    }

    return true;
}
```

The binary returns byte value `1`; a C++ `bool` return is the most likely source form, but this remains a return-type hypothesis rather than a proven original signature.

## Ownership Decision

Keep this helper with `app/Application.cpp`, probably as a private/static startup helper near `Application::Initialize`. It is not a `DATFileMgr` method: its policy is startup-specific, it decides where a numbered series stops, and all observed callers are inside `Application::Initialize`.

The helper depends on the archive manager API, so [UID:0000IO][DATFileMgr](by-file/DATFileMgr.md) should document the called wrapper, but the original source placement should stay with application startup.

## Cross-References

- [UID:0000T5][LoadIndexedDATSeries](by-global/LoadIndexedDATSeries.md)
- [UID:0000HG][Application](by-file/Application.md)
- [UID:00000D][Application](by-class/Application.md)
- [UID:0000YR][0x00463310-0x004679be.ApplicationLifecycle](by-memory/0x00463310-0x004679be.ApplicationLifecycle.md)
- [UID:0001R6][application-startup-dat-archives](by-resource/application-startup-dat-archives.md)
- [UID:00012C][0x0049be70-0x0049be7c.ForwardLoadDATFileIndex](by-memory/0x0049be70-0x0049be7c.ForwardLoadDATFileIndex.md)
- [UID:0000IO][DATFileMgr](by-file/DATFileMgr.md)
- [UID:0000QQ][g_pDATFileMgr](by-global/g_pDATFileMgr.md)
- [UID:0000IM][DATArchive](by-file/DATArchive.md)
- [UID:0001QC][client_dat_specifications](by-meta/client_dat_specifications.md)

## Changes

- 2026-05-30: Raised completion/confidence from `50/65` to `90/94`. Previously this page had good 2026-05-25 evidence but stale score metadata; it now records the current IDA MCP boundary/caller/callee recheck, includes the verified source-level C++ shape, and keeps only the return-type/original-signature caveat open.
- 2026-05-31 autogen attachment:
  - What existed before: the verified source-level C++ shape lived only in the narrative reconstruction notes.
  - What it was changed to: the same helper was added to the reconstruction code block and attached under [UID:0000HG][Application](by-file/Application.md).
  - Summary/evidence: live IDA MCP rechecked `0x00467410` as size `0xdd`; all 19 callers remain inside `Application::Initialize`, supporting `Application.cpp` ownership.
