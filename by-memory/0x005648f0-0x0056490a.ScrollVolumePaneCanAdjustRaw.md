*** UID:00031O | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:80 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool ScrollVolumePane::CanAdjust() const
{
    return m_enabled && m_range > 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready raw ScrollVolumePane predicate returns inferred m_enabled && m_range > 0 from +0x102/+0x100; no direct static route remains a liveness/confidence cap, not an open source-name blocker. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

## 2026-07-26 Accepted Aggregate-Split Reconciliation

- Direct semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), position 80. UID0001H2 no longer participates in output routing.
- Exact retained body remains `[0x005648f0,0x0056490a)`, size `0x1a`, bounded by six-byte all-`0xcc` gaps.
- Exact bytes test `m_enabled` at `+0x102` and signed `m_range` at `+0x100`, returning true only when enabled and range is positive. The accepted source signature is `bool CanAdjust() const`.
- Zero raw-start xrefs, pointer encodings, and direct branch routes remain documented. The positive-control successor virtual route at `0x00624104` confirms the route scan worked; absence here is only a liveness cap.
- Rejected alternatives include visibility/focus/drag predicates, unsigned range, aggregate ownership, padding, and a forced raw/decompiler name. The role and source-facing name are resolved at high probability.

# 0x005648f0-0x0056490a - ScrollVolumePane Can Adjust Raw

## Status

- Address range: `0x005648f0-0x0056490a`, exclusive end.
- Verified size: `0x1a` / 26 bytes (Verified with tools/int_convert.py).
- Disposition: reconstructable ScrollVolumePane raw helper with accepted inferred first-draft predicate C++.
- Owner evidence: function-shaped code between `Disable` and current `HandlePointerOrMouseEvent` (historically labeled `OnMouseEvent`) inside [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md).
- Current class/source: [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md) / [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md).
- Parent assignment: attached to [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) at position `50`; both child and direct parent clear the strict `85/85` gate.
- B007 source-quality result: current MCP session `80de0a67` reconfirms the exact bytes, boundary padding, no-function status, no raw-start/end xrefs, no pointer/direct-branch route, accepted field roles, inferred `CanAdjust() const` source name, and unchanged formal C++ body. The no-route finding is retained as a liveness/confidence cap only.

## Behavior

This raw helper returns true only when the control is enabled and its maximum/range word is positive:

- returns `0` if byte `this + 0x102` (`+258`, Verified with tools/int_convert.py) is clear;
- returns `1` if word `this + 0x100` (`+256`, Verified with tools/int_convert.py) is greater than zero;
- otherwise returns `0`.

The behavior matches a small private predicate for whether the slider can be adjusted. B014 accepted `CanAdjust() const` as the best source-facing descriptive name; B007 rechecked that decision with current MCP and keeps it as accepted inferred source-facing naming. Exact original symbol spelling is still not proven, but the final-name-open wording is stale because no stronger competing name or owner survived the current evidence pass.

For source C++, `m_enabled && m_range > 0` preserves the observed code. The machine body uses `xor eax, eax; cmp ax, [ecx+100h]; jge false`, so it returns true exactly when zero is less than the range word after the enabled check. Current class writers clamp/set the documented `m_range` as a non-negative short, so the source expression is behavior-preserving for this control.

## Raw Code Evidence

B007 MCP session `80de0a67` was health-ready against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, with input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and strings cache size `2067`.

Current `lookup_funcs` results:

| Query | Result |
| --- | --- |
| `0x005648d0` | function `sub_5648D0`, size `0x1a` / 26 bytes (Verified with tools/int_convert.py) |
| `0x005648ea` | not a function |
| `0x005648f0` | not a function |
| `0x00564909` | not a function |
| `0x0056490a` | not a function |
| `0x00564910` | function `sub_564910`, size `0x1e1` / 481 bytes (Verified with tools/int_convert.py) |

B007 `get_bytes` evidence:

| Range | Bytes | Meaning |
| --- | --- | --- |
| `0x005648ea-0x005648f0` | `cc cc cc cc cc cc` | Six-byte `Disable` to raw predicate alignment gap; `0x6` = 6 bytes (Verified with tools/int_convert.py). |
| `0x005648f0-0x0056490a` | `80 b9 02 01 00 00 00 74 0e 33 c0 66 3b 81 00 01 00 00 7d 03 b0 01 c3 32 c0 c3` | Complete raw predicate body. |
| `0x0056490a-0x00564910` | `cc cc cc cc cc cc` | Six-byte raw predicate to current `HandlePointerOrMouseEvent` alignment gap; historical `OnMouseEvent` wording is superseded. `0x6` = 6 bytes (Verified with tools/int_convert.py). |

B007 `insn_query` disassembly over the predecessor, target, and successor boundary:

```asm
0x005648d0  cmp byte ptr [ecx+102h], 1
0x005648d7  jnz short locret_5648E9
0x005648d9  mov edx, [ecx]
0x005648db  lea eax, [ecx+44h]
0x005648de  push eax
0x005648df  mov byte ptr [ecx+102h], 0
0x005648e6  call dword ptr [edx+20h]
0x005648e9  retn
0x005648ea  align 10h
0x005648f0  cmp  byte ptr [ecx+102h], 0
0x005648f7  jz   0x00564907
0x005648f9  xor  eax, eax
0x005648fb  cmp  ax, [ecx+100h]
0x00564902  jge  0x00564907
0x00564904  mov  al, 1
0x00564906  retn
0x00564907  xor  al, al
0x00564909  retn
0x0056490a  align 10h
```

`decompile 0x005648f0` returned `code:null` and `error:"Decompilation failed at 0x5648f0"`, matching raw non-function status rather than a modeled method body. `analyze_function 0x005648d0` confirms the predecessor `Disable` helper is distinct, writes byte `+0x102` to zero, invalidates `this+0x44`, is directly called by `sub_541A90` at `0x00541ac4` and `0x00541b0f`, and ends before the six `0xcc` bytes at `0x005648ea-0x005648f0`.

## Callers And Boundaries

- IDA `lookup_funcs` reports `0x005648f0` as `Not a function`; this is a raw/projected helper rather than an IDA-modeled function.
- IDA `xrefs_to` reports no direct xrefs to `0x005648f0`; B007 also confirmed `xrefs_to 0x0056490a` returns zero xrefs.
- B007 positive control: `xrefs_to 0x00564910` returns one data xref at `0x00624104`, the secondary vtable mouse-handler slot, proving the xref lookup sees ordinary data routes in the same class cluster.
- 2026-06-12 A005 Batch249 live IDA MCP recheck and 2026-06-26 B007 current MCP recheck both confirm `0x005648d0` is modeled as `sub_5648D0`, size `0x1a` / 26 bytes (Verified with tools/int_convert.py), ending at `0x005648ea`; `0x005648f0` is still `Not a function`; and `0x00564910` is modeled as `sub_564910`, size `0x1e1` / 481 bytes (Verified with tools/int_convert.py).
- IDA `callers 0x005648f0` reports no callers, and `callees 0x005648f0` reports no function object for callee analysis.
- B007 `xref_query` on `0x005648f0` with direction `both` found no incoming raw-start route. The only target-range result is an internal raw code edge with `fn:null`, not an external caller or pointer route.
- `find_bytes` found no little-endian pointer-pattern hits for `f0 48 56 00`; B007 also checked `f0 48 16 00` for RVA `0x001648f0` and `0a 49 56 00` for the target end, both with zero matches. Positive control `10 49 56 00` for current `HandlePointerOrMouseEvent` (historically `OnMouseEvent`) returned `0x00624104`.
- A byte-level IDA/MCP pass confirmed the previous modeled function, `Disable`, ends at `0x005648ea`; `0x005648ea-0x005648f0` is `0x6` / 6 bytes of `0xcc` alignment (Verified with tools/int_convert.py).
- The same byte pass confirmed this helper body bytes as `80 b9 02 01 00 00 00 74 0e 33 c0 66 3b 81 00 01 00 00 7d 03 b0 01 c3 32 c0 c3`.
- Current `HandlePointerOrMouseEvent` starts at `0x00564910`; `0x0056490a-0x00564910` is `0x6` / 6 bytes of `0xcc` alignment (Verified with tools/int_convert.py). `OnMouseEvent` is the superseded historical label.

Read-only local PE route scan over `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` parsed imagebase `0x400000`, found `.text` at RVA `0x1000` / raw pointer `0x400`, and maps `0x005648f0` to file offset `0x163cf0`. The scan found zero absolute-VA dword hits, zero RVA dword hits, zero file-offset dword hits, and zero direct `E8/E9` rel32 branch/call targets to `0x005648f0`; it also found zero VA/RVA/file-offset dword hits and zero direct `E8/E9` routes to `0x0056490a`.

Positive controls in that same PE scan:

| Target | Direct route / pointer result |
| --- | --- |
| `0x00564820` `SetRange` | Direct `E8` hits at `0x005403c7` and `0x005403de`. |
| `0x00564880` `SetValue` | Direct hits at `0x00540416` and `0x00540463`. |
| `0x005648b0` `Enable` | Direct hits at `0x00541aa7` and `0x00541af3`. |
| `0x005648d0` `Disable` | Direct hits at `0x00541ac4` and `0x00541b0f`. |
| `0x00564910` `HandlePointerOrMouseEvent` (historically `OnMouseEvent`) | No direct calls, but one absolute-VA vtable pointer at file offset `0x222b04` / VA `0x00624104`. |
| `0x00565010` `GetPartRect` | 13 direct calls, including `0x00564987`, `0x00564a22`, `0x00564a3a`, `0x00564a59`, `0x00564d98`, `0x0056519c`, `0x005651b8`, `0x00565211`, `0x00565229`, `0x00565248`, `0x0056533a`, and `0x00565385`. |
| `0x005652a0` `CommitInteraction` | Direct calls at `0x00564a83`, `0x00564b28`, and `0x00565272`. |
| `0x00565360` `NotifyValueChange` | Direct calls at `0x005652fb` and `0x00565324`. |

The no-route result is therefore meaningful. It caps liveness/confidence but does not refute ownership, executable status, source C++ readiness, or the inferred `CanAdjust() const` name.

## Field Use And Source Interpretation

The accepted fields remain `m_enabled` at `+0x102` and `m_range` at `+0x100`; `0x100` = 256 and `0x102` = 258 (Verified with tools/int_convert.py).

B007 scoped `search_text "102h"` over `0x00564710-0x005654ec` found 11 hits:

- the constructor initializes `+0x102/+0x103` through `mov word ptr [esi+102h], 0FF01h`, setting `m_enabled = 1` and current `m_hoverPart = -1`; historical `m_highlightPart` is superseded;
- `Enable` compares and writes byte `+0x102` to `1`;
- `Disable` compares and writes byte `+0x102` to `0`;
- this raw predicate compares byte `+0x102` to zero; and
- `OnPaint`, `HitTestPart`, and `GetPartRect` test byte `+0x102` for enabled rendering and geometry behavior.

This supports `m_enabled`. Rejected alternatives for `+0x102` are `m_visible`, `m_hasFocus`, `m_activePart`, `m_hoverPart`, and drag-state names, because neighboring fields and uses already establish those roles elsewhere.

B007 scoped `search_text "100h"` found 11 hits:

- `SetRange` compares and writes word `+0x100`;
- this raw predicate compares word `+0x100` against zero; and
- `OnPaint`, `HitTestPart`, `GetPartRect`, and `NotifyValueChange` use word `+0x100` as the slider maximum/range for geometry and value math.

This supports `m_range`. Rejected alternatives for `+0x100` are `m_value`, which belongs to `+0xfe`; `m_enabled`, which belongs to `+0x102`; `m_scrollStyle`, which belongs to `+0xfa`; part fields, which belong to `+0x103/+0x104`; and drag offset, which belongs to `+0x108/+0x10c`.

B007 scoped `search_text "0FEh"` found 10 hits separating `+0xfe` as `m_value`: the constructor clears it, `SetRange` clamps it, `SetValue` writes it, and paint/hit-test/geometry/notify paths read it as the current slider value.

## Source-Facing Name Decision

Retain `ScrollVolumePane::CanAdjust() const` as the best current human source-facing name. The name is inferred rather than original-symbol proof, but the class/source family has accepted it at current source-quality standard.

Positive name evidence:

- the body is a side-effect-free member predicate that only reads `this + 0x102` and `this + 0x100`, returns `AL`, and has no calls or writes;
- it sits between `Disable` and current `HandlePointerOrMouseEvent` in the ScrollVolumePane implementation island;
- it combines enabled state and positive range, exactly the gate that determines whether slider adjustment is possible;
- the class declaration already uses private `bool CanAdjust() const`;
- `HitTestPart` and generated output use the same source expression `m_enabled && m_range > 0` for adjustable slider geometry; and
- neighboring raw helpers in this island use private member-style names, not file-global helper names.

Rejected source names:

- `IsEnabled() const`: too narrow because this method also requires `m_range > 0`.
- `HasRange() const`: too narrow because this method also requires `m_enabled`.
- `IsScrollable() const`: rejected superseded interpretation for `0x00564b00-0x00564b05`; the current source identity is the false-return `HandleKeyOrTextEvent(Event *)` override.
- `CanScroll() const`: weaker fit because this class is a horizontal volume slider, and the separate false virtual is current `HandleKeyOrTextEvent`, not the historical/superseded `IsScrollable` query interpretation.
- `CanDrag() const` or `CanMoveThumb() const`: too specific because the same gate applies to page regions, thumb geometry, hit testing, and paint, not only thumb drag.
- `IsActive() const`, `IsDragging() const`, and highlight/active-part names: contradicted by `+0x103`, `+0x104`, `+0xfb`, and `+0x108/+0x10c` roles.
- File-global/static helper placement: rejected because the body is thiscall-style member code using `ECX` object state and sits in the class implementation island.

## Owner, Emitter, And Split Decision

Retain `CANONICAL_OWNER:0001H2`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0001H2`, and source route through [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md), [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md), and [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md) / `NexusTK/ui/controls/ScrollVolumePane.cpp`.

Rejected owners and placements:

- Not `OptionPane` or `NewOptionPane`: those construct/configure volume sliders and receive callbacks, but this body reads only ScrollVolumePane fields and belongs in the reusable control implementation.
- Not `ScrollBar` or `ScrollableControlPane`: the field offsets, vtables, resources, and class docs identify this as the volume slider, not a generic scrollbar implementation.
- Not `TextEditPane`: stale generated signatures affected other helpers historically, but current class/file/docs reject that generated-owner pollution for this source island.
- Not a vtable/data item: the bytes are executable predicate instructions with `retn`, not table data.
- Not compiler padding: both sides are `0xcc` alignment, while the target range itself has executable comparisons and returns.
- Not a no-owner/non-emitting item: the direct parent, class, and file emitters all clear the current gate and already route child method code.

Keep the target as exact half-open range `0x005648f0-0x0056490a`.

Rejected split/merge candidates:

- Do not merge backward with `Disable`: `sub_5648D0` is a modeled 26-byte function ending at `0x005648ea`; `0x005648ea-0x005648f0` is six bytes of `0xcc`.
- Do not include `0x005648ea-0x005648f0`: it is alignment padding, not source logic.
- Do not split the predicate internally: `0x005648f0-0x00564909` is one complete branch-and-return method body with a false label at `0x00564907`; both returns belong to the same predicate.
- Do not include `0x0056490a-0x00564910`: it is six bytes of `0xcc`.
- Do not merge forward with current `HandlePointerOrMouseEvent`: `0x00564910` is modeled `sub_564910`, size `0x1e1` / 481 bytes (Verified with tools/int_convert.py), and is referenced by the secondary vtable slot at `0x00624104`. Historical `OnMouseEvent` wording is superseded.
- Do not mark the target as covered by `HandlePointerOrMouseEvent`: no direct call is found, but the raw predicate has its own distinct executable bytes and formal child C++ already routes through the ScrollVolumePane source.

## Formal C++ Disposition

The existing formal target C++ at the top of this page is correct and intentionally unchanged. It is the actual target body, not prose sample code. It reflects plausible source structure for a private, const, side-effect-free control predicate and preserves the exact runtime behavior described above.

## Parent Gate And Score Rationale

The current semantic owner/emitter is [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md). [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) is the `92/94` false/non-emitting address index and documents this exact child between `Disable` and `HandlePointerOrMouseEvent`.

This child now clears `89/91` because current B007 MCP/source-quality evidence records the byte range, exact body bytes, predicate behavior, touched fields, no-function status, decompile failure, no direct xrefs/callers, zero raw-start/end pointer or direct-branch route, positive route controls, neighboring padding spans, source-facing `CanAdjust() const` role, formal C++ correctness, owner/emitter proof, and rejected split/name/owner alternatives. Completion remains below final-audit quality only because IDA still has no function object and no live static route to the raw start. Confidence remains capped below final because exact original symbol spelling is not proven.

Positive evidence summary:

- MCP session `80de0a67` is health-ready against the NexusTK IDB.
- The raw body bytes and instructions exactly implement an enabled-and-positive-range predicate.
- The target has exact pre/post `0xcc` padding and stable modeled neighbors: `Disable` before it and current `HandlePointerOrMouseEvent` after it.
- The direct field-use census supports `m_enabled` at `+0x102` and `m_range` at `+0x100`.
- `CanAdjust() const` is already in the accepted class declaration and generated source.
- Owner/emitter/source placement through `ScrollVolumePane` is valid and gate-cleared.
- Route scans include positive controls, so the no-route finding is meaningful rather than a broken scan.

Negative evidence summary:

- No IDA function exists at `0x005648f0`, `0x00564909`, or `0x0056490a`.
- Hex-Rays decompile fails for `0x005648f0` because there is no function object.
- `xrefs_to 0x005648f0` and `xrefs_to 0x0056490a` return zero xrefs.
- MCP `find_bytes` and local PE scanning find no absolute VA, RVA, file-offset pointer, or direct `E8/E9` route to `0x005648f0`.
- Exact original symbol spelling is not proven by PDB/source metadata. The source-facing name is inferred, but strongly supported.
- Generated tracker/coverage/source comments may remain stale until validator-owned generated output refreshes from the updated source page.

## Cross-References

- [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md)
- [UID:0001W2][ScrollVolumePaneLayout](by-type/by-struct/ScrollVolumePaneLayout.md)
- [UID:0000CO][ScrollVolumePane](by-class/ScrollVolumePane.md)
- [UID:0000NK][ScrollVolumePane](by-file/ScrollVolumePane.md)

## Changes

- 2026-06-26 B007 source-quality implementation:
  - Changed completion/confidence from `87/90` to `89/91`; owner, reconstructable status, emitter, and the formal `ScrollVolumePane::CanAdjust() const` C++ block remain unchanged.
  - Replaced stale "final source name remains open" wording with accepted inferred-name/source-ready wording. The no-function/no-xref/no-route status remains documented as a liveness/confidence cap, not a source-name blocker.
  - Evidence: MCP session `80de0a67` records health-ready IDB state, exact predecessor/target/successor `lookup_funcs`, exact body bytes, six-byte pre/post padding, bounded disassembly, Hex-Rays decompile failure at the raw start, zero raw-start/end xrefs, current `HandlePointerOrMouseEvent` vtable positive control (historically labeled `OnMouseEvent`), zero VA/RVA/file-offset/direct-branch routes, field-use census for `+0x102` `m_enabled`, `+0x100` `m_range`, and `+0xfe` `m_value`, rejected names, rejected owners, rejected split/merge candidates, and retained formal C++.
- 2026-06-08 A008 Batch 137 split:
  - Historical state before the split: the parent aggregate jumped from `Disable` ending at `0x005648ea` to `0x00564910` under the superseded `OnMouseEvent` label; current source identity is `HandlePointerOrMouseEvent`, and the predicate is now documented as an exact child.
  - Changed to: exact raw helper page for `0x005648f0-0x0056490a`.
  - Evidence: live IDA MCP disassembly, `lookup_funcs`, `xrefs_to`, and byte-gap review confirmed the raw helper body, enabled/range predicate, no direct xrefs, and surrounding alignment.
- 2026-06-12 A005 Batch249:
  - Before: `78/85`, parent blank under the strict gate.
  - Changed to: `85/88`, `AUTOGEN_PARENT_UID:0001H2`, position `50`; reconstruction C++ remains blank below the `95/95` bar.
  - Evidence: live IDA MCP reconfirmed `0x005648f0` as a raw non-function start with no xrefs/callers, no pointer-pattern hits, exact `0x1a` / 26-byte body size, `0x6` / 6-byte pre/post alignment spans, enabled byte `+0x102`, range word `+0x100`, and [UID:0001H2][0x00564710-0x005654ec.ScrollVolumePane](by-memory/0x00564710-0x005654ec.ScrollVolumePane.md) as the direct enclosing `85/88` parent.
- 2026-06-19 B014 class source-quality incorporation:
  - Changed completion/confidence from `85/88` to `87/90` and populated first-draft `ScrollVolumePane::CanAdjust() const` C++.
  - Evidence: B014 class pass accepted `m_enabled` and `m_range` names, confirmed this helper's source-shaped predicate body, kept the no-xref/raw-start caveat, and resolved the helper as private `ScrollVolumePane` logic rather than external/no-code support.
