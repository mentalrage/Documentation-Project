*** UID:0004BM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000007 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000007 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
AddItemDialog::AddItemDialog(unsigned int exchangeSessionId,
                             unsigned char allowMultipleSelection)
    : DialogPane(L"", 9, 1)
{
    RectBounds bounds;
    int dialogWidth;
    int dialogHeight;

    if (g_useEpfAssets == 1)
    {
        InitRectBounds(&bounds, 0, 0, 241, 340);
        AddControl(new EPFImageControlPane(L"DLGEXC2.EPF", 0, 1,
                                            &bounds, L"PAL01.PAL"));

        InitRectBounds(&bounds, 55, 303, 118, 327);
        AddControl(new ImageButtonControlPane(36, &bounds));

        InitRectBounds(&bounds, 132, 303, 195, 327);
        AddControl(new ImageButtonControlPane(22, &bounds));

        MyItemListPane *itemList = new MyItemListPane();
        itemList->SetMultipleSelectionEnabled(allowMultipleSelection);

        InitRectBounds(&bounds, 30, 30, 218, 270);
        AddControl(new ScrollableControlPane(&bounds, itemList, 0, 0, 13));

        SetBackgroundResource(L"DLGEXC2.EPF", NULL);
        SetHoverControl(3);
        SetFocusedControl(1);
        SetPendingControl(2);
        dialogWidth = 241;
        dialogHeight = 340;
    }
    else
    {
        InitRectBounds(&bounds, 0, 0, 287, 277);
        AddControl(new EPFImageControlPane(L"DLGEXC2.EPD", 0, 1,
                                            &bounds, L"NPAL8.PAL"));

        InitRectBounds(&bounds, 69, 225, 141, 241);
        AddControl(new ImageButtonControlPane(36, &bounds));

        InitRectBounds(&bounds, 146, 225, 218, 241);
        AddControl(new ImageButtonControlPane(22, &bounds));

        MyItemListPane *itemList = new MyItemListPane();
        itemList->SetMultipleSelectionEnabled(1);

        InitRectBounds(&bounds, 36, 61, 251, 205);
        AddControl(new ScrollableControlPane(&bounds, itemList, 0, 0, 0));

        SetBackgroundResource(L"DLGEXC2.EPD", NULL);
        SetHoverControl(3);
        SetFocusedControl(1);
        SetPendingControl(2);
        dialogWidth = 287;
        dialogHeight = 277;
    }

    m_exchangeSessionId = exchangeSessionId;
    InitRectBounds(&bounds, 0, 0, dialogWidth, dialogHeight);
    OnCreate(&bounds, 0, NULL, g_mainUiLayerSlots.overlayPaneLayerContext);
    OnShow(NULL, g_pBackPane);
    SlideOpenVertical();
    g_pAddItemDialog = this;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: AddItemDialog constructor; builds the exact EPF/EPD control tree, configures MyItemListPane selection mode, stores the exchange session context, creates/shows/slides the dialog, and publishes the active singleton last. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ae4c0-0x004ae8f5 AddItemDialog Constructor

## Status

- Disposition: reconstructable NexusTK project code; exact ordinary AddItemDialog constructor, not an aggregate or split candidate.
- Entity kind: constructor child of [UID:00033Q][0x004ae4c0-0x004aea7d.AddItemDialogCore](by-memory/0x004ae4c0-0x004aea7d.AddItemDialogCore.md).
- Owner/emitter: [UID:000007][AddItemDialog](by-class/AddItemDialog.md); source file [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md).
- ABI: `__thiscall` with two four-byte stack argument slots, `retn 8`, and `this` returned in EAX. Source-facing parameters are `unsigned int exchangeSessionId` and `unsigned char allowMultipleSelection`.
- Exact range: `0x004ae4c0-0x004ae8f5`, size `0x435`, 398 instructions, 48 basic blocks. Pre-padding `0x004ae4b6-0x004ae4c0` and post-padding `0x004ae8f5-0x004ae900` are external non-emitting spans.
- Rebuild handling: source-authored constructor at position 10. Vptr writes, stack-cookie operations, SEH state, cleanup funclets, and adjacent padding are compiler/data concerns and are excluded from formal C++.

## Exact Construction Behavior

- Base construction is `DialogPane(L"", 9, 1)` at `0x004ae4fb`. Derived vptr stores to object offsets `+0`, `+0xa0`, and `+0xa4` install AddItemDialog tables `0x00619e8c`, `0x00619eec`, and `0x00619f1c`; they are compiler lowering, not handwritten assignments.
- The branch predicate is exact equality `g_useEpfAssets == 1`.
- EPF branch: background `EPFImageControlPane(L"DLGEXC2.EPF",0,1,(0,0,241,340),L"PAL01.PAL")`; action button id 36 at `(55,303,118,327)`; cancel button id 22 at `(132,303,195,327)`; local MyItemListPane receives `allowMultipleSelection`; ScrollableControlPane uses `(30,30,218,270)`, `(list,0,0,13)`; background resource is `DLGEXC2.EPF`; final dialog size is `241x340`.
- EPD branch: background `EPFImageControlPane(L"DLGEXC2.EPD",0,1,(0,0,287,277),L"NPAL8.PAL")`; action button id 36 at `(69,225,141,241)`; cancel button id 22 at `(146,225,218,241)`; local MyItemListPane is forced to multiple-selection value `1`; ScrollableControlPane uses `(36,61,251,205)`, `(list,0,0,0)`; background resource is `DLGEXC2.EPD`; final dialog size is `287x277`.
- Both branches add controls in exact insertion order: background, action button, cancel button, scroll/list wrapper. Selector `3` therefore addresses the fourth inserted control; there is no explicit selector-bearing `AddChild` call.
- Both branches set hover/focused/pending controls to `3`, `1`, and `2`. The list selection operation is exact helper [UID:0004LX][0x004f3dd0-0x004f3e1c.ListPaneSetMultipleSelectionEnabled](by-memory/0x004f3dd0-0x004f3e1c.ListPaneSetMultipleSelectionEnabled.md), not generated `SetSortEnabled`.
- MyItemListPane is a local allocation passed to ScrollableControlPane, which assumes ownership. No list pointer is stored in AddItemDialog.
- Common tail order is exact: store `exchangeSessionId` at `this+0x26c`; build the final bounds; `OnCreate(&bounds,0,NULL,g_mainUiLayerSlots.overlayPaneLayerContext)`; `OnShow(NULL,g_pBackPane)`; `SlideOpenVertical()`; then publish `g_pAddItemDialog = this` last.
- There is no direct constructor call to UID0004BP. The `OnCreate` call at `0x004ae8b5` synchronously reaches generic refresh wrapper `0x0049fe20` through `0x0049e119`; that wrapper virtual-dispatches primary slot `+0x4c` after the four controls and AddItem vtables exist, so dynamic AddItem-family construction does initialize action-button state before the constructor returns.

## Caller, Layout, And Dependency Evidence

- Five direct callers cover employee, exchange, mix, and add-mixing flows: `0x004a339f` allocates derived size `0x274` and calls `(0,0)`; raw `0x004a4aee` also calls `(0,0)` and stores its derived owner at `+0x270`; `0x004ad124` allocates exact size `0x270` and calls `(m_exchangeSessionId,1)`; `0x004af924` allocates `0x274` and calls `(0,1)`; `0x004b0031` calls the base constructor `(0,1)` from a derived constructor.
- Exact AddItemDialog size is `0x270`; derived classes add one dword at `+0x270`. The target has no AddItemDialog field at `+0x270`.
- Each branch performs allocations of 332, 276, 276, 332, and 268 bytes for EPFImageControlPane, two ImageButtonControlPane objects, MyItemListPane, and ScrollableControlPane.
- The helper calls at `0x004ae652` and `0x004ae803` use the dynamic EPF byte and forced EPD value one. Three additional helper callers at `0x0047496a`, `0x0047928a`, and `0x0047982c` prove generic ListPane ownership.
- Accepted support names retained without score inflation are `g_useEpfAssets`, `g_mainUiLayerSlots.overlayPaneLayerContext`, `g_pBackPane`, and `g_pAddItemDialog`.

## EH And Compiler-Generated Behavior

- SEH table `stru_65C824` and cleanup funclets `0x005fe43b-0x005fe4d6` model base cleanup state zero and ten allocation states, one for each branch allocation.
- States 1-5 clean the EPF branch's EPF image, two buttons, list, and scroll wrapper if construction throws; states 6-10 do the same for the EPD branch. Successful construction resets state before the next allocation.
- Ordinary `new` expressions and base construction regenerate these null/cleanup paths. Explicit source deletes, catches, vptr stores, cookie checks, or cleanup calls would duplicate compiler behavior and are rejected.

## Negative Evidence And Historical Corrections

- No instruction stores `allowMultipleSelection` in the object; the EPD branch ignores the incoming byte and passes literal one.
- No instruction stores MyItemListPane as `m_itemList`, calls `CreateItemDialogControls`, directly calls UID0004BP `UpdateActionButton`, or performs the stale explicit `AddChild(3,...)` operation. The direct-call negative does not negate the separately proven synchronous `OnCreate -> 0x0049fe20 -> virtual +0x4c` route.
- No target-internal function start, internal padding, table, disjoint owner, or source-authored cleanup warrants a split.
- Historical B007 draft names `packetType`, `m_packetType`, `mode`, `m_mode`, `CreateItemDialogControls`, `m_itemList`, and `AddChild` were shallow placeholders and are superseded by direct binary evidence. The prior early singleton and direct constructor-call descriptions are rejected; the broader historical no-constructor-refresh conclusion is also superseded by the synchronous virtual route above.
- `exchangeSessionId`, `allowMultipleSelection`, `m_exchangeSessionId`, and `SetMultipleSelectionEnabled` are descriptive source spellings backed by field use, caller semantics, and exact helper behavior; original lexical symbols remain stripped.

## Source Placement

This constructor remains in AddItemDialog within `NexusTK/ui/dialogs/ItemDialogs.cpp`. ExchangeDialog, employee/mix callers, MyItemListPane, and generic control sources are consumers or dependencies, not alternate owners. The reusable selection setter emits through ListPane in `NexusTK/ui/controls/ListPane.cpp`.

## Score Rationale

`92/94` reflects complete range, ABI, branch/control/resource geometry, caller/layout, dependency, lifecycle, failure-path, source-placement, and formal-source resolution. Scores remain below 95 because private argument/member/helper spellings and access specifiers are behavior-backed rather than symbol-recovered.

## Cross-References

- [UID:000007][AddItemDialog](by-class/AddItemDialog.md)
- [UID:00033Q][0x004ae4c0-0x004aea7d.AddItemDialogCore](by-memory/0x004ae4c0-0x004aea7d.AddItemDialogCore.md)
- [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md)
- [UID:00008W][MyItemListPane](by-class/MyItemListPane.md)
- [UID:0004LX][0x004f3dd0-0x004f3e1c.ListPaneSetMultipleSelectionEnabled](by-memory/0x004f3dd0-0x004f3e1c.ListPaneSetMultipleSelectionEnabled.md)

## Changes

- 2026-07-13 B005 UID0004BP callback: retained `92/94`, position `10`, and the complete accepted constructor block; preserved the no-direct-target-call fact while documenting the synchronous `OnCreate -> 0x0049fe20 -> virtual +0x4c` updater route and historicalizing the broader no-constructor-refresh assumption.

- 2026-07-13 B001 UID0004BM callback: replaced the historical shallow draft with the exact accepted constructor, score/position/summary, full binary behavior, five-caller/layout evidence, selection-helper route, compiler-EH exclusions, source placement, negative evidence, and superseded-name history.
