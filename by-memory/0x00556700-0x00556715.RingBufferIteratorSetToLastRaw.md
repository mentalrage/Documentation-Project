*** UID:0004EZ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:87 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000C2 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:220 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RingBufferIterator::SetToLast()
{
    mCurrentIndex = (mOwner->mWriteIndex - 1 + mOwner->mCapacity) % mOwner->mCapacity;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact raw RingBufferIterator SetToLast helper; stores `(mOwner->mWriteIndex - 1 + mOwner->mCapacity) % mOwner->mCapacity` into mCurrentIndex. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00556700-0x00556715 RingBufferIterator SetToLast Raw

## Status

- Address range: `0x00556700-0x00556715`, exclusive end before alignment to `0x00556720`.
- Entity kind: raw iterator helper.
- Owner: [UID:0000C2][RingBufferIterator](by-class/RingBufferIterator.md), emitted through [UID:0000N8][RingBuffer](by-file/RingBuffer.md).
- Split parent: [UID:0001G4][0x005563d0-0x005568c8.RingBuffer](by-memory/0x005563d0-0x005568c8.RingBuffer.md).
- Current B007 score disposition: `87/90`. The score is raised from `86/89` because the current live MCP pass closes stale evidence gaps with exact bytes, bounded disassembly, unique exact and wildcard signatures, boundary padding, and negative xref/name checks. It is capped at `87/90` because this remains raw no-function code with no original symbol, no direct inbound caller route, and unresolved public/private source exposure.

## Behavior

Positions the iterator on the last occupied slot by computing `(mOwner->mWriteIndex - 1 + mOwner->mCapacity) % mOwner->mCapacity` and storing the result in `mCurrentIndex`.

Field roles remain:

- RingBufferIterator `+0x04`: `mOwner`.
- RingBufferIterator `+0x08`: `mCurrentIndex`.
- RingBuffer `+0x14`: `mCapacity`.
- RingBuffer `+0x24`: `mWriteIndex`.

This is the write-side set-to-last helper. It is distinct from the read-boundary/reset helpers because it reads the ring write index at owner `+0x24`, subtracts one, wraps by capacity, and stores that wrapped previous write slot. The read-boundary helpers use the ring read index at owner `+0x20` or compare against read/write boundary state instead of unconditionally storing the previous write slot.

## Evidence

B007 live MCP session `18aed30a` was active for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` returned `ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.

Range and boundary evidence:

- MCP `lookup_funcs`: `0x00556700` and `0x00556715` are not IDA function objects.
- MCP `lookup_funcs`: the previous modeled helper is `sub_5566F0` at `0x005566f0`, size `0xa`.
- MCP `get_bytes`: `0x005566fa-0x00556700` is six `0xcc` alignment bytes before this helper.
- MCP `insn_query 0x00556700-0x00556715`: returned 11 instructions, all with `fn: null`.
- MCP `get_bytes`: the target bytes at `0x00556700` are `8b 41 04 56 8b 70 14 8b 40 24 48 03 c6 99 f7 fe 5e 89 51 08 c3`.
- MCP `get_bytes`: `0x00556715-0x00556720` is eleven `0xcc` alignment bytes after this helper.
- MCP `lookup_funcs`: the next modeled helper is `sub_556720` at `0x00556720`, size `0x29`.

Signature and uniqueness evidence:

- MCP `make_signature_for_range 0x00556700-0x00556715`, exact operands: `8B 41 04 56 8B 70 14 8B 40 24 48 03 C6 99 F7 FE 5E 89 51 08 C3`, `unique: true`.
- MCP `make_signature_for_range 0x00556700-0x00556715`, wildcarded operands: `8B 41 ? 56 8B 70 ? 8B 40 ? 48 03 C6 99 F7 FE 5E 89 51 ? C3`, `unique: true`.
- MCP `find_bytes` for the exact 21-byte sequence returned one match: `0x556700`.

Instruction-level behavior:

- `0x556700`: `mov eax, [ecx+4]` loads iterator `mOwner`.
- `0x556704`: `mov esi, [eax+14h]` loads owner `mCapacity`.
- `0x556707`: `mov eax, [eax+24h]` loads owner `mWriteIndex`.
- `0x55670a-0x55670b`: `dec eax; add eax, esi` computes `mWriteIndex - 1 + mCapacity`.
- `0x55670d-0x55670e`: `cdq; idiv esi` computes the signed division remainder by capacity.
- `0x556711`: `mov [ecx+8], edx` stores the remainder to iterator `mCurrentIndex`.
- `0x556714`: `retn` returns from the raw helper.

Route and negative evidence:

- MCP `xrefs_to 0x00556700`: zero inbound xrefs.
- MCP `xrefs_to 0x00556715`: zero inbound xrefs.
- MCP `entity_query` for names in `0x005566d0-0x005568c8`: no names in this helper cluster.
- MCP `entity_query` found RingBufferIterator vtable/RTTI names, including `??_7RingBufferIterator@@6B@` at `0x006230dc`.
- MCP `xrefs_to 0x006230dc`: one data xref at `0x5566d7`, matching the RingBufferIterator constructor vptr store and supporting the class route.
- The source route remains `NexusTK/util/RingBuffer.cpp`, emitted through [UID:0000N8][RingBuffer](by-file/RingBuffer.md), with [UID:0000C2][RingBufferIterator](by-class/RingBufferIterator.md) as canonical owner.

## Source-Facing Name Decision

`RingBufferIterator::SetToLast()` is retained as an inferred descriptive source-facing name. It reflects the behavior better than raw labels such as `sub_556700`, address-derived names, or generic raw-helper names, and it is distinct from weaker alternatives such as `Reset`, `AtReadBoundary`, or `PreviousFromWriteIndex`: the helper does not read the ring read index, does not test a boundary, and does not merely return a computed value; it stores the iterator on the wrapped slot immediately before the write index.

No original source symbol or public/private exposure proof was recovered. That unresolved state caps confidence but does not justify reverting to a compiler/raw label in source-facing documentation or generated C++.

## Open Questions

- Original helper name: attempted through MCP name queries, target-range function lookup, support docs, generated route, and prior reports. No source symbol was found. `SetToLast` remains an inferred descriptive name and is not claimed as proven original spelling.
- Public/private exposure: attempted through class/file support docs and generated output. No source header or symbol evidence was found. Exposure remains unresolved and caps the score.
- Direct callers/reachability: attempted through `xrefs_to` on the target start and end boundary, plus nearby helper starts. No direct inbound route was recovered. This supports the raw-helper/no-direct-caller cap rather than a higher score.
- Empty-buffer behavior: the raw helper has no count guard; any precondition belongs to the caller or higher-level RingBuffer semantics. The target C++ intentionally preserves the helper body only.
- Signed modulo form: the assembly uses `idiv`, but the field roles are non-negative capacity/index values. The current source-level modulo expression is the correct compact reconstruction for this helper.

## C++ Disposition

Formal first-draft C++ is retained unchanged because the raw bytes uniquely implement the previous-slot-with-wrap calculation. The helper name is inferred and documented as such. Owner, emitter, reconstructable status, emitter position, range, and formal C++ body remain unchanged.
