*** UID:0002GH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ColorStringChattingMessage::Draw(GrafPort *grafPort, RectBounds *bounds)
{
    if (g_useEpfAssets == true) {
        EPFTileContext unusedContext;

        grafPort->SetTextDrawMode(1);
        grafPort->SetTextColor(m_foregroundColor);

        if (m_textStyleFlag != 0) {
            grafPort->DrawTextInRect(m_text, static_cast<int>(wcslen(m_text)), bounds);
            bounds->Offset(1, 0);
            grafPort->SetTextDrawMode(1);
            grafPort->SetTextColor(m_foregroundColor);
            grafPort->SetTextBackColor(0);
            grafPort->DrawTextInRect(m_text, static_cast<int>(wcslen(m_text)), bounds);
            return;
        }

        grafPort->SetTextBackColor(m_backgroundColor);
        if (m_customForegroundRgb != 0 || m_customBackgroundRgb != 0) {
            DLPalette *textPalette = g_pPaletteLib->GetTextPalette();
            const unsigned short savedForeground = textPalette->MapColor(m_foregroundColor);
            const unsigned short savedBackground = textPalette->MapColor(m_backgroundColor);

            textPalette->SetColor(
                m_foregroundColor,
                static_cast<unsigned char>(m_customForegroundRgb >> 16),
                static_cast<unsigned char>(m_customForegroundRgb >> 8),
                static_cast<unsigned char>(m_customForegroundRgb));
            textPalette->SetColor(
                m_backgroundColor,
                static_cast<unsigned char>(m_customBackgroundRgb >> 16),
                static_cast<unsigned char>(m_customBackgroundRgb >> 8),
                static_cast<unsigned char>(m_customBackgroundRgb));

            DLPalette *savedPalette = grafPort->GetPalette();
            grafPort->SetPalette(textPalette);
            grafPort->DrawOutlinedText(m_text, static_cast<int>(wcslen(m_text)), bounds);
            textPalette->SetPackedColor(m_foregroundColor, savedForeground);
            textPalette->SetPackedColor(m_backgroundColor, savedBackground);
            grafPort->SetPalette(savedPalette);
            return;
        }

        grafPort->DrawOutlinedText(m_text, static_cast<int>(wcslen(m_text)), bounds);
        return;
    }

    EPFTileContext background;

    int frameIndex;
    switch (m_backgroundColor) {
    case 143:
        frameIndex = 0;
        break;
    case 12:
        frameIndex = 1;
        break;
    case 1:
        frameIndex = 2;
        break;
    default:
        return;
    }

    g_pEPFLib->LookupLayoutEntry(L"CHATBACK.EPD", frameIndex, &background);
    grafPort->SetDrawMode(0);
    grafPort->DrawTiledBackground(
        bounds,
        reinterpret_cast<const FrameDrawRecord *>(&background),
        L"NP");
    grafPort->SetTextDrawMode(1);
    grafPort->SetTextColor(m_foregroundColor);
    grafPort->DrawTextInRect(m_text, static_cast<int>(wcslen(m_text)), bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready ColorStringChattingMessage::Draw with exact GrafPort/RectBounds ABI and 0x1c message layout; preserves current EPF text mode, repeated wrapped two-pass caller-bounds offset, outlined text, borrowed text-palette RGB save/mutate/install/draw/entry-restore/palette-restore ordering, legacy CHATBACK.EPD frame mapping 143/12/1 to 0/1/2, NP tiled background, invalid-key void return, exact 636-byte body, compiler jump/selector tables, one-byte tail alignment, vtable-only liveness, Chatting.cpp placement, blank child H because the class owns the declaration, and protected no-handwritten-table handling. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004836a0-0x004839c0 - ColorStringChattingMessage Draw With Tables

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored virtual draw method plus compiler-emitted switch/mapping tail data and one tail-alignment byte.
- Entity kind: class method / virtual draw method with tail tables.
- Owner: [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md).
- Parent aggregate: [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md).
- Confidence: very strong for the exact ABI, `0x1c` receiver layout, body and tail ranges, vtable ownership, both render branches, palette transaction, caller behavior, compiler tables, and Chatting source placement. Original lexical spellings and whether the original switch spelled out the default-equivalent key `5` remain unrecoverable but do not affect behavior or source readiness.

## Behavior

This virtual method draws one chat message through the current-layout/high-resolution path or the older `CHATBACK.EPD` backed path. It takes the message object as the receiver, a draw context argument, and a target rectangle argument; the epilogue returns with `retn 8`.

When [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md) is `1`, the method initializes a local draw scratch object, enables the draw-context byte at offset `+0x88`, and applies the message foreground palette selector `m_foregroundColor` from object offset `+0x08`. The byte at object offset `+0x10`, now source-modeled as `m_textStyleFlag`, selects a two-pass offset draw in this branch: the method draws the owned wide string through `0x004bad70`, offsets the rectangle by `(1, 0)` through `0x004b78f0`, restores the foreground color, forces the background id to `0`, then draws the same text again through `0x004bad70`.

When `m_textStyleFlag` is clear and either RGB override field `m_customForegroundRgb` at `+0x14` or `m_customBackgroundRgb` at `+0x18` is nonzero, the method gets the embedded `DLPalette` object through `g_pPaletteLib` at `0x0067a7e0` and helper `0x00543d60`, saves the current foreground/background palette entries, and writes replacement RGB components with `DLPalette::SetColor` at `0x00542ae0`. It then uses [UID:000163][0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers](by-memory/0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers.md): getter call `0x004837a7` saves the current borrowed GrafPort `DLPalette *`, setter `0x004837b2` installs the temporary palette, and setter `0x004837fd` restores the saved pointer after `DLPalette::SetPackedColor` at `0x00542b50` restores both entries. If there are no RGB overrides, it draws the owned wide string directly through `0x004bafa0`. The message is a consumer only; the accessors remain GrafPort-owned.

When `g_useEpfAssets` is not `1`, the method uses the message background selector `m_backgroundColor` at object offset `+0x0c` as a switch key. Background ids `143`, `12`, and `1` map to `CHATBACK.EPD` variants `0`, `1`, and `2` respectively; every other id takes a bare `void` return without drawing. The former residual-EAX return interpretation came from the incomplete IDA pretype and is rejected by the virtual declaration, vtable slot, callers, and shared return block. For a recognized id, the method loads the `CHATBACK.EPD` frame through the resource-layout helper at `0x004d02f0`, sets draw mode `0`, draws the tiled background through `0x004ba6b0` with palette name `NP`, sets text draw mode `1`, applies `m_foregroundColor` from offset `+0x08`, computes the owned wide-string length from `m_text`, and renders through `0x004bad70`.

[UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) preserves every field consumed here by forwarding `m_text`, `m_foregroundColor`, `m_backgroundColor`, `m_textStyleFlag`, `m_customForegroundRgb`, and `m_customBackgroundRgb` into the constructor. This confirms the source-facing field model and explains why clone is not a shallow memcpy or generic `LObject` helper. B009's clone/draw reconciliation treats older `m_useCustomRgb` wording as historical shorthand only: the byte selects the two-pass/outline branch, while custom foreground/background RGB are independent packed values.

The modeled function body ends at `0x0048391c`. The compiler-emitted jump table occupies `0x0048391c-0x00483930`, the switch byte-map table occupies `0x00483930-0x004839bf`, and a single `0xcc` alignment byte at `0x004839bf-0x004839c0` precedes the next function.

## IDA MCP Evidence

- Live IDA function lookup reports a modeled function at `0x004836a0` with size `0x27c`; `0x0048391c` and `0x00483930` are not function starts, and the next modeled function starts at `0x004839c0`.
- `xrefs_to 0x004836a0` reports the vtable data entry at `0x00615280`; `callers 0x004836a0` reports no ordinary direct callers, matching virtual-only dispatch.
- Tail-data xrefs are internal to the modeled body: `0x00483894 -> 0x0048391c` for the jump table and `0x0048388d -> 0x00483930` for the byte-map table.
- `get_bytes 0x0048369a size 6` reports six `0xcc` bytes before the draw method. `get_bytes 0x0048391c size 0xa4` shows five jump-table dwords, the byte-map table, and a final `0xcc` byte before `0x004839c0`.
- `callees 0x004836a0` reports direct calls to `0x00457a60`, `0x004b78f0`, `0x004b9680`, `0x004b9690`, `0x004b96a0`, `0x004b96b0`, `0x004ba6b0`, `0x004bad70`, `0x004bafa0`, `0x004d02f0`, `0x00542ac0`, `0x00542ae0`, `0x00542b50`, `0x00543d60`, and the stack-cookie check at `0x005c772f`.
- Live decompilation checks the global at `0x0066da97`, loads `CHATBACK.EPD`, writes draw-context bytes at offsets `+0x70` and `+0x88`, branches on message fields at offsets `+0x08`, `+0x0c`, `+0x10`, `+0x14`, and `+0x18`, and draws the owned wide string through the wrapped-text or direct wide-text helpers.

## Cross-References

- [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md)
- [UID:000104][0x0047efb0-0x00483ef7.ChattingUI](by-memory/0x0047efb0-0x00483ef7.ChattingUI.md)
- [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md)
- [UID:000163][0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers](by-memory/0x004b96a0-0x004b96c0.DrawContextBrushHandleHelpers.md)
- [UID:00016A][0x004ba6b0-0x004ba81d.GrafPortDrawTiledBackground](by-memory/0x004ba6b0-0x004ba81d.GrafPortDrawTiledBackground.md)
- [UID:00016D][0x004bad70-0x004baf92.DrawTextInRect](by-memory/0x004bad70-0x004baf92.DrawTextInRect.md)
- [UID:00016E][0x004bafa0-0x004bb0db.SimpleHelpTextPartPaneTextEffects](by-memory/0x004bafa0-0x004bb0db.SimpleHelpTextPartPaneTextEffects.md)

## B010 2026-08-14 Source-Ready Draw Closure

### Exact ABI, Layout, And Liveness

- The source ABI is `void __thiscall ColorStringChattingMessage::Draw(GrafPort *grafPort, RectBounds *bounds)`. The previous IDA type `int __thiscall(int this, int, _DWORD *)` is incomplete; the primary vtable cell at `0x00615280` points to this body at slot `+0x14`, after `MeasureLines` at slot `+0x10`.
- The concrete object is exactly `0x1c` bytes: vptr `+0x00`, owned `wchar_t *m_text` `+0x04`, `m_foregroundColor` `+0x08`, `m_backgroundColor` `+0x0c`, byte `m_textStyleFlag` `+0x10`, `m_customForegroundRgb` `+0x14`, and `m_customBackgroundRgb` `+0x18`. The source-facing names are high-probability inferred spellings grounded in constructor, clone, measure, and draw use; no complete IDA UDT is required.
- ChattingPane reaches the method through virtual dispatch at `0x0047fa4c` in the current-width path and `0x0047fb75` in the legacy-width path. Those consumers call `MeasureLines` first, using widths `783` and `408`, then pass a full `RectBounds` row. Zero ordinary direct callers plus the vtable data reference therefore prove live virtual use rather than dead code.

### Exact Current And Legacy Behavior

- The current-layout branch tests `g_useEpfAssets == true`, initializes an otherwise unused `EPFTileContext`, sets text draw mode `1`, and selects `m_foregroundColor`. The equality test and unused initialization are preserved because they are observable lowering facts even though a simplified reconstruction could omit them.
- With `m_textStyleFlag != 0`, the method evaluates `wcslen(m_text)` for the first wrapped draw, mutates the caller-owned bounds by `(1, 0)`, reapplies text mode/foreground, sets text background color `0`, reevaluates `wcslen`, and draws again. It does not restore the caller rectangle.
- With the style byte clear, it sets `m_backgroundColor`. If both custom RGB fields are zero it performs one outlined draw. If either custom RGB field is nonzero, both palette entries are rewritten: each packed RGB value is split high/middle/low into red/green/blue, the old mapped entries are saved, the borrowed text palette is installed on GrafPort, outlined text is drawn, both packed entries are restored through `SetPackedColor`, and the previously borrowed GrafPort palette is restored. There is no palette release.
- The legacy branch initializes a separate `EPFTileContext`, maps background keys `143`, `12`, and `1` to frames `0`, `1`, and `2`, and returns for every other key. It resolves `CHATBACK.EPD`, sets draw mode `0`, calls `DrawTiledBackground` with `NP`, then sets text draw mode `1`, foreground color, and draws the text. `EPFTileContext` remains the local source type; the only layout-boundary cast is to `const FrameDrawRecord *` at `DrawTiledBackground` because both records are `0x28` bytes but have distinct source semantics.
- Each source draw call independently evaluates the string length. The two-pass style route scans twice. The switch key `5` reaches the same default return and does not justify an invented source case.

### Exact Pocket, Tables, And Protected Boundaries

| Region | Exact identity and source disposition |
| --- | --- |
| Authored body | `[0x004836a0,0x0048391c)`, 636 bytes, SHA256 `DF4DFC2018D31D28EEF3DAE2A168A8A813E75B440911EFAA9617EA10B9EC3DAA`; retain one modeled function. |
| Jump table | `jpt_483894 [0x0048391c,0x00483930)`, 20 bytes, SHA256 `A64B3BF81649689C54387F441C95D98A4774C5F39B866E8B245D378D64D0DA29`; compiler-emitted switch data, not handwritten source. |
| Selector table | `byte_483930 [0x00483930,0x004839bf)`, 143 bytes, SHA256 `0ACDCE702936EAECA6DF5142EAED9C6334C1CD4920B2955E734133A4EC447119`; indexes `0/4/11/142` select `00/01/02/03`, all others `04`; compiler-emitted, not handwritten source. |
| Tail alignment | `[0x004839bf,0x004839c0)` is one `CC` byte, SHA256 `1DD8312636F6A0BF3D21FA2855E63072507453E93A5CED4301B364E91C9D87D6`; exclude from source. |
| Complete pocket | `[0x004836a0,0x004839c0)`, 800 bytes, SHA256 `97E2EA981E07E0DB79783BC4528DB44B9E144E2C4828A71968A4D4F8B999A654`; do not split, merge, or define table functions. |
| Prepad | `[0x0048369a,0x004836a0)` is six `CC` bytes, SHA256 `33CC4A00A0F04735602819132DA3E3FAC4CDEB8EC7AEE92551DC79BA8488E722`; preserve as non-source padding. |
| Successor | `sub_4839C0 [0x004839c0,0x004839cb)`, 11 bytes, SHA256 `9522583BF93440680B9666758D5A9FFD30A81E34547BC80F041957F39A05D52A`; separate function and protected neighbor. |
| Vtable cell | `[0x00615280,0x00615284)` contains `A0 36 48 00`, SHA256 `92548BA999CE4AC9E43AA2114714BD7C3E5A1E0844A6555D919050AE46859A8E`; preserve the sole outbound data xref to the method. |

The complete semantic callee inventory is EPFTileContext initialization, RectBounds offset, GrafPort text/draw/palette accessors, tiled-background and text helpers, `CHATBACK.EPD` lookup, `DLPalette::MapColor`, `DLPalette::SetColor`, `DLPalette::SetPackedColor`, `PaletteLib::GetTextPalette`, plus the compiler security-cookie helper. These are dependencies, not alternate owners or emitters; no third-party import, new translation unit, handwritten table, or direct-call wrapper is introduced.

### Source Placement And IDA Handoff

- The direct owner/emitter remains [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md), which already owns the declaration in `Chatting.h`; this child therefore keeps formal H blank. File ownership/emission remains [UID:0000I5][Chatting](by-file/Chatting.md), and the definition belongs in `NexusTK/social/Chatting.cpp` without a new file.
- Supervisor Gate 2B may rename only function `0x004836a0` from `sub_4836A0` to `ColorStringChattingMessage__Draw`, apply type `void __thiscall ColorStringChattingMessage__Draw(ColorStringChattingMessage *this, GrafPort *grafPort, RectBounds *bounds)`, and add regular function comment `ColorStringChattingMessage::Draw; renders current EPF text or legacy CHATBACK.EPD background/text and restores temporary palette state.` The exact current body hash, blank entry/function comments, one inbound vtable xref, destination-name absence, and protected prepad/table/tail/vtable/successor states must match before mutation.
- The only permitted frame change is `arg_0 +0x54 GrafPort *` becoming `grafPort +0x54 GrafPort *`; all other ten frame rows, every byte, protected item, xref, neighbor, and opaque `ColorStringChattingMessage` local type remain unchanged. Any additional delta requires fail-closed rollback rather than broadening this source-document callback.

## Changes

- 2026-08-14 B010 source-ready implementation callback:
  - Raised `86/92 -> 94/96`, retained UID00002Z owner/emitter, exact range, reconstructable state, and Chatting.cpp route, and inserted the accepted human-quality CPP while keeping child H blank under class ownership.
  - Replaced the stale residual-integer default interpretation with the exact void ABI; preserved equality-mode selection, unused context initialization, repeated scans, caller-bounds mutation, both palette-entry overrides and restoration order, sparse legacy mapping, and the one-boundary record cast.
  - Added exact body/table/tail/pocket/prepad/successor/vtable hashes, virtual caller sites, complete source placement, dependency/no-import decisions, and bounded supervisor-owned IDA handoff without emitting compiler tables or moving ownership.

- 2026-07-12 B004 UID000163 draw-support sync:
  - Score, metadata, formal C++, body/table boundaries, and all unrelated draw behavior remain unchanged.
  - Replaced mixed brush/palette-handle wording with exact `DLPalette *` save at `0x004837a7`, temporary install at `0x004837b2`, and restore at `0x004837fd`, preserving palette-entry mutation/restoration and GrafPort ownership.
- 2026-06-05: Updated completion/confidence from `78/88` to `86/92` and attached the page to [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md).
  - Before: the page documented the broad high-resolution versus legacy behavior but still used stale generated labels, left parent attachment blank, and treated the whole tail through `0x004839c0` as non-padding data.
  - After: the page records live IDA function size, vtable ownership, virtual-only caller state, callee set, exact high-resolution and legacy draw branches, palette override restoration, two-pass offset drawing, jump-table and byte-map table boundaries, and the one-byte tail alignment.
  - Evidence: live IDA MCP lookup, xrefs, callers, callees, decompilation, disassembly, and byte reads for `0x0048369a`, `0x004836a0`, `0x0048391c`, `0x00483930`, and `0x004839c0`.
- 2026-06-21 B006 Rule 26 support update:
  - Score unchanged at `86/92`.
  - Summary/evidence: aligned the draw field names with [UID:0002GF][0x004835b0-0x0048362b.ColorStringChattingMessageClone](by-memory/0x004835b0-0x0048362b.ColorStringChattingMessageClone.md) and [UID:00002Z][ColorStringChattingMessage](by-class/ColorStringChattingMessage.md): `m_text`, `m_foregroundColor`, `m_backgroundColor`, `m_useCustomRgb`, `m_customForegroundRgb`, and `m_customBackgroundRgb`. Preserved the nuance that `m_useCustomRgb` also participates in a current-layout outlined/two-pass draw path, so it should not be renamed narrowly to only `m_outlineText`.
- 2026-06-22 B009 Rule 26 reconciliation:
  - Score unchanged at `86/92`.
  - Summary/evidence: refined the `+0x10` source-facing name to `m_textStyleFlag` / possible original `m_useOutlineText` after B009 confirmed the current-layout branch semantics. Nonzero selects the outlined/two-pass draw path; when clear, nonzero `m_customForegroundRgb` / `m_customBackgroundRgb` values drive temporary palette overrides. Historical `m_useCustomRgb` wording is preserved only as shorthand and should not be used as the literal field interpretation.
