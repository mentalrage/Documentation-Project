*** UID:0003RB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CV | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CV | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SelfLookPane2::OnPaint()
{
    EPFTileContext tileContext;
    RectBounds imageBounds;
    RectBounds overlayBounds;
    RectBounds destinationBounds;
    RectBounds buttonBounds;
    RectBounds statBounds;
    wchar_t statText[128];

    DLPalette *palette = g_pPaletteLib->GetPaletteByName(L"SELFLOOK.PAL");

    if (m_viewMode == ViewMode_Default ||
        m_viewMode == ViewMode_Spelled ||
        m_viewMode == ViewMode_Legend)
    {
        g_pEPFLib->LookupLayoutEntry(L"SELFLOOK.EPF",
                                     m_viewMode,
                                     &tileContext);
    }

    g_pfnBlitSprite(this,
                     &tileContext,
                     &tileContext.bounds,
                     &m_bounds,
                     0,
                     palette,
                     NULL);

    if (m_viewMode == ViewMode_Default)
    {
        if (m_objectStatus.m_statusKind == ObjectStatusBlob::HumanObject)
        {
            g_pNewHumanImageLib->CalculateBounds(&m_objectStatus,
                                                 2,
                                                 0,
                                                 0,
                                                 &imageBounds,
                                                 0,
                                                 0,
                                                 1,
                                                 0);
            g_pNewHumanImageLib->ComputeOverlayBounds(&m_objectStatus,
                                                      2,
                                                      0,
                                                      0,
                                                      &overlayBounds,
                                                      0,
                                                      1);

            destinationBounds.bottom = 171;
            destinationBounds.top =
                171 - (imageBounds.bottom - imageBounds.top);
            destinationBounds.left = 80;
            destinationBounds.right =
                80 + (imageBounds.right - imageBounds.left);
            OffsetRect(&destinationBounds, overlayBounds.left, 0);
            destinationBounds.left +=
                imageBounds.left - overlayBounds.left;
            destinationBounds.right +=
                imageBounds.right - overlayBounds.right;

            g_pNewHumanImageLib->Draw(this,
                                      &destinationBounds,
                                      &m_objectStatus,
                                      2,
                                      0,
                                      0,
                                      0,
                                      0,
                                      0,
                                      0.0f,
                                      0,
                                      1,
                                      0);
        }
        else if (m_objectStatus.m_statusKind ==
                 ObjectStatusBlob::MonsterObject)
        {
            g_pMonsterImageLib->ComputeRenderBounds(&m_objectStatus,
                                                    7,
                                                    0,
                                                    &imageBounds,
                                                    NULL);

            const int width = imageBounds.right - imageBounds.left;
            const int halfWidthOffset = width / -2;
            destinationBounds.left = 93 + halfWidthOffset;
            destinationBounds.right = 117 + width + halfWidthOffset;
            destinationBounds.top = imageBounds.top + 140;
            destinationBounds.bottom = imageBounds.bottom + 140;

            g_pMonsterImageLib->RenderMonsterImage(this,
                                                   &destinationBounds,
                                                   &m_objectStatus,
                                                   7,
                                                   0,
                                                   0,
                                                   0,
                                                   NULL,
                                                   0.0f);
        }

        if (m_headId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_headId, &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 44 + halfHeight;
            destinationBounds.bottom =
                44 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_headId),
                                              m_headPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_leftAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_leftAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 153 + halfWidth;
            destinationBounds.right =
                153 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 139 + halfHeight;
            destinationBounds.bottom =
                139 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_leftAccessoryId),
                m_leftAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_rightAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_rightAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 31 + halfWidth;
            destinationBounds.right =
                31 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 139 + halfHeight;
            destinationBounds.bottom =
                139 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_rightAccessoryId),
                m_rightAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_leftSubAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_leftSubAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 31 + halfWidth;
            destinationBounds.right =
                31 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 202 + halfHeight;
            destinationBounds.bottom =
                202 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_leftSubAccessoryId),
                m_leftSubAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_rightSubAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_rightSubAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 84 + halfWidth;
            destinationBounds.right =
                84 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 202 + halfHeight;
            destinationBounds.bottom =
                202 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_rightSubAccessoryId),
                m_rightSubAccessoryPalette,
                NULL,
                0.0f);
        }

        g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                     5 * m_nextButtonFrame,
                                     &tileContext);
        GetButtonRect(ButtonId_Next, &buttonBounds);
        RenderTileFrame(&tileContext,
                        &tileContext.bounds,
                        &buttonBounds,
                        0,
                        L"BUTTON.PAL",
                        NULL);

        g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                     5 * m_previousButtonFrame + 1,
                                     &tileContext);
        GetButtonRect(ButtonId_Previous, &buttonBounds);
        RenderTileFrame(&tileContext,
                        &tileContext.bounds,
                        &buttonBounds,
                        0,
                        L"BUTTON.PAL",
                        NULL);

        GetButtonRect(ButtonId_Look, &buttonBounds);
        if (m_lookToggleState == ButtonVisualState_Disabled)
        {
            SetDrawColor(143);
            m_drawMode = 0;
            g_pfnFillRect(this, &buttonBounds);
        }
        else
        {
            g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                         m_lookToggleState ? 7 : 2,
                                         &tileContext);
            RenderTileFrame(&tileContext,
                            &tileContext.bounds,
                            &buttonBounds,
                            0,
                            L"BUTTON.PAL",
                            NULL);
        }

        GetButtonRect(ButtonId_Equipment, &buttonBounds);
        if (m_equipmentToggleState == ButtonVisualState_Disabled)
        {
            SetDrawColor(143);
            m_drawMode = 0;
            g_pfnFillRect(this, &buttonBounds);
        }
        else
        {
            g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                         m_equipmentToggleState ? 8 : 3,
                                         &tileContext);
            RenderTileFrame(&tileContext,
                            &tileContext.bounds,
                            &buttonBounds,
                            0,
                            L"BUTTON.PAL",
                            NULL);
        }

        GetButtonRect(ButtonId_Stats, &buttonBounds);
        if (m_statsToggleState == ButtonVisualState_Disabled)
        {
            SetDrawColor(143);
            m_drawMode = 0;
            g_pfnFillRect(this, &buttonBounds);
            return;
        }

        g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                     m_statsToggleState ? 9 : 4,
                                     &tileContext);
        return;
    }

    if (m_viewMode == ViewMode_Spelled)
    {
        SetTextColor(128);
        m_textDrawMode = 1;

        wcscpy_s(m_identityLine1,
                 256,
                 L"\ub3cc\uc544\uc628\ud6c4\ub808\uc2dc\ub9e8\ub274");
        MoveTo(59, 28);
        SetTextColor(128);
        DrawWideText(m_identityLine1,
                     static_cast<int>(wcslen(m_identityLine1)));
        MoveTo(58, 27);
        SetTextColor(37);
        DrawWideText(m_identityLine1,
                     static_cast<int>(wcslen(m_identityLine1)));
        MoveTo(57, 27);
        DrawWideText(m_identityLine1,
                     static_cast<int>(wcslen(m_identityLine1)));

        wcscpy_s(m_identityLine2,
                 256,
                 L"\ud3c9\ud654\uc758\uc218\ud638\uc790");
        MoveTo(59, 48);
        SetTextColor(128);
        DrawWideText(m_identityLine2,
                     static_cast<int>(wcslen(m_identityLine2)));
        MoveTo(58, 47);
        SetTextColor(37);
        DrawWideText(m_identityLine2,
                     static_cast<int>(wcslen(m_identityLine2)));
        MoveTo(57, 47);
        DrawWideText(m_identityLine2,
                     static_cast<int>(wcslen(m_identityLine2)));

        InitRectBounds(&statBounds, 26, 58, 56, 70);
        if (m_exposedRegion.IntersectsRect(&statBounds))
        {
            swprintf_s(statText,
                       128,
                       L"%u",
                       static_cast<unsigned int>(
                           g_activeUserStatusPane->GetPercentStatA()));
            OffsetRect(&statBounds,
                       (statBounds.right -
                        static_cast<int>(10 * wcslen(statText)) -
                        statBounds.left) /
                           2,
                       0);
            DrawStatText(statText, statBounds.left, statBounds.top, true);
        }

        InitRectBounds(&statBounds, 83, 58, 113, 70);
        if (m_exposedRegion.IntersectsRect(&statBounds))
        {
            swprintf_s(statText,
                       128,
                       L"%u",
                       static_cast<unsigned int>(
                           g_activeUserStatusPane->GetPercentStatB()));
            OffsetRect(&statBounds,
                       (statBounds.right -
                        static_cast<int>(10 * wcslen(statText)) -
                        statBounds.left) /
                           2,
                       0);
            DrawStatText(statText, statBounds.left, statBounds.top, true);
        }

        InitRectBounds(&statBounds, 140, 58, 170, 70);
        if (m_exposedRegion.IntersectsRect(&statBounds))
        {
            swprintf_s(statText,
                       128,
                       L"%u",
                       static_cast<unsigned int>(
                           g_activeUserStatusPane->GetPercentStatC()));
            OffsetRect(&statBounds,
                       (statBounds.right -
                        static_cast<int>(10 * wcslen(statText)) -
                        statBounds.left) /
                           2,
                       0);
            DrawStatText(statText, statBounds.left, statBounds.top, true);
        }
    }
    else if (m_viewMode != ViewMode_Legend)
    {
        return;
    }

    g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                 5 * m_nextButtonFrame,
                                 &tileContext);
    GetButtonRect(ButtonId_Next, &buttonBounds);
    RenderTileFrame(&tileContext,
                    &tileContext.bounds,
                    &buttonBounds,
                    0,
                    L"BUTTON.PAL",
                    NULL);

    g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                 5 * m_previousButtonFrame + 1,
                                 &tileContext);
    GetButtonRect(ButtonId_Previous, &buttonBounds);
    RenderTileFrame(&tileContext,
                    &tileContext.bounds,
                    &buttonBounds,
                    0,
                    L"BUTTON.PAL",
                    NULL);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact primary +0x44 virtual void OnPaint: 3141-byte/hash/918-instruction/68-block source body with SELFLOOK background/palette, human/monster and five item layers, navigation/toggle states, exact Korean labels, independently clipped percent stats, preserved mode quirks, complete formal C++, and compiler-only cookie exclusions. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00570fc0-0x00571c05 - SelfLookPane2OnPaint

## Status

- Role: vtable-routed `SelfLookPane2` paint method.
- IDA status: modeled function `sub_570FC0`, size `0xc45`, ending at `0x00571c05`.
- Owner/emitter: [UID:0000CV][SelfLookPane2](by-class/SelfLookPane2.md), with source-file route through [UID:0000NL][SelfLookPane](by-file/SelfLookPane.md).
- Reconstruction state: source-ready complete formal C++; all resource, field, geometry, helper, ABI, ownership, and compiler-output blockers are resolved.

## Evidence

- Live IDA reports the paint method at `0x00570fc0-0x00571c05`.
- Vtable/data reference `0x0062473c` anchors this method as a `SelfLookPane2` virtual slot.
- The method references `SelfLookPane2` resource strings, draws stat text through [UID:0003RF][0x005726a0-0x00572771.SelfLookPane2DrawStatText](by-memory/0x005726a0-0x00572771.SelfLookPane2DrawStatText.md), and computes button rectangles through [UID:0003RG][0x00572780-0x00572885.SelfLookPane2GetButtonRect](by-memory/0x00572780-0x00572885.SelfLookPane2GetButtonRect.md).

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Complete behavior-identical source covers every observed branch, resource, field, helper, geometry, text/stat route, negative check, and compiler exclusion. |
| Confidence | 94 | Exact bytes/hash, full CFG/instruction/callee inventory, primary vtable route, current support APIs, and complete source-shaped behavior leave only lexical spelling uncertainty. |

## Complete Behavior And Source Evidence

- Exact range is `[0x00570fc0,0x00571c05)`, size `0xc45` / 3,141 bytes, SHA256 `CC267264CF264CC59CB83085165B0144DDD5780B2A0FA530B5687F437557C13E`. It contains 918 instructions, 68 basic blocks, cyclomatic complexity 29, and 28 distinct direct callees.
- Liveness is the primary SelfLookPane2 vtable cell `0x0062473c` at slot `+0x44`. There are zero direct code callers and no interior entry; virtual dispatch fully explains that negative result.
- Source ABI is `virtual void OnPaint()`. Only `this` enters in ECX, the function ends in plain `ret`, branch-dependent EAX residues are ignored, and sibling paint contracts reject the old bool/int interpretation.
- The background sequence initializes one `EPFTileContext`, obtains `L"SELFLOOK.PAL"`, selects `L"SELFLOOK.EPF"` frame 0/1/2 for view modes Default/Spelled/Legend, and calls the direct sprite blit callback. Unsupported modes intentionally pass the initialized but unpopulated context to the callback and return after that blit.
- Status kind 0 uses NewHuman bounds and overlay helpers with selector 2, baseline/y 171 and x 80, applies image/overlay deltas, and calls NewHuman draw with the observed zero/one option sequence. Status kind 1 uses monster selector 7, centers width over x 93..117, offsets y by 140, and calls the monster renderer. Other kinds skip the base body while retaining all optional equipment work.
- Five independent nonzero-id layers call `g_pItemObjImageLib->GetItemGlyphBounds`, center the full glyph, and call `DrawItemImage` through the same singleton with the paired palette byte: head `(92,44)`, left accessory `(153,139)`, right accessory `(31,139)`, left sub-accessory `(31,202)`, and right sub-accessory `(84,202)`. Zero ids skip only their own layer. The hidden receiver at all five call sites rejects the historical unqualified/free-helper form.
- Navigation uses `5*m_nextButtonFrame` for button Next/id 4 and `5*m_previousButtonFrame+1` for Previous/id 3. Both render `L"STATBUT.EPF"` through `L"BUTTON.PAL"` with mode 0 and null options; the old anonymous A/B names are superseded by the target's exact action-to-field mapping.
- In mode 0, disabled state `0xff` sets draw color 143, forces inherited `m_drawMode` to zero, and fills the button rectangle. Enabled Look uses frame 7/2 and Equipment uses 8/3. Enabled Stats performs frame 9/4 lookup but deliberately does not call `RenderTileFrame`; this observed tail is preserved rather than normalized.
- In mode 1, text color starts at 128 and `m_textDrawMode` becomes 1. The two exact UTF-16 labels are `L"\ub3cc\uc544\uc628\ud6c4\ub808\uc2dc\ub9e8\ub274"` and `L"\ud3c9\ud654\uc758\uc218\ud638\uc790"`; each is copied into its existing wide buffer and drawn three times at the exact shadow/highlight positions with colors 128/37/37.
- The three percent-stat rectangles are exactly `(26,58,56,70)`, `(83,58,113,70)`, and `(140,58,170,70)`. Each has an independent `m_exposedRegion.IntersectsRect` gate, calls the matching UserStatusPane getter, formats `L"%u"`, centers at ten pixels per digit, and calls `DrawStatText(..., true)`.
- Exact source literals remain use-site expressions: `L"SELFLOOK.EPF"`, `L"SELFLOOK.PAL"`, `L"STATBUT.EPF"`, `L"BUTTON.PAL"`, `L"%u"`, and both escaped labels. No synthetic resource global or duplicate string declaration is introduced.
- The complete callee set is EPFTileContext initialization; palette/layout lookup; direct sprite/fill callbacks; NewHuman, Monster, and Item image bounds/render helpers; `OffsetRect`; `GetButtonRect`; GrafPort frame/color/text operations; `InitRectBounds`; Region intersection; three UserStatusPane getters; secure unsigned formatting; `DrawStatText`; and compiler security-cookie checking. The cookie route, stack-slot reuse, vtable/RTTI data, padding, and helper switch table are compiler-generated rather than handwritten source.
- No source null guard, invalidation call, explicit EPFTileContext release, EH cleanup, mode-3 paint branch, third stats-button render, target split, duplicate packet body, or manual ABI/table source is supported.
- Owner/emitter remain UID0000CV SelfLookPane2 through UID0000NL `NexusTK/ui/panels/SelfLookPane.cpp`; the broad UID0001HK parent remains non-emitting and exact children alone provide source.

## Cross-References

- [UID:0001HK][0x0056fe80-0x005729c3.SelfLookPane2](by-memory/0x0056fe80-0x005729c3.SelfLookPane2.md)
- [UID:0003RF][0x005726a0-0x00572771.SelfLookPane2DrawStatText](by-memory/0x005726a0-0x00572771.SelfLookPane2DrawStatText.md)
- [UID:0003RG][0x00572780-0x00572885.SelfLookPane2GetButtonRect](by-memory/0x00572780-0x00572885.SelfLookPane2GetButtonRect.md)
- [UID:0000CV][SelfLookPane2](by-class/SelfLookPane2.md)

## Changes

- 2026-07-16 Agent-B004 UID0003RD callback: preserved `93/94` and the complete accepted OnPaint body; changed only the four frame-field uses to `m_nextButtonFrame`/`m_previousButtonFrame` and qualified the five glyph-bound calls through `g_pItemObjImageLib`, matching the live hidden receiver and current ItemObjImageLib member contract. Every unrelated render branch, literal, score, field, and compiler exclusion remains unchanged.

- 2026-06-16 B001 SelfLookPane2 split execution: Created exact paint child from [UID:0001HK][0x0056fe80-0x005729c3.SelfLookPane2](by-memory/0x0056fe80-0x005729c3.SelfLookPane2.md) with live IDA function, vtable, resource, helper-call, and boundary evidence.
- 2026-07-14 Agent-B002 source-quality callback: raised `86/90 -> 93/94`, installed the complete accepted void OnPaint block, and preserved exact binary identity, CFG/vtable/callee evidence, all mode/body/item/button/text/stat behavior, negative evidence, source placement, rejected alternatives, and compiler exclusions.
