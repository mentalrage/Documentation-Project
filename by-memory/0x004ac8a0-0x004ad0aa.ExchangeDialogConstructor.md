*** UID:00049P | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ExchangeDialog::ExchangeDialog(const unsigned char *packet)
    : DialogPane(L"", 9, true)
{
    m_exchangeSessionId = PacketReadUInt32BE(packet + 2);

    unsigned int partnerNameLength = PacketReadByte(packet + 6);
    char partnerNameBytes[256] = {0};
    memcpy(partnerNameBytes, packet + 7, partnerNameLength);
    partnerNameBytes[partnerNameLength] = 0;

    wchar_t partnerName[64] = {0};
    int partnerNameChars = MultiByteToWideChar(CP_ACP, 0, partnerNameBytes,
                                               partnerNameLength,
                                               partnerName, 64);
    if (partnerNameChars < 0) {
        partnerNameChars = 0;
    }
    if (partnerNameChars >= 64) {
        partnerNameChars = 63;
    }
    partnerName[partnerNameChars] = 0;

    unsigned int partnerDisplayValue =
        PacketReadUInt16BE(packet + 7 + partnerNameLength);

    AddControl(new EPFImageControlPane(MakeRect(0, 0, 451, 371),
                                       0x14c,
                                       L"DLGEXC1.EPF",
                                       L"DLGEXC1.PAL"));
    AddImageButton(1, Rect(84, 335, 147, 359), 14);
    AddImageButton(2, Rect(188, 335, 251, 359), 36);
    AddImageButton(3, Rect(292, 335, 355, 359), 22);

    wchar_t localName[64] = {0};
    wchar_t localStatusLine[64] = {0};
    wchar_t localLabel[128] = {0};
    g_activeUserStatusPane->CopyNameLineA(localName, 64);
    g_activeUserStatusPane->CopyNameLineB(localStatusLine, 64);
    swprintf_s(localLabel, 128, L"%s(%s)", localName, localStatusLine);
    AddControl(new StaticTextControlPane(Rect(30, 60, 197, 72), localLabel));

    AddControl(5, new ExchangeItemListPane(), Rect(30, 110, 222, 283));

    ExchangeMoneyEditControlPane *moneyEdit =
        new ExchangeMoneyEditControlPane(Rect(73, 295, 197, 307), L"0");
    AddControl(6, moneyEdit);

    AddControl(new StaticTextControlPane(Rect(241, 60, 408, 72), partnerName));
    AddControl(8, new ExchangeItemListPane(), Rect(241, 110, 432, 283));
    AddControl(9, new StaticTextControlPane(Rect(284, 295, 408, 307), L"0"));

    wchar_t amountText[32];
    swprintf_s(amountText, 32, L"%d",
               (int)g_activeUserStatusPane->m_statusClassByte);
    AddControl(new StaticTextControlPane(Rect(82, 83, 210, 95), amountText));

    swprintf_s(amountText, 32, L"%d", partnerDisplayValue);
    AddControl(new StaticTextControlPane(Rect(295, 83, 423, 95), amountText));

    LoadDialogBackground(L"DLGEXC1.EPF");

    if (m_activeControlId == 6 && moneyEdit->m_editActive) {
        wchar_t localAmountText[10];
        moneyEdit->ReadText(localAmountText, 10);

        unsigned int amount = 0;
        if (_wtol(localAmountText) > 0) {
            amount = (unsigned int)_wtol(localAmountText);
        }

        if (amount == 0) {
            moneyEdit->SetText(L"0");
        }

        SubmitExchangeAmount(amount);
    }

    SetDialogMode(2);
    SetActiveControl(1);
    SetCurrentControl(3);

    m_localOfferPending = 0;
    m_localReadyConfirmed = 0;
    m_peerReadyConfirmed = 0;
    m_localExchangeAmount = 0;
    m_amountDecreasePending = 0;

    Rect bounds(0, 0, 451, 371);
    SetFrameRect(&bounds, 0, 0, g_pDefaultParent);
    OnShow(g_pScreenPane, 0);
    Show();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: First-draft emitting ExchangeDialog constructor; builds the DLGEXC1 exchange dialog from packet fields, local status pane labels, item-list and money controls, and initializes exchange state through the ExchangeDialog.cpp route. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ac8a0-0x004ad0aa ExchangeDialog Constructor

## Status

- Address range: `0x004ac8a0-0x004ad0aa` (`[start,end)`), IDA function start `0x004ac8a0`, size `0x80a`, body through `0x004ad0a9`.
- Direct owner: [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md).
- Source route: [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md) -> [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md).
- Rebuild handling: source-authored constructor body.
- Emission disposition: first-draft emitting through [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md) into [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md). Formal C++ is populated for `ExchangeDialog::ExchangeDialog(const unsigned char *packet)` after B005 MCP session `B005_00049P_20260629` resolved the stale no-code blocker.

## Behavior

The constructor builds the player-to-player exchange dialog from the incoming server packet. It loads the `DLGEXC1.EPF` and `DLGEXC1.PAL` resources, creates the fixed trade dialog, initializes the exchange id, partner/player display labels, local and remote item-list panes, money edit/display controls, ready/offer state fields, and vtable-backed child controls.

Packet parsing is source-ready at first-draft confidence:

- `m_exchangeSessionId` is read from packet `+2` as a 32-bit big-endian value.
- Partner name byte length is read from packet `+6`.
- Counted narrow partner-name bytes begin at packet `+7` and are converted with `MultiByteToWideChar(CP_ACP, 0, ...)` into a 64-wide-character buffer.
- Partner display amount/value is read from `packet + 7 + partnerNameLength` as a 16-bit big-endian value.

The local player label is not modeled as a magic wrapper. The constructor reads [UID:0000PS][g_activeUserStatusPane](by-global/g_activeUserStatusPane.md), calls the documented copy-out accessors `CopyNameLineA(localName, 64)` and `CopyNameLineB(localStatusLine, 64)`, and formats those two 64-wide-character fields with the raw-byte-proven `%s(%s)` literal. The local numeric label uses the temporary first-draft `m_statusClassByte` surface for `g_activeUserStatusPane + 0x280`; [UID:0001WF][UserStatusPaneSummaryFields](by-type/by-struct/UserStatusPaneSummaryFields.md) documents that byte as a layout-mode-dependent status/class byte, not proven gold/money.

## MCP Evidence

- B001 implementation MCP session `B001_00014K_20260628` reopened `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` through normal MCP `idb_open`; `server_health` reported status `ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- Current `entity_query functions 0x004ac8a0-0x004ae4c0` reports `sub_4AC8A0` at `0x004ac8a0`, size `0x80a`.
- The accepted report also preserved earlier MCP sessions `b009_0001Q4_20260628` and `880a9892`; both reported the same constructor function inventory before the implementation callback.
- Positive routed evidence from the report: constructor callers at `0x005086c0` and `0x00513a77`, `DLGEXC1.PAL` xref at `0x004ac9d2`, `DLGEXC1.EPF` xrefs at `0x004ac9e2` and `0x004acf8a`, and `ExchangeItemListPane` vtable stores around `0x004acbef` and `0x004acd9c`.
- B005 report MCP session `B005_00049P_20260629` reopened `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported status `ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and strings cache size `2067`.
- B005 `lookup_funcs`/`entity_query` confirms exact range `0x004ac8a0-0x004ad0aa`: `0x004ac8a0` is `sub_4AC8A0`, size `0x80a`, `0x004ad0a9` is inside this function, `0x004ad0aa` is not a function, and successor [UID:00049Q][0x004ad0b0-0x004ad1ca.ExchangeDialogOnButtonAction](by-memory/0x004ad0b0-0x004ad1ca.ExchangeDialogOnButtonAction.md) starts at `0x004ad0b0`.
- B005 caller evidence confirms code xrefs from `0x005086c0` in `sub_507C90` and `0x00513a77` in `sub_513A20`; both callers allocate `0x27c` bytes and pass the server packet pointer, proving this is live constructor code and not a retained duplicate.
- B005 vtable evidence confirms ExchangeDialog vtable stores `0x00619d2c`, `0x00619d8c`, and `0x00619dbc` at `0x004ac8f4`, `0x004ac8fa`, and `0x004ac904`; `ExchangeItemListPane` vtable stores `0x00619dc8`, `0x00619e50`, and `0x00619e80` at `0x004acbef`/`0x004acbf5`/`0x004acbff` and `0x004acd9c`/`0x004acda2`/`0x004acdac`; `ExchangeMoneyEditControlPane` vtable stores `0x0061a264`, `0x0061a2cc`, and `0x0061a2fc` at `0x004accd3`, `0x004accd9`, and `0x004acce3`.

## Resource Bytes And Controls

Raw bytes at `0x0061a304` decode as `DLGEXC1.PAL\0DLGEXC1.EPF\0%s(%s)\0\0`. IDA string typing truncates the first two UTF-16 strings, so the raw byte sequence is the authoritative proof. Constructor xrefs are:

- `0x0061a304` / `DLGEXC1.PAL`: xref `0x004ac9d2`.
- `0x0061a31c` / `DLGEXC1.EPF`: xrefs `0x004ac9e2` and `0x004acf8a`.
- `0x0061a334` / `%s(%s)`: xref `0x004acb40`.

Control construction matrix from B005 bounded disassembly:

| Control/order | Evidence | Source-facing disposition |
| --- | --- | --- |
| background | `DLGEXC1.EPF/PAL`, rect `(0,0,451,371)`, allocation `0x14c` | `EPFImageControlPane`, `LoadDialogBackground(L"DLGEXC1.EPF")` |
| control `1` | button rect `(84,335,147,359)`, resource id `14` | confirm/ready/OK button |
| control `2` | button rect `(188,335,251,359)`, resource id `36` | add-item button |
| control `3` | button rect `(292,335,355,359)`, resource id `22` | cancel button |
| control `4` | static local player label rect `(30,60,197,72)` | local label using `%s(%s)` |
| control `5` | first `ExchangeItemListPane`, wrapper rect `(30,110,222,283)` | local offered-item list |
| control `6` | `ExchangeMoneyEditControlPane`, rect `(73,295,197,307)`, initial text `L"0"` | local money edit |
| control `7` | static partner label rect `(241,60,408,72)` | remote player label |
| control `8` | second `ExchangeItemListPane`, wrapper rect `(241,110,432,283)` | remote offered-item list |
| control `9` | static text rect `(284,295,408,307)`, initial text `L"0"` | remote money display |
| amount labels | rects `(82,83,210,95)` and `(295,83,423,95)` | local and partner display amount labels |

## C++ / No-Code Disposition

Formal C++ now emits as `ExchangeDialog::ExchangeDialog(const unsigned char *packet)`. The old no-code blocker is superseded by B005 session `B005_00049P_20260629`, accepted sibling first-draft method bodies, and support docs for the resource strings, item-list pane, money edit pane, packet helpers, and exchange state fields. Combined score `88/89` clears the current reconstruction-code gate for a reconstructable page with nonblank `EMITTER_UIDS:00004R`.

Source-shape caveats:

- The formal body intentionally uses VC-era-compatible source shape: explicit fixed counts, `{0}` initialization, no `std::size`, no C++17 helpers, and no unexplained local wrapper helpers.
- `CopyNameLineA`, `CopyNameLineB`, `m_statusClassByte`, UI constructor overload names, `AddControl`/`AddImageButton`, `LoadDialogBackground`, `SetFrameRect`, `OnShow`, and `Show` are source-facing inferred names or overload surfaces, not original-symbol proof. Their binary data flow is documented above and in support pages; they cap confidence but no longer block emission.
- Partner name conversion is exact at behavior level but not at original source guard spelling. The binary proves counted narrow copy, `MultiByteToWideChar(CP_ACP, ...)` into a 64-wide-character buffer, and a NUL terminator with range-checked termination behavior. The formal body uses an explicit conservative clamp before the terminator rather than compiler/runtime-check scaffolding.
- Local label flow is documented directly through `g_activeUserStatusPane->CopyNameLineA/B` and `%s(%s)`. The local numeric display is documented as `m_statusClassByte` for `g_activeUserStatusPane + 0x280`, not a proven gold or money field.

Rejected alternatives:

- `CANONICAL_OWNER:NONE` is rejected because the receiver, vtables, fields, controls, and packet-fed construction are ExchangeDialog-specific.
- Direct file ownership by [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) is rejected because the file is the output route; the semantic owner is [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md).
- Resource docs, [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md), and [UID:00004T][ExchangeMoneyEditControlPane](by-class/ExchangeMoneyEditControlPane.md) are dependencies only, not owners of the outer constructor.
- Covered-by/no-code and blank-emitter disposition are rejected because no sibling emitted method covers background/control construction, packet partner setup, local status-pane labels, and initial state.

## Boundary Notes

- The constructor body is followed by `0x004ad0aa-0x004ad0b0` alignment before [UID:00049Q][0x004ad0b0-0x004ad1ca.ExchangeDialogOnButtonAction](by-memory/0x004ad0b0-0x004ad1ca.ExchangeDialogOnButtonAction.md).
- The next source-bearing method is the vtable-routed button action handler at `0x004ad0b0`.

## Cross-References

- [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)
- [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md)
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)
- [UID:0001RA][exchange-dialog-resources](by-resource/exchange-dialog-resources.md)

## Changes

- 2026-06-30 B005 accepted UID00049P implementation callback:
  - Before: `84/86`, `CANONICAL_OWNER:00004R`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank formal C++, and stale no-code prose saying constructor resource/control declarations were unresolved.
  - Changed to: `88/89`, `CANONICAL_OWNER:00004R`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:00004R`, and first-draft formal `ExchangeDialog::ExchangeDialog(const unsigned char *packet)` C++.
  - Summary/evidence: incorporated MCP session `B005_00049P_20260629`, exact range `0x004ac8a0-0x004ad0aa`, successor `0x004ad0b0`, live callers `0x005086c0` and `0x00513a77` that allocate `0x27c`, raw UTF-16 resource proof for `DLGEXC1.PAL`, `DLGEXC1.EPF`, and `%s(%s)`, vtable/control stores for ExchangeDialog, `ExchangeItemListPane`, and `ExchangeMoneyEditControlPane`, packet fields `+2`, `+6`, `+7`, and `packet+7+len`, controls `1/2/3/5/6/8/9`, state clears `+0x270/+0x271/+0x272/+0x274/+0x278`, and the constructor/setup call to [UID:00049V][0x004ad960-0x004ada16.ExchangeDialogSubmitExchangeAmount](by-memory/0x004ad960-0x004ada16.ExchangeDialogSubmitExchangeAmount.md) when local money edit control `6` is active. Source-facing names `CopyNameLineA/B`, `m_statusClassByte`, UI helper overloads, show/attach helpers, and the partner-name termination guard remain confidence caps, not emission blockers.
- 2026-06-28 B001 accepted split implementation:
  - Created as the exact constructor child required by [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)'s empty-emitter repair.
  - Metadata is `84/86`, owner [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, and blank formal C++ by accepted non-emitting source-route disposition.
