*** UID:000565 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:5839984 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// CPP@00591C70 | UID0000OJ accepted formal channel
bool TextEditPane::IsPasteUnavailable() const
{
    if (s_styledUnicodeClipboardFormat != 0 &&
        IsClipboardFormatAvailable(s_styledUnicodeClipboardFormat) > 0)
        return false;
    return IsClipboardFormatAvailable(CF_TEXT) <= 0;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact source-ready TextEditPane clipboard-availability query at [0x00591c70,0x00591ca1), routed through TextEditPane with its declaration owned once by the class page. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00591c70-0x00591ca1 TextEditPane IsPasteUnavailable

## Status And Source Placement

- Exact range: `[0x00591c70,0x00591ca1)`, 49 bytes.
- Entity: ordinary source-authored `TextEditPane` member definition.
- Canonical owner and emitter: [UID:0000EO][TextEditPane](by-class/TextEditPane.md); source module [UID:0000ON][TextEditPane](by-file/TextEditPane.md).
- Exact source order is after [UID:0004U1][0x00591a20-0x00591c65.GetScrapFromClipboard](by-memory/0x00591a20-0x00591c65.GetScrapFromClipboard.md) and before [UID:000566][0x00591cb0-0x00591d52.TextEditPaneDuplicateTextRange](by-memory/0x00591cb0-0x00591d52.TextEditPaneDuplicateTextRange.md).
- This exact page owns the sole CPP body. Its H channel stays blank because UID0000EO already owns `bool IsPasteUnavailable() const;` once in the complete class declaration.

## Exact Boundary And Liveness Evidence

- The accepted whole-file bounded inventory classifies this exact half-open range as raw source body R23: no separate IDA function object, zero physical start xrefs, no first inbound reference, and two outbound direct-call instructions.
- Predecessor padding `[0x00591c65,0x00591c70)` is eleven `0xcc` bytes; successor padding `[0x00591ca1,0x00591cb0)` is fifteen `0xcc` bytes. Both have zero start xrefs and remain compiler-covered outside this page.
- The two calls are the two ordered `IsClipboardFormatAvailable` tests. The first test is conditional on the registered styled-format identifier being nonzero; the second tests Win32 `CF_TEXT` only when the first route does not establish availability.
- Zero start xrefs do not make the bytes padding or a second source unit. The complete TextEditPane corridor, the ordinary class declaration, the two call sites inside the range, and the accepted R23 address merge retain it as one source member body.

## Behavior And Data Flow

- Read file-static `s_styledUnicodeClipboardFormat` without modifying it. When it is nonzero and Windows reports that format available, return false immediately.
- Otherwise query `CF_TEXT`; return false when that fallback format is available and true only when neither accepted format is available.
- The result is intentionally negative-polarity: true means paste is unavailable. Preserve the short-circuit ordering and do not add clipboard open/close, data allocation, payload reads, Unicode-only policy, or object-state mutation.

## Name, Signature, And Ownership Reasoning

- `IsPasteUnavailable` matches the exact negative return polarity and the existing class declaration; `IsPasteAvailable` would invert the observed result and is rejected.
- The body performs a query only and mutates neither TextEditPane nor file state, supporting the accepted trailing `const` member qualifier. A non-const overload would add an unsupported API surface.
- A file-local free helper was considered but rejected by the accepted TextEditPane family placement and declaration inventory. The narrow class remains semantic owner while this exact by-memory page owns only the range-limited definition.
- UID0000EO's CPP child marker emits this page through UID0000ON between `CPP@00591A20` and `CPP@00591CB0`; copying the body into the class page would recreate the duplicate-definition defect this split closes.

## Score Rationale And Historical Correction

- `94/94` reflects exact bounds, neighboring padding, call count, control flow, static/global usage, negative-polarity behavior, source-facing signature, owner/emitter route, source order, and complete formal C++.
- Confidence remains below 95 because stripped symbols cannot prove the original lexical spelling independently of the accepted family reconstruction.
- Historical UID0000OJ text that routed R23's definition directly through the class CPP block is superseded only for physical formal ownership: the body and behavior are unchanged, but UID000565 is now the sole exact CPP owner.

## Rebuild Policy

- Emit exactly the managed body through UID0000EO/UID0000ON at `CPP@00591C70`.
- Keep both adjacent padding spans compiler-covered, retain the declaration only in UID0000EO H, and do not emit raw addresses, xref commentary, or duplicate source.
