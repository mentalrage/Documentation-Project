*** UID:0001MM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
ThrowReallyInputPane::ThrowReallyInputPane(unsigned char itemSlot)
    : CharInputPane(g_pLanguageMan->GetLocalizedString(0xa5)),
      m_itemSlot(itemSlot)
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained ThrowReallyInputPane constructor body: localized prompt id 0xa5, CharInputPane base construction, item-slot argument stored at inferred m_itemSlot (this+0x108), vtable stores and inline construction mirrors confirmed; no raw-start function/xrefs/pointers, but formal first-draft constructor C++ is ready under current policy. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-8 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005b5b30-0x005b5b7e ThrowReallyInputPaneRawConstructor

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw/projected class constructor bytes, not an IDA function object
- Direct parent: [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md)
- Likely source module: [UID:0000KC][ItemActionInputPanes](by-file/ItemActionInputPanes.md)
- Rebuild handling: `source-authored` constructor body with compiler-generated vtable stores from source declarations.
- Source disposition: exact retained raw `ThrowReallyInputPane` constructor; formal first-draft C++ is populated in the header block, while the absent raw-start function/xref/pointer route remains a confidence cap.
- Confidence: strong for constructor-shaped behavior, prompt id, stored-slot field, vtable identity, inline construction mirrors, exact body/padding boundary, and first-draft constructor C++ readiness; still below final-source quality because IDA does not model `0x005b5b30` as a function, no direct code xref targets the raw start, no raw-start pointer route was found, and the original source field spelling for `this+0x108` is inferred as `m_itemSlot`.

## Address Range

| Range | Kind | Notes |
| --- | --- | --- |
| `0x005b5b2c-0x005b5b30` | predecessor padding | Four `0xcc` bytes between the preceding raw throw-packet helper and this constructor (`0x04` / 4 bytes, Verified with `int_convert.py`). |
| `0x005b5b30-0x005b5b7e` | raw constructor body | Exact constructor-shaped body, `0x4e` / 78 bytes (Verified with `int_convert.py`), ending at `retn 4`. |
| `0x005b5b7e-0x005b5b80` | successor padding | Two `0xcc` bytes (`0x02` / 2 bytes, Verified with `int_convert.py`) before `ThrowReallyInputPane::OnAccept` at `0x005b5b80`. |

The old page range ended at `0x005b5b80`, but the live 2026-06-13 byte audit shows the constructor body itself ends at `0x005b5b7e` and the final two bytes are padding. The filename was therefore renamed from `0x005b5b30-0x005b5b80.ThrowReallyInputPaneRawConstructor.md` to this exact half-open body range.

## IDA Evidence

Live IDA MCP refresh on 2026-06-13 in database `a002_batch_clear_timer`:

- `lookup_funcs` reports no function object at `0x005b5b20`, `0x005b5b30`, `0x005b5b5d`, or `0x005b5b7f`; it reports the next modeled function as `sub_5B5B80`, size `0xa0` / 160 bytes (Verified with `int_convert.py`).
- `get_bytes(0x005b5b20, 0x70)` shows the predecessor helper epilogue, four padding bytes, this constructor body, and the two successor padding bytes.
- `xrefs_to(0x005b5b30)` returns no direct refs, matching a raw constructor body that is mirrored by inline construction sites instead of called directly.

Live B006 IDA MCP refresh on 2026-06-26 used current database session `80de0a67` (`NexusTK.exe.i64`, `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`) and rechecked the prior blocker claims rather than carrying them forward as future work:

- `lookup_funcs` reports no function object at `0x005b5b2c`, `0x005b5b30`, `0x005b5b5d`, or `0x005b5b7e`; it reports the next modeled function as `sub_5B5B80`, size `0xa0` / 160 bytes (Verified with `int_convert.py`). The construction mirrors remain modeled as `sub_5A76C0`, size `0xbfd` / 3069 bytes (Verified with `int_convert.py`), with `0x005a7b70` inside that function, and `sub_5ABA80`, size `0x9a` / 154 bytes (Verified with `int_convert.py`).
- `get_bytes` confirms the exact split: four `0xcc` bytes at `0x005b5b2c-0x005b5b30`, the exact `0x4e` / 78-byte body at `0x005b5b30-0x005b5b7e` (Verified with `int_convert.py`), and two `0xcc` bytes at `0x005b5b7e-0x005b5b80` (Verified with `int_convert.py`).
- `make_signature_for_range 0x005b5b30-0x005b5b7e` returned `unique:true` for the full constructor body: `55 8B EC 51 56 8B F1 8B 0D 50 A7 67 00 68 A5 00 00 00 89 75 FC E8 06 A8 F3 FF 50 8B CE E8 4E CD F3 FF 8A 45 08 88 86 08 01 00 00 8B C6 C7 06 78 02 63 00 C7 86 A0 00 00 00 C8 02 63 00 C7 86 A4 00 00 00 F8 02 63 00 5E 8B E5 5D C2 04 00`.
- `xrefs_to 0x005b5b30` and `xrefs_to 0x005b5b7e` both return zero xrefs. `xrefs_to 0x005b5b80` returns the vtable data xref at `0x006302c0`, so the accept handler has a dispatch route while the retained constructor body does not.
- `find_bytes` finds no VA pointer hit for raw constructor start pattern `30 5B 5B 00`, no RVA pointer hit for `30 5B 1B 00`, and no pointer hit for boundary pattern `7E 5B 5B 00`. The accept-handler pointer `80 5B 5B 00` is present at `0x006302c0`. This preserves the no-route caveat, but it does not block constructor C++ under the current retained raw-constructor policy.

## Raw Instruction Evidence

| Address | Instruction | Evidence |
| --- | --- | --- |
| `0x005b5b30` | `push ebp` | Standard frame setup for the raw constructor body. |
| `0x005b5b31` | `mov ebp, esp` | Standard frame setup. |
| `0x005b5b33` | `push ecx` | Reserves local storage for `this`. |
| `0x005b5b34` | `push esi` | Saves `esi`; constructor uses it as the object pointer. |
| `0x005b5b35` | `mov esi, ecx` | Captures `this`. |
| `0x005b5b37` | `mov ecx, dword_67A750` | Loads [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md). |
| `0x005b5b3d` | `push 0xa5` | Localized confirmation prompt id `0xa5` / 165 (Verified with `int_convert.py`). |
| `0x005b5b42` | `mov [ebp-4], esi` | Stores `this` in the local slot. |
| `0x005b5b45` | `call sub_4F0350` | Resolves localized prompt text. |
| `0x005b5b4a` | `push eax` | Passes the prompt to the base constructor. |
| `0x005b5b4b` | `mov ecx, esi` | Sets `this` for the base call. |
| `0x005b5b4d` | `call sub_4F28A0` | Calls [UID:00001P][CharInputPane](by-class/CharInputPane.md) constructor. |
| `0x005b5b52` | `mov al, [ebp+8]` | Reads the incoming inventory slot argument. |
| `0x005b5b55` | `mov [esi+0x108], al` | Stores the slot byte at `this + 0x108`; `0x108` / 264 bytes (Verified with `int_convert.py`). |
| `0x005b5b5b` | `mov eax, esi` | Returns the constructed object pointer. |
| `0x005b5b5d` | `mov [esi], 0x00630278` | Installs primary `ThrowReallyInputPane` vtable. |
| `0x005b5b63` | `mov [esi+0xa0], 0x006302c8` | Installs secondary vtable; `0xa0` / 160 bytes (Verified with `int_convert.py`). |
| `0x005b5b6d` | `mov [esi+0xa4], 0x006302f8` | Installs tertiary vtable; `0xa4` / 164 bytes (Verified with `int_convert.py`). |
| `0x005b5b77` | `pop esi` | Restores saved register. |
| `0x005b5b78` | `mov esp, ebp` | Tears down the frame. |
| `0x005b5b7a` | `pop ebp` | Restores caller frame. |
| `0x005b5b7b` | `retn 4` | Returns after consuming the stack argument. |

## Xrefs And Inline Construction

| Target | Xrefs | Notes |
| --- | --- | --- |
| `0x00630278` | `0x005a7b87`, `0x005abae5`, `0x005b5b5d` | Primary vtable stores appear in `UserPane` dispatcher, helper `sub_5ABA80`, and this raw body. |
| `0x006302c8` | `0x005a7b8d`, `0x005abaeb`, `0x005b5b63` | Secondary vtable mirrors the same construction sites. |
| `0x006302f8` | `0x005a7b97`, `0x005abaf5`, `0x005b5b6d` | Tertiary vtable mirrors the same construction sites. |
| `0x005b5b80` | `0x006302c0` | Primary vtable action slot points to the accept handler. |

IDA MCP `analyze_function(0x005aba80)` reports size `0x9a` / 154 bytes (Verified with `int_convert.py`). The helper narrows the source slot byte, allocates `0x10c` / 268 bytes (Verified with `int_convert.py`), resolves prompt id `0xa5` / 165 (Verified with `int_convert.py`), calls `sub_4F28A0`, stores the same three `ThrowReallyInputPane` vtables, and copies the slot byte to `+0x108`. This mirror explains the missing direct xref to the raw constructor start.

B006 current MCP `xrefs_to` and `find_bytes` rechecked the vtable and mirror route on 2026-06-26:

- `0x00630278` has xrefs at `0x005a7b87`, `0x005abae5`, and raw constructor store `0x005b5b5d`.
- `0x006302c8` has xrefs at `0x005a7b8d`, `0x005abaeb`, and raw constructor store `0x005b5b63`.
- `0x006302f8` has xrefs at `0x005a7b97`, `0x005abaf5`, and raw constructor store `0x005b5b6d`.
- Vtable pointer byte patterns occur only at the two construction mirrors and the target raw body: `78 02 63 00` at `0x005a7b89`, `0x005abae7`, `0x005b5b5f`; `C8 02 63 00` at `0x005a7b93`, `0x005abaf1`, `0x005b5b69`; `F8 02 63 00` at `0x005a7b9d`, `0x005abafb`, `0x005b5b73`.
- `insn_query 0x005a7b70-0x005a7ba5` inside `sub_5A76C0` repeats the source constructor sequence: load `g_pLanguageMan`, push prompt id `0xa5`, call [UID:00040P][0x004f0350-0x004f0372.LanguageManGetLocalizedString](by-memory/0x004f0350-0x004f0372.LanguageManGetLocalizedString.md), call [UID:00001P][CharInputPane](by-class/CharInputPane.md) construction through `0x004f28a0`, install the three `ThrowReallyInputPane` vtables, and store the slot byte at `this+0x108`.
- `insn_query 0x005abacd-0x005abb08` and `analyze_function 0x005aba80` show the same mirror: the helper narrows `Block+1`, allocates `0x10c` / 268 bytes (Verified with `int_convert.py`), resolves localized string id `0xa5` / 165 (Verified with `int_convert.py`), calls the `CharInputPane` base constructor, installs the three vtables, and writes the slot byte to offset `0x108` / 264 (Verified with `int_convert.py`).

## Accept Handler Tie-In

IDA MCP `analyze_function(0x005b5b80)` reports `sub_5B5B80` as `0xa0` / 160 bytes (Verified with `int_convert.py`). The handler reads one character, accepts only `y` (`0x79`) or `Y` (`0x59`), and sends a three-byte packet through [UID:0000Q5][g_packetSender](by-global/g_packetSender.md):

| Packet byte | Source |
| --- | --- |
| Opcode `0x17` | Throw operation. |
| Mode byte `1` | Confirmation/throw-really path, distinct from direct `ThrowInputPane` mode `0`. |
| Slot byte | Stored constructor byte at `this + 0x108`. |

The vtable slot and stored-field consumption tie this constructor directly to [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md), not merely to the broader throw/use/eat aggregate.

B006 current MCP `analyze_function 0x005b5b80` and `insn_query 0x005b5bc0-0x005b5c16` reconfirm the field-use chain: the handler accepts only `y` (`0x79` / 121, ASCII `y`, Verified with `int_convert.py`) or `Y` (`0x59` / 89, ASCII `Y`, Verified with `int_convert.py`), writes opcode `0x17` / 23 (Verified with `int_convert.py`), writes mode byte `1`, reads `byte ptr [esi+108h]`, passes explicit send length `3`, and writes a local trailing zero that is not part of the send. The constructor argument is therefore the confirmed throw item slot, source-facing inferred field `m_itemSlot` at `this+0x108`. The exact original member spelling remains inferred, but the offset, lifetime, and packet role are now resolved enough for first-draft constructor C++.

## Source-Quality And C++ Decision

The old blank-C++ rationale is superseded. This page now satisfies the current code-entry rule: it is reconstructable, has nonblank emitter route [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md), and its updated score average is `(88 + 90) / 2 = 89`, above the current threshold. The source shape is a retained `ThrowReallyInputPane::ThrowReallyInputPane(unsigned char itemSlot)` body, not an aggregate, helper, or compiler-only vtable artifact.

The formal C++ block intentionally emits only human source-level constructor semantics:

- `g_pLanguageMan->GetLocalizedString(0xa5)` uses the stabilized source-facing singleton and lookup helper; no resource enum constant is invented because the original constant name is not recovered.
- `CharInputPane(...)` is the base construction represented by the `0x004f28a0` call.
- `m_itemSlot(itemSlot)` is the best current source-facing field interpretation for the byte written at `this+0x108` and consumed by `ThrowReallyInputPane::OnAccept` plus the adjacent retained helper.
- The three vtable stores are compiler output from derived construction and are deliberately not emitted in source.
- The adjacent [UID:0003NY][0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw](by-memory/0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw.md) remains a separate class-owned retained helper and now emits formal first-draft `ThrowReallyInputPane::SendThrowConfirmedItemPacket()` C++ on its own exact page. Its no-xref/no-pointer/no-rel32 route evidence remains a liveness confidence cap; the constructor still must not call, inline, or absorb that packet helper without new route evidence.

Rejected alternatives preserved after B006 reanalysis:

- Do not route this constructor to `CharInputPane`; it is only the base constructor callee.
- Do not route it to `LanguageMan`; localization supplies only prompt text.
- Do not route it to `Socket`, [UID:0000Q5][g_packetSender](by-global/g_packetSender.md), or packet helpers; sending happens outside the constructor.
- Do not keep the formal C++ blank under the old 95+ gate; no-function/no-xref/no-pointer evidence is now a confidence cap under current item-action retained raw-constructor policy, not a no-code proof.
- Do not merge this target with `0x005b5b7e-0x005b5b80` padding, `0x005b5b80-0x005b5c20` accept handling, or `0x005b5c20-0x005b5c93` retained packet helper.

## Touched State

| State or dependency | Use |
| --- | --- |
| [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md) / `dword_67A750` | Supplies localized prompt id `0xa5`. |
| [UID:00001P][CharInputPane](by-class/CharInputPane.md) / `0x004f28a0` | Base one-character prompt construction. |
| `this + 0x108` | Stored selected inventory slot byte consumed by `0x005b5b80`. |
| [UID:0001XU][ItemActionInputPaneVtableFamily](by-type/by-vtable/ItemActionInputPaneVtableFamily.md) | Records the `ThrowReallyInputPane` vtable bases and action slot. |

## Reconstruction Notes

This range is reconstructable as `ThrowReallyInputPane::ThrowReallyInputPane(unsigned char itemSlot)` in the item-action input-pane source module, while preserving the IDA raw-boundary caveat. The live call paths currently favor inline construction rather than direct calls to the raw start, but that no-route state is only a callgraph/source-liveness caveat after the B006 source-quality reanalysis.

The direct parent remains [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md). After the paired parent repair, B006 update, and B007 helper sync, this child (`88/90`) and the direct parent (`87/89`) clear the active gate. Formal first-draft constructor C++ is now present because helper names, split boundaries, adjacent raw-helper separation, and source-facing field interpretation have been resolved enough for current policy. Completion stays at `88` because no direct raw-start function/xref/pointer route exists and because exact original source names for `m_itemSlot` and the prompt id constant remain inferred. Confidence stays at `90` because the bytes, range, vtables, mirrors, and field-use chain are independently confirmed.

## Score And Assignment Audit

- Before Goal 2 audit: `74/84`, range `0x005b5b30-0x005b5b80`, parent [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md).
- After Goal 2 audit: `86/90`, range `0x005b5b30-0x005b5b7e`, parent [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md) retained.
- After B006 source-quality implementation: `88/90`, range and parent retained, formal first-draft constructor C++ inserted.
- Justification: live IDA MCP reconfirmed exact raw bytes, no IDA function object, no raw-start xrefs, no raw-start pointer hits, constructor-body end before two padding bytes, vtable xrefs from two inline construction paths plus the raw body, prompt id/base constructor/slot store semantics, and the accept handler consuming the stored slot for opcode `0x17`, mode `1`, send length `3`, with the trailing zero local-only.
- Remaining cap: no direct IDA caller/function object/pointer to this raw constructor start, `m_itemSlot` is a source-facing inferred field spelling, and prompt id `0xa5` has no recovered source enum name. Neighboring raw helper bodies have exact child pages; [UID:0003NY][0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw](by-memory/0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw.md) remains separate/no-route but is no longer blank-C++; it emits retained helper C++ from its own exact page and must not be merged into this constructor.

## Cross-References

- [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md)
- [UID:0001ML][0x005b5890-0x005b60c0.ThrowUseEatInputPanes](by-memory/0x005b5890-0x005b60c0.ThrowUseEatInputPanes.md)
- [UID:0000KC][ItemActionInputPanes](by-file/ItemActionInputPanes.md)
- [UID:0001XU][ItemActionInputPaneVtableFamily](by-type/by-vtable/ItemActionInputPaneVtableFamily.md)
- [UID:0000RC][g_pLanguageMan](by-global/g_pLanguageMan.md)

## Changes

- 2026-06-02 IDA MCP refresh:
  - Before: scored `50/65`, reconstructable metadata was blank, and reachability was described only from prior raw-disassembly notes.
  - After: raised to `74/84`, marked reconstructable, attached to [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md), and added refreshed xref/touched-state evidence.
  - Evidence: IDA MCP confirms no direct function at `0x005b5b30`, vtable stores at `0x005b5b5d`, inline construction xrefs at `0x005a7b87` and `0x005abae5`, prompt id `0xa5`, base `CharInputPane` construction, and stored slot byte at `this + 0x108`.
- 2026-06-13 A003 Goal 2 audit:
  - Before: `74/84`, filename/range `0x005b5b30-0x005b5b80`.
  - After: `86/90`, filename/range corrected to `0x005b5b30-0x005b5b7e`, parent [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md) retained, C++ remains blank.
  - Evidence: live IDA MCP database `a002_batch_clear_timer` reconfirmed no IDA function at the raw start, no direct start xrefs, exact constructor bytes, the `retn 4` body end at `0x005b5b7e`, two `0xcc` padding bytes before `0x005b5b80`, vtable-store mirrors at `0x005a7b87` and `0x005abae5`, `sub_5ABA80` allocation/inline construction, and `sub_5B5B80` consuming `this+0x108` for opcode `0x17` mode `1`.
- 2026-06-26 B006 source-quality implementation:
  - Before: `86/90`, formal C++ blank under obsolete 95+ gate language.
  - After: `88/90`, formal first-draft constructor C++ inserted, owner/emitter [UID:0000EZ][ThrowReallyInputPane](by-class/ThrowReallyInputPane.md) retained, `Item Summary` updated, and no-route/no-pointer evidence preserved as a confidence cap.
  - Evidence: current IDA MCP session `80de0a67` reconfirmed no function at `0x005b5b30`, no xrefs to `0x005b5b30` or `0x005b5b7e`, no VA/RVA pointer hits for the raw constructor start, exact `0x4e` / 78-byte body, four predecessor and two successor `0xcc` padding bytes, unique byte signature, vtable stores `0x00630278`/`0x006302c8`/`0x006302f8`, inline construction mirrors at `0x005a7b70-0x005a7ba5` and `0x005abacd-0x005abb08`, prompt id `0xa5`, `CharInputPane` base construction, `m_itemSlot` inference at `this+0x108`, and accept-handler consumption for opcode `0x17`, mode `1`, send length `3`.
- 2026-06-28 B007 SendThrowConfirmedItemPacket retained-helper sync:
  - Score and constructor C++ unchanged at `88/90`.
  - Changed: removed stale statements that [UID:0003NY][0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw](by-memory/0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw.md) remains blank-C++. It remains separate and no-route, but now emits formal first-draft retained helper C++ from its own exact page.
  - Evidence: B007 current MCP/PE pass confirms [UID:0003NY][0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw](by-memory/0x005b5c20-0x005b5c93.SendThrowConfirmedItemPacketRaw.md) source-shaped helper bytes, opcode `0x17`, mode `1`, `m_itemSlot` at `this+0x108`, local-only terminator, send length `3`, exact end `0x005b5c93`, no direct xrefs/pointers/rel32 routes, and positive-control vtable route to `ThrowReallyInputPane::OnAccept`. The constructor/helper separation is preserved: this constructor initializes `m_itemSlot`; `OnAccept` and the retained helper consume it for confirmed-throw packet emission.
