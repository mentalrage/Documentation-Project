*** UID:0001HH | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00002Y | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00002Y | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
CollectionPane2::~CollectionPane2()
{
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Non-deleting `CollectionPane2::~CollectionPane2()` body: restores compiler-generated `CollectionPane2` vtable views at `+0`, `+0xa0`, and `+0xa4`, then tail-jumps to `PanelPane::~PanelPane()`; source C++ is an empty destructor while scalar/delete glue remains in [UID:00038Y][0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor](by-memory/0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0056fda0-0x0056fdbf CollectionPane2 Destructor

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: non-deleting destructor
- Owner class: [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md)
- Likely source module: [UID:0000IC][CollectionPane](by-file/CollectionPane.md)
- Confidence: very strong

## Behavior

This is the ordinary non-deleting `CollectionPane2::~CollectionPane2()` body. The binary restores the three `CollectionPane2` vtable views at object offsets `+0`, `+0xa0`, and `+0xa4`, then tail-jumps to the `PanelPane` destructor path at `0x005450d0`. It does not delete object storage, branch on deleting flags, call `operator delete`, release member children, touch globals, load resources, or contain any source-authored cleanup statements beyond the implicit base destructor chain.

## IDA MCP Evidence

Checked on 2026-05-25:

- `lookup_funcs 0x0056fda0` reports `sub_56FDA0` with size `0x1f`, giving end-exclusive range `0x0056fdbf`.
- Decompilation shows vtable writes to offsets `+0x00`, `+0xa0`, and `+0xa4`, followed by a base cleanup call.
- `xrefs_to 0x0056fda0` currently reports no direct xrefs in the IDA database.
- 2026-06-01 live IDA MCP recheck again reports `sub_56FDA0` size `0x1f`; the prior decompilation evidence shows the base cleanup call to `sub_5450D0(this)`.
- 2026-06-04 live IDA MCP `py_eval` against `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` confirms `sub_56FDA0` as `0x0056fda0-0x0056fdbf`; decompilation restores the three `CollectionPane2` vtable views and calls `sub_5450D0(this)`.
- 2026-06-04 vtable-slot xrefs show the same vtable constants are written by the raw constructor (`0x0056fd7f`, `0x0056fd87`, `0x0056fd91`), this non-deleting destructor (`0x0056fda0`, `0x0056fda6`, `0x0056fdb0`), and scalar deleting destructor (`0x00573316`, `0x0057331c`, `0x00573326`).
- 2026-06-15 live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirmed `sub_56FDA0` size `0x1f`; `analyze_function include_asm=true` shows the three vtable stores at `this+0x00`, `this+0xa0`, and `this+0xa4`, then a tail jump to `sub_5450D0`. Xrefs include the `CollectionPane2` primary vtable data route at `0x0062466c`.
- [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md) is now `87/90` and documents this destructor as part of the same constructor/destructor/scalar-deleting-destructor vtable lifecycle under [UID:0000IC][CollectionPane](by-file/CollectionPane.md).
- 2026-06-27 B003 accepted MCP pass used active session `b001_000241_20260627` on `NexusTK.exe` with server health `status:ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- `lookup_funcs` in that session reported `0x0056fda0 -> sub_56FDA0`, size `0x1f` / 31 decimal (Verified with MCP `int_convert`); `0x0056fdbf` is not a function and is the successor alignment byte; `0x0056fdc0 -> sub_56FDC0`; `0x005450d0 -> sub_5450D0`, the `PanelPane` destructor path; and `0x00573310 -> sub_573310`, the `CollectionPane2` scalar deleting destructor wrapper.
- Target bytes from `0x0056fda0-0x0056fdbf` are `C7 01 6C 46 62 00 C7 81 A0 00 00 00 BC 46 62 00 C7 81 A4 00 00 00 EC 46 62 00 E9 11 53 FD FF`; the next byte at `0x0056fdbf` is `0xcc` alignment. `make_signature_for_range` over the target returned the same no-wildcard sequence with `unique:true`.
- `insn_query` decoded exactly four target instructions: store `??_7CollectionPane2@@6B@` to `[ecx]` at `0x0056fda0`, store `??_7CollectionPane2@@6B@_0` to `[ecx+0xa0]` at `0x0056fda6`, store `??_7CollectionPane2@@6B@_1` to `[ecx+0xa4]` at `0x0056fdb0`, and `jmp sub_5450D0` at `0x0056fdba`; `0x0056fdbf` is `align 10h`.
- `analyze_function` / `decompile` returned `void __thiscall(_DWORD *this)` with only the three `CollectionPane2` vtable stores followed by `sub_5450D0(this)`. The function has no strings, constants, source member cleanup, ordinary direct callers, or extra callees beyond the tail-jumped base cleanup route; basic blocks are `2` and cyclomatic complexity is `1`.
- `xrefs_to 0x0056fda0` and the matching `xref_query` reported zero direct xrefs to the ordinary destructor entry. This is negative evidence for ordinary call dispatch, not a no-code proof, because the destructor lifecycle is proven by vtable stores and scalar-wrapper data.
- Vtable lifecycle xrefs tie the same three table bases to constructor, ordinary destructor, and scalar wrapper: `0x0062466c` refs from constructor `0x0056fd7f`, this destructor `0x0056fda0`, and scalar wrapper `0x00573316`; `0x006246bc` refs from constructor `0x0056fd87`, this destructor `0x0056fda6`, and scalar wrapper `0x0057331c`; `0x006246ec` refs from constructor `0x0056fd91`, this destructor `0x0056fdb0`, and scalar wrapper `0x00573326`.
- Scalar-wrapper comparison at `0x00573310` shows the delete-flag logic is not part of this exact body: the wrapper is size `0x55` / 85 decimal (Verified with MCP `int_convert`), restores the same three `CollectionPane2` vtable views, calls `sub_5450D0`, conditionally calls `sub_4F4AC0` when deleting flag bit `1` is set and bit `4` is clear, and is reached through secondary/tertiary adjustor thunks plus the primary vtable slot.
- `PanelPane` base route comparison at `0x005450d0` shows a `0x1f` / 31 decimal body that restores the three `PanelPane` vtable views and tail-jumps to `sub_544580`, the `Pane` teardown path. That base cleanup is implicit C++ destructor lowering and should not be handwritten inside `CollectionPane2::~CollectionPane2()`.
- Historical pre-Gate2B observations above retain the then-current `sub_56FDA0` and `sub_5450D0` names as dated evidence. Supervisor catalog entry `0349` subsequently applied and read back `CollectionPane2__Destructor` over unchanged range `[0x0056FDA0,0x0056FDBF)` with declaration `void __thiscall CollectionPane2__Destructor(CollectionPane2 *this)`. Its exact function-regular comment is `Out-of-line CollectionPane2 destructor body. Compiler vptr restoration is followed by implicit PanelPane teardown; the human source destructor body is empty.` Function-repeatable, entry-regular, and entry-repeatable comments remain blank.
- The same catalog entry applied and read back the base identity `PanelPane__Destructor` over unchanged range `[0x005450D0,0x005450EF)` with declaration `void __thiscall PanelPane__Destructor(PanelPane *this)` and function-regular comment `PanelPane ordinary destructor body: restores the three PanelPane vptr views and tail-jumps to Pane teardown.` Its other three comment channels remain blank.
- Gate 2B preserved all bytes, boundaries, xrefs, vtable identities, dependency types, and unrelated entities. The saved IDB checkpoint is SHA256 `B46BEB1B347B569129E0B9F9BB53C51E4560EF6249081256D31EAC53A95F1A41`, size `143190134` bytes, last write `2026-07-29T02:03:25.4572488-04:00`. These analysis names/comments do not change the empty human-source destructor body below.

## Source C++ Disposition

Emit this exact target as the ordinary source destructor:

- The range is a single modeled non-deleting function with no mixed children, no scalar-delete flag branch, no storage free, and no source-authored member cleanup.
- The body is source-equivalent to an empty `CollectionPane2::~CollectionPane2()`; the vptr restores and base destructor tail jump are compiler lowering from the class destructor and inheritance layout.
- The sibling `+0x04` and `+0x08` virtual slots are now resolved on their own pages as pointer/mouse and key/text EventHandler overrides. They are not referenced by this destructor body and do not alter its exact empty formal C++.
- [UID:00038Y][0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor](by-memory/0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor.md) remains the compiler scalar deleting wrapper and must not emit handwritten scalar deleting pseudocode.

## Scalar-Wrapper Source Cause

- The formal empty destructor on this page and the explicit `virtual ~CollectionPane2();` declaration on [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md) are the complete human-source cause of [UID:00038Y][0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor](by-memory/0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor.md).
- The compiler lowers that source into the three vptr restores, implicit `PanelPane` teardown, primary scalar-deleting wrapper, `+0xA0`/`+0xA4` facet adjustors, and decorated vtable entries. None of those ABI effects belongs in this formal CPP body.
- Exact size evidence supports an empty derived body: `CollectionPane2` deletes as `0xF8`, equal to `PanelPane`, and this ordinary destructor performs no derived-member cleanup before the base tail jump.
- The neighboring `CollectionPane` scalar wrapper contains visible owned-child cleanup at `+0x108`; its absence from this class is negative evidence against a hidden member-destructor statement.
- UID00038Y is therefore semantic-owner-only, `RECONSTRUCTABLE:FALSE`, and non-emitting. The earlier class-emitter route on that wrapper is superseded, not an omission from this destructor.

## Boundary Notes

Keep this non-deleting destructor body with `CollectionPane2` rather than treating it as unrelated base cleanup. The direct owner/emitter route remains [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md) through [UID:0000IC][CollectionPane](by-file/CollectionPane.md). The scalar deleting wrapper at `0x00573310` owns optional object release, delete-flag handling, and adjustor-thunk dispatch; `PanelPane` owns the base destructor callee reached by the tail jump.

## Rejected Alternatives

- Keeping formal C++ blank because surrounding field/interface names remain provisional is stale for this exact target. The destructor contains no explicit field, interface, resource, or helper references that require those sibling names.
- Keeping a no-code marker because the ordinary destructor has zero direct xrefs is rejected. The no-direct-xref fact is documented, but vtable lifecycle and scalar-wrapper comparison prove this is the ordinary class destructor body.
- Handwriting the three vptr assignments is rejected because they are compiler-generated destructor lowering, not source-authored statements.
- Handwriting an explicit `PanelPane::~PanelPane()` call is rejected because base teardown is implicit C++ destructor lowering and an explicit base call would be source-inaccurate.
- Emitting scalar deleting destructor pseudocode here or in [UID:00038Y][0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor](by-memory/0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor.md) is rejected. Delete flags, optional `sub_4F4AC0`, guard/runtime checks, and adjustor thunk dispatch are compiler ABI glue.
- Moving ownership to `PanelPane`, main `CollectionPane`, or `SelfLookPane2` is rejected. This body restores `CollectionPane2` vtables, shares lifecycle refs with the `CollectionPane2` constructor and scalar wrapper, and ends before the `SelfLookPane2` successor boundary.
- Treating the old generated `class_CollectionPane2.cpp` omission as authority is rejected. Current by-* docs, generated coverage, and the live MCP session supersede stale generated-output gaps.

## Score Rationale

| Metric | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | Exact boundary, one-byte successor alignment, unique byte signature, four-instruction body, three-view vtable restore, `PanelPane` base cleanup chain, zero ordinary-entry xrefs, constructor/destructor/scalar-wrapper lifecycle refs, scalar-wrapper separation, owner/emitter route, and formal empty destructor source are now documented. It remains below final-audit range because original source spelling/declaration proof and sibling `+0x04`/`+0x08` virtual names are not final. |
| Confidence | 92 | Very strong from live IDA MCP session `b001_000241_20260627`, byte/signature evidence, disassembly/decompilation agreement, vtable xrefs shared with the source-ready constructor and scalar wrapper, and accepted destructor-source policy. Confidence is not higher because there is no original source/PDB proof and no direct ordinary-entry callers. |

## Cross-References

- [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md)
- [UID:0000IC][CollectionPane](by-file/CollectionPane.md)
- [UID:0001HD][0x0056e940-0x0056fe75.CollectionPane](by-memory/0x0056e940-0x0056fe75.CollectionPane.md)

## Changes

- 2026-07-29 B003 UID00038Y implementation callback:
  - Score remains `90/92`; formal empty destructor source is unchanged.
  - Added the explicit class-declaration/ordinary-destructor source-cause relationship for non-emitting scalar wrapper UID00038Y, exact `0xF8` no-new-fields evidence, and neighboring-wrapper negative comparison.
  - Confirmed that compiler vptr stores, base teardown, delete flags, adjustors, and vtable data remain excluded from the human CPP body.
  - Post-Gate2B reconciliation: catalog entry `0349` applied and verified the exact derived/base destructor analysis names, declarations, and regular comments while preserving this page's formal empty destructor source.

- 2026-06-01: Raised from `70/85` to `74/88`, marked reconstructable, and attached to [UID:0000IC][CollectionPane](by-file/CollectionPane.md).
- Superseded 2026-06-27: the old blank-C++ rationale was too broad for this exact target. Provisional sibling field/interface names remain support caveats, but this destructor body has no explicit source statements and is safe to emit as an empty `CollectionPane2::~CollectionPane2()`.
- 2026-06-04: Raised from `74/88` to `78/90`.
  - What existed before: the page had the exact boundary and destructor behavior but only summarized the vtable restoration evidence.
  - Changed to: added current IDA binary identity, live decompilation evidence, and xrefs showing the same three vtable views shared by the raw constructor, non-deleting destructor, and scalar deleting destructor.
  - Historical reason for score increase: the destructor was strongly tied to the class vtable lifecycle and scalar wrapper, but at that time completion remained below C++-ready because final field/interface names were still treated as provisional. Superseded by the 2026-06-27 B003 callback for this exact destructor body.
- 2026-06-15 A002 Goal 2 candidate/encoder/collection memory batch:
  - Raised from `78/90` to `85/91`.
  - Routing unchanged: owner/emitter remain [UID:0000IC][CollectionPane](by-file/CollectionPane.md), through [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md).
  - Evidence: live IDA MCP reconfirmed the `0x1f`-byte destructor, three `CollectionPane2` vtable stores at offsets `0x00`, `0xa0`, and `0xa4`, tail jump to `sub_5450D0`, primary vtable data xref at `0x0062466c`, and parent class support now clears the strict route gate.
- 2026-06-17 B001 report execution:
  - Raised from `85/91` to `86/91`.
  - Rerouted owner/emitter from file [UID:0000IC][CollectionPane](by-file/CollectionPane.md) to direct class [UID:00002Y][CollectionPane2](by-class/CollectionPane2.md); behavior remains the ordinary/non-deleting destructor body, with scalar deleting storage release represented by [UID:00038Y][0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor](by-memory/0x00573310-0x00573365.CollectionPane2ScalarDeletingDestructor.md).
- 2026-06-27 B003 implementation callback:
  - Raised from `86/91` to `90/92`.
  - Inserted formal first-draft `CollectionPane2::~CollectionPane2()` source as an empty ordinary destructor.
  - Evidence: accepted MCP session `b001_000241_20260627` confirmed the exact `0x1f` / 31-byte modeled body, unique byte signature, `0xcc` successor alignment, three compiler-generated `CollectionPane2` vptr restores at `+0`, `+0xa0`, and `+0xa4`, zero direct xrefs to the ordinary entry, lifecycle refs shared with constructor and scalar wrapper, and tail jump to `PanelPane::~PanelPane()`.
  - Source policy: vptr stores, implicit base teardown, scalar deleting wrapper flags/free logic, and adjustor-thunk dispatch are compiler-generated effects, not handwritten source statements.
