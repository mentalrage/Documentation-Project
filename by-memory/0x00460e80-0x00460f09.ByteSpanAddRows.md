*** UID:00020D | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __cdecl ByteSpanAdd8Blocks(
    const unsigned char* sourceBytes,
    int sourcePitchBytes,
    unsigned char* destinationBytes,
    int destinationPitchBytes,
    int blockCount8Bytes,
    int rowCount);

void __cdecl ByteSpanAddRows(
    const unsigned char* sourceBytes,
    int sourcePitchBytes,
    unsigned char* destinationBytes,
    int destinationPitchBytes,
    int widthBytes,
    int rowCount)
{
    const int blockCount8Bytes = widthBytes / 8;
    const int tailBytes = widthBytes - blockCount8Bytes * 8;

    if (blockCount8Bytes > 0) {
        ByteSpanAdd8Blocks(
            sourceBytes,
            sourcePitchBytes,
            destinationBytes,
            destinationPitchBytes,
            blockCount8Bytes,
            rowCount);

        sourceBytes += blockCount8Bytes * 8;
        destinationBytes += blockCount8Bytes * 8;
    }

    while (rowCount-- > 0) {
        for (int byteIndex = 0; byteIndex < tailBytes; ++byteIndex) {
            destinationBytes[byteIndex] = static_cast<unsigned char>(
                destinationBytes[byteIndex] + sourceBytes[byteIndex]);
        }

        sourceBytes += sourcePitchBytes;
        destinationBytes += destinationPitchBytes;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ByteSpanAddRows(const unsigned char *source,
                     int sourceStride,
                     unsigned char *destination,
                     int destinationStride,
                     int width,
                     int rowCount);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms exact 0x89-byte byte-span row add helper, sole DrawFrameWithBlendMode mode-1 caller at 0x00462eef, one callee to ByteSpanAdd8Blocks at 0x00460eaf, caller argument order, width/8 block split plus scalar wrapping byte-add tail, source/destination row-pitch handling, sibling paddb block-helper behavior, and boundary padding; B013 adds formal ByteSpanAddRows C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00460e80-0x00460f09 Byte Span Add Rows

## Accepted Header Closure

- This exact declaration is emitted through [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) so `AlphaMaskSurface.cpp` can call the wrapping-add row helper through its owning header.
- The existing CPP implementation, ownership, and unsigned-byte wrapping behavior are unchanged; this callback closes only the previously missing declaration route.

## Status

- Disposition: reconstructable NexusTK render/support code.
- Entity kind: free helper.
- Accepted source file/owner/emitter: [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md); `AlphaMaskSurface.cpp` is a consumer through `DrawFrameWithBlendMode`, not a competing owner.
- Confidence: very strong for exact boundary, sole caller, callee, source-facing signature, block/tail split, row stepping, wrapping byte-add behavior, SoftwareBlend16 ownership, and formal CPP/H; only original lexical spelling remains inferred.
- Rebuild handling: `source-authored`.
- C++ reconstruction: 2026-06-30 B013 populates formal row-wrapper C++ with the accepted `ByteSpanAdd8Blocks` dependency prototype and modulo-256 byte-tail addition.

## Address Range

`0x00460e80-0x00460f09` (`sub_460E80`, size `0x89`, 63 decoded instructions).

The preceding `0x00460e79-0x00460e80` and following `0x00460f09-0x00460f10` bytes are alignment padding.

## Behavior

Adds source bytes into destination bytes row-by-row. The helper receives a source row pointer, source row pitch, destination row pointer, destination row pitch, byte width, and row count.

For each span it splits the width into `width / 8` eight-byte blocks and a `width % 8` scalar tail. When the block count is positive it calls [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md), then advances both row pointers past the completed block bytes. It adds each remaining tail byte from source into destination with normal byte wrapping semantics and advances source/destination by their row pitches for each row.

## Source-Facing Signature And Source-Form Policy

The best source-facing name and prototype for this helper are:

```cpp
void __cdecl ByteSpanAddRows(
    const unsigned char* sourceBytes,
    int sourcePitchBytes,
    unsigned char* destinationBytes,
    int destinationPitchBytes,
    int widthBytes,
    int rowCount);
```

The source-facing type should be byte pointers rather than `__m64*`. The block callee performs qword `paddb`, but this row helper owns byte-width arithmetic, scalar wrapping byte-add tails, and row stepping. The return type should be `void`; the sole caller uses only the destination side effect.

The formal CPP at the top is the active B013 implementation and includes the accepted `static` dependency prototype for `ByteSpanAdd8Blocks`; the formal H exports the row wrapper for the UID0000HF consumer. Both channels are nonblank and source-ready.

### Historical / Superseded Blank-C++ Rationale

Before B013 completed the row and block-helper family, B001 intentionally kept this wrapper blank to avoid an isolated fragment. That dated caution remains useful process history but no longer describes the current formal source.

## Rule 26 Incorporated B001 Research

The B001 source-quality report incorporated into this page resolves the helper as a row-level byte-span wrapping-add primitive. The sole direct caller is [UID:0000YN][0x00462e10-0x00462f1e.DrawFrameWithBlendMode](by-memory/0x00462e10-0x00462f1e.DrawFrameWithBlendMode.md), mode `1`, but the body is a generic strided source/destination byte operation and has no AlphaMaskSurface layout dependency.

[UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) remains the direct owner because this helper is grouped with the SoftwareBlend16 byte-block helpers and delegates the eight-byte `paddb` bulk path to [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md). [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md) is a caller/consumer context through `DrawFrameWithBlendMode`, not the owner of this primitive. `Surface`, `GrafPort`, and higher-level callbacks are rejected as direct owners for the same reason.

The B001 report also settles pointer typing. The source-facing row wrapper should use `uint8_t*`/`const uint8_t*`, not `__m64*`, because the row helper owns byte-width arithmetic, row pitches, and scalar wrapping byte-add tails. The eight-byte callee is the qword/MMX component.

The historical first-draft decision required the row and byte-block helpers to be resolved coherently. B013 satisfied that requirement by populating this wrapper and the sibling helper family together; it is no longer an open code pass.

Historical assumption resolved: neither the old `95/95` gate nor helper-family coherence now justifies blank source. Current CPP/H is present, and the exact owner/emitter remains UID0000NT.

## Reconstruction Evidence

| Item | Evidence | Reconstruction impact |
| --- | --- | --- |
| Mode caller | [UID:0000YN][0x00462e10-0x00462f1e.DrawFrameWithBlendMode](by-memory/0x00462e10-0x00462f1e.DrawFrameWithBlendMode.md) documents this helper as mode `1` at `0x00462eef`. | This is the additive byte-mask/frame blend operation. |
| Block helper | IDA decompilation dispatches to [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md). | Eight-byte `paddb` work is factored into a sibling helper. |
| Block split | Live disassembly computes the block count at `0x00460e84-0x00460e9a`, calls `sub_461050` at `0x00460eaf`, then subtracts `blocks * 8` from the remaining width. | Reconstruction should keep a row helper that delegates the eight-byte bulk path and handles only the residual tail locally. |
| Scalar tail | IDA decompilation shows scalar byte `+=` behavior after block processing. | Rebuild must preserve byte wrap/add semantics rather than saturating add unless later assembly proves otherwise. |
| Row stepping | IDA decompilation advances destination/source pointers by pitch each row. | Signature should stay row/span based. |
| Callee set | IDA MCP `callees` reports only [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md). | There are no hidden project-code dependencies inside this range beyond the sibling block helper. |
| Boundary | `0x00460f09-0x00460f10` is confirmed alignment padding in [UID:0000VN][-ignored](by-memory/-ignored.md). | Range is exact and safe to attach to SoftwareBlend16. |

## Evidence

- 2026-06-05 live IDA MCP `lookup_funcs` reports `sub_460E80` at `0x00460e80-0x00460f09`, size `0x89`; `disasm` decodes 63 instructions.
- IDA `callers`/`XrefsTo(0x00460e80)` reports one direct caller, `0x00462eef` inside [UID:0000YN][0x00462e10-0x00462f1e.DrawFrameWithBlendMode](by-memory/0x00462e10-0x00462f1e.DrawFrameWithBlendMode.md). The mode-1 call window pushes source pointer, source pitch, destination pointer, destination pitch, byte width, and row count.
- IDA `callees` for `0x00460e80` reports one internal callee, [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md), called at `0x00460eaf`.
- Live disassembly at `0x00460e84-0x00460ec6` computes `width / 8`, calls `sub_461050` when positive, advances source and destination by `blocks * 8`, and stores the remaining tail byte count.
- Live disassembly at `0x00460ed0-0x00460f00` adds the scalar tail one byte at a time with `add [eax-1], cl`, then advances the source pointer by source pitch and destination pointer by destination pitch until the row count reaches zero.
- The sibling block helper `sub_461050` is modeled at `0x00461050-0x0046109e`, has 37 decoded instructions, no callees, and performs `movq` destination loads, `paddb` with source qwords, qword stores, row-pitch advancement, and `emms`.
- Byte audit confirms `0x00460e79-0x00460e80` and `0x00460f09-0x00460f10` are seven `0xcc` bytes each; the next modeled function is `sub_460F10` at `0x00460f10-0x00460f99`.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Exact bounds, caller/callee set, source/destination signature, block/tail split, wrapping byte-add tail, row stepping, sibling helper relationship, UID0000NT ownership, boundary padding, and formal CPP/H are complete. |
| Confidence | 91 | Disassembly, decompilation, xrefs, callee evidence, `DrawFrameWithBlendMode` mode dispatch, sibling `paddb` block helper, SoftwareBlend16 grouping, and padding audit agree. Confidence stays below final-source quality because the exact original helper spelling and helper-family source style remain inferred. |
| Reconstructable | true | This is source-authored NexusTK render/support code attached to the SoftwareBlend16 source family; 2026-06-30 B013 populates formal row-wrapper C++ and the sibling byte-block helpers now carry their own formal bodies. |

## Cross-References

- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) SoftwareBlend16.
- [UID:0000YN][0x00462e10-0x00462f1e.DrawFrameWithBlendMode](by-memory/0x00462e10-0x00462f1e.DrawFrameWithBlendMode.md)
- [UID:00020H][0x00461050-0x0046109e.ByteSpanAdd8Blocks](by-memory/0x00461050-0x0046109e.ByteSpanAdd8Blocks.md)

## Changes

- 2026-08-17 B005 UID0000HF Gate 2A reconciliation:
  - Replaced active blank-C++, unresolved-family, `86/91`, and uncertain-file statements with current `88/91`, exact UID0000NT owner/emitter, nonblank formal CPP/H, and completed B013 helper-family disposition.
  - Retained the B001 no-fragment rationale under an explicit historical/superseded boundary and preserved all wrapping-add and binary evidence.

- 2026-06-02: Raised from `50/85` to `74/86`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md).
  - Added mode-dispatch caller evidence, sibling `paddb` block-helper evidence, scalar tail behavior, row stepping, and alignment-bounded range.
  - Left C++ blank because final helper prototypes and source split are not source-final.
- 2026-06-05: Raised from `74/86` to `82/90`.
  - Added live IDA MCP evidence for exact `sub_460E80` bounds, 63 decoded instructions, sole mode-1 caller at `0x00462eef`, one-callee set to `sub_461050`, caller argument order, `width / 8` block split, scalar wrapping byte-add tail, row-pitch advancement, sibling `paddb` block-helper behavior, and boundary padding.
  - Kept [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) parent, `RECONSTRUCTABLE:TRUE`, and blank final C++ metadata unchanged.
- 2026-06-17 B001 source-quality execution: Raised from `82/90` to `86/91`.
  - Resolved the source-facing `ByteSpanAddRows` prototype, byte pointer/pitch roles, wrapping add semantics, `void` return policy, SoftwareBlend16 ownership despite the AlphaMaskSurface caller, and first-draft C++ policy.
  - Kept formal C++ blank because isolated wrapper output would be incomplete until sibling byte-block helpers and the SoftwareBlend16 MMX/intrinsic source style are decided together.
  - Rule 26 remediation added the report's consumer-versus-owner reasoning, byte-pointer type policy, rejected owner alternatives, target-specific no-code reasoning, and historical gate correction directly to this by-memory page.
