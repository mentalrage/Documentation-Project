*** UID:0001AX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000L3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000L3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// No standalone body: this retained raw duplicate is covered by the live MapPane::HandlePacket opcode 0x20 route in [UID:00037T][0x00507c90-0x00508f60.MapPanePacketDispatcher](by-memory/0x00507c90-0x00508f60.MapPanePacketDispatcher.md).
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained raw MapPane opcode 0x20 day/night duplicate; exact no-route/retn-4/padding/dispatcher equivalence remains non-emitting, while UID0004RB/UID0004RC resolve the brightness/tint helper bodies, tint-curve fields, setter signature, live dispatcher ownership, and no-standalone-body formal policy. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00510400-0x005104c7 MapPane Day/Night Packet Raw Body

## Status

- Disposition: reconstructable NexusTK project code retained as raw MapPane packet-handler evidence.
- Entity kind: retained raw duplicate of a `MapPane::HandlePacket` opcode body.
- Likely source module: [UID:0000L3][MapPane](by-file/MapPane.md).
- Parent aggregate: [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md).
- Rebuild handling: source-authored behavior already represented by the live dispatcher branch; keep this exact raw body documented for audit, but do not reconstruct it as an independent final method unless later xref/table evidence proves reachability.
- Confidence: strong for raw bytes, behavior, MapPane ownership, dispatcher equivalence, and current unreferenced status; medium for why this duplicate body was retained.
- Formal C++ carries a no-code comment for this target. This is a target-specific no-code decision, not a score-gate deferral: current IDA-backed docs and the 2026-06-18 B003 raw PE scan confirm no modeled function, no direct caller/xref, and no rel32/VA/RVA/raw-offset route to `0x00510400`. The live source route is the equivalent `MapPane::HandlePacket` opcode `0x20` branch at `0x00507e45-0x00507f04`; emitting this target as a standalone `MapPane::HandleWeatherPacket` or `MapPane::HandleDayNightPacket` would duplicate dispatcher behavior and preserve a stale generated boundary.

## Summary

This raw body duplicates the day/night payload logic used by `MapPane::HandlePacket` opcode `0x20` / decimal case `32`.

It reads:

- `packet[1]` as `dayNightHour` / `hour`, valid only below `24`;
- `packet[2]` as `minuteOfHour`, valid only when `60 * value < 3600`, equivalent to `value < 60`.

When valid, the body stores the validated hour byte at `MapPane +0x40f` (`m_dayNightHour`), looks up `s_dayNightBrightnessByHour[hour]` / `dword_66DD78[hour]`, applies `MapPane +0x3e4` (`m_dayNightBrightnessScale`) and `MapPane +0x3e8` (`m_dayNightBrightnessBias`) around the `0.5f` midpoint, clamps the result to `[0.0f, 1.0f]`, computes tint amount as `MapPane +0x3c8` (`m_dayNightTintCurveScale`) times the square of `(target - MapPane +0x3b8 / m_currentDayNightBrightness)` plus `MapPane +0x3cc` (`m_dayNightTintCurveBase`), then calls UID0004RC `MapPane::SetDayNightFadeTarget(float targetBrightness, float tintAmount)`. The return value is always true, including invalid-payload paths.

## Raw Assembly Shape

```asm
00510400  push    ebp
00510401  mov     ebp, esp
00510403  push    ebx
00510404  push    esi
00510405  push    edi
00510406  mov     edi, [ebp+8]
00510409  mov     esi, ecx
0051040b  inc     edi
0051040d  call    byte-read helper
00510417  cmp     bl, 18h
0051041a  jnb     loc_5104BE
00510424  call    byte-read helper
00510439  cmp     eax, 0E10h
0051043e  jnb     loc_5104BE
00510446  mov     [esi+40Fh], bl
0051044c  movss   xmm1, dword_66DD78[eax*4]
0051045d  mulss   xmm1, dword ptr [esi+3E4h]
0051046d  addss   xmm1, dword ptr [esi+3E8h]
00510482  minss   xmm2, xmm1
00510491  subss   xmm0, dword ptr [esi+3B8h]
005104a6  addss   xmm1, dword ptr [esi+3CCh]
005104b9  call    sub_50A8B0
005104c0  mov     al, 1
005104c4  retn    4
005104c7  align   10h
```

## Evidence Notes

- 2026-06-06 IDA MCP `lookup_funcs` reports `0x00510400` and `0x005104c0` as `Not a function`, while `0x005104d0` is modeled as `sub_5104D0`, size `0x488`, and `0x00507c90` is the modeled `MapPane::HandlePacket` dispatcher, size `0x1176`.
- Raw disassembly shows a complete prologue/body/`retn 4` from `0x00510400` through byte `0x005104c6`, making `0x005104c7` the end-exclusive raw-body endpoint.
- The next modeled function begins at `0x005104d0`; `0x005104c7-0x005104d0` is alignment padding before that effect/map-state packet handler.
- 2026-06-06 IDA MCP `xrefs_to 0x00510400` and `callers 0x00510400` report no direct references. A raw pointer scan across loaded segments also found no dword equal to `0x00510400`, so no obvious untyped callback/jump table points at this raw start in the current IDB.
- By contrast, `xrefs_to 0x005104d0` reports the live dispatcher call at `0x00507f10`.
- `xrefs_to 0x0050a8b0` reports calls at `0x00506f17`, live dispatcher `0x00507efd`, this raw body `0x005104b9`, and modeled `sub_5104D0` at `0x00510865`. The raw-body call is not attributed to a modeled function because IDA does not own the raw start.
- `xrefs_to 0x0066dd78` reports the day/night table loads at live dispatcher `0x00507e90`, helper `0x0050a84a`, this raw body `0x0051044c`, and modeled `sub_5104D0` at `0x00510805`.
- 2026-06-18 B003 raw PE route scan used `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (`SHA256 9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`) and independently confirmed the target bytes from the current executable. The `.text` section base is VA `0x00401000`, raw offset `0x400`; the target body is the exact `0xc7` / 199-byte sequence at `0x00510400-0x005104c7`, with `cc cc cc` before it and nine `0xcc` bytes at `0x005104c7-0x005104d0`.
- B003 found no rel32 branch/call route to `0x00510400`, `0x005104c0`, or `0x005104c7`; no VA literal for `0x00510400`; no RVA literal for `0x00110400`; and no raw-offset literal for `0x0010f800`. Positive controls found the expected call to successor `0x005104d0` at `0x00507f10` and calls to `0x0050a8b0` at `0x00506f17`, `0x00507efd`, `0x005104b9`, and `0x00510865`, proving the scan would find ordinary routes.
- The only dword-pattern collision for `0x005104c7` is unrelated: bytes inside `0x00415ffc` are part of a conditional branch displacement (`0f 87 0d ff ff ff`) targeting `0x00415f0f`, not a pointer to this raw body.

## Duplicate Dispatcher Evidence

The live `MapPane::HandlePacket` dispatcher at `0x00507c90` contains the same day/night branch inline under jump-table case `32`:

- `0x00507e45-0x00507e7e` reads the two payload bytes and applies the same `< 24` and `< 3600` validity checks.
- `0x00507e8a` writes the mode byte to `MapPane + 0x40f`.
- `0x00507e90` loads the same `dword_66DD78[mode]` table entry.
- `0x00507ea1-0x00507eea` applies the same scale, bias, clamp, brightness-delta, and quadratic tint math.
- `0x00507efd` calls `0x0050a8b0`, then `0x00507f02-0x00507f04` returns success through the dispatch epilogue.

This proves the raw body is useful source-behavior evidence, but the currently proven source route is still the modeled dispatcher branch rather than an independently called handler at `0x00510400`.

The 2026-06-18 B003 PE comparison confirms the dispatcher branch and this raw target differ only in dispatcher-local setup/exit mechanics. The payload byte reads, `<24` hour check, `(minute * 60) < 3600` minute check, `+0x40f` store, `dword_66DD78` table lookup, clamp logic, tint calculation, and `0x0050a8b0` call are equivalent.

## Touched State

| State | Evidence | Reconstruction note |
| --- | --- | --- |
| `packet[1]` | Read through `0x00575470`, validated `< 24`, stored at `MapPane +0x40f`, and used as the 24-entry table index. | `dayNightHour` / `hour`. |
| `packet[2]` | Read through `0x00575470`; `(value * 60) < 3600` is equivalent to `< 60`. The value is validation-only in this body. | `minuteOfHour`; reject `durationSeconds` because the computed seconds value is never stored or passed. |
| `dword_66DD78` | Indexed load at `0x0051044c`, with matching live dispatcher load at `0x00507e90`, helper load at `0x0050a84a`, and modeled effect/map-state handler load at `0x00510805`. | `s_dayNightBrightnessByHour[24]` / `kDayNightBrightnessByHour`; descriptive source name, original symbol not proven. |
| `MapPane +0x40f` | Written by opcode `0x20`; reread by `0x005104d0` effect/map-state handler before the same table lookup. | `m_dayNightHour`. |
| `MapPane +0x3e4` | Multiplies `(table[hour] - 0.5f)`; effect/map-state handler writes the current lighting scale. | `m_dayNightBrightnessScale`. |
| `MapPane +0x3e8` | Added after midpoint restoration; effect/map-state handler writes the current lighting bias/offset. | `m_dayNightBrightnessBias`. |
| `MapPane +0x3b8` | Constructor seeds `0.60000002f`; target subtracts it from the clamped brightness target. | `m_currentDayNightBrightness`. |
| `MapPane +0x3c8` | Constructor seeds `-1.56250024f`; UID0004RB uses it as the quadratic coefficient for tint amount. | `m_dayNightTintCurveScale`; historical `m_dayNightDurationCurveScale` is a superseded search alias. |
| `MapPane +0x3cc` | Constructor seeds `0.89999998f`; UID0004RB adds it as the tint base term. | `m_dayNightTintCurveBase`; historical `m_dayNightDurationBase` is superseded. |
| `0x0050a8b0` | Called by both this raw body and the live dispatcher branch; UID0004RC proves a void member taking two floats, writing target/tint/projection fields, updating palette/filter state only when shadows are disabled, redrawing visible tiles, and queuing whole-map motion on change. | `MapPane::SetDayNightFadeTarget(float targetBrightness, float tintAmount)`. |

## Inferred Support Names

| Binary item | Best descriptive name | Confidence | Reason |
| --- | --- | ---: | --- |
| `dword_66DD78` | `s_dayNightBrightnessByHour[24]` / `kDayNightBrightnessByHour` | high | 24 float entries indexed by an hour byte in range `0..23`; values form a dim-night/full-day/mirrored-evening lighting curve. |
| `packet[1]` | `dayNightHour` / `hour` | high | Read through byte helper, validated `< 24`, stored at `MapPane +0x40f`, and used as the table index. |
| `packet[2]` | `minuteOfHour` | medium-high | Read through byte helper and multiplied by `60`; the check `minute * 60 < 3600` is equivalent to `< 60`. |
| `0x0050a840-0x0050a8b0` | UID0004RB `MapPane::ComputeDayNightBrightnessAndTint(...)` | high | Complete raw helper takes hour, scale, bias, and two output pointers; its exact dataflow computes clamped brightness and tint amount. No raw-start caller route survives, which caps liveness confidence rather than source readiness. |
| `0x0050a8b0-0x0050a932` | UID0004RC `MapPane::SetDayNightFadeTarget(float targetBrightness, float tintAmount)` | very high | Exact modeled void member updates target/tint/projection state and performs the accepted changed-path palette/redraw/motion sequence. |

## Ownership Decision

Keep this body under [UID:0000L3][MapPane](by-file/MapPane.md) and [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md) as retained raw evidence for opcode `0x20` / decimal case `32`. Do not merge it into the neighboring `0x005104d0` effect/map-state handler merely because IDA lacks a function object, and do not treat it as an independently called final method without a repaired boundary or xref.

Rejected alternatives: `MapPane::HandleWeatherPacket` is stale generated output and wrong for opcode `0x20`; a standalone `MapPane::HandleDayNightPacket` remains unproven because no route reaches `0x00510400`; weather layer/effect classes and packet reader helpers are dependencies, not owners. Keep this page as retained raw duplicate evidence until new route evidence appears.

## First-Draft C++ Policy

Do not populate a standalone body on [UID:0001AX]; the formal block records the no-code marker.

Target-specific no-code proof:

1. `0x00510400` has a complete function-shaped body but no modeled function object in current IDA-backed docs.
2. Current PE scans found no direct call/branch, no VA literal, no RVA literal, and no raw-offset literal to `0x00510400`.
3. The same behavior is already present inside the live dispatcher case at `0x00507e45-0x00507f04`.
4. The current generated `project-documentation/auto-generated/NexusTK/map/MapPane.cpp` emits no [UID:0001AX] body.
5. Emitting a standalone target body would create a duplicate source behavior path and would preserve the stale `HandleWeatherPacket` boundary that current evidence rejects.

The correct source location is [UID:00037T][0x00507c90-0x00508f60.MapPanePacketDispatcher](by-memory/0x00507c90-0x00508f60.MapPanePacketDispatcher.md) case `0x20` / [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md), or an exact dispatcher-case child if that container is split. The live source algorithm reads hour/minute bytes, validates `<24` and `<60`, stores the hour, maps/clamps brightness through `s_dayNightBrightnessByHour`, computes quadratic tint through `m_dayNightTintCurveScale` and `m_dayNightTintCurveBase`, calls UID0004RC `SetDayNightFadeTarget(targetBrightness, tintAmount)`, and returns success. This algorithm remains prose because any C++ belongs only to the proven live dispatcher emitter; UID0001AX retains only its managed no-standalone-body marker.

## Autogen Status

- Reconstructable: true, as retained MapPane packet-handler project code with exact raw-body evidence.
- Parent: [UID:0000L3][MapPane](by-file/MapPane.md).
- C++: formal no-code comment. The live dispatcher branch is the proven source route, and this page's no-code policy is based on the no-route duplicate proof rather than a generic 95/95 score gate.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | The page records exact raw-body bounds/instructions/routes, dispatcher equivalence, packet/table fields, UID0004RB/UID0004RC complete brightness/tint helper contracts, corrected tint semantics, rejected standalone emission, target-specific no-code proof, and current source placement. |
| Confidence | 92 | IDA-backed and raw-PE evidence strongly support behavior, boundaries, retained-duplicate disposition, tint semantics, and non-emitting formal decision. Original symbol spellings and why the compiler/linker retained an out-of-line duplicate remain unproven. |

## Changes

- 2026-07-15 B002 UID00037W accepted callback: raised the retained raw duplicate to `88/92`; replaced current duration aliases with exact tint semantics, linked real UID0004RB/UID0004RC source helpers, removed the non-managed illustrative C++ in favor of prose, and preserved the existing live-dispatcher ownership/no-standalone-body formal marker and all route/packet evidence.
- 2026-06-29 Agent-B004 MapPane empty-emitter callback:
  - Inserted the accepted formal no-code comment for the retained raw opcode `0x20` duplicate.
  - Evidence: existing B003 no-route proof and the current report's dispatcher evidence keep the live source route in [UID:00037T][0x00507c90-0x00508f60.MapPanePacketDispatcher](by-memory/0x00507c90-0x00508f60.MapPanePacketDispatcher.md), not this raw start.
- 2026-06-18 B003 source-quality / no-route duplicate recheck:
  - Before: score `84/88`; the page had the right retained-duplicate direction but still framed C++ as a generic final-source gate and kept field/table/helper names mostly generic.
  - After: score `86/90`; owner/emitter remain [UID:0000L3][MapPane](by-file/MapPane.md), reconstructable remains true as retained raw MapPane packet evidence, and formal C++ remains blank with target-specific no-code proof.
  - Historical summary/evidence: B003 verified the current PE SHA256, exact `0xc7`-byte body, padding before/after, no rel32/VA/RVA/raw-offset route to `0x00510400`, positive-control route hits to `0x005104d0` and `0x0050a8b0`, live dispatcher opcode `0x20` equivalence, `s_dayNightBrightnessByHour[24]` table values/refs, packet byte roles `dayNightHour` and `minuteOfHour`, then-current helper aliases `ComputeDayNightBrightnessAndDuration` and `SetDayNightFadeTarget`, stale `HandleWeatherPacket` rejection, and dispatcher-route source placement. UID00037W supersedes only the duration aliases with tint names.
- 2026-06-06 A006: Raised to `84/88`, attached to [UID:0000L3][MapPane](by-file/MapPane.md), removed stale generated-owner framing, and added live IDA MCP evidence for raw bytes, missing raw-start xrefs/callers, pointer-scan absence, dispatcher case `32` equivalence, `dword_66DD78` and `0x0050a8b0` xrefs, adjacent `0x005104d0` modeled handler contrast, and blank final C++ below the final-source gate.

## Cross-References

- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)
- [UID:0001SO][MapServerPacketOpcode](by-type/by-enum/MapServerPacketOpcode.md)
- [UID:0001QG][client_map_system](by-meta/client_map_system.md)
