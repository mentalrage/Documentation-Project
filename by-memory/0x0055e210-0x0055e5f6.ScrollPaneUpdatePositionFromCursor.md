*** UID:0004YM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CM | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollPane::UpdatePositionFromCursor(int localY, int localX)
{
    RectBounds firstButton;
    RectBounds secondButton;
    RectBounds thumb;
    GetScrollPartRect(kScrollPanePartFirstButton, &firstButton);
    GetScrollPartRect(kScrollPanePartSecondButton, &secondButton);
    GetScrollPartRect(kScrollPanePartThumb, &thumb);

    int thumbStart;
    int lowerBound;
    int upperBound;
    if (m_orientation != 0) {
        thumbStart = localX - m_thumbDragOffset.x;
        if (g_useEpfAssets) {
            ++thumbStart;
        }
        lowerBound = firstButton.right;
        upperBound = secondButton.left + thumb.left - thumb.right;
    } else {
        thumbStart = localY - m_thumbDragOffset.y;
        if (g_useEpfAssets) {
            ++thumbStart;
        }
        lowerBound = firstButton.bottom;
        upperBound = secondButton.top + thumb.top - thumb.bottom;
    }

    if (thumbStart < lowerBound) {
        thumbStart = lowerBound;
    } else if (thumbStart > upperBound) {
        thumbStart = upperBound;
    }

    int newPosition;
    if (m_orientation == 0 && g_useEpfAssets) {
        newPosition =
            ((m_scrollRange + 1) * (thumbStart - lowerBound)) /
            (upperBound - lowerBound);
        if (newPosition > m_scrollRange) {
            newPosition = m_scrollRange;
        }
    } else {
        newPosition =
            (m_scrollRange * (thumbStart - lowerBound)) /
            (upperBound - lowerBound);
    }

    const short oldPosition = m_scrollPosition;
    if (newPosition != oldPosition) {
        ScrollablePane *owner =
            static_cast<ScrollablePane *>(GetParentPane());
        owner->ApplyScrollPosition(
            m_orientation,
            oldPosition,
            static_cast<short>(newPosition));
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready generic ScrollPane thumb-drag position updater; derives horizontal or vertical track bounds from exact part geometry, applies saved drag offsets and EPF/classic arithmetic, clamps to the scroll range, calls the owner only on change, preserves the observed no-zero-denominator-guard behavior, and records exact Y-first caller, range, ownership, and source evidence. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0055e210-0x0055e5f6 ScrollPane UpdatePositionFromCursor

## Status And Source Placement

- Disposition: reconstructable NexusTK project code and source-ready private [UID:0000CM][ScrollPane](by-class/ScrollPane.md) method.
- Exact source signature: `void ScrollPane::UpdatePositionFromCursor(int localY, int localX)`.
- Direct canonical owner and emitter: [UID:0000CM][ScrollPane](by-class/ScrollPane.md).
- Source definition root: [UID:0000NF][ScrollBar](by-file/ScrollBar.md), reconstructed as `NexusTK/ui/core/ScrollBar.cpp`.
- [UID:0003A5][0x0055d960-0x0055e65c.ScrollPaneInputGeometryHelpers](by-memory/0x0055d960-0x0055e65c.ScrollPaneInputGeometryHelpers.md) is the non-emitting split index.
- Formal H remains blank here because the class page owns the declaration.

## Range And Boundaries

- Exact method: `[0x0055e210,0x0055e5f6)`, size `0x3e6`.
- It follows the complete `ProcessActivePart` compiler tail `[0x0055e1ed,0x0055e210)`.
- It ends before ten `0xcc` compiler-alignment bytes at `[0x0055e5f6,0x0055e600)`, documented by [UID:0000VN][-ignored](by-memory/-ignored.md).

## Behavior And Data Flow

Request rectangles for the first button, second button, and thumb through `GetScrollPartRect`. Horizontal mode uses `localX-m_thumbDragOffset.x`; vertical mode uses `localY-m_thumbDragOffset.y`; EPF mode applies the observed `+1` adjustment. Derive lower and upper track limits, clamp the dragged thumb coordinate to them, and calculate the new scroll position.

EPF vertical mode uses `(m_scrollRange + 1) * (thumbStart-lowerBound) / (upperBound-lowerBound)`, followed by an upper range clamp. The other paths use `m_scrollRange * (thumbStart-lowerBound) / (upperBound-lowerBound)`. The original has no denominator-zero guard, so the formal C++ deliberately preserves that behavior. Recover the owner and call `ApplyScrollPosition` only when the calculated result differs from `m_scrollPosition`.

## Caller, Callee, And Xref Evidence

- Direct code callers are `0x0055e055` and `0x0055e0db`, both inside `ScrollPane::ProcessActivePart`.
- Direct dependencies are [UID:0001GK][0x0055d9e0-0x0055de99.ScrollPaneGetScrollPartRect](by-memory/0x0055d9e0-0x0055de99.ScrollPaneGetScrollPartRect.md), parent/owner retrieval, and owner `ApplyScrollPosition`.
- Data dependencies are orientation, EPF mode, drag offset, position, range, and exact part geometry.
- No unrelated external caller, data xref, or vtable route was found.

## Ownership And Emitter Rationale

All geometry and state fields belong to accepted `ScrollPane`, both callers pass the same object, and owner callbacks match generic scroll contracts and sibling thumb-drag methods. Direct owner/emitter are [UID:0000CM][ScrollPane](by-class/ScrollPane.md), with source emission through [UID:0000NF][ScrollBar](by-file/ScrollBar.md).

## Positive Evidence

- Exact large modeled body and clean range fences.
- Two direct active-part dispatcher callers.
- Three exact geometry requests.
- Orientation-specific coordinate selection and saved drag-offset use.
- Exact EPF/classic formula differences and `+1` behavior.
- Track-coordinate clamp and changed-only owner callback.
- Matching sibling drag behavior and accepted field map.

## Negative Evidence And Rejected Alternatives

- No evidence supports adding a defensive zero-denominator guard; doing so would change observable behavior.
- No evidence supports swapping X/Y arguments, collapsing EPF/classic formulas, using floating point, widening one-byte part state, or assigning the method to a derived pane.
- No data/vtable route supports a public virtual or standalone helper.

## Score And Confidence Rationale

`93/94` is warranted by branch-by-branch arithmetic, callers, fields, route, and boundary proof. Exact historical spelling and final compiler-equivalence remain the only material caps.

## IDA Applied State

Supervisor Gate 2B catalog entry `0359` applied and independently verified the source-facing analysis state. The historical prestate was `sub_55E210` with type `void __thiscall(int this, int, int)` and all four address/function comment channels blank. The current shared state is:

- range/name/type: `[0x0055e210,0x0055e5f6)`, `ScrollPane__UpdatePositionFromCursor`, `void __thiscall(ScrollPane *this, int localY, int localX)`;
- comments: address regular `""`, address repeatable `""`, function regular `""`, and function repeatable exactly `ScrollPane thumb-position update: converts local cursor and drag offset to a clamped scroll position using part rectangles, then applies a changed position through the owning ScrollablePane.`;
- frame after prototype normalization: nonargument entries remain `offset +0xc/0x8 Point`, `position +0x14/0x8 Point`, `oldPosition +0x1c/0x4 unsigned __int16[2]`, `var_34 +0x20/0x4 _DWORD`, `var_30 +0x24/0x4 _DWORD`, `var_2C +0x28/0x4 _DWORD`, `var_28 +0x2c/0x4 _DWORD`, `var_24 +0x30/0x10 _OWORD`, `var_14 +0x40/0x10 _OWORD`, `var_4 +0x50/0x4 _DWORD`, `__saved_registers +0x54/0x4 _DWORD`, and `__return_address +0x58/0x4 _UNKNOWN *`; former `arg_0`/`arg_4` are now `localY +0x5c/0x4 int` and `localX +0x60/0x4 int`;
- xrefs: exactly two code refs, `0x0055e055` and `0x0055e0db` in `ScrollPane__ProcessActivePart`;
- protected bytes: the 998-byte body remains SHA256 `DDEB358FEF0878DECBD4829196C069AFD010CA2E86ADF31B89332041CA546FAE`; following `[0x0055e5f6,0x0055e600)` remains ten `0xcc` bytes, SHA256 `22E52BBA61DA8AD6732BE550205A10B89A1BB3B0EA491BE07DE9294F0EB6F045`;
- persistence history/currentness: byte-identical pre-mutation backup `E:\NTK\Resources\NexusTK\backups\NexusTK.exe.pre-B005-UID0003A5-20260729-124819.i64` is SHA256 `E9600F6C16B46FE1F5A7FEE463EF7890E415905CE465F8F6C55954FF5AA91611`, 143,190,173 bytes. Gate 2B originally saved the verified ScrollPane state as IDB SHA256 `08D31FD1C7B80721D6CD7774B42C5A5157B3A4683670A3E27EA5660290554F5A`, 143,190,452 bytes, at `2026-07-29T12:53:48.4762005-04:00`. Shared saves `03F10207763C01455EA5F767F330A371FEDD9885FBFD98705F5A486985DBE5DA`, `0E0AF9383DE743CB91E076959498A0D9DAF906C5CA6E620F99425931A09AB481`, and `3C8F31781C94DF74AADDB65B3D944CD2CA4387C448918BBBFFD60B41C3625B69` are historical. After the later supervisor save, bounded live read-only recheck against session `1da2b2ae` confirmed the same target state in current authoritative IDB SHA256 `296ED21C6665B7C0D8EFBC515B137101F8506FD945C6C7E14C3E6F655063CF8B`, 143,190,676 bytes, saved `2026-07-29T14:22:15.9735862-04:00`.

Only the accepted name, receiver/argument prototype, argument presentation, and function-repeatable comment changed. Body/fence bytes, range, arithmetic, xrefs, nonargument frame storage, adjacent functions, and unrelated analysis state remain unchanged.

## Historical And Corrected Assumptions

Former documentation retained raw `sub_55E210`, left the formulas unresolved, allowed X-first ordering, and invited a defensive zero-denominator guard. Exact arithmetic and caller order supersede those assumptions; preserving the no-guard behavior is required for reconstruction fidelity.

## Cross-References

- [UID:0000CM][ScrollPane](by-class/ScrollPane.md)
- [UID:0000NF][ScrollBar](by-file/ScrollBar.md)
- [UID:0003A5][0x0055d960-0x0055e65c.ScrollPaneInputGeometryHelpers](by-memory/0x0055d960-0x0055e65c.ScrollPaneInputGeometryHelpers.md)
- [UID:0001GK][0x0055d9e0-0x0055de99.ScrollPaneGetScrollPartRect](by-memory/0x0055d9e0-0x0055de99.ScrollPaneGetScrollPartRect.md)

## Changes

- 2026-07-29 B005 Gate 2B saved-state reconciliation:
  - Recorded catalog entry `0359`'s applied `ScrollPane__UpdatePositionFromCursor` name/type/comment, normalized Y/X argument entries, preserved frame and two xrefs, exact body/fence hashes, and backup/detailed save/current identities without changing formal C++ or arithmetic evidence.

- 2026-07-29 B005 UID0003A5 implementation callback:
  - Created this exact child from the accepted split report with direct class ownership/emission, formal C++, blank child H, complete arithmetic/data-flow/xref evidence, behavior-fidelity exclusions, score rationale, and corrected historical assumptions.
  - The validator assigns the stable UID; the report and manual supervisor-owned coverage staging are reconciled after scoped validation.
