*** UID:0002K3 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BL | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
tm RankingCategoryRecord::GetStartTime() const
{
    return m_startTime;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact 31-byte MSVC x86 lowering of `tm RankingCategoryRecord::GetStartTime() const`: returns the 36-byte standard `tm` value `m_startTime` at record offset `+0x0c` through a hidden result pointer, has one live RankingUserListPane paint caller at `0x0045f4fe`, copies all nine `tm` integers without normalizing direct packet month/full-year values or initializing trailing fields, preserves exact `0xcc` boundaries and body hashes, and emits through class owner UID0000BL with a blank child H channel. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045bf90-0x0045bfae RankingCategoryRecordCopyStartTimeParts

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: class method.
- Parent class: [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md).
- Aggregate range: [UID:0000Y9][0x0045bf60-0x0045c257.RankingCategoryRecord](by-memory/0x0045bf60-0x0045c257.RankingCategoryRecord.md).
- Likely source module: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md).
- Exact half-open range: `[0x0045bf90,0x0045bfaf)`; documented inclusive endpoint `0x0045bfae`.
- Confidence: very strong for exact behavior, ABI, standard `tm` identity, member offset, caller, boundaries, owner/emitter route, and formal source shape; strong for the inferred original spelling `GetStartTime`.
- Metadata: `93/95`, canonical owner/emitter UID0000BL, reconstructable true. Formal CPP owns this method body; formal H stays blank because UID0000BL owns the declaration.

## Behavior

Returns the 36-byte standard `tm` member `m_startTime` from record offset `+0x0c` by value. The body copies two 16-byte chunks from receiver offsets `+0x0c` and `+0x1c`, then the final dword from `+0x2c`, into destination offsets `+0x00`, `+0x10`, and `+0x20`. EAX returns the same hidden destination pointer supplied as stack `arg_0`.

The developer-facing source is `tm RankingCategoryRecord::GetStartTime() const`. MSVC x86 lowers that 36-byte value return into the observed hidden result-pointer copy; the explicit output argument is ABI machinery, not a source parameter. The receiver is read-only, so `const` is source-correct.

## Type Resolution

- IDA's existing 32-bit `tm` UDT is exactly `0x24` / 36 bytes and has nine ordered `int` members: `tm_sec +0x00`, `tm_min +0x04`, `tm_hour +0x08`, `tm_mday +0x0c`, `tm_mon +0x10`, `tm_year +0x14`, `tm_wday +0x18`, `tm_yday +0x1c`, and `tm_isdst +0x20`.
- The getter's copy width and ordering match that UDT exactly. No separate `RankingDateTimeParts` type exists in current IDA.
- The start packed setter writes only `tm_year/tm_mon/tm_mday/tm_hour/tm_min/tm_sec`. Checked parser construction paths leave `tm_wday/tm_yday/tm_isdst` untouched, while this getter still copies them as part of the complete value.

## Runtime Constraints

- Preserve direct protocol values: setters store packet month and full year directly in `tm_mon` and `tm_year`, and paint prints those values directly.
- Do not subtract one from month, subtract 1900 from year, normalize through a C runtime function, validate ranges, zero the value, or synthesize trailing fields. Those changes would diverge from the original executable.

## IDA MCP Evidence

- `lookup_funcs` reports the modeled function at `0x0045bf90` with size `0x1f`.
- Decompilation copies two 16-byte chunks from record offsets `+0x0c` and `+0x1c`, then copies the final dword from offset `+0x2c`, for a total of 36 bytes.
- `callers` reports a live caller from `RankingUserListPane` paint logic at `0x0045f4fe`.
- `analyze_function` reports one basic block, cyclomatic complexity 1, no callees, no branches, and no embedded strings or constants.

## Consumer Evidence

- The sole direct call at `0x0045f4fe` in `sub_45F3A0` supplies a 36-byte local beginning at `ebp-0xb4`.
- `RankingUserListPane::OnPaint` consumes only the first six fields for its `From` timestamp. It does not read `tm_wday`, `tm_yday`, or `tm_isdst`.

## Range, Padding, And Raw Identity

- Body range is `[0x0045bf90,0x0045bfaf)`, raw PE offset `0x5b390`, length `0x1f`.
- The preceding byte at `0x45bf8f` is `0xcc`. The following item `[0x45bfaf,0x45bfb0)` is one unnamed, untyped, non-code `0xcc` alignment byte before the paired end accessor at `0x45bfb0`; it must not be merged into either method.
- Body SHA1: `b48f8b04295d3ff8d425fd71e887567305e313e4`.
- Body SHA256: `556fb8a2b0e4905fedf233d2ef4b599a716224ee58bd05d8c9107e5c74eb7222`.

## Negative Evidence

- No second caller, callee, virtual-table route, export, pointer table, or target VA/RVA/raw-offset pointer pattern was found.
- A wildcarded 36-byte copy-body scan matches only this accessor and paired UID0002K4, supporting one start/end accessor family rather than a generic copy helper.
- `SYSTEMTIME` is rejected because its width/order do not match. A six-dword type is rejected because the getter copies nine dwords. An authored output parameter is rejected because the hidden-return ABI fully explains it.

## Reconstruction Notes

This is source-ready as `tm RankingCategoryRecord::GetStartTime() const` in `NexusTK/ui/dialogs/RankingDialog.cpp`. UID0000BL formal H owns `tm GetStartTime() const;` and `tm m_startTime;`; this child must not duplicate the declaration in formal H.

The `GetStartTime` spelling is inferred from the accessor cluster and source role because retail symbols are stripped. It is preferable to compiler-shaped `CopyStartTimeParts(out)` and raw IDA labels, while the binary behavior remains exact.

## Historical Assumptions

- Historical B002 documentation modeled the value as `RankingDateTimeParts { second, minute, hour, day, month, year, reserved0, reserved1, reserved2 }` and exposed the hidden result pointer as an authored `out` argument. That model preserved the bytes and helped establish the first-six-field order, but it is superseded by the exact existing `tm` size/member identity and MSVC structure-return ABI.
- Historical statements that final type naming remained open are retained here as superseded research history, not current blockers. The final three dwords are source-visible `tm_wday`, `tm_yday`, and `tm_isdst`, even though checked setters leave them untouched and paint ignores them.

## Cross-References

- [UID:0000BL][RankingCategoryRecord](by-class/RankingCategoryRecord.md)
- [UID:0001VN][RankingCategoryRecordLayout](by-type/by-struct/RankingCategoryRecordLayout.md)
- [UID:0000Y9][0x0045bf60-0x0045c257.RankingCategoryRecord](by-memory/0x0045bf60-0x0045c257.RankingCategoryRecord.md)
- [UID:0000BQ][RankingUserListPane](by-class/RankingUserListPane.md)

## Changes

- 2026-06-05: Raised to `82/90` after live IDA reconfirmed the exact copy size, no callees, user-list paint caller, and layout match to the start date/time block. Final date/time type naming remains open.
- 2026-06-17 B002 source-quality execution:
  - Raised from `82/90` to `86/91` and populated first-draft C++.
  - Evidence: B002 rechecked the exact 36-byte copy, user-list paint consumer, packed setter agreement, and `RankingDateTimeParts` field order.
- 2026-08-02 B009 UID0002K3 accepted implementation:
  - Raised from `86/91` to `93/95`, preserved owner/emitter UID0000BL, replaced the ABI-shaped custom-type output method with source-shaped `tm RankingCategoryRecord::GetStartTime() const`, and kept formal H blank for the class-owned declaration route.
  - Added exact ABI, UDT, caller/local, direct protocol-value, padding, raw-offset/hash, negative-route, paired-pattern, rejected-alternative, and historical-assumption evidence without discarding the useful earlier copy analysis.
