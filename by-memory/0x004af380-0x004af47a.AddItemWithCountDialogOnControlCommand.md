*** UID:0004BR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:20 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void AddItemWithCountDialog::OnControlCommand(int controlIndex, int notifyCode)
{
    (void)notifyCode;

    if (controlIndex == 1)
    {
        wchar_t countText[16];
        unsigned short parsedCount;
        TextEditControlPane *countInput =
            GetChild<TextEditControlPane>(4);

        countInput->ReadText(countText, 4);
        swscanf(countText, L"%hu", &parsedCount);
        if (parsedCount >= 0xff)
            parsedCount = 0xff;

        unsigned char packet[9];
        PacketBufferWriteUInt8(0x4a, packet);
        PacketBufferWriteUInt8(2, packet + 1);
        PacketBufferWriteUInt32BE(m_exchangeSessionId, packet + 2);
        PacketBufferWriteUInt8(m_inventorySlot, packet + 6);
        PacketBufferWriteUInt8(static_cast<unsigned char>(parsedCount),
                               packet + 7);
        packet[8] = 0;
        g_packetSender->QueueAndSendPacket(packet, 8);
    }
    else if (controlIndex != 2)
    {
        return;
    }

    SlideClose();
    CloseDialog();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: AddItemWithCountDialog control-command override; control 1 reads and clamps selector-4 count text, sends opcode 0x4A/2 with the signed inventory slot, then slides/closes, while control 2 only slides/closes. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004af380-0x004af47a AddItemWithCountDialog OnControlCommand

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: `DialogPane` primary-slot `+0x48` control-command override split from [UID:00033S][0x004af040-0x004af4b7.AddItemWithCountDialogCore](by-memory/0x004af040-0x004af4b7.AddItemWithCountDialogCore.md).
- Owner: [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md) AddItemWithCountDialog.
- Source file: [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md) ItemDialogs.

## Exact ABI And Control Flow

- Exact range `[0x004af380,0x004af47a)` is `0xfa` bytes with 85 instructions and five basic blocks. Both physical return sites use `retn 8`: the source callback returns `void` and consumes two 32-bit arguments.
- The first argument is the control selector. The second argument is accepted as `notifyCode` but is never read. This matches current `DialogPane::OnControlCommand(int controlIndex, int notifyCode)` at primary relative slot `+0x48`; historical `int OnAction(unsigned int)` is rejected.
- Control 1 gets selector 4 as `TextEditControlPane`, reads at most four wide characters into `wchar_t countText[16]`, calls the wide `"%hu"` scan wrapper, and uses an intentionally uninitialized `unsigned short parsedCount`. No instruction preinitializes the parse destination.
- The clamp condition is exactly `parsedCount >= 0xff`, followed by assignment to `0xff`. It is not a strict `>` check.
- The local `unsigned char packet[9]` is not aggregate-zeroed. Bytes 0 through 7 are written as opcode `0x4a`, subcommand `2`, big-endian exchange-session id, signed `m_inventorySlot`, and clamped count. Byte 8 receives an explicit scratch zero, while the send length remains exactly eight bytes.
- After a control-1 send, the method calls `SlideClose()` and then `CloseDialog()`. Control 2 performs the same two close calls without parsing or sending. Every other control returns without side effects.

## Liveness, Boundaries, And Retained Helper

- Primary vtable cell `0x0061a034` routes directly to `0x004af380`. Six `0xcc` bytes at `[0x004af47a,0x004af480)` separate the method from the updater; fifteen bytes at `[0x004af371,0x004af380)` separate it from the constructor.
- [UID:00033T][0x004af4f0-0x004af570.AddItemWithCountDialogSubmitPacketHelper](by-memory/0x004af4f0-0x004af570.AddItemWithCountDialogSubmitPacketHelper.md) is a retained no-route helper with the same packet serialization but no text read, parse, clamp, or close flow. The live control command contains the send inline; the retained helper is not called from this body.
- `GetCountInputText`, `ParseUnsignedShort`, `m_selectedItemType`, aggregate packet zero initialization, close-without-slide, one-argument action ABI, explicit raw offsets, and a helper call replacing the inline send are historical source-shape errors and are rejected.

## Score Rationale

`92/94` reflects exact range, instruction/block/return ABI, vtable route, two-selector behavior, typed text read, scan/clamp details, uninitialized locals, packet layout/send length, signed slot semantics, close order, retained-helper relationship, source route, and exact formal C++. Stripped local/access spellings cap confidence below final-audit certainty.

## Changes

- 2026-07-13 UID0004BQ callback: validator-preservingly renamed UID0004BR from `AddItemWithCountDialogOnAction` to `AddItemWithCountDialogOnControlCommand`, raised `88/91 -> 92/94`, set position 20, and replaced the stale one-argument/int/helper/zero-init body with the exact two-argument void callback and active inline send.
