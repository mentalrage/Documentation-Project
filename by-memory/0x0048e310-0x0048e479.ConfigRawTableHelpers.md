*** UID:00021Z | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000IE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000IE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int Config::FindOrAppendFixedConfigTableRow()
{
    wchar_t playerName[40];
    GetLocalPlayerName(playerName, _countof(playerName));

    int rowCount = m_fixedConfigRowCount;
    int rowIndex = 0;

    if (rowCount > 0)
    {
        ConfigCharacterServerMacroRow *row = m_fixedConfigRows;
        const wchar_t *currentServerName = g_pApplication->currentServerName;

        do
        {
            if (wcscmp(row->playerName.text, playerName) == 0 &&
                wcscmp(row->serverName.text, currentServerName) == 0)
            {
                return rowIndex;
            }

            ++rowIndex;
            ++row;
            rowCount = m_fixedConfigRowCount;
        }
        while (rowIndex < rowCount);
    }

    m_fixedConfigRowCount = rowCount + 1;
    return rowIndex;
}

void Config::ResetFixedConfigTableRows()
{
    for (int rowIndex = 0; rowIndex < 100; ++rowIndex)
    {
        ConfigCharacterServerMacroRow &row = m_fixedConfigRows[rowIndex];

        row.playerName.length = 0;
        row.serverName.length = 0;
        memset(row.macroEnabled, 0, sizeof(row.macroEnabled));

        for (int macroIndex = 0; macroIndex < 20; ++macroIndex)
        {
            row.primaryMacroKeys[macroIndex] = 0;
            row.alternateMacroKeys[macroIndex] = 0;
        }

        row.selectedMacroIndex = 0;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Config fixed-table lookup/append and reset raw helpers with a 40-wide local-player buffer, typed 100-row character/server macro slab, player/server matching, append index, exact bank-field reset, complete source-clean C++, and retained no-function/xref/direct-route lexical confidence cap. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0048e310-0x0048e479 ConfigRawTableHelpers

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw helper cluster
- Source module: [UID:0000IE][Config](by-file/Config.md)
- Likely owner class: [UID:000031][Config](by-class/Config.md)
- Rebuild handling: source-authored
- Confidence: strong for configuration-table ownership, byte boundaries, and no-direct-xref status; medium for final helper names/callability because the two bodies are raw helper-shaped bytes with no current IDA function object and no confirmed caller path.

## Covered Ranges

| Range | Entity | Notes |
| --- | --- | --- |
| `0x0048e310-0x0048e408` | `FindOrAppendFixedConfigTableRow` | Ghidra models raw `FUN_0048e310`; IDA has no normal function/xref route. The body calls `GetLocalPlayerName(localPlayerName, 0x28)` into `wchar_t[40]`, scans active typed rows, compares `row.playerName.text` and `row.serverName.text` against local-player/current-server identity, returns a match, or increments the count and returns the append index. |
| `0x0048e410-0x0048e479` | `ResetFixedConfigTableRows` / config row reset helper | Raw function-shaped body that clears the large fixed config table: loops 100 rows at stride `0x6894`, zeroes lead/trailing wide fields, clears row counters, uses `xorps`/`movups` for part of each row, and zeroes two 20-entry wide-value subarrays. |

## Evidence

- Direct PE byte review of `E:\NTK\Resources\NexusTK\NexusTK.exe` shows the preceding `CollectionEntryControlPane` destructor returns at `0x0048e304`, followed by eleven `0xcc` bytes from `0x0048e305-0x0048e310`.
- `0x0048e310` begins `55 8b ec 83 ec 5c ...` and ends at `0x0048e408` after a stack-cookie check and `ret`; `0x0048e408-0x0048e410` is eight `0xcc` alignment bytes.
- `0x0048e410` begins `56 57 8d 91 06 02 00 00 ...`, returns at `0x0048e478`, and is followed by seven `0xcc` bytes through `0x0048e480`.
- 2026-06-06 live IDA MCP `lookup_funcs` reports no function object at `0x0048e310`, `0x0048e408`, `0x0048e410`, or `0x0048e479`, while the neighboring modeled functions are `sub_48E2B0` covering `0x0048e2b0-0x0048e305` and `sub_48E480` size `0x28`.
- 2026-06-06 live IDA MCP `xrefs_to` reports no direct xrefs to `0x0048e310` or `0x0048e410`. The `find_bytes` direct-call/jump probes for the obvious relative `call`/`jmp` encodings to both starts also returned zero matches.
- 2026-06-15 live IDA MCP reconfirms `0x0048e310` and `0x0048e410` are not function objects, reconfirms no direct xrefs to either start, confirms neighbor functions at `0x0048e2b0` and `0x0048e480`, and `make_signature_for_range 0x0048e310-0x0048e479` returns a unique IDA signature for the whole raw helper island.
- 2026-06-26 B007 accepted MCP refresh from session `80de0a67` reconfirmed that `0x0048e310`, `0x0048e408`, `0x0048e410`, and `0x0048e479` are not function objects; direct `xrefs_to` and inbound `xref_query` checks remain zero for both raw starts; VA/RVA pointer-byte patterns for both raw starts are absent; and a read-only PE `.text` scan found zero direct `E8`/`E9` rel32 call or jump destinations to either start. This no-route evidence remains a score cap, not a no-code reason.
- 2026-06-06 live IDA MCP disassembly confirms the first helper saves `ecx` as the config object, clears a stack buffer with `sub_5A2DA0`, reads row count from `this + 0x28d9d4`, compares row text at `this + 0x6` and `this + 0x208`-relative fields, compares a second key from `g_pApplication + 0x858` (older text called this `dword_67AB1C + 0x858`), returns the found/scanned index in `eax`, and runs a stack-cookie check before returning.
- 2026-06-06 live IDA MCP disassembly confirms the second helper uses `ecx` as the config object, starts at `this + 0x206`, iterates `0x64` rows at stride `0x6894`, clears leading/trailing UTF-16 slots, clears a row count/value dword at `+0x668e`, and loops `0x14` times per row over two wide-value subarrays.
- B007 corrects the earlier local-buffer wording: the `0x48e328 push 28h` clear is `0x28` bytes, meaning 20 UTF-16 characters, not 40 wide characters.
- B007 resolves `g_pApplication + 0x858` through current Application and ServerSelect support docs as the current selected-server-name buffer. Use source-facing `g_pApplication->currentServerName` or the documented equivalent `selectedServerNameDisplay`; keep `dword_67AB1C + 0x858` only as historical IDA-label/search context.
- `resources/exported_data/master_function_list.json` includes a Ghidra/GhidrAssist entry for `0x0048e310` summarized as a wide-string config-table lookup/append helper, but no IDA function name. No corresponding generated `simroot_v2` source body was found for `0x0048e310` or `0x0048e410`.
- The second helper's constants match the `Config` constructor/default table layout documented in [UID:000110][0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults](by-memory/0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults.md): 100 rows, row stride `0x6894`, and repeated 20-wide-value clears.

## Reconstruction Evidence

| Evidence | What it supports |
| --- | --- |
| [UID:0000IE][Config](by-file/Config.md) projected to `NexusTK/config/` | Parent source ownership is valid for autogen attachment; the proposed contents row explicitly lists this helper cluster as `ConfigRawTableHelpers`. |
| [UID:000031][Config](by-class/Config.md) | Class page names the raw table helpers as related `Config` support code beside constructor, deleting destructor, config-entry initialization, and cleanup. |
| [UID:000110][0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults](by-memory/0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults.md) | Adjacent defaults page records the following exact config-entry/default functions and states that this raw helper cluster belongs to the same source family. |
| [UID:00028Q][0x0067a7c8-0x0067a7cc.g_pConfig](by-memory/0x0067a7c8-0x0067a7cc.g_pConfig.md) | Confirms the active config singleton and broad `Config`/`RegistryConfig` object role for field-offset consumers. |
| [UID:0000VN][-ignored](by-memory/-ignored.md) | Confirms surrounding and internal alignment: `0x0048e305-0x0048e310`, `0x0048e408-0x0048e410`, and `0x0048e479-0x0048e480`. |
| Raw byte evidence on this page | Bounds two complete helper-shaped bodies: lookup/append at `0x0048e310-0x0048e408` and reset at `0x0048e410-0x0048e479`. |

## Fixed Table Layout

Historical B007 pre-callback analysis correctly fixed the row slab base, count, stride, and raw-helper boundaries, but its `0x28`-bytes/20-wide empty-key interpretation and schematic field names are superseded by the current 40-wide local-player copy and typed row model. The arithmetic evidence remains preserved below as evidence-time provenance.

The following table is the historical pre-callback schematic row interpretation. It is retained only to show the former offset model and is superseded by `ConfigCharacterServerMacroRow`, `playerName`, `serverName`, selected/index state, and primary/alternate macro-bank fields in the current managed declaration:

| Row offset | Width | Meaning |
| ---: | ---: | --- |
| `+0x0000` | word | `lookupKey.header`, cleared by reset. |
| `+0x0002` | 256 UTF-16 chars | `lookupKey.text`, compared by the lookup helper against the zeroed local 20-wide-character buffer. |
| `+0x0202` | word | `serverName.header`, cleared by reset. |
| `+0x0204` | 256 UTF-16 chars | `serverName.text`, compared by the lookup helper against `g_pApplication->currentServerName`. |
| `+0x0404` | 20 bytes | `stateBlock`, cleared by `xorps`/`movups` plus dword clear. |
| `+0x1840` | 20 UTF-16 values | `wideValueSetA`, cleared by the inner reset loop. |
| `+0x4068` | 20 UTF-16 values | `wideValueSetB`, cleared by the inner reset loop. |
| `+0x6890` | dword | `trailingStateValue`, cleared before advancing to the next row. |

## Source Reconstruction Notes

Treat these helpers as Config-table support code that should be accounted for near `Config.cpp` once the large config table layout is finalized. They should not be left as unknown bytes and should not be attached to the preceding collection dialog island or moved to `RegistryConfig` merely because registry persistence later touches inherited Config fields.

Best descriptive names are `FindOrAppendFixedConfigTableRow` for `0x0048e310` and `ResetFixedConfigTableRows` for `0x0048e410`. The names remain descriptive/inferred rather than original-symbol proof, but they are now source-quality enough for first-draft C++ because the helper bodies are complete Config methods, the fixed table layout is coherent, and the `Application + 0x858` comparison is resolved to the current selected server-name buffer.

The missing direct caller/use-site route must remain in evidence and score rationale. Live IDA xrefs, inbound-route checks, pointer-byte searches, and direct PE rel32 call/jump scans did not find references to either raw start; they may be retained private helpers, address-taken through a route not covered by the scans, or currently unreferenced project code. B007 resolves that as a score cap, not a C++ blocker.

Rejected alternatives:

- Padding/data: rejected by the executable instruction bodies and unique helper shape.
- Compiler/runtime helper: rejected by direct `Config` object and fixed-table field semantics.
- RegistryConfig ownership: rejected because the helpers act on base fixed storage before ConfigEntry construction.
- No-owner/non-emitting: rejected because target and parent clear the current code gate, the owner/emitter is stable, and method behavior is reconstructable.
- Keeping C++ blank solely because no caller route exists: rejected by B007. The no-route fact caps score and must stay documented, but it does not invalidate the source-authored method bodies.
- Raw `dword_67AB1C` naming: retained only as historical IDA search context; current source-facing docs should use `g_pApplication->currentServerName` or documented equivalent `selectedServerNameDisplay`.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `88` | The page has exact raw-island identity, alignment-bounded bodies, no-function/no-xref/direct-route evidence, Config owner route, 40-wide local-player copy, typed 100-row slab/count, player/server comparison, append/reset behavior, and complete managed C++. Completion remains capped because no live caller or original helper spelling is proved. |
| Confidence `90` | Confidence is strong that these bytes are source-authored Config table helpers rather than padding or dialog code: the unique signatures, constants, adjacent Config defaults page, config singleton support, row-stride/table-clear behavior, Application `+0x858` field evidence, and support docs agree. Confidence is capped because direct reachability and exact original names remain unresolved. |

## Cross-References

- [UID:0000IE][Config](by-file/Config.md)
- [UID:000031][Config](by-class/Config.md)
- [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md)
- [UID:000110][0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults](by-memory/0x0048e480-0x0048f3f1.ConfigAndRegistryDefaults.md)
- [UID:00028Q][0x0067a7c8-0x0067a7cc.g_pConfig](by-memory/0x0067a7c8-0x0067a7cc.g_pConfig.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-06-02 rescore pass:
  - What existed before: `COMPLETION:55`, `CONFIDENCE:65`, blank reconstructable state, and no autogen parent.
  - Changed to: `COMPLETION:74`, `CONFIDENCE:84`, `RECONSTRUCTABLE:TRUE`, and parent [UID:0000IE][Config](by-file/Config.md).
  - Summary/evidence: parent path, class owner, adjacent defaults/config-entry functions, config singleton evidence, raw helper byte bounds, and exact ignored alignment are now recorded. C++ remains blank because caller reachability and final table/member names are not proved.
- 2026-06-06 A010 live IDA refresh:
  - Changed to: `COMPLETION:78`, `CONFIDENCE:86`.
  - Summary/evidence: IDA MCP rechecked function modeling, direct xrefs, direct call/jump byte probes, and detailed disassembly for both raw starts. The page now records the no-function/no-xref status, the global `dword_67AB1C + 0x858` comparison in the lookup helper, the exact per-row clearing pattern in the reset helper, and the remaining caller/name caveats.
- 2026-06-15 A002 Goal 2 parser/config/error batch:
  - Changed to: `COMPLETION:85`, `CONFIDENCE:88`; owner/emitter unchanged at [UID:0000IE][Config](by-file/Config.md).
  - Summary/evidence: live IDA MCP reconfirmed no function objects and no direct xrefs at `0x0048e310`/`0x0048e410`, confirmed neighbor modeled functions at `0x0048e2b0` and `0x0048e480`, and returned a unique signature for `0x0048e310-0x0048e479`.
  - Remaining blocker: direct caller/use-site provenance remains unresolved, so the C++ block stays blank.
- 2026-06-21 B012 Rule 26 Config source-quality pass:
  - Score unchanged at `85/88`.
  - Summary/evidence: renamed the source-facing roles to `FindOrAppendFixedConfigTableRow` and `ResetFixedConfigTableRows`, replaced active `dword_67AB1C +0x858` wording with `g_pApplication +0x858`, and preserved the no-code proof: no IDA function objects, no xrefs, no direct call/jump/VA/RVA pointer route, and no original helper-name context for the raw starts.
- 2026-06-26 B007 implementation of accepted source-quality report:
  - Changed to: `COMPLETION:88`, `CONFIDENCE:90`; owner/emitter remain [UID:0000IE][Config](by-file/Config.md).
  - Summary/evidence: accepted MCP session `80de0a67` reconfirmed no function objects, no xrefs, absent pointer-byte route, and absent PE direct call/jump route for both raw starts; corrected the stale local-buffer wording to 20 UTF-16 characters / 40 bytes; resolved the fixed row slab at `Config + 0x4`, stride `0x6894`, 100 rows, count at `+0x28d9d4`, and row field meanings; tied `g_pApplication + 0x858` to `currentServerName` / `selectedServerNameDisplay`; and inserted formal first-draft C++. The no-direct-route condition now caps score instead of keeping C++ blank.

## 2026-07-15 UID0003UP Fixed-Table Helper Callback

### Exact helper behavior

- `FindOrAppendFixedConfigTableRow` calls `GetLocalPlayerName(wchar_t *, size_t)` into a local `wchar_t[40]` with capacity `0x28`, compares player-name and current-server text against active typed rows, returns the first match, or increments the active count and returns the append index. The helper's return is not confused with profile serialization or filename-stem behavior.
- `ResetFixedConfigTableRows` iterates all 100 `ConfigCharacterServerMacroRow` entries and clears player/server text headers, selected profile/index state, both twenty-row macro banks' enabled/name/key/text fields, and trailing state exactly as the accepted body specifies.
- The typed row uses player/server identity and primary/alternate macro banks. Historical empty-key, generic `stateBlock`, wide-value A/B, and giant reserved-array models are rejected as current source shape.
- The helpers remain Config-owned, reconstructable, at the exact retained range with unchanged `88/90` score and no direct modeled caller. Lack of a direct route or original symbol caps lexical confidence but does not justify a blank marker or schematic body.
- No extra bounds guard, count clamp, allocation check, or error propagation is introduced; retail append/reset behavior remains exact.
