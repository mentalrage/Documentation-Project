*** UID:0003GY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:NONE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: MCP session `ff68e691` confirms stale-named `___std_fs_create_symbolic_link@8` is a 20-byte wrapper that preserves `ecx`, pushes literal `0`, forwards its two stack arguments, calls `0x00596960` / Thread queue dispatch, and returns `retn 8`; `xrefs_to`, code/data/immediate refs, VA/RVA byte-route checks, import/string checks, and FileDownloader-global controls find no caller, pointer, vtable/table, branch, endpoint, filesystem, FileDownloader, or source-placement route, so keep no-owner/non-emitting with blank formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0041b2d0-0x0041b2e4 Unreferenced Thread Dispatch Wrapper

## Status

- Address range: `0x0041b2d0-0x0041b2e4`
- Entity kind: tiny thread-queue dispatch wrapper / unreferenced source helper.
- Current IDA name: `___std_fs_create_symbolic_link@8`; this name is treated as stale or misleading because the body has no filesystem behavior.
- Disposition: reconstructable source helper, but `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS` remain because no direct owner or source-output route is proven.
- Rebuild handling: `source-authored` or source-declared helper around the base thread queue post primitive.
- Owner/emitter route: `CANONICAL_OWNER:NONE` and blank `EMITTER_UIDS` until a direct owner or source-output route is proven above the assignment gate.

## Summary

This 20-byte body preserves `ecx` as the receiver, pushes a zero third argument, forwards its two explicit stack arguments to [UID:0000EV][Thread](by-class/Thread.md) queue-post helper `0x00596960`, and returns with `retn 8`.

The best forced semantic candidate is a generic `Thread`/worker-thread dispatch overload because the only callee is the documented base queue primitive. Assignment is still left blank because live IDA finds no ordinary callers, no vtable/data references to this exact start, no receiver type evidence, and no class-specific field access. Physical adjacency to the FileDownloader dispatch cluster is weak address-neighborhood evidence only.

## Raw Code Evidence

Live IDA MCP on 2026-06-12 (`b001_nexustk`) reports:

- `lookup_funcs 0x0041b2d0`: function `___std_fs_create_symbolic_link@8`, size `0x14`.
- `lookup_funcs 0x0041b2e4`: not a function, confirming the exclusive end.
- `disasm 0x0041b2d0`: `push ebp; mov ebp, esp; push 0; push [ebp+arg_4]; push [ebp+arg_0]; call sub_596960; pop ebp; retn 8`.
- `decompile 0x0041b2d0`: `return sub_596960(this, a2, a3, 0);`.
- `callees 0x0041b2d0`: only `0x00596960`.
- `xrefs_to 0x0041b2d0`: zero cross-references.
- Byte check around `0x0041b2c0` shows `0xcc` alignment before the function at `0x0041b2c9-0x0041b2d0`, this body at `0x0041b2d0-0x0041b2e4`, and `0xcc` alignment after it at `0x0041b2e4-0x0041b2f0`.

The callee [UID:0000EV][Thread](by-class/Thread.md) / [UID:0000OR][Thread](by-file/Thread.md) documentation identifies `0x00596960` as the generic asynchronous queue post primitive: it packs six dwords, queues the message, and signals the thread semaphore.

## Ownership Analysis

### Best candidate: Thread / utility threading

- Evidence for: the wrapper does nothing except forward to `Thread::DispatchRequest`-style queue posting with an explicit zero optional argument; the receiver remains in `ecx`, matching a method wrapper over a thread object; existing Thread docs already classify `0x00596960` as generic base-thread infrastructure used by downloader and packet/request callers.
- Evidence against: no caller, vtable, RTTI, constructor, or source-island evidence ties this exact wrapper to [UID:0000EV][Thread](by-class/Thread.md) rather than a derived thread class or file-local wrapper.
- Decision: strongest semantic candidate, but not attached because direct owner evidence is below the strict assignment gate.

### Weaker candidate: FileDownloader

- Evidence for: the wrapper is physically inside the broader [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md), immediately after the FileDownloader cash-shop version submit helper.
- Evidence against: no direct FileDownloader callers, no `dword_67A738` access, no downloader message constant, no FileDownloader fields, and no vtable reference. The wrapper is not used by the three documented FileDownloader submit helpers, which call `0x00596960` directly.
- Decision: rejected as a direct parent. Address adjacency alone is insufficient.

### Rejected candidate: MSVC filesystem/runtime helper

- Evidence for: IDA currently names the function `___std_fs_create_symbolic_link@8`.
- Evidence against: the body has no filesystem arguments, path handling, Windows file API calls, error translation, or CRT filesystem behavior; it forwards into the NexusTK thread queue helper.
- Decision: reject the current IDA name as stale/misapplied.

## Reconstruction Guidance

Keep this page `RECONSTRUCTABLE:TRUE` because the body is real source-side project helper code, not padding or CRT code. Do not emit final C++ yet; the wrapper has no ordinary callers and its direct class/file owner is still unresolved. If later evidence finds a matching source signature or caller, the likely attachment should be to [UID:0000EV][Thread](by-class/Thread.md) or the derived owner that actually declares the overload, not to the broad FileDownloader inventory.

## 2026-06-18 B001 Current Source-Routing Recheck

B001 rechecked the current source-route state using the current by-* docs and a local PE scan because live IDA MCP at `http://127.0.0.1:13337/mcp` was unreachable in that session. No IDA database edits were made. The live-IDA baseline remains the 2026-06-17 evidence already incorporated above and in the Thread/FileDownloader support pages.

Scanned PE:

```text
Path: E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe
MD5: 4247e04e20b65d6414c7238aa8ff5515
SHA256: 9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632
Image base: 0x00400000
Size: 2679296 bytes
```

Address translation used these sections:

```text
.text  0x00401000-0x0060c600  raw 0x00000400+0x20b600
.rdata 0x0060d000-0x0066c200  raw 0x0020ba00+0x05f200
.data  0x0066d000-0x0069ce24  raw 0x0026ac00+0x00d800
.rsrc  0x0069d000-0x006b2e00  raw 0x00278400+0x015e00
```

The target maps to raw file offset `0x0001a6d0`. The exact bytes are:

```text
55 8b ec 6a 00 ff 75 0c ff 75 08 e8 80 b6 17 00 5d c2 08 00
```

The neighboring bytes from `0x0041b2c0-0x0041b300` preserve the split-island shape:

```text
9c b6 17 00 5e 8b e5 5d c3 cc cc cc cc cc cc cc
55 8b ec 6a 00 ff 75 0c ff 75 08 e8 80 b6 17 00 5d c2 08 00
cc cc cc cc cc cc cc cc cc cc cc cc
c7 05 38 a7 67 00 00 00 00 00 c3 cc cc cc cc cc
```

The PE scan found no absolute VA dword `0x0041b2d0`, no RVA dword `0x0001b2d0`, no `push 0x0041b2d0`, no generic immediate dword surface for the entry, no direct `call rel32`, `jmp rel32`, or `jcc rel32` targeting the entry, no real short branch targeting the entry, and no absolute VA/RVA pointer to endpoint `0x0041b2e4` or successor `0x0041b2f0`. A naive scan reported a short-branch candidate at `0x0041b2d6`, but that byte is the `0x75` operand byte inside `ff 75 0c` (`push [ebp+0x0c]`), not an instruction-boundary branch.

Positive controls confirmed the scan could find real routes around this island:

```text
Direct jump to successor helper 0x0041b2f0: rel32 jump at 0x005f670e
Immediate/global surface for dword_67A738 (0x0067a738): 9 hits, including 0x0041b2f2
Direct calls to callee 0x00596960: 22 call sites
```

The 22 direct call sites to the Thread queue primitive `0x00596960` are:

```text
0x0041b1e8
0x0041b253
0x0041b2aa
0x0041b2bf
0x0041b2db
0x00453a92
0x005282ed
0x0052838e
0x005283cb
0x00574aed
0x00574b0d
0x00574b26
0x00574b5d
0x00574b7a
0x00574b9b
0x00574c05
0x00574c2c
0x00574c94
0x00574cbc
0x00574d31
0x00574e2e
0x005752af
```

The exact target body occurs once, at `0x0041b2d0`. A wrapper-shape scan for `55 8b ec 6a 00 ff 75 ?? ff 75 ?? e8 .... 5d c2 08 00` found this target and one unrelated same-shape wrapper:

```text
0x0041b2d0 -> 0x00596960
0x0058ea60 -> 0x0058f690
```

The duplicate bytes at `0x0058ea60` are:

```text
55 8b ec 6a 00 ff 75 0c ff 75 08 e8 20 0c 00 00 5d c2 08 00
```

This duplicate same-shape wrapper weakens any owner-by-shape argument. The tiny default-argument forwarding pattern is not unique to this source island.

The stale `___std_fs_create_symbolic_link@8` name remains rejected after a fresh string/import route check. The PE has no import name containing `CreateSymbolicLink`, `Symbolic`, or `std_fs`; no wide `CreateSymbolicLinkW`; no ASCII `CreateSymbolicLinkA`; and no `___std_fs_create_symbolic_link` string. The only ASCII `CreateSymbolicLinkW` string is at `0x00634034`, and the only route found for it is a pointer/immediate use at `0x005c710d` in the MSVC runtime/import-support initializer area. That runtime API-name route belongs with the documented runtime support pages, not with this wrapper.

Ownership conclusions from this recheck:

- Thread remains a semantic dependency only. The target calls the Thread queue-post primitive, but there is no caller, pointer, vtable, table slot, declaration, source-neighbor route, or current Thread doc claim that makes this wrapper emitted Thread output.
- FileDownloader remains rejected as direct owner. The target is physically adjacent to FileDownloader-family helpers, but it does not read or write `dword_67A738`, use FileDownloader constants `10000`/`10001`/`10002`, access FileDownloader fields, appear in a FileDownloader vtable/method list, or have the caller/global route held by neighboring helpers.
- Derived worker families such as MiscWorkThread, Socket/packet send, CashShopRequest, and FileDownloader submit helpers are rejected because their assigned wrappers have separate singleton, vtable, caller, dispatcher, field-initialization, or source-neighbor evidence that this target lacks.
- Runtime/filesystem ownership remains rejected. The stale name and standalone API-name string are contradicted by the target body and by the runtime initializer route for `CreateSymbolicLinkW`.
- A new one-function source group remains unsupported because there is no source path, caller, type declaration, table entry, or cohesive family beyond this orphan wrapper.

The only safe source-facing description remains behavior-only pseudocode, not formal `RECONSTRUCTION_CPP`:

```cpp
// Descriptive only. Do not use as formal RECONSTRUCTION_CPP without a proven emitter.
bool ThreadDispatchRequestDefaultParamWrapper(Thread *thread, unsigned int messageId, void *payload) {
    return thread->DispatchRequest(messageId, payload, 0);
}
```

Safe descriptive names are `UnreferencedThreadDispatchWrapper` and `ThreadDispatchRequestDefaultParamWrapper`. Avoid `___std_fs_create_symbolic_link@8`, `create_symbolic_link`, filesystem/path helper names, FileDownloader method names, and CashShopRequest method names. The receiver should be described only as Thread-like or queue-owner-like until a caller or table route proves a concrete type; `arg_0` is a message/request id or dispatch code, `arg_4` is a payload/context pointer, and the constant fourth argument is `0`.

## 2026-06-22 Rule 26 Archived B-Agent Cluster Incorporation

Rule 26 audit incorporated the archived B-agent report cluster for this target:

- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-current-goal2-pass.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-current-no-owner-research.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-current-no-owner-sweep.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-fresh-no-owner-pass.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-goal2-verification.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-live-goal2-no-owner-pass.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-post-migration.md`
- `Agent-B001/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-post-migration-recheck.md`
- `Agent-B002/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-restart-no-owner-research.md`
- `Agent-B003/research/executed/older/0003GY-UnreferencedThreadDispatchWrapper-second-pass.md`

All ten archived reports reached the same current-model result: keep this page `COMPLETION:86`, `CONFIDENCE:86`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank formal C++. The reports are not treated as "no work needed" artifacts; their valid evidence is consolidated below so the target page carries the reasoning directly instead of depending on archived research.

### Current Metadata And Coverage State

The generated memory coverage state checked by the archived reports listed this target as no-owner with `CANONICAL_OWNER:NONE`, blank emitters, no generated source file, and no C++ body. A later 2026-06-23 B001 MCP-backed redo supersedes the older "manual coverage row already matched" wording: current manual `by-memory/-coverage-report.md` did not contain a standalone `0003GY` row before that pass, even though generated coverage and by-* docs already matched the same no-owner/non-emitting decision. B001 supplied exact supervisor-owned coverage insertion text and did not edit coverage directly.

This is a valid current-model reconstructable no-owner/non-emitting item. It is not a dead coverage mistake: the body is real project code and should remain visible until a future caller, table, debug/source, or receiver-origin route proves where it belongs. Blank `EMITTER_UIDS` is intentional because there is no source-use context and no formal C++ block to route.

### Function Boundary And Body Evidence

Multiple live IDA sessions (`b001_0003gy`, `b001_0002bd`, and `a001_goal2_class_batch`) and raw PE scans agree on the exact body and boundaries:

| Address | Evidence decision |
| --- | --- |
| `0x0041b270` | Previous modeled helper, `sub_41B270`, size `0x59`, the FileDownloader cash-shop version submit helper with its own caller route. |
| `0x0041b2c9-0x0041b2d0` | Seven `0xcc` bytes. No fallthrough from the previous helper. |
| `0x0041b2d0-0x0041b2e4` | This exact `0x14` / decimal 20 byte function, currently stale-named `___std_fs_create_symbolic_link@8` in IDA. |
| `0x0041b2e4-0x0041b2f0` | Twelve `0xcc` bytes. No fallthrough into the following helper. |
| `0x0041b2f0` | Next modeled helper, `sub_41B2F0`, size `0x0b`, FileDownloader constructor-unwind global clear with an xref from `0x005f670e`. |

The target bytes are:

```text
55 8b ec 6a 00 ff 75 0c ff 75 08 e8 80 b6 17 00 5d c2 08 00
```

The disassembly is:

```asm
0041b2d0  push ebp
0041b2d1  mov  ebp, esp
0041b2d3  push 0
0041b2d5  push [ebp+arg_4]
0041b2d8  push [ebp+arg_0]
0041b2db  call sub_596960
0041b2e0  pop  ebp
0041b2e1  retn 8
```

Hex-Rays renders it as a `thiscall` wrapper equivalent to `return sub_596960(this, a2, a3, 0);`. The `BOOL`, `HANDLE *`, and symbolic-link name in that decompile are type/name pollution from the stale IDA label. The machine code preserves `ecx` as a receiver, forwards the two stack arguments, supplies literal zero as the third queue payload field, and returns with `retn 8`.

The sole callee is `0x00596960`. Current [UID:0000OR][Thread](by-file/Thread.md), [UID:0000EV][Thread](by-class/Thread.md), [UID:0001JX][0x00596250-0x0059756e.ThreadAndThreadMan](by-memory/0x00596250-0x0059756e.ThreadAndThreadMan.md), and [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) documentation identify `0x00596960` as the generic base `Thread::DispatchRequest`-style queue-post primitive. Its body builds a six-dword `ThreadMessage` record `{messageId,arg1,arg2,0,0,0}`, queues it through `this+0x4c`, and signals the primary wake semaphore at `this+0x0c`.

### Reference And Raw-Route Evidence

IDA and raw scans found no route into this function:

- `xrefs_to 0x0041b2d0`: zero ordinary inbound xrefs.
- `xrefs_to 0x0041b2e4`: zero endpoint/tail xrefs.
- `trace_data_flow` backward from `0x0041b2d0`: no incoming route beyond the target start node.
- Raw absolute VA dword `0x0041b2d0` (`d0 b2 41 00`): zero hits.
- Raw RVA dword `0x0001b2d0` (`d0 b2 01 00`): zero meaningful hits to the start.
- `push 0x0041b2d0`: zero hits.
- Real rel32 call/jump, short branch, or near conditional branch target to `0x0041b2d0`: zero hits after instruction-boundary review.
- Vtable/data/function-pointer/table evidence: none found in IDA xrefs or raw pointer scans.

Positive controls and contrasts worked:

- `0x00596960` has 22 direct call sites across FileDownloader request helpers, MiscWorkThread-style request wrappers, Socket command wrappers, packet-send helpers, and this wrapper. That broad fan-in supports base Thread ownership for the callee, but not direct ownership for this caller wrapper.
- `0x0041b270` has a caller at `0x0041c21b` and FileDownloader request semantics that this target lacks.
- `0x0041b2f0` has the FileDownloader constructor-unwind xref at `0x005f670e` and clears `dword_67A738`; this target does not touch the global.
- Direct jump/pointer scans find the following helper and `dword_67A738` routes, proving the scan method can find nearby real routes.

Two raw-scan false positives were explicitly rejected:

- A naive short-branch candidate at `0x0041b2d6` or `0x0041b2d9` is inside the operand bytes of `ff 75 0c` / `ff 75 08` (`push [ebp+arg_*]`), not an instruction-boundary branch.
- B003 found two raw RVA-range byte-pattern hits at `0x0047d648` and `0x0049f16b`, but both bytes are `E8` call displacements in unrelated code, not loaded RVA/data pointers to this target.

### Wrapper Shape And Stale-Name Evidence

The exact target body occurs once at `0x0041b2d0`. A broader wrapper-shape signature for `push 0; push arg; push arg; call helper; retn 8` occurs twice:

```text
0x0041b2d0 -> 0x00596960
0x0058ea60 -> 0x0058f690
```

The duplicate `0x0058ea60` function is also stale-named in the `___std_fs_create_symbolic_link@8` family, is also `0x14` bytes, has zero inbound xrefs in the reports, and calls unrelated `0x0058f690`. Existing documentation ties `0x0058f690` to TextEditPane-style coordinate/navigation behavior, not filesystem behavior. This duplicate is useful negative evidence: the IDA name and tiny wrapper idiom are not reliable owner/source-name proof for this target.

The filesystem/runtime hypothesis is rejected:

- The target has no path/string arguments, no filesystem API call, no error/status translation, no CRT/STL filesystem helper behavior, and no import/IAT relation.
- Searches found no `___std_fs_create_symbolic_link` string.
- Raw `.rdata` does contain ASCII `CreateSymbolicLinkW` at `0x00634034`, but the only route found for that string is the MSVC/runtime API resolver initializer area around `0x005c710c`/`0x005c710d`, alongside other API-name strings such as `FlsAlloc`, `CreateEventExW`, `CreateSemaphoreW`, `GetCurrentProcessorNumber`, `GetCurrentPackageId`, and `GetTickCount64`.
- No target xref, pointer, branch, import, or behavior connects `0x0041b2d0` to that API-name string.

Historical correction: older shorthand that said "no symbolic-link import lead" was too broad if read as "no symbolic-link string exists anywhere." The correct current statement is narrower: `CreateSymbolicLinkW` exists as an API-resolver string, but it has no route to this wrapper and does not support the stale IDA name.

### Candidate Owner And Emitter Decisions

Accepted current state:

- `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, `RECONSTRUCTABLE:TRUE`.
- Reason: this is real project code, but no direct declaration owner, receiver-origin route, source-use context, or generated-output emitter is proven.

Rejected candidates:

- [UID:0000OR][Thread](by-file/Thread.md) / [UID:0000EV][Thread](by-class/Thread.md): strongest forced semantic candidate because the sole callee is the base `Thread` queue-post primitive and the body looks like a default-argument overload. Rejected for current assignment because the wrapper sits outside the Thread island and has no caller, vtable slot, constructor route, receiver-origin proof, source-neighbor proof, raw pointer, or declaration evidence tying this exact wrapper to `Thread.cpp`. The callee's owner is not automatically the caller wrapper's declaration owner.
- [UID:0000JC][FileDownloader](by-file/FileDownloader.md) / [UID:00004W][FileDownloader](by-class/FileDownloader.md): rejected. The target is physically inside the mixed FileDownloader dispatch inventory and between FileDownloader-related neighbors, but it is separated by `0xcc` padding, has no `dword_67A738` access, no downloader message id `10000`/`10001`/`10002`, no request payload allocation, no FileDownloader fields, no FileDownloader vtable/data ref, and no caller passing the FileDownloader singleton. Neighboring helpers have positive FileDownloader evidence; this one does not.
- CashShopRequest/CashShopVersionRequest: rejected. The predecessor posts a cash-shop version request, but this target has no payload object, request vtable, message id, cash-shop caller, or request-field access.
- Socket / packet sender routes: rejected. Socket owns other wrappers over `0x00596960` because those wrappers have receiver globals, packet buffers, command ids, dispatcher consumers, and callers. This target has none of those route proofs.
- ProtocolSend / Session placeholders: rejected. Existing project-structure notes leave those as placeholders without a proven route, and this wrapper has no caller cluster to promote either source root.
- CRT/STL/filesystem: rejected for the stale-name reasons above.
- Compiler/linker ignored artifact or `RECONSTRUCTABLE:FALSE`: rejected. The body is executable NexusTK project code calling an internal Thread queue primitive; it is not padding, an import thunk, EH metadata, RTTI, scalar deleting destructor glue, or pure runtime code.
- `CANONICAL_OWNER:NONE` with one or more emitters: rejected. That model fits pooled/shared source data with proven independent source-use contexts. This is a function with zero source-use contexts and no formal C++ body.
- New one-function source group: rejected. No table, source path, debug breadcrumb, caller cluster, object-file cluster, declaration family, or coherent related item set proves a new owner around a single unreferenced wrapper.

### Support-Document Cross-Check

The support pages already carry the conclusions needed by this target:

- [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md) is a reviewed non-emitting mixed inventory and lists this target as a real parent-blank child rather than absorbing it into FileDownloader output.
- [UID:0000OR][Thread](by-file/Thread.md) and [UID:0000EV][Thread](by-class/Thread.md) treat this wrapper as dependency-only semantic evidence for `0x00596960`, not as an emitted Thread child.
- [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) documents `0x00596960` as base Thread message dispatch infrastructure with broad caller fan-in.
- FileDownloader and Socket pages document what sufficient routed wrappers look like: concrete caller/receiver/global/message/dispatcher evidence. This target fails those same thresholds.

### Reconstruction Guidance From The Cluster

Do not enter formal `RECONSTRUCTION_CPP` for this page yet. The target is above the minimum research gate but has no owner/emitter route and no final source placement. The descriptive pseudocode in this page remains behavior evidence only:

```cpp
// Descriptive only. Do not use as formal RECONSTRUCTION_CPP without a proven emitter.
bool ThreadDispatchRequestDefaultParamWrapper(Thread *thread, unsigned int messageId, void *payload) {
    return thread->DispatchRequest(messageId, payload, 0);
}
```

If future evidence identifies a real caller, vtable/data slot, raw pointer table, source/debug symbol, link map entry, matching source declaration, dispatcher/case relationship, or receiver-origin route, rerun the owner/emitter gate and prefer that proven route over the current callee-only Thread inference.

## 2026-06-23 B001 MCP-Backed Redo

B001 redid the source-quality pass with active MCP session `ff68e691` after the older active report was rejected for MCP-unavailable fallback evidence. MCP `server_health` was `ok` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `lookup_funcs`, `get_bytes`, `make_signature_for_range`, `disasm`, `decompile`, `analyze_function`, `xrefs_to`, `find`, `find_bytes`, `imports_query`, `find_regex`, `entity_query`, and `trace_data_flow` reconfirmed the existing no-owner/no-emitter result. The historical 2026-06-18 local PE/manual-fallback section remains useful as history, but this `ff68e691` pass is the current accepted evidence.

Current MCP confirms `0x0041b2d0` is `___std_fs_create_symbolic_link@8`, size `0x14` / decimal 20, and `0x0041b2e4` is not a function start. The exact body is bounded by seven `0xcc` bytes at `0x0041b2c9-0x0041b2d0` and twelve `0xcc` bytes at `0x0041b2e4-0x0041b2f0`. Its bytes are unchanged:

```text
55 8B EC 6A 00 FF 75 0C FF 75 08 E8 80 B6 17 00 5D C2 08 00
```

The body preserves `ecx`, pushes literal `0`, forwards `[ebp+arg_4]` and `[ebp+arg_0]`, calls `0x00596960`, and returns with `retn 8`. MCP `analyze_function 0x00596960` reconfirmed the callee as the base Thread queue-post primitive: it builds a six-dword message record, writes the three supplied fields followed by three zero fields, queues through receiver offset `+0x4c`, and releases the wake semaphore at receiver offset `+0x0c`. This keeps Thread as the best forced semantic family, but only as a dependency; the callee's ownership does not prove this wrapper's declaration owner.

Current no-route checks found no start or endpoint xrefs, no caller, no backward data-flow edge into the target, no code/data/immediate refs to `0x0041b2d0` or `0x0041b2e4`, no VA/RVA byte-pointer route, no `push 0x0041b2d0`, no vtable/table/global route, no import/string route, and no source-placement route. Positive controls in the same MCP pass found the successor helper jump to `0x0041b2f0`, nine `dword_67A738` data refs, the `CreateSymbolicLinkW` runtime `GetProcAddress` route at `0x005c710c`, and all 22 code refs to `0x00596960`, so the absence of routes into `0x0041b2d0` is meaningful.

The stale symbolic-link name remains rejected. `imports_query` found no `CreateSymbolicLink`, `Symbolic`, or `std_fs` imports; `find_regex` found only the unrelated `too many symbolic link levels` string at `0x00633d98` with no xrefs and `CreateSymbolicLinkW` at `0x00634034` routed through `?initialize_pointers@@YAHXZ` / `GetProcAddress`, with no target relation. MCP also reconfirmed the duplicate same-shape stale-named wrapper at `0x0058ea60`, which calls unrelated `0x0058f690`; the tiny wrapper idiom and IDA name are not owner/source-name proof.

Accepted owner/source-placement decision remains unchanged. Reject direct Thread ownership because there is no caller, vtable/data pointer, source-neighbor, receiver-origin, or declaration route to this exact wrapper. Reject FileDownloader because the body has no `dword_67A738` access, downloader message id, payload setup, FileDownloader field access, or singleton caller route, despite address adjacency. Reject derived-worker, runtime/filesystem, ProtocolSend/Session, `RECONSTRUCTABLE:FALSE`, no-owner-with-emitters, and new one-function source-group outcomes for the same absence of route proof. Keep `COMPLETION:86`, `CONFIDENCE:86`, `CANONICAL_OWNER:NONE`, `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, blank `EMITTER_POSITION_OPTIONAL`, and blank formal C++.

Formal C++ remains intentionally blank. The body is mechanically reconstructable, but current MCP still finds no valid source emitter. Emitting a helper-only `Thread`, FileDownloader, derived-worker, runtime/filesystem, ProtocolSend/Session, or artificial new-file C++ body would create polluted pseudo-source. Revisit only if future evidence finds a real caller, pointer, vtable/table slot, source/debug/declaration route, dispatcher relationship, or receiver-origin path.

Coverage note: generated memory coverage lists [UID:0003GY] as no-owner/non-emitting, but current manual `by-memory/-coverage-report.md` did not contain a standalone `0003GY` row before this redo. B001 supplied exact supervisor-owned coverage insertion text in the report and did not edit any `-coverage-report.md` file.

## Cross-References

- [UID:0000WI][0x0041a670-0x0041b69f.FileDownloaderDispatch](by-memory/0x0041a670-0x0041b69f.FileDownloaderDispatch.md)
- [UID:0000EV][Thread](by-class/Thread.md)
- [UID:0000OR][Thread](by-file/Thread.md)

## Changes

- 2026-06-12 B001: Created exact child page for the previously hidden real-code span inside `0000WI`. Live IDA confirms exact boundaries, queue-post callee, zero inbound xrefs, and surrounding `0xcc` padding. `CANONICAL_OWNER:NONE` remains because Thread is the best semantic candidate but not proven as the direct owner.
- 2026-06-13 B003 second-pass owner/emitter review: kept `86/86`, `RECONSTRUCTABLE:TRUE`, `CANONICAL_OWNER:NONE`, blank `EMITTER_UIDS`, and blank reconstruction C++. Raw PE scans confirmed no absolute VA/RVA pointer, no real call/jump/branch into the target, no vtable/data slot, and no source-use context. The stale IDA `___std_fs_create_symbolic_link@8` name remains rejected: raw `.rdata` does contain `CreateSymbolicLinkW` in a generic API-resolver string area, but this wrapper has no call, pointer, branch, import/IAT relation, or filesystem behavior tied to that string.
- 2026-06-17 B002 source-quality reanalysis: current MCP session `b003_00018a_ime_comp_20260617` reconfirmed exact `0x14` function body, stale `___std_fs_create_symbolic_link@8` label, zero xrefs to `0x0041b2d0`, zero xrefs to endpoint `0x0041b2e4`, sole callee `0x00596960`, duplicate stale-name wrapper at `0x0058ea60` calling unrelated `0x0058f690`, and `CreateSymbolicLinkW` string with no import/xref relation. Local PE scan found one exact body hit, two wrapper-shape hits, no VA/RVA pointer hits to `0x0041b2d0`, no real branch/call target to the wrapper, and positive-control hits for `0x00596960`, `0x0041b2f0`, and `dword_67A738`. Keep `86/86`, `CANONICAL_OWNER:NONE`, blank emitters, `RECONSTRUCTABLE:TRUE`, and blank formal C++; the best source-facing semantic model is a retained Thread queue convenience wrapper equivalent to posting `{messageId, payload, 0, 0, 0, 0}` through `Thread::DispatchRequest`, but no Thread, FileDownloader, derived-worker, filesystem, runtime, or new-source-group owner route is direct enough to emit.
- 2026-06-18 B001 current-source-routing recheck: local PE scan of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`) reconfirmed the exact `0x0041b2d0` body as the only exact-body hit, found two same-shape wrappers (`0x0041b2d0 -> 0x00596960`, `0x0058ea60 -> 0x0058f690`), found no VA/RVA/immediate pointer or real direct branch/call target to `0x0041b2d0`, found 22 direct calls to `0x00596960`, found no `CreateSymbolicLink*` import, and routed the only `CreateSymbolicLinkW` string use to the MSVC runtime/API initializer area at `0x005c710d`. IDA MCP was unreachable in this B001 session, so current live-IDA facts remain those already incorporated from 2026-06-17. Keep `86/86`, `CANONICAL_OWNER:NONE`, blank emitters, `RECONSTRUCTABLE:TRUE`, and blank formal C++.
- 2026-06-22 Rule 26 archived B-agent cluster incorporation: audited ten pending archived reports from B001/B002/B003 and consolidated their valid details into this page. No metadata, owner, emitter, score, C++, split/merge, or shared coverage changes are warranted. Added the complete negative-route matrix, duplicate stale-name wrapper context, `CreateSymbolicLinkW` API-resolver-string correction, support-document cross-check, rejected-owner list, and future-evidence criteria for revisiting the no-owner/non-emitting state.
- 2026-06-23 B001 MCP-backed redo implementation: accepted report `B001-redo-unreferenced-thread-dispatch-wrapper-source-quality-0003GY-mcp-20260623` made session `ff68e691` the current accepted evidence, preserved the 2026-06-18 MCP-unavailable PE/manual pass as historical, corrected the manual coverage-state wording, and kept metadata/source-output unchanged: `86/86`, `CANONICAL_OWNER:NONE`, blank emitters, `RECONSTRUCTABLE:TRUE`, and blank formal C++.
