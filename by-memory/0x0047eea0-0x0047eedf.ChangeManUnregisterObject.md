*** UID:000100 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChangeMan::UnregisterObject(LObject *object)
{
    for (int index = m_changeList->GetCount() - 1; index >= 0; --index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry->owner == object || entry->listener == object)
            m_changeList->RemoveRange(index, 1);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChangeMan::UnregisterObject method; reverse-walks the sorted registration list and removes every ChangeManEntry whose LObject owner or LObject listener equals the supplied object. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047eea0-0x0047eedf ChangeMan UnregisterObject

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: method
- Owner class: [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- Likely source module: [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- Autogen parent: attached to [UID:00001K][ChangeMan](by-class/ChangeMan.md) at position `50`; first-draft formal C++ is populated under the current combined-score/emitter gate.
- Confidence: strong for behavior, medium for final method name.

## Behavior

`ChangeMan::UnregisterObject` removes every registration whose owner pointer or listener pointer equals the supplied object. It walks the registration list backward so removals do not invalidate unvisited indices.

This broad cleanup path is used by object teardown code, including `BackPane` destructor paths that must clear subscriptions before destroying UI objects.

## 2026-07-13 B003 UID0000ZW Type Synchronization

- The exact reverse traversal from `GetCount() - 1`, row fetch, owner/listener comparisons, and one-row removal are unchanged; walking backward preserves unvisited indices during compaction.
- The source argument is `LObject *object`, matching both `ChangeManEntry` pointer fields and the common listener/owner base proven by wrapper receiver constructor chains and primary-vtable callback routing.
- The two direct callers remain BackPane teardown paths. They prove broad object-lifecycle use but do not transfer ownership away from ChangeMan.
- This is distinct from filtered `Unregister`: it has no listener wildcard, message type, or all-messages argument and removes on either pointer equality.
- Owner/emitter UID00001K, position 50, exact range, ChangeMan.cpp route, and `Nested:0` are preserved. Exact original overload spelling remains inferred; behavior and concrete pointer type are resolved.

## B002 2026-06-16 Source-Quality Recheck

B002 rechecked this method as part of the ChangeMan method source-quality audit. The method remains direct [UID:00001K][ChangeMan](by-class/ChangeMan.md) class code emitted through the ChangeMan source unit. The two direct callers are [UID:00000S][BackPane](by-class/BackPane.md) teardown paths that pass BackPane `this` through [UID:0000QI][g_pChangeMan](by-global/g_pChangeMan.md); BackPane is a caller surface, not the owning class.

The best current source-facing behavior is `ChangeMan::UnregisterObject(void *object)`. It reads `changeList->count`, walks backward from `count - 1`, fetches each [UID:0001TV][ChangeManEntry](by-type/by-struct/ChangeManEntry.md) through the [UID:0000DF][SortedList](by-class/SortedList.md) get slot `+0x10`, and removes one entry through the remove slot `+0x1c` when the supplied object matches either `entry->owner` or `entry->listener`. This is broad object cleanup, not the filtered unregister method and not listener-only cleanup.

The Rule 26 closeout preserves B002's instruction-level checks: `0x0047eea7-0x0047eead` derives `count - 1`, `0x0047eeb6-0x0047eebc` fetches an entry through virtual slot `+0x10`, `0x0047eebf` compares the entry owner, `0x0047eec3` compares the entry listener, `0x0047eecb` pushes remove count `1`, `0x0047eed0` calls the remove slot `+0x1c`, and `0x0047eed3` decrements the reverse-scan index. The two BackPane teardown callers restore BackPane vtables and then call this broad cleanup through `g_pChangeMan`, proving a source-authored lifecycle helper rather than dead code, filtered unregister, or compiler glue.

The score was raised to `86/91` because behavior, direct BackPane reachability, owner/listener field semantics, removal helper usage, owner/emitter route, and rejected filtered/unreferenced/compiler-artifact alternatives were documented. B009's 2026-07-01 empty-emitter implementation raises the page to `88/92` and populates first-draft formal C++ because exact broad-unregister overload spelling remains a source-name caveat rather than a behavior blocker.

## Raw Code Evidence

IDA raw disassembly on 2026-05-30 confirms:

- `0x0047eea7-0x0047eead` loads `m_changeList->count - 1`.
- `0x0047eeb0` exits immediately when the list is empty.
- `0x0047eebc` calls the list vtable slot `+0x10` to fetch each entry by index.
- `0x0047eebf-0x0047eec6` compares the supplied object against `entry.owner` and `entry.listener`.
- `0x0047eec8-0x0047eed0` calls the list vtable slot `+0x1c` with `(index, 1)` when either field matches.
- `0x0047eed3-0x0047eed6` decrements the index and continues while nonnegative, giving a reverse walk that is stable under removals.

## C++ Reconstruction Candidate

```cpp
void ChangeMan::UnregisterObject(LObject *object)
{
    for (int index = m_changeList->GetCount() - 1; index >= 0; --index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry->owner == object || entry->listener == object)
            m_changeList->RemoveRange(index, 1);
    }
}
```

`UnregisterObject` is the best descriptive name for the exact broad cleanup behavior. A final source tree may use an overload-style `Unregister(object)`, but `UnregisterListener` is too narrow because the binary checks both `owner` and `listener`.

## 2026-07-01 B009 Empty-Emitter Implementation

The accepted B009 report promoted the reconstruction candidate into the formal `RECONSTRUCTION_CPP CODE` block. Live MCP session `supervisor_resume_20260629` reconfirmed `lookup_funcs 0x0047eea0` as size `0x3f`, direct callers at `0x00467936` and `0x00467acd` from BackPane teardown functions, and no ordinary Application ownership for this broad cleanup method.

Bounded disassembly preserves the exact reverse-removal shape used by the formal C++: `0x0047eea7-0x0047eead` computes `count - 1`, `0x0047eeb0` exits if the list is empty, `0x0047eebc` fetches each entry through slot `+0x10`, `0x0047eebf` compares the owner field, `0x0047eec3` compares the listener field, `0x0047eed0` removes through slot `+0x1c` with count `1`, and `0x0047eed3-0x0047eed6` decrements and repeats while nonnegative. Filtered-unregister, listener-only, compiler-generated, BackPane-owned, and no-owner interpretations remain rejected.

## Evidence

- IDA MCP `lookup_funcs` reports `0x0047eea0` as size `0x3f`, giving end-exclusive range `0x0047eedf`.
- IDA MCP `decompile 0x0047eea0` shows a reverse loop from `count - 1` to `0`, an entry fetch through vtable slot `+0x10`, owner/listener comparisons, and remove-at through vtable slot `+0x1c`.
- 2026-05-26 IDA MCP recheck reconfirmed size `0x3f` and the two direct BackPane teardown callers at `0x00467936` and `0x00467acd`.
- 2026-05-30 IDA raw disassembly recheck confirms the reverse index loop and the exact sorted-list fetch/remove vtable slots.

## Cross-References

- [UID:0000ZU][0x0047ec70-0x0047efae.ChangeMan](by-memory/0x0047ec70-0x0047efae.ChangeMan.md)
- [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- [UID:00000S][BackPane](by-class/BackPane.md)
- [UID:0001TV][ChangeManEntry](by-type/by-struct/ChangeManEntry.md)

## Changes

- 2026-06-07 A007: Set `AUTOGEN_PARENT_UID` to [UID:00001K][ChangeMan](by-class/ChangeMan.md) with position `50`. Evidence: the page documents the IDA-verified broad unregister method and the class page lists this exact range in its method map. No score or final C++ change.
- 2026-06-16 B002 source-quality recheck: raised `82/90` to `86/91`. Evidence: report-only audit rechecked the reverse count-minus-one scan, SortedList get/remove slots, owner-or-listener object matching, two BackPane destructor callers, ChangeMan owner/emitter route, rejected filtered/listener-only/compiler-artifact alternatives, and final-C++ blockers.
- 2026-05-30: Updated completion/confidence from `50/65` to `82/90`; added raw reverse-loop evidence, exact sorted-list slot offsets, and a C++ reconstruction candidate. Evidence is from IDA MCP decompilation, xrefs, and raw disassembly recheck of `0x0047eea0-0x0047eedf`.
- 2026-05-31: `RECONSTRUCTABLE` changed from blank to `TRUE`. Evidence: the page documents an IDA-verified NexusTK broad unregister method used by object teardown paths.
- 2026-07-01 B009 empty-emitter implementation: changed score from `86/91` to `88/92` and inserted formal first-draft `void ChangeMan::UnregisterObject(void *object)` C++. Evidence: live MCP reconfirmed function size, two BackPane callers, reverse count-minus-one walk, owner/listener comparisons, and remove slot `+0x1c` with count `1`.
