*** UID:000344 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include <deque>

struct FolderTreeContentDimensionsContext
{
    FolderTreePane *pane;
    int rowCount;
    int maxWidth;
};

template <>
template <>
void Tree<FolderTreePane::TreeElem>::Traverse<FolderTreeContentDimensionsContext>(
    TreeItor<FolderTreePane::TreeElem> *start,
    FolderTreeContentDimensionsContext *context)
{
    std::deque<int> pending;
    pending.push_back(start->GetIndex());

    while (!pending.empty())
    {
        const int nodeIndex = pending.back();
        pending.pop_back();

        TreeNode<FolderTreePane::TreeElem>& treeNode = m_nodes[nodeIndex];
        FolderTreePane::TreeElem& node = treeNode.data;

        int depth = 0;
        for (int parentIndex = treeNode.parentIndex; parentIndex != -1;
             parentIndex = m_nodes[parentIndex].parentIndex)
        {
            ++depth;
        }

        node.depth = static_cast<short>(depth);
        node.rowIndex = static_cast<short>(context->rowCount);

        const int rowWidth =
            context->pane->GetTextWidth(node.name.c_str()) + (16 * (depth + 1));
        if (context->maxWidth < rowWidth)
        {
            context->maxWidth = rowWidth;
        }

        ++context->rowCount;

        if (node.expanded && treeNode.lastChildIndex != -1)
        {
            for (int childIndex = treeNode.lastChildIndex; childIndex != -1;
                 childIndex = m_nodes[childIndex].previousSiblingIndex)
            {
                pending.push_back(childIndex);
            }
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Formal content-dimension traversal helper; walks visible FolderTreePane tree nodes with a deque-backed pending list, writes row/depth, measures names, updates max width/row count, and queues expanded children without hidden-node filtering. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3400-0x004b3645 FolderTreeContentDimensionsTraversal

## Current Accepted UID0004YK Consumer Policy

This body remains FolderTreePane-owned content-dimension policy over the generic Tree traversal shape. Its storage references now use Tree<T>::m_nodes; row/depth writes, name measurement, width/count accumulation, expansion rule, and exact 0x004b3400-0x004b3645 range remain consumer-specific evidence. The shared deque depth-first skeleton is authored once in Tree.h.

## Status

- Disposition: reconstructable NexusTK source helper.
- Entity kind: `FolderTreePane` visible-tree traversal callback for content-size calculation.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: file-local helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), not a standalone class method. The only ordinary caller is `FolderTreePane::CalculateContentDimensions` at `0x004b2f60`.
- Rebuild handling: `source-authored`; this is filesystem tree-control traversal logic.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b3400-0x004b3645` | `FolderTreeContentDimensionsTraversal` | Walks visible folder-tree nodes, assigns row/depth words, measures node-name text width, updates content width/row count, and queues expanded children. |

## IDA Evidence

- 2026-06-10 IDA MCP `lookup_funcs 0x004b3400` reports `sub_4B3400`, size `0x245`, giving exact exclusive end `0x004b3645`.
- 2026-06-10 IDA MCP `callers 0x004b3400` reports a single ordinary caller at `0x004b2fbf` inside `sub_4B2F60`, the documented `FolderTreePane::CalculateContentDimensions` path.
- 2026-06-10 IDA MCP `callees 0x004b3400` reports traversal/container and text-measure helpers: `0x005c74f6`, `0x004baa70`, `0x004a88e0`, `0x0043e0b0`, `0x00439e00`, and `0x00584540`.
- 2026-06-10 decompilation shows the helper creates a traversal work queue, seeds it from the passed tree iterator/range, writes `TreeElem` row/depth words at offsets `+0x18/+0x1a`, obtains the node-name text pointer through `0x00584540`, measures it through `0x004baa70`, updates the maximum content width and visible row count in the caller-provided accumulator, and enqueues first-child/previous-sibling links when the node's expanded byte at `+0x1d` is set.
- The field offsets match [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md): row at `+0x18`, depth/indent at `+0x1a`, expanded flag at `+0x1d`, and child/sibling link dwords.
- 2026-06-28 B015 restore refresh used active MCP session/database `agent_b009_0002my_20260628` after supervisor confirmed MCP was restored. `server_health` id `301` returned `status:ok`, imagebase `0x400000`, `hexrays_ready:true`, and `strings_cache_ready:true`; all B015 MCP calls were read-only.
- `lookup_funcs` id `302` reconfirmed `sub_4B3400` at `0x004b3400` with size `0x245` and no function at exclusive end `0x004b3645`. `get_bytes` id `307` reconfirmed `int3` padding before the prologue, `ret 8` before `0x004b3645`, and padding before the next function at `0x004b3650`.
- `xrefs_to` id `303` reconfirmed exactly one code xref to the target, callsite `0x004b2fbf` inside `sub_4B2F60`, and no xrefs to `0x004b3645`. Negative read-only checks `find` ids `308` through `310` and `find_bytes` id `311` found no boundary data refs, immediate refs, or VA/RVA pointer encodings.
- `decompile` ids `315` through `317` reconfirmed the target/caller/consumer route: `CalculateContentDimensions` builds a root `TreeItor<FolderTreePane::TreeElem>` over `this+0x130`, passes context fields `pane`, `rowCount`, and `maxWidth`, and after the target returns stores content extents from `maxWidth` and `rowCount`; `GetNodeBounds` consumes the written `rowIndex` and `depth`.
- The current MCP server did not expose a `callers` tool during B015's restore pass; attempted `callers` id `304` failed with `Method 'callers' not found`. Caller proof therefore uses `xrefs_to` id `303` and `analyze_function` id `306`.

## Ownership And Parent Gate

The direct semantic owner is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). This helper is reached only from the folder-tree content-dimension method and directly manipulates `FolderTreePane::TreeElem` visible-row state, but it is emitted as a helper function rather than a normal class method. The file parent currently scores `89/85`, so it clears the strict direct-parent gate.

The broad aggregate [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md) is only a split container and should not be the autogen parent.

## Reconstruction Notes

- UID000149 factorization: the exact source expression `pending.push_back(start->GetIndex())` calls the shared out-of-line `std::deque<int>::push_back` body at `0x004b3480`; later child-index appends inline the same push logic and call `_Growmap(1)` at `0x004b35ac` only on demand. Preserve the native `<deque>` expression and do not add a custom Deque dependency.

- Final source now emits as the concrete source-facing helper `Tree<FolderTreePane::TreeElem>::TraverseContentDimensions`, used by `FolderTreePane::CalculateContentDimensions`.
- The helper is not a public `FolderTreePane` method: the binary receiver is the embedded `Tree<FolderTreePane::TreeElem> *`, and the owning pane is passed through the three-field `FolderTreeContentDimensionsContext`.
- `std::deque<int>` is the accepted source shape for the observed Dinkumware/STL deque-like segmented pending container. The helper uses `push_back`, `back`, and `pop_back`, preserving the observed LIFO traversal without documenting raw compiler container internals.
- The accepted field names are `parentIndex`, `lastChildIndex`, `previousSiblingIndex`, `name`, `rowIndex`, `depth`, and `expanded`. The helper intentionally does not read `TreeElem +0x20` (`hidden`), so hidden-node filtering must not be imported into this traversal. `isHidden` is only superseded historical spelling and `visibleTraversalSkip` a descriptive alias.
- The remaining inferred names, `TraverseContentDimensions` and `FolderTreeContentDimensionsContext`, are source-facing names rather than recovered original symbols. B015 `type_query` id `312`, `entity_query` id `313`, and `search_text` id `314` found no original type/name/text proof for `FolderTree`, `ContentDimensions`, or `TreeItor`; this caps final-audit confidence but no longer blocks first-draft C++.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 90 | Exact range, sole caller, callee set, row/depth writes, text-width calculation, accumulator updates, child traversal behavior, direct file owner, parent-gate rationale, current MCP restore evidence, source-placement decision, rejected alternatives, and formal first-draft C++ are documented. |
| Confidence | 91 | IDA/MCP confirms the function boundary, caller, callee set, caller context, consumer of row/depth fields, no boundary pointer/data refs, and current generated empty-marker symptom. Confidence remains below final audit because exact historical helper/context names and shared tree-template header factoring are inferred. |
| Reconstructable | true | Source-owned folder-tree layout traversal required by rebuilt `FolderTreePane`. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis keeps this as a file-local traversal callback owned by [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). It supports `FolderTreePane::CalculateContentDimensions` by walking visible `TreeElem` rows, using `rowIndex`, `depth`, and name width to compute content extents. It should not be promoted to a public method or migrated into a generated standalone traversal class.

The accepted class layout context is `m_tree` at `this+0x130`, `m_selection` at `this+0x170`, and inherited viewport state in `ScrollablePane`. Any source C++ for this helper should stay in the exact child/helper context, while the class page emits only the declaration-level shell.

## 2026-06-28 B015 Accepted Implementation Sync

B015's accepted source-quality report changes this page from an empty-emitter source-quality blocker to a formal first-draft C++ emitter. Current generated output before this implementation still showed [UID:000344] as `Completion:86 | Confidence:88 | Empty Emitter Marker`; that symptom was caused by the blank formal C++ block, not by an owner/emitter route defect.

Accepted disposition:

- `COMPLETION` rises from `86` to `90`.
- `CONFIDENCE` rises from `88` to `91`.
- `CANONICAL_OWNER:0000JG`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000JG`, and blank `EMITTER_POSITION_OPTIONAL:` remain correct.
- The broad [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md) page remains a non-emitting inventory/container; this exact child owns the emitted body.

Rejected alternatives preserved from the accepted report: covered-by/no-code marker, clearing `EMITTER_UIDS`, moving direct ownership to [UID:00005A][FolderTreePane](by-class/FolderTreePane.md), moving ownership to GrafPort/text helpers, moving ownership to `LObject`/`StringBase`/runtime/STL pages, splitting the range, keeping blank C++ because names are inferred, and emitting the traversal as `std::stack<int>` instead of the evidence-preserving `std::deque<int>` operations.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)

## 2026-07-29 UID0002MZ Node/Payload Traversal Reconciliation

- Formal CPP remains the exact concrete consumer specialization, now expressed as `Tree<FolderTreePane::TreeElem>::Traverse<FolderTreeContentDimensionsContext>` rather than a FolderTree-specific generic API name.
- `TreeNode<TreeElem>` owns parent/last-child/previous-sibling links; row/depth/name/expanded are reached through `treeNode.data`.
- The exact deque traversal, depth walk, row assignment, text-width accumulation, expanded-child descent, caller/range/callee evidence, and lack of disabled-state filtering are preserved.
- Prior direct 36-byte `TreeElem` field access is retained as historical source-model evidence and superseded by the generic node plus 12-byte payload split. Score/route/range remain `90/91`, UID0000JG.

## Changes

- 2026-07-14 B001 UID0002N0 field-name synchronization:
  - Preserved `90/91`, owner/emitter UID0000JG, reconstructable true, blank position, and the complete accepted traversal body.
  - Updated only the existing negative evidence to final `hidden`: this aggregate traversal does not read `+0x20`; hidden filtering remains in the iterator/per-node paths.

- 2026-07-12 B003 UID000149 factorization sync: preserved score/route/body and added exact initial-call/later-inline `std::deque<int>` evidence; `<deque>` is supplied once by UID0001WP.

- 2026-06-28 B015 accepted implementation:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`, owner/emitter [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), and blank formal C++ produced an empty emitter marker in generated `FolderTreePane.cpp`.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`, owner/emitter unchanged at [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), and formal `Tree<FolderTreePane::TreeElem>::TraverseContentDimensions` C++ inserted.
  - Summary/evidence: B015 MCP restore session/database `agent_b009_0002my_20260628` reconfirmed the `0x004b3400` function boundary, exclusive end, sole caller from `CalculateContentDimensions`, callee set, caller context layout, row/depth consumer, negative boundary reference checks, and generated empty-emitter state. The helper walks visible nodes with a deque-backed pending list, writes `rowIndex`/`depth`, measures `name` through `GetTextWidth`, updates `maxWidth`/`rowCount`, queues expanded children through `lastChildIndex` and `previousSiblingIndex`, and does not read the `hidden` byte.

- 2026-06-10 B001-028 split repair:
  - What existed before: this exact traversal helper was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b3400-0x004b3645`.
  - Summary/evidence: IDA MCP confirms the `0x245`-byte function, sole caller in `CalculateContentDimensions`, text measurement callees, row/depth writes, and expanded-child traversal behavior.
