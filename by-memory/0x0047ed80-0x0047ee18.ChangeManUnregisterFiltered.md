*** UID:0000ZY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ChangeMan::Unregister(LObject *owner,
                           LObject *listener,
                           MessageType messageType,
                           bool allMessages)
{
    ChangeManEntry key;
    key.owner = owner;

    int index = m_changeList->FindFirstEqual(&key);
    if (index < 0)
        return;

    int count = m_changeList->GetCount();
    for (; index < count; ++index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry == NULL || entry->owner != owner)
            break;

        const bool listenerMatches =
            (listener == NULL || entry->listener == listener);
        const bool messageMatches =
            (allMessages == true) ||
            (allMessages == false && entry->messageType == messageType);

        if (listenerMatches && messageMatches) {
            m_changeList->RemoveRange(index, 1);
            --index;
            --count;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChangeMan::Unregister filtered method; lower-bounds by LObject owner, honors a null LObject listener wildcard and exact bool allMessages/MessageType filtering, removes matching rows, and compensates both index and cached count after compaction. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047ed80-0x0047ee18 ChangeMan UnregisterFiltered

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: method
- Owner class: [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- Likely source module: [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- Autogen parent: attached to [UID:00001K][ChangeMan](by-class/ChangeMan.md) at position `30`; first-draft source C++ is now populated under the active source-quality gate.
- Confidence: strong for behavior, medium for final method name.

## Behavior

The filtered `ChangeMan::Unregister` removes registrations for one owner, optionally narrowing by listener and message filter. It finds the first equal owner entry in the sorted list, scans forward while the owner still matches, and removes matching entries through the list remove/remove-at helper.

Match behavior:

| Filter | Match rule |
| --- | --- |
| listener pointer | `0` is a wildcard, otherwise must match entry offset `0x04`. |
| `bool allMessages` | exact `true` is an all-message wildcard. |
| all-messages/message type | exact `false` requires `MessageType` to match entry offset `0x0c`; invalid non-boolean bytes match neither branch. |

After a removal, the method decrements the current index and cached count before continuing, which prevents skipped entries after compaction. Source-facing return type is modeled as `void`: the inherited LObject wrapper/caller evidence proves only side effects and does not prove that a boolean result or removed-count value is consumed. The document file name may remain `ChangeManUnregisterFiltered`; the source-facing member name is filtered `ChangeMan::Unregister`, with exact original overload spelling still a non-blocking header question.

## 2026-07-13 B003 UID0000ZW Family Synchronization

- The exact method range, owner lower-bound, same-owner forward scan, null-listener wildcard, and remove/index/count compensation are unchanged.
- The explicit arguments are now source-modeled as `LObject *owner`, `LObject *listener`, `MessageType messageType`, and `bool allMessages`. The one-byte branch is ordinary boolean lowering, not a `ChangeMessageScope` enum.
- Match ordering is preserved exactly: `(allMessages == true) || (allMessages == false && entry->messageType == messageType)`. Invalid byte values remain negative binary evidence and match neither explicit branch.
- All seven containing wrapper callers pass `false`: five use `g_pBackPane` and two use `g_pApplication`. That receiver mix proves inherited LObject ownership for the wrapper and rejects direct Application/free-helper ownership.
- `void` remains source-faithful because no caller consumes EAX. Owner/emitter UID00001K, position 30, exact range, `Nested:0`, and ChangeMan.cpp placement remain unchanged.

## Raw Code Evidence

IDA raw disassembly on 2026-05-30 confirms the loop mechanics that are easy to lose in decompiler output:

- `0x0047ed98` calls `sub_4F3780(m_changeList, &key)` to find the owner lower-bound.
- `0x0047eda5` reads the current list count from `m_changeList + 0x0c`.
- `0x0047edbb` calls the list vtable slot `+0x10` to fetch the current entry.
- `0x0047edc1-0x0047edc3` breaks when `entry.owner != owner`.
- `0x0047edc5-0x0047edd4` implements listener wildcard matching: null listener matches all, otherwise the listener must equal `entry.listener`.
- `0x0047edd4-0x0047edee` implements scope/message matching: scope byte `1` matches all messages, scope byte `0` matches only `entry.messageType == messageType`, and other scope values fail.
- `0x0047edfa-0x0047ee06` calls list vtable slot `+0x1c` with `(index, 1)`, then decrements both index and cached count before the loop increment.

## Historical C++ Reconstruction Candidate Before 2026-07-13

The following pre-resolution draft is retained only to preserve the earlier exact loop reconstruction. Its `void *`, `ChangeListener`, `ChangeMessageScope`, and explicit enum vocabulary are superseded by the current managed `LObject *`, `MessageType`, and `bool allMessages` implementation above.

```cpp
void ChangeMan::Unregister(void *owner,
                           ChangeListener *listener,
                           unsigned int messageType,
                           ChangeMessageScope scope)
{
    ChangeManEntry key;
    key.owner = owner;

    int index = m_changeList->FindFirstEqual(&key);
    if (index < 0)
        return;

    int count = m_changeList->GetCount();
    for (; index < count; ++index) {
        ChangeManEntry *entry =
            static_cast<ChangeManEntry *>(m_changeList->GetElementAt(index));
        if (entry == NULL || entry->owner != owner)
            break;

        const bool listenerMatches =
            (listener == NULL || entry->listener == listener);
        const bool messageMatches =
            (scope == ChangeMessageScope_AllTypes) ||
            (scope == ChangeMessageScope_MatchType &&
             entry->messageType == messageType);

        if (listenerMatches && messageMatches) {
            m_changeList->RemoveRange(index, 1);
            --index;
            --count;
        }
    }
}
```

At that historical stage, `ChangeMessageScope` was a source-facing placeholder for the proven byte-sized field. The 2026-07-13 caller/family proof resolves the source declaration as ordinary `bool allMessages`; invalid stored byte values remain binary negative evidence rather than a reason to emit a source enum.

## 2026-06-19 B010 Source-Quality Implementation

- Score recommendation implemented: `84/90 -> 86/90`.
- Canonical owner/emitter remains [UID:00001K][ChangeMan](by-class/ChangeMan.md); the source module remains [UID:0000I2][ChangeMan](by-file/ChangeMan.md).
- First-draft C++ is now emitted for filtered `void ChangeMan::Unregister(...)`; boolean-return and removed-count interpretations are rejected because no caller proves result use.
- The key and scan use the accepted `ChangeManEntry` shape: `owner/listener/scope/messageType` at `+0x00/+0x04/+0x08/+0x0c`, total size `0x10`.
- The list operations belong to [UID:0000DF][SortedList](by-class/SortedList.md): `FindFirstEqual`, `GetElementAt`, count access, and remove/remove-at-one-element semantics. They are not `ChangeMan`-local helper ownership.
- Stale optional split endpoints `0x0047ed73`, `0x0047ee1c`, and `0x0047ef33` are rejected; the current exact range remains `0x0047ed80-0x0047ee18`.

## Evidence

- IDA MCP `lookup_funcs` reports `0x0047ed80` as size `0x98`, giving end-exclusive range `0x0047ee18`.
- IDA MCP `decompile 0x0047ed80` shows lower-bound helper `0x004f3780`, same-owner scan, wildcard listener handling, scope/message filtering, and remove-at through vtable slot `+0x1c`.
- IDA MCP caller/xref checks found the application wrapper at `0x004f4b50` as the relevant public call surface.
- 2026-05-26 IDA MCP recheck reconfirmed size `0x98` and the only direct caller/xref at `0x004f4b63` inside [UID:00019A][0x004f4b50-0x004f4b6c.LObjectUnregisterChangeListener](by-memory/0x004f4b50-0x004f4b6c.LObjectUnregisterChangeListener.md).
- 2026-05-30 IDA raw disassembly recheck confirms the fetch slot `+0x10`, remove-at slot `+0x1c`, the same-owner break, and the post-remove `--index`/`--count` compensation.

## Cross-References

- [UID:0000ZX][0x0047ed50-0x0047ee18.ChangeManRegistrationMethods](by-memory/0x0047ed50-0x0047ee18.ChangeManRegistrationMethods.md)
- [UID:0000ZU][0x0047ec70-0x0047efae.ChangeMan](by-memory/0x0047ec70-0x0047efae.ChangeMan.md)
- [UID:00001K][ChangeMan](by-class/ChangeMan.md)
- [UID:0000I2][ChangeMan](by-file/ChangeMan.md)
- [UID:00019A][0x004f4b50-0x004f4b6c.LObjectUnregisterChangeListener](by-memory/0x004f4b50-0x004f4b6c.LObjectUnregisterChangeListener.md)
- [UID:0001TV][ChangeManEntry](by-type/by-struct/ChangeManEntry.md)

## Changes

- 2026-06-07 A007: Set `AUTOGEN_PARENT_UID` to [UID:00001K][ChangeMan](by-class/ChangeMan.md) with position `30`. Evidence: the page documents the IDA-verified filtered unregister method and the class page lists this exact range in its method map. No score or final C++ change.
- 2026-05-30: Updated completion/confidence from `50/65` to `84/90`; added raw loop evidence, slot-level sorted-list calls, exact listener/scope/message matching behavior, and a C++ reconstruction candidate. Evidence is from IDA MCP decompilation, xrefs, and raw disassembly recheck of `0x0047ed80-0x0047ee18`.
- 2026-05-31: `RECONSTRUCTABLE` changed from blank to `TRUE`. Evidence: the page documents an IDA-verified NexusTK filtered unregister method with Application wrapper caller evidence.
- 2026-06-19 B010: Implemented accepted source-quality report `0000ZT-0000ZW-0000ZY-000101-000103-ChangeMan-source-quality.md`: completion changed to `86`, first-draft filtered `void ChangeMan::Unregister(...)` formal C++ added, scope semantics changed from bool-only wording to exact byte behavior, and stale below-gate no-code wording replaced.
