*** UID:0001HR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000NO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000NO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Mixed ServerSelectPane.cpp executable evidence with exact constructor, OnControlCommand, UpdateActionButton, list/helper, ordinary-cleanup, adjustor, scalar-wrapper, frame/hash and lifecycle boundaries; complete three-view class ABI linkage; selected-server packet behavior; raw-wrapper liveness limits; and UID0000NO/UID0000S8 source routing while formal aggregate CPP/H remains intentionally blank. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00573d20-0x005747df ServerSelectPane

## Status

- Completion: 91
- Confidence: strong for dialog/list behavior, exact function boundaries and hashes, corrected virtual identities, singleton ownership, the direct empty `Singleton<ServerSelectPane>` base, the complete three-view ABI family, `ServerSelectDirectory`/`ServerSelectEntry` layout, packet semantics, raw helper roles, and live xrefs; residual uncertainty is lexical spelling and raw-wrapper reachability.
- Covered file/module: [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md)
- IDA owner context: `ServerSelectPane`, `ServerSelectMenuItemList`, and adjacent raw helper islands.
- Disposition: reconstructable login/main-menu project code. Formal C++ remains blank on this aggregate because source emission belongs to exact method/helper children or a coordinated `ServerSelectPane.cpp` file reconstruction; this range mixes two classes, file-local helper code, raw no-function wrappers, compiler glue, and padding.

## Covered Ranges

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x00573d20-0x00574300` | `ServerSelectPane::ServerSelectPane(ServerSelectDirectory *serverDirectory)` | Builds classic/EPF `DLGSERV` server-selection dialog and server list. Caller `0x004f70db` passes `g_pConfig + 0x28db10`; constructor stores it at `this + 0x26c` and reads count at directory `+0x300`. The final `00` byte is the trailing byte of `ret 4`; padding begins at `0x00574301`. |
| `0x00574310-0x00574338` | `ServerSelectPane::~ServerSelectPane()` non-deleting body | Restores three `ServerSelectPane` vtables, clears [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md), and tail-jumps to base teardown; this is destructor behavior, not a public helper. |
| `0x00574340-0x00574430` | `ServerSelectPane::OnControlCommand(int controlIndex, int notifyCode)` | Primary vtable slot `+0x48` and `retn 8` prove the inherited two-argument control-command override. It handles OK/cancel; OK unwraps child id `3`, calls `ServerSelectMenuItemList::ApplySelectedServer()` from both layout branches, creates the dimmer transition, and closes. |
| `0x00574440-0x00574496` | `ServerSelectPane::UpdateActionButton()` | Primary vtable slot `+0x4c` proves the inherited action-button update. It updates action/scroll button enabled state from child id `3` list contents and the enabled-entry/count helper. |
| `0x005744a0-0x005744f8` | raw `ServerSelectMenuItemList::ServerSelectMenuItemList(ServerSelectPane *owner)` constructor-shaped bytes | Directly constructs a `ListPane` with row stride `4`, installs the three `ServerSelectMenuItemList` views, and stores owner at `this + 0x14c`. No direct xrefs/function object were found in current IDA. |
| `0x00574500-0x0057450e` | raw `ServerSelectMenuItemList::AddServerEntry(ServerSelectEntry *entry)` wrapper | Takes the address of the entry pointer and calls `ListPane::AddEntry`; no direct refs found. |
| `0x00574510-0x0057454e` | `ServerSelectMenuItemList::ApplySelectedServer()` | Reads inherited selected index at `+0x134`, fetches a `ServerSelectEntry *` through `GetItem`, copies `entry->name` to `g_pConfig + 0x2916d0`, then calls the nonstatic member `SendServerSelectPacket(entry->serverId, entry)`. |
| `0x00574550-0x00574561` | raw `ServerSelectMenuItemList::SelectServerEntryIndex(int index)` | Calls virtual slot `+0x78` with `(index, 0)`; no direct start refs found. |
| `0x00574570-0x005745ac` | raw `ServerSelectMenuItemList::ApplyServerEntry(const ServerSelectEntry *entry)` helper | Copies the selected entry name to `g_pApplication + 0x858` / current-server display state, then calls `SendServerSelectPacket`; IDA does not currently model this as a function. |
| `0x005745b0-0x0057465e` | private member `ServerSelectMenuItemList::SendServerSelectPacket(unsigned int serverId, const ServerSelectEntry *entry)` | Emits opcode `0x57`, subbyte `0`, selected id, sends three bytes, stores selected id at `g_pConfig + 0x28d9d8`, and formats endpoint text when `g_pConfig + 0x28d9dd` is clear. ECX at both callers proves the member ABI. |
| `0x00574660-0x0057466b` | `ServerSelectMenuItemList::OnItemActivated` | Primary slot `+0x7c`; forwards activation to `m_owner`. |
| `0x00574670-0x00574748` | `ServerSelectMenuItemList::DrawListEntry` | Primary slot `+0x80`; formats/draws server row text, updates selected server name, and subtracts 10 from the right edge in EPF mode. |
| `0x00574750-0x0057475a` | singleton clear helper | Tiny unwind/cleanup helper that clears [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md). |
| `0x0057475b-0x00574770` | `ServerSelectPane` adjustor thunks | Compiler-generated thunks forwarding to destructor. |
| `0x00574780-0x005747df` | `ServerSelectPane::ScalarDeletingDestructor` | Clears active-pane global, tears down base dialog pane, and conditionally deletes. |

## IDA MCP Evidence

Checked on 2026-05-23:

- IDA confirms every real function row above except `0x005744a0`, which is not currently a function start.
- IDA reports the main constructor caller at `0x004f70db`.
- IDA reports refs to `0x00574510` from `ServerSelectPane::OnControlCommand`, and refs to `0x005745b0` from the `0x00574510` helper.

Checked on 2026-05-24:

- `xrefs_to 0x00574510` reports only the two `ServerSelectPane::OnControlCommand` branch calls at `0x00574399` and `0x005743db`.
- Decompilation of `0x00574510` copies the selected entry name into `g_pConfig + 0x2916d0` and calls `0x005745b0` with the entry id byte and entry pointer.
- Decompilation of `0x005745b0` emits bytes `0x57`, `0`, and selected id through the packet send helper, stores selected id at `g_pConfig + 0x28d9d8`, and formats endpoint state from the selected entry when the alternate-connection flag is clear.
- `xrefs_to 0x0069b4ac` ties [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md) to main-menu cleanup/startup and the constructor/destructor family.

Checked on 2026-06-01:

- Live IDA MCP `lookup_funcs` reports `sub_573D20` size `0x5e1`, `sub_574310` size `0x29`, `sub_574340` size `0xf1`, `sub_574440` size `0x57`, `sub_574510` size `0x3e`, `sub_5745B0` size `0xae`, `sub_574660` size `0xb`, `sub_574670` size `0xd8`, `sub_574750` size `0xb`, `sub_57475B` size `0xb`, `sub_574766` size `0xb`, and `sub_574780` size `0x5f`. It reports `0x005744a0`, `0x005747df`, and `0x005747e0` as non-starts for this page; `0x005747e0` starts the following socket lifecycle function.
- Live IDA MCP `xrefs_to` confirms the constructor caller at `0x004f70db` inside `sub_4F6D80`; vtable data refs to action/update methods at `0x00624d70` and `0x00624d74`; only two code callers for `0x00574510` at `0x00574399` and `0x005743db`; the modeled call to `0x005745b0` at `0x00574546`; list-method vtable refs at `0x00624e40` and `0x00624e44`; destructor thunk/data refs to `0x00574780`; and seven refs to singleton slot `0x0069b4ac`.
- Live IDA MCP `callees` confirms constructor dependencies on dialog/pane construction, resource loading, list insertion, scroll/list control setup, and screen positioning helpers; `OnControlCommand` calls the server-selection helper, screen-dimmer allocation, close/slide helpers, and final dialog close; `UpdateActionButton` calls the enabled-entry/count helper; the packet helper calls byte writer `0x00575380` and send queue `0x00574bb0`; row drawing calls text-format/draw and color/font helpers.
- Decompilation confirms constructor branches for `DLGSERV.EPF`/`DLGSERV.PAL` high-resolution layout versus `DLGSERV.EPD`/`NPAL` classic layout, constructs a `ServerSelectMenuItemList`, inserts server records from the config array at `a2 + 192`, selects the previously configured server name when available, creates a scroll/list pane, and opens/registers the pane.
- Decompilation confirms `OnControlCommand` handles OK versus cancel paths, selects child id `3`, calls `0x00574510`, allocates a `252` byte transition/dimmer helper, and uses layout-specific screen/global pane pointers depending on layout branch before closing.
- Decompilation confirms `DrawListItem` formats `"%s : %s"` from server-entry name/description, adjusts the rect in high-resolution mode, changes drawing colors for selected rows, and copies the selected server name into application/session display state.
- Byte sweep confirms boundary/padding: `0x00573d15-0x00573d20`, `0x00574339-0x00574340`, `0x00574431-0x00574440`, `0x00574497-0x005744a0`, `0x0057465e-0x00574660`, `0x0057466b-0x00574670`, `0x00574748-0x00574750`, `0x00574771-0x00574780`, and `0x005747df-0x005747e0` are `0xcc` alignment. `0x00574300-0x00574310` starts with the constructor `retn 4` trailing byte (`00`) followed by alignment, so the modeled constructor range ending at `0x00574301` should be treated as exact even though the aggregate table keeps the conventional `0x00574300` row.

Checked on 2026-06-03:

- Live IDA MCP reconfirmed exact modeled helper functions `0x00574510-0x0057454e` and `0x005745b0-0x0057465e`, plus the caller/action function `0x00574340-0x00574431` and row draw function `0x00574670-0x00574748`.
- `xrefs_to 0x00574510` are only the two `OnControlCommand` branch calls at `0x00574399` and `0x005743db`; `xrefs_to 0x005745b0` are the modeled helper call at `0x00574546` and the raw wrapper call at `0x005745a1`.
- The raw packet wrapper starts at `0x00574570`, not `0x00574580`: `0x00574570-0x005745a9` is the wrapper body, and `0x005745ac-0x005745b0` is alignment before `sub_5745B0`.
- Decompilation again confirms `0x00574510` copies `[entry + 4]` to `g_pConfig + 0x2916d0`, `0x005745b0` emits `0x57/0/selectedId`, writes selected-id/session state, and formats endpoint bytes/port from selected-entry fields.

Checked on 2026-06-14:

- Live IDA MCP session `a001_goal2_class_batch` (`NexusTK.exe.i64`, image base `0x400000`, auto-analysis and Hex-Rays ready) reconfirmed the modeled function map: constructor `0x00573d20` size `0x5e1`, destructor helper `0x00574310` size `0x29`, action handler `0x00574340` size `0xf1`, scroll-button updater `0x00574440` size `0x57`, selection helper `0x00574510` size `0x3e`, packet/helper `0x005745b0` size `0xae`, list virtuals `0x00574660` size `0x0b` and `0x00574670` size `0xd8`, singleton clear `0x00574750` size `0x0b`, adjustor thunks `0x0057475b`/`0x00574766` size `0x0b`, and scalar deleting destructor `0x00574780` size `0x5f`. Raw starts `0x005744a0`, `0x00574500`, `0x00574550`, `0x00574570`, and endpoint `0x005747df` are still not IDA function starts.
- Xrefs remain coherent with `login/ServerSelectPane.cpp`: constructor caller `0x004f70db`, only two `OnControlCommand` calls to `0x00574510` at `0x00574399` and `0x005743db`, packet/helper calls at `0x00574546` and raw-wrapper site `0x005745a1`, list virtual-slot refs at `0x00624e40` and `0x00624e44`, destructor thunk/data refs to `0x00574780`, and seven lifecycle refs to [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md).
- Byte checks reconfirm `0x00573d1d-0x00573d20` and `0x005747df` as `0xcc` padding, raw `0x005744a0` as the list constructor-shaped body installing `0x00624dc4`, `0x00624e4c`, and `0x00624e7c`, raw `0x00574500` as a short insertion wrapper, raw `0x00574550` as an activation wrapper followed by padding, and raw `0x00574570` as the selected-entry packet wrapper ending before `0x005745b0`.
- Decompilation reconfirmed `0x00574510` reads selected index `this[77]`, copies the selected entry name from `[entry + 4]` to `g_pConfig + 0x2916d0`, and forwards selected id/entry pointer to `0x005745b0`. `0x005745b0` writes bytes `0x57`, `0`, and selected id, sends three bytes through `0x00574bb0`, stores selected id at `g_pConfig + 0x28d9d8`, and updates endpoint display state when the alternate-path flag is clear. `0x00574670` still draws selected/unselected server rows and formats the row name/description.
- Component analysis over constructor/action/selection/list/dtor functions reports internal edges from action handler to `0x00574510` and from `0x00574510` to `0x005745b0`, with shared class/source globals `ServerSelectPane` vtables, [UID:0000SW][g_useEpfAssets](by-global/g_useEpfAssets.md), [UID:00028Q][0x0067a7c8-0x0067a7cc.g_pConfig](by-memory/0x0067a7c8-0x0067a7cc.g_pConfig.md), and layout-specific main-menu screen globals.

Checked on 2026-06-22:

- B013 local PE/source-quality recheck keeps this exact aggregate under [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md), recommends `87/89`, and keeps formal aggregate C++ blank because source emission must be split across method/helper children or a coordinated file reconstruction.
- Boundary/liveness recheck agrees with the current half-open aggregate: modeled constructor body ends at `0x00574301` after the trailing byte of `ret 4`, padding starts at `0x00574301`; modeled packet helper ends at `0x0057465e`; raw starts `0x005744a0`, `0x00574500`, `0x00574550`, and `0x00574570` remain source-shaped but have no direct function object, ordinary caller, vtable slot, absolute VA pointer, or RVA pointer route to their starts.
- Rel32 scan found constructor caller `0x004f70db`, selected helper callers `0x00574399` and `0x005743db`, packet helper callers `0x00574546` and raw site `0x005745a1`, and no direct entry route to the raw wrapper starts.
- The constructor caller passes `g_pConfig + 0x28db10`; the constructor stores that directory pointer at `this + 0x26c` and iterates a 32-entry, stride-`0x18` directory whose active count is at directory `+0x300`.
- The helper at `0x005745b0` is login server-selection source, not Socket, PacketBuffer, CashShopRequest, or ProtocolSend source. Those modules are only dependencies used to serialize and queue the feature packet.

## ServerSelectEntry And Directory Layout

Best current behavior names:

```cpp
struct ServerSelectEntry {
    unsigned char serverId;      // +0x00, packet byte 2
    wchar_t *name;               // +0x04, owned selected/configured server name
    wchar_t *description;        // +0x08, owned row display text
    wchar_t *agreementText;      // +0x0c, owned agreement/default-space text
    unsigned char address[4];    // +0x10..+0x13, endpoint address octets
    unsigned short port;         // +0x14, big-endian packet endpoint port
};

struct ServerSelectDirectory {
    ServerSelectEntry entries[32]; // 0x18 * 32 == 0x300
    int entryCount;                // +0x300
    unsigned char version;         // +0x304, MultiServerInfoVersion
};
```

The semantic fields are resolved by independent Config constructor/destructor, registry save/load, packet case `0x56`, and ServerSelect consumers. Exact original private spellings remain inferred. Natural x86 alignment supplies the gaps before `name` and after `port`, yielding sizes 0x18 and 0x308 without source-visible reserved members. The list class stores `ServerSelectEntry *` pointers, not inline records.

## State Fields And Helper Semantics

| Offset or address | Best current name | Evidence |
| --- | --- | --- |
| `ServerSelectPane + 0x26c` | `m_serverDirectory` | Constructor stores the `g_pConfig + 0x28db10` argument here and iterates its records. |
| `ServerSelectMenuItemList + 0x14c` | `m_owner` | Raw constructor stores the owner pointer; activation thunk loads it and jumps to owner action logic. |
| `g_pConfig + 0x28db10` | `m_serverDirectory` | Main-menu caller passes this exact 0x308-byte directory; 32 records, `entryCount` at `+0x300`, and `version` at `+0x304`. |
| `g_pConfig + 0x2916d0` | `selectedServerName[16]` / `configuredServerName` | `_wcscpy_s(..., 0x10, entry->name)` in `0x00574510`; constructor compares this buffer against entry names to preselect a saved server. |
| `g_pConfig + 0x28d9d8` | `selectedServerId` | `SendServerSelectPacket` stores the selected id after packet opcode `0x57`. |
| `g_pConfig + 0x28d9dd` | `alternateServerEndpointActive` / `preserveExistingServerEndpoint` | Nonzero skips endpoint string formatting. Exact UI/source label remains unresolved. |
| `g_pConfig + 0x291230` | `selectedServerEndpointText[64]` behavior alias | Destination for `swprintf_s(buffer, 0x40, L"%d.%d.%d.%d.%d.%d", ...)`; this overlaps older music-directory documentation and must stay caveated until the wider Config layout resolves the original member name. |
| `g_pConfig + 0x2912b0` | `serverEndpointPrefix` | First decimal argument to the endpoint format before entry address bytes and suffix. |
| `g_pApplication + 0x858` | `currentServerName[256]` / `selectedServerNameDisplay` | Constructor initializes the buffer to `TestServer`; row drawing and raw entry helper update it from `entry->name`. |

`SendServerSelectPacket(unsigned int selectedServerId, const ServerSelectEntry *entry)` writes packet bytes `0x57`, `0`, and selected id via [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md), queues exactly three bytes through [UID:0001HU][0x00574bb0-0x00574c13.QueueAndSendPacket](by-memory/0x00574bb0-0x00574c13.QueueAndSendPacket.md), stores selected id in `g_pConfig`, and conditionally formats endpoint text from `serverEndpointPrefix`, `entry->address[0..3]`, and unsigned-short `entry->port`.

## Raw Helper Policy

The raw helpers are source-shaped and should not be treated as padding:

- `0x005744a0` constructs a derived `ServerSelectMenuItemList` and stores vtables.
- `0x00574500` wraps adding a `ServerSelectEntry *` row to `ListPane`.
- `0x00574550` wraps virtual selection/activation.
- `0x00574570` copies an entry name to `g_pApplication + 0x858` and calls `SendServerSelectPacket`.

All four starts still lack direct function objects, ordinary callers, start xrefs, or pointer/table routes. Document the roles here and in support docs, but do not force IDA function creation or standalone final child C++ solely from this evidence.

Rejected alternatives: `0x00574510` is not chat/color code because both callers are `ServerSelectPane::OnControlCommand` and all data flow is server selection; `0x005745b0` is not Socket/PacketBuffer-owned because those are only serialization/queue dependencies; the derived list class belongs with `login/ServerSelectPane.cpp`, not generic `ListPane.cpp`; the raw wrappers are not dead padding because they have coherent bodies and one raw wrapper has a live internal call to `0x005745b0`.

## Source Guidance And Exact Emitter Delegation

This broad range remains formally blank because it contains two classes, file-level/compiler glue, raw no-function starts, and padding. The complete accepted human source is no longer a prose-only draft: [UID:0000D1][ServerSelectMenuItemList](by-class/ServerSelectMenuItemList.md) owns the exact CPP/H at position 30, and [UID:0003D9][0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData](by-memory/0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData.md) emits its compiler marker through that class at position 40.

The source-bearing child preserves constructor constants, pointer-row storage, selected-index guard, `GetItem`, exact string capacities, 16-byte packet local with three-byte send, opcode `0x57`, selected-id and endpoint state, signed port formatting, owner activation, colors/fill, right-edge EPF adjustment, and the 1024-wide-character row buffer. This aggregate documents those behaviors and their exact ranges but does not duplicate definitions.

## UID0003D9 Exact ListPane Construction Closure

The exact source-level base-constructor tuple is `ListPane(sizeof(ServerSelectEntry *), 16, 303, 14, g_useEpfAssets != 1, true, 1)`. On 32-bit x86, `sizeof(ServerSelectEntry *)` is the observed row stride `4`; the remaining literal arguments are capacity `16`, width `303` (`0x12f`), row height `14` (`0x0e`), the inverse-EPF-assets flag, `true`, and `1`. This tuple is the complete constructor-constant evidence required by C3D9-012; it supersedes the older row-stride-only and unspecified-constructor-constant descriptions without removing them as historical evidence.

The parent `ServerSelectPane` constructor contains two equivalent inlined `ServerSelectMenuItemList` construction routes for its layout branches:

- Route A installs the primary/EventHandler/TimerHandler views at `0x00573f0b`, `0x00573f11`, and `0x00573f1b` after constructing the ListPane base with the exact tuple above.
- Route B installs the same three views at `0x005741c8`, `0x005741ce`, and `0x005741d8` after an equivalent ListPane-base construction with the same tuple.

Both parent routes then operate on `ServerSelectEntry *` pointer rows and iterate the same `ServerSelectDirectory` records with exact stride `0x18` / 24 bytes. They are two layout-dependent construction sites for the same source class, not two different classes or constructor signatures. Independently, the retained raw constructor `[0x005744a0,0x005744f8)` calls `ListPane::ListPane` at `0x005744c7 -> 0x004f3a50` with the same tuple, stores `m_owner` at `+0x14c`, and writes the matching vptr triad at `0x005744d7`, `0x005744dd`, and `0x005744e7`; this third body corroborates the source constructor but does not replace the two parent-route facts.

## Target Aggregate No-Code Proof

Keep the formal `RECONSTRUCTION_CPP` block blank for [UID:0001HR]:

- The range has many independent function starts plus raw no-function bodies and padding; it is not one source body.
- It crosses `ServerSelectPane`, `ServerSelectMenuItemList`, file-local packet/state helpers, singleton cleanup, destructor thunks, scalar deleting destructor ABI code, and compiler-generated vtable references.
- Exact vtable data pages already document compiler-generated bytes; final source should recreate class declarations/destructors, not hand-author vtable or thunk bytes.
- Raw wrapper starts currently lack direct caller/table/pointer routes; formal aggregate C++ would overclaim source reachability.
- Child/file-level C++ is now emitted once through UID0000D1 with complete declarations and definitions. This aggregate stays blank to avoid extending source beyond a singular owner or duplicating the accepted class emitter.

## UID0003D9 List-Class Executable Closure

| Range | Source/compiler role | SHA256 | Static route |
| --- | --- | --- | --- |
| `0x5744a0-0x5744f8` | authored list constructor | `33B52EB198759B733A68BA5F9FCF1CB47B23D1EF97A1A0524552F3ABA4D3CF8A` | raw start; no modeled function/xref/pointer route |
| `0x574500-0x57450e` | authored `AddServerEntry` | `886AD26E93528F55E3566C3C759EFF70D8AF52AC1607E00E5E120F6D31BBA458` | raw start; no modeled function/xref/pointer route |
| `0x574510-0x57454e` | authored `ApplySelectedServer` | `16EB3EFBB21760F6CA7894F0F99CF9B7867A5229C457FE803E4C6889D870650C` | modeled; callers `0x574399`, `0x5743db` |
| `0x574550-0x574561` | authored `SelectServerEntryIndex` | `561A9B169FC69F1118B225C80C6D232CBC9CD0356C16853EAA43880851B30233` | raw start; no modeled function/xref/pointer route |
| `0x574570-0x5745ac` | authored `ApplyServerEntry` | `BE5DEA2A3BB6610B5C72D6EC42D6256355BC73C5B40290BB6FCD3D0F928E240D` | raw start; no modeled function/xref/pointer route; calls `0x5745b0` |
| `0x5745b0-0x57465e` | authored private member packet/state helper | `E888F2CA0129A9D67D1E45111FDC3E84E8F649E22386A8B77BF28268386EDBB5` | modeled; callers `0x574546`, raw `0x5745a1` |
| `0x574660-0x57466b` | authored `OnItemActivated` override | `0DC893A29B224721AF27EC45D6D8FEB036A65DD8964B43109FF3E6D6A588224D` | primary vtable slot `0x624e40` |
| `0x574670-0x574748` | authored `DrawListEntry` override | `2895FFC3C48C81351F93BF8D5EE7732DD7F709897374E9B8C6377D1BD0601B2A` | primary vtable slot `0x624e44` |
| `0x47e9d0-0x47ea0b` | shared folded deleting wrapper | compiler-covered | shared table uses; no authored source body |
| `0x47e881-0x47e88c` | `this-0xa0` destructor adjustor | compiler-covered | inherited EventHandler view |
| `0x47e88c-0x47e897` | `this-0xa4` destructor adjustor | compiler-covered | inherited TimerHandler view |

- The raw-start negative checks included inbound xrefs, rel32 routes, encoded absolute VA, and RVA pointer patterns. Positive controls found the modeled callers and vtable references, so the absence is meaningful rather than a failed search.
- Complete source-shaped bodies are retained despite that bounded static-route gap. They are not padding: the constructor installs all three ABI views, each wrapper terminates coherently, and `ApplyServerEntry` calls the live packet helper.
- Exact ABI child UID0003D9 classifies 49 cells, three COLs, a shared eight-base RTTI graph, direct `ListPane` inheritance, and handler PMDs `+0xa0/+0xa4`. The compiler recreates those data objects from UID0000D1's declaration and virtual order.
- Historical `SimpleListPane`, `DrawListItem`, top-edge mutation, static packet helper, `GetSelectedEntry`, and blank-C++ conclusions are superseded. They remain useful correction history but are not current source guidance.

## Singleton Base, Storage, And One-Definition Evidence

The current singleton evidence resolves the global's runtime type, inheritance cause, external linkage, static initialization, and source route without changing this broad aggregate's no-code disposition:

- RTTI type descriptors identify `ServerSelectPane` at `0x006791a4` and `Singleton<ServerSelectPane>` at `0x006791c4`; the corresponding `ServerSelectPane` complete-hierarchy descriptor and base array are at `0x00650298` and `0x006502a8`.
- The `ServerSelectPane` self base-class descriptor at `0x006502cc` has PMD `0/-1/0`, while the direct `Singleton<ServerSelectPane>` descriptor at `0x006502e8` has PMD `620/-1/0` (`+0x26c/-1/0`) and attributes `0x40`. The base therefore begins exactly where the constructor stores `m_serverDirectory` at `this + 0x26c`; this is the expected empty-base-optimization overlap, not a second data member. The constructor allocation remains `0x270` bytes.
- Exact storage [UID:0002XN][0x0069b4ac-0x0069b4b0.g_pServerSelectPane](by-memory/0x0069b4ac-0x0069b4b0.g_pServerSelectPane.md) is four bytes in the loader-zeroed virtual tail of `.data`. Its static value is `0x00000000`; the older `0xffffffff` assumption is superseded by raw-image/PE mapping and live zero-byte/value reads.
- The seven exact references are main-menu cleanup read `0x004f69fb`, startup/create guard read `0x004f70a8`, constructor publish `0x00573d7e`, constructor fallback clear `0x00573d85`, ordinary destructor clear `0x0057432a`, constructor-EH clear `0x00574750`, and scalar deleting destructor clear `0x005747a0`. The two reads outside this implementation cluster prove external rather than file-local linkage.
- Current IDA prestate for `0x0069b4ac-0x0069b4b0` is four separate one-byte undefined heads: only the first is named `unk_69B4AC`; all types and regular/repeatable address comments are blank, and no containing function exists. The bytes and both signed/unsigned dword interpretations are zero. No exact `g_pServerSelectPane` name/global collision or complete local `ServerSelectPane` UDT exists. This is documentation of the supervisor Gate 2B handoff, not a claim that IDA was changed in this callback.
- The clean lower neighbor is the separate four-byte slot at `0x0069b4a8`; the clean upper neighbor is `g_pTransferServerDialog` at `0x0069b4b0` with 20 independent refs, followed by the distinct `int[7]` item beginning at `0x0069b4b4`. No normalization or source route may cross those boundaries.
- Semantic global [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md) is the sole source emitter under [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md), at emitter position `0`. It owns the `ServerSelectPane *g_pServerSelectPane = NULL;` definition and matching `extern` declaration. UID0002XN is physical evidence only (`RECONSTRUCTABLE:FALSE`, no emitter), while this mixed aggregate keeps formal CPP/H blank.

Rejected alternatives remain unsupported: the storage is not an integer, sentinel `-1`, `void *`, class-static member, file-local static, smart pointer, reference, or member of the mixed physical aggregate. UID00029W/MainMenuPane do not own the declaration merely because they are adjacent or consume it, and emitting the global from UID0002XN or this broad range would create a duplicate definition.

## Ownership Decision

This is a coherent login/server-selection cluster and should migrate under `login/ServerSelectPane.cpp`. The server-selection helper functions and projected list constructor need cleanup before doing final source naming because one helper had been mislabeled in older staging notes and IDA still has no function object for the projected list constructor.

The existing parent attachment to [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md) is still the correct direct route after the 2026-06-14 refresh. This broad executable cluster crosses the `ServerSelectPane` class, the `ServerSelectMenuItemList` class, raw wrappers, and file-local helper code, so routing it to only one class would hide mixed ownership. Exact children such as [UID:0003D9][0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData](by-memory/0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData.md) route through their own direct parent evidence. Keep [UID:0000VG][ServerSelectHelpers_574510_5745b0](by-item/ServerSelectHelpers_574510_5745b0.md) as a helper note until the selected-server entry struct and session fields are final.

## Accepted ServerSelectPane ABI And Function Closure

- Exact class ABI child [UID:0003D7][0x00624d24-0x00624dc0.ServerSelectPaneVtableData](by-memory/0x00624d24-0x00624dc0.ServerSelectPaneVtableData.md) contains 39 classified cells: one COL plus 23 primary slots, one COL plus 11 `EventHandler` slots, and one COL plus two `TimerHandler` slots. Shared RTTI proves complete-object offsets `0`, `+0xa0`, and `+0xa4`, with the empty Singleton base at `+0x26c`.
- The complete three-view store triad occurs in constructor `0x573d20`, ordinary cleanup `0x574310`, and scalar deleting wrapper `0x574780`. `0x57475b/0x574766` are base-adjusting deleting-destructor thunks; `0x574750` is construction-failure singleton cleanup. These are compiler artifacts and remain outside authored formal C++.
- Exact byte hashes are constructor `129C5D7168EE71A3AED93F549AF94EF01CE521A5D132D88AE76D32C64612B4CF`, ordinary cleanup `AB4FFFE6E1692CC63CFE836EFC789DF632F5C2E62D80823F223F115E97E8129F`, control command `7745344EE283CB6F17F69C94E4F2A269436B8617A61C73B6CCE02A6AEFD38C64`, action-button update `59ED1E2524A2E799CC53506F64C77EF9FE7D619C75EBB334F38C8C6F74AF459C`, singleton clear `638DC309EDD566266F6B3F5ACD90B32566AA56CE7B6AA0FB97F54A2781178D08`, `+0xa0` adjustor `9910A1B667C587D2BF626DA1F49B097F1E88C8097BB6D195EB1756A6A46F0E60`, `+0xa4` adjustor `001BD22E06AE1148A95F25C61707EE5FECC18B3B51EC7FDD30B21BE1AD2B5E9A`, and scalar wrapper `1DA30E62B9D93755286BFB47E813DF6B04D9FCAFA8808E2BA4970CF163CE1EA7`.
- `0x574340` is the primary `+0x48` `OnControlCommand(int,int)` override: the inherited slot, two arguments, `retn 8`, and body behavior agree. `0x574440` is the primary `+0x4c` `UpdateActionButton()` override. The prior `OnDialogAction` and `UpdateScrollButtons` names are retained only as rejected historical aliases.
- The aggregate formal CPP/H stays blank because it combines two classes, file-local helpers, source-shaped raw starts, compiler glue, and padding. The class declaration now emits through [UID:0000D2][ServerSelectPane](by-class/ServerSelectPane.md), while exact method/helper children remain the body owners.

## Score Rationale

- Completion `91`: current docs cover exact modeled function starts/sizes/hashes, raw wrapper byte bodies, constructor/list/helper/cleanup/lifecycle xrefs, selected-server packet behavior, corrected virtual identities, complete three-view RTTI/vtable/compiler-lowering disposition, direct `Singleton<ServerSelectPane>` PMD/EBO evidence, loader-zeroed global storage, sole external-linkage source routing, mixed ownership, exact data layouts, raw-helper liveness limits, and aggregate no-code proof.
- Confidence `94`: boundaries, source file, behavior, vtable slots, RTTI, all three vptr-store families, exact seven-reference global lifecycle, file route through [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md), packet semantics, and config/application offsets agree. Remaining limits are stripped original lexical spellings and raw-wrapper start reachability, not the corrected virtual methods or class ABI.

## Cross-References

- [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md)
- [UID:0000D2][ServerSelectPane](by-class/ServerSelectPane.md)
- [UID:0000D1][ServerSelectMenuItemList](by-class/ServerSelectMenuItemList.md)
- [UID:0000VG][ServerSelectHelpers_574510_5745b0](by-item/ServerSelectHelpers_574510_5745b0.md)
- [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md)
- [UID:0000L0][MainMenuPane](by-file/MainMenuPane.md)

## Changes

- 2026-08-15 B010 UID0003D9 accepted implementation callback:
  - Score `91/94`, owner/emitter UID0000NO, reconstructable state, blank aggregate formal channels, and broad-range role remain unchanged.
  - Corrected all list-method exclusive ends and source identities, resolved the packet helper as a nonstatic private member, and replaced stale `DrawListItem`/`GetSelectedEntry`/top-edge source guidance.
  - Added all eight authored list-method hashes, callers/routes, four raw-start negative-route proofs, three compiler-wrapper ranges, exact ABI-child relationship, emitter delegation, and historical corrections without duplicating child source.

- 2026-08-10 B010 UID0002PA accepted implementation callback:
  - Score, owner/emitter, broad range, child inventory, and blank aggregate CPP/H policy remain unchanged.
  - Completed the dependency layout with owned `agreementText` at `+0x0c`, unsigned-short `port` at `+0x14`, and directory `version` at `+0x304`; endpoint guidance now consumes `entry->port`.
  - Historical `reserved01`, `reserved0c`, `reserved16`, signed `portOrEndpointSuffix`, missing-version, and unresolved-layout assumptions remain in dated history below but are rejected as current source shape.

- Before: this page and the coverage row ended at `0x005747de`, leaving the final `00` byte of the `retn 4` instruction in an `UNKNOWN` gap.
- Changed to: the canonical range now ends at `0x005747df`, and only `0x005747df-0x005747e0` remains ignored padding before `SocketLifecycle`.
- Evidence: 2026-05-28 IDA MCP byte/disassembly check shows `0x00574780-0x005747df` is the scalar deleting destructor body and `0x005747df` is the following `0xcc` alignment byte.

- 2026-06-01: Raised from `50/65` to `78/84`, marked reconstructable, and attached to [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md).
  - Evidence: live IDA MCP verified function starts/sizes, constructor caller, vtable and singleton xrefs, callees, decompilation behavior for constructor/action/update/list/selection/destructor paths, raw helper bytes, and surrounding padding.
  - C++ remains blank because the raw helper ranges, selected-server entry structure, and application/session field names still need final source-quality naming.
- 2026-06-03: Refined the raw selected-entry packet wrapper start from `0x00574580` to `0x00574570` and removed stale staging-owner wording from the helper rows.
  - Evidence: live IDA disassembly shows `push ebp` at `0x00574570`, name-copy setup through `0x00574592`, the call to `0x005745b0` at `0x005745a1`, wrapper return at `0x005745a9`, and `0xcc` alignment at `0x005745ac-0x005745b0`.
- 2026-06-12 A005 Batch267:
  - Updated the ownership note to remove the stale `84/80` file-parent score and old 80% gate wording.
  - Score unchanged at `78/84`; this broad executable cluster still has helper/field-name caveats, while exact ServerSelectMenuItemList vtable routing is handled by [UID:0003D9][0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData](by-memory/0x00624dc0-0x00624e84.ServerSelectMenuItemListVtableData.md).
- 2026-06-14 Agent-C001 Goal 2:
  - Before: `78/84`, owner/emitter [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md).
  - Changed to: `85/86`, owner/emitter unchanged.
  - Evidence: live IDA MCP session `a001_goal2_class_batch` reconfirmed the function map, raw wrapper bodies, constructor/action/helper/list/destructor xrefs, singleton lifecycle refs, selected-server helper decompilation, and component relationships. Routing stays with the file parent because the range intentionally aggregates `ServerSelectPane`, `ServerSelectMenuItemList`, raw wrappers, and helper code.
- 2026-06-22 B013 Rule 26 incorporation:
  - Before: `85/86`, owner/emitter [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md), formal C++ blank.
  - Changed to: `87/89`, owner/emitter unchanged, formal aggregate C++ still blank.
  - Evidence: B013 local PE/source-quality recheck resolved constructor argument shape through `g_pConfig + 0x28db10`, `ServerSelectDirectory` and `ServerSelectEntry` layouts, `ServerSelectPane::m_serverDirectory`, `ServerSelectMenuItemList::m_owner`, selected-server config/application state fields, raw helper source roles with no-route caveats, opcode `0x57` packet semantics, and the exact reason this broad aggregate should not emit one mixed C++ block.
- 2026-07-26 B005 UID0002XN callback:
  - Before: `87/89`, owner/emitter [UID:0000NO][ServerSelectPane](by-file/ServerSelectPane.md), broad formal CPP/H blank.
  - Changed to: `89/92`, owner/emitter and blank aggregate code unchanged.
  - Evidence added: direct `Singleton<ServerSelectPane>` RTTI/PMD at `+0x26c`, empty-base overlap with `m_serverDirectory`, exact `0x270` allocation, loader-zeroed four-byte storage, all seven publish/clear/consumer refs, current four-head IDA prestate, collision/type negatives, protected neighbors, and sole semantic-global definition/header routing through [UID:0000S8][g_pServerSelectPane](by-global/g_pServerSelectPane.md). All prior B013 method/layout/helper evidence and raw-helper caveats remain authoritative.

## 2026-07-15 UID0003UP Server-Endpoint Synchronization

- The aggregate's score, ownership, child inventory, formal state, full server-selection behavior, and unrelated history remain unchanged.
- Complete Config layout resolves high buffer `+0x291230` as `m_serverEndpointText[64]`. Server selection composes/stores the accepted prefix/address/port representation there while ANSI `m_loginServerAddress` and startup port/mode fields remain distinct.
- The former MP3/music-directory interpretation at `+0x291230` is now explicitly superseded. `m_musicFolderPath` is the separate tail buffer at `+0x29190c`.
- No new ServerSelect C++ or behavior change is introduced; this is bounded declaration/consumer synchronization.
