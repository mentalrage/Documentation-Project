*** UID:0004J8 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00001X | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
short ChattingPane::GetScrollRange(char axis)
{
    if (g_useEpfAssets == 1) {
        if (axis != 0)
            return 0;

        const int visibleLineCount = m_visibleChatPixelHeight / 13;
        if (ComputeLineCount() - visibleLineCount < 0)
            return 0;

        return static_cast<short>(ComputeLineCount() - visibleLineCount);
    }

    if (axis != 0)
        return 0;
    if (ComputeLineCount() - 5 < 0)
        return 0;

    return static_cast<short>(ComputeLineCount() - 5);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact ChattingPane primary-vtable GetScrollRange override at 0x0047f520-0x0047f5b7; returns the nonnegative vertical maximum in 13-pixel chat-line units, preserves current-layout dynamic visible rows and legacy five-row behavior, and emits through UID00001X to NexusTK/social/Chatting.cpp. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0047f520-0x0047f5b7 ChattingPane GetScrollRange

## Status And Source Route

- Disposition: reconstructable, source-authored `ChattingPane` virtual method.
- Owner/emitter: [UID:00001X][ChattingPane](by-class/ChattingPane.md), routed through [UID:0000I5][Chatting](by-file/Chatting.md) to `NexusTK/social/Chatting.cpp`.
- Split parent: [UID:0002ES][0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals](by-memory/0x0047f520-0x0047f6e5.ChattingPaneScrollMetricVirtuals.md), retained as a non-emitting index.
- ABI: `short ChattingPane::GetScrollRange(char axis)`; primary vtable slot `+0x48` at `0x00614d28`.

## Behavior

- Axis `0` is vertical and axis `1` is horizontal. The horizontal path returns `0` in both layout modes.
- With `g_useEpfAssets == 1`, visible rows are `m_visibleChatPixelHeight / 13`; the method returns `max(ComputeLineCount() - visibleRows, 0)`.
- In the legacy layout, it returns `max(ComputeLineCount() - 5, 0)`.
- The successful paths deliberately call `ComputeLineCount()` again after the negative test. The formal body preserves that binary call count instead of caching the first result.
- The result is a signed 16-bit scroll metric. Consumers read `AX`; broadening the source return to `int` is rejected.

## Direct Evidence

- Evidence-time IDA MCP lookup identified a modeled function at `0x0047f520` with size `0x97`, ending exactly at `0x0047f5b7`.
- Decompilation and disassembly show `g_useEpfAssets`, axis testing, division by literal `13`, legacy subtraction by `5`, repeated calls to [UID:0002EX][0x0047fc70-0x0047fd3b.ChattingPaneComputeLineCount](by-memory/0x0047fc70-0x0047fd3b.ChattingPaneComputeLineCount.md), and zero clamping.
- The only incoming reference to the function start is the data xref from ChattingPane primary-vtable entry `0x00614d28`; no direct code caller or unrelated receiver was found.
- ScrollablePane core synchronization invokes inherited slot `+0x48` with axis `1` for the horizontal child and axis `0` for the vertical child, proving the axis meaning used here.

## Range And Padding

- Exact source body range: `0x0047f520-0x0047f5b7`.
- The following `0x0047f5b7-0x0047f5c0` span is nine `0xcc` alignment bytes owned as padding evidence by the split parent, not by this method and not emitted as source.
- No string, embedded table, secondary function start, or outside-owner bytes occur inside this child range.

## Naming, Fields, And Rejected Alternatives

- `GetScrollRange` follows the inherited ScrollablePane slot contract and the surviving ListPane source inventory; historical descriptive `GetVerticalScrollMaxLines` wording remains role evidence, not the canonical declaration.
- `m_visibleChatPixelHeight` is the `ChattingPane +0x134` field written by `SetVisibleRowCount` as `13 * visibleRows`.
- Literal `13` is the chat line-height unit. The stale interpretation that a neighboring slot is `GetLineHeight` is rejected.
- This method is not a free helper, ListPane method, or new-file source root; constructor/vtable ownership and the existing class/file emitter route are direct and complete.

## Score Rationale

- Completion `92`: exact range, ABI, slot, owner/emitter, axis semantics, fields, branch formulas, repeated-call behavior, callers, padding, and first-draft source are documented.
- Confidence `93`: mechanical behavior and routing are direct; the remaining cap reflects absent original symbols/header spelling rather than a behavior or source-placement blocker.
