*** UID:0004HS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:96 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00003Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00003Z | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void DLPalette::SetPackedColor(unsigned int colorIndex,
                               unsigned short packedColor)
{
    m_mappedColors[colorIndex] = packedColor;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SetPackedColor(unsigned int colorIndex, unsigned short packedColor);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready exact DLPalette::SetPackedColor child split from [UID:0001E4][0x00542ac0-0x00543149.DLPaletteMethodCluster](by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md); an unchecked void member stores a caller-supplied native 16-bit packed color in m_mappedColors[colorIndex], emits its definition/declaration through [UID:00003Z][DLPalette](by-class/DLPalette.md) into Palette.cpp/Palette.h, and is reached at four ignored-result restoration call sites in BalloonObjectPane and ColorStringChattingMessage. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00542b50-0x00542b63 DLPalette::SetPackedColor

## Status

- Entity kind: `DLPalette` method.
- Rebuild handling: source-ready, source-authored out-of-line member with exact CPP and H formals on this child.
- Intended direct owner/emitter: [UID:00003Z][DLPalette](by-class/DLPalette.md) `DLPalette`.
- Source route: definition in `NexusTK/render/Palette.cpp` and public declaration in `NexusTK/render/Palette.h`, aggregated through the `DLPalette` `[[CHILDREN]]` shells.
- Split source: [UID:0001E4][0x00542ac0-0x00543149.DLPaletteMethodCluster](by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md) `DLPaletteMethodCluster` accepted B008 implementation callback.

## Address Range

- Range: `0x00542b50-0x00542b63`, start inclusive and end exclusive.
- IDA function: `sub_542B50`, size `0x13`.
- Exact body bytes: `55 8B EC 8B 45 08 66 8B 55 0C 66 89 54 41 2C 5D C2 08 00`.
- Body SHA256: `233A68EDDB00577A6FE612B4571EF9BBE6EA7D22079D7A9A19034294B27317E6`.
- Unique signature: `55 8B EC 8B 45 ? 66 8B 55`.
- Predecessor fence `[0x00542b4a,0x00542b50)`: six `0xcc` bytes after `SetColor`, SHA256 `33CC4A00A0F04735602819132DA3E3FAC4CDEB8EC7AEE92551DC79BA8488E722`.
- Successor fence `[0x00542b63,0x00542b70)`: thirteen `0xcc` bytes before the raw expand helper, SHA256 `0D0BB866505BC0E2D4276FCDEE1AFDC8887662D04CCE9E4BBB4C10FBA6D0F1CE`.
- One modeled function and one basic block occupy the exact range; there is no internal child, overlap, split, merge, or padding reclassification.

## Behavior

`DLPalette::SetPackedColor` performs exactly one unchecked indexed 16-bit store:

```cpp
m_mappedColors[colorIndex] = packedColor;
```

The body has no bounds comparison, mask, clamp, branch, callee, exception path, or explicit return. The compiler loads `colorIndex` into EAX for the scaled address and leaves that value there after the store. Hex-Rays historically rendered this residue as an `int` return, but all four callers discard EAX. That decompiler-shaped return interpretation is superseded by the source-semantic `void` setter contract and is retained here only as historical analysis evidence.

## Raw Evidence

- B008 live MCP recheck originally established `sub_542B50`, size `0x13`, the `this[a2 + 22] = a3` store, and four code xrefs.
- B002 dated read-only MCP sessions `b005-uid0002yf-final-persist-verify` and `b008-uid0003fs-rollback` reconfirmed the exact function, seven-instruction body, bytes, fences, frame, comments, UDT, and caller inventory. Session/PID identities are historical evidence, not permanent current authority.
- Exact instruction sequence: `push ebp; mov ebp,esp; mov eax,[ebp+8]; mov dx,[ebp+0xc]; mov [ecx+eax*2+0x2c],dx; pop ebp; retn 8`.
- Physical frame at the research snapshot: saved-register row `+0x0` width 4, return-address row `+0x4` width 4, dword argument `+0x8`, word argument `+0x0c`, and no physical locals.
- Exactly four code xrefs and zero data xrefs were found. No absolute-VA or RVA pointer-pattern hit names the target start.
- Entry regular/repeatable, function regular/repeatable, and instruction comments were absent at the research snapshot; the supervisor-owned IDA handoff is retained in the accepted B002 report.
- The target has no outgoing call and no callback/provider dependency.

## Caller Behavior

Both observed callers use the method only to restore packed palette values after a temporary draw-time override:

| Call site | Caller | Exact role |
| --- | --- | --- |
| `0x0046820c` | `sub_467B30` / `BalloonObjectPane::PaintBubble` | Restores the saved packed value at index `253`; EAX is ignored. |
| `0x0046821f` | same caller | Restores the saved packed value at `m_textPaletteIndex + 1`; the active branch proves the base index is `253`, so this call uses `254`; EAX is ignored. |
| `0x004837e5` | `sub_4836A0` / `ColorStringChattingMessage::Draw` | Restores the saved foreground mapped color from object field `+0x08`; EAX is ignored. |
| `0x004837f3` | same caller | Restores the saved background mapped color from object field `+0x0c`; EAX is ignored. |

The same callers first read the old values through `DLPalette::MapColor`, write temporary RGB-derived values through `DLPalette::SetColor`, draw, and then invoke this setter twice. Their next work restores GrafPort palette state rather than consuming EAX. This save/mutate/draw/restore protocol proves side-effect-only `void` semantics.

## Touched State

| State | Meaning |
| --- | --- |
| `this + 0x00` | `DLPalette`/`LObject` vftable pointer. |
| `this + 0x04` | Optional entry-move data pointer. |
| `this + 0x08` | Entry-move count. |
| `this + 0x0c` | 32-byte palette header. |
| `this + 0x2c` | `unsigned short m_mappedColors[256]`; target writes element `colorIndex` without a bounds check. |

The current documented/IDA `DLPalette` layout is exactly `0x22c` / 556 bytes. The target's word store and scaled index agree with the 512-byte table at `+0x2c`.

## Ownership And Source Placement

The ECX receiver, `+0x2c` table offset, adjacent `MapColor`/`SetColor` methods, exact UDT, existing owner/emitter metadata, and caller-held `DLPalette *` values make [UID:00003Z][DLPalette](by-class/DLPalette.md) the direct semantic owner and emitter. [UID:0000MA][Palette](by-file/Palette.md) is the established `Palette.cpp` / `Palette.h` source root, while UID0001E4 remains a non-emitting split inventory.

Reject PaletteLib, Surface, BalloonObjectPane, ColorStringChattingMessage, and GrafPort as direct owners. PaletteLib supplies instances, Surface supplies callbacks used by siblings, and the UI classes are consumers; none owns this receiver method. Also reject a standalone `DLPaletteSetPackedColor.cpp`, helper source file, caller-local helper, or inline-only header body. No split, merge, or broader emitter is needed.

## C++ Disposition

Formal CPP and H are source-ready on this child. The definition is an out-of-line `void DLPalette::SetPackedColor(unsigned int colorIndex, unsigned short packedColor)` in `Palette.cpp`; the declaration belongs in the public `DLPalette` section in `Palette.h`. The emitted code preserves exactly one unchecked store and no explicit return.

`SetPackedColor`, `colorIndex`, and `packedColor` are source-facing inferred spellings selected from established project vocabulary, sibling method symmetry, and caller roles. No PDB/original source proved the exact lexical spellings, but finalized code must not retain `sub_542B50`, `a2`, `a3`, or decompiler temporaries. Do not add an index guard, cast, helper call, `return colorIndex`, modern fixed-width type, or inline implementation.

## Cross-References

- [UID:0001E4][0x00542ac0-0x00543149.DLPaletteMethodCluster](by-memory/0x00542ac0-0x00543149.DLPaletteMethodCluster.md)
- [UID:00003Z][DLPalette](by-class/DLPalette.md)
- [UID:0000MA][Palette](by-file/Palette.md)

## Changes

- 2026-08-04 B002 UID0004HS ordinary implementation callback:
  - Raised `88/91 -> 94/96` while preserving canonical owner/emitter UID00003Z, reconstructable true, and blank optional position.
  - Added exact formal CPP/H for the unchecked void setter and resolved the historical incidental-EAX return interpretation through all four ignored-result restoration calls.
  - Added exact body/fence bytes and hashes, caller protocols, UDT/field evidence, source-facing names, Palette.cpp/Palette.h placement, rejected alternatives, and source-shape constraints without removing the B008 split history.
- 2026-07-06 B008 implementation callback: created as an exact child of UID0001E4.
