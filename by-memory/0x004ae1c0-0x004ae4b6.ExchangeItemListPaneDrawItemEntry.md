*** UID:0004A0 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004S | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ExchangeItemListPane::DrawItemEntry(int, ExchangeItemListRow *row, RectBounds *rowBounds)
{
    EPFTileContext itemContext;
    RectBounds imageBounds;
    wchar_t text[256];

    GetItemGlyphBounds(static_cast<short>(row->itemId), &itemContext.bounds);

    const int itemWidth = itemContext.bounds.right - itemContext.bounds.left;
    const int itemHeight = itemContext.bounds.bottom - itemContext.bounds.top;
    const int rowCenterY = (rowBounds->top + rowBounds->bottom) / 2;

    if (g_useEpfAssets == 1) {
        const int scaledWidth = itemWidth / 2;
        const int scaledHeight = itemHeight / 2;

        imageBounds.left = 25 - scaledWidth / 2;
        imageBounds.top = rowCenterY - scaledHeight / 2;
        imageBounds.right = imageBounds.left + scaledWidth;
        imageBounds.bottom = imageBounds.top + scaledHeight;

        IntersectRects(&imageBounds, rowBounds, &imageBounds);
        g_pItemObjImageLib->DrawItemImageScaled(this, &imageBounds, static_cast<short>(row->itemId), row->color);
    } else {
        imageBounds.left = 25 - itemWidth / 2;
        imageBounds.top = rowCenterY - itemHeight / 2;
        imageBounds.right = imageBounds.left + itemWidth;
        imageBounds.bottom = imageBounds.top + itemHeight;

        g_pItemObjImageLib->DrawItemImage(this, &imageBounds, static_cast<short>(row->itemId), row->color, nullptr, 0.0f);
    }

    m_textDrawMode = 1;
    SetTextColor(37);
    SetTextBackColor(0);

    wcscpy_s(text, 0x100, row->name);
    MoveTo(50, rowCenterY + 6);
    DrawWideText(text, static_cast<int>(wcslen(text)));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Vtable-routed ExchangeItemListPane draw virtual and first-draft row renderer; current evidence resolves the inherited draw callback shape, row payload layout, item-image helper calls, text draw path, owner [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md), and emitter route [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ae1c0-0x004ae4b6 ExchangeItemListPane DrawItemEntry

## Status

- Address range: `0x004ae1c0-0x004ae4b6` (`[start,end)`), IDA function `sub_4AE1C0`, size `0x2f6` / 758 bytes, body through `0x004ae4b5`.
- Direct owner: [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md).
- Source route: [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md) -> [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) / `NexusTK/ui/dialogs/ExchangeDialog.cpp`.
- Entity kind: class method / vtable row draw callback override.
- Rebuild handling: source-authored virtual row renderer.
- Emission disposition: first-draft formal C++ present; `EMITTER_UIDS:00004S` routes this method through the private `ExchangeItemListPane` class.
- Score rationale: `88/90` is supported by exact live function boundaries, vtable route, padding, row-field reads, branch behavior, text behavior, inherited callback shape, helper declarations, source route, and negative owner exclusions. The score stays below final-audit quality because exact original draw method spelling, `Pane *drawOwner` helper static type, and source-order/declaration cleanup remain inferred rather than symbol-proven.

## MCP Status And Evidence

Current IDA MCP evidence came from the accepted B004 source-quality report. The first `idb_list` session `3fa0535f` disappeared, and target calls returned `Session not found: 3fa0535f`. The MCP listener remained active at `127.0.0.1:13337` with owning process `7364`; observed processes included `idalib-mcp` pid `16824` and Python workers. A fresh `idb_list` returned active read-only session `B005_00049P_20260629` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, `is_analyzing:false`, backend worker pid `16832`.

`server_health` reported module `NexusTK.exe`, imagebase `0x400000`, input path `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, `auto_analysis_ready:true`, `hexrays_ready:true`, and strings cache ready.

- `lookup_funcs` confirms `0x004ae1c0` is `sub_4AE1C0`, size `0x2f6`, covering through `0x004ae4b5`; `0x004ae4b6` is not a function; successor `0x004ae4c0` is `sub_4AE4C0`.
- `xrefs_to 0x004ae1c0` reports exactly one data xref from `0x00619e48`; there are no direct code callers.
- `get_int 0x00619e48` returned decimal `4907456`, which converts to `0x004ae1c0`; sibling slot `0x00619e40` returned `0x004ae1b0`.
- `get_bytes 0x004ae1b0` confirms [UID:00049Z][0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual](by-memory/0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual.md) bytes `c2 08 00`, thirteen `0xcc` bytes at `0x004ae1b3-0x004ae1c0`, target body bytes from `0x004ae1c0`, final `ret 0x0c` ending at `0x004ae4b6`, and ten `0xcc` bytes at `0x004ae4b6-0x004ae4c0` before the AddItemDialog successor.
- `analyze_function 0x004ae1c0` reports prototype shape `void __thiscall(int this, int, int, int *)`, size `0x2f6`, cyclomatic complexity `4`, no direct callers, one data xref at `0x00619e48`, and callees `sub_457A60`, `sub_4DF460`, `sub_4B7CC0`, `sub_4DF2C0`, `sub_4DEE50`, `sub_4B9600`, `sub_4B9680`, `sub_4B9690`, `sub_4BAB70`, `_wcscpy_s`, and `@__security_check_cookie@4`.
- `decompile 0x004ae1c0` confirms the first stack argument after `this` is the unused row index, the second stack argument is the row payload pointer, and the third stack argument is the row-bounds pointer.

## Signature And Row Layout

The accepted source-facing signature is `void ExchangeItemListPane::DrawItemEntry(int, ExchangeItemListRow *row, RectBounds *rowBounds)`. [UID:0003U3][0x004f43e0-0x004f45b0.ListPanePaintHitTestGeometry](by-memory/0x004f43e0-0x004f45b0.ListPanePaintHitTestGeometry.md) documents the inherited `ListPane` paint loop as dispatching derived row drawing with `(rowIndex, itemPayload, rowBounds)`. This target matches that shape: the first callback argument is unused, `row` is the item payload, and `rowBounds` is the paint rectangle. The `ret 0x0c` confirms three stack arguments after `this`.

The row payload is [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)'s source-facing `ExchangeItemListRow`: key byte `+0`, reserved byte `+1`, `unsigned short itemId +2`, `unsigned char color +4`, reserved byte `+5`, `wchar_t name[256] +6`, total row size `0x206`. The draw method reads `itemId`, `color`, and `name`; constructor/upsert/find/dispatcher evidence owns the key and layout details.

## Helper And Callee Table

| Binary callee/data | Source-facing surface | Use in this method |
| --- | --- | --- |
| `sub_457A60` | `EPFTileContext::Initialize` / `InitTileContext` | Initializes the stack item context; its embedded `bounds` field is reused for glyph bounds. |
| `sub_4DF460` | `GetItemGlyphBounds(short itemId, RectBounds *outRect)` | Fills `itemContext.bounds` for the selected item id. |
| `sub_4B7CC0` | `IntersectRects` | Clips the EPF half-size destination rectangle against the row bounds. |
| `sub_4DF2C0` | `ItemObjImageLib::DrawItemImageScaled(Pane *drawOwner, RectBounds *dstRect, short itemId, unsigned char paletteFlags)` | Draws the EPF/high asset branch. |
| `sub_4DEE50` | `ItemObjImageLib::DrawItemImage(Pane *drawOwner, RectBounds *dstRect, short itemId, unsigned char paletteFlags, AlphaMaskSurface *overlayMask, float overlayStrength)` | Draws the legacy/full-size asset branch with `nullptr` overlay and `0.0f` strength. |
| `dword_67A758` | `g_pItemObjImageLib` | Global image library used for both image draw branches. |
| `byte_66DA97` | `g_useEpfAssets` / EPF asset-mode selector | Branch selector for scaled EPF rendering versus legacy rendering. |
| `sub_4B9600` | `GrafPort::MoveTo` | Moves the text cursor to `(50, rowCenterY + 6)`. |
| `sub_4B9680` | `GrafPort::SetTextColor` | Sets foreground text color `37`. |
| `sub_4B9690` | `GrafPort::SetTextBackColor` | Sets background/secondary text color `0`. |
| `sub_4BAB70` | `GrafPort::DrawWideText` | Draws the copied row name using `wcslen(text)`. |
| direct byte write `this+0x88` | `GrafPort::m_textDrawMode` | Sets text draw mode to `1` before rendering the name. |

`@__security_check_cookie@4` is the compiler stack-cookie epilogue support and does not represent handwritten source logic.

## Behavior

The method initializes an `EPFTileContext`, calls `GetItemGlyphBounds` for `row->itemId`, computes `itemWidth`, `itemHeight`, and the row vertical center `(rowBounds->top + rowBounds->bottom) / 2`, then renders the item glyph centered around x-coordinate `25`.

EPF/high branch (`g_useEpfAssets == 1`): halves the glyph width and height, builds a half-size destination rectangle centered at x `25` and the row center, aliases `imageBounds` as both input and output to `IntersectRects(&imageBounds, rowBounds, &imageBounds)`, and calls `g_pItemObjImageLib->DrawItemImageScaled(this, &imageBounds, static_cast<short>(row->itemId), row->color)`.

Legacy branch: uses the full glyph width and height, centers the full destination rectangle at x `25` and the row center, and calls `g_pItemObjImageLib->DrawItemImage(this, &imageBounds, static_cast<short>(row->itemId), row->color, nullptr, 0.0f)`.

Both branches then set `m_textDrawMode = 1`, text color `37`, background/secondary color `0`, copy `row->name` into a 256-wide-character stack buffer with `_wcscpy_s`, move the text cursor to `(50, rowCenterY + 6)`, and draw the copied name with `DrawWideText(text, static_cast<int>(wcslen(text)))`.

Do not add `ReleaseBuffers`; the binary does not call it, and this initialized context path has no allocated buffers to release.

## Ownership, Exclusions, And Negative Evidence

- This is a routed virtual child, not an orphan helper: `0x00619e48 -> 0x004ae1c0` is the only live route, and MCP reports no direct code callers.
- The neighboring primary vtable slot `0x00619e40 -> 0x004ae1b0` is [UID:00049Z][0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual](by-memory/0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual.md), already resolved as `ExchangeItemListPane::OnItemSelected(int, int)`.
- `0x004ae4b6` has no xrefs and is padding start, not an executable successor. `0x004ae4c0` starts AddItemDialog territory and remains outside this target.
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md) is the source-file route, not the direct semantic owner of this method body.
- `ItemObjImageLib`, `GrafPort`, `RectBounds`, `EPFTileContext`, `PaletteLib`, `ResourceLayoutTable`, and Surface callbacks are dependencies only. They provide helper APIs or render data, not exchange row ownership.
- `MyItemListPaneDrawListEntry` is a close item-row rendering analog, but it does not own the exchange pane. Keep `DrawItemEntry` as the formal name; `DrawListEntry` is only a bounded alternate if a future `ListPane` naming pass standardizes that spelling.
- `DLGEXC1.EPF` and `DLGEXC1.PAL` belong to exchange dialog background/resource setup, not this row renderer.
- The called `ItemObjImageLib` draw child pages may keep their own formal bodies blank while internal renderer callbacks remain unresolved. This target only needs their source-facing declarations.

## Boundary Notes

- Preceded by `0x004ae1b3-0x004ae1c0` padding after [UID:00049Z][0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual](by-memory/0x004ae1b0-0x004ae1b3.ExchangeItemListPaneNullVirtual.md).
- Followed by `0x004ae4b6-0x004ae4c0` padding before the next AddItemDialog boundary function at `0x004ae4c0`.

## Cross-References

- [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)
- [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md)
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- [UID:0003U3][0x004f43e0-0x004f45b0.ListPanePaintHitTestGeometry](by-memory/0x004f43e0-0x004f45b0.ListPanePaintHitTestGeometry.md)
- ExchangeItemListPane vtable and item-readonly data evidence recorded on [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md).

## Changes

- 2026-06-30 B004 implementation callback:
  - Changed completion/confidence from `83/87` to `88/90`, set `EMITTER_UIDS:00004S`, preserved `CANONICAL_OWNER:00004S`, `RECONSTRUCTABLE:TRUE`, and blank `EMITTER_POSITION_OPTIONAL`, and inserted first-draft formal `ExchangeItemListPane::DrawItemEntry(int, ExchangeItemListRow *, RectBounds *)` C++.
  - Summary/evidence: accepted current MCP session `B005_00049P_20260629` confirms exact range `0x004ae1c0-0x004ae4b6`, size `0x2f6`, `0x004ae4b6` not a function, successor `0x004ae4c0`, vtable/data route `0x00619e48 -> 0x004ae1c0`, no direct code callers, sibling `0x00619e40 -> 0x004ae1b0`, final `ret 0x0c`, and post-padding `0x004ae4b6-0x004ae4c0`. The resolved inherited `ListPane` draw callback shape is `(rowIndex, itemPayload, rowBounds)`, with row payload `ExchangeItemListRow *` using `itemId +2`, `color +4`, and `name +6`. The body initializes `EPFTileContext`, calls `GetItemGlyphBounds`, selects EPF half-size clipped `DrawItemImageScaled` versus legacy full-size `DrawItemImage(..., nullptr, 0.0f)`, then writes `m_textDrawMode`, sets colors `37/0`, copies the wide row name, moves to `(50,rowCenterY+6)`, and draws text. Shared ItemObjImageLib/GrafPort/RectBounds/EPFTileContext pages remain dependency support only.
- 2026-06-28 B001 accepted split implementation:
  - Created exact child with `83/87`, owner [UID:00004S][ExchangeItemListPane](by-class/ExchangeItemListPane.md), `RECONSTRUCTABLE:TRUE`, blank `EMITTER_UIDS`, and blank formal C++ by then-accepted score-capped non-emitting disposition. Superseded by the 2026-06-30 B004 implementation callback.
