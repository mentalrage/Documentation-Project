*** UID:0003NP | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000097 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000097 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Raw retained NewOptionPane visible-option hit-test body at 0x00540ef0 is not emitted as a source helper yet.
// Current MCP reports no function record and no inbound xref/pointer route; NewOptionPane::OnMouseClick contains the live inline-equivalent scan.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw no-function NewOptionPane visible-option hit-test body; B006 session 9600ec5b confirms no active function record, zero xrefs/xref_query inbound routes, and zero VA/RVA pointer encodings; loops 0x1b entries with stride 0x98, accepts active-page or 0xff entries, tests rectangle +0x17c through 0x004b7e80, returns hit index or -1, and preserves the formal no-code proof pending source-visible caller/signature and OptionEntry declarations. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00540ef0-0x00540f4d NewOptionPaneVisibleOptionHitTest

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: raw function-shaped class helper in current B006 MCP evidence session `9600ec5b`; it was historically promoted to a neutral IDA function during the 2026-06-15 C001 IDA pass, but that function record is not retained in current accepted B001/B015/B006 evidence sessions. Older `0b5e057e`, B001 `ff68e691`, and B015 `80de0a67` evidence agrees on the raw/no-route state.
- Direct owner: [UID:000097][NewOptionPane](by-class/NewOptionPane.md) NewOptionPane.
- Source root: [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- Parent aggregate: [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- Confidence: strong for hit-test behavior, option-entry field offsets, byte-level boundaries, and current no-route status including zero VA/RVA pointer encodings; still below final-source quality because no inbound caller/pointer route has been found, active IDB has no retained function record, and the source-visible helper/argument names remain unresolved.

## Behavior

Scans the fixed option-entry array for a visible entry whose rectangle contains the supplied point/event coordinates. The helper:

- treats `ecx` as the `NewOptionPane` instance;
- loops through `0x1b` option entries with stride `0x98`;
- accepts entries whose page byte equals `this+0x12b9` or is `0xff`;
- tests the entry rectangle at entry offset `+0x17c` through `0x004b7e80`;
- returns the matching entry index as a short value, or `-1` if no visible option entry is hit.

## IDA MCP Evidence

- 2026-07-06 B006 MCP session `9600ec5b` used active adopted database `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- B006 `lookup_funcs` reports `0x00540ef0` as `Not a function`. `entity_query` for functions/names in `0x00540e80-0x00540f70` returned zero entries, so the current IDB has no local function/name record covering this raw start.
- B006 route checks found no helper-start route: `xrefs_to 0x00540ef0` returned count `0`; `xref_query` to `0x00540ef0` for any/code/data each returned total `0`; `find_bytes` found zero little-endian VA pointer bytes `f0 0e 54 00` and zero little-endian RVA pointer bytes `f0 0e 14 00`. A broad immediate instruction scan was attempted during report research but timed out/truncated, so it is not used as proof.
- B006 disassembly from `0x00540ef0-0x00540f50` reconfirmed the exact helper shape: normal prologue, `edi = ecx`, `esi` loop index, `imul eax, 0x98`, option entry base `lea ecx, [eax+edi]`, page byte read at `entry+0x18c`, active page compare against `this+0x12b9`, `0xff` global-page acceptance, rectangle pointer `entry+0x17c`, containment call `sub_4B7E80` at `0x00540f29`, loop bound `0x1b`, miss return `-1`, hit return `mov ax, si`, and `retn 8` at `0x00540f4d`.
- B006 `analyze_function 0x00540910` reconfirmed `NewOptionPane::OnMouseClick` contains the live inline-equivalent 27-slot visible-option scan using stride `0x98`, active page `this+0x12b9`, entry page byte `+0x18c`, entry rectangle `+0x17c`, and containment helper `0x004b7e80`; it does not call or reference `0x00540ef0`.
- B006 `analyze_function 0x005411b0` reconfirmed the option-entry setup layout: index stride `0x98` (`this[76 * a2]`), label storage at entry `+0xf8`, rectangle at entry `+0x17c`, action/category byte at entry `+0x178`, and page/visibility byte at entry `+0x18c`. These remain observed offsets and descriptive/inferred field roles, not final original source member names.
- 2026-06-25 B015 MCP session `80de0a67` reconfirmed `lookup_funcs 0x00540ef0` as `Not a function`; `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search found zero inbound routes. The raw body still treats `ecx` as the pane pointer, loops `0x1b` entries with stride `0x98`, accepts active-page or `0xff` entries, tests rectangle `+0x17c` through `0x004b7e80`, and returns a hit index or `-1`.
- 2026-06-23 B001 MCP evidence session `ff68e691` reports `lookup_funcs 0x00540ef0` as `Not a function`. `xrefs_to`, code-ref search, data-ref search, immediate search, and little-endian VA pointer-byte search for `f0 0e 54 00` all found zero inbound routes. The older `0b5e057e` pass is historical and agrees.
- 2026-06-15 pre-pass `lookup_funcs 0x00540ef0` reported `Not a function`; C001 then defined explicit bounds `0x00540ef0-0x00540f50`, and post-pass `lookup_funcs` in that session reported `sub_540EF0` size `0x60`. This promotion is preserved as historical/session-specific evidence, not the accepted B001 evidence-session state.
- 2026-06-15 instruction query over `0x00540ef0-0x00540f50` shows a normal prologue, `edi = ecx`, `esi` loop index, `imul eax, 0x98`, page byte read at `entry+0x18c`, active page compare against `this+0x12b9`, rectangle pointer `entry+0x17c`, call to `0x004b7e80`, loop bound `0x1b`, `-1` return on miss, and hit return via `mov ax, si; retn 8` at `0x00540f4d`.
- The next body starts at `0x00540f50`; the return instruction occupies `0x00540f4d-0x00540f50`, so the IDA function's exclusive end is `0x00540f50`.
- 2026-06-15 `xrefs_to 0x00540ef0`, backward data-flow from the raw start, and little-endian VA byte search for `f0 0e 54 00` also found no inbound xrefs or raw pointer hits, so the exact caller route remains unresolved across both historical and current passes.
- 2026-06-15 Wave2 `method`, `decomp`, and `xrefs` have no record for `0x00540ef0`; `filter --class NewOptionPane --all-methods` lists neighboring modeled methods but not this helper.
- 2026-06-15 live decompile of `0x00540910` (`NewOptionPane::OnMouseClick`) shows an inline equivalent 27-entry scan using the same stride, page-byte filter, rectangle offset, and `0x004b7e80` containment helper; it does not call `0x00540ef0`.
- 2026-06-15 live decompile of `0x005411b0` (`SetOptionEntry`-shaped helper) confirms the option-entry layout used here: `index * 0x98`, label storage at entry `+0xf8`, rectangle at entry `+0x17c`, action/category byte at entry `+0x178`, and page/visibility byte at entry `+0x18c`. These are observed offsets, not final struct field names.

## Touched State And Dependencies

| Item | Evidence | Meaning |
| --- | --- | --- |
| `this+0x12b9` | Compared with each entry page byte. | Active option page selector. |
| entry stride `0x98` | Loop address computation. | Fixed option-entry record size. |
| entry `+0x18c` | Page byte read. | Per-entry page visibility selector. |
| entry `+0x17c` | Rectangle pointer passed to `0x004b7e80`. | Hit-test rectangle inside each option entry. |
| `0x004b7e80` | Callee. | Rectangle/point containment helper. |

## Reconstruction Notes

This should be reconstructed as a `NewOptionPane` helper once the caller path and argument names are final. Keep final C++ blank until the event/point type, option-entry layout names, and source-visible helper name are source-quality. The OnMouseClick inline scan is related evidence, not proof that this helper is currently called.

B001 `ff68e691` and B007 `supervisor_resume_20260629` evidence-session state reinforces the no-code decision: the body is byte-recovered and source-shaped, but current evidence has no function record, no direct xref, no pointer route, and no original-name proof. The formal block now carries an explicit no-code proof comment so generated output is not visually blank.

B015 `80de0a67` preserves the same no-code rationale with current-session evidence. The blocker is specific: no inbound route exists, `NewOptionPane::OnMouseClick` has an inline equivalent scan, and the source-visible caller/signature plus `OptionEntry` declarations are not synchronized.

B006 `9600ec5b` raises confidence in the no-code disposition by adding current zero `xref_query` any/code/data results and zero VA/RVA pointer-byte evidence while preserving the same behavior and source-quality blocker. The helper name `NewOptionPaneVisibleOptionHitTest` remains descriptive rather than original-name proof, and the formal block must not emit a helper body until a real caller/address route and source-visible signature are proven.

## Cross-References

- [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md) NewOptionPane server/config/packet helper island.
- [UID:000097][NewOptionPane](by-class/NewOptionPane.md) NewOptionPane.
- [UID:0000M7][OptionPane](by-file/OptionPane.md) OptionPane.
- [UID:0001DW][0x0053ff90-0x00541b2b.NewOptionPane](by-memory/0x0053ff90-0x00541b2b.NewOptionPane.md) NewOptionPane aggregate memory range.

## Changes

- 2026-06-14 A001 split this raw hit-test helper out of [UID:0001DX][0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers](by-memory/0x00540db0-0x00541114.NewOptionPaneServerOptionHelpers.md). Live IDA MCP listing confirmed the loop bound, stride, page filtering, rectangle hit-test callee, miss return, exact `retn 8` endpoint, and unresolved no-direct-xref caveat.
- 2026-06-15 C001 refreshed Wave2/live IDA evidence, defined the bounded IDA function record `sub_540EF0` without renaming or typing it, added a start comment, saved the IDB, recorded the no-xref/no-pointer-hit blocker, and raised scores from `82/84` to `85/88`.
- 2026-06-23 B001 active-session correction: no score, owner, emitter, or C++ change.
  - Evidence: current B001 MCP evidence session `ff68e691` reports this helper start as `Not a function` with zero xref/code-ref/data-ref/immediate/pointer routes. Older `0b5e057e` evidence is historical and agrees. The 2026-06-15 C001 function record is historical/session-specific; behavior, ownership, byte boundary, and no-C++ conclusions remain valid.
- 2026-06-25 B015 implementation callback: metadata and blank C++ unchanged at that time; added current session `80de0a67` route/behavior evidence, no-code rationale, and concise `Item Summary`. This blank-C++ disposition is superseded by the 2026-06-30 formal no-code marker.
- 2026-06-30 B007 implementation callback:
  - Changed from `85/88` to `86/89` and inserted the accepted formal no-code proof comment.
  - Evidence: `lookup_funcs 0x00540ef0` remains `Not a function`, `xrefs_to` has no inbound route, and `NewOptionPane::OnMouseClick` has the live inline-equivalent scan. This is retained raw helper evidence, not padding.
- 2026-07-06 B006 implementation callback:
  - Changed from `86/89` to `88/91`, retaining owner/emitter [UID:000097][NewOptionPane](by-class/NewOptionPane.md), `RECONSTRUCTABLE:TRUE`, blank emitter position, filename, and the formal no-code proof comment.
  - Evidence: current MCP session `9600ec5b` reports `0x00540ef0` as `Not a function`, has zero xrefs and zero `xref_query` any/code/data inbound routes, and finds no little-endian VA (`f0 0e 54 00`) or RVA (`f0 0e 14 00`) pointer encodings. The target disassembly, `OnMouseClick` inline-equivalent scan, `SetOptionEntry` layout, historical C001 promotion rejection, and unresolved source-signature/name caveats are now recorded at report-level detail.
