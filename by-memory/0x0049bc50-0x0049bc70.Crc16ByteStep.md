*** UID:00032M | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00032J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00032J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
unsigned short Crc16ByteStep(unsigned char value, unsigned short crc)
{
    const unsigned int tableIndex = crc >> 8;
    return static_cast<unsigned short>((crc << 8) ^ s_crc16ByteTable[tableIndex] ^ value);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Externally linked, dead-retained one-byte update over private writable `s_crc16ByteTable`; exact range/signature, non-augmented MSB-first polynomial-`0x1021` recurrence, exhaustive static-image no-route proof, and paired buffer relationship are resolved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0049bc50-0x0049bc70 Crc16 Byte Step

## Status

- Address range: `0x0049bc50-0x0049bc70`
- Entity kind: raw callable CRC-16/CCITT helper body
- Disposition: reconstructable utility code
- Inferred owner: [UID:00032J][Crc16](by-file/Crc16.md) `Crc16`
- Rebuild handling: `source-authored`
- IDA function status: not promoted as an IDA function object.

## Behavior

This helper updates a running 16-bit CRC-style accumulator from one byte. It loads the current accumulator from `[ebp+0Ch]`, indexes `word_66D680` by the high byte of the accumulator, shifts the accumulator left by eight bits, zero-extends the input byte from `[ebp+8]`, XORs the table word and input byte, and returns the result in `AX`.

In source terms, this is the single-step byte variant that backs the byte-buffer loop at `0x0049bc70-0x0049bca9`.

## Draft C++ Notes

The accepted draft uses the inferred `Crc16ByteStep` name and an `unsigned char` input because the binary zero-extends `[ebp+8]` before XORing it into the 16-bit accumulator. `s_crc16ByteTable` preserves the binary's separate second writable table object even though complete comparison proves it is identical to the word table.

Historical/superseded source assumption: the earlier block marked the function `static` and used const-signaling `kCrc16ByteTable`. The optimized standalone body survives with no static-image route, and the table occupies writable `.data`; externally linked function source over a private writable table is now accepted. External source linkage does not mean PE export.

## IDA Evidence

- Historical 2026-07-05 evidence-time MCP session `supervisor_recovery_20260705` reported server status `ok` for `NexusTK.exe` with imagebase `0x400000`; no present session availability is asserted.
- That evidence-time `lookup_funcs` reported `0x0049bc50` and the byte-buffer start `0x0049bc70` were not function starts; the next modeled function was `sub_49BCB0` at `0x0049bcb0`, size `0xe`.
- That evidence-time `xrefs_to 0x0049bc50` reported zero entrypoint xrefs. `xrefs_to 0x0066d680` reported exactly two data xrefs: this helper at `0x49bc5f` and the byte-buffer helper at `0x49bc92`.
- That evidence-time `find_bytes "50 BC 49 00"` reported zero absolute little-endian pointer-pattern matches to the target start; decompilation failed because the raw start was not modeled as a function.
- Prior B001 raw loaded-segment scan found no little-endian pointer constants, direct `E8` calls, or direct `E9` jumps to `0x0049bc50` in loaded segments; B002 preserved this as historical supporting evidence and did not repeat an unbounded raw scan.
- Disassembly:

```asm
0x0049bc50  push    ebp
0x0049bc51  mov     ebp, esp
0x0049bc53  movzx   eax, word ptr [ebp+0Ch]
0x0049bc57  mov     ecx, eax
0x0049bc59  shl     eax, 8
0x0049bc5c  shr     ecx, 8
0x0049bc5f  xor     ax, word_66D680[ecx*2]
0x0049bc67  movzx   ecx, byte ptr [ebp+8]
0x0049bc6b  xor     ax, cx
0x0049bc6e  pop     ebp
0x0049bc6f  retn
```

## Boundaries

- Evidence-time `get_bytes 0x0049bc47 size 0x29` confirmed previous internal padding `0x0049bc47-0x0049bc50` is nine `0xcc` bytes.
- Evidence-time `get_bytes 0x0049bc50 size 0x20` confirmed the exact target body bytes are `55 8b ec 0f b7 45 0c 8b c8 c1 e0 08 c1 e9 08 66 33 04 4d 80 d6 66 00 0f b6 4d 08 66 33 c1 5d c3`.
- This body contains no `0xcc` bytes and ends at the `retn` byte at `0x0049bc6f`.
- The byte-buffer helper begins immediately at `0x0049bc70`; there is no padding between the byte-step and byte-buffer helper bodies.

## Ownership Notes

The direct table dependency is [UID:00027A][0x0066d47c-0x0066d880.ChecksumLookupTablesData](by-memory/0x0066d47c-0x0066d880.ChecksumLookupTablesData.md) table `word_66D680` at `0x0066d680`; its xref from this body is exactly `0x0049bc5f`. This helper is paired with the byte-buffer helper in the same narrow [UID:00032J][Crc16](by-file/Crc16.md) source owner.

B001-019 split the broader [UID:000225][0x0049bbf0-0x0049bca9.ChecksumTableHelpers](by-memory/0x0049bbf0-0x0049bca9.ChecksumTableHelpers.md) aggregate into exact reconstructable children and kept that aggregate as a non-emitting index. The B001 split remains correct for this child: `Crc16ByteStep` is an exact child, not a range that needs further split or merge repair.

The accepted B002 evidence keeps [UID:00032J][Crc16](by-file/Crc16.md) as the best direct owner and emitter. [UID:00027N][0x0066db4c-0x0066dd50.MapPaneChecksumLookupTable](by-memory/0x0066db4c-0x0066dd50.MapPaneChecksumLookupTable.md) has the same polynomial-table opening bytes, but the 2026-07-05 evidence-time MCP `xrefs_to 0x0066db50` reported eight separate MapPane-side xrefs under `sub_507C90`, `sub_50C040`, and raw MapPane helper sites; that table is not evidence for moving this helper out of `Crc16`. Zlib/LodePNG are rejected because their checksum code is separate third-party CRC-32/Adler/PNG support, not this 16-bit helper/table pair. Neighboring Crasher and Monitor/critical-section ranges are rejected as address adjacency only: they are separated by `0xcc` padding and share no table xrefs, globals, callers, or source semantics with this helper.

## 2026-07-12 B002 C01-C20 Callback

- Signature and recurrence: stack args are `unsigned char value` then `unsigned short crc`; `movzx` proves unsigned byte input. The body returns `s_crc16ByteTable[crc >> 8] ^ (crc << 8) ^ value` as `unsigned short` under default 32-bit `__cdecl`-compatible shape.
- Polynomial semantics: both complete 256-word tables match MSB-first polynomial `0x1021`. For a byte input, XOR and OR into the cleared low byte are equivalent; this is the historical non-augmented high-byte update form. It must not be described as a complete named variant without accounting for seed/augmentation/finalization.
- Table and family: `0x0066d680` is read only here at `0x0049bc5f` and by UID00032N at `0x0049bc92`. Both arrays are byte-identical, writable, and separately retained for their exclusive function pairs.
- Exhaustive liveness: xrefs, code/data refs, immediates, VA/RVA/raw-offset patterns, and decoded E8/E9 targets found no route to any of the four starts. The PE has no exports. The family is dead-retained in this image.
- Source shape: external function linkage best explains the retained optimized step body and allows its source-level use to inline into UID00032N. Macro/duplicated recurrence and file-static linkage are weaker source alternatives. Names remain descriptive rather than symbol-proven.
- Metadata: owner/emitter [UID:00032J][Crc16](by-file/Crc16.md), reconstructability, exact range, and `Nested:0` remain unchanged; score is `90/92`, emitter position `40`, and the complete accepted formal block is installed.

## Score Rationale

Completion is `90` because the prior exact bytes/function-model/xref evidence is preserved and now augmented with complete family liveness, table hash/polynomial/mutability, algorithm qualification, external/dead-retained linkage, source inlining, source order, and corrected formal code.

Confidence is `92` because the range, behavior, table dependency, owner/emitter route, and negative ownership/liveness evidence are direct and mutually consistent. It remains below `95+` because original function/table names, linkage/header declarations, and historical runtime use are not symbol-proven.

## Cross-References

- [UID:00032J][Crc16](by-file/Crc16.md) `Crc16`
- [UID:000225][0x0049bbf0-0x0049bca9.ChecksumTableHelpers](by-memory/0x0049bbf0-0x0049bca9.ChecksumTableHelpers.md) aggregate source-family index.
- [UID:00027A][0x0066d47c-0x0066d880.ChecksumLookupTablesData](by-memory/0x0066d47c-0x0066d880.ChecksumLookupTablesData.md) backing CRC lookup tables.
