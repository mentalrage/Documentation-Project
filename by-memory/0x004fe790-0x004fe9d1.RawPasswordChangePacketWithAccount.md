*** UID:0002QB | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000I3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void __stdcall SendPasswordChangeRequestWithAccountId(
    const wchar_t* accountName,
    const wchar_t* currentPassword,
    const wchar_t* newPassword,
    const wchar_t* accountIdText)
{
    char packet[0x100];
    char converted[0x100];

    short accountNameLength = static_cast<short>(wcslen(accountName));
    short currentPasswordLength = static_cast<short>(wcslen(currentPassword));
    short newPasswordLength = static_cast<short>(wcslen(newPassword));
    long accountId = _wtol(accountIdText);

    PacketBufferWriteUInt8(0x26, packet);

    int accountNameByteLength = WideCharToMultiByte(0, 0, accountName, accountNameLength, converted, 0x100, NULL, NULL);
    if (accountNameByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[accountNameByteLength] = 0;
    PacketBufferWriteUInt8(accountNameByteLength, packet + 1);
    memmove(packet + 2, converted, accountNameByteLength);

    unsigned int packetLength = static_cast<unsigned int>(accountNameByteLength + 2);

    int currentPasswordByteLength = WideCharToMultiByte(0, 0, currentPassword, currentPasswordLength, converted, 0x100, NULL, NULL);
    if (currentPasswordByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[currentPasswordByteLength] = 0;
    PacketBufferWriteUInt8(currentPasswordByteLength, packet + packetLength);
    memmove(packet + packetLength + 1, converted, currentPasswordByteLength);
    packetLength += static_cast<unsigned int>(currentPasswordByteLength + 1);

    int newPasswordByteLength = WideCharToMultiByte(0, 0, newPassword, newPasswordLength, converted, 0x100, NULL, NULL);
    if (newPasswordByteLength >= 0x100)
        __report_rangecheckfailure();
    converted[newPasswordByteLength] = 0;
    PacketBufferWriteUInt8(newPasswordByteLength, packet + packetLength);
    memmove(packet + packetLength + 1, converted, newPasswordByteLength);
    packetLength += static_cast<unsigned int>(newPasswordByteLength + 1);

    PacketBufferWriteUInt32BE(accountId, packet + packetLength);
    packetLength += 4;
    if (packetLength >= 0x100)
        __report_rangecheckfailure();

    packet[packetLength] = 0;
    g_packetSender->QueueAndSendPacket(
        packet, static_cast<short>(packetLength));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Current MCP route evidence supports a retained opcode `0x26` password-change helper with three converted text fields plus an `_wtol` numeric field; active submit still routes to the three-field sibling, so no-function/no-xref/no-pointer evidence remains a confidence cap. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***














# 0x004fe790-0x004fe9d1 - Raw Password Change Packet With Account

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored packet sender.
- Confidence: strong for behavior, range, packet layout, source family, and formal first-draft C++ after the 2026-06-25 B005 MCP-backed correction; still capped by no IDA function object or direct caller/xref/table/pointer route for the raw start.
- Autogen parent: [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md).
- Parent aggregate: [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).

## Address Range

| Range | Kind | Notes |
| --- | --- | --- |
| `0x004fe790-0x004fe9d1` | raw function-shaped helper | Four-argument stack-call packet builder; IDA does not define a function object at `0x004fe790`. Size is `0x241` bytes. |
| `0x004fe9c9` | return | Ends with `retn 10h`, matching four stack arguments. |
| `0x004fe9cc-0x004fe9d1` | range-check failure tail | Calls `___report_rangecheckfailure` from the overflow path before the alignment gap. |
| `0x004fe9d1-0x004fe9e0` | following padding | Fifteen `0xcc` bytes before [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md). |

## Behavior

This raw helper constructs an opcode `0x26`/decimal `38` password-change style packet with three length-prefixed UTF-16 text inputs plus a fourth numeric account/id-style input. It computes the first three wide-string lengths, converts those fields to bounded multibyte strings with `WideCharToMultiByte`, writes one-byte field lengths through [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md) `sub_575380` / inferred `PacketBufferWriteUInt8`, copies each payload through [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) `sub_516220` represented as source-facing `memmove(...)`, parses the fourth wide input through the CRT [UID:0001O5][0x005cea43-0x005cea6d.CrtWtolWrapper](by-memory/0x005cea43-0x005cea6d.CrtWtolWrapper.md), appends that value through `sub_5753F0` / inferred `PacketBufferWriteUInt32BE` as a 32-bit big-endian field, writes a final local terminator outside the sent length, and sends the finished packet through Socket-owned [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) / `dword_67A7EC` via `sub_574BB0` / `QueueAndSendPacket(g_packetSender, packet, packetLength)`.

The helper is similar to [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md), but inserts the extra parsed numeric field after the three text fields. Treat it as a retained source-family account/password packet sender with formal first-draft C++; do not treat it as the current submit route unless a direct caller or dispatch table later proves liveness. The formal `accountIdText` name is inferred from the wide-text-to-`_wtol` conversion and 32-bit packet write, not from a surviving caller-side parameter name.

## Ownership Evidence

| Evidence | Interpretation |
| --- | --- |
| Adjacent range | The helper begins immediately after the `0x004fe782-0x004fe790` alignment gap that follows [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md). |
| Aggregate owner | [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md) is already attached to [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) from IDA-confirmed packet send/reply helpers. |
| Matching packet pattern | This helper writes opcode `0x26`, performs the same UTF-16 to multibyte packet-field conversion pattern as [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md), adds a `__wtol`/`sub_5753F0` numeric field, and sends through the same global network object. |
| Reachability caveat | IDA MCP and local PE route scans have no function object, callers, start xrefs, immediates, byte-pattern pointer, data-ref, VA/RVA/raw-offset dword hit, or direct transfer to `0x004fe790`; attach it as retained source-family code, not as a proven active submit path. |

## Raw Evidence

- 2026-06-12 IDA MCP `server_health` reports `NexusTK.exe`, imagebase `0x400000`, Hex-Rays ready.
- 2026-06-12 IDA MCP `lookup_funcs` reports `0x004fe790`, `0x004fe9c9`, and `0x004fe9d1` are not functions; `0x004fe9e0` is `sub_4FE9E0` size `0x202`.
- 2026-06-12 IDA MCP `xrefs_to 0x004fe790` reports no cross-references. The active sibling start `0x004fe9e0` has one code xref at `0x004fe778` inside `sub_4FE560`.
- 2026-06-12 IDA MCP raw bytes at `0x004fe780` show the previous helper's `ff ff`, fourteen `0xcc` alignment bytes, and the raw prologue `55 8b ec 81 ec 18 02 00 00` at `0x004fe790`. Bytes at `0x004fe9d1` are fifteen `0xcc` bytes before the sibling prologue at `0x004fe9e0`.
- 2026-06-12 IDA MCP `insn_query 0x004fe790-0x004fe9d1` returns 165 decoded instructions with no containing IDA function. The prologue allocates a `0x218` stack frame, saves four stack arguments from `[ebp+8]`, `[ebp+0xc]`, `[ebp+0x10]`, and `[ebp+0x14]`, and calls `sub_516030`.
- The same instruction query shows three UTF-16 length walks and three `WideCharToMultiByte` calls at `0x004fe85f`, `0x004fe8c3`, and `0x004fe92f`, each guarded by `cmp ..., 100h` and overflow branches to `0x004fe9cc`.
- The fourth argument is not converted with `WideCharToMultiByte`: `0x004fe81b-0x004fe82a` pushes `[ebp-20Ch]` into `unknown_libname_24` / CRT `__wtol`, stores the parsed value, and `0x004fe984-0x004fe98b` writes it through `sub_5753F0`.
- The packet layout evidence is: opcode `0x26` written at `0x004fe837-0x004fe839`; text-field length writes through `sub_575380` / `PacketBufferWriteUInt8`; payload copies through [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) `sub_516220` / `memmove`; numeric field append through `sub_5753F0` / `PacketBufferWriteUInt32BE`; final `dword_67A7EC` load at `0x004fe99e`; final send at `0x004fe9b4`; security-cookie epilogue and `retn 10h` at `0x004fe9c1-0x004fe9c9`; range-check failure call at `0x004fe9cc`.
- 2026-06-12 IDA MCP `make_signature_for_range 0x004fe790-0x004fe9d1` produced a unique wildcarded IDA-format signature for the raw body, supporting the exact range and boundary.

## 2026-06-23/2026-06-24 B004 MCP/PE Source-Quality Recheck

- MCP provenance: the accepted B004 pass used endpoint `http://127.0.0.1:13337/mcp` and session/database `80de0a67` against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. Initial 2026-06-23 health reported module `NexusTK.exe`, executable input `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, strings cache ready, and string cache size `2067`. After the 2026-06-24 MCP pause, the supervisor reopened the same IDB as `80de0a67`; B004 rechecked `idb_list` and `server_health`, which again returned one active adopted worker session and healthy analysis/Hex-Rays/string-cache state. The final evidence is current-session MCP-backed, not fallback-only.
- Executable provenance and PE map: both `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` and `E:\NTK\Resources\NexusTK\NexusTK.exe` hash to SHA-256 `9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`. The local route scan used `.text` VA `0x00401000`, RVA `0x1000`, virtual size `0x20b4ac`, raw pointer `0x400`, raw size `0x20b600`; `.rdata` VA `0x0060d000`, raw pointer `0x20ba00`; `.data` VA `0x0066d000`, raw pointer `0x26ac00`; and `.rsrc` VA `0x0069d000`, raw pointer `0x278400`. The target VA `0x004fe790` maps to RVA `0x0fe790`, raw offset `0x0fdb90`, inside `.text`.
- Boundary recheck: MCP `lookup_funcs` reports `0x004fe790`, `0x004fe9c9`, `0x004fe9d1`, `0x004febe2`, and `0x004fe782` are not functions; `0x004fe780` and `0x004fe778` resolve inside `sub_4FE560` at `0x004fe560`, size `0x222`; the successor [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md) starts at `sub_4FE9E0`, size `0x202`; `0x004febf0` is `sub_4FEBF0`, size `0x221`; and `0x004fee40` is `sub_4FEE40`, size `0x1ff`. The 2026-06-24 restored-session `lookup_funcs database=80de0a67` recheck reconfirmed `0x004fe790` and `0x004fe9d1` are not functions and the sibling/control functions retain the same sizes.
- Byte and padding proof: MCP `get_bytes` around `0x004fe780` shows the previous helper tail bytes `ff ff`, fourteen `0xcc` alignment bytes, and raw prologue `55 8b ec 81 ec 18 02 00 00` at `0x004fe790`. MCP `get_bytes` around `0x004fe9c0` shows the security-cookie epilogue, `retn 10h` at `0x004fe9c9`, the `___report_rangecheckfailure` call bytes at `0x004fe9cc`, fifteen `0xcc` bytes from `0x004fe9d1` to `0x004fe9e0`, and the sibling prologue at `0x004fe9e0`.
- Raw instruction proof: MCP `insn_query` over `0x004fe790-0x004fe9d1` decoded 165 instructions, all with `fn:null`, confirming a raw function-shaped island with no IDA function ownership. The call list includes `sub_516030`, CRT `unknown_libname_24` / `__wtol`, four `sub_575380` calls, three `WideCharToMultiByte` calls, three `sub_516220` calls, `sub_5753F0`, `sub_574BB0`, `@__security_check_cookie@4`, and `___report_rangecheckfailure`. MCP `decompile 0x004fe790` failed because no function object exists. MCP `make_signature_for_range 0x004fe790-0x004fe9d1` produced a unique wildcarded IDA-format signature for the raw body.
- Negative route proof: MCP `xrefs_to 0x004fe790` reports zero xrefs. MCP `find code_ref`, `find data_ref`, and `find immediate` for `0x004fe790` all return zero matches. MCP `find_bytes` for little-endian VA bytes `90 e7 4f 00` returns zero matches. The 2026-06-24 restored-session route recheck reconfirmed `xref_count:0` plus zero `find code_ref`, `find data_ref`, `find immediate`, and `find_bytes "90 e7 4f 00"` hits. The local PE scan found zero `.text` `E8`/`E9`/near-conditional direct-transfer hits to `0x004fe790` and zero dword hits for `0x004fe790` as VA `0x004fe790`, RVA `0x000fe790`, or raw offset `0x000fdb90`.
- Positive controls: MCP `xrefs_to/find code_ref 0x004fe9e0` finds the single active sibling call at `0x004fe778` inside `sub_4FE560`, and the PE direct-transfer scan finds the same `0x004fe778 -> 0x004fe9e0` call at raw `0x0fdb78`. MCP `find code_ref 0x004febf0` and the PE direct-transfer scan find the reply-handler route from `0x004fe551` to `0x004febf0`. MCP `find data_ref` plus the local PE dword scan find nearby vtable dwords `0x0061d6f4 -> 0x004fe460`, `0x0061d6f8 -> 0x004fe490`, and `0x0061d71c -> 0x004fe530`; the same data/pointer searches find no route to `0x004fe790`.
- Packet behavior proof: `insn_query` finds the only `push 0x26` at `0x004fe837`, followed by `sub_575380` at `0x004fe839`. It finds exactly three `push 0x100` conversion-buffer calls at `0x004fe84b`, `0x004fe8a9`, and `0x004fe910`, matching three bounded `WideCharToMultiByte` text-field conversions. The fourth argument is stored from `[ebp+0x14]` into `[ebp-0x20c]`, passed to CRT `__wtol` at `0x004fe825`, stored back as the parsed integer, and later written through `sub_5753F0` at `0x004fe98b`; this proves a wide numeric field serialized as 32-bit big-endian, not a fourth length-prefixed string. Packet scalar-writer support now lives at [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md), with `sub_575380` as inferred `PacketBufferWriteUInt8` and `sub_5753F0` as inferred `PacketBufferWriteUInt32BE`.
- Active sibling/control mapping: MCP `decompile 0x004fe560` shows the live submit helper reads controls `3`, `4`, `5`, and `6`, copies them into four 16-wide-character buffers with max count `15`, requires the control `5`/new-password buffer to contain a digit, compares the control `5` buffer against control `6`, creates the dimmer/loading object on success, and calls [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md) at `0x004fe778` with only the control `3`, control `4`, and control `5` buffers. MCP `decompile 0x004fe9e0` confirms the active sibling shape is three `const unsigned __int16 *` stack arguments that write opcode `38`, convert exactly three wide strings, append a local terminator, and send through `dword_67A7EC`. The current live submit path does not pass control `6` to either sender and does not route to `0x004fe790`.
- Generated-source caveat: generated and readonly artifacts use useful order hints such as `usernameBuffer`, `currentPwdBuffer`, `newPwdBuffer`, `confirmPwdBuffer`, and older `local_94`, `local_74`, `local_34`, `local_54` names. Those names support the active control-order interpretation but are not source proof for the retained raw helper's fourth numeric argument and do not prove `0x004fe790` is an active account-id submit path.

## 2026-06-25 B005 Formal-C++ Correction

- MCP session/database `80de0a67` was rechecked and remained active against `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `server_health` reported `status=ok`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and string cache size `2067`.
- `lookup_funcs` reconfirmed `0x004fe790` is not an IDA function, `0x004fe560` is `sub_4FE560` size `0x222`, `0x004fe778` is inside `sub_4FE560`, and the active sibling [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md) is `sub_4FE9E0` size `0x202`.
- `xrefs_to` reconfirmed `0x004fe790` has `xref_count:0`, while `0x004fe9e0` has the single active code xref at `0x004fe778` inside `sub_4FE560`. A `search_text` check over the submit helper again found the only submit-path sender call as `.text:004FE778 call sub_4FE9E0`.
- `lookup_funcs` and `analyze_function` for `0x00516220` identify [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) / `sub_516220` as a real `0x18`-byte one-basic-block wrapper with prototype shape `void *__stdcall(void *, void *Src, size_t Size)` and decompiled body `return memmove(a1, Src, Size);`.
- `search_text` found the same three `sub_516220` payload-copy sites in this raw helper at `0x004fe8a0`, `0x004fe907`, and `0x004fe975`, plus the active sibling copy sites at `0x004feacf`, `0x004feb36`, and `0x004feba0`. The accepted source-facing representation is therefore `memmove(...)` for the three converted payload copies, with [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) as support evidence. These calls are not PacketBuffer scalar writes and should not be rewritten as `PacketBufferWrite*` helpers.
- B005 supersedes B004's target-change conclusion that formal C++ must stay blank. B004's no-function, no-xref, no-pointer, no-active-submit-route, and no-caller-side fourth-field-name evidence remains valid audit evidence and a confidence cap; it is no longer a no-code blocker because the helper is source-owned, reconstructable, self-contained, and its packet body is exact.

## Rejected Alternatives And Superseded No-Code Proof

- Active submit path as direct caller to `0x004fe790` is rejected: MCP and PE checks found no code ref, xref, direct call/jump, immediate, VA/RVA/raw-offset pointer, or data ref to `0x004fe790`, while the live submit helper calls only `0x004fe9e0`.
- `0x004fe790` as the current confirmation-control sender is rejected: the live path reads control `6` only to compare against control `5`; it does not serialize control `6` and does not call the raw helper.
- A fourth string-field interpretation is rejected: the raw body has exactly three `WideCharToMultiByte` conversions, while the fourth stack argument is parsed through CRT `__wtol` and written through `sub_5753F0` / `PacketBufferWriteUInt32BE`.
- Socket / `g_packetSender` is rejected as canonical owner because Socket owns the transport global and send funnel, not the password-dialog opcode `0x26` packet layout. PacketBuffer is rejected as canonical owner because it owns scalar writer helpers, not the feature packet body. MainMenuPane and `OpenChangePasswordDialog` are rejected because they construct or launch the dialog but do not serialize password-change packets. A no-owner/non-emitting classification is rejected because the bytes are source-authored packet logic in the ChangePasswordDialogPane helper cluster.
- B004's blank-formal-C++/no-code proof is explicitly superseded by the accepted B005 source-quality correction. The old proof's negative evidence remains valid: IDA still has no function object, no start xref, no caller/table/pointer route, no active submit path, and no caller-side fourth-field spelling. Those facts cap confidence and keep this helper documented as retained/no-route, but they no longer block formal C++ because the body is complete, source-owned, stack-cleaning, isolated by padding, and its packet layout is exact.
- The B005 formal name `SendPasswordChangeRequestWithAccountId` and parameter `accountIdText` are source-facing first-draft names, not recovered symbols. They preserve the observed runtime behavior: the fourth wide text argument is converted by `_wtol`, serialized by `PacketBufferWriteUInt32BE`, and sent after the three text fields. [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md) remains the active three-argument submit sender and is not replaced or expanded by this retained helper.

## Touched State And Calls

| Reference | Role |
| --- | --- |
| `dword_67A7EC` / [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) | Socket-owned network/session send object passed to `sub_574BB0`. |
| `sub_575380` / [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md) | Writes opcode and per-field length bytes. |
| `sub_5753F0` / [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md) | Writes the parsed fourth argument as a 32-bit big-endian field plus trailing zero byte. |
| `sub_516220` / [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) | Copies multibyte field payloads into the packet buffer; B005 represents this as source-facing `memmove(...)` because current MCP decompiles the helper as a one-block CRT `_memmove` wrapper. |
| `WideCharToMultiByte` | Converts UTF-16 user/password fields to multibyte packet fields. |
| `unknown_libname_24` / `__wtol` | Parses the fourth wide input into a numeric field before packet encoding. |
| `sub_516030` | Returns the shared memory/packet context used by the later copy helper calls; final source-facing helper name remains unresolved because this report only rechecked the packet sender and copy/scalar support helpers. |
| `___report_rangecheckfailure` | Overflow guard for 256-byte field/buffer bounds. |

## Cross-Document Evidence

- [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md) identifies this exact range as the first child of the password-change packet helper cluster, records the surrounding alignment, and keeps the no-function/no-xref caveat.
- [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md) documents the active three-string sender with the same opcode `0x26`, `WideCharToMultiByte`, length-write, payload-copy, and `dword_67A7EC` send pattern.
- [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md) confirms the dialog core ends immediately before the `0x004fe782-0x004fe790` padding gap and calls the active sibling sender at `0x004fe9e0` from submit logic.
- [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) includes this child in the proposed `ChangePasswordDialogPane.cpp` contents and records the adjacent packet helper cluster as part of the login/account password-change source candidate.
- [UID:00001L][ChangePasswordDialogPane](by-class/ChangePasswordDialogPane.md) records the packet send/reply helper family as part of the password-change dialog class behavior while keeping the original file fold with login/account dialogs open.
- [UID:0003YJ][0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers](by-memory/0x00575380-0x00575470.PacketBufferScalarBigEndianWriteHelpers.md) owns the scalar writer child evidence for `sub_575380` as inferred `PacketBufferWriteUInt8` and `sub_5753F0` as inferred `PacketBufferWriteUInt32BE`. The older broad PacketBuffer aggregate remains a split index and should not be the only support citation for this target.
- [UID:0001O5][0x005cea43-0x005cea6d.CrtWtolWrapper](by-memory/0x005cea43-0x005cea6d.CrtWtolWrapper.md) documents `unknown_libname_24` as CRT `__wtol`, matching the numeric fourth-field parse at `0x004fe825`.

## 2026-06-22 B002 MEMTOOL Warning Triage

Agent-B002 reviewed the `auto-generated/by-memory-tool-report.md` Advanced-Error-Scan warning that claimed this page was missing document text for `0x004fe790`, `0x004fe9d1`, and `RawPasswordChangePacketWithAccount`. That warning is treated as scanner hygiene rather than a substantive range, owner, emitter, split, or source-quality defect. The current page text contains the start and end addresses, and the title `Raw Password Change Packet With Account` satisfies the tool's alphanumeric-normalized label check even though the exact filename token is not repeated as a contiguous prose phrase.

The B002 pass rechecked the current generated row and kept [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) as both `CANONICAL_OWNER` and `EMITTER_UIDS`, with `RECONSTRUCTABLE:TRUE`, score `86/87`, and blank direct C++. It also reconfirmed that `0x004fe790` and `0x004fe9d1` are not IDA function starts, `0x004fe9e0` is the active sibling sender reached from `0x004fe778`, `0x004fe790-0x004fe9d1` has clean padding boundaries, and no xrefs currently prove a direct caller for this raw start. No target metadata, range/name, owner/emitter, split/merge, IDA, or C++ action was required by that scanner-hygiene triage unless a future normal `memory_ranges.py report` rerun still reported the same scanner warning; in that fallback case, adding a scanner-visible slug line would have been a hygiene-only edit, not a behavior change. B005 supersedes the old blank-C++/no-action conclusion for source-quality purposes while preserving the no-function/no-xref facts as confidence caps.

## 2026-07-24 Current Container And IDA Handoff Recheck

- B002 re-read the exact child against live IDA MCP session `dc9b367a-4569-4166-8fa6-4d2bcb44d3d2`, IDB session `1ce787ed`, at shared-IDB checkpoint SHA-256 `F28C8B463F64693561E118790FD5E9D68739F85C697B9B58F391F56E1911ED99`. The checkpoint may be superseded by later unrelated supervisor saves; the range/body/route facts below remain the evidence used for this callback.
- Current prestate remains an unmodeled exact source-shaped range `[0x004fe790,0x004fe9d1)`: 165 decoded instructions, prologue `55 8B EC 81 EC 18 02 00 00`, `retn 10h` at `0x004fe9c9`, range-check call at `0x004fe9cc`, 16 calls, no function name/type/comment channels, and no function object. Leading `[0x004fe782,0x004fe790)` is 14 `0xcc`; following `[0x004fe9d1,0x004fe9e0)` is 15 `0xcc`.
- Current route evidence remains exhaustive and negative: zero xrefs and zero code/data/immediate/little-endian-pointer routes to `0x004fe790`. Positive control `0x004fe778 -> 0x004fe9e0` proves the live submit uses UID0002QC instead. This child remains retained source, not active submit code and not compiler padding.
- Runtime packet shape remains exact: opcode `0x26`; three `WideCharToMultiByte` fields with one-byte lengths and `memmove` payload copies; fourth wide argument parsed through `_wtol`; big-endian uint32 append; local terminator outside the sent fields; final `QueueAndSendPacket(g_packetSender, packet, packetLength)`. Its existing formal C++ and `90/90` score remain unchanged.
- Parent relationship is now explicit: [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md) is a reviewed false/non-emitting address index. This child continues to own its exact source body through UID0000I3; parent reclassification removes only the duplicate empty parent marker.
- Supervisor Gate 2B handoff, not applied by B002: create exact function `[0x004fe790,0x004fe9d1)`, name it `SendPasswordChangeRequestWithAccountId`, apply `void __stdcall SendPasswordChangeRequestWithAccountId(const wchar_t *accountName, const wchar_t *currentPassword, const wchar_t *newPassword, const wchar_t *accountIdText)`, and set only the function repeatable comment to `Retained ChangePasswordDialogPane opcode 0x26 sender: three length-prefixed multibyte fields followed by a big-endian uint32 parsed from accountIdText; no current caller, xref, or pointer route.` Preserve bytes, exact end, zero xrefs, and absent function-regular/entry-regular/entry-repeatable comments.

## Score Rationale

- Completion is `90`: the page records current MCP session `80de0a67`, exact IDB/executable provenance and SHA-256, PE section map, no-function/no-decompile state, no-route/no-pointer proof, positive controls, precise raw call sequence, active sibling/control mapping, PacketBuffer scalar-writer child support, [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md) / `memmove` copy-helper support, generated-source caveats, rejected owner alternatives, B004 supersession, and a formal first-draft C++ block that preserves the exact packet layout.
- Confidence is `90`: behavior, range, source owner, and negative route conclusions are supported by current MCP and local PE evidence, not only older notes. It does not rise higher because no direct caller/table/pointer proves the raw body's liveness, IDA has no function object/prototype, the original helper spelling is unrecovered, and `accountIdText` remains an inferred fourth-field name.

## Evidence-Backed Remaining Caps

- No current IDA function object, start xref, table entry, pointer hit, immediate, direct transfer, or caller-side path proves reachability. Treat the helper as retained source-family code, not as an active submit path.
- Final name remains conservative because the exact fourth field role is not proven from a caller; current evidence proves only a wide numeric input parsed through `_wtol` and encoded as a 32-bit big-endian packet field.

## Cross-References

- [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md)
- [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md)
- [UID:0002QA][0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore](by-memory/0x004fdd40-0x004fe782.ChangePasswordDialogPaneCore.md)
- [UID:0002QC][0x004fe9e0-0x004febe2.SendPasswordChangeRequest](by-memory/0x004fe9e0-0x004febe2.SendPasswordChangeRequest.md)
- [UID:00001L][ChangePasswordDialogPane](by-class/ChangePasswordDialogPane.md)

## Changes

### 2026-08-14 B003 Socket API closure

- The formal body now calls the reconstructed Socket member directly as `g_packetSender->QueueAndSendPacket(packet, static_cast<short>(packetLength))`. The explicit narrowing matches `Socket::QueueAndSendPacket(const void *, short)` and removes the undocumented free-function facade.
- The helper remains a file-local `static void __stdcall` retained account-id packet variant at `0x004fe790-0x004fe9d1`, with no current direct code xref or accepted pointer-table route. Opcode `0x26`, three length-prefixed converted text fields, `_wtol`, the big-endian numeric field, range checks, and sent-length behavior are unchanged.
- UID0000I3 remains owner/emitter at `90/90`; H stays blank because the retained helper is file-local. This source inclusion preserves the no-route liveness caveat rather than promoting the helper to the active submit path.

- 2026-07-24 B002 UID00022W aggregate implementation callback support sync:
  - Score/formal/owner/emitter remain `90/90`, existing source-ready retained-helper C++, and UID0000I3.
  - Added current checkpoint, exact no-function and no-route evidence, active-sibling contrast, packet shape, false-parent relationship, and complete supervisor-only IDA action/readback handoff without changing retained liveness.
- 2026-06-25 B005 accepted implementation callback:
  - Changed to: `90/90`, with [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) owner/emitter routing, reconstructable state, and blank emitter position preserved.
  - Inserted formal `RECONSTRUCTION_CPP CODE` for `SendPasswordChangeRequestWithAccountId`, matching opcode `0x26`, three `WideCharToMultiByte` conversions, three `0x100` rangechecks, `PacketBufferWriteUInt8` length/opcode writes, three `memmove(...)` payload copies supported by [UID:0001BH][0x00516220-0x00516238.MemmoveWrapper](by-memory/0x00516220-0x00516238.MemmoveWrapper.md), `_wtol` fourth-field conversion, `PacketBufferWriteUInt32BE` numeric append, local terminator, and final `QueueAndSendPacket(g_packetSender, packet, packetLength)` send length.
  - Superseded B004's blank-formal-C++/no-code proof. B004's no-function/no-xref/no-pointer/no-active-route evidence remains valid audit evidence and confidence cap, but no longer blocks formal C++ for this retained source-family helper.
- 2026-06-12 live IDA repair:
  - What existed before: the page scored `74/84`, relied on stored evidence, and described the helper as converting four UTF-16 strings.
  - Changed to: `86/87`, with live IDA MCP byte, lookup, xref, instruction, helper-decompile, sibling-decompile, and range-signature evidence.
  - Why: direct disassembly proves the first three arguments are converted string fields, while the fourth argument is parsed through CRT `__wtol` and appended through `sub_5753F0` as a 32-bit big-endian value.
  - At that time C++ remained blank because the body had no direct caller/function object and the fourth field's source-level name was still not proven; B005 later superseded that no-code disposition with formal retained-helper C++.
- 2026-06-22 B002 MEMTOOL triage:
  - Reviewed the generated Advanced-Error-Scan warning for missing start/end/name text and rejected it as a current substantive documentation defect because the page already contains both addresses and the normalized title label.
  - Kept `86/87`, [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) owner/emitter routing, exact range/name, reconstructable state, and blank direct C++ unchanged.
  - Recommended only a future scanner-visible slug hygiene line if a refreshed `memory_ranges.py report` still flags the page after this evidence is preserved.
- 2026-06-23/2026-06-24 B004 MCP-backed source-quality implementation:
  - Changed at the time to: `88/89`, with [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md) owner/emitter routing, reconstructable state, blank emitter position, and blank formal C++ unchanged.
  - Summary/evidence: current MCP session `80de0a67`, PE SHA-256 `9AEC210BBC5CE592176A21DD8E9D9FD8F250B8D9EA78237915A99BA8CFA9A632`, route-negative MCP/PE scans, positive controls, raw packet behavior, active sibling/control mapping, generated-source caveats, rejected owner alternatives, and the then-current no-code proof were incorporated.
  - B005 supersedes the B004 conclusion that C++ remains blank; the B004 route-negative evidence remains useful as confidence-cap audit history.
- 2026-06-03 evidence consolidation:
  - What existed before: `70/82`, with source-family ownership and stored IDA notes but no cross-document evidence section.
  - Changed to: `74/84`, with aggregate, active sibling sender, dialog core, file/class, and older generated-record absence evidence.
  - B005 supersedes the historical blank-C++ conclusion; reachability and fourth-field source spelling remain confidence caps.
- 2026-06-02: Raised scoring from `64/78` to `70/82` and attached autogen parent [UID:0000I3][ChangePasswordDialogPane](by-file/ChangePasswordDialogPane.md).
  - Before: the page named `ChangePasswordDialogPane` as a likely owner but stayed unassigned because no caller reaches the raw start.
  - After: the page records a conservative source-family attachment through the adjacent attached aggregate and matching packet-send behavior, while preserving the no-caller reachability caveat. B005 later superseded the initial blank-C++ disposition with formal retained-helper C++.
  - Evidence: existing 2026-06-01 IDA MCP lookup/caller/xref/disassembly/raw-byte notes on this page and [UID:00022W][0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers](by-memory/0x004fe790-0x004ff03f.ChangePasswordDialogPacketHelpers.md).
