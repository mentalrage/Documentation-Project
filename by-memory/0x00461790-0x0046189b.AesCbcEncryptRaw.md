*** UID:00020L | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00020J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00020J | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void AesCbcEncryptBuffer(unsigned char* dst, unsigned char* src, unsigned int length,
                                const unsigned char* keyOrNull, const unsigned char* ivOrNull)
{
    unsigned char tail = (unsigned char)(length & 0x0f);

    for (int i = 0; i < 16; ++i)
        dst[i] = src[i];

    s_aesState.currentBlock = dst;

    if (keyOrNull != 0)
    {
        s_aesState.currentKey = keyOrNull;
        AesExpandKey128();
    }

    const unsigned char* chain = ivOrNull != 0 ? ivOrNull : s_aesState.cbcChain;
    s_aesState.cbcChain = chain;

    if (length != 0)
    {
        unsigned int blockCount = ((length - 1) >> 4) + 1;

        while (blockCount-- != 0)
        {
            for (int i = 0; i < 16; ++i)
                src[i] ^= chain[i];

            for (int i = 0; i < 16; ++i)
                dst[i] = src[i];

            s_aesState.currentBlock = dst;
            AesEncryptBlock();

            chain = dst;
            src += 16;
            dst += 16;
        }

        s_aesState.cbcChain = chain;
    }

    if (tail != 0)
    {
        for (int i = 0; i < 16; ++i)
            dst[i] = src[i];

        for (int i = tail; i < 16; ++i)
            dst[i] = 0;

        s_aesState.currentBlock = dst;
        AesEncryptBlock();
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Raw AES-CBC encrypt wrapper with 93 decoded instructions, key-schedule/encrypt-block call offsets, scratch-global refs, no raw-start xrefs, and padding documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00461790-0x0046189b AesCbcEncryptRaw

## Status

- Disposition: reconstructable source-authored AES/CBC support code.
- Entity kind: raw function-shaped wrapper, not an IDA-modeled function.
- Confidence: very strong for the raw range, AES-CBC encrypt behavior, local AES-island ownership, wrapper negative-liveness search, and source-facing signature direction.
- Likely source owner: [UID:00020J][AesBlockCipher](by-file/AesBlockCipher.md).
- Rebuild handling: source-authored wrapper logic around source-declared/generated-binary AES tables and mutable scratch state.

## Behavior

This raw wrapper copies an initial 16-byte block from input to output, optionally rebuilds the AES-128 key schedule when a key pointer is supplied, and then processes `ceil(length / 16)` blocks.

For each full block it XORs the input block with the current chaining block, copies the result into the output block, sets `0x0067a878` to that output block, calls the AES block encrypt helper at `0x004619c0`, and advances the chaining pointer to the encrypted output block.

If `length & 0x0f` is nonzero after the loop, the tail path copies another final partial block at the post-loop source pointer, zero-fills the remaining bytes to a 16-byte block, and encrypts it. This is exact binary behavior and should not be simplified to ordinary one-block zero padding without the caveat.

## Control Flow Map

| Phase | Binary behavior | Reconstruction note |
| --- | --- | --- |
| Chaining setup | Copies the supplied IV/chaining block into the first output block when present, otherwise uses the stored chaining state at `0x0067a930`. | Preserve the binary's chaining semantics until live callers prove a cleaner API. |
| Optional key setup | When the key pointer argument is non-null, writes it to `0x0067a87c` and calls [UID:00020Q][0x00461fe0-0x00462114.AesKeySchedule128](by-memory/0x00461fe0-0x00462114.AesKeySchedule128.md). | This is an AES-128 schedule refresh, not generic cipher setup. |
| Full-block loop | XORs a plaintext block with the current chaining block, copies the XORed block into `dst`, sets `0x0067a878` to that destination block, then calls [UID:00020N][0x004619c0-0x00461c07.AesEncryptBlock](by-memory/0x004619c0-0x00461c07.AesEncryptBlock.md). | The block helper encrypts in place through the scratch current-block pointer. |
| Chain update | Advances the chain pointer to the encrypted output block after each full block. | This is standard CBC encrypt chaining. |
| Partial tail | Copies the remaining bytes, zero-fills to 16 bytes, and encrypts one final padded block. | Padding is zero-fill behavior in this wrapper; external framing remains unknown. |

## Scratch State Map

| Address | Role in this wrapper | Related owner |
| --- | --- | --- |
| `0x0067a878` | Current 16-byte block pointer passed implicitly to the AES block encrypt helper. | [UID:00028Y][0x0067a878-0x0067a934.AesMutableStateAndKeySchedule](by-memory/0x0067a878-0x0067a934.AesMutableStateAndKeySchedule.md) |
| `0x0067a87c` | Key pointer consumed by the key schedule helper when a new key is supplied. | [UID:00020Q][0x00461fe0-0x00462114.AesKeySchedule128](by-memory/0x00461fe0-0x00462114.AesKeySchedule128.md) |
| `0x0067a880-0x0067a92f` | Expanded AES-128 key schedule produced before encrypting blocks. | [UID:00020K][AesBlockCipherStateAndTables](by-global/AesBlockCipherStateAndTables.md) |
| `0x0067a930` | Stored default chaining block/state when the caller does not supply an IV. | [UID:00028Y][0x0067a878-0x0067a934.AesMutableStateAndKeySchedule](by-memory/0x0067a878-0x0067a934.AesMutableStateAndKeySchedule.md) |

## Instruction Landmarks

| Address | Instruction evidence | Interpretation |
| --- | --- | --- |
| `0x00461790` | Prologue begins with `push ebp; mov ebp, esp`; IDA still reports no function object. | Raw function-shaped entry. |
| `0x004617a0` | Masks `[ebp+0x10]` with `0x0f` into a local byte. | Saves the partial-tail byte count. |
| `0x004617b0-0x004617bc` | 16-byte copy loop from `[ebp+0x0c]` to `[ebp+0x08]`. | Copies the initial source block to the destination/current block area. |
| `0x004617cb-0x004617d0` | Stores `[ebp+0x14]` to `0x0067a87c` and calls `0x00461fe0` when non-null. | Optional AES-128 key-schedule refresh. |
| `0x004617da-0x004617e6` | Loads `0x0067a930`, conditionally replaces it with `[ebp+0x18]`, then stores back to `0x0067a930`. | Selects caller IV or prior chaining block. |
| `0x004617f0-0x004617f7` | Computes `((length - 1) >> 4) + 1`. | Full-loop count is `ceil(length / 16)` for nonzero length. |
| `0x00461810-0x00461819` | XORs 16 bytes in the source block against the current chain. | In-place CBC pre-encrypt XOR. |
| `0x00461826-0x0046183a` | Copies the XORed block to `dst`, writes `0x0067a878 = dst`, then calls `0x004619c0`. | Encrypts the current destination block in place. |
| `0x0046184d` | Writes the encrypted destination block pointer to `0x0067a930`. | Persists the new chain after the loop. |
| `0x00461871-0x0046188f` | Zero-fills `16 - tail` bytes with `_memset`, writes `0x0067a878 = dst`, and calls `0x004619c0`. | Encrypts the partial padded block. |
| `0x0046189a` | `retn`; following bytes `0x0046189b-0x004618a0` are `0xcc`. | Exact raw end and alignment before decrypt wrapper. |

## Interface Hypothesis

Likely source-facing shape:

```cpp
void AesCbcEncryptBuffer(unsigned char* dst,
                         unsigned char* src,
                         unsigned int length,
                         const unsigned char* keyOrNull,
                         const unsigned char* ivOrNull);
```

The binary mutates through the source pointer during the pre-encrypt XOR step before copying to the destination block. Preserve that behavior until callers or original source evidence prove a safer non-mutating interface.

## Rule 26 Incorporated B002 Research

The B002 AES source-quality report resolves this wrapper as retained source-authored AES-CBC encrypt support with no surviving external entry evidence. IDA still has no modeled function at `0x00461790`, `xrefs_to 0x00461790` is empty, and literal VA scans for all six AES helper starts found no stored address bytes. That rejects ordinary direct-call, callback-table, vtable, and stored function-pointer reachability. The body is still source-authored code rather than padding or compiler glue because it is a complete prologued wrapper with internal calls to [UID:00020Q][0x00461fe0-0x00462114.AesKeySchedule128](by-memory/0x00461fe0-0x00462114.AesKeySchedule128.md) and [UID:00020N][0x004619c0-0x00461c07.AesEncryptBlock](by-memory/0x004619c0-0x00461c07.AesEncryptBlock.md).

Source placement remains [UID:00020J][AesBlockCipher](by-file/AesBlockCipher.md). Network, auth, archive, startup, and class-object ownership are rejected because there are no subsystem callers, protocol strings, resource references, object allocation, `this` pointer, vtable, constructor, or destructor. The implementation is an isolated utility island with AES-only table/state xrefs.

The best source model is file-static AES state, not a class. B002 recommends a file-static `AesBlockCipherState` with `currentBlock`, `currentKey`, `expandedKey[176]`, and `cbcChain`, mapped to [UID:00028Y][0x0067a878-0x0067a934.AesMutableStateAndKeySchedule](by-memory/0x0067a878-0x0067a934.AesMutableStateAndKeySchedule.md). The wrapper should not expose `dword_67A878`/`dword_67A930` style names in source-facing documentation.

Formal C++ now emits the private `AesCbcEncryptBuffer` wrapper at emitter position `50`, after the table, state, key schedule, and block helpers. The body intentionally preserves source-buffer mutation during the pre-encrypt XOR and the binary's extra post-loop zero-filled tail block for non-multiple lengths.

## Evidence

- 2026-06-05 IDA MCP `lookup_funcs` reports `0x00461790`, `0x0046189b`, and `0x004618a0` are not functions; modeled callees remain `sub_4619C0` at `0x004619c0` (`0x247` bytes) and `sub_461FE0` at `0x00461fe0` (`0x134` bytes).
- 2026-06-05 IDA MCP range decode covers 93 instructions from `0x00461790` through the `retn` at `0x0046189a`.
- 2026-06-05 IDA MCP `xrefs_to` shows no xrefs to `0x00461790`, `0x0046189b`, or `0x004618a0`.
- 2026-06-05 IDA MCP `xrefs_to` confirms `0x004619c0` is called from raw-wrapper offsets `0x0046183a` and `0x0046188f`, and `0x00461fe0` is called from `0x004617d0`.
- 2026-06-05 IDA MCP `xrefs_to` shows this wrapper writes/reads `0x0067a878` at `0x004617c1`, `0x00461834`, and `0x00461889`; `0x0067a87c` at `0x004617cb`; and `0x0067a930` at `0x004617da`, `0x004617e6`, and `0x0046184d`.
- A 2026-05-27 loaded-dword scan found no stored pointer to `0x00461790`.
- 2026-06-05 IDA MCP byte reads confirm `0x0046178a-0x00461790` and `0x0046189b-0x004618a0` are `0xcc` padding, matching [UID:0000VN][-ignored](by-memory/-ignored.md).

## Autogen Status

- Reconstructable: true.
- Parent: [UID:00020J][AesBlockCipher](by-file/AesBlockCipher.md), position `10`.
- C++: first-draft formal wrapper present at emitter position `50`, depending on the coordinated AES file-level state/table/helper declarations.

## Score Rationale

Completion is raised to 90 for byte-level range decoding, exact instruction landmarks, scratch-global xrefs, local callee xrefs, wrapper negative-liveness search, file-static state model, nonstandard tail caveat, refreshed boundary evidence, and first-draft formal C++ at emitter position `50`. Confidence rises for the raw behavior, but stays below final-source levels because exact original source spelling and public API exposure remain inferred.

## Evidence-Bounded Remaining Questions

- No direct caller, stored entrypoint, vtable/callback, or literal VA reference is proven. The wrapper is retained source-linked code with no surviving external route in current evidence.
- The tail behavior is exact, but the original framing contract is not proven. Do not rename it as PKCS padding or a normal one-block zero-pad API.

## Cross-References

- [UID:00020J][AesBlockCipher](by-file/AesBlockCipher.md)
- [UID:00020K][AesBlockCipherStateAndTables](by-global/AesBlockCipherStateAndTables.md)
- [UID:00020N][0x004619c0-0x00461c07.AesEncryptBlock](by-memory/0x004619c0-0x00461c07.AesEncryptBlock.md)
- [UID:00020Q][0x00461fe0-0x00462114.AesKeySchedule128](by-memory/0x00461fe0-0x00462114.AesKeySchedule128.md)

## Changes

- 2026-05-31: Changed autogen reconstruction status from unassigned to reconstructable under [UID:00020J][AesBlockCipher](by-file/AesBlockCipher.md). Summary: IDA MCP recheck confirmed this raw wrapper belongs to the isolated AES helper island and calls the local key-schedule and encrypt-block helpers; the C++ code section remains empty until a range-local rewrite is completed.

- 2026-05-31: Added exact local-call xrefs. Summary/evidence: IDA MCP `xrefs_to` shows the modeled encrypt-block helper is called from two raw-wrapper sites and the key schedule helper from this wrapper's key-refresh path.
- 2026-06-02: Raised completion to `70` and added control-flow, scratch-state, autogen, boundary, and score-rationale sections. Summary/evidence: existing AES helper/data pages already split the block encrypt helper, key schedule, mutable state, and padding around this raw wrapper, allowing the wrapper behavior to be documented more completely without emitting final C++.
- 2026-06-05: Raised completion/confidence to `82/90` and added instruction landmarks from live IDA. Summary/evidence: IDA MCP decoded the raw range as 93 instructions ending at `0x0046189a`, confirmed no xrefs to the raw start or neighbor boundary, refreshed the key-schedule/encrypt-block call offsets, refreshed scratch-global refs, and byte-confirmed `0xcc` padding on both sides; C++ remains blank below the 95/95 gate.
- 2026-06-17 B002 source-quality execution: Raised to `86/91`.
  - Incorporated wrapper liveness recheck, literal VA negative evidence, source-facing `AesCbcEncryptBuffer` direction, file-static state model, exact extra zero-filled tail caveat, rejected subsystem/class owners, and coordinated no-C++ rationale.
- 2026-06-29 B012 UID00020J implementation callback: raised completion/confidence to `90/92`, set `EMITTER_POSITION_OPTIONAL:50`, and populated formal C++ with the accepted `AesCbcEncryptBuffer` body. Summary/evidence: accepted report `00020J-AesBlockCipher-empty-emitter-family-source-quality.md` preserves optional key refresh, IV/default-chain selection, source-buffer XOR mutation, block encryption, chain update, and the nonstandard tail block.
