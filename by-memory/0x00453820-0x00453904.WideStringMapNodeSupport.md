*** UID:0001ZY | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct WideStringMapNode
{
    WideStringMapNode *left;
    WideStringMapNode *parent;
    WideStringMapNode *right;
    unsigned char color;
    unsigned char isSentinel;
    unsigned char reserved[2];
    SimpleUString key;
    int value;
};

static WideStringMapNode *CreateWideStringMapSentinel()
{
    WideStringMapNode *node = static_cast<WideStringMapNode *>(
        operator new(sizeof(WideStringMapNode)));
    node->left = node;
    node->parent = node;
    node->right = node;
    node->color = 1;
    node->isSentinel = 1;
    return node;
}

static WideStringMapNode *CreateWideStringMapNodeFromHeader(WideStringMapNode *header)
{
    WideStringMapNode *node = static_cast<WideStringMapNode *>(
        operator new(sizeof(WideStringMapNode)));
    node->left = header->left;
    node->parent = header->left;
    node->right = header->left;
    return node;
}

static void DestroyWideStringMapNodes(WideStringMapNode *node)
{
    while (!node->isSentinel) {
        DestroyWideStringMapNodes(node->right);
        WideStringMapNode *next = node->left;
        node->key.Clear();
        FreeMemory(node);
        node = next;
    }
}

static void FreeWideStringMapNode(WideStringMapNode *node)
{
    FreeMemory(node);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Assigned to [UID:0000OB][StringUtil](by-file/StringUtil.md) after Batch073 repaired stale header/gate text; live IDA confirms four wide-string map/tree helper functions, padding boundaries, 44-byte node allocation/destruction behavior, minimap and metadata caller spread, shared source-owned support status, and LinkedList exclusion. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00453820-0x00453904 WideStringMap Node Support

## Supervisor Parent-Gate Audit

Batch 073 repairs stale gate text and restores the file-level autogen assignment to [UID:0000OB][StringUtil](by-file/StringUtil.md). The child is `86/88`, and the direct parent is now `85/88`, so both sides satisfy the corrected `85/85` gate. The assignment remains a utility-file placement rather than a final source-name claim: the helper island is shared by minimap and metadata callers, uses 44-byte UTF-16 map/tree nodes, and is explicitly not the separate 16-byte [UID:0000KR][LinkedList](by-file/LinkedList.md) intrusive-list helper family.

## Status

- Entity kind: shared helper function island
- Disposition: reconstructable NexusTK project support code
- Assigned owner: [UID:0000OB][StringUtil](by-file/StringUtil.md) as shared wide-string keyed container support used by minimap and metadata code
- Rebuild handling: `source-authored`
- Confidence: strong for function boundaries and behavior; medium-strong for `StringUtil` parent placement because final original source-module name is still open
- Evidence basis: live IDA MCP `py_eval`, Hex-Rays decompilation, caller xrefs, callee xrefs, and boundary bytes on 2026-06-03; live IDA MCP boundary recheck on 2026-06-07.

## Covered Functions

| Range | Proposed name | Notes |
| --- | --- | --- |
| `0x00453820-0x00453839` | `CreateWideStringMapSentinel` | Allocates a `0x2c`-byte node, self-links the first three pointer slots, and sets the sentinel/color flags word at `+0x0c` to `0x0101`. |
| `0x00453840-0x0045385d` | `CreateWideStringMapNodeFromHeader` | Allocates a `0x2c`-byte node and initializes the three link fields from the owner/header node. |
| `0x00453860-0x004538e8` | `DestroyWideStringMapNodes` | Recursively walks child links until the sentinel flag at `+0x0d`, frees heap-backed wide-string payload storage, resets the inline string fields, and frees each node. |
| `0x004538f0-0x00453904` | `FreeWideStringMapNode` | Thin wrapper around the shared deallocator at `0x005c7526`. |

## Behavior

This is the shared tree/list support island for a small-string wide-character keyed map. It is not a `MiniMapDialog` method even though nearby minimap code calls it.

The sentinel constructor initializes a self-linked node with sentinel/color flags. The cleanup routine treats offset `+0x0d` as the sentinel marker, recursively destroys the right/child chain at slot `+0x08`, advances through the left/next slot, and releases the UTF-16 small-string payload when the node capacity at `+0x24` is heap-backed.

## Parent Assignment

This page now attaches to [UID:0000OB][StringUtil](by-file/StringUtil.md) rather than to `MiniMap` or `MetaMan`. The ownership evidence is the combination of a wide-string payload layout, map/tree sentinel behavior, and caller spread across the minimap and metadata subsystems. Feature callers instantiate or use the helper, but no single feature owns the container mechanics.

[UID:0000KR][LinkedList](by-file/LinkedList.md) is not the right parent: that file documents the separate 16-byte intrusive list helper family around `0x004570e0`, `0x00457430`, and `0x00457550`. This range uses 44-byte tree nodes with UTF-16 small-string payload fields and should stay with utility string/container support.

## 2026-06-07 Supervisor Gate Audit

- Final score for this batch: `86/88`.
- Assignment state: assigned to [UID:0000OB][StringUtil](by-file/StringUtil.md).
- Assignment justification: the page first gained exact live IDA function-size evidence, decompiled constructor/destructor behavior for all four helpers, caller spread across minimap and metadata code, and the `LinkedList` layout exclusion. That evidence clears the corrected 85/85 assignment gate before the parent claim: the functions are source-authored shared wide-string keyed container support, not a minimap- or metadata-private routine.
- Remaining cap: source-facing helper/type names and the exact original utility file split remain provisional, so no final C++ is emitted.

## Evidence

- Live IDA MCP `py_eval` on 2026-06-03 used `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, image base `0x00400000`, md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- IDA reports four functions in this helper island:
  - `sub_453820` at `0x00453820-0x00453839`
  - `sub_453840` at `0x00453840-0x0045385d`
  - `sub_453860` at `0x00453860-0x004538e8`
  - `sub_4538F0` at `0x004538f0-0x00453904`
- Current adjacent starts are `sub_4537E0` at `0x004537e0-0x0045381b` before this island and `sub_453910` at `0x00453910-0x00453985` after it. `0x0045381b-0x00453820` is five `0xcc` bytes, and `0x00453904-0x00453910` is twelve `0xcc` bytes.
- Hex-Rays shows `sub_453820` allocates `0x2c` bytes, writes the first three link slots to the node itself, and sets `*((WORD*)node + 6) = 257`.
- Hex-Rays shows `sub_453840` allocates `0x2c` bytes and initializes the first three link slots from the owner/header node, matching a non-sentinel node constructor.
- Hex-Rays shows `sub_453860` recursively destroys child links, checks the sentinel byte at `+0x0d`, frees heap string storage through `sub_5C7526` when capacity at `+0x24` is at least `8`, applies the MSVC large-allocation guard for wide strings, resets the inline string fields, and frees the node.
- Hex-Rays shows `sub_4538F0` is a one-call wrapper around `sub_5C7526`.
- 2026-06-07 live IDA MCP `lookup_funcs` reconfirmed the four function starts and sizes: `0x00453820` size `0x19`, `0x00453840` size `0x1d`, `0x00453860` size `0x88`, and `0x004538f0` size `0x14`.

## Caller Notes

Live caller xrefs on 2026-06-03 show this is shared support rather than a `MiniMapDialog`-local helper:

- `sub_453820` has two external callers: `0x00450d4c` in `sub_450CA0` (`MiniMapDialog` constructor storage at `+0x5bc`) and `0x00522836` in `sub_5227D0` (`MetaMan` constructor storage).
- `sub_453840` has two external callers: `0x004530be` in `sub_453090` and `0x00523d1e` in `sub_523CF0`, matching parallel insertion/allocation helper paths in minimap and metadata code.
- `sub_453860` has six external caller sites plus its recursive self-call: `0x00451441` in `sub_451330`, `0x004524a7` in `sub_4523D0`, `0x004536a2` in `sub_453670`, `0x005228a2` in `sub_522860`, `0x005233f0` in `sub_5233D0`, and `0x005244e6` in `sub_5244E0`.
- `sub_4538F0` has two external callers: `0x004532fb` in `sub_453090` and `0x00523d99` in `sub_523CF0`, both exception-cleanup paths after node allocation.

The caller spread supports a shared utility owner rather than assigning the range to `MiniMapDialog` or `MetaMan` alone.

## Autogen And C++ Handling

- `RECONSTRUCTABLE:TRUE` is appropriate because the four functions are source-owned container support used by project code, not compiler-only runtime code.
- `AUTOGEN_PARENT_UID` is [UID:0000OB][StringUtil](by-file/StringUtil.md). This reduces the unassigned queue while keeping the helper island with shared string utility code rather than `MiniMapDialog`, `MetaMan`, or `LinkedList`.
- `RECONSTRUCTION_CPP` now carries the accepted first-draft four-helper cluster. Type/helper names remain inferred, but the formal block is the current source-quality disposition.

## 2026-07-01 B007 Source-Quality Disposition

The accepted implementation callback emits the four-helper first draft in the formal block. Field names remain inferred (`left`, `parent`, `right`, `color`, `isSentinel`, `key`, `value`), but the ownership decision is settled: this is `StringUtil.cpp` local wide-string map support, not MiniMap, MetaMan, or generic LinkedList ownership.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 89 | The page records current IDB identity, exact function boundaries, adjacent padding, decompiled behavior for all four helpers, caller spread across minimap and metadata code, the LinkedList non-owner contrast, explicit parent/autogen/C++ handling, and the accepted four-helper first-draft C++ cluster. |
| Confidence | 89 | Confidence is high for behavior, boundaries, and shared utility ownership because live IDA confirms the helper island and caller spread. It remains below final because the exact original source module and field/helper spellings remain inferred. |
| Reconstructable | true | The helper island is source-authored wide-string map/tree support used by NexusTK-owned minimap and metadata code. |

## Cross-References

- [UID:0000XK][0x00450ca0-0x0045381b.MiniMapDialog](by-memory/0x00450ca0-0x0045381b.MiniMapDialog.md)
- [UID:00008C][MiniMapDialog](by-class/MiniMapDialog.md)
- [UID:000088][MetaMan](by-class/MetaMan.md)
- [UID:0000LC][MetaMan](by-file/MetaMan.md)
- [UID:0000OB][StringUtil](by-file/StringUtil.md)
- [UID:0000KR][LinkedList](by-file/LinkedList.md)

## Changes

- 2026-07-01 B007 [UID:0000OB][StringUtil](by-file/StringUtil.md) implementation callback: raised score to `89/89`, inserted the accepted four-helper C++ cluster, and preserved inferred `WideStringMapNode` field-name caveats plus rejected MiniMap/MetaMan/LinkedList ownership.
- 2026-06-07 Batch073 gate repair:
  - Changed to: `AUTOGEN_PARENT_UID:0000OB` with the corrected parent score `85/88`.
  - Summary/evidence: removed stale placeholder gate text that said StringUtil was below gate; the child remains `86/88`, StringUtil now clears `85/85`, and the existing written evidence ties the range to shared wide-string keyed map/tree support through exact helper boundaries, 44-byte node behavior, minimap/metadata caller spread, and the LinkedList exclusion. Final C++ remains blank because final helper/type names are still provisional.
- 2026-06-07 A009 Batch 001 parent assignment:
  - Changed to: `COMPLETION:86`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID:0000OB`.
  - Summary/evidence: live IDA MCP reconfirmed the four exact helper starts and sizes, while existing caller evidence keeps the range shared between minimap and metadata. Attached to `StringUtil` only after the page cleared the corrected 85/85 gate as shared wide-string keyed container support, not `MiniMap`, `MetaMan`, or `LinkedList`; `LinkedList` covers a different 16-byte intrusive-list family. C++ remains blank because final helper/type names are still below final-source quality.

- 2026-06-03: Raised from `70/85` to `84/90` and set `RECONSTRUCTABLE:TRUE` after live IDA MCP revalidation of the current IDB identity, four exact function boundaries, adjacent padding, decompiled node-constructor/destructor behavior, minimap and metadata caller spread, recursive cleanup semantics, and deallocator wrapper role. Parent and C++ remain blank because shared source ownership and final source names are still below the attachment/final-source threshold.
