*** UID:00020B | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:82 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000NT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x00460d60-0x00460dee Byte Span Fill Rows

## Status

- Disposition: reconstructable NexusTK render/support code.
- Entity kind: raw/unmodeled free helper.
- Likely source file: [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) `SoftwareBlend16` or neighboring alpha-mask/span utility source.
- Confidence: strong for byte boundary, raw-body instruction window, row-loop behavior, block helper call, byte-value expansion, scalar tail, and SoftwareBlend16 ownership; medium for final source-facing helper name and reachability because IDA has no function object or direct external xref to the raw start.
- Rebuild handling: `source-authored`.

## Address Range

`0x00460d60-0x00460dee` (raw/unmodeled body, size `0x8e`, 64 decoded instructions).

The preceding `0x00460d51-0x00460d60` and following `0x00460dee-0x00460df0` bytes are alignment padding.

IDA MCP `lookup_funcs`/`py_eval` on 2026-06-05 confirms `ida_funcs.get_func(0x00460d60)` is `None` and `idautils.XrefsTo(0x00460d60)` is empty. This page therefore documents a raw helper body that must be reconstructed even though IDA has not modeled it as a function.

## Inferred Signature

Current argument use supports this source-level shape:

```cpp
void ByteSpanFillRows(
    unsigned char* dst,
    int dstPitchBytes,
    unsigned char value,
    int widthBytes,
    int rowCount);
```

`widthBytes` is split into an eight-byte block count and a scalar byte tail. `value` is expanded to a repeated `0x01010101 * value` dword for the scalar `rep stosd` portion.

## Behavior

This helper fills byte spans across multiple rows with one byte value. It divides the row width into eight-byte blocks, calls [UID:00020F][0x00460fa0-0x00460ffe.ByteSpanFill8Blocks](by-memory/0x00460fa0-0x00460ffe.ByteSpanFill8Blocks.md), and then handles the scalar tail with `rep stosd`/`rep stosb`.

## Reconstruction Evidence

| Item | Evidence | Reconstruction impact |
| --- | --- | --- |
| Raw body boundary | Live IDA sees no function object at `0x00460d60`, but code heads decode from `0x00460d60` through terminal `retn` at `0x00460ded`. | Keep this as a raw helper page rather than a modeled-function page; range is exact even without an IDA function object. |
| No raw-start xrefs | `XrefsTo(0x00460d60)` is empty. | Reachability/name confidence stays below the modeled byte-span helpers. |
| Block helper call | The body calls [UID:00020F][0x00460fa0-0x00460ffe.ByteSpanFill8Blocks](by-memory/0x00460fa0-0x00460ffe.ByteSpanFill8Blocks.md) at `0x00460d8e`, and that helper reports the reciprocal raw xref. | Eight-byte bulk fill is delegated to the sibling helper before scalar tail work. |
| Caller argument order | The push window at `0x00460d89-0x00460d8e` passes destination pointer, destination pitch, fill byte, eight-byte block count, and row count. | The inferred signature should keep `value` as a byte and block count in qword units for the helper call. |
| Block split | Live disassembly computes `width / 8` at `0x00460d70-0x00460d82`, calls the block helper when positive, then advances the destination pointer by `blocks * 8`. | Reconstruction should split the row width into eight-byte blocks plus scalar tail. |
| Scalar tail | Live disassembly at `0x00460db4-0x00460dd2` expands the fill byte with `imul eax, 1010101h`, emits `rep stosd` for dwords, then `rep stosb` for the last bytes. | Source-level fill must preserve exact byte fill semantics while allowing compiler string-store lowering. |
| Row stepping | Live disassembly advances the destination pointer by `dstPitchBytes` at `0x00460ddd`, decrements row count, and loops back to the scalar tail. | Helper is a strided row/span fill, not a flat memset. |
| Boundary padding | `0x00460d51-0x00460d60` and `0x00460dee-0x00460df0` are `0xcc` padding. | The raw range does not absorb neighboring modeled helpers. |

## Reconstructed C++ Candidate

```cpp
void ByteSpanFillRows(unsigned char* dst, int dstPitchBytes,
                      unsigned char value, int widthBytes, int rowCount)
{
    const int blockBytes = (widthBytes / 8) * 8;
    const int tailBytes = widthBytes - blockBytes;

    if (blockBytes > 0)
        ByteSpanFill8Blocks(dst, dstPitchBytes, value, blockBytes / 8, rowCount);

    dst += blockBytes;

    for (int y = 0; y < rowCount; ++y)
    {
        unsigned char* row = dst;
        for (int x = 0; x < tailBytes; ++x)
            row[x] = value;

        dst += dstPitchBytes;
    }
}
```

The compiled tail uses dword and byte string stores instead of the simple scalar loop shown above. The loop is equivalent source-level C++ and avoids encoding compiler string-instruction artifacts as source requirements.

## Evidence

- 2026-06-05 live IDA MCP `lookup_funcs` reports `0x00460d60` is not an IDA function, while the previous modeled helper is `sub_460C90` at `0x00460c90-0x00460d51` and the next modeled helper is `sub_460DF0` at `0x00460df0-0x00460e79`.
- 2026-06-05 live IDA MCP `py_eval` decodes 64 instructions in the raw range `0x00460d60-0x00460dee`, with terminal `retn` at `0x00460ded`.
- IDA `XrefsTo(0x00460d60)` is empty, while `XrefsTo(0x00460fa0)` reports the raw internal call from `0x00460d8e`.
- Live disassembly at `0x00460d70-0x00460da6` computes `width / 8`, calls `sub_460FA0` when positive, then advances the destination pointer by `blocks * 8` and stores the remaining tail byte count.
- Live disassembly at `0x00460db4-0x00460dd2` expands the fill byte into `0x01010101 * value`, fills tail dwords with `rep stosd`, then fills remaining bytes with `rep stosb`.
- Live disassembly at `0x00460dda-0x00460de5` reloads destination pitch/fill byte, advances the row pointer by pitch, decrements row count, and loops until all rows are filled.
- Byte audit confirms `0x00460d51-0x00460d60` is fifteen `0xcc` bytes and `0x00460dee-0x00460df0` is two `0xcc` bytes.
- IDA MCP `py_eval` disassembly on 2026-05-27 and 2026-05-30 shows a raw body at `0x00460d60-0x00460dee`, block dispatch `call sub_460FA0` at `0x00460d8e`, byte replication through `imul eax, 0x01010101`, `rep stosd` / `rep stosb` scalar-tail stores, and `retn` at `0x00460ded`.
- IDA `XrefsTo(0x00460fa0)` reports the internal call from this helper.
- 2026-05-30 IDA MCP: `ida_funcs.get_func(0x00460d60)` is `None`, and no direct xrefs target the raw start.
- 2026-06-03 restarted IDA MCP recheck confirms `ida_funcs.get_func(0x00460d60)` is still `None`, there are no direct xrefs to the raw start, the previous modeled helper is `sub_460C90` at `0x00460c90-0x00460d51`, `0x00460d51-0x00460d60` is fifteen bytes of `0xcc` padding, `0x00460ded` is this helper's terminal `retn`, `0x00460dee-0x00460df0` is two bytes of `0xcc` padding, and `0x00460df0` starts the next modeled helper `sub_460DF0`.
- The same live pass confirms the push order for `0x00460d8e -> sub_460FA0` as `dst`, `dstPitchBytes`, byte fill value, eight-byte block count, and row count; after the call the scalar tail advances `dst` by the completed block bytes and fills the remaining row bytes with `rep stosd`/`rep stosb`.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 82 | Live IDA now records exact raw bounds, instruction count, no function object, no raw-start xrefs, block-helper call/xref, helper argument order, block/tail split, byte expansion, `rep stosd`/`rep stosb` scalar tail, row-pitch stepping, and boundary padding. Remaining gaps are final original helper name, final source-facing reachability, argument names/types, and final loop/string-store source spelling. |
| Confidence | 88 | Disassembly, raw xref evidence, byte audit, surrounding modeled helpers, and the updated ByteSpanFill8Blocks page agree on boundary and behavior. Confidence stays below the modeled sibling helpers because IDA has no function object or direct caller/xref to the raw start. |
| Reconstructable | true | This is source-authored NexusTK render/support code attached to the SoftwareBlend16 source family; final reconstruction metadata remains blank under the 95/95 gate. |

## Cross-References

- [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) SoftwareBlend16.
- [UID:00020A][0x00460c90-0x00460d51.Rgb565AlphaMapMmxBlocks](by-memory/0x00460c90-0x00460d51.Rgb565AlphaMapMmxBlocks.md) preceding RGB565 alpha-map block helper.
- [UID:00020C][0x00460df0-0x00460e79.ByteSpanCopyRows](by-memory/0x00460df0-0x00460e79.ByteSpanCopyRows.md) adjacent byte-span row copy helper.
- [UID:00020F][0x00460fa0-0x00460ffe.ByteSpanFill8Blocks](by-memory/0x00460fa0-0x00460ffe.ByteSpanFill8Blocks.md) eight-byte block helper.

## Changes

- Existing documentation scored this raw helper at `40/65` and summarized only the block call and scalar tail. It is now scored `70/80` with inferred signature, row-loop semantics, equivalent source-level C++ candidate, and IDA evidence for byte expansion/string stores. Evidence: IDA MCP byte/function/xref review on 2026-05-30.
- 2026-06-03: Raised metadata from `70/80` to `78/86`, marked reconstructable, and attached to [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md). Evidence is restarted IDA MCP confirming the raw prologue/retn boundary, surrounding padding and neighbor functions, no raw-start xrefs, the `sub_460FA0` block-helper call and argument order, byte-value expansion, scalar string-store tail, and SoftwareBlend16 ownership. The reconstruction header remains blank because final source-facing helper names and raw reachability are not yet source-final.
- 2026-06-05: Raised from `78/86` to `82/88`.
  - Added live IDA MCP evidence for exact raw `0x00460d60-0x00460dee` bounds, 64 decoded instructions, no IDA function object, no raw-start xrefs, block-helper xref at `0x00460d8e`, caller argument order, block/tail split, byte expansion, `rep stosd`/`rep stosb` tail fill, row-pitch stepping, and boundary padding.
  - Kept [UID:0000NT][SoftwareBlend16](by-file/SoftwareBlend16.md) parent, `RECONSTRUCTABLE:TRUE`, and blank final C++ metadata unchanged.
