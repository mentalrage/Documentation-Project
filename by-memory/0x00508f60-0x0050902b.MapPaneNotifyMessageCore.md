*** UID:0003ZT | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool MapPane::HandleSystemOrControlEvent(Event *event)
{
    if (g_useEpfAssets && m_queuedMapStateRefresh)
    {
        m_queuedMapStateRefresh = false;
        TimerHandler::CancelTimer(6);
        TimerHandler::CancelTimer(8);
        ReleaseMouseCapture();
    }

    if (event->m_type != kEventWorkNotification)
        return false;

    if (g_pUrlAlertPane != NULL)
        return false;

    new ConnectionClosedDialog;
    return true;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact live `MapPane::HandleSystemOrControlEvent(Event *)` secondary-EventHandler virtual at `[0x00508f60,0x0050902b)` and vtable slot `0x0061e77c`; normalizes the `MapPane+0xa0` receiver, drains canonical `bool m_queuedMapStateRefresh` before the discriminator under `g_useEpfAssets`, cancels inherited TimerHandler ids 6 then 8, calls proven `Pane::ReleaseMouseCapture`, handles `Event::m_type == kEventWorkNotification`, rejects an existing `g_pUrlAlertPane`, performs ordinary `new ConnectionClosedDialog`, and returns true only after the attempted construction path. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00508f60-0x0050902b MapPane Notify Message Core

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact `MapPane` virtual method body split from [UID:00037U][0x00508f60-0x00509470.MapPaneNotifyTimerCore](by-memory/0x00508f60-0x00509470.MapPaneNotifyTimerCore.md).
- Direct owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md).
- Source module route: [UID:00007Q][MapPane](by-class/MapPane.md) through [UID:0000L3][MapPane](by-file/MapPane.md), following the `map/MapPane.cpp` convention.
- Best source-facing method name: `MapPane::HandleSystemOrControlEvent(Event *event)`, matching the inherited EventHandler system/control slot family and trusted Event contract.
- Confidence: very strong for exact range/hash, MapPane ownership, vtable reachability, Event ABI, receiver adjustment, field/timer/helper semantics, global guard, allocation size, direct constructor call, source order, and dependency-closed first-draft C++; exact original lexical spelling remains inferred.

## Range And Boundary Evidence

| Range | Classification | Evidence |
| --- | --- | --- |
| `0x00508f60-0x0050902b` | `MapPane` secondary-facet notify/message virtual body | IDA export names `sub_508F60`, size `0xcb`, and records a data xref from the `MapPane` secondary vtable slot at `0x0061e77c`. |
| `0x0050902b-0x00509030` | internal alignment | Five bytes before the next modeled method start. This span is not source C++ and should be recorded as ignored/alignment in supervisor-owned coverage. |
| `0x00509030` | next child start | [UID:0003ZS][0x00509030-0x00509439.MapPaneMovementTimerHandler](by-memory/0x00509030-0x00509439.MapPaneMovementTimerHandler.md) starts here as the separate timer-handler vtable method. |

This child is half-open and ends at `0x0050902b`. The successor `0x00509030` body is not part of this method. The later `0x00509470` getter is also not part of this child or of the split parent; it is successor `MapPane::GetMapId` / `GetCurrentMapId` evidence for the parent end boundary.

## Vtable Reachability And Adjusted `this`

The method has no ordinary direct code callers in the current export. That is expected: it is reached from the `MapPane` secondary vtable slice. The `.rdata` slot at `0x0061e77c` points to `0x00508f60`, and [UID:0002SQ][0x0061e71c-0x0061e7a0.MapPaneVtableData](by-memory/0x0061e71c-0x0061e7a0.MapPaneVtableData.md) records this slot in the `??_7MapPane@@6B@_0` secondary table.

The raw decompiler treats `this` as a byte pointer. The relevant state normalizes back to the `MapPane` base:

| Raw expression / address | Normalized role | Evidence |
| --- | --- | --- |
| `this[765]` | `MapPane + 0x39d`, queued movement/map-state timer flag | Tested and cleared here; required by timer ids `6` and `8` in the sibling timer handler; clearer `0x00505cc0` invalidates the same timer events. |
| `this + 4` | timer-handler-compatible facet at approximately `MapPane + 0xa4` | Passed to [UID:0001K9][0x00597610-0x00597645.TimerHandlerWrappers](by-memory/0x00597610-0x00597645.TimerHandlerWrappers.md) for event-specific cancellation. |
| secondary vtable slot `0x0061e77c` | virtual notify/message callback route | Explains lack of direct code callers and proves this is live callback code, not dead address adjacency. |

## Behavior

The method performs two source-visible tasks.

First, it handles pending MapPane movement state during the global state-byte mode. If `byte_66DA97 == 1` and the queued movement/map-state flag at normalized `MapPane +0x39d` is set, it clears that flag, cancels timer event `6`, cancels timer event `8`, and calls unresolved helper `0x005449f0`. The best descriptive name for the flag is `m_queuedMapStateRefresh` or `m_movementTimerQueued`. The direct cancel helper is the generic `TimerHandler::CancelTimer` wrapper at [UID:0001K9][0x00597610-0x00597645.TimerHandlerWrappers](by-memory/0x00597610-0x00597645.TimerHandlerWrappers.md), not a MapPane-owned TimerMgr implementation. The accepted 0001AP split adds the explicit clearer page [UID:00042F][0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh](by-memory/0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh.md) for the same `+0x39d` clear and timer-id `6/8` invalidation role.

Second, it handles message code `22`. The raw method checks `Block[4] == 22`, reads `dword_69B4DC` / [UID:0000SM][g_pUrlAlertPane](by-global/g_pUrlAlertPane.md), and only allocates a new alert/dialog object when the guard is null. It allocates `0x270` / 624 bytes through `0x004f4aa0` and calls [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) at `0x00508ffc`. It returns handled (`1`) only for that successful message-code path; mismatched message codes and already-open guard state return unhandled (`0`).

The read of `g_pUrlAlertPane` is an alert/open guard. It is not proof that the constructed object is `UrlAlertPane`: direct constructor evidence points to the `ConnectionClosedDialog` constructor variant. Older wording that described the allocation as a `UrlAlertPane-shaped object` is stale and should be replaced by "uses `g_pUrlAlertPane` as an active-alert guard; constructor evidence points to `ConnectionClosedDialogConstructorVariant`."

## Callee And Dependency Roles

| Callee / global | Source-facing role | Ownership decision |
| --- | --- | --- |
| `g_useEpfAssets` at `0x0066da97` | process-wide `bool` EPF/current-layout selector | Definition remains with StartupWindow; MapPane consumes an explicit source-root `extern bool g_useEpfAssets;`. |
| `0x00597610` | inherited `TimerHandler::CancelTimer` | Generic TimerHandler wrapper. The adjusted call receiver is the inherited facet at complete `MapPane+0xa4`, not an `m_timerHandler` member. |
| `0x005449f0` | `Pane::ReleaseMouseCapture()` | Exact 14-byte paired helper passes zero to the shared capture manager; 20 caller sites and paired `0x005449d0` `Pane::CaptureMouse()` reject the former refresh-helper interpretation. |
| [UID:0000SM][g_pUrlAlertPane](by-global/g_pUrlAlertPane.md) | active alert/open guard | AlertPanes-owned global. This method reads it but does not own it. |
| `0x004f4aa0` | allocation helper for a 624-byte dialog object | Allocator dependency, not MapPane-owned logic. |
| [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md) | connection-closed dialog constructor variant | Constructed object owner remains [UID:000036][ConnectionClosedDialog](by-class/ConnectionClosedDialog.md). |

## Rejected Alternatives And Historical Corrections

- Reject keeping `sub_508F60`, `OnNotifyMessage`, or `HandleNotifyMessage` as the current source-facing name. The secondary-vtable slot order, adjusted receiver, trusted `Event *`, `Event::m_type`, and accepted EventHandler family support `MapPane::HandleSystemOrControlEvent`.
- Reject treating the absence of direct callers as dead-code evidence. The data xref at `0x0061e77c` is the live virtual callback route.
- Reject describing the constructed object as definitely `UrlAlertPane`. The global guard is `g_pUrlAlertPane`, but the direct constructor call is the `ConnectionClosedDialog` constructor variant at `0x00553c10`.
- Reject merging this method with the timer handler at `0x00509030`. The two bodies are different vtable slots with different adjusted-this interpretations and separate function ranges.
- Reject folding successor `0x00509470` into this range. The successor is an independent map-id getter and supports the split parent boundary.

## Open Questions And Resolutions

- Ownership/liveness: closed. The sole data xref `0x0061e77c -> 0x00508f60` is the live secondary EventHandler vtable route; no ordinary direct caller is expected.
- Method/parameter/event: closed to the strongest source answer, `bool MapPane::HandleSystemOrControlEvent(Event *event)` with `event->m_type == kEventWorkNotification` (`22`).
- Receiver/layout: closed. The raw secondary receiver is complete `MapPane+0xa0`; `this[765]` normalizes to canonical `bool m_queuedMapStateRefresh` at complete offset `+0x39d`, and the timer receiver normalizes to the inherited TimerHandler facet at `+0xa4`.
- Helper: closed. `0x005449f0` is `Pane::ReleaseMouseCapture`; paired `0x005449d0` is `Pane::CaptureMouse`; shared manager `0x004a6f40` stores a nonzero handler and clears handler/state for zero.
- Dialog path: closed. `g_pUrlAlertPane` is only the existing-alert guard; allocation is exactly `0x270` bytes and the direct callee is `ConnectionClosedDialog::ConnectionClosedDialog()`.
- Remaining uncertainty is lexical only: stripped symbols do not prove the developers' exact spelling of the virtual or field. The project-consistent names above are the committed source-facing result and do not leave IDA labels or placeholders in emitted C++.

## First-Draft C++ Notes

The formal block is behavior-exact C++03-shaped source under the documented MapPane dependency package: complete `Event`/`EventType` from self-contained `Event.h -> LObject.h`, inherited TimerHandler and Pane facets, `AlertPanes.h` for `g_pUrlAlertPane`, `ReconnectDialog.h` for complete `ConnectionClosedDialog`, and an explicit source-root declaration for `g_useEpfAssets`. It intentionally preserves the unconditional queued-state drain before the Event discriminator, the 6-then-8 cancellation order, release-capture side effect, trusted non-null Event contract, and true return after the attempted ordinary `new` path.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | Exact range/hash/CFG, vtable slot, adjusted receiver, Event contract, timer-state drain, helper family, global/dialog behavior, source order, historical corrections, and dependency-closed first-draft C++ are documented. |
| Confidence | 94 | Direct binary and current support evidence close behavior, ABI, owner/emitter, helper, field, and declaration routes. Confidence remains below 95 only because original method/field/header lexical spellings are stripped and reconstructed by project-consistent inference. |

## UID0003ZT Source-Quality Closure

- Exact target identity: `[0x00508f60,0x0050902b)`, 203 bytes, SHA-256 `ACFE0DF9EE39DD2E68B2131E33F30AC7D8F086987D7A6FB4D089A740D8F4C80`; IDA models one 13-block thiscall function with complexity 4.
- Exact following boundary: `[0x0050902b,0x00509030)` is five `0xcc` alignment bytes, SHA-256 `992CF51486F901AA1AA7D1F63E18C150C9156166E08E0B9E59CC3FAF98177329`; sibling UID0003ZS begins at `0x00509030` and must remain separate.
- Control-flow order: when `g_useEpfAssets` and the queued flag are true, clear the flag, cancel timers 6 and 8, and release mouse capture before testing the incoming Event type. A non-work Event or non-null alert guard returns false. The null-guard work-event path allocates 624 bytes, invokes the connection-closed constructor, and reaches true even across the compiler-lowered allocator-null branch.
- Source ownership remains [UID:00007Q][MapPane](by-class/MapPane.md) `MapPane`, emitter [UID:00007Q][MapPane](by-class/MapPane.md), source root [UID:0000L3][MapPane](by-file/MapPane.md) `NexusTK/map/MapPane.cpp`, after UID00037T and before UID0003ZS. The mixed UID00037U parent remains a non-emitting split index.
- Superseded assumptions retained as history: `OnNotifyMessage`, raw `const unsigned char *message`, magic `22`, `m_movementTimerQueued`, an `m_timerHandler` member, `RefreshQueuedMapState`, and UrlAlertPane construction. None is current source vocabulary.

## Cross-References

- [UID:00037U][0x00508f60-0x00509470.MapPaneNotifyTimerCore](by-memory/0x00508f60-0x00509470.MapPaneNotifyTimerCore.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:0002SQ][0x0061e71c-0x0061e7a0.MapPaneVtableData](by-memory/0x0061e71c-0x0061e7a0.MapPaneVtableData.md)
- [UID:0001K9][0x00597610-0x00597645.TimerHandlerWrappers](by-memory/0x00597610-0x00597645.TimerHandlerWrappers.md)
- [UID:00042F][0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh](by-memory/0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh.md)
- [UID:0000SM][g_pUrlAlertPane](by-global/g_pUrlAlertPane.md)
- [UID:0002WC][0x0069b4dc-0x0069b4e0.g_pUrlAlertPane](by-memory/0x0069b4dc-0x0069b4e0.g_pUrlAlertPane.md)
- [UID:00023O][0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant](by-memory/0x00553c10-0x00553cbf.ConnectionClosedDialogConstructorVariant.md)

## Changes

- 2026-06-19 B013 00037U implementation: created exact child page for the `0x00508f60-0x0050902b` MapPane notify/message virtual method after source-quality reanalysis found that the old [UID:00037U][0x00508f60-0x00509470.MapPaneNotifyTimerCore](by-memory/0x00508f60-0x00509470.MapPaneNotifyTimerCore.md) page was a mixed two-method aggregate with internal and tail bytes.
- 2026-06-25 Agent-B009 support sync: linked [UID:00042F][0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh](by-memory/0x00505cc0-0x00505cf9.MapPaneClearQueuedMovementRefresh.md) as the exact accepted clearer for the same `+0x39d` / timer-id `6/8` state drained by this notify callback.
