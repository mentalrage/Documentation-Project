*** UID:000156 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FolderTreePane::SetSelection(TreeItor<TreeElem> *selection)
{
    const unsigned char kVerticalAxis = 0;

    if (selection != &m_selection)
        m_selection = *selection;

    RectBounds selectedBounds;
    GetNodeBounds(&m_selection, &selectedBounds);

    const int scrollTop = GetScrollPosition(kVerticalAxis);
    const int scrollBottom =
        scrollTop + (m_contentRect.bottom - m_contentRect.top);
    const int oldScrollTop = GetScrollPosition(kVerticalAxis);

    if (selectedBounds.top < scrollTop)
    {
        int newScrollTop = oldScrollTop + selectedBounds.top - scrollTop;
        if (newScrollTop < 0)
            newScrollTop = 0;

        ApplyScrollPosition(
            kVerticalAxis,
            static_cast<unsigned short>(oldScrollTop),
            static_cast<unsigned short>(newScrollTop));
    }
    else if (selectedBounds.bottom > scrollBottom)
    {
        int newScrollTop =
            oldScrollTop + selectedBounds.bottom - scrollBottom;
        const int maxScrollTop = GetScrollMax(kVerticalAxis);
        if (newScrollTop > maxScrollTop)
            newScrollTop = maxScrollTop;

        ApplyScrollPosition(
            kVerticalAxis,
            static_cast<unsigned short>(oldScrollTop),
            static_cast<unsigned short>(newScrollTop));
    }

    InvalidateRect(NULL);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `FolderTreePane` selection method; updates `m_selection`, computes node bounds, scrolls the selected row into view through inherited `ScrollablePane` helpers, and invalidates the pane. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3210-0x004b32c9 FolderTreePaneSetSelection

## Status

- Disposition: reconstructable NexusTK project code.
- Rebuild handling: source-authored `FolderTreePane` method.
- Entity kind: `FolderTreePane` method.
- Current generated owner label: `FolderTreePane`
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- Confidence: strong for range, behavior, caller/reachability, owner/emitter route, direct scroll helper roles, and first-draft C++ shape; medium-high for final inherited rectangle member spelling.

## Covered Range

| Range | Current or proposed name | Notes |
| --- | --- | --- |
| `0x004b3210-0x004b32c9` | `FolderTreePane::SetSelection` | Copies a tree iterator into the selected-iterator field, scrolls the selected row into view, and invalidates/updates the pane. |

## Behavior

IDA MCP decompilation on 2026-05-26 showed a real method body even though the older `source-3/simroot_v2/class_FolderTreePane.cpp` generated source had a scoped marker and no emitted function definition for this method. That historical generated-output issue is now superseded for this by-memory page: current source documentation contains formal C++, and current generated `auto-generated/NexusTK/ui/controls/FolderTreePane.cpp` contains a source-shaped `SetSelection` body, although some generated tracker metadata still lags the source page.

The method checks whether the incoming iterator differs from the selected iterator at `this + 0x170`. If it changed, it copies the iterator payload into the selected-iterator storage, calls the local node-bounds helper at `0x004b3010`, compares the selected row bounds against the current scroll position and visible inherited rectangle height, adjusts the vertical scroll offset through the `ScrollablePane` helpers at `0x0055e950`, `0x0055e910`, and `0x0055eae0`, and finally calls the pane invalidate virtual slot at vtable offset `0x20` with argument `0`.

IDA MCP recheck on 2026-06-01 confirms `lookup_funcs 0x004b3210` as a `0xb9` byte function ending at `0x004b32c9`, with `0x004b32c9` not a function start. The decompilation again shows the iterator copy into `this + 0x174` and `this + 0x178`, node-bounds call at `0x004b323b`, scroll reads through `0x0055e950`, maximum-scroll clamp through `0x0055e910`, scroll update through `0x0055eae0`, and final virtual call through `*this + 0x20`.

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis resolves this page as a normal source-authored `FolderTreePane` method ready for first-draft method C++. The best current source-facing signature is `void FolderTreePane::SetSelection(TreeItor<TreeElem> *selection)`. Pointer form matches the observed call sites (`&iter` and `&this->m_selection`) better than a reference signature, and the return type should be modeled as `void`: decompiler output may surface the final invalidate/update call result, but callers use the function for side effects.

The original B008 formal C++ used source-facing wrapper names:

- `m_selection` is the persistent selected iterator at `this+0x170`; `m_selectedIterator` remains a descriptive alias in prose.
- `GetNodeBounds` is the local helper at `0x004b3010`.
- `GetVerticalScrollPosition`, `GetMaxVerticalScrollPosition`, and `SetVerticalScrollPosition` are source-facing names for `0x0055e950`, `0x0055e910`, and `0x0055eae0`.
- `m_viewportTop` / `m_viewportBottom` represent inherited `ScrollablePane` state at `this+0x114` and `this+0x11c`; exact inherited field names are not yet symbol-proven.
- `Invalidate(false)` represents the final virtual slot `+0x20` call with argument `0`.

These B008 wrapper and field names are retained as a historical source-quality step, but they are superseded by the B011 implementation below. Current `ScrollablePane` support pages and accepted Chatting scroll helpers resolve the direct helper trio as `GetScrollPosition(axis)`, `GetScrollMax(axis)`, and `ApplyScrollPosition(axis, oldPosition, targetPosition)`. The inherited `this+0x114` and `this+0x11c` accesses are the `top` and `bottom` fields of the inherited `ScrollablePane` rectangle at `+0x110`; this implementation uses `m_contentRect.top` and `m_contentRect.bottom` as the current best-supported source-facing spelling. The final vtable slot `+0x20` call with argument `0` is modeled as `InvalidateRect(NULL)` through the inherited `Pane` API, not `Invalidate(false)`.

Rejected alternatives: this body is not a compiler thunk, not a missing-binary artifact, and not a generic iterator assignment helper. It is reached by `SelectNodeByPath`, `OnKeyEvent`, and `OnMouseEvent`, copies the selected iterator only when needed, scrolls the selected row into the visible viewport, clamps downward scrolling to the maximum vertical scroll, and updates the pane. The old raw/missing generated `sub_4B3210` symptom is now treated as historical generated-output quality context; the by-memory page contains source-shaped formal C++.

## 2026-06-26 B011 Source-Quality Implementation

B011 report `000156-FolderTreePaneSetSelection-source-quality.md` accepts and implements a stricter source-quality body for this exact method. MCP session `80de0a67` was live and returned active IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`; `lookup_funcs` confirmed `0x004b3210` (`0xb9`), `0x004b3010` (`0x74`), `0x0055e950` (`0x39`), `0x0055e910` (`0x39`), and `0x0055eae0` (`0x120`). `xrefs_to 0x004b3210` returned exactly three normal callers: `0x004b296a` in `SelectNodeByPath`, `0x004b2d70` in `OnKeyEvent`, and `0x004b2f2f` in `OnMouseEvent`.

The accepted formal C++ keeps the binary's observed two scroll reads and delta form. It reads the current vertical scroll through `GetScrollPosition(kVerticalAxis)`, computes the visible bottom from `m_contentRect.bottom - m_contentRect.top`, reads the old/current scroll again before applying movement, clamps upward movement to zero, clamps downward movement to `GetScrollMax(kVerticalAxis)`, applies movement through `ApplyScrollPosition(kVerticalAxis, oldScrollTop, newScrollTop)`, and ends with `InvalidateRect(NULL)`.

The implementation intentionally uses `m_contentRect` for the inherited `ScrollablePane +0x110` rectangle because current `ScrollablePaneCore` documents a rectangle at `+0x110` and `FolderTreePane` only consumes its `top`/`bottom` fields here. The exact historical member name remains a final-audit naming cap only. It does not block current C++ emission, owner/emitter routing, or the score increase because the target no longer depends on standalone `m_viewportTop` / `m_viewportBottom` pseudo-fields.

Rejected alternatives from the B011 pass:

- `RECONSTRUCTABLE:FALSE`: rejected because this is normal source-authored UI behavior with exact range, callers, and emitter route.
- Blank formal C++: rejected because the target is eligible, bounded, and already emits source-shaped code.
- `CANONICAL_OWNER:NONE` or `ScrollablePane` ownership: rejected because all callers and selected state are `FolderTreePane`; `ScrollablePane` only supplies inherited helpers and rectangle state.
- Leaving `sub_55E950`, `sub_55E910`, or `sub_55EAE0` as unresolved blockers: rejected because accepted `ScrollablePane` and `ChattingPane` docs resolve the helper trio enough for first-draft source.
- `Invalidate(false)`: rejected because vtable slot `+0x20` is the inherited `Pane::InvalidateRect` path and the binary passes a null rectangle argument.
- Manual edits to generated tracker/coverage files: rejected. The stale `78/86` generated research row is a validator refresh issue, not a source-page reason to lower or block the target.

## Caller Evidence

IDA MCP caller checks on 2026-05-26 report direct calls from:

- `0x004b296a` inside `FolderTreePane::SelectNodeByPath`.
- `0x004b2d70` inside `FolderTreePane::OnKeyEvent`.
- `0x004b2f2f` inside `FolderTreePane::OnMouseEvent`.

This is therefore normal tree-control selection behavior, not a dead helper or compiler thunk.

## Touched State

| State/helper | Use | Evidence |
| --- | --- | --- |
| `this + 0x170` | Selected iterator base. | Method compares incoming iterator address against `this + 368`. |
| `this + 0x174`, `this + 0x178` | Selected iterator payload copied from incoming iterator offsets `+4` and `+8`. | 2026-06-01 decompilation writes these fields before calculating bounds. |
| `this + 0x114`, `this + 0x11c` | `m_contentRect.top` / `m_contentRect.bottom`, inherited `ScrollablePane +0x110` rectangle fields used to compute visible height. | Decompilation computes `scrollY + *(this + 284) - *(this + 276)`; `ScrollablePaneCore` documents the owning rect at `+0x110`. |
| `0x004b3010` | Node-bounds helper. | Direct callee at `0x004b323b`. |
| `0x0055e950`, `0x0055e910`, `0x0055eae0` | `GetScrollPosition(0)`, `GetScrollMax(0)`, and `ApplyScrollPosition(0, old, target)`. | Direct callees used for current scroll, max scroll, and scroll update; accepted `ScrollablePane`/`ChattingPane` docs use the same helper names. |
| vtable slot `+0x20` | `InvalidateRect(NULL)` after selection. | Final indirect call with argument `0`; `Pane` support resolves slot `+0x20` as `InvalidateRect`. |

## 2026-06-15 Live IDA Refresh

- IDA MCP `lookup_funcs` confirms `sub_4B3210` at `0x004b3210`, size `0xb9` / 185 bytes (Verified with int_convert.py); `0x004b32c8` is still inside the function and `0x004b32c9` is not a function start, confirming the exclusive end before the documented padding.
- `xrefs_to 0x004b3210` reports the same three normal callers: `0x004b296a` inside `SelectNodeByPath`, `0x004b2d70` inside `OnKeyEvent`, and `0x004b2f2f` inside `OnMouseEvent`.
- IDA decompilation confirms the selected-iterator comparison/copy: when the incoming iterator is not `this + 0x170`, the method copies incoming payload dwords to `this + 0x174` and `this + 0x178`.
- The scroll-into-view flow is also reconfirmed: call [UID:000155][0x004b1b90-0x004b32c9.FolderTreePaneCore](by-memory/0x004b1b90-0x004b32c9.FolderTreePaneCore.md) helper `0x004b3010`, read current scroll through `sub_55E950(0)`, compute visible bottom from `this + 0x11c` and `this + 0x114`, clamp downward scrolling through `sub_55E910(0)`, update scroll through `sub_55EAE0`, then call vtable slot `+0x20` with argument `0`.
- `callees 0x004b3210` confirms exactly `0x004b3010`, `0x0055e950`, `0x0055e910`, and `0x0055eae0`.

## 2026-06-26 Live MCP / Generated-State Refresh

- B011 MCP session `80de0a67` confirmed the server was active and not analyzing before implementation.
- `lookup_funcs` again returned `sub_4B3210` size `0xb9`, `sub_4B3010` size `0x74`, `sub_55E950` size `0x39`, `sub_55E910` size `0x39`, and `sub_55EAE0` size `0x120`.
- `xrefs_to 0x004b3210` again returned only `0x004b296a`, `0x004b2d70`, and `0x004b2f2f`, proving normal caller/reachability and rejecting dead-code or orphan-helper interpretations.
- `xrefs_to` for the helper trio showed this method's direct helper calls at `0x004b3244`, `0x004b3261`, `0x004b3296`, `0x004b32a4`, and `0x004b32b2`, consistent with the formal C++ body.
- Current generated state was inconsistent before this implementation: `auto-generated/-ag-research-tracker.md` still listed this UID as `78/86`, but `auto-generated/-ag-memory-coverage.md` classified it as coded and current generated `FolderTreePane.cpp` contained a source-shaped method. That stale generated metadata is validator-owned state and should be refreshed by scoped validation/autogen, not edited by hand.

## Confidence And Open Questions

- Confirmed: exact function boundary, three normal callers, scroll-into-view behavior, copied iterator fields, parent source file, source-facing method name, pointer-form signature, direct scroll helper roles, inherited invalidate call, and first-draft source body.
- Remaining caveat: exact original name of the inherited `ScrollablePane +0x110` rectangle is still inferred. This implementation uses `m_contentRect.top` / `m_contentRect.bottom` as the current best-supported spelling; a future final-audit `ScrollablePane` naming pass may rename the rectangle consistently, but this is not a blocker.

## Score Rationale

Completion is `90` and confidence is `91`. The target has an exact `0xb9`-byte range, normal `FolderTreePane` callers, correct owner/emitter route through [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), formal C++ inside the target's own range, direct `ScrollablePane` helper names supported by current docs, and the final inherited invalidate call modeled as `InvalidateRect(NULL)`. It is not higher because the exact historical spelling of the inherited `+0x110` rectangle and the project-wide axis constant style remain final-audit naming questions, and generated tracker metadata still needs validator refresh. These caveats cap final-audit confidence but no longer block the score increase or emitted C++.

## Data Issue

Historical generated output omitted or raw-emitted the method body after a scoped install marker. Current generated output contains source-shaped code for `FolderTreePane::SetSelection`, but generated tracker/comment metadata may still carry stale `78/86` values until validator refresh catches up. Keep [UID:0000VD][ScopedMarkerMissingMethodBodies](by-item/ScopedMarkerMissingMethodBodies.md) as a generated-output quality tracker only; it is no longer evidence that this by-memory page lacks formal C++.

## Cross-References

- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:000155][0x004b1b90-0x004b32c9.FolderTreePaneCore](by-memory/0x004b1b90-0x004b32c9.FolderTreePaneCore.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:0000UO][FolderTreePaneOwnerPollution_47f280_58a8e0](by-item/FolderTreePaneOwnerPollution_47f280_58a8e0.md)
- [UID:0000VD][ScopedMarkerMissingMethodBodies](by-item/ScopedMarkerMissingMethodBodies.md)

## Changes

- 2026-06-26 B011 implementation callback:
  - Before: `COMPLETION:86`, `CONFIDENCE:88`; owner/emitter [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md); formal C++ used local vertical-scroll wrapper names and `Invalidate(false)`.
  - Changed to: `COMPLETION:90`, `CONFIDENCE:91`; owner/emitter unchanged; formal C++ now uses `GetScrollPosition`, `GetScrollMax`, `ApplyScrollPosition`, `m_contentRect.top` / `m_contentRect.bottom`, and `InvalidateRect(NULL)`.
  - Summary/evidence: B011 MCP session `80de0a67` confirmed the exact function and helper sizes, three normal callers, direct helper call sites, inherited rectangle offsets, and final vtable slot. Accepted support docs resolve the helper trio through `ScrollablePane`/`ChattingPane` evidence and resolve slot `+0x20` through `Pane::InvalidateRect`.
- 2026-06-15 A001-goal2-exchange-foldertree-memory-20260615-1:
  - Before: `COMPLETION:78`, `CONFIDENCE:86`.
  - Changed at that time to: `COMPLETION:86`, `CONFIDENCE:88`; owner/emitter remained [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md), and final C++ still remained blank before the later B011 implementation.
  - Summary/evidence: live IDA MCP reconfirmed the `0xb9`-byte function, three normal callers, iterator-copy fields, `GetNodeBounds` call, scroll read/max/update helper calls, and final vtable update call.
- 2026-06-01 completion/confidence and autogen metadata:
  - What existed before: `COMPLETION:50`, `CONFIDENCE:65`, blank `RECONSTRUCTABLE`, and blank `AUTOGEN_PARENT_UID`.
  - Changed to: `COMPLETION:78`, `CONFIDENCE:86`, `RECONSTRUCTABLE:TRUE`, and parent [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
  - Summary/evidence: IDA MCP recheck confirmed the exact function boundary, three callers, scroll helper callees, copied iterator fields, and final invalidate/update call; at that stage final C++ remained blank because member/helper names were not yet final-source quality.
