*** UID:00018N | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000K4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000K4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include <setjmp.h>
#include <stdio.h>
#include <stdlib.h>
#include "jpeglib.h"

struct JpegScreenshotErrorManager
{
    jpeg_error_mgr pub;
    jmp_buf setjmpBuffer;
};

struct JpegMemorySourceManager
{
    jpeg_source_mgr pub;
    const JOCTET *sourceBytes;
    size_t sourceByteCount;
    boolean sourceLoaded;
};

static void JpegMemorySourceInit(j_decompress_ptr cinfo);
static boolean JpegMemorySourceFill(j_decompress_ptr cinfo);
static void JpegMemorySourceSkip(j_decompress_ptr cinfo, long byteCount);
static void JpegMemorySourceTerm(j_decompress_ptr cinfo);
static void JpegScreenshotErrorExit(j_common_ptr cinfo);

int WriteJpegFileFromRgb565Surface(
    const unsigned short *pixels,
    int width,
    unsigned int height,
    bool usesRgb565,
    FILE *output)
{
    jpeg_compress_struct cinfo;
    JpegScreenshotErrorManager errorManager;
    JSAMPLE *rgbBuffer = 0;

    cinfo.err = jpeg_std_error(&errorManager.pub);
    errorManager.pub.error_exit = JpegScreenshotErrorExit;
    errorManager.pub.output_message = 0;

    if (setjmp(errorManager.setjmpBuffer) != 0) {
        jpeg_destroy_compress(&cinfo);
        if (rgbBuffer != 0) {
            free(rgbBuffer);
        }
        return -1;
    }

    jpeg_create_compress(&cinfo);
    jpeg_stdio_dest(&cinfo, output);
    cinfo.image_width = width;
    cinfo.image_height = height;
    cinfo.input_components = 3;
    cinfo.in_color_space = JCS_RGB;
    jpeg_set_defaults(&cinfo);
    jpeg_start_compress(&cinfo, TRUE);

    const int pixelCount = width * static_cast<int>(height);
    rgbBuffer = static_cast<JSAMPLE *>(malloc(3 * pixelCount));

    if (usesRgb565) {
        for (int i = 0; i < pixelCount; ++i) {
            const unsigned int pixel = pixels[i];
            JSAMPLE *out = rgbBuffer + (3 * i);
            out[0] = static_cast<JSAMPLE>((pixel >> 8) & 0xf8);
            out[1] = static_cast<JSAMPLE>((pixel >> 3) & 0xfc);
            out[2] = static_cast<JSAMPLE>(8 * pixel);
        }
    } else {
        for (int i = 0; i < pixelCount; ++i) {
            const unsigned int pixel = pixels[i];
            JSAMPLE *out = rgbBuffer + (3 * i);
            out[0] = static_cast<JSAMPLE>((pixel >> 7) & 0xf8);
            out[1] = static_cast<JSAMPLE>((pixel >> 2) & 0xf8);
            out[2] = static_cast<JSAMPLE>(8 * pixel);
        }
    }

    const int rowStride = 3 * width;
    while (cinfo.next_scanline < cinfo.image_height) {
        JSAMPROW row = rgbBuffer + (rowStride * cinfo.next_scanline);
        jpeg_write_scanlines(&cinfo, &row, 1);
    }

    free(rgbBuffer);
    rgbBuffer = 0;
    jpeg_finish_compress(&cinfo);
    jpeg_destroy_compress(&cinfo);
    return 0;
}

static void JpegMemorySourceInit(j_decompress_ptr)
{
}

static boolean JpegMemorySourceFill(j_decompress_ptr cinfo)
{
    JpegMemorySourceManager *source =
        reinterpret_cast<JpegMemorySourceManager *>(cinfo->src);

    if (source->sourceLoaded == FALSE) {
        source->sourceLoaded = TRUE;
        source->pub.next_input_byte = source->sourceBytes;
        source->pub.bytes_in_buffer = source->sourceByteCount;
    } else {
        source->pub.next_input_byte = s_jpegEoiMarker;
        source->pub.bytes_in_buffer = 2;
    }

    return TRUE;
}

static void JpegMemorySourceSkip(j_decompress_ptr cinfo, long byteCount)
{
    if (byteCount > 0) {
        cinfo->src->next_input_byte += byteCount;
        cinfo->src->bytes_in_buffer -= byteCount;
    }
}

static void JpegMemorySourceTerm(j_decompress_ptr)
{
}

static void JpegScreenshotErrorExit(j_common_ptr cinfo)
{
    JpegScreenshotErrorManager *errorManager =
        reinterpret_cast<JpegScreenshotErrorManager *>(cinfo->err);

    if (errorManager->pub.output_message != 0) {
        errorManager->pub.output_message(cinfo);
    }

    longjmp(errorManager->setjmpBuffer, 1);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: ImageWriters-owned libjpeg compression wrapper plus retained memory-source callback family at 0x004efbc0-0x004efe94 (0x2d4/724 bytes); exact main/raw/null/error boundaries, screenshot-path call and argument order, RGB565/RGB555 conversion, scanline/error cleanup, byte-identical active source-manager duplicate, FF D9 fallback, strict padding, full source-quality C++, and resolved raw-path/source-placement evidence support 92/94. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004efbc0-0x004efe94 - JPEG Screenshot Writer

## Status

- Confidence: strong for IJG-backed JPEG writer behavior, exact terminal boundary, local error callback ownership, retained memory-source callback behavior, caller xref, callee set, active duplicate proof, marker ownership, and internal padding; inferred only for exact original helper and file spelling.
- Canonical owner/emitter: [UID:0000K4][ImageWriters](by-file/ImageWriters.md) with caller-side workflow ownership in [UID:0000ND][ScreenshotCapture](by-file/ScreenshotCapture.md).
- Related library owner: [UID:0000KN][LibJPEG](by-file/LibJPEG.md)
- Reconstruction status: source-owned wrapper is reconstructable and emits through [UID:0000K4][ImageWriters](by-file/ImageWriters.md). The target is `92/94`; exact writer behavior, retained source-manager helpers, error callback, source marker route, and formal C++ are incorporated.
- Resolved blockers: the raw helper contract is `jpeg_source_mgr`, not `jpeg_destination_mgr`; active ImageLoaders duplicates prove the callback slots and field meanings; raw screenshot-path body `0x00557840-0x00557a96` is a complete retained/legacy source body with the direct writer call at `0x005579d3`; `ImageWriters.cpp` is the best source placement. Only original source spelling remains inferred and is not represented by compiler-generated labels.
- Evidence basis: read-only IDA MCP decompile/callee/xref/byte checks on 2026-05-25, 2026-05-26, 2026-05-28, 2026-06-01, 2026-06-07, 2026-06-11, and B011 MCP session `supervisor_recovery_20260705` on 2026-07-06.

## Functions

| Range | IDA status | Proposed name | Role |
| --- | --- | --- | --- |
| `0x004efbc0-0x004efde2` | `sub_4EFBC0`, size `0x222` | `WriteJpegFileFromRgb565Surface` | Converts a 16-bit source surface to temporary 24-bit RGB rows and writes scanlines through IJG libjpeg compression helpers to a caller-supplied `FILE*`. |
| `0x004efdf0-0x004efdf1` | `nullsub_36` | `JpegMemorySourceInit` | One-byte no-op `init_source` callback in the retained ImageWriters source-manager family. |
| `0x004efe00-0x004efe35` | raw unmodeled function body | `JpegMemorySourceFill` | Publishes the saved input pointer/count once, then the [UID:0002ZL][0x0066db40-0x0066db42.JpegScreenshotEoiMarker](by-memory/0x0066db40-0x0066db42.JpegScreenshotEoiMarker.md) pointer with count `2`; returns `TRUE`. |
| `0x004efe40-0x004efe57` | raw unmodeled function body | `JpegMemorySourceSkip` | For a positive count, advances `next_input_byte` and decreases `bytes_in_buffer`. |
| `0x004efe60-0x004efe61` | `nullsub_37` | `JpegMemorySourceTerm` | One-byte no-op `term_source` callback in the retained source-manager family. |
| `0x004efe70-0x004efe94` | `sub_4EFE70` | `JpegScreenshotErrorExit` | IJG error-exit callback: invokes the installed output-message callback if present, then `longjmp`s to the writer's setjmp buffer. |

The full page range is `0x004efbc0-0x004efe94`, `0x2d4` / 724 bytes (B011 verified with IDA MCP `int_convert`). The function receives source pixels, width, height, a byte controlling 555/565 channel extraction, and an output stream. It constructs an IJG compression object with version `62`, configures image dimensions and RGB input, allocates a `3 * width * height` temporary buffer, converts the source image to byte RGB, writes scanlines, frees the temporary buffer, and destroys the compression object.

## 2026-07-06 B011 MCP Evidence And Historical First-Draft Incorporation

- MCP provenance: `idb_list` returned active session `supervisor_recovery_20260705` for `NexusTK.exe.i64`; `server_health` returned status `ok`, module `NexusTK.exe`, imagebase `0x400000`, auto-analysis ready, Hex-Rays ready, and strings cache ready.
- Range and metrics: whole target `0x004efbc0-0x004efe94` is `0x2d4` / 724 bytes. `sub_4EFBC0` is `0x222` / 546 bytes, with 159 instructions and 16 basic blocks. `0x004efe00-0x004efe35` is a raw `0x35` / 53-byte body with 21 instructions. `0x004efe40-0x004efe57` is a raw `0x17` / 23-byte body with 11 instructions. `sub_4EFE70` is `0x24` / 36 bytes, with 15 instructions and 3 basic blocks.
- Stack/prototype facts: the main writer is typed by IDA as `int __cdecl(int, int, unsigned int, char, int)`, matching source pixels, width, height, pixel-format flag, and `FILE *`. The error callback is typed as `void __cdecl __noreturn(int *)`.
- Current xrefs: `xrefs_to 0x004efbc0` reports one code xref at raw `0x005579d3` and no modeled callers. The call site pushes `esi` as the output stream, `unk_69B3D5` as the mode byte, `word_66DA98` as height, `word_66DA94` as width, and `[edi+20h]` as the source-pixel pointer. `xrefs_to 0x004efe70` reports the writer-local data assignment at `0x004efc0d`; `xrefs_to 0x0066db40` reports the fallback marker data xref at `0x004efe22`.
- Negative xrefs: `xrefs_to 0x004efdf0`, `0x004efe00`, `0x004efe40`, and `0x004efe60` are empty. B011 correctly recorded retained/no-route status, but its destination/null source names were an interim historical interpretation superseded by the exact active source-manager duplicate.
- Current callee map: `callees 0x004efbc0` returns `0x00401000` (`jpeg_std_error`), `__setjmp3`, `0x00403920` (`jpeg_CreateCompress`), `0x00403cc0` (`jpeg_stdio_dest`), `0x00403f70` (`jpeg_set_defaults`), `0x004047a0` (`jpeg_start_compress`), allocator/free helpers, `0x00404820` (`jpeg_write_scanlines`), `0x00403a60` (`jpeg_finish_compress`), `0x004039f0` (`jpeg_destroy_compress`), and the security-cookie check. `callees 0x004efe70` returns only `_longjmp`.
- Decompile behavior: the main writer initializes the IJG error manager, installs `sub_4EFE70`, clears the error-manager output-message slot, wraps compression in `_setjmp3`, creates the compressor with version `62` and struct size `360`, installs `jpeg_stdio_dest`, sets width/height/components/color space, allocates `3 * width * height`, converts either RGB565 or RGB555 pixels to 24-bit RGB rows, writes scanlines, frees the RGB buffer, finishes compression, destroys the compressor, returns `0` on success, and returns `-1` through the setjmp error path after destroying/freeing as needed.
- Error manager source shape: the error callback calls the installed output-message callback at `(*err)->output_message` when present, then `longjmp`s through the setjmp buffer at offset `0x84`. The formal C++ uses `JpegScreenshotErrorManager` as `jpeg_error_mgr` plus `jmp_buf`.
- Historical raw-callback interpretation: B011 read `[arg0+0x18]` as `cinfo->dest` and the public pair as output pointer/free count. Current exact libjpeg layout and duplicate-family evidence disprove that reading: `jpeg_decompress_struct +0x18` is `src`; `0x004efe00` fills `next_input_byte/bytes_in_buffer`, and `0x004efe40` implements signed-positive `skip_input_data`.
- Pre-callback generated/tracker state recorded by the accepted B011 report: `auto-generated/NexusTK/render/ImageWriters.cpp` command `000000007492` still listed UID00018N as `Completion:86 | Confidence:89 | Empty Emitter Marker`; `auto-generated/-ag-coverage-report-by-memory.md` command `000000007494` listed `emits_code:false`; `auto-generated/-ag-research-tracker.md` command `000000007497` listed `86/89`, combined `87.5`, reconstructable `true`, reports `0`. Those generated files are validator-owned and should be refreshed by scoped validation after this target update.

## 2026-08-01 UID0002ZL Source-Manager Resolution

- Live read-only MCP session `b3b2bf88` reconfirmed the full writer/support island, exact marker operand, raw function boundaries, current item state, and all caller/pointer negatives used below.
- In libjpeg 6b's 32-bit `jpeg_decompress_struct`, offset `+0x18` is `src`; it is not the compressor's `dest` field. The public manager pair at offsets `+0/+4` is `next_input_byte/bytes_in_buffer`.
- `JpegMemorySourceManager` extends `jpeg_source_mgr` with saved source fields at `+0x1c/+0x20` and byte-like `boolean sourceLoaded` at `+0x24`.
- `JpegMemorySourceFill` publishes the saved pointer/count once, marks it loaded, and on later calls publishes `s_jpegEoiMarker` with count `2`. `JpegMemorySourceSkip` adjusts input pointer/count only when the signed `long` argument is positive.
- `0x004efdf0` and `0x004efe60` are the no-op `init_source` and `term_source` callbacks. Their relative order follows the active callback table and byte-identical family, rather than arbitrary null-stub naming.
- Exact byte comparison: ImageWriters `0x004efdf0-0x004efe61` and active ImageLoaders `0x004e4e60-0x004e4ed1` match for all 113 bytes except the embedded EOI marker address (`0x0066db40` versus `0x0066db3c`).
- Active ImageLoaders initializer `0x004e7030` installs init/fill/skip/term into `jpeg_source_mgr` slots `+8/+0x0c/+0x10/+0x18`, with `jpeg_resync_to_restart` at `+0x14`; callers at `0x004d0869` and `0x004d0b21` make that duplicate live.
- No inbound xrefs or VA/RVA/raw-offset pointer routes target the four local source-helper starts. This proves retained/dead status, not absence of source semantics. The complete bodies remain valid translation-unit reconstruction evidence.
- `0x00557840` has no modeled function, direct xref, or VA/RVA/raw-offset pointer route, but bounded bytes form a complete source body that directly calls this writer at `0x005579d3`. This resolves it as retained/legacy screenshot capture code rather than an unresolved reachability blocker.
- `ImageWriters.cpp` remains the best source placement: the main compression wrapper, retained source helpers, error callback, and [UID:0002ZG][JpegScreenshotEoiMarker](by-global/JpegScreenshotEoiMarker.md) are one contiguous product-side image-output cluster. No evidence supports a separate `JpegScreenshotWriter.cpp`.
- The marker definition is emitted earlier by [UID:0002ZG][JpegScreenshotEoiMarker](by-global/JpegScreenshotEoiMarker.md) at file position `0`; this page consumes `s_jpegEoiMarker` without inventing an external declaration, `const` storage, or `const_cast`.

## IDA Behavior Evidence

2026-06-01 IDA MCP recheck:

| Evidence point | IDA detail | Interpretation |
| --- | --- | --- |
| Main function identity | `lookup_funcs 0x004efbc0` returns `sub_4EFBC0`, size `0x222`, exclusive end `0x004efde2`. | The main writer body boundary is exact; `0x004efde2-0x004efdf0` is post-body alignment, not code. |
| Error handler install | Decompile stores `sub_4EFE70` into the JPEG error-handler table at `0x004efc0d`. | The `0x004efe70` callback is writer-local support for this compression wrapper. |
| Error recovery | Decompile calls `_setjmp3`; on error it destroys the compression object, frees the temporary RGB block if allocated, and returns `-1`. | The function is a normal IJG `setjmp`/`longjmp` wrapper, not stock IJG internals. |
| JPEG setup | Calls `sub_403920(v25, 62, 360)`, `sub_403CC0`, sets width/height, input components `3`, color space `2`, then calls `sub_403F70` and `sub_4047A0(v25, 1)`. | Uses libjpeg 6b compression APIs to write an RGB JPEG to the caller-supplied stream. |
| Pixel conversion, 565 path | For nonzero format flag, emits red as `HIBYTE(pixel) & 0xf8`, green as `(pixel >> 3) & 0xfc`, blue as `8 * pixel`. | Converts RGB565-like 16-bit pixels to 24-bit RGB rows. |
| Pixel conversion, 555 path | For zero format flag, emits red as `(pixel >> 7) & 0xf8`, green as `(pixel >> 2) & 0xf8`, blue as `8 * pixel`. | Handles the alternate 15-bit/555-style active surface format. |
| Scanline loop | Loops while current scanline `< height`, passes a one-row pointer to `sub_404820`. | Writes one JPEG scanline at a time from the temporary RGB buffer. |
| Success cleanup | Frees the RGB block, calls `sub_403A60`, then `sub_4039F0`, and returns `0`. | Normal completion path finishes compression and destroys the IJG object. |

IDA `callees 0x004efbc0` reports `_setjmp3`, heap allocation/free, security cookie check, and libjpeg-range helpers `0x00401000`, `0x00403920`, `0x004039f0`, `0x00403a60`, `0x00403cc0`, `0x00403f70`, `0x004047a0`, and `0x00404820`. These map to the documented [UID:0000VS][0x00403920-0x004048e2.LibJpegCompressApiFrontEnd](by-memory/0x00403920-0x004048e2.LibJpegCompressApiFrontEnd.md) and adjacent [UID:0000VP][0x00401000-0x00414283.LibJpeg6bCore](by-memory/0x00401000-0x00414283.LibJpeg6bCore.md) island.

## Local Callback Evidence

| Range | 2026-06-01 IDA evidence | Notes |
| --- | --- | --- |
| `0x004efdf0-0x004efdf1` | `lookup_funcs` returns `nullsub_36`, size `0x1`; bytes are `c3`; callers/xrefs empty. | Retained no-op `jpeg_source_mgr::init_source`; role proven by exact active duplicate/table order. |
| `0x004efe00-0x004efe35` | IDA still reports not-a-function, but bytes show a normal `push ebp; mov ebp, esp` prologue and `pop ebp; ret` epilogue. `xrefs_to 0x0066db40` reports data xref at `0x004efe22`. | Retained `fill_input_buffer`: publishes saved source once and then the EOI marker/count `2`. |
| `0x004efe40-0x004efe57` | IDA reports not-a-function, but bytes show a complete prologue/epilogue body. | Retained `skip_input_data`: advances `next_input_byte` and subtracts from `bytes_in_buffer` for a positive count. |
| `0x004efe60-0x004efe61` | `lookup_funcs` returns `nullsub_37`, size `0x1`; bytes are `c3`; callers/xrefs empty. | Retained no-op `jpeg_source_mgr::term_source`; role proven by exact active duplicate/table order. |
| `0x004efe70-0x004efe94` | `lookup_funcs` returns `sub_4EFE70`, size `0x24`; `xrefs_to` reports a data xref from `sub_4EFBC0` at `0x004efc0d`; callees report only `_longjmp`. | Error-exit callback calls the installed output-message callback at `(*err)->output_message` if present, then `longjmp`s through the writer's setjmp buffer at offset `0x84`. |

## Caller Evidence

- IDA `callers` reports no modeled function callers.
- IDA `xrefs_to 0x004efbc0` reports one code xref at `0x005579d3`, which lies inside the unmodeled [UID:0001G8][0x00557840-0x00557a96.ScreenshotJpegCapturePath](by-memory/0x00557840-0x00557a96.ScreenshotJpegCapturePath.md).
- The xref passes the active surface pixel pointer, screen width `word_66DA94`, screen height `word_66DA98`, the pixel-format flag `byte_69B3D5`, and the open output stream.
- 2026-05-26 IDA MCP recheck reconfirmed `sub_4EFBC0` size `0x222`, no modeled callers, and the same raw xref at `0x005579d3`.
- 2026-05-28 IDA MCP recheck shows `sub_4EFBC0` ends at `0x004efde2`; byte `0x004efde1` is the final `retn` byte.
- 2026-06-01 IDA MCP recheck reconfirmed no modeled callers for `0x004efbc0`, `0x004efdf0`, `0x004efe00`, `0x004efe40`, `0x004efe60`, or `0x004efe70`; the only inbound code xref remains raw address `0x005579d3`.
- `sub_4EFBC0` writes `sub_4EFE70` into its local IJG error-handler table at `0x004efc0d`, confirming `JpegScreenshotErrorExitLongjmp` is writer-local support code.
- IDA currently does not model functions at `0x004efe00` or `0x004efe40`, but raw disassembly shows complete prologue/epilogue-bounded callback bodies at those addresses.

## Internal Padding

- [UID:0000VN][-ignored](by-memory/-ignored.md) records the compiler/linker alignment spans at `0x004efde2-0x004efdf0`, `0x004efdf1-0x004efe00`, `0x004efe35-0x004efe40`, `0x004efe57-0x004efe60`, `0x004efe61-0x004efe70`, and `0x004efe94-0x004efea0`.
- 2026-06-01 IDA byte reads confirm those spans are all `0xcc`: 14 bytes after `sub_4EFBC0`, 15 bytes after `nullsub_36`, 11 bytes after the first raw callback, 9 bytes after the second raw callback, 15 bytes after `nullsub_37`, and 12 bytes before `KeySpeedMgr`.
- The following subsystem starts at [UID:00018O][0x004efea0-0x004f0008.KeySpeedMgr](by-memory/0x004efea0-0x004f0008.KeySpeedMgr.md); it is not part of the JPEG writer island.

## 2026-06-07 A005 IDA MCP Recheck

- Live IDA MCP `idb_meta` again identifies `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`.
- `lookup_funcs` reconfirms `sub_4EFBC0` size `0x222`, `nullsub_36` at `0x004efdf0`, `nullsub_37` at `0x004efe60`, `sub_4EFE70` size `0x24`, and next subsystem start `sub_4EFEA0` at `0x004efea0`; `0x004efde2`, `0x004efe00`, `0x004efe40`, and `0x004efe94` are not modeled function starts.
- `callers` reports no modeled callers for the main writer, null stubs, raw callbacks, or error callback. `xrefs_to 0x004efbc0` still reports the single raw code xref at `0x005579d3`; `xrefs_to 0x004efe70` reports the writer-local data assignment at `0x004efc0d`.
- `callees 0x004efbc0` reconfirms `_setjmp3`, heap allocation/free, security-cookie check, and IJG compression helpers `0x00401000`, `0x00403920`, `0x004039f0`, `0x00403a60`, `0x00403cc0`, `0x00403f70`, `0x004047a0`, and `0x00404820`. `callees 0x004efe70` reports only `_longjmp`.
- Live decompilation reports the main writer shape as `int __cdecl sub_4EFBC0(int pixels, int width, unsigned int height, char is565, int file)`. It installs `sub_4EFE70` as the IJG error exit, wraps compression in `_setjmp3`, creates a libjpeg v6b compressor with version `62`, configures width/height/components/color-space, allocates `3 * width * height` bytes, expands either RGB565 or RGB555 pixels into 24-bit rows, writes scanlines through `sub_404820`, frees the row buffer, finishes compression, and destroys the compressor.
- Historical A005 disassembly correctly recorded the field operations but mislabeled their manager direction. Current libjpeg layout proves `[arg0 + 0x18]` is `cinfo->src`; the `0x004efe00` body fills `next_input_byte/bytes_in_buffer`, falls back to [UID:0002ZL][0x0066db40-0x0066db42.JpegScreenshotEoiMarker](by-memory/0x0066db40-0x0066db42.JpegScreenshotEoiMarker.md) with count `2`, and owns the xref at `0x004efe22`.
- The `0x004efe40-0x004efe57` field operations are correspondingly `skip_input_data`: for a positive signed count they advance the input pointer and subtract from the buffered byte count.
- Live decompilation of `sub_4EFE70` confirms the error-exit callback calls the installed output-message slot at `(*err)->output_message` when present, then `longjmp`s through the setjmp buffer at offset `0x84`.
- Byte checks reconfirm `0x004efde2-0x004efdf0` and `0x004efe94-0x004efea0` as `0xcc` alignment; the other internal padding spans remain recorded in [UID:0000VN][-ignored](by-memory/-ignored.md).

## 2026-06-11 A010 Batch 157 Recheck

- Live IDA MCP `idb_meta` again identifies `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`.
- `lookup_funcs` reconfirms `sub_4EFBC0` at `0x004efbc0` size `0x222`, `nullsub_36` at `0x004efdf0`, `nullsub_37` at `0x004efe60`, `sub_4EFE70` at `0x004efe70` size `0x24`, and the next subsystem start `sub_4EFEA0` at `0x004efea0`. IDA still does not model `0x004efe00`, `0x004efe40`, `0x004efde2`, or `0x004efe94` as function starts.
- `xrefs_to 0x004efbc0` still reports the single raw screenshot-path call at `0x005579d3`; `xrefs_to 0x004efe70` reports the writer-local table assignment at `0x004efc0d`; `xrefs_to 0x0066db40` reports the fallback marker data reference at `0x004efe22`.
- `callers` remains empty for the main writer, retained source callbacks, and error callback because the writer call sits inside a raw, unmodeled screenshot body. Current bounded-body and pointer-route analysis resolves that body as retained/legacy source, so empty modeled callers no longer defers reconstruction.
- `callees 0x004efbc0` reconfirms `_setjmp3`, allocator/free helpers, security-cookie check, and IJG compression helpers `0x00401000`, `0x00403920`, `0x004039f0`, `0x00403a60`, `0x00403cc0`, `0x00403f70`, `0x004047a0`, and `0x00404820`; `callees 0x004efe70` reports only `_longjmp`.
- Live decompilation again shows the writer installing `sub_4EFE70` as IJG error exit, constructing a libjpeg v6b compressor with version `62`, setting width/height/components/color-space, allocating `3 * width * height`, converting RGB565 or RGB555 16-bit pixels to RGB bytes, writing scanlines through `0x00404820`, freeing the buffer, finishing compression, and destroying the compressor.
- Historical A010 disassembly correctly recorded the operations but used the then-current destination interpretation. Current source-manager resolution maps the same operations to saved input pointer/count, `sourceLoaded`, EOI fallback count `2`, and positive-count `skip_input_data`.
- Historical 2026-06-11 direct parent gate recheck: [UID:0000K4][ImageWriters](by-file/ImageWriters.md) was then `86/85`, documented this wrapper and marker as render/image-output support, and routed related [UID:0002ZG][JpegScreenshotEoiMarker](by-global/JpegScreenshotEoiMarker.md). That cleared the corrected `85/85` direct-parent requirement for this page before the later B011 `88/90` update.

## Library Boundary

This is not stock IJG source. It is NexusTK-owned output wrapper code that uses the statically linked [UID:0000VP][0x00401000-0x00414283.LibJpeg6bCore](by-memory/0x00401000-0x00414283.LibJpeg6bCore.md), specifically the public compression APIs mapped in [UID:0000VS][0x00403920-0x004048e2.LibJpegCompressApiFrontEnd](by-memory/0x00403920-0x004048e2.LibJpegCompressApiFrontEnd.md). This is analogous to [UID:0000K4][ImageWriters](by-file/ImageWriters.md) wrapping LodePNG for PNG output.

Keep IJG compression internals under [UID:0000KN][LibJPEG](by-file/LibJPEG.md). Keep the screenshot decision/path handling in [UID:0000ND][ScreenshotCapture](by-file/ScreenshotCapture.md). Rejected direct-owner alternatives remain: ScreenshotCapture as the direct source owner, LibJPEG as the product-wrapper owner, ImageLoaders, the separate marker-data owner [UID:0002ZG][JpegScreenshotEoiMarker](by-global/JpegScreenshotEoiMarker.md)/[UID:0002ZL][0x0066db40-0x0066db42.JpegScreenshotEoiMarker](by-memory/0x0066db40-0x0066db42.JpegScreenshotEoiMarker.md), and a standalone `JpegScreenshotWriter.cpp` without source-tree evidence.

## Assignment Gate

`CANONICAL_OWNER` and `EMITTER_UIDS` remain [UID:0000K4][ImageWriters](by-file/ImageWriters.md). This page is `92/94`; source behavior, retained-helper meaning, marker definition order, formal CPP, and file placement are now resolved beyond the `85/85` gate. [UID:0000ND][ScreenshotCapture](by-file/ScreenshotCapture.md) remains the caller/workflow owner, not the direct owner of this image-writer wrapper. [UID:0000KN][LibJPEG](by-file/LibJPEG.md) remains the library owner for the public IJG APIs used by the product wrapper.

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `92` | Exact main/support ranges, modeled and raw boundaries, caller and negative routes, IJG compressor setup, error recovery, heap cleanup, RGB565/RGB555 formulas, scanline loop, retained source-manager family, active duplicate/table proof, EOI fallback, local error exit, padding, source/library split, owner/emitter order, raw-path disposition, and exact formal CPP are incorporated. |
| Confidence `94` | Main/error behavior and storage routes are exact; the 113-byte active duplicate plus initializer slots independently prove source-manager roles and fields. Human source names and `ImageWriters.cpp` placement are high-probability inferences rather than surviving symbols, keeping the page below final-audit `95+`. |

## Cross-References

- [UID:0000K4][ImageWriters](by-file/ImageWriters.md)
- [UID:0000ND][ScreenshotCapture](by-file/ScreenshotCapture.md)
- [UID:0001G8][0x00557840-0x00557a96.ScreenshotJpegCapturePath](by-memory/0x00557840-0x00557a96.ScreenshotJpegCapturePath.md)
- [UID:0000KN][LibJPEG](by-file/LibJPEG.md)
- [UID:0000VP][0x00401000-0x00414283.LibJpeg6bCore](by-memory/0x00401000-0x00414283.LibJpeg6bCore.md)
- [UID:0000VS][0x00403920-0x004048e2.LibJpegCompressApiFrontEnd](by-memory/0x00403920-0x004048e2.LibJpegCompressApiFrontEnd.md)
- [UID:0002ZL][0x0066db40-0x0066db42.JpegScreenshotEoiMarker](by-memory/0x0066db40-0x0066db42.JpegScreenshotEoiMarker.md)
- [UID:0001QE][client_libraries](by-meta/client_libraries.md)

## Changes

### 2026-08-01 - B002 UID0002ZL implementation callback

- Raised `88/90 -> 92/94` without changing `CANONICAL_OWNER:0000K4`, `RECONSTRUCTABLE:TRUE`, or `EMITTER_UIDS:0000K4`.
- Replaced the historical external const marker, `const_cast`, and destination-manager first draft with the exact accepted `jpeg_source_mgr` reconstruction, including saved source pointer/count fields, first-fill state, EOI fallback, signed-positive skip behavior, no-op init/term callbacks, and the existing compressor/error wrapper.
- Added the byte-identical active ImageLoaders callback family and initializer-slot proof, raw helper and raw screenshot-path pointer-route negatives, retained/legacy disposition, exact marker definition ordering, `ImageWriters.cpp` source placement, and human source-name rationale.
- Preserved all prior main writer, argument order, caller, callee, RGB565/RGB555 conversion, scanline, error, cleanup, range, successor, and `0xcc` padding evidence. Earlier destination-manager conclusions remain below as explicitly historical evidence rather than active reconstruction claims.
- The H formal block remains intentionally blank because this page emits translation-unit implementation bodies only; shared includes and `s_jpegEoiMarker` are emitted earlier by [UID:0002ZG][JpegScreenshotEoiMarker](by-global/JpegScreenshotEoiMarker.md).

### 2026-07-06 - B011 implementation callback

- Historical state: changed to `COMPLETION:88`, `CONFIDENCE:90`, preserving `CANONICAL_OWNER:0000K4`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000K4`, and blank `EMITTER_POSITION_OPTIONAL`.
- Historical first draft: added product wrapper and adjacent helper bodies using public IJG 6b types/API names, `JpegScreenshotErrorManager`, behavior-based destination/null names, and an external marker declaration. The current callback replaces those disproven destination/extern/const-cast choices while retaining B011's valid binary observations.
- Summary/evidence: B011 MCP session `supervisor_recovery_20260705` reconfirmed the `0x004efbc0-0x004efe94` range as `0x2d4` / 724 bytes, `sub_4EFBC0` as `0x222` / 546 bytes with 159 instructions and 16 basic blocks, raw destination bodies as `0x35` / 53 bytes and `0x17` / 23 bytes, `sub_4EFE70` as `0x24` / 36 bytes with 15 instructions and 3 blocks, no modeled callers, the raw screenshot-path call at `0x005579d3`, exact argument pushes, public IJG callee route, RGB565/RGB555 conversion formulas, error-manager longjmp behavior, raw destination callback no-xref caveat, fallback marker xref, and all `0xcc` padding spans.
- Historical B011 conclusion: libjpeg evidence was then sufficient for a first draft, while helper names and raw JPG reachability were still treated as caps. The UID0002ZL pass has since resolved those behavioral/source-placement questions to the current `92/94` standard.

### 2026-06-11 - A010 Batch 157 parent assignment

- Changed to: `COMPLETION:86`, `CONFIDENCE:89`, `AUTOGEN_PARENT_UID:0000K4`.
- Historical state before the B011 callback: live IDA MCP reconfirmed the exact modeled and raw support boundaries, single raw screenshot-path writer xref, writer-local error-exit assignment, fallback EOI marker xref, full callee set, RGB565/RGB555 conversion behavior, raw destination callback disassembly, and successor boundary. [UID:0000K4][ImageWriters](by-file/ImageWriters.md) was then `86/85` and already owned the related screenshot EOI marker global, so the child and direct parent cleared the corrected `85/85` gate. C++ remained blank in that historical state because final local callback and libjpeg field names had not yet been reanalyzed to B011 source-quality.

### 2026-06-01 - Rescored with current IDA callback/callee evidence

- Raised completion/confidence from `50/65` to `74/84` and marked `RECONSTRUCTABLE:TRUE`.
- Added exact current IDA MCP evidence for function sizes, raw callback bytes, callee set, caller/xref map, error-handler installation, pixel conversion behavior, and internal alignment spans.
- Left `AUTOGEN_PARENT_UID` blank because [UID:0000K4][ImageWriters](by-file/ImageWriters.md) has no projected reconstruction path yet.
- Left C++ blank because the wrapper is reconstructable, but local libjpeg struct field names and callback source names are not final-source quality.

### 2026-06-07 - A005 Batch 006 live IDA evidence pass

- Changed to: `COMPLETION:84`, `CONFIDENCE:88`, `AUTOGEN_PARENT_UID:` blank.
- Summary/evidence: live IDA MCP reconfirmed the exact main writer boundary, support callback/null-stub boundaries, raw callback non-function status, single raw screenshot-path xref, writer-local error callback assignment, complete callee set, decompiled JPEG setup/conversion/scanline flow, raw destination callback disassembly, fallback storage xref, error-exit longjmp behavior, and padding bytes.
- Parent remains blank because the child is below 85 completion and direct parent candidates [UID:0000K4][ImageWriters](by-file/ImageWriters.md) and [UID:0000ND][ScreenshotCapture](by-file/ScreenshotCapture.md) do not clear the corrected 85/85 gate.

### 2026-05-28 - Extended range to include writer-local JPEG callbacks

- What existed before: the page and filename ended at `0x004efde1`, leaving `0x004efde1-0x004efea0` as an UNKNOWN gap.
- What changed: the page and filename now cover `0x004efbc0-0x004efe94`, including the final byte of `sub_4EFBC0` and adjacent writer-local support callbacks/null stubs; only internal alignment and the final `0x004efe94-0x004efea0` alignment are ignored.
- Evidence: IDA MCP shows `sub_4EFBC0` ending at `0x004efde2`, `sub_4EFBC0` assigning `sub_4EFE70` at `0x004efc0d`, raw callback bodies at `0x004efe00-0x004efe35` and `0x004efe40-0x004efe57`, `nullsub_36`/`nullsub_37` at `0x004efdf0` and `0x004efe60`, and `KeySpeedMgr` beginning at `0x004efea0`.
