*** UID:00045F | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006L | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IntegrateMacroDialog::OnDraw()
{
    constexpr int kInstructionXInset = 9;
    constexpr int kInstructionLineStep = 18;
    constexpr int kInstructionFirstLineOffset = 36;
    constexpr int kInstructionDrawColor = 0;
    constexpr int kInstructionTextColor = 143;
    constexpr int kInstructionTextBackColor = 128;
    constexpr unsigned char kInstructionTextDrawMode = 1;

    wchar_t text[256];

    SetDrawColor(kInstructionDrawColor);
    FillRect(&m_bounds);
    SetTextColor(kInstructionTextColor);
    SetTextBackColor(kInstructionTextBackColor);
    m_textDrawMode = kInstructionTextDrawMode;
    SetFontId(IsLegacyAssetMode() ? 1 : 0);

    const int x = m_bounds.left + kInstructionXInset;
    const int y = m_bounds.bottom;

    swprintf_s(text, _countof(text), L"Spell : Select 'SPELLS' tab and press key.");
    DrawOutlinedText(x, y - kInstructionFirstLineOffset, text, static_cast<int>(wcslen(text)));

    swprintf_s(text, _countof(text), L"Item  : Select 'ITEMS' tab and press key.");
    DrawOutlinedText(x, y - kInstructionLineStep, text, static_cast<int>(wcslen(text)));

    swprintf_s(text, _countof(text), L"Text  : Press ';' key");
    DrawOutlinedText(x, y, text, static_cast<int>(wcslen(text)));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-authored IntegrateMacroDialog::OnDraw virtual draw handler at primary vtable slot 0x00621174; emits formal C++ that fills m_bounds, selects draw/text colors and font mode, then draws the Spell/Item/Text instruction literals from 0x00621688/0x006216e0/0x00621734. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00541e50-0x00541f96 IntegrateMacroDialogOnDraw

## Status

- Address range: `0x00541e50-0x00541f96` half-open, size `0x146`.
- Current IDA/MCP function: `sub_541E50`.
- Intended direct owner after split: [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) `IntegrateMacroDialog`, emitted through [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md) `MacroDialogs`.
- Parent split/container: [UID:0001E1][0x00541b30-0x00542265.IntegrateMacroDialog](by-memory/0x00541b30-0x00542265.IntegrateMacroDialog.md) `0x00541b30-0x00542265.IntegrateMacroDialog`.
- Rebuild handling: source-authored draw handler for the integrated macro sequence dialog.
- Formal C++ disposition: accepted formal `IntegrateMacroDialog::OnDraw()` child emitter through [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) and [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md). The previous split-stage blank C++ wording is superseded.

## Behavior

`OnDraw` paints the integrated macro dialog frame/background and instruction text for the macro sequence editor. It is a virtual draw handler routed through primary vtable slot `0x00621174` and has no direct code callers, which is normal virtual-dispatch evidence rather than a no-code reason.

The accepted helper sequence is:

- `SetDrawColor(0)` and `FillRect(&m_bounds)`. `FillRect(&m_bounds)` is the source-facing wrapper for the raw full-bounds fill through the inherited GrafPort slot.
- `SetTextColor(143)`, `SetTextBackColor(128)`, and `m_textDrawMode = 1`. `SetTextBackColor(128)` names the observed `this+0x80` text background/outline color.
- `SetFontId(IsLegacyAssetMode() ? 1 : 0)`. This intentionally omits the raw ignored `g_pFontImageLib` load and keeps the source-facing legacy asset mode helper.
- Compute `x = m_bounds.left + 9` and `y = m_bounds.bottom`, then draw the three instruction strings at `(x, y - 36)`, `(x, y - 18)`, and `(x, y)`.
- Each `swprintf_s` writes into a 256-wide-character stack buffer. `_countof(text)` is the source-facing form of the observed `0x100` wide-char count.

The instruction text confirmed during the accepted MCP pass is:

- `Spell : Select 'SPELLS' tab and press key.`
- `Item  : Select 'ITEMS' tab and press key.`
- `Text  : Press ';' key`

## Evidence

- B003 live MCP `lookup_funcs` command id `103` identifies `0x00541e50` as `sub_541E50`, size `0x146`, ending at `0x00541f96`; `0x00541f96` is not a function and the next modeled function is `0x00541fa0`.
- B003 live MCP `analyze_function` command id `106` reports no direct code callers and one data xref at `0x00621174`. B003 `xrefs_to` command id `301` likewise reports the target xref only from `0x00621174`.
- [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md) records the IntegrateMacroDialog primary vtable `0x00621130-0x0062118c`, slot `+0x44 -> 0x00541e50` as `OnDraw`.
- B003 `decompile` command id `104` and `disasm` command id `105` confirm the accepted call sequence: `SetDrawColor(0)`, inherited full-bounds fill using `this+0x44`/`this+0x50` bounds, `SetTextColor(143)`, `SetTextBackColor(128)`, text draw mode byte `1`, `IsLegacyAssetMode()` into `SetFontId`, three `swprintf_s` calls, and three outlined text draws.
- Helper checks in the accepted report keep these source-facing names: `0x004b60b0` is `IsLegacyAssetMode()` / `byte_66DA97 != 1`; `0x004b9640` is `SetFontId`; `0x004b9660` is `SetDrawColor`; `0x004b9680` is `SetTextColor`; `0x004b9690` is `SetTextBackColor`; and `0x004bacd0` is `DrawOutlinedText`.
- B003 `xrefs_to` command id `301` confirms literal refs from `0x00541eb8`, `0x00541efa`, and `0x00541f43`. `get_string` command id `303` returns full Spell/Item strings but truncates `0x00621734` to `Te`; `get_bytes` command id `401` decodes the full UTF-16 bytes as `Text  : Press ';' key`.
- Earlier command ids `70`, `72`, and `80` remain consistent: the child uses drawing/text helper calls only and does not own macro-record mutation.

## Boundaries

- Previous range `0x00541e4f-0x00541e50` is one byte of `0xcc` padding after the destructor helper.
- Next range `0x00541f96-0x00541fa0` is ten bytes of `0xcc` padding before `OnButtonClick`.
- `OnDraw` is not owned by `OptionPane` or `TargetOptionDialog`; it belongs to the macro-dialog family through [UID:00006L][IntegrateMacroDialog](by-class/IntegrateMacroDialog.md) and [UID:0000KY][MacroDialogs](by-file/MacroDialogs.md).

## Formal C++ Disposition

Formal C++ is now accepted on this exact child. Blank C++, covered-by classification, inlining/re-emitting shared GrafPort helpers, rerouting this source body to GrafPort, assigning it to constructor/control-update/resource-load paths, and inventing new out-of-line helpers were rejected. This page emits only the source-authored virtual draw method; shared draw-state/text helpers remain documented by their own support pages.

## Changes

- 2026-06-28 B003 implementation callback for UID00045F:
  - Raised this target from `88/91` to `90/92`, inserted the accepted formal `IntegrateMacroDialog::OnDraw()` C++ body, and replaced stale split-stage blank-emitter wording.
  - Recorded the primary vtable slot `0x00621174`, exact `0x146` function range, no direct code callers, helper call sequence, bounds offsets, color/text mode/font mode values, all three instruction strings, the `get_string` truncation for `0x00621734`, and the rejected alternatives from the accepted report.
