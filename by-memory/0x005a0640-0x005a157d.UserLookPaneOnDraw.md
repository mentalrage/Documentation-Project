*** UID:0003RM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserLookPane::OnDraw()
{
    EPFTileContext tileContext;
    RectBounds imageBounds;
    RectBounds overlayBounds;
    RectBounds destinationBounds;
    RectBounds buttonBounds;

    void *backgroundPalette = g_pEPFLib;

    switch (m_viewMode)
    {
    case ViewMode_MoreInfo:
        g_pEPFLib->LookupLayoutEntry(L"USERLOOK.EPF", 0, &tileContext);
        backgroundPalette =
            g_pPaletteLib->GetPaletteByName(L"USERLOOK.PAL");
        break;

    case ViewMode_Profile:
        (void)g_pApplication->GetVersionMajor();
        g_pEPFLib->LookupLayoutEntry(L"USERLOOK.EPF", 1, &tileContext);
        backgroundPalette =
            g_pPaletteLib->GetPaletteByName(L"USERLOOK.PAL");
        break;

    case ViewMode_Bio:
        g_pEPFLib->LookupLayoutEntry(L"PFLOOK.EPF", 0, &tileContext);
        backgroundPalette =
            g_pPaletteLib->GetPaletteByName(L"PFLOOK.PAL");
        break;

    case ViewMode_Legend:
        g_pEPFLib->LookupLayoutEntry(L"USERLOOK.EPF", 3, &tileContext);
        backgroundPalette =
            g_pPaletteLib->GetPaletteByName(L"USERLOOK.PAL");
        break;
    }

    g_pfnBlitSprite(this,
                     &tileContext,
                     &tileContext.bounds,
                     &m_bounds,
                     0,
                     backgroundPalette,
                     NULL);

    switch (m_viewMode)
    {
    case ViewMode_MoreInfo:
    {
        const int headerLength = static_cast<int>(wcslen(m_headerText));
        const int headerHalfWidth = GetTextWidth(m_headerText,
                                                 headerLength) / -2;
        const int headerShadowX = 35 + headerHalfWidth;
        SetTextColor(128);
        MoveTo(headerShadowX > 3 ? headerShadowX : 3, 25);
        DrawWideText(m_headerText,
                     static_cast<int>(wcslen(m_headerText)));
        SetTextColor(143);
        MoveTo(headerShadowX > 3 ? 34 + headerHalfWidth : 2, 24);
        DrawWideText(m_headerText,
                     static_cast<int>(wcslen(m_headerText)));

        const int targetNameLength =
            static_cast<int>(wcslen(m_targetUserName));
        const int targetNameHalfWidth =
            GetTextWidth(m_targetUserName, targetNameLength) / -2;
        SetTextColor(128);
        MoveTo(118 + targetNameHalfWidth, 25);
        DrawWideText(m_targetUserName,
                     static_cast<int>(wcslen(m_targetUserName)));
        SetTextColor(m_targetNameHighlighted ? 143 : 10);
        MoveTo(117 + targetNameHalfWidth, 24);
        DrawWideText(m_targetUserName,
                     static_cast<int>(wcslen(m_targetUserName)));

        const int line1Length = static_cast<int>(wcslen(m_identityLine1));
        const int line1HalfWidth =
            GetTextWidth(m_identityLine1, line1Length) / -2;
        SetTextColor(128);
        MoveTo(106 + line1HalfWidth, 45);
        DrawWideText(m_identityLine1,
                     static_cast<int>(wcslen(m_identityLine1)));
        SetTextColor(143);
        MoveTo(105 + line1HalfWidth, 44);
        DrawWideText(m_identityLine1,
                     static_cast<int>(wcslen(m_identityLine1)));
        SetTextBackColor(0);

        const int line2Length = static_cast<int>(wcslen(m_identityLine2));
        const int line2HalfWidth =
            GetTextWidth(m_identityLine2, line2Length) / -2;
        SetTextColor(128);
        MoveTo(106 + line2HalfWidth, 66);
        DrawWideText(m_identityLine2,
                     static_cast<int>(wcslen(m_identityLine2)));
        SetTextColor(143);
        MoveTo(105 + line2HalfWidth, 65);
        DrawWideText(m_identityLine2,
                     static_cast<int>(wcslen(m_identityLine2)));

        const int line3Length = static_cast<int>(wcslen(m_identityLine3));
        const int line3HalfWidth =
            GetTextWidth(m_identityLine3, line3Length) / -2;
        SetTextColor(128);
        MoveTo(106 + line3HalfWidth, 87);
        DrawWideText(m_identityLine3,
                     static_cast<int>(wcslen(m_identityLine3)));
        SetTextColor(143);
        MoveTo(105 + line3HalfWidth, 86);
        DrawWideText(m_identityLine3,
                     static_cast<int>(wcslen(m_identityLine3)));

        if (m_status.m_statusKind == ObjectStatusBlob::HumanObject)
        {
            g_pNewHumanImageLib->CalculateBounds(&m_status,
                                                 2,
                                                 0,
                                                 0,
                                                 &imageBounds,
                                                 0,
                                                 0,
                                                 1,
                                                 0);
            g_pNewHumanImageLib->ComputeOverlayBounds(&m_status,
                                                      2,
                                                      0,
                                                      0,
                                                      &overlayBounds,
                                                      0,
                                                      1);

            destinationBounds.bottom = 187;
            destinationBounds.top =
                187 - (imageBounds.bottom - imageBounds.top);
            destinationBounds.left = 54;
            destinationBounds.right =
                54 + (imageBounds.right - imageBounds.left);
            OffsetRect(&destinationBounds, overlayBounds.left, 0);
            destinationBounds.left +=
                imageBounds.left - overlayBounds.left;
            destinationBounds.right +=
                imageBounds.right - overlayBounds.right;

            g_pNewHumanImageLib->Draw(this,
                                      &destinationBounds,
                                      &m_status,
                                      2,
                                      0,
                                      0,
                                      0,
                                      0,
                                      0,
                                      0.0f,
                                      0,
                                      1,
                                      0);
        }
        else if (m_status.m_statusKind == ObjectStatusBlob::MonsterObject)
        {
            g_pMonsterImageLib->ComputeRenderBounds(&m_status,
                                                    7,
                                                    0,
                                                    &imageBounds,
                                                    NULL);
            const int width = imageBounds.right - imageBounds.left;
            const int height = imageBounds.bottom - imageBounds.top;
            const int halfWidth = width / -2;
            const int halfHeight = height / -2;
            destinationBounds.left = 78 + halfWidth;
            destinationBounds.right = 78 + width + halfWidth;
            destinationBounds.top = 131 + halfHeight;
            destinationBounds.bottom = 131 + height + halfHeight;

            g_pMonsterImageLib->RenderMonsterImage(this,
                                                   &destinationBounds,
                                                   &m_status,
                                                   7,
                                                   0,
                                                   0,
                                                   0,
                                                   NULL,
                                                   0.0f);
        }

        GetActionButtonRect(ActionButton_Exchange, &buttonBounds);
        if (m_exchangeActionState == ButtonVisualState_Disabled)
        {
            SetDrawColor(143);
            m_drawMode = 0;
            g_pfnFillRect(this, &buttonBounds);
        }
        else
        {
            g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                         m_exchangeActionState ? 8 : 3,
                                         &tileContext);
            RenderTileFrame(&tileContext,
                            &tileContext.bounds,
                            &buttonBounds,
                            0,
                            L"BUTTON.PAL",
                            NULL);
        }

        GetActionButtonRect(ActionButton_Group, &buttonBounds);
        if (m_groupActionState == ButtonVisualState_Disabled)
        {
            SetDrawColor(143);
            m_drawMode = 0;
            g_pfnFillRect(this, &buttonBounds);
        }
        else
        {
            g_pEPFLib->LookupLayoutEntry(L"STATBUT.EPF",
                                         m_groupActionState ? 7 : 2,
                                         &tileContext);
            RenderTileFrame(&tileContext,
                            &tileContext.bounds,
                            &buttonBounds,
                            0,
                            L"BUTTON.PAL",
                            NULL);
        }
        break;
    }

    case ViewMode_Profile:
    {
        if (m_bodyId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_bodyId, &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 92 + halfHeight;
            destinationBounds.bottom =
                92 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_bodyId),
                                              m_bodyPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_leftHandId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_leftHandId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 150 + halfWidth;
            destinationBounds.right =
                150 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 92 + halfHeight;
            destinationBounds.bottom =
                92 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_leftHandId),
                                              m_leftHandPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_rightHandId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_rightHandId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 34 + halfWidth;
            destinationBounds.right =
                34 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 92 + halfHeight;
            destinationBounds.bottom =
                92 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_rightHandId),
                                              m_rightHandPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_headId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_headId, &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 36 + halfHeight;
            destinationBounds.bottom =
                36 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_headId),
                                              m_headPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_faceAccessory1Id != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_faceAccessory1Id,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 36 + halfWidth;
            destinationBounds.right =
                36 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 36 + halfHeight;
            destinationBounds.bottom =
                36 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_faceAccessory1Id),
                m_faceAccessory1Palette,
                NULL,
                0.0f);
        }

        if (m_headAccessory2Id != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_headAccessory2Id,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 150 + halfWidth;
            destinationBounds.right =
                150 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 36 + halfHeight;
            destinationBounds.bottom =
                36 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_headAccessory2Id),
                m_headAccessory2Palette,
                NULL,
                0.0f);
        }

        if (m_rightAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_rightAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 147 + halfWidth;
            destinationBounds.right =
                147 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 146 + halfHeight;
            destinationBounds.bottom =
                146 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_rightAccessoryId),
                m_rightAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_leftAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_leftAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 34 + halfWidth;
            destinationBounds.right =
                34 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 146 + halfHeight;
            destinationBounds.bottom =
                146 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_leftAccessoryId),
                m_leftAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_leftSubAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_leftSubAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 34 + halfWidth;
            destinationBounds.right =
                34 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 198 + halfHeight;
            destinationBounds.bottom =
                198 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_leftSubAccessoryId),
                m_leftSubAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_rightSubAccessoryId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_rightSubAccessoryId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 150 + halfWidth;
            destinationBounds.right =
                150 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 198 + halfHeight;
            destinationBounds.bottom =
                198 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(
                this,
                &destinationBounds,
                static_cast<short>(m_rightSubAccessoryId),
                m_rightSubAccessoryPalette,
                NULL,
                0.0f);
        }

        if (m_necklaceId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_necklaceId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 146 + halfHeight;
            destinationBounds.bottom =
                146 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_necklaceId),
                                              m_necklacePalette,
                                              NULL,
                                              0.0f);
        }

        if (m_footId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_footId, &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 34 + halfWidth;
            destinationBounds.right =
                34 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 250 + halfHeight;
            destinationBounds.bottom =
                250 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_footId),
                                              m_footPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_coatId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_coatId, &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 250 + halfHeight;
            destinationBounds.bottom =
                250 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_coatId),
                                              m_coatPalette,
                                              NULL,
                                              0.0f);
        }

        if (m_mantleId != 0)
        {
            g_pItemObjImageLib->GetItemGlyphBounds(m_mantleId,
                                                   &imageBounds);
            const int halfWidth =
                (imageBounds.right - imageBounds.left) / -2;
            const int halfHeight =
                (imageBounds.bottom - imageBounds.top) / -2;
            destinationBounds.left = 92 + halfWidth;
            destinationBounds.right =
                92 + (imageBounds.right - imageBounds.left) + halfWidth;
            destinationBounds.top = 198 + halfHeight;
            destinationBounds.bottom =
                198 + (imageBounds.bottom - imageBounds.top) + halfHeight;
            g_pItemObjImageLib->DrawItemImage(this,
                                              &destinationBounds,
                                              static_cast<short>(m_mantleId),
                                              m_mantlePalette,
                                              NULL,
                                              0.0f);
        }
        break;
    }

    case ViewMode_Bio:
    {
        RectBounds portraitBounds;
        InitRectBounds(&portraitBounds, 70, 48, 118, 104);
        void *portraitPalette =
            g_pPaletteLib->GetSlotPalette(13, 0, 0);

        if (m_portraitPayloadLength != 0)
        {
            g_pfnBlitSprite(this,
                             &m_portraitImage,
                             &m_portraitImage.bounds,
                             &portraitBounds,
                             0,
                             portraitPalette,
                             NULL);
        }
        else
        {
            g_pEPFLib->LookupLayoutEntry(L"FACE.EPF",
                                         m_statusEncoding != 0,
                                         &tileContext);
            SurfaceSpriteBlitOptions portraitOptions;
            portraitOptions.mode = 4;
            g_pfnBlitSprite(this,
                             &tileContext,
                             &tileContext.bounds,
                             &portraitBounds,
                             1,
                             NULL,
                             &portraitOptions);
        }

        g_pEPFLib->LookupLayoutEntry(L"NATION.EPF",
                                     m_portraitFallbackFrame,
                                     &tileContext);
        break;
    }

    case ViewMode_Legend:
        break;

    default:
        return;
    }

    g_pEPFLib->LookupLayoutEntry(L"INVENBUT.EPF",
                                 7 * m_nextButtonFrame + 6,
                                 &tileContext);
    GetActionButtonRect(ActionButton_Next, &buttonBounds);
    RenderTileFrame(&tileContext,
                    &tileContext.bounds,
                    &buttonBounds,
                    0,
                    L"INVENBUT.PAL",
                    NULL);

    g_pEPFLib->LookupLayoutEntry(L"INVENBUT.EPF",
                                 7 * m_previousButtonFrame + 5,
                                 &tileContext);
    GetActionButtonRect(ActionButton_Previous, &buttonBounds);
    RenderTileFrame(&tileContext,
                    &tileContext.bounds,
                    &buttonBounds,
                    0,
                    L"INVENBUT.PAL",
                    NULL);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Complete primary-vtable +0x44 UserLookPane::OnDraw source with exact 3,901-byte/hash/1,356-instruction/97-block behavior, four mode backgrounds, repeated text work, human/monster status, fourteen equipment images, portrait/fallback/NATION behavior, action/navigation states, odd unknown/mode-3 tails, and compiler-only cookie exclusion. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005a0640-0x005a157d - UserLookPaneOnDraw

## Status

- Role: exact `UserLookPane` render/paint body child split from [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md).
- IDA status: modeled function `sub_5A0640`, size `0xf3d`, ending at `0x005a157d`.
- Owner/emitter: [UID:0000FP][UserLookPane](by-class/UserLookPane.md), with file route through [UID:0000P0][UserLookPane](by-file/UserLookPane.md).
- Reconstruction note: complete behavior-identical source now emits here as `void UserLookPane::OnDraw()`. UID0003RK resolves the consumed state, UID0003RP/UID0003RQ own exact geometry helpers, and compiler-generated cookie/switch/vtable lowering remains excluded.

## Evidence

- B001 live IDA audit confirms primary-vtable `+0x44` routing from `0x0062ed10`; adjacent `+0x48` is UID0003RK `OnActivate` at `0x0059f610`.
- The renderer draws the remote-character look/profile panel, uses resource/string data from [UID:00026V][0x0062eccc-0x0062ef0c.UserLookReadOnlyData](by-memory/0x0062eccc-0x0062ef0c.UserLookReadOnlyData.md), and calls [UID:0003RP][0x005a1a70-0x005a1b4d.UserLookPaneGetActionButtonRect](by-memory/0x005a1a70-0x005a1b4d.UserLookPaneGetActionButtonRect.md).
- Support docs keep image-library/resource dependencies as consumers, not owners of the user-look panel render body.
- UID0003RK writes the exact state rendered here: three 256-wide identity/display lines; 128-wide header and target-user-name strings; complete 68-byte ObjectStatusBlob; fourteen accepted equipment id/palette records; target object id; exchange/group action bytes; portrait fallback/payload buffer/EPFTileContext; view mode; next/previous `ButtonFrame` values; and target-name highlight at `+0xb7c`.
- Renderer coordinates/selectors are the cross-consumer evidence for Body, LeftHand, RightHand, Head, RightAccessory, LeftAccessory, FaceAccessory1, HeadAccessory2, LeftSubAccessory, RightSubAccessory, Necklace, Foot, Coat, and Mantle source order. The fifth leading packet string is the target user name; the first four remain descriptive identity/header names because renderer positions do not recover their original labels.
- Shared `STATBUT.EPF`, image libraries, Part tables, palette maps, ObjectStatusBlob, portrait decode, and action rectangle helpers remain dependencies. No duplicate packet parsing, raw offsets, or ownership transfer is accepted.

## Mode-2 PFLOOK And UID0003RT Evidence

- The live renderer's mode-2 branch loads `PFLOOK.EPF` and `PFLOOK.PAL`, then renders the stored portrait or fallback state. This is direct evidence that mode 2 is not a text-only `ProfilePane` page.
- UID0003RK supplies the complementary inputs before switching: `m_profileText[256]`, `m_profileTextLength`, ProfilePane text, portrait payload/fallback state, and `m_portraitImage`. UID0003RT selects and lays out that already-populated page; this renderer remains responsible for drawing it.
- The settled view map is mode 0 MoreInfo overview, mode 1 equipment/profile display, mode 2 Bio/profile text plus PFLOOK portrait, and mode 3 Legend. Mode 1 and mode 2 intentionally share ProfilePane without sharing one public method name.
- `ShowBioView` is retained for UID0003RT because it distinguishes the composite mode-2 page from mode-1 `ShowProfileView`. `ShowProfileTextView` would omit this renderer-owned portrait facet; `ShowBiographyView` has no stronger lexical evidence.
- UID0003RT does not own or duplicate renderer source. It only stores mode 2, detaches inactive children, resets/attaches ProfilePane, orders it, and invalidates UserLookPane bounds. This page is `93/94`, owner/emitter UID0000FP, and carries the complete independent OnDraw body; the view helper remains separate.
- Negative ownership evidence remains exact: resource loads, image libraries, PFLOOK assets, portrait decode/storage, and UID0003RT consumer calls do not transfer renderer or view-helper ownership to those dependencies or create a second target body.

## Exact Binary And Dispatch Evidence

- Exact body range is `[0x005a0640,0x005a157d)`, size `0xf3d` / 3,901 bytes. Local PE and MCP bytes are identical with SHA256 `7838C2B5B07A811FE20282A537BDD01692FF9B5ADD38AB4DAA24A03B3AC9C373`.
- Entry bytes are `55 8b ec 81 ec a0 00 00 00 a1 24 2f 67 00 33 c5`; final bytes are `4d fc 5f 5e 33 cd 5b e8 b6 61 02 00 8b e5 5d c3`. Three `0xcc` alignment bytes at `[0x005a157d,0x005a1580)` are outside this child.
- The modeled function contains 1,356 instructions, 97 basic blocks, cyclomatic complexity 46, and 25 unique callees. It has no direct code caller because it is virtual.
- Sole liveness xref is primary UserLookPane vtable cell `0x0062ed10`, relative slot `+0x44`; adjacent `+0x48` is UID0003RK `OnActivate`. The source ABI is therefore `virtual void OnDraw()`, not the historical behavior title `RenderUserLook` and not an integer-return decompiler artifact.
- Callee families are EPFTileContext initialization, Application version access, EPF/palette lookup, GrafPort movement/color/background/width/text/tile helpers, rectangle initialization/offset, NewHuman/Monster bounds and drawing, item glyph/drawing, action geometry, direct sprite/fill callbacks, and compiler cookie verification.

## Exact Drawing Behavior

- Modes 0, 1, and 3 load USERLOOK frames 0, 1, and 3 with `USERLOOK.PAL`; mode 2 loads PFLOOK frame 0 with `PFLOOK.PAL`. Mode 1 calls `Application::GetVersionMajor()` and discards its result before background lookup.
- Unknown modes intentionally retain the initialized empty tile and pass `g_pEPFLib` through the palette slot to the background callback, then return. This odd path is preserved rather than normalized.
- Mode 0 header center is clamped at x 3 for the dark pass and x 2 for the light pass. Target name centers at 118/117 with foreground 143 only when highlighted, otherwise 10. Three identity lines center at 106/105 with y pairs 45/44, 66/65, and 87/86.
- Every displayed string is scanned separately for width and for both draw calls. Text background resets to zero only after identity line 1; repeated scans and draw order are source-observable and remain repeated.
- HumanObject composition uses NewHuman bounds plus overlay bounds with direction 2, baseline bottom 187 and left 54, then exact overlay/image corrections before draw. MonsterObject uses selector 7/frame 0 and centers the complete image at `(78,131)`. Other status kinds skip the character image.
- Fourteen independent nonzero equipment branches call `g_pItemObjImageLib->GetItemGlyphBounds` and `DrawItemImage` with matching palette bytes. Centers are body `(92,92)`, left hand `(150,92)`, right hand `(34,92)`, head `(92,36)`, face accessory 1 `(36,36)`, head accessory 2 `(150,36)`, right accessory `(147,146)`, left accessory `(34,146)`, left sub-accessory `(34,198)`, right sub-accessory `(150,198)`, necklace `(92,146)`, foot `(34,250)`, coat `(92,250)`, and mantle `(92,198)`.
- Mode 0 Exchange id 1 consumes state `+0xb84` and frames 8/3; Group id 0 consumes `+0xb85` and frames 7/2. State `0xff` sets color 143, sets inherited `m_drawMode` to zero, and invokes direct fill before the common navigation tail.
- Mode 2 uses portrait rectangle `(70,48,118,104)` and palette slot `(13,0,0)`. Nonzero portrait data draws `m_portraitImage`; zero data loads FACE frame `(m_statusEncoding != 0)`, sets only `SurfaceSpriteBlitOptions::mode = 4`, blits in mode 1 with null palette, then performs the observed result-unused `NATION.EPF` lookup with `m_portraitFallbackFrame`.
- Mode 3 intentionally has no mode-local body but reaches common navigation. Invalid rectangles from UID0003RP are therefore passed through as `(-1,-1,-1,-1)` rather than causing an invented early return.
- Common Next id 3 uses INVENBUT frame `7 * m_nextButtonFrame + 6`; Previous id 2 uses `7 * m_previousButtonFrame + 5`; both use `INVENBUT.PAL`. Mouse writes independently prove normal/hover/pressed values 0/1/2.

## Source Shape And Exclusions

- Source ownership is exact child UID0003RM, class emitter UID0000FP, and file emitter UID0000P0. Resource-data/vtable pages document physical storage but do not emit duplicate globals or raw ABI tables.
- `SurfaceSpriteBlitOptions` is the accepted 32-byte shared source type. This method sets only byte-zero member `mode`; value-initializing or fabricating the remaining fields would change the observed source shape.
- UID0003RP owns the mode/action rectangle matrix and UID0003RQ owns first-hit action testing. This method calls those helpers rather than duplicating their source responsibilities.
- Security-cookie calls, switch tables, raw vtable/RTTI arrays, alignment bytes, and incidental final-call EAX values are compiler-owned. No manual vptr store, cookie, SEH state, table, adjustor, scalar wrapper, or decompiler label is emitted.
- Historical blank-body, behavior-title, hover-action-index, pressed-action-index, and x/y assumptions are retained as superseded history. Exact private spelling for some display strings and `m_portraitFallbackFrame` remains a confidence cap only; no behavior/type/body blocker remains.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Complete source body, exact range/hash/instruction/CFG/callee inventory, all modes/resources/fields/helpers, ownership, source placement, and compiler exclusions are documented. |
| Confidence | 94 | Exact bytes, unique vtable dispatch, complete branch behavior, independent parser/mouse/helper corroboration, and compile-directed dependency contracts leave only bounded private lexical uncertainty. |

## Cross-References

- [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md)
- [UID:0003RP][0x005a1a70-0x005a1b4d.UserLookPaneGetActionButtonRect](by-memory/0x005a1a70-0x005a1b4d.UserLookPaneGetActionButtonRect.md)
- [UID:00026V][0x0062eccc-0x0062ef0c.UserLookReadOnlyData](by-memory/0x0062eccc-0x0062ef0c.UserLookReadOnlyData.md)
- [UID:0000FP][UserLookPane](by-class/UserLookPane.md)
- [UID:0000P0][UserLookPane](by-file/UserLookPane.md)

## Changes

- 2026-07-20 B002 UID0003RM accepted implementation callback: UID-preserving rename from `UserLookPaneRenderUserLook` to `UserLookPaneOnDraw`; raised `86/90 -> 93/94`; installed complete `void UserLookPane::OnDraw()` source; added exact bytes/hash/instruction/CFG/callee/vtable/mode/text/status/equipment/portrait/action/resource/compiler evidence; and historicalized the prior blank-body/title/frame-field assumptions without transferring resource or ABI ownership.

- 2026-07-13 B001 UID0003RT accepted callback support sync: preserved `86/90`, owner/emitter UID0000FP, and blank formal body; added direct mode-2 `PFLOOK.EPF`/`PFLOOK.PAL` portrait/fallback evidence, parser/view-helper role separation, settled four-mode naming, and no-duplicate/no-owner-transfer disposition.
- 2026-07-13 B005 UID0003RK accepted callback support sync: preserved `86/90`, owner/emitter UID0000FP, and blank formal body; synchronized the primary `+0x44` slot, exact target-produced field/selector consumers, descriptive-name caps, resource dependencies, and exclusion of duplicate parser/ownership claims.
- 2026-06-16 B001 UserLookPane split execution: Created exact render method child after B001 confirmed the function boundary, vtable route, helper calls, read-only-data context, and remaining render/source-name blockers.
