*** UID:0002IC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void HierList::InsertBefore(const void *parentPayload, const void *referencePayload, const void *payload)
{
    MemoryMan *memoryMan = GetMemoryMan();
    HierListNode *node =
        static_cast<HierListNode *>(memoryMan->AllocateBufferMemory(m_elementSize));
    node->childList = NULL;
    memoryMan->MemmoveWrapper(node->payload, payload, m_elementSize - 11);

    int referenceIndex;
    if (parentPayload == NULL) {
        HierList *referenceOwnerList;
        if (referencePayload != NULL) {
            FindNodeByPayload(referencePayload, &referenceOwnerList, &referenceIndex);
        } else {
            referenceOwnerList = this;
            referenceIndex = -1;
        }

        node->parent = ownerNode;
        InsertAt(referenceIndex + 1, 1, node);
    } else {
        HierListNode *parentNode = FindNodeByPayload(parentPayload, NULL, NULL);
        HierList *childList = parentNode->childList;
        if (childList == NULL) {
            childList = new HierList(m_elementSize - 11, parentNode);
        }

        if (referencePayload != NULL) {
            FindNodeByPayload(referencePayload, NULL, &referenceIndex);
        } else {
            referenceIndex = -1;
        }

        node->parent = parentNode;
        childList->InsertAt(referenceIndex + 1, 1, node);
    }

    memoryMan->FreeBufferMemory(node);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Inserts a copied hierarchy row into root/child storage via lookup-driven slot `index + 1`; live return is the free-helper zero. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ce860-0x004ce9ad HierListInsertBefore

## Layer Consumer Synchronization - 2026-07-14

[UID:0004NU][0x004f0840-0x004f0a5d.LayerAddChildBefore](by-memory/0x004f0840-0x004f0a5d.LayerAddChildBefore.md) calls this exact HierList source body at `0x004f0a3b`. Layer passes a 48-byte LayerNode payload; the physical HierList row adds `parent` and `childList` before payload and has the established 59-byte stride. Parent/reference arguments are payload pointers, while HierList resolves row headers and owner lists internally.

The Layer caller initializes both bounds rectangles but intentionally leaves paneFlags/padding indeterminate. That caller detail does not alter this page's accepted score, HierList ownership, null-reference semantics, or formal body.

## Status

- Entity kind: class method
- Owner: [UID:000065][HierList](by-class/HierList.md)
- Likely source file: [UID:0000JV][HierList](by-file/HierList.md)
- Parent aggregate: [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md)
- Rebuild handling: source-authored, reconstructable; attached to the `HierList` class metadata for `util/HierList.cpp`. Formal first-draft C++ is now present.

## Behavior

Creates a temporary hierarchy row and inserts it into either the root list or a lazily-created child `HierList`.

The method treats the first argument as an optional parent payload, the second as an optional sibling/reference payload, and the third as the caller payload to copy. It allocates `this->elementSizeWithHierarchyOverhead` bytes with `sub_516050`, clears the child-list pointer at row `+0x04`, then copies `elementSizeWithHierarchyOverhead - 11` payload bytes to row `+0x08`.

When a parent payload is supplied, it resolves the parent node with [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md). If the parent node has no child list, it allocates 24 bytes, calls the `List` constructor with the same element size and page size `32`, installs the `HierList` vtable, writes the parent node into the child list owner field at `+0x14`, and stores the child list at parent node `+0x04`. The new row's parent pointer at `+0x00` is the found parent node.

When no parent payload is supplied, the target list is either the root list or the owner list returned while resolving the sibling payload. The new row's parent pointer is copied from the root list owner field at `this + 0x14`.

The raw insertion slot is always `resolvedIndex + 1`. With no sibling/reference payload this method uses `-1`, so the root/no-sibling and new-child-list defaults insert at index `0`. With a resolved sibling payload, `FindNodeByData` writes the actual zero-based index, and the virtual slot `+0x14` dispatches to [UID:0002IG][0x004ceb70-0x004ceba9.HierListInsertAt](by-memory/0x004ceb70-0x004ceba9.HierListInsertAt.md) with `count = 1`.

Companion [UID:0002ID][0x004ce9b0-0x004ceafc.HierListInsertAfter](by-memory/0x004ce9b0-0x004ceafc.HierListInsertAfter.md) is now accepted as formal `void HierList::InsertAfter(const void *parentPayload, const void *referencePayload, const void *payload)` C++. The two cores share the optional parent/reference/payload source contract and lazy child-list creation, but their null-reference defaults and allocation helpers differ: this `InsertBefore` core defaults the reference index to `-1`, so `referenceIndex + 1` inserts at the selected root/child head, and uses `AllocateBufferMemory`; accepted `InsertAfter` defaults to `count - 1`, so `referenceIndex + 1` appends at the selected root/child tail, and uses `ZeroAllocateBufferMemory`.

The live tail frees the temporary row with `sub_516170` immediately before returning. That helper returns zero, so the observed binary does not preserve `List::InsertAt`'s destination pointer as a useful return value; callers use the list mutation side effect.

## Evidence

- IDA MCP `lookup_funcs` confirms `sub_4CE860` at `0x004ce860-0x004ce9ad`, size `0x14d`; the last instruction is `retn 0Ch` at `0x004ce9aa`, followed by `0x004ce9ad-0x004ce9b0` alignment padding.
- IDA MCP `disasm` confirms allocation through `sub_516050`, payload copy through `sub_516220`, child-list construction through `sub_4F4AA0` and `sub_4F3060`, and virtual insertion through slot `+0x14`.
- IDA MCP `decompile`/`disasm` of [UID:0002IK][0x004cec30-0x004ced0f.HierListFindNodeByData](by-memory/0x004cec30-0x004ced0f.HierListFindNodeByData.md) confirms the out index is the actual zero-based row index, which explains the `resolvedIndex + 1` insertion slot.
- IDA xrefs show direct calls from [UID:0002IA][0x004ce7e0-0x004ce839.HierListTailInsertWrapper](by-memory/0x004ce7e0-0x004ce839.HierListTailInsertWrapper.md) / accepted `InsertLast` at `0x004ce802` and `0x004ce82e`, from [UID:0002IB][0x004ce840-0x004ce854.HierListDefaultInsertWrapper](by-memory/0x004ce840-0x004ce854.HierListDefaultInsertWrapper.md) / accepted `InsertFirst` at `0x004ce84b`, and from [UID:00018W][0x004f0480-0x004f1bf6.Layer](by-memory/0x004f0480-0x004f1bf6.Layer.md) at `0x004f0a3b`.
- IDA MCP `callees` reports `sub_516030`, `sub_516050`, `sub_516220`, `sub_516170`, `sub_4CEC30`, `sub_4F4AA0`, and `sub_4F3060`, matching the memory-manager, lookup, child-list, and insert behavior.
- 2026-06-15 live IDA MCP recheck on session `a003_objectlist_dispatch_20260615` reconfirms `sub_4CE860` size `0x14d`, `0x004ce9ad` as non-function alignment before `sub_4CE9B0`, and the same xrefs at `0x004ce802`, `0x004ce82e`, `0x004ce84b`, and `0x004f0a3b`.
- Current `analyze_function(0x004ce860)` reports callees `sub_516030`, `sub_516050`, `sub_516220`, `sub_516170`, `sub_4CEC30`, `sub_4F4AA0`, and `sub_4F3060`, 18 basic blocks, and cyclomatic complexity 4.
- The current decompilation confirms non-zeroing allocation for the temporary row, copy of `this[1] - 11` payload bytes into row `+0x08`, lazy child-list allocation and vtable install, parent-node storage at child list `+0x14`, virtual insertion through slot `+0x14`, and final temporary-row free through `sub_516170`.
- `make_signature_for_range(0x004ce860,0x004ce9ad)` returns a unique wildcarded signature for the exact method span.
- 2026-06-26 B003 accepted [UID:0002IA][0x004ce7e0-0x004ce839.HierListTailInsertWrapper](by-memory/0x004ce7e0-0x004ce839.HierListTailInsertWrapper.md) as `HierList::InsertLast(parentPayload, payload)`, so this core's `0x004ce802` and `0x004ce82e` raw callers are now known source-level `InsertLast` delegate calls. Those calls pass either the resolved root/child tail payload as the reference payload or pass a null reference payload for an empty/missing child list.
- 2026-06-26 B015 accepted [UID:0002IB][0x004ce840-0x004ce854.HierListDefaultInsertWrapper](by-memory/0x004ce840-0x004ce854.HierListDefaultInsertWrapper.md) as `HierList::InsertFirst(parentPayload, payload)`, so this core's `0x004ce84b` raw caller is now a known source-level `InsertFirst` delegate call. That call passes a null sibling/reference payload, causing this core's no-reference path to use `-1 + 1` and insert at slot `0` in the selected root or parent child list.
- 2026-06-26 B007 accepted companion [UID:0002ID][0x004ce9b0-0x004ceafc.HierListInsertAfter](by-memory/0x004ce9b0-0x004ceafc.HierListInsertAfter.md) as formal `HierList::InsertAfter(parentPayload, referencePayload, payload)` C++ on active IDB session `80de0a67`. B007's MCP comparison id `632` confirmed this `InsertBefore` core has the same parent/reference/payload shape, while the accepted companion differs by defaulting null reference to `count - 1` instead of this page's `-1`, and by using `ZeroAllocateBufferMemory` instead of this page's `AllocateBufferMemory`.

## Confidence And Caveats

The class ownership, allocation/copy behavior, child-list creation, recursive lookup usage, and raw insertion slot are strong. B003 resolves the `0x004ce802` and `0x004ce82e` raw callers as source-level `InsertLast` delegations, B015 resolves the `0x004ce84b` raw caller as the source-level `InsertFirst` delegation that passes a null sibling/reference, B007 resolves the companion [UID:0002ID][0x004ce9b0-0x004ceafc.HierListInsertAfter](by-memory/0x004ce9b0-0x004ceafc.HierListInsertAfter.md) as accepted formal `InsertAfter` C++, and B010's 2026-07-01 implementation accepts this core's formal `InsertBefore` C++. Remaining uncertainty is limited to exact original local/parameter spelling, not source readiness.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 85 | The page documents the exact range and padding boundary, parent/child target-list logic, row allocation/copy behavior, lazy child-list construction, lookup-driven insertion index, caller/callee sets, return-register caveat, and unique method signature. |
| Confidence | 92 | Confidence is strong because live IDA confirms the method boundary, xrefs, callees, decompilation, owner route, pair relationship with `HierListInsertAfter`, root dispatch through `this->InsertAt`, lazy child-list construction, and final free-helper tail. Exact original parameter/local spellings remain inferred. |

## Changes

- 2026-07-01 B010 accepted empty-emitter family implementation:
  - Raised this page to `COMPLETION:90`, `CONFIDENCE:92` and inserted formal `void HierList::InsertBefore(const void *parentPayload, const void *referencePayload, const void *payload)` C++.
  - Current MCP decompile of `0x004ce860` reconfirmed non-zeroing temporary-row allocation through `AllocateBufferMemory(m_elementSize)`, explicit `node->childList = NULL`, payload copy of `m_elementSize - 11` bytes into `node->payload`, optional parent/reference lookup through `FindNodeByPayload`, lazy child-list construction with `new HierList(m_elementSize - 11, parentNode)`, insertion at `referenceIndex + 1`, and final temporary-row free.
  - The no-parent branch keeps the `referenceOwnerList` out-param local for source-shape symmetry with `InsertAfter`, but current decompile shows root/no-parent insertion dispatches through `this->InsertAt(referenceIndex + 1, 1, node)`, not through `referenceOwnerList`.
  - The parent branch dispatches through `childList->InsertAt(...)`. Null reference payload defaults to `-1`, so insertion occurs at the selected root/child head. This preserves the documented difference from accepted `InsertAfter`, whose null-reference default appends at `count - 1 + 1`.
  - Return-register behavior remains non-source: the live tail returns the free-helper result, but accepted base List mutation APIs are `void` and callers use the side effect.

- 2026-06-26 B007 accepted source-quality support update:
  - Added the companion relationship to accepted [UID:0002ID][0x004ce9b0-0x004ceafc.HierListInsertAfter](by-memory/0x004ce9b0-0x004ceafc.HierListInsertAfter.md) / `HierList::InsertAfter`.
  - Distinguished the null-reference behavior: this `InsertBefore` core defaults to `-1` and inserts at index `0`, while accepted `InsertAfter` defaults to `count - 1` and appends at the selected list tail.
  - Distinguished the allocator behavior: this core uses `AllocateBufferMemory`, while accepted `InsertAfter` uses `ZeroAllocateBufferMemory`.

- 2026-06-26 B015 accepted source-quality support update:
  - Resolved raw caller `0x004ce84b` as [UID:0002IB][0x004ce840-0x004ce854.HierListDefaultInsertWrapper](by-memory/0x004ce840-0x004ce854.HierListDefaultInsertWrapper.md) / `HierList::InsertFirst` delegating into this core with a null sibling/reference payload.
  - B010's 2026-07-01 implementation supersedes this former blank formal C++ state by inserting accepted `InsertBefore` source.

- 2026-06-26 B003 accepted source-quality support update:
  - Resolved raw callers `0x004ce802` and `0x004ce82e` as [UID:0002IA][0x004ce7e0-0x004ce839.HierListTailInsertWrapper](by-memory/0x004ce7e0-0x004ce839.HierListTailInsertWrapper.md) / `HierList::InsertLast` delegate calls into this core.
  - B010's 2026-07-01 implementation supersedes this former blank formal C++ state; B015 had already resolved the sibling-wrapper part of that uncertainty.

- 2026-06-17 B001 source-quality execution:
  - Raised completion from `85` to `86`; confidence remains `90`.
  - Evidence: B001 rechecked Layer caller naming evidence, recursive payload lookup, parent/sibling resolution, lazy child-list allocation, row copy length `elementSize - 0x0b`, and rejected `Layer`/`List` ownership alternatives.
  - B010's 2026-07-01 implementation supersedes the former class-wide overload/wrapper sequencing blocker with formal `InsertBefore` source.

### 2026-06-03 - Live IDA insertion-flow and return audit

- What existed before: the page was scored `68/84`, had no autogen parent, and described the method as returning the caller-visible payload pointer.
- Changed to: score raised to `78/88`, attached to [UID:000065][HierList](by-class/HierList.md), and the behavior now records exact allocation, lookup, child-list construction, insertion index, callers/callees, and return-register behavior.
- Summary/evidence: live IDA MCP decompilation/disassembly confirmed the exact range, `FindNodeByData` index semantics, virtual slot `+0x14` insertion, and final `sub_516170` call. The method mutates the list; the live return register is the free-helper zero rather than a preserved inserted-payload pointer.

### 2026-06-15 - A002 Goal 2 memory refresh

- What existed before: the page was scored `78/88` and had behavior-level IDA evidence but lacked current-session boundary/xref/decompilation/signature proof.
- Changed to: raised to `85/90`, with owner/emitter [UID:000065][HierList](by-class/HierList.md) unchanged and C++ still blank at that time.
- Summary/evidence: live IDA reconfirmed method size, padding boundary, four xrefs, callee set, allocation/copy/child-list/virtual-insert/free behavior, complexity, and a unique range signature. B010's 2026-07-01 implementation later resolved the source-level overload/API shape and parameter naming blocker enough for formal first-draft C++.
