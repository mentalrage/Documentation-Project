*** UID:0003SS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000G9 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000PB | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void WorldMapPane::UpdateHoverFromMouse()
{
    Point cursorPosition;
    g_pEventMan->GetCursorPosition(&cursorPosition);

    const int nodeIndex =
        HitTestNode(cursorPosition.x, cursorPosition.y);

    if ((nodeIndex == -1 || m_reachableNodes[nodeIndex]) &&
        m_hoveredNodeIndex != nodeIndex)
    {
        m_hoveredNodeIndex = nodeIndex;
        InvalidateRect(0);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***
*** Item Summary: Clear-capable WorldMapPane cursor-hover update that polls EventMan, hit-tests nodes, clears on a miss, accepts reachable hits, and invalidates only when m_hoveredNodeIndex changes. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005c5080-0x005c50e6 - WorldMapPaneUpdateHoverFromMouse

## Status

- Role: exact clear-capable private `WorldMapPane` cursor-hover update child.
- Source model: `void WorldMapPane::UpdateHoverFromMouse()` is the accepted high-confidence descriptive name and ABI. No PDB, original source, mangled private symbol, RTTI method name, or string proves the original spelling.
- IDA status: current modeled function `sub_5C5080` at `[0x005c5080,0x005c50e6)`, size `0x66`, 39 instructions, and five basic blocks. IDA naming/type/comment changes remain supervisor-owned Gate 2B work.
- Owner/emitter: [UID:0000G9][WorldMapPane](by-class/WorldMapPane.md), with source-file route through [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md).
- Reconstruction state: the exact CPP definition is populated above. The target H block intentionally remains blank because UID0000G9 already owns exactly one private `void UpdateHoverFromMouse();` declaration.

## Exact Range, ABI, And Frame

- Exact half-open body: `[0x005c5080,0x005c50e6)`, `0x66` / 102 bytes, 39 instructions, five blocks, SHA256 `FDD207906B32F452FB48C445080B1C85C75E28B51EBCEF87CDF0F6E73C3F9041`.
- Source ABI is private `void WorldMapPane::UpdateHoverFromMouse()` with no explicit arguments and one eight-byte `Point` local.
- Current stack frame is `position` at `0x4`, size 8, type `Point`; saved-register row at `0xc`, size 4; return-address row at `0x10`, size 4.
- Callers do not consume `EAX`. The invalidating path leaves the unrelated void `InvalidateRect` call result while no-change exits leave the hit index, so the current IDA `unsigned int` type cannot represent a coherent source return contract.

## Exact Behavior

1. Construct one local `Point cursorPosition`.
2. Poll the sole input source with `g_pEventMan->GetCursorPosition(&cursorPosition)` at call site `0x005c5093`; no `Event` pointer or payload coordinate is passed into this method.
3. Call [UID:0003SJ][0x005c37e0-0x005c38fd.WorldMapPaneHitTestNode](by-memory/0x005c37e0-0x005c38fd.WorldMapPaneHitTestNode.md) at `0x005c50a0` as `HitTestNode(cursorPosition.x, cursorPosition.y)`. `Point` stores `y` at `+0x00` and `x` at `+0x04`; machine push order is consistent with source `(x, y)` arguments.
4. Accept `nodeIndex == -1` directly. For a nonnegative result, accept only when `m_reachableNodes[nodeIndex]` is true; an unreachable hit exits without changing hover.
5. Compare the accepted value with `m_hoveredNodeIndex`. Only a different value is stored, followed by `Pane::InvalidateRect(0)`.

The short-circuit ordering is required: the `nodeIndex == -1` arm prevents vector indexing at `-1`. A miss clears hover to `-1`; an unreachable nonnegative hit preserves the prior hover; an equal accepted value produces no write or invalidation.

## Hit-Test And Field Contract

- `HitTestNode` converts current cursor x/y into map space using the active view and EPF center `512/384` or legacy center `320/240`, then scans `0x94`-byte `WorldMapNode` records with strict `+/-20` bounds. It returns the first matching index or `-1`.
- The `+0x274` access is `std::vector<bool> m_reachableNodes`. The Dinkumware word/bit lowering at `0x005c50ab-0x005c50c3` is normal source indexing, not a custom bitset helper or raw offset expression.
- The `+0x1e8` compare/store is `int m_hoveredNodeIndex`. It is distinct from signed `short m_currentNodeIndex` at `+0x1ec`, which belongs to route/reachability state.
- The only target side effects are the conditional hover write and following inherited `Pane::InvalidateRect(0)` through primary vtable slot `+0x20`. Otherwise there is no write, invalidation, allocation, packet, timer, cursor, global, or return side effect.

## Caller Inventory

- `0x005c40f5` in [UID:0003SM][0x005c40c0-0x005c4245.WorldMapPaneHandleMouseEvent](by-memory/0x005c40c0-0x005c4245.WorldMapPaneHandleMouseEvent.md): on `kEventLeftButtonDown` (`1`) when navigation is inactive, refresh hover before choosing drag setup for `-1` versus `StartTravelToNode(m_hoveredNodeIndex)`.
- `0x005c4187` in UID0003SM: on `kEventLeftButtonUp` (`3`) after an active drag, show the cursor, clear `m_draggingView`, then run this clear-capable update.
- These are the exact two direct target xrefs. No third caller, vtable entry, data pointer, string reference, or end-address xref exists.

## Strict Sibling Distinction

- [UID:0003ST][0x005c50f0-0x005c5156.WorldMapPaneUpdateHoverFromMouseStrict](by-memory/0x005c50f0-0x005c5156.WorldMapPaneUpdateHoverFromMouseStrict.md) has the same `0x66` size, 39-instruction/five-block source shape, cursor reader, hit test, fields, and conditional invalidation.
- Its one exact behavioral difference is the miss branch: UID0003ST accepts only reachable nonnegative hits, so misses and unreachable hits preserve hover. Its sole call at `0x005c4237` handles passive `kEventCursorMove` when not dragging and navigation is inactive.
- `UpdateHoverFromMouseStrict` is therefore the accepted descriptive sibling name; it is not a second name for this target and its formal body remains outside UID0003SS coverage.

## Boundary, CFG, And Padding Evidence

- CFG blocks are `[0x005c5080,0x005c50aa)`, `[0x005c50aa,0x005c50ca)`, `[0x005c50ca,0x005c50d2)`, `[0x005c50d2,0x005c50e1)`, and `[0x005c50e1,0x005c50e6)`.
- `[0x005c5075,0x005c5080)` is an eleven-byte `0xcc` alignment data item after `BuildReachabilityData`; zero xrefs; SHA256 `3682C0A62515050101266CC639BF81F5D8C5395F49E7DA93FCC22603018E0943`.
- `[0x005c50e6,0x005c50f0)` is a ten-byte `0xcc` alignment data item between this method and UID0003ST; zero xrefs; SHA256 `22E52BBA61DA8AD6732BE550205A10B89A1BB3B0EA491BE07DE9294F0EB6F045`.
- `[0x005c5156,0x005c5160)` is the same ten-byte `0xcc` alignment shape after UID0003ST and before `FindPredecessorNode`; zero xrefs and the same ten-byte hash.
- All three spans are outside functions, emit no authored source, and are documented by [UID:0000VN][-ignored](by-memory/-ignored.md).

## Source Placement And Formal Disposition

- Canonical owner remains UID0000G9; emitter remains UID0000PB; `Nested:0`, reconstructable true, and blank optional position remain unchanged.
- The one definition belongs in `NexusTK/map/WorldMapPane.cpp`, next to its established WorldMapPane method family. Event.cpp, MapPane.cpp, a generic input module, and a new hover helper file are rejected because this method reads/writes private WorldMapPane state and is called only by its mouse virtual.
- The formal CPP above uses typed `Point`, `EventMan`, `HitTestNode`, `std::vector<bool>`, the named hover field, and inherited invalidation. It contains no raw offsets, decompiler labels, duplicate sibling code, explicit return, or speculative guard.
- The formal H block stays empty. UID0000G9's complete class formal already emits `void UpdateHoverFromMouse();`; another declaration here would duplicate class-owned source. No target-specific `WorldMapPane.h` file is created.
- Third-party import is not applicable: this is first-party NexusTK class source, not a vetted staged embed.

## Negative Evidence And Rejected Alternatives

- No original/private source symbol survives, so the selected private name is descriptive rather than original-proof.
- No evidence supports Event payload input, reversed source argument order, clearing unreachable hits, preserving a miss in this target, unconditional invalidation, a custom bitset, raw offset source, an extra nonnegative/range guard, an explicit return value, target-owned strings/data/resources, a new source file, or duplicate H declaration.
- No caller reads `EAX`; no target string, resource, packet, timer, allocation, deallocation, cursor show/hide, navigation write, or global write exists.

## Historical Assumption Resolution

- Historical assumption (2026-06-16): B001 created this exact child but left final C++ blank because sampled decompilation did not resolve the source name, hover fields, input source, or sibling distinction.
- Superseding evidence: the current complete UID0000G9 class formal identifies both private declarations and exact fields; accepted `Point` and `EventMan` declarations close the input contract; full target/sibling disassembly and five-block CFG comparison isolate the single miss-branch difference; full UID0003SM decompilation explains the two target calls and one strict-sibling call.
- Result: every behavior, ABI, field, caller, boundary, owner, emitter, and formal-source blocker is closed. Only exact original private spelling and rebuilt-byte equivalence remain unavailable, which cap confidence without blocking source.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Exact body/boundaries/hash, ABI/frame, input and coordinate order, predicate, callers/callees, fields, sibling distinction, side effects, source route, formal CPP, blank-H proof, negative evidence, and history are complete. |
| Confidence | 94 | Current binary, caller, and accepted type evidence establish behavior and placement. The cap reflects absent original private-symbol/source proof and no rebuilt binary comparison. |

## Cross-References

- [UID:0001O4][0x005c2ac0-0x005c5c87.WorldMapPane](by-memory/0x005c2ac0-0x005c5c87.WorldMapPane.md)
- [UID:0003SJ][0x005c37e0-0x005c38fd.WorldMapPaneHitTestNode](by-memory/0x005c37e0-0x005c38fd.WorldMapPaneHitTestNode.md)
- [UID:0003ST][0x005c50f0-0x005c5156.WorldMapPaneUpdateHoverFromMouseStrict](by-memory/0x005c50f0-0x005c5156.WorldMapPaneUpdateHoverFromMouseStrict.md)
- [UID:0003SM][0x005c40c0-0x005c4245.WorldMapPaneHandleMouseEvent](by-memory/0x005c40c0-0x005c4245.WorldMapPaneHandleMouseEvent.md)
- [UID:0000G9][WorldMapPane](by-class/WorldMapPane.md)
- [UID:0000PB][WorldMapPane](by-file/WorldMapPane.md)

## Changes

- 2026-08-24 B009 UID0003SS implementation callback:
  - What existed before: `85/88`, exact child range and broad hover/hit-test role, blank formal CPP/H, and an active unresolved source-name/field blocker.
  - Changed to: `93/94` with the exact clear-capable member definition, intentionally blank child H, complete range/hash/CFG/frame/ABI, EventMan/Point input, HitTestNode contract, typed fields, predicate and side effects, two callers, strict-sibling distinction, three alignment spans, source route, rejected alternatives, and historicalized blocker.
  - Evidence: fresh accepted read-only IDA target/sibling/caller/callee/type/boundary evidence plus the existing complete UID0000G9 class and UID0000PB emitter contracts.
- 2026-06-16 B001 WorldMapPane split execution: created exact hover-update child and recorded the unresolved final-name issue after B001 checked caller/callee evidence.
