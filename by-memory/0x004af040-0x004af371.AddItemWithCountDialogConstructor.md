*** UID:0004BQ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000008 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
AddItemWithCountDialog::AddItemWithCountDialog(unsigned int exchangeSessionId,
                                               char inventorySlot)
    : DialogPane(L"", 9, 1)
{
    RectBounds bounds;
    wchar_t promptText[256];

    InitRectBounds(&bounds, 0, 0, 239, 283);
    AddControl(new EPFImageControlPane(L"DLGEXC3.EPF", 0, 1,
                                       &bounds, L"PAL01.PAL"));

    InitRectBounds(&bounds, 50, 149, 113, 173);
    AddControl(new ImageButtonControlPane(36, &bounds));

    InitRectBounds(&bounds, 129, 149, 192, 173);
    AddControl(new ImageButtonControlPane(22, &bounds));

    LocalInventorySlotRecord *inventoryItem =
        g_pUserPane->GetInventorySlotAddress(inventorySlot);
    swprintf_s(promptText, 256,
               g_pLanguageMan->GetLocalizedString(158),
               inventoryItem->displayName);

    InitRectBounds(&bounds, 28, 35, 211, 107);
    AddControl(new StaticTextControlPane(promptText, true, 128, 0,
                                         &bounds, false, false));

    InitRectBounds(&bounds, 28, 111, 211, 123);
    AddControl(new TextEditControlPane(L"", true, 128, 0,
                                       &bounds, false, false, false, false,
                                       1.0f, 0));

    SetBackgroundResource(L"DLGEXC3.EPF", NULL);
    SetHoverControl(4);
    SetFocusedControl(1);
    SetPendingControl(2);

    m_exchangeSessionId = exchangeSessionId;
    m_inventorySlot = inventorySlot;

    InitRectBounds(&bounds, 0, 0, 239, 283);
    OnCreate(&bounds, 0, NULL, g_mainUiLayerSlots.overlayPaneLayerContext);
    OnShow(NULL, g_pBackPane);
    SlideOpenVertical();
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Direct DialogPane-derived AddItemWithCountDialog constructor; builds DLGEXC3 controls and a localized inventory-slot count prompt, stores exchange-session/slot state, then creates, shows, and vertically slides the dialog. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004af040-0x004af371 AddItemWithCountDialog Constructor

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: constructor split from [UID:00033S][0x004af040-0x004af4b7.AddItemWithCountDialogCore](by-memory/0x004af040-0x004af4b7.AddItemWithCountDialogCore.md).
- Owner: [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md) AddItemWithCountDialog.
- Source file: [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md) ItemDialogs.

## Exact Range And Binary Evidence

- Exact function: `[0x004af040,0x004af371)`, size `0x331` (817 bytes), SHA-256 `C564ED1BB9A099D848C683B7D69A9E0A297C758CCF07B9A61D827AAD02F1908E`.
- The physical body has 234 instructions. Function analysis reports 25 basic blocks because nine external exception-cleanup nodes contribute another 37 instructions; the ordinary source path has one `retn 8` return.
- The predecessor ends at `0x004af031`; `[0x004af031,0x004af040)` is fifteen `0xcc` bytes. `[0x004af371,0x004af380)` is another fifteen-byte `0xcc` pad before the control-command method. The exact target prefix has one binary match, so no byte is shared with an inline duplicate or adjacent child.
- Constructor ABI is `__thiscall` with `unsigned int exchangeSessionId` and signed `char inventorySlot`. The low-byte parameter is sign-extended by its inventory accessor and remains a slot index, not an item id or item-type enum.

## Callers And Source Ownership

- Live caller `0x004ad3bf` in [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) allocates `0x274` bytes and passes the primary exchange session id plus packet byte `+2` as the signed local inventory slot.
- Retained no-route caller `0x004adaeb` in [UID:00014M][0x004ada90-0x004adb02.ExchangeDialogAddItemWithCountHelper](by-memory/0x004ada90-0x004adb02.ExchangeDialogAddItemWithCountHelper.md) performs the same allocation and call. Its lack of a current route does not change the target signature.
- The direct base call is `DialogPane(L"", 9, 1)`. There is no call to `AddItemDialog`; AddItemDialog is a source sibling. The class owner/emitter remains [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md), routed by [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md) to `NexusTK/ui/dialogs/ItemDialogs.cpp`.

## Control Construction And Data Flow

The authored construction order is exact:

1. Create an `EPFImageControlPane` with bounds `(0,0,239,283)`, `DLGEXC3.EPF`, frame `0`, mode `1`, and `PAL01.PAL`; the allocation size is `0x14c`.
2. Create image button 36 at `(50,149,113,173)` and insert it as selector 1, the action button.
3. Create image button 22 at `(129,149,192,173)` and insert it as selector 2, the cancel button.
4. Resolve `inventorySlot` through `g_pUserPane->GetInventorySlotAddress(char)`, load the record `displayName` at `+0x06`, look up LanguageMan string id 158, and format a 256-wide-character prompt with the secure formatter shape.
5. Create prompt static text at `(28,35,211,107)` with the exact boolean/style/color arguments represented in the formal block.
6. Create the count `TextEditControlPane` at `(28,111,211,123)` with the exact empty text, style, flags, scale, and mode arguments; it becomes selector 4.

The common tail sets `DLGEXC3.EPF` as the background, hover selector 4, focused selector 1, and pending selector 2. Only after all controls and selectors exist does it store `exchangeSessionId` at `+0x26c` and the signed slot byte at `+0x270`. It then restores full bounds, calls `OnCreate` on the overlay layer, calls `OnShow` against `g_pBackPane`, and opens vertically.

There is no direct constructor call to `UpdateActionButton`. `DialogPane::OnCreate` synchronously reaches generic refresh `0x0049fe20`, which dispatches primary virtual slot `+0x4c` to [UID:0004BS][0x004af480-0x004af4b7.AddItemWithCountDialogUpdateActionButton](by-memory/0x004af480-0x004af4b7.AddItemWithCountDialogUpdateActionButton.md) after controls, vptrs, and fields are valid.

## Layout, Vtables, And Compiler Exclusions

- `DialogPane` contributes `0x26c` bytes. The exchange-session dword is at `+0x26c`, the signed slot byte is at `+0x270`, and natural tail alignment occupies `+0x271..+0x273`; total object size is `0x274`. The source declaration needs no explicit padding member.
- Compiler stores vtable views `0x00619fec`, `0x0061a04c`, and `0x0061a07c` at `+0`, `+0xa0`, and `+0xa4`. These are compiler effects of the class declaration and multiple base views, not source assignments.
- Cleanup chunks `[0x005fe509,0x005fe57d)` cover base and five allocation-failure states. Vptr stores, stack cookies, delete-on-failure funclets, and EH state changes are excluded from handwritten C++.
- No instruction supports singleton publication, hard-coded English prompt text, `CreateCountInputControls`, item-type semantics, explicit vptr/padding code, or a target split.

## Score Rationale

`92/94` reflects complete range, byte hash, blocks, return, caller, ABI, base, control/resource, localization, layout, lifecycle, EH, padding, ownership, and formal-source coverage. Original lexical local/member spellings and exact access control are stripped, so confidence remains below final-audit certainty.

## Changes

- Replaced the historical shallow `AddItemDialog`/item-type/helper draft with the exact direct-DialogPane constructor, signed inventory-slot semantics, five-control construction, localized prompt, field timing, and lifecycle sequence. Live binary evidence, both callers, class layout, and the accepted UID0004BQ report contradict the old draft.
