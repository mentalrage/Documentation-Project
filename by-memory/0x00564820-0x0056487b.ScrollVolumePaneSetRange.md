*** UID:0004XR | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000CO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:40 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void ScrollVolumePane::SetRange(short range)
{
    if (range < 0) {
        range = 0;
    }
    if (range > 30000) {
        range = 30000;
    }

    if (m_range == range) {
        return;
    }

    if (m_value > range) {
        m_value = range;
        InvalidateRect(&m_visibleBounds);
    }

    m_range = range;
    InvalidateRect(&m_visibleBounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact signed ScrollVolumePane range setter clamps to 0..30000, returns before every side effect when unchanged, directly clamps the current value when needed, and performs ordered one/two invalidations through inherited m_visibleBounds. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00564820-0x0056487b ScrollVolumePane SetRange

## Status

- Exact range `[0x00564820,0x0056487b)`, size `0x5b`; direct class owner/emitter UID0000CO, position 40.

## Behavior

- Treats the input, `m_value` at `+0xfe`, and `m_range` at `+0x100` as signed 16-bit values.
- Clamps input below zero to zero and above 30000 to 30000.
- Returns before changing `m_value`, `m_range`, or invalidation state when the clamped input equals `m_range`.
- Only inside that changed-range path, if the new range is below the current value, stores the range directly into `m_value` and invalidates inherited `m_visibleBounds` once.
- Stores `m_range` and invalidates `m_visibleBounds` once more. The exact invalidation count is zero when unchanged, one for range-only change, and two when current value also clamps. The decompiler's integer return is incidental EAX state; callers do not consume a source return, so the human signature is `void`.

## Evidence And Boundaries

- Live IDA modeled `sub_564820` size `0x5b`; callers are `0x005403c7` and `0x005403de` in NewOptionPane setup.
- Signed branches and 16-bit loads/stores distinguish this state from unsigned style/type fields.
- Preceding `[0x00564814,0x00564820)` and following `[0x0056487b,0x00564880)` are all-`0xcc` alignment.
- UID000450 live MCP proof shows this body is byte-identical to `ScrollNewGroupPane::SetMaxRange` at `0x00560a10` and `ScrollCollectionPane::SetMaxRange` at `0x00561ec0`; all three 91-byte bodies hash to `C504A31BD1577B41537AE91425C5D2575483ED073207193C545B615792BE1F23`.

## Source-Quality And Negative Evidence

- `SetRange(short)` matches the homologous slider family and preserves exact clamp order.
- Do not widen the stored fields to `int`, return decompiler EAX, merge this method with SetValue, or place the equality test only around the final range store.
- Original private spelling is stripped, but role, type, owner, and source shape are resolved.
- Historical first-draft C++ allowed the value clamp/invalidation to run before testing whether `m_range` changed and used inherited `m_bounds`. The outer branch and current layout evidence supersede that shape with a full unchanged-range no-op and `m_visibleBounds`.

## Changes

- 2026-07-29 B002 UID000450 support correction:
  - Score and owner/emitter metadata remain `92/94`, UID0000CO, position `40`.
  - Corrected formal CPP and prose so the outer equality gate encloses both current-value and range updates, replaced `m_bounds` with `m_visibleBounds`, and added the exact clone hash.
  - Historicalized the narrower equality-gate assumption from the initial first draft rather than deleting it.
