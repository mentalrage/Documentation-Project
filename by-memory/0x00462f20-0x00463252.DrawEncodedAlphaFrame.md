*** UID:0000YO | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:95 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006K | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:12 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IntAlphaSurface::DrawEncodedAlphaFrame(
    const RectBounds *destinationRect,
    const EPFTileContext *sourceContext,
    const RectBounds *sourceOrigin,
    int alphaEndY,
    unsigned char alphaEnd,
    int alphaStartY,
    unsigned char alphaStart,
    AlphaMaskBlendMode blendMode)
{
    if (sourceContext->encodedMaskBytes == NULL) {
        return;
    }

    RectBounds clippedDestination;
    IntersectRects(&m_bounds, destinationRect, &clippedDestination);

    const unsigned char *run =
        reinterpret_cast<const unsigned char *>(sourceContext->encodedMaskBytes);
    int rowsToSkip =
        sourceOrigin->top - destinationRect->top
        + clippedDestination.top - sourceContext->bounds.top;

    while (rowsToSkip-- > 0) {
        while (*run++ != 0) {
        }
    }

    const int width =
        clippedDestination.right - clippedDestination.left;
    const int sourceX =
        clippedDestination.left + sourceOrigin->left - destinationRect->left;
    const float alphaStep =
        static_cast<float>(alphaEnd - alphaStart) /
        static_cast<float>(alphaEndY - alphaStartY);

    for (int y = clippedDestination.top;
         y < clippedDestination.bottom;
         ++y) {
        const unsigned char alpha = static_cast<unsigned char>(
            (y - alphaStartY) * alphaStep + alphaStart + 0.5f);
        unsigned char *destination =
            m_pixels + y * m_stride + clippedDestination.left;
        int x = sourceContext->bounds.left - sourceX;

        unsigned char token;
        while ((token = *run++) != 0) {
            const int count = static_cast<int>(token & 0x7f);
            if ((token & 0x80) != 0) {
                int first = x;
                int last = x + count;
                if (first < 0) {
                    first = 0;
                }
                if (last > width) {
                    last = width;
                }

                for (int column = first; column < last; ++column) {
                    if (blendMode == kAlphaMaskBlendCopy) {
                        destination[column] = alpha;
                    } else if (blendMode == kAlphaMaskBlendAdd) {
                        destination[column] = static_cast<unsigned char>(
                            destination[column] + alpha);
                    } else if (blendMode == kAlphaMaskBlendSubtract) {
                        destination[column] = static_cast<unsigned char>(
                            destination[column] - alpha);
                    }
                }
            }
            x += count;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
    void DrawEncodedAlphaFrame(const RectBounds *destinationRect,
                               const EPFTileContext *sourceContext,
                               const RectBounds *sourceOrigin,
                               int alphaEndY,
                               unsigned char alphaEnd,
                               int alphaStartY,
                               unsigned char alphaStart,
                               AlphaMaskBlendMode blendMode);
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready `IntAlphaSurface::DrawEncodedAlphaFrame` for EPFTileContext row-run masks; preserves null-stream no-op, ignored intersection result, zero row terminators, low-seven-bit run lengths, high-bit filled runs, vertical alpha interpolation, and copy/wrapping-add/wrapping-subtract modes across the closed nine-caller inventory. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00462f20-0x00463252 DrawEncodedAlphaFrame

## Accepted Current Source Resolution

- The exact source identity is `void IntAlphaSurface::DrawEncodedAlphaFrame`, emitted through [UID:00006K][IntAlphaSurface](by-class/IntAlphaSurface.md) at child position `12` in [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md).
- A null `encodedMaskBytes` stream is an immediate no-op. The observed call to `IntersectRects` is retained without an invented early return; this intentionally differs from ordinary clipped helpers whose machine control flow branches on the Boolean result.
- Encoded rows end at byte `0`. Every nonzero token uses `token & 0x7f` as the unsigned run length, `token & 0x80` to classify filled versus skipped runs, and advances the logical source X by the decoded length in either case.
- Filled spans are clipped to the destination row, use the observed vertical alpha interpolation, and preserve byte-wrapping copy/add/subtract semantics. No saturation, signed-magnitude interpretation, implicit fill for skipped runs, or alternate row delimiter is introduced.
- The caller inventory remains exactly nine sites: six modeled callers and the three recovered raw enclosing bodies `0x004d4f70`, `0x004d5f80`, and `0x004dcaa0`. All ignore the machine EAX value, establishing logical source return type `void` and superseding the former blank-source/return-contract no-code assumption.

## UID00041S Fixed-Slot Overlay Caller Contract

- [UID:00041S][0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot](by-memory/0x004defc0-0x004df1d4.ItemObjImageLibDrawItemImageIn43x43Slot.md) calls this member at `0x004df1bd` only when `overlayMask != 0`, after the primary item-sprite dispatch. The overlay object is the `IntAlphaSurface` receiver in `ECX`.
- The eight explicit arguments are, in source order: `destinationBounds`, `&tileContext`, `destinationBounds`, `destinationBounds->bottom`, `static_cast<unsigned char>(overlayStrength * 32.0f + 0.5f)`, `destinationBounds->top`, `0`, and `1`.
- This maps the accepted signature fields exactly: destination bounds, encoded `EPFTileContext`, source-origin/bounds pointer, alpha-ramp end Y, byte-wrapped end alpha, start Y, zero start alpha, and additive blend mode `1`. The target's use of the same mutable 43-by-43 destination bounds for both rectangle pointers is deliberate.
- The UID00041S caller ignores this helper's machine return. Together with all eight other callers, that establishes the logical source return type `void`; stale machine-register residue is not a source-level return contract.
- Owner/emitter UID00006K, nine-callsite inventory, recovered raw enclosing bodies, encoded-row behavior, and replace/add/subtract modes remain unchanged. This section adds exact caller mapping rather than moving ownership into ItemObjImageLib.

## Status

- Confidence: very strong for the exact boundary, behavior, `IntAlphaSurface` member ABI, source-facing signature, and closed nine-caller grouping; exact original private spellings and rebuilt-binary comparison remain confidence limits.
- Covered item: [UID:0000UH][DrawEncodedAlphaFrame_00462F20](by-item/DrawEncodedAlphaFrame_00462F20.md)
- Likely source file: [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md)
- Rebuild handling: source-authored `IntAlphaSurface` member routed to `NexusTK/render/AlphaMaskSurface.cpp`; exact formal CPP/H is present above.

## Function

| Range | Function | Notes |
| --- | --- | --- |
| `0x00462f20-0x00463252` | `void IntAlphaSurface::DrawEncodedAlphaFrame` | Walks encoded row runs from an `EPFTileContext` source object and applies a vertical alpha ramp into the destination alpha surface with replace/add/subtract modes. |

## Evidence Notes

- IDA MCP decompilation shows the function clips a destination rect against an alpha-surface bounds rect, walks zero-terminated per-row run data, and applies byte writes/adds/subtracts.
- IDA MCP live recheck on 2026-05-26 confirms function start `0x00462f20`, end-exclusive `0x00463252`, IDA name `sub_462F20`, and nine direct code xrefs.
- IDA MCP recheck on 2026-05-25 reported nine code xrefs total. Six resolved to modeled caller functions at `0x4db5a4`, `0x4dd7f6`, `0x4de289`, `0x4defa2`, `0x4df1bd`, and `0x4e31c4`; the remaining xrefs sat at `0x4d5ea6`, `0x4d6b16`, and `0x4dcca5`.
- 2026-05-26 IDA MCP caller/xref recheck maps the six modeled callers to [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md), [UID:0000O7][StaticObjImageLib](by-file/StaticObjImageLib.md), [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md), [UID:0000KH][ItemObjImageLib](by-file/ItemObjImageLib.md), and [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md) render methods. Each modeled call site is an optional overlay/alpha-mask pass after the primary sprite or composition draw.
- The xrefs at `0x004d5ea6`, `0x004d6b16`, and `0x004dcca5` were not IDA function starts or contained by modeled functions in that dated database. Whole-file recovery subsequently established their complete source-shaped enclosing bodies at `0x004d4f70`, `0x004d5f80`, and `0x004dcaa0`; they are closed caller evidence, not new owners for the helper.
- The function uses the same rect intersection helper family as `AlphaMaskSurface` and `DrawFrameWithBlendMode`.
- Use [UID:0001TP][AlphaMaskSurfaceLayout](by-type/by-struct/AlphaMaskSurfaceLayout.md) for the target alpha-surface fields this helper writes through.
- 2026-05-30 IDA MCP `lookup_funcs` reports `sub_462F20` size `0x332`; `0x00463251` is inside the function and `0x00463252` is not a function.
- 2026-05-30 IDA MCP `callers 0x00462f20` still reports the six modeled callers at `0x004db5a4`, `0x004dd7f6`, `0x004de289`, `0x004defa2`, `0x004df1bd`, and `0x004e31c4`.
- 2026-05-30 IDA MCP `xrefs_to 0x00462f20` still reports nine code xrefs total: the six modeled callers above plus raw, not-in-function xrefs at `0x004d5ea6`, `0x004d6b16`, and `0x004dcca5`.
- 2026-05-30 decompilation confirms the helper clips through `RectBounds::Intersect`, reads encoded row data from source object field `a3[9]`, uses target stride `this[8]`, writes target pixels from `this[5]`, and computes vertical alpha as `round((row - a7) * ((a6 - a8) / (a5 - a7)) + a8)`.
- B004 2026-06-20 EPFTileContext support sync identified source field `a3[9]` as the same tokenized row stream carried by `EPFTileContext::encodedMaskBytes` at `+0x24`. This confirms the encoded-mask stream is also consumed by alpha-mask overlay helpers, not only by Surface sprite/tile blitters.
- 2026-06-16 A002 live IDA MCP refresh against session `b001_maildialogs_0000ZO_20260616` reports `sub_462F20` at `0x00462f20`, size `0x332`, with the same nine code xrefs. Six xrefs are inside modeled functions at `0x004db3b0`, `0x004dd2c0`, `0x004de190`, `0x004dee50`, `0x004defc0`, and `0x004e2fe0`; three remain raw no-containing-function refs at `0x004d5ea6`, `0x004d6b16`, and `0x004dcca5`.
- The dated compact analysis listed the same six modeled image-library callers and callees `sub_4B7CC0`, `_memset`, and the security-cookie check. Its decompilation read encoded row pointers from `a3[9]`, used `this[5]`/`this[8]` as target pixel pointer/stride, skipped source rows by walking zero terminators, decoded each nonzero token as an unsigned byte with `token & 0x7f` length and `token & 0x80` filled-run classification, and branched on mode `0`, `1`, and `2` for replace/add/subtract behavior.
- 2026-06-16 A001 live IDA MCP refresh against session `b001_selflookpane_0001H7_20260616` reconfirmed `lookup_funcs 0x00462f20` as `sub_462F20` size `0x332`, `analyze_function` callers as the same six modeled image-library functions, xrefs as the same nine code refs, and the same compact callee set.
- A section-mapped PE scan of `NexusTK.exe` found no absolute VA/RVA pointer hits to `0x00462f20` and exactly the nine documented rel32 callsites. Byte reads around `0x00462f00` and `0x00463240` reconfirm the `0xcc`-bounded start after `DrawFrameWithBlendMode` and the `retn 20h` tail followed by alignment. Byte reads around `0x004d5ea6`, `0x004d6b16`, and `0x004dcca5` show inline overlay-strength setup before the calls, while IDA still reports those three sites as not contained by modeled functions.
- 2026-07-02 B003 UID00041R support sync resolves the caller-facing route for [UID:00041R][0x004dee50-0x004defba.ItemObjImageLibDrawItemImage](by-memory/0x004dee50-0x004defba.ItemObjImageLibDrawItemImage.md). The caller places the overlay `AlphaMaskSurface` in `ecx`, then passes `RectBounds *dstRect`, `EPFTileContext *sourceContext`, `RectBounds *sourceOrigin`, `alphaEndY`, `alphaEnd`, `alphaStartY`, `alphaStart`, and `blendMode`. UID00041R passes blend mode `1` add, `alphaStart` zero, row endpoints sourced from `dstRect[3]` and `dstRect[1]`, and `alphaEnd` computed as `(unsigned char)(overlayStrength * 32.0f + 0.5f)`.
- 2026-07-09 B001 MCP session `supervisor_nexustk_20260709` reported healthy NexusTK analysis and reconfirmed `sub_462F20` at `0x00462f20`, size `0x332`, eight stack arguments after `this`, `retn 20h`, and modeled prototype `int __thiscall(int *this, int *, _DWORD *, _DWORD *, int, unsigned __int8, int, unsigned __int8, int)`. Decompilation confirms destination bounds/pixels/stride at AlphaMaskSurface offsets `+0x04/+0x14/+0x20`, encoded row bytes at `sourceContext+0x24`, clipping, alpha interpolation, and modes `0` replace / `1` add / `2` subtract.
- The same B001 pass reconfirmed all nine direct callsites. The three sites at `0x004d5ea6`, `0x004d6b16`, and `0x004dcca5` still have no containing IDA function, but their instruction windows prepare the same overlay-alpha arguments and set `ECX` to an AlphaMaskSurface-like receiver. They are caller-boundary cleanup evidence for image-library pages, not owner, source-placement, range-split, or target-confidence blockers for UID0000YO.

## Historical Pre-Callback Reconstruction Notes

The retained sketch and no-code argument in this section are historical assumptions. The accepted current resolution above supersedes the `AlphaMaskSurface` identity, signed-token wording, `int` return, and blank-source conclusion while preserving the caller and payload evidence that led to the corrected `IntAlphaSurface` source.

The historical pass kept this as a shared AlphaMaskSurface member blitter rather than copying it into each image library caller. The caller set proved that several image-library render paths consume the helper, while the implementation operates entirely on the alpha-surface layout and the encoded row stream exposed by `EPFTileContext::encodedMaskBytes` at `+0x24`. That existing type remains sufficient for the source-facing signature; no new encoded-frame payload type is introduced.

Historical source-facing member signature (rejected by the closed return/class analysis):

```cpp
int AlphaMaskSurface::DrawEncodedAlphaFrame(
    RectBounds *dstRect,
    EPFTileContext *sourceContext,
    RectBounds *sourceOrigin,
    int alphaEndY,
    unsigned char alphaEnd,
    int alphaStartY,
    unsigned char alphaStart,
    int blendMode);
```

For UID00041R, `sourceContext` is the item `EPFTileContext` carrying the encoded row stream through `encodedMaskBytes` / `+0x24`, `sourceOrigin` is the selected source rectangle/origin, `alphaEnd` is `(unsigned char)(overlayStrength * 32.0f + 0.5f)`, `alphaStart` is zero, and `blendMode` is `1` for additive overlay. UID00041S uses the same encoded context and additive ramp but passes its mutable centered `destinationBounds` as both destination and source-origin rectangle, with bottom/top as the ramp endpoints. The older generic `EncodedFrame`/`Point` sketch is rejected because existing EPFTileContext support supplies the required field without inventing another payload type. The accepted behavior is: skip encoded source rows above the clipped start, walk zero-terminated unsigned tokens per row, draw only high-bit-set runs, clip each run horizontally, and use mode `0` replace, mode `1` add, and mode `2` subtract.

Historical rejected conclusion: formal `RECONSTRUCTION_CPP` was left blank because variant machine-register residue was mistaken for a source return contract. Whole-file caller recovery proved that all nine callers ignore EAX, so the accepted logical source return is `void` and the exact formal CPP/H now appears above.

## Historical Pre-Callback Score Rationale

- Completion `88`: current MCP resolves the exact range, member ABI, eight stack arguments, caller set, callee set, AlphaMaskSurface fields, `EPFTileContext::encodedMaskBytes` access, encoded-row walker, vertical alpha ramp, three blend modes, source placement, raw-xref classification, and the no-code decision. Completion stays below final-source quality because the formal body is intentionally blank and the exact original project-native payload name is not proven.
- Confidence `91`: current function records, stack frame, xrefs, decompilation, bytes, PE scanning, and support docs agree on the AlphaMaskSurface member route and source-facing signature. Confidence remains below final-audit level because three caller boundaries are still not modeled and exact original source spelling remains inferred.

Current callback-applied score is `93/95`: all three raw enclosing bodies and all nine ignored-EAX caller contracts are closed, exact void CPP/H is emitted, and the remaining cap is limited to inferred private spellings and absent rebuilt-binary comparison.

## Cross-References

- [UID:0000UH][DrawEncodedAlphaFrame_00462F20](by-item/DrawEncodedAlphaFrame_00462F20.md)
- [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md)
- [UID:0001TP][AlphaMaskSurfaceLayout](by-type/by-struct/AlphaMaskSurfaceLayout.md)
- [UID:0000LJ][MonsterImageLib](by-file/MonsterImageLib.md)
- [UID:0000O7][StaticObjImageLib](by-file/StaticObjImageLib.md)
- [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md)
- [UID:0000KH][ItemObjImageLib](by-file/ItemObjImageLib.md)
- [UID:00041R][0x004dee50-0x004defba.ItemObjImageLibDrawItemImage](by-memory/0x004dee50-0x004defba.ItemObjImageLibDrawItemImage.md)
- [UID:0000LR][NewHumanImageLib](by-file/NewHumanImageLib.md)

## Changes

- 2026-07-09 B001 UID0000YO source-quality implementation callback:
  - Before: `86/90`, with correct owner/emitter routing but stale payload/raw-xref blocker wording and no explicit return-contract no-code proof.
  - Changed to: `88/91`; owner/emitter remain [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md), `RECONSTRUCTABLE:TRUE` is preserved, and the formal C++ body remains blank.
  - Evidence: accepted MCP session `supervisor_nexustk_20260709` resolves the `thiscall` ABI, eight explicit arguments, half-open range, nine callers, `EPFTileContext` encoded stream at `+0x24`, three raw xrefs as caller-boundary evidence, and the variant/stale return-register behavior that requires the target-specific no-code disposition.

- 2026-06-16 A001 file/item completion refresh:
  - Before: `85/89`.
  - Changed to: `86/90`; owner/emitter remain [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md); final C++ remains blank.
  - Evidence: live IDA MCP `lookup_funcs`, `xrefs_to`, `analyze_function`, and `get_bytes` reconfirmed the exact range, six modeled callers, three raw no-function callsites, callees, decoding behavior, and boundary bytes. A section-mapped PE scan found no VA/RVA pointer refs and exactly the nine documented rel32 callsites, leaving encoded-frame type names and raw caller function-boundary recovery as the remaining real blockers.

- 2026-07-02 B003 UID00041R caller-route sync:
  - Score unchanged. Replaced the active generic `EncodedFrame`/`Point` sketch for UID00041R with the accepted `AlphaMaskSurface::DrawEncodedAlphaFrame` thiscall route while preserving broader frame-source/raw-xref caveats.
  - Evidence: UID00041R calls this helper with overlay mask in `ecx`, `RectBounds *dstRect`, `EPFTileContext *sourceContext`, `RectBounds *sourceOrigin`, alpha ramp endpoints derived from `dstRect` and `overlayStrength * 32.0f + 0.5f`, and blend mode `1`. Broader frame-source names and three raw xrefs remain open without blocking the UID00041R support route.

- 2026-06-16 A002 file/item completion refresh:
  - Before: `82/88`.
  - Changed to: `85/89`; owner/emitter remain [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md); final C++ remains blank.
  - Evidence: live IDA reconfirmed the exact `0x332` function size, nine xrefs, six modeled image-library callers, three still-raw callsites, compact callee set, signed-run token decoding, vertical alpha-ramp math, and replace/add/subtract write modes. The remaining blockers are real source-quality issues, not missing review: exact encoded-frame type names and the three raw caller boundaries cannot be safely resolved from current IDA function ownership.

- Before: this page and the function table ended at `0x00463251`.
- After: this page and the function table end at `0x00463252`.
- Summary/evidence: this aligns the page header/table with the existing IDA evidence note: IDA MCP reports `sub_462F20` as `0x00462f20-0x00463252`, and byte checks show `0x00463251` is the final immediate byte of `retn 20h`.
- 2026-05-30: Raised completion/confidence from `50/65` to `82/88`. Added current IDA MCP boundary, caller/xref, and decompilation evidence plus a source-level reconstruction sketch; full frame-source type and three raw image-library xrefs remain open.
- 2026-06-05 A002 coverage classification:
  - Marked `RECONSTRUCTABLE:TRUE` and attached to [UID:0000HF][AlphaMaskSurface](by-file/AlphaMaskSurface.md).
  - Reasoning: the page is `82/88`, the direct parent is `82/89`, and the existing evidence identifies this as a shared source-owned alpha-mask blitter rather than compiler/runtime glue.
