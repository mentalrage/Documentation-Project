*** UID:0001D9 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000049 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Non-emitting EffectObjectPane split/container for the exact constructor, destructor child, StartIdleTimer child, bounds, frame-bounds, update/timer, render, and successor IsLooping ranges; current MCP session 60724697 proves boundaries, xrefs, callees, padding, field flow, generated state, and no-monolithic-parent-C++ disposition. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005387b0-0x00538baa EffectObjectPane Core

## Status

- Confidence: strong for class ownership, function boundaries, xrefs, callees, padding, and split/container disposition; medium-high for final child method names/signatures.
- Disposition: non-emitting split/container page for exact `EffectObjectPane` methods and adjacent child artifacts.
- Current generated owner: `EffectObjectPane`
- Historical generated lead, not current evidence: older Wave3/simroot_v2 output staged this class as `source-3/simroot_v2/class_EffectObjectPane.cpp`; current ownership/source route is based on IDA MCP and by-* evidence.
- Related file doc: [UID:0000IZ][Effects](by-file/Effects.md)
- Rebuild handling: parent/index-only. Exact source output belongs to exact child pages, not this aggregate.
- Reconstruction note: formal output is blank and `RECONSTRUCTABLE` is `FALSE` because this range is not one source method. Current MCP session `60724697` proves the range contains multiple independent functions and padding. Existing exact child pages own the ordinary destructor and StartIdleTimer bodies; future exact child pages should own constructor, bounds, frame-bounds, update/timer, and render bodies before any additional source is emitted.

## UID0003TF Constructor And UpdatePosition Synchronization - 2026-07-15

- This aggregate remains `88/91`, owner UID000049, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal C++, and `Nested:-4`. UID0003TF completion changes no split/container metadata and does not authorize a monolithic EffectObjectPane body.
- Source-ready [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md) is the sole constructor caller at `0x0050eacf` and sole StartIdleTimer caller at `0x0050eb14`. It uses ordinary `new EffectObjectPane`; pool allocation, constructor-failure cleanup, vptr stores, security cookie, EH, and adjusted-this mechanics are compiler/class lowering.
- The constructor receives a borrowed `LivingObjectPane *target`, map row/column, by-value `EffectInfo`, `sequenceIndex`, and `renderAfterLivingObjects`. All four audited MapPane create callsites, UID0003TF frame offsets, and the constructor's ObjectPane forwarding fix that order. It stores target `+0x128`; selected descriptor header `+0x12c..+0x143`; current frame `+0x144=-1`; effect id `+0x148`; `m_looping` `+0x14c`; render-after selector `+0x14d`; and inferred `m_timerExtensionLocked +0x14e=false`. The former `compact flag` interpretation for `+0x14d` is superseded by complementary MapPane render passes.
- Vtable data at `0x0062066c` points to exact child range `[0x005388c0,0x00538956)`. Its source-facing role is `EffectObjectPane::UpdatePosition`: active-frame sprite bounds or zero bounds are offset by attached screen position and applied through the inherited virtual bounds setter. UID0003TF calls it for non-null targets, then separately invokes inherited `GetBounds` and invalidation. The prior `GetEffectBounds` spelling is historical and incorrectly conflated update with the later bounds read.
- Future child inventory is therefore constructor, `UpdatePosition`, `GetFrameBounds`, `UpdateEffectAnimation`, and `RenderEffect`, alongside already exact destructor/StartIdleTimer/IsLooping artifacts. This naming correction does not create children in the current callback.
- Negative evidence remains unchanged: no monolithic parent source, MapPane ownership transfer, EffectObjImageLib/TimerMgr/AttachedObjectPane ownership transfer, compiler scaffolding body, raw-label source, or generated/simroot authority.

## UID0003TE Coordinate Consumer Synchronization - 2026-07-15

- Source-ready [UID:0003TE][0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch](by-memory/0x0050e850-0x0050ea30.MapPaneCoordinateEffectDescriptorDispatch.md) reuses EffectObjectPane instances only when their selected-row position column, copied effect id at `+0x148`, and [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) state match. Its two direct `IsLooping` calls remain the exact `0x0050e8fa` and `0x0050e9a0` xrefs recorded by the current aggregate evidence.
- A missing match calls UID0003TF with `target == NULL`, row before column, sequence `0` or `1`, and the caller's timer/render inputs. The same constructor at `0x005387b0` owns the resulting target-null state; UID0003TE does not introduce another constructor, layout, pool path, or EffectObjectPane source owner.
- This bounded consumer update preserves the aggregate's `88/91`, false reconstructability, blank emitter/formal, `Nested:-4`, complete range/padding inventory, and future exact-child disposition.

## UID0003TG Timer-Extension Lock Synchronization - 2026-07-15

- Source-ready [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md) resolves the constructor-zeroed byte at complete offset `+0x14e` as inferred `m_timerExtensionLocked`. The target tests it only after null, ordinary-delete, zero, and event-2-cancel paths; true returns and false permits the due-tick query/replacement path.
- Bounded MapPane/EffectObjectPane searches found no observed nonzero direct writer. This is preserved as negative evidence, not proof that no writer exists outside the searched family. Predicate polarity rejects `m_allowTimerExtension`; independent consumers reject aliasing with `m_looping +0x14c` or `m_renderAfterLivingObjects +0x14d`; controlled event-2 side effects reject padding or a generic object-state byte.
- UID0003TG converts the complete object to its inherited TimerHandler view with source-level typed conversion, queries event 2, preserves zero-on-miss and the signed unscaled remaining-time comparison, then cancels before scheduling `1000 * timerAction`, payload `0,0`. Raw `+0xa4` adjustment and scalar deleting-destructor flags are compiler lowering, not aggregate source.
- This synchronization preserves `88/91`, UID000049 ownership, `RECONSTRUCTABLE:FALSE`, blank emitter/position/formal, `Nested:-4`, all child ranges, padding, vtable/xref evidence, and future-child policy. No monolithic parent C++ is authorized.

## Covered Ranges

| Range | Method | Notes |
| --- | --- | --- |
| `0x005387b0-0x0053887c` | future exact child `EffectObjectPaneConstructor` | Calls `AttachedObjectPane` construction at `0x005372d0`, stores borrowed LivingObjectPane target, copies the selected `EffectInfo` frame-sequence header, sets three EffectObjectPane vtable pointers, initializes `+0x144` current frame to `-1`, stores effect id at `+0x148`, derives `m_looping` at `+0x14c`, stores `m_renderAfterLivingObjects` at `+0x14d`, and initializes inferred `m_timerExtensionLocked` at `+0x14e` to false. |
| `0x00538880-0x0053889f` | [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md) | Ordinary `EffectObjectPane::~EffectObjectPane()` body; resets primary, inherited EventHandler/pane-handler `+0xa0`, and inherited TimerHandler/event `+0xa4` vtable views before tail-calling attached-object cleanup at `0x00538100`; no direct xrefs or raw VA/RVA entry pointers found. |
| `0x005388a0-0x005388b4` | [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) | Source-ready `StartIdleTimer` child; schedules timer/event id `1` with zero delay/payload through `TimerHandler::ScheduleTimer(1, 0, 0, 0)` on the inherited `+0xa4` TimerHandler adjusted view, called immediately after construction from MapPane object setup. |
| `0x005388c0-0x00538956` | future exact child `EffectObjectPaneUpdatePosition` | Resolves bounds for the active effect frame, falls back to zero bounds for invalid current-frame state, offsets by attached screen position, and calls the inherited virtual bounds setter. Vtable and UID0003TF call order supersede the old `GetEffectBounds` name. |
| `0x00538960-0x005389c9` | future exact child `EffectObjectPaneGetFrameBounds` | Copies active frame bounds or zero bounds into caller-provided rectangles. |
| `0x005389d0-0x00538aef` | future exact child `EffectObjectPaneUpdateEffectAnimation` | TimerHandler-adjusted event/update body; advances animation, handles loop/end removal, updates lighting or active map effect state, refreshes parent visual state, and schedules the next timer event. |
| `0x00538af0-0x00538baa` | future exact child `EffectObjectPaneRenderEffect` | Renders the active effect sprite frame through [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md), optionally with render-context overlay state. |

The scalar deleting destructor is nearby in source ownership but not in this contiguous memory span: [UID:000382][0x0053d100-0x0053d197.EffectObjectPaneScalarDeletingDestructor](by-memory/0x0053d100-0x0053d197.EffectObjectPaneScalarDeletingDestructor.md). The successor loop accessor [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) follows `0x00538baa-0x00538bb0` padding and remains an exact child outside this parent range.

## 2026-07-04 B010 Split/Container Callback Evidence

B010 current MCP session `60724697` supersedes the older aggregate-emitter wording and proves the parent should be a non-emitting split/container page:

- Command `102` `server_health`: database `60724697`, `status:ok`, IDB `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, module `NexusTK.exe`, imagebase `0x400000`, `auto_analysis_ready:true`, `hexrays_ready:true`, `strings_cache_ready:true`, and `strings_cache_size:2067`.
- Command `104` `lookup_funcs`: constructor `sub_5387B0` at `0x005387b0`, size `0xcc`; ordinary destructor child `sub_538880` at `0x00538880`, size `0x1f`; StartIdleTimer child `sub_5388A0` at `0x005388a0`, size `0x14`; bounds helper `sub_5388C0` at `0x005388c0`, size `0x96`; frame-bounds helper `sub_538960` at `0x00538960`, size `0x69`; update/timer handler `sub_5389D0` at `0x005389d0`, size `0x11f`; render helper `sub_538AF0` at `0x00538af0`, size `0xba`; successor loop accessor `sub_538BB0` at `0x00538bb0`, size `0x7`.
- Command `105` `xrefs_to`: constructor has one code xref at `0x0050eacf` in [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md); ordinary destructor has zero direct xrefs; StartIdleTimer has one code xref at `0x0050eb14` in the same MapPane creation helper; bounds/update/render functions have vtable data xrefs at `0x0062066c`, `0x0062064c`, `0x006206a8`, and `0x00620658`; successor IsLooping has six code xrefs at `0x0050e397`, `0x0050e42d`, `0x0050e8fa`, `0x0050e9a0`, `0x005a2f48`, and `0x005a2fa9`.
- Command `106` `callees`: constructor calls `0x005372d0`; ordinary destructor has no modeled callee in the queried form; StartIdleTimer calls `0x005975e0`; `UpdatePosition` at `0x005388c0` calls `0x004de2a0`, `0x004b7c50`, `0x005386c0`, `0x004b7e10`, and security-cookie check; `GetFrameBounds` calls `0x004de2a0` and `0x004b7c50`; `UpdateEffectAnimation` calls `0x0050a940`, `0x0053c980`, and `0x00597910`; `RenderEffect` calls `0x0050ab40` and [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md).
- Command `107` `get_bytes`: `0x0053887c` size `68` shows four `0xcc` bytes before the ordinary destructor, one `0xcc` byte between ordinary destructor and StartIdleTimer, the exact StartIdleTimer body bytes, and twelve `0xcc` bytes before the next bounds function. `0x00538baa` size `6` is six `0xcc` bytes before the successor IsLooping accessor.
- Command `108` constructor decompile: delegates construction through `0x005372d0`, stores the target object at complete offset `+0x128`, writes EffectObjectPane vtables at complete offsets `+0x00`, `+0xa0`, and `+0xa4`, clears byte `+0x134`, stores render-after/caller flag `+0x14d`, copies `EffectInfo +0x00` to complete `+0x148`, selects either the `EffectInfo +0x04` or `+0x1c` sequence header into complete `+0x12c..+0x143`, initializes `+0x144` to `-1`, sets loop byte `+0x14c` when selected range fields `+0x13c/+0x140` are nonnegative, and initializes inferred `m_timerExtensionLocked +0x14e` to false.
- Command `111` `UpdatePosition` decompile: reads current frame at complete `+0x144`, frame count at `+0x138`, and frame record table at `+0x12c`; valid frames call `GetSpriteBounds`/`0x004de2a0` with the four `EffectFrameRecord` fields; invalid frames zero a rect through `0x004b7c50`; the result is offset by attached position from `0x005386c0` through `0x004b7e10`, then applied through virtual slot `+44`.
- Command `112` `GetFrameBounds` decompile: uses the same `+0x144/+0x138/+0x12c` current-frame guard and frame-record stride, writes zero bounds for invalid frame state, otherwise calls `0x004de2a0`, then copies the result into the second output rectangle.
- Command `109` `UpdateEffectAnimation` decompile: receiver is the inherited `+0xa4` TimerHandler adjusted view, so adjusted offsets translate back to complete-object fields. Event command `1` advances current frame, wraps to loop start when current frame equals loop end and `m_looping` is set, removes the owner pane on sequence completion, applies frame lighting through [UID:000075][LightingObjectPane](by-class/LightingObjectPane.md) or active MapPane effect update, refreshes virtual slots `+112` and `+32`, and reschedules timer event `1` through `0x00597910`. Event command `2` clears the complete-object `m_looping` byte; event command `0` takes the removal path.
- Command `110` `RenderEffect` decompile: checks current frame `+0x144` against frame count `+0x138`, indexes the `EffectFrameRecord` table at `+0x12c`, forwards all four record fields to [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md), and either passes no overlay state or passes render-context values from the second argument at offsets `+0x3dc/+0x3e0` plus EffectObjectPane float `+0x11c`.
- Current generated-output check: `auto-generated/NexusTK/render/Effects.cpp` was generated by validator command `000000006160` at `2026-07-04T05:57:51-04:00`; it contains the class-level UID000049 withheld-source comment and no UID0001D9-specific body or empty marker. This page therefore should not try to repair generated output directly.

Accepted split plan: create no child pages in this callback, but document that future exact child pages should own `EffectObjectPaneConstructor`, `EffectObjectPaneUpdatePosition`, `EffectObjectPaneGetFrameBounds`, `EffectObjectPaneUpdateEffectAnimation`, and `EffectObjectPaneRenderEffect`. Existing [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md), [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md), [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md), [UID:000351][0x006205f8-0x006206ac.EffectObjectPaneVtableData](by-memory/0x006205f8-0x006206ac.EffectObjectPaneVtableData.md), [UID:0002WO][0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage](by-memory/0x0069b90c-0x0069b934.EffectObjectPanePoolStaticStorage.md), and [UID:000382][0x0053d100-0x0053d197.EffectObjectPaneScalarDeletingDestructor](by-memory/0x0053d100-0x0053d197.EffectObjectPaneScalarDeletingDestructor.md) remain separate exact children/artifacts.

Negative evidence preserved: monolithic parent C++ is rejected because this range spans multiple functions and padding; MapPane is a caller/creator, not the direct owner; EffectObjImageLib owns asset/frame rendering dependencies, not the pane methods; TimerMgr/TimerHandler owns generic scheduling infrastructure, not this override body; AttachedObjectPane/ObjectPane are base cleanup and virtual-slot dependencies, not the derived class owner; and generated/simroot/decompiler names or raw labels such as `sub_*`, `unk_*`, and `dword_*` must not be used as source-facing names.

## IDA MCP Notes

IDA MCP confirmed the aggregate function list on 2026-06-01:

- `0x005387b0-0x0053887c`
- `0x00538880-0x0053889f`
- `0x005388a0-0x005388b4`
- `0x005388c0-0x00538956`
- `0x00538960-0x005389c9`
- `0x005389d0-0x00538aef`
- `0x00538af0-0x00538baa`
- destructor `0x0053d100-0x0053d197`

Earlier coverage rows were one byte short at the observed end address. Use IDA exclusive end `0x00538baa` for this contiguous cluster. Raw byte review confirms `0x005387a3-0x005387b0`, `0x0053887c-0x00538880`, `0x0053889f-0x005388a0`, `0x005388b4-0x005388c0`, and `0x00538baa-0x00538bb0` are alignment gaps around the modeled functions.

- Constructor decompilation calls `0x005372d0` with object type derived from the effect kind, stores the target object at `+0x128`, writes EffectObjectPane vtables at `+0x00`, `+0xa0`, and `+0xa4`, copies one of two frame descriptor blocks from the constructor data, initializes current-frame state to `-1`, stores the copied effect descriptor id at `+0x148`, sets loop/reuse byte `+0x14c` from the selected range validity at `+0x13c/+0x140`, stores caller flag `+0x14d`, and initializes inferred `m_timerExtensionLocked +0x14e` to false.
- [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md) passes the selected sequence index and its by-value `EffectInfo` argument into this constructor. The constructor copies `EffectInfo +0x00` to `+0x148`, sets `m_looping` at `+0x14c` from selected sequence range validity, stores `m_renderAfterLivingObjects` at `+0x14d`, and initializes `m_timerExtensionLocked` at `+0x14e` to false. This is the layout contract consumed by UID000232, UID0003TE, and the UID0003TG existing-pane action helper.
- `UpdatePosition` and `GetFrameBounds` read current frame index at `+0x144`, frame count at `+0x138`, and frame descriptor table at `+0x12c`; valid frames call `0x004de2a0`, while invalid frames zero the rect through `0x004b7c50`.
- `UpdatePosition` calls the attached-position helper at `0x005386c0`, offsets the frame rectangle through `0x004b7e10`, then calls the virtual bounds setter at vtable slot `+44`.
- `UpdateEffectAnimation` responds to timer/event command `1` by advancing `+0x140`, wrapping to loop start at `+0x130` when `+0x13c` and `+0x148` allow it, deleting the owner pane on sequence completion, and reading the current effect-frame record's signed light-image index at record offset `+0x0c`. When that value is not `-1`, the call at `0x00538a90` invokes [UID:0001DI][0x0053c980-0x0053c9b5.LightingObjectPaneSetLightImageIndex](by-memory/0x0053c980-0x0053c9b5.LightingObjectPaneSetLightImageIndex.md) on the optional [UID:000075][LightingObjectPane](by-class/LightingObjectPane.md). The method then refreshes the parent visual through virtual slots `+112` and `+32` and schedules sound/timing through `0x00597910`.
- `RenderEffect` checks `+0x144` against `+0x138`, optionally uses the motion/overlay context when `+0x11c` is nonzero and `0x0050ab40` allows it, then calls [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md) at `0x00538ba0` with `g_pEffectObjImageLib` at `0x0069b44c`. B002 2026-06-17 reanalysis identifies the caller-passed `MapPane/renderContext + 0x3e0` value as an overlay-mask gate, while the alpha draw target remains the second `RenderEffectFrame` parameter.
- IDA `xrefs_to` shows the constructor has a direct caller at `0x0050eacf` inside MapPane object setup; the virtual methods are reached through vtable data xrefs at `0x0062064c`, `0x00620658`, `0x0062066c`, and `0x006206a8`.

## 2026-06-15 Current Verification

- Live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirms the exact modeled function starts and endpoints in this aggregate: constructor `0x005387b0-0x0053887c`, non-deleting destructor `0x00538880-0x0053889f`, timer helper `0x005388a0-0x005388b4`, `UpdatePosition` at `0x005388c0`, frame-bounds method at `0x00538960`, timer update method at `0x005389d0`, render method `0x00538af0-0x00538baa`, and successor `sub_538BB0` at `0x00538bb0`.
- Live xrefs reconfirm constructor caller `0x0050eacf` and timer-helper caller `0x0050eb14` inside `sub_50EA30`, no direct xrefs to the non-deleting destructor, and virtual-method vtable/data refs at `0x0062064c`, `0x00620658`, `0x0062066c`, and `0x006206a8`.
- Live `analyze_function` samples reconfirm constructor delegation to `sub_5372D0`, EffectObjectPane vtable stores, effect/timeline data copies at `+0x12c` and `+0x13c`, current-frame initialization, update callees `sub_53C980`, `sub_597910`, and `sub_50A940`, and render callees `sub_50AB40` and [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md).
- Live xrefs to [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md) address `0x0069b44c` include this aggregate's bounds/render users at `0x005388fa`, `0x00538982`, and `0x00538b97`, tying runtime EffectObjectPane rendering to the effect object image library.
- 2026-06-15 `make_signature_for_range 0x005387b0-0x00538baa` with operand wildcards returns a unique full-range signature.
- Historical Wave2 method-analysis is retained only as superseded discovery context: it listed `GetEffectBounds` for `0x005388c0`, but current vtable and UID0003TF call-order evidence resolves that method as `UpdatePosition`. Current source conclusions rely on IDA MCP and by-* evidence, not Wave2 names.
- 2026-06-17 B001 reanalysis raised [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md) to `86/91` and resolved it as the ordinary complete-object `EffectObjectPane::~EffectObjectPane()` body. B006 2026-06-30 now raises that child to `88/92` and emits the exact empty destructor body; this aggregate stays a child-method cluster with formal comment-only output.
- 2026-06-18 B003 reanalysis resolves the tiny successor [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) as an `EffectObjectPane` accessor rather than a `LivingObjectPane` method. It follows `0x00538baa-0x00538bb0` padding and returns the loop/reuse byte at `+0x14c`.
- 2026-06-25 B002 source-quality implementation raises [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) to `89/92` and inserts exact child C++. Current MCP session `80de0a67` reconfirms the 20-byte body, sole MapPane creation caller at `0x0050eb14`, no pointer/table route to `0x005388a0`, raw `int` as TimerHandler wrapper return propagation, and `+0xa4` as the inherited TimerHandler adjusted view. This removes the exact child timer-subobject naming blocker without changing this aggregate's comment-only source policy.
- 2026-06-29 B015 source-quality implementation raises [UID:000351][0x006205f8-0x006206ac.EffectObjectPaneVtableData](by-memory/0x006205f8-0x006206ac.EffectObjectPaneVtableData.md) to `88/92` with a formal covered-by marker for the class declaration. This confirms the vtable/RTTI table route without changing this core aggregate's comment-only source policy.

## 2026-07-13 B003 UID0001DM Lighting Index Support Sync

- The animation caller at `0x00538a90` treats effect-frame record `+0x0c` as a signed image-table selection: `-1` suppresses the setter call, while any other value is passed to UID0001DI. This sentinel/index behavior rejects the historical intensity interpretation.
- UID0001DI is now documented and validator-renamed as `void LightingObjectPane::SetLightImageIndex(int lightImageIndex)`. `EffectObjectPane` remains only the caller and owner of frame timing/selection; [UID:000075][LightingObjectPane](by-class/LightingObjectPane.md) owns the selected index and the Detach/write/Attach update contract.
- This support synchronization changes no UID0001D9 metadata, score, owner, emitter, aggregate split, child plan, blank formal block, Effects source route, or generated-source policy.

## Score Rationale

Completion is `88` because the page now records the exact aggregate function inventory, existing child links, current session `60724697` function/xref/callee/byte/decompile evidence, generated-output state, split/container disposition, future exact-child plan, and negative evidence against monolithic parent source. Confidence is `91` because live MCP and current support docs agree on the EffectObjectPane owner, Effects source route, existing destructor/timer/loop/vtable/pool children, frame descriptor flow, render dependency, and padding. Score is capped below final-source quality because the five missing exact child pages have not been created in this callback and their method-local signatures/type spellings still need child-page bodies or child-specific no-code proofs. The parent itself is now `RECONSTRUCTABLE:FALSE`, blank-emitter, and blank-C++ because it is an index/container rather than a source body.

## Cross-References

- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md)
- [UID:0000IZ][Effects](by-file/Effects.md)
- [UID:0000IY][EffectObjImageLib](by-file/EffectObjImageLib.md)
- [UID:00017M][0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame](by-memory/0x004de190-0x004de29f.EffectObjImageLibRenderEffectFrame.md)
- [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md)
- [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md)
- [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md)
- [UID:000075][LightingObjectPane](by-class/LightingObjectPane.md)
- [UID:0001DI][0x0053c980-0x0053c9b5.LightingObjectPaneSetLightImageIndex](by-memory/0x0053c980-0x0053c9b5.LightingObjectPaneSetLightImageIndex.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)

## 2026-07-04 B010 Split/Container Callback

B010 reclassified this page from a reconstructable comment-only aggregate to a non-emitting split/container page at `88/91`. The accepted callback applied current MCP session `60724697` evidence, blanked formal C++, cleared `EMITTER_UIDS`, set `RECONSTRUCTABLE:FALSE`, and documented the exact missing child-page plan without creating child pages. The previous comment-only parent output is now historical: exact source output must live on exact method children, while this page preserves parent range, ownership, dependency, and negative evidence.

## 2026-06-30 B006 Empty-Emitter Callback

Historical callback superseded by B010 on 2026-07-04. B006 changed formal output to a comment-only aggregate block while keeping the page reconstructable. B010 later made the stricter source-quality correction: the page is now non-emitting, not reconstructable, and blank-C++. The accepted child state remains useful: [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) emits the timer helper, [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) owns the loop accessor, [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md) emits the exact empty destructor, and [UID:000351][0x006205f8-0x006206ac.EffectObjectPaneVtableData](by-memory/0x006205f8-0x006206ac.EffectObjectPaneVtableData.md) remains compiler-emitted layout coverage.

## Changes

- 2026-07-15 B004 UID0003TG accepted source-quality callback:
  - Preserved `88/91`, UID000049 owner, non-reconstructable/non-emitting split metadata, blank formal, `Nested:-4`, complete function/padding inventory, and all unrelated evidence.
  - Replaced only current generic guard/duration-update wording for constructor byte `+0x14e` with inferred `m_timerExtensionLocked`, including false initialization, true-blocks-replacement polarity, bounded no-observed-nonzero-writer evidence, adjacent-field distinctions, UID0003TG typed TimerHandler consumer behavior, compiler exclusions, and rejected aliases.

- 2026-07-15 Agent-B005 UID0003TE callback:
  - Added the exact coordinate reuse match and null-target creation relationship, retained the two IsLooping xrefs, and corrected the constructor contract to target, row, column.
  - Preserved the non-emitting split/container metadata, blank formal, all method/padding evidence, compiler-lowering exclusions, and future-child policy.

- 2026-07-15 B001 UID0003TF accepted source-quality callback:
  - Preserved the `88/91` non-emitting split/container disposition, exact ranges/padding, blank formal/emitter, and all unrelated method evidence.
  - Synchronized the typed constructor contract, `m_renderAfterLivingObjects +0x14d`, ordinary-new/compiler-lowering boundary, and exact UID0003TF constructor/StartIdleTimer calls.
  - Corrected current `0x005388c0` source-facing references from `GetEffectBounds` to `UpdatePosition`; historicalized the former compact/GetEffectBounds/Wave2 naming rather than deleting its provenance.

### 2026-07-13 B003 - UID0001DM Lighting Index Support Sync

- What existed before: current behavior prose called the frame-selected value a LightingObjectPane intensity even though the caller used frame record `+0x0c` with `-1` as a sentinel.
- What changed: the caller is now tied to the validator-renamed UID0001DI `SetLightImageIndex` destination, exact call site `0x00538a90`, signed index/sentinel semantics, and the caller-versus-owner boundary.
- Why: frame-table selection and sentinel use support an image-table row, not photometric intensity. UID0001D9 scores, owner/emitter, source route, and non-emitting aggregate disposition remain unchanged.

### 2026-06-25 B002 - StartIdleTimer Exact-Child Source-Quality Implementation

- [UID:0001DA][0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer](by-memory/0x005388a0-0x005388b4.EffectObjectPaneStartIdleTimer.md) changed from `86/90` to `89/92` with owner/emitter still [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md).
- Summary/evidence: synchronized this aggregate with the exact child result. The child now emits `void EffectObjectPane::StartIdleTimer()` and calls `TimerHandler::ScheduleTimer(1, 0, 0, 0)` through the inherited `+0xa4` TimerHandler adjusted view. Current MCP session `80de0a67` preserves the exact body, caller route through [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md), no table/pointer route, raw `int` versus source `void` rationale, and stale BackPane rejection.
- Aggregate score, owner/emitter, reconstructable state, and comment-only policy are unchanged; remaining blockers are class-wide field/source-split/emission-policy issues, not this exact timer child.

### 2026-06-29 B015 - UID000351 Vtable Marker Support Sync

- [UID:000351][0x006205f8-0x006206ac.EffectObjectPaneVtableData](by-memory/0x006205f8-0x006206ac.EffectObjectPaneVtableData.md) changed from `85/88` to `88/92` with owner/emitter still [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md), and its formal C++ block now uses the covered-by marker for class-declaration-generated RTTI/vtable data.
- Summary/evidence: synchronized this aggregate with the exact vtable child result. Current MCP sessions `agent_b009_0002my_20260628` and `07c55da0` confirm the `0x006205f8-0x006206ac` boundary, primary and inherited adjusted vtable bases, constructor/destructor/scalar-destructor store parity, and BowGauge successor boundary.
- Aggregate score, owner/emitter, reconstructable state, and comment-only policy are unchanged; remaining blockers are class-wide field/source-split/emission-policy issues, not the exact UID000351 vtable data route.

### 2026-06-15 A002 - Current IDA And Wave2 Workflow Refresh

- What existed before: the page was scored `82/84`, with detailed aggregate behavior but no current-session signature/xref refresh and no Wave2 workflow note.
- What changed: the page was scored `86/88`, owner/emitter unchanged as [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md), and formal output remained withheld at that time; B006 2026-06-30 now records comment-only aggregate output.
- Why: live IDA MCP reconfirmed all function boundaries, constructor/timer callers, vtable/data refs, representative constructor/update/render behavior, image-lib global users, and a unique full-range signature. Wave2 method-analysis confirms the constructor signature and sibling method set but still leaves source-quality field names and final module split unresolved.

### 2026-06-17 B001 - Destructor Child Source-Quality Reanalysis

- [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md) changed from `82/88` to `86/91`; this aggregate score is unchanged.
- Summary/evidence: B001 bounded the no-xref condition with raw VA/RVA pointer checks, confirmed the target as the ordinary `EffectObjectPane::~EffectObjectPane()` body, resolved `+0xa0`/`+0xa4` as inherited adjusted handler vtable views, and withheld target formal output pending a coordinated class source pass. B006 2026-06-30 supersedes that child disposition with an exact empty destructor body on [UID:0002QV][0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor](by-memory/0x00538880-0x0053889f.EffectObjectPaneNonDeletingDestructor.md).

### 2026-06-20 B007 - MapPane RequestObjectEffect Support

- Historical 2026-06-20 wording added the create-helper-to-constructor layout contract used by [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md): `MapPane::CreateEffectObjectPane` passes `EffectInfo` by value, supplies the selected sequence index, copies the effect id/key to `+0x148`, sets `m_looping` at `+0x14c`, and clears `+0x14e`; its then-provisional `compact flag` name for `+0x14d` is superseded by current `m_renderAfterLivingObjects` evidence.

### 2026-05-28 - End Boundary Corrected

- What existed before: this page and coverage row used `0x005387b0-0x00538ba9`, leaving byte `0x00538ba9` in an UNKNOWN gap.
- What changed: the documented range is now `0x005387b0-0x00538baa`, matching IDA's exclusive function end for `RenderEffect`.
- Why: 2026-05-28 IDA MCP byte/function review reports `sub_538AF0` as ending at `0x00538baa`; the following bytes `0x00538baa-0x00538bb0` are padding before the successor accessor now documented as [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md).

### 2026-06-01 - Aggregate Filled With Child Functions

- What existed before: this page listed only the constructor and four virtual methods, omitting two real functions inside the aggregate and leaving the low placeholder score.
- What changed: the child destructor-style body at `0x00538880-0x0053889f` is now documented, `0x005388a0-0x005388b4` is reowned as an EffectObjectPane timer helper, and the page now records detailed constructor/bounds/update/render behavior plus raw boundary evidence.
- Why: IDA MCP `lookup_funcs`, `py_eval`, `decompile`, `callers`, `xrefs_to`, `callees`, and raw bytes prove the full function list and show that the timer helper is invoked on the same object pointer constructed by `0x005387b0`.
- Completion/confidence score update: existed before as `50/65`; updated to `82/84` because the aggregate now has exact function coverage, child pages for the embedded helpers, caller/callee evidence, vtable xrefs, touched fields/globals, and boundary padding. Formal output was withheld because field names and final source-file split were not final-audit quality; current output is a comment-only aggregate marker.
