*** UID:00014M | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:86 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00004R | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
// Emitted code for this retained no-route add-count helper is covered by [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md case 1.
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Retained ExchangeDialog add-count helper with no direct route; exact signed inventory-slot caller body is documented, and emitted behavior is covered by dispatcher case 1. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004ada90-0x004adb02 ExchangeDialog AddItemWithCount Helper

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: `ExchangeDialog` helper
- Address range: `0x004ada90-0x004adb02` (`[start,end)`, IDA size `0x72`).
- Signature: best descriptive source shape `AddItemWithCountDialog *ExchangeDialog::OpenAddItemWithCountDialog(const unsigned char *packet)`; `this` is the primary exchange dialog object, the stack argument is packet-buffer-like, and the return value is the constructed dialog pointer or `0`.
- Likely source file: [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- Current generated state: omitted from active `source-3/simroot_v2/class_ExchangeDialog.cpp`
- Autogen parent: [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md)
- Confidence: strong for the body, boundary, class association, packet byte role, exchange-id field role, and no-static-route evidence; medium-high for the original source call shape because no direct caller or pointer route reaches this retained helper in the current binary.
- Formal C++ disposition: the block contains only a covered-by marker naming [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `1`. The live routed dispatcher case emits the equivalent add-count behavior inline.

## Behavior

This helper constructs the item-count dialog used by the exchange feature. It reads the signed local inventory slot at `packet+2`, allocates `0x274` bytes, and calls the `AddItemWithCountDialog` constructor with the exchange session id stored at `this+0x26c`.

IDA names are still generic, but the decompile shape is small and clear:

- converts the inventory-slot byte from the packet using the narrow-char helper and preserves signed `char` semantics at the constructor boundary;
- allocates `628` bytes;
- calls the item-with-count dialog constructor at `0x004af040` with the exchange id and converted packet byte.
- returns `0` if allocation fails; otherwise returns the constructor result.

The helper looks source-authored rather than compiler glue: it has a normal `__thiscall` body, uses `ExchangeDialog` state at `+0x26c`, and delegates to a reusable item-picker class instead of performing compiler-only cleanup. Its current reachability remains open because IDA and raw PE scans report no direct code, data, vtable, jump-table, or pointer route to `0x004ada90`; the packet dispatcher at [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) separately contains the equivalent subcommand-1 constructor path and calls the same `AddItemWithCountDialog` constructor at `0x004ad3bf`.

## Raw Code Evidence

Live IDA MCP recheck on 2026-06-06 against `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` confirmed:

- `lookup_funcs 0x004ada90` reports `sub_4ADA90`, size `0x72`, with IDA function end `0x004adb02`.
- `lookup_funcs 0x004ada8f` and `0x004adb02` report no function, while `lookup_funcs 0x004adca0` still reports no function for the later raw money-update body.
- IDA's function chunks are `0x004ada90-0x004adb02` plus the far exception/unwind chunk `0x005fe3a9-0x005fe3cf`.
- The body reads `packet + 2`, calls `std::_Narrow_char_traits<char,int>::to_char_type`, stores the converted byte on the stack, allocates `0x274` bytes with `sub_4F4AA0`, and calls `sub_4AF040(result, [this+0x26c], convertedByte)` when allocation succeeds.
- The epilogue uses `retn 4`; the last body bytes at `0x004adb00-0x004adb01` are `04 00`, and `0x004adb02-0x004adb10` is `0xcc` alignment before the next raw non-modeled exchange code island at `0x004adb10`.
- Manual `idautils.XrefsTo(0x004ada90)` and MCP `callers`/`xrefs_to` report no current callers or data refs to this helper entry.
- `xrefs_to 0x004af040` reports calls from the packet dispatcher at `0x004ad3bf` and from this helper at `0x004adaeb`.

## 2026-06-18 B003 Source-Quality Recheck

B003 rechecked the target against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` (MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`) after IDA MCP was unavailable in-session. Direct PE/Capstone inspection confirms the primary body `0x004ada90-0x004adb02` is `0x72` / 114 bytes (Verified with int_convert.py), preceded by `0x004ada84-0x004ada90` `0xcc` padding and followed by `0x004adb02-0x004adb10` `0xcc` padding. The body is a normal primary-object `__thiscall` helper ending in `ret 4`; the stack argument is a packet pointer and `eax` returns the constructed dialog pointer or `0`.

The helper reads `packet+2` through `0x00575470`, allocates `0x274` / 628 bytes for `AddItemWithCountDialog` (Verified with int_convert.py), and calls constructor `0x004af040` with primary ExchangeDialog `this+0x26c` plus signed `char inventorySlot`. The best descriptive helper signature remains `AddItemWithCountDialog *ExchangeDialog::OpenAddItemWithCountDialog(const unsigned char *packet)`. The byte at `packet+2` is not a count, item id, or item-type enum; the callee stores it as `m_inventorySlot` at `+0x270`, uses it with `GetInventorySlotAddress(char)`, and later serializes it in packet `0x4a/2` with the user-entered count.

Raw PE route scans found no rel32 branch target to `0x004ada90`, no full-file absolute VA dword `0x004ada90`, no RVA dword `0x000ada90`, and no end-pointer dword `0x004adb02`. The only rel32 calls to `AddItemWithCountDialog` constructor `0x004af040` are dispatcher subcommand `1` at `0x004ad3bf` and this helper at `0x004adaeb`. The live dispatcher block at `0x004ad380-0x004ad3c6` duplicates the helper's packet-byte read, `0x274` allocation, and constructor call. Its `[this+0x1cc]` access is the secondary-vtable receiver view of the same primary ExchangeDialog field at `this+0x26c` because the secondary view is installed at object offset `+0xa0`.

The target remains ExchangeDialog-owned and reconstructable, but formal C++ should stay blank for now under the current combined-score/emitter policy. The blocker is not the stale `95/95` gate; it is the concrete source-shape issue that no static caller/table/vtable route reaches the helper while the dispatcher already contains the equivalent subcommand body. Treat this range as a retained out-of-line helper or retained duplicate of an inlined dispatcher helper until the broader `ExchangeDialog.cpp` reconstruction decides whether dispatcher case `1` should call a private helper or remain inline.

## Callers And Callees

- Callers: none currently modeled by IDA/MCP, and B003 raw PE scans found no rel32 branch, VA/RVA pointer, vtable, jump-table, or static table route to `0x004ada90`. Treat this as an omitted/reachability-open retained helper until the broader ExchangeDialog source integration decides whether to model a helper call or the live inline dispatcher body.
- Callees:
  - `0x004af040`: `AddItemWithCountDialog` constructor in [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md).
  - `0x004f4aa0`: allocator helper.
  - `0x00575470`: `std::_Narrow_char_traits<char,int>::to_char_type`.

## Touched State

- `this+0x26c`: exchange session id passed into the item-count dialog.
- `packet+2`: signed local inventory-slot index from exchange server subcommand `1`, passed to `AddItemWithCountDialog`, stored at `+0x270`, and later serialized in opcode `0x4a` subcommand `2` with the user-entered count.
- No globals, resources, strings, or persistent fields are written by this helper.

## Boundary And Open Questions

- The exact code-body range is `[0x004ada90,0x004adb02)`. Rename this page and dependent UID annotations from the old final-byte spelling `0x004adb01` to `0x004adb02` during the next shared validator/report sync.
- `0x004adb02-0x004adb10` is `0xcc` alignment; `0x004adb10` begins raw function-shaped exchange packet/list code that is not currently split into its own by-memory page.
- Reachability remains the source-shape reason not to emit a standalone helper body. The helper is source-shaped and class-owned, but live IDA and raw PE scans have no route to the entry; dispatcher subcommand `1` constructs `AddItemWithCountDialog` through an inline equivalent block. The formal block is now a covered-by marker tied to [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `1`, not a blank emitter and not standalone C++.

## Migration Notes

Keep the helper in `ExchangeDialog.cpp`, but do not move `AddItemWithCountDialog` itself into this file. The picker implementation belongs with [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md); exchange only invokes it.

## Cross-References

- [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md)
- [UID:0000J9][ExchangeDialog](by-file/ExchangeDialog.md)
- [UID:0000KE][ItemDialogs](by-file/ItemDialogs.md)
- [UID:00014K][0x004ac8a0-0x004ae4b6.ExchangeDialog](by-memory/0x004ac8a0-0x004ae4b6.ExchangeDialog.md)
- [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md)
- [UID:000008][AddItemWithCountDialog](by-class/AddItemWithCountDialog.md)

## Changes

- 2026-07-13 UID0004BQ callback: preserved `86/90`, exact range/padding/EH/no-route state, ExchangeDialog owner/emitter, and dispatcher-case-1 covered-by marker; corrected every current selected item/type claim to signed local inventory-slot semantics and recorded the constructor/accessor/packet reuse proof.

- 2026-06-28 B001 empty-emitter implementation:
  - Changed the formal C++ block from blank to the exact covered-by marker for [UID:00014L][0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher](by-memory/0x004ad320-0x004ad7b3.ExchangeDialogPacketDispatcher.md) case `1`.
  - Metadata stays `86/90`, owner/emitter [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), `RECONSTRUCTABLE:TRUE`; this page now no longer creates an empty nonblank emitter while preserving the retained no-route helper evidence.
- 2026-06-18 B003 source-quality recheck:
  - Before: `COMPLETION:84`, `CONFIDENCE:88`, with source-facing name/packet-byte role/secondary-facet field conflict and stale final-C++ gate wording unresolved.
  - After: `COMPLETION:86`, `CONFIDENCE:90`; owner/emitter [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), reconstructable state, and no standalone helper body remained unchanged.
  - Evidence: direct PE/Capstone recheck confirms the `0x72` / 114-byte primary-object `__thiscall` body (Verified with int_convert.py), `0x004ada84-0x004ada90` and `0x004adb02-0x004adb10` padding, C++ EH cleanup/handler at `0x005fe3a9`/`0x005fe3b4`, the then-described `packet+2` selected item/type byte read through `0x00575470`, `0x274` / 628-byte `AddItemWithCountDialog` allocation (Verified with int_convert.py), constructor call `0x004af040` with primary ExchangeDialog exchange id `this+0x26c`, no rel32 branch/VA/RVA/end-pointer route to helper start, and duplicate dispatcher subcommand-1 constructor path at `0x004ad380-0x004ad3c6` using secondary-facet `this+0x1cc` for the same field. B001 later converted the formal block to the dispatcher case `1` covered-by marker because the unresolved issue is retained helper versus inlined dispatcher source shape, not a numeric 95/95 gate. The 2026-07-13 callback supersedes only the item/type label with signed local inventory-slot meaning.
- 2026-06-06 A002 IDA boundary/body recheck:
  - Before: scored `70/85`, parent UID blank, and documented only the broad constructor/allocation shape.
  - After: scored `84/88`, attached to [UID:00004R][ExchangeDialog](by-class/ExchangeDialog.md), and still leaves final C++ blank below the 95/95 gate.
  - Evidence: live IDA MCP confirms the exact `0x004ada90-0x004adb02` function body, far EH chunk, `packet+2` byte conversion, `0x274` allocation, `this+0x26c` exchange-id argument, `sub_4AF040` constructor call, no current callers/xrefs to the helper entry, `0x004adb02-0x004adb10` alignment, and a separate dispatcher call to the same item-count constructor at `0x004ad3bf`.
- 2026-06-06 A002 filename sync:
  - Before: the page title/body documented the exact half-open `0x004ada90-0x004adb02` range, but the filename still used the stale inclusive-final-byte spelling `0x004adb01`.
  - After: renamed the page to `0x004ada90-0x004adb02.ExchangeDialogAddItemWithCountHelper.md`; behavior, score, parent, and blank C++ block are unchanged.
