*** UID:0003MD | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00008U | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00008U | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MusicControlDialog::OnControlCommand(int controlIndex, int notifyCode)
{
    (void)notifyCode;

    switch (controlIndex)
    {
    case kControlSoundEffects:
        SendMusicDialogOptionPacket(0x0d);
        ApplySoundEffectEnabled(GetSelectedValue(kControlSoundEffects) != 1);
        break;

    case kControlSampleVolume:
        g_pSoundManager->SetSampleVolume(GetSliderValue(kControlSampleVolume));
        break;

    case kControlSoundFrequency:
        ApplySoundFrequencySelection(
            GetSelectedValue(kControlSoundFrequency));
        break;

    case kControlMusicVolume:
        g_pSoundManager->SetStreamVolume(GetSliderValue(kControlMusicVolume));
        break;

    case kControlMusicSourceMode:
    {
        int mode = GetSelectedValue(kControlMusicSourceMode);
        if (g_pConfig->m_musicSourceMode != mode)
        {
            g_pConfig->m_musicSourceMode = mode;
            ApplyPlaybackState();
        }
        break;
    }

    case kControlMusicFolderPath:
        break;

    case kControlBrowseFolder:
    {
        StringBaseW currentPath = GetControlText(kControlMusicFolderPath);
        typedef void (MusicControlDialog::*FolderPathCallback)(StringBaseW);
        FolderSelectCallback *callback =
            new PlainMemberFunctionObjectT<
                FolderPathCallback,
                MusicControlDialog,
                StringBaseW>(
                    this, &MusicControlDialog::FolderPathSelected);
        new FolderSelectDialog(callback, currentPath.c_str());
        break;
    }

    case kControlCdDrive:
    {
        PopupMenuControlPane *popup = GetControlAs<PopupMenuControlPane>(kControlCdDrive);
        int selectedIndex = popup->GetSelectedIndex();
        if (selectedIndex < 0)
            break;

        wchar_t driveLetter = ParseDriveLetter(popup->GetSelectedText(selectedIndex));
        if (driveLetter != 0 && g_pConfig->m_redbookDriveLetter != driveLetter)
        {
            g_pConfig->m_redbookDriveLetter = driveLetter;
            ApplyPlaybackState();
        }
        break;
    }

    case kControlShuffleMusic:
        g_pConfig->m_randomizeMusicOrder =
            (GetSelectedValue(kControlShuffleMusic) != 0);
        break;

    case kControlRepeatMusic:
        g_pConfig->m_repeatMusic =
            (GetSelectedValue(kControlRepeatMusic) == 0);
        break;

    case kControlPreviousTrack:
        if (g_pConfig->m_musicSourceMode == kMusicSourceStream)
        {
            if (g_pSoundManager->currentStreamTrackIndex() > 1)
            {
                g_pSoundManager->PlayStreamTrack(
                    g_pSoundManager->currentStreamTrackIndex() - 1,
                    100,
                    true);
            }
        }
        else if (g_pConfig->m_musicSourceMode == kMusicSourceRedbook)
        {
            g_pSoundManager->RewindRedbookTrack();
        }

        if (m_musicPaused)
            TogglePauseState();
        break;

    case kControlPausePlay:
        TogglePauseState();
        break;

    case kControlNextTrack:
        g_pSoundManager->setMusicAdvanceRequested(true);
        if (g_pConfig->m_musicSourceMode == kMusicSourceStream)
            g_pSoundManager->PlayNextTrack();
        else if (g_pConfig->m_musicSourceMode == kMusicSourceRedbook)
            g_pSoundManager->AdvanceRedbookTrack();

        if (m_musicPaused)
            TogglePauseState();
        break;

    case kControlClose:
        CloseDialog();
        break;

    default:
        break;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: DialogPane-slot MusicControlDialog::OnControlCommand at 0x00529790-0x00529b70 dispatches controls 1..14; case 3 calls ApplySoundFrequencySelection through kControlSoundFrequency while the executable preserves the behavior-identical inline path, and case 7 binds the by-value StringBaseW folder callback. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***


# 0x00529790-0x00529b70 MusicControlDialog OnControlCommand

## Status

- Address range: `0x00529790-0x00529b70` exclusive end.
- Entity kind: method plus switch table.
- Disposition: reconstructable MusicControlDialog source.
- Rebuild handling: `source-authored` method body plus compiler/linker-generated switch table.
- Direct owner: [UID:00008U][MusicControlDialog](by-class/MusicControlDialog.md).
- Source file: [UID:0000LN][MusicControlDialog](by-file/MusicControlDialog.md).

## Behavior

This primary `DialogPane` virtual override at slot `+0x48` dispatches MusicControlDialog control IDs `1..14`. Its source-facing signature is `void MusicControlDialog::OnControlCommand(int controlIndex, int notifyCode)`; `HandleControlCommand`, `controlId`, and `notification` are preserved only as historical descriptive names.

- `1` toggles sound effects and delegates to [UID:00008U][MusicControlDialog](by-class/MusicControlDialog.md) sound-effect application helper.
- `2` and `4` apply sample/music volume slider values through SoundManager calls.
- `3` calls `ApplySoundFrequencySelection(GetSelectedValue(kControlSoundFrequency))`. The shipped executable contains the behavior-identical inline expansion, so the call is the human source shape and the retained helper remains valid source evidence.
- `5` changes playback mode and reapplies playback state.
- `7` constructs the exact by-value three-parameter `PlainMemberFunctionObjectT<FolderPathCallback, MusicControlDialog, StringBaseW>` callback and directly invokes `new FolderSelectDialog(callback, currentPath.c_str())`. Disassembly shows a `0x274` / 628-byte FolderSelectDialog allocation, `0x18`-byte concrete callback-wrapper allocation, callback method pointer `0x00529ee0`, zero adjustment, MusicControlDialog `this`, initial-path conversion through `0x00584540`, and constructor call `0x0052986d`. The concrete value specialization and callback callee's explicit argument destruction establish by-value `StringBaseW`, superseding the older generic `String`, two-parameter template, and `FolderSelectDialog::Show` drafts.
- `8` reads the selected drive control, updates config drive state, and reapplies playback.
- `9` and `10` update music option flags.
- `11`, `12`, and `13` implement previous, pause, and next control actions.
- `14` closes/accepts the dialog.

The current Config source names used by the formal block are `m_musicSourceMode`, `m_redbookDriveLetter`, `m_randomizeMusicOrder`, and `m_repeatMusic`. Control id `3` is `kControlSoundFrequency` because persisted key `SoundFreq` and SoundManager behavior establish values `0` disabled, `1` 22050 Hz, and `2` 44100 Hz.

## Range Details

- `0x00529790-0x00529b2e`: IDA function body `sub_529790`.
- `0x00529b2e-0x00529b30`: two-byte alignment before the switch table.
- `0x00529b30-0x00529b68`: switch table referenced by the dispatch at `0x005297d7`.
- `0x00529b68-0x00529b70`: `0xcc` alignment before the next unmodeled helper body.

## IDA Evidence

- 2026-06-13 MCP `lookup_funcs` reports `sub_529790` at `0x00529790` with size `0x39e`; byte/insn review shows its switch table continues immediately after the function body, so this child page covers the method plus table through `0x00529b70`.
- MCP decompilation shows direct calls to the playback-state helper at `0x00529b09`, pause toggle at `0x005299fd` and `0x00529a46`, and sound-effect helper at `0x005299af`.
- MCP decompilation shows case `7` writes the concrete value-specialization vtable now documented as `PlainMemberFunctionObjectT<FolderPathCallback, MusicControlDialog, StringBaseW>`, stores callback target `sub_529EE0`, and passes it into the folder-selection path. Earlier generic/two-parameter spellings were evidence-time aliases. [UID:0000JF][FolderSelectDialog](by-file/FolderSelectDialog.md) remains dialog owner and [UID:0000JO][FunctionObjects](by-file/FunctionObjects.md) remains reusable template owner; this method owns only the concrete construction/caller site.
- B003 MCP session `2ec9c08f` bounded disassembly reconfirmed case `7`: `0x005297de` pushes allocation size `0x274` / 628 for the FolderSelectDialog object, `0x00529816-0x00529823` allocates `0x18` bytes for the callback wrapper, `0x00529842` writes the by-value three-parameter callback-specialization vtable, `0x00529848` stores method pointer `0x00529ee0`, `0x0052984f` stores zero adjustment, `0x00529856` stores MusicControlDialog `this`, `0x00529864` converts the current path, and `0x0052986d` calls [UID:000457][0x004b1590-0x004b18d6.FolderSelectDialogConstructor](by-memory/0x004b1590-0x004b18d6.FolderSelectDialogConstructor.md). This remains caller/callback-provider evidence only and does not transfer ownership of FolderSelectDialog, its vtables, drive-root data, or FolderTreePane.
- MCP `xrefs_to 0x00529b30` reports the data xref from `0x005297d7` inside this command handler, confirming the switch-table ownership.
- MCP component analysis records internal edges from this handler to `0x0052a120`, `0x0052a180`, and `0x0052a2d0`.
- 2026-07-07 B007 UID0003MO support check used current MCP session `507affd6` to reconfirm the previous-track route without changing this page's metadata or formal C++: the switch-table entry at `0x00529b30` still points control id `11` to `0x00529a07`, not to raw helper [UID:0003MO][0x0052a280-0x0052a2cb.MusicControlDialogPreviousTrackHelper](by-memory/0x0052a280-0x0052a2cb.MusicControlDialogPreviousTrackHelper.md); the inline case body calls `SoundManager::RewindRedbookTrack` at `0x00529a1d -> 0x0057b2c0`, calls stream/local `PlayTrackByIndex(index - 1, 100, 1)` at `0x00529a32 -> 0x00579e30`, and calls `TogglePauseState` at `0x00529a46 -> 0x0052a180` when the dialog pause byte is set.

## Source-Quality Reanalysis

B003's 2026-06-18 source-quality pass decodes every command id in the switch table and resolves the old blank-C++ blocker for first-draft emission. The switch table is compiler-generated lowering and remains by-memory evidence only; formal C++ is the source `switch (controlIndex)` shown above.

Direct PE/disassembly checks verified the table at `0x00529b30-0x00529b68`:

| Control id | Target | Source-facing role |
| --- | --- | --- |
| `1` | `0x00529978` | Sound-effects toggle; sends shared option opcode `0x011b` code `0x0d`, then applies `selected != 1` through `ApplySoundEffectEnabled`. |
| `2` | `0x00529951` | Sample/SFX volume slider; reads the slider word and calls `SoundManager::SetSampleVolume`. |
| `3` | `0x005298b0` | Sound-frequency selection; source calls `ApplySoundFrequencySelection(GetSelectedValue(kControlSoundFrequency))`. The inlined machine body writes `m_soundFrequency`, directly constructs localized AlertPane `244` on changed active values, and mutes/unmutes SoundManager. |
| `4` | `0x00529889` | Music/stream volume slider; reads the slider word and calls `SoundManager::SetStreamVolume`. |
| `5` | `0x00529a50` | Music source mode; writes `g_pConfig+0x291908` and calls `ApplyPlaybackState` when changed. |
| `6` | `0x00529b10` | Folder path display control; command body is a no-op/default path. |
| `7` | `0x005297de` | Browse folder; builds the exact three-parameter member callback targeting `0x00529ee0` and directly constructs `FolderSelectDialog` at `0x0052986d`. No `Show` shorthand remains in active source. |
| `8` | `0x00529abe` | CD/Redbook drive popup; parses the selected drive, writes `g_pConfig+0x291910`, and reapplies playback when changed. |
| `9` | `0x00529a7c` | Shuffle/randomize; writes `g_pConfig+0x291912 = selected != 0`. |
| `10` | `0x00529a9d` | Repeat/wrap; writes `g_pConfig+0x291913 = selected == 0`, inverted from the UI selection. |
| `11` | `0x00529a07` | Previous track; calls stream previous or Redbook rewind and clears pause through `TogglePauseState` if needed. |
| `12` | `0x005299fd` | Pause/play; calls `TogglePauseState`. |
| `13` | `0x005299c3` | Next track; sets the SoundManager advance-request flag, calls stream next or Redbook advance, and clears pause if needed. |
| `14` | `0x005299b9` | Close/OK; calls the dialog close/accept helper. |

The helper direction is source-ready: `0x00529d00` is inherited override `UpdateActionButton`, `0x0052a120` is `ApplyPlaybackState`, `0x0052a2d0` applies sound-effect enablement, and `0x0052a310` is `ApplySoundFrequencySelection`. Exact original UI label text and server-side option-code names remain unrecovered confidence caps, but they do not justify raw labels or blank C++.

## Reconstruction Notes

The prior blank-C++ blocker is resolved. Every command ID `1..14` has a defensible role and action, the switch table is fully bounded, case `3` is normalized to its source call, and case `7` uses the concrete by-value `StringBaseW` callback specialization. Remaining uncertainty is limited to original UI label text and exact option-code server-side names.

The by-memory child continues to cover the method body plus the compiler-generated switch table at `0x00529b30-0x00529b68`. Formal source is `OnControlCommand` with `switch (controlIndex)`; it does not emit a literal jump table.

Use descriptive command IDs: sound effects, sample volume, music output, music volume, music source mode, folder path, browse folder, CD drive, shuffle/randomize, repeat/wrap, previous, pause/play, next, and close. Control `10` writes the persisted repeat/wrap flag inverted as `selected == 0`.

The case `1` packet is the shared option/settings opcode `0x011b` with option code `0x0d`, sent inline before applying sound-effect enablement. The exact server-side option name remains unrecovered, but the client-side control route is strong.

B014 FolderSelectDialog support sync: case `7` is caller/concrete callback-binding evidence only. It does not make MusicControlDialog the owner of [UID:000059][FolderSelectDialog](by-class/FolderSelectDialog.md), the dialog vtables, the drive-root literal/buffer, or [UID:00005A][FolderTreePane](by-class/FolderTreePane.md). The callback wrapper's reusable template shape remains with [UID:0000JO][FunctionObjects](by-file/FunctionObjects.md); MusicControl owns the bound target `MusicControlDialog::FolderPathSelected`.

## 2026-07-29 Supervisor Gate 2B Reconciliation (Catalog 0353)

- I03 was applied and verified at `0x00529790`: current name `MusicControlDialog__OnControlCommand`, prototype `void __thiscall(MusicControlDialog *this, int controlIndex, int notifyCode)`, and the accepted regular function comment. Both address comment channels and the repeatable function-comment channel remain blank.
- Prototype-driven frame normalization renamed/typed only existing dword argument slots: `controlIndex` is the former `arg_0` at frame offset `+0x5c`, width `4`, type `int`; `notifyCode` is the former `arg_4` at `+0x60`, width `4`, type `int`. All nonargument storage, offsets, widths, and return behavior are unchanged. `sub_529790` is historical prestate/search provenance only.
- Persistence checkpoint: prestate IDB SHA256 `FD8282A51836D47961EA386D7C2D25F80D0BF88B96ACFF106FFA802277F78B16`; byte-identical backup `E:\NTK\Resources\NexusTK\backups\NexusTK.exe.pre-B010-UID0003MQ-20260729-082218.i64`; saved IDB SHA256 `6D20767D448CC568508E462790D05678838216C6CFEE77E3CFF5C4B5905244AB`, `143190049` bytes, `2026-07-29T08:26:09.5759690-04:00`. Catalog entry `0353` records `APPLIED_VERIFIED_RECONCILIATION_PENDING`; B010 did not mutate IDA.

## Changes

- 2026-07-29 B010 implementation callback: renamed the source override to `OnControlCommand`, normalized control id `3` to `kControlSoundFrequency`, replaced the inlined draft case with the exact `ApplySoundFrequencySelection` call, corrected the folder callback to by-value `StringBaseW`, adopted current Config member names, and preserved the executable inline expansion/switch table as historical binary evidence. Score remains `88/90`.
- 2026-07-03 B003 accepted FolderSelectDialogConstructor support update:
  - What existed before: case `7` already documented the source-level `FolderSelectDialog::Show` shorthand caveat and constructor call at `0x0052986d`.
  - Changed to: added current MCP session `2ec9c08f` allocation/callback details for the UID000457 constructor caller route; MusicControlDialog metadata and C++ remain unchanged.
  - Summary/evidence: B003 bounded disassembly shows FolderSelectDialog object allocation `0x274` / 628, callback wrapper allocation `0x18`, callback method pointer `0x00529ee0`, MusicControlDialog `this` storage, initial-path conversion, and constructor call `0x0052986d`; this remains caller/provider evidence only, not ownership of FolderSelectDialog.
- 2026-07-07 B007 UID0003MO support sync: metadata and formal C++ unchanged; added current session `507affd6` case `11` confirmation that the switch table routes to inline body `0x00529a07`, with calls at `0x00529a1d`, `0x00529a32`, and `0x00529a46`, while raw UID0003MO remains marker-only/no-entry support.
- 2026-06-18 B003 source-quality pass: raised score to `88/90`, kept owner/emitter [UID:00008U][MusicControlDialog](by-class/MusicControlDialog.md), decoded the complete `1..14` command map, confirmed switch-table ownership at `0x00529b30-0x00529b68`, identified the folder-selection member callback target `0x00529ee0`, separated `RefreshControlStates` (`0x00529d00`) from `ApplyPlaybackState` (`0x0052a120`), tied cases to config fields `+0x291908/+0x291910/+0x291912/+0x291913/+0x291914`, and added first-draft command-handler C++.
- 2026-06-19 B014 FolderSelectDialog support sync: score unchanged; clarified that the draft `FolderSelectDialog::Show` is source-level convenience shorthand, while the checked binary route for case `7` is allocation plus constructor call at `0x0052986d`; kept MusicControlDialog as caller/concrete callback owner only.
- 2026-06-13 A003 split pass: created exact command-handler child and widened the child range to include the compiler switch table that the old aggregate left implicit.
