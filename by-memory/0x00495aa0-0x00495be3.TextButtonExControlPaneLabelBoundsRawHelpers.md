*** UID:0003NC | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000EK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000EK | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect)
{
    wchar_t labelText[128];

    m_labelTextBox->CopyWideText(labelText, 0x7f);

    const short textWidth = GetTextWidth(labelText);

    RectBounds controlBounds;
    GetBounds(&controlBounds);

    outRect->left = (controlBounds.left + controlBounds.right - textWidth) / 2;
    outRect->right = outRect->left + textWidth + 2;
    outRect->top = (controlBounds.top + controlBounds.bottom - 12) / 2;
    outRect->bottom = outRect->top + 12;

    IntersectRects(&controlBounds, outRect, outRect);
}

void TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect, const wchar_t *text)
{
    const short textWidth = GetTextWidth(text);

    RectBounds controlBounds;
    GetBounds(&controlBounds);

    outRect->left = (controlBounds.left + controlBounds.right - textWidth) / 2;
    outRect->right = outRect->left + textWidth + 2;
    outRect->top = (controlBounds.top + controlBounds.bottom - 12) / 2;
    outRect->bottom = outRect->top + 12;

    IntersectRects(&controlBounds, outRect, outRect);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B007 source-quality pass corrects the half-open endpoint to include the final `ret 8` byte; two raw non-function `TextButtonExControlPane::CalculateLabelRect` overloads at `0x00495aa0-0x00495b51` and `0x00495b60-0x00495be3`, internal padding `0x00495b51-0x00495b60`, no direct branch/call or VA/RVA pointer refs to helper starts, current/supplied text measurement through `TextEditPane::CopyWideText` and `GrafPort::GetTextWidth`, inherited bounds fetch `GetBounds`, in-place clamp through `IntersectRects`, inferred `TextBoxPane *m_labelTextBox` at `+0x10c`, first-draft C++ populated, and owner/emitter route remains [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00495aa0-0x00495be3 - TextButtonExControlPane Label Bounds Raw Helpers

## Status

- Disposition: reconstructable NexusTK project code; IDA-unmodeled raw helper island with two related label-bounds bodies.
- Rebuild handling: `source-authored` helper code inside the corrected [UID:000118][0x00494eb0-0x0049803a.ButtonChoiceControlCore](by-memory/0x00494eb0-0x0049803a.ButtonChoiceControlCore.md) ButtonChoice control aggregate.
- Direct owner: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`.
- Emitter route: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`, which routes to [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md) `ButtonControlPane.cpp`.
- Confidence: strong for exact bytes, text-provider and rectangle behavior, field/dependency interpretation, owner route, and no-direct-reachability status; medium-high for final source-level helper/member spelling because neither raw body is promoted as an IDA function and no original source symbols are present.
- C++ reconstruction: first-draft source-authored helper code is populated. The helper names `CalculateLabelRect` and field name `m_labelTextBox` are high-probability descriptive source-facing names; exact historical spelling remains below final-source certainty.

## Covered Ranges

| Range | Entity | Role |
| --- | --- | --- |
| `0x00495aa0-0x00495b51` | `TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect)` | Provider/current-text overload. Pulls text from the `m_labelTextBox` child at `this+0x10c` into a stack buffer, measures it, computes a centered 12-pixel label rectangle, and clamps it to the control bounds. |
| `0x00495b51-0x00495b60` | padding | Fifteen `0xcc` bytes between the two raw helper bodies. |
| `0x00495b60-0x00495be3` | `TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect, const wchar_t *text)` | Supplied-text overload. Measures a caller-supplied text pointer, computes the same centered 12-pixel label rectangle, and clamps it to the control bounds. |

Boundary notes:

- `0x00495a95-0x00495aa0` is eleven `0xcc` bytes after `TextButtonExControlPane::OnPaint` at `0x00495940-0x00495a95`.
- `0x00495be3-0x00495bf0` is thirteen `0xcc` bytes before the `ImageButtonControlPane` constructor at `0x00495bf0`.
- B007's raw PE/Capstone recheck against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` corrected the prior half-open endpoint: the first helper's `ret 4` is the three-byte sequence `c2 04 00` ending at `0x00495b50`, and the second helper's `ret 8` is `c2 08 00` ending at `0x00495be2`. The old `0x00495b50` and `0x00495be2` padding starts each included the final zero byte of the preceding `ret imm16`.
- MCP `lookup_funcs` on 2026-06-13 reports `0x00495aa0` and `0x00495b60` as `Not a function`, and reports successor `0x00495bf0` as `sub_495BF0`, size `0xb5`.
- MCP `make_signature_for_range` reported unique signatures for both original body checks; the corrected half-open source bodies are `0x00495aa0-0x00495b51` and `0x00495b60-0x00495be3`.

## Raw Behavior

The two bodies implement the same label-rectangle calculation with different text sources.

The first helper:

- reserves a `0x114` byte stack frame with a security cookie;
- reads text from the `TextBoxPane` / `TextEditPane`-derived provider child at `this+0x10c`, best documented here as `TextBoxPane *m_labelTextBox`, through [UID:0002O9][0x0058e380-0x0058e3d7.TextEditPaneCopyWideText](by-memory/0x0058e380-0x0058e3d7.TextEditPaneCopyWideText.md) (`sub_58E380`), with maximum `0x7f` UTF-16 characters;
- measures the local text buffer through [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) (`sub_4BAA70`);
- gets the inherited/control-pane bounds through the documented [UID:000161][0x004b8e00-0x004b8e11.GrafPortGetBounds](by-memory/0x004b8e00-0x004b8e11.GrafPortGetBounds.md) body (`sub_4B8E00`), used in emitted C++ as inherited `GetBounds(&controlBounds)` through GrafPort rather than as TextButtonExControlPane or historical BackPane ownership;
- computes left/right from centered text width plus two pixels of horizontal padding;
- computes top/bottom as a centered 12-pixel-tall rectangle;
- calls [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) (`sub_4B7CC0`) as `IntersectRects(&controlBounds, outRect, outRect)` to clamp/intersect the output rectangle to the control bounds;
- returns with `retn 4`.

The second helper:

- reserves a smaller `0x14` byte stack frame with a security cookie;
- measures the caller-supplied text pointer through [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md) (`sub_4BAA70`);
- gets the control bounds through inherited `GetBounds(&controlBounds)` (`sub_4B8E00`);
- performs the same centered 12-pixel label rectangle calculation and clamp/intersect call;
- returns with `retn 8`.

## Source-Quality Closure

B007's 2026-06-19 source-quality pass resolves the previous class/field/helper/reachability blockers well enough for first-draft source emission.

Best source-facing helper interpretation:

- `0x00495aa0-0x00495b51`: `TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect)`, the current/provider-text overload.
- `0x00495b60-0x00495be3`: `TextButtonExControlPane::CalculateLabelRect(RectBounds *outRect, const wchar_t *text)`, the supplied-text overload.

The names are descriptive and high-probability rather than symbol-proven. `GetTextBounds` was rejected because the body constructs a label-control rectangle rather than only returning text metrics. `SetLabelBounds` was rejected because the helper writes a caller-provided rectangle and does not apply it to the child pane. The page name remains useful for documentation, but it is not the best source method name.

Field/type interpretation:

- `this+0x10c` is the owned label child installed by the `TextButtonExControlPane` constructor at `0x00495450`.
- [UID:0002RV][0x00595390-0x005954b3.TextBoxPaneConstructor](by-memory/0x00595390-0x005954b3.TextBoxPaneConstructor.md) confirms the constructor target at `0x00595390` builds a `TextBoxPane`, which inherits the `TextEditPane` text-copy behavior used by [UID:0002O9][0x0058e380-0x0058e3d7.TextEditPaneCopyWideText](by-memory/0x0058e380-0x0058e3d7.TextEditPaneCopyWideText.md).
- The best source-facing field name for this page is `m_labelTextBox`; `m_textBoxPane` remains a safe fallback if a later class-declaration pass chooses less role-specific spelling.

Reachability conclusion:

- Existing MCP `xrefs_to` found no cross-references to the raw starts `0x00495aa0` or `0x00495b60`.
- B007's raw PE scan found no direct `E8`/`E9`, rel8, or Jcc branch/call references to either start and no exact little-endian VA/RVA occurrences for either start in the file.
- No vtable-cell, function-pointer, or table route is currently evidenced.
- The no-direct-route result is retained as a confidence cap and direct-call caveat, not as a no-code proof. Both bodies have source-shaped prologues, stack cookies, documented project callees, class-local state, rectangle writes, and proper `ret imm16` epilogues; they are best treated as retained unreferenced private helpers in the `ButtonControlPane.cpp` control source unit.

Rejected owner/source alternatives:

- `FittingRoomDownloadControlPane` and related generated owner labels are stale owner pollution. They supplied a useful semantic lead for `CalculateLabelRect` but no fitting-room state, resources, vtables, or call route appears in these bodies.
- `TextButtonControlPane` lacks the `TextBoxPane` label child at `+0x10c` and is the sibling block before TextButtonEx.
- `ImageButtonControlPane` begins at the successor `0x00495bf0`; the corrected `0x00495be3-0x00495bf0` padding separates this page from the successor.
- `TextBoxPane`, `TextEditPane`, `GrafPort`, `BackPane`, and `RectBounds` are dependencies or shared helper owners, not the source owner of this TextButtonEx-specific label-rectangle pair.

## IDA MCP Evidence

MCP `search_text` over the two ranges returned the following key raw listing facts:

| Address | Instruction evidence |
| --- | --- |
| `0x00495aa0` | First body prologue: `push ebp`; `sub esp, 114h`; security cookie setup. |
| `0x00495ac1` | Pushes `0x7f` and stack buffer, loads `ecx = [edi+10ch]`, calls `sub_58E380`. |
| `0x00495ad8` | Calls `sub_4BAA70` to measure the stack-buffer text. |
| `0x00495ae9` | Calls `sub_4B8E00` to fetch bounds. |
| `0x00495b31` | Pushes output and bounds rectangles, calls `sub_4B7CC0`. |
| `0x00495b4e` | Returns with three-byte `retn 4` sequence `c2 04 00`, so the first helper's half-open end is `0x00495b51`. |
| `0x00495b60` | Second body prologue: `push ebp`; `sub esp, 14h`; security cookie setup. |
| `0x00495b7c` | Calls `sub_4BAA70` on the caller-supplied text pointer. |
| `0x00495b8a` | Calls `sub_4B8E00` to fetch bounds. |
| `0x00495bc8` | Calls `sub_4B7CC0` to clamp/intersect the computed label rectangle. |
| `0x00495be0` | Returns with three-byte `retn 8` sequence `c2 08 00`, so the second helper's half-open end is `0x00495be3`. |

MCP `xrefs_to` on 2026-06-13 reports no cross-references to `0x00495aa0` or `0x00495b60`.

MCP `decompile 0x00495450` confirms the constructor performs the same label-rectangle concept for the initial text before allocating the provider child at `this+0x10c`: it measures the text with `sub_4BAA70`, gets bounds with `sub_4B8E00`, centers a 12-pixel rectangle, clamps it with `sub_4B7CC0`, allocates the provider, and stores it at `this+0x10c`.

MCP `analyze_batch 0x00495940` confirms the preceding modeled `TextButtonExControlPane::OnPaint` ends at `0x00495a95`, uses `TextButtonExControlPane` state bytes, and contains the same active-refresh idioms as the surrounding text-button-ex control family.

## Ownership Analysis

Best owner: [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`.

Evidence for:

- The first raw helper reads the text-provider child at `this+0x10c`, the defining owned child for `TextButtonExControlPane`.
- The constructor at `0x00495450` uses the same text-measure/bounds/clamp pattern while installing `TextButtonExControlPane` vtables and creating the provider child.
- The helpers sit immediately after `TextButtonExControlPane::OnPaint` and before `ImageButtonControlPane` begins.
- [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) is `86/86` and attached to the source-family file [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md), so parent routing clears the strict gate.

Rejected owners:

- `TextButtonControlPane`: the first helper uses a provider object at `+0x10c`, not a plain owned text buffer, and the page sits after the TextButtonEx method block.
- `ImageButtonControlPane`: the successor begins at `0x00495bf0`; these helpers do not use image-button frame/state fields.
- Feature/fitting-room generated owners: old Wave2 labels around these starts are treated as owner pollution. Current IDA evidence shows generic text-provider label layout behavior within the TextButtonEx block.

## Score Rationale

Completion is `88`: exact corrected raw body ranges, internal/post padding, endpoint byte correction, no-function/no-xref status, raw PE direct-branch/pointer scan results, source-facing helper/field/dependency names, behavior, owner routing, rejected alternatives, and first-draft C++ are documented.

Confidence is `89`: behavior and owner are well supported by `+0x10c` provider use, TextBoxPane/TextEditPane dependency evidence, constructor pairing, local address context, and independent raw PE endpoint/reachability checks. Confidence stays below final because direct reachability to the raw starts is not recovered and exact original helper/member spellings remain inferred.

## Cross-References

- [UID:000118][0x00494eb0-0x0049803a.ButtonChoiceControlCore](by-memory/0x00494eb0-0x0049803a.ButtonChoiceControlCore.md) `ButtonChoiceControlCore`
- [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md) `TextButtonExControlPane`
- [UID:0000HY][ButtonControlPane](by-file/ButtonControlPane.md) `ButtonControlPane`
- [UID:0000EJ][TextButtonControlPane](by-class/TextButtonControlPane.md) `TextButtonControlPane`
- [UID:00006D][ImageButtonControlPane](by-class/ImageButtonControlPane.md) `ImageButtonControlPane`
- [UID:0002O9][0x0058e380-0x0058e3d7.TextEditPaneCopyWideText](by-memory/0x0058e380-0x0058e3d7.TextEditPaneCopyWideText.md)
- [UID:0002RV][0x00595390-0x005954b3.TextBoxPaneConstructor](by-memory/0x00595390-0x005954b3.TextBoxPaneConstructor.md)
- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [UID:000161][0x004b8e00-0x004b8e11.GrafPortGetBounds](by-memory/0x004b8e00-0x004b8e11.GrafPortGetBounds.md)
- [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md)

## Changes

- 2026-06-13 B003 split repair: created the initial child page for the previously unsplit raw label-bounds helper island inside UID `000118` under the then-recorded `0x00495aa0-0x00495be2` endpoint. Evidence: MCP `lookup_funcs`, scoped `search_text`, `xrefs_to`, `get_bytes`, `make_signature_for_range`, constructor decompilation, and surrounding function analysis confirmed non-function status, provider/bounds behavior, and owner routing; the 2026-06-21 entry below corrects the one-byte endpoint defect.
- 2026-06-21 Rule 26/B007 source-quality incorporation:
  - Before: file/range and covered subranges ended at `0x00495be2`, the internal padding began at `0x00495b50`, the `this+0x10c` field and callee names were mostly raw placeholders, reachability was left as a C++ blocker, and formal C++ was blank.
  - Changed to: file/range `0x00495aa0-0x00495be3`, first helper `0x00495aa0-0x00495b51`, internal padding `0x00495b51-0x00495b60`, second helper `0x00495b60-0x00495be3`, `COMPLETION:88`, `CONFIDENCE:89`, and first-draft `TextButtonExControlPane::CalculateLabelRect` overload C++.
  - Summary/evidence: B007 raw PE/Capstone recheck proves both `ret imm16` endpoints include a final zero byte that the old page treated as padding; no direct branch/call or VA/RVA pointer route reaches either raw start; [UID:0002RV][0x00595390-0x005954b3.TextBoxPaneConstructor](by-memory/0x00595390-0x005954b3.TextBoxPaneConstructor.md) and [UID:0002O9][0x0058e380-0x0058e3d7.TextEditPaneCopyWideText](by-memory/0x0058e380-0x0058e3d7.TextEditPaneCopyWideText.md) support `TextBoxPane *m_labelTextBox` at `+0x10c`; [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md), inherited [UID:000161][0x004b8e00-0x004b8e11.GrafPortGetBounds](by-memory/0x004b8e00-0x004b8e11.GrafPortGetBounds.md), and [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) resolve `sub_4BAA70`, `sub_4B8E00`, and `sub_4B7CC0` dependency names. Owner/emitter remain [UID:0000EK][TextButtonExControlPane](by-class/TextButtonExControlPane.md); stale FittingRoom, TextButtonControlPane, ImageButtonControlPane, TextBoxPane/TextEditPane, direct GrafPort/BackPane helper ownership, and RectBounds ownership alternatives remain rejected.
