*** UID:0004CI | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000OR | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000OR | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void Thread::WaitForQueueDrain()
{
    HANDLE event = CreateEventW(NULL, TRUE, FALSE, NULL);

    ThreadResultEntry entry;
    entry.event = event;
    entry.result = 0;

    m_resultGate->Lock();
    m_resultList->Add(&entry);
    m_resultGate->Unlock();

    ThreadMessage message;
    message.messageId = 2;
    message.arg1 = 0;
    message.arg2 = 0;
    message.resultEvent = event;
    message.syncArg1 = 0;
    message.syncArg2 = 0;

    m_messageQueue->Push(&message);
    ReleaseSemaphore(m_waitHandles[0], 1, NULL);

    WaitForSingleObject(event, INFINITE);

    m_resultGate->Lock();
    for (int i = 0; i < m_resultList->GetCount(); ++i)
    {
        ThreadResultEntry *current =
            static_cast<ThreadResultEntry *>(m_resultList->GetAt(i));
        if (current->event == event)
        {
            m_resultList->RemoveAt(i);
            break;
        }
    }
    m_resultGate->Unlock();

    CloseHandle(event);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `Thread::WaitForQueueDrain()` child split from [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md); queues synchronous no-op message id `2`, waits on a result event, removes the result-list entry, and emits first-draft C++ under [UID:0000OR][Thread](by-file/Thread.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00596620-0x0059675e Thread Wait For Queue Drain

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: base `Thread` queue-drain barrier helper.
- Source owner decision: [UID:0000OR][Thread](by-file/Thread.md).
- Rebuild handling: source-authored utility/threading code.
- Confidence: strong for the exact range, queue-drain behavior, source-file ownership, and field/record roles; medium-high for exact original helper/API spellings.

## Address Range

`0x00596620-0x0059675e`, end-exclusive.

## Behavior

`Thread::WaitForQueueDrain()` creates an event, records `{event,0}` in the synchronous result list under `m_resultGate`, queues a synchronous no-op `ThreadMessage` with message id `2`, releases the primary wake semaphore at `m_waitHandles[0]`, waits for the event, removes the result entry, unlocks, and closes the event. This preserves the binary's duplicated result-removal logic rather than silently calling `Thread::WaitForResult`, because the compiled body at `0x00596620` does not call `0x00596760`.

## Reconstructed C++ Candidate

Formal C++ belongs in the validator-managed block after UID assignment:

```cpp
void Thread::WaitForQueueDrain()
{
    HANDLE event = CreateEventW(NULL, TRUE, FALSE, NULL);

    ThreadResultEntry entry;
    entry.event = event;
    entry.result = 0;

    m_resultGate->Lock();
    m_resultList->Add(&entry);
    m_resultGate->Unlock();

    ThreadMessage message;
    message.messageId = 2;
    message.arg1 = 0;
    message.arg2 = 0;
    message.resultEvent = event;
    message.syncArg1 = 0;
    message.syncArg2 = 0;

    m_messageQueue->Push(&message);
    ReleaseSemaphore(m_waitHandles[0], 1, NULL);

    WaitForSingleObject(event, INFINITE);

    m_resultGate->Lock();
    for (int i = 0; i < m_resultList->GetCount(); ++i)
    {
        ThreadResultEntry *current =
            static_cast<ThreadResultEntry *>(m_resultList->GetAt(i));
        if (current->event == event)
        {
            m_resultList->RemoveAt(i);
            break;
        }
    }
    m_resultGate->Unlock();

    CloseHandle(event);
}
```

## Evidence

- [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) records this range as the queue-drain barrier helper, with event creation, result-list insertion/removal, message id `2`, `m_messageQueue`, semaphore release, and wait/close behavior.
- The parent aggregate records nine direct callers across UI, terminal, and session cleanup/reset paths, proving generic `Thread` infrastructure rather than CashShop ownership.
- Command-id-2 barrier proof in [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) shows Socket async dispatch handles ids `3-14` and sync dispatch handles only `0x0c` specially, so id `2` falls through as a base no-op work item.

## Caveats

- `WaitForQueueDrain`, `RequestSyncGate::Lock`, `List::Add`, `List::GetAt`, `List::RemoveAt`, and `RingBuffer::Push` are source-quality inferred names. The behavior and ownership are strong enough for first-draft C++; exact original spelling remains a source-quality caveat.

## Cross-References

- [UID:0000OR][Thread](by-file/Thread.md)
- [UID:0000EV][Thread](by-class/Thread.md)
- [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md)

## Changes

- 2026-07-01 B007 implementation callback: created as an exact child of [UID:0001JY][0x00596620-0x005969b0.CashShopRequestWaitDispatch](by-memory/0x00596620-0x005969b0.CashShopRequestWaitDispatch.md) so the broad aggregate can become non-emitting without losing the source-owned `Thread::WaitForQueueDrain()` body.
