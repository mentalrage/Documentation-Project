*** UID:0004IU | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AN | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void PopupMenuControlPane::OnDraw()
{
    EPFTileContext leftTile;
    EPFTileContext centerTile;
    EPFTileContext rightTile;


    g_pResourceLayoutTable->LookupLayoutEntry(L"POPUPMNU.EPF", 0, &leftTile);
    g_pResourceLayoutTable->LookupLayoutEntry(L"POPUPMNU.EPF", 1, &centerTile);
    g_pResourceLayoutTable->LookupLayoutEntry(L"POPUPMNU.EPF", 2, &rightTile);

    const int leftWidth = leftTile.bounds.right - leftTile.bounds.left;
    const int rightWidth = rightTile.bounds.right - rightTile.bounds.left;

    RectBounds destination = m_bounds;
    destination.right = destination.left + leftWidth;
    m_drawMode = 0;
    RenderTileFrame(&leftTile, &leftTile.bounds, &destination, 0, L"NPAL8.PAL", 0);

    destination = m_bounds;
    destination.left += leftWidth;
    destination.right -= rightWidth;
    DrawTiledBackground(&destination, &centerTile, L"NPAL8.PAL");

    destination = m_bounds;
    destination.left = destination.right - rightWidth;
    RenderTileFrame(&rightTile, &rightTile.bounds, &destination, 0, L"NPAL8.PAL", 0);

    wchar_t selectedText[256];
    selectedText[0] = L'\0';
    if (m_selectedIndex != -1)
        m_menuPane->GetItem(m_selectedIndex)->GetText(selectedText, _countof(selectedText));

    SetTextColor(0x25);
    MoveTo(m_bounds.left + 1, m_bounds.bottom - 2);
    DrawWideText(selectedText, static_cast<int>(wcslen(selectedText)));

    if (!m_controlVisible) {
        m_drawMode = 2;
        SetDrawColor(0x8f);
        g_surfaceRenderCallbacks.fillOrInvalidate(this, &m_bounds);
    }

    if (m_controlVisible && m_controlActive) {
        SetDrawColor(0x80);
        m_drawMode = 0;
        DrawRectFrame(&m_bounds);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact PopupMenuControlPane draw virtual; primary vtable-only route composes POPUPMNU.EPF frames 0/1/2 with NPAL8.PAL, draws selected MenuItem text, and applies disabled/active frame states. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00498270-0x004984bc PopupMenuControlPane On Draw

## Status

- Exact range: `0x00498270-0x004984bc`, size `0x24c` / 588 bytes.
- Owner/emitter: [UID:0000AN][PopupMenuControlPane](by-class/PopupMenuControlPane.md).
- Reachability: primary vtable cell `0x00617ffc`; no ordinary direct callers, as expected for a virtual draw entry.

## Draw Flow

The method initializes three `EPFTileContext` locals and loads exact UTF-16 resource `POPUPMNU.EPF` frames 0, 1, and 2. Frame 0 draws the left cap, frame 1 tiles the center, and frame 2 draws the right cap. Exact palette string is `NPAL8.PAL`; byte reads during MCP session `bf5519ae` decoded both literals directly.

Destination widths come from each tile's `bounds.right - bounds.left`. With no selection the local 256-wide-character buffer remains empty. Otherwise the method obtains `m_menuPane->GetItem(m_selectedIndex)` and dispatches the `MenuItem::GetText` virtual into that buffer. It uses text color `0x25`, position `m_bounds.left + 1, m_bounds.bottom - 2`, and draws the full wide string.

The concrete dispatch is exact: `0x004983ff` obtains the selected `MenuItem *`; `0x00498404` executes `push 100h`; `0x00498409` addresses local `text`; and `0x00498414` executes `call dword ptr [edx+10h]`. The physical frame places `text` at `+0x90` and the next local at `+0x290`, so the destination occupies `0x200` bytes / 256 UTF-16 code units. For `StringMenuItem`, physical vtable cell `0x0061ebc4` resolves slot `+0x10` to [UID:0002J9][0x005170d0-0x005170e9.StringMenuItemGetText](by-memory/0x005170d0-0x005170e9.StringMenuItemGetText.md), whose current contract is `errno_t GetText(wchar_t *destination, size_t sizeInWords) const`.

The instruction after the call begins text-color setup. No comparison, branch, store, or later use consumes EAX, so `OnDraw` intentionally ignores the CRT status while consuming the caller-owned destination. This does not make the callee `void`: UID0002J9 returns `_wcscpy_s` status unchanged, including success `0`, `EINVAL` / `22`, and `ERANGE` / `34` with the CRT's valid-destination clearing behavior.

The disabled/not-visible path sets draw mode 2 and color `0x8f` before the shared Surface fill/invalidate callback. The visible active path sets color `0x80`, draw mode 0, and draws the bounds frame.

## Dependencies And Source Shape

ResourceLayout lookup, EPFTileContext, GrafPort tile/frame/text helpers, Surface callback infrastructure, MenuPane, and MenuItem are dependencies. None owns this class virtual. Compiler stack-cookie/EH/local cleanup artifacts are omitted; source local lifetimes regenerate them.

## Boundaries And Negatives

- Three `0xcc` bytes precede the body at `0x0049826d-0x00498270`.
- Four `0xcc` bytes follow at `0x004984bc-0x004984c0`.
- No ordinary code callers exist; the primary vtable cell is the positive route.
- Exact final ResourceLayout singleton public type and original event/helper spellings remain shared source-name caps, not blockers to this body.

## Score Rationale

`90/91` reflects exact body, vtable route, resources, frames/palette, geometry, text, state paths, dependencies, and source reconstruction. Shared renderer type naming and original private field spellings cap confidence below higher leaf-method values.

## Changes

- 2026-08-11 B010 UID0002J9 support synchronization:
  - Preserved `90/91`, owner/emitter UID0000AN, exact formal body, range, resources, geometry, and draw-state behavior.
  - Added the literal `0x00498404 push 0x100`, `0x00498414 call [edx+0x10]`, physical 256-wide local proof, UID0002J9/StringMenuItem slot resolution, and status-ignored/caller-buffer disposition.
- 2026-07-12 B005 accepted UID00011F implementation callback: created this exact virtual child with literal draw body and full resource/state/negative evidence.
