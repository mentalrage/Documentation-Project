*** UID:0002BX | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:80 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_UID:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** AUTOGEN_PARENT_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***

# 0x00526ce0-0x00526d9a MidiTrackBufferRefill

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: raw function-shaped helper
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for source-family ownership, byte boundary, source-state effects, and refill behavior; medium for direct reachability because IDA still does not model it as a function and reports no entry xrefs.
- Autogen status: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because the child and parent file now meet the >=80 confidence threshold.

## Score Rationale

The score is raised from `72/82` to `80/88` because live IDA MCP now records the exact bytes, raw instruction stream, no-function/no-entry-xref status, adapter-backed and Win32-backed seek/read branches, 0x400-byte refill clamp, track record counter updates, success/failure returns, and padding before the next modeled function. It is not raised higher because IDA still does not model the start as a function, no direct entry xref is known, and the final track-record/source-context type names remain provisional.

## Address Range

`0x00526ce0-0x00526d9a`.

IDA does not currently model this start as a function, but live disassembly shows a normal prologue, source I/O body, and return-shaped end before padding.

## Behavior

Refills a raw MIDI track buffer from the source using the track cursor and remaining byte count. Unlike the byte-reader helper, this range performs buffer refill/reset work without directly returning one byte to the caller.

The helper receives the per-track record pointer as its stack argument and uses the reader/source context in `ecx`. It exits early with failure status if the track remaining-byte counter at record offset `+0x24` is zero. Otherwise it resets the track buffer read pointer from record offset `+0x0c` into `+0x10`, seeks the source to the track cursor at record offset `+0x20`, reads up to `0x400` bytes or the remaining-byte count, updates record offsets `+0x08`, `+0x20`, and `+0x24`, and returns success only when the backing read succeeds and reports a nonzero byte count.

## Source-State And Boundary Notes

| Evidence | Meaning |
| --- | --- |
| `0x00526ce0-0x00526d9a` | Function-shaped raw body inside the MIDI reader/parser aggregate; immediately follows VLQ reader padding and ends before track-prime reset padding. |
| `this+0x14` | Adapter-active flag; zero selects the Win32 handle path, nonzero selects the adapter virtual path. |
| `this+0x18` | Adapter/source subobject used by the virtual seek/tell/read path. |
| `this+0x2c` | Win32 file handle used by `SetFilePointer` and `ReadFile` when the adapter path is inactive. |
| track `+0x08` | Current refill byte count or bytes actually read. |
| track `+0x0c` / `+0x10` | Buffer base and reset buffer cursor. |
| track `+0x20` | Source cursor, updated to `oldCursor + bytesRead`. |
| track `+0x24` | Remaining track byte count, decremented by bytes read. |
| `SetFilePointer` at `0x00526d21` | Repositions the Win32 file-backed source to the track cursor before refill. |
| `ReadFile` at `0x00526d63` | Reads the next chunk into the track buffer and writes the actual byte count into the local reused stack slot. |
| [UID:0002BT][0x005266b0-0x00526784.MidiTrackBufferedByteReader](by-memory/0x005266b0-0x00526784.MidiTrackBufferedByteReader.md) | Sibling byte-reader helper with the same per-track buffering model. |
| [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md) | Neighbor that primes all track buffers before event playback. |

## Evidence

- Parent page records this as a function-shaped raw body at `0x00526ce0`.
- IDA live disassembly starts with `push ebp`, `mov ebp, esp`, and register saves.
- IDA live analysis shows `SetFilePointer` at `0x00526d21` and `ReadFile` at `0x00526d63`, matching track-buffer refill behavior.

## 2026-06-06 Live IDA MCP Evidence

- IDB: `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe`, md5 `4247e04e20b65d6414c7238aa8ff5515`, sha256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- `lookup_funcs` still reports `0x00526ce0` and `0x00526d9a` as not function objects; the next modeled function remains [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md) at `0x00526da0-0x00526f09`.
- `xrefs_to` and `callers` for `0x00526ce0` are empty. IDA records internal target-side refs such as `0x00526d1e -> SetFilePointer` and `0x00526d60 -> ReadFile` with `fn:null`, matching an unmodeled caller range.
- Raw bytes for `0x00526ce0-0x00526d9d` begin with `55 8b ec 53 56 57`, contain the complete refill body, end the live body with `retn 4` at `0x00526d97`, and show `0xcc` padding at `0x00526d9a-0x00526d9c` before the modeled prime/reset function.
- The adapter path checks `this+0x14`, uses the subobject at `this+0x18`, calls virtual slots `+0x18` and `+0x14` around the seek/tell step, and calls virtual slot `+0x20` for the read step.
- The Win32 path uses handle `this+0x2c`, calls `SetFilePointer` at `0x00526d21`, clamps the requested read length to `0x400`, then calls `ReadFile` at `0x00526d63`.
- Track record updates are explicit: `+0x10` is reset from `+0x0c`, `+0x08` receives the requested/actual refill count, `+0x24` is decremented by bytes read, and `+0x20` advances by `sourcePosition + bytesRead`.
- Return shape is binary: a successful nonzero read returns `0` at `0x00526d84-0x00526d88`; exhausted/failed paths store the last byte count and return `1` at `0x00526d90-0x00526d97`.

## Reconstruction Notes

Keep the raw-start caveat until IDA/Wave3 models this as a function or another caller path confirms the exact entry. `RECONSTRUCTABLE:TRUE` and `AUTOGEN_PARENT_UID:0000LD` are appropriate because the helper is source-authored MIDI reader logic, but the C++ block remains blank until track record and source adapter types are stable.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)
- [UID:0002BT][0x005266b0-0x00526784.MidiTrackBufferedByteReader](by-memory/0x005266b0-0x00526784.MidiTrackBufferedByteReader.md)
- [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md)

## Changes

- 2026-06-02 low-score pass:
  - What existed before: range, behavior, and evidence were documented, but metadata stayed at `58/72` and reconstructable was blank.
  - Changed to: scores `68/76`, `RECONSTRUCTABLE:TRUE`, explicit autogen deferral, and score rationale.
  - Summary/evidence: raw prologue, `SetFilePointer`/`ReadFile` calls, source cursor/refill behavior, and the MIDI reader aggregate support reconstructable project-code status without final C++.
- 2026-06-03 parent attachment:
  - What existed before: the helper remained unattached because its parent/source confidence was treated as provisional.
  - Changed to: score `72/82` and `AUTOGEN_PARENT_UID:0000LD`.
  - Summary/evidence: the owning `MidiPlayer` file is now at `80/84` with `NexusTK/audio/` placement, and this page records the source-state/boundary links needed for attachment; final C++ remains blank.
- 2026-06-06 live IDA evidence pass:
  - What existed before: score `72/82` with broad refill behavior, source owner, and SetFilePointer/ReadFile evidence.
  - Changed to: score `80/88`; reconstructable status, parent UID, and blank C++ block are retained.
  - Summary/evidence: live IDA reconfirmed no function object and no direct entry xrefs, exact byte boundaries and padding, adapter-backed virtual seek/tell/read slots, Win32 `SetFilePointer`/`ReadFile` path, 0x400-byte refill clamp, track record field effects, and success/failure return shape.
