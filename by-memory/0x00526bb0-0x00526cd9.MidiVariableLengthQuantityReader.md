*** UID:0002BW | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000LD | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static int MidiReadVariableLength(MidiDocument* document, MidiTrackState* track, DWORD* value)
{
    DWORD decoded;

    decoded = 0;

    if ((track->flags & MIDI_TRACK_DONE) != 0) {
        return 1;
    }

    for (;;) {
        BYTE byteValue;

        if (track->bufferedBytes == 0) {
            DWORD sourceOffset;
            DWORD bytesToRead;
            DWORD bytesRead;
            int readOk;

            if (track->bytesRemaining == 0) {
                track->flags |= MIDI_TRACK_DONE;
                return 1;
            }

            track->cursor = track->buffer;

            if (document->datFileSource != 0) {
                document->datFile.Seek(track->sourceOffset, FILE_BEGIN);
                sourceOffset = document->datFile.Tell();
            } else {
                sourceOffset = SetFilePointer(document->fileHandle, track->sourceOffset, NULL, FILE_BEGIN);
            }

            if (sourceOffset == INVALID_SET_FILE_POINTER) {
                return 1;
            }

            bytesToRead = track->bytesRemaining;
            if (bytesToRead > 0x400) {
                bytesToRead = 0x400;
            }

            bytesRead = bytesToRead;
            track->bufferedBytes = bytesToRead;

            if (document->datFileSource != 0) {
                readOk = document->datFile.Read(track->buffer, bytesToRead);
            } else {
                readOk = ReadFile(document->fileHandle, track->buffer, bytesToRead, &bytesRead, NULL);
            }

            track->bytesRemaining -= bytesRead;
            track->bufferedBytes = bytesRead;
            track->sourceOffset = sourceOffset + bytesRead;

            if (readOk == 0 || bytesRead == 0) {
                return 1;
            }
        }

        byteValue = *track->cursor++;
        --track->bufferedBytes;

        decoded = (decoded << 7) | (byteValue & 0x7F);
        if ((byteValue & 0x80) == 0) {
            *value = decoded;
            return 0;
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Attached to `MidiPlayer.cpp`; formal `MidiReadVariableLength` now decodes SMF VLQ values with inline 0x400-byte DATFile/Win32 track-buffer refill and binary success/failure return. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00526bb0-0x00526cd9 MidiVariableLengthQuantityReader

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: function
- Parent range: [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- Likely source module: [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- Rebuild handling: source-authored
- Confidence: strong for behavior, modeled boundary, caller set, inline refill field map, VLQ accumulation, error flag behavior, padding, accepted private helper name, and MidiPlayer source-family ownership; medium for unrecovered original symbol spelling and final shared `MidiDocument`/`MidiTrackState` type definitions
- Parent assignment: attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) because this child and the parent file both meet the >=80 confidence threshold.

## Address Range

`0x00526bb0-0x00526cd9`.

IDA models this exact range as `sub_526BB0`, size `0x129`.

## Behavior

Reads a Standard MIDI File variable-length quantity from the track stream, refilling the track buffer when needed, and returns the decoded delta-time or length value to the caller.

The formal draft helper name is `MidiReadVariableLength`. It is a private file-scope `MidiPlayer.cpp` helper over `MidiDocument` and `MidiTrackState`, not a `MidiPlayer` object method and not a `DATFile` method. It returns `0` after writing the decoded value and `1` for already-done tracks, seek/read exhaustion, or refill failure.

## VLQ Role

| Caller or use | Evidence | Meaning |
| --- | --- | --- |
| [UID:0002BU][0x00526790-0x005269ea.MidiEventReader](by-memory/0x00526790-0x005269ea.MidiEventReader.md) | Existing caller evidence records this helper in event parsing. | Decodes event delta times and variable payload lengths. |
| [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md) | Existing caller evidence records this helper during track priming. | Decodes each track's first delta before playback starts. |
| Track stream refill path | IDA live disassembly shows `SetFilePointer` and `ReadFile` references in refill paths. | The reader operates over the same 0x400-byte buffered track source as the byte reader. |
| [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md) | Live IDA caller `0x005276bf`. | Decodes variable-length values during SMF document loading. |

## Boundary And Relationships

| Range or target | Relationship |
| --- | --- |
| `0x00526bb0-0x00526cd9` | IDA-modeled body `sub_526BB0`, size `0x129`. |
| `0x00526ba8-0x00526bb0` | Internal alignment padding after [UID:0002BV][0x005269f0-0x00526ba8.MidiStreamEventEncoder](by-memory/0x005269f0-0x00526ba8.MidiStreamEventEncoder.md). |
| `0x00526cd9-0x00526ce0` | Internal alignment padding before [UID:0002BX][0x00526ce0-0x00526d9a.MidiTrackBufferRefill](by-memory/0x00526ce0-0x00526d9a.MidiTrackBufferRefill.md). |

## Evidence

- IDA `lookup_funcs` confirms `sub_526BB0` at `0x00526bb0-0x00526cd9`.
- IDA live disassembly shows file-position and read references to `SetFilePointer` and `ReadFile` in refill paths.
- The MIDI event reader at `0x00526790` and track-prime helper at `0x00526da0` call this helper.

## 2026-06-06 Live IDA MCP Evidence

Live IDA MCP against NexusTK.exe MD5 `4247e04e20b65d6414c7238aa8ff5515` reconfirms:

- IDA models `sub_526BB0` exactly at `0x00526bb0-0x00526cd9`, size `0x129`.
- Direct entry xrefs are `0x005268e5`, `0x0052695f`, and `0x005269cc` from [UID:0002BU][0x00526790-0x005269ea.MidiEventReader](by-memory/0x00526790-0x005269ea.MidiEventReader.md), `0x00526eb3` from [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md), and `0x005276bf` from [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md).
- The helper rejects track records with flag bit `+0x00 & 1`, and when no bytes remain at `+0x24`, it sets that flag bit and returns failure.
- The inline refill path uses the same field map as the byte reader: `+0x08` buffered count, `+0x0c` buffer pointer, `+0x10` cursor, `+0x20` source offset, and `+0x24` remaining source bytes.
- Refill uses adapter vtable slots `+0x18`, `+0x14`, and `+0x20` or Win32 `SetFilePointer`/`ReadFile` through handle `this+0x2c`, clamps to `0x400`, and updates the remaining/buffered/source-offset fields.
- VLQ accumulation reads bytes from cursor `+0x10`, decrements buffered count `+0x08`, accumulates `(value << 7) | (byte & 0x7f)`, continues while the high bit is set, writes the decoded value to the caller out pointer, and returns `0` on success.
- Padding before and after isolates the helper between [UID:0002BV][0x005269f0-0x00526ba8.MidiStreamEventEncoder](by-memory/0x005269f0-0x00526ba8.MidiStreamEventEncoder.md) and [UID:0002BX][0x00526ce0-0x00526d9a.MidiTrackBufferRefill](by-memory/0x00526ce0-0x00526d9a.MidiTrackBufferRefill.md).

## 2026-06-16 Live IDA MCP Evidence

Live IDA MCP session `b001_mappane_0001AW_20260616` reconfirms:

- `lookup_funcs` still models `sub_526BB0` exactly at `0x00526bb0`, size `0x129`.
- `xrefs_to 0x00526bb0` reports five direct call sites: `0x005268e5`, `0x0052695f`, and `0x005269cc` in [UID:0002BU][0x00526790-0x005269ea.MidiEventReader](by-memory/0x00526790-0x005269ea.MidiEventReader.md), `0x00526eb3` in [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md), and `0x005276bf` in [UID:0002C7][0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper](by-memory/0x005273a0-0x005277bb.MidiSMFDocumentLoadHelper.md).
- Current decompilation gives `int __thiscall sub_526BB0(int this, DWORD trackRecord, int *outValue)` and shows no named project type for either context pointer.
- The helper immediately rejects records whose flags at `+0x00` already have bit `1` set. If no source bytes remain at `+0x24`, it sets that same flag bit and returns failure.
- Inline refill repeats the byte-reader field map and source paths: reset cursor `+0x10` from buffer `+0x0c`, seek to `+0x20` through adapter slots `+0x18/+0x14` or `SetFilePointer`, clamp the next read to `0x400`, read through adapter slot `+0x20` or `ReadFile`, then update `+0x08`, `+0x20`, and `+0x24`.
- The VLQ loop reads one byte at a time from cursor `+0x10`, decrements `+0x08`, accumulates `value = (value << 7) | (byte & 0x7f)`, continues while the high bit is set, writes the decoded value through `outValue`, and returns `0`.
- `analyze_component` keeps this helper internal to the MIDI reader core with callers from event-reader, track-prime, and SMF-loader functions; this supports private `MidiPlayer` source-family ownership but not a public API name.
- `get_bytes 0x00526ba8 size 8` and `get_bytes 0x00526cd9 size 7` return all `0xcc`, preserving both adjacent boundaries.
- IDA local type and by-type searches did not find a project SMF/VLQ/track-record type. Only Win32 `midihdr_tag` and unrelated mouse-track types matched the broad searches, so the final helper/type names remain unresolved rather than ignored.

## 2026-06-29 B008 Source-Quality Implementation

B008's accepted implementation callback uses current MCP session `992d23de` and the accepted MIDI source-family naming precedent to resolve the old blank-C++ blocker for this exact child. The session confirmed `sub_526BB0` at `0x00526bb0-0x00526cd9`, the five direct callers (`0x005268e5`, `0x0052695f`, `0x005269cc`, `0x00526eb3`, and `0x005276bf`), and both `0xcc` padding spans at `0x00526ba8-0x00526bb0` and `0x00526cd9-0x00526ce0`.

The body is represented as `static int MidiReadVariableLength(MidiDocument* document, MidiTrackState* track, DWORD* value)`. The helper checks `MIDI_TRACK_DONE` before reading, sets the same flag when no track bytes remain, refills the per-track 0x400-byte buffer inline rather than calling [UID:0002BT][0x005266b0-0x00526784.MidiTrackBufferedByteReader](by-memory/0x005266b0-0x00526784.MidiTrackBufferedByteReader.md), and decodes each VLQ byte with `decoded = (decoded << 7) | (byteValue & 0x7f)` until the byte lacks bit `0x80`. The refill path seeks through embedded `DATFile::Seek`/`Tell` when `document->datFileSource` is active, otherwise seeks and reads through the Win32 `fileHandle` with `SetFilePointer` and `ReadFile`. It updates the track buffered count, cursor, remaining-byte count, and source offset before returning failure on failed or zero-length reads.

`MidiReadVariableLength` is preferred over the documentation title `MidiVariableLengthQuantityReader`, over longer `MidiReadVariableLengthQuantity` or acronym `MidiReadVLQ`, over a `MidiPlayer::` member form, over `DATFile` ownership, and over covered-by-parent or new `MidiSMFDocument.cpp` placement. The name matches the already accepted private helper declaration used by emitted sibling bodies while keeping original-symbol absence as a confidence cap. No current IDA local type, by-type page, string, or symbol proves original `MidiDocument`/`MidiTrackState` declarations, so the shared declaration gap remains source-family context rather than a reason to keep this exact emitting child blank.

## Score Rationale

Raised from `86/90` to `88/91` on 2026-06-29. Current MCP-backed evidence plus accepted MIDI source-family precedent resolves the exact helper name, owner/emitter route, binary success/failure contract, inline refill behavior, caller set, padding, and formal first-draft C++ for this child. The score remains below final-audit range because original symbol spellings and dedicated by-type definitions for `MidiDocument`, `MidiTrackState`, and related source-adapter fields are still unrecovered.

## Reconstruction Notes

This should be represented as a local SMF VLQ reader, probably taking a document/track context pointer.

`RECONSTRUCTABLE:TRUE` is appropriate because the body is source-authored Standard MIDI File parser logic. The reconstruction C++ block is now populated with the accepted `MidiReadVariableLength` helper body. Earlier blank-C++/source-name caution is historical: descriptive source-facing MIDI names are accepted for first-draft output, while unrecovered original symbols and shared type pages remain confidence caps.

## Changes

- 2026-06-29 B008 implementation: raised from `86/90` to `88/91` and inserted the formal private file-scope `static int MidiReadVariableLength(MidiDocument* document, MidiTrackState* track, DWORD* value)` body. Evidence: current MCP session `992d23de` confirms exact bounds, five callers, inline 0x400-byte DATFile/Win32 refill, `MIDI_TRACK_DONE` failure behavior, VLQ accumulation, output write, and both padding spans. The owner/emitter route remains [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md) / `NexusTK/audio/MidiPlayer.cpp`; rejected alternatives are `MidiPlayer::` method, `DATFile` owner, covered-by-parent marker, and new unproven `MidiSMFDocument.cpp` source placement.
- 2026-06-02: Raised from `66/80` to `73/84`, marked reconstructable, and attached to [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md). Evidence: IDA-recorded exact function boundary, event-reader and track-prime caller relationships, and documented buffered track-stream refill behavior.
- 2026-06-06: Raised from `73/84` to `80/88` after live IDA MCP reconfirmed exact `sub_526BB0` bounds, event-reader/track-prime/SMF-loader callers, track error flag, buffered field map, 0x400-byte inline refill, VLQ `(value << 7) | (byte & 0x7f)` accumulation, decoded-output write, and padding. Final C++ remains blank below the source-grade threshold.
- 2026-06-16: Raised from `80/88` to `86/90` after live IDA MCP `b001_mappane_0001AW_20260616` reconfirmed exact bounds, all five caller sites, decompiled error/refill/VLQ paths, internal-only component role, and both `0xcc` padding spans. Final C++ remains blank because source-quality track/source/helper names were checked in IDA local types and by-type docs and remain unavailable.

## Cross-References

- [UID:00023B][0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers](by-memory/0x00526650-0x005270d5.MidiFileReaderAndSMFParserHelpers.md)
- [UID:0002BU][0x00526790-0x005269ea.MidiEventReader](by-memory/0x00526790-0x005269ea.MidiEventReader.md)
- [UID:0002BV][0x005269f0-0x00526ba8.MidiStreamEventEncoder](by-memory/0x005269f0-0x00526ba8.MidiStreamEventEncoder.md)
- [UID:0002BX][0x00526ce0-0x00526d9a.MidiTrackBufferRefill](by-memory/0x00526ce0-0x00526d9a.MidiTrackBufferRefill.md)
- [UID:0002BY][0x00526da0-0x00526f09.MidiTrackBufferPrimeReset](by-memory/0x00526da0-0x00526f09.MidiTrackBufferPrimeReset.md)
- [UID:0000LD][MidiPlayer](by-file/MidiPlayer.md)
- [UID:00008A][MidiPlayer](by-class/MidiPlayer.md)
