*** UID:0004GG | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000055 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000055 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void FittingRoomTextButtonControlPane::OnPaint()
{
    const unsigned int kNormalTopLeftColor = 208;
    const unsigned int kNormalBottomRightColor = 216;
    const unsigned int kPressedTopLeftColor = 216;
    const unsigned int kPressedBottomRightColor = 208;
    const unsigned int kButtonFillColor = 212;
    const unsigned int kEnabledTextColor = 128;
    const unsigned int kDisabledTextColor = 134;
    const unsigned char kPressedVisualState = 11;
    const int kLabelHalfHeight = 6;

    const bool pushed = (m_visualState == kPressedVisualState || m_selected);

    unsigned int topLeftColor = kNormalTopLeftColor;
    unsigned int bottomRightColor = kNormalBottomRightColor;
    if (pushed) {
        topLeftColor = kPressedTopLeftColor;
        bottomRightColor = kPressedBottomRightColor;
    }

    const unsigned int textColor = m_controlVisible ? kEnabledTextColor : kDisabledTextColor;

    RectBounds frame = m_bounds;

    SetDrawColor(kButtonFillColor);
    FillRect(&frame);

    MoveTo(frame.left, frame.bottom - 1);
    SetDrawColor(topLeftColor);
    LineTo(frame.left, frame.top);
    LineTo(frame.right - 1, frame.top);
    SetDrawColor(bottomRightColor);
    LineTo(frame.right - 1, frame.bottom - 1);
    LineTo(frame.left, frame.bottom - 1);

    InsetRect(&frame, 1, 1);

    MoveTo(frame.left, frame.bottom - 1);
    SetDrawColor(topLeftColor);
    LineTo(frame.left, frame.top);
    LineTo(frame.right - 1, frame.top);
    SetDrawColor(bottomRightColor);
    LineTo(frame.right - 1, frame.bottom - 1);
    LineTo(frame.left, frame.bottom - 1);

    RectBounds textRect = m_bounds;
    const int textCenterY = (textRect.top + textRect.bottom) / 2;
    textRect.top = textCenterY - kLabelHalfHeight;
    textRect.bottom = textCenterY + kLabelHalfHeight;

    if (pushed) {
        OffsetRect(&textRect, 1, 1);
    }

    SetTextColor(textColor);
    m_textDrawMode = 1;
    m_textAlign = 1;

    DrawTextInRect(m_text, static_cast<int>(wcslen(m_text)), &textRect);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready FittingRoomTextButtonControlPane::OnPaint body; vtable-only handler fills the button, draws normal/pressed bevel frames, handles disabled text color, centers and offsets the label, and emits through [UID:000055][FittingRoomTextButtonControlPane](by-class/FittingRoomTextButtonControlPane.md). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# FittingRoomTextButtonControlPane OnPaint `0x00424080-0x0042425d`

## Status

- Disposition: exact paint-handler child of [UID:0002SC][0x00424020-0x004242e4.FittingRoomTextButtonControlPane](by-memory/0x00424020-0x004242e4.FittingRoomTextButtonControlPane.md).
- Owner: [UID:000055][FittingRoomTextButtonControlPane](by-class/FittingRoomTextButtonControlPane.md).
- Source route: emits first-draft formal `FittingRoomTextButtonControlPane::OnPaint()` through [UID:000055][FittingRoomTextButtonControlPane](by-class/FittingRoomTextButtonControlPane.md) into [UID:0000JE][FittingRoom](by-file/FittingRoom.md). The exact original palette-constant spellings and `+0x10c` text-member spelling remain confidence caps, but accepted helper/field evidence now supports formal source.

## Live IDA MCP Evidence

- Current MCP session `supervisor_nexustk_20260709` was healthy during the evidence pass: `idb_list` reported the active NexusTK database, and `server_health` returned `ok`, auto-analysis ready, and Hex-Rays ready.
- Current `lookup_funcs(0x00424080)` resolves `sub_424080`, size `0x1dd`; `lookup_funcs(0x0042425d)` is not a function, and `lookup_funcs(0x00424260)` resolves the selected-state setter `sub_424260`, size `0x10`.
- Raw bytes at `0x00424080` start `55 8b ec 83 ec 18 a1 24 2f 67 00 33 c5 89 45 fc`; raw bytes at `0x0042425d` are `cc cc cc` before the setter prologue at `0x00424260`. The child range remains exactly `0x00424080-0x0042425d`.
- `xrefs_to 0x00424080` has one data xref at `0x0060e018` and no ordinary code xrefs; `find_bytes 80 40 42 00` matches only `0x0060e018`, confirming the vtable-only paint-slot route.
- Vtable/data bytes around `0x0060e000` include the target pointer at `0x0060e018`, adjustor pointers at `0x0060e040` and `0x0060e070`, and the neighboring scalar deleting destructor pointer at `0x0060dfd4`.
- Current `callees(0x00424080)` includes `0x004b9660` (`SetDrawColor`), `0x0069b3fc` (surface fill/presentation callback used here as `FillRect`), `0x004b9600` (`MoveTo`), `0x004b98f0` (`LineTo`), `0x004b7e30` (`InsetRect`), `0x004b7e10` (`OffsetRect`), `0x004b9680` (`SetTextColor`), `0x004bad70` (`DrawTextInRect`), plus the compiler security-cookie check.
- Decompilation shows that `m_visualState` at `this+0x103` equal to `11` or `m_selected` at `this+0x110` chooses pressed colors `216/208`; the normal state chooses `208/216`. [UID:0004GH][0x00424260-0x00424270.FittingRoomTextButtonControlPaneSetSelected](by-memory/0x00424260-0x00424270.FittingRoomTextButtonControlPaneSetSelected.md) names the selected byte as `m_selected`.
- `m_controlVisible` at `this+0x101` selects text color `128`, with disabled/invisible state using `134`; the fill color before the rectangle callback is `212`.
- The handler copies `m_bounds` from `this+0x44`, fills the rectangle, draws an outer bevel frame, insets by `(1,1)`, draws an inner bevel frame, recomputes a text rectangle from the original bounds, centers it with top `center - 6` and bottom `center + 6`, offsets it by `(1,1)` when pressed/selected, sets text color, writes `this+0x88 = 1` and `this+0x8c = 1`, scans the UTF-16 label pointer at `this+0x10c`, and calls `DrawTextInRect`.
- Historical B005 MCP session `c9b60f19` recorded the same size, vtable reference, callee set, branch/color behavior, and padding boundary; the current session refresh supersedes the old blank-code blocker.

## Source-Ready C++ Proof

The paint behavior is target-specific source behavior, not compiler-only ABI output. The previous no-code proof is historical: current support docs name the target-critical helpers (`SetDrawColor`, `MoveTo`, `LineTo`, `InsetRect`, `OffsetRect`, `SetTextColor`, `DrawTextInRect`, and the fill callback role), and [UID:0004GH][0x00424260-0x00424270.FittingRoomTextButtonControlPaneSetSelected](by-memory/0x00424260-0x00424270.FittingRoomTextButtonControlPaneSetSelected.md) names `m_selected`. The formal block uses descriptive palette constants and an inferred `m_text` member for `+0x10c`; both spellings are documented confidence caps, not blockers.

Generated output was stale before this callback: `auto-generated/NexusTK/cashshop/FittingRoom.cpp` command `000000008071`, refreshed `2026-07-09T11:53:24-04:00`, had only the class marker plus source-ready constructor/setter children and omitted UID0004GG. The tracker direct-report count `0` is resolved by B004's UID0004GG report and does not imply bad ownership.

## Rejected Alternatives

- Aggregate parent C++: rejected because this paint body sits in a mixed range with constructor, setter, destructor glue, adjustor thunks, and padding.
- PrettyButton or generic button ownership: rejected because the vtable reference and constructor/caller context tie this handler to FittingRoomTextButtonControlPane.
- Disabled-frame overlay interpretation: rejected for this range; the bytes choose button colors and label colors but do not prove a separate overlay source object.
- Blank formal OnPaint C++: rejected for the current state because helper and field names are now source-quality enough for a first-draft method body; exact original constant spellings remain a confidence cap only.

## Score Rationale

Completion is `88` because the range, size, vtable-only route, exact successor padding, callee set, branch conditions, color values, field offsets, text-centering behavior, rejected owner alternatives, generated stale state, and formal source body are now documented. Confidence is `91` because current MCP evidence is strong for behavior and ownership; it stays below final because the original palette constant names, exact `+0x10c` text-member spelling, and broad class/header source shape remain inferred.
