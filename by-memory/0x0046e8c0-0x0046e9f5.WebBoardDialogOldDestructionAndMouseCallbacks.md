*** UID:000212 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000G3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000G3 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
WebBoardDialogOld::~WebBoardDialogOld()
{
    g_pWebBoardDialogOld = NULL;
}

void WebBoardDialogOld::OnControlCommand(int controlIndex, int notifyCode)
{
    (void)notifyCode;

    if (controlIndex != 1)
        return;

    OnBrowserDialogClosed();
    m_browserControlPane->ShutdownBrowserWindow();
    OnDestroy();
    OnHide();
    g_pApplicationCleanupQueue->QueuePaneForDeferredDeletion(this);
}

bool WebBoardDialogOld::HandlePointerOrMouseEvent(Event *event)
{
    if (event->m_type != 0 || !m_isTitleDragActive)
        return DialogPane::HandlePointerOrMouseEvent(event);

    const bool handled = DialogPane::HandlePointerOrMouseEvent(event);

    RECT browserClient;
    ::GetClientRect(g_activeBrowserControlPane->m_browserHostHwnd, &browserClient);

    RectBounds dialogBounds;
    GetScreenBounds(&dialogBounds);

    ::MoveWindow(m_browserControlPane->m_browserHostHwnd,
                 dialogBounds.left + 30,
                 dialogBounds.top + 30,
                 browserClient.right - browserClient.left,
                 browserClient.bottom - browserClient.top,
                 FALSE);

    g_pScreenPane->RenderPresentation();
    return handled;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact reconstructable WebBoardDialogOld three-method cluster: 0x0046e8c0 ordinary destructor clears only g_pWebBoardDialogOld; primary +0x48 OnControlCommand(int,int) gates on control-list index 1 and performs lifecycle-hook, browser shutdown, OnDestroy, OnHide, and deferred-delete sequencing without null guards or a member clear; secondary +0x04 HandlePointerOrMouseEvent(Event *) preserves adjusted-view/base-call order and directly repositions m_browserControlPane->m_browserHostHwnd by +30,+30 before RenderPresentation. Exact function/padding ranges, vtable/RTTI/deleting routes, BrowserPane size 0x26c, derived pointer +0x26c, opaque retained storage [+0x270,+0x278), complete size 0x278, direct hosted-HWND access, and formal three-method CPP/parent-owned H disposition are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0046e8c0-0x0046e9f5 WebBoardDialogOld Destruction And Mouse Callbacks

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: class destructor and vtable callback cluster
- Current generated owner: incompletely/polluted under `RankingDialog` and generated browser-dialog data
- Current owner hypothesis: [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md)
- Likely source module: [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- Rebuild handling: source-authored `WebBoardDialogOld` destructor/callback code; internal `0xcc` alignment is compiler/linker output
- Completion: 92%
- Confidence: very strong for exact function boundaries, canonical inherited callback contracts, vtable ownership, singleton cleanup, control-command teardown, direct hosted-HWND reposition behavior, complete object extent, padding bytes, and first-draft C++ readiness. Confidence remains below final only for original lexical spelling of the folded lifecycle hook and the semantic names of the retained eight bytes.

## Autogen Status

- Reconstructable: true. The destructor and callback bodies are source-owned `WebBoardDialogOld` behavior, even though the surrounding vtable slots and alignment are compiler/linker output.
- Autogen parent: attached to [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md). The parent is above the attachment threshold and already documents this destructor/callback cluster.
- Reconstruction status: formal C++ now uses the canonical `OnControlCommand(int,int)` and `HandlePointerOrMouseEvent(Event *)` inherited contracts, exact unchecked dereferences and call order, direct `m_browserHostHwnd` fields, and parent-owned declarations. Raw names and the superseded close-state/input-event interpretation are retained only as dated history.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 92 | Live IDA confirms exact function sizes/hashes, adjacent vtable slots, source/compiler boundaries, call order, singleton clear, BrowserPane restoration/deleting route, `controlIndex == 1`, direct hosted-HWND fields, fixed `+30,+30` movement, exact `0x278` extent, and implementation-ready CPP/H routing. |
| Confidence | 94 | Base-vtable contracts resolve both callback identities and signatures; allocation/layout, xrefs, fields, globals, negative API/null/clear checks, and exact ranges agree. Lost folded-hook spelling and semantically unobservable retained bytes cap the score below 95 without leaving a source blocker. |

## Covered Range

| Range | Current method or group | Notes |
| --- | --- | --- |
| `0x0046e8c0-0x0046e8e9` | `WebBoardDialogOld::~WebBoardDialogOld()` | Clears [UID:0000SR][g_pWebBoardDialogOld](by-global/g_pWebBoardDialogOld.md) / historical `0x0067aba0`, restores `BrowserPane` vtables on the primary and secondary subobjects, and tail-calls the base teardown/destructor path. This is the ordinary source destructor body, not the deleting wrapper. |
| `0x0046e8e9-0x0046e8f0` | internal alignment | Seven bytes of `0xcc` between destructor and the next vtable callback. |
| `0x0046e8f0-0x0046e92b` | `WebBoardDialogOld::OnControlCommand(int controlIndex, int notifyCode)` | Primary vtable slot `+0x48` at `0x006136d4`; `controlIndex == 1` is the ordered control-list index, not resource id `30`. The exact sequence is primary `+0x68` (`OnBrowserDialogClosed`), `m_browserControlPane->ShutdownBrowserWindow()`, primary `+0x38` (`OnDestroy`), primary `+0x40` (`OnHide`), then deferred deletion through [UID:0000Q8][g_pApplicationCleanupQueue](by-global/g_pApplicationCleanupQueue.md). There are no member/global null guards and no post-shutdown member clear. |
| `0x0046e92b-0x0046e930` | internal alignment | Five bytes of `0xcc` before the secondary-subobject input callback. |
| `0x0046e930-0x0046e9f5` | `WebBoardDialogOld::HandlePointerOrMouseEvent(Event *event)` | Secondary EventHandler slot `+0x04` at `0x00613704`. Physical `this` is complete object `+0xa0`; normalization maps the inherited drag flag to complete `+0x208` and `m_browserControlPane` to first derived field `+0x26c`. Non-primary events or inactive drag return the base result immediately; the active path calls the base first, reads `g_activeBrowserControlPane->m_browserHostHwnd`, obtains `RectBounds` through primary `+0x28` (`GetScreenBounds`), moves `m_browserControlPane->m_browserHostHwnd` by `+30,+30`, calls `g_pScreenPane->RenderPresentation()`, and returns the saved base result. No event/global/member/screen null guard is present. |

## IDA MCP Evidence

Checked on 2026-05-27:

- IDA function enumeration over `0x0046e8be-0x0046ea00` finds modeled functions at `0x0046e8c0-0x0046e8e9`, `0x0046e8f0-0x0046e92b`, and `0x0046e930-0x0046e9f5`.
- The bytes `0x0046e8be-0x0046e8c0`, `0x0046e8e9-0x0046e8f0`, `0x0046e92b-0x0046e930`, and `0x0046e9f5-0x0046ea00` are `0xcc` alignment padding.
- Vtable xrefs place `0x0046e8f0` at `WebBoardDialogOld` vtable entry `0x006136d4`, and `0x0046e930` at secondary vtable entry `0x00613704`.
- `0x0046e8c0` writes `dword_67aba0 = 0`, then restores `BrowserPane` vtable pointers at offsets `0`, `0xa0`, and `0xa4` before entering the base destructor path.
- `0x0046e8f0` uses object slot `+0x26c`, which matches the old-dialog browser-control pointer established by the constructor and nearby raw helper coverage.
- `0x0046e930` delegates to `sub_49e240`, then calls `GetClientRect(dword_67ab28 + 0x11c)`, asks the primary object (`this - 0xa0`) for its rectangle through virtual slot `+0x28`, calls `MoveWindow` on the embedded browser HWND at `(rect.left + 30, rect.top + 30)`, and refreshes `dword_67a7cc` through `sub_557140`.
- 2026-06-05 live IDA MCP against `NexusTK.exe` md5 `4247e04e20b65d6414c7238aa8ff5515` confirms exact modeled functions: `sub_46E8C0` is `0x0046e8c0-0x0046e8e9` (`0x29` bytes), `sub_46E8F0` is `0x0046e8f0-0x0046e92b` (`0x3b` bytes), `sub_46E930` is `0x0046e930-0x0046e9f5` (`0xc5` bytes), and the next packet callback [UID:0000ZC][0x0046ea00-0x0046ea23.WebBoardDialogOldPacketForwarder](by-memory/0x0046ea00-0x0046ea23.WebBoardDialogOldPacketForwarder.md) begins at `0x0046ea00`.
- 2026-06-05 live vtable audit places `sub_46E8F0` at `0x006136d4`, `sub_46E930` at `0x00613704`, and adjacent packet forwarder `sub_46EA00` at `0x00613710`, with the `WebBoardDialogOld` RTTI pointer at `0x006136fc`.
- 2026-06-05 live xrefs show no refs to the ordinary destructor `0x0046e8c0`, a vtable data ref to `sub_46E8F0` from `0x006136d4`, a vtable data ref to `sub_46E930` from `0x00613704`, and the adjacent packet callback data ref from `0x00613710`.
- 2026-06-05 live decompilation of `sub_46E8C0` clears `dword_67ABA0`, restores the three `BrowserPane` vptrs at primary, `+0xa0`, and `+0xa4`, then enters the base destructor path.
- 2026-06-05 live decompilation of `sub_46E8F0` gates on `a2 == 1`, calls virtual slot `+0x68`, shuts down `this[155]` with `sub_46BA90`, calls virtual slots `+0x38` and `+0x40`, and unregisters through `sub_469180(dword_67A74C, this)`.
- 2026-06-05 live decompilation of `sub_46E930` delegates to `sub_49E240`, checks the event/active-state gates, reads the active browser-control client rect through `dword_67AB28 + 0x11c`, asks the adjusted primary owner `this - 0xa0` for its rect via virtual slot `+0x28`, moves the embedded browser HWND at `[this + 0x1cc] + 0x11c` to `(rect.x + 30, rect.y + 30)`, and refreshes `dword_67A7CC` through `sub_557140`.
- 2026-06-05 live byte audit confirms all surrounding alignment spans are `0xcc`: `0x0046e8be-0x0046e8c0`, `0x0046e8e9-0x0046e8f0`, `0x0046e92b-0x0046e930`, and `0x0046e9f5-0x0046ea00`.
- B002 2026-06-17 local PE scan found vtable dword refs to `0x46e8f0` at `0x006136d4` and `0x46e930` at `0x00613704`, plus no direct ordinary destructor refs to `0x46e8c0`, matching source destructor reachability through generated deleting-destructor routes.
- B004 2026-07-12 compared this ordinary body with [UID:00033F][0x004707f0-0x0047084f.WebBoardDialogOldScalarDeletingDestructor](by-memory/0x004707f0-0x0047084f.WebBoardDialogOldScalarDeletingDestructor.md). The ordinary source body contributes only `g_pWebBoardDialogOld = NULL`; UID00033F duplicates that effect while lowering three vptr writes, `BrowserPane`/`DialogPane` base destruction, deleting-flag handling, guarded complete size `0x278`, optional `operator delete`, and return-this ABI behavior. Those extra operations are compiler-generated wrapper/base lowering and must not be copied into this destructor body.
- Zero direct xrefs to `0x0046e8c0` are expected for this build: primary vtable cell `0x0061368c` reaches UID00033F, while secondary/tertiary cells `0x00613700` and `0x00613730` reach `-0xa0`/`-0xa4` adjustors that tail-dispatch to UID00033F. Source still declares `virtual ~WebBoardDialogOld()` and emits this singleton-only ordinary body; the compiler regenerates deleting dispatch.
- B002 2026-06-17 rejected treating `0x46e8f0` as an image-button id `30` close-button command handler, a deleting destructor, or generic `DialogPane` hook: no button id or command payload is decoded, scalar deleting destructor wrappers are separate slots, and this callback is in the WebBoardDialogOld vtable with a runtime state gate.
- B002 2026-06-17 field/global reanalysis names `this + 0x26c` as `m_browserControlPane`, BrowserControlPane `+0x11c` as the hosted browser child HWND, `dword_67AB28` as [UID:0000PO][g_activeBrowserControlPane](by-global/g_activeBrowserControlPane.md) / [UID:0001P3][0x0067ab28-0x0067ab2c.g_activeBrowserControlPane](by-memory/0x0067ab28-0x0067ab2c.g_activeBrowserControlPane.md), `dword_67A7CC` as [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md), and `dword_67A74C` as [UID:0000Q8][g_pApplicationCleanupQueue](by-global/g_pApplicationCleanupQueue.md).
- B002 2026-08-03 live MCP reanalysis resolves `0x46e8f0` through the current DialogPane primary `+0x48` contract as `OnControlCommand(int,int)` and `0x46e930` through the EventHandler secondary `+0x04` contract as `HandlePointerOrMouseEvent(Event *)`. Exact body hashes are `DB31D9F33B476DB861460C1F85E69EC92193F00C181891DB910DE258622CAD26` for the destructor, `1A052EA56CA93FDBC801A66B59D5AF15CA84C8C9B8A297739DFFF3971F7B68ED` for the command callback, and `8D2D40842F708884393F97D3665A81C442D17FDCFA1C8FA5FBDDBB6C79EDDE43` for the pointer/mouse callback. The complete 309-byte target range `[0x0046e8c0,0x0046e9f5)` retains aggregate SHA256 `9DEB91FB3D9569C10C965B41F37BE7051509C00D42A3FAC3B181FC0BEE40F36C`.
- BrowserPane occupies exactly `[+0x000,+0x26c)`. Two constructor callers allocate `0x278` bytes; `m_browserControlPane` occupies `[+0x26c,+0x270)`, and exhaustive old-family searches find no read, write, or address-taking use of `+0x270` or `+0x274`. The required source disposition is therefore `unsigned char m_reserved[8]` for `[+0x270,+0x278)`: it is proven retained storage, not alignment padding, and no typed-member semantics are invented.
- No modeled function, IDA name, current by-* declaration, or repository declaration supports `GetBrowserWindow()`. Both HWND operands are direct `BrowserControlPane+0x11c` reads. The source-facing field is `m_browserHostHwnd`, exposed by UID000015's Browser.h declaration.

## Reconstruction Notes

The destructor and callback evidence establishes this as reconstructable `WebBoardDialogOld` source-owned code. `0x46e8c0` contributes only the source destructor's singleton clear; BrowserPane vptr restoration, base-tail teardown, cookie lowering, scalar wrapper, adjustors, deleting flags, and optional deallocation are compiler-generated. `0x46e8f0` is the canonical primary-slot `OnControlCommand(int,int)` override, not a state callback, and `0x46e930` is the canonical secondary-slot `HandlePointerOrMouseEvent(Event *)` override, not an `InputEvent` helper.

The command argument is a control-list index. Index `1` follows the constructor's ordered controls and must not be conflated with resource id `30`. Preserve the exact close order and unchecked dereferences: lifecycle hook, browser shutdown, `OnDestroy`, `OnHide`, deferred-delete queue; do not import the active variant's member clear. For the event callback, preserve branch-sensitive base-call order, physical secondary view at complete `+0xa0`, direct hosted-HWND reads, `GetScreenBounds`, fixed `+30,+30` geometry, `RenderPresentation`, and the saved base return value. Do not add null guards.

Complete-object evidence fixes BrowserPane at `0x26c`, `m_browserControlPane` at `+0x26c`, opaque retained bytes `[+0x270,+0x278)`, and old class size `0x278`. The retained bytes have no observed access and no surviving semantic declaration; `m_reserved[8]` is the least-committal human-source disposition. Omitting them, calling them alignment padding, or inventing typed fields would contradict the allocation/layout evidence.

The complete formal block is intentional. Preserve the unrelated accepted callback bodies exactly, and preserve the ordinary destructor as singleton-only source. Do not expand it with UID00033F's vptr restoration, base teardown, size guard, deleting flag, free, or return-this mechanics; those are regenerated from the virtual declaration, this body, inheritance, and the compiler ABI.

## Ownership Decision

Keep this range with [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md). The destructor writes the old singleton `0x0067aba0`, the callbacks sit in the `WebBoardDialogOld` vtable block, and the browser HWND reposition path uses the same fixed `+30,+30` inset as the legacy `WEBBOARD.EPD` constructor layout. Do not migrate these callbacks to generic [UID:00003T][DialogPane](by-class/DialogPane.md), [UID:000019][BrowserPane](by-class/BrowserPane.md), or [UID:0000BM][RankingDialog](by-class/RankingDialog.md) ownership based on generated output. Ranking owns other shared `WEBBOARD.EPF`/`WEBBOARD.PAL` resource consumers, but this old-dialog callback cluster is proven by old singleton, old vtables, `WEBBOARD.EPD`/`WEBBOARD.PAD` constructor resources, and fixed old browser-control geometry.

## Open Questions

- No behavior, ownership, signature, layout, or code-emission question remains open for this range. Canonical inherited contracts fix `OnControlCommand(int,int)` and `HandlePointerOrMouseEvent(Event *)`.
- Original lexical spelling of the folded primary `+0x68` lifecycle hook and semantic names/types for `[+0x270,+0x278)` do not survive. The current source-facing dispositions `OnBrowserDialogClosed` and `m_reserved[8]` are explicit high-probability/opaque choices, not deferred blockers.

## Cross-References

- [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md)
- [UID:0000P9][WebBoardDialog](by-file/WebBoardDialog.md)
- [UID:0000ZB][0x0046e640-0x0046e8be.WebBoardDialogOldConstructor](by-memory/0x0046e640-0x0046e8be.WebBoardDialogOldConstructor.md)
- [UID:0000ZC][0x0046ea00-0x0046ea23.WebBoardDialogOldPacketForwarder](by-memory/0x0046ea00-0x0046ea23.WebBoardDialogOldPacketForwarder.md)
- [UID:0000SR][g_pWebBoardDialogOld](by-global/g_pWebBoardDialogOld.md)
- [UID:00003T][DialogPane](by-class/DialogPane.md)
- [UID:0000BM][RankingDialog](by-class/RankingDialog.md)

## Changes

- 2026-08-03 B002 UID000212 accepted callback incorporation:
  - Raised metadata from `86/91` to `92/94` and retained owner/emitter UID0000G3, reconstructable true, and blank position.
  - Replaced the stale `OnCloseStateChanged` and `OnInputEventWithBrowserReposition` formal bodies with canonical `OnControlCommand(int,int)` and `HandlePointerOrMouseEvent(Event *)` implementations, exact call order, direct hosted-HWND fields, and no invented guards/accessor/member clear.
  - Added exact complete-object layout, retained-tail no-improvement proof, function/padding/vtable/RTTI/deleting-route evidence, and parent-owned H disposition.
  - Historical correction: the 2026-06-17 callback names, null-guarded sketch, `Shutdown`/`HideDialog`/`ReleaseDialogControls`, `GetBrowserWindow`, `GetPaneRect`, and `Refresh` expressions below are superseded assumptions retained only to document why current slot/API evidence disproved them.

- 2026-07-12 B004 UID00033F source-quality callback:
  - Preserved the complete accepted formal block byte-for-byte, including the singleton-only `WebBoardDialogOld::~WebBoardDialogOld()` body and both unrelated callback bodies.
  - Corrected stale blank-C++ prose and documented the exact ordinary-versus-scalar relationship, zero ordinary xrefs, vtable/adjustor reachability, compiler-generated vptr/base/delete lowering, and rejection of copying wrapper mechanics into source.

- 2026-06-17 B002 source-quality execution:
  - Raised metadata from `82/90` to `86/91`.
  - Resolved `0x46e8f0` as `WebBoardDialogOld::OnCloseStateChanged(int state, int reason)` with `state == 1` semantics, not a close-button id `30` command handler or deleting destructor.
  - Resolved `0x46e930` as `WebBoardDialogOld::OnInputEventWithBrowserReposition(...)` with the fixed old-dialog `+30,+30` hosted-browser inset.
  - Recorded `m_browserControlPane`, `g_activeBrowserControlPane`, `g_pScreenPane`, `g_pApplicationCleanupQueue`, local PE/vtable facts, no member-null store after shutdown, and rejected `DialogPane`/`BrowserPane`/Ranking/resource owner alternatives.
  - Populated accepted first-draft C++ for the destructor, close-state callback, and reposition callback.

- 2026-06-02 documentation scoring update:
  - What existed before: score was `55/85`, reconstructable and parent metadata were blank, and the page included a behavioral C++ sketch below the reconstruction threshold.
  - Changed to: score is `72/86`, reconstructable is `TRUE`, parent is [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md), and the C++ sketch was replaced with reconstruction notes.
  - Summary/evidence: existing IDA MCP evidence already proves function boundaries, padding, vtable slots, singleton behavior, browser-control teardown, HWND reposition, and WebBoardDialogOld ownership; confidence remains capped by unresolved source-level callback names.
- 2026-06-05 A001 live IDA refresh:
  - Raised metadata from `72/86` to `82/90` and attached [UID:0000G3][WebBoardDialogOld](by-class/WebBoardDialogOld.md) because both child and parent satisfy the `80/80` gate.
  - Confirmed exact function sizes, vtable slots `0x006136d4` and `0x00613704`, adjacent packet slot `0x00613710`, old RTTI at `0x006136fc`, destructor singleton/vptr cleanup, close callback teardown/unregister behavior, fixed-inset browser reposition through `dword_67AB28` and `dword_67A7CC`, no ordinary-destructor refs, and all alignment bytes.
  - Final C++ remains blank because original callback names, callback typedefs, and inherited `BrowserPane` slot names are still provisional.
