*** UID:0002BF | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007P | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007P | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapNamePane::DrawNumberGlyphString(
    const wchar_t *text, int x, int y, signed char glyphRow)
{
    EPFTileContext glyphContext;
    glyphContext.rowStridePixels = 9;
    InitRectBounds(&glyphContext.bounds, 0, 0, 9, 11);

    RectBounds destinationBounds;
    InitRectBounds(&destinationBounds, x, y, x + 9, y + 11);

    for (const wchar_t *scan = text; *scan != L'\0'; ++scan)
    {
        if (*scan != L' ' && *scan != L'/')
        {
            const int glyphIndex =
                static_cast<int>(*scan) + glyphRow * 10 - 47;
            glyphContext.pixelData = m_mapNameGlyphs + glyphIndex * 99;
            g_pfnBlitSprite(this,
                            &glyphContext,
                            &glyphContext.bounds,
                            &destinationBounds,
                            1, NULL, NULL);
        }

        OffsetRect(&destinationBounds, 9, 0);
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Private MapNamePane 9x11 UTF-16 glyph renderer; exact source body skips spaces/slashes, indexes 99-byte records from `m_mapNameGlyphs` at `this+0x220`, dispatches through `g_pfnBlitSprite`, advances 9 pixels, and emits at child position 10. No IDA function object/xref/pointer route proves runtime liveness, so confidence remains capped. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00503110-0x005031e1 Wide Glyph Resource Text Renderer Raw Helper

## Status

- Address range: `0x00503110-0x005031e1`
- Disposition: reconstructable project code; raw IDA-missed private `MapNamePane::DrawNumberGlyphString` body, emitted through [UID:00007P][MapNamePane](by-class/MapNamePane.md) at child position `10`. Live reachability remains unproved and limits confidence, not source reconstruction.
- Entity kind: raw UI text/resource renderer helper.
- Parent aggregate: [UID:00022Z][0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers](by-memory/0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers.md)
- Direct source parent: [UID:00007P][MapNamePane](by-class/MapNamePane.md), based on the private `this + 0x220` 9X11 font-table lifecycle in the adjacent constructor/destructor. The helper remains nested under [UID:00022Z][0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers](by-memory/0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers.md) for physical split-inventory context.
- Rebuild handling: `source-authored`.
- Confidence: strong for boundary, glyph iteration, render callback use, and MapNamePane source ownership; low for live reachability because no caller/table/vtable reference has been found.
- 2026-06-07 A007 IDA MCP recheck: still no IDA function object, no direct `XrefsTo` hits, and no raw little-endian pointer hits for `0x00503110`.
- 2026-06-11 A009 Batch162 live IDA recheck: still no function object, no direct start xrefs, and no raw little-endian pointer hits for `0x00503110`; the stack-cookie body, glyph loop, render callback, destination advance, and trailing padding were reconfirmed from IDB `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`.
- 2026-06-11 A009 continuation: exported decompilation ties `this + 0x220` to the same font pointer initialized and freed by [UID:00007P][MapNamePane](by-class/MapNamePane.md), but independent PE and vtable scans still find no direct call/table/vtable reference to `0x00503110`.
- 2026-06-12 A003 Batch311 recheck: typed MCP and local PE scans still find no IDA function object, direct xrefs, raw pointer hits, absolute/RVA references, relative branch targets, or MapName/MiniMapButton-adjacent vtable slot for `0x00503110`; behavior now has a documented analogue in [UID:0003AF][0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString](by-memory/0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString.md), while owner assignment remained blocked pending deeper inference.
- 2026-06-12 B001-0002BF ownership pass: assigned to [UID:00007P][MapNamePane](by-class/MapNamePane.md) because the helper is a source-authored `thiscall` 9X11 glyph renderer that uniquely reads the MapNamePane-owned `this + 0x220` font-table field; `MapNamePane` constructor `0x005031f0` loads `9X11FONT.BIN` into `this[136]`/offset `0x220`, and destructor `0x005032d0` frees the same field. The no-live-caller caveat remains.

## Supervisor Parent-Gate Audit

The child is now `92/93`, direct class parent [UID:00007P][MapNamePane](by-class/MapNamePane.md) is `94/95`, and file parent [UID:0000L2][MapNamePane](by-file/MapNamePane.md) is `94/95`. B001-0002BF upgraded the former Batch311 blocker from unresolved ownership to a defensible source-parent inference: the helper is `thiscall`, reads `this + 0x220`, and that exact field is loaded with `9X11FONT.BIN` by the adjacent MapNamePane constructor and released by the MapNamePane destructor. The later whole-file pass completed the exact body and source-facing type route. MCP/PE scans and vtable dumps still found no caller, absolute pointer, RVA, relative branch/call, or vtable slot, so the emitted retained helper is not claimed as a live paint route.

## Boundary

IDA MCP `py_eval` on 2026-06-03 shows `ida_funcs.get_func(0x00503110)` is `None`, `idautils.XrefsTo(0x00503110)` is empty, and a raw little-endian address-byte scan finds no direct table/pointer references to `0x00503110`. The body starts at `0x00503110`, uses a stack cookie, returns with `retn 0x10` at `0x005031de`, and is followed by `0x005031e1-0x005031f0` alignment recorded in [UID:0000VN][-ignored](by-memory/-ignored.md).

The same IDA snapshot shows the nearest modeled function objects are `sub_502E30` ending at `0x0050305c` and `sub_5031F0` starting at `0x005031f0`; this helper is the third raw function-shaped body in the unmodeled cluster.

2026-06-07 A007 `py_eval` reconfirms this state: `0x00503110` is still not inside an IDA function, `XrefsTo(0x00503110)` is empty, raw pointer-byte hits to `10 31 50 00` are empty across loaded segments, and the body is followed by fifteen `0xcc` bytes from `0x005031e1` through `0x005031ef` before the next modeled function at `0x005031f0-0x005032c3`.

2026-06-11 A009 Batch162 `py_eval` reconfirms `ida_funcs.get_func(0x00503110) == None`, empty `XrefsTo(0x00503110)`, and no raw pointer-byte hits for `10 31 50 00`. The raw byte window is `55 8b ec 83 ec 3c ... c2 10 00`; xrefs from the raw body include `___security_cookie`, `sub_457A60`, `sub_4B7C50`, [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) `dword_69B3E8`, `sub_4B7E10`, and the security check. The following `0x005031e1-0x005031f0` window remains fifteen `0xcc` bytes before `sub_5031F0`.

2026-06-11 A009 continuation PE scan against `C:\Users\admin\Desktop\Clone\NexusTK\NexusTK.exe` found no references to `0x00503110` as an absolute VA, as RVA `0x00103110`, as an `E8/E9 rel32` target, or as a `0F 8x rel32` target. Nearby `MapNamePane` vtable dumps also contain no `0x00503110` slot.

2026-06-12 A003 Batch311 typed MCP recheck reports `0x00503110` and `0x005031e1` as non-functions; neighboring modeled functions remain `sub_5031F0` at `0x005031f0` size `0xd3`, `sub_5032D0` at `0x005032d0` size `0x7d`, `sub_503350` at `0x00503350` size `0x14f`, and `sub_5034A0` at `0x005034a0` size `0xd4`. `xrefs_to` found zero references to `0x00503110`; MapName vtable heads `0x0061e5b4`, `0x0061e600`, and `0x0061e630` only show normal constructor/destructor/scalar-destructor store references, not a slot or table reference to this raw start.

The same Batch311 pass found nine references to `9X11FONT.BIN` at `0x0061682c` from multiple UI/status consumers (`0x0048dc70`, `0x0049999c`, `0x0050329f`, `0x0056568e`, `0x0056ca5d`, `0x0056fed2`, `0x005b8438`, `0x005bab65`, and `0x005bdaa5`). This keeps the `MapNamePane +0x220` font-field association useful but shows the resource itself is shared and cannot prove MapName ownership by itself.

Batch311 PE scanning of `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` found no absolute-VA, RVA, `E8/E9`, `0F 8x`, or short conditional-branch references to `0x00503110`. The raw body bytes still end with `retn 0x10` at `0x005031de`, and the following `0x005031e1-0x005031f0` window remains fifteen `0xcc` bytes.

## Behavior

The helper receives `this` in `ecx`, a UTF-16 string pointer, two coordinate arguments, and a one-byte style/index argument. It initializes a small source rectangle of `9x11`, initializes a destination rectangle from the caller coordinates, then walks the UTF-16 string one code unit at a time.

Exported decompilation provides the strongest owner association found so far: `0x00503110` reads `*(this + 0x220)` as the glyph base, while [UID:00007P][MapNamePane](by-class/MapNamePane.md) constructor `0x005031f0` loads `9X11FONT.BIN` through `0x004bb120` and stores it at `this + 0x220`, and `MapNamePane` destructor `0x005032d0` frees that same field. This supports a MapName-related retained helper hypothesis but does not satisfy the direct parent gate without a call/table/vtable reference.

Batch311 byte comparison against assigned [UID:0003AF][0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString](by-memory/0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString.md) shows this helper is behavior-equivalent to the known UserStatusPane 9x11 glyph renderer shape: stack-cookie frame, `sub_457A60` setup, `sub_4B7C50` source/destination rectangle initialization, UTF-16 loop, space/slash skip, style-based glyph indexing, `dword_69B3E8` render callback, and 9-pixel destination advance. The important difference is ownership evidence: the UserStatusPane helper has ten direct paint callers and a class parent, while `0x00503110` has no direct caller/table/vtable evidence, so the analogue confirms behavior rather than source owner.

For each character:

- stops at NUL;
- skips space (`0x20`) and slash (`0x2f`);
- computes a glyph/resource offset as `((codeUnit + style * 10) - 47) * 99 + *(this + 0x220)`;
- writes that computed resource pointer/index into the local render descriptor;
- dispatches through [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md);
- advances the destination rectangle by `9` pixels before the next character.

## Raw Code Evidence

```asm
50312e  call    sub_457A60
503133  push    0Bh
503135  push    9
503146  call    sub_4B7C50
50315f  call    sub_4B7C50
503164  movzx   eax, word ptr [esi]
503170  cmp     ax, 20h
503176  cmp     ax, 2Fh
503188  lea     ecx, [ecx+ecx*4]
50318b  lea     eax, [eax+ecx*2]
503190  add     eax, 0FFFFFFD1h
503193  imul    eax, 63h
503196  add     eax, [edi+220h]
5031ab  call    dword_69B3E8
5031bc  call    sub_4B7E10
5031de  retn    10h
```

## Reconstruction Gate

The exact formal CPP above emits through [UID:00007P][MapNamePane](by-class/MapNamePane.md). The body preserves every observed operation: local context initialization, row stride 9, 9x11 source/destination bounds, NUL termination, space/slash skip, signed style-row contribution, 99-byte glyph records from `m_mapNameGlyphs`, direct `g_pfnBlitSprite` arguments, and 9-pixel destination advance. UID0002BF H stays blank because the private declaration exists once in UID00007P H. [UID:00022Z][0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers](by-memory/0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers.md) remains only the non-emitting physical split inventory for the neighboring raw cluster.

The no-xref result is deliberately retained: this source revision contains executable helper bytes and a class-owned field contract even though no direct route is represented in the current binary metadata. Omitting those bytes or leaving an empty emitter would lose reconstructable source. The helper is not described as a live `OnPaint` call unless later direct evidence proves that route.

## Score Rationale

- Completion is `92` because the exact raw range, stack-cookie body, complete UTF-16/glyph algorithm, source-facing signature/body, child position, parent route, padding, repeated no-xref/no-pointer evidence, MapName `this + 0x220` lifecycle, shared resource context, and analogous assigned renderer are all documented and emitted.
- Confidence is `93` because behavior, boundaries, source ownership, and unreferenced status have repeated IDA/PE confirmation and a same-pattern source analogue. It remains below direct-called methods because no live caller/table/vtable/pointer route or original private symbol survives.
- Exact source-facing resolution is `DrawNumberGlyphString(const wchar_t *, int, int, signed char)`, `m_mapNameGlyphs`, and `g_pfnBlitSprite`. These are the most plausible human-written names/types supported by project conventions and binary behavior; raw IDA labels are not retained in final C++.

## Open Questions

- Runtime liveness was exhaustively tested through IDA function/xref queries, absolute VA/RVA/pointer scans, relative branch/call scans, and nearby vtable/table inspection; all are negative. Best current resolution is retained but uncalled private source, which caps confidence and does not block the exact body.
- The one-byte row selector participates only as signed `glyphRow * 10` in the observed formula. `glyphRow` is the narrow source-facing name; no broader semantic label is asserted. Slash and space are both advance-only characters exactly because the binary tests both before rendering.
- The object field at `+0x220` is an `unsigned char *m_mapNameGlyphs`: the constructor stores the byte buffer returned by `LoadDatFileBuffer`, this helper performs byte-addressed 99-byte indexing, and the destructor frees the same pointer.
- No unrecognized route is deferred. Any later table/callback discovery would strengthen liveness only; it would not change the current exact algorithm, ownership, formal C++, or source placement.

## Cross-References

- [UID:00022Z][0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers](by-memory/0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers.md) parent aggregate.
- [UID:0000TN][SurfaceRenderCallbackTable](by-global/SurfaceRenderCallbackTable.md) render callback table.
- [UID:00019I][0x004f6700-0x004fb62a.MainMenuLoginAndAccountDialogs](by-memory/0x004f6700-0x004fb62a.MainMenuLoginAndAccountDialogs.md) nearby main-menu/login/account dialog range.
- [UID:0001AL][0x005031f0-0x0050395f.MapNameAndMiniMapButtonPanes](by-memory/0x005031f0-0x0050395f.MapNameAndMiniMapButtonPanes.md) following MapName/MiniMapButton pane range.
- [UID:0000VN][-ignored](by-memory/-ignored.md) alignment ledger.

## Changes

- 2026-06-22 Rule 26 B001-0002BF closeout:
  - Changed to: kept `87/91`, `CANONICAL_OWNER:00007P`, `EMITTER_UIDS:00007P`, reconstructable status, and blank formal C++; corrected the reconstruction-gate text to match the accepted MapNamePane source-owner route rather than the older blank-parent gate.
  - Summary/evidence: B001-0002BF compared the raw helper against the adjacent `MapNamePane` constructor/destructor lifecycle and the UserStatusPane 9x11 glyph-helper family. The helper remains an IDA-missed raw body with no direct xrefs, raw pointer hits, absolute-VA/RVA/relative branch or call references, or MapName/MiniMapButton-adjacent vtable slot, but it uniquely reads `this + 0x220`, where `MapNamePane::MapNamePane()` loads `9X11FONT.BIN`/slot `0x63` and `MapNamePane::~MapNamePane()` frees the same field. The accepted disposition is a retained/unreferenced private `MapNamePane` glyph renderer with unresolved live reachability and no formal C++ yet.

- 2026-06-12 A003 Batch311 recheck:
  - Changed to: raised scores from `86/90` to `87/91`, retained blank `AUTOGEN_PARENT_UID`, and documented the active MCP no-function/no-xref pass, PE no-reference pass, shared `9X11FONT.BIN` xref context, and behavior analogue with [UID:0003AF][0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString](by-memory/0x005ba020-0x005ba0f1.UserStatusPaneDrawNumberGlyphString.md).
  - Summary/evidence: MCP reports no function object at `0x00503110`, zero direct xrefs, and no MapName vtable slot; PE scanning found no absolute-VA, RVA, relative branch/call, or short conditional references; `9X11FONT.BIN` has multiple UI/status consumers; and the UserStatusPane glyph renderer confirms this helper's 9x11 render-loop behavior. MapName remains the strongest association through `this + 0x220`, but the direct-parent gate still fails without caller/table/vtable evidence.

- 2026-06-11 A009 Batch162 continuation:
  - Changed to: raised scores from `84/88` to `86/90`, retained blank `AUTOGEN_PARENT_UID`, and documented the independent PE no-reference scan plus `MapNamePane +0x220` font-storage association.
  - Summary/evidence: PE scan found no absolute-VA, RVA, `E8/E9 rel32`, or `0F 8x rel32` references to `0x00503110`; nearby MapName vtables do not contain the helper start. Exported decompilation shows this helper's `this + 0x220` glyph base matches `MapNamePane` constructor loading `9X11FONT.BIN` into `this + 0x220` and destructor freeing the same field. That makes MapName the strongest hypothesis, but still not a defensible direct parent without call/table/vtable evidence.

- 2026-06-11 A009 Batch162 overlapping-cluster recheck:
  - Changed to: raised scores from `82/86` to `84/88`, retained blank `AUTOGEN_PARENT_UID`, and added fresh IDA evidence from the overlapping raw-helper cluster.
  - Summary/evidence: IDA MCP `lookup_funcs` and `py_eval` on 2026-06-11 reconfirmed no function object, no direct start xrefs, no raw pointer hits, the `0x00503110-0x005031e1` byte window ending with `retn 0x10` at `0x005031de`, source/destination rectangle setup through `sub_4B7C50`, UTF-16 loop skip rules, glyph formula `(codeUnit + style * 10 - 47) * 99 + *(this + 0x220)`, render dispatch through `dword_69B3E8`, destination advance through `sub_4B7E10`, and fifteen `0xcc` bytes before `sub_5031F0`. Assignment still does not clear because no live caller, table, or source owner is proven.

- 2026-06-07 A007 Batch 006 corrected-gate recheck:
  - What existed before: score was `78/84`, the page had 2026-06-03 raw helper evidence but no corrected 85/85 parent-gate audit.
  - Changed to: score is `82/86`, current IDA boundary/xref/pointer evidence is recorded, and `AUTOGEN_PARENT_UID` remains blank.
  - Summary/evidence: IDA MCP `tools/list` and `py_eval` on 2026-06-07 reconfirmed no IDA function object, no `XrefsTo` hits, no raw pointer hits for `0x00503110`, the glyph render callback path through `dword_69B3E8`, `retn 0x10` at `0x005031de`, fifteen `0xcc` bytes before `sub_5031F0`, and neighboring modeled functions at `0x00502e30-0x0050305c` and `0x005031f0-0x005032c3`. Assignment remains blocked because the child is below 85 completion and no direct source parent is both gate-valid and evidence-supported.

- 2026-06-03 IDA evidence pass:
  - What existed before: the page documented the glyph loop but left reconstructable/parent metadata blank, carried a below-gate C++ sketch, and only recorded older empty-xref evidence.
  - Changed to: raised `75/80` to `78/84`, marked it reconstructable, kept it nested under [UID:00022Z][0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers](by-memory/0x00503060-0x005031e1.UnreferencedUiResourceTextHelpers.md) in documentation while leaving autogen parent blank, removed the source sketch, and added the raw address-byte scan caveat.
  - Summary/evidence: IDA MCP `py_eval` on 2026-06-03 confirmed no IDA function object, no direct xrefs, no raw address-byte references to `0x00503110`, modeled neighboring functions at `0x00502e30-0x0050305c` and `0x005031f0-0x005032c3`, the `0x005031de` return, direct calls to `0x00457a60`, `0x004b7c50`, `0x004b7e10`, and the render callback `dword_69B3E8`.
