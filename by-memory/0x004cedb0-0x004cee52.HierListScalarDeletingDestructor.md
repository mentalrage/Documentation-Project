*** UID:0002IM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000065 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:FALSE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Compiler scalar deleting destructor wrapper reached only through the HierList vtable slot; duplicates `HierList::~HierList()` child-list cleanup, applies delete flags and optional object free, and should be regenerated from the class destructor rather than hand-authored as source. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004cedb0-0x004cee52 HierListScalarDeletingDestructor

## Status

- Entity kind: compiler-generated class scalar deleting destructor ABI wrapper
- Owner: [UID:000065][HierList](by-class/HierList.md)
- Likely source file: [UID:0000JV][HierList](by-file/HierList.md)
- Parent aggregate: [UID:00016X][0x004ce730-0x004cee52.HierList](by-memory/0x004ce730-0x004cee52.HierList.md)
- Rebuild handling: non-emitting compiler ABI wrapper generated from the source `HierList::~HierList()` destructor and virtual class declaration; source class still reconstructable through [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md).
- Autogen status: documentation-owned by [UID:000065][HierList](by-class/HierList.md), but no source emitter. Formal C++ intentionally remains blank because this wrapper is compiler output, not a human-authored method.

## Behavior

Implements the compiler-emitted `HierList` scalar deleting destructor entry in the vtable. It installs destructor state, duplicates the source destructor's nested child-list cleanup from [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md), destroys the base list storage, and conditionally frees `this` based on the deleting-destructor flag. Original-looking source should provide `HierList::~HierList()` on [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md); the compiler/linker should regenerate this ABI wrapper.

## Evidence

- IDA MCP `py_eval` confirmed an IDA function object at `0x004cedb0-0x004cee52`, size `0xa2`.
- IDA xrefs show this wrapper in the `HierList` vtable at `0x0061b344`.
- Live IDA MCP on 2026-06-03 reconfirmed the vtable xref at `0x0061b344`, constructor/destructor/insert methods storing `??_7HierList@@6B@`, and slot `0x00 == 0x004cedb0`.
- The body installs `HierList::vftable`, loops `edi < this[3]`, gets each row via `sub_4F32B0`, deletes any child list at row `+0x04`, clears row `+0x04`, calls the base list destructor helper `sub_4F30E0`, and conditionally frees `this` through `sub_4F4AC0` when `(flags & 1) != 0 && (flags & 4) == 0`.
- IDA MCP byte review confirmed `0x004cee52-0x004cee60` is fourteen `0xcc` alignment bytes before the next `HourPane` function.

## 2026-06-26 B011 ABI-Wrapper Reclassification

Agent-B011 used live IDA MCP session `80de0a67`, with the server reporting health OK, to recheck this wrapper against [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md) and the [UID:0003BL][0x0061b340-0x0061b36c.HierListVtableData](by-memory/0x0061b340-0x0061b36c.HierListVtableData.md).

| Query / evidence route | Result |
| --- | --- |
| `lookup_funcs 0x004cedb0` | Modeled function `sub_4CEDB0`, exact range `0x004cedb0-0x004cee52`, size `0xa2` / 162 bytes (Verified with int_convert.py). |
| `lookup_funcs 0x004cee52` | No function at the endpoint; the next modeled function starts at `0x004cee60` in the unrelated HourPane range. |
| `xrefs_to 0x004cedb0` | Exactly one incoming xref, the data pointer at vtable slot `0x0061b344`. |
| Raw pointer search for little-endian `0x004cedb0` (`B0 ED 4C 00`) | Exactly one match, also `0x0061b344`; no independent direct-call or table route was found. |
| `xrefs_to 0x0061b344` | Constructor/destructor/insert/scalar-wrapper vtable stores at `0x004ce74e`, `0x004ce789`, `0x004ce93e`, `0x004cea8d`, and `0x004cede0`. |
| `callees 0x004cedb0` | Calls the accepted row accessor `List::GetElementAt` (`0x004f32b0`), base `List::~List()` body (`0x004f30e0`), object free helper (`0x004f4ac0`), and `_guard_check_icall_nop`. |
| Focus disassembly | Shows `flags & 1` object-free behavior, `flags & 4` guarded delete path, pushed size `0x18` / 24 (Verified with int_convert.py), and `retn 4`. |

This makes [UID:0002IM] a no-code compiler ABI artifact. The source-bearing destructor body is [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md), which performs the child-list loop without a delete-flag parameter or explicit object free. Rejected alternatives are: hand-authoring a source method named like `ScalarDeletingDestructor(flags)`, putting object-free logic in `HierList::~HierList()`, assigning this wrapper to `Layer`, or assigning it to the base `List` source. `Layer` is a caller/consumer of `HierList`; `List` supplies base storage/destructor behavior but not the hierarchy-specific child-list cleanup or vtable slot.

## 2026-06-14 Live IDA Pass

Active IDA MCP session `a001_goal2_class_batch` reports `NexusTK.exe.i64` loaded for `NexusTK.exe`, imagebase `0x400000`, with auto-analysis and Hex-Rays ready.

| Query | Result |
| --- | --- |
| `lookup_funcs 0x004cedb0` | Modeled function `sub_4CEDB0`, size `0xa2`; local `int_convert.py` converts `0xa2` to `162` bytes. |
| `lookup_funcs 0x004cee52/0x004cee53` | No function at either endpoint; `0x004cee60` is the next modeled function and belongs to the HourPane successor. |
| `xrefs_to 0x004cedb0` | Exactly one xref, the `HierList` vtable entry at `0x0061b344`. |
| `xrefs_to 0x0061b344` | Five vtable-store/data refs from constructor `0x004ce730`, non-deleting destructor `0x004ce760`, insert-before `0x004ce860`, insert-after `0x004ce9b0`, and this scalar deleting destructor at `0x004cede0`. |
| `callees 0x004cedb0` | Calls `sub_4F32B0`, `sub_4F30E0`, `sub_4F4AC0`, and `_guard_check_icall_nop`. |
| `analyze_function 0x004cedb0` | Fourteen basic blocks, cyclomatic complexity `4`, with a decompiled child-list release loop and deleting-destructor flag tests. |
| `analyze_component 0x004ce730,0x004ce760,0x004ceb00,0x004cec30,0x004cedb0` | Treats `0x004cedb0` as internal-only vtable ABI glue; shared globals are `??_7HierList@@6B@` and the security cookie. |
| `get_bytes 0x004ceda4 size 0xcc` | Twelve `0xcc` bytes before the wrapper, exact `0xa2` byte body ending in `retn 4`, then fourteen `0xcc` bytes before `0x004cee60`. |

The current decompiler installs `HierList::vftable`, iterates row indices while `index < this[3]`, obtains each row through `sub_4F32B0`, deletes and clears a child list at row `+0x04` when present, calls `sub_4F30E0` for base list cleanup, and applies the MSVC deleting-destructor predicate. The ordinary scalar-delete path calls `sub_4F4AC0(this)` when `(flags & 1) != 0 && (flags & 4) == 0`; the guarded flag-`4` path pushes size `0x18` / 24 before `_guard_check_icall_nop`.

## Confidence And Caveats

The ABI role, vtable-only xref state, boundary, cleanup loop, delete-flag behavior, object-free path, and relationship to [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md) are strong. Do not hand-write this wrapper as normal project C++; the source-level destructor body is modeled through `HierList::~HierList()` on [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md), while compiler output regenerates this wrapper to preserve binary-equivalent behavior.

## Score Rationale

Current B011 rationale: `90/93` is justified because the target's source disposition is now resolved instead of deferred. Completion rises from `86` to `90` because the page now records the exact wrapper range, vtable-only route, raw pointer negative evidence, delete-flag semantics, helper callees, endpoint padding, no-code proof, and relationship to the source destructor page. Confidence rises from `90` to `93` because all direct routes point to compiler-generated scalar deleting destructor glue and no evidence supports a human-authored source method for this wrapper. The score stays below final-audit level because exact original class declaration/header organization is still a class-wide source pass, not because this wrapper remains unresolved.

Raised from `76/88` to `85/89` because the 2026-06-14 live IDA pass reconfirmed the exact `0xa2` / 162-byte vtable-only wrapper, the full child-list release loop, base-list destructor call, scalar-delete and guarded-delete paths, shared `HierList` vtable refs, and endpoint padding. The page now has enough current boundary, behavior, vtable, and parent-route evidence to leave the low-completion bucket. It remains below final-audit level because compiler ABI wrapper representation and exact source destructor declaration policy are still not final.

## Changes

- 2026-06-26 B011 accepted implementation:
  - Reclassified from `86/90`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:000065` to `90/93`, `RECONSTRUCTABLE:FALSE`, blank `EMITTER_UIDS`.
  - Preserved [UID:000065][HierList](by-class/HierList.md) as documentation owner while removing this wrapper from human source emission.
  - Evidence: MCP session `80de0a67` confirmed the exact modeled range, single vtable-slot xref at `0x0061b344`, unique raw pointer match at that slot, no direct caller route, delete-flag/free/guarded-size disassembly, helper callees, and endpoint padding.
  - Superseded assumption: earlier text left this page reconstructable/emitting while deferring destructor wrapper policy. The current disposition resolves that policy: [UID:0002I9][0x004ce760-0x004ce7d1.HierListNonDeletingDestructor](by-memory/0x004ce760-0x004ce7d1.HierListNonDeletingDestructor.md) emits source `HierList::~HierList()`; this page documents generated scalar deleting destructor ABI glue and keeps formal C++ blank.

- 2026-06-17 B001 source-quality execution:
  - Raised from `85/89` to `86/90`.
  - Evidence: B001 rechecked the vtable-only ABI-wrapper role, child-list release loop, base storage destructor call, delete-flag behavior, exact HourPane successor padding, and rejected hand-authoring this as a normal source method.
  - Formal C++ remains blank by accepted ABI-wrapper/destructor policy sequencing.

- 2026-06-03 live IDA recheck and parent attachment:
  - What existed before: scored `70/84`, reconstructable, but had no autogen parent and only a short ABI-wrapper summary.
  - Changed to: scored `76/88`, attached to [UID:000065][HierList](by-class/HierList.md), and expanded vtable, loop, deletion-flag, and padding evidence.
  - Summary/evidence: IDA MCP verified `0x004cedb0-0x004cee52`, vtable slot `0x0061b344 -> 0x004cedb0`, vtable stores at constructor/destructor/insert sites, child-list deletion and clear via row `+0x04`, base destructor call `0x004cee16 -> 0x004f30e0`, conditional free `0x004cee27 -> 0x004f4ac0`, and fourteen trailing `0xcc` bytes. Final C++ remains blank.

- 2026-06-14 C001 low-completion pass:
  - What existed before: scored `76/88` with correct owner/emitter routing but stale live evidence and no current-session component/byte/decompiler table.
  - Changed to: scored `85/89`, added current IDA MCP session, lookup/xref/callee/decompiler/component, byte-boundary, and local integer-conversion evidence. Owner [UID:000065][HierList](by-class/HierList.md), emitter [UID:000065][HierList](by-class/HierList.md), reconstructable flag, and blank final C++ block are unchanged.
  - Summary/evidence: IDA MCP verified active `NexusTK.exe.i64`, modeled `sub_4CEDB0` size `0xa2`, only direct xref `0x0061b344`, vtable-store refs from constructor/destructor/insert methods, calls to `sub_4F32B0`, `sub_4F30E0`, `sub_4F4AC0`, and `_guard_check_icall_nop`, child-list delete/clear loop, delete-flag tests, size constant `0x18` / 24 for the guarded path, and surrounding `0xcc` padding. Final C++ remains blank because ABI-wrapper source representation is not final-source quality.
