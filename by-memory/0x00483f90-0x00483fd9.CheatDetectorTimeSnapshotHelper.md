*** UID:0002EN | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000020 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000020 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void CheatDetector::CaptureTimeSnapshot(unsigned int *timerTick, unsigned __int64 *systemFileTime)
{
    FILETIME fileTime;
    GetSystemTimeAsFileTime(&fileTime);

    const unsigned __int64 capturedFileTime =
        (static_cast<unsigned __int64>(fileTime.dwHighDateTime) << 32) |
        fileTime.dwLowDateTime;

    m_lastSystemFileTime = capturedFileTime;

    const unsigned int currentTick = g_pTimerMgr->m_currentTick;
    m_lastTimerTick = currentTick;

    *timerTick = currentTick;
    *systemFileTime = capturedFileTime;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live IDA confirms raw helper bytes, `GetSystemTimeAsFileTime` import use, receiver timestamp/timer stores, timer singleton field sampling, `retn 8` shape, padding, and no incoming xrefs; original name and reachability remain unresolved. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00483f90-0x00483fd9 CheatDetector Time Snapshot Helper

## Status

- Disposition: reconstructable NexusTK project code with call-surface caveat.
- Entity kind: unmodeled helper/function body inside the `CheatDetector` neighborhood.
- Covered class: [UID:000020][CheatDetector](by-class/CheatDetector.md)
- Likely source file: [UID:0000I6][CheatDetector](by-file/CheatDetector.md)
- Autogen status: reconstructable helper routed to [UID:000020][CheatDetector](by-class/CheatDetector.md); first-draft helper C++ is ready as private retained source code. The no-route finding blocks active-use claims, not the source body.
- Confidence: strong for raw behavior and source role, lower for exact original spelling and caller surface because IDA does not model this as a function and local scans found no incoming route.

## Signature

IDA does not create a function record at `0x00483f90`, but the raw body has a complete `__thiscall` shape:

- `ecx` is the `CheatDetector` instance.
- stack argument `+0x08` is a pointer receiving the current timer tick snapshot.
- stack argument `+0x0c` is a pointer receiving the copied 64-bit `FILETIME`.
- the epilogue uses `retn 8`, so the helper consumes two explicit stack arguments and returns no value.

## Behavior

The helper captures current system time, stores that 64-bit timestamp into receiver offsets `+0x08` and `+0x0c`, reads a timer value from the object at singleton address `0x0067ab80` offset `+0x18`, stores that value at receiver offset `+0x10`, writes the timer value to the first out-parameter, and writes the captured 64-bit timestamp to the second out-parameter.

The range starts after ten bytes of `0xcc` padding following the constructor and ends before seven bytes of `0xcc` alignment before the always-true virtual function at `0x00483fe0`.

## Instruction Evidence

IDA does not model a function at `0x00483f90`, but `get_bytes 0x00483f90 size 0x49` confirms a complete function-shaped body. The byte stream begins with a standard `push ebp; mov ebp, esp; sub esp, 8` prologue, calls the imported `GetSystemTimeAsFileTime` pointer at `0x0060d150`, stores the low/high timestamp dwords at receiver offsets `+0x08/+0x0c`, reads singleton `0x0067ab80` and its field `+0x18`, writes the timer snapshot to the first explicit argument, writes the timestamp dwords to the second explicit argument, and ends with `retn 8`.

## Source-Level Shape

Source should express this as a small private `CheatDetector` helper best named `CaptureTimeSnapshot(unsigned int *timerTick, unsigned __int64 *systemFileTime)`. The method samples `GetSystemTimeAsFileTime`, combines the low/high `FILETIME` dwords into a 64-bit file-time value, stores that value at object offsets `+0x08/+0x0c`, reads the timer manager field at `g_pTimerMgr + 0x18`, stores that value at receiver offset `+0x10`, and copies both snapshots to the caller-provided output pointers.

The first-draft C++ uses `unsigned __int64` for the file-time storage to preserve the observed 8-byte field semantics. The class declaration should keep this helper private. Do not add a null check for `g_pTimerMgr`; the binary has none. If final style prefers `ULARGE_INTEGER` for the local conversion, that is equivalent as long as the class field remains a 64-bit file-time value.

The helper still has no proven active caller. That caps runtime-behavior confidence and active anti-cheat claims, but it does not require leaving this source-authored helper without code.

## IDA MCP Evidence

- Live `lookup_funcs 0x00483f90` reports `Not a function`, while modeled functions exist immediately before and after at `0x00483f00` and `0x00483fe0`.
- Live `callers 0x00483f90` and `xrefs_to 0x00483f90` report no callers or xrefs to the raw helper entry.
- Live byte reads show `0x00483f86-0x00483f90` is ten `0xcc` bytes after the constructor, and `0x00483fd9-0x00483fe0` is seven `0xcc` bytes before the vtable helper at `0x00483fe0`.
- The only outgoing external references are the imported `GetSystemTimeAsFileTime` pointer at `0x0060d150` and timer singleton storage `0x0067ab80`.
- Live `xrefs_to 0x0067ab80` reports many timer-manager singleton users, including this helper's read at `0x00483fb4`, matching [UID:0000SI][g_pTimerMgr](by-global/g_pTimerMgr.md) ownership.
- 2026-06-16 source-quality refresh in IDA session `a002_goal65_20260616` reconfirmed `0x00483f90` is still not an IDA function, `xref_query 0x00483f90` has total `0`, and byte-pattern scans for the raw start VA/RVA encodings found no direct pointer encodings to the helper.
- 2026-06-19 B002 local PE route scan found no direct call, direct jump, VA pointer, or RVA pointer to `0x00483f90`. Neighboring positive controls still find the constructor caller, vtable-only callback/destructor slots, constructor-unwind jump to `0x00483ff0`, and `g_pTimerMgr` references, so the no-route finding is specific to this retained helper entry.
- The same refresh decompiled the neighboring constructor at `0x00483f00`: it sets `g_pCheatDetector`, calls the `TimerHandler`-family base constructor at `0x00597570`, writes the `CheatDetector` vtable, and clears the fields later sampled by this helper. That confirms owner/source placement but does not prove the original private helper name.

## Callers And Callees

| Evidence | Address | Meaning |
| --- | --- | --- |
| direct callers | none found | No IDA function, code xref, interior xref, direct rel32 call/jump, VA pointer, RVA pointer, or literal function-pointer ref currently reaches this raw helper. |
| imported API | `0x0060d150` | `GetSystemTimeAsFileTime`, used to capture the 64-bit wall-clock timestamp. |
| timer singleton | `0x0067ab80` | `g_pTimerMgr`; the helper reads field `+0x18` from the timer manager object. |

## Touched State

| State | Evidence | Interpretation |
| --- | --- | --- |
| receiver offset `+0x08/+0x0c` | stores low/high dwords after `GetSystemTimeAsFileTime` | `m_lastSystemFileTime`, a 64-bit file-time snapshot. |
| receiver offset `+0x10` | stores `[g_pTimerMgr + 0x18]` | `m_lastTimerTick`, the current timer-manager tick/current-time field. |
| first explicit argument | receives `[g_pTimerMgr + 0x18]` | `timerTick` output pointer. |
| second explicit argument | receives receiver offsets `+0x08/+0x0c` | `systemFileTime` output pointer receiving the 64-bit file-time snapshot. |

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `87` | The page records raw signature shape, exact bytes, padding-bounded range, no-xref/no-pointer-encoding reachability audit, outgoing API/singleton refs, constructor/base-class context, touched fields, private helper name/signature, field/out-argument names, and first-draft C++. Completion remains below final audit because the helper has no proven active caller and exact original spelling is inferred. |
| Confidence `89` | The instruction-level behavior, `CheatDetector` state writes, timer singleton dependency, private helper role, and 64-bit file-time/tick layout are supported by local byte evidence and neighboring constructor/class docs. Confidence is not higher because the helper remains unreachable by direct call/jump/VA/RVA scans and `+0x14/+0x18` baseline fields are inferred from layout. |

## Cross-References

- [UID:000108][0x00483f00-0x0048402d.CheatDetector](by-memory/0x00483f00-0x0048402d.CheatDetector.md)
- [UID:000020][CheatDetector](by-class/CheatDetector.md)
- [UID:0000I6][CheatDetector](by-file/CheatDetector.md)
- [UID:0000QJ][g_pCheatDetector](by-global/g_pCheatDetector.md)
- [UID:0001P5][0x0067ab3c-0x0067ab40.g_pCheatDetector](by-memory/0x0067ab3c-0x0067ab40.g_pCheatDetector.md)
- [UID:0000SI][g_pTimerMgr](by-global/g_pTimerMgr.md)

## B004 Retained Source/Liveness Closure

- Exact range/body remain `[0x00483f90,0x00483fd9)`, 0x49 bytes, SHA256 `8B32E34D4E8606C600DE9B2D47553973837349E0E2C370D8CA123CD938D1E2BA`; the accepted managed body is unchanged byte-for-byte.
- The helper reads system file time, combines high/low dwords into a 64-bit value, stores it at object `+0x08`, reads `g_pTimerMgr->m_currentTick`, stores that at `+0x10`, and writes both values to the two out parameters without null checks.
- Its name remains descriptive because symbols are stripped. `CaptureTimeSnapshot` best matches the two outputs and private field updates without claiming anti-cheat validation.
- Exhaustive call/jump/VA/RVA/raw-pointer scans found no incoming route, with surrounding functions and vtable entries serving as positive controls. This is retained source-authored code, not padding or compiler support, but active runtime use is unproven.
- Nested delta changes from `+2` to `0` because the preceding registered constructor child UID0004UO already established cumulative child level `I+2`; this helper stays at that level.
- Rejected alternatives remain a free helper, TimerMgr-owned method, active detector rule, generated runtime body, or removal as dead padding.

## Changes

- 2026-06-16 A002 source-quality refresh: raised completion/confidence from `82/86` to `85/88` and kept owner/emitter [UID:000020][CheatDetector](by-class/CheatDetector.md).
  - Evidence: live IDA session `a002_goal65_20260616` checked `lookup_funcs`, `xref_query`, `find_bytes`, `get_bytes`, neighboring constructor decompilation, `g_pTimerMgr` xrefs, source-filename string search, and the generated root file length. The helper remains a raw no-xref body with no VA/RVA pointer encodings to the start, and `auto-generated/NexusTK/security/CheatDetector.cpp` is empty.
  - Final C++ remains blank because caller/reachability, exact private helper name, and receiver field names are still not source-quality. No IDA DB changes were made.
- 2026-06-20 B002 Rule 26 incorporation: raised `85/88` to `87/89`, named the helper `CheatDetector::CaptureTimeSnapshot`, added first-draft source C++, and narrowed the blocker. Local direct-call, direct-jump, VA-pointer, and RVA-pointer scans still found no route to `0x00483f90`, so active execution remains unproven, but the exact private helper behavior, out-parameter model, and object-field writes are sufficiently resolved for draft source.
- 2026-06-05: Updated completion/confidence from `76/82` to `82/86` and replaced raw generated labels with live address-based evidence.
  - Before: the page included a raw disassembly block with generated labels and older `py_eval` wording.
  - After: the page documents the same raw helper behavior using live `lookup_funcs`, `callers`, `xrefs_to`, and `get_bytes` evidence, keeping C++ blank under the code-emission gate.
  - Evidence: live IDA MCP lookup/callers/xrefs/bytes for `0x00483f86-0x00483fe0`, plus singleton xrefs for `0x0067ab80`.
- 2026-06-03 low-score evidence refresh: Existing metadata left reconstructable/parent blank and included a C++ candidate despite the page being below the `95+` code-emission gate. Changed `RECONSTRUCTABLE` to `TRUE`, attached the helper to [UID:000020][CheatDetector](by-class/CheatDetector.md), raised completion/confidence from `70/78` to `76/82`, removed the below-threshold C++ candidate, and added signature, caller/callee, touched-state, and score-rationale sections. Evidence: IDA MCP review confirmed no function record, no external refs to instruction heads, no literal refs to the raw start/interior/return, exact `0xcc` padding on both sides, and outgoing references only to `GetSystemTimeAsFileTime` and timer singleton address `0x0067ab80`; existing [UID:0000SI][g_pTimerMgr](by-global/g_pTimerMgr.md) documentation confirms that singleton ownership.
- 2026-05-30: Created this page from a previously undocumented executable range inside the `CheatDetector` parent; set completion/confidence to `70/78`. Evidence is IDA MCP raw disassembly and byte/xref audit showing a complete unmodeled body at `0x00483f90-0x00483fd9`, with no incoming xrefs and known outgoing references to `GetSystemTimeAsFileTime` and timer singleton address `0x0067ab80`.
