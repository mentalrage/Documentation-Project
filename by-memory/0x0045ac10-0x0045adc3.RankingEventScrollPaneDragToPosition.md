*** UID:0000Y6 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000BO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000BO | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void RankingEventScrollPane::DragToPosition(int y, int x)
{
    const short oldPosition = m_scrollPosition;
    RectBounds thumbRect;
    RectBounds leadingRect;
    RectBounds trailingRect;
    Point cursor;
    Point origin;

    GetPartRect(kRankingScrollPartThumb, &thumbRect);

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.y != -1000 || origin.x != -1000) {
        leadingRect = m_bounds;
        if (m_orientation != 0) {
            leadingRect.right = leadingRect.left;
        } else {
            leadingRect.bottom = leadingRect.top;
        }
    }

    g_pEventMan->GetCursorPosition(&cursor);
    GetScreenOffset(&origin);
    if (origin.y != -1000 || origin.x != -1000) {
        trailingRect = m_bounds;
        if (m_orientation != 0) {
            trailingRect.left = trailingRect.right;
        } else {
            trailingRect.top = trailingRect.bottom;
        }
    }

    short newPosition;
    if (m_orientation != 0) {
        int thumbStart = x - m_thumbDragOffset.x + 1;
        const int trackStart = leadingRect.right;
        if (thumbStart < trackStart) {
            thumbStart = trackStart;
        }

        const int maxThumbStart = trailingRect.left +
            thumbRect.left - thumbRect.right;
        if (thumbStart > maxThumbStart) {
            thumbStart = maxThumbStart;
        }

        const int trackSpan = trailingRect.left + thumbRect.left -
            thumbRect.right - leadingRect.right;
        newPosition = static_cast<short>(
            (m_scrollRange * (thumbStart - leadingRect.right)) /
            trackSpan);
    } else {
        int thumbStart = y - m_thumbDragOffset.y + 1;
        const int trackStart = leadingRect.bottom;
        if (thumbStart < trackStart) {
            thumbStart = trackStart;
        }

        const int maxThumbStart = trailingRect.top +
            thumbRect.top - thumbRect.bottom;
        if (thumbStart > maxThumbStart) {
            thumbStart = maxThumbStart;
        }

        const int trackSpan = trailingRect.top + thumbRect.top -
            thumbRect.bottom - leadingRect.bottom;
        newPosition = static_cast<short>(
            ((m_scrollRange + 1) *
             (thumbStart - leadingRect.bottom)) /
            trackSpan);

        if (newPosition > m_scrollRange) {
            newPosition = m_scrollRange;
        }
    }

    if (oldPosition == newPosition) {
        return;
    }

    RankingEventListPane *owner =
        static_cast<RankingEventListPane *>(GetOwnerPane());
    owner->SetScrollPosition(0, oldPosition, newPosition);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready RankingEventScrollPane drag-position helper with exact y/x geometry, orientation-specific signed scaling, sentinel-controlled bounds initialization, six local callers, and factorized RankingEventListPane::SetScrollPosition owner synchronization. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:-4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0045ac10-0x0045adc3 RankingEventScrollPane Drag To Position

## Status

- Disposition: reconstructable/source-ready NexusTK project method.
- Entity kind: nonvirtual private helper method.
- Exact signature: `void RankingEventScrollPane::DragToPosition(int y, int x)`.
- Canonical owner/emitter: [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md).
- Likely source module: [UID:0000MZ][RankingDialog](by-file/RankingDialog.md), generated path `NexusTK/ui/dialogs/RankingDialog.cpp`.
- Rebuild handling: source-authored; security-cookie, SIMD rectangle copies, and expanded owner-method internals are compiler lowering rather than source operations.
- Confidence: strong for range, callers, coordinate order, arithmetic, fields, owner factorization, no-change behavior, and placement; exact private spelling remains inferred rather than symbol-proven.

## Address Range And Padding

- IDA models `sub_45AC10` at `[0x0045ac10, 0x0045adc4)`, size `0x1b4`, 140 instructions, 21 basic blocks, cyclomatic complexity 11, and terminal `retn 8` at `0x0045adc1`.
- The project filename endpoint `0x0045adc3` is the inclusive final instruction byte, not a one-byte-short defect.
- Predecessor [UID:0000Y5][0x0045a990-0x0045ac0f.RankingEventScrollPaneRawHoverDragHelpers](by-memory/0x0045a990-0x0045ac0f.RankingEventScrollPaneRawHoverDragHelpers.md) ends with its raw return at `0x0045ac0f`; this modeled function starts immediately at `0x0045ac10`.
- `0x0045adc4-0x0045add0` is exactly twelve `0xcc` alignment bytes with no xrefs at `0x0045adc4`.
- Raw successor [UID:0000Y7][0x0045add0-0x0045ae2b.RankingEventScrollPaneRawResetState](by-memory/0x0045add0-0x0045ae2b.RankingEventScrollPaneRawResetState.md) starts at `0x0045add0`, remains a separate no-function/no-incoming-xref helper, and is not merged into this page.
- No split, rename, reclassification, new child, or padding ownership change is required.

## Caller And Coordinate Evidence

Exactly six direct code xrefs reach this helper, all inside `RankingEventScrollPane` behavior:

| Call site | Caller context | Meaning |
| --- | --- | --- |
| `0x00459c1c` | [UID:0000Y3][0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent](by-memory/0x00459a60-0x00459ccd.RankingEventScrollPaneOnMouseEvent.md) | active thumb update with `(localY, localX)` |
| `0x00459c38` | UID0000Y3 | matching active non-thumb part update |
| `0x0045aad8` | UID0000Y5 raw begin-interaction method | immediate active-thumb update |
| `0x0045aaf4` | UID0000Y5 raw begin-interaction method | non-thumb update after matching hit-test |
| `0x0045abab` | UID0000Y5 raw active-interaction refresh | repeated active-thumb update |
| `0x0045abd4` | UID0000Y5 raw active-interaction refresh | repeated non-thumb update after matching hit-test |

- Every call pushes the x component before the y component, producing source order `(y, x)` under x86 right-to-left argument passing.
- Canonical [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) stores `Point.y` at offset `0` and `Point.x` at `+4`. UID0000Y3/Y5 store `event.y - thumbRect.top` at `m_thumbDragOffset.y +0x108` and `event.x - thumbRect.left` at `.x +0x10c`.
- The decompiler `int` return is incidental live-register state. All six callers ignore EAX, the no-change and changed paths leave unrelated values, and the source method is `void`.

## Geometry And Arithmetic

The method snapshots signed-short `m_scrollPosition +0xfe`, obtains part `2` through `GetPartRect(kRankingScrollPartThumb, &thumbRect)`, and preserves two separate cursor/origin samples. Each `GetScreenOffset` result initializes its corresponding bounds rectangle only when `origin.y != -1000 || origin.x != -1000`; the exact invalid pair is `(-1000, -1000)`. The formal body deliberately does not invent a fallback or early return for those compiler-observed branches.

`m_orientation +0xfc == 0` is vertical; nonzero is horizontal. `m_scrollRange +0x100` is a signed short promoted to signed `int` arithmetic.

Horizontal branch:

- `leadingRect = m_bounds`, then `leadingRect.right = leadingRect.left`; `trailingRect = m_bounds`, then `trailingRect.left = trailingRect.right`.
- `thumbStart = x - m_thumbDragOffset.x + 1`.
- Clamp to `[leadingRect.right, trailingRect.left + thumbRect.left - thumbRect.right]`.
- `trackSpan = trailingRect.left + thumbRect.left - thumbRect.right - leadingRect.right`.
- `newPosition = m_scrollRange * (thumbStart - leadingRect.right) / trackSpan` using signed `cdq; idiv`, truncating toward zero. There is no explicit horizontal post-cap.

Vertical branch:

- `leadingRect = m_bounds`, then `leadingRect.bottom = leadingRect.top`; `trailingRect = m_bounds`, then `trailingRect.top = trailingRect.bottom`.
- `thumbStart = y - m_thumbDragOffset.y + 1`.
- Clamp to `[leadingRect.bottom, trailingRect.top + thumbRect.top - thumbRect.bottom]`.
- `trackSpan = trailingRect.top + thumbRect.top - thumbRect.bottom - leadingRect.bottom`.
- `newPosition = (m_scrollRange + 1) * (thumbStart - leadingRect.bottom) / trackSpan` with signed truncation, followed by a cap to `m_scrollRange`.

No branch contains rounding-to-nearest, a denominator guard, a preclamp of `m_scrollRange`, or removal of the observed `+1`; adding any would change behavior.

## Owner Method Factorization And No-Change Behavior

- If `oldPosition == newPosition`, control jumps directly to the epilogue. There is no owner lookup, write, refresh, or invalidation.
- On change, inherited primary vtable slot `+0x1c` at `0x00610a68` resolves through `0x005447a0` to the pane owner/parent route and yields [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md).
- Binary `0x0045ad79-0x0045adae` stores owner `+0x10c`, conditionally synchronizes/invalidate the child at owner `+0x108`, calls `0x0045b080`, then invalidates owner bounds. This sequence is not direct source field access.
- Independently modeled `RankingEventListPane::SetScrollPosition` at `0x0045b200` has arguments `(int, short, short)`, ends `retn 0x0c`, ignores argument 1, compares old/new, and then executes the exact same owner/child/refresh/invalidation sequence.
- Its ordinary calls at `0x0045b4c4` and `0x0045b4e6` pass source arguments `(0, oldPosition, newPosition)`. Because UID0000Y6 already proves old/new differ, inlining removes the callee's redundant guard and yields the observed tail exactly.
- The source operation is therefore `owner->SetScrollPosition(0, oldPosition, newPosition)`. Direct access to `RankingEventListPane` private fields is rejected as decompiler/inliner expansion.

## IDA MCP Evidence

- Evidence-time database `bf5519ae` was healthy during both report passes: `idb_list`/`server_health` requests `1131-1132` and `1201-1202` reported the NexusTK worker, `status:ok`, imagebase `0x400000`, and analysis/Hex-Rays/strings caches ready.
- `lookup_funcs` request `1102` confirmed target size `0x1b4`, end-exclusive `0x0045adc4`, and no function at the padding/successor starts.
- `decompile`/full `disasm` requests `1111-1112` confirmed all field accesses, both sentinel-controlled rectangle initializations, horizontal/vertical formulas, signed divisions, changed-value guard, and expanded owner tail.
- `xref_query` requests `1114` and `1134` confirmed exactly six code xrefs, no target data xref, and zero xrefs at `0x0045adc4`/`0x0045add0`.
- `get_bytes` request `1105` confirmed final `5d c2 08 00`, twelve following `0xcc` bytes, and the raw successor prologue.
- `get_int`/analysis request `1101`/`1121` confirmed primary slot `+0x1c` points to shared owner lookup `0x005447a0`.
- Correction requests `1203`, `1205`, and `1206` compared `0x0045b200` and UID0000Y6 instruction by instruction; request `1204` confirmed both leading-zero ordinary calls.

## Source-Quality And Negative Evidence

- Accepted source-facing names are `DragToPosition`, `GetPartRect`, `GetOwnerPane`, `SetScrollPosition`, `m_scrollPosition`, `m_scrollRange`, `m_orientation`, `m_thumbDragOffset`, `thumbRect`, `leadingRect`, `trailingRect`, `thumbStart`, `trackStart`, `maxThumbStart`, and `trackSpan`. Exact private spellings remain inferred, not original-symbol proof.
- Generic `ScrollPane`/`ScrollBar.cpp` ownership is rejected because the changed path is a ranking-list method call and all callers are ranking-local.
- Direct `RankingEventListPane` ownership is rejected because the receiver, pre-callback fields, and six callers are `RankingEventScrollPane`; the list pane is the callback consumer.
- Direct owner field access is rejected by the independently modeled `SetScrollPosition` factorization and potential private-access violation.
- X-first coordinates, unsigned range semantics, nearest rounding, defensive division, a one-argument owner overload, and merging UID0000Y7 are all contradicted by current evidence.
- The old instruction to keep C++ blank is historical and superseded. Original-symbol uncertainty caps confidence at `92`; it no longer blocks a source-shaped first draft.

## Score Rationale

- Completion `90`: exact formal C++, half-open range/padding, six callers, coordinate/type contract, complete arithmetic, owner-method factorization, no-change path, negative evidence, and source placement are documented.
- Confidence `92`: fresh MCP, accepted geometry/layout/caller docs, B005 Destination 6, independent `0x0045b200` comparison, and ordinary owner-method callers agree. Confidence remains below final/high-90s because original private spellings and the complete class declarations are unavailable.

## Cross-References

- [UID:0000MZ][RankingDialog](by-file/RankingDialog.md)
- [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md)
- [UID:0000BN][RankingEventListPane](by-class/RankingEventListPane.md)
- [UID:0001VO][RankingEventScrollPaneLayout](by-type/by-struct/RankingEventScrollPaneLayout.md)
- [UID:0001ZB][0x0045a710-0x0045a98a.RankingEventScrollPaneGetScrollRegionRect](by-memory/0x0045a710-0x0045a98a.RankingEventScrollPaneGetScrollRegionRect.md)
- [UID:0000Y5][0x0045a990-0x0045ac0f.RankingEventScrollPaneRawHoverDragHelpers](by-memory/0x0045a990-0x0045ac0f.RankingEventScrollPaneRawHoverDragHelpers.md)
- [UID:0000XZ][0x00458610-0x0045f9f5.RankingDialog](by-memory/0x00458610-0x0045f9f5.RankingDialog.md)

## Changes

- 2026-07-12 B003 UID0000Y6 source-quality implementation callback:
  - Before: `86/90`, blank formal C++, stale no-code deferral, and expanded owner-field prose without source factorization.
  - Changed to: `90/92` with exact `void RankingEventScrollPane::DragToPosition(int y, int x)` managed C++, complete sentinel/geometry/arithmetic/caller/padding evidence, no-change behavior, and `GetOwnerPane()->SetScrollPosition(0, oldPosition, newPosition)` factorization. Owner/emitter UID0000BO, source route UID0000MZ, reconstructable true, blank optional position, `Nested:0`, inclusive filename endpoint, and adjacent UID0000Y5/UID0000Y7 dispositions remain unchanged.
  - Evidence: accepted B003 report SHA256 `197763CFB8A279D81C02392E482DF0DFD10DE52B3328D9F473A733EA01C7258C`; MCP database `bf5519ae` requests `1101-1105`, `1111-1114`, `1121`, `1131-1134`, and `1201-1206`; accepted UID0000Y3/Y4/Y5/ZA/ZB and geometry UID00015S support. B005-owned broader class/split work is explicitly outside this narrow page edit.

- 2026-05-31: Marked reconstructable and attached to [UID:0000BO][RankingEventScrollPane](by-class/RankingEventScrollPane.md); replaced the provisional C++ drag-position candidate with source-shape notes.
  - Before: the page contained a C++ sketch with unverified owner/geometry/helper names.
  - After: the page records exact drag math, owner update path, and dependencies while deferring final C++.
  - Evidence: IDA decompilation confirms old/current position fields, thumb rectangle helper call, owner callback path, list refresh call, and mirrored scroll-child write.
- 2026-06-04: Raised completion from `70` to `86` and confidence from `85` to `90` after live IDA MCP re-check of the modeled helper.
  - Before: the page had older caller/callee notes and collapsed the scroll-pane drag-anchor offsets with the owner list-pane offsets.
  - After: the page records the exact boundary, six direct callers, direct and indirect call sites, orientation/max/current-position reads, scroll-pane drag-anchor point at `+0x108/+0x10c`, and the owner/child update path after the vtable `+0x1c` owner callback.
  - Evidence: live disassembly confirms `sub_45AC10` at `0x0045ac10-0x0045adc4`, `CodeRefsTo(0x0045ac10)`, direct calls at `0x0045ac35`, `0x0045ac44`, `0x0045ac4f`, `0x0045ac91`, `0x0045ac9c`, and `0x0045ada1`, indirect calls at `0x0045ad74`, `0x0045ad9c`, and `0x0045adae`, and field accesses at `+0x44`, `+0xfc`, `+0xfe`, `+0x100`, `+0x108`, and `+0x10c`.
