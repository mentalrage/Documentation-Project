*** UID:0002KQ | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000N5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:10 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#include "ResourceLayoutTable.h"

#include "EPFTileContext.h"
#include "ImageFrameTable.h"
#include "ImageLib.h"
#include "../ui/core/RectBounds.h"
#include "../util/MemoryMan.h"

void __stdcall ResourceLayoutRawBufferLookupEntry(
    const unsigned char *buffer,
    EPFTileContext *outContext)
{
    MemoryMan *memory = GetMemoryMan();
    unsigned short header[4];
    int entryTableOffset;
    memory->MemmoveWrapper(header, buffer, sizeof(header));
    memory->MemmoveWrapper(&entryTableOffset, buffer + 8,
                           sizeof(entryTableOffset));

    ResourceLayoutEntry *entries =
        static_cast<ResourceLayoutEntry *>(
            memory->AllocateBufferMemory(
                sizeof(ResourceLayoutEntry) * (header[0] + 1)));
    const unsigned char *record = buffer + 12 + entryTableOffset;
    short top;
    short left;
    short bottom;
    short right;
    int offsets[2];
    memory->MemmoveWrapper(&top, record, sizeof(top));
    memory->MemmoveWrapper(&left, record + 2, sizeof(left));
    memory->MemmoveWrapper(&bottom, record + 4, sizeof(bottom));
    memory->MemmoveWrapper(&right, record + 6, sizeof(right));
    memory->MemmoveWrapper(offsets, record + 8, sizeof(offsets));

    InitRectBounds(reinterpret_cast<RectBounds *>(&entries[0]),
                   left, top, right, bottom);
    unsigned char *payloadBase =
        const_cast<unsigned char *>(buffer + 12);
    entries[0].pixelData = payloadBase + offsets[0];
    entries[0].encodedMaskData = payloadBase + offsets[1];

    if (outContext != 0) {
        outContext->pixelData = entries[0].pixelData;
        outContext->rowStridePixels =
            entries[0].right - entries[0].left;
        outContext->bounds =
            *reinterpret_cast<RectBounds *>(&entries[0]);
        // Preserve the original uninitialized next-row read and leak.
        outContext->encodedMaskByteCount =
            static_cast<int>(entries[1].pixelData -
                             entries[0].encodedMaskData);
        outContext->encodedMaskBytes =
            entries[0].encodedMaskData;
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
#ifndef NEXUSTK_RENDER_RESOURCELAYOUTTABLE_H
#define NEXUSTK_RENDER_RESOURCELAYOUTTABLE_H

struct ArchiveMetadataTable;
struct EPFTileContext;
struct RectBounds;

void __stdcall ResourceLayoutRawBufferLookupEntry(
    const unsigned char *buffer,
    EPFTileContext *outContext);
RectBounds *__stdcall ResourceLayoutRawRecordGetEntryRect(
    const ArchiveMetadataTable *table,
    int entryIndex,
    RectBounds *bounds);

#endif
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: B001-023 assigns parent [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md); exact half-open boundary, one `UserLookPane::ParseLookPacket` caller, no pointer hits, `__stdcall` raw-buffer behavior, and rejection of class/caller/global ownership are documented. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004d03a0-0x004d04d0 ResourceLayoutRawBufferLookupEntry

## Status

- Entity kind: externally linked free helper.
- Disposition: reconstructable NexusTK project code.
- Rebuild handling: `source-authored`
- Parent aggregate: [UID:000174][0x004d0120-0x004d1860.ResourceLayoutTable](by-memory/0x004d0120-0x004d1860.ResourceLayoutTable.md)
- IDA function: `sub_4D03A0`, `0x130` bytes, ending at IDA exclusive end `0x004d04d0`.
- 2026-06-06 A008 live IDA recheck: still a two-argument `__stdcall` helper with one code xref at `0x005a0042`; the caller preloads `ecx = dword_67A744`, but the callee overwrites `ecx`, so the evidence does not support attaching this as a `thiscall` `ImageLib`/`ResourceLayoutTable` method.

## Supervisor Parent-Gate Audit

Assign this target to [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md). The child clears the corrected gate at `86/89`, and B001-023 raises the file-level helper parent to `88/86`. The helper is not a `thiscall` [UID:0000BY][ResourceLayoutTable](by-class/ResourceLayoutTable.md) method because it returns with `retn 8` and overwrites the caller-preloaded `ecx`; it is also not owned by [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md), because the caller merely supplies a raw profile-image buffer to centralized ResourceLayout-format parser code. A class-parent assignment would overclaim method membership, but a file-level raw-helper assignment is the best defensible source-owner inference.

## Autogen Status

- Reconstructable: true. This is source-authored raw-buffer parsing code.
- Autogen parent: [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md). This is a file-level raw helper, not a class method.
- Reconstruction status: complete formal CPP and centralized owner H are implemented. The unusual next-row read and unreleased allocation are preserved literally as observed behavior rather than treated as a blocker.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 93 | Exact range/ABI/body, raw header/row parsing, one-row initialization, output projection, original uninitialized next-row read, unreleased allocation, owner/header dependencies, source position, caller/callee and generated expectations are complete. |
| Confidence | 93 | Boundary, sole caller, `__stdcall` ABI, ResourceLayout-family ownership, exact field contracts and negative behavior evidence are direct; only the recovered helper spelling remains inferred. |

## Behavior

Parses a raw in-memory resource-layout buffer and optionally fills an [UID:00004I][EPFTileContext](by-class/EPFTileContext.md)-like output for the first entry. This helper does not take a `this` pointer; IDA reports it as `__stdcall`.

IDA MCP decompilation on 2026-05-31 shows:

- copies the first 8 header bytes and a 4-byte table offset from the caller-provided buffer;
- allocates `24 * (entryCount + 1)` bytes for [UID:0000VB][ResourceLayoutEntry](by-item/ResourceLayoutEntry.md) rows;
- reads one table row from `buffer + tableOffset + 12`;
- initializes a rectangle with the same `0x004b7c50` helper used by the DAT-backed loader;
- converts pixel and encoded-mask offsets relative to `buffer + 12`;
- if an output pointer is supplied, writes pixel pointer, row stride, bounds, encoded-mask size, and mask pointer in the same field pattern used by `ResourceLayoutTable::LookupLayoutEntry`.

This appears to be a raw-buffer companion to the DAT-backed resource-layout loader, likely used when the EPF/EPD payload has already been materialized in memory.

2026-06-06 A008 IDA recheck adds these details:

- `lookup_funcs` keeps `0x004d03a0-0x004d04d0` as `sub_4D03A0`; `0x004d04cf` is still inside this helper, and `0x004d04d0` begins `sub_4D04D0`.
- The function returns with `retn 8`, matching exactly two stack arguments: raw source buffer and optional output context pointer.
- The caller at `0x005a0042` loads `ecx = dword_67A744` immediately before the call, but the helper overwrites `ecx` with the memory-manager context and never reads a `this` pointer. Keep the apparent singleton preload as caller-side context, not method evidence.
- `sub_516050` decompiles as a plain `malloc` wrapper with throw-on-failure behavior, and `sub_516220` decompiles as `memmove`. Because the allocation is not zero-fill, the helper's `output+0x20 = row1.pixel - row0.mask` calculation still needs caller-side or format-side confirmation before final C++ is emitted.

Historical 2026-07-02 B010 source-quality reanalysis resolved the old owner/type-name blockers but kept C++ blank:

- [UID:0002KO][0x004d0120-0x004d02e5.ResourceLayoutTableLoadResourceIndex](by-memory/0x004d0120-0x004d02e5.ResourceLayoutTableLoadResourceIndex.md) proves the safe DAT-backed next-row pattern: it loops over every `ResourceLayoutEntry`, rebases each row's pixel/mask offsets, then writes a zero-bounds sentinel row whose pointer fields point at the table boundary.
- [UID:0002KP][0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry](by-memory/0x004d02f0-0x004d039e.ResourceLayoutTableLookupLayoutEntry.md) uses the same `nextEntry.pixelData - currentEntry.encodedMaskData` encoded-mask byte count, but that class-backed method is safe because `LoadResourceIndex` populated rows plus the sentinel.
- This raw-buffer helper lacks the DAT-backed row loop and lacks the sentinel write. Its bounded disassembly directly reads second-row `pixelData` from `[edx+28h]` and subtracts first-row `encodedMaskData` from `[edx+14h]` after initializing only the first row in the temporary allocation.
- The companion raw rectangle helper [UID:0002KT][0x004d05a0-0x004d05e6.ResourceLayoutRawRecordGetEntryRect](by-memory/0x004d05a0-0x004d05e6.ResourceLayoutRawRecordGetEntryRect.md) does not resolve this path: it copies rectangles from caller-provided record arrays and has no call edge into this helper.
- Rejected alternatives for final C++: not a `ResourceLayoutTable` class method, not an `ImageLib` method, not `UserLookPane`-owned parser code, not a UID0002KT route, not a decompiler artifact, and not safe source-natural C++ yet. A loop, synthesized sentinel, or forced zero mask count would add behavior not present in this `0x130`-byte helper.

## Caller Evidence

2026-06-03 restarted IDA MCP `py_eval` finds one direct xref/caller for `0x004d03a0`: callsite `0x005a0042` inside `sub_59F610` / [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md) `UserLookPane::ParseLookPacket`.

The caller window distinguishes two profile/look image payload cases:

- `0x0059ff9b-0x0059fff4` checks for literal `"JPF"` and sends JPF payloads to [UID:0002TL][0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext](by-memory/0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext.md).
- `0x005a001d-0x005a0042` copies the non-JPF payload into the pane buffer at `this+0xb90`, rejects records whose bytes `+6` or `+7` are nonzero, loads `dword_67A744` / [UID:0000QU][g_pEPFLib](by-global/g_pEPFLib.md) into `ecx`, then calls this helper with the raw buffer pointer and output context at `this+0xd9c`.

This supports the current interpretation as a raw in-memory resource-layout/profile-image helper. It does not make the helper source-owned by `UserLookPane`; the body belongs to the ResourceLayout helper family and consumes the same 24-byte layout-entry format as its neighboring ResourceLayout functions.

2026-06-06 A008 live caller-window check reconfirmed the exact branch:

- `0x0059ff9b` compares the packet payload against literal `"JPF"`.
- The JPF branch copies the payload into `this+0xb90`, then calls [UID:0002TL][0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext](by-memory/0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext.md) with output at `this+0xd9c`.
- The non-JPF branch copies through `sub_516220`, rejects nonzero bytes at raw offsets `+6` and `+7`, pushes `this+0xd9c` and the raw buffer pointer, preloads `ecx` with [UID:0000QU][g_pEPFLib](by-global/g_pEPFLib.md), and calls this helper at `0x005a0042`. Preserve that preload as semantic ResourceLayout/ImageLib context only; it is not `thiscall` or owner evidence because the callee uses two stack arguments and returns with `retn 8`.

## Callees And Dependencies

2026-06-03 restarted IDA MCP `py_eval` reports outbound refs from this helper to:

- `sub_516030`, `sub_516220`, and `sub_516050` for runtime/library buffer/memory allocation/copy-style work;
- [UID:00015S][0x004b7c30-0x004b7f87.RectGeometryHelpers](by-memory/0x004b7c30-0x004b7f87.RectGeometryHelpers.md) helper `0x004b7c50` for rectangle initialization from the parsed `left/top/right/bottom` words;
- `___security_cookie` and `@__security_check_cookie@4`, which are compiler security-cookie support and not source-level ResourceLayout dependencies.

2026-06-06 A008 decompiled the support helpers used here: `0x00516050` is `malloc(size)` plus Win32Error throw-on-failure handling, and `0x00516220` is a three-argument `memmove` wrapper. Those checks confirm the raw-buffer helper owns heap allocation for a temporary `ResourceLayoutEntry` array and does not receive zero-initialized storage from the allocator.

## Boundary Evidence

IDA enumerates this function between `LookupLayoutEntry` and `CopyEntryTileContext`. The bytes from `0x004d039e` to `0x004d03a0` are two `0xcc` alignment bytes; the function runs from `0x004d03a0` through IDA exclusive end `0x004d04d0`.

2026-06-03 restarted IDA MCP `py_eval` reconfirms `sub_4D03A0` at `0x004d03a0-0x004d04d0`, adjacent `sub_4D04D0` at `0x004d04d0-0x004d0522`, no function at padding endpoint `0x004d039e`, no xrefs to terminal byte `0x004d04cf`, and no literal dword references to `0x004d03a0`. Boundary bytes are `00 5e 5d c2 0c 00 cc cc` before the helper start and `0f 00 8b e5 5d c2 08 00` at the helper tail, matching `retn 8` immediately before the next prologue.

2026-06-06 A008 `py_eval` repeated the byte and xref checks: previous bytes before the start are `00 00 c7 46 24 00 00 00 00 5e 5d c2 0c 00 cc cc`, tail bytes are `8b 4d fc 33 cd e8 65 72 0f 00 8b e5 5d c2 08 00`, the next bytes begin `55 8b ec 83 ec 2c`, code refs to the start are exactly `[0x005a0042]`, and there are no data refs to the start.

2026-06-07 A007 Batch 005 IDA MCP recheck confirms the file-range correction: `0x004d03a0` is a `0x130`-byte function ending at exclusive address `0x004d04d0`, byte `0x004d04cf` is the final `00` byte of this helper's `retn 8`, and `0x004d04d0` begins the next function with `55 8b ec 83 ec`. The same check found one code xref at `0x005a0042` inside `sub_59F610` / `UserLookPane::ParseLookPacket`, no pointer hits to the start address, and the caller branch that rejects non-JPF records with nonzero raw bytes `+6` or `+7` before pushing the raw buffer and `this+0xd9c` output context.

2026-06-10 B001-023 parent/source audit resolves the prior ownership uncertainty to a file-level helper assignment. [UID:0000BY][ResourceLayoutTable](by-class/ResourceLayoutTable.md) and [UID:0000K2][ImageLib](by-file/ImageLib.md) remain useful context, but the class parent would be too strong for a `retn 8` raw helper and the ImageLib singleton preload is not read by the callee. [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md) is the narrower direct source grouping for raw ResourceLayout-format helpers that are physically interleaved with the method band but do not take `this`.

## Open Questions

- The direct caller set is audited to one known `UserLookPane::ParseLookPacket` callsite. Original helper spelling remains inferred.
- It is intentionally attached to the file-level [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md) helper grouping, not to the class page, because the calling convention is not a class method.
- Parent/source candidates checked for B001-023: [UID:0000BY][ResourceLayoutTable](by-class/ResourceLayoutTable.md), [UID:0000K2][ImageLib](by-file/ImageLib.md), [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md), [UID:0000QU][g_pEPFLib](by-global/g_pEPFLib.md), and the sole caller aggregate [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md). The ResourceLayoutTable file-level helper grouping is the strongest direct owner; class, global, and caller owners are rejected.
- The function only parses/fills one entry while computing encoded-mask size from the next entry's pixel field. The accepted reconstruction preserves that exact read and the unreleased allocation; a loop, synthesized sentinel, zero-fill, free, forced mask count, or new guard would change the historical client's behavior and is rejected.

## Cross-References

- [UID:000174][0x004d0120-0x004d1860.ResourceLayoutTable](by-memory/0x004d0120-0x004d1860.ResourceLayoutTable.md)
- [UID:00004I][EPFTileContext](by-class/EPFTileContext.md)
- [UID:0000VB][ResourceLayoutEntry](by-item/ResourceLayoutEntry.md)
- [UID:0001VT][ResourceLayoutNameRecord](by-type/by-struct/ResourceLayoutNameRecord.md)
- [UID:0001KK][0x0059f260-0x005a2523.UserLookPaneAndProfilePanes](by-memory/0x0059f260-0x005a2523.UserLookPaneAndProfilePanes.md)
- [UID:0002TL][0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext](by-memory/0x004d07b0-0x004d09a7.DecodeJpfImageToTileContext.md)
- [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md)

## Changes

## 2026-08-16 UID0000N5 Source-Quality Closure

The accepted callback resolves the former blank-emitter blocker by distinguishing unsafe behavior from uncertain behavior. The instructions prove a complete source-authored operation: allocate `entryCount+1` rows, initialize only row zero, optionally project row zero into the output, read row one's uninitialized pixel pointer for the mask span, and return without releasing the allocation. Faithful preservation requires expressing those operations, not silently repairing them.

UID0002KQ now owns position 10 and the complete `ResourceLayoutTable.cpp` preamble because it is the first child. Its H block owns the sole guarded `ResourceLayoutTable.h` declaration route for both raw helpers. UID0002KT follows at position 20 and keeps a blank H channel. Generated output must contain exactly KQ then KT and no empty marker, static cross-file helper, duplicate row/table type, or second header declaration.

- 2026-07-02 B010 source-quality implementation:
  - Changed score from `85/88` to `86/89`; preserved `CANONICAL_OWNER:0000N5`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000N5`, the exact range, and blank formal C++.
  - Summary/evidence: B010 accepted report rechecked the raw-helper owner route, the `__stdcall`/not-`thiscall` proof, the `g_pEPFLib` caller preload as semantic context only, support-doc field names for `ResourceLayoutEntry` and `EPFTileContext`, the DAT-backed `LoadResourceIndex` row-loop/sentinel pattern, the class-backed `LookupLayoutEntry` next-row mask-size formula, and the companion UID0002KT raw rectangle helper. The helper remains source-authored and file-level ResourceLayoutTable-owned, but final C++ stays blank because this function lacks the DAT-backed row loop/sentinel write and directly reads `entries[1].pixelData` from `[edx+28h]` after only first-row initialization.
- 2026-06-10 B001-023 owner inference:
  - Changed `AUTOGEN_PARENT_UID` from blank to [UID:0000N5][ResourceLayoutTable](by-file/ResourceLayoutTable.md), with position `30`.
  - Summary/evidence: live IDA MCP reconfirmed the two-argument `__stdcall` body, one UserLook raw-profile caller, no pointer hits, and ResourceLayout-format parsing. The class-method parent remains rejected because the helper has no `this`; the caller parent remains rejected because UserLook only supplies a raw buffer. The file-level ResourceLayoutTable raw-helper grouping now clears the `85/85` parent gate and is the best direct source-owner inference.
- 2026-06-08 A010 Batch120 ownership audit:
  - Changed from `84/87` to `85/88`; `AUTOGEN_PARENT_UID` remains blank.
  - Summary/evidence: Batch120 updated the associated source-owner chain and reclassified the blocker as `reviewed-85-but-ownership-unknown`. B001-023 supersedes that status by assigning the file-level ResourceLayoutTable helper parent after rechecking the same `__stdcall`/no-`this` evidence and rejecting class/caller/global ownership.
- 2026-06-07 A007 Batch 005 corrected-range and gate audit:
  - What existed before: score was `78/82`, the page filename/title used `0x004d03a0-0x004d04cf` even though the body documented IDA's exclusive end at `0x004d04d0`, and no corrected-gate parent audit was recorded.
  - Changed to: score is `84/87`, the page is renamed to the corrected half-open range `0x004d03a0-0x004d04d0`, and `AUTOGEN_PARENT_UID` remains blank.
  - Summary/evidence: IDA MCP boundary/caller recheck on 2026-06-07 confirmed the exclusive end at `0x004d04d0`, next prologue bytes at `0x004d04d0`, one code xref from `UserLookPane::ParseLookPacket`, no pointer hits to the helper start, and the unchanged `__stdcall` raw-buffer behavior. Assignment remains blocked because the child is below 85 completion and the direct candidate parents do not meet the corrected 85/85 gate.

- 2026-06-06 A008 caller/convention and allocation caveat update:
  - What existed before: score was `74/80`, the page documented the one caller and raw-buffer behavior, but did not explicitly record that the caller's `ecx = g_pEPFLib` preload is unused by the callee or that the allocation helper is non-zeroing `malloc`.
  - Changed to: score is `78/82`, with live IDA evidence for `retn 8`, one code xref, no data refs, exact caller branch window, support-helper decompilation, and the next-row mask-size caveat.
  - Summary/evidence: IDA MCP `lookup_funcs`, `decompile`, `disasm`, `callers`, `callees`, and `py_eval` on 2026-06-06 confirmed the `0x004d03a0-0x004d04d0` helper boundary, the non-method calling convention despite the caller's singleton preload, the JPF versus raw profile-image branch, the `this+0xd9c` output context, `malloc`/`memmove` support helpers, and the remaining unresolved source-owner/final-C++ issues.

- 2026-06-02 score rationale update:
  - What existed before: score was `58/70`, reconstructable was already `TRUE`, and the page had strong decompilation evidence but no rationale/autogen status.
  - Changed to: score is `70/78` with explicit autogen status and score rationale; parent remains blank because final source ownership is still unresolved.
  - Summary/evidence: IDA MCP decompilation and boundary notes already document the raw-buffer parser behavior, record allocation, `EPFTileContext`-style output writes, and alignment before the function.
- 2026-06-03 caller/dependency audit:
  - What existed before: score was `70/78`, coverage still had an older `58%` row, and the page recorded caller audit as open.
  - Changed to: score is `74/80`, direct caller evidence is documented, and parent/C++ remain blank because final source ownership and output type names are not final-source quality.
  - Summary/evidence: Restarted IDA MCP `py_eval` confirmed the exact function and neighboring boundaries, no literal start-address references, one direct caller at `0x005a0042` inside `UserLookPane::ParseLookPacket`, the JPF versus raw-buffer branch context, calls to memory helpers and `0x004b7c50`, and security-cookie-only compiler support on 2026-06-03.
