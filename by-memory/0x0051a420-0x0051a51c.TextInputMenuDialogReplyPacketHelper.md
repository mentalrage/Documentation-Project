*** UID:000239 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000ER | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000ER | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void TextInputMenuDialog::SendInputReply(const wchar_t *inputText)
{
    unsigned char packet[0x208];
    int cursor = 0;

    PacketBufferAppendUInt8(0x39, packet, &cursor);
    PacketBufferAppendUInt8(m_replyType, packet, &cursor);
    PacketBufferAppendUInt32BE(m_menuObjectId, packet, &cursor);
    PacketBufferAppendUInt16BE(m_selectedOptionId, packet, &cursor);

    if (m_hasExtraString) {
        PacketBufferAppendAnsiString8(
            m_extraString.c_str(), packet, &cursor);
    }

    PacketBufferAppendWideStringAsAnsi8(
        inputText, packet, &cursor);
    g_packetSender->QueueAndSendPacket(
        packet, static_cast<short>(cursor));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready retained TextInputMenuDialog SendInputReply helper with value-first numeric and text-first string PacketBuffer appends, dialog fields, optional stored ANSI text, caller wide-to-ANSI text, exact send route, no-start-xref history, and complete formal C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***




# 0x0051a420-0x0051a51c - TextInputMenuDialog Reply Packet Helper

## UID0003YM PacketBuffer Source Synchronization - 2026-07-16

- The formal body now uses the raw source contracts proven by UID0003YM and UID0003YO: numeric appends are value first, packet base second, cursor third; ANSI/wide string appends are text first, packet base second, cursor third.
- Exact order remains opcode `0x39`, `m_replyType`, big-endian inherited `m_menuObjectId`, big-endian `m_selectedOptionId`, optional `m_extraString`, caller `inputTextWide`, then queue/send with the final cursor narrowed to `short`.
- The prior destination-first calls were stale convenience-wrapper syntax with no emitted wrapper declaration or definition. They are superseded history and must not be restored.
- The local `unsigned char packet[0x208]`, `int cursor`, object fields, optional-string branch, wide-to-ANSI conversion route, raw no-function/no-start-xref caveat, security-cookie/compiler epilog, exact padding, score `90/89`, owner/emitter UID0000ER, and `Nested:0` remain unchanged.
- No bounds checks, null checks, cursor abstraction, duplicate helper, direct byte-write rewrite, or route claim is added. The body remains retained source-authored class code despite absent current inbound reachability.

## Status

- Confidence: strong for raw code boundary, packet serialization behavior, `TextInputMenuDialog` ownership, source-facing signature, PacketBuffer helper roles, field naming, optional stored ANSI text, and caller wide-to-ANSI conversion. The remaining cap is reachability: IDA does not model this as a function and reports no direct xrefs to the raw start.
- Entity kind: raw helper / method body.
- Likely owner: [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md).
- Proposed source file: [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md).
- Related range: [UID:0001BN][0x00517ec0-0x0051a417.TextMenuDialogs](by-memory/0x00517ec0-0x0051a417.TextMenuDialogs.md).
- Evidence basis: IDA MCP raw disassembly, xref, function lookup, PacketBuffer-helper decompilation, and byte checks on 2026-05-28, 2026-06-01, 2026-06-12, and 2026-06-25.
- Autogen handling: assigned to direct semantic owner [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md). The child is `90/89`, the class page is `88/88`, and [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md) is `94/94`, so the strict child/direct-parent gate is satisfied without bypassing the class owner.

## Role

This `0xfc` / 252-byte raw helper serializes a text-input merchant/menu response packet (Verified with int_convert.py). Best source-facing signature:

```cpp
void TextInputMenuDialog::SendInputReply(const wchar_t *inputTextWide);
```

It builds opcode `0x39`, writes dialog/menu identity fields from the `TextInputMenuDialog` object, optionally writes the extra stored dialog string through the ANSI packet writer, appends the caller-supplied wide input text through the wide-to-ANSI packet writer, and sends the packet through the shared packet sender.

The helper sits between the modeled `TextInputMenuDialog` action/update methods and the `ServerItemMenuDialog` constructor block. It should migrate with `TextInputMenuDialog` even though the current IDB has no function object at `0x0051a420` and therefore cannot expose normal caller/callee metadata for the body.

B010 source-quality reanalysis resolves the prior packet-writer/field-name blocker enough for first-draft C++ emission. The no-direct-xref/no-IDA-function state remains a reachability and confidence cap, not an ownership blocker.

## Parent Gate Analysis

The helper is reconstructable project code and belongs with the text-input menu dialog family:

- Direct owner: [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md), not the broader source file, because the body serializes fields from a `TextInputMenuDialog` instance and is named as `SendInputReply` on the class page.
- Direct parent gate: [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md) is now `88/88`, so it clears the strict `85/85` parent gate.
- Child gate: this page is now `90/89` after the B010 source-quality correction incorporated the full raw body, packet field names, PacketBuffer helper names, optional stored ANSI string path, caller wide-to-ANSI path, call list, security-cookie epilog, and padding boundaries.
- File fallback rejected as unnecessary: [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md) is `94/94`, but this class-owned helper attaches to the direct class owner.
- Open caveat: IDA still reports no direct xrefs to `0x0051a420` and no function object at the raw start, so the score stays below high-final levels despite the strong packet-body evidence.

## Raw Code Anchors

| Address | Operation |
| --- | --- |
| `0x0051a420` | Raw prolog with `0x208` / 520 bytes of local packet-buffer state and security-cookie setup (Verified with int_convert.py). |
| `0x0051a43a` | Calls `0x00516030` before packet buffer construction. |
| `0x0051a457` | Pushes opcode byte `0x39` and calls `0x00575650`. |
| `0x0051a46c` | Reads byte field `this + 0x278` and writes it through `0x00575650`. |
| `0x0051a487` | Pushes dword field `this + 0x270` and calls `0x005756d0`. |
| `0x0051a4a0` | Reads word field `this + 0x27a` and writes it through `0x00575670`. |
| `0x0051a4b0` | Checks optional string flag at `this + 0x27c`. |
| `0x0051a4c7` | Appends optional stored text at `this + 0x280` through `0x00584540`/`0x005757d0`. |
| `0x0051a4e9` | Pushes the caller-supplied wide-text argument; `0x0051a4ea` calls `0x00575850` / `PacketBufferAppendWideStringAsAnsi8`. |
| `0x0051a4ef` | Loads [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) and sends the packet through `0x00574bb0`. |
| `0x0051a508` | Verifies the stack cookie through `0x005c7740`. |
| `0x0051a519` | `retn 4`; helper body ends at `0x0051a51c`. |

## Packet Serialization Semantics

| Order | Serialized value | Evidence |
| --- | --- | --- |
| 1 | opcode byte `0x39` | Immediate `6a 39` at `0x0051a457`, call target `0x00575650`. |
| 2 | `m_replyType` byte at `this + 0x278` | `movzx eax, byte ptr [esi+278h]` at `0x0051a46c`. |
| 3 | inherited `m_menuObjectId` dword at `this + 0x270` | `push dword ptr [esi+270h]` at `0x0051a487`; source helper is `PacketBufferAppendUInt32BE`. |
| 4 | `m_selectedOptionId` word at `this + 0x27a` | `movzx eax, word ptr [esi+27Ah]` at `0x0051a4a0`; source helper is `PacketBufferAppendUInt16BE`. |
| 5 | optional `m_extraString` at `this + 0x280` | Guard byte `m_hasExtraString` at `this + 0x27c`; when nonzero, `lea ecx, [esi+280h]`, `0x00584540`, then `0x005757d0` / `PacketBufferAppendAnsiString8`. |
| 6 | caller-supplied `inputTextWide` argument | Loads `[ebp+8]` into `edi`; later pushes `edi` at `0x0051a4e9` and calls `0x00575850` / `PacketBufferAppendWideStringAsAnsi8` at `0x0051a4ea`. |
| 7 | packet send | Loads [UID:0000Q5][g_packetSender](by-global/g_packetSender.md) from `0x0067a7ec`, pushes local packet size/data state, and calls `0x00574bb0`. |

The helper has two distinct string paths. The optional stored `m_extraString` path is guarded by `this+0x27c`, loads `this+0x280`, calls `0x00584540`, and then calls `0x005757d0` / `PacketBufferAppendAnsiString8` at `0x0051a4d3`. The caller argument path loads `[ebp+8]` into `edi`, pushes `edi` at `0x0051a4e9`, and calls `0x00575850` at `0x0051a4ea`; [UID:0003YO][0x005757d0-0x00575a07.PacketBufferStringWriteHelpers](by-memory/0x005757d0-0x00575a07.PacketBufferStringWriteHelpers.md) documents that target as the wide-to-ANSI append helper. MCP decompilation of `0x00575850` takes `LPCWCH`, converts the wide input through the string conversion path, writes one length byte, copies the ANSI bytes, advances the cursor, and releases the temporary string.

This corrected wide argument matches the modeled `TextInputMenuDialog::OnControlCommand` submit path in [UID:0001BN][0x00517ec0-0x0051a417.TextMenuDialogs](by-memory/0x00517ec0-0x0051a417.TextMenuDialogs.md), which reads wide text from child edit control id `6` and uses `0x00575850` at `0x0051a39d`. Do not rewrite the submit path to call this raw helper unless future route evidence proves a direct call or a source macro/inline wrapper relationship.

## Source Draft And Emission Notes

The formal draft above intentionally models the retained raw helper as ordinary class source, not as IDA-generated raw conversion. It preserves the binary's two string writers: optional stored text stays on the ANSI helper and caller input is accepted as wide text, then converted through `PacketBufferAppendWideStringAsAnsi8`. It uses source-facing field names resolved by the B010 pass:

| Field | Offset | Role |
| --- | --- | --- |
| inherited `m_menuObjectId` | `+0x270` | Server menu-object id owned by `MerchantDialogPane`. |
| `m_replyType` | `+0x278` | Packet byte serialized after opcode `0x39`. |
| `m_selectedOptionId` | `+0x27a` | Selected option/choice id. |
| `m_hasExtraString` | `+0x27c` | Optional-string guard. |
| `m_extraString` | `+0x280` | Optional StringBase/SimpleUString-backed ANSI text source. |

The helper is not moved to PacketBuffer, sockets, or the packet sender. Those are callee infrastructure; ownership is the text-input dialog because this body reads dialog fields and emits a dialog-specific opcode `0x39` payload.

## Boundary And Function-Model Evidence

| Check | IDA MCP result |
| --- | --- |
| Previous modeled body | `lookup_funcs 0x0051a3e0` returns `sub_51A3E0`, size `0x37`, ending at `0x0051a417`. |
| Helper body | `lookup_funcs 0x0051a420` returns `Not a function`, but bytes begin with a normal `push ebp; mov ebp, esp; sub esp, 208h` prolog and end with `c2 04 00`; range size is `0xfc` / 252 bytes (Verified with int_convert.py). |
| Next modeled body | `lookup_funcs 0x0051a520` returns `sub_51A520`, size `0x76e`; xrefs to that constructor are from factory paths at `0x0051762b` and `0x00517a5d`. |
| Leading padding | `get_bytes 0x0051a417 size 9` returns nine `0xcc` bytes through `0x0051a41f`. |
| Trailing padding | `get_bytes 0x0051a51c size 4` returns four `0xcc` bytes through `0x0051a51f`. |

## IDA Evidence

- 2026-06-12 A005 live IDA MCP used `NexusTK.exe` MD5 `4247e04e20b65d6414c7238aa8ff5515`, SHA-256 `9aec210bbc5ce592176a21dd8e9d9fd8f250b8d9ea78237915a99ba8cfa9a632`.
- IDA reports no function object at `0x0051a420`, but disassembly from `0x0051a420` through `0x0051a519` is a complete prolog/body/epilog helper with `retn 4`.
- Raw bytes `0x0051a417-0x0051a420` and `0x0051a51c-0x0051a520` are `0xcc` boundary alignment.
- IDA reports modeled `sub_51a3e0` ending at `0x0051a417` and modeled `sub_51a520` beginning at `0x0051a520`.
- `lookup_funcs` confirms `0x00574bb0`, `0x005757d0`, `0x00575850`, `0x00584540`, and `0x00516030` as modeled callees that the raw body reaches by direct calls.
- 2026-06-12 `py_eval` call scan observed direct calls at `0x0051a43a`, `0x0051a459`, `0x0051a474`, `0x0051a48d`, `0x0051a4a8`, `0x0051a4cd`, `0x0051a4d3`, `0x0051a4ea`, `0x0051a505`, and the security-cookie check at `0x0051a511`.
- 2026-06-25 B010 MCP session `80de0a67` reconfirmed that `0x0051a4d3` calls `0x005757d0` for the optional stored string, while `0x0051a4ea` calls `0x00575850` for the caller argument. Decompilation of `0x00575850` shows a wide-string parameter (`LPCWCH`) converted to ANSI bytes before the one-byte-length PacketBuffer append.
- 2026-06-12 disassembly slice reconfirmed the serialized field reads: `this + 0x278` at `0x0051a46c`, `this + 0x270` at `0x0051a487`, `this + 0x27a` at `0x0051a4a0`, optional guard `this + 0x27c` at `0x0051a4b0`, optional stored string base `this + 0x280` at `0x0051a4c7`, and packet sender global `dword_67A7EC` at `0x0051a4ef`.
- `xrefs_to 0x0051a420` reports no direct xrefs in the current IDB; treat this as potentially omitted/unreferenced helper material until class vtable/caller analysis proves otherwise.
- The helper's opcode `0x39` and field reads match the surrounding text-menu dialog reply family documented in [UID:0001BN][0x00517ec0-0x0051a417.TextMenuDialogs](by-memory/0x00517ec0-0x0051a417.TextMenuDialogs.md).

## Score Rationale

- Completion `90`: the page now records exact half-open bounds, raw-body size, stack frame size, packet serialization order, field-read anchors, resolved field/helper names, optional stored ANSI path, caller wide-to-ANSI path, call list, boundary padding, direct parent decision, corrected first-draft C++, generated-output drift, and the no-xref/no-function caveat.
- Confidence `89`: ownership as `TextInputMenuDialog::SendInputReply` is strong from the object field reads, adjacent class methods, packet opcode, parent class documentation, and PacketBuffer helper names, and the caller argument type is now directly supported by `0x0051a4ea -> 0x00575850`; confidence remains below high-final because IDA still lacks a function object and direct xrefs for the raw helper start.

## Cross-References

- [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md)
- [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md)
- [UID:0001BN][0x00517ec0-0x0051a417.TextMenuDialogs](by-memory/0x00517ec0-0x0051a417.TextMenuDialogs.md)
- [UID:0000Q5][g_packetSender](by-global/g_packetSender.md)
- [UID:0003YM][0x00575650-0x00575703.PacketBufferCursorAppendBigEndianHelpers](by-memory/0x00575650-0x00575703.PacketBufferCursorAppendBigEndianHelpers.md)
- [UID:0003YO][0x005757d0-0x00575a07.PacketBufferStringWriteHelpers](by-memory/0x005757d0-0x00575a07.PacketBufferStringWriteHelpers.md)

## Changes

- 2026-08-22 B002 UID0000OP implementation callback: rebased the retained packet body and active field/packet prose to inherited `m_menuObjectId +0x270`; the exact opcode, endian order, optional-string behavior, wide-input conversion, raw boundaries, and no-direct-route caveat are unchanged.
- 2026-08-21 B002 Gate 2A repair: removed stale copied `proposed` claim-mapping rows. The exact helper bytes, wide-input packet behavior, zero-direct-route evidence, inline liveness rationale, source route, and generated disposition remain authoritative here and in the UID0000OP report ledger.
- 2026-06-25 B010 accepted implementation:
  - Raised `88/88` to `90/89`.
  - Corrected the formal source signature from `TextInputMenuDialog::SendInputReply(const char *inputTextAnsi)` to `TextInputMenuDialog::SendInputReply(const wchar_t *inputTextWide)`.
  - Preserved the optional stored text path as `0x0051a4d3 -> 0x005757d0` / `PacketBufferAppendAnsiString8`, and corrected only the caller argument path to `0x0051a4e9 push edi`, `0x0051a4ea -> 0x00575850` / `PacketBufferAppendWideStringAsAnsi8`.
  - Added the historical/superseded note that the earlier target/class/file wording treated the caller argument as ANSI because it misattributed the `0x0051a4ea` call to `0x005757d0`; current MCP disassembly supersedes that caller-text claim while leaving the owner, field names, optional stored string path, no-xref cap, and padding boundaries intact.
- 2026-07-16 B002 UID0003YM callback synchronization:
  - Preserved the complete B010 body, fields, raw boundaries, and reachability caveat while correcting all numeric and string appends to their accepted source-argument-first ABI. Score and ownership are unchanged.
- 2026-06-12 A005 Batch244:
  - Raised `76/82` to `86/86` and assigned direct parent [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md) after confirming the child and class parent both satisfy the strict `85/85` gate.
  - Evidence: live IDA MCP reconfirmed IDB metadata, `lookup_funcs` still reports the raw start as non-function, `xrefs_to` still reports no direct refs, `py_eval` reconfirmed the `0xfc` / 252-byte body, `0x208` / 520-byte local packet-buffer frame, serialized field reads, call list, `retn 4`, and `0xcc` padding boundaries. `int_convert.py` verified the recorded converted sizes.
- 2026-06-21 B010 Rule 26 source-quality incorporation (partly superseded on 2026-06-25):
  - Raised `86/86` to `88/88`.
  - Resolved field names for `+0x278/+0x270/+0x27a/+0x27c/+0x280`, tied the optional stored string path to `0x005757d0` / `PacketBufferAppendAnsiString8`, distinguished this retained raw helper from the observed `OnButtonPress` inline submit path, added first-draft helper C++, and kept the no-direct-xref/no-function state as a confidence cap instead of an ownership blocker. Its earlier `const char *` caller-argument wording was superseded by the 2026-06-25 MCP pass.
- 2026-06-10 A002 strict-gate repair:
  - Raised completion from `74` to `76` for documenting the corrected direct-parent analysis and MCP retry limitation, without raising confidence or adding new raw-body claims.
  - Cleared `AUTOGEN_PARENT_UID:0000OP` because this helper is below `85/85`, the direct semantic owner [UID:0000ER][TextInputMenuDialog](by-class/TextInputMenuDialog.md) is `86/82`, and the source-file parent [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md) must not be used as a bypass for class-owned helper assignment.
- 2026-06-01: Raised completion/confidence from `50/65` to `74/82`, marked reconstructable, and attached to [UID:0000OP][TextMenuDialogs](by-file/TextMenuDialogs.md).
  - Evidence added: exact packet serialization order, callee address map, parent/adjacent function lookup evidence, raw `0xcc` boundary bytes, and the remaining no-xref caveat.
  - Reconstruction status at the time: metadata C++ box remained blank because the page was below the older 95% threshold and field/helper names were not final-source quality. This is superseded by the 2026-06-21 B010 pass, which resolves the source-facing helper/field names and adds first-draft C++ while retaining the no-direct-xref score cap.

## B002 UID0000OP Accepted Whole-File Reconciliation

Retained helper SHA256 0178A271A9088C87DA9EFBFF60F81AD248E5C2C16F1C3A4B804B85C3768D75DC; opcode 0x39, inherited id, BE option, optional ANSI text, CP_ACP wide input.

### Exact Accepted Inventory Disposition

| ID | Exact identity / range | O/E/P | Historical pre-callback -> accepted current score and metadata | CPP/H/compiler and generated disposition | Destination / claim |
| --- | --- | --- | --- | --- | --- |
| I029 | raw retained `TextInputMenuDialog::SendInputReply [0x0051a420,0x0051a51c)`, SHA256 `0178A271A9088C87DA9EFBFF60F81AD248E5C2C16F1C3A4B804B85C3768D75DC` | O `0000ER`; E `0000ER`; P `0000ER` | UID000239 `90/89 -> 93/94`; true; position follows class `30` | child CPP; no H; D25850 body present outside complete class route; callback preserves and routes it; IDA AP only | UID000239 / C043 |

### Exact Caller / Xref Disposition

| ID | Exact inbound code set/count | Exact inbound data set/count | Exact outgoing call disposition |
| --- | --- | --- | --- |
| I029 | `{}` / 0 | `{}` / 0 | calls `51a43a->516030,51a459->575650,51a474->575650,51a48d->5756d0,51a4a8->575670,51a4cd->584540,51a4d3->5757d0,51a4ea->575850,51a505->574bb0,51a511->5c772f` |

The formal source channel above is authoritative. Older generated names and body-only sketches are dated provenance; compiler vtables, RTTI, adjustor thunks, and deleting wrappers are regenerated from ordinary source and are not handwritten. This is first-party NexusTK source, so no third-party import directive applies.
