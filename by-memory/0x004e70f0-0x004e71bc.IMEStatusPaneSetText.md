*** UID:0004BW | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00006I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00006I | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void IMEStatusPane::SetText(const wchar_t *text, int length)
{
    if (length > 255) {
        length = 255;
    }

    const int textWidth = MeasureTextWidth(text, length);
    const int lineHeight = GetLineHeight();
    const int screenWidth = g_pScreenPane->GetScreenWidth();
    const int screenHeight = g_pScreenPane->GetScreenHeight();

    RectBounds statusRect;
    statusRect.right = screenWidth;
    statusRect.bottom = screenHeight;
    statusRect.left = screenWidth;
    if (textWidth > 0) {
        statusRect.left -= textWidth + 4;
    }
    statusRect.top = screenHeight - lineHeight - 4;

    SetBounds(&statusRect, 0);

    wcsncpy_s(statusText, 256, text, length);
    statusText[length] = L'\0';

    Invalidate(&m_bounds);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact emitting IMEStatusPane::SetText split child; current MCP supports the 0xcc-byte function, constructor/mode-change caller closure, upper-only 255 clamp, statusText[256] copy at +0xf8 with explicit terminator, lower-right RectBounds placement, inherited SetBounds, and inherited Invalidate(&m_bounds). | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004e70f0-0x004e71bc IMEStatusPaneSetText

## Status

- Entity kind: `IMEStatusPane` method.
- Direct owner: [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md).
- Source file route: [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md) -> [UID:0000K5][IMEPanes](by-file/IMEPanes.md).
- Containing aggregate: [UID:000189][0x004e70f0-0x004e8433.IMEPaneFamily](by-memory/0x004e70f0-0x004e8433.IMEPaneFamily.md).
- Emitter status: exact child page now emits through [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md) into [UID:0000K5][IMEPanes](by-file/IMEPanes.md). B009 current MCP resolved the child-specific body and replaced the earlier split-callback no-code placeholder.

## Behavior

`IMEStatusPane::SetText` updates the compact ENG/IME/Closed status label. The binary clamps only `length > 255` down to `255`; it does not add a lower-bound clamp, null pointer guard, or `min/max` wrapper. The direct constructor and mode-change callers supply small positive counts.

After the upper clamp, the method measures the counted UTF-16 text through the inherited GrafPort text-width helper at `0x004ba9a0` and reads the current line height through `0x004baaa0`. It then reads the cached screen width and height from `g_pScreenPane` through `0x00557450` and `0x00557460`.

The method builds a four-int `RectBounds` stack local for lower-right placement:

- `right = screenWidth`
- `bottom = screenHeight`
- `left = screenWidth`
- if `textWidth > 0`, subtract `textWidth + 4` from `left`
- `top = screenHeight - lineHeight - 4`

It applies that rectangle through the inherited `Pane::SetBounds` vtable slot `+0x2c`, copies the caller-provided text into `statusText[256]` at `this+0xf8` with `wcsncpy_s(statusText, 256, text, length)`, writes the explicit trailing UTF-16 NUL at `statusText[length]`, and invalidates the current local bounds through inherited `Pane::InvalidateRect` vtable slot `+0x20` with `this+0x44` / `m_bounds`.

## Evidence

- B009 MCP availability: JSON-RPC `initialize` id `1`, `tools/list` id `2`, `idb_list` id `3`, and `server_health` id `4` confirmed active session `supervisor_resume_20260629` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, schema-current tools, `status:"ok"`, `auto_analysis_ready:true`, and `hexrays_ready:true`.
- B009 MCP `lookup_funcs` id `40`: `0x004e70f0 -> sub_4E70F0`, size `0xcc`; `0x004e71bc` is not a function; successor `0x004e71c0 -> sub_4E71C0`, size `0xca`; predecessor `0x004e70e9` is not a function.
- B009 MCP `analyze_function` id `41`: prototype-shaped as `int __thiscall(int this, wchar_t *Source, int)`, callees `_wcsncpy_s`, `sub_4BA9A0`, `sub_4BAAA0`, `sub_557450`, `sub_557460`, and security-cookie check; callers `sub_4E71C0` and `sub_4E72B0`.
- B009 MCP `decompile` id `42`: clamps only `length > 255`, measures width through `0x004ba9a0`, gets line height through `0x004baaa0`, reads `dword_67A7CC` screen width/height through `0x00557450`/`0x00557460`, builds the four-int stack rectangle, calls inherited vtable slot `+0x2c`, copies with `wcsncpy_s((wchar_t *)(this+248), 0x100, Source, MaxCount)`, writes `*(WORD *)(this + 2*MaxCount + 248) = 0`, and calls inherited vtable slot `+0x20` with `this+68`.
- B009 MCP `disasm` id `43`: `cmovg eax, 0xff` implements the upper-only clamp; stack locals from `ebp-0x14` through `ebp-0x8` form `RectBounds`; the vtable `+0x2c` call is at `0x004e7178`; `_wcsncpy_s` call at `0x004e718c`; explicit NUL write at `0x004e7196`; vtable `+0x20` invalidation at `0x004e71a6`; return at `0x004e71b9`.
- B009 MCP `xrefs_to` id `44`: exactly two code xrefs to `0x004e70f0`, constructor caller `0x004e7249` inside `sub_4E71C0` and mode-change caller `0x004e72ec` inside `sub_4E72B0`; no data/vtable xrefs were reported, matching a non-virtual class helper.
- B009 MCP `callees` id `45`: direct callees are `0x004ba9a0`, `0x004baaa0`, `0x00557450`, `0x00557460`, `_wcsncpy_s`, and the security-cookie check.
- B009 MCP `get_bytes` id `46`: predecessor bytes `0x004e70e9-0x004e70ef` are seven `0xcc` bytes; successor bytes `0x004e71bc-0x004e71bf` are four `0xcc` bytes.
- B009 MCP helper/caller evidence ids `60`-`69`: helper sizes confirmed for text width, line height, screen width/height, `Pane::SetBounds`, and `Pane::InvalidateRect`; constructor decompile zeroes `this+0xf8` for `0x200` bytes and calls this method with `L"Closed", 6`; mode-change decompile chooses `L"IME"` or `L"ENG"` for Event type `11` and calls this method with lengths `4` or `6`; `get_string` decoded `Closed`, `IME`, and `ENG`.
- [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md) declares `void SetText(const wchar_t *text, int length)` and `wchar_t statusText[256]` at `+0xf8`.

## Direct Xref / Caller Inventory

| Address / item | Xref / caller / callee | Meaning |
| --- | --- | --- |
| `0x004e7249` | Constructor `sub_4E71C0` calls `0x004e70f0` | Initial status label is set to `Closed` with length `6`; constructor also zeroes `statusText[256]`. |
| `0x004e72ec` | Mode-change handler `sub_4E72B0` calls `0x004e70f0` | Event type `11` switches status label between `IME` length `4` and `ENG` length `6`. |
| `0x004ba9a0` | Direct callee | Counted UTF-16 text-width measurement helper, documented by GrafPort text-run support. |
| `0x004baaa0` | Direct callee | Current line-height helper. |
| `0x00557450` / `0x00557460` | Direct callees on `g_pScreenPane` | Screen width and screen height. |
| `0x00544bd0` | Inherited vtable slot `+0x2c` target | `Pane::SetBounds`. |
| `0x00544800` | Inherited vtable slot `+0x20` target | `Pane::InvalidateRect`. |

## Reconstruction Notes

- This is source-authored status-pane code, not a shared text utility, GrafPort helper, ScreenPane helper, RectBounds owner, read-only-data owner, or InputMan event bridge.
- Formal C++ is populated because current MCP and support docs resolve the child body, owner route, field, helper roles, caller closure, range, and rejected alternatives above the current code-entry gate.
- The C++ uses source-facing IME-family names already accepted on sibling pages: `RectBounds`, `MeasureTextWidth`, `GetLineHeight`, `g_pScreenPane`, `SetBounds`, `Invalidate`, `statusText`, and `m_bounds`. The helper at `0x004ba9a0` is also documented by GrafPort support as `GrafPort::GetTextWidth`; this naming difference is a source-spelling confidence cap, not a behavior blocker.
- The source signature is `void IMEStatusPane::SetText(const wchar_t *text, int length)`. Hex-Rays' integer return is the tail invalidation artifact; both direct callers ignore it and the class shell declares the method as `void`.
- Do not add a split or new UID: MCP proves one complete `0xcc`-byte function with `0xcc` padding before and after.
- Do not add lower-bound or null checks: the binary does not contain those checks and direct callers supply valid small positive counts.
- Do not emit through [UID:0000K5][IMEPanes](by-file/IMEPanes.md) directly: [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md) is the narrow class owner and emits through the file route.

## Cross-References

- [UID:00006I][IMEStatusPane](by-class/IMEStatusPane.md)
- [UID:000189][0x004e70f0-0x004e8433.IMEPaneFamily](by-memory/0x004e70f0-0x004e8433.IMEPaneFamily.md)
- [UID:0004BX][0x004e71c0-0x004e728a.IMEStatusPaneConstructor](by-memory/0x004e71c0-0x004e728a.IMEStatusPaneConstructor.md)
- [UID:0004BZ][0x004e72b0-0x004e72f8.IMEStatusPaneOnIMEModeChange](by-memory/0x004e72b0-0x004e72f8.IMEStatusPaneOnIMEModeChange.md)
- [UID:0004C0][0x004e7300-0x004e737b.IMEStatusPaneOnPaintFrame](by-memory/0x004e7300-0x004e737b.IMEStatusPaneOnPaintFrame.md)
- [UID:00025J][0x0061c360-0x0061c5a0.IMEPaneReadOnlyData](by-memory/0x0061c360-0x0061c5a0.IMEPaneReadOnlyData.md)
- [UID:00016C][0x004ba9a0-0x004bad66.GrafPortTextRunHelpers](by-memory/0x004ba9a0-0x004bad66.GrafPortTextRunHelpers.md)
- [UID:0000S7][g_pScreenPane](by-global/g_pScreenPane.md)
- [UID:0001G7][0x00557380-0x00557467.ScreenCursorHelpers](by-memory/0x00557380-0x00557467.ScreenCursorHelpers.md)

## Score Rationale

| Score | Rationale |
| --- | --- |
| Completion `89` | Current MCP resolves the exact body, caller closure, padding, upper-only clamp, rectangle construction, inherited helper slots, text copy, invalidation, support route, rejected alternatives, and first-draft formal C++. Completion remains below final audit because exact original helper/member spellings are inferred and several sibling status-pane methods remain below individual formal-body review. |
| Confidence `91` | Current MCP and support docs strongly agree on body behavior, owner route, helper dependencies, field use, and caller context. Confidence remains below absolute certainty because original source names are stripped and `MeasureTextWidth` is a source-facing alias over the current GrafPort support name. |

## Changes

- 2026-06-30 B009 implementation callback:
  - Before: score was `82/88`, `EMITTER_UIDS` was blank, formal C++ was blank, and the page still said method-body C++ remained for a later child-specific pass.
  - Changed to: score `89/91`, `EMITTER_UIDS:00006I`, formal `IMEStatusPane::SetText` C++ body, and report-level evidence for the exact range, caller closure, upper-only clamp, `statusText[256]` copy, lower-right `RectBounds` placement, inherited `SetBounds`, and inherited `Invalidate(&m_bounds)`.
  - Summary/evidence: accepted B009 report using current MCP session `supervisor_resume_20260629`; target MCP ids `40`-`46` and helper/caller ids `60`-`69` prove the 0xcc-byte function, `0xcc` padding before/after, constructor/mode-change direct callers, caller literals `Closed`/`IME`/`ENG`, helper callee roles, and rejected alternatives for split/no-code/direct-file/helper/global/read-only ownership.
- 2026-06-30 B008 implementation callback: Created exact split child page from the accepted [UID:000189][0x004e70f0-0x004e8433.IMEPaneFamily](by-memory/0x004e70f0-0x004e8433.IMEPaneFamily.md) aggregate split plan.
