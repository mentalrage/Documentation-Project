*** UID:0002BM | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000N4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000N4 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
static void ParseMihosoftLauncher(RegistryConfig *config)
{
    MemoryMan *memoryMan = MemoryMan::GetMemoryMan();
    wchar_t commandLine[1024];
    wchar_t *tokens[8];
    wchar_t *tokenContext;
    char ansiHost[128];

    wchar_t *argumentText = CopyLauncherCommandLineTail(GetCommandLineW(),
                                                        commandLine,
                                                        _countof(commandLine));
    if (argumentText == NULL)
        return;

    int tokenCount = 0;
    tokenContext = NULL;

    wchar_t *token = wcstok_s(argumentText, L" ", &tokenContext);
    while (token != NULL)
    {
        if (tokenCount >= _countof(tokens))
            return;

        tokens[tokenCount++] = token;
        token = wcstok_s(NULL, L" ", &tokenContext);
    }

    if (tokenCount < 3 || _wcsicmp(tokens[0], L"Mihosoft") != 0)
        AbortWithLauncherMessage(L"\uC774 \uBC84\uC804\uC758 \uBC14\uB78C\uC758 \uB098\uB77C\uB294 \uC804\uC6A9 \uD074\uB77C\uC774\uC5B8\uD2B8\uC785\uB2C8\uB2E4. \uC785\uB825\uC815\uBCF4\uC5D0 \uC624\uB958\uAC00 \uC788\uC2B5\uB2C8\uB2E4.");

    wchar_t *hostText = tokens[1];
    wchar_t *portText = tokens[2];

    if (hostText[0] >= L'0' && hostText[0] <= L'9')
    {
        tokenContext = NULL;
        wchar_t *part = wcstok_s(hostText, L".", &tokenContext);

        for (int i = 0; i < 4; ++i)
        {
            if (part == NULL)
                return;

            config->m_startupAddress[i] =
                static_cast<unsigned char>(_wtol(part));

            part = wcstok_s(NULL, L".", &tokenContext);
        }
    }
    else
    {
        size_t hostLength = wcslen(hostText);
        memoryMan->MemmoveWrapper(config->m_startupHostName,
                                  hostText,
                                  hostLength * sizeof(wchar_t));
        config->m_startupHostName[hostLength] = L'\0';

        int converted = WideCharToMultiByte(CP_ACP,
                                            0,
                                            config->m_startupHostName,
                                            static_cast<int>(hostLength),
                                            ansiHost,
                                            _countof(ansiHost),
                                            NULL,
                                            NULL);
        if (converted >= _countof(ansiHost))
            ReportRangeCheckFailure();

        ansiHost[converted] = '\0';

        hostent *host = gethostbyname(ansiHost);
        if (host == NULL)
            AbortWithLauncherHostMessage(L"\uC774 \uBC84\uC804\uC758 \uBC14\uB78C\uC758 \uB098\uB77C\uB294 \uC804\uC6A9 \uD074\uB77C\uC774\uC5B8\uD2B8\uC785\uB2C8\uB2E4. \uC785\uB825\uC815\uBCF4 \"%s\" \uC5D0 \uC624\uB958\uAC00 \uC788\uC2B5\uB2C8\uB2E4.", hostText);

        memoryMan->MemmoveWrapper(config->m_startupAddress,
                                  host->h_addr_list[0],
                                  4);
    }

    config->m_startupPort = _wtol(portText);
    config->m_startupReady = 1;
    config->m_startupConnectionMode = 5;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Live MCP session `80de0a67` confirms the raw no-function `Mihosoft` selector/host/port parser, one-block boundary, command-line tokenization, dotted IPv4 and DNS host paths, startup address/host/port/ready/mode writes, localized failure paths, no payload writes, and source-ready first-draft `ParseMihosoftLauncher` C++. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00490810-0x00490b01 MihosoftLauncherParser

## Status

- Disposition: reconstructable NexusTK project code
- Entity kind: function
- Parent range: [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md)
- Likely source module: [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md), or a config-adjacent launcher/bootstrap helper module
- Rebuild handling: source-authored
- Confidence: strong for registry/bootstrap ownership, range placement, raw-start status, source-ready helper shape, and launcher parser behavior; capped below final audit because the selector/caller still is not modeled.

## Rebuild Handling

- Reconstructability: true for source-authored launcher parser logic under the registry/config startup parser family.
- Parent handling: attach directly to [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md), which is `88/85` and owns the command-line/session parser island. Keep [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) as the aggregate evidence page, but do not use it as parent until it is raised above the attachment threshold.
- C++ handling: emit first-draft child C++ as file-static `ParseMihosoftLauncher(RegistryConfig *config)`. The raw exported start and missing dispatcher remain caveats for final naming/class-method promotion, but they no longer block a scoped source-shaped child body because this pass resolves the local argument layout, helper roles, field names, failure paths, no-payload negative evidence, and quiet-return behavior.
- Boundary handling: document the range as raw exported code ending before IDA-modeled `sub_490B10`, not as an IDA-modeled function.

## Address Range

`0x00490810-0x00490b01`, ending before the IDA-modeled `sub_490B10` at `0x00490b10`.

IDA does not currently model `0x00490810`, `0x00490b01`, or `0x00490b02` as function objects. Live IDA MCP session `80de0a67` still decodes a normal stack-frame prologue at `0x00490810`; the nearest modeled function after this raw start is `sub_490B10` at `0x00490b10-0x004910c0`.

The predecessor range leaves seven `0xcc` bytes before this start (`0x00490809-0x00490810`), and this range leaves fourteen `0xcc` bytes before the modeled successor (`0x00490b02-0x00490b10`). Session `80de0a67` found zero direct xrefs to `0x00490810`, `0x00490b01`, `0x00490b02`, and `0x00490b10`, and raw VA/RVA pointer-byte scans found no simple stored route for the entry or successor.

## Behavior

This helper skips the executable path, tokenizes the remaining command line on spaces with `_wcstok_s`, and requires at least three tokens before accepting the parse. The first token must case-insensitively match the UTF-16 `Mihosoft` selector at `0x00617048`; the next two tokens are interpreted as host and port. Additional tokens below the loop cap are ignored.

Quiet returns are part of the preserved behavior: missing command-tail delimiters, reaching the eight-token cap, or missing dotted-IPv4 components return without the localized failure dialog. This parser does not consume token three or later and does not write the launcher payload/scratch buffers at `+0x28d9e0`, `+0x28da20`, or sibling payload offsets.

For host tokens beginning with an ASCII digit, it splits on `.` and parses the first four decimal octets with the Visual C wide decimal integer helper at `0x005cea43`, storing the low byte of each octet into `param_1 + 0x2915b4`. For nonnumeric host tokens, it copies the wide host into `param_1 + 0x2915c4` through the `memmove` wrapper at `0x00516220`, converts it to an ANSI scratch buffer with `WideCharToMultiByte`, calls `gethostbyname`, and copies four resolved address bytes into `param_1 + 0x2915b4`.

On success it parses the port token with the same wide decimal helper and writes it to `param_1 + 0x2916c4`, sets `param_1 + 0x2916ca` to `1`, and sets state byte `param_1 + 0x28de28` to `5`.

Malformed token streams or selector mismatches format the UTF-16 message `이 버전의 바람의 나라는 전용 클라이언트입니다. 입력정보에 오류가 있습니다.` and show it through `MessageBoxW`, followed by `PostQuitMessage(0)` and `__loaddll(0)`. DNS failure uses the host-specific UTF-16 format string `이 버전의 바람의 나라는 전용 클라이언트입니다. 입력정보 "%s" 에 오류가 있습니다.` before the same exit path.

## Parser Island Context

- The parent island [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) lists `0x00490810` as one of twelve exported/parser starts between `RegistryConfig` defaults and registry persistence.
- Its predecessor [UID:0002BL][0x00490510-0x00490809.LauncherHostPortPayloadParser](by-memory/0x00490510-0x00490809.LauncherHostPortPayloadParser.md) ends immediately before this raw exported start.
- Its successor [UID:0002BN][0x00490b10-0x004910c0.ComBackedLauncherAuthenticationParser](by-memory/0x00490b10-0x004910c0.ComBackedLauncherAuthenticationParser.md) is the next IDA-modeled authentication parser at `0x00490b10`.
- [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md) explicitly owns the command-line/session parser helper family as part of registry-backed startup normalization.

## Evidence

- Live IDA function lookup reports no modeled function at `0x00490810`, `0x00490b01`, or `0x00490b02`; `sub_490B10` is the modeled successor at `0x00490b10-0x004910c0`.
- Live IDA boundary bytes show seven `0xcc` bytes before `0x00490810` and fourteen `0xcc` bytes before `0x00490b10`; xref and raw pointer scans found no inbound references to either address.
- Live IDA disassembly at `0x00490810` reserves `0xaac` bytes, stores `ecx` in `ebx` as the startup-state base, calls `GetCommandLineW`, copies up to `0x400` wide characters, and skips the executable path by searching for either a quote or a space with `_wcschr`.
- The token loop at `0x00490891-0x004908de` uses `_wcstok_s` with the space delimiter at `0x0060de18`, caps the counter below eight, stores tokens in stack slots, and requires at least three tokens before continuing.
- The selector check at `0x004908e4-0x004908f9` compares the first token with UTF-16 `Mihosoft` at `0x00617048` via `__wcsicmp`; mismatches branch to the general failure UI.
- Numeric host handling at `0x00490912-0x00490984` splits on the `.` delimiter at `0x00612e38`, calls the wide decimal integer parser at `0x005cea43`, and writes four octets to `param_1 + 0x2915b4`.
- DNS host handling at `0x00490989-0x00490a17` copies the wide hostname through `sub_516220` (`memmove` wrapper), converts it with `WideCharToMultiByte`, bounds-checks the `0x80` byte scratch buffer, calls `gethostbyname`, and copies four resolved bytes into `param_1 + 0x2915b4`.
- Success writes at `0x00490a1c-0x00490a39` parse the port, store `param_1 + 0x2916c4`, set `param_1 + 0x2916ca = 1`, and set `param_1 + 0x28de28 = 5`.
- General failure at `0x00490a50-0x00490a9e` formats the UTF-16 dedicated-client input-error message from `0x00617060`, displays it with `MessageBoxW`, posts quit, and calls `__loaddll`; DNS failure at `0x00490aa3-0x00490af7` uses the host-specific format at `0x006170b8`.
- [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md) records the exact surrounding exported starts and notes that IDA models some siblings while this start remains raw but disassembles as real code.

## 2026-06-15 Current Verification

- Live IDA MCP session `a003_objectlist_dispatch_20260615` against `E:\2026\Resources\Read_Only\NexusTK\NexusTK.exe` returned `status: ok`, imagebase `0x400000`, and Hex-Rays ready.
- `lookup_funcs` still reports `0x00490810`, `0x00490b01`, and `0x00490b02` as not IDA-modeled functions, while `0x00490b10` remains modeled as `sub_490B10` with size `0x5b0`.
- `xrefs_to` reports zero cross-references to the raw entry `0x00490810` and zero cross-references to the modeled successor `0x00490b10`, preserving the raw launcher-dispatch caveat.
- Current data xrefs confirm the selector and failure operands used by this page: `Mihosoft` at `0x00617048` from `0x004908e4`, the general UTF-16 dedicated-client input-error message at `0x00617060` from `0x00490a50`, and the host-specific UTF-16 failure format at `0x006170b8` from `0x00490aaf`.
- `make_signature_for_range 0x00490810-0x00490b01` returned `unique: true`; the signature starts with the expected stack-frame/security-cookie prologue `55 8B EC 81 EC AC 0A 00 00 A1 24 2F 67 00`.

## 2026-06-27 B003 Implementation Verification

- Accepted B003 report `0002BM-MihosoftLauncherParser-source-quality.md` used live IDA MCP session `80de0a67` on `ida-pro-mcp`; `server_health` reported the active `NexusTK.exe` database ready with auto-analysis, Hex-Rays, and string cache available.
- `lookup_funcs` kept `0x00490810`, `0x00490b01`, and `0x00490b02` as non-functions while preserving `sub_490B10` at `0x00490b10-0x004910c0` as the modeled successor.
- `xrefs_to`, `find_bytes`, `get_bytes`, `insn_query`, `entity_query`, and `make_signature_for_range` reconfirmed zero direct entry/successor xrefs, zero simple VA/RVA/raw pointer route, exact padding/prologue bytes, a coherent 210-instruction raw body, no source symbols or named non-function entities in the island, and a unique full-range signature.
- Direct UTF-16 byte decoding preserved the `Mihosoft` selector and both Korean failure-message roles because IDA string typing is incomplete in this string cluster.

## Reconstruction Notes

Keep this grouped with launcher/vendor bootstrap parsing. The `Mihosoft` mode name is behavior evidence, not proof of a separate source file.

The source-facing draft uses private file-static `ParseMihosoftLauncher(RegistryConfig *config)` rather than the page-title label `MihosoftLauncherParser`. Completed whole-island evidence rejects the former class-method rehome theory: no entry, pointer, or vtable route exists, while `ECX` use is represented by the explicit parameter.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 88 | Current IDA/MCP evidence records raw boundary bytes, modeled-function absence, xref/pointer absence, exact selector/token gating, host/port field mapping, numeric IPv4 parsing, DNS resolution, range-check behavior, success writes, failure formatting, helper identities, no-payload negative evidence, quiet-return cases, and a unique full-range byte signature. Formal first-draft C++ is now present, but completion remains below final-audit quality because the caller/mode selector and higher-level launcher dispatch table remain unresolved. |
| Confidence | 90 | RegistryConfig ownership, raw range placement, and Mihosoft launcher behavior are supported by live MCP disassembly/query evidence, neighboring parser context, exact selector/failure string operands, absence of direct entry/pointer references, field-name support from sibling parser docs, and a unique range signature. Confidence remains capped because IDA lacks a modeled function object and no inbound dispatch reference has been recovered for this raw start. |

## Cross-References

- [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md)
- [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md)
- [UID:0000BW][RegistryConfig](by-class/RegistryConfig.md)
- [UID:0002BL][0x00490510-0x00490809.LauncherHostPortPayloadParser](by-memory/0x00490510-0x00490809.LauncherHostPortPayloadParser.md)
- [UID:0002BN][0x00490b10-0x004910c0.ComBackedLauncherAuthenticationParser](by-memory/0x00490b10-0x004910c0.ComBackedLauncherAuthenticationParser.md)

## 2026-08-15 Whole-Island Source-Placement Closure

- Final source placement for this child is private file-static `RegistryConfig.cpp` helper `static void ParseMihosoftLauncher(RegistryConfig *config)`; it is not a `RegistryConfig::` member and has no header declaration.
- This entry is a raw start. Its bounded prologue preserves incoming `ECX` as the Config/RegistryConfig object in `EBX`. That proves the explicit object parameter used by the formal C++ but does not prove member-method syntax.
- Whole-island checks cover all twelve parser starts: every entry has zero external inbound entry xrefs; all 24 little-endian VA/RVA start-address searches return zero matches; only `0x0048fca0`, `0x00490b10`, and `0x004918e0` are modeled functions; and none appears in the five-slot RegistryConfig vtable.
- The common evidence fixes one coherent family of private file-static helpers. The stripped dispatcher/call mechanism and original private symbols remain score and exact-spelling caps only; they are not a source-placement or code-emission blocker.
- Accepted Config storage names are `m_startupAddress`, `m_startupHostName`, union `m_startupPort`/`m_launcherAuthenticationToken`, `m_startupServerId`, `m_startupReady`, `m_startupConnectionMode`, `m_startupLaunchCommand`, `m_startupLauncherId`, `m_thrunetAuthenticationToken`, `m_lgInternetSessionId`, `m_lgInternetSessionKey`, `m_launcherPayload`, `m_launcherAuthenticationObject`, `m_kornetWorldPayload`, and `m_kornetWorldPayloadData`.
- Any earlier prose on this page that called the file-static signature provisional, deferred source placement, or proposed a later class-method rehome is retained only as superseded historical reasoning. The current placement above is final for this reconstruction pass.

## Changes

- 2026-06-27 B003 implementation callback:
  - Before: the page was `85/88`, carried blank-C++ blocker wording, and did not preserve the accepted no-payload/quiet-return proof in the score rationale.
  - After: raised to `88/90`, kept owner/emitter/reconstructable metadata unchanged, and inserted formal first-draft `static void ParseMihosoftLauncher(RegistryConfig *config)` C++.
  - Historical summary/evidence: accepted B003 report with live MCP session `80de0a67` reconfirmed raw no-function status, exact `0x00490810-0x00490b01` boundary, no direct xrefs or simple VA/RVA/raw pointer route, `Mihosoft` selector, dotted-IPv4 and DNS host paths, `m_startupAddress`/`m_startupHostName`/`m_startupPort`/`m_startupReady`/`m_startupConnectionMode` writes, range-check behavior, generic and host-specific failure paths, quiet returns, and no payload-buffer writes. Its then-pending class-method question is superseded by the 2026-08-15 whole-island file-static closure.
- 2026-06-02:
  - Before: the page was reconstructable in prose but left unmarked, unparented, and visible in low-completion/low-both queues.
  - After: raised to `72/80`, marked `RECONSTRUCTABLE:TRUE`, and attached to [UID:0000N4][RegistryConfig](by-file/RegistryConfig.md).
  - Summary/evidence: registry-file ownership at `88/82`, parser-island context [UID:000220][0x0048f400-0x00491b28.RegistryCommandLineParsers](by-memory/0x0048f400-0x00491b28.RegistryCommandLineParsers.md), neighboring parser boundaries, and live IDA disassembly/call mix support source reconstruction while retaining the raw-start caveat.
- 2026-06-04:
  - Before: the page still carried stale non-IDA evidence and only summarized the parser behavior.
  - After: raised to `80/86`, removed stale evidence, and recorded live IDA boundary/xref facts, selector/token layout, numeric/DNS host paths, success-field writes, helper identities, and both failure-message paths.
  - Summary/evidence: live IDA reports no function object and no inbound xrefs/pointer hits at `0x00490810`/`0x00490b10`, seven- and fourteen-byte `0xcc` boundary gaps, `_wcstok_s` token gating at `0x00490891-0x004908de`, `Mihosoft` comparison at `0x004908e4-0x004908f9`, IPv4 writes at `0x00490942`/`0x00490974`, DNS resolution at `0x00490989-0x00490a17`, startup-state writes at `0x00490a2a-0x00490a39`, and Unicode failure paths at `0x00490a50-0x00490a9e` and `0x00490aa3-0x00490af7`.
- 2026-06-15:
  - Before: the page was `80/86` and lacked a current-session boundary/signature/xref refresh.
  - After: raised to `85/88` with no owner/emitter/routing change.
  - Summary/evidence: live IDA MCP session `a003_objectlist_dispatch_20260615` reconfirmed the raw unmodeled entry, modeled successor, zero entry/successor xrefs, exact `Mihosoft` and UTF-16 failure string operand xrefs, and a unique full-range byte signature. Final C++ remains blank because source-quality dispatcher, helper, and field names are still unresolved.

## B001 UID0003FV Accepted Callback Synchronization - 2026-07-20

- Final source-quality state is `90/92`; owner/emitter, reconstructable route, position, nesting, range, child relations, and unrelated history remain unchanged.
- This page is the accepted Mihosoft selector/host/port parser. Its three exact wide strings remain direct use-site literals. Selector and token-count checks, dotted-IP and DNS alternatives, address/port/ready/mode writes, and abort behavior are preserved without introducing target-owned arrays or unresolved symbolic names.
- Public RegistryConfig inheritance from the complete Config declaration makes every startup-field access source-legal. The target physical range remains ownerless pooled output; it supplies no cross-translation-unit static declaration and no duplicate literal/GUID array.
- Current formal C++ is the complete destination-specific source body. Earlier blank, comment-only, inferred-symbolic, or unresolved-name conclusions are historical and superseded; raw-entry reachability and original lexical-name uncertainty remain documented confidence caps rather than source blockers.
