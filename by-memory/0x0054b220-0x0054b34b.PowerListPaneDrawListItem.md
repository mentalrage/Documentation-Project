*** UID:0004B7 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000AQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000AP | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:130 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void PowerListPane::DrawListItem(int rowIndex,
                                 const PowerEntryRecord *entry,
                                 const RectBounds *rowBounds)
{
    m_textDrawEnabled = true;

    if (IsItemSelected(rowIndex)) {
        SetDrawColor(143);
        DrawRectFrame(rowBounds);
    }

    const int displayColor = entry->displayColor;
    const int palettePage = (displayColor + 4) / 256;
    RectBounds swatchBounds = MakeRect(rowBounds->left + 5,
                                       rowBounds->top + 1,
                                       rowBounds->left + 15,
                                       rowBounds->bottom - 1);

    if (palettePage != 0) {
        FillColorRect(&swatchBounds, palettePage, displayColor % 256);
    } else {
        SetDrawColor(displayColor);
        FillRect(&swatchBounds);
    }

    SetTextColor(37);
    const wchar_t *label = entry->label;
    const int labelWidth = GetTextWidth(label);
    MoveTo(rowBounds->right - labelWidth,
           ((rowBounds->top + rowBounds->bottom) / 2) + 6);
    DrawWideText(label, wcslen(label));
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact `PowerListPane::DrawListItem` emitter for the `0x12b` vtable-dispatched draw-item body; emits selected-row frame, display-color swatch geometry, UTF-16 label width/length scan and draw, with UID0004B8 retained as the separate fill-color helper. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0054b220-0x0054b34b - PowerListPane DrawListItem

## Status

- Accepted split child of [UID:0001F6][0x00549c20-0x0054b5d5.PowerDialogPane](by-memory/0x00549c20-0x0054b5d5.PowerDialogPane.md).
- Current metadata: `COMPLETION:88`, `CONFIDENCE:91`, `CANONICAL_OWNER:0000AQ`, `RECONSTRUCTABLE:TRUE`, `EMITTER_UIDS:0000AP`, position `130`, formal C++ supplied above. Canonical class ownership is unchanged; direct UID0000AP routing is source ordering only.
- Source placement: [UID:0000MO][PowerDialogPane](by-file/PowerDialogPane.md), `NexusTK/ui/dialogs/PowerDialogPane.cpp`, class [UID:0000AQ][PowerListPane](by-class/PowerListPane.md).
- Formal C++ disposition: source-ready first-draft method emitter. The accepted no-code/blank-emitter disposition was rejected because IDA identifies a live `0x12b` virtual row renderer at `0x0054b220`, and the helper dependency at `0x0054b350` remains split as UID0004B8 rather than blocking this caller body.

## Behavior

This function draws one `PowerListPane` row. The formal arguments are `rowIndex`, `entry`, and `rowBounds`: the first drives the selected-row test, the second supplies the `PowerEntryRecord` fields, and the third supplies the row rectangle used for the frame, swatch, and text placement.

The body first enables text drawing, tests whether `rowIndex` is selected, and, for selected rows, sets draw color `143` and draws a frame over `rowBounds`. It then reads `entry->displayColor`, applies the binary's `(displayColor + 4) / 256` split, and constructs a color swatch rectangle at `left + 5`, `top + 1`, `left + 15`, `bottom - 1`. Nonzero palette pages call [UID:0004B8][0x0054b350-0x0054b4f0.PowerListPaneFillColorRect](by-memory/0x0054b350-0x0054b4f0.PowerListPaneFillColorRect.md); page zero uses direct draw color and `FillRect`.

After the swatch, the method sets text color `37`, treats `entry->label` at row offset `+0x0c` as UTF-16, measures its rendered width, positions text at `rowBounds->right - labelWidth` and vertical center `((top + bottom) / 2) + 6`, then performs an explicit UTF-16 label-length scan through `wcslen(label)` before `DrawWideText`.

The row record is the same `PowerEntryRecord` layout used by the constructor and refresh body: entry id `+0x000`, list column `+0x004`, amount `+0x008`, label `+0x00c`, and display color `+0x20c`.

## Evidence

- MCP session `279422f0` reconfirmed `lookup_funcs 0x0054b220` as `sub_54B220`, size `0x12b`.
- MCP `get_bytes` confirmed `0x0054b34b-0x0054b350` is exactly five `0xcc` successor padding bytes before UID0004B8.
- B004 report recheck confirms the vtable slot `0x00622274 -> 0x0054b220`; `xrefs_to` reports the data xref from that slot and no ordinary static callers, so this is a virtual draw handler rather than a file-local helper.
- The entry write at method start sets inherited byte `this+0x88` / decimal `136` to `1`; the formal C++ records this source-facing state as `m_textDrawEnabled = true` while keeping final inherited field spelling as a confidence cap.
- The argument roles are stable in decompilation: the method uses the row index only for `IsItemSelected(rowIndex)`, uses the row record for `displayColor` and the label, and uses the row bounds for frame/swatch/text coordinates.
- The selected-row branch sets color `143` and draws the row frame before color/text output.
- The color path reads `displayColor` from row offset `+0x20c`, computes `(displayColor + 4) / 256`, uses modulo `256` for the color index, calls UID0004B8 at `0x0054b2d7` only when the split produces a nonzero palette page, and otherwise sets the draw color then dispatches the shared fill callback at `0x0069b3fc`.
- The swatch geometry is exact: `left + 5`, `top + 1`, `left + 15`, `bottom - 1`.
- The text path renders the UTF-16 label from row offset `+0x0c`, places it flush right using measured text width, and performs an explicit label-length scan before draw. The accepted C++ expresses that scan as `wcslen(label)`.
- UID0004B8 remains a separate helper call target at `0x0054b350-0x0054b4f0`; this page does not inline its clipping/palette-fill body.
- Rejected dispositions: no-code/blank-emitter is not appropriate for this live virtual method, and aggregate emission is not appropriate because [UID:0001F6][0x00549c20-0x0054b5d5.PowerDialogPane](by-memory/0x00549c20-0x0054b5d5.PowerDialogPane.md) is a non-emitting source-island container.

## Score Rationale

- `COMPLETION:88`: the exact range, virtual dispatch slot, row/entry/bounds roles, selected frame, swatch geometry, color split, text placement, and helper call are all documented and now represented in a first-draft C++ body. Remaining incompleteness is limited to final engine helper spellings and exact private class declaration details in the surrounding source.
- `CONFIDENCE:91`: confidence is high because MCP and report evidence agree on range, size, vtable route, field offsets, and instruction-level behavior. It is capped below higher final-source confidence by inferred renderer helper names and the retained separate UID0004B8 helper body.

## Cross-References

- [UID:0001F6][0x00549c20-0x0054b5d5.PowerDialogPane](by-memory/0x00549c20-0x0054b5d5.PowerDialogPane.md)
- [UID:0000AQ][PowerListPane](by-class/PowerListPane.md)
- [UID:0000MO][PowerDialogPane](by-file/PowerDialogPane.md)
- [PowerListPaneFillColorRect](by-memory/0x0054b350-0x0054b4f0.PowerListPaneFillColorRect.md)

## Changes

- 2026-07-15 B001 UID0004B4 accepted callback: preserved `88/91`, canonical owner UID0000AQ, complete draw behavior/evidence/formal C++, and changed only output routing to UID0000AP position `130`, after OnMouseEvent UID0004B6 and before FillColorRect UID0004B8.

- 2026-07-02 B004 accepted UID0004B7 implementation callback:
  - Raised metadata to `COMPLETION:88`, `CONFIDENCE:91`, kept `CANONICAL_OWNER:0000AQ`, kept `RECONSTRUCTABLE:TRUE`, set `EMITTER_UIDS:0000AQ`, and kept blank emitter position.
  - Inserted formal `void PowerListPane::DrawListItem(int rowIndex, const PowerEntryRecord *entry, const RectBounds *rowBounds)` C++ from the accepted report, with UID0004B8 retained as the separate `FillColorRect` helper.
  - Added report-level evidence for the exact `0x12b` range, vtable slot `0x00622274 -> 0x0054b220`, argument roles, `displayColor + 4` split, swatch geometry, selected-row frame, text placement, explicit UTF-16 length scan, rejected no-code disposition, and confidence caps.
- 2026-06-29 B002 UID0001F6 implementation: created as the exact private list draw-item child.
