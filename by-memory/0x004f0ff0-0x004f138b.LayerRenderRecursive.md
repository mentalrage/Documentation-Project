*** UID:0004O6 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:000073 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:000073 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:170 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
bool Layer::RenderRecursive(GrafPort *grafPort, int xOffset, int yOffset,
    Region *renderedRegion, HierList *list)
{
    bool rendered = false;
    if (list == NULL) {
        list = m_nodes;
    }

    LayerNode *owner = NodeData(list->ownerNode);
    Region parentRegion;
    owner->pane->GetRenderRegion(&parentRegion);

    RectBounds ownerBounds;
    owner->pane->GetBounds(&ownerBounds);
    parentRegion.IntersectWithRect(&ownerBounds);
    parentRegion.Offset(yOffset, xOffset);

    RectBounds screenBounds;
    InitRectBounds(&screenBounds, 0, 0, g_screenWidth, g_screenHeight);

    const int count = list->GetCount();
    for (int index = 0; index < count; ++index) {
        LayerNode *node = NodeAt(list, index);
        Pane *pane = node->pane;
        if (!pane->IsVisible()) {
            continue;
        }

        Point origin;
        pane->GetLocalOffset(&origin);
        origin.x += xOffset;
        origin.y += yOffset;

        if (node->dirty) {
            rendered = true;

            if (pane->GetMode() != 4) {
                Region drawRegion;
                drawRegion.CopyRectFrom(*node->dirtyRegion);

                RectBounds paneBounds;
                pane->GetBounds(&paneBounds);
                drawRegion.IntersectWithRect(&paneBounds);

                Region paneRegion;
                pane->GetRenderRegion(&paneRegion);
                drawRegion.IntersectWith(paneRegion);
                drawRegion.Offset(origin.y, origin.x);
                drawRegion.IntersectWith(parentRegion);
                drawRegion.IntersectWithRect(&screenBounds);

                Region localRegion;
                localRegion.CopyRectFrom(drawRegion);
                localRegion.Offset(-origin.y, -origin.x);
                grafPort->SetClipRegion(&drawRegion);

                RectBounds clipBounds;
                RectBounds localBounds;
                localRegion.GetRect(&clipBounds);
                drawRegion.GetRect(&localBounds);

                if (!IsRectEmptyOrInvalid(&clipBounds)) {
                    if (pane->UsesCustomPaint()) {
                        grafPort->BeginPaint();
                        pane->OnPaint(grafPort, &clipBounds, &localBounds);
                        grafPort->EndPaint();
                    } else if (g_pActiveMapPane != NULL
                        && pane == g_pActiveMapPane
                        && g_pActiveMapPane->HasScreenEffecters()) {
                        ScreenEffecter *effecter =
                            g_pActiveMapPane->FindActivePixelEffecter();
                        if (effecter != NULL) {
                            effecter->Apply(grafPort, &clipBounds, &localBounds);
                        } else {
                            grafPort->DrawPane(
                                pane, &clipBounds, &localBounds, pane->GetMode());
                        }
                    } else {
                        grafPort->DrawPane(
                            pane, &clipBounds, &localBounds, pane->GetMode());
                    }

                    renderedRegion->UnionWithRect(&localBounds);
                }
            }

            node->dirty = false;
            node->dirtyRegion->SetEmpty();
        }

        HierListNode *header = NodeHeader(node);
        if (header->childList != NULL && !node->blocksChildTraversal) {
            if (RenderRecursive(grafPort, origin.x, origin.y,
                    renderedRegion, header->childList)) {
                rendered = true;
            }
        }
    }

    return rendered;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Recursively renders visible Layer nodes, clips and offsets Regions, clears processed dirty state, and applies the active MapPane ScreenEffecter branch. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004f0ff0-0x004f138b LayerRenderRecursive

## Status

- Exact exclusive range: `[0x004f0ff0,0x004f138b)`; 923 bytes; accepted SHA256 evidence `0B9F9639...F5CF`.
- Disposition: source-authored Layer member, reconstructable through [UID:000073][Layer](by-class/Layer.md) at source position 170.
- Split parent: [UID:00018W][0x004f0480-0x004f1bf6.Layer](by-memory/0x004f0480-0x004f1bf6.Layer.md).
- Source family: [UID:0000KL][Layer](by-file/Layer.md), with the Layer class as semantic owner for source-authored members.

## Behavior

The method iterates the selected or top-level HierList, skips null/sentinel or non-visible rows as accepted, intersects node damage with the current render Region, translates coordinates, dispatches pane painting through the accepted GrafPort context, recurses into child lists unless blocksChildTraversal is set, accumulates rendered damage, and clears processed dirty state. For the active MapPane route it checks the effect list, finds the active pixel effecter, and calls the returned ScreenEffecter apply slot instead of the normal sprite/blit path.

ScreenPane and MapPane are consumers, not owners. Temporary Region cleanup, EH state, and graphics scaffolding remain compiler/source-library mechanics exactly as reflected by the formal body.

## Binary, ABI, And Boundary Evidence

- Fresh accepted IDA-backed reanalysis fixes the start at `0x004f0ff0`, exclusive end at `0x004f138b`, and exact size at 923 bytes.
- [0x004f138b,0x004f1390) is five bytes of all-0xcc alignment.
- The function lies wholly inside parent `[0x004f0480,0x004f1bf6)`, whose 6,006 bytes hash to `1444CFBA7D8179ED1BD2A084485C028364BBAC2DAD841AC0D48D1643352C232B`.
- No alternate entry, discontiguous chunk, embedded table, or non-Layer RTTI route intersects this leaf.
- Layer is an eight-byte LObject-derived object whose only data member is `HierList *m_nodes` at `+0x04`.
- Each LayerNode payload is exactly 48 bytes: pane `+0x00`, paneFlags `+0x04`, padding `+0x05..+0x07`, bounds `+0x08`, previousBounds `+0x18`, visible `+0x28`, blocksChildTraversal `+0x29`, dirty `+0x2a`, padding `+0x2b`, and owned Region pointer `+0x2c`. A complete HierList row has the eight-byte parent/childList header, this payload, and the established 59-byte stride.

## Reachability And Dependencies

ScreenPane::TraversePresentationList calls this body at 0x00559547. The body self-recurses at 0x004f12fe. MapPane predicate dependencies are UID0002I1 at 0x00503e80 and UID0002I2 at 0x00503e90, followed by the returned virtual slot +0x1c.

The Layer vtable starts at `0x0061ca48`; its scalar destructor slot, inherited slots, and adjacent RTTI boundary support Layer ownership without moving ordinary source bodies into a vtable/data page. Pane, ScreenPane, MapPane, HierList, Region/Motion, Application, WorldMapPane, and FlyingParcelPane remain callers, clients, or dependencies rather than implementation owners.

## Source Shape And Rejected Alternatives

The populated managed block above is the accepted behavior-identical human source shape. It preserves observed ordering, null/precondition behavior, payload asymmetries, recursion, and ownership while omitting only compiler mechanics.

Historical aggregate wording that treated this leaf as part of a 22-function monolith, left generated names/LayerNode fields unresolved, or proposed render-side ownership is superseded. Raw no-xref status, where applicable, is retained as negative reachability evidence rather than used to erase complete source. PDB spelling is unavailable, so the chosen name is the highest-probability source-facing spelling supported by behavior, callers, class order, and peer conventions.

## Cross-References

- [UID:000073][Layer](by-class/Layer.md)
- [UID:0000KL][Layer](by-file/Layer.md)
- [UID:000065][HierList](by-class/HierList.md)
- [UID:0000A2][Pane](by-class/Pane.md)
- [UID:0001FW][0x00554680-0x00554b38.RegionAndMotionRect](by-memory/0x00554680-0x00554b38.RegionAndMotionRect.md)
- [UID:0003OK][0x0061ca44-0x0061ca54.LayerVtableData](by-memory/0x0061ca44-0x0061ca54.LayerVtableData.md)

## Changes

### 2026-07-14 - Split from UID00018W

Created as an exact Layer leaf from mandatory live-IDA evidence. This page records the complete accepted behavior, range, ABI, ownership, source placement, negative evidence, and historical disposition without changing unrelated support metadata.

