*** UID:0004CS | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DZ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000O5 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
int StartupWindow::RunUpdateCheck(bool *runUpdater)
{
    RECT windowRect = { 0, 0, 500, 430 };
    WNDCLASSEXA windowClass = {};

    windowClass.cbSize = sizeof(windowClass);
    windowClass.style = CS_CLASSDC;
    windowClass.lpfnWndProc = StartupWindowUpdateCheckWindowProc;
    windowClass.hInstance = GetModuleHandleW(NULL);
    windowClass.hCursor = LoadCursorA(NULL, IDC_ARROW);
    windowClass.lpszClassName = g_szBaramNoticeWndClass;

    const DWORD windowStyle = WS_POPUP | WS_CLIPCHILDREN;
    AdjustWindowRect(&windowRect, windowStyle, FALSE);

    int x = CW_USEDEFAULT;
    int y = CW_USEDEFAULT;
    RECT desktopRect;
    HWND desktopWindow = GetDesktopWindow();
    if (desktopWindow != NULL && GetWindowRect(desktopWindow, &desktopRect)) {
        OffsetRect(&windowRect,
                   desktopRect.left + (desktopRect.right - desktopRect.left) / 2 - (windowRect.right - windowRect.left) / 2 - windowRect.left,
                   desktopRect.top + (desktopRect.bottom - desktopRect.top) / 2 - (windowRect.bottom - windowRect.top) / 2 - windowRect.top);
        x = windowRect.left;
        y = windowRect.top;
    }

    if (g_startupWindowClassAtom == 0) {
        g_startupWindowClassAtom = RegisterClassExA(&windowClass);
        if (g_startupWindowClassAtom == 0) {
            return kAbortStartup;
        }
    }

    m_noticeWindow = CreateWindowExA(WS_EX_APPWINDOW,
                                    g_szBaramNoticeWndClass,
                                    g_szNoticeTitle,
                                    windowStyle,
                                    x,
                                    y,
                                    windowRect.right - windowRect.left,
                                    windowRect.bottom - windowRect.top,
                                    GetDesktopWindow(),
                                    NULL,
                                    windowClass.hInstance,
                                    NULL);
    if (m_noticeWindow == NULL) {
        return kAbortStartup;
    }

    ShowWindow(m_noticeWindow, SW_SHOW);
    UpdateWindow(m_noticeWindow);

    BrowserWindow *browser = new BrowserWindow(m_instance, m_noticeWindow, CRect(17, 96, 484, 336), "http://www.nexustk.com/news/news.asp");
    browser->SetVisible(true);

    DWORD localVersion = 0;
    DWORD valueType = REG_DWORD;
    DWORD valueSize = sizeof(localVersion);
    HKEY key = NULL;
    if (RegOpenKeyExW(HKEY_CURRENT_USER, L"Software\\KRU\\NexusTK", 0, KEY_READ, &key) == ERROR_SUCCESS) {
        RegQueryValueExW(key, L"updvr", NULL, &valueType, reinterpret_cast<BYTE *>(&localVersion), &valueSize);
        RegCloseKey(key);
    }

    std::string responseText;
    curl_global_init(CURL_GLOBAL_ALL);
    for (int attempt = 0; attempt < 2 && responseText.empty(); ++attempt) {
        CURL *curl = curl_easy_init();
        curl_easy_setopt(curl, CURLOPT_URL, "https://s3.amazonaws.com/kru-downloads/tk/update/nexustk.ver");
        curl_easy_setopt(curl, CURLOPT_FOLLOWLOCATION, 1L);
        curl_easy_setopt(curl, CURLOPT_TIMEOUT, 5L);
        curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, CurlWriteCallback);
        curl_easy_setopt(curl, CURLOPT_WRITEDATA, &responseText);
        if (curl_easy_perform(curl) != CURLE_OK) {
            responseText.clear();
        }
        curl_easy_cleanup(curl);
    }

    bool versionMatches = false;
    wchar_t minimapVersion[34] = {};
    if (!responseText.empty()) {
        int versionKey = AnsiSmallStringFindFrom(responseText, "version", 0);
        int versionColon = AnsiSmallStringFindFrom(responseText, ":", versionKey + 1);
        int versionStart = AnsiSmallStringFindFrom(responseText, "\n", versionColon + 1);
        int versionEnd = AnsiSmallStringFindFrom(responseText, "\n", versionStart + 1);
        int remoteVersion = atoi(responseText.substr(versionStart + 1, versionEnd - versionStart - 1).c_str());
        versionMatches = (localVersion == static_cast<DWORD>(remoteVersion));

        int minimapKey = AnsiSmallStringFindFrom(responseText, "minimap", 0);
        if (minimapKey >= 0) {
            int minimapColon = AnsiSmallStringFindFrom(responseText, ":", minimapKey + 1);
            int minimapStart = AnsiSmallStringFindFrom(responseText, "\n", minimapColon + 1);
            int minimapEnd = AnsiSmallStringFindFrom(responseText, "\n", minimapStart + 1);
            std::wstring wideValue = StartupAnsiRangeToWideString(responseText.data() + minimapStart + 1, responseText.data() + minimapEnd);
            wcscpy_s(minimapVersion, 34, wideValue.c_str());
        }
    }

    if (_wcsicmp(g_pMiniMapVersionManager->GetVersionString(), minimapVersion) != 0) {
        std::string hashListText;
        for (int attempt = 0; attempt < 2 && hashListText.empty(); ++attempt) {
            CURL *curl = curl_easy_init();
            curl_easy_setopt(curl, CURLOPT_URL, "https://s3.amazonaws.com/kru-downloads/tk/minimaps/HashList.txt");
            curl_easy_setopt(curl, CURLOPT_FOLLOWLOCATION, 1L);
            curl_easy_setopt(curl, CURLOPT_TIMEOUT, 20L);
            curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, CurlWriteCallback);
            curl_easy_setopt(curl, CURLOPT_WRITEDATA, &hashListText);
            if (curl_easy_perform(curl) != CURLE_OK) {
                hashListText.clear();
            }
            curl_easy_cleanup(curl);
        }
        if (!hashListText.empty()) {
            std::wstring wideHashList = StartupAnsiRangeToWideString(hashListText.data(), hashListText.data() + hashListText.length());
            g_pMiniMapVersionManager->UpdateHashList(minimapVersion, wideHashList.c_str(), true);
        }
    }

    m_resultReady = true;
    int result = kAbortStartup;
    if (versionMatches) {
        *runUpdater = false;
        MSG message;
        while (GetMessageA(&message, NULL, 0, 0) > 0) {
            TranslateMessage(&message);
            if (message.message == WM_CLOSE) {
                break;
            }
            if (message.message == WM_USER + 11) {
                result = static_cast<int>(message.wParam);
                break;
            }
            DispatchMessageA(&message);
        }
    } else {
        *runUpdater = true;
        result = kAbortStartup;
    }

    delete browser;
    ShowWindow(m_noticeWindow, SW_HIDE);
    DestroyWindow(m_noticeWindow);
    m_noticeWindow = NULL;
    return result;
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact StartupWindow::RunUpdateCheck body at 0x00580870-0x005810fd; creates the update notice window/browser, fetches update metadata and minimap hash data through libcurl, runs the modal message loop, and returns the updater decision. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x00580870-0x005810fd StartupWindow RunUpdateCheck

## Status

- Address range: `0x00580870-0x005810fd`, half-open.
- Entity kind: `StartupWindow` method.
- Canonical owner: [UID:0000DZ][StartupWindow](by-class/StartupWindow.md).
- Source route: [UID:0000O5][StartupWindow](by-file/StartupWindow.md).
- Rebuild handling: `source-authored`.

## Evidence

IDA records this as `StartupWindow__RunUpdateCheck`, size `0x88d`, with `_WinMain@16` as the only ordinary caller at `0x004f5d52`. The body registers/creates the `BaramNoticeWnd_Class` notice HWND, creates a `BrowserWindow` for `http://www.nexustk.com/news/news.asp`, reads `HKCU\Software\KRU\NexusTK\updvr`, fetches `https://s3.amazonaws.com/kru-downloads/tk/update/nexustk.ver`, parses `version` and `minimap`, optionally fetches `https://s3.amazonaws.com/kru-downloads/tk/minimaps/HashList.txt`, and updates [UID:00008H][MiniMapVersionManager](by-class/MiniMapVersionManager.md).

[UID:000288][0x00670260-0x00670268.StartupWindowNoticePointerData](by-memory/0x00670260-0x00670268.StartupWindowNoticePointerData.md) owns this method's two notice-string pointer objects. The class pointer slot at `0x00670260` is loaded at `0x005808ac`, assigned to `WNDCLASSEXA::lpszClassName`, and reused for registration plus `CreateWindowExA`; the title pointer slot at `0x00670264` is loaded at `0x00580ab1` for `CreateWindowExA`. Each `.data` slot has exactly one additional read in raw UID0004D2, and each `.rdata` literal target has only its corresponding slot xref. The accepted declarations are file-local mutable pointers to const ANSI literals, emitted by UID000288 at position `0` before this body. This method's complete formal C++ and `88/90` score remain unchanged; it consumes but does not own or duplicate those declarations.

`xrefs_to 0x00581b80` shows the exact [UID:0001IP][0x00581b80-0x00581ce6.CurlWriteCallback](by-memory/0x00581b80-0x00581ce6.CurlWriteCallback.md) setup sites at `0x00580c6f` and `0x00580efb`. The accepted source block preserves that callback and uses the accepted startup-local parse helpers created by this split.

B007's 2026-07-04 UID0004D3 support recheck on MCP session `ddf5b602` confirms that this method's two calls to [UID:0004D3][0x00581cf0-0x00581d30.StartupAnsiRangeToWideString](by-memory/0x00581cf0-0x00581d30.StartupAnsiRangeToWideString.md) are the complete caller set for that helper. The `0x00580e3f` call converts the bounded minimap field substring before this method copies the wide temporary to the minimap version buffer with `wcscpy_s`; the `0x00580fb6` call converts the fetched `HashList.txt` byte range before passing the wide text into the MiniMapVersionManager update path. UID0004D3 now emits direct range-widening source; there is no target-level `MultiByteToWideChar`/`mbstowcs_s` call or third source parameter to expose here.

B010's 2026-07-04 UID0004D6 support recheck on MCP session `ddf5b602` confirms that this method's eight calls to [UID:0004D6][0x00581e40-0x00581f46.AnsiSmallStringFindFrom](by-memory/0x00581e40-0x00581f46.AnsiSmallStringFindFrom.md) are the complete code-ref set for that helper, with no data refs. The call sites at `0x00580cb3`, `0x00580cc2`, `0x00580cd1`, `0x00580ce4`, `0x00580db3`, `0x00580dca`, `0x00580dd9`, and `0x00580dec` implement the downloaded `nexustk.ver` `version`/colon/newline and `minimap` parse probes. UID0004D6 now emits the StartupWindow-local `std::string`/SSO find-from-offset helper with `_memchr`, empty-needle offset return, and `-1` no-match sentinel; there is no target-level StringUtil/runtime call or `StartupWindow` member method to expose here.

B011's 2026-07-04 UID0004D7 support sync on MCP session `ddf5b602` reclassifies [UID:0004D7][0x00581f50-0x0058206e.AnsiSmallStringSubstring](by-memory/0x00581f50-0x0058206e.AnsiSmallStringSubstring.md) as non-emitting MSVC/Dinkumware `std::string::substr` support, not a StartupWindow project helper. The two target refs at `0x00580cf8` and `0x00580e00` remain in this parser window, but the body has no StartupWindow state and implements the SSO-15 substring runtime path with offset bounds, count clamping, allocation/copy/terminator/free behavior, and runtime callees. The formal source here now expresses the remote-version parse through direct `responseText.substr(...)` syntax instead of the stale `AnsiSmallStringSubstring(...)` wrapper.

2026-07-04 B006 UID0004D2 support sync: current MCP session `13191102` rechecked the alternate notice setup helper at `0x005819d0-0x00581b75` and proved the shared notice-window setup constants are `CS_CLASSDC` and `WS_POPUP | WS_CLIPCHILDREN`, not the stale `CS_OWNDC` and `WS_POPUP | WS_BORDER | WS_SYSMENU` values. This page was updated only for those proven stale constants; RunUpdateCheck caller flow, libcurl/update behavior, browser setup, modal loop, and cleanup are otherwise unchanged.

## Cross-References

- [UID:0000O5][StartupWindow](by-file/StartupWindow.md)
- [UID:0000DZ][StartupWindow](by-class/StartupWindow.md)
- [UID:0001IO][0x005807d0-0x0058206e.StartupWindowUpdateCheck](by-memory/0x005807d0-0x0058206e.StartupWindowUpdateCheck.md)
- [UID:0001IP][0x00581b80-0x00581ce6.CurlWriteCallback](by-memory/0x00581b80-0x00581ce6.CurlWriteCallback.md)
- [UID:0004D7][0x00581f50-0x0058206e.AnsiSmallStringSubstring](by-memory/0x00581f50-0x0058206e.AnsiSmallStringSubstring.md)
- [UID:0000HV][Browser](by-file/Browser.md)
- [UID:00008H][MiniMapVersionManager](by-class/MiniMapVersionManager.md)
- [UID:000288][0x00670260-0x00670268.StartupWindowNoticePointerData](by-memory/0x00670260-0x00670268.StartupWindowNoticePointerData.md)

## Changes

- 2026-07-20 B004 UID000288 support synchronization: preserved `88/90` and the complete formal body byte-for-byte; added exact class/title slot loads, pointer/literal ownership and xref cardinality, file-static `const char *` provenance, position-0 declaration order, and no-duplicate ownership.
- 2026-07-04 B011 UID0004D7 support sync: replaced the stale `AnsiSmallStringSubstring(...)` formal caller expression with direct `responseText.substr(...)` source syntax and recorded that `0x00581f50-0x0058206e` is non-emitting MSVC/Dinkumware `std::string::substr` support generated by this parser expression, not a separate StartupWindow API.
- 2026-07-04 B010 UID0004D6 support sync: recorded direct helper coverage for all eight `AnsiSmallStringFindFrom` calls used by the `version`/`minimap` parser, including MCP session `ddf5b602`, exact call sites, zero data refs, `_memchr` helper behavior, empty-needle/offset semantics, `-1` sentinel, and rejected StringUtil/runtime/member alternatives.
- 2026-07-01 B008 accepted StartupWindow split implementation: created this exact method page and inserted the accepted formal first-draft C++ body.
- 2026-07-04 B006 UID0004D2 support sync: corrected the notice-window class/style constants to `CS_CLASSDC` and `WS_POPUP | WS_CLIPCHILDREN` from current MCP evidence while preserving all unrelated `RunUpdateCheck` behavior.
