*** UID:000346 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:89 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:90 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000JG | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
struct FolderTreeHitTestContext
{
    FolderTreePane *pane;
    int x;
    int y;
    TreeItor<FolderTreePane::TreeElem> result;
};

template <>
template <>
void Tree<FolderTreePane::TreeElem>::Traverse<FolderTreeHitTestContext>(
    TreeItor<FolderTreePane::TreeElem> *start,
    FolderTreeHitTestContext *context)
{
    const int kStopTraversal = 1;
    const int kSkipChildren = 2;

    std::deque<int> pending;
    pending.push_back(start->GetIndex());

    while (!pending.empty())
    {
        const int nodeIndex = pending.back();
        pending.pop_back();

        TreeNode<FolderTreePane::TreeElem>& treeNode = m_nodes[nodeIndex];
        FolderTreePane::TreeElem& node = treeNode.data;

        RectBounds bounds;
        bounds.left = context->pane->GetTreeIndentLeft(node.depth);
        bounds.top = context->pane->GetTreeRowTop(node.rowIndex);
        bounds.right = bounds.left + 16 + context->pane->GetTextWidth(node.name.c_str());
        bounds.bottom = bounds.top + 16;

        int traversalState = kSkipChildren;
        if (bounds.ContainsPoint(context->y, context->x))
        {
            context->result.Set(this, nodeIndex);
            traversalState = kStopTraversal;
        }
        else if (node.expanded)
        {
            traversalState = 0;
        }

        if (traversalState == kStopTraversal)
            break;

        if (traversalState == kSkipChildren || treeNode.lastChildIndex == -1)
            continue;

        for (int childIndex = treeNode.lastChildIndex; childIndex != -1;
             childIndex = m_nodes[childIndex].previousSiblingIndex)
        {
            pending.push_back(childIndex);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Exact FolderTree hit-test `Tree<T>::Traverse<Context>` specialization: deque-backed visible traversal, generic node linkage, payload geometry/name/expanded state through `.data`, hit rectangle test, result iterator update, and stop/skip child states. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x004b3850-0x004b3ae2 FolderTreeHitTestTraversal

## Current Accepted UID0004YK Consumer Policy

This body remains FolderTreePane-owned hit-test policy over the generic Tree traversal shape. Its storage references now use Tree<T>::m_nodes; geometry, result iterator, stop/skip states, expansion rule, and the exact 0x004b3850-0x004b3ae2 range remain consumer-specific. The shared deque depth-first skeleton is authored once in Tree.h.

## Status

- Disposition: reconstructable NexusTK source helper.
- Entity kind: `FolderTreePane` visible-tree traversal helper for hit-test/search.
- Likely source module: [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md).
- Direct owner decision: file-local helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). Ordinary callers are `0x004b2680` and `FolderTreePane::OnMouseEvent` at `0x004b2dc0`.
- Rebuild handling: `source-authored`; this is folder-tree UI input/search logic.

## Covered Range

| Range | Current/proposed name | Notes |
| --- | --- | --- |
| `0x004b3850-0x004b3ae2` | `FolderTreeHitTestTraversal` | Traverses visible nodes, computes each node's row rectangle, tests a point/rectangle against the node bounds, records the hit node, and descends into expanded children. |

## IDA Evidence

- 2026-06-10 IDA MCP `lookup_funcs 0x004b3850` reports `sub_4B3850`, size `0x292`, giving exact exclusive end `0x004b3ae2`.
- 2026-06-10 IDA MCP `callers 0x004b3850` reports calls at `0x004b26f3` in `sub_4B2680` and `0x004b2efd` in `sub_4B2DC0`, matching the folder-tree traversal and mouse-event paths.
- 2026-06-10 IDA MCP `callees 0x004b3850` reports allocation/traversal helpers plus `0x004b76d0`, `0x004baa70`, `0x00584540`, and security-cookie support.
- 2026-06-10 decompilation shows the helper building a traversal queue, loading the current `FolderTreePane::TreeElem`, computing bounds from row/depth words at `+0x18/+0x1a`, measuring the node name via `0x00584540` and `0x004baa70`, checking the computed rectangle through `0x004b76d0`, storing the matched iterator/node into the caller-provided result state, and traversing expanded children when byte `+0x1d` is set.
- The row/depth/name/expanded accesses agree with [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md) and the `FolderTreePane::OnMouseEvent` ownership context in [UID:00005A][FolderTreePane](by-class/FolderTreePane.md).

## Ownership And Parent Gate

The direct semantic owner is [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). The helper is exclusively used by folder-tree traversal/input paths and has no evidence of broader shared utility ownership. The file parent is `89/85`, satisfying the strict gate.

## Reconstruction Notes

- UID000149 factorization: `pending.push_back(start->GetIndex())` calls shared out-of-line `std::deque<int>::push_back` at `0x004b38d9`; later child-index appends inline the same push logic and call `_Growmap(1)` at `0x004b3a3c` only on demand. Preserve the native source expression and no custom Deque dependency.

- Final source should express this as a private helper for selecting/toggling visible tree rows from mouse coordinates or traversal context.
- Emit the accepted first-draft traversal body above. Exact result-state struct names and helper names remain inferred and should be renamed only when stronger source evidence appears.

## Score Rationale

| Field | Value | Rationale |
| --- | ---: | --- |
| Completion | 86 | Exact range, two folder-tree callers, callee set, row/depth/name-bound calculation, rectangle hit-test, result-state update, child traversal, direct file owner, and parent gate are documented. |
| Confidence | 88 | IDA confirms the function boundary, caller set, and decompiled hit-test behavior; confidence remains below final audit because result-state names are inferred. |
| Reconstructable | true | Source-owned folder-tree hit-test/search traversal required by rebuilt `FolderTreePane`. |

## 2026-06-19 B008 Source-Quality Implementation Sync

Accepted B008 reanalysis keeps this as a file-local hit-test/search traversal helper under [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md). It supports mouse hit-testing and the core helper at `0x004b2680`, using `TreeElem::rowIndex` and `TreeElem::depth` through node bounds to identify the clicked visible row.

The final class/source model treats mouse handling as `FolderTreePane::OnMouseEvent`, with this helper as internal traversal machinery. It should not be emitted as a standalone generated source file or a public class method.

## 2026-06-30 B010 Accepted First-Draft Body
The accepted FolderTreePane callback promotes this page from an empty marker to first-draft formal C++ for `Tree<FolderTreePane::TreeElem>::HitTestVisibleNodes`. MCP evidence anchors the body at `sub_4B3850` size `0x293`, with visible-node traversal, hit rectangle construction from row/depth placement, result iterator writes, and skip/stop traversal states. The route remains this by-memory specialization because a shared `Tree<T>` template owner has not been validated.

## Cross-References

- [UID:0000JG][FolderTreePane](by-file/FolderTreePane.md)
- [UID:00005A][FolderTreePane](by-class/FolderTreePane.md)
- [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md)
- [UID:0001UJ][FolderTreePane__TreeElem](by-type/by-struct/FolderTreePane__TreeElem.md)

## 2026-07-29 UID0002MZ Node/Payload Hit-Test Reconciliation

- Formal CPP is the consumer-specific `Traverse<FolderTreeHitTestContext>` specialization. `Tree<T>` retains only the generic `Traverse<Visitor>` declaration.
- Generic child/sibling links come from `TreeNode<TreeElem>`; name, row, depth, and expanded state come from `treeNode.data`.
- Exact two-caller route, bounds construction, point hit, result iterator write, stop/skip behavior, deque order, range, and score remain unchanged at `89/90`, UID0000JG.
- The prior `HitTestVisibleNodes` generic API name and direct 36-byte payload access are historical assumptions superseded by the shared-template/consumer-specialization model.

## Changes

- 2026-07-12 B003 UID000149 factorization sync: preserved score/route/body and added exact initial-call/later-inline `std::deque<int>` evidence; `<deque>` is supplied once by UID0001WP.

- 2026-06-10 B001-028 split repair:
  - What existed before: this exact hit-test/search traversal helper was only a row inside [UID:000157][0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers](by-memory/0x004b3350-0x004b5c3f.FolderTreePaneTreeAndSortHelpers.md).
  - Changed to: created this exact child page for `0x004b3850-0x004b3ae2`.
  - Summary/evidence: IDA MCP confirms the `0x292`-byte function, two folder-tree callers, row/depth/name-bound calculations, rectangle hit-test call, and expanded-child traversal.
