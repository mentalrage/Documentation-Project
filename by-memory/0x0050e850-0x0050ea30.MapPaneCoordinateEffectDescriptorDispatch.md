*** UID:0003TE | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:94 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:00007Q | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void MapPane::RequestCoordinateEffect(
    int mapColumn,
    int mapRow,
    int effectDescriptorId,
    int timerAction,
    bool renderAfterLivingObjects)
{
    MapRect visibleBounds;
    GetClampedVisibleTileBounds(&visibleBounds);
    if (!PointInRect(mapRow, mapColumn, &visibleBounds))
        return;

    EffectInfo effectInfo;
    EffectObjImageLibGetEffectInfo(&effectInfo, effectDescriptorId);

    if (effectInfo.secondaryFrameCount > 0)
    {
        EffectObjectPane *matchingEffect = NULL;

        if (effectInfo.secondaryRangeStart >= 0 &&
            effectInfo.secondaryRangeEnd >= 0)
        {
            List *effects = m_objectList->GetSecondaryRowBucket(mapRow);
            for (int index = effects->GetCount() - 1; index >= 0; --index)
            {
                EffectObjectPane *effectObject =
                    *static_cast<EffectObjectPane **>(
                        effects->GetElementAt(index));
                MapPoint position;
                effectObject->GetMapPosition(&position);
                if (position.column == mapColumn &&
                    effectObject->m_effectId == effectInfo.effectKeyOrId &&
                    effectObject->IsLooping())
                {
                    matchingEffect = effectObject;
                    break;
                }
            }
        }

        if (matchingEffect != NULL)
        {
            ApplyEffectObjectPaneTimerAction(matchingEffect, timerAction);
        }
        else
        {
            CreateEffectObjectPane(
                NULL,
                mapRow,
                mapColumn,
                effectInfo,
                timerAction,
                0,
                renderAfterLivingObjects);
        }
    }

    if (effectInfo.primaryFrameCount > 0)
    {
        EffectObjectPane *matchingEffect = NULL;

        if (effectInfo.primaryRangeStart >= 0 &&
            effectInfo.primaryRangeEnd >= 0)
        {
            List *effects = m_objectList->GetPrimaryRowBucket(mapRow);
            for (int index = effects->GetCount() - 1; index >= 0; --index)
            {
                EffectObjectPane *effectObject =
                    *static_cast<EffectObjectPane **>(
                        effects->GetElementAt(index));
                MapPoint position;
                effectObject->GetMapPosition(&position);
                if (position.column == mapColumn &&
                    effectObject->m_effectId == effectInfo.effectKeyOrId &&
                    effectObject->IsLooping())
                {
                    matchingEffect = effectObject;
                    break;
                }
            }
        }

        if (matchingEffect != NULL)
        {
            ApplyEffectObjectPaneTimerAction(matchingEffect, timerAction);
        }
        else
        {
            CreateEffectObjectPane(
                NULL,
                mapRow,
                mapColumn,
                effectInfo,
                timerAction,
                1,
                renderAfterLivingObjects);
        }
    }
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Source-ready MapPane::RequestCoordinateEffect with clamped half-open coordinate validation, 52-byte EffectInfo copy, secondary-before-primary ObjectList row-bucket reverse scans, same-column/effect-id/looping reuse, null-target row-before-column creation, sequence 0/1, and renderAfterLivingObjects pass-through. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0050e850-0x0050ea30 MapPane Coordinate Effect Descriptor Dispatch

## UID0003TE Source-Quality Implementation - 2026-07-15

- Final source identity is private `MapPane::RequestCoordinateEffect(int mapColumn, int mapRow, int effectDescriptorId, int timerAction, bool renderAfterLivingObjects)` in `NexusTK/map/MapPane.cpp`. The UID00007Q owner/emitter, reconstructable state, blank explicit position, exact range, and `Nested:0` are unchanged.
- Fresh IDA MCP session `64c11373` reconfirmed `sub_50E850` as one 480-byte function at `[0x0050e850,0x0050ea30)`: 168 instructions, 26 basic blocks, complexity 17, final `retn 0x14`, SHA256 `A8B3951BA69C1AC4553004561FE2AF23ECFAAAB21C50824556DBBABFDD90C194`, eight-byte predecessor alignment, and an independent UID0003TF prologue at the exclusive end.
- The sole direct caller is `0x00511d51` in UID0003TO. Its packet fields are object id at `+1`, descriptor id at `+5`, signed timer action at `+7`, map column at `+9`, and map row at `+11`; object id zero selects this method. Descriptor 134 uses the separate flash branch, while only descriptors 160 through 162 set `renderAfterLivingObjects`.
- The method obtains clamped visible bounds through UID000420 and applies the half-open `PointInRect(mapRow, mapColumn, &visibleBounds)` y/x contract. Coordinates outside that rectangle return before the 52-byte EffectInfo copy or any list/timer/create side effect.
- The descriptor copy occurs once before both passes. Secondary fields and `GetSecondaryRowBucket(mapRow)` are processed first; primary fields and `GetPrimaryRowBucket(mapRow)` are processed second. Nonpositive frame counts suppress the corresponding pass. Negative reuse ranges skip only the scan and still permit creation.
- Reuse scans each `List` backward through pointer slots without invented list-element or pane null checks. Row is already selected by the ObjectList bucket, so candidates compare only returned `MapPoint::column`, `EffectObjectPane::m_effectId`, and `IsLooping()`. The first match is passed to source-ready UID0003TG, which preserves null return, ordinary delete for `-1`, zero no-op, event-2 cancel for `-2`, and the guarded query/replacement path for every other signed action.
- A miss calls UID0003TF with null `LivingObjectPane *`, machine-proven `mapRow` then `mapColumn`, the by-value EffectInfo, unchanged timer action, sequence `0` for secondary or `1` for primary, and unchanged render-after flag. Exact caller pushes, UID0003TF frame offsets, and EffectObjectPane-to-ObjectPane constructor forwarding independently prove the row-before-column helper contract.
- Positive evidence includes one coherent MapPane caller, MapPane ObjectList state, sibling UID000232 symmetry, exact helper xrefs, EffectInfo field use, GetMapPosition row/column layout, and source adjacency. Negative evidence rejects EffectObjectPane, EffectObjImageLib, TimerMgr, standalone/new-file, split/merge, raw-helper, duplicate-inline, compiler-only, `compactFlag`, type-number, and `Apply*` ownership/source alternatives.
- Completion/confidence are `92/94`: behavior, ABI, types, ownership, source placement, and complete human source are closed; confidence remains below symbol-proof certainty only because original private method/local lexical spellings were stripped.

## Historical UID0003TF Coordinate Caller Synchronization - 2026-07-15

- Historical state before the accepted UID0003TE callback: this helper remained `86/89` with blank formal C++ because its own argument/local/helper naming pass had not yet been completed. That blank-body disposition is superseded by the source-quality implementation above; owner/emitter UID00007Q, reconstructable true, blank explicit position, and `Nested:0` remain unchanged.
- Its two calls to [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md) occur at `0x0050e945` and `0x0050e9e3`. Both pass a null `LivingObjectPane *target`; the secondary descriptor route uses sequence `0`, the primary route uses sequence `1`, and the caller flag is carried as `renderAfterLivingObjects`.
- The target helper is now source-ready at `92/94`: it takes `LivingObjectPane *`, map column/row, by-value `EffectInfo`, `timerAction`, sequence, and render-after flag; suppresses creation only for action `-1`; uses ordinary `new EffectObjectPane`; registers non-null targets only; inserts every created pane into MapPane `m_objectList`; starts event 1; conditionally schedules event 2; and finishes with bounds invalidation. Null coordinate callers intentionally skip owner-list registration and `UpdatePosition` while retaining the common creation/list/timer/bounds tail.
- Existing-child reuse remains local to this coordinate dispatcher: secondary-before-primary descriptor passes, nonnegative range gates, reverse scans over coordinate-selected effect children, `m_effectId` equality, `IsLooping()`, and UID0003TG timer-action reuse. UID0003TG owns only the existing-pane action policy: inferred `m_timerExtensionLocked` gates event-2 due-tick query, signed unscaled comparison, cancel-before-schedule, and exact `1000 * timerAction`, `0,0` replacement. Neither helper merges or duplicates the other's caller logic.
- Historical sibling alignment used `compactFlag`, generic type-2/type-4 terminology, or `ObjectPane *` for UID0003TF. Those names are superseded by constructor/render/list evidence; they remain history only and are not current declaration guidance.

## Status

- Disposition: reconstructable NexusTK project code.
- Entity kind: exact MapPane private packet/effect helper child split from [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md).
- Direct owner/emitter: [UID:00007Q][MapPane](by-class/MapPane.md); file owner [UID:0000L3][MapPane](by-file/MapPane.md).
- Source-facing name: `MapPane::RequestCoordinateEffect`, the coordinate/no-target counterpart to [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) / `MapPane::RequestObjectEffect`.
- Confidence: very strong for boundary, caller, MapPane ownership, descriptor copy, exact pass/list/match/create behavior, row-before-column helper contract, and complete source shape; only stripped lexical spelling keeps confidence below 95.
- Reconstruction note: complete managed C++ is current. No target argument, local, helper, ownership, split, source-placement, or compile-surface blocker remains.

## Address Range

- Start: `0x0050e850`
- End: `0x0050ea30` end-exclusive
- IDA function: `sub_50E850`
- IDA size: `0x1e0` bytes
- Preceded by ignored padding `0x0050e848-0x0050e850` after [UID:0003TD][0x0050e810-0x0050e848.MapPaneRemoveGroupMemberHitBar](by-memory/0x0050e810-0x0050e848.MapPaneRemoveGroupMemberHitBar.md).
- Followed immediately by [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md).

## Behavior

This helper handles the coordinate/no-target effect descriptor path for MapPane packet processing. Its direct caller reads packet fields and uses this path when no target object id is present. The helper:

- obtains current MapPane view/coordinate state through the `0x00505290` helper;
- validates packet coordinates through `0x004b7e80`;
- copies a 52-byte [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md) descriptor through [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md);
- runs two descriptor-controlled passes over existing effect-object child collections obtained through `0x00532550` / `0x005325b0`;
- compares existing `EffectObjectPane` child ids at `+0x148` against the copied descriptor id and applies [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md) as the loop/reuse-active gate;
- reuses a matching child through [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md);
- creates/applies a missing child through [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md).

The clamped rectangle test is half-open. Secondary processing precedes primary processing. Each pass is frame-count gated; nonnegative descriptor ranges enable backward reuse scans, while negative ranges skip reuse without suppressing creation. The scan selects the row bucket, compares only candidate column/effect id/looping state, and intentionally preserves the binary's pointer-slot dereference assumptions. Misses use null target, row-before-column creation, sequence 0/1, and unchanged timer/render forwarding.

The existing-pane action dependency is source-ready [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md). `timerAction` remains signed: `-1` is source delete, `-2` cancels event 2, zero is a no-op, and every other signed value reaches the `m_timerExtensionLocked`/due-tick path. Historical generic-callback, positive-only, raw `+0x14e` guard, free/static, and blank-body descriptions are superseded; this target's complete body, promoted coordinate parameters, row-before-column create calls, score, metadata, and source route remain unchanged.

## Sibling Naming Resolution

The object-bound sibling [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) is `MapPane::RequestObjectEffect`. The accepted coordinate/no-target spelling is `MapPane::RequestCoordinateEffect`: `Coordinate` distinguishes map row/column inputs from screen, viewport, and ObjectPane positions, and `Request` covers create, reuse, timer action, and no-op outcomes.

B008's `MapPane::ApplyObjectEffect` and the prior `RequestPositionEffect` alternative remain explicit rejected historical names. `Apply` is too narrow for reuse/cancel/no-op behavior, while `Position` is ambiguous in this subsystem.

## IDA MCP Evidence

- B001 live IDA MCP session `b001_mappane_0001AW_20260616` reports `sub_50E850` at `0x0050e850`, size `0x1e0`, 26 basic blocks, cyclomatic complexity 17.
- `analyze_function` reports direct callees: `sub_505290`, `sub_50EB90`, `sub_4B7E80`, `sub_5374D0`, `sub_50EA30`, `sub_532550`, `sub_5325B0`, `sub_4DE3E0`, [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md), and `@__security_check_cookie@4`.
- 2026-06-18 B003 source-quality reanalysis corrected the stale `sub_538BB0` owner/name: this callee returns `EffectObjectPane +0x14c`, and this coordinate helper reaches it only after matching an existing effect child `+0x148` against the copied descriptor id.
- `xrefs_to` / caller review reports direct call site `0x00511d51` inside `sub_511C90`.
- Decompilation of the caller shows packet-field reads through `sub_5754C0` and `sub_575480`; when object id is absent the caller invokes this helper, and when object id exists and lookup succeeds it invokes sibling [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md).
- [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md) records caller `0x0050e89d` inside this function, confirming the descriptor-provider route.

## Ownership Decision

This is MapPane source. The function is reached from MapPane packet handling, uses MapPane coordinate/object-list context, and delegates descriptor storage, effect-object construction, and timer mechanics to separate support owners. Do not attach it to `EffectObjectPane`, `EffectObjImageLib`, or TimerMgr.

## Rejected Owners

- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md): constructed/reused object class, not owner of this MapPane descriptor-dispatch body.
- [UID:00004A][EffectObjImageLib](by-class/EffectObjImageLib.md): descriptor provider only.
- TimerMgr support: generic callee utilities only.

## Score Rationale

Completion is `92` because the exact range, one caller, packet argument route, clamped gate, descriptor copy, two-pass ObjectList scans, match/reuse/create behavior, ABI, owner/emitter, source route, complete formal body, support integration, and rejected history are reconstructed. Confidence is `94` because independent bytes, CFG, caller pushes, callee frames, fields, constructor forwarding, and sibling source agree; only stripped original lexical spellings cap it below symbol-proof certainty.

## Cross-References

- [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md)
- [UID:00007Q][MapPane](by-class/MapPane.md)
- [UID:0000L3][MapPane](by-file/MapPane.md)
- [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md)
- [UID:0003TF][0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane](by-memory/0x0050ea30-0x0050eb8a.MapPaneCreateEffectObjectPane.md)
- [UID:0003TG][0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction](by-memory/0x0050eb90-0x0050ec2e.MapPaneApplyEffectObjectPaneTimerAction.md)
- [UID:0002HA][0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo](by-memory/0x004de3e0-0x004de420.EffectObjImageLibGetEffectInfo.md)
- [UID:0001U8][EffectInfo](by-type/by-struct/EffectInfo.md)
- [UID:000049][EffectObjectPane](by-class/EffectObjectPane.md)
- [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md)
- [UID:0000VN][-ignored](by-memory/-ignored.md)

## Changes

- 2026-07-15 B004 UID0003TG accepted source-quality callback:
  - Preserved `92/94`, UID00007Q owner/emitter, complete B005 formal body, promoted `int` coordinate signature, row-before-column UID0003TF calls, descriptor/List behavior, and every unrelated current fact.
  - Synchronized only the UID0003TG dependency to ordinary delete, full signed action domain, inferred `m_timerExtensionLocked`, typed TimerHandler due-tick/cancel/replace flow, dimensional quirk, and superseded callback/positive/raw-guard/blank-body history. No formal, score, metadata, source route, or B005 correction changed.

- 2026-07-15 B005 UID0003TE accepted source-quality callback:
  - Raised only this target from `86/89` to `92/94`; preserved UID00007Q owner/emitter, reconstructable true, blank position, exact range/path/title, and `Nested:0`.
  - Installed the complete `MapPane::RequestCoordinateEffect` formal body with exact clamped half-open gate, one 52-byte descriptor copy, secondary-before-primary passes, reverse pointer-slot scans, same-column/effect-id/looping reuse, null-target row-before-column creation, sequence 0/1, and timer/render forwarding.
  - Added fresh bytes/hash/instruction/CFG/ABI/caller/callee/source-placement evidence, historicalized the prior blank-body and generic-name state, and retained all rejected owners/source alternatives and unsafe binary assumptions.

- 2026-07-15 B001 UID0003TF accepted source-quality callback:
  - Preserved `86/89`, UID00007Q owner/emitter, blank formal, exact range, and all caller-local behavior.
  - Added exact null-target sequence-0/1 call relationship, render-after pass-through, target completion, ordinary-new behavior, and null-route effects on registration/update versus common list/timer/bounds handling.
  - Historicalized compact/ObjectPane/type-number sibling guidance without claiming this coordinate helper's own body is complete.

- 2026-06-20 B007 object-effect descriptor implementation support:
  - Added sibling naming alignment after [UID:000232][0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch](by-memory/0x0050e320-0x0050e4b6.MapPaneObjectEffectDescriptorDispatch.md) was implemented as `MapPane::RequestObjectEffect`.
  - Kept this coordinate dispatcher C++ blank and did not rework it under UID 000232.
- 2026-06-21 B008 source-quality wording sync:
  - Recorded the rejected `ApplyObjectEffect` sibling-name alternative and kept this coordinate helper aligned with the accepted `Request*Effect` naming family.
- 2026-06-16 B001 continuation batch3 recommendation: create this exact child from the remaining [UID:0001AW][0x0050e4c0-0x00514920.MapPanePacketHandlers](by-memory/0x0050e4c0-0x00514920.MapPanePacketHandlers.md) broad leaf. Parent remains reconstructable because later source-bearing leaves remain unsplit.
- 2026-06-18 B003 support update: replaced the generic active/gate helper wording with [UID:00023H][0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping](by-memory/0x00538bb0-0x00538bb7.EffectObjectPaneIsLooping.md), matching the corrected `EffectObjectPane +0x148/+0x14c` field roles.
