*** UID:0004TK | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:92 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:93 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000FT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000FT | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL:50 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void UserStatusPane2::OnPaint()
{
    RectBounds fieldBounds;
    RectBounds fillBounds;
    wchar_t numberText[16];
    wchar_t groupText[4];

#define DRAW_STATUS_BAR(fieldId, currentValue, fullValue, middleFrame, leftFrame, rightFrame) \
    do { \
        GetStatusFieldRect(fieldId, &fieldBounds); \
        if ((fullValue) != 0) { \
            fillBounds = fieldBounds; \
            if ((currentValue) < (fullValue)) \
                fillBounds.left = fillBounds.right - \
                    MulDiv(fillBounds.right - fillBounds.left, currentValue, fullValue); \
            EPFTileContext middle; \
            EPFTileContext leftCap; \
            EPFTileContext rightCap; \
            middle.Initialize(L"BAR.EPF", middleFrame, L"BAR.PAL"); \
            leftCap.Initialize(L"BAR.EPF", leftFrame, L"BAR.PAL"); \
            rightCap.Initialize(L"BAR.EPF", rightFrame, L"BAR.PAL"); \
            RenderTileFrame(&middle, &fillBounds); \
            RectBounds capBounds = fillBounds; \
            capBounds.right = capBounds.left + 1; \
            RenderTileFrame(&leftCap, &capBounds); \
            capBounds.left = fillBounds.right - 1; \
            capBounds.right = fillBounds.right; \
            RenderTileFrame(&rightCap, &capBounds); \
        } \
    } while (0)

    DRAW_STATUS_BAR(4, m_statusRow0PartialValue, m_statusRow0FullValue, 0, 1, 2);
    DRAW_STATUS_BAR(5, m_statusRow1PartialValue, m_statusRow1FullValue, 3, 4, 5);

    GetStatusFieldRect(2, &fieldBounds);
    if (m_statusRow2Percent != 0)
    {
        fillBounds = fieldBounds;
        fillBounds.left = fillBounds.right -
            MulDiv(fillBounds.right - fillBounds.left, m_statusRow2Percent, 100);
        EPFTileContext middle;
        EPFTileContext leftCap;
        EPFTileContext rightCap;
        middle.Initialize(L"BAR.EPF", 6, L"BAR.PAL");
        leftCap.Initialize(L"BAR.EPF", 7, L"BAR.PAL");
        rightCap.Initialize(L"BAR.EPF", 8, L"BAR.PAL");
        RenderTileFrame(&middle, &fillBounds);
        RectBounds capBounds = fillBounds;
        capBounds.right = capBounds.left + 1;
        RenderTileFrame(&leftCap, &capBounds);
        capBounds.left = fillBounds.right - 1;
        capBounds.right = fillBounds.right;
        RenderTileFrame(&rightCap, &capBounds);
    }

#define DRAW_GROUPED_VALUE(value, fieldId, glyphRow) \
    do { \
        swprintf_s(numberText, 16, L"%10u", static_cast<unsigned int>(value)); \
        GetStatusFieldRect(fieldId, &fieldBounds); \
        if (m_exposedRegion.IntersectsRect(&fieldBounds)) { \
            int firstDigit = 0; \
            while (firstDigit < 9 && numberText[firstDigit] == L' ') ++firstDigit; \
            int x = fieldBounds.right - 27; \
            groupText[0] = numberText[7]; groupText[1] = numberText[8]; \
            groupText[2] = numberText[9]; groupText[3] = L'\0'; \
            DrawNumberGlyphString(groupText, x, fieldBounds.top, glyphRow); \
            if (firstDigit < 7) { \
                EPFTileContext comma; \
                comma.Initialize(L"COMMA.EPF", 0, L"COMMA.PAL"); \
                RectBounds commaBounds; \
                InitRectBounds(&commaBounds, x - 5, fieldBounds.top + 7, \
                               x, fieldBounds.top + 11); \
                RenderTileFrame(&comma, &commaBounds); \
                x -= 32; \
                groupText[0] = numberText[4]; groupText[1] = numberText[5]; \
                groupText[2] = numberText[6]; \
                DrawNumberGlyphString(groupText, x, fieldBounds.top, glyphRow); \
            } \
            if (firstDigit < 4) { \
                EPFTileContext comma; \
                comma.Initialize(L"COMMA.EPF", 0, L"COMMA.PAL"); \
                RectBounds commaBounds; \
                InitRectBounds(&commaBounds, x - 5, fieldBounds.top + 7, \
                               x, fieldBounds.top + 11); \
                RenderTileFrame(&comma, &commaBounds); \
                x -= 32; \
                groupText[0] = numberText[1]; groupText[1] = numberText[2]; \
                groupText[2] = numberText[3]; \
                DrawNumberGlyphString(groupText, x, fieldBounds.top, glyphRow); \
            } \
            if (numberText[0] != L' ') { \
                groupText[0] = numberText[0]; groupText[1] = L'\0'; \
                DrawNumberGlyphString(groupText, x - 9, fieldBounds.top, glyphRow); \
            } \
        } \
    } while (0)

    DRAW_GROUPED_VALUE(m_statusRow0PartialValue, 4, 2);
    DRAW_GROUPED_VALUE(m_statusRow1PartialValue, 5, 2);
    DRAW_GROUPED_VALUE(m_statusRow2Value, 2, 2);
    DRAW_GROUPED_VALUE(m_statusRow3Value, 3, 0);

    swprintf_s(numberText, 16, L"%03u", static_cast<unsigned int>(m_positionX));
    GetStatusFieldRect(7, &fieldBounds);
    if (m_exposedRegion.IntersectsRect(&fieldBounds))
        DrawNumberGlyphString(numberText, fieldBounds.left, fieldBounds.top, 1);

    swprintf_s(numberText, 16, L"%03u", static_cast<unsigned int>(m_positionY));
    GetStatusFieldRect(8, &fieldBounds);
    if (m_exposedRegion.IntersectsRect(&fieldBounds))
        DrawNumberGlyphString(numberText, fieldBounds.left, fieldBounds.top, 1);

#undef DRAW_GROUPED_VALUE
#undef DRAW_STATUS_BAR
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: Complete compact UserStatusPane2 OnPaint: draws three BAR.EPF tracks, four grouped numeric values with comma sprites, and two coordinate values with exact field rectangles, clipping, glyph rows, and right-anchored arithmetic. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x005baf80-0x005bbe15 UserStatusPane2 OnPaint

## Status

- Exact range: `[0x005baf80,0x005bbe15)`; size `0xe95 / 3733`; SHA256 `D9A7A74E845DF08C388F4B8E14E48556F6C00C01DA61BEEC57CC630D910992FD`.
- Source-authored UserStatusPane2 member; canonical owner/emitter [UID:0000FT][UserStatusPane2](by-class/UserStatusPane2.md), source route [UID:0000P2][UserStatusPane](by-file/UserStatusPane.md) `NexusTK/ui/panels/UserStatusPane.cpp`.
- Parent split/index [UID:0001NO][0x005bab00-0x005bc60c.UserStatusPane2](by-memory/0x005bab00-0x005bc60c.UserStatusPane2.md); this child emits once through UID0000FT `[[CHILDREN]]`.
- Emitter position 50; relative address-sorted Nested delta `0`.

## Behavior And Call Flow

- Primary vtable slot `+0x44` calls this exact method; the body has ten calls to the local 9x11 glyph renderer.
- BAR rows are exactly frames `0/1/2`, `3/4/5`, and percent `6/7/8`; denominator-zero and percent-zero skip drawing, partial fills anchor right, and values above full/100 are not clamped.
- Grouped `%10u` values map to `(field,glyphRow)` `(4,2)`, `(5,2)`, `(2,2)`, `(3,0)` with `COMMA.EPF/PAL`; `%03u` positions use fields 7/8 and glyph row 1.
- Graphical bars are unconditional; grouped values and coordinates retain `m_exposedRegion.IntersectsRect` gating.

## Direct Binary Evidence

- Modeled start `0x005baf80`, size `0xe95`, end `0x005bbe15`; next eleven bytes are parent-only `0xcc`.
- Hex-Rays body is 427 lines and references exact BAR/COMMA resources, `MulDiv`, `EPFTileContext`, rect APIs, and the class fields closed by UID0000FT.
- Ten glyph calls occur at `0x005bb2d0`, `0x005bb386`, `0x005bb650`, `0x005bb706`, `0x005bb9e5`, `0x005bba9b`, `0x005bbbf4`, `0x005bbcaa`, `0x005bbd56`, `0x005bbdff`.
- Exact body/range hash `D9A7A74E845DF08C388F4B8E14E48556F6C00C01DA61BEEC57CC630D910992FD`; live bounded IDA MCP evidence was rechecked under healthy database session `9b0396a3`.

## Source Shape And Ownership

- Accepted source identity: `UserStatusPane2 OnPaint`; the managed block is the complete human-written destination for this range.
- Embedded tables/alignment remain binary evidence, not handwritten C++; vptr, adjustor, cookie, EH, deleting-wrapper, and RTTI forms are compiler exclusions.

## Positive Evidence

- Primary vtable route, resources, exact arithmetic, field accesses, and ten helper calls make the complete visible behavior auditable.

## Negative Evidence And Rejected Alternatives

- The stale four-bar, HP/MP-specific, generic text-renderer, unconditional-text, and clamped-percent interpretations are contradicted by the body.
- No evidence supports third-party/MenuVariety ownership, raw IDA names, duplicate emission, or source-authored padding.

## Score Rationale

- `92/93`: exact range/hash/behavior/state/source/owner/emission/boundary evidence is complete; the cap reflects inferred compact lexical spellings, not an unresolved behavior blocker.

## Historical Corrections

- The former broad UID0001NO no-split/empty-emitter disposition is superseded; stale broad labels are retained only as historical search provenance.

## Changes

- 2026-07-20: Agent-B003 created this child in the accepted UID0001NO split callback with full formal and evidence detail.

