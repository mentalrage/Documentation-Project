*** UID:0004A3 | DO NOT MODIFY OR REMOVE!!! ***
*** COMPLETION:88 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CONFIDENCE:91 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** CANONICAL_OWNER:0000DQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTABLE:TRUE | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_UIDS:0000DQ | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** EMITTER_POSITION_OPTIONAL: | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:[[[]]] | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_CPP CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
void SpellMacroEditControlPane::OnPaint()
{
    const Rect row = GetClientRect();
    BeginMacroRowPaint();

    const SpellRecord &spell = g_spellRecords[NormalizeSpellKey(m_spellKey)];
    const int centerY = (row.Height() + 12) / 2;

    if (UseEpfMacroDialogLayout())
    {
        DrawMacroRowText(row.left + 5, centerY, FormatSpellKey(m_spellKey));
        DrawMacroRowText(row.left + 15, centerY, spell.displayName);
    }
    else
    {
        DrawMacroRowText(row.left + 1, row.bottom - 1, FormatSpellKey(m_spellKey));
    }

    if (IsFocused())
        DrawMacroRowFocus(row);
}
*** RECONSTRUCTION_CPP CODE:END | DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:BEGIN | ONLY MODIFY BETWEEN BEGIN/END - DO NOT REMOVE!!! ***
*** RECONSTRUCTION_H CODE:END | DO NOT REMOVE!!! ***

*** Item Summary: SpellMacroEditControlPane paint handler: draws the spell macro row key/name text with layout-dependent coordinates and focus frame. | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***
*** Nested:0 | ONLY MODIFY VALUE - DO NOT REMOVE!!! ***

# 0x0057f7d0-0x0057f927 SpellMacroEditControlPane OnPaint

## Status

- Address range: `0x0057f7d0-0x0057f927` half-open.
- Entity kind: `SpellMacroEditControlPane` virtual paint method.
- Direct owner/emitter: [UID:0000DQ][SpellMacroEditControlPane](by-class/SpellMacroEditControlPane.md) `SpellMacroEditControlPane`.
- Vtable route: Spell primary paint slot in [UID:0001Y1][MacroDialogFamilyVtables](by-type/by-vtable/MacroDialogFamilyVtables.md) and [UID:0003DB][0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData](by-memory/0x0062d1e0-0x0062d3cc.MacroEditControlPaneVtableData.md).
- Split parent: [UID:0001IK][0x0057f750-0x005807c5.MacroEditControlPanes](by-memory/0x0057f750-0x005807c5.MacroEditControlPanes.md) `MacroEditControlPanes`.
- Rebuild handling: source-authored virtual method; emit the formal C++ above.

## Evidence

Current MCP/report evidence gives function size `0x157` and vtable-routed dispatch through the Spell edit-control primary vtable. The method reads the `wchar_t m_spellKey` field at complete-object `+0x108`, normalizes that character for the spell-record lookup and visible key text, reads spell display-name data, chooses coordinates from the macro-dialog layout mode, and draws the focus frame when the row owns focus.

## Spell-Key Lifecycle Context

- Both constructor callers initialize `m_spellKey` from `Config::m_legacyShortcutText[row][0]`; the key/text handler later replaces it only with an accepted ASCII letter widened into the same two-byte `wchar_t` field.
- This method consumes that field as character state: `FormatSpellKey(m_spellKey)` produces visible wide text, while `NormalizeSpellKey(m_spellKey)` supplies the lookup form used to select the associated spell record. No numeric spell-index field or separate display key is evidenced.
- `SpellMacroEditControlPane::GetSpellKey()` returns the same field and `SpellMacroDialog::OnControlCommand` stores it unchanged back into the first wide-character slot, followed by a wide zero terminator. `SaveUserSettings` persists the legacy shortcut section through `fputwc`, and the runtime spell-shortcut handler reads the same table.
- The earlier generic word/`unsigned short` interpretation described only the machine width. The constructor/edit/display/save/persistence/runtime chain establishes `wchar_t` as the stronger human source type without changing this method's accepted body, score, owner, route, or formal block.

## Historical And Negative Evidence

- No evidence supports separate key-code and display-character members, a raw `uint16_t` source API, a spell index stored at `+0x108`, or an explicit cast/raw-offset access in human source.
- The formatting and normalization helper names remain source-facing reconstructions rather than recovered symbols. That lexical uncertainty is already reflected by the existing confidence score and does not change the accepted paint behavior.

## Changes

- 2026-07-16 B005 UID0004A1 implementation callback: synchronized the accepted `wchar_t` field lifecycle and historicalized the former generic word interpretation; preserved the exact paint formal, `88/91` score, ownership, vtable route, and all recovered drawing behavior.
