# UID0000KF Removed Operational Package

Source report: [0000KF-ItemMenuDialogs-file-source-quality.md](0000KF-ItemMenuDialogs-file-source-quality.md)

This sibling is an inert historical preservation companion. It is non-authoritative, must never be executed, must never be submitted to `execute_report`, and receives no Gate, coverage, score, IDA, validator, or lifecycle credit. Everything below is operational package material removed verbatim from failed source-report SHA256 `F790C66CBA7B8FD776BEC4BE91213543B5105825CB757B22028BE27B27E800A1`.

## Removed Report-Level Package References

- The supervisor-only physical handoff is the declarative `POWERSHELL_FILE_REPLACE_V1` protocol: direct write-through PowerShell/.NET file creation and flush, one-attempt same-volume atomic `System.IO.File.Replace`, and two agreeing independent fresh-process closed namespace receipts. It does not rely on the access-denied parent-directory flush or the policy-blocked custom harness, and B006 executed none of it.
- Canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` is dated read-only research evidence and the source for independently verified backup/TX copies only. It is never the Gate 2B mutation or save target.
- Gate 2B authority begins only after a byte-identical isolated transaction copy `TX` is opened with `mode:"force_headless"`, the returned session reports exact `input_path == TX` and a nonempty `session_id`, that returned identifier alone is bound as `DB`, and schema-1 runtime attestation succeeds against `expected_database:DB` and `expected_canonical_path:TX`.

Gate 2B action accounting is explicit: 13 pure renames, 13 immediate persisted-name `inspect_items/addrs` gates, 13 cache invalidations, 13 decompilations/readback groups, and 7 address-repeatable-comment writes; P01 and PV01-PV12 are no-action protections. Persistent IDA mutation count remains 20; all readbacks, capability probing, candidate creation/replacement and recovery are non-IDA verification/persistence work. The supervisor requires actual top-level and worker-nested schema/success/empty-errors fields plus complete actual listener identity/process/module closure; binds five pairwise-distinct non-reusable roles; performs the sole `idb_save({database:DB,path:TX})`; and retires routes. `POWERSHELL_FILE_REPLACE_V1` first passes its disposable pre-IDB same-parent capability gate, then promotion and restore independently enforce exact-parent/same-volume non-alias identity, unique regular one-link `CreateNew` candidates, file `WriteThrough`/`Flush(true)`/closure, collision failure without deleting the collided path, closed exclusive probes, one direct fresh-process atomic `File.Replace` without retry/fallback, two agreeing fresh-process namespace receipts, independent canonical reopen/hash/close, unchanged source/BACKUP and zero residue. Success closes through distinct `DB_FINAL`; any post-promotion failure is classified through distinct `DB_ROLLBACK`, restored only from BACKUP, and proved through distinct `DB_RESTORE`. No listener-local status fields are invented, and unavailable directory flushing receives no success credit.
- CKF-059 through CKF-062 use the concrete `POWERSHELL_FILE_REPLACE_V1` handoff. Its pre-IDB disposable `File.Replace` probe makes primitive availability a current gate, its file and namespace receipts are separately observable, and its failure branches preserve canonical/sealed BACKUP without claiming that the unavailable parent-directory flush succeeded.
- The 94/94 source score does not depend on executing CKF-059 through CKF-062; the supervisor-only persistence proposal is independently ready for a fresh capability/receipt gate and retains no unresolved parent-directory-flush dependency.
- Persistence handoff is resolved as `POWERSHELL_FILE_REPLACE_V1`: file bytes use direct write-through `FileStream` plus `Flush(true)` and closed receipts; namespace transitions use exactly one direct same-volume atomic `File.Replace` plus two agreeing independent fresh-process namespace receipts. Directory-handle access denial and the blocked custom harness are classified `DIRECTORY_FLUSH_UNAVAILABLE`, not success. Failure of the supported direct primitive is tested by the disposable probe before BACKUP/TX or IDB open, so no future investigation or permanently impossible normal path remains.
- Supervisor verification rule: bind any review to the exact current report artifact and independently verify ordinary documentation and manual coverage. Any IDA action uses the complete Section 21 contract: actual top-level and worker-nested schema/success/empty-errors fields, full listener identity/process/module closure without listener-local status, five pairwise-distinct non-reusable roles, TX-only save and route retirement. First pass the disposable pre-IDB `POWERSHELL_FILE_REPLACE_V1` capability gate. Promotion and restore then independently require exact-parent/same-volume non-alias `CreateNew` candidates, `WriteThrough`/`Flush(true)` and closed exclusive probes, collision failure without deleting/reusing the collided path, one direct fresh-process atomic `File.Replace` with no retry/fallback, two agreeing fresh-process namespace receipts, canonical reopen/hash/close, unchanged source/BACKUP and zero residue; success proves `DB_FINAL`, while post-promotion failure mandates `DB_ROLLBACK` then BACKUP-only `DB_RESTORE`. Directory-handle flush and the policy-blocked custom harness are neither requested nor credited. Lifecycle commands are authorized only by current supervisor state after successful required verification.
- Persistence-handoff confidence: high for the concrete supported protocol; actual Gate 2B persistence remains contingent on its fresh pre-IDB capability gate and every named closed file/namespace receipt, with no credit for an unavailable flush.

## Removed CKF-059 Through CKF-062 Ledger Rows

| CKF-059 | 00040D | Supervisor A01-A03 closure: enforce schema-accurate role-specific runtime proof, each row's independent no-`set_type` decision and rename gate, TX-only save/verification, and five non-reusable roles; use `POWERSHELL_FILE_REPLACE_V1` with a pre-IDB disposable `File.Replace` capability gate, write-through `CreateNew`/`Flush(true)` candidates, exact-parent/same-volume anti-alias closure, one direct atomic replacement attempt, and two agreeing fresh-process namespace receipts before `DB_FINAL` or after `DB_RESTORE`. | High | Per-A01-A03 type rationale; complete runtime/save receipts; exact `ORIGINAL`/failed-state guards; candidate/promoted/failed/restored/final closed-file and namespace receipts; complete expected/failed/original readbacks. | Section 21 transaction/type/promotion/rollback/restore/final package/A01-A03 | incorporate | proposed |
| CKF-060 | 0001BP | Supervisor A04-A07/P01 closure: enforce each A row's independent no-`set_type` rationale and immediate rename gate while P01 remains literal no-change; inherit the complete five-role TX/save/runtime contract and independent `POWERSHELL_FILE_REPLACE_V1` promotion/BACKUP-only restore paths with collision-failing create-new candidates, file write-through/flush/closure, one direct atomic `File.Replace`, canonical rehash, two fresh-process namespace receipts and zero candidate residue. | High | A04-A07 type-specific evidence; no listener-local status invention or role reuse; saved/promoted/final equal TX, restored equal ORIGINAL; complete `DB_ROLLBACK`/`DB_RESTORE`/`DB_FINAL` readbacks and receipts. | Section 21 transaction/type/promotion/rollback/restore/final package/A04-A07/P01 | incorporate | proposed |
| CKF-061 | 00040E/0001BQ | Supervisor A08-A13 closure: enforce six independent no-`set_type` decisions and immediate pure-rename gates, exact public save/runtime schemas and five one-use roles; promotion and restore each use resolved exact-parent/same-volume non-alias regular one-link `CreateNew` candidates, `FileOptions.WriteThrough` plus `Flush(true)`, closed exclusive probes, one fresh-process direct atomic `File.Replace` with no retry/fallback, two agreeing namespace receipts, canonical rehash and zero residue before distinct `DB_FINAL` or recovered `DB_RESTORE`. | High | A08-A13 item-specific ABI/frame rationale; exact `{ok,error?,path}`; separate role baselines/sessions/routes and physical receipts; complete expected/failed/original readbacks. | Section 21 transaction/type/promotion/rollback/restore/final package/A08-A13 | incorporate | proposed |
| CKF-062 | 0001BP/0001BQ | Supervisor R01-R07 closure: apply seven AP-only writes in TX/DB while preserving each unnamed/untyped one-byte head, function absence, range/hash and exact xrefs; carry them through the same independent `POWERSHELL_FILE_REPLACE_V1` write-through/create-new/closed-probe/one-attempt-atomic-replace/two-receipt promotion and BACKUP-restore contract, classify failed canonical in distinct `DB_ROLLBACK`, prove original in `DB_RESTORE`, and prove expected comments in `DB_FINAL`. | High | Seven independent current/pre/post/hard-stop rows; TX-only mutation; five non-reusable roles; collision failure has no cleanup/reuse authority; complete physical and semantic receipts. | Section 21 transaction/promotion/rollback/restore/final R01-R07 | incorporate | proposed |

## Removed Section 21 Operational Package

All operations are supervisor-owned. No set_type, stack rename, function creation, byte change, frame edit, UDT edit, vtable/COL rename, save, cache action, or physical replacement was performed by B006. Canonical `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` is read-only evidence and the verified source for backup/TX copies; it never supplies a Gate 2B mutation session and is never an IDA mutation/save target. Its only authorized promoted change is `POWERSHELL_FILE_REPLACE_V1`: guarded physical replacement from a closed/write-through-flushed/rehashed same-parent candidate derived solely from verified saved TX after route retirement; recovery may replace it only from a separately closed/sealed BACKUP-derived restore candidate after `DB_ROLLBACK` classification. Both paths independently require the exact-parent/same-volume anti-alias, create-new/no-overwrite, collision, closed exclusive-probe, one direct atomic `System.IO.File.Replace` attempt, two agreeing independent fresh-process namespace receipts, canonical reopen/hash/close, and zero-residue contract below. `DB` below means only the nonempty `session.session_id` returned by the exact isolated-TX `idb_open` call and subsequently accepted by TX-bound runtime attestation. Every row below is independently literal; any mismatch in name, persisted type, public prototype, decompiler declaration, frame, comments, bytes, range, hash, xrefs, callees, item kind, or boundary hard-stops that row before mutation and save.

### Isolated Gate 2B transaction startup

1. Before creating `BACKUP`/`TX`, opening any IDB or starting any mutation worker, the supervisor passes the disposable same-parent `POWERSHELL_FILE_REPLACE_V1` capability gate defined below. Only then obtain the fresh gate-time `ORIGINAL` closed-file/namespace receipt for canonical; create `BACKUP` and separate transaction copy `TX` from separate reads of those verified canonical bytes using exclusive `FileMode.CreateNew`, `FileShare.None`, `FileOptions.WriteThrough`, `Flush(true)` and complete disposal; and obtain two agreeing independent fresh-process receipts. `BACKUP` and `TX` must match `ORIGINAL` while remaining distinct regular one-link file identities in canonical's sealed parent and volume. These are filesystem receipts, never `runtime_attestation` fields. Any capability, collision, creation, close, exclusive-probe or receipt failure hard-stops before MCP mutation; canonical is unchanged, and a successfully sealed BACKUP is preserved.
2. The only mutation-worker open is literal `idb_open {input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:3600}`. It must return `success:true`, a `session` object, exact `session.input_path == TX`, and a nonempty `session.session_id`. The supervisor binds that returned `session.session_id`, without substitution, as `DB`. Canonical-path sessions and historical session identifiers confer no authority.
3. Before the first attestation or any mutation, the supervisor must issue the public MCP `tools/list` request against the exact listener, follow every `nextCursor` page to exhaustion, and retain the complete advertised-tool/schema receipt. It must prove exactly one exact-name public `runtime_attestation`, zero advertised tool names, aliases, or paths exposing internal `runtime/attest`, and exactly one exact-name public `idb_save` whose input schema requires string `database`, permits optional string `path`, and introduces no alternate save authority. Absence, duplication, pagination ambiguity, schema drift, or internal exposure hard-stops before mutation. Repeat the exhaustive public `tools/list` check after any listener restart or listener PID, generation, source-root, or role-specific implementation-manifest change and before resuming authority. Never construct, address, or call internal `runtime/attest`.
4. Only after the public-tools check passes and `DB` is bound, call literal public `runtime_attestation {expected_database:DB,expected_canonical_path:TX}` and validate the complete package below. The advertised public `runtime_attestation` endpoint is the sole attestation authority.

### Complete public runtime-attestation package

The runtime package is one indivisible fail-closed precondition and must follow the actual public schema. Independently require top-level `schema_version == 1`, top-level `ok == true`, and top-level `errors` present as an array exactly equal to `[]`; for every phase-selected worker independently require `worker.attestation.schema_version == 1`, `worker.attestation.ok == true`, and `worker.attestation.errors` present as an array exactly equal to `[]`. Missing, null, false, malformed, non-array, or nonempty actual status/error fields hard-stop even when another level passes. The listener object has no listener-local `schema_version`, `ok`, or `errors`, so none may be required or synthesized; instead every actual listener role/freshness/endpoint/port/process/generation/source-root/manifest/module field below is mandatory and fail-closed. Every other named runtime field must also be present, non-null, well-formed, and exactly consistent, with no stale, ambiguous, duplicate, or extra phase-session/path ownership. Freshness means a new `attested_at_utc` and nonce for the boundary being authorized; cached responses do not confer authority. `runtime_attestation` proves runtime/session/path/process/route/module identity only and never supplies or substitutes for a physical IDB byte-count/SHA256 receipt.

| Closure | Required exact checks |
| --- | --- |
| Public tools surface | Require the complete paged public `tools/list` receipt described above: exactly one exact-name `runtime_attestation`, no advertised `runtime/attest` name/alias/path, and exactly one exact-name `idb_save` with required string `database` and optional string `path`. The receipt must belong to the same listener PID/generation and role-specific listener source manifest as the package; a generic assertion, cached listing, or implicit/default-path save schema is insufficient. |
| Listener identity and port ownership | Require actual `listener.role == "supervisor_listener"`; fresh `listener.attested_at_utc` and `listener.nonce`; exact loopback `listener.endpoint.host` and configured `listener.endpoint.port`; and `listener.process.pid` independently confirmed as the current TCP owner of that port. Record and require exact `parent_pid`, `creation_time_100ns`, `generation`, `sys_executable`, `process_image`, and `argv`; reject PID or generation reuse and any listener change not followed by a wholly fresh package. The public listener object has no local schema/`ok`/`errors`, and this row does not invent them. |
| Listener implementation closure | Require exact `listener.source_root` and 64-hex `listener.implementation_manifest_sha256`. For every listener `modules` row require and record exact `sys_modules_name`, `module_id`, `file`, `spec_origin`, `resolved_path`, `size`, `mtime_ns`, `ctime_ns`, and 64-hex `sha256`; every resolved path must remain under the attested source root, and the complete ordered field/hash set must equal only the accepted listener-role baseline unless a deliberate listener restart is fully rebound and reattested. Never compare this set or aggregate manifest for equality with a worker-role set or manifest. |
| One owned DB/TX route | Require exactly one `workers` route to claim both `DB` and `TX`. Its `route.host`/`route.port` must identify the attested worker endpoint. Its `supervisor_sessions` must contain exactly one row with `session_id == DB`, canonical path exactly `TX`, expected backend, `owned == true`, and exact `redirector_pid` and `registered_pid`; reject duplicate, inactive, historical, canonical-path, or competing session claims. |
| Separate physical IDB receipts | Obtain `CLOSED_FILE_RECEIPT` and required paired `CLOSED_NAMESPACE_RECEIPT` evidence directly from fresh filesystem processes for `ORIGINAL` canonical, `BACKUP`, initial/saved/verifier/final-pre-promotion `TX`, closed promotion candidate, immediately promoted canonical, failed canonical before/after `DB_ROLLBACK`, closed restore candidate, immediately restored canonical, `DB_RESTORE` pre/post readback canonical, and `DB_FINAL` pre/post readback canonical. Keep every receipt distinct from runtime proof. `BACKUP`/initial TX/restored identities equal `ORIGINAL`; verifier/promotion-candidate/promoted/final contents equal saved TX; failed-state receipts must equal each other across read-only classification without being accepted as good; `BACKUP` remains unchanged throughout. |
| Worker process, parent route, and session | For every phase-selected worker independently require literal `worker.attestation.schema_version == 1`, `worker.attestation.ok == true`, and present-array `worker.attestation.errors == []`; exact worker role; fresh worker `attested_at_utc` and nonce; and endpoint host/port exactly equal the owned route. Independently confirm worker PID owns the route port; require exact `parent_pid`, `creation_time_100ns`, `generation`, `sys_executable`, `process_image`, and `argv`. Require exactly one active session for the current phase identifier and exact current phase path, with no competing writable or canonical session. |
| Worker implementation closure | Require exact worker `source_root` and 64-hex `implementation_manifest_sha256`. For every worker `modules` row require and record exact `sys_modules_name`, `module_id`, `file`, `spec_origin`, `resolved_path`, `size`, `mtime_ns`, `ctime_ns`, and 64-hex `sha256`; paths must remain under the worker source root and the complete ordered field/hash set must equal only the accepted worker-role baseline. Listener and worker roles intentionally load different module sets: cross-role row-set, field-set, per-module-hash, or aggregate-manifest equality is invalid and must never be required. |
| Phase-role isolation | `DB`, `DB_VERIFY`, `DB_ROLLBACK`, `DB_RESTORE`, and `DB_FINAL` are five pairwise-distinct one-use role labels, each bound once to its own newly returned nonempty session ID, worker route, ownership row, runtime package sequence, and accepted worker-role baseline. No session identifier, worker route/port, ownership row, role label, or cached package may be reused, substituted, aliased, or promoted from one phase role to another, even after retirement or when two roles open the same physical path. |

Repeat and pass a fresh successful complete public runtime package at every literal boundary: (a) immediately before every persistent rename/comment mutation and after its complete readback; (b) immediately before and after the sole isolated-TX save; (c) immediately after distinct read-only `DB_VERIFY` opens, before its complete readback, after readback, and at pre-promotion; (d) immediately after distinct read-only `DB_FINAL` opens, before complete expected-state readback, and after readback; (e) after a promoted-state failure, immediately after distinct read-only `DB_ROLLBACK` opens, before complete failed-state readback, and after readback; and (f) after physical restore, immediately after distinct read-only `DB_RESTORE` opens, before complete original-prestate readback, and after readback. Every repetition independently requires actual top-level and worker-nested schema/success/empty-errors fields, complete actual listener identity/process/module closure, a new timestamp/nonce, exact phase database/path binding, and the phase worker's own baseline; runtime never carries physical size/SHA. Repeat exhaustive public `tools/list` at each pre-promotion, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` boundary below.

The listener identity/generation and mutation worker identity/route/session/same-role module closure must remain stable across an authorized mutation/save sequence. The verifier reopen must return a new nonempty session bound as `DB_VERIFY` on a freshly owned read-only worker route whose session, worker PID, route port, and ownership row are independent of `DB` and its mutation route; only after its own worker-role baseline passes for `expected_database:DB_VERIFY` and `expected_canonical_path:TX`, and a separate verifier filesystem receipt equals the accepted saved-TX receipt, may persistence readback begin. `DB_VERIFY` is readback-only and grants no mutation or save. Its complete expected-poststate readback, fresh post-readback runtime package, and matching final TX receipt establish only promotion eligibility; they do not establish final canonical persistence.

Any public-tools/schema, actual top-level or worker-nested status/error, actual listener identity/process/module, same-role baseline, semantic readback, role-isolation, route-retirement, candidate-seal, or filesystem-receipt mismatch hard-stops. Before promotion, failure retires/discards isolated routes and leaves canonical and the sealed BACKUP at `ORIGINAL`; any exact receipt-bound unconsumed candidate cleanup must itself end in two agreeing fresh-process absence receipts. At or after the single promotion call, any exception, ambiguity, replacement or `DB_FINAL` mismatch enters distinct `DB_ROLLBACK` failed-state classification before the separately sealed BACKUP restore and distinct `DB_RESTORE` proof; no failed promoted state may be accepted, saved again, directly restored without classification, retried, or reused as another role.

### Promotion and restoration path-identity, collision, and persistence invariant

This is the declarative `POWERSHELL_FILE_REPLACE_V1` acceptance contract, not an executable package. Its exact supported file primitive is a direct PowerShell/.NET `System.IO.FileStream` opened with `FileMode.CreateNew`, `FileAccess.Write`, `FileShare.None`, and `FileOptions.WriteThrough`, followed by `Flush(true)` and disposal. Its exact supported namespace primitive is one direct `[System.IO.File]::Replace($source,$destination,$null,$true)` call from a fresh PowerShell process; on Windows this is the same-volume atomic `ReplaceFileW` replacement operation exposed by .NET. The null backup argument prevents an implicit side backup, `ignoreMetadataErrors:true` prevents metadata-copy errors from substituting a second operation, and the disposable candidate makes verified TX/BACKUP source-preserving. Directory-handle `FlushFileBuffers` is neither invoked nor credited: its observed `Access is denied` and the blocked custom write-through harness are classified `DIRECTORY_FLUSH_UNAVAILABLE`, which selects this supported protocol rather than treating the unavailable flush as success. If any exact direct `FileStream`, `Flush(true)`, or `File.Replace` primitive is itself unavailable, access-denied, policy-blocked or ambiguous, the capability gate below stops before BACKUP/TX creation, `idb_open`, mutation or save; there is no rename, copy-overwrite, custom-harness or hash-only fallback.

Promotion and restoration apply the protocol independently; passing it for one candidate grants no authority to the other. Resolve `CANONICAL`, its exact parent, `BACKUP`, `TX`, and every probe/candidate by normalized absolute lexical path and final handle identity. Seal the canonical parent by resolved path, volume identity and directory file ID. Reject any path component or final object that is a reparse point, symbolic link, junction, mount point, alternate data stream, hardlink, short-name/long-name alias, case-fold alias, namespace alias, or any other name resolving to canonical, BACKUP, TX, a prior probe/candidate, …286 tokens truncated…testation and the unavailable directory flush do not substitute for them.

Before any BACKUP/TX creation or IDB open, seal a preliminary canonical `CLOSED_FILE_RECEIPT` and choose never-used nonce-bearing `PROBE_SOURCE` and `PROBE_DESTINATION` names in the sealed canonical parent. Require both names absent under case-insensitive enumeration, create both exactly once with the direct create-new/write-through/flush/dispose primitive and distinct nonce payloads, and seal their closed receipts. From a new process invoke `[System.IO.File]::Replace($PROBE_SOURCE,$PROBE_DESTINATION,$null,$true)` exactly once. Two agreeing new-process namespace receipts must prove source absent, destination present with the former source's exact size/SHA256 and a fresh regular-file identity, parent/volume unchanged, and canonical exactly equal to the preliminary receipt. After exact identity/hash authorization, delete only that disposable probe destination with direct `[System.IO.File]::Delete`, then require two agreeing fresh-process receipts proving both probe names absent and canonical still equal to the preliminary receipt; the subsequent fresh receipt becomes `ORIGINAL`. Any exception, collision, alias/reparse/hardlink/ADS defect, exclusive-open failure, unexpected source survival, receipt disagreement, canonical change or probe residue stops before BACKUP/TX, IDB open or mutation and preserves canonical. The known directory-handle denial and custom-harness policy block do not fail this gate because neither unsupported operation is requested.

For `PROMOTION_CANDIDATE` and independently `RESTORE_CANDIDATE`, choose a never-used nonce-bearing basename in the exact sealed parent, prove it absent case-insensitively, and create it once with the same exclusive create-new/write-through/flush/dispose primitive from only the authorized sealed source. Require a regular one-link new file identity distinct from canonical/BACKUP/TX/all prior roles, exact parent/volume identity, no ADS or alias, successful closed exclusive probe, and two agreeing fresh-process receipts with the exact source size/SHA256. An existing name, case-insensitive collision, unexpected object, create race or identity drift hard-stops without overwriting, deleting, recycling or retrying that collided pathname. A later pre-replacement candidate failure may delete only the exact receipt-bound unconsumed candidate after identity authorization; two agreeing fresh-process receipts must then prove its absence and unchanged canonical/BACKUP. Candidate cleanup never grants another promotion attempt.

Each phase permits exactly one guarded same-parent replacement from a new PowerShell process. Promotion's immediately current canonical receipt must equal `ORIGINAL`; restore's immediately current canonical receipt must equal the failed-state receipt preserved across `DB_ROLLBACK`. Invoke exactly one direct `[System.IO.File]::Replace($CANDIDATE,$CANONICAL,$null,$true)`; TX and BACKUP are never source arguments and remain unchanged. No copy-over, delete-then-rename, cross-volume move, open-handle replacement, fallback API, cleanup-and-retry, candidate recreation, second attempt or unguarded overwrite is authorized. After the call, two agreeing fresh-process namespace receipts must prove the candidate consumed and absent, canonical present under the sealed parent with the authorized source size/SHA256 and valid regular one-link/no-ADS/no-reparse/no-alias identity, source and BACKUP unchanged, and no case variant, temporary replacement residue or second promotable/restorable artifact. Promotion-call exception or receipt ambiguity is post-promotion failed state requiring `DB_ROLLBACK`; restore-call exception or receipt ambiguity stops with classified failed-state and all receipts preserved, never a retry. These physical checks remain separate from all runtime attestations and preserve the five pairwise-distinct one-use roles.

5. Every mutation-phase request carries only `database:DB`; post-save TX verification carries only `database:DB_VERIFY`; failed promoted-canonical classification carries only `database:DB_ROLLBACK`; restored-original proof carries only `database:DB_RESTORE`; final promoted-canonical proof carries only `database:DB_FINAL`. These are pairwise-distinct newly returned sessions and routes: no identifier, role, route, worker ownership row, or package may be reused or substituted across phases. Any drift invokes fail-closed handling.
6. The sole durable IDA save is the exact public call `idb_save({database:DB,path:TX})`. Its result contract is exactly `{ok,error?,path}`: require `ok:true`, absent or empty `error`, and returned `path` normalized exactly to `TX`; default-path, missing-path, alternate-database, null-path, or extra save authority is forbidden. Without intervening mutation, obtain the separate saved-TX filesystem receipt, then pass the immediate post-save `DB` runtime package. Open and attest independent read-only `DB_VERIFY`, obtain a matching verifier receipt, perform the complete expected-poststate readback, pass fresh post-readback runtime proof, and obtain the matching final pre-promotion TX receipt. No second IDA save is authorized.

### Pre-promotion and canonical replacement closure

7. Immediately before promotion, rerun exhaustive public `tools/list`; pass fresh `DB_VERIFY` proof against `TX`; reread complete expected A01-A13/R01-R07/P01/PV01-PV12 state; require saved/verifier/final-pre-promotion TX receipts identical; and require canonical/BACKUP still equal `ORIGINAL`. Retire `DB`, `DB_VERIFY`, every route capable of writing canonical or TX, and every historical canonical session. Fresh `idb_list`, route ownership and runtime evidence must prove no active mutation route and no worker/session claim on canonical or TX. Candidate creation before this retirement proof is forbidden.
8. Only after step 7, apply `POWERSHELL_FILE_REPLACE_V1` to a new `PROMOTION_CANDIDATE` created from verified saved-TX bytes. Require its closed exclusive-probe and two agreeing file/namespace receipts to equal saved TX, canonical/BACKUP to remain `ORIGINAL`, and all routes to remain retired. From a new process perform the single guarded direct atomic `[System.IO.File]::Replace($PROMOTION_CANDIDATE,$CANONICAL,$null,$true)` against the immediately rechecked `ORIGINAL` canonical receipt, then require two agreeing fresh-process namespace receipts proving candidate consumption/absence, canonical equal to saved TX, unchanged TX/BACKUP and zero alias/residue; independently reopen/hash/close canonical again. This is not `idb_save`. A pre-call collision or candidate failure follows the exact authorized unconsumed-candidate cleanup branch and never attempts promotion. Once the call is invoked, exception, ambiguity, candidate survival, receipt drift, partial/unknown replacement, BACKUP drift or `DB_FINAL` failure invokes step 9 classification before restore; no retry or fallback is authorized.

### Failed-state DB_ROLLBACK classification

9. Any failure or ambiguity at/after promotion first retires `DB_FINAL` if opened and every promoted-canonical/TX route, then proves no active mutation route. Before restore, obtain a failed-canonical physical receipt and open canonical solely for classification with `idb_open {input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:3600}`. Require a newly returned nonempty session/route used by no prior role, bind it only as `DB_ROLLBACK`, require exact canonical input/path, and give it no mutation, cache-mutation, save, promotion or restore authority. Immediately post-open, pre-read and post-read, exhaust public schemas and pass fresh `runtime_attestation {expected_database:DB_ROLLBACK,expected_canonical_path:CANONICAL}` with actual top-level/worker-nested schema/success/error checks, complete actual listener closure, and its own accepted worker-role baseline. Read back every A01-A13/R01-R07/P01/PV01-PV12 field and explicitly classify each as expected-poststate, original-prestate, other, missing or unreadable; this is complete failed-state evidence, never acceptance. A separate post-read physical receipt must equal the pre-open failed receipt, proving classification was read-only. Retire `DB_ROLLBACK` and prove its session/route absent before restore.

### Closed-route physical restore and DB_RESTORE closure

10. Reverify sealed `BACKUP` equals its original receipt and `ORIGINAL`; with all canonical/TX/rollback routes retired, apply `POWERSHELL_FILE_REPLACE_V1` independently to a never-used `RESTORE_CANDIDATE` created only from BACKUP bytes. Its closed exclusive probe and two agreeing file/namespace receipts must equal `ORIGINAL`. From a new process invoke the one guarded direct atomic `[System.IO.File]::Replace($RESTORE_CANDIDATE,$CANONICAL,$null,$true)` only while canonical still equals the failed-state receipt preserved across `DB_ROLLBACK`; never restore from TX, failed canonical bytes, promotion candidate, an alias or an open handle. Two agreeing fresh-process namespace receipts must prove candidate consumption/absence, canonical equal to `ORIGINAL`, unchanged BACKUP/TX, sealed parent/volume and zero alias/residue; independently reopen/hash/close canonical again. A collided pathname is never cleaned or reused. An ambiguous/failed restore preserves `DB_ROLLBACK` classification and physical receipts and stops without copy/delete fallback, candidate recreation, path reuse or a second attempt.
11. Open restored canonical solely for readback with literal `idb_open {input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:3600}`; require exact canonical path and a newly returned nonempty session/route used by no prior role, bound only as `DB_RESTORE`. It has no mutation, cache-mutation, save, promotion or restore authority. Immediately post-open, pre-read and post-read, exhaust public schemas and pass fresh `runtime_attestation {expected_database:DB_RESTORE,expected_canonical_path:CANONICAL}` with actual top-level/worker-nested schema/success/error checks, complete actual listener closure, and its own accepted worker-role baseline. Complete readback must equal every original A01-A13/R01-R07/P01/PV01-PV12 prestate; separate canonical receipts before/after readback equal `ORIGINAL`; BACKUP remains `ORIGINAL`. Retire `DB_RESTORE` and prove its route absent and no active mutation route. This is a failed/recovered Gate 2B disposition, never promoted success.

### Final canonical DB_FINAL persistence closure

12. Only after the successful promotion call, two agreeing fresh-process closed namespace receipts, independent canonical reopen/hash/close, unchanged source/BACKUP and zero-residue scan pass, open canonical read-only with literal `idb_open {input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:3600}`; require exact canonical path and a newly returned nonempty session/route used by no prior role, bound only as `DB_FINAL`. It permits no mutation, cache mutation, save or replacement. Immediately post-open, pre-read and post-read, exhaust public schemas and pass fresh `runtime_attestation {expected_database:DB_FINAL,expected_canonical_path:CANONICAL}` with actual top-level/worker-nested schema/success/error checks, complete actual listener closure, and its own accepted worker-role baseline. Complete readback must match every expected A01-A13/R01-R07 state and unchanged P01/PV01-PV12 protections. Separate canonical receipts before/after readback equal saved TX, BACKUP remains `ORIGINAL`, and final route evidence shows `DB`, `DB_VERIFY`, `DB_ROLLBACK`, and `DB_RESTORE` absent with no active mutation route. Retire `DB_FINAL`, prove its route absent, independently reopen/hash/close canonical again, and obtain two final agreeing fresh-process namespace/file receipts proving canonical still equals saved TX, source/BACKUP unchanged and no candidate residue. Final persistence exists only after all status, runtime, semantic, physical, protocol, role-isolation and route checks pass; otherwise step 9 `DB_ROLLBACK` classification and steps 10-11 restore are mandatory.

## Removed CKF-059 Through CKF-062 Checklist Twins

| [ ] | CKF-059 | 00040D | Supervisor A01-A03 closure: enforce schema-accurate role-specific runtime proof, each row's independent no-`set_type` decision and rename gate, TX-only save/verification, and five non-reusable roles; use `POWERSHELL_FILE_REPLACE_V1` with a pre-IDB disposable `File.Replace` capability gate, write-through `CreateNew`/`Flush(true)` candidates, exact-parent/same-volume anti-alias closure, one direct atomic replacement attempt, and two agreeing fresh-process namespace receipts before `DB_FINAL` or after `DB_RESTORE`. | High | Per-A01-A03 type rationale; complete runtime/save receipts; exact `ORIGINAL`/failed-state guards; candidate/promoted/failed/restored/final closed-file and namespace receipts; complete expected/failed/original readbacks. | Section 21 transaction/type/promotion/rollback/restore/final package/A01-A03 | incorporate | proposed |
| [ ] | CKF-060 | 0001BP | Supervisor A04-A07/P01 closure: enforce each A row's independent no-`set_type` rationale and immediate rename gate while P01 remains literal no-change; inherit the complete five-role TX/save/runtime contract and independent `POWERSHELL_FILE_REPLACE_V1` promotion/BACKUP-only restore paths with collision-failing create-new candidates, file write-through/flush/closure, one direct atomic `File.Replace`, canonical rehash, two fresh-process namespace receipts and zero candidate residue. | High | A04-A07 type-specific evidence; no listener-local status invention or role reuse; saved/promoted/final equal TX, restored equal ORIGINAL; complete `DB_ROLLBACK`/`DB_RESTORE`/`DB_FINAL` readbacks and receipts. | Section 21 transaction/type/promotion/rollback/restore/final package/A04-A07/P01 | incorporate | proposed |
| [ ] | CKF-061 | 00040E/0001BQ | Supervisor A08-A13 closure: enforce six independent no-`set_type` decisions and immediate pure-rename gates, exact public save/runtime schemas and five one-use roles; promotion and restore each use resolved exact-parent/same-volume non-alias regular one-link `CreateNew` candidates, `FileOptions.WriteThrough` plus `Flush(true)`, closed exclusive probes, one fresh-process direct atomic `File.Replace` with no retry/fallback, two agreeing namespace receipts, canonical rehash and zero residue before distinct `DB_FINAL` or recovered `DB_RESTORE`. | High | A08-A13 item-specific ABI/frame rationale; exact `{ok,error?,path}`; separate role baselines/sessions/routes and physical receipts; complete expected/failed/original readbacks. | Section 21 transaction/type/promotion/rollback/restore/final package/A08-A13 | incorporate | proposed |
| [ ] | CKF-062 | 0001BP/0001BQ | Supervisor R01-R07 closure: apply seven AP-only writes in TX/DB while preserving each unnamed/untyped one-byte head, function absence, range/hash and exact xrefs; carry them through the same independent `POWERSHELL_FILE_REPLACE_V1` write-through/create-new/closed-probe/one-attempt-atomic-replace/two-receipt promotion and BACKUP-restore contract, classify failed canonical in distinct `DB_ROLLBACK`, prove original in `DB_RESTORE`, and prove expected comments in `DB_FINAL`. | High | Seven independent current/pre/post/hard-stop rows; TX-only mutation; five non-reusable roles; collision failure has no cleanup/reuse authority; complete physical and semantic receipts. | Section 21 transaction/promotion/rollback/restore/final R01-R07 | incorporate | proposed |

