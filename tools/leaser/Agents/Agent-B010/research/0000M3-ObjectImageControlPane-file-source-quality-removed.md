# Removed Operational Persistence Material

Source report: [0000M3-ObjectImageControlPane-file-source-quality.md](0000M3-ObjectImageControlPane-file-source-quality.md)

This inert companion is historical and non-authoritative. Never execute its contents and never submit this file to `execute_report`. It is not a B report and receives no Gate, coverage, score, IDA, validator, or lifecycle credit. It contains only operational persistence material removed verbatim from the linked source report.

## Verbatim Removed Operational Package

All actions are supervisor-only. The canonical IDB is read-only prestate evidence and the source for a verified durable `BACKUP`; no R/Q/T/C or other mutation endpoint may target a canonical session. Immediately before Gate 2B, the supervisor must recheck canonical literal prestates, close that route, create and verify `BACKUP`, create isolated transaction copy `TX`, and verify `TX` is byte-identical to canonical. `BACKUP` is thereafter sealed and closed as a physical restore source only: it is never passed to `idb_open`, never bound to any database role, and never mutated or saved. A missing backup, hash/size mismatch, non-identical `TX`, nonclosed `BACKUP` handle, or route/reparse/link ambiguity is a hard stop before mutation open.

The isolated copy must be opened through literal current-schema `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`, where `...` may contain only optional current-schema session/output fields and may not replace any named required field. Hard-stop unless the endpoint reports success, returned `session.input_path` equals `TX` byte-for-byte, and returned `session.session_id` is nonempty; assign that exact returned session ID to `DB`. Do not query or require nonexistent `session.canonical_path`. Only after that open contract passes, call `runtime_attestation({expected_database:DB,expected_canonical_path:TX})` and hard-stop unless it succeeds against the isolated path. Every R/Q/T/C, protection-read, and the sole save endpoint through the accepted post-save boundary must bind `database:DB`. Execute one endpoint stage at a time and reread immediately. Across the complete transaction/recovery lifecycle the only database roles are exactly `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE`; every role is the exact fresh nonempty session ID returned by its own successful open, all five are pairwise distinct, all but `DB` are read-only, `DB_FINAL` is used only for successfully promoted canonical, `DB_ROLLBACK` only for failed/ambiguous promoted canonical classification, and `DB_RESTORE` only for restored canonical baseline proof. Retire each role route before subsequent replacement or recovery work.

### AT01 public runtime-attestation and tool-surface contract

AT01 is a mandatory fail-closed acceptance contract, not an optional receipt. Immediately before every AT01 call, a fresh public `tools/list` response must contain exactly one public tool entry named `runtime_attestation`, zero public or internal tool/method entries named `runtime/attest`, and the current public `idb_save` schema: required input `database:string`, optional input `path:string`, and structured result `{ok:boolean,error?:string,path:string|null}`. The only authorized save instantiation is `idb_save({database:DB,path:TX}) -> {ok,error?,path}`. This report does not call, construct, proxy, package, advertise, or invoke the internal worker method. A missing or duplicate public attestation entry, any advertised internal entry, or a changed attestation/save schema is a hard stop before any stateful endpoint.

Primary mutation-session AT01 calls are literally `runtime_attestation({expected_database:DB,expected_canonical_path:TX})`. Persistence phases use only these role-correct literal calls: `runtime_attestation({expected_database:DB_VERIFY,expected_canonical_path:TX})` for saved-TX verifier and pre-promotion proof; `runtime_attestation({expected_database:DB_FINAL,expected_canonical_path:CANONICAL})` only for successfully promoted canonical; `runtime_attestation({expected_database:DB_ROLLBACK,expected_canonical_path:CANONICAL})` only for complete failed/ambiguous-canonical classification before restoration; and `runtime_attestation({expected_database:DB_RESTORE,expected_canonical_path:CANONICAL})` only for restored-canonical original-baseline proof. No AT01 call or database role ever targets `BACKUP`. For each call let `D` and `P` be that call's exact session/path pair; accept it only when all of the following pass together:

1. The top-level response has current `schema_version:1`, `ok:true`, and `errors:[]`. Every attested active stateful-worker entry also has nested `attestation.schema_version:1`, `attestation.ok:true`, and `attestation.errors:[]`; a partial response, omitted active worker, or error anywhere is rejection. The listener contract contains role/process/route/freshness/module fields but no listener-local `ok` or `errors`; those fields must not be invented, queried, or required at listener scope.
2. `listener.role` is `supervisor_listener`; `listener.attested_at_utc` is fresh for the boundary; `listener.nonce` is nonempty and fresh; `listener.endpoint` is exactly host `127.0.0.1`, port `13337`; and `listener.process.pid`, `parent_pid`, creation identity, and `generation` match the expected supervisor-launched listener route. An independent port-owner/PID-reuse check must prove that exact listener PID/generation currently owns `127.0.0.1:13337`.
3. Exactly one active worker route matches `D` and `P`. Its `supervisor_sessions` row has `session_id == D`, normalized `canonical_path == P`, `backend == "worker"`, `owned:true`, and one expected redirector identity `redirector_pid == registered_pid == R`. Its nested `worker.role` is `stateful_worker`; fresh `attested_at_utc` and nonce are present; nested endpoint equals the advertised worker route; and worker process PID/generation match the expected supervisor-spawned worker `W` with `parent_pid == R`. The nested active session has `session_id == D`, `input_path == P`, normalized `canonical_path == P`, and `is_active:true`. An inactive row, alias session, second active `D`/`P` route, unexpected redirector, PID reuse, or parent mismatch is rejection. `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` must each be the exact nonempty `session.session_id` returned by its own successful role-correct `idb_open`, must be pairwise distinct, and must never be inferred from a requested or preferred ID; a retired role may not be rebound or reused.
4. Runtime path binding and physical disk identity pass as distinct proofs. The response's matching supervisor-session and nested-session paths equal normalized `P`; `runtime_attestation` supplies no physical IDB byte count or SHA256. A separately obtained filesystem receipt supplies the phase-correct exact physical path, SHA256, and byte size. Primary pre-mutation/pre-save `DB`/`TX` checks pair with the verified transaction-copy receipt; post-save `DB`/`TX`, `DB_VERIFY`/`TX`, and pre-promotion checks pair with `SAVED_TX`; successful `DB_FINAL`/`CANONICAL` checks pair with the promoted `SAVED_TX` identity; failed/ambiguous `DB_ROLLBACK`/`CANONICAL` checks pair with a fresh failed-state canonical receipt used for complete classification; and `DB_RESTORE`/`CANONICAL` checks pair with canonical's restored original identity. `BACKUP` is independently reverified only as a closed physical path/size/SHA256 source and is never runtime-attested or opened as a database. Runtime path evidence never substitutes for a filesystem path/size/SHA256 receipt, and a filesystem receipt never substitutes for runtime attestation.
5. Listener and matching-worker loaded-module identities are independently complete and sealed against separate role-specific baselines captured at the first accepted AT01 snapshot for each process generation. For the listener, `source_root`, `implementation_manifest_sha256`, and every actually loaded `ida_pro_mcp`/`zeromcp` module row must remain equal only to the accepted listener baseline; for the matching stateful worker, those fields and rows must remain equal only to the accepted worker baseline. Never require the listener and worker implementation-manifest hashes, module sets, or module rows to equal each other. Within each role, every row preserves exact `sys_modules_name`, `file`, `spec_origin`, `resolved_path`, `size`, `mtime_ns`, `ctime_ns`, and SHA256; `file == spec_origin == resolved_path`, and fail-closed containment proves every resolved path lies inside that role's claimed `source_root`. The response exposes no `relative_path`; none may be invented without that containment proof. A venv executable, command line, native-module list, tools-list digest, site-packages hash, route-derived file, or separately launched interpreter is not loaded-module proof.

AT01 plus the fresh public `tools/list` gate must pass for `DB`/`TX` immediately after isolated open and again immediately before the first mutation if another request intervened; after each complete R-plus-five-read, Q-plus-six-read, T-plus-six-read, and C-plus-six-read stage for every I01-I14 row (or after each explicitly bounded replacement atomic stage); after the initial and final P01-P04/function-body protection sweeps; immediately before the sole save; and immediately after save. After `DB` is retired, the gate must pass for fresh `DB_VERIFY`/`TX` before verifier entity/protection reads and again immediately before promotion; retire `DB_VERIFY` before candidate work. Successful promotion requires fresh `DB_FINAL`/`CANONICAL` before final reads and again at final acceptance. Any promotion ambiguity/failure or later `DB_FINAL` failure requires retirement of `DB_FINAL` and every canonical route, then fresh `DB_ROLLBACK`/`CANONICAL` for complete failed-state classification; retire it before restoration. After sealed-candidate restoration, fresh `DB_RESTORE`/`CANONICAL` must pass before original-baseline reads and again at recovery acceptance. No persistence phase may silently rebind or reuse a role, substitute one role for another, open `BACKUP`, or use `DB_FINAL` for restored canonical.

Every AT01 and public `tools/list` request is an additional transaction-protection request. None is included in the 392 row-request topology, the 56 R/Q/T/C stages, the 14 collision guards, or the 322 row readbacks. Any AT01/tool-surface failure or drift forbids the next stateful endpoint. Before save, save nothing. After save but before candidate promotion, perform no further save; retire transaction/verifier workers as required, leave canonical and sealed `BACKUP` untouched, and let only the supervisor quarantine or discard `TX`. After a promotion attempt, any operation ambiguity, physical-canonical mismatch, `DB_FINAL` failure, entity/protection mismatch, or attestation failure enters the distinct `DB_ROLLBACK` classification then sealed `RESTORE_CANDIDATE` recovery and `DB_RESTORE` proof defined below; it must not be handled as a pre-promotion `TX` discard or by opening `BACKUP`.

Every exact item read in this transaction uses current-schema `inspect_items({database:DB,addrs:[...]})`. For a function row, `addrs` is the one-element array containing that row's exact function entry; for an explicitly bounded data/protection sweep it contains only the independently enumerated addresses named by that sweep. Shorthand `inspect_items(address)`, omission of `database`, an empty `addrs`, or any unlisted address is invalid and a hard stop.

After all actions, repeat all protected reads against `DB`, then pass fresh public `tools/list`, including its current public save schema, and complete `DB`/`TX` AT01 immediately before the sole save. Only if every final read and that boundary pass may the supervisor issue the literal request/result contract `idb_save({database:DB,path:TX}) -> {ok,error?,path}`. Require `ok:true`, absent or empty `error`, and non-null returned `path` exactly equal to normalized `TX`; any `ok:false`, null/missing/different path, or nonempty error is failure. No request may save to `CANONICAL` or `BACKUP`. Immediately and separately read the physical file at the returned path, require exact normalized path `TX` and a regular nonempty file, and record its exact byte size and SHA256 as the `SAVED_TX` filesystem receipt; `runtime_attestation` does not supply either physical value. Then repeat fresh public `tools/list` and complete `DB`/`TX` AT01, pairing that runtime proof with the separate `SAVED_TX` receipt. No verifier or promotion begins unless this post-save boundary passes.

After successful post-save proof, retire the primary mutation worker so a same-path open cannot return its existing session. Separately open `TX` for a read-only verification role with literal current-schema `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`; the current public schema has no `read_only` argument, so read-only is enforced by allowing only attestation and bounded read endpoints. Require `success:true`, exact returned `session.input_path == TX`, and a nonempty exact returned `session.session_id -> DB_VERIFY` with `DB_VERIFY != DB`. Pass fresh public `tools/list` and full `runtime_attestation({expected_database:DB_VERIFY,expected_canonical_path:TX})`, then perform the complete F01-F08/G01-G06 and P01-P04 entity/protected-state readback only through `DB_VERIFY`. No rename, cache refresh, type, comment, analysis mutation, `idb_save`, or other mutation/save endpoint may use `DB_VERIFY`. Physical `TX` path/size/SHA256 must still equal `SAVED_TX` before and after verifier reads.

### Sealed exact-parent promotion and restoration protocol

The immediate pre-promotion gate must pass all four identities together: physical `CANONICAL` still equals its immutable original preflight path/size/SHA256; sealed closed physical `BACKUP` still equals its separately verified original path/size/SHA256; physical `TX` still equals `SAVED_TX`; and fresh public `tools/list`, complete `DB_VERIFY`/`TX` AT01, and all verifier entity/protection reads still equal the intended saved post-action state. Any mismatch forbids promotion. After that gate, retire `DB_VERIFY`, `DB`, and every IDA or filesystem handle on `CANONICAL`, `TX`, `BACKUP`, and candidate paths. Resolve and seal `CANONICAL`'s exact containing-directory identity. `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` must be fresh unique collision-checked direct-child paths in that exact directory, pairwise distinct from one another and from `CANONICAL`, `TX`, and `BACKUP`; neither path nor any route component may be a reparse point, symlink, hard-link alias, alternate stream, redirected mount, or different-volume route. Both candidates must be absent before promotion begins, and no stale promotable or restore artifact may exist in the sealed parent.

The supervisor seals `PROMOTION_CANDIDATE` by exclusive no-overwrite creation and copies bytes only from physical `TX` after an independent re-verification proves `TX` still equals the recorded `SAVED_TX` path/size/SHA256 receipt. It must durably flush candidate file data and metadata, durably flush the containing directory, close every source/candidate/directory handle, then independently reopen the candidate read-only and prove its exact normalized path, regular-file/no-link identity, byte size, and SHA256 equal `SAVED_TX`; close that verification handle before replacement. Immediately before consumption, recheck sealed-parent identity, route/collision/reparse/link state, original `CANONICAL`, closed physical `BACKUP`, `TX == SAVED_TX`, exact candidate identity, absence of `RESTORE_CANDIDATE`, and absence of any second promotable artifact. One guarded same-volume atomic replacement may consume `PROMOTION_CANDIDATE` into `CANONICAL`; no retry, copy-over, second candidate, or non-atomic fallback is authorized. After the operation, durably flush the containing directory, close all handles, and prove `PROMOTION_CANDIDATE` leaves no residue, `RESTORE_CANDIDATE` remains absent, no second promotable artifact exists, and physical `CANONICAL` has exact normalized path/size/SHA256 equal to `SAVED_TX`. Any uncertain result enters rollback classification rather than another promotion attempt.

Only after that unambiguous physical promotion proof may the supervisor open canonical read-only with literal current-schema `idb_open({input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`. Require `success:true`, exact returned `session.input_path == CANONICAL`, and fresh nonempty returned `session.session_id -> DB_FINAL`, pairwise distinct from `DB`, `DB_VERIFY`, `DB_ROLLBACK`, and `DB_RESTORE`. Pass fresh public `tools/list`, full `runtime_attestation({expected_database:DB_FINAL,expected_canonical_path:CANONICAL})`, complete intended entity/protection readback, and independent physical `CANONICAL == SAVED_TX` proof; repeat that tool-surface/AT01/physical gate at final persistence acceptance. Never mutate or save through `DB_FINAL`. On acceptance retire `DB_FINAL` before cleanup. `DB_FINAL` is never used for a restored canonical; any open, attestation, readback, or physical failure retires `DB_FINAL` and every canonical route and enters rollback classification.

On any promotion operation ambiguity/failure or any later physical/`DB_FINAL` verification failure, preserve the failure receipts, retire `DB_FINAL` and all canonical routes, close every handle, and do not retry promotion. Independently receipt the failed/ambiguous physical canonical, then open that canonical read-only through literal current-schema `idb_open({input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`. Require `success:true`, exact returned `session.input_path == CANONICAL`, and fresh nonempty returned `session.session_id -> DB_ROLLBACK`, pairwise distinct from all other roles. Pass fresh public `tools/list` and full `runtime_attestation({expected_database:DB_ROLLBACK,expected_canonical_path:CANONICAL})`; perform the complete entity/protection readback and physical path/size/SHA proof through the failed canonical route; and classify it completely as exact original baseline, exact `SAVED_TX`, or a specifically recorded foreign/indeterminate state. `DB_ROLLBACK` never mutates or saves. Retire `DB_ROLLBACK` and every canonical route before restoration work regardless of classification.

With all database and canonical routes retired, independently reverify sealed closed physical `BACKUP` against its original path/regular-file/no-link identity, byte size, and SHA256; never pass it to `idb_open`. Seal `RESTORE_CANDIDATE` by the same exact-parent protocol: confirm unique collision-free absence of both candidates and any second restore/promotable artifact; exclusively create it with no overwrite; copy bytes only from reverified `BACKUP`; durably flush candidate file data and metadata and then the containing directory; close all source/candidate/directory handles; independently reopen read-only and prove exact path/regular-file/no-link identity, size, and SHA256 equal `BACKUP`; close the handle; recheck parent/route/collision/reparse/link state and source identities; then perform one guarded same-volume atomic replacement consuming `RESTORE_CANDIDATE` into `CANONICAL`, with no retry or non-atomic fallback. Afterward durably flush the containing directory, close all handles, and prove `RESTORE_CANDIDATE` and `PROMOTION_CANDIDATE` leave no residue, no second restore/promotable artifact exists, and physical `CANONICAL` and closed physical `BACKUP` both equal the immutable original path/size/SHA256 baseline.

Only after that physical restoration proof may the supervisor open restored canonical read-only through literal current-schema `idb_open({input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`. Require `success:true`, exact returned `session.input_path == CANONICAL`, and fresh nonempty returned `session.session_id -> DB_RESTORE`, pairwise distinct from `DB`, `DB_VERIFY`, `DB_FINAL`, and `DB_ROLLBACK`. Pass fresh public `tools/list`, full `runtime_attestation({expected_database:DB_RESTORE,expected_canonical_path:CANONICAL})`, complete original-baseline entity/protection readback, and independent physical canonical/closed-backup equality proof; repeat that gate at recovery acceptance. Never mutate or save through `DB_RESTORE`; retire it before cleanup. A restored route records transaction failure plus verified recovery, never successful promotion. Save, candidate creation, promotion, rollback classification, restoration, cleanup, and canonical replacement occurrence remain supervisor-owned and externally authoritative.

## Verbatim Removed Dependencies

The accepted persistence handoff specifies exactly five pairwise-distinct returned database roles: `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE`. `BACKUP` is a sealed, closed physical restore source only. Successful promotion and failed/ambiguous-promotion recovery use distinct sealed `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` files in `CANONICAL`'s exact parent, with durable file/directory flush, closed-handle independent verification, one guarded atomic consumption, and zero-residue proof as specified in Section 21. This contract changes no ordinary or formal-source state.

Preserve the accepted ordinary state and the fail-closed Section 21 persistence contract. Supervisor gate sequence, generated-output verification, manual coverage, validation, IDA application, report execution, and archive occurrence remain authoritative only from the report's current path and validator-owned external state; this report prose does not assert a current lifecycle phase.

This canonical session is dated read-only evidence and a backup source only; it is not an authorized mutation destination.

Before Gate 2B, the supervisor must dynamically reopen canonical read-only for prestate only, repeat all literal prestates, close that route, create and verify durable `BACKUP`, and create byte-identical isolated `TX`. The only authorized mutation start is literal current-schema `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,...})`; hard-stop unless it succeeds, returned `session.input_path == TX`, and returned nonempty `session.session_id -> DB`. Do not query or require nonexistent `session.canonical_path`. `BACKUP` remains a sealed closed physical source and is never opened as a database. Exactly five fresh database roles may exist across the transaction and recovery lifecycle: mutation/save `DB`, saved-TX verifier `DB_VERIFY`, successful-promotion canonical verifier `DB_FINAL`, failed/ambiguous-canonical classifier `DB_ROLLBACK`, and restored-canonical verifier `DB_RESTORE`; all five must be pairwise distinct and each route is retired before replacement or recovery work continues.

Before any mutation, a fresh public `tools/list` response must contain exactly one public tool entry named `runtime_attestation`, zero public or internal tool/method entries named `runtime/attest`, and the current public `idb_save` input/result schema used below; this report neither calls nor constructs the internal method. The Section 21 AT01 contract then requires literal primary-session `runtime_attestation({expected_database:DB,expected_canonical_path:TX})` with top-level and matching-worker `schema_version:1`, `ok:true`, empty error arrays, the exact active `DB`/normalized `TX` runtime path, fresh listener and worker nonces, expected listener PID/generation ownership of `127.0.0.1:13337`, exact redirector/worker PID-parent-route binding, and complete listener and worker loaded-module identities each unchanged against its own accepted role-specific baseline, never cross-role equality. `ok`/`errors` are top-level and matching-worker attestation fields; no listener-local `ok` or `errors` field may be invented or required. Separate filesystem receipts, not `runtime_attestation`, supply every phase-correct physical path, byte size, and SHA256. R/Q/T/C, protection, and the sole save remain bound to `database:DB`; every later role is a separately returned read-only session and never reuses or aliases another role.

Fresh public `tools/list` plus complete AT01 acceptance is mandatory immediately after isolated open and before mutation, after every R/Q/T/C mutation-and-readback stage (or explicitly bounded replacement atomic stage), immediately before save, immediately after save, for `DB_VERIFY` before verifier reads and again before promotion, for successful-promotion-only `DB_FINAL`, for failed/ambiguous-canonical `DB_ROLLBACK` before restoration, and for restored-canonical `DB_RESTORE` at recovery acceptance. `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` each require the complete sealed exact-parent physical protocol in Section 21. Any missing field, error, stale nonce, route/path/hash/size/module drift, internal-tool advertisement, database mismatch, candidate residue, or durability uncertainty is a hard stop under the phase-specific abort/restore rules. Session IDs, worker PIDs, health fields, nonces, and generated command identities are dated observations, not permanent current authority.

The Section 21 AT01 public tool-surface/current-save-schema, runtime-response, process-route, separate filesystem-identity, role-specific module-baseline, repeated-boundary, save, verifier, sealed-candidate promotion, rollback classification, sealed-candidate restore, and final-persistence contract is part of the exact meaning of every supervisor-owned IDA claim C0M3-049 through C0M3-053 and C0M3-059 through C0M3-076 and of each exact checklist twin. None authorizes an action, protection disposition, save, promotion, or restore unless fresh public `tools/list` and complete role-correct AT01 checks pass at every mandatory boundary. The exactly five pairwise-distinct roles are `DB`/`TX`, `DB_VERIFY`/`TX`, successful-promotion-only `DB_FINAL`/`CANONICAL`, failed/ambiguous-canonical `DB_ROLLBACK`/`CANONICAL`, and restored-canonical `DB_RESTORE`/`CANONICAL`; `BACKUP` is never opened and remains a sealed closed physical source only. Distinct unique collision-checked `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` paths in canonical's exact parent must satisfy the full durable creation, independent verification, guarded atomic consumption, post-flush, and no-residue protocols. Runtime proof is never physical size/SHA proof, listener/worker module identities are never compared across roles, and these checks remain additional protection requests outside the 392 row-request topology.

| C0M3-057 | 0000M3 | Dated generated/session facts remain snapshots; the accepted supervisor-owned persistence contract requires verified closed physical `BACKUP`, byte-identical isolated `TX`, exact primary `DB` open/AT01 and sole literal `idb_save({database:DB,path:TX}) -> {ok,error?,path}` success, distinct read-only `DB_VERIFY`/`TX`, sealed exact-parent `PROMOTION_CANDIDATE` from verified `SAVED_TX`, successful-promotion-only `DB_FINAL`/`CANONICAL`, and on ambiguity/failure fresh `DB_ROLLBACK`/`CANONICAL` classification followed by sealed exact-parent `RESTORE_CANDIDATE` from `BACKUP` and fresh `DB_RESTORE`/`CANONICAL` original-baseline proof. | Strong | Every applicable fresh public `tools/list` proves exactly one public `runtime_attestation`, no public/internal `runtime/attest`, and current public `idb_save` schema; exactly five pairwise-distinct returned roles are path-bound and read-only except `DB`, `BACKUP` is never opened, and each candidate requires exclusive no-overwrite creation, authorized verified-source bytes, durable file data/metadata and directory flush, closed handles, independent reopen/path-size-SHA proof, guarded atomic consumption, post-flush, and zero residue or second artifact. | Persistence/IDA transaction safety. | incorporate | applied |

This dated canonical evidence is prestate/backup evidence only; Gate 2B must reproduce it on the isolated `DB` session from `TX`.

The isolated `idb_open` prerequisite, every fresh public `tools/list` gate, every repeated AT01 public `runtime_attestation` call, and the separately bounded initial/final P01-P04 and function-body protection sweeps are additional transaction/protection requests and are not folded into the 392 row-scoped total.

The repaired IDA persistence handoff uses exactly five pairwise-distinct roles: `DB` for isolated `TX` mutation and the sole `TX` save, `DB_VERIFY` for saved-TX read-only verification, `DB_FINAL` only for successfully promoted canonical, `DB_ROLLBACK` for complete failed/ambiguous-canonical classification, and `DB_RESTORE` only for restored-canonical original-baseline proof. `BACKUP` is a sealed closed physical source and is never opened. Distinct sealed `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` paths in canonical's exact parent require exclusive no-overwrite creation from their authorized verified source, durable file data/metadata and directory flushes, closed-handle independent path/size/SHA verification, one guarded atomic consumption, post-replacement directory flush, and proof of no candidate or second-artifact residue.

Gate 2 still must prove the one-item refresh response, fresh name-token readback, tagged parser normalization, and every literal protection dynamically.

3. Only after future supervisor gates authorize it, the supervisor must use canonical solely as closed-route read-only prestate/backup source, seal physical-only `BACKUP`, verify byte-identical isolated `TX`, obtain exact primary `DB`, and apply still-unchecked IDA contracts only under Section 21 AT01. Every fresh public `tools/list` proves exactly one public `runtime_attestation`, no public/internal `runtime/attest`, and current public `idb_save` schema; top-level and matching-worker schema/ok/no-errors are required without inventing listener-local `ok/errors`. The exact five pairwise-distinct roles are `DB`/`TX`, read-only `DB_VERIFY`/`TX`, successful-promotion-only `DB_FINAL`/`CANONICAL`, failed/ambiguous-canonical `DB_ROLLBACK`/`CANONICAL`, and restored-canonical `DB_RESTORE`/`CANONICAL`; `BACKUP` is never opened. The sole `idb_save({database:DB,path:TX}) -> {ok,error?,path}` and separate `SAVED_TX` receipt precede distinct sealed exact-parent `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` protocols, each requiring exclusive no-overwrite creation from its authorized source, durable file data/metadata and directory flushes, closed-handle independent verification, one guarded atomic consumption, post-flush, and zero residue or second artifact. Every role-correct boundary retains exact runtime path/process/route/per-role-module proof plus separate physical path/size/SHA receipts.

A later dated report-text repair changed only C0M3-057's persistence wording and established no new Gate 2A or lifecycle occurrence.

| `C0M3-057` | PASS | Dated ordinary implementation remains unchanged, and the accepted supervisor-owned persistence contract uses five pairwise-distinct database roles plus two sealed exact-parent candidates. Its occurrence is determined only by supervisor-owned external evidence, not report lifecycle prose. |

Historical report-text repair cycles through 2026-08-22 changed only this report and left the accepted ordinary implementation, formal source, IDA handoff, and persistence contract intact.

The Section 21 AT01 public tool-surface/current-save-schema, runtime-response, process-route, separate filesystem-identity, role-specific module-baseline, repeated-boundary, save, verifier, sealed-candidate promotion, rollback classification, sealed-candidate restore, and final-persistence contract is part of the exact meaning of every supervisor-owned IDA checklist twin C0M3-049 through C0M3-053 and C0M3-059 through C0M3-076. None authorizes an action, protection disposition, save, promotion, or restore unless fresh public `tools/list` and complete role-correct AT01 checks pass at every mandatory boundary. The exactly five pairwise-distinct roles are `DB`/`TX`, `DB_VERIFY`/`TX`, successful-promotion-only `DB_FINAL`/`CANONICAL`, failed/ambiguous-canonical `DB_ROLLBACK`/`CANONICAL`, and restored-canonical `DB_RESTORE`/`CANONICAL`; `BACKUP` is never opened and remains a sealed closed physical source only. Distinct unique collision-checked `PROMOTION_CANDIDATE` and `RESTORE_CANDIDATE` paths in canonical's exact parent must satisfy the full durable creation, independent verification, guarded atomic consumption, post-flush, and no-residue protocols. Runtime proof is never physical size/SHA proof, listener/worker module identities are never compared across roles, and these checks remain additional protection requests outside the 392 row-request topology.

| [x] | C0M3-057 | 0000M3 | Dated generated/session facts remain snapshots; the accepted supervisor-owned persistence contract requires verified closed physical `BACKUP`, byte-identical isolated `TX`, exact primary `DB` open/AT01 and sole literal `idb_save({database:DB,path:TX}) -> {ok,error?,path}` success, distinct read-only `DB_VERIFY`/`TX`, sealed exact-parent `PROMOTION_CANDIDATE` from verified `SAVED_TX`, successful-promotion-only `DB_FINAL`/`CANONICAL`, and on ambiguity/failure fresh `DB_ROLLBACK`/`CANONICAL` classification followed by sealed exact-parent `RESTORE_CANDIDATE` from `BACKUP` and fresh `DB_RESTORE`/`CANONICAL` original-baseline proof. | Strong | Every applicable fresh public `tools/list` proves exactly one public `runtime_attestation`, no public/internal `runtime/attest`, and current public `idb_save` schema; exactly five pairwise-distinct returned roles are path-bound and read-only except `DB`, `BACKUP` is never opened, and each candidate requires exclusive no-overwrite creation, authorized verified-source bytes, durable file data/metadata and directory flush, closed handles, independent reopen/path-size-SHA proof, guarded atomic consumption, post-flush, and zero residue or second artifact. | Persistence/IDA transaction safety. | incorporate | applied |
