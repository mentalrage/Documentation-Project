<!-- REMOVED-MATERIAL-ARCHIVE -->
# Removed Material Archive

- Status: Non-authoritative archival material only.
- Source report: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\Agent-B010\research\0000MW-Queue-file-source-quality.md`
- Safety: This file must never be executed, replayed, or used as an operational runbook.
- Credit: This archive provides no gate, score, coverage, IDA, validator, or lifecycle credit.

# Removed Executable / Transaction Material Archive

- Source report: [0000MW-Queue-file-source-quality.md](0000MW-Queue-file-source-quality.md)
- Status: non-authoritative preservation archive; this is not a B-agent report and grants no Gate 1, Gate 2, IDA, validator, score, or lifecycle credit.
- Safety: this material must never be executed, imported, evaluated, replayed, or used as operational instruction.
- Scope: exact canonical-open/attestation/backup/save/reopen/rollback contract, MCP request schemas, and ordered per-action call sequences removed from the source report.

## Removed Block R001

~~~text
All IDA actions are proposed and primary-supervisor-owned. B010 performed read-only research and claims no mutation, backup, save, persisted readback, Gate 2B, validation, lifecycle, or execution credit. These actions are not needed to understand the reconstructed source, but every recommendation the supervisor accepts must receive item-by-item Gate 2B closure before report execution. Apply accepted actions serially only after fresh Gate 1 and independent Gate 2A passes, a new canonical `idb_open` with auto-analysis disabled, public runtime attestation of the exact returned native session/path/worker, and exact prestate confirmation. Dated session names below are evidence receipts, not permanent authority.

For this literal-prestate repair, `idb_list` dynamically selected active canonical-path session `supervisor-b003-uid0001NW-retry-20260811`; bounded `server_health` returned `status:ok` for `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`, and fresh `inspect_items`, `get_comments`, `stack_frame`, `get_bytes`, and `xrefs_to` calls succeeded. In the rows below, `absent ("")` means the endpoint returned the literal zero-length string for that field. `N/A because no function exists` is used only when function-comment fields are structurally unavailable; no absent field is implied by a grouped descriptor.

### Supervisor-Owned Gate 2B Runtime, Persistence, And Rollback Contract

The report supplies structured actions and protections only; it contains no transaction program and grants no operational credit. The supervisor must execute these stages outside the report:

| Stage | Exact public binding/action contract | Required receipt, stop, or rollback result |
| --- | --- | --- |
| Canonical open | Call public `idb_open` with `input_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"`, `mode:"force_headless"`, `run_auto_analysis:false`, `build_caches:false`, `init_hexrays:false`, `idle_ttl_sec:600`, and `preferred_session_id:""`. | Require `success:true`, exact canonical path, and a nonempty native `session.session_id`. Record that literal returned value. It is the only permitted `database` for this worker; no report session, preferred ID, alias, stale registry row, or invented identifier is authority. |
| Runtime attestation | Before the first stateful call, before every later mutation boundary, and before save, call only public `runtime_attestation` with `expected_database` byte-for-byte equal to that returned native `session.session_id` and `expected_canonical_path:"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"`. | Require top-level `schema_version:1`, `ok:true`, `errors:[]`, fresh listener nonce/generation, listener PID owning `127.0.0.1:13337`, and exactly one active worker route matching the returned session/path and supervisor-spawned PID/parent. Listener and worker manifests must enumerate the actually loaded `ida_pro_mcp` and `zeromcp` modules with exact `file`/`spec_origin`/`resolved_path` equality, source root, size, timestamps, and SHA256; derive a relative path only after containment proof. Never call internal `runtime/attest`. Any missing, partial, conflicting, stale, or mismatched result stops before mutation. |
| Collision-safe durable backup | With canonical state quiescent and before I01 or any mutation, record canonical disk path, SHA256, bytes, timestamps, worker PID/start/path, and exclusive-open result. Construct a destination outside the canonical and temporary paths from UID `0000MW`, UTC timestamp, full canonical prestate SHA256, and a fresh nonce; require create-new/fail-if-exists semantics, never overwrite, then reread the backup byte-for-byte. | Record the literal backup path, nonce, SHA256, bytes, and timestamps. Backup SHA256 and bytes must exactly equal canonical prestate. Any existing destination collision, active/ambiguous canonical worker, PID reuse ambiguity, failed exclusive-open probe, overwrite-capable result, or backup mismatch stops before mutation. Retain the verified backup through fresh-session persisted readback and any rollback decision. |
| Serial mutation and immediate readback | Apply only I01-I07 accepted actions in order and no P01-P12 mutation. Every action uses the exact returned native session as top-level `database`. | Immediately reread complete name/type/item/range/comments/frame/bytes/xrefs and neighbors specified by that row. Any unexpected/no-op/extra delta stops before the next action and forbids save. |
| Complete pre-save verification | After the last accepted action, rerun every I01-I07 expected readback and every P01-P12 no-change row in the same attested native session. | All action and protection rows must pass together. Partial or mixed state stops without save. |
| Sole save | Call public `idb_save` exactly once with `path:""` and `database` equal to the exact returned native session; never retry an absent, failed, or indeterminate response. | Require a definite successful response. Record raw save result plus canonical disk SHA256, bytes, timestamps, and the one save-attempt count. A response alone never overrides physical disk/session evidence. |
| Fresh-session persisted readback | Retire only the exact transaction worker under supervisor process controls; prove exact PID/start/path absence, no active canonical-path session, and successful exclusive-open classification. Reopen canonical with the exact open contract, use its newly returned native session, and reattest that new identity. | Repeat every I01-I07 and P01-P12 readback from the fresh session and record persisted disk identity. A stale inactive registry row is non-authoritative; an active/ambiguous route is a hard stop. |
| Guarded rollback/cleanup | If failure occurs before save, make no `idb_save` call, retire the exact unsaved worker, prove its PID/start/path route absent, and fresh-open canonical with the exact open contract to prove the original prestate. If a definite/indeterminate save may have affected disk, first classify exact canonical/backup identities under complete quiescence. | On the no-save branch require canonical SHA256/bytes to equal the recorded prestate and every original I/P readback to pass; do not restore over that unchanged canonical file. Restore the verified backup only for a transaction-attributable saved failure with a single unambiguous bad canonical identity. Partial, mixed, both-pass, neither-pass, unknown, third-identity, active-worker, or PID-ambiguous state stops without restore. After any authorized restore, fresh-open/re-attest and prove the full original I/P prestate and exact disk identity. |

**Exact MCP call schemas**

The public MCP schemas accept a single object or array where noted. `<returned-native-session>` means only the literal `session.session_id` returned by the immediately preceding canonical `idb_open`; every action/readback and the sole save must use it byte-for-byte.

| Tool | Exact required call shape used by this handoff | Purpose / result contract |
| --- | --- | --- |
| `idb_open` | `{"input_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64","mode":"force_headless","run_auto_analysis":false,"build_caches":false,"init_hexrays":false,"idle_ttl_sec":600,"preferred_session_id":""}` | Require `success:true`; record exact returned native `session.session_id` and canonical path. |
| `runtime_attestation` | `{"expected_database":"<returned-native-session>","expected_canonical_path":"E:\\NTK\\Resources\\NexusTK\\NexusTK.exe.i64"}` | Require the complete runtime-binding result specified above before every protected boundary. |
| `server_health` | `{"database":"<returned-native-session>"}` | Require `status:ok`, canonical `idb_path`, module `NexusTK.exe`, and imagebase `0x400000`. |
| `type_inspect` | `{"database":"<returned-native-session>","queries":{"name":"Queue","include_members":true,"max_members":32}}` | Prestate must be `exists:false`; poststate must return the exact I01 members. |
| `declare_type` | `{"database":"<returned-native-session>","decls":"struct Queue { unsigned char LObject_base[4]; int m_slotSize; int m_slotCount; void *m_buffer; int m_readIndex; int m_writeIndex; };"}` | Declare only I01. This schema has no dry-run field, so absent-prestate, attestation, and backup are blocking. |
| `define_func` | `{"database":"<returned-native-session>","items":{"addr":"<start>","end":"<end>"}}` | Define only I04-I06 with explicit half-open bounds. |
| `rename` dry run | `{"database":"<returned-native-session>","batch":{"func":{"addr":"<start>","name":"<exact-name>"},"stop_on_error":true,"dry_run":true,"allow_overwrite":false,"pure":true}}` | Reject collision/invalid-name cases before mutation. |
| `rename` apply | Same shape as the dry run with `"dry_run":false`. | Apply one exact function name only after the dry run succeeds. |
| `set_type` | `{"database":"<returned-native-session>","edits":{"addr":"<start>","kind":"function","signature":"<exact-signature>"}}` | Apply only I02, I04, I05, I06, and I07 signatures. I03 deliberately has no `set_type`; no separate local/stack edit is requested. |
| `set_function_comments` | `{"database":"<returned-native-session>","items":{"addr":"<start>","comment":"<exact-comment>"}}` | Apply one literal function-regular comment after a function object exists. |
| `inspect_items` | `{"database":"<returned-native-session>","addrs":["<address>"]}` | Read back exact item head/end/name/type/code-data state. |
| `get_comments` | `{"database":"<returned-native-session>","addrs":["<address>"]}` | Read back address regular/repeatable and function regular/repeatable channels without mutation. |
| `stack_frame` | `{"database":"<returned-native-session>","addrs":["<function-start>"]}` | Prove the exact frame delta or preserved frame rows. |
| `get_bytes` | `{"database":"<returned-native-session>","regions":{"addr":"<start>","size":<decimal-size>}}` | Protect exact code/data/padding bytes. |
| `xrefs_to` | `{"database":"<returned-native-session>","addrs":["<address>"],"limit":100}` | Protect exact vtable/RTTI and function xref counts. |
| `idb_save` | `{"database":"<returned-native-session>","path":""}` | One non-retry save only after full pre-save readback; record success and physical disk identity. |


| I01 | Local type `Queue` | Fresh `type_inspect` returned `{"name":"Queue","exists":false,"error":"Type not found: Queue"}`. | `struct Queue { unsigned char LObject_base[4]; int m_slotSize; int m_slotCount; void *m_buffer; int m_readIndex; int m_writeIndex; };` | Stop if Queue exists, declaration fails, any member differs, or size is not `0x18`; do not declare or replace `LObject`; make a supervisor backup before this no-dry-run call. | `LObject_base +0x00 size 0x04`; `m_slotSize +0x04 size 0x04`; `m_slotCount +0x08 size 0x04`; `m_buffer +0x0c size 0x04`; `m_readIndex +0x10 size 0x04`; `m_writeIndex +0x14 size 0x04`; total `0x18`. |

| I02 `0x005539e0-0x00553a5c` size `0x7c` | Function exists; current name `sub_5539E0`; declaration/type `_DWORD *__thiscall(_DWORD *this, int, int)`; address regular absent (`""`); address repeatable absent (`""`); function regular absent (`""`); function repeatable absent (`""`). Fresh complete physical frame: `var_10 +0x08 size 0x04 LObject *`; `var_C +0x0c size 0x04 _DWORD`; `var_4 +0x14 size 0x04 _DWORD`; `__saved_registers +0x18 size 0x04 _DWORD`; `__return_address +0x1c size 0x04 _UNKNOWN *`; `arg_0 +0x20 size 0x04 _DWORD`; `arg_4 +0x24 size 0x04 _DWORD`; no other rows. | Rename to `Queue_constructor`; set signature `Queue *__thiscall Queue_constructor(Queue *this, int slotSize, int slotCount)`; set function regular to `Queue::Queue; allocates zeroed fixed-slot storage.`; preserve address regular/repeatable and function repeatable as absent (`""`). | Exact post-`set_type` frame: preserve the five non-argument rows byte-for-byte by name, offset, width, and type; replace only `arg_0 +0x20 size 0x04 _DWORD` with `slotSize +0x20 size 0x04 int`, and only `arg_4 +0x24 size 0x04 _DWORD` with `slotCount +0x24 size 0x04 int`. Added rows `0`; removed rows `0`; `this` remains ECX-only and has no physical frame row. | `rename` dry-run/apply, then `set_type`, then `set_function_comments`; expected readback is the exact final name/signature/function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, exact frame at left, unchanged `0x7c` range/124 bytes/xrefs. Stop/no-save on any unlisted delta. |
| I03 `0x00553a60-0x00553ab9` size `0x59` | Function exists; current name `sub_553A60`; declaration/type `void __thiscall(LObject *this)`; address regular absent (`""`); address repeatable absent (`""`); function regular absent (`""`); function repeatable absent (`""`). Fresh complete physical frame: `var_C +0x08 size 0x04 _DWORD`; `__saved_registers +0x14 size 0x04 _DWORD`; `__return_address +0x18 size 0x04 _UNKNOWN *`; `arg_4 +0x20 size 0x04 _DWORD`; no other rows. Fresh decompile exposes only register argument `this`; disassembly uses `arg_4` only in out-of-line SEH helper `0x00606ec3`, not as a destructor call argument. | Rename to `Queue_destructor`; retain current IDA declaration/type `void __thiscall(LObject *this)`; perform no I03 `set_type`; set function regular to `Queue::~Queue; releases the owned slot buffer.`; preserve address regular/repeatable and function repeatable as absent (`""`). Clean source remains `Queue::~Queue()` in formal CPP/H. | Deterministic no-change frame: all four current rows remain exactly named, typed, sized, and offset as at left. Added rows `0`; removed rows `0`. The `arg_4` analyzer row is protected SEH metadata and is neither renamed nor represented as a source parameter. | `rename` dry-run/apply, skip `set_type`, then `set_function_comments`; expected readback is final name, unchanged exact declaration/type, exact function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, unchanged frame, `0x59` range/89 bytes/xrefs. Stop/no-save if any unlisted state changes. |
| I04 `0x00553ac0-0x00553b05` size `0x45` | Function existence absent: `stack_frame` returned `vars:null` and `No function found at address 5585600`. Start item `0x00553ac0-0x00553ac1` is code width `0x01`; current name absent (`""`); declaration/type absent (`""`); address regular absent (`""`); address repeatable absent (`""`); function regular N/A because no function exists; function repeatable N/A because no function exists; zero incoming xrefs. | Define only `[0x00553ac0,0x00553b05)`; rename to `Queue_WriteSlot`; set signature `void __thiscall Queue_WriteSlot(Queue *this, const void *slot)`; set function regular to `Queue::WriteSlot; copies one fixed-size slot and advances the write index.`; preserve address regular/repeatable and function repeatable as absent (`""`). | Exact post-`define_func` frame: `__saved_registers +0x04 size 0x04 _DWORD`; `__return_address +0x08 size 0x04 _UNKNOWN *`; `arg_0 +0x0c size 0x04 _DWORD`; no local row at `+0x00` and no other rows. Exact post-`set_type` frame: first two rows unchanged; replace only `arg_0` with `slot +0x0c size 0x04 const void *`; added rows `0`, removed rows `0`; `this` is ECX-only. | Define exact half-open range and require the post-definition frame before rename; then rename dry-run/apply, type, and comment. Expected readback is function existence, exact range/name/signature/function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, exact post-type frame, all 69 bytes, zero xrefs, and separate `0xcc` spans. Stop/no-save on any unlisted delta or overlap. |
| I05 `0x00553b10-0x00553b55` size `0x45` | Function existence absent: `stack_frame` returned `vars:null` and `No function found at address 5585680`. Start item `0x00553b10-0x00553b11` is code width `0x01`; current name absent (`""`); declaration/type absent (`""`); address regular absent (`""`); address repeatable absent (`""`); function regular N/A because no function exists; function repeatable N/A because no function exists; zero incoming xrefs. | Define only `[0x00553b10,0x00553b55)`; rename to `Queue_ReadSlot`; set signature `void __thiscall Queue_ReadSlot(Queue *this, void *slot)`; set function regular to `Queue::ReadSlot; copies one fixed-size slot and advances the read index.`; preserve address regular/repeatable and function repeatable as absent (`""`). | Exact post-`define_func` frame: `__saved_registers +0x04 size 0x04 _DWORD`; `__return_address +0x08 size 0x04 _UNKNOWN *`; `arg_0 +0x0c size 0x04 _DWORD`; no local row at `+0x00` and no other rows. Exact post-`set_type` frame: first two rows unchanged; replace only `arg_0` with `slot +0x0c size 0x04 void *`; added rows `0`, removed rows `0`; `this` is ECX-only. | Define exact half-open range and require the post-definition frame before rename; then rename dry-run/apply, type, and comment. Expected readback is function existence, exact range/name/signature/function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, exact post-type frame, all 69 bytes, zero xrefs, and separate `0xcc` spans. Stop/no-save on any unlisted delta or overlap. |
| I06 `0x00553b60-0x00553b6a` size `0x0a` | Function existence absent: `stack_frame` returned `vars:null` and `No function found at address 5585760`. Start item `0x00553b60-0x00553b63` is code width `0x03`; current name absent (`""`); declaration/type absent (`""`); address regular absent (`""`); address repeatable absent (`""`); function regular N/A because no function exists; function repeatable N/A because no function exists; zero incoming xrefs. | Define only `[0x00553b60,0x00553b6a)`; rename to `Queue_IsEmpty`; set signature `bool __thiscall Queue_IsEmpty(const Queue *this)`; set function regular to `Queue::IsEmpty; returns whether read and write indices are equal.`; preserve address regular/repeatable and function repeatable as absent (`""`). | Exact post-`define_func` frame is the sole synthetic row `__return_address +0x00 size 0x04 _UNKNOWN *`; locals `0`, saved-register rows `0`, explicit argument rows `0`, and no other rows. Exact post-`set_type` frame is identical; register-only `this` adds no physical row. Fresh typed frameless comparator `LObject__GetRuntimeClass` at `0x004f4b10` exposes the same sole return row. | Define exact half-open range and require the sole-row frame before rename; then rename dry-run/apply, type, and comment. Expected readback is function existence, exact range/name/signature/function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, same sole-row frame, exact ten bytes, zero xrefs, and separate padding. Stop/no-save on any unlisted delta. |
| I07 `0x00553b70-0x00553c07` size `0x97` | Function exists; current name `sub_553B70`; declaration/type `_DWORD *__thiscall(_DWORD *Block, char)`; address regular absent (`""`); address repeatable absent (`""`); function regular absent (`""`); function repeatable absent (`""`); one vtable data xref. Fresh complete physical frame: `var_C +0x08 size 0x04 _DWORD`; `var_4 +0x10 size 0x04 _DWORD`; `__saved_registers +0x14 size 0x04 _DWORD`; `__return_address +0x18 size 0x04 _UNKNOWN *`; `arg_0 +0x1c size 0x04 int`; no other rows. | Rename to `Queue_scalar_deleting_destructor`; set signature `Queue *__thiscall Queue_scalar_deleting_destructor(Queue *this, unsigned int flags)`; set function regular to `Compiler-generated Queue scalar deleting destructor; source body belongs to Queue::~Queue.`; preserve address regular/repeatable and function repeatable as absent (`""`). | Exact post-`set_type` frame: preserve the four non-argument rows byte-for-byte by name, offset, width, and type; replace only `arg_0 +0x1c size 0x04 int` with `flags +0x1c size 0x04 unsigned int`. Added rows `0`; removed rows `0`; `this` remains ECX-only. | Rename dry-run/apply, then type and comment; expected readback is exact final name/signature/function-regular text, address regular `""`, address repeatable `""`, function repeatable `""`, exact frame, all 151 bytes, one data xref from `0x00622d24`, delete/guard paths, and `0x18` size constant. Stop/no-save on any unlisted delta. |
~~~
