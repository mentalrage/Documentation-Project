# Agent-B009 Current Assignment

Project context: this is a local old-game preservation effort for an unsupported NexusTK client, intended to keep the game runnable through archival compatibility research and faithful source reconstruction.

## UID0000JL Same-Report Gate 1 Formal-C++ And Current-Authority Repair

- Agent: `B009`; preserve `CHATGPT | 5.6 | xHigh`, use the project-level `ntk-b-agent-workflow` skill, and do not spawn subagents.
- Repair in place: `tools/leaser/Agents/Agent-B009/research/0000JL-FrameChrome-file-source-quality.md`.
- Failed exact artifact SHA256: `2DD12C882D7A3F55F03749F070622F4725F964F8727FD70F1086643D562170F6`.
- Preserve valid research, all 33 exact headings, 55 ledger/checklist twins, 52/3 actor allocation, accepted unrelated formal CPP/H, the 27-row destination inventory, and all unaffected evidence. This is an additive repair, not a rewrite.

## Exact Gate 1 Failures

1. The formal reconstructed `BuildFramePartPanes` C++ calls nonexistent `bounds.Set(...)` and `bounds.Offset(...)` members. Replace all five construction/offset pairs with the evidence-backed free helpers `InitRectBounds` at `0x004b7c50` and `OffsetRect` at `0x004b7e10`. Repair the formal CPP and every affected claim/checklist twin together. Do not leave the invalid member-call form anywhere in active authoritative text.
2. Historicalize generated command `000000028021`; it is stale. Re-read the current generated outputs after command `000000028163` and record full exact SHA256 values, not prefixes, for `AboveFrame.cpp`, `AboveFrame.h`, `FramePartPane.cpp`, and `FramePartPane.h`. Preserve the five-emitter/zero-empty-marker/no-`FrameChrome` conclusions only if the physical reread still proves them.
3. Add literal exact immediate response-envelope contracts for both `declare_type` actions, the A08 `define_func` action, and all ten `set_type` actions. Each contract must specify exact row count, allowed top-level and row members, submitted declaration/edit identity, success fields, optional-error handling, and rejection of extra rows/fields/values under the current MCP endpoint shape.
4. Reconcile the manual-coverage receipt ledger against current files. Twenty-five of 27 rows were exact at review; only UID0000YI and UID0000YJ were stale. Re-read those two current rows and repair their receipts/claims without overwriting later compatible work.
5. Repair `Current Target State`, evidence, `IDA Rename / Type / Comment Recommendations`, formal-source reasoning, `Validator Results`, `Changed Files`, `Final Recommendation`, affected Claim And Incorporation Ledger rows, exact checklist twins, and terminal self-audit wherever the failed claims propagated.
6. Remove the terminal marker while repairing and restore exactly one terminal marker only after the complete repaired artifact passes the local self-audit.

## Restrictions And Return Boundary

- Edit only this report. No by-*, generated, coverage, tracker, audit, registry, lifecycle, archive, goal, IDB, or other-report edits.
- IDA/MCP use is read-only. Do not run validators, `execute_report`, or any mutation/save/start/stop/process-management operation.
- Lease the report only while editing and release immediately. End with zero B009 leases.
- Immediately use the status tool: `python .\tools\leaser\status\status.py -report working B009 "UID0000JL formal-C++ and current-authority Gate 1 repair in progress"`; refresh at least every 15 minutes while active.
- On completion use `-report complete` with exact report SHA/shape, all repaired endpoint-contract counts, full generated-output hashes/current command proof, 27/27 current coverage receipt proof, 55/55 twin proof, 52/3 allocation proof, one-marker/zero-script proof, prohibited-action proof, and zero leases. Stop for a wholly fresh Supervisor Gate 1.
