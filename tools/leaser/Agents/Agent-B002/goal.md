# Agent-B002 Current Assignment

Project context: this is a local old-game preservation effort for an unsupported NexusTK client, intended to keep the game runnable through archival compatibility research and faithful source reconstruction.

## UID0000N9 Accepted Ordinary Implementation Callback

- Agent: `B002`; preserve `CHATGPT | 5.6 | xHigh`, use the project-level `ntk-b-agent-workflow` skill, and do not spawn subagents.
- Accepted exact report: `tools/leaser/Agents/Agent-B002/research/0000N9-SayInputPanes-file-source-quality.md`.
- Accepted SHA256: `2E77FCEA4EACD993A3210103D047509CD53CC57B56F06E5638D7C0AEC13DEF6E`.
- Controlling audit: `2026-08-25T19:51:00Z - B002 UID0000N9 Exact-Artifact Gate 1 PASS`.

## Authorized Implementation

1. Implement exactly `C0000N9-001` through `C0000N9-051` from the accepted report. Incorporate all report-level behavior, evidence, ownership, source placement, range/split/padding, current-versus-historical, rejected alternatives, raw/compiler/literal dispositions, scores/metadata, and source-quality detail into every affected target/support by-* document.
2. Create the exact two source-bearing UserPane support pages and exact one non-emitting raw page specified by claims 010, 011, and 041. Let the validator allocate their UIDs; then update the report's current state and exact twins with the assigned identities before checking those claims.
3. Apply every accepted formal CPP/H payload to the intended emitter channels, including guarded `SayInputPanes.h`, 18 namespace-scope pane definitions, exact UserPane enum plus ten declarations/definitions, corrected Event/submit names, and direct include dependencies. Do not leave illustrative/example C++ outside formal channels.
4. Preserve accepted existing packet bodies without compressing or reformatting away details. Historicalize stale source names, split assumptions, generated receipts, and Wave3 migration prose rather than silently deleting useful history.
5. Lease each ordinary file only immediately before editing and release it immediately afterward. If another agent holds a needed file, continue with another accepted destination and return later; never edit through a conflicting lease.
6. Do not edit any `-coverage-report.md`; claims `C0000N9-052` through `C0000N9-055` remain supervisor-owned. Do not check `C0000N9-052` through `C0000N9-098`.
7. Run exact scoped validation from `source-3/project-documentation` for every changed by-* destination: `python .\tools\validator.py --mode file --file <relative-by-star-path> --apply --queue-timeout 240`; add `--wait-generated` to the final relevant validation as accepted by the report.
8. Physically reread the final validator-owned SayInputPanes CPP/H and UserPane CPP/H. Require the report's exact definition/declaration/include/empty-marker/stale-name acceptance inventory, record command/timestamp/hash/freshness receipts, and leave any mismatched claim unchecked.
9. Update this report additively with callback receipts and check exactly the 51 authorized B002 twins only after each is physically complete. Preserve 98 claim identities/order, four supervisor coverage rows, 43 supervisor Gate 2B rows, and one terminal marker.

## Restrictions And Status

- Do not mutate IDA. Do not edit generated files directly, coverage reports, audit/catalog/tracker/registry/lifecycle files, goals, or other reports. Never run `execute_report` or any lifecycle/probing variant.
- Immediately run `python .\tools\leaser\status\status.py -report working B002 "UID0000N9 accepted ordinary implementation in progress"` before any edit. Refresh `working` within every 15 minutes while active.
- On completion, release every lease, then run `python .\tools\leaser\status\status.py -report complete B002 "UID0000N9 ordinary implementation ready for Gate 2A; <exact report SHA/mechanics, allocated UIDs, changed paths, validators/generated receipts, 51 checked/47 unchecked, zero leases>"` and stop for supervisor verification.
