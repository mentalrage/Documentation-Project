# Removed Operational Persistence Package

Source report: [0000OP-TextMenuDialogs-file-source-quality.md](0000OP-TextMenuDialogs-file-source-quality.md)

This companion is inert and non-authoritative. It must never be executed and must never be submitted to `execute_report`. It is not a B report and receives no Gate 1, Gate 2, coverage, score, IDA, validator, audit, catalog, or lifecycle credit.

Only operational material removed from failed source report SHA256 `ECFF0E1F51319A08FF2127954616062ED08713BDC5F824C643134E65A47A7B75` is preserved below. Every `text` block is verbatim; headings outside the blocks identify extraction locations only.

## Finalized Recommendation Dependency

```text
- **Current Gate 2B runtime/persistence contract:** every fresh package independently requires literal top-level `schema_version:1`, `ok:true`, `errors:[]` and worker-local `schema_version:1`, `ok:true`, `errors:[]`; the listener has no local status/error fields and is instead closed by role, freshness, nonce, endpoint/port ownership, process/parent/generation, source root, manifest, and modules. Each listener and worker role is sealed only against its own accepted loaded-module baseline; cross-role equality is invalid. Fresh public `tools/list` proof, five non-reusable database roles, the exact sole save, and separate physical receipts close verifier, promotion, final, rollback, and BACKUP-only restore phases. Physical namespace transitions use the declarative supported `POWERSHELL_FILE_REPLACE_V1` protocol: a pre-IDB disposable capability gate, exclusive `CreateNew` candidates written with `FileOptions.WriteThrough` and `Flush(true)`, one direct same-volume `[System.IO.File]::Replace` call, closed exclusive probes, and two agreeing independent fresh-process namespace receipts. Directory-handle `FlushFileBuffers` and the policy-blocked custom harness are neither invoked nor credited.
```

## Supervisor Active Recheck Dependency

```text
- Gate 2B must use literal top-level and worker-local schema/success/empty-error checks without inventing listener-local status fields, role-specific module baselines, exact current public tools/save schemas, and five pairwise-distinct one-use roles: mutation `DB`, saved-TX `DB_VERIFY`, promoted-canonical `DB_FINAL`, failed-canonical `DB_ROLLBACK`, and restored-canonical `DB_RESTORE`. Before BACKUP/TX creation or IDB open, `POWERSHELL_FILE_REPLACE_V1` must pass its disposable exact-parent capability gate. Promotion and BACKUP-only restore then require retired routes, separately sealed `CreateNew` candidates written with `FileOptions.WriteThrough` plus `Flush(true)`, all handles closed, exclusive reopen/path-size-SHA and collision/route/reparse/alias checks, one direct fresh-process same-volume `[System.IO.File]::Replace` call, two agreeing fresh-process namespace receipts, and zero residue. The access-denied directory flush and policy-blocked custom harness are not requested or credited; unavailability/access denial/policy blocking of any selected direct primitive hard-stops at the pre-IDB capability gate with CANON unchanged. The report grants no replacement authority.
```

## C084 Ledger Row

```text
| C084 | 0000OP | Enforce Section 21 P07 for every A01-A22 R/I/F/D chain and A23-A26 comment endpoint, including exact-address persisted-name/type proof and unchanged protected state. Every fresh public package requires exactly one `runtime_attestation`, no `runtime/attest`, current `idb_save({database,path})->{ok,error?,path}`, literal top-level and worker-local `schema_version:1`/`ok:true`/`errors:[]`, no invented listener-local status/errors, complete listener closure, and role-specific module baselines without cross-role equality. Use five pairwise-distinct one-use roles with no session/route/package reuse: mutate and perform the sole exact save only through `DB`; verify saved TX read-only through fresh `DB_VERIFY`; verify promoted CANON through fresh read-only `DB_FINAL`; classify any failed/ambiguous promoted CANON through fresh read-only `DB_ROLLBACK`; and verify BACKUP-restored CANON through fresh read-only `DB_RESTORE`. Before BACKUP/TX creation or IDB open, pass the declarative `POWERSHELL_FILE_REPLACE_V1` disposable capability gate in CANON's sealed exact parent: direct `FileStream` `CreateNew`/`FileShare.None`/`FileOptions.WriteThrough`, `Flush(true)`, close, and exactly one fresh-process `[System.IO.File]::Replace(source,destination,null,true)` must be available and must produce two agreeing fresh-process closed namespace receipts while CANON stays unchanged. Promotion and restoration independently require never-used collision-failing same-parent/same-volume one-link candidates copied only from verified saved TX or freshly reverified sealed BACKUP, complete anti-alias/hardlink/ADS/case-fold/reparse checks, closed exclusive reopen/path-size-SHA proof, exactly one direct `File.Replace` that atomically consumes the candidate while preserving TX/BACKUP, two agreeing post-call namespace receipts, independent CANON reopen/hash, and zero candidate/temporary/second-artifact residue. Directory-handle `FlushFileBuffers` access denial and the policy-blocked custom harness are classified `DIRECTORY_FLUSH_UNAVAILABLE` and receive no success credit. If direct `FileStream`, `Flush(true)`, `File.Replace`, closed receipt, or namespace receipt is unavailable, access-denied, policy-blocked, mismatched, or ambiguous, the disposable gate hard-stops before BACKUP/TX, IDB open, mutation, or save with CANON unchanged and no rename/copy-overwrite/custom-harness/hash-only fallback. A post-promotion exception/ambiguity requires `DB_ROLLBACK` classification then BACKUP-only restoration; a restore exception/ambiguity preserves the failed-state receipts and hard-stops without retry. Runtime proof never substitutes for separate physical receipts, and this report grants no promotion/restore authority. | very-high | literal row-local prestates, current-schema persisted-name proofs, schema-accurate role-specific runtime/tools-list proof, five-role isolation, exact save response, pre-IDB supported-primitive gate, write-through closed candidates, atomic `File.Replace`, independent namespace/file receipts, complete semantic readbacks, retired routes, no-residue proof, and separate physical receipts | `IDA protection P07` | incorporate | proposed |
```

## Operational Gate 2B And Persistence Package

```text
This is a structured supervisor handoff, not an executable transaction package. Bind the exact normalized canonical IDB as `CANON` and seal its closed-file/namespace receipt first. Before creating `BACKUP` or `TX`, opening any IDB, or starting any mutation worker, pass the disposable same-parent `POWERSHELL_FILE_REPLACE_V1` capability gate defined below. Only after that gate passes may the supervisor create collision-safe sealed original-byte `BACKUP` and separately receipted `TX` through the protocol's exclusive write-through creation primitive and verify distinct regular-file identities plus exact physical size/SHA256 receipts with `BACKUP == CANON` and initial `TX == CANON`. Then call literal `idb_open({input_path:TX,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,idle_ttl_sec:600,preferred_session_id:"supervisor_uid0000op_gate2b_<UTC>"})`. Require `success:true`, returned `session.input_path == TX`, and a nonempty returned `session.session_id`. The preferred ID is only a request; bind only the returned `session.session_id` as `DB`. Every later request for this mutation role uses that exact returned `DB`; `idb_list`, health, or a requested preferred ID cannot substitute for the runtime-returned binding.

The mandatory current runtime proof is the public read-only `runtime_attestation({expected_database:ROLE_DB,expected_canonical_path:ROLE_PATH})` tool, where the phase supplies the exact newly returned role/session and exact normalized path. Before every applicable attestation, a fresh complete public `tools/list` result must prove exactly one exact-name public `runtime_attestation`, no public/internal/alias `runtime/attest`, and exactly one current public save schema `idb_save({database:DB,path:TX}) -> {ok,error?,path}` with required database and explicit path support. Each attestation independently requires literal top-level `schema_version:1`, `ok:true`, and present array `errors:[]`; the selected worker independently requires literal worker-local `schema_version:1`, `ok:true`, and present array `errors:[]`. Missing, null, false, malformed, non-array, or nonempty top-level or worker-local status/error fields hard-stop. The listener has no listener-local `schema_version`, `ok`, or `errors`; never require or synthesize those nonexistent fields. Instead require exact listener role, fresh timestamp/nonce, endpoint and operating-system port ownership, PID/process/parent/generation, source root, role manifest, and complete loaded modules. Require the exact active worker route to identify the phase session/path, worker endpoint/PID/port ownership, supervisor-spawned parent route, generation/source root/role manifest/modules, and no unbound or ambiguous active stateful worker. Seal the listener and worker independently against phase-specific accepted role baselines; their module sets and implementation manifests differ by design, so cross-role equality is invalid and must never be tested. Any missing field, partial response, PID/port mismatch, nonce/generation ambiguity, session/path/parent-route mismatch, unexplained same-role baseline delta, unbound worker, wrong tools schema, duplicate/missing `runtime_attestation`, or exposed `runtime/attest` hard-stops for supervisor repair.

Runtime binding is not a startup-only check. Repeat a fresh complete public `tools/list` plus the complete schema-accurate `runtime_attestation` package with the exact active role's newly returned session and normalized path before every A01-A22 rename and A23-A26 comment mutation and after its complete readback; immediately before and after the sole save; immediately post-open, pre-read, and post-read for `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE`; and again at pre-promotion, rollback-classification, restore, and restored-disk boundaries. Each repetition must have a fresh timestamp/nonce and that phase's own accepted listener and worker baselines. Stop if any package is absent, stale, partial, changed without an explained supervisor-owned role transition, or inconsistent with the exact boundary. Runtime proof never carries physical size/SHA256, and cross-role manifest inequality is expected rather than a failure.

`runtime_attestation` proves runtime/session/path/process/route/module identity and freshness only; it does not supply physical IDB byte counts or SHA256. Record canonical, collision-safe backup, initial `TX`, post-save `TX`, verifier input/output, promotion/restore candidates, rollback/restored disk when applicable, and final disk byte count/SHA256 as separate closed filesystem receipts at their exact boundaries; every namespace transition additionally requires two agreeing fresh-process closed namespace receipts. The sole save request is exactly `idb_save({database:DB,path:TX})`; require response `ok:true`, `error` absent or empty, and returned normalized `path` exactly equal to normalized `TX`. Immediately after that response, obtain a separate physical post-save `TX` size/SHA256 receipt. Neither attestation nor the save response may substitute for any physical or namespace receipt, and no implicit/default-path or alternate save request is authorized.

`DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are five pairwise-distinct one-use role labels. Each must bind only a newly returned nonempty session ID from a separate fresh `idb_open`, plus its own route/port/ownership row and fresh phase-specific listener/worker baselines and runtime packages. No identifier, worker route, port, ownership row, baseline, cached package, or role may be reused, substituted, aliased, or promoted between roles, even when roles open the same physical path. Only `DB` has mutation and sole-save authority; all four later roles are operationally read-only, meaning no mutation, cache-mutation, save, promotion, restore, or replacement endpoint may be invoked through them.

### Supported POWERSHELL_FILE_REPLACE_V1 persistence protocol

`POWERSHELL_FILE_REPLACE_V1` is declarative supervisor handoff, not report-embedded automation. Its exact supported file primitive is direct PowerShell/.NET `System.IO.FileStream` creation with `FileMode.CreateNew`, `FileAccess.Write`, `FileShare.None`, and `FileOptions.WriteThrough`, followed by complete-byte/EOF checks, `Flush(true)`, disposal, and a closed exclusive reopen probe. Its exact namespace primitive is one direct `[System.IO.File]::Replace($source,$destination,$null,$true)` call from a fresh PowerShell process; on Windows this is same-volume `ReplaceFileW` through .NET. The null backup argument forbids an implicit side backup, `ignoreMetadataErrors:true` prevents metadata-copy errors from selecting another operation, and candidates copied from TX/BACKUP keep those sealed sources unchanged. No directory-handle `FlushFileBuffers`, custom write-through harness, rename, delete-then-move, copy-overwrite, cross-volume move, retry, or fallback API is authorized.

Seal CANON's parent by normalized/final resolved path, volume identity, and directory file identity. For CANON, BACKUP, TX, every probe, and every candidate, reject a reparse point, symlink, junction, mount point, alternate data stream, hardlink count other than one, short/long-name alias, case-fold alias, namespace alias, path redirection, or any identity shared with another protocol role. A `CLOSED_FILE_RECEIPT` is taken only after all producing handles/processes close and records normalized/final path, exact sealed-parent identity, volume, regular-file/reparse attributes, file identity, hardlink names/count, streams, byte count, UTC write time, SHA256, and successful read-only `FileShare.None` reopen/close. A `CLOSED_NAMESPACE_RECEIPT` comes from a fresh independent PowerShell process and records the sealed parent/volume, case-insensitive exact-name enumeration and a fresh file receipt for every present protocol role. Two consecutive independent fresh-process namespace receipts must agree before a namespace state is accepted.

Before BACKUP/TX creation or any IDB open, choose never-used nonce-bearing `PROBE_SOURCE` and `PROBE_DESTINATION` names in the sealed parent, prove both absent case-insensitively, create each once with distinct nonce bytes through direct `CreateNew`/write-through/flush/close, and seal both receipts. A fresh process invokes exactly one direct `File.Replace(PROBE_SOURCE,PROBE_DESTINATION,null,true)`. Two agreeing fresh-process namespace receipts must prove source absent, destination equal the former source size/SHA, fresh regular one-link identity, parent/volume unchanged, and CANON unchanged. After exact identity authorization, direct `File.Delete` may remove only that disposable destination; two more agreeing receipts must prove both probe names absent and CANON unchanged. Any primitive exception, access denial, policy block, collision, alias/reparse/hardlink/ADS defect, exclusive-open failure, unexpected source survival, receipt disagreement, CANON drift, or probe residue is `POWERSHELL_FILE_REPLACE_V1_UNAVAILABLE` and hard-stops before BACKUP/TX, `idb_open`, mutation, or save. The observed directory-handle `Access is denied` and custom-harness policy block are classified `DIRECTORY_FLUSH_UNAVAILABLE`, receive no success credit, and select this supported protocol because neither rejected operation is requested. If this protocol's direct primitive is itself unavailable, there is no rename, copy-overwrite, custom-harness, hash-only, cleanup-and-retry, or investigate-later branch.

Promotion and restoration apply the protocol independently; success in one phase grants no authority to the other. Each uses a never-used nonce-bearing exact-parent/same-volume candidate proven absent case-insensitively and created once with collision-failing no-overwrite semantics only from verified saved TX or freshly reverified sealed BACKUP. Require a distinct regular one-link file identity, no reparse/ADS/alias, exact source byte count/SHA, write-through `Flush(true)` closure, successful exclusive reopen, and two agreeing fresh-process namespace receipts. An existing/collided pathname is never overwritten, deleted, recycled, or retried. A pre-call failure may delete only the exact receipt-bound unconsumed candidate after identity authorization, then requires two agreeing receipts proving candidate absence and unchanged CANON/BACKUP; cleanup never authorizes a new attempt. Each phase permits exactly one guarded fresh-process `File.Replace(CANDIDATE,CANON,null,true)` while immediate CANON/source/BACKUP receipts equal the authorized prestate. Post-call receipts must prove candidate consumed/absent, CANON equals authorized candidate bytes, TX/BACKUP unchanged, and no case variant, temporary replacement residue, alias, or second candidate. A promotion exception or ambiguity is a failed promoted state requiring `DB_ROLLBACK`; a restore exception or ambiguity preserves the failed-state receipts and hard-stops without retry or fallback.

After the sole successful save and separate saved-TX receipt, pass the fresh post-save `DB` package, then retire the mutation session/worker route through the current supervisor-owned route-retirement mechanism and prove its session, worker, route, and handles absent. Freshly open exact saved `TX`, bind only its newly returned session as `DB_VERIFY`, and prove exact TX path plus independent role ownership and its own accepted listener/worker baselines. Require fresh complete post-open, pre-read, and post-read packages; separate TX physical receipts immediately before and after readback equal the saved-TX receipt; and complete persisted readback of every expected A01-A26 action plus operative P01/P02/P04/P07 protection and P03/P05/P06 no-action disposition. `DB_VERIFY` grants no mutation, save, promotion, restore, or replacement authority. Retire it and prove its route/session/handles absent before candidate creation.

Immediately before promotion, prove `DB` and `DB_VERIFY` retired; no mutation/verifier/canonical/TX worker route or open handle remains; `CANON` and sealed `BACKUP` still equal their original receipts; and closed `TX` still equals its saved receipt. Only then apply `POWERSHELL_FILE_REPLACE_V1` to one never-used `PROMOTION_CANDIDATE` created solely from verified saved-TX bytes. Require all exact-parent/same-volume anti-alias checks, write-through `CreateNew`/`Flush(true)`/closure, independent exclusive reopen/path-size-SHA receipt, and two agreeing namespace receipts while CANON/BACKUP remain original and routes remain retired. From a new process perform the single direct `File.Replace(PROMOTION_CANDIDATE,CANON,null,true)` against the immediately rechecked original CANON receipt. Two agreeing fresh-process namespace receipts and an independent CANON reopen/hash/close must prove candidate consumption/absence, CANON equal saved TX, unchanged TX/BACKUP, and zero alias/residue/second candidate. A pre-call candidate failure follows only the bound-candidate cleanup branch and never attempts promotion; any call exception, receipt ambiguity, candidate survival, partial/unknown replacement, or BACKUP drift is a failed promoted state requiring `DB_ROLLBACK`. This report grants no promotion or replacement authority.

After normal promotion, freshly open exact `CANON` operationally read-only and bind only its new session as `DB_FINAL`. Require its own listener/worker baselines and fresh complete post-open, pre-read, and post-read packages, separate CANON receipts before and after readback equal saved TX, a fresh sealed-BACKUP receipt equal the original baseline, and complete expected A01-A26 semantic plus P01-P07 protection/exclusion readback. Retire `DB_FINAL` and prove its session/route/handles absent. Gate 2B normal success exists only after this final semantic, runtime, physical, role-isolation, and retirement closure.

Any failure or ambiguity at or after promotion first retires failed `DB_FINAL` and every failed/ambiguous canonical or TX route and proves no mutation/replacement-capable route or handle remains. Obtain a failed-CANON physical receipt, freshly open exact failed/ambiguous `CANON` operationally read-only, and bind only its new session as `DB_ROLLBACK`, with its own listener/worker baselines and fresh complete post-open, pre-read, and post-read packages. Read and explicitly classify every affected A01-A26 action and P01-P07 protection/exclusion as expected saved state, original baseline, or failed/ambiguous other; missing or unreadable is failed/ambiguous other, never acceptance. Separate CANON receipts before and after classification must match, proving read-only classification. `DB_ROLLBACK` has no mutation, cache-mutation, save, promotion, restore, or replacement authority. Retire it and prove its route/session/handles absent before any restore.

If restoration is required, first retire every canonical/TX/rollback route, prove no worker/session/handle claims CANON or the restore path, preserve the CANON failed-state receipt across `DB_ROLLBACK`, and freshly reverify sealed `BACKUP` exactly equals its original receipt. Apply `POWERSHELL_FILE_REPLACE_V1` independently to one never-used `RESTORE_CANDIDATE` created solely from sealed BACKUP bytes. Require all exact-parent/same-volume anti-alias checks, write-through `CreateNew`/`Flush(true)`/closure, independent exclusive reopen/path-size-SHA receipt, and two agreeing namespace receipts proving candidate equals BACKUP. From a new process perform the single direct `File.Replace(RESTORE_CANDIDATE,CANON,null,true)` only while CANON still equals the classified failed-state receipt. Two agreeing fresh-process namespace receipts and an independent CANON reopen/hash/close must prove candidate consumption/absence, CANON equal BACKUP, unchanged BACKUP/TX, and zero alias/residue/second restore artifact. Never restore from TX, failed CANON, a promotion candidate, an open handle, or any source other than sealed BACKUP. A collided path is never cleaned/reused; a call exception or receipt ambiguity preserves classification/physical receipts and hard-stops without candidate recreation, retry, copy/delete, or alternate API. Freshly open restored CANON operationally read-only, bind only its new session as `DB_RESTORE`, require its own listener/worker baselines and fresh complete post-open, pre-read, and post-read packages, and read every A01-A26/P01-P07 original baseline. Separate pre/post CANON receipts must equal BACKUP, which remains sealed. Retire `DB_RESTORE` and prove no active canonical mutation route remains. This is failed/recovered disposition, not normal Gate 2B success.
```

## Row-Local Startup Wrapper

```text
After startup and before mutation, use `DB` for an immediate bounded preflight of every literal A01-A26 and P01-P07 prestate. The exact public R request is `rename({database:DB,batch:{func:{addr:"0xADDRESS",name:"NEW_NAME"},pure:true,allow_overwrite:false,dry_run:false,stop_on_error:true}})`. Run one row at a time in A01-A22 order; each row is four serialized stages: R (pure rename), I (immediate exact-address persisted-state inspection), F (exact-address `force_recompile`), then D (fresh exact-address `decompile`).
```

## P07 Operational Dependency

```text
| P07 | After each A01-A22 R stage, immediately perform that row's literal exact-address I request before any cache refresh. Require the requested persisted name and exact entry address, then reread unchanged public type, full frame, AR/AP/FR/FP, range/hash, and separate inbound code/data sets/counts; only the name may change in persisted state, while the explicitly non-authoritative immediate cache presentation may remain old or become new. Then perform only the row's exact-address F request and require matching returned address/no error/no persisted delta; then perform only the row's exact-address D request and require the literal fresh declaration, with only A14/A21's stated A11 callee-token refresh additionally permitted. After each A23-A26 call, reread that row's literal raw range/hash, absent name/type/frame, AR/AP/FR/FP, and separate inbound code/data sets/counts; AP is the sole permitted delta. Apply independently literal P01/P02/P04 final readbacks. Gate 2B persistence additionally requires the pre-IDB `POWERSHELL_FILE_REPLACE_V1` capability gate; five non-reused roles; exact save/DB_VERIFY; write-through closed `CreateNew` candidates; one direct same-volume `File.Replace` per promotion/restore phase; two agreeing fresh-process namespace receipts; complete DB_FINAL/DB_ROLLBACK/DB_RESTORE readbacks; and zero residue. No outbound-xref, vtable-cell, neighbor, unspecified baseline, unavailable directory flush, or blocked custom harness receives success credit. | Stop at the first wrong persisted name/address, protected-state delta, stale post-refresh declaration, endpoint error, unlisted cache/body delta, direct-primitive unavailable/access-denied/policy-blocked/ambiguous result, physical/namespace receipt mismatch, alias/collision, role reuse, or candidate residue. A capability failure stops before BACKUP/TX or IDB open; a promotion ambiguity proceeds only to DB_ROLLBACK/BACKUP restore; a restore ambiguity hard-stops without retry. Do not continue, save, compensate, infer, or use a fallback. |
```

## Operational Dependency Order

```text
Dependency order is sealed CANON receipt and successful disposable pre-IDB `POWERSHELL_FILE_REPLACE_V1` capability gate; write-through/closed/twice-observed BACKUP and TX creation; fresh mutation `DB` binding/package/preflight; A01-R/I/F/D through A22-R/I/F/D; A23-A26; complete P01-P07 disposition; the re-attested sole `idb_save({database:DB,path:TX})`; exact save-response and separate saved-TX receipt; mutation-route retirement; fresh read-only `DB_VERIFY` post-open/pre-read/post-read packages, complete persisted A/P readback, before/after TX receipts, and retirement; pre-promotion CANON/BACKUP/TX and route proof; one closed collision-failing same-parent `PROMOTION_CANDIDATE` from saved TX; one guarded direct fresh-process `File.Replace`; two agreeing namespace receipts, independent CANON rehash and zero residue; then fresh read-only `DB_FINAL` post-open/pre-read/post-read packages, complete expected A/P readback, CANON/BACKUP physical proof, and retirement. Any promotion/final ambiguity instead requires failed-route retirement, fresh read-only `DB_ROLLBACK` packages and complete saved/original/other A/P classification, retirement, then BACKUP-only restore through one unique same-parent `RESTORE_CANDIDATE` under the same write-through close/reopen/receipt, one-attempt `File.Replace`, twice-observed namespace, source-preservation, and zero-residue rules. Fresh read-only `DB_RESTORE` then receives its packages, complete original-baseline A/P readback, physical proof, and retirement. Directory-level flush denial is `DIRECTORY_FLUSH_UNAVAILABLE`, not success; if any selected direct primitive is unavailable, access-denied, policy-blocked, mismatched, or ambiguous, the exact phase fails closed under the capability/promotion/restore branch with no fallback. No database role or route/package/baseline may be reused or substituted, runtime proof never substitutes for physical/namespace receipts, and this report grants no promotion/restore authority. P03/P05/P06 remain deterministic no-action exclusions. The handoff still contains 26 logical mutation rows, 88 named serialized R/I/F/D endpoint calls for A01-A22, four one-item address-comment calls, four operative protection/readback rows P01/P02/P04/P07, and three no-action exclusions P03/P05/P06.
```

## Final Recommendation Dependency

```text
- Responsibility split is fixed: C006/C009/C010/C028/C051/C127 remain proposed until an explicitly authorized ordinary naming callback and generated readback complete; IDA, manual coverage, gate decisions, and lifecycle processing remain supervisor-owned. The previously unproven endpoints, helper disposition, protection scope, dependent formal sources, and supported fail-closed `POWERSHELL_FILE_REPLACE_V1` handoff are literal in Sections 14, 21, and 22. This report is ready only for fresh Gate 1, not execution.
```

## Score Dependency

```text
- The supervisor-only persistence handoff does not change either source score: `POWERSHELL_FILE_REPLACE_V1` is proposed protection for later Gate 2B, and its disposable capability gate must pass before BACKUP/TX or any IDB open. Directory-flush denial receives no success credit; direct-primitive unavailability/access denial/policy blocking is an exact pre-mutation hard stop rather than a score inference.
```

## Open-Question Dependency

```text
| Windows namespace persistence when directory `FlushFileBuffers` is denied? | observed access denial, policy-blocked custom harness, supported .NET `FileStream` write-through/`Flush(true)` and same-volume `File.Replace`, closed exclusive probes, disposable pre-IDB replacement test, and two independent fresh-process namespace receipts | resolved by fail-closed `POWERSHELL_FILE_REPLACE_V1`; the unsupported operations are not invoked or credited, while unavailability/access denial/policy blocking of a selected direct primitive stops before BACKUP/TX, IDB open, mutation, or save with CANON unchanged and no fallback |
```

## Follow-Up Dependency

```text
5. Gate 2B is supervisor-owned and follows Section 21 exactly. Every boundary requires fresh `tools/list` proof of exactly one `runtime_attestation`, no `runtime/attest`, and current exact `idb_save` schema, plus literal top-level and worker-local `schema_version:1`/`ok:true`/`errors:[]`; listener-local status/errors do not exist, while listener identity/freshness/nonce/endpoint/port/process/parent/generation/source-root/manifest/modules and each role-specific listener/worker baseline remain mandatory without cross-role equality. Before BACKUP/TX or any IDB open, pass the disposable `POWERSHELL_FILE_REPLACE_V1` capability gate. Mutate and perform the sole exact save only through fresh `DB`, take the separate saved-TX receipt, retire it, then use distinct read-only `DB_VERIFY`. Promotion and BACKUP-only restoration independently use a never-used exact-parent/same-volume `CreateNew` candidate, `FileOptions.WriteThrough` plus `Flush(true)`, closed exclusive receipt/rehash, one direct fresh-process `File.Replace`, two agreeing fresh-process namespace receipts, source preservation and zero residue. Normal closure uses distinct read-only `DB_FINAL`; post-promotion failure uses distinct classification-only `DB_ROLLBACK` before BACKUP restore; restored closure uses distinct read-only `DB_RESTORE`. The access-denied directory flush and policy-blocked custom harness are neither invoked nor credited. If a selected direct primitive or receipt is unavailable, access-denied, policy-blocked, mismatched, or ambiguous, the capability/promotion/restore branch hard-stops exactly as Section 21 states with no retry or fallback. All five roles and their sessions/routes/packages/baselines are non-reusable, and this report grants no replacement authority. Only after normal `DB_FINAL` success may the supervisor perform authorized manual coverage and lifecycle work.
```

## Confidence Dependency

```text
- The later supervisor-only persistence protocol is concrete but still proposed: capability failure is observable before mutation, namespace transitions require twice-observed closed receipts, and no unavailable directory-flush success is inferred. It changes neither the current `93/93` nor post-callback `94/94` source assessment.
```

## Changed-Files Dependency

```text
- This additive Gate 1 repair changes only this report: it makes C006/C009/C010/C028/C051/C127 proposed in both exact twins, recomputes allocation to 83 checked/45 unchecked, removes the premature terminal marker, and replaces the unavailable directory-flush contract with `POWERSHELL_FILE_REPLACE_V1`. It performs no ordinary callback, validator, generated refresh, coverage edit, IDA action, lifecycle action, or archive/execute operation.
```

## C084 Checklist Row

```text
| [ ] | C084 | 0000OP | Enforce Section 21 P07 for every A01-A22 R/I/F/D chain and A23-A26 comment endpoint, including exact-address persisted-name/type proof and unchanged protected state. Every fresh public package requires exactly one `runtime_attestation`, no `runtime/attest`, current `idb_save({database,path})->{ok,error?,path}`, literal top-level and worker-local `schema_version:1`/`ok:true`/`errors:[]`, no invented listener-local status/errors, complete listener closure, and role-specific module baselines without cross-role equality. Use five pairwise-distinct one-use roles with no session/route/package reuse: mutate and perform the sole exact save only through `DB`; verify saved TX read-only through fresh `DB_VERIFY`; verify promoted CANON through fresh read-only `DB_FINAL`; classify any failed/ambiguous promoted CANON through fresh read-only `DB_ROLLBACK`; and verify BACKUP-restored CANON through fresh read-only `DB_RESTORE`. Before BACKUP/TX creation or IDB open, pass the declarative `POWERSHELL_FILE_REPLACE_V1` disposable capability gate in CANON's sealed exact parent: direct `FileStream` `CreateNew`/`FileShare.None`/`FileOptions.WriteThrough`, `Flush(true)`, close, and exactly one fresh-process `[System.IO.File]::Replace(source,destination,null,true)` must be available and must produce two agreeing fresh-process closed namespace receipts while CANON stays unchanged. Promotion and restoration independently require never-used collision-failing same-parent/same-volume one-link candidates copied only from verified saved TX or freshly reverified sealed BACKUP, complete anti-alias/hardlink/ADS/case-fold/reparse checks, closed exclusive reopen/path-size-SHA proof, exactly one direct `File.Replace` that atomically consumes the candidate while preserving TX/BACKUP, two agreeing post-call namespace receipts, independent CANON reopen/hash, and zero candidate/temporary/second-artifact residue. Directory-handle `FlushFileBuffers` access denial and the policy-blocked custom harness are classified `DIRECTORY_FLUSH_UNAVAILABLE` and receive no success credit. If direct `FileStream`, `Flush(true)`, `File.Replace`, closed receipt, or namespace receipt is unavailable, access-denied, policy-blocked, mismatched, or ambiguous, the disposable gate hard-stops before BACKUP/TX, IDB open, mutation, or save with CANON unchanged and no rename/copy-overwrite/custom-harness/hash-only fallback. A post-promotion exception/ambiguity requires `DB_ROLLBACK` classification then BACKUP-only restoration; a restore exception/ambiguity preserves the failed-state receipts and hard-stops without retry. Runtime proof never substitutes for separate physical receipts, and this report grants no promotion/restore authority. | very-high | literal row-local prestates, current-schema persisted-name proofs, schema-accurate role-specific runtime/tools-list proof, five-role isolation, exact save response, pre-IDB supported-primitive gate, write-through closed candidates, atomic `File.Replace`, independent namespace/file receipts, complete semantic readbacks, retired routes, no-residue proof, and separate physical receipts | `IDA protection P07` | incorporate | proposed |
```
