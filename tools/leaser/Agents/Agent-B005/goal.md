# Agent-B005 Current Assignment

## UID0000NR SnowingLayerPane Whole-File Report-Only Research

Project context: this is a local old-game preservation effort for an unsupported NexusTK client, intended to keep the game runnable through archival compatibility research and faithful source reconstruction.

- Agent identity: `B005`.
- Model/thinking requirement: `CHATGPT | 5.6 | xHigh`.
- Required project skill: `ntk-b-agent-workflow`.
- Primary target: [UID:0000NR] `by-file/SnowingLayerPane.md`.
- New report path: `tools/leaser/Agents/Agent-B005/research/0000NR-SnowingLayerPane-file-source-quality.md`.
- Queue authority: current `auto-generated/-ag-research-tracker.md` `## by-file`; the reconstructable uncovered bucket is empty, and UID0000NR is the first eligible uncovered blank-reconstructability row at `90/89` with zero direct/additional reports.

## Required First Phase

Produce a new report-only whole-file research report. Do not edit target/support by-* documents until the Supervisor completes fresh Gate 1 review and sends an explicit implementation callback.

1. First run `python .\tools\leaser\status\status.py -report working B005 "UID0000NR SnowingLayerPane whole-file report research started"` from `source-3/project-documentation`. Refresh official `working` status within every 15 minutes while active. On completion, report `complete` with exact report path/hash/bytes/LF/headings/twins/allocation/marker/fence/lease proof and stop for Supervisor review.
2. Read this goal, the `ntk-b-agent-workflow` skill, and its B-agent research/implementation reference before research. Agent-specific instructions here override general defaults only where they explicitly differ.
3. Treat this as exhaustive whole-file research. Establish the complete in-scope Snowing source inventory: authored functions/methods, constructor/destructor forms, compiler-generated wrappers/thunks, vtable/RTTI/data/literals/local-static descriptors, child/support pages, exact source order, and all exclusions. Do not stop after representative methods.
4. Use live read-only IDA MCP evidence against the canonical NexusTK IDB. Resolve every open question as far as direct evidence or high-probability inference allows. If MCP becomes unavailable or repeatedly fails, stop MCP-dependent work and inform the Supervisor; do not start/stop/restart MCP or finish from fallback-only evidence.
5. The main current ambiguity must be actively investigated and resolved: whether authored Snowing code belonged in a dedicated historical `SnowingLayerPane.cpp/.h`, the shared `WeatherLayerPane.cpp/.h`, or another source arrangement. Reconcile UID0000NR with UID0000P8, UID0000DC, UID0001O0, UID0001O1, UID0003O0-UID0003O4, UID0001OO, UID0001Q4, UID0000TP, UID0001WJ, MapPane construction/callers, vtable/RTTI ownership, physical neighborhoods, source ordering, and generated/current documentation. A statement that provenance or aliases merely need future investigation is not acceptable; perform that investigation now and provide a ranked best-supported conclusion with positive, negative, and rejected-alternative evidence.
6. Investigate all heuristic/source-quality issues encountered, including compiler-generated names, human source-facing names, types, fields, globals, helpers, inheritance, calling conventions, object layout, local/static data, reachability, ownership, emitter routing, split/range/padding, and CPP/H placement. Supply best-probability human names and source shape rather than leaving final recommendations as `sub_`, `dword_`, `func_`, raw addresses, or decompiler artifacts.
7. Preserve exact runtime behavior while reconstructing plausible late-1999 through mid-2000s human-written C++. If any first-draft/example C++ is recommended, place it only in the report's formal CPP/H channels with complete reasoning and correct emitter boundaries. Do not place C++ in prose as an alternative emission surface.
8. Produce all required B-report headers, exactly 33 required H2 sections, positive and negative evidence, direct xref/caller and exhaustive inventory tables, ownership/source-placement/range analysis, open questions with attempted resolution, score/metadata recommendations, exact ordinary-document changes, structured one-endpoint IDA recommendation rows when justified, validator/coverage handoffs, claim ledger, exact ordered implementation-checklist twins, and exactly one terminal readiness marker.
9. Score improvement is an assignment outcome, not optional commentary. Research the evidence currently capping UID0000NR and associated in-scope items; justify all resulting scores. Rare irreducible uncertainty must have exact proof of the investigation performed and must not be a disguised `needs investigation` deferral.
10. Reports contain no executable PowerShell/Python/batch/shell/JavaScript/C# or other automation package. Short tool commands may appear only as inline literals or non-executable receipt fields. Do not create scripts for report proof.
11. Report-only phase permissions: edit only this new report after leasing it immediately before the edit and release that lease immediately afterward. Do not edit this goal, target/support by-* files, any `-coverage-report.md`, generated/tracker/audit/registry/lifecycle file, another report, or IDA. Do not run validators, `execute_report`, lifecycle commands, IDA mutation/save, process management, or spawn subagents.
12. B005 never executes its own report. Only the Supervisor may run validator `execute_report`, mutate/save/promote IDA, edit coverage reports, or move/archive lifecycle artifacts.

Stop after the report and official `complete` status. Wait for fresh Supervisor Gate 1 review; if rejected, repair this same report additively rather than rewriting it from scratch.
