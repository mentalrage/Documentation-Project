# UID0000LM Removed Operational Material

- Source report: [0000LM-MSGHandler-file-source-quality.md](0000LM-MSGHandler-file-source-quality.md)
- Status: inert, non-authoritative preservation copy.
- Never execute this material and never submit this companion to `execute_report`.
- This companion receives no Gate, coverage, score, IDA, validator, or lifecycle credit.
## Verbatim Removed Blocks

The following blocks are preserved verbatim from the source report revision SHA256 `E555D4133678381D15A883553BF659B60C0BDE6F20E30E2C06A9AB2BEEB444DA`. Section labels outside the blocks are archival navigation only.

### Supervisor Active Recheck, original line 47


- Persistence-package recheck: verification reopens the exact saved `TX` as a new read-only authority rather than verifying a copy. Gate 2B first seals a fresh stable canonical receipt rather than requiring a permanent historical hash. Promotion and restoration each bind a unique closed candidate to the sealed resolved `CANONICAL` parent, reject reparse/symlink/hardlink/path-alias/alternate-stream and cross-volume states, write and flush the candidate through `FileStream` with `FileOptions.WriteThrough` plus `Flush(true)`, independently reopen/hash/close and exclusive-handle-prove it, then consume it through one four-argument `.NET File.Replace` call. No directory-handle `FlushFileBuffers` success is required or inferred. A successful call is followed by an exclusive write-through canonical-file flush, independent canonical reopen/hash/close, and exact-parent no-residue proof; unavailable, access-denied, policy-blocked, thrown, or ambiguous outcomes follow the explicit fail-closed branches below. Mutation, verifier, final, failed-state-classification, and restored-state sessions remain non-reusable roles with independent status/error, runtime, semantic, and physical receipts.

### Ledger C0000LM-059, original line 150


| C0000LM-059 | 0000LM | Supervisor Gate 2B executes the complete fresh-receipt isolated-TX/save/verifier/promotion/final-or-rollback/restore package through write-through `FileStream` plus four-argument `.NET File.Replace`, with explicit unavailable/access-denied/policy-blocked branches and no weaker fallback. | Exact proposed gate | Fresh `R_CANON0`; five-role attestation/save/readback contract; supported persistence and recovery protocol | this report `## IDA Rename / Type / Comment Recommendations` -> exact `CANONICAL`, `CANON_PARENT`, `BACKUP`, `TX`, unique candidate paths, fresh `R_CANON0/R_BACKUP0/R_TX0`, isolated `DB` mutation, sole `idb_save`, physical `TX` receipt, exact-TX read-only `DB_VERIFY`, route retirement, anti-alias/reparse/same-volume guards, write-through candidate creation plus `Flush(true)`, independent reopen/hash/close and exclusive-handle proof, one four-argument `.NET File.Replace`, post-replacement canonical-file flush, `DB_FINAL`, failed-state `DB_ROLLBACK`, BACKUP-derived replacement, restored `DB_RESTORE`, final canonical receipts, and exact-parent no-residue proof | supervisor-owned | pending-supervisor |

### Ledger package clarification, original line 157


- Gate 2B persistence clarification for the ledger/checklist twin: no ordinary source claim or allocation changes. The complete indivisible execution package is supervisor-owned and consists of a fresh stable `R_CANON0`, `DB` mutation/save, exact-saved-`TX` read-only `DB_VERIFY`, exact-parent/anti-alias guarded candidate creation through write-through `FileStream`, closed-candidate promotion through one four-argument `.NET File.Replace` call tied to the expected canonical receipt, post-replacement canonical-file write-through flush and independent canonical readback, read-only `DB_FINAL`, and, only on failed/ambiguous promoted state, distinct `DB_ROLLBACK` classification followed by the symmetric BACKUP-only candidate/`File.Replace` contract and distinct read-only `DB_RESTORE` verification. Every successful or restored terminal path includes an exact-parent no-residue scan. Directory-handle `FlushFileBuffers` is neither required nor credited; no role, receipt, candidate, or fallback primitive may be reused as another.

### IDA authority and canonical package dependencies, original lines 279-280


- Evidence date and authority boundary: the prestates below were read from session `9a58a2aa` on `2026-08-22`. They are dated evidence only. The supervisor must establish a fresh isolated transaction session `DB` from the exact `TX` path below and re-prove every literal prestate before mutation.
- Canonical evidence source is `E:\NTK\Resources\NexusTK\NexusTK.exe.i64`. The earlier 2026-08-22 receipt was 143,210,685 bytes with SHA256 `7F4B827D48F6A88131C0E63DD27E2BB637CA62001663BF3E7AB039BB7CFB0BF9`; the fresh report-repair read at `2026-08-22T01:36:46.9162392-04:00` was the same size with SHA256 `4C159ADDE049BFDFAB43C5DEB6222073A6636B46A9709E76D42A99E81945D7EF`. Both are dated evidence only and neither is a permanent Gate 2B baseline. The supervisor must seal a fresh stable gate-time `R_CANON0` before any copy or open; the canonical path is never the first mutation target.

### Exact isolated transaction and attestation contract, original lines 297-320


### Exact Isolated Transaction And Attestation Contract

- Canonical IDB path: `E:\NTK\Resources\NexusTK\NexusTK.exe.i64` (`CANONICAL`).
- Durable backup path: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\ida-backups\NexusTK.exe.uid0000LM-pre-gate2b.i64` (`BACKUP`).
- Isolated transaction path: `E:\NTK\GhidraBridge\source-3\project-documentation\tools\leaser\Agents\ida-backups\NexusTK.exe.uid0000LM-gate2b-tx.i64` (`TX`).
- Closed same-directory promotion candidate: `E:\NTK\Resources\NexusTK\NexusTK.exe.uid0000LM-promote-candidate.i64` (`PROMOTE_CANDIDATE`).
- Closed same-directory restoration candidate: `E:\NTK\Resources\NexusTK\NexusTK.exe.uid0000LM-restore-candidate.i64` (`RESTORE_CANDIDATE`).
- Exact-parent seal: before any copy or open, normalize the absolute `CANONICAL` path without following a user-supplied alias, resolve its containing directory to one physical volume/file-system identity, and seal that resolved identity as `CANON_PARENT`. The lexical parent and resolved parent of both candidate paths must equal `CANON_PARENT` exactly, and both candidates must be on the same physical volume as `CANONICAL`. A different parent identity despite similar text or casing is a hard stop.
- Anti-alias seal: `CANONICAL`, `BACKUP`, `TX`, each candidate, every parent component used by a candidate, and every operation source/destination must have no alternate-data-stream suffix beyond the drive designator and no symlink, junction, mount-point, or other reparse traversal. Each existing source/destination file and each created candidate must be a regular file with link count exactly one. Candidate creation must yield a new file identity distinct from `CANONICAL`, `BACKUP`, and `TX`; all source and destination identities must be pairwise distinct until the atomic operation consumes the selected candidate. Any reparse tag, path alias, ADS syntax, hardlink count/identity ambiguity, normalization escape, case-folded duplicate, or same-volume uncertainty hard-stops before copy or replacement.
- Exact-parent residue set: seal the two candidate basenames and every operation-specific temporary, swap, and replacement-backup basename before action. Scan only the non-recursive exact `CANON_PARENT`; before create-new, require the selected candidate and every operation-specific residue name absent, and never clean or overwrite a collision as part of this package. At each terminal success/restored-state boundary, require both named candidates and every sealed operation-residue name absent. An undeclared implementation-created name, residue match, or inability to enumerate the exact parent is a hard stop/incident.
- Fresh gate-time canonical receipt: before creating `BACKUP` or `TX`, close every canonical route that is not part of the read-only receipt, prove exclusive open/close, and obtain two consecutive complete closed-file reads separated by a bounded stability interval. Both must produce one nonzero byte count `S_CANON0` and SHA256 `H_CANON0` while normalized/resolved path, `CANON_PARENT` volume identity, canonical file identity, link count, ADS/reparse state, and last-write metadata remain exact. Seal that indivisible receipt as `R_CANON0`. A difference from a dated hash is recorded as dated drift and requires the supervisor to accept the fresh current starting artifact explicitly; any difference between the two gate-time reads or after sealing is a hard stop.
- Before opening: supervisor creates `BACKUP` from the exact closed `R_CANON0` canonical and creates `TX` only from the independently closed/rehashed `BACKUP`, using collision-failing create-new/no-overwrite copies. Independently reopen/read/hash/close all three and bind distinct regular-file identities `R_CANON0`, `R_BACKUP0`, and `R_TX0`; every byte count and hash must equal dynamic `S_CANON0/H_CANON0`, while identities remain pairwise distinct and all anti-alias constraints pass. Existing destinations, a copy/flush/close/reopen error, receipt mismatch, canonical drift, or identity/path violation is a no-open/no-mutation hard stop.
- `BACKUP` is a sealed physical restore source only: never open it as an IDB, never save to it, never promote it directly while a handle is open, and re-prove exact `R_BACKUP0` normalized/resolved path, file identity, `S_CANON0/H_CANON0`, no-reparse/no-ADS status, and non-alias relation at every save, verifier, promotion, rollback, restore, and final boundary. `PROMOTE_CANDIDATE` and `RESTORE_CANDIDATE` use collision-failing create-new/no-overwrite semantics only; neither may be overwritten or reused after a collision.
- Literal current-schema open request: public `idb_open` with `input_path:TX`, `mode:"force_headless"`, `run_auto_analysis:false`, `build_caches:true`, `init_hexrays:true`, and `preferred_session_id:"uid0000lm_gate2b_tx"`. Require `success:true`, returned `session.input_path` exactly `TX`, `is_analyzing:false`, and bind the returned session ID dynamically as `DB`; never substitute dated session `9a58a2aa`.
- Public tool inventory gate: each applicable fresh public MCP `tools/list` receipt must contain exactly one public tool named `runtime_attestation`, zero tools or aliases named `runtime/attest`, and exactly one `idb_save` with current request schema `database` required string plus optional `path` string and response schema `{ok:boolean,error?:string,path:string|null}`. It must also advertise `idb_open`, `declare_type`, `rename`, `set_type`, `set_address_comments`, `force_recompile`, `decompile`, `disasm`, `inspect_items`, `get_comments`, `stack_frame`, `get_bytes`, `xref_query`, `callees`, `entity_query`, `type_query`, and `type_inspect`. `set_address_comments` must have required `database` and `items`, with each item requiring `addr` and `comment`, and response `result` rows containing `addr` plus optional `error`. Duplicate/missing tools, an internal alias, a different comment endpoint schema, or a different save schema is a hard stop; the supervisor must never call internal RPC `runtime/attest` directly.
- Indivisible attestation package rule: every named runtime package below uses public `runtime_attestation` with the package's exact path and database role. The response top level must contain literal `schema_version:1`, literal `ok:true`, and a present `errors` array exactly equal to `[]`. The selected owning worker's nested attestation must independently contain literal `schema_version:1`, literal `ok:true`, and a present `errors` array exactly equal to `[]`. A missing, null, false, malformed, non-array, or nonempty required top-level or worker-attestation status/error field hard-stops the package.
- Listener schema rule: the current public listener object has no listener-local `ok` or `errors` fields, so no package requires or infers either field. Instead, every package fail-closed validates the complete listener object through exact role `supervisor_listener`, fresh nonempty attestation timestamp and nonce, exact endpoint host/port, complete process identity including PID/parent/generation/creation/executable/image/argv, exact source root and implementation-manifest SHA256, and the complete loaded-module identity array. Missing, null, malformed, stale, or changed required listener identity/process/source/manifest/module evidence hard-stops the package.
- Every package captures listener endpoint/PID/generation/creation/source root/implementation-manifest hash and its complete loaded-module identity array. In the one worker record owning the package database, require exactly one matching `supervisor_sessions` row with exact database/canonical path/route and capture its `redirector_pid` and `registered_pid`; capture the same worker's endpoint/PID/parent PID/generation/creation/source root/implementation-manifest hash and complete loaded-module identity array. The required chain is selected-row `redirector_pid == registered_pid`, and owning-worker `process.parent_pid == selected-row redirector_pid`; listener PID, routing PID, and worker PID remain distinct roles. Dated receipt `2026-08-22T01:51:16Z` proved `16684 == 16684` and worker parent `16684` with listener PID `3612` and worker PID `19476`, but every package must recompute its own relation.
- Package completeness is indivisible: the exact top-level and selected-worker-attestation status/errors, complete listener identity/process/module evidence, DB/path/route/process relation, role-specific module seals, exact-cardinality/schema `tools/list`, phase-appropriate semantic readback, and separate phase-appropriate physical receipts must all pass under one named checkpoint. No field, baseline, tools receipt, semantic result, physical receipt, or nonce from another package/session/phase may fill a missing result.
- Separate filesystem receipt `F0`, obtained independently of `runtime_attestation`, must record normalized/resolved path, physical identity, byte count, and SHA256 for canonical, `BACKUP`, and `TX` immediately after open. They must still equal their distinct sealed `R_CANON0/R_BACKUP0/R_TX0` identities and common dynamic `S_CANON0/H_CANON0` content. Runtime and filesystem receipts remain separately labeled at every later checkpoint.
- Role-specific module sealing: the first complete package for each distinct database role (`DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, `DB_RESTORE`) independently establishes that role's listener baseline and owning-worker baseline from implementation-manifest hash plus every loaded-module row's module name, resolved path, file size, timestamps, and SHA256. Later packages for that same role must equal its own accepted same-role baselines. Do not inherit a baseline from another database role and never require listener rows/hashes to equal worker rows/hashes. Fresh nonce/timestamp is required at every package; same-role PID/generation/route/module drift is a hard stop for that role.
- Mutation-route checkpoints against `DB/TX`: after open/before mutation (`M_OPEN/F0`); after S01; after R02/T02/C02; after R03/C03; after R04/C04; after R05/C05; after R06/C06; after R07/T07/C07; immediately before save; and immediately after save/readback. I03-I06 have no `set_type` stage. Each is a complete indivisible package. Verification, promotion, final, failed-state classification, restore, and restored-state packages are defined separately below.
- Database roles are non-substitutable: `DB` is the only mutation/save authority; `DB_VERIFY` reads exact saved `TX`; `DB_FINAL` reads successfully promoted canonical state; `DB_ROLLBACK` only classifies failed/ambiguous promoted canonical state; `DB_RESTORE` only verifies BACKUP-restored canonical state. Every role must be a newly returned session ID, distinct from all earlier IDs. No role may be renamed, reused, reopened under an old ID, supplied to another role's package, or granted a mutation/save endpoint outside its definition.


### Save, verifier, promotion, rollback, and final persistence, original lines 357-380


### Save, Verifier, Promotion, Rollback, And Final Persistence

- Before save: complete the fresh `M_PRE_SAVE` indivisible package, including exact-cardinality/schema `tools/list`, independent filesystem receipts for canonical/`BACKUP`/`TX`, and all complete semantic poststate checks. Canonical and `BACKUP` must retain exact `R_CANON0` and `R_BACKUP0` with common dynamic `S_CANON0/H_CANON0` content. Any mismatch stops without save.
- Save only isolated state with the exact current public call `idb_save({database:DB,path:TX})`. Its response must match `{ok:true,error absent-or-empty,path:TX}` after normalized-path comparison. Never save DB to canonical or `BACKUP`; a false `ok`, nonempty error, null/wrong path, additional implicit destination, or response-schema mismatch is a hard stop.
- After save, complete `M_POST_SAVE`: exact response readback, complete in-memory semantic poststate, and separate physical `TX` receipts. Obtain two consecutive stable closed-file size/SHA readings for `TX` separated by a bounded stability interval; both must equal one nonzero `S_TX1/H_TX1`. Independently re-receipt canonical as unchanged `R_CANON0` and `BACKUP` as unchanged `R_BACKUP0`, each still with `S_CANON0/H_CANON0` content. These physical receipts are not part of and must not be attributed to `idb_save` or `runtime_attestation`.
- Retire the mutation route after `M_POST_SAVE`: close `DB` through supervisor-owned public/process handling, then prove its session inactive, its selected supervisor-session route absent, its worker PID/port no longer owns or listens for `DB/TX`, and an OS-level exclusive-handle open/close check on `TX` succeeds without changing `S_TX1/H_TX1`. `DB` can never be reopened or reused after this boundary. Failure to prove retirement forbids verification and promotion.
- Exact-saved-TX verifier: do not create or copy a separate verifier database. Reopen the exact saved `TX` path with public `idb_open`, `mode:"force_headless"`, `run_auto_analysis:false`, `build_caches:true`, `init_hexrays:true`, and preferred session `uid0000lm_gate2b_verify`; bind the newly returned ID only as `DB_VERIFY`, require `DB_VERIFY != DB`, and grant it no mutation or save authority. A physical `TX` receipt immediately before open must equal `S_TX1/H_TX1`.
- `DB_VERIFY` must pass three fresh complete packages using its independently sealed listener/worker baselines: `V_POST_OPEN` immediately after open, `V_PRE_READ` immediately before semantic verification, and `V_POST_READ` after verification. The semantic verification independently proves the entire expected I01-I07 poststate, all four comment channels, exact T02/T07 zero frame deltas, exact I03-I06 preserved raw TInfo/frames, body hashes, ranges/items, xrefs/callees, decompile/disasm behavior, and all protected entities. Physical `TX` receipts before and after the read sequence must both equal `S_TX1/H_TX1`; any difference forbids promotion.
- Retire `DB_VERIFY` after `V_POST_READ`: close it, prove the session inactive, selected route absent, worker PID/port no longer owns or listens for `DB_VERIFY/TX`, and repeat the exclusive-handle open/close plus `S_TX1/H_TX1` receipt on `TX`. The verifier may never become mutation, save, final, rollback, or restore authority.
- Pre-promotion route/handle closure: prove both `DB` and `DB_VERIFY` IDs inactive in public session state, both selected redirector/registered routes absent from current attestation, both owning worker PID/port claims inactive, no active canonical or `TX` mutation route, and exclusive-handle open/close checks succeed for both exact `TX` and `CANONICAL`. Re-receipt `TX` as `S_TX1/H_TX1`, canonical as exact `R_CANON0`, and `BACKUP` as exact `R_BACKUP0`; reseal `CANON_PARENT`, the anti-alias/non-reparse/same-volume invariants, and the exact-parent residue set. Any active claim, open handle, receipt drift, identity drift, or residue hard-stops before candidate creation.
- Supported Windows persistence primitive: use only PowerShell-callable built-in .NET APIs, not a custom compiled harness. Candidate bytes are written through a `System.IO.FileStream` opened with `FileMode.CreateNew`, `FileAccess.Write`, `FileShare.None`, and `FileOptions.WriteThrough`; after the complete source copy, call `Flush(true)`, close the stream, and independently reopen/read/hash/close it. Atomic consumption is exactly the four-argument `System.IO.File.Replace(sourceFileName, destinationFileName, destinationBackupFileName:null, ignoreMetadataErrors:false)`. After a successful return, exclusively reopen `CANONICAL` through `FileStream` with `FileAccess.ReadWrite`, `FileShare.None`, and `FileOptions.WriteThrough`, call `Flush(true)`, close it, then independently reopen/read/hash/close it. This protocol does not call or infer directory-handle `FlushFileBuffers`; the supported atomic replacement result, post-replacement canonical-file write-through flush, physical receipt, and exact-parent no-residue scan are the persistence boundary.
- Deterministic primitive-failure branches: before candidate creation, reflectively require the four-argument `File.Replace` overload and `FileStream.Flush(bool)`; an unavailable/missing/unsupported API, constrained-language denial, application-control/policy block, or access-denied result stops before promotion and leaves `R_CANON0`, `R_BACKUP0`, and saved `TX` unchanged. During candidate creation/copy/`Flush(true)`/close/reopen, any unavailable, access-denied, policy-blocked, or I/O result closes handles, proves the exact package-owned candidate identity if it exists, removes only that identity, proves no residue, and stops without calling `File.Replace`; inability to prove/remove it is an incident, not cleanup authority. If `File.Replace` is invoked and returns/throws any error, or if the post-return canonical-file flush/reopen/receipt is unavailable, access-denied, policy-blocked, or mismatched, the destination state is failed/ambiguous: do not retry, weaken flags, invoke a custom harness, use `Move-Item`, copy over, delete-then-move, or claim persistence. Preserve `BACKUP` and `TX`, enter `DB_ROLLBACK` classification, and perform only the symmetric BACKUP-derived restoration protocol below. If that exact restore primitive or its required file flush is unavailable/access-denied/policy-blocked, stop as a supervisor incident with no restored-state claim.
- Durable promotion candidate: create `PROMOTE_CANDIDATE` once with the supported write-through `FileStream` protocol, collision-failing create-new/no-overwrite semantics, exact resolved parent `CANON_PARENT`, and source only the verified saved `TX`. Require a regular one-link non-reparse file with a new identity, complete `Flush(true)` success, and every source/candidate handle closed. Independently reopen the exact candidate path read-only, prove resolved path/parent/file identity/one-link/no-ADS/no-reparse state, read/hash the complete file to exact `S_TX1/H_TX1`, and close that handle. A separate exclusive-handle open/close and unchanged receipt must then pass. Never open the candidate as an IDB, substitute another source, require a directory flush, or continue from a partial/unflushed candidate.
- Guarded promotion and atomic consumption: immediately bind expected destination receipt `R_CANON_PROMOTE` to exact current `R_CANON0`, including normalized/resolved path, file identity, `S_CANON0/H_CANON0`, and anti-alias state; re-prove the closed candidate's independently reopened `S_TX1/H_TX1`, exact `R_BACKUP0`, exact saved `TX`, and no active role route. A non-recursive exact-parent scan must show `PROMOTE_CANDIDATE` as the sole selected package-created operation file while `RESTORE_CANDIDATE` and every other sealed operation-residue name remain absent. Invoke the supported four-argument `File.Replace(PROMOTE_CANDIDATE,CANONICAL,null,false)` exactly once only after the immediately preceding canonical receipt still equals `R_CANON_PROMOTE`. It must consume the closed same-volume candidate; any destination change or call failure follows the failed/ambiguous branch. No delete-then-move, in-place write, copy fallback, implicit alternate overwrite, cross-volume fallback, open candidate handle, second attempt, or unguarded destination is permitted.
- Promotion persistence and independent canonical receipt: after a successful `File.Replace` return, complete the exclusive write-through canonical-file `Flush(true)`/close step, then independently reopen exact `CANONICAL` read-only, prove its resolved path/parent, regular non-reparse/non-ADS one-link identity and no alias with `BACKUP`/`TX`, read/hash the complete file to exact `S_TX1/H_TX1`, and close it. Follow with canonical exclusive-handle open/close, unchanged `TX` `S_TX1/H_TX1`, unchanged `R_BACKUP0`, and a non-recursive exact-parent scan proving `PROMOTE_CANDIDATE`, `RESTORE_CANDIDATE`, and all sealed operation residue absent. This independent closed-handle receipt is `R_CANON_PROMOTED`; a call/identity/hash/close/file-flush/residue failure enters failed/ambiguous classification and cannot be called promotion success.
- Final canonical verification open contract: only after `R_CANON_PROMOTED` succeeds, call public `idb_open` exactly as `{input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,preferred_session_id:"uid0000lm_gate2b_final"}`. Require `success:true`, a fresh nonempty returned session ID, returned `session.input_path` normalized/resolved exactly to `CANONICAL`, and returned `is_analyzing:false`; bind the actual returned ID dynamically as `DB_FINAL`, even if the preferred ID is adjusted, and require it distinct from `DB`, `DB_VERIFY`, every retired/active session, and every later rollback/restore role. `DB_FINAL` is read-only and has no mutation/save authority. Immediately obtain fresh role-specific `runtime_attestation` and public-tool receipts, independently seal its listener/worker baselines, and complete `F_POST_OPEN`, `F_PRE_READ`, and `F_POST_READ` indivisible packages. Semantic readback must prove the complete expected I01-I07 poststate and every protected entity. Physical receipts before/after reads must prove canonical and exact saved `TX` both equal `S_TX1/H_TX1`, `BACKUP` remains exact `R_BACKUP0` with `S_CANON0/H_CANON0`, the exact-parent residue set remains absent, and no mutation/verifier route is active. Any open return/path/session/analyzing/attestation/readback mismatch enters failed/ambiguous classification.
- Successful final persistence requires retiring `DB_FINAL`, proving its route/PID/session inactive and canonical exclusively openable/closed, then independently reopening exact `CANONICAL`, reading/hash-receipting it as `S_TX1/H_TX1`, and closing it before the final package. The final post-retirement physical package requires canonical and `TX` exact `S_TX1/H_TX1`, `BACKUP` exact `R_BACKUP0` with `S_CANON0/H_CANON0`, both candidates and every sealed operation-residue name absent from the non-recursive exact-parent scan, and no active mutation/save authority. No directory flush is called or credited. Preserve sealed `BACKUP` and saved `TX` until the supervisor's archived-artifact audit completes.
- Failed/ambiguous promoted-state classification open contract: if guarded promotion, any canonical physical receipt, `DB_FINAL` open/package/readback, or final route retirement is failed or ambiguous, stop all mutation and first retire any `DB_FINAL` route. Independently receipt the exact closed current canonical path as `R_CANON_FAILED`, then call public `idb_open` exactly as `{input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,preferred_session_id:"uid0000lm_gate2b_rollback"}`. Require `success:true`, a fresh nonempty returned session ID, returned `session.input_path` normalized/resolved exactly to the same `R_CANON_FAILED` canonical path, and returned `is_analyzing:false`; bind the actual returned ID dynamically as `DB_ROLLBACK`, even if the preferred ID is adjusted, and require it distinct from `DB`, `DB_VERIFY`, `DB_FINAL`, every retired/active session, and future `DB_RESTORE`. Grant no mutation/save/restore authority. Immediately obtain fresh role-specific `runtime_attestation` and public-tool receipts, independently seal its listener/worker baselines, and complete `RB_POST_OPEN`, `RB_PRE_READ`, and `RB_POST_READ` packages. Record canonical physical receipts before/after and a complete failed-state semantic classification for every I01-I07 and protected item as exact expected poststate, exact original prestate, or explicit neither/ambiguous; separately classify whether `PROMOTE_CANDIDATE` was consumed, remains as the exact closed operation-created identity with `S_TX1/H_TX1`, or is missing/different/ambiguous. Do not infer, repair, adapt, save, or replace database state inside this role. Any open return/path/session/analyzing/attestation/readback mismatch is a supervisor incident and forbids restoration until classified safely.
- Before restoration, retire `DB_ROLLBACK` and prove its session/route/PID/port inactive plus canonical exclusive-handle closure. Re-prove exact `R_BACKUP0` normalized/resolved path, identity, `S_CANON0/H_CANON0`, no-reparse/no-ADS state, and non-alias relation; re-prove `TX` exact `S_TX1/H_TX1`; reseal `CANON_PARENT` and same-volume/anti-alias invariants. If and only if `DB_ROLLBACK` classified a surviving `PROMOTE_CANDIDATE` as the exact closed operation-created identity with `S_TX1/H_TX1`, prove exclusive access, remove that exact file without following an alias, and independently scan the exact parent to prove it absent; a missing/different/ambiguous or nonexclusive residue is an incident and forbids restoration. This is disposal of a proved package-owned failed-operation artifact, not collision cleanup or candidate reuse. Require the complete exact-parent no-residue prestate before creating `RESTORE_CANDIDATE`. Once failure/ambiguity entered `DB_ROLLBACK`, restoration is mandatory even if classification resembles the original state.
- Durable BACKUP-only restoration candidate: bind expected destination receipt `R_CANON_RESTORE` to the exact closed failed/ambiguous canonical path, identity, size/SHA256, and classification last proved by `DB_ROLLBACK`. Create `RESTORE_CANDIDATE` once through the same supported write-through `FileStream` protocol, collision-failing create-new/no-overwrite semantics, exact resolved parent `CANON_PARENT`, and source only exact `R_BACKUP0`; no failed canonical artifact, `TX`, promotion candidate, or other source is permitted. Require a new regular one-link non-reparse identity, complete `Flush(true)` success, and all source/candidate handles closed. Independently reopen exact `RESTORE_CANDIDATE` read-only, prove path/parent/identity/link/no-ADS/no-reparse state, read/hash it to dynamic `S_CANON0/H_CANON0`, close it, then prove exclusive-handle open/close and unchanged receipt. Any primitive failure follows the deterministic branches above.
- Guarded restoration and atomic consumption: re-prove the independently closed candidate, exact `R_BACKUP0`, exact saved `TX`, and no active role route. A non-recursive exact-parent scan must show `RESTORE_CANDIDATE` as the sole selected package-created operation file while `PROMOTE_CANDIDATE` and every other sealed operation-residue name remain absent. Invoke the same supported four-argument `File.Replace(RESTORE_CANDIDATE,CANONICAL,null,false)` exactly once only while canonical still equals the freshly sealed `R_CANON_RESTORE`. Any canonical drift or alias/handle/path/receipt/call mismatch follows the restoration-failure incident branch. No delete-then-move, in-place write, copy fallback, implicit alternate overwrite, cross-volume fallback, open BACKUP/candidate handle, second attempt, or unguarded destination is permitted.
- Restoration persistence and independent canonical receipt: after successful `File.Replace`, complete the exclusive write-through canonical-file `Flush(true)`/close step; independently reopen exact `CANONICAL` read-only, prove resolved path/parent/regular-file identity/no-reparse/no-ADS/non-alias state, read/hash it to dynamic `S_CANON0/H_CANON0`, and close it. Then prove canonical exclusive-handle open/close, unchanged `R_BACKUP0`, unchanged `TX` `S_TX1/H_TX1`, and an exact-parent scan with both candidates and every sealed operation-residue name absent. Bind this closed-handle receipt as `R_CANON_RESTORED`; any mismatch is a supervisor incident and forbids restored-state success.
- Restored-canonical verification open contract: only after `R_CANON_RESTORED` succeeds, call public `idb_open` exactly as `{input_path:CANONICAL,mode:"force_headless",run_auto_analysis:false,build_caches:true,init_hexrays:true,preferred_session_id:"uid0000lm_gate2b_restore"}`. Require `success:true`, a fresh nonempty returned session ID, returned `session.input_path` normalized/resolved exactly to the restored `CANONICAL`, and returned `is_analyzing:false`; bind the actual returned ID dynamically as `DB_RESTORE`, even if the preferred ID is adjusted, and require it distinct from `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and every retired/active session, with no mutation/save authority. Immediately obtain fresh role-specific `runtime_attestation` and public-tool receipts, independently seal its listener/worker baselines, and complete `RS_POST_OPEN`, `RS_PRE_READ`, and `RS_POST_READ` packages. Complete semantic readback must prove the original state: absent local `MSGHandler`, six raw names/TInfo values, four blank comment channels per function, exact body hashes/ranges/items/full frames/xrefs/callees, and every protected entity. Retire `DB_RESTORE`, prove route/PID/session and exclusive-handle closure, independently reopen/hash/close exact canonical once more, and repeat exact `R_CANON_RESTORED/R_BACKUP0` plus saved-TX physical receipts and exact-parent no-residue scan. Any open return/path/session/analyzing/attestation/readback, restore, or restored-state mismatch is a supervisor incident and no success may be claimed.
- Absolute role separation: `DB`, `DB_VERIFY`, `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE` are distinct newly returned IDs and mutually exclusive authorities. A receipt, role baseline, nonce, tools list, semantic result, physical result, PID, route, or closed-handle proof from one cannot satisfy another. None of the four read-only roles may call mutation or save endpoints; `DB` may not be reused after retirement. No canonical save, promotion, rollback, restore, or lifecycle success is authorized from this report alone.

### Operational hard stops, original line 389


- Hard stops: prestate mismatch; unstable or drifting fresh `R_CANON0`; candidate collision; unresolved/nonmatching `CANON_PARENT`; cross-volume path; reparse/symlink/junction/mount-point/ADS/path-alias/hardlink ambiguity; non-unique or non-create-new candidate; undeclared or surviving exact-parent operation residue; unavailable/access-denied/policy-blocked write-through `FileStream`, `Flush(true)`, or four-argument `File.Replace`; missing candidate or post-replacement canonical-file flush; failed independent reopen/full-hash/close or exclusive-handle proof; destination drift from `R_CANON_PROMOTE` or `R_CANON_RESTORE`; `File.Replace` error/throw/ambiguity; any custom-harness/directory-flush/weaker/non-atomic fallback; failed independent canonical readback; missing/null/false/malformed top-level or selected-worker-attestation schema/`ok:true`; missing/null/non-array/nonempty top-level or selected-worker-attestation errors; missing/null/malformed/stale listener identity/process/source/manifest/module evidence; public tool cardinality/schema mismatch; advertised internal `runtime/attest`; same-role listener or worker baseline drift; cross-role baseline/receipt reuse; session/path/parent/route mismatch; active retired route/PID/port/session; exclusive-handle failure; independently receipted filesystem hash/size mismatch; parser coercion; item/range/body/frame/xref/callee delta; protected-entity delta; nonblank preexisting comment; cache/decompile failure; partial mutation; exact `idb_save({database:DB,path:TX})` response mismatch; saved-TX stability mismatch; verifier mismatch; open/nonmatching promotion or restore candidate; canonical/backup drift; failed-state classification failure; BACKUP-only restore violation; restored-state mismatch; final read-only persistence mismatch; or any database-role reuse/substitution.

### Open-question persistence implementation, original line 493


- Windows persistence primitive: resolved to PowerShell-callable built-in `FileStream` with `FileOptions.WriteThrough`/`Flush(true)` plus one four-argument `.NET File.Replace` call per promotion or restoration attempt. Directory-handle `FlushFileBuffers` is unsupported here and is not required or credited. The exact unavailable/access-denied/policy-blocked branches stop before replacement when possible and classify/restore without retry or weaker fallback after any invoked/ambiguous replacement.

### Open-question canonical package dependency, original line 492

- Canonical-baseline drift: resolved by treating both `7F4B...BF9` and current dated `4C159...D7EF` as historical receipts only. Gate 2B seals a fresh stable `R_CANON0` from two complete closed-file reads and binds all backup, transaction, promotion-guard, and restoration expectations to its dynamic `S_CANON0/H_CANON0`; no permanent hash is accepted.

### Follow-up package instruction, original line 514


- Supervisor Gate 2B: execute Section 21's indivisible package exactly: seal fresh stable `R_CANON0/R_BACKUP0/R_TX0`; obtain literal top-level and selected-worker-attestation schema/`ok:true`/empty-errors receipts plus complete listener identity/process/module validation; perform isolated `DB` mutation and sole save; reopen exact saved `TX` as read-only `DB_VERIFY`; retire mutation/verifier routes; enforce exact `CANON_PARENT` and anti-alias/reparse/same-volume gates; create each unique candidate through write-through `FileStream` plus `Flush(true)`; independently reopen/hash/close and exclusive-handle-prove it; invoke one four-argument `.NET File.Replace` with no fallback; complete post-replacement canonical-file write-through flush, independent canonical reopen/hash/close, and exact-parent no-residue proof; then use literal current-schema opens with `run_auto_analysis:false`, unique preferred IDs, exact returned path/analyzing checks, fresh dynamic session binding, role-specific attestation/readback/retirement, and no reuse for `DB_FINAL`, failed-canonical `DB_ROLLBACK`, and restored-canonical `DB_RESTORE`. Failed/ambiguous state uses only the symmetric BACKUP-derived candidate/`File.Replace` restoration path. Apply the explicit unavailable/access-denied/policy-blocked branches, preserve no T03-T06 call, never require directory-handle flushing, and never reuse a database role, baseline, receipt, or candidate.

### Changed-files package summary, original lines 548-549


- Gate 1 repair scope: failed SHA256 revisions were additively repaired in this same report; valid research and original C0000LM-001 through C0000LM-040 identities/order were preserved, C010's false five-gap count was corrected, and C041-C051 were appended as itemized six-gap and core RTTI claims with exact checklist twins. C052-C063 now provide stable unchecked eight-field twins for every I01-I07 supervisor action/protection group, the repaired persistence package, manual coverage, Gate 2A, execution/archive, and post-move audit. Generated/lifecycle prose distinguishes historical `3/2/1`/empty-emitter evidence from current mutable `3/3/0` output. The 2026-08-22 endpoint/frame repair retains one-channel `set_address_comments`, no T03-T06 writes, exact zero-delta T02/T07 frame contracts, and the selected-session route relation. The persistence contract now seals a fresh gate-time canonical receipt, uses supported write-through `FileStream` plus four-argument `.NET File.Replace` with explicit failure branches, requires no unsupported directory-handle flush, and preserves exact-saved-`TX`, exact-parent/anti-alias/atomic/no-residue, BACKUP-only restore, role-retirement, and no-reuse gates for `DB_FINAL`, `DB_ROLLBACK`, and `DB_RESTORE`.
- Current report-only repair changed no ordinary by-* page, generated/coverage/tracker/audit/catalog/validator/lifecycle state, or IDA database. It changed only this report's canonical receipt protocol, Windows persistence contract, dependent gate prose, and twelve supervisor-owned ledger/checklist twins.

### Checklist twin C0000LM-059, original line 618


| [ ] | C0000LM-059 | 0000LM | Supervisor Gate 2B executes the complete fresh-receipt isolated-TX/save/verifier/promotion/final-or-rollback/restore package through write-through `FileStream` plus four-argument `.NET File.Replace`, with explicit unavailable/access-denied/policy-blocked branches and no weaker fallback. | Exact proposed gate | Fresh `R_CANON0`; five-role attestation/save/readback contract; supported persistence and recovery protocol | this report `## IDA Rename / Type / Comment Recommendations` -> exact `CANONICAL`, `CANON_PARENT`, `BACKUP`, `TX`, unique candidate paths, fresh `R_CANON0/R_BACKUP0/R_TX0`, isolated `DB` mutation, sole `idb_save`, physical `TX` receipt, exact-TX read-only `DB_VERIFY`, route retirement, anti-alias/reparse/same-volume guards, write-through candidate creation plus `Flush(true)`, independent reopen/hash/close and exclusive-handle proof, one four-argument `.NET File.Replace`, post-replacement canonical-file flush, `DB_FINAL`, failed-state `DB_ROLLBACK`, BACKUP-derived replacement, restored `DB_RESTORE`, final canonical receipts, and exact-parent no-residue proof | supervisor-owned | pending-supervisor |

### Separate supervisor package gate, original line 628


- [ ] Supervisor Gate 2B seals fresh stable `R_CANON0/R_BACKUP0/R_TX0`; requires complete indivisible top-level and selected-worker-attestation schema/`ok:true`/empty-errors plus listener identity/process/module packages; performs I01-I07 with no T03-T06 call in isolated `DB`; saves only exact `TX`; retires `DB`; reopens exact saved `TX` as distinct read-only `DB_VERIFY`; retires verifier and mutation routes; and enforces exact `CANON_PARENT`, anti-alias/reparse/same-volume, create-new collision failure, write-through `FileStream`/`Flush(true)`, independent candidate receipt, one four-argument `.NET File.Replace`, post-replacement canonical-file write-through flush, canonical receipt, and exact-parent no-residue contracts. `DB_FINAL`, failed-canonical `DB_ROLLBACK`, and restored-canonical `DB_RESTORE` use literal current opens, fresh distinct sessions and packages, BACKUP-only symmetric restoration, and no role/baseline/receipt/candidate reuse. Directory-handle flushing and every weaker/custom fallback are forbidden; unavailable/access-denied/policy-blocked outcomes follow Section 21's fail-closed branches.
